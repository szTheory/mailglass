# v1.14 release — PAUSED at publish gate (EEF dep-security advisory wave)

**Status:** PAUSED 2026-06-29. PR #97 MERGED, release tags created, **NOTHING published to Hex.** No irreversibility. Working tree clean.

## Where the release got to (Phase 124, plan 124-01)

- Waves 0–2 complete: rebased the 126-commit v1.14 body onto origin (#88), landed `fix(inbound): re-pin == 1.10.0`, ff-pushed, fixed everything the first real CI run surfaced (see below), maintainer approved, admin-merged PR #97 (self-approval blocked → `--admin` per the documented stall path).
- **Merge commit:** `f0c84ec0 chore: release main (#97)` on origin/main. ci.yml GREEN on it (all required lanes; only advisory Demo Browser Evidence red = Docker flake).
- **Release tags created** (all on `f0c84ec0`): `mailglass-v1.10.0`, `mailglass_admin-v1.10.0`, `mailglass_inbound-v1.5.2` + GitHub Releases.
- **Publish BLOCKED:** all 3 racing publish-hex.yml runs failed at `prepublish-summary` → `mix mailglass.publish.check` `[conflict] run hex.audit` "Delivery blocked" on the advisory wave below. publish-core/inbound/admin all SKIPPED. **Nothing on Hex.**

## The blocker: coordinated EEF advisory wave (fresh CVE-2026-* batch)

`hex.audit` (CI/publish runner has fresh advisory data; local hex v2.4.2 does NOT — that's why Wave-0 publish.check passed clean) flags 7 deps in the package locks. **These are in the dev `mix.lock`, which Hex does NOT ship — adopters resolve their own (patched) deps from the version constraints. So this is a publish-gate / dev-lock hygiene blocker, NOT an adopter-facing vulnerability.** But `publish.check` correctly refuses to ship with known-vulnerable locks.

| Dep | Locked (vuln) | Latest | Direct? | Notes |
|---|---|---|---|---|
| cowlib | 2.16.0 | 2.17.1 | transitive | HIGH×2, MED×2, LOW (via cowboy/plug_cowboy) |
| cowboy | 2.14.2 | 2.16.1 | transitive | HIGH (CVE-2026-8466) |
| postgrex | 0.22.0 | 1.0.0 | **DIRECT** | HIGH SQLi (CVE-2026-32687). **MAJOR bump 0.x→1.0 — check breaking changes + Ecto compat** |
| phoenix | 1.8.5 | 1.8.8 | **DIRECT** | HIGH (CVE-2026-32689). Safe patch bump |
| mint | 1.7.1 | 1.9.0 | transitive | HIGH×2, MED, LOW — all fixed @1.9.0 (OSV). Core already @1.9.0 (clean); only admin/inbound at 1.7.1 |
| req | 0.5.17 | 0.6.2 | transitive | HIGH, LOW. Minor bump 0.5→0.6 |
| decimal | 2.3.0 | 3.1.1 | transitive | MED (CVE-2026-32686). **MAJOR 2→3 — verify the patched 2.x exists OR that 3.x satisfies constraints** |

Run `gh run view --job <prepublish-summary job> --log` on publish-hex run `28409111983` for the full advisory text, or re-derive: `curl -s https://api.osv.dev/v1/query -d '{"package":{"name":"<pkg>","ecosystem":"Hex"},"version":"<locked>"}'` for exact fixed-version ranges (prefer the minimal patched version per minor line, like plug 1.19.3, over jumping majors when a same-minor/major patch exists).

## Dep-security pass — plan

1. For each flagged dep, find the **minimal patched version** (OSV `fixed` events) that satisfies the existing public constraints. Prefer same-line patches; only take majors (postgrex 1.0, decimal 3.x) if no patched lower version exists, and then verify constraint/breaking-change impact.
2. Bump in all 3 package locks (mix.lock, mailglass_admin/mix.lock, mailglass_inbound/mix.lock). **Watch for transitive drift** — restore any unrelated lines (the plug fix earlier accidentally dragged floki/premailex in inbound; had to restore). Keep diffs to the flagged deps + their forced transitive co-deps only.
3. **DO NOT touch** reference/host_app + reference/demo_app locks (frozen baselines; coordinated 5-file change, out of scope).
4. Verify: `mix compile --warnings-as-errors` (note: local is Elixir 1.19.5/OTP 28 — operator_live.ex:505 `selected_delivery={nil}` warns locally but is green on CI's Elixir 1.18/OTP 27, pre-existing), Support Contract Core/Admin, Inbound Test, and the operator browser gate (`cd mailglass_admin && npx playwright test --config=playwright.config.cjs --workers=1`). **phoenix + postgrex bumps are the regression risk** — run the full suites.
5. Confirm `mix mailglass.publish.check --package {mailglass,mailglass_admin,mailglass_inbound}` would pass on a fresh-advisory runner (local hex won't show it — rely on CI / a pushed run).

## Release recovery (after deps are clean on main)

Nothing published, so keep **1.10.0/1.10.0/1.5.2**. After the dep-fix commit(s) land on origin/main (call it SHA `g`) with green ci.yml on `g`:
- **Recommended:** move the 3 release tags to `g` (`git tag -f <tag> g && git push -f origin <tag>` for each), then re-dispatch the publish fan-out per tag: `gh workflow run publish-hex.yml --ref <tag> --field tag=<tag> --field dry_run=false --field package=all`. gate-ci-green will find the green ci.yml on `g`. (The GitHub Release objects keep pointing at f0c84ec0 — cosmetic SHA mismatch; the Hex tarball builds from the tag tree = correct.)
- **Fallback:** if the dep bumps are deemed to warrant a version note, let RP cut 1.10.1/1.10.1/1.5.3 (fix(deps): commit → new RP PR → repeat the fix(inbound) re-pin == 1.10.1 dance). Leaves dangling f0c84ec0 1.10.0 tags/releases.
- Then resume Phase 124 plan 124-01 from **Wave 3** (confirm Hex live) → Wave 4 (consumer + post-publish smoke) → Wave 5 (milestone audit, archive, `git tag v1.14`).

## Fix-forward already landed on origin/main (pre-merge, all CI-verified, folded into 1.10.0/1.5.2 CHANGELOGs)

- `fc17fdfd` fix(deps): plug 1.19.2→1.19.3 (CVE-2026-54892) — the FIRST advisory found
- `c8b19960` fix(admin): 2 real a11y bugs (preview backdrop aria-pressed; inbound reveal ARIA disclosure)
- `8a10e584` fix(admin): 3 stale operator-browser-gate specs realigned to the v1.14 redesign
- `8bfb7ac5` fix(admin): re-redact button px-md→px-sm (CI-only gallery overflow, RATCHET-02)
- `6cae5a30` chore: refresh publish allowlist snapshots 1.7.0→1.9.0
- Debug session archived: `.planning/debug/resolved/operator-browser-gate-v114.md`

## Root lesson

The v1.14 body was built entirely on local main and never pushed; phase verification deferred browser/persona re-shoots ("demo unrunnable in-env", cached evidence). The first real CI run surfaced 7 browser regressions + the plug CVE; the publish gate then surfaced the full EEF advisory wave. Local hex (v2.4.2) cannot see fresh advisories, so dep-security blockers only appear on CI/publish runners — always validate hex.audit on a runner before trusting a clean local publish.check.
