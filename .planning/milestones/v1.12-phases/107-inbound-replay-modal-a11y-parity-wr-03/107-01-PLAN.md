---
phase: 107-inbound-replay-modal-a11y-parity-wr-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Pressing Escape while the inbound replay modal is open closes it via the existing close_replay handler"
    - "Opening the inbound replay modal moves keyboard focus into the modal; closing it returns focus to the trigger button"
    - "The inbound dialog div and the replay trigger button carry real, resolvable id attributes (inbound-replay-modal / inbound-replay-open-btn)"
    - "A structural Playwright assertion proves role=dialog/aria-modal=true, the Escape keydown attributes, and Escape-closes behavior on the inbound modal"
    - "The committed priv/static/ admin CSS bundle is unchanged after rebuild (or, if it changed, the regenerated bundle is committed — never hand-edited)"
    - "No new phx-hook and no new JS asset are introduced; the operator modal is left untouched"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex"
      provides: "Escape-to-close keydown attributes + real id on the inbound dialog div"
      contains: 'phx-window-keydown="close_replay"'
    - path: "mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex"
      provides: "Real id on the replay trigger button (return-focus target)"
      contains: 'id="inbound-replay-open-btn"'
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Focus-management sibling span (phx-mounted focus-in / phx-remove focus-return)"
      contains: "JS.focus_first(to: \"#inbound-replay-modal\")"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Structural focus-management-parity assertion in the inbound describe block"
      contains: "inbound-replay-open"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex (focus span)"
      to: "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (#inbound-replay-modal)"
      via: "JS.focus_first(to: \"#inbound-replay-modal\") resolves the dialog id"
      pattern: "focus_first\\(to: \"#inbound-replay-modal\"\\)"
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex (focus span phx-remove)"
      to: "mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex (#inbound-replay-open-btn)"
      via: "JS.focus(to: \"#inbound-replay-open-btn\") resolves the trigger id"
      pattern: "JS.focus\\(to: \"#inbound-replay-open-btn\"\\)"
    - from: "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (phx-window-keydown)"
      to: "mailglass_admin/lib/mailglass_admin/inbound_live.ex (close_replay handler, lines ~179-180)"
      via: "Escape keydown dispatches the existing close_replay live event"
      pattern: "phx-window-keydown=\"close_replay\""
---

<objective>
Bring the admin **inbound** replay modal to operator-modal accessibility parity:
focus-on-open + return-focus-on-close + Escape-to-close — matching the existing
operator replay modal's *intent*, using pure Phoenix LiveView JS commands (no new
`phx-hook`, no new JS asset). Add a structural Playwright assertion and confirm the
committed `priv/static/` bundle stays clean.

Purpose: Closes requirement A11Y-01 (folded-in ex-v1.11 WR-03). This is a **parity
port**, not net-new work — the operator modal already demonstrates the target
behavior; the inbound modal must match it. Scope is **inbound only** — do NOT touch
the operator modal.

Output: Escape-closes + focus-management wiring on the inbound modal, two new real
`id` attributes so the focus selectors resolve, a structural focus-management-parity
Playwright test, and a verified-clean (or committed-if-changed) admin CSS bundle.

Honest framing (D-06/D-07): deliver and describe exactly focus-on-open +
return-focus-on-close + Escape-to-close. This is "focus management parity with the
operator modal," NEVER WCAG dialog conformance or full focus containment. Do NOT
implement a cyclic Tab focus trap (that needs custom JS, against posture).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-inbound-replay-modal-a11y-parity-wr-03/107-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Operator reference (the pattern to mirror) and inbound targets. Use directly — no further exploration needed. -->

REFERENCE — operator/replay_modal.ex dialog div (lines 23-32): carries
`data-testid="operator-replay-modal"`, `role="dialog"`, `aria-modal="true"`,
`aria-labelledby="replay-modal-title"`, `phx-key="Escape"`,
`phx-window-keydown="close_replay"`. NOTE the latent bug (D-04): the dialog div has
NO `id=` attribute, only the `data-testid`, so the operator focus selector below is a
no-op. Do NOT reproduce that bug on inbound; do NOT fix it on operator (D-05).

REFERENCE — operator_live.ex focus span (lines 499-503):
`<span :if={@replay_modal_open?} phx-mounted={JS.focus_first(to: "#operator-replay-modal")} phx-remove={JS.focus(to: "#replay-open-btn")} />`
placed immediately before `<ReplayModal.replay_modal ... />`.

TARGET — inbound/replay_modal.ex dialog div (lines 27-33): currently
`data-testid="inbound-replay-modal"`, `role="dialog"`, `aria-modal="true"`, plus the
`phx-remove` hide transition on line 32. It is MISSING `phx-key`,
`phx-window-keydown`, and a real `id`.

TARGET — inbound_live.ex: `close_replay` handler ALREADY exists (lines ~179-180,
`assign(socket, ...)`/`close_replay_modal(socket)`) — Escape needs no new event.
`alias Phoenix.LiveView.JS` and `alias MailglassAdmin.Inbound.ReplayModal` are
already in scope (inbound_live.ex lines 33, 36). The modal call is at line ~416:
`<ReplayModal.replay_modal open?={@replay_modal_open?} record={selected_record_struct(@detail)} />`.

TARGET — inbound/detail_header.ex trigger button (lines 86-94): currently
`phx-click="open_replay"`, `data-testid="inbound-replay-open"`,
`disabled={replay_disabled?(@outcome)}`. It is MISSING a real `id`. Replay is
disabled only for `:no_match` records (`replay_disabled?(:no_match) -> true`).

PLAYWRIGHT IDIOM — e2e/structural.spec.js inbound describe block ("inbound state
coverage, responsive grid, and contrast", opens ~line 483, closes ~line 634). Helper
`openInbound(page, query = ...)` logs in and navigates to `/ops/mail/inbound`. Rows
carry `data-testid="inbound-record-row"`. A no-match row is selected via
`.filter({ has: page.locator(".badge-warning", { hasText: "No match" }) })`. The
browser seed (operator_fixtures.ex `seed_inbound_matrix!`) includes a MATCHED
(`:accept`) record `support@browser-scenario.example` whose replay button is ENABLED
— select a NON-no-match row to reach an openable modal. Assertions use
`getByTestId` / `getByRole` / `getAttribute` / `page.keyboard.press`, NOT pixel
snapshots.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Escape-to-close + real ids on the inbound modal and trigger button</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (TARGET dialog div, lines 27-33)
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (REFERENCE Escape wiring, lines 28-29; note the latent missing-id at lines 23-32 — D-04, do NOT reproduce)
    - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex (TARGET trigger button, lines 86-94)
  </read_first>
  <action>
    In inbound/replay_modal.ex, on the inner dialog `<div>` (the one currently
    carrying `data-testid="inbound-replay-modal"`, `role="dialog"`,
    `aria-modal="true"` at lines 27-33), add three attributes, mirroring operator
    replay_modal.ex lines 28-29 (D-01): `id="inbound-replay-modal"`,
    `phx-key="Escape"`, and `phx-window-keydown="close_replay"`. Keep the existing
    `data-testid`, `role`, `aria-modal`, `class`, and `phx-remove` attributes intact.
    The `close_replay` handler already exists on inbound_live.ex (lines ~179-180), so
    Escape wires to a live handler with no new event. Adding the real `id` (D-03) is
    required so the Task 2 focus-first selector resolves; this is the one intentional
    divergence from a verbatim operator copy (D-04 — operator's focus-first targets a
    non-existent id). Do NOT add a focus-trap hook or any client JS.

    In inbound/detail_header.ex, on the replay trigger `<button>` (lines 86-94,
    currently `phx-click="open_replay"` + `data-testid="inbound-replay-open"` +
    `disabled={...}`), add `id="inbound-replay-open-btn"` so the Task 2 `phx-remove`
    return-focus selector resolves (D-03). Keep every existing attribute. This mirrors
    the operator-side `id="replay-open-btn"` return target.

    Do NOT touch operator/replay_modal.ex or operator_live.ex (D-05 — scope is inbound
    only; the operator latent-id bug is explicitly deferred).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q 'id="inbound-replay-modal"' lib/mailglass_admin/inbound/replay_modal.ex && grep -q 'phx-window-keydown="close_replay"' lib/mailglass_admin/inbound/replay_modal.ex && grep -q 'phx-key="Escape"' lib/mailglass_admin/inbound/replay_modal.ex && grep -q 'id="inbound-replay-open-btn"' lib/mailglass_admin/inbound/detail_header.ex && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - inbound/replay_modal.ex contains `id="inbound-replay-modal"`, `phx-key="Escape"`, and `phx-window-keydown="close_replay"` on the dialog div
    - inbound/replay_modal.ex still contains its original `data-testid="inbound-replay-modal"`, `role="dialog"`, and `aria-modal="true"`
    - inbound/detail_header.ex contains `id="inbound-replay-open-btn"` on the replay trigger button (alongside the unchanged `data-testid="inbound-replay-open"`)
    - operator/replay_modal.ex and operator_live.ex are byte-identical to pre-task (`git diff --quiet mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex mailglass_admin/lib/mailglass_admin/operator_live.ex` exits 0)
    - `cd mailglass_admin && mix compile --warnings-as-errors` exits 0
    - No `phx-hook` attribute and no new file under `mailglass_admin/assets/js/` introduced
  </acceptance_criteria>
  <done>The inbound modal closes on Escape via the existing close_replay handler, and both the dialog div and trigger button carry real resolvable ids. Operator modal untouched. Compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the focus-management sibling span to inbound_live.ex</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (modal mount ~line 416; aliases at lines 33 + 36 confirm JS and ReplayModal in scope; close_replay handler lines ~179-180)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (REFERENCE focus span, lines 499-503)
  </read_first>
  <action>
    In inbound_live.ex, immediately BEFORE the
    `<ReplayModal.replay_modal open?={@replay_modal_open?} record={selected_record_struct(@detail)} />`
    call (~line 416), insert the focus-management sibling span, mirroring
    operator_live.ex lines 499-503 (D-01), but targeting the inbound ids:
    `<span :if={@replay_modal_open?} phx-mounted={JS.focus_first(to: "#inbound-replay-modal")} phx-remove={JS.focus(to: "#inbound-replay-open-btn")} />`.
    On open, `phx-mounted` moves focus into the modal (resolving the real
    `id="inbound-replay-modal"` added in Task 1); on close, `phx-remove` returns focus
    to the trigger (`id="inbound-replay-open-btn"`). `JS` and `ReplayModal` are already
    aliased (lines 33, 36) — add no imports. Optionally precede the span with a brief
    HEEx comment naming it as focus-management parity (not WCAG / not focus trap, per
    D-07). Do NOT add any client JS, phx-hook, or Tab-wrapping logic (D-06).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q 'JS.focus_first(to: "#inbound-replay-modal")' lib/mailglass_admin/inbound_live.ex && grep -q 'JS.focus(to: "#inbound-replay-open-btn")' lib/mailglass_admin/inbound_live.ex && mix compile --warnings-as-errors && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - inbound_live.ex contains a `<span>` with `:if={@replay_modal_open?}`, `phx-mounted={JS.focus_first(to: "#inbound-replay-modal")}`, and `phx-remove={JS.focus(to: "#inbound-replay-open-btn")}`
    - The span appears immediately before the `<ReplayModal.replay_modal ... />` call (~line 416)
    - No new `alias`/`import` lines added (JS + ReplayModal already in scope)
    - No `phx-hook` and no Tab-cycle/focus-trap client JS introduced
    - `cd mailglass_admin && mix compile --warnings-as-errors` exits 0
    - `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` passes
  </acceptance_criteria>
  <done>Opening the inbound modal moves focus into it; closing returns focus to the trigger button. Pure LiveView JS commands, no custom JS asset. Existing inbound_live tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Structural Playwright focus-management-parity assertion + bundle-clean verify</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (inbound describe block ~lines 483-634; `openInbound` helper ~line 48; `noMatchRow` helper ~line 242; getByTestId/getAttribute/getByRole idiom)
    - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (the attribute contract the test asserts)
    - mailglass_admin/test/support/operator_fixtures.ex (seed_inbound_matrix! ~line 306 — confirms a matched/`:accept` replayable row `support@browser-scenario.example` exists in the browser scenario)
    - mailglass_admin/mix.exs (`:tailwind` dep line 99; `verify.preview` alias lines 183-188 — the rebuild + `git diff --exit-code priv/static/` steps)
  </read_first>
  <action>
    Add ONE new structural test inside the existing inbound describe block ("inbound
    state coverage, responsive grid, and contrast", ~line 483, before its closing
    `});` at ~line 634), per D-10/D-11. Name it for focus-management parity, NOT WCAG
    (e.g. `"Inbound: replay modal has focus-management parity with the operator modal
    (role/aria + Escape-to-close)"`). Use the repo's structural idiom
    (getByTestId/getByRole/getAttribute/page.keyboard), NOT pixel snapshots.

    Body: call `await openInbound(page);` (default tenant). Select a REPLAYABLE
    (non-no-match) inbound row — the matched `:accept` row — and click it: e.g.
    `page.getByTestId("inbound-record-row").filter({ hasNot: page.locator(".badge-warning", { hasText: "No match" }) }).first()`,
    then `.click()`; await `page.waitForURL(/inbound_id=/)`. Click the trigger via
    `page.getByTestId("inbound-replay-open").click()`. Then assert the DOM contract on
    `page.getByTestId("inbound-replay-modal")`: `getAttribute("role")` === `"dialog"`,
    `getAttribute("aria-modal")` === `"true"`,
    `getAttribute("phx-window-keydown")` === `"close_replay"`, and
    `getAttribute("phx-key")` === `"Escape"`. Then assert Escape-closes:
    `await page.keyboard.press("Escape")` followed by
    `await expect(page.getByTestId("inbound-replay-modal")).toHaveCount(0)` (modal
    hidden / removed). Optionally also assert the focus span target id resolves
    (`page.locator("#inbound-replay-modal")` present) as a nice-to-have. Keep the test
    description and any comments framed as focus-management parity (D-07), never WCAG
    conformance or focus containment.

    Then rebuild the admin CSS bundle and confirm it is unchanged: run
    `cd mailglass_admin && mix mailglass_admin.assets.build` and then
    `git diff --exit-code priv/static/`. The change is attributes-only with no new
    Tailwind classes, so the bundle SHOULD be unchanged (D-08). If `git diff` shows a
    change, COMMIT the regenerated `priv/static/` bundle alongside the source — never
    hand-edit it (D-09; CI gates it via `git diff --exit-code`).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix mailglass_admin.assets.build && git diff --exit-code priv/static/ && npx playwright test e2e/structural.spec.js -g "focus-management parity"</automated>
  </verify>
  <acceptance_criteria>
    - e2e/structural.spec.js contains a new test inside the inbound describe block whose description names "focus management parity" (or "focus-management parity") and does NOT use the words "WCAG" or "focus trap" / "focus containment"
    - The test opens the modal via `getByTestId("inbound-replay-open").click()` after selecting a non-no-match (replayable) row
    - The test asserts `role` === `"dialog"`, `aria-modal` === `"true"`, `phx-window-keydown` === `"close_replay"`, and `phx-key` === `"Escape"` on `inbound-replay-modal`
    - The test asserts Escape-closes via `page.keyboard.press("Escape")` then `inbound-replay-modal` count 0 / hidden
    - The new Playwright test passes: `cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "focus-management parity"` exits 0
    - `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after `mix mailglass_admin.assets.build` (OR the regenerated bundle is committed in this plan's diff if it legitimately changed)
  </acceptance_criteria>
  <done>A structural assertion proves the inbound modal's role/aria + Escape attributes and Escape-closes behavior, framed as focus-management parity (not WCAG). The committed admin CSS bundle is verified clean (or the rebuilt bundle is committed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → admin LiveView (inbound modal) | Authenticated operator keyboard/click input crosses into the inbound replay modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | `phx-window-keydown="close_replay"` Escape wiring on inbound dialog | accept | Reuses the existing, already-authorized `close_replay` live handler (inbound_live.ex ~179-180); no new event surface, no new auth path. Escape only closes a modal — no state mutation, no destructive action. |
| T-107-02 | Elevation of Privilege | replay trigger / modal open path | accept | A11Y change is presentation-only (attributes + a focus span). It adds NO new event, NO new route, NO new auth seam. Replay confirm still flows through the unchanged `confirm_replay` handler and existing Auth.authorize/3 gate. |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. `:tailwind` (mix.exs:99) is a pre-existing dev/test build dep; `npx playwright` runs an already-installed local binary. No new dependency added — package legitimacy gate not triggered. |
</threat_model>

<verification>
- `cd mailglass_admin && mix compile --warnings-as-errors` exits 0
- `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` passes
- `cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "focus-management parity"` passes (the new structural assertion — the phase's Dimension-8 validation surface per CONTEXT D-10/D-11)
- `cd mailglass_admin && mix mailglass_admin.assets.build && git diff --exit-code priv/static/` exits 0 (bundle-clean gate, D-08/D-09)
- operator/replay_modal.ex and operator_live.ex unchanged (inbound-only scope, D-05)
- No `phx-hook` and no new JS asset introduced (D-01/D-02)
</verification>

<success_criteria>
- Inbound replay modal closes on Escape via the existing `close_replay` handler (D-01)
- Focus moves into the modal on open and returns to the trigger button on close, with real resolvable ids `#inbound-replay-modal` / `#inbound-replay-open-btn` (D-01/D-03)
- Structural Playwright assertion proves role/aria + Escape attributes + Escape-closes, framed as focus-management parity, not WCAG (D-10/D-11/D-07)
- `git diff --exit-code priv/static/` clean after rebuild, or the regenerated bundle committed (D-08/D-09)
- No new `phx-hook` / no new JS asset; operator modal untouched (D-02/D-05)
- A11Y-01 satisfied
</success_criteria>

<output>
Create `.planning/phases/107-inbound-replay-modal-a11y-parity-wr-03/107-01-SUMMARY.md` when done
</output>
