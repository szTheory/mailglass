---
phase: "105"
plan: "03"
type: execute
wave: 3
depends_on:
  - "105-01"
  - "105-02"
files_modified:
  - guides/getting-started.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "guides/getting-started.md ends on a ## Next steps section (it is the last ## heading in the file)"
    - "The Troubleshooting the Installer section precedes Next steps, not the other way around"
    - "The Troubleshooting entry for 401s reflects Phase 104 fail-closed behavior: Mix.raise, --force, mix mailglass.doctor"
    - "The Next steps section links the first-week path and points at guides/learning-path.md"
    - "docs_contract_test.exs asserts the Next steps section is present and is the final ## heading"
    - "mix test test/mailglass/docs_contract_test.exs exits 0"
  artifacts:
    - path: "guides/getting-started.md"
      provides: "Reordered guide: install → configure → routes → send → troubleshooting → Next steps (last)"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "Contract assertion that Next steps is the last ## heading"
  key_links:
    - from: "guides/getting-started.md ## Next steps"
      to: "guides/learning-path.md"
      via: "inline link in Next steps prose"
      pattern: "learning-path.md"
    - from: "guides/getting-started.md ## Troubleshooting the Installer"
      to: "mix mailglass.doctor"
      via: "updated 401-troubleshooting entry describing fail-closed behavior"
      pattern: "mix mailglass.doctor"
---

<objective>
Reorder guides/getting-started.md so it ends on a "## Next steps" section and update the 401
troubleshooting entry to reflect Phase 104's fail-closed behavior (D-07). Add a contract
assertion proving Next steps is the final ## heading in the file.

Purpose: DOCS-02 requires the guide to end on a first-week arc rather than on installer
troubleshooting. The current file ends at "## Troubleshooting the Installer" (lines 89-104);
reordering so Troubleshooting precedes Next steps makes the guide forward-looking. The 401
troubleshooting entry (D-07) must reflect Phase 104's new reality: mix mailglass.install now
fails closed on an unmanaged Plug.Parsers conflict instead of silently continuing.

Output: guides/getting-started.md (reordered: Troubleshooting before Next steps; 401 entry
updated; new ## Next steps section added), test/mailglass/docs_contract_test.exs (new test
"Getting Started ends on a Next steps section").
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md
@.planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-01-SUMMARY.md
@.planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-02-SUMMARY.md
</context>

<interfaces>
From guides/getting-started.md (current structure for the executor's reference):
  - ## Prerequisites (lines 5-12)
  - ## 1) Install and verify (lines 13-20)
  - ## 2) Configure mailglass (lines 22-32)
  - ## 3) Mount preview and webhook routes (lines 34-55)
  - ## 4) Send your first message (lines 57-78)
  - ## End-to-End Example (lines 80-87)
  - ## Troubleshooting the Installer (lines 89-104, CURRENTLY LAST)

Target structure after this plan:
  - [all above sections unchanged through ## End-to-End Example]
  - ## Troubleshooting the Installer (with updated 401 entry per D-07)
  - ## Next steps (NEW, must be LAST)

From .planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-CONTEXT.md (D-06):
  Phase 104 behavior: mix mailglass.install now fails closed (Mix.raise, non-zero exit) on an
  unmanaged Plug.Parsers conflict. --force escape hatch proceeds. mix mailglass.doctor confirms
  CachingBodyReader wiring, exits non-zero if absent.

From test/support/docs_helpers.ex:
  extract_block_after_heading/2 matches ## headings only.
  The existing "Getting Started compiles" test locates code via heading "4) Send your first message".
  The existing "Config examples are valid" test locates config via heading "2) Configure mailglass".
  These MUST remain passing — do not rename those headings.

From docs/api_stability.md: use canonical vocabulary — "Mailglass.Webhook.CachingBodyReader",
"Plug.Parsers", "Mix.raise", "mix mailglass.doctor". Do not over-claim.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reorder getting-started.md — move Troubleshooting before new ## Next steps</name>

  <read_first>
    - guides/getting-started.md (full — read the EXACT current content before editing; the executor must not work from memory)
    - .planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-CONTEXT.md (D-05, D-06, D-07 for the exact --force / fail-closed / doctor behavior to document accurately)
    - docs/api_stability.md (lines 1-50 — canonical error/module vocabulary)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (D-05, D-06, D-07 for Next steps content requirements)
    - guides/learning-path.md (first 5 lines — confirm file exists and note its title for the link label)
    - guides/jobs.md (first 5 lines — confirm file exists and note its title)
    - guides/authoring-mailables.md (first 5 lines — confirm file exists)
    - guides/preview.md (first 5 lines — confirm file exists)
    - guides/webhooks.md (first 5 lines — confirm file exists)
    - guides/testing.md (first 5 lines — confirm file exists)
    - guides/telemetry.md (first 5 lines — confirm file exists)
  </read_first>

  <action>
    PART A — Update the "Webhooks return 401 after installation" troubleshooting entry (D-07):

    Find the section "## Troubleshooting the Installer" and within it the "### Webhooks return 401
    after installation" sub-section. Replace its content to reflect Phase 104's behavior.

    The updated content must convey (without inventing new public API):
    - From Phase 104: mix mailglass.install now fails closed (exits non-zero via Mix.raise) when
      it detects an unmanaged Plug.Parsers plug (one lacking a :body_reader option) in endpoint.ex.
      This prevents silent webhook 401s at install time.
    - The --force flag bypasses this check and proceeds with installation, inserting the managed
      parser above the existing one. Use --force only if you understand the parser ordering risk.
    - Run `mix mailglass.doctor` after installation to confirm Mailglass.Webhook.CachingBodyReader
      is present in your endpoint parser. The command exits non-zero if the wiring is absent.
    - If you previously installed mailglass before Phase 104 (version < 1.7), check your
      endpoint.ex manually for a Plug.Parsers block lacking body_reader and add the CachingBodyReader
      wiring as shown in the Webhooks guide.

    Do NOT over-claim. Use "fails closed" to describe the behavior per D-07. Reference
    Mailglass.Webhook.CachingBodyReader and Plug.Parsers by their canonical names. No invented
    error struct names — Mix.raise is a stdlib function, not a Mailglass.Error.

    Keep the existing "### mix mailglass.install fails to find endpoint.ex" entry unchanged.

    PART B — Add ## Next steps as the final section (D-05/D-06):

    Append a new "## Next steps" section at the END of the file, after "## Troubleshooting the
    Installer". It must be the LAST ## heading in the file (the contract assertion in Task 2 will
    verify this).

    Content requirements per DOCS-02/D-06 and D-10:
    - Brief intro sentence: "The first hour is behind you. Here is a natural week-one path:"
    - Ordered list with links (D-06 sequence: jobs → authoring-mailables → preview → webhooks →
      testing → operate):
        1. [What you can do with mailglass](jobs.md) — the JTBD map; read once straight through
           when evaluating.
        2. [Authoring mailables](authoring-mailables.md) — native setter API, HEEx layouts,
           reusable components.
        3. [Preview](preview.md) — dev preview at /dev/mail; device-width and dark-mode toggles.
        4. [Webhooks](webhooks.md) — webhook ingest, verification, and suppression wiring.
        5. [Testing](testing.md) — Fake adapter, TestAssertions, and the deliver/2 baseline.
        6. [Telemetry and operating](telemetry.md) — [:mailglass, :outbound, :dispatch, ...]
           events and alerting.
    - A closing line that points to the learning-path index for a fuller map:
      "For a fuller ordered index, see the [learning path](learning-path.md)."

    Do NOT add sub-headings inside ## Next steps — keep it as a single section with a list.
    Use relative paths for all links (no https:// URLs to HexDocs or external sites).

    Keep the trailing footer (*Last updated: ...*) below ## Next steps if it currently exists, or
    remove it — it is not load-bearing for any contract assertion. Do not add a new datestamp.
  </action>

  <verify>
    <automated>grep -n "^## " guides/getting-started.md | tail -1</automated>
  </verify>

  <done>
    guides/getting-started.md contains "## Next steps" as its final ## heading (grep -n "^## "
    | tail -1 shows "## Next steps"). The file contains "mix mailglass.doctor" in the
    Troubleshooting section. The file contains a link to "learning-path.md" in Next steps.
    All existing ## heading names are unchanged (Prerequisites, 1) Install and verify,
    2) Configure mailglass, 3) Mount preview and webhook routes, 4) Send your first message,
    End-to-End Example, Troubleshooting the Installer).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DOCS-02 contract assertion — Next steps is the last ## heading</name>

  <read_first>
    - test/mailglass/docs_contract_test.exs (full — find the "Guide contracts" describe block and the "Getting Started compiles" and "Config examples are valid" tests that must remain passing)
    - guides/getting-started.md (as edited in Task 1 — read the final state to confirm ## Next steps is present and last)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (Claude's Discretion note on assertion strategy)
  </read_first>

  <action>
    In the "Guide contracts" describe block in test/mailglass/docs_contract_test.exs, add a new
    test function named "Getting Started ends on a Next steps section".

    The assertion strategy:
      getting_started = File.read!("guides/getting-started.md")
      # Collect all ## headings in document order
      headings = Regex.scan(~r/^## (.+)$/m, getting_started) |> Enum.map(fn [_, h] -> h end)
      assert headings != [], "getting-started.md has no ## headings"
      last_heading = List.last(headings)
      assert last_heading == "Next steps",
             "getting-started.md must end on '## Next steps' but last heading is '## #{last_heading}'"
      # Guide links to learning-path.md in the Next steps section
      assert getting_started =~ "learning-path.md",
             "getting-started.md Next steps must link to learning-path.md"
      # Updated troubleshooting entry mentions mix mailglass.doctor
      assert getting_started =~ "mix mailglass.doctor",
             "getting-started.md Troubleshooting must describe mix mailglass.doctor"

    Use Regex.scan with the multiline ~r/^## (.+)$/m pattern (not a greedy dotall scan) so only
    ## headings at line start are matched, not ### or deeper. List.last/1 on the heading list is
    the exact last-heading proof.

    Insert this test AFTER the existing "Config examples are valid" test and BEFORE the new
    "learning-path is registered in both mix.exs docs lists" test (from Plan 01) — so the
    getting-started tests are grouped together.

    Verify the two existing getting-started tests still pass:
    - "Getting Started compiles" (locates code via heading "4) Send your first message") — must
      still pass; that heading is unchanged.
    - "Config examples are valid" (locates config via heading "2) Configure mailglass") — must
      still pass; that heading is unchanged.
  </action>

  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs</automated>
  </verify>

  <done>
    docs_contract_test.exs "Getting Started ends on a Next steps section" test passes.
    Existing "Getting Started compiles" and "Config examples are valid" tests still pass.
    mix test test/mailglass/docs_contract_test.exs exits 0 with all tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Docs-only change — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package installs; slopcheck not applicable |

Docs-only change — no new attack surface, no input handling, no auth/data flow. No secrets or
credentials in the updated troubleshooting prose or Next steps link list.
</threat_model>

<verification>
Run the full docs-contract suite:

```bash
mix test test/mailglass/docs_contract_test.exs
```

All tests must pass (exit 0).

Spot-checks:

```bash
grep -n "^## " guides/getting-started.md | tail -3    # last heading must be "## Next steps"
grep -n "mix mailglass.doctor" guides/getting-started.md
grep -n "learning-path" guides/getting-started.md
grep -n "Next steps" test/mailglass/docs_contract_test.exs
```
</verification>

<success_criteria>
- guides/getting-started.md final ## heading is "## Next steps" (grep -n "^## " | tail -1 shows it)
- guides/getting-started.md Troubleshooting section contains "mix mailglass.doctor" and references fail-closed / --force behavior
- guides/getting-started.md ## Next steps contains a link to "learning-path.md"
- guides/getting-started.md ## Next steps lists the six-step arc: jobs → authoring-mailables → preview → webhooks → testing → telemetry
- docs_contract_test.exs "Getting Started ends on a Next steps section" test is present and passes
- Existing "Getting Started compiles" and "Config examples are valid" tests still pass
- mix test test/mailglass/docs_contract_test.exs exits 0
</success_criteria>

<output>
Create .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-03-SUMMARY.md when done.
</output>
