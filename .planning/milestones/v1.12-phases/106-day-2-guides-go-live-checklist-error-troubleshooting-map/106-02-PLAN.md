---
phase: 106-day-2-guides-go-live-checklist-error-troubleshooting-map
plan: 02
type: execute
wave: 2
depends_on:
  - 106-01
files_modified:
  - mix.exs
  - test/mailglass/docs_contract_test.exs
  - docs/api_stability.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "Both new guides are registered in BOTH mix.exs extras: and groups_for_extras: [Guides: ...] lists"
    - "docs_contract_test.exs contains an OPS-01 section-presence assertion verifying checklist literal markers"
    - "docs_contract_test.exs contains an OPS-02 error-coverage assertion verifying all ten error struct names appear in errors-and-troubleshooting.md"
    - "docs_contract_test.exs contains registration assertions for both guides (Regex.scan >= 2 occurrences + File.exists?)"
    - "mix test test/mailglass/docs_contract_test.exs exits 0 with all new assertions passing"
    - "docs/api_stability.md summary line correctly says ten error structs and includes StreamPolicyError in the stable list"
  artifacts:
    - path: "mix.exs"
      provides: "docs registration for both new guides in extras: and Guides: group"
      contains: "guides/production-go-live-checklist.md"
    - path: "mix.exs"
      provides: "docs registration for both new guides in extras: and Guides: group"
      contains: "guides/errors-and-troubleshooting.md"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "OPS-01 and OPS-02 contract assertions"
      contains: "production-go-live-checklist"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "OPS-01 and OPS-02 contract assertions"
      contains: "errors-and-troubleshooting"
  key_links:
    - from: "test/mailglass/docs_contract_test.exs"
      to: "mix.exs"
      via: "Regex.scan on mix_exs content"
      pattern: "guides/production-go-live-checklist\\.md"
    - from: "test/mailglass/docs_contract_test.exs"
      to: "guides/errors-and-troubleshooting.md"
      via: "File.read! + literal presence check"
      pattern: "StreamPolicyError"
---

<objective>
Register both new guides in mix.exs docs lists, add docs-contract assertions for OPS-01 and
OPS-02, apply a minimal factual correction to docs/api_stability.md's error-count summary line
and stable-list omission, then verify the full docs-contract suite exits 0.

Purpose: Without registration, the guides are invisible in HexDocs. Without contract assertions,
regressions are invisible to CI. The api_stability.md correction prevents the errors guide from
routing readers to a document that contradicts the guide's own ten-struct coverage.

Output:
- mix.exs: both guides appended to extras: and to the Guides: group in groups_for_extras:
- test/mailglass/docs_contract_test.exs: three new test blocks (registration x2, OPS-01
  section-presence, OPS-02 error-coverage)
- docs/api_stability.md: summary line "six" → "ten", StreamPolicyError added to stable list
- mix test test/mailglass/docs_contract_test.exs exits 0
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/106-day-2-guides-go-live-checklist-error-troubleshooting-map/106-CONTEXT.md

<interfaces>
<!-- Key patterns the executor needs. Read the files below before editing. -->

From test/mailglass/docs_contract_test.exs — registration test pattern to mirror (lines 158-168):
  test "learning-path is registered in both mix.exs docs lists" do
    mix_exs = File.read!("mix.exs")
    matches = Regex.scan(~r/"guides\/learning-path\.md"/, mix_exs)
    assert length(matches) >= 2,
           "expected \"guides/learning-path.md\" to appear in both extras: and " <>
             "groups_for_extras: [Guides: ...] in mix.exs, but found #{length(matches)} occurrence(s)"
    assert File.exists?("guides/learning-path.md"),
           "guides/learning-path.md does not exist on disk"
  end

Section-presence-by-literal pattern to mirror (docs_contract_test.exs:240-263, 328-341):
  - Read the target guide with File.read!
  - assert guide =~ "literal string", "descriptive failure message"
  - Use one assert per required literal; group under a single test

From mix.exs extras: (current state after Phase 105, lines ~383-407):
  extras: [
    "README.md",
    "docs/api_stability.md",
    "guides/compatibility-and-deprecations.md",
    "guides/upgrading-to-v1_0.md",
    "guides/getting-started.md",
    "guides/learning-path.md",
    "guides/jobs.md",
    ...
    "guides/migration-from-swoosh.md",
    "docs/upgrade-from-0.x.md",
    "MAINTAINING.md",
    ...
  ]
  APPEND both new guides AFTER "guides/migration-from-swoosh.md" and BEFORE "docs/upgrade-from-0.x.md"
  OR after "docs/upgrade-from-0.x.md" and before "MAINTAINING.md" — either position is acceptable
  as long as they appear in BOTH lists.

From mix.exs groups_for_extras: Guides group (current state, lines ~414-430):
  Guides: [
    "guides/upgrading-to-v1_0.md",
    "guides/getting-started.md",
    "guides/learning-path.md",
    ...
    "guides/migration-from-swoosh.md",
    "docs/upgrade-from-0.x.md"
  ]
  APPEND both new guides to the end of this list (or after migration-from-swoosh.md if
  docs/upgrade-from-0.x.md looks out of place — use judgment on ordering).
  The main: "getting-started" line (357) MUST NOT be changed.

From docs/api_stability.md — stale lines to correct (minimal surgical edits only):
  Line 214: "- `@type t` — union of the six error structs"
    → change "six" to "ten"
  Lines 56-60: stable list currently omits Mailglass.StreamPolicyError
    Current (lines 56-60):
      `Mailglass.Error`, `Mailglass.SendError`, `Mailglass.TemplateError`,
      `Mailglass.SignatureError`, `Mailglass.SuppressedError`,
      `Mailglass.RateLimitError`, `Mailglass.ConfigError`,
      `Mailglass.EventLedgerImmutableError`, `Mailglass.TenancyError`, and
      `Mailglass.PublishError`.
    Add `Mailglass.StreamPolicyError` before `Mailglass.PublishError` in that list.
  These are the ONLY two edits to api_stability.md. Do not reorder, add sections,
  or change any atom sets.

From lib/mailglass/error.ex @error_modules (lines 65-76) — drive OPS-02 assertions from these names:
  Mailglass.SendError, Mailglass.TemplateError, Mailglass.SignatureError,
  Mailglass.SuppressedError, Mailglass.RateLimitError, Mailglass.ConfigError,
  Mailglass.EventLedgerImmutableError, Mailglass.TenancyError,
  Mailglass.StreamPolicyError, Mailglass.PublishError
  Short names to assert in guide content: SendError, TemplateError, SignatureError,
  SuppressedError, RateLimitError, ConfigError, EventLedgerImmutableError,
  TenancyError, StreamPolicyError, PublishError
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register both guides in mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (lines 350-440) — read the current state of extras: and groups_for_extras: BEFORE editing; line numbers may have shifted since planning due to Phase 105 commits
  </read_first>
  <action>
Read mix.exs lines 350-440 first to confirm the current extras: and groups_for_extras: layout.

Make two targeted edits to the docs/0 function:

EDIT 1 — extras: list: Append "guides/production-go-live-checklist.md" and
"guides/errors-and-troubleshooting.md" to the extras: list. Insert them at the end of the
guides block — after "guides/migration-from-swoosh.md" and before "docs/upgrade-from-0.x.md".
If "docs/upgrade-from-0.x.md" is already at the end of the guides section, insert before
"MAINTAINING.md". The exact position is at your discretion (D-10 says ordering is Claude's
discretion); both must appear somewhere in extras:.

EDIT 2 — groups_for_extras: Guides: list: Append both new guides to the Guides: list after
the current last entry. Keep docs/upgrade-from-0.x.md (if present) as the last-or-near-last
entry per the existing pattern, or append after it. Both guides must appear somewhere in the
Guides: list.

INVARIANT: main: "getting-started" (line ~357) MUST NOT be changed. Do not remove or reorder
any existing entries.

After editing, verify via grep:
  grep -c '"guides/production-go-live-checklist.md"' mix.exs   → 2
  grep -c '"guides/errors-and-troubleshooting.md"' mix.exs     → 2
  </action>
  <verify>
    <automated>grep -c '"guides/production-go-live-checklist.md"' mix.exs && grep -c '"guides/errors-and-troubleshooting.md"' mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - "guides/production-go-live-checklist.md" appears exactly 2 times in mix.exs (once in extras:, once in Guides:)
    - "guides/errors-and-troubleshooting.md" appears exactly 2 times in mix.exs (once in extras:, once in Guides:)
    - main: "getting-started" is unchanged
    - No existing extras: or groups_for_extras: entries were removed or reordered
    - `grep -c '"guides/production-go-live-checklist.md"' mix.exs` returns 2
    - `grep -c '"guides/errors-and-troubleshooting.md"' mix.exs` returns 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add docs-contract assertions for OPS-01 and OPS-02</name>
  <files>test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - test/mailglass/docs_contract_test.exs (full file) — read the CURRENT state before appending; confirm the existing describe-block structure and the last test in the "Guide contracts" describe block so the new tests append cleanly
    - test/support/docs_helpers.ex (lines 17-19) — confirm extract_block_after_heading/2 ## constraint
    - guides/production-go-live-checklist.md — confirm the exact literal strings that will be asserted (must be present in the file from Plan 01)
    - guides/errors-and-troubleshooting.md — confirm the ten struct short-names are literally present
  </read_first>
  <action>
Append three new tests inside the existing "Guide contracts" describe block in
test/mailglass/docs_contract_test.exs. Insert them BEFORE the closing `end` of that describe
block, after the last existing test in it.

TEST 1 — Registration test for production-go-live-checklist (mirrors learning-path test at lines 158-168):
  test "production-go-live-checklist is registered in both mix.exs docs lists" do
    Verify: Regex.scan(~r/"guides\/production-go-live-checklist\.md"/, File.read!("mix.exs"))
    has length >= 2, plus File.exists?("guides/production-go-live-checklist.md")
  end

TEST 2 — Registration test for errors-and-troubleshooting (same pattern):
  test "errors-and-troubleshooting is registered in both mix.exs docs lists" do
    Verify: Regex.scan(~r/"guides\/errors-and-troubleshooting\.md"/, File.read!("mix.exs"))
    has length >= 2, plus File.exists?("guides/errors-and-troubleshooting.md")
  end

TEST 3 — OPS-01 section-presence (mirrors 240-263 and 328-341 patterns):
  test "production-go-live-checklist covers required go-live topics" do
    checklist = File.read!("guides/production-go-live-checklist.md")
    # Both distinct doctor commands must appear literally
    assert checklist =~ "mix mail.doctor"
    assert checklist =~ "mix mailglass.doctor"
    # Oban queue sizing section marker
    assert checklist =~ "Oban"
    # Suppression section marker
    assert checklist =~ "suppression"
    # Telemetry section marker
    assert checklist =~ "telemetry"
    # Webhook secret rotation section marker
    assert checklist =~ "rotation"
  end

TEST 4 — OPS-02 error-coverage: all ten error module short-names appear in errors-and-troubleshooting.md.
Drive from a literal list (matches @error_modules short names), verify each with assert guide =~
plus an api_stability.md route-link assertion:
  test "errors-and-troubleshooting covers all ten error structs and routes to api_stability.md" do
    guide = File.read!("guides/errors-and-troubleshooting.md")
    error_names = [
      "SendError", "TemplateError", "SignatureError", "SuppressedError",
      "RateLimitError", "ConfigError", "EventLedgerImmutableError",
      "TenancyError", "StreamPolicyError", "PublishError"
    ]
    for name <- error_names do
      assert guide =~ name,
             "errors-and-troubleshooting.md is missing error struct: #{name}"
    end
    # The guide must route canonical atom-set truth to api_stability.md (D-03)
    assert guide =~ "api_stability.md",
           "errors-and-troubleshooting.md must cross-link to docs/api_stability.md"
  end

Exact test names (use these verbatim — they become the CI output identifiers):
  "production-go-live-checklist is registered in both mix.exs docs lists"
  "errors-and-troubleshooting is registered in both mix.exs docs lists"
  "production-go-live-checklist covers required go-live topics"
  "errors-and-troubleshooting covers all ten error structs and routes to api_stability.md"

Do NOT add a new describe block — append inside the existing "Guide contracts" describe block.
Do NOT modify any existing tests.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix test test/mailglass/docs_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All four new tests exist in the "Guide contracts" describe block
    - `mix test test/mailglass/docs_contract_test.exs` exits 0
    - No existing tests were modified or removed
    - The four new test names appear in the output
    - Test count increases by 4 compared to the pre-plan count (28 tests → 32 tests, or whatever the current count is)
    - 0 failures, at most 1 skip (the pre-existing Phase 38 skip)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Correct stale error-count and missing StreamPolicyError in docs/api_stability.md</name>
  <files>docs/api_stability.md</files>
  <read_first>
    - docs/api_stability.md (lines 50-65) — confirm the exact current text of the stable-list paragraph mentioning Mailglass.Error / the nine-name list that omits StreamPolicyError
    - docs/api_stability.md (lines 208-220) — confirm the exact current text of the "union of the six error structs" summary line
  </read_first>
  <action>
Apply EXACTLY TWO surgical prose edits to docs/api_stability.md. No other changes.

EDIT 1 — Stable list (around lines 56-60): The current stable list ends with
  `Mailglass.EventLedgerImmutableError`, `Mailglass.TenancyError`, and
  `Mailglass.PublishError`.
Insert `Mailglass.StreamPolicyError,` BEFORE `Mailglass.PublishError` so the list reads:
  `Mailglass.EventLedgerImmutableError`, `Mailglass.TenancyError`,
  `Mailglass.StreamPolicyError`, and `Mailglass.PublishError`.

EDIT 2 — Error Hierarchy summary (around line 214): Change the sentence
  "- `@type t` — union of the six error structs"
  to
  "- `@type t` — union of ten error structs"

Verification after edits:
  grep "StreamPolicyError" docs/api_stability.md      → at least 1 match
  grep "ten error structs" docs/api_stability.md      → 1 match
  grep "six error structs" docs/api_stability.md      → 0 matches (the stale line is gone)

SCOPE BOUNDARY: These are the ONLY changes permitted in this task. Do not:
- Add a StreamPolicyError section with atom sets (that is a future api_stability.md editorial task)
- Change any existing per-struct atom sets or Retryable lines
- Add, remove, or reorder any other content
  </action>
  <verify>
    <automated>grep -c "ten error structs" docs/api_stability.md && grep -c "StreamPolicyError" docs/api_stability.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "six error structs" docs/api_stability.md` returns no matches (stale count removed)
    - `grep "ten error structs" docs/api_stability.md` returns 1 match
    - `grep "StreamPolicyError" docs/api_stability.md` returns at least 1 match (stable list now includes it)
    - No other lines in docs/api_stability.md were changed (diff is exactly 2 lines changed)
    - `mix test test/mailglass/docs_contract_test.exs` still exits 0 after this edit (no existing assertion breaks)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies mix.exs docs config, an ExUnit test file, and a markdown docs file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03 | Tampering | mix.exs docs config | accept | mix.exs changes are limited to the extras: and groups_for_extras: lists; no build or runtime behavior changes. Reviewed via PR. |
| T-106-04 | Tampering | docs/api_stability.md | accept | Two prose-only corrections to a documentation file. No closed atom sets changed. Reviewed via PR. |
| T-106-05 | Denial of Service | docs_contract_test.exs | accept | Adding assertions to an existing ExUnit test file. New assertions gate docs regressions; they cannot affect runtime. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan. |

No runtime attack surface exists in this plan. All changes are documentation, test, and docs config.
</threat_model>

<verification>
Run after all three tasks complete:

1. Registration counts in mix.exs:
   `grep -c '"guides/production-go-live-checklist.md"' mix.exs`  → 2
   `grep -c '"guides/errors-and-troubleshooting.md"' mix.exs`    → 2

2. main: unchanged:
   `grep "main:" mix.exs` → contains "getting-started"

3. api_stability.md corrections:
   `grep "ten error structs" docs/api_stability.md`  → 1 match
   `grep -c "six error structs" docs/api_stability.md` → 0
   `grep "StreamPolicyError" docs/api_stability.md`  → >= 1 match

4. Full docs-contract suite green:
   `mix test test/mailglass/docs_contract_test.exs`
   Expected: all tests pass, 0 failures, at most 1 skip (pre-existing Phase 38 tag: skip)
   New tests visible in output:
     "production-go-live-checklist is registered in both mix.exs docs lists"
     "errors-and-troubleshooting is registered in both mix.exs docs lists"
     "production-go-live-checklist covers required go-live topics"
     "errors-and-troubleshooting covers all ten error structs and routes to api_stability.md"
</verification>

<success_criteria>
- Both guides registered in mix.exs extras: and Guides: group (2 occurrences each)
- main: "getting-started" unchanged
- 4 new docs-contract tests pass (registration x2 + OPS-01 section-presence + OPS-02 error-coverage)
- `mix test test/mailglass/docs_contract_test.exs` exits 0
- docs/api_stability.md "six" → "ten" and StreamPolicyError added to stable list
- Phase 106 OPS-01 and OPS-02 requirements are fully gated by CI
</success_criteria>

<output>
Create .planning/phases/106-day-2-guides-go-live-checklist-error-troubleshooting-map/106-02-SUMMARY.md when done
</output>
