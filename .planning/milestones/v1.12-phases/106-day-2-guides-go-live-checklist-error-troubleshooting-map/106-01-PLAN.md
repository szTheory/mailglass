---
phase: 106-day-2-guides-go-live-checklist-error-troubleshooting-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/production-go-live-checklist.md
  - guides/errors-and-troubleshooting.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "guides/production-go-live-checklist.md exists with one ## section per checklist topic"
    - "The checklist surfaces both mix mail.doctor and mix mailglass.doctor as distinct commands with distinct purposes"
    - "guides/errors-and-troubleshooting.md exists with one ## section per error struct (ten sections)"
    - "StreamPolicyError has a dedicated ## section sourced from its module moduledoc (not from api_stability.md)"
    - "Every section in the errors guide routes canonical type/retryable truth to docs/api_stability.md rather than restating closed atom sets"
    - "The errors guide cross-links operator-incident-support.md and webhook-troubleshooting.md without absorbing them"
  artifacts:
    - path: "guides/production-go-live-checklist.md"
      provides: "Pre-production verification surface for operators"
      contains: "mix mail.doctor"
    - path: "guides/production-go-live-checklist.md"
      provides: "Pre-production verification surface for operators"
      contains: "mix mailglass.doctor"
    - path: "guides/errors-and-troubleshooting.md"
      provides: "Unified error struct map with cause/fix/remediation"
      contains: "StreamPolicyError"
  key_links:
    - from: "guides/errors-and-troubleshooting.md"
      to: "docs/api_stability.md"
      via: "per-section remediation cross-link"
      pattern: "api_stability"
    - from: "guides/production-go-live-checklist.md"
      to: "guides/multi-tenancy.md"
      via: "cross-link"
      pattern: "multi-tenancy"
---

<objective>
Write the two new day-2 operator guides. Both files are pure markdown and carry
no runtime contracts.

Purpose: Give adopters the runbooks they need on day 2 — a structured checklist
to work through before going live, and a unified map of every error struct so
they can diagnose failures by matching a struct and type atom rather than
reading scattered module docs.

Output:
- guides/production-go-live-checklist.md — thin orchestrating checklist with
  one ## section per topic, cross-linking existing topic guides, surfacing both
  doctor commands with their distinct purposes (D-05, D-06, D-07).
- guides/errors-and-troubleshooting.md — one ## section per error struct (ten
  sections, including StreamPolicyError which is absent from api_stability.md),
  routing canonical type/retryable truth to docs/api_stability.md (D-01 through
  D-04, D-08).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/106-day-2-guides-go-live-checklist-error-troubleshooting-map/106-CONTEXT.md

<interfaces>
<!-- Key types and source prose the executor needs. No codebase exploration required. -->

From lib/mailglass/error.ex — authoritative ten-name list (@error_modules lines 65-76):
  Mailglass.SendError, Mailglass.TemplateError, Mailglass.SignatureError,
  Mailglass.SuppressedError, Mailglass.RateLimitError, Mailglass.ConfigError,
  Mailglass.EventLedgerImmutableError, Mailglass.TenancyError,
  Mailglass.StreamPolicyError, Mailglass.PublishError

From lib/mailglass/errors/stream_policy_error.ex (TRAP — absent from api_stability.md):
  @types [:stream_policy_violated]
  "Raised when a message violates stream policy."
  "the message violates rules for its assigned stream"
  retryable?: false

From lib/mix/tasks/mail.doctor.ex (lines 8-19, 35):
  "Run DNS-only deliverability checks for one explicit domain."
  Usage: mix mail.doctor --domain example.com [--dkim-selector ...] [--verbose] [--format json]
  Requires app.start (line 35: Mix.Task.run("app.start"))
  Checks: DKIM, SPF, DMARC

From lib/mix/tasks/mailglass.doctor.ex (lines 6-31, 98-104):
  "Run a static webhook-wiring check on the host app's endpoint.ex"
  Usage: mix mailglass.doctor
  OFFLINE: does NOT boot the host application
  Exit codes: 0=CachingBodyReader wired, 1=absent (run mix mailglass.install), 2=cannot diagnose
  Three-state exit, no app.start

From test/support/docs_helpers.ex lines 17-19:
  extract_block_after_heading/2 pattern: ~r/##\s+#{heading}\n.*?```(?:elixir|bash)\n(.*?)\n```/s
  CRITICAL: matches ## (two-hash) ONLY. ### headings silently return nil.
  All headings that contract assertions may anchor to MUST be ##.

Existing guides for checklist cross-links (content lives there, not in the checklist):
  guides/multi-tenancy.md, guides/telemetry.md, guides/dkim-setup.md,
  guides/rate-limiting.md, guides/unsubscribe.md, guides/webhooks.md

Existing incident guides the errors guide cross-links (do NOT absorb):
  guides/operator-incident-support.md — symptom-first incident runbook
  guides/webhook-troubleshooting.md — webhook shim

Pinned contract assertions guarding those files (do NOT break):
  docs_contract_test.exs:231-263 (telemetry/webhook vocabulary)
  docs_contract_test.exs:359-375 (trust-entry docs route to api_stability.md)

Brand voice (CLAUDE.md): clear, exact, confident, "thoughtful maintainer".
Error message pattern: "[Noun] [past-tense verb]: [specific cause]" — never "Oops!"
Vocabulary: Mailable, Message, Delivery, Event, Mailbox, Suppression (from CLAUDE.md Domain Language).
No over-claims (Honest-Surface lens). "dispatch" != "delivered".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/production-go-live-checklist.md</name>
  <files>guides/production-go-live-checklist.md</files>
  <read_first>
    - lib/mix/tasks/mail.doctor.ex (lines 1-40) — exact usage, flags, requires app.start
    - lib/mix/tasks/mailglass.doctor.ex (lines 1-50, 95-106) — exact usage, three-state exit codes
    - lib/mailglass/installer/doctor.ex — doctor findings detail
    - guides/webhooks.md (lines 1-30) — understand the canonical webhook guide to cross-link accurately
    - guides/multi-tenancy.md (lines 1-20) — confirm the canonical guide exists and its focus
    - guides/telemetry.md (lines 1-20) — confirm canonical guide exists and its focus
  </read_first>
  <action>
Create guides/production-go-live-checklist.md as a THIN ORCHESTRATING CHECKLIST (cross-link-not-duplicate pattern, per D-05). Use ## (two-hash exactly) headings throughout.

File structure — one ## section per topic (D-07), in this order:

1. ## Deliverability: mix mail.doctor
   Explain this command runs DNS-only DKIM/SPF/DMARC checks against a live domain and requires app.start. Surface the exact command: `mix mail.doctor --domain yourdomain.com`. Cross-link to guides/dkim-setup.md for DKIM record detail. Note: this command requires the application to be running (app.start) — do not conflate with mailglass.doctor.

2. ## Webhook wiring: mix mailglass.doctor
   Explain this is the OFFLINE static endpoint-wiring check (Phase 104, INSTALL-03). Surface the exact command: `mix mailglass.doctor`. Describe the three-state exit: exit 0 = CachingBodyReader wired correctly; exit 1 = absent, run `mix mailglass.install` to fix; exit 2 = cannot diagnose (endpoint.ex not found). Note: OFFLINE — does not boot the application. Cross-link to guides/webhooks.md for webhook setup detail.

3. ## Webhook secret provisioning and rotation
   Cover: provisioning the webhook verification secret/key from the provider dashboard at deploy time; rotation procedure (update the env var, redeploy, verify with mix mailglass.doctor). Cross-link to guides/webhooks.md. Reference the webhook-troubleshooting.md shim for incident recovery. Do NOT absorb the content of those guides.

4. ## Oban queue sizing
   Explain that Mailglass.Outbound.Worker runs under Oban for deliver_later/2, and queue sizing affects throughput. Describe the `config :mailglass, oban_queue:` knob (or whatever the actual config key is — read mix.exs / config docs to confirm the exact key). Cross-link to guides/authoring-mailables.md for deliver_later usage. State that queue size depends on send volume; provide a conservative default recommendation consistent with the existing config examples.

   NOTE: If the exact Oban queue config key is not immediately derivable from the existing guides, describe the concern accurately ("configure the :mailglass Oban queue size in your Oban config") rather than inventing a key name. The Honest-Surface lens prohibits over-claims.

5. ## Per-tenant adapter routing
   State that multi-tenancy with different ESPs per tenant requires implementing `Mailglass.Tenancy.resolve_outbound_adapter_ref/2`. Cross-link to guides/multi-tenancy.md for the full callback interface and config examples. Do not duplicate the multi-tenancy guide content here.

6. ## Suppression strategy
   Explain the suppression list guards against re-sending to opted-out, hard-bounced, or tenant-excluded recipients, and that Mailglass.SuppressedError is the signal when delivery is blocked. Reference guides/unsubscribe.md for RFC 8058 List-Unsubscribe wiring. Cross-link to the suppression APIs (Mailglass.Suppression). Do not duplicate suppression guide content.

7. ## Telemetry and alerting
   Explain that mailglass emits telemetry on [:mailglass, :outbound, :dispatch, :*], [:mailglass, :render, :message, :*], [:mailglass, :webhook, :ingest, :*], [:mailglass, :webhook, :reconcile, :*]. Cross-link to guides/telemetry.md for the full telemetry reference. Suggest attaching handlers to track delivery failures and webhook reconcile failures in production. State that PII is never emitted in telemetry metadata (by convention).

Voice constraints:
- Brand voice: clear, exact, confident. No "Oops!", no "experience the full pipeline."
- Every cross-link must use markdown link syntax pointing to the correct file.
- No fenced code blocks containing syntax that would fail Code.string_to_quoted unless they are bash code blocks (not elixir).
- The file must start with a # title, then a brief intro paragraph, then the ## sections.
  </action>
  <verify>
    <automated>grep -c "^## " guides/production-go-live-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at guides/production-go-live-checklist.md
    - Exactly 7 ## sections (two-hash headings), one per checklist topic (D-07)
    - The literal string "mix mail.doctor" appears in the file
    - The literal string "mix mailglass.doctor" appears in the file
    - The word "OFFLINE" or "offline" appears in the webhook-wiring section (distinguishing it from mail.doctor which needs app.start)
    - Cross-links to guides/multi-tenancy.md, guides/telemetry.md, guides/dkim-setup.md, guides/webhooks.md are present
    - No ## headings are ### headings (all section headings are two-hash)
    - No content that duplicates or absorbs guides/operator-incident-support.md or guides/webhook-troubleshooting.md
    - `grep -c "^## " guides/production-go-live-checklist.md` returns 7
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write guides/errors-and-troubleshooting.md</name>
  <files>guides/errors-and-troubleshooting.md</files>
  <read_first>
    - lib/mailglass/error.ex (full file) — @error_modules list (lines 65-76), @type t (lines 47-57), moduledoc
    - lib/mailglass/errors/send_error.ex — @types, moduledoc cause/fix prose
    - lib/mailglass/errors/template_error.ex — @types, moduledoc
    - lib/mailglass/errors/signature_error.ex — @types, moduledoc (includes legacy atoms)
    - lib/mailglass/errors/suppressed_error.ex — @types, moduledoc
    - lib/mailglass/errors/rate_limit_error.ex — @types, moduledoc
    - lib/mailglass/errors/config_error.ex — @types, moduledoc
    - lib/mailglass/errors/event_ledger_immutable_error.ex — @types, moduledoc
    - lib/mailglass/errors/tenancy_error.ex — @types, moduledoc
    - lib/mailglass/errors/stream_policy_error.ex — @types [:stream_policy_violated], moduledoc (PRIMARY SOURCE — absent from api_stability.md)
    - lib/mailglass/errors/publish_error.ex — @types, moduledoc
    - docs/api_stability.md (lines 208-420) — per-struct canonical atom sets + Retryable lines; PublishError (408-420)
    - guides/operator-incident-support.md (lines 1-10) — confirm file exists, note it is symptom-first
    - guides/webhook-troubleshooting.md (lines 1-12) — confirm it is a shim
    - test/support/docs_helpers.ex (lines 17-19) — ## heading constraint
  </read_first>
  <action>
Create guides/errors-and-troubleshooting.md. This guide is organized by ERROR STRUCT (not by symptom — that is guides/operator-incident-support.md's axis). Use ## (two-hash exactly) headings for every error struct section — this is mandatory because extract_block_after_heading/2 matches ## only (D-01).

File structure:

Start with a # title ("Errors and Troubleshooting") and a brief intro paragraph that:
- States this guide covers all ten Mailglass error structs with their causes, fix actions, and remediation pointers.
- Explains that each struct has a closed `:type` atom set; always match on the struct module and `:type` field — never on `:message` (cite error.ex's pattern-match example idiom).
- Notes that canonical closed atom sets and Retryable values are documented in docs/api_stability.md.
- Cross-links guides/operator-incident-support.md as the COMPLEMENTARY symptom-first guide for incident runbooks.
- Cross-links guides/webhook-troubleshooting.md as the webhook-specific incident shim.

Then produce exactly TEN ## sections, one per struct, in @error_modules order (D-01, D-02):

1. ## SendError
2. ## TemplateError
3. ## SignatureError
4. ## SuppressedError
5. ## RateLimitError
6. ## ConfigError
7. ## EventLedgerImmutableError
8. ## TenancyError
9. ## StreamPolicyError
10. ## PublishError

Per-section format (prose paragraphs, no fenced code blocks needed):
- First sentence: "Raised when [module moduledoc opening phrase]."
- Cause paragraph: describe what triggers each type atom in the closed set. Use the module's own @types and moduledoc prose as the source. For SignatureError: mention both current atoms (`:missing_header`, `:malformed_header`, `:bad_credentials`, `:ip_disallowed`, `:bad_signature`, `:timestamp_skew`, `:malformed_key`) and the legacy aliases (`:missing`, `:malformed`, `:mismatch`).
- Fix paragraph: what the operator should do to resolve.
- Remediation sentence: "For the canonical closed `:type` atom set and Retryable policy, see [docs/api_stability.md](../docs/api_stability.md)."

EXCEPTION — StreamPolicyError (D-04, the trap):
- Source ENTIRELY from lib/mailglass/errors/stream_policy_error.ex because api_stability.md has NO StreamPolicyError section.
- @types [:stream_policy_violated] — "the message violates rules for its assigned stream."
- retryable?: false
- The remediation line still routes to docs/api_stability.md for general context but must note: "Note: StreamPolicyError's type set is sourced from its module; it does not currently have a dedicated section in api_stability.md."

EXCEPTION — PublishError (D-03):
- Source from publish_error.ex moduledoc + docs/api_stability.md lines 408-420.
- @types [:publish_blocked_golden_drift] — "installer golden drift detected during mix mailglass.publish.check."
- Fix: run `MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors` to regenerate the goldens.
- Route to api_stability.md for the canonical atom set.

Accuracy constraints (D-03):
- Do NOT restate or copy the closed :type atom sets as authority in this guide — point to api_stability.md.
- The cause paragraph may ENUMERATE the type atoms (so operators know what to match on) but must not present this guide as the source-of-truth for the atoms.

Cross-link constraints (D-08):
- Do NOT absorb guides/operator-incident-support.md or guides/webhook-troubleshooting.md content.
- A single cross-link sentence in the intro is sufficient; individual sections that are webhook-adjacent (e.g., SignatureError) may add one cross-link sentence pointing to webhook-troubleshooting.md.

Voice constraints:
- Brand voice: clear, exact, confident. "thoughtful maintainer" — no "Oops!", no fuzzy language.
- Error format: "[Noun] [past-tense verb]: [specific cause]" (from existing format_message/2 clauses in each module).
- No over-claims. Honest-Surface lens.
  </action>
  <verify>
    <automated>grep -c "^## " guides/errors-and-troubleshooting.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at guides/errors-and-troubleshooting.md
    - Exactly 10 ## sections (two-hash headings), one per error struct (D-01)
    - `grep -c "^## " guides/errors-and-troubleshooting.md` returns 10
    - All ten struct short-names appear literally: SendError, TemplateError, SignatureError, SuppressedError, RateLimitError, ConfigError, EventLedgerImmutableError, TenancyError, StreamPolicyError, PublishError
    - "api_stability.md" appears in the file (routing canonical truth there per D-03)
    - "StreamPolicyError" appears in a ## heading (not a ### heading)
    - "stream_policy_violated" appears in the file (sourced from stream_policy_error.ex, not api_stability.md)
    - "operator-incident-support.md" appears in the file (cross-link, not absorbed per D-08)
    - "webhook-troubleshooting.md" appears in the file (cross-link, not absorbed per D-08)
    - No ## heading uses ### (all struct headings are exactly two-hash)
    - File does not contain fenced elixir blocks that would fail Code.string_to_quoted (pure prose guide; if elixir blocks are included, they must parse)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan writes markdown guides and has no runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Information Disclosure | guides/*.md | accept | Guides are public documentation; they contain no secrets, credentials, or PII. All cross-links point to other public docs. |
| T-106-02 | Tampering | guides/*.md | accept | Markdown files in a git repo; changes are reviewed via PR and commit history. No runtime impact. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan. Pure markdown creation. |

No runtime attack surface exists in this plan. The guides are public operator documentation with no user input, network paths, auth flows, or data processing.
</threat_model>

<verification>
Run after both tasks complete:

1. Both guide files exist:
   `ls guides/production-go-live-checklist.md guides/errors-and-troubleshooting.md`

2. Checklist has 7 ## sections:
   `grep -c "^## " guides/production-go-live-checklist.md` → 7

3. Both doctor commands present in checklist:
   `grep "mix mail.doctor\|mix mailglass.doctor" guides/production-go-live-checklist.md`

4. Errors guide has 10 ## sections:
   `grep -c "^## " guides/errors-and-troubleshooting.md` → 10

5. All ten struct names present in errors guide:
   `grep -l "SendError\|TemplateError\|SignatureError\|SuppressedError\|RateLimitError\|ConfigError\|EventLedgerImmutableError\|TenancyError\|StreamPolicyError\|PublishError" guides/errors-and-troubleshooting.md`

6. StreamPolicyError has a ## heading (not ###):
   `grep "^## StreamPolicyError" guides/errors-and-troubleshooting.md`

7. api_stability.md cross-link present in errors guide:
   `grep "api_stability" guides/errors-and-troubleshooting.md`

NOTE: Do NOT run `mix test` at this plan stage — the guides are not yet registered in mix.exs and
the docs-contract assertions do not yet exist. Registration and assertions are Plan 02.
</verification>

<success_criteria>
- guides/production-go-live-checklist.md exists with 7 ## sections, containing both "mix mail.doctor" and "mix mailglass.doctor" as distinct commands with distinct descriptions
- guides/errors-and-troubleshooting.md exists with 10 ## sections, one per error struct in @error_modules order
- StreamPolicyError has a ## section sourced from stream_policy_error.ex (not from api_stability.md)
- Every struct section routes to api_stability.md for canonical atom-set truth
- Both guides cross-link (do not absorb) operator-incident-support.md and/or webhook-troubleshooting.md
- Voice is clear, exact, confident — "thoughtful maintainer" throughout
</success_criteria>

<output>
Create .planning/phases/106-day-2-guides-go-live-checklist-error-troubleshooting-map/106-01-SUMMARY.md when done
</output>
