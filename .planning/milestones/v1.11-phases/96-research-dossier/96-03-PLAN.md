---
phase: 96-research-dossier
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v1.11/COMPONENT-STATES.md
autonomous: true
requirements:
  - RESEARCH-03
must_haves:
  truths:
    - ".planning/research/v1.11/COMPONENT-STATES.md exists and ends in a ## LOCKED DECISION block"
    - "Every LOCKED row carries a STATE-LD-NN ID, a Decision cell stating the literal state list or token, an Applies-to cell naming the exact archetype, a Constraint-binding cell, and a Closes-GAP cell"
    - "The matrix covers every archetype in the D-09 inventory: icon, logo, flash, badge, status_badge (full status-atom set), nav_link, theme_toggle, tenant_chip, orientation_strip, shell, master-detail list+detail, filters_form, support-card triage grid, suppression card, timeline, replay modal, device frame, preview tabs/sidebar, routing_trace, evidence_card"
    - "status_badge, routing_trace, evidence_card, and tenant_chip are all explicitly named in the LOCKED DECISION block"
    - "Honors governing CONTEXT decisions D-04 (every LOCKED row carries a Constraint-binding cell) and D-05 (critic-then-lock adversarial synthesis stress-tests each state decision against constraints + GAP rows before locking); this is the codebase-led dossier per D-07 (real archetype inventory from components.ex / operator+preview / inbound modules, external sources secondary)"
  artifacts:
    - path: ".planning/research/v1.11/COMPONENT-STATES.md"
      provides: "Component-state dossier with adversarially-synthesized LOCKED DECISION block covering the full D-09 archetype inventory"
      contains: "## LOCKED DECISION"
  key_links:
    - from: "COMPONENT-STATES.md LOCKED DECISION block"
      to: "downstream phases 97 (Component Layer + Gallery), 98/99/100 (surfaces)"
      via: "STATE-LD-NN IDs cited in plan frontmatter / task actions"
      pattern: "STATE-LD-[0-9]+"
---

<objective>
Research and write the COMPONENT-STATES dossier: enumerate the canonical state matrix (rest/hover/focus/active/disabled/loading/empty/error) for every component archetype in the real mailglass_admin codebase (per D-09), grounded in file:line citations and secondary external best practice. End with an adversarially-synthesized LOCKED DECISION block.

AXIS OWNERSHIP: This dossier owns WHICH states exist per archetype. It does NOT decide how transitions animate (MOTION dossier owns that) or how each state renders in dark theme (DARK-MODE dossier owns that). Cross-reference by LD-ID if overlap is noted.

Purpose: Phase 97 (Component Layer + Gallery) uses this matrix as the spec for the gallery specimen list and state implementation; the Phase 95 ratchet's Spacing/Type/Color pillars have concrete per-component state specs to score against.

Output: `.planning/research/v1.11/COMPONENT-STATES.md` — research body + adversarial synthesis + LOCKED DECISION block at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-research-dossier/96-CONTEXT.md
@.planning/RATCHET-GAP-REGISTER.md
@mailglass_admin/docs/design-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the component-state matrix from codebase archetypes</name>
  <files>.planning/research/v1.11/COMPONENT-STATES.md</files>
  <read_first>
    - .planning/phases/96-research-dossier/96-CONTEXT.md (D-08 axis-ownership, D-09 archetype inventory, D-10 GAP mapping — read in full)
    - .planning/STATE.md (hard design constraints; COMP-01..03 requirement intent)
    - mailglass_admin/docs/design-system.md (per-component conformance checklist; token layers; motion vocabulary — read in full)
    - .planning/RATCHET-GAP-REGISTER.md (GAP-01: btn-sm touch-target on support_cards.ex:56 sev=3; GAP-04: inbound filter labels off-token sev=2; GAP-05: gallery route gap sev=2)
    - mailglass_admin/lib/mailglass_admin/components.ex (icon/logo/flash/badge/status_badge definitions with full status-atom set lines 207-230, status_icon lines 232+)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (nav_link line 201, tenant_chip line 245, theme_toggle line 260, orientation_strip line 314, shell line 116)
    - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex (triage grid; the GAP-01 btn-sm lines 56/102/152)
    - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex (filters_form — GAP-04 source)
    - mailglass_admin/lib/mailglass_admin/operator/timeline.ex
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
    - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex
    - mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex (routing_trace lines 31+; locked/info reveal)
    - mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex (evidence_card lines 29+; mono chips on surface-sunken)
    - mailglass_admin/lib/mailglass_admin/preview/device_frame.ex
    - mailglass_admin/lib/mailglass_admin/preview/tabs.ex
    - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
  </read_first>

  <action>
    Create `.planning/research/v1.11/COMPONENT-STATES.md`.

    Structure the dossier body with these sections (exact heading names at your discretion):
    1. Archetype Inventory — list every archetype from the D-09 inventory with its source file:line. Confirm all 20+ archetypes are accounted for: icon, logo, flash, badge, status_badge (with its full status-atom set: dispatched/queued/sent/delivered/deferred/bounced/failed/rejected/complained/unsubscribed/opened/clicked/autoresponded/unknown/accepted/no_match/ignore/failed_ingest/webhook_replay_requested/webhook_replay_succeeded/webhook_replay_failed/reconciled), nav_link, theme_toggle, tenant_chip, orientation_strip, shell; operator: master-detail (deliveries_list + detail_header), filters_form, support-card triage grid, suppression_card, timeline, replay_modal; preview: device_frame, tabs, sidebar; inbound: routing_trace (locked/info reveal), evidence_card (mono chips on surface-sunken).

    2. State Matrix — for each archetype, enumerate the applicable states from {rest, hover, focus, active, disabled, loading, empty, error}. Not every state applies to every archetype — document which states are applicable vs not-applicable with rationale. For applicable states, note the current implementation method from the codebase (file:line). Mark states currently missing (i.e., not implemented but should exist per the archetype's UX contract).

    3. External Best Practice Reference — cite WCAG 2.1 SC 2.4.7 (visible focus), WCAG 1.4.11 (non-text contrast), ARIA Authoring Practices for relevant patterns (combobox for filters, dialog for modal, listbox for navigation). Note where mailglass_admin already conforms and where it deviates.

    4. Gap Analysis — identify which archetypes have missing states that represent UX failures (priority: archetypes with no visible focus state, no loading/empty state, no error state where those states are reachable). Note the D-08 axis-ownership boundary: note where the MOTION dossier owns the transition and where the DARK-MODE dossier owns the dark-theme rendering.

    5. Adversarial Synthesis — challenge each draft state-list decision against (a) the hard design constraints (semantic tokens, flat elevation, font-weight 400/700 only, no faux-bold) and (b) the open GAP rows. Specifically: does the touch-target decision for support_cards address GAP-01? Does the filters_form state list address GAP-04? Does the routing_trace / evidence_card locked/info-reveal state model address Phase 99's scope? Revise any decisions that fail the critic.

    6. LOCKED DECISION block — the table defined below.

    The LOCKED DECISION block MUST:
    - Use heading `## LOCKED DECISION`
    - Contain a markdown table with columns: `LD-ID | Decision | Applies-to (surface/archetype) | Constraint-binding | Closes-GAP`
    - Use ID prefix `STATE-LD-NN` (e.g. STATE-LD-01, STATE-LD-02, ...)
    - Decision cell: state the LITERAL state list or token/class for that archetype (e.g. "status_badge: states = {rest, disabled}; no hover/active/focus states as it is display-only; all 22 status atoms must resolve to a non-default class via status_class/1", "routing_trace: states = {rest, info-reveal-open, info-reveal-closed, locked}; locked state renders with opacity-50 and aria-disabled=true")
    - Constraint-binding cell: name the specific hard design constraint(s) (e.g. "semantic tokens only; font-weight 400/700 only; flat elevation; min-h-11 for interactive controls")
    - Closes-GAP cell: reference the GAP-NN ID or "—"
    - Must cover at minimum one row per archetype group (shared components, operator group, preview group, inbound group)
    - The full status_badge status-atom set (all 22 atoms) must be explicitly addressed in at least one row
    - GAP-01 (btn-sm touch target) must appear in Closes-GAP for at least one row (the support_cards / interactive control row)
    - GAP-04 (inbound filter labels) must appear in Closes-GAP for at least one row (the filters_form row)

    Axis-ownership cross-reference rule: where the MOTION dossier will own transition animation, note "see MOTION-LD-NN" in the Decision cell. Where the DARK-MODE dossier will own dark rendering, note "see DARK-LD-NN" (use placeholder LD-IDs if the other dossiers are not yet complete; the Wave-2 reconciliation plan will finalize cross-references).

    Do NOT modify any admin code, CSS, brandbook files, or the RATCHET-GAP-REGISTER.md.
  </action>

  <verify>
    <automated>grep -l "## LOCKED DECISION" .planning/research/v1.11/COMPONENT-STATES.md && grep -c "STATE-LD-" .planning/research/v1.11/COMPONENT-STATES.md && grep -c "status_badge\|routing_trace\|evidence_card\|tenant_chip" .planning/research/v1.11/COMPONENT-STATES.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/research/v1.11/COMPONENT-STATES.md` exists
    - File contains `## LOCKED DECISION` heading
    - File contains at least 10 STATE-LD-NN rows (grep -c "STATE-LD-" returns >= 10; the D-09 inventory has 20+ archetypes requiring at minimum grouped coverage)
    - All four of these tokens appear in the file: `status_badge`, `routing_trace`, `evidence_card`, `tenant_chip`
    - `GAP-01` appears at least once in the Closes-GAP column
    - `GAP-04` appears at least once in the Closes-GAP column
    - File mentions the 22 status atoms or at minimum these key ones: `dispatched`, `delivered`, `bounced`, `webhook_replay_succeeded`, `reconciled`
    - File references ARIA or WCAG in the external best practice section
    - No row in the LOCKED DECISION table is missing the Constraint-binding cell
  </acceptance_criteria>

  <done>COMPONENT-STATES.md exists, ends in a LOCKED DECISION block with at least 10 STATE-LD-NN rows covering the full D-09 archetype inventory (including status_badge/routing_trace/evidence_card/tenant_chip explicitly), every row has a non-empty Constraint-binding cell, GAP-01 and GAP-04 appear in Closes-GAP cells, and no locked decision violates the hard design constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codebase reads → dossier text | Executor reads existing .ex files; content is read-only input to a markdown file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-SC | Tampering | No npm/pip/cargo installs in this plan | accept | Research-only phase; no package manager invocations |
| T-96-03-I | Information Disclosure | COMPONENT-STATES.md | accept | Docs-only research phase — no executable attack surface; the only integrity concern is that a LOCKED decision must not violate the hard design constraints (guarded by the D-04 constraint-binding cell + the downstream CI conformance/motion/token-parity gates) |
</threat_model>

<verification>
- `.planning/research/v1.11/COMPONENT-STATES.md` exists with content
- `grep "## LOCKED DECISION" .planning/research/v1.11/COMPONENT-STATES.md` returns a match
- `grep -c "STATE-LD-" .planning/research/v1.11/COMPONENT-STATES.md` returns >= 10
- `grep "status_badge" .planning/research/v1.11/COMPONENT-STATES.md` returns matches including in the LOCKED DECISION block
- `grep "routing_trace" .planning/research/v1.11/COMPONENT-STATES.md` returns a match
- `grep "evidence_card" .planning/research/v1.11/COMPONENT-STATES.md` returns a match
- `grep "GAP-01" .planning/research/v1.11/COMPONENT-STATES.md` returns a match
- `grep "GAP-04" .planning/research/v1.11/COMPONENT-STATES.md` returns a match
</verification>

<success_criteria>
COMPONENT-STATES.md is a complete, adversarially-synthesized research dossier that ends in a LOCKED DECISION block with stable STATE-LD-NN IDs, covering the full D-09 archetype inventory, with literal state-list decisions, non-empty Constraint-binding cells, and GAP-01/GAP-04 in Closes-GAP cells — ready for downstream citation by Phase 97 (gallery specimen list) and Phases 98/99/100 (surface uplift) without re-reading the body.
</success_criteria>

<output>
Create `.planning/phases/96-research-dossier/96-03-SUMMARY.md` when done.
</output>
