---
phase: 96-research-dossier
plan: 06
type: execute
wave: 2
depends_on:
  - 96-01
  - 96-02
  - 96-03
  - 96-04
  - 96-05
files_modified:
  - .planning/research/v1.11/SUMMARY.md
autonomous: true
requirements:
  - RESEARCH-01
  - RESEARCH-02
  - RESEARCH-03
  - RESEARCH-04
  - RESEARCH-05
must_haves:
  truths:
    - ".planning/research/v1.11/SUMMARY.md exists and contains all five LOCKED DECISION blocks verbatim"
    - "SUMMARY.md contains all five LD-ID prefixes: MOTION-LD, IA-LD, STATE-LD, DARK-LD, COPY-LD"
    - "Axis-ownership cross-references (MOTION/COMPONENT-STATES/DARK-MODE overlaps) are resolved — no two dossiers lock conflicting decisions for the same cell"
    - "Every open GAP row is addressed in SUMMARY.md — sev-3 rows (GAP-01/02/03) appear in at least one dossier Closes-GAP column; sev-2 rows (GAP-04/05) appear in a dossier Closes-GAP column or in the ## GAP Coverage Gaps section with the downstream phase noted"
    - "SUMMARY.md is the canonical downstream read — it contains the blocks themselves, not pointers to body sections"
    - "Reconciliation confirms every dossier honored governing CONTEXT decisions D-04 (Constraint-binding cell on every LOCKED row) and D-05 (critic-then-lock adversarial synthesis) before any decision was locked"
  artifacts:
    - path: ".planning/research/v1.11/SUMMARY.md"
      provides: "Single-file canonical downstream read hoisting all five LOCKED DECISION blocks verbatim"
      contains: "MOTION-LD"
  key_links:
    - from: "SUMMARY.md"
      to: "downstream phases 97-102 plan frontmatter and task actions"
      via: "LD-ID citations in plan files"
      pattern: "(MOTION|IA|STATE|DARK|COPY)-LD-[0-9]+"
---

<objective>
Reconcile axis-ownership cross-references across the five completed dossiers, resolve any conflicting decisions (D-08), then produce `SUMMARY.md` — the canonical single-file downstream read that hoists all five LOCKED DECISION blocks verbatim. Downstream phases 97–102 read SUMMARY.md only; they never re-read the dossier bodies.

Purpose: SUMMARY.md is the integration point that makes the five parallel research outputs a coherent, contradiction-free decision set. The Phase 103 audit will verify that every build-phase task cites an LD-ID present in this file.

Output: `.planning/research/v1.11/SUMMARY.md` containing all five LOCKED DECISION blocks verbatim plus the reconciliation notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-research-dossier/96-CONTEXT.md
@.planning/RATCHET-GAP-REGISTER.md
@.planning/research/v1.11/MOTION.md
@.planning/research/v1.11/IA.md
@.planning/research/v1.11/COMPONENT-STATES.md
@.planning/research/v1.11/DARK-MODE.md
@.planning/research/v1.11/MICROCOPY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile axis-ownership conflicts and produce SUMMARY.md</name>
  <files>.planning/research/v1.11/SUMMARY.md</files>
  <read_first>
    - .planning/research/v1.11/MOTION.md (read in full — extract the complete LOCKED DECISION block verbatim)
    - .planning/research/v1.11/IA.md (read in full — extract the complete LOCKED DECISION block verbatim)
    - .planning/research/v1.11/COMPONENT-STATES.md (read in full — extract the complete LOCKED DECISION block verbatim)
    - .planning/research/v1.11/DARK-MODE.md (read in full — extract the complete LOCKED DECISION block verbatim)
    - .planning/research/v1.11/MICROCOPY.md (read in full — extract the complete LOCKED DECISION block verbatim)
    - .planning/phases/96-research-dossier/96-CONTEXT.md (D-02: SUMMARY.md is canonical downstream read; D-03: LD-ID schema; D-08: axis-ownership rules; D-10: GAP mapping — re-read D-08 carefully before reconciliation)
    - .planning/RATCHET-GAP-REGISTER.md (all five GAP rows — confirm every row appears in at least one dossier's Closes-GAP column)
  </read_first>

  <action>
    Perform the axis-ownership reconciliation pass, then create `.planning/research/v1.11/SUMMARY.md`.

    RECONCILIATION PASS (do this before writing SUMMARY.md):

    For each of the three overlap axes, check the five dossiers for conflicting decisions:

    1. MOTION vs COMPONENT-STATES overlap: Does COMPONENT-STATES define a state transition that MOTION has already animated differently? For example, if STATE-LD-NN says "nav_link: states = {rest, hover, active}" and MOTION-LD-NN says "row-state uses transition-colors duration-fast", are these compatible? If COMPONENT-STATES locked an animation (which it should not — MOTION owns animation), flag and reconcile: update cross-reference placeholder LD-IDs (e.g. "see MOTION-LD-NN") in COMPONENT-STATES to point to the real MOTION-LD IDs. Do NOT modify the dossier files; note reconciliation outcomes as comments in SUMMARY.md.

    2. DARK-MODE vs COMPONENT-STATES overlap: Does COMPONENT-STATES reference a dark-mode rendering decision that conflicts with DARK-MODE's token choices? Verify placeholder "see DARK-LD-NN" cross-references in COMPONENT-STATES map to valid DARK-LD IDs. Note any gaps.

    3. DARK-MODE vs MOTION overlap: Does DARK-MODE affect any motion decision (e.g. does dark mode require different reduced-motion behaviour)? If MOTION-LD-NN covers reduced-motion snap behaviour, does DARK-MODE agree? Note any conflict.

    GAP COVERAGE CHECK: Confirm that across the five dossiers, every open GAP row is addressed:
    - GAP-01 (deliveries btn-sm touch target, sev=3): must appear in COMPONENT-STATES Closes-GAP column
    - GAP-02 (preview empty-state focusable CTA, sev=3): must appear in MOTION or MICROCOPY Closes-GAP column (or both)
    - GAP-03 (preview ignores dark theme, sev=3): must appear in DARK-MODE Closes-GAP column
    - GAP-04 (inbound filter labels off-token, sev=2): must appear in COMPONENT-STATES or IA Closes-GAP column
    - GAP-05 (gallery route gap, sev=2): must appear in at least one dossier's Closes-GAP column noting Phase 97 dependency
    If any GAP is uncovered, note it in SUMMARY.md under "## GAP Coverage Gaps" with which downstream phase must address it. Do NOT add retroactive rows to the dossier files.

    SUMMARY.md STRUCTURE:

    The file must contain exactly the following sections:

    ```
    # v1.11 Research Dossier — SUMMARY

    > Canonical downstream read for Phases 97–102. Read this file; never re-read the dossier bodies.
    > Every locked decision below is adversarially synthesized and constraint-verified.
    > Phase 103 audit verifies build-phase tasks cite LD-IDs present here.

    ## Axis-Ownership Map

    [Brief table: MOTION owns / COMPONENT-STATES owns / DARK-MODE owns — the D-08 axis boundaries]

    ## GAP Coverage Index

    [Table: GAP-NN | Addressed by LD-ID | Dossier | Status]
    Must include all five GAP rows.

    ## Reconciliation Notes

    [Any cross-reference updates; any conflicts found and resolved; any uncovered GAPs]

    ## MOTION — LOCKED DECISION

    [Verbatim paste of the LOCKED DECISION block from MOTION.md — the full markdown table with all MOTION-LD-NN rows]

    ## IA — LOCKED DECISION

    [Verbatim paste of the LOCKED DECISION block from IA.md — the full markdown table with all IA-LD-NN rows]

    ## COMPONENT-STATES — LOCKED DECISION

    [Verbatim paste of the LOCKED DECISION block from COMPONENT-STATES.md — the full markdown table with all STATE-LD-NN rows, with any placeholder LD-IDs updated to real IDs per reconciliation]

    ## DARK-MODE — LOCKED DECISION

    [Verbatim paste of the LOCKED DECISION block from DARK-MODE.md — the full markdown table with all DARK-LD-NN rows]

    ## MICROCOPY — LOCKED DECISION

    [Verbatim paste of the LOCKED DECISION block from MICROCOPY.md — the full markdown table with all COPY-LD-NN rows]
    ```

    Rules for the SUMMARY.md content:
    - Each dossier section heading must be `## {DOSSIER NAME} — LOCKED DECISION` exactly (this is what the Wave-2 grep checks scan for).
    - The LOCKED DECISION tables are pasted verbatim from each dossier, with one allowed modification: cross-reference placeholder LD-IDs (e.g. "see MOTION-LD-NN placeholder") may be updated to the real LD-IDs discovered during reconciliation.
    - Do not summarize or paraphrase the locked tables — they must be copy-pasteable into a build plan's `<read_first>` and be fully self-contained.
    - The Axis-Ownership Map and GAP Coverage Index are brief (one table each, < 10 rows each) — they are navigation aids, not additional research.

    Do NOT modify the five dossier files (MOTION.md, IA.md, COMPONENT-STATES.md, DARK-MODE.md, MICROCOPY.md) or the RATCHET-GAP-REGISTER.md. All reconciliation notes live in SUMMARY.md only.
  </action>

  <verify>
    <automated>grep -c "MOTION-LD\|IA-LD\|STATE-LD\|DARK-LD\|COPY-LD" .planning/research/v1.11/SUMMARY.md && grep "GAP-01\|GAP-02\|GAP-03\|GAP-04\|GAP-05" .planning/research/v1.11/SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/research/v1.11/SUMMARY.md` exists
    - File contains all five LD-ID prefixes: `MOTION-LD`, `IA-LD`, `STATE-LD`, `DARK-LD`, `COPY-LD` (grep -c returns >= 5 distinct matches across the file)
    - File contains all five GAP row IDs: `GAP-01`, `GAP-02`, `GAP-03`, `GAP-04`, `GAP-05`
    - File contains `## Axis-Ownership Map` or equivalent section
    - File contains `## GAP Coverage Index` or equivalent section
    - File contains five `## {NAME} — LOCKED DECISION` headings (one per dossier)
    - The MOTION-LD section in SUMMARY.md has at least as many MOTION-LD-NN IDs as MOTION.md's LOCKED DECISION block (grep comparison)
    - The STATE-LD section has at least as many STATE-LD-NN IDs as COMPONENT-STATES.md's LOCKED DECISION block
    - No LOCKED decision row in SUMMARY.md has an empty Constraint-binding cell (tables are verbatim from validated dossiers)
    - File does NOT contain "placeholder" without accompanying resolution (reconciliation must have resolved all placeholders or noted them explicitly under Reconciliation Notes)
  </acceptance_criteria>

  <done>SUMMARY.md exists and contains: (1) all five LOCKED DECISION blocks verbatim from the five dossiers, (2) an axis-ownership map resolving D-08 overlaps, (3) a GAP coverage index confirming all five GAP rows are addressed, and (4) reconciliation notes for any cross-reference updates — forming the single canonical downstream read for Phases 97–102.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Five dossier files → SUMMARY.md | Executor reads the five Wave-1 outputs and consolidates into one file; no external sources fetched in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06-SC | Tampering | No npm/pip/cargo installs in this plan | accept | Research-only phase; no package manager invocations |
| T-96-06-I | Information Disclosure | SUMMARY.md | accept | Docs-only research phase — no executable attack surface; the only integrity concern is that a LOCKED decision must not violate the hard design constraints (guarded by the D-04 constraint-binding cell + the downstream CI conformance/motion/token-parity gates); the reconciliation pass explicitly checks this) |
</threat_model>

<verification>
- `.planning/research/v1.11/SUMMARY.md` exists with content
- `grep "MOTION-LD\|IA-LD\|STATE-LD\|DARK-LD\|COPY-LD" .planning/research/v1.11/SUMMARY.md` returns matches for all five prefixes
- `grep "GAP-01" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "GAP-02" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "GAP-03" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "GAP-04" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "GAP-05" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "## MOTION" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "## IA" .planning/research/v1.11/SUMMARY.md` returns a match
- `grep "## COMPONENT-STATES\|## DARK-MODE\|## MICROCOPY" .planning/research/v1.11/SUMMARY.md` returns matches
- All five dossier files still unmodified (only SUMMARY.md was written by this plan)
</verification>

<success_criteria>
SUMMARY.md is the canonical single-file downstream read: it contains all five LOCKED DECISION blocks verbatim (with reconciled cross-reference IDs), an axis-ownership map resolving D-08 overlaps, a GAP coverage index confirming all five open GAP rows are addressed, and reconciliation notes — giving Phases 97–102 a single file to cite LD-IDs from without re-reading any dossier body.
</success_criteria>

<output>
Create `.planning/phases/96-research-dossier/96-06-SUMMARY.md` when done.
</output>
