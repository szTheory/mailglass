---
phase: 101-microcopy-pass
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - mailglass_admin/test/mailglass_admin/voice_test.exs
autonomous: true
requirements:
  - COPY-01

must_haves:
  truths:
    - "The voice test covers all three admin surfaces: Preview, Operator, and Inbound"
    - "Banned-word assertions (oops, whoops, uh oh, something went wrong) run against all three surfaces"
    - "Canonical-string assertions cover representative locked strings from LD-01/03/05/11/12/14/16 per surface"
    - "The flash strings from inbound_live.ex (put_flash at lines ~219 and ~227) are covered by source-level grep guards"
    - "The existing script-strip regex is reused on all three surfaces to avoid the phoenix.mjs noops false positive"
    - "The check is data-driven: banned-word list and per-surface canonical strings are module attributes"
    - "cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs exits 0"
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/voice_test.exs"
      provides: "3-surface data-driven voice conformance check"
      contains: "@banned_words"
  key_links:
    - from: "voice_test.exs operator describe block"
      to: "LD-01/11/14 canonical strings"
      via: "live(conn, /ops/mail) render"
      pattern: "No Deliveries match your filters"
    - from: "voice_test.exs inbound describe block"
      to: "LD-03/12/16 canonical strings"
      via: "live(conn, /ops/mail/inbound) render"
      pattern: "No InboundMessages match these filters"
    - from: "voice_test.exs flash grep guard"
      to: "inbound_live.ex put_flash strings (LD-13 / LD-16)"
      via: "grep against source file"
      pattern: "InboundMessage's timeline"
---

<objective>
Extend `mailglass_admin/test/mailglass_admin/voice_test.exs` — currently Preview-only — to
cover Operator (`/ops/mail`) and Inbound (`/ops/mail/inbound`) with the same banned-word
refutation and canonical-string assertions, making the Phase 101 success criterion 3
("a conformance/voice check stays green") meaningful across all three surfaces.

Purpose: Without this plan, two of three surfaces have no automated copy conformance gate.
Adding data-driven Operator and Inbound `describe` blocks on `MailglassAdmin.LiveViewCase`
closes that gap and prevents future copy regressions across the entire admin.

Output: Extended `voice_test.exs` with Operator and Inbound describe blocks, data-driven
`@banned_words` module attribute, per-surface canonical-string assertions, source-level
grep guards for flash-only strings, and the existing script-strip regex reused throughout.
All tests pass.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/101-microcopy-pass/101-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/101-microcopy-pass/101-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend voice_test.exs with data-driven 3-surface conformance check</name>
  <files>mailglass_admin/test/mailglass_admin/voice_test.exs</files>

  <read_first>
    - mailglass_admin/test/mailglass_admin/voice_test.exs (READ IN FULL before editing — preserve the existing script-strip regex at line 27, the existing @moduledoc, the three existing describe blocks; extend, do not rewrite)
    - .planning/research/v1.11/MICROCOPY.md (LOCKED DECISION table lines 484–504 — canonical strings to assert)
    - .planning/phases/101-microcopy-pass/101-CONTEXT.md (D-08 through D-11 for the check design)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (confirm the exact post-Plan-01 flash strings at lines 219 and 227 before writing grep guards)
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs (precedent for how Inbound is mounted in LiveViewCase)
  </read_first>

  <behavior>
    - Test: banned words ("oops", "whoops", "uh oh", "something went wrong") are absent from the Operator surface rendered HTML (script-stripped)
    - Test: banned words are absent from the Inbound surface rendered HTML (script-stripped)
    - Test: Operator renders the LD-01 primary empty-state string "No Deliveries match your filters" (requires triggering the filter-empty state OR checking a conditional path — use a seed-less tenant or no-match filter)
    - Test: Operator renders the LD-11 orientation tip "Delivery never arrived? Start here."
    - Test: Operator renders the LD-14 suppression card copy "No active Suppression for this Delivery." OR the heading "Suppression" (whichever is present in the initial render without a selected delivery — choose what the page actually renders on load)
    - Test: Inbound renders the LD-03 empty-state string "No InboundMessages match these filters"
    - Test: Inbound renders the LD-12 orientation tip "InboundMessage didn't route as expected? Inspect the routing trace."
    - Test: Inbound renders the LD-16 select-record prompt "Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence." (rendered-pane string, present in first render at line 385)
    - Source grep guard: inbound_live.ex source contains "InboundMessage's timeline" (LD-13 flash, only fires on action — covered by source assertion not live render)
    - Source grep guard: inbound_live.ex source contains "Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence." in a put_flash context (LD-16 flash; grep the file for the string)
  </behavior>

  <action>
    Extend `mailglass_admin/test/mailglass_admin/voice_test.exs`. Do NOT rewrite existing content.
    Preserve the @moduledoc, existing `describe` blocks, and the alias line.

    STEP 1 — Add a module attribute `@banned_words` at the module level (after the `use` and `alias`
    lines, before the first `describe`):

      @banned_words ~w[oops whoops "uh oh" "something went wrong"]

    This makes the banned-word list data-driven and cheaply extensible (D-09).

    STEP 2 — Refactor the existing banned-exclamations test in the Preview describe block to use
    `@banned_words` and the `strip_scripts/1` helper (see Step 3). Keep semantics identical.

    STEP 3 — Extract the script-strip regex from the existing test at line 27 into a private helper:

      defp strip_scripts(html) do
        Regex.replace(~r/<script\b[^>]*>.*?<\/script>/si, html, "")
      end

    Update the existing test to call `strip_scripts(html)` instead of the inline Regex.replace.

    STEP 4 — Add an `"Operator surface (/ops/mail)"` describe block after the existing
    "canonical brand copy" describe block. Use `MailglassAdmin.LiveViewCase`.

    The Operator surface renders with the OperatorLive mount signature. Check
    `inbound_live_test.exs` and the existing voice_test for the session setup pattern.
    Use a minimal session (no tenant, no filters) so the overview or deliveries list renders.

    Assertions in the Operator describe block:
    a) Banned-word refutation test: mount `/ops/mail`, strip scripts, Enum.each @banned_words
       to refute each. One test named "banned words absent from Operator surface".
    b) Canonical-string test: mount `/ops/mail`, assert each of:
       - "Delivery never arrived? Start here." (LD-11, orientation tip, shell.ex copy_for(:deliveries))
       - The subtitle or orientation copy signals the surface uses domain nouns (spot-check one more
         string present in the initial render — executor chooses the most reliably present string
         without a selected delivery, e.g. "Deliveries" heading or support-card copy; keep it simple)

    STEP 5 — Add an `"Inbound surface (/ops/mail/inbound)"` describe block after the Operator block.

    Assertions in the Inbound describe block:
    a) Banned-word refutation test: mount `/ops/mail/inbound`, strip scripts, Enum.each @banned_words
       to refute each. One test named "banned words absent from Inbound surface".
    b) Canonical-string test: mount `/ops/mail/inbound`, assert each of:
       - "InboundMessage didn't route as expected? Inspect the routing trace." (LD-12, orientation tip)
       - "Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence." (LD-16, rendered-pane, present in initial render)
       - "No InboundMessages match these filters" (LD-03, records list empty state — present in initial
         render if no inbound records exist in the test DB; confirm this is the case from inbound_live_test.exs patterns)

    STEP 6 — Add a `"Flash string source guards (action-only strings)"` describe block at the end.
    These cover strings that only render after an action, so we assert at source level:

      test "inbound replay-success flash uses InboundMessage domain noun" do
        source = File.read!("lib/mailglass_admin/inbound_live.ex")
        assert source =~ "InboundMessage's timeline",
               "LD-13: replay flash must use InboundMessage not 'message'"
      end

      test "inbound no-selection flash matches LD-16 locked string" do
        source = File.read!("lib/mailglass_admin/inbound_live.ex")
        assert source =~ ~s|"Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence."|,
               "LD-16: no-selection put_flash must use the locked LD-16 string"
      end

    Note: File.read! path is relative from the test runner CWD (mailglass_admin/) so use
    "lib/mailglass_admin/inbound_live.ex" (no leading slash, no mailglass_admin/ prefix).

    CONSTRAINTS:
    - Do NOT create a new test file — extend voice_test.exs per D-10.
    - Do NOT add tests for loading states (D-12 NO-OP).
    - Do NOT assert against operator_live.ex flash strings beyond what is rendered on first mount
      (those are conformant per D-07 sweep).
    - Keep `async: false` (live views share router; do not change).
    - The existing @tag :skip test must remain skipped and untouched.
    - No fenced code blocks are implementation — the above is directive prose specifying behavior,
      not literal code to copy. Executor writes the actual Elixir test code following these specifications.
  </action>

  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs</automated>
    Expected: All tests pass, exit 0. The previously green Preview tests remain green. The new
    Operator and Inbound banned-word tests pass. The new canonical-string tests pass. The flash
    source-guard tests pass.
  </verify>

  <acceptance_criteria>
    - voice_test.exs contains module attribute @banned_words with the four banned patterns
    - voice_test.exs contains a defp strip_scripts/1 helper using the existing regex (~r/<script\b[^>]*>.*?<\/script>/si)
    - voice_test.exs contains a describe block for the Operator surface (/ops/mail)
    - voice_test.exs contains a describe block for the Inbound surface (/ops/mail/inbound)
    - voice_test.exs contains a describe block or standalone tests for flash string source guards
    - Each banned-word test uses Enum.each @banned_words (data-driven, per D-09)
    - Each surface's banned-word test calls strip_scripts/1 to avoid the phoenix.mjs noops false positive (per D-10)
    - The source-guard test for LD-13 asserts inbound_live.ex contains "InboundMessage's timeline"
    - The source-guard test for LD-16 flash asserts the put_flash locked string is present in inbound_live.ex
    - cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs exits 0 with all tests passing
    - The existing three describe blocks ("banned exclamations", "canonical brand copy", "live reload info log") are preserved and still pass
    - No new test files are created
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → source file read | File.read! on a local source file; no user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-02 | Information Disclosure | live test sessions mounting /ops routes | accept | Test sessions use no real PII; LiveViewCase uses test adapters; no recipient data in test fixtures used by voice check |
| T-101-SC | Tampering | no package installs in this plan | accept | Test-only extension; no npm/pip/cargo installs; no supply-chain surface |

Surface: test file extension only. No production code changes. No input handling, auth, network, or data-flow changes. PII minimization preserved — the added tests render live views under test adapter conditions, same as existing voice tests.
</threat_model>

<verification>
```
cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs
```
Expected: All tests pass (previously green + newly added). No compilation warnings.

Also confirm the existing full admin test suite is not broken by the test additions:
```
cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs --seed 0
```
</verification>

<success_criteria>
voice_test.exs covers all three admin surfaces with data-driven banned-word refutation and
canonical-string assertions. Flash-only strings (inbound put_flash lines 219 and 227) are covered
by source-level grep guards. The script-strip regex is extracted into a reusable defp helper.
All tests pass. The Phase 101 ROADMAP success criterion 3 — "a conformance/voice check stays
green" — is now meaningful and enforced across Operator, Inbound, and Preview.
</success_criteria>

<output>
Create `.planning/phases/101-microcopy-pass/101-02-SUMMARY.md` when done.
Include in SUMMARY: list of new tests added with their assert/refute targets, and the canonical
strings asserted per surface keyed to their LD-ID.
</output>
