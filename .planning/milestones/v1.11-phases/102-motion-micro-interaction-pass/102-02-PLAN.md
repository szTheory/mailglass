---
phase: 102-motion-micro-interaction-pass
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - mailglass_admin/assets/css/app.css
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true

must_haves:
  truths:
    - "A token-named symmetric easing (--ease-symmetric) exists and the tab-swap crossfade resolves through it"
    - "Focus-ring color transitions resolve through --duration-instant (90ms, ≤100ms) so focus motion is token-named and within the MOTION-LD-06 ceiling"
    - "A loading skeleton exists as a connection-state placeholder (opacity-only, no layout-property animation), shown while disconnected and hidden once connected"
    - "Cross-document View Transitions are enabled as CSS-only progressive enhancement, wrapped in @media (prefers-reduced-motion: no-preference) so reduced-motion disables them"
    - "The committed priv/static/app.css bundle is rebuilt and bit-clean (git diff --exit-code priv/static/)"
  artifacts:
    - path: "mailglass_admin/assets/css/app.css"
      provides: "--ease-symmetric token, .mg-skeleton connection placeholder, @view-transition PE block, tab-swap pointing at --ease-symmetric"
      contains: "--ease-symmetric"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "rebuilt compiled bundle"
  key_links:
    - from: ".motion-tab-swap"
      to: "--ease-symmetric"
      via: "animation easing var"
      pattern: "var\\(--ease-symmetric\\)"
    - from: ".mg-skeleton"
      to: "phx-connected / phx-disconnected body state"
      via: "attribute selector, opacity-only"
      pattern: "phx-connected"
    - from: "@view-transition"
      to: "prefers-reduced-motion: no-preference"
      via: "media-query wrapper"
      pattern: "no-preference"
---

<objective>
Apply the app.css motion-token uplift consuming the MOTION dossier (MOTION-LD-05/06/09/10) and the two RESEARCH reconciliations: add the `--ease-symmetric` token and point the tab-swap crossfade at it; standardize focus-ring transitions on `--duration-instant`; add the connection-state loading skeleton; and add the CSS-only cross-document View-Transitions progressive enhancement. Rebuild and commit the bundle.

Purpose: These are the pure-CSS deliverables of MOTION-01 (token-named easing, focus transitions, loading skeletons, View-Transitions PE) plus the MOTION-02 reduced-motion gating of VT. They land in app.css with no HEEx behavior change, so they are independent of the exit-motion HEEx work in Plan 03 — except both touch the committed bundle, so Plan 03 sequences after this.

Output: Updated `assets/css/app.css` + rebuilt `priv/static/app.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md
@.planning/research/v1.11/MOTION.md

<interfaces>
<!-- app.css @theme block defines --ease-out / --ease-in-out (lines ~135-139).
     :root defines --duration-instant: 90ms, --duration-fast: 150ms, --duration-reveal: 220ms (~195-198).
     Named motions are @keyframes + .motion-* animation rules (~241-288).
     Reduced-motion @media block (~292-300) zeroes animation/transition duration with 0.01ms !important.
     Tailwind v4 standalone binary scans lib/*.ex for LITERAL utility strings — dynamically built
     class names are tree-shaken (Pitfall 2). Authored .motion-*/.mg-skeleton classes live directly
     in app.css and are NOT subject to scanning. -->

Current tab-swap easing (app.css:266): .motion-tab-swap { animation: mg-fade-in var(--duration-fast) var(--ease-out) both; }
MOTION-LD-05 prescribes var(--ease-symmetric) for tab-swap (RESEARCH Pitfall 4: --ease-symmetric does not yet exist).
LiveView body connection state: body carries phx-connected / phx-disconnected (RESEARCH Assumption A1 — confirm exact attribute/class form against the bundled phoenix_live_view.js before authoring selectors).
@view-transition is a cross-document at-rule: inert for LiveView WS patches, no-ops on unsupported browsers (RESEARCH § View Transitions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --ease-symmetric token + point tab-swap at it; standardize focus-ring duration token</name>
  <read_first>
    - mailglass_admin/assets/css/app.css (the file being modified — @theme easing block ~135-139, :root duration tokens ~195-198, .motion-tab-swap ~265-267)
    - .planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md (Pitfall 4 --ease-symmetric reconciliation; § Focus Transitions for the --duration-instant vs --duration-fast resolution)
    - .planning/research/v1.11/MOTION.md (MOTION-LD-05 tab-swap uses var(--ease-symmetric); MOTION-LD-06 focus/row-state ≤100ms)
    - mailglass_admin/docs/design-system.md (motion rules — source-of-truth; easing/duration token row)
  </read_first>
  <action>
    In the app.css `@theme` block, add `--ease-symmetric: var(--ease-in-out);` immediately after the existing `--ease-in-out` definition (RESEARCH Pitfall 4 recommendation: alias the in-out curve under the dossier's name so the MOTION-LD-05 reference resolves; reversible public-CSS-token decision, research-and-locked, no escalation). Update `.motion-tab-swap` to use `var(--ease-symmetric)` in place of `var(--ease-out)` (MOTION-LD-05 — the single permitted symmetric-crossfade exception). Add a short comment on the token citing MOTION-LD-05 and that `ease-in`/`ease-in-out`/`--ease-symmetric` are the ONLY non-ease-out curves permitted, scoped to symmetric crossfades. Add a comment in the :root duration block documenting the MOTION-LD-06 resolution from RESEARCH § Focus Transitions: focus rings resolve through `--duration-instant` (90ms, satisfies ≤100ms); row background/color hover stays on `--duration-fast` (150ms). Do NOT change focus utilities in HEEx in this plan (that is Plan 03's HEEx work); this task only establishes the token + documents the binding. Do not introduce any layout-property transition (MOTION-LD-10 — MOTION-GATE from Plan 01 enforces this).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q -- '--ease-symmetric' assets/css/app.css && grep -q 'motion-tab-swap' assets/css/app.css && grep -A2 'motion-tab-swap' assets/css/app.css | grep -q 'ease-symmetric'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--ease-symmetric:' mailglass_admin/assets/css/app.css` returns ≥ 1
    - The `.motion-tab-swap` rule references `var(--ease-symmetric)` (not `var(--ease-out)`): `grep -A2 'motion-tab-swap {' mailglass_admin/assets/css/app.css | grep -q 'var(--ease-symmetric)'`
    - No new layout-property transition introduced — `cd mailglass_admin && scripts/check-conformance.sh` exits 0
    - A comment near the :root duration tokens names the MOTION-LD-06 focus→`--duration-instant` / hover→`--duration-fast` resolution
  </acceptance_criteria>
  <done>--ease-symmetric token added (aliasing --ease-in-out), tab-swap resolves through it, MOTION-LD-06 duration resolution documented; conformance clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add connection-state loading skeleton + CSS-only View-Transitions PE; rebuild and commit bundle</name>
  <read_first>
    - mailglass_admin/assets/css/app.css (the file being modified — reduced-motion @media block ~292-300; add new rules in the motion-vocabulary region)
    - .planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md (§ Loading Skeletons — connection-state placeholder, opacity-only, NOT a shimmer; § View Transitions — CSS-only cross-document, reduced-motion gated; Pitfall 1 bundle rebuild)
    - .planning/research/v1.11/MOTION.md (MOTION-LD-09 reduced-motion; MOTION-LD-10 prohibited properties — opacity-only)
    - mailglass_admin/docs/design-system.md (motion vocabulary, asset-URL limitation ~162-178 — source-of-truth)
    - mailglass_admin/mix.exs (verify.preview alias ~183-188 — runs compile + test + assets.build + git diff --exit-code priv/static/)
  </read_first>
  <action>
    Add a `.mg-skeleton` connection-state placeholder to app.css per RESEARCH § Loading Skeletons: opacity-only, NO shimmer/pulse keyframe, NO layout-property animation. The honest implementation is `.mg-skeleton { opacity: 1; }`, hidden once connected via the LiveView body connection-state hook — `[phx-connected] .mg-skeleton { display: none; }` and (optionally) `[phx-disconnected] .mg-skeleton { opacity: 1; }`. FIRST confirm the exact attribute/class form (`phx-connected` attribute vs class) against the bundled `priv/static/` phoenix_live_view runtime (RESEARCH Assumption A1); if the form differs, use the confirmed selector. The real "content settled into place" signal remains `.motion-reveal` on content arrival — document in a comment that the skeleton is a deliberately minimal connection-state placeholder (no fetch shimmer; the LiveViews render synchronously). Add the cross-document View-Transitions progressive enhancement: a `@view-transition { navigation: auto; }` block wrapped in `@media (prefers-reduced-motion: no-preference) { ... }` (RESEARCH § View Transitions — the no-preference wrapper is REQUIRED, not optional, because the global reduce block does not auto-cover VT pseudo-elements). Add a comment that VT is inert for LiveView WS patches and only cross-fades full hard-navigation document loads (set expectations). Then rebuild the bundle: `cd mailglass_admin && mix mailglass_admin.assets.build`, and commit the rebuilt `priv/static/app.css` (Pitfall 1 — bundle MUST be committed bit-clean). Verify with the verify.preview gate.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q 'mg-skeleton' assets/css/app.css && grep -B3 'view-transition' assets/css/app.css | grep -q 'no-preference' && mix mailglass_admin.assets.build && git diff --exit-code priv/static/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mg-skeleton' mailglass_admin/assets/css/app.css` returns ≥ 1 and the rule animates opacity/display only (no height/width/max-height/transform-of-layout)
    - `.mg-skeleton` contains NO `@keyframes` shimmer/pulse reference (grep for a pulse keyframe name returns 0)
    - `@view-transition` appears inside a `@media (prefers-reduced-motion: no-preference)` block: `grep -B5 '@view-transition' mailglass_admin/assets/css/app.css | grep -q 'prefers-reduced-motion: no-preference'`
    - `cd mailglass_admin && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh` exit 0
    - `cd mailglass_admin && mix mailglass_admin.assets.build && git diff --exit-code priv/static/` exits 0 (bundle rebuilt + clean)
    - `npx playwright test e2e/structural.spec.js -g "reduced-motion"` still passes (the new computed-duration assertion from 102-01 holds)
  </acceptance_criteria>
  <done>.mg-skeleton connection placeholder (opacity-only, no shimmer) and reduced-motion-gated @view-transition PE added; bundle rebuilt + committed bit-clean; conformance + reduced-motion structural assertion green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only changes (easing token, skeleton placeholder, VT at-rule) plus a compiled-bundle rebuild. No data flows through CSS; no route, input, or auth surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02 | Information Disclosure | @view-transition / .mg-skeleton | accept | VT and skeletons add no routes and expose no data (RESEARCH § Security Domain). The skeleton placeholder renders no PII; motion carries no data. |
| T-102-NB | — | motion/CSS surface | accept | No new trust boundary; PII-minimization (`mask_recipient/1`) and multi-tenant scoping untouched (no lib runtime code changes). |
</threat_model>

<verification>
- `cd mailglass_admin && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh` — exit 0 (MOTION-GATE clean: no layout-property transition, no stray ease-in)
- `cd mailglass_admin && mix mailglass_admin.assets.build && git diff --exit-code priv/static/` — exits 0
- `cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "reduced-motion"` — passes
</verification>

<success_criteria>
- --ease-symmetric token added; tab-swap resolves through it (MOTION-LD-05)
- MOTION-LD-06 focus→--duration-instant resolution documented in app.css
- .mg-skeleton connection-state placeholder (opacity-only, no shimmer) added
- @view-transition PE added, wrapped in prefers-reduced-motion: no-preference (MOTION-02)
- Bundle rebuilt + committed bit-clean
</success_criteria>

<output>
Create `.planning/phases/102-motion-micro-interaction-pass/102-02-SUMMARY.md` when done
</output>
