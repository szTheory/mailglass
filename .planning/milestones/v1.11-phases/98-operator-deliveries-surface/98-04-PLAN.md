---
phase: 98-operator-deliveries-surface
plan: 04
type: execute
wave: 3
depends_on: ["98-02", "98-03"]
files_modified:
  - mailglass_admin/test/support/operator_fixtures.ex
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/e2e/operator.spec.js
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements: [FLOW-01, FLOW-02, PAGE-02, RESP-01, A11Y-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Every operator state (happy, no-tenant, filtered-empty, truly-empty, detail-error, active-suppression, :suppressed row) is reachable from the ONE seeded dataset by URL params"
    - "The new :suppressed delivery row is timed hours_ago(7)+ so it appends LAST and the existing deliveryRow indices 0-3 stay timestamp-pinned (all 5 existing replay/selection specs stay green)"
    - "Playwright structural assertions confirm the per-state cells (detail-error, filtered-empty, truly-empty, suppressed badge) and the master-detail computed grid-template-columns at 390/768/1440"
    - "The rebuilt app.css bundle is committed and git diff --exit-code priv/static/ is clean (verify.preview green)"
  artifacts:
    - path: "mailglass_admin/test/support/operator_fixtures.ex"
      provides: "extended single-seed reaching all states; :suppressed row timed hours_ago(7)+"
      contains: "status: :suppressed"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "per-state + responsive grid + a11y operator assertions (no new LLM baseline cells)"
      contains: "operator-detail-error"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "rebuilt committed bundle"
      contains: ""
  key_links:
    - from: "operator_fixtures.ex :suppressed row"
      to: "deliveryRow index stability"
      via: "hours_ago(7)+ append-last timing"
      pattern: "hours_ago\\([7-9]"
    - from: "structural.spec.js"
      to: "operator-master-detail computed grid-template-columns"
      via: "setViewportSize 768/1440 + getComputedStyle"
      pattern: "grid-template-columns"
---

<objective>
Make every operator state reachable from the single seeded dataset by URL params
(FLOW-01), validate the audit-why-a-delivery-failed JTBD end-to-end (FLOW-02), add
Playwright structural assertions for the per-state matrix + responsive grid + a11y
(PAGE-02/RESP-01/A11Y-01) WITHOUT adding LLM-baseline cells (the 36-cell baseline
is frozen), and rebuild + commit the app.css bundle so verify.preview stays green.

Purpose: this is the verification + reachability wave. It depends on the final
markup/testids from Plans 02 and 03. The seed is the single source of truth for
the e2e harness — extend it in place, never add a second dataset, and preserve the
timestamp-pinned row ordering the existing specs depend on.

Output: extended seed; new structural/operator assertions; committed bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/98-operator-deliveries-surface/98-RESEARCH.md
@.planning/phases/98-operator-deliveries-surface/98-UI-SPEC.md
@.planning/phases/98-operator-deliveries-surface/98-VALIDATION.md
@.planning/RATCHET-GAP-REGISTER.md

<interfaces>
<!-- Verified seed + e2e harness facts. -->

operator_fixtures.ex seed_browser_scenario!/0 (lines 12-153):
  Row ordering by last_event_at (list renders newest-first):
    index 0 = browser-selected (hours_ago 1) — has suppression already seeded (lines 38-44)
    index 1 = browser-noop (hours_ago 2)
    index 2 = browser-ambiguous (hours_ago 2)
    index 3 = browser-exact (hours_ago 2)
    index 4 = browser-other / failed sendgrid (hours_ago 6)
    (inbound record hours_ago 10 — separate surface)
  Helpers: insert_delivery!/1 (defaults :169-187), insert_suppression!/1 (:268), insert_event!/2,
    hours_ago/1 (:447). @tenant_id "browser-tenant" (:10). reset!/0 truncates (:155).
  insert_delivery! defaults stream: :transactional, status: :sent.

e2e harness:
  operator.spec.js deliveryRow(page, index) (:9) indexes operator-delivery-row; tests click
    nth(0)/nth(1)/nth(2)/nth(3) (:52/:108/:140/:168 etc). openOperator (:13-31) resets + logs in via
    /ops/browser-reset + /ops/browser-login, then /ops/mail?...&view=deliveries.
  structural.spec.js openOperator mirror (:18-30); accent allowlist + FACT 1-6 blocks.
  Both drive the surface via /ops/mail (the :ops auth mount).

D-04 State Coverage (how each state is reached — no new seed entry point):
  no-tenant: /ops/mail (no tenant_id) → load_deliveries returns [] (operator_live.ex:504)
  filtered-empty: ?tenant_id=browser-tenant&view=deliveries&status=bounced (no bounced rows)
  truly-empty: ?tenant_id=<empty-tenant-with-no-rows>&view=deliveries (a tenant present but no rows)
  detail-error (:not_found): ?tenant_id=browser-tenant&view=deliveries&delivery_id=<nonexistent>
  active-suppression: select index 0 (browser-selected — suppression seeded)
  :suppressed row: ADD insert_delivery!(%{status: :suppressed, ...}) timed hours_ago(7)+ (appends last)

CRITICAL row-index rule (Research §Seed / Pitfall 2): the new :suppressed row MUST be timed
  hours_ago(7) or later so it sorts AFTER browser-other (hours_ago 6) at index >=5; otherwise it
  inserts mid-list and breaks the 5 existing index-pinned specs. Re-run operator.spec.js to confirm
  indices 0-3 unchanged.

Bundle gate: mix verify.preview (mix.exs:183-188) runs compile → test → mailglass_admin.assets.build
  → git diff --exit-code priv/static/. Rebuild via `mix mailglass_admin.assets.build` and COMMIT
  priv/static/app.css.
</interfaces>

<resolved_decisions>
- **truly-empty vs filtered-empty distinction in seed:** truly-empty is reached by a tenant that has
  rows seeded? No — truly-empty means "tenant present, zero deliveries ever". Reach it with a tenant
  that has NO seeded rows (e.g. ?tenant_id=browser-empty&view=deliveries) so @filter_params has a
  non-blank tenant but load_deliveries returns []. filters_active? is FALSE there (only window_hours
  differs from default), so DeliveriesList renders COPY-LD-02 (truly-empty, no reset). filtered-empty
  uses ?tenant_id=browser-tenant&status=bounced → filters_active? TRUE → COPY-LD-01 + reset. No seed
  change needed for either empty state; the seed change is ONLY the :suppressed row.
- **No new LLM baseline cells:** all new assertions go to structural.spec.js / operator.spec.js /
  ExUnit. Do NOT touch docs/ui-baseline-scores.json or ratchet_baseline_test.exs (frozen 36-cell).
</resolved_decisions>

<source_audit>
GOAL: FLOW-01 (URL-reachable seed states), FLOW-02 (audit-why-failed JTBD e2e), PAGE-02 (state
  coherence asserted), RESP-01 (390/768/1440 grid asserted), A11Y-01 (focus/ARIA/one-h1/touch asserted).
REQ: FLOW-01, FLOW-02, PAGE-02, RESP-01, A11Y-01.
RESEARCH: seed extension table (Architecture Patterns); row-index stability (Pitfall 2); Validation
  Architecture map; Wave 0 gaps (structural assertions, ExUnit empty-state, seed :suppressed).
CONTEXT: D-04 (single-seed URL reachability), D-08 (testids + bundle commit).
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the single seed with a :suppressed row (append-last) + ExUnit ordering snapshot</name>
  <files>mailglass_admin/test/support/operator_fixtures.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/test/support/operator_fixtures.ex (full file; lines 12-153 seed; 169-187 insert_delivery!; 447 hours_ago)
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (test idiom)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (seed extension table; Pitfall 2 row-index stability)
    - .planning/phases/98-operator-deliveries-surface/98-UI-SPEC.md (State Coverage matrix)
  </read_first>
  <action>
    Cite GAP-09 (seed-state-coverage) as the anti-churn anchor. In seed_browser_scenario!/0, add ONE
    new delivery via insert_delivery!(%{recipient: "browser-suppressed@example.com", provider:
    "postmark", provider_message_id: "pm_browser_suppressed", status: :suppressed, last_event_type:
    :suppressed, last_event_at: hours_ago(8)}) — timed hours_ago(8) so it sorts AFTER browser-other
    (hours_ago 6) and appends LAST in the list (index >= 5). Use @tenant_id (browser-tenant) and a
    non-PII @example.com address (mask_recipient applies on display). Do NOT change the timing of any
    existing row. Do NOT add a second seed entry point or per-state script (D-04). Add an ExUnit test
    asserting the seeded deliveries ordering: index 0 recipient is browser-selected and the LAST row is
    browser-suppressed with status :suppressed (snapshot guard for Pitfall 2).
  </action>
  <acceptance_criteria>
    - operator_fixtures.ex contains `status: :suppressed` with `last_event_at: hours_ago(8)` (or any >=7)
    - the existing rows' hours_ago timings are byte-unchanged (git diff shows only additions)
    - new ExUnit test asserts browser-selected is first and browser-suppressed is last with status :suppressed
    - `mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` green
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && grep -q 'status: :suppressed' test/support/operator_fixtures.ex && grep -Eq 'hours_ago\([7-9]' test/support/operator_fixtures.ex && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Seed reaches the :suppressed state, appended last; ordering snapshot guards row-index stability.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright per-state + responsive-grid + a11y assertions (no new baseline cells)</name>
  <files>mailglass_admin/e2e/structural.spec.js, mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (openOperator mirror :18-30; FACT 1-6 blocks)
    - mailglass_admin/e2e/operator.spec.js (deliveryRow :9; openOperator :13-31; existing index-pinned tests)
    - .planning/phases/98-operator-deliveries-surface/98-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (Validation Architecture map; anti-pattern: no new baseline keys)
  </read_first>
  <action>
    Cite GAP-09 (seed-state-coverage) as the anti-churn anchor. In structural.spec.js add assertions
    (extend, do NOT add LLM-baseline cells):
    - PAGE-02 per-state: navigate /ops/mail?tenant_id=browser-tenant&view=deliveries&delivery_id=does-not-exist
      and assert getByTestId("operator-detail-error") visible; navigate
      &status=bounced and assert getByTestId("operator-empty-filtered") visible (COPY-LD-01 + a
      clear_filters button); navigate ?tenant_id=browser-empty&view=deliveries and assert
      getByTestId("operator-empty-truly") visible with NO clear_filters button; filter
      ?status=suppressed and assert a delivery-row status badge contains the neutral fallback (badge-outline / "Unknown").
    - RESP-01: at setViewportSize 768 then 1440, read getByTestId("operator-master-detail") computed
      grid-template-columns via evaluate(getComputedStyle) and assert two non-zero column tracks whose
      ratio is ~40/60 at 768 and ~33/67 at 1440 (allow a small tolerance); at 390 assert the master list
      is full-width when unselected and that selecting a row hides the list (max-md:hidden) and shows
      operator-detail-back.
    - A11Y-01: assert exactly one getByRole("heading", { level: 1 }) on the deliveries page; assert the
      operator-filters-toggle and operator-detail-back controls meet >=44px height (boundingBox.height >= 44).
    In operator.spec.js add the FLOW-02 extension: select the failed-sendgrid row (browser-other,
    confirm its index after the seed change — it should remain index 4) and assert its status badge
    shows "Failed" / badge-error, exercising the audit-why-a-delivery-failed inspection. Do NOT change
    deliveryRow indices 0-3. Do NOT modify docs/ui-baseline-scores.json or ratchet_baseline_test.exs.
  </action>
  <acceptance_criteria>
    - structural.spec.js references operator-detail-error, operator-empty-filtered, operator-empty-truly, operator-master-detail (grid-template-columns), operator-detail-back, operator-filters-toggle
    - structural.spec.js asserts exactly one level-1 heading on the deliveries page
    - operator.spec.js adds a failed-row inspection assertion (FLOW-02)
    - docs/ui-baseline-scores.json and test/mailglass_admin/ratchet_baseline_test.exs are unchanged
    - the existing index-pinned operator.spec.js tests are not renumbered (indices 0-3 unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && grep -q 'operator-detail-error' e2e/structural.spec.js && grep -q 'operator-empty-truly' e2e/structural.spec.js && grep -q 'grid-template-columns' e2e/structural.spec.js && grep -q 'operator-detail-back' e2e/structural.spec.js && git diff --quiet docs/ui-baseline-scores.json test/mailglass_admin/ratchet_baseline_test.exs && echo OK</automated>
  </verify>
  <done>Per-state, responsive-grid, and a11y assertions added without touching the frozen baseline; FLOW-02 failed-row inspection covered.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild + commit the app.css bundle; verify.preview bundle-clean gate</name>
  <files>mailglass_admin/priv/static/app.css</files>
  <read_first>
    - mailglass_admin/mix.exs (lines 183-188 verify.preview alias)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (Build artifacts / bundle-clean gate; Common Pitfalls)
    - .planning/STATE.md (D-08 bundle-clean gate)
  </read_first>
  <action>
    Cite GAP-06 (the IA markup change that altered class strings) as the anti-churn anchor. Run
    `cd mailglass_admin && mix mailglass_admin.assets.build` to regenerate priv/static/app.css from the
    new class strings introduced in Plans 02/03/this plan, then COMMIT priv/static/app.css. Run
    `mix verify.preview` and confirm it exits 0 (its final step is git diff --exit-code priv/static/).
    Do NOT run `mix compile --no-optional-deps --force` on the shared main _build during the gate
    (MEMORY: no-optional-deps build pollution); verify.preview already runs the correct compile step.
    If verify.preview reds on the pre-existing voice_test "Oops" dep-JS noise or the ~57 unrelated Oban
    failures, scope the assertion to the bundle-clean step: `mix mailglass_admin.assets.build` then
    `git diff --exit-code priv/static/` returning 0.
  </action>
  <acceptance_criteria>
    - `mix mailglass_admin.assets.build` followed by `git diff --exit-code priv/static/` returns 0 (bundle committed, no drift)
    - priv/static/app.css reflects the new class strings (committed)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix mailglass_admin.assets.build && git diff --exit-code priv/static/ && echo OK</automated>
  </verify>
  <done>Bundle rebuilt + committed; git diff --exit-code priv/static/ clean; verify.preview bundle gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed → test DB (TestRepo) | seed writes only to the truncated test DB; never prod data |
| URL query params → state reachability | per-state nav via ?delivery_id / ?status / ?tenant_id (existing normalized inputs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-09 | Information Disclosure | seed recipient data / PII | mitigate | New :suppressed row uses a non-PII @example.com address; display masks via mask_recipient/1; @tenant_id scoping preserved (V4 Access Control) |
| T-98-10 | Information Disclosure | detail-error reveal path | accept | detail_error_for/2 returns generic :not_found; the nonexistent delivery_id is not echoed beyond the existing pattern (V7) |
| T-98-11 | DoS | ?status= atom-table exhaustion | accept | cast_enum/2 uses String.to_existing_atom + allowlist membership (already correct); not weakened |
| T-98-12 | Tampering | frozen LLM baseline | mitigate | No new baseline cells; docs/ui-baseline-scores.json + ratchet_baseline_test.exs left unchanged (asserted) |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` green
- Playwright `e2e/operator.spec.js e2e/structural.spec.js` green at 390/768/1440 (per-state + grid + a11y)
- `mix mailglass_admin.assets.build && git diff --exit-code priv/static/` returns 0
- docs/ui-baseline-scores.json + ratchet_baseline_test.exs unchanged (frozen 36-cell)
</verification>

<success_criteria>
- Every operator state reachable from the single seed by URL; row indices 0-3 unchanged
- Per-state + responsive-grid + a11y structural assertions pass; FLOW-02 JTBD validated
- Bundle committed and bundle-clean gate green; no new LLM baseline cells
</success_criteria>

<output>
Create `.planning/phases/98-operator-deliveries-surface/98-04-SUMMARY.md` when done
</output>
