---
phase: 94
plan: 03
type: execute
wave: 2
depends_on:
  - "94-01"
  - "94-02"
files_modified:
  - mailglass_admin/assets/css/app.css
  - mailglass_admin/priv/static/app.css
  - mailglass_admin/test/mailglass_admin/token_parity_test.exs
  - mailglass_admin/test/mailglass_admin/brand_test.exs
  - mailglass_admin/mix.exs
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-04
  - TOKEN-05

must_haves:
  truths:
    - "app.css opens with @import \"../../../brandbook/tokens.css\" before @import tailwindcss"
    - "Every --color-* line in both daisyUI theme blocks references var(--mg-*) — no raw hex literals remain in theme selectors"
    - "Seven token-role corrections land: base-200 light→surface-raised, base-300 light→border, accent light→Glass, info light→info-solid, base-300 dark→border, secondary dark→text-muted, error dark→error-solid (plus secondary-content/accent/accent-content/neutral/neutral-content dark fixes)"
    - "All -content slots explicitly set (no daisyUI auto-derivation)"
    - "priv/static/app.css is rebuilt by mix mailglass_admin.assets.build and committed"
    - "mix verify.preview exits 0 (git diff --exit-code priv/static/ is clean)"
    - "token_parity_test.exs structural + value-equality assertions pass GREEN against the rebuilt bundle"
    - "The parity test runs GREEN in the support_contract_admin CI lane (required, branch-protection)"
    - "brand_test.exs three assertions use var(--mg-*) wiring form and pass GREEN against the rebuilt bundle"
    - "mix verify.support_contract.admin exits 0 (parity test included)"
    - "No HEEx component markup changed in this phase"
  artifacts:
    - path: "mailglass_admin/assets/css/app.css"
      provides: "@import of brandbook/tokens.css + full var(--mg-*) rewrite of both theme blocks"
      contains: "@import \"../../../brandbook/tokens.css\""
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt compiled bundle with --mg-* declarations inlined"
      contains: "--mg-color-background"
    - path: "mailglass_admin/test/mailglass_admin/token_parity_test.exs"
      provides: "Fail-closed parity test (TOKEN-04; structural TOKEN-01)"
      exports: ["MailglassAdmin.TokenParityTest"]
    - path: "mailglass_admin/test/mailglass_admin/brand_test.exs"
      provides: "Updated 3 assertions (TOKEN-01/02) — var+hex two-tier form, green against rebuilt bundle"
    - path: "mailglass_admin/mix.exs"
      provides: "token_parity_test.exs in verify.support_contract.admin alias"
      contains: "token_parity_test.exs"
  key_links:
    - from: "mailglass_admin/assets/css/app.css @import"
      to: "brandbook/tokens.css"
      via: "../../../brandbook/tokens.css (3 levels up from assets/css/, build cwd = mailglass_admin/)"
    - from: "priv/static/app.css [data-theme=mailglass-light]"
      to: "--mg-color-*"
      via: "var(--mg-*) references resolved at browser render time"
    - from: "token_parity_test.exs"
      to: "priv/static/app.css + brandbook/tokens.json"
      via: "compiled-bundle seam + oracle compare — GREEN after rebuild"
    - from: "mix.exs verify.support_contract.admin"
      to: "token_parity_test.exs"
      via: "test path string in alias"
---

<objective>
Perform the full atomic re-baseline: rewrite app.css, rebuild the compiled bundle, then create token_parity_test.exs and update brand_test.exs + mix.exs alias — all in the same plan so the plan's end state is fully GREEN on every required CI lane. This plan merges as one green unit; no intermediate commit leaves main red on a required lane.

Purpose: D-01 + D-02 + D-03 + D-09 + D-10 from CONTEXT.md. The @import is the single source of truth; the slot rewrite corrects seven documented token-role bugs; the parity test and brand_test updates land WITH the rewrite that makes them green; the bundle commit is the only adopter-visible artifact.

Output: mailglass_admin/assets/css/app.css (source, two theme blocks fully rewritten), mailglass_admin/priv/static/app.css (rebuilt compiled bundle, committed), token_parity_test.exs (new, green), brand_test.exs (three assertions updated, green), mix.exs (support_contract alias updated). No HEEx files touched. mix verify.preview + mix verify.support_contract.admin both exit 0.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md

<interfaces>
<!-- Full slot mapping — the authoritative rewrite table for app.css -->
<!-- Source: 94-RESEARCH.md §Full Per-Slot daisyUI Mapping Table (VERIFIED 2026-06-13) -->

mailglass-LIGHT theme — full --color-* rewrite:
  --color-base-100: var(--mg-color-background)          /* was #F8FBFD — no value change, role correct */
  --color-base-200: var(--mg-color-surface-raised)      /* was #EAF6FB (Mist) — CORRECTED to #FFFFFF */
  --color-base-300: var(--mg-color-border)              /* was #A6EAF2 (Ice) — CORRECTED to #C7DCE5 */
  --color-base-content: var(--mg-color-text)            /* was #0D1B2A — no value change */
  --color-primary: var(--mg-color-accent)               /* was #277B96 — no value change */
  --color-primary-content: var(--mg-color-background)   /* was #F8FBFD — no value change */
  --color-secondary: var(--mg-color-text-muted)         /* was #5C6B7A — no value change */
  --color-secondary-content: var(--mg-color-background) /* was #F8FBFD — no value change */
  --color-accent: var(--mg-color-accent)                /* was #0D1B2A (Ink!) — CORRECTED to Glass #277B96 */
  --color-accent-content: var(--mg-color-text-inverse)  /* was #F8FBFD — reclassified */
  --color-neutral: var(--mg-color-text-muted)           /* was #5C6B7A — no value change */
  --color-neutral-content: var(--mg-color-background)   /* was #F8FBFD — no value change */
  --color-info: var(--mg-color-info-solid)              /* was #277B96 (Glass) — CORRECTED to #1D637A */
  --color-info-content: var(--mg-color-text-inverse)    /* ADD (was absent) */
  --color-success: var(--mg-color-success-solid)        /* was #166534 — no value change */
  --color-success-content: var(--mg-color-success-on-solid) /* ADD (was absent) */
  --color-warning: var(--mg-color-warning-solid)        /* was #A95F10 — no value change */
  --color-warning-content: var(--mg-color-warning-on-solid) /* ADD (was absent) */
  --color-error: var(--mg-color-error-solid)            /* was #B42318 — no value change */
  --color-error-content: var(--mg-color-error-on-solid) /* ADD (was absent) */

NON-COLOR SLOTS light (keep as literal values — do NOT rewrite to var()):
  --radius-selector: 0.25rem
  --radius-field: 0.25rem
  --radius-box: 0.5rem
  --border: 1px
  --depth: 0
  --noise: 0

mailglass-DARK theme — full --color-* rewrite:
  --color-base-100: var(--mg-color-background)          /* was #0D1B2A — no value change */
  --color-base-200: var(--mg-color-surface-raised)      /* was #152538 — no value change */
  --color-base-300: var(--mg-color-border)              /* was #1F3049 (overlay!) — CORRECTED to #315069 */
  --color-base-content: var(--mg-color-text)            /* was #EAF6FB — no value change */
  --color-primary: var(--mg-color-accent)               /* was #A6EAF2 (Ice) — no value change */
  --color-primary-content: var(--mg-color-text-inverse) /* was #0D1B2A — already passes AA 12.98:1 */
  --color-secondary: var(--mg-color-text-muted)         /* was #5C6B7A (3.18:1!) — CORRECTED to #B8CAD4 */
  --color-secondary-content: var(--mg-color-text-inverse) /* was #F8FBFD — CORRECTED to #0D1B2A on dark fills */
  --color-accent: var(--mg-color-accent)                /* was #277B96 (Glass/light!) — CORRECTED to Ice #A6EAF2 */
  --color-accent-content: var(--mg-color-text-inverse)  /* was #F8FBFD — CORRECTED to #0D1B2A */
  --color-neutral: var(--mg-color-text-muted)           /* was #5C6B7A (3.18:1!) — CORRECTED to #B8CAD4 */
  --color-neutral-content: var(--mg-color-text-inverse) /* was #F8FBFD — CORRECTED to #0D1B2A */
  --color-info: var(--mg-color-info-solid)              /* was #A6EAF2 (Ice) — no value change for dark */
  --color-info-content: var(--mg-color-info-on-solid)   /* ADD (was absent) */
  --color-success: var(--mg-color-success-solid)        /* was #8BB77F — no value change */
  --color-success-content: var(--mg-color-success-on-solid) /* ADD (was absent) */
  --color-warning: var(--mg-color-warning-solid)        /* was #E0A955 — no value change */
  --color-warning-content: var(--mg-color-warning-on-solid) /* ADD (was absent) */
  --color-error: var(--mg-color-error-solid)            /* was #D47368 (OFF-PALETTE!) — CORRECTED to #E29089 */
  --color-error-content: var(--mg-color-error-on-solid) /* ADD (was absent) */

NON-COLOR SLOTS dark (keep as literal values):
  Same as light: --radius-selector, --radius-field, --radius-box, --border, --depth, --noise

@import position: Insert BEFORE @import "tailwindcss" (i.e., the very first line of app.css).
@import comment to include:
  /* Resolves to: <monorepo-root>/brandbook/tokens.css (build cwd = mailglass_admin/)    */
  /* Standalone Tailwind v4.1.12 binary inlines all --mg-* declarations at build time.   */
  /* Adopting apps consume only priv/static/app.css (bundle); brandbook/ excluded from   */
  /* Hex tarball (mix.exs :files). Editing brandbook/tokens.css requires a bundle rebuild.*/

build command (TOKEN-05, D-09): cd mailglass_admin && mix mailglass_admin.assets.build
post-build gate: cd mailglass_admin && mix verify.preview

From mailglass_admin/test/mailglass_admin/brand_test.exs:
- Module: MailglassAdmin.BrandTest
- Compiled-bundle seam (lines 15-23, copy verbatim for token_parity_test.exs):
    @css_path Path.join([Application.app_dir(:mailglass_admin, "priv"), "static", "app.css"])
    setup do; css = File.read!(@css_path); {:ok, css: css}; end
- Lines 44-52 (THREE literal-hex assertions to replace with two-tier var+hex form):
    assert lowered =~ "--color-base-100: #f8fbfd" or lowered =~ "--color-base-100:#f8fbfd"
    assert lowered =~ "--color-primary: #277b96" or lowered =~ "--color-primary:#277b96"
    assert lowered =~ "--color-base-content: #0d1b2a" or lowered =~ "--color-base-content:#0d1b2a"
  Replacement form:
    assert css =~ "--color-base-100: var(--mg-color-background)" or css =~ "--color-base-100:var(--mg-color-background)"
    assert String.downcase(css) =~ "--mg-color-background: #f8fbfd"
    (same two-tier pattern for primary→accent and base-content→text)
- Lines 26-37 ("brand palette hex values" block): DO NOT TOUCH
- Lines 56-65 ("mailglass-dark theme" block): DO NOT TOUCH
- Lines 68-82 ("visual DON'Ts" block): DO NOT TOUCH

Oracle path for token_parity_test.exs:
  @tokens_path Path.expand(Path.join([__DIR__, "..", "..", "..", "brandbook", "tokens.json"]))
  # Three levels up: test/mailglass_admin/ → test/ → mailglass_admin/ → monorepo root → brandbook/

mix.exs verify.support_contract.admin alias (current):
  "verify.support_contract.admin": [
    "test test/mailglass_admin/post_installer_smoke_test.exs ... test/mailglass_admin/auth_test.exs --warnings-as-errors"
  ]
  After: append "test/mailglass_admin/token_parity_test.exs" before "--warnings-as-errors"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mailglass_admin/assets/css/app.css and rebuild the compiled bundle</name>
  <files>
    mailglass_admin/assets/css/app.css
    mailglass_admin/priv/static/app.css
  </files>
  <read_first>
    - mailglass_admin/assets/css/app.css (FULL file read — understand the complete current structure including @theme block, both daisyUI plugin blocks, and any other @import or directives present; identify exact current slot names and order to preserve non-color slots)
    - brandbook/tokens.css (FULL file read — verify all --mg-color-* token names are present that the daisyUI slots reference; confirm light/dark blocks exist; confirm the file is self-contained CSS)
    - mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex (verify the task name and any options)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §app.css
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md §Common Pitfalls (Pitfall 3: @import not inlined = grep --mg-color-background on compiled bundle)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md D-01 + D-03 + D-09
  </read_first>
  <action>
    Edit mailglass_admin/assets/css/app.css. This is a surgical rewrite of the daisyUI theme blocks only — all other parts of the file (@theme block, heroicons-inline @plugin, @layer, @utility declarations) are preserved verbatim.

    Step 1 — add @import at the top of the file (before the existing @import "tailwindcss" line): insert the four-line comment block then `@import "../../../brandbook/tokens.css";` as the first CSS declaration. The resolved path comment is mandatory per the interface block above.

    Step 2 — rewrite mailglass-light theme block: inside the `@plugin "../vendor/daisyui-theme" { name: "mailglass-light"; ... }` block, replace EVERY existing `--color-*: <value>` line with `--color-*: var(--mg-color-*)` per the full mapping in the interfaces block above. Add the four new -content slots (info-content, success-content, warning-content, error-content) that are absent from the current file. Keep ALL non-color slots (--radius-selector, --radius-field, --radius-box, --border, --depth, --noise) as their existing literal values — do NOT convert them to var(). Keep all other daisyUI plugin header declarations (name, default, prefersdark, color-scheme) unchanged.

    Step 3 — rewrite mailglass-dark theme block: same process for `@plugin "../vendor/daisyui-theme" { name: "mailglass-dark"; ... }`. Rewrite all --color-* to var(--mg-*) per the dark mapping above. Add the four absent -content slots. Keep non-color slots as literals.

    Critical correctness check: after the rewrite, scan the theme blocks to confirm NO `--color-*: #` hex literal remains. The only #-containing values permitted are inside comments.

    Step 4 — rebuild the bundle: `cd mailglass_admin && mix mailglass_admin.assets.build`. This runs the standalone Tailwind v4.1.12 binary with --minify, inlining all @import-ed declarations (including brandbook/tokens.css --mg-* block) into priv/static/app.css.

    Step 5 — verify the @import was inlined: `grep '--mg-color-background' mailglass_admin/priv/static/app.css`. If this returns empty, the @import path is wrong — recount the ../ levels in app.css (should be 3) and rebuild. Do NOT proceed until the --mg-* declarations appear in the compiled bundle.

    Step 6 — run the bundle-clean gate: `cd mailglass_admin && mix verify.preview`. Stage `git add mailglass_admin/priv/static/app.css` before this step so the built artifact is committed; verify.preview checks the committed state.

    Step 7 — run the hard conformance gate: `bash mailglass_admin/scripts/check-conformance.sh`. Must exit 0. If it fails due to a HEX-GATE hit (a hex value somehow in a lib/*.ex file), investigate — this task should not have touched any .ex files.

    Do NOT modify the @theme block (which holds type/spacing/elevation/easing custom properties). Do NOT modify any @layer or @utility rule. Do NOT touch app.css if it contains any HEEx-adjacent content (it should not — this is pure CSS).
  </action>
  <verify>
    <automated>grep -n '@import.*brandbook' /Users/jon/projects/mailglass/mailglass_admin/assets/css/app.css</automated>
    <automated>grep -En '--color-[a-z-]+: #[0-9a-fA-F]' /Users/jon/projects/mailglass/mailglass_admin/assets/css/app.css | grep -v ':.*\/\*'</automated>
    <automated>grep -c '\-\-mg-color-background' /Users/jon/projects/mailglass/mailglass_admin/priv/static/app.css</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.preview 2>&1 | tail -5</automated>
    <automated>bash /Users/jon/projects/mailglass/mailglass_admin/scripts/check-conformance.sh && echo "conformance exit: $?"</automated>
  </verify>
  <done>Source app.css rewritten: @import added, all --color-* slots in both themes now reference var(--mg-*), no raw hex remains in theme selectors, non-color slots preserved as literals. priv/static/app.css rebuilt with --mg-* declarations inlined. mix verify.preview exits 0. check-conformance.sh exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create token_parity_test.exs (green against rebuilt bundle)</name>
  <files>
    mailglass_admin/test/mailglass_admin/token_parity_test.exs
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/brand_test.exs (full — copy compiled-bundle seam from lines 15-23; study module header + docstring style)
    - mailglass_admin/test/mailglass_admin/bundle_test.exs (full — study failure message style with self-serve fix)
    - brandbook/tokens.json (read once to understand the W3C token format — palette tier + color.light/dark semantic tier with {palette.x} aliases)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §token_parity_test.exs
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md §token_parity_test seam and §Full Per-Slot daisyUI Mapping Table
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md D-02
  </read_first>
  <action>
    Create mailglass_admin/test/mailglass_admin/token_parity_test.exs as a new file. At this point in task sequence, priv/static/app.css has already been rebuilt with var(--mg-*) references and inlined --mg-* declarations. The tests in this file must be GREEN against the rebuilt bundle.

    Module: MailglassAdmin.TokenParityTest. Use `use ExUnit.Case, async: true`. @tag :token_parity as a convenience selector (not excluded from any CI lane).

    @moduledoc must state: (1) this reads compiled priv/static/app.css; (2) if it fails with "expected var(--mg-*) but got something else" the bundle is stale — run `cd mailglass_admin && mix mailglass_admin.assets.build && git add priv/static/` and commit; (3) if it fails with a value mismatch, brandbook/tokens.json changed without resyncing app.css; (4) never hand-edit priv/static/app.css; (5) `actual` is what currently SHIPS.

    @css_path: copy verbatim from brand_test.exs lines 15-23 (Application.app_dir path construction).

    @tokens_path: `Path.expand(Path.join([__DIR__, "..", "..", "..", "brandbook", "tokens.json"]))` — three levels up from test/mailglass_admin/ to monorepo root.

    setup_all: assert `File.exists?(@tokens_path)` with message "tokens.json not found at #{@tokens_path} — run from mailglass_admin/"; decode the JSON with `Jason.decode!(File.read!(@tokens_path))`. Return `{:ok, tokens: decoded}`.

    setup (per-test): read @css_path via `File.read!` and make available as `%{css: css}` in test context.

    @mapping module attribute: a map of `{theme_name, daisyui_slot} => mg_token_name` encoding the FULL slot table from RESEARCH.md. Include ALL slots for both themes (not just the changed ones — the full map is the slot→role contract; a token rename forces a deliberate edit here).

    Helper: a private `resolve_oracle(tokens, mg_token_name)` function that looks up the token value in the W3C format: check `tokens["color"]["light"][key]` or `tokens["color"]["dark"][key]` based on a convention, resolving `{palette.x}` aliases via `tokens["palette"][x]["$value"]`. The `mg_token_name` maps to the key by stripping `--mg-color-` prefix.

    Structural test ("no raw hex in --color-* lines in theme selectors"): search the compiled CSS for the pattern `[data-theme=mailglass-` to find theme block boundaries, then assert no `--color-[a-z-]+: #[0-9a-fA-F]` raw hex form appears within those theme blocks. Failure message: "TOKEN-01 violation: raw hex literal on a --color-* line inside a daisyUI theme block. All --color-* values must reference var(--mg-*). Run mix mailglass_admin.assets.build."

    Value-equality test: iterate the @mapping, for each `{theme, slot} => mg_token`, find the daisyUI theme block in the compiled CSS, extract the value for that slot, assert it equals `var(#{mg_token})` (either spaced or unspaced colon form). Then resolve mg_token against the inlined --mg-* declarations in the CSS and compare to the oracle value from tokens.json. Collect ALL mismatches before asserting (build a list, assert the list is empty with the full list as the message).

    Failure message per mismatch: "#{theme} #{slot}: expected var(#{mg_token}); actual: #{actual_value}. Bundle is stale or app.css hand-edited — run mix mailglass_admin.assets.build"
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix compile 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>token_parity_test.exs created with full @mapping, structural assertion, and value-equality assertions. mix compile exits 0. mix test exits 0 — ALL parity assertions GREEN against the rebuilt bundle.</done>
</task>

<task type="auto">
  <name>Task 3: Update brand_test.exs three assertions and add token_parity_test.exs to mix.exs support_contract alias</name>
  <files>
    mailglass_admin/test/mailglass_admin/brand_test.exs
    mailglass_admin/mix.exs
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/brand_test.exs (full — identify lines 44-52 exactly; confirm untouched sections at lines 26-37, 56-65, 68-82)
    - mailglass_admin/mix.exs lines 187-196 (verify.support_contract.admin alias current form)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §brand_test.exs and §mix.exs
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md §brand_test.exs seam
  </read_first>
  <action>
    In brand_test.exs, replace the three literal-hex assertions at approximately lines 44-52 with the two-tier var+hex form. Each assertion becomes a pair: (a) assert `var(--mg-*)` reference in the theme block, (b) assert the resolved hex from the inlined --mg-* declarations. The three replacements:

    --color-base-100 / Paper: replace with assertion for `--color-base-100: var(--mg-color-background)` (or without space) AND assert `String.downcase(css) =~ "--mg-color-background: #f8fbfd"` with message `--mg-color-background token (#F8FBFD Paper) must be inlined in compiled CSS`.

    --color-primary / Glass: replace with assertion for `--color-primary: var(--mg-color-accent)` AND assert `String.downcase(css) =~ "--mg-color-accent: #277b96"` with message `--mg-color-accent token (#277B96 Glass) must be inlined in compiled CSS`.

    --color-base-content / Ink: replace with assertion for `--color-base-content: var(--mg-color-text)` AND assert `String.downcase(css) =~ "--mg-color-text: #0d1b2a"` with message `--mg-color-text token (#0D1B2A Ink) must be inlined in compiled CSS`.

    Check the actual test context binding from the file (the existing tests may use `lowered` or `css` directly) and use whichever binding is correct. These assertions are GREEN against the already-rebuilt bundle (the @import inlines the --mg-* declarations; the theme blocks now use var(--mg-*) references).

    Lines 26-37 ("brand palette hex values"), lines 56-65 ("mailglass-dark theme"), and lines 68-82 ("visual DON'Ts") MUST NOT be changed.

    In mix.exs, update the `verify.support_contract.admin` alias string at lines 189-191: append `test/mailglass_admin/token_parity_test.exs` to the space-separated test path list, before `--warnings-as-errors`. Preserve the exact string format (single string in a list).
  </action>
  <verify>
    <automated>grep -n 'mg-color-background\|mg-color-accent\|mg-color-text' /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/brand_test.exs</automated>
    <automated>grep -n 'token_parity_test' /Users/jon/projects/mailglass/mailglass_admin/mix.exs</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix compile 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/brand_test.exs 2>&1 | tail -10</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.support_contract.admin 2>&1 | tail -10</automated>
  </verify>
  <done>brand_test.exs three assertions updated to two-tier var+hex form — GREEN against the rebuilt bundle. mix.exs verify.support_contract.admin alias includes token_parity_test.exs. mix compile exits 0. mix test brand_test.exs exits 0. mix verify.support_contract.admin exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @import "../../../brandbook/tokens.css" | Resolves only at MAINTAINER build time (mix mailglass_admin.assets.build); not an adopter-visible path |
| priv/static/app.css | Pre-resolved bundle committed to git; adopters consume only this file; brandbook/ excluded from Hex tarball (mix.exs :files) |
| test suite → priv/static/app.css | File read at test time; no exec; path is repo-local |
| test suite → brandbook/tokens.json | File read via __DIR__-relative path; JSON parsed with Jason; no exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | Tampering | cross-package @import path traversal | accept | Path resolves only at maintainer build time; brandbook/ is in the same monorepo, not an external supply-chain artifact; adopters never see the @import or brandbook/; no path-traversal exposure to consumers |
| T-94-08 | Tampering | priv/static/app.css (committed bundle) | mitigate | verify.preview gate enforces git diff --exit-code priv/static/ — stale or hand-edited bundle breaks CI; parity test reads the committed artifact so stale bundle is self-reporting |
| T-94-09 | Spoofing | daisyUI var(--mg-*) resolution at browser render time | accept | Custom property resolution is standard CSS cascade; no eval/exec; tokens are color values only (no PII, no secrets) |
| T-94-04 | Tampering | token_parity_test.exs reading priv/static/app.css | accept | File is repo-committed; tests are read-only; no write path |
| T-94-05 | Information Disclosure | brandbook/tokens.json oracle path via __DIR__ | accept | tokens.json contains only color hex values — no PII, no secrets; file is repo-committed |
| T-94-06 | Denial of Service | parity test fails on stale bundle blocking CI | accept | This is intentional fail-closed behavior (TOKEN-04 requirement); the failure message names the self-serve fix |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; Tailwind binary pinned at v4.1.12 in config.exs; Jason is an existing test dep |

**Assessment: LOW threat surface.** The cross-package @import is the one notable architectural decision — assessed as no supply-chain risk because brandbook/ is in the same git repo at maintainer scope, not an external dependency. Adopters receive only the pre-resolved priv/static/app.css in the Hex tarball.
</threat_model>

<verification>
Final phase-level verification (all three plans complete):

```
# Gates
bash mailglass_admin/scripts/check-conformance.sh
# exit 0, "OK: design-system conformance clean."

bash mailglass_admin/scripts/check-conformance-advisory.sh
# exit 0, WARN lines for TYPE/TRACK violations (advisory only)

bash scripts/check_motion_conformance.sh
# exit 0, "OK: motion conformance clean."

# Tests — all GREEN
cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs
# exit 0

cd mailglass_admin && mix test test/mailglass_admin/brand_test.exs
# exit 0

cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs
# exit 0

# Bundle-clean + full suite
cd mailglass_admin && mix verify.preview
# exit 0 (includes git diff --exit-code priv/static/ clean)

# Required CI lane
cd mailglass_admin && mix verify.support_contract.admin
# exit 0 (token_parity_test.exs included, passes)

# No markup changed
git diff --name-only HEAD~3..HEAD | grep -v 'mailglass_admin/assets/css/app.css\|mailglass_admin/priv/static/app.css\|mailglass_admin/test/\|mailglass_admin/scripts/\|mailglass_admin/mix.exs\|scripts/check_motion_conformance.sh\|.github/workflows/ci.yml'
# no matches (only expected files changed)
```
</verification>

<success_criteria>
- mailglass_admin/assets/css/app.css opens with @import "../../../brandbook/tokens.css" (3 levels up)
- Both daisyUI theme blocks contain only var(--mg-*) references on --color-* lines; no raw hex
- Seven documented token-role corrections applied (base-200 light, base-300 light, accent light, info light, base-300 dark, secondary dark, error dark) plus secondary-content/accent/accent-content/neutral/neutral-content dark
- All -content slots explicitly set in both themes
- priv/static/app.css rebuilt and committed; mix verify.preview exits 0
- token_parity_test.exs GREEN: structural assertion passes (no raw hex in compiled theme blocks), value-equality assertions pass (all @mapping slots resolve to oracle values from tokens.json)
- brand_test.exs three assertions GREEN: var(--mg-*) wiring + resolved hex both present in compiled CSS
- mix verify.support_contract.admin exits 0 (parity test included and green)
- accessibility_test.exs exits 0 (from Wave 1; unchanged in this plan)
- Zero .ex files in mailglass_admin/lib/ modified
- commit message: `feat(admin): re-baseline app.css onto brandbook tokens`
</success_criteria>

<validation>
Dimension 8 proof commands:

| Requirement | Command | Expected result |
|-------------|---------|-----------------|
| TOKEN-01 (no raw hex, --mg-* inlined) | `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs` | exit 0 (structural assertion green) |
| TOKEN-02 (light/dark slot remap) | `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs test/mailglass_admin/brand_test.exs` | exit 0 |
| TOKEN-04 (parity oracle + CI lane) | `cd mailglass_admin && mix verify.support_contract.admin` | exit 0 |
| TOKEN-05 (bundle rebuilt + committed) | `cd mailglass_admin && mix verify.preview` | exit 0, git diff --exit-code priv/static/ clean |
| SC-4 (zero markup changes) | `git diff --name-only HEAD \| grep 'mailglass_admin/lib'` | no output |
</validation>

<output>
Create `.planning/phases/94-token-re-baseline-onto-canonical-brand/94-03-SUMMARY.md` when done.
</output>
