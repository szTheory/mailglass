---
phase: 94
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/test/mailglass_admin/accessibility_test.exs
autonomous: true
requirements:
  - TOKEN-03

must_haves:
  truths:
    - "Five new contrast assertions in accessibility_test.exs prove dark muted (#B8CAD4) and error (#E29089) pass AA 4.5:1 on Ink, primary-content Ink on Ice is pinned ≥4.5:1, and both border roles are pinned sub-3:1 with a decorative-exemption docstring"
    - "All five new tests are GREEN immediately (they call contrast_ratio/2 on hardcoded hex literals, independent of the compiled bundle)"
    - "All pre-existing accessibility_test.exs tests continue to pass"
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/accessibility_test.exs"
      provides: "5 new contrast tests (TOKEN-03)"
  key_links:
    - from: "accessibility_test.exs contrast_ratio/2"
      to: "hardcoded hex literals from brandbook/tokens.css"
      via: "inline WCAG 2.1 luminance calculation (no bundle read)"
---

<objective>
Add five contrast assertions to accessibility_test.exs that prove TOKEN-03 AA compliance for the dark-mode token fixes and pin the intentional sub-3:1 border roles. These tests call contrast_ratio/2 on hardcoded hex literals and are GREEN the moment they are written — no bundle rebuild needed.

Purpose: D-04 + D-10. Contrast proof lands BEFORE the CSS rewrite (Wave 1) so it is in place as a permanent record independent of the bundle state. The math is oracle-free: hardcoded hex values from brandbook/tokens.css, no file I/O, no setup_all.

Output: accessibility_test.exs extended with two new describe blocks (~30 lines). No other files changed.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from existing test files -->

From mailglass_admin/test/mailglass_admin/accessibility_test.exs:
- Module: MailglassAdmin.AccessibilityTest
- Private functions (defp, already exist — do NOT redefine):
    defp contrast_ratio("#" <> _ = hex_a, "#" <> _ = hex_b) — WCAG 2.1 relative luminance
    defp luminance("#" <> hex), defp parse_hex, defp hex_pair, defp linearize
- Existing describe blocks: "canonical contrast pairs" (lines 15-43), "borderline pair" (lines 45-67)
- TWO NEW describe blocks to add AFTER the last existing describe block:

describe "dark-mode token fixes (TOKEN-03)" do
  test "dark muted text #B8CAD4 on Ink #0D1B2A passes AA (≥4.5:1)" do
    # --mg-color-text-muted in dark theme; was #5C6B7A (3.18:1 — FAILED AA)
    assert contrast_ratio("#B8CAD4", "#0D1B2A") >= 4.5
  end

  test "dark error #E29089 on Ink #0D1B2A passes AA (≥4.5:1)" do
    # --mg-color-error-solid in dark theme; was off-palette #D47368
    assert contrast_ratio("#E29089", "#0D1B2A") >= 4.5
  end

  test "dark primary-content Ink #0D1B2A on Ice #A6EAF2 passes AA (≥4.5:1)" do
    # --color-primary-content in dark; already passes at 12.98:1 — pinned against regression
    assert contrast_ratio("#0D1B2A", "#A6EAF2") >= 4.5
  end
end

describe "border role is intentionally sub-3:1 (WCAG 1.4.11 decorative exemption)" do
  # Brand classifies base-300 as decorative hairlines/dividers only.
  # WCAG 1.4.11 (Non-text Contrast) only requires 3:1 for CONTROL BOUNDARIES.
  # Input borders use --color-input (border-input var, independent of base-300).
  # These assertions are intentionally <3.0 — do NOT raise the value to "fix" them.
  test "light border #C7DCE5 on Paper #F8FBFD is sub-3:1 (decorative hairline)" do
    assert contrast_ratio("#C7DCE5", "#F8FBFD") < 3.0
  end

  test "dark border #315069 on Ink #0D1B2A is sub-3:1 (decorative hairline)" do
    assert contrast_ratio("#315069", "#0D1B2A") < 3.0
  end
end
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend accessibility_test.exs with dark-mode and border contrast tests</name>
  <files>
    mailglass_admin/test/mailglass_admin/accessibility_test.exs
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/accessibility_test.exs (full — read existing contrast_ratio/2 + luminance/1 defp implementations; identify the append point after the last describe block; confirm module and alias structure)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §accessibility_test.exs
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md D-04
  </read_first>
  <action>
    Edit mailglass_admin/test/mailglass_admin/accessibility_test.exs. Append two new describe blocks AFTER the existing last describe block (after the closing `end` of the "borderline pair" describe block, before the module's closing `end`). Do NOT modify any existing tests, imports, or defp functions.

    Block 1 — "dark-mode token fixes (TOKEN-03)": three tests using the existing contrast_ratio/2 defp. Use hardcoded hex literals from brandbook/tokens.css (source-of-truth values). Include inline comments citing the token name and the failure context (was / now). Assertions: muted #B8CAD4 vs Ink #0D1B2A >= 4.5; error #E29089 vs Ink #0D1B2A >= 4.5; primary-content Ink #0D1B2A vs Ice #A6EAF2 >= 4.5.

    Block 2 — "border role is intentionally sub-3:1 (WCAG 1.4.11 decorative exemption)": two tests with a describe-level docstring explaining WHY sub-3:1 is intentional (decorative hairlines; WCAG 1.4.11 only requires 3:1 for control boundaries; inputs use border-input var independent of base-300). Assertions: light border #C7DCE5 vs Paper #F8FBFD < 3.0; dark border #315069 vs Ink #0D1B2A < 3.0. Include a comment on each test: "Do NOT raise this value — WCAG 1.4.11 exempts decorative dividers."

    Use the exact describe/test structure from the interfaces block above. All five tests call contrast_ratio/2 on hardcoded hex literals — no file I/O, no setup, no external dependencies.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix compile 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>accessibility_test.exs has 5 new passing contrast tests: dark muted, dark error, dark primary-content (all ≥4.5:1), light border and dark border (both < 3.0). mix compile exits 0. mix test exits 0 for the full accessibility_test.exs suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → hardcoded hex literals | Pure in-memory WCAG math; no file I/O, no network, no exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Tampering | accessibility_test.exs hardcoded hex values | accept | Values sourced from brandbook/tokens.css (the canonical token file committed in git); no runtime input; any future token change requires a deliberate test update |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure ExUnit arithmetic |

**Assessment: MINIMAL threat surface.** Five pure math assertions on hardcoded hex literals. No network, no file reads, no PII, no auth. The intentional sub-3:1 assertions are protected by explicit docstrings against well-intentioned "fixes."
</threat_model>

<verification>
Run after Task 1 completes:

```
cd mailglass_admin && mix compile
# Expected: exit 0

cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs
# Expected: all tests pass (including 5 new) — exit 0
```
</verification>

<success_criteria>
- accessibility_test.exs compiled without errors
- All 5 new tests pass immediately (green without any bundle changes): muted #B8CAD4 ≥4.5:1, error #E29089 ≥4.5:1, primary-content ≥4.5:1, light border <3.0, dark border <3.0
- All pre-existing accessibility_test.exs tests continue to pass
- commit message: `test(admin): extend accessibility_test with dark-mode AA + border contrast proofs`
</success_criteria>

<validation>
Dimension 8 proof commands:

| Requirement | Command | Expected result |
|-------------|---------|-----------------|
| TOKEN-03 (dark AA + border sub-3:1 pin) | `cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs` | exit 0 (all tests green, including 5 new) |
| TOKEN-03 (compiles) | `cd mailglass_admin && mix compile` | exit 0 |
</validation>

<output>
Create `.planning/phases/94-token-re-baseline-onto-canonical-brand/94-02-SUMMARY.md` when done.
</output>
