---
phase: 95-audit-apparatus-quality-ratchet-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
  - mailglass_admin/docs/ui-baseline-scores.json
  - mailglass_admin/mix.exs
autonomous: true
requirements:
  - RATCHET-01
must_haves:
  truths:
    - "ratchet_baseline_test.exs exists in the always-run verify.support_contract.admin lane"
    - "The test asserts all 36 surface×pillar×theme cells are present and scores are in 1–4"
    - "The test validates schema_version is present and equals 1"
    - "compare_baselines/2 is defined as a private function but never called (Phase 103 hook point)"
    - "An initial ui-baseline-scores.json placeholder with all 36 cells and every score set to integer 1 (minimum valid score) keeps the ExUnit test green immediately, satisfying D-08 commit 2 (no null/out-of-range values that would red the range assertion)"
    - "The test is fail-closed: any missing cell or out-of-range score makes it red"
    - "The test is NOT vacuous: it makes real assertions on shape, range, and schema_version"
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      provides: "D-04/D-05 fail-closed ExUnit baseline assertion"
      exports: ["MailglassAdmin.RatchetBaselineTest"]
    - path: "mailglass_admin/docs/ui-baseline-scores.json"
      provides: "D-03 score baseline — placeholder with all 36 cells"
      contains: "schema_version"
    - path: "mailglass_admin/mix.exs"
      provides: "verify.support_contract.admin alias wired to include ratchet_baseline_test.exs"
  key_links:
    - from: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      to: "mailglass_admin/docs/ui-baseline-scores.json"
      via: "Path.join(__DIR__, relative path to docs/)"
      pattern: "ui-baseline-scores.json"
    - from: "mailglass_admin/mix.exs verify.support_contract.admin alias"
      to: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      via: "explicit test file path in alias list"
      pattern: "ratchet_baseline_test.exs"
---

<objective>
Add the fail-closed ExUnit baseline assertion (D-04/D-05) and wire it into the required
`verify.support_contract.admin` CI lane. Create the placeholder `ui-baseline-scores.json`
with all 36 cells so the test compiles and makes real shape/range assertions even before
the real LLM scores land in Plan 95-04.

This is D-08 commit 2: ExUnit shape/range assertion green before the Playwright spec is added.

Purpose: Establish the machine-checkable baseline gate. On Phase 95 this gate validates
the JSON's shape (36 cells), range (scores 1–4), and schema_version. The meet-or-beat
regression teeth (`compare_baselines/2`) are defined here but dormant until Phase 103.

Output:
- `mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs` — the test
- `mailglass_admin/docs/ui-baseline-scores.json` — placeholder (all 36 cells, scores 1 as initial)
- `mailglass_admin/mix.exs` — `verify.support_contract.admin` alias updated to include the test file
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md

<interfaces>
<!-- Template: mirror token_parity_test.exs module layout exactly. -->

From mailglass_admin/test/mailglass_admin/token_parity_test.exs (read before implementing):
- Module: MailglassAdmin.TokenParityTest
- Path idiom: Application.app_dir(:mailglass_admin, "priv") for priv/ files;
  for docs/ (outside priv/), use Path.join([__DIR__, "..", "..", "docs", "filename"])
- setup_all pattern: assert File.exists?(@path), then Jason.decode!(File.read!(@path))
- Collect-all-mismatches-before-asserting pattern (for loop → reject nil → assert [])
- Failure message style: names the oracle file and gives the exact self-serve fix command

From mailglass_admin/mix.exs (verify.support_contract.admin alias, lines 189–192):
  "verify.support_contract.admin": [
    "test test/mailglass_admin/post_installer_smoke_test.exs
           test/mailglass_admin/operator_live_test.exs
           test/mailglass_admin/operator_trust_doc_test.exs
           test/mailglass_admin/stability_contract_test.exs
           test/mailglass_admin/router_test.exs
           test/mailglass_admin/auth_test.exs
           test/mailglass_admin/token_parity_test.exs
           --warnings-as-errors"
  ]
  NOTE: ratchet_baseline_test.exs must be appended to this list.

From 95-RESEARCH.md code examples (ExUnit module, @surfaces/@pillars/@themes, test bodies):
  @surfaces ["deliveries", "inbound", "preview"]
  @pillars  ["Spacing", "Radius", "Color", "Type", "Elevation", "Motion+A11y"]
  @themes   ["light", "dark"]
  @valid_scores 1..4
  JSON path: Path.join([__DIR__, "..", "..", "docs", "ui-baseline-scores.json"])
  JSON structure: {"run_id": "...", "schema_version": 1, "pillar_rubric": "...",
                   "grade_scale": "...", "surfaces": {"deliveries": {"Spacing": {"light": N, "dark": N}, ...}}}
  36 graded cells = 3 surfaces × 6 pillars × 2 themes

  compare_baselines/2 signature (private, defined but not called in Phase 95):
  defp compare_baselines(prior, current) — collects regressions where current_score < prior_score
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ratchet_baseline_test.exs with shape/range assertions and compare_baselines/2 hook</name>
  <files>
    mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs,
    mailglass_admin/docs/ui-baseline-scores.json
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/token_parity_test.exs
      (full file — the exact structural template: module layout, @path macro, setup_all,
      collect-all-mismatches pattern, failure message style)
    - mailglass_admin/docs/design-system.md lines 104–121
      (canonical 6 pillars: Spacing / Radius / Color / Type / Elevation / Motion+A11y — D-01)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md
      (ExUnit code example, JSON schema, compare_baselines/2 signature, 36-cell reconciliation)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md (D-03, D-04, D-05)
    - mailglass_admin/mix.exs (Jason dep — verify it is listed as a test dep, no :only)
  </read_first>
  <behavior>
    - Test "schema_version is present and supported": fails if schema_version key absent or != 1
    - Test "all 36 graded cells are present (3 surfaces × 6 pillars × 2 themes)": fails
      with the list of missing "surface.pillar.theme" strings if any cell is nil
    - Test "all 36 scores are in the valid range 1-4": fails with the list of
      "surface.pillar.theme: value" strings for any score not in 1..4
    - setup_all: assert File.exists? with explicit message naming ui-baseline-scores.json
      and the scoring step command; then Jason.decode! the file
    - compare_baselines/2: private, defined, never called — accepts (prior_map, current_map)
      and collects any cell where current_score < prior_score into a regression list,
      then asserts the list is empty with a named-cell message
  </behavior>
  <action>
    Step 1 — Create the placeholder ui-baseline-scores.json at
    mailglass_admin/docs/ui-baseline-scores.json with:
    - "run_id": "2026-06-13-phase-95-placeholder"
    - "schema_version": 1
    - "pillar_rubric": "design-system.md:104-121"
    - "grade_scale": "1-4 (1=non-conformant, 2=significant-gaps, 3=mostly-conformant, 4=excellent)"
    - "surfaces" object containing all 3 surfaces (deliveries, inbound, preview), each with all 6
      pillars (Spacing, Radius, Color, Type, Elevation, Motion+A11y), each with light and dark
      keys, all set to integer 1 (minimum valid score, not null/0 — the test must pass cleanly
      on this placeholder so commit 2 of D-08 is green before the real scores land in commit 4).
    That gives all 36 cells present, all scores in 1..4. The ExUnit test must be green on this.

    Step 2 — Create ratchet_baseline_test.exs mirroring token_parity_test.exs structure:
    - Module: MailglassAdmin.RatchetBaselineTest
    - @moduledoc: explain Phase 95 = establish-and-freeze, Phase 103 = meet-or-beat regression.
      Call out that if the test fails with "missing cell" the scoring step was incomplete;
      if "score out of range" the LLM scored outside 1–4. compare_baselines/2 exists but
      is NOT called (Phase 103 adds only the call site — not a rewrite).
    - use ExUnit.Case, async: true
    - @scores_path: Path.join([__DIR__, "..", "..", "docs", "ui-baseline-scores.json"])
      (three levels: test/mailglass_admin/ → test/ → mailglass_admin/ → docs/)
    - @surfaces, @pillars, @themes, @valid_scores module attributes as listed in interfaces
    - setup_all: assert File.exists?(@scores_path) with message "Run the LLM scoring step (D-07)
      first"; {:ok, baseline: Jason.decode!(File.read!(@scores_path))}
    - Test 1: schema_version == 1 (assert b["schema_version"] == 1)
    - Test 2: all 36 cells present — nested for comprehension, collect missing paths, assert []
    - Test 3: all 36 scores in 1..4 — nested for comprehension, collect out-of-range paths, assert []
    - defp compare_baselines(prior, current): collects cells where
      get_in(current, ["surfaces", surface, pillar, theme]) <
      get_in(prior, ["surfaces", surface, pillar, theme] || 0);
      asserts regressions == [] with message listing each "surface.pillar.theme: N → M (REGRESSION)"

    Path resolution note (per RESEARCH pitfall 7 / open question 2): use __DIR__-relative path
    NOT Application.app_dir/2, because docs/ is outside priv/. Token parity test uses
    Application.app_dir for priv/; this test uses __DIR__ for docs/.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs exists
    - mailglass_admin/docs/ui-baseline-scores.json exists and is valid JSON
    - `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs` exits 0
    - All 3 tests pass: schema_version, 36-cell coverage, score range
    - `cat mailglass_admin/docs/ui-baseline-scores.json | python3 -c "import sys,json; d=json.load(sys.stdin); cells=[(s,p,t) for s in ['deliveries','inbound','preview'] for p in ['Spacing','Radius','Color','Type','Elevation','Motion+A11y'] for t in ['light','dark']]; missing=[f'{s}.{p}.{t}' for s,p,t in cells if d['surfaces'].get(s,{}).get(p,{}).get(t) is None]; print(len(missing),'missing'); assert missing==[]"` exits 0
    - `grep -c "compare_baselines" mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs` returns >= 1 (function defined)
    - `grep -c "compare_baselines" mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs` check: the function is defp (private) and has no call site in the module (Phase 103 hook)
  </acceptance_criteria>
  <done>
    ratchet_baseline_test.exs passes locally with 3 tests green against the placeholder JSON.
    All 36 cells present in the JSON. compare_baselines/2 is defined but uncalled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ratchet_baseline_test.exs into verify.support_contract.admin alias</name>
  <files>mailglass_admin/mix.exs</files>
  <read_first>
    - mailglass_admin/mix.exs lines 183–200
      (verify.support_contract.admin alias definition — append ratchet_baseline_test.exs
      to the explicit file list in the "test ..." command)
  </read_first>
  <action>
    Edit the verify.support_contract.admin alias in mailglass_admin/mix.exs to append
    test/mailglass_admin/ratchet_baseline_test.exs to the explicit file list. The alias is a
    single "test ..." command with explicit file paths and --warnings-as-errors. Append the
    new test file before the --warnings-as-errors flag so it runs in the same Mix test invocation.

    Do NOT add a second element to the alias list — keep it as a single "test ..." command.
    Do NOT add :test_only tag to the test file — it must run unconditionally (mirrors
    token_parity_test.exs which has no @moduletag :skip or :test_only).
    Do NOT change any other alias or mix.exs section.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.support_contract.admin</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix verify.support_contract.admin` exits 0
    - `grep "ratchet_baseline_test.exs" mailglass_admin/mix.exs` returns a match inside the verify.support_contract.admin alias
    - The existing 7 test files in verify.support_contract.admin are still present (no regression)
    - `cd mailglass_admin && mix verify.support_contract.admin` output includes "ratchet_baseline_test.exs" in the test list
  </acceptance_criteria>
  <done>
    `mix verify.support_contract.admin` exits 0 and includes ratchet_baseline_test.exs in the
    required lane. This is D-08 commit 2 green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON file → ExUnit | ratchet_baseline_test.exs reads ui-baseline-scores.json from the filesystem at test time — the file is a committed repo artifact, not external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-V5 | Information Disclosure / Tampering | ratchet_baseline_test.exs reading ui-baseline-scores.json | mitigate | V5 input validation: test validates schema_version (expected integer 1), all 36 cells present, all scores in 1..4 integer range — parsed structure asserted before use; malformed JSON raises Jason.DecodeError and fails the test clearly |
| T-95-03 | Tampering | ui-baseline-scores.json score values (could be set to 4 to defeat regression gate) | accept | Scores are set by local maintainer/subagent step (D-07); Phase 103 can only catch regression on scores that were honestly set; scope-of-concern is maintainer discipline, not an external adversary |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — Jason is existing dep; slopcheck not applicable |

High-severity assessment: No high-severity threats apply. The one substantive threat is V5 input
validation on the JSON, mitigated by the explicit cell-coverage and range assertions in the test.
PII minimization: test reads only score integers and string keys (surface, pillar, theme names) —
no PII present in ui-baseline-scores.json.
</threat_model>

<verification>
After both tasks complete:
- `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs` exits 0 (3 tests pass)
- `cd mailglass_admin && mix verify.support_contract.admin` exits 0 (full required lane green including the new test)
- `grep "compare_baselines" mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs` confirms the function is defined (defp) with no call site
- `python3 -c "import json; d=json.load(open('mailglass_admin/docs/ui-baseline-scores.json')); print(sum(1 for s in d['surfaces'].values() for p in s.values() for score in p.values()))"` prints 36
</verification>

<success_criteria>
1. ratchet_baseline_test.exs exists with 3 tests (schema_version, 36-cell coverage, score range) — all green
2. compare_baselines/2 defined as private, never called — Phase 103 hook point intact
3. ui-baseline-scores.json has all 36 surface×pillar×theme cells with valid integer scores 1–4
4. verify.support_contract.admin alias includes ratchet_baseline_test.exs and exits 0
5. D-08 commit 2 is green: `mix verify.support_contract.admin` passes end-to-end
</success_criteria>

<output>
Create `.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-02-SUMMARY.md` when done.
</output>
