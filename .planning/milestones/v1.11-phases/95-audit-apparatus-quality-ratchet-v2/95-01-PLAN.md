---
phase: 95-audit-apparatus-quality-ratchet-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/RATCHET-GAP-REGISTER.md
autonomous: true
requirements:
  - RATCHET-02
must_haves:
  truths:
    - "A RATCHET-GAP-REGISTER.md file exists at the planning root with stable_ids: true and the v1.11 milestone header"
    - "The register's anti-churn contract verbatim mirrors the v1.7 74-GAP-REGISTER.md language, targeting Phases 98–103"
    - "The column schema includes all 9 columns: GAP-NN, surface, component:line, pillar, sev, evidence PNG, fix sketch, status, run_id, first_seen_run"
    - "The idempotent re-run semantics (reopen/skip) are documented in the header"
    - "The file is header-only (no data rows yet) and commits clean/green immediately"
  artifacts:
    - path: ".planning/RATCHET-GAP-REGISTER.md"
      provides: "v1.11 GAP register — header + schema, rows TBD in Plan 95-04"
      contains: "stable_ids: true"
  key_links:
    - from: ".planning/RATCHET-GAP-REGISTER.md"
      to: "downstream Phases 98–103 build tasks"
      via: "sev≥3 citation gate (anti-churn contract)"
      pattern: "GAP-\\d+ | sev.*[345]"
---

<objective>
Stand up the carried-forward GAP register as a header-only file at the planning root.
This is D-08 commit 1: the register file + schema must exist and be green before the
ExUnit baseline assertion or the Playwright structural spec are added.

Purpose: Establish the stable-ID citation anchor for all downstream build phases (98–103).
The register is a planning artifact, not code — committing it header-only is immediately
green because no test or CI lane validates the markdown content.

Output: `.planning/RATCHET-GAP-REGISTER.md` with the full YAML frontmatter, anti-churn
contract, idempotent re-run semantics, and 9-column schema block. Data rows (GAP-01..NN)
are populated by Plan 95-04 after the seed run.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md

<interfaces>
<!-- Column shape and anti-churn contract sourced from the v1.7 precedent register. -->

From .planning/milestones/v1.7-phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md:
- YAML frontmatter: phase, slug, artifact, stable_ids: true, created
- Anti-churn section: "Every build task in Phases 75-78 MUST cite a row from this register
  at severity ≥ 3. No citation → no merge."
- v1.7 columns (7): GAP-NN | surface | component:line | pillar | sev | evidence PNG | fix sketch
- New ratchet columns (3 added per D-02): status (open/fixed/downgraded) | run_id | first_seen_run

From 95-CONTEXT.md D-02:
- File path: .planning/RATCHET-GAP-REGISTER.md (milestone-root, NOT phase-buried)
- IDs restart at GAP-01 (fresh file, no collision with frozen 74-GAP-REGISTER.md)
- Supersedes: frozen v1.7 register (column-shape precedent only; rows do NOT migrate)

From 95-RESEARCH.md GAP register header example:
- Frontmatter keys: milestone, artifact, stable_ids, created, supersedes
- Section order: header prose → Anti-Churn Contract → Idempotent Re-Run Semantics → Column Schema
- Scoring step reference: documents the invocation procedure for the seed run in the header
  so future maintainers know how to produce a run_id
- Phase 103 re-run semantics documented in header: regressed cell reopens GAP-NN,
  fixed + confirmed-absent row is skipped, open + same-finding row is skipped (no duplicates)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RATCHET-GAP-REGISTER.md with full schema and anti-churn contract</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md
      (lines 1–40: YAML frontmatter, Anti-Churn Contract, Row Schema — exact text to continue)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md
      (GAP register schema section, header template block, idempotent re-run section)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md (D-02)
    - .planning/ROADMAP.md (Phase 95 success criterion 2, Phase 103 re-run context)
  </read_first>
  <action>
    Create .planning/RATCHET-GAP-REGISTER.md as a fresh header-only file. The file has NO
    data rows yet — rows are added by Plan 95-04 after the seed run.

    YAML frontmatter:
    - milestone: v1.11
    - artifact: ratchet-gap-register
    - stable_ids: true
    - created: 2026-06-13
    - supersedes: the frozen v1.7 register path (document DO NOT reopen note)

    Top-level header and description prose explaining:
    - Fresh baseline as of Phase 95; IDs restart at GAP-01 in this NEW file.
    - v1.7 74-GAP-REGISTER.md is frozen — do NOT migrate or reopen it.
    - This register spans Phases 95→103 (milestone-root location, deliberately not phase-buried).

    Anti-Churn Contract section (verbatim continuation of v1.7 language, re-targeted):
    - "Every build task in Phases 98–103 MUST cite a row from this register at severity ≥ 3.
      No citation → no merge. Rows are never renumbered once assigned."

    Idempotent Re-Run Semantics section (active from Phase 103, defined here):
    - On re-run: a regressed cell reopens its GAP-NN row and stamps the new run_id.
    - A row marked 'fixed' whose finding is confirmed absent is skipped (no duplicate).
    - A row marked 'open' with the same finding is skipped (no duplicate).
    - 'run_id' records the last touch; 'first_seen_run' never changes once set.

    Seed Run Procedure section (documents how to produce a run_id for future maintainers):
    - Step 1: Boot the reference demo (reference/demo_app, port 4015, mix phx.server).
    - Step 2: Run mailglass_admin/scripts/ui-audit.sh to produce 18 PNGs under
      tmp/ui-audit/{surface}-{vp}-{theme}.png (gitignored, never committed).
    - Step 3: Score each surface×theme pair (6 pairs) against the D-01 6 pillars using
      the 1–4 scale — use a multimodal subagent or manual review of the PNGs.
    - Step 4: Write scores to mailglass_admin/docs/ui-baseline-scores.json.
    - Step 5: Commit ONLY the JSON (not the PNGs).

    Column Schema section — a table with all 9 columns:
    - GAP-NN: Stable ID — never renumber once assigned
    - surface: deliveries / inbound / preview / all
    - component:line: Path relative to mailglass_admin/lib/ (or docs/) and line number
    - pillar: One of the 6 D-01 pillars (Spacing / Radius / Color / Type / Elevation / Motion+A11y)
    - sev: 1–5 (1=cosmetic, 2=minor, 3=significant, 4=major, 5=critical)
    - evidence PNG: tmp/ui-audit/{surface}-{vp}-{theme}.png (gitignored path reference)
    - fix sketch: Concise implementation direction
    - status: open / fixed / downgraded
    - run_id: Run ID that last touched this row (format: YYYY-MM-DD-phase-NN)
    - first_seen_run: Run ID of initial discovery — never changes

    Six Conformance Pillars subsection listing the D-01 set with one-line descriptions:
    - Spacing, Radius, Color, Type, Elevation, Motion+A11y

    Severity Rubric subsection (5 levels, matching v1.7 rubric style):
    - 5: Accessibility failure / brand violation visible to every user
    - 4: Major design-system violation affecting core UX
    - 3: Significant gap (the anti-churn citation threshold)
    - 2: Minor inconsistency
    - 1: Cosmetic / polish

    End with a ## Gaps section header with a note: "Rows populated by Phase 95 seed run (Plan 95-04). See run_id: 2026-06-13-phase-95-baseline."

    Do NOT add any GAP-NN data rows. The table header row and separator are fine so the
    format is ready for rows, but leave the body empty.
  </action>
  <verify>
    <automated>grep -c "stable_ids: true" .planning/RATCHET-GAP-REGISTER.md && grep -c "MUST cite a row" .planning/RATCHET-GAP-REGISTER.md && grep -c "first_seen_run" .planning/RATCHET-GAP-REGISTER.md && grep -c "Motion+A11y" .planning/RATCHET-GAP-REGISTER.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/RATCHET-GAP-REGISTER.md exists
    - `stable_ids: true` appears in YAML frontmatter
    - The anti-churn contract contains "MUST cite a row" and mentions "severity ≥ 3"
    - All 9 columns appear in the Column Schema table: GAP-NN, surface, component:line, pillar, sev, evidence PNG, fix sketch, status, run_id, first_seen_run
    - "first_seen_run" appears (distinguishes from v1.7 schema)
    - "Motion+A11y" appears in the Conformance Pillars list (verifies D-01 pillar set)
    - No GAP-NN data rows exist (file is header-only at this commit)
    - File is valid Markdown (no unclosed tables, no broken YAML frontmatter)
    - `git status .planning/RATCHET-GAP-REGISTER.md` shows a new file, no other .planning/ files modified
  </acceptance_criteria>
  <done>
    The RATCHET-GAP-REGISTER.md exists at the planning root with the full 9-column schema,
    anti-churn contract, idempotent re-run semantics, and seed-run procedure documented.
    No data rows. Commits green. Downstream phases 98–103 can cite this file immediately.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown content → human reviewer | The GAP register is a markdown planning artifact; trust is enforced by PR review, not machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | .planning/RATCHET-GAP-REGISTER.md (stable IDs) | mitigate | Anti-churn contract documents "never renumber once assigned"; ID stability enforced by PR review rule |
| T-95-02 | Information Disclosure | evidence PNG references in GAP rows | accept | PNG paths reference tmp/ui-audit/ which is gitignored; no PNGs committed; no PII in surface paths |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — register is a pure markdown artifact; slopcheck not applicable |

No high-severity threats: this plan creates only a planning-root markdown file with no code, auth, or data flows.
</threat_model>

<verification>
After Task 1 completes:
- `grep -c "stable_ids: true" .planning/RATCHET-GAP-REGISTER.md` exits 0 with count >= 1
- `grep -c "first_seen_run" .planning/RATCHET-GAP-REGISTER.md` exits 0 with count >= 1
- `git diff --stat HEAD` shows only .planning/RATCHET-GAP-REGISTER.md added
- File parses as valid Markdown (no unclosed code blocks, balanced table pipes)
</verification>

<success_criteria>
.planning/RATCHET-GAP-REGISTER.md exists with:
1. YAML frontmatter: milestone v1.11, artifact ratchet-gap-register, stable_ids: true
2. Anti-churn contract verbatim (phases 98–103, sev≥3, no citation → no merge)
3. Idempotent re-run semantics for Phase 103 documented in header
4. All 9 columns in schema table (7 v1.7 + status + run_id + first_seen_run)
5. D-01 six pillars listed (Spacing / Radius / Color / Type / Elevation / Motion+A11y)
6. No data rows — rows are populated by Plan 95-04
7. Commit is green (no failing CI gates on a pure .planning/ file)
</success_criteria>

<output>
Create `.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-01-SUMMARY.md` when done.
</output>
