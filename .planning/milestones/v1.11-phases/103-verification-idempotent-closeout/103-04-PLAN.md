---
phase: 103-verification-idempotent-closeout
plan: 04
type: execute
wave: 4
depends_on: [103-03]
files_modified:
  - .planning/v1.11-MILESTONE-AUDIT.md
autonomous: true
requirements: [CLOSE]
must_haves:
  truths:
    - "The milestone audit is regenerated LAST, only after the GAP register is reconciled, the ratchet is armed, and 103's VERIFICATION.md exists"
    - "The regenerated audit no longer reports phases 101/102/103 as missing and no longer lists COPY-01/MOTION-01/MOTION-02 as unsatisfied"
    - "The audit status flips from gaps_found to passed (or surfaces a genuine remaining gap, not a stale one)"
  artifacts:
    - path: ".planning/v1.11-MILESTONE-AUDIT.md"
      provides: "Truthful regenerated v1.11 milestone audit"
      contains: "v1.11"
  key_links:
    - from: ".planning/v1.11-MILESTONE-AUDIT.md"
      to: ".planning/phases/103-*/103-*-VERIFICATION.md"
      via: "gsd-audit-milestone reads per-phase VERIFICATION.md"
      pattern: "103"
---

<objective>
Regenerate the v1.11 milestone audit LAST (D-14, CLOSE) via the `gsd-audit-milestone` skill,
after the GAP register is reconciled (Plan 01), the ratchet is armed (Plan 02), the gates are
green + readiness note written (Plan 03), AND Phase 103's VERIFICATION.md exists. This is the
FINAL step of the D-15 locked ordering.

Purpose: Replace the STALE `.planning/v1.11-MILESTONE-AUDIT.md` (dated 2026-06-15, status
`gaps_found`, falsely reporting phases 101/102/103 nonexistent and COPY-01/MOTION-01/MOTION-02
unsatisfied) with a truthful regeneration that flips gaps_found → passed. Completes ROADMAP
Phase 103 success criterion 4 (the milestone audit passes).
Output: Regenerated `.planning/v1.11-MILESTONE-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md
@.planning/RATCHET-GAP-REGISTER.md

<interfaces>
<!-- The gsd-audit-milestone skill reads per-phase *-VERIFICATION.md files, cross-references
     REQUIREMENTS/ROADMAP, and overwrites .planning/v1.11-MILESTONE-AUDIT.md. -->

Skill: gsd-audit-milestone (~/.claude/skills/gsd-audit-milestone)
Ordering dependency (DISC-3/DISC-4, Pitfall 4):
  - The current audit (2026-06-15) is STALE — it is the TRIGGER for reconciliation, never the
    basis for any flip (D-10). Trust no number in it.
  - The skill reads `103-*-VERIFICATION.md`. That file does NOT exist at plan-write time
    (DISC-4); it is produced during /gsd:verify-work. The audit MUST run only AFTER it exists,
    or the audit re-reports Phase 103 "not started" and stays gaps_found.
  - Prerequisites already satisfied by earlier plans: GAP register reconciled (Plan 01),
    ratchet armed (Plan 02), gates green (Plan 03).
</interfaces>

@.planning/v1.11-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm prerequisites then regenerate the v1.11 milestone audit</name>
  <files>.planning/v1.11-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v1.11-MILESTONE-AUDIT.md (the STALE audit — the trigger, not evidence)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-14 audit-last; D-10 trigger-vs-evidence)
    - .planning/RATCHET-GAP-REGISTER.md (confirm zero open sev-4/5 rows from Plan 01)
    - ~/.claude/skills/gsd-audit-milestone/SKILL.md (the regeneration procedure)
  </read_first>
  <action>
    GATE FIRST — confirm all D-15 prerequisites before invoking the skill (Pitfall 4):
    1. `ls .planning/phases/103-*/103-*-VERIFICATION.md` returns a file (produced during
       /gsd:verify-work). If it does NOT exist, STOP — the audit must not run yet (it would
       re-report Phase 103 missing). Surface this as a blocked-on-verify-work dependency.
    2. Confirm the GAP register has zero `| open |` rows among GAP-01/04/06/07/08/09 (Plan 01).
    3. Confirm the ratchet test is armed and green (Plan 02) and gates passed (Plan 03 SUMMARY).
    Then run the `gsd-audit-milestone` skill for milestone v1.11. It reads the per-phase
    `*-VERIFICATION.md` files (94-103), cross-references REQUIREMENTS/ROADMAP, and OVERWRITES
    `.planning/v1.11-MILESTONE-AUDIT.md`. Do NOT hand-write audit prose — the skill owns the file.
    After regeneration, confirm the audit no longer reports phases 101/102/103 as nonexistent
    and no longer lists COPY-01/MOTION-01/MOTION-02 as unsatisfied, and that status reads
    `passed`. If the regenerated audit surfaces a GENUINE remaining gap (not a stale one),
    surface it as a real finding rather than overriding the skill's output.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && ls .planning/phases/103-*/103-*-VERIFICATION.md && [ "$(grep -cE '^\| GAP-0[14689] .*\| open \|' .planning/RATCHET-GAP-REGISTER.md)" = "0" ] && grep -qiE 'passed|status' .planning/v1.11-MILESTONE-AUDIT.md && ! grep -qi 'no .*101-.* directory exists' .planning/v1.11-MILESTONE-AUDIT.md && echo AUDIT_REGENERATED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/103-*/103-*-VERIFICATION.md` exists before the skill runs (ordering gate honored).
    - The GAP register has zero open rows among GAP-01/04/06/07/08/09 at audit time.
    - The regenerated audit no longer claims any `.planning/phases/101-*` (or 102/103) directory is missing.
    - The audit no longer lists COPY-01, MOTION-01, or MOTION-02 as unsatisfied.
    - Audit status reads `passed` (or a genuine remaining gap is surfaced as a real finding).
    - The audit file was written by the gsd-audit-milestone skill, not hand-authored.
  </acceptance_criteria>
  <done>The v1.11 milestone audit is regenerated truthfully (gaps_found → passed); the stale 2026-06-15 report is replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale audit → reconciliation trigger | The 2026-06-15 audit is the untrusted trigger, never the basis for a flip or a number (D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-09 | Repudiation | audit regenerated too early (before 103 VERIFICATION.md) → re-reports stale gaps as false assurance | mitigate | Task 1 GATE: confirm 103 VERIFICATION.md + reconciled register + armed ratchet BEFORE invoking the skill (D-14/D-15, Pitfall 4). |
| T-103-10 | Tampering | hand-authoring audit prose to force `passed` | mitigate | The gsd-audit-milestone skill owns the file; no hand-editing; a genuine remaining gap is surfaced, not overridden. |

No HIGH-severity unmitigated threats. No product code, no routes, no auth, no crypto.
</threat_model>

<verification>
- `103-*-VERIFICATION.md` exists before the skill runs.
- The regenerated audit no longer reports 101/102/103 missing or COPY-01/MOTION-01/MOTION-02 unsatisfied.
- `git diff --stat` shows only `.planning/v1.11-MILESTONE-AUDIT.md` modified.
</verification>

<success_criteria>
ROADMAP Phase 103 success criterion 4 fully met: the linked-version release ceremony is staged
prepare-only (Plan 03) AND the milestone audit passes (this plan). v1.11 is closed out.
</success_criteria>

<output>
Create `.planning/phases/103-verification-idempotent-closeout/103-04-SUMMARY.md` when done.
</output>
