---
phase: "97"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
  - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "Delivery list row buttons carry a visible focus ring (inset) in both themes"
    - "Both detail_header h2 headings use text-heading token, not the banned text-xl"
    - "All five filters_form label spans have no tracking-[0.08em] arbitrary value"
    - "support_cards tier-1 CTA buttons have no btn-sm and effective min-height of 44px"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      provides: "Row button with focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset"
      contains: "focus-visible:ring-inset"
    - path: "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
      provides: "h2 with text-heading token"
      contains: "text-heading"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex"
      provides: "h2 with text-heading token"
      contains: "text-heading"
    - path: "mailglass_admin/lib/mailglass_admin/operator/filters_form.ex"
      provides: "Label spans without arbitrary tracking value"
    - path: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      provides: "CTA buttons without btn-sm, with min-h-11"
      contains: "min-h-11"
  key_links:
    - from: "deliveries_list.ex row button"
      to: "ring-primary token"
      via: "focus-visible:ring-primary focus-visible:ring-inset"
      pattern: "focus-visible:ring-inset"
    - from: "detail_header.ex h2"
      to: "app.css @theme --text-heading"
      via: "text-heading class"
      pattern: "text-heading"
---

<objective>
Uplift four operator components and the inbound detail_header in a single Wave-1 pass: add row focus ring to deliveries_list, fix text-xl → text-heading on both detail_headers, remove forbidden `tracking-[0.08em]` from all filters_form labels, and remove `btn-sm` from support_cards CTAs.

Purpose: These are targeted in-place edits — each change is 1-5 lines per location. Grouping them removes the need for separate context windows on what are fundamentally "find and replace" uplifts.
Output: Five modified files; no bundle rebuild (plan 06).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key contracts extracted from source files -->

deliveries_list.ex:37-40 (current row button class list):
  class={["flex min-h-11 w-full flex-col gap-sm px-4 py-4 text-left transition-colors",
          row_classes(@selected_delivery, delivery)]}
  ADD: focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset (ring-inset not ring-offset — row fills full width)
  Existing a11y at lines 35-36: aria-current + aria-selected — must not regress.

operator/detail_header.ex:21 (current h2):
  <h2 class="text-xl font-bold text-base-content">{@delivery.recipient}</h2>
  CHANGE: text-xl → text-heading

inbound/detail_header.ex:38 (current h2):
  <h2 class="text-xl font-bold text-base-content">
    {Components.mask_recipient(@record.envelope_recipient)}
  </h2>
  CHANGE: text-xl → text-heading

filters_form.ex:16, 29, 42, 59, 76 (current label spans, all five):
  <span class="mb-1 text-label font-bold uppercase tracking-[0.08em] text-secondary">
  CHANGE: remove tracking-[0.08em] (conformance gate violation — arbitrary value)
  Result: <span class="mb-1 text-label font-bold uppercase text-secondary">

support_cards.ex:56, 102, 152 (tier-1 primary CTAs):
  class="btn btn-sm btn-primary mt-sm"
  CHANGE: class="btn btn-primary px-md mt-sm min-h-11"

support_cards.ex:204 (tier-2 ghost CTA):
  class="btn btn-ghost btn-sm px-3"
  CHANGE: class="btn btn-ghost px-sm min-h-11"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deliveries_list row focus ring + detail_header text-xl fixes</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/operator/detail_header.ex, mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (read lines 29-61 — row button class and existing aria attributes)
    - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex (read lines 15-30 — h2 class at line 21)
    - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex (read lines 23-45 — h2 class at line 38)
    - SUMMARY.md STATE-LD-11: "ADD focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset to row buttons — currently missing." Use ring-inset because the button fills the full list item width.
    - SUMMARY.md STATE-LD-12: "Phase 97 executor must replace text-xl with text-heading in both operator/detail_header.ex:21 and inbound/detail_header.ex:38."
    - 97-PATTERNS.md section "deliveries_list row button" and "detail_header text-xl"
  </read_first>
  <action>
    deliveries_list.ex — In the static class string (the first string in the class list, beginning "flex min-h-11 w-full flex-col..."), append `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset`. Use `ring-inset` not `ring-offset-1` — the row button spans full list width and an offset ring would clip at the li boundary (per 97-PATTERNS.md). Verify `aria-current` and `aria-selected` attributes at lines 35-36 are not disturbed.

    operator/detail_header.ex — At line 21, change `text-xl` to `text-heading` in the h2 class string. No other change in this file.

    inbound/detail_header.ex — At line 38, change `text-xl` to `text-heading` in the h2 class string. Confirm `Components.mask_recipient(...)` call is intact.
  </action>
  <verify>
    <automated>grep -c "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset" mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex</automated>
    Expected: 1.
    <automated>grep -c "text-xl" mailglass_admin/lib/mailglass_admin/operator/detail_header.ex mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex</automated>
    Expected: 0 (text-xl absent from both files).
    <automated>grep -c "text-heading" mailglass_admin/lib/mailglass_admin/operator/detail_header.ex mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex</automated>
    Expected: 2 (one per file).
  </verify>
  <done>Row button carries focus-visible:ring-inset ring. Both detail_header h2 elements use text-heading. Existing aria-current, aria-selected, mask_recipient intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tracking-[0.08em] from all filters_form labels</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/filters_form.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex (read lines 13-90 — all five label spans at lines 16, 29, 42, 59, 76)
    - SUMMARY.md STATE-LD-13: "Label token: text-label font-bold uppercase text-secondary (drop tracking-[0.08em] — arbitrary value, not a token)."
    - SUMMARY.md IA-LD-04: "The arbitrary Tailwind value tracking-[0.08em] MUST be removed; letter-spacing is token-owned."
    - 97-PATTERNS.md section "filters_form tracking removal" — confirms all five occurrences at lines 16/28/41/58/75
  </read_first>
  <action>
    Read filters_form.ex lines 13-90.

    Find every occurrence of `tracking-[0.08em]` in the file. According to the pattern map, there are exactly five occurrences, all in label `<span>` elements at approximately lines 16, 29, 42, 59, 76.

    In each occurrence, remove the string ` tracking-[0.08em]` from the class attribute. The resulting class list on each span must be `"mb-1 text-label font-bold uppercase text-secondary"` (without any tracking value — letter-spacing for headings is handled by the global h1/h2/h3 rule; label spans are not headings and carry no explicit tracking).

    Do NOT change the label text content (Tenant, Provider, Status, Event, Window). Do NOT change any other classes.
  </action>
  <verify>
    <automated>grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/filters_form.ex | grep -c "tracking-\[0.08em\]"</automated>
    Expected: 0 (all occurrences removed).
    <automated>grep -c "text-label font-bold uppercase text-secondary" mailglass_admin/lib/mailglass_admin/operator/filters_form.ex</automated>
    Expected: 5 (all five spans have the correct class set).
  </verify>
  <done>tracking-[0.08em] absent from filters_form.ex. All five label spans carry text-label font-bold uppercase text-secondary without arbitrary tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Remove btn-sm from support_cards CTA buttons</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/support_cards.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex (read lines 45-215 — tier-1 buttons at 56, 102, 152 and tier-2 at 204)
    - SUMMARY.md STATE-LD-14: "Remove btn-sm from lines 56, 102, 152 — use btn btn-primary with px-md. Line 204: remove btn-sm, use btn btn-ghost with px-sm."
    - 97-PATTERNS.md section "support_cards btn-sm removal" — exact before/after class strings
  </read_first>
  <action>
    Read support_cards.ex lines 45-215.

    At lines 56, 102, and 152 (tier-1 primary action buttons):
    Change `class="btn btn-sm btn-primary mt-sm"` to `class="btn btn-primary px-md mt-sm min-h-11"`.
    Explanation: `btn-sm` forces min-height 32px (below 44px floor). Removing it and adding `px-md` provides horizontal padding without the height restriction. `min-h-11` (44px) then applies cleanly (GAP-01, STATE-LD-14).

    At line 204 (tier-2 ghost action button):
    Change `class="btn btn-ghost btn-sm px-3"` to `class="btn btn-ghost px-sm min-h-11"`.
    Explanation: `px-3` (12px) is replaced with `px-sm` (8px token). The result stays compact while meeting the touch-target floor.

    Do not change the phx-click, phx-value-*, or conditional `:if` attributes on any button. Do not touch tier-1 card container classes.
  </action>
  <verify>
    <automated>grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/support_cards.ex | grep "btn-sm" | grep -v "btn-sm-variant\|btn-small"</automated>
    Expected: 0 lines (btn-sm absent from all CTA buttons).
    <automated>grep -c "btn btn-primary px-md mt-sm min-h-11\|btn btn-ghost px-sm min-h-11" mailglass_admin/lib/mailglass_admin/operator/support_cards.ex</automated>
    Expected: 4 (three tier-1 + one tier-2).
  </verify>
  <done>btn-sm absent from all four support_cards CTA buttons. Tier-1 buttons use btn btn-primary px-md mt-sm min-h-11. Tier-2 uses btn btn-ghost px-sm min-h-11. GAP-01 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component renders → browser DOM | All changes are HEEx class string edits; no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Tampering | deliveries_list aria attributes | accept | Verify steps confirm aria-current/aria-selected not removed by edit |
| T-97-02-02 | Tampering | support_cards phx-click handlers | accept | Only class strings are changed; event bindings are not touched |
| T-97-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure HEEx edits |
</threat_model>

<verification>
After all three tasks:

1. `grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex | grep -c "focus-visible:ring-inset"` — must be 1
2. `grep -c "text-xl" mailglass_admin/lib/mailglass_admin/operator/detail_header.ex mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex` — must be 0
3. `grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/filters_form.ex | grep -c "tracking-\[0.08em\]"` — must be 0
4. `grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/support_cards.ex | grep "btn-sm" | wc -l` — must be 0
5. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5` — must exit 0
</verification>

<success_criteria>
- deliveries_list row buttons carry `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset`; aria-current and aria-selected not regressed.
- Both operator and inbound detail_header h2 elements use `text-heading`; text-xl absent.
- filters_form has zero occurrences of `tracking-[0.08em]`; five label spans carry `text-label font-bold uppercase text-secondary`.
- support_cards has zero `btn-sm` on CTA buttons; four buttons carry `min-h-11` (44px touch target — GAP-01 closed).
- `mix compile --warnings-as-errors` clean.
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-02-SUMMARY.md` when done.
</output>
