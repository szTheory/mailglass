---
phase: 166-earned-greens-and-controls-that-can-pass
plan: 06
type: execute
wave: 6
depends_on: ["166-05"]
files_modified:
  - .github/workflows/post-publish-smoke.yml
  - scripts/release_policy.exs
  - scripts/check_post_publish_target.sh
  - dev/mix/tasks/mailglass.repo.hygiene.ex
  - test/mix/tasks/mailglass.repo.hygiene_test.exs
autonomous: false
requirements: [CTRL-01, CTRL-05]
user_setup: []

estimate:
  tokens: 86000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `workflow_dispatch` of `post-publish-smoke`'s schedule path against the current `inactive` ledger exits 0 and uploads `post-publish-resolution.json` (CTRL-01)."
    - "CTRL-01 is four coordinated edits, not just a new policy verb: a `baseline-versions` resolution verb handling `status == \"inactive\"`; a new `workflow_dispatch` mode input; a cron-guard predicate widened to accept a DISTINCT `baseline=true` alongside `completed=true`; and a baseline-mode target check that does not demand a `publishable_content.digest` (D-17)."
    - "The baseline proof runs the SAME exact Hex checksum and endpoint proof against the published baseline, verifying the three tags resolve to `required_evidence_identifiers.historical_tag_sha` plus `hex_release_checksums` and `hex_release_endpoints` (D-18)."
    - "Dispatch semantics for a LIVE release are unchanged: all existing dispatch inputs stay `required: true` and the exact-40-hex ref guard and exact-SemVer-triple assertions are kept, merely conditioned on mode (D-17, CTRL-01 acceptance)."
    - "`completed=true` is never reused for an inactive ledger — a distinct `baseline=true` signal is emitted, because reusing the completed signal would make a fail-closed control lie (D-17)."
    - "`repo-hygiene` gives `:cannot_check` its own distinct, still-NON-ZERO exit code, separate from `:blocked` — making it exit 0 is prohibited, because that converts an unobservable control into a permanent green (D-34)."
    - "The `repo-hygiene` PR predicate changes from any-open-PR to open more than 14 days OR failing a required check, which requires widening the `gh pr list --json` field list to add `createdAt` and `statusCheckRollup` (D-35)."
    - "`.github/workflows/repo-hygiene.yml` needs no change — the logic lives entirely in the Mix task (D-36)."
    - "The `repo-hygiene` task and its test move in the same commit; the test pins the current behavior (D-35)."
    - "The 40-hex ref regex still rejects 39 characters, 41 characters and uppercase hex on the live-dispatch path; baseline mode does not consult the dispatch ref at all (edge: CTRL-01/boundary)."
    - "Baseline mode compares exact SemVer triples and exact Hex checksums read from the ledger — no prefix, range or fuzzy matching (edge: CTRL-01/precision)."
    - "A PR open for exactly 14 days is NOT blocked and one open for 15 days IS, and a PR with an all-green rollup is not blocked regardless of age below the threshold (edge: CTRL-05 predicate boundary)."
    - statement: "A schedule firing while a dispatch runs resolves independently; neither writes shared state the other reads."
      verification: backstop
    - statement: "An empty open-PR list still concludes pass, and a missing or null `statusCheckRollup` on a PR is treated as not-failing rather than crashing the predicate."
      verification: backstop
    - statement: "CTRL-05's probe row was returned unclassified by the deterministic edge probe and remains UNRESOLVED — surfaced here as a planner assumption rather than silently dropped or auto-dismissed."
      verification: backstop
  artifacts:
    - .github/workflows/post-publish-smoke.yml
    - scripts/release_policy.exs
    - scripts/check_post_publish_target.sh
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - test/mix/tasks/mailglass.repo.hygiene_test.exs
  key_links:
    - "The resolve job's command selection in `post-publish-smoke.yml` chooses which `release_policy.exs` verb runs; the new `baseline-versions` verb must be selected when the ledger status is inactive, and the existing authorized/completed selections must be unchanged for their events."
    - "The cron-guard job's schedule predicate consumes the resolution job's outputs; the new `baseline` output is a DISTINCT boolean, never an overload of `completed`."
    - "`scripts/check_post_publish_target.sh`'s 64-hex digest requirement is unsatisfiable in baseline mode because the ledger close-out sets that digest to null — the bypass is scoped to baseline mode only, never to the live-dispatch path."
    - "`dev/mix/tasks/mailglass.repo.hygiene.ex`'s status aggregation feeds its exit-code mapping; splitting the exit code requires both to change together."
  prohibitions:
    - statement: "Do not add `continue-on-error` to `cron-guard`, and do not drop a cron from `post-publish-smoke.yml`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not delete `recheck_by`, `expired_entries/1`, or `unused_entries/1`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not lower `max_runs` on the inbound property test or narrow its generator."
      status: pending
      verification: flagged-unverified
    - statement: "Do not make `:cannot_check` exit 0 in `repo-hygiene` — a distinct non-zero code is the requirement; exit 0 converts an unobservable control into a permanent green (D-34)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not reuse `completed=true` for an inactive ledger; emit a distinct `baseline=true` (D-17)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not loosen the exact-40-hex ref guard or the exact-SemVer-triple assertions, and do not make any existing dispatch input optional (CTRL-01 acceptance, ASVS V4/V5 partial)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not remove the 64-hex digest requirement from the live-dispatch path; the bypass is scoped to baseline mode only (D-17)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not edit `.planning/release-target.json` as part of this work — ledger edits are their own reviewed PR (CLAUDE.md)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not widen any workflow permissions scope beyond the existing minimal `contents: read` grants."
      status: pending
      verification: flagged-unverified
---

<objective>
Second half of PR-5 (D-37). Two more independent diffs, both of the same shape: a fail-closed control
that is structurally unable to reach its own success state gains a path to EARN green.

CTRL-01: `post-publish-smoke`'s schedule path is unreachable between releases — it hard-branches on
the event and then requires a completed ledger that does not exist while the ledger is `inactive`.
CTRL-05: `repo-hygiene` collapses every non-pass into one exit code and fires on any open PR at all,
so healthy activity reads as an alarm.

**If the timebox tightens, CTRL-01 is the one that must not be dropped (D-37)** — it is milestone exit
criterion 3, and the only requirement whose acceptance needs a `workflow_dispatch` on `main` after
merge.

Output: a baseline resolution path through `post-publish-smoke.yml`, `scripts/release_policy.exs` and
`scripts/check_post_publish_target.sh`; and an exit-code split plus a widened PR predicate in the
repo-hygiene Mix task with its test moving in lockstep.
</objective>

<toolchain_precondition>
Every `<automated>` command below already carries
`export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27` where a `mix` invocation is
involved. The tracked `.tool-versions` pins versions that are not installed locally; bare `mix` fails
with a misleading `corrupt atom table`. Do not "fix" `.tool-versions` — it is the correct CI contract.
</toolchain_precondition>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-CONTEXT.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-RESEARCH.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CTRL-01 — a baseline resolution path that lets the scheduled smoke earn green on an inactive ledger</name>
  <files>.github/workflows/post-publish-smoke.yml, scripts/release_policy.exs, scripts/check_post_publish_target.sh</files>
  <read_first>
    - `.github/workflows/post-publish-smoke.yml` in full. **Re-locate every edit point by literal
      string, never by the line numbers any planning document cites — 166-CONTEXT.md D-17's citations
      are stale by 10 to 133 lines (166-RESEARCH.md Correction 1).** Use:
      `grep -n 'command="authorized-versions"' .github/workflows/post-publish-smoke.yml` for the
      event-branched command selection;
      `grep -n "eventName === 'schedule' && process.env.COMPLETED" .github/workflows/post-publish-smoke.yml`
      for the cron-guard predicate;
      `grep -n 'INPUT_TARGET_REF' .github/workflows/post-publish-smoke.yml` for the fail-closed
      40-hex guard; and
      `grep -n 'workflow_dispatch' -A 30 .github/workflows/post-publish-smoke.yml` for the input block.
    - `scripts/release_policy.exs` — the existing resolution verbs and their shared validation; locate
      with `grep -n 'authorized-versions\|completed-versions\|def close_out_successor\|publishable_content' scripts/release_policy.exs`.
      Note that close-out sets the publishable-content digest to null.
    - `scripts/check_post_publish_target.sh` — the 64-hex digest requirement; locate with
      `grep -n 'digest' scripts/check_post_publish_target.sh`.
    - `.planning/release-target.json` — READ ONLY. Confirm the current status, and the presence of
      `baselines`, `required_evidence_identifiers.historical_tag_sha`, `hex_release_checksums` and
      `hex_release_endpoints`. Do not edit this file.
    - `test/scripts/release_policy_contract_test.exs`, `test/scripts/scheduled_control_evidence_test.exs`,
      `test/scripts/workflow_hardening_contract_test.exs` — the contracts this change must keep green.
    - `166-CONTEXT.md` D-17 and D-18; `166-PATTERNS.md` section on `post-publish-smoke.yml`.
  </read_first>
  <action>
    Four coordinated edits (D-17). Keep every fail-closed property; add a path that can be satisfied.

    (1) `scripts/release_policy.exs`: add a `baseline-versions` resolution verb that handles a ledger
    whose status is the inactive value. It reads `baselines` plus
    `required_evidence_identifiers.historical_tag_sha`, and emits the same resolution shape the
    existing completed-versions verb emits, with the three exact SemVer triples and the exact Hex
    checksum and endpoint values from the ledger (D-18). It must re-validate its own output through
    the same shared full-ledger validator the other verbs use — never a local partial parser. It must
    never emit anything that authorizes a publish.

    (2) `.github/workflows/post-publish-smoke.yml`, the resolve job: extend the event-branched command
    selection so the scheduled path chooses the new verb when the ledger status is inactive, keeping
    the existing completed selection for an active completed ledger and leaving the
    authorized-versions selection for `workflow_dispatch` untouched. Add ONE new `workflow_dispatch`
    mode input (exact name is discretionary per D-17) that lets a maintainer exercise the schedule
    path on demand. Keep every existing dispatch input `required: true`. Keep the exact-40-hex ref
    guard and the exact-SemVer-triple assertions — condition them on mode rather than removing them,
    so the live-release dispatch path validates exactly as it does today.

    (3) The cron-guard job: emit a DISTINCT `baseline` boolean output from the resolution job and
    widen the schedule predicate from "completed must be true" to "completed is true OR baseline is
    true", keeping the SemVer-triple and 40-hex-ref assertions inside the guard. Never set the
    completed signal true for an inactive ledger — that is making a fail-closed control lie, the
    prohibited diff named by name. Add no `continue-on-error` and remove no cron.

    (4) `scripts/check_post_publish_target.sh`: make the 64-hex digest requirement conditional on
    baseline mode, because ledger close-out sets that digest to null and the check is therefore
    unsatisfiable in baseline mode by construction. The live-dispatch path keeps the requirement
    exactly as it is today.

    Add no `permissions:` entry; preserve the existing minimal `contents: read` scopes. Do not modify
    `.planning/release-target.json` — ledger edits are their own reviewed PR.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && actionlint .github/workflows/post-publish-smoke.yml && shellcheck -S warning scripts/check_post_publish_target.sh && python3 -c "import yaml;yaml.safe_load(open('.github/workflows/post-publish-smoke.yml'));print('YAML_OK')"</automated>
    <fails_when>non-zero exit, or stdout lacks YAML_OK, or any actionlint or shellcheck diagnostic is printed</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/scripts/release_policy_contract_test.exs test/scripts/release_policy_test.exs test/scripts/release_policy_close_out_test.exs test/scripts/scheduled_control_evidence_test.exs test/scripts/workflow_hardening_contract_test.exs</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && elixir scripts/release_policy.exs baseline-versions .planning/release-target.json</automated>
    <fails_when>non-zero exit, or the emitted resolution omits any of the three exact SemVer triples, the historical tag SHA, the Hex checksums, or the Hex endpoints read from the ledger</fails_when>
    <automated>cd /Users/jon/projects/mailglass && git diff --quiet -- .planning/release-target.json && echo LEDGER_UNTOUCHED</automated>
    <fails_when>non-zero exit, or stdout lacks LEDGER_UNTOUCHED — the ledger must not be edited by this work</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/release_policy.exs` defines a `baseline-versions` verb that resolves against an
      inactive-status ledger and re-validates through the shared full-ledger validator.
    - `.github/workflows/post-publish-smoke.yml` declares exactly one new `workflow_dispatch` input,
      and every pre-existing dispatch input still carries `required: true`
      (`grep -c 'required: true' .github/workflows/post-publish-smoke.yml` is at least its
      pre-change count).
    - The 40-hex ref regex literal is still present in the file and is reached on the live-dispatch
      path.
    - The resolution job emits a `baseline` output distinct from `completed`, and the cron-guard
      schedule predicate accepts either.
    - `grep -c 'continue-on-error' .github/workflows/post-publish-smoke.yml` has not increased, and
      `grep -c 'cron:' .github/workflows/post-publish-smoke.yml` is unchanged.
    - `scripts/check_post_publish_target.sh`'s 64-hex digest check is conditional on baseline mode and
      still unconditional on the live-dispatch path.
    - No `permissions:` block gained a scope.
    - `.planning/release-target.json` is unmodified.
  </acceptance_criteria>
  <done>The scheduled smoke can resolve a published baseline from an inactive ledger and run the same exact Hex proof, with every live-release guard intact.</done>
  <reversibility rating="costly">A defect here can hard-fail a release-path control or, worse, weaken a live-dispatch guard; reverting is mechanical but the blast radius is the release path, so the guards' immutability is asserted explicitly.</reversibility>
</task>
<task type="auto" tdd="true">
  <name>Task 2: CTRL-05 — split the repo-hygiene exit code and widen the PR predicate, with its test in lockstep</name>
  <files>dev/mix/tasks/mailglass.repo.hygiene.ex, test/mix/tasks/mailglass.repo.hygiene_test.exs</files>
  <read_first>
    - `dev/mix/tasks/mailglass.repo.hygiene.ex` — the `pull_requests/1` function including its
      `gh pr list --json` field list and the any-open-PR predicate; the status aggregation that ranks
      cannot-check above blocked; and the exit-code mapping that collapses every non-pass to a single
      shutdown code. Locate each with
      `grep -n 'pull_requests\|--json\|Enum.empty?\|defp status(\|:shutdown\|cannot_check' dev/mix/tasks/mailglass.repo.hygiene.ex`.
    - `test/mix/tasks/mailglass.repo.hygiene_test.exs` — the existing `pull_requests` cases and the
      `with_hygiene_environment/3` fixture that generates a fake `gh` executable on PATH branching on
      the subcommand and returning a caller-supplied JSON string. Locate with
      `grep -n 'with_hygiene_environment\|pr_response\|pr_query_exit\|:pull_requests' test/mix/tasks/mailglass.repo.hygiene_test.exs`.
    - `.github/workflows/repo-hygiene.yml` — confirm it only invokes the task with pipefail, so no
      workflow change is needed (D-36).
    - `166-CONTEXT.md` D-34, D-35, D-36; `166-PATTERNS.md` section on `mailglass.repo.hygiene.ex`.
  </read_first>
  <behavior>
    Write these test cases in `test/mix/tasks/mailglass.repo.hygiene_test.exs` BEFORE changing the
    task, reusing the existing `gh` stub fixture's `pr_response:` option. Each case supplies a PR JSON
    list and asserts the resulting `:pull_requests` check status:
    - Empty open-PR list: status is pass, open_count 0.
    - One PR created 3 days ago with an all-SUCCESS `statusCheckRollup`: status is pass. This is the
      "a freshly opened healthy PR does not turn it red" half of CTRL-05's acceptance.
    - One PR created exactly 14 days ago with an all-SUCCESS rollup: status is pass (the threshold is
      strictly more than 14 days).
    - One PR created 15 days ago with an all-SUCCESS rollup: status is blocked, and the details name
      the age reason.
    - One PR created 1 day ago with a rollup containing a FAILURE conclusion: status is blocked, and
      the details name the failing-check reason.
    - One PR created 1 day ago with a null or absent `statusCheckRollup`: status is pass, not a crash.
    - The `gh pr list` call failing (non-zero stub exit): status is cannot_check with the existing
      unavailable reason, unchanged.
    Plus an exit-code case: a run whose aggregate status is cannot_check exits with a distinct
    non-zero code, and a run whose aggregate status is blocked exits with a different non-zero code;
    neither exits 0.
  </behavior>
  <action>
    Two edits to the task plus the test cases above, all in one commit (D-35 — the test pins current
    behavior and must move together).

    (a) Exit-code split (D-34). The task currently collapses every non-pass aggregate into one
    shutdown exit. Give the cannot-check aggregate its own distinct exit code, still NON-ZERO, so a
    non-verdict is observably different from an alarm in the workflow log and in any consumer.
    Keep the aggregation's existing precedence (cannot-check outranks blocked). Making cannot-check
    exit 0 is prohibited — that converts an unobservable control into a permanent green, the same
    failure class the release-please gate refuses.

    (b) PR predicate (D-35). Widen the `gh pr list --json` field list to add `createdAt` (needed for
    age) and `statusCheckRollup` (needed for failing-check detection) alongside the fields already
    fetched. Replace the any-open-PR predicate with: blocked when any open PR was created more than
    14 days ago, OR when any open PR's rollup contains a failing conclusion; otherwise pass. Compute
    age against the current time, tolerate a missing, null or empty rollup as not-failing, and keep
    the existing details map shape (open_count and the PR list) so existing consumers do not break —
    extend it with the specific reason rather than replacing it.

    `.github/workflows/repo-hygiene.yml` needs no change (D-36) — do not touch it.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/mix/tasks/mailglass.repo.hygiene_test.exs</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary, or the summary reports fewer tests than the pre-change count plus the eight new cases</fails_when>
    <automated>cd /Users/jon/projects/mailglass && grep -c 'statusCheckRollup' dev/mix/tasks/mailglass.repo.hygiene.ex && grep -c 'createdAt' dev/mix/tasks/mailglass.repo.hygiene.ex</automated>
    <fails_when>either grep prints 0 — the JSON field list was not widened, so the predicate cannot see age or check status</fails_when>
    <automated>cd /Users/jon/projects/mailglass && grep -v '^ *#' dev/mix/tasks/mailglass.repo.hygiene.ex | grep -c 'exit({:shutdown, 0})'</automated>
    <fails_when>stdout prints any number other than 0 — a zero-valued shutdown exit would mean a non-pass verdict can report success</fails_when>
    <automated>cd /Users/jon/projects/mailglass && git diff --quiet -- .github/workflows/repo-hygiene.yml && echo HYGIENE_WORKFLOW_UNTOUCHED</automated>
    <fails_when>non-zero exit, or stdout lacks HYGIENE_WORKFLOW_UNTOUCHED</fails_when>
  </verify>
  <acceptance_criteria>
    - `dev/mix/tasks/mailglass.repo.hygiene.ex`'s `gh pr list --json` field list contains both
      `createdAt` and `statusCheckRollup` in addition to the fields it already fetched.
    - The task exits with two distinct non-zero codes for the cannot-check aggregate and the blocked
      aggregate; neither path exits 0.
    - `test/mix/tasks/mailglass.repo.hygiene_test.exs` contains cases for: empty list, 3-day healthy
      PR, exactly-14-day healthy PR, 15-day PR, failing-rollup PR, null-rollup PR, `gh` query failure,
      and the exit-code split — all green.
    - A freshly opened healthy PR yields `:pull_requests` status pass.
    - `.github/workflows/repo-hygiene.yml` is unmodified.
    - The task and the test file appear in the same commit.
  </acceptance_criteria>
  <done>`repo-hygiene` distinguishes a non-verdict from an alarm by exit code and no longer fires on healthy activity, with the behavior pinned by tests.</done>
  <reversibility rating="reversible">The predicate and exit-code mapping can be retuned freely; no published artifact or external contract depends on them.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Post-merge evidence — the CTRL-01 dispatch and two consecutive green repo-hygiene runs</name>
  <read_first>
    - `166-VALIDATION.md` § Manual-Only Verifications, the CTRL-01 and CTRL-05 rows.
    - The new mode input's exact name as implemented in Task 1.
  </read_first>
  <action>
    Both acceptance criteria here are inherently post-merge and are tracked as pending post-merge
    evidence, never silently assumed passing.

    CTRL-01. After the PR merges to `main`, run a `workflow_dispatch` of `post-publish-smoke` using the
    new mode input to exercise the schedule path against the current inactive ledger. Record the run
    URL, confirm the run exits 0, and confirm `post-publish-resolution.json` is uploaded as an
    artifact. Download the artifact and confirm the resolved versions and tag SHA match the ledger's
    baseline values. This is milestone exit criterion 3.

    CTRL-05. Wait for two consecutive naturally-triggered scheduled `repo-hygiene` runs and record
    both run URLs and their conclusions; each must conclude success with an overall pass status.
    Keep at least one healthy PR open across both firings and record its number, so the "a freshly
    opened healthy PR does not turn it red" half is actually exercised rather than vacuously true on
    an empty PR list.

    Do not dispatch or re-run `repo-hygiene` to manufacture its two observations — they must be
    natural cron firings. The CTRL-01 dispatch is explicitly part of the requirement's acceptance and
    is the one permitted manual invocation here. If either observation does not arrive inside the
    5-day timebox, record what was observed and carry the remainder as explicitly pending evidence —
    do not report it as passing.
  </action>
  <what-built>PR-5 is merged: `post-publish-smoke` has a `baseline-versions` resolution path plus a new dispatch mode input and a widened cron-guard predicate, and `repo-hygiene` has a distinct non-zero cannot-check exit code and an age-or-failing-check PR predicate.</what-built>
  <how-to-verify>1. Run `gh workflow run post-publish-smoke.yml` on `main` with the new mode input; confirm the run exits 0 and uploads `post-publish-resolution.json`, then download the artifact and check its versions and tag SHA against the ledger's baseline values.
2. Wait for two consecutive naturally-triggered scheduled `repo-hygiene` runs; confirm each concluded success with an overall pass, with at least one healthy PR open across both.
3. Confirm no `repo-hygiene` run exited 0 on a non-pass verdict.</how-to-verify>
  <resume-signal>Reply `recorded` with the dispatch run URL, the artifact's resolved values, both `repo-hygiene` run URLs and the open PR number; or `pending: <what is missing>` to carry the shortfall as explicitly pending evidence.</resume-signal>
  <verify>
    <human-check>A post-merge `workflow_dispatch` of `post-publish-smoke`'s schedule path against the inactive ledger exited 0 and uploaded `post-publish-resolution.json` with baseline values matching the ledger (run URL recorded); and two consecutive naturally-triggered scheduled `repo-hygiene` runs concluded success with pass, with at least one healthy PR open across both (both run URLs and the PR number recorded).</human-check>
  </verify>
  <acceptance_criteria>
    - A recorded `post-publish-smoke` dispatch run URL with exit 0 and the uploaded resolution
      artifact, whose resolved versions and tag SHA match `.planning/release-target.json`'s baseline
      values.
    - Two recorded scheduled `repo-hygiene` run URLs, both concluding success with pass, and the
      number of the healthy PR open across both.
    - Any shortfall is recorded as pending evidence, not as a pass.
  </acceptance_criteria>
  <done>The two post-merge observations are recorded with run URLs and artifact evidence, or the shortfall is explicitly carried as pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workflow_dispatch` inputs to the release-path workflow | Human-supplied values cross into a control that gates publish-adjacent verification |
| `.planning/release-target.json` to the resolution job | Repo-local ledger state decides which proof runs |
| GitHub REST API (`gh pr list`) to the hygiene task | Untrusted external JSON crosses into a control verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-20 | Elevation of Privilege | the new `workflow_dispatch` mode input opening a path that skips live-release validation | high | mitigate | The mode input only selects which proof runs; every existing input stays `required: true`, the exact-40-hex ref guard and the exact-SemVer-triple assertions are kept and merely conditioned on mode, and the digest bypass is scoped to baseline mode only. Asserted by acceptance criteria. |
| T-166-21 | Spoofing | reusing `completed=true` for an inactive ledger, making a fail-closed control report a state that does not exist | high | mitigate | A distinct `baseline` output is emitted and the cron-guard predicate accepts either signal explicitly; reuse of the completed signal is a named prohibition. |
| T-166-22 | Tampering | `repo-hygiene` cannot-check silently becoming a green | high | mitigate | The split exit code stays non-zero and the acceptance criteria assert no zero-valued shutdown exit exists in the task. |
| T-166-23 | Tampering | a malicious or malformed `statusCheckRollup` payload crashing or subverting the predicate | medium | mitigate | The predicate tolerates missing, null and empty rollups as not-failing and is exercised by a dedicated test case using the existing `gh` stub fixture; a decode failure still classifies to cannot-check, not to pass. |
| T-166-24 | Elevation of Privilege | `GITHUB_TOKEN` scope creep from the widened `gh pr list --json` field list | high | mitigate | `createdAt` and `statusCheckRollup` are read-only fields on an already-performed read call; no new scope and no new `permissions:` entry. |
| T-166-25 | Information Disclosure | resolution artifact contents | low | accept | `post-publish-resolution.json` carries published versions, tag SHAs and public Hex checksums — all already public. |
| T-166-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. 166-RESEARCH.md § Package Legitimacy Audit shows no `[ASSUMED]`/`[SUS]` package in this phase, so no blocking human legitimacy checkpoint is required. |
</threat_model>

<verification>
- `actionlint .github/workflows/post-publish-smoke.yml` clean; `shellcheck` clean on the target-check
  script; both YAML files parse.
- `mix test test/scripts/ test/mix/tasks/` green.
- The `baseline-versions` verb resolves the current inactive ledger locally and emits the exact
  baseline triples, tag SHA, checksums and endpoints.
- `.planning/release-target.json` and `.github/workflows/repo-hygiene.yml` are unmodified.
- Every diff is auditable as "made a control able to reach its own pass state": an unreachable
  schedule path gains a satisfiable baseline proof, and a collapsed exit code gains a distinguishable
  non-verdict. No gate is relaxed — every live-release guard is retained, and cannot-check remains
  non-zero.

## Post-Merge Verification (166-VALIDATION.md Manual-Only rows owned here)

- [ ] **CTRL-01 row** — a `workflow_dispatch` of the schedule path against the inactive ledger exits 0
      and uploads `post-publish-resolution.json`. Record the run URL and the artifact's resolved
      values. This is milestone exit criterion 3. (Task 3.)
- [ ] **CTRL-05 row** — two consecutive naturally-triggered scheduled `repo-hygiene` runs conclude
      success with pass, with a freshly opened healthy PR open across both. Record both run URLs and
      the PR number. (Task 3.)
</verification>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New symbols / verbs**
- `scripts/release_policy.exs`: a new `baseline-versions` resolution verb handling an inactive-status
  ledger via `baselines` and `required_evidence_identifiers.historical_tag_sha`.

**New workflow inputs / outputs / steps**
- `.github/workflows/post-publish-smoke.yml`: one new `workflow_dispatch` mode input (name chosen at
  implementation, per D-17); a new `baseline` boolean output from the resolution job, distinct from
  `completed`; a widened cron-guard schedule predicate accepting either signal.

**Modified scripts**
- `scripts/check_post_publish_target.sh`: the 64-hex publishable-content digest requirement becomes
  conditional on baseline mode; unconditional on the live-dispatch path.
- `dev/mix/tasks/mailglass.repo.hygiene.ex`: widened `gh pr list --json` field list adding
  `createdAt` and `statusCheckRollup`; a new age-or-failing-check PR predicate; a distinct non-zero
  exit code for the cannot-check aggregate.

**New tests**
- `test/mix/tasks/mailglass.repo.hygiene_test.exs`: eight new cases (empty, 3-day healthy, exactly
  14-day, 15-day, failing rollup, null rollup, `gh` query failure, exit-code split).

**Runtime artifacts produced in CI (not committed)**
- `post-publish-resolution.json` from the baseline-mode run.

**Unchanged by design**
- `.planning/release-target.json` (ledger edits are their own reviewed PR).
- `.github/workflows/repo-hygiene.yml` (D-36).
- Every cron schedule; every `permissions:` block.
</artifacts_this_phase_produces>

<flagged_assumptions>
- No SPEC.md exists, so edge predicates were authored from the deterministic probe report. CTRL-01's
  `concurrency` edge and CTRL-05's `empty` edge are recorded as `backstop` truths: a verifier that
  cannot confirm them with explicit evidence must abstain (`human_needed`, reason
  `insufficient_spec`), never silently pass.
- **CTRL-05's probe row was returned `unclassified` and remains UNRESOLVED.** It is surfaced here
  rather than auto-dismissed or silently dropped. The planner's reading is that CTRL-05's real edge
  surface is the predicate boundary (exactly 14 days versus 15) and the null-rollup case, both of
  which are covered by explicit test cases in Task 2 — but the probe itself proposed no category, so
  this is an assumption, not a resolution.
- The exact `workflow_dispatch` mode input name is Claude's discretion per D-17 and is fixed at
  implementation; Task 3's dispatch instructions must be updated with the chosen name.
- 166-CONTEXT.md D-17's line citations for `post-publish-smoke.yml` are stale by 10 to 133 lines.
  Every edit point in Task 1 is located by literal-string grep instead.
</flagged_assumptions>

<success_criteria>
- CTRL-01: a `workflow_dispatch` of the schedule path against the current inactive ledger exits 0 and
  uploads `post-publish-resolution.json`, with dispatch semantics for a live release unchanged.
- CTRL-05: two consecutive scheduled `repo-hygiene` runs conclude success with pass, and a freshly
  opened healthy PR does not turn it red; `cannot_check` has its own distinct non-zero exit code.
- No gate relaxed: no cron dropped, no `continue-on-error` added to `cron-guard`, no dispatch input
  made optional, no validation loosened, no non-pass verdict exiting 0.
</success_criteria>

<output>
Create `.planning/phases/166-earned-greens-and-controls-that-can-pass/166-06-SUMMARY.md` when done.
</output>
