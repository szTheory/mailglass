---
phase: 166-earned-greens-and-controls-that-can-pass
plan: 05
type: execute
wave: 5
depends_on: ["166-04"]
files_modified:
  - .github/workflows/ci.yml
  - docs/ci-cache-isolation.md
autonomous: false
requirements: [GREEN-04, GREEN-05]
user_setup: []

estimate:
  tokens: 64000
  raw_tokens: 49000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A CI lane resolves the demo app's Hex deps rather than path deps to the working tree: an isolated build runs `MAILGLASS_DEMO_DEPS=hex mix deps.get --check-locked` followed by `mix compile` in `reference/demo_app` (GREEN-04, D-11)."
    - "The isolated hex build is a NEW build with its own deps and build paths — it is never a flip of the existing path-dep demo steps, which deliberately prove the working tree under test and feed core coverage plus the deterministic suite (D-11)."
    - "No lock edit is required: `reference/demo_app/mix.exs` swaps to hex deps when the env var is exactly `hex`, and `reference/demo_app/mix.lock` already carries hex entries for all three packages at 2.0.0, so `--check-locked` succeeds today (D-12)."
    - "GREEN-05 is closed by a committed note at `docs/ci-cache-isolation.md` that DEMONSTRATES non-contamination in three parts: a key-space table of which jobs save which path lists under the shared key string; a CI step listing `reference/host_app/deps` before `deps.get` in each trust lane; and a lock-authority proof that resolution is 2.0.0 (D-15)."
    - "The two trust lanes' cache keys are disambiguated with a lane-specific segment — a one-line, zero-relaxation belt-and-braces addition landing alongside the note (D-15)."
    - "The note records the refutation of FINDINGS FG-4 by construction: `actions/cache` hashes the declared path list into the cache version and the server filters candidates by version, so restore-keys prefix matching cannot rescue a mismatch; and restore extracts all-or-nothing into the workspace, so selective materialization does not exist as a mechanism (D-13)."
    - "The note records that neither trust lane caches `reference/host_app/deps` at all — both declare a deps path only — and that their `deps.get` still honors `reference/host_app/mix.lock`, which pins 2.0.0, so the live-2.6.0 premise was never established either (D-14)."
    - "The PR states the one-run cold-start caveat: changing any existing cache path list or key creates a brand-new cache entry, so a first-run cache-miss message is expected and is not a regression (D-16)."
    - "The note names all five distinct path lists sharing the `mix-` key string and shows they hash to distinct cache versions; lanes with identical path lists are documented as sharing one entry by design (edge: GREEN-05/adjacency)."
    - "The hex-deps build uses deps and build paths disjoint from the existing path-dep demo build, leaving the path-dep `reference/demo_app/deps` and `_build` contents unchanged (edge: GREEN-04/adjacency)."
    - statement: "If the demo-deps env var is unset or not exactly the hex value, the demo silently resolves path deps — the new step sets it explicitly and must fail rather than pass on a silent fallback."
      verification: backstop
    - statement: "The new hex build step runs after the existing path-dep steps and its ordering does not change their outcome."
      verification: backstop
    - statement: "A first run after the lane-specific key segment lands reports a cache miss — a cold start, documented in the PR, not a regression."
      verification: backstop
    - statement: "The trust-lane evidence step's ordering is load-bearing: it must list `reference/host_app/deps` BEFORE `mix deps.get` runs, or it observes the post-install state instead of what restore materialized."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - docs/ci-cache-isolation.md
  key_links:
    - "`reference/demo_app/mix.exs`'s hex-deps predicate reads the `MAILGLASS_DEMO_DEPS` env var and swaps all three sibling deps from path to hex — the new CI step is the only place that env var is set anywhere in `ci.yml`."
    - "`docs/ci-cache-isolation.md` is the deliverable artifact for GREEN-05; the requirement's acceptance bar is demonstration, so the note's three parts each cite observed evidence, not reasoning alone."
    - "The lane-specific cache-key segment lands in the `trust_lane_repo_head` and `trust_lane_clean_baseline` jobs' cache steps only — no other cache step's key or path list changes."
  prohibitions:
    - statement: "Do not add `continue-on-error` to `cron-guard`, and do not drop a cron from `post-publish-smoke.yml`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not delete `recheck_by`, `expired_entries/1`, or `unused_entries/1`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not lower `max_runs` on the inbound property test or narrow its generator."
      status: pending
      verification: flagged-unverified
    - statement: "Do not flip the existing path-dep demo build steps to hex resolution — that would make a required lane compile a published release instead of the branch under test, a silent loss of the exact signal this milestone is about (D-11)."
      status: pending
      verification: flagged-unverified
    - statement: "A 'probably fine' verdict does not satisfy GREEN-05 — the refutation must be demonstrated with cited evidence (requirement text)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not consolidate the 19 cache steps or the five path lists — that is CI-efficiency work, SEED-006 is deferred, and it is filed not fixed (CONTEXT Deferred Ideas)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not add a new CI job for GREEN-04 without registering it in the required-lane registry and the drift guards; prefer an isolated step inside an existing job so no lane-classification contract is silently bypassed."
      status: pending
      verification: flagged-unverified
    - statement: "Do not widen any workflow permissions scope beyond the existing minimal grants."
      status: pending
      verification: flagged-unverified
---

<objective>
First half of PR-5 (D-37). Two genuinely independent, low-coupling diffs: prove the demo app's Hex
pins in a CI lane (GREEN-04), and close the trust-lane cache question in writing with demonstrated
evidence (GREEN-05).

Purpose: the demo app's published-consumer path has never actually been resolved from Hex in CI, and
the cache-pollution premise in FINDINGS FG-4 has never been either confirmed or refuted on the
record.

Output: one isolated hex-resolution build step in `ci.yml`, a lane-specific cache-key segment on the
two trust lanes, and a committed `docs/ci-cache-isolation.md` note that demonstrates rather than
asserts.
</objective>

<toolchain_precondition>
Every `<automated>` command below already carries
`export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27` where a `mix` invocation is
involved. The tracked `.tool-versions` pins versions that are not installed locally; bare `mix` fails
with a misleading `corrupt atom table`. Do not "fix" `.tool-versions` — it is the correct CI contract.
</toolchain_precondition>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-CONTEXT.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-RESEARCH.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GREEN-04 — add an isolated hex-resolution demo build step that never disturbs the path-dep build</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `reference/demo_app/mix.exs` — the three dep-swap helper functions and the predicate reading
      `MAILGLASS_DEMO_DEPS`; locate with `grep -n 'MAILGLASS_DEMO_DEPS\|hex_deps?\|mailglass_dep' reference/demo_app/mix.exs`.
    - `reference/demo_app/mix.lock` — confirm hex entries exist for all three sibling packages
      (`grep -n 'mailglass' reference/demo_app/mix.lock`), so `--check-locked` succeeds without a lock
      edit (D-12).
    - `.github/workflows/ci.yml` — the existing path-dep demo steps in the `support_contract_core:`
      job and in `core_deterministic_suite:`; locate with
      `grep -n 'reference/demo_app' .github/workflows/ci.yml`. These prove the working tree and must
      NOT be flipped (D-11).
    - `.github/workflows/ci.yml` — the `ci_green:` job's `needs:` list and the
      `scripts/ci_green_policy.sh` argument list, to understand why a NEW job would also require
      registry and drift-guard changes.
    - `test/scripts/lane_classification_drift_test.exs` and `test/scripts/ci_parity_drift_test.exs` —
      the lane registries a new job would have to be registered in.
  </read_first>
  <action>
    Implement GREEN-04 as an isolated STEP inside an existing job rather than a new job (D-11 leaves
    this to discretion; a new job would additionally require registration in the required-lane
    registry consumed by `ci_green`, the parity drift test and the lane-classification drift test —
    more coupling and more blast radius for the same signal).

    Add a step named `Prove demo app Hex pins` to the `support_contract_core:` job, placed AFTER the
    existing path-dep demo install and compile steps. Set `working-directory: reference/demo_app` and,
    in the step's `env:` block, set `MAILGLASS_DEMO_DEPS: hex` together with a `MIX_DEPS_PATH` and a
    `MIX_BUILD_PATH` that point at scratch directories distinct from the defaults the path-dep build
    used (for example a hex-suffixed sibling of each). Run
    `mix deps.get --check-locked` followed by `mix compile`.

    The isolation is the load-bearing part: after the step runs, the path-dep build's `deps` and
    `_build` directories must be byte-unchanged. Verify this locally before committing by recording a
    directory listing of `reference/demo_app/deps` before and after the isolated run.

    Change none of the existing demo steps. Do not edit `reference/demo_app/mix.lock`. Add no
    `permissions:` entry. If `--check-locked` fails, STOP and surface it — the fix is not to drop the
    flag.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass/reference/demo_app && before=$(ls -A deps 2>/dev/null | sort | md5) && MAILGLASS_DEMO_DEPS=hex MIX_DEPS_PATH=deps_hex MIX_BUILD_PATH=_build_hex/dev mix deps.get --check-locked && MAILGLASS_DEMO_DEPS=hex MIX_DEPS_PATH=deps_hex MIX_BUILD_PATH=_build_hex/dev mix compile && after=$(ls -A deps 2>/dev/null | sort | md5) && test "$before" = "$after" && echo HEX_BUILD_ISOLATED</automated>
    <fails_when>non-zero exit, or stdout lacks HEX_BUILD_ISOLATED, or the output contains "Mix.Dep.LockError" or "the lock is outdated"</fails_when>
    <automated>cd /Users/jon/projects/mailglass && git diff --quiet -- reference/demo_app/mix.lock reference/demo_app/mix.exs && echo DEMO_SOURCES_UNTOUCHED</automated>
    <fails_when>non-zero exit, or stdout lacks DEMO_SOURCES_UNTOUCHED — the demo app's own sources must not change</fails_when>
    <automated>cd /Users/jon/projects/mailglass && grep -c 'MAILGLASS_DEMO_DEPS' .github/workflows/ci.yml</automated>
    <fails_when>stdout prints any number other than 1 — 0 means the env var was never set, more than 1 means an existing path-dep step was also touched</fails_when>
    <automated>cd /Users/jon/projects/mailglass && actionlint .github/workflows/ci.yml</automated>
    <fails_when>non-zero exit, or any diagnostic line is printed for this file</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains exactly one step setting `MAILGLASS_DEMO_DEPS` to the hex
      value, and it runs `mix deps.get --check-locked` and `mix compile` with
      `working-directory: reference/demo_app`.
    - That step declares `MIX_DEPS_PATH` and `MIX_BUILD_PATH` values distinct from the path-dep
      build's defaults.
    - The existing path-dep demo install and compile steps are unchanged
      (`git diff -- .github/workflows/ci.yml` shows only additions in the new step's range).
    - `reference/demo_app/mix.lock` and `reference/demo_app/mix.exs` are unmodified.
    - The local isolated run resolves all three sibling packages from Hex and compiles, with the
      path-dep `deps` directory listing unchanged before and after.
    - No new CI job was added, so `ci_green`'s `needs:` list and the lane registries are unchanged.
  </acceptance_criteria>
  <done>A CI lane resolves the demo app's Hex deps with `--check-locked`, proven in isolation, with the existing working-tree demo build untouched.</done>
  <reversibility rating="reversible">Deleting the step restores prior behavior; nothing else depends on it.</reversibility>
</task>
<task type="auto">
  <name>Task 2: GREEN-05 — disambiguate the trust-lane cache keys and commit the three-part isolation note</name>
  <files>.github/workflows/ci.yml, docs/ci-cache-isolation.md</files>
  <read_first>
    - `.github/workflows/ci.yml` — every `actions/cache` step: collect each one's `path:` list and
      `key:` expression with `grep -n 'actions/cache' -A 8 .github/workflows/ci.yml`. There are five
      distinct path lists sharing one `mix-` key string, plus a separate `plt-` key.
    - `.github/workflows/ci.yml` — the `trust_lane_repo_head:` and `trust_lane_clean_baseline:` jobs:
      their cache step's `path:` (a bare deps path in both) and their
      `working-directory: reference/host_app` steps including the `mix deps.get`.
    - `reference/host_app/mix.lock` — confirm the sibling pins it actually resolves
      (`grep -n 'mailglass' reference/host_app/mix.lock`).
    - `166-CONTEXT.md` D-13, D-14, D-15, D-16 — the refutation by construction, the two closing facts,
      the three required parts of the note, and the cold-start caveat.
    - `166-PATTERNS.md` § No Analog Found — this note is a genuinely novel artifact shape; D-13/D-14/
      D-15 prose is the source material.
  </read_first>
  <action>
    Two deliverables in this task.

    (a) Cache-key disambiguation. In the `trust_lane_repo_head:` and `trust_lane_clean_baseline:` jobs
    only, add a lane-specific literal segment to the cache `key:` and the `restore-keys:` prefix so
    the two lanes can never select each other's entry even by prefix. Name the segment after the job
    (the exact naming is discretionary per D-15). Change no other cache step's key, and change no
    `path:` list anywhere — altering a path list creates a new cache entry and cold-starts that job
    (D-16), which is acceptable for these two lanes but must not be done incidentally elsewhere.

    (b) Add a trust-lane evidence step. In each of the two trust lanes, immediately BEFORE the
    `mix deps.get` that runs with `working-directory: reference/host_app`, add a step that lists the
    contents of `reference/host_app/deps` (tolerating its absence without failing the job). This is
    part 2 of the D-15 proof: it records what cache restore actually materialized, as opposed to what
    was inferred. The ordering is load-bearing — after `deps.get` it observes the post-install state
    instead.

    (c) Write `docs/ci-cache-isolation.md`. Three parts, each demonstrating rather than asserting:

      Part 1 — Key-space table. One row per `actions/cache` step in `ci.yml`: the job id, the declared
      `path:` list verbatim, and the key expression. Group the rows by distinct path list, and state
      how many independent cache entries the shared `mix-` key string therefore produces. Explain the
      mechanism: `actions/cache` SHA-256-hashes the declared path list into the cache version and the
      server filters candidates by version, so `restore-keys` prefix matching cannot rescue a version
      mismatch; and restore extracts all-or-nothing into the workspace with no path argument, so
      partial or selective materialization does not exist as a mechanism (D-13).

      Part 2 — CI-observed restore evidence. After the PR's first CI run, paste the two trust lanes'
      cache-restore log lines and the output of the new listing step, showing what was actually
      present before `deps.get`. Record the run URL. Note the closing fact that neither trust lane
      declares `reference/host_app/deps` in its cache path at all — the jobs that save that path are
      the core support-contract and core deterministic lanes (D-14).

      Part 3 — Lock-authority proof. Show, from a local run, that the trust lanes' `deps.get` honors
      `reference/host_app/mix.lock` and resolves the sibling packages at the pinned 2.0.0 line — a
      `mix deps.tree` excerpt or a lock diff. This closes the second half of FG-4's premise: the
      "resolving live 2.6.0" claim was never established either (D-14).

      Close the note with an explicit verdict sentence stating that FINDINGS FG-4 is refuted by
      construction, and with the cold-start caveat (D-16) so the first post-merge cache-miss message
      is not misread.

    The PR description must carry the same cold-start caveat.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && actionlint .github/workflows/ci.yml && python3 -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'));print('YAML_OK')"</automated>
    <fails_when>non-zero exit, or stdout lacks YAML_OK, or any actionlint diagnostic is printed</fails_when>
    <automated>cd /Users/jon/projects/mailglass && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/ci.yml'))
keys={}
for jn in ('trust_lane_repo_head','trust_lane_clean_baseline'):
    for s in d['jobs'][jn]['steps']:
        if 'actions/cache' in str(s.get('uses','')):
            keys[jn]=s['with']['key']
print('DISTINCT' if len(set(keys.values()))==2 else 'COLLIDING:'+str(keys))"</automated>
    <fails_when>stdout lacks DISTINCT, or prints a line beginning COLLIDING: — the two trust lanes still share an identical cache key expression</fails_when>
    <automated>cd /Users/jon/projects/mailglass && test -s docs/ci-cache-isolation.md && grep -c 'reference/host_app/deps' docs/ci-cache-isolation.md</automated>
    <fails_when>non-zero exit (the note is missing or empty), or stdout prints 0 — the note never names the path the FG-4 premise was about</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/scripts/</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
  </verify>
  <acceptance_criteria>
    - The `trust_lane_repo_head` and `trust_lane_clean_baseline` cache steps have distinct `key:`
      expressions and distinct `restore-keys:` prefixes.
    - No other `actions/cache` step's `key:` or `path:` changed
      (`git diff -- .github/workflows/ci.yml` touches only those two cache blocks plus the two new
      listing steps and the GREEN-04 step from Task 1).
    - Each trust lane has a listing step for `reference/host_app/deps` positioned before its
      `mix deps.get`, and that step does not fail when the directory is absent.
    - `docs/ci-cache-isolation.md` exists and contains: a table with one row per `actions/cache` step
      in `ci.yml` naming job id, path list and key; an explicit count of distinct cache entries the
      shared key string produces; the cache-version-hashes-the-path-list mechanism; the
      all-or-nothing extraction fact; a CI run URL with pasted restore log lines and listing output; a
      lock-authority excerpt showing 2.0.0 resolution; a verdict sentence refuting FG-4; and the
      cold-start caveat.
    - The note contains no hedged verdict — the strings "probably fine", "likely fine" and "should be
      fine" do not appear in it.
  </acceptance_criteria>
  <done>The trust-lane cache keys are disambiguated, the restore behavior is observed rather than inferred, and the committed note demonstrates non-contamination in three parts.</done>
  <reversibility rating="reversible">Reverting the key segment cold-starts those two caches once more; the note is documentation and can be revised freely.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Post-merge evidence — paste the observed trust-lane restore evidence into the note</name>
  <read_first>
    - `166-VALIDATION.md` § Manual-Only Verifications, the GREEN-05 cache-restore row.
    - `docs/ci-cache-isolation.md` Part 2 as drafted in Task 2.
  </read_first>
  <action>
    Part 2 of the D-15 proof requires a real CI run to observe what `actions/cache` restore actually
    materialized — it cannot be produced pre-push. Trigger it naturally by pushing the PR branch, then
    from that run collect, for each trust lane: the cache step's restore log line (hit or miss, and
    the resolved key), and the output of the new `reference/host_app/deps` listing step.

    Paste both into `docs/ci-cache-isolation.md` Part 2 along with the run URL, and amend the PR.
    Expect and annotate a cache MISS on the two trust lanes in this first run — the lane-specific key
    segment is new, so a cold start is correct behavior, not a regression (D-16).

    Present the completed note to the maintainer for a yes/no on whether it demonstrates rather than
    asserts, since a "probably fine" verdict explicitly does not satisfy GREEN-05.
  </action>
  <what-built>The PR branch is pushed and CI has run: the two trust lanes carry lane-specific cache keys and a `reference/host_app/deps` listing step before `deps.get`, and `docs/ci-cache-isolation.md` Parts 1 and 3 are written with Part 2 awaiting the real run's output.</what-built>
  <how-to-verify>1. Open the PR's CI run; for each trust lane copy the cache step's restore log line (hit or miss and the resolved key) and the listing step's output.
2. Paste both into `docs/ci-cache-isolation.md` Part 2 with the run URL, annotating the expected first-run cold start.
3. Read the whole note and confirm it demonstrates rather than asserts — a hedged verdict does not satisfy GREEN-05.</how-to-verify>
  <resume-signal>Reply `approved` if the note demonstrates non-contamination with real pasted evidence, or `revise: <what is missing>`.</resume-signal>
  <verify>
    <human-check>The maintainer confirms `docs/ci-cache-isolation.md` Part 2 carries real pasted CI output (restore log lines and directory listings) with a run URL, and that the note as a whole demonstrates non-contamination rather than asserting it.</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/ci-cache-isolation.md` Part 2 contains a CI run URL and verbatim pasted output from both
      trust lanes' cache-restore and directory-listing steps.
    - A first-run cache miss on the two trust lanes, if present, is annotated as the expected cold
      start.
    - The maintainer's yes/no is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The note's observed-evidence half is populated from a real run and accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry to the demo build | The isolated build resolves published packages instead of the working tree |
| GitHub Actions cache service to the workspace | Restored archive contents are extracted into `$GITHUB_WORKSPACE` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-16 | Tampering | flipping an existing path-dep demo build to hex resolution | high | mitigate | Named as a plan prohibition and asserted by acceptance criteria: exactly one occurrence of the demo-deps env var in `ci.yml`, and the existing steps unchanged in diff. A flip would make a required lane compile a published release instead of the branch under test. |
| T-166-17 | Tampering | cross-lane cache contamination materializing a different dependency tree than the lane tests | high | mitigate | Refuted by construction in `docs/ci-cache-isolation.md` (cache version hashes the path list; extraction is all-or-nothing) and further prevented by the lane-specific key segment. Part 2 supplies observed evidence, not inference. |
| T-166-18 | Elevation of Privilege | new CI steps | low | accept | The added steps run `mix` and `ls` with no new `permissions:` entry and no secret access. |
| T-166-19 | Repudiation | a GREEN-05 verdict that cannot be audited later | high | mitigate | The note is a committed tracked artifact citing a run URL, verbatim log output and a lock excerpt; the hedged-verdict strings are asserted absent. |
| T-166-SC | Tampering | npm/pip/cargo installs | high | mitigate | The isolated demo build runs `mix deps.get --check-locked` against `reference/demo_app/mix.lock`, so resolution is pinned to already-locked hashes and no unlocked registry fetch occurs. No new package enters any lock. 166-RESEARCH.md § Package Legitimacy Audit shows no `[ASSUMED]`/`[SUS]` package in this phase. |
</threat_model>

<verification>
- `actionlint .github/workflows/ci.yml` clean and the file parses as YAML.
- The local isolated hex build resolves and compiles with `--check-locked`, leaving the path-dep
  `deps` directory unchanged.
- The two trust lanes' cache keys are provably distinct by parsing the workflow.
- `docs/ci-cache-isolation.md` carries all three parts, a verdict, and no hedged language.
- Every diff is auditable as "made a control able to reach its own pass state": a never-exercised
  consumer path is now exercised, and an unproven cache claim is now proven. Nothing is relaxed.

## Post-Merge Verification (166-VALIDATION.md Manual-Only rows owned here)

- [ ] **GREEN-05 cache-restore row** — a real CI run is required to observe `actions/cache` restore
      behavior across the trust lanes. Collect the restore log lines and the
      `reference/host_app/deps` listing output from both lanes, paste them into
      `docs/ci-cache-isolation.md` Part 2 with the run URL, and annotate the expected first-run cold
      start. (Task 3.)
- [ ] **GREEN-04** — confirm on the post-merge `main` run that the `Prove demo app Hex pins` step
      resolved all three sibling packages from Hex and compiled, and record the run URL.
</verification>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New files**
- `docs/ci-cache-isolation.md` — the GREEN-05 deliverable: key-space table, CI-observed restore
  evidence, lock-authority proof, verdict, cold-start caveat.

**New workflow steps / properties in `.github/workflows/ci.yml`**
- Step `Prove demo app Hex pins` in the `support_contract_core` job, with env
  `MAILGLASS_DEMO_DEPS: hex`, `MIX_DEPS_PATH`, `MIX_BUILD_PATH`, and
  `working-directory: reference/demo_app`. First and only use site of `MAILGLASS_DEMO_DEPS` in CI.
- A `reference/host_app/deps` listing step in each of `trust_lane_repo_head` and
  `trust_lane_clean_baseline`, positioned before their `mix deps.get`.
- A lane-specific literal segment added to the `key:` and `restore-keys:` of those two jobs' cache
  steps.

**Unchanged by design**
- The existing path-dep demo install and compile steps.
- `reference/demo_app/mix.exs` and `reference/demo_app/mix.lock`.
- Every other `actions/cache` step's key and path list.
</artifacts_this_phase_produces>

<flagged_assumptions>
- No SPEC.md exists, so edge predicates were authored from the deterministic probe report. Four edges
  across GREEN-04 (`empty`, `ordering`) and GREEN-05 (`empty`, `ordering`) are recorded as `backstop`
  truths: a verifier that cannot confirm them with explicit evidence must abstain (`human_needed`,
  reason `insufficient_spec`), never silently pass.
- D-11 leaves job-versus-step to discretion. This plan chooses a step inside `support_contract_core`
  because a new job would additionally require registration in the required-lane registry consumed by
  `ci_green`, the parity drift test and the lane-classification drift test. If the maintainer prefers
  an independently-reportable lane, that is a deliberate follow-up with those three registrations.
- Cache-shape consolidation across the 19 cache steps and five path lists is explicitly filed, not
  fixed (CONTEXT Deferred Ideas; SEED-006 remains deferred).
</flagged_assumptions>

<success_criteria>
- GREEN-04: a CI lane resolves the demo app's Hex deps rather than path deps to the working tree,
  with `--check-locked` and no lock edit, and the existing path-dep build is untouched.
- GREEN-05: the trust-lane cache keys are disambiguated AND a committed note demonstrates the lanes
  cannot contaminate each other — with observed evidence, not a "probably fine" verdict.
</success_criteria>

<output>
Create `.planning/phases/166-earned-greens-and-controls-that-can-pass/166-05-SUMMARY.md` when done.
</output>
