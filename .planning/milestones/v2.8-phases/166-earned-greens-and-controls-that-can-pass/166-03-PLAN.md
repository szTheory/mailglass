---
phase: 166-earned-greens-and-controls-that-can-pass
plan: 03
type: execute
wave: 3
depends_on: ["166-02"]
files_modified:
  - lib/mailglass/supply_chain/accepted_advisories.ex
  - test/mailglass/supply_chain/accepted_advisories_test.exs
autonomous: false
requirements: [CTRL-04]
user_setup: []

estimate:
  tokens: 68000
  raw_tokens: 52000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`mix mailglass.audit --kind hex` passes with the system clock faked to 2026-12-01, and each advisory disposition cites the evidence it was re-verified against (CTRL-04)."
    - "Both cowlib entries in `lib/mailglass/supply_chain/accepted_advisories.ex` carry `recheck_by: ~D[2027-03-17]` — a 6-month extension, the recorded user decision (D-30)."
    - "Both entries' `:reason` strings carry the permanent-refusal framing: the six closed upstream PR numbers, the maintainer's stated position plus the security-strategy URL, the 2026-09-17 source verification against cowlib 2.20.0, the OSV re-confirmation date with its absent `fixed` event, the no-upgrade-escape fact, and the Cowboy/Gun framework-layer mitigation (D-29)."
    - "Each `:reason` names the cheap falsifiable re-verification: whether a cowlib release above 2.20.0 validates input, or whether OSV adds a `fixed` event (D-30)."
    - "The entry data and the test dates move in the SAME commit — the test currently asserts the direct negation of the acceptance criterion, so splitting them reds the core suite deterministically (D-33, the Phase 125 pin-drift shape, third occurrence)."
    - "`expired_entries/1` and `unused_entries/1` are byte-for-byte unchanged; this is a data-only edit to the `:reason` and `:recheck_by` fields of two entry maps (D-31 lib/ exemption, stated explicitly in plan and PR)."
    - "No in-process `--today` flag or env override is added to `mix mailglass.audit` — that would ship a maintainer-settable expiry bypass (D-32)."
    - "`expired_entries(~D[2027-03-17])` returns `[]` (recheck_by exactly today is not flagged, strictly-after semantics) and `expired_entries(~D[2027-03-18])` returns both entry ids (edge: CTRL-04/adjacency)."
    - "`expired_entries/1` returns ids in `@entries` declaration order — the 43966 entry before the 43969 entry (edge: CTRL-04/ordering)."
    - statement: "`unused_entries/1` still reports an entry that stops appearing in the hex scan, so the allowlist cannot go silently inert after the date bump."
      verification: backstop
  artifacts:
    - lib/mailglass/supply_chain/accepted_advisories.ex
    - test/mailglass/supply_chain/accepted_advisories_test.exs
  key_links:
    - "`dev/mix/tasks/mailglass.audit.ex` calls `AcceptedAdvisories.expired_entries/0` with its `Date.utc_today()` default — the audit's expiry verdict flows straight from the `recheck_by` values edited here."
    - "`test/mailglass/supply_chain/accepted_advisories_test.exs` hardcodes the boundary dates verbatim; it is the lockstep partner of the entry data."
    - "`dev/mix/tasks/mailglass.audit.ex` `@scan_dirs` includes `mailglass_admin`, whose lock carries cowlib — this is what keeps both entries `used` and out of `unused_entries/1` (D-28)."
  prohibitions:
    - statement: "Do not add `continue-on-error` to `cron-guard`, and do not drop a cron from `post-publish-smoke.yml`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not delete `recheck_by`, `expired_entries/1`, or `unused_entries/1` — deleting the expiry machinery does not satisfy CTRL-04."
      status: pending
      verification: flagged-unverified
    - statement: "Do not lower `max_runs` on the inbound property test or narrow its generator."
      status: pending
      verification: flagged-unverified
    - statement: "Do not add a `--today` flag, env override, or `Mailglass.Clock` route to `mix mailglass.audit` — an in-process date override is a maintainer-settable expiry bypass (D-32)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not remove either advisory entry from the allowlist, and do not attempt a cowlib upgrade to clear the audit — 2.20.0 is the newest release and cowboy 2.19.0 requires it (D-28)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not change any function body, guard, or default argument in `accepted_advisories.ex`; only the `:reason` and `:recheck_by` values of the two entry maps may move (D-31)."
      status: pending
      verification: flagged-unverified
---

<objective>
PR-3 of five (D-37), scheduled no later than mid-phase because it carries a hard calendar deadline:
both cowlib advisories expire on 2026-10-26 and red the required `Hex Audit` lane on 2026-10-27 with
zero commits.

Defuse the time bomb truthfully. Upstream has permanently declined the fix on principle (D-26/D-27),
and there is no upgrade escape (D-28) — so the entries stay, their justification is rewritten from a
stale "not yet fixed" framing to a permanent-refusal framing, and `recheck_by` moves to
`~D[2027-03-17]` (D-30, user decision).

**lib/ exemption, stated explicitly (D-31):** the milestone's "no product code changes" constraint
collides with CTRL-04, which necessarily edits `lib/mailglass/supply_chain/accepted_advisories.ex`.
This is a **data/justification edit only** — the `:reason` and `:recheck_by` fields of two entry maps.
No behavior changes. **The PR description must name this exemption** so a reviewer does not read it as
scope drift.

Output: two rewritten entry maps, a rewritten date-boundary test file, and demonstrated evidence that
the audit passes at the faked date.
</objective>

<toolchain_precondition>
Every `<automated>` command below already carries
`export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27`. The tracked
`.tool-versions` pins versions that are not installed locally; bare `mix` fails with a misleading
`corrupt atom table`. Do not "fix" `.tool-versions` — it is the correct CI contract.
</toolchain_precondition>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-CONTEXT.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-RESEARCH.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decide the clock-fake mechanism — does faketime intercept BEAM clock reads?</name>
  <files>none — probe only; no tracked file is modified</files>
  <read_first>
    - `166-RESEARCH.md` Open Question 1 and Assumption A1 — the unverified claim and its documented
      fallback.
    - `dev/mix/tasks/mailglass.audit.ex` — the `--kind hex` path and its call to
      `AcceptedAdvisories.expired_entries()`; locate with
      `grep -n 'expired_entries\|@scan_dirs\|--kind' dev/mix/tasks/mailglass.audit.ex`. Confirm it does
      NOT route through `Mailglass.Clock`.
  </read_first>
  <precondition>`faketime` (libfaketime) is installable locally via `brew install libfaketime` or the distro package; if installation itself fails, record that and take the fallback branch — do not spend time fighting the install.</precondition>
  <action>
    RESEARCH flags as untested whether Erlang/OTP 27's clock reads respect an `LD_PRELOAD`-based fake.
    Settle it with a one-line smoke test before committing to the mechanism, so the executor is never
    blocked.

    Primary branch: install `faketime`, then run a single Elixir expression under it that prints
    `Date.utc_today()` with an apparent clock of 2026-12-01. If the printed date is 2026-12-01, the
    mechanism works — Task 3 will use `faketime` to run the real, unmodified
    `mix mailglass.audit --kind hex`.

    Fallback branch (if the printed date is today's real date, or `faketime` cannot be installed):
    record the negative result explicitly in the SUMMARY and switch Task 3 to the documented fallback —
    a date-boundary unit assertion that `expired_entries/1` returns an empty list at 2026-12-01 after
    the bump (proving the logic), plus a real un-faked `mix mailglass.audit --kind hex` run (proving
    the current date passes). Together these satisfy the criterion's substance without a literal
    system-clock fake.

    Whichever branch is taken, record it as a decision in the SUMMARY with the observed output. Scope
    any `faketime` usage to this single verification invocation — never wrap a whole job, and never
    wrap anything that touches release, publish, or cache-timestamp machinery (threat T-166-07).
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && if command -v faketime >/dev/null 2>&1; then faketime '2026-12-01 00:00:00' elixir -e 'IO.puts(Date.to_iso8601(Date.utc_today()))' | tee /tmp/mg-faketime-probe.txt; else echo FAKETIME_UNAVAILABLE | tee /tmp/mg-faketime-probe.txt; fi</automated>
    <fails_when>the command produces no output file content at all; a printed date of 2026-12-01 selects the primary branch, and either FAKETIME_UNAVAILABLE or any other printed date selects the documented fallback branch — neither outcome is a task failure, but an empty or missing probe result is</fails_when>
  </verify>
  <acceptance_criteria>
    - `/tmp/mg-faketime-probe.txt` is non-empty and contains either an ISO-8601 date or the token
      FAKETIME_UNAVAILABLE.
    - The SUMMARY records which branch was selected (faketime works / fallback) and quotes the probe
      output verbatim.
    - No tracked file is modified by this task (`git status --porcelain` lists no tracked changes).
  </acceptance_criteria>
  <done>The clock-fake mechanism for Task 3 is decided by measurement, with the evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite both advisory entries and their boundary tests in one atomic commit</name>
  <files>lib/mailglass/supply_chain/accepted_advisories.ex, test/mailglass/supply_chain/accepted_advisories_test.exs</files>
  <read_first>
    - `lib/mailglass/supply_chain/accepted_advisories.ex` — the `@entries` list; both entry maps and
      their `:reason` / `:recheck_by` fields; and, to confirm they stay untouched,
      `expired_entries/1` (with its `Date.utc_today()` default) and `unused_entries/1`. Locate with
      `grep -n '@entries\|recheck_by\|accepted_on\|def expired_entries\|def unused_entries' lib/mailglass/supply_chain/accepted_advisories.ex`.
    - `test/mailglass/supply_chain/accepted_advisories_test.exs` — the four date-boundary tests; locate
      every hardcoded sigil date with `grep -n '~D\[' test/mailglass/supply_chain/accepted_advisories_test.exs`.
    - `166-PATTERNS.md` section on `accepted_advisories.ex` — both entries and all four tests quoted
      verbatim; this is the exact before-state.
    - `166-CONTEXT.md` D-26 through D-33 — the upstream evidence to reproduce in the reason text.
  </read_first>
  <action>
    ONE commit, both files (D-33). The existing boundary test asserting that both entries are expired
    at 2026-12-01 is the direct negation of CTRL-04's acceptance criterion — it becomes false the
    instant `recheck_by` moves past that date, so splitting the change produces a deterministic red.

    (a) `lib/mailglass/supply_chain/accepted_advisories.ex`, entry `EEF-CVE-2026-43966` and entry
    `EEF-CVE-2026-43969`: set `recheck_by:` on both to `~D[2027-03-17]` (D-30, user decision — a
    6-month extension that halves the churn of re-litigating a settled refusal while keeping the expiry
    real and the control fail-closed). Leave `accepted_on:`, `id:`, `aliases:`, `package:` and
    `severity:` exactly as they are.

    (b) Rewrite both `:reason` strings. The current text frames the situation as a fix that has not
    landed yet, and is stale against the locked cowlib 2.20.0. Replace that framing with the
    permanent-refusal framing (D-29), and make each reason carry, in prose:
      - the six closed upstream PR numbers on ninenines/cowlib: 154, 163, 164, 165, 166, 169;
      - the maintainer's position that both CVEs are invalid because this validation belongs at a
        different layer and neither Gun nor Cowboy is vulnerable, recorded on ninenines/cowlib#167
        on 2026-08-06, with the URL https://ninenines.eu/articles/security-strategy/;
      - the 2026-09-17 direct source verification against cowlib 2.20.0: `cow_cookie:cookie/1` emits
        values verbatim and `cow_http_struct_hd:escape_string/2` escapes only backslash and
        double-quote, passing CR/LF through;
      - the OSV re-confirmation on 2026-09-09 (one day after cowlib 2.20.0 shipped to Hex on
        2026-09-08), with a SEMVER range carrying an `introduced: 2.9.0` event and NO `fixed` event;
      - the no-upgrade-escape fact: cowlib 2.20.0 is the latest and cowboy 2.19.0 requires
        `>= 2.20.0 and < 3.0.0`, so the repo is already on the newest of both;
      - the framework-layer mitigation mailglass inherits via plug_cowboy: Cowboy 2.16.0+
        `invalid_response_headers` and Gun 2.4.0+ `invalid_request_headers`;
      - the named cheap falsifiable re-verification for the next recheck: does a cowlib release above
        2.20.0 validate input, or does OSV add a `fixed` event.
    Keep each entry's existing entry-specific mirego note verbatim — they remain accurate (the
    `GHSA-g2wm-735q-3f56.yml` empty `first_patched_versions` with its range closing at 2.16.1, and the
    `GHSA-w4f7-4cxr-rv3c` 404/absence). No new schema field is needed; the free-text `:reason` carries
    the justification (D-29).

    (c) `test/mailglass/supply_chain/accepted_advisories_test.exs`: move every hardcoded boundary date
    to the new boundary. The "exactly today is NOT flagged" case asserts an empty list at
    `~D[2027-03-17]`; the "yesterday IS flagged" case asserts both ids at `~D[2027-03-18]`; the case
    that previously asserted both entries expired at 2026-12-01 must now assert an empty list at
    `~D[2027-03-17]` or be retargeted to a date past the new boundary — the test names must describe
    what they now assert, not what they used to. Leave the `accepted_on`-era case at its existing early
    date; it still correctly proves nothing is flagged before `recheck_by` arrives.

    Touch no function body in `accepted_advisories.ex`. Do not add a schema field. Do not add a
    `--today` flag anywhere (D-32).
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/mailglass/supply_chain/accepted_advisories_test.exs</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix run -e 'IO.inspect(Enum.map(Mailglass.SupplyChain.AcceptedAdvisories.expired_entries(~D[2027-03-17]), & &1.id)); IO.inspect(Enum.map(Mailglass.SupplyChain.AcceptedAdvisories.expired_entries(~D[2027-03-18]), & &1.id))'</automated>
    <fails_when>non-zero exit, or the first inspected list is not empty, or the second inspected list is not exactly the two entry ids in declaration order (43966 before 43969)</fails_when>
    <automated>cd /Users/jon/projects/mailglass && git show --stat --pretty=format: "$(git rev-parse HEAD)"</automated>
    <fails_when>the output is not exactly the two paths `lib/mailglass/supply_chain/accepted_advisories.ex` and `test/mailglass/supply_chain/accepted_advisories_test.exs` — the lockstep pair must land as one commit with nothing else riding along</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'recheck_by: ~D\[2027-03-17\]' lib/mailglass/supply_chain/accepted_advisories.ex`
      returns 2.
    - `grep -c 'no upstream fix as of cowlib' lib/mailglass/supply_chain/accepted_advisories.ex`
      returns 0 — the stale framing is gone from both entries.
    - `grep -c 'ninenines.eu/articles/security-strategy' lib/mailglass/supply_chain/accepted_advisories.ex`
      returns at least 1, and `grep -c '2.20.0' lib/mailglass/supply_chain/accepted_advisories.ex`
      returns at least 2.
    - `git show <this task's commit sha> -- lib/mailglass/supply_chain/accepted_advisories.ex` shows changed lines
      only inside the two entry maps' `reason:` and `recheck_by:` values — no `def`, `defp`, guard,
      or default-argument line appears in the diff.
    - `grep -c '2026-10-26' test/mailglass/supply_chain/accepted_advisories_test.exs` returns 0.
    - `mix test test/mailglass/supply_chain/accepted_advisories_test.exs` reports 0 failures.
  </acceptance_criteria>
  <done>Both entries carry the new expiry and the permanent-refusal justification, the boundary tests assert the new dates, and both files landed in one commit.</done>
  <reversibility rating="costly">Reverting restores an expiry that fires on 2026-10-27 and reds a required lane with zero commits; the justification research would have to be re-gathered. Not one-way — the data can be re-edited at any time.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Demonstrate the audit passes at the faked date, by the mechanism Task 1 selected</name>
  <files>none — evidence capture only; no tracked file is modified</files>
  <read_first>
    - The SUMMARY note or probe output from Task 1 recording which branch was selected.
    - `dev/mix/tasks/mailglass.audit.ex` — the `--kind hex` path, `@scan_dirs`, and the expiry and
      unused-entry reporting; confirm no code change is being made here.
  </read_first>
  <action>
    Primary branch (faketime intercepted the BEAM clock in Task 1): run the real, unmodified
    `mix mailglass.audit --kind hex` under `faketime` with an apparent clock of 2026-12-01 00:00:00
    and capture the full output. Scope the wrapper to this single command — never a whole job, and
    never anything touching release, publish, or cache-timestamp machinery.

    Fallback branch (faketime did not intercept, or was unavailable): run the real un-faked
    `mix mailglass.audit --kind hex` and capture its output, AND record the Task 2 date-boundary
    assertion at 2026-12-01 (an empty expired list, proven by the committed test) as the logic half of
    the proof. State in the SUMMARY, plainly, that the literal system-clock fake was not achievable on
    this toolchain and name the substituted evidence — do not claim the faked run happened.

    Either way: also confirm the audit reports no unused entries, so the two allowlist entries are
    still exercised by the `mailglass_admin` lock scan (D-28) and the allowlist has not gone inert.

    Record the exact command, the branch taken, and the verbatim audit output in the SUMMARY. Make no
    file edits in this task.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && if grep -q '2026-12-01' /tmp/mg-faketime-probe.txt; then faketime '2026-12-01 00:00:00' mix mailglass.audit --kind hex; else mix mailglass.audit --kind hex; fi</automated>
    <fails_when>non-zero exit, or the output names either cowlib advisory id as expired, or the output reports any unused allowlist entry</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/mailglass/supply_chain/ test/scripts/</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
  </verify>
  <acceptance_criteria>
    - The audit command exits 0 and its output names neither `EEF-CVE-2026-43966` nor
      `EEF-CVE-2026-43969` as an expired entry.
    - The audit output reports zero unused allowlist entries.
    - The SUMMARY states which branch was taken, quotes the exact command, and quotes the audit output.
    - If the fallback branch was taken, the SUMMARY says so explicitly and does not describe a faked
      run as having occurred.
  </acceptance_criteria>
  <done>The Hex Audit is demonstrated to pass past the old expiry date, by a named and recorded mechanism, with no in-process date override added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Confirm the CTRL-04 lib/ exemption and the clock-proof mechanism before the PR opens</name>
  <read_first>
    - The Task 2 diff of `lib/mailglass/supply_chain/accepted_advisories.ex`.
    - The Task 3 SUMMARY evidence block.
  </read_first>
  <action>
    Present to the maintainer, for an explicit yes/no:
    1. The full `lib/` diff, alongside the D-31 statement that this is a data-only edit to two entry
       maps' `:reason` and `:recheck_by` values and is the milestone's named exemption to the
       "no product code changes" constraint. Confirm the PR description will carry that statement.
    2. Which clock-proof branch was taken (faketime, or the documented fallback), and whether the
       recorded evidence is accepted as satisfying CTRL-04's "clock faked to 2026-12-01" clause.
    3. The new `recheck_by` of `~D[2027-03-17]` and the named falsifiable re-check, confirming the
       6-month window is still the decision.

    Halt until answered. Do not open the PR without item 1 confirmed.
  </action>
  <what-built>The CTRL-04 lockstep commit: both cowlib advisory entries carry `recheck_by: ~D[2027-03-17]` and a permanent-refusal `:reason`, the date-boundary tests assert the new boundary, and Task 3 captured the audit evidence by the branch Task 1 selected.</what-built>
  <how-to-verify>1. Read the `lib/mailglass/supply_chain/accepted_advisories.ex` diff and confirm only `:reason` and `:recheck_by` values changed — no `def`, `defp`, guard or default argument.
2. Read the Task 3 evidence block in the SUMMARY: the exact command, the branch taken (faketime or fallback), and the verbatim audit output showing neither advisory id flagged expired and no unused entry.
3. Confirm the new expiry date and the named falsifiable re-check read as a decision you still endorse.</how-to-verify>
  <resume-signal>Reply `approved` to open the PR with the lib/ exemption named in its description, or `blocked: <reason>` to stop. A rejection of the clock-proof evidence must not be worked around by adding a date override.</resume-signal>
  <verify>
    <human-check>Maintainer confirms: the lib/ diff is data-only and the exemption will be named in the PR; the clock-proof branch and its evidence are accepted; the 2027-03-17 recheck date stands.</human-check>
  </verify>
  <acceptance_criteria>
    - A recorded yes/no on each of the three items, captured in the SUMMARY.
    - If the clock-proof evidence is rejected, the plan stops and the gap is surfaced rather than
      worked around by adding a date override.
  </acceptance_criteria>
  <done>The exemption and the substituted evidence are explicitly accepted by the maintainer and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream advisory feeds (OSV / EEF / Hex) to the committed allowlist | External security claims cross into a repo-local acceptance decision |
| OS clock to the audit task | A faked clock crosses into a control that decides whether an expiry has fired |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-07 | Tampering | a `faketime` wrapper leaking outside its isolated verification step into release/publish machinery or cache timestamps | high | mitigate | `faketime` is invoked for exactly one command in Task 3, locally, never inside a workflow file, and never around any release, publish, or cache step. No workflow file is modified by this plan. |
| T-166-08 | Repudiation | an accepted advisory whose justification no longer matches upstream reality | high | mitigate | The rewritten `:reason` cites dated, checkable evidence (closed PR numbers, maintainer statement URL, OSV re-confirmation date, source verification date) and names the cheap falsifiable re-check for the next cycle. `recheck_by` stays real and the control stays fail-closed. |
| T-166-09 | Elevation of Privilege | an in-process `--today` / env date override becoming a maintainer-settable expiry bypass | high | mitigate | Explicitly prohibited (D-32); no flag, env var, or `Mailglass.Clock` route is added, and the acceptance criteria assert no `def`/`defp` line changes in the module. |
| T-166-10 | Information Disclosure | advisory reason text | low | accept | The reason text contains only public upstream URLs, PR numbers, and version facts. |
| T-166-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; `faketime` is a system tool used locally for verification only and never enters any `mix.lock`. 166-RESEARCH.md § Package Legitimacy Audit rates the phase's only dependency addition OK with no `[ASSUMED]`/`[SUS]` entries. |
</threat_model>

<verification>
- `mix test test/mailglass/supply_chain/accepted_advisories_test.exs` green.
- `mix mailglass.audit --kind hex` exits 0 with no expired and no unused entries, under the selected
  clock mechanism.
- The lockstep commit contains exactly the two files.
- Every diff is auditable as "made a control able to reach its own pass state": the expiry stays, the
  machinery stays, only the justification and the date move — on re-verified upstream evidence.

## Post-Merge Verification (166-VALIDATION.md Manual-Only rows owned here)

- [ ] **CTRL-04 clock-fake row.** After merge, re-run the selected clock-proof command on `main` and
      confirm the `Hex Audit` required lane is green on the merge commit. Record the run URL and the
      command output in the phase VERIFICATION record. If the fallback branch was taken pre-merge,
      this post-merge run is the second, un-faked half of the evidence and must be recorded as such —
      not described as a faked run.
</verification>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Modified data (no new symbols)**
- `lib/mailglass/supply_chain/accepted_advisories.ex`: the `:reason` and `:recheck_by` values of the
  `EEF-CVE-2026-43966` and `EEF-CVE-2026-43969` entry maps. New `recheck_by` value: `~D[2027-03-17]`.
  No new function, attribute, or schema field.

**Rewritten tests**
- `test/mailglass/supply_chain/accepted_advisories_test.exs`: the date-boundary cases retargeted to
  `~D[2027-03-17]` / `~D[2027-03-18]`, including the case that previously asserted both entries were
  expired at 2026-12-01, now asserting the opposite at the new boundary.

**Verification tooling (not committed)**
- A local `faketime` invocation and the probe file `/tmp/mg-faketime-probe.txt`. No workflow step,
  no dependency, no committed script.
</artifacts_this_phase_produces>

<flagged_assumptions>
- No SPEC.md exists, so edge predicates were authored from the deterministic probe report. CTRL-04's
  `empty` edge is a `backstop` truth: a verifier that cannot confirm it with explicit evidence must
  abstain (`human_needed`, reason `insufficient_spec`), never silently pass.
- RESEARCH Assumption A2: D-26/D-27's upstream facts (the OSV re-confirmation dated 2026-09-09 and
  the ninenines PR closures) were carried forward from the CONTEXT session and not independently
  re-fetched during research. If any prove wrong, the `:reason` text needs revision; the mechanical
  fix (date bump plus test lockstep) is unaffected. Task 4 is where a maintainer can challenge it.
- RESEARCH Open Question 1 (does `faketime` intercept BEAM clock reads) is unresolved by design and is
  settled by measurement in Task 1, with both branches planned.
</flagged_assumptions>

<success_criteria>
- CTRL-04: `mix mailglass.audit --kind hex` passes past the old expiry date by the recorded mechanism,
  and each entry's disposition cites the evidence it was re-verified against.
- The expiry machinery (`recheck_by`, `expired_entries/1`, `unused_entries/1`) is intact and
  unmodified.
- The lib/ exemption is stated in both the plan and the PR description.
</success_criteria>

<output>
Create `.planning/phases/166-earned-greens-and-controls-that-can-pass/166-03-SUMMARY.md` when done.
</output>