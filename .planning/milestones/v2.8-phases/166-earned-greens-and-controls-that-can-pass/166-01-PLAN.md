---
phase: 166-earned-greens-and-controls-that-can-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/mix.exs
  - mailglass_admin/mix.lock
  - config/coverage_baselines/admin.json
  - .github/workflows/ci.yml
  - test/scripts/coverage_floor_contract_test.exs
autonomous: true
requirements: [GREEN-01, GREEN-02]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `Support Contract Admin` CI lane log reports at least 510 tests and 0 failures, not 185 (GREEN-01, D-01)."
    - "`mix ci` at the repo root runs the same widened admin alias body — the alias name `verify.support_contract.admin` is unchanged, so `ci_parity_drift_test.exs` stays green with zero `ci.yml` invocation edits (D-02)."
    - "`mailglass_admin` declares `test_coverage: [tool: ExCoveralls]` and `{:excoveralls, \"~> 0.18\", only: [:test]}`, mirroring `mailglass_inbound/mix.exs` (D-05, GREEN-02)."
    - "`config/coverage_baselines/admin.json` carries `covered_lines`, `relevant_lines`, `percentage`, `report_sha256`, `toolchain`, `image`, `command`, `measured_at` — every numeric field measured from a green run of the WIDENED 510-test suite, with no safety margin (D-06)."
    - "`scripts/check_coverage_floor.sh` exits non-zero when any one of the measured triple falls below the committed admin baseline — demonstrated by a deliberate regression drill, not asserted (GREEN-02)."
    - "The widened `verify.support_contract.admin` alias runs exactly the set `mix test` discovers under `mailglass_admin/test/`; the 9 formerly allow-listed files are each executed once, not twice (edge: GREEN-01/adjacency)."
    - "Under `mix ci` the widened admin run and the core suite still share the `mailglass_test` database serially and report 0 failures (edge: GREEN-01/concurrency)."
    - "`scripts/check_coverage_floor.sh` passes when the measured triple exactly equals the baseline and fails when any single member is one unit below it (edge: GREEN-02/boundary)."
    - "`percentage` in `admin.json` is the report-derived float covered/relevant*100 written with no rounding up and no hand-picked margin; the script's comparison is strict less-than (edge: GREEN-02/precision)."
    - statement: "A directory-scoped admin run that discovers zero test files must fail rather than report success on an empty cohort."
      verification: backstop
    - statement: "The widened admin lane's pass/fail verdict is independent of ExUnit's random seed ordering."
      verification: backstop
  artifacts:
    - mailglass_admin/mix.exs
    - mailglass_admin/mix.lock
    - config/coverage_baselines/admin.json
    - .github/workflows/ci.yml
    - test/scripts/coverage_floor_contract_test.exs
  key_links:
    - "The `support_contract_admin` job step `cd mailglass_admin && mix verify.support_contract.admin` in `.github/workflows/ci.yml` resolves to the redefined alias body in `mailglass_admin/mix.exs` — the lane invocation is by NAME and must stay byte-identical."
    - "`test/scripts/ci_parity_drift_test.exs` maps the lane name to the alias NAME `verify.support_contract.admin` — parity is name-matched, not body-matched (D-02)."
    - "New `ci.yml` admin coverage step calls `scripts/check_coverage_floor.sh` against `config/coverage_baselines/admin.json`."
    - "`test/scripts/coverage_floor_contract_test.exs` canonical-baselines test reads the new `admin.json` cohort string."
  prohibitions:
    - statement: "Do not add `continue-on-error` to `cron-guard`, and do not drop a cron from `post-publish-smoke.yml`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not delete `recheck_by`, `expired_entries/1`, or `unused_entries/1`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not lower `max_runs` on the inbound property test or narrow its generator."
      status: pending
      verification: flagged-unverified
    - statement: "`--warnings-as-errors` stays on the widened admin alias body — dropping it to make the lane green is a prohibited relaxation (D-04)."
      status: pending
      verification: flagged-unverified
    - statement: "The admin coverage floor is a measured triple plus report_sha256 behind the exact-toolchain assertion in `scripts/check_coverage_floor.sh` — never a hand-picked percentage (D-06)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not point `verify.support_contract.admin` at `verify.preview`; its asset rebuild plus `git diff --exit-code priv/static/` is a token-parity landmine on a required lane (D-01)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not widen any workflow permissions scope beyond the existing minimal grants."
      status: pending
      verification: flagged-unverified
---

<objective>
PR-1 of five (D-37). Make the `Support Contract Admin` lane tell the truth: replace the
hand-enumerated 9-file allow-list with a directory-scoped run (GREEN-01), and give `mailglass_admin`
the same measured-ratchet coverage floor core and inbound already carry (GREEN-02).

Purpose: 325 of the 510 admin tests have never executed in any CI lane. This is the milestone's
largest blast radius and is deliberately first so failures surface on day 1, not day 4.

Output: a widened alias body, an ExCoveralls-wired `mailglass_admin`, a measured
`config/coverage_baselines/admin.json`, a new CI coverage-floor step, and a demonstrated (not
asserted) proof that the floor fires on regression.

**Atomicity (D-38):** GREEN-01 and GREEN-02 are a mandatory-atomic pair and ship in ONE PR. A
baseline committed before the widening would be measured over the wrong 185-test cohort.
</objective>

<toolchain_precondition>
Every `<automated>` command below already carries
`export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27`. The tracked
`.tool-versions` pins versions that are not installed locally; bare `mix` fails with a misleading
`corrupt atom table`. Do not "fix" `.tool-versions` — it is the correct CI contract.
</toolchain_precondition>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-CONTEXT.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-RESEARCH.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Widen verify.support_contract.admin to a directory-scoped run and prove 510 tests end-to-end</name>
  <files>mailglass_admin/mix.exs</files>
  <read_first>
    - `mailglass_admin/mix.exs` — the alias table; the 9-file allow-list in
      `verify.support_contract.admin` and, for contrast only, `verify.preview` directly above it.
    - `166-PATTERNS.md` section on `mailglass_admin/mix.exs` — the before/after alias bodies, verbatim.
    - `test/scripts/ci_parity_drift_test.exs` — locate the line mapping
      `Support Contract Admin (Elixir 1.18 / OTP 27)` to `["verify.support_contract.admin"]`; confirm
      it matches on alias NAME so no edit is required here.
    - `.github/workflows/ci.yml` — the `support_contract_admin:` job and its
      `run: cd mailglass_admin && mix verify.support_contract.admin` step; confirm no edit is required.
    - `mailglass_admin/test/test_helper.exs` — confirm `ExUnit.start(exclude: [:skip])` (D-03: no
      exclusion work needed).
  </read_first>
  <action>
    This is the phase tracer: one thin path through every layer the phase touches (package alias then
    `mix ci` fan-out then CI lane), wired end-to-end and verified before any expansion work.

    In `mailglass_admin/mix.exs`, replace the body of the `verify.support_contract.admin` alias —
    currently a single `test` invocation listing nine explicit file paths followed by the
    warnings-as-errors flag — with the directory-scoped single-element list whose only member is the
    string `test --warnings-as-errors`, per D-01/D-02. Locate it with
    `grep -n 'verify.support_contract.admin' mailglass_admin/mix.exs`, never by line number.

    Keep `--warnings-as-errors` (D-04 prohibition). Do NOT delegate to `verify.preview` (D-01: it runs
    `mailglass_admin.assets.build` plus a `git diff --exit-code priv/static/` check, a daisyUI rebuild
    that breaks `token_parity_test` on bundle drift unrelated to the 325 newly-run tests).

    Change nothing else: the alias NAME is unchanged, so the `ci.yml` invocation, `mix ci`,
    `verify.phase_05`, the root `mix.exs` `ci.full` and `verify.stability_contract` aliases, and
    `ci_parity_drift_test.exs` all keep working with zero edits (D-02). Do not add a `coveralls.json`
    config file — none exists anywhere in this repo and none is wanted.

    Per D-04 as corrected by RESEARCH Correction 2: do NOT budget or perform a warning-cleanup pass.
    The widened command was measured at zero compile warnings on 1.18.4/OTP 27. If this run
    nevertheless surfaces a genuine compile warning, fix it in the offending admin test file — never
    by relaxing the flag.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass/mailglass_admin && MIX_ENV=test mix verify.support_contract.admin 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports fewer than 510 tests, or any failure count above 0, or the output contains the phrase "Test suite aborted after successful execution due to warnings"</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/scripts/ci_parity_drift_test.exs</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'post_installer_smoke_test.exs' mailglass_admin/mix.exs` returns 0 — the nine explicit
      test paths no longer appear in the alias body.
    - The `verify.support_contract.admin` alias body in `mailglass_admin/mix.exs` is a one-element list
      whose member is exactly the directory-scoped test invocation with the warnings-as-errors flag.
    - `git show --stat --pretty=format: <this task's commit sha>` lists `mailglass_admin/mix.exs` and
      nothing else.
    - `git diff --quiet -- .github/workflows/ci.yml` exits 0 (the workflow is untouched by this task).
    - The admin run's ExUnit summary reports at least 510 tests and 0 failures.
  </acceptance_criteria>
  <done>The directory-scoped alias runs the full admin suite green under the warnings-as-errors flag, `mix ci` parity is unchanged, and the change is committed.</done>
  <reversibility rating="reversible">Restoring the nine-file allow-list is a one-line revert; no data or published artifact is affected.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire ExCoveralls into mailglass_admin, measure the admin baseline, and enforce it in CI</name>
  <files>mailglass_admin/mix.exs, mailglass_admin/mix.lock, config/coverage_baselines/admin.json, .github/workflows/ci.yml, test/scripts/coverage_floor_contract_test.exs</files>
  <read_first>
    - `mailglass_inbound/mix.exs` — the working ExCoveralls wiring to copy: the `test_coverage:` key in
      `project/0` and the `{:excoveralls, "~> 0.18", only: [:test]}` entry in `deps/0`.
    - `mailglass_admin/mix.exs` — `project/0` (where `test_coverage:` lands) and `deps/0` (where the
      dep lands, near the other `only: :test` deps).
    - `config/coverage_baselines/core.json` and `config/coverage_baselines/inbound.json` — the exact
      key set and value shapes to mirror, including `report_sha256`, `image`, `command`, `measured_at`.
    - `scripts/check_coverage_floor.sh` — the consuming ratchet; note its exact-toolchain assertion and
      that it compares `covered_lines` AND `relevant_lines` AND `percentage`.
    - `.github/workflows/ci.yml` — the `Collect and enforce inbound coverage floor` step (the
      `cd mailglass_inbound` then `cd ..` shape) and the core equivalent; plus the
      `support_contract_admin:` job where the new step lands, immediately after
      `Run admin support contract`.
    - `test/scripts/coverage_floor_contract_test.exs` — the canonical-baselines test that currently
      reads only `core.json` and `inbound.json`.
  </read_first>
  <action>
    Four coupled edits, all in this commit (D-05/D-06).

    (a) `mailglass_admin/mix.exs`: add `test_coverage: [tool: ExCoveralls]` to the `project/0` keyword
    list and `{:excoveralls, "~> 0.18", only: [:test]}` to `deps/0`. Run `mix deps.get` in
    `mailglass_admin/` and commit ONLY the resulting clean `excoveralls` (plus any genuinely new
    transitive) entries in `mailglass_admin/mix.lock` — never unrelated transitive drift, per this
    repo's mix.lock discipline.

    (b) Measure the baseline. Run, in `mailglass_admin/`,
    `MIX_ENV=test mix coveralls.json --output-dir ../coverage/admin test --warnings-as-errors` against
    the WIDENED 510-test suite from Task 1. Derive `covered_lines`, `relevant_lines` and `percentage`
    using the same arithmetic `check_coverage_floor.sh` uses: every non-null entry in each
    `source_files[].coverage` map counts as relevant, every entry greater than 0 counts as covered,
    percentage equals covered divided by relevant times 100. Compute `report_sha256` as the sha256 of
    the emitted `coverage/admin/excoveralls.json`. Write `config/coverage_baselines/admin.json` with
    `package` set to `mailglass_admin`, `cohort` set to `test`, `toolchain` set to `1.18.4/27`, the
    same `image` string the sibling baselines carry, a `command` string recording the exact invocation
    used, the measured triple, `report_sha256`, and `measured_at` set to the execution date.
    No safety margin and no rounding up (D-06) — the committed numbers are the measured numbers.

    (c) `.github/workflows/ci.yml`: add a step named `Collect and enforce admin coverage floor` to the
    `support_contract_admin:` job, immediately after the existing `Run admin support contract` step,
    mirroring the inbound step's `cd mailglass_admin` then `cd ..` shape: first
    `mix coveralls.json --output-dir ../coverage/admin test --warnings-as-errors`, then
    `bash scripts/check_coverage_floor.sh config/coverage_baselines/admin.json coverage/admin/excoveralls.json 1.18.4/27`.
    Add no new `permissions:` key and modify no existing step.

    (d) `test/scripts/coverage_floor_contract_test.exs`: extend the canonical-baselines test to also
    read `config/coverage_baselines/admin.json` and assert it names `mailglass_admin`, so a future
    deletion of the admin baseline is caught by the same contract that already guards core and inbound.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass/mailglass_admin && MIX_ENV=test mix coveralls.json --output-dir ../coverage/admin test --warnings-as-errors >/dev/null && cd .. && bash scripts/check_coverage_floor.sh config/coverage_baselines/admin.json coverage/admin/excoveralls.json 1.18.4/27 && echo ADMIN_FLOOR_OK</automated>
    <fails_when>non-zero exit, or stdout lacks the line ADMIN_FLOOR_OK, or stderr contains "coverage regression", "coverage baseline missing", or "coverage toolchain mismatch"</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/scripts/coverage_floor_contract_test.exs</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
    <automated>cd /Users/jon/projects/mailglass && actionlint .github/workflows/ci.yml</automated>
    <fails_when>non-zero exit, or any diagnostic line printed referencing the `support_contract_admin` job</fails_when>
  </verify>
  <acceptance_criteria>
    - `config/coverage_baselines/admin.json` exists and contains the keys `package`, `cohort`,
      `covered_lines`, `relevant_lines`, `percentage`, `toolchain`, `image`, `command`,
      `report_sha256`, `measured_at`.
    - `jq -r .package config/coverage_baselines/admin.json` prints `mailglass_admin` and
      `jq -r .toolchain config/coverage_baselines/admin.json` prints `1.18.4/27`.
    - `jq -r .report_sha256 config/coverage_baselines/admin.json` equals
      `shasum -a 256 coverage/admin/excoveralls.json | cut -d" " -f1` from the measuring run.
    - `grep -c 'config/coverage_baselines/admin.json' .github/workflows/ci.yml` returns 1.
    - `grep -c 'excoveralls' mailglass_admin/mix.lock` returns at least 1.
    - `git show <this task's commit sha> -- mailglass_admin/mix.lock` shows added lines only for
      excoveralls and genuinely new transitives — no version changes to pre-existing entries.
  </acceptance_criteria>
  <done>The admin lane collects coverage and enforces a measured floor mirroring core and inbound; the contract test pins the new baseline's existence.</done>
  <reversibility rating="reversible">Dropping the dep, baseline file and CI step restores prior behavior; nothing published depends on it.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Demonstrate the admin floor actually fires — deliberate regression drill</name>
  <files>none — verification-only drill; config/coverage_baselines/admin.json must end byte-identical to Task 2</files>
  <read_first>
    - `scripts/check_coverage_floor.sh` — the comparison block; note that it fails when covered is
      below baseline OR relevant is below baseline OR percentage is below baseline.
    - `config/coverage_baselines/admin.json` — as written by Task 2.
    - `test/scripts/coverage_floor_contract_test.exs` — the existing non-regressing and fail-closed
      cases, for the shape of a deliberate-failure assertion.
  </read_first>
  <action>
    GREEN-02's acceptance is "the admin lane FAILS when coverage drops below the committed threshold."
    Prove it rather than assert it, and prove all three members of the ratchet, not just percentage.

    Using a throwaway copy of the committed baseline in a scratch directory (never by editing the
    committed file in place, and never by editing the report): produce three perturbed baselines — one
    with `covered_lines` incremented by 1, one with `relevant_lines` incremented by 1, one with
    `percentage` incremented by a small epsilon — and run `scripts/check_coverage_floor.sh` against the
    real measured report with each. Each run must exit non-zero with a `coverage regression` message.
    Then re-run against the unmodified committed baseline and confirm exit 0.

    Record the three observed failure messages and the passing run in the plan SUMMARY as the
    demonstration evidence. Leave `config/coverage_baselines/admin.json` byte-identical to Task 2's
    measured output — `git diff --quiet -- config/coverage_baselines/admin.json` must exit 0 at the end
    of this task. Do not add a safety margin to make the drill easier.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && d=$(mktemp -d) && for f in covered_lines relevant_lines; do jq ".$f += 1" config/coverage_baselines/admin.json > "$d/$f.json"; if bash scripts/check_coverage_floor.sh "$d/$f.json" coverage/admin/excoveralls.json 1.18.4/27 2>/dev/null; then echo "DRILL_FAILED_$f"; else echo "DRILL_FIRED_$f"; fi; done; jq '.percentage += 0.001' config/coverage_baselines/admin.json > "$d/pct.json"; if bash scripts/check_coverage_floor.sh "$d/pct.json" coverage/admin/excoveralls.json 1.18.4/27 2>/dev/null; then echo DRILL_FAILED_percentage; else echo DRILL_FIRED_percentage; fi; bash scripts/check_coverage_floor.sh config/coverage_baselines/admin.json coverage/admin/excoveralls.json 1.18.4/27 && echo BASELINE_STILL_PASSES</automated>
    <fails_when>stdout contains any line beginning DRILL_FAILED_, or is missing any of DRILL_FIRED_covered_lines, DRILL_FIRED_relevant_lines, DRILL_FIRED_percentage, or BASELINE_STILL_PASSES</fails_when>
    <automated>cd /Users/jon/projects/mailglass && git diff --quiet -- config/coverage_baselines/admin.json && echo BASELINE_UNMODIFIED</automated>
    <fails_when>non-zero exit, or stdout lacks BASELINE_UNMODIFIED — meaning the drill left the committed baseline perturbed</fails_when>
  </verify>
  <acceptance_criteria>
    - All three perturbations (covered_lines, relevant_lines, percentage) produce a non-zero exit from
      `scripts/check_coverage_floor.sh` with a `coverage regression` message.
    - The unmodified committed baseline still passes against the same report.
    - `config/coverage_baselines/admin.json` is unchanged in git after the drill.
    - The SUMMARY records the three observed regression messages verbatim.
  </acceptance_criteria>
  <done>The admin coverage floor is demonstrated to fire on each member of the measured triple, and the committed baseline is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry to `mailglass_admin/mix.lock` | A new dependency (`excoveralls`) crosses into the build |
| CI workflow to shell script | `ci.yml` step invokes `scripts/check_coverage_floor.sh` with attacker-irrelevant but integrity-relevant paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-SC | Tampering | `mix deps.get` of `excoveralls` in `mailglass_admin` | high | mitigate | 166-RESEARCH.md § Package Legitimacy Audit rates `excoveralls` OK — already locked at `~> 0.18` in both `mix.lock` (core) and `mailglass_inbound/mix.lock`; this is an internal mirror of an already-vetted dep, no new registry surface. No `[ASSUMED]`/`[SUS]` package, so no blocking legitimacy checkpoint is required. Task 2 commits only the clean intentional lock entry. |
| T-166-01 | Elevation of Privilege | new `ci.yml` admin coverage step | high | mitigate | The step adds no `permissions:` key and inherits the job's existing minimal scope; it runs only `mix coveralls.json` and a repo-local bash script with repo-relative paths. |
| T-166-02 | Tampering | `config/coverage_baselines/admin.json` as a silently-loosened gate | high | mitigate | The measured triple plus `report_sha256` plus the exact-toolchain assertion makes a hand-relaxed floor visible in diff; Task 3's drill proves the ratchet fires on each member. |
| T-166-03 | Information Disclosure | `coverage/admin/excoveralls.json` written into the workspace | low | accept | Coverage reports contain source file paths and hit counts only; no secret material, and the path is already the established convention for core and inbound. |
</threat_model>

<verification>
- `cd mailglass_admin && MIX_ENV=test mix verify.support_contract.admin` reports >= 510 tests, 0 failures.
- `mix test test/scripts/ci_parity_drift_test.exs test/scripts/coverage_floor_contract_test.exs` green.
- `actionlint .github/workflows/ci.yml` clean.
- The full `mix ci` fan-out runs green before the PR opens (admin and core share `mailglass_test`
  serially — confirm no sandbox-ownership red appears now that 325 more tests execute).
- Every diff in this plan is auditable as "made a control able to reach its own pass state": the
  alias widening adds tests, the coverage step adds a gate. Nothing is relaxed.

## Post-Merge Verification (166-VALIDATION.md Manual-Only rows owned here)

None. GREEN-01 and GREEN-02 are both observable in this PR's own required-lane CI run. Record the
`Support Contract Admin` lane log's test count and the coverage-floor step's outcome from that run in
the SUMMARY as the phase-exit evidence for milestone exit criterion 1.
</verification>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New files**
- `config/coverage_baselines/admin.json` — keys: `package`, `cohort`, `covered_lines`,
  `relevant_lines`, `percentage`, `toolchain`, `image`, `command`, `report_sha256`, `measured_at`.

**New symbols / configuration**
- `mailglass_admin/mix.exs`: new `project/0` key `test_coverage: [tool: ExCoveralls]`; new dep
  `{:excoveralls, "~> 0.18", only: [:test]}`.
- `mailglass_admin/mix.lock`: new `excoveralls` entry (plus any genuinely new transitives).
- `.github/workflows/ci.yml`: new step named `Collect and enforce admin coverage floor` in the
  `support_contract_admin` job.
- Redefined (not renamed) Mix alias body: `verify.support_contract.admin`.

**Extended tests**
- `test/scripts/coverage_floor_contract_test.exs`: the canonical-baselines test now also reads
  `config/coverage_baselines/admin.json`.

**Runtime paths produced during CI (not committed)**
- `coverage/admin/excoveralls.json`.
</artifacts_this_phase_produces>

<flagged_assumptions>
- No SPEC.md exists for this phase, so `## Edge Coverage` and `## Prohibitions` were authored from the
  deterministic probe report and the standing prohibition. Two GREEN-01 edges (`empty`, `ordering`)
  are recorded as `backstop` truths: no defensible explicit acceptance criterion exists for them here
  and a verifier that cannot confirm them with explicit evidence must abstain (`human_needed`,
  reason `insufficient_spec`), never silently pass.
</flagged_assumptions>

<success_criteria>
- GREEN-01: the `Support Contract Admin` lane reports >= 510 tests, 0 failures, in CI and under `mix ci`.
- GREEN-02: the admin lane fails when coverage drops below the committed measured triple, demonstrated
  by the Task 3 drill.
- No gate weakened: `--warnings-as-errors` retained, no percentage hand-picked, no asset rebuild added
  to a required lane.
</success_criteria>

<output>
Create `.planning/phases/166-earned-greens-and-controls-that-can-pass/166-01-SUMMARY.md` when done.
</output>
