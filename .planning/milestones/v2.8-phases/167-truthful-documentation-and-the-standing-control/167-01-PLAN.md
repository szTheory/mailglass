---
phase: 167-truthful-documentation-and-the-standing-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
  - CONTRIBUTING.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements: [DOCS-03]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`MAINTAINING.md` states the exclude-paths count as a word, and `test/mailglass/docs_contract_test.exs` derives that count at test time from `release-please-config.json` at JSON path `.packages[\".\"][\"exclude-paths\"]` — no literal count is hardcoded in the test."
    - "Adding or removing an entry from `release-please-config.json`'s core `exclude-paths` array turns the docs contract test red until `MAINTAINING.md`'s prose is updated to match."
    - "`MAINTAINING.md` no longer contains the phrase `hands-free publish fan-out`, and the corrected passage names three `required_reviewers` approval stops."
    - "`CONTRIBUTING.md` no longer instructs a maintainer to make a deliberate `fix(inbound):` floor-bump commit on a core minor release, and the surviving unrelated `fix(inbound):` example earlier in the file is untouched."
    - "`MAINTAINING.md` contains a `## Release Close-Out` section naming the five ledger states, `scripts/release_policy_close_out.sh`, its accepted pre-states, and `scripts/check_published_baseline_of_record.sh`."
    - "The MAINTAINING.md close-out section is complete enough that a reader who has never seen the ledger can run close-out from it alone: it names the five ledger states in order, the wrapper script and its `--write` flag, the three accepted pre-states, the published-baseline guard invoked from inside it, and the rule that a ledger write is its own reviewed PR."
    - statement: "A future release-please-config.json widening PR that changes the exclude-paths count is caught by the derived assertion rather than by a human proofreading MAINTAINING.md."
      verification: backstop
  artifacts:
    - MAINTAINING.md
    - CONTRIBUTING.md
    - test/mailglass/docs_contract_test.exs
  key_links:
    - "`release-please-config.json` `.packages[\".\"][\"exclude-paths\"]` → derived count in `docs_contract_test.exs` → `MAINTAINING.md` prose word"
    - "`scripts/release_policy_close_out.sh` → `scripts/check_published_baseline_of_record.sh` (invoked at :110) → the runbook step order in the new MAINTAINING.md section"
  prohibitions:
    - "No existing assertion in `test/mailglass/docs_contract_test.exs` is deleted, skipped, or loosened."
    - "The unrelated `fix(inbound):` example commit message earlier in `CONTRIBUTING.md` is not removed or blanket-matched by a refute."
    - "No `lib/` file is modified by this plan."
    - "No gate, threshold, or matcher anywhere in the diff is weakened."
---

<objective>
Correct the three false release-mechanics claims in `MAINTAINING.md` / `CONTRIBUTING.md` and add the
close-out runbook `MAINTAINING.md` has never had — each pinned so it cannot drift back (DOCS-03).

Purpose: the omission of a close-out procedure is what stranded the 2.5.0 ledger for four weeks, and
the "twelve exclude-paths" count is exactly the kind of number that silently drifts on every future
widening PR. This plan leads with the phase's tracer: one complete correction carried all the way
through a *derived* pin, proving the pinning mechanism end-to-end before the rest of the phase fans
out against it.

Output: corrected `MAINTAINING.md` + `CONTRIBUTING.md`, and a new `describe "MAINTAINING.md contract"`
/ `describe "CONTRIBUTING.md contract"` pair in `test/mailglass/docs_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-PATTERNS.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-VALIDATION.md
</context>

<execution_preamble>
Every `mix` invocation in this plan MUST be preceded in the same shell by:

export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27

The tracked `.tool-versions` pins versions not installed locally; a bare `mix` fails with a
misleading `corrupt atom table`. Do not modify `.tool-versions`.
</execution_preamble>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — derive the exclude-paths count from release-please-config.json and correct MAINTAINING.md to match</name>
  <files>MAINTAINING.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - MAINTAINING.md (read lines 125-150 for the exact current sentence at :136-137)
    - release-please-config.json (confirm the array lives at `.packages["."]["exclude-paths"]`)
    - test/mailglass/docs_contract_test.exs (lines 1-30 for the module header and `import Mailglass.DocsHelpers`; lines 550-560 for the `assert`/`refute` drift-guard idiom; lines 1049-1074 for the `package_major_minor!` derived-helper idiom)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (Defect Ledger row for MAINTAINING.md:137, and the DOCS-03 note recommending a derived count over a second hardcoded literal)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-PATTERNS.md (the `docs_contract_test.exs` self-analog section)
  </read_first>
  <behavior>
    - Test 1: with `release-please-config.json` carrying 14 entries in `.packages["."]["exclude-paths"]`, the test derives `14`, maps it to the English word `fourteen`, and asserts `MAINTAINING.md` contains the substring `fourteen ` immediately preceding the backticked `exclude-paths` token in that sentence.
    - Test 2 (the RED proof): run the new test BEFORE editing `MAINTAINING.md`. It must fail with a message naming both the derived count and the stale word `twelve`. This is the tracer's end-to-end proof that the pin can actually fail.
    - Test 3 (the negative half): the test also `refute`s the stale string `twelve \`exclude-paths\`` so the corrected prose cannot silently regain the old number.
  </behavior>
  <action>
<!-- planner-discipline-allow: twelve `exclude-paths` -->
<!-- planner-discipline-allow: "fourteen" -->
Add a new `describe "MAINTAINING.md contract"` block to `test/mailglass/docs_contract_test.exs`,
placed after the existing guide-contract describe blocks and before the private helper functions at
the bottom of the file. Inside it add one test named
`exclude-paths count in prose is derived from release-please-config.json`.

The test reads `MAINTAINING.md` with `File.read!/1` (paths are repo-root relative in this file — no
`Path.expand` or `__DIR__`), decodes `release-please-config.json` with
`Jason.decode!(File.read!("release-please-config.json"))`, and reads the count as
`length(get_in(config, ["packages", ".", "exclude-paths"]))`. **The JSON path is
`.packages["."]["exclude-paths"]`, NOT `.["."]["exclude-paths"]`** — 167-PATTERNS.md states the
latter and is wrong; verified against the file this session, the top-level keys are `$schema`,
`last-release-sha`, `packages`, `plugins`.

Never hardcode `14` or `"fourteen"` as the expected value. Add a private helper
`defp count_word!(n)` at the bottom of the test file alongside `package_major_minor!/1`, backed by a
literal map covering at least 1..30 (`1 => "one"` … `30 => "thirty"`), which calls `flunk/1` with a
message naming the out-of-range count when `n` has no mapping — so a future config growth past the
map's ceiling is a loud failure, not a silent skip.

Assert positively that `MAINTAINING.md` matches a regex binding the derived word to the
`exclude-paths` token in the same sentence — build it as
`Regex.compile!("#{word}\\s+`exclude-paths`\\s+entries")` — with a failure message naming the derived
count, the derived word, and the file. Then `refute` the literal stale phrase
`twelve \`exclude-paths\`` with its own message.

Run the test now, before touching prose. It MUST be red. Capture the failure output into the task's
commit body as the tracer's RED evidence.

Then edit `MAINTAINING.md:136-137`, replacing the single word `twelve` with `fourteen` in the
sentence `every non-excluded path, and the twelve \`exclude-paths\` entries (\`#263\`) narrow`.
Change nothing else in that sentence — the `#263` reference is a correct historical citation.

Run the test again. It MUST be green. Commit both the test and the prose edit together with a
`docs(167):` subject so the root-claiming release-please core package does not classify this as a
shippable change.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'twelve `exclude-paths`' MAINTAINING.md` returns `0`.
    - `grep -c 'fourteen `exclude-paths` entries' MAINTAINING.md` returns `1`.
    - `grep -n '"packages", "\.", "exclude-paths"' test/mailglass/docs_contract_test.exs` returns at least one line, proving the derived JSON path is used.
    - `grep -vE '^\s*#' test/mailglass/docs_contract_test.exs | grep -c '"fourteen"'` returns `0` — the word is derived, never asserted as a literal in test code.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
    - The commit body contains the captured pre-correction failure output (the RED proof) showing the test naming both `14`/`fourteen` and the stale `twelve`.
    - Mutation proof, run and discarded: temporarily append one element to `.packages["."]["exclude-paths"]` in `release-please-config.json`, re-run the test, confirm it goes RED, then `git checkout -- release-please-config.json`. The observed RED output is recorded in the commit body.
  </acceptance_criteria>
  <reversibility rating="reversible">A prose word and one test block; reverting is a single `git revert` with no released artifact affected.</reversibility>
  <done>The exclude-paths count claim is generated rather than proofread: it cannot be wrong without the docs contract test being red, and that failure mode was observed, not assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire the "hands-free publish fan-out" wording and CONTRIBUTING.md's mandatory fix(inbound): floor bump</name>
  <files>MAINTAINING.md, CONTRIBUTING.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - MAINTAINING.md (lines 625-670 — the `Monitor the hands-free publish fan-out` heading at :631 and the `hands-free path can never self-skip its own gate` sentence at :662)
    - CONTRIBUTING.md (lines 155-200 — the legitimate unrelated `fix(inbound):` example around :161, and the stale floor-bump paragraph at :186-190)
    - CLAUDE.md (line 119 — the more accurate "three approvals on a linked core+admin+inbound release" statement this correction must agree with; do NOT edit CLAUDE.md here, Plan 02 owns it)
    - mailglass_admin/mix.exs (line ~151) and mailglass_inbound/mix.exs (line ~149) — confirm both declare `{:mailglass, "~> 2.0"}` with no `>=` floor
    - test/mailglass/docs_contract_test.exs (the `describe "MAINTAINING.md contract"` block added by Task 1)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (Defect Ledger rows for MAINTAINING.md:631/:662 and CONTRIBUTING.md:186-190; the scoped-refute warning in the DOCS-03 notes)
  </read_first>
  <behavior>
    - Test 1: `MAINTAINING.md` does not contain `hands-free publish fan-out`.
    - Test 2: `MAINTAINING.md` does not contain the phrase `hands-free path can never self-skip`.
    - Test 3: the corrected MAINTAINING.md passage contains both `required_reviewers` and `three` so deleting the sentence outright cannot pass the refutes.
    - Test 4: `CONTRIBUTING.md` does not contain `requires a deliberate` followed by the `fix(inbound):` token — a narrowly scoped refute that leaves the legitimate example at :161 alone.
    - Test 5: `CONTRIBUTING.md` positively states the current bare-`~>` reality; assert it contains `~> 2.0` in the sibling-mix.exs paragraph.
  </behavior>
  <action>
<!-- planner-discipline-allow: hands-free publish fan-out -->
<!-- planner-discipline-allow: hands-free path can never self-skip -->
<!-- planner-discipline-allow: requires a deliberate -->
<!-- planner-discipline-allow: ~> 1.10 and >= 1.10.2 -->
**MAINTAINING.md — two corrections.** At :631 replace the heading text
`3. **Monitor the hands-free publish fan-out.**` with wording that keeps the step number and bold
shape but states the truth: the fan-out stops for one manual `required_reviewers` approval per
package — three stops on a linked core+admin+inbound release — and "hands-free" describes everything
up to that gate, not past it. At :662 replace the clause
`the hands-free path can never self-skip its own gate` with wording naming the actual mechanism: the
`release` event path is inert for the override, so a release triggered without a dispatch can never
self-skip the gate. Both rewrites must agree with `CLAUDE.md:119`'s already-accurate statement, and
must not delete the surrounding operational guidance — reword only.

**CONTRIBUTING.md — one correction.** Rewrite the paragraph at :186-190 (beginning
`As of v1.15 Phase 125 the sibling packages use hand-maintained pessimistic`). The current text cites
`~> 1.10 and >= 1.10.2` / `~> 1.10` and instructs a deliberate `fix(inbound):` floor-bump commit on a
core minor. Both siblings now declare a bare `{:mailglass, "~> 2.0"}` with no explicit floor. The
corrected paragraph must state: the sibling `mix.exs` core-dep declarations are bare `~>`
constraints; a core patch AND a core minor are both satisfied with no sibling `mix.exs` edit; only a
core **major** requires a deliberate sibling constraint bump. Delete the mandatory `fix(inbound):`
instruction. Leave the unrelated `fix(inbound):` example commit message earlier in the file
completely untouched.

**Pins.** Extend the `describe "MAINTAINING.md contract"` block from Task 1 with one test named
`publish fan-out is described as gated, not hands-free`, and add a new
`describe "CONTRIBUTING.md contract"` block with one test named
`sibling pin guidance describes the bare ~> constraint, not a floor bump`.

Use the negative/positive pairing choice deliberately and record it in the test comments:
- MAINTAINING.md uses **negative-first** (`refute` the two stale phrases) because the defect is a
  specific misleading word, and there are many valid ways to phrase the correction — a positive-only
  assertion would over-constrain the prose. A positive backstop (`assert` on `required_reviewers`
  and `three`) is added so that deleting the sentence cannot pass.
- CONTRIBUTING.md uses a **narrowly scoped negative** (`refute contributing =~ "requires a
  deliberate"` combined with the `fix(inbound):` token, not a blanket `refute` on `fix(inbound):`)
  because a blanket match would false-positive on the legitimate example at :161. Pair it with a
  positive `assert contributing =~ "~> 2.0"` so the paragraph cannot simply be deleted.

Because `MAINTAINING.md` prose is compared byte-for-byte by these refutes, none of the literal stale
strings (`hands-free publish fan-out`, `hands-free path can never self-skip`,
`requires a deliberate`) may appear verbatim anywhere in the rewritten prose, including in any
explanatory comment or parenthetical added by the rewrite.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hands-free publish fan-out' MAINTAINING.md` returns `0`.
    - `grep -c 'hands-free path can never self-skip' MAINTAINING.md` returns `0`.
    - `grep -c 'required_reviewers' MAINTAINING.md` returns at least `1`.
    - `grep -c 'requires a deliberate' CONTRIBUTING.md` returns `0`.
    - `grep -c 'fix(inbound):' CONTRIBUTING.md` returns at least `1` — the legitimate unrelated example survives.
    - `grep -c '~> 1.10 and >= 1.10.2' CONTRIBUTING.md` returns `0`.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0 and reports strictly more tests than the pre-plan baseline of 49.
  </acceptance_criteria>
  <reversibility rating="reversible">Prose rewording plus two test blocks; no released artifact and no gate behavior changes.</reversibility>
  <done>No `grep` for a named stale claim in `MAINTAINING.md` or `CONTRIBUTING.md` returns a hit, and each corrected claim has both a negative and a positive assertion so neither regression nor deletion can pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the MAINTAINING.md Release Close-Out runbook and pin its load-bearing identifiers</name>
  <files>MAINTAINING.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - MAINTAINING.md (skim the section headings to choose the insertion point; read :170-215 for the existing `.planning/` exclude-paths discussion so the new section does not duplicate it)
    - scripts/release_policy_close_out.sh (full — lines 1-45 for the usage string and accepted pre-states; line ~110 for the `check_published_baseline_of_record.sh` invocation)
    - scripts/release_policy.exs (the `cli(["close-out", target_path, tag_sha, checksums_path])` clause around :444)
    - .planning/release-target.json (current `status` value, to name the resting state accurately)
    - CLAUDE.md (line ~21 — the canonical ledger state-machine wording to reuse verbatim for consistency)
    - test/mailglass/docs_contract_test.exs (the `describe "MAINTAINING.md contract"` block from Tasks 1-2)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (DOCS-03 notes — the verified script facts and the recommended 4-step runbook shape; note assumption A1: the runbook prose is a synthesis, the underlying facts are verified)
  </read_first>
  <behavior>
    - Test 1: `MAINTAINING.md` contains a heading whose text is exactly `Release Close-Out`.
    - Test 2: the close-out section contains `scripts/release_policy_close_out.sh`.
    - Test 3: the close-out section contains `check_published_baseline_of_record.sh`.
    - Test 4: the close-out section names all five ledger states — `inactive`, `captured`, `authorized`, `published`, `completed`.
    - Test 5: every script path named in the section exists on disk (`File.exists?/1` per extracted path), so a renamed script turns the doc red instead of silently misdirecting a reader.
  </behavior>
  <action>
Add a new top-level `## Release Close-Out` section to `MAINTAINING.md`, placed immediately after the
publish fan-out section corrected in Task 2 (close-out is what follows a successful fan-out).

Required content, all facts verified in 167-RESEARCH.md § DOCS-03 notes:

1. State the ledger and its state machine using CLAUDE.md's own wording:
   `.planning/release-target.json`, `inactive → captured → authorized → published → completed →
   inactive`. State plainly that a release is not finished when Hex accepts it, and that skipping
   close-out strands the ledger and fails `release-please` closed — the 2.5.0 strand ran about four
   weeks.
2. A numbered runbook with these four steps and nothing vaguer:
   (1) confirm `.planning/release-target.json`'s `status` is one of `authorized`, `published`, or
   `completed` — the wrapper rejects any other value;
   (2) run `scripts/release_policy_close_out.sh --write` (document the full usage line
   `release_policy_close_out.sh [--target PATH] [--repo PATH] [--write]`, and state that omitting
   `--write` is the dry run);
   (3) note that the wrapper re-verifies live Hex checksums and invokes
   `scripts/check_published_baseline_of_record.sh`, which fails the ledger write loudly if the four
   published-baseline records disagree — a loud failure here is the control working, not a bug to
   route around;
   (4) confirm the ledger `status` returned to `inactive` and the published baseline of record
   advanced, then commit the ledger change as its own reviewed PR — a ledger edit is never folded
   into another PR.
3. Name `scripts/release_policy.exs`'s `close-out` CLI verb as the underlying implementation the
   wrapper calls, so a maintainer debugging the wrapper knows where to look.

Write in the project voice: direct, specific, no exclamation, no "simply". Do not include any
sentence that could be read as permission to bypass the baseline guard.

**Pin.** Add one test to the `describe "MAINTAINING.md contract"` block named
`close-out runbook names the ledger states and every script it tells the reader to run`. Extract the
section body by matching from the `## Release Close-Out` heading to the next `^## ` heading or
end-of-file (mirror the `current_compatibility_section!/2` regex shape at
`docs_contract_test.exs:~1062`, but inline — do not modify that helper). Assert the five state names
and both script paths are present in the extracted section. Then `Regex.scan` the section for
`scripts/[a-z0-9_]+\.(sh|exs)` and `assert File.exists?(path)` for each match, with a failure message
naming the missing path. This is the positive-assertion choice: the defect is an *absent* section, so
there is no stale string to refute — only presence and referential integrity can be asserted.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Release Close-Out$' MAINTAINING.md` returns `1`.
    - `awk '/^## Release Close-Out$/,/^## /' MAINTAINING.md | grep -c 'release_policy_close_out.sh'` returns at least `1`.
    - `awk '/^## Release Close-Out$/,/^## /' MAINTAINING.md | grep -c 'check_published_baseline_of_record.sh'` returns at least `1`.
    - For each of `inactive captured authorized published completed`: `awk '/^## Release Close-Out$/,/^## /' MAINTAINING.md | grep -c "$state"` returns at least `1`.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
    - Referential-integrity mutation proof, run and discarded: `git mv scripts/release_policy_close_out.sh scripts/tmp_rename.sh`, re-run the test, confirm RED with the missing path named, then `git mv` back. The observed RED output is recorded in the commit body.
  </acceptance_criteria>
  <reversibility rating="reversible">An additive documentation section plus one test; no behavior, gate, or released artifact is touched.</reversibility>
  <done>A reader who has never seen the ledger can complete close-out from `MAINTAINING.md` alone, and every script path the runbook names is proven to exist by a test rather than by a human having checked once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → release ledger | A human reads `MAINTAINING.md` and then executes privileged release-authorization commands against `.planning/release-target.json` and Hex. The document is the only instruction channel; a wrong instruction here is executed with release credentials. |
| repo prose → GSD/Claude execution | `MAINTAINING.md` and `CONTRIBUTING.md` are read by agents as ground truth during release ceremonies; a false claim is acted on, not just misread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-01 | Tampering | `## Release Close-Out` runbook in MAINTAINING.md | high | mitigate | A close-out procedure documented wrongly is a supply-chain hazard: it authorizes advancing the published baseline of record. Every command, flag, accepted pre-state, and script path in the new section is transcribed from the scripts read in `<read_first>`, and Task 3 pins every named script path with a `File.exists?` assertion plus an observed rename-mutation RED proof, so a renamed or deleted script cannot leave the runbook silently pointing at nothing. |
| T-167-02 | Elevation of Privilege | close-out step 3 wording | high | mitigate | The `check_published_baseline_of_record.sh` guard exists so a baseline disagreement fails loudly before the ledger write. Task 3 explicitly prohibits any sentence readable as permission to bypass it, and states that a loud failure there is the control working. No `--force`, `--skip`, or override flag is documented. |
| T-167-03 | Repudiation | ledger commit guidance | medium | mitigate | Step 4 states the ledger change is committed as its own reviewed PR, preserving a discrete, reviewable audit record per CLAUDE.md's existing rule, rather than being buried inside a docs PR. |
| T-167-04 | Information Disclosure | corrected publish-approval prose | low | accept | The corrected `required_reviewers` wording names the approval mechanism and count, which are already public in `CLAUDE.md:119` and in the workflow file. No secret, token name, or credential path is added. |
| T-167-05 | Tampering | derived exclude-paths assertion | medium | mitigate | `exclude-paths` is the mechanism that keeps tooling commits from becoming phantom releases (RELH-01, #222). Task 1 makes the documented count derived from `release-please-config.json` at test time, so a future widening that a maintainer reads about in MAINTAINING.md can never be understated. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan — no dependency is added to any `mix.exs`, and `mix.lock` is not modified. The Package Legitimacy Gate is not applicable; if any task is later amended to add a dependency, that amendment must run the gate first. |
</threat_model>

<verification>
- `export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0 and reports strictly more tests than the 49-test pre-plan baseline.
- `export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix verify.support_contract.core` exits 0 (this is the required `support_contract_core` CI lane, and `docs_contract_test.exs` is the first file in its list — `mix.exs:323`).
- `git diff --stat` shows exactly three files changed: `MAINTAINING.md`, `CONTRIBUTING.md`, `test/mailglass/docs_contract_test.exs`.
- `git diff` contains no deletion of an existing `assert`, `refute`, or `assert_raise` line, and no added `@tag :skip`.
- CI-lane reachability: none of these three paths is under `.planning/` or `prompts/`, so `ci.yml`'s `Detect Non-Doc Changes` job classifies the PR `code=true` and the full required matrix — including `support_contract_core` — runs these new assertions. Confirm with `sed -n '22,68p' .github/workflows/ci.yml`.
</verification>

<success_criteria>
- All three DOCS-03 defects named in `167-RESEARCH.md`'s Defect Ledger are corrected and the close-out section exists.
- Each correction has a companion assertion in `test/mailglass/docs_contract_test.exs` that runs in the `support_contract_core` lane.
- Two of the three pins were proven to fail against a mutated source (the config-array mutation in Task 1, the script rename in Task 3) — not assumed to work.
- No existing assertion is deleted, skipped, or loosened.
</success_criteria>

<artifacts_this_plan_produces>
New symbols created by this plan:

- `describe "MAINTAINING.md contract"` — new describe block in `test/mailglass/docs_contract_test.exs`
  - test `exclude-paths count in prose is derived from release-please-config.json`
  - test `publish fan-out is described as gated, not hands-free`
  - test `close-out runbook names the ledger states and every script it tells the reader to run`
- `describe "CONTRIBUTING.md contract"` — new describe block in `test/mailglass/docs_contract_test.exs`
  - test `sibling pin guidance describes the bare ~> constraint, not a floor bump`
- `defp count_word!/1` — new private helper in `test/mailglass/docs_contract_test.exs` (integer → English word, 1..30, `flunk` outside range)
- `## Release Close-Out` — new top-level documentation section heading in `MAINTAINING.md`

No new script, Mix task, workflow, or dependency is produced by this plan.
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/167-truthful-documentation-and-the-standing-control/167-01-SUMMARY.md` when done.
</output>

