---
phase: 167-truthful-documentation-and-the-standing-control
plan: 02
type: execute
wave: 2
depends_on: ["167-01"]
files_modified:
  - CLAUDE.md
  - README.md
  - CHANGELOG.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements: [DOCS-02, DOCS-06]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`CLAUDE.md` contains no `{:mailglass, \"== <version>\"}` sibling-pin instruction, and its sibling-pin sentence agrees with the `~>` statement already in the same file."
    - "`CLAUDE.md` no longer claims a Release Please PR auto-merges on green; it names the disarmed-auto-merge plus protected candidate-digest dispatch mechanism the workflow step actually echoes."
    - "The CLAUDE.md `~>` assertion does not pass merely because the file's earlier paragraph already contains `~>`; the pin assertion is scoped to the sibling-pin sentence being corrected."
    - "`README.md` links `guides/upgrading-to-v2_0.md`, and the test generalizes to every `guides/upgrading-*.md` file on disk so a future upgrade guide cannot go unlinked either."
    - "If `Path.wildcard(\"guides/upgrading-*.md\")` returns an empty list the README-link test flunks rather than passing vacuously."
    - "`CHANGELOG.md`'s 2.0.0 section names the PostgreSQL schema-isolation move as the adopter-facing breaking change, appended after — never replacing — the release-please-generated bullet."
    - statement: "Deleting the CLAUDE.md sibling-pin sentence entirely fails the positive assertion rather than passing the refute."
      verification: backstop
    - statement: "README link matching compares each guide's full relative path as a byte substring of README.md, so a case- or directory-variant link does not count as linked."
      verification: backstop
  artifacts:
    - CLAUDE.md
    - README.md
    - CHANGELOG.md
    - test/mailglass/docs_contract_test.exs
  key_links:
    - "`Path.wildcard(\"guides/upgrading-*.md\")` → per-file basename assertion against `README.md` body"
    - "`.github/workflows/release-please.yml:904` step echo → the corrected CLAUDE.md auto-merge sentence"
  prohibitions:
    - "No release-please-generated CHANGELOG.md history line is deleted, reworded, or reordered."
    - "No new file is created under `guides/` — `guides/upgrading-to-v2_0.md` already exists."
    - "No existing assertion in `test/mailglass/docs_contract_test.exs` is deleted, skipped, or loosened."
    - "No `lib/` file is modified by this plan."
---

<objective>
Make `CLAUDE.md` describe the release pipeline as it actually behaves, and make the v2.0 upgrade path
discoverable from `README.md` and honestly labeled in `CHANGELOG.md` (DOCS-02, DOCS-06).

Purpose: `CLAUDE.md` is read as ground truth by both maintainers and by Claude Code itself during
release ceremonies — a maintainer following it today is told to wait for an auto-merge that has been
deliberately disarmed, and to re-pin a sibling with an `==` constraint the same file's own line 24
declares stale. `guides/upgrading-to-v2_0.md` already exists on disk but is reachable only by
guessing its filename.

Output: corrected `CLAUDE.md`, a README link, an appended CHANGELOG clarification, and a
`describe "CLAUDE.md contract"` block plus a generalized upgrade-guide link assertion in
`test/mailglass/docs_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-PATTERNS.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-01-SUMMARY.md
</context>

<execution_preamble>
Every `mix` invocation in this plan MUST be preceded in the same shell by:

export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27

Do not modify `.tool-versions`.
</execution_preamble>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct CLAUDE.md's sibling-pin and auto-merge claims, pinned by a scoped drift-guard block</name>
  <files>CLAUDE.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - CLAUDE.md (read lines 18-30 for the already-correct `~>` statement the correction must agree with; lines 50-60 for the contradictory sibling-pin bullet at :56; lines 110-122 for the auto-merge bullet at :119; lines 11-17 for the inbound "stable `1.0` contract" line at :13-15)
    - .github/workflows/release-please.yml (lines 890-915 — the `Arm auto-merge on the release PR` step at ~:898 and the exact echo at ~:904)
    - mailglass_admin/mix.exs (line ~151) and mailglass_inbound/mix.exs (line ~149) — both declare `{:mailglass, "~> 2.0"}`
    - test/mailglass/docs_contract_test.exs (the `describe "MAINTAINING.md contract"` / `describe "CONTRIBUTING.md contract"` blocks added by Plan 01, to place the new block consistently; lines 550-560 for the drift-guard idiom)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (Defect Ledger rows for CLAUDE.md:56, :119, :13; DOCS-02 notes including landmine 3 — line 56 must be made to agree with line 24, never the reverse; and the judgement that the inbound "stable 1.0 contract" line is defensible)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-PATTERNS.md (the example `describe "CLAUDE.md contract"` shape)
  </read_first>
  <behavior>
    - Test 1: `CLAUDE.md` does not contain the token sequence for an exact sibling pin instruction in the Engineering DNA bullet — specifically the file does not contain a `mailglass_admin/mix.exs declares` sentence paired with an `==` constraint.
    - Test 2: the corrected Engineering DNA bullet positively contains `~> ` and the words `linked-versions`, so deleting the bullet cannot pass the refute.
    - Test 3: `CLAUDE.md` does not contain `auto-merges on green`.
    - Test 4: the corrected release bullet positively contains both `Disarmed` (or `disarmed`) and `candidate-digest`, tying the prose to the workflow step's own echo.
    - Test 5 (adjacency guard): the sibling-pin assertion is extracted from the Engineering DNA bullet specifically, not from the whole file, so the pre-existing `~>` in the "release mechanics" paragraph cannot satisfy it on its own.
  </behavior>
  <action>
<!-- planner-discipline-allow: {:mailglass, "== <version>"} -->
<!-- planner-discipline-allow: auto-merges on green -->
Three edits to `CLAUDE.md`, all one-liners.

**Edit 1 — the sibling-pin bullet at :56.** The bullet currently ends
`` `mailglass_admin/mix.exs` declares `{:mailglass, "== <version>"}`. `` Replace the trailing
constraint with the bare pessimistic form the sibling mix files actually carry:
`` `mailglass_admin/mix.exs` declares `{:mailglass, "~> <core-major.minor>"}`, and the linked-versions
plugin locks the minor at release time. `` This makes :56 agree with :24, which is the correct
direction — :24 is newer, more detailed, and independently corroborated by both sibling `mix.exs`
files. Do not weaken or rewrite :24.

**Edit 2 — the auto-merge claim at :119.** The bullet currently asserts a Release Please PR
auto-merges on green. The workflow's `Arm auto-merge on the release PR` step
(`.github/workflows/release-please.yml`, step at ~:898) now echoes at ~:904 that ordinary auto-merge
is disarmed and a later protected exact candidate-digest dispatch is required. Rewrite the clause to
state that: ordinary auto-merge on the release PR is disarmed, and the merge proceeds only via a
later protected exact candidate-digest dispatch. Do not simply delete the auto-merge sentence — a
maintainer must be told what replaces it. Leave the rest of the bullet (the `hex-publish`
environment, `required_reviewers`, the three-approval count, "Plan a release with a human present",
and the do-not-remove-the-gate warning) exactly as written; that part is already accurate and is a
security control statement.

**Edit 3 — the inbound contract line at :13-15.** 167-RESEARCH.md judges this claim defensible
rather than false (assumption A3): `mailglass_inbound/docs/api_stability.md` genuinely is the
canonical contract inventory and inbound has never had a breaking removal. Soften the wording for
precision only — replace `now on its own stable \`1.0\` contract` with
`now on its own stable contract (semver-honored since 1.0)` — and do **not** spend a pinning
assertion on it. Record in the SUMMARY that this line was softened, not pinned, and why.

**Pin.** Add a new `describe "CLAUDE.md contract"` block to `test/mailglass/docs_contract_test.exs`
with two tests: `sibling pin guidance matches the ~> convention the sibling mix files carry` and
`release PR merge path is described as disarmed plus protected dispatch`.

Negative/positive choice, to be recorded as a comment in the test:
- Both defects are **specific wrong strings**, so the primary assertion is a `refute` — the corrected
  prose can legitimately be phrased many ways and a positive-only assertion would over-constrain a
  human-edited file.
- Each `refute` is paired with a **positive backstop** asserting the corrected concept
  (`~>` + `linked-versions` for the first, `disarmed` + `candidate-digest` for the second) so that
  deleting the sentence entirely cannot turn the test green.
- The adjacency hazard is real: `CLAUDE.md` already contains `~>` in an unrelated paragraph. Scope
  the positive sibling-pin assertion to the Engineering DNA bullet by first extracting the single
  line beginning with `- **Sibling packages with linked-version releases.**` via
  `Enum.find(String.split(claude, "\n"), &String.starts_with?(&1, "- **Sibling packages"))` and
  asserting against that line alone, flunking with a named message if the bullet is absent.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"== <version>"' CLAUDE.md` returns `0`.
    - `grep -c 'auto-merges on green' CLAUDE.md` returns `0`.
    - `grep -c 'required_reviewers' CLAUDE.md` returns at least `1` — the accurate security-control half of the bullet survived the rewrite.
    - `grep -c 'three approvals' CLAUDE.md` returns at least `1`.
    - `grep -n -- '- \*\*Sibling packages with linked-version releases' CLAUDE.md` returns exactly one line, and that line contains `~>`.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
    - Adjacency mutation proof, run and discarded: delete the Engineering DNA sibling-pin bullet from `CLAUDE.md`, re-run the test, confirm RED (the positive backstop fires, not a vacuous pass), then `git checkout -- CLAUDE.md` and re-apply the intended edits. The observed RED output is recorded in the commit body.
  </acceptance_criteria>
  <reversibility rating="reversible">Three prose one-liners plus one test block; nothing released, no gate behavior changed.</reversibility>
  <done>A maintainer following `CLAUDE.md` through a release is never told to wait for an auto-merge that will not happen, nor to apply an `==` sibling pin the repo abandoned in Phase 125 — and both claims are held in place by paired negative/positive assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Link the v2.0 upgrade guide from README and name the real v2.0 breaking change in CHANGELOG</name>
  <files>README.md, CHANGELOG.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - README.md (lines 268-300 — the `## Documentation` list where `upgrading-to-v1_0.md` and `upgrading-from-v0_1.md` are already linked)
    - guides/upgrading-to-v2_0.md (skim the opening to write an accurate one-line description; the file ALREADY EXISTS — do not create it)
    - CHANGELOG.md (lines 165-180 — the 2.0.0 heading and its single `⚠ BREAKING CHANGES` bullet)
    - test/mailglass/docs_contract_test.exs (line ~505-516 — the `learning-path is registered in both mix.exs docs lists` test showing the `File.exists?` + README-registration idiom to generalize)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (DOCS-06 notes: the guide already exists, CHANGELOG must be APPENDED not rewritten, and assumption A2 — pull the exact v2.0 phrasing from the milestone archive rather than the research draft)
    - .planning/milestones/ (locate the v2.0 archive records to source the authoritative description of what shipped in v2.0 — the Phases 132-137 PostgreSQL schema-isolation move)
  </read_first>
  <behavior>
    - Test 1: for every path returned by `Path.wildcard("guides/upgrading-*.md")`, `README.md` contains that path as a substring.
    - Test 2: the wildcard result is asserted non-empty first, so an empty `guides/` directory flunks rather than passing vacuously.
    - Test 3: the failure message for an unlinked guide names the specific unlinked path, not a count.
    - Test 4: the region of `CHANGELOG.md` from the `## [2.0.0]` heading to the next `## [` heading contains `schema` and `132` (or the authoritative phase range sourced from the milestone archive).
    - Test 5: `CHANGELOG.md` still contains the original release-please-generated bullet text — asserted positively, so this phase cannot be the diff that rewrote release history.
  </behavior>
  <action>
**README.md.** Add one bullet to the `## Documentation` list linking
`guides/upgrading-to-v2_0.md`, placed immediately before the existing
`guides/upgrading-to-v1_0.md` bullet so the list reads newest-upgrade-path first. Match the
surrounding style exactly: a markdown link whose text is the backticked path, an em dash, and a
one-line description sourced from the guide's own opening — not invented. Do not create or rename any
file under `guides/`; `guides/upgrading-to-v2_0.md` already exists.

**CHANGELOG.md.** Immediately after the existing `⚠ BREAKING CHANGES` bullet in the `## [2.0.0]`
section, APPEND one new bullet. Do not delete, reword, or reorder the existing bullet — it is
release-please-generated history, and rewriting it would falsify the record (this is explicitly named
in REQUIREMENTS.md Out of Scope). The appended bullet names the adopter-facing breaking change: the
PostgreSQL schema-isolation move shipped by Phases 132-137, relocating the mailglass tables out of
the `public` schema. Source the exact scope wording from the v2.0 milestone archive under
`.planning/milestones/` rather than from 167-RESEARCH.md's draft (research assumption A2 flags the
draft as unsourced). Prefix the bullet so its provenance is unambiguous, e.g. a leading
`**Note (added Phase 167):**`.

**Pins.** Two additions to `test/mailglass/docs_contract_test.exs`:

1. In the existing README-contract describe block, a new test
   `every upgrade guide on disk is linked from README.md`. Compute
   `guides = Path.wildcard("guides/upgrading-*.md")`, `refute guides == []` with a named message,
   then `Enum.each(guides, fn path -> assert readme =~ path, "README.md does not link #{path}" end)`.
   Compare the full relative path (`guides/upgrading-to-v2_0.md`), not the basename, so a link that
   points at a different directory does not count. This is the **positive-assertion** choice, chosen
   because the defect is an absence: there is no stale string to refute, and generalizing across the
   wildcard means the next upgrade guide is covered without anyone editing the test.
2. A new test `2.0.0 changelog entry names the schema-isolation breaking change`. Extract the 2.0.0
   section with a regex from `^## \[2\.0\.0\]` to the next `^## \[` or end-of-file, assert the
   section contains the schema-move wording, AND assert the original release-please bullet substring
   is still present — the second assertion is what makes this diff auditable as additive rather than
   history-rewriting.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'guides/upgrading-to-v2_0.md' README.md` returns at least `2` (markdown link target plus link text).
    - `for g in guides/upgrading-*.md; do grep -q "$g" README.md || echo "UNLINKED $g"; done` prints nothing.
    - `ls guides/upgrading-to-v2_0.md` succeeds and `git status --porcelain guides/` shows no added or renamed file.
    - `awk '/^## \[2\.0\.0\]/,/^## \[1\./' CHANGELOG.md | grep -ci 'schema'` returns at least `1`.
    - `git diff CHANGELOG.md | grep -c '^-'` returns `1` — the diff-header line only, proving no existing CHANGELOG line was deleted.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
    - Generalization mutation proof, run and discarded: `touch guides/upgrading-to-v9_9.md`, re-run the test, confirm RED naming that exact path, then `rm guides/upgrading-to-v9_9.md`. The observed RED output is recorded in the commit body.
  </acceptance_criteria>
  <reversibility rating="reversible">One README bullet, one appended CHANGELOG line, two tests; fully revertible and nothing published changes.</reversibility>
  <done>A 1.x adopter reaching the README finds the v2 upgrade guide without prior knowledge, the 2.0.0 changelog names the Postgres schema move alongside its original tooling note, and a future upgrade guide cannot go unlinked without the docs contract test going red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo prose → agent execution | `CLAUDE.md` is the front-page context loaded by Claude Code on every session in this repo. A false release-mechanics claim here is executed, not merely read. |
| adopter → upgrade path | A 1.x adopter reads `README.md`/`CHANGELOG.md` to decide whether and how to cross a major version boundary that relocates their database tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-06 | Tampering | CLAUDE.md release bullet at :119 | high | mitigate | The bullet at :119 carries a live security control statement (`hex-publish` `required_reviewers`, three approvals, "do not remove that gate"). Task 1 rewrites only the auto-merge clause and pins the surviving control language with an explicit acceptance criterion (`required_reviewers` and `three approvals` must still grep to ≥1), so the correction cannot accidentally delete the gate description. |
| T-167-07 | Spoofing | corrected merge-path description | medium | mitigate | Replacing "auto-merges on green" with an accurate "disarmed, protected exact candidate-digest dispatch required" description removes a false expectation that could lead a maintainer to assume a release merged itself and skip verifying who authorized it. The corrected text is transcribed from the workflow step's own echo, not paraphrased from memory. |
| T-167-08 | Repudiation | CHANGELOG.md 2.0.0 section | high | mitigate | CHANGELOG.md is the published, immutable release record. Task 2 appends only; an explicit acceptance criterion (`git diff CHANGELOG.md \| grep -c '^-'` returns exactly `1`, the diff header) mechanically proves no historical line was deleted, and a test asserts the original generated bullet is still present. |
| T-167-09 | Information Disclosure | README upgrade-guide link | low | accept | Linking an already-published guide that already ships in the Hex docs exposes nothing new. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; no dependency is added and `mix.lock` is not modified. The Package Legitimacy Gate is not applicable. |
</threat_model>

<verification>
- `export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix verify.support_contract.core` exits 0.
- `git diff --stat` shows exactly four files: `CLAUDE.md`, `README.md`, `CHANGELOG.md`, `test/mailglass/docs_contract_test.exs`.
- `git diff` contains no deleted `assert`/`refute`/`assert_raise` line and no added `@tag :skip`.
- None of the four paths is under `.planning/` or `prompts/`, so `ci.yml`'s `Detect Non-Doc Changes` job classifies the PR `code=true` and the `support_contract_core` required lane runs these assertions.
</verification>

<success_criteria>
- All three DOCS-02 CLAUDE.md claims are corrected; the two falsifiable ones are pinned with paired negative/positive assertions and the third is explicitly and deliberately left unpinned with the reason recorded.
- DOCS-06's README link exists and is held by a wildcard-generalized assertion covering future guides.
- The CHANGELOG correction is provably additive.
</success_criteria>

<artifacts_this_plan_produces>
New symbols created by this plan:

- `describe "CLAUDE.md contract"` — new describe block in `test/mailglass/docs_contract_test.exs`
  - test `sibling pin guidance matches the ~> convention the sibling mix files carry`
  - test `release PR merge path is described as disarmed plus protected dispatch`
- test `every upgrade guide on disk is linked from README.md` — added to the existing `describe "README.md contract"` block
- test `2.0.0 changelog entry names the schema-isolation breaking change` — added to `test/mailglass/docs_contract_test.exs`
- One new bullet in `README.md`'s `## Documentation` list
- One new appended bullet in `CHANGELOG.md`'s `## [2.0.0]` `⚠ BREAKING CHANGES` block

No new script, Mix task, workflow, dependency, or guide file is produced by this plan.
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/167-truthful-documentation-and-the-standing-control/167-02-SUMMARY.md` when done.
</output>
