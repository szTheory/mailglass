---
phase: 08-release-engineering-hardening
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - lib/mailglass/outbound/async_adapter.ex
  - lib/mailglass/outbound/async_adapter/task_supervisor.ex
  - lib/mailglass/outbound/async_adapter/inline.ex
  - lib/mailglass/outbound.ex
  - test/support/citext_probe.ex
  - test/support/mailer_case.ex
  - test/support/data_case.ex
  - test/support/webhook_case.ex
  - test/test_helper.exs
  - test/mailglass/persistence_integration_test.exs
  - .github/workflows/ci.yml
autonomous: false
requirements: [REL-10]
must_haves:
  truths:
    - "Mailglass.Outbound.AsyncAdapter behaviour exists with the narrow `dispatch(fun, opts)` callback (D-08-11)"
    - "Two impls ship: TaskSupervisor (prod default) and Inline (test default)"
    - "AsyncAdapter resolution uses a NEW env key `:async_adapter_impl` (NOT `:async_adapter`) — the existing `:async_adapter` key reads `:task_supervisor | :oban` atoms and MUST NOT collide (verified in PATTERNS.md and CONTEXT.md callsite map at outbound.ex lines 437 + 607)"
    - "Mailglass.TestSupport.CitextProbe extracts the probe loop from data_case.ex/mailer_case.ex/persistence_integration_test.exs into a single shared module (D-08-10)"
    - "MailerCase, DataCase, WebhookCase all call CitextProbe.run/1 in their setup blocks; AdminCase inherits via use Mailglass.MailerCase (no direct change per D-08-10)"
    - "test_helper.exs calls CitextProbe.run([]) at cold start after migrations"
    - "Inline impl honors Mailglass.Tenancy.with_tenant/2 re-stamping (D-08-15) — caller-side wrapping in outbound.ex already does this; behaviour signature stays narrow"
    - "Snapshot-then-restore pattern (HI-01) applies to the new :async_adapter_impl env key in MailerCase setup"
    - "5 consecutive `mix test --warnings-as-errors --seed <random>` runs pass locally (PR-A acceptance per D-08-13)"
    - "PR-A ships AsyncAdapter + CitextProbe + CaseTemplate hardening; PR-B adds advisory tests-strict CI lane; PR-C flips Tests lane to halt-on-failure (branch-protection update is szTheory-only)"
    - "Random seed retained in CI per D-08-14 — no `--seed 0` pin"
    - "PR-C deletes the advisory tests-strict lane after the existing lane is flipped"
  artifacts:
    - path: "lib/mailglass/outbound/async_adapter.ex"
      provides: "AsyncAdapter behaviour namespace + dispatch/2 callback"
      exports: ["dispatch/2"]
    - path: "lib/mailglass/outbound/async_adapter/task_supervisor.ex"
      provides: "Prod-default impl wrapping Task.Supervisor.start_child"
      exports: ["dispatch/2"]
    - path: "lib/mailglass/outbound/async_adapter/inline.ex"
      provides: "Test-default impl running closure synchronously"
      exports: ["dispatch/2"]
    - path: "test/support/citext_probe.ex"
      provides: "Shared citext OID-cache drain"
      exports: ["run/1"]
    - path: ".github/workflows/ci.yml"
      provides: "Tests lane flipped to continue-on-error: false (PR-C); advisory tests-strict lane deleted"
      contains: "mix test"
  key_links:
    - from: "lib/mailglass/outbound.ex (lines ~437, 607)"
      to: "Mailglass.Outbound.AsyncAdapter.dispatch/2"
      via: "module dispatch with caller-side Tenancy.with_tenant/2 wrap retained"
      pattern: "AsyncAdapter.dispatch"
    - from: "AsyncAdapter.dispatch/2"
      to: "Application.get_env(:mailglass, :async_adapter_impl, TaskSupervisor)"
      via: "behaviour-impl resolution at call time"
      pattern: ":async_adapter_impl"
    - from: "DataCase/MailerCase/WebhookCase setup blocks"
      to: "Mailglass.TestSupport.CitextProbe.run([])"
      via: "ExUnit setup callback"
      pattern: "CitextProbe.run"
---

<objective>
Re-tighten the Tests gate from advisory (`continue-on-error: true`) to
halt-on-failure. Two foundational pieces ship first; then the gate flips
in a 3-PR rollout (D-08-13):

1. **AsyncAdapter behaviour** (D-08-11) — new `Mailglass.Outbound.AsyncAdapter`
   pluggable behaviour with `TaskSupervisor` (prod default) and `Inline`
   (test default) impls, mirroring the `Mailglass.Clock` injection pattern.
   Replaces the 2 `Task.Supervisor.start_child(Mailglass.TaskSupervisor, ...)`
   callsites in `lib/mailglass/outbound.ex` (verified ~lines 437 + 607).
2. **CitextProbe extraction** (D-08-10) — collapse the 3 duplicated probe
   loops (data_case.ex:62-69, mailer_case.ex:94-101, persistence_integration_test.exs:60-73)
   into `Mailglass.TestSupport.CitextProbe.run/1`. Wire into every CaseTemplate
   setup block + `test_helper.exs` cold-start.
3. **3-PR rollout (D-08-13):**
   - **PR-A** ships #1 + #2 + CaseTemplate hardening; existing Tests lane
     keeps `continue-on-error: true`. Acceptance: 5 random-seed runs locally.
   - **PR-B** adds new advisory `tests-strict` CI lane (NOT required); soaks
     for ~1 week (>=5 random-seed runs in CI).
   - **PR-C** flips existing Tests lane to `continue-on-error: false`; deletes
     advisory lane; updates branch protection (szTheory-only admin action).

Engineering DNA: AsyncAdapter is the **5th first-class behaviour** (after
`Mailglass.Tenancy`, `Mailglass.Clock`, `Mailglass.Adapters`,
`Mailglass.OptionalDeps`) — "Pluggable behaviours over magic" (CLAUDE.md).

**Critical env-key disambiguation (CONTEXT.md flag confirmed):** the
existing `:async_adapter` key in `outbound.ex:362-364` reads `:task_supervisor | :oban`
atoms, NOT a behaviour module. The new behaviour resolution uses a NEW
key `:async_adapter_impl`. DO NOT collide. The PATTERNS.md `<truths>` and
this plan's `<truths>` both call this out — do not repurpose `:async_adapter`.

Purpose: Bare `mix test` halts on failure consistently with no
citext-OID-cache flakes and no async-test sandbox-ownership leaks.

Output: New AsyncAdapter behaviour + 2 impls, new CitextProbe module,
hardened CaseTemplates, 3 ci.yml diffs across PR-A/B/C, branch-protection
update flagged for szTheory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-engineering-hardening/08-CONTEXT.md
@.planning/phases/08-release-engineering-hardening/08-PATTERNS.md
@./CLAUDE.md
@lib/mailglass/clock.ex
@lib/mailglass/clock/system.ex
@lib/mailglass/clock/frozen.ex
@lib/mailglass/outbound.ex
@test/support/mailer_case.ex
@test/support/data_case.ex

<interfaces>
Behaviour callback (PATTERNS.md §async_adapter.ex lines 78-80):
```
@callback dispatch(fun :: (-> any()), opts :: keyword()) :: {:ok, pid()} | :ok
```

Resolution (mirror clock.ex:32-37):
```
defp impl do
  case Application.get_env(:mailglass, :async_adapter_impl) do
    nil -> Mailglass.Outbound.AsyncAdapter.TaskSupervisor
    mod when is_atom(mod) -> mod
  end
end
```

Existing prod callsite (outbound.ex ~line 437):
```
Task.Supervisor.start_child(Mailglass.TaskSupervisor, fn ->
  Mailglass.Tenancy.with_tenant(tenant_id, fn ->
    ...
  end)
end)
```

Replacement:
```
Mailglass.Outbound.AsyncAdapter.dispatch(fn ->
  Mailglass.Tenancy.with_tenant(tenant_id, fn ->
    ...
  end)
end, [])
```

Caller-side `with_tenant/2` re-stamping is preserved (D-08-15) — works for both prod (fresh process) and test (caller process).

CitextProbe extraction target (PATTERNS.md §test/support/citext_probe.ex lines 209-253):
```
defmodule Mailglass.TestSupport.CitextProbe do
  @moduledoc \"\"\"
  ...
  \"\"\"
  @default_max_attempts 5

  @spec run(keyword()) :: :ok
  def run(opts \\\\ []) do
    repo = Keyword.get(opts, :repo, Mailglass.TestRepo)
    max_attempts = Keyword.get(opts, :max_attempts, @default_max_attempts)
    do_probe(repo, max_attempts)
  end

  defp do_probe(_repo, 0), do: :ok
  defp do_probe(repo, remaining) do
    try do
      repo.query!(\"SELECT 'probe'::citext\")
      :ok
    rescue
      Postgrex.Error -> do_probe(repo, remaining - 1)
    end
  end
end
```

HI-01 snapshot/restore pattern (mailer_case.ex:122-125, 179-191):
```
prior = Application.get_env(:mailglass, :KEY)
Application.put_env(:mailglass, :KEY, value)
on_exit(fn ->
  if prior != nil do
    Application.put_env(:mailglass, :KEY, prior)
  else
    Application.delete_env(:mailglass, :KEY)
  end
end)
```

Apply this pattern to `:async_adapter_impl` in MailerCase setup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AsyncAdapter behaviour + TaskSupervisor + Inline impls + swap outbound.ex callsites (PR-A part 1)</name>
  <files>lib/mailglass/outbound/async_adapter.ex, lib/mailglass/outbound/async_adapter/task_supervisor.ex, lib/mailglass/outbound/async_adapter/inline.ex, lib/mailglass/outbound.ex</files>
  <read_first>
    - lib/mailglass/clock.ex (entire file — exact template for behaviour resolution)
    - lib/mailglass/clock/system.ex (impl pattern)
    - lib/mailglass/clock/frozen.ex (alternative impl pattern)
    - lib/mailglass/outbound.ex (entire file; specifically locate the 2 `Task.Supervisor.start_child(Mailglass.TaskSupervisor, ...)` callsites — PATTERNS.md says lines 437 + 607 but line numbers may have drifted)
    - lib/mailglass/optional_deps.ex (namespace+moduledoc shape — AsyncAdapter mirrors this)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md (D-08-11, D-08-15, D-08-29)
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §async_adapter.ex + §async_adapter/task_supervisor.ex + §async_adapter/inline.ex + §lib/mailglass/outbound.ex
  </read_first>
  <action>
    Part A — Create `lib/mailglass/outbound/async_adapter.ex`:

    ```
    defmodule Mailglass.Outbound.AsyncAdapter do
      @moduledoc false
      # Internal — may move to public surface in Phase 9 per D-08-29.
      #
      # Pluggable behaviour for async dispatch in the Outbound pipeline.
      # Two impls ship in v0.2:
      #
      #   * Mailglass.Outbound.AsyncAdapter.TaskSupervisor (prod default)
      #   * Mailglass.Outbound.AsyncAdapter.Inline (test default)
      #
      # Resolved via Application.get_env(:mailglass, :async_adapter_impl).
      # NOTE: separate env key from :async_adapter (which selects between
      # :task_supervisor and :oban for the prod-side dispatch strategy).

      @callback dispatch(fun :: (-> any()), opts :: keyword()) :: {:ok, pid()} | :ok

      @spec dispatch((-> any()), keyword()) :: {:ok, pid()} | :ok
      def dispatch(fun, opts \\\\ []) when is_function(fun, 0) do
        impl().dispatch(fun, opts)
      end

      defp impl do
        case Application.get_env(:mailglass, :async_adapter_impl) do
          nil -> Mailglass.Outbound.AsyncAdapter.TaskSupervisor
          mod when is_atom(mod) -> mod
        end
      end
    end
    ```

    Part B — Create `lib/mailglass/outbound/async_adapter/task_supervisor.ex`:

    ```
    defmodule Mailglass.Outbound.AsyncAdapter.TaskSupervisor do
      @moduledoc false
      @behaviour Mailglass.Outbound.AsyncAdapter

      @impl true
      def dispatch(fun, _opts) when is_function(fun, 0) do
        Task.Supervisor.start_child(Mailglass.TaskSupervisor, fun)
      end
    end
    ```

    Part C — Create `lib/mailglass/outbound/async_adapter/inline.ex`:

    ```
    defmodule Mailglass.Outbound.AsyncAdapter.Inline do
      @moduledoc false
      @behaviour Mailglass.Outbound.AsyncAdapter

      @impl true
      def dispatch(fun, _opts) when is_function(fun, 0) do
        fun.()
        :ok
      end
    end
    ```

    Part D — Swap callsites in `lib/mailglass/outbound.ex`. Locate via:
    ```
    grep -nE "Task\\.Supervisor\\.start_child\\(Mailglass\\.TaskSupervisor" lib/mailglass/outbound.ex
    ```

    There are exactly 2 callsites (PATTERNS.md verified). For each, replace the `Task.Supervisor.start_child(Mailglass.TaskSupervisor, fn -> ... end)` with `Mailglass.Outbound.AsyncAdapter.dispatch(fn -> ... end, [])`. Keep the inner `Mailglass.Tenancy.with_tenant(tenant_id, fn -> ... end)` wrapping at the callsite — it works for both prod and Inline impls (D-08-15).

    Add a comment above each replaced site:
    `# AsyncAdapter dispatch (D-08-11). TaskSupervisor (prod) | Inline (test).`

    DO NOT touch the existing `:async_adapter` env-key reading code (the `:task_supervisor | :oban` selector at outbound.ex:362-364). That's a separate dispatch-strategy concern. The new behaviour resolution uses `:async_adapter_impl`.

    Part E — `lib/mailglass/application.ex` already supervises `{Task.Supervisor, name: Mailglass.TaskSupervisor}` per PATTERNS.md §task_supervisor.ex line 130. NO supervision-tree change needed.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -q "defmodule Mailglass.Outbound.AsyncAdapter" lib/mailglass/outbound/async_adapter.ex && grep -q "defmodule Mailglass.Outbound.AsyncAdapter.TaskSupervisor" lib/mailglass/outbound/async_adapter/task_supervisor.ex && grep -q "defmodule Mailglass.Outbound.AsyncAdapter.Inline" lib/mailglass/outbound/async_adapter/inline.ex && [ "$(grep -cE 'Task\.Supervisor\.start_child\(Mailglass\.TaskSupervisor' lib/mailglass/outbound.ex)" = "0" ] && [ "$(grep -cE 'Mailglass\.Outbound\.AsyncAdapter\.dispatch' lib/mailglass/outbound.ex)" -ge "2" ] && grep -q ":async_adapter_impl" lib/mailglass/outbound/async_adapter.ex && [ "$(grep -c ':async_adapter_impl' lib/mailglass/outbound.ex)" = "0" ]
  </verify>
  <acceptance_criteria>
    - 3 new files exist with the correct module names + `@behaviour` declarations
    - `mix compile --warnings-as-errors` exits 0
    - `grep -cE 'Task\.Supervisor\.start_child\(Mailglass\.TaskSupervisor' lib/mailglass/outbound.ex` == 0 (both callsites swapped)
    - `grep -cE 'Mailglass\.Outbound\.AsyncAdapter\.dispatch' lib/mailglass/outbound.ex` >= 2 (both replacements made)
    - `:async_adapter_impl` appears ONLY in `async_adapter.ex` (env-key isolation; no leak into `outbound.ex`)
    - `:async_adapter` (the existing key) is unchanged in `outbound.ex` — collision avoided
    - `mix test --warnings-as-errors --seed 0` exits 0 (compile-clean baseline; full random-seed sweep happens in Task 4)
  </acceptance_criteria>
  <done>
    AsyncAdapter behaviour + 2 impls compile cleanly; both callsites in outbound.ex swapped; env-key disambiguation verified; existing `:async_adapter` selector untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract CitextProbe + wire into CaseTemplates + test_helper.exs (PR-A part 2)</name>
  <files>test/support/citext_probe.ex, test/support/data_case.ex, test/support/mailer_case.ex, test/support/webhook_case.ex, test/test_helper.exs, test/mailglass/persistence_integration_test.exs</files>
  <read_first>
    - test/support/data_case.ex (entire file — find the probe loop at ~lines 62-69)
    - test/support/mailer_case.ex (entire file — find the probe loop at ~lines 94-101 + the snapshot/restore pattern at ~lines 122-125, 179-191)
    - test/support/webhook_case.ex (entire file — confirm probe is missing per PATTERNS.md)
    - test/support/admin_case.ex (entire file — confirm `use Mailglass.MailerCase` inheritance per PATTERNS.md)
    - test/mailglass/persistence_integration_test.exs (entire file — find `probe_until_clean/1` recursive helper at ~lines 56, 60-73)
    - test/test_helper.exs (entire file — find the cold-start point after migrations)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-10, D-08-12
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §test/support/citext_probe.ex (lines 158-261) + §test/support/{mailer,data,webhook,admin}_case.ex (lines 547-583)
  </read_first>
  <action>
    Part A — Create `test/support/citext_probe.ex` using the verbatim skeleton from PATTERNS.md §test/support/citext_probe.ex (lines 209-253). The module is `Mailglass.TestSupport.CitextProbe` with `run/1` taking `[repo: Ecto.Repo.t(), max_attempts: pos_integer()]` and a 5-iteration default loop.

    Part B — Replace probe loops in 3 files:

    1. `test/support/data_case.ex:62-69` (find via `grep -n "SELECT 'probe'::citext" test/support/data_case.ex`):
       ```
       # BEFORE:
       for _ <- 1..5 do
         try do
           Mailglass.TestRepo.query!("SELECT 'probe'::citext")
         rescue
           Postgrex.Error -> :ok
         end
       end
       # AFTER:
       Mailglass.TestSupport.CitextProbe.run([])
       ```
       Keep the surrounding moduledoc/comment (~lines 38-61) — it documents *why* the probe exists.

    2. `test/support/mailer_case.ex:94-101` (find via grep). Same replacement.

    3. `test/mailglass/persistence_integration_test.exs:60-73` (the recursive `probe_until_clean/1` form). Replace the call site with `Mailglass.TestSupport.CitextProbe.run(repo: Mailglass.TestRepo)` and DELETE the local `defp probe_until_clean/1` pair (no longer needed).

    Part C — Add CitextProbe to `webhook_case.ex` setup (per D-08-10, currently missing). Place the call immediately after `Ecto.Adapters.SQL.Sandbox.start_owner!`:
    ```
    Mailglass.TestSupport.CitextProbe.run([])
    ```

    Part D — `admin_case.ex` inherits via `use Mailglass.MailerCase`. **NO direct change.** Verify by reading `admin_case.ex` and confirming the `using opts do ... use Mailglass.MailerCase, unquote(opts) ... end` macro expansion includes the probe.

    Part E — Add cold-start probe to `test/test_helper.exs`. Place after `Ecto.Migrator.up_to/3` or equivalent migration call:
    ```
    Mailglass.TestSupport.CitextProbe.run([])
    ```

    Part F — Update MailerCase setup to:

    1. Default `:async_adapter_impl` to `Mailglass.Outbound.AsyncAdapter.Inline` for the test process.
    2. The `set_mailglass_global` opt-in path flips to `Mailglass.Outbound.AsyncAdapter.TaskSupervisor` + `Sandbox.mode(repo, {:shared, self()})` + forces `async: false`.
    3. Apply HI-01 snapshot/restore pattern (PATTERNS.md §test/support/{mailer,data,webhook,admin}_case.ex lines 558-573):

    ```
    prior_async_adapter_impl = Application.get_env(:mailglass, :async_adapter_impl)

    Application.put_env(:mailglass, :async_adapter_impl, Mailglass.Outbound.AsyncAdapter.Inline)

    on_exit(fn ->
      if prior_async_adapter_impl != nil do
        Application.put_env(:mailglass, :async_adapter_impl, prior_async_adapter_impl)
      else
        Application.delete_env(:mailglass, :async_adapter_impl)
      end
    end)
    ```

    The same snapshot/restore pattern MUST apply when `set_mailglass_global` flips the impl back to TaskSupervisor.

    Part G — Update MailerCase moduledoc (~lines 30-39) to replace the "## Async tests and deliver_later/2" section with the new AsyncAdapter rule per D-08-12:

    ```
    ## Async tests and deliver_later/2

    By default, MailerCase configures :async_adapter_impl ->
    Mailglass.Outbound.AsyncAdapter.Inline so deliver_later/2 runs the
    dispatch synchronously under the calling test's connection.

    Tests that exercise real async dispatch (Task.Supervisor in the prod
    supervision tree) MUST set @tag :set_mailglass_global which flips the
    impl back to TaskSupervisor, switches Sandbox.mode to :shared, and
    forces async: false.
    ```
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -q "defmodule Mailglass.TestSupport.CitextProbe" test/support/citext_probe.ex && grep -q "Mailglass.TestSupport.CitextProbe.run" test/support/data_case.ex && grep -q "Mailglass.TestSupport.CitextProbe.run" test/support/mailer_case.ex && grep -q "Mailglass.TestSupport.CitextProbe.run" test/support/webhook_case.ex && grep -q "Mailglass.TestSupport.CitextProbe.run" test/test_helper.exs && [ "$(grep -cE \"SELECT 'probe'::citext\" test/support/)" -le 1 ] && ! grep -q "defp probe_until_clean" test/mailglass/persistence_integration_test.exs && grep -q ":async_adapter_impl" test/support/mailer_case.ex && grep -q "AsyncAdapter.Inline" test/support/mailer_case.ex && for i in 1 2 3 4 5; do mix test --warnings-as-errors --seed $RANDOM || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/citext_probe.ex` exists with `Mailglass.TestSupport.CitextProbe.run/1`
    - The probe loop is removed from data_case.ex (replaced with CitextProbe.run/1 call)
    - The probe loop is removed from mailer_case.ex (replaced)
    - The recursive `defp probe_until_clean/1` is removed from persistence_integration_test.exs
    - webhook_case.ex setup calls CitextProbe.run/1
    - test_helper.exs calls CitextProbe.run/1 at cold start
    - MailerCase sets `:async_adapter_impl` to `Mailglass.Outbound.AsyncAdapter.Inline` by default with HI-01 snapshot/restore
    - 5 consecutive `mix test --warnings-as-errors --seed <random>` runs PASS (PR-A acceptance per D-08-13)
    - admin_case.ex is NOT directly modified (inherits via `use Mailglass.MailerCase`)
  </acceptance_criteria>
  <done>
    CitextProbe extraction complete; all 3 duplicated probes collapsed; CaseTemplates use CitextProbe + AsyncAdapter Inline default with snapshot/restore; 5 random-seed runs pass cleanly. PR-A is ready to merge with the existing Tests lane still at `continue-on-error: true`.
  </done>
</task>

<task type="auto">
  <name>Task 3: PR-B — add advisory tests-strict CI lane (NOT required, ~1 week soak)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (entire file — find the existing Tests job at ~lines 116-168)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-13 (PR-B step), D-08-14 (random seed retained)
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §.github/workflows/ci.yml (lines 731-754)
  </read_first>
  <action>
    Add a NEW CI job named `tests_strict` next to the existing Tests job. It runs `mix test --warnings-as-errors` (no `--seed 0` per D-08-14 — random seed retained). It is NOT marked required; it runs in parallel with the existing Tests lane (which still has `continue-on-error: true`) for ~1 week of advisory soak.

    YAML shape (mirror the existing Tests job's services block, env, and setup steps; only the `name`, the run step, and the absence of `continue-on-error: true` differ):

    ```
    tests_strict:
      name: Tests Strict (advisory, ~1 week soak)
      runs-on: ubuntu-latest
      services:
        postgres:
          # ... mirror existing Tests job services block exactly ...
      env:
        # ... mirror existing Tests job env block exactly ...
      steps:
        - uses: actions/checkout@<sha>  # use the same SHA as the existing Tests job
        - uses: erlef/setup-beam@<sha>  # same
          with:
            otp-version: ${{ matrix.otp }}
            elixir-version: ${{ matrix.elixir }}
        # ... mirror cache + deps.get + compile setup ...
        - name: Wait for postgres + create test DB
          env:
            PGPASSWORD: postgres
          run: |
            until pg_isready -h localhost -U postgres; do sleep 1; done
            mix ecto.create -r Mailglass.TestRepo --quiet
        - name: Run tests strict (advisory)
          # PR-B: NOT marked required. Soaks in parallel with the existing
          # Tests lane (>=5 random-seed runs) before PR-C flips the existing
          # lane to halt-on-failure.
          run: mix test --warnings-as-errors
    ```

    Reuse the same matrix (Elixir/OTP versions) as the existing Tests job. Reuse the same SHA pins from Task 4 of plan 08-04 (already refreshed for 2026-Q2).

    DO NOT add `continue-on-error: true` here — the strict lane should fail loudly. But also DO NOT mark it required in branch protection yet — that's PR-C.

    DO NOT modify the existing Tests job. PR-B is additive.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && grep -q "tests_strict:" .github/workflows/ci.yml && grep -q "Tests Strict (advisory" .github/workflows/ci.yml && [ "$(grep -cE 'continue-on-error: true' .github/workflows/ci.yml)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/ci.yml` exits 0
    - `tests_strict:` job exists in ci.yml
    - The job has NO `continue-on-error: true`
    - The existing Tests job (with `continue-on-error: true`) is UNCHANGED
    - The strict job uses the same matrix + services + SHA pins as the existing Tests job
    - The strict job does NOT have a `--seed 0` pin (random seed retained per D-08-14)
    - In a follow-up branch, triggering the workflow and running it >=5 times produces all-green strict results before PR-C
  </acceptance_criteria>
  <done>
    PR-B is ready to merge. tests_strict job is wired in as advisory; existing Tests lane unchanged; >=5 random-seed advisory soak runs validate stability before PR-C.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: PR-C — flip existing Tests lane to halt-on-failure + delete advisory lane + branch protection update (szTheory-only)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the Tests job and the tests_strict job from Task 3)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-13 (PR-C step)
  </read_first>
  <action>
    PRECONDITION (user-confirmed before invoking this task): the advisory `tests_strict` lane added in Task 3 has soaked in CI for ~1 week with >=5 green random-seed runs. NO flakes.

    Claude pre-flight (automated):

    1. Open a new branch off main: `git checkout -b chore/08-05-pr-c-tests-strict-flip`
    2. Edit `.github/workflows/ci.yml`:
       - Find the existing Tests job and CHANGE `continue-on-error: true` to `continue-on-error: false` (or delete the line entirely — `false` is the default).
       - DELETE the entire `tests_strict:` job block (added in Task 3).
    3. Run `actionlint .github/workflows/ci.yml` — must exit 0.
    4. Push the branch + open a PR titled `chore(08): PR-C — flip Tests gate to halt-on-failure (REL-10 D-08-13)`.
    5. Wait for CI: `gh pr checks <num> --watch`. All checks must be green.

    User action required (szTheory-only — branch protection):

    1. Merge the PR via `gh pr merge <num> --squash --delete-branch`.
    2. Update branch protection on `main` (admin-only):
       - Navigate: Repo Settings -> Branches -> main -> Edit
       - Under "Require status checks to pass before merging", add the `Tests` job (the now-strict lane) as a required check.
       - Save changes.
    3. Verify via API: `gh api repos/szTheory/mailglass/branches/main/protection/required_status_checks` — the Tests job must appear in the `contexts` array.
    4. Synthetic-failure regression check: open a draft PR with `test "synthetic failure", do: assert false`; confirm the PR is BLOCKED by the strict Tests gate. CLOSE the draft PR (do NOT merge) once the block is verified.

    Brand voice: any failure summary follows "Delivery blocked: <specific reason>" pattern.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && ! grep -q "tests_strict:" .github/workflows/ci.yml && ! grep -E "continue-on-error:\s*true" .github/workflows/ci.yml | grep -i test && gh api repos/szTheory/mailglass/branches/main/protection/required_status_checks --jq '.contexts | map(select(. | test("Tests"))) | length >= 1'</automated>
  </verify>
  <how-to-verify>
    Verification checklist for the user:

    1. `actionlint .github/workflows/ci.yml` exits 0
    2. The Tests job no longer has `continue-on-error: true`
    3. The `tests_strict:` job block is deleted from ci.yml
    4. `gh api repos/szTheory/mailglass/branches/main/protection/required_status_checks` lists the Tests job in the `contexts` array
    5. The synthetic-failure draft PR is BLOCKED by the strict gate (verified, then closed without merge)
  </how-to-verify>
  <resume-signal>Type "branch protection updated" once the Tests job is in the required-checks list; or "blocked: <reason>" if the soak surfaced unexpected flakes.</resume-signal>
  <done>
    PR-C is merged; tests_strict advisory lane is deleted; existing Tests lane is `continue-on-error: false`; branch protection on main lists the Tests job as required; synthetic-failure draft PR was blocked (verified by user). Phase 8 Tests gate is officially halt-on-failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> Sandbox connection | Sandbox owner / shared mode is the trust mechanism for test isolation; AsyncAdapter Inline runs in the caller process so the connection is naturally owned. |
| AsyncAdapter behaviour resolution -> :async_adapter_impl env key | Env-key disambiguation is critical: `:async_adapter` selects prod-side strategy (`:task_supervisor | :oban`); `:async_adapter_impl` selects the behaviour module. Collision would silently break prod. |
| ci.yml Tests job -> branch protection required-check list | Required-check list is admin-only; szTheory updates it after the soak completes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | Env-key collision (`:async_adapter` repurposed for behaviour module) | mitigate | Use NEW key `:async_adapter_impl`; existing `:async_adapter` reads `:task_supervisor | :oban` atoms unchanged. PATTERNS.md callsite map verifies. |
| T-08-05-02 | Information disclosure | Inline impl runs closure synchronously - tenancy could leak across tests | mitigate | Caller-side `Mailglass.Tenancy.with_tenant/2` wrap stamps and restores tenant pid-dict on entry/exit (D-08-15). Snapshot/restore (HI-01) on `:async_adapter_impl` env key prevents inter-test leakage. |
| T-08-05-03 | Denial of service | CitextProbe infinite loop on persistent error | mitigate | Hard cap of 5 attempts per `@default_max_attempts`; returns `:ok` after exhaustion (does not raise). |
| T-08-05-04 | Repudiation | Branch-protection bypass on PR-C merge | accept | szTheory has admin rights; required-check update is intentional. |
| T-08-05-05 | Elevation of privilege | tests_strict lane masking real failures by being non-required during soak | accept | Soak is time-boxed; PR-C is the explicit flip after >=5 green random-seed runs. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after Task 1.
- 5 consecutive `mix test --warnings-as-errors --seed <random>` runs pass after Task 2 (PR-A acceptance).
- `actionlint .github/workflows/ci.yml` exits 0 after Tasks 3 and 4.
- After PR-C: a synthetic `assert false` test on a draft PR is BLOCKED by the strict Tests gate.
- After PR-C: `gh api repos/.../branches/main/protection/required_status_checks` lists the Tests job in `contexts`.
- Env-key isolation verified: `grep -n ":async_adapter\b" lib/mailglass/outbound.ex` and `grep -n ":async_adapter_impl" lib/mailglass/outbound.ex` show the two keys never appear on the same line.
</verification>

<success_criteria>
- `Mailglass.Outbound.AsyncAdapter` ships as the 5th first-class behaviour with `dispatch/2` callback + 2 impls (TaskSupervisor prod, Inline test).
- Both `Task.Supervisor.start_child(Mailglass.TaskSupervisor, ...)` callsites in outbound.ex are swapped to `AsyncAdapter.dispatch/2`.
- `Mailglass.TestSupport.CitextProbe` extracts the duplicated probe into a shared module; data_case, mailer_case, webhook_case, test_helper.exs all use it; persistence_integration_test.exs's local `probe_until_clean/1` is deleted.
- `:async_adapter_impl` env key is disambiguated from `:async_adapter` (no collision).
- HI-01 snapshot/restore pattern applies to `:async_adapter_impl` in MailerCase setup.
- 3-PR rollout: PR-A (foundations) -> PR-B (advisory tests_strict lane) -> PR-C (existing lane flipped + advisory deleted + branch protection updated by szTheory).
- Random seed retained in CI (no `--seed 0` pin).
- Post-PR-C: a deliberate test failure blocks merge.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-engineering-hardening/08-05-SUMMARY.md`. Include:
- Exact line numbers of the 2 swapped callsites in outbound.ex (file may have drifted from PATTERNS.md's stated 437/607)
- The `:async_adapter` vs `:async_adapter_impl` separation, with grep evidence that the keys never collide
- The 5 random-seed test runs (seeds + timestamps) that validated PR-A
- The advisory soak run history for tests_strict (>=5 green random-seed runs in CI before PR-C)
- The branch-protection diff (before -> after) in the required-check list
- Confirmation that the synthetic-failure draft PR was blocked post-PR-C
</output>
