---
phase: 08-release-engineering-hardening
plan: 06
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - .credo.exs
  - mix.exs
  - mailglass_admin/mix.exs
  - .dialyzer_ignore.exs
  - mailglass_admin/.dialyzer_ignore.exs
  - scripts/check_dialyzer_ignore.sh
  - scripts/check_credo_suppressions.sh
  - .github/workflows/ci.yml
  - lib/mailglass/error.ex
  - lib/mailglass/installer/apply.ex
  - lib/mailglass/webhook/providers/postmark.ex
  - lib/mailglass/repo.ex
  - test/support/fake_fixtures/tracking_mailer.ex
  - test/support/fake_fixtures/test_mailer.ex
autonomous: false
requirements: [REL-11, REL-12]
must_haves:
  truths:
    - "mix credo --strict passes with zero warnings (D-08-16)"
    - ":disabled_checks lists the 5 baseline disables from D-08-18 verbatim, each preceded by # Reason: AND # Tracking: comments"
    - "scripts/check_credo_suppressions.sh exits 0 today and exits non-zero if a future PR adds a Credo disable without Reason+Tracking comments (D-08-19)"
    - "mix dialyzer halts on warnings (default behavior — verified Dialyxir 1.4.7); --ignore-exit-status REMOVED from ci.yml; --halt-exit-status NEVER added (does not exist as a Dialyxir flag — STATE.md correction propagated, D-08-09)"
    - ":dialyzer config block in BOTH root mix.exs and mailglass_admin/mix.exs declares flags: [:error_handling, :missing_return, :no_opaque, :no_match, :underspecs] (D-08-01) + ignore_file: '.dialyzer_ignore.exs' + list_unused_filters: true (D-08-04)"
    - "Per-package ignore files: .dialyzer_ignore.exs at root + mailglass_admin/.dialyzer_ignore.exs (D-08-02); ignore_file_strict format pinning to {file, short_description} tuples (D-08-03)"
    - "scripts/check_dialyzer_ignore.sh exits 0 today and exits non-zero if a future PR adds an ignore entry without # Reason: comment (D-08-05)"
    - "PLT cache key in ci.yml includes ${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }} (D-08-06)"
    - ".dialyzer_ignore.exs has <=15 entries (D-08-07 1-day triage tripwire — STOP and open v0.3 deep-clean phase if over)"
    - "@dialyzer {:nowarn_function, ...} source-level pragmas BANNED — all suppressions live in the centralized ignore file (D-08-08)"
    - "Phase-9-firewall respected: D-08-29 — Mailable, Message, Outbound mailer DSL, Swoosh.Email injection site at mailable.ex:129 NOT touched here; Refactor fixes target installer/apply.ex + webhook/providers/postmark.ex which are Phase-9-stable"
    - "Mailglass.Error.is_error?/1 NOT renamed — inline credo:disable-for-next-line with Tracking: Phase 9 rename to error?/1 (D-08-20)"
    - "@moduledoc false added to test/support/fake_fixtures/{tracking_mailer,test_mailer}.ex (do NOT disable Readability.ModuleDoc; it's high-signal in lib/) (D-08-17)"
    - ":included config stays ['lib/', 'test/']; do NOT add credo_checks/ — Credo would lint its own checks (D-08-21)"
    - "Two CI shell-gate scripts share identical shape (paired siblings per D-08-27); one runs in dialyzer job, one in credo_strict job"
  artifacts:
    - path: ".credo.exs"
      provides: "strict: true + 5 baseline disables with Reason+Tracking comments"
      contains: "strict: true"
    - path: "mix.exs"
      provides: ":dialyzer config block with flags + ignore_file + list_unused_filters"
      contains: "list_unused_filters: true"
    - path: "mailglass_admin/mix.exs"
      provides: "Same :dialyzer config in admin package"
      contains: "list_unused_filters: true"
    - path: ".dialyzer_ignore.exs"
      provides: "Per-package ignore-file (root) — <=15 documented entries"
      contains: "Reason:"
    - path: "mailglass_admin/.dialyzer_ignore.exs"
      provides: "Per-package ignore-file (admin) — <=15 documented entries"
      contains: "Reason:"
    - path: "scripts/check_dialyzer_ignore.sh"
      provides: "Validates every ignore entry has # Reason: comment"
      contains: "# Reason:"
    - path: "scripts/check_credo_suppressions.sh"
      provides: "Validates every Credo disable has # Reason: AND # Tracking:"
      contains: "# Tracking:"
    - path: ".github/workflows/ci.yml"
      provides: "Credo step: bash check + mix credo --strict; Dialyzer step: bash check + mix dialyzer (no --ignore-exit-status)"
      contains: "mix credo --strict"
  key_links:
    - from: ".github/workflows/ci.yml credo_strict job"
      to: "scripts/check_credo_suppressions.sh + mix credo --strict"
      via: "shell run steps in sequence"
      pattern: "check_credo_suppressions.sh"
    - from: ".github/workflows/ci.yml dialyzer job"
      to: "scripts/check_dialyzer_ignore.sh + mix dialyzer"
      via: "shell run steps in sequence"
      pattern: "check_dialyzer_ignore.sh"
    - from: "mix.exs :dialyzer block"
      to: "Dialyxir 1.4.7 runtime"
      via: "ignore_file_strict + flags + list_unused_filters: true"
      pattern: "ignore_file: \".dialyzer_ignore.exs\""
---

<objective>
Final Phase 8 plan — flip Credo to `--strict` and Dialyzer to halt-on-warnings,
both gated by paired shell-script CI gates that validate the comment
convention (`# Reason:` for Dialyzer; `# Reason:` + `# Tracking:` for Credo)
on every suppression entry. This makes the project's Credo / Dialyzer posture
match Oban's archetype (D-08-28) — strict-with-curated-disables, every
disable a documented decision (D-08-27).

Two foundation moves:

1. **REL-11 (Credo --strict, D-08-16..D-08-21):** flip `.credo.exs` to
   `strict: true`, add 5 baseline disables (D-08-18 verbatim) with
   `# Reason:` AND `# Tracking:` comments on each, fix the 10 in-scope
   findings (5 Warnings + 4 Refactor + 1 spec; D-08-17), `@moduledoc false`
   on 2 test fixtures, suppress `is_error?/1` inline (D-08-20).
   `scripts/check_credo_suppressions.sh` paired with the Dialyzer twin.
2. **REL-12 (Dialyzer triage, D-08-01..D-08-09):** add `:dialyzer` config
   block to BOTH `mix.exs` files (per D-08-02) with `flags: [:error_handling, :missing_return, :no_opaque, :no_match, :underspecs]`,
   `ignore_file: ".dialyzer_ignore.exs"`, `list_unused_filters: true`.
   Triage 230 -> <=15 documented entries with `# Reason:` comments.
   REMOVE `--ignore-exit-status` from ci.yml; do NOT add `--halt-exit-status`
   (does not exist). PLT cache key fixed to include OTP+Elixir+lockfile.
   `scripts/check_dialyzer_ignore.sh` paired with Credo twin.

**1-day triage tripwire (D-08-07):** if the post-flag-tune findings exceed
15 entries, STOP this plan and recommend a v0.3 "Dialyzer-deep-clean"
research phase. The cap is the signal, not the goal.

**Phase-9-firewall (D-08-29):** Refactor fixes target `installer/apply.ex`
and `webhook/providers/postmark.ex` (Phase-9-stable). DO NOT touch
`mailable.ex`, `message.ex`, the Outbound mailer DSL, or the
`import Swoosh.Email` site at `mailable.ex:129` — those land in Phase 9.
The `is_error?/1` predicate is suppressed inline with
`Tracking: Phase 9 rename to error?/1` (D-08-20).

Engineering DNA: "Custom Credo checks at lint time" + "Documented decisions,
not silent suppressions" (D-08-27). Brand voice on script error messages.

Output: `.credo.exs` strict-mode + comments; both `mix.exs` `:dialyzer`
blocks; 2 ignore files; 2 shell scripts; ci.yml step transforms;
spec/refactor fixes in 4 source files; 2 fixture moduledoc adds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-engineering-hardening/08-CONTEXT.md
@.planning/phases/08-release-engineering-hardening/08-PATTERNS.md
@./CLAUDE.md
@.credo.exs
@mix.exs
@mailglass_admin/mix.exs
@.github/workflows/ci.yml
@lib/mailglass/error.ex

<interfaces>
:dialyzer block to add (PATTERNS.md §mix.exs lines 700-714):
```
defp dialyzer do
  [
    flags: [:error_handling, :missing_return, :no_opaque, :no_match, :underspecs],
    ignore_file: ".dialyzer_ignore.exs",
    list_unused_filters: true
  ]
end
```
And add `dialyzer: dialyzer()` to the project keyword list.

5 baseline Credo disables (D-08-18 verbatim) — every entry MUST have BOTH
`# Reason:` AND `# Tracking:` comments:

```
# Reason: stylistic; conflicts with deliberate apply/3 use in adapter dispatch.
# Tracking: permanent.
{Credo.Check.Refactor.Apply, false},

# Reason: macro-heavy library; quote do blocks in Mailable/MailglassAdmin.Router
# are intentionally long for use injection.
# Tracking: permanent.
{Credo.Check.Refactor.LongQuoteBlocks, false},

# Reason: low signal in a 33k-LOC codebase with mixed nesting depth.
# Tracking: permanent (Oban posture).
{Credo.Check.Readability.AliasOrder, false},

# Reason: 102 findings, 99% in test files where nested-module-aliases are
# deliberate scoping.
# Tracking: permanent.
{Credo.Check.Design.AliasUsage, false},

# Reason: explicit try/rescue in webhook/providers/sendgrid.ex and
# webhook/plug.ex documents the rescue-and-rewrap contract for
# Mailglass.SignatureError.
# Tracking: permanent (house style).
{Credo.Check.Readability.PreferImplicitTry, false}
```

is_error?/1 inline suppression (D-08-20):
```
# credo:disable-for-next-line Credo.Check.Readability.PredicateFunctionNames -- Tracking: Phase 9 rename to error?/1
def is_error?(...)
```

PLT cache key (D-08-06):
```
key: ${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}
```

Dialyzer step (D-08-09):
```
- name: Validate dialyzer ignore-file documented
  run: bash scripts/check_dialyzer_ignore.sh
- name: Run Dialyzer
  # Default mix dialyzer halts on warnings (Dialyxir 1.4.7 verified — D-08-09).
  # --halt-exit-status does NOT exist as a Dialyxir flag.
  # --ignore-exit-status was the advisory flag, REMOVED here.
  run: mix dialyzer
```

Credo step (D-08-16, D-08-19):
```
- name: Validate credo suppressions documented
  run: bash scripts/check_credo_suppressions.sh
- name: Run Credo (strict)
  run: mix credo --strict
```

Shell script skeletons in PATTERNS.md §scripts/check_dialyzer_ignore.sh (lines 360-396) and §scripts/check_credo_suppressions.sh (lines 408-449).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Credo to strict + add baseline disables with Reason+Tracking + create check_credo_suppressions.sh + fix in-scope findings (REL-11)</name>
  <files>.credo.exs, scripts/check_credo_suppressions.sh, lib/mailglass/error.ex, lib/mailglass/installer/apply.ex, lib/mailglass/webhook/providers/postmark.ex, lib/mailglass/repo.ex, test/support/fake_fixtures/tracking_mailer.ex, test/support/fake_fixtures/test_mailer.ex</files>
  <read_first>
    - .credo.exs (entire file — current `extra_checks` and the existing `strict: false` block at lines 60-83)
    - lib/mailglass/error.ex (find the `is_error?/1` function — must add inline suppression)
    - lib/mailglass/installer/apply.ex (find the Refactor.Nesting / CyclomaticComplexity hotspots flagged by Credo)
    - lib/mailglass/webhook/providers/postmark.ex (find the same kind of hotspots)
    - lib/mailglass/repo.ex (find the `%Postgrex.Result{}` `@spec` leak)
    - test/support/fake_fixtures/tracking_mailer.ex (add `@moduledoc false`)
    - test/support/fake_fixtures/test_mailer.ex (add `@moduledoc false`)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-16..D-08-21
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §.credo.exs (lines 587-665) + §scripts/check_credo_suppressions.sh (lines 403-451)
    - First run `mix credo --strict 2>&1 | tee /tmp/credo_strict.log` to see ALL findings; categorize them per D-08-17 (5 Warning + 4 Refactor + 1 spec) and identify the actual file:line for each
  </read_first>
  <action>
    Part A — Update `.credo.exs`. Change `strict: false` to `strict: true`. Keep `:included` as `["lib/", "test/"]` (D-08-21 — do NOT add `credo_checks/`; Credo would lint its own checks). Append the 5 baseline disables to the `checks:` list AFTER the existing `{Credo.Check.Consistency.ExceptionNames, false}` entry. Each disable MUST have BOTH `# Reason:` AND `# Tracking:` comments verbatim from CONTEXT.md D-08-18 (text shown in `<interfaces>` above).

    Add a comment in `.credo.exs` at the `:included` line:
    ```
    # D-08-21: included stays ["lib/", "test/"]; do NOT add credo_checks/
    # (Credo would lint its own checks, producing false positives).
    ```

    Part B — Create `scripts/check_credo_suppressions.sh` using the verbatim skeleton from PATTERNS.md §scripts/check_credo_suppressions.sh lines 408-449. The script asserts every `{Credo.Check..., false}` tuple is preceded by BOTH `# Reason:` AND `# Tracking:` comment lines. Set executable: `chmod +x scripts/check_credo_suppressions.sh`.

    Brand-voice errors: the FAIL message in the script reads `"FAIL: credo disabled-check entries missing # Reason: or # Tracking: comments"` — specific and composed.

    Part C — Fix the 10 in-scope findings (D-08-17). Run `mix credo --strict` first to enumerate them; the categories are:
    - 5 Warning findings (real bugs / `length/1` on large lists / `comparison will always return true`)
    - 4 `Refactor.Nesting` / `CyclomaticComplexity` hotspots in `installer/apply.ex` and `webhook/providers/postmark.ex`
    - 1 `%Postgrex.Result{}` `@spec` leak in `Mailglass.Repo`

    Apply minimal fixes — no scope creep. Refactor.Nesting fixes split a deeply-nested function into helpers; CyclomaticComplexity fixes extract cond/case branches into private functions. The `@spec` leak is replaced with the documented public type or `term()` if not exposed.

    Part D — `@moduledoc false` on 2 test fixtures (D-08-17):
    - `test/support/fake_fixtures/tracking_mailer.ex` — add `@moduledoc false` immediately after `defmodule`
    - `test/support/fake_fixtures/test_mailer.ex` — same

    Do NOT disable `Credo.Check.Readability.ModuleDoc` globally — it's high-signal in `lib/`. Only the 2 test fixtures get `@moduledoc false`.

    Part E — Inline suppress `Mailglass.Error.is_error?/1` (D-08-20). Find the function in `lib/mailglass/error.ex` and add the line above it:
    ```
    # credo:disable-for-next-line Credo.Check.Readability.PredicateFunctionNames -- Tracking: Phase 9 rename to error?/1
    def is_error?(...)
    ```

    DO NOT rename. DO NOT touch other `*?/1` predicates. The Phase-9-firewall (D-08-29) requires this function survives Phase 8 unchanged.

    Part F — DO NOT modify any code in:
    - `lib/mailglass/mailable.ex` (Phase 9)
    - `lib/mailglass/message.ex` (Phase 9)
    - The Outbound mailer DSL (Phase 9)
    - The `import Swoosh.Email` site at `mailable.ex:129` (Phase 9)

    Part G — Verify locally:
    ```
    mix compile --warnings-as-errors
    bash scripts/check_credo_suppressions.sh
    mix credo --strict
    ```
    All three MUST exit 0.

    Negative test: introduce a synthetic `{Credo.Check.Readability.Specs, false}` line in `.credo.exs` WITHOUT the Reason+Tracking comments; run `bash scripts/check_credo_suppressions.sh`; confirm exit non-zero. Revert before commit.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && bash scripts/check_credo_suppressions.sh && mix credo --strict && grep -q "strict: true" .credo.exs && grep -q "Tracking: permanent" .credo.exs && [ "$(grep -cE '^\s*\{Credo\.Check\.[^,]+,\s*false\}' .credo.exs)" -ge "6" ] && grep -q "Tracking: Phase 9 rename to error?/1" lib/mailglass/error.ex && grep -q "@moduledoc false" test/support/fake_fixtures/tracking_mailer.ex && grep -q "@moduledoc false" test/support/fake_fixtures/test_mailer.ex && [ -x scripts/check_credo_suppressions.sh ]</automated>
  </verify>
  <acceptance_criteria>
    - `.credo.exs` has `strict: true`
    - `:included` stays `["lib/", "test/"]` with the D-08-21 explanatory comment
    - 5 new `{Credo.Check.*, false}` entries (per D-08-18 verbatim) added; each preceded by `# Reason:` AND `# Tracking:` lines
    - `scripts/check_credo_suppressions.sh` exists, is executable, and exits 0 on the current `.credo.exs`
    - `mix credo --strict` exits 0 (10 in-scope findings fixed)
    - `lib/mailglass/error.ex` has the inline `Tracking: Phase 9 rename to error?/1` suppression above `is_error?/1`; the function is NOT renamed
    - 2 test fixtures have `@moduledoc false` (no global Readability.ModuleDoc disable)
    - `mix compile --warnings-as-errors` exits 0
    - Phase-9-firewall: `git diff lib/mailglass/mailable.ex lib/mailglass/message.ex` is empty (untouched)
    - Negative test: synthetic Credo disable without Reason+Tracking causes the script to exit non-zero. Revert before commit.
  </acceptance_criteria>
  <done>
    Credo strict mode passes; baseline disables are documented; in-scope findings fixed; `is_error?/1` suppressed inline pending Phase 9; shell-gate script in place. Phase-9-firewall respected — no Mailable/Message/DSL touches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add :dialyzer config blocks to mix.exs + create per-package ignore files + create check_dialyzer_ignore.sh + triage 230 -> <=15 documented entries (REL-12)</name>
  <files>mix.exs, mailglass_admin/mix.exs, .dialyzer_ignore.exs, mailglass_admin/.dialyzer_ignore.exs, scripts/check_dialyzer_ignore.sh</files>
  <read_first>
    - mix.exs (entire `project/0` keyword list — find where to insert `dialyzer: dialyzer()`; find the end of `defp` blocks to add `defp dialyzer do ... end`)
    - mailglass_admin/mix.exs (same surface)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-01..D-08-09 (every Dialyzer decision is in scope)
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §mix.exs (lines 689-715), §scripts/check_dialyzer_ignore.sh (lines 360-396)
    - https://hexdocs.pm/dialyxir/readme.html — confirm `--format ignore_file_strict` and `list_unused_filters: true` are 1.4.7 features
    - First run `mix dialyzer` (current state) and capture the 230 findings; then add the flag block; rerun; expect 230 -> 30-60 (D-08-01); fix the cheap ones; document the rest
  </read_first>
  <action>
    Part A — Add `:dialyzer` config to `mix.exs`. Insert in the project keyword list:
    ```
    dialyzer: dialyzer(),
    ```

    Add the helper as a `defp`:
    ```
    defp dialyzer do
      [
        # D-08-01: :no_opaque + :no_match kill the Elixir 1.18 opaque-type
        # cascade (elixir-lang/elixir#14837). :error_handling, :missing_return,
        # :underspecs improve coverage; revisit removing :underspecs after the
        # <=15-entry baseline is hit (D-08-07).
        flags: [:error_handling, :missing_return, :no_opaque, :no_match, :underspecs],
        # D-08-03: ignore_file_strict pins to {file, short_description} tuples
        # (stable across line-number drift). D-08-04: list_unused_filters fails
        # CI loudly when a future fix invalidates an existing ignore (prevents
        # silent drift).
        ignore_file: ".dialyzer_ignore.exs",
        list_unused_filters: true
      ]
    end
    ```

    Apply the IDENTICAL block to `mailglass_admin/mix.exs` (D-08-02 — separate package, separate PLT, separate ignore file).

    Part B — Create the per-package ignore files (initially empty stubs):

    `.dialyzer_ignore.exs` (root):
    ```
    # See `defp dialyzer/0` in mix.exs for flag and format rationale.
    # Format: ignore_file_strict tuples — {file, short_description}.
    # Each entry MUST be preceded by a `# Reason: <one-line>` comment
    # validated by `scripts/check_dialyzer_ignore.sh`.
    # Hard cap: 15 entries (D-08-07). Over-budget triggers a v0.3 deep-clean phase.

    [
      # entries added during triage in Part D
    ]
    ```

    `mailglass_admin/.dialyzer_ignore.exs`: same shape.

    Part C — Create `scripts/check_dialyzer_ignore.sh` using the verbatim skeleton from PATTERNS.md §scripts/check_dialyzer_ignore.sh lines 360-396. The script asserts every `{...}` tuple line is preceded by a `# Reason:` comment line. Cover BOTH ignore files (`FILES=(".dialyzer_ignore.exs" "mailglass_admin/.dialyzer_ignore.exs")`). Set executable: `chmod +x scripts/check_dialyzer_ignore.sh`.

    Brand-voice errors: `"FAIL: dialyzer ignore entries missing # Reason: comments"`.

    Part D — Triage. Run the new flag block and capture findings:
    ```
    mix dialyzer 2>&1 | tee /tmp/dialyzer_root.log
    cd mailglass_admin && mix dialyzer 2>&1 | tee /tmp/dialyzer_admin.log && cd ..
    ```

    Expected: 230 -> 30-60 (the `:no_opaque` + `:no_match` flags kill the Elixir 1.18 cascade, D-08-01).

    For each remaining finding:
    1. If it's a one-line `@spec` correction or genuine bug -> FIX it.
    2. If it's structural / unfixable -> add to the appropriate `.dialyzer_ignore.exs` with `# Reason: <one-line>` comment immediately above the tuple.

    Use the `--format ignore_file_strict` flag once during triage to generate the initial tuple shape:
    ```
    mix dialyzer --format ignore_file_strict
    ```
    Copy each tuple into the ignore file and add the `# Reason:` line.

    DO NOT use regex entries (D-08-03). DO NOT add `@dialyzer {:nowarn_function, ...}` source-level pragmas (D-08-08 — banned). DO NOT touch `mailable.ex` / `message.ex` / Outbound DSL (Phase-9-firewall).

    **HARD STOP — D-08-07 tripwire:** if `.dialyzer_ignore.exs` has > 15 entries after one focused triage day, STOP. Do NOT pad the file. Open a v0.3 "Dialyzer-deep-clean" research phase by:
    1. Logging a blocker in `.planning/STATE.md` blockers/concerns
    2. Adding a v0.3 backlog entry
    3. Returning the SUMMARY with `## DIALYZER OVERFLOW — V0.3 PHASE NEEDED` so the orchestrator surfaces it.

    Part E — Verify:
    ```
    mix compile --warnings-as-errors
    bash scripts/check_dialyzer_ignore.sh
    mix dialyzer
    cd mailglass_admin && mix dialyzer && cd ..
    wc -l < .dialyzer_ignore.exs   # rough sanity check; expect <=15 tuples
    ```

    All four MUST exit 0. The last (line count) is just informational; the real cap is # of tuple entries (D-08-07).

    Negative test: add a tuple to `.dialyzer_ignore.exs` WITHOUT the `# Reason:` comment; run `bash scripts/check_dialyzer_ignore.sh`; confirm exit non-zero. Revert before commit.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && bash scripts/check_dialyzer_ignore.sh && mix dialyzer && (cd mailglass_admin && mix dialyzer) && grep -q "list_unused_filters: true" mix.exs && grep -q "list_unused_filters: true" mailglass_admin/mix.exs && grep -q ":no_opaque" mix.exs && grep -q ":no_match" mix.exs && grep -q ":no_opaque" mailglass_admin/mix.exs && [ -x scripts/check_dialyzer_ignore.sh ] && [ "$(grep -cE '^\s*\{' .dialyzer_ignore.exs)" -le "15" ] && [ "$(grep -cE '^\s*\{' mailglass_admin/.dialyzer_ignore.exs)" -le "15" ] && ! grep -rE "@dialyzer\s+\{:nowarn" lib/</automated>
  </verify>
  <acceptance_criteria>
    - `defp dialyzer/0` exists in BOTH `mix.exs` files with `flags: [:error_handling, :missing_return, :no_opaque, :no_match, :underspecs]`, `ignore_file: ".dialyzer_ignore.exs"`, `list_unused_filters: true`
    - Both `mix.exs` `project/0` keyword lists contain `dialyzer: dialyzer()`
    - Both `.dialyzer_ignore.exs` files exist with the documented header comment
    - `scripts/check_dialyzer_ignore.sh` exists, is executable, and covers BOTH ignore-file paths
    - `bash scripts/check_dialyzer_ignore.sh` exits 0
    - `mix dialyzer` exits 0 in BOTH packages (no warnings — default halts; --halt-exit-status NOT used; --ignore-exit-status NOT used)
    - `grep -cE '^\s*\{' .dialyzer_ignore.exs` <= 15 (D-08-07 cap)
    - `grep -cE '^\s*\{' mailglass_admin/.dialyzer_ignore.exs` <= 15
    - Each tuple in the ignore files is preceded by a `# Reason:` line on the immediately-prior non-blank line
    - `grep -rE "@dialyzer\s+\{:nowarn" lib/` returns 0 (D-08-08 ban verified)
    - `git diff lib/mailglass/mailable.ex lib/mailglass/message.ex` empty (Phase-9-firewall)
    - Negative test: tuple without `# Reason:` causes the script to exit non-zero. Revert before commit.
  </acceptance_criteria>
  <done>
    `:dialyzer` config in both packages; flag tune kills the Elixir 1.18 opaque cascade; per-package ignore files <=15 entries each, all documented; shell-gate script paired with the Credo twin. If the 15-cap is breached, the SUMMARY surfaces the v0.3 deep-clean signal instead of padding.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ci.yml — Credo + Dialyzer step transforms + PLT cache key fix + paired shell-gate steps</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (entire file — focus on existing Credo step ~lines 195-204 and Dialyzer step ~lines 251-260, and the PLT cache step earlier in the dialyzer job)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-06, D-08-09, D-08-16, D-08-19
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §.github/workflows/ci.yml (lines 731-794)
  </read_first>
  <action>
    Part A — Credo step transform (D-08-16, D-08-19). Replace:
    ```
    - name: Run Credo (advisory until post-publish cleanup)
      run: mix credo --mute-exit-status
    ```
    with:
    ```
    - name: Validate credo suppressions documented
      run: bash scripts/check_credo_suppressions.sh
    - name: Run Credo (strict)
      run: mix credo --strict
    ```
    Remove any `continue-on-error: true` on the Credo job. Default is false.

    Part B — Dialyzer step transform (D-08-09). Replace:
    ```
    - name: Run Dialyzer (advisory until post-publish cleanup)
      continue-on-error: true
      run: mix dialyzer --halt-exit-status
    ```
    with:
    ```
    - name: Validate dialyzer ignore-file documented
      run: bash scripts/check_dialyzer_ignore.sh
    - name: Run Dialyzer
      # Default mix dialyzer halts on warnings (Dialyxir 1.4.7 verified — D-08-09).
      # --halt-exit-status does NOT exist as a Dialyxir flag.
      # --ignore-exit-status was the advisory flag, REMOVED here.
      run: mix dialyzer
    ```

    Part C — PLT cache key fix (D-08-06). Locate the existing PLT cache step in the dialyzer job. Replace:
    ```
    key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
    ```
    (or whatever variant is currently there) with:
    ```
    key: ${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}
    ```

    OTP minor-version bumps invalidate the PLT silently otherwise (CONTEXT.md D-08-06).

    Part D — DO NOT touch:
    - The Tests job (08-05's territory; PR-A/B/C is the rollout)
    - The new docs.check step from 08-02
    - The new advisory_matrix Postgres setup from 08-04
    - SHA pins (08-04 already refreshed them for 2026-Q2)

    Part E — Verify:
    ```
    actionlint .github/workflows/ci.yml
    ```
    MUST exit 0.

    Trigger ci.yml manually after merge: `gh workflow run ci.yml`. Confirm:
    - `Validate credo suppressions documented` step runs and passes
    - `Run Credo (strict)` step runs and passes
    - `Validate dialyzer ignore-file documented` step runs and passes
    - `Run Dialyzer` step runs and passes
    - PLT cache hit/miss varies correctly with OTP/Elixir matrix entries (no silent OTP-bump invalidation surprises)
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && grep -q "bash scripts/check_credo_suppressions.sh" .github/workflows/ci.yml && grep -q "bash scripts/check_dialyzer_ignore.sh" .github/workflows/ci.yml && grep -q "mix credo --strict" .github/workflows/ci.yml && grep -E "^\s*run:\s*mix dialyzer\s*$" .github/workflows/ci.yml && ! grep -q "mix credo --mute-exit-status" .github/workflows/ci.yml && ! grep -q "mix dialyzer --halt-exit-status" .github/workflows/ci.yml && ! grep -q "mix dialyzer --ignore-exit-status" .github/workflows/ci.yml && grep -q "matrix.otp" .github/workflows/ci.yml | head -1 && grep -E "key:.*matrix\.otp.*matrix\.elixir.*hashFiles" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/ci.yml` exits 0
    - Credo step runs `bash scripts/check_credo_suppressions.sh` BEFORE `mix credo --strict`
    - Dialyzer step runs `bash scripts/check_dialyzer_ignore.sh` BEFORE `mix dialyzer`
    - `--mute-exit-status` removed from the Credo step
    - `--ignore-exit-status` removed from the Dialyzer step
    - `--halt-exit-status` does NOT appear anywhere in ci.yml (D-08-09 — flag does not exist)
    - PLT cache key includes `${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}`
    - Manually triggered ci.yml run produces all-green Credo + Dialyzer steps with the new gates
    - The new shell-gate steps run BEFORE their main mix tasks (fail-fast on missing comments)
  </acceptance_criteria>
  <done>
    ci.yml Credo step is strict + paired with the suppressions-doc gate; Dialyzer step is halt-on-warnings + paired with the ignore-doc gate; PLT cache key includes OTP+Elixir+lockfile; actionlint passes; no advisory-mode flags survive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.credo.exs` / `.dialyzer_ignore.exs` -> CI shell-gate scripts | Scripts read repo-controlled config files; no untrusted input crosses. |
| `mix dialyzer` PLT cache -> CI runner | PLT cache key includes OTP+Elixir+lockfile to prevent silent OTP-bump invalidation. |
| Inline `# credo:disable-for-next-line` -> source files | Inline suppressions are visible in `git blame`; the `Tracking: Phase 9 rename to error?/1` annotation creates an audit trail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | Silent suppression added without documentation | mitigate | Paired shell-gate scripts (`check_dialyzer_ignore.sh` + `check_credo_suppressions.sh`) fail CI on any disable/ignore lacking the required `# Reason:` (and `# Tracking:` for Credo) comment. |
| T-08-06-02 | Tampering | `@dialyzer {:nowarn_function, ...}` source pragma bypasses centralized ignore file | accept (manual review) | D-08-08 ban; codified as backlog item to author a Credo check banning the pragma; current mitigation is grep in CI verification + manual code review. |
| T-08-06-03 | Information disclosure | PLT cache leaks OTP/Elixir version (cache key in workflow logs) | accept | Cache key is non-sensitive; visible in workflow logs by design. |
| T-08-06-04 | Tampering | PLT cache poisoning (OTP minor-bump silently invalidates PLT, leading to false-clean Dialyzer runs) | mitigate | D-08-06 — cache key includes OTP+Elixir+lockfile; OTP minor-bump produces a new key, forcing PLT rebuild. |
| T-08-06-05 | Repudiation | `is_error?/1` rename forgotten in Phase 9 | mitigate | Inline `Tracking: Phase 9 rename to error?/1` is greppable; can be enumerated via `grep -rn "Tracking: Phase 9"` at Phase 9 kickoff. |
| T-08-06-06 | Denial of service | 15-entry cap blown -> dialyzer-deep-clean phase needed | accept (escalation) | D-08-07 tripwire; SUMMARY surfaces the overflow signal instead of padding. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix credo --strict` exits 0.
- `bash scripts/check_credo_suppressions.sh` exits 0.
- `mix dialyzer` exits 0 in BOTH `./` and `mailglass_admin/` (default halts on warnings).
- `bash scripts/check_dialyzer_ignore.sh` exits 0.
- `actionlint .github/workflows/ci.yml` exits 0.
- `grep -cE '^\s*\{' .dialyzer_ignore.exs` <= 15 (D-08-07 cap).
- `grep -cE '^\s*\{' mailglass_admin/.dialyzer_ignore.exs` <= 15.
- `grep -rE "@dialyzer\s+\{:nowarn" lib/` returns 0 (D-08-08).
- `grep -rn "Tracking: Phase 9" lib/` returns >=1 (the inline `is_error?/1` suppression is visible for the Phase 9 review).
- Phase-9-firewall: `git diff lib/mailglass/mailable.ex lib/mailglass/message.ex` is empty.
- Negative test (Credo): synthetic disable without Reason+Tracking exits the script non-zero.
- Negative test (Dialyzer): synthetic ignore without `# Reason:` exits the script non-zero.
- All Phase 8 CI gates green: docs.check (08-02) + Tests strict (08-05) + Credo strict + Dialyzer + advisory-matrix (08-04).
</verification>

<success_criteria>
- `mix credo --strict` passes; 5 baseline disables documented (D-08-18 verbatim) with paired Reason+Tracking comments; 10 in-scope findings fixed (D-08-17); `is_error?/1` suppressed inline (D-08-20); 2 fixtures get `@moduledoc false` (D-08-17).
- `mix dialyzer` halts on warnings (default; `--ignore-exit-status` REMOVED; `--halt-exit-status` NOT used per D-08-09); flag block in BOTH `mix.exs` files; per-package ignore files (D-08-02) with strict-format tuples (D-08-03) and `list_unused_filters: true` (D-08-04).
- Per-package ignore files have <=15 documented entries each (D-08-07).
- Paired shell-gate scripts (`check_credo_suppressions.sh` + `check_dialyzer_ignore.sh`) enforce comment conventions (D-08-05, D-08-19, D-08-27).
- `@dialyzer {:nowarn_function, ...}` source pragmas remain banned (D-08-08).
- PLT cache key includes OTP+Elixir+lockfile (D-08-06).
- Phase-9-firewall respected — Mailable, Message, Outbound DSL, mailable.ex:129 untouched (D-08-29).
- Oban-archetype posture (D-08-28) reflected in disable list rationale + flag selection.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-engineering-hardening/08-06-SUMMARY.md`. Include:
- Pre-flag-tune Dialyzer finding count (the 230 baseline) vs post-flag-tune count
- Final entry count in `.dialyzer_ignore.exs` and `mailglass_admin/.dialyzer_ignore.exs` (each <=15)
- A table of the 10 Credo findings fixed with file:line for each
- The exact Credo disable-list ordering with each entry's Reason+Tracking text
- Confirmation that the negative tests passed locally before commit (synthetic disable / synthetic ignore both rejected by their respective shell-gate scripts)
- Confirmation that Phase-9-firewall files were not touched (`git diff` evidence)
- IF .dialyzer_ignore.exs would have exceeded 15 entries -> the SUMMARY MUST start with `## DIALYZER OVERFLOW — V0.3 PHASE NEEDED` and the v0.3 backlog/STATE.md entry should be filed before merging this plan.
</output>
