---
phase: 09-mailable-api-redesign-freeze
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - guides/upgrading-from-v0_1.md
autonomous: true
requirements:
  - API-07
---

<objective>
Write the `upgrading-from-v0_1.md` guide to help adopters migrate to the v0.2 API surface.

Purpose: Provide a clear, step-by-step path for migrating existing applications, leveraging the automated codemod and explaining manual fallbacks.
Output: A comprehensive markdown guide with before/after examples, the codemod walkthrough, and edge-case handling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Upgrading Guide</name>
  <files>guides/upgrading-from-v0_1.md</files>
  <action>
    Create `guides/upgrading-from-v0_1.md`.
    Follow the `Guide Structure Pattern` (analog: `guides/migration-from-swoosh.md`) as documented in `09-PATTERNS.md`.
    Include the following sections:
    - **Before/After Examples**: Show a v0.1 mailable using `Swoosh.Email` vs a v0.2 mailable using native `Message` setters.
    - **Codemod Walkthrough**: Document running `mix deps.add igniter --dev` and `mix mailglass.upgrade.v0_2 --apply`.
    - **Ambiguous Cases / Recipes**: Explain what to do if the codemod emits a warning (e.g., using `update_swoosh/2` for unsupported Swoosh functions).
    - **Dependency Matrix**: State the minimum versions required for this upgrade.
    - **Rollback Procedure**: Document how to revert if the codemod causes issues (e.g., using `git checkout`).
  </action>
  <verify>
    <automated>cat guides/upgrading-from-v0_1.md | grep -q "mix mailglass.upgrade.v0_2"</automated>
  </verify>
  <done>Guide exists with all required sections and accurate codemod instructions.</done>
</task>

</tasks>

<must_haves>
  truths:
    - Adopters have clear instructions on how to use the automated upgrade path.
    - Manual fallback recipes are provided for custom Swoosh extensions.
  artifacts:
    - path: "guides/upgrading-from-v0_1.md"
      provides: "Migration instructions"
  key_links:
    - from: "guides/upgrading-from-v0_1.md"
      to: "mix mailglass.upgrade.v0_2"
      via: "documented command"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> Adopter | Guide ensures adopters correctly use the migration tool without data/code loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | `upgrading.md` | accept | Standard documentation practices apply. No sensitive info. |
</threat_model>

<verification>
File `guides/upgrading-from-v0_1.md` contains all expected headings.
</verification>

<success_criteria>
An adopter can successfully follow the guide to transition their v0.1 app to v0.2 using the Igniter codemod with confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mailable-api-redesign-freeze/09-05-SUMMARY.md`
</output>