---
phase: 09-mailable-api-redesign-freeze
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/mix/tasks/mailglass.stability.check.ex
  - docs/api_stability.md
autonomous: true
requirements:
  - API-06
---

<objective>
Enforce the v0.2 API contract by adding a stability script and updating stability documentation.

Purpose: Prevent future developer oversight from accidentally re-introducing `Swoosh.Email` types or functions into the public API footprint. Lock the API surface for the v0.2 milestone.
Output: A Mix task `mix mailglass.stability.check` that scans typespecs/docs for `Swoosh.Email.t()`, and an updated `docs/api_stability.md` detailing the freeze policy and escape hatches.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API Stability Script</name>
  <files>lib/mix/tasks/mailglass.stability.check.ex</files>
  <action>
    Create a mix task `mix mailglass.stability.check`.
    Follow the `Script/Task Execution Pattern` (analog: `lib/mix/tasks/mailglass.publish.check.ex`) and the `Shared Patterns` (Error Handling / Mix Tasks) as documented in `09-PATTERNS.md`.
    The script should compile the project and scan either the BEAM chunks (via `Code.Typespec.fetch_specs/1`) or grep the generated documentation/source for any exposed `Swoosh.Email.t()` in `@spec` or `@type` definitions within the `Mailglass` public namespace.
    If a leak is found, it must `exit(1)` with a clear error message. If clean, `exit(0)`.
  </action>
  <verify>
    <automated>mix mailglass.stability.check</automated>
  </verify>
  <done>Script exists and correctly exits 0 when no Swoosh types are leaked.</done>
</task>

<task type="auto">
  <name>Task 2: Write api_stability.md v2</name>
  <files>docs/api_stability.md</files>
  <action>
    Update `docs/api_stability.md` to reflect the v0.2 API freeze.
    Follow the `Documentation Structure Pattern` (analog: `docs/api_stability.md`) as documented in `09-PATTERNS.md`.
    Include a section on the deprecation policy and the `Since:` annotations.
    Add a specific section for `§Message Extensions` that explicitly documents `Mailglass.Message.update_swoosh/2` as the named escape hatch for advanced Swoosh functionality not covered by the native setters.
    Ensure the promise "freeze-until-vNext" is clearly stated.
  </action>
  <verify>
    <automated>grep -q "update_swoosh" docs/api_stability.md</automated>
  </verify>
  <done>Documentation is updated with the new policies and escape hatch.</done>
</task>

</tasks>

<must_haves>
  truths:
    - `mix mailglass.stability.check` fails if `Swoosh.Email.t()` leaks into the public API.
    - `update_swoosh/2` is officially documented as the escape hatch.
  artifacts:
    - path: "lib/mix/tasks/mailglass.stability.check.ex"
      provides: "Contract enforcement script"
    - path: "docs/api_stability.md"
      provides: "v0.2 API Stability Policy"
  key_links:
    - from: "lib/mix/tasks/mailglass.stability.check.ex"
      to: "Mailglass.* modules"
      via: "Typespec introspection"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal implementation -> Public Interface | Prevents internal engine (`Swoosh`) details from becoming load-bearing for adopters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `stability.check` | mitigate | Script ensures no internal types leak via specs, containing the blast radius of future internal engine changes. |
</threat_model>

<verification>
mix mailglass.stability.check exits 0 on a clean project.
</verification>

<success_criteria>
The v0.2 API boundary is machine-verified and documented, guaranteeing downstream OSS consumers can safely pin to `~> 0.2`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mailable-api-redesign-freeze/09-04-SUMMARY.md`
</output>