---
phase: 09-mailable-api-redesign-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/mailglass/message.ex
  - test/mailglass/message_test.exs
autonomous: true
requirements:
  - API-01
  - API-02
  - API-04
---

<objective>
Add `igniter` dev dependency and implement 8 native field setters on `Mailglass.Message`.

Purpose: Provide a native API surface that delegates to the underlying `Swoosh.Email` engine, so adopters no longer need to depend on or call `Swoosh.Email` directly.
Output: `Mailglass.Message` exposes `to/2`, `from/2`, `subject/2`, `text_body/2`, `html_body/2`, `header/3`, `attach/2`, and `put_tag/2`. Deprecated annotations on legacy signatures.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mailglass/message.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Igniter dev dependency</name>
  <files>mix.exs</files>
  <action>Add `igniter: "~> 0.7"` as a `:dev` only dependency. Ensure `mix deps.get` completes successfully.</action>
  <verify>
    <automated>mix deps.get && mix run -e 'System.halt(0)'</automated>
  </verify>
  <done>`igniter` is successfully added to the dependency tree for `:dev` environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 8 native field setters on Message</name>
  <files>lib/mailglass/message.ex, test/mailglass/message_test.exs</files>
  <behavior>
    - Test 1: `to/2` updates the underlying Swoosh struct correctly.
    - Test 2: `attach/2` proxies correctly to `Swoosh.Email.attachment/2`.
    - Test 3: The other 6 setters (`from/2`, `subject/2`, `text_body/2`, `html_body/2`, `header/3`, `put_tag/2`) update correctly.
  </behavior>
  <action>
    Implement `to/2`, `from/2`, `subject/2`, `html_body/2`, `text_body/2`, `header/3`, `attach/2`, and `put_tag/2`.
    Follow the `Native Setter Pattern` (analog: `lib/mailglass/message.ex`) as documented in `09-PATTERNS.md`.
    Use `update_swoosh/2` to mutate the underlying `%Swoosh.Email{}` struct.
    For `attach/2`, map it to `Swoosh.Email.attachment/2`.
    For `put_tag/2`, manually update the `tags: []` list on the `%Mailglass.Message{}` struct rather than Swoosh.
    Ensure `@doc since: "0.2.0"` is added to each new setter.
    Document `update_swoosh/2` prominently as the named escape hatch for advanced Swoosh extensions.
  </action>
  <verify>
    <automated>mix test test/mailglass/message_test.exs</automated>
  </verify>
  <done>All 8 setters exist and correctly alter the message struct, tested via unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add @deprecated annotations to legacy paths</name>
  <files>lib/mailglass/message.ex</files>
  <action>Identify any existing public functions on `Mailglass.Message` that expected a `%Swoosh.Email{}` as a direct parameter, or explicitly exposed Swoosh to adopters, and add Elixir 1.18+ `@deprecated "Use native Mailglass.Message setters instead"` annotations. Do NOT deprecate `update_swoosh/2` (it is an intentional escape hatch).</action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Deprecated annotations correctly applied where needed.</done>
</task>

</tasks>

<must_haves>
  truths:
    - Adopters can set basic email attributes via `Mailglass.Message` functions.
    - `update_swoosh/2` remains available for custom Swoosh features.
    - `mix deps.get` includes `igniter` for dev environment.
  artifacts:
    - path: "lib/mailglass/message.ex"
      provides: "Native setters proxying to Swoosh"
      exports: ["to", "from", "subject", "text_body", "html_body", "header", "attach", "put_tag"]
  key_links:
    - from: "lib/mailglass/message.ex"
      to: "Swoosh.Email"
      via: "update_swoosh/2 delegation"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter Code -> Mailable API | Input from adopter provided arguments for headers, to, from, etc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `Mailglass.Message` setters | mitigate | Setters delegate exactly to Swoosh which performs standard input validation against header injection. |
</threat_model>

<verification>
mix test test/mailglass/message_test.exs validates native behavior.
</verification>

<success_criteria>
`Mailglass.Message` handles the standard 8 email building responsibilities natively via direct function calls, completely hiding the `Swoosh.Email` wrapper for common use cases.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mailable-api-redesign-freeze/09-01-SUMMARY.md`
</output>