---
phase: 77-motion-and-microinteraction-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 77-01
  - 77-02
  - 77-03
files_modified:
  - mailglass_admin/priv/static/assets/app.css
  - mailglass_admin/priv/static/assets/app.js
autonomous: true
requirements:
  - MOTION-02
must_haves:
  truths:
    - "mix mailglass_admin.assets.build completes without error"
    - "git diff --exit-code priv/static/ returns 0 (bundle is committed and clean, or confirmed no-op)"
    - "mix verify.preview exits 0 (compile + ExUnit + bundle gate all pass)"
    - "bash scripts/check_motion_conformance.sh exits 0 (no new violations introduced)"
  artifacts:
    - path: "mailglass_admin/priv/static/assets/app.css"
      provides: "Rebuilt admin CSS bundle (may be bit-identical to previous; run confirms no-op)"
      contains: "mg-reveal"
  key_links:
    - from: "mailglass_admin/mix.exs verify.preview alias"
      to: "mailglass_admin/priv/static/"
      via: "mix mailglass_admin.assets.build + git diff --exit-code"
      pattern: "verify.preview"
---

<objective>
Rebuild the admin asset bundle and commit priv/static/ as required by D-08. The HEEx changes in Plan 01 (id attributes added to two divs) do not add new Tailwind classes, so the bundle rebuild may be bit-identical (no-op). The rebuild MUST still run — skipping it would leave priv/static/ in a state that has not been confirmed clean, and the Hex tarball ships whatever is in priv/static/. The mix verify.preview alias enforces this gate.

Purpose: Satisfy MOTION-02 SC4 (bundle-clean gate, D-08) and complete the anti-churn contract for GAP-19. This plan must be the last to execute after all HEEx/shell/CSS changes settle.
Output: Rebuilt priv/static/ committed (or confirmed no-op diff), mix verify.preview green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md
@.planning/phases/77-motion-and-microinteraction-polish/77-CONTEXT.md
@.planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md
@.planning/phases/77-motion-and-microinteraction-polish/77-01-SUMMARY.md
@.planning/phases/77-motion-and-microinteraction-polish/77-02-SUMMARY.md
@.planning/phases/77-motion-and-microinteraction-polish/77-03-SUMMARY.md

<interfaces>
<!-- mailglass_admin/mix.exs:183-188 — verify.preview alias: -->
<!-- "verify.preview": [ -->
<!--   "compile --no-optional-deps --warnings-as-errors", -->
<!--   "test --warnings-as-errors --exclude flaky", -->
<!--   "mailglass_admin.assets.build", -->
<!--   "cmd git diff --exit-code priv/static/" -->
<!-- ] -->

<!-- Vendored binary: mailglass_admin/tailwind-macos-arm64 (present) -->
<!-- The assets.build task invokes the vendored binary to compile assets/css/app.css → priv/static/ -->
<!-- Run from within mailglass_admin/ directory -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild admin asset bundle and confirm priv/static/ clean (D-08 / GAP-19)</name>
  <files>mailglass_admin/priv/static/assets/app.css, mailglass_admin/priv/static/assets/app.js</files>
  <read_first>
    - mailglass_admin/mix.exs:183-188 (the verify.preview alias — confirm the exact task sequence: compile, test, assets.build, git diff --exit-code priv/static/)
    - .planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md Anchor 8 (verify.preview is a local developer gate; the bundle rebuild uses the vendored tailwind-macos-arm64 binary; D-01/D-02 only add id attributes so bundle may be bit-identical — run it anyway, never skip)
  </read_first>
  <action>From within the mailglass_admin/ directory, run the full verify.preview alias: `mix verify.preview`. This executes in sequence: compile --no-optional-deps --warnings-as-errors, then tests, then mailglass_admin.assets.build (rebuilds app.css using the vendored tailwind-macos-arm64 binary), then git diff --exit-code priv/static/ (the bundle-clean gate). The HEEx changes from Plan 01 (adding id attributes) do not add new Tailwind classes, so the bundle output will likely be bit-identical to the pre-phase baseline. Run it anyway and let git diff confirm.

If git diff --exit-code priv/static/ exits 0 (clean — no-op rebuild as expected): the bundle gate passes, nothing to commit beyond what Plan 01-03 already staged.

If git diff shows priv/static/ modified (unexpected but possible if Tailwind JIT detected a class dependency change): stage and commit the updated priv/static/ files with the commit message pattern: `chore(77): rebuild admin bundle (D-08, GAP-19)`.

Also run bash scripts/check_motion_conformance.sh from repo root to confirm no violations were introduced across the full phase. Expected: exits 0, "OK: motion conformance clean."

Cites GAP-19 (sev 3) per anti-churn contract.</action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.preview 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mix verify.preview in mailglass_admin/ exits 0 (all four stages pass: compile, test, assets.build, git diff --exit-code)
    - The "cmd git diff --exit-code priv/static/" stage exits 0 (clean — bundle matches what is committed)
    - mix test --seed 0 --exclude flaky stage: 187 tests, 1 pre-existing voice_test failure only (no new failures)
    - bash scripts/check_motion_conformance.sh from repo root exits 0 and prints "OK: motion conformance clean."
    - If priv/static/ had any diff: it is committed in this plan with the chore(77) commit message
  </acceptance_criteria>
  <done>mix verify.preview exits 0; git diff --exit-code priv/static/ is clean; bash scripts/check_motion_conformance.sh exits 0. Phase 77 is self-verified and ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset build pipeline | tailwind-macos-arm64 binary reads app.css, writes priv/static/; binary is vendored and pinned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-01 | Tampering | Vendored tailwind-macos-arm64 binary writes to priv/static/ | accept | Binary is the same vendored copy used throughout v1.7 milestone; committed at Phase 76; git diff gate catches any unexpected output. No new binary introduced. |
| T-77-04-02 | Spoofing | Stale priv/static/ in Hex tarball | mitigate | The git diff --exit-code priv/static/ gate in verify.preview is the mitigation. If bundle is stale, the gate fails and blocks release. |
| T-77-04-SC | Tampering | No package installs in this plan | accept | No new npm/mix deps. All tooling already installed. |

No new untrusted input, no new auth surface, no PII, no external I/O. This plan rebuilds a static asset bundle and verifies cleanness.
</threat_model>

<verification>
After task:
1. `cd mailglass_admin && mix verify.preview` exits 0 (full end-to-end gate)
2. `bash scripts/check_motion_conformance.sh` from repo root exits 0
3. `cd mailglass_admin && npm run test:operator-browser` — all tests pass (final confirmation)
4. `git status` shows no unstaged changes in priv/static/ (bundle is committed or confirmed bit-identical)
</verification>

<success_criteria>
- mix verify.preview exits 0: compile clean, 187 ExUnit tests (1 pre-existing voice_test failure), bundle rebuilt, git diff --exit-code priv/static/ clean
- bash scripts/check_motion_conformance.sh exits 0
- npm run test:operator-browser passes all new tests (delivery id-presence, reduced-motion), skips inbound test, passes existing tests
- priv/static/ is in a committed clean state — the Hex tarball would ship the correct bundle
</success_criteria>

<output>
Create `.planning/phases/77-motion-and-microinteraction-polish/77-04-SUMMARY.md` when done
</output>
