---
phase: 77-motion-and-microinteraction-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_motion_conformance.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - MOTION-02
must_haves:
  truths:
    - "scripts/check_motion_conformance.sh exists at repo root, is executable, and exits 0 against the current codebase"
    - "Pass A greps both mailglass_admin/lib/ and mailglass_admin/assets/css/app.css for layout-thrashing tokens and duration-300+"
    - "Pass B greps mailglass_admin/lib/ ONLY for ease-in-out/ease-linear/bare ease-in (avoiding the false positive on --ease-in-out CSS variable at app.css:120)"
    - "ci.yml credo_strict job wires bash scripts/check_motion_conformance.sh immediately after check_credo_suppressions.sh"
    - "Script returns OK: motion conformance clean. when no violations present"
  artifacts:
    - path: "scripts/check_motion_conformance.sh"
      provides: "CI-runnable motion conformance grep gate"
      contains: "check_motion_conformance"
    - path: ".github/workflows/ci.yml"
      provides: "CI wiring of the new grep gate in credo_strict job"
      contains: "check_motion_conformance.sh"
  key_links:
    - from: "ci.yml:credo_strict job"
      to: "scripts/check_motion_conformance.sh"
      via: "run: bash scripts/check_motion_conformance.sh step"
      pattern: "check_motion_conformance.sh"
---

<objective>
Author the motion conformance grep gate (D-06) as a new repo-root scripts/check_motion_conformance.sh shell script and wire it into the credo_strict CI job. The script implements a two-pass banned-token check: Pass A covers layout-thrashing tokens (both lib/ and app.css), Pass B covers easing classes (lib/ only, to avoid false positive on the legitimate --ease-in-out CSS custom property at app.css:120). This gate is reused at Phase 79 as the motion half of the full conformance check.

Purpose: Satisfy MOTION-02 — CI-enforced regression protection against future layout-thrashing class or banned easing additions. Current baseline is zero violations (confirmed by RESEARCH.md Anchor 4). Cites GAP-19 (sev 3) per anti-churn contract.
Output: scripts/check_motion_conformance.sh (new, ~30 lines) + one-step addition to ci.yml credo_strict job.

Scope (D-04): GAP-20 (timeline stagger) and the layout-thrashing sweep are treated as **verification-only** — this grep gate IS that verification. No CSS/code is authored to "fix" the already-clean baseline (zero banned tokens per RESEARCH Anchor 4); the timeline stagger stops re-firing once the parent reveal div is id-keyed by Plan 01 (D-01), so no separate change is needed here.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md
@.planning/phases/77-motion-and-microinteraction-polish/77-CONTEXT.md
@.planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md
@.planning/phases/77-motion-and-microinteraction-polish/77-PATTERNS.md

<interfaces>
<!-- Analog: scripts/check_credo_suppressions.sh (lines 1-43) — exact structure to mirror -->
<!-- #!/usr/bin/env bash -->
<!-- set -euo pipefail -->
<!-- FILE=".credo.exs" -->
<!-- errors=0 -->
<!-- # grep/awk logic — on hit: errors=$((errors + 1)) -->
<!-- if [[ $errors -gt 0 ]]; then echo "FAIL: ..." >&2; exit 1; fi -->
<!-- echo "OK: all credo suppressions are documented." -->

<!-- Analog: ci.yml:395-398 — the step to mirror immediately after -->
<!-- - name: Verify suppression docs (shell gate) -->
<!--   # Fails CI if any {Credo.Check..., false} entry is missing -->
<!--   # a `# Reason:` + `# Tracking:` comment block. -->
<!--   run: bash scripts/check_credo_suppressions.sh -->

<!-- False-positive risk: app.css:120 defines --ease-in-out as a CSS custom property. -->
<!-- Pass B (easing classes) must grep lib/ ONLY, not app.css. -->
<!-- Pass A (layout-thrashing) may grep both lib/ and app.css (no false positive risk there). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/check_motion_conformance.sh (D-06 / GAP-19)</name>
  <files>scripts/check_motion_conformance.sh</files>
  <read_first>
    - scripts/check_credo_suppressions.sh (full file — exact structure, shebang, set -euo pipefail, errors counter, exit 1 / echo OK pattern to mirror; note: uses [[:space:]]* not \s* for POSIX awk portability on macOS)
    - scripts/check_dialyzer_ignore.sh (lines 1-37 — shows multi-file target iteration pattern)
    - .planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md Anchor 4 (verified banned token baseline: all zero; the false-positive nuance for ease-in-out in app.css:120)
    - .planning/phases/77-motion-and-microinteraction-polish/77-PATTERNS.md (Pattern 3: Shell Conformance Gate Structure — exact Pass A/B split and banned-token sets)
  </read_first>
  <action>Create scripts/check_motion_conformance.sh at the repo root scripts/ directory (NOT mailglass_admin/scripts/ which holds only ui-audit.sh). Follow the exact conventions of check_credo_suppressions.sh: shebang #!/usr/bin/env bash, set -euo pipefail, errors=0 counter, exit 1 on nonzero, echo "OK: motion conformance clean." on success.

Two-pass structure (required to avoid false positive on --ease-in-out CSS var at app.css:120):

Pass A — layout-thrashing + duration tokens — grep BOTH mailglass_admin/lib/ AND mailglass_admin/assets/css/app.css. Banned patterns: transition-height, transition-max-height, transition-padding, transition-all, duration-300 (and higher: duration-[4-9][0-9][0-9], duration-[0-9]{4,}). Use grep -rE with pattern joined by |.

Pass B — banned easing classes — grep mailglass_admin/lib/ ONLY (NOT app.css, which legitimately defines --ease-in-out as a CSS custom property at line 120; including app.css in Pass B produces a false positive that blocks every CI run). Banned patterns: ease-in-out, ease-linear, ease-in[^-] (BSD/GNU portable form; avoids matching ease-in-out; do NOT use \b which behaves differently across macOS BSD grep and Ubuntu GNU grep).

The script runs from repo root (paths are relative to repo root). Make the file executable (chmod +x equivalent in the creation step, or note that CI invokes it via `bash scripts/...` so executable bit is not strictly required but good practice). On any grep hit in either pass: print the offending lines to stderr, increment errors. If errors > 0: echo "FAIL: motion conformance violations found (see above)" to stderr, exit 1. Cites GAP-19 (sev 3) in the file header comment per anti-churn contract.</action>
  <verify>
    <automated>bash /Users/jon/projects/mailglass/scripts/check_motion_conformance.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check_motion_conformance.sh exists at repo root scripts/ directory (not mailglass_admin/scripts/)
    - bash scripts/check_motion_conformance.sh exits 0 and prints "OK: motion conformance clean." against current codebase (current baseline is clean per RESEARCH.md Anchor 4)
    - The script greps mailglass_admin/lib/ for Pass A tokens (transition-height, transition-max-height, transition-padding, transition-all, duration-300 / higher)
    - The script greps mailglass_admin/assets/css/app.css for Pass A tokens (same set)
    - The script greps mailglass_admin/lib/ ONLY for Pass B tokens (ease-in-out, ease-linear, ease-in[^-])
    - The script does NOT grep app.css for Pass B tokens (false-positive protection for --ease-in-out at app.css:120)
    - Script exits 1 (not 0) when a banned token is manually injected for smoke-test purposes
  </acceptance_criteria>
  <done>bash scripts/check_motion_conformance.sh exits 0 and prints "OK: motion conformance clean." from the repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Wire check_motion_conformance.sh into credo_strict CI job (D-06)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:393-401 (the credo_strict job's "Verify suppression docs" and "Run Credo strict" steps; the new step inserts immediately after line 398, before "Run Credo strict")
    - scripts/check_motion_conformance.sh (just authored — confirm the exact filename to reference in the yaml run: command)
  </read_first>
  <action>In .github/workflows/ci.yml, add a new step in the credo_strict job immediately after the existing "Verify suppression docs (shell gate)" step (currently at lines 395-398, which runs bash scripts/check_credo_suppressions.sh). The new step:

name: Verify motion conformance (shell gate)
comment: Fails CI if any banned layout-thrashing or easing token appears in lib/ or app.css per UI-SPEC Motion Rules (Phase 74 FROZEN contract, GAP-19 sev 3).
run: bash scripts/check_motion_conformance.sh

Mirror the existing step's YAML format exactly (2-space indent, name/run fields, comment as a YAML comment `#`). No other changes to ci.yml. The credo_strict job already has the working directory set correctly for repo-root scripts.</action>
  <verify>
    <automated>grep -n "check_motion_conformance" /Users/jon/projects/mailglass/.github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a step with run: bash scripts/check_motion_conformance.sh in the credo_strict job
    - The new step appears after the check_credo_suppressions.sh step (line order: check_credo_suppressions.sh step, then check_motion_conformance.sh step, then Run Credo strict step)
    - grep -A2 "check_motion_conformance" ci.yml shows the step is inside the credo_strict job block
    - No other ci.yml content is altered
  </acceptance_criteria>
  <done>.github/workflows/ci.yml credo_strict job includes bash scripts/check_motion_conformance.sh step after check_credo_suppressions.sh; bash scripts/check_motion_conformance.sh continues to exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI shell script execution | check_motion_conformance.sh runs as a CI step; inputs are repo-internal files only (lib/, app.css) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-01 | Tampering | check_motion_conformance.sh — CI gate | accept | Script only reads repo-internal files; no external I/O, no secrets, no network. Grep output is printed to stderr on failure; no sensitive data in scanned files. |
| T-77-02-02 | Denial of Service | Pathological grep pattern causing runaway regex | accept | Patterns are simple literal/character-class regexes with no catastrophic backtracking risk; all scan bounded internal paths. |
| T-77-02-SC | Tampering | No package installs in this plan | accept | No npm/mix deps added; no package legitimacy audit needed. |

No new untrusted input, no new auth surface, no PII, no external I/O. This plan adds a read-only shell grep gate and a CI YAML step.
</threat_model>

<verification>
After both tasks:
1. `bash scripts/check_motion_conformance.sh` from repo root — exits 0, prints "OK: motion conformance clean."
2. `grep -n "check_motion_conformance" .github/workflows/ci.yml` — shows one match in the credo_strict job block
3. Confirm step ordering in ci.yml: check_credo_suppressions.sh line < check_motion_conformance.sh line < "Run Credo strict" line
</verification>

<success_criteria>
- scripts/check_motion_conformance.sh exists, is a valid bash script following check_credo_suppressions.sh conventions
- bash scripts/check_motion_conformance.sh exits 0 against current codebase (zero violations)
- Pass B easing check scopes to lib/ only (no false positive on --ease-in-out at app.css:120)
- ci.yml credo_strict job includes the new step after check_credo_suppressions.sh
</success_criteria>

<output>
Create `.planning/phases/77-motion-and-microinteraction-polish/77-02-SUMMARY.md` when done
</output>
