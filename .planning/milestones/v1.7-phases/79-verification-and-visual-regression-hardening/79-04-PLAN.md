---
phase: 79-verification-and-visual-regression-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 79-01
  - 79-02
  - 79-03
files_modified:
  - mailglass_inbound/mix.exs
autonomous: true
requirements:
  - VERIF-04

must_haves:
  truths:
    - "mailglass_inbound/mix.exs pins {:mailglass, '== 1.5.0'} in the MIX_PUBLISH branch"
    - "The inbound pin change is the only file modified in this plan (no mix.lock drift committed)"
    - "release-please-config.json is unchanged — mailglass_inbound is not added to the linked group"
    - "The commit message uses a conventional-commit prefix (docs or chore) so Release Please recognizes the inbound patch bump"
    - "All three verification gates from Wave 1 (conformance script, e2e suite, closeout artifact) are confirmed green before this plan runs"
  artifacts:
    - path: "mailglass_inbound/mix.exs"
      provides: "Inbound exact-pin updated to target core version 1.5.0"
      contains: "== 1.5.0"
    - path: "release-please-config.json"
      provides: "Confirmed unchanged — inbound NOT in linked group (read-only verification)"
  key_links:
    - from: "mailglass_inbound/mix.exs line 116"
      to: "mailglass core 1.5.0 target release"
      via: "{:mailglass, \"== 1.5.0\"}"
      pattern: "== 1.5.0"
---

<objective>
Update the `mailglass_inbound` exact-pin in `mailglass_inbound/mix.exs` from `== 1.4.5` to `== 1.5.0`, and verify the Release Please pipeline is ready to produce the linked 1.5.0 bump for mailglass + mailglass_admin and the 1.1.6 patch bump for mailglass_inbound. This is the PREPARE-ONLY release ceremony deliverable — no `mix hex.publish`, no hand-merge of the Release Please PR.

Purpose: When the hands-free Release Please pipeline detects the conventional commits from Phases 75–79 on main, it will open a PR bumping mailglass+mailglass_admin to 1.5.0 (linked group) and mailglass_inbound to 1.1.6 (independent). The inbound exact-pin must be pre-updated to avoid a stale `== 1.4.5` dependency conflict that would block adopters who install both packages after the release.
Output: `mailglass_inbound/mix.exs` line 116 updated; conventional-commit history confirmed; Release Please configuration verified intact.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md

<interfaces>
<!-- mailglass_inbound/mix.exs lines 114–119 (from PATTERNS.md) -->
Current state:
  defp mailglass_dep do
    if System.get_env("MIX_PUBLISH") == "true" do
      {:mailglass, "== 1.4.5"}          ← line 116: change to "== 1.5.0"
    else
      {:mailglass, path: "..", override: true}
    end

Target state (line 116 only):
  {:mailglass, "== 1.5.0"}

The `path:` dev branch (line 118) is untouched.

Release Please state (read-only verification):
  .release-please-manifest.json current: { ".": "1.4.5", "mailglass_admin": "1.4.5", "mailglass_inbound": "1.1.5" }
  Post-pipeline target:               { ".": "1.5.0", "mailglass_admin": "1.5.0", "mailglass_inbound": "1.1.6" }
  Phase 79 does NOT hand-edit this file — Release Please owns it.

  release-please-config.json linked group: ["mailglass", "mailglass_admin"] only.
  mailglass_inbound is NOT in the linked group. Do NOT add it.

Wave 1 confirmation gate (before proceeding):
  bash mailglass_admin/scripts/check-conformance.sh → must exit 0
  cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js → must pass
  grep -c "CLOSED" .planning/phases/79-verification-and-visual-regression-hardening/79-GAP-CLOSEOUT.md → must be ≥ 5

Swoosh lock drift gotcha:
  reference/demo_app re-bumps swoosh lock on ANY mix run in that directory.
  This plan does NOT touch reference/demo_app.
  If demo_app/mix.lock appears dirty from prior work: git checkout reference/demo_app/mix.lock
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Wave 1 gates green and update inbound exact-pin</name>
  <files>mailglass_inbound/mix.exs</files>
  <read_first>
    - mailglass_inbound/mix.exs (read lines 110–125 — the mailglass_dep/0 function block; confirm current pin is "== 1.4.5" on the exact line before editing)
    - release-please-config.json (read to confirm mailglass_inbound is NOT in the linked-versions components array — verify intact, do not modify)
    - .release-please-manifest.json (read to confirm current state: 1.4.5/1.4.5/1.1.5)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md (Area 6: inbound exact-pin re-pin gotcha, Pitfall 4, Pitfall 5)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md (mailglass_inbound/mix.exs section — targeted single-line edit only)
  </read_first>
  <action>
Step 1 — Confirm Wave 1 gates are green before proceeding:
  Run `bash mailglass_admin/scripts/check-conformance.sh` from repo root — must exit 0.
  Run `cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js` — all tests must pass.
  Run `grep -c "CLOSED" .planning/phases/79-verification-and-visual-regression-hardening/79-GAP-CLOSEOUT.md` — must return ≥ 5.
  If any gate fails, do NOT proceed to the pin update — document the failure and halt.

Step 2 — Read mailglass_inbound/mix.exs to confirm the exact line number of `{:mailglass, "== 1.4.5"}`. RESEARCH.md says line 116, but confirm before editing.

Step 3 — Update the single line:
  Change `{:mailglass, "== 1.4.5"}` to `{:mailglass, "== 1.5.0"}`.
  Leave the `{:mailglass, path: "..", override: true}` dev branch entirely untouched.
  Leave all other mix.exs content unchanged.

Step 4 — Verify no mix.lock drift from prior work:
  Run `git status` and check if `reference/demo_app/mix.lock` appears dirty. If so, run `git checkout reference/demo_app/mix.lock` before staging anything.

Step 5 — Verify the inbound package compiles against the local path dep (dev mode):
  Run `cd mailglass_inbound && mix compile` — this uses the `path:` branch (MIX_PUBLISH is not set), not the `== 1.5.0` pin, so it should compile cleanly against the local workspace.

Step 6 — Confirm release-please-config.json is unchanged:
  Run `git diff release-please-config.json` — must show no changes. Do NOT add mailglass_inbound to the linked-versions components array.

Step 7 — Verify target state:
  `grep '== 1.5.0' mailglass_inbound/mix.exs` must match exactly one line.
  `grep '== 1.4.5' mailglass_inbound/mix.exs` must return no results.

Use commit prefix `docs(79):` or `chore(79):` for this release-ceremony preparation commit. The Release Please pipeline will detect this and include it in the inbound changelog as a patch bump trigger.
  </action>
  <verify>
    <automated>grep "== 1.5.0" mailglass_inbound/mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "== 1.5.0" mailglass_inbound/mix.exs` matches exactly one line containing {:mailglass, "== 1.5.0"}
    - `grep "== 1.4.5" mailglass_inbound/mix.exs` returns no results (old pin removed)
    - `cd mailglass_inbound && mix compile` exits 0 (local dev path dep unaffected)
    - `git diff release-please-config.json` shows no changes (mailglass_inbound NOT added to linked group)
    - `git diff .release-please-manifest.json` shows no changes (Release Please owns this file, not Phase 79)
    - `git diff --name-only` shows ONLY mailglass_inbound/mix.exs in the staged diff (no mix.lock drift, no demo_app changes)
    - Wave 1 gates confirmed green (check-conformance.sh exits 0, Playwright suite passes, 79-GAP-CLOSEOUT.md has ≥ 5 CLOSED rows)
  </acceptance_criteria>
  <done>mailglass_inbound/mix.exs pins {:mailglass, "== 1.5.0"}; release-please-config.json unchanged; mix.lock clean; Wave 1 gates confirmed green</done>
</task>

<task type="auto">
  <name>Task 2: Run full verify.preview and confirm release-ceremony readiness</name>
  <files></files>
  <read_first>
    - mailglass_admin/mix.exs (lines 183–195 — the verify.preview alias steps, to understand what it runs)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-CONTEXT.md (D-09/D-10/D-11: what Phase 79 prepares vs. what the pipeline owns)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md (Area 6: release ceremony — what triggers the 1.5.0 bump, what Release Please owns)
  </read_first>
  <action>
Run the full verification suite and document the release-ceremony readiness state.

Step 1 — Run mix verify.preview:
  `cd mailglass_admin && mix verify.preview`
  This runs: compile --no-optional-deps --warnings-as-errors, test --warnings-as-errors --exclude flaky, mailglass_admin.assets.build, cmd git diff --exit-code priv/static/
  Must exit 0. Do NOT run with --force (per project memory project_execute_phase_no_optional_deps_pollution).
  Expected: 189 tests, 0 failures, voice_test "Oops" pre-existing exclusion is fine (it is a dep-JS noise test, not a phase gate — per project memory voice_test_noops_dep_js).

Step 2 — Run the full Playwright suite one final time:
  `cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js`
  Must pass.

Step 3 — Record the release-ceremony readiness in the plan SUMMARY (not a separate file). Confirm:
  a. Conventional-commit history contains feat() and fix() commits from Phases 75-79 that will trigger the Release Please linked-group 1.4.5 → 1.5.0 minor bump.
  b. mailglass_inbound/mix.exs now pins == 1.5.0, which will trigger the inbound 1.1.5 → 1.1.6 patch bump when the Release Please PR for inbound is opened.
  c. Phase 79 does NOT: run mix hex.publish, hand-merge the Release Please PR, bump mix.exs version numbers manually, or edit CHANGELOG files.
  d. The hands-free pipeline (Release Please auto-PR, auto-merge on green CI, hex-publish fan-out) owns all of the above once v1.7 work lands on main.

Step 4 — Verify no regressions were introduced by checking the ExUnit suite:
  `cd mailglass_admin && mix test --seed 0 --warnings-as-errors`
  Must pass (same baseline: 189 tests, 0 failures, pre-existing voice_test exclusion).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix verify.preview</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix verify.preview` exits 0 (all sub-steps pass including bundle-clean gate)
    - `cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js` passes
    - `cd mailglass_admin && mix test --seed 0 --warnings-as-errors` exits 0
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0
    - SUMMARY records: (a) what Phase 79 prepared, (b) what the pipeline owns, (c) current .release-please-manifest.json state (1.4.5/1.4.5/1.1.5), (d) target state post-pipeline (1.5.0/1.5.0/1.1.6)
    - No mix hex.publish was run, no Release Please PR was hand-merged, no CHANGELOG was hand-edited
  </acceptance_criteria>
  <done>Full verification suite green; release-ceremony readiness confirmed and documented in SUMMARY; inbound pin at == 1.5.0; pipeline prepare-only posture maintained</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix.exs config → hex.pm (publish) | This plan does NOT publish; the exact-pin edit is a dependency version string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Tampering | mailglass_inbound/mix.exs | accept | Single-line dep version string edit; path: dev branch unchanged; mix compile confirms local path dep still resolves |
| T-79-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; verify.preview uses existing toolchain |

**Minimal threat surface assessment:** This plan edits a single dependency version string in a mix.exs file and runs verification commands. No new auth surfaces, no network endpoints, no data surfaces are introduced. The hands-free Release Please pipeline owns the actual Hex publish; Phase 79 only prepares. No stable seam is touched. No ASVS categories require new mitigations.
</threat_model>

<verification>
All four gates must pass before the phase is complete:

```
bash mailglass_admin/scripts/check-conformance.sh
cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js
cd mailglass_admin && mix verify.preview
grep "== 1.5.0" mailglass_inbound/mix.exs
```
</verification>

<success_criteria>
- mailglass_inbound/mix.exs pins {:mailglass, "== 1.5.0"} (old 1.4.5 pin removed)
- mix verify.preview exits 0 (compile + test + bundle-clean gate)
- Full Playwright suite passes (10 tests)
- check-conformance.sh exits 0 (five gates clean)
- Release-ceremony posture: prepare-only confirmed (no hex.publish, no hand-merge, no CHANGELOG edits)
- SUMMARY documents: what Phase 79 prepared vs. what the pipeline owns, with current/target manifest versions
</success_criteria>

<output>
Create `.planning/phases/79-verification-and-visual-regression-hardening/79-04-SUMMARY.md` when done
</output>
