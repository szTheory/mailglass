---
phase: 79-verification-and-visual-regression-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/e2e/operator.spec.js
autonomous: true
requirements:
  - VERIF-02

must_haves:
  truths:
    - "operator.spec.js has structural coverage for operator-overview-health and operator-overview-nav testids"
    - "operator.spec.js asserts inbound-orientation and preview-orientation testids are visible"
    - "The 'exact replay flow' test passes — it is not skipped, not marked todo, not commented out"
    - "All 8+ Playwright tests in the suite end green"
  artifacts:
    - path: "mailglass_admin/e2e/operator.spec.js"
      provides: "Extended e2e coverage for Operator Overview and all three orientation strips; fixed replay-flow test"
      contains: "operator-overview-health"
  key_links:
    - from: "operator.spec.js"
      to: "operator-overview-health testid"
      via: "getByTestId"
      pattern: "operator-overview-health"
    - from: "operator.spec.js"
      to: "inbound-orientation testid"
      via: "getByTestId"
      pattern: "inbound-orientation"
    - from: "operator.spec.js"
      to: "exact replay flow — timeline assertion"
      via: "toContainText with extended timeout"
      pattern: "Replay audit"
---

<objective>
Extend `operator.spec.js` with structural coverage for the new Operator Overview landing and all three orientation-strip testids, and fix the pre-existing "exact replay flow" test failure. The full Playwright suite must end green with all tests passing.

Purpose: VERIF-02 requires that new IA surfaces (Overview health cards, nav CTAs, inbound/preview orientation strips) have structural e2e coverage, and that the one tracked pre-existing failure is resolved — not skipped.
Output: `mailglass_admin/e2e/operator.spec.js` (two new tests added; replay-flow test fixed).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/todos/pending/preexisting-replay-flow-e2e-failure.md

<interfaces>
<!-- Key testids confirmed in operator_live.ex:280-328 and shell.ex:314-333 -->

Operator Overview testids (operator_live.ex):
  operator-overview          — wraps entire overview section (line 280)
  operator-overview-health   — health-count cards container (line 284)
  operator-overview-health-failures — (line 290)
  operator-overview-health-orphans  — (line 299)
  operator-overview-health-suppressions — (line 308)
  operator-overview-health-allclear — (line 317; renders when failures=0, orphans=0)
  operator-overview-nav      — navigation cards container (line 328)

Orientation strip testids (shell.ex:320, data-testid={"#{@surface}-orientation"}):
  deliveries-orientation  (already asserted at line 101 in the mobile test)
  inbound-orientation     (surface :inbound)
  preview-orientation     (surface :preview, at /dev/mail/)

URL patterns:
  Operator Overview:  /ops/mail?tenant_id=browser-tenant             (default view)
  Deliveries list:    /ops/mail?tenant_id=browser-tenant&view=deliveries
  Inbound list:       /ops/mail/inbound?tenant_id=browser-tenant
  Preview:            /dev/mail/

openOperator() already navigates to Overview landing then to Deliveries view.
For Overview tests: call openOperator(), then navigate back to Overview URL.
For inbound/preview: navigate to the surface-specific URL (page.goto).

Replay-flow failure root cause (RESEARCH.md Area 3, todo):
  Failure is at line 128: operator-timeline does not contain "Replay audit" after confirm.
  The preceding assertions at lines 124-127 PASS (replay executed, detail header updated).
  Root cause: Playwright timing — timeline DOM update is async relative to LiveView socket.
  Fix approach:
    1. Change deliveryRow(page, 3) → deliveryRow(page, 1) to anchor to browser-exact@example.com
       (confirmed sort order: desc last_event_at, desc inserted_at, desc id — exact is index 1
       post Phase-78 seed expansion per RESEARCH.md Area 3 assumption table)
    2. Add { timeout: 10000 } to the toContainText("Replay audit") assertion at line 128
    3. If the test still fails after the timeout extension, log the timeline innerHTML for
       diagnosis (RESEARCH.md recommendation): add a console.log or page.getByTestId("operator-timeline").innerHTML() before the assertion to diagnose whether the event row is present but text format differs vs. genuinely missing

Existing test structure (operator.spec.js):
  All tests are inside: test.describe("operator browser gate", () => { ... })
  Add new tests at the end of the describe block, before the closing })
  Follow the openOperator() → navigate → assert pattern exactly
  Use getByTestId() for structural assertions, toBeVisible() for presence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the "exact replay flow" e2e test</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js (read the full file — especially lines 104–131: the failing test; lines 163–182: noop test which uses deliveryRow(page, 1) for noop delivery; the full delivery order is visible in the surrounding tests)
    - mailglass_admin/test/support/operator_fixtures.ex (read to confirm the delivery sort order: browser-selected, browser-exact, browser-ambiguous, browser-noop post Phase-78 seed expansion)
    - .planning/todos/pending/preexisting-replay-flow-e2e-failure.md (the tracked failure — confirms line 128 is the failing assertion, confirms line 111 passes)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md (Area 3: Replay-flow failure root cause — recommended fix approach)
  </read_first>
  <action>
Fix the "exact replay flow" test (lines 104–131 of operator.spec.js). Do NOT skip or document-and-skip — this is the only sanctioned permanent skip is the voice_test "Oops" dep-JS noise.

Step 1 — Diagnose the current index: Before changing the index, verify the current delivery row order from `operator_fixtures.ex`. The RESEARCH.md sort analysis (desc: last_event_at, desc: inserted_at, desc: id) predicts:
  index 0 = browser-selected@example.com (hours_ago(1))
  index 1 = browser-exact@example.com (hours_ago(2), earliest inserted)
  index 2 = browser-ambiguous@example.com (hours_ago(2))
  index 3 = browser-noop@example.com (hours_ago(2), latest inserted or UUID tiebreak)

The current test uses `deliveryRow(page, 3)` and the assertion at line 111 (`exactRecipient`) passes, suggesting UUID ordering makes index 3 actually be exact. However, this is fragile. The noop test at line 167 uses `deliveryRow(page, 1)` for `noopRecipient`. Read the actual fixture to determine the definitive delivery order, then anchor the exact-flow selector to the correct stable index (or use provider_event_id "browser-exact-delivery" via a page.locator if index anchoring is unreliable).

Step 2 — Fix the timeline assertion: Add `{ timeout: 10000 }` to the `toContainText("Replay audit")` call at line 128. This gives the LiveView socket time to propagate the webhook_replay_succeeded event to the DOM.

Step 3 — Diagnostic fallback: If after adding the extended timeout the test still fails in your test run, add a diagnostic line immediately before the assertion:
  `const timelineHTML = await page.getByTestId("operator-timeline").innerHTML();`
  `console.log("DEBUG timeline HTML:", timelineHTML);`
This reveals whether the event row is present but text format differs (e.g., different badge label) or genuinely absent. Resolve based on what the diagnostic reveals, then remove the debug log before committing.

The final committed test must:
  - Not be skipped, commented, or marked test.skip
  - Use a stable delivery selection (anchored by confirmed sort order or provider_event_id)
  - Have { timeout: 10000 } on the "Replay audit" toContainText assertion
  - Pass when the full suite runs

Run the isolated test after the fix: `cd mailglass_admin && npx playwright test --config=playwright.config.cjs -g "exact replay flow"`
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test --config=playwright.config.cjs -g "exact replay flow"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --config=playwright.config.cjs -g "exact replay flow"` exits 0 (1 test passed)
    - The test is not marked test.skip, test.fixme, or commented out
    - The timeline assertion at (formerly) line 128 uses { timeout: 10000 } or equivalent extended wait
    - `grep -c "skip\|fixme\|todo.*replay" mailglass_admin/e2e/operator.spec.js` returns 0 (no new skips)
  </acceptance_criteria>
  <done>The "exact replay flow" test passes in isolation; no skip annotations added</done>
</task>

<task type="auto">
  <name>Task 2: Add Operator Overview and orientation-strip structural tests</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js (read the full file after Task 1 edits — focus on the openOperator() helper lines 13–30; existing inbound test lines 246–261; the describe block closing braces)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (lines 280–335 — operator-overview, operator-overview-health, operator-overview-health-* and operator-overview-nav testids)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (lines 314–333 — orientation strip testid pattern)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md (Area 3: "What is NOT covered" section — the four missing testids)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md (operator.spec.js patterns section — new test skeleton)
  </read_first>
  <action>
Add two new tests to the `test.describe("operator browser gate", ...)` block, placed at the end (before the closing `})`).

Test A: "operator overview landing has health cards and navigation CTAs"
  - setViewportSize({ width: 1280, height: 900 })
  - Call openOperator(page) — this navigates to Overview then to Deliveries
  - Navigate back to Overview: `await page.goto("/ops/mail?tenant_id=" + tenantId)`
  - Wait for the overview heading: `await expect(page.getByRole("heading", { name: "Operator overview", exact: true })).toBeVisible()`
  - Assert operator-overview container visible: `await expect(page.getByTestId("operator-overview")).toBeVisible()`
  - Assert operator-overview-health container visible: `await expect(page.getByTestId("operator-overview-health")).toBeVisible()`
  - Assert operator-overview-nav container visible: `await expect(page.getByTestId("operator-overview-nav")).toBeVisible()`
  - Note on health sub-testids: the `allclear` card only renders when failures=0 AND orphans=0; the `failures/orphans/suppressions` cards render when those counts are non-zero. With the northstar browser seed, assert whichever sub-testid is reliably present (read operator_live.ex lines 284–320 to confirm which branches render for the browser-tenant seed state). Assert at least `operator-overview-health` container, not necessarily a specific sub-card.

Test B: "inbound and preview surfaces render their orientation strips"
  - setViewportSize({ width: 1280, height: 900 })
  - Call openOperator(page) first (resets session)
  - Navigate to inbound: `await page.goto("/ops/mail/inbound?tenant_id=" + tenantId)`
  - Assert: `await expect(page.getByTestId("inbound-orientation")).toBeVisible()`
  - Navigate to preview: `await page.goto("/dev/mail/")`
  - Assert: `await expect(page.getByTestId("preview-orientation")).toBeVisible()`

Follow all existing operator.spec.js patterns:
  - Use `const { test, expect } = require("@playwright/test")` (already at top — do not duplicate)
  - Use getByTestId() for structural assertions, not locator("h2") or text selectors
  - Use await/async throughout
  - Keep consistent indentation (2 spaces) matching the file

Run the full suite after both tests are added: `cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js`
All tests (now 10 total) must pass.
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Full operator.spec.js Playwright suite exits 0 (10 tests passed — 8 original + 2 new)
    - `grep -c "operator-overview-health" mailglass_admin/e2e/operator.spec.js` returns >= 1
    - `grep -c "operator-overview-nav" mailglass_admin/e2e/operator.spec.js` returns >= 1
    - `grep -c "inbound-orientation" mailglass_admin/e2e/operator.spec.js` returns >= 1
    - `grep -c "preview-orientation" mailglass_admin/e2e/operator.spec.js` returns >= 1
    - No existing test is removed, skipped, or commented out
    - The replay-flow fix from Task 1 still passes (run -g "exact replay flow" to confirm)
  </acceptance_criteria>
  <done>10 Playwright tests pass; operator-overview-health, operator-overview-nav, inbound-orientation, preview-orientation are each asserted at least once; no test skips added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test → test server | Structural DOM assertions only; test server is local ExUnit-backed OperatorBrowserServer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages; Playwright already installed; no install step |

**Minimal threat surface assessment:** This plan modifies only the Playwright test file — a test-only asset with no production code path. No new auth surfaces, no new network endpoints, no new data surfaces are introduced. No stable seam is touched. No PII in test assertions (testids and recipient addresses are synthetic browser-tenant seeds, not real user data). No ASVS categories require new mitigations.
</threat_model>

<verification>
```
cd mailglass_admin && npx playwright test --config=playwright.config.cjs operator.spec.js
```

All tests must pass (exit 0). Additionally verify:

```
grep -c "operator-overview-health" mailglass_admin/e2e/operator.spec.js
grep -c "inbound-orientation" mailglass_admin/e2e/operator.spec.js
grep -c "preview-orientation" mailglass_admin/e2e/operator.spec.js
npx playwright test --config=playwright.config.cjs -g "exact replay flow"
```
</verification>

<success_criteria>
- Full Playwright suite green (all tests pass, no skips added)
- "exact replay flow" test passes with stable anchor + extended timeout on timeline assertion
- operator-overview-health, operator-overview-nav, inbound-orientation, preview-orientation each appear in at least one test assertion
- No existing test coverage is weakened or removed
</success_criteria>

<output>
Create `.planning/phases/79-verification-and-visual-regression-hardening/79-02-SUMMARY.md` when done
</output>
