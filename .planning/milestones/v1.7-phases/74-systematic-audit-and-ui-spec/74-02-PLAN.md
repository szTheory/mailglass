---
phase: 74-systematic-audit-and-ui-spec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "Every test() heading from operator.spec.js and demo.spec.js is inventoried"
    - "Every heading-text and seed-count assertion that Phases 75-78 will ripple is recorded with its source file:line"
    - "PNG path references from the audit-matrix run are recorded; the binaries themselves stay gitignored"
  artifacts:
    - path: ".planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md"
      provides: "Before-baseline: e2e heading/seed-count assertion inventory + PNG path references"
      min_lines: 40
  key_links:
    - from: "74-ASSERTION-INVENTORY.md"
      to: "mailglass_admin/e2e/operator.spec.js + reference/demo_app/assets/e2e/demo.spec.js"
      via: "every test() heading and heading/count assertion cited by file:line"
      pattern: "operator.spec.js"
---

<objective>
Produce `74-ASSERTION-INVENTORY.md` — the AUDIT-03 before-baseline. A text inventory of every demo/e2e `test()` heading and every heading-text / seed-count assertion across `mailglass_admin/e2e/operator.spec.js` and `reference/demo_app/assets/e2e/demo.spec.js` that the build phases (75-78) will ripple, PLUS the committed PNG *path references* produced by the audit-matrix run (Plan 01). The PNG binaries stay gitignored in `tmp/ui-audit/` (D-06) — this artifact records paths, never binaries.

Purpose: Phases 75-78 change IA headings ("Deliveries", "Inbound records"), add the Operator Overview, and expand seed data — each ripples e2e assertions. Phase 79 diffs against this baseline. Capturing the exact current assertions (with file:line) makes the ripple deliberate, not accidental.

Output: `.planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md`.

CONSTRAINT (ROADMAP success criterion 5): Zero production code changes. The e2e spec files are READ ONLY in this phase — they are not modified until the build phases. No file under `mailglass_admin/lib/`, no spec file, and no `priv/static/` file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-CONTEXT.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md

<interfaces>
<!-- The two e2e spec sources. The inventory enumerates every test() and every -->
<!-- heading/seed-count assertion. Grounded counts from the live tree: -->
<!-- operator.spec.js = 5 test() blocks; demo.spec.js = 3 test() blocks. -->

mailglass_admin/e2e/operator.spec.js — 5 tests under describe "operator browser gate":
  - Heading assertion: `getByRole("heading", { name: "Deliveries", exact: true })` (openOperator helper)
  - testids: operator-deliveries-list, operator-deliveries-list-card, operator-detail-column,
    operator-empty-detail, operator-delivery-row, operator-detail-header, operator-timeline,
    operator-suppression-card, operator-replay-open, operator-replay-modal, operator-replay-confirm
  - Text/copy assertions: "Replay is unavailable.", "Replay is ready.", "Replay is choice required.",
    "Replay completed with new work.", "Replay completed with no change.", "Last replay: completed · …"

reference/demo_app/assets/e2e/demo.spec.js — 3 tests under describe "mailglass demo evidence":
  - Heading assertions: `getByRole("heading", { name: "Northstar Ops", exact: true })`,
    `{ name: "Deliveries", exact: true }`, `{ name: "Inbound records", exact: true }`
  - Link/nav text: /preview mailables/i, /outbound operator/i, /inbound operator/i
  - testids: operator-deliveries-list, operator-delivery-row, operator-detail-header,
    operator-timeline, inbound-records-list, inbound-record-row, inbound-detail-header
  - Seeded-evidence assertions (reset via POST /demo/evidence/reset)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 74-ASSERTION-INVENTORY.md — heading + seed-count assertion baseline</name>
  <files>.planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js (read fully — every test() block, every getByRole heading, every getByText/toContainText copy assertion, every getByTestId)
    - reference/demo_app/assets/e2e/demo.spec.js (read fully — every test() block, heading assertions, link-name assertions, testids, the /demo/evidence/reset seed-reset call)
    - .planning/REQUIREMENTS.md (AUDIT-03 acceptance criterion — "inventory of every demo/e2e heading and seed-count assertion later phases will ripple")
    - .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md "Orientation Strip Contract" + "Per-Surface Acceptance Checklists" (which headings/testids Phases 75-78 will change — informs the "ripple risk" column)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-CONTEXT.md (D-06 PNG-path-references rule; Claude's-discretion note that AUDIT-03 may be a sibling file — chosen here as 74-ASSERTION-INVENTORY.md)
  </read_first>
  <action>
    Create `.planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md`. Structure it in three sections:

    SECTION 1 — operator.spec.js inventory. A table with one row per assertion that Phases 75-78 may ripple. Columns: `{spec_file:line, test() name, assertion kind (heading | testid | copy-text | seed-count), exact asserted string/value, ripple risk (which build phase will likely change it and why)}`. Cover all 5 test() blocks. Heading assertion `name: "Deliveries"` is high ripple-risk (IA-03 vocabulary pass, Phase 75). Replay copy strings are low risk (Phase 77 is motion, not copy). Enumerate every getByTestId so Phase 75's new Operator Overview / orientation-strip testids land against a known prior set.

    SECTION 2 — demo.spec.js inventory. Same column schema. Cover all 3 test() blocks. Heading assertions `"Northstar Ops"`, `"Deliveries"`, `"Inbound records"` are ripple candidates for IA-03 (Phase 75). The seeded-evidence assertions and the `/demo/evidence/reset` reset contract are the seed-count baseline that SEED-02 (Phase 78) updates "in the same change". Flag those rows as Phase 78 ripple.

    SECTION 3 — Before-baseline PNG path references. List the deterministic PNG paths the extended ui-audit.sh (Plan 01) writes under `tmp/ui-audit/` — `{surface}-{viewport}-{theme}.png` for surface in {preview, deliveries/operator, inbound}, viewport in {390,768,1440}, theme in {light,dark}. State explicitly (D-06) that these are gitignored reproducible evidence; the binaries are NEVER committed; the committed durable artifact is this inventory of paths. Note that Phase 79 re-runs the same matrix to the same paths for the before/after diff (VERIF-01).

    Add a one-line header stating zero production code / zero spec-file edits occurred (the specs are read-only in Phase 74). No fenced code blocks containing implementations — tables and prose only.
  </action>
  <verify>
    <automated>test -f .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md && grep -q 'operator.spec.js' .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md && grep -q 'demo.spec.js' .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md && grep -q 'Northstar Ops' .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md && grep -q 'Inbound records' .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md && grep -q 'tmp/ui-audit' .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `74-ASSERTION-INVENTORY.md` exists
    - Every `test()` heading from operator.spec.js (5 blocks) and demo.spec.js (3 blocks) appears in the inventory (cross-check: `grep -c "test(" mailglass_admin/e2e/operator.spec.js` = 5 and `grep -c "test(" reference/demo_app/assets/e2e/demo.spec.js` = 3 are both represented)
    - The heading assertions "Deliveries", "Inbound records", and "Northstar Ops" each appear as inventoried rows with a ripple-risk annotation
    - The seed-reset / seed-count baseline (`/demo/evidence/reset` and seeded-evidence assertions) is recorded with a Phase 78 (SEED-02) ripple flag
    - PNG path references for the 390/768/1440 x light/dark x 3-surface matrix are listed under `tmp/ui-audit/`; the doc states the binaries stay gitignored (D-06)
    - `git diff --name-only` shows changes ONLY within `.planning/` — no spec file, no `mailglass_admin/lib/`, no `priv/static/` change
  </acceptance_criteria>
  <done>74-ASSERTION-INVENTORY.md inventories every e2e test heading/copy/testid and the seed-count baseline with file:line and ripple-risk, plus the gitignored PNG path references; no spec or production file changed.</done>
</task>

</tasks>

<threat_model>
Evidence-only docs plan. No new attack surface: no production code, no new routes/inputs/deps. The e2e specs are read-only.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02 | Information disclosure | Inventory could quote PII from seeded e2e fixtures | mitigate | Inventory quotes only aggregate/structural strings (headings, testids, copy contracts) — not raw recipient/subject PII; PNG binaries stay gitignored (D-06) |

No high-severity threats. No package installs — `T-74-SC` not applicable.
</threat_model>

<verification>
- `74-ASSERTION-INVENTORY.md` exists and references both spec files
- All 8 test() blocks (5 + 3) are represented
- Headings "Deliveries", "Inbound records", "Northstar Ops" inventoried with ripple risk
- Seed-count baseline + `/demo/evidence/reset` recorded with Phase 78 flag
- PNG path references present; binaries gitignored
- `git diff --name-only` stays within `.planning/`
</verification>

<success_criteria>
- A committed before-baseline assertion inventory exists that Phase 79 can diff against
- Every e2e heading and seed-count assertion later phases ripple is captured with file:line
- PNG path references recorded without committing binaries
- Zero production-code and zero spec-file changes
</success_criteria>

<output>
Create `.planning/phases/74-systematic-audit-and-ui-spec/74-02-SUMMARY.md` when done
</output>
