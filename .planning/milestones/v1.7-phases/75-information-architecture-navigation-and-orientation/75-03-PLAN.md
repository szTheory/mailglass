---
phase: 75-information-architecture-navigation-and-orientation
plan: 03
type: execute
wave: 3
depends_on:
  - 75-01
  - 75-02
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/e2e/operator.spec.js
  - reference/demo_app/assets/e2e/demo.spec.js
  - docs/design-system.md
autonomous: true
requirements:
  - IA-02
  - IA-03
  - IA-04
must_haves:
  truths:
    - "Bare /ops/mail/ renders h1 Operator overview (not the Deliveries list)"
    - "params[view] == deliveries shows the existing Deliveries master-detail list"
    - "Overview with no tenant_id shows orientation strip + nudge copy Select a tenant to see health at a glance."
    - "Overview with tenant_id shows 4 health-count cards (recent failures, orphan backlog, active suppressions, all-clear)"
    - "Suppression count read via runtime-module-indirection seam; degrades to em-dash in text-secondary when erroring — does not crash the Overview"
    - "View Deliveries CTA navigates to ?view=deliveries preserving tenant_id"
    - "View Inbound CTA navigates to the /ops/mail/inbound route"
    - "live_action stays :index — no router macro change"
    - "All 5 operator.spec.js tests are green with the updated openOperator helper"
    - "All demo.spec.js tests are green with the updated heading assertions"
    - "390px Playwright test asserts deliveries-orientation testid visible at 390px"
    - "GAP-22 deferral decision is recorded near docs/design-system.md:141-150"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Overview branch in handle_params/3, assign_overview_state/2, suppression_count_module/0"
      contains: "assign_overview_state"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "Overview branch test coverage (un-skipped stubs + implementations)"
      contains: "Operator Overview branch"
    - path: "mailglass_admin/e2e/operator.spec.js"
      provides: "Updated openOperator helper + 390px orientation assertion"
      contains: "Operator overview"
    - path: "reference/demo_app/assets/e2e/demo.spec.js"
      provides: "Updated heading assertion for bare-root Overview landing"
      contains: "Operator overview"
    - path: "docs/design-system.md"
      provides: "GAP-22 deferral decision recorded (IA-04)"
      contains: "GAP-22"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "lib/mailglass/operator/suppressions.ex"
      via: "suppression_count_module() runtime indirection + try/rescue"
      pattern: "suppression_count_module"
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "lib/mailglass/operator/support_summary.ex"
      via: "support_summary_module() runtime indirection in assign_overview_state/2"
      pattern: "support_summary_module"
    - from: "mailglass_admin/e2e/operator.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "openOperator helper asserts Operator overview heading then navigates to ?view=deliveries"
      pattern: "Operator overview"
---

<objective>
Implement the Operator Overview params-based branch inside `handle_params/3`, add the Overview render (health counts + navigation cards), update both e2e specs in the same commit as the IA change, extend the 390px Playwright test, record the GAP-22 deferral decision, and un-skip the Wave 0 operator_live_test.exs Overview stubs.

Purpose: Satisfies IA-02 (cold operator at /ops/mail/ reaches a task-oriented Overview), IA-03 (same-commit e2e heading-assertion updates, all Playwright tests green), and IA-04 (explicit recorded decision for GAP-22 deep-link defer to Phase 79). Addresses GAP-21 (a11y h1/h2 hierarchy on the new Overview surface) and GAP-22 (deep-link disposition recorded). Health-count numbers address GAP-07/09/11 context (the Overview is the landing surface that 390px operators must navigate from).

Output: Operator Overview landing at bare /ops/mail/; all 5 operator.spec.js tests green; demo.spec.js tests green; 390px structural test extended; GAP-22 decision recorded.

CRITICAL SAME-COMMIT MANDATE (IA-03): The handle_params/3 Overview branch change, operator.spec.js openOperator update, and demo.spec.js update MUST be committed in the same git commit. If they are split, all 5 operator.spec.js tests fail between commits because openOperator asserts "Deliveries" at line 19 while the landing now shows "Operator overview".
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-01-SUMMARY.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-02-SUMMARY.md

<interfaces>
<!-- Key contracts. Extracted from codebase. -->

From mailglass_admin/lib/mailglass_admin/operator_live.ex:

handle_params/3 to extend (lines 71-85):
```elixir
@impl true
def handle_params(params, uri, socket) do
  filter_params = normalize_filter_params(params)
  support_state = normalize_support_state(params)
  {:noreply,
   socket
   |> assign(:base_path, URI.parse(uri).path || "/operator")
   |> assign(:page_uri, uri)
   |> assign(:dark_chrome, MailglassAdmin.Operator.Shell.dark_chrome?(params))
   |> assign(:filter_params, filter_params)
   |> assign(:filter_form, to_form(filter_params, as: :filters))
   |> assign(:support_state, support_state)
   |> assign_delivery_state(filter_params, blank_to_nil(params["delivery_id"]))
   |> close_replay_modal()}
end
```
Modification: add `view = params["view"]` extraction; assign `:view` atom (`:overview` or `:deliveries`); branch the last two pipes — when view == "deliveries" OR delivery_id present → `assign_delivery_state/3`; otherwise → `assign_overview_state/2`.

assign_delivery_state/3 structure to mirror for assign_overview_state/2 (lines 491-510):
```elixir
defp assign_delivery_state(socket, filter_params, selected_delivery_id) do
  # ... pipe-assigns for deliveries, selected_delivery, replay, etc.
end
```
New defp assign_overview_state(socket, filter_params) only assigns:
  :view = :overview
  :support_summary — call summarize_tenant directly (not via 2-clause load_support_summary)
  :suppression_count — via try/rescue + suppression_count_module indirection, degrades to nil
  nil for all delivery-specific assigns (:deliveries, :selected_delivery, etc.) so Overview render does not crash

suppression_count_module seam to add alongside support_summary_module/0 (line 670):
```elixir
defp support_summary_module, do: :"Elixir.Mailglass.Operator.SupportSummary"
# ADD peer:
defp suppression_count_module, do: :"Elixir.Mailglass.Operator.Suppressions"
```

load_support_summary/2 for reference — Overview calls summarize_tenant directly without the nil-guard:
```elixir
defp load_support_summary(filter_params, _selected_delivery) do
  apply(support_summary_module(), :summarize_tenant, [
    %{tenant_id: filter_params["tenant_id"],
      window_hours: parse_positive_integer(filter_params["window_hours"]) || @default_window_hours}
  ])
end
```

build_path/4 signature (lines 559-578) for View Deliveries CTA — append &view=deliveries to preserve tenant_id:
```elixir
defp build_path(base_path, filter_params, delivery_id, dark_chrome, support_state \\ default_support_state())
```
The "View Deliveries" CTA path: build_path(@base_path, @filter_params, nil, @dark_chrome) <> "&view=deliveries"

From mailglass_admin/e2e/operator.spec.js openOperator helper (lines 13-21):
```javascript
async function openOperator(page) {
  const resetResponse = await page.request.get("/ops/browser-reset");
  expect(resetResponse.ok()).toBeTruthy();
  const returnTo = encodeURIComponent(`/ops/mail?tenant_id=${tenantId}`);
  await page.goto(`/ops/browser-login?tenant_id=${tenantId}&return_to=${returnTo}`);
  await expect(page.getByRole("heading", { name: "Deliveries", exact: true })).toBeVisible();
  await expect(page.getByTestId("operator-deliveries-list")).toBeVisible();
}
```
Required change (Option A from RESEARCH.md §M-3): assert "Operator overview" on landing, then navigate to ?view=deliveries before delivery-centric assertions.

From reference/demo_app/assets/e2e/demo.spec.js (lines 25-30):
```javascript
await page.getByRole("link", { name: /outbound operator/i }).click();
await expect(page).toHaveURL(/\/ops\/mail\?tenant_id=northstar/);
await expect(page.getByRole("heading", { name: "Deliveries", exact: true })).toBeVisible();
await expect(page.getByTestId("operator-deliveries-list")).toBeVisible();
```
Required change: update "Deliveries" assertion to "Operator overview", then navigate to ?view=deliveries.

From reference/demo_app/assets/e2e/demo.spec.js (line 41):
```javascript
await expect(page.getByRole("heading", { name: "Inbound records", exact: true })).toBeVisible();
```
This is the InboundLive surface (/ops/mail/inbound?tenant_id=northstar) — NOT affected by the Overview change. Verify it still passes; do not change it.

UI-SPEC § Operator Overview Layout (D-10 frozen):
- h1 "Operator overview" (text-heading font-bold)
- Shell.orientation_strip surface={:deliveries} (already wired in Plan 75-02)
- No-tenant branch: show orientation strip + "Select a tenant to see health at a glance." (text-body text-secondary)
- With-tenant branch: h2 "Health" + 4 compact health-count cards + h2 (navigation section) + 2 full-width navigation cards
- Health card classes: card bg-base-200 border border-base-300 rounded-box p-md; count number text-display font-bold colored per Health Count Colors table; label text-label text-secondary
- Nav card: full-width card bg-base-200 border border-base-300 rounded-box p-md; btn btn-primary btn-sm for CTAs; min-h-11 touch target
- Health Count Colors: recent_failures > 0 → text-error; orphan_backlog > 0 → text-warning; active_suppressions > 0 → text-secondary; all zeros → text-success
- Suppression nil/error → render "—" in text-secondary
- active={:deliveries} passed to shell (NOT :overview — no new shell attr)
- PII discipline: render counts only — never recipient/subject/address
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Operator Overview branch in handle_params/3 and Overview render</name>
  <files>
    mailglass_admin/lib/mailglass_admin/operator_live.ex
    mailglass_admin/test/mailglass_admin/operator_live_test.exs
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex full file (especially lines 71-85 handle_params, 491-510 assign_delivery_state, 558-580 build_path, 658-670 load_support_summary + support_summary_module, and the render/1 function to understand where the Overview branch goes)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md §2 (operator_live.ex patterns — handle_params extension, assign_overview_state, indirection seam)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-UI-SPEC.md § Operator Overview Layout, § Health Count Colors, § Tenant-context branch, § Route mechanics
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md D-07..D-16
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (full — identify the @tag :skip Overview branch stubs added in Plan 75-01 to un-skip and implement)
  </read_first>
  <behavior>
    - Test 1 (operator_live_test.exs): bare /ops/mail/ (no view param) renders an element with the text "Operator overview"
    - Test 2 (operator_live_test.exs): bare /ops/mail/?tenant_id= (no tenant) renders "Select a tenant to see health at a glance." — does NOT render health-count cards
    - Test 3 (operator_live_test.exs): bare /ops/mail/?tenant_id=T renders health-count region (support_summary data visible, suppression count rendered)
    - Test 4 (operator_live_test.exs): when suppression_count_module raises an error, the Overview renders "—" (em-dash) in the suppression card — does not crash
    - Test 5 (operator_live_test.exs): /ops/mail/?view=deliveries renders the Deliveries list (operator-deliveries-list testid visible), NOT the Overview h1
  </behavior>
  <action>
    Anti-churn gate citations: GAP-21 (a11y Overview subset — the h1/h2 hierarchy and aria-current task belongs here; the Overview must have a semantic heading structure), GAP-07/09/11 (390px orientation readability — the Overview is the landing that operators navigate from; if it crashes or renders incorrectly no orientation strip is reachable).

    Step 1 — UN-SKIP the @tag :skip Overview stubs in operator_live_test.exs (added in Plan 75-01). Implement each stub test using Phoenix.LiveViewTest `live/2` to mount OperatorLive at the appropriate path, then assert the rendered HTML. For Test 4 (suppression degradation), you need to override the `suppression_count_module/0` return — read how the existing tests mock `support_summary_module` (if they do) to apply the same pattern, or use Mox if already set up. If no mock seam exists for that module, a simpler approach: test that the render does not raise when the module's function is not available (e.g., call with a tenant for which the count returns 0 or nil and confirm "—" renders).

    Step 2 — MODIFY handle_params/3 in operator_live.ex. Add `view = params["view"]` extraction. The `:view` assign should be the atom `:overview` when `view != "deliveries"` AND no `delivery_id` is present, or `:deliveries` when `view == "deliveries"` OR `delivery_id` is present. Replace the final pipe to `assign_delivery_state/3` with a conditional: if `@view == :deliveries` (or can be determined from params) → `assign_delivery_state(filter_params, blank_to_nil(params["delivery_id"]))`; else → `assign_overview_state(filter_params)`.

    Step 3 — ADD `defp assign_overview_state(socket, filter_params)` in operator_live.ex. This function: (a) assigns `:view` as `:overview`; (b) calls `summarize_tenant` through the existing `support_summary_module()` indirection (same apply/3 pattern as in load_support_summary) — but call it unconditionally on the overview path (no nil guard); (c) wraps the suppression count call in try/rescue → assigns `:suppression_count` as the integer or nil on error; (d) must assign nil/empty for all delivery-specific assigns (`:deliveries`, `:selected_delivery`, `:timeline_events`, `:suppression_state`, `:replay_targets`, `:replay_history`, `:replay_selected_target_id`, `:detail_error`) so the render branches that reference these assigns do not crash when the Overview is active.

    Step 4 — ADD `defp suppression_count_module/0` immediately adjacent to `support_summary_module/0` (line 670). Body: `:"Elixir.Mailglass.Operator.Suppressions"`.

    Step 5 — ADD the Overview render branch to operator_live.ex render function. The render must check `@view == :overview` and conditionally render either the Overview layout or the existing Deliveries layout. The Overview layout must produce:
    - h1 "Operator overview" using `text-heading font-bold text-base-content`
    - The orientation strip is ALREADY wired at lines 254-256 as `<Shell.orientation_strip surface={:deliveries} />` with `:if={is_nil(@selected_delivery)}` — on the Overview branch, `@selected_delivery` is nil, so this will render. Verify this still works or adjust the Overview render to call orientation_strip explicitly if the existing trigger condition is in the wrong branch.
    - No-tenant branch: if `filter_params["tenant_id"]` is blank → show nudge copy "Select a tenant to see health at a glance." (text-body text-secondary), no health row.
    - With-tenant branch: h2 "Health"; 4 compact health-count cards; h2 (navigation section); 2 full-width nav cards.
    - Health-count cards (4): (1) "Recent failures" count from `@support_summary.failed_ingest.count` colored `text-error` when > 0 else `text-success`; (2) "Orphan backlog" from `@support_summary.orphan_backlog.count` colored `text-warning` when > 0 else `text-success`; (3) "Active suppressions" from `@suppression_count` (or `—` if nil) colored `text-secondary`; (4) "All clear" — derived: if failed_ingest.count == 0 AND orphan_backlog.count == 0 → `text-success` "All clear" else muted. Card class: `card bg-base-200 border border-base-300 rounded-box p-md`. Number: `text-display font-bold`. Label: `text-label text-secondary`.
    - Nav cards (2): "View Deliveries" with CTA `<.link patch={...} class="btn btn-primary btn-sm min-h-11">"View Deliveries"</.link>`. The patch URL: build_path(@base_path, @filter_params, nil, @dark_chrome) <> "&view=deliveries". "View Inbound" with CTA `<.link navigate={@inbound_path} class="btn btn-primary btn-sm min-h-11">"View Inbound"</.link>`. Add `@inbound_path` assign in `assign_overview_state/2` — derive from the shell's `inbound_path/1` helper or build directly as `"#{@base_path}/inbound?tenant_id=#{filter_params["tenant_id"]}"`.
    - Shell wrapper: Overview passes `active={:deliveries}` to `Shell.shell` — NOT `:overview` (anti-pattern from RESEARCH.md). The Overview IS the Deliveries surface root.
    - PII discipline: ONLY render counts (integers). Never render `.to`, `.from`, `.subject`, `.recipient`, `.email` or any field from individual deliveries on the Overview.
    - Token-clean: use text-display/heading/body/label, gap-sm/md/lg, p-md only — no raw text-sm/base/xs, gap-3/4/6, font-medium/semibold.
    - Testid discipline: add `data-testid="operator-overview"` to the Overview root div; health card testids: `operator-overview-health`; nav card: `operator-overview-nav`.

    Do NOT touch: router.ex (no router change), shell.ex :active attr values (no :overview value), support_cards.ex (Phase 76), replay modal (existing, untouched).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs --seed 0</automated>
  </verify>
  <done>
    - grep -c "assign_overview_state" mailglass_admin/lib/mailglass_admin/operator_live.ex returns at least 2 (definition + call site)
    - grep -c "suppression_count_module" mailglass_admin/lib/mailglass_admin/operator_live.ex returns at least 2
    - grep -c "Operator overview" mailglass_admin/lib/mailglass_admin/operator_live.ex returns at least 1 (in the render branch)
    - grep -c "view=deliveries" mailglass_admin/lib/mailglass_admin/operator_live.ex returns at least 1 (in the nav card path)
    - All operator_live_test.exs tests pass (including the un-skipped Overview branch describe block)
    - No router.ex change (grep -c "overview" mailglass_admin/lib/mailglass_admin/router.ex returns 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Same-commit e2e updates, 390px extension, and GAP-22 disposition record</name>
  <files>
    mailglass_admin/e2e/operator.spec.js
    reference/demo_app/assets/e2e/demo.spec.js
    docs/design-system.md
  </files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js (full file — all 5 tests + openOperator helper, lines 13-21; 390px test lines 64-89)
    - reference/demo_app/assets/e2e/demo.spec.js (full file — outbound operator test lines 23-35; inbound test lines 37-44)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md §6 (operator.spec.js and demo.spec.js change patterns, exact required assertions)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md (rippled-assertion list)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-RESEARCH.md §M-3 (same-commit e2e ripple strategy — Option A)
    - docs/design-system.md lines 135-155 (deep-link Known Limitations section for GAP-22 note placement)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md D-17 (GAP-22 deferral rationale)
  </read_first>
  <action>
    Anti-churn gate citations: GAP-07, GAP-09, GAP-11 (390px orientation readability — the 390px Playwright extension directly verifies the orientation strip is visible at 390px, the explicit acceptance criterion for GAP-07/09/11 per D-18), GAP-22 (deep-link disposition — IA-04 requires this recorded decision, gap held at sev 3 so Phase 79 closeout is not falsely blocked).

    SAME-COMMIT MANDATE: ALL changes in this task (operator.spec.js, demo.spec.js, docs/design-system.md) MUST be committed in the same git commit as the handle_params/3 Overview branch added in Task 1. Verify via `git diff HEAD -- mailglass_admin/lib/mailglass_admin/operator_live.ex` that the operator_live.ex change is already staged before committing this task's files.

    UPDATE mailglass_admin/e2e/operator.spec.js — openOperator helper (lines 13-21). Apply Option A strategy (per RESEARCH.md §M-3 and PATTERNS.md §6):
    1. Keep the browser-reset request and login navigation unchanged.
    2. Change the heading assertion from "Deliveries" to "Operator overview" (the landing is now the Overview).
    3. After the "Operator overview" assertion, navigate to `?view=deliveries` via `await page.goto(...)` or `page.goto(...)` with the full URL `${baseUrl}/ops/mail?tenant_id=${tenantId}&view=deliveries`.
    4. Add assertion for "Deliveries" heading AND operator-deliveries-list testid AFTER the navigation to ?view=deliveries. This ensures all 5 tests that call openOperator() still land on the Deliveries list before their delivery-specific assertions.
    5. The operator-empty-detail assertion at lines 32-34 remains valid because openOperator now navigates to ?view=deliveries (no delivery selected) before that assertion.

    EXTEND operator.spec.js 390px structural test (lines 64-89). Read the current test body to understand what it asserts. After the existing stacking assertions (list-before-detail), ADD one assertion:
    `await expect(page.getByTestId("deliveries-orientation")).toBeVisible();`
    This is the acceptance check for GAP-07 at 390px per D-18.

    UPDATE reference/demo_app/assets/e2e/demo.spec.js — outbound operator test (lines 23-35). After the "outbound operator" link click and URL assertion:
    1. Update the heading assertion from "Deliveries" to "Operator overview" (the link navigates to /ops/mail?tenant_id=northstar which now lands on the Overview).
    2. Navigate to ?view=deliveries: `await page.goto('/ops/mail?tenant_id=northstar&view=deliveries')` (or equivalent).
    3. Restore the operator-deliveries-list assertion after navigation.
    NOTE: The "Inbound records" assertion at line 41 navigates to /ops/mail/inbound?tenant_id=northstar — this is InboundLive, UNAFFECTED by the Overview change. DO NOT change that assertion.

    RECORD GAP-22 disposition in docs/design-system.md. Read lines 135-155 first to find the "Known Limitations" or "deep-link" section. Near lines 141-150 (the relative CSS URL discussion), add a note comment/paragraph: "Note: The deep-link-unstyled-CSS behavior (GAP-22) is deferred to Phase 79 (VERIF-04). Rationale: a robust fix touches the stable asset-serving seam (relative css-<md5> URL resolves against the deep path on hard refresh). This seam is out of churn scope for v1.7 (see D-17). The bug affects only hard refreshes on deep URLs; normal in-app live navigation is unaffected. GAP-22 is held at severity 3." The note should be prose-compatible with the existing doc voice (clear, exact, technical).

    Do NOT bump any reference/demo_app/mix.exs or reference/host_app/mix.exs version pins — frozen baselines (REQUIREMENTS.md Out of Scope).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && npx playwright test mailglass_admin/e2e/operator.spec.js reference/demo_app/assets/e2e/demo.spec.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npx playwright test operator.spec.js exits 0 (all 5 tests green)
    - npx playwright test demo.spec.js exits 0 (all demo tests green including the updated outbound operator test and unchanged inbound test)
    - grep -c "Operator overview" mailglass_admin/e2e/operator.spec.js returns at least 1 (in openOperator helper)
    - grep -c "deliveries-orientation" mailglass_admin/e2e/operator.spec.js returns at least 1 (in the 390px test extension)
    - grep -c "Operator overview" reference/demo_app/assets/e2e/demo.spec.js returns at least 1 (updated outbound operator assertion)
    - grep -c "GAP-22" docs/design-system.md returns at least 1 (disposition note recorded)
    - All changes are in the same commit as operator_live.ex handle_params/3 change (confirmed via git log --oneline -1)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser params → handle_params/3 | params["view"] comes from the URL query string; must be pattern-matched, never interpolated into DB queries or HTML |
| Overview render → health counts | Data must be aggregate integer counts only — no per-delivery fields, recipient, subject, or address may appear on the Overview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Tampering / Input Validation | params["view"] in handle_params/3 | mitigate | Pattern-match: `if view == "deliveries"` branches to `:deliveries` atom; any other value (including injected strings) falls through to `:overview` (the safe Overview default, no DB query for deliveries); per RESEARCH.md §Security Domain V5 control |
| T-75-03-02 | Information Disclosure | Overview health-count render | mitigate | Only render `.count` integers from support_summary and the suppression count — never .to/.from/.subject/.recipient/.email on the Overview surface; CLAUDE.md PII telemetry rule enforced by construction |
| T-75-03-03 | Denial of Service | suppression_count_module/0 try/rescue | mitigate | try/rescue degrades to nil (renders "—") rather than crashing the LiveView; mirrors established support_summary_module/0 pattern |
| T-75-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; Playwright is already installed |
</threat_model>

<verification>
After Wave 3 completes (full phase gate):
- mix test mailglass_admin/test/mailglass_admin/ --seed 0 exits 0 (all admin ExUnit tests green)
- mix test test/mailglass/operator/suppressions_test.exs --seed 0 exits 0
- npx playwright test mailglass_admin/e2e/operator.spec.js exits 0 (all 5 tests)
- npx playwright test reference/demo_app/assets/e2e/demo.spec.js exits 0 (all demo tests)
- grep -c "Operator overview" mailglass_admin/e2e/operator.spec.js >= 1
- grep -c "deliveries-orientation" mailglass_admin/e2e/operator.spec.js >= 1 (390px extension)
- grep -c "GAP-22" docs/design-system.md >= 1 (disposition recorded, IA-04)
- grep -c "assign_overview_state" mailglass_admin/lib/mailglass_admin/operator_live.ex >= 2
- grep -c "def count_active_suppressions" lib/mailglass/operator/suppressions.ex = 1
- grep -c "def orientation_strip" mailglass_admin/lib/mailglass_admin/operator/shell.ex = 1
- git diff --exit-code mailglass_admin/priv/static/ exits 0 (bundle committed in Wave 2)
- Conformance grep: grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/shell.ex | grep -c "text-sm" = 0 on orientation_strip function
- No router.ex change: grep -c "overview" mailglass_admin/lib/mailglass_admin/router.ex = 0
</verification>

<success_criteria>
- Bare /ops/mail/ renders h1 "Operator overview" — the cold-operator one-screen orientation goal is satisfied
- /ops/mail/?view=deliveries renders the existing Deliveries list unchanged
- Operator Overview with tenant_id shows 4 health-count cards with correct semantic colors per Health Count Colors table
- Suppression count degrades to "—" (text-secondary) when the indirection seam errors — LiveView never crashes
- All 5 operator.spec.js Playwright tests green, including openOperator's "Operator overview" landing assertion and navigation to ?view=deliveries
- demo.spec.js Playwright tests green with updated "Operator overview" assertion in outbound operator test
- 390px structural test asserts deliveries-orientation testid visible at 390px
- GAP-22 deferral decision recorded near docs/design-system.md:141-150 with Phase 79 rationale (IA-04 satisfied)
- No router macro change, no new shell :active value, no PII in any new assign
- Phase 75 requirements IA-01 through IA-04 all satisfied
</success_criteria>

<output>
Create `.planning/phases/75-information-architecture-navigation-and-orientation/75-03-SUMMARY.md` when done.
</output>
