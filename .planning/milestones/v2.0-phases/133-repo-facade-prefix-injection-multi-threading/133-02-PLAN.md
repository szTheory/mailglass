---
phase: 133-repo-facade-prefix-injection-multi-threading
plan: 02
type: execute
wave: 2
depends_on:
  - 133-01
files_modified:
  - test/mailglass/schema_isolation_integration_test.exs
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FACADE-03
  - FACADE-04
user_setup: []

must_haves:
  truths:
    - "A dedicated schema-isolation integration test creates a mailglass schema, migrates it (prefixed), round-trips an insert/read through the facade + Multi path, and asserts rows land under mailglass.* while public holds none of the mailglass tables' rows"
    - "Operator read-model reads (SupportSummary orphan-count + a list read) resolve under the prefix in the integration test"
    - "Tenancy.scope/2 composes with the schema prefix (WHERE + schema both apply) in the integration test"
    - "The admin dashboard-render test boots against a schema-isolated DB and renders a written record with ZERO mailglass_admin code changes (write→read round-trip, so a facade-bypassing write surfaces as an empty dashboard)"
    - "A verification asserts no lib/ runtime call-site bypasses the facade with a direct host-repo call (D-08 stays true)"
    - "ROADMAP Phase 133 success criterion (4) and REQUIREMENTS FACADE-04 are reworded to move the full-core-suite-green-under-both-schemas clause to Phase 134"
  artifacts:
    - "test/mailglass/schema_isolation_integration_test.exs — the new FACADE-04 integration test module"
    - "mailglass_admin/test/mailglass_admin/operator_live_test.exs — one render test adapted to the schema-isolated DB (test-only change; no lib/ change)"
    - "Updated ROADMAP.md + REQUIREMENTS.md wording (D-06 split recorded)"
  key_links:
    - "The FACADE-04 test's setup must CREATE SCHEMA + run the prefixed migration BEFORE the SQL Sandbox owner starts (footgun 13)"
    - "The wrapper migration uses SET LOCAL search_path to bind v01's unqualified immutability trigger to the prefix (Phase 134 fixes the raw DDL; here a search-path pin stands the schema up)"
    - "This plan depends on Plan 01 having threaded prefix through the facade + Multi builders"
  prohibitions:
    - "No mailglass_admin/lib/ code changes (FACADE-03 is a ZERO-admin-code-change proof)"
    - "Do NOT bump admin's == <core> pin (that is Phase 137)"
    - "Do NOT attempt to turn the whole core suite green under schema:\"mailglass\" (that is Phase 134 — the immutability trigger is unqualified until then)"
    - "Do NOT build CREATE SCHEMA / DROP SCHEMA into the production migration entrypoint (Phase 134)"
---

<objective>
Prove the Plan-01 facade threading end-to-end and record the roadmap split. This is the
VERIFICATION + DOCS half of Design Phase B (FACADE-03, FACADE-04).

Purpose: Turn "the facade injects a prefix" from a unit-level claim into an observable,
schema-isolated round-trip — rows physically land under `mailglass.*`, `public` stays
clean, admin renders unchanged, and no call-site bypasses the facade. Per D-06, the full
`schema: [public, mailglass]` CI matrix axis is DEFERRED to Phase 134 (the immutability
trigger is created unqualified until Phase C); this phase ships a dedicated integration
test whose own harness stands the schema up with a search-path pin.

Output: a new schema-isolation integration test; an adapted admin render test; a facade-
bypass verification; and reworded ROADMAP/REQUIREMENTS recording the D-06 split.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/133-repo-facade-prefix-injection-multi-threading/133-CONTEXT.md
@.planning/phases/133-repo-facade-prefix-injection-multi-threading/133-PATTERNS.md

# The near-copy analog harness for the new integration test
@test/mailglass/shipped_migration_divergence_test.exs
@test/support/data_case.ex
@test/test_helper.exs

# Facade + Multi paths the test exercises (threaded in Plan 01)
@lib/mailglass/repo.ex
@lib/mailglass/events.ex
@lib/mailglass/outbound.ex
@lib/mailglass/operator/support_summary.ex

# The admin render test to adapt + its harness
@mailglass_admin/test/mailglass_admin/operator_live_test.exs
@mailglass_admin/test/support/live_view_case.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dedicated schema-isolation integration test (FACADE-04)</name>
  <files>test/mailglass/schema_isolation_integration_test.exs</files>
  <read_first>
    - test/mailglass/shipped_migration_divergence_test.exs (l.22-85 — the near-copy harness: inline wrapper migration, CREATE SCHEMA, Sandbox :auto, prefixed Ecto.Migrator, on_exit cleanup, raw-count assertions)
    - 133-PATTERNS.md "NEW schema-isolation integration test" section (full harness + the 5 assertions + the SET LOCAL search_path rationale)
    - lib/mailglass/events.ex (append/1 + append_multi/3 — the write path to exercise)
    - lib/mailglass/operator/support_summary.ex (orphan-count read under prefix)
    - test/support/data_case.ex (Sandbox setup ordering — footgun 13)
  </read_first>
  <action>
    Implement D-06 / FACADE-04. Copy the harness from shipped_migration_divergence_test.exs
    wholesale and change what it ASSERTS.

    Structure (per 133-PATTERNS.md):
    - `use ExUnit.Case, async: false` (DDL can't run inside the Sandbox tx wrapper).
    - An inline PrefixedWrapperMigration whose up/down issue `execute("SET LOCAL search_path TO mailglass, public")` then delegate to Mailglass.Migration.up/down(prefix: "mailglass", repo: Mailglass.TestRepo). The search_path line is load-bearing: v01's immutability trigger/function are created unqualified (Phase 134 fixes this), so the pin binds that DDL to mailglass.mailglass_events. Add a comment stating this is why D-06 defers the full-suite axis to Phase 134.
    - setup: flip Sandbox to :auto; DROP SCHEMA IF EXISTS mailglass CASCADE; CREATE SCHEMA mailglass (BEFORE the Sandbox owner starts — footgun 13); run the prefixed migration via Ecto.Migrator with a unique monotonic version; on_exit drops the schema, deletes the schema_migrations row, and restores Sandbox :manual.

    Assertions (the 5 from 133-PATTERNS.md / CONTEXT `<specifics>`):
    1. Insert a Delivery + Events through the facade / Multi path (Mailglass.Events.append/1 and/or the Outbound Multi path) — round-trips into mailglass.mailglass_*.
    2. Raw `SELECT COUNT(*) FROM public.mailglass_events` (and deliveries/suppressions where those tables exist in public) == 0 — public holds none of the mailglass rows.
    3. Raw `SELECT COUNT(*) FROM mailglass.mailglass_events` > 0 — rows land under the prefix.
    4. Operator reads resolve under the prefix: Operator.SupportSummary orphan-count + one Operator.* list read return the written rows (proves FACADE-01 reads + the Plan-01 put_query_prefix/2 subquery).
    5. Tenancy.scope/2 still composes — a scoped read returns only the tenant's rows AND executes under the mailglass schema (WHERE + schema both apply).

    Discretion (CONTEXT): schema creation in the module's own setup (recommended, mirrors the analog with Sandbox :auto) vs. test_helper.exs suite setup; test module/file name. Use test/mailglass/schema_isolation_integration_test.exs.
  </action>
  <verify>
    <automated>mix test test/mailglass/schema_isolation_integration_test.exs</automated>
  </verify>
  <done>
    The test creates + migrates the mailglass schema, round-trips insert/read through the facade + Multi path, asserts mailglass.* holds the rows while public holds none, asserts operator reads + orphan-count resolve under the prefix, and asserts Tenancy.scope/2 composes — all green.
  </done>
  <acceptance_criteria>
    - `test/mailglass/schema_isolation_integration_test.exs` exists
    - `grep -c 'SET LOCAL search_path' test/mailglass/schema_isolation_integration_test.exs` >= 1
    - `grep -c 'public.mailglass_events' test/mailglass/schema_isolation_integration_test.exs` >= 1
    - `mix test test/mailglass/schema_isolation_integration_test.exs` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Admin zero-code-change render proof (FACADE-03) + facade-bypass verification (D-08)</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (existing render test at l.214-288 + helpers insert_delivery!/1 l.1056, insert_event!/2 l.1076)
    - mailglass_admin/test/support/live_view_case.ex (Sandbox owner setup l.34, tenant stamp l.38 — mirror the core test's CREATE SCHEMA + prefixed migrate BEFORE the owner)
    - 133-PATTERNS.md "operator_live_test.exs" section
    - test/mailglass/schema_isolation_integration_test.exs (Task 1 — mirror its setup ordering)
  </read_first>
  <action>
    Implement D-07 (FACADE-03) + D-08 verification. NO mailglass_admin/lib/ changes — this is a
    ZERO-admin-code-change proof; only the test file is touched.

    FACADE-03: adapt ONE dashboard-render test in operator_live_test.exs so it boots against the
    schema-isolated DB (mirror the core integration test's setup: CREATE SCHEMA + prefixed migrate
    BEFORE the Sandbox owner starts — footgun 13). Make it a
    write→read→render round-trip: WRITE a Delivery + Event via the existing insert_delivery!/insert_event!

    **Sandbox-owner-ordering (resolve the harness tension — DO THIS explicitly):**
    `live_view_case.ex` starts the Sandbox owner in *shared* mode inside its OWN `setup` (l.34), but
    the CREATE SCHEMA + prefixed-migrate DDL needs Sandbox `:auto` and must run BEFORE any per-test
    owner starts (footgun 13). These are in tension, so do NOT just "reuse the live_view_case setup
    as-is." Stand the schema up in a `setup_all` (or a module-level bootstrap that runs before the
    per-test owner): flip to `:auto`, DROP/CREATE SCHEMA mailglass, run the prefixed migration once
    for the whole module, then let the inherited per-test shared owner start normally on top of the
    already-existing schema (the shared owner does not need `:auto` — only the one-time DDL does). If
    `setup_all` cannot cleanly interleave with the `live_view_case` `setup` owner-start ordering, write
    a bespoke module setup that supersedes the shared one and reproduces the tenant-stamp (l.38) +
    owner-start it provides — do not silently inherit an ordering that starts the owner before the
    schema exists. State the chosen mechanism in the test so the ordering is legible.
    helpers (which go through the facade), then live-mount the operator path and assert the rendered HTML
    contains the written delivery's recipient. Because the write and the render both route through the
    prefixed schema, a hidden facade-bypassing write (one landing in public) would surface as an EMPTY
    dashboard — that is the proof. Do NOT bump admin's == <core> pin (Phase 137). Do NOT edit any
    mailglass_admin/lib/ file.

    D-08 verification: add a lib/-bypass assertion so the "every runtime read/write routes through
    Mailglass.Repo" invariant stays true. The cleanest home is a focused test (in the core integration
    test module or a small dedicated test) that greps lib/mailglass for direct host-repo call sites
    bypassing the facade. D-08 already verified the only direct-host-repo hits are in migration.ex
    (resolve_repo().__adapter__() / Application.get_env(:mailglass, :repo)) — correctly out of scope
    (Phase C). Encode that as an allowlist: assert no NEW direct Application.get_env(:mailglass, :repo)
    or resolve_repo().<crud> call appears in lib/mailglass/ outside migration.ex. Keep the assertion
    literal-free of any grep-tripping token in comments (comment-text discipline). If a plainer home is
    warranted, place this assertion in the schema_isolation_integration_test.exs from Task 1 instead.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs</automated>
  </verify>
  <done>
    The adapted admin render test boots against the schema-isolated DB and renders a written record with zero mailglass_admin/lib/ changes; the D-08 bypass assertion passes (no new facade-bypassing lib/ call-site).
  </done>
  <acceptance_criteria>
    - `git status --porcelain mailglass_admin/lib/` prints nothing (no admin lib changes)
    - the adapted test performs a write→read→render round-trip (recipient assertion present)
    - `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Record the D-06 roadmap/requirements split (docs)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 133 Success Criteria l.120-136, especially criterion (4) l.134-136)
    - .planning/REQUIREMENTS.md (FACADE-04 l.38-41)
    - 133-CONTEXT.md D-06 (the exact split wording)
  </read_first>
  <action>
    Implement the docs half of D-06 — record the split so it is not silently dropped.

    ROADMAP.md Phase 133 success criterion (4): reword so it covers ONLY the dedicated integration
    test (creates schema, migrates, round-trips insert/read, asserts mailglass tables under mailglass.*
    while public holds none). MOVE the "and the full core suite runs green under BOTH schema:\"public\"
    and schema:\"mailglass\" (new CI matrix axis)" clause OUT of Phase 133 and into Phase 134's success
    criteria (the CI matrix axis lands with the raw-DDL trigger/function qualification). Also update the
    one-line Phase 133 checklist entry (l.42) the same way. Use scoped Edit — do NOT rewrite the whole file.

    REQUIREMENTS.md FACADE-04 (l.38-41): reword identically — FACADE-04 is satisfied by the dedicated
    integration test; the "full core suite green under BOTH ... (new CI matrix axis)" clause moves to a
    Phase-134 MIGR requirement (add it to the appropriate MIGR requirement's acceptance wording, or note
    it as deferred-to-134 in FACADE-04 with a pointer). Keep FACADE-04's ID and Phase-133 ownership for
    the integration-test portion; the CI-matrix portion becomes Phase 134's.

    Cross-reference D-06 in both edits so the split is traceable.
  </action>
  <verify>
    <automated>grep -c 'schema: "mailglass"' .planning/ROADMAP.md && grep -n 'CI matrix axis' .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    ROADMAP Phase 133 criterion (4) + the Phase 133 checklist line no longer claim full-suite-green-under-both-schemas (moved to Phase 134); REQUIREMENTS FACADE-04 reworded with the CI-matrix clause moved/deferred to Phase 134; both cite D-06.
  </done>
  <acceptance_criteria>
    - Phase 133's success criterion (4) in ROADMAP.md no longer contains "full core suite runs green under BOTH"
    - Phase 133's one-line checklist entry (ROADMAP.md l.42) also no longer claims cross-schema verification (its "Verified under both `public` and `mailglass` schemas (new CI matrix axis)" clause is removed/moved), so the split is fully recorded, not half-recorded
    - Phase 134's section (or a MIGR requirement) now owns the CI-matrix-axis clause
    - both edits reference D-06
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Postgres | The test issues raw CREATE SCHEMA / migration DDL against the test DB (test-only, not the production migration path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-04 | Tampering (test DDL) | schema_isolation_integration_test.exs CREATE/DROP SCHEMA | low | accept | DDL runs only against the isolated test DB with a hardcoded `mailglass` schema name (no interpolation of external input); on_exit drops it. No production surface. |
| T-133-05 | Information disclosure (false-green isolation proof) | the integration + admin round-trip assertions | low | mitigate | The write→read round-trip design ensures a facade-bypassing write (landing in public) surfaces as an empty result / empty dashboard rather than a silent pass — the test fails loudly rather than vacuously green. |
| T-133-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager installs in this phase — no new dependencies. Not applicable. |

**No high-severity threat introduced.** ASVS L1 block-on-`high` gate: PASS. The injected prefix
remains `Config.schema()` (boot-validated, static config, non-user-input) — the upstream Phase-132
mitigation covers identifier injection; this plan only observes and documents.
</threat_model>

<verification>
- `mix test test/mailglass/schema_isolation_integration_test.exs` green (FACADE-04).
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` green (FACADE-03).
- `git status --porcelain mailglass_admin/lib/` empty (zero admin lib changes).
- D-08 bypass assertion green (no new facade-bypassing lib/ call-site).
- ROADMAP + REQUIREMENTS reworded per D-06 (full-suite-under-both-schemas moved to Phase 134).
- `mix credo --strict` green (run it — do not grep).
</verification>

<success_criteria>
- FACADE-03: admin dashboard renders a written record against the schema-isolated DB with zero mailglass_admin/lib/ changes.
- FACADE-04: dedicated integration test creates + migrates the schema, round-trips insert/read, asserts mailglass.* holds the rows while public holds none, and operator reads + orphan-count + Tenancy.scope compose under the prefix.
- D-06 split recorded in ROADMAP + REQUIREMENTS; D-08 bypass invariant asserted.
</success_criteria>

<output>
Create `.planning/phases/133-repo-facade-prefix-injection-multi-threading/133-02-SUMMARY.md` when done.

## Artifacts this phase produces (MANDATORY — downstream drift verification must EXCLUDE these as intentional additions)
- `Mailglass.Repo.multi_opts/1` — new PUBLIC function (Plan 01)
- `Mailglass.Repo.put_prefix/1` — new PRIVATE helper (Plan 01)
- `Mailglass.Repo.aggregate/4` — the added `opts \\ []` arg on the previously 3-arity aggregate (Plan 01)
- `test/mailglass/schema_isolation_integration_test.exs` — the new FACADE-04 integration test module (Plan 02)
- The test-only adaptation of `mailglass_admin/test/mailglass_admin/operator_live_test.exs` (Plan 02; no lib change)
</output>
