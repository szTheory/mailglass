---
phase: 134-migration-entrypoint-raw-ddl-trigger-qualification
plan: 03
type: execute
wave: 3
depends_on: ["134-01", "134-02"]
files_modified:
  - credo_checks/no_schema_prefix_attribute.ex
  - test/mailglass/credo/no_schema_prefix_attribute_test.exs
  - .credo.exs
  - config/runtime.exs
  - .github/workflows/advisory-matrix.yml
autonomous: true
requirements: [MIGR-06]
must_haves:
  truths:
    - "A custom Credo check (Mailglass.Credo.NoSchemaPrefixAttribute) FAILS the build if any module under lib/mailglass/ declares @schema_prefix; it is registered in .credo.exs and has a matching regression test under test/mailglass/credo/. [D: dossier §1 decision 6, embedded MIGR-06] [SC-6]"
    - "The @schema_prefix guard passes on the current codebase (no mailglass schema module declares @schema_prefix) — proven by mix credo --strict green + the check's own test. [SC-6]"
    - "The 'Core Full Suite Advisory' job gains a schema: [public, mailglass] matrix axis; the run step exports the schema to the app env (MAILGLASS_SCHEMA) so Config.schema/0 returns the matrix value, and config/runtime.exs (test env) reads MAILGLASS_SCHEMA to override config :mailglass, :schema. [D-06, embedded Success Criterion 7] [SC-7]"
    - "The full core suite runs green under BOTH schema: public and schema: mailglass; the mailglass axis preserves the existing --exclude requires_workspace flag. The mailglass axis fails closed: under MAILGLASS_SCHEMA=mailglass a mailglass.* table (mailglass_events) PHYSICALLY exists post-boot, not merely Config.schema/0 returning the string, guarding against a boot-order no-op. [SC-7]"
  prohibitions:
    - id: no-schema-prefix-attribute
      must_not: "No mailglass schema module (or any module under lib/mailglass/) declares @schema_prefix — this inverts read-vs-write prefix precedence and is forbidden by decision 6."
      status: enforced
      verification: "Mailglass.Credo.NoSchemaPrefixAttribute fails the build on any @schema_prefix under lib/mailglass/; mix credo --strict is green on the current tree; the check's regression test asserts it fires on a fixture and passes on clean source."
  artifacts:
    - credo_checks/no_schema_prefix_attribute.ex
    - test/mailglass/credo/no_schema_prefix_attribute_test.exs
    - "Mailglass.Credo.NoSchemaPrefixAttribute (new check module)"
    - "new schema: [public, mailglass] matrix axis on Core Full Suite Advisory"
  key_links:
    - "credo_checks/no_schema_prefix_attribute.ex -> registered in .credo.exs extra_checks -> mix credo --strict"
    - "advisory-matrix.yml schema axis -> MAILGLASS_SCHEMA env -> config/runtime.exs (test) -> config :mailglass, :schema -> Config.schema/0 -> facade + migration prefix"
---

<objective>
Enforce the pure-runtime-prefix decision with a build-time guard, and stand the full
core suite up under both `public` and `mailglass` schemas in CI now that Plans 134-01/02
have made the `mailglass` schema physically installable and its raw DDL qualified.

Purpose: Close MIGR-06 (a Credo/grep guard that fails the build on any `@schema_prefix`
declaration — blocking the read-vs-write precedence inversion) and Success Criterion 7 /
D-06 (the CI matrix axis that runs the full core suite green under BOTH schemas). The
matrix axis is only passable because THIS phase qualified the raw DDL + issues CREATE
SCHEMA.

Output: `Mailglass.Credo.NoSchemaPrefixAttribute` + its test + `.credo.exs` registration;
a `MAILGLASS_SCHEMA` override in `config/runtime.exs` (test env); a
`schema: [public, mailglass]` axis on the Core Full Suite Advisory job.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@credo_checks/no_compile_env_outside_config.ex
@credo_checks/no_default_module_name_singleton.ex
@test/mailglass/credo/no_compile_env_outside_config_test.exs
@test/mailglass/credo/checks_have_tests_test.exs
@.credo.exs
@config/runtime.exs
@config/test.exs
@.github/workflows/advisory-matrix.yml
@lib/mailglass/config.ex
</context>

<artifacts>
## Artifacts this phase produces (Plan 134-03)

New/changed symbols:
- `Mailglass.Credo.NoSchemaPrefixAttribute` — NEW custom Credo check under credo_checks/
- `test/mailglass/credo/no_schema_prefix_attribute_test.exs` — NEW regression test (fires on fixture, clean on valid source)
- `.credo.exs` — new check registered in `extra_checks`
- `config/runtime.exs` — NEW test-env `MAILGLASS_SCHEMA` override of `config :mailglass, :schema`
- `.github/workflows/advisory-matrix.yml` — NEW `schema: [public, mailglass]` matrix axis on the Core Full Suite Advisory job(s), with the run step exporting the schema to the app env
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the NoSchemaPrefixAttribute Credo check + its regression test + register it (MIGR-06)</name>
  <files>credo_checks/no_schema_prefix_attribute.ex, test/mailglass/credo/no_schema_prefix_attribute_test.exs, .credo.exs</files>
  <read_first>
    - credo_checks/no_compile_env_outside_config.ex (structure: `use Credo.Check`, param_defaults with included_path_prefixes, run/2 gated on included_path?/2, Macro.traverse for AST walking, format_issue)
    - credo_checks/no_default_module_name_singleton.ex (analog module-attribute / AST detection check — closest analog for detecting a @attr declaration)
    - test/mailglass/credo/no_compile_env_outside_config_test.exs (test shape: setup_all starts :credo, run_check/2 via SourceFile.parse |> Check.run([]), asserts issue count + message; and the "ignores files outside path scope" case)
    - test/mailglass/credo/checks_have_tests_test.exs (META-GUARD: every credo_checks/*.ex MUST have test/mailglass/credo/<name>_test.exs AND be registered in .credo.exs — all three are mandatory or this meta-test fails)
    - .credo.exs lines 1-160 (extra_checks list — where to register; and the requires: ["./credo_checks/*.ex"] + included: ["lib/", ...] structure)
  </read_first>
  <behavior>
    - Given a source file under lib/mailglass/ containing `@schema_prefix "mailglass"` (or any @schema_prefix value) inside a defmodule, the check returns exactly one issue whose message names @schema_prefix and the pure-runtime-prefix rule.
    - Given a source file under lib/mailglass/ with NO @schema_prefix, the check returns [].
    - Given a file OUTSIDE the lib/mailglass/ path scope (e.g. test/support fixture) that declares @schema_prefix, the check returns [] (path-scoped like NoCompileEnvOutsideConfig).
  </behavior>
  <action>
    Create `credo_checks/no_schema_prefix_attribute.ex` defining
    `Mailglass.Credo.NoSchemaPrefixAttribute` with `use Credo.Check` (category: :warning,
    base_priority: :high) following the `NoCompileEnvOutsideConfig` structure. Param
    defaults: `included_path_prefixes: ["lib/mailglass/"]`. In `run/2`, gate on
    `included_path?/2`, then `Macro.traverse` (or `Macro.prewalk`) the AST looking for a
    module-attribute definition node for `@schema_prefix` — i.e. an AST node of the shape
    `{:@, meta, [{:schema_prefix, _, _}]}`. For each match, `format_issue` with a message
    like "`@schema_prefix` is forbidden: mailglass uses a runtime facade-injected prefix
    (Config.schema()); a compile-time @schema_prefix inverts read-vs-write precedence
    (decision 6)." and the node's line/column.

    Register the check in `.credo.exs` by adding
    `{Mailglass.Credo.NoSchemaPrefixAttribute, []}` to the `extra_checks` list (top of the
    file). The `requires: ["./credo_checks/*.ex"]` line already loads the file.

    Create `test/mailglass/credo/no_schema_prefix_attribute_test.exs` mirroring
    `no_compile_env_outside_config_test.exs`: `setup_all` starts `:credo`; a `run_check/2`
    helper parses a source string + filename and runs the check with `[]`. Three tests:
      1. flags `@schema_prefix "mailglass"` in a module at
         `lib/mailglass/outbound/bad_schema.ex` (asserts 1 issue, message mentions
         `@schema_prefix`).
      2. allows a module with no `@schema_prefix` at `lib/mailglass/outbound/ok.ex`
         (asserts []).
      3. ignores a `@schema_prefix` in a file outside path scope
         (`test/support/some_fixture.exs`, asserts []).

    Comment-text discipline: do NOT place, inside any lib/ source comment, a literal that
    a negative grep would trip on. (This check is AST-based, not grep-based, so there is no
    negative-grep gate on the literal — but keep the check's own module docstring free of a
    bare `@schema_prefix ...` assignment that could be mis-detected; reference it in prose.)
  </action>
  <verify>
    <automated>mix test test/mailglass/credo/no_schema_prefix_attribute_test.exs --warnings-as-errors</automated>
    <automated>mix test test/mailglass/credo/checks_have_tests_test.exs</automated>
    <automated>mix credo --strict</automated>
    <automated>mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `Mailglass.Credo.NoSchemaPrefixAttribute` exists under `credo_checks/`, is registered in `.credo.exs` extra_checks, and has `test/mailglass/credo/no_schema_prefix_attribute_test.exs` (satisfies the checks_have_tests meta-guard on BOTH dimensions).
    - The check flags `@schema_prefix` under `lib/mailglass/`, returns [] when absent, and is path-scoped (ignores files outside `lib/mailglass/`).
    - `mix credo --strict` is GREEN on the current tree (no existing mailglass module declares `@schema_prefix` — confirmed by 132-01/133 using runtime facade injection, not the attribute).
    - `mix test test/mailglass/credo/checks_have_tests_test.exs` green (the new check is registered + tested).
    - Compiles/formats clean.
  </acceptance_criteria>
  <done>A build-time guard fails on any @schema_prefix under lib/mailglass/, is wired into the strict ruleset, carries its own regression test, and is green on the current tree.</done>
</task>

<task type="auto">
  <name>Task 2: Read MAILGLASS_SCHEMA in config/runtime.exs (test env) to override the schema</name>
  <files>config/runtime.exs, config/test.exs</files>
  <read_first>
    - config/runtime.exs (currently only adopter-example comments; runs AFTER config/test.exs in the test env, so a runtime override wins over the test.exs "public" pin)
    - config/test.exs lines 11-15 (config :mailglass, :schema, "public" — the suite default that the runtime override must be able to flip to "mailglass" for the CI axis)
    - lib/mailglass/config.ex lines 667-676 (Config.schema/0 reads Application.get_env(:mailglass, :schema, "mailglass"))
  </read_first>
  <action>
    In `config/runtime.exs`, add a guarded block that, ONLY in the `:test` env and ONLY
    when `System.get_env("MAILGLASS_SCHEMA")` is set to a non-empty value, sets
    `config :mailglass, :schema, System.get_env("MAILGLASS_SCHEMA")`. Use
    `if config_env() == :test do ... end` so production/dev are never affected and the
    default suite (no env var) still gets the `config/test.exs` "public" pin. Add a short
    comment explaining this is the D-06 CI matrix-axis hook: the advisory-matrix job sets
    `MAILGLASS_SCHEMA=mailglass` to run the whole suite under the isolated schema.

    Validate that with the env var UNSET, the value resolves to "public" (test.exs pin,
    unchanged) and with `MAILGLASS_SCHEMA=mailglass` it resolves to "mailglass". Do not
    change config/test.exs's default "public" pin (the override layers on top of it at
    runtime).

    Note the interaction with test_helper.exs: it runs `Ecto.Migrator.run` which invokes
    `Mailglass.Migration.up/0` → `Config.schema()` (after Plan 134-01). So with
    MAILGLASS_SCHEMA=mailglass, the suite's migrations issue CREATE SCHEMA + migrate the
    tables into `mailglass`, and DataCase transactional tests resolve via the facade's
    injected prefix. No test_helper.exs change is required for this plan — but if the
    migration step needs the schema present before migrating, Plan 134-01's
    maybe_create_schema/1 (first action of up/1) already handles it. Confirm by running
    the suite locally under the env var (see verify).

    BOOT-ORDER FAIL-CLOSED PROOF (residual risk): config/runtime.exs is auto-evaluated by
    Mix and test_helper.exs migrates at boot — so a boot-order regression could migrate
    under "public" BEFORE the MAILGLASS_SCHEMA override applies, silently no-op'ing the axis.
    To make the axis fail closed, the acceptance requires a test asserting a `mailglass.*`
    table PHYSICALLY EXISTS post-boot under MAILGLASS_SCHEMA=mailglass — not merely that
    `Config.schema/0` returns the string "mailglass". Reuse the `table_exists?` /
    schema-aware `information_schema.tables` query helper pattern from
    test/mailglass/migration_test.exs (lines 176-184), but parameterize on
    `table_schema = 'mailglass'` (the existing helper hardcodes `'public'`), asserting
    `mailglass.mailglass_events` exists. If migration_test.exs already gains a
    schema-parameterized existence assertion under 134-01/134-02, assert against that; if
    not, add a targeted assertion (in an existing schema-isolation test, or a minimal case
    file) that under MAILGLASS_SCHEMA=mailglass the append-only `mailglass_events` table is
    present in the `mailglass` schema. A string-only `Config.schema()=="mailglass"` check is
    NOT sufficient for acceptance — the physical-table assertion is the fail-closed guard.
  </action>
  <verify>
    <automated>MAILGLASS_SCHEMA=mailglass mix test test/mailglass/migration_test.exs test/mailglass/schema_isolation_immutability_test.exs --warnings-as-errors</automated>
    <automated>mix test test/mailglass/migration_test.exs --warnings-as-errors</automated>
    <automated>MAILGLASS_SCHEMA=mailglass mix run -e '{:ok, %{rows: rows}} = Mailglass.TestRepo.query("SELECT 1 FROM information_schema.tables WHERE table_schema = '"'"'mailglass'"'"' AND table_name = '"'"'mailglass_events'"'"'"); if rows == [], do: (IO.puts("FAIL: mailglass.mailglass_events not physically present under MAILGLASS_SCHEMA=mailglass"); System.halt(1)), else: IO.puts("OK: mailglass.mailglass_events physically exists")'</automated>
    <automated>mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - With `MAILGLASS_SCHEMA` unset, `Config.schema/0` in the test env returns "public" (config/test.exs pin unchanged).
    - With `MAILGLASS_SCHEMA=mailglass`, `Config.schema/0` returns "mailglass" and the named tests pass under that schema.
    - FAIL-CLOSED (boot-order proof): under `MAILGLASS_SCHEMA=mailglass`, a `mailglass.*` table PHYSICALLY EXISTS post-boot — assert `mailglass.mailglass_events` is present in the `mailglass` schema via a schema-aware `information_schema.tables` query (reusing the `table_exists?` pattern from test/mailglass/migration_test.exs, parameterized on `table_schema = 'mailglass'`). A string-only `Config.schema()=="mailglass"` assertion does NOT satisfy this criterion — the physical-table check guards against a boot-order no-op that migrates under "public" before the override applies. (Verify snippet uses `Mailglass.TestRepo` — the project's test repo, per test/mailglass/migration_test.exs.)
    - The override is guarded to `config_env() == :test` and a non-empty env value; production/dev config is untouched.
    - Format clean.
  </acceptance_criteria>
  <done>An operator/CI env var flips the whole test suite's schema through the existing Config.schema/0 path without touching the test.exs default.</done>
</task>

<task type="auto">
  <name>Task 3: Add the schema: [public, mailglass] matrix axis to Core Full Suite Advisory (Success Criterion 7 / D-06)</name>
  <files>.github/workflows/advisory-matrix.yml</files>
  <read_first>
    - .github/workflows/advisory-matrix.yml lines 19-165 (BOTH "Core Full Suite Advisory" jobs: core_full_suite_advisory (PR + push, elixir 1.18/otp 27) and core_latest_elixir_advisory (non-PR only, elixir 1.19/otp 28). Both have a matrix.include with elixir/otp, a postgres service, an env: block, and a "Run advisory full suite" step: `mix test --warnings-as-errors --exclude requires_workspace`)
    - config/runtime.exs (the MAILGLASS_SCHEMA hook added in Task 2 — the env var the run step must export)
  </read_first>
  <action>
    Add a `schema` axis to the Core Full Suite Advisory matrix so the full suite runs under
    BOTH "public" and "mailglass". Because both jobs use `matrix.include:` (not top-level
    matrix keys), add the schema value to each include entry by expanding to two rows per
    toolchain, e.g.:

      matrix:
        include:
          - elixir: "1.18"
            otp: "27"
            schema: "public"
          - elixir: "1.18"
            otp: "27"
            schema: "mailglass"

    Apply the same 2-row expansion to `core_latest_elixir_advisory` (1.19/28 × public,
    mailglass). Add `${{ matrix.schema }}` to each job's `name:` so the two rows are
    distinguishable in the checks UI (e.g. "Core Full Suite Advisory (Elixir ... / OTP ...
    / schema ...)"). NOTE: changing the name string changes the required-checks context
    identity — these are ADVISORY (non-required) lanes, so that is safe, but preserve the
    "Core Full Suite Advisory" leading substring so any `isAdvisory()` name-matching in
    publish-hex still classifies them as advisory.

    In the "Run advisory full suite" step of each job, export the schema to the app env by
    adding an `env:` on the step (or extending the job `env:`) with
    `MAILGLASS_SCHEMA: ${{ matrix.schema }}`, which config/runtime.exs (Task 2) reads. KEEP
    the existing `--exclude requires_workspace` flag on the `mix test` command unchanged.

    Do NOT convert these advisory jobs into required checks and do NOT touch the required
    `mix ci` lanes — the schema axis lives only on the advisory full-suite lanes per the
    embedded decision (Core Full Suite Advisory is the ONLY full-core mix test lane).

    Because CI cannot be executed from this environment, the verification is a
    toolchain-independent literal assertion on the YAML (plain `grep` for BOTH
    `schema: "public"` and `schema: "mailglass"` under the include rows — no ruby/YAML
    interpreter, because this asdf env has NO selected ruby version and a red ruby shim
    would masquerade as a failed axis) plus the local proof that the suite passes under
    MAILGLASS_SCHEMA (from Task 2). Emit the two schema literals in the exact
    `schema: "public"` / `schema: "mailglass"` quoted form the grep asserts. Document in the
    SUMMARY that the green-under-both-schemas outcome is confirmed by the local
    MAILGLASS_SCHEMA=mailglass run and the CI axis will exercise it on push.
  </action>
  <verify>
    <automated>grep -q 'schema: "public"' .github/workflows/advisory-matrix.yml && grep -q 'schema: "mailglass"' .github/workflows/advisory-matrix.yml && echo "both schema literals present"</automated>
    <automated>test "$(grep -c 'schema: "public"' .github/workflows/advisory-matrix.yml)" -ge 2 && test "$(grep -c 'schema: "mailglass"' .github/workflows/advisory-matrix.yml)" -ge 2 && echo "schema axis present on both advisory jobs (public + mailglass rows each)"</automated>
    <automated>MAILGLASS_SCHEMA=mailglass mix test test/mailglass/migration_test.exs test/mailglass/schema_isolation_integration_test.exs test/mailglass/schema_isolation_immutability_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Both Core Full Suite Advisory jobs (`core_full_suite_advisory` and `core_latest_elixir_advisory`) carry a `schema` axis with values "public" and "mailglass" (2 rows per toolchain).
    - Each "Run advisory full suite" step exports `MAILGLASS_SCHEMA: ${{ matrix.schema }}` and keeps `--exclude requires_workspace`.
    - Job `name:` includes `${{ matrix.schema }}` while preserving the "Core Full Suite Advisory" leading substring (so `isAdvisory()` still classifies them advisory).
    - No required `mix ci` lane is modified; the axis lives only on the advisory lanes.
    - A toolchain-independent `grep` assertion confirms BOTH `schema: "public"` and `schema: "mailglass"` literals appear (≥2 each — one row per advisory job) in advisory-matrix.yml; NO ruby/YAML interpreter is used (this asdf env has no selected ruby). The local `MAILGLASS_SCHEMA=mailglass` run of the schema-isolation + migration tests is green (proving the mailglass axis is passable).
  </acceptance_criteria>
  <done>The full core suite is wired to run under both public and mailglass schemas on the advisory lane, and the mailglass axis is proven passable locally.</done>
</task>

</tasks>

<verification>
- `mix credo --strict` green (new `@schema_prefix` guard registered + passing on the current tree).
- `mix test test/mailglass/credo/no_schema_prefix_attribute_test.exs test/mailglass/credo/checks_have_tests_test.exs` green (check fires on fixture, is registered + tested).
- With `MAILGLASS_SCHEMA` unset the suite runs under "public" (unchanged); with `MAILGLASS_SCHEMA=mailglass` the schema-isolation + migration tests are green AND `mailglass.mailglass_events` physically exists in the `mailglass` schema post-boot (fail-closed against a boot-order no-op) — proving the CI axis is passable.
- A toolchain-independent `grep` assertion (no ruby/YAML interpreter — this asdf env has no selected ruby) confirms BOTH `schema: "public"` and `schema: "mailglass"` literals appear on the Core Full Suite Advisory job(s).
- `mix compile --warnings-as-errors` clean; `mix format --check-formatted` clean.
- Note (execution env): CI itself cannot run here; the green-under-both-schemas outcome is proven locally via the `MAILGLASS_SCHEMA=mailglass` targeted run, and the CI axis exercises the full suite on push.
</verification>

<success_criteria>
- MIGR-06: a Credo guard (`Mailglass.Credo.NoSchemaPrefixAttribute`) fails the build on any `@schema_prefix` under `lib/mailglass/`, is registered in `.credo.exs`, has a regression test, and is green on the current tree.
- Success Criterion 7 / D-06: the full core suite is wired to run green under BOTH `schema: "public"` and `schema: "mailglass"` via a new matrix axis on Core Full Suite Advisory; the mailglass axis is proven passable by the local `MAILGLASS_SCHEMA=mailglass` run.
</success_criteria>

<output>
Create `.planning/phases/134-migration-entrypoint-raw-ddl-trigger-qualification/134-03-SUMMARY.md` when done.
</output>