---
phase: 137-linked-2-0-release-ceremony-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/mix.exs
  - mailglass_inbound/mix.exs
  - reference/host_app/mix.exs
  - reference/host_app/mix.lock
  - reference/demo_app/mix.exs
  - reference/demo_app/mix.lock
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "mailglass_admin/mix.exs MIX_PUBLISH branch pins {:mailglass, \"~> 2.0\"} (was ~> 1.10)"
    - "mailglass_inbound/mix.exs MIX_PUBLISH branch pins {:mailglass, \"~> 2.0\"} with the `and >= 1.10.2` floor dropped"
    - "reference/host_app and reference/demo_app mailglass sibling constraints advanced ~> 1.0 -> ~> 2.0"
    - "Both reference mix.lock siblings updated with siblings-only diff (no transitive drift)"
    - "Reference baseline dogfoods the mailglass default schema (no :schema \"public\" pin added)"
    - "Trust-journey + clean-baseline lanes pass under the mailglass default schema"
  artifacts:
    - mailglass_admin/mix.exs
    - mailglass_inbound/mix.exs
    - reference/host_app/mix.exs
    - reference/host_app/mix.lock
    - reference/demo_app/mix.exs
    - reference/demo_app/mix.lock
  key_links:
    - "inbound `~> 2.0` core pin is load-bearing: lets publish-inbound version-solve against a 2.0 core"
    - "reference baseline schema adoption couples to the trust-journey checkpoint contract (the one place a schema change ripples into an existing guard)"
  prohibitions:
    - "Do NOT reintroduce an `==` inbound core pin (retires the v1.15 loosened-`~>` keystone win)"
    - "Do NOT pin `config :mailglass, :schema, \"public\"` in the reference baseline (D-07 dogfoods the real 2.0 default)"
    - "Do NOT edit check_clean_baseline_hex_only.sh or ci_trust_lane_contract_test.exs — they are version-agnostic; the version move needs no guard edit"
    - "Do NOT introduce transitive lock drift; regenerate mix.lock siblings-only"
---

<objective>
Land the pre-release manual edits that release-please cannot make on its own, so the
2.0 publish fan-out can version-solve. Two independent groups of edits:

1. **Sibling `mix.exs` dep-pin edits (D-03, D-05):** hand-edit the `{:mailglass, ...}`
   constraints in `mailglass_admin/mix.exs` and `mailglass_inbound/mix.exs` (the
   `MIX_PUBLISH == "true"` branch of each `mailglass_dep/0`) to `~> 2.0`, dropping
   inbound's `and >= 1.10.2` floor. Release-please's sed only rewrites README/display
   pins, never `mix.exs` — without this edit `publish-admin`/`publish-inbound` fail
   version-solving against a 2.0 core after core has already published.

2. **Coordinated reference-baseline advance (D-06, D-07):** advance `~> 1.0` -> `~> 2.0`
   in `reference/host_app/mix.exs` + `reference/demo_app/mix.exs` and regenerate both
   `mix.lock` siblings-only. Adopt the new `mailglass` default schema (do NOT pin
   `:schema "public"`) — the frozen baseline dogfoods the real 2.0 adopter behavior
   (tables land in `mailglass.*`). Verify the trust-journey + clean-baseline lanes
   green under the new schema, treating any checkpoint-contract drift as an intended
   baseline advance, not a regression.

Purpose: These are the load-bearing pre-conditions for a clean 2.0 fan-out. They MUST
land on main BEFORE the release-please PR opens (Plan 02), or the publish jobs break.
Output: Six edited files (2 sibling mix.exs, 2 reference mix.exs, 2 reference mix.lock),
committed to main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-CONTEXT.md

# Hand-edit targets (read the exact current pins before editing)
@mailglass_admin/mix.exs
@mailglass_inbound/mix.exs
@reference/host_app/mix.exs
@reference/demo_app/mix.exs

# Version-agnostic guards — DO NOT EDIT, referenced to confirm no edit is needed
@scripts/check_clean_baseline_hex_only.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-edit sibling mix.exs core pins to ~> 2.0 (D-03, D-05)</name>
  <files>mailglass_admin/mix.exs, mailglass_inbound/mix.exs</files>
  <read_first>
    - mailglass_admin/mix.exs mailglass_dep/0 (~line 145-149): MIX_PUBLISH branch currently `{:mailglass, "~> 1.10"}`
    - mailglass_inbound/mix.exs mailglass_dep/0 (~line 141-145): MIX_PUBLISH branch currently `{:mailglass, "~> 1.10 and >= 1.10.2"}`
    - Per D-05: the `and >= 1.10.2` floor only ever excluded broken 1.10.0/1.10.1 core builds; no analog on the fresh 2.0 line — drop it
  </read_first>
  <action>
    In mailglass_admin/mix.exs, in the `MIX_PUBLISH == "true"` branch of mailglass_dep/0,
    change the core constraint from `{:mailglass, "~> 1.10"}` to `{:mailglass, "~> 2.0"}`.
    Leave the path-dep local-dev branch and the optional inbound sibling dep untouched.

    In mailglass_inbound/mix.exs, in the `MIX_PUBLISH == "true"` branch of mailglass_dep/0,
    change `{:mailglass, "~> 1.10 and >= 1.10.2"}` to `{:mailglass, "~> 2.0"}` per D-05 —
    dropping the `and >= 1.10.2` floor entirely. Keep the loosened `~>` form; do NOT
    reintroduce an `==` re-pin (that would retire the v1.15 keystone win). Update the
    explanatory head-comment above the def if it names the old `~> 1.10` boundary, so the
    comment matches the new `~> 2.0` pin.

    Do NOT touch reference apps in this task (Task 2 handles those).
  </action>
  <verify>
    <automated>grep -A3 'MIX_PUBLISH' mailglass_admin/mix.exs | grep -q '{:mailglass, "~> 2.0"}' && grep -A3 'MIX_PUBLISH' mailglass_inbound/mix.exs | grep -q '{:mailglass, "~> 2.0"}' && ! grep -q 'and >= 1.10.2' mailglass_inbound/mix.exs && ! grep -q '{:mailglass, "== ' mailglass_inbound/mix.exs && echo PINS_OK</automated>
  </verify>
  <acceptance_criteria>
    Both sibling MIX_PUBLISH branches pin `{:mailglass, "~> 2.0"}`; inbound's `and >= 1.10.2`
    floor is gone; no `==` core pin exists anywhere in inbound mix.exs.
  </acceptance_criteria>
  <done>
    mailglass_admin and mailglass_inbound publish-branch core pins both read `~> 2.0`;
    inbound floor dropped; loosened `~>` preserved (no `==` reintroduced).
  </done>
</task>

<task type="auto">
  <name>Task 2: Advance reference baseline to ~> 2.0 + adopt mailglass default schema (D-06, D-07)</name>
  <files>reference/host_app/mix.exs, reference/host_app/mix.lock, reference/demo_app/mix.exs, reference/demo_app/mix.lock</files>
  <read_first>
    - reference/host_app/mix.exs lines 35-37: `{:mailglass, "~> 1.0"}`, `{:mailglass_admin, "~> 1.0"}`, `{:mailglass_inbound, "~> 1.0"}`
    - reference/demo_app/mix.exs mailglass_dep/0, mailglass_admin_dep/0, mailglass_inbound_dep/0 (lines 58-71): each hex branch pins `~> 1.0`; hex_deps?/0 gates on MAILGLASS_DEMO_DEPS == "hex"
    - MEMORY project_reference_baseline_coupling: baseline advance is a coordinated mix.exs + mix.lock change; siblings-only lock diff; demo_app needs MAILGLASS_DEMO_DEPS=hex
    - MEMORY project_demo_app_swoosh_lock_drift: any mix run in reference/demo_app re-bumps swoosh lock; git checkout the lock for any transitive drift before committing
    - D-07: adopt the real 2.0 default schema — do NOT add `config :mailglass, :schema, "public"` anywhere in the reference apps
  </read_first>
  <action>
    Advance all three mailglass sibling constraints from `~> 1.0` to `~> 2.0`:
    - reference/host_app/mix.exs lines 35-37 (mailglass, mailglass_admin, mailglass_inbound)
    - reference/demo_app/mix.exs mailglass_dep/0, mailglass_admin_dep/0, mailglass_inbound_dep/0 hex branches

    Regenerate both locks siblings-only. For host_app run `mix deps.get` (or
    `mix deps.update mailglass mailglass_admin mailglass_inbound`) in reference/host_app;
    for demo_app run with `MAILGLASS_DEMO_DEPS=hex mix deps.get` in reference/demo_app so
    the hex branch is exercised. After each, inspect `git diff` on the lock and restrict it
    to the three mailglass sibling entries only — `git checkout` any transitive drift
    (e.g. swoosh re-bump in demo_app per the known lock-drift gotcha).

    Do NOT add any `config :mailglass, :schema, "public"` opt-out to either reference app —
    per D-07 the frozen baseline must dogfood the real 2.0 default (tables land in
    `mailglass.*`). Do NOT edit check_clean_baseline_hex_only.sh or
    ci_trust_lane_contract_test.exs — they are version-agnostic (assert `:hex` source +
    well-formed version, no hardcoded version list), so the version move needs no guard edit.

    NOTE for the executor: the 2.0 sibling packages are not yet on Hex at this point
    (they publish in Plan 02). If `mix deps.get` cannot resolve `~> 2.0` from Hex here,
    record that the lock regeneration is deferred to the post-publish verification in
    Plan 02 (Task: consumer/baseline re-resolve) and land the mix.exs constraint bumps
    now; do NOT fabricate lock entries for unpublished versions. Capture which path was
    taken in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c '"~> 2.0"' reference/host_app/mix.exs | grep -qx 3 && grep -c '"~> 2.0"' reference/demo_app/mix.exs | grep -qx 3 && ! grep -rn ':schema, "public"' reference/host_app reference/demo_app && echo BASELINE_CONSTRAINTS_OK</automated>
  </verify>
  <acceptance_criteria>
    Both reference mix.exs files carry three `~> 2.0` mailglass sibling constraints; neither
    reference app pins `:schema "public"`; locks are siblings-only (or lock regen explicitly
    deferred to Plan 02 with rationale recorded in SUMMARY).
  </acceptance_criteria>
  <done>
    reference/host_app and reference/demo_app mailglass constraints read `~> 2.0`; no
    `:schema "public"` opt-out present; lock diffs are siblings-only or deferred with a
    recorded reason.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify trust-journey + clean-baseline lanes green under the mailglass schema (D-07)</name>
  <files>test/mailglass/publish/ci_trust_lane_contract_test.exs (read-only), scripts/check_clean_baseline_hex_only.sh (read-only)</files>
  <read_first>
    - D-07 consequence: adopting the mailglass default schema relocates the baseline's tables; the trust-journey checkpoint contract (check_trust_runner_checkpoint.sh / ci_trust_lane_contract_test.exs) may need updating to expect the schema-qualified shape — treat any drift as an intended baseline advance, NOT a regression
    - scripts/check_clean_baseline_hex_only.sh is version-agnostic (asserts :hex source + well-formed version)
    - MEMORY project_execute_phase_no_optional_deps_pollution: verify deterministically on main, don't pollute the shared _build
  </read_first>
  <action>
    Run the trust-lane contract test and the clean-baseline guard to confirm they pass
    under the new `~> 2.0` + mailglass-default-schema baseline:
    - `mix test test/mailglass/publish/ci_trust_lane_contract_test.exs`
    - `bash scripts/check_clean_baseline_hex_only.sh` (with any required args per its usage)

    If the trust-journey checkpoint contract drifts because tables are now schema-qualified
    (`mailglass.*` instead of `public.*`), update the EXPECTATION in the guard/contract to
    the schema-qualified shape and document it as an intended baseline advance (cite D-07).
    Do NOT weaken or disable the guard, and do NOT add a `:schema "public"` pin to make the
    old shape reappear — that defeats the dogfooding intent.

    If lock regeneration was deferred in Task 2 (2.0 not yet on Hex), note that the FULL
    trust-journey lane (which resolves siblings from Hex) is re-verified empirically in
    Plan 02 post-publish; run here whatever subset passes without published 2.0 artifacts
    and record the split in the SUMMARY.
  </action>
  <verify>
    <automated>mix test test/mailglass/publish/ci_trust_lane_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    ci_trust_lane_contract_test passes; clean-baseline guard passes (or its check is
    explicitly deferred to Plan 02 post-publish with rationale). Any schema-qualified
    contract update is documented as intended (D-07), not a weakening.
  </acceptance_criteria>
  <done>
    Trust-lane contract test green; clean-baseline guard green or deferred-with-reason; any
    checkpoint-contract drift resolved as an intended schema-qualified baseline advance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> Hex publish fan-out | mix.exs pins here decide whether publish-admin/publish-inbound can version-solve at release |
| reference baseline -> trust-journey CI | baseline schema adoption crosses into an existing checkpoint contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-01 | Tampering | inbound core pin | high | mitigate | Keep loosened `~> 2.0`; verify grep asserts no `==` reintroduced (retiring the v1.15 keystone would silently re-pin) |
| T-137-02 | Denial-of-service | publish fan-out | high | mitigate | Land sibling `mix.exs` `~> 2.0` pins on main BEFORE the RP PR opens, or publish-admin/publish-inbound fail version-solving |
| T-137-03 | Tampering | reference mix.lock | medium | mitigate | Restrict lock diff to the three mailglass siblings; `git checkout` transitive drift (known swoosh re-bump) |
| T-137-04 | Repudiation | trust-journey contract | low | accept | Schema-qualified drift is an intended baseline advance (D-07); document rather than suppress |
</threat_model>

<verification>
- `grep` confirms both sibling MIX_PUBLISH branches pin `{:mailglass, "~> 2.0"}`
- `grep` confirms inbound `and >= 1.10.2` floor dropped and no `==` core pin present
- Both reference mix.exs carry three `~> 2.0` mailglass constraints
- No `:schema "public"` opt-out anywhere in reference apps
- `ci_trust_lane_contract_test.exs` green; clean-baseline guard green (or deferred with reason)

**Nyquist note (Dimension 8 — sampling test coverage):** this is a release/ops setup plan.
No new library behavior is introduced; there is no new ExUnit surface to sample. Verification
is operational (grep on pins + running the two existing guards). Fabricating unit tests here
would be noise — explicitly N/A.
</verification>

<success_criteria>
- Sibling `mix.exs` core pins are `~> 2.0` (admin + inbound), floor dropped, no `==`
- Reference baseline advanced to `~> 2.0` across host_app + demo_app, locks siblings-only (or deferred)
- Reference baseline adopts the mailglass default schema (no `:schema "public"` pin)
- Trust-journey + clean-baseline lanes green under the new schema (or deferred to Plan 02)
- All six files committed to main so the RP PR (Plan 02) opens against a solvable 2.0 baseline
</success_criteria>

<output>
Create `.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-01-SUMMARY.md` when done.
Record: which lock-regeneration path was taken (regenerated now vs deferred to Plan 02
because 2.0 is not yet on Hex), and any schema-qualified trust-journey contract update made
under D-07.
</output>
