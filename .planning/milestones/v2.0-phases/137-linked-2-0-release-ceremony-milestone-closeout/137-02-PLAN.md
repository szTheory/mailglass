---
phase: 137-linked-2-0-release-ceremony-milestone-closeout
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - reference/host_app/mix.lock
  - reference/demo_app/mix.lock
autonomous: false
requirements: [REL-01, REL-02]

must_haves:
  truths:
    - "release-please PR targets 2.0.0 (mailglass) + 2.0.0 (mailglass_admin, linked) + 2.0.0 (mailglass_inbound, standalone) — confirmed BEFORE merge"
    - "mix hex.info reports 2.0.0 for mailglass, mailglass_admin, and mailglass_inbound"
    - "consumer-install smoke boots and GET /dev/mail/ returns HTTP 200 against the published 2.0 packages"
    - "post-publish-smoke workflow is green (or its green state confirmed via mix hex.info + consumer smoke when the run races red)"
    - "reference baseline lock resolves the published 2.0 siblings from Hex (if deferred from Plan 01)"
  artifacts:
    - "Hex releases: mailglass 2.0.0, mailglass_admin 2.0.0, mailglass_inbound 2.0.0"
    - "git tags mailglass-v2.0.0, mailglass_admin-v2.0.0, mailglass_inbound-v2.0.0 (RP-created)"
  key_links:
    - "the empty `Release-As: 2.0.0` footer commit(s) are what force the major — RP would otherwise score 1.12.0/1.12.0/1.7.0"
    - "the D-02 dry-run PR inspection is the gate that catches per-component path-attribution errors before an irreversible merge"
    - "mix hex.info (not publish-hex run status) is the source of truth for published state (racing fan-outs, D-10)"
  prohibitions:
    - "Do NOT merge the RP PR until the dry-run inspection confirms all three targets are 2.0.0 (D-02)"
    - "Do NOT pre-edit .release-please-manifest.json — the manifest edit is the documented FALLBACK only, used solely if the dry-run PR shows wrong targets (D-02)"
    - "Do NOT pre-edit scripts/consumer_install_smoke.sh or post-publish-smoke.yml — they are already 2.0-ready (D-08); verify empirically, do not touch"
    - "Do NOT trust a red `publish-hex` run as proof of a failed publish — verify via mix hex.info + consumer smoke (D-10)"
    - "Do NOT reintroduce an `==` inbound core pin at any point"
---

<objective>
Trigger, cut, and verify the linked 2.0 release. Four movements, strictly serial:

1. **Version trigger (D-01):** no `feat!:`/`BREAKING CHANGE:` is banked in 132-136, so
   left alone release-please cuts 1.12.0/1.12.0/1.7.0. Force the major with an explicit
   `Release-As: 2.0.0` footer for the linked core+admin group, and a SEPARATE
   `Release-As: 2.0.0` footer for standalone `mailglass_inbound` (D-04 — inbound goes to
   2.0.0, not the minor RP would score, because Phase 135 gave it its own breaking
   changes). `Release-As` names the exact target; a bare `BREAKING CHANGE:` footer would
   only guarantee "a major," not "2.0.0."

2. **Rehearse before merge (D-02):** inspect the opened release-please PR and confirm it
   targets `2.0.0`/`2.0.0` (linked core+admin) AND `2.0.0` (inbound) BEFORE merging. This
   catches any per-component path-attribution issue with RP v5's handling of the empty
   `Release-As` commit. The `.release-please-manifest.json` pre-edit is the documented
   FALLBACK only — used solely if the dry-run PR shows wrong targets.

3. **Merge on green + publish (REL-01):** RP auto-merges on green and the publish fan-out
   runs hands-free. Expect a possible "CI-the-body" catch — the accumulated 132-136 body
   hits its first real CI of the whole body here (push-before-merge lesson, D-10).

4. **Verify all three packages (REL-02, D-08, D-10):** confirm 2.0.0 via `mix hex.info`
   for all three (racing-fan-out safe — never trust the publish-hex run status), run the
   consumer-install smoke (GET /dev/mail/ == 200), and confirm post-publish-smoke green.

Purpose: this is the actual release cut. Output: three 2.0.0 Hex packages + three
RP-created `-v2.0.0` tags, empirically verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-CONTEXT.md
@.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-01-SUMMARY.md

# Release machinery (read to understand targets + fan-out; do NOT edit smoke/manifest)
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@scripts/consumer_install_smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the Release-As trigger commit(s) (D-01, D-04)</name>
  <files>(git — empty commit(s), no tracked-file change)</files>
  <read_first>
    - .release-please-manifest.json: current 1.11.0 / 1.11.0 / 1.6.0
    - release-please-config.json: linked-versions group = [mailglass, mailglass_admin]; mailglass_inbound is standalone
    - D-01: prefer `Release-As` over `BREAKING CHANGE:` — Release-As names the exact target (2.0.0), a breaking footer only guarantees "a major"
    - D-04: inbound goes to 2.0.0 (its own breaking changes from Phase 135), not the 1.7.0 minor RP would score
    - Claude's Discretion (CONTEXT): whether the two Release-As footers ride one commit or two is left to the executor, provided the dry-run PR confirms correct targets before merge
  </read_first>
  <action>
    Create the empty `Release-As: 2.0.0` trigger commit(s) on main (or the release branch
    per repo convention) so release-please cuts 2.0.0 for the linked core+admin group and
    2.0.0 for standalone mailglass_inbound.

    Because the linked group and inbound are attributed by path, the safest shape is TWO
    empty commits, each carrying a `Release-As: 2.0.0` footer scoped to the right component:
    one whose changes attribute to the linked core+admin group, one attributing to
    mailglass_inbound. If the executor instead uses a single commit with two footers, the
    D-02 dry-run inspection (Task 2) is the mandatory gate that must confirm all three
    targets came out at 2.0.0 before any merge.

    Use a conventional-commit subject (e.g. `chore(release): trigger 2.0.0 major`). Do NOT
    pre-edit `.release-please-manifest.json` — that is the fallback only (D-02). Push so the
    release-please workflow opens/updates its PR.
  </action>
  <verify>
    <automated>git log --format='%B' -5 | grep -c 'Release-As: 2.0.0' | awk '$1>=1{print "RELEASE_AS_PRESENT"; exit} {print "MISSING"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    At least one `Release-As: 2.0.0` footer is present in recent history and pushed; the
    release-please workflow has run and opened/updated its release PR.
  </acceptance_criteria>
  <done>
    `Release-As: 2.0.0` trigger commit(s) pushed; release-please PR opened/updated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rehearse — confirm RP PR targets 2.0.0/2.0.0/2.0.0 BEFORE merge (D-02)</name>
  <files>(no file change — inspection checkpoint on the release-please PR)</files>
  <action>
    This is a blocking rehearsal checkpoint (D-02). Inspect the release-please PR and confirm
    all three targets are exactly 2.0.0 before merging. Follow the how-to-verify steps below.
    Do NOT merge until targets read 2.0.0/2.0.0/2.0.0. Manifest pre-edit is the fallback only.
  </action>
  <what-built>
    The empty `Release-As: 2.0.0` trigger commit(s) from Task 1 have driven release-please
    to open/update its release PR. Per D-02 the release must be REHEARSED: the PR's targeted
    versions must be inspected and confirmed correct before the irreversible merge.
  </what-built>
  <how-to-verify>
    1. Find the open release PR:
       `gh pr list --label 'autorelease: pending' --json number,title,url`
    2. Inspect the version targets RP computed. Read the PR body / changed
       `.release-please-manifest.json` diff:
       `gh pr view <N> --json files,body` and
       `gh pr diff <N> -- .release-please-manifest.json`
    3. CONFIRM all three targets are exactly:
       - mailglass: 2.0.0
       - mailglass_admin: 2.0.0 (linked with core)
       - mailglass_inbound: 2.0.0 (standalone — NOT 1.7.0)
    4. If ANY target is wrong (e.g. inbound landed at 1.7.0, or a component missed 2.0.0
       due to path-attribution): apply the documented FALLBACK — edit
       `.release-please-manifest.json` to the correct 2.0.0 targets, push to the release
       branch, let RP re-render, and re-inspect. Only the fallback path touches the manifest.
    5. Do NOT merge until targets read 2.0.0/2.0.0/2.0.0.
  </how-to-verify>
  <resume-signal>Type "approved: 2.0.0/2.0.0/2.0.0 confirmed" once the RP PR targets are correct, or describe the drift + fallback applied.</resume-signal>
  <verify>RP PR targets confirmed 2.0.0 (mailglass) / 2.0.0 (mailglass_admin) / 2.0.0 (mailglass_inbound) via `gh pr view` before merge.</verify>
  <done>All three RP PR version targets read 2.0.0; PR not yet merged; fallback manifest edit applied only if drift was found.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Merge on green + watch the publish fan-out (REL-01, D-10)</name>
  <files>(no file change — merge + fan-out observation checkpoint)</files>
  <action>
    This is a blocking merge checkpoint. Let the RP PR auto-merge on green (admin-merge if
    it stalls), then watch the publish-hex fan-out. Follow the how-to-verify steps. A red
    publish-hex run does NOT imply failure (D-10) — verification happens in Task 4.
  </action>
  <what-built>
    The RP PR targets are confirmed 2.0.0/2.0.0/2.0.0 (Task 2). The v1.15-hardened pipeline
    auto-merges the RP PR on green (`CI Green` fan-in gate) and runs the publish fan-out
    hands-free through the `hex-publish` environment.
  </what-built>
  <how-to-verify>
    1. Let the RP PR auto-merge on green. If CI goes red on the WHOLE accumulated 132-136
       body (the expected "CI-the-body" catch, D-10), triage and fix on main; this is the
       first real CI of the whole body — validate the WHOLE publish lane, not just required
       jobs.
    2. If auto-merge stalls (review-required BLOCKED), admin-merge is the documented override:
       `gh pr merge <N> --squash --admin`.
    3. Watch the publish fan-out:
       `gh run list --workflow publish-hex.yml --limit 3` then
       `gh run watch <run-id>`.
    4. CRITICAL (D-10, racing fan-outs): a RED `publish-hex` run does NOT imply a failed
       publish. admin's `needs:[publish-core,publish-inbound]` can succeed while core/inbound
       tag-runs go red on transient TLS. Do NOT declare failure from run status — proceed to
       Task 4 and verify published state via `mix hex.info`.
  </how-to-verify>
  <resume-signal>Type "merged + fan-out ran" once the RP PR is merged and the publish-hex fan-out has completed (green or racing-red — verification happens in Task 4).</resume-signal>
  <verify>RP PR merged (auto or `gh pr merge --squash --admin`); publish-hex fan-out run completed and its run id captured for Task 4.</verify>
  <done>RP PR merged on green (or after CI-the-body triage); publish-hex fan-out has run; run status recorded but NOT treated as publish proof.</done>
</task>

<task type="auto">
  <name>Task 4: Verify all three packages published at 2.0.0 + consumer/post-publish smoke (REL-02, D-08, D-10)</name>
  <files>reference/host_app/mix.lock, reference/demo_app/mix.lock (only if lock regen was deferred from Plan 01)</files>
  <read_first>
    - D-10: verify published state via `mix hex.info` (racing-fan-out safe), NEVER the publish-hex run status
    - D-08: post-publish-smoke.yml is already 2.0-ready (checkout fix 67f4b33d present; inbound-compat detection is `~>`-aware). consumer_install_smoke.sh writes self-consistent `== 2.0.0` sibling pins that resolve against inbound's `~> 2.0` core pin. Verify empirically — do NOT pre-edit either.
    - scripts/consumer_install_smoke.sh: phx.new -> deps -> install -> boot -> assert GET /dev/mail/ == 200; INCLUDE_INBOUND toggles the inbound sibling
    - 137-01-SUMMARY.md: whether reference lock regeneration was deferred to this plan
  </read_first>
  <action>
    Verify the published state for all three packages (do NOT trust the run status):
    - `mix hex.info mailglass` -> reports 2.0.0
    - `mix hex.info mailglass_admin` -> reports 2.0.0
    - `mix hex.info mailglass_inbound` -> reports 2.0.0
    (If any is missing, the fan-out genuinely failed a leg — re-dispatch that publish job
    from the release tag per the workflow's documented tag-pinned workflow_dispatch fallback,
    then re-check hex.info. A racing-red run with all three present on Hex is a SUCCESS.)

    Run the consumer-install smoke against the published packages:
    `DEP_MODE=hex INCLUDE_INBOUND=true bash scripts/consumer_install_smoke.sh` (per its
    usage) and confirm it boots and `GET /dev/mail/` returns HTTP 200. Do NOT edit the
    script — it is already 2.0-ready (D-08).

    Confirm post-publish-smoke.yml is green:
    `gh run list --workflow post-publish-smoke.yml --limit 3`. If it raced red while
    hex.info + consumer smoke are green, treat the release as verified (D-10) and record the
    racing-red run for the SUMMARY.

    If reference lock regeneration was DEFERRED from Plan 01 (2.0 not yet on Hex then): now
    that the 2.0 siblings are live, regenerate reference/host_app/mix.lock and
    reference/demo_app/mix.lock (siblings-only, `MAILGLASS_DEMO_DEPS=hex` for demo_app),
    `git checkout` any transitive drift, and re-run the trust-journey + clean-baseline lanes
    to confirm green under the mailglass default schema (closing the D-07 empirical check).
  </action>
  <verify>
    <automated>for p in mailglass mailglass_admin mailglass_inbound; do mix hex.info "$p" 2>/dev/null | grep -qi '2\.0\.0' && echo "$p:2.0.0 OK" || echo "$p:MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    `mix hex.info` reports 2.0.0 for all three packages; consumer-install smoke returns
    GET /dev/mail/ == 200; post-publish-smoke green (or green-equivalent via hex.info +
    consumer smoke under a racing-red run, recorded). If lock regen was deferred, reference
    locks now resolve the published 2.0 siblings and trust-journey + clean-baseline lanes
    are green under the mailglass schema.
  </acceptance_criteria>
  <done>
    All three packages verified at 2.0.0 on Hex; consumer + post-publish smoke green (or
    verified-equivalent under racing fan-out); reference locks resolved if previously deferred.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RP PR merge | irreversible: merging with wrong targets ships a wrong version to Hex |
| publish-hex run status -> "did it publish?" | run status is NOT authoritative under racing fan-outs |
| published Hex package -> consumer | consumer smoke is the real end-to-end resolution proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-05 | Tampering | RP version targets | critical | mitigate | D-02 dry-run inspection blocks merge until 2.0.0/2.0.0/2.0.0 confirmed |
| T-137-06 | Information disclosure | publish-hex run status | high | mitigate | Verify via `mix hex.info` + consumer smoke, not run status (racing fan-outs, D-10) |
| T-137-07 | Denial-of-service | first CI of accumulated body | high | mitigate | Expect + triage the "CI-the-body" catch; validate the WHOLE publish lane (D-10) |
| T-137-08 | Elevation of privilege | hex-publish environment | medium | accept | Environment intentionally has no required reviewer (hands-free release, per CLAUDE.md) — rationale documented |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (release/ops only); consumer smoke uses already-published mailglass siblings from Hex |
</threat_model>

<verification>
- RP PR targets confirmed 2.0.0/2.0.0/2.0.0 before merge (D-02 checkpoint)
- `mix hex.info` reports 2.0.0 for mailglass, mailglass_admin, mailglass_inbound
- consumer-install smoke: GET /dev/mail/ == 200 against published 2.0 packages
- post-publish-smoke green (or green-equivalent via hex.info + consumer smoke under racing-red)
- three `-v2.0.0` tags created by RP

**Nyquist note (Dimension 8):** release/ops phase — no new library behavior, no new ExUnit
surface to sample. Verification is operational (RP targets, `mix hex.info`, consumer smoke,
post-publish-smoke). Fabricating unit tests here would be noise — explicitly N/A.
</verification>

<success_criteria>
- Release-As trigger cut; RP PR targets confirmed 2.0.0/2.0.0/2.0.0 pre-merge (D-02)
- RP PR merged on green; publish fan-out ran
- All three packages verified at 2.0.0 via `mix hex.info` (not run status)
- Consumer + post-publish smoke green (or green-equivalent under racing fan-out)
- Reference locks resolve the published 2.0 siblings; trust-journey + clean-baseline green under the mailglass schema
- Manifest and smoke scripts left untouched (fallback-only / already-2.0-ready)
</success_criteria>

<output>
Create `.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-02-SUMMARY.md` when done.
Record: the publish-hex run id(s) and whether any raced red while hex.info confirmed the
publish; the consumer-smoke result; whether the manifest fallback was needed (and why); and
whether reference lock regeneration happened here (deferred from Plan 01) or already in Plan 01.
</output>
