---
phase: 136-upgrade-codemod-docs-api-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/upgrade_v2_docs_test.exs
  - guides/upgrading-to-v2_0.md
  - docs/api_stability.md
  - mailglass_inbound/docs/api_stability.md
  - mix.exs
  - .planning/publish/mailglass-files.expected
autonomous: true
requirements:
  - UPG-02
  - UPG-03
must_haves:
  truths:
    - "`guides/upgrading-to-v2_0.md` exists and documents Route A (one-line `config :mailglass, :schema, \"public\"` opt-out) and Route B (the `mix mailglass.upgrade.v2_schema` move migration)"
    - "The guide documents the `create_schema: false` grants block (CREATE SCHEMA … AUTHORIZATION / GRANT USAGE / ALTER DEFAULT PRIVILEGES) for locked-down prod roles"
    - "The guide documents the `public.mailglass_*` literal-SQL grep checklist (find hard-coded `public.` table refs that qualification cannot rescue)"
    - "The guide documents the `SET LOCAL lock_timeout` + `55P03` retry-off-peak posture"
    - "`docs/api_stability.md` documents `config :mailglass, :schema` as a stable 2.0 surface (`Since: 2.0.0`, default `\"mailglass\"`, `\"public\"` the explicit opt-out) with the tenancy-vs-schema orthogonality statement"
    - "`mailglass_inbound/docs/api_stability.md` documents `config :mailglass_inbound, :schema` with the mirror `:schema` contract + orthogonality, without removing any existing required tokens"
    - "`guides/upgrading-to-v2_0.md` is present in `.planning/publish/mailglass-files.expected` (sorted) AND wired into `mix.exs` `extras:` + `groups_for_extras:` under `Guides:`"
  artifacts:
    - "guides/upgrading-to-v2_0.md"
    - "docs/api_stability.md (§Schema config (2.0) subsection added)"
    - "mailglass_inbound/docs/api_stability.md (:schema subsection added)"
    - "test/mailglass/upgrade_v2_docs_test.exs"
  key_links:
    - "guides/upgrading-to-v2_0.md line in .planning/publish/mailglass-files.expected → prevents the 1.10.2 publish-allowlist tag-move dance at Phase 137's 2.0 release (Pitfall 1, LOAD-BEARING)"
    - "guide wired into mix.exs extras + groups_for_extras → renders on HexDocs under Guides"
    - "the guide avoids literal D-NN / LINT-NN tokens → safe if later added to docs.check @tier1_paths (Pitfall 8)"
---

<objective>
Author the adopter-facing v2.0 upgrade documentation (UPG-02) and declare `:schema` a stable 2.0 config surface (UPG-03). Ship `guides/upgrading-to-v2_0.md` (both routes, grants, grep checklist, locking posture), add the `:schema` contract to both `api_stability.md` files (core + inbound) with the tenancy-vs-schema orthogonality statement, wire the guide into HexDocs (`mix.exs`), and — LOAD-BEARING for the 2.0 release — add the guide to the publish allowlist so Phase 137's release does not repeat the 1.10.2 tag-move dance.

Purpose: adopters need clear, exact guidance to choose a route and execute it safely; the api_stability contract must declare `:schema` stable so downstream tooling and reviewers treat it as a supported surface, not an accident.
Output: the new guide, the two api_stability `:schema` sections, the mix.exs + allowlist wiring, and a docs-token test proving every required section/token is present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-upgrade-codemod-docs-api-stability/136-RESEARCH.md
@.planning/phases/136-upgrade-codemod-docs-api-stability/136-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (136-02) produces:
- `guides/upgrading-to-v2_0.md` — NEW adopter-facing guide
- `docs/api_stability.md` — `## §Schema config (2.0)` subsection added (`:schema`, Since 2.0.0, orthogonality)
- `mailglass_inbound/docs/api_stability.md` — mirror `:schema` subsection added
- `mix.exs` — `guides/upgrading-to-v2_0.md` added to `extras:` + `groups_for_extras: Guides:`
- `.planning/publish/mailglass-files.expected` — `guides/upgrading-to-v2_0.md` line added (sorted; load-bearing for the 2.0 release gate)
- `test/mailglass/upgrade_v2_docs_test.exs` — NEW (UPG-02/03 doc-token + allowlist-presence)

Plan 136-01 (parallel, Wave 1) produces the `mix mailglass.upgrade.v2_schema` codemod + its two execution tests — near-zero file overlap with this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — write the failing docs-token + allowlist-presence test</name>
  <files>test/mailglass/upgrade_v2_docs_test.exs</files>
  <read_first>
    - docs/api_stability.md:208-505 (the `Since: <ver>` convention + section shape to mirror for the new `:schema` subsection)
    - mailglass_inbound/docs/api_stability.md:145-364 (inbound stable-inventory shape; the existing required tokens that must NOT be removed)
    - .planning/publish/mailglass-files.expected:8-27 (the enumerated, sorted guides allowlist)
    - .planning/phases/136-upgrade-codemod-docs-api-stability/136-RESEARCH.md (Decision 4 exact file list + Decision 5 doc-token test strategy; Pitfall 1 allowlist landmine)
  </read_first>
  <action>
    Write `test/mailglass/upgrade_v2_docs_test.exs` RED (fails because the guide/sections/allowlist entry do not exist yet — Tasks 2-4 make it GREEN). Plain `use ExUnit.Case, async: true` (no DB — pure `File.read!` token assertions). Assert:

    UPG-02 (guide content) — `File.read!("guides/upgrading-to-v2_0.md")` contains, by concept-token (case-insensitive substring is fine; pick tokens that are unambiguous but do not themselves become negative-grep landmines):
      - a "Route A" section marker and the `config :mailglass, :schema, "public"` opt-out line
      - a "Route B" section marker and a reference to `mix mailglass.upgrade.v2_schema`
      - the `create_schema: false` grants block (assert presence of `GRANT USAGE ON SCHEMA` and `ALTER DEFAULT PRIVILEGES`)
      - the grep checklist (assert presence of the `public.mailglass_` literal-SQL search string the checklist tells adopters to grep for)
      - the locking posture (assert `lock_timeout` and the `55P03` retry token)

    UPG-03 (api_stability contracts):
      - `File.read!("docs/api_stability.md")` contains `config :mailglass, :schema`, `Since: 2.0.0`, and an orthogonality token (assert both `tenant` and `schema` co-occur in a sentence-level check, e.g. the doc contains a phrase mapping `:schema` ≠ per-tenant prefix — pick a stable literal you will write in Task 3, such as "orthogonal to `tenant_id`")
      - `File.read!("mailglass_inbound/docs/api_stability.md")` contains `config :mailglass_inbound, :schema`, `Since: 2.0.0`, and the mirror orthogonality token

    Release gate (UPG-02 allowlist — Pitfall 1, LOAD-BEARING):
      - `File.read!(".planning/publish/mailglass-files.expected")` contains a line exactly `guides/upgrading-to-v2_0.md`
      - `File.read!("mix.exs")` contains `guides/upgrading-to-v2_0.md` (proves it is wired into extras/groups)

    Keep every asserted literal as a MATCH TARGET only; do not add a file-wide negative grep for `public.mailglass_` or `45A01` in this test file.
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade_v2_docs_test.exs --seed 0 2>&1 | tail -20</automated>
  </verify>
  <done>The docs test exists, is `async: true`, and FAILS RED (guide missing / api_stability sections missing / allowlist line missing). `mix format --check-formatted` passes on the new file.</done>
</task>

<task type="auto">
  <name>Task 2: Author guides/upgrading-to-v2_0.md (UPG-02)</name>
  <files>guides/upgrading-to-v2_0.md</files>
  <read_first>
    - guides/upgrading-to-v1_0.md (the existing v1_0 guide — mirror its structure, heading style, and mailglass voice)
    - .planning/research/milestone-schema-isolation/SCHEMA-ISOLATION-DESIGN.md:425-521 (§4.2 Route A/B + the exact move-migration SQL + "Facts that make Route B safe" + the `public.mailglass_*` grep note; §4.3 rollback; §4.4 the exact grants block CREATE SCHEMA AUTHORIZATION / GRANT USAGE / ALTER DEFAULT PRIVILEGES)
    - .planning/phases/136-upgrade-codemod-docs-api-stability/136-RESEARCH.md (Decision 3 lock_timeout/55P03 retry posture prose; Decision 4 guide content list; Pitfall 8 banned-token note)
  </read_first>
  <action>
    Write `guides/upgrading-to-v2_0.md` in mailglass brand voice ("thoughtful maintainer" — clear, exact, confident-not-cocky; errors/prose per CLAUDE.md; the direct word over the clever one). Structure (source §4.2/4.3/4.4 verbatim where it locks content):
      - Intro: what 2.0 changes (mailglass domain tables default to a dedicated `mailglass` Postgres schema instead of `public`) and that there are two upgrade routes.
      - Route A — keep `public`: the one-line `config :mailglass, :schema, "public"` opt-out; zero data movement; when to choose it; note it is the explicit breaking opt-out.
      - Route B — adopt the `mailglass` schema (recommended long-term): run `mix mailglass.upgrade.v2_schema` to generate the move migration, review it, then `mix ecto.migrate`. Explain the generated migration `CREATE SCHEMA`s, `ALTER TABLE … SET SCHEMA`s all four tables under `SET LOCAL lock_timeout`, and recreates the immutability trigger+function schema-qualified. State the safety facts (metadata-only OID relnamespace swap; indexes/constraints/sequences move with the table; citext stays in `public`; the version-marker comment survives).
      - Locking posture: `SET LOCAL lock_timeout = '5s'` makes the `ALTER TABLE … SET SCHEMA` (ACCESS EXCLUSIVE) fail fast with `55P03` (lock_not_available) instead of queue-blocking behind long readers; the whole migration is transactional so a lock abort rolls back — retry `mix ecto.migrate` off-peak; the move is metadata-only and instant once the lock is granted.
      - The `public.mailglass_*` literal-SQL grep checklist: explicit qualification cannot rescue hard-coded `public.mailglass_events` (etc.) strings in adopter SQL/views/functions — grep your codebase for `public.mailglass_` and requalify or drop the schema prefix.
      - `create_schema: false` grants (locked-down prod): for roles lacking `CREATE`, a DBA pre-creates the schema + grants; include the §4.4 SQL block (`CREATE SCHEMA mailglass AUTHORIZATION app_role;` / `GRANT USAGE ON SCHEMA mailglass TO app_role;` / `ALTER DEFAULT PRIVILEGES IN SCHEMA mailglass GRANT SELECT,INSERT,UPDATE,DELETE ON TABLES TO app_role;`) then run `Mailglass.Migration.up(create_schema: false)`.
      - Rollback: Route A = revert the one config line; Route B = the generated migration ships a real `down/0` that moves tables back to `public`.
    Keep internal IDs (`D-NN`, `LINT-NN`) OUT of the prose (Pitfall 8 — they do not belong in an adopter guide and would trip docs.check @banned_patterns if the guide is later tier-1-listed).
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade_v2_docs_test.exs --seed 0 2>&1 | grep -iE "route|grant|lock_timeout|55P03|public.mailglass|passed|failed" | tail -20</automated>
  </verify>
  <done>`guides/upgrading-to-v2_0.md` exists with Route A, Route B, the `create_schema: false` grants block, the `public.mailglass_*` grep checklist, and the `lock_timeout`/`55P03` retry posture. The guide-content assertions in the docs test now pass. No `D-NN`/`LINT-NN` tokens in the prose.</done>
</task>

<task type="auto">
  <name>Task 3: Document :schema as a stable 2.0 surface in both api_stability docs (UPG-03)</name>
  <files>docs/api_stability.md, mailglass_inbound/docs/api_stability.md</files>
  <read_first>
    - docs/api_stability.md:94-176, 463-505 (Stable Inventory + the `## §Telemetry Extensions (Phase 3)` / `## §Repo.multi (Phase 3)` subsection pattern with `Since:` lines — mirror this shape for the new `## §Schema config (2.0)` subsection)
    - mailglass_inbound/docs/api_stability.md:145-284 (inbound Stable Inventory + Mix tasks + the existing required tokens the docs.check/publish.check rules depend on — ADD, never remove)
    - .planning/research/milestone-schema-isolation/SCHEMA-ISOLATION-DESIGN.md:400-414 (§3.9 tenancy interaction — the orthogonality source) + §5 non-goals ("not per-tenant schemas")
    - .planning/phases/136-upgrade-codemod-docs-api-stability/136-RESEARCH.md (Decision 4 api_stability wording; the `@schema` config key already exists — this task just DOCUMENTS it)
  </read_first>
  <action>
    CORE — `docs/api_stability.md`: add a new `## §Schema config (2.0)` subsection (mirror the existing `## §Telemetry Extensions (Phase 3)` / `## §Repo.multi (Phase 3)` shape). Document `config :mailglass, :schema`:
      - Contract: a valid unquoted Postgres identifier (validated at boot via `Mailglass.Config.schema/0` + `Mailglass.Identifier`); default `"mailglass"`; `"public"` is the explicit breaking opt-out.
      - `Since: 2.0.0`.
      - Tenancy-vs-schema orthogonality paragraph: `:schema` selects the single fixed library schema for ALL mailglass tables; it is orthogonal to `tenant_id` multi-tenant data scoping (which composes with the prefix). `:schema` is NOT a per-tenant prefix — do not conflate them; mailglass does not use per-tenant schemas.

    INBOUND — `mailglass_inbound/docs/api_stability.md`: add the mirror subsection for `config :mailglass_inbound, :schema` (same contract shape, `Since: 2.0.0`, same orthogonality statement). CRITICAL: this file is in docs.check `@tier1_paths` + inbound `required_file_entries` — ADD the `:schema` prose ONLY; do NOT remove or reword any existing required token (run the existing inbound docs gate after editing to confirm no required token was disturbed).

    Use the same orthogonality literal in both files that Task 1's test asserts (e.g. "orthogonal to `tenant_id`") so the docs-token test passes on both.
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade_v2_docs_test.exs --seed 0 2>&1 | tail -15 && grep -c "config :mailglass, :schema" docs/api_stability.md && grep -c "config :mailglass_inbound, :schema" mailglass_inbound/docs/api_stability.md</automated>
  </verify>
  <done>Both api_stability docs document `:schema` with `Since: 2.0.0` and the tenancy-vs-schema orthogonality statement; the api_stability assertions in the docs test pass; no existing inbound required token was removed.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the guide into HexDocs (mix.exs) + the publish allowlist (UPG-02 release gate)</name>
  <files>mix.exs, .planning/publish/mailglass-files.expected</files>
  <read_first>
    - mix.exs:435-494 (the `extras:` list and `groups_for_extras: Guides:` list — insert the new guide next to the `upgrading-to-v1_0.md` entries at :439 and :469)
    - .planning/publish/mailglass-files.expected:8-27 (sorted guides enumeration — the new line slots between `upgrading-from-v0_1.md` and `upgrading-to-v1_0.md` alphabetically)
    - .planning/phases/136-upgrade-codemod-docs-api-stability/136-RESEARCH.md (Decision 4 exact file list items 1-2; Pitfall 1 — the allowlist is byte-compared at publish and hard-blocks on any added file; this is the exact 1.10.2 landmine)
  </read_first>
  <action>
    1. `mix.exs`: add `"guides/upgrading-to-v2_0.md"` to the `extras:` list (adjacent to the existing `"guides/upgrading-to-v1_0.md"` entry) AND to `groups_for_extras: Guides:` (adjacent to its `"guides/upgrading-to-v1_0.md"` entry). Both are literal-path lists — add the string in both places using scoped Edits (do not rewrite the lists).
    2. `.planning/publish/mailglass-files.expected`: add the line `guides/upgrading-to-v2_0.md`, keeping the file SORTED — it slots between `guides/upgrading-from-v0_1.md` and `guides/upgrading-to-v1_0.md`. This is LOAD-BEARING for Phase 137's 2.0 release (Pitfall 1): the publish allowlist is byte-compared against the tarball file list and hard-blocks on any un-enumerated file; the guide ships via the `files: ~w(… guides …)` glob, so it MUST be pre-listed here or the release blocks and forces a tag-move recovery (the 1.10.2 landmine).

    Do NOT touch the `files:` glob (the whole `guides/` dir already ships wholesale). Adding the guide to docs.check `@tier1_paths` is OPTIONAL and out of scope for this task (the `*_docs_test.exs` already locks the guide's contract).
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade_v2_docs_test.exs --seed 0 2>&1 | tail -10 && sort -c .planning/publish/mailglass-files.expected && echo "allowlist sorted OK"</automated>
  </verify>
  <done>`guides/upgrading-to-v2_0.md` is in `mix.exs` `extras:` + `groups_for_extras: Guides:` and in `.planning/publish/mailglass-files.expected` (file still passes `sort -c`). The allowlist-presence + mix.exs assertions in the docs test pass. Full docs test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → published Hex tarball | The publish allowlist gate byte-compares the tarball file list against a checked-in enumeration; an un-listed shipped file hard-blocks the release |
| adopter reading the guide → production DB migration | The guide's grants/grep/locking guidance directly shapes how an adopter mutates their production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-05 | Denial of Service | new guide ships but is absent from `.planning/publish/mailglass-files.expected` → Phase 137's 2.0 release hard-blocks at the publish allowlist gate (the 1.10.2 tag-move dance) | high | mitigate | Add the guide to the allowlist (sorted) IN THIS PHASE (Task 4); the docs test asserts the line is present so the omission cannot regress silently |
| T-136-06 | Information Disclosure | guide under-specifies the `public.mailglass_*` grep checklist → adopter misses a hard-coded schema ref and a query silently breaks post-move | medium | mitigate | Guide includes the explicit `public.mailglass_` grep string + the "qualification cannot rescue literal SQL" caveat (§4.2 source); docs test asserts the checklist token |
| T-136-07 | Tampering | editing the inbound api_stability doc removes an existing docs.check/publish.check required token → inbound docs/publish gate reds | medium | mitigate | ADD-only edit; re-run the inbound docs gate after editing; the docs test only adds new required tokens, never removes existing coverage |
| T-136-08 | Repudiation | `:schema` documented ambiguously as a per-tenant prefix → adopters conflate it with `tenant_id` and mis-model isolation | low | mitigate | Both api_stability docs carry the explicit tenancy-vs-schema orthogonality statement (§3.9 / §5 source); docs test asserts the orthogonality token |
| T-136-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan (docs + config wiring only) |
</threat_model>

<verification>
Per-task: the targeted `mix test test/mailglass/upgrade_v2_docs_test.exs --seed 0` in each `<verify>` block, plus `sort -c` on the allowlist.
Plan gate (per 136-VALIDATION.md):
- `mix test test/mailglass/upgrade_v2_docs_test.exs --seed 0` (full docs test GREEN)
- `mix mailglass.docs.check` (confirms the inbound api_stability edit disturbed no existing tier-1 required token)
- `mix format --check-formatted`
- `mix compile --warnings-as-errors` (mix.exs edit compiles)
</verification>

<success_criteria>
- `guides/upgrading-to-v2_0.md` documents Route A, Route B, the `create_schema: false` grants, the `public.mailglass_*` grep checklist, and the `lock_timeout`/`55P03` retry posture (UPG-02).
- Both `api_stability.md` files document `config :mailglass[_inbound], :schema` as a stable 2.0 surface (`Since: 2.0.0`) with the tenancy-vs-schema orthogonality statement (UPG-03).
- The guide is wired into `mix.exs` extras/groups and present in the publish allowlist (sorted) — Phase 137's release will not hit the 1.10.2 allowlist landmine.
- The docs-token + allowlist-presence test is GREEN; no existing inbound required token was removed.
</success_criteria>

<output>
Create `.planning/phases/136-upgrade-codemod-docs-api-stability/136-02-SUMMARY.md` when done
</output>
