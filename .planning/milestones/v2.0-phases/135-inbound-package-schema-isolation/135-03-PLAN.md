---
phase: 135-inbound-package-schema-isolation
plan: 03
type: execute
wave: 2
depends_on: ["135-01", "135-02"]
files_modified:
  - mailglass_inbound/test/test_helper.exs
  - .github/workflows/advisory-matrix.yml
autonomous: true
requirements: [INB-03]
must_haves:
  truths:
    - "The inbound test harness migrates via MailglassInbound.Migration.up(prefix: schema) inside the with_repo pool-override block, replacing the raw Ecto.Migrator.run path."
    - "The harness reads MAILGLASS_SCHEMA from the environment defaulting to \"public\", and aligns BOTH the migrated-table schema AND MailglassInbound.Config.schema/0 (the facade default) to that same value so facade reads/writes and migrated tables agree."
    - "Because Migration.up/1 runs CREATE SCHEMA as its first statement inside with_repo (before Sandbox.mode(:manual) and any test checkout), the schema exists before the SQL Sandbox owner starts (footgun 13 resolved)."
    - "The inbound suite runs green under BOTH schema: \"public\" and schema: \"mailglass\"."
    - "A mailglass_inbound advisory (non-blocking) CI job runs the schema: [public, mailglass] matrix axis with MAILGLASS_SCHEMA exported and mix test --seed 0."
  artifacts:
    - "mailglass_inbound/test/test_helper.exs — rewired to Migration.up/1 + MAILGLASS_SCHEMA-driven dual-schema alignment"
    - ".github/workflows/advisory-matrix.yml — new mailglass_inbound dual-schema advisory job"
  key_links:
    - "MAILGLASS_SCHEMA env → both Migration.up(prefix:) (Plan 02) AND config :mailglass_inbound, :schema (facade, Plan 01) → the two must agree or facade reads miss the migrated tables."
    - "CREATE SCHEMA inside with_repo before Sandbox.mode(:manual) → schema-must-exist-before-owner footgun resolved for free."
---

<objective>
Stand up the dual-schema validation: rewire `mailglass_inbound/test/test_helper.exs` to migrate via
`MailglassInbound.Migration.up(prefix: schema)` reading `MAILGLASS_SCHEMA` (default `"public"`) and align
the facade's `Config.schema/0` to the same value, then add a non-blocking `mailglass_inbound` CI job on the
`schema: [public, mailglass]` axis — proving the inbound suite is green under both schemas (INB-03,
D-12/D-13).

Purpose: the green-under-both proof is the validation strategy for the whole phase. It exercises Plan 01's
facade threading AND Plan 02's migration entrypoint end-to-end under a real non-public schema. This plan
depends on both landing.

Output: a dual-schema test harness + a deterministic advisory CI axis.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-inbound-package-schema-isolation/135-CONTEXT.md
@.planning/phases/135-inbound-package-schema-isolation/135-01-PLAN.md
@.planning/phases/135-inbound-package-schema-isolation/135-02-PLAN.md
</context>

<artifacts_produced>
New symbols/artifacts this plan introduces (excluded from source-grounding drift verification — net-new here):
- The rewired `mailglass_inbound/test/test_helper.exs` dual-schema migration + config-alignment block
- The `mailglass_inbound` advisory job in `.github/workflows/advisory-matrix.yml`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rewire test_helper.exs to Migration.up/1 with MAILGLASS_SCHEMA-driven dual-schema alignment</name>
  <files>mailglass_inbound/test/test_helper.exs</files>
  <read_first>
    - mailglass_inbound/test/test_helper.exs — the current harness: it computes `migrations_path`, overrides the pool from Sandbox to `DBConnection.ConnectionPool` inside `Ecto.Migrator.with_repo/2`, runs `Ecto.Migrator.run(repo, path, :up, all: true)`, restores the Sandbox pool, starts TestRepo, then `Sandbox.mode(:manual)`. Preserve the pool-override dance (still needed so the migrator checkout doesn't hang).
    - mailglass_inbound/lib/mailglass_inbound/migration.ex (from Plan 02) — `MailglassInbound.Migration.up/1` accepting `prefix:`.
    - mailglass_inbound/lib/mailglass_inbound/config.ex — `MailglassInbound.Config.schema/0` warms lazily from `Application.get_env(:mailglass_inbound, :schema, "mailglass")` into `:persistent_term` under key `{MailglassInbound.Config, :schema}`. The harness must SET `config :mailglass_inbound, :schema` to the target BEFORE any facade call AND reset the persistent_term cache (or set the env before the first `schema/0` call so the lazy warm reads the right value).
    - config/runtime.exs:42 — the core `MAILGLASS_SCHEMA` env-override template (guarded to non-empty binary). Mirror the read semantics (default when unset/empty). NOTE: inbound has no runtime.exs, so this read lives in test_helper.exs (D-12).
    - config/test.exs:15 — core pins `config :mailglass, :schema, "public"`. Inbound has NO equivalent pin today — that is the gap this task closes (align the facade default to the migrated schema).
  </read_first>
  <action>
    Rewire `test_helper.exs` (D-12): read `schema = System.get_env("MAILGLASS_SCHEMA")` and default to `"public"` when unset/empty (mirror core runtime.exs semantics). Before the migration step, set `config :mailglass_inbound, :schema` (via `Application.put_env(:mailglass_inbound, :schema, schema)`) AND reset the `MailglassInbound.Config` `:schema` `:persistent_term` cache (delete the `{MailglassInbound.Config, :schema}` key, or otherwise force `Config.schema/0` to re-warm) so the facade's `put_prefix/1` default resolves to the SAME schema the tables are migrated into — otherwise the default suite (schema "public") would migrate tables into "public" while the facade injects "mailglass", and every facade read would miss the tables. Inside the existing `Ecto.Migrator.with_repo/2` pool-override block, REPLACE the raw `Ecto.Migrator.run(repo, migrations_path, :up, all: true, log: false)` with `MailglassInbound.Migration.up(prefix: schema)`. Because `Migration.up/1` issues `CREATE SCHEMA IF NOT EXISTS` as its first statement inside `with_repo` — before `Sandbox.mode(:manual)` and any test checkout — the schema-must-exist-before-Sandbox-owner footgun is resolved for free (footgun 13). Keep the Sandbox→ConnectionPool→Sandbox restore dance and the explicit `TestRepo.start_link()` + `Sandbox.mode(:manual)` ordering intact. The `migrations_path` computation may be removed if no longer referenced. (Reading the schema once at module load is fine — Claude's Discretion; it must be set before the first `Config.schema/0` call and before the migration.)
  </action>
  <verify>
    <automated>cd mailglass_inbound && MAILGLASS_SCHEMA=mailglass mix test --seed 0 && mix test --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The harness migrates via `MailglassInbound.Migration.up(prefix: schema)` with `schema` from `MAILGLASS_SCHEMA` (default `"public"`).
    - The facade default (`Config.schema/0`) is aligned to the same `schema` (env set + persistent_term reset before first use).
    - The suite is green under BOTH `MAILGLASS_SCHEMA=mailglass` and the default (public) run, both with `--seed 0`.
  </acceptance_criteria>
  <done>The inbound suite migrates into and reads from the same configured schema, proving green under both public and mailglass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the mailglass_inbound dual-schema advisory CI job</name>
  <files>.github/workflows/advisory-matrix.yml</files>
  <read_first>
    - .github/workflows/advisory-matrix.yml — the core `core_full_suite_advisory` job (lines 20-106): the `schema: [public, mailglass]` matrix axis (`include:` rows), the Postgres service block, setup-beam + cache + deps.get + compile, the `mix ecto.create -r <TestRepo>` step, and the run step exporting `MAILGLASS_SCHEMA: ${{ matrix.schema }}`. The workflow triggers on push+PR+schedule+workflow_dispatch (lines 3-10) and is advisory (non-blocking). Mirror this job shape for inbound.
    - mailglass_inbound/config/test.exs — the inbound TestRepo is `MailglassInbound.TestRepo`; DB name `mailglass_inbound_test#{MIX_TEST_PARTITION}`; standard POSTGRES_* creds. The inbound suite runs from the `mailglass_inbound/` subdirectory (working-directory).
    - .planning/STATE.md — the known phase-45 1000-iter property-test pool flake (tcp recv: closed); D-13 pins `mix test --seed 0` to dodge it deterministically.
  </read_first>
  <action>
    Add a new `mailglass_inbound_dual_schema_advisory` job to `advisory-matrix.yml` mirroring `core_full_suite_advisory` but scoped to the inbound package: the identical `schema: [public, mailglass]` matrix axis (elixir 1.18 / otp 27 rows), the same Postgres service block, setup-beam + deps cache + `mix deps.get` + `mix compile --warnings-as-errors`, a `mix ecto.create -r MailglassInbound.TestRepo --quiet` step, and a run step exporting `MAILGLASS_SCHEMA: ${{ matrix.schema }}` running `mix test --seed 0` (D-13 — pin the seed to dodge the phase-45 property-test pool flake). Set the job's `defaults.run.working-directory: mailglass_inbound` (or `cd mailglass_inbound &&` on each step) so all mix commands run in the inbound package. Keep it ADVISORY / non-blocking (this workflow is never a required PR check per the fake-adapter-is-the-gate DNA — do not add it to any required-status gate). Name the job clearly (e.g. `Inbound Full Suite Advisory (schema ${{ matrix.schema }})`) so it is unmistakably advisory. Do not modify the existing core jobs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/advisory-matrix.yml')); j=d['jobs']; k=[n for n in j if 'inbound' in n.lower()]; assert k, 'no inbound job'; job=j[k[0]]; axis=job['strategy']['matrix']['include']; schemas={r['schema'] for r in axis}; assert {'public','mailglass'} <= schemas, schemas; print('OK', k[0], sorted(schemas))"</automated>
  </verify>
  <acceptance_criteria>
    - A `mailglass_inbound` advisory job exists in `advisory-matrix.yml` with the `schema: [public, mailglass]` matrix axis, exporting `MAILGLASS_SCHEMA: ${{ matrix.schema }}` and running `mix test --seed 0` in the inbound package.
    - The job is advisory/non-blocking (not added to any required-status gate); existing core jobs are unchanged.
  </acceptance_criteria>
  <done>A deterministic dual-schema advisory CI axis proves the inbound suite green under both public and mailglass on every push/cron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI env `MAILGLASS_SCHEMA` → test harness `prefix:` + facade `:schema` | The CI matrix value flows into `Migration.up(prefix:)` and `config :mailglass_inbound, :schema`. Both paths already validate the identifier (Plan 01/02 chokepoints); the CI axis only supplies the fixed literals `public`/`mailglass`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-06 | Repudiation | dual-schema green is claimed but never actually exercised under `mailglass` | medium | mitigate | The CI job runs the FULL inbound suite under both schemas on every push/cron; the harness aligns facade + migrated schema so a facade-vs-table schema mismatch fails loudly rather than passing vacuously. |
| T-135-07 | Denial of Service | phase-45 property-test pool flake reds the dual-schema gate nondeterministically | low | accept | `mix test --seed 0` pins the ordering to dodge the known flake (D-13); the lane is advisory (non-blocking) regardless. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test --seed 0` green (default/public run).
- `cd mailglass_inbound && MAILGLASS_SCHEMA=mailglass mix test --seed 0` green (isolated-schema run).
- `.github/workflows/advisory-matrix.yml` parses as valid YAML and contains a `mailglass_inbound` job with the `schema: [public, mailglass]` axis exporting `MAILGLASS_SCHEMA`.
</verification>

<success_criteria>
- The inbound test harness migrates via `MailglassInbound.Migration.up(prefix: schema)` and aligns the facade default to the same schema, resolving the schema-before-Sandbox-owner footgun.
- The inbound suite is green under BOTH `schema: "public"` and `schema: "mailglass"`.
- A non-blocking `mailglass_inbound` dual-schema advisory CI job runs the axis deterministically with `--seed 0`.
</success_criteria>

<output>
Create `.planning/phases/135-inbound-package-schema-isolation/135-03-SUMMARY.md` when done.
</output>