---
phase: 161-canonical-workspace-and-evidence-preservation
plan: 02
type: execute
wave: 2
depends_on: [161-01]
files_modified:
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
autonomous: true
requirements: [WSPC-03]
must_haves:
  truths:
    - "D-02: Every non-sentinel ledger row has exactly one allowed disposition and content, unique-work, reachability, and evidence support."
    - "D-03/D-07: Absence from `main`, age, detached state, or fsck unreachability alone never supports removal."
    - "WSPC-03 adjacency/order: identities sharing an OID, range boundary, or evidence block remain separately dispositioned in stable category/identity order."
    - "WSPC-03 empty-input: an empty category remains an explicit zero-result sentinel and is never fabricated into an object disposition."
    - "WSPC-03 idempotency: rerunning assessment at the same capture HEAD produces the same row identities and dispositions without duplicate rows or Git mutation."
    - statement: "WSPC-03 concurrency safety requires abort-and-recapture if canonical HEAD, stash OIDs, worktree registrations, or assessed ref OIDs change during assessment."
      verification: backstop
  artifacts:
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      provides: "Per-item content/reachability evidence and allowed disposition matrix"
      contains: "Disposition Rationale"
  key_links:
    - from: "content/unique-work and reachability evidence"
      to: "disposition"
      via: "same ledger row and evidence reference"
      pattern: "retain|handoff|merge|archive|remove"
  prohibitions:
    - statement: "MUST NOT classify work as disposable solely because it is old, detached, absent from `main`, or reported unreachable without reflogs."
      status: resolved
      verification: judgment
    - statement: "MUST NOT collapse distinct workspace or ref identities into one disposition merely because they resolve to the same object."
      status: resolved
      verification: judgment
---

<objective>
Turn the pre-mutation snapshot into a complete, evidence-backed disposition ledger without changing workspace or ref state.

Purpose: Satisfy WSPC-03 and D-02/D-03/D-07 by making every decision independently reviewable before preservation or cleanup.
Output: Assessed ledger rows with content, uniqueness, reachability, rationale, and one allowed disposition.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-01-SUMMARY.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
@MAINTAINING.md
@.planning/release-target.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove content uniqueness and reachability for every assessed identity</name>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - MAINTAINING.md
    - .planning/release-target.json
    - .planning/milestones/v2.6-phases/160-certification-documentation-and-release/160-06-SUMMARY.md
  </read_first>
  <action>Assert that canonical HEAD, worktree registration count/paths, stash selector-to-OID mapping, and inventoried ref OIDs still equal the Plan 01 capture; if any differ, append a new full pre-mutation snapshot before assessment. For each `WT-*`, `STASH-*`, `REF-*`, `RANGE-*`, `REL-*`, and `OBJ-*` row, run only quoted read-only Git commands appropriate to its identity: `git show --stat --format=fuller <oid>`, `git diff --stat`/`git diff --binary` references for dirty paths, `git rev-list --left-right --count main...<ref>`, `git log --left-right --cherry-mark --oneline main...<ref>`, `git branch --contains <oid>`, `git tag --contains <oid>`, and `git merge-base --is-ancestor <oid> <named-ref>`. Record command, output/exit meaning, content summary, whether work is patch-equivalent or unique, and exact reachability. Keep equal-OID identities separate but cross-reference shared evidence. Per D-03/D-07, unresolved command errors, ambiguous patch equivalence, dirty content, and unreachable commits remain uncertain preservation candidates.</action>
  <verify>
    <automated>inventory=.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md; rg -n 'Content and Unique-Work Evidence|Reachability Evidence|git branch --contains|git tag --contains|git merge-base --is-ancestor|git log --left-right --cherry-mark' "$inventory" &amp;&amp; test "$(rg -c '^\| (WT|STASH|REF|RANGE|REL|OBJ)-' "$inventory")" -eq "$(rg -c '^\| (WT|STASH|REF|RANGE|REL|OBJ)-.*\| EVID-[^|]+\|' "$inventory")"</automated>
  </verify>
  <acceptance_criteria>
    - Every non-sentinel item row has a stable `EVID-*` reference to content/unique-work and reachability results.
    - Dirty worktree diffs, stash contents, detached commits, release proof, and unreachable commits have content-level summaries rather than age/name-only judgments.
    - Any state drift detected after Plan 01 causes a new append-only snapshot before assessment continues.
    - Equal-OID items remain separate rows and retain independent permitted-next-action fields.
  </acceptance_criteria>
  <done>Every candidate has enough captured content and graph evidence to support or block a disposition without mutating Git state.</done>
</task>

<task type="auto">
  <name>Task 2: Assign one evidence-backed disposition and validate matrix completeness</name>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - MAINTAINING.md
  </read_first>
  <action>For each non-sentinel row, choose exactly one of `retain`, `handoff`, `merge`, `archive`, or `remove` per D-02. `remove` is eligible only when evidence proves no unique/uncertain work remains or a specific preservation/handoff prerequisite is named; `merge` records a target and remains non-executing in this phase unless already settled locally; `handoff` names Phase 162 and a concrete unresolved remote question/condition; `archive` names the future recoverable ref; `retain` names why the current ref/path remains authoritative. Add a disposition rationale that cites its `EVID-*` entries, a preservation/handoff target, and exact permitted next action. Reconcile fresh enumerator counts to stable row IDs, verify no duplicate category+identity key, sort by category then bytewise identity, and append a disposition coverage table with total/allowed/pending counts. WSPC-03 passes only when pending is zero for non-sentinel rows; uncertainty is expressed through `retain`, `handoff`, or `archive`, not a missing decision.</action>
  <verify>
    <automated>inventory=.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md; test "$(rg -c '^\| (WT|STASH|REF|RANGE|REL|OBJ)-' "$inventory")" -eq "$(rg -c '^\| (WT|STASH|REF|RANGE|REL|OBJ)-.*\| (retain|handoff|merge|archive|remove) \|' "$inventory")" &amp;&amp; rg -n 'Disposition Coverage|pending: 0|retain|handoff|merge|archive|remove' "$inventory"</automated>
  </verify>
  <acceptance_criteria>
    - Every non-sentinel row has exactly one allowed disposition, `EVID-*` citations, preservation/handoff target, and permitted next action per D-02.
    - The disposition coverage summary reports zero pending non-sentinel rows.
    - No `remove` row relies only on age, naming, detached state, absence from main, or fsck classification.
    - Re-running the assessment at unchanged input preserves row IDs/order and creates no duplicate identity rows.
    - This plan performs no branch/ref creation, merge, removal, stash change, reset, force action, garbage collection, or history rewrite.
  </acceptance_criteria>
  <done>Every inventoried identity has one independently supported disposition, with uncertain work retained, archived, or handed off rather than guessed away.</done>
</task>

</tasks>

## Artifacts this phase produces

- Updated `161-WORKSPACE-INVENTORY.md` with stable `EVID-*` blocks, content/uniqueness/reachability fields, per-row dispositions, and a zero-pending coverage summary.
- Contract: category+identity is the stable row key; equal OIDs may share evidence but never erase identity-specific dispositions.
- Contract: changed live Git inputs trigger an append-only recapture before assessment continues.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git object graph → human disposition | Reachability is evidence, not a value judgment; content must also be assessed. |
| duplicated OID → distinct identities | Shared object identity must not erase separate workspace/ref ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-01 | Tampering / Denial of Service | unique local work | high | mitigate | Preserve uncertainty by default and require content plus reachability evidence before `remove`. |
| T-161-02 | Tampering | inventory completeness | high | mitigate | Reconcile counts, stable keys, allowed dispositions, and zero-pending coverage. |
| T-161-03 | Repudiation | release proof | medium | mitigate | Cite file hashes/diffs and use Phase 162 handoffs for external meaning. |
| T-161-04 | Tampering | shell/ref ambiguity | medium | mitigate | Quote OIDs/refs/paths and bind outputs to stable `EVID-*` references. |
</threat_model>

<verification>
Re-run Plan 02 commands at the recorded capture HEAD. Independently sample every dirty, stash, detached, unreachable, and release-proof row; verify its disposition cites both content and reachability evidence and that no state-changing Git command appears in captured execution history.
</verification>

<success_criteria>
- WSPC-03 and D-02/D-03/D-07 are complete with zero pending item rows.
- Adjacency, empty, ordering, idempotency, and concurrency candidates have explicit or backstop coverage.
- All descriptor-less safety prohibitions remain visible for verification judgment.
</success_criteria>

<output>
Create `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-02-SUMMARY.md` when done.
</output>
