---
phase: 161-canonical-workspace-and-evidence-preservation
plan: 05
type: execute
wave: 5
depends_on: [161-04]
files_modified:
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
autonomous: true
gap_closure: true
requirements: [WSPC-02]
must_haves:
  truths:
    - "D-04: The newest canonical-main evidence identifies `/Users/jon/projects/mailglass` on branch `main` as the sole canonical workspace."
    - "D-05: The newest evidence records execution-time HEAD and the exact live `origin/main...HEAD` behind/ahead counts without changing or normalizing Git history."
    - "D-06: A clean canonical working tree is reported separately from the honest non-release-clean verdict while upstream settlement remains outstanding."
    - "The stale e2be2c94 / behind 0 / ahead 29 capture remains immutable history, and a later timestamped recapture supersedes it only as the current-state claim."
    - "The just-appended branch, HEAD, status, upstream, behind count, and ahead count agree with fresh live Git output at verification time."
  artifacts:
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      provides: "Append-only execution-time canonical-main recapture"
      contains: "Canonical Main Recapture"
  key_links:
    - from: "git branch/status/rev-parse/rev-list output"
      to: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      via: "one timestamped field-by-field canonical-main evidence block"
      pattern: "Canonical Main Recapture"
    - from: "newest appended evidence fields"
      to: "fresh live Git state"
      via: "automated exact-value comparison, excluding only the evidence file being appended from the post-write cleanliness projection"
      pattern: "behind|ahead|non-release-clean"
  assumptions:
    - "FLAGGED (WSPC-02 unclassified): the ahead count and HEAD are capture-time facts that may advance with later documentation commits; freshness is proven by timestamped append-only recapture, not by treating any recorded count as permanent."
  prohibitions:
    - statement: "MUST NOT rewrite, delete, or silently correct the prior Final Reconciliation capture; the correction is a new evidence block."
      status: flagged-unverified
      verification: judgment
    - statement: "MUST NOT use fetch, pull, checkout, reset, merge, rebase, ref mutation, cleanup, or any remote operation to manufacture the captured canonical state."
      status: flagged-unverified
      verification: judgment
    - statement: "MUST NOT call the canonical workspace release-clean merely because its porcelain status is empty while upstream settlement remains outstanding."
      status: flagged-unverified
      verification: judgment
---

<objective>
Close the WSPC-02 verification gap by appending one fresh, read-only canonical-main recapture whose exact values are checked against execution-time Git state.

Purpose: Restore the current-state truth without weakening the immutable evidence contract from Plans 01-04, implementing D-04 through D-06 at the live HEAD.
Output: One timestamped `Canonical Main Recapture` block appended to `161-WORKSPACE-INVENTORY.md`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-VERIFICATION.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-04-SUMMARY.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
@MAINTAINING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Append and live-verify the canonical-main recapture</name>
  <precondition>From `/Users/jon/projects/mailglass`, `git branch --show-current` returns `main`, `git rev-parse --abbrev-ref --symbolic-full-name '@{upstream}'` succeeds, and `git status --porcelain --untracked-files=all` is empty before the evidence file is edited; otherwise stop without changing the inventory and report the exact mismatch.</precondition>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-VERIFICATION.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-04-SUMMARY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - MAINTAINING.md
  </read_first>
  <action>From the exact canonical root `/Users/jon/projects/mailglass`, collect one observation bundle before editing: UTC timestamp via `date -u +%Y-%m-%dT%H:%M:%SZ`; repository top-level via `git rev-parse --show-toplevel`; branch via `git branch --show-current`; full HEAD via `git rev-parse HEAD`; porcelain status via `git status --porcelain --untracked-files=all`; upstream via `git rev-parse --abbrev-ref --symbolic-full-name '@{upstream}'`; and divergence via `git rev-list --left-right --count '@{upstream}...HEAD'`, labeling the left value `behind` and the right value `ahead`. Keep these values together in a task-owned temporary snapshot so the document cannot mix observations from different HEADs. Append, after all existing content, a new heading exactly `## Canonical Main Recapture — &lt;UTC timestamp&gt;` and a two-column field table with exact labels `Capture timestamp (UTC)`, `Canonical path`, `Branch`, `HEAD`, `Status command`, `Porcelain status record count`, `Working tree`, `Upstream`, `Behind`, `Ahead`, and `Release verdict`. Serialize every field row exactly as a pipe, one space, the exact label, one space, a pipe, one space, the value enclosed in backticks, one space, and a closing pipe; equivalently, each row must have the literal shape ``| Label | `value` |`` with no added columns or text. This contract applies to every listed row, including the exact row ``| Status command | `git status --porcelain --untracked-files=all` |`` and the captured timestamp row. Record the full captured values, zero porcelain records as `clean`, and the verdict `non-release-clean` because D-06 requires explicit upstream settlement and the current divergent range remains unsettled; identify nonzero drift when present. State that this block supersedes older blocks only for current-state reporting and that the earlier e2be2c94 / behind 0 / ahead 29 Final Reconciliation remains immutable evidence. Per D-04/D-05, do not alter branch, upstream, commits, refs, or working-tree content other than appending this evidence file. Use no network, remote-release interpretation, cleanup, or state-changing Git command. Delete only the task-owned temporary snapshot after the append and comparisons complete.</action>
  <verify>
    <automated>inventory=.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md; baseline=$(git show HEAD:"$inventory" | awk '/^## Canonical Main Recapture — / {n++} END {print n+0}'); current=$(awk '/^## Canonical Main Recapture — / {n++} END {print n+0}' "$inventory"); test "$current" -eq "$((baseline + 1))" &amp;&amp; test -z "$(git diff --unified=0 -- "$inventory" | awk '!/^--- / &amp;&amp; !/^\+\+\+ / &amp;&amp; /^-/ {print}')" &amp;&amp; block=$(awk '/^## Canonical Main Recapture — / {capture=1; text=""} capture {text=text $0 ORS} END {printf "%s", text}' "$inventory") &amp;&amp; captured_timestamp=$(printf '%s' "$block" | sed -n 's/^| Capture timestamp (UTC) | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_path=$(printf '%s' "$block" | sed -n 's/^| Canonical path | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_branch=$(printf '%s' "$block" | sed -n 's/^| Branch | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_head=$(printf '%s' "$block" | sed -n 's/^| HEAD | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_status_command=$(printf '%s' "$block" | sed -n 's/^| Status command | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_status_count=$(printf '%s' "$block" | sed -n 's/^| Porcelain status record count | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_tree=$(printf '%s' "$block" | sed -n 's/^| Working tree | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_upstream=$(printf '%s' "$block" | sed -n 's/^| Upstream | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_behind=$(printf '%s' "$block" | sed -n 's/^| Behind | `\([^`]*\)` |$/\1/p') &amp;&amp; captured_ahead=$(printf '%s' "$block" | sed -n 's/^| Ahead | `\([^`]*\)` |$/\1/p') &amp;&amp; set -- $(git rev-list --left-right --count '@{upstream}...HEAD') &amp;&amp; test -n "$captured_timestamp" &amp;&amp; printf '%s' "$captured_timestamp" | rg -q '^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$' &amp;&amp; test "$captured_path" = "$(git rev-parse --show-toplevel)" &amp;&amp; test "$captured_branch" = "$(git branch --show-current)" &amp;&amp; test "$captured_head" = "$(git rev-parse HEAD)" &amp;&amp; test "$captured_status_command" = "git status --porcelain --untracked-files=all" &amp;&amp; test "$captured_upstream" = "$(git rev-parse --abbrev-ref --symbolic-full-name '@{upstream}')" &amp;&amp; test "$captured_behind" = "$1" &amp;&amp; test "$captured_ahead" = "$2" &amp;&amp; test "$captured_status_count" = "0" &amp;&amp; test "$captured_tree" = "clean" &amp;&amp; test -z "$(git status --porcelain --untracked-files=all -- . ':(exclude).planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md')" &amp;&amp; printf '%s' "$block" | rg -q '^\| Release verdict \| `non-release-clean` \|$' &amp;&amp; printf '%s' "$block" | rg -q 'e2be2c941300fb0de3194bef6d62e087e96b5722|e2be2c94' &amp;&amp; git diff --check -- "$inventory"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the pre-existing `Final Reconciliation` block, including e2be2c94 and behind 0 / ahead 29, remains present and byte-preserved; the working-tree diff for the inventory contains additions and no removed lines.
    - Behavior assertion: exactly one new `Canonical Main Recapture` block is appended relative to `HEAD`, with a UTC timestamp and exact canonical path, branch, full HEAD, status, upstream, behind, ahead, and release-verdict fields.
    - Command assertion: the automated gate compares the newest appended path, branch, HEAD, upstream, behind, and ahead values to fresh live Git commands and proves the working tree is clean after excluding only the intentionally modified evidence file.
    - D-04/D-06 assertion: the block names `/Users/jon/projects/mailglass` on `main` as canonical, records `clean` separately, and retains the honest `non-release-clean` verdict while settlement remains outstanding.
    - Safety assertion: Git branch/ref/object/worktree/stash state and remote state are unchanged; no previous capture is edited or deleted.
  </acceptance_criteria>
  <done>WSPC-02's stale-current-state gap is closed by a fresh, append-only canonical-main record whose exact live values pass the automated comparison.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new symbols or files.
- Appends one timestamped `Canonical Main Recapture` evidence block to `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md`; all prior captures remain unchanged.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | One explained clean canonical `main`; preserve accountability for all workspace/Git/release residue | 01-05 | COVERED | Plan 05 refreshes only the stale canonical-current-state claim; Plans 01-04 retain all inventory/disposition/preservation coverage. |
| REQ | WSPC-01 | Complete pre-mutation inventory | 01, 04 | COVERED | Existing immutable capture and final inventory remain untouched. |
| REQ | WSPC-02 | Explained canonical `main`, clean tree, upstream, and ahead/behind state | 01, 04, 05 | COVERED | Plan 05 appends and live-compares the execution-time current-state fields. |
| REQ | WSPC-03 | Evidence-backed disposition for every item | 02, 04 | COVERED | Gap plan does not change identity or disposition evidence. |
| REQ | WSPC-04 | Recoverability before normal Git removal | 03, 04 | COVERED | Recovery refs and zero-remove outcome remain immutable and out of the recapture mutation surface. |
| RESEARCH | — | Record live ahead/behind count, timestamp, HEAD, and full range separately at every final recapture | 05 | COVERED | One atomic observation bundle feeds the appended field table and live-value gate. |
| RESEARCH | — | Native read-only Git boundary; no new dependency/tooling | 05 | COVERED | Fixed local Git commands only; external-API detector is false. |
| CONTEXT | D-01 | Preserve tracked inventory evidence before mutation | 01, 05 | COVERED | New capture appends; it does not rewrite prior evidence. |
| CONTEXT | D-02 | Per-item disposition and support | 02, 04 | COVERED | Existing rows are not modified. |
| CONTEXT | D-03 | Uncertain work remains preserved | 01-04 | COVERED | No workspace/object disposition is touched. |
| CONTEXT | D-04 | Root `main` is the sole canonical workspace | 01, 04, 05 | COVERED | Exact root and branch are freshly asserted. |
| CONTEXT | D-05 | Explain semantic range without normalization | 01, 04, 05 | COVERED | Live HEAD/counts are captured without Git mutation; prior semantic explanation remains intact. |
| CONTEXT | D-06 | Clean tree alone is not release-clean | 01, 04, 05 | COVERED | Status and release verdict are separate fields. |
| CONTEXT | D-07 | Retain/handoff until evidence exists | 01-03 | COVERED | Existing preservation outcomes remain untouched. |
| CONTEXT | D-08 | Preserve before ordinary removal; prohibit destructive cleanup | 03, 04 | COVERED | Plan 05 performs no cleanup or preservation mutation. |
| CONTEXT | D-09 | Remote release meaning belongs to Phase 162 | 01-05 | COVERED | No remote query, operation, or interpretation is introduced. |

Excluded without gap: all CONTEXT Deferred Ideas remain assigned to Phases 162-164 or outside v2.7. The specless edge rows for WSPC-01 and WSPC-03 remain owned by the executed plans and backstop review; this recapture does not duplicate or fabricate their resolution. The materially touched WSPC-02 unclassified freshness edge is retained as a flagged assumption. The deterministic external-API detector is false for this local Git/file append, the assumption-delta detector is false because no identity model changes, and the schema-path detector is false because no supported ORM schema path is in scope.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live local Git state → appended inventory evidence | Branch, HEAD, status, upstream, and divergence values become a durable claim. |
| immutable prior captures → newest current-state claim | The correction must supersede only freshness, never historical evidence. |
| appended evidence → automated verification | Parsed field values must match a second fresh read of the same canonical repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-05-01 | Tampering / Repudiation | canonical-main recapture | high | mitigate | Collect one observation bundle, append every exact field, and compare the newest block to fresh Git output. |
| T-161-05-02 | Tampering | prior immutable evidence | high | mitigate | Require one appended block and reject any removed inventory line relative to `HEAD`. |
| T-161-05-03 | Tampering | shell/ref parsing | medium | mitigate | Use fixed quoted Git arguments, assign left/right counts explicitly as behind/ahead, and avoid `eval` or dynamic ref/path execution. |
| T-161-05-04 | Elevation of Privilege | release-state boundary | medium | mitigate | Perform no remote operation or release interpretation; preserve the Phase 162 handoff and non-release-clean verdict. |
</threat_model>

<verification>
Run the task's automated gate from `/Users/jon/projects/mailglass`. It must prove one append-only capture, exact live path/branch/HEAD/upstream/divergence equality, clean status outside the intentionally modified inventory, the non-release-clean verdict, retention of the stale historical block, and `git diff --check` success.
</verification>

<success_criteria>
- WSPC-02 is no longer blocked by a stale final canonical-main claim.
- D-04 through D-06 are demonstrated at execution-time current HEAD with exact live values.
- Plans 01-04 evidence remains immutable, and no Git or remote state is mutated.
- All four Phase 161 requirements remain covered across Plans 01-05 with no source-audit gap.
</success_criteria>

<output>
Create `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-05-SUMMARY.md` when done.
</output>
