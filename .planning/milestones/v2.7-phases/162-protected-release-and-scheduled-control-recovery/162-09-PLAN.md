---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 09
type: execute
wave: 5
depends_on: [162-06, 162-07]
files_modified:
  - dev/mix/tasks/mailglass.repo.hygiene.ex
  - test/mix/tasks/mailglass.repo.hygiene_test.exs
autonomous: true
gap_closure: true
requirements: [AUTO-04]
must_haves:
  truths:
    - "D-05 / AUTO-04: Repository hygiene can identify CI from the SHA checked out by a scheduled detached-HEAD run without inventing a branch name or substituting a manual control run."
    - "D-06: `ci_state/1` queries `ci.yml` with the exact `git rev-parse HEAD` SHA, validates the returned `headSha`, and feeds its pass/blocked/cannot-check result into the existing one-map CLI/summary/artifact contract."
    - "AUTO-04 detached-HEAD edge: an executable fixture actually detaches HEAD, makes `git branch --show-current` empty, requires the SHA selector in fake-`gh` argv, and proves a matching completed-success run passes the CI check."
    - "An unavailable CI query remains `cannot-check`, while a completed run for a different `headSha` or a non-successful run remains policy `blocked`; neither can become pass."
  artifacts:
    - path: "dev/mix/tasks/mailglass.repo.hygiene.ex"
      provides: "Commit-selected `ci.yml` lookup with exact returned-head validation"
      contains: "--commit"
    - path: "test/mix/tasks/mailglass.repo.hygiene_test.exs"
      provides: "Argument-sensitive detached-HEAD scheduled CI fixture"
      contains: "checkout --detach"
  key_links:
    - from: "git rev-parse HEAD"
      to: "gh run list --workflow ci.yml"
      via: "the exact checkout SHA is passed with `--commit` even when the current branch is empty"
      pattern: "--commit|headSha"
    - from: "GitHub Actions run JSON"
      to: "ci_state check status"
      via: "pass requires returned `headSha` equality plus completed/success; absence or mismatch cannot pass"
      pattern: "headSha|completed|success"
    - from: "ci_state check"
      to: "repo-hygiene.json and Actions summary"
      via: "the unchanged audit result map and existing artifact-first renderer"
      pattern: "checks|ci_state"
  assumptions:
    - "FLAGGED (AUTO-01 unclassified): the executed release reconciliation remains append-only and complete; this Mix task/test repair does not alter its source rows."
    - "FLAGGED (AUTO-02 adjacency/empty/ordering): release identity equality, explicit empty categories, and stable disposition ordering remain governed by the executed reconciliation contract."
    - "FLAGGED (AUTO-05 unclassified): the executed post-publish serializer remains the exhaustive resolver-outcome contract; this repository-hygiene repair does not change it."
  prohibitions:
    - statement: "MUST NOT report CI as checked or green unless the returned run's immutable head SHA equals the scheduled checkout SHA and the run completed successfully."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
    - statement: "MUST NOT substitute a manual run, inferred branch name, or unrelated latest CI run for scheduled checkout evidence."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Close the repository-hygiene scheduled blocker by querying CI with the immutable checked-out SHA and behaviorally proving the path from a real detached HEAD to a validated result.

Purpose: Restore truthful AUTO-04 scheduled evidence per D-05/D-06 without changing workflow topology, permissions, or three-state aggregation.
Output: A commit-selected `ci_state/1` query and an argument-sensitive detached-HEAD ExUnit fixture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-03-SUMMARY.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
@test/mix/tasks/mailglass.repo.hygiene_test.exs
@.github/workflows/repo-hygiene.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace scheduled detached HEAD through an exact-SHA CI query and validated result</name>
  <reversibility rating="reversible">The selector changes only the internal read-only `ci_state/1` query and its fixture; no persisted data, public API, workflow permission, or release contract changes.</reversibility>
  <files>dev/mix/tasks/mailglass.repo.hygiene.ex, test/mix/tasks/mailglass.repo.hygiene_test.exs</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-03-SUMMARY.md
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - test/mix/tasks/mailglass.repo.hygiene_test.exs
    - .github/workflows/repo-hygiene.yml
  </read_first>
  <behavior>
    - "Detached schedule: current branch is empty, HEAD is a 40-character SHA, fake `gh` requires `--commit <that SHA>`, and matching completed-success `headSha` yields `ci_state: pass`."
    - "Wrong-head fixture: the same exact query returns a different `headSha`; `ci_state` is blocked and cannot report green."
    - "Unavailable query fixture: `gh run list` exits nonzero; `ci_state` is cannot-check with the command error retained."
    - "The aggregate still follows cannot-check over blocked over pass and the existing JSON/text/summary artifact spellings remain unchanged."
  </behavior>
  <action>Start RED in `test/mix/tasks/mailglass.repo.hygiene_test.exs` by creating a ready disposable repository, recording `git rev-parse HEAD`, running an actual detached checkout, and asserting `git branch --show-current` is empty before calling `Hygiene.audit/1`. Make the test's fake `gh` parse and log argv, reject a missing, empty, or unequal commit selector, require `run list --workflow ci.yml --commit <recorded-sha> --limit 1 --json headSha,conclusion,status,url`, and return a completed-success row whose `headSha` equals the selector. Assert the `ci_state` check is pass even though the independent detached upstream check may make the aggregate cannot-check. Add a wrong-head response asserting blocked and a nonzero query response asserting cannot-check; these cases must use the same argument-validating shim rather than returning a matching run regardless of argv. Then change only `ci_state/1` in `dev/mix/tasks/mailglass.repo.hygiene.ex`: remove the current-branch lookup and replace `--branch <branch>` with `--commit <sha>`, where `sha` is the exact trimmed output of `git rev-parse HEAD`. Retain `--workflow ci.yml`, limit, JSON fields, and the explicit returned `headSha == sha`, `status == completed`, and `conclusion == success` pass gate. Keep no-run/wrong-head/non-success as blocked, command failure as cannot-check, the existing check order, three-state precedence, CLI exits, JSON/text encoding, safe-action authority, and `.github/workflows/repo-hygiene.yml` untouched per D-05/D-06. Do not add a branch fallback or query an unrelated latest run.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The fixture performs a real detached checkout and proves the branch name is empty before audit execution.
    - The fake `gh` fails unless argv contains the exact checked-out SHA after `--commit`; it no longer returns a green row independently of arguments.
    - A matching completed-success `headSha` passes `ci_state`; a different `headSha`, absent run, incomplete run, or failed conclusion remains blocked; command failure remains cannot-check.
    - Existing pass/blocked/cannot-check aggregate, text, JSON, nonzero-exit, summary, and artifact-source tests remain green.
    - `.github/workflows/repo-hygiene.yml` schedule, detached checkout behavior, permissions, concurrency, job topology, and artifact-first summary/upload remain unchanged.
  </acceptance_criteria>
  <done>Scheduled repository hygiene identifies CI by immutable checkout SHA and cannot claim green from an empty branch selector or unrelated run.</done>
</task>

</tasks>

## Artifacts this phase produces

- `dev/mix/tasks/mailglass.repo.hygiene.ex` — `ci_state/1` uses the existing checkout `sha` as `gh run list --commit` selector and retains returned `headSha` validation.
- `test/mix/tasks/mailglass.repo.hygiene_test.exs` — detached-HEAD, exact-selector, wrong-head, and unavailable-query fixtures; test-local argument logging/validation support.
- Created symbols, CLI flags, result fields, and new files: none; `--commit` is an existing `gh` selector used internally, and the existing `sha` detail field remains the evidence identity.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain blocked release state while controls report truthful bounded outcomes | 01-09 | COVERED | Executed reconciliation/control work plus both Wave 5 blocker repairs. |
| REQ | AUTO-01 | Evidence-backed release narrative | 01, 05 | COVERED | Already verified; Plans 08-09 preserve it unchanged. |
| REQ | AUTO-02 | Explicit protected-merge/retire/retain dispositions | 01, 05 | COVERED | Already verified; Plans 08-09 add no release-state mutation. |
| REQ | AUTO-03 | Truthful proposal-only control/schedule result without authority growth | 02, 05, 06, 08 | COVERED | Plan 08 closes empty-digest/no-proposal schedule behavior. |
| REQ | AUTO-04 | Three-state hygiene with truthful scheduled CI evidence | 03, 05, 09 | COVERED | Plan 09 closes detached-HEAD selection and head-SHA validation. |
| REQ | AUTO-05 | Exact immutable post-publish proof or bounded blocked/inapplicable result | 04, 05, 07 | COVERED | Already verified; Plans 08-09 preserve it unchanged. |
| RESEARCH | — | Existing release topology and policy owner remain authoritative | 08 | COVERED | Discovery/reporting only; exact protected gate remains unchanged. |
| RESEARCH | — | Mix task owns hygiene result; workflow renders identical JSON | 09 | COVERED | CI selection changes inside `ci_state/1`; one-map rendering remains unchanged. |
| RESEARCH | — | No new package, service, workflow, or API surface | 08, 09 | COVERED | Existing GitHub CLI and ExUnit seams only; `COVERAGE.md` remains applicable. |
| CONTEXT | D-01 | Append-only blocked reconciliation | 01, 05; preserved by 08-09 | COVERED | No ledger edit. |
| CONTEXT | D-02 | Fresh facts and cannot-check for unavailable evidence | 01, 05, 08, 09 | COVERED | Query failures remain cannot-check. |
| CONTEXT | D-03 | One safe disposition and retained evidence | 01, 05; preserved by 08-09 | COVERED | No PR/ref/evidence mutation. |
| CONTEXT | D-04 | Ordinary release triggers remain proposal-only | 02, 05, 06, 08 | COVERED | Idle pending adds no authority. |
| CONTEXT | D-05 | Separate control/scheduled provenance | 02, 03, 05, 08, 09 | COVERED | Real schedule event/run and checkout SHA are retained. |
| CONTEXT | D-06 | Bounded agreeing machine/human outcomes | 02, 03, 05, 06, 08, 09 | COVERED | Plan 08 fixes idle result; Plan 09 restores the scheduled CI input to the existing result map. |
| CONTEXT | D-07 | Exact immutable post-publish target | 04, 05, 07 | COVERED | Out of the blocker edit set and preserved. |
| CONTEXT | D-08 | Unpublished remains blocked; no forced release | 04, 05, 07; preserved by 08 | COVERED | Idle proposal pending is not publication proof. |

Descriptor-less probe coverage: AUTO-03 adjacency/empty/ordering and AUTO-04 detached-HEAD/unavailable-selector are direct Wave 5 truths and executable fixtures. AUTO-01 unclassified, AUTO-02 adjacency/empty/ordering, and AUTO-05 unclassified remain explicitly flagged preservation assumptions across Plans 08-09. No probe item is dismissed or silently omitted. Deferred Phase 163 timeout repairs, Phase 164 closeout work, the post-publish polling warning, the stale-branch-classification warning, and SEED-006 remain excluded.

Prohibition-probe result: routine implementation correctness was dropped, while the phase-specific truth/authority prohibitions are retained under `must_haves.prohibitions` without `check_*` descriptors. Canonical GitHub access-control risks are covered by the threat registers rather than duplicated as bespoke prohibitions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detached Git checkout → CI selector | An empty branch name cannot identify the scheduled checkout's CI evidence. |
| GitHub run JSON → hygiene check | A returned run is untrusted until its immutable head SHA and terminal success state match. |
| Mix result → workflow summary/artifact | The repaired check must continue through the existing one-map three-state evidence path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-33 | Spoofing / Tampering | CI run selection | high | mitigate | Select by exact checked-out commit and make the fake reject missing or unequal `--commit` argv. |
| T-162-34 | Tampering | returned CI run | high | mitigate | Retain exact `headSha` equality plus completed/success checks; wrong-head and non-success fixtures cannot pass. |
| T-162-35 | Repudiation | scheduled evidence | high | mitigate | Preserve the checkout SHA in check details and never substitute branch inference or a manual control run. |
| T-162-36 | Elevation of Privilege | repository-hygiene workflow | medium | mitigate | Leave workflow permissions, triggers, topology, and safe-action behavior unchanged; the change is a read-only selector. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | Auth/query failure remains cannot-check with its error; it cannot become absence, drift, or pass. |
| V4 Access Control | Applicable | Existing read-only workflow permissions and Mix safe-action boundaries are unchanged and regression-checked by the workflow source test. |
| V5 Input Validation | Applicable | The test validates the exact 40-character checkout SHA in argv and response, plus completed/success terminal fields. |
| V6 Cryptography | N/A | The task compares Git object identities but introduces no cryptographic operation or digest contract. |
</threat_model>

<verification>
Run the focused hygiene suite and inspect the fake-`gh` log to confirm `--commit` receives the detached checkout SHA. Then run `mix test test/scripts/release_trigger_recovery_test.exs test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0` as the Wave 5 local blocker gate. A future real scheduled run must still be observed and retained separately; local fixtures do not constitute scheduled-run proof.
</verification>

<success_criteria>
- AUTO-04's detached-HEAD scheduled CI blocker is closed by a real detached fixture and exact argument assertions.
- D-05/D-06 three-state artifact agreement and schedule/control separation remain intact.
- No workflow, permission, authority, stale-branch policy, polling timeout, API surface, dependency, or schema change is introduced.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-09-SUMMARY.md` when done.
</output>
