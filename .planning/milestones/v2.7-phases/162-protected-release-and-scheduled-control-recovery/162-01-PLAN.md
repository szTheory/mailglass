---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md
  - test/scripts/phase_162_release_reconciliation_test.exs
  - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VALIDATION.md
autonomous: true
requirements: [AUTO-01, AUTO-02]
must_haves:
  truths:
    - "D-01: One append-only record joins PR #222, exact candidate/published identities, tags, Hex versions/checksums, the tracked target, WT-03 deltas, and release/recovery refs without treating the candidate as authorized."
    - "D-02: Every time-sensitive source has an execution-time capture timestamp and reports `cannot-check` when acquisition is unavailable; `authorized` plus `publication: not_started` confers no release authority."
    - "D-03: PR #222 and every scoped stale release/recovery branch or check receives exactly one `protected-merge`, `retire`, or `retain` disposition with evidence or a named recovery condition."
    - "AUTO-02 adjacency: equal SHAs, tag targets, versions, or branch names do not merge distinct evidence identities; exact equality is recorded while each identity keeps its own row and outcome."
    - "AUTO-02 empty-input: an empty scoped PR, stale-branch, or check category is represented by an explicit zero-count `NONE-*` row; null or unavailable remote responses are `cannot-check`, never empty."
    - "AUTO-02 ordering: evidence rows are stable by category and bytewise immutable identity, so equal comparison keys do not produce unstable dispositions."
    - statement: "AUTO-01 unclassified edge assumption: the reconciliation is complete only when every canonical source named by D-01 has a row, even if its observation is `cannot-check`."
      verification: backstop
      status: flagged
  artifacts:
    - path: ".planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md"
      provides: "Append-only release-state evidence and one-outcome disposition ledger"
      contains: "Release-State Capture"
    - path: "test/scripts/phase_162_release_reconciliation_test.exs"
      provides: "Deterministic reconciliation schema and coverage contract"
      contains: "Phase162ReleaseReconciliationTest"
    - path: ".planning/phases/162-protected-release-and-scheduled-control-recovery/162-VALIDATION.md"
      provides: "Final task-to-proof map with Wave 0 completion state"
      contains: "wave_0_complete"
  key_links:
    - from: "GitHub PR/ref/check, Git tag/release, Hex, and Phase 161 evidence"
      to: ".planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md"
      via: "timestamped source rows keyed by immutable SHA/version/checksum"
      pattern: "Captured UTC|Immutable identity|Observation|Disposition"
    - from: ".planning/release-target.json"
      to: ".planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md"
      via: "exact lifecycle, proposal SHA, digest, version, and publication comparison"
      pattern: "publication.*not_started|candidate_digest|proposal_head"
  assumptions:
    - "FLAGGED (AUTO-01 unclassified): every canonical input listed by D-01 is required even when fresh remote acquisition yields `cannot-check`; no narrower source set is assumed complete."
  prohibitions:
    - statement: "MUST NOT destroy or rewrite retained uncertain release evidence merely to make the blocked state appear settled."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
    - statement: "MUST NOT present unavailable, manual-only, or stale observations as fresh scheduled or published proof."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Create the production tracer for Phase 162: capture one real release identity from source through report and explicit disposition, then expand the same append-only contract across every release-state identity.

Purpose: Make AUTO-01 and AUTO-02 reviewable before any control repair or release mutation, implementing D-01 through D-03.
Output: `162-RELEASE-RECONCILIATION.md`, its deterministic ExUnit contract, and the finalized validation map.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-RESEARCH.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-PATTERNS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VALIDATION.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv
@.planning/release-target.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one PR-to-disposition release-state path end to end</name>
  <reversibility rating="costly">The tracked evidence schema is consumed by later plans and Phase 164, so renaming fields requires coordinated test and closeout updates.</reversibility>
  <files>test/scripts/phase_162_release_reconciliation_test.exs, .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RESEARCH.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-PATTERNS.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/release-target.json
    - test/scripts/release_policy_contract_test.exs
  </read_first>
  <behavior>
    - "A PR #222 row is valid only with capture UTC, source command/URL, head SHA, base SHA, check/mergeability/auto-merge observation, and exactly one disposition or named recovery condition."
    - "An unavailable GitHub query becomes a `cannot-check` observation and cannot be rendered as absence, retirement authority, or protected-merge eligibility."
  </behavior>
  <action>Start RED by creating `test/scripts/phase_162_release_reconciliation_test.exs` with module `Mailglass.Scripts.Phase162ReleaseReconciliationTest` and a parser for the tracked Markdown matrices. Then create `162-RELEASE-RECONCILIATION.md` with an immutable capture header (`Captured UTC`, canonical HEAD, method), source ledger, evidence matrix, and disposition matrix. Run read-only `gh pr view 222 --json number,state,headRefName,headRefOid,baseRefName,baseRefOid,mergeStateStatus,autoMergeRequest,url`, `gh pr checks 222 --json name,state,link,bucket`, and exact local `git`/`jq` reads. Carry one production PR #222 row through capture, comparison with `.planning/release-target.json`, report, and one D-03 disposition. Per D-01/D-02, identify current candidate, ledger proposal, and published identities separately and record `cannot-check` for acquisition failure. Do not merge, close, enable auto-merge, create/delete refs, tag, publish, push, or modify retained evidence.</action>
  <verify>
    <automated>mix test test/scripts/phase_162_release_reconciliation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The focused test fails before the artifact exists or when the PR row lacks any required field, then passes with the committed record.
    - The tracer names PR #222 head/base SHAs, source and capture time, observed checks/auto-merge state, ledger identity comparison, and exactly one D-03 outcome.
    - The artifact states that `authorized` plus `publication: not_started` is blocked reconciliation evidence, not merge/tag/publication authorization per D-01/D-02.
    - Every remote query is read-only and acquisition failure produces `cannot-check` with a recovery command.
  </acceptance_criteria>
  <done>A real maintainer can trace PR #222 from fresh source evidence to one safe, inspectable disposition without crossing the protected release boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the reconciliation to every candidate, publication, branch, check, and retained-evidence identity</name>
  <files>test/scripts/phase_162_release_reconciliation_test.exs, .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md, .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VALIDATION.md</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md
    - test/scripts/phase_162_release_reconciliation_test.exs
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VALIDATION.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv
    - .planning/release-target.json
    - .planning/publish/mailglass-publish-summary.json
    - .planning/publish/mailglass_admin-publish-summary.json
    - .planning/publish/mailglass_inbound-publish-summary.json
    - scripts/release_policy.exs
  </read_first>
  <behavior>
    - "Every scoped identity has exactly one disposition; equal OIDs remain separate rows and empty categories get an explicit zero row."
    - "Rows sort by category then bytewise immutable identity, and every public package observation carries exact version plus checksum."
  </behavior>
  <action>Extend the test first to enforce required source fields, stable category/identity ordering, explicit `NONE-*` rows, exactly one disposition per scoped PR/branch/check/ref, and presence of all D-01 sources. Append fresh read-only observations for PR/checks, release/recovery branches and preservation refs, exact tags/releases, Hex package versions/checksums, `.planning/release-target.json`, canonical publish summaries, the WT-03 detached SHA plus exact retained diff hash, and relevant Phase 161 recovery refs. Use exact SHA/version/digest/checksum keys and keep coincident identities distinct. Apply the resolved PR #222 rule: if fresh head SHA, base SHA, content digest, and required checks satisfy the named protected recovery condition, retain it for the existing exact candidate-digest protected dispatch; if any immutable identity or policy prerequisite mismatches, retire it with the exact mismatch reason. Evidence capture itself never merges it, and no ordinary trigger gains authority. Give every other stale release/recovery branch and check one evidence-backed `retire` or `retain` outcome with a named recovery condition. Append rather than rewrite the tracer capture. Add executable assertions that prior capture blocks remain byte-present and that unavailable observations cannot satisfy freshness or scheduled/published proof. Update `162-VALIDATION.md` task IDs to the actual plan/wave ownership and retain `wave_0_complete: false`; Plans 02-04 own the remaining missing contract cases and Plan 05 is the first dependency-safe owner allowed to set it true.</action>
  <verify>
    <automated>mix test test/scripts/phase_162_release_reconciliation_test.exs test/scripts/release_policy_test.exs test/scripts/release_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every D-01 canonical source has at least one timestamped evidence row or an explicit `cannot-check` row with source and recovery command.
    - Each PR #222, release/recovery branch, and scoped check has exactly one D-03 outcome; none is auto-merge-armed or unexplained.
    - Package facts include the exact three-package version set and checksums; tags include exact commit SHAs; the ledger and WT-03 identities remain distinct.
    - Equal identities remain separate rows, empty categories use explicit zero rows, and the test proves stable category/bytewise-identity ordering.
    - Validation retains `wave_0_complete: false`, maps tasks to execution Waves 1-3, and identifies Plan 05 Task 1 as the dependency-safe completion gate after all missing contract cases exist and pass.
  </acceptance_criteria>
  <done>The complete blocked release state has one append-only narrative and one explicit outcome per dispositionable identity.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md` — capture blocks, source/evidence/disposition matrices, `NONE-*` rows, and named recovery conditions.
- `test/scripts/phase_162_release_reconciliation_test.exs` — `Mailglass.Scripts.Phase162ReleaseReconciliationTest` and deterministic ledger parser/assertions.
- `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VALIDATION.md` — finalized task/wave map and Wave 0 completion state.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub/Hex remote facts → tracked evidence | Unavailable, stale, or attacker-controlled metadata must not become authorization. |
| retained Phase 161 evidence → Phase 162 disposition | Dirty worktree and preservation facts remain immutable inputs, not cleanup targets. |
| release ledger → maintainer decision | Lifecycle fields inform disposition but never self-authorize a release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-01 | Tampering / Repudiation | release identities | high | mitigate | Bind every claim to capture time, source, exact SHA/version/digest/checksum, and preserve distinct identities per D-01/D-02. |
| T-162-02 | Elevation of Privilege | disposition ledger | high | mitigate | Allow `protected-merge` only as an outcome of the existing exact-candidate path; authorization fields alone never suffice. |
| T-162-03 | Tampering | retained WT-03 evidence | high | mitigate | Read and hash retained evidence without rewriting, deleting, or normalizing it. |
| T-162-04 | Repudiation | unavailable remote data | medium | mitigate | Emit `cannot-check` with acquisition source and recovery condition rather than inferring absence. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | Preserve existing GitHub credentials and read-only acquisition; `test/scripts/phase_162_release_reconciliation_test.exs` rejects unavailable evidence represented as authenticated proof. |
| V4 Access Control | Applicable | `protected-merge` is accepted only when the existing exact candidate-digest protected path passes; `test/scripts/phase_162_release_reconciliation_test.exs` rejects ordinary-trigger authority. |
| V5 Input Validation | Applicable | `test/scripts/phase_162_release_reconciliation_test.exs` requires full SHA/version/digest/checksum identities, stable ordering, explicit empty rows, and exactly one disposition. |
| V6 Cryptography | Applicable | Record and compare the existing SHA-256 content and artifact digests exactly; `test/scripts/release_policy_contract_test.exs` preserves the existing digest contract. |
</threat_model>

<verification>
Run the Plan 01 focused ExUnit commands. Confirm the phase record is append-only relative to its first capture and that every scoped disposition identity has exactly one outcome. At end of Wave 1, run `mix test`; keep `wave_0_complete: false` pending Plans 02-04 and the Plan 05 completion gate.
</verification>

<success_criteria>
- AUTO-01 and AUTO-02 are represented by fresh, immutable, test-enforced evidence.
- D-01 through D-03 and all AUTO-02 adjacency/empty/ordering predicates are explicit.
- No release, ref, remote branch, or retained evidence mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-01-SUMMARY.md` when done.
</output>
