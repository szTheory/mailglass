---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 03
type: execute
wave: 2
depends_on: [162-01]
files_modified:
  - dev/mix/tasks/mailglass.repo.hygiene.ex
  - test/mix/tasks/mailglass.repo.hygiene_test.exs
  - .github/workflows/repo-hygiene.yml
autonomous: true
requirements: [AUTO-04]
must_haves:
  truths:
    - "D-05: Repository-hygiene control and observed schedule evidence remain separate by event/run identity; missing scheduled evidence is pending or `cannot-check`."
    - "D-06: The Mix task owns `pass`, policy `blocked`, and `cannot-check`; text, JSON, exit policy, workflow summary, and uploaded artifact agree from one result map."
    - statement: "AUTO-04 unclassified edge assumption: aggregate precedence is `cannot-check` over `blocked` over `pass`, because unavailable evidence prevents a complete policy verdict even when another established check blocks."
      verification: backstop
      status: flagged
  artifacts:
    - path: "dev/mix/tasks/mailglass.repo.hygiene.ex"
      provides: "Three-state aggregate and rendering contract"
      contains: "cannot_check"
    - path: "test/mix/tasks/mailglass.repo.hygiene_test.exs"
      provides: "Separate aggregate, rendering, and exit tests for all statuses"
      contains: "cannot-check"
    - path: ".github/workflows/repo-hygiene.yml"
      provides: "Artifact-first summary derived from the Mix task JSON"
      contains: "repo-hygiene.json"
  key_links:
    - from: "Mix.Tasks.Mailglass.Repo.Hygiene.audit/1"
      to: "text and JSON emitters"
      via: "single result map with explicit aggregate precedence"
      pattern: "cannot_check|blocked|pass"
    - from: "$RUNNER_TEMP/repo-hygiene.json"
      to: "$GITHUB_STEP_SUMMARY and repo-hygiene artifact"
      via: "jq reads status, reason, and checks from the same file"
      pattern: "jq -r"
  assumptions:
    - "FLAGGED (AUTO-04 unclassified): mixed `cannot-check` and `blocked` aggregates report `cannot-check`; focused tests backstop this chosen honest-evidence precedence."
  prohibitions:
    - statement: "MUST NOT report unavailable branch-protection, CI, PR, or remote evidence as verified policy drift or as a passing repository state."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Restore repository-hygiene's truthful three-state contract from the Mix task through CLI output, workflow summary, and uploaded JSON.

Purpose: Satisfy AUTO-04 and D-05/D-06 without changing triggers, permissions, topology, or protected CI.
Output: A tested `cannot-check` classification and artifact-first workflow rendering.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-RESEARCH.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-PATTERNS.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
@test/mix/tasks/mailglass.repo.hygiene_test.exs
@.github/workflows/repo-hygiene.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the repo-hygiene CLI three-state and fail closed</name>
  <reversibility rating="costly">Changing serialized status vocabulary affects CLI users, workflow summaries, and evidence parsers together, but no public package API or persisted data migration is involved.</reversibility>
  <files>test/mix/tasks/mailglass.repo.hygiene_test.exs, dev/mix/tasks/mailglass.repo.hygiene.ex</files>
  <read_first>
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - test/mix/tasks/mailglass.repo.hygiene_test.exs
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RESEARCH.md
  </read_first>
  <behavior>
    - "All pass checks aggregate to `:pass`; any verified policy failure with complete evidence aggregates to `:blocked`; any evidence-unavailable check aggregates to `:cannot_check` even when another check blocks."
    - "Text prints `cannot-check`; JSON serializes `cannot-check`; both non-pass states retain nonzero CLI exit."
  </behavior>
  <action>Write failing ExUnit cases first for pure pass, policy blocked, evidence unavailable, mixed blocked plus unavailable precedence, JSON status spelling, text status spelling, and independent nonzero CLI exits. In `dev/mix/tasks/mailglass.repo.hygiene.ex`, replace internal `:unknown` results with `:cannot_check` through a single `cannot_check/3` constructor, implement aggregate precedence `cannot_check`, then `blocked`, then `pass`, and encode only the external literal `cannot-check` at text/JSON boundaries. Keep `audit/1` check order and detail maps, `run/1` non-pass exit behavior, `apply_safe_actions/1`, Git/GitHub commands, and policy classification seams otherwise unchanged. Do not weaken a confirmed `DRIFT:` result to evidence-unavailable and do not add a successful exit for either non-pass status.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.repo.hygiene_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Missing verifier, missing `gh`, missing token, inaccessible API, and missing upstream assert per-check `:cannot_check` and aggregate `:cannot_check`.
    - Confirmed branch-protection drift remains `:blocked`; a clean complete repository remains `:pass`.
    - A mixed blocked/unavailable fixture proves aggregate precedence and cannot pass through iteration order.
    - Text and decoded JSON both spell `cannot-check`; `pass` and `blocked` spellings remain stable.
    - CLI exit is zero only for `pass` and nonzero for both `blocked` and `cannot-check`.
  </acceptance_criteria>
  <done>The repository-hygiene task exposes an honest, tested three-state result without changing audit scope or safe-action authority.</done>
</task>

<task type="auto">
  <name>Task 2: Render and retain exactly the Mix task result in Actions</name>
  <files>.github/workflows/repo-hygiene.yml, test/mix/tasks/mailglass.repo.hygiene_test.exs</files>
  <read_first>
    - .github/workflows/repo-hygiene.yml
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - test/mix/tasks/mailglass.repo.hygiene_test.exs
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
  </read_first>
  <action>Extend the existing workflow contract assertions in `test/mix/tasks/mailglass.repo.hygiene_test.exs` to require the summary's aggregate status/reason and per-check lines to come from `$RUNNER_TEMP/repo-hygiene.json`. In `.github/workflows/repo-hygiene.yml`, preserve the current audit command, `pipefail`, `if: always()` summary, SHA-pinned upload, schedule, dispatch, permissions, concurrency, and job topology. Change the summary renderer to display `.status`, an explicit status-appropriate reason, and `.checks[]` from the JSON file produced by the Mix task; do not recompute pass/block/cannot-check from shell exit or individual checks. Keep missing-artifact reporting distinct and retain `if-no-files-found: error`.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.repo.hygiene_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Workflow summary aggregate status and check lines are read from the uploaded JSON file, not independently derived from step outcome.
    - Artifact bytes are the same file rendered by the summary, including on expected non-pass runs.
    - Schedule, dispatch, least-privilege permissions, concurrency, job topology, and SHA-pinned actions remain unchanged.
    - The focused workflow-source and three-state contract tests pass within the task feedback target.
  </acceptance_criteria>
  <done>Maintainers receive agreeing CLI, summary, and JSON artifact evidence for control and scheduled hygiene runs.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Mix.Tasks.Mailglass.Repo.Hygiene.cannot_check/3` — internal evidence-unavailable check constructor.
- Aggregate status atom `:cannot_check` and external serialized status `cannot-check`.
- `.github/workflows/repo-hygiene.yml` summary fields sourced from `.status`, status reason, and `.checks[]` in `repo-hygiene.json`.
- Expanded `test/mix/tasks/mailglass.repo.hygiene_test.exs` three-state, mixed-precedence, text/JSON, exit, and workflow-source assertions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git/GitHub evidence → Mix task | Remote failure must remain distinguishable from verified policy drift. |
| Mix result → CLI/workflow | Serialization must not alter semantic status. |
| workflow file → summary/artifact | Human and machine evidence must derive from one result file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-09 | Tampering / Repudiation | aggregate classification | high | mitigate | Explicit precedence and mixed-status tests prevent unavailable evidence from becoming a verified policy verdict. |
| T-162-10 | Tampering | workflow summary | high | mitigate | Render summary and upload from the identical JSON path; forbid independent verdict computation. |
| T-162-11 | Elevation of Privilege | workflow permissions | high | mitigate | Preserve existing read-only permissions, triggers, and topology byte-for-byte outside reporting. |
| T-162-12 | Denial of Service | non-pass exit policy | low | accept | Both honest non-pass states intentionally fail the job; retained artifact and summary make recovery inspectable. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | `test/mix/tasks/mailglass.repo.hygiene_test.exs` asserts missing `gh` authentication or inaccessible remote evidence as `cannot-check`; existing credentials remain least-privilege. |
| V4 Access Control | Applicable | Workflow assertions in `test/mix/tasks/mailglass.repo.hygiene_test.exs` preserve read-only permissions, triggers, concurrency, and job topology; no safe-action authority is broadened. |
| V5 Input Validation | Applicable | `test/mix/tasks/mailglass.repo.hygiene_test.exs` covers pass, blocked, unavailable, mixed precedence, JSON/text spelling, and exit behavior from one result map. |
| V6 Cryptography | N/A | This plan does not create or verify cryptographic material; it preserves the existing artifact bytes and does not introduce a digest boundary. |
</threat_model>

<verification>
Run the focused Mix task test after each task. Compare workflow permissions/triggers against the pre-task source. At end of Wave 2, run `mix test` once as the documented wave-level gate.
</verification>

<success_criteria>
- AUTO-04 distinguishes all three outcomes across internal, text, JSON, exit, summary, and artifact seams.
- D-05/D-06 and the flagged AUTO-04 aggregate-precedence assumption are explicit and tested.
- Protected controls and workflow authority remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-03-SUMMARY.md` when done.
</output>
