---
phase: 163-deterministic-release-path-timeout-repairs
plan: 06
type: execute
wave: 2
depends_on: [163-04, 163-05]
files_modified:
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md
autonomous: true
gap_closure: true
requirements: [DTRM-02, DTRM-04]
must_haves:
  truths:
    - "D-05/DTRM-02/DTRM-04: Both attributed repairs and their three focused first-attempt proofs are validated before either complete integration command runs."
    - "D-06/DTRM-02: The unchanged pinned deterministic-core command passes on its first attempt after valid database proof."
    - "D-06/DTRM-04: The unchanged one-worker operator-browser package command passes on its first attempt after valid gallery proof."
    - "The integration task commit SHA is frozen before later proof-only edits and is the sole SHA eligible for protected reconciliation."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md"
      provides: "Focused-ledger audit, unchanged local integration results, baseline blob comparison, and immutable repair SHA"
  key_links:
    - from: "163-04/05 evidence"
      to: "complete local gates"
      via: "machine-checked attributed repair and count=3 first-attempt prerequisites"
      pattern: "attributed|repetition: 3|first-attempt"
    - from: "integration task commit"
      to: "protected handoff"
      via: "40-character repair_sha captured before later documentation edits"
      pattern: "repair_sha: [0-9a-f]{40}"
  prohibitions:
    - "RESOLVED/test — DTRM-02/DTRM-04 proof integrity: MUST NOT run or credit integration before both focused evidence contracts pass."
    - "RESOLVED/test — DTRM-02/DTRM-04 policy integrity: MUST NOT change workflow topology, package command, retries, workers, global/server/job deadlines, or coverage to obtain integration green."
---

<objective>
Carry both evidence-authorized repairs through unchanged complete local gates and freeze the exact repair commit for protected proof.

Purpose: Establish the local integration half of DTRM-02/DTRM-04 without weakening policy or confusing documentation commits with the repair identity.
Output: `163-PROOF.md` with prerequisite audit, local integration evidence, protected baseline comparison, and immutable `repair_sha`.

Execution-path status: Plans 163-04 through 163-08 supersede the failed/inconclusive execution path of 163-01 through 163-03 for gap closure. Preserve 163-01 through 163-03 and their summaries as immutable historical evidence; this plan depends only on the new evidence-authorized 163-04 and 163-05 outcomes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-VERIFICATION.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-04-SUMMARY.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-05-SUMMARY.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
@.github/workflows/ci.yml
@mailglass_admin/package.json
@mailglass_admin/playwright.config.cjs

<interfaces>
- Protected database equivalent: `make toolchain CMD='mix test --warnings-as-errors'` for `core_deterministic_suite`.
- Protected browser equivalent: `CI=true npm run test:operator-browser` from `mailglass_admin`; the package script owns one worker.
- `repair_sha` is the Task 1 commit captured by Task 2 before Task 2 edits; later planning/proof commits never replace it.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove both repaired paths through unchanged complete local gates</name>
  <precondition>Both 163-04 and 163-05 evidence checkpoints were resolved through their `supply-evidence` branches (not re-scope/defer), and both ledgers contain validated immutable failing-run evidence, an attributed repair, fail-first regression, three consecutive first-attempt focused passes, and no unresolved edge/prohibition that blocks integration.</precondition>
  <reversibility rating="reversible">This task runs existing gates and writes an evidence ledger without changing integration policy.</reversibility>
  <files>.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-04-SUMMARY.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-05-SUMMARY.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - .github/workflows/ci.yml
    - mailglass_admin/package.json
    - mailglass_admin/playwright.config.cjs
  </read_first>
  <action>Per D-05, machine-audit both ledgers for immutable original failure identity, attributed owner, repair identity, fail-first regression, exactly three consecutive first-attempt post-repair rows, complete elapsed/coverage data, and resolved edge/prohibition status. Halt without running integration if either ledger is inconclusive or incomplete. Record pre-Phase-163 and current Git blob IDs for `.github/workflows/ci.yml`, `mailglass_admin/package.json`, and `mailglass_admin/playwright.config.cjs`; require protected commands/topology, retries, workers, global/server/job bounds to be baseline-identical except an evidence-authorized individual-test local setting. Run `make toolchain CMD='mix test --warnings-as-errors'` and then `CI=true npm run test:operator-browser` from `mailglass_admin`, each once and without rerun. Create `163-PROOF.md` with source links, exact commands/toolchains, first-attempt exits/elapsed values, retained failures, blob comparison, and separate local database/browser verdicts. A failure remains recorded and blocks Task 2.</action>
  <verify>
    <automated>make toolchain CMD='mix test --warnings-as-errors' &amp;&amp; cd mailglass_admin &amp;&amp; CI=true npm run test:operator-browser</automated>
  </verify>
  <acceptance_criteria>
    - Neither complete command starts until both three-run focused prerequisites are machine-confirmed.
    - Both exact protected-command equivalents pass on their first and only attempt; failure is retained rather than rerun into green.
    - Protected workflow, package command, config-global timeout/retry/server lifecycle, workers, and job deadlines match their Phase 163 baseline.
    - `163-PROOF.md` distinguishes focused, local integration, and future remote protected evidence.
  </acceptance_criteria>
  <done>Both repaired paths pass unchanged complete local integration gates after valid focused proof, with protected policy byte-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the integration task commit as the protected repair SHA</name>
  <precondition>`163-PROOF.md` records first-attempt success for both unchanged local integration commands, and the worktree has no uncommitted production/test/config changes.</precondition>
  <reversibility rating="reversible">The SHA is an immutable evidence reference; correcting a capture error requires a new explicit evidence row, not history rewriting.</reversibility>
  <files>.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - .github/workflows/ci.yml
  </read_first>
  <action>Before editing the proof, capture the current full `git rev-parse HEAD`; this is the committed Task 1 integration/repair identity. Verify it contains the current narrow repairs and the initial proof, and that no later documentation-only commit is substituted. Append `repair_sha`, capture time, parent baseline, changed-path inventory, and `protected_handoff: pending` to `163-PROOF.md`. Per D-06, state that only a normally triggered pull-request or main protected run with this exact SHA and both named jobs is eligible; manual dispatch, another SHA, or advisory status cannot reconcile the phase.</action>
  <verify>
    <automated>proof=.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md; test -s "$proof" &amp;&amp; rg -n '^repair_sha: [0-9a-f]{40}$|^protected_handoff: pending$|Core Deterministic Suite|Operator Browser Gate' "$proof"</automated>
  </verify>
  <acceptance_criteria>
    - `repair_sha` is the full Task 1 commit captured before Task 2 changes the proof.
    - The proof inventories the repair paths and confirms the SHA contains both local integration results.
    - Only a normal protected PR/main run for that exact SHA is declared eligible.
  </acceptance_criteria>
  <done>An immutable exact repair SHA is ready for the protected-run handoff without conflating later proof edits.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md` — new fields/sections: focused prerequisite audit, baseline blob IDs, local database/browser commands and verdicts, `repair_sha`, and `protected_handoff`.
- No new production symbol, API, schema, dependency, workflow, timeout policy, retry, worker, or package command.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Focused ledgers → complete gates | Incomplete or manufactured proof must not unlock integration. |
| Local Git commit → remote protected run | Exact repair identity must survive later documentation commits. |
| Complete command output → proof | Initial failures and exact command ownership must remain visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-163-21 | Repudiation | proof ordering | high | mitigate | Machine-audit focused evidence before gates and retain first attempts. |
| T-163-22 | Tampering | protected topology/policy | high | mitigate | Compare baseline/current blobs and named command/config fields. |
| T-163-23 | Spoofing | repair SHA | high | mitigate | Capture full Task 1 HEAD before Task 2 edits and never recompute from later HEAD. |
| T-163-SC | Tampering | package supply chain | low | accept | No package/action/dependency install or change is planned. |
</threat_model>

<verification>
Audit both focused ledgers, run each unchanged complete command once, verify baseline bytes, and capture the Task 1 commit as the immutable repair SHA.
</verification>

<success_criteria>
- DTRM-02 and DTRM-04 have valid focused prerequisites followed by first-attempt complete local success.
- Protected topology and every prohibited broad lever remain unchanged.
- The exact repair SHA is ready for normal protected evidence.
</success_criteria>

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-06-SUMMARY.md` when done.
</output>
