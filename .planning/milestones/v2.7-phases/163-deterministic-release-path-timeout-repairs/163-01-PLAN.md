---
phase: 163-deterministic-release-path-timeout-repairs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/properties/idempotency_convergence_test.exs
  - test/mailglass/properties/webhook_idempotency_convergence_test.exs
  - lib/mailglass/webhook/ingest.ex
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
autonomous: true
requirements: [DTRM-01, DTRM-02]
must_haves:
  truths:
    - "D-01/DTRM-01: Both existing properties still execute max_runs: 1000 with their original convergence invariants, generator ranges, per-owner shared checkout, idempotency property sandbox: false committed-write semantics, ten-minute ownership bound, and existing cleanup/settle behavior."
    - "D-02/DTRM-01: A captured PostgreSQL error identifies SQLSTATE 57014, the affected property, fixture/session/query step, effective statement and lock settings, and non-PII query shape before a repair is selected."
    - "D-02/DTRM-01: The repair changes only the reproduced fixture, session, isolation, or query boundary; a source that cannot be attributed leaves execution honestly blocked."
    - "D-05/DTRM-02: Three consecutive unseeded focused runs on the pinned gating toolchain are first-attempt green after repair, and evidence records command, toolchain, repetition count, per-run elapsed milliseconds, elapsed range, first-attempt result, and attributed boundary."
    - "D-06/DTRM-02: No seed, property exclusion, reduced max_runs, product schema/API change, retry, global database timeout, CI topology, worker, or job deadline change is used to manufacture proof."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md"
      provides: "Append-only reproduction, attribution, repair-selection, boundary, and repeated-proof record"
      contains: "SQLSTATE 57014"
    - path: "test/mailglass/properties/idempotency_convergence_test.exs"
      provides: "Original 1,000-run committed-write convergence property plus a change only if its fixture/session/query is proven responsible"
      contains: "max_runs: 1000"
    - path: "test/mailglass/properties/webhook_idempotency_convergence_test.exs"
      provides: "Original 1,000-run webhook convergence property plus structured attribution/regression at the proven seam"
      contains: "max_runs: 1000"
    - path: "lib/mailglass/webhook/ingest.ex"
      provides: "Existing transaction-local timeout seam, modified only if captured evidence proves it is the narrow owner"
      contains: "SET LOCAL statement_timeout"
  key_links:
    - from: "test/mailglass/properties/webhook_idempotency_convergence_test.exs"
      to: "lib/mailglass/webhook/ingest.ex"
      via: "Ingest.ingest_multi/3 under the real PostgreSQL-backed property"
      pattern: "Ingest\.ingest_multi"
    - from: "property reproduction"
      to: "163-DATABASE-TIMEOUT-EVIDENCE.md"
      via: "structured Postgrex SQLSTATE and labelled fixture/session/query capture"
      pattern: "57014|query_canceled"
    - from: "narrow repair"
      to: "gating-toolchain repeated proof"
      via: "three independent unseeded invocations with first attempts visible"
      pattern: "make toolchain"
  flagged_assumptions:
    - "FLAGGED-UNVERIFIED — DTRM-01 boundary: the exact timing owner is unknown, so its minimum, maximum, threshold, and one-integer-millisecond-below/above cases must be derived from the captured seam before Task 2 can encode or explicitly document them."
    - "FLAGGED-UNVERIFIED — DTRM-01 precision: timing uses monotonic elapsed integer milliseconds; the exact database cancellation rounding and equality/tie behavior must be measured at the attributed seam rather than inferred."
  prohibitions:
    - "FLAGGED-UNVERIFIED — MUST NOT relax a global database/CI deadline or the established ten-minute owner bound to conceal cancellation or lock contention."
    - "FLAGGED-UNVERIFIED — MUST NOT use seed pinning, skipped/excluded properties, fewer than 1,000 runs, or retries to manufacture green property evidence."
    - "FLAGGED-UNVERIFIED — MUST NOT persist raw payloads, recipient/message content, tenant secrets, or other PII in diagnostics or evidence."
---

<objective>
Trace SQLSTATE 57014 through one real 1,000-run property path, attribute its exact owner, repair only that owner, and establish repeatable first-attempt-visible property proof.

Purpose: Satisfy DTRM-01 and the focused portion of DTRM-02 without weakening either property or confusing PostgreSQL statement cancellation with the already-settled Sandbox ownership clock.
Output: One append-only database timeout evidence record and the smallest evidence-authorized property/session/query change with regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-PATTERNS.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/milestones/v2.2-phases/143-test-harness-truth/143-gap-closure-ownership-timeout-SUMMARY.md
@test/mailglass/properties/idempotency_convergence_test.exs
@test/mailglass/properties/webhook_idempotency_convergence_test.exs
@lib/mailglass/webhook/ingest.ex
@test/support/sandbox_ownership.ex

<interfaces>
- `SandboxOwnership.checkout!/1` is the only checkout/release door; preserve the target modules' exact `shared`, `sandbox`, `ownership_timeout`, settle, and `context` options.
- `Ingest.ingest_multi/3` returns `{:ok, finalized}` or `{:error, reason}` around `Repo.transact/1`; structured `%Postgrex.Error{postgres: %{code: :query_canceled}}` metadata, not error text, identifies 57014.
- The webhook transaction currently applies transaction-local `statement_timeout = '2s'` and `lock_timeout = '500ms'`; these are hypotheses until reproduction attributes the cancellation.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace SQLSTATE 57014 from focused property invocation to its exact PostgreSQL boundary</name>
  <reversibility rating="costly">A mistaken attribution could weaken a production transaction guard or destabilize both long-running property fixtures, so no repair follows ambiguous evidence.</reversibility>
  <files>test/mailglass/properties/idempotency_convergence_test.exs, test/mailglass/properties/webhook_idempotency_convergence_test.exs, .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PATTERNS.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
    - .planning/milestones/v2.2-phases/143-test-harness-truth/143-gap-closure-ownership-timeout-SUMMARY.md
    - test/mailglass/properties/idempotency_convergence_test.exs
    - test/mailglass/properties/webhook_idempotency_convergence_test.exs
    - lib/mailglass/webhook/ingest.ex
    - test/support/sandbox_ownership.ex
  </read_first>
  <action>Per D-01 and D-02, run each property independently, unseeded and unmodified, first on the available local test database and then through `make toolchain` so attribution represents the pinned Elixir 1.18.4/OTP 27, 2-vCPU gate. Use a finite three-invocation diagnostic budget per property; record every command, toolchain, first-attempt exit, monotonic elapsed milliseconds, and whether 57014 occurred. When 57014 appears, capture only structured Postgrex fields (`code`, `severity`, `routine` when present), the affected property, a stable non-PII operation label (setup truncate, per-iteration truncate, `Ingest.ingest_multi/3`, aggregate, snapshot, or cleanup), effective session `statement_timeout`/`lock_timeout`, and a normalized query shape. Add the smallest test-harness-only structured wrapper needed to identify that operation when the existing exception lacks attribution; never rescue the error into success. Create `163-DATABASE-TIMEOUT-EVIDENCE.md` append-only with an explicit diagnosis verdict: owner timeout, fixture cleanup, transaction-local statement timeout, transaction-local lock timeout, another named query, or `unattributed`. If no 57014 is captured or two owners remain plausible, stop after recording honest `unattributed` evidence and do not begin Task 2.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/mailglass/properties/idempotency_convergence_test.exs test/mailglass/properties/webhook_idempotency_convergence_test.exs --warnings-as-errors; test -s .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md &amp;&amp; rg -n "SQLSTATE 57014|query_canceled" .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The evidence names exactly one affected property and one fixture/session/query operation, or explicitly records `unattributed` and halts before repair.
    - Captured failure metadata proves PostgreSQL 57014 independently of DBConnection ownership-timeout text.
    - Both source properties still contain `max_runs: 1000`; the idempotency property still uses `sandbox: false`, and both retain their existing per-owner ten-minute ownership settings and invariants.
    - Durable diagnostics contain timing, operation labels, SQLSTATE, and query shape only; no raw webhook body, email/recipient/message content, or tenant secret is persisted.
  </acceptance_criteria>
  <done>A real focused invocation reaches PostgreSQL and leaves a structured, non-PII, single-owner 57014 attribution that can safely select one repair seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair only the attributed database seam and prove three unseeded first attempts</name>
  <precondition>`163-DATABASE-TIMEOUT-EVIDENCE.md` contains one reproduced SQLSTATE 57014 and one unambiguous fixture/session/query owner; an `unattributed` verdict halts this task.</precondition>
  <reversibility rating="costly">A production transaction-local timeout change affects all webhook ingests; use it only when the evidence rules out a fixture-only repair and retain a finite guard.</reversibility>
  <files>test/mailglass/properties/idempotency_convergence_test.exs, test/mailglass/properties/webhook_idempotency_convergence_test.exs, lib/mailglass/webhook/ingest.ex, .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - test/mailglass/properties/idempotency_convergence_test.exs
    - test/mailglass/properties/webhook_idempotency_convergence_test.exs
    - lib/mailglass/webhook/ingest.ex
    - test/support/sandbox_ownership.ex
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - "The affected property still executes all 1,000 generated cases and preserves its exact convergence assertions and generator minima/maxima."
    - "A regression reproduces the attributed operation and fails before the local repair, then passes after it without swallowing `%Postgrex.Error{code: :query_canceled}`."
    - "Three consecutive pinned-toolchain focused invocations are green on their first and only attempts, with no fixed seed or retry."
    - "The evidence records the attributed threshold's configured integer milliseconds, measured elapsed values, and below/equal/above boundary behavior when controllable; otherwise it keeps the DTRM-01 boundary/precision assumptions explicitly flagged for Phase 163 verification."
  </behavior>
  <action>Start RED with a focused regression in the affected property module at the exact operation from Task 1. Select the repair mechanically: fixture cleanup/session residue permits only a fixture-local reset or isolation correction; a property-only query permits only a test query/fixture adjustment; an `Ingest.ingest_multi/3` cancellation permits a change inside its existing `Repo.transact/1` session/query seam only after the evidence rules out test residue and lock contention. Keep every bound finite and justify any integer-millisecond value from the slowest captured successful/failed operation plus explicit headroom; preserve `SET LOCAL`, result tuples, post-commit telemetry, and API/schema behavior. Do not change the ten-minute ownership bound, `@moduletag timeout: :infinity`, generators, `max_runs`, checkout mode, cleanup cardinality, or CI files. Remove temporary instrumentation unless the regression requires its stable non-PII label. Run the affected property three consecutive times through `make toolchain` with no seed or retry. Append the exact commands, toolchain versions, count=3, every first-attempt result, elapsed values/range, chosen source boundary, finite bound/rationale, and boundary/precision observation to the evidence file per D-05/D-06 and DTRM-02.</action>
  <verify>
    <automated>make toolchain CMD='mix test test/mailglass/properties/idempotency_convergence_test.exs test/mailglass/properties/webhook_idempotency_convergence_test.exs --warnings-as-errors'</automated>
  </verify>
  <acceptance_criteria>
    - The regression is red before and green after the narrow repair and asserts structured behavior rather than matching error text.
    - Both properties complete with 1,000 runs and unchanged convergence assertions, checkout ownership, committed-write semantics, and generator ranges.
    - Three consecutive focused gating-toolchain invocations are first-attempt green and fully recorded; a retry or fixed seed invalidates the proof.
    - The chosen repair is confined to the one attributed fixture/session/isolation/query seam and introduces no product schema/API, dependency, global database config, workflow, job-timeout, or retry change.
    - The evidence either resolves the DTRM-01 threshold and timing-precision probes or retains both as explicit flagged-unverified assumptions for final verification.
  </acceptance_criteria>
  <done>The reproduced SQLSTATE 57014 path is narrowly repaired and repeatedly green without reducing either 1,000-run proof contract or broadening any deadline.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md` — new append-only evidence file; fields are command, toolchain, repetition count, first-attempt result, monotonic elapsed milliseconds/range, SQLSTATE, non-PII source boundary/query shape, effective local bounds, repair verdict, and flagged boundary/precision status.
- Conditional test-only diagnostic/regression helpers in exactly one affected property module; names are chosen to match the attributed operation and must remain private.
- No new public symbol, API, schema field, configuration flag, dependency, workflow, worker, or retry is expected.
- `lib/mailglass/webhook/ingest.ex` changes only if reproduced evidence proves its transaction-local seam owns 57014; no public symbol or result shape changes.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StreamData/property fixture → PostgreSQL session | Generated work and cleanup cross a real database session with finite owner, statement, and lock clocks. |
| PostgreSQL error → diagnostic evidence | Structured database metadata is useful, but payload/query values may contain sensitive message or tenant data. |
| Local focused result → release-path claim | A pass is not trustworthy if seeds, retries, run count, or toolchain identity differ from the protected contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-01 | Denial of Service | database session/query repair | high | mitigate | Attribute one SQLSTATE/query/session owner before change; keep the repair finite and local; retain global and job bounds unchanged. |
| T-163-02 | Repudiation | repeated property evidence | high | mitigate | Record every first attempt, command, toolchain, count, and elapsed value; reject fixed-seed, skipped, reduced, or retry-assisted proof. |
| T-163-03 | Information Disclosure | diagnostic capture | high | mitigate | Persist only structured SQLSTATE, stable operation labels, normalized query shape, and timing; exclude raw values, payloads, addresses, messages, and tenant secrets. |
| T-163-04 | Tampering | property invariant/coverage | high | mitigate | Run both unchanged 1,000-run properties and assert checkout/invariant/source markers remain present after repair. |
| T-163-SC | Tampering | package supply chain | low | accept | No package install or dependency/action reference change is planned. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V3 Session Management | Applicable | Database timeout state remains transaction-local or fixture-local and finite. |
| V5 Input Validation | Applicable | Existing generators and convergence assertions remain unchanged across full cardinality. |
| V7 Error Handling and Logging | Applicable | Structured Postgrex metadata is captured without raw message data or error-string matching. |
</threat_model>

<verification>
Execute the Task 1 focused pair and Task 2 pinned-toolchain pair exactly as written. Confirm `163-DATABASE-TIMEOUT-EVIDENCE.md` contains one reproduced 57014, one attributed source boundary, count=3 repeated first-attempt proof, elapsed range, and either resolved or explicitly flagged DTRM-01 edge/precision status. Run `git diff --check` on the owned files.
</verification>

<success_criteria>
- DTRM-01 has reproduced, structured, non-PII attribution and a regression-backed repair at one narrow seam.
- DTRM-02 has three unseeded, first-attempt-visible, pinned-toolchain focused passes without weakening the properties or broadening policy.
- Any unresolved threshold/precision behavior remains visibly flagged rather than silently inferred.
</success_criteria>

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-01-SUMMARY.md` when done.
</output>
