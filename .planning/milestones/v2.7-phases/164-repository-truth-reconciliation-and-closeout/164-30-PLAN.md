---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 30
type: execute
wave: 27
depends_on: [164-28]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05/D-12/TRTH-02: An existing empty or incomplete authority root returns exit 1 with stable `missing_authority_subject` data, usage text, and no exception stack trace."
    - "TRTH-02: Non-raising authority reads stop at the first missing subject in declared @ignore_files order; equal/adjacent missing inputs do not merge or reorder diagnostics."
    - "TRTH-02: Canonical complete authority still validates, and malformed ledger, exact-one subject, tracked-stage, and canonical-relationship contracts remain unchanged."
  artifacts:
    - path: "scripts/validate_repository_truth.exs"
      provides: "Result-returning authority subject reader and stable CLI error propagation"
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Empty/incomplete authority-root CLI regressions with bounded diagnostic assertions"
  key_links:
    - from: "scripts/validate_repository_truth.exs ignore_subjects/1"
      to: "audit_subjects/2 and main/1"
      via: "{:ok, subjects} | {:error, {:missing_authority_subject, relative_path}}"
      pattern: "missing_authority_subject"
    - from: "test/scripts/phase_164_repository_truth_test.exs"
      to: "scripts/validate_repository_truth.exs CLI"
      via: "System.cmd elixir subprocess with an existing incomplete authority root"
      pattern: "authority-root"
  flagged_assumptions:
    - "TRTH-01 unclassified probe: this validator-only repair does not alter already-verified maintainer/package prose."
  prohibitions:
    - statement: "D-05/D-06/D-12: MUST NOT weaken exact-one ledger coverage, tracked evidence preservation, or canonical relationship validation while bounding authority-read errors."
      status: unverified
      flagged: true
    - statement: "T-164-109: terminal evidence MUST NOT be followed by a tracked write; this plan creates no terminal evidence."
      status: unverified
      flagged: true
---

<objective>
Make incomplete authority roots a controlled public-validator rejection instead of an uncaught File error.

Purpose: Preserve the ledger validator's stable CLI contract for malformed-but-existing authority inputs.
Output: Non-raising subject discovery and focused empty/incomplete-root regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@scripts/validate_repository_truth.exs
@test/scripts/phase_164_repository_truth_test.exs
</context>

<assumption_delta_decision>
Primary noun: repository-truth authority root. Decision: no-change — there is still one singular authority root whose required subjects are read in one declared order; no alternate authority architecture is added.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an empty authority root to one bounded CLI diagnostic</name>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
  </read_first>
  <behavior>
    - Test 1: an existing empty temporary directory passed as --authority-root exits exactly 1.
    - Test 2: stderr contains `repository truth ledger: {:missing_authority_subject, ".gitignore"}` and the stable usage line once.
    - Test 3: output contains no File.Error, stacktrace banner, source-frame line, or exception formatting.
    - Test 4: the same invocation is deterministic across repeated runs and never creates files in the authority root.
  </behavior>
  <action>Start RED with a CLI subprocess regression that creates an existing empty authority directory, passes the canonical tracked ledger and repository, captures stdout/stderr together, and asserts exact exit/diagnostic/usage/no-stack behavior plus unchanged empty contents. Replace the first raising ignore-file read with a result-returning read of `.gitignore`; on `:enoent` or any non-regular/missing required authority subject, return `{:error, {:missing_authority_subject, ".gitignore"}}` through `audit_subjects/2`, `validate/3`, and `main/1`. Keep the public `main/1` return convention and bounded `inspect(reason)` diagnostic. Do not rescue broad exceptions, invent fallback subjects, read from the canonical repository when the authority root is incomplete, or modify ledger semantics.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_incomplete_authority_root --warnings-as-errors --no-deps-check</automated>
    <fails_when>Non-zero exit; zero selected tests; the CLI status is not 1; the tagged `.gitignore` diagnostic or usage is absent/duplicated; a stack trace appears; or the empty authority root changes.</fails_when>
  </verify>
  <acceptance_criteria>
    - The empty-root CLI path is active and executes the real script entry point.
    - The first missing required authority subject is represented as stable tagged data.
    - No raising read or exception rescue defines expected malformed-input behavior.
  </acceptance_criteria>
  <done>An empty authority root fails through the validator's ordinary exit-1 diagnostic contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize ordered non-raising reads across all authority ignore subjects</name>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
  </read_first>
  <behavior>
    - Test 1: ignore_subjects/1 returns `{:ok, subjects}` only after every @ignore_files member is a readable regular file.
    - Test 2: roots containing only an initial prefix report the next relative subject in @ignore_files order.
    - Test 3: directory, symlink, unreadable, and malformed required-subject cases remain non-raising and return a stable tagged reason.
    - Test 4: canonical validation still prints `repository truth ledger: valid`, and row-order independence plus exact-one checks remain green.
  </behavior>
  <action>Refactor `ignore_subjects/1` into an ordered `Enum.reduce_while` over `@ignore_files` that lstat-checks a regular non-symlink subject, uses `File.read/1`, parses lines with the existing trim/comment rules, and returns `{:ok, flattened_subjects}` only after the complete sequence succeeds. Use `{:missing_authority_subject, relative_path}` for absent or wrong-type subjects and a separate stable `{:unreadable_authority_subject, relative_path, reason}` tuple for non-raising read failures. Update `audit_subjects/2` to bind the successful list inside its `with` before composing tracked/proof/phase subjects. Add prefix-root and wrong-type CLI/API tests proving deterministic first-error ordering and retain the canonical positive validator regression. Preserve D-05/D-12 header, disposition, uniqueness, tracked-stage, canonical digest, and ignore-line interpretation exactly.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; zero tests; canonical output lacks the valid verdict; incomplete subjects raise or reorder; exact-one/canonical relationship regressions fail; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every authority ignore-file read is non-raising and ordered by @ignore_files.
    - Empty, partial, wrong-type, and unreadable roots produce bounded tagged exit-1 diagnostics.
    - Complete canonical validation retains all prior exactness and evidence-preservation behavior.
  </acceptance_criteria>
  <done>The standalone validator handles every incomplete authority-root read through stable result data.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/validate_repository_truth.exs` — result-returning `ignore_subjects/1` and stable `missing_authority_subject` / `unreadable_authority_subject` errors.
- `test/scripts/phase_164_repository_truth_test.exs` — `phase_164_incomplete_authority_root` CLI group and ordered prefix/wrong-type regressions.
- New files or flags: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; malformed local authority input crosses the public CLI boundary and must fail closed without uncontrolled disclosure.

## Trust Boundaries

| Boundary | Description |
|---|---|
| --authority-root filesystem → validator | Existing directories may omit, replace, or deny required authority subjects. |
| Validator reason → CLI stderr | Internal exceptions must become bounded stable diagnostics, not stack disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-114 | Denial of Service | authority subject discovery | medium | mitigate | Replace raising streams with ordered result-returning lstat/read operations. |
| T-164-115 | Information Disclosure | CLI diagnostic | medium | mitigate | Emit stable tagged relative subjects and no exception stack. |
| T-164-116 | Tampering | authority fallback | high | mitigate | Never substitute canonical files for missing authority-root subjects. |
| T-164-117 | Repudiation | subject ordering | medium | mitigate | Test deterministic first-missing behavior in declared @ignore_files order. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager or dependency operation occurs. |
</threat_model>

<verification>
Run the full repository-truth contract, the canonical standalone validator, and diff hygiene. No external installed state or terminal command is touched.
</verification>

<success_criteria>
- Empty/incomplete authority roots exit 1 through stable tagged diagnostics without stack traces.
- Canonical ledger validation retains all exact-one, stage-aware, and evidence-preserving contracts.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifact/ignore evidence remains reliable under malformed authority input | 164-30 | COVERED |
| REQ | TRTH-02 | Evidence-backed exact classification and discoverability | Tasks 1–2 | COVERED |
| CONTEXT | D-05, D-06, D-07, D-08, D-12 | Ledger completeness, preservation, narrow ignores, and exact outcomes remain unchanged | Tasks 1–2 | COVERED |
| RESEARCH | — | One-row/one-disposition validator fails closed | Tasks 1–2 | COVERED |
| VERIFICATION | Gap 3 | Empty authority root must not raise | Tasks 1–2 | COVERED |
| EDGE | TRTH-02 adjacency/empty/ordering | Prefix boundaries, empty root, deterministic first-missing order | Tasks 1–2 | COVERED |
| EDGE | TRTH-01 unclassified | Explicit no-change assumption | must_haves.flagged_assumptions | COVERED-FLAGGED |
| PROHIBITION | Evidence preservation and T-164-109 | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-30-SUMMARY.md` when done. Do not update requirement/phase completion metadata or run terminal finalization.
</output>
