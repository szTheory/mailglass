---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 18
type: execute
wave: 16
depends_on: [164-17]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-05/D-06/D-12/TRTH-02: A tracked disposition is accepted only when the exact literal subject has exactly one NUL-delimited stage-0 Git-index entry; stage-1/2/3 conflict entries are never tracked proof."
    - "TRTH-02: The authoritative ledger still validates for ordinary stage-0 tracked files, including literal metacharacter and prefix-adjacent subjects."
  artifacts:
    - path: "scripts/validate_repository_truth.exs"
      provides: "Exact stage-aware Git-index authentication for tracked ledger subjects"
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Production-seam unmerged stage-1/2/3 regression and stage-0 positive controls"
  key_links:
    - from: "scripts/validate_repository_truth.exs"
      to: "the target repository Git index"
      via: "git --literal-pathspecs ls-files --stage -z --error-unmatch followed by exact record parsing"
      pattern: "ls-files.*--stage.*-z.*--error-unmatch"
  flagged_assumptions:
    - "TRTH-02 adjacency remains unresolved by a phase SPEC: the plan treats byte-exact NUL-delimited path equality as the only defensible identity and does not merge adjacent or prefix-related subjects."
    - "TRTH-02 empty remains unresolved by a phase SPEC: zero records, an empty path, or a path with no stage-0 record is planned to fail closed rather than become vacuous proof."
    - "TRTH-02 ordering remains unresolved by a phase SPEC: record order cannot grant authority; exactly one matching stage-0 record is required and any additional stage record rejects the subject."
  prohibitions:
    - statement: "D-05/D-06/D-12: MUST NOT represent invalid or unmerged Git-index state as tracked repository proof."
      status: unresolved
      flagged: true
---

<objective>
Trace one tracked ledger claim through the stage-aware production Git-index seam.

Purpose: Close the verifier's concrete conflict-stage bypass without reopening the already satisfied documentation requirement.
Output: Exact NUL-delimited stage-0 validation and an unmerged-index behavioral regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-17-SUMMARY.md
@scripts/validate_repository_truth.exs
@test/scripts/phase_164_repository_truth_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Require one exact stage-0 index record for every tracked subject</name>
  <reversibility rating="reversible">The change tightens an internal evidence predicate and its fixtures without changing the ledger schema or public package behavior.</reversibility>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-17-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
  </read_first>
  <behavior>
    - Test 1: a disposable repository containing an unresolved `UU` subject with only index stages 1, 2, and 3 makes `Mailglass.RepositoryTruthLedger.tracked_subject_in_index/2` and the production `validate/2` path return a specific non-success result.
    - Test 2: one ordinary committed stage-0 record for the byte-exact subject returns `:ok`.
    - Test 3: literal pathspec metacharacters, prefix-adjacent names, embedded newlines, and record ordering cannot satisfy another subject or collapse multiple records into one.
    - Test 4: zero records, more than one record, any nonzero stage, malformed stage metadata, or a returned path different from the requested subject fails closed.
  </behavior>
  <action>Start RED by extending the existing production-seam trust-anchor group with a tagged `:phase_164_stage0_index` disposable-Git fixture that creates a genuine three-stage unmerged entry and proves the current helper accepts it. Then replace the newline/pathname-only lookup with argv-only `git --literal-pathspecs ls-files --stage -z --error-unmatch -- &lt;subject&gt;`. Parse the mode, object ID, numeric stage, tab separator, and NUL-terminated pathname without trimming or line splitting; require exactly one complete record, stage exactly zero, and byte-exact pathname equality. Preserve the existing regular-file check and distinct error categories for untracked, identity mismatch, malformed output, and Git command failure. Per D-05/D-06/D-12, do not weaken full-ledger inventory, uniqueness, canonical relationship hashes, or disposition semantics.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_stage0_index --warnings-as-errors --no-deps-check</automated>
    <fails_when>The command exits nonzero, selects zero tagged tests, the unmerged stage-1/2/3 subject is accepted, a valid single stage-0 subject is rejected, or literal/NUL path identity is not preserved.</fails_when>
  </verify>
  <acceptance_criteria>
    - The regression constructs and asserts a real multi-stage index with no stage-0 entry, then exercises both the helper and production validation path.
    - The parser consumes NUL-delimited staged output and rejects nonzero, duplicate, missing, malformed, or path-mismatched records.
    - The authoritative Phase 164 ledger remains valid and existing metacharacter/prefix-adjacency tests remain green.
    - No ledger row, ignore rule, documentation, release authority, dependency, schema, UI, package version, or terminal evidence artifact changes.
  </acceptance_criteria>
  <done>Every tracked ledger claim is backed by one exact stage-0 Git-index identity, and the production validator rejects an unresolved multi-stage subject.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/validate_repository_truth.exs` — refined `Mailglass.RepositoryTruthLedger.tracked_subject_in_index/2` staged-record parser; no new public API or CLI flag.
- `test/scripts/phase_164_repository_truth_test.exs` — new `:phase_164_stage0_index` production-seam fixtures for stage-0, stage-1/2/3, malformed, duplicate, literal, and NUL-delimited identity behavior.
- No new path, dependency, schema, workflow, release authority, package version, external integration, or terminal report.

<threat_model>
## ASVS Level

OWASP ASVS L1. The high-severity tracked-proof spoofing defect blocks completion until the production-seam regression passes.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Ledger `state=tracked` claim → Git index | Untrusted repository state must prove one exact stage-0 identity rather than merely returning a pathname. |
| Git staged output → Elixir validator | Record framing, stage, and pathname must be parsed without newline normalization or pathspec expansion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-65 | Spoofing | `tracked_subject_in_index/2` | high | mitigate | Require one NUL-delimited exact-path stage-0 record and reject every nonzero or additional stage. |
| T-164-66 | Tampering | Git staged-record parser | high | mitigate | Exercise malformed, multi-record, embedded-newline, metacharacter, and prefix-adjacent cases through argv-only Git calls. |
| T-164-67 | Repudiation | tracked disposition verdict | medium | mitigate | Preserve distinct bounded errors and execute the complete production validation path in the conflict fixture. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run the tagged stage-0 group, the complete repository-truth test file, the canonical validator invocation, Elixir formatting, and `git diff --check`. A nonzero exit, zero selected tests, or acceptance of any non-stage-0 record is failure.
</verification>

<success_criteria>
- The verifier's exact unmerged-index reproduction is now red before implementation and green only after the production helper rejects it.
- Ordinary stage-0 tracked subjects retain literal byte identity and validate successfully.
- TRTH-01 remains satisfied and untouched.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and unresolved: D-05/D-06/D-12 prohibit representing invalid or unmerged index state as tracked proof.
- Canon-referral breadcrumb: generic command injection and path traversal remain ASVS/security-review concerns; argv-only literal pathspec execution and exact record parsing mitigate their phase-specific forms.
- Dropped as routine engineering: empty output, malformed fields, duplicate records, and ordering are explicit behavioral cases rather than new values/safety prohibitions.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts describe the repository's actual state | 164-18 | COVERED |
| REQ | TRTH-02 | Evidence-backed tracked/generated classification | 164-18 Task 1 | COVERED |
| CONTEXT | D-05, D-06, D-12 | Exact-one disposition and durable tracked proof | 164-18 Task 1 | COVERED |
| RESEARCH | one-row ledger / Git-tracked proof | Stage-aware production validator | 164-18 Task 1 | COVERED |
| PATTERNS | production validator plus disposable Git fixtures | Existing validator/test seams retained | 164-18 Task 1 | COVERED |
| VERIFICATION | gap 1 / CR-01 | Exactly one NUL-delimited stage-0 entry and unmerged regression | 164-18 Task 1 | COVERED |
| REVIEW | CR-01 | Multi-stage conflict must not certify a tracked artifact | 164-18 Task 1 | COVERED |
| EDGE | TRTH-02 adjacency, empty, ordering | Exact identity, non-vacuous stage-0 record, order-independent rejection | 164-18 flagged assumptions and Task 1 | COVERED-FLAGGED |
| PROHIBITION | D-05/D-06/D-12 invalid tracked proof | Descriptor-less unresolved constraint | 164-18 | COVERED-FLAGGED |
| REQ | TRTH-01 | Current maintainer/package truth | Executed baseline 164-16 | SATISFIED-UNCHANGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-18-SUMMARY.md` when done. Do not run `/finalize-phase 164` during execution.
</output>
