---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 23
type: execute
wave: 21
depends_on: [164-22]
files_modified:
  - /Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env
  - /Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env
  - /Users/jon/.local/bin/mailglass-finalize-phase
  - /Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.<prior-sha256>
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low
user_setup:
  - service: local-command-installation
    why: "Approve the exact immutable identity before the executor places a trust-root executable outside the mutable repository"
    dashboard_config:
      - task: "Review the captured OID, SHA-256, destination, prior-destination disposition, exact prior digest/mode/stat identity/provenance/rollback path when applicable, and mode 0500; approval is persisted at mode 0400 before the executor installs"
        location: "Plan 164-23 blocking-human checkpoint"
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: `/Users/jon/.local/bin/mailglass-finalize-phase` is a regular non-symlink executable outside `/Users/jon/projects/mailglass` whose bytes equal the captured committed loader blob."
    - "TRTH-03: Installation records the source commit OID and SHA-256, rejects a dirty or noncanonical source checkout, and replaces no unexpected destination object."
    - "TRTH-03: An absent destination or a distinct regular non-symlink user-owned prior loader may be approved only through one persisted immutable tuple containing its exact digest, mode, filesystem identity, provenance, and rollback path; any post-approval drift hard-stops."
    - "TRTH-03: Any approved prior Mailglass loader is preserved byte-for-byte at a private mode-0400 rollback path with verified digest before replacement, and the recorded atomic restore path is exercised against a same-filesystem probe before completion."
  artifacts:
    - path: "/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env"
      provides: "Plan-owned mode-0400 checkpoint record containing the exact human-approved installation and prior-object tuple"
    - path: "/Users/jon/.local/bin/mailglass-finalize-phase"
      provides: "User-installed pre-evaluation authority executable (external operational state, sourced from tracked Git bytes)"
    - path: "/Users/jon/.local/share/mailglass/rollback/"
      provides: "Conditional private recoverable copy and provenance for a replaced prior Mailglass loader"
  key_links:
    - from: "captured commit:scripts/mailglass_finalize_phase_loader.mjs"
      to: "/Users/jon/.local/bin/mailglass-finalize-phase"
      via: "private temporary extraction, byte/digest comparison, non-symlink destination check, mode-0500 atomic install"
      pattern: "git show|shasum -a 256|install -m 0500"
    - from: "/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env"
      to: "installation, rollback object, and 164-23-SUMMARY.md"
      via: "exact field comparison before every mutation and again during post-install verification"
      pattern: "installation_source_oid|source_sha256|prior_stat_identity|prior_provenance|rollback_path"
  flagged_assumptions:
    - "TRTH-03 empty/ordering probes: installation must have one non-empty committed source blob and completes verification before any installed invocation; an absent destination or distinct prior regular loader proceeds only when its exact persisted tuple is explicitly approved, while every other object or any later drift hard-stops."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT install loader bytes read directly from a dirty working tree or overwrite a symlink/unexpected destination."
      status: unresolved
      flagged: true
---

<objective>
Install the authenticated loader outside the checkout and verify its exact provenance.

Purpose: Cross the approved external trust-boundary step required before the old project extension can be replaced operationally.
Output: One user-owned mode-0500 installed executable byte-identical to a captured Git commit blob.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the exact immutable installation identity</name>
  <reversibility rating="reversible">This preflight reads committed bytes and destination metadata without changing the installed command.</reversibility>
  <files>/Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env; source is scripts/mailglass_finalize_phase_loader.mjs and the proposed destination is /Users/jon/.local/bin/mailglass-finalize-phase</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>From `/Users/jon/projects/mailglass`, require the canonical physical root, an empty stable porcelain result, and one full `HEAD^{commit}` OID. Resolve `/Users/jon/.local/bin` physically and prove it is outside the repository. Inspect the proposed destination with lstat semantics and without executing or following it. Absence is acceptable. A present candidate may have bytes distinct from the new committed blob, but it enters the approval tuple only when it is a regular non-symlink file owned by the current user, executable, and not group/world writable. Record that candidate's SHA-256, numeric mode, exact device/inode/uid/gid stat identity, physical destination identity, and provenance label `observed-user-owned-external-command-awaiting-explicit-approval`; human approval, rather than byte equality with the new blob, is what authorizes treating it as the prior Mailglass loader. Reject every other destination object. Derive a digest-addressed rollback path beneath `/Users/jon/.local/share/mailglass/rollback/`; preflight names but does not create that backup. Extract `scripts/mailglass_finalize_phase_loader.mjs` from the captured OID into a mode-0600 private temporary file, require non-empty bytes, and compute its SHA-256. Persist the complete proposal atomically as a regular non-symlink mode-0400 file at `/Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env`, using a strict fixed-key `key=value` schema containing `record_version`, `phase_plan`, installation OID/digest/destination/mode, destination disposition, and every conditional prior identity/provenance/rollback field. Re-check checkout HEAD, porcelain, and the lstat/digest tuple of the still-unmodified destination after publishing the proposal; any drift removes the proposal and hard-stops. Remove the loader temporary and assert it is absent. Display the persisted tuple for approval. Do not alter the destination, create rollback bytes, or run any loader mode during preflight.</action>
  <verify>
    <automated>set -eu; repo=/Users/jon/projects/mailglass; dest=/Users/jon/.local/bin/mailglass-finalize-phase; proposal=/Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env; test "$(cd "$(git -C "$repo" rev-parse --show-toplevel)" &amp;&amp; pwd -P)" = "$repo"; test -z "$(git -C "$repo" status --porcelain=v1 --untracked-files=all)"; install_oid="$(git -C "$repo" rev-parse --verify 'HEAD^{commit}')"; test "${#install_oid}" = 40; case "$install_oid" in *[!0-9a-f]*) exit 1;; esac; dest_parent="$(cd "$(dirname "$dest")" &amp;&amp; pwd -P)"; case "$dest_parent/" in "$repo/"*) exit 1;; esac; tmp_root="$(mktemp -d "${TMPDIR:-/tmp}/mailglass-install-preflight.XXXXXX")"; tmp_loader="$tmp_root/loader"; trap 'rm -f "$tmp_loader"; rmdir "$tmp_root" 2&gt;/dev/null || true' EXIT; git -C "$repo" show "$install_oid:scripts/mailglass_finalize_phase_loader.mjs" &gt;"$tmp_loader"; chmod 0600 "$tmp_loader"; test -s "$tmp_loader"; source_digest="$(shasum -a 256 "$tmp_loader" | awk '{print $1}')"; test "${#source_digest}" = 64; disposition=absent; prior_digest=none; prior_mode=none; prior_stat_identity=none; prior_physical_identity=none; prior_provenance=none; rollback_path=none; if test -e "$dest" || test -L "$dest"; then test ! -L "$dest"; test -f "$dest"; test -x "$dest"; test "$(stat -f '%u' "$dest")" = "$(id -u)"; prior_digest="$(shasum -a 256 "$dest" | awk '{print $1}')"; prior_mode="$(stat -f '%Lp' "$dest")"; case "$prior_mode" in ?[2367]|??[2367]) exit 1;; esac; prior_stat_identity="$(stat -f '%d:%i:%u:%g' "$dest")"; prior_physical_identity="$(cd "$(dirname "$dest")" &amp;&amp; pwd -P)/$(basename "$dest")"; prior_provenance=observed-user-owned-external-command-awaiting-explicit-approval; disposition=approval-required-prior-loader; rollback_path="/Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.$prior_digest"; fi; install -d -m 0700 "$(dirname "$proposal")"; proposal_tmp="$(mktemp "$(dirname "$proposal")/.164-23-proposal.XXXXXX")"; printf 'record_version=1\nphase_plan=164-23\ninstallation_source_oid=%s\nsource_sha256=%s\ndestination=%s\ninstall_mode=0500\ndestination_disposition=%s\nprior_sha256=%s\nprior_mode=%s\nprior_stat_identity=%s\nprior_physical_identity=%s\nprior_provenance=%s\nrollback_path=%s\n' "$install_oid" "$source_digest" "$dest" "$disposition" "$prior_digest" "$prior_mode" "$prior_stat_identity" "$prior_physical_identity" "$prior_provenance" "$rollback_path" &gt;"$proposal_tmp"; chmod 0400 "$proposal_tmp"; mv -f "$proposal_tmp" "$proposal"; test -f "$proposal"; test ! -L "$proposal"; test "$(stat -f '%Lp' "$proposal")" = 400; test "$(git -C "$repo" rev-parse --verify 'HEAD^{commit}')" = "$install_oid"; test -z "$(git -C "$repo" status --porcelain=v1 --untracked-files=all)"; if test "$disposition" = absent; then test ! -e "$dest" &amp;&amp; test ! -L "$dest"; else test "$(stat -f '%d:%i:%u:%g' "$dest")" = "$prior_stat_identity" &amp;&amp; test "$(stat -f '%Lp' "$dest")" = "$prior_mode" &amp;&amp; test "$(shasum -a 256 "$dest" | awk '{print $1}')" = "$prior_digest"; fi; rm -f "$tmp_loader"; rmdir "$tmp_root"; trap - EXIT; test ! -e "$tmp_root"; git -C "$repo" diff --check</automated>
    <fails_when>The checkout is noncanonical or dirty; the OID is not full/stable; the committed loader blob is absent; the destination is a symlink/non-regular/non-user-owned/non-executable/writable-by-others object; a distinct regular candidate is rejected merely because its digest differs from the new blob; any destination identity, mode, or digest changes during preflight; the fixed-key proposal is missing, symlinked, not mode 0400, or incomplete; any installation/finalization dispatch occurs; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - One persisted proposal records a full stable source OID, committed-blob SHA-256, external destination, destination disposition, intended mode 0500, and conditional prior digest/mode/stat identity/physical identity/provenance/rollback path.
    - A distinct prior regular non-symlink user-owned executable is eligible for explicit approval; equality with the new loader digest is not required.
    - The tuple derives from `git show <captured-oid>:scripts/mailglass_finalize_phase_loader.mjs`, never checkout bytes.
    - Preflight leaves the destination unchanged, rejects drift after observation, removes its loader temporary, and persists only the mode-0400 proposal record.
  </acceptance_criteria>
  <done>The exact external installation proposed to the user is immutable, bounded, and fully identified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact trust-root installation tuple</name>
  <files>/Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env, /Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>Pause before mutating user-global executable state and display the complete persisted Task 1 proposal: full source OID, SHA-256, exact destination `/Users/jon/.local/bin/mailglass-finalize-phase`, intended mode 0500, current destination disposition, and conditional prior digest/mode/stat identity/physical identity/provenance/rollback path. Approval is scoped to every byte of that exact tuple, including a distinct prior version when present. After an explicit `approved` response, and before completing the checkpoint, the executor must atomically create `/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env` as a regular non-symlink mode-0400 copy of the proposal plus exactly `approval_status=approved`; refuse a pre-existing approval record unless it is byte-identical. Re-read the persisted approval record and recheck its exact OID/digest against the committed blob and its absent-or-present destination tuple against lstat/digest observations. Any mismatch, moving HEAD, dirty source, unsafe destination identity, changed prior bytes/mode/stat identity, or request to change the destination invalidates approval, removes no destination bytes, and returns to Task 1.</action>
  <what-built>An authenticated, non-mutating installation proposal for a pre-evaluation Phase 164 authority command.</what-built>
  <how-to-verify>Confirm that the displayed source is the committed Plan 164-21 loader, the destination is the approved user-owned external command path, mode is 0500, no unexpected prior object will be replaced, and any prior eligible loader has an exact private rollback destination.</how-to-verify>
  <verification>The executor reads the mode-0400 approval record, not a newly inferred tuple; it rechecks that its exact OID is still checkout HEAD, porcelain remains empty, the committed blob still hashes to its recorded SHA-256, and every absent-or-present destination field remains unchanged.</verification>
  <resume-signal>Reply `approved` to authorize only the displayed tuple, or describe any mismatch to stop without installation.</resume-signal>
  <instructions>
    Review the exact OID, digest, destination, destination disposition, conditional rollback provenance, and mode shown by the executor.
    Approve only when every displayed installation and conditional prior-object field matches the authorized external trust-root boundary.
  </instructions>
  <verify>
    <automated>set -eu; repo=/Users/jon/projects/mailglass; proposal=/Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env; approval=/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env; test -f "$proposal"; test ! -L "$proposal"; test "$(stat -f '%Lp' "$proposal")" = 400; test -f "$approval"; test ! -L "$approval"; test "$(stat -f '%Lp' "$approval")" = 400; test "$(grep -c '^approval_status=approved$' "$approval")" = 1; for key in record_version phase_plan installation_source_oid source_sha256 destination install_mode destination_disposition prior_sha256 prior_mode prior_stat_identity prior_physical_identity prior_provenance rollback_path; do test "$(grep -c "^$key=" "$proposal")" = 1; test "$(grep -c "^$key=" "$approval")" = 1; test "$(sed -n "s/^$key=//p" "$proposal")" = "$(sed -n "s/^$key=//p" "$approval")"; done; approved_oid="$(sed -n 's/^installation_source_oid=//p' "$approval")"; approved_digest="$(sed -n 's/^source_sha256=//p' "$approval")"; dest="$(sed -n 's/^destination=//p' "$approval")"; disposition="$(sed -n 's/^destination_disposition=//p' "$approval")"; test "$(git -C "$repo" rev-parse --verify 'HEAD^{commit}')" = "$approved_oid"; test -z "$(git -C "$repo" status --porcelain=v1 --untracked-files=all)"; test "$(git -C "$repo" show "$approved_oid:scripts/mailglass_finalize_phase_loader.mjs" | shasum -a 256 | awk '{print $1}')" = "$approved_digest"; if test "$disposition" = absent; then test ! -e "$dest" &amp;&amp; test ! -L "$dest"; else test "$disposition" = approval-required-prior-loader; test ! -L "$dest"; test -f "$dest"; test "$(stat -f '%d:%i:%u:%g' "$dest")" = "$(sed -n 's/^prior_stat_identity=//p' "$approval")"; test "$(stat -f '%Lp' "$dest")" = "$(sed -n 's/^prior_mode=//p' "$approval")"; test "$(shasum -a 256 "$dest" | awk '{print $1}')" = "$(sed -n 's/^prior_sha256=//p' "$approval")"; fi</automated>
    <fails_when>The proposal or approval record is absent, symlinked, not mode 0400, malformed, multiply keyed, or differs on any tuple field; approval is not explicit; the approved OID is not the current full commit; porcelain is nonempty; the approved committed-blob digest differs; the destination's absence or exact prior digest/mode/stat identity changed; or any installation/finalization dispatch occurs before approval.</fails_when>
  </verify>
  <acceptance_criteria>
    - Human approval names the exact immutable tuple, including distinct prior-version identity/provenance when present, and is never auto-approved.
    - The approved tuple is persisted in a plan-owned regular non-symlink mode-0400 record that Task 3 can consume without reconstructing approval from current HEAD.
    - Any changed component invalidates the approval and leaves the destination untouched.
  </acceptance_criteria>
  <done>The exact trust-root mutation is explicitly approved or the plan stops without changing external state.</done>
</task>

<task type="auto">
  <name>Task 3: Atomically install and self-check the approved loader</name>
  <reversibility rating="reversible">Before replacement, exact prior bytes are preserved with verified digest at the approved private rollback path; rollback can atomically restore those bytes and remove the newly installed file.</reversibility>
  <files>/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env, /Users/jon/.local/bin/mailglass-finalize-phase, conditional /Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.&lt;prior-sha256&gt;, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>Read the exact Task 2 mode-0400 approval record and never derive a substitute OID or tuple from current HEAD. Validate its strict fixed-key schema, then require current HEAD to equal its `installation_source_oid`, the committed loader blob to equal its `source_sha256`, stable porcelain to remain empty, and the destination to have the exact approved absence or exact approved prior lstat/digest/mode/physical identity. Any post-approval destination or source drift hard-stops before backup or replacement. Create the user command, checkpoint, and rollback directories with mode 0700 when absent. Extract only the approved OID-addressed committed blob to a same-directory mode-0500 temporary and verify its digest. For `approval-required-prior-loader`, preserve the distinct prior bytes without following links at the exact approved rollback path, require a regular non-symlink mode-0400 backup with the approved digest, refuse a conflicting existing rollback object, and recheck the live destination's complete approved stat/digest tuple after backup and immediately before replacement. Atomically rename only the approved same-directory temporary onto `/Users/jon/.local/bin/mailglass-finalize-phase`; the absent branch likewise rechecks absence immediately before rename. Verify installed digest, regular non-symlink identity, external containment, and mode 0500. Run `--version` and `--self-check --repo /Users/jon/projects/mailglass --expected-source-oid &lt;installation_source_oid-from-approval-record&gt;`; never pass newly resolved HEAD as the expected installation OID. When a prior loader exists, exercise the recorded restore mechanics without disturbing the installed authority: copy the mode-0400 digest-verified backup to a same-filesystem private restore temporary, apply the recorded prior mode, atomically rename it onto a unique probe sibling, verify probe digest/mode, then remove the probe and every restore temporary while retaining the real rollback object. For the absent branch, assert that all prior and rollback fields are `none`. Before Task 3 verification, write exactly one machine-readable line for every approved field to `164-23-SUMMARY.md`, plus `approval_record_sha256`, `rollback_mode`, `rollback_restore_test`, and `rollback_cleanup_test`; every value must be copied from or cryptographically checked against the persisted approval/evidence, not recaptured. Remove every installation/restore temporary but retain the immutable approval record and conditional rollback object.</action>
  <verify>
    <automated>set -eu; repo=/Users/jon/projects/mailglass; approval=/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env; summary="$repo/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md"; get_approval() { test "$(grep -c "^$1=" "$approval")" = 1; sed -n "s/^$1=//p" "$approval"; }; get_summary() { test "$(grep -c "^$1=" "$summary")" = 1; sed -n "s/^$1=//p" "$summary"; }; test -f "$approval"; test ! -L "$approval"; test "$(stat -f '%Lp' "$approval")" = 400; test "$(get_approval approval_status)" = approved; test "$(get_approval record_version)" = 1; test "$(get_approval phase_plan)" = 164-23; install_oid="$(get_approval installation_source_oid)"; source_digest="$(get_approval source_sha256)"; dest="$(get_approval destination)"; disposition="$(get_approval destination_disposition)"; prior_digest="$(get_approval prior_sha256)"; prior_mode="$(get_approval prior_mode)"; prior_stat_identity="$(get_approval prior_stat_identity)"; prior_physical_identity="$(get_approval prior_physical_identity)"; prior_provenance="$(get_approval prior_provenance)"; rollback_path="$(get_approval rollback_path)"; test "$dest" = /Users/jon/.local/bin/mailglass-finalize-phase; test "$(get_approval install_mode)" = 0500; test "${#install_oid}" = 40; test "${#source_digest}" = 64; case "$install_oid$source_digest" in *[!0-9a-f]*) exit 1;; esac; test "$(git -C "$repo" rev-parse --verify 'HEAD^{commit}')" = "$install_oid"; test "$(git -C "$repo" show "$install_oid:scripts/mailglass_finalize_phase_loader.mjs" | shasum -a 256 | awk '{print $1}')" = "$source_digest"; test -f "$dest"; test -x "$dest"; test ! -L "$dest"; test "$(stat -f '%Lp' "$dest")" = 500; test "$(shasum -a 256 "$dest" | awk '{print $1}')" = "$source_digest"; approval_digest="$(shasum -a 256 "$approval" | awk '{print $1}')"; for key in installation_source_oid source_sha256 destination install_mode destination_disposition prior_sha256 prior_mode prior_stat_identity prior_physical_identity prior_provenance rollback_path; do test "$(get_summary "$key")" = "$(get_approval "$key")"; done; test "$(get_summary approval_record_sha256)" = "$approval_digest"; if test "$disposition" = absent; then test "$prior_digest" = none; test "$prior_mode" = none; test "$prior_stat_identity" = none; test "$prior_physical_identity" = none; test "$prior_provenance" = none; test "$rollback_path" = none; test "$(get_summary rollback_mode)" = none; test "$(get_summary rollback_restore_test)" = not-applicable; test "$(get_summary rollback_cleanup_test)" = passed; else test "$disposition" = approval-required-prior-loader; test "$prior_digest" != none; test "$prior_mode" != none; test "$prior_stat_identity" != none; test "$prior_physical_identity" != none; test "$prior_provenance" = observed-user-owned-external-command-awaiting-explicit-approval; test "$rollback_path" = "/Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.$prior_digest"; test -f "$rollback_path"; test ! -L "$rollback_path"; test "$(stat -f '%Lp' "$rollback_path")" = 400; test "$(shasum -a 256 "$rollback_path" | awk '{print $1}')" = "$prior_digest"; test "$(get_summary rollback_mode)" = 0400; test "$(get_summary rollback_restore_test)" = passed; test "$(get_summary rollback_cleanup_test)" = passed; restore_stage="$(mktemp /Users/jon/.local/bin/.mailglass-restore-stage.XXXXXX)"; restore_probe="${restore_stage}.probe"; trap 'rm -f "$restore_stage" "$restore_probe"' EXIT; install -m "$prior_mode" "$rollback_path" "$restore_stage"; mv "$restore_stage" "$restore_probe"; test -f "$restore_probe"; test ! -L "$restore_probe"; test "$(stat -f '%Lp' "$restore_probe")" = "$prior_mode"; test "$(shasum -a 256 "$restore_probe" | awk '{print $1}')" = "$prior_digest"; rm -f "$restore_probe"; trap - EXIT; test ! -e "$restore_stage"; test ! -e "$restore_probe"; test -f "$rollback_path"; fi; "$dest" --version; "$dest" --self-check --repo "$repo" --expected-source-oid "$install_oid"</automated>
    <fails_when>The approval record is missing, mutable, symlinked, malformed, or not explicitly approved; current HEAD differs from the exact approved installation OID; the approved source digest no longer matches that OID-addressed blob; installed identity/mode/digest differs; self-check receives or reports any OID other than the approved installation OID, observes dirty/wrong-range/repository-contained state, or dispatches Bash; any approved prior field, provenance, rollback path, backup type, mode 0400, or digest is absent or changed; the summary lacks exactly one field or differs from the approval/observed tuple; the same-filesystem atomic restore probe does not reproduce the prior bytes and mode or its temporary/probe cleanup fails; or the retained rollback object disappears.</fails_when>
  </verify>
  <acceptance_criteria>
    - The destination is one regular non-symlink file outside the repository with exact mode 0500.
    - Installed bytes equal the approved commit blob byte-for-byte and share the approved SHA-256.
    - The exact approved installation OID/digest/destination/prior-object tuple survives in a regular non-symlink mode-0400 plan-owned checkpoint record and is the only authority Task 3 consumes.
    - When a distinct prior loader is replaced, its private regular non-symlink mode-0400 backup exists with the approved prior digest/provenance before destination replacement, and the same-filesystem atomic restore-and-cleanup procedure is exercised without removing the retained backup.
    - The summary records the tested restore procedure as same-directory staging plus atomic rename, the prior digest/mode postconditions, the rule that restore temporaries are removed, and the rule that the retained backup is not deleted by the probe.
    - `--version` and `--self-check` pass without Bash, pre-verification, terminal finalization, or evidence collection.
    - The summary records exactly one copy of every approved provenance field plus approval-record digest and conditional rollback verification results for Plan 164-24.
  </acceptance_criteria>
  <done>The authoritative loader is installed outside mutable checkout evaluation with exact approved provenance and is ready for production-boundary regression.</done>
</task>

</tasks>

## Artifacts this phase produces

- `/Users/jon/.local/bin/mailglass-finalize-phase` — external user-installed mode-0500 authority executable.
- `/Users/jon/.local/share/mailglass/checkpoints/164-23-install-proposal.env` — private pre-approval proposal with exact source and destination observations.
- `/Users/jon/.local/share/mailglass/checkpoints/164-23-install-approval.env` — immutable mode-0400 human-approved installation and conditional prior-object tuple consumed by Task 3.
- Conditional `/Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.<prior-sha256>` — retained mode-0400 prior loader bytes.
- `164-23-SUMMARY.md` — exact approved tuple, approval-record digest, and conditional restore/cleanup proof.

<threat_model>
## ASVS Level

OWASP ASVS L1; installation provenance and destination substitution are high-severity trust-root threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Captured Git blob → private install temporary | Only immutable committed bytes may enter installation. |
| Private temporary → user executable path | Destination identity, containment, permissions, and replacement provenance must be checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-84 | Tampering | loader source during installation | high | mitigate | Extract by captured OID, re-check HEAD/porcelain, and require source/destination digest equality. |
| T-164-85 | Elevation of Privilege | destination path | high | mitigate | Reject symlink/non-regular/unexpected targets, require outside-repository containment, and install mode 0500. |
| T-164-86 | Repudiation | install provenance | medium | mitigate | Persist the exact approved OID, digest, destination, mode, and conditional prior-object tuple in a mode-0400 checkpoint record; bind the summary to its digest. |
| T-164-86A | Tampering | distinct prior-loader rollback | high | mitigate | Admit a distinct prior regular loader only through explicit immutable approval of digest/mode/stat identity/provenance/rollback path; reject post-approval drift, preserve it at mode 0400 before replacement, and exercise same-filesystem atomic restore plus temporary cleanup while retaining the backup. |
| T-164-SC | Tampering | external dependency | low | accept | Installation introduces no third-party package; it copies authenticated project source into a user-owned command path. |
</threat_model>

<verification>
Read the persisted approval record, verify exact approved source/destination identity and summary parity, conditionally verify the rollback object's type/mode/digest/provenance and exercise atomic restore/cleanup, then run non-dispatching version and self-check modes with the approved installation OID. No finalization mode runs.
</verification>

<success_criteria>
- Installed executable is outside the checkout and byte-identical to one committed source blob.
- Installation cannot follow or overwrite an unapproved destination, while an explicitly approved distinct prior Mailglass loader can be replaced.
- Any post-approval source or destination drift hard-stops before mutation.
- Replacement preserves a byte-verifiable mode-0400 rollback object and proves its atomic restore-and-cleanup mechanics.
- Non-dispatching self-check proves exact provenance and supported Phase 164 range.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational command exists in its documented trusted state | 164-23 | COVERED |
| REQ | TRTH-03 | Reproducible trusted finalization entry | 164-23 Tasks 1–3 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Installed dual-proof authority before mutable checkout execution | 164-23 Tasks 1–3 | COVERED |
| RESEARCH | exact identity and volatile final proof | Immutable source install without final report capture | 164-23 Tasks 1–3 | COVERED |
| VERIFICATION | gap 3 | Trusted installed command exists before use | 164-23 Tasks 1–3 | COVERED |
| EDGE | TRTH-03 empty/ordering | Non-empty source and install-before-use order | 164-23 flagged assumption/task | COVERED-FLAGGED |
| PROHIBITION | No dirty source or destination substitution | Descriptor-less unresolved constraint retained | 164-23 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md` with the exact approval-record-bound OID/digest/destination/prior-object/rollback evidence and restore-test result. Do not run finalization.
</output>
