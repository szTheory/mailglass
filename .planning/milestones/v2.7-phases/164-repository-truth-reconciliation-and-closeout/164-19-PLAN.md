---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 19
type: execute
wave: 17
depends_on: [164-18]
files_modified:
  - .gsd/extensions/finalize-phase/index.ts
  - scripts/finalize_phase_164.sh
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: `/finalize-phase 164` authenticates the lexical regular-file Phase 164 shim before realpath containment checks and rejects a symlink, missing file, non-regular file, or HEAD/index/worktree divergence before Bash dispatch."
    - "TRTH-03: The dispatcher truthfully supports only Phase 164; another positive phase number is rejected before phase discovery or downstream execution."
    - "D-09/D-10/D-11/TRTH-03: Every repository executable and data input transitively used by Phase 164 finalization is sourced from or byte-bound to authenticated HEAD, so hidden checkout mutation cannot affect the verdict."
    - "TRTH-03: Success, ordinary failure, and print-mode failure all remove the private materialization before the command returns or reports non-success."
  artifacts:
    - path: ".gsd/extensions/finalize-phase/index.ts"
      provides: "Phase-164-only lexical dispatcher and complete authenticated dependency materializer"
    - path: "scripts/finalize_phase_164.sh"
      provides: "Finalizer entry that separates canonical target-repository observations from authenticated code/data roots"
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Closeout composition that invokes repository helpers and reads evidence inputs only through the authenticated authority root"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Real-handler symlink, alternate-phase, print-cleanup, and transitive hidden-mutation regressions"
  key_links:
    - from: ".gsd/extensions/finalize-phase/index.ts"
      to: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh"
      via: "canonical lexical relative path, lstat regular-file check, HEAD/index/worktree authentication, then realpath containment"
      pattern: "lstatSync|164-FINALIZE.sh|authenticateHeadFile"
    - from: ".gsd/extensions/finalize-phase/index.ts"
      to: "scripts/finalize_phase_164.sh and all transitive helpers/data"
      via: "closed Phase 164 dependency manifest materialized from authenticated HEAD bytes under one mode-0700 private root"
      pattern: "finalize_phase_164|closeout_repository_truth|validate_repository_truth|ci_monitor|scheduled_control_evidence|verify_workspace_evidence"
    - from: "scripts/finalize_phase_164.sh"
      to: "scripts/closeout_repository_truth.sh"
      via: "authenticated authority-root path while the canonical repository remains the observation target"
      pattern: "authority_root|closeout_repository_truth"
  flagged_assumptions:
    - "TRTH-03 adjacency remains unresolved by a phase SPEC: exact lexical phase-shim and manifest-relative path equality is treated as authoritative; prefix/sibling paths never substitute."
    - "TRTH-03 empty remains unresolved by a phase SPEC: a missing, empty, untracked, non-regular, symlinked, or unauthenticated manifest member fails before finalization rather than being skipped."
    - "TRTH-03 ordering remains unresolved by a phase SPEC: all manifest members must authenticate and materialize before Bash starts, and cleanup must finish before print-mode failure exits."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT execute finalization dependencies absent from or different from authenticated HEAD."
      status: unresolved
      flagged: true
    - statement: "D-09/D-10/D-11: MUST NOT substitute another lexical phase identity or finalizer for Phase 164."
      status: unresolved
      flagged: true
---

<objective>
Make the project-local Phase 164 finalizer an immutable, truthful, fully authenticated execution chain.

Purpose: Close the lexical-shim, transitive-helper, cross-phase dispatch, and print-cleanup defects before any lifecycle claim is refreshed.
Output: A Phase-164-only dispatcher, authenticated code/data authority root, bound shell composition, and behavioral regressions through the real handler.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-17-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-18-SUMMARY.md
@.gsd/extensions/finalize-phase/index.ts
@scripts/finalize_phase_164.sh
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the exact lexical Phase 164 shim through truthful dispatch and cleanup</name>
  <reversibility rating="reversible">The project-local dispatcher can be tightened or replaced without changing a published package API, persistent data, or remote authority.</reversibility>
  <files>.gsd/extensions/finalize-phase/index.ts, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-17-SUMMARY.md
    - .gsd/extensions/finalize-phase/index.ts
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: replacing the committed lexical `164-FINALIZE.sh` with an in-repository symlink is rejected before any Bash call or finalizer marker, even when the symlink target is a clean tracked regular file.
    - Test 2: the unchanged lexical regular-file shim authenticates at its canonical repository-relative path and reaches the Phase 164 downstream path.
    - Test 3: `165` and any other positive non-164 phase are rejected before phase-directory discovery and produce no downstream marker.
    - Test 4: when the materialized downstream returns nonzero under a real subprocess whose argv includes `--print`, the private directory observed by the fixture is gone before the subprocess exits nonzero.
  </behavior>
  <action>Start RED with a tagged `:phase_164_dispatcher_boundary` expansion of the real TypeScript handler harness. Make the command explicitly Phase-164-only: preserve the public `finalize-phase 164 [--pre-verification]` syntax, but reject every other phase before filesystem lookup because this extension has only one authenticated downstream mapping. Preserve the canonical lexical shim path from phase-directory selection; use `lstatSync` to require a non-symlink regular file, authenticate that exact repository-relative path against HEAD/index/worktree, and perform `realpathSync` only afterward as an additional containment assertion. Refactor `commandError` so it never calls `process.exit` inside the handler: set `process.exitCode`, notify/print as applicable, throw, and let `finally` remove private state before the outer host exits. Retain bounded output, exact directory-count, notification, result-code, and mode behavior.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_dispatcher_boundary --warnings-as-errors --no-deps-check</automated>
    <fails_when>The command exits nonzero, selects zero tagged tests, a symlinked lexical shim reaches Bash, a non-164 phase reaches discovery/dispatch, the valid Phase 164 path regresses, or print-mode failure leaves the private directory.</fails_when>
  </verify>
  <acceptance_criteria>
    - Lexical `lstat` and exact Git authentication precede all realpath target resolution and all materialization.
    - Only phase `164` is accepted; the error is bounded and no Phase 164 finalizer runs for another phase.
    - `commandError` contains no immediate process termination path inside the cleanup-protected handler.
    - The subprocess print-mode regression records the allocated private path and proves it is absent at process completion.
  </acceptance_criteria>
  <done>The accepted command names exactly one lexical regular-file Phase 164 shim, rejects unsupported phases, and cleans private state before every reported outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the complete finalization dependency chain from authenticated HEAD authority</name>
  <reversibility rating="costly">The private authority-root contract coordinates the TypeScript dispatcher and both shell entry points, so undoing it requires a synchronized three-file change and regression update.</reversibility>
  <files>.gsd/extensions/finalize-phase/index.ts, scripts/finalize_phase_164.sh, scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - .gsd/extensions/finalize-phase/index.ts
    - scripts/finalize_phase_164.sh
    - scripts/closeout_repository_truth.sh
    - scripts/verify_workspace_evidence.sh
    - scripts/validate_repository_truth.exs
    - scripts/ci_monitor.cjs
    - scripts/scheduled_control_evidence.sh
    - .github/scheduled-controls.json
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: the real handler authenticates and materializes before Bash every repository script transitively launched by finalization: `finalize_phase_164.sh`, `closeout_repository_truth.sh`, `verify_workspace_evidence.sh`, `validate_repository_truth.exs`, `ci_monitor.cjs`, and `scheduled_control_evidence.sh`.
    - Test 2: the same authority set covers `.github/scheduled-controls.json`, the truth-disposition ledger, the Phase 161 inventory/reconciliation inputs, and every Phase 164 plan/summary/verification/finalization/roadmap/requirements file read by the shell gates; a missing or non-blob member fails before Bash.
    - Test 3: marking each checkout helper assume-unchanged, mutating it with a unique marker, and invoking the real handler cannot execute or read the marker; authenticated committed behavior still runs once with the canonical repository as observation target.
    - Test 4: staged, unstaged, assume-unchanged, symlink, missing, and duplicate-manifest variants at any executable/data member cannot gain pass authority, and success/failure removes the entire private authority root.
  </behavior>
  <action>Extend the RED harness with behavioral mutations against every transitive repository helper and representative data members, including `git update-index --assume-unchanged` mutations that leave ordinary porcelain/diff clean. In the extension, define one closed Phase 164 dependency manifest, expand the numbered plan/summary set from authenticated HEAD rather than working-tree globs, authenticate each exact relative path as a HEAD blob, and materialize its bytes under the same mode-0700 private authority root before any Bash call. Executable files receive mode 0500; data receives read-only mode. Pass that private authority root to the materialized finalizer through one explicit internal contract while continuing to pass `/Users/jon/projects/mailglass` as the canonical repository whose Git/GitHub state is observed. Update `scripts/finalize_phase_164.sh` and `scripts/closeout_repository_truth.sh` so every repository script invocation and every ledger/registry/phase-evidence read resolves beneath the authenticated authority root; checkout paths may be used only as the canonical target for Git status, origin/main, ignored-output, and live repository identity observations. The authority manifest must include the six named scripts, `.github/scheduled-controls.json`, `164-TRUTH-DISPOSITION.tsv`, both Phase 161 workspace-evidence inputs, current Phase 164 plans/summaries/verification/validation/finalization artifacts, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`, plus any other repository path discovered to be transitively opened. Preserve all D-09/D-10/D-11 attempt-one, exact-SHA, freshness, history, output, and no-remote-mutation gates. Do not run either finalization mode during implementation.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_transitive_chain --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_phase_164.sh scripts/closeout_repository_truth.sh</automated>
    <fails_when>The command exits nonzero, selects zero tagged tests, Bash syntax fails, any hidden checkout helper/data marker affects behavior, any transitive member is omitted or read from checkout, the canonical target-repository observation changes, or private cleanup is incomplete.</fails_when>
  </verify>
  <acceptance_criteria>
    - One reviewed manifest accounts for every repository path executed or opened by the Phase 164 finalizer/closeout chain; executable/data classification and materialized relative path are explicit.
    - All manifest entries are exact HEAD blobs and are fully materialized before the first Bash call; no lazy checkout fallback exists.
    - Both shell scripts distinguish authenticated authority-root reads from canonical target-repository Git/GitHub observations, and reject a missing, escaping, or unexpected authority root.
    - Assume-unchanged mutations to each named transitive helper cannot write their markers or alter the normalized report.
    - Existing normal attempt-one CI, natural schedule, exact-main, complete-ledger, stable-porcelain, and no-terminal-capture contracts remain green.
  </acceptance_criteria>
  <done>Phase 164 finalization executes and reads only its authenticated HEAD dependency chain while reporting the live canonical repository state.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.gsd/extensions/finalize-phase/index.ts` — new internal `SUPPORTED_PHASE`/Phase 164 dependency-manifest and private authority-root materialization behavior; preserves the public `finalize-phase 164 [--pre-verification]` command and rejects other phases.
- `scripts/finalize_phase_164.sh` — new internal authenticated-authority-root input consumed separately from the canonical repository target; no new public finalization mode.
- `scripts/closeout_repository_truth.sh` — new internal authenticated-authority-root input used for helper and data paths while existing `--repo`, `--ledger`, `--ci-run-id`, and `--output` behavior remains the operational contract.
- `test/scripts/phase_164_closeout_test.exs` — new `:phase_164_dispatcher_boundary` and `:phase_164_transitive_chain` real-handler regressions, including symlink, alternate-phase, print-cleanup, and assume-unchanged helper/data cases.
- No API, schema, dependency, workflow, release authority, package version, external service, freshness limit, or terminal evidence artifact is created.

<threat_model>
## ASVS Level

OWASP ASVS L1. High-severity lexical substitution and unauthenticated code/data execution block completion until the real-handler regressions pass.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Command phase/shim pathname → authenticated Git object | Only literal Phase 164 lexical identity may select the finalizer. |
| HEAD dependency objects → private authority root | Every executable/data member must authenticate and materialize before dispatch. |
| Private authority root → canonical repository | Authenticated code observes live Git/GitHub state without reading executable or policy inputs from mutable checkout paths. |
| Failure reporting → cleanup | Print and non-print failures must not terminate the process before private-state removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-68 | Spoofing | lexical phase shim / phase argument | high | mitigate | Require phase 164, lexical lstat regular-file identity, exact HEAD path authentication, then containment. |
| T-164-69 | Tampering | transitive executable/data chain | high | mitigate | Authenticate a closed manifest, materialize all members before Bash, and reject missing/non-blob/escaping members. |
| T-164-70 | Elevation of Privilege | hidden checkout helper mutation | high | mitigate | Execute helpers from the private HEAD authority root and prove assume-unchanged markers cannot run. |
| T-164-71 | Information Disclosure | abandoned private materialization | medium | mitigate | Remove the complete authority root in `finally` on success, ordinary failure, and print-mode failure. |
| T-164-72 | Repudiation | cross-phase dispatch result | high | mitigate | Reject unsupported phases before discovery and assert no downstream call/marker for alternate phase input. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run both new tagged groups, the complete focused Phase 164 test set, the direct authoritative-ledger validator, shell syntax, Elixir formatting, and `git diff --check`. Any nonzero exit, zero selected tests, omitted manifest member, checkout marker execution, phase substitution, or leftover private root is failure. Do not invoke `/finalize-phase 164` in this plan.
</verification>

<success_criteria>
- The lexical Phase 164 shim is authenticated before resolution and unsupported phases cannot run Phase 164 code.
- Hidden checkout mutation cannot influence any repository helper or data input used by finalization.
- Print-mode and ordinary failures leave no private materialization.
- The post-execution `/finalize-phase 164` boundary remains intact and unrun.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and unresolved: D-09/D-10/D-11 prohibit execution of code/data absent from or different from authenticated HEAD.
- Kept descriptor-less and unresolved: D-09/D-10/D-11 prohibit substituting another lexical phase identity or finalizer.
- Canon-referral breadcrumb: generic path traversal, command injection, and temporary-file attacks remain ASVS/security-review concerns; exact manifest paths, argv-only execution, containment, restrictive permissions, and cleanup mitigate their phase-specific forms.
- Dropped as routine engineering: missing members, empty output, argument errors, and process exit ordering are explicit behavioral cases rather than new values/safety prohibitions.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence describes the repository's actual operational state | 164-19 | COVERED |
| REQ | TRTH-03 | Reproducible exact-main closeout evidence | 164-19 Tasks 1-2 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, exact canonical state, authenticated exact evidence | 164-19 Tasks 1-2 | COVERED |
| RESEARCH | two-phase closeout using existing evidence tools | Authenticated full-chain composition | 164-19 Task 2 | COVERED |
| PATTERNS | fail-closed shell and real-handler fixtures | Existing extension/shell/ExUnit seams retained | 164-19 Tasks 1-2 | COVERED |
| VERIFICATION | gap 2 | Lexical shim plus complete executable/data dependency chain | 164-19 Tasks 1-2 | COVERED |
| VERIFICATION | gap 3 | Accepted phase maps truthfully to its finalizer | 164-19 Task 1 | COVERED |
| REVIEW | CR-02 | Mutable transitive helpers | 164-19 Task 2 | COVERED |
| REVIEW | CR-03 | Symlink target authenticated instead of lexical shim | 164-19 Task 1 | COVERED |
| REVIEW | CR-04 | Every phase runs Phase 164 | 164-19 Task 1 | COVERED |
| REVIEW | WR-01 | Print failure bypasses cleanup | 164-19 Task 1 | COVERED |
| EDGE | TRTH-03 adjacency, empty, ordering | Exact paths, complete nonempty manifest, authenticate-before-dispatch and cleanup-before-exit | 164-19 flagged assumptions and Tasks 1-2 | COVERED-FLAGGED |
| PROHIBITION | Unauthenticated dependency execution | Descriptor-less unresolved constraint | 164-19 | COVERED-FLAGGED |
| PROHIBITION | Lexical/phase substitution | Descriptor-less unresolved constraint | 164-19 | COVERED-FLAGGED |
| REQ | TRTH-01 | Current maintainer/package truth | Executed baseline 164-16; complete suite rerun | SATISFIED-UNCHANGED |
| REQ | TRTH-02 | Exact tracked-state proof | 164-18; complete suite rerun | COVERED-UPSTREAM |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-19-SUMMARY.md` when done. Do not run pre-verification or terminal `/finalize-phase 164` during execution.
</output>
