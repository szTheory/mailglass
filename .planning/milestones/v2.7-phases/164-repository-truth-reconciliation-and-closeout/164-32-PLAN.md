---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 32
type: execute
wave: 29
depends_on: [164-31]
files_modified:
  - test/scripts/phase_164_closeout_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
user_setup:
  - service: local-command-reinstallation
    why: "Approve replacement of the Plan 164-27 installed loader with exact committed 01-34 source bytes"
    dashboard_config:
      - task: "Review source OID/digest, destination/mode, trusted tools, exact prior object, rollback path, and approval status"
        location: "Plan 164-32 blocking-human checkpoint"
must_haves:
  truths:
    - "TRTH-03: A mode-0400 proposal binds the exact committed 01-34 loader blob, trusted tool identities, Plan 164-27 installed object, destination, mode, and absent digest-addressed rollback target before approval."
    - "TRTH-03: Approval is exactly the proposal plus one approval_status=approved field; any repository/source/tool/destination/prior-object/rollback drift stops before mutation."
    - "D-09/D-10/D-11: Proposal and approval do not run the installed command, pre-verification, terminal finalization, CI dispatch, rerun, release, or publication."
  artifacts:
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Plan 164-32 replacement-tuple schema and drift contract"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Pending exact replacement tuple and approval boundary"
  key_links:
    - from: "captured commit:scripts/mailglass_finalize_phase_loader.mjs"
      to: "Plan 164-32 proposal/approval"
      via: "immutable blob extraction, SHA-256, exact prior lstat/digest/mode, and human approval"
      pattern: "phase_plan=164-32"
  flagged_assumptions:
    - "TRTH-01 unclassified probe: approval is operational evidence only and does not change maintainer/package guidance."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT auto-approve, mutate the installed destination, or run readiness/finalization during the approval plan."
      status: unverified
      flagged: true
    - statement: "T-164-109: terminal evidence MUST NOT be followed by a tracked write; terminal capture remains pending."
      status: unverified
      flagged: true
---

<objective>
Bind the changed exact-history loader to one human-approved, recoverable replacement tuple before external mutation.

Purpose: Preserve the immutable installation trust boundary after the deliberate 01–34 source change.
Output: Tracked tuple contract, mode-0400 proposal, and explicit approval or a safe stop.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-31-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@test/scripts/phase_164_closeout_test.exs
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
</context>

<assumption_delta_decision>
Primary noun: Phase 164 installed authority tuple. Decision: no-change — one exact tuple supersedes the prior exact tuple; no secondary or interchangeable installation authority is introduced.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the committed 01-34 loader into a non-mutating replacement proposal</name>
  <files>test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-31-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  </read_first>
  <behavior>
    - Test 1: proposal preflight requires committed source with terminal last=34, canonical repository/origin, absolute trusted tools, sanitized environment, and ancestry gate.
    - Test 2: the exact Plan 164-27 approval and installed digest/mode/stat identity are the prior-object inputs.
    - Test 3: proposal schema has the same 19 authority keys as Plan 164-27 with phase_plan=164-32 and approval_status absent until human approval.
    - Test 4: proposal publication changes no installed or rollback object.
  </behavior>
  <action>Add a `phase_164_gap_install_proposal` contract that validates the tracked loader's 01–34 authority and the complete immutable proposal schema. From a clean canonical checkout after committing this contract, capture one full HEAD commit OID; extract `scripts/mailglass_finalize_phase_loader.mjs` from that OID, calculate SHA-256, and verify the approved absolute shebang/tool identities and source gates. Lstat/hash the current installed command and require it to equal the exact Plan 164-27 approved regular mode-0500 object. Choose a private digest-addressed rollback path that does not exist. Atomically publish a regular non-symlink mode-0400 `164-32-install-proposal.env` containing record_version, phase_plan, source OID/digest, destination/mode, seven executable paths, prior approval digest, prior digest/mode/stat identity, rollback path, and no approval field. Recheck clean HEAD, committed blob, tools, current destination, and rollback absence after publication. Update `164-FINALIZATION.md` with the displayed pending tuple but no readiness claim.</action>
  <verify>
    <automated>set -eu; proposal=/Users/jon/.local/share/mailglass/checkpoints/164-32-install-proposal.env; test -f "$proposal"; test ! -L "$proposal"; test "$(stat -f '%Lp' "$proposal")" = 400; test "$(grep -c '^phase_plan=164-32$' "$proposal")" = 1; test "$(grep -c '^approval_status=' "$proposal")" = 0; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_gap_install_proposal --warnings-as-errors --no-deps-check</automated>
    <fails_when>Non-zero exit; zero selected tests; proposal is absent/symlinked/wrong-mode/malformed; approval is prefilled; source or prior tuple differs; rollback already exists; installed state changes; or any finalization mode runs.</fails_when>
  </verify>
  <acceptance_criteria>
    - Proposal binds the committed changed source and exact Plan 164-27 prior object.
    - Proposal is private, immutable in content for approval, and creates no destination/rollback mutation.
    - Tracked lifecycle text remains pending and preserves terminal ordering.
  </acceptance_criteria>
  <done>One complete non-mutating replacement tuple is ready for explicit human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve only the persisted Plan 164-32 tuple</name>
  <files>scripts/mailglass_finalize_phase_loader.mjs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-31-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  </read_first>
  <action>Display every persisted proposal field and the proposal SHA-256, plus read-only rechecks for clean exact HEAD, committed source digest, trusted executables, exact Plan 164-27 destination identity, and rollback absence. Pause before any mutation. On the exact response `approved`, atomically publish a regular non-symlink mode-0400 approval file whose bytes are the proposal plus exactly one `approval_status=approved` line; otherwise stop with the destination and rollback untouched. Revalidate proposal/approval parity and all bound external identities after approval publication.</action>
  <what-built>A complete immutable proposal for replacing only the exact prior installed object with exact committed 01-34 loader bytes.</what-built>
  <how-to-verify>Check the displayed source OID/digest, destination/mode, seven executable identities, Plan 164-27 prior approval/object fields, rollback path, proposal digest, and absence of any terminal action.</how-to-verify>
  <verification>The executor proves approval equals proposal plus one approval line and rechecks every tuple field without installing or invoking the command.</verification>
  <resume-signal>Reply `approved` to authorize only the displayed Plan 164-32 tuple, or describe a mismatch to stop.</resume-signal>
  <instructions>Approve only if every persisted field and read-only recheck agrees.</instructions>
  <verify>
    <automated>set -eu; proposal=/Users/jon/.local/share/mailglass/checkpoints/164-32-install-proposal.env; approval=/Users/jon/.local/share/mailglass/checkpoints/164-32-install-approval.env; comparison=$(mktemp); trap 'rm -f "$comparison"' EXIT; test -f "$proposal"; test ! -L "$proposal"; test -f "$approval"; test ! -L "$approval"; test "$(stat -f '%Lp' "$proposal")" = 400; test "$(stat -f '%Lp' "$approval")" = 400; test "$(grep -c '^approval_status=approved$' "$approval")" = 1; sed '/^approval_status=approved$/d' "$approval" &gt; "$comparison"; diff -u "$proposal" "$comparison"</automated>
    <fails_when>Non-zero exit; either record is absent/symlinked/wrong-mode; approval has anything beyond the proposal plus one exact approval line; tuple drift exists; or destination/rollback mutation occurs.</fails_when>
  </verify>
  <acceptance_criteria>
    - Approval scope is the entire immutable tuple, not a path or future source generally.
    - Approval record is mode 0400, non-symlink, exact-copy-plus-status data.
    - Rejection or drift leaves external installed and rollback state unchanged.
  </acceptance_criteria>
  <done>The exact Plan 164-32 replacement is approved, or execution stops before mutation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `test/scripts/phase_164_closeout_test.exs` — `phase_164_gap_install_proposal` schema/drift contract.
- `164-FINALIZATION.md` — pending Plan 164-32 tuple and preserved lifecycle order.
- External execution artifact — `164-32-install-proposal.env` mode-0400 exact tuple.
- External execution artifact — `164-32-install-approval.env` mode-0400 proposal copy plus approval status, only after explicit approval.

<threat_model>
## ASVS Level

OWASP ASVS L1; external executable replacement requires explicit authorization because stale or substituted bytes are a high-severity authority change.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Git commit blob → external proposal | Only one captured committed loader may supply proposed bytes. |
| Human response → approval record | Approval authorizes every tuple field exactly once and no mutation itself. |
| Plan 164-27 installation → rollback proposal | Prior bytes and identity must be exact before replacement can be authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-121 | Tampering | proposal source | high | mitigate | Extract by full commit OID and bind SHA-256/tool identities. |
| T-164-122 | Elevation of Privilege | approval scope | high | mitigate | Blocking human approval over a complete persisted tuple. |
| T-164-123 | Spoofing | prior object | high | mitigate | Require exact Plan 164-27 approval digest plus installed lstat/digest/mode/stat identity. |
| T-164-124 | Repudiation | approval parity | high | mitigate | Approval is byte-for-byte proposal plus one exact status line. |
| T-164-SC | Tampering | external source movement | high | mitigate | Source is an authenticated project Git blob; no package-manager install occurs. |
</threat_model>

<verification>
Verify the tracked proposal contract and persisted proposal/approval parity. Do not mutate or invoke the installed command in this plan.
</verification>

<success_criteria>
- One exact replacement tuple is approved or safely rejected.
- Plan 164-27 prior authority remains recoverable and terminal finalization remains pending.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Supported operational state changes only through auditable authority | 164-32 | COVERED |
| REQ | TRTH-03 | Reproducible installed authority | Tasks 1–2 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, canonical state, protected ordering | Tasks 1–2 | COVERED |
| RESEARCH | — | Immutable identity and recoverability-first mutation | Tasks 1–2 | COVERED |
| VERIFICATION | Gap 1 / range consequence | Reapprove changed installed authority without terminal action | Tasks 1–2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact tuple equality, missing-field stop, approval-before-mutation | Tasks 1–2 | COVERED |
| EDGE | TRTH-01 unclassified | Explicit no-change assumption | must_haves.flagged_assumptions | COVERED-FLAGGED |
| PROHIBITION | No auto-approval/mutation/terminal capture | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-32-SUMMARY.md` when approved. Do not install until Plan 164-33.
</output>
