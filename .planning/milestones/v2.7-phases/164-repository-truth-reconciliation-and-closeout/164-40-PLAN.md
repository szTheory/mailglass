---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 40
type: execute
wave: 37
depends_on: [164-39]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05/D-06/D-12/TRTH-02: A tracked ledger subject is accepted only when its worktree entry is an lstat regular file and its sole stage-0 Git-index record uses regular-file mode 100644 or 100755."
    - "TRTH-02: A worktree symlink to an external regular file and a Git mode-120000 entry both fail through the public validator API and standalone CLI with stable tagged non-success."
    - "TRTH-02 adjacency/ordering edge: literal, metacharacter, prefix-adjacent, newline-bearing, and reordered staged records retain byte-exact subject identity; no neighboring record can satisfy another subject."
    - "TRTH-02 empty edge: empty, truncated, malformed, duplicate, or non-stage-0 index output remains non-success."
  artifacts:
    - path: "scripts/validate_repository_truth.exs"
      provides: "lstat plus allowed-regular-index-mode tracked-subject authority"
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "public API/CLI regressions for external symlinks and Git index modes"
  key_links:
    - from: "scripts/validate_repository_truth.exs:ensure_tracked_subjects_exist/2"
      to: "scripts/validate_repository_truth.exs:tracked_subject_in_index/2"
      via: "lstat regular-file gate followed by exact single stage-0 mode validation"
      pattern: "File.lstat|100644|100755"
    - from: "test/scripts/phase_164_repository_truth_test.exs"
      to: "Mailglass.RepositoryTruthLedger.main/1 and validate/3"
      via: "fully disposable clone containing an external-target symlink staged as mode 120000"
      pattern: "phase_164_validator_file_identity"
  flagged_assumptions:
    - "TRTH-02 adjacency probe: exact byte identity, not prefix or record order, distinguishes adjacent tracked subjects; the staged-record matrix verifies the assumption."
    - "TRTH-02 empty/ordering probes: no empty or multiply framed index result is valid, while the two allowed regular modes are order-independent only after exact-one selection."
  prohibitions:
    - "D-06/D-07: Contractual or forensic proof MUST NOT be hidden or reclassified merely to manufacture a quiet repository."
---

<objective>
Close the central tracked-object trust gap by carrying one real ledger subject from lstat and Git-index identity through the public validator boundary.

Purpose: Enforce D-05, D-06, D-12, and TRTH-02 against external symlink bytes and non-regular Git objects without weakening exact-path behavior.
Output: A fail-closed regular-file/index-mode validator and executable public regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-39-SUMMARY.md
@scripts/validate_repository_truth.exs
@test/scripts/phase_164_repository_truth_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject an external-target tracked symlink through the public validator</name>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
  </read_first>
  <behavior>
    - RED 1: replace tracked `README.md` in a disposable clone with a symlink to a regular file outside the clone, stage it as Git mode 120000, and require `Ledger.validate/3` to return `{:error, {:tracked_subject_not_regular, "README.md"}}`.
    - RED 2: run the standalone script against that clone and canonical ledger; require exit 1, the same stable tag, one usage line, output below 1024 bytes, and no exception or stack-trace framing.
    - RED 3: a normal tracked README with one stage-0 mode-100644 record continues to validate.
  </behavior>
  <action>Add `@regular_index_modes MapSet.new(["100644", "100755"])` and make `ensure_tracked_subjects_exist/2` inspect `Path.join(repo_root, subject)` with `File.lstat/1`; only `%File.Stat{type: :regular}` proceeds to Git-index validation, while every missing, symlink, directory, device, or other type returns `{:error, {:tracked_subject_not_regular, subject}}` without following the entry. Extend `validate_staged_index_output/2` so its exact-one parsed record must have stage `0`, byte-identical `path`, and a mode in `@regular_index_modes`; a single exact record with another well-formed mode returns `{:error, {:tracked_subject_invalid_index_mode, subject, mode}}`. Write the failing public API and CLI tests first under tag `:phase_164_validator_file_identity`, using the existing disposable clone helper and an external file allocated outside the clone. For `test/scripts/phase_164_repository_truth_test.exs`, inherit the mapped `test/scripts/phase_162_release_reconciliation_test.exs` convention: keep phase artifact paths at module scope, parse each fixture result once, assert a nonempty record set before exact identity and uniqueness checks, and assert the precise tagged failure rather than accepting a broad rejection. Keep the canonical CLI success assertion and never expose the external absolute target in the diagnostic.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_validator_file_identity --warnings-as-errors --no-deps-check</automated>
    <fails_when>Non-zero exit; zero selected tests; a symlink or mode-120000 record returns success; the API tag differs; CLI output is unbounded or contains exception/stack formatting; the external target path is printed; or the ordinary mode-100644 control regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - `ensure_tracked_subjects_exist/2` follows no worktree symlink and returns `tracked_subject_not_regular` for every non-regular lstat type.
    - `validate_staged_index_output/2` accepts only exact-one stage-0 records in modes `100644` and `100755`.
    - The external-target symlink fails identically through `Ledger.validate/3` and the standalone CLI.
    - Public diagnostics are stable, bounded, relative to the ledger subject, and contain no stack trace.
  </acceptance_criteria>
  <reversibility rating="reversible">The stricter internal predicate and its tests are local to the repository-truth validator.</reversibility>
  <done>A mutable external target cannot satisfy a tracked regular-file disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the regular-mode and staged-record identity matrix</name>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: exact-one stage-0 records in modes 100644 and 100755 succeed for byte-identical subjects.
    - Test 2: modes 120000, 160000, and syntactically valid non-regular modes fail with `tracked_subject_invalid_index_mode`.
    - Test 3: duplicate, reordered, adjacent, newline-bearing, missing, malformed, and stage-1/2/3 records retain their existing exact failure classes.
    - Test 4: the canonical repository and ledger still print exactly `repository truth ledger: valid` with exit 0.
  </behavior>
  <action>Extend the existing stage-0 describe block rather than creating a parallel parser test. Parameterize the test helper `stage_record/3` with an explicit mode while retaining its default `100644`, add a real executable mode-100755 staged file, and cover 120000 and 160000 through both `validate_staged_index_output/2` and `tracked_subject_in_index/2`. Preserve `tracked_subject_identity_mismatch` for exact-one/path/stage disagreements and `tracked_subject_malformed_output` for framing or syntax errors; use `tracked_subject_invalid_index_mode` only after a record is structurally valid and otherwise exact. Retain the mapped `test/scripts/phase_162_release_reconciliation_test.exs` parse-once and singular-identity pattern: assert the parsed matrix is nonempty, derive the identity tuples once, and require uniqueness before checking reordered, adjacent, or duplicate records and their exact failure classes. Finish by running the whole repository-truth test file and canonical standalone invocation so the tracer cannot narrow D-05/D-12 completeness or D-08 removal behavior.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv</automated>
    <fails_when>Non-zero exit; zero tests; either allowed regular mode fails; a non-regular mode passes; an identity/framing failure changes class without cause; canonical output omits the valid verdict; or D-08 or exact-one ledger checks regress.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both regular Git blob modes pass and every non-regular Git mode fails deterministically.
    - Existing byte-exact path, stage, framing, duplicate, adjacency, and ordering contracts remain green.
    - The canonical ledger remains valid under the public CLI after the stricter trust anchor lands.
  </acceptance_criteria>
  <reversibility rating="reversible">The mode set is an explicit repository-local policy with focused regression coverage.</reversibility>
  <done>The regular-file/index trust anchor is complete without changing ledger scope or dispositions.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Mailglass.RepositoryTruthLedger.@regular_index_modes` — closed allowed set `100644|100755` for tracked regular Git objects.
- `Mailglass.RepositoryTruthLedger.ensure_tracked_subjects_exist/2` — lstat-based worktree type gate.
- `Mailglass.RepositoryTruthLedger.validate_staged_index_output/2` — exact-one stage/path/mode gate with `tracked_subject_invalid_index_mode` failures.
- `:phase_164_validator_file_identity` regressions — public API/CLI proof for external symlink and index-mode attacks.
- New tracked files: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; mutable external bytes and non-regular Git objects crossing into tracked truth are high severity and block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Worktree entry → validator | Filesystem type is untrusted until lstat proves a regular file without following links. |
| Git index bytes → ledger claim | Mode, stage, framing, and path must jointly identify one regular Git object. |
| Validator error → operator | Failure output must be bounded and must not disclose an external target or stack trace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-155 | Tampering | tracked subject worktree entry | high | mitigate | Require lstat type `:regular` before any index or content claim. |
| T-164-156 | Spoofing | stage-0 Git index record | high | mitigate | Accept only exact-one byte-identical stage-0 mode 100644/100755 records. |
| T-164-157 | Information Disclosure | public CLI diagnostic | medium | mitigate | Return subject-relative tagged errors with bounded output and no stack trace. |
| T-164-158 | Denial of Service | malformed staged output | medium | mitigate | Retain finite NUL framing and syntax parsing with deterministic non-success. |
| T-164-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs. |
</threat_model>

<verification>
Run the tagged symlink/index tracer, the complete repository-truth test file, the canonical standalone validator, and diff hygiene. Every test selection and public verdict must be non-vacuous.
</verification>

<success_criteria>
- External symlink bytes and non-regular Git modes cannot satisfy a tracked ledger row.
- Both repository-supported regular Git modes remain valid.
- Exact subject adjacency, empty, stage, ordering, and framing behavior remains deterministic.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts describe actual repository state | 164-40 Tasks 1–2 | COVERED |
| REQ | TRTH-02 | Every tracked subject is classified from exact regular Git objects | Tasks 1–2 | COVERED |
| CONTEXT | D-05/D-06/D-12 | Exact durable subjects and one evidence-backed disposition | Tasks 1–2 | COVERED |
| RESEARCH | — | Exact identity and fail-closed composition using existing Git/Elixir seams | Tasks 1–2 | COVERED |
| VERIFICATION | CR-01 | Reject external-target symlink and non-regular index mode | Tasks 1–2 | COVERED |
| EDGE | TRTH-02 adjacency/empty/ordering | Exact path matrix and empty/malformed record failures | must_haves / Task 2 | COVERED-FLAGGED |
| PROHIBITION | Preserve contractual/forensic proof | Descriptor-less transparency constraint | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-40-SUMMARY.md` after both tasks pass. Do not update completion metadata or invoke either finalization mode.
</output>
