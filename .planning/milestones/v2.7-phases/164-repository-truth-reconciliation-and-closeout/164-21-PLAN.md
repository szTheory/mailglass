---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 21
type: execute
wave: 19
depends_on: [164-20]
files_modified:
  - scripts/mailglass_finalize_phase_loader.mjs
  - scripts/finalize_phase_164.sh
  - test/scripts/phase_164_closeout_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: One pre-evaluation loader installed outside the mutable checkout captures one full commit OID and uses that same OID for every tree and blob authority operation."
    - "D-09/D-10/D-11/TRTH-03: The loader re-resolves checkout HEAD immediately before Bash and refuses dispatch when it differs from the captured authority OID."
    - "D-12/TRTH-03: The authenticated Phase 164 baseline contains exactly one PLAN and one SUMMARY for every number 01 through 20; the additive closure history through Plan 24 is also explicit and cannot shrink when either member of a pair disappears."
    - "TRTH-03: Middle-pair, baseline-terminal-pair, current-terminal-pair, and moving-HEAD fixtures all stop before the downstream marker or finalizer executes."
  artifacts:
    - path: "scripts/mailglass_finalize_phase_loader.mjs"
      provides: "Tracked source for the standalone, globally installed pre-evaluation finalization authority"
      exports: ["main", "captureAuthorityCommit", "expectedPhaseArtifacts", "authenticateCommitFile"]
    - path: "scripts/finalize_phase_164.sh"
      provides: "Terminal state gate consuming only the fixed authenticated numbered history"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Moving-HEAD and exact numbered-pair production-seam regressions"
  key_links:
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "one captured Git commit object"
      via: "full-OID capture, OID-addressed cat-file/show/ls-tree/diff operations, and a final HEAD equality check"
      pattern: "rev-parse.*HEAD\\^\\{commit\\}|cat-file|ls-tree|show"
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "scripts/finalize_phase_164.sh"
      via: "mode-0500 private materialization followed by Bash only after all authentication completes"
      pattern: "mailglass-finalize-164|0o500|spawn"
    - from: "scripts/mailglass_finalize_phase_loader.mjs --self-check"
      to: "captured commit:scripts/mailglass_finalize_phase_loader.mjs"
      via: "non-dispatching repository-containment, clean-state, source-byte, source-OID, and exact-range validation"
      pattern: "--self-check|--expected-source-oid|01.*24"
  flagged_assumptions:
    - "TRTH-01 unclassified probe remains unresolved: this plan changes no package/version claim; Plan 164-22 will contract-check the supported command wording instead of inferring a broader documentation edge."
    - "TRTH-03 adjacency probe: adjacent plan numbers remain distinct exact two-member identities; neither a prefix nor the next number can satisfy a missing pair."
    - "TRTH-03 empty probe: an absent, empty, singleton, duplicate, or incomplete numbered set is non-authoritative and cannot dispatch."
    - "TRTH-03 ordering probe: authentication order is fixed as capture OID, enumerate exact set, authenticate/materialize all members, re-check HEAD, then dispatch; tree enumeration order has no effect on set equality."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT let mutable checkout executable bytes establish or replace the repository authority used for finalization."
      status: unresolved
      flagged: true
    - statement: "D-12: MUST NOT infer completeness from only the PLAN/SUMMARY pairs that remain present."
      status: unresolved
      flagged: true
---

<objective>
Create the immutable finalization authority that closes the moving-HEAD and shrinking-history defects.

Purpose: Establish one pre-evaluation trust root that authenticates a complete repository state before any Phase 164 shell code runs.
Output: A standalone installable loader, fixed numbered-history gate, and adversarial production-seam regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-19-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-20-SUMMARY.md
@.gsd/extensions/finalize-phase/index.ts
@scripts/finalize_phase_164.sh
@test/scripts/phase_164_closeout_test.exs
</context>

<assumption_delta_decision>
Primary noun: supported phase identity. Decision: no-change — “alternate phase” is a hostile-input fixture, not a second supported identity; Phase 164 remains the sole command target.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one immutable commit through authentication to private Bash dispatch</name>
  <reversibility rating="costly">The installed-loader boundary coordinates command installation, Git object authentication, fixture harnesses, and the terminal shell entry contract.</reversibility>
  <files>scripts/mailglass_finalize_phase_loader.mjs, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-19-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-19-SUMMARY.md
    - .gsd/extensions/finalize-phase/index.ts
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: an unchanged disposable repository reaches a private downstream fixture only after every Git object read uses one captured 40-character commit OID.
    - Test 2: advancing checkout HEAD after the first authority read makes the real loader exit nonzero before Bash and before any marker is written.
    - Test 3: staged, unstaged, missing, symlinked, or assume-unchanged mutations in a declared repository dependency cannot affect the private execution bytes.
    - Test 4: invalid phase/mode arguments and any repository-discovery failure emit bounded diagnostics and exit nonzero.
    - Test 5: `--version` emits the stable loader identity and exits zero without repository discovery, private Bash materialization, evidence collection, or Bash spawn.
    - Test 6: `--self-check --repo &lt;canonical-repository&gt; --expected-source-oid &lt;full-oid&gt;` accepts only an executable outside that repository whose own bytes equal the committed loader blob at the supplied OID, reports the compiled exact Phase 164 terminal range 01–24, and observes a clean repository whose current HEAD contains the same loader bytes.
    - Test 7: self-check rejects a repository-contained executable, wrong or abbreviated OID, changed committed source bytes, dirty/staged/untracked state, a compiled terminal range other than 01–24, and source-byte drift between the supplied installation OID and current HEAD; every rejection occurs without spawning Bash.
  </behavior>
  <action>Start RED with subprocess tests tagged `phase_164_immutable_loader` that invoke the real standalone Node entry, use disposable Git repositories, and instrument Git through a PATH shim that advances HEAD at a deterministic call boundary. Create `scripts/mailglass_finalize_phase_loader.mjs` as the tracked source for a command installed outside the checkout. Preserve the existing Phase-164-only argument, containment, regular-file, exact-index, bounded-output, restrictive-permission, and finally-cleanup contracts from the project extension. After repository discovery, resolve exactly one lowercase full commit OID with `git rev-parse --verify HEAD^{commit}` and retain it as immutable data. Pass that OID, never symbolic HEAD, to every `cat-file`, `show`, `ls-tree`, and commit-relative staged comparison. Continue checking the live index and working tree against that captured commit so staged, unstaged, untracked, symlink, and assume-unchanged substitutions remain non-authoritative. Authenticate every dependency and materialize every executable/data byte beneath one mode-0700 private root, with executable files mode 0500 and data mode 0400. Immediately before spawning Bash, re-run full checkout HEAD resolution and require byte equality with the captured OID; do not dispatch when HEAD moved. Implement two explicit inspection branches before finalization dispatch: `--version` prints one stable loader/version identity without repository or Bash work; `--self-check --repo &lt;absolute-path&gt; --expected-source-oid &lt;40-lowercase-hex&gt;` resolves the repository's physical root, requires the running executable's physical path to be a regular non-symlink outside that root, rejects noncanonical/dirty staged/unstaged/untracked state, verifies the compiled terminal range is exactly 01–24, compares the running executable bytes to the supplied installation OID's loader blob, and proves current HEAD contains byte-identical loader source even when HEAD is a later metadata-only commit. Self-check reports the supplied installation OID, current OID, loader SHA-256, physical executable path, mode, and terminal range; it must not require those two OIDs to be equal. Instrument the subprocess harness so both inspection branches fail if the Bash-spawn seam is called. Export narrow pure helpers for direct tests while making direct CLI invocation the only path that calls `main`. Do not import, evaluate, or delegate to `.gsd/extensions/finalize-phase/index.ts`, and do not run either finalization mode against the canonical repository during implementation.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_immutable_loader --warnings-as-errors --no-deps-check &amp;&amp; node --check scripts/mailglass_finalize_phase_loader.mjs &amp;&amp; git diff --check</automated>
    <fails_when>The tag selects zero tests; any command exits nonzero; any tree/blob operation still addresses symbolic HEAD; the moving-HEAD fixture reaches Bash or writes a marker; mutated checkout dependency bytes execute; version or self-check reaches the Bash-spawn seam; self-check accepts repository containment, dirty state, wrong source bytes/OID, or a terminal range other than 01–24; private state survives; syntax is invalid; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - The real loader is exercised as a subprocess and reaches a real private Bash fixture on the unchanged positive path.
    - One captured full commit OID is passed to every commit-object lookup and comparison; no awaited authentication operation re-resolves symbolic HEAD as its object authority.
    - A deterministic mid-authentication HEAD advance exits nonzero before Bash and identifies the authority change in bounded output.
    - The final live HEAD equality check occurs after authentication/materialization and immediately before the only Bash spawn.
    - All repository-derived executable/data bytes come from the captured commit and are cleaned in a finally path.
    - Direct invalid invocation is fail-closed, while importing the module for tests has no side effects.
    - Version and self-check are explicit non-dispatching CLI modes; tests prove neither can reach the Bash spawn seam.
    - Self-check binds installed bytes to the supplied immutable installation OID while separately proving the current commit retains byte-identical loader source, so later metadata-only commits do not invalidate installation provenance.
  </acceptance_criteria>
  <done>The installable loader proves one complete immutable commit and refuses a moving checkout before any phase shell code can execute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authenticate the exact numbered history and bind the shell gate to it</name>
  <reversibility rating="costly">The explicit terminal plan number is a coordinated finalization contract that must change whenever another Phase 164 gap plan is authorized.</reversibility>
  <files>scripts/mailglass_finalize_phase_loader.mjs, scripts/finalize_phase_164.sh, test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-15-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-15-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: exactly one PLAN and SUMMARY for every baseline number 01 through 20 is required, including exact distinct identities for adjacent numbers.
    - Test 2: deleting both members of a middle pair such as 10 makes authentication fail rather than shrink the expected set.
    - Test 3: deleting both members of baseline terminal pair 20 makes authentication fail.
    - Test 4: because this additive gap set ends at Plan 24, exactly one pair for 21 through 24 is also required before terminal finalization; deleting current terminal pair 24 fails.
    - Test 5: duplicates, malformed suffixes, singleton pairs, empty enumeration, or any unexpected numbered PLAN/SUMMARY path in the authoritative range fail closed independent of ls-tree order.
  </behavior>
  <action>Extend the RED subprocess matrix before production edits. Replace discovery-derived numbered dependencies with an authoritative constant range whose locked baseline is 01–20 and whose explicitly authorized additive closure range is 21–24. Generate exactly two repository-relative paths per number and compare the NUL-delimited captured-OID `ls-tree` result as a set and cardinality against that expected set; reject missing, duplicate, singleton, malformed, and unexpected numbered members instead of deriving expectations from survivors. Include all expected PLAN/SUMMARY paths in the private authority root. Tighten `require_terminal_state` in `scripts/finalize_phase_164.sh` to iterate the same explicit 01–24 range and require both members as regular authenticated files; it must not glob the remaining PLAN files to decide which summaries matter. Add a stable source contract asserting the loader and shell terminal constants agree, and preserve the pre-verification summary rule as a separate lifecycle contract. Add exact ledger rows for the new tracked loader and its durable consumer per D-05/D-06/D-12 without removing or weakening any existing disposition. Do not make Plan 25 implicit: any later additive plan must deliberately update both constants and the contract test.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_immutable_loader --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_phase_164.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; git diff --check</automated>
    <fails_when>The tag selects zero tests; any deletion/duplicate/malformed fixture reaches Bash; Plan 20 or Plan 24 can vanish as a complete pair; loader and shell ranges disagree; the canonical ledger is invalid; shell syntax fails; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - The authoritative expected baseline is the exact 40-path set formed by one PLAN and one SUMMARY for each number 01–20.
    - The complete terminal set additionally names one exact pair for each authorized gap plan 21–24, preventing the new closure history from becoming an unverified suffix.
    - Paired deletion of Plan 10, Plan 20, or Plan 24 produces nonzero authentication and no Bash call.
    - The loader validates NUL framing, exact cardinality, uniqueness, exact repository-relative identity, and order-independent set equality at the captured commit.
    - The shell gate consumes authenticated files for the same explicit range and never derives required summaries from a surviving glob.
    - The canonical ledger includes one complete evidence-backed row for the installed-loader source and remains valid.
  </acceptance_criteria>
  <done>Neither a middle nor terminal PLAN/SUMMARY pair can disappear silently, and Bash receives the complete fixed authenticated Phase 164 history.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/mailglass_finalize_phase_loader.mjs` — new standalone CLI source and exported authentication helpers: `main`, `captureAuthorityCommit`, `expectedPhaseArtifacts`, and `authenticateCommitFile`.
- `scripts/finalize_phase_164.sh` — fixed-range terminal history enforcement consuming authenticated pairs 01–24.
- `test/scripts/phase_164_closeout_test.exs` — new `phase_164_immutable_loader` moving-HEAD and exact-set subprocess group.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv` — exact disposition for the new loader source.

<threat_model>
## ASVS Level

OWASP ASVS L1; high-severity trust-bootstrap, mixed-commit, and evidence-omission threats block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Installed loader → mutable checkout | The authority process is evaluated before and outside repository-controlled extension bytes. |
| Captured commit → private authority root | Git object bytes cross into executable/data materialization under restrictive permissions. |
| Private authority root → Bash | Dispatch occurs only after complete authentication and a final checkout-HEAD equality check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-76 | Tampering | commit-object authentication | high | mitigate | Capture one full OID once; address every tree/blob operation by that OID; re-check live HEAD immediately before Bash. |
| T-164-77 | Repudiation | numbered Phase 164 history | high | mitigate | Compare captured-OID NUL tree output to the explicit exact PLAN/SUMMARY set through Plan 24, including locked baseline 01–20. |
| T-164-78 | Elevation of Privilege | private Bash dispatch | high | mitigate | Materialize only authenticated bytes with 0700/0500/0400 modes and remove the root in finally. |
| T-164-79 | Denial of Service | adversarial Git output | medium | mitigate | Reject malformed framing/cardinality and bound diagnostics/output. |
| T-164-SC | Tampering | package supply chain | low | accept | No npm/pip/cargo install or third-party dependency is introduced; the later install copies one authenticated repository blob to a private user command path. |
</threat_model>

<verification>
Run the named immutable-loader subprocess group non-vacuously, validate Node and Bash syntax, validate the canonical ledger, and require clean diff formatting. Do not run canonical finalization.
</verification>

<success_criteria>
- Every authority read is pinned to one full commit OID.
- Moving HEAD prevents Bash dispatch.
- Exact PLAN/SUMMARY pairs 01–20 and authorized closure pairs 21–24 cannot shrink silently.
- The installed-loader source and shell consumer remain syntax-valid and ledgered.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence describes actual repository state | 164-21 | COVERED |
| REQ | TRTH-03 | Reproducible final closeout evidence | 164-21 Tasks 1–2 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Immutable dual-proof execution and exact clean-main identity | 164-21 Task 1 | COVERED |
| CONTEXT | D-12 | Exactly one disposition/history identity | 164-21 Task 2 | COVERED |
| RESEARCH | two-phase closeout and exact identity | Captured OID and post-metadata terminal boundary retained | 164-21 | COVERED |
| VERIFICATION | gap 1 | One OID plus moving-HEAD regression | 164-21 Task 1 | COVERED |
| VERIFICATION | gap 2 | Exact expected Plan/Summary set and deletion regressions | 164-21 Task 2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact set and authentication sequence are explicit | 164-21 flagged assumptions/tasks | COVERED-FLAGGED |
| PROHIBITION | No mutable checkout authority or survivor-derived completeness | Descriptor-less unresolved constraints retained | 164-21 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md` when done. Do not invoke canonical pre-verification or terminal finalization.
</output>
