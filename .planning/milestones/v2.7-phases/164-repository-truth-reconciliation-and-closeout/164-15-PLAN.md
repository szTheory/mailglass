---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 15
type: execute
wave: 13
depends_on: [164-14]
files_modified:
  - scripts/finalize_phase_164.sh
  - scripts/scheduled_control_evidence.sh
  - test/scripts/phase_164_closeout_test.exs
  - test/scripts/scheduled_control_evidence_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [TRTH-01, TRTH-02, TRTH-03]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/TRTH-01: The integrated Phase 164 closeout reruns the existing maintainer and package contracts without changing the settled protected-release authority, supported package truth, or historical boundaries."
    - "D-05/D-06/D-07/D-08/D-12/TRTH-02: The integrated closeout reruns the authoritative exact-one ledger contract and preserves every tracked durable artifact, narrow ignore rule, and locked D-08 disposition."
    - "D-09/D-10/D-11/TRTH-03: Terminal finalization accepts a passing verifier only when its explicit `verified_implementation_sha` is a valid ancestor of terminal HEAD and every commit on the intervening first-parent chain changes only paths in the exact completion-metadata allowlist."
    - "D-09/D-10/TRTH-03: Pre-verification requires the complete executed repair summary set through `164-13-SUMMARY.md`; absence of Plan 13 proof stops before CI or scheduled evidence collection."
    - "TRTH-03: Phase-owned hostile-path fixtures clean up only an exclusively allocated directory whose ownership token and non-symlink identity still match this test invocation."
    - "D-11/TRTH-03: Scheduled-run freshness is a closed interval from zero through the registry maximum age; malformed or future `updated_at` values fail both the producer sweep and finalizer's independent raw-source validation."
  artifacts:
    - path: "scripts/finalize_phase_164.sh"
      provides: "Hash-bound terminal verifier gate, complete pre-verification prerequisites, and independent two-sided freshness validation"
    - path: "scripts/scheduled_control_evidence.sh"
      provides: "Two-sided scheduled-run freshness gate"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Source-after-verification, missing-summary, owned-cleanup, and finalizer timestamp regressions"
    - path: "test/scripts/scheduled_control_evidence_test.exs"
      provides: "Invalid and future scheduled timestamp fixtures at the evidence producer boundary"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Human lifecycle contract for verified implementation SHA binding and Plans 01-13 pre-verification completeness"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md"
      provides: "Plan 164-15 task-to-command, observable failure-direction, threat, and revised terminal SHA lifecycle coverage"
  key_links:
    - from: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md"
      to: "scripts/finalize_phase_164.sh"
      via: "`status: passed` plus a full-hex `verified_implementation_sha`, ancestor check, and per-first-parent-commit exact metadata-only path allowlist"
      pattern: "verified_implementation_sha|rev-list --first-parent|diff-tree"
    - from: "164-13-SUMMARY.md"
      to: "pre-verification evidence collection"
      via: "required summary presence before any `gh`, closeout, CI, or scheduled-control command"
      pattern: "164-13-SUMMARY.md"
    - from: "scripts/scheduled_control_evidence.sh"
      to: "scripts/finalize_phase_164.sh"
      via: "matching nonnegative and registry-bounded age predicates exercised by independent fixtures"
      pattern: "age >= 0|now - .*fromdateiso8601"
  flagged_assumptions:
    - "TRTH-01 unclassified: no plain SPEC classifies another edge family; the already passing maintainer/package contracts are rerun unchanged as the authoritative integrated proof."
    - "TRTH-02 adjacency/empty/ordering: these specless rows remain governed by the existing exact-subject, non-vacuous, set-based production ledger validator and its Plan 164-13 regressions; this plan reruns them and adds no collection semantics."
    - "TRTH-03 adjacency/empty/ordering is resolved from D-09 through D-11 and the current gaps: SHA/path identity is exact, missing verifier/summary/timestamp input fails closed, and metadata path ordering cannot affect exact allowlist membership."
  prohibitions:
    - statement: "Terminal or scheduled evidence must not report quiet when its verifier binding or timestamp is stale, missing, malformed, future-dated, or separated from terminal HEAD by a source change."
      status: unverified
      flagged: true
    - statement: "A phase-owned test must not recursively delete a path unless this invocation exclusively created it and still proves ownership at cleanup time."
      status: unverified
      flagged: true
---

<objective>
Close the final verifier-binding, prerequisite-completeness, fixture-ownership, and timestamp gaps through one production closeout path.

Purpose: Ensure the terminal quiet verdict describes the implementation actually verified and that every phase-owned evidence check is safe and temporally possible.
Output: Hash-bound finalization, complete pre-verification gating, owned test fixtures, two-sided freshness checks, and focused regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-13-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-14-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
@scripts/finalize_phase_164.sh
@scripts/scheduled_control_evidence.sh
@test/scripts/phase_164_closeout_test.exs
@test/scripts/scheduled_control_evidence_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a verified implementation SHA through terminal HEAD authorization</name>
  <reversibility rating="costly">The verifier frontmatter field and metadata allowlist become a shared terminal lifecycle contract requiring coordinated verifier, finalizer, test, and documentation changes to revise.</reversibility>
  <files>scripts/finalize_phase_164.sh, test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-14-SUMMARY.md
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <behavior>
    - Test 1: terminal state rejects a passing verification document with an absent, malformed, nonexistent, or non-ancestor `verified_implementation_sha` before evidence collection.
    - Test 2: terminal state accepts a full-hex verified implementation SHA when terminal HEAD differs only at `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, or `.planning/STATE.md`.
    - Test 3: adding a commit that changes `scripts/finalize_phase_164.sh` or any other path outside that exact allowlist after the passing verifier's SHA makes terminal state nonzero before evidence collection.
    - Test 4: committing a forbidden source change and then committing its revert still makes terminal state nonzero before evidence collection even though the verified-SHA-to-HEAD trees have no net difference at that path.
    - Test 5: current lifecycle and validation documentation require the verifier to record the evaluated implementation SHA and name the same per-first-parent-commit metadata-only allowance as the executable gate.
  </behavior>
  <action>Write process-level failing fixtures first, using an isolated Git repository with distinct verified, verification-metadata, completion-metadata, and post-verification-source commits. Include a history-erasure regression that commits a forbidden source change, commits a revert of that change, and asserts terminal rejection before the evidence-collection marker despite the restored terminal tree. Extend the terminal verification frontmatter contract to require `verified_implementation_sha` as exactly 40 lowercase hexadecimal characters. In `require_terminal_state`, prove the named commit exists and is an ancestor of terminal HEAD, enumerate every commit in `verified_implementation_sha..HEAD` with `git rev-list --first-parent --reverse "$verified_implementation_sha..HEAD"`, and inspect every enumerated commit with `git diff-tree --no-commit-id --name-only -r "$commit^1" "$commit"` so its changed paths are measured against its first parent. Reject the range when any per-commit path is outside the four exact tracked completion-metadata paths in the behavior block. For a merge commit, this inspects only the merge result relative to its first parent; do not traverse or reject merely because its second parent contains older unrelated history. Do not use a single endpoint tree diff, because a forbidden change followed by a revert has no net path difference. Do not infer authority from timestamps, commit messages, branch names, or the most recent commit that touched the verifier. Keep `status: passed` necessary but insufficient. Update `164-FINALIZATION.md` to make the field a required verifier output and explain the bounded per-commit first-parent metadata delta. Add the `164-15-01` row to `164-VALIDATION.md` with this task's exact automated command, observable failure directions for invalid/non-ancestor SHA, forbidden history (including change-then-revert), and permitted metadata-only history, threat T-164-53, and the revised verified-SHA terminal lifecycle. Preserve D-09/D-10/D-11: terminal evidence remains ignored and runs only after tracked completion reaches protected main; no command dispatches, reruns, pushes, merges, publishes, or modifies protected controls.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_phase_164.sh</automated>
    <fails_when>The command exits nonzero, Bash syntax fails, a malformed/unrelated SHA passes, a forbidden commit or its later revert disappears from enforcement, either forbidden-history fixture reaches evidence collection, a permitted completion-metadata-only fixture is rejected, or the 164-15-01 validation row lacks the exact command and observable failure directions.</fails_when>
  </verify>
  <acceptance_criteria>
    - `status: passed` without a valid `verified_implementation_sha` is non-authoritative and returns nonzero.
    - The exact verified commit is resolved from frontmatter, exists in the canonical repository, and is an ancestor of terminal HEAD.
    - The only permitted intervening paths are the Phase 164 verifier plus root planning `ROADMAP.md`, `REQUIREMENTS.md`, and `STATE.md`; prefix, sibling, case, and directory matches do not expand the allowlist.
    - Every commit on the first-parent chain from the verified implementation SHA (exclusive) through terminal HEAD (inclusive) is inspected against its first parent, so both a committed source change and a later reverting commit remain visible and cause rejection before any CI or scheduled-control observation.
    - A merge is judged by its result relative to its first parent; unrelated history reachable only through the second parent does not independently fail the allowlist.
    - `164-FINALIZATION.md`, `164-VALIDATION.md`, and the production gate state the same hash and per-commit first-parent metadata-delta contract, and validation row `164-15-01` maps its exact automated command, observable pass/fail directions, and T-164-53.
  </acceptance_criteria>
  <done>Terminal finalization is end-to-end bound to the exact implementation evaluated by the passing verifier, with only narrowly named completion metadata allowed afterward.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require the complete repair summary set and contain hostile fixture cleanup</name>
  <reversibility rating="reversible">Both changes tighten internal prerequisites and test ownership without changing the closeout report, remote authority, or durable ledger schema.</reversibility>
  <files>scripts/finalize_phase_164.sh, test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-12-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-13-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-14-SUMMARY.md
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: pre-verification succeeds past the prerequisite seam only when every summary from `164-01-SUMMARY.md` through `164-13-SUMMARY.md` exists.
    - Test 2: removing only `164-13-SUMMARY.md` returns nonzero and leaves the evidence-collection marker absent.
    - Test 3: the lexical-prefix collision fixture uses a directory created atomically for this invocation and cleanup proceeds only while an invocation-specific ownership token matches and the path remains a real directory rather than a symlink.
    - Test 4: a pre-existing candidate path or a replaced/missing ownership token is never recursively removed by teardown.
  </behavior>
  <action>Extend `require_pre_verification_state` through Plan 13, and update `164-FINALIZATION.md` from Plans 01-11 to the exact Plans 01-13 prerequisite. Add a process regression that omits only `164-13-SUMMARY.md` and asserts failure occurs before the marker representing any `gh`, closeout, CI, or scheduled collection. Repair the existing sibling lexical-prefix fixture without weakening the identity attack: allocate its directory exclusively, fail the test rather than reuse it when allocation collides, place a random per-invocation ownership token inside it, and register cleanup that first verifies lstat is a directory rather than a symlink, the resolved parent/basename remain the expected sibling shape, and the token contents still match. Recursively remove only after all ownership checks pass; otherwise leave the path untouched and fail visibly. Keep all other disposable fixture paths beneath `temporary_root!()` and never call `File.rm_rf!/1` on an accepted pre-existing path. Add the `164-15-02` row to `164-VALIDATION.md` with this task's exact automated command, observable failure directions for the missing Plan 13 summary and each cleanup-ownership violation, and threats T-164-54 and T-164-55.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_phase_164.sh</automated>
    <fails_when>The command exits nonzero, missing Plan 13 does not block before collection, fixture allocation reuses an existing sibling, teardown can follow a symlink or delete a token-mismatched path, the Plans 01-13 documented and executable prerequisites differ, or the 164-15-02 validation row lacks the exact command, threats, and observable failure directions.</fails_when>
  </verify>
  <acceptance_criteria>
    - Pre-verification mechanically requires all thirteen expected implementation/repair summaries and explicitly reports the missing Plan 13 filename.
    - The missing-summary fixture proves no downstream collection command ran.
    - The sibling fixture retains the lexical-prefix adversarial value while exclusive creation and ownership-token validation make recursive cleanup invocation-owned.
    - A pre-existing or identity-replaced sibling survives teardown unchanged.
    - Validation row `164-15-02` maps the exact automated command, the missing-summary and cleanup-ownership failure directions, and T-164-54/T-164-55.
    - No production repository, ledger, output-path, CI, scheduled-control, workflow, or release-authority boundary is broadened.
  </acceptance_criteria>
  <done>The reusable pre-verification gate cannot omit Plan 13 proof, and rerunning its hostile repository tests cannot delete unrelated developer data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject invalid and future timestamps at both scheduled-evidence boundaries</name>
  <reversibility rating="reversible">The predicates close an acceptance bug within existing registry limits and can be reverted locally without changing evidence schemas or schedule policy.</reversibility>
  <files>scripts/scheduled_control_evidence.sh, scripts/finalize_phase_164.sh, test/scripts/scheduled_control_evidence_test.exs, test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
    - .github/scheduled-controls.json
    - scripts/scheduled_control_evidence.sh
    - scripts/finalize_phase_164.sh
    - scripts/closeout_repository_truth.sh
    - test/scripts/scheduled_control_evidence_test.exs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: the scheduled-control sweep rejects an unparsable `updated_at` and preserves a nonzero result rather than a pass report.
    - Test 2: the scheduled-control sweep rejects an otherwise authoritative run timestamped one day in the future.
    - Test 3: `raw_sources_are_acceptable` rejects the same invalid and future timestamps even if an upstream sweep is forged to status pass.
    - Test 4: age zero and in-range past timestamps remain accepted, while a timestamp older than the per-control registry maximum remains rejected.
  </behavior>
  <action>Add failing fixtures at both production seams before changing predicates. In `scripts/scheduled_control_evidence.sh`, parse `updated_at` fail-closed and require computed age to be both greater than or equal to zero and less than or equal to that control's `max_age_seconds`. Apply the identical closed interval independently inside `raw_sources_are_acceptable` in `scripts/finalize_phase_164.sh`; do not trust aggregate `status: pass` and do not read an ambient or caller-inflated age. Use the tracked registry's existing per-control values without adding clock skew, expanding ages, changing cron, selecting another run, or operating any workflow. Ensure malformed date parsing yields nonzero without coercion. Retain exact attempt-one, natural schedule, branch, head/workflow SHA, reason, payload digest, and artifact digest checks. Add the `164-15-03` row to `164-VALIDATION.md` with this task's exact automated command, observable failure directions for invalid, future, zero-age, in-range, and stale timestamps at both seams, and threats T-164-56 and T-164-57. Update the validation sampling/lifecycle prose, verification assets, Wave 0 ownership, sign-off counts, and pending terminal gate so the document explicitly covers Plans 164-01 through 164-15 and describes the verified implementation SHA plus per-first-parent-commit allowlist lifecycle established by Task 1; do not claim these Plan 164-15 checks are already green before execution.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_phase_164.sh scripts/scheduled_control_evidence.sh</automated>
    <fails_when>The command exits nonzero, Bash syntax fails, either production layer accepts invalid/future time, age zero or an in-range fixture is rejected, stale evidence passes, any registry age/provenance assertion is weakened, or validation remains scoped through Plan 164-14 or omits the 164-15-03 command, assets, threats, and failure directions.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both production predicates require `0 &lt;= now - updated_at &lt;= max_age_seconds` using the authoritative registry value.
    - Invalid ISO-8601 and one-day-future fixtures are independently non-pass in the sweep and finalizer tests.
    - Boundary-age and ordinary current fixtures pass, and over-age fixtures remain non-pass.
    - No clock-skew exception, age expansion, schedule/workflow change, dispatch, rerun, or caller-selected substitute is introduced.
    - `164-VALIDATION.md` contains rows `164-15-01`, `164-15-02`, and `164-15-03`, their exact task commands, verification assets, observable failure directions, and threat mappings covering exactly T-164-53 through T-164-57; its lifecycle prose describes the bound verified SHA and per-first-parent-commit terminal history gate.
    - The complete focused Phase 164 suite, authoritative ledger CLI, formatting, shell syntax, and `git diff --check` remain green.
  </acceptance_criteria>
  <done>Temporally impossible evidence is rejected where it is produced and again where terminal finalization independently validates it.</done>
</task>

</tasks>

## Artifacts this phase produces

- `verified_implementation_sha` — new required `164-VERIFICATION.md` frontmatter field consumed by terminal finalization; full lowercase 40-hex commit identity.
- `test/scripts/phase_164_closeout_test.exs` — new terminal source-after-verification, forbidden-change-then-revert history, missing `164-13-SUMMARY.md`, owned sibling teardown, invalid timestamp, and future timestamp regression cases; no new production CLI flag or output file.
- `test/scripts/scheduled_control_evidence_test.exs` — new invalid and future `updated_at` sweep fixtures; no evidence schema field changes.
- Existing functions `require_terminal_state`, `require_pre_verification_state`, and `raw_sources_are_acceptable` are tightened; no new externally callable symbol is required.
- `164-VALIDATION.md` — Plan 164-15 rows, exact commands, verification assets, observable failure directions, T-164-53 through T-164-57 mappings, and revised terminal SHA/history lifecycle.
- No new file, API, database schema, UI, dependency, workflow, release authority, scheduled-control registry value, clock-skew rule, or external service is created.

<threat_model>
## ASVS Level

OWASP ASVS L1. All high-severity threats below are blocking: execution cannot claim completion until their named regressions pass against production behavior.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Passing verifier metadata → terminal protected-main authority | A stale status must not authorize source that the verifier never evaluated. |
| Test-owned temporary path → developer filesystem | Recursive cleanup must remain confined to an exclusively owned identity. |
| Remote scheduled timestamp → current evidence verdict | Malformed or future time must not acquire freshness authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-53 | Tampering | terminal verifier-to-HEAD binding | high | mitigate | Require explicit verified implementation SHA, ancestor proof, exact per-first-parent-commit metadata path checks, and direct plus change-then-revert process regressions. |
| T-164-54 | Repudiation | pre-verification repair prerequisites | high | mitigate | Require summaries 01-13 and prove missing Plan 13 stops before evidence collection. |
| T-164-55 | Denial of Service | hostile fixture teardown | high | mitigate | Use exclusive allocation plus token, lstat, resolved-parent, and basename ownership checks before recursive cleanup. |
| T-164-56 | Spoofing | scheduled `updated_at` freshness | high | mitigate | Reject malformed and negative age independently in producer sweep and terminal raw validation with boundary fixtures. |
| T-164-57 | Repudiation | integrated TRTH-01/TRTH-02 proof | medium | mitigate | Rerun maintainer/package contracts and authoritative ledger validator with the complete focused suite. |
</threat_model>

<verification>
Run the two focused script contracts and shell syntax after each task. At plan completion run `mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs test/mailglass/publish/maintaining_release_gate_contract_test.exs test/mailglass/docs_contract_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check`, then `elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv`, `mix format --check-formatted test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs`, `bash -n scripts/finalize_phase_164.sh scripts/scheduled_control_evidence.sh .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh`, and `git diff --check`. Any nonzero exit, missing explicit success signal, or high-severity threat regression blocks completion.
</verification>

<success_criteria>
- All four current `164-VERIFICATION.md` gaps have named non-vacuous regressions against production seams.
- Terminal authority is bound to the verified implementation commit and admits only four exact completion-metadata paths in every subsequent first-parent commit, including merge results relative to their first parent and otherwise-hidden change-then-revert history.
- Pre-verification requires summaries through Plan 13 before observation begins.
- Test teardown cannot recursively delete an unowned path.
- Both scheduled-evidence layers reject malformed, future, and stale timestamps while retaining current boundary behavior.
- TRTH-01, TRTH-02, and TRTH-03 integrated proof remains green without product, API, schema, UI, dependency, CI-topology, workflow, provider, or release-authority changes.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and flagged-unverified: false quiet-state claims from stale verifier identity or impossible time are Phase 164 evidence-integrity constraints.
- Kept descriptor-less and flagged-unverified: recursive deletion without exclusive fixture ownership is a phase-specific developer-data safety constraint.
- Dropped with breadcrumb: generic shell injection, path traversal, secret handling, and access control remain canonical security concerns represented by the ASVS L1 threat model rather than bespoke prohibitions.
- Dropped as routine engineering: empty inputs, exact equality, adjacency, and ordering are covered by the explicit behavior and acceptance contracts above.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Documentation, tracked artifacts, ignore rules, and final evidence describe actual supported and operational state | 164-15 Tasks 1-3 | COVERED |
| REQ | TRTH-01 | Maintainer/package truth remains aligned during integrated closeout | 164-15 plan-level verification | COVERED |
| REQ | TRTH-02 | Complete ledger and durable proof remain tracked/discoverable | 164-15 plan-level verification | COVERED |
| REQ | TRTH-03 | Reproducible canonical closeout with green CI, explained schedules, and exact dispositions | 164-15 Tasks 1-3 | COVERED |
| CONTEXT | D-01-D-04 | Layered authority, historical boundary, manifest-derived package claims, one current maintainer path | Existing contracts rerun by 164-15 | COVERED |
| CONTEXT | D-05-D-08, D-12 | Exact-one ledger, durable proof, narrow ignores, locked stale-root disposition | Existing validator rerun by 164-15 | COVERED |
| CONTEXT | D-09-D-11 | Dual proof, exact clean canonical main, exact protected/scheduled evidence | 164-15 Tasks 1-3 | COVERED |
| RESEARCH | Authority hierarchy and non-self-referential closeout | Hash-bound verifier field plus ignored terminal report | 164-15 Task 1 | COVERED |
| RESEARCH | Fail-closed exact provenance and registry freshness | Two independent closed-interval predicates | 164-15 Task 3 | COVERED |
| VERIFICATION | Gap 1 | Bind passing verifier to implementation and reject later source | 164-15 Task 1 | COVERED |
| VERIFICATION | Gap 2 | Require summaries through Plan 13 | 164-15 Task 2 | COVERED |
| VERIFICATION | Gap 3 | Prove exclusive fixture cleanup ownership | 164-15 Task 2 | COVERED |
| VERIFICATION | Gap 4 | Reject invalid/future timestamps in both layers | 164-15 Task 3 | COVERED |
| VALIDATION | Plan 164-15 task/threat lifecycle coverage | Add 164-15-01/02/03 commands, assets, observable failure directions, T-164-53 through T-164-57, and per-first-parent terminal SHA lifecycle | 164-15 Tasks 1-3 | COVERED |
| EDGE | TRTH-01 unclassified | Rerun settled contracts; no invented behavior | 164-15 verification | COVERED-FLAGGED |
| EDGE | TRTH-02 adjacency/empty/ordering | Preserve and rerun exact, non-vacuous, set-based ledger contract | 164-15 verification | COVERED-ASSUMPTION |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact SHA/path membership, missing-input failure, order-independent allowlist | 164-15 Tasks 1-3 | COVERED-ASSUMPTION |
| PROHIBITION | False quiet verdict / unowned recursive cleanup | Descriptor-less flagged safety constraints | 164-15 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-15-SUMMARY.md` when done.
</output>
