# Phase 164: Repository Truth Reconciliation and Closeout - Pattern Map

**Mapped:** 2026-08-26
**Files analyzed:** 11 planned files/paths
**Analogs found:** 10 / 11

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `MAINTAINING.md` | documentation | transform | `MAINTAINING.md` current release-flow/runbook sections | exact |
| `README.md` | documentation | transform | `test/mailglass/docs_contract_test.exs` version-derived README assertions | role-match |
| `mailglass_admin/README.md` | documentation | transform | `README.md` sibling package install/version contract | role-match |
| `mailglass_inbound/README.md` | documentation | transform | `README.md` + `test/mailglass/docs_contract_test.exs` independent-line contract | role-match |
| `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv` | config / durable evidence ledger | transform | `161-PRESERVATION-RECONCILIATION.tsv` | exact |
| `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md` | documentation / durable evidence | transform | `162-RELEASE-RECONCILIATION.md` | exact |
| `scripts/closeout_repository_truth.sh` (name at planner discretion) | utility | request-response / file-I/O | `scripts/verify_workspace_evidence.sh` and `scripts/scheduled_control_evidence.sh` | role-match |
| `test/scripts/phase_164_repository_truth_test.exs` | test | transform | `test/scripts/phase_162_release_reconciliation_test.exs` | exact |
| `test/scripts/phase_164_closeout_test.exs` | test | request-response / file-I/O | `test/scripts/workspace_evidence_contract_test.exs` | exact |
| `test/mailglass/docs_contract_test.exs` | test | transform | existing module assertions for `MAINTAINING.md` and manifest-derived README pins | exact |
| `scheduled-control-sweep.json` | generated artifact (remove) | file-I/O | scheduled-control sweep report generated by `scripts/scheduled_control_evidence.sh` | exact |

`.gitignore` and the five package/host ignore files are audit inputs, not presumed edits: create one disposition-ledger row for every rule in all six files. Reuse the existing narrowly ignored `/tmp/` producer directory for a volatile report where possible; do not add a broad proof-hiding ignore rule.

## Pattern Assignments

### `MAINTAINING.md` (documentation, transform)

**Analog:** its current operational entry and historical runbook, [`MAINTAINING.md`](../../../MAINTAINING.md).

**Current-command / authority projection** (lines 5-25):

```markdown
Before release work starts, run:

    mix mailglass.repo.hygiene --check

The release branch must start from a clean worktree with no local ahead/behind
drift from `origin/main`.
```

Put one new **current protected release and recovery path** before historical material. It must use the actual, final commands: hygiene JSON, exact protected candidate control, exact run inspection, current manifest/Hex facts, and scheduled-control proof. Do not retain the current auto-merge statement as a parallel authority.

**Historical applicability boundary** (lines 452-470):

```markdown
Use the Phase 38 release-day proof forms while running these steps:
- `.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-CHECKLIST.md`
...
The archived Phase 38 forms remain the linked core/admin v1.0 record; the Phase 73 forms cover
the inbound-only slice.
```

Preserve these links as provenance but explicitly label the whole legacy runbook and its `~> 1.3` / `~> 1.0` smoke snippet (lines 539-570) historical, so it cannot be read as current installation or recovery guidance.

### Package README files (documentation, transform)

**Analogs:** manifest-derived current-surface assertions in [`test/mailglass/docs_contract_test.exs`](../../../test/mailglass/docs_contract_test.exs), lines 6-47; current install snippets in root [`README.md`](../../../README.md).

**Dynamic compatibility truth test** (lines 14-34):

```elixir
version = Mix.Project.config()[:version]
[major, minor | _] = String.split(version, ".")
expected_major_minor = "#{major}.#{minor}"

for dep <- ["mailglass", "mailglass_admin"] do
  [_, major_minor] =
    Regex.run(~r/\{:#{dep},\s*"~>\s*(\d+\.\d+)/, readme) ||
      flunk("README is missing a `{:#{dep}, \"~> X.Y\"}` dep pin")

  assert major_minor == expected_major_minor
end
```

Update only current adopter claims and keep them derived/contract-checked against `mix.exs`, `mailglass_admin/mix.exs`, and `mailglass_inbound/mix.exs`. Preserve historically bounded upgrade references rather than globally replacing old version strings.

### `164-TRUTH-DISPOSITION.tsv` (durable evidence ledger, transform)

**Analog:** [`161-PRESERVATION-RECONCILIATION.tsv`](../../161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv), line 1.

**Header plus one-row-per-subject format**:

```tsv
source_row\tdisposition\tevidence_ref\tpreservation_requirement\tmechanism\texpected_oid\ttarget\tobserved_oid\thandoff_location\tblocking_condition\tpermitted_next_action\tstatus
REF-0001\tarchive\tEVID-REF-GRAPH\trequired\tref\t<oid>\trefs/heads/preserve/...\t<oid>\t-\t-\tretain archived evidence\tverified
```

Adopt TSV because it is already tracked, diffable, and shell-parseable. Expand the header with Phase 164’s required columns: stable ID, subject/path-or-pattern, kind, producer, tracked/ignored state, authority, reproducibility, currentness, durable consumer, evidence, disposition, and rationale. Each audited path and each individual ignore pattern gets exactly one row. Include root `scheduled-control-sweep.json` as `remove`, with locked SHA-256 `331810b4b1724452f0e2707c800230e52fabea01c3773d362b3a1240040ece7e` and its no-producer/no-consumer reason before deleting the untracked file.

### `164-CLOSEOUT.md` (durable documentation/evidence, transform)

**Analog:** [`162-RELEASE-RECONCILIATION.md`](../../162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md), as parsed by [`phase_162_release_reconciliation_test.exs`](../../../test/scripts/phase_162_release_reconciliation_test.exs), lines 1-173.

**Markdown matrix parser contract** (lines 154-173):

```elixir
defp matrix_rows(ledger, title) do
  [_, table] = String.split(ledger, "### #{title}\n\n", parts: 2)

  [header, _separator | rows] =
    table
    |> String.split("\n")
    |> Enum.take_while(&String.starts_with?(&1, "|"))
    |> Enum.map(&parse_row/1)

  Enum.map(rows, &Map.new(Enum.zip(header, &1)))
end
```

Use a stable usage/evidence-contract document, not a committed final state snapshot. It should name the post-merge command, generated machine-local report location, pass conditions, policy-blocked allowance, and the fact that identities are queried at runtime. Link to the TSV rather than duplicating every mutable observation.

### Closeout wrapper (utility, request-response / file-I/O)

**Analogs:** [`scripts/verify_workspace_evidence.sh`](../../../scripts/verify_workspace_evidence.sh), lines 1-40 and 59-117; [`scripts/scheduled_control_evidence.sh`](../../../scripts/scheduled_control_evidence.sh), lines 1-17 and 300-375.

**Fail-closed shell CLI and parsing shape**:

```bash
#!/usr/bin/env bash
set -euo pipefail

usage() {
  fail "usage: $0 static INVENTORY.md RECONCILIATION.tsv | live REPO_ROOT INVENTORY.md RECONCILIATION.tsv"
}

case "$mode" in
  static) ... ;;
  live) ... ;;
  *) usage ;;
esac
```

**Exact-main scheduled verdict shape** (scheduled script lines 345-373):

```bash
jq -s --arg expected_main_sha "$expected_main_sha" '
  (. | all(.evidence_valid == true)) as $valid |
  {kind: "sweep", status: (if $valid then "pass" else "pending" end),
   expected_main_sha: $expected_main_sha, controls: .}' \
  "$temporary"/*.json >"$output"

if ! jq -e '.status == "pass"' "$output" >/dev/null; then
  jq -r '.reason' "$output" >&2
  return 1
fi
```

The wrapper only composes existing authorities: `mix mailglass.repo.hygiene --check --format json`, Phase 161 static verification, scheduled sweep, `git status --porcelain=v1 --untracked-files=all`, and an exact-SHA CI query. Write JSON/text under a narrowly ignored machine-local `tmp/` producer directory and report the queried SHA/timestamp. Preserve nonzero exits and distinguish `pass`, evidence-backed `blocked`, `pending`, and `cannot-check`; never coerce failure to pass.

### Phase 164 ExUnit tests (test, transform and request-response/file-I/O)

**Ledger-test analog:** [`test/scripts/phase_162_release_reconciliation_test.exs`](../../../test/scripts/phase_162_release_reconciliation_test.exs), lines 4-15 and 55-71.

```elixir
@ledger Path.expand("../../.planning/phases/.../LEDGER.md", __DIR__)

rows = matrix_rows(ledger, "Final disposition matrix")
assert rows != []
assert Enum.uniq_by(rows, &{&1["Category"], &1["Immutable identity"]}) == rows
```

For a TSV ledger, parse the header once, assert every mandatory field is nonblank, subjects are unique, `disposition` is one of `retain|update|archive|remove|ignore`, every six-file ignore-rule subject is covered, and the stale root sweep row has the locked digest plus `remove`.

**Fixture-backed command-test analog:** [`test/scripts/workspace_evidence_contract_test.exs`](../../../test/scripts/workspace_evidence_contract_test.exs), lines 13-26, 104-133, and 260-280.

```elixir
assert {output, 0} = run(["live", fixture.repo, fixture.inventory, fixture.tsv])
assert output =~ "workspace evidence live contract: PASS"

assert {output, status} = run(["live", fixture.repo, fixture.inventory, fixture.tsv])
assert status != 0
assert output =~ "monitored assessment inputs changed"
```

Build disposable Git/JSON fixtures. Cover dirty state, non-`main`, `HEAD != origin/main`, wrong/exact CI SHA, pending or cannot-check scheduled evidence, duplicate/missing ledger evidence, and the allowed evidence-valid policy-blocked control result.

### `test/mailglass/docs_contract_test.exs` (test, transform)

**Analog:** its existing `MAINTAINING.md` contract at lines 320-365 and README manifest-derived assertions at lines 6-47.

Add narrow assertions for the new current path and historical label. Avoid exact claims that must be live-queried after a merge; test stable terms, canonical command links, and forbidden stale-current framing instead.

### `scheduled-control-sweep.json` (generated artifact, remove)

**Producer analog:** `sweep_controls` in [`scripts/scheduled_control_evidence.sh`](../../../scripts/scheduled_control_evidence.sh), lines 300-375.

```bash
status: (if $valid then "pass" else "pending" end),
reason: (if $valid then "all_controls_current" else "awaiting_current_main_schedule" end),
expected_main_sha: $expected_main_sha,
controls: .
```

This proves the root file is generated output rather than source. Delete only this explicitly identified root file after recording its SHA and disposition in the tracked ledger. Do not replace it with a repository-wide scheduled-control ignore rule.

## Shared Patterns

### Three-state status and failure precedence

**Sources:** [`dev/mix/tasks/mailglass.repo.hygiene.ex`](../../../dev/mix/tasks/mailglass.repo.hygiene.ex), lines 420-476; [`scripts/scheduled_control_evidence.sh`](../../../scripts/scheduled_control_evidence.sh), lines 345-373.

```elixir
cond do
  Enum.any?(checks, &(&1.status == :cannot_check)) -> :cannot_check
  Enum.any?(checks, &(&1.status == :blocked)) -> :blocked
  true -> :pass
end
```

Apply to the closeout report: `cannot-check` and `pending` always prevent quiet closeout. A `blocked` scheduled/recovery row is acceptable only when it is already a defined policy outcome with complete event/run/workflow-SHA/artifact evidence.

### Exact identity, never “latest green”

**Source:** [`scripts/ci_monitor.cjs`](../../../scripts/ci_monitor.cjs), lines 5-45.

```javascript
case "inspect":
  return ["run", "view", exactRunId(args), "--json", RUN_VIEW_FIELDS];
```

Bind all CI observations to the final `main` SHA and an exact run ID. Do not choose the newest branch run by name only.

### Durable evidence versus volatile report

**Sources:** [`MAINTAINING.md`](../../../MAINTAINING.md), lines 65-74; [`scripts/verify_workspace_evidence.sh`](../../../scripts/verify_workspace_evidence.sh), lines 1-40.

```markdown
The files under `.planning/publish/*-publish-summary.json` are tracked release
proof snapshots, not scratch output.
```

Keep the ledger, historical evidence, release/publish summaries, and package allowlists tracked. Generate final exact-main closeout output after merge in `tmp/`, never as a committed self-invalidating snapshot.

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| Exact final closeout report path/name | generated report | file-I/O | The repository has all component verifiers but no existing single Phase-164 aggregate report. Use a focused wrapper that composes them; do not add a service or workflow. |

## Metadata

**Analog search scope:** `MAINTAINING.md`, package READMEs/manifests, `scripts/`, `dev/mix/tasks/`, `test/scripts/`, `test/mailglass/`, Phase 161 and 162 artifacts, and root ignore rules.
**Files scanned:** 18
**Pattern extraction date:** 2026-08-26
