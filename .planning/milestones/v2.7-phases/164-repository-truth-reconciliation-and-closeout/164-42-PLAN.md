---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 42
type: execute
wave: 37
depends_on: [164-39]
files_modified:
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each repository-only loader attack authenticates a fully disposable canonical repository and asserts its mutation-specific rejection."
    - "Repository-only tests cannot dispatch the live checkout's pre-verification or terminal finalizer."
    - "The repository CI alias passes from both exact-main and one-commit-ahead-main disposable states; controlled-host proof remains separate."
  artifacts:
    - path: test/scripts/phase_164_closeout_test.exs
      provides: "Side-effect-free disposable-loader regressions and branch-state alias proofs"
  key_links:
    - from: test/scripts/phase_164_closeout_test.exs
      to: scripts/mailglass_finalize_phase_loader.mjs
      via: "immutable_loader_fixture!/1 canonical-path rewrite into a disposable repository"
    - from: test/scripts/phase_164_closeout_test.exs
      to: mix.exs
      via: "repository-only and controlled-host aliases remain independently runnable"
  flagged_assumptions:
    - "TRTH-03 adjacency: repository-only attack cases remain grouped under the repository alias but do not absorb controlled-host tests."
    - "TRTH-03 empty: each disposable repository contains the exact required plan range and no missing-history shortcut."
    - "TRTH-03 ordering: loader authentication must complete before any disposable Bash dispatch, in exact-main and ahead-main states."
  prohibitions:
    - "Repository tests or installed readiness MUST NOT be presented as terminal proof."
    - "Terminal capture MUST NOT be followed by a tracked write."
---

<objective>
Repair the five repository-only loader attack regressions so they prove their intended security boundary without consulting or dispatching the live checkout per TRTH-03.

Purpose: Repository verification must be deterministic, mutation-specific, and side-effect free.
Output: Disposable authenticated loader fixtures, exact rejection assertions, and exact/ahead-main repository-alias proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-39-SUMMARY.md
@test/scripts/phase_164_closeout_test.exs
@scripts/mailglass_finalize_phase_loader.mjs
@mix.exs
</context>

<assumption_delta>
Decision: no-change. The primary noun is the physical canonical repository plus its authenticated HEAD. Alternate roots are invalid inputs, not supported product variants; disposable fixtures reproduce that same identity contract without changing it.
</assumption_delta>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry foreign-origin and moving-HEAD attacks through a disposable loader</name>
  <files>test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - test/scripts/phase_164_closeout_test.exs — repository-only attack group, `immutable_loader_fixture!/1`, `production_installed_fixture!/0`, and invocation helpers
    - scripts/mailglass_finalize_phase_loader.mjs — canonical identity, origin, HEAD-race, and Bash-dispatch contracts
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md — CR-04 failures and live-checkout dispatch risk
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md — D-06 durable evidence, D-09 dual proof, and D-10/D-11 canonical identity boundaries
  </read_first>
  <behavior>
    - "A disposable canonical repository with a foreign origin fails specifically with `canonical repository origin is not szTheory/mailglass`."
    - "A disposable canonical repository whose HEAD moves before Bash dispatch fails specifically with `authority commit changed before Bash dispatch`."
    - "Neither case mentions the live checkout's origin/main relation or invokes its pre-verification script."
  </behavior>
  <action>Replace the two production-backed attack cases with `immutable_loader_fixture!/1` fixtures whose loader canonical path is rewritten to their disposable repository and whose finalizer/transitive scripts are fixture-owned stubs. Apply each mutation only after the disposable repository satisfies the loader's normal identity/history contract. For `test/scripts/phase_164_closeout_test.exs`, inherit the mapped `test/scripts/workspace_evidence_contract_test.exs` fixture contract: allocate a unique temporary Git repository and fixture-owned files per case, invoke the command with an explicit repository working directory and merged stderr, mutate only copied fixture bytes, and assert the returned `{output, status}` plus the exact rejection text. Assert the mutation-specific diagnostic and exit status, plus absence of the live checkout path and dispatch marker. Do not modify or reinstall the active Plan 164-38 loader bytes.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_installed_boundary --warnings-as-errors --no-deps-check</automated>
    <fails_when>The command exits nonzero, either attack fails on an unrelated origin/main predicate, or a live-checkout/pre-verification dispatch marker appears.</fails_when>
  </verify>
  <done>Two representative attacks authenticate disposable state and reject precisely at their intended boundary without production side effects.</done>
  <acceptance_criteria>
    - Foreign-origin assertion matches its exact loader diagnostic.
    - Moving-HEAD assertion matches its exact pre-dispatch race diagnostic.
    - Both fixtures use disposable repository, history, scripts, and installed-loader paths.
    - Test output contains neither `/Users/jon/projects/mailglass` as an invoked target nor a live pre-verification marker.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert every repository-only attack and prove both main-relation states</name>
  <files>test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - test/scripts/phase_164_closeout_test.exs — all five `:phase_164_installed_boundary` cases and disposable Git helpers
    - scripts/mailglass_finalize_phase_loader.mjs — exact range, retired-extension, argv/env, and dispatch contracts
    - mix.exs — `verify.ci_lane_contract` and `verify.phase_164.installed_boundary` alias separation
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md — disposable fixture and exact diagnostic recommendations
  </read_first>
  <behavior>
    - "Missing historical plan/summary pairs fail with the exact history diagnostic for phases 10, 20, and 34."
    - "A hostile retired extension cannot influence disposable pre-verification; invalid argv fails with exact usage; environment overrides cannot replace canonical identity."
    - "The repository alias passes when disposable HEAD equals origin/main and when it is one commit ahead; controlled-host tests run only under their separate alias."
  </behavior>
  <action>Convert the remaining history, retired-extension, and argv/environment attack paths to fully disposable authenticated fixtures. Replace broad rejection allowlists with exact mutation-specific assertions. Remove `production_installed_fixture!/0`, `invoke_production_loader`, and broad assertion helpers once unused. Add a fixture option that sets `refs/remotes/origin/main` to current HEAD or its predecessor and prove ahead counts 0 and 1 respectively before exercising the repository contract. Continue the mapped `test/scripts/workspace_evidence_contract_test.exs` helper separation: create each repository through one fixture constructor, perform Git setup through checked helpers, allocate collision-safe temporary paths, and make every file-I/O mutation local to that fixture before command execution. Preserve the existing repository/controlled-host tag exclusions, retained loader proof, and separate alias invocations per D-06, D-09, D-10, and D-11.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract</automated>
    <fails_when>The repository alias exits nonzero, any loader attack accepts its mutation, an assertion permits an unrelated diagnostic, or a repository-only case invokes production state.</fails_when>
  </verify>
  <done>All five attack clusters are mutation-specific and disposable, and the repository alias is green from exact-main and ahead-main fixture states without absorbing controlled-host proof.</done>
  <acceptance_criteria>
    - Every `:phase_164_installed_boundary` loader invocation resolves its canonical path to a temporary repository.
    - Deleted plan/summary cases assert exact missing-history diagnostics; retired-extension, argv, environment, foreign-origin, and moving-HEAD cases assert their own behavior.
    - Exact-main and one-commit-ahead-main fixture proofs assert ahead count and repository-alias success.
    - `mix verify.phase_164.installed_boundary` remains a distinct command and its tagged cases are excluded from `mix verify.ci_lane_contract`.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- Disposable installed-loader fixture option for exact-main and one-commit-ahead-main remote state.
- Mutation-specific foreign-origin, moving-HEAD, missing-history, retired-extension, argv, and environment regression assertions.
- Side-effect assertion proving repository-only tests do not invoke live checkout pre-verification.
</artifacts_produced>

<threat_model>
## ASVS Level

OWASP ASVS L1; high-severity fixture escape or unauthorized dispatch risks block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture → installed loader | Fixture paths and Git identity must not escape to the live checkout |
| Authenticated Node loader → Bash | HEAD and canonical bytes must remain authenticated before dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-161 | Tampering | repository-only attack fixtures | high | mitigate | Use fully disposable canonical repositories and exact mutation-specific assertions. |
| T-164-162 | Elevation of Privilege | live pre-verification dispatch | high | mitigate | Rewrite canonical paths to fixture-owned scripts and assert no production marker/path. |
| T-164-163 | Spoofing | origin/main fixture state | medium | mitigate | Set and assert exact remote ref state before each loader invocation. |
| T-164-SC | Tampering | package supply chain | high | accept | No npm, pip, or cargo package install occurs in this plan. |
</threat_model>

<verification>
- Run `mix verify.ci_lane_contract`.
- Failure condition: nonzero exit, mutation-specific diagnostics are not enforced, or any repository-only fixture resolves to the live checkout.
- Run `ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.installed_boundary` separately.
- Failure condition: nonzero exit or controlled-host proof becomes part of the repository-only alias.
</verification>

<success_criteria>
- All five repository-only attack clusters use authenticated disposable state and exact assertions.
- Exact-main and ahead-main states both prove the repository alias.
- Controlled-host evidence remains separate and production pre-verification is unreachable from repository-only fixtures.
</success_criteria>

## Source Coverage Audit

| Source | Item | Planned implementation | Coverage |
|---|---|---|---|
| GOAL | Operational verification reflects supported state | Disposable repository-alias proof | COVERED |
| REQ | TRTH-03 | Tasks 1–2 | COVERED |
| CONTEXT | D-06/D-09/D-10/D-11 | Durable proof, dual proof, and canonical identity | COVERED |
| RESEARCH | Side-effect-free loader seam | Rewritten disposable canonical path | COVERED |
| VERIFICATION | CR-04 | All five loader attack clusters | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Separate alias, complete history, auth-before-dispatch | COVERED-FLAGGED |
| PROHIBITION | Installed evidence is not terminal; no post-capture write | Descriptor-less lifecycle constraints | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-42-SUMMARY.md` after both tasks pass. Do not update completion metadata or invoke terminal finalization.
</output>
