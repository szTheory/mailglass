---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 13
type: execute
wave: 11
depends_on: [164-12]
files_modified:
  - scripts/validate_repository_truth.exs
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_repository_truth_test.exs
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02, TRTH-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05/D-12/TRTH-02: The production ledger validator rejects empty and header-only input, incomplete audited-subject inventories, duplicate equal subjects whether adjacent or separated, every exact-currentness prefix/suffix forgery, and stale rows paired with retain."
    - "D-05/D-12/TRTH-02: Validity is set-based: reordering distinct complete rows preserves the result, while equal subject identities never acquire order-dependent precedence."
    - "D-09/D-10/TRTH-03: The production closeout entry rejects noncanonical repositories, copied or arbitrary ledgers, lexical-prefix path collisions, and destinations outside its ignored output boundary before evidence collection."
    - "D-09/D-10/TRTH-03: A clean observation sampled before output creation cannot override worktree dirt observed after component or final-report writes."
  artifacts:
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Named gap-closure regression matrix for complete exact-one ledger semantics"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Named gap-closure integration matrix for canonical identity and post-write cleanliness"
  key_links:
    - from: "test/scripts/phase_164_repository_truth_test.exs"
      to: "scripts/validate_repository_truth.exs"
      via: "direct calls and executable CLI checks against the production parser and audited-subject inventory"
      pattern: "RepositoryTruthLedger.*(parse|audit_subjects|validate)"
    - from: "test/scripts/phase_164_closeout_test.exs"
      to: "scripts/closeout_repository_truth.sh"
      via: "process-level hostile fixtures that exercise canonical path gates and final stable-porcelain sampling"
      pattern: "canonical_repo|canonical_ledger|post_write_porcelain_dirty"
  flagged_assumptions:
    - "TRTH-01 unclassified: the specless probe supplies no additional behavior family; the already passing maintainer/package contracts remain the authority and are rerun in Plan 164-14."
  prohibitions:
    - statement: "A quiet closeout verdict must not misrepresent incomplete, forged, stale, alternate-repository, or self-dirtying evidence as the actual canonical repository state."
      status: unverified
      flagged: true
---

<objective>
Lock the verifier's exact ledger and repository-identity attacks into one runnable production regression path.

Purpose: Make the already-landed gap repairs explicit, non-vacuous, and independently rerunnable before another protected evidence capture.
Output: Focused adversarial contracts for the shared ledger validator and closeout command.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-09-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
@scripts/validate_repository_truth.exs
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_repository_truth_test.exs
@test/scripts/phase_164_closeout_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace forged and incomplete ledger bytes through the production validator</name>
  <reversibility rating="reversible">The change adds regression fixtures to an existing contract without changing the ledger schema or repository authority.</reversibility>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .gitignore
    - mailglass_admin/.gitignore
    - mailglass_inbound/.gitignore
    - reference/demo_app/.gitignore
    - reference/host_app/.gitignore
    - test/example/.gitignore
  </read_first>
  <behavior>
    - Test 1: `current-forged`, `current_extra`, `historical-forged`, `stale-looking`, and whitespace/case variants each return an exact invalid-currentness error from `Mailglass.RepositoryTruthLedger.parse/1`.
    - Test 2: exact `stale` combined with retain fails, while the allowed stale outcomes remain accepted only when the rest of the canonical relationship is valid.
    - Test 3: empty bytes, the exact header alone, a one-row ledger, and removal of one subject from each audited class fail without a vacuous-success path.
    - Test 4: adjacent duplicates and separated duplicates fail identically; reversing or deterministically shuffling distinct valid rows preserves success and error identity.
  </behavior>
  <action>Add a named `:phase_164_gap_closure` regression group to the existing ExUnit contract and drive every case through `Mailglass.RepositoryTruthLedger`, not a test-local parser. Use the authoritative ledger as the positive control, mutate one semantic dimension per negative fixture, and assert the precise production error category. Cover required subjects derived from all six tracked ignore files, tracked publish/proof paths, and Phase 164 artifacts. Retain exact subject equality and set-based order behavior per D-05/D-12; do not loosen canonical relationship hashes, required fields, the disposition enum, or D-08's locked removal identity. If a new fixture reveals a production defect, repair only `scripts/validate_repository_truth.exs` and add that tracked path to the task commit; otherwise leave production code unchanged.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_gap_closure --warnings-as-errors --no-deps-check</automated>
    <fails_when>The command exits non-zero, selects no tagged tests, or any forged, incomplete, duplicate, stale-retain, or order-invariance assertion fails.</fails_when>
  </verify>
  <acceptance_criteria>
    - The tagged group runs non-vacuously and exercises the production module from `scripts/validate_repository_truth.exs`.
    - Every malformed-currentness spelling is compared by exact enum membership rather than prefix matching.
    - Empty/header-only/one-row/missing-subject fixtures and both duplicate placements return nonzero/error results.
    - The unmodified authoritative ledger and its reordered distinct rows validate successfully.
    - The D-08 row retains its locked SHA-256 and remove disposition.
  </acceptance_criteria>
  <done>The complete semantic validator has an explicit runnable regression path for every verifier ledger attack and TRTH-02 collection edge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise canonical identity and post-write cleanliness through the closeout process</name>
  <reversibility rating="reversible">The change extends fixture coverage around an existing read-only command and can be reverted without changing operational state.</reversibility>
  <files>scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - scripts/closeout_repository_truth.sh
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_closeout_test.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .gitignore
  </read_first>
  <behavior>
    - Test 1: a disposable clean main checkout, a sibling lexical-prefix collision, and a foreign symlink target are rejected before any component command runs.
    - Test 2: copied-complete, one-row, missing-subject, malformed-currentness, and stale-retain ledgers cannot replace the resolved authoritative ledger.
    - Test 3: a tracked-root destination, a sibling-prefix destination, a non-ignored destination, and a pre-existing component symlink are rejected without changing an external sentinel.
    - Test 4: fixture-injected dirt after a component write and after the first final-report write forces the Git component/final verdict non-pass and remains observable after the command exits.
  </behavior>
  <action>Add the corresponding `:phase_164_gap_closure` process-level cases to the existing closeout test. Reuse its isolated command stubs and canonical-repository substitution helper while keeping the production command's literal `/Users/jon/projects/mailglass` authority unchanged. Assert both nonzero status and absence of the marker that represents downstream evidence collection for preflight rejections. For post-write cases, inject a visible untracked sentinel only after the initial porcelain sample and assert the persisted report records the dirty-after-write reason rather than pass. Exercise `scripts/validate_repository_truth.exs` for semantic ledger mutations; do not introduce another ledger parser. If a fixture exposes a production defect, repair only `scripts/closeout_repository_truth.sh` and preserve its read-only evidence composition and fail-closed precedence.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs --only phase_164_gap_closure --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/closeout_repository_truth.sh scripts/finalize_phase_164.sh</automated>
    <fails_when>The command exits non-zero, selects no tagged tests, Bash syntax fails, any hostile path reaches evidence collection, an external sentinel changes, or either injected post-write dirt case reports pass.</fails_when>
  </verify>
  <acceptance_criteria>
    - The tagged cases invoke the closeout process and production validator rather than matching source prose alone.
    - Repository and ledger identity use resolved exact equality; lexical adjacency never counts as containment or authority.
    - Only the existing ignored canonical output boundary is eligible, and pre-existing component symlinks cannot redirect writes.
    - Stable porcelain is sampled after component and report writes; late dirt has non-pass precedence.
    - The full focused Phase 164 suite remains green with `.tool-versions` left untouched.
  </acceptance_criteria>
  <done>The closeout entry has non-vacuous end-to-end regression proof for the verifier's canonical-repository and output-cleanliness gaps.</done>
</task>

</tasks>

## Artifacts this phase produces

- `test/scripts/phase_164_repository_truth_test.exs` — new `:phase_164_gap_closure` cases; no new production symbol, field, script option, or file path.
- `test/scripts/phase_164_closeout_test.exs` — new `:phase_164_gap_closure` process fixtures; no new production symbol, field, script option, or file path.
- No API, database schema, UI, dependency, workflow, release authority, or external service is created or modified.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TSV fixture bytes → production ledger verdict | Forged enum prefixes, incomplete sets, and duplicate identities must remain distinguishable from authoritative evidence. |
| Caller paths/output writes → canonical closeout verdict | Alternate identities and late worktree dirt must not acquire pass authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-45 | Tampering | ledger currentness and subject set | high | mitigate | Production-module mutation matrix covers exact enums, complete inventory, duplicate placement, and order invariance. |
| T-164-46 | Spoofing | repository and ledger path identity | high | mitigate | Process fixtures require resolved exact canonical identities and prove preflight rejection before collection. |
| T-164-47 | Tampering | report/component destinations | high | mitigate | Reject non-ignored/symlinked destinations and exercise final stable-porcelain precedence after writes. |
| T-164-48 | Repudiation | regression evidence | medium | mitigate | Named tagged tests emit precise error assertions through the production seams. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run both tagged regression groups, then the existing complete focused Phase 164 suite, Bash syntax, Elixir formatting, the production authoritative-ledger CLI, and `git diff --check`. Every runnable command fails on nonzero exit or a missing selected-test/success signal.
</verification>

<success_criteria>
- Every code-level failure reported by `164-VERIFICATION.md` is represented by an executable hostile fixture against production behavior.
- Exact equality, empty input, duplicate adjacency, and stable ordering semantics are explicit and non-vacuous.
- No unrelated product, workflow, authority, dependency, UI, schema, or `.tool-versions` change occurs.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and flagged-unverified: falsely representing forged, stale, alternate-repository, or self-dirtying evidence as canonical quiet state is a repository-specific transparency/safety constraint.
- Dropped with breadcrumb: shell injection, path traversal, and generic secret/PII exposure are canonical security concerns owned by the Phase 164 threat model and `$gsd-secure-phase`; they are not minted as bespoke prohibitions.
- Dropped as routine engineering: parser exceptions, empty input, duplicate rows, equality, and ordering are covered by explicit behaviors and acceptance criteria.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts and ignore rules describe actual supported state | 164-13 | COVERED |
| REQ | TRTH-02 | Complete exact-one semantic ledger and discoverable proof | 164-13 Task 1 | COVERED |
| REQ | TRTH-03 | Canonical clean repository and fail-closed report generation | 164-13 Task 2 | COVERED |
| CONTEXT | D-05, D-12 | Twelve-field exact-one ledger semantics | 164-13 Task 1 | COVERED |
| CONTEXT | D-09, D-10 | Dual proof and exact canonical clean workspace | 164-13 Task 2 | COVERED |
| RESEARCH | One-row ledger and two-phase closeout patterns | Production validator/closeout regression groups | 164-13 | COVERED |
| VERIFICATION | Gap 1 | Arbitrary/incomplete ledger, exact currentness, stale outcome, and missing-subject attacks | 164-13 Task 1 | COVERED |
| VERIFICATION | Gap 2 | Noncanonical repository, output boundary, and post-write dirt attacks | 164-13 Task 2 | COVERED |
| EDGE | TRTH-02 adjacency/empty/ordering | Exact duplicates, non-vacuous input, stable set-based validation | 164-13 Task 1 | COVERED-ASSUMPTION |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact resolved identity, missing-source failure, and late-observation precedence | 164-13 Task 2 | COVERED-ASSUMPTION |
| PROHIBITION | False canonical quiet verdict | Descriptor-less flagged-unverified transparency/safety item | 164-13 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-13-SUMMARY.md` when done.
</output>
