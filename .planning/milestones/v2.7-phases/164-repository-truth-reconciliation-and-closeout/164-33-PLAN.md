---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 33
type: execute
wave: 30
depends_on: [164-32]
files_modified:
  - test/scripts/phase_164_closeout_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TRTH-03: Only the exact approved Plan 164-32 committed bytes replace the exact Plan 164-27 object, after the prior bytes are preserved at the approved private rollback path."
    - "TRTH-03: The resulting installed command is a regular non-symlink mode-0500 file whose digest equals approval source_sha256 and whose installation OID is an ancestor of current HEAD."
    - "TRTH-03: The controlled-host alias proves both immutable Plan 164-27 prior provenance and the active Plan 164-32 tuple through direct installed --version/--self-check; disposable attacks remain repository-only."
    - "D-09/D-10/D-11: Installation readiness runs neither canonical pre-verification nor terminal finalization and changes no requirement/phase completion metadata."
  artifacts:
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Active Plan 164-32 installed-tuple proof plus preserved Plan 164-27 provenance"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Observed non-terminal reinstall readiness record"
  key_links:
    - from: "Plan 164-32 approval"
      to: "installed mailglass-finalize-phase"
      via: "revalidation, rollback-before-replace, private materialization, atomic rename, digest/mode, and self-check"
      pattern: "phase_plan=164-32"
  flagged_assumptions:
    - "TRTH-01 unclassified probe: reinstall affects only operational evidence and leaves already-verified current/historical guidance unchanged."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT install checkout bytes, accept tuple drift, or run either finalization mode during readiness proof."
      status: unverified
      flagged: true
    - statement: "T-164-109: terminal evidence MUST NOT be followed by a tracked write; terminal evidence remains absent."
      status: unverified
      flagged: true
---

<objective>
Recoverably install the approved exact-history loader and prove its active external tuple without entering terminal lifecycle.

Purpose: Restore installed authority after the deliberate 01–34 source change.
Output: Approved mode-0500 installed bytes, verified rollback, active controlled-host proof, and truthful readiness record.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-32-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@test/scripts/phase_164_closeout_test.exs
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
</context>

<assumption_delta_decision>
Primary noun: Phase 164 installed authority tuple. Decision: no-change — the explicitly approved Plan 164-32 tuple becomes the single active authority and retains Plan 164-27 as prior provenance only.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: Atomically replace the exact Plan 164-27 object with approved committed bytes</name>
  <precondition>The regular mode-0400 Plan 164-32 approval exists, equals its proposal plus one approval line, and every bound source/tool/destination/prior-object/rollback field still matches.</precondition>
  <reversibility rating="reversible">The exact Plan 164-27 installed bytes are preserved at the approved private rollback target before replacement and can be atomically restored on post-install failure.</reversibility>
  <files>scripts/mailglass_finalize_phase_loader.mjs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-32-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-32-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>Consume only the persisted Plan 164-32 approval as data. Revalidate approval/proposal parity, clean canonical HEAD, committed source blob/digest, all approved trusted executable identities, the exact current Plan 164-27 destination lstat/digest/mode/stat identity, and rollback absence. Copy prior installed bytes to the exact approved private rollback target, publish it as a regular non-symlink mode-0400 object, and verify its digest before destination mutation. Privately materialize the approved Git blob, verify digest and absolute shebang, set mode 0500, then atomically rename within the destination directory and re-read lstat/mode/digest. Run direct `--version` and `--self-check --repo /Users/jon/projects/mailglass --expected-source-oid <approved OID>`. If any post-install check fails, atomically restore the verified rollback and return non-success. Do not run phase argument `164`, `164 --pre-verification`, dispatch/rerun/release actions, or any terminal evidence path.</action>
  <verify>
    <automated>set -eu; approval=/Users/jon/.local/share/mailglass/checkpoints/164-32-install-approval.env; installed=/Users/jon/.local/bin/mailglass-finalize-phase; oid=$(sed -n 's/^installation_source_oid=//p' "$approval"); digest=$(sed -n 's/^source_sha256=//p' "$approval"); rollback=$(sed -n 's/^rollback_path=//p' "$approval"); prior=$(sed -n 's/^prior_sha256=//p' "$approval"); test -f "$installed"; test ! -L "$installed"; test "$(stat -f '%Lp' "$installed")" = 500; test "$(shasum -a 256 "$installed" | awk '{print $1}')" = "$digest"; test -f "$rollback"; test ! -L "$rollback"; test "$(stat -f '%Lp' "$rollback")" = 400; test "$(shasum -a 256 "$rollback" | awk '{print $1}')" = "$prior"; "$installed" --version; "$installed" --self-check --repo /Users/jon/projects/mailglass --expected-source-oid "$oid"</automated>
    <fails_when>Non-zero exit; tuple drift; rollback absent/wrong-mode/wrong-digest; installed object symlinked/wrong-mode/wrong-digest; version/self-check mismatch; installation OID non-ancestor; or any finalization report is created.</fails_when>
  </verify>
  <acceptance_criteria>
    - Prior approved bytes are recoverable before destination replacement.
    - Installed bytes come only from the approved commit blob and match mode/digest/source OID.
    - Version and ancestry-aware self-check pass without running finalization.
  </acceptance_criteria>
  <done>The approved 01–34 loader is the active installed executable with verified rollback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove active and prior approval chains through the controlled-host alias</name>
  <files>test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-32-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: Plan 164-27 approval remains exact immutable prior provenance and its digest equals the verified rollback bytes.
    - Test 2: active proof reads the exact Plan 164-32 approval schema and actual installed object, then asserts blob digest, ancestry, --version, and --self-check output.
    - Test 3: controlled-host selection is nonzero and contains no disposable source-loader repository fixture.
    - Test 4: lifecycle text records readiness only and retains ordinary-verifier/protected-main/terminal pending order.
  </behavior>
  <action>Extend the controlled-host group to retain a Plan 164-27 prior-provenance/rollback assertion and switch its active installed tuple assertion to the Plan 164-32 approval. Reuse the exact parser/type/mode/digest/ancestry/self-check helpers from Plan 164-29; do not duplicate weaker parsing. Require the approved source blob to contain terminal last=34 and require self-check to report that range. Update `164-FINALIZATION.md` with observed approval/installed/rollback identities and named alias result as installation readiness only. Keep all disposable foreign-repository, moving-HEAD, missing-pair, hostile-extension, and override tests in repository-only selection.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.installed_boundary &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; either alias selects zero tests; active proof does not read the real Plan 164-32 approval/installation; Plan 164-27 provenance/rollback is lost; repository CI touches host state; terminal mode runs; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - Active installed proof binds exact Plan 164-32 approval and installed bytes.
    - Plan 164-27 remains auditable prior provenance linked to exact rollback.
    - Controlled-host and repository-only aliases both pass under distinct authorities.
    - No terminal or completion claim is made.
  </acceptance_criteria>
  <done>The actual current installation and its approval chain are proven fail-closed and non-terminal.</done>
</task>

</tasks>

## Artifacts this phase produces

- External installed command — approved regular mode-0500 01-34 loader bytes.
- External rollback object — exact prior Plan 164-27 loader bytes at approved mode-0400 digest path.
- `test/scripts/phase_164_closeout_test.exs` — active Plan 164-32 installed proof and immutable Plan 164-27 rollback provenance.
- `164-FINALIZATION.md` — observed reinstall readiness, with terminal evidence still pending.
- New product files or flags: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; external installation mutation and false readiness are high severity and block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved record → destination mutation | Every field must remain exact immediately before replacement. |
| Prior destination → rollback storage | Recoverable bytes must be published and verified before replacement. |
| Installed executable → controlled-host evidence | Real bytes and their committed ancestry must be asserted directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-125 | Tampering | installed replacement | high | mitigate | Revalidate exact approval and install only authenticated commit bytes atomically. |
| T-164-126 | Repudiation | rollback provenance | high | mitigate | Publish verified digest-addressed mode-0400 prior bytes before replacement. |
| T-164-127 | Spoofing | active installed proof | high | mitigate | Directly read real approval/install and assert digest, OID ancestry, version, and self-check. |
| T-164-128 | Elevation of Privilege | terminal execution | high | mitigate | Permit only inspection modes during installation readiness. |
| T-164-SC | Tampering | external source movement | high | mitigate | Approved source is one authenticated repository blob; no package manager participates. |
</threat_model>

<verification>
Verify rollback and installed objects, direct version/self-check, explicit controlled-host alias, repository-only lane, and diff hygiene. Do not invoke finalization mode.
</verification>

<success_criteria>
- The approved 01-34 loader is installed recoverably and matches its active approval.
- Controlled-host proof covers the real object; repository-only full suites remain hermetic.
- Terminal lifecycle remains pending.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Actual supported installed state is auditable and reproducible | 164-33 | COVERED |
| REQ | TRTH-03 | Reproducible installed closeout authority | Tasks 1–2 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, canonical state, protected ordering | Tasks 1–2 | COVERED |
| RESEARCH | — | Exact identity, recoverability-first mutation, fail-closed proof | Tasks 1–2 | COVERED |
| VERIFICATION | Gap 1 | Actual installed executable and approval chain | Task 2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact active/prior separation, missing tuple stop, rollback-before-replace | Tasks 1–2 | COVERED |
| EDGE | TRTH-01 unclassified | Explicit no-change assumption | must_haves.flagged_assumptions | COVERED-FLAGGED |
| PROHIBITION | No checkout install or terminal execution | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-33-SUMMARY.md` when done. Then reconcile records in Plan 164-34; do not run terminal finalization.
</output>
