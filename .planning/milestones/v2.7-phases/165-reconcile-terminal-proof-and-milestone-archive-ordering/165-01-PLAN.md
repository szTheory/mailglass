---
phase: 165-reconcile-terminal-proof-and-milestone-archive-ordering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mailglass_finalize_milestone_loader.mjs
  - scripts/finalize_milestone_v2_7.sh
  - test/scripts/phase_165_milestone_finalizer_test.exs
  - mix.exs
  - test/test_helper.exs
autonomous: true
requirements: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A repository-local fixture can traverse the complete v2.7 staging, archive-authentication, evidence-selection, and ignored-report path without altering the historical Phase 164 authority (D-01, D-02, D-03)."
    - "The repository-only verification lane cannot dispatch the terminal command, while the installed-boundary lane is separately named and excluded from ordinary CI (D-04, D-05, D-12)."
    - "Hostile fixtures fail closed for stale audit, incomplete archive layout, dirty or moving HEAD, caller-selected or rerun CI, non-natural schedules, tracked-output leakage, installed-byte mismatch, and repository-lane terminal dispatch (D-12, D-15, D-17)."
  artifacts:
    - scripts/mailglass_finalize_milestone_loader.mjs
    - scripts/finalize_milestone_v2_7.sh
    - test/scripts/phase_165_milestone_finalizer_test.exs
    - mix.exs
    - test/test_helper.exs
  key_links:
    - "mix.exs `verify.phase_165.repository` -> ExUnit fixtures -> loader -> staged finalizer -> ignored report assertion"
    - "test/test_helper.exs exclusion -> `phase_165_installed_production_boundary` -> explicit installed-boundary alias only"
    - "loader authenticated manifest -> shell finalizer exact-SHA checks -> report write followed by HEAD and cleanliness rechecks"
---

<objective>
Create the production-quality tracer and hostile fixture matrix for the separate v2.7 terminal authority.

Purpose: Prove the irreversible end of the lifecycle in repository-local isolation before any installed executable is proposed, while preserving Phase 164 as immutable historical evidence per D-01 and D-02.
Output: A pinned loader, fail-closed v2.7 finalizer, isolated ExUnit contract, and explicit Mix verification lanes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-CONTEXT.md
@.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-RESEARCH.md
@.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-PATTERNS.md
@scripts/mailglass_finalize_phase_loader.mjs
@scripts/finalize_phase_164.sh
@mix.exs
@test/test_helper.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one archived v2.7 fixture through the separate terminal authority</name>
  <files>scripts/mailglass_finalize_milestone_loader.mjs, scripts/finalize_milestone_v2_7.sh, test/scripts/phase_165_milestone_finalizer_test.exs, mix.exs, test/test_helper.exs</files>
  <read_first>
    - .planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-CONTEXT.md
    - .planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-RESEARCH.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/finalize_phase_164.sh
    - mix.exs
    - test/test_helper.exs
  </read_first>
  <behavior>
    - "Happy path: exact argument `v2.7`, normalized repository identity `szTheory/mailglass`, clean immutable HEAD, complete archived phases 161-165, semantically passing audit, exact attempt-1 CI, and natural attempt-1 schedules produce one pass report in ignored storage."
    - "Boundary path: the loader authenticates index and worktree bytes at one full commit OID, constructs a closed staged manifest, pins the physical runtime closure, and rechecks HEAD immediately before dispatch."
    - "Isolation path: `mix verify.phase_165.repository` exercises fixture staging without invoking the installed terminal command; `phase_165_installed_production_boundary` remains excluded from ordinary test selection."
    - "Proposal path: `/Users/jon/.local/bin/mailglass-finalize-milestone` is lstat'd immediately before proposal emission; absent yields create, one safe regular predecessor yields an exact digest/mode/owner backup-and-rollback tuple, and every other predecessor state fails closed (D-02, D-16)."
  </behavior>
  <action>
Start with the failing end-to-end ExUnit fixture, then implement `buildInstallationProposal`, `authenticateClosedManifest`, `selectExactAttemptOneCi`, `selectNaturalSchedules`, and `writeTerminalReport` as named logical boundaries in the new loader/finalizer pair. Bind implementation explicitly to the exact assignments in `165-PATTERNS.md`: `scripts/mailglass_finalize_phase_loader.mjs` for the loader and its authentication, bounded-error, secret-environment, and private-materialization patterns; `scripts/finalize_phase_164.sh` for the shell verifier and exact evidence/late-cleanliness patterns; `test/scripts/phase_164_closeout_test.exs` for fixture and lane non-vacuity; and the Phase 164 in-file aliases/exclusions in `mix.exs` and `test/test_helper.exs`. Accept only the exact milestone token `v2.7` per D-03 and reject alternate repository identities or abbreviated OIDs. Pin physical Node, Git, Bash, gh, jq, Mix, Elixir, and Erlang identities; authenticate both staged source and data bytes from one commit; use a private staging root with 0700 directories, 0500 executables, and 0400 data; pass an allowlisted environment; and make the report write the last filesystem effect before late HEAD and cleanliness rechecks. For `/Users/jon/.local/bin/mailglass-finalize-milestone`, lstat immediately before proposal emission; accept only absence or a safe regular non-symlink predecessor, and encode the exact create-or-backup/replace/rollback tuple without mutating the destination before approval (D-16). Add `verify.phase_165.repository` and `verify.phase_165.installed_boundary` aliases, with the installed test tagged `phase_165_installed_production_boundary` and excluded in test helper. Do not edit either Phase 164 source file or make the historical installed Phase 164 command part of this authority (D-01, D-02). Do not add dependencies or dispatch any remote workflow (D-10, D-17).
  </action>
  <verify>
    <automated>mix test test/scripts/phase_165_milestone_finalizer_test.exs --only phase_165_tracer --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_milestone_v2_7.sh &amp;&amp; mix format --check-formatted test/scripts/phase_165_milestone_finalizer_test.exs</automated>
    <fails_when>The command exits non-zero, the tracer does not emit a pass report for the exact archived fixture, shell syntax fails, formatting differs, or the installed production boundary runs in the tracer selection.</fails_when>
  </verify>
  <acceptance_criteria>
    - "The exact v2.7 happy path runs end to end through a real staged shell finalizer and an ignored report assertion."
    - "The stage is bound to one full OID and authenticated index/worktree bytes with the required modes and runtime closure."
    - "The historical Phase 164 loader, finalizer, and installed command contract are byte-for-byte unchanged (D-01, D-02)."
  </acceptance_criteria>
  <done>The exact archived-fixture happy path passes end to end, staging is authenticated at one full OID, and Phase 164 remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every stale, forged, or wrongly dispatched terminal proof fail closed</name>
  <files>scripts/mailglass_finalize_milestone_loader.mjs, scripts/finalize_milestone_v2_7.sh, test/scripts/phase_165_milestone_finalizer_test.exs, mix.exs, test/test_helper.exs</files>
  <read_first>
    - scripts/mailglass_finalize_milestone_loader.mjs
    - scripts/finalize_milestone_v2_7.sh
    - test/scripts/phase_165_milestone_finalizer_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-SECURITY.md
  </read_first>
  <behavior>
    - "Audit failures: reject stale audit OID, any score other than requirements 16/16, phases 5/5, integration 16/16, flows 5/5, a non-validated phase, or absent disclosure of the 14-PR accepted policy debt (D-10, D-13)."
    - "Archive failures: reject a missing archived ledger, any phase set other than exactly 161-165, legacy quick-task inclusion, live/archive lifecycle disagreement, or a tracked terminal-output target (D-11, D-14)."
    - "Repository failures: reject dirty initial state, HEAD movement at any recheck, installed-byte mismatch, non-ignored report storage, and any attempt to dispatch the terminal command from the repository-only lane (D-05, D-12, D-15)."
    - "Evidence failures: reject caller-supplied run selection, rerun attempts, wrong SHA/branch/conclusion, manual schedule events, and schedules whose attempt or SHA does not match the final archived commit (D-12, D-15, D-17)."
  </behavior>
  <action>
Expand the fixture table using the exact hostile-fixture, archive-manifest, CI-selector, and moving-HEAD analogs assigned in `165-PATTERNS.md` from `test/scripts/phase_164_closeout_test.exs`; retain the shared authentication, bounded-error, evidence-separation, and non-dispatch patterns from the mapped Phase 164 loader/finalizer. Give each locked negative a distinct assertion and diagnostic: stale audit; incomplete archive layout; dirty HEAD; moving HEAD before dispatch or after report write; caller-selected CI; CI rerun; manual or otherwise non-natural schedule; wrong schedule attempt, branch, or SHA; tracked-output leakage; installed-byte mismatch; unsafe predecessor kind/ownership/mode; and terminal dispatch from `verify.phase_165.repository`. Derive CI and schedule evidence only from read-only gh queries, choose the unique successful attempt-1 records for the exact full SHA, and never accept a caller run ID. Require exactly phases 161-165 and exclude all legacy quick tasks per D-11. Treat the 14 open PRs only as disclosed accepted policy debt per D-10. Preserve the one-shot terminal rule and the prohibition on later lifecycle mutation per D-04 and D-05. The tests must exercise fixture doubles only; they must not call the installed command, mutate remotes, dispatch or rerun workflows, push tags, delete branches, release, publish, close PRs, change CI topology, or touch product/API/schema/UI behavior (D-10, D-17).
  </action>
  <verify>
    <automated>mix test test/scripts/phase_165_milestone_finalizer_test.exs --exclude phase_165_installed_production_boundary --warnings-as-errors --no-deps-check &amp;&amp; mix verify.phase_165.repository &amp;&amp; mix verify.ci_lane_contract &amp;&amp; git diff --check HEAD --</automated>
    <fails_when>Any hostile fixture succeeds, an expected diagnostic is absent, the repository lane reaches terminal dispatch, the installed-boundary tag appears in ordinary selection, the CI lane contract fails, or the diff has whitespace errors.</fails_when>
  </verify>
  <acceptance_criteria>
    - "Every negative case required by D-12 and D-15 has an exact fail-closed fixture."
    - "No fixture accepts caller-selected or rerun CI, non-natural schedules, an incomplete archive, or tracked report output."
    - "The repository lane proves readiness without crossing the installed terminal boundary."
  </acceptance_criteria>
  <done>All required hostile fixtures fail closed and the repository-only lane cannot reach terminal dispatch.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 165-01 local inventory)

- **New tracked implementation paths:** `scripts/mailglass_finalize_milestone_loader.mjs`, `scripts/finalize_milestone_v2_7.sh`, and `test/scripts/phase_165_milestone_finalizer_test.exs`.
- **New symbols/contracts in those paths:** `buildInstallationProposal`, `authenticateClosedManifest`, `selectExactAttemptOneCi`, `selectNaturalSchedules`, `writeTerminalReport`, and ExUnit tag `phase_165_installed_production_boundary`.
- **Existing tracked paths modified:** `mix.exs` gains `verify.phase_165.repository`, `verify.phase_165.installed_boundary`, and the required-lane exclusion; `test/test_helper.exs` gains the matching default exclusion.
- **Executor-owned generated output:** `.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-01-SUMMARY.md` after both tasks complete.
- **Archived/workflow outputs:** none in this plan; canonical audit and archive execution occur only through the Plan 05 post-completion runbook.
- **External or ignored runtime outputs:** none in this plan; proposal generation is read-only, installation belongs to Plan 04, and the terminal report belongs to the post-completion runbook.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> authenticated stage | Tracked loader and finalizer bytes cross into a private execution tree. |
| GitHub CLI evidence -> terminal verdict | Untrusted remote metadata is selected for the exact archived commit. |
| repository-only tests -> installed authority | Ordinary fixture execution must never cross the production terminal boundary. |

## STRIDE Threat Register (OWASP ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-01-T01 | Tampering | staged manifest and installed bytes | high | mitigate | Block on full-OID/index/worktree/digest mismatch; use closed manifest and private modes. |
| T-165-01-S01 | Spoofing | CI and scheduled evidence selectors | high | mitigate | Derive unique attempt-1 records for exact SHA/branch/event; reject caller selection and reruns. |
| T-165-01-R01 | Repudiation | terminal report | high | mitigate | Emit semantic component evidence and late HEAD/cleanliness values, then forbid later lifecycle writes. |
| T-165-01-E01 | Elevation of privilege | repository verification lane | high | mitigate | Exclude installed boundary by tag and assert that repository-only execution cannot dispatch it. |
</threat_model>

<verification>
Run the focused ExUnit contract, shell syntax check, both explicit Mix lanes, the existing CI lane contract, formatting, and `git diff --check`. Confirm the Phase 164 source pair is unchanged from the plan's starting commit.
</verification>

<success_criteria>
One production-quality fixture traverses every layer of the separate v2.7 authority, all named hostile cases fail closed, and ordinary verification cannot invoke the installed terminal boundary.
</success_criteria>

<output>
Create `.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-01-SUMMARY.md` when done.
</output>
