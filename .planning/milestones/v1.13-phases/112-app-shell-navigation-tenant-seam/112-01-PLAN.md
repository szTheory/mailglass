---
phase: 112-app-shell-navigation-tenant-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/operator/tenants.ex
  - mailglass_admin/lib/mailglass_admin/operator/tenants.ex
  - mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
  - test/mailglass/operator/tenants_test.exs
  - mailglass_admin/test/mailglass_admin/operator/tenants_test.exs
  - mailglass_admin/test/mailglass_admin/optional_deps/mailglass_inbound_test.exs
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
must_haves:
  truths:
    - "Accessible tenant choices are discovered by a core operator read model per D-01."
    - "Tenant listing is scoped through Mailglass.Tenancy.scope/2 per D-02."
    - "The selector returns distinct, sorted tenant rows from outbound activity plus inbound-only tenant ids when the optional inbound gateway is loaded."
  artifacts:
    - path: "lib/mailglass/operator/tenants.ex"
      provides: "Scoped core outbound tenant projection"
      exports: ["list_tenants/2"]
    - path: "mailglass_admin/lib/mailglass_admin/operator/tenants.ex"
      provides: "Unified shell-accessible tenant selector"
      exports: ["list_tenants/2"]
    - path: "test/mailglass/operator/tenants_test.exs"
      provides: "Core tenant projection regression tests"
    - path: "mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex"
      provides: "Optional inbound tenant projection gateway"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/mount.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/tenants.ex"
      via: "operator_actor passed as read-model context"
      pattern: "Operator\\.Tenants\\.list_tenants"
    - from: "lib/mailglass/operator/tenants.ex"
      to: "lib/mailglass/tenancy.ex"
      via: "Tenancy.scope/2 on the tenant projection query"
      pattern: "Tenancy\\.scope"
---

<objective>
Create the core tenant selector read model that Phase 112 shell behavior depends on.

Purpose: tenant selection is an authorization/data-read question, so the admin shell must consume a scoped core read model instead of querying admin storage or deriving tenant choices from UI/session fixtures.
Output: scoped `Mailglass.Operator.Tenants.list_tenants/2` for outbound tenant ids, `MailglassAdmin.Operator.Tenants.list_tenants/2` as the single shell selector seam, optional inbound tenant projection gateway support, and tests proving distinct tenant ids, ordering, nil/blank exclusion, inbound-only tenant inclusion, and tenancy scoping.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-CONTEXT.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-RESEARCH.md
@lib/mailglass/operator/deliveries.ex
@lib/mailglass/tenancy.ex
@mailglass_admin/lib/mailglass_admin/auth.ex
@mailglass_admin/lib/mailglass_admin/operator/mount.ex
</context>

## Artifacts this phase produces

- `Mailglass.Operator.Tenants` core read model for shell tenant discovery.
- `MailglassAdmin.Operator.Tenants` unified shell seam that combines core outbound tenant ids with optional inbound-only ids.
- Optional inbound gateway function for shell-accessible inbound-only tenant ids when `mailglass_inbound` is loaded.
- Core tests that prove the read model does not duplicate tenant ids or bypass `Mailglass.Tenancy.scope/2`.
- A stable API surface for later admin shell plans to call through `operator_actor`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scoped core tenant projection tests</name>
  <files>test/mailglass/operator/tenants_test.exs, mailglass_admin/test/mailglass_admin/operator/tenants_test.exs, mailglass_admin/test/mailglass_admin/optional_deps/mailglass_inbound_test.exs</files>
  <read_first>
    @test/mailglass/operator/deliveries_test.exs
    @lib/mailglass/operator/deliveries.ex
    @lib/mailglass/tenancy.ex
    @mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
  </read_first>
  <behavior>
    - Test 1: seeded deliveries for tenant-a and tenant-b produce two rows sorted by tenant id.
    - Test 2: multiple deliveries for one tenant produce one selector row.
    - Test 3: nil or blank tenant ids are omitted.
    - Test 4: a custom tenancy resolver can restrict the projection to the actor/context tenant.
    - Test 5: inbound-only tenant ids are included through the optional inbound gateway when `mailglass_inbound` is loaded.
  </behavior>
  <action>Create `test/mailglass/operator/tenants_test.exs` before production code. Use existing delivery fixture patterns from `deliveries_test.exs`. The core tests must call `Mailglass.Operator.Tenants.list_tenants/2` with the authenticated `operator_actor` map shape, assert `%{id: tenant_id, label: tenant_id}` rows, document extraction of `actor[:tenant_id]` where present, and configure/reset a custom tenancy resolver to prove `Tenancy.scope/2` receives the actor/context. Add admin seam tests for `MailglassAdmin.Operator.Tenants.list_tenants/2` proving outbound tenant ids and inbound-only tenant ids are returned as one de-duplicated sorted selector list. Add optional gateway tests proving inbound ids are surfaced when `mailglass_inbound` is loaded and remain absent when the optional gateway is unavailable. This implements D-01 and D-02.</action>
  <verify>
    <automated>mix test test/mailglass/operator/tenants_test.exs mailglass_admin/test/mailglass_admin/operator/tenants_test.exs mailglass_admin/test/mailglass_admin/optional_deps/mailglass_inbound_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>The new test file fails before `Mailglass.Operator.Tenants` exists and covers distinctness, ordering, blank filtering, and tenancy scoping.</acceptance_criteria>
  <done>The new test file fails before `Mailglass.Operator.Tenants` exists and covers distinctness, ordering, blank filtering, and tenancy scoping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Mailglass.Operator.Tenants.list_tenants/2</name>
  <files>lib/mailglass/operator/tenants.ex, mailglass_admin/lib/mailglass_admin/operator/tenants.ex, mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex</files>
  <read_first>
    @lib/mailglass/operator/deliveries.ex
    @lib/mailglass/tenancy.ex
    @mailglass_admin/lib/mailglass_admin/auth.ex
    @mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
  </read_first>
  <behavior>
    - `list_tenants(context, opts \\ [])` returns selector rows for accessible outbound and inbound-only tenant ids.
    - The outbound query applies `Tenancy.scope(query, context)` and admin never performs raw Repo access.
    - Rows have only selector data needed by the shell: `:id` and `:label`.
  </behavior>
  <action>Add `lib/mailglass/operator/tenants.ex` with `Mailglass.Operator.Tenants.list_tenants/2` for the core outbound projection. Use a distinct projection over existing `Mailglass.Outbound.Delivery` records, exclude nil/blank tenant ids, sort ascending by tenant id, select `%{id: delivery.tenant_id, label: delivery.tenant_id}`, apply `Tenancy.scope/2` with the caller actor/context, and call `Mailglass.Repo.all/1`. Add `MailglassAdmin.Operator.Tenants.list_tenants/2` as the single shell-accessible selector seam: it calls the core outbound projection and the optional inbound gateway, unions outbound and inbound-only ids, de-duplicates, sorts, and returns `%{id: tenant_id, label: tenant_id}` rows. Extend `MailglassAdmin.OptionalDeps.MailglassInbound` with a runtime-gated tenant projection function for inbound-only tenant ids when `mailglass_inbound` is loaded, using the existing `apply/3` gateway style so admin shell code never references `MailglassInbound.*` directly. Do not add migrations, persisted tenant tables, admin Repo access, tenant CRUD, invites, pins, favorites, or cross-tenant aggregate counts.</action>
  <verify>
    <automated>mix test test/mailglass/operator/tenants_test.exs test/mailglass/operator/deliveries_test.exs mailglass_admin/test/mailglass_admin/operator/tenants_test.exs mailglass_admin/test/mailglass_admin/optional_deps/mailglass_inbound_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>`Mailglass.Operator.Tenants.list_tenants/2` returns scoped core outbound rows; `MailglassAdmin.Operator.Tenants.list_tenants/2` returns distinct shell selector rows including inbound-only tenants when available; core delivery, admin seam, and optional gateway tests pass.</acceptance_criteria>
  <done>`Mailglass.Operator.Tenants.list_tenants/2` returns scoped core outbound rows; `MailglassAdmin.Operator.Tenants.list_tenants/2` returns distinct shell selector rows including inbound-only tenants when available; core delivery, admin seam, and optional gateway tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator actor -> core read model | Authenticated actor/context controls which tenant ids can be listed. |
| database -> shell selector | Tenant ids become UI labels and must remain scoped and escaped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Information Disclosure | `Mailglass.Operator.Tenants.list_tenants/2`, `MailglassAdmin.Operator.Tenants.list_tenants/2` | mitigate | Apply `Mailglass.Tenancy.scope/2` to the distinct core tenant query, use the optional inbound gateway for inbound ids, and test restrictive actor context plus inbound-only union behavior. |
| T-112-02 | Tampering | tenant label rendering downstream | mitigate | Return plain string fields only; downstream HEEx renders escaped text. |
| T-112-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `mix test test/mailglass/operator/tenants_test.exs test/mailglass/operator/deliveries_test.exs mailglass_admin/test/mailglass_admin/operator/tenants_test.exs mailglass_admin/test/mailglass_admin/optional_deps/mailglass_inbound_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
SHELL-01/SHELL-02 have a core tenant discovery seam available for later shell plans, and no admin code reads tenant data directly.
</success_criteria>

<output>
Create `.planning/phases/112-app-shell-navigation-tenant-seam/112-01-SUMMARY.md` when done.
</output>
