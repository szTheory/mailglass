---
phase: 112-app-shell-navigation-tenant-seam
plan: 05
type: execute
wave: 5
depends_on:
  - 112-04
files_modified:
  - lib/mailglass/operator/deliveries.ex
  - mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex
  - mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
  - test/mailglass/operator/deliveries_test.exs
  - mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs
autonomous: true
requirements:
  - SHELL-06
must_haves:
  truths:
    - "Read models expose tenant-scoped total count and page boundary metadata for operator and inbound lists."
    - "Pagination controls render only when real read-model metadata says `total_pages > 1` per D-11."
    - "Boundary previous/next controls are disabled, not hidden, and totals are never inferred from truncated entry length per D-10."
  artifacts:
    - path: "lib/mailglass/operator/deliveries.ex"
      provides: "Paginated delivery read metadata"
      exports: ["list_recent_deliveries_page/2"]
    - path: "mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex"
      provides: "Paginated inbound read metadata"
      exports: ["list_records_page/2"]
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "lib/mailglass/operator/deliveries.ex"
      via: "page metadata assigned from delivery read model"
      pattern: "list_recent_deliveries_page"
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex"
      via: "optional gateway exposes inbound page metadata"
      pattern: "list_records_page"
---

<objective>
Add honest pagination metadata APIs for outbound deliveries and inbound records.

Purpose: list chrome cannot claim totals or page boundaries from capped arrays; counts and pagination must be backed by tenant-scoped read models before UI renders controls.
Output: page metadata APIs and read-model tests proving count/page boundaries are real.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-app-shell-navigation-tenant-seam/112-CONTEXT.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-RESEARCH.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-UI-SPEC.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md
@lib/mailglass/operator/deliveries.ex
@mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex
@mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
@mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
@mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
</context>

## Artifacts this phase produces

- Tenant-scoped pagination metadata for outbound and inbound list read models.
- Tests proving totals and boundaries are computed from count queries, not capped list length.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add read-model pagination metadata tests</name>
  <files>test/mailglass/operator/deliveries_test.exs, mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs</files>
  <read_first>
    @test/mailglass/operator/deliveries_test.exs
    @mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs
    @lib/mailglass/operator/deliveries.ex
    @mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex
  </read_first>
  <behavior>
    - Delivery page API returns `%{entries, total_count, page, per_page, total_pages, has_previous?, has_next?}`.
    - Inbound page API returns the same metadata shape through tenant-scoped filters.
    - Seeding more rows than `per_page` reports the full total while returning only the requested page entries.
    - Count queries respect tenant and filters.
  </behavior>
  <action>Add tests before implementation for `Mailglass.Operator.Deliveries.list_recent_deliveries_page/2` and `MailglassInbound.Internal.Operator.Records.list_records_page/2`. Include at least one case with `per_page` smaller than total count so a truncated entry list cannot pass as total. Include first page and last page boundary booleans. This implements D-10 and prepares D-11 UI wiring in Plan 112-06.</action>
  <verify>
    <automated>mix test test/mailglass/operator/deliveries_test.exs mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Read-model tests fail until real count/page metadata exists for both outbound and inbound lists.</acceptance_criteria>
  <done>Read-model tests fail until real count/page metadata exists for both outbound and inbound lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement page metadata APIs and optional gateway</name>
  <files>lib/mailglass/operator/deliveries.ex, mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex, mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex</files>
  <read_first>
    @lib/mailglass/operator/deliveries.ex
    @mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex
    @mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
  </read_first>
  <behavior>
    - New page APIs reuse the same tenant/filter pipeline as existing list APIs.
    - Count query runs before limit/offset and after tenant/filter predicates.
    - Existing `list_recent_deliveries/2` and `list_records/2` remain compatible by returning entries.
  </behavior>
  <action>Add paginated wrappers without breaking existing callers. Normalize `page` and `per_page` as positive integers, cap `per_page` with the existing max-limit constants, compute `total_count` from the same scoped filter query before limit/offset, calculate `total_pages`, `has_previous?`, and `has_next?`, and return entries using the existing projection. Expose inbound pagination through `MailglassAdmin.OptionalDeps.MailglassInbound.list_records_page/2` with the same runtime `apply/3` pattern. Do not infer totals from `Enum.count(entries)`, add infinite scroll, or render always-on pagination.</action>
  <verify>
    <automated>mix test test/mailglass/operator/deliveries_test.exs mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Read models return honest page metadata under tenant/filter scope while old list APIs remain green.</acceptance_criteria>
  <done>Read models return honest page metadata under tenant/filter scope while old list APIs remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL page params -> read model | Page and per-page values are untrusted input. |
| database count -> UI | Count metadata reveals tenant-scoped record volume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-11 | Information Disclosure | pagination counts | mitigate | Count query reuses tenant/filter pipeline and `Tenancy.scope/2`; tests include foreign-tenant rows. |
| T-112-12 | Tampering | page/per_page params | mitigate | Parse positive integers, cap page size, and normalize invalid values. |
| T-112-13 | Denial of Service | count queries | mitigate | Keep filters indexed-compatible and cap page size; no unbounded entry load. |
| T-112-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `mix test test/mailglass/operator/deliveries_test.exs mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
SHELL-06 backend metadata is ready for UI controls that do not misstate result totals or page boundaries.
</success_criteria>

<output>
Create `.planning/phases/112-app-shell-navigation-tenant-seam/112-05-SUMMARY.md` when done.
</output>
