---
phase: 115-pages-flows-micro-animation-microcopy
plan: 03
type: execute
wave: 2
depends_on: ["115-01", "115-02"]
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
autonomous: true
requirements: [FLOW-03, FLOW-04]
must_haves:
  truths:
    - "A new VOICE-GATE greps *.ex ONLY, banning the Oops-class phrases (Oops/Whoops/Uh oh/Something went wrong) (D-12)"
    - "VOICE-GATE's .ex-only scope sidesteps the phoenix.mjs inlined-noops false-positive (the token lives in a JS asset, never an .ex file) (D-12)"
    - "VOICE-GATE does NOT grep standalone Email/Status/Notification — Status is a legitimate <th> header; those rules are positive render assertions in voice_test, never a ban grep (D-12)"
    - "MOTION-GATE gains a positive check: .motion-overlay must declare transform-origin: var(--mg-origin (D-09)"
    - "MOTION-GATE gains a negative check: no transition-colors on the theme-picker label / data-theme root chrome (D-09)"
    - "Both gates plug into the existing check-conformance.sh gate sequence and increment errors the same way as sibling gates"
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "VOICE-GATE (.ex-scoped Oops-class ban) + MOTION-GATE additions (positive origin var, negative theme transition-colors)"
  key_links:
    - from: "check-conformance.sh VOICE-GATE"
      to: "lib/**/*.ex copy"
      via: "grep over *.ex only for Oops-class phrases (sidesteps phoenix.mjs noops)"
      pattern: "Oops|Whoops|Uh oh|Something went wrong"
  prohibitions:
    - "Do NOT scope VOICE-GATE beyond *.ex (no JS/asset scan) — that reintroduces the phoenix.mjs noops false-red (D-12)"
    - "Do NOT add a ban grep for standalone Email/Status/Notification (permanently false-red on the Status <th>) (D-12)"
    - "Do NOT add pixel-diff or any toHaveScreenshot — gates stay grep-only (D-03)"
    - "Do NOT touch fixtures, routes, brandbook/, or the ratchet baseline (D-13/D-04)"
---

<objective>
Extend `check-conformance.sh` with the FLOW-04 VOICE-GATE and the FLOW-03 MOTION-GATE additions, deterministic and grep-only.

Purpose: Make the banned-language and origin-aware-overlay / theme-transition contracts enforceable at lint time, reusing the existing gate idiom. Scope VOICE-GATE to `.ex` only so the `phoenix.mjs` "noops" token never trips it, and never ban standalone domain nouns (those are positive render assertions).
Output: VOICE-GATE block + MOTION-GATE positive/negative additions in check-conformance.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- New conformance gate: VOICE-GATE (`.ex`-scoped Oops-class ban).
- Extended conformance gate: MOTION-GATE positive (`.motion-overlay` declares `transform-origin: var(--mg-origin`) + negative (no `transition-colors` on theme-picker label / `data-theme` root chrome).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add VOICE-GATE — .ex-only Oops-class ban</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - mailglass_admin/scripts/check-conformance.sh (PHASE112-SHELL-GATE ~302-329 — the gate idiom + `$LIB`, `$DELIVERIES_LIST`, `$RECORDS_LIST`, `$OPERATOR_LIVE`, `$INBOUND_LIVE`, `errors` counter; VOICE-GATE belongs adjacent to it)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-12 ban verification — VOICE-GATE .ex-only; the phoenix.mjs noops false-positive; no standalone-noun ban)
    - mailglass_admin/test/mailglass_admin/voice_test.exs (the phoenix.mjs noops note ~32-35 — context for why .ex-only)
  </read_first>
  <action>
    Add a VOICE-GATE block adjacent to PHASE112-SHELL-GATE following the same idiom (grep, echo FAIL, `errors=$((errors + 1))`). Grep over `"$LIB" --include="*.ex"` ONLY (the `.ex`-only scope is mandatory — it sidesteps the `phoenix.mjs` inlined "noops" false-positive, which lives in a JS asset and is never in an `.ex` file). Ban the Oops-class phrases case-insensitively as whole phrases: `Oops`, `Whoops`, `Uh oh`, `Something went wrong`. Use a grep -E alternation; do NOT match the substring "noops" (anchor "Oops" so it is not preceded by a word character, e.g. require a non-word boundary before it). On any hit, print the offending lines and `echo "FAIL: VOICE-GATE — banned Oops-class phrase found in admin copy (.ex only); use cause-naming recovery copy (COPY-LD-07)" >&2` then increment `errors`.
    Do NOT add any grep for standalone `Email`/`Status`/`Notification` — `Status` is a legitimate `<th>` header (`deliveries_list.ex` ~106); a blanket ban goes permanently false-red. Those domain-noun rules are positive render assertions in voice_test (Plan 01), never a ban grep here.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q "VOICE-GATE" scripts/check-conformance.sh && bash scripts/check-conformance.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: VOICE-GATE's grep uses `--include="*.ex"` and does NOT scan `.js`/`.mjs`/asset files.
    - Source assertion: VOICE-GATE contains no alternation banning standalone `Email`/`Status`/`Notification`.
    - Behavior assertion: with the Plan-01 copy in place, `bash scripts/check-conformance.sh` passes VOICE-GATE (no Oops-class phrase in `.ex` copy); the `phoenix.mjs` "noops" token does not trip it.
  </acceptance_criteria>
  <done>VOICE-GATE bans the Oops-class in `.ex` copy only, with no standalone-noun ban and no phoenix.mjs false-red.</done>
</task>

<task type="auto">
  <name>Task 2: Extend MOTION-GATE — positive origin var + negative theme transition-colors</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - mailglass_admin/scripts/check-conformance.sh (MOTION-GATE ~364-393 — the existing layout-property + ease-in checks, the grep/echo/errors idiom)
    - mailglass_admin/assets/css/app.css (the `.motion-overlay` transform-origin var added in Plan 02; theme chrome)
    - mailglass_admin/lib/mailglass_admin/components.ex (theme-picker label ~337 — transition-colors removed in Plan 01)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-09 conformance: positive transform-origin var; negative no transition-colors on theme label / data-theme chrome)
  </read_first>
  <action>
    Extend the MOTION-GATE section (~364-393) with two checks following the existing idiom:
    (a) POSITIVE — assert `assets/css/app.css` contains a `.motion-overlay` rule declaring `transform-origin: var(--mg-origin`; if the grep does NOT match, `echo "FAIL: MOTION-GATE — .motion-overlay must declare transform-origin: var(--mg-origin (origin-aware overlays, D-07)" >&2` and increment `errors`. Filter comment lines (`grep -v '^[[:space:]]*\*'` or anchor on the declaration) so header prose cannot self-satisfy/self-invalidate the gate.
    (b) NEGATIVE — assert no `transition-colors` is declared on the theme-picker label / `data-theme`-driven root chrome: grep the theme-picker label region of `components.ex` (and any `data-theme` root chrome) for `transition-colors`; on a hit, `echo "FAIL: MOTION-GATE — theme-driven chrome must not animate color (state-layer opt-in only, D-08)" >&2` and increment `errors`. Scope the negative grep narrowly (theme-picker label / `data-theme` chrome) so legitimate state-layer `transition-colors` elsewhere is NOT flagged.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q "transform-origin: var(--mg-origin" scripts/check-conformance.sh && bash scripts/check-conformance.sh && echo GATES_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: MOTION-GATE includes a positive grep for `transform-origin: var(--mg-origin` against `app.css` that FAILs when absent (comment lines filtered so prose cannot self-satisfy).
    - Source assertion: MOTION-GATE includes a negative grep for `transition-colors` scoped to the theme-picker label / `data-theme` chrome only (state-layer transitions elsewhere not flagged).
    - Behavior assertion: with Plans 01+02 in place, `bash scripts/check-conformance.sh` exits 0 (`GATES_PASS` printed).
  </acceptance_criteria>
  <done>MOTION-GATE positively requires the origin var and negatively forbids theme-chrome color transitions; the full gate suite passes against Plans 01+02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI lint gate → committed source | Gates are static grep over committed source; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-05 | Tampering | check-conformance.sh grep scope | mitigate | VOICE-GATE scoped to `.ex` only; no standalone-noun ban — prevents permanent false-red that would erode gate trust (D-12) |
| T-115-06 | Tampering | npm/pip/cargo installs | accept | No package installs; shell-script grep edits only |
</threat_model>

<verification>
- `grep "VOICE-GATE" scripts/check-conformance.sh` matches; its grep uses `--include="*.ex"` only.
- MOTION-GATE positive/negative additions present.
- `bash scripts/check-conformance.sh` exits 0 against Plans 01+02.
</verification>

<success_criteria>
- FLOW-04 VOICE-GATE enforces the Oops-class ban in `.ex` copy without the phoenix.mjs false-red or a standalone-noun ban; FLOW-03 MOTION-GATE enforces the origin var positively and bans theme-chrome color transitions negatively; the whole gate suite is green.
</success_criteria>

<output>
Create `.planning/phases/115-pages-flows-micro-animation-microcopy/115-03-SUMMARY.md` when done.
</output>