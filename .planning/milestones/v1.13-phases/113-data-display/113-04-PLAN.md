---
phase: 113-data-display
plan: 04
type: execute
wave: 3
depends_on: ["113-01", "113-02", "113-03"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "The component lab renders table, card, four data-state, and long-value stress specimens, each auto-wrapped in light, dark, and system (no data-theme) themes (D-08)."
    - "Playwright proves the table is visible and cards hidden at >=768px, and cards visible and table hidden at <768px, on both surfaces (DATA-01)."
    - "Playwright proves zero horizontal overflow for operator-deliveries-table/-cards, inbound-records-table/-cards, and the page body at 320px and 768px (DATA-05)."
    - "Playwright proves status/outcome badges show icon+visible-label in both presentations and selecting a row sets aria-selected=true in both (DATA-04/D-01)."
    - "check-conformance.sh fails on badge drift, mixed/generic data-state branches, and missing data-state heroicons (DATA-02/03/04)."
    - "priv/static/app.css is rebuilt and committed bit-clean after all Phase 113 class changes (bundle discipline)."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      provides: "Phase 113 specimens: deliveries/inbound table+cards, data_state four kinds, long-value stress"
      contains: "data_state"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Responsive/overflow/status/data-state structural assertions for new testids; migrated legacy consumers"
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "STATUS-BADGE-GATE and DATA-STATE-GATE; data-state icon coverage via ICON-EXISTS-GATE"
      contains: "DATA-STATE-GATE"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt bit-clean admin CSS bundle including new table/card/data-state utility classes"
  key_links:
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      via: "getByTestId for operator-deliveries-table/-cards + assertNoElementHorizontalOverflow at 320/768px"
      pattern: "operator-deliveries-(table|cards)"
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "DATA-STATE-GATE greps the four distinct data-state-* literals; STATUS-BADGE-GATE bans badge drift outside status_badge/1"
      pattern: "data-state-(empty|error|permission-denied|stale)"
---

<objective>
Prove the Phase 113 data-display contract repo-locally (D-08) and arm the no-drift gates — the LAST wave because it depends on the testids and selectors created by Plans 01-03. Add gallery specimens (table, cards, four data-states, long-value stress) inheriting light/dark/system for free; extend `structural.spec.js` with responsive/overflow/status/data-state assertions for the new testids while migrating the legacy `operator-deliveries-list*` / `inbound-records-list*` / `noMatchRow()` consumers so the suite never goes red; extend `check-conformance.sh` with `STATUS-BADGE-GATE` and `DATA-STATE-GATE` and confirm the data-state icons via the existing `ICON-EXISTS-GATE`; and perform the SINGLE committed `mix mailglass_admin.assets.build` bundle rebuild for the whole phase.

Purpose: Close DATA-01..05 with structural+gate proof (no pixel diff, no screenshots, no new deps per D-08) and guarantee the committed CSS bundle stays bit-clean (CI `git diff --exit-code`).
Output: Gallery specimens, extended structural spec, extended conformance gates, committed bit-clean `priv/static/app.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-data-display/113-CONTEXT.md
@.planning/phases/113-data-display/113-PATTERNS.md
@.planning/phases/113-data-display/113-UI-SPEC.md
@.planning/phases/113-data-display/113-01-SUMMARY.md
@.planning/phases/113-data-display/113-02-SUMMARY.md
@.planning/phases/113-data-display/113-03-SUMMARY.md
</context>

## Artifacts this phase produces

This plan introduces the conformance gate names `STATUS-BADGE-GATE` and `DATA-STATE-GATE`, new gallery specimen ids for `deliveries_list`/`records_list` table+cards, `data_state` four kinds, and long-value stress, and rebuilds the committed `priv/static/app.css`. (See Plan 01 for the full phase artifact list.)

<tasks>

<task type="auto">
  <name>Task 1: Gallery specimens for table, cards, data states, and long-value stress</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/gallery_live.ex (the file being modified — `render_specimen/1` dispatcher clauses lines 157-392, the `@specimens` list lines 394+, the existing `{:stat_card, "long-label"|"long-value", ...}` tuples lines 510-516, the existing `:deliveries_list` clause line 248, the theme-wrapper harness lines 97-146 that auto-wraps each cell in light/dark/system)
    - mailglass_admin/lib/mailglass_admin/components.ex (the Wave-1 `data_state/1` signature)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex and inbound/records_list.ex (the Wave-2 dual presentations + new testids to render in the lab)
    - .planning/phases/113-data-display/113-PATTERNS.md (gallery section: add `render_specimen` clauses + `@specimens` entries the same way; long-value stress states)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Light / Dark / System Contract" gallery coverage requirement)
    - reference/demo_app/lib/mailglass_demo/demo_data.ex (realistic long-value stress inputs — long subjects, non-ASCII names, long ids)
  </read_first>
  <action>
    Add `render_specimen/1` clauses and `@specimens` entries (the `{component, state, assigns_map}` tuple shape) for: (a) `data_state` in each of the four kinds `:empty`/`:error`/`:permission_denied`/`:stale` (calls `Components.data_state/1`); (b) the deliveries and inbound list components in a `table`/`cards` rendered state with present rows; and (c) long-value stress specimens feeding the lists/data-state realistic long UUIDs, tenant ids, provider message ids, module/function-like names, URLs, non-ASCII names, and long timestamps (source from demo_data.ex patterns). Each specimen is auto-wrapped by the existing theme harness into light, dark, and system (no `data-theme`) cells — do NOT add per-specimen theme logic or `matchMedia`/`localStorage`. Keep stable, unique testids per specimen cell so the structural spec can target them. Do not introduce new components; only render the Wave-1/2/3 ones.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/ --warnings-as-errors --only gallery 2>/dev/null; mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data_state' mailglass_admin/lib/mailglass_admin/gallery_live.ex` is `>= 1` (a data_state dispatcher clause exists).
    - `@specimens` contains entries for all four `data_state` kinds and for list table/cards + long-value stress.
    - `mix compile --warnings-as-errors` succeeds in mailglass_admin.
    - No `matchMedia`/`localStorage`/`data-theme="system"` added to gallery_live.ex.
  </acceptance_criteria>
  <done>The component lab renders table, card, four data-state, and long-value stress specimens, each inheriting light/dark/system coverage from the existing harness.</done>
</task>

<task type="auto">
  <name>Task 2: Extend structural.spec.js with responsive/overflow/status/data-state proof and migrate legacy consumers</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (the file being modified — `assertNoElementHorizontalOverflow` lines 494-499, `assertStatCardShape` 501-524, `assertPrimitiveThemeWrappers` line 348, `noMatchRow()` 549-551, the `openOperator`/`openInbound` fixtures, the `operator-master-detail` grid assertions 829-858, the legacy `operator-deliveries-list` refs at lines 53/1195/1223/1297/1379, `operator-deliveries-list-card` 850/856, `inbound-records-list-card` 891/897, and the synchronous-invariant assertion ~937 that bans `assign_async`/`inbound-loading`)
    - .planning/phases/113-data-display/113-PATTERNS.md ("Cross-file coupling": legacy testids must be migrated or kept alongside new ones in the same wave so the suite never goes red; structural.spec.js section: overflow helper for all four new testids, responsive breakpoint pattern, selected-row a11y on BOTH presentations)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Conformance and Gate Contracts" OVERFLOW-GATE selectors and viewports; "Responsive Behavior Contract")
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex and inbound/records_list.ex (new + preserved testids from Wave 2)
  </read_first>
  <action>
    Extend `structural.spec.js` to prove DATA-01/04/05 structurally (no screenshots/pixel diff — D-08):
    - Responsive: at `setViewportSize({width: 768})` assert `operator-deliveries-table` and `inbound-records-table` are visible and `operator-deliveries-cards`/`inbound-records-cards` are hidden; at `{width: 390}` (or 320) assert the inverse. Mirror the existing `operator-master-detail` assertion style.
    - Overflow (OVERFLOW-GATE): call `assertNoElementHorizontalOverflow` for `operator-deliveries-table`, `operator-deliveries-cards`, `inbound-records-table`, `inbound-records-cards`, and the page body at 320px and 768px.
    - Status/severity (DATA-04): assert each status/outcome badge renders a visible text label (not icon-only) in BOTH the table and card presentation; use the `inbound-outcome-{outcome}` / delivery-status testids.
    - Selection a11y: after clicking a row in BOTH presentations, assert `aria-selected="true"` (existing pattern).
    - Data-state: assert the four `data-state-*` testids render distinctly (e.g. via the gallery specimens or the live empty/error/permission/stale routes), and re-confirm the synchronous-invariant assertion still passes (no `assign_async`/`inbound-loading`).
    - Migrate legacy consumers: update the references to `operator-deliveries-list`, `operator-deliveries-list-card`, `inbound-records-list-card`, and `noMatchRow()` so they resolve against the new/preserved testids — keep the suite green. Do NOT delete an assertion that still proves a real contract; re-point it.
    Run the suite (it builds the bundle first via the npm script) and ensure 0 failures, 0 unexpected skips.
  </action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "responsive|stat_card|status|overflow|data.state|Data"</automated>
  </verify>
  <acceptance_criteria>
    - `structural.spec.js` references all four new testids `operator-deliveries-table|cards` and `inbound-records-table|cards` with overflow + visibility assertions at 320/768px.
    - The legacy `operator-deliveries-list*`/`inbound-records-list*`/`noMatchRow()` consumers resolve (no orphaned selectors) and the suite passes.
    - Selected-row `aria-selected="true"` asserted on both presentations; status labels asserted visible in both.
    - `npm run test:operator-browser -- --grep "responsive|stat_card|status|overflow|data.state|Data"` passes with 0 failures.
  </acceptance_criteria>
  <done>The browser structural suite proves responsive table/card switching, zero overflow, visible status labels, selection a11y on both presentations, and distinct data states — with all legacy consumers migrated and green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend conformance gates and commit the single bit-clean CSS bundle</name>
  <files>mailglass_admin/scripts/check-conformance.sh, mailglass_admin/priv/static/app.css</files>
  <read_first>
    - mailglass_admin/scripts/check-conformance.sh (the file being modified — `LIB`/`HEROICONS` resolution lines 15-33, PRIMITIVE-DRIFT-GATE 35-76, FORM-DRIFT-GATE 80-113, STATCARD-GATE 118-134, ICON-EXISTS-GATE 138-160, BADGE-GATE 162-166, TYPE-GATE 170+ — copy these clause shapes; note the `grep -v '^#'`/exclusion hygiene already used)
    - .planning/phases/113-data-display/113-PATTERNS.md (conformance section: extend not weaken; add Phase 113 deltas ONLY after the selectors/components exist — which they now do post-Wave-1/2; "Comment-Text Discipline" — the literals a negative grep bans must not appear verbatim in HEEx)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Conformance and Gate Contracts" table: STATUS-BADGE-GATE, DATA-STATE-GATE, ICON-EXISTS-GATE deltas)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, inbound/records_list.ex, components.ex (the sources the gates scan)
  </read_first>
  <action>
    Add two new gate clauses to `check-conformance.sh`, mirroring the existing BADGE-GATE/STATCARD-GATE clause style, scoped to `$LIB` `.ex` sources:
    - STATUS-BADGE-GATE: fail if a `badge-` class literal or a severity icon appears in the two list modules (`deliveries_list.ex`, `records_list.ex`) OUTSIDE a `Components.status_badge` call — i.e. enforce that list-row status/outcome rendering routes through the canonical primitive. Use a positive check that each list module calls `Components.status_badge` and a negative check that no `defp` badge/severity map was added there. Use `grep -v '^#'` so header comments do not self-trip.
    - DATA-STATE-GATE: fail if the four distinct literals `data-state-empty`, `data-state-error`, `data-state-permission-denied`, `data-state-stale` are not each present, or if a single generic merged empty-state testid is reintroduced — proving the four states stay distinct (D-05). Confirm `Components.data_state/1` is the single public definition.
    - Confirm the existing ICON-EXISTS-GATE now passes with the new `hero-inbox`/`hero-lock-closed`/`hero-clock` keys present (no gate change needed if Plan 01 embedded them correctly; if it greps a missing key, fail loudly).
    Respect Comment-Text Discipline: any literal a gate negative-greps must not appear verbatim in a HEEx body or head-comment that would echo into compiled output; if a banned literal must legitimately appear, add the `<!-- planner-discipline-allow: ... -->` marker.
    THEN perform the single phase-wide bundle rebuild: run `mix mailglass_admin.assets.build` and commit the resulting `priv/static/app.css`. Confirm the bundle is bit-clean (a second build produces no diff) so CI `git diff --exit-code` passes. Revert any incidental `mix.lock`/swoosh drift introduced by the build (do not commit transitive lock drift).
  </action>
  <verify>
    <automated>cd mailglass_admin && ./scripts/check-conformance.sh && mix verify.preview && git diff --exit-code -- priv/static/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `check-conformance.sh` contains `STATUS-BADGE-GATE` and `DATA-STATE-GATE` clauses and exits 0 on current code.
    - The four `data-state-*` literals are each detected; no merged generic empty-state testid remains.
    - ICON-EXISTS-GATE passes with the three new data-state icon keys.
    - `mix verify.preview` exits 0 and `git diff --exit-code -- priv/static/app.css` is clean (bundle committed bit-clean).
    - No `mix.lock` transitive drift is committed.
  </acceptance_criteria>
  <done>STATUS-BADGE-GATE and DATA-STATE-GATE are armed and green, data-state icons resolve, and the committed `priv/static/app.css` is rebuilt bit-clean with no lock drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gallery/structural proof → repo-local lanes only | Verification must stay inside ExUnit/Playwright-structural/conformance — no new runtime dep, no screenshot/pixel pipeline (D-08) |
| compiled CSS bundle → committed artifact | The bundle is a tracked artifact; CI fails on drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-13 | Tampering | data-state distinctness | mitigate | DATA-STATE-GATE fails closed if the four states collapse into a generic branch, preserving the permission-denied≠no-data security property at lint time |
| T-113-14 | Spoofing/Tampering | badge drift outside status_badge/1 | mitigate | STATUS-BADGE-GATE bans page-local badge/severity maps in list modules, keeping icon+label+color encoding canonical |
| T-113-15 | Tampering | committed CSS bundle | mitigate | Bundle rebuilt bit-clean and `git diff --exit-code` asserted; no unbundled utilities ship |
| T-113-16 | Information Disclosure | gallery long-value specimens | accept | Specimens use synthetic/demo stress data, not production PII; recipients still routed through mask_recipient in list specimens |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan/phase (RESEARCH audit "No install"); `@axe-core/playwright` explicitly NOT added (Phase 116 scope) |
</threat_model>

<verification>
- `cd mailglass_admin && ./scripts/check-conformance.sh` exits 0 with the two new gates.
- `cd mailglass_admin && mix verify.preview` exits 0; `git diff --exit-code -- priv/static/app.css` clean.
- `cd mailglass_admin && npm run test:operator-browser -- --grep "responsive|stat_card|status|overflow|data.state|Data"` passes.
- Phase gate command (UI-SPEC): `cd mailglass_admin && mix verify.preview && ./scripts/check-conformance.sh && npm run test:operator-browser -- --grep "responsive|stat_card|status|overflow|data.state|Data"`.
</verification>

<success_criteria>
- DATA-01: Playwright proves table/card responsive switching at 768/320px on both surfaces.
- DATA-02/04: gates + structural proof confirm canonical stat_card/status_badge usage and visible labels.
- DATA-03: DATA-STATE-GATE + specimens prove four distinct states; ICON-EXISTS passes.
- DATA-05: OVERFLOW assertions prove zero horizontal overflow for all four list testids + body.
- Bundle committed bit-clean; no new deps; no screenshots/pixel diff (D-08).
</success_criteria>

<output>
Create `.planning/phases/113-data-display/113-04-SUMMARY.md` when done.
</output>
