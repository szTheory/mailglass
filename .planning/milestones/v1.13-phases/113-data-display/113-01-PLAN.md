---
phase: 113-data-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/assets/vendor/heroicons-inline.js
  - mailglass_admin/test/mailglass_admin/components_test.exs
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "An operator can tell a no-data list apart from an unavailable/error list, a permission-denied list, and a stale-data list (D-05)."
    - "Each of the four data states renders a distinct icon, heading, and body — none collapse into shared copy or a shared testid (D-05)."
    - "Every data-state icon (hero-inbox, hero-exclamation-circle, hero-lock-closed, hero-clock) renders a real embedded SVG, never an invisible blank."
    - "Stat-card all-clear, empty, loading, and unavailable render as real readable text states, never a bare dash (D-03/D-04)."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "Public Components.data_state/1 with four distinct kinds and certified stat_card severity/state text"
      contains: "def data_state"
    - path: "mailglass_admin/assets/vendor/heroicons-inline.js"
      provides: "Embedded inbox, lock-closed, clock outline SVGs for the data-state icons"
      contains: "lock-closed"
    - path: "mailglass_admin/test/mailglass_admin/components_test.exs"
      provides: "render_component contract tests for data_state/1 four kinds + stat_card meaningful-text states"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/components.ex"
      to: "mailglass_admin/assets/vendor/heroicons-inline.js"
      via: "data_state/1 emits <.icon name='hero-inbox|hero-lock-closed|hero-clock|hero-exclamation-circle'> resolved by the vendored plugin"
      pattern: "hero-(inbox|lock-closed|clock|exclamation-circle)"
---

<objective>
Establish the shared Phase 113 data-display primitive: a single public `MailglassAdmin.Components.data_state/1` function component encoding the four DISTINCT data states (`:empty`, `:error`, `:permission_denied`, `:stale`) per D-05, and embed the three net-new Heroicons it needs (`hero-inbox`, `hero-lock-closed`, `hero-clock`; `hero-exclamation-circle` already exists). This is Wave 1 because both list surfaces (Plans 02/03) and the gallery/proof surface (Plan 04) consume it.

Resolves RESEARCH Open Question 1 in favor of `Components.data_state/1` (not list-local markup): three surfaces share the identical four-state taxonomy — `deliveries_list.ex`, `records_list.ex`, and `inbound_live.ex` detail-error — which meets the ">=2 surfaces" rule (D-05, UI-SPEC Public ownership).

Purpose: Fix the four data states ONCE at the primitive level so every consuming surface inherits distinct empty/error/permission/stale presentation (DATA-03), and certify the canonical `stat_card/1` meaningful-text contract (DATA-02) so no consumer reintroduces bare-dash placeholders.
Output: `data_state/1` public component, 3 embedded icons, and contract tests. No bundle rebuild here (Plan 04 owns the single committed `priv/static/app.css` rebuild after all class changes land).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-data-display/113-CONTEXT.md
@.planning/phases/113-data-display/113-PATTERNS.md
@.planning/phases/113-data-display/113-UI-SPEC.md
</context>

## Artifacts this phase produces

(Full phase-wide list; this plan introduces the items marked **[01]**.)

- **[01]** `Components.data_state/1` — public function component, attr `kind` values `[:empty, :error, :permission_denied, :stale]`.
- **[01]** New vendored Heroicon keys: `hero-inbox`, `hero-lock-closed`, `hero-clock` (newly embedded in `heroicons-inline.js`).
- **[01]** New testids: `data-state-empty`, `data-state-error`, `data-state-permission-denied`, `data-state-stale`.
- [02] New testids: `operator-deliveries-table`, `operator-deliveries-cards`.
- [03] New testids: `inbound-records-table`, `inbound-records-cards`, `inbound-outcome-{outcome}`.
- [04] New conformance gate names: `STATUS-BADGE-GATE`, `DATA-STATE-GATE`. New gallery specimen ids for table/cards/data-state/long-value stress.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Embed the three net-new data-state Heroicons</name>
  <files>mailglass_admin/assets/vendor/heroicons-inline.js</files>
  <read_first>
    - mailglass_admin/assets/vendor/heroicons-inline.js (the file being modified — note its `"key": "<svg path data>"` object-entry format; the existing `exclamation-circle`, `inbox-stack`, `x-circle` entries are the template to copy)
    - mailglass_admin/scripts/check-conformance.sh (ICON-EXISTS-GATE lines 138-160 — keys are extracted by `grep -E '^[[:space:]]*"[-a-z0-9]+":'` then `sed` strip; the new keys MUST match that line shape exactly or the gate cannot see them)
    - .planning/phases/113-data-display/113-PATTERNS.md ("Asset bundle" coupling note)
  </read_first>
  <action>
    Add three new object entries to the vendored Heroicons map in heroicons-inline.js, using the EXACT same `"key": "<svg-inner-markup>"` line shape the ICON-EXISTS-GATE parses (key without the `hero-` prefix, on its own line starting with optional whitespace then a quoted key then a colon): `inbox`, `lock-closed`, `clock`. Source each SVG body from the canonical Heroicons outline set (24x24 viewBox, outlined paths, `currentColor` stroke matching the existing entries' rendering convention so they inherit light/dark chrome). Do NOT add `exclamation-circle` — it already exists. Do NOT touch unrelated entries. These three keys back the `:empty`, `:permission_denied`, and `:stale` data states respectively; `:error` reuses the existing `exclamation-circle`.
    Do not rebuild the CSS bundle in this task — Plan 04 performs the single committed `mix mailglass_admin.assets.build` after every Phase 113 class change has landed.
  </action>
  <verify>
    <automated>cd mailglass_admin && for k in inbox lock-closed clock exclamation-circle; do grep -qE "^[[:space:]]*\"$k\":" assets/vendor/heroicons-inline.js || { echo "MISSING $k"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^[[:space:]]*"(inbox|lock-closed|clock)":' mailglass_admin/assets/vendor/heroicons-inline.js` returns all three keys (each on a gate-parseable line).
    - `exclamation-circle` remains present (not duplicated).
    - No pre-existing icon entry is removed or altered.
  </acceptance_criteria>
  <done>The three missing data-state icons are embedded in the vendored plugin in the gate-parseable format; `exclamation-circle` is untouched and present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public Components.data_state/1 with four distinct kinds</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex, mailglass_admin/test/mailglass_admin/components_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/components.ex (the file being modified — study `stat_card/1` lines 358-408 and its private clause helpers `stat_severity_icon/1`/`stat_severity_class/1` lines 614-630, and `filter_field/1`, as the exact `attr`-declaration + private-clause-helper convention any new public component must ape)
    - mailglass_admin/test/mailglass_admin/components_test.exs (the file being modified — `render_component/2` assertion pattern, ~lines 26-119)
    - .planning/phases/113-data-display/113-PATTERNS.md (`components.ex` section: the optional `data_state/1` shape, distinct testids, and icon/color mapping)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Distinct Data-State Templates (DATA-03)" table: exact heading/body copy, testid, icon, and icon-color per kind; "Copywriting Contract" banned words; "Accessibility Contract" data-state a11y row)
    - .planning/phases/113-data-display/113-RESEARCH.md (Pattern 2 — Distinct Data-State Helper, and Security Domain "Permission-denied rendered as no-data" mitigation)
  </read_first>
  <behavior>
    - Test 1: `render_component(&Components.data_state/1, kind: :empty, title: "No deliveries", body: "No Deliveries have been recorded yet.")` includes testid `data-state-empty`, `hero-inbox`, `text-secondary`, the title, and the body.
    - Test 2: `kind: :error` includes `data-state-error`, `hero-exclamation-circle`, `text-error`.
    - Test 3: `kind: :permission_denied` includes `data-state-permission-denied`, `hero-lock-closed`, `text-warning`.
    - Test 4: `kind: :stale` includes `data-state-stale`, `hero-clock`, `text-secondary`.
    - Test 5: distinctness — the `:empty` testid and the `:error` testid differ, and `:permission_denied` shares neither icon nor testid with `:empty` (security: permission-denied must not look like no-data).
    - Test 6: the icon carries `aria-hidden="true"` and the heading renders inside a visible `<h3>` (a11y: icon decorative, text visible).
  </behavior>
  <action>
    Add a public `data_state/1` function component to `MailglassAdmin.Components` following the existing `stat_card/1` attr+private-helper convention. Declare attrs: `kind` (`:atom`, `values: [:empty, :error, :permission_denied, :stale]`, required), `title` (`:string`, required), `body` (`:string`, required), `icon` (`:string`, default `nil`), and `rest` (`:global`). Render a `<section>` carrying a per-kind testid via a private `data_state_testid/1` clause set returning `data-state-empty` / `data-state-error` / `data-state-permission-denied` / `data-state-stale` (one literal per clause — these four literals are distinct; do NOT compute them with string interpolation, so the DATA-STATE-GATE in Plan 04 can grep each literal). Render an `<.icon>` whose name comes from `@icon || data_state_icon(@kind)` with `aria-hidden="true"`, where private `data_state_icon/1` maps `:empty`→`hero-inbox`, `:error`→`hero-exclamation-circle`, `:permission_denied`→`hero-lock-closed`, `:stale`→`hero-clock`, and private `data_state_icon_class/1` maps `:empty`→`text-secondary`, `:error`→`text-error`, `:permission_denied`→`text-warning`, `:stale`→`text-secondary`. Render the title in a visible `<h3 class="text-body font-bold text-base-content">` and body in `<p class="text-body text-secondary">`. Use only `text-label`/`text-body`/`text-heading`/`text-display` type tokens (no raw `text-sm`/`text-xl`) and only spacing-scale utilities per the UI-SPEC. Keep all interpolation HEEx-escaped (no `raw/1`) so long titles/bodies cannot inject markup.
    Add the six contract tests above to `components_test.exs` using `render_component/2`, mirroring the existing stat_card/status_badge test style.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def data_state' mailglass_admin/lib/mailglass_admin/components.ex` is `1` (single public definition).
    - All four literals `data-state-empty`, `data-state-error`, `data-state-permission-denied`, `data-state-stale` appear verbatim in components.ex.
    - `mix test test/mailglass_admin/components_test.exs --warnings-as-errors` passes including the six new behavior tests.
    - No `raw(` call appears in the `data_state/1` body.
  </acceptance_criteria>
  <done>`Components.data_state/1` exists as the single public four-state primitive with distinct per-kind testid/icon/color, decorative icon + visible heading, and passing contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Certify stat_card meaningful-text states (DATA-02 contract lock)</name>
  <files>mailglass_admin/test/mailglass_admin/components_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/components.ex (`stat_card/1` lines 358-408; `stat_display_value/1` lines 605-612 maps `:empty`→empty_text, `:loading`→loading_text, `:unavailable`→unavailable_text, `value: nil`→empty_text; `stat_severity_label/1` lines 620-624 where `:neutral`→"All clear")
    - mailglass_admin/test/mailglass_admin/components_test.exs (the file being modified — existing stat_card assertions to extend, not duplicate)
    - .planning/phases/113-data-display/113-PATTERNS.md (`components.ex` section: the REAL `stat_card/1` signature — `state` values `[:ready, :empty, :loading, :unavailable]` NOT `:ok`; `severity` values `[:neutral, :info, :success, :warning, :error]`)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Canonical Stat-Card Usage (DATA-02)" and "Copywriting Contract" all-clear/loading/unavailable rows)
  </read_first>
  <behavior>
    - Test 1: `stat_card` with `state: :empty` renders the `empty_text` (default "No data yet"), never a bare "—" or "___".
    - Test 2: `state: :unavailable` renders the `unavailable_text` (default "Unavailable").
    - Test 3: `state: :loading` renders the `loading_text` (default "Resolving").
    - Test 4: `severity: :neutral` renders the visible "All clear" severity label plus the `hero-minus-circle` icon and `text-secondary` class (all-clear reads as a real state).
    - Test 5: `value: nil` with default state falls back to `empty_text`, not a dash.
  </behavior>
  <action>
    Add (do not duplicate existing) assertions to `components_test.exs` proving `stat_card/1` renders meaningful text for every non-`:ready` state and that `:neutral` severity surfaces a visible "All clear" label with an icon. Use the REAL `state` atoms `[:ready, :empty, :loading, :unavailable]` from the verified signature in PATTERNS.md — do NOT assert against the UI-SPEC's loose `:ok`/`:warning`/`:error` *state* prose (severity, not state, carries warning/error). Assert no bare dash placeholder string appears for any state. This is a certification-only task: it locks the DATA-02 contract that Plans 02/03 call sites rely on; it does not change `stat_card/1`.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert `state: :empty|:loading|:unavailable` each produce their respective non-dash text.
    - A test asserts `severity: :neutral` yields a visible "All clear" label and an icon class.
    - `mix test test/mailglass_admin/components_test.exs --warnings-as-errors` passes.
    - `stat_card/1` source is unchanged (certification only).
  </acceptance_criteria>
  <done>The DATA-02 meaningful-text contract is locked by tests covering empty/loading/unavailable/all-clear, with no public-API change to `stat_card/1`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-model assigns → HEEx render | Display data (titles, bodies, future long values) crosses into rendered markup; must stay HEEx-escaped |
| admin actor → tenant-scoped data | Inherited unchanged from Phase 112; this plan adds no data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | data_state/1 permission-denied template | mitigate | `:permission_denied` is a DISTINCT kind/testid/icon from `:empty`, so authorization failure never renders as no-data (RESEARCH Security Domain) |
| T-113-02 | Tampering (XSS) | data_state/1 title/body interpolation | mitigate | HEEx auto-escaping preserved; no `raw/1` on title/body/icon; acceptance criterion greps for absence of `raw(` |
| T-113-03 | Information Disclosure | data-state copy | accept | Copy uses domain nouns and never echoes raw UUIDs/tenant/provider IDs (UI-SPEC Copywriting Contract); low risk, no PII path introduced |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan or phase (RESEARCH Package Legitimacy Audit = "No install"); nothing to verify |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --warnings-as-errors` passes (data_state + stat_card contract tests).
- All four data-state icon keys resolve in the vendored plugin (Task 1 verify).
- `grep -c 'def data_state' mailglass_admin/lib/mailglass_admin/components.ex` == 1.
- No `mix mailglass_admin.assets.build` here — bundle commit is owned by Plan 04.
</verification>

<success_criteria>
- `Components.data_state/1` is the single public four-state primitive with distinct testid/icon/color per kind and a passing contract suite (DATA-03 foundation).
- `hero-inbox`, `hero-lock-closed`, `hero-clock` are embedded and gate-visible; `hero-exclamation-circle` confirmed present.
- `stat_card/1` meaningful-text + all-clear contract is locked by tests (DATA-02 foundation).
</success_criteria>

<output>
Create `.planning/phases/113-data-display/113-01-SUMMARY.md` when done.
</output>
