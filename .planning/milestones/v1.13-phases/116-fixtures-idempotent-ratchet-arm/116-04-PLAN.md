---
phase: 116-fixtures-idempotent-ratchet-arm
plan: 04
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/priv/static/app.css
  - mailglass_admin/e2e/gallery-matrix.spec.js
autonomous: true
requirements: [RATCHET-02]
user_setup: []

must_haves:
  truths:
    - "The gallery renders the existing hand-enumerated specimens at four viewport widths (320/390/768/wide) x three themes (light/dark/system) — the matrix is a Playwright resize loop over the SAME stable testids, never programmatic cartesian generation"
    - "The stable data-testid=gallery-{component}-{state} contract is preserved (conformance awk-assertions + Playwright depend on it)"
    - "At every viewport x theme, no specimen overflows its container horizontally (scrollWidth <= clientWidth + 1) and at 320px nothing is clipped or hidden"
    - "The system theme wrapper exposes no data-theme attribute (system = absence of explicit theme)"
    - "The non-ascii-tenant and long-value-stress specimens use the SAME literals as the fjordline-aps persona so the plan 116-01 drift-guard stays green"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      provides: "Hand-enumerated stress specimens mirroring the fjordline-aps non-ASCII / long-ID / long-module-name / nil-reject-reason edge values with stable testids"
      contains: "gallery-"
    - path: "mailglass_admin/e2e/gallery-matrix.spec.js"
      provides: "Playwright resize loop asserting no overflow across 320/390/768/wide x light/dark/system over stable testids"
      contains: "320"
  key_links:
    - from: "mailglass_admin/e2e/gallery-matrix.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      via: "resize loop selects the stable data-testid=gallery-{component}-{state} specimens at each width/theme"
      pattern: "gallery-"
    - from: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      to: "reference/demo_app/lib/mailglass_demo/personas.ex"
      via: "gallery stress literals must equal the fjordline-aps persona literals (drift-guard asserts consistency)"
      pattern: "Bjørn Hansen"
---

<objective>
Widen the dev-only component-lab gallery to a component x state x {light,dark,system} x {320..wide}
matrix (RATCHET-02) using a Playwright resize loop over the EXISTING stable specimen testids — never
programmatic cartesian generation (which breaks the testid contract and explodes to 324 cells).

Purpose: The gallery is the audit surface where every component is proven across the full
viewport/theme matrix without overflow or clipping. The stress specimens (non-ASCII, long-ID, long
module name, nil reject_reason) are the library-pure mirrors of the fjordline-aps persona, kept
byte-consistent so the plan 116-01 drift-guard holds. Depends on 116-01 for the canonical literals.

Output: stress specimens in `gallery_live.ex` (with rebuilt + committed `priv/static/app.css` if
classes change), a `gallery-matrix.spec.js` resize-loop overflow check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-CONTEXT.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-RESEARCH.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-UI-SPEC.md

# Existing seam this plan extends (read before implementing):
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
@mailglass_admin/e2e/structural.spec.js
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stress specimens mirroring the fjordline-aps edge values</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex, mailglass_admin/priv/static/app.css</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/gallery_live.ex (the in-code static specimen list, the Coverage docstring, and the existing data-testid=gallery-{component}-{state} contract; note existing non-ascii-tenant / long-value-stress specimens per STATE.md [110-03] / CONTEXT code-context — additive only)
    - 116-01-SUMMARY.md (the exact persona literals chosen — the gallery MUST reuse them)
    - 116-UI-SPEC.md "Gallery Stress Specimen Values" (Bjørn Hansen / 山田太郎 / >=26-char ULID del_01JXW… / >=60-char module name / nil reject_reason shows NO badge) + "RATCHET-02: Gallery Matrix Rendering" + Typography (long values use text-label/text-body 400 + truncate)
  </read_first>
  <action>
    Extend the hand-enumerated gallery specimen list (D-09 — keep it as the source of truth; the testid
    contract data-testid=gallery-{component}-{state} stays stable for the conformance awk-assertions). Ensure
    specimens exist that mirror EACH fjordline-aps edge value using the EXACT literals from plan 116-01 /
    116-UI-SPEC: non-ASCII display names "Bjørn Hansen" and "山田太郎"; a >=26-char ULID-class long delivery
    ID; a >=60-char long Mailable module name; a :delivered event with reject_reason: nil rendering NO
    reject-reason badge. Long values must use text-label/text-body at weight 400 with truncate /
    overflow-hidden text-ellipsis so they never overflow (per Typography contract; SPACE-GATE/TYPE-GATE
    must stay green). The system theme wrapper must expose no data-theme attribute (system = absence of
    explicit theme, STATE.md [110-03]). Do NOT generate specimens programmatically — additive named rows only.

    If any class changes, rebuild and COMMIT priv/static/app.css (bundle-clean gate — mix
    mailglass_admin.assets.build must produce bit-identical committed output). If a new hero-* icon is
    introduced, embed its SVG in heroicons-inline.js and rebuild (invisible-icon footgun) — but prefer
    reusing the existing vendored icon inventory.
  </action>
  <verify>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh 2>&1 | tail -15 && mix mailglass_admin.assets.build >/dev/null 2>&1 && git diff --exit-code priv/static/app.css && echo "BUNDLE_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - Gallery specimens mirror the fjordline-aps non-ASCII / long-ID / long-module-name / nil-reject-reason edge values using the exact shared literals
    - Stable gallery-{component}-{state} testids preserved; system wrapper has no data-theme
    - check-conformance.sh green; priv/static/app.css rebuilt + committed bit-identical
    - plan 116-01 drift-guard still passes (literals consistent)
  </acceptance_criteria>
  <done>The gallery carries the stress specimens that mirror the persona edge values, with stable testids and a clean asset bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright resize-loop matrix overflow gate</name>
  <files>mailglass_admin/e2e/gallery-matrix.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (existing gallery testid selectors, emulateMedia theme drive, the canonical width/theme ordering per STATE.md [110-04])
    - 116-CONTEXT.md D-09 + 116-RESEARCH.md "Anti-Patterns to Avoid" (resize loop over SAME testids, NOT programmatic cartesian) + 116-UI-SPEC.md "Breakpoints" (320/390/768/wide=1440) + "RATCHET-02"
  </read_first>
  <action>
    Create e2e/gallery-matrix.spec.js implementing the RATCHET-02 matrix as a Playwright resize loop over
    the SAME stable gallery testids (D-09): for each width in [320, 390, 768, 1440] x theme in
    [light, dark, system], set the viewport (page.setViewportSize) and theme (emulateMedia + the app
    3-wrapper; system => no explicit data-theme), then for each gallery specimen assert no horizontal
    overflow (scrollWidth <= clientWidth + 1) and at 320px that content is not clipped or hidden. Assert the
    existing non-ascii-tenant and long-value-stress specimens render without overflow at all widths. Do NOT
    introduce new specimen rows in the spec — iterate the existing testids only (the 324-cell explosion
    SUMMARY warns against is avoided by resizing, not multiplying rows). Keep it in the single-worker
    operator-browser flow.
  </action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "gallery.matrix|gallery resize|gallery overflow" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Resize loop iterates 320/390/768/1440 x light/dark/system over the stable testids
    - No specimen overflows horizontally at any cell; nothing clipped at 320px
    - No new specimen rows added in the spec (iterates existing testids only)
  </acceptance_criteria>
  <done>A Playwright resize loop proves the full gallery matrix renders without overflow across all widths and themes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only gallery route → committed asset bundle | Gallery specimen class changes must rebuild + commit priv/static/app.css; an uncommitted bundle is a CI gate failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; axe devDep handled in plan 116-02. |
| T-116-08 | Tampering | priv/static/app.css bundle | mitigate | Rebuild + commit the bundle after class changes; `git diff --exit-code priv/static/app.css` proves bit-identical output (bundle-clean gate). |
| T-116-09 | Information disclosure | gallery is dev-only | accept | GalleryLive is a dev-only component lab, not a production route; stress specimens are fictional values, no PII. |
</threat_model>

<verification>
- `cd mailglass_admin && bash scripts/check-conformance.sh` green
- `mix mailglass_admin.assets.build` produces bit-identical committed `priv/static/app.css`
- `cd mailglass_admin && npm run test:operator-browser -- --grep "gallery"` green across 320/390/768/1440 x 3 themes
- plan 116-01 drift-guard still green (gallery literals == persona literals)
</verification>

<success_criteria>
- Gallery matrix proven across component x state x {light,dark,system} x {320,390,768,1440} (RATCHET-02)
- Stable testid contract preserved; no programmatic cartesian generation (D-09)
- No horizontal overflow at any cell; clean asset bundle
</success_criteria>

<output>
Create `.planning/phases/116-fixtures-idempotent-ratchet-arm/116-04-SUMMARY.md` when done.
Record: the `--grep` label for the matrix spec and any new specimen testids (input to plan 116-06's
full-matrix run).
</output>
