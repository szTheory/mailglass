---
phase: 116-fixtures-idempotent-ratchet-arm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/lib/mailglass_demo/personas.ex
  - reference/demo_app/lib/mailglass_demo/demo_data.ex
  - reference/demo_app/priv/repo/seeds.exs
  - mailglass_admin/mix.exs
  - mailglass_admin/test/support/operator_fixtures.ex
  - mailglass_admin/test/mailglass_admin/persona_cohort_test.exs
  - mailglass_admin/test/mailglass_admin/persona_drift_guard_test.exs
autonomous: true
requirements: [RATCHET-01]
user_setup: []

must_haves:
  truths:
    - "The demo seed materializes three personas: northstar (many/high-count/error), fjordline-aps (one/long-ID/non-ASCII/null), helios-void (zero deliveries)"
    - "list_tenants/2 returns >= 2 selectable tenants (northstar + fjordline-aps) so the Phase-112 picker has a reason to render"
    - "helios-void is absent from list_tenants/2 because it has zero Delivery rows"
    - "The admin test-support seed_persona_cohort!/0 materializes the SAME persona spec into TestRepo via a test-only path dep"
    - "A drift-guard fails closed if any of the three materializers (demo seed, admin test-support, gallery values) diverge from the Personas spec persona-name + edge-case set"
  artifacts:
    - path: "reference/demo_app/lib/mailglass_demo/personas.ex"
      provides: "Declarative single-source persona cohort spec (names + 8 edge cases + payload shapes) and Personas.seed!/1"
      contains: "def spec"
    - path: "mailglass_admin/test/support/operator_fixtures.ex"
      provides: "seed_persona_cohort!/0 admin-side materializer of the same spec"
      contains: "seed_persona_cohort!"
    - path: "mailglass_admin/test/mailglass_admin/persona_cohort_test.exs"
      provides: "Integration assertion: 3 personas seed, >=2 selectable, helios-void absent"
    - path: "mailglass_admin/test/mailglass_admin/persona_drift_guard_test.exs"
      provides: "Fail-closed drift guard across the 3 materializers (D-07)"
  key_links:
    - from: "mailglass_admin/test/support/operator_fixtures.ex"
      to: "reference/demo_app/lib/mailglass_demo/personas.ex"
      via: "test-only path dep {:mailglass_demo, path: \"../reference/demo_app\", only: [:test], runtime: false} — admin test-support calls Personas.spec()"
      pattern: "MailglassDemo.Personas"
    - from: "reference/demo_app/lib/mailglass_demo/personas.ex"
      to: "lib/mailglass/operator/tenants.ex"
      via: "helios-void has zero Delivery rows; list_tenants/2 keys off distinct Delivery.tenant_id so it is correctly absent"
      pattern: "Delivery"
---

<objective>
Land the multi-tenant persona stress-fixture cohort (RATCHET-01) as a single declarative spec
materialized by three thin builders, with a drift-guard that defeats triplication drift.

Purpose: This cohort is the keystone proving substrate of v1.13 — it gives the Phase-112 tenant
picker a real reason to exist (>=2 selectable tenants) and supplies the no-data / long-ID /
non-ASCII / null edge data that every downstream stress assertion (gallery, interaction pillar,
Bucket-A guards, demo run) depends on. It must land in Wave 1 so all later waves can consume it.

Output: `personas.ex` (declarative spec + `seed!/1`), parameterized demo seeding, admin
test-support `seed_persona_cohort!/0` via a test-only path dep, and two fail-closed ExUnit tests
(cohort integration + drift guard).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-CONTEXT.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-RESEARCH.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-UI-SPEC.md

# Existing seam this plan builds on (read before implementing):
@reference/demo_app/lib/mailglass_demo/demo_data.ex
@lib/mailglass/operator/tenants.ex
@mailglass_admin/test/support/operator_fixtures.ex
@mailglass_admin/mix.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declarative Personas spec module + parameterized demo seeding</name>
  <files>reference/demo_app/lib/mailglass_demo/personas.ex, reference/demo_app/lib/mailglass_demo/demo_data.ex, reference/demo_app/priv/repo/seeds.exs</files>
  <read_first>
    - reference/demo_app/lib/mailglass_demo/demo_data.ex (FULL — note @tenant "northstar" is hardcoded into EVERY private helper: delivery!/1, event!/4, webhook!, suppression!/4, inbound_record!/1, inbound_evidence!/2, inbound_run!, orphan/reconcile/failed-ingest inserts; truncate!/0 wipes RESTART IDENTITY CASCADE; @empty_tenant "empty-tenant" is declared but never seeded — RESEARCH Landmine 1+2)
    - reference/demo_app/priv/repo/seeds.exs (confirm it calls DemoData.reset!)
    - 116-CONTEXT.md D-06, D-08 (persona->edge-case map); 116-UI-SPEC.md "Gallery Stress Specimen Values" (exact literals)
    - reference/demo_app/lib/mailglass_demo/repo.ex + the demo app's `Delivery` schema alias (confirm the exact module — `Mailglass.Delivery` is the library schema; adjust the inline `mix run` seed-assertion query in <verify> if the demo app aliases it differently, so the distinct-tenant_id check resolves)
  </read_first>
  <behavior>
    - Personas.spec/0 returns a list of three persona maps, each: %{name, edge_cases, payload} where name in ["northstar","fjordline-aps","helios-void"] and edge_cases is a MapSet/list drawn from the 8 cases (no-data, one, many, long-id, non-ascii, null, high-count, error).
    - northstar.edge_cases covers many/high-count/error; fjordline-aps covers one/long-id/non-ascii/null; helios-void covers no-data.
    - Personas.seed!(repo) inserts: northstar via the EXISTING full lifecycle payload; fjordline-aps with exactly ONE Delivery (recipient display names "Bjørn Hansen" and "山田太郎" on from[].name, a >=26-char ULID-class long delivery id e.g. "del_01JXW9ZQKB3V1N4P2RMT7FHCG", one Event with reject_reason: nil on a :delivered, a >=60-char Mailable module name "Mailglass.Demo.Mailables.TransactionalEmailWithVeryLongModuleName"); helios-void with ZERO Delivery rows.
    - After Personas.seed!(repo), querying distinct non-empty Delivery.tenant_id returns exactly ["fjordline-aps","northstar"] (helios-void absent — it has no Delivery).
  </behavior>
  <action>
    Create reference/demo_app/lib/mailglass_demo/personas.ex as a PURE data/spec module (no demo-web,
    no @tenant hardcoding) per D-06: expose spec/0 (the single source of truth: names + 8 edge-case
    assignments per D-08 + the payload literals from 116-UI-SPEC "Gallery Stress Specimen Values")
    and seed!/1 (the demo-Repo side effect). Keep spec/0 and seed!/1 separable so admin test-support
    can consume spec/0 without running the seed side effect.

    Parameterize the demo seeding so fjordline-aps and helios-void can be seeded with their OWN
    tenant_id (Pitfall 2 — the existing helpers close over @tenant "northstar"): either add a
    tenant_id argument to the relevant DemoData inserters, OR give Personas its own thin inserter that
    sets tenant_id per persona. Keep the existing northstar lifecycle body as northstar's payload
    (D-08 "keep northstar as-is"); add fjordline-aps (single Delivery, per the behavior block literals)
    and helios-void (zero Delivery rows — realized via ABSENCE per RESEARCH Landmine 2). Wire
    DemoData.reset! to call Personas.seed!(Repo) so the demo seed materializes all three (this also
    serves RATCHET-04's harness-boot seeding — verify seeds.exs / reset! path runs it). Library lib
    code stays demo-free; this is all demo-app code (RATCHET-01, D-06).

    Use domain-true naming per prompts/mailer-domain-language-deep-research.md and the Anymail event
    taxonomy. The non-ASCII literals and long-ID/long-module-name MUST exactly match the values in
    116-UI-SPEC so the Task 3 drift-guard and the gallery (plan 116-04) stay consistent.
  </action>
  <verify>
    <automated>cd reference/demo_app && MIX_ENV=dev mix compile --warnings-as-errors 2>&1 | tail -20 && MIX_ENV=dev mix run -e 'MailglassDemo.DemoData.reset!(); names = MailglassDemo.Personas.spec() |> Enum.map(& &1.name) |> Enum.sort(); true = names == ["fjordline-aps","helios-void","northstar"]; tids = MailglassDemo.Repo.all(Ecto.Query.from(d in Mailglass.Delivery, distinct: true, where: not is_nil(d.tenant_id), select: d.tenant_id)) |> Enum.sort(); true = tids == ["fjordline-aps","northstar"]; IO.puts("SEED_OK")' 2>&1 | tail -10; git checkout mix.lock 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - personas.ex compiles; Personas.spec/0 returns 3 persona maps with the D-08 name+edge-case assignments
    - Personas.seed!(Repo) seeds fjordline-aps (1 Delivery, non-ASCII names, long-ID, nil reject_reason, long module name) and helios-void (0 Deliveries); northstar lifecycle preserved
    - DemoData.reset! invokes Personas.seed!(Repo); the inline `mix run` seed assertion prints SEED_OK (spec names == 3 personas; distinct non-null Delivery.tenant_id == ["fjordline-aps","northstar"], helios-void absent)
  </acceptance_criteria>
  <done>The demo app seeds three personas from one declarative spec; northstar + fjordline-aps are deliveries-bearing, helios-void is zero-delivery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test-only path dep + admin-side seed_persona_cohort!/0</name>
  <files>mailglass_admin/mix.exs, mailglass_admin/test/support/operator_fixtures.ex, mailglass_admin/test/mailglass_admin/persona_cohort_test.exs</files>
  <read_first>
    - mailglass_admin/mix.exs (defp deps/0 ~L79 — note the only: [:dev, :test] / runtime: false convention already used for :tailwind ~L99; the no-optional-deps lane compiles bare refs cleanly)
    - mailglass_admin/test/support/operator_fixtures.ex (existing TestRepo materialization patterns to mirror)
    - 116-RESEARCH.md Pitfall 3 + Assumption A1 (path-dep compilability is a MEDIUM risk — verify mix deps.get && mix compile; fall back to a minimal extracted Personas module only if a mailglass/phoenix version conflict surfaces)
    - lib/mailglass/operator/tenants.ex (list_tenants/2 keys off distinct non-null Delivery.tenant_id)
  </read_first>
  <behavior>
    - seed_persona_cohort!/0 materializes MailglassDemo.Personas.spec() into TestRepo, producing the same three personas' Delivery/Event/InboundMessage rows as the demo seed.
    - After seed_persona_cohort!/0, Mailglass.Operator.Tenants.list_tenants/2 (scoped through the authenticated actor, NOT a raw admin Repo query) returns >= 2 rows and includes "northstar" and "fjordline-aps".
    - list_tenants/2 does NOT include "helios-void" (zero Delivery rows).
    - Navigating a helios-void-scoped surface by direct URL renders the empty/permission state (no crash) — assert no 500 / unhandled exception.
  </behavior>
  <action>
    Add the test-only path dep to mailglass_admin/mix.exs deps: a {:mailglass_demo, path: "../reference/demo_app",
    only: [:test], runtime: false} entry (confirm the relative path "../reference/demo_app" is correct from
    mailglass_admin/) per D-06 mechanism 2 — this is the ONLY allowed boundary crossing (CLAUDE.md:
    library lib code never depends on demo; only test support may). Run MIX_ENV=test mix deps.get && mix compile
    to discharge Assumption A1; if a mailglass/phoenix version conflict surfaces, fall back to extracting
    personas.ex as a minimal standalone pure module (no Phoenix deps) and depend on that, per RESEARCH Pitfall 3.

    Add seed_persona_cohort!/0 to operator_fixtures.ex that reads MailglassDemo.Personas.spec() and
    materializes the same three personas into TestRepo (mirror the existing TestRepo insert patterns).
    Author persona_cohort_test.exs asserting the behavior block: 3 personas seeded; list_tenants/2 (scoped
    through the authenticated actor per PHASE112-SHELL-GATE / Tenancy.scope) returns >=2 and includes
    northstar + fjordline-aps; helios-void absent; helios-void direct-URL surface renders the empty state,
    no crash (per 116-UI-SPEC helios-void copy "No deliveries have been recorded yet.").

    Do NOT bend OperatorBrowserServer (it seeds browser-tenant against TestRepo, a different seam — D-10).
    Commit mix.lock ONLY if a clean intentional new-dep entry results; never transitive drift (MEMORY).
  </action>
  <verify>
    <automated>cd mailglass_admin && MIX_ENV=test mix deps.get >/dev/null 2>&1 && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/mailglass_admin/persona_cohort_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test-only path dep compiles in admin test env (A1 discharged) OR documented fallback to extracted module applied
    - persona_cohort_test.exs passes: >=2 selectable tenants incl northstar+fjordline-aps; helios-void absent; helios-void direct URL renders empty state not a crash
  </acceptance_criteria>
  <done>Admin test-support materializes the same cohort into TestRepo; list_tenants shows the two deliveries-bearing personas and excludes the zero-delivery one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persona drift-guard (3 materializers cannot diverge)</name>
  <files>mailglass_admin/test/mailglass_admin/persona_drift_guard_test.exs</files>
  <read_first>
    - 116-CONTEXT.md D-07 (drift-guard intent); 116-RESEARCH.md "The drift-guard (D-07)" (assert demo seed, admin seed_persona_cohort!, and gallery specimen set all cover the same persona names + edge-case value classes; fail closed when a persona is added to the spec but not to a materializer)
    - 116-UI-SPEC.md "Gallery Stress Specimen Values" (the exact non-ASCII / long-ID / long-module-name literals the gallery and persona must share)
  </read_first>
  <behavior>
    - The guard reads MailglassDemo.Personas.spec() as the canonical persona-name + edge-case set.
    - It asserts the admin seed_persona_cohort!/0 materialization produces exactly those tenant_ids and edge-case coverage.
    - It asserts the gallery specimen VALUE classes (non-ASCII literals "Bjørn Hansen"/"山田太郎", the >=26-char long delivery ID, the >=60-char long Mailable module name, the nil reject_reason case) match the fjordline-aps payload literals (so gallery and demo persona stay byte-consistent).
    - Adding a persona to spec() without adding it to a materializer FAILS the guard (fail-closed).
  </behavior>
  <action>
    Author persona_drift_guard_test.exs (D-07). Treat Personas.spec() as the single source of truth and
    assert all three materializers agree: (a) the admin seed_persona_cohort!/0 produces exactly the spec's
    tenant_ids + edge-case assignments; (b) the gallery specimen value literals (read from gallery_live.ex
    or the shared literals — the gallery widening lands in plan 116-04, so assert against the persona-spec
    literals that the gallery must mirror) match the fjordline-aps payload. Make the guard fail closed: a
    persona present in spec() but absent from a materializer, or a literal mismatch, must fail. This defeats
    the triplication-drift failure mode (RESEARCH names it the #1 risk for N-materializer fixtures).

    NOTE on gallery ordering: the gallery rows are widened in plan 116-04 (Wave 2). This guard asserts the
    SHARED literal values are consistent; if the gallery specimen module is not yet present at this plan's
    execution, assert against the canonical spec literals and a documented constant list that 116-04 must
    consume — so 116-04 wiring the gallery to those same literals satisfies the guard.
  </action>
  <verify>
    <automated>cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/persona_drift_guard_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Drift-guard passes when all materializers agree on persona names + edge-case literals
    - Drift-guard fails closed when a persona is added to spec() but not a materializer (assert via a documented negative scenario or by construction)
  </acceptance_criteria>
  <done>A single declarative spec drives three materializers; the drift-guard fails closed on any divergence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin test env → demo app (path dep) | Test-only path dep crosses the lib/demo boundary; must stay `only: [:test], runtime: false` and never leak into prod or the no-optional-deps lane |
| operator context → tenant read model | `list_tenants/2` must be reached through `Tenancy.scope/2` via the authenticated actor, never a raw admin Repo query (PHASE112-SHELL-GATE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | npm/pip/cargo installs | mitigate | No package-manager install in this plan; the only net-new dep (`@axe-core/playwright`) is installed in plan 116-02 with its own legitimacy checkpoint. The `:mailglass_demo` path dep is a local source dep, not a registry fetch. |
| T-116-02 | Information disclosure | persona fixture payloads | accept | Fixtures use fictional recipient names/IDs; no real PII. Telemetry/event metadata stays whitelisted (counts/statuses/IDs), no `:to`/`:from`/`:subject` per Engineering DNA. |
| T-116-03 | Elevation of privilege | helios-void direct-URL | mitigate | Direct navigation to a zero-delivery tenant surface must render the empty/permission state via the scoped read model, not expose another tenant's data or crash — asserted in persona_cohort_test.exs. |
</threat_model>

<verification>
- `cd reference/demo_app && MIX_ENV=dev mix compile --warnings-as-errors` (revert mix.lock drift after)
- `cd mailglass_admin && MIX_ENV=test mix deps.get && mix compile --warnings-as-errors`
- `cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/persona_cohort_test.exs test/mailglass_admin/persona_drift_guard_test.exs`
- Confirm `list_tenants/2` returns exactly northstar + fjordline-aps after the cohort seeds (helios-void absent)
</verification>

<success_criteria>
- Three personas seed from one declarative `Personas.spec()` (RATCHET-01)
- `>=2` selectable tenants (picker has a reason to render); helios-void absent from the switcher
- Admin test-support materializes the same cohort into TestRepo via the test-only path dep
- The drift-guard fails closed on any materializer divergence (D-07)
</success_criteria>

<output>
Create `.planning/phases/116-fixtures-idempotent-ratchet-arm/116-01-SUMMARY.md` when done.
Record: the chosen path-dep vs extracted-module decision (A1 outcome), the exact persona literals
used (so plan 116-04 mirrors them), and whether DemoData.reset! seeds the cohort at harness boot
(input to RATCHET-04 / plan 116-06).
</output>
