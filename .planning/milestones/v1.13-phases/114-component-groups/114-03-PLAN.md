---
phase: 114-component-groups
plan: 03
type: execute
wave: 3
depends_on: ["114-01", "114-02"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex
  - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex
  - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/operator/timeline.ex
  - mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex
  - mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/inbound/timeline.ex
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03]
must_haves:
  truths:
    - "support_cards.ex inner cards are borderless bg-base-100 sunken insets (box prison removed); outer section carries shadow-raised"
    - "All 8 group surfaces render their outer shell through <.card> (data-group-card on each)"
    - "No raw off-grid spacing literals remain in the 8 group files (SPACE-GATE green)"
    - "No same-tone bg-base-200 card-in-card signature remains (GROUP-GATE green)"
    - "Status is never color-alone: any border-l-4 left-rule pairs with the colored count + label"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      provides: "box-prison fix: borderless bg-base-100 insets + shadow-raised outer section"
      contains: "bg-base-100"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "outer shell rendered through <.card> (Components.card/1)"
      pattern: "\\.card"
    - from: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      to: "mailglass_admin/scripts/check-conformance.sh"
      via: "SPACE-GATE + GROUP-GATE now pass against the swept file"
      pattern: "data-group-card"
---

<objective>
Apply the real consistency fixes: demote the `support_cards.ex` box-prison inner cards to borderless
`bg-base-100` sunken insets + `shadow-raised` on the outer section, swap all 8 group-surface shells to
`<.card>` (each carrying `data-group-card`), and sweep the ~93 raw off-grid spacing tokens across the
8 files to the 4px-grid semantic scale. This turns the SPACE-GATE / GROUP-GATE / PRIMITIVE-DRIFT gates
(plan 01) green.

Purpose: D-04/D-05 (single elevation rule; support_cards is the lone box-prison offender — demote inner
cards, raise the outer section) + D-06 (content-weight emphasis; optional status left-rule paired with
the colored count, WCAG 1.4.1) + D-03 (semantic-token sweep, ~93 tokens in 8 files) + D-01 shell swap.
The 4 reference-impl modules already have correct elevation — sweep their spacing ONLY, do NOT touch
their tonal treatment.

Output: 8 group modules rendering through `<.card>` with semantic spacing; box prison removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-component-groups/114-CONTEXT.md
@.planning/phases/114-component-groups/114-RESEARCH.md
@.planning/phases/114-component-groups/114-PATTERNS.md
@.planning/phases/114-component-groups/114-UI-SPEC.md

# The new shell + gates from plan 01, and reference insets to copy from:
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/support_cards.ex
@mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
@mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex
@mailglass_admin/lib/mailglass_admin/operator/timeline.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Box-prison fix + shell swap + sweep in support_cards.ex (the lone offender)</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/support_cards.ex</files>
  <read_first>
    - support_cards.ex:20-23 (outer `<section>` — add shadow-raised) and :38/:84/:130 (the 3 same-tone inner cards) and :22 (the `p-md` outer shell to swap to `<.card>`)
    - support_cards.ex:41/:87/:133 (the existing `text-display font-bold text-{error|warning}` count — keep) and :56/:102/:152 (the one `btn btn-primary` CTA — keep)
    - inbound/evidence_card.ex:56-66 + operator/timeline.ex:45-48 (correct borderless bg-base-100 inset pattern to copy FROM)
    - inbound/routing_trace.ex:62-65 (the `border-l-4 border-error` status left-rule idiom for D-06)
  </read_first>
  <action>
    Fix the lone box-prison offender per D-04/D-05/D-06 (PATTERNS.md "operator/support_cards.ex"):

    (1) Demote each of the 3 inner `<article>` cards (lines 38/84/130, currently
    `card bg-base-200 border border-base-300 rounded-box p-lg` — the same-tone card-in-card) to a
    borderless `bg-base-100` sunken inset: drop `card`, `bg-base-200`, and `border border-base-300`;
    target shape `rounded-box bg-base-100 p-lg` (mirrors evidence_card/timeline insets). This removes
    the GROUP-GATE signature and brings depth to section → inset (≤2).

    (2) Swap the outer shell at :22 (`card rounded-box border border-base-300 bg-base-200 p-md`) to
    `<.card padding={:md} data-group-card data-testid="...">` (preserve the existing data-testid), and
    add `shadow-raised` to the outer `<section>` (:20-23) via `class` so the raised-outer / sunken-inner
    tonal step is unambiguous (D-05). Note `shadow-raised` is NOT in the shell — pass it on the section
    or as a `class` override at this call site (RESEARCH Pattern 1 note).

    (3) D-06 emphasis is already present — KEEP the `text-display font-bold text-{error|warning}` count
    (:41/:87/:133) and the single `btn btn-primary` CTA (:56/:102/:152). OPTIONALLY add the brand Alert
    left-rule `border-l-4 border-error` / `border-l-4 border-warning` to the failure/orphan insets
    (idiom at routing_trace.ex:62-65) — ONLY if hierarchy holds at 320px, and ALWAYS paired with the
    colored count + label (status never color-alone, WCAG 1.4.1). Skip it if it crowds at narrow width.

    (4) Sweep the ~35 raw off-grid spacing tokens in this file to semantic tokens:
    `space-y-1`->`space-y-xs` (:25), `mt-1`->`mt-xs` (:68/:74/:114/:120), `space-y-2`->`space-y-sm`
    (:46/:92/:140), etc. (`gap-sm`/`gap-lg`/`gap-md`/`mt-md`/`px-md`/`mt-sm`/`min-h-11` are already
    correct — leave them). Use the app.css 4px-grid scale (xs..3xl).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c "bg-base-100" lib/mailglass_admin/operator/support_cards.ex && grep -c "shadow-raised" lib/mailglass_admin/operator/support_cards.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 3 inner cards are borderless `bg-base-100` insets (no `bg-base-200`/`border border-base-300`/`card` on them)
    - Outer shell renders through `<.card>` with `data-group-card` + preserved data-testid; outer section has `shadow-raised`
    - The `text-display font-bold` count + single `btn btn-primary` CTA preserved; any left-rule paired with colored count + label
    - No raw off-grid spacing literal remains in this file
    - `mix compile --warnings-as-errors` clean
  </acceptance_criteria>
  <done>Box prison removed; support-cards triage has unambiguous raised-outer / sunken-inner hierarchy with semantic spacing.</done>
</task>

<task type="auto">
  <name>Task 2: Shell swap + sweep in the 7 remaining group surfaces (reference impls + headers/suppression)</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex, mailglass_admin/lib/mailglass_admin/operator/detail_header.ex, mailglass_admin/lib/mailglass_admin/operator/timeline.ex, mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex, mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex, mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex, mailglass_admin/lib/mailglass_admin/inbound/timeline.ex</files>
  <read_first>
    - suppression_card.ex:12, detail_header.ex:17, timeline.ex:18, inbound/routing_trace.ex:35, inbound/evidence_card.ex:33, inbound/detail_header.ex:32, inbound/timeline.ex:20 (the 7 hand-rolled `card rounded-box border border-base-300 bg-base-200 p-6` shells to swap)
    - inbound/evidence_card.ex:56-66, inbound/routing_trace.ex:49-52, operator/timeline.ex:45-48, inbound/timeline.ex:43 (already-correct bg-base-100 insets — PRESERVE verbatim, sweep spacing only)
    - 114-PATTERNS.md sections for suppression/detail_header and the 4 reference impls + the sweep token map
  </read_first>
  <action>
    For each of the 7 remaining group surfaces, per D-01/D-03:

    (1) Swap the outer hand-rolled shell `<article class="card rounded-box border border-base-300
    bg-base-200 p-6">` for `<.card padding={:lg} data-group-card data-testid="...">` (use `:lg` to
    match the current `p-6`≈24px; preserve the existing data-testid through `@rest`). Keep the
    semantic element where one is needed by passing it via the call site, or let `<.card>`'s `<div>`
    root carry the testid/data-group-card — both work (RESEARCH Open Question 1).

    (2) For the 4 REFERENCE impls (operator/timeline.ex, inbound/timeline.ex, inbound/routing_trace.ex,
    inbound/evidence_card.ex): do NOT change their tonal/elevation treatment — their inner
    `bg-base-100` insets are already correct (PATTERNS.md "Already-correct insets to preserve verbatim").
    Sweep ONLY their numeric spacing.

    (3) Sweep all raw off-grid spacing tokens to semantic tokens across the 7 files (~58 tokens total):
    `p-4`->`p-md`, `px-2 py-1`->`px-sm py-xs`, `mb-4`->`mb-md`, `mb-3`->`mb-sm`,
    `space-y-1/2/3/4`->`space-y-xs/sm/md/...`, `gap-2`->`gap-sm`, `mt-1`->`mt-xs`, `mt-2`->`mt-sm`,
    `p-3`->`p-sm`, `px-4`->`px-md`, `px-3`->`px-sm`, `pt-4`->`pt-md`, `mt-6`->`mt-lg`, `mt-4`->`mt-md`
    (per the RESEARCH Pitfall 2 inventory). `gap-2` is currently NOT caught by GAP-GATE — the new
    SPACE-GATE closes it, so it MUST be swept. Do NOT touch sizing tokens (`min-h-11`, `h-3`,
    `border-l-4`, `w-px`) or the legitimate `mt-0.5` icon-alignment uses — those are not spacing.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3 && grep -rc "data-group-card" lib/mailglass_admin/operator/suppression_card.ex lib/mailglass_admin/inbound/routing_trace.ex</automated>
  </verify>
  <acceptance_criteria>
    - All 7 shells render through `<.card>` with `data-group-card` + preserved data-testid
    - The 4 reference impls' `bg-base-100` insets are unchanged (tonal treatment preserved)
    - All raw off-grid spacing literals swept to semantic tokens across the 7 files (incl. `gap-2`)
    - No sizing token (`min-h-11`/`h-3`/`border-l-4`) or `mt-0.5` altered
    - `mix compile --warnings-as-errors` clean
  </acceptance_criteria>
  <done>All 8 group surfaces share the single `<.card>` shell with semantic spacing; reference elevation preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Green the gates + confirm CSS bundle clean</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - 114-RESEARCH.md Runtime State Inventory (bundle rebuild likely a no-op — shadow-raised + spacing tokens already exist; rebuild ONLY if a new @theme/@utility line was added)
    - CLAUDE.md "Things Not To Do" #6 (don't write priv/static without committing rebuilt bundle)
    - Project MEMORY: validate gates by running, not grep proofs
  </read_first>
  <action>
    Confirm the discipline is enforced and the bundle is clean (no permanent code change here unless a
    gate exposes a missed token):

    (1) Run `bash scripts/check-conformance.sh` and confirm SPACE-GATE, GROUP-GATE, and
    PRIMITIVE-DRIFT-GATE (`card`) are ALL green now that the 8 files are swept/fixed. If SPACE-GATE
    still fails, a spacing token was missed — return to Task 1/2 and sweep it (a post-sweep grep in the
    8 files must return no spacing numerics, RESEARCH Pitfall 2 "Warning signs").

    (2) Confirm the committed CSS bundle is clean: run `mix verify.preview` and `git diff --exit-code
    priv/static/app.css`. Per RESEARCH, no new CSS utility is expected (shadow-raised + `--spacing-*`
    already exist app.css:110-116,132), so this should be a no-op. ONLY if the executor added a new
    `@theme`/`@utility` line (e.g. a left-rule utility — but `border-l-4`/`border-error` are stock,
    not new) must the bundle be rebuilt and committed (CLAUDE.md #6).

    (3) Run the scoped admin tests for the touched group modules to confirm no regression
    (`mix test test/mailglass_admin/operator/`). Per MEMORY, do NOT run bare `mix test` as the
    pass/fail gate (~57 unrelated Oban failures + voice_test "Oops" dep-JS false-positive in worktrees).
  </action>
  <verify>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh && echo "conformance OK" && git diff --exit-code priv/static/app.css && echo "bundle clean"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-conformance.sh` exits 0 (SPACE-GATE + GROUP-GATE + PRIMITIVE-DRIFT all green)
    - A post-sweep grep in the 8 group files returns no raw off-grid spacing numerics
    - `git diff --exit-code priv/static/app.css` clean (bundle unchanged, or rebuilt+committed if a utility was added)
    - Scoped operator group tests pass
  </acceptance_criteria>
  <done>All conformance gates green; CSS bundle clean; the consistency defect is enforced shut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-class / elevation / spacing swaps on existing presentational components. No new route, input, auth surface, data flow, or destructive action. The existing replay confirmation modal is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-05 | Information Disclosure | routing_trace.ex / inbound detail_header.ex PII masking | mitigate | Only spacing/elevation classes change; the existing `Components.mask_recipient/1` data paths are untouched. No PII added to test attributes. |
| T-114-06 | Tampering | scope fence (D-12) | mitigate | Only the 8 named group modules touched; deliveries/inbound lists, overview/stat-card strips, nav/shell, and preview are NOT touched. |
| T-114-07 | Tampering | committed CSS bundle | mitigate | `git diff --exit-code priv/static/app.css` enforces no stale/uncommitted bundle (CLAUDE.md #6). |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero-Node, no new Hex dep — D-11). |
</threat_model>

<verification>
- `cd mailglass_admin && bash scripts/check-conformance.sh` exits 0 (all gates green)
- `cd mailglass_admin && git diff --exit-code priv/static/app.css` clean
- `cd mailglass_admin && mix compile --warnings-as-errors` clean
- `cd mailglass_admin && mix test test/mailglass_admin/operator/` passes
- Scope fence (D-12) holds — only the 8 group modules touched
</verification>

<success_criteria>
- support_cards box prison removed (borderless bg-base-100 insets + shadow-raised outer section)
- All 8 group surfaces render through `<.card>` with `data-group-card`; ~93 tokens swept to semantic scale
- SPACE-GATE + GROUP-GATE + PRIMITIVE-DRIFT all green; CSS bundle clean
- Status never color-alone (any left-rule paired with colored count + label)
</success_criteria>

<output>
Create `.planning/phases/114-component-groups/114-03-SUMMARY.md` when done
</output>
