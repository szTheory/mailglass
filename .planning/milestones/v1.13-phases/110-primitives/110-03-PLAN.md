---
phase: 110-primitives
plan: "03"
type: execute
wave: 3
depends_on:
  - "110-02"
files_modified:
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - PRIM-01
  - PRIM-02
  - PRIM-03
  - PRIM-04
  - PRIM-05
must_haves:
  truths:
    - "D-03: `GalleryLive` renders real public primitive components, not gallery-only copies."
    - "Every named primitive has gallery specimens or explicit state-matrix decisions for active/selected, inactive, hover-ready, focus-visible, disabled where applicable, loading where applicable, explicit loading non-applicability where not applicable, error/unavailable or empty where applicable, and long-content states."
    - "Light, dark, and system specimens are present for each gallery primitive state; the system specimen omits an explicit theme attribute."
    - "Gallery test ids remain stable as `gallery-{component}-{state}` and add states without renaming existing anchors unnecessarily."
    - "The gallery uses `theme_picker`, not the old binary theme toggle."
  prohibitions:
    - statement: "No gallery-only component API is introduced for primitives already owned by `MailglassAdmin.Components`."
      status: active
      verification: "Plan 110-04 PRIMITIVE-DRIFT-GATE plus gallery source scan."
    - statement: "No explicit system theme attribute is rendered by the gallery system specimen."
      status: active
      verification: "Plan 110-04 structural system wrapper proof."
    - statement: "No PhoenixStorybook, registry block, or new UI dependency is introduced."
      status: active
      verification: "Package manifest diff and source scan."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      provides: "Real-component primitive state gallery"
      contains: "Components.theme_picker"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt committed CSS bundle for gallery primitive state classes"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "render_specimen dispatchers call public Components primitives"
      pattern: "Components\\.(nav_link|nav_pill|tenant_chip|theme_picker|stat_card)"
    - from: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      to: "mailglass_admin/e2e/structural.spec.js"
      via: "stable gallery data-testid anchors consumed by Playwright"
      pattern: "gallery-[a-z_]+-[a-z0-9-]+"
---

<objective>
Make the component gallery certify the same primitive components used by the shell and overview surfaces.

Purpose: The gallery is the primitive stress surface. If it renders copied HEEx, it can pass while the real shell drifts. This plan replaces copied primitive specimens with public component calls and widens state/theme coverage before hard browser assertions land.
Output: `GalleryLive` renders public primitive components across expanded states and light/dark/system wrappers, with the CSS bundle rebuilt if class scanning changes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-primitives/110-CONTEXT.md
@.planning/phases/110-primitives/110-RESEARCH.md
@.planning/phases/110-primitives/110-VALIDATION.md
@.planning/phases/110-primitives/110-UI-SPEC.md
@.planning/phases/110-primitives/110-01-SUMMARY.md
@.planning/phases/110-primitives/110-02-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/assets/css/app.css
@mailglass_admin/mix.exs
</context>

## Multi-Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
ROADMAP | goal | Shared atoms are single public components; states/themes/viewports meet WCAG/APG; `stat_card` and tri-state picker exist | 110-01..110-04 | COVERED | API, consumers, gallery, gates, structural proof.
ROADMAP | SC-1 | Gallery-inlined atoms promoted and copy-drift gate passes | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan removes gallery copies.
ROADMAP | SC-2 | Primitive states in light/dark/system meet WCAG 2.2 AA and APG | 110-01, 110-03, 110-04 | COVERED | This plan creates state/theme specimens.
ROADMAP | SC-3 | Canonical `stat_card` shape enforced | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan adds stat-card gallery states.
ROADMAP | SC-4 | Three-way theme picker and compiled-bundle 44x44 proof | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan adds picker gallery states.
ROADMAP | SC-5 | Every icon exists and is not sole meaning | 110-01, 110-04 | COVERED | Later gate covers inventory.
REQUIREMENTS | PRIM-01 | Promote gallery atoms to public components with no copy drift | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan updates gallery rendering.
REQUIREMENTS | PRIM-02 | Render every primitive interaction state in light/dark/system at 320->wide | 110-01, 110-03, 110-04 | COVERED | This plan adds the specimens.
REQUIREMENTS | PRIM-03 | Disabled controls are visually and programmatically distinct | 110-01, 110-03, 110-04 | COVERED | This plan adds disabled specimens.
REQUIREMENTS | PRIM-04 | Canonical `stat_card` primitive | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan adds stat-card gallery states.
REQUIREMENTS | PRIM-05 | Three-way system/light/dark theme picker | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan adds picker state rows.
REQUIREMENTS | PRIM-06 | 44x44 default target proof; 24px exceptions gated or recorded | 110-04 | COVERED | Later structural proof measures this gallery output.
REQUIREMENTS | PRIM-07 | Icon-exists guard and non-icon-alone meaning | 110-01, 110-04 | COVERED | Later gate covers inventory.
RESEARCH | R-01 | Gallery must certify real shell/public components | 110-03, 110-04 | COVERED | This plan changes dispatchers.
RESEARCH | R-02 | State coverage belongs in `GalleryLive`, not parallel API | 110-03 | COVERED | Specimen rows call public components.
RESEARCH | R-03 | System means no explicit theme value | 110-03, 110-04 | COVERED | System wrapper omits theme attr.
RESEARCH | R-04 | Structural tests consume stable gallery anchors | 110-03, 110-04 | COVERED | Test ids preserved and expanded.
RESEARCH | R-05 | Rebuild bundle after class changes | 110-03 | COVERED | Asset build included.
CONTEXT | D-01 | Public primitive ownership | 110-01, 110-02, 110-03 | COVERED | Gallery consumes public functions.
CONTEXT | D-02 | Copy-drift guard | 110-04 | COVERED | This plan prepares clean source for the gate.
CONTEXT | D-03 | Gallery public component state coverage | 110-03, 110-04 | COVERED | Primary plan objective.
CONTEXT | D-04 | Theme choices and system absence of explicit value | 110-01, 110-02, 110-03, 110-04 | COVERED | System wrapper and picker state.
CONTEXT | D-05 | Radio group, not independent pressed buttons | 110-01, 110-03, 110-04 | COVERED | Gallery renders public radio primitive.
CONTEXT | D-06 | No persistence/no-FOUC/root ownership | 110-02, 110-03, 110-04 | COVERED | Gallery only renders specimens.
CONTEXT | D-07 | Add and migrate `stat_card` | 110-01, 110-02, 110-03 | COVERED | Gallery state rows added.
CONTEXT | D-08 | `stat_card` locked shape | 110-01, 110-02, 110-03, 110-04 | COVERED | Gallery includes long/empty/unavailable states.
CONTEXT | D-09 | STATCARD gate | 110-04 | COVERED | Later plan.
CONTEXT | D-10 | Icon-exists guard | 110-04 | COVERED | Later plan.
CONTEXT | D-11 | Icons never carry meaning alone | 110-01, 110-03, 110-04 | COVERED | Gallery labels remain visible.
CONTEXT | D-12 | WCAG 24px minimum gate/exception | 110-04 | COVERED | Later proof.
CONTEXT | D-13 | 44x44 default target floor | 110-01, 110-03, 110-04 | COVERED | Specimens expose measured targets.
UI-SPEC | UI-01 | Real-component gallery rendering | 110-03 | COVERED | This plan.
UI-SPEC | UI-02 | Stable test ids | 110-03, 110-04 | COVERED | Preserve and add anchors.
UI-SPEC | UI-03 | Light/dark/system coverage | 110-03, 110-04 | COVERED | Three wrappers.
UI-SPEC | UI-04 | Interaction states | 110-03, 110-04 | COVERED | State rows.
UI-SPEC | UI-05 | Disabled distinction | 110-03, 110-04 | COVERED | Disabled state rows and later checks.
VALIDATION | V-01 | Component tests | 110-01 | COVERED | Prior plan.
VALIDATION | V-02 | Gallery public component coverage | 110-03, 110-04 | COVERED | This plan and browser proof.
VALIDATION | V-03 | System/theme structural proof | 110-03, 110-04 | COVERED | System wrapper and tests.
VALIDATION | V-04 | Target-size and overflow proof | 110-04 | COVERED | Later plan.
VALIDATION | V-05 | Bundle-clean proof | 110-03, 110-04 | COVERED | Asset build and final validation.

<tasks>

<task id="110-03-01" type="auto" files="mailglass_admin/lib/mailglass_admin/gallery_live.ex">
  <name>Task 1: Render public primitives in light, dark, and system gallery wrappers</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/gallery_live.ex
    mailglass_admin/lib/mailglass_admin/components.ex
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-RESEARCH.md
    .planning/phases/110-primitives/110-UI-SPEC.md
    .planning/phases/110-primitives/110-VALIDATION.md
  </read_first>
  <action>Update `MailglassAdmin.GalleryLive` so primitive specimens render `Components.nav_link/1`, `Components.nav_pill/1`, `Components.tenant_chip/1`, `Components.theme_picker/1`, and `Components.stat_card/1` directly per D-01 and D-03. Remove the inlined HEEx bodies for `nav_link`, `nav_pill`, `tenant_chip`, and the old binary theme toggle. Rename the gallery component identity from `:theme_toggle` to `:theme_picker` throughout `@specimens`, `component_label/1`, comments, and render dispatch. In the gallery cell layout, keep the existing light and dark wrappers and add a third system wrapper for every specimen. The system wrapper must have a stable test id such as `gallery-#{component}-#{state}-system`, must not set an explicit theme attribute, and must render the same public component call as light/dark. Preserve existing `data-testid="gallery-{component}-{state}"` cell anchors.</action>
  <acceptance_criteria>
    - `gallery_live.ex` dispatchers for `:nav_link`, `:nav_pill`, `:tenant_chip`, `:theme_picker`, and `:stat_card` call `Components.*` functions directly.
    - `gallery_live.ex` no longer contains copied shell HEEx for nav links, nav pills, tenant chips, or the binary theme toggle.
    - `gallery_live.ex` contains no `:theme_toggle` specimen or `component_label(:theme_toggle)` clause.
    - Every specimen cell still has `data-testid={"gallery-#{component}-#{state}"}`.
    - Every specimen cell renders light, dark, and system wrappers; the system wrapper has no explicit theme attribute.
    - `GalleryLive` still compiles without warnings.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Components\\.(nav_link|nav_pill|tenant_chip|theme_picker|stat_card)' mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    <automated>! rg -n 'theme_toggle|defp render_specimen\\(%\\{component: :theme_toggle\\}' mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <done>The gallery renders the public primitive functions and provides light, dark, and system wrappers for every specimen cell.</done>
</task>

<task id="110-03-02" type="auto" files="mailglass_admin/lib/mailglass_admin/gallery_live.ex, mailglass_admin/priv/static/app.css">
  <name>Task 2: Add primitive state specimens and rebuild bundle</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex, mailglass_admin/priv/static/app.css</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/gallery_live.ex
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/e2e/structural.spec.js
    mailglass_admin/mix.exs
    .planning/phases/110-primitives/110-UI-SPEC.md
    .planning/phases/110-primitives/110-VALIDATION.md
  </read_first>
  <action>Expand `@specimens` with public primitive states per D-03. For `nav_link`, include `active`, `inactive`, `hover-ready`, `focus-visible`, `disabled`, and `long-label`; loading is explicitly not applicable because the primitive is immediate navigation and disabled/not-available covers unavailable navigation. For `nav_pill`, include `active`, `inactive`, `hover-ready`, `focus-visible`, `disabled`, and `long-label`; loading is explicitly not applicable for the same immediate-navigation reason. For `tenant_chip`, include `with-tenant`, `no-tenant`, `long-tenant`, and `non-ascii-tenant`; hover, focus, disabled, and loading are explicitly not applicable because Phase 110 keeps it read-only and non-interactive. For `theme_picker`, include `system-selected`, `light-selected`, `dark-selected`, `hover-ready`, `focus-visible`, and `disabled`; loading is explicitly not applicable because Phase 110 provides only the primitive and Phase 112 owns async persistence/no-FOUC wiring per D-06. For `stat_card`, include `neutral`, `info`, `success`, `warning`, `error`, `empty`, `loading`, `unavailable`, `long-label`, and `long-value`; hover and focus are explicitly not applicable unless a future consumer makes a card actionable. Use visible labels from the UI-SPEC copy contract: `System`, `Light`, `Dark`, `No tenant selected`, `No data yet`, `Loading`, `Unavailable`, `All clear`, and `Needs attention` where those states apply. Keep icons adjacent to visible text. Rebuild the CSS bundle with `mix mailglass_admin.assets.build` and commit `mailglass_admin/priv/static/app.css` if Tailwind emits a delta.</action>
  <acceptance_criteria>
    - `gallery_live.ex` contains the listed state rows for `nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, and `stat_card`, including hover-ready and focus-visible rows for interactive primitives.
    - `gallery_live.ex` contains a `stat_card` loading row and records loading as not applicable for `nav_link`, `nav_pill`, `tenant_chip`, and `theme_picker` in the state matrix or adjacent specimen metadata.
    - The old `theme_toggle` gallery states `light-mode` and `dark-mode` are replaced by `theme_picker` selected states.
    - `stat_card` gallery states include all five severity atoms plus empty, loading, unavailable, long-label, and long-value cases.
    - Disabled nav and picker specimens pass disabled assigns into the public components.
    - Long tenant and stat label/value specimens include full text available through `title` in rendered public components.
    - `cd mailglass_admin && mix mailglass_admin.assets.build` exits 0, and `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after committing any bundle delta.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'nav_link.*hover-ready|nav_link.*focus-visible|nav_pill.*hover-ready|nav_pill.*focus-visible|theme_picker.*focus-visible|stat_card.*loading|loading.*not applicable' mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    <automated>rg -n 'nav_link.*disabled|nav_pill.*disabled|tenant_chip.*long-tenant|theme_picker.*system-selected|theme_picker.*dark-selected|stat_card.*long-value|stat_card.*unavailable' mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    <automated>cd mailglass_admin && mix mailglass_admin.assets.build</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/</automated>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "gallery"</automated>
  </verify>
  <done>The gallery exposes real public primitive state coverage across light, dark, and system specimens, and the committed CSS bundle is synchronized.</done>
</task>

</tasks>

## Artifacts this phase produces

- Public component `MailglassAdmin.Components.nav_link/1`.
- Public component `MailglassAdmin.Components.nav_pill/1`.
- Public component `MailglassAdmin.Components.tenant_chip/1`.
- Public component `MailglassAdmin.Components.theme_picker/1`.
- Public component `MailglassAdmin.Components.stat_card/1`.
- Component contract tests in `mailglass_admin/test/mailglass_admin/components_test.exs`.
- Shell consumers of `Components.nav_link/1`, `Components.nav_pill/1`, `Components.tenant_chip/1`, and `Components.theme_picker/1`.
- Public shell helpers `MailglassAdmin.Operator.Shell.theme_choice/1` and `MailglassAdmin.Operator.Shell.set_theme_path/2`.
- LiveView event `set_theme` in `MailglassAdmin.OperatorLive` and `MailglassAdmin.InboundLive`.
- Operator and inbound overview consumers of `Components.stat_card/1`.
- Gallery specimens for public primitives and states under `gallery-nav_link-*`, `gallery-nav_pill-*`, `gallery-tenant_chip-*`, `gallery-theme_picker-*`, and `gallery-stat_card-*`.
- Light wrapper with `data-theme="mailglass-light"`, dark wrapper with `data-theme="mailglass-dark"`, and system wrapper with no explicit theme attribute for gallery specimen cells.
- `PRIMITIVE-DRIFT-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `STATCARD-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `ICON-EXISTS-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- Playwright structural assertions for primitive gallery state coverage, system wrapper absence of explicit theme, radio semantics, disabled distinction, 44px target sizes, icon meaning, and stat-card overflow/no-wrap behavior.
- Rebuilt `mailglass_admin/priv/static/app.css` when source class strings change.
- `.planning/phases/110-primitives/110-01-SUMMARY.md`.
- `.planning/phases/110-primitives/110-02-SUMMARY.md`.
- `.planning/phases/110-primitives/110-03-SUMMARY.md`.
- `.planning/phases/110-primitives/110-04-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| gallery specimens -> real component behavior | Gallery can mislead verification if it renders copied markup rather than public components. |
| system specimen -> root/theme behavior | System coverage must prove absence of explicit choice without changing root behavior. |
| state fixture data -> rendered long content | Long IDs and labels can overflow or hide meaning if specimens are not realistic. |

## STRIDE Threat Register

Execution must stop on any HIGH or CRITICAL threat whose status is `open`. The executor must either complete the mitigation and rerun the listed verification, or create a blocking checkpoint that records why the threat cannot be closed.

| Threat ID | STRIDE | ASVS L1 Mapping | Severity | Status | Component | Mitigation / Verification |
|---|---|---|---|---|---|---|
| T-110-09 | Tampering | V14 Configuration | HIGH | planned | `GalleryLive` primitive dispatch | Render public `Components.*` functions directly and remove copied shell HEEx. Verification: source scan and Plan 110-04 drift gate. |
| T-110-10 | Spoofing | V5 Input Validation | HIGH | planned | disabled gallery states | Include disabled states that expose programmatic disabled markers from public components. Verification: Plan 110-04 structural disabled checks. |
| T-110-11 | Tampering | V14 Configuration | MEDIUM | planned | system wrapper | The system wrapper omits explicit theme attributes. Verification: source scan and Playwright no-theme assertion. |
| T-110-12 | Denial of Service | V5 Input Validation | MEDIUM | planned | long-content specimens | Add long tenant/stat specimens so overflow defects are measurable. Verification: Plan 110-04 stat-card overflow checks. |
| T-110-SC | Tampering | V14 Configuration | LOW | accepted | package installs | No package-manager install is part of this plan. Verification: final package manifest diff remains empty. |
</threat_model>

<verification>
Plan-level verification:
- `cd mailglass_admin && mix compile --warnings-as-errors`
- `cd mailglass_admin && mix mailglass_admin.assets.build`
- `cd mailglass_admin && git diff --exit-code priv/static/`
- `cd mailglass_admin && npm run test:operator-browser -- --grep "gallery"`
- `bash mailglass_admin/scripts/check-conformance.sh`
</verification>

<success_criteria>
Plan 110-03 is complete when `GalleryLive` uses public primitive components for the named atoms, includes expanded primitive state rows, renders light/dark/system wrappers for each cell with no explicit system theme attribute, keeps gallery anchors stable, and leaves the static CSS bundle clean.
</success_criteria>

<output>
Create `.planning/phases/110-primitives/110-03-SUMMARY.md` when done.
</output>
