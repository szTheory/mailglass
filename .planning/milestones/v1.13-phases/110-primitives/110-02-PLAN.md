---
phase: 110-primitives
plan: "02"
type: execute
wave: 2
depends_on:
  - "110-01"
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound/overview.ex
  - mailglass_admin/test/mailglass_admin/operator/shell_test.exs
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - PRIM-01
  - PRIM-03
  - PRIM-04
  - PRIM-05
must_haves:
  truths:
    - "D-01: The operator shell renders public `MailglassAdmin.Components` primitives instead of private shell copies."
    - "D-04: Shell theme selection maps `system` to absence of an explicit theme query value."
    - "D-05: The shell renders the three-choice theme picker as the public radio primitive, not as independent pressed buttons."
    - "D-04 and D-05: The shell consumes the Plan 110-01 `theme_picker/1` event API; it passes `set_theme` through the public component attrs and relies on component-rendered per-option `phx-value-theme` values without editing `components.ex` in this plan."
    - "D-07: Operator overview and inbound overview KPI/stat cards render `Components.stat_card/1`."
    - "D-08: Overview stat values are no-wrap tabular values and runtime-missing states use meaningful copy."
  prohibitions:
    - statement: "No host-scoped cookie, local storage, match-media script, LiveView hook, or first-paint no-FOUC behavior is added in this plan."
      status: active
      verification: "Source scan in Plan 110-04."
    - statement: "No tenant listing, tenant switching, or tenant auto-select behavior is added in this plan."
      status: active
      verification: "Diff remains limited to primitive consumers and tests."
    - statement: "No page-local stat helper remains in operator or inbound overview after migration."
      status: active
      verification: "STATCARD-GATE in Plan 110-04."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      provides: "Public primitive shell consumers and theme choice/path helpers"
      exports: ["theme_choice", "set_theme_path"]
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Operator theme picker event handling and stat-card consumers"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Inbound theme picker event handling"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/overview.ex"
      provides: "Inbound overview stat-card consumers"
    - path: "mailglass_admin/test/mailglass_admin/operator/shell_test.exs"
      provides: "Shell primitive consumer and theme path regression tests"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "direct `Components.*` component calls"
      pattern: "Components\\.(nav_link|nav_pill|tenant_chip|theme_picker)"
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      via: "`set_theme` event calls `Shell.set_theme_path/2`"
      pattern: "set_theme_path"
    - from: "mailglass_admin/lib/mailglass_admin/inbound/overview.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "`Components.stat_card/1`"
      pattern: "Components\\.stat_card"
---

<objective>
Migrate the real shell and overview surfaces to the public primitive API.

Purpose: A primitive does not prevent copy drift until real consumers call it. This plan removes the private shell atoms, wires the theme picker through existing URL patch semantics without taking over persistence, and moves current KPI cards onto `Components.stat_card/1`.
Output: Shell and overview call sites use public primitives, theme values route through deterministic helper functions, and focused shell tests cover the consumer seam.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-primitives/110-CONTEXT.md
@.planning/phases/110-primitives/110-RESEARCH.md
@.planning/phases/110-primitives/110-VALIDATION.md
@.planning/phases/110-primitives/110-UI-SPEC.md
@.planning/phases/109-foundations-gate-tightening/109-02-SUMMARY.md
@.planning/phases/109-foundations-gate-tightening/109-04-SUMMARY.md
@.planning/phases/110-primitives/110-01-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/inbound_live.ex
@mailglass_admin/lib/mailglass_admin/inbound/overview.ex
@mailglass_admin/test/mailglass_admin/operator/shell_test.exs
</context>

## Multi-Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
ROADMAP | goal | Shared atoms are single public components; states/themes/viewports meet WCAG/APG; `stat_card` and tri-state picker exist | 110-01..110-04 | COVERED | API, consumers, gallery, gates, structural proof.
ROADMAP | SC-1 | Gallery-inlined atoms promoted and copy-drift gate passes | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan removes shell copies.
ROADMAP | SC-2 | Primitive states in light/dark/system meet WCAG 2.2 AA and APG | 110-01, 110-03, 110-04 | COVERED | Consumer markup preserves public component semantics.
ROADMAP | SC-3 | Canonical `stat_card` shape enforced | 110-01, 110-02, 110-04 | COVERED | This plan migrates operator/inbound overview stats.
ROADMAP | SC-4 | Three-way theme picker and compiled-bundle 44x44 proof | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan renders it in the shell.
ROADMAP | SC-5 | Every icon exists and is not sole meaning | 110-01, 110-04 | COVERED | Shell icons remain adjacent to labels.
REQUIREMENTS | PRIM-01 | Promote gallery atoms to public components with no copy drift | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan migrates shell call sites.
REQUIREMENTS | PRIM-02 | Render every primitive interaction state in light/dark/system at 320->wide | 110-01, 110-03, 110-04 | COVERED | Gallery/browser plans own full matrix.
REQUIREMENTS | PRIM-03 | Disabled controls are visually and programmatically distinct | 110-01, 110-03, 110-04 | COVERED | This plan keeps public disabled contract available to shell/gallery.
REQUIREMENTS | PRIM-04 | Canonical `stat_card` primitive | 110-01, 110-02, 110-04 | COVERED | This plan migrates stat consumers.
REQUIREMENTS | PRIM-05 | Three-way system/light/dark theme picker | 110-01, 110-02, 110-03, 110-04 | COVERED | This plan adds shell rendering and URL value mapping.
REQUIREMENTS | PRIM-06 | 44x44 default target proof; 24px exceptions gated or recorded | 110-04 | COVERED | Later structural proof uses this plan's shell output.
REQUIREMENTS | PRIM-07 | Icon-exists guard and non-icon-alone meaning | 110-01, 110-04 | COVERED | Later gate covers inventory.
RESEARCH | R-01 | Move primitives to `MailglassAdmin.Components` before migrating consumers | 110-01, 110-02 | COVERED | Dependency enforces order.
RESEARCH | R-02 | Theme picker must be radio semantics | 110-01, 110-02, 110-04 | COVERED | Shell renders the public radio primitive.
RESEARCH | R-03 | `stat_card` cannot remain page-local | 110-02, 110-04 | COVERED | This plan removes page-local stat markup.
RESEARCH | R-04 | Avoid theme persistence scope creep | 110-02, 110-04 | COVERED | This plan only maps event values to URL patches.
RESEARCH | R-05 | Rebuild committed CSS after class changes | 110-02 | COVERED | Bundle verification included.
CONTEXT | D-01 | Public primitive ownership in `MailglassAdmin.Components` | 110-01, 110-02, 110-03 | COVERED | This plan consumes public functions.
CONTEXT | D-02 | Concrete copy-drift guard | 110-04 | COVERED | This plan makes gate green by removing copies.
CONTEXT | D-03 | Gallery renders real public components and named states | 110-03, 110-04 | COVERED | Not modified here.
CONTEXT | D-04 | Theme choices and system absence of explicit value | 110-01, 110-02, 110-04 | COVERED | `Shell.set_theme_path/2`.
CONTEXT | D-05 | Radio group, not independent pressed buttons | 110-01, 110-02, 110-04 | COVERED | Shell renders `Components.theme_picker/1`.
CONTEXT | D-06 | Primitive only; no persistence/no-FOUC/root ownership | 110-02, 110-04 | COVERED | Scope limited to event/path helper.
CONTEXT | D-07 | Add `stat_card` and migrate overviews | 110-01, 110-02 | COVERED | This plan completes migration.
CONTEXT | D-08 | `stat_card` locked shape | 110-01, 110-02, 110-04 | COVERED | This plan uses component for all overview stats.
CONTEXT | D-09 | STATCARD gate | 110-04 | COVERED | Later plan.
CONTEXT | D-10 | Icon inventory guard | 110-04 | COVERED | Later plan.
CONTEXT | D-11 | Icons never carry meaning alone | 110-01, 110-02, 110-04 | COVERED | Consumer labels preserved.
CONTEXT | D-12 | WCAG target-size minimum | 110-04 | COVERED | Later proof.
CONTEXT | D-13 | 44x44 default target floor | 110-01, 110-04 | COVERED | Component class contract and browser proof.
UI-SPEC | UI-01 | Public ownership and consumer rule | 110-01, 110-02, 110-03 | COVERED | This plan migrates real consumers.
UI-SPEC | UI-02 | System theme boundary | 110-02, 110-04 | COVERED | No persistence/root changes.
UI-SPEC | UI-03 | Stat-card consumers | 110-02 | COVERED | Operator/inbound overview migration.
UI-SPEC | UI-04 | Icon and severity non-color-alone rule | 110-01, 110-02, 110-04 | COVERED | `stat_card` severity visible text.
UI-SPEC | UI-05 | Bundle discipline | 110-02 | COVERED | Asset build included.
VALIDATION | V-01 | Component tests | 110-01 | COVERED | Prior plan.
VALIDATION | V-02 | Shell no-copy proof | 110-02, 110-04 | COVERED | This plan removes copies; later gate enforces.
VALIDATION | V-03 | Stat-card focused proof | 110-02, 110-04 | COVERED | Migration plus gate/browser proof.
VALIDATION | V-04 | Theme picker semantic proof | 110-01, 110-02, 110-04 | COVERED | Consumer and structural tests.
VALIDATION | V-05 | Bundle-clean proof | 110-02, 110-04 | COVERED | Asset build and final validation.

<tasks>

<task id="110-02-01" type="auto" files="mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/test/mailglass_admin/operator/shell_test.exs">
  <name>Task 1: Replace private shell atoms with public component calls</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/test/mailglass_admin/operator/shell_test.exs</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/test/mailglass_admin/operator/shell_test.exs
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-UI-SPEC.md
    .planning/phases/110-primitives/110-VALIDATION.md
  </read_first>
  <action>In `MailglassAdmin.Operator.Shell`, replace private `<.nav_link>`, `<.nav_pill>`, `<.tenant_chip>`, and `<.theme_toggle>` usage with direct `Components.nav_link`, `Components.nav_pill`, `Components.tenant_chip`, and `Components.theme_picker` calls per D-01. Remove private `defp nav_link/1`, `defp nav_pill/1`, `defp tenant_chip/1`, and `defp theme_toggle/1` from `shell.ex`. Add public helper `theme_choice/1` that returns `:dark` for `"dark"` or `"mailglass-dark"`, `:light` for `"light"` or `"mailglass-light"`, and `:system` when no explicit theme choice is present. Add public helper `set_theme_path/2` that takes the current URI and a value `"system"`, `"light"`, or `"dark"`; `"system"` deletes the theme query key, `"light"` writes `theme=light`, and `"dark"` writes `theme=dark`, preserving all unrelated query keys. Add a `:theme_choice` attr to `shell/1`, pass it to `Components.theme_picker/1`, and configure the picker through the Plan 110-01 public component API by passing the selected-value event attr as `set_theme`; the component, not this plan, owns rendering `phx-click="set_theme"` and the per-option `phx-value-theme` values. Do not edit `mailglass_admin/lib/mailglass_admin/components.ex` in this plan; if the Plan 110-01 event/value API is missing, stop and fix Plan 110-01 rather than adding ad hoc theme-picker wiring in shell. Update `ShellTest` to assert active nav still marks only the current surface, the shell renders the theme radio labels `System`, `Light`, and `Dark`, rendered shell output includes `phx-click="set_theme"` plus per-option `phx-value-theme` values from the public component, and `set_theme_path/2` preserves tenant/filter params while mapping system to no explicit theme query value per D-04 and D-06.</action>
  <acceptance_criteria>
    - `operator/shell.ex` contains `Components.nav_link`, `Components.nav_pill`, `Components.tenant_chip`, and `Components.theme_picker`.
    - `operator/shell.ex` contains no private `defp nav_link`, `defp nav_pill`, `defp tenant_chip`, or `defp theme_toggle` definitions.
    - `Shell.theme_choice(%{"theme" => "dark"}) == :dark`, `Shell.theme_choice(%{"theme" => "light"}) == :light`, and `Shell.theme_choice(%{}) == :system` are covered by tests.
    - `Shell.set_theme_path("/ops/mail?tenant_id=acme&theme=dark", "system")` returns `/ops/mail?tenant_id=acme`, and tests cover preserving unrelated params.
    - Rendered shell output contains three theme radio labels and no `aria-pressed` theme option.
    - Rendered shell output contains `phx-click="set_theme"` and `phx-value-theme` values for `system`, `light`, and `dark`, supplied by `Components.theme_picker/1` without modifying `components.ex` in this plan.
    - Existing aria-current tests for Deliveries and Inbound continue passing.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs --warnings-as-errors</automated>
    <automated>rg -n 'Components\\.(nav_link|nav_pill|tenant_chip|theme_picker)|def theme_choice|def set_theme_path' mailglass_admin/lib/mailglass_admin/operator/shell.ex</automated>
    <automated>! rg -n 'defp (nav_link|nav_pill|tenant_chip|theme_toggle)\\b' mailglass_admin/lib/mailglass_admin/operator/shell.ex</automated>
  </verify>
  <done>The shell consumes public primitives and exposes deterministic theme choice/path helpers without private primitive copies.</done>
</task>

<task id="110-02-02" type="auto" files="mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex">
  <name>Task 2: Route shell theme picker events through existing URL patch semantics</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/operator_live.ex
    mailglass_admin/lib/mailglass_admin/inbound_live.ex
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-RESEARCH.md
  </read_first>
  <action>In both `MailglassAdmin.OperatorLive` and `MailglassAdmin.InboundLive`, assign `:theme_choice` from `MailglassAdmin.Operator.Shell.theme_choice(params)` inside `handle_params/3`, pass `theme_choice={@theme_choice}` into `<MailglassAdmin.Operator.Shell.shell>`, and add `handle_event("set_theme", %{"theme" => theme}, socket)` that pushes a patch to `MailglassAdmin.Operator.Shell.set_theme_path(socket.assigns.page_uri, theme)`. Keep existing `:dark_chrome` behavior for the current root data-theme decision so this plan does not take over Phase 112 root/system/no-FOUC behavior per D-06. Preserve tenant and filter query params when changing theme values. Do not add browser storage, cookies, match-media scripts, root layout scripts, or LiveView hooks in this task.</action>
  <acceptance_criteria>
    - `operator_live.ex` and `inbound_live.ex` assign `:theme_choice` during `handle_params/3`.
    - Both LiveViews pass `theme_choice={@theme_choice}` to `MailglassAdmin.Operator.Shell.shell`.
    - Both LiveViews implement `handle_event("set_theme", %{"theme" => theme}, socket)` and call `Shell.set_theme_path/2`.
    - Existing `:dark_chrome` assigns remain in place for current light/dark root behavior.
    - No new browser storage API, cookie-setting code, match-media script, root theme script, or theme hook is introduced.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'theme_choice|set_theme|set_theme_path' mailglass_admin/lib/mailglass_admin/operator_live.ex mailglass_admin/lib/mailglass_admin/inbound_live.ex</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs --warnings-as-errors</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>Operator and inbound surfaces can render and respond to the public three-way theme picker while preserving the current URL patch model and Phase 110 scope boundary.</done>
</task>

<task id="110-02-03" type="auto" files="mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound/overview.ex, mailglass_admin/priv/static/app.css">
  <name>Task 3: Migrate overview stat cards to the canonical primitive</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound/overview.ex, mailglass_admin/priv/static/app.css</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/operator_live.ex
    mailglass_admin/lib/mailglass_admin/inbound/overview.ex
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/test/mailglass_admin/components_test.exs
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-UI-SPEC.md
    .planning/phases/110-primitives/110-VALIDATION.md
  </read_first>
  <action>Replace the four ad hoc operator overview health cards in `operator_live.ex` with `Components.stat_card/1` calls per D-07 and D-08. Use stable labels `Recent failures`, `Orphan backlog`, `Active suppressions`, and `All-clear status`; use meaningful runtime-missing copy such as `Unavailable` where data failed to resolve; use `All clear` and `Needs attention` for resolved all-clear state. Map failures to `:error` when count is greater than 0 and `:success` when 0; map orphan backlog to `:warning` when count is greater than 0 and `:success` when 0; map suppressions to `:info` or `:neutral`; map all-clear to `:success` or `:warning`. In `MailglassAdmin.Inbound.Overview`, add `alias MailglassAdmin.Components`, remove private `defp stat/1`, and render `Components.stat_card/1` for `InboundMessages`, `No match`, `Accepted`, and `No-match rate` with severity labels that include visible text and semantic severity. Preserve existing `data-testid` anchors by passing ids/test ids through the component API where needed so browser tests can keep targeting overview cells. Rebuild the CSS bundle after source class changes and commit any static bundle delta.</action>
  <acceptance_criteria>
    - `operator_live.ex` contains `Components.stat_card` for all four operator overview health stats.
    - `inbound/overview.ex` contains `Components.stat_card` for all four inbound overview stats and contains no private `defp stat`.
    - Operator overview stat values do not render a bare dash or underscore sequence for runtime-missing resolved states.
    - All stat cards have visible severity labels and semantic severity atoms supplied to `Components.stat_card/1`.
    - Existing operator overview and inbound overview test IDs needed by browser tests remain present in rendered output through component attrs or wrappers.
    - `cd mailglass_admin && mix mailglass_admin.assets.build` exits 0, and `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after committing any bundle delta.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Components\\.stat_card' mailglass_admin/lib/mailglass_admin/operator_live.ex mailglass_admin/lib/mailglass_admin/inbound/overview.ex</automated>
    <automated>! rg -n 'defp stat\\b|text-display font-bold break-words|mono mt-2 text-display .*break-words' mailglass_admin/lib/mailglass_admin/operator_live.ex mailglass_admin/lib/mailglass_admin/inbound/overview.ex</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs test/mailglass_admin/operator/shell_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin && mix mailglass_admin.assets.build</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/</automated>
  </verify>
  <done>Operator and inbound overview stat/KPI cards are canonical `Components.stat_card/1` consumers, and the old page-local stat-card shapes are gone.</done>
</task>

</tasks>

## Artifacts this phase produces

- Public component `MailglassAdmin.Components.nav_link/1`.
- Public component `MailglassAdmin.Components.nav_pill/1`.
- Public component `MailglassAdmin.Components.tenant_chip/1`.
- Public component `MailglassAdmin.Components.theme_picker/1`.
- Public component `MailglassAdmin.Components.stat_card/1`.
- Component contract tests in `mailglass_admin/test/mailglass_admin/components_test.exs`.
- Shell consumers of `Components.nav_link/1`, `Components.nav_pill/1`, `Components.tenant_chip/1`, and `Components.theme_picker/1`.
- Public shell helpers `MailglassAdmin.Operator.Shell.theme_choice/1` and `MailglassAdmin.Operator.Shell.set_theme_path/2`.
- LiveView event `set_theme` in `MailglassAdmin.OperatorLive` and `MailglassAdmin.InboundLive`.
- Operator and inbound overview consumers of `Components.stat_card/1`.
- Gallery specimens for public primitives and states under `gallery-nav_link-*`, `gallery-nav_pill-*`, `gallery-tenant_chip-*`, `gallery-theme_picker-*`, and `gallery-stat_card-*`.
- `PRIMITIVE-DRIFT-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `STATCARD-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `ICON-EXISTS-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- Playwright structural assertions for primitive gallery state coverage, system wrapper absence of explicit theme, radio semantics, disabled distinction, 44px target sizes, icon meaning, and stat-card overflow/no-wrap behavior.
- Rebuilt `mailglass_admin/priv/static/app.css` when source class strings change.
- `.planning/phases/110-primitives/110-01-SUMMARY.md`.
- `.planning/phases/110-primitives/110-02-SUMMARY.md`.
- `.planning/phases/110-primitives/110-03-SUMMARY.md`.
- `.planning/phases/110-primitives/110-04-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shell component -> operator/inbound LiveView events | Theme picker emits user-controlled values into LiveView event handlers. |
| primitive consumer -> tenant scope query params | Theme changes must preserve tenant/filter params and not add tenant selection behavior. |
| overview data -> stat-card rendering | Missing or error-prone data must not be presented as ambiguous glyph-only state. |

## STRIDE Threat Register

Execution must stop on any HIGH or CRITICAL threat whose status is `open`. The executor must either complete the mitigation and rerun the listed verification, or create a blocking checkpoint that records why the threat cannot be closed.

| Threat ID | STRIDE | ASVS L1 Mapping | Severity | Status | Component | Mitigation / Verification |
|---|---|---|---|---|---|---|
| T-110-05 | Tampering | V5 Input Validation | HIGH | planned | `set_theme` event | Route only `"system"`, `"light"`, and `"dark"` through `Shell.set_theme_path/2`; invalid values fall back to deleting the explicit theme value or no-op per implementation choice. Verification: Shell tests. |
| T-110-06 | Information Disclosure | V4 Access Control | HIGH | planned | tenant/filter query preservation | Preserve unrelated query params when theme changes; do not add tenant listing/switching. Verification: Shell path tests. |
| T-110-07 | Spoofing | V5 Input Validation | MEDIUM | planned | stat-card severity | Severity must be visible icon plus label plus color. Verification: component tests and STATCARD gate. |
| T-110-08 | Tampering | V14 Configuration | MEDIUM | planned | primitive copy drift | Remove private shell component definitions before drift gate lands. Verification: negative `rg` and Plan 110-04 gate. |
| T-110-SC | Tampering | V14 Configuration | LOW | accepted | package installs | No package-manager install is part of this plan. Verification: final package manifest diff remains empty. |
</threat_model>

<verification>
Plan-level verification:
- `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs test/mailglass_admin/operator/shell_test.exs --warnings-as-errors`
- `bash mailglass_admin/scripts/check-conformance.sh`
- `cd mailglass_admin && mix mailglass_admin.assets.build`
- `cd mailglass_admin && git diff --exit-code priv/static/`
- `! rg -n 'defp (nav_link|nav_pill|tenant_chip|theme_toggle)\\b|defp stat\\b' mailglass_admin/lib/mailglass_admin/operator/shell.ex mailglass_admin/lib/mailglass_admin/inbound/overview.ex`
</verification>

<success_criteria>
Plan 110-02 is complete when the operator shell, operator overview, and inbound overview render the public primitives directly; theme picker events map to URL patches without persistence/root-scope expansion; shell tests and component tests pass; conformance remains green; and the CSS bundle is clean.
</success_criteria>

<output>
Create `.planning/phases/110-primitives/110-02-SUMMARY.md` when done.
</output>
