---
phase: 111-forms
plan: "03"
type: execute
wave: 2
depends_on:
  - 111-01
files_modified:
  - mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/test/mailglass_admin/preview_live_test.exs
  - mailglass_admin/test/mailglass_admin/operator/replay_modal_test.exs
  - mailglass_admin/test/mailglass_admin/inbound/replay_modal_test.exs
autonomous: true
requirements:
  - FORM-02
  - FORM-03
must_haves:
  truths:
    - "D-03: Preview assigns controls and replay target radios are explicitly updated or certified, not left to a vague sweep."
    - "D-04/FORM-02: Non-filter authored controls have visible associated labels and programmatic descriptions where applicable."
    - "D-06/FORM-03: Preview unsupported/atom values and replay target controls do not misuse disabled as read-only."
  artifacts:
    - path: mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
      provides: "Explicit label/id/help/read-only semantics for Preview assigns controls"
    - path: mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
      provides: "Explicit radio IDs/labels/descriptions and non-color selected cue for ambiguous replay targets"
    - path: mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
      provides: "Certified inbound replay modal labelling and no target-radio form controls"
    - path: mailglass_admin/test/mailglass_admin/preview_live_test.exs
      provides: "Preview assigns form certification"
    - path: mailglass_admin/test/mailglass_admin/operator/replay_modal_test.exs
      provides: "Operator replay radio contract tests"
    - path: mailglass_admin/test/mailglass_admin/inbound/replay_modal_test.exs
      provides: "Inbound replay modal certification tests"
  key_links:
    - from: mailglass_admin/test/mailglass_admin/preview_live_test.exs
      to: mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
      via: "rendered Preview scenario and direct AssignsForm.field/1 assertions"
      pattern: "preview-assigns-form"
    - from: mailglass_admin/test/mailglass_admin/operator/replay_modal_test.exs
      to: mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
      via: "render_component(&ReplayModal.replay_modal/1, ambiguous replay targets)"
      pattern: "operator-replay-targets"
---

<objective>
Update and certify the non-filter authored form surfaces called out by D-03.

Purpose: FORM-02 and FORM-03 are not complete if filter forms improve while Preview assigns controls and replay target radios keep ambiguous label/read-only/selected-state semantics. This plan handles those surfaces explicitly without redesigning unrelated workflows.

Output: Preview assigns controls with explicit labels/help/read-only behavior, operator replay target radios with explicit label/description and non-color selected state, inbound replay modal certification, and focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/111-forms/111-CONTEXT.md
@.planning/phases/111-forms/111-RESEARCH.md
@.planning/phases/111-forms/111-VALIDATION.md
@.planning/phases/111-forms/111-01-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
@mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
@mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
@mailglass_admin/test/mailglass_admin/preview_live_test.exs
</context>

## Artifacts this phase produces

- Preview assigns form field helpers for stable `assigns-{key}` IDs, visible labels, help IDs, and correct read-only/display-only handling.
- Operator replay modal radio target IDs and label/description wiring for ambiguous target selection.
- Inbound replay modal labelling certification and explicit proof that it has no replay-target radios.
- Focused tests for Preview assigns, operator replay radios, and inbound replay modal controls.
- Later Plan 111-04 adds browser coverage for these states through structural tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Preview assigns controls</name>
  <files>mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex, mailglass_admin/test/mailglass_admin/preview_live_test.exs</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
    @mailglass_admin/test/mailglass_admin/preview_live_test.exs
    @.planning/phases/111-forms/111-CONTEXT.md
    @.planning/phases/111-forms/111-RESEARCH.md
  </read_first>
  <behavior>
    - Editable Preview assigns controls have stable IDs, visible labels using `for`, and help text connected by `aria-describedby`.
    - Boolean checkbox labels are associated by ID, not by wrapper-only labelling.
    - Atom and unsupported values render as read-only display rows, not as disabled submitted controls.
    - Existing `assigns_changed`, `render_preview`, and `reset_assigns` behavior stays intact.
  </behavior>
  <action>In `MailglassAdmin.Preview.AssignsForm`, add small helpers for `control_id(key)`, `control_name(key)`, `help_id(key)`, and shared label/help rendering. Update every `field/1` clause so editable string, integer, float, DateTime, Date, map, struct, and boolean controls render explicit IDs, `label for`, and help text connected through `aria-describedby`. Keep the existing sorted field order and event names. For atom and unsupported values, stop using disabled text inputs as read-only display; render a visible non-editable display element with `data-readonly-display` and include a hidden input only if PreviewLive needs that value in change payloads. Add Preview tests that directly render representative `AssignsForm.field/1` clauses and assert exact IDs such as `assigns-user_name`, labels, help IDs, native `readonly` only where valid, and no fake disabled read-only control.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `preview_live_test.exs` asserts `data-testid="preview-assigns-form"` still renders in a real scenario branch.
    - At least one string field assertion includes `label for="assigns-user_name"`, `id="assigns-user_name"`, and `aria-describedby="assigns-user_name-help"`.
    - Boolean field tests assert the checkbox has a stable ID and a visible associated label.
    - Atom/unsupported value tests assert `data-readonly-display` and refute a disabled text input being used as the read-only representation.
    - `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <done>Preview assigns controls are explicitly labelled and read-only states are honest without changing Preview workflow behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update and certify replay target controls</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex, mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/test/mailglass_admin/operator/replay_modal_test.exs, mailglass_admin/test/mailglass_admin/inbound/replay_modal_test.exs</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
    @mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
    @mailglass_admin/test/mailglass_admin/operator_live_test.exs
    @mailglass_admin/test/mailglass_admin/inbound_live_test.exs
    @.planning/phases/111-forms/111-CONTEXT.md
  </read_first>
  <behavior>
    - Operator ambiguous replay targets are native radios with stable IDs and visible labels.
    - Each radio has connected descriptive text that identifies the provider/event target.
    - Selected target state includes visible text or icon+text, not only a border/background color change.
    - Inbound replay modal has correct dialog labelling and is certified as having no replay target radio group.
  </behavior>
  <action>In `MailglassAdmin.Operator.ReplayModal`, update the ambiguous-target form so each candidate radio has a stable ID derived from `webhook_event_id`, a visible `label for`, and connected descriptive text through `aria-describedby`. Keep the existing `operator-replay-targets` form ID and `choose_replay_target` event. Add a non-color selected cue inside `target_card/1`, such as visible `Selected target` text with an adjacent check icon, and leave exact/unavailable branches as non-radio states. In `MailglassAdmin.Inbound.ReplayModal`, add or confirm `aria-labelledby` points at a stable heading ID and prove the modal has no target-radio group because inbound replay has no ambiguous target branch. Add new component test files under `test/mailglass_admin/operator/` and `test/mailglass_admin/inbound/` rather than editing broad LiveView tests.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator/replay_modal_test.exs test/mailglass_admin/inbound/replay_modal_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Operator replay modal tests render ambiguous candidates and assert `id="operator-replay-target-..."`, `for="operator-replay-target-..."`, `aria-describedby`, `name="webhook_event_id"`, and visible `Selected target` after a selected candidate is rendered.
    - Operator tests assert exact replay target branch does not render unnecessary radio choices.
    - Inbound replay modal tests assert `role="dialog"`, `aria-modal="true"`, stable `aria-labelledby`, and no `type="radio"` in the rendered inbound modal.
    - Existing replay event names `choose_replay_target`, `close_replay`, and `confirm_replay` are preserved.
  </acceptance_criteria>
  <done>Replay target controls are explicitly labelled/certified and selected state is not color-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Preview assigns change event | Editable preview values cross from user input into preview rendering. |
| Browser -> replay target selection | Operator-selected webhook target ID crosses into replay confirmation flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07 | Tampering | Preview assigns controls | mitigate | Preserve existing PreviewLive parsing; this plan changes labelling/read-only markup only, with tests proving existing change behavior remains. |
| T-111-08 | Tampering | `operator-replay-targets` radio values | mitigate | Keep existing `choose_replay_target` and `selected_replay_target` validation; markup only makes target selection explicit. |
| T-111-09 | Spoofing | selected replay target state | mitigate | Add visible selected copy/icon so state is not conveyed only by color. |
| T-111-SC | Tampering | package installs | mitigate | No package install task exists; use existing Mix test tooling only. |
</threat_model>

<verification>
Plan-level automated checks:

- `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix test test/mailglass_admin/operator/replay_modal_test.exs test/mailglass_admin/inbound/replay_modal_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- Preview assigns controls have explicit labels and honest read-only semantics.
- Operator replay ambiguous target radios have explicit labels/descriptions and a non-color selected cue.
- Inbound replay modal is explicitly certified as not needing target radios and has stable dialog labelling.
- No unrelated non-filter workflow redesign, tenant seam work, theme persistence, or package install is introduced.
</success_criteria>

<output>
Create `.planning/phases/111-forms/111-03-SUMMARY.md` when done.
</output>
