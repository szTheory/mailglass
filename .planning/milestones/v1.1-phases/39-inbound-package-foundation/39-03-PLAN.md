---
phase: "39"
plan: "03"
type: execute
wave: 3
depends_on:
  - "39-01"
  - "39-02"
files_modified:
  - "mailglass_inbound/mix.exs"
  - "mailglass_inbound/README.md"
  - "mailglass_inbound/docs/api_stability.md"
  - "mailglass_inbound/lib/mailglass_inbound.ex"
  - "mailglass_inbound/lib/mailglass_inbound/optional_deps.ex"
  - "mailglass_inbound/test/test_helper.exs"
  - "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
autonomous: true
requirements:
  - MODEL-01
  - ROUTE-01
  - MAILBOX-01
must_haves:
  truths:
    - "The sibling `mailglass_inbound` package compiles as a coherent package surface without making Oban a mandatory dependency."
    - "Baseline package docs describe only the supported Phase 39 contract and explicitly exclude deferred ingress, execution, and UI scope."
    - "Contract-proof tests fail when the documented inbound surface drifts from the public modules and storage boundaries defined in Plans 39-01 and 39-02."
  artifacts:
    - path: "mailglass_inbound/mix.exs"
      provides: "Sibling package scaffold with linked-version posture and optional dependency declarations"
      contains: "def project do"
    - path: "mailglass_inbound/lib/mailglass_inbound/optional_deps.ex"
      provides: "Package-local optional dependency seams preserving Oban-optional philosophy"
      contains: "OptionalDeps"
    - path: "mailglass_inbound/docs/api_stability.md"
      provides: "Canonical contract inventory for the inbound package foundation"
      contains: "stable"
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Automated proof that docs do not overstate the Phase 39 surface"
      contains: "api_stability"
  key_links:
    - from: "mailglass_inbound/mix.exs"
      to: "mailglass_inbound/lib/mailglass_inbound/optional_deps.ex"
      via: "optional deps declared in the package are gated through package-local shims"
      pattern: "optional: true|OptionalDeps"
    - from: "mailglass_inbound/docs/api_stability.md"
      to: "mailglass_inbound/lib/mailglass_inbound/inbound_message.ex"
      via: "docs inventory names only the stable public modules and fields implemented in prior plans"
      pattern: "InboundMessage|Router|Mailbox"
    - from: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      to: "mailglass_inbound/README.md"
      via: "docs contract test guards against deferred-scope drift in package docs"
      pattern: "Postmark|SendGrid|Oban|replay"
---

<objective>
Wire the sibling package scaffold, optional-dependency seams, and baseline contract docs/tests.

Purpose: Finish Phase 39 by turning the public contract and persistence foundation into a coherent sibling package that stays honest about optional Oban support and deferred scope.
Output: `mailglass_inbound` package scaffold, package-local optional-deps namespace, narrow docs, and docs-contract proof tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-inbound-package-foundation/39-CONTEXT.md
@.planning/phases/39-inbound-package-foundation/39-RESEARCH.md
@.planning/phases/39-inbound-package-foundation/39-PATTERNS.md
@.planning/phases/39-inbound-package-foundation/39-VALIDATION.md
@.planning/phases/39-inbound-package-foundation/39-01-PLAN.md
@.planning/phases/39-inbound-package-foundation/39-02-PLAN.md
@mailglass_admin/mix.exs
@lib/mailglass/optional_deps.ex
@README.md
@docs/api_stability.md

<interfaces>
From mailglass_admin/mix.exs:
```elixir
defp mailglass_dep do
  if System.get_env("MIX_PUBLISH") == "true" do
    {:mailglass, "== 0.3.2"}
  else
    {:mailglass, path: "..", override: true}
  end
end
```

From lib/mailglass/optional_deps.ex:
```elixir
@moduledoc """
Namespace for optional dependency gateway modules.
"""
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold the sibling package and preserve package-local optional dependency seams</name>
  <files>mailglass_inbound/mix.exs, mailglass_inbound/lib/mailglass_inbound.ex, mailglass_inbound/lib/mailglass_inbound/optional_deps.ex, mailglass_inbound/test/test_helper.exs</files>
  <action>Create the minimal sibling package scaffold for `mailglass_inbound` using the `mailglass_admin` linked-version shape as the packaging analog: package metadata, path dependency on the repo root during local development, exact-version pin in publish mode, and a package root module. In `mailglass_inbound/lib/mailglass_inbound/optional_deps.ex`, create package-local gateway namespaces that preserve the optional-Oban philosophy from D-39-16 and D-39-26 through D-39-28: later async execution may use Oban, but the package must compile and document a seam rather than a hard dependency. Keep this file package-local; do not reuse `Mailglass.OptionalDeps.*` directly across package boundaries, and do not introduce direct `Oban` references outside the gateway surface. Add the minimal `test/test_helper.exs` wiring required for the package-local Phase 39 contract tests.</action>
  <verify>
    <automated>cd mailglass_inbound && mix compile --warnings-as-errors && mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <done>The inbound package has a coherent Hex package scaffold and explicit optional-dependency seams without making Oban mandatory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish narrow foundation docs and guard them with docs-contract tests</name>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/api_stability.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <behavior>
    - Test 1: docs inventory names the stable Phase 39 public modules and their narrow promises: `InboundMessage`, `Router`, and `Mailbox`.
    - Test 2: package docs describe storage as canonical normalized rows plus raw evidence/replay truth, and they do not claim provider-specific ingress or execution features that are deferred to later phases.
    - Test 3: docs and tests make the optional-Oban seam explicit without turning Oban into a mandatory install/runtime assumption.
  </behavior>
  <action>Write `mailglass_inbound/README.md` and `mailglass_inbound/docs/api_stability.md` as the canonical Phase 39 package docs. Mirror the repo’s current stability-document posture: explicitly inventory what is stable now, what remains internal, and what is deferred. The stable surface for this phase is only the public `InboundMessage`, `Router`, and `Mailbox` contract plus the operator-facing storage distinction from Plan 39-02. State clearly that provider-specific Postmark/SendGrid ingress, async execution implementations, Conductor UI, and expanded matcher surface are not part of Phase 39 yet. Add `docs_contract_test.exs` as the automated proof from `39-VALIDATION.md`, asserting that the package docs mention the supported contract and exclude deferred router predicates, body/attachment matching, multi-match fan-out, mailbox lifecycle hooks, mandatory Oban coupling, and provider/UI claims that have not shipped.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The sibling package docs are honest, narrow, and automatically checked against contract drift and deferred-scope overstatement.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package docs -> adopters | Adopters will trust package docs and compile behavior as the authoritative contract for the new sibling package. |
| optional dependency declarations -> runtime integrations | Package dependency setup determines whether future execution paths accidentally require Oban or other optional libraries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Denial of Service / Tampering | package compile surface | mitigate | Keep optional dependencies behind package-local gateway seams and require `mix compile --no-optional-deps --warnings-as-errors` to pass so accidental hard coupling fails immediately. |
| T-39-08 | Repudiation | package docs and stability inventory | mitigate | Use one narrow README and `docs/api_stability.md` plus `docs_contract_test.exs` so the shipped docs cannot overstate deferred ingress, execution, or UI scope. |
| T-39-09 | Tampering | sibling package release/config posture | mitigate | Mirror the existing sibling linked-version pattern from `mailglass_admin/mix.exs` so `mailglass_inbound` ships as a coherent package instead of ad hoc root-module reachability. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix compile --warnings-as-errors`
- `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
</verification>

<success_criteria>
`mailglass_inbound` exists as a coherent sibling package with honest docs, package-local optional-dependency seams, and automated proof that the Phase 39 contract stays narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/39-inbound-package-foundation/39-03-SUMMARY.md`
</output>
