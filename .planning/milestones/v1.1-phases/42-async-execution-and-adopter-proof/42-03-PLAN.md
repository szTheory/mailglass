---
phase: "42"
plan: "03"
type: execute
wave: 3
depends_on:
  - "42-01"
  - "42-02"
files_modified:
  - "mix.exs"
  - "lib/mix/tasks/mailglass.publish.check.ex"
  - "lib/mix/tasks/mailglass.docs.check.ex"
  - ".github/workflows/release-please.yml"
  - "release-please-config.json"
  - ".release-please-manifest.json"
  - ".planning/publish/mailglass_inbound-files.expected"
  - ".planning/publish/mailglass_inbound-publish-summary.json"
  - "mailglass_inbound/mix.exs"
  - "test/mailglass/stability_contract_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
  - "MAINTAINING.md"
autonomous: true
requirements:
  - ADOPT-01
must_haves:
  truths:
    - "Root-level verification and release proof explicitly cover `mailglass_inbound` now that the sibling package has a canonical install/support lane."
    - "The repo's proof surface stays narrow and semantic: update existing publish/docs/stability checks rather than inventing a new release subsystem."
    - "Release automation and proof artifacts remain honest about which `mailglass_inbound` files and package metadata participate in sibling-package release truth."
  artifacts:
    - path: "lib/mix/tasks/mailglass.publish.check.ex"
      provides: "Repo-root publish proof extended to include `mailglass_inbound` expectations where Phase 42 requires them"
      contains: "mailglass_inbound"
    - path: "mix.exs"
      provides: "Semantic verification alias or wiring that includes the new inbound proof lane"
      contains: "verify."
    - path: ".github/workflows/release-please.yml"
      provides: "Linked-version or release automation coverage kept aligned with sibling-package truth"
      contains: "mailglass"
    - path: "test/mailglass/stability_contract_test.exs"
      provides: "Deterministic root proof assertions for the inbound sibling-package coverage"
      contains: "mailglass_inbound"
  key_links:
    - from: "lib/mix/tasks/mailglass.publish.check.ex"
      to: ".planning/publish/mailglass_inbound-files.expected"
      via: "publish proof gets a sibling-package expectation set dedicated to `mailglass_inbound` instead of overloading the core package artifact list"
      pattern: "expected|mailglass_inbound|publish"
    - from: "mix.exs"
      to: "test/mailglass/stability_contract_test.exs"
      via: "semantic root verification stays synchronized with the new inbound proof lane"
      pattern: "verify\\.|stability"
    - from: ".github/workflows/release-please.yml"
      to: "mailglass_inbound/mix.exs"
      via: "release automation remains explicit about sibling-package version and proof participation"
      pattern: "version|mailglass_inbound|release"
---

<objective>
Extend sibling-package release and root verification proof to cover
`mailglass_inbound`.

Purpose: Complete the Phase 42 trust story by making the repo's existing
publish/docs/stability proof lanes aware of the inbound sibling package and its
new canonical adoption surface.
Output: Root verification wiring, publish/docs proof updates, release automation
alignment, and deterministic tests that fail when `mailglass_inbound` drifts
out of the supported release/support posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/42-async-execution-and-adopter-proof/42-CONTEXT.md
@.planning/phases/42-async-execution-and-adopter-proof/42-RESEARCH.md
@.planning/phases/42-async-execution-and-adopter-proof/42-PATTERNS.md
@.planning/phases/42-async-execution-and-adopter-proof/42-01-PLAN.md
@.planning/phases/42-async-execution-and-adopter-proof/42-02-PLAN.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-03-PLAN.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PLAN.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-03-PLAN.md
@mix.exs
@lib/mix/tasks/mailglass.publish.check.ex
@lib/mix/tasks/mailglass.docs.check.ex
@test/mailglass/stability_contract_test.exs
@mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
@MAINTAINING.md
@mailglass_inbound/mix.exs
@.github/workflows/release-please.yml
@release-please-config.json
@.release-please-manifest.json
@.planning/publish/mailglass-files.expected
@.planning/publish/mailglass-publish-summary.json
@.planning/publish/mailglass_inbound-files.expected
@.planning/publish/mailglass_inbound-publish-summary.json

<interfaces>
From lib/mix/tasks/mailglass.publish.check.ex:
```elixir
def run(args) do
  ...
end
```

From mix.exs:
```elixir
"verify.stability_contract": [...]
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the root proof lane so `mailglass_inbound` is part of semantic verification</name>
  <files>mix.exs, lib/mix/tasks/mailglass.publish.check.ex, lib/mix/tasks/mailglass.docs.check.ex, test/mailglass/stability_contract_test.exs, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs, MAINTAINING.md</files>
  <behavior>
    - Test 1: the repo has a semantic root verification path that names the inbound sibling package proof lane explicitly.
    - Test 2: docs and publish checks treat the `mailglass_inbound` canonical adoption story as release-blocking truth where appropriate.
    - Test 3: root stability tests fail deterministically if inbound proof wiring is removed or if the new docs-contract lane drops out of root verification expectations.
  </behavior>
  <action>Update the existing semantic proof surfaces rather than creating new bespoke tooling. Extend `mix.exs`, `lib/mix/tasks/mailglass.publish.check.ex`, `lib/mix/tasks/mailglass.docs.check.ex`, and any maintainer-facing verification guidance so `mailglass_inbound` participates in the repo's proof story now that it has a canonical install/support lane. The exact mechanism can be an existing alias extension, a publish-check enhancement, or a docs-check addition, but it must stay lightweight and semantic like the prior stability/release phases. Then add focused assertions to `test/mailglass/stability_contract_test.exs` and the existing inbound docs-contract test so proof drift fails immediately if the root lane stops covering the sibling package.</action>
  <verify>
    <automated>mix test test/mailglass/stability_contract_test.exs --warnings-as-errors && cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors && cd .. && rg -n "mailglass_inbound|verify\\.|publish|docs" mix.exs lib/mix/tasks/mailglass.publish.check.ex lib/mix/tasks/mailglass.docs.check.ex test/mailglass/stability_contract_test.exs MAINTAINING.md</automated>
  </verify>
  <done>The repo-root verification story now includes the inbound sibling package without adding a heavy new subsystem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align release automation and publish expectations with the inbound sibling package proof</name>
  <files>.github/workflows/release-please.yml, release-please-config.json, .release-please-manifest.json, .planning/publish/mailglass_inbound-files.expected, .planning/publish/mailglass_inbound-publish-summary.json, mailglass_inbound/mix.exs, lib/mix/tasks/mailglass.publish.check.ex, test/mailglass/stability_contract_test.exs</files>
  <behavior>
    - Test 1: release automation remains explicit about how `mailglass_inbound` participates in linked-version or release truth.
    - Test 2: publish-proof expectations capture any new release-blocking files or metadata tied to the inbound package's supported surface.
    - Test 3: root tests fail if automation and proof expectations drift apart around the inbound sibling package.
  </behavior>
  <action>Review `.github/workflows/release-please.yml`, `release-please-config.json`, `.release-please-manifest.json`, `mailglass_inbound/mix.exs`, the publish-check task, and the root publish expectations to make sure Phase 42's sibling-package support story is actually represented in the repo's release proof. Register `mailglass_inbound` in the real release-please control files, not only in the workflow wrapper, and add a dedicated inbound publish expectation artifact set, preferably `.planning/publish/mailglass_inbound-files.expected` plus the corresponding committed summary JSON shape, rather than overloading the core package's proof files. If `mailglass_inbound` package metadata, expected published files, or release-please behavior need updates for the new canonical docs/support lane, make those changes and pin them with focused assertions. Keep this work recommendation-first and narrow; do not invent a broader multi-package release framework beyond what the repo already uses for sibling-package proof.</action>
  <verify>
    <automated>actionlint .github/workflows/release-please.yml && mix test test/mailglass/stability_contract_test.exs --warnings-as-errors && rg -n "mailglass_inbound|release-please|publish|expected" .github/workflows/release-please.yml release-please-config.json .release-please-manifest.json lib/mix/tasks/mailglass.publish.check.ex .planning/publish/mailglass_inbound-files.expected .planning/publish/mailglass_inbound-publish-summary.json mailglass_inbound/mix.exs test/mailglass/stability_contract_test.exs</automated>
  </verify>
  <done>The inbound sibling package is covered by the repo's release-proof lane and the automation/proof surfaces stay aligned.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root verification -> release decision | Maintainers rely on repo-root proof lanes to decide whether the sibling package is supportable and publish-ready. |
| release automation -> published sibling package | Workflow configuration and package metadata determine whether release truth matches the package surface the docs promise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Repudiation | root verification and publish proof | mitigate | Keep `mailglass_inbound` inside semantic root proof lanes so release decisions remain auditable. |
| T-42-08 | Tampering | release automation and package metadata | mitigate | Align workflow config, package metadata, and expected published files with focused proof checks. |
| T-42-09 | Misrepresentation | maintainer guidance | mitigate | Update the existing root guidance to name the inbound package proof lanes explicitly rather than assuming maintainers infer them. |
</threat_model>

<verification>
- `mix test test/mailglass/stability_contract_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- `actionlint .github/workflows/release-please.yml`
- `rg -n "mailglass_inbound|release-please|publish|verify\\." mix.exs lib/mix/tasks/mailglass.publish.check.ex lib/mix/tasks/mailglass.docs.check.ex .github/workflows/release-please.yml release-please-config.json .release-please-manifest.json .planning/publish/mailglass_inbound-files.expected .planning/publish/mailglass_inbound-publish-summary.json test/mailglass/stability_contract_test.exs MAINTAINING.md`
</verification>

<success_criteria>
The repo's root proof and release surfaces explicitly cover `mailglass_inbound`
so the sibling package's canonical install/support story is backed by the same
lightweight semantic verification posture as the rest of the project.
</success_criteria>

<output>
After completion, create `.planning/phases/42-async-execution-and-adopter-proof/42-03-SUMMARY.md`
</output>
