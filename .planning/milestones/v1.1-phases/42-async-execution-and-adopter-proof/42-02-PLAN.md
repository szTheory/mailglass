---
phase: "42"
plan: "02"
type: execute
wave: 2
depends_on:
  - "42-01"
files_modified:
  - "mailglass_inbound/README.md"
  - "mailglass_inbound/docs/postmark_ingress.md"
  - "mailglass_inbound/docs/sendgrid_ingress.md"
  - "mailglass_inbound/docs/api_stability.md"
  - "mailglass_admin/docs/operator-trust.md"
  - "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/replay_test.exs"
autonomous: true
requirements:
  - ADOPT-01
  - EXEC-01
  - EXEC-02
must_haves:
  truths:
    - "Adopters can follow one canonical manual setup path for the inbound slice without relying on installer guesses."
    - "The docs distinguish durable Oban execution from bounded fallback execution precisely and keep replay/operator semantics honest."
    - "The public stability story remains narrow: no public replay API, no operator UI promise, and no public `%Oban.Job{}` or worker contract."
  artifacts:
    - path: "mailglass_inbound/README.md"
      provides: "Canonical adoption lane for install, configuration, execution modes, testing, and support posture"
      contains: "Oban"
    - path: "mailglass_inbound/docs/api_stability.md"
      provides: "Updated stability inventory that preserves the narrow public contract after async execution lands"
      contains: "stable"
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Docs-contract proof that rejects durability overstatements and premature public operator/replay claims"
      contains: "test "
  key_links:
    - from: "mailglass_inbound/README.md"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      via: "the docs name the real parser, router, and provider mount path adopters must use"
      pattern: "Plug.Parsers|body_reader|SendGrid|Postmark"
    - from: "mailglass_inbound/docs/api_stability.md"
      to: "mailglass_inbound/lib/mailglass_inbound/execution/worker.ex"
      via: "internal worker and queue details remain out of the stable public contract"
      pattern: "internal|stable|replay|worker"
    - from: "mailglass_admin/docs/operator-trust.md"
      to: "mailglass_inbound/test/mailglass_inbound/replay_test.exs"
      via: "operator trust wording stays aligned with replay reality and failure cases"
      pattern: "replay|recover|fresh receive|operator"
---

<objective>
Publish canonical install, testing, and operator-trust docs for the core
inbound slice.

Purpose: Deliver D-42-09 through D-42-17 by turning the inbound package's
current scattered setup notes into one honest adopter lane with strict
docs-contract proof.
Output: Updated README and package guides, a tightened stability inventory,
operator-trust wording aligned with replay reality, and tests that fail when
the docs overstate durability, replay surface, or installer support.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-async-execution-and-adopter-proof/42-CONTEXT.md
@.planning/phases/42-async-execution-and-adopter-proof/42-RESEARCH.md
@.planning/phases/42-async-execution-and-adopter-proof/42-PATTERNS.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-03-PLAN.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-03-PLAN.md
@.planning/phases/42-async-execution-and-adopter-proof/42-01-PLAN.md
@mailglass_inbound/README.md
@mailglass_inbound/docs/api_stability.md
@mailglass_inbound/docs/postmark_ingress.md
@mailglass_inbound/docs/sendgrid_ingress.md
@mailglass_admin/docs/operator-trust.md
@mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
@mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs
@mailglass_inbound/test/mailglass_inbound/replay_test.exs

<interfaces>
From mailglass_inbound/docs/api_stability.md:
```markdown
### `stable`
- `MailglassInbound`
- `MailglassInbound.InboundMessage`
- `MailglassInbound.Router`
- `MailglassInbound.Mailbox`
```

From mailglass_admin/docs/operator-trust.md:
```markdown
Replay ...
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Publish one canonical inbound adoption lane with explicit Oban and fallback semantics</name>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/postmark_ingress.md, mailglass_inbound/docs/sendgrid_ingress.md, mailglass_inbound/docs/api_stability.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <behavior>
    - Test 1: docs provide one explicit manual setup path covering deps, migrations, parser/body-reader wiring, provider mounts, Oban wiring, and fallback semantics.
    - Test 2: docs state plainly that Oban is the durable path and Task.Supervisor fallback is best-effort only with replay/operator action as recovery.
    - Test 3: stability docs do not promote internal execution workers, queue names, replay internals, or installer helpers into the stable public contract.
    - Test 4: docs reject any claim that inbound setup is installer-driven in this phase.
  </behavior>
  <action>Rewrite the `mailglass_inbound` README so it becomes the canonical adoption path for the shipped inbound slice instead of a high-level overview plus scattered provider notes. Fold in the real package setup story: dependency and migration requirements, endpoint parser/body-reader wiring, router mounts for Postmark and SendGrid, Oban-backed runtime setup, bounded fallback semantics when Oban is absent, and the recommended verification commands adopters should run. Then tighten `docs/postmark_ingress.md`, `docs/sendgrid_ingress.md`, and `docs/api_stability.md` so the provider guides stay focused, the README remains the primary lane, and the stability inventory stays narrow despite the new internal async execution modules. Use docs-contract assertions to make durability wording, installer absence, and replay/internal-surface boundaries mechanically enforced.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Adopters have one honest manual setup path and the package docs stay precise about what is durable, what is best-effort, and what remains internal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align operator-trust wording and proof lanes with async execution and replay reality</name>
  <files>mailglass_admin/docs/operator-trust.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs, mailglass_inbound/test/mailglass_inbound/replay_test.exs</files>
  <behavior>
    - Test 1: operator-trust wording distinguishes replay from fresh provider receipt and describes missing-lineage or prior-`no_match` failure cases honestly.
    - Test 2: docs-contract proof fails if docs claim a public replay API, a new operator UI, or silent rerouting semantics.
    - Test 3: verification-oriented tests remain aligned with the docs' promised behavior around fallback mode, duplicate handling, and replay outcomes.
  </behavior>
  <action>Update `mailglass_admin/docs/operator-trust.md` and the `mailglass_inbound` docs proof lane so the operator story matches the new runtime reality: persisted truth first, async execution second, and replay as an internal operator/recovery tool rather than a public API or fresh provider receive. The wording should explain how operators reason about fresh durable execution, fresh best-effort fallback execution, duplicates, failures, and replay without implying there is already a public UI or stable replay command surface. Then extend the existing docs-contract and targeted behavior tests so future copy drift cannot reintroduce public replay/API/UI claims or overstate the fallback guarantee.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/replay_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The operator-trust story is honest about replay, recovery, and durability differences, and the proof lane guards that story against drift.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopters | Readers will treat setup and execution guarantees as contract and configure production systems from them. |
| docs -> operators | Recovery and replay wording influences how users diagnose failures and duplicates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Misrepresentation | README and provider docs | mitigate | Make the canonical lane explicit about Oban durability vs bounded fallback and keep installer support out of scope. |
| T-42-05 | Repudiation | operator-trust wording | mitigate | State replay and recovery semantics plainly, including failure cases where replay cannot infer a prior mailbox safely. |
| T-42-06 | Information Disclosure / Contract drift | stability inventory and docs-contract tests | mitigate | Keep internal worker, queue, and replay implementation details out of the stable public surface and prove it with tests. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/replay_test.exs --warnings-as-errors`
</verification>

<success_criteria>
Adopters can install, configure, test, and support the core inbound slice from
one honest first-party doc lane, and the docs remain mechanically protected
against durability overstatements or premature public replay/operator claims.
</success_criteria>

<output>
After completion, create `.planning/phases/42-async-execution-and-adopter-proof/42-02-SUMMARY.md`
</output>
