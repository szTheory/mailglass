---
phase: "40"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/providers/postmark.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
  - "mailglass_inbound/test/mailglass_inbound/ingress/caching_body_reader_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/postmark_provider_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs"
autonomous: true
requirements:
  - INGRESS-01
must_haves:
  truths:
    - "Authentic Postmark inbound requests can enter `mailglass_inbound` through one first-party Plug path that verifies before any tenant or persistence work."
    - "Verified Postmark payloads normalize into the already-locked `%MailglassInbound.InboundMessage{}` shape without adding provider-only public fields."
    - "Rejected or misconfigured ingress requests fail with explicit HTTP outcomes and do not continue into tenant resolution or storage."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      provides: "Verify-first inbound ingress orchestration for Postmark-backed requests"
      contains: "def call"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/postmark.ex"
      provides: "Sealed Postmark verifier and normalizer that returns canonical message plus evidence facts"
      contains: "def verify!"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex"
      provides: "Path-local raw body capture for inbound verification"
      contains: "def read_body"
    - path: "mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs"
      provides: "Automated proof of success and rejection HTTP outcomes"
      contains: "test "
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/postmark.ex"
      via: "Plug verifies first, then normalizes through the sealed provider seam"
      pattern: "verify|normalize"
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      via: "raw request bytes are read from conn.private[:raw_body]"
      pattern: "raw_body"
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/postmark.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/inbound_message.ex"
      via: "provider output is constrained to the locked canonical struct"
      pattern: "InboundMessage"
---

<objective>
Implement the Postmark ingress verification and normalization seam for `mailglass_inbound`.

Purpose: Deliver D-40-01 through D-40-12 and D-40-21 by creating the verify-first request path, sealed Postmark provider logic, and canonical normalization boundary without widening the stable public contract or executing mailboxes.
Output: Package-local inbound body reader, provider behaviour, Postmark verifier/normalizer, ingress plug, and request/provider tests that prove success and rejection paths.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-CONTEXT.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-RESEARCH.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-PATTERNS.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-VALIDATION.md
@.planning/phases/39-inbound-package-foundation/39-01-PLAN.md
@mailglass_inbound/lib/mailglass_inbound/inbound_message.ex
@lib/mailglass/webhook/plug.ex
@lib/mailglass/webhook/provider.ex
@lib/mailglass/webhook/providers/postmark.ex
@lib/mailglass/webhook/caching_body_reader.ex

<interfaces>
From mailglass_inbound/lib/mailglass_inbound/inbound_message.ex:
```elixir
@type t :: %__MODULE__{
        tenant_id: String.t() | nil,
        provider: provider() | nil,
        provider_message_id: String.t() | nil,
        message_id: String.t() | nil,
        envelope_recipient: String.t() | nil,
        from: [address()],
        to: [address()],
        cc: [address()],
        bcc: [address()],
        reply_to: [address()],
        subject: String.t() | nil,
        headers: %{optional(String.t()) => [String.t()]},
        sent_at: DateTime.t() | nil,
        received_at: DateTime.t() | nil,
        text_body: String.t() | nil,
        html_body: String.t() | nil,
        attachments: [attachment()]
      }
```

From lib/mailglass/webhook/provider.ex:
```elixir
@callback verify!(
            raw_body :: binary(),
            headers :: [{String.t(), String.t()}],
            config :: map()
          ) :: :ok | {:ok, :replay}

@callback normalize(
            raw_body :: binary(),
            headers :: [{String.t(), String.t()}]
          ) :: [Mailglass.Events.Event.t()]
```

From lib/mailglass/webhook/caching_body_reader.ex:
```elixir
def read_body(conn, opts) do
  case Plug.Conn.read_body(conn, opts) do
    {:ok, body, conn} ->
      raw = IO.iodata_to_binary([conn.private[:raw_body] || <<>>, body])
      {:ok, body, Plug.Conn.put_private(conn, :raw_body, raw)}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the path-local inbound body reader and sealed Postmark provider seam</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex, mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex, mailglass_inbound/lib/mailglass_inbound/ingress/providers/postmark.ex, mailglass_inbound/test/mailglass_inbound/ingress/caching_body_reader_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/postmark_provider_test.exs</files>
  <behavior>
    - Test 1: the inbound body reader preserves exact request bytes in `conn.private[:raw_body]` on the inbound path only per D-40-06.
    - Test 2: Postmark verification defaults to HTTP Basic Auth, optionally enforces IP allowlisting, and fails closed on missing or bad credentials per D-40-07.
    - Test 3: Postmark normalization returns the locked `%MailglassInbound.InboundMessage{}` fields only, with `MessageID -> provider_message_id`, RFC `Message-Id -> message_id`, and `OriginalRecipient -> envelope_recipient` per D-40-08 through D-40-12.
    - Test 4: structured sender/recipient data drives `from/to/cc/bcc/reply_to`, while attachment bytes, stripped-body helpers, verification facts, and parse warnings stay outside the public struct per D-40-11 and D-40-12.
  </behavior>
  <action>Create `MailglassInbound.Ingress.CachingBodyReader` by mirroring the existing webhook raw-body capture pattern from `Mailglass.Webhook.CachingBodyReader`, keeping the storage key `conn.private[:raw_body]` unless a package-local naming conflict exists. Add a sealed internal provider contract at `MailglassInbound.Ingress.Provider` per D-40-04 and D-40-05 with conn-free `verify!/3` and `normalize/2` callbacks shaped for one canonical inbound message plus package-internal evidence facts. Implement `MailglassInbound.Ingress.Providers.Postmark` so verification honors D-40-07 exactly: default Basic Auth, optional IP allowlist, fail closed on unverifiable requests, no tenant resolution. Implement pure normalization into the already-locked `%MailglassInbound.InboundMessage{}` from Phase 39 per D-40-08, preserving the identifier mapping and structured-address rules from D-40-09 through D-40-11. Keep provider-only helpers, raw MIME reconstruction, attachment bytes, and other evidence-only material out of the public struct per D-40-12 and D-40-16.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/caching_body_reader_test.exs test/mailglass_inbound/ingress/postmark_provider_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The package has a sealed inbound provider seam and a Postmark verifier/normalizer that produces only the locked canonical message contract while preserving evidence-only details outside the public struct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the verify-first ingress Plug with explicit HTTP outcomes and no mailbox execution</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
  <behavior>
    - Test 1: the Plug reads `conn.private[:raw_body]`, resolves Postmark config, verifies first, then and only then resolves tenant context per D-40-04 and D-40-06.
    - Test 2: success, auth failure, missing body reader wiring, tenant failure, and config failure map to explicit HTTP outcomes with no hidden retries or mailbox execution per D-40-01 and D-40-21.
    - Test 3: the Plug hands a normalized `%InboundMessage{}` plus internal evidence bundle to the persistence handoff seam without widening the adopter-facing provider contract per D-40-05.
  </behavior>
  <action>Implement `MailglassInbound.Ingress.Plug` as the single ingress choke point for Postmark-backed requests, following the ordering and explicit response-mapping discipline of `Mailglass.Webhook.Plug`. It must read the exact raw body, verify before tenant resolution, normalize through the sealed provider module, and return a compact internal handoff payload for Plan 40-02 to persist; use a forward reference if needed, but do not inline persistence logic here. Keep the provider seam internal per D-40-05, keep request outcomes explicit per D-40-21, and stop at `verify -> normalize -> handoff` per D-40-01. Do not invoke `Mailbox.process/1`, start async execution, create `ReplayRun` rows, or broaden the router/mailbox contract in any way per D-40-01, D-40-02, and D-40-20.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Postmark requests can enter through one package-local Plug that verifies first, normalizes into the locked canonical struct, and exposes explicit success or rejection outcomes without touching mailbox execution.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external Postmark request -> inbound plug | Untrusted network input crosses into package code and must be verified before any tenant, routing, or storage work happens. |
| provider payload -> canonical message | Provider-specific fields are converted into the stable inbound contract that adopters and later plans will trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Spoofing | `MailglassInbound.Ingress.Providers.Postmark.verify!/3` | mitigate | Enforce Basic Auth by default, keep IP allowlisting opt-in, and fail closed on missing or malformed credentials per D-40-07. |
| T-40-02 | Tampering | `MailglassInbound.Ingress.CachingBodyReader` and `Plug` | mitigate | Preserve exact raw bytes in `conn.private[:raw_body]` and reject missing body-reader wiring so verification never runs on reconstructed payload state per D-40-06. |
| T-40-03 | Elevation of Privilege / Information Disclosure | Postmark normalization | mitigate | Normalize only into the locked `%InboundMessage{}` fields and keep attachment bytes, verification facts, stripped-body helpers, and provider-only extras in internal evidence data per D-40-08 through D-40-12. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/caching_body_reader_test.exs test/mailglass_inbound/ingress/postmark_provider_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors`
</verification>

<success_criteria>
`mailglass_inbound` can accept authentic Postmark inbound requests through one verify-first Plug path and produce the locked canonical `%InboundMessage{}` without widening the public contract or implying mailbox execution already exists.
</success_criteria>

<output>
After completion, create `.planning/phases/40-postmark-ingress-and-replayable-persistence/40-01-SUMMARY.md`
</output>
