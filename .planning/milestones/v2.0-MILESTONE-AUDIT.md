# v2.0 Postgres Schema Isolation — Milestone Audit

**Status: passed** · Shipped 2026-07-04 · **mailglass 2.0.0 / mailglass_admin 2.0.0 / mailglass_inbound 2.0.0 live on Hex**

## Scope (phases 132–137, ~20 reqs across SCHEMA/MIGR/UPG/REL)

| Phase | Deliverable |
|-------|-------------|
| 132 | Config + `Mailglass.Identifier` foundation — `config :mailglass, :schema` (default `"mailglass"`), boot-validated `:persistent_term`-cached `Config.schema/0`, mirror on `:mailglass_inbound` |
| 133 | Runtime facade `prefix:` injection across the read/write surface (NOT `@schema_prefix`); `Ecto.Multi` prefix threaded per-builder |
| 134 | Schema-qualified DDL, immutability trigger/function + CHECK under the isolated schema (MIGR-05 keystone: 45A01 immutability under a non-public schema, no `search_path` pin); `citext` stays in `public` |
| 135 | Inbound schema alignment (`config :mailglass_inbound, :schema`), programmatic inbound migrations |
| 136 | `mix mailglass.upgrade.v2_schema` codemod (Route B `ALTER TABLE … SET SCHEMA` + `lock_timeout` + trigger recreation + `down/0`); `guides/upgrading-to-v2_0.md`; `:schema` contract in `api_stability.md` |
| 137 | Linked 2.0/2.0/2.0 release ceremony + this closeout |

## Verification basis

- **Full CI green on the release** (PR #119 head): all 5 required leaves (compile_no_optional_deps,
  installer_host_smoke, support_contract_core, support_contract_admin, trust_lane_repo_head) + advisory
  lanes green, except the pre-existing Core Full Suite Advisory flake (bucket-(b), red on v1.14/v1.15 too).
- **Consumer smoke green**: a fresh app resolves and fetches the `{:mailglass, "~> 2.0"}` +
  `mailglass_admin`/`mailglass_inbound` `~> 2.0` triple from Hex at 2.0.0 with compatible deps.
- **All three packages live on Hex at 2.0.0** (verified via Hex API, not run status).
- **Reference baseline advanced** to `~> 2.0` (host_app + demo_app), hex-first clean-baseline guard green.

## Load-bearing outcomes

- MIGR-05 keystone proven: the append-only immutability trigger/function fires (SQLSTATE 45A01) under a
  non-`public` schema with NO `search_path` pin — every write routes through explicit facade `prefix:`.
- Decision (3) validated the hard way: the isolation TEST HARNESS's connection `search_path` was found to
  be a false-green generator that had MASKED a real `lib/mailglass/webhook/ingest.ex` missing-`prefix:`
  bug (fixed by threading `Repo.multi_opts()`→`prefix: Config.schema()` through the ingest Multi).

## Fix-forward narrative

The never-pushed 132–136 body's first full CI surfaced ~7 cross-phase regressions, fixed by a 4-round
`/gsd-debug` campaign (`debug/resolved/schema-isolation-regressions.md`). After merge, the publish
fan-out surfaced 3 packaging defects (core file-allowlist, inbound `priv`/allowlist, admin stale `~> 1.1`
inbound pin), each recovered via force tag-move + `workflow_dispatch` re-publish. Full mechanics captured
in the `project_v2_0_release_blocked` memory + `137-RELEASE-BLOCKED-DEBUG-SCOPE.md`.

## Tracked follow-up (post-milestone, non-blocking)

- **bucket-(b)**: ~48 test-fixture files still rely on the harness `search_path` for raw
  `TestRepo.insert(...)` reads/writes; fully dropping the harness `search_path` (relying solely on facade
  `prefix:`) is blocked on that sweep. Core Full Suite Advisory stays the known persistent flake until then.

_(Scope counted directly from phases 132–137 to avoid `milestone.complete` 999.x leftover-dir inflation.)_
