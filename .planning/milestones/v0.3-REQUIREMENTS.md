# Requirements Archive: v0.3 Webhook Coverage Complete

**Archived:** 2026-04-30
**Status:** SHIPPED

For future requirements, start the next milestone with `$gsd-new-milestone`.

---

# Requirements: mailglass v0.3

**Status:** ✅ SHIPPED 2026-04-30
**Defined:** 2026-04-29
**Archived:** 2026-04-30
**Core Value:** Email you can see, audit, and trust before it ships.

> This is the archived v0.3 milestone requirements snapshot. All v0.3 requirements closed in the current tree and shipped on the `0.3.x` line (`mailglass` / `mailglass_admin` `0.3.2` live on Hex.pm).

## v0.3 Requirements

### Resend
- [x] **RESEND-01**: Webhook plug verifies Svix HMAC-SHA256 signature using `svix-id`, `svix-timestamp`, and raw request body.
- [x] **RESEND-02**: Webhook maps Resend events (delivered, bounced, complained) to `mailglass` normalized taxonomy.

### Mailgun
- [x] **MAILGUN-01**: Webhook plug verifies HMAC-SHA256 signature using `timestamp`, `token`, and webhook signing key.
- [x] **MAILGUN-02**: Token caching mechanism prevents replay attacks for previously verified tokens.
- [x] **MAILGUN-03**: Webhook maps Mailgun events to `mailglass` normalized taxonomy.

### SES
- [x] **SES-01**: Webhook plug parses SNS payloads arriving with `text/plain` Content-Type.
- [x] **SES-02**: System automatically performs HTTP GET to `SubscribeURL` upon receiving `SubscriptionConfirmation` events. Implementation is satisfied via the accepted D-07 TopicArn+Token override recorded in `16-VERIFICATION.md`.
- [x] **SES-03**: Webhook plug verifies RSA-SHA1/SHA256 signatures using X.509 certificates fetched from AWS.
- [x] **SES-04**: X.509 certificates are fetched via `:httpc` and cached in `:ets` to avoid synchronous network I/O per webhook.
- [x] **SES-05**: Webhook maps SES events inside the SNS `Message` envelope to `mailglass` normalized taxonomy.

### Release
- [x] **DELIV-04**: Webhook normalization covers Postmark, SendGrid, Mailgun, SES, and Resend, and the v0.3.x release line is published with updated provider guides.

## Future Requirements

- v0.4 milestone definition deferred until `$gsd-new-milestone`

## Out of Scope

- **Inbound message processing** (Action Mailbox equivalent) — deferred to v0.5+ sibling package.
- **Provider-specific sending implementations** — Mailglass remains a transactional email framework over Swoosh; adapter sending stays in Swoosh/provider land.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| RESEND-01 | Phase 14 (implemented), Phase 17 (verification), Phase 18 (release surface evidence) | Complete |
| RESEND-02 | Phase 14 (implemented), Phase 17 (verification), Phase 18 (release surface evidence) | Complete |
| MAILGUN-01 | Phase 15 | Complete |
| MAILGUN-02 | Phase 15 | Complete |
| MAILGUN-03 | Phase 15 | Complete |
| SES-01 | Phase 16 (unit), Phase 19 (E2E ingest fix) | Complete |
| SES-02 | Phase 16 (implemented per D-07), Phase 21 (formal override paperwork) | Complete |
| SES-03 | Phase 16 (unit), Phase 19 (E2E ingest fix) | Complete |
| SES-04 | Phase 16 (unit), Phase 19 (plug-level regression coverage) | Complete |
| SES-05 | Phase 16 (unit), Phase 19 (E2E ingest fix) | Complete |
| DELIV-04 | Phase 18 | Complete |
