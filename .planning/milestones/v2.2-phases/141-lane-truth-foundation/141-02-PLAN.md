---
phase: 141-lane-truth-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/TOOLING-DEFECTS.md
  - .planning/REQUIREMENTS.md
  - CONTRIBUTING.md
autonomous: true
requirements: [HIST-01, TRUTH-09]

must_haves:
  truths:
    - "`.planning/TOOLING-DEFECTS.md` exists at the `.planning/` root, carries no `milestone:` frontmatter key, and states in its own text why it is deliberately milestone-independent."
    - "The `phases.clear` defect record carries a recognize-on-screen symptom line: `cleared: N` reported with no `milestones/<version>-phases/` directory written."
    - "The record's PRIMARY mitigation is a post-condition check (list `.planning/milestones/<outgoing>-phases/` and compare the file count against `git show --stat HEAD` before committing); `--archive-version` is recorded as necessary but insufficient, with the 2026-07-28 evidence that it was passed and the defect fired anyway."
    - "`.planning/REQUIREMENTS.md`'s TRUTH-09 text describes the same classification model the implementation ships, so the requirement does not itself become a sixth disagreeing registry."
    - "`CONTRIBUTING.md` no longer claims five required status checks; it names the two true contexts and points at `MAINTAINING.md` § \"Required Checks\" rather than restating a list that can drift again."
    - "No file under `.planning/milestones/v2.0-phases/` or `.planning/milestones/v2.1-phases/` is created, modified, or deleted by this plan."
  artifacts:
    - .planning/TOOLING-DEFECTS.md
    - .planning/REQUIREMENTS.md
    - CONTRIBUTING.md
  key_links:
    - "`CONTRIBUTING.md` is a repo-root file; `release-please-config.json` claims root paths, so a `feat:`/`fix:` commit here can trigger an unintended release train (the 1.6.2 incident). The conventional-commit type MUST be `docs:`."
    - "`CONTRIBUTING.md`'s new pointer targets `MAINTAINING.md` § \"Required Checks\" by SECTION, never by line number — that section is rewritten in plan 141-06 and a line range would go stale immediately."
  prohibitions:
    - statement: "MUST NOT record a classification, disposition, or mitigation in any repo document that is not machine-verified or evidence-backed. Writing a confident claim nobody re-checks is the exact defect class this phase exists to remove — D-19 prescribed `--archive-version` as the fix, and the commit body of `70099869` shows it was passed while the defect fired anyway."
      status: unverified
      verification: judgment
    - statement: "MUST NOT re-derive, overwrite, regenerate, or 'restore' any file under `.planning/milestones/v2.0-phases/` or `.planning/milestones/v2.1-phases/`. Both restorations were verified byte-exact on 2026-07-28 (48/48 and 39/39); re-deriving from a stale blob would turn a closed defect into a live one."
      status: unverified
      verification: test
---

<objective>
Write down the two truths this phase can record without touching a single line of CI: the `phases.clear`
tooling defect that deleted planning artifacts twice, and the two requirement/doc surfaces whose text
disagrees with reality.

Purpose: HIST-01's restoration half is already complete and byte-exact (RESEARCH.md **F7** re-verified
48/48 files for v2.0's 132-137 and 39/39 `R100` renames for v2.1's 138-140), so the **only** remaining
HIST-01 work is authoring the defect record. Today that defect exists solely in the commit body of
`70099869`; a grep of `.planning/**/*.md` + `CLAUDE.md` finds `phases.clear` nowhere except the
requirement text itself. A commit message is not "somewhere a future run would recognize it as a repeat"
— the defect fired twice and the second catch was luck. Recurrence risk is live: v2.2's own milestone
close invokes the same command against phases 141-144.

Output: `.planning/TOOLING-DEFECTS.md` (new), an amended TRUTH-09 in `.planning/REQUIREMENTS.md`, and a
corrected `CONTRIBUTING.md` branch-protection claim.

**Decisions implemented here:** D-18 (the only remaining HIST-01 work is writing the defect record at
`.planning/TOOLING-DEFECTS.md`, at the `.planning/` root, deliberately milestone-independent, carrying a
recognize-on-re-run symptom line) · D-19 (a dated note with mitigations, not a blanket warning — with
its mitigation ranking corrected by RESEARCH F6) · D-16 and D-17 (budget zero restoration work; do not
restore files that never existed) · D-03 (amend TRUTH-09's text in `.planning/REQUIREMENTS.md` to the
model this phase ships) · D-14 (`CONTRIBUTING.md:116` is corrected — all five claimed contexts are
wrong) · D-10 (the live-verified two-context branch-protection truth the correction restates).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-lane-truth-foundation/141-RESEARCH.md
@.planning/phases/141-lane-truth-foundation/141-PATTERNS.md
</context>

## Decisions recorded here (never made by omission)

1. **D-19's prescribed primary mitigation is REFUTED and demoted, not repeated.** D-19 says the record
   should prescribe "pass `--archive-version` explicitly". The commit body of `70099869` states
   verbatim that `gsd-tools query phases.clear --archive-version v2.1` reported `cleared: 3` but deleted
   the three phase directories without writing the archive. The flag **was** passed and the defect fired
   anyway. Recording it as *the* fix would write down a mitigation already known not to work, inside the
   document whose entire purpose is stopping a third recurrence. The post-condition check is promoted to
   sole primary mitigation; the flag is recorded as necessary-but-insufficient with its evidence.
   D-19's framing (a dated note with mitigations, not a blanket "this command destroys data" warning) is
   preserved — only the ranking changes, and it changes on repo evidence, not opinion.

2. **Zero restoration work is budgeted.** D-16/D-17 are confirmed by RESEARCH.md F7 with per-file
   `git hash-object` comparison. Phases 134 and 136 genuinely never had `CONTEXT.md` /
   `DISCUSSION-LOG.md` — they went straight to planning (`dc26471c docs(134): create phase plan`). Do
   not "restore" files that never existed.

3. **Commit type is `docs:` — non-releasing.** `CONTRIBUTING.md` is a repo-root file, and per
   `CLAUDE.md` the 1.6.2 accidental release train was triggered by root-path commits claiming all paths
   in `release-please-config.json`. v2.2 is a repo-artifact milestone with **no release intended**.

## Artifacts this phase produces

Symbols created by **this phase** (across all six plans) — new, so no prior source grounding exists:

| Symbol | Kind | Created in |
|---|---|---|
| `Mailglass.CIYaml` / `job_names/1` / `matrix_job_names/1` | module + functions | 141-01 |
| `Mailglass.Scripts.LaneClassificationDriftTest` | module | 141-01 |
| `verify.ci_lane_contract` (+ its `preferred_cli_env` entry) | mix alias | 141-01 |
| `Run CI lane-contract meta-tests` | `ci.yml` step name | 141-01 |
| `.planning/TOOLING-DEFECTS.md` | file | 141-02 |
| `TOOL-01` | stable defect ID | 141-02 |
| `conformance_gates` | `ci.yml` job id | 141-03 |
| `Design System Conformance (shell gates)` | `ci.yml` job display name | 141-03 |
| `@publish_gating_lanes` / `publish_gating_lanes/0` | attribute + function | 141-04 |
| `@advisory_classified_lanes` / `advisory_classified_lanes/0` | attribute + function | 141-04 |
| `@structural_lanes` / `structural_lanes/0` | attribute + function | 141-04 |
| `Mailglass.CILanes.all_classified_lanes/0` | function | 141-04 |
| `PUBLISH_GATING_LANES`, `STRUCTURAL_LANES`, `startsWithAny`, `classify` | `publish-hex.yml` JS | 141-04 |
| `MAINTAINING.md` § "Required Checks" 24-row disposition table | doc structure | 141-06 |

## Flagged Assumptions (unresolved edge-probe rows)

- **HIST-01 / `unclassified`** — the deterministic edge probe returned `unclassified` for HIST-01
  ("review manually") and is left **UNRESOLVED**, not auto-backstopped. Surfaced assumption: RESEARCH.md
  F7 verified the restoration byte-exact on **2026-07-28** (48/48 by `git hash-object`, 39/39 `R100`
  renames). This plan re-runs only the two **file-count** commands as closing evidence — it does **not**
  re-run a per-file SHA comparison. If any restored artifact were altered between 2026-07-28 and
  execution, this phase would not detect it. Not resolved here.

<tasks>

<task type="auto">
  <name>Task 1: Author `.planning/TOOLING-DEFECTS.md` with the `phases.clear` defect record (TOOL-01)</name>

  <files>.planning/TOOLING-DEFECTS.md</files>

  <read_first>
    - `.planning/RATCHET-GAP-REGISTER.md` (`:1-31`) — analog 1: YAML frontmatter block, an H1 repeating
      the file name, then a blockquote scoping/contract note. See `141-PATTERNS.md`
      § "`.planning/TOOLING-DEFECTS.md` (CREATE — planning register)" for the verbatim excerpt.
    - `.planning/research/v1.14/DEFECT-REGISTER.md` (`:1-40`) — analog 2: bolded-label metadata line,
      a "How to read this register" legend section, and the per-entry prose shape with a stable ID.
    - `141-RESEARCH.md` § "F6" (the refutation and its evidence) and § "F7" (the two occurrences with
      their exact commit SHAs and file counts)
    - `.planning/REQUIREMENTS.md` (`:136-139`) — HIST-01's full text
  </read_first>

  <reversibility rating="reversible">
    A new standalone `.planning/` markdown file with a stable ID namespace; deleting or restructuring it
    later costs nothing outside this repo.
  </reversibility>

  <action>
    Create `.planning/TOOLING-DEFECTS.md` at the `.planning/` **root** — deliberately not under a
    milestone directory.

    **Frontmatter.** Copy `RATCHET-GAP-REGISTER.md`'s shape but **omit the `milestone:` key**: keep
    `artifact: tooling-defects`, `stable_ids: true`, `created: 2026-07-28`. Both existing register
    precedents in this repo are milestone-scoped by design; this one deliberately breaks that pattern.

    **H1 + blockquote.** Title the register after the file. The blockquote must state *why* it is
    milestone-independent: a defect record whose only job is to be recognized on a future run must
    survive a milestone boundary, and both precedent registers (`RATCHET-GAP-REGISTER.md`,
    `.planning/research/v1.14/DEFECT-REGISTER.md`) would be archived away with their milestone.

    **"How to read this register" section.** A short legend: stable `TOOL-NN` IDs never renumbered; each
    entry carries **Symptom**, **Evidence**, **Occurrences**, **Mitigations**, **Status**; the Symptom
    line is the load-bearing content because someone re-running the command must recognize the failure
    from what they see on screen.

    **Entry `TOOL-01` — `gsd-tools query phases.clear` deletes phase directories without writing the
    archive.** Author it with these labelled parts:
    - **Symptom** (the load-bearing line, phrase it as the operator sees it): `cleared: N` is reported
      with no `milestones/<version>-phases/` directory written.
    - **Occurrences** — two, both in this repo:
      1. v2.0 phases 132-137. `b5fed519` ("docs: start milestone v2.1") deleted 48 files under
         `.planning/phases/132-*` … `137-*`; `a629fb82` restored all 48 under
         `.planning/milestones/v2.0-phases/`. Per-file `git hash-object` comparison against
         `b5fed519^`: `ok=48 missing=0 differ=0`.
      2. v2.1 phases 138-140, during v2.2's own opening on 2026-07-28. `70099869` moved 39 files;
         `git show --name-status -M 70099869` reports all 39 as `R100`. The delete and the by-hand
         repair landed in the same commit.
    - **Evidence** — quote the relevant sentence from `70099869`'s commit body, which is the only place
      this defect was recorded before this file existed.
    - **Mitigations**, explicitly ranked:
      - **PRIMARY (post-condition check).** After any `phases.clear`, list
        `.planning/milestones/<outgoing>-phases/` and compare its file count against
        `git show --stat HEAD` **before committing**. A count mismatch, or a missing directory, is the
        defect.
      - **Necessary but insufficient — does NOT prevent this.** Passing `--archive-version <outgoing>`
        explicitly. It was passed on the 2026-07-28 occurrence and the archive was still not written.
        Record it because `new-milestone` runs `state.milestone-switch` **before** `phases.clear`, so a
        live `STATE.md` read would file the archive under the *incoming* milestone — the flag is still
        required for correct filing; it is simply not a fix for this defect.
    - **Status / tool version observed** — `gsd-sdk v1.42.3`
      (`~/.claude/gsd-core/bin/lib/milestone.cjs`), which archives rather than hard-deletes (`#1871`),
      refuses to run against uncommitted phase work (`#1447`), and accepts `--archive-version` (`#2288`).
      Note the **live recurrence risk**: v2.2's own milestone close invokes the same command against
      phases 141-144.

    Keep the register's prose in the project brand voice — specific and composed, never "Oops!". Do not
    write a blanket "this command destroys data" warning; D-19's dated-note-with-mitigations framing
    stands.
  </action>

  <verify>
    <automated>test -f .planning/TOOLING-DEFECTS.md && grep -q 'cleared:' .planning/TOOLING-DEFECTS.md && grep -q 'TOOL-01' .planning/TOOLING-DEFECTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/TOOLING-DEFECTS.md` succeeds.
    - `grep -c 'cleared:' .planning/TOOLING-DEFECTS.md` returns at least `1`.
    - `grep -c 'milestones/<version>-phases/' .planning/TOOLING-DEFECTS.md` returns at least `1`.
    - `grep -c 'TOOL-01' .planning/TOOLING-DEFECTS.md` returns at least `1`.
    - `grep -c 'necessary but insufficient' .planning/TOOLING-DEFECTS.md` returns at least `1`
      (the `--archive-version` demotion is explicit, not implied by omission).
    - `grep -c '70099869' .planning/TOOLING-DEFECTS.md` returns at least `1` and
      `grep -c 'b5fed519' .planning/TOOLING-DEFECTS.md` returns at least `1` (both occurrences cited).
    - `awk '/^---$/{n++; next} n==1' .planning/TOOLING-DEFECTS.md | grep -c '^milestone:'` returns `0`
      — the frontmatter is deliberately milestone-independent.
    - `find .planning/milestones/v2.0-phases -type f | wc -l` returns `48` and
      `find .planning/milestones/v2.1-phases -type f | wc -l` returns `39` — unchanged by this task.
    - `git status --porcelain .planning/milestones/` produces no output.
  </acceptance_criteria>

  <done>
    A future operator running `gsd-tools query phases.clear` who sees `cleared: N` with no archive
    directory can find this defect by grepping `.planning/` for `phases.clear`, recognize it as a
    repeat rather than a discovery, and apply a mitigation that is not already known to fail.
    HIST-01 is closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend TRUTH-09's text in `.planning/REQUIREMENTS.md` to the shipped classification model</name>

  <files>.planning/REQUIREMENTS.md</files>

  <!-- planner-discipline-allow: The 9+ -->

  <read_first>
    - `.planning/REQUIREMENTS.md` (`:96-128`) — the TRUTH section; TRUTH-09 is at `:98-102`. See
      `141-PATTERNS.md` § "`.planning/REQUIREMENTS.md` (MODIFY — amend TRUTH-09)" for the verbatim
      current text and the sibling (TRUTH-05, `:126-128`) whose italic cross-reference footer form is
      the model for noting the amendment.
    - `141-CONTEXT.md` D-02 and D-03 — the user-confirmed "Name the third tier" decision and the
      instruction to amend the requirement rather than silently satisfy one and violate the other
    - `141-RESEARCH.md` § "Lane Classification Ledger" — the four buckets and their exact counts
      (5 required · 4 advisory · 13 publish-gating · 2 structural = 24)
  </read_first>

  <reversibility rating="reversible">
    A requirement-text edit inside `.planning/`, revertible with `git revert`; no code, CI, or external
    system depends on the wording.
  </reversibility>

  <action>
    Rewrite TRUTH-09's body in place, preserving the surrounding format exactly: the `- [ ] **TRUTH-09**: `
    prefix, two-space continuation indent, and ~110-column wrapping.

    The new text must say: every `ci.yml` job is explicitly classified into exactly one named bucket —
    **merge-gating (required)**, **publish-gating**, or **advisory** for check lanes, plus a
    **structural** bucket for the two jobs that are not check lanes (`changes` / `Detect Non-Doc Changes`
    and `ci_green` / `CI Green`) — and no job may sit in none of them and thereby block publish by
    accident.

    Append an italic cross-reference footer in the style of TRUTH-05's, recording the amendment and its
    reason in one sentence each: the original wording said "merge-gating **or** advisory"; a two-bucket
    model would either let a Hex publish proceed on red Dialyzer / red trust-evidence lanes or promote
    them to merge-gating, contradicting D-04 for `Trust Lane Clean Baseline` and lengthening every PR's
    critical path. The named publish-gating bucket preserves today's effective publish posture
    byte-for-byte. Cite `.planning/phases/141-lane-truth-foundation/141-CONTEXT.md` D-02/D-03 as the
    decision record.

    Replace the stale "The 9+ currently-unclassified jobs (…)" enumeration with the accurate list, which
    now includes `Branch Protection Advisory` and the new `Design System Conformance` lane. Do not touch
    any other requirement in the file.
  </action>

  <verify>
    <automated>grep -q 'publish-gating' .planning/REQUIREMENTS.md && grep -q 'structural' .planning/REQUIREMENTS.md && grep -c 'TRUTH-09' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'as merge-gating or advisory' .planning/REQUIREMENTS.md` returns `0` — the two-bucket
      wording is gone. Measured at HEAD on 2026-07-28: `1`.
    - `grep -c 'The 9+' .planning/REQUIREMENTS.md` returns `0` — the stale
      "9+ currently-unclassified jobs" enumeration is replaced. Measured at HEAD on 2026-07-28: `1`.
      (Do **not** also gate on `grep -c 'classified\s*$'`. That matches TRUTH-09's line 98 only because
      the current 107-column wrap happens to end on the word "classified"; the prescribed replacement
      sentence still opens "…is explicitly classified into exactly one named bucket" and would wrap at
      the same point, so the gate would fire on a *correct* edit. Measured at HEAD: `1`.)
    - `awk '/\*\*TRUTH-09\*\*/{f=1} f && /^- \[ \] \*\*TRUTH-07\*\*/{f=0} f' .planning/REQUIREMENTS.md | grep -c 'publish-gating'`
      returns at least `1`.
    - `awk '/\*\*TRUTH-09\*\*/{f=1} f && /^- \[ \] \*\*TRUTH-07\*\*/{f=0} f' .planning/REQUIREMENTS.md | grep -c 'structural'`
      returns at least `1`.
    - `awk '/\*\*TRUTH-09\*\*/{f=1} f && /^- \[ \] \*\*TRUTH-07\*\*/{f=0} f' .planning/REQUIREMENTS.md | grep -c '141-CONTEXT.md'`
      returns at least `1` — the amendment cites its decision record.
    - `grep -c '^- \[ \] \*\*TRUTH-' .planning/REQUIREMENTS.md` returns the same value as before the edit
      (no requirement added or removed).
    - `grep -c '^  ' .planning/REQUIREMENTS.md` is non-zero and the TRUTH-09 block's continuation lines
      all begin with exactly two spaces (`awk` block above contains no line starting with 3+ spaces).
  </acceptance_criteria>

  <done>
    TRUTH-09's recorded text and the model this phase ships are the same model — the requirement does
    not itself become a sixth disagreeing registry (D-03).
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct `CONTRIBUTING.md`'s branch-protection claim — the fifth registry</name>

  <files>CONTRIBUTING.md</files>

  <read_first>
    - `CONTRIBUTING.md` (`:110-125`) — the file being edited; the wrong claim spans `:116-119`. See
      `141-PATTERNS.md` § "`CONTRIBUTING.md` (MODIFY — correct the fifth registry)" for the verbatim
      current text.
    - `scripts/setup_branch_protection.sh` (`:17-20`) — `REQUIRED_CHECKS=("CI Green" "Guard Release Trigger")`,
      the correct source
    - `test/scripts/required_checks_test.exs` (`:45-58`) — GATE-01, which asserts exactly that two-element
      expected set
    - `MAINTAINING.md` (`:152-158`) — the only accurate registry in that file today, and the section the
      new pointer targets
  </read_first>

  <reversibility rating="reversible">
    Prose-only edit to a repo-root doc; revertible with `git revert` and verified live by D-10's
    `gh api .../branches/main/protection` call.
  </reversibility>

  <action>
    Replace the sentence at `CONTRIBUTING.md:116-119` that claims `main` requires five status checks.
    All five named contexts are wrong: live branch protection was re-verified on 2026-07-28 via
    `gh api repos/szTheory/mailglass/branches/main/protection --jq '.required_status_checks.contexts'`
    and returned exactly `["CI Green","Guard Release Trigger"]`.

    The replacement must (a) name exactly those two contexts, (b) state explicitly that individual
    `ci.yml` leaf lanes are deliberately **not** required contexts, and (c) point at
    `MAINTAINING.md` § "Required Checks" **by section name, never by line number**, for every lane's
    classification and disposition. Reason for the section-citation form: `MAINTAINING.md`'s Required
    Checks section is rewritten in plan 141-06, so a hardcoded line range would go stale the moment it
    was written — that is Pitfall 9, the confidently-worded citation nobody re-checks, and this phase
    exists to remove that class of defect.

    Preserve the surrounding two sentences about `scripts/setup_branch_protection.sh` and
    `.github/workflows/branch-protection-drift.yml`, which are accurate. Change nothing else in the file.
  </action>

  <verify>
    <automated>grep -q 'CI Green' CONTRIBUTING.md && grep -q 'Guard Release Trigger' CONTRIBUTING.md && ! grep -v '^#' CONTRIBUTING.md | grep -q 'PR title (semantic)'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v '^#' CONTRIBUTING.md | grep -c 'PR title (semantic)'` returns `0`.
    - `grep -c 'CI Green' CONTRIBUTING.md` returns at least `1`.
    - `grep -c 'Guard Release Trigger' CONTRIBUTING.md` returns at least `1`.
    - `grep -c 'MAINTAINING.md' CONTRIBUTING.md` returns at least `1` and the new pointer text contains
      the literal `Required Checks` with no digit-range citation:
      `grep -c 'MAINTAINING.md.*:[0-9]' CONTRIBUTING.md` returns `0`.
    - `grep -c 'setup_branch_protection.sh' CONTRIBUTING.md` returns at least `1` and
      `grep -c 'branch-protection-drift.yml' CONTRIBUTING.md` returns at least `1` — the surrounding
      accurate sentences survive.
    - `git diff --numstat CONTRIBUTING.md` shows fewer than 15 changed lines total.
  </acceptance_criteria>

  <done>
    The fifth registry agrees with live branch protection and with `scripts/setup_branch_protection.sh`,
    and it defers to one authoritative section rather than restating a list that can drift again (D-14).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-root commit → `release-please-config.json` path claim → hex.pm | A root-path commit with a releasing conventional-commit type crosses here and can cut an unintended release train. |
| Recorded repo documentation → future maintainer's action | Untrusted-by-default prose crosses here; a confidently-worded but wrong claim causes the wrong action to be taken (this milestone's originating incident class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-08 | Elevation of privilege | `CONTRIBUTING.md` (repo root) + `release-please-config.json` | high | mitigate | The root `.` path claim caused the 1.6.2 accidental release train (`CLAUDE.md`). v2.2 ships no Hex release. Mitigation: this plan's commits use the `docs:` conventional-commit type only — never `feat:` or `fix:`. Verified post-commit with `git log -1 --format=%s` starting with `docs`. |
| T-141-03 | Repudiation | `.planning/TOOLING-DEFECTS.md`, `CONTRIBUTING.md`, `.planning/REQUIREMENTS.md` | medium | mitigate | A recorded claim with no evidence trail is indistinguishable from a wrong one. Every factual claim authored here cites a commit SHA, a script line, or a live API result: `b5fed519`/`a629fb82`/`70099869` for the occurrences, `setup_branch_protection.sh:17-20` + the 2026-07-28 `gh api` result for the contexts. |
| T-141-09 | Tampering | `.planning/milestones/v2.0-phases/`, `.planning/milestones/v2.1-phases/` | high | mitigate | Re-deriving restored artifacts from a stale blob would overwrite good files and turn a closed defect into a live one (Pitfall 5). Mitigation: zero restoration tasks are budgeted, and `git status --porcelain .planning/milestones/` is asserted empty in `<acceptance_criteria>`. |
| T-141-SC | Tampering | npm/pip/cargo installs | high | accept | Not applicable — this plan installs no packages and adds no GitHub Action. `.planning/research/v2.2/SUMMARY.md:32` locks "no new dependency" for the whole milestone. |
</threat_model>

<verification>
- `test -f .planning/TOOLING-DEFECTS.md` succeeds and `grep -c 'cleared:' .planning/TOOLING-DEFECTS.md` ≥ 1.
- `find .planning/milestones/v2.0-phases -type f | wc -l` returns `48`.
- `find .planning/milestones/v2.1-phases -type f | wc -l` returns `39`.
- `git status --porcelain .planning/milestones/` produces no output.
- `git log --oneline -3 --format=%s` shows only `docs(...)`-typed commits for this plan.
</verification>

<success_criteria>
- ROADMAP criterion 5 is met: the artifacts are present (already true, re-confirmed) and the defect is
  written down where a future run of the same command would be recognized as a repeat.
- The two non-CI registries (`REQUIREMENTS.md` TRUTH-09, `CONTRIBUTING.md:116`) agree with reality.
- No `.planning/milestones/` file is touched.
</success_criteria>

<output>
Create `.planning/phases/141-lane-truth-foundation/141-02-SUMMARY.md` when done.
**Commit type MUST be `docs:`** (non-releasing) — see T-141-08. Record the two `find … | wc -l` outputs
in the SUMMARY as closing evidence for ROADMAP criterion 5.
</output>
