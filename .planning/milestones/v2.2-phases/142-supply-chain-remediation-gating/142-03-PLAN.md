---
phase: 142-supply-chain-remediation-gating
plan: 03
type: execute
wave: 2
depends_on: ["142-01"]
files_modified: []
autonomous: false
requirements: [VULN-05, VULN-03]

must_haves:
  truths:
    - "A real `ci.yml` PR run's `Hex Audit (Elixir 1.18 / OTP 27)` job log names BOTH `EEF-CVE-2026-43966` and `EEF-CVE-2026-43969` as detected in `mailglass_admin` AND suppressed by the allowlist — not merely 'no findings' (D-14's exact requirement)."
    - "A deliberate negative-control run (one `@entries` item temporarily removed from `Mailglass.SupplyChain.AcceptedAdvisories`) makes `mix mailglass.audit --kind hex` exit non-zero, naming that specific advisory — proving the allowlist genuinely filters rather than passing vacuously."
    - "142-04 (the atomic promotion) does not start until both observations above are recorded in this plan's SUMMARY.md with the actual log/output excerpts, not a paraphrase."
  artifacts: []
  key_links:
    - "This plan is the mechanical expression of D-14: 'Wave 2 must not start on Wave 1 merged — it starts on Wave 1 observed green with the accepted advisories actually in the lane's scope.' It is a standalone plan (not a task folded into 142-01 or 142-04) specifically so 142-04's atomic promotion commit has a real, recorded dependency gate in front of it, per the split-signal rule that a checkpoint and its gated implementation belong in separate plans."
  prohibitions: []
---

<objective>
Gate Wave 3 (the merge-gating promotion) on live evidence, not on "Plan 142-01 merged" — the exact
distinction D-14 draws. Confirm, on a real CI run, that `Hex Audit` is green BECAUSE the allowlist
suppressed two genuinely-detected findings, and confirm the allowlist can genuinely fail closed via a
deliberate negative control. Only then does 142-04 proceed.

Purpose: root `mix hex.audit` is clean today. A Wave 1 that quietly stayed root-only would report
green for the wrong reason and hand the promotion a false precondition — precisely the
vacuous-green class this whole milestone exists to eliminate (RESEARCH's Pitfall 1, applied at the
Wave boundary). This plan is the blocking checkpoint D-14 requires between Wave 1 and the promotion,
not a note in a SUMMARY.

**Decisions implemented here:** D-14 (Wave 2/promotion gates on observed-green-with-cowlib-in-scope,
not on merged) · D-15 (the negative-control proof, mirrored here as a live, human-observed run
supplementing 142-01's unit tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/142-supply-chain-remediation-gating/142-CONTEXT.md
@.planning/phases/142-supply-chain-remediation-gating/142-VALIDATION.md
@.planning/phases/142-supply-chain-remediation-gating/142-01-SUMMARY.md
</context>

## Artifacts this phase produces

(Reproduced from 142-01 for the mandatory per-plan section; this plan creates no new symbols.)

| Symbol | Kind | Created in |
|---|---|---|
| `Mailglass.SupplyChain.AcceptedAdvisories` | module | 142-01 |
| `Mix.Tasks.Mailglass.Audit` (`mix mailglass.audit --kind hex\|deps`) | Mix task | 142-01/02 |
| `Deps Audit (Elixir 1.18 / OTP 27)` | renamed display name | 142-04 |
| `## Dependency Advisory Triage` | `MAINTAINING.md` section | 142-05 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Wave 1 is green with cowlib genuinely in scope (D-14 gate)</name>
  <what-built>
    142-01 wired `Mailglass.SupplyChain.AcceptedAdvisories` + `mix mailglass.audit` into `ci.yml`'s
    `hex_audit` job (and, per 142-02's parallel plan, disposed of the dependabot backlog). Both
    lanes remain non-required through this checkpoint — `deps_audit_advisory` still keeps
    `continue-on-error: true`, `Hex Audit` is still publish-gating, not merge-gating.
  </what-built>
  <how-to-verify>
    1. Confirm 142-01's commit(s) are pushed and a `ci.yml` run has completed on that SHA (open the
       PR or push directly if working on a protected branch; trigger via a no-op commit if needed).
    2. Open the `Hex Audit (Elixir 1.18 / OTP 27)` job log for that run. Confirm the log's `Run Hex
       audit` step output explicitly names BOTH `EEF-CVE-2026-43966` and `EEF-CVE-2026-43969` for
       `mailglass_admin`, followed by the "accepted-advisories allowlist ... delivery allowed"
       message — not a bare "no findings" or empty log. Paste the exact excerpt into this plan's
       SUMMARY.md.
    3. Locally, perform the negative control: temporarily comment out or delete ONE entry from
       `Mailglass.SupplyChain.AcceptedAdvisories`'s `@entries` (e.g. the `EEF-CVE-2026-43969` entry),
       run `mix mailglass.audit --kind hex` from the repo root, and confirm it now exits non-zero,
       naming that specific advisory in the blocking output (not a generic failure). Revert the
       change (`git checkout -- lib/mailglass/supply_chain/accepted_advisories.ex`) and confirm `git
       status --short` is clean before proceeding. Paste the negative-control output into SUMMARY.md.
    4. Only after both excerpts are recorded, resume — this unblocks 142-04.
  </how-to-verify>
  <resume-signal>
    Paste both observed excerpts (the live CI job log naming both suppressed advisories, and the
    local negative-control failure output), or type "approved" once both are recorded in SUMMARY.md.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 1 merged state → Wave 3 promotion precondition | The exact boundary D-14 exists to make explicit: "merged" is not "verified green for the right reason." |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-05 | Denial of Service (self-inflicted) | `ci_green.needs` / `Mailglass.CILanes.required_lanes/0` (142-04's future edit) | high | mitigate | This checkpoint is the mitigation itself: 142-04 does not start until this plan's SUMMARY.md records live evidence the promoted lane is genuinely green with cowlib in scope, preventing a promotion that immediately red-blocks every PR. |
</threat_model>

<verification>
- `142-03-SUMMARY.md` exists and contains both required excerpts (live CI log; local negative
  control), not paraphrases.
- `git status --short` is clean (the negative control was reverted).
</verification>

<success_criteria>
- D-14's gate is satisfied with recorded evidence, not assumed from "142-01 merged."
- 142-04 has an explicit, checked dependency (`depends_on: ["142-03"]`) rather than an implicit
  ordering note.
</success_criteria>

<output>
Create `.planning/phases/142-supply-chain-remediation-gating/142-03-SUMMARY.md` with both observed
excerpts verbatim.
</output>
</content>
