---
phase: 143-test-harness-truth
plan: 03
type: execute
wave: 2
depends_on: ["143-01"]
files_modified:
  - .planning/phases/143-test-harness-truth/143-MECHANISM.md
  - .planning/phases/143-test-harness-truth/143-LEDGER-public.txt
  - .planning/phases/143-test-harness-truth/143-LEDGER-mailglass.txt
  - test/scripts/mechanism_account_contract_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - test/support/ci_lanes.ex
autonomous: true
requirements: [HARNESS-01]

must_haves:
  truths:
    - "A written mechanism account exists that explains `{:badmatch, :already_shared}` end-to-end — from the Class B schema drift that makes `CitextProbe.run` raise, through the unguarded acquire/release window, to the surviving unlinked Agent and the victim's `MatchError` — and it cites the confirming CI run and job by ID."
    - "Both of D-04's falsifiable predictions are recorded pass or fail against real evidence: every `:already_shared` failure sits in an `async: false` module with zero in `async: true`, and the module immediately preceding the first `:already_shared` shows an acquisition with no matching release."
    - "The account is a three-class inventory across all four matrix legs, not a Class C narrative — the leaking module for Class A (baseline teardown) and Class B (`Config.schema()` drift) is named from the ledger, not assumed from the research candidates."
    - "The `--max-cases 1`-changes-nothing negative control is recorded once, with the reason: `ExUnit.Runner.async_loop/4` runs sync modules strictly after all async modules, one at a time, so there is no async/sync overlap to serialize away. Seed bisection is recorded as rejected for the same structural reason (`:auto` files heal, so leaker and victim are not adjacent)."
    - "The upstream artifacts no longer disagree with the evidence: `REQUIREMENTS.md` HARNESS-01, `ROADMAP.md` Phase 143 criterion 1, and `ci_lanes.ex`'s exclusions moduledoc each carry the recorded correction rather than a silent divergence."
  artifacts:
    - .planning/phases/143-test-harness-truth/143-MECHANISM.md
    - .planning/phases/143-test-harness-truth/143-LEDGER-public.txt
    - .planning/phases/143-test-harness-truth/143-LEDGER-mailglass.txt
    - test/scripts/mechanism_account_contract_test.exs
  key_links:
    - "`test/scripts/mechanism_account_contract_test.exs` is auto-collected by `verify.ci_lane_contract`'s `test test/scripts/` directory glob (`mix.exs:296-298`) into the REQUIRED `mix_task_tests` lane — no `mix.exs` change. A docs-contract assertion that runs in no lane enforces nothing."
    - "The ledger dumps are the evidence the account cites; the account is not accepted on narrative alone (D-04's artifact class b+)."
  prohibitions:
    - "MUST NOT record test data, recipient addresses, subjects, headers, or bound query-parameter values in the committed ledger dumps — module names, pool modes, schema names, relation names, counts, and signature atoms only."
    - "MUST NOT silently diverge from a requirement. Every correction to HARNESS-01, ROADMAP criterion 1, or the `ci_lanes.ex` moduledoc is written into the artifact with its reason, following the Phase 141 TRUTH-09 amendment precedent."
---

<objective>
Produce HARNESS-01's evidence artifact: a written mechanism account for all three leak classes, backed by
committed ledger dumps from instrumented pre-fix runs, with the upstream artifacts amended so they stop
contradicting it.

HARNESS-01's "empirically confirmed before the fix is written" bar for **Class C is already met** —
RESEARCH.md § "Mechanism Account" (`:248-468`) confirms the full chain from live CI logs (run
`30464215272`, job `90617762038`): `webhook_idempotency_convergence_test.exs:58` (`CitextProbe.run`) raises
`42P01` *between* the `start_owner!(shared: true, ownership_timeout: 600_000)` at `:52` and the `on_exit` at
`:64`; the unlinked Agent survives holding `{:shared, pid}`; `manager.ex:153` returns `:already_shared`;
`sandbox.ex:458` badmatches; the victim's setup raises. This plan **cites and codifies** that account and
**widens it to Classes A and B**. It does not re-derive it.

Purpose: no fix is written until the mechanism is on paper with evidence behind it, and no requirement is
left saying something the evidence contradicts.
Output: `143-MECHANISM.md`, two ledger dumps, a docs-contract test in the required lane, and the D-31
amendments landed upstream.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/143-test-harness-truth/143-CONTEXT.md
@.planning/phases/143-test-harness-truth/143-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the instrumented pre-fix ledger on both schema axes</name>
  <precondition>PostgreSQL is reachable for `Mailglass.TestRepo` — `scripts/preflight_postgres.sh` exits 0. If it does not, fall back to dispatching `advisory-matrix.yml` on this branch and pulling the two 1.18 job logs with `gh api`, and record which path was used.</precondition>
  <files>.planning/phases/143-test-harness-truth/143-LEDGER-public.txt, .planning/phases/143-test-harness-truth/143-LEDGER-mailglass.txt</files>
  <read_first>
    - `test/support/suite_truth_formatter.ex` and `test/support/sandbox_ownership.ex` as delivered by plan `143-01` — the ledger record shape and the four violation class atoms
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "Empirical Confirmation Technique (HARNESS-01)" (`:1156-1194`) — what is already proven versus what Wave 1 must still produce, and the table of techniques that will not help
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "Current Four-Leg State" (`:190-244`) — the per-leg failure inventory and the relation-name-shape diagnosis table this ledger must corroborate or refute
    - `test/test_helper.exs` — how `MAILGLASS_SCHEMA` selects the axis
  </read_first>
  <action>
Run the full suite twice with instrumentation on, before any fix exists, and commit the ledgers.

Run one: `MAILGLASS_SANDBOX_TRACE=1 mix test --warnings-as-errors --exclude requires_workspace --seed 0`,
capturing combined output. Run two: the same command with `MAILGLASS_SCHEMA=mailglass` prefixed. The
`--seed 0` pin is a **stabilizer for artifact reproducibility only**, not a diagnostic — record that
distinction in the ledger header so no future reader mistakes it for a fix.

Extract the ledger section from each run into
`.planning/phases/143-test-harness-truth/143-LEDGER-public.txt` and `143-LEDGER-mailglass.txt`. Each file
begins with a header block recording: the exact command, the Elixir and OTP versions from
`elixir --version`, the resolved `Mailglass.Config.schema()` boot value, the ExUnit seed, the run's
`total`/`failures`/`excluded`/`skipped` counts as printed, and the wall-clock duration. Then the ledger
records in execution order, then a per-class tally (`:pool_mode_leaked`, `:config_schema_drift`,
`:baseline_missing`, `:cannot_verify`).

Scrub before committing: the ledger must contain module names, pool modes, schema names, relation names,
counts, and signature atoms — nothing else. If any record carries a recipient address, subject, header, or
bound query-parameter value, that is a defect in the formatter, not something to redact by hand; fix the
formatter and re-run.

Record the `--max-cases 1` negative control once: run
`MAILGLASS_SANDBOX_TRACE=1 mix test --warnings-as-errors --exclude requires_workspace --seed 0 --max-cases 1`
on the `public` axis only and append its per-class tally to `143-LEDGER-public.txt` under a clearly-marked
negative-control heading. The expectation is that the tally is materially unchanged, because
`ExUnit.Runner.async_loop/4` already runs sync modules strictly after all async modules, one at a time.
Record the observed result whichever way it comes out.

Do not attempt seed bisection or delta debugging, do not add a `:telemetry` handler (neither
`Ecto.Adapters.SQL.Sandbox` nor `DBConnection.Ownership` emits ownership telemetry — verified against
`deps/`), and do not add a `dev/mix/tasks/` diagnostic task. All three are recorded dead ends per D-05;
the instrumentation runs inside `mix test` behind the trace env var so the lane command stays unchanged.
  </action>
  <verify>
    <automated>test -s .planning/phases/143-test-harness-truth/143-LEDGER-public.txt && test -s .planning/phases/143-test-harness-truth/143-LEDGER-mailglass.txt</automated>
    <automated>grep -Eq 'pool_mode_leaked|config_schema_drift|baseline_missing' .planning/phases/143-test-harness-truth/143-LEDGER-public.txt</automated>
  </verify>
  <acceptance_criteria>
    - Both ledger files exist, are non-empty, and each begins with a header block naming the exact command, the Elixir/OTP versions, the boot schema, the seed, and the four ExUnit counts
    - `grep -Ec 'max-cases|negative control' .planning/phases/143-test-harness-truth/143-LEDGER-public.txt` is at least 1 and the observed tally is recorded
    - Each ledger carries a per-class tally line for all four violation class atoms, including explicit zeros
    - `grep -Eic '@example\.com|subject:|recipient' .planning/phases/143-test-harness-truth/143-LEDGER-*.txt` is 0
    - The `mailglass`-axis ledger's `excluded` count exceeds the `public`-axis ledger's by exactly 1 — the `:public_only` delta, confirming the axis actually switched
  </acceptance_criteria>
  <done>Two committed pre-fix ledgers, one per schema axis, each carrying a reproducible header, per-module records for all three leak classes, and the recorded `--max-cases 1` negative control.</done>
</task>

<task type="auto">
  <name>Task 2: Write the mechanism account and its docs-contract test</name>
  <files>.planning/phases/143-test-harness-truth/143-MECHANISM.md, test/scripts/mechanism_account_contract_test.exs</files>
  <read_first>
    - `.planning/phases/143-test-harness-truth/143-LEDGER-public.txt` and `143-LEDGER-mailglass.txt` from Task 1
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "Mechanism Account" through § "The two confirmed leak windows" (`:248-428`) — the five-step causal chain, the exact ExUnit failure term, the healing-path table, and the two leak-window table rows. Carry the causal chain into the account largely as written; do not paraphrase away the `file:line` citations.
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "D-07 (S2)" (`:429-465`) — the four no-op sites and the full 32-site raw-`Sandbox.*` inventory table
    - `test/scripts/lane_classification_drift_test.exs` lines 1-65 — the `test/scripts/` file header convention: `use ExUnit.Case, async: true`, a moduledoc naming the requirement ID and the CI lane that runs it, and `@repo_root`-anchored path attributes
    - `test/mailglass/docs_contract_test.exs` — this repo's docs-contract assertion idiom (narrow, literal, over named files)
    - `brandbook/brand-book.md` — voice: name the cause, stay composed, prefer the direct word
  </read_first>
  <action>
Write `.planning/phases/143-test-harness-truth/143-MECHANISM.md` as the phase's HARNESS-01 evidence artifact.

Required sections:

1. **Verdict** — one paragraph. The mechanism is confirmed; it is not what HARNESS-01 and ROADMAP criterion
   1 say it is. The nine `:auto`-mode files *heal* a leaked owner rather than colliding with it, and
   `Mailglass.DataCase` is the victim, never the culprit.
2. **The proven causal chain** — the five-step chain from RESEARCH.md § "The Proven Causal Chain", carried
   with its citations: `manager.ex:148-159` as the only `:already_shared` source and its `Process.alive?`
   guard; `sandbox.ex:451-460` as the unlinked `Agent.start` and the bare match at `:458`;
   `webhook_idempotency_convergence_test.exs:52-64` as the unguarded window;
   `data_case.ex:35` as the observed victim site. Cite run `30464215272`, job `90617762038`, and include
   the verbatim `MatchError` term with its nested `{:error, {{:badmatch, :already_shared}, stack}}` shape —
   the term the Wave-3 classifier must match. State plainly that a classifier matching
   `{:badmatch, :already_shared}` at the top level of the ExUnit failure term matches nothing, and that
   SASL crasher reports inflate any log-grep count (354 log hits against 213 failures).
3. **Blast radius and duration** — `db_connection`'s default ownership timeout is 120 seconds
   (`proxy.ex:9`), `config/test.exs` does not override it, but
   `webhook_idempotency_convergence_test.exs:53-56` sets ten minutes. Record the four healing paths from the
   research table and the note that a timeout-healed run leaves a harmless zombie Agent alive — so a ledger
   keyed on owner-pid liveness would misread it, and the pool mode is the correct key.
4. **The three-class inventory** — one subsection per class. For each: the observed relation-name shape, the
   per-leg counts from the four-leg table, the module named by the Task-1 ledger as the culprit, and whether
   that confirms or refutes research assumptions A2 (Class B originates in one of the six
   `:schema_isolation`-tagged files) and A3 (Class A is `migration_test.exs`'s incomplete restoration). If
   the ledger names a different module, the ledger wins and the assumption is recorded as refuted.
5. **D-04's falsifiable predictions** — a two-row table, each row marked PASS or FAIL with the ledger or log
   excerpt that decides it. Prediction 1: every `:already_shared` failure is in an `async: false` module,
   zero in `async: true`. Prediction 2: the module immediately preceding the first `:already_shared` shows
   an acquisition with no matching release.
6. **Rejected diagnostics, recorded once** — `--max-cases 1` (with the observed negative-control tally),
   seed bisection and delta debugging, an ownership `:telemetry` handler, `mix test --slowest`, a JUnit
   formatter, and single-file reproduction. Each with its one-line structural reason.
7. **What this account does NOT claim** — it does not claim a deterministic full-suite reproduction. SEED-007
   establishes that no single file reproduces the bug, and the healing behaviour makes even a fixed file
   pair unstable across seeds. The deterministic proof is mechanism-level and lands in plan `143-04`.

Then create `test/scripts/mechanism_account_contract_test.exs`. Follow the `test/scripts/` header
convention: `use ExUnit.Case, async: true`, a moduledoc naming HARNESS-01 and stating that the file runs in
the required `mix_task_tests` lane via `verify.ci_lane_contract`'s directory glob, and `@repo_root`-anchored
path attributes for the account and the two ledgers. Assert: all three files exist and are non-empty; the
account contains each of the seven section headings; the account cites the confirming run ID and job ID;
the account contains a PASS/FAIL marker for each of the two D-04 predictions; and each ledger header
records the four ExUnit counts. Add an anti-vacuity guard in the `required_checks_test.exs:30-34` shape: a
`> 0` assertion on the parsed section-heading count, with a message naming the parser, so a formatting
change to the account cannot make the contract pass trivially.
  </action>
  <verify>
    <automated>mix test test/scripts/mechanism_account_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/143-test-harness-truth/143-MECHANISM.md` exists and contains all seven required section headings
    - `grep -c '30464215272' .planning/phases/143-test-harness-truth/143-MECHANISM.md` is at least 1 and `grep -c '90617762038'` is at least 1
    - The account contains the nested `MatchError` term shape and an explicit statement that a top-level match on the badmatch tuple matches nothing
    - `grep -Ec 'PASS|FAIL' .planning/phases/143-test-harness-truth/143-MECHANISM.md` is at least 2, one per D-04 prediction
    - The three-class inventory names a specific module for Class A and for Class B, sourced from the ledger, and marks assumptions A2/A3 confirmed or refuted
    - `mix test test/scripts/mechanism_account_contract_test.exs --warnings-as-errors` exits 0 with 5 or more tests
    - `mix test test/scripts/ --warnings-as-errors` exits 0 — the new file is auto-collected and the pre-existing files stay green
    - `git diff --name-only mix.exs` is empty — the directory glob needs no alias change
  </acceptance_criteria>
  <done>HARNESS-01's evidence bar is met on paper and mechanically guarded: the account exists, cites its confirming run, records both D-04 predictions with evidence, names the Class A and Class B culprits from the ledger, and a docs-contract test in the required lane fails if any of that disappears.</done>
</task>

<task type="auto">
  <name>Task 3: Land the D-31 upstream artifact amendments</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, test/support/ci_lanes.ex</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the HARNESS section (lines 61-81) and the amendment style used by the TRUTH-09 entry, which carries its correction inline in italics with the reason and a pointer to the CONTEXT.md decision
    - `.planning/ROADMAP.md` — the Phase 143 block (lines 120-131), specifically criterion 1's parenthetical hypothesis
    - `test/support/ci_lanes.ex` lines 40-80 — the exclusions moduledoc, including the "cron-only / live-provider canary" description of Core Full Suite
    - `.planning/phases/143-test-harness-truth/143-MECHANISM.md` from Task 2 — the source of every correction landed here
    - `.planning/phases/141-lane-truth-foundation/141-CONTEXT.md` § D-02/D-03 — the precedent for amending a requirement rather than silently diverging from it
  </read_first>
  <action>
Amend the upstream artifacts so none of them still asserts something the evidence contradicts. Follow the
Phase 141 TRUTH-09 precedent exactly: the original text stays readable and the correction is appended
inline with its reason and a pointer to the deciding artifact. Never delete and rewrite a requirement in
place.

In `.planning/REQUIREMENTS.md`, amend **HARNESS-01**:
- `Mailglass.DataCase` is exonerated as a candidate — it registers `on_exit(stop_owner)` on the line
  immediately following acquisition (`data_case.ex:35-36`), which is Ecto's own documented idiom. It is the
  observed victim, not the culprit.
- The confirmed sites are `properties/webhook_idempotency_convergence_test.exs`'s acquire/release gap and
  `test/support/mailer_case.ex`'s ninety-line equivalent.
- The `mailer_case.ex:158` / `:248` entry is correct but for a different reason than stated: those calls are
  provable no-ops that run after the pool is already shared, so their discarded return value is
  `:already_shared`. They are redundant, not broken, and deleting them is behaviour-preserving.
- The phase closed **three** global-state leak classes, not one. Name Class A (migration-baseline teardown),
  Class B (`Mailglass.Config.schema()` drift), and Class C (the ownership leak), with the per-leg evidence
  pointer to `143-MECHANISM.md`. Record that SEED-007's "already ruled out" entry for `migration_test.exs`
  teardown was measured locally against a restored baseline and does not hold under CI's cold database, so
  that entry is re-opened.

Also add to `.planning/REQUIREMENTS.md` an explicit **HARNESS-02 versus HARNESS-04 scope split** note:
HARNESS-02 is judged on lane content — **all four** legs green in the evidence. HARNESS-04's gating scope is
deliberately narrower — **two** legs, the Elixir 1.18 / OTP 27 pair that matches the declared `~> 1.18`
floor. State this plainly so a future reader does not mistake the narrower gate for a failure to meet
HARNESS-02.

In `.planning/ROADMAP.md`, amend Phase 143 **criterion 1**: the stated leading hypothesis is corrected — the
`:auto`-mode sibling files *heal* a leaked owner (`Sandbox.mode(repo, :auto)` reaches
`manager.ex:169-172`'s explicit mode reset, and `unshare/2` via `proxy_checkin/3` at `:289`) rather than
colliding with it. That is why the lane was partially rather than wholly red, and why no single file
reproduces the bug. Point to `143-MECHANISM.md`.

In `test/support/ci_lanes.ex`, amend the exclusions moduledoc that calls Core Full Suite a "cron-only /
live-provider canary". The lane runs on push, pull request, cron, and dispatch
(`advisory-matrix.yml:3-10`), and it is about to gate a publish. Correct the name and the classification
claim. The *parity* exclusion itself stays correct and stays: `mix ci` runs the suite with one exclusion
token and the lane runs it with a different one, so they are different suites and a parity matcher would be
a false parity claim. Change no constant, no accessor, and no hardcoded count in this file — this is a
moduledoc-only edit.
  </action>
  <verify>
    <automated>mix test test/scripts/ --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` HARNESS-01 carries an inline amendment naming all three leak classes and exonerating `Mailglass.DataCase`, pointing to `143-MECHANISM.md`
    - `.planning/REQUIREMENTS.md` states the HARNESS-02 (four legs) versus HARNESS-04 (two legs) scope split explicitly
    - `.planning/ROADMAP.md` Phase 143 criterion 1 records that the `:auto` files heal rather than collide, citing `manager.ex:169-172`
    - `grep -c 'cron-only' test/support/ci_lanes.ex` is 0
    - `git diff -- test/support/ci_lanes.ex` shows changes only inside the moduledoc — no `@`-attribute list, accessor, or count line appears in the diff
    - `mix test test/scripts/lane_classification_drift_test.exs --warnings-as-errors` exits 0 — the 24-row and set-equality assertions are untouched
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>REQUIREMENTS.md, ROADMAP.md, and `ci_lanes.ex`'s moduledoc agree with the evidence, each correction carries its reason, and no registry constant or count changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-suite output → committed repository artifact | The ledger dumps become permanent, publicly readable files. |
| CI job log → phase artifact | Excerpts from a job log are copied into the mechanism account. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-09 | Information Disclosure | Committed ledger dumps and account excerpts | high | mitigate | The ledger carries module names, pool modes, schema names, relation names, counts, and signature atoms only. The `42P01` messages quoted carry relation names (safe); the accompanying `query:` lines carry bound-parameter placeholders, never values. An acceptance criterion greps the committed files for address/subject/recipient markers and requires zero hits. |
| T-143-10 | Repudiation | The mechanism account as evidence | medium | mitigate | The account is bound by `mechanism_account_contract_test.exs` running in the REQUIRED `mix_task_tests` lane, with an anti-vacuity `> 0` guard on its own parser. An account that loses its run citation or its prediction verdicts reds a required lane. |
| T-143-11 | Tampering | Editing `test/support/ci_lanes.ex` while amending its moduledoc | high | mitigate | Moduledoc-only edit; an acceptance criterion inspects the diff for any attribute/accessor/count line and `lane_classification_drift_test.exs` must stay green. The 24-row and set-equality assertions are the mechanical guard. |
</threat_model>

<verification>
- `mix test test/scripts/ --warnings-as-errors` exits 0 (the required lane, now including the new contract test)
- `mix compile --warnings-as-errors` exits 0
- `mix credo --strict` exits 0
- `mix format --check-formatted` exits 0
- Both ledger files and `143-MECHANISM.md` are committed
</verification>

<success_criteria>
- HARNESS-01's evidence bar is met: written account + committed ledgers + both D-04 predictions recorded with evidence
- The Class A and Class B culprits are named from the ledger, and research assumptions A2/A3 are marked confirmed or refuted
- REQUIREMENTS.md, ROADMAP.md, and `ci_lanes.ex` no longer contradict the evidence
- No fix code was written in this plan
</success_criteria>

<artifacts_this_phase_produces>
Symbols created by this plan (excluded from cross-source drift verification):

- File `.planning/phases/143-test-harness-truth/143-MECHANISM.md`
- File `.planning/phases/143-test-harness-truth/143-LEDGER-public.txt`
- File `.planning/phases/143-test-harness-truth/143-LEDGER-mailglass.txt`
- Module `Mailglass.Scripts.MechanismAccountContractTest` — `test/scripts/mechanism_account_contract_test.exs`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/143-test-harness-truth/143-03-SUMMARY.md` when done.
</output>
