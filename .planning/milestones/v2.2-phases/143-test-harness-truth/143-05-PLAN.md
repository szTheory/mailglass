---
phase: 143-test-harness-truth
plan: 05
type: execute
wave: 4
depends_on: ["143-04"]
files_modified:
  - test/support/data_case.ex
  - test/support/mailer_case.ex
  - test/mailglass/properties/webhook_idempotency_convergence_test.exs
  - test/mailglass/schema_axis_boot_order_test.exs
  - test/mailglass/outbound/deliver_many_test.exs
  - test/mailglass/outbound/deliver_later_test.exs
  - test/mailglass/mailer_case_test.exs
autonomous: true
requirements: [HARNESS-01]

must_haves:
  truths:
    - "The two confirmed leak windows are closed by construction, not by moving a statement: `mailer_case.ex`'s ninety-line acquire-to-registration gap and `webhook_idempotency_convergence_test.exs`'s four-statement gap both go through `SandboxOwnership.checkout!/1`, whose release is registered before anything that can raise."
    - "The four provable no-op `Sandbox.mode(repo, {:shared, self()})` calls are deleted and the comment that asserted a guarantee the code did not provide is corrected. Deletion is behaviour-preserving: the pool is already `{:shared, agent_pid}` from the preceding `start_owner!(shared: true)`, so those calls returned `:already_shared` and changed nothing."
    - "`set_mailglass_global/0`'s observable semantics are unchanged after the deletions, proven by running the Oban-tagged subset before and after and comparing."
    - "`test/mailglass/schema_axis_boot_order_test.exs`'s bare `Sandbox.checkout/1` is MIGRATED to the sanctioned door, not allowlisted. An allowlist entry is a permanent exception a future reader copies; the helper handles the bare-checkout case at no cost."
  artifacts:
    - test/support/data_case.ex
    - test/support/mailer_case.ex
  key_links:
    - "`data_case.ex` is the dominant acquisition site (35 files depend on it) — migrating it routes the majority of the harness through the sanctioned door in one edit."
    - "The migration of `schema_axis_boot_order_test.exs:27` is what lets `:checkout` join the Credo check's forbidden-function list in plan `143-08`; the decision must land before that check is written."
  prohibitions:
    - "MUST NOT change any file's async attribute value. SEED-007 forbids serializing the bug away, and the four-leg evidence is only interpretable if the async/sync split is byte-identical before and after."
    - "MUST NOT change `set_mailglass_global/0`'s semantics. The four deletions are behaviour-preserving; if the Oban subset diverges, the deletion was wrong, not the test."
---

<!-- planner-discipline-allow: async: -->

<objective>
Route the two case templates and the confirmed leak sites through the sanctioned door, and delete the four
no-op mode calls whose discarded return value was telling them they did nothing.

`test/support/data_case.ex:35-36` is already correct and becomes the smallest edit — it simply stops calling
Ecto directly. `test/support/mailer_case.ex` is the ninety-line window: `CitextProbe.run/1`,
`Fake.checkout()`, `Phoenix.PubSub.subscribe`, and `start_supervised!({Oban, ...})` all sit between the
acquisition at `:93` and the `on_exit` at `:185`, and the release at `:206` is the last statement after five
preceding restores. `webhook_idempotency_convergence_test.exs` is the site the CI log caught in the act.

Purpose: close both confirmed windows and remove the four sites where the harness reported a guarantee it
did not have — a check that cannot do its job and reports green, inside the milestone that exists to
eliminate exactly that (D-07).
Output: seven files routed through `checkout!/1`, four deletions, one corrected comment, and an Oban
before/after diff proving the deletions changed nothing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-test-harness-truth/143-CONTEXT.md
@.planning/phases/143-test-harness-truth/143-PATTERNS.md
@.planning/phases/143-test-harness-truth/143-MECHANISM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route both case templates through `checkout!/1`</name>
  <precondition>PostgreSQL is reachable for `Mailglass.TestRepo` — `scripts/preflight_postgres.sh` exits 0. Migrating the two case templates changes setup for the majority of the suite, so the verification runs boot the repo.</precondition>
  <files>test/support/data_case.ex, test/support/mailer_case.ex</files>
  <read_first>
    - `test/support/sandbox_ownership.ex` — `checkout!/1`'s signature, its release-first invariant, its async guards, and `assert_manual!/2`
    - `test/support/data_case.ex` — full file; the acquisition at `:35` and the `on_exit` at `:36`
    - `test/support/mailer_case.ex` — full file, with particular attention to the I-12 guard at `:78-91`, the acquisition at `:93`, `CitextProbe.run/1` at `:98`, `Fake.checkout()` at `:100`, the PubSub subscribe at `:113-116`, the raw mode call and its comment at `:153-162`, the `on_exit` registration at `:185`, the five restores at `:186-205`, the release at `:206`, and `set_mailglass_global/0` including its raw mode call at `:248`
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "The two confirmed leak windows" (`:416-427`) and § "D-07 (S2)" (`:429-465`) — the window table and the four-site verdict table
  </read_first>
  <action>
In `test/support/data_case.ex`, replace the direct `Ecto.Adapters.SQL.Sandbox.start_owner!/2` plus
`on_exit(stop_owner)` pair with a single `Mailglass.TestSupport.SandboxOwnership.checkout!/1` call passing
the same `shared:` value derived from the module's async tag. This file is the control — its behaviour must
not change, only its call path. Do not touch the async tag derivation itself.

In `test/support/mailer_case.ex`:

- Replace the acquisition at `:93` with `SandboxOwnership.checkout!/1`, passing the same `shared:` value.
  The helper registers the release immediately, so the ninety-line window closes by construction — every
  statement from `CitextProbe.run/1` onward now sits below a registered release.
- Remove the now-redundant `Sandbox.stop_owner/1` call from the existing `on_exit` block. Leave the five
  preceding restores at `:186-205` in place and in their existing order; they are not this phase's concern.
  Because `on_exit` callbacks run in reverse registration order, the helper's release (registered first)
  runs last, which preserves today's teardown sequencing exactly.
- Delete the raw `Sandbox.mode(repo, {:shared, self()})` call in the Oban setup path. It runs after
  `checkout!(shared: true)` has already put the pool in `{:shared, agent_pid}`; the test process is not the
  registered owner and the agent is alive, so `manager.ex:154` returns `:already_shared` and nothing
  changes. Rewrite the comment above it — it currently asserts a guarantee the code does not provide.
  The replacement comment states the truth: Oban's internal processes reach the database because the pool
  is genuinely in shared mode from the checkout, and names the helper as the mechanism.
- Delete the second raw mode call inside `set_mailglass_global/0` for the same reason, and correct its
  comment the same way. `set_mailglass_global/0`'s observable semantics do not change.
- Leave the I-12 guard at `:78-91` exactly as it is. It is the shape the new async guards copy and it still
  covers Oban.Testing mode, which is D-11's sanctioned reason two.

Change no file's async attribute value in either file.
  </action>
  <verify>
    <automated>mix test test/mailglass/mailer_case_test.exs test/support/webhook_case_test.exs --warnings-as-errors</automated>
    <automated>mix test --only oban --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Ecto.Adapters.SQL.Sandbox' test/support/data_case.ex` is 0
    - `grep -c 'SandboxOwnership.checkout!' test/support/data_case.ex` is 1
    - `grep -Ec 'Sandbox\.mode\(.*\{:shared, self\(\)\}' test/support/mailer_case.ex` is 0
    - `grep -Ec 'Sandbox\.(start_owner!|stop_owner)' test/support/mailer_case.ex` is 0
    - `grep -c 'SandboxOwnership' test/support/mailer_case.ex` is at least 1
    - The I-12 guard block is present and unchanged — `git diff -- test/support/mailer_case.ex` shows no line inside it
    - `mix test --only oban --warnings-as-errors` exits 0 with the same test count as on the parent commit
    - `git diff --unified=0 -- test/support/ | grep -Ec '^[+-][^+-].*\basync: (true|false)'` is 0
    - `mix compile --warnings-as-errors`, `mix credo --strict`, and `mix format --check-formatted` all exit 0
  </acceptance_criteria>
  <done>Both case templates acquire through the sanctioned door, the ninety-line window is closed by construction, and the two no-op mode calls in `mailer_case.ex` are gone with their comments corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Close the confirmed leak site and the remaining raw call sites</name>
  <files>test/mailglass/properties/webhook_idempotency_convergence_test.exs, test/mailglass/schema_axis_boot_order_test.exs, test/mailglass/outbound/deliver_many_test.exs, test/mailglass/outbound/deliver_later_test.exs</files>
  <read_first>
    - `test/mailglass/properties/webhook_idempotency_convergence_test.exs` — full file; the acquisition at `:52-56` with its ten-minute ownership timeout, `CitextProbe.run/1` at `:58`, `Tenancy.put_current/1` at `:59`, the two TRUNCATEs at `:61-62`, the `on_exit` at `:64`, the two TRUNCATEs and `Tenancy.clear()` at `:65-67`, and the release at `:68`
    - `test/mailglass/schema_axis_boot_order_test.exs` — full file; the bare `Sandbox.checkout/1` at `:27` with no checkin and no `on_exit`
    - `test/mailglass/outbound/deliver_many_test.exs` — the raw mode call at `:17` and the `:manual` revert at `:35`
    - `test/mailglass/outbound/deliver_later_test.exs` — the raw mode call at `:37` and the `:manual` revert at `:54`
    - `test/support/sandbox_ownership.ex` — `checkout!/1` and `assert_manual!/2`
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "D-07 (S2)" (`:429-465`) — the note that the two `:manual` reverts in the deliver tests are themselves healing calls, and why `on_exit` reverse ordering makes that safe
  </read_first>
  <action>
Close the site the CI log caught in the act, then clear the remaining raw call sites.

In `test/mailglass/properties/webhook_idempotency_convergence_test.exs`, replace the acquisition with
`SandboxOwnership.checkout!/1`, carrying the same `shared: true` and the same ten-minute
`ownership_timeout`. The helper registers the release immediately, so `CitextProbe.run/1`, the tenancy
stamp, and both TRUNCATEs move below a registered release and the first window closes. Then remove the
release from the file's own `on_exit` block, leaving the two TRUNCATEs and the tenancy clear in place — the
helper's release, registered first, now runs last, so a raise in one of those statements no longer strands
the owner and the second window closes too. Keep the ten-minute timeout: this plan closes the window, it
does not retune the timeout, and shortening it would change the file's semantics for reasons unrelated to
the defect.

In `test/mailglass/schema_axis_boot_order_test.exs`, migrate the bare `Sandbox.checkout/1` at `:27` to
`SandboxOwnership.checkout!/1`. **Decision recorded here, resolving RESEARCH.md Open Question 4: migrate,
do not allowlist.** An allowlist entry is a permanent exception a future reader copies; the helper covers
the bare-checkout case at no cost, and migrating is what lets `:checkout` join the Credo check's
forbidden-function list in plan `143-08`. Today the call is safe only because Ecto auto-releases on owner
death; after migration the release is explicit and registered. Add a one-line comment recording that the
call is now released deterministically rather than by owner-process death.

In `test/mailglass/outbound/deliver_many_test.exs` and `test/mailglass/outbound/deliver_later_test.exs`,
delete the raw `Sandbox.mode(repo, {:shared, self()})` call in each. Both run after `DataCase`'s
`checkout!(shared: true)` — `ExUnit.CaseTemplate` setups precede the module's own — so the pool is already
shared and each call returned `:already_shared` and changed nothing. Correct the comment above each the
same way `mailer_case.ex` was corrected. Leave each file's `Sandbox.mode(repo, :manual)` revert in place
for now: those are healing calls whose reverse-order `on_exit` placement means they run before `DataCase`'s
release, and plan `143-08`'s Credo check allowlists nothing, so if the check flags them the migration to
`SandboxOwnership` happens there with the rest of the `:auto`-mode inventory. Record that hand-off in a
comment.

Change no file's async attribute value.
  </action>
  <verify>
    <automated>mix test test/mailglass/schema_axis_boot_order_test.exs test/mailglass/outbound/deliver_many_test.exs test/mailglass/outbound/deliver_later_test.exs --warnings-as-errors</automated>
    <automated>mix test test/mailglass/properties/webhook_idempotency_convergence_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec 'Sandbox\.(start_owner!|stop_owner|checkout)\(' test/mailglass/properties/webhook_idempotency_convergence_test.exs test/mailglass/schema_axis_boot_order_test.exs` is 0
    - `grep -Ec 'Sandbox\.mode\(.*\{:shared, self\(\)\}' test/mailglass/outbound/deliver_many_test.exs test/mailglass/outbound/deliver_later_test.exs` is 0
    - `grep -c '600_000\|10 \* 60_000' test/mailglass/properties/webhook_idempotency_convergence_test.exs` is at least 1 — the ten-minute timeout is preserved, not silently retuned
    - Each of the four files passes individually with `--warnings-as-errors`
    - `git diff --unified=0 -- test/mailglass/ | grep -Ec '^[+-][^+-].*\basync: (true|false)'` is 0
    - `mix credo --strict` and `mix format --check-formatted` exit 0
  </acceptance_criteria>
  <done>The confirmed leak site's two windows are closed, the bare checkout is migrated rather than allowlisted, and the last two S2 no-ops are deleted with corrected comments.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the four deletions were behaviour-preserving</name>
  <files>test/mailglass/mailer_case_test.exs</files>
  <read_first>
    - `test/mailglass/mailer_case_test.exs` — full file; the moduledoc summary line and the `# Test N:` numbered-comment convention at `:1-43`
    - `test/support/mailer_case.ex` as modified in Task 1 — `set_mailglass_global/0` after the deletion
    - `.planning/phases/143-test-harness-truth/143-RESEARCH.md` § "Assumptions Log" row A8 — the mitigation is to run the Oban-tagged subset before and after and diff
    - `deps/db_connection/lib/db_connection/ownership/manager.ex` lines 148-159 — why `:already_shared` was the return value and why deleting the call changes nothing
  </read_first>
  <action>
Turn assumption A8 into evidence. The deletions in Tasks 1 and 2 are provable no-ops by construction, but
the Oban-tagged tests are the observable proof.

Capture the Oban-tagged subset's result on the parent commit and after the change: run
`mix test --only oban --warnings-as-errors` at both points and compare the test count, the failure count,
and the set of test names reported. Record the comparison in the plan's SUMMARY. Any divergence means the
deletion was wrong and must be reverted, not that the test needs adjusting.

Extend `test/mailglass/mailer_case_test.exs` with assertions proving `set_mailglass_global/0`'s observable
semantics are unchanged, continuing the existing `# Test N:` numbering and updating the moduledoc summary
line to mention the new coverage. Assert, from a module that goes through the Oban path: the pool is in
shared mode after setup (via `SandboxOwnership.live_holder/0` returning a live pid, not by re-asserting the
mode); a process spawned outside the test process can reach the database; and after
`set_mailglass_global/0` the same two properties still hold. These assert the *effect* the deleted calls
were supposed to provide, so they would have failed if the deletion had removed a real guarantee.

Add one assertion that the harness no longer contains the deleted pattern anywhere under `test/support/`,
as a cheap tripwire ahead of the Credo check landing in plan `143-08`. Read the support files as text and
assert the raw shared-mode mode call does not appear. Keep the tripwire narrow — `test/support/` only — so
it does not duplicate the Credo check's job.
  </action>
  <verify>
    <automated>mix test test/mailglass/mailer_case_test.exs --warnings-as-errors</automated>
    <automated>mix test --only oban --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mailglass/mailer_case_test.exs --warnings-as-errors` exits 0 with at least 3 more tests than on the parent commit
    - `mix test --only oban --warnings-as-errors` reports the same test count and the same test names as on the parent commit, and 0 failures — the before/after comparison is recorded in the SUMMARY
    - The new assertions verify shared mode via `SandboxOwnership.live_holder/0`, not by calling `Sandbox.mode/2` again
    - The text tripwire asserts the raw shared-mode call is absent from `test/support/` and fails if reintroduced
    - `mix test test/mailglass/test_support/ test/scripts/ --warnings-as-errors` exits 0
    - `mix credo --strict` and `mix format --check-formatted` exit 0
  </acceptance_criteria>
  <done>The four deletions are recorded as behaviour-preserving with a before/after Oban comparison, and `mailer_case_test.exs` asserts the effect those calls were meant to provide is still there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| case template → every test module that uses it | `data_case.ex` and `mailer_case.ex` set up the majority of the suite; a setup regression is a suite-wide regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-15 | Tampering | Deleting the four raw shared-mode calls | high | mitigate | Deletion is provable behaviour-preserving from `manager.ex:148-159` plus setup ordering, and is confirmed observably by an Oban-tagged before/after comparison plus new `mailer_case_test.exs` assertions that check the *effect*, not the call. |
| T-143-16 | Repudiation | Changing an async attribute value to make the suite pass | high | mitigate | An acceptance criterion on every task greps the diff for async-value changes and requires zero. SEED-007 forbids serializing the bug away, and the four-leg evidence needs a byte-identical async/sync split. |
| T-143-17 | Denial of Service | Shortening the ten-minute ownership timeout as an incidental "cleanup" | medium | mitigate | An acceptance criterion asserts the timeout literal survives. Retuning it is out of scope and would change the file's semantics for reasons unrelated to the defect. |
</threat_model>

<verification>
- `mix test test/mailglass/test_support/ test/scripts/ --warnings-as-errors` exits 0
- `mix test --only oban --warnings-as-errors` matches the parent commit's count and names
- `mix compile --warnings-as-errors`, `mix credo --strict`, `mix format --check-formatted` all exit 0
- Zero async-attribute-value changes across the whole plan diff
</verification>

<success_criteria>
- Seven files route sandbox acquisition through `SandboxOwnership`
- Both confirmed leak windows are closed by construction, not by statement reordering
- All four S2 no-ops are deleted and their comments corrected
- `schema_axis_boot_order_test.exs`'s bare checkout is migrated, and that decision is recorded in the plan
</success_criteria>

<artifacts_this_phase_produces>
This plan creates no new symbols. It routes existing call sites through symbols created in `143-01` and
`143-04` (`SandboxOwnership.checkout!/1`, `.live_holder/0`) and deletes four call sites. Recorded here so
the drift pass does not read the deletions as unexplained removals:

- Deleted: the four raw `Ecto.Adapters.SQL.Sandbox.mode(repo, {:shared, self()})` call sites in
  `test/support/mailer_case.ex` (two), `test/mailglass/outbound/deliver_many_test.exs`, and
  `test/mailglass/outbound/deliver_later_test.exs`
- Deleted: the direct `start_owner!` / `stop_owner` pairs in `test/support/data_case.ex`,
  `test/support/mailer_case.ex`, and `test/mailglass/properties/webhook_idempotency_convergence_test.exs`
- Deleted: the bare `Sandbox.checkout/1` in `test/mailglass/schema_axis_boot_order_test.exs`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/143-test-harness-truth/143-05-SUMMARY.md` when done.
</output>
