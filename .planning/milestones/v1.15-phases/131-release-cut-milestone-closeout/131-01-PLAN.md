---
phase: 131-release-cut-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
  - .github/workflows/post-publish-smoke.yml
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass-publish-summary.json
  - .planning/publish/mailglass_admin-files.expected
  - .planning/publish/mailglass_admin-publish-summary.json
  - .planning/publish/mailglass_inbound-files.expected
  - .planning/publish/mailglass_inbound-publish-summary.json
  - .planning/milestones/v1.15-ROADMAP.md
  - .planning/milestones/v1.15-REQUIREMENTS.md
  - .planning/milestones/v1.15-MILESTONE-AUDIT.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/RETROSPECTIVE.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03

must_haves:
  truths:
    - "All three Hex packages are live: mailglass 1.11.0, mailglass_admin 1.11.0, mailglass_inbound 1.6.0 (D-01, SHIP-01)"
    - "mailglass_inbound carries the LOOSENED dep {mailglass, ~> 1.10 and >= 1.10.2} on Hex — NOT a re-pinned == — proving the keystone `~>` decoupling (D-02, D-03, LD-2)"
    - "The inbound floor stays >= 1.10.2 (NOT bumped to >= 1.11.0); the `~> 1.10` admits core 1.11.0 (D-03)"
    - "A fresh consumer app resolves all three packages from Hex under DEP_MODE=hex, with inbound 1.6.0's `~> 1.10 and >= 1.10.2` resolving against core 1.11.0 (SHIP-02, D-09)"
    - "post-publish-smoke runs the inbound-compatibility test (not silently skipped) after the D-11 `~>`-aware grep fix, and passes (or only the known #32 hackney false-positive is red) (SHIP-02, D-11)"
    - "The inbound docs_contract_test passes on BOTH main semantics (core 1.10.2 / README `~> 1.10` / floor `~> 1.10`) AND the RP release-branch semantics (core 1.11.0 / README `~> 1.11` via sed / floor `~> 1.10`) (D-05)"
    - "origin/main carries the full v1.15 body (71 commits) pushed by a clean fast-forward — never --force (D-04)"
    - "Milestone v1.15 is audited (26/26 requirements status: passed across 7 phases 125-131), archived, and tagged v1.15 (D-10, SHIP-03)"
  artifacts:
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "The D-05 docs_contract fix — re-derives the expected core pin from the CORE RELEASE version (manifest .[\".\"] / core mix.exs @version), not from the inbound floor pin; keeps a positive Version.match? floor-admits-core assertion; passes on main AND the RP release branch"
      contains: "release-please-manifest.json"
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "The D-11 `~>`-aware inbound-compat check so post-publish-smoke actually runs the inbound smoke under the loosened pin (was silently skipped by the `== ${VERSION}` grep)"
    - path: ".planning/milestones/v1.15-MILESTONE-AUDIT.md"
      provides: "Per-requirement pass/fail matrix at true scope (7 phases 125-131, 26 requirements)"
      contains: "status: passed"
  key_links:
    - from: "core release version in .release-please-manifest.json .[\".\"]"
      to: "docs_contract_test expected_mailglass_pin"
      via: "test re-derives the expected README core pin from the CORE version, not the inbound floor (D-05)"
      pattern: "release-please-manifest.json"
    - from: "post-publish-smoke inbound-compat check (`~>`-aware)"
      to: "include_inbound=true → inbound consumer smoke runs"
      via: "grep matches `mailglass ~>` / `>=` instead of `== ${VERSION}` (D-11)"
      pattern: "mailglass ~>"
    - from: "inbound 1.6.0 dep `~> 1.10 and >= 1.10.2`"
      to: "consumer `mix deps.get` resolving core 1.11.0"
      via: "consumer_install_smoke.sh DEP_MODE=hex (the keystone `~>`-admits-newer-core proof)"
      pattern: "~> 1.10 and >= 1.10.2"
    - from: "pushed origin/main HEAD SHA"
      to: "gate-ci-green in publish-hex.yml"
      via: "a green ci.yml run must exist on that human-authored SHA"
      pattern: "ci.yml"
---

<objective>
Release cut and milestone closeout for mailglass v1.15 — Release-Pipeline Efficiency & Contributor DX.

Purpose: Cut the real linked **v1.15** Hex release through the pipeline hardened across Phases 125–130
— mailglass 1.11.0 / mailglass_admin 1.11.0 (core+admin linked, currently 1.10.2) with mailglass_inbound
scoring a **minor 1.6.0** (currently 1.5.4) from the `feat(125-01)` pin-loosening commit — then prove
consumer + post-publish smoke against the published packages and audit + archive the v1.15 milestone.
This validates the whole "CI-the-body" method end-to-end (LD-1, SHIP-01..03).

This is a **release-ceremony + closeout** phase. It writes essentially NO product/source code. The ONLY
source edits are the two Wave-0 pre-flight infra fixes (D-05, D-11) that Phase 125's pin loosening left
behind and that block/degrade this release. This is a near-verbatim RETARGET of the v1.14 (124) release
ceremony — made SIMPLER by the keystone (loosened `~>` pins → no rebase-reconcile, no `fix(inbound):
re-pin ==` dance) and HARDER by two pin-migration landmines (D-05, D-11).

Output: All three packages live on Hex at 1.11.0/1.11.0/1.6.0; inbound carries the LOOSENED
`{:mailglass, "~> 1.10 and >= 1.10.2"}` (NOT a re-pin); consumer + post-publish smoke green; milestone
audit (26 reqs) and git tag v1.15.

LIVE STATE (verified at plan time — use these facts):
- Current versions: mailglass 1.10.2 / mailglass_admin 1.10.2 / mailglass_inbound 1.5.4 (manifest matches).
- Current pins (ALREADY LOOSENED by Phase 125): inbound `{:mailglass, "~> 1.10 and >= 1.10.2"}`
  (mix.exs:143), admin `{:mailglass, "~> 1.10"}` (mailglass_admin/mix.exs). `mix mailglass.publish.check`
  (verify_deps ~L814) already REJECTS `==` pins — the pins are already correct for this cut.
- Target versions (RP-scored, VERIFY — do not hardcode): 1.11.0 / 1.11.0 / 1.6.0 (D-01). `Release-As:
  1.11.0` is a FALLBACK only if RP scores a patch. Do NOT hand-edit `.release-please-manifest.json` or
  `@version`.
- Git: local `main` (HEAD f42eb7fa) is 71 ahead / 0 behind `origin/main` — a clean fast-forward push
  (`git push origin main`, NEVER --force) — no rebase needed (D-04).
- NO re-pin dance: pins stay `~>` (D-02, LD-2 keystone payoff). Inbound's 1.6.0 minor is ALREADY BANKED
  from the `feat(125-01)` commit; no deliberate commit is needed to cut it. Do NOT bump the inbound floor
  to `>= 1.11.0` (D-03) — record "verified against core 1.11" in the inbound CHANGELOG, not the floor.
- TWO Wave-0 landmines that DID NOT exist for 124: D-05 (inbound docs_contract_test is RED against the
  loosened pin — must be fixed BEFORE the push) and D-11 (post-publish-smoke's `== ${VERSION}` grep
  silently skips inbound smoke under the `~>` pin — must be made `~>`-aware).
- Milestone audit scope: 7 phases (125–131), 26 requirements: PIN-01..05, GATE-01..04, DET-01..02,
  MIXCI-01..05, CACHE-01..02, SUPPLY-01..05, SHIP-01..03 (23 already complete; SHIP-01..03 close here).
  Do NOT trust `gsd-sdk milestone.complete` (999.x inflation) — hand-count to true scope (D-10).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/CLAUDE.md
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/131-release-cut-milestone-closeout/131-CONTEXT.md

Canonical references (READ before acting — CONTEXT.md mandates these):
- .planning/research/milestone-cicd/SYNTHESIS.md (LD-1..13 decisions of record; LD-1 release dogfood, LD-2 `~>` pins)
- .planning/milestones/v1.14-phases/124-release-cut-milestone-closeout/124-01-PLAN.md (the near-verbatim 6-wave ceremony precedent this plan retargets — see the RETARGET DELTAS in the objective)
- MAINTAINING.md (Release Runbook ~L274, Release-As fallback ~L319, Retract Decision Tree, required vs. advisory lanes, Publish Summary Snapshot Protocol)
- .github/workflows/release-please.yml (README `{:mailglass, "~> X.Y"}` pin sed sync ~L153-207 — seds README core pin to CORE_MM=1.11, the root of the D-05 invariant break; auto-merge arming; cron self-heal; the `==` sed rewrites were DELETED in Phase 125/PIN-03)
- .github/workflows/publish-hex.yml (gate-ci-green SHA-bound requirement; publish ordering core → inbound → admin; publish-admin needs [publish-core, publish-inbound]; idempotency guards)
- .github/workflows/post-publish-smoke.yml (smoke chain; the D-11 inbound-compat `mailglass == ${VERSION}` grep at ~L356)
- scripts/consumer_install_smoke.sh (DEP_MODE=hex consumer smoke — SHIP-02 Hex-resolution proof)
- lib/mix/tasks/mailglass.publish.check.ex (verify_deps ~L814 rejects `==`; confirms pins are correctly `~>`)
- mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs (the D-05 blocker — test ~L446-472; regex L458, invariant L466-471)
- mailglass_inbound/mix.exs (~L141-149 — the loosened mailglass_dep; the forward-guidance comment D-03 supersedes)
</context>

<artifacts_produced>
## Artifacts This Phase Produces

This is a release-ceremony + closeout phase. No new source symbols, components, or product docs. Artifacts:

- **The D-05 docs_contract fix** (`mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`,
  commit `test(inbound):`): re-derives the expected README core pin from the CORE RELEASE version
  (manifest `.["."]` / core `../../../mix.exs` @version), NOT from the inbound floor pin; keeps a positive
  `Version.match?(core_version, floor_constraint)` assertion; passes on main AND the RP release branch.
- **The D-11 `~>`-aware inbound-compat fix** (`.github/workflows/post-publish-smoke.yml`, commit `ci:`):
  makes the inbound-compat check match `mailglass ~>` / `>=` (or Version.match) so inbound smoke actually
  runs under the loosened pin instead of being silently skipped.
- **The pushed origin/main HEAD**: the full 71-commit v1.15 body fast-forward pushed (never --force). This
  human-authored HEAD SHA is what RP tags and what gate-ci-green requires a green ci.yml run on (D-04).
- **(If needed) regenerated publish allowlist snapshots** (committed as `chore:`): the six
  `.planning/publish/*` files. Phases 125–130 added NEW tracked files, so — unlike v1.14 — EXPECT a
  possible legitimate allowlist snapshot update this cycle (D-06). Only commit if `publish.check` rewrites.
- **Published Hex versions**: mailglass 1.11.0, mailglass_admin 1.11.0, mailglass_inbound 1.6.0 — inbound
  carrying the LOOSENED `{:mailglass, "~> 1.10 and >= 1.10.2"}` (NOT a re-pin; the keystone payoff).
- **Archive docs**: `.planning/milestones/v1.15-ROADMAP.md`, `.planning/milestones/v1.15-REQUIREMENTS.md`,
  `.planning/milestones/v1.15-MILESTONE-AUDIT.md`, and appended `.planning/RETROSPECTIVE.md`.
- **Updated planning docs**: `.planning/MILESTONES.md`, `PROJECT.md`, `ROADMAP.md`, `STATE.md`.
- **The `v1.15` git tag** (a milestone marker, distinct from the RP-created `*-v1.11.0` / `*-v1.6.0` Hex
  release tags).
</artifacts_produced>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 0 — Pre-flight fixes (reversible) — D-05, D-11, D-06
     Two SEPARATE commits (test(inbound): for D-05, ci: for D-11) + a
     fail-closed publish.check that may legitimately update the allowlist.
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1 (Wave 0): Fix the inbound docs_contract_test contract break — re-derive the expected core pin from the CORE RELEASE version, keep a positive floor-admits-core assertion (D-05, BLOCKING)</name>
  <files>mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>

  <read_first>
    CONTEXT D-05 (the full resolution direction) — this is the load-bearing landmine.
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs — the failing test "README and install
    guide pins match current inbound and mailglass release lines" at ~L446-472. The two coupled problems:
      (1) L457-458: `expected_mailglass_pin` is regex-extracted from mix.exs via
          `~r/\{:mailglass,\s*"==\s*(\d+\.\d+)\.\d+"/` — that `==`-only regex no longer matches the loosened
          `{:mailglass, "~> 1.10 and >= 1.10.2"}`, so it flunks with "mix.exs is missing the MIX_PUBLISH
          mailglass pin".
      (2) L466-471: for each doc it asserts the doc's `{:mailglass, "~> X.Y"}` core pin equals that
          extracted value. On the RP release branch, release-please.yml's sed (release-please.yml ~L188)
          rewrites the README/install core pin to CORE_MM (the CORE major.minor = 1.11), while inbound's
          mix.exs floor intentionally stays 1.10 — so even a naive `~>`-regex fix would fail 1.11 ≠ 1.10.
    The module attributes at the top of the file: @mixfile_path resolves to `../../mix.exs` (inbound mix.exs)
    from the test dir; the repo root is `../../../` from the test file (verified: `../../../
    .release-please-manifest.json` and `../../../mix.exs` both exist and are the repo-root core files).
    release-please.yml ~L153-207 — CORE_MM = core manifest `.["."]` major.minor is what the README sed
    targets; that is the value the README core pin tracks on the release branch.
  </read_first>

  <action>
    Confirm the test is RED first (baseline): run
    `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --seed 0` — expect 1
    failure ("mailglass_inbound/mix.exs is missing the MIX_PUBLISH mailglass pin"). This confirms the
    landmine before the fix.

    Rewrite ONLY the test "README and install guide pins match current inbound and mailglass release lines"
    (~L446-472). Keep every OTHER test in the file untouched (including the separate "readme
    mailglass_inbound dep pin major.minor matches the package version" test at ~L423, which is orthogonal
    and already `~>`-based).

    The corrected test MUST:
    1. Re-derive `expected_mailglass_pin` (the expected README/install core `~> X.Y`) from the CORE RELEASE
       version, NOT from the inbound mix.exs floor pin. Read the core version at repo root: read
       `.release-please-manifest.json` (path relative to the test file: `../../../.release-please-manifest.json`
       via Path.expand, matching the existing module-attr idiom), parse JSON, take the `"."` key; if that
       read fails, fall back to the core mix.exs `@version` at `../../../mix.exs`. Take its major.minor.
       Rationale to encode as a comment: the README tracks "what adopters install alongside inbound" =
       CORE major.minor (1.11), which release-please.yml's CORE_MM sed keeps synced — NOT the decoupled
       inbound compatibility floor (1.10).
    2. Keep the inbound-self-pin assertion (`inbound_pin == expected_inbound_pin`, where
       expected_inbound_pin is inbound's own @version major.minor) exactly as-is — that is correct and
       independent of core.
    3. Positively assert the inbound FLOOR PIN still EXISTS and ADMITS the core version, so the decoupling
       stays intentional (not accidental): extract the floor constraint from the loosened mix.exs pin
       (parse `{:mailglass, "~> 1.10 and >= 1.10.2"}` — a `~>`-and-`>=` form, NOT the old `==` form) and
       assert `Version.match?(core_version, floor_constraint)` is true (core 1.11.0 satisfies
       `~> 1.10 and >= 1.10.2`). Do NOT assert the floor equals the README pin (that is the invariant that
       breaks under decoupling). Read the full core version (not just major.minor) from the same manifest/
       mix.exs source for the Version.match?.

    DO NOT introduce a bare literal `== ` mailglass-pin regex expectation anywhere — the whole point is the
    pin is now `~>`. The fixed test must PASS on BOTH:
      - main TODAY: core 1.10.2, README `{:mailglass, "~> 1.10"}`, floor `~> 1.10 and >= 1.10.2` →
        expected core `~> 1.10` == README `~> 1.10` ✓, and 1.10.2 satisfies the floor ✓.
      - RP release branch: core 1.11.0 (manifest `.["."]` = 1.11.0 after RP bump), README seded to
        `{:mailglass, "~> 1.11"}`, floor still `~> 1.10 and >= 1.10.2` → expected core `~> 1.11` ==
        seded README `~> 1.11` ✓, and 1.11.0 satisfies the floor ✓.
      Simulate the release-branch case in the reasoning by treating the manifest core value as 1.11.0 and
      the README core pin as `~> 1.11` — the test derives expected from the manifest, so both move together.

    Run the inbound docs suite to green:
    `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --seed 0` → 0 failures.
    Then run the whole inbound docs-adjacent contract file once more to confirm no collateral breakage.

    Commit (test-only; inbound's minor is already banked from feat(125-01), so the type does NOT affect
    versioning):
    `git add mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`
    `git commit -m "test(inbound): derive docs_contract core pin from core release version, not inbound floor"`

    Scope is inbound-ONLY (D-05): the core `test/mailglass/docs_contract_test.exs` and demo docs_contract
    tests do NOT compare a self-pin this way (no mailglass-`==`-self regex) — do NOT touch them.
    Do NOT run `mix compile --no-optional-deps --force` on the shared _build (CLAUDE.md).
  </action>

  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --seed 0 2>&1 | grep -Eq '0 failures'</automated>
  </verify>

  <acceptance_criteria>
    Before the fix: `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --seed 0`
    reported 1 failure. After the fix it reports `0 failures`.

    `grep -c 'release-please-manifest.json' mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`
    is ≥ 1 (the expected core pin is now derived from the core release manifest, not the inbound floor).

    The corrected test still asserts `inbound_pin == expected_inbound_pin` (inbound self-pin unchanged) AND
    contains a `Version.match?` call asserting the core version satisfies the inbound floor constraint (the
    positive floor-admits-core assertion, D-05).

    `git show --stat HEAD` shows ONLY
    `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` changed, with a `test(inbound):`
    message. No core/demo docs_contract test files are touched.
  </acceptance_criteria>

  <done>
    The inbound docs_contract_test derives the expected README/install core pin from the CORE RELEASE
    version (manifest `.["."]` / core mix.exs @version), keeps the inbound-self-pin assertion, and adds a
    positive Version.match?(core, floor) assertion. It passes on main today AND on the simulated RP
    release-branch semantics (core 1.11 / README `~> 1.11` / floor `~> 1.10`). Committed as
    `test(inbound):`. The inbound docs suite is green.
  </done>
</task>


<task type="auto">
  <name>Task 2 (Wave 0): Make the post-publish-smoke inbound-compat check `~>`-aware so inbound smoke actually runs under the loosened pin (D-11, SHIP-02)</name>
  <files>.github/workflows/post-publish-smoke.yml</files>

  <read_first>
    CONTEXT D-11 (LANDMINE B) — the silently-skipped-inbound-smoke degradation.
    .github/workflows/post-publish-smoke.yml — the "Check published inbound compatibility" step (id:
    inbound-compat) at ~L341-362. The decision at ~L356 is
    `if echo "${INFO}" | grep -F "mailglass == ${VERSION}" >/dev/null; then include_inbound=true`.
    Under the loosened pin, `mix hex.info mailglass_inbound 1.6.0` shows the dependency as
    `mailglass ~> 1.10 and >= 1.10.2` (a `~>`/`>=` form), NOT `mailglass == 1.11.0` — so the `-F "mailglass
    == ${VERSION}"` fixed-string grep NEVER matches → include_inbound=false → the inbound consumer smoke is
    SILENTLY SKIPPED. That skip defeats exactly the `~>`-resolves-from-Hex proof SHIP-02 wants.
    scripts/consumer_install_smoke.sh — its INCLUDE_INBOUND input flows from this step's output.
    Note: `consumer_install_smoke.sh` injects the CONSUMER's `== ${VERSION}` pins itself — that is a
    separate, correct mechanism and MUST stay unchanged (D-11).
  </read_first>

  <action>
    Edit ONLY the "Check published inbound compatibility" step's compatibility decision (~L356) in
    `.github/workflows/post-publish-smoke.yml`. Make it `~>`-aware so that the loosened inbound dep on
    `mailglass` is recognized as compatible with the just-published core `${VERSION}` and sets
    `include_inbound=true`.

    Preferred approach (robust, avoids reparsing prose): after `INFO=$(mix hex.info mailglass_inbound
    "${VERSION_INBOUND}" ...)`, extract the mailglass dependency requirement string from INFO and test
    whether the published core `${VERSION}` satisfies it. Two acceptable implementations:
      (a) Grep-based `~>`-aware match: treat the step as compatible if INFO contains a `mailglass` dependency
          line in EITHER the legacy exact form (`mailglass == ${VERSION}`) OR the loosened form
          (a `mailglass ~>` / `mailglass .* >=` requirement). This keeps backward-compat with any future
          `==` pin while admitting the current `~>` pin. Anchor the match on the `mailglass` dependency line
          (not `mailglass_inbound`, not `mailglass_admin`) to avoid false matches.
      (b) Version.match via Elixir: capture the requirement substring (everything after `mailglass ` on the
          dependency line) and run a one-liner `mix run`/`elixir -e` that asserts
          `Version.match?("${VERSION}", req)`; set include_inbound from its exit status.
    Either is fine (Claude's discretion) — (a) is simpler and sufficient. Keep the existing empty-
    `VERSION_INBOUND` early-`include_inbound=false` guard (~L347-351) unchanged. Keep the echoed diagnostic
    lines (adjust their wording to reflect the `~>` case).

    Do NOT change `consumer_install_smoke.sh` (its consumer-side `==` pins are correct and intentional).
    Do NOT change the publish-hex.yml ordering or the release-please pins.

    Validate the workflow YAML: run `actionlint .github/workflows/post-publish-smoke.yml` if actionlint is
    available locally (Phase 130 wired it), else `python3 -c "import yaml,sys;
    yaml.safe_load(open('.github/workflows/post-publish-smoke.yml'))"` to confirm it parses. Reason through
    the two hex.info shapes: assert your changed condition sets include_inbound=true for input
    `mailglass ~> 1.10 and >= 1.10.2` with VERSION=1.11.0, AND still true for a hypothetical
    `mailglass == 1.11.0`, AND false for a genuinely incompatible pin (e.g. `mailglass ~> 2.0`).

    Commit (root/.github path — attributed to core, which is already scoring a minor; verify in Task 4 it
    does not perturb the intended 1.11.0 core score):
    `git add .github/workflows/post-publish-smoke.yml`
    `git commit -m "ci: make post-publish-smoke inbound-compat check ~>-aware for loosened sibling pin"`
  </action>

  <verify>
    <automated>grep -q 'mailglass ~>' .github/workflows/post-publish-smoke.yml || grep -q 'Version.match' .github/workflows/post-publish-smoke.yml</automated>
  </verify>

  <acceptance_criteria>
    The "Check published inbound compatibility" step no longer relies solely on
    `grep -F "mailglass == ${VERSION}"`; it now recognizes a loosened `mailglass ~> ... >= ...` dependency
    (via a `~>`-aware grep OR a `Version.match?` check) as compatible and sets `include_inbound=true`.

    `.github/workflows/post-publish-smoke.yml` parses as valid YAML (actionlint clean, or `yaml.safe_load`
    succeeds).

    `git show --stat HEAD` shows ONLY `.github/workflows/post-publish-smoke.yml` changed with a `ci:`
    message. `scripts/consumer_install_smoke.sh` is NOT modified.
  </acceptance_criteria>

  <done>
    post-publish-smoke.yml's inbound-compat check is `~>`-aware: under the loosened
    `{:mailglass, "~> 1.10 and >= 1.10.2"}` pin it sets include_inbound=true so the inbound consumer smoke
    actually runs (no silent skip). YAML validates. Committed as `ci:`. consumer_install_smoke.sh unchanged.
  </done>
</task>


<task type="auto">
  <name>Task 3 (Wave 0): Fail-closed publish.check for all three packages; commit any legitimate allowlist snapshot drift (D-06)</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-files.expected, .planning/publish/mailglass_admin-publish-summary.json, .planning/publish/mailglass_inbound-files.expected, .planning/publish/mailglass_inbound-publish-summary.json</files>

  <read_first>
    CONTEXT D-06 — unlike v1.14, Phases 125–130 added NEW tracked files (e.g. lib/mailglass/ci_lanes.ex,
    mix_audit wiring), and the allowlist gate ONLY runs at release (not in PR CI — the 1.10.2 recovery lost
    3 tag-move cycles to exactly this). So EXPECT a possible legitimate allowlist snapshot update this cycle
    — do NOT assume a clean no-op.
    MAINTAINING.md — "Publish Summary Snapshot Protocol" (mix mailglass.publish.check, regeneration steps,
    commit-message guidance) + the required-vs-advisory lane list.
    lib/mix/tasks/mailglass.publish.check.ex — verify_deps (~L814 rejects `==`), verify_linked_constraint
    (~L836): confirms pins are correctly `~>` (they already are — Phase 125).

    CRITICAL (CLAUDE.md "Things Not To Do" + D-06): Do NOT run `mix compile --no-optional-deps --force` on
    the shared _build. Use only `mix mailglass.publish.check --package <pkg>`. Per the demo_app
    swoosh-lock-drift gotcha, if any mix invocation re-bumps a reference mix.lock, `git checkout` that lock
    before committing — never commit transitive drift here.
    Note (D-07): phoenix_storybook lives ONLY in reference/demo_app/mix.exs (only: :dev); the demo is never
    published — no allowlist action for it.
  </read_first>

  <action>
    Step 3.1 — Clean worktree check:
    Run `git status`. The allowlist snapshot captures untracked files, so the source tree must be clean
    before running publish.check. If the user carries dirty .planning/ WIP, note it but do not regenerate
    against a dirty SOURCE tree (source files must be clean; the two Wave-0 commits above are already made).

    Step 3.2 — Fail-closed publish.check (D-06):
    From repo root run each in turn:
      `mix mailglass.publish.check --package mailglass`
      `mix mailglass.publish.check --package mailglass_admin`
      `mix mailglass.publish.check --package mailglass_inbound`
    Each MUST exit 0. This also confirms the pins are correctly `~>` (verify_deps would fail on a stray
    `==`). If a package fails on an ACTUAL policy violation (not a snapshot refresh), STOP and report — do
    not paper over it.

    Step 3.3 — Handle allowlist snapshot drift (EXPECTED possible this cycle, D-06):
    Run `git status .planning/publish/`. If publish.check rewrote any `.planning/publish/*` snapshot:
      - review the diff (`git diff .planning/publish/`) and confirm the added/removed files are the
        legitimate Phase 125–130 additions (e.g. lib/mailglass/ci_lanes.ex, mix_audit wiring) — not
        something unexpected;
      - confirm no reference mix.lock drift snuck in (`git checkout` any drifted reference lock FIRST);
      - stage ONLY the changed `.planning/publish/*` files;
      - commit: `chore: refresh publish allowlist snapshots for v1.11.0 release`
        (`chore:` does NOT trigger a Release Please version bump — intentional).
    If nothing changed, make NO commit and proceed. Either outcome is acceptable (D-06 expects a possible
    update but a no-op is also fine).
  </action>

  <verify>
    <automated>mix mailglass.publish.check --package mailglass && mix mailglass.publish.check --package mailglass_admin && mix mailglass.publish.check --package mailglass_inbound</automated>
  </verify>

  <acceptance_criteria>
    `mix mailglass.publish.check --package mailglass` exits 0.
    `mix mailglass.publish.check --package mailglass_admin` exits 0.
    `mix mailglass.publish.check --package mailglass_inbound` exits 0.

    `git status --porcelain .planning/publish/` is empty (no-op), OR `git show --stat HEAD` shows only
    `.planning/publish/` files with a `chore: refresh publish allowlist snapshots` message and NO reference
    mix.lock files staged.
  </acceptance_criteria>

  <done>
    publish.check exits 0 for all three packages (pins confirmed `~>`). Any legitimate Phase 125–130
    allowlist snapshot drift was reviewed and committed as a scoped `chore:` (or a clean no-op with no
    commit). No reference mix.lock drift committed. Source tree ready for the fast-forward push.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 1 — Clean ff-push + regenerate/verify the RP PR (reversible)
              — D-01, D-02, D-03, D-04
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 4 (Wave 1): Clean fast-forward push of the v1.15 body; confirm green ci.yml on the new HEAD (D-04, SHIP-01)</name>
  <files></files>

  <read_first>
    CONTEXT D-04 (clean fast-forward; NEVER --force) — local main is 71 ahead / 0 behind origin/main, so a
    plain `git push origin main` is an ordinary fast-forward. NO rebase-reconcile (that was 124's step;
    DROPPED here) and NO `fix(inbound): re-pin ==` commit (DROPPED — the keystone payoff; the inbound 1.6.0
    minor is already banked from feat(125-01)).
    CLAUDE.md — the never-`--force` rule; confirm a direct `git push origin main` is permitted by branch
    protection before relying on it.
    .github/workflows/release-please.yml — auto-merge arming; cron self-heal; confirm (visually) the `==`
    sed rewrites were DELETED in Phase 125 (PIN-03) so a core bump no longer touches sibling pin lines.

    HARD RULE: NEVER `git push --force` (D-04). If a non-fast-forward rejection appears, origin advanced
    (a new dependabot merge) — `git fetch origin && git rebase origin/main`, re-confirm the two Wave-0
    commits are present, then re-push. NEVER force.
  </read_first>

  <action>
    Step 4.1 — Confirm the Wave-0 commits are present on local main:
    `git log --oneline -6` should show the `test(inbound):` (D-05) and `ci:` (D-11) commits (and the
    optional `chore:` allowlist commit if one was made) on top of the v1.15 body. `git status` clean.

    Step 4.2 — Re-fetch and confirm still a clean fast-forward (close to push time):
    `git fetch origin && git rev-list --left-right --count origin/main...HEAD`
    Expect `0<TAB>N` (0 behind, N ahead) — a fast-forward. If it shows a non-zero left count (behind),
    origin advanced: `git rebase origin/main`, re-confirm the two Wave-0 commits with `git log --oneline`,
    then re-check. NEVER force.

    Step 4.3 — Fast-forward push (NEVER --force, D-04):
    `git push origin main`
    Because local HEAD descends from current origin HEAD, this is an ordinary fast-forward — no force. If a
    non-fast-forward rejection appears, re-run Step 4.2 (fetch + rebase) and re-push. NEVER force.

    Step 4.4 — Confirm ci.yml runs and goes GREEN on the new origin/main HEAD (D-04 — load-bearing):
    The push is human-authored (admin direct push), so it DOES trigger ci.yml (the "no ci.yml runs found
    for SHA" anti-recursion gap bites only the later bot-auto-merged release SHA, covered by
    gate-ci-green's self-heal — not this push). Watch it:
    `gh run list --workflow ci.yml --branch main --limit 3`
    Wait for the run on the new HEAD to complete and confirm the required lanes are GREEN (the aggregate
    `CI Green` job from Phase 126/GATE-01 is the SOLE required gate — confirm it is green; advisory lanes
    such as Operator Browser Gate / Core Full Suite Advisory may be yellow/red without blocking). The
    pushed HEAD SHA is what RP will tag and what gate-ci-green requires a green ci.yml run on — it MUST be
    green before authorizing the RP-PR merge in Wave 2.
  </action>

  <verify>
    <automated>test "$(git rev-parse origin/main)" = "$(git rev-parse HEAD)"</automated>
  </verify>

  <acceptance_criteria>
    `git rev-parse origin/main` equals `git rev-parse HEAD` (the full v1.15 body + the two Wave-0 fixes
    reached origin via fast-forward).

    `git reflog` / `git log` show NO force-push was used (origin HEAD is a descendant of the prior origin
    HEAD; the push was a fast-forward).

    `gh run list --workflow ci.yml --branch main --limit 3` shows a completed run on the new origin HEAD
    with the aggregate `CI Green` gate (and required lanes) green.
  </acceptance_criteria>

  <done>
    The full v1.15 body (incl. the two Wave-0 fixes) is on origin/main via a clean fast-forward push (no
    force). origin/main == local HEAD. ci.yml is green on the new HEAD — the SHA RP will tag is
    gate-ci-green-eligible. The push has triggered Release Please to regenerate the linked release PR
    (verified next task).
  </done>
</task>


<task type="auto">
  <name>Task 5 (Wave 1): Verify the regenerated Release Please PR — core+admin 1.11.0, inbound minor 1.6.0, pins STAY `~>` (no re-pin), CHANGELOGs, required CI green, auto-merge armed (D-01, D-02, D-03, SHIP-01)</name>
  <files></files>

  <read_first>
    .github/workflows/release-please.yml — the README `{:mailglass, "~> X.Y"}` pin sed sync (~L153-207;
    seds the README/install core pin to CORE_MM=1.11); auto-merge arming; cron self-heal. Confirm there is
    NO `==` sibling-pin sed anymore (deleted Phase 125/PIN-03) — the sibling mix.exs pin LINES must stay
    `~>` on the PR branch.
    MAINTAINING.md — Step 2 "Review the release PR diff before merge" + the Release-As fallback (~L319).
    CONTEXT D-01 (RP scores; verify, don't hardcode; Release-As: 1.11.0 fallback only if RP scores a patch),
    D-02 (pins stay `~>`; NO re-pin commit), D-03 (floor stays >= 1.10.2, NOT bumped to >= 1.11.0).
  </read_first>

  <action>
    Step 5.1 — Wait for Release Please to regenerate the linked PR (D-01):
    The Task-4 push fired release-please.yml. Wait for it:
    `gh run list --workflow release-please.yml --limit 3` (watch the latest run to success). Then locate
    the regenerated "chore: release main" PR:
    `gh pr list --search "chore: release main" --state open --json number,title,headRefName`
    Record its number (PR_N). If RP did not open/update the PR within ~10 min of the push completing,
    dispatch it: `gh workflow run release-please.yml` (no inputs) and re-check. (NOTE: PR #96 is an
    unrelated dependabot igniter bump — do NOT mistake it for the release PR.)

    Step 5.2 — Verify manifest + @version bumps are 1.11.0 / 1.11.0 / 1.6.0 (D-01):
    `gh pr diff PR_N` — confirm the regenerated PR shows:
    - `.release-please-manifest.json`: `"."` (core) = "1.11.0", `"mailglass_admin"` = "1.11.0",
      `"mailglass_inbound"` = "1.6.0".
    - `mix.exs` has `@version "1.11.0"` (core)
    - `mailglass_admin/mix.exs` has `@version "1.11.0"`
    - `mailglass_inbound/mix.exs` has `@version "1.6.0"`
    FALLBACK (D-01): if core/admin scored a PATCH (e.g. 1.10.3) despite the body being on origin, apply
    `Release-As: 1.11.0` per MAINTAINING.md ~L319 (land an empty commit carrying the `Release-As: 1.11.0`
    footer on origin/main, then re-dispatch release-please.yml). Do NOT reach for Release-As unless the
    natural re-score is demonstrably a patch — D-01 makes it a fallback only. Do NOT hand-edit the manifest
    or @version.

    Step 5.3 — Verify inbound 1.6.0 is present in the SAME PR (D-01 — BLOCKING):
    Confirm PR_N includes the inbound 1.6.0 version bump AND CHANGELOG entry (scored by RP from the banked
    feat(125-01) pin-loosening commit — no deliberate re-pin commit was needed). If inbound is ABSENT from
    the regenerated PR, STOP and investigate before merge — publish-admin `needs: [publish-core,
    publish-inbound]` would block admin publish without a paired inbound release.

    Step 5.4 — Verify the sibling pins STAY `~>` (D-02, D-03 — the keystone payoff, BLOCKING):
    `gh pr diff PR_N -- mailglass_inbound/mix.exs` — the `{:mailglass, ...}` dep line MUST remain
    `{:mailglass, "~> 1.10 and >= 1.10.2"}`. There MUST be NO change re-pinning it to `== 1.11.0` and NO
    floor bump to `>= 1.11.0` (D-03). `gh pr diff PR_N -- mailglass_inbound/mix.exs | grep -F '== 1.11.0'`
    MUST return NOTHING; `gh pr diff PR_N -- mailglass_inbound/mix.exs | grep -F '>= 1.11.0'` MUST return
    NOTHING.
    `gh pr diff PR_N -- mailglass_admin/mix.exs` — the admin `{:mailglass, "~> 1.10"}` dep line MUST remain
    `~>` (no `== 1.11.0`). (The only sibling-file diffs expected are @version bumps and CHANGELOG entries —
    NOT pin-line rewrites, since the `==` seds were deleted in Phase 125.)

    Step 5.5 — Verify the README/install core pins WERE seded to `~> 1.11` (release-please.yml CORE_MM sed):
    `gh pr diff PR_N -- mailglass_inbound/README.md` (and `mailglass_inbound/docs/inbound-install.md`) —
    the `{:mailglass, "~> X.Y"}` install pin should be rewritten to `~> 1.11` by the CORE_MM sed. This is
    the exact scenario the Task-1 D-05 fix accommodates (README core pin 1.11, inbound floor still 1.10).

    Step 5.6 — Review CHANGELOGs (SHIP-01):
    `gh pr diff PR_N -- CHANGELOG.md` (core), `... -- mailglass_admin/CHANGELOG.md`,
    `... -- mailglass_inbound/CHANGELOG.md`. Confirm a `## [1.11.0]` heading for core+admin and a
    `## [1.6.0]` heading for inbound. For the inbound CHANGELOG, per D-03, ensure the "verified against core
    1.11" assertion lands in the release notes (light hand-curation at Claude's discretion — add a one-line
    "Verified against mailglass 1.11.0; compatibility floor intentionally stays `>= 1.10.2` per LD-2" note
    if RP's generated entry doesn't already convey it). RP-generated entries are otherwise sufficient for a
    routine infra/DX cycle.

    Step 5.7 — Check required CI + auto-merge (SHIP-01):
    `gh pr checks PR_N` — the aggregate `CI Green` required gate (and any other required lanes) must be
    REPORTING and green (advisory lanes may be yellow/red without blocking). If any REQUIRED lane is red,
    investigate before the Wave-2 checkpoint. Confirm the D-11 post-publish-smoke change did not turn a
    required lane red (it's a post-publish workflow, not a PR-required lane — it should not gate the PR).
    `gh pr view PR_N --json autoMergeRequest` — the field must be non-null (RP arms it). If null:
    `gh pr merge PR_N --auto --squash`.

    Do NOT approve or merge yet. Task 6 is the irreversible maintainer go/no-go checkpoint.
  </action>

  <verify>
    <automated>PR_N=$(gh pr list --search "chore: release main" --state open --json number -q '.[0].number') && gh pr diff "$PR_N" | grep -q '1.11.0' && gh pr diff "$PR_N" | grep -q '1.6.0' && ! gh pr diff "$PR_N" -- mailglass_inbound/mix.exs | grep -qF '== 1.11.0'</automated>
  </verify>

  <acceptance_criteria>
    A single open "chore: release main" PR exists (PR_N).

    `gh pr diff PR_N` shows manifest "1.11.0" for `.` (core) and "mailglass_admin", and "1.6.0" for
    "mailglass_inbound"; @version "1.11.0" in mix.exs and mailglass_admin/mix.exs; @version "1.6.0" in
    mailglass_inbound/mix.exs. (If a Release-As: 1.11.0 fallback was needed, the diff still ends at these
    values.)

    `gh pr diff PR_N -- mailglass_inbound/mix.exs | grep -F '== 1.11.0'` returns NOTHING, and
    `... | grep -F '>= 1.11.0'` returns NOTHING (pins stay `~> 1.10 and >= 1.10.2`; no re-pin, no floor
    bump — D-02/D-03). `gh pr diff PR_N -- mailglass_admin/mix.exs | grep -F '== 1.11.0'` returns NOTHING.

    `gh pr diff PR_N -- CHANGELOG.md | grep '## \[1.11.0\]'` returns a match; the admin CHANGELOG diff has
    `## [1.11.0]`; the inbound CHANGELOG diff has `## [1.6.0]`.

    `gh pr checks PR_N` shows the required `CI Green` gate green (or orange while waiting; not red on
    required). `gh pr view PR_N --json autoMergeRequest` returns a non-null autoMergeRequest.
  </acceptance_criteria>

  <done>
    The regenerated linked release PR is verified: core+admin 1.11.0, inbound minor 1.6.0 in the SAME PR,
    sibling pins STAY `~> 1.10 and >= 1.10.2` (no re-pin, no floor bump — the keystone payoff), README/
    install core pins seded to `~> 1.11`, CHANGELOG entries for all three packages (inbound notes "verified
    against core 1.11"), required CI green, auto-merge armed. (Release-As: 1.11.0 fallback applied only if
    RP scored a patch.) Ready for the maintainer go/no-go.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 2 — Merge + publish (IRREVERSIBLE — maintainer go/no-go) — D-08
     ═══════════════════════════════════════════════════════════════════ -->

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6 (Wave 2): MAINTAINER GO/NO-GO — authorize merge of the linked release PR (IRREVERSIBLE after Hex publish)</name>
  <files></files>

  <action>
    HARD HUMAN GATE — no autonomous action. The executor PAUSES here and presents the go/no-go to the
    maintainer (see what-built / how-to-verify / resume-signal below). The executor does not approve,
    merge, or dispatch any publish on its own. After the maintainer types "approved" and the publish
    fan-out is observed starting in Actions, proceed to Task 7 (Wave 3). If "blocked: [reason]", halt the
    ceremony and report. This is the single irreversible authorization point (D-08).
  </action>

  <what-built>
    Wave 0 landed the two pre-flight fixes the keystone pin-loosening left behind: D-05 (the inbound
    docs_contract_test now derives the expected core pin from the CORE RELEASE version and asserts the floor
    admits it — green on main AND the RP release branch) and D-11 (post-publish-smoke's inbound-compat
    check is now `~>`-aware so inbound smoke actually runs), plus a fail-closed publish.check for all three
    packages (any legitimate Phase 125–130 allowlist drift committed as chore:). Wave 1 fast-forward pushed
    the full v1.15 body (NO force, D-04 — no rebase-reconcile was needed; no `fix(inbound): re-pin` dance —
    the keystone payoff) and confirmed ci.yml is green on the new origin HEAD. That push re-scored the
    regenerated Release Please PR to mailglass 1.11.0 / mailglass_admin 1.11.0 / mailglass_inbound 1.6.0
    (D-01), with the sibling pins VERIFIED to STAY `~> 1.10 and >= 1.10.2` (no re-pin, no floor bump —
    D-02/D-03), README/install core pins seded to `~> 1.11`, and CHANGELOG entries for all three packages.
    Required CI (`CI Green`) is green and auto-merge is armed. The pipeline is fully hands-free once this
    checkpoint passes.

    IRREVERSIBILITY WARNING: After mailglass 1.11.0 publishes to Hex, Hex.pm allows revert only within
    60 minutes AND only if zero downloads have occurred. After that window, only `mix hex.retire`
    (deprecation) or a patch release remain. This is the one human go/no-go gate in the ceremony.
  </what-built>

  <how-to-verify>
    1. Visit the open "chore: release main" PR (PR_N) on https://github.com/szTheory/mailglass/pulls.
    2. Confirm it is the REGENERATED PR (versions 1.11.0/1.11.0/1.6.0, not a stale snapshot).
    3. Confirm the Files Changed tab shows:
       - mailglass @version "1.11.0", mailglass_admin @version "1.11.0", mailglass_inbound @version "1.6.0"
       - mailglass_inbound/mix.exs dep stays `{:mailglass, "~> 1.10 and >= 1.10.2"}` — NO `== 1.11.0`, NO
         `>= 1.11.0` floor bump (the keystone `~>` decoupling)
       - mailglass_admin/mix.exs dep stays `{:mailglass, "~> 1.10"}` — NO `== 1.11.0`
       - README/install `{:mailglass, "~> X.Y"}` install pins seded to `~> 1.11`
       - CHANGELOG entries under ## [1.11.0] (core+admin) / ## [1.6.0] (inbound); the inbound entry notes
         it is verified against core 1.11 with the floor intentionally staying >= 1.10.2
    4. Confirm the required `CI Green` check is green on the PR.
    5. If satisfied: APPROVE — auto-merge will squash-merge the PR; the hourly cron or a manual
       `gh workflow run release-please.yml` will tag the commit and fire the publish fan-out.

    After approving, monitor the Actions tab for:
    - release-please.yml completing (tag creation, GitHub Release)
    - publish-hex.yml fan-out triggered by `release: published`
    - prepublish-summary → gate-ci-green → publish-core → publish-inbound → publish-admin
      (publish-admin needs BOTH publish-core AND publish-inbound)

    STALL RECOVERY (documented, NOT the default path — from MAINTAINING.md):
    - Anti-recursion stall (PR merged but no tag): `gh workflow run release-please.yml` (no inputs). A
      bot-merged release SHA gets no ci.yml run, so gate-ci-green can block with "no ci.yml runs found for
      SHA" — gate-ci-green self-heals (dispatches ci.yml on the tag, waits ~30 min).
    - publish fan-out failed after tag: re-dispatch from the RELEASE TAG, NEVER from main:
      `gh workflow run publish-hex.yml --ref mailglass-v1.11.0 --field tag=mailglass-v1.11.0 --field dry_run=false --field package=all`
  </how-to-verify>

  <resume-signal>
    Type "approved" after authorizing the merge and observing that the publish fan-out has started
    (publish-hex.yml run visible in Actions), or type "blocked: [reason]" to halt and describe what you
    found in the PR diff.
  </resume-signal>

  <verify>
    <human-check>Maintainer reviewed the linked release PR (versions 1.11.0/1.11.0/1.6.0, sibling pins STAY `~> 1.10 and >= 1.10.2` with NO re-pin and NO floor bump, CHANGELOGs, required CI green) and typed "approved"; publish-hex.yml fan-out is visible in the Actions tab.</human-check>
  </verify>

  <done>
    Maintainer authorized the merge of the linked release PR; publish-hex.yml fan-out has started (visible
    in Actions), or the ceremony was halted with a documented "blocked: [reason]".
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 3 — Confirm Hex live (read-only) — SHIP-01, D-02
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 7 (Wave 3): Confirm all three packages live on Hex at target versions + inbound shows the LOOSENED `~>` pin (SHIP-01, D-02)</name>
  <files></files>

  <read_first>
    .github/workflows/publish-hex.yml — publish job ordering: publish-core → publish-inbound →
    publish-admin; publish-admin needs [publish-core, publish-inbound]; idempotency guards (mix hex.info
    check before publish); advisory lane list.
    MAINTAINING.md — Step 3 "Monitor the hands-free publish fan-out".
    CONTEXT D-02 — inbound's Hex dependency must show the LOOSENED `~> 1.10 and >= 1.10.2` (NOT `== 1.11.0`).
    This is the keystone proof, inverted from 124's `== 1.10.0` expectation.
  </read_first>

  <action>
    Step 7.1 — Watch the publish fan-out in Actions:
    `gh run list --workflow publish-hex.yml --limit 3` to find the run triggered by `release: published`.
    Monitor jobs: prepublish-summary → gate-ci-green → publish-core → publish-inbound → publish-admin. All
    must succeed (or skip via idempotency — already-published is success). Advisory jobs may fail without
    blocking. NOTE: publish-admin will not start until BOTH publish-core and publish-inbound succeed — a
    missing inbound release would stall admin here.

    Step 7.2 — Verify core on Hex:
    `mix hex.info mailglass 1.11.0`. Wait for Hex indexing if needed (up to ~5 min; publish-core has a
    built-in index wait). Output must contain "Released:".

    Step 7.3 — Verify admin on Hex:
    `mix hex.info mailglass_admin 1.11.0`. Output must contain "Released:".

    Step 7.4 — Verify inbound on Hex with the LOOSENED pin (SHIP-01, D-02 — the keystone proof):
    `mix hex.info mailglass_inbound 1.6.0`. Output must contain "Released:" AND must show the dependency as
    the LOOSENED form. Confirm:
    `mix hex.info mailglass_inbound 1.6.0 | grep 'mailglass'` returns a line matching
    `mailglass ~> 1.10 and >= 1.10.2` (NOT `== 1.11.0`). If it shows `== anything`, the keystone regressed —
    STOP and investigate (a stray `==` pin would defeat LD-2).

    Step 7.5 — HexDocs spot check (may lag Hex by 5–10 min):
    - https://hexdocs.pm/mailglass/1.11.0/
    - https://hexdocs.pm/mailglass_admin/1.11.0/
    - https://hexdocs.pm/mailglass_inbound/1.6.0/
    Each URL must return HTTP 200 (not 404 / redirect to latest).

    NOTE: the 60-minute revert window starts when publish-core completes (Step 7.2 green). Set a timer —
    Task 8 (consumer smoke) must complete within this window.
  </action>

  <verify>
    <automated>mix hex.info mailglass 1.11.0 | grep -q 'Released:' && mix hex.info mailglass_admin 1.11.0 | grep -q 'Released:' && mix hex.info mailglass_inbound 1.6.0 | grep -q 'Released:'</automated>
  </verify>

  <acceptance_criteria>
    `mix hex.info mailglass 1.11.0` output contains "Released:".
    `mix hex.info mailglass_admin 1.11.0` output contains "Released:".
    `mix hex.info mailglass_inbound 1.6.0` output contains "Released:" AND
    `mix hex.info mailglass_inbound 1.6.0 | grep 'mailglass'` returns a line matching
    `mailglass ~> 1.10 and >= 1.10.2` (the loosened pin — NOT `== 1.11.0`).
    All three publish-hex.yml jobs (publish-core, publish-inbound, publish-admin) show
    `completed: success` or `completed: skipped` (idempotent re-run) in `gh run view`.
  </acceptance_criteria>

  <done>
    mailglass 1.11.0, mailglass_admin 1.11.0, mailglass_inbound 1.6.0 confirmed live on Hex. Inbound
    dependency shows the LOOSENED `mailglass ~> 1.10 and >= 1.10.2` (keystone proof). 60-minute revert
    window timer started.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 4 — Consumer + post-publish smoke (within 60-minute window)
              — SHIP-02, D-09, D-11
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 8 (Wave 4): Consumer smoke (DEP_MODE=hex) + post-publish-smoke green within the 60-minute window — the `~>`-resolves-from-Hex keystone proof (SHIP-02, D-09, D-11)</name>
  <files></files>

  <read_first>
    scripts/consumer_install_smoke.sh — DEP_MODE=hex path; OPS-01 guard (no hackney/finch in mix.lock);
    INCLUDE_INBOUND flag; VERSION / VERSION_INBOUND vars. The consumer app pins its OWN deps `== 1.11.0` /
    `== 1.6.0` (consumer's choice — fine), forcing mix to resolve inbound 1.6.0's `~> 1.10 and >= 1.10.2`
    against core 1.11.0 → this IS the keystone `~>`-admits-newer-core proof (D-09).
    .github/workflows/post-publish-smoke.yml — smoke job chain; the D-11-FIXED inbound-compat check (now
    `~>`-aware, so it sets include_inbound=true and the inbound smoke actually runs — do NOT accept a
    silently-skipped inbound smoke as a pass).
    MAINTAINING.md — Step 4 (60-minute window manual smoke) + the Retract Decision Tree.
    CONTEXT D-09 — issue #32 swoosh/hackney OPS-01 false-positive and advisory lanes going red are
    NON-BLOCKERS; any OTHER red is a real regression.
  </read_first>

  <action>
    Step 8.1 — Run consumer smoke within the 60-minute window (SHIP-02 Hex-resolution / keystone proof):
    From repo root:
    `VERSION=1.11.0 VERSION_INBOUND=1.6.0 DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh`
    The smoke creates a fresh phx.new sandbox, injects `== 1.11.0` / `== 1.6.0` consumer deps, runs mix
    deps.get, mix mailglass.install, mix compile --warnings-as-errors, boots, and hits GET /dev/mail/ → 200.
    Exit 0 = pass.

    Step 8.2 — Verify mix deps.get Hex resolution — the keystone assertion (SHIP-02, D-09):
    Confirm the smoke's `mix deps.get` step resolved mailglass from Hex at 1.11.0 and mailglass_inbound from
    Hex at 1.6.0, AND that inbound 1.6.0's `~> 1.10 and >= 1.10.2` requirement RESOLVED against core 1.11.0
    without conflict (visible in the resolution output). This is the SHIP-02 proof that the loosened `~>`
    pin admits the newer core from Hex.

    Step 8.3 — Monitor post-publish-smoke (SHIP-02):
    `gh run list --workflow post-publish-smoke.yml --limit 3` to find the run triggered by `release:
    published`. Monitor: cron-guard → wait-for-index → wait-for-hexdocs → inbound-compat (must set
    include_inbound=true now, thanks to the D-11 fix) → consumer-install → published-trust-journey →
    retracted-check. If no run fired within ~10 min of publish, dispatch:
    `gh workflow run post-publish-smoke.yml --field tag=mailglass-v1.11.0`
    CRITICAL (D-11): confirm the inbound-compat step logged include_inbound=true / "compatible" and the
    consumer-install job actually included inbound — a silently-skipped inbound smoke (include_inbound=false)
    is a FAILURE of the SHIP-02 proof, not a pass, even if the run is green. All jobs must pass AND inbound
    must not have been skipped.

    Step 8.4 — D-09 known-noise classification (NON-BLOCKER):
    The issue #32 swoosh/hackney OPS-01 false-positive in `consumer-install` (output containing "hackney" /
    "missing hackney dependency") is an ACCEPTABLE non-blocker, as are advisory lanes going red. If that is
    the only red, document that it matches #32 and proceed. ANY OTHER red is a real regression — evaluate it
    against the MAINTAINING.md Retract Decision Tree. Do not accept any non-#32 failure as noise, and do not
    accept a skipped-inbound smoke as noise.

    Step 8.5 — 60-minute decision (Retract Decision Tree):
    If both consumer smoke (8.1) and post-publish-smoke (8.3, with inbound actually run) pass (or only #32
    noise is red), proceed to Task 9. If a real (non-#32) regression is found within 60 minutes of publish:
    - Zero downloads: revert per MAINTAINING.md for affected packages.
    - Non-zero downloads or past 60 min: evaluate the Retract rules and open a patch release PR.
  </action>

  <verify>
    <automated>VERSION=1.11.0 VERSION_INBOUND=1.6.0 DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh</automated>
  </verify>

  <acceptance_criteria>
    `scripts/consumer_install_smoke.sh` (with VERSION=1.11.0 VERSION_INBOUND=1.6.0 DEP_MODE=hex
    INCLUDE_INBOUND=true) exits 0 AND its `mix deps.get` output shows mailglass 1.11.0 and mailglass_inbound
    1.6.0 fetched from Hex, with inbound's `~> 1.10 and >= 1.10.2` resolving against core 1.11.0.

    `gh run view` of post-publish-smoke.yml shows all jobs passed AND the inbound-compat step set
    include_inbound=true (the D-11 fix worked — inbound smoke was NOT silently skipped), OR the only red job
    is `consumer-install` with output containing "hackney" (issue #32 false-positive, acceptable non-blocker
    per D-09). No other failure, and no silently-skipped inbound smoke, is accepted as a pass.
  </acceptance_criteria>

  <done>
    Consumer smoke passed within the 60-minute window; Hex resolution confirmed inbound 1.6.0's `~>` pin
    admits core 1.11.0 (the keystone SHIP-02 proof). Post-publish smoke passed with the inbound smoke
    actually run (D-11 fix confirmed), or only the #32 hackney noise red. Retract-window decision: proceed.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 5 — Milestone closeout — SHIP-03, D-10
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 9 (Wave 5): Milestone audit (7 phases 125-131 / 26 reqs), archive, git tag v1.15 (SHIP-03, D-10)</name>
  <files>.planning/milestones/v1.15-ROADMAP.md, .planning/milestones/v1.15-REQUIREMENTS.md, .planning/milestones/v1.15-MILESTONE-AUDIT.md, .planning/MILESTONES.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/RETROSPECTIVE.md, .planning/STATE.md</files>

  <read_first>
    CONTEXT D-10 — the full audit scope + the count-inflation gotcha.
    STATE.md — the v1.14 closeout precedent and the milestone.complete count-inflation gotcha.
    124-01-PLAN.md Task 7 (Step 5.1–5.13) for the exact archive sequence + audit-file shape (retarget it).
    .planning/REQUIREMENTS.md — the 26-requirement traceability (23 Complete; SHIP-01/02/03 close in this
    phase) — the authoritative scope source for the audit.

    SCOPE (D-10): 7 phases (125–131) and 26 requirements. The 26 REQ-IDs (verify verbatim against
    REQUIREMENTS.md): PIN-01, PIN-02, PIN-03, PIN-04, PIN-05, GATE-01, GATE-02, GATE-03, GATE-04, DET-01,
    DET-02, MIXCI-01, MIXCI-02, MIXCI-03, MIXCI-04, MIXCI-05, CACHE-01, CACHE-02, SUPPLY-01, SUPPLY-02,
    SUPPLY-03, SUPPLY-04, SUPPLY-05, SHIP-01, SHIP-02, SHIP-03. All 26 status: passed (23 were complete pre-
    phase; SHIP-01..03 close here).

    COUNT-INFLATION WARNING (D-10): Do NOT use `gsd-sdk milestone.complete` raw counts — the CLI sweeps
    leftover 999.1/999.2 backlog dirs and inflates the matrix. Hand-count to the true v1.15 scope: 7 phases
    (125–131), 26 requirements. MILESTONES.md lives at `.planning/MILESTONES.md` (NOT `.planning/milestones/`).
  </read_first>

  <action>
    Step 9.1 — Contract baseline check:
    Run the repo's contract gate (e.g. `mix ci` or the MAINTAINING.md-named support/stability contract
    target) from repo root to confirm the shipped v1.11.0 codebase passes. (If the demo/reference path
    re-bumps a mix.lock, `git checkout` it after — do NOT commit transitive drift.)

    Step 9.2 — Archive ROADMAP:
    `cp .planning/ROADMAP.md .planning/milestones/v1.15-ROADMAP.md`

    Step 9.3 — Archive REQUIREMENTS:
    `cp .planning/REQUIREMENTS.md .planning/milestones/v1.15-REQUIREMENTS.md`

    Step 9.4 — Write MILESTONE-AUDIT (SHIP-03, D-10):
    Create `.planning/milestones/v1.15-MILESTONE-AUDIT.md`. Include:
    - Header: "# v1.15 Milestone Audit — Release-Pipeline Efficiency & Contributor DX"
    - Audit date (actual)
    - Verdict line: `status: passed`
    - True scope stats (CORRECTED, not from gsd-sdk): 7 phases (125–131), 26 requirements, packages shipped
      mailglass 1.11.0 / mailglass_admin 1.11.0 / mailglass_inbound 1.6.0 — inbound carrying the loosened
      `{:mailglass, "~> 1.10 and >= 1.10.2"}` (the keystone payoff)
    - Per-requirement pass/fail table: ALL 26 REQ-IDs marked `status: passed` — PIN-01..05, GATE-01..04,
      DET-01..02, MIXCI-01..05, CACHE-01..02, SUPPLY-01..05, SHIP-01..03
    - Phase table: 125–131 all Complete
    - Evidence references: published Hex URLs (1.11.0/1.11.0/1.6.0), the inbound `~>`-pin hex.info excerpt,
      post-publish-smoke run URL (with inbound smoke actually run per D-11), consumer-smoke keystone proof,
      release PR URL
    - Intent audit (SHIP-03): note the "CI-the-body" method (push phase/NN, require green CI) meant this
      ceremony was a confirmation, and the two Wave-0 landmines (D-05, D-11) were pipeline-migration debt
      surfaced by planning, not regressions

    Step 9.5 — Update MILESTONES.md (at `.planning/MILESTONES.md`):
    Add a v1.15 row with CORRECTED counts:
    `✅ v1.15 Release-Pipeline Efficiency & Contributor DX — 7 phases (125–131), 26 reqs, shipped YYYY-MM-DD — mailglass 1.11.0 / mailglass_admin 1.11.0 / mailglass_inbound 1.6.0 (see milestones/v1.15-MILESTONE-AUDIT.md)`

    Step 9.6 — Update PROJECT.md:
    Advance current-milestone notation to v1.15 shipped; update the "Current state" line to reflect
    1.11.0/1.11.0/1.6.0 as the current live Hex versions and note the keystone `~>` sibling-pin decoupling.

    Step 9.7 — Update ROADMAP.md (scoped Edits, never Write the whole file):
    Move Phase 131 to Complete; flip the v1.15 milestone entry to ✅ SHIPPED; move v1.15 into the
    shipped/archived milestone history list; clear/stub the active-milestone section ("no active milestone
    — see /gsd-new-milestone for next", noting v2.0 schema isolation is queued per the deferred section).

    Step 9.8 — Remove REQUIREMENTS.md (or replace with stub), following the v1.13/v1.14 precedent:
    `git rm .planning/REQUIREMENTS.md` (archived in v1.15-REQUIREMENTS.md), or replace with a one-line stub:
    "# Requirements — awaiting next milestone definition (v2.0 schema isolation queued)".

    Step 9.9 — Append RETROSPECTIVE entry:
    Append a v1.15 section to `.planning/RETROSPECTIVE.md`:
    - Milestone summary (7 phases: CI/CD efficiency + contributor DX + the real linked release dogfood)
    - What worked: the keystone-first ordering (loosen `~>` pins in Phase 125 → the release ceremony no
      longer needs a rebase-reconcile OR a `fix(inbound): re-pin ==` dance); "CI-the-body" per-phase green
      gate meaning the ceremony was a confirmation; hand-count-not-gsd-sdk for the milestone scope
    - Key lesson (the structural delta this cycle): pin-loosening left TWO migration landmines the 124
      precedent never faced — D-05 (inbound docs_contract_test compared a self-`==`-pin; had to re-derive
      the expected core pin from the CORE RELEASE version and keep a Version.match floor-admits-core
      assertion so decoupling stays intentional) and D-11 (post-publish-smoke's `== ${VERSION}` grep
      silently skipped inbound smoke under `~>`; made it `~>`-aware). Planning surfaced both; per-phase CI
      would not have, because the allowlist/post-publish gates only run at release.
    - Key lesson: inbound's minor (1.6.0) was scored by RP from the banked feat(125-01) commit — no
      deliberate re-pin commit was needed; the floor intentionally stayed >= 1.10.2 (NOT bumped to 1.11.0)
      to preserve LD-2 decoupling; "verified against core 1.11" went in the CHANGELOG, not the floor
    - Release outcome: 1.11.0/1.11.0/1.6.0 live; inbound Hex dep shows `~> 1.10 and >= 1.10.2` (first cut
      proving the loosened sibling-pin decoupling end-to-end)

    Step 9.10 — Update STATE.md (docs(state): reflecting Phase 131 complete, v1.15 closed).

    Step 9.11 — Commit all milestone artifacts:
    `git add .planning/milestones/v1.15-ROADMAP.md .planning/milestones/v1.15-REQUIREMENTS.md .planning/milestones/v1.15-MILESTONE-AUDIT.md .planning/MILESTONES.md .planning/PROJECT.md .planning/ROADMAP.md .planning/RETROSPECTIVE.md .planning/STATE.md`
    Plus the REQUIREMENTS.md removal (`git rm .planning/REQUIREMENTS.md`) if taking that path.
    Commit: `docs(state): v1.15 complete — mailglass 1.11.0/1.11.0/1.6.0 on Hex`

    Step 9.12 — Tag v1.15 (SHIP-03):
    `git tag v1.15` then `git push origin v1.15`.
    NOTE: the git tag v1.15 is a MILESTONE marker, distinct from the RP-created Hex release tags
    (mailglass-v1.11.0, mailglass_admin-v1.11.0, mailglass_inbound-v1.6.0).

    Step 9.13 — Push the commit: `git push origin main` (fast-forward; NEVER --force).
  </action>

  <verify>
    <automated>ls .planning/milestones/v1.15-MILESTONE-AUDIT.md && grep -q 'status: passed' .planning/milestones/v1.15-MILESTONE-AUDIT.md && git tag -l v1.15 | grep -q v1.15</automated>
  </verify>

  <acceptance_criteria>
    `ls .planning/milestones/v1.15-*` returns at least three files: v1.15-ROADMAP.md, v1.15-REQUIREMENTS.md,
    v1.15-MILESTONE-AUDIT.md.

    `grep 'status: passed' .planning/milestones/v1.15-MILESTONE-AUDIT.md` returns a match. The audit file
    lists all 26 REQ-IDs (spot-check:
    `grep -E 'PIN-05|GATE-04|DET-02|MIXCI-05|CACHE-02|SUPPLY-05|SHIP-01|SHIP-02|SHIP-03' .planning/milestones/v1.15-MILESTONE-AUDIT.md`
    returns matches; and `grep -c 'passed' .planning/milestones/v1.15-MILESTONE-AUDIT.md` is at least 26)
    and states "7 phases" (not inflated by 999.x dirs).

    `grep 'v1.15' .planning/MILESTONES.md` returns a row containing "1.11.0", "7 phases", and "26 reqs".

    `git tag -l v1.15` returns "v1.15" AND `git ls-remote --tags origin v1.15` returns the tag ref on origin.
  </acceptance_criteria>

  <done>
    Milestone v1.15 audited (7 phases 125–131, 26/26 requirements status: passed, 1.11.0/1.11.0/1.6.0 on
    Hex with the inbound `~>` pin). Three archive files created under .planning/milestones/. MILESTONES.md,
    PROJECT.md, ROADMAP.md, RETROSPECTIVE.md, STATE.md updated; REQUIREMENTS.md removed; v1.15 tag on origin.
    v1.15 milestone is CLOSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Hex publish | Irreversible: once mailglass 1.11.0 is on Hex and 60 min pass, it cannot be unpublished |
| RP PR diff → maintainer review | Load-bearing PR (README core-pin sed sync inside); must be human-reviewed before auto-merge fires |
| publish-hex.yml → hex-publish environment | HEX_API_KEY scoped to the hex-publish GitHub Environment; never exposed to PR jobs |
| local main → origin/main push | The fast-forward push re-scores the RP PR; a force-push or partial push could publish an unintended version or break linear history |
| docs_contract_test (release branch) → RP PR CI | The D-05 fix must hold on the release branch (core 1.11 / README `~> 1.11` / floor `~> 1.10`); a wrong fix turns a required lane red on the PR and blocks the release |
| post-publish-smoke inbound-compat → SHIP-02 proof | The D-11 fix must set include_inbound=true under `~>`; if left `==`-only, inbound smoke is silently skipped and SHIP-02's proof is a false pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-01 | Tampering | .release-please-manifest.json | high | mitigate | Never hand-edit; let Release Please score; MAINTAINING.md Step 2 prohibits manual edits; Task 5 verifies the regenerated 1.11.0/1.11.0/1.6.0 values rather than authoring them |
| T-131-02 | Information Disclosure | HEX_API_KEY secret | high | mitigate | Key scoped to the hex-publish GitHub Environment only; never visible to PR jobs (CLAUDE.md Commit & Branch Conventions) |
| T-131-03 | Repudiation | Hex publish origin | medium | mitigate | publish-hex.yml gated on gate-ci-green; publish only from the release tag (never main); all publish runs logged in GitHub Actions |
| T-131-04 | Tampering | force-push corrupting origin history / publishing an unintended version | critical | mitigate | HARD never-`--force` rule (D-04); Task 4 acceptance gates on a fast-forward push (`origin/main == HEAD`, 0 behind); Task 5 re-verifies 1.11.0/1.11.0/1.6.0 AND that sibling pins stay `~>` before the Wave-2 human go/no-go authorizes the irreversible merge |
| T-131-05 | Tampering | D-05 docs_contract fix wrong on the release branch (README core 1.11 vs floor 1.10) | high | mitigate | Task 1 re-derives the expected core pin from the CORE RELEASE version (manifest `.["."]`), keeps a Version.match floor-admits-core assertion, and is reasoned green on BOTH main and the simulated release-branch semantics; Task 5 confirms required CI green on the actual PR branch before merge |
| T-131-06 | Denial of Service | D-11 inbound smoke silently skipped under the `~>` pin → SHIP-02 false pass | high | mitigate | Task 2 makes the inbound-compat check `~>`-aware; Task 8 Step 8.3 explicitly asserts include_inbound=true and refuses to accept a skipped-inbound smoke as a pass |
| T-131-07 | Denial of Service | inbound 1.6.0 absent from the RP PR → publish-admin (needs publish-inbound) stalls | medium | mitigate | Task 5 Step 5.3 is a BLOCKING gate that refuses to merge an inbound-less PR; the inbound minor is already banked from feat(125-01), so RP scores it automatically; Task 7 confirms publish-inbound before publish-admin |
| T-131-08 | Denial of Service | Anti-recursion stall (no publish fan-out after bot-merge) | low | accept | Self-healed by hourly cron + gate-ci-green auto-dispatch; manual recovery via `gh workflow run release-please.yml`; acceptable latency ≤ 60 min |
| T-131-09 | Elevation of Privilege | Dispatch publish-hex.yml from main | medium | mitigate | Workflow docs + MAINTAINING.md state "never dispatch from main"; always use `--ref mailglass-v1.11.0 --field tag=mailglass-v1.11.0` |
| T-131-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no new packages; the ceremony uses the existing mix toolchain only (the two source edits are a test file and a workflow YAML — no new application attack surface) |
</threat_model>

<verification>
Full phase gate (all must pass before Phase 131 is marked Complete):

- Wave 0: inbound `docs_contract_test.exs` GREEN (`0 failures`) with the expected core pin derived from the
  CORE RELEASE version + a Version.match floor-admits-core assertion (D-05); post-publish-smoke inbound-compat
  is `~>`-aware (D-11); `mix mailglass.publish.check --package {mailglass,mailglass_admin,mailglass_inbound}`
  each exit 0 (any legitimate allowlist drift committed as chore:, D-06). D-05 and D-11 are SEPARATE commits.
- Wave 1: `git rev-parse origin/main == HEAD` via fast-forward (NO force, D-04); ci.yml green on the new HEAD;
  regenerated RP PR shows 1.11.0/1.11.0/1.6.0 with sibling pins STAYING `~> 1.10 and >= 1.10.2` (NO re-pin,
  NO floor bump — D-02/D-03), README/install core pins seded to `~> 1.11`, CHANGELOG `## [1.11.0]` /
  `## [1.6.0]`, required `CI Green` gate green, auto-merge armed (D-01). Release-As: 1.11.0 fallback only if
  RP scored a patch.
- Wave 2: maintainer go/no-go approved; publish fan-out visible in Actions (or stall recovery applied) (D-08).
- Wave 3: `mix hex.info mailglass 1.11.0` / `mailglass_admin 1.11.0` / `mailglass_inbound 1.6.0` each contain
  "Released:"; inbound shows the loosened `mailglass ~> 1.10 and >= 1.10.2` (NOT `== 1.11.0`) (SHIP-01, D-02).
- Wave 4: `consumer_install_smoke.sh` exits 0 with DEP_MODE=hex VERSION=1.11.0 VERSION_INBOUND=1.6.0
  (inbound `~>` resolves against core 1.11.0 — the keystone proof); post-publish-smoke passes with the
  inbound smoke actually run (include_inbound=true, D-11) — or only #32 hackney noise red (SHIP-02, D-09).
- Wave 5: `ls .planning/milestones/v1.15-*` returns 3 files; MILESTONE-AUDIT.md `status: passed` with all 26
  REQ-IDs and 7-phase scope; `git tag -l v1.15` + `git ls-remote --tags origin v1.15` confirm the tag
  (SHIP-03, D-10).
</verification>

<success_criteria>
1. mailglass 1.11.0 is live on Hex: `mix hex.info mailglass 1.11.0` contains "Released:" (SHIP-01).
2. mailglass_admin 1.11.0 is live on Hex: `mix hex.info mailglass_admin 1.11.0` contains "Released:" (SHIP-01).
3. mailglass_inbound 1.6.0 is live with the LOOSENED pin: `mix hex.info mailglass_inbound 1.6.0 | grep mailglass`
   returns `mailglass ~> 1.10 and >= 1.10.2` (NOT `== 1.11.0`) — the keystone `~>` decoupling (SHIP-01, D-02).
4. mix deps.get resolves all three from Hex; inbound 1.6.0's `~>` pin resolves against core 1.11.0 (consumer
   smoke passes — SHIP-02, the keystone proof).
5. Post-publish smoke passes with the inbound smoke ACTUALLY RUN (include_inbound=true, D-11), or only the
   known #32 hackney false-positive is red (SHIP-02, D-09).
6. origin/main carries the v1.15 body via a clean fast-forward push (no force-push); no `fix(inbound): re-pin`
   commit was added; the inbound floor was NOT bumped to >= 1.11.0 (D-02/D-03/D-04).
7. The inbound docs_contract_test passes on BOTH main semantics and the RP release-branch semantics (D-05);
   post-publish-smoke's inbound-compat grep is `~>`-aware (D-11).
8. v1.15 milestone is archived: `.planning/milestones/v1.15-MILESTONE-AUDIT.md` exists with all 26
   requirements `status: passed`, scope corrected to 7 phases (125–131) / 26 reqs (not inflated by 999.x
   dirs) (SHIP-03, D-10).
9. git tag v1.15 exists on origin (SHIP-03).
</success_criteria>

<output>
Create `.planning/phases/131-release-cut-milestone-closeout/131-01-SUMMARY.md` when done.

Summary must include:
- Hex versions confirmed live (with `mix hex.info` output excerpts): 1.11.0/1.11.0/1.6.0
- Inbound pin verification: the LOOSENED `mailglass ~> 1.10 and >= 1.10.2` (NOT `== 1.11.0`) — the keystone proof
- The D-05 docs_contract fix commit SHA + confirmation it is green on main AND the release-branch semantics
- The D-11 post-publish-smoke `~>`-aware fix commit SHA + confirmation inbound smoke actually ran (include_inbound=true)
- Origin push: clean fast-forward evidence (no force-push, no rebase-reconcile needed), new HEAD SHA, green ci.yml run on it
- Confirmation NO `fix(inbound): re-pin` commit was added and the inbound floor stayed >= 1.10.2 (D-02/D-03)
- Consumer smoke exit code + GET /dev/mail/ result + the SHIP-02 Hex-resolution evidence (inbound `~>` resolves against core 1.11.0)
- post-publish-smoke run URL + outcome (inbound smoke run, not skipped) + any #32 noise classification
- publish allowlist: whether a chore: snapshot update was needed this cycle (D-06)
- Milestone audit: scope counts (7 phases, 26 reqs), all 26 REQ-IDs confirmed status: passed
- Git tag v1.15: `git tag -l v1.15` and push confirmation
- Any stall recovery or Release-As: fallback steps taken
</output>
