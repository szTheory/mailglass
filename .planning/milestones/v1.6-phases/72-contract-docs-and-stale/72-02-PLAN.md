---
phase: 72-contract-docs-and-stale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - PROOF-02

must_haves:
  truths:
    - "inbound docs-contract test asserts the corrected DX inventory horizon wording (1.x, not 0.x) in the compatibility guide"
    - "inbound docs-contract test refutes the stale 0.x horizon wording after the compatibility guide is corrected by Plan 01"
    - "all existing inbound docs_contract_test assertions that pin compatibility guide tokens continue to pass"
  artifacts:
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "DX inventory horizon guards added to adoption path test"
      contains: "Through `mailglass_inbound` `1.x`"
  key_links:
    - from: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      to: "guides/compatibility-and-deprecations.md"
      via: "File.read! via @compatibility_path"
      pattern: "assert compatibility =~ \"Through.*mailglass_inbound.*1\\.x\""
---

<objective>
Add two targeted assertions to the "adoption path and compatibility routing stay
canonical" test in mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
that guard the corrected DX inventory support-until horizon wording (Plan 01
changes "0.x" to "1.x" in three cells). Without these guards the inbound
package-local test suite would silently accept a future regression back to the
stale 0.x wording.

Purpose: PROOF-02 requires executable guards pinning the highest-risk stale
claims. The DX inventory horizon cells in guides/compatibility-and-deprecations.md
are read by the inbound docs-contract test, which is the appropriate seam (per
D-05: extend existing proof seams). Plan 01 fixes the prose; this plan closes
the assertion gap that PATTERNS.md identified as a "coverage gap."

Output:
- mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs with two new
  assertions added to the "adoption path and compatibility routing stay canonical"
  test block: refute for the stale 0.x horizon and assert for the corrected 1.x
  horizon.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-PATTERNS.md

<interfaces>
<!-- Key patterns from mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs -->

Path module attributes (lines 4-14) — use @compatibility_path, do not re-read the path inline:
  @readme_path Path.expand("../../README.md", __DIR__)
  @stability_path Path.expand("../../docs/api_stability.md", __DIR__)
  @compatibility_path Path.expand("../../../guides/compatibility-and-deprecations.md", __DIR__)

Target test block (lines 475-524) — add two assertions AFTER the existing for-loop at lines 508-512:
  test "adoption path and compatibility routing stay canonical across README, install, and guide" do
    ...
    assert compatibility =~ "mailglass_inbound/docs/api_stability.md"
    assert compatibility =~ "stable/internal/deferred source"
    assert compatibility =~ "Reachability is not a compatibility promise."
    assert compatibility =~ "## mailglass_inbound compatibility"
    assert compatibility =~ "## Inbound deprecation-DX inventory"

    for required_heading <- ["| Surface | Bridge or replacement | ..."] do
      assert compatibility =~ required_heading
    end
    # ← ADD HERE after the for-loop, before end:
    #   refute compatibility =~ "Through `mailglass_inbound` `0.x`"
    #   assert compatibility =~ "Through `mailglass_inbound` `1.x`"

    for forbidden <- [...] do
      refute readme =~ forbidden
      refute install =~ forbidden
    end
  end

String assertion convention (assert/refute =~, exact token, no regex for these guards):
  assert compatibility =~ "exact token"
  refute compatibility =~ "stale phrase"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DX inventory horizon guards to inbound adoption path test</name>
  <files>mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <read_first>
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs lines 475-524 (full "adoption path and compatibility routing stay canonical" test block — find the precise insertion point after the required_heading for-loop and before the forbidden for-loop)
    - .planning/phases/72-contract-docs-and-stale/72-PATTERNS.md "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs" section (exact coverage gap assertion text)
  </read_first>
  <behavior>
    - refute compatibility =~ "Through `mailglass_inbound` `0.x`" must be present in this test
    - assert compatibility =~ "Through `mailglass_inbound` `1.x`" must be present in this test
    - All existing assertions in this test block must be preserved unchanged
    - No other test blocks are modified
    - cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors passes only AFTER Plan 01 has corrected the compatibility guide prose (the refute will fail on current stale wording; after Plan 01 edits the guide both assertions pass)
  </behavior>
  <action>
    In mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs, locate the "adoption path and compatibility routing stay canonical across README, install, and guide" test block. Find the for-loop that asserts required_heading tokens (the for-loop ends before the for-loop that checks forbidden tokens in readme and install). Add these two assertions immediately after the required_heading for-loop closes:

    refute compatibility =~ "Through `mailglass_inbound` `0.x`"
    assert compatibility =~ "Through `mailglass_inbound` `1.x`"

    Insert them with consistent indentation (four spaces, matching the surrounding assertions). Do not alter any other line in the test block.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -n "Through.*mailglass_inbound.*0\.x\|Through.*mailglass_inbound.*1\.x" mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</automated>
  </verify>
  <done>
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs contains both refute compatibility =~ "Through `mailglass_inbound` `0.x`" and assert compatibility =~ "Through `mailglass_inbound` `1.x`" inside the adoption path test
    - The two assertions appear after the required_heading for-loop in the same test block
    - No other assertions in the test file were altered
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file reads | docs_contract_test.exs reads compatibility guide at test time; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No external packages; single test file assertion addition |
| T-72-02-01 | Tampering | docs_contract_test.exs | accept | Test-only change; reads local files; no PII or network I/O |
</threat_model>

<verification>
After task completes:

1. grep "Through.*mailglass_inbound.*0\.x" mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs returns one match (the refute assertion)
2. grep "Through.*mailglass_inbound.*1\.x" mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs returns one match (the assert assertion)
3. Both new lines appear inside the "adoption path and compatibility routing stay canonical" test block
4. After Plan 01 corrects the compatibility guide: cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors exits 0
</verification>

<success_criteria>
- mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs contains refute compatibility =~ "Through `mailglass_inbound` `0.x`" inside the adoption path test
- mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs contains assert compatibility =~ "Through `mailglass_inbound` `1.x`" inside the adoption path test
- No other lines in the test file were altered
- After both Plan 01 and Plan 02 complete: cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors exits 0
</success_criteria>

<output>
Create `.planning/phases/72-contract-docs-and-stale/72-02-SUMMARY.md` when done.
</output>
