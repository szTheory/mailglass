---
phase: 73-inbound-1-0-publish-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md
  - .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-CHECKLIST.md
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "An inbound-scoped RELEASE-RECORD exists in the Phase 73 dir carrying the full REL-03 field set (D-03/D-04)"
    - "Post-publish-only fields (Hex index URL, HexDocs URL, install/smoke proof, 60-minute outcome) read as explicit pending/not run, never as captured (D-05, Honest Surface Area)"
    - "The obsolete GitHub-Environment approver fields are absent from the inbound record (D-04, hands-free publish)"
    - "mix mailglass.publish.check --package mailglass_inbound exits 0 and its committed summary path is cited as the proof bundle"
    - "The root stability_contract inbound-preflight-consistency test passes, proving inbound source/publish truth agrees"
    - "Tag reads as staged-not-cut: 'mailglass_inbound-v1.0.0 (staged, not cut)' — no live publish-triggering tag created (D-01/D-02)"
  artifacts:
    - path: ".planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md"
      provides: "Inbound REL-03 release evidence record with pending markers"
      contains: "Tag: mailglass_inbound-v1.0.0 (staged, not cut)"
    - path: ".planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-CHECKLIST.md"
      provides: "Repo-proved-before-publish vs manual/external-pending gate separation for the inbound-only slice"
      contains: "Repo-proved before publish"
  key_links:
    - from: "73-01-RELEASE-RECORD.md"
      to: ".planning/publish/mailglass_inbound-publish-summary.json"
      via: "Proof bundle path citation"
      pattern: "mailglass_inbound-publish-summary"
    - from: "73-01-RELEASE-CHECKLIST.md"
      to: "mix mailglass.publish.check --package mailglass_inbound"
      via: "repo-proved bucket gate"
      pattern: "mailglass.publish.check --package mailglass_inbound"
---

<objective>
Author the inbound-scoped release-evidence pair — `73-01-RELEASE-RECORD.md` and
`73-01-RELEASE-CHECKLIST.md` — in the Phase 73 directory, cloning the Phase 38 form
shape but scoped to the single package `mailglass_inbound` at tag
`mailglass_inbound-v1.0.0`. Carry the full REL-03 field set, drop the obsolete
hands-free-publish approver fields, and mark every post-publish-only field as
explicit `pending` / `not run` under the prepare-and-stage posture.

Run the two deterministic repo-native proof lanes (`mix mailglass.publish.check
--package mailglass_inbound` and the root inbound-preflight-consistency test) and
record their results as the captured (non-pending) evidence in the record.

Purpose: REL-03 — the maintainer can record inbound release evidence honestly,
with captured fields captured and post-publish fields visibly pending, before any
irreversible Hex publish (D-01/D-03/D-04/D-05).
Output: two new planning artifacts in the Phase 73 directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-inbound-1-0-publish-evidence/73-CONTEXT.md
@.planning/phases/73-inbound-1-0-publish-evidence/73-RESEARCH.md
@.planning/phases/73-inbound-1-0-publish-evidence/73-PATTERNS.md

# Shape source — clone, do NOT edit in place (D-03)
@.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-RECORD.md
@.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-CHECKLIST.md

# Committed inbound publish truth (cited as proof, not edited)
@.planning/publish/mailglass_inbound-publish-summary.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the inbound RELEASE-RECORD with pending markers</name>
  <files>.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-RECORD.md (analog field set + "not run" convention, lines 3-29)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-PATTERNS.md (top-block field set to clone, lines 24-49)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-CONTEXT.md (D-03/D-04/D-05)
    - .planning/publish/mailglass_inbound-publish-summary.json (the committed summary to cite as the proof bundle)
  </read_first>
  <action>
    Create `73-01-RELEASE-RECORD.md` cloning the Phase 38 RELEASE-RECORD's flat `Key: value`
    top block (NOT a table), scoped to the single package `mailglass_inbound`.
    Title: `# Phase 73 — Inbound Release Record`.
    Top-block fields (exact headers, exact values):
    `Release type: prepare-and-stage`;
    `Tag: mailglass_inbound-v1.0.0 (staged, not cut)`;
    `Release-vs-dispatch path: workflow_dispatch fallback rehearsal (package=mailglass_inbound), tag-pinned — canonical path is release: published, deferred to maintainer`;
    `Publish workflow run URL: pending; see 73-02 rehearsal evidence`;
    `Post-publish smoke run URL: not run`;
    `Proof bundle path: .planning/publish/mailglass_inbound-publish-summary.json (mix mailglass.publish.check --package mailglass_inbound)`;
    `Install/upgrade rehearsal path: pending; deferred to post-publish maintainer trigger`;
    `Hex index confirmation: not run; rehearsal stayed repo-local`;
    `HexDocs URLs: pending; not published under prepare posture`;
    `Fallback path used: not run`;
    `60-minute outcome: not run; no live publish window started`.
    Then a `## Proof links` bullet section citing `73-01-RELEASE-CHECKLIST.md`,
    `.planning/publish/mailglass_inbound-publish-summary.json`, and
    `MAINTAINING.md` (inbound-only publish/fallback runbook, refined in 73-02).
    Then a `## Notes` paragraph stating the prepare-and-stage posture honestly: this record
    captures repo-local staging only; live publish, Hex index, HexDocs verification, install/smoke
    proof, and the 60-minute revert window remain explicit `pending`/`not run` until the maintainer's
    deferred `mailglass_inbound 1.0.0` publish trigger runs (D-01).
    DROP the Phase 38 `## Manual approvals and external checks` block entirely — no
    `GitHub Environment approver`, no `Approval timestamp` (publish is hands-free; the
    `hex-publish` environment has no required reviewers, per D-04). Do NOT add a Branch-protection
    field; if you reference it at all, state it does not apply to the inbound-only slice.
    Do NOT edit the archived Phase 38 forms.
  </action>
  <verify>
    <automated>test -f .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md && grep -q "mailglass_inbound-v1.0.0 (staged, not cut)" .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md && ! grep -q "GitHub Environment approver" .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md</automated>
  </verify>
  <acceptance_criteria>
    - `73-01-RELEASE-RECORD.md` exists and contains the literal `Tag: mailglass_inbound-v1.0.0 (staged, not cut)`.
    - `grep -c "GitHub Environment approver" 73-01-RELEASE-RECORD.md` returns 0 (approver fields dropped, D-04).
    - Each of these headers is present: `Hex index confirmation`, `HexDocs URLs`, `Install/upgrade rehearsal path`, `60-minute outcome`, `Fallback path used`, `Publish workflow run URL`, `Release-vs-dispatch path`.
    - Each post-publish-only field line reads `pending` or `not run` (Honest Surface Area, D-05) — verified by `grep -E "Hex index confirmation: not run|HexDocs URLs: pending|60-minute outcome: not run"`.
  </acceptance_criteria>
  <done>Inbound RELEASE-RECORD authored with the full REL-03 field set, approver fields dropped, post-publish fields visibly pending, tag staged-not-cut.</done>
</task>

<task type="auto">
  <name>Task 2: Author the inbound RELEASE-CHECKLIST and run the deterministic proof lanes</name>
  <files>.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-CHECKLIST.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-CHECKLIST.md (two-bucket structure, lines 3, 23-50)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-PATTERNS.md (two-bucket clone + bucket swap, lines 53-72)
    - lib/mix/tasks/mailglass.publish.check.ex (the deterministic preflight lane; @shortdoc line 4)
    - test/mailglass/stability_contract_test.exs (the inbound-preflight-consistency test at line 116)
  </read_first>
  <action>
    First run the two deterministic lanes from the repo root and note their outcomes for the record:
    `mix mailglass.publish.check --package mailglass_inbound` (must exit 0; refreshes/confirms
    `.planning/publish/mailglass_inbound-publish-summary.json`) and
    `mix test test/mailglass/stability_contract_test.exs` (the
    "inbound release preflight truth is internally consistent across source and publish evidence"
    test at line 116 must pass).
    Then create `73-01-RELEASE-CHECKLIST.md` cloning the Phase 38 two-bucket structure.
    Title: `# Phase 73 — Inbound Release Checklist`.
    Section `## Repo-proved before publish` with numbered gates, each carrying an indented
    `Proof fields:` block, swapped to the inbound-native lanes:
    gate 1 `mix mailglass.publish.check --package mailglass_inbound` exits 0 (proof field: exit status + summary path);
    gate 2 `mix verify.stability_contract` green (proof field: result);
    gate 3 `.planning/publish/mailglass_inbound-publish-summary.json` reviewed (proof fields: `source_ref`, `source_ref_pattern`, pin `== 1.3.0`).
    Section `## Manual/external proof` keeping `Fallback dispatch`, `Live package and docs verification`
    (Hex/HexDocs URLs), and the `60-minute smoke and release decision window`, every field marked
    `pending` / `not run`.
    DROP the Phase 38 `GitHub Environment approval for hex-publish` gate including
    `GitHub Environment approver` / `Approval timestamp` proof fields (D-04).
    Closing line: `Use 73-01-RELEASE-RECORD.md to store the filled values above.`
  </action>
  <verify>
    <automated>mix mailglass.publish.check --package mailglass_inbound && mix test test/mailglass/stability_contract_test.exs && test -f .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-CHECKLIST.md && grep -q "Repo-proved before publish" .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix mailglass.publish.check --package mailglass_inbound` exits 0.
    - `mix test test/mailglass/stability_contract_test.exs` passes (inbound-preflight-consistency test green).
    - `73-01-RELEASE-CHECKLIST.md` contains both bucket headers `Repo-proved before publish` and `Manual/external proof`.
    - `grep -c "GitHub Environment approval for hex-publish" 73-01-RELEASE-CHECKLIST.md` returns 0 (approver gate dropped, D-04).
    - The repo-proved bucket cites `mix mailglass.publish.check --package mailglass_inbound` and `mix verify.stability_contract` verbatim.
  </acceptance_criteria>
  <done>Inbound RELEASE-CHECKLIST authored with the two-bucket separation, approver gate dropped, repo-proved bucket pointing at inbound-native lanes that pass; manual/external bucket all pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → release-governance artifacts | Authoring evidence that future maintainers trust as a release contract |
| evidence record → actual Hex/HexDocs state | Captured-vs-pending integrity; the record must not over-claim live state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Spoofing | post-publish fields in 73-01-RELEASE-RECORD.md | mitigate | Post-publish-only fields (Hex index, HexDocs, smoke, 60-minute) MUST read `pending`/`not run`; acceptance criteria grep-assert pending markers (Honest Surface Area, D-05). A captured-looking pending value is block-worthy. |
| T-73-02 | Tampering | accidental real tag creation | mitigate | Tag is recorded as `mailglass_inbound-v1.0.0 (staged, not cut)`; this plan creates NO git tag and runs NO `mix hex.publish`. Only `publish.check` (read-only preflight) and an ExUnit test run (D-01/D-02). |
| T-73-03 | Repudiation | dropped approver fields | accept | Approver/approval-timestamp fields are intentionally absent because publish is hands-free with no required reviewers (D-04). Reintroducing a non-existent approval claim would be the actual integrity risk. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable"); nothing added to any registry. |
</threat_model>

<verification>
- `mix mailglass.publish.check --package mailglass_inbound` exits 0.
- `mix test test/mailglass/stability_contract_test.exs` passes.
- `grep -E "phases/38" .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md` returns nothing (record points only at archived/current paths, no stale Phase 38 path).
- Both new files exist; neither archived Phase 38 form was modified (`git status` shows the v1.0-phases/38 dir untouched).
</verification>

<success_criteria>
- REL-03 evidence artifacts authored: inbound RELEASE-RECORD + RELEASE-CHECKLIST in the Phase 73 dir.
- Full REL-03 field set present; approver fields dropped; post-publish fields visibly pending (SC-4, D-04/D-05).
- Deterministic proof lanes (`publish.check`, inbound-preflight-consistency test) pass and are cited (SC-1).
- No git tag cut, no publish run, no reference-pin flip (D-01/D-09).
</success_criteria>

<output>
Create `.planning/phases/73-inbound-1-0-publish-evidence/73-01-SUMMARY.md` when done.
</output>
