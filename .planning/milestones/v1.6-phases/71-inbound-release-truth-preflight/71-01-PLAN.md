---
phase: 71-inbound-release-truth-preflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/stability_contract_test.exs
  - README.md
  - MAINTAINING.md
  - test/mailglass/docs_contract_test.exs
  - .planning/publish/mailglass_inbound-publish-summary.json
  - .planning/phases/71-inbound-release-truth-preflight/71-VERIFICATION.md
autonomous: true
requirements:
  - REL-01
  - PROOF-01
must_haves:
  truths:
    - Maintainer can prove `mailglass_inbound` `1.0.0` source/package truth across manifest, source version, changelog, README/install pins, publish pin, allowlist evidence, and publish-summary output.
    - `mix mailglass.publish.check --package mailglass_inbound` remains the canonical preflight lane; no second tarball/package verifier is introduced.
    - Required release proof is clearly separated from provider-live and ecosystem advisory checks.
    - Known stale claims are explicitly classified as Phase 71 blockers or deferred Phase 72/73 work.
  artifacts:
    - path: test/mailglass/stability_contract_test.exs
      provides: Exact Phase 71 release-truth assertions for inbound source/package parity.
    - path: test/mailglass/docs_contract_test.exs
      provides: Root docs/runbook guards for inbound release truth and required-vs-advisory proof language.
    - path: .planning/phases/71-inbound-release-truth-preflight/71-VERIFICATION.md
      provides: Auditable blocker/deferred classification and command evidence for the preflight.
    - path: .planning/publish/mailglass_inbound-publish-summary.json
      provides: Refreshed canonical publish-summary evidence produced by the existing publish-check lane.
  key_links:
    - from: test/mailglass/stability_contract_test.exs
      to: .planning/publish/mailglass_inbound-publish-summary.json
      via: Exact assertions on version, manifest_version, linked_versions, and mailglass_inbound_publish_pin.
      pattern: mailglass_inbound_publish_pin|manifest_version|linked_versions|version
    - from: test/mailglass/docs_contract_test.exs
      to: README.md
      via: Root README wording assertions for inbound independent `1.0` truth.
      pattern: mailglass_inbound|1.0|v0.5
    - from: test/mailglass/docs_contract_test.exs
      to: MAINTAINING.md
      via: Required-vs-advisory proof and inbound-only fallback wording assertions.
      pattern: Provider Live Advisory|mailglass_inbound-v1.0.0|~> 0.3
---

<objective>
Reconcile current `mailglass_inbound` `1.0.0` source/package truth and the
required-vs-advisory proof boundary before the inbound release ceremony.

Purpose: satisfy REL-01 and PROOF-01 without expanding into the publish
ceremony, broad stale-claim guard work, or any forced core/admin release.
Output: exact root release-truth assertions, blocker-only root docs/runbook
fixes, refreshed inbound publish-summary evidence, and one phase-local
verification artifact that classifies remaining stale claims.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-inbound-release-truth-preflight/71-CONTEXT.md
@.planning/phases/71-inbound-release-truth-preflight/71-RESEARCH.md
@.planning/phases/71-inbound-release-truth-preflight/71-PATTERNS.md
@.planning/milestones/v1.4-phases/66-release-position-decision/66-CONTEXT.md
@.planning/milestones/v1.4-phases/66-release-position-decision/66-01-SUMMARY.md
@test/mailglass/stability_contract_test.exs
@test/mailglass/docs_contract_test.exs
@lib/mix/tasks/mailglass.publish.check.ex
@mailglass_inbound/mix.exs
@mailglass_inbound/CHANGELOG.md
@README.md
@MAINTAINING.md
@.planning/publish/mailglass_inbound-publish-summary.json
@.release-please-manifest.json
@release-please-config.json
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml

<interfaces>
From `lib/mix/tasks/mailglass.publish.check.ex`:
```elixir
defp packages("mailglass_inbound"), do: [:mailglass_inbound]
```

The existing preflight already owns metadata parity, linked-version constraint,
allowlist comparison, isolated compile, and publish-summary writing. Do not
reimplement tarball/package logic in a new checker.

From `mailglass_inbound/mix.exs`:
```elixir
@version "1.0.0"
{:mailglass, "== 1.3.0"}
source_ref: "v" <> @version
```

From `.planning/publish/mailglass_inbound-publish-summary.json`:
```json
{
  "package": "mailglass_inbound",
  "manifest_version": "1.0.0",
  "mailglass_inbound_publish_pin": "== 1.3.0",
  "source_ref": "v1.0.0",
  "version": "1.0.0"
}
```

Known stale-claim hotspots already identified by research:
- `README.md`: inbound still described as `v0.5+`.
- `MAINTAINING.md`: smoke example still uses `{:mailglass_inbound, "~> 0.3"}`.
- `MAINTAINING.md`: fallback example still uses core-only tag wording.
- `reference/host_app/mix.exs`, `reference/demo_app/mix.exs`,
  `guides/compatibility-and-deprecations.md`: classify now, do not broaden this
  phase into pre-publish Hex pin churn or broad docs cleanup unless a direct
  REL-01/PROOF-01 contradiction is discovered.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten exact inbound release-truth proof on the existing preflight seam</name>
  <files>test/mailglass/stability_contract_test.exs, .planning/publish/mailglass_inbound-publish-summary.json</files>
  <action>Update the root release-truth assertions per D-01, D-02, D-03, and D-04 so Phase 71 proves exact current inbound truth instead of only SemVer-shaped truth. Assert that `.release-please-manifest.json`, `mailglass_inbound/mix.exs`, `mailglass_inbound/CHANGELOG.md`, inbound install pins, `MIX_PUBLISH=true` publish pin, and `.planning/publish/mailglass_inbound-publish-summary.json` agree on the current `mailglass_inbound` `1.0.0` line and `mailglass == 1.3.0` publish dependency. Refresh the summary only by running `mix mailglass.publish.check --package mailglass_inbound`; do not add a second verifier or duplicate tarball/allowlist/package logic outside `Mix.Tasks.Mailglass.Publish.Check`.</action>
  <verify>
    <automated>mix mailglass.publish.check --package mailglass_inbound && mix test test/mailglass/stability_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>The root release-truth test fails closed on any mismatch between manifest, source, changelog, install-pin, publish-pin, or publish-summary truth, and the refreshed inbound publish-summary snapshot is still produced by the repo-native publish-check lane.</acceptance_criteria>
  <done>The existing root proof lane asserts exact Phase 71 inbound truth without introducing a new package verifier.</done>
</task>

<task type="auto">
  <name>Task 2: Correct only Phase 71-blocking root docs and runbook release claims</name>
  <files>README.md, MAINTAINING.md, test/mailglass/docs_contract_test.exs</files>
  <action>Per D-05, D-06, D-07, D-08, D-09, and D-10, fix only the stale root-level claims that directly block REL-01 or PROOF-01. Update `README.md` so inbound is described with its own stable `1.0` truth instead of `v0.5+`, and update `MAINTAINING.md` so the smoke-install example no longer tells maintainers to use `{:mailglass_inbound, "~> 0.3"}` and the fallback publish example no longer implies a core-only `mailglass-v1.0.0` tag for inbound-only recovery. Back those edits with focused root docs-contract assertions that required release proof is deterministic repo/package/workflow evidence while provider-live checks remain advisory and non-blocking. Do not broaden this task into compatibility-guide rewrites, reference/demo app published-Hex pin changes, or general stale-claim guard expansion; those are classified for Phase 72 or Phase 73 unless execution finds a direct release blocker.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Root docs and the maintainer runbook no longer contradict inbound `1.0.0` truth or the required-vs-advisory proof boundary, and the root docs contract fails closed on these exact blocker claims without widening Phase 71 into broad docs cleanup.</acceptance_criteria>
  <done>Direct Phase 71 stale claims are corrected and guarded by the existing root docs-contract seam.</done>
</task>

<task type="auto">
  <name>Task 3: Record blocker versus deferred stale-claim disposition for the release preflight</name>
  <files>.planning/phases/71-inbound-release-truth-preflight/71-VERIFICATION.md</files>
  <action>Create a phase-local verification artifact that records the command evidence and classifies known drift per D-07 and D-08. Mark the root `README.md` and `MAINTAINING.md` contradictions as Phase 71 blockers resolved here. Mark `reference/host_app/mix.exs`, `reference/demo_app/mix.exs`, and `guides/compatibility-and-deprecations.md` as deferred with explicit rationale: pre-publish published-Hex examples still resolve `0.3.0`, Phase 72 owns broad docs/guard expansion, and Phase 73 owns post-publish smoke/install pin truth. If execution discovers any new contradiction through `mix mailglass.publish.check --package mailglass_inbound`, classify it explicitly as a blocker instead of silently widening scope.</action>
  <verify>
    <automated>rg -n "Phase 71 blocker|Phase 72|Phase 73|mailglass.publish.check --package mailglass_inbound" .planning/phases/71-inbound-release-truth-preflight/71-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>There is one auditable artifact that tells downstream release work exactly what Phase 71 fixed, what remains deferred, and why those deferrals do not violate REL-01 or PROOF-01.</acceptance_criteria>
  <done>Phase 71 ends with an explicit blocker/deferred map instead of leaving stale-claim scope to ad hoc judgment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer edits -> release truth tests/docs | Human-written release claims can drift from source/package truth. |
| repo source -> publish-summary evidence | The publish-summary snapshot is derived from source/package state and must not become an unverified independent truth source. |
| release runbook -> manual publish fallback | Incorrect tag/package instructions can send maintainers down the wrong recovery path during the release ceremony. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | `test/mailglass/stability_contract_test.exs`, `.planning/publish/mailglass_inbound-publish-summary.json` | mitigate | Add exact parity assertions for manifest/source/changelog/install pin/publish pin/summary truth and require `mix mailglass.publish.check --package mailglass_inbound` to regenerate summary evidence. Treat any mismatch as a release blocker. |
| T-71-02 | Repudiation | `MAINTAINING.md`, `test/mailglass/docs_contract_test.exs` | mitigate | Pin required-vs-advisory wording so provider-live and ecosystem checks cannot be misrepresented as deterministic release blockers, and so inbound-only fallback instructions point at the correct package/tag shape. |
| T-71-03 | Denial of service | `reference/host_app/mix.exs`, `reference/demo_app/mix.exs` | accept | Do not pre-publish bump published-Hex examples to inbound `1.0`; record them as deferred until Phase 73 can validate post-publish smoke/install behavior against live Hex truth. |
| T-71-SC | Tampering | package-manager installs | accept | No npm/pip/cargo installs are in scope for this phase. |
</threat_model>

<verification>
- `mix mailglass.publish.check --package mailglass_inbound`
- `mix test test/mailglass/stability_contract_test.exs --warnings-as-errors`
- `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors`
- Only if execution touches inbound package-local docs because a direct blocker is discovered: `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- `mix mailglass.publish.check --package mailglass_inbound` remains the single canonical preflight and passes with inbound summary truth matching current source truth.
- Root release-truth assertions fail closed on exact inbound `1.0.0` / `mailglass == 1.3.0` drift rather than only asserting SemVer-shaped values.
- Root README and MAINTAINING no longer claim inbound `v0.5+`, `~> 0.3`, or a core-only fallback path for inbound-only release recovery.
- The phase leaves one explicit verification artifact that separates Phase 71 blockers from Phase 72/73 deferred stale claims.
</success_criteria>

<output>
Create `.planning/phases/71-inbound-release-truth-preflight/71-01-SUMMARY.md` when done
</output>
