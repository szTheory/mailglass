---
phase: 89-collateral-specimens-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook-fable/examples/landing-page.html
  - brandbook-fable/examples/email-template.html
  - brandbook-fable/examples/readme-header.svg
  - brandbook-fable/examples/docs-page.svg
  - brandbook-fable/examples/og-card.svg
  - brandbook-fable/examples/diagram-language.svg
  - brandbook-fable/copy/copy-blocks.md
  - brandbook-fable/copy/microcopy.md
  - brandbook-fable/index.html
  - .planning/phases/89-collateral-specimens-copy/89-visual-audit.md
autonomous: true
requirements: [COLL-01, COLL-02, COLL-03, COPY-01, COPY-02]
tags: [brandbook, collateral, copy, svg, email-html, specimens]

must_haves:
  truths:
    - "examples/landing-page.html opens from file:// as a full self-contained landing blueprint (hero, install snippet, feature grid, code block, comparison row, footer) deployable with a find-and-replace"
    - "examples/email-template.html is an email-client-safe branded transactional specimen: table-based layout, inline styles, both color-scheme metas, zero SVG, zero tracking"
    - "SVG specimens cover README header (legible on both GitHub themes), docs-page framing, a 1200x630 social-card template with documented local PNG export policy, and a worked diagram-language spec using the exact domain nouns Mailable -> Message -> Delivery -> Event"
    - "copy/copy-blocks.md ships paste-ready copy for GitHub About (<=350 chars), Hex.pm description, HexDocs intro, landing hero + feature blurbs, CTAs, launch post, and a release-note voice template — no placeholders, no lorem, every fact verified against the repo"
    - "copy/microcopy.md ships UX strings for all seven domain nouns across error/empty/success/warning states (>=28 entries), consistent with the Anymail event taxonomy and the dispatch-vs-delivered distinction"
    - "index.html section 08 renders the shipped specimens and still passes its Phase 88 gates (size, zero external URLs, 9 headings, denylist) after the slot-in"
  artifacts:
    - path: "brandbook-fable/examples/landing-page.html"
      provides: "Self-contained landing blueprint (DIF-07, COLL-01)"
      min_lines: 200
    - path: "brandbook-fable/examples/email-template.html"
      provides: "Client-safe transactional email specimen (DIF-08, COLL-02)"
      min_lines: 100
    - path: "brandbook-fable/examples/readme-header.svg"
      provides: "GitHub README header, outlined paths, dual-theme-safe fills (COLL-03)"
    - path: "brandbook-fable/examples/docs-page.svg"
      provides: "HexDocs-style page framing specimen (COLL-03)"
    - path: "brandbook-fable/examples/og-card.svg"
      provides: "1200x630 social-card source template with PNG export note (DIF-12, COLL-03)"
    - path: "brandbook-fable/examples/diagram-language.svg"
      provides: "Worked diagram-language spec, pane motif, exact domain nouns (DIF-10, COLL-03)"
    - path: "brandbook-fable/copy/copy-blocks.md"
      provides: "Paste-ready per-surface copy library (DIF-09, COPY-01)"
    - path: "brandbook-fable/copy/microcopy.md"
      provides: "Seven-noun x four-state UX strings (DIF-09, COPY-02)"
    - path: ".planning/phases/89-collateral-specimens-copy/89-visual-audit.md"
      provides: "Playwright visual audit record (iterations, both themes, both widths)"
  key_links:
    - from: "brandbook-fable/index.html"
      to: "brandbook-fable/examples/*"
      via: "img/iframe refs inside the section 08 specimen-grid figures"
      pattern: "examples/"
    - from: "brandbook-fable/examples/landing-page.html"
      to: "brandbook-fable/tokens.css"
      via: "relative stylesheet link"
      pattern: "\\.\\./tokens\\.css"
    - from: "brandbook-fable/copy/copy-blocks.md"
      to: "brandbook-fable/examples/landing-page.html"
      via: "landing hero headline + feature blurbs appear verbatim in both"
    - from: "brandbook-fable/copy/microcopy.md"
      to: "prompts/mailer-domain-language-deep-research.md"
      via: "Anymail event taxonomy vocabulary (bounced, deferred, complained, suppressed...)"
---

<objective>
Fill the fable brand system's `examples/` and `copy/` directories — landing page,
transactional email, four SVG specimens, and the two copy libraries — then slot the
rendered specimens into the Phase 88 book's prepared section 08 grid and prove
everything with a Playwright visual audit plus consolidated grep/parse/size gates.

Purpose: every real launch surface (landing, email, README, docs, social card,
diagrams, per-surface copy) gets a deployable, brand-true specimen (DIF-07..10,
DIF-12). This closes COLL-01..03 and COPY-01..02 and hands Phase 90 a complete
artifact set.

Output: 8 new files under `brandbook-fable/` (the final 8 of the 21-file manifest),
one scoped edit to `brandbook-fable/index.html` (section 08 figures only), and
`89-visual-audit.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-collateral-specimens-copy/89-CONTEXT.md
@.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md
@.planning/phases/87-logo-tournament/87-decision-record.md
@.planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md
@brandbook-fable/tokens.css

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

Token roles (tokens.css — `:root, [data-theme="light"]` at line 9, `[data-theme="dark"]` at line 89,
`@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) }` at line 140):
--mg-color-background #F8FBFD (Paper) | --mg-color-surface #FFFFFF | --mg-color-surface-sunken #EAF6FB (Mist)
--mg-color-text #0D1B2A (Ink) | --mg-color-text-muted #5C6B7A (Slate) | --mg-color-accent #277B96 (Glass)
--mg-color-accent-text/--mg-color-link #1D637A (glass-deep) | --mg-color-link-hover #174E61
feedback: success #166534/#E8F3EA, warning #96520E(text)/#A95F10(solid)/#FFF8F0, error #B42318/#FAE7E5, info #1D637A/#EAF6FB
fonts: --mg-font-display "Inter Tight", Inter, system-ui... | --mg-font-ui Inter, system-ui... | --mg-font-mono "IBM Plex Mono", ui-monospace...
type: display 44 / h1 36 / h2 30 / h3 24 / body 16 / small 14 / code 14 px
space: xs 4 / sm 8 / md 16 / lg 24 / xl 40 / 2xl 64 px; radius: sm 4 / md 8 / lg 12

Canonical logo assets (brandbook-fable/assets/ — 87-decision-record names, NOT the manifest's old names):
logo-primary.svg (light flagship — NEVER on dark grounds), logo-typemark.svg, logo-mark.svg,
logo-monochrome.svg (currentColor — inline-only contexts), logo-with-tagline.svg (outlined
"Email, made visible." tagline paths in a g[data-tagline] at line 29), favicon.svg,
social-avatar.svg, social-avatar-dark.svg.
Color program: light surfaces = Ink pane + Glass seal; dark surfaces = Mist pane + Ice #A6EAF2 seal.

index.html slot target (section 08, lines ~960-1007): six `.specimen-card` figures, each
currently figcaption-only — README header, Docs page, Landing page, Transactional email,
Social card, Diagram language. `.specimen-card figure { margin: 0 }` already styled.
88 inline-mark pattern: masthead/header marks are drawn inline from live tokens
(pane = var(--mg-color-text), seal = var(--mg-color-accent)) so the toggle re-skins them
— search "masthead" in index.html and reuse for the landing page hero mark.

Verified repo facts for honest copy (re-verify at execution time):
{:mailglass, "~> 1.5"} and {:mailglass_admin, "~> 1.5", only: [:dev]} (README.md lines 61-62);
Elixir ~> 1.18, OTP 27+, Phoenix ~> 1.8, LiveView ~> 1.1, Swoosh ~> 1.25 (README lines 28-34);
mix tasks live at lib/mix/tasks/: mail.doctor, mailglass.install, mailglass.gen.mailable,
mailglass.gen.mailbox, mailglass.gen.migration, mailglass.gen.unsubscribe, mailglass.reconcile,
mailglass.suppressions.resync, mailglass.webhooks.prune, and others — a task named in copy
MUST have a matching lib/mix/tasks/{name}.ex file.

The seven domain nouns (verbatim): Mailable, Message, Delivery, Event, InboundMessage,
Mailbox, Suppression. Anymail taxonomy: queued, sent, rejected, failed, bounced, deferred,
delivered, autoresponded, opened, clicked, complained, unsubscribed, subscribed, unknown.
dispatch != delivered (handed to provider vs downstream accepted).

Process-vocabulary denylist (Phase 88's regex + this phase's REQ prefixes — zero hits
across all of brandbook-fable/):
phase|milestone|roadmap|tournament|codex|gsd|REQ-[0-9A-Z]|BOOK-0|DIF-[0-9]|CDX-|COLL-0|COPY-0|FOUND-0|LOGO-0|GATE-0|TODO|FIXME|lorem|placeholder|draft
Write reader-facing from the first keystroke — never use these words in any shipped file
(including HTML/SVG comments and the release-note template; say "release", not "milestone").

Dual-theme-safe fills for readme-header.svg (computed):
Glass #277B96 — 4.86:1 on #FFFFFF, 3.90:1 on GitHub-dark #0d1117 (both pass >=3:1).
Slate #5C6B7A — 5.39:1 on #FFFFFF, 3.51:1 on #0d1117 (both pass).
FAIL on dark: Ink #0D1B2A (1.13), glass-deep #1D637A (2.80). FAIL on light: Mist, Ice.
readme-header fills are therefore restricted to Glass + Slate only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HTML specimens — landing-page.html + email-template.html</name>
  <files>brandbook-fable/examples/landing-page.html, brandbook-fable/examples/email-template.html</files>
  <read_first>
    - .planning/phases/89-collateral-specimens-copy/89-CONTEXT.md (locked decisions for both files)
    - .planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md — P-13 (file:// links), P-17/P-18/P-19 (email rules), P-25, P-27 (left-aligned composition), P-28 (no generic microcopy)
    - brandbook-fable/index.html — the theme-toggle head script, masthead inline-mark markup (search "masthead"), and section styles to echo
    - brandbook-fable/tokens.css (full role set)
    - README.md lines 20-140 + guides/jobs.md — real install facts, real API surface for the code block
    - .planning/phases/87-logo-tournament/87-decision-record.md — usage rules (primary never on dark)
  </read_first>
  <action>
    First, capture the pre-phase dirty-tree baseline for Task 4's no-stray-changes check:
    `git status --porcelain | grep -vE '^.. (brandbook-fable/|\.planning/)' | sort > /tmp/fable-89-baseline.txt`

    **landing-page.html** (budget 50 KB, COLL-01, DIF-07). Self-contained except two
    legitimate relative subresources: `<link rel="stylesheet" href="../tokens.css">` and
    layout CSS + toggle JS inline. Reuse the index.html three-state toggle pattern verbatim
    (auto -> light -> dark via data-theme, try/catch localStorage, no-flash head script,
    color-scheme on :root). Left-aligned, documentation-like composition per the
    differentiation brief — content anchored to a left column, ragged-right, NO centered
    hero, no three-icon symmetric columns (P-27). Sections, in order:
    1. Hero: inline token-drawn mark (copy the index.html masthead pattern — pane =
       var(--mg-color-text), seal = var(--mg-color-accent) — so the toggle re-skins it;
       do NOT use an img of logo-primary.svg, it breaks the never-on-dark rule), the
       hero headline + subhead (these exact strings also ship in copy-blocks.md — Task 3
       copies them from here), and a working install snippet above the fold:
       `{:mailglass, "~> 1.5"}` then `mix mailglass.install`.
    2. Feature grid: 3-6 features as an asymmetric list/grid, each blurb in maintainer
       voice naming real shipped capability (preview dashboard, normalized webhook events,
       suppression lists, RFC 8058 List-Unsubscribe, append-only event ledger,
       mix mail.doctor) — verify each against README.md/guides/jobs.md before writing.
    3. Real code block: a HEEx/Elixir sample whose every module and function exists in the
       repo (e.g., a `use Mailglass.Mailable` definition mirroring guides/README examples).
       Grep the repo for each module/function named before committing it to the page.
    4. Comparison row: "why not just Swoosh?" framing — mailglass composes ON Swoosh, does
       not replace it (positioning thesis). Calm two-column compare, no competitor-bashing.
    5. Footer: nav links to GitHub / Hex.pm / HexDocs as `href="#"` fragments with real
       link text (find-and-replace deployment target — zero http(s) URLs anywhere in the
       file), license line (MIT).
    No `placeholder` attributes on any element; no fetch/XMLHttpRequest/module scripts;
    every internal link document-relative to a concrete file (P-13).

    **email-template.html** (budget 50 KB, COLL-02, DIF-08). Content: a real
    mailglass-flavored sign-in (magic-link style) transactional email in brand voice —
    the exact surface where tracking is forbidden, so the specimen carries none.
    Hard rules, all enumerated:
    - Table-based layout only: nested `<table role="presentation">`, 600px content width,
      no flex/grid, no rem, all styles inline on elements (a small `<style>` block allowed
      only for the prefers-color-scheme block + client resets).
    - Both metas in head: `<meta name="color-scheme" content="light dark">` and
      `<meta name="supported-color-schemes" content="light dark">`, plus
      `:root { color-scheme: light dark; }` and a `@media (prefers-color-scheme: dark)`
      block for Apple Mail (P-19).
    - Colors are the token hexes, six-digit only, and NEVER `#FFFFFF`/`#000000` — use
      Paper #F8FBFD as the light ground and Ink #0D1B2A for text (mid-tones survive Gmail
      inversion); CTA button Glass #277B96 background with Paper #F8FBFD text.
    - ZERO svg: no inline svg, no img pointing at svg — in fact zero `<img>` tags at all
      (kills tracking pixels too). Masthead is type-only: "mailglass" set in the web-safe
      stack (Helvetica/Arial fallback chain) in brand colors. Do not write the string
      "svg" anywhere in the file, comments included (the gate is `grep -i svg == 0`); the
      production-logo note in the file says "host a PNG or JPG logo" without naming the
      vector source format.
    - Bulletproof CTA: padded table-cell button + `<!--[if mso]>` VML fallback for
      classic Outlook.
    - No tracking of any kind and no tracking-related prose in the file (no 1x1 images,
      no open/click pixels, no "tracking" language — the no-tracking rule is documented in
      index.html's caption, not in the email).
    - Body copy: greeting, one-sentence purpose ("Here's your sign-in link..."), the CTA,
      an expiry note, a "didn't request this?" reassurance line, footer with sender line —
      all specific and composed, never "Oops", consistent with microcopy voice.
    - Links are `href="#"` fragments (find-and-replace target; zero external URLs).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && l=brandbook-fable/examples/landing-page.html && e=brandbook-fable/examples/email-template.html && [ "$(grep -Ec '(href|src)="https?://' "$l")" -eq 0 ] && [ "$(grep -c 'fetch(' "$l")" -eq 0 ] && [ "$(grep -c 'XMLHttpRequest' "$l")" -eq 0 ] && [ "$(grep -c '<script src' "$l")" -eq 0 ] && [ "$(wc -c < "$l")" -le 51200 ] && [ "$(grep -c '\.\./tokens\.css' "$l")" -ge 1 ] && [ "$(grep -c 'data-theme' "$l")" -ge 2 ] && [ "$(grep -c 'mailglass.install' "$l")" -ge 1 ] && [ "$(grep -c '~> 1.5' "$l")" -ge 1 ] && [ "$(grep -ci 'swoosh' "$l")" -ge 1 ] && [ "$(grep -c 'placeholder' "$l")" -eq 0 ] && [ "$(grep -ic 'svg' "$e")" -eq 0 ] && [ "$(grep -c '<img' "$e")" -eq 0 ] && [ "$(grep -c '<table' "$e")" -ge 3 ] && [ "$(grep -c 'role="presentation"' "$e")" -ge 3 ] && [ "$(grep -Eic 'display:[[:space:]]*(flex|grid)' "$e")" -eq 0 ] && [ "$(grep -Eic '#(ffffff|000000)' "$e")" -eq 0 ] && ! grep -Eq '#[0-9A-Fa-f]{3}([^0-9A-Fa-f]|$)' "$e" && [ "$(grep -c 'name="color-scheme"' "$e")" -ge 1 ] && [ "$(grep -c 'supported-color-schemes' "$e")" -ge 1 ] && [ "$(grep -ci 'prefers-color-scheme' "$e")" -ge 1 ] && [ "$(grep -ci 'if mso' "$e")" -ge 1 ] && [ "$(grep -Ec '(href|src)="https?://' "$e")" -eq 0 ] && [ "$(grep -Eic 'track|width="1"|height="1"' "$e")" -eq 0 ] && [ "$(grep -c '600' "$e")" -ge 1 ] && [ "$(wc -c < "$e")" -le 51200 ] && [ "$(grep -ci 'oops' "$l" "$e" | awk -F: '{s+=$NF} END{print s+0}')" -eq 0 ] && echo T1-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Landing: hero + install snippet + feature grid + real code block + Swoosh comparison
      row + footer, all left-aligned per the compositional stance; toggle re-skins it;
      opens complete from file://
    - Every module/function/mix-task/version named on the landing page verified to exist
      in the repo (record the checked facts in the SUMMARY)
    - Email: table-based, inline styles, both metas, mid-tone token hexes only, zero svg
      substring, zero img tags, VML fallback present, magic-link content with no tracking
    - T1-GATES-PASS printed
  </acceptance_criteria>
  <done>Both HTML specimens exist within their 50 KB budgets and pass every enumerated gate; the hero headline/subhead strings are noted for Task 3 reuse.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SVG specimens — readme-header, docs-page, og-card, diagram-language</name>
  <files>brandbook-fable/examples/readme-header.svg, brandbook-fable/examples/docs-page.svg, brandbook-fable/examples/og-card.svg, brandbook-fable/examples/diagram-language.svg</files>
  <read_first>
    - .planning/phases/87-logo-tournament/87-decision-record.md (geometry, color program, standing constraints C-15/C-16)
    - brandbook-fable/assets/logo-primary.svg, logo-typemark.svg, logo-mark.svg, logo-with-tagline.svg — reuse their path data verbatim (the wordmark and tagline are already outlined; never redraw them)
    - .planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md — P-01..P-08 (SVG portability + a11y), P-20 (og-card reality), P-25 (no glassmorphism)
    - brandbook-fable/index.html section 08 per-surface reference table (lines ~995-1006) — each file must honor its stated rule
  </read_first>
  <action>
    All four files: root `<svg>` with `viewBox`, `role="img"`, `aria-labelledby` pointing
    at a `<title>` first child (unique prefixed ids, e.g. `mg-fable-readme-title` — P-04),
    `<desc>` where content is complex. Outlined paths ONLY — zero `<text>`, zero
    `font-family`, zero `currentColor` (these are standalone img-context files, P-02),
    zero gradients/filters/foreignObject/scripts/external refs. Explicit six-digit hex
    fills from the token palette. No editor metadata, no process vocabulary anywhere.

    **readme-header.svg** (budget 20 KB): width-bounded GitHub README banner — the lockup
    plus the one-line positioning. Compose by reusing path data: the mark geometry from
    logo-primary.svg and the wordmark paths from logo-typemark.svg, plus the outlined
    tagline paths from logo-with-tagline.svg's `g[data-tagline]` group ("Email, made
    visible."). RECOLOR for dual-theme survival: fills restricted to Glass #277B96 (mark +
    wordmark) and Slate #5C6B7A (tagline) — both computed >=3:1 on white AND on GitHub-dark
    #0d1117 (see interfaces block). Ink, glass-deep, Mist, and Ice are forbidden in this
    file. No background rect (transparent — GitHub's ground shows through; this also keeps
    the no-plate rule). After authoring, verify computationally: extract every fill hex and
    confirm contrast >= 3.0 against both #FFFFFF and #0d1117 with a python3 one-liner using
    the WCAG relative-luminance formula.

    **docs-page.svg** (budget 24 KB): HexDocs-style page framing — sidebar with the
    logo-mark path (light expression: Ink pane + Glass seal, this specimen renders on a
    light ground), content column with greeked text (rounded-rect bars in text/muted hexes,
    NOT drawn glyphs), a code block on the sunken Mist surface, a link bar in glass-deep
    #1D637A (the per-surface rule: body-size accent copy uses #1D637A, not Glass). The
    framing demonstrates layout hierarchy, not readable prose — greeked bars are the
    correct genre move and keep the zero-text gate.

    **og-card.svg** (budget 20 KB): `viewBox="0 0 1200 630"`, Paper #F8FBFD ground
    rectangle (this is a card template — the documented ground, not a logo plate), mark +
    wordmark paths reused from assets at generous scale, the outlined tagline line, calm
    left-anchored composition. The `<desc>` documents the export policy in reader-facing
    words: this file is the source template; export a 1200x630 PNG locally (open in a
    browser and screenshot, or any vector tool) before publishing — crawlers do not render
    vector og:images (P-20). The string "PNG" must appear in the desc.

    **diagram-language.svg** (budget 24 KB): the worked diagram-language spec. Two zones:
    (a) a legend specifying the vocabulary by drawn example — node = pane (rounded rect,
    radius 4, flat token fill, Mist surface with Ink content bars), ONE stroke weight
    (2px in border-strong #5C6B7A or accent #277B96), one arrowhead spec (small filled
    triangle, no fancy markers), the pane/light motif (voids read as light passing
    through — never breakage, C-15); (b) one real worked flow: Mailable -> Message ->
    Delivery -> Event as four pane nodes connected left-to-right with the specified
    arrows. Node labels are drawn as paths: construct a minimal geometric stencil
    letterform set (integer-grid, mono-weight strokes converted to filled paths — label
    craft, not wordmark craft; keep it simple and legible at the rendered size). The
    EXACT nouns Mailable, Message, Delivery, Event must also appear as plain words in the
    `<title>`/`<desc>` (that is what the grep gate reads, and what screen readers get).
    No gradients, flat fills only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && d=brandbook-fable/examples && for s in readme-header docs-page og-card diagram-language; do f="$d/$s.svg"; xmllint --noout "$f" || exit 1; [ "$(grep -c '<text' "$f")" -eq 0 ] || { echo "TEXT: $f"; exit 1; }; [ "$(grep -ic 'font-family' "$f")" -eq 0 ] || { echo "FONT: $f"; exit 1; }; [ "$(grep -c 'viewBox' "$f")" -ge 1 ] || { echo "VIEWBOX: $f"; exit 1; }; [ "$(grep -c '<title' "$f")" -ge 1 ] || { echo "TITLE: $f"; exit 1; }; [ "$(grep -c 'role="img"' "$f")" -ge 1 ] || { echo "ROLE: $f"; exit 1; }; [ "$(grep -Eic 'foreignObject|<script|href="http|@import|onload=|currentColor' "$f")" -eq 0 ] || { echo "PORTABILITY: $f"; exit 1; }; [ "$(grep -Eic 'linearGradient|radialGradient|feGaussianBlur|backdrop-filter' "$f")" -eq 0 ] || { echo "GRADIENT: $f"; exit 1; }; done && [ "$(grep -c 'viewBox="0 0 1200 630"' $d/og-card.svg)" -ge 1 ] && [ "$(grep -c 'PNG' $d/og-card.svg)" -ge 1 ] && for w in Mailable Message Delivery Event; do [ "$(grep -c "$w" $d/diagram-language.svg)" -ge 1 ] || { echo "NOUN: $w"; exit 1; }; done && [ "$(grep -Eic '#0D1B2A|#EAF6FB|#A6EAF2|#1D637A' $d/readme-header.svg)" -eq 0 ] && [ "$(wc -c < $d/readme-header.svg)" -le 20480 ] && [ "$(wc -c < $d/docs-page.svg)" -le 24576 ] && [ "$(wc -c < $d/og-card.svg)" -le 20480 ] && [ "$(wc -c < $d/diagram-language.svg)" -le 24576 ] && echo T2-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - All four SVGs xmllint-parse, carry viewBox + role="img" + title, and contain zero
      text elements, zero font-family, zero currentColor, zero gradients/filters
    - readme-header fills are Glass/Slate only and the python3 contrast check confirms
      every fill >= 3:1 on both #FFFFFF and #0d1117 (record the computed ratios in the SUMMARY)
    - og-card is exactly 1200x630 with the PNG export policy in its desc
    - diagram-language shows legend (node shape, single stroke weight, arrowhead, pane
      motif) + the Mailable -> Message -> Delivery -> Event flow, nouns present in title/desc
    - All four within their manifest byte budgets; T2-GATES-PASS printed
  </acceptance_criteria>
  <done>Four portable, accessible, on-program SVG specimens exist in brandbook-fable/examples/ within budget.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Copy library — copy-blocks.md + microcopy.md</name>
  <files>brandbook-fable/copy/copy-blocks.md, brandbook-fable/copy/microcopy.md</files>
  <read_first>
    - prompts/mailer-domain-language-deep-research.md — seven nouns + Anymail taxonomy, verbatim vocabulary
    - prompts/mailglass-brand-book.md — voice principles (clear, exact, confident not cocky, warm not cute)
    - README.md + guides/jobs.md — every fact a copy block asserts
    - brandbook-fable/examples/landing-page.html (Task 1 output) — hero headline/subhead/feature blurbs to mirror verbatim
    - ls lib/mix/tasks/ — the authoritative mix-task list for honesty checks
  </read_first>
  <action>
    **copy-blocks.md** (budget 10 KB, COPY-01, DIF-09). Paste-ready blocks, no
    placeholders, no lorem, no hype words (banned: blazing/blazingly, supercharge, 10x;
    never "Oops"). Exact section headings (the gates grep them):
    `## GitHub About` — the About text as a single `> `-prefixed blockquote line,
    <= 350 characters net of the prefix (GitHub's hard limit; the gate counts it).
    `## Hex.pm description` — the mix.exs description string, one paragraph.
    `## HexDocs intro` — the opening paragraph for the docs front page.
    `## Landing hero` — headline + subhead, VERBATIM the strings shipped in
    landing-page.html (single source of truth; copy from the file, do not re-write).
    `## Feature blurbs` — the landing feature-grid blurbs, verbatim from the file.
    `## Calls to action` — primary + secondary CTA strings.
    `## Launch post` — a social/launch post in maintainer voice (no character-count
    claims, no emoji, no hashtag spam — at most one or two plain sentences + link line).
    `## Release notes template` — the release-note voice template: structure (what
    changed / why it matters / upgrade note), tense and person rules, one filled example
    drawn from a real shipped feature. Say "release", never the denylisted process words.
    HONESTY PASS (explicit, recorded in the SUMMARY): for every package name, version
    pin, mix task, module name, and capability claim in both files, verify it against
    README.md / guides/ / lib/ — e.g. `{:mailglass, "~> 1.5"}` matches README, every
    `mix mail*.*` string has a lib/mix/tasks/{name}.ex file, every module name greps in
    lib/. Anything unverifiable gets rewritten or cut.

    **microcopy.md** (budget 10 KB, COPY-02). One `## {Noun}` section per domain noun in
    this order: Mailable, Message, Delivery, Event, InboundMessage, Mailbox, Suppression.
    Each section is a 4-row table with EXACTLY this row format (the gate counts
    `^| Error`, `^| Empty`, `^| Success`, `^| Warning` — 7 each):
    `| Error | ... |`, `| Empty | ... |`, `| Success | ... |`, `| Warning | ... |`
    Rules for every string: specific and composed, brand error style ("Delivery blocked:
    recipient is on the suppression list" — never "Oops"); exact domain vocabulary;
    Anymail taxonomy terms used correctly (bounced, deferred, complained, suppressed,
    rejected with its reject_reason set); the dispatch/delivered distinction respected —
    at least one Delivery string distinguishes "dispatched to the provider" from
    "delivered" (downstream accepted); empty states say what will appear and how to cause
    it; success states confirm the observed fact in past tense (Events are observed
    facts). Include a short preamble noting strings are keyed to the event taxonomy.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && c=brandbook-fable/copy/copy-blocks.md && m=brandbook-fable/copy/microcopy.md && for h in 'GitHub About' 'Hex.pm description' 'HexDocs intro' 'Landing hero' 'Feature blurbs' 'Calls to action' 'Launch post' 'Release notes template'; do grep -q "^## $h" "$c" || { echo "MISSING SECTION: $h"; exit 1; }; done && a=$(sed -n '/^## GitHub About/,/^## /p' "$c" | grep '^> ' | sed 's/^> //' | tr -d '\n' | wc -c | tr -d ' ') && [ "$a" -le 350 ] && [ "$a" -ge 80 ] && for st in Error Empty Success Warning; do [ "$(grep -c "^| $st" "$m")" -ge 7 ] || { echo "STATE COUNT: $st"; exit 1; }; done && for n in Mailable Message Delivery Event InboundMessage Mailbox Suppression; do grep -q "$n" "$m" || { echo "NOUN: $n"; exit 1; }; done && grep -qi 'dispatch' "$m" && grep -qi 'delivered' "$m" && [ "$(cat "$c" "$m" | grep -Eic 'lorem|ipsum|blazing|supercharge|10x|oops')" -eq 0 ] && for t in $(grep -hoE '\bmix mail(glass)?\.[a-z_.]+' "$c" "$m" brandbook-fable/examples/landing-page.html | awk '{print $2}' | sort -u); do [ -f "lib/mix/tasks/$t.ex" ] || { echo "FAKE TASK: $t"; exit 1; }; done && [ "$(wc -c < "$c")" -le 10240 ] && [ "$(wc -c < "$m")" -le 10240 ] && echo T3-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - copy-blocks.md has all 8 sections, GitHub About <= 350 chars (gate-measured),
      hero/blurbs verbatim-identical to landing-page.html
    - microcopy.md has 7 noun sections x 4 state rows = 28+ entries in the exact
      gate-countable row format
    - Honesty pass recorded: every named package/version/task/module verified against
      the repo; every mix task named has a real lib/mix/tasks file (gate-enforced)
    - Zero hype words, zero "Oops", taxonomy-consistent, dispatch-vs-delivered present
    - T3-GATES-PASS printed
  </acceptance_criteria>
  <done>Both copy files exist within 10 KB budgets, paste-ready and fact-true.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: index.html Specimens slot-in + Playwright visual audit + consolidated gates</name>
  <files>brandbook-fable/index.html, .planning/phases/89-collateral-specimens-copy/89-visual-audit.md</files>
  <read_first>
    - brandbook-fable/index.html section 08 (lines ~960-1007) — the six figcaption-only specimen cards and per-surface table
    - .planning/phases/88-brand-book-assembly/88-visual-audit.md — the audit record format to mirror
    - .planning/phases/88-brand-book-assembly/88-01-PLAN.md lines 355-365 — the original Task 1 gate set being re-run (with the examples/ check inverted)
  </read_first>
  <action>
    **Slot-in (the ONLY index.html change — edits scoped to the six section 08 figures;
    figcaptions may get at most one-line accuracy tweaks):**
    - README header / Docs page / Social card / Diagram language figures: insert
      `<img src="examples/{file}.svg" alt="{meaningful alt}">` above each figcaption,
      rendered on a fixed light chip (explicit Paper #F8FBFD background + 1px border via
      a small class or inline style) so the img-context light-expression artwork stays
      deterministic in both page themes — the established logo-chip pattern from 88;
      file-based SVGs cannot follow the page toggle (P-02 lesson).
    - Landing page / Transactional email figures: insert
      `<iframe src="examples/landing-page.html" loading="lazy" title="Landing page specimen">`
      (and the email equivalent) at a sensible fixed height, plus a plain relative
      `<a href="examples/...">open the file</a>` link in the figcaption. Relative iframe
      src works on file://; no srcdoc inlining (budget).
    All refs document-relative to concrete files (P-13). No new external URLs, no new
    script. Stay within the 150 KB index budget.

    **Playwright visual audit (real, iterative — the 88 pattern):**
    Screenshot to an ignored tmp dir `/tmp/fable-89-audit/` using the available browser
    automation (Playwright / agent-browser), then READ each screenshot and log defects;
    fix; re-shoot; iterate until a full pass records zero defects. Surfaces:
    1. landing-page.html at 1440px and 390px, light AND dark (toggle both ways) — check
       composition stays left-anchored, code block legible, toggle re-skins everything,
       hero mark visible in both themes.
    2. email-template.html in the browser at 600px-ish and 390px — table layout intact,
       CTA renders, nothing overflows.
    3. Each of the four SVGs embedded via `<img>` in a throwaway harness page
       (/tmp, not committed) on BOTH a #FFFFFF and a #0d1117 ground — this simulates the
       GitHub img sandbox; readme-header must be fully legible on both grounds; others
       must be legible on the light ground.
    4. index.html section 08 at 1440px and 390px, both themes — all six figures render,
       light chips hold in dark mode, iframes load.
    Record iterations, surfaces, defects found/fixed, and the final zero-defect pass in
    `.planning/phases/89-collateral-specimens-copy/89-visual-audit.md` (mirror the
    88-visual-audit.md format; mention dark and 390 explicitly).

    **Consolidated phase gates** (run after fixes settle): the re-run of Phase 88's
    index gates with the examples/ check inverted (refs must now exist), the
    whole-folder denylist, folder/file size budgets, file inventory, and the
    no-stray-changes diff against /tmp/fable-89-baseline.txt from Task 1.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && f=brandbook-fable/index.html && [ "$(grep -c 'examples/' "$f")" -ge 6 ] && [ "$(grep -Ec '(href|src)="https?://' "$f")" -eq 0 ] && [ "$(grep -c 'fetch(' "$f")" -eq 0 ] && [ "$(grep -c '<script src' "$f")" -eq 0 ] && [ "$(grep -c 'XMLHttpRequest' "$f")" -eq 0 ] && [ "$(wc -c < "$f")" -le 153600 ] && [ "$(grep -c '<noscript>' "$f")" -ge 1 ] && [ "$(grep -c 'getComputedStyle' "$f")" -ge 1 ] && [ "$(grep -c 'data-theme' "$f")" -ge 2 ] && [ "$(grep -c '16.74' "$f")" -ge 1 ] && [ "$(grep -c '4.82' "$f")" -ge 1 ] && for h in 'Orientation &amp; Essence' 'Voice' 'Color &amp; Tokens' 'Typography' 'Contrast Matrix' 'Component Gallery' 'Logo System' 'Specimens &amp; Applications' 'Usage &amp; Export Policy'; do [ "$(grep -c "$h" "$f")" -ge 1 ] || { echo "MISSING HEADING: $h"; exit 1; }; done && [ "$(awk '/<script>/{x=1} x{n++} /<\/script>/{x=0} END{print n+0}' "$f")" -le 150 ] && [ "$(grep -riEc 'phase|milestone|roadmap|tournament|codex|gsd|REQ-[0-9A-Z]|BOOK-0|DIF-[0-9]|CDX-|COLL-0|COPY-0|FOUND-0|LOGO-0|GATE-0|TODO|FIXME|lorem|placeholder|draft' brandbook-fable/ | awk -F: '{s+=$NF} END{print s+0}')" -eq 0 ] && [ "$(du -sk brandbook-fable | awk '{print $1}')" -le 500 ] && [ -z "$(find brandbook-fable -type f -size +100k)" ] && for p in examples/landing-page.html examples/email-template.html examples/readme-header.svg examples/docs-page.svg examples/og-card.svg examples/diagram-language.svg copy/copy-blocks.md copy/microcopy.md; do [ -f "brandbook-fable/$p" ] || { echo "MISSING: $p"; exit 1; }; done && a=.planning/phases/89-collateral-specimens-copy/89-visual-audit.md && [ -f "$a" ] && [ "$(grep -ci 'iteration' "$a")" -ge 1 ] && [ "$(grep -ci 'dark' "$a")" -ge 1 ] && [ "$(grep -ci '390' "$a")" -ge 1 ] && diff <(git status --porcelain | grep -vE '^.. (brandbook-fable/|\.planning/)' | sort) /tmp/fable-89-baseline.txt >/dev/null && echo T4-GATES-PASS</automated>
  </verify>
  <acceptance_criteria>
    - All six section 08 figures render real collateral; the slot-in is the only
      index.html change and every Phase 88 index gate still passes (size, zero external
      URLs, 9 headings, 8 asset refs, script-line cap)
    - Whole-folder denylist returns zero hits; folder <= 500 KB; no file > 100 KB;
      all 8 manifest files present
    - 89-visual-audit.md records iterative screenshot reads (1440 + 390, both themes,
      SVGs on white and #0d1117 grounds) ending in a zero-defect pass
    - No files outside brandbook-fable/ and .planning/ changed beyond the Task 1 baseline
    - T4-GATES-PASS printed
  </acceptance_criteria>
  <done>The book renders its complete specimen grid, the visual audit record exists with a zero-defect final iteration, and the consolidated gate prints T4-GATES-PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public artifacts | brandbook-fable/ ships in a public repo; internal planning vocabulary crossing into it is information disclosure |
| brand book -> file:// browser | HTML/SVG opened from disk; any external ref or script breaks the zero-network contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Information Disclosure | all new brandbook-fable files | mitigate | Extended process-vocabulary denylist grep over the whole folder (Task 4 gate, zero hits) |
| T-89-02 | Tampering | landing/email/index HTML | mitigate | Zero external URLs / fetch / script-src gates per file (Tasks 1, 4); SVGs gated against scripts/foreignObject/external refs (Task 2) |
| T-89-03 | Spoofing | copy-blocks facts | mitigate | Honesty pass: every package/version/task/module verified against README/guides/lib; mix-task existence gate (Task 3) |
| T-89-SC | Tampering | package installs | accept | No npm/pip/cargo/hex installs in this phase — pure text artifacts |
</threat_model>

<verification>
- Task gates T1..T4 all print PASS (each is a single consolidated command).
- 89-visual-audit.md final iteration records zero defects across landing (both themes,
  both widths), email, four SVGs on white + #0d1117 grounds, and index section 08.
- Folder inventory matches the 21-file manifest exactly (13 pre-existing + 8 new).
- Working tree outside brandbook-fable/ and .planning/ identical to the pre-phase baseline.
</verification>

<success_criteria>
Maps 1:1 to the five ROADMAP Phase 89 success criteria:
1. landing-page.html is a full self-contained, find-and-replace-deployable blueprint (T1 gate + audit). [COLL-01]
2. email-template.html is client-safe: tables, inline styles, both color-scheme metas (T1 gate). [COLL-02]
3. Four SVG specimens ship — README header (dual-GitHub-theme legible), docs framing, 1200x630 og-card with documented PNG export policy, diagram-language spec with the exact noun flow (T2 gate + audit). [COLL-03]
4. copy-blocks.md is paste-ready with GitHub About <= 350 chars, all 8 surfaces, zero placeholders/lorem, facts verified (T3 gate). [COPY-01]
5. microcopy.md keys all seven nouns x four states (>= 28 entries), taxonomy-consistent (T3 gate). [COPY-02]
Plus integration: index.html renders the specimens and still passes its Phase 88 gates (T4).
</success_criteria>

<output>
Create `.planning/phases/89-collateral-specimens-copy/89-01-SUMMARY.md` when done.
Commits: `feat(89-01): ...` for brandbook-fable files, `docs(89-01): ...` for planning
artifacts (89-visual-audit.md). No branching — commit on main.
</output>
