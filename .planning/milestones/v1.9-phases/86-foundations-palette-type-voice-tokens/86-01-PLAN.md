---
phase: 86-foundations-palette-type-voice-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook-fable/tokens.json
  - brandbook-fable/tokens.css
  - .planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04]

must_haves:
  truths:
    - "tokens.css applied to a page yields complete light AND dark themes: every --mg-color-* role defined under :root is reassigned under [data-theme=\"dark\"] (and mirrored in the prefers-color-scheme block) — zero missing dark roles"
    - "The six research fix hexes appear in tokens.json bound to the roles they fix: #1D637A (link/accent-text/info-text light), #174E61 (link-hover/active light), #96520E (warning-text light), #74909F (border-input light), #62809A (border-input dark), #E29089 (error-text dark)"
    - "The decision record contains a script-computed WCAG matrix covering every text-role/surface-role pair in BOTH themes with ratio, AA/AAA normal + large verdicts, and a one-line usage rule per pair — including Glass #277B96 on white at 4.82 (AA pass) and the rule routing accent text on tinted surfaces through glass-deep"
    - "Every seed palette/typography decision has an evolve-vs-keep row with contrast math justifying any deviation; the codex dark ramp adoption and the four open dark feedback backgrounds are recorded with their verified ratios"
    - "grep -riE '(phase|plan|milestone|codex|draft|tbd)' over brandbook-fable/ returns nothing"
  artifacts:
    - path: "brandbook-fable/tokens.json"
      provides: "DTCG-2025.10-style two-tier tokens (palette + semantic roles, light and dark, interaction + feedback states, type scale, space, radius, focus)"
      contains: "\"$value\""
    - path: "brandbook-fable/tokens.css"
      provides: "Genuinely usable --mg-* custom properties: :root light block, [data-theme=\"dark\"] block, prefers-color-scheme media block, honest font fallback stacks"
      contains: "[data-theme=\"dark\"]"
    - path: ".planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md"
      provides: "Evolve-vs-keep records + full computed contrast matrix data (structured, Phase 88 renders it)"
      contains: "4.82"
  key_links:
    - from: "brandbook-fable/tokens.css"
      to: "brandbook-fable/tokens.json"
      via: "hand-synced hex values — every hex literal in tokens.css exists in tokens.json"
      pattern: "#[0-9A-Fa-f]{6}"
    - from: ".planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md"
      to: "brandbook-fable/tokens.json"
      via: "matrix computed from the final shipped token hexes (no drift)"
      pattern: "#1D637A"
    - from: "brandbook-fable/tokens.css [data-theme=\"dark\"] block"
      to: "brandbook-fable/tokens.css :root block"
      via: "identical --mg-color-* property name set (parity check)"
      pattern: "--mg-color-"
---

<objective>
Create the first `brandbook-fable/` artifacts: a contrast-proven, two-tier token
foundation (`tokens.json` + `tokens.css`) for light and dark themes, plus the
decision record carrying every evolve-vs-keep call and the full computed WCAG
contrast matrix. Every later v1.9 artifact (logo gallery rendering, book,
specimens) consumes these tokens.

Purpose: Fix the v1.8 root failures at the foundation layer — contrast never
validated (computed matrix now, not "later"), dark tokens incomplete (complete
parallel role set, parity-checked), planning-language leakage (gated in-phase).

Output: `brandbook-fable/tokens.json`, `brandbook-fable/tokens.css`,
`.planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/86-foundations-palette-type-voice-tokens/86-CONTEXT.md
@.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md
@.planning/research/v1.9-brandbook-fable/TOKENS-A11Y.md
@.planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md

Scope locks (v1.9, apply here): only `brandbook-fable/` plus `.planning/` are
written. `brandbook/` is the frozen A/B baseline — never opened for editing.
Text artifacts only. No Node toolchain, no build step, no webfonts. Work on
main — no git branching. Throwaway scripts run from /tmp and are NEVER
committed.

Commit conventions: `feat(86-01): ...` for brandbook-fable/ token files,
`docs(86-01): ...` for the decision record. Conventional Commits enforced.

Pitfalls this plan must clear (per the brief's mapping): P-09 (honest font
fallback stacks — Inter is preinstalled nowhere), P-11 (token structure
supports the three-state toggle: :root light + [data-theme="dark"] + media
block), P-15 (zero planning-language from the first draft), P-16 (semantic +
state role names whose values change per theme while names stay constant; no
numeric ramps, no light-only roles with dark bolt-ons).
</context>

<interfaces>
<!-- The complete role inventory and final hex values. Executors use these
     directly — no re-derivation. Source: TOKENS-A11Y.md + 86-CONTEXT.md. -->

CSS custom-property naming: `--mg-{category}-{role}[-{variant}][-{state}]`,
lowercase kebab, state suffix last, default state unsuffixed.

39 color roles per theme (identical key sets in light and dark):

Surfaces (6): background, surface, surface-raised, surface-overlay,
  surface-sunken, surface-selected
Borders (3): border (decorative), border-input (>=3:1 verified), border-strong
Text (4): text, text-muted, text-disabled, text-inverse
Accent/link (6): accent (non-text), accent-text (AA-verified), link,
  link-hover, link-active, focus-ring
Feedback (4 kinds x 5 = 20): {success|warning|error|info}-text, -bg, -border,
  -solid, -on-solid

LIGHT values (all computed-verified in TOKENS-A11Y.md):
  background #F8FBFD (Paper) · surface #FFFFFF · surface-raised #FFFFFF ·
  surface-overlay #FFFFFF · surface-sunken #EAF6FB (Mist) ·
  surface-selected #DDF2F7
  border #C7DCE5 · border-input #74909F (slate-soft, 3.24 Paper / 3.06 Mist) ·
  border-strong #5C6B7A (Slate)
  text #0D1B2A (Ink, 16.74) · text-muted #5C6B7A (Slate, 5.26 — AA, fails
  AAA) · text-disabled #9AA8B4 (exempt SC 1.4.3) · text-inverse #FFFFFF
  accent #277B96 (Glass — icons/borders/focus/large-text/solid-fills ONLY) ·
  accent-text #1D637A (glass-deep, 6.12 worst light surface) ·
  link #1D637A · link-hover #174E61 (glass-deepest, 8.30 worst) ·
  link-active #174E61 · focus-ring #277B96
  success: text #166534 (Pine) · bg #E8F3EA (6.26) · border #166534 ·
  solid #166534 · on-solid #FFFFFF (7.13)
  warning: text #96520E (amber-deep, 5.43 Mist) · bg #FFF8F0 (5.68) ·
  border #A95F10 (Amber, >=3:1 all light surfaces) · solid #A95F10 ·
  on-solid #FFFFFF (4.85)
  error: text #B42318 (Crimson) · bg #FAE7E5 (5.52) · border #B42318 ·
  solid #B42318 · on-solid #FFFFFF (6.57)
  info: text #1D637A (glass-deep — Glass FAILS here at 4.37) · bg #EAF6FB ·
  border #277B96 · solid #1D637A · on-solid #FFFFFF (6.74)

DARK values (codex ramp adopted, verified; new values marked):
  background #0D1B2A (Ink) · surface #0D1B2A · surface-raised #152538 ·
  surface-overlay #1F3049 · surface-sunken #0A1521 · surface-selected #1B3E55
  border #315069 (hairlines only, 1.83-2.06) · border-input #62809A
  (slate-bright, NEW — 4.20/3.75/3.22) · border-strong #62809A
  text #EAF6FB (Mist, 15.80 base / 11.37 worst) · text-muted #B8CAD4 (AAA
  everywhere, 7.41 worst) · text-disabled #6B7E8F (slate-dim, NEW — exempt,
  4.15 base) · text-inverse #0D1B2A (Ink, for Ice/light/solid fills)
  accent #A6EAF2 (Ice) · accent-text #A6EAF2 (12.98 base / 8.40 worst) ·
  link #A6EAF2 · link-hover #D6F7FB (11.06 worst) · link-active #D6F7FB ·
  focus-ring #A6EAF2 (>=9.34 everywhere)
  success: text #8BB77F (5.47 worst) · border #8BB77F · solid #8BB77F ·
  on-solid #0D1B2A (7.60)
  warning: text #E0A955 (5.95 worst) · border #E0A955 · solid #E0A955 ·
  on-solid #0D1B2A (8.26)
  error: text #E29089 (crimson-bright, NEW — replaces codex #D47368 which
  fails at 3.85-4.09; 4.60 worst incl. selected) · border #E29089 ·
  solid #E29089 · on-solid #0D1B2A (7.11)
  info: text #A6EAF2 · border #A6EAF2 · solid #A6EAF2 · on-solid #0D1B2A
  Dark feedback BACKGROUNDS are OPEN (Task 1 decides + script-verifies).
  Starting candidates (low-chroma hue tints near the raised-surface
  lightness): success-bg #142B22 · warning-bg #2B2314 · error-bg #2E1B1E ·
  info-bg #11293A. The guarantee: {kind}-text >= 4.5:1 on {kind}-bg.

Font stacks (P-09 — honest fallbacks, exact strings):
  --mg-font-display: "Inter Tight", Inter, system-ui, -apple-system,
    "Segoe UI", Roboto, "Helvetica Neue", sans-serif
  --mg-font-ui: Inter, system-ui, -apple-system, "Segoe UI", Roboto,
    "Helvetica Neue", sans-serif
  --mg-font-mono: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo,
    Consolas, monospace

Type scale (one exact scale within the seed ranges; suggested):
  display 44px · h1 36px · h2 30px · h3 24px · body 16px · small 14px ·
  code 14px

Space/radius/focus (Claude's discretion; suggested minimal set):
  space xs 4px / sm 8px / md 16px / lg 24px / xl 40px / 2xl 64px
  radius sm 4px / md 8px / lg 12px / full 9999px
  focus-ring-width 2px / focus-ring-offset 2px
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify open hexes, then author brandbook-fable/tokens.json + tokens.css</name>
  <files>brandbook-fable/tokens.json, brandbook-fable/tokens.css</files>
  <read_first>
    .planning/phases/86-foundations-palette-type-voice-tokens/86-CONTEXT.md,
    .planning/research/v1.9-brandbook-fable/TOKENS-A11Y.md,
    .planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md (P-09, P-11, P-15, P-16),
    .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md (Non-Negotiables, File Manifest rows for tokens.json/tokens.css)
  </read_first>
  <action>
    First, write a throwaway WCAG script at /tmp/mg_contrast.py (python3, ~15
    lines — NOT committed, never placed under brandbook-fable/ or .planning/):
    implements relative luminance per channel c'=c/255; c_lin = c'/12.92 if
    c' &lt;= 0.04045 else ((c'+0.055)/1.055)**2.4; L = 0.2126*R + 0.7152*G +
    0.0722*B; ratio = (L_lighter+0.05)/(L_darker+0.05). Use it to (a) re-verify
    the six research fix hexes against their stated contexts — #1D637A on
    #F8FBFD/#EAF6FB/#DDF2F7 (expect ~6.48/6.12/5.81), #174E61 on #F8FBFD
    (~8.79), #96520E on #EAF6FB (~5.43), #74909F on #F8FBFD (~3.24, >=3.0),
    #62809A on #0D1B2A (~4.20), #E29089 on #1F3049 (~5.44) — and (b) decide
    the four OPEN dark feedback backgrounds: start from candidates
    success-bg #142B22, warning-bg #2B2314, error-bg #2E1B1E, info-bg #11293A
    and verify {kind}-text >= 4.5:1 on each ({success #8BB77F, warning
    #E0A955, error #E29089, info #A6EAF2}); adjust the tint darker if any pair
    fails (Claude's discretion per 86-CONTEXT). Keep the script and its full
    output — Task 2 extends it into the full matrix.

    Then author brandbook-fable/tokens.json (first file in the new folder),
    DTCG-2025.10 style per TOKENS-A11Y.md "Token Architecture Recommendation",
    exactly two tiers:
    - Property names $value / $type / $description; group-level "$type":
      "color" on color groups; NO $schema line; hex-string $value (document
      this deviation in a meta.notes string: hex-string color values with an
      upgrade path to DTCG color objects if tooling ever consumes the file —
      phrased reader-facing, no process vocabulary).
    - Tier 1 `palette.*`: brand-named modifiers, NOT numeric ramps (P-16).
      Families: ink (#0D1B2A) + ink-raised #152538 + ink-overlay #1F3049 +
      ink-sunken #0A1521 + ink-selected #1B3E55; glass #277B96 + glass-deep
      #1D637A + glass-deepest #174E61; ice #A6EAF2 + ice-bright #D6F7FB; mist
      #EAF6FB; paper #F8FBFD; white #FFFFFF; slate #5C6B7A + slate-soft
      #74909F + slate-bright #62809A + slate-dim #6B7E8F; pine #166534 +
      pine-bright #8BB77F; amber #A95F10 + amber-deep #96520E + amber-bright
      #E0A955; crimson #B42318 + crimson-bright #E29089; plus the light
      feedback tints (#E8F3EA, #FFF8F0, #FAE7E5), light decorative border
      #C7DCE5, light disabled #9AA8B4, light selected #DDF2F7, and the four
      verified dark feedback tints. Exact modifier words for the tints are
      discretion (descriptive, e.g. pine-tint/pine-shadow) — never numeric.
    - Tier 2: `color.light.*` and `color.dark.*` sibling groups with the
      IDENTICAL 39-role key set from the interfaces block, each $value an
      alias `{palette.x}` referencing tier 1; plus non-color groups `font`
      (display/ui/mono stacks), `text` (type scale: display 44px, h1 36px,
      h2 30px, h3 24px, body 16px, small 14px, code 14px), `space`, `radius`,
      `focus`. No component tier, no composite tokens, no state.* group
      divorced from roles.
    - Every $description describes USAGE ("primary action background", "link
      text on any light surface including tinted callouts"), never provenance.
      Forbidden anywhere in either file (P-15 / FOUND-04): phase, plan,
      milestone, codex, draft, TBD, GSD, REQ-, tournament, baseline, option-.

    Then author brandbook-fable/tokens.css, hand-synced 1:1 to tokens.json —
    genuinely usable CSS (Phase 88 gallery and Phase 89 landing page import it
    directly), flat semantic custom properties with literal hex values (the
    palette tier lives only in tokens.json):
    - `:root, [data-theme="light"]` block: `color-scheme: light;` + all 39
      --mg-color-* light values + ALL theme-invariant tokens (--mg-font-*,
      --mg-text-*, --mg-space-*, --mg-radius-*, --mg-focus-ring-*).
    - `[data-theme="dark"]` block: `color-scheme: dark;` + all 39
      --mg-color-* dark values (complete parity — the codex reassigned only
      18 of ~70 roles; the fable dark set reassigns every color role).
    - `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"])
      { ... } }` block mirroring the dark block exactly, byte-for-byte
      identical declarations (P-11 three-state pattern: auto/light/dark).
    - A short reader-facing header comment (what the file is, how the toggle
      works) — same vocabulary ban applies to comments.
    Budgets: tokens.json &lt;= 16 KB, tokens.css &lt;= 10 KB.

    Commit: `feat(86-01): add fable brand tokens (light+dark, DTCG json + css)`
    — commit ONLY the two brandbook-fable/ files.
  </action>
  <acceptance_criteria>
    - `python3 -m json.tool brandbook-fable/tokens.json > /dev/null` exits 0
    - `grep -c '"\$value"' brandbook-fable/tokens.json` >= 100 (two tiers x both themes x 39 roles + palette + non-color groups)
    - All six fix hexes present in tokens.json: `for h in 1D637A 174E61 96520E 74909F 62809A E29089; do grep -qi "#$h" brandbook-fable/tokens.json || echo "MISSING $h"; done` prints nothing
    - Dark ramp present: `for h in 0D1B2A 152538 1F3049 0A1521 1B3E55; do grep -qi "#$h" brandbook-fable/tokens.json || echo "MISSING $h"; done` prints nothing
    - No legacy dialect: `grep -cE '"\$schema"|"value":' brandbook-fable/tokens.json` returns 0 (grep exits 1)
    - `grep -oE -- '--mg-color-[a-z0-9-]+:' brandbook-fable/tokens.css | sort | uniq -c | awk '$1 != 3 {bad=1; print} END {exit bad}'` exits 0 (every color property declared exactly 3 times: light, dark attr block, dark media block)
    - `grep -oE -- '--mg-color-[a-z0-9-]+:' brandbook-fable/tokens.css | sort -u | wc -l` returns exactly 39
    - `grep -c 'color-scheme' brandbook-fable/tokens.css` >= 2 and `grep -c 'prefers-color-scheme: dark' brandbook-fable/tokens.css` == 1
    - Honest stacks: `grep -q 'system-ui' brandbook-fable/tokens.css` and `grep -q 'ui-monospace' brandbook-fable/tokens.css` both succeed; `grep -ciE 'font-family[^;]*Inter(["'"'"' ]*,[ ]*(sans-serif)?;)' brandbook-fable/tokens.css` finds no Inter-then-bare-sans-serif stack; `grep -c '@font-face' brandbook-fable/tokens.css` == 0
    - `python3 -c "s=open('brandbook-fable/tokens.css').read(); assert s.count('{')==s.count('}')"` exits 0
    - `! grep -riE '(phase|plan|milestone|codex|draft|tbd)' brandbook-fable/` exits 0
    - Size: tokens.json &lt;= 16384 bytes, tokens.css &lt;= 10240 bytes (`stat -f%z`)
    - /tmp/mg_contrast.py exists in /tmp only: `git status --porcelain | grep -i contrast` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>python3 -m json.tool brandbook-fable/tokens.json > /dev/null && grep -oE -- '--mg-color-[a-z0-9-]+:' brandbook-fable/tokens.css | sort | uniq -c | awk '$1 != 3 {bad=1} END {exit bad}' && ! grep -riE '(phase|plan|milestone|codex|draft|tbd)' brandbook-fable/</automated>
  </verify>
  <done>brandbook-fable/ exists containing exactly tokens.json + tokens.css; both parse; 39 color roles with full dark parity across three CSS blocks; six fix hexes + dark ramp + verified dark feedback bgs locked in; zero process vocabulary; committed as feat(86-01)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compute the full WCAG matrix and write the foundations decision record</name>
  <files>.planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md</files>
  <read_first>
    brandbook-fable/tokens.json (the final shipped hexes — matrix MUST be computed from these, not from research tables),
    .planning/research/v1.9-brandbook-fable/TOKENS-A11Y.md (expected ratios to cross-check against),
    .planning/phases/86-foundations-palette-type-voice-tokens/86-CONTEXT.md (decisions sections),
    prompts/mailglass-brand-book.md (palette/type/voice seeds being ruled on)
  </read_first>
  <action>
    Extend /tmp/mg_contrast.py to emit the FULL pair matrix from the final
    token values in brandbook-fable/tokens.json (parse the JSON or paste the
    hexes — either way the inputs are the shipped values). For EACH theme,
    compute every text-role x surface-role pair:
    - text roles: text, text-muted, text-disabled, accent-text, link,
      link-hover, link-active, and the four {kind}-text values
    - surface roles: background, surface, surface-raised, surface-overlay,
      surface-sunken, surface-selected, and the four {kind}-bg values
    - plus the on-solid pairs: each {kind}-on-solid on {kind}-solid, and
      text-inverse on accent/solid fills
    For each pair emit: ratio (2 decimals), AA normal (>=4.5), AAA normal
    (>=7.0), AA large (>=3.0), AAA large (>=4.5). Also emit the non-text
    >=3.0 checks for border-input and focus-ring against their surfaces.
    Sanity cross-checks the script output MUST reproduce: Ink/Paper 16.74,
    Glass/white 4.82, Glass/Mist 4.37 (FAIL — this is why accent-text routes
    through glass-deep), glass-deep/Mist 6.12, codex-dark-error #D47368 on
    #1F3049 4.09 (FAIL — why #E29089 replaced it). If any computed value
    disagrees with TOKENS-A11Y.md by more than rounding, STOP and reconcile
    before writing the record.

    Then write 86-foundations-decisions.md in the phase directory with:
    1. **Evolve-vs-keep records (FOUND-01)** — one row per seed decision:
       KEEP Ink/Glass/Ice/Mist/Paper/Slate/Pine/Crimson (with their passing
       ratios); EVOLVE glass-deep #1D637A, glass-deepest #174E61, amber-deep
       #96520E, slate-soft #74909F, slate-bright #62809A, crimson-bright
       #E29089 (each with the failing seed ratio it fixes AND the computed
       passing ratios); ADOPT the codex dark ramp #0D1B2A/#152538/#1F3049
       (+#0A1521 sunken, #1B3E55 selected) with verification ratios; DECIDED
       the four dark feedback backgrounds with their {kind}-text ratios.
       Typography: KEEP Inter Tight/Inter/IBM Plex Mono as named preferences
       with the honest-fallback statement — record explicitly that Inter is
       preinstalled on no major OS and most viewers see the system fallback;
       record the exact type scale chosen. Voice: KEEP thoughtful-maintainer
       seeds (calm/exact/confident/warm); full voice system ships in the
       book and copy library, not here.
    2. **Computed contrast matrix (FOUND-03)** — the full pair tables for
       both themes (markdown tables, structured: foreground role, hex,
       background role, hex, ratio, AA/AAA normal, AA/AAA large, usage rule).
       Every pair gets a one-line usage rule (e.g. "Glass #277B96: icons,
       strong borders, focus rings, large text on Paper/white, solid fills
       with white text — never normal-size text on Mist/selected/info
       surfaces; use accent-text"; "text-muted: AA only — use text where AAA
       matters"; "text-disabled: exempt under SC 1.4.3, never color alone").
       State the method (WCAG relative-luminance formula, sRGB 8-bit, 0.04045
       threshold) so Phase 88/90 can recompute identically.
    3. A short note that the matrix data is the Phase 88 rendering input
       (the book recomputes from tokens at runtime) and that the generator
       script was a throwaway run from /tmp, not committed.
    This file lives in .planning/ — process vocabulary is allowed HERE
    (decision records are planning artifacts), but its data must match the
    shipped token hexes exactly.

    Commit: `docs(86-01): record foundations decisions + computed contrast matrix`
  </action>
  <acceptance_criteria>
    - 86-foundations-decisions.md exists in .planning/phases/86-foundations-palette-type-voice-tokens/
    - Contains the anchor ratios as literal strings: `grep -c '4\.82' ...decisions.md` >= 1 (Glass on white), `grep -c '4\.37' ...decisions.md` >= 1 (Glass on Mist FAIL), `grep -c '16\.74' ...decisions.md` >= 1 (Ink on Paper)
    - All six fix hexes appear with ratios: `for h in 1D637A 174E61 96520E 74909F 62809A E29089; do grep -qi "$h" ...decisions.md || echo "MISSING $h"; done` prints nothing
    - The dark feedback backgrounds appear with computed {kind}-text ratios >= 4.5 stated per pair
    - Both themes have a matrix table: `grep -ci 'light theme' ...decisions.md` >= 1 and `grep -ci 'dark theme' ...decisions.md` >= 1; total matrix rows >= 50 across both themes (10+ text roles x 10 surfaces per theme, minus nonsensical pairs is fine — count table rows with `grep -cE '^\|.*#[0-9A-Fa-f]{6}' ...decisions.md` >= 50)
    - Every hex in the matrix exists in brandbook-fable/tokens.json: `comm -23 <(grep -oiE '#[0-9A-F]{6}' ...decisions.md | tr 'a-f' 'A-F' | sort -u) <(grep -oiE '#[0-9A-F]{6}' brandbook-fable/tokens.json | tr 'a-f' 'A-F' | sort -u)` outputs at most the documented rejected codex value #D47368 (cited as evidence) — no other strays
    - Inter fallback honesty recorded: `grep -qi 'preinstalled' ...decisions.md` (or equivalent statement matching `grep -qiE 'system (font|fallback)'`)
    - `git status --porcelain` shows no /tmp script or generated scratch files staged
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md && grep -q '4\.82' .planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md && [ "$(grep -cE '^\|.*#[0-9A-Fa-f]{6}' .planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md)" -ge 50 ]</automated>
  </verify>
  <done>Decision record committed with complete evolve-vs-keep coverage (palette, dark ramp, dark feedback bgs, typography, voice) and a script-computed both-theme matrix whose hexes match tokens.json exactly, AA/AAA verdicts and usage rule per pair</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run the consolidated in-phase hygiene + completeness gate</name>
  <files>(none if green — fix tokens.json/tokens.css/decision record in place if any gate fails, then re-run)</files>
  <read_first>
    brandbook-fable/tokens.json, brandbook-fable/tokens.css,
    .planning/phases/86-foundations-palette-type-voice-tokens/86-foundations-decisions.md
  </read_first>
  <action>
    Run every gate below from the repo root as a single final pass (these are
    the Phase 86 subset of the Phase 90 gate — failures are fixed NOW, not
    deferred). Write a small throwaway checker at /tmp/mg_gate.sh if
    convenient (not committed). Gates:
    1. JSON parses: `python3 -m json.tool brandbook-fable/tokens.json > /dev/null`
    2. CSS brace balance: `python3 -c "s=open('brandbook-fable/tokens.css').read(); assert s.count('{')==s.count('}')"`
    3. Process-vocabulary denylist over the SHIPPED folder only:
       `! grep -riE '(phase|plan|milestone|codex|draft|tbd|gsd|req-|tournament|option-)' brandbook-fable/`
    4. Dark role-set completeness: every --mg-color-* property appears exactly
       3 times (light block, dark attr block, dark media block) AND the dark
       attr block and dark media block contain identical declaration sets —
       extract both blocks with a python3 one-liner from /tmp, normalize
       whitespace, diff; empty diff required.
    5. tokens.json theme parity: python3 one-liner loads the JSON and asserts
       set(color.light keys) == set(color.dark keys) recursively at the role
       level (set-difference must be empty in both directions).
    6. Size budgets: tokens.json &lt;= 16384 bytes, tokens.css &lt;= 10240
       bytes (`stat -f%z` on macOS).
    7. Hex sync: every unique hex in tokens.css exists in tokens.json (comm
       -23 of the sorted-unique uppercased sets is empty).
    8. Scope check: `git status --porcelain` shows changes ONLY under
       brandbook-fable/ and .planning/ relative to the phase's starting
       point; `brandbook/` (frozen baseline) untouched —
       `git diff --name-only HEAD~2 -- brandbook/` empty. Note: the working
       tree has pre-existing user WIP in brandbook/ and .planning/ — the
       check is that THIS phase's commits touched nothing outside
       brandbook-fable/ + the phase planning dir + ROADMAP/STATE.
    If every gate passes and no file changed, no commit is needed for this
    task. If a gate forced fixes, recommit with the appropriate prefix
    (`feat(86-01)` for token files, `docs(86-01)` for the record) and re-run
    all gates until green.
  </action>
  <acceptance_criteria>
    - All 8 gates exit 0 in a single consecutive run (no stale-file passes)
    - Gate 4's block diff is empty (dark attr block == dark media block, normalized)
    - Gate 5 prints empty set-difference both directions for color.light vs color.dark role keys
    - Gate 7 comm output is empty
    - No throwaway script (/tmp/mg_contrast.py, /tmp/mg_gate.sh) is tracked: `git ls-files | grep -iE 'mg_(contrast|gate)'` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>python3 -m json.tool brandbook-fable/tokens.json > /dev/null && python3 -c "s=open('brandbook-fable/tokens.css').read(); assert s.count('{')==s.count('}')" && ! grep -riE '(phase|plan|milestone|codex|draft|tbd|gsd|req-|tournament|option-)' brandbook-fable/ && python3 -c "
import json,sys
d=json.load(open('brandbook-fable/tokens.json'))
l=set(d['color']['light'].keys())-{'\$type','\$description'}
k=set(d['color']['dark'].keys())-{'\$type','\$description'}
sys.exit(0 if l==k and len(l)>0 else 1)" && [ "$(stat -f%z brandbook-fable/tokens.json)" -le 16384 ] && [ "$(stat -f%z brandbook-fable/tokens.css)" -le 10240 ]</automated>
  </verify>
  <done>Every in-phase gate green in one consecutive run; brandbook-fable/ contains exactly two clean, parity-complete, vocabulary-clean token files within budget; nothing outside brandbook-fable/ + .planning/ touched by this phase's commits</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase authors static JSON/CSS/MD brand artifacts; no code paths, no untrusted input, no package installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Information disclosure | tokens.json/tokens.css descriptions | mitigate | FOUND-04 denylist grep gate (Task 1 + Task 3) keeps internal planning vocabulary out of shipped artifacts |
| T-86-02 | Tampering | frozen brandbook/ baseline | mitigate | Task 3 gate 8 asserts phase commits touch only brandbook-fable/ + .planning/ |
</threat_model>

<verification>
- `python3 -m json.tool brandbook-fable/tokens.json` exits 0
- 39 unique `--mg-color-*` properties, each declared exactly 3x in tokens.css; dark attr block == dark media block
- `color.light` / `color.dark` key sets identical in tokens.json
- Six fix hexes (#1D637A #174E61 #96520E #74909F #62809A #E29089) and dark ramp (#0D1B2A #152538 #1F3049 #0A1521 #1B3E55) present in tokens.json
- Decision record contains both-theme computed matrix (>= 50 hex-bearing table rows), anchor ratios 16.74 / 4.82 / 4.37, and a usage rule per pair
- `! grep -riE '(phase|plan|milestone|codex|draft|tbd)' brandbook-fable/`
- tokens.json <= 16 KB, tokens.css <= 10 KB
- No throwaway script committed anywhere
</verification>

<success_criteria>
Maps 1:1 to the four ROADMAP Phase 86 success criteria:
1. (FOUND-01) Decision record has explicit evolve-vs-keep rows with contrast math for every seed palette/typography decision, the six computed fix hexes, the adopted codex dark ramp, and the decided dark feedback backgrounds.
2. (FOUND-02) tokens.json (DTCG-2025.10 style, two tiers) + tokens.css define palette → semantic roles → interaction states → feedback states + type scale, space, radius, focus — complete for light AND dark (full parity, not the codex's 18-of-70).
3. (FOUND-03) The matrix covers every text-role/surface-role pair in both themes with computed AA/AAA verdicts and a usage rule per pair; Glass stays AA-valid on white at 4.82; tinted-surface accent text routes through glass-deep.
4. (FOUND-04) Zero process vocabulary anywhere under brandbook-fable/, gated by grep in-phase.
</success_criteria>

<output>
Create `.planning/phases/86-foundations-palette-type-voice-tokens/86-01-SUMMARY.md` when done.
</output>
