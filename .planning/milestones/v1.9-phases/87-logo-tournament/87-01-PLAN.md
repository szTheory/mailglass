---
phase: 87-logo-tournament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/87-logo-tournament/tournament/options/option-1.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-2.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-3.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-4.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-5.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-6.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-7.svg
  - .planning/phases/87-logo-tournament/tournament/options/option-8.svg
  - .planning/phases/87-logo-tournament/tournament/round-1.html
  - .planning/phases/87-logo-tournament/87-pre-flight.md
  - .planning/phases/87-logo-tournament/87-01-CHECKPOINT.md
autonomous: true
requirements: [LOGO-05, LOGO-06, LOGO-07]

must_haves:
  truths:
    - "Exactly 8 option SVGs exist, 2 per axis (A integrated typemark, B mark+wordmark lockup, C monogram/glyph, D negative space), each generated independently from the brand essence"
    - "Every presented option passed the 14-constraint pre-flight screen (fix-or-replace, never shown failing) recorded in 87-pre-flight.md as 112 PASS rows"
    - "round-1.html opens from file:// with zero external network references and shows a fixed 6-cell evidence strip per option (large light, large dark, 32px+16px favicon row, mono/currentColor, fake GitHub README context, ≤1-line rationale)"
    - "Each option was rendered in a real browser and visually audited at full size, 32px, 16px, mono, and dark BEFORE presentation; craft failures were fixed or the option replaced"
    - "87-01-CHECKPOINT.md records the hard pause (awaiting-maintainer-selection), the valid resume signals, the full-rejection replacement protocol, and the two-rejection circuit breaker; nothing auto-advances"
  artifacts:
    - path: ".planning/phases/87-logo-tournament/tournament/options/option-1.svg"
      provides: "Axis A option (and siblings option-2..8.svg per axis assignment)"
      contains: "<title"
    - path: ".planning/phases/87-logo-tournament/tournament/round-1.html"
      provides: "Self-contained rendered-evidence gallery, light + dark sections, fable tokens"
      min_lines: 200
    - path: ".planning/phases/87-logo-tournament/87-pre-flight.md"
      provides: "Constraint screen: one row per option x constraint (8 x C-01..C-14 = 112 rows), all PASS"
    - path: ".planning/phases/87-logo-tournament/87-01-CHECKPOINT.md"
      provides: "Hard-pause record with resume signals and circuit breaker"
      contains: "awaiting-maintainer-selection"
  key_links:
    - from: ".planning/phases/87-logo-tournament/tournament/round-1.html"
      to: "tournament/options/option-*.svg"
      via: "inlined SVG markup with per-option o{N}- ID prefixes (required for the mono/currentColor cells)"
      pattern: "o[1-8]-"
    - from: ".planning/phases/87-logo-tournament/tournament/round-1.html"
      to: "brandbook-fable/tokens.css"
      via: "tokens.css contents inlined into a <style> block (no <link>, no relative fetch)"
      pattern: "--mg-color-"
    - from: ".planning/phases/87-logo-tournament/87-01-CHECKPOINT.md"
      to: "tournament/round-1.html"
      via: "gallery path stated in the pause record"
      pattern: "round-1\\.html"
---

<objective>
Author the round-1 field of the v1.9 logo tournament: 8 hand-drawn outlined-path
SVG options (2 per axis A-D), pre-screened against the hard-constraint checklist
and the 8-point craft self-audit, presented in a self-contained rendered-evidence
gallery that has itself been browser-rendered and visually audited before the
maintainer ever sees it. The plan ends by writing 87-01-CHECKPOINT.md and
STOPPING the phase at the milestone's only HARD PAUSE.

Purpose: LOGO-05 (8 options, 2 per axis, pre-screened), LOGO-06 (every presented
option passes the hard constraints), LOGO-07 presentation half (rendered-evidence
gallery + hard pause + recorded protocol). This is the anti-thrash core of the
phase: an option that fails any check is fixed or replaced, never shown.

Output: option-1..8.svg, round-1.html, 87-pre-flight.md, 87-01-CHECKPOINT.md —
all under `.planning/phases/87-logo-tournament/` (tournament exhaust NEVER goes
in `brandbook-fable/`).

NOTE ON AUTONOMY: this plan runs fully autonomously to completion — but the
PHASE pauses after it. Plan 87-02 is blocked until the maintainer's selection is
recorded in 87-01-CHECKPOINT.md. Do not start 87-02. Do not infer a default pick.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/87-logo-tournament/87-CONTEXT.md
@.planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md
@.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md
@.planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md
@brandbook-fable/tokens.css

Negative evidence (read once, do not imitate any shape in it):
@.planning/milestones/v1.8-phases/82-logo-and-svg-asset-system/82-02-CHECKPOINT.md

<interfaces>
Token names the gallery uses (from brandbook-fable/tokens.css, Phase 86 — inline
the whole file into the gallery's <style>; these are the load-bearing roles):

- Light: `--mg-color-background: #F8FBFD`, `--mg-color-surface: #FFFFFF`,
  `--mg-color-text: #0D1B2A`, `--mg-color-text-muted: #5C6B7A`,
  `--mg-color-accent: #277B96`, `--mg-color-accent-text: #1D637A`,
  `--mg-color-border: #C7DCE5`, `--mg-color-surface-sunken: #EAF6FB`.
- Dark values are reassigned under `[data-theme="dark"]`; pages follow OS
  preference by default and can force a theme by setting `data-theme` on the
  root element. The gallery's dark section forces `data-theme="dark"` on a
  wrapper or uses a second themed region — both large-dark cells and a whole
  dark section must exist.
- Mark color usage per option (Claude's discretion per CONTEXT): Ink #0D1B2A,
  Glass #277B96, glass-deep #1D637A, Ice #A6EAF2 — and every option must still
  work single-color (the mono cell proves it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author options 1-4 (Axis A integrated typemarks x2, Axis B mark+wordmark lockups x2)</name>
  <files>.planning/phases/87-logo-tournament/tournament/options/option-1.svg, .planning/phases/87-logo-tournament/tournament/options/option-2.svg, .planning/phases/87-logo-tournament/tournament/options/option-3.svg, .planning/phases/87-logo-tournament/tournament/options/option-4.svg</files>
  <read_first>.planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md (the whole file: precedents, l-g-l cluster, grid + optical-correction table, per-glyph recipes, failure modes), .planning/phases/87-logo-tournament/87-CONTEXT.md, .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md (Banned Motif Families + Non-Negotiables), .planning/milestones/v1.8-phases/82-logo-and-svg-asset-system/82-02-CHECKPOINT.md (rejection history — negative evidence only), brandbook-fable/tokens.css</read_first>
  <action>
    Create `.planning/phases/87-logo-tournament/tournament/options/` and author
    options 1-4 as standalone SVGs. Each option is generated INDEPENDENTLY from
    the brand essence "mail you can see through" — not derived from a shared
    sketch, and not derived from each other.

    OPTIONS 1-2 — Axis A, integrated custom typemark (per CONTEXT D-axis-A):
    the full word "mailglass" with exactly ONE earned modification worked into
    the letterforms (Braun/FedEx dosage: one trick, every other glyph flawless,
    word fully legible to viewers who never notice it). Prime site per
    LOGO-CRAFT is the l-g-l cluster at the mail|glass seam — candidate plays
    (executor's pick, two DIFFERENT plays across the two options):
    counter-as-lens g (Goodwill pattern), negative-space pane in the l-g
    channel (FedEx pattern), baseline-breaking geometrized g descender with an
    open aperture, or a single refraction shear of ONE vertical stem at the
    seam (Gillette pattern: shear position not angle, hairline gap >= 0.35 x
    stem width, never interrupt a curve). The ss pair is the riskiest site —
    avoid unless the result is unambiguous. NEVER touch the i dot.

    OPTIONS 3-4 — Axis B, mark + tight wordmark lockup: a NEW mark concept
    (NOT codex's envelope+pane, NOT any v1.8 A-R or 07r shape — check the
    82-02 rejection history and avoid all of it). The mark must physically
    break a boundary: overshoot cap height, cross the baseline, or interlock
    with the first letter. Lockup gap must satisfy BOTH bounds: <= 0.4 x mark
    width (CONTEXT) and 0.5-0.75 x wordmark x-height = 50-75 grid units
    (LOGO-CRAFT) — when they conflict the smaller value binds. Vertically
    align the mark's optical center to the x-height midline (y = 90), not the
    ascender midline. Mark must also pass a single-color test and must not
    resemble Linear's slats, Glassdoor, Gmail, or any envelope-category mark.

    GRID + CRAFT DISCIPLINE (applies to every wordmark in every option):
    integer grid with x-height = 100 units, ascender top y = 0, x-height line
    y = 40, baseline y = 140, descender floor y = 180; round-shape overshoot
    3 units past baseline/x-height (zero overshoot on flat i/l/m tops); stem
    weight S = 22 for ALL vertical stems (numerically identical — subtract
    coordinates to prove it); horizontals 0.90-0.94 x S (20-21 units); round
    strokes at N/S extremes 0.88-0.92 x S; m shoulders thin to ~0.85 x S where
    they meet stems; joint thinning 12-18% over the last ~1.5 S before every
    branch join (m shoulders, a bowl-to-stem, g bowl-to-stem). Quarter-circle
    bezier handle constant 0.5523 x r (one constant everywhere; 0.57-0.60
    allowed if used uniformly). Glyph skeletons: double-story a, single-story
    g, grotesque horizontal/vertical terminal cuts, i dot a perfect circle of
    diameter 1.1-1.2 x S sitting one stem-width above the stem — drawn once,
    never modified, never recolored. Per-glyph advance widths from LOGO-CRAFT:
    m~150, a~92, i~22+sidebearings, l~22+sidebearings, g~96, s~84; straight-
    stem gaps ~30-32 units, round-to-straight ~10% tighter, round-to-round
    ~18% tighter; a-bowl counter >= 26 units; g hook aperture >= 0.6 x S;
    s top counter ~90% of bottom counter, spine inflection ON an on-curve node.

    SVG hygiene per option: filled outline paths only (draft strokes deleted)
    — live text may be used ONLY as a throwaway proportion reference and must
    never appear in the file; one <path> per glyph with data-glyph attribute
    and an XML comment recording its advance width; on-curve points at curve
    extrema, handles horizontal/vertical there; coordinates integer or .5, two
    decimals max (grep the d attributes for 3+ decimals); duplicate letters
    (l, a, s) ship as duplicated literal paths, not <use>; counters hollow via
    winding/fill-rule (render to confirm, don't trust the math); no gradients,
    filters, masks, or <script>; viewBox minimal (~0 0 980 180 for a bare
    wordmark, adjusted per option); root element carries role="img", a
    <title>, and a <desc>; EVERY id in option-N.svg is prefixed `oN-` (e.g.
    o1-title) so the gallery can inline all 8 without collisions. No
    rectangular or rounded-rect plate behind anything — options sit on the
    raw page background.

    After drawing each option, run the 8-point AI-letterform self-audit from
    LOGO-CRAFT and fix what it finds BEFORE moving on: (1) curve quality —
    zoom mentally to extrema placement, no lumps; (2) stroke-weight
    consistency — measure every stem numerically; (3) joint handling — no
    clots at squint/10% size; (4) spacing rhythm — word reads as one texture,
    no dark l-g collision, no a-s hole; (5) baseline/x-height discipline —
    all letters on y=140/y=40 exactly; (6) overshoot present on a/g/s
    extremes (137/143-style crossings), absent on flats; (7) counter shapes —
    hollow, >= 26 units, both s counters top-smaller; (8) optical centering of
    marks and modifications. Duplicated glyphs must be geometrically identical
    unless asymmetry IS the concept.

    Commit as feat(87-01) when the four options pass self-audit.
  </action>
  <verify>
    <automated>xmllint --noout .planning/phases/87-logo-tournament/tournament/options/option-1.svg .planning/phases/87-logo-tournament/tournament/options/option-2.svg .planning/phases/87-logo-tournament/tournament/options/option-3.svg .planning/phases/87-logo-tournament/tournament/options/option-4.svg && grep -L '<title' .planning/phases/87-logo-tournament/tournament/options/option-{1,2,3,4}.svg | wc -l | grep -qx '0' && grep -l -e '<text' -e 'font-family' .planning/phases/87-logo-tournament/tournament/options/option-{1,2,3,4}.svg | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - option-1.svg .. option-4.svg exist; xmllint exits 0 on all four
    - Zero `<text` and zero `font-family` matches in any of the four files
    - Every file has <title> and <desc> and role="img" and a viewBox
    - All ids prefixed oN-; `grep -ho 'id="[^"]*"' option-{1,2,3,4}.svg | sort | uniq -d` outputs nothing
    - No coordinate with 3+ decimals: `grep -E '\.[0-9]{3,}' option-{1,2,3,4}.svg` outputs nothing
    - Options 1-2 are full-word typemarks with one modification each (different plays); options 3-4 are mark+wordmark lockups with new marks and a physical boundary-break
  </acceptance_criteria>
  <done>Four axis-A/B options exist as valid, path-only, self-audited SVGs with unique prefixed IDs and accessibility elements, committed feat(87-01).</done>
</task>

<task type="auto">
  <name>Task 2: Author options 5-8 (Axis C monogram/glyph x2, Axis D negative-space x2)</name>
  <files>.planning/phases/87-logo-tournament/tournament/options/option-5.svg, .planning/phases/87-logo-tournament/tournament/options/option-6.svg, .planning/phases/87-logo-tournament/tournament/options/option-7.svg, .planning/phases/87-logo-tournament/tournament/options/option-8.svg</files>
  <read_first>.planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md (Small-Size Survival section especially), .planning/phases/87-logo-tournament/87-CONTEXT.md, .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md (Banned Motif Families), brandbook-fable/tokens.css</read_first>
  <action>
    Author options 5-8 in the same options/ directory, same grid, same SVG
    hygiene, same self-audit, and same id-prefix rule (o5- .. o8-) as Task 1.

    OPTIONS 5-6 — Axis C, monogram/glyph: an `mg` ligature or single-glyph
    construction that is 16px-honest BY DESIGN — the mark itself is <= 3
    distinct shapes/strokes when redrawn at favicon scale (Vercel-triangle
    lesson; an interlocked ss is 4+ curve events at 16px and therefore not
    viable). Strongest single-glyph candidates per LOGO-CRAFT: a lens-g (bowl
    ring + descender hook = 2 shapes) or an m built from three vertical
    strokes with one pane-like crossing element. These options may present as
    mark-only or mark-plus-small-wordmark; if a wordmark appears, all Task 1
    craft rules apply to it. The mark must still break an implied boundary
    (descender, overshoot, escaping stroke) — break the mark's implied frame,
    NOT the viewBox (keep >= 1px internal margin at favicon scale so browser-
    tab cropping never clips it).

    OPTIONS 7-8 — Axis D, negative space: the see-through idea carried by
    ABSENCE — a void pane, a shared figure/ground cut, a channel that reads as
    a pane edge. FedEx rule is binding: the mark must be 100% functional for
    viewers who never see the hidden figure. Negative-space motifs need
    surrounding mass — keep weight SemiBold+ or the void is just spacing. No
    literal broken glass; glass means clarity, never damage.

    Both axes obey every hard constraint from Task 1: no plates, no banned
    motifs (glassmorphism, gradients-in-marks, paper planes, mailboxes, send
    arrows, chat bubbles, mascots, node-graphs, hexagons, orbit rings), no
    v1.8 A-R/07r revival, no Linear/Glassdoor/Gmail resemblance, i dot
    untouchable, single-color survivable.

    Run the 8-point self-audit on each; fix before proceeding. Commit as
    feat(87-01).
  </action>
  <verify>
    <automated>xmllint --noout .planning/phases/87-logo-tournament/tournament/options/option-5.svg .planning/phases/87-logo-tournament/tournament/options/option-6.svg .planning/phases/87-logo-tournament/tournament/options/option-7.svg .planning/phases/87-logo-tournament/tournament/options/option-8.svg && ls .planning/phases/87-logo-tournament/tournament/options/option-*.svg | wc -l | grep -qx '8' && grep -l -e '<text' -e 'font-family' .planning/phases/87-logo-tournament/tournament/options/option-*.svg | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 8 files match tournament/options/option-*.svg; xmllint exits 0 on all
    - Zero `<text`/`font-family` across all 8; ids globally unique across all 8 files (`grep -ho 'id="[^"]*"' option-*.svg | sort | uniq -d` empty)
    - Options 5-6 marks decompose to <= 3 shapes at favicon scale; options 7-8 carry the motif in negative space and read correctly without it
    - Every option has title/desc/role="img"/viewBox and passed the 8-point self-audit
  </acceptance_criteria>
  <done>The full 8-option field exists (2 per axis), all valid path-only SVGs, all self-audited, committed feat(87-01).</done>
</task>

<task type="auto">
  <name>Task 3: Pre-flight screen, evidence gallery, browser visual audit, hard-pause checkpoint</name>
  <files>.planning/phases/87-logo-tournament/87-pre-flight.md, .planning/phases/87-logo-tournament/tournament/round-1.html, .planning/phases/87-logo-tournament/87-01-CHECKPOINT.md</files>
  <read_first>.planning/phases/87-logo-tournament/87-CONTEXT.md (Presentation + Self-verification sections), .planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md (Craft Failure Checklist + test matrix), .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md (Compositional Stance), brandbook-fable/tokens.css (inline it whole)</read_first>
  <action>
    THREE sub-deliverables, in order. An option that fails ANY check below is
    fixed or replaced (regenerate within its axis, re-screen) — never shown
    failing.

    (1) PRE-FLIGHT SCREEN — write 87-pre-flight.md as a markdown table with
    ONE ROW PER option x constraint pair: 8 options x 14 constraints = 112
    rows, columns: Option | Constraint | Verdict | Evidence. Constraint IDs:
    C-01 no background plate — no <rect> (or rounded-rect path) whose width
    AND height each cover > 80% of the viewBox sitting behind the mark/word
    group; for every <rect> found in any option, record its numeric w/h vs
    viewBox in the Evidence cell. C-02 visible boundary-break at presentation
    size. C-03 tight lockup — gap <= 0.75 x x-height, and for axis B also
    <= 0.4 x mark width (record measured units). C-04 no subtitle/slogan.
    C-05 legible at 16px (verdict deferred to the screenshot audit below —
    fill after rendering). C-06 legible at 32px (same). C-07 works
    mono/currentColor (same). C-08 works on dark (same). C-09 i dot
    untouched (perfect circle, standard size/position/color). C-10 no banned
    motif family (glassmorphism, gradients-in-mark, paper plane, mailbox,
    send arrow, chat bubble, mascot, node-graph/hexagon/orbit, literal broken
    glass, envelope-with-swoosh). C-11 no v1.8 revival (A-R shapes, 07r
    envelope+pane). C-12 SVG validity (xmllint pass, viewBox, role="img",
    title+desc, oN- prefixed globally-unique ids). C-13 paths only (no
    <text>, no font-family, no leftover draft strokes). C-14 no resemblance
    to Linear slats / Glassdoor / Gmail / envelope-category marks.

    (2) EVIDENCE GALLERY — write tournament/round-1.html. Self-contained:
    inline the FULL contents of brandbook-fable/tokens.css into a <style>
    block (no <link>, no @import, no webfonts, no fetch); inline each
    option's SVG markup directly into the page (required so the mono cells
    can drive fill via currentColor; the oN- id prefixes prevent collisions).
    The page itself is a brand artifact: calm, precise, doc-like,
    left-aligned/ragged-right per the brief's compositional stance, fable
    tokens for all colors, no plates behind any mark, no hype. Options are
    labeled 1-8, neutral and unranked, each with a fixed evidence strip of
    EXACTLY these six cells: (a) large render on light background; (b) the
    same large render on dark (Ink-family token background); (c) actual-size
    32px + 16px renders presented in a fake browser-tab/favicon row; (d)
    mono/currentColor render (single color, prove the single-color test);
    (e) in-context fake GitHub README header (light card mimicking a repo
    README top: option lockup + fake repo title/description text styled with
    system fonts — page chrome may use font stacks; the OPTIONS may not);
    (f) one line of rationale, hard-capped at one sentence. Include a
    whole-page dark section (force data-theme="dark" on a wrapper) so the
    gallery demonstrates both themes. System font stacks only.

    (3) BROWSER VISUAL AUDIT (anti-thrash, BEFORE the pause) — render
    round-1.html in a real browser via Playwright (or the locally available
    agent-browser tooling; install NOTHING new), writing screenshots to the
    gitignored repo tmp dir `tmp/87-logo-tournament/round-1/`: one full-page
    light, one full-page dark, and one per-option close-up of each evidence
    strip (>= 10 PNGs total). READ each screenshot and visually audit every
    option at full size, 32px, 16px, mono, and dark: counters hollow, no
    clotted joints, gla cluster legible, favicon row not fog, boundary-break
    visible, dark cells readable. Fix failures in the option SVG or gallery
    and re-render until clean, then back-fill pre-flight rows C-05..C-08 with
    screenshot evidence paths. Run the mechanical gates (verify command
    below).

    Finally write 87-01-CHECKPOINT.md recording the HARD PAUSE:
    - `status: awaiting-maintainer-selection`, date, gallery path
      (.planning/phases/87-logo-tournament/tournament/round-1.html), and the
      8 options listed by number + axis + their one-line rationale.
    - VALID RESUME SIGNALS (verbatim protocol): (a) "pick N" or "pick N and
      M" — 1 or 2 option numbers, with optional directional notes; recorded
      in the Selection section, unblocks plan 87-02. (b) "reject all" with
      failure notes — triggers ONE replacement round of exactly 8 new options
      drawing on the notes (re-run this plan's flow; increment
      rejection_count to 1; pause again). (c) A second consecutive full-set
      rejection — CIRCUIT BREAKER: STOP, no further generation, escalate to a
      re-brief discussion with the maintainer.
    - Explicit statements: NO AUTO-ADVANCE — plan 87-02 must not start until
      the Selection section is filled in by/with the maintainer; no default
      pick may be inferred; `rejection_count: 0`.
    - An empty `## Selection` section template (picks, rationale, date).

    Commit gallery+SVG fixes as feat(87-01) and the two planning docs as
    docs(87-01). Then STOP: present the gallery path and a one-paragraph
    summary; the phase pauses here.
  </action>
  <verify>
    <automated>xmllint --noout .planning/phases/87-logo-tournament/tournament/options/option-*.svg && grep -l -e '<text' -e 'font-family' .planning/phases/87-logo-tournament/tournament/options/option-*.svg | wc -l | grep -qx '0' && grep -Eo 'https?://[^"< ]+' .planning/phases/87-logo-tournament/tournament/round-1.html | grep -v 'www\.w3\.org' | wc -l | grep -qx '0' && grep -c '^| option-' .planning/phases/87-logo-tournament/87-pre-flight.md | grep -qx '112' && grep '^| option-' .planning/phases/87-logo-tournament/87-pre-flight.md | grep -cw 'PASS' | grep -qx '112' && ls tmp/87-logo-tournament/round-1/*.png | wc -l | awk '{exit ($1>=10)?0:1}' && grep -q 'awaiting-maintainer-selection' .planning/phases/87-logo-tournament/87-01-CHECKPOINT.md && grep -qi 'circuit breaker' .planning/phases/87-logo-tournament/87-01-CHECKPOINT.md</automated>
  </verify>
  <acceptance_criteria>
    - 87-pre-flight.md has exactly 112 option-rows and all 112 carry PASS (verdicts on data rows, not header prose)
    - round-1.html exists, inlines tokens.css (`--mg-color-` present) and all 8 SVGs; the only http(s) substrings are SVG xmlns declarations (www.w3.org) — nothing else matches `https?://`
    - >= 10 screenshots exist under tmp/87-logo-tournament/round-1/ and each was visually read and audited; failures found were fixed and re-rendered
    - `grep -ho 'id="[^"]*"' tournament/options/option-*.svg | sort | uniq -d` outputs nothing
    - 87-01-CHECKPOINT.md contains: status awaiting-maintainer-selection, gallery path, the three resume signals including the one-replacement-round protocol and the two-rejection circuit breaker, the no-auto-advance statement, rejection_count: 0, and an empty Selection section
    - Execution STOPS after this task; 87-02 is not started
  </acceptance_criteria>
  <done>All 8 options pre-screened (112/112 PASS), gallery built and browser-audited, checkpoint file records the hard pause and protocol; phase is paused awaiting maintainer selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| round-1.html -> maintainer's browser | locally-authored HTML is opened from file:// by the maintainer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information disclosure | round-1.html external requests | mitigate | zero-external-ref gate: `grep -Eo 'https?://...' \| grep -v www.w3.org` must be empty; no <link>/@import/fetch |
| T-87-02 | Tampering | option SVGs (script-in-SVG) | mitigate | no <script>, no event attributes; xmllint + grep gates; SVGs are hand-authored, no third-party assets |
| T-87-SC | Tampering | package installs | accept | no package installs in this plan — rendering uses already-installed Playwright/agent-browser tooling only; installing anything new is out of scope |
</threat_model>

<verification>
- `ls .planning/phases/87-logo-tournament/tournament/options/option-*.svg | wc -l` == 8; xmllint exits 0 on all
- No `<text`, `font-family`, or 3+-decimal coordinates in any option SVG; ids globally unique with oN- prefixes
- 87-pre-flight.md: 112 data rows, 112 PASS, every <rect> occurrence justified numerically against the >80%-of-viewBox plate rule
- round-1.html: self-contained (tokens inlined, SVGs inlined), light + dark sections, six-cell evidence strip per option, zero non-xmlns http(s) refs
- Screenshot evidence exists in gitignored tmp/87-logo-tournament/round-1/ and was visually audited before presentation
- 87-01-CHECKPOINT.md records pause state, resume signals, replacement protocol, circuit breaker, no-auto-advance
- Nothing was written under brandbook-fable/
</verification>

<success_criteria>
- Roadmap SC1 satisfied: exactly 8 options, 2 per axis, each pre-screened before being shown (LOGO-05)
- Roadmap SC2 satisfied: every presented option passes the hard-constraint set (LOGO-06)
- Roadmap SC3 half-satisfied: rendered-evidence gallery exists in .planning/phases/87-*/tournament/ and the phase is at the hard pause with the selection protocol recorded (LOGO-07 presentation)
- The maintainer can open one file, look at renders (not essays), and answer with 1-2 numbers
</success_criteria>

<output>
Create `.planning/phases/87-logo-tournament/87-01-SUMMARY.md` when done. The
summary must state the pause loudly: phase blocked on maintainer selection in
87-01-CHECKPOINT.md; 87-02 must not run until then.
</output>
