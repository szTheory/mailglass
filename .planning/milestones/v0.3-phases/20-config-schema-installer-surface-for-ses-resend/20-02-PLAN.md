---
phase: 20-config-schema-installer-surface-for-ses-resend
plan: "02"
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - lib/mailglass/errors/publish_error.ex
  - lib/mailglass/error.ex
  - lib/mailglass/publish/installer_golden_check.ex
  - lib/mix/tasks/mailglass.publish.check.ex
  - docs/api_stability.md
  - test/mailglass/error_test.exs
  - test/mailglass/errors/publish_error_test.exs
  - test/mailglass/publish/installer_golden_check_test.exs
autonomous: true
requirements:
  - SES-01
  - SES-02
  - SES-03
  - SES-04
  - SES-05
  - RESEND-01
  - RESEND-02
tags:
  - release
  - error-contract
  - mix-task
  - publish

must_haves:
  truths:
    - "Installer golden drift is represented by a dedicated Mailglass sibling exception, not `Mailglass.ConfigError` and not a fictional `%Mailglass.Error{}` struct."
    - "The new error module exposes a closed `:type` atom set containing the golden-drift failure class."
    - "`Mailglass.Error.t()` and `Mailglass.Error.is_error?/1` recognize the new publish error."
    - "`mix mailglass.publish.check` still prints the exact installer refresh command at the Mix boundary."
    - "`docs/api_stability.md` and error tests stay aligned with the new public error contract."
  artifacts:
    - path: "lib/mailglass/errors/publish_error.ex"
      provides: "typed publish drift error"
      contains: "publish_blocked_golden_drift"
    - path: "lib/mailglass/publish/installer_golden_check.ex"
      provides: "deterministic drift-path helper seam"
      contains: "publish_blocked_golden_drift"
    - path: "lib/mix/tasks/mailglass.publish.check.ex"
      provides: "typed drift failure path wired to the helper seam"
      contains: "MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors"
    - path: "test/mailglass/errors/publish_error_test.exs"
      provides: "focused error contract coverage"
      contains: "__types__"
    - path: "test/mailglass/publish/installer_golden_check_test.exs"
      provides: "deterministic publish drift-path coverage"
      contains: "publish_blocked_golden_drift"
  key_links:
    - from: "lib/mailglass/errors/publish_error.ex"
      to: "lib/mailglass/error.ex"
      via: "new sibling error must be added to the shared union and registry"
      pattern: "Mailglass.PublishError"
    - from: "lib/mix/tasks/mailglass.publish.check.ex"
      to: "test/mailglass/install/install_golden_test.exs"
      via: "drift failure must preserve the exact snapshot refresh command"
      pattern: "MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors"
---

<objective>
Convert the installer-golden publish failure from a string-only task abort into a typed Mailglass error path while preserving the existing operator ergonomics of `mix mailglass.publish.check`.

Purpose: Phase 20 closes the final release-hygiene seam after the installer contract is corrected. The internal failure becomes pattern-testable and part of the public error taxonomy, but the CLI still fails like a normal Mix task with the exact regeneration command.

Output:
- new `Mailglass.PublishError` sibling exception module
- `Mailglass.Error` union / registry updated
- `docs/api_stability.md` documents the new error contract
- publish-check drift branch routes through the new error path
- focused tests cover the new error module and the shared union
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/20-config-schema-installer-surface-for-ses-resend/20-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/20-config-schema-installer-surface-for-ses-resend/20-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/20-config-schema-installer-surface-for-ses-resend/20-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/phases/20-config-schema-installer-surface-for-ses-resend/20-VALIDATION.md
@/Users/jon/projects/mailglass/lib/mailglass/error.ex
@/Users/jon/projects/mailglass/lib/mailglass/errors/config_error.ex
@/Users/jon/projects/mailglass/lib/mailglass/errors/stream_policy_error.ex
@/Users/jon/projects/mailglass/lib/mailglass/publish/installer_golden_check.ex
@/Users/jon/projects/mailglass/lib/mix/tasks/mailglass.publish.check.ex
@/Users/jon/projects/mailglass/docs/api_stability.md
@/Users/jon/projects/mailglass/test/mailglass/error_test.exs
@/Users/jon/projects/mailglass/test/mailglass/errors/config_error_test.exs
@/Users/jon/projects/mailglass/test/mailglass/publish/installer_golden_check_test.exs
@/Users/jon/projects/mailglass/test/mailglass/install/install_golden_test.exs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 20-02-01: Add `Mailglass.PublishError` and register it in the shared error contract</name>
  <files>lib/mailglass/errors/publish_error.ex, lib/mailglass/error.ex, docs/api_stability.md, test/mailglass/error_test.exs, test/mailglass/errors/publish_error_test.exs</files>
  <read_first>
    - lib/mailglass/errors/config_error.ex (sibling error shape)
    - lib/mailglass/errors/stream_policy_error.ex (compact one-atom sibling error shape)
    - lib/mailglass/error.ex (`@type t`, `@error_modules`, helper behavior)
    - docs/api_stability.md (error hierarchy sections)
    - test/mailglass/error_test.exs (shared union and `__types__/0` assertions)
    - test/mailglass/errors/config_error_test.exs (focused sibling error tests)
    - .planning/phases/20-config-schema-installer-surface-for-ses-resend/20-RESEARCH.md (Pattern 3 + Pitfall 3)
  </read_first>
  <behavior>
    - `Mailglass.PublishError.__types__()` returns exactly `[:publish_blocked_golden_drift]`.
    - `Mailglass.PublishError.retryable?/1` returns `false`.
    - `Mailglass.Error.is_error?(Mailglass.PublishError.new(...))` returns `true`.
    - `docs/api_stability.md` documents the new sibling module and its one-atom closed set.
  </behavior>
  <action>
    Create `lib/mailglass/errors/publish_error.ex` as a sibling `defexception` module named `Mailglass.PublishError`.

    Use this exact contract:
    - `@behaviour Mailglass.Error`
    - `@types [:publish_blocked_golden_drift]`
    - `@derive {Jason.Encoder, only: [:type, :message, :context]}`
    - `defexception [:type, :message, :cause, :context]`
    - `@type t :: %__MODULE__{type: :publish_blocked_golden_drift, message: String.t(), cause: Exception.t() | nil, context: %{atom() => term()}}`
    - `def __types__, do: @types`
    - `def retryable?(%__MODULE__{}), do: false`
    - `new/2` builder matching the existing sibling error pattern

    Format the message so it includes:
    - the phrase `Publish blocked: installer goldens drifted`
    - the exact remediation command `MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors`
    - optional subprocess output appended from `context[:output]`

    In `lib/mailglass/error.ex`:
    - add `Mailglass.PublishError.t()` to `@type t`
    - add `Mailglass.PublishError` to `@error_modules`
    - update the moduledoc error inventory list to mention the new sibling

    In `docs/api_stability.md`:
    - add a new `### Mailglass.PublishError` section in the Error Hierarchy
    - document the single closed atom `:publish_blocked_golden_drift`
    - state retryable is `false`

    In `test/mailglass/error_test.exs`, extend the shared assertions so the new error is included in the union/registry expectations.

    Create `test/mailglass/errors/publish_error_test.exs` with focused tests for:
    - `__types__/0`
    - `retryable?/1`
    - `new/2` message content including the exact remediation command
  </action>
  <verify>
    <automated>mix test test/mailglass/error_test.exs test/mailglass/errors/publish_error_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mailglass/errors/publish_error.ex` exits 0.
    - `rg -n "publish_blocked_golden_drift" lib/mailglass/errors/publish_error.ex docs/api_stability.md test/mailglass/error_test.exs test/mailglass/errors/publish_error_test.exs` returns matches in all four surfaces.
    - `rg -n "Mailglass.PublishError" lib/mailglass/error.ex test/mailglass/error_test.exs` returns registry/union coverage.
    - `mix test test/mailglass/error_test.exs test/mailglass/errors/publish_error_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The publish drift failure has a typed sibling error module and every public error inventory surface is updated to recognize it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 20-02-02: Extract a deterministic installer-golden helper and verify the typed drift path</name>
  <files>lib/mailglass/publish/installer_golden_check.ex, lib/mix/tasks/mailglass.publish.check.ex, test/mailglass/publish/installer_golden_check_test.exs</files>
  <read_first>
    - lib/mix/tasks/mailglass.publish.check.ex (`verify_installer_goldens/1`, `fail_step/2`, task flow)
    - test/mailglass/install/install_golden_test.exs (current mismatch message and exact refresh command)
    - lib/mailglass/errors/publish_error.ex (new typed error message contract)
    - lib/mailglass/errors/config_error.ex (constructor + context/message style)
    - .planning/phases/20-config-schema-installer-surface-for-ses-resend/20-RESEARCH.md (Pattern 3 + Pitfall 4)
  </read_first>
  <behavior>
    - A deterministic helper returns `:ok` on status `0`.
    - The same helper returns `{:error, %Mailglass.PublishError{type: :publish_blocked_golden_drift}}` on non-zero status and includes the exact remediation command in `Exception.message/1`.
    - `mix mailglass.publish.check --package mailglass` continues to use the helper and still terminates with a normal Mix-facing failure message when the helper returns an error.
  </behavior>
  <action>
    Create `lib/mailglass/publish/installer_golden_check.ex` as a narrow helper module dedicated to the Phase 20 drift seam. Give it one public function:

    ```elixir
    run(repo_root, runner \\ &System.cmd/3)
    ```

    Required behavior:
    - call the existing installer-golden subprocess command using `runner`
    - return `:ok` when the subprocess status is `0`
    - return `{:error, Mailglass.PublishError.new(:publish_blocked_golden_drift, context: %{output: output, command: "MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors"})}` when status is non-zero

    Then update `lib/mix/tasks/mailglass.publish.check.ex` so `verify_installer_goldens/1` delegates to this helper. Preserve:
    - task ordering (installer goldens still run before tarball build)
    - the exact subprocess command
    - final Mix-facing failure text via `Exception.message(error)`

    Add `test/mailglass/publish/installer_golden_check_test.exs` with deterministic tests that inject a fake `runner`:
    - success case: fake runner returns `{"ok", 0}` and helper returns `:ok`
    - drift case: fake runner returns `{"snapshot mismatch", 2}` and helper returns `{:error, %Mailglass.PublishError{type: :publish_blocked_golden_drift}}`
    - drift case asserts `Exception.message(error)` contains the exact remediation command
  </action>
  <verify>
    <automated>mix test test/mailglass/publish/installer_golden_check_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mailglass/publish/installer_golden_check.ex` exits 0.
    - `test -f test/mailglass/publish/installer_golden_check_test.exs` exits 0.
    - `rg -n "publish_blocked_golden_drift|MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors" lib/mailglass/publish/installer_golden_check.ex test/mailglass/publish/installer_golden_check_test.exs` returns matches in both files.
    - `mix test test/mailglass/publish/installer_golden_check_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The publish drift branch is covered deterministically through an extracted helper seam that returns a typed `Mailglass.PublishError`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 20-02-03: Keep the green publish-check path intact after helper extraction</name>
  <files>lib/mix/tasks/mailglass.publish.check.ex</files>
  <read_first>
    - lib/mix/tasks/mailglass.publish.check.ex (installer-golden call site after Task 20-02-02)
    - lib/mailglass/publish/installer_golden_check.ex
    - .planning/phases/20-config-schema-installer-surface-for-ses-resend/20-VALIDATION.md
  </read_first>
  <behavior>
    - `MIX_PUBLISH=true mix mailglass.publish.check --package mailglass` exits 0 when goldens are current.
    - Installer goldens still run before tarball build.
  </behavior>
  <action>
    After the helper extraction is wired in, run the existing green-path package gate:

    ```bash
    MIX_PUBLISH=true mix mailglass.publish.check --package mailglass
    ```

    If the extraction changed call ordering or failure handling, fix `verify_installer_goldens/1` so the task still:
    - runs installer goldens first
    - fails fast on drift
    - continues unchanged when the helper returns `:ok`
  </action>
  <verify>
    <automated>MIX_PUBLISH=true mix mailglass.publish.check --package mailglass</automated>
  </verify>
  <acceptance_criteria>
    - `MIX_PUBLISH=true mix mailglass.publish.check --package mailglass` exits 0.
    - The installer-golden check still happens before tarball build in task order.
    - No later publish step is moved ahead of installer drift detection.
  </acceptance_criteria>
  <done>The helper extraction does not regress the success path of `mix mailglass.publish.check`.</done>
</task>

</tasks>

<verification>
  <automated>
    - mix test test/mailglass/error_test.exs test/mailglass/errors/publish_error_test.exs test/mailglass/publish/installer_golden_check_test.exs --warnings-as-errors
    - MIX_PUBLISH=true mix mailglass.publish.check --package mailglass
  </automated>
</verification>

<success_criteria>
  <criterion>`Mailglass.PublishError` exists as a sibling error module with the closed type `:publish_blocked_golden_drift`.</criterion>
  <criterion>`Mailglass.Error.t()`, `Mailglass.Error.is_error?/1`, and `docs/api_stability.md` all include the new publish error.</criterion>
  <criterion>The publish-check drift path preserves the exact golden refresh command and still fails before tarball build.</criterion>
  <criterion>Focused error tests and shared error-contract tests pass.</criterion>
</success_criteria>

<threat_model>
  <threat id="T-20-03" severity="high">Installer drift is caught only by ad hoc stderr text, making release failures hard to classify and test.</threat>
  <mitigation>Create `Mailglass.PublishError` with a closed type set and route publish-check drift through that typed path.</mitigation>
  <threat id="T-20-04" severity="medium">CLI ergonomics regress while adding the typed error path, hiding the exact regeneration command from maintainers.</threat>
  <mitigation>Keep the current remediation command verbatim in the final Mix-facing failure message.</mitigation>
</threat_model>
