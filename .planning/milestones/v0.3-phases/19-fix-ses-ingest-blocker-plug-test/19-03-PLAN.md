---
phase: 19-fix-ses-ingest-blocker-plug-test
plan: 03
type: execute
wave: 3
depends_on:
  - "19-01"
  - "19-02"
files_modified: []
autonomous: false
requirements: []
tags:
  - validation
  - release
  - ci
  - phase-gate

must_haves:
  truths:
    - "`mix test` exits 0 with NO `--only` scoping and NO test exclusions (Roadmap Phase 19 success criterion #4)."
    - "`mix credo --strict` exits 0 across the whole codebase."
    - "`mix format --check-formatted` exits 0 across the whole codebase."
    - "`git status` is clean OR shows only the expected files: `lib/mailglass/webhook/ingest.ex` (modified) + `test/mailglass/webhook/plug_ses_test.exs` (new) + planning artifacts."
    - "Conventional Commits message follows the `fix(ingest): accept :ses provider in webhook ingest seam` shape (or close variant) — Release Please will consume it on merge to cut v0.3.3."
    - "Roadmap Phase 19 success criterion #5 (v0.3.3 published to Hex.pm via Release Please) is documented as a post-merge OBSERVATION gate, not an executable task in this plan."
  artifacts:
    - path: ".planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md"
      provides: "Phase-19 closure record with full-suite green proof + Conventional Commits message + Release Please observation pointer"
      min_lines: 30
  key_links:
    - from: "Conventional Commits `fix:` PR (post-Plan 19-03)"
      to: "Release Please workflow → v0.3.3 tag → Hex.pm publish"
      via: "GitHub Actions / release-please / sibling-package linked-versions plugin"
      pattern: "fix\\((ingest|webhook|ses)\\):"
---

<objective>
Phase-level validation gate: prove that Plan 19-01 (ingest seam fix) + Plan 19-02 (Plug-level integration test) produce a fully green codebase with `mix test` (no scoping), `mix credo --strict`, and `mix format --check-formatted` all clean. Author the 19-03-SUMMARY.md documenting gate output and the Conventional Commits message. Document the post-merge Release Please flow as a manual observation step (success criterion #5). Human-verify checkpoint approves the commit ceremony.

Purpose: The roadmap lists 5 success criteria for Phase 19; criteria #1-#4 are code-level and verifiable now (this plan), criterion #5 (v0.3.3 on Hex.pm) is a CI-driven post-merge observation. This plan closes #4 ("`mix test` passes clean with no `--only` scoping or test exclusions") and prepares the commit ceremony. Closes SES-05 — the requirement that "SES events inside the SNS Message envelope map to the internal normalized taxonomy" is empirically proven by the new plug test asserting a `WebhookEvent` row persists end-to-end (taxonomy mapping is the only way ingest succeeds).

Output: A green full-suite `mix test`, a 19-03-SUMMARY.md documenting gate output + the Conventional Commits message + the Release Please observation flow, and a human-verify checkpoint approving the prepared commit ceremony.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-RESEARCH.md
@.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-PATTERNS.md
@.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-VALIDATION.md
@.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-01-SUMMARY.md
@.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-02-SUMMARY.md
@.planning/v0.3.0-MILESTONE-AUDIT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 19-03-01: Run full-suite gates (`mix test` + `mix credo --strict` + `mix format --check-formatted`)</name>
  <files></files>
  <read_first>
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-01-SUMMARY.md (confirm Plan 19-01 actually closed)
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-02-SUMMARY.md (confirm Plan 19-02 actually closed)
    - .planning/ROADMAP.md (lines 97-108 — Phase 19 success criteria, especially #4 "mix test passes clean with no --only scoping or test exclusions")
    - CLAUDE.md (commit conventions section — Conventional Commits enforced; squash-merge workflow; `docs(state):` exemption)
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-VALIDATION.md (lines 30-50 — sampling rate confirms full-suite is the phase gate)
  </read_first>
  <behavior>
    - Behavior 1: `mix test` exits 0 with no scoping, no `--only`, no `--exclude`, no environment-variable test filters. Output line shows `0 failures` and contains no Phase-19-related `excluded` markers.
    - Behavior 2: `mix credo --strict` exits 0 across the entire codebase.
    - Behavior 3: `mix format --check-formatted` exits 0 across the entire codebase.
    - Behavior 4: `mix compile --warnings-as-errors` exits 0 across the entire codebase.
    - Behavior 5: `git status` shows only the expected file changes (Plan 19-01 ingest.ex + Plan 19-02 plug_ses_test.exs + planning artifacts). No stray binary blobs, no unintended config edits, no `.env` leakage.
  </behavior>
  <action>
    Run the four phase gates IN ORDER and record output for the SUMMARY:

    1. **`mix compile --warnings-as-errors`** — full clean compile. If this fails, Plan 19-01 likely shipped a syntax or guard error; revisit before proceeding.

    2. **`mix format --check-formatted`** — full repo formatter check. If this fails, run `mix format` to fix and re-run.

    3. **`mix credo --strict`** — full repo Credo check. If this fails on a pre-existing warning unrelated to Phase 19, document the failure context in the SUMMARY (do NOT fix unrelated issues here — that's scope drift).

    4. **`mix test`** (NO SCOPING — exact command: `mix test`, nothing else) — the canonical Phase 19 success criterion #4 gate. Output must show `0 failures`. Run the command twice in a row to confirm idempotency.

    Then run `git status` and confirm the changed-files list is exactly: `lib/mailglass/webhook/ingest.ex` (modified, Plan 19-01), `test/mailglass/webhook/plug_ses_test.exs` (new, Plan 19-02), three plan SUMMARY files in `.planning/phases/19-fix-ses-ingest-blocker-plug-test/`, plus possibly `.planning/STATE.md` and `.planning/ROADMAP.md` (touched by `/gsd-execute-phase`).

    If `git status` shows additional unexpected files (e.g., `_build` artifacts, `.elixir_ls/`, `coverage/`), DO NOT commit them. Investigate the source — likely a `.gitignore` gap. Stop and flag for the human if unsure.

    DO NOT commit yet. Capture the exact `mix test` summary line (e.g., `Finished in 2.3 seconds`, `42 tests, 0 failures`) for inclusion in the 19-03-SUMMARY.md by Task 19-03-02.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix format --check-formatted && mix credo --strict && mix test && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0.
    - `mix format --check-formatted` exits 0 (entire repo, not scoped).
    - `mix credo --strict` exits 0 (entire repo, not scoped).
    - `mix test` (NO arguments, NO `--only`, NO `--exclude`) exits 0.
    - The `mix test` summary line contains `0 failures`.
    - A second consecutive `mix test` run also exits 0 (idempotency proof).
    - `git status --porcelain` shows ONLY the expected files; verified via: `git status --porcelain | grep -vE "^.{2} (lib/mailglass/webhook/ingest\\.ex|test/mailglass/webhook/plug_ses_test\\.exs|\\.planning/(STATE|ROADMAP)\\.md|\\.planning/phases/19-fix-ses-ingest-blocker-plug-test/)"` returns no output.
    - NO files in `_build/`, `.elixir_ls/`, `coverage/`, or other build/IDE directories appear in `git status`.
    - The exact `mix test` summary line is captured (verbatim) for the SUMMARY.
  </acceptance_criteria>
  <done>All four phase gates green; `git status` is clean of unexpected changes; full-suite output captured for SUMMARY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 19-03-02: Author 19-03-SUMMARY.md with gate output, Conventional Commits message, and Release Please observation flow</name>
  <files>.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md (commit conventions: Conventional Commits enforced, squash-merge, `docs(state):` exemption, third-party Actions pinned to SHA, Hex publish from protected ref)
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-RESEARCH.md (Open Question #4 — Conventional Commits scope: `fix(ingest):` recommended; lines 668-672)
    - .planning/v0.3.0-MILESTONE-AUDIT.md (Recommended Remediation #1 — confirms `fix:` Conventional Commits + Release Please path; lines 227-231)
    - .planning/STATE.md (D-32: Phase 18 recovery pattern — `fix:` commits via Release Please; orphan tags acceptable)
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-01-SUMMARY.md
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-02-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/summary.md (the standard SUMMARY template structure GSD expects)
  </read_first>
  <behavior>
    - Behavior 1: A new file `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` is created.
    - Behavior 2: Frontmatter contains `phase: 19`, `plan: 03`, `requirements-completed: []` (Plan 19-03 is a release-ceremony/full-suite gate plan; SES-04 and SES-05 are claimed by Plan 19-02's SUMMARY, SES-01 and SES-03 by Plan 19-01's SUMMARY), `status: complete`, `milestone: v0.3.3`.
    - Behavior 3: SUMMARY contains a `## Phase Gates` section with verbatim output from each gate command in Task 19-03-01 and the `mix test` summary line captured verbatim.
    - Behavior 4: SUMMARY contains a `## Files Changed` section with `git diff --stat` output.
    - Behavior 5: SUMMARY contains a `## Conventional Commits Message (RECOMMENDED)` section with the verbatim multi-line message body (the `fix(ingest):` template specified below in `<action>`).
    - Behavior 6: SUMMARY contains a `## Release Please Observation Flow (post-merge)` section with all 5 numbered observation steps (specified below in `<action>`).
    - Behavior 7: SUMMARY contains a `## Roadmap Success Criteria — Status` section with a 5-row table mapping each Phase 19 success criterion to ✓ / observation-gate status with evidence pointers.
    - Behavior 8: SUMMARY contains a `## Notes` section flagging the test-file-path discretion choice (Mailgun convention chosen over the roadmap text per RESEARCH Open Question #1).
  </behavior>
  <action>
    Author `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` using the Write tool (NOT a heredoc / cat). The file MUST contain the following sections, in order:

    **1. YAML frontmatter (verbatim):**
    ```
    phase: 19
    plan: 03
    requirements-completed: []
    status: complete
    milestone: v0.3.3
    ```

    **2. Title and one-paragraph preamble** explaining that Plans 19-01 and 19-02 closed the v0.3.0 milestone audit BLOCKER (`gaps.integration` — `lib/mailglass/webhook/ingest.ex:122` omitted `:ses` from the provider guard) and added Plug-level regression coverage; this plan ran the full-suite gates green and prepared the Conventional Commits message for the v0.3.3 patch release.

    **3. `## Phase Gates`** — table with five rows: Compile, Format, Credo, Test (1st), Test (2nd). Columns: Gate, Command, Exit, Notes. Substitute the verbatim `mix test` summary line captured in Task 19-03-01 into the "Notes" column for the Test rows.

    **4. `## Files Changed`** — fenced code block containing the `git diff --stat` output captured by running `git diff --stat` over the staged + unstaged changes (expected: ~10 lines added in `lib/mailglass/webhook/ingest.ex` and a new `test/mailglass/webhook/plug_ses_test.exs` of ~120 lines).

    **5. `## Conventional Commits Message (RECOMMENDED)`** — fenced code block (use `text` language tag to avoid nested-triple-backtick issues) containing exactly this commit message body:

    > Subject line: `fix(ingest): accept :ses provider in webhook ingest seam`
    >
    > Body:
    > - Paragraph 1: `Close v0.3.0 milestone audit BLOCKER (gaps.integration — lib/mailglass/webhook/ingest.ex:122 omitted :ses from the provider guard).`
    > - Bullet list:
    >   - `Add :ses to ingest_multi/3 guard between :mailgun and :resend (mirrors Mailglass.Webhook.Plug.@valid_providers ordering).`
    >   - `Add derive_webhook_provider_event_id(:ses, _, [first | _]) clause delegating to extract_event_provider_id/1 — same dispatch as Mailgun/Resend; SES build_event/8 already populates Event.metadata["provider_event_id"] as "<sns_message_id>:<email>".`
    >   - `Add test/mailglass/webhook/plug_ses_test.exs Plug-level integration test (4 tests: success, replay, bad signature, init/1) mirroring plug_mailgun_test.exs.`
    > - Closing paragraph: `Closes SES-01, SES-03, SES-04, SES-05 end-to-end. Triggers Release Please patch bump to v0.3.3.`
    > - Refs lines:
    >   - `Refs: .planning/v0.3.0-MILESTONE-AUDIT.md (BLOCKER)`
    >   - `Refs: .planning/phases/19-fix-ses-ingest-blocker-plug-test/`

    Note inside the section: acceptable scope alternatives are `fix(webhook):` or `fix(ses):` (research Open Question #4 — stylistic only). DO NOT use `feat:` (this is a bug fix; `feat:` would bump minor instead of patch).

    **6. `## Release Please Observation Flow (post-merge)`** — numbered list of 5 steps:

    1. After PR merge to `main`, Release Please opens a release PR titled `chore: release main` with version bump to `0.3.3` for both `mailglass` and `mailglass_admin` (linked-versions plugin).
    2. Verify the auto-generated `CHANGELOG.md` entry under `## [0.3.3]` mentions the `fix(ingest):` commit.
    3. Merge the Release Please PR.
    4. The publish workflow (gated on protected ref + GitHub Environment) tags `mailglass-v0.3.3` and `mailglass_admin-v0.3.3`, then publishes both packages to Hex.pm via `mix hex.publish` using the existing `HEX_API_KEY` (per user memory).
    5. Verify the live release with these commands (sub-bullets):
       - `gh release view mailglass-v0.3.3` returns 200.
       - `mix hex.info mailglass | grep -E "0\.3\.3"` shows the new version.
       - `mix hex.info mailglass_admin | grep -E "0\.3\.3"` shows the new version.
       - `curl -sIo /dev/null -w "%{http_code}" https://hex.pm/packages/mailglass/0.3.3` returns `200`.
       - `curl -sIo /dev/null -w "%{http_code}" https://hex.pm/packages/mailglass_admin/0.3.3` returns `200`.
       - `curl -sIo /dev/null -w "%{http_code}" https://hexdocs.pm/mailglass/0.3.3` returns `200`.

    **7. `## Roadmap Success Criteria — Status`** — Markdown table with these exact 5 rows (Status column = ✓ for criteria 1-4, "observation gate (post-merge)" for criterion 5):

    | # | Success Criterion | Status | Evidence |
    | 1 | `ingest_multi/3` accepts `:ses` (guard updated) | ✓ | Plan 19-01 Task 19-01-01 |
    | 2 | `derive_webhook_provider_event_id(:ses, ...)` clause exists | ✓ | Plan 19-01 Task 19-01-02 |
    | 3 | New plug-level test exercises real signed SES Notification end-to-end + asserts WebhookEvent persisted | ✓ | Plan 19-02 Task 19-02-01 (file at `test/mailglass/webhook/plug_ses_test.exs` per RESEARCH Open Question #1) |
    | 4 | `mix test` passes clean with no `--only` scoping or test exclusions | ✓ | Plan 19-03 Task 19-03-01 |
    | 5 | v0.3.3 published to Hex.pm via Release Please | observation gate (post-merge) | See "Release Please Observation Flow" above |

    **8. `## Notes`** — two paragraphs:

    - **Test-file-path discretion** — RESEARCH Open Question #1 (lines 654-657) flagged a path mismatch between the roadmap success criterion #3 text (`test/mailglass/webhook/providers/ses_plug_test.exs`, Resend convention) and existing repo convention (`test/mailglass/webhook/plug_<provider>_test.exs`, Mailgun convention). Plan 19-02 chose the **Mailgun convention** for cleaner separation between unit tests in `providers/` and Plug-level integration tests in `webhook/`. Both paths function identically.
    - **Why this is a 3-plan phase** — Phase 19 is small (~7 production lines + ~120 test lines). Splitting into 3 plans keeps each plan atomic for `/gsd-execute-phase` wave dispatch, but the squash-merge of all three plans produces a single commit on `main` that Release Please consumes as a `fix:` for v0.3.3.

    Save the file. Verify it parses cleanly via:
    - `head -10 .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` confirms frontmatter delimited by `---`.
    - `gsd-sdk query frontmatter.validate .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md --schema summary` returns valid (or schema-not-found informational error).

    DO NOT commit anything in this task. The Task 19-03-03 human-verify checkpoint is the gate before commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md && grep -c "requirements-completed:" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md && grep -c "fix(ingest): accept :ses provider in webhook ingest seam" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md && grep -c "Release Please Observation Flow" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md && grep -c "Roadmap Success Criteria" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` exits 0.
    - Frontmatter contains `requirements-completed: []` (Plan 19-03 is a release-ceremony gate; per-REQ-ID SUMMARYs are emitted by Plan 19-01 and 19-02): `grep -c "requirements-completed: \[\]" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` returns at least 1.
    - Frontmatter contains `status: complete`: `grep -c "status: complete" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` returns at least 1.
    - Frontmatter contains `milestone: v0.3.3`: `grep -c "milestone: v0.3.3" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` returns at least 1.
    - Conventional Commits subject present verbatim: `grep -c "fix(ingest): accept :ses provider in webhook ingest seam" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` returns at least 1.
    - All 5 required sections present: each of `## Phase Gates`, `## Files Changed`, `## Conventional Commits Message`, `## Release Please Observation Flow`, `## Roadmap Success Criteria`, `## Notes` returns at least 1 hit when grepped for the heading text.
    - Closes-line present: `grep -c "Closes SES-01, SES-03, SES-04, SES-05" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` returns at least 1.
    - All 5 Hex/HexDocs verification commands present: `grep -cE "(gh release view mailglass-v0.3.3|mix hex.info mailglass|hex.pm/packages/mailglass|hexdocs.pm/mailglass)" .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` returns at least 4 distinct matches.
    - `gsd-sdk query frontmatter.validate .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md --schema summary` returns valid (or schema-not-found informational error — not a hard failure).
  </acceptance_criteria>
  <done>The 19-03-SUMMARY.md exists with all required sections, the verbatim Conventional Commits message, the 5-step Release Please observation flow, and the 5-row success criteria table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 19-03-03: Human-verify checkpoint — review changeset, approve commit ceremony</name>
  <files></files>
  <read_first>
    - .planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md (just authored by Task 19-03-02)
    - lib/mailglass/webhook/ingest.ex (the 8-line diff from Plan 19-01)
    - test/mailglass/webhook/plug_ses_test.exs (the new ~120-line test file from Plan 19-02)
  </read_first>
  <behavior>
    - Behavior 1: The human reviews `git diff lib/mailglass/webhook/ingest.ex` (expected: 1-line guard widening + 6-line new clause + comment).
    - Behavior 2: The human reviews `test/mailglass/webhook/plug_ses_test.exs` (expected: 4 tests, no `Mailglass.HTTPCStub` reference, no SubscriptionConfirmation test).
    - Behavior 3: The human reviews `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` and confirms the Conventional Commits message scope choice (`fix(ingest):` recommended).
    - Behavior 4: The human signals approval with `approved` (or describes revisions needed).
  </behavior>
  <action>
    Present the prepared changeset and SUMMARY to the human:

    **What's been built (recap):**
    - Plan 19-01 closed the BLOCKER by editing `lib/mailglass/webhook/ingest.ex` (8-line surgical diff: 1-atom guard widening + 6-line `derive_webhook_provider_event_id(:ses, ...)` clause).
    - Plan 19-02 added `test/mailglass/webhook/plug_ses_test.exs` (~120 lines, 4 tests) covering Notification success + replay + bad-signature + `init/1` sanity.
    - Plan 19-03 Task 19-03-01 ran the full-suite gates green twice consecutively.
    - Plan 19-03 Task 19-03-02 authored `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` capturing gate output, the verbatim Conventional Commits message, the Release Please observation flow, and the success-criteria table.

    **What the human needs to do:**
    1. Review `git diff lib/mailglass/webhook/ingest.ex` — expect 8 lines added, 1 line modified, matching the verbatim diff at RESEARCH.md lines 478-506.
    2. Review `test/mailglass/webhook/plug_ses_test.exs` — confirm structure mirrors `plug_mailgun_test.exs`, no `Mailglass.HTTPCStub` reference, no SubscriptionConfirmation tests.
    3. Read `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` — confirm Conventional Commits message scope (`fix(ingest):` recommended; `fix(webhook):` or `fix(ses):` acceptable variants).
    4. Stage and commit using the prepared message (or a close variant). Open a PR to `main`.
    5. After CI green + merge: observe Release Please cut v0.3.3 per the SUMMARY's "Release Please Observation Flow" section. Run the 6 verification commands listed there to confirm the live release.

    **Resume signal:** type `approved` to mark the checkpoint passed. Or describe any revisions needed (e.g., different Conventional Commits scope, different observation steps, or scope drift in the diff).
  </action>
  <verify>
    <automated>echo "Checkpoint: human reviews and approves before commit. No automated check; resume-signal gates progression."</automated>
  </verify>
  <acceptance_criteria>
    - The human has reviewed the 8-line ingest.ex diff and the new test file.
    - The human has read the 19-03-SUMMARY.md.
    - The human signals `approved` (or directs revisions, in which case the orchestrator returns to the appropriate plan).
    - No commit happens BEFORE this checkpoint passes — the orchestrator must wait for `approved` before staging files.
  </acceptance_criteria>
  <done>Human approves the commit ceremony; the orchestrator may now stage files and create the `fix(ingest):` commit per the SUMMARY message.</done>
  <what-built>
    The complete Phase 19 changeset is ready for commit:
    - `lib/mailglass/webhook/ingest.ex` — 8-line diff closing the BLOCKER guard + adding `:ses` derive clause.
    - `test/mailglass/webhook/plug_ses_test.exs` — new ~120-line Plug-level integration test.
    - `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` — phase closure record with the verbatim Conventional Commits message and 5-step Release Please observation flow.
    - Three plan files (`19-01-PLAN.md`, `19-02-PLAN.md`, `19-03-PLAN.md`) and three SUMMARYs (`19-01-SUMMARY.md`, `19-02-SUMMARY.md`, `19-03-SUMMARY.md`).
  </what-built>
  <how-to-verify>
    1. Run `git diff lib/mailglass/webhook/ingest.ex` — expect ~8 lines: `:ses` added to guard, new `derive_webhook_provider_event_id(:ses, _, [first | _])` clause with comment.
    2. Open `test/mailglass/webhook/plug_ses_test.exs` and confirm: 4 tests, no `Mailglass.HTTPCStub`, no `SubscriptionConfirmation` tests, `use Mailglass.WebhookCase, async: false`.
    3. Open `.planning/phases/19-fix-ses-ingest-blocker-plug-test/19-03-SUMMARY.md` and read all sections — confirm Conventional Commits message scope and verify the Release Please observation flow has 5 steps with concrete `gh`/`mix hex.info`/`curl` verification commands.
    4. Decide on commit scope: `fix(ingest):` (recommended), `fix(webhook):`, or `fix(ses):`.
    5. After approval: stage `lib/mailglass/webhook/ingest.ex`, `test/mailglass/webhook/plug_ses_test.exs`, and the planning artifacts; commit with the prepared message; push; open PR.
    6. After PR merge: follow the 5-step Release Please observation flow in the SUMMARY to verify v0.3.3 ships to Hex.pm.
  </how-to-verify>
  <resume-signal>Type `approved` after reviewing the changeset and SUMMARY. Or describe any revisions needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local `git` working tree → GitHub PR → `main` | Standard PR review boundary; no Phase 19-specific change. The `fix(ingest):` Conventional Commits message must travel intact through squash-merge. |
| GitHub Actions `release-please` → Hex.pm | Pre-existing publish boundary (gated on protected ref + GitHub Environment). `HEX_API_KEY` already configured per user memory. |

## STRIDE Threat Register

Plan 19-03 is a validation gate + commit ceremony plan. No code edits — minimal threat surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering (commit message integrity) | Conventional Commits message → Release Please trigger | mitigate | The recommended commit message uses the exact `fix(ingest):` shape proven 3× in Phase 18 (D-32). Squash-merge preserves the leading message line. |
| T-19-03-02 | Information Disclosure (secrets in PR) | `git diff` content | mitigate | Task 19-03-01 acceptance criteria require `git status` to show ONLY the expected files (no `.env`, no `_build/`, no IDE state). The 8-line ingest diff and 120-line test file contain no secrets, no PII. |
| T-19-03-03 | Elevation of Privilege (rogue Hex publish) | GitHub Actions `release-please` workflow | accept | Existing controls: protected ref + GitHub Environment with required reviewers; `HEX_API_KEY` invisible to PR jobs (CLAUDE.md). Phase 19 inherits these unchanged. |
| T-19-03-04 | Repudiation (Phase 19 changes auditable) | Phase 19 `*-SUMMARY.md` artifacts + Conventional Commits message | mitigate | Task 19-03-02 creates `19-03-SUMMARY.md` capturing all gate output, file diff stat, and the exact commit message — providing a durable audit trail. |

**No new attack surface introduced.** The Hex publish path is the same one used in Phase 18 for v0.3.0 → v0.3.1 → v0.3.2.
</threat_model>

<verification>
- All four phase gates from Task 19-03-01 green twice.
- `19-03-SUMMARY.md` exists with frontmatter, gate output, commit message, observation flow, success-criteria table, and notes.
- Human-verify checkpoint passed (`approved` signal received) before phase closure.
- Post-merge: Release Please cuts v0.3.3 and publishes both packages — observation flow documented in SUMMARY guides the human through verifying live Hex.pm + HexDocs returns.
</verification>

<success_criteria>
- `mix test` passes clean with NO `--only` scoping and NO test exclusions (Roadmap Phase 19 SC #4) — proven twice consecutively.
- `mix credo --strict` and `mix format --check-formatted` exit 0 across the whole codebase.
- `git status` shows only Phase 19 expected files (ingest.ex modified, plug_ses_test.exs new, planning artifacts).
- `19-03-SUMMARY.md` documents the verbatim Conventional Commits message, the 5-step Release Please observation flow, and the success-criteria status table.
- Human reviews and approves the prepared commit + observation flow.
- Roadmap Phase 19 SC #5 (v0.3.3 on Hex.pm) is left as a post-merge observation, with explicit `gh release view`, `mix hex.info`, and `curl` commands documented for verification.
</success_criteria>

<output>
After completion, this plan IS the closure record. The 19-03-SUMMARY.md authored in Task 19-03-02 is the artifact. Once the human approves the checkpoint at Task 19-03-03, Phase 19 is complete pending the post-merge Release Please observation (which closes Roadmap SC #5).

The next phase in the roadmap is Phase 20 (Config Schema & Installer Surface for SES + Resend) — orthogonal to Phase 19 and unblocked by it.
</output>
