---
phase: 15-mailgun-webhook-provider
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - lib/mailglass/webhook/plug.ex
  - lib/mailglass/webhook/router.ex
  - lib/mailglass/config.ex
  - test/support/webhook_case.ex
  - test/mailglass/webhook/plug_mailgun_test.exs
  - test/mailglass/webhook/router_test.exs
  - test/mailglass/config_test.exs
autonomous: true
requirements:
  - MAILGUN-01
  - MAILGUN-02
  - MAILGUN-03
must_haves:
  truths:
    - "Mailgun routes can be mounted explicitly without changing the default webhook surface."
    - "A replayed Mailgun webhook returns a successful non-retrying outcome instead of 401."
    - "Mailgun runtime config is validated centrally with safe defaults for replay and timestamp handling."
  artifacts:
    - path: "lib/mailglass/webhook/plug.ex"
      provides: "Replay-aware Mailgun request orchestration and response matrix"
    - path: "lib/mailglass/webhook/router.ex"
      provides: "Explicit `:mailgun` opt-in route validation"
    - path: "lib/mailglass/config.ex"
      provides: "Validated Mailgun runtime config subtree"
  key_links:
    - from: "lib/mailglass/webhook/plug.ex"
      to: "lib/mailglass/webhook/providers/mailgun.ex"
      via: "verify_with_telemetry! and provider_module(:mailgun)"
      pattern: "provider_module\\(:mailgun\\)"
    - from: "lib/mailglass/webhook/router.ex"
      to: "lib/mailglass/webhook/plug.ex"
      via: "post route with [provider: :mailgun]"
      pattern: "provider: :mailgun"
    - from: "test/support/webhook_case.ex"
      to: "lib/mailglass/config.ex"
      via: "Mailgun test config env keys"
      pattern: "signing_key|timestamp_tolerance_seconds|future_skew_seconds|replay_cache_ttl_seconds"
---

<objective>
Wire Mailgun into the webhook runtime: config schema, plug/router integration, test harness updates, and integration-style tests for replay and route behavior.

Purpose: Turn the provider core into a mountable, replay-safe webhook path that preserves Mailglass's existing route and error contracts.
Output: Mailgun runtime wiring, router/config integration, webhook test harness support, and runtime tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mailgun-webhook-provider/15-CONTEXT.md
@.planning/phases/15-mailgun-webhook-provider/15-RESEARCH.md
@.planning/phases/15-mailgun-webhook-provider/15-PATTERNS.md
@.planning/phases/15-mailgun-webhook-provider/15-01-PLAN.md
@lib/mailglass/webhook/plug.ex
@lib/mailglass/webhook/router.ex
@lib/mailglass/config.ex
@test/support/webhook_case.ex
@test/mailglass/webhook/router_test.exs
@test/mailglass/webhook/plug_test.exs
@test/mailglass/config_test.exs

<interfaces>
From lib/mailglass/webhook/plug.ex:
```elixir
@valid_providers [:postmark, :sendgrid]
defp resolve_config!(:sendgrid, _conn) do
  %{public_key: env[:public_key], timestamp_tolerance_seconds: env[:timestamp_tolerance_seconds] || 300}
end
```

From lib/mailglass/webhook/router.ex:
```elixir
@valid_providers [:postmark, :sendgrid]
@default_providers @valid_providers
defmacro mailglass_webhook_routes(path, opts \\ [])
```

From lib/mailglass/config.ex:
```elixir
sendgrid: [
  keys: [
    enabled: [type: :boolean, default: true],
    public_key: [type: {:or, [:string, nil]}, default: nil],
    timestamp_tolerance_seconds: [type: :pos_integer, default: 300]
  ]
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Mailgun config schema and plug/router runtime wiring</name>
  <read_first>lib/mailglass/webhook/plug.ex, lib/mailglass/webhook/router.ex, lib/mailglass/config.ex, lib/mailglass/webhook/provider.ex, lib/mailglass/webhook/providers/mailgun.ex, .planning/phases/15-mailgun-webhook-provider/15-CONTEXT.md</read_first>
  <files>lib/mailglass/webhook/plug.ex, lib/mailglass/webhook/router.ex, lib/mailglass/config.ex</files>
  <behavior>
    - `Mailglass.Webhook.Plug` treats `{:ok, :replay}` from Mailgun verification as HTTP 200 with replay-specific stop metadata instead of the generic signature-failure rescue.
    - `mailglass_webhook_routes "/webhooks"` still generates only Postmark and SendGrid routes; Mailgun appears only when `providers: [...]` explicitly includes `:mailgun`.
    - `Mailglass.Config` validates a `:mailgun` subtree with explicit runtime defaults and no ad hoc config lookups elsewhere.
  </behavior>
  <action>Modify `lib/mailglass/webhook/plug.ex` so `@valid_providers` includes `:mailgun`, `provider_module(:mailgun)` resolves to `Mailglass.Webhook.Providers.Mailgun`, and `resolve_config!(:mailgun, _conn)` returns `%{signing_key: env[:signing_key], timestamp_tolerance_seconds: env[:timestamp_tolerance_seconds] || 28_800, future_skew_seconds: env[:future_skew_seconds] || 300, replay_cache_ttl_seconds: env[:replay_cache_ttl_seconds] || 28_800}` per D-04. In `verify_with_telemetry!/4` and `do_call/3`, preserve the provider result so `{:ok, :replay}` short-circuits to `send_resp(conn, 200, "")` with stop metadata `%{provider: :mailgun, status: :replay, duplicate: true, event_count: 0}` and never enters tenant resolution or ingest per D-05. Update `lib/mailglass/webhook/router.ex` so `@valid_providers` includes `:mailgun` but `@default_providers` remains `[:postmark, :sendgrid]` per D-06. Add a `mailgun:` subtree to `Mailglass.Config` with keys `enabled: true`, `signing_key: nil`, `timestamp_tolerance_seconds: 28_800`, `future_skew_seconds: 300`, and `replay_cache_ttl_seconds: 28_800`; keep all validation inside `Mailglass.Config` per CLAUDE.md.</action>
  <acceptance_criteria>
    - `lib/mailglass/webhook/plug.ex` contains `:mailgun` in `@valid_providers`, `provider_module(:mailgun)`, and a replay-specific `status: :replay` 200 path.
    - `lib/mailglass/webhook/router.ex` keeps `@default_providers` at `[:postmark, :sendgrid]`.
    - `lib/mailglass/config.ex` contains a `mailgun:` subtree with keys `signing_key`, `timestamp_tolerance_seconds`, `future_skew_seconds`, and `replay_cache_ttl_seconds`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/webhook/router_test.exs test/mailglass/config_test.exs test/mailglass/webhook/plug_mailgun_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The runtime can resolve Mailgun config, mount explicit Mailgun routes, and return 200 on replay without changing the default webhook mount surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend webhook test support for Mailgun signing and config setup</name>
  <read_first>test/support/webhook_case.ex, test/support/webhook_fixtures.ex, lib/mailglass/config.ex, .planning/phases/15-mailgun-webhook-provider/15-PATTERNS.md</read_first>
  <files>test/support/webhook_case.ex</files>
  <behavior>
    - `Mailglass.WebhookCase` can build a signed Mailgun conn from raw fixture bytes without storing signatures on disk.
    - Test setup installs and restores Mailgun provider config alongside SendGrid and Postmark.
  </behavior>
  <action>Update `test/support/webhook_case.ex` to snapshot and restore `Application.get_env(:mailglass, :mailgun)` in setup/on_exit, install default Mailgun test config `enabled: true`, `signing_key: "test-mailgun-signing-key"`, `timestamp_tolerance_seconds: 28_800`, `future_skew_seconds: 300`, and `replay_cache_ttl_seconds: 28_800`, and add `mailglass_webhook_conn(:mailgun, raw_body, opts)` that uses `Mailglass.WebhookFixtures.sign_mailgun_payload/3` to inject a top-level `"signature"` object into the JSON body while also setting `conn.private[:raw_body]` to the exact bytes sent. Keep the helper parallel to existing Postmark/SendGrid branches and re-export any needed Mailgun fixture helper through the existing imports.</action>
  <acceptance_criteria>
    - `test/support/webhook_case.ex` mentions `:mailgun` in setup restore logic.
    - `test/support/webhook_case.ex` defines `mailglass_webhook_conn(:mailgun, raw_body, opts)`.
    - The Mailgun setup config includes `signing_key`, `timestamp_tolerance_seconds`, `future_skew_seconds`, and `replay_cache_ttl_seconds`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/webhook/plug_mailgun_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Runtime-style tests can construct authentic Mailgun requests and install deterministic Mailgun config without leaking state across tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Mailgun plug, router, and config regression tests</name>
  <read_first>test/mailglass/webhook/plug_test.exs, test/mailglass/webhook/router_test.exs, test/mailglass/config_test.exs, test/support/webhook_case.ex, .planning/phases/15-mailgun-webhook-provider/15-CONTEXT.md</read_first>
  <files>test/mailglass/webhook/plug_mailgun_test.exs, test/mailglass/webhook/router_test.exs, test/mailglass/config_test.exs</files>
  <behavior>
    - Plug tests prove valid Mailgun requests return 200, replayed requests return 200 with replay metadata, bad signatures return 401, and missing config returns 500 without PII leakage.
    - Router tests prove explicit `providers: [:mailgun]` and mixed provider lists compile, while the zero-arg default route count remains unchanged.
    - Config tests prove `Mailglass.Config.new!/1` accepts the Mailgun subtree and rejects invalid Mailgun keys or types.
  </behavior>
  <action>Create `test/mailglass/webhook/plug_mailgun_test.exs` as a Mailgun-specific integration suite rather than overloading `plug_test.exs`; cover `WebhookPlug.init(provider: :mailgun)`, valid signed Mailgun request -> 200, same token reused -> 200 replay, bit-flipped signature -> 401 with `provider=mailgun`, and missing `signing_key` -> 500. Update `test/mailglass/webhook/router_test.exs` so the default router still asserts exactly two routes, add a positive router module that mounts `providers: [:postmark, :sendgrid, :mailgun]`, and replace the old `:mailgun` compile-failure expectation with an unknown provider that is still invalid. Extend `test/mailglass/config_test.exs` with Mailgun subtree acceptance and invalid-key/invalid-type checks for `signing_key`, `future_skew_seconds`, and `replay_cache_ttl_seconds`.</action>
  <acceptance_criteria>
    - `test/mailglass/webhook/plug_mailgun_test.exs` asserts replay returns HTTP 200 and does not assert 401 for replay.
    - `test/mailglass/webhook/router_test.exs` still asserts default route count `== 2` and includes a positive `:mailgun` mount case.
    - `test/mailglass/config_test.exs` contains a Mailgun subtree validation case and an invalid Mailgun config failure case.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/webhook/plug_mailgun_test.exs test/mailglass/webhook/router_test.exs test/mailglass/config_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Mailgun runtime integration is locked down by regression tests around route surface, replay semantics, and central config validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix route mount → webhook plug | Public webhook endpoints become reachable based on compile-time router configuration |
| Application env → runtime verifier | Secrets and tolerance values cross from adopter config into security-critical verification logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | D | `Mailglass.Webhook.Plug` | mitigate | Replay-specific `200` short-circuit prevents Mailgun retry amplification and keeps replays out of the generic 401 rescue path per D-05. |
| T-15-05 | T | `Mailglass.Webhook.Router` | mitigate | Keep `@default_providers` at `[:postmark, :sendgrid]` and require explicit `providers: [...]` to expose `:mailgun` per D-06. |
| T-15-06 | E | `Mailglass.Config` | mitigate | Validate the complete Mailgun subtree centrally, including numeric tolerance keys and presence/shape of `:signing_key`, so provider code never trusts arbitrary env values. |
</threat_model>

<verification>
Run `mix test test/mailglass/webhook/plug_mailgun_test.exs test/mailglass/webhook/router_test.exs test/mailglass/config_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
Mailgun is a replay-safe first-party webhook option behind explicit router opt-in, with runtime config centralized in `Mailglass.Config` and regression coverage around the response matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/15-mailgun-webhook-provider/15-02-SUMMARY.md`
</output>
