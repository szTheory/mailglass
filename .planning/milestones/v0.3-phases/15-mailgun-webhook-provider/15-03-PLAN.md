---
phase: 15-mailgun-webhook-provider
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - lib/mailglass/installer/templates.ex
  - guides/webhooks.md
  - test/mailglass/install/install_golden_test.exs
  - test/example/README.md
autonomous: true
requirements:
  - MAILGUN-01
  - MAILGUN-02
  - MAILGUN-03
must_haves:
  truths:
    - "Adopters see explicit Mailgun router and config snippets instead of inferring them from the codebase."
    - "Published webhook docs explain that Mailgun replay requests converge to HTTP 200 rather than 401."
    - "Installer-facing examples stay in sync with the explicit Mailgun opt-in surface."
  artifacts:
    - path: "lib/mailglass/installer/templates.ex"
      provides: "Installer webhook snippet showing explicit Mailgun opt-in"
    - path: "guides/webhooks.md"
      provides: "Published Mailgun setup and replay semantics guide"
    - path: "test/example/README.md"
      provides: "Golden installer snapshots updated for webhook snippet changes"
  key_links:
    - from: "lib/mailglass/installer/templates.ex"
      to: "test/example/README.md"
      via: "installer golden snapshot content"
      pattern: "mailglass_webhook_routes \"/webhooks\", providers:"
    - from: "guides/webhooks.md"
      to: "lib/mailglass/webhook/plug.ex"
      via: "documented Mailgun replay 200 response semantics"
      pattern: "200"
---

<objective>
Update installer and documentation surfaces so Mailgun adopters get the exact router/config/replay contract without reading internal code.

Purpose: Keep the public setup story aligned with the new explicit Mailgun opt-in behavior and replay semantics.
Output: Updated installer snippets, webhook guide changes, and refreshed installer goldens.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-mailgun-webhook-provider/15-CONTEXT.md
@.planning/phases/15-mailgun-webhook-provider/15-RESEARCH.md
@.planning/phases/15-mailgun-webhook-provider/15-PATTERNS.md
@.planning/phases/15-mailgun-webhook-provider/15-02-PLAN.md
@lib/mailglass/installer/templates.ex
@guides/webhooks.md
@test/mailglass/install/install_golden_test.exs
@test/example/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update installer snippets and webhook guide for explicit Mailgun opt-in</name>
  <read_first>lib/mailglass/installer/templates.ex, guides/webhooks.md, lib/mailglass/webhook/router.ex, lib/mailglass/config.ex, .planning/phases/15-mailgun-webhook-provider/15-CONTEXT.md</read_first>
  <files>lib/mailglass/installer/templates.ex, guides/webhooks.md</files>
  <action>Change `Mailglass.Installer.Templates.webhook_mount_snippet/1` so the generated router example uses `mailglass_webhook_routes "/webhooks", providers: [:postmark, :sendgrid, :mailgun]` when documenting Mailgun per D-07, while preserving the zero-arg default behavior in code per D-06. Update `guides/webhooks.md` to describe Mailgun as a shipped first-party verifier, add an explicit Mailgun setup section with config `config :mailglass, :mailgun, enabled: true, signing_key: System.fetch_env!("MAILGUN_WEBHOOK_SIGNING_KEY"), timestamp_tolerance_seconds: 28_800, future_skew_seconds: 300, replay_cache_ttl_seconds: 28_800`, explain that Mailgun verification reads the `"signature"` object from the JSON body, and document that replayed Mailgun tokens return HTTP `200` as an idempotent no-op rather than `401` per D-05. Preserve existing direct language and no-PII guidance from CLAUDE.md.</action>
  <acceptance_criteria>
    - `lib/mailglass/installer/templates.ex` contains `providers: [:postmark, :sendgrid, :mailgun]`.
    - `guides/webhooks.md` contains `MAILGUN_WEBHOOK_SIGNING_KEY`.
    - `guides/webhooks.md` documents replay requests as HTTP `200`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The public setup story explicitly shows how to mount and configure Mailgun and accurately describes its replay behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh installer golden expectations for the new webhook snippet</name>
  <read_first>test/mailglass/install/install_golden_test.exs, test/example/README.md, lib/mailglass/installer/templates.ex</read_first>
  <files>test/example/README.md, test/mailglass/install/install_golden_test.exs</files>
  <action>Refresh the installer golden snapshots in `test/example/README.md` so the `GOLDEN_FRESH` and any impacted webhook-related sections reflect the explicit Mailgun provider list emitted by the installer snippet. Only change `test/mailglass/install/install_golden_test.exs` if the assertion text or snapshot markers need to mention Mailgun explicitly; otherwise keep the test harness intact and update only the snapshot content.</action>
  <acceptance_criteria>
    - `test/example/README.md` includes the Mailgun provider list in the installer snapshot block.
    - `test/mailglass/install/install_golden_test.exs` still references `GOLDEN_FRESH` and `GOLDEN_NO_ADMIN` without broken markers.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors && rg -n 'providers: \\[:postmark, :sendgrid, :mailgun\\]|MAILGUN_WEBHOOK_SIGNING_KEY|replay.*200' lib/mailglass/installer/templates.ex guides/webhooks.md test/example/README.md</automated>
  </verify>
  <done>Installer snapshots and documentation examples are synchronized with the shipped Mailgun route/config contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs/snippets → adopter application setup | Public examples drive security-sensitive route and secret configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | T | installer snippets and guide text | mitigate | Publish the explicit `providers: [:postmark, :sendgrid, :mailgun]` snippet and exact Mailgun config keys so adopters do not infer an incorrect route or secret shape per D-07. |
| T-15-08 | R | webhook response matrix docs | mitigate | Document replay `200` semantics in the guide so operators can distinguish expected idempotent replays from signature failures and retry storms. |
</threat_model>

<verification>
Run `mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors` and grep for the Mailgun snippet/config strings in the updated docs.
</verification>

<success_criteria>
Installer-generated snippets, guide text, and committed golden snapshots all describe the same explicit Mailgun setup and replay behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/15-mailgun-webhook-provider/15-03-SUMMARY.md`
</output>
