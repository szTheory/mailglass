---
phase: 18-ship-v0-3-0
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - mailglass_admin/CHANGELOG.md
  - guides/webhooks.md
  - README.md
  - MAINTAINING.md
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
autonomous: true
requirements:
  - DELIV-04

must_haves:
  truths:
    - "The `mailglass` `0.3.0` changelog entry is maintainer-written, additive, and explicitly says no codemod / urgent migration story is implied"
    - "The `mailglass_admin` `0.3.0` changelog entry is short, version-coordinated, and does not invent an admin-only feature narrative"
    - "`guides/webhooks.md` documents Resend as an explicit opt-in provider with literal route/config examples and a `CachingBodyReader` reminder"
    - "No public doc still claims SES or Resend 'land later' or places Mailgun / SES / Resend verification in a future milestone"
    - "The release runbook and workflow comments describe the actual `0.3.0` fallback path without introducing new release mechanics"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "curated `0.3.0` entry plus removal or cleanup of misleading duplicate-history clutter"
      contains: "## [0.3.0]"
    - path: "mailglass_admin/CHANGELOG.md"
      provides: "coordinated sibling `0.3.0` release note"
      contains: "## [0.3.0]"
    - path: "guides/webhooks.md"
      provides: "Resend setup section with explicit opt-in routing and secret/config shape"
      contains: "### Resend setup"
    - path: "README.md"
      provides: "updated provider-coverage / milestone positioning for shipped webhook support"
      contains: "Resend"
    - path: "MAINTAINING.md"
      provides: "v0.3.0-aligned publish fallback wording"
      contains: "mailglass-v0.3.0"
  key_links:
    - from: "guides/webhooks.md"
      to: "lib/mailglass/webhook/router.ex"
      via: "Resend remains opt-in; zero-arg route mount stays narrow"
    - from: "guides/webhooks.md"
      to: "lib/mailglass/webhook/providers/resend.ex"
      via: "Svix headers, `whsec_...` secret shape, and timestamp tolerance wording"
    - from: "MAINTAINING.md"
      to: ".github/workflows/publish-hex.yml"
      via: "manual fallback dispatch uses the reviewed core release tag"
---

<objective>
Refresh the public release surface so `0.3.0` is truthful, copy-paste safe, and ready for the irreversible publish step.

Purpose: Phase 18 should ship only after the changelog, README, webhook guide, runbook, and workflow commentary all agree on the same public contract: webhook coverage is now complete, Resend is shipped but opt-in, and the publish/smoke path stays boring and auditable.

Output: A coherent `0.3.0` release narrative, a correct Resend provider guide, cleaned-up stale milestone wording, and v0.3.0-aligned publish fallback text.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/18-ship-v0-3-0/18-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/18-ship-v0-3-0/18-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/milestones/v0.2-phases/13-v0-2-release-ceremony/13-CONTEXT.md
@/Users/jon/projects/mailglass/CHANGELOG.md
@/Users/jon/projects/mailglass/mailglass_admin/CHANGELOG.md
@/Users/jon/projects/mailglass/guides/webhooks.md
@/Users/jon/projects/mailglass/README.md
@/Users/jon/projects/mailglass/MAINTAINING.md
@/Users/jon/projects/mailglass/.github/workflows/publish-hex.yml
@/Users/jon/projects/mailglass/.github/workflows/post-publish-smoke.yml
@/Users/jon/projects/mailglass/lib/mix/tasks/mailglass.publish.check.ex
@/Users/jon/projects/mailglass/lib/mailglass/webhook/router.ex
@/Users/jon/projects/mailglass/lib/mailglass/webhook/plug.ex
@/Users/jon/projects/mailglass/lib/mailglass/webhook/providers/resend.ex
@/Users/jon/projects/mailglass/lib/mailglass/webhook/caching_body_reader.ex
@/Users/jon/projects/mailglass/lib/mailglass/installer/templates.ex
@/Users/jon/projects/mailglass/test/mailglass/webhook/providers/resend_webhook_plug_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the curated `mailglass` and `mailglass_admin` `0.3.0` changelog entries</name>
  <files>CHANGELOG.md, mailglass_admin/CHANGELOG.md</files>
  <action>
    Add maintainer-written `0.3.0` entries for both packages.

    In `CHANGELOG.md`:
    - Open with a short maintainer summary that answers: what changed, who should care, and whether any urgent migration or codemod action is required.
    - Frame `0.3.0` as the webhook-coverage-complete release: Mailgun, SES, and Resend are now part of the supported public webhook ingest story.
    - Explicitly state this is not a `0.2.0`-style migration release.
    - Include a concise categorized ledger under the curated top section.
    - Clean up the duplicate generated `0.2.0` heading/stub so the changelog does not retain obvious release-note clutter.

    In `mailglass_admin/CHANGELOG.md`:
    - Add a short coordinated `0.3.0` entry that says the package remains version-paired with core.
    - Avoid inventing an admin-only migration story or claiming standalone user-visible changes unless the actual diff warrants it.
    - Clean up the duplicate generated `0.2.0` stub here as well.
  </action>
  <verify>
    <automated>test "$(rg -c '^## \\[0\\.2\\.0\\]' CHANGELOG.md)" = "1" && test "$(rg -c '^## \\[0\\.2\\.0\\]' mailglass_admin/CHANGELOG.md)" = "1" && rg -n "^## \\[0\\.3\\.0\\]" CHANGELOG.md mailglass_admin/CHANGELOG.md</automated>
  </verify>
  <done>The release notes read like a thoughtful maintainer update instead of a generated ledger, and the changelog history no longer carries obvious duplicate-release clutter.</done>
</task>

<task type="auto">
  <name>Task 2: Update the public webhook guide for shipped Resend support</name>
  <files>guides/webhooks.md</files>
  <action>
    Update `guides/webhooks.md` in place.

    Required outcomes:
    - Remove stale wording that says SES and Resend land later.
    - Preserve the existing guide structure: shared setup first, provider sections second.
    - Add a dedicated `### Resend setup` section with literal examples:
      - `mailglass_webhook_routes "/webhooks", providers: [:postmark, :sendgrid, :resend]`
      - `config :mailglass, :resend, enabled: true, secret: System.fetch_env!("RESEND_WEBHOOK_SECRET"), timestamp_tolerance_seconds: 300`
      - note that the secret value must look like `whsec_...`
      - short explanation of Svix header verification (`svix-id`, `svix-timestamp`, `svix-signature`)
      - supported normalized event coverage
    - Reinforce that `Mailglass.Webhook.CachingBodyReader` is required because Resend verification depends on exact raw request bytes.
    - Keep Resend explicit opt-in; do not imply the zero-arg route macro mounts it by default.
  </action>
  <verify>
    <automated>rg -n "### Resend setup|providers: \\[:postmark, :sendgrid, :resend\\]|whsec_|svix-id|CachingBodyReader" guides/webhooks.md && ! rg -n "SES and Resend land later" guides/webhooks.md</automated>
  </verify>
  <done>The webhook guide is copy-paste safe for Resend adopters and no longer contradicts the shipped provider surface.</done>
</task>

<task type="auto">
  <name>Task 3: Align README, runbook, and workflow comments with the `0.3.0` contract</name>
  <files>README.md, MAINTAINING.md, .github/workflows/publish-hex.yml, .github/workflows/post-publish-smoke.yml</files>
  <action>
    Update the remaining public-facing release surfaces so they agree with the `0.3.0` cut.

    In `README.md`:
    - Remove stale milestone language that still puts Mailgun / SES / Resend webhook verification in `v0.5`.
    - Keep the project positioning concise and truthful; do not turn the README into a release changelog.

    In `MAINTAINING.md` and the workflow comments:
    - Update any `0.2.0`-specific fallback examples to the current `0.3.0` release tag examples where doing so improves operator clarity.
    - Preserve the existing release mechanics and fallback path; do not redesign the workflows.
    - Keep the documented story aligned with `publish-hex.yml` and `post-publish-smoke.yml`.
  </action>
  <verify>
    <automated>! rg -n "v0\\.5 .*Mailgun|v0\\.5 .*SES|v0\\.5 .*Resend|SES and Resend land later" README.md guides/webhooks.md && rg -n "mailglass-v0\\.3\\.0|0\\.3\\.0" MAINTAINING.md .github/workflows/publish-hex.yml .github/workflows/post-publish-smoke.yml</automated>
  </verify>
  <done>The remaining public release surfaces tell one coherent `0.3.0` story and point the maintainer at the correct release-day fallback examples.</done>
</task>

<task type="auto">
  <name>Task 4: Run prepublish checks and a final consistency sweep</name>
  <files>CHANGELOG.md, mailglass_admin/CHANGELOG.md, guides/webhooks.md, README.md, MAINTAINING.md</files>
  <action>
    Run the repo-local publish gate for both packages:
    - `mix mailglass.publish.check --package mailglass`
    - `mix mailglass.publish.check --package mailglass_admin`

    Then do one consistency sweep:
    - changelog claims do not exceed actual provider/docs state
    - webhooks guide examples match router/provider reality
    - README / runbook wording does not lag the changelog
  </action>
  <verify>
    <automated>mix mailglass.publish.check --package mailglass && mix mailglass.publish.check --package mailglass_admin</automated>
  </verify>
  <done>The repository is ready for the real release cut and the publish gate reflects the final docs/changelog state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal webhook reality -> public docs | Misstating route defaults, raw-body requirements, or supported events would create adopter-facing setup failures |
| Release mechanics -> maintainer runbook | Stale fallback examples or divergent wording can mislead the real publish ceremony |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | release messaging | mitigate | Tie every `0.3.0` claim to shipped provider code and docs examples |
| T-18-02 | Repudiation | publish fallback path | mitigate | Keep runbook and workflow commentary aligned to the exact reviewed tag-dispatch contract |
</threat_model>

<verification>
`mix mailglass.publish.check` passes for both packages, the Resend docs are explicit and copy-paste safe, and no release-facing doc still describes provider coverage as future work.
</verification>

<success_criteria>
The repository is release-ready: `0.3.0` messaging is accurate, Resend is documented correctly, and the maintainer runbook matches the actual publish/smoke path.
</success_criteria>

<output>
After completion, create `.planning/phases/18-ship-v0-3-0/18-01-SUMMARY.md`
</output>
