---
phase: 66-release-position-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/66-release-position-decision/66-VERIFICATION.md
  - .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "Fresh Phase 66 release-blocking evidence exists for the inbound candidate instead of relying on prior-phase memory."
    - "The release-position record is binary and explicit: promote to `1.0.0` or fall back to one final `0.x` confidence release."
    - "The decision record cites Phase 63 inventory truth, Phase 64 proof-lane truth, Phase 65 docs/DX verification, current Hex release truth, and fresh Phase 66 verification commands."
  artifacts:
    - path: ".planning/phases/66-release-position-decision/66-VERIFICATION.md"
      provides: "Fresh release-blocking command evidence and verification tables"
    - path: ".planning/phases/66-release-position-decision/66-RELEASE-POSITION.md"
      provides: "Canonical binary release-position artifact with explicit promotion or fallback framing"
  key_links:
    - from: ".planning/phases/66-release-position-decision/66-VERIFICATION.md"
      to: "mix verify.stability_contract"
      via: "behavioral spot-check evidence"
      pattern: "mix verify\\.stability_contract"
    - from: ".planning/phases/66-release-position-decision/66-VERIFICATION.md"
      to: "mix mailglass.publish.check --package mailglass_inbound"
      via: "behavioral spot-check evidence"
      pattern: "mailglass\\.publish\\.check --package mailglass_inbound"
    - from: ".planning/phases/66-release-position-decision/66-RELEASE-POSITION.md"
      to: ".planning/phases/65-compatibility-docs-and-dx-lock/65-VERIFICATION.md"
      via: "committed evidence citations"
      pattern: "65-VERIFICATION|Phase 65"
---

<objective>
Create fresh Phase 66 release evidence and the single canonical binary release-position record before any version-truth edits happen.

Purpose: Satisfy REL-01 with committed, current evidence while keeping Phase 66 scoped to release governance per D-01 through D-05 and D-10 through D-11.
Output: `66-VERIFICATION.md` with fresh command evidence and `66-RELEASE-POSITION.md` with an explicit `1.0.0` or final `0.x` decision.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/66-release-position-decision/66-CONTEXT.md
@.planning/phases/66-release-position-decision/66-RESEARCH.md
@.planning/phases/66-release-position-decision/66-VALIDATION.md
@.planning/phases/66-release-position-decision/66-PATTERNS.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
@.planning/phases/64-contract-verification-hardening/64-CONTEXT.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-VERIFICATION.md

<interfaces>
From `mailglass_inbound/mix.exs`:
```elixir
@version "0.3.0"
"verify.stability_contract": ["verify.support_contract.inbound"]
{:mailglass, "== 1.3.0"} # when MIX_PUBLISH=true
```

From `mix.exs`:
```elixir
"verify.stability_contract": [
  "verify.support_contract.core",
  "cmd --cd mailglass_admin mix verify.support_contract.admin",
  "cmd --cd mailglass_inbound mix verify.support_contract.inbound",
  "mailglass.docs.check",
  "compile --no-optional-deps --warnings-as-errors"
]
```

From `lib/mix/tasks/mailglass.publish.check.ex`:
```elixir
defp packages("mailglass_inbound"), do: [:mailglass_inbound]
proof_summary_file: Path.join(repo_root, ".planning/publish/#{package}-publish-summary.json")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture fresh Phase 66 release-blocking evidence and current release truth</name>
  <read_first>
    .planning/phases/66-release-position-decision/66-CONTEXT.md
    .planning/phases/66-release-position-decision/66-RESEARCH.md
    .planning/phases/66-release-position-decision/66-VALIDATION.md
    .planning/phases/66-release-position-decision/66-PATTERNS.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-VERIFICATION.md
    mailglass_inbound/mix.exs
    mix.exs
    .release-please-manifest.json
    release-please-config.json
    .github/workflows/release-please.yml
    .github/workflows/publish-hex.yml
    lib/mix/tasks/mailglass.publish.check.ex
    .planning/publish/mailglass_inbound-publish-summary.json
  </read_first>
  <files>.planning/phases/66-release-position-decision/66-VERIFICATION.md</files>
  <action>Create `66-VERIFICATION.md` using the `65-VERIFICATION.md` report shape from `66-PATTERNS.md`. Per D-03 through D-05, rerun the existing repo-native release gates `mix verify.stability_contract` and `mix mailglass.publish.check --package mailglass_inbound`, then record the exact pass/fail posture, behavioral spot-check evidence, and blocker text if either command fails. Per D-04 and D-10, capture current package truth before any version edits by recording `mix hex.info mailglass_inbound 0.3.0`, the `mailglass_inbound/mix.exs` `@version`, the `.release-please-manifest.json` `mailglass_inbound` entry, and the current `.planning/publish/mailglass_inbound-publish-summary.json` version fields. Per D-11, add a source-truth note that release automation still runs through release-please plus the documented `workflow_dispatch` publish fallback; do not invent or bless a second publish path.</action>
  <acceptance_criteria>
    - `66-VERIFICATION.md` has frontmatter status plus Goal Achievement, Observable Truths, Behavioral Spot-Checks, and Requirements Coverage sections matching the established verification-report pattern.
    - The report includes fresh results for `mix verify.stability_contract` and `mix mailglass.publish.check --package mailglass_inbound`, plus current Hex/source truth showing `0.3.0` before candidate-version edits.
    - The report names any release blocker explicitly instead of collapsing failures into vague “not ready” wording.
    - The report includes a source assertion that release-please and the documented `workflow_dispatch` fallback remain the active automation topology.
    - Fast smoke check passes before the full release gates: `rg -n "mix verify\\.stability_contract|mix mailglass\\.publish\\.check --package mailglass_inbound|0\\.3\\.0|release-please|workflow_dispatch" .planning/phases/66-release-position-decision/66-VERIFICATION.md`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mix verify\\.stability_contract|mix mailglass\\.publish\\.check --package mailglass_inbound|0\\.3\\.0|release-please|workflow_dispatch" .planning/phases/66-release-position-decision/66-VERIFICATION.md && mix verify.stability_contract && mix mailglass.publish.check --package mailglass_inbound && mix hex.info mailglass_inbound 0.3.0</automated>
  </verify>
  <done>`66-VERIFICATION.md` becomes the fresh Phase 66 evidence artifact for the binary release decision, with current 0.3.0 truth and required command results recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write the canonical binary release-position artifact</name>
  <read_first>
    .planning/phases/66-release-position-decision/66-VERIFICATION.md
    .planning/phases/66-release-position-decision/66-CONTEXT.md
    .planning/phases/66-release-position-decision/66-RESEARCH.md
    .planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-VERIFICATION.md
    mailglass_inbound/docs/api_stability.md
    guides/compatibility-and-deprecations.md
  </read_first>
  <files>.planning/phases/66-release-position-decision/66-RELEASE-POSITION.md</files>
  <action>Create `66-RELEASE-POSITION.md` as the one canonical release-position record for Phase 66. Per D-01, if Task 1 shows both required gates green and no release-blocking drift, record promotion to `1.0.0` and state plainly, per D-08, that the promotion is justified because the stable contract is explicit, narrow, documented, and verified. Per D-02, if Task 1 exposes a real blocker, record the blocker, switch the candidate to one final explicit `0.4.0` confidence release, and use the exact framing “next is 1.0” as the fallback posture. Per D-03 and D-04, cite Phase 63 inventory reconciliation, Phase 64 compiled-doc/docs-contract proof, Phase 65 docs/DX verification, current Hex `0.3.0` truth, and the fresh Phase 66 evidence without reopening feature scope. Per D-07, route compatibility truth to `mailglass_inbound/docs/api_stability.md` and `guides/compatibility-and-deprecations.md` instead of creating a second canonical contract inside this artifact.</action>
  <acceptance_criteria>
    - `66-RELEASE-POSITION.md` contains one explicit active decision path: `1.0.0` promotion or `0.4.0` fallback, with any blocker named concretely if fallback is chosen.
    - The artifact cites Phase 63, Phase 64, Phase 65, current Hex `0.3.0` truth, and both required Phase 66 verification commands.
    - The artifact states the compatibility posture by linking to `mailglass_inbound/docs/api_stability.md` and `guides/compatibility-and-deprecations.md` rather than restating a second canonical stable/internal/deferred contract.
    - The artifact does not propose matcher expansion, lifecycle callbacks, public replay/provider APIs, worker/queue contracts, synthetic UI, `gen_smtp`, or ecosystem integrations.
    - Fast smoke check passes before any heavier validation: `rg -n "1\\.0\\.0|0\\.4\\.0|next is 1\\.0|api_stability\\.md|compatibility-and-deprecations\\.md" .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "1\\.0\\.0|0\\.4\\.0|next is 1\\.0|mix verify\\.stability_contract|mix mailglass\\.publish\\.check --package mailglass_inbound|api_stability\\.md|compatibility-and-deprecations\\.md|Phase 63|Phase 64|Phase 65" .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md</automated>
  </verify>
  <done>`66-RELEASE-POSITION.md` becomes the single source of truth for the Phase 66 binary release decision and its evidence basis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior proof artifacts -> Phase 66 decision record | Earlier phase evidence is being re-used to justify a publish-line decision; stale or contradictory inputs would corrupt the decision. |
| release-blocking commands -> verification artifact | The required Mix commands become release-governance truth for this phase. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-66-01 | Tampering | high | `66-VERIFICATION.md` evidence capture | mitigate | yes | Per D-05, rerun `mix verify.stability_contract` and `mix mailglass.publish.check --package mailglass_inbound`, capture current `0.3.0` truth, and refuse to rely on stale prior-phase memory; blocking acceptance is the combined automated verify command in Task 1. |
| T-66-02 | Repudiation | high | `66-RELEASE-POSITION.md` | mitigate | yes | Per D-01, D-02, and D-04, the decision artifact must name the active candidate version, cite Phase 63/64/65 evidence, cite fresh Phase 66 commands, and name blockers explicitly if fallback is chosen; blocking acceptance is the Task 2 `rg` verify command. |
| T-66-03 | Spoofing | medium | release automation narrative | mitigate | no | Per D-11, the verification artifact must confirm release-please plus the documented `workflow_dispatch` fallback remain the active release path and must not create a new manual publish story. |
</threat_model>

<verification>
- `mix verify.stability_contract`
- `mix mailglass.publish.check --package mailglass_inbound`
- `mix hex.info mailglass_inbound 0.3.0`
- `rg -n "1\\.0\\.0|0\\.4\\.0|next is 1\\.0|mix verify\\.stability_contract|mix mailglass\\.publish\\.check --package mailglass_inbound|api_stability\\.md|compatibility-and-deprecations\\.md|Phase 63|Phase 64|Phase 65" .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md`
</verification>

<success_criteria>
- Phase 66 has a fresh verification artifact instead of relying on previous milestone memory.
- The repo contains one canonical release-position record that states either `1.0.0` promotion or final `0.4.0` fallback and cites the exact evidence behind that choice.
</success_criteria>

<output>
Create `.planning/phases/66-release-position-decision/66-01-SUMMARY.md` when done
</output>
