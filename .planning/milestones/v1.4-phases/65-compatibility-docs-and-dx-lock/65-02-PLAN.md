---
phase: 65-compatibility-docs-and-dx-lock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/docs/inbound-operator.md
  - mailglass_inbound/docs/inbound-routing-debug.md
  - mailglass_inbound/docs/inbound-testing.md
  - mailglass_admin/docs/operator-trust.md
autonomous: true
requirements:
  - DX-02
  - DX-03
  - DX-04
must_haves:
  truths:
    - "Operators can understand doctor/replay/prune semantics, tenant guards, exit behavior, and destructive confirmations without mistaking replay for a fresh receive."
    - "Adopters understand `MailboxCase`, `Test.Ingress`, `async: false`, process-local capture, and one-assertion-per-drive behavior."
    - "Admin trust docs do not imply stable UI/DOM/component APIs or silent reroute semantics."
  artifacts:
    - path: "mailglass_inbound/docs/inbound-operator.md"
      provides: "Stable operator command semantics and guardrail wording"
    - path: "mailglass_inbound/docs/inbound-testing.md"
      provides: "Canonical testing harness and assertion-consumption guidance"
    - path: "mailglass_admin/docs/operator-trust.md"
      provides: "Admin/operator trust boundaries and non-contract framing"
  key_links:
    - from: "mailglass_inbound/docs/inbound-operator.md"
      to: "mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex"
      via: "documented CLI semantics"
      pattern: "mix mailglass\\.inbound\\.replay"
    - from: "mailglass_inbound/docs/inbound-testing.md"
      to: "mailglass_inbound/lib/mailglass_inbound/test_assertions.ex"
      via: "capture consumption and assertion semantics"
      pattern: "assert_inbound_"
    - from: "mailglass_admin/docs/operator-trust.md"
      to: "mailglass_inbound/docs/api_stability.md"
      via: "canonical trust-boundary routing"
      pattern: "mailglass_inbound/docs/api_stability\\.md"
---

<objective>
Tighten operator, troubleshooting, testing, and admin trust docs around the shipped semantics and explicit non-contract boundaries.

Purpose: Satisfy DX-02, DX-03, and DX-04 while keeping operator semantics stable at the command layer only and keeping testing/admin guidance honest about replay, tenancy, and UI contract boundaries.
Output: Updated operator, routing-debug, testing, and admin trust docs that speak with one precise semantics-first voice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-RESEARCH.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-PATTERNS.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-VALIDATION.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-UI-SPEC.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-01-SUMMARY.md
@.planning/phases/64-contract-verification-hardening/64-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock operator and troubleshooting docs to stable command semantics only</name>
  <read_first>
    mailglass_inbound/docs/inbound-operator.md
    mailglass_inbound/docs/inbound-routing-debug.md
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
    .planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/docs/inbound-operator.md, mailglass_inbound/docs/inbound-routing-debug.md</files>
  <action>Implement D-06, D-07, and D-08 by tightening `mailglass_inbound/docs/inbound-operator.md` and `mailglass_inbound/docs/inbound-routing-debug.md` around stable command semantics only. Follow the `65-PATTERNS.md` analogs for `mailglass_inbound/docs/inbound-operator.md`: keep the command-per-section layout, exit-code plus flags-table structure, and guardrail wording pattern; for `mailglass_inbound/docs/inbound-routing-debug.md`, reuse the role-match troubleshooting pattern that routes back to the operator guide instead of inventing a second command contract. Document `mix mailglass.inbound.doctor`, `mix mailglass.inbound.replay`, and `mix mailglass.inbound.prune` in terms of their documented flags and options, exit semantics, tenant guards, confirmation tiers, destructive confirmations, prune behavior, and replay-over-stored-truth semantics. Ensure replay is described as acting on stored inbound records rather than a fresh provider receipt and never as a silent reroute. Keep internal helpers, worker modules, queue names, retry tuning, direct Oban job shapes, and admin UI details explicitly non-contractual, and route troubleshooting readers back to the operator guide for the canonical command contract.</action>
  <acceptance_criteria>
    - `mailglass_inbound/docs/inbound-operator.md` contains explicit sections for `mix mailglass.inbound.doctor`, `mix mailglass.inbound.replay`, and `mix mailglass.inbound.prune` with exit-code or outcome semantics, flag tables, tenant-guard wording, and destructive confirmation language.
    - `mailglass_inbound/docs/inbound-routing-debug.md` links back to the operator guide for command semantics and does not describe replay as a fresh provider receive or silent reroute.
    - Neither document promotes `MailglassInbound.Internal.*`, worker or queue details, retry tuning, or admin UI implementation details into the stable contract.
  </acceptance_criteria>
  <verify>
    <automated>mix mailglass.docs.check</automated>
  </verify>
  <done>Operator-facing docs describe the shipped CLI contract precisely and keep troubleshooting details inside the intended trust boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify the inbound testing harness and admin trust boundaries</name>
  <read_first>
    mailglass_inbound/docs/inbound-testing.md
    mailglass_admin/docs/operator-trust.md
    mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
    mailglass_inbound/lib/mailglass_inbound/test/ingress.ex
    mailglass_inbound/lib/mailglass_inbound/test_assertions.ex
    mailglass_inbound/docs/api_stability.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/docs/inbound-testing.md, mailglass_admin/docs/operator-trust.md</files>
  <action>Implement D-08 through D-11 by making `mailglass_inbound/docs/inbound-testing.md` and `mailglass_admin/docs/operator-trust.md` describe the shipped adopter harness and admin trust boundaries exactly. Follow the `65-PATTERNS.md` analogs for `mailglass_inbound/docs/inbound-testing.md` MailboxCase default-harness plus one-assertion-per-drive contract patterns, and for `mailglass_admin/docs/operator-trust.md` the stable-seams versus intentionally-internal split plus replay-semantics boundary wording. Center the testing guide on `MailglassInbound.MailboxCase` and `MailglassInbound.Test.Ingress`, make the process-local capture semantics explicit, require `async: false`, explain why each consuming assertion uses one capture, and show that each assertion must drive a fresh inbound message. In `mailglass_admin/docs/operator-trust.md`, keep replay framed as stored-truth recovery that can yield `new work` or `no change` but is not a fresh provider receipt, not a silent reroute, and not a public replay API; keep DOM, components, assigns, LiveView modules, routes, and CSS labeled as implementation detail rather than stable UI contract.</action>
  <acceptance_criteria>
    - `mailglass_inbound/docs/inbound-testing.md` contains `use MailglassInbound.MailboxCase, async: false`, `Test.Ingress.receive_inbound`, explicit process-local capture wording, and one-assertion-per-drive examples that use fresh drives/messages.
    - `mailglass_admin/docs/operator-trust.md` states replay may create `new work` or `no change`, frames replay as stored-truth recovery rather than fresh receipt, and labels UI/DOM/component/LiveView details as implementation detail only.
    - Neither document implies stable DOM/component APIs, public replay API guarantees, or silent reroute behavior.
  </acceptance_criteria>
  <verify>
    <automated>mix mailglass.docs.check</automated>
  </verify>
  <done>Testing and admin trust docs are explicit about capture semantics, replay semantics, and the boundary between stable operator behavior and internal UI/runtime details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/operator docs -> tenant-affecting operator actions | Operators may follow docs directly for replay and prune commands that affect tenant-scoped stored inbound data. |
| testing/admin docs -> adopter expectations | Adopters may build tests or admin workflows around whatever the docs imply is stable. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-65-04 | Elevation of Privilege | high | replay/prune operator docs | mitigate | yes | Blocking acceptance: operator docs keep tenant guards, confirmation tiers, exit semantics, and destructive behavior explicit, and `mix mailglass.docs.check` passes after the wording changes. |
| T-65-05 | Tampering | high | prune/replay semantics | mitigate | yes | Blocking acceptance: replay remains stored-truth recovery, prune remains explicitly destructive, and troubleshooting docs do not imply silent reroute or fresh receive semantics. |
| T-65-06 | Repudiation | high | testing/admin trust docs | mitigate | yes | Blocking acceptance: testing docs encode `async: false`, process-local capture, and one-assertion-per-drive semantics, while admin trust docs keep UI/DOM/component surfaces non-contractual, and `mix mailglass.docs.check` passes. |
</threat_model>

<verification>
- `mix mailglass.docs.check`
</verification>

<success_criteria>
- Operator-facing docs describe the stable command contract clearly enough that replay/prune behavior cannot be misread across tenants.
- Testing and admin trust docs stay precise about capture semantics, replay semantics, and non-contract UI/runtime details.
</success_criteria>

<output>
Create `.planning/phases/65-compatibility-docs-and-dx-lock/65-02-SUMMARY.md` when done
</output>
