---
phase: 65-compatibility-docs-and-dx-lock
plan: 04
type: execute
wave: 3
depends_on:
  - 65-02
  - 65-03
files_modified:
  - mailglass_inbound/docs/inbound-operator.md
  - mailglass_inbound/docs/inbound-testing.md
  - mailglass_admin/docs/operator-trust.md
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
  - lib/mix/tasks/mailglass.docs.check.ex
autonomous: true
requirements:
  - DX-02
  - DX-03
  - DX-04
must_haves:
  truths:
    - "Operator, testing, and admin trust wording is locked by the existing inbound docs-contract lane and Tier 1 docs checker."
    - "Replay stays documented as stored-truth recovery, not a fresh receive or silent reroute."
    - "Testing semantics stay explicit about process-local capture and one-assertion-per-drive behavior."
  artifacts:
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Fail-closed operator/testing/admin trust wording checks"
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Tier 1 required-token enforcement for operator/testing/admin trust docs"
  key_links:
    - from: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      to: "mailglass_inbound/docs/inbound-operator.md"
      via: "required and forbidden replay/prune/testing assertions"
      pattern: "mix mailglass\\.inbound\\.(doctor|replay|prune)"
    - from: "lib/mix/tasks/mailglass.docs.check.ex"
      to: "mailglass_admin/docs/operator-trust.md"
      via: "Tier 1 trust-boundary rules"
      pattern: "implementation detail"
---

<objective>
Lock operator, testing, and admin trust wording into the existing inbound docs-contract seam and the root Tier 1 docs checker.

Purpose: Make DX-02, DX-03, and DX-04 fail closed on future wording drift without inventing a new checker or widening the public contract.
Output: Executable docs checks for operator semantics, testing harness semantics, replay wording, and non-contract UI boundaries.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-RESEARCH.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-PATTERNS.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-VALIDATION.md
@.planning/phases/64-contract-verification-hardening/64-04-SUMMARY.md
@.planning/phases/64-contract-verification-hardening/64-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the inbound docs-contract test for operator, testing, and admin trust semantics</name>
  <read_first>
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    mailglass_inbound/docs/inbound-operator.md
    mailglass_inbound/docs/inbound-testing.md
    mailglass_admin/docs/operator-trust.md
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
    mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
    mailglass_inbound/lib/mailglass_inbound/test/ingress.ex
    mailglass_inbound/lib/mailglass_inbound/test_assertions.ex
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/docs/inbound-operator.md, mailglass_inbound/docs/inbound-testing.md, mailglass_admin/docs/operator-trust.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <action>Extend `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` to encode D-06 through D-11 as package-local contract checks over `mailglass_inbound/docs/inbound-operator.md`, `mailglass_inbound/docs/inbound-testing.md`, and `mailglass_admin/docs/operator-trust.md`. Follow the exact `65-PATTERNS.md` analog for `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`: preserve the current path-constant setup, reuse the existing docs-contract assertion helpers, and extend them with the shared Docs Contract Enforcement pattern rather than inventing a new checker style. Add path constants plus assertions that operator docs include the stable command names, tenant guards, dry-run and confirmation semantics, exit semantics, and stored-truth replay framing; testing docs include `MailglassInbound.MailboxCase`, `MailglassInbound.Test.Ingress`, `async: false`, process-local capture semantics, and one-assertion-per-drive wording; admin trust docs include `new work`, `no change`, canonical stability links, `mix verify.stability_contract`, and implementation-detail framing for UI internals. Use paragraph-scoped or section-scoped helpers where needed so the test fails on over-claims such as fresh-receive replay, silent reroute, public replay API, or stable DOM/component/LiveView guarantees without forbidding honest negative framing. Tune the three docs only where the new assertions expose real semantic drift.</action>
  <acceptance_criteria>
    - `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` reads the operator, testing, and admin trust docs directly and asserts the stable CLI, testing-harness, and non-contract UI boundaries.
    - The new assertions require `async: false`, process-local capture semantics, one-assertion-per-drive language, tenant guards, destructive confirmation semantics, `new work`/`no change`, and canonical stability routing.
    - The new assertions fail on replay-as-fresh wording, silent-reroute wording, public replay API claims, or stable DOM/component/LiveView guarantees while still allowing explicit negative boundary phrasing.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The package-local inbound docs-contract lane now fails on operator/testing/admin trust wording drift instead of relying on manual prose review.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Tier 1 docs-check rules for operator, testing, and admin trust docs</name>
  <read_first>
    lib/mix/tasks/mailglass.docs.check.ex
    mailglass_inbound/docs/inbound-operator.md
    mailglass_inbound/docs/inbound-testing.md
    mailglass_admin/docs/operator-trust.md
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
  </read_first>
  <files>lib/mix/tasks/mailglass.docs.check.ex</files>
  <action>Update `lib/mix/tasks/mailglass.docs.check.ex` so Tier 1 required-token enforcement matches the final operator/testing/admin trust story from Task 1. Follow the exact `65-PATTERNS.md` analog for `lib/mix/tasks/mailglass.docs.check.ex`: keep the edits in `@tier1_paths` and `@tier1_surface_rules`, using the shared Tier-1 Drift Guard pattern of exact required tokens plus narrow forbidden tokens only where they are safe. For `mailglass_inbound/docs/inbound-operator.md`, require the three command names plus tenant, retention, dry-run, and confirmation phrasing that matches the shipped command contract. For `mailglass_inbound/docs/inbound-testing.md`, require `use MailglassInbound.MailboxCase`, `Test.Ingress.receive_inbound`, `async: false`, process-local wording, and one-assertion-per-drive wording. For `mailglass_admin/docs/operator-trust.md`, require canonical stability links, `mix verify.stability_contract`, `new work`, `no change`, and implementation-detail framing for UI internals. Prefer exact required phrases over naive forbidden strings when the safety property depends on negative framing such as “not a fresh provider receipt” or “does not silently reroute.”</action>
  <acceptance_criteria>
    - `lib/mix/tasks/mailglass.docs.check.ex` required-token rules for the inbound operator/testing docs and `mailglass_admin/docs/operator-trust.md` match the final wording contract from Task 1.
    - The checker requires exact negative-boundary phrasing where raw forbidden tokens would create false positives, especially around replay semantics and UI internals.
    - Running `mix mailglass.docs.check` after the updates validates the operator/testing/admin trust story without introducing a new checker.
  </acceptance_criteria>
  <verify>
    <automated>mix mailglass.docs.check</automated>
  </verify>
  <done>The root Tier 1 docs checker now reinforces the operator, testing, and admin trust boundaries that the package-local docs-contract test asserts in detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/testing/admin wording -> release-blocking docs checks | These docs define how adopters reason about tenant-scoped replay/prune behavior, testing semantics, and UI stability. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-65-10 | Elevation of Privilege | high | operator docs contract | mitigate | yes | Blocking acceptance: package-local docs-contract assertions require tenant guards, exit semantics, dry-run/confirmation behavior, and stored-truth replay framing, and `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` passes. |
| T-65-11 | Spoofing | high | replay/admin trust wording | mitigate | yes | Blocking acceptance: both docs-contract and Tier 1 checks require negative-boundary replay phrasing and reject fresh-receive, silent-reroute, and stable-UI overclaims, and `mix mailglass.docs.check` passes. |
| T-65-12 | Repudiation | high | testing harness docs | mitigate | yes | Blocking acceptance: the testing guide is locked to `MailglassInbound.MailboxCase`, `Test.Ingress`, `async: false`, process-local capture semantics, and one-assertion-per-drive language, and both verification commands pass. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- `mix mailglass.docs.check`
- `mix verify.stability_contract`
</verification>

<success_criteria>
- Operator, testing, and admin trust wording is protected by the same existing contract seams already used for inbound docs truth.
- Replay, tenant, destructive-action, and non-contract UI semantics cannot drift silently in Tier 1 docs.
</success_criteria>

<output>
Create `.planning/phases/65-compatibility-docs-and-dx-lock/65-04-SUMMARY.md` when done
</output>
