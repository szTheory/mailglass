---
phase: 64-contract-verification-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/docs/api_stability.md
  - mailglass_inbound/README.md
  - mailglass_inbound/docs/inbound-install.md
  - mailglass_inbound/CHANGELOG.md
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
autonomous: true
requirements:
  - PROOF-02
  - PROOF-03
must_haves:
  truths:
    - "Inbound closed structured-error `:type` sets stay locked to the canonical docs inventory."
    - "Inbound adoption and stability docs fail closed on over-claims and stale release-line pins."
  artifacts:
    - path: "mailglass_inbound/docs/api_stability.md"
      provides: "Canonical closed-set and contract-language source of truth"
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Primary docs drift, closed-set, and over-claim proof lane"
    - path: "mailglass_inbound/docs/inbound-install.md"
      provides: "Canonical install pin and adoption prose for the current package line"
  key_links:
    - from: "mailglass_inbound/docs/api_stability.md"
      to: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      via: "section parsing plus exact `__types__/0` comparisons"
      pattern: "Closed `:type` set:"
    - from: "mailglass_inbound/README.md"
      to: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      via: "dep-pin and forbidden-claim assertions"
      pattern: "~> 0\\.3"
---

<objective>
Harden the package-local inbound docs contract against closed-set drift, over-claims, and stale release-line truth.

Purpose: Make `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` the primary fail-closed proof seam for PROOF-02 and PROOF-03.
Output: Canonical docs and docs-contract tests that lock the stable error sets, current install pins, and allowed contract language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-verification-hardening/64-CONTEXT.md
@.planning/phases/64-contract-verification-hardening/64-RESEARCH.md
@.planning/phases/64-contract-verification-hardening/64-PATTERNS.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-01-SUMMARY.md

<interfaces>
Closed type sets that the docs parser must compare exactly, in order:
```elixir
MailglassInbound.MIMEError.__types__() == [:inbound_mime_invalid, :gen_smtp_unavailable]
MailglassInbound.SignatureError.__types__() == [:bad_signature, :missing_header, :malformed_header, :timestamp_skew, :subscribe_url_untrusted]
MailglassInbound.S3FetchError.__types__() == [:s3_object_not_ready, :s3_fetch_failed]
```

Current dep-pin targets from package truth:
```elixir
mailglass_inbound version: 0.3.0 -> README/install pin `~> 0.3`
mailglass_dep publish pin: {:mailglass, "== 1.3.0"} -> README/install pin `~> 1.3`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing MIME closed-set docs and centralize exact docs/code comparison</name>
  <read_first>
    mailglass_inbound/docs/api_stability.md
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    mailglass_inbound/lib/mailglass_inbound/mime_error.ex
    mailglass_inbound/lib/mailglass_inbound/signature_error.ex
    mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/docs/api_stability.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <action>Implement D-06 through D-10 inside the inbound docs-contract seam: add an explicit `Closed :type set` list to the `MailglassInbound.MIMEError` section with exactly ```:inbound_mime_invalid``` then ```:gen_smtp_unavailable```; extend `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` to parse each error-module section in `docs/api_stability.md`, extract the ordered bullet list, and compare it exactly to `MailglassInbound.MIMEError.__types__/0`, `MailglassInbound.SignatureError.__types__/0`, and `MailglassInbound.S3FetchError.__types__/0` rendered as backticked atom tokens. Keep the proof inside `docs_contract_test.exs` (not a generated file) so the docs remain canonical and failures stay diagnostic.</action>
  <acceptance_criteria>
    - The `MailglassInbound.MIMEError` section in `mailglass_inbound/docs/api_stability.md` contains a `Closed :type set:` block with ```:inbound_mime_invalid``` followed by ```:gen_smtp_unavailable```.
    - `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` contains one centralized assertion that checks the exact ordered closed set for `MIMEError`, `SignatureError`, and `S3FetchError`.
    - The centralized assertion compares docs tokens to `__types__/0` output rather than generating docs from code or code from docs.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/mime_error_test.exs test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The canonical inbound stability inventory explicitly documents every stable structured-error closed set and the docs-contract lane fails on any future code/docs mismatch.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh current release truth and section-scoped over-claim guards in the adoption docs</name>
  <read_first>
    mailglass_inbound/README.md
    mailglass_inbound/docs/inbound-install.md
    mailglass_inbound/CHANGELOG.md
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    mailglass_inbound/mix.exs
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/inbound-install.md, mailglass_inbound/CHANGELOG.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <action>Implement D-11 through D-15 by making the docs-contract lane the primary guard for current inbound release truth and over-claim posture: update the README install snippet to `{:mailglass_inbound, "~> 0.3"}` and `{:mailglass, "~> 1.3"}`; update `docs/inbound-install.md` to the same pins; rewrite the `## [Unreleased]` lead in `CHANGELOG.md` so it references the current shipped `0.3.0` line and the still-true `0.x` posture without stale `0.2.0` or pre-1.0 expansion language; extend `docs_contract_test.exs` with structured dep-pin comparisons against `Mix.Project.config()[:version]` and `mailglass_inbound/mix.exs`, plus section-scoped forbidden-claim checks that block public replay API claims, stable worker/queue claims, provider-module extension claims, replay-as-fresh wording, synthetic/operator UI shipped claims, and inbound `1.x` stability claims in `stable`, README, install-guide, and `CHANGELOG.md` `Unreleased` prose. Keep those same phrases allowed in `### deferred` when explicitly framed as not promised.</action>
  <acceptance_criteria>
    - `mailglass_inbound/README.md` and `mailglass_inbound/docs/inbound-install.md` both contain `{:mailglass_inbound, "~> 0.3"}` and `{:mailglass, "~> 1.3"}`.
    - `mailglass_inbound/CHANGELOG.md` `## [Unreleased]` no longer claims `0.2.0` is the current line or frames synthetic UI / other deferred features as the active pre-1.0 roadmap.
    - `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` asserts the current dep pins and restricts forbidden phrases to the stable/adoption/current-release sections while explicitly allowing deferred-language references in the `deferred` section.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The primary inbound docs-contract lane blocks stale package pins, stale release-line prose, and contract over-claims without forbidding honest deferred-language references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical docs -> contract proof | The canonical inbound inventory and adoption docs feed directly into the fail-closed `docs_contract_test.exs` lane. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-64-07 | Tampering | high | inbound contract docs | mitigate | yes | Blocking acceptance: `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` performs exact ordered closed-set comparisons for MIME, Signature, and S3 against `__types__/0`, and `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/mime_error_test.exs test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs --warnings-as-errors` passes. |
| T-64-08 | Repudiation | high | release-line and install truth | mitigate | yes | Blocking acceptance: README/install pins are refreshed to `~> 0.3` and `~> 1.3`, `CHANGELOG.md` `Unreleased` reflects the current `0.3.0` posture, and `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` passes. |
| T-64-09 | Elevation of privilege | high | stable/deferred boundary | mitigate | yes | Blocking acceptance: section-scoped forbidden-claim checks fail on replay/worker/provider/operator-UI/`1.x` over-claims in stable and adoption prose while still permitting clearly deferred references, and the docs-contract lane passes with those scoped checks enabled. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/mime_error_test.exs test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- The inbound docs contract fails closed on closed-set drift, stale dep pins, stale release-line prose, and stable-surface over-claims.
- The `deferred` section remains an allowed place to name future capabilities without implying they are shipped.
</success_criteria>

<output>
Create `.planning/phases/64-contract-verification-hardening/64-04-SUMMARY.md` when done
</output>
