---
phase: 64-contract-verification-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound.ex
  - mailglass_inbound/lib/mailglass_inbound/inbound_message.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  - mailglass_inbound/lib/mailglass_inbound/router.ex
  - mailglass_inbound/lib/mailglass_inbound/mailbox.ex
  - mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Stable inbound runtime seams expose package-version compiled-doc metadata."
    - "Runtime entrypoints adopters call directly carry exact `since` tags."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound.ex"
      provides: "Package identity metadata pinned to the inbound package line"
    - path: "mailglass_inbound/lib/mailglass_inbound/inbound_message.ex"
      provides: "Stable value-object metadata for the root inbound record contract"
    - path: "mailglass_inbound/lib/mailglass_inbound/router.ex"
      provides: "Stable macro metadata for the router authoring seam"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/router.ex"
      to: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      via: "Code.fetch_docs/1 macro metadata assertions"
      pattern: "defmacro (?:__using__|route)"
    - from: "mailglass_inbound/lib/mailglass_inbound/mailbox.ex"
      to: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      via: "callback metadata assertions"
      pattern: "@callback process"
---

<objective>
Normalize package-version `since` metadata on the stable inbound runtime seams.

Purpose: Give the Phase 64 compiled-doc proof a truthful source-level contract for the stable runtime entrypoints introduced in `0.1.0` and the later `0.2.0` additions.
Output: Stable runtime modules, macros, callbacks, and public helper functions tagged with exact package-line `since` metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/64-contract-verification-hardening/64-CONTEXT.md
@.planning/phases/64-contract-verification-hardening/64-RESEARCH.md
@.planning/phases/64-contract-verification-hardening/64-PATTERNS.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-01-SUMMARY.md

<interfaces>
From `mailglass_inbound/lib/mailglass_inbound.ex`:
```elixir
def version() :: String.t()
```

From `mailglass_inbound/lib/mailglass_inbound/inbound_message.ex`:
```elixir
def suppression_flagged?(t()) :: boolean()
```

From `mailglass_inbound/lib/mailglass_inbound/router.ex`:
```elixir
defmacro __using__(_opts)
defmacro route(mailbox, opts)
```

From `mailglass_inbound/lib/mailglass_inbound/mailbox.ex`:
```elixir
@callback process(InboundMessage.t()) :: outcome()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct package-line `since` metadata on identity, value-object, and PubSub seams</name>
  <read_first>
    mailglass_inbound/lib/mailglass_inbound.ex
    mailglass_inbound/lib/mailglass_inbound/inbound_message.ex
    mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-PATTERNS.md
  </read_first>
  <files>mailglass_inbound/lib/mailglass_inbound.ex, mailglass_inbound/lib/mailglass_inbound/inbound_message.ex, mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex</files>
  <action>Correct existing inbound compiled-doc metadata to the package version line per D-02, D-03, and the file-introduction history already visible in `git log`: set `MailglassInbound` `@moduledoc since:` and `version/0` `@doc since:` to `0.1.0`; add `@moduledoc since: "0.1.0"` to `MailglassInbound.InboundMessage`; change `suppression_flagged?/1` `@doc since:` from `1.2.0` to `0.2.0` because that helper arrived in Phase 45 / package `0.2.0`; add `@moduledoc since: "0.2.0"` to `MailglassInbound.PubSub.Topics` and preserve `inbound_record_inserted/1` at `0.2.0`. Do not add metadata for nested `Signals` structs, generated implementation helpers, or undocumented internals.</action>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound.ex` contains `@moduledoc since: "0.1.0"` and `@doc since: "0.1.0"` above `version/0`.
    - `mailglass_inbound/lib/mailglass_inbound/inbound_message.ex` contains `@moduledoc since: "0.1.0"` and `suppression_flagged?/1` is tagged `@doc since: "0.2.0"`.
    - `mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex` contains `@moduledoc since: "0.2.0"` and keeps `inbound_record_inserted/1` at `0.2.0`.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Package identity, value-object, and PubSub compiled-doc metadata match the inbound package release line and exclude non-contract helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime seam metadata only where adopters call the surface directly</name>
  <read_first>
    mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex
    mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
    mailglass_inbound/lib/mailglass_inbound/router.ex
    mailglass_inbound/lib/mailglass_inbound/mailbox.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-PATTERNS.md
  </read_first>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/caching_body_reader.ex, mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/lib/mailglass_inbound/router.ex, mailglass_inbound/lib/mailglass_inbound/mailbox.ex</files>
  <action>Implement the D-02 and D-03 scope boundary on the runtime seams introduced in package `0.1.0`: add `@moduledoc since: "0.1.0"` to `MailglassInbound.Ingress.CachingBodyReader`, `MailglassInbound.Ingress.Plug`, `MailglassInbound.Router`, and `MailglassInbound.Mailbox`; add `@doc since: "0.1.0"` only to the direct adopter-facing entrypoints `read_body/2`, `__using__/1`, `route/2`, and callback `process/1`; keep `init/1`, `call/2`, `__before_compile__/1`, `validate_matcher/1`, `valid_outcome?/1`, and every `@doc false` helper outside the compiled-doc contract.</action>
  <acceptance_criteria>
    - Each of the four files contains `@moduledoc since: "0.1.0"`.
    - `read_body/2`, `__using__/1`, `route/2`, and `process/1` each have an explicit `@doc since: "0.1.0"` or callback-doc metadata token.
    - `call/2`, `init/1`, `__before_compile__/1`, `validate_matcher/1`, and `valid_outcome?/1` remain unasserted implementation helpers rather than newly documented stable entrypoints.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/caching_body_reader_test.exs test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The runtime routing, ingress, and mailbox seams carry exact `0.1.0` compiled-doc metadata without widening the public contract past the locked direct entrypoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source annotations -> compiled docs | Stable runtime modules and entrypoints cross from source comments into executable contract proof via `Code.fetch_docs/1`. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-64-01 | Tampering | high | stable runtime metadata | mitigate | yes | Blocking acceptance: the exact `0.1.0` and `0.2.0` `@moduledoc since:` / `@doc since:` tokens from Tasks 1-2 are present in the seven runtime files, and `cd mailglass_inbound && mix compile --warnings-as-errors` passes. |
| T-64-02 | Repudiation | medium | version-history truth for public seams | mitigate | no | Correct the stale runtime tags called out in Task 1 before the aggregate proof lane ships so package history, not milestone numbering, defines the public runtime contract. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix compile --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/caching_body_reader_test.exs test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- Stable runtime seams introduced in `0.1.0` and `0.2.0` expose exact source metadata for the later compiled-doc proof.
- No internal or generated runtime helper is accidentally promoted into the stable metadata contract.
</success_criteria>

<output>
Create `.planning/phases/64-contract-verification-hardening/64-01-SUMMARY.md` when done
</output>
