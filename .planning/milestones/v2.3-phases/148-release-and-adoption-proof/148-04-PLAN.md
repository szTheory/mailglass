---
phase: 148-release-and-adoption-proof
plan: 04
type: execute
wave: 4
depends_on:
  - 148-03
files_modified:
  - .planning/release-target.json
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - test/scripts/release_trigger_recovery_test.exs
  - test/scripts/linked_release_concurrency_test.exs
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "Machine gates, not human judgment, authorize the protected release."
    - "Release Please auto-merges only an exact core/admin 2.4.0 and inbound 2.1.1 target."
    - "Publication remains fail-closed behind tagged-SHA CI and the protected hex-publish environment."
  artifacts:
    - path: ".planning/release-target.json"
      provides: "Machine-readable exact package target"
      contains: '"status": "active"'
    - path: ".github/workflows/release-please.yml"
      provides: "Pre-auto-merge target validation"
      contains: "Validate automated release target"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Release-SHA validation and derived proof versions"
      contains: "steps.release-target.outputs.active"
  prohibitions:
    - "MUST NOT require human approval or UAT after machine gates pass."
    - "MUST NOT run mix hex.publish locally, dispatch against main, or create tags manually."
    - "MUST NOT publish mailglass_inbound on a release event."
---

<objective>
Replace the stale human go/no-go with an exact, machine-readable release target and let the established protected Release Please path run hands-free.

Purpose: Make the irreversible edge safer through deterministic validation rather than operator ceremony.
Output: A protected implementation PR, an auto-merged Release Please PR, and externally running publish/smoke workflows.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
@.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
@.planning/phases/148-release-and-adoption-proof/148-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce the exact release target before auto-merge and publication</name>
  <files>.planning/release-target.json, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml, test/scripts/release_trigger_recovery_test.exs, test/scripts/linked_release_concurrency_test.exs</files>
  <action>Create an active target for mailglass 2.4.0, mailglass_admin 2.4.0, and mailglass_inbound 2.1.1. Validate the generated Release Please branch manifest and all three source versions before arming auto-merge. Revalidate the release checkout and tag before protected publication. Derive the sanitized proof artifact's versions from validated source files instead of hardcoded strings.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs test/scripts/linked_release_concurrency_test.exs --warnings-as-errors &amp;&amp; actionlint .github/workflows/release-please.yml .github/workflows/publish-hex.yml .github/workflows/post-publish-smoke.yml</automated>
  </verify>
  <done>A wrong version, wrong tag, changed inbound version, missing target, red CI run, or failed prepublish check stops before Hex credentials are used.</done>
</task>

<task type="auto">
  <name>Task 2: Run and observe the established protected release chain</name>
  <files>.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md</files>
  <action>Integrate the implementation through a protected PR with auto-merge. Observe Release Please create and auto-merge its linked-version PR after required checks. Observe release.published, publish-hex.yml, and post-publish-smoke.yml to completion. Automatically diagnose and fix deterministic failures through scoped commits; use bounded retries for external outages. Never weaken or override a release-event gate.</action>
  <verify>
    <automated>gh release view mailglass-v2.4.0 --json tagName,url,isDraft,isPrerelease &amp;&amp; gh release view mailglass_admin-v2.4.0 --json tagName,url,isDraft,isPrerelease &amp;&amp; test "$(gh release view mailglass_inbound-v2.4.0 --json tagName 2&gt;/dev/null || true)" = ""</automated>
  </verify>
  <done>Both intended releases and protected runs exist, inbound 2.4.0 does not exist, and the run URLs are available for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release target → Release Please PR | Exact manifest and source versions must match before auto-merge. |
| protected release event → Hex | Only tag-bound jobs behind green CI and `hex-publish` may publish. |

## ASVS L1 Release Gate

V4 is covered by protected refs, required checks, and the `hex-publish` environment. V5 is covered by exact semver, manifest, source-version, and tag validation. Any mismatch fails closed automatically.

## STRIDE Threat Register

| Threat ID | Category | Severity | Mitigation |
|-----------|----------|----------|------------|
| T-148-15 | Repudiation | high | Target JSON, release SHA, workflow runs, and proof artifact provide machine-verifiable provenance. |
| T-148-16 | Tampering | high | Exact target validation blocks accidental inbound or wrong-version release. |
| T-148-17 | Information Disclosure / Elevation | high | Credentials remain step-local inside `hex-publish`; no local publication. |
| T-148-18 | Denial of Service | high | Existing self-dispatch recovery, static concurrency, idempotency, and bounded retries remain mandatory. |
</threat_model>

<verification>
The autonomous chain returns concrete release and run URLs while proving inbound 2.1.1 was not republished.
</verification>

<success_criteria>
The irreversible release occurs only after every machine gate passes, with no human approval or UAT.
</success_criteria>

<output>
Create `.planning/phases/148-release-and-adoption-proof/148-04-SUMMARY.md` when done.
</output>
