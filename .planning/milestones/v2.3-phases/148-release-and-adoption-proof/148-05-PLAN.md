---
phase: 148-release-and-adoption-proof
plan: 05
type: execute
wave: 5
depends_on:
  - 148-04
files_modified:
  - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "Hex contains mailglass 2.4.0 and mailglass_admin 2.4.0, while mailglass_inbound remains available at 2.1.1 and has no 2.4.0 publication (D-01, D-02)."
    - "The release-triggered post-publish smoke passes the unchanged shared consumer harness in Hex mode with core/admin 2.4.0 and inbound 2.1.1 (D-07)."
    - "The final proof cites protected run URLs and sanitized artifacts without claiming external B2C launch completion or adding Crosswake scope (D-03, D-08, D-09)."
  artifacts:
    - path: ".planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md"
      provides: "Final published release and adoption proof"
      contains: "Post-publication evidence"
  key_links:
    - from: "post-publish-smoke consumer-install"
      to: "scripts/consumer_install_smoke.sh"
      via: "DEP_MODE=hex, VERSION=2.4.0, VERSION_INBOUND=2.1.1, INCLUDE_INBOUND=true"
      pattern: "DEP_MODE: hex"
    - from: "GitHub protected runs and Hex registry"
      to: ".planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md"
      via: "URLs, versions, conclusions, artifact name, and bounded outcome transcription"
      pattern: "Post-publication evidence"
  assumptions:
    - "FLAGGED UNRESOLVED (REL-01): the spec-less edge remains flagged until the maintainer reviews the final registry and consumer-smoke evidence; this plan records but does not auto-dismiss it."
    - "DESCRIPTOR-LESS / FLAGGED UNVERIFIED: these transparency prohibitions have no machine-readable descriptor; a human reviewer must explicitly retain and assess them, and they must not be silently dropped."
  prohibitions:
    - "MUST NOT represent a local-path consumer pass as proof that the published packages adopters install work."
    - "MUST NOT represent the Mailglass 2.4.0 release as satisfying the separately recorded external B2C production launch gates."
---

<objective>
Close REL-01 with registry-backed, clean-consumer evidence from the packages adopters actually install (D-01, D-02, D-03, D-07).

Purpose: Turn the irreversible ceremony into an auditable result while keeping external launch gates and Crosswake outside Mailglass completion (D-08, D-09).
Output: Final `148-RELEASE-PROOF.md` with protected run and Hex evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
@.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
@.planning/phases/148-release-and-adoption-proof/148-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Hex versions and finalize the published-consumer proof</name>
  <files>.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md</files>
  <read_first>
    - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
    - .planning/phases/148-release-and-adoption-proof/148-04-SUMMARY.md
    - .github/workflows/publish-hex.yml
    - .github/workflows/post-publish-smoke.yml
    - scripts/consumer_install_smoke.sh
    - .release-please-manifest.json
    - mix.exs
    - mailglass_admin/mix.exs
    - mailglass_inbound/mix.exs
    - .planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
  </read_first>
  <action>Use `gh` and `mix hex.info` read-only queries to confirm the Release Please tags/releases, protected publish conclusions, and exact registry versions. Inspect the release-triggered post-publish run's `consumer-install` logs and downloaded `phase-148-release-proof-*` artifact to prove `DEP_MODE=hex`, `VERSION=2.4.0`, `VERSION_INBOUND=2.1.1`, `INCLUDE_INBOUND=true`, fresh dependency resolution, compile, boot, and HTTP success. Confirm no inbound 2.4.0 GitHub release/tag or Hex version exists and that the protected publish graph did not execute inbound. Append exact URLs, run IDs, conclusions, resolved versions, artifact checksum/name, and bounded outcomes to the ledger; retain the pre-publication entries and record any failure honestly. State that D-08 external launch gates remain open outside this phase and D-09 Crosswake remains absent.</action>
  <reversibility rating="reversible">The task performs read-only external verification and updates the evidence ledger after the one-way release.</reversibility>
  <verify>
    <automated>bash -euo pipefail -c 'mix hex.info mailglass 2.4.0; mix hex.info mailglass_admin 2.4.0; mix hex.info mailglass_inbound 2.1.1; gh release view mailglass-v2.4.0 --json url,tagName; gh release view mailglass_admin-v2.4.0 --json url,tagName; set +e; hex_absence=$(mix hex.info mailglass_inbound 2.4.0 2&gt;&amp;1); hex_status=$?; set -e; test "$hex_status" -ne 0; test "$hex_absence" = "No release with name mailglass_inbound 2.4.0"; repo=$(gh repo view --json nameWithOwner --jq .nameWithOwner); for endpoint in "repos/$repo/releases/tags/mailglass_inbound-v2.4.0" "repos/$repo/git/ref/tags/mailglass_inbound-v2.4.0"; do set +e; response=$(gh api --include "$endpoint" 2&gt;&amp;1); status=$?; set -e; test "$status" -ne 0; printf "%s\n" "$response" | rg -q "^HTTP/[0-9.]+ 404"; done; runs=$(gh run list --workflow publish-hex.yml --event release --limit 20 --json databaseId,headBranch,conclusion); for tag in mailglass-v2.4.0 mailglass_admin-v2.4.0; do run_id=$(printf "%s" "$runs" | jq -er --arg tag "$tag" "[.[] | select(.headBranch == \$tag and .conclusion == \"success\")] | if length == 1 then .[0].databaseId else error(\"expected exactly one successful release run for \" + \$tag) end"); gh run view "$run_id" --json event,conclusion,jobs | jq -e ".event == \"release\" and .conclusion == \"success\" and ([.jobs[] | select(.name == \"publish-inbound\") | .conclusion] == [\"skipped\"])"; done'</automated>
  </verify>
  <acceptance_criteria>
    - All five positive registry/release queries exit 0 and their URLs/versions are recorded.
    - The release-triggered `post-publish-smoke` conclusion is success and its consumer log proves the exact Hex-mode 2.4.0/2.4.0/2.1.1 set with inbound included.
    - Fail-closed commands establish the exact Hex not-found result, HTTP 404 for both the inbound 2.4.0 GitHub release and tag ref, and `publish-inbound: skipped` in each successful core/admin release-event workflow run.
    - The final ledger contains no credentials, PII, or claim that the external B2C launch gates are complete.
  </acceptance_criteria>
  <done>The ledger proves the exact published 2.4.0/2.4.0/2.1.1 package set, green Hex-mode consumer journey, and absence of an inbound 2.4.0 publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub/Hex external state → evidence ledger | External results must be queried exactly and transcribed without secrets or unsupported conclusions. |
| published packages → clean consumer | The real registry solver and runtime boot are the adoption boundary. |

## ASVS L1 Release Gate

ASVS L1 applies at these primary boundaries: V4 Access Control is covered by read-only authenticated metadata queries and credential-free evidence transcription; V5 Input Validation is covered by exact package/tag/run predicates and fail-closed absence checks. Every critical/high threat is dispositioned `mitigate`. High-severity findings are blocking: any unresolved high-severity finding blocks final verification and release closure.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-19 | Spoofing / Repudiation | final release evidence | high | mitigate | Record direct URLs, run IDs, exact version queries, conclusions, and artifact identity. |
| T-148-20 | Tampering | accidental inbound republish | high | mitigate | Query absence of inbound 2.4.0 and inspect release job results alongside positive 2.1.1 evidence. |
| T-148-21 | Information Disclosure | downloaded logs/artifacts | high | mitigate | Transcribe bounded allowlisted fields only; never copy environments or secret-bearing logs. |
| T-148-22 | Denial of Service | missing release-SHA CI / smoke | high | mitigate | Require successful protected publish and release-triggered smoke URLs before closure. |
| T-148-23 | Tampering | duplicate release execution | high | mitigate | Verify static non-cancelling concurrency and idempotent package-job outcomes in the cited run. |
</threat_model>

## Artifacts this phase produces

- New symbols/functions: none.
- New workflow jobs/inputs: none.
- New CLI/env flags: none; verification observes existing Hex-mode variables.
- New external/runtime artifacts: published Hex packages for core/admin 2.4.0, GitHub release/publish/smoke run records, and the sanitized proof artifact.
- New repository files: final content in `.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md`.

<verification>
Direct registry/release queries are green, the release-triggered smoke proves the exact clean-consumer package set, and inbound 2.4.0 is absent.
</verification>

<success_criteria>
REL-01 is closed with published evidence: core/admin 2.4.0 exist, inbound remains 2.1.1, and a clean Hex consumer installs and boots the exact package family.
</success_criteria>

<output>
Create `.planning/phases/148-release-and-adoption-proof/148-05-SUMMARY.md` when done.
</output>
