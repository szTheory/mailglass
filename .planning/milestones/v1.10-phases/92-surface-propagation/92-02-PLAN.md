---
phase: 92-surface-propagation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/priv/static/mailglass-logo.svg
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/test/mailglass_admin/bundle_test.exs
autonomous: true
requirements: [SURF-03]
must_haves:
  truths:
    - "SURF-03 / D-09: The v0.1 live-text admin placeholder is replaced during Phase 92, not deferred."
    - "D-10: The light-surface primary lockup is not copied verbatim into dark admin chrome where Ink paths would disappear."
    - "D-11: Admin rendering uses a theme-aware monochrome/currentColor strategy for light and dark chrome."
    - "D-12: The public asset route remains `<mount>/logo.svg`; any component change is limited to the minimum needed for theme-safe rendering."
    - "D-13: Any admin static/component change is verified with `cd mailglass_admin && mix verify.preview`."
    - "D-15: HexDocs logo/favicon wiring, ex_doc SVG width/height preparation, and release hardening remain out of Phase 92."
  artifacts:
    - path: "mailglass_admin/priv/static/mailglass-logo.svg"
      provides: "Served admin sealed-flap logo asset"
      contains: "currentColor"
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "Theme-aware admin logo renderer"
      contains: "currentColor"
    - path: "mailglass_admin/test/mailglass_admin/bundle_test.exs"
      provides: "Regression check that admin logo remains outlined and font-free"
      contains: "font-family"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/components.ex"
      to: "mailglass_admin/priv/static/mailglass-logo.svg"
      via: "same sealed-flap lockup path geometry"
      pattern: "mailglass"
    - from: "mailglass_admin/lib/mailglass_admin/controllers/assets.ex"
      to: "mailglass_admin/priv/static/mailglass-logo.svg"
      via: "@logo File.read!"
      pattern: "mailglass-logo.svg"
---

<objective>
Replace the shipped admin wordmark placeholder with the sealed-flap identity
and keep it visible in both `mailglass-light` and `mailglass-dark` chrome.

Purpose: the running admin dashboard is a public repo surface. It must no
longer ship a font-dependent v0.1 placeholder while README and social-preview
surfaces carry the canonical brand.

Output: an outlined, font-free admin logo asset; a theme-safe admin logo
component if needed; and regression coverage through the existing admin
preview verification gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/92-surface-propagation/92-CONTEXT.md
@.planning/phases/92-surface-propagation/92-RESEARCH.md
@.planning/phases/92-surface-propagation/92-VALIDATION.md
@.planning/phases/92-surface-propagation/92-UI-SPEC.md
@.planning/phases/92-surface-propagation/92-PATTERNS.md
@brandbook/assets/logo-monochrome.svg
@brandbook/assets/logo-primary.svg
@mailglass_admin/priv/static/mailglass-logo.svg
@mailglass_admin/lib/mailglass_admin/controllers/assets.ex
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/assets/css/app.css
@mailglass_admin/mix.exs
@mailglass_admin/test/mailglass_admin/bundle_test.exs
@mailglass_admin/test/mailglass_admin/assets_test.exs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace the admin static logo with sealed-flap paths</name>
  <files>mailglass_admin/priv/static/mailglass-logo.svg</files>
  <read_first>
    mailglass_admin/priv/static/mailglass-logo.svg,
    brandbook/assets/logo-monochrome.svg,
    brandbook/assets/logo-primary.svg,
    .planning/phases/92-surface-propagation/92-CONTEXT.md (D-09, D-10, D-11, D-12),
    .planning/phases/92-surface-propagation/92-UI-SPEC.md (Admin dashboard wordmark surface contract),
    .planning/phases/92-surface-propagation/92-PATTERNS.md (admin static asset pattern)
  </read_first>
  <action>Replace `mailglass_admin/priv/static/mailglass-logo.svg` with the sealed-flap lockup based on `brandbook/assets/logo-monochrome.svg`. The replacement must use outlined paths only; no `<text>`, no `font-family`, no letter-spacing, no external href, no CSS url reference, and no gradients. Add explicit `width="973"` and `height="212"` matching the lockup viewBox scale so the served asset has deterministic intrinsic sizing. Keep accessible naming for `mailglass`. Preserve the file path and filename exactly so `MailglassAdmin.Controllers.Assets` continues to serve `<mount>/logo.svg` without route changes.</action>
  <verify>
    <automated>test -f mailglass_admin/priv/static/mailglass-logo.svg && rg 'currentColor' mailglass_admin/priv/static/mailglass-logo.svg && rg 'width="973"' mailglass_admin/priv/static/mailglass-logo.svg && rg 'height="212"' mailglass_admin/priv/static/mailglass-logo.svg && ! rg -n '<text|font-family|letter-spacing|url\\(|href=' mailglass_admin/priv/static/mailglass-logo.svg && test "$(stat -f%z mailglass_admin/priv/static/mailglass-logo.svg)" -lt 20000</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mailglass_admin/priv/static/mailglass-logo.svg` contains `currentColor`.
    - Source assertion: `mailglass_admin/priv/static/mailglass-logo.svg` contains `width="973"` and `height="212"`.
    - Source assertion: `rg -n '<text|font-family|letter-spacing|url\\(|href=' mailglass_admin/priv/static/mailglass-logo.svg` prints no lines.
    - File assertion: `stat -f%z mailglass_admin/priv/static/mailglass-logo.svg` prints a number less than `20000`.
    - Source assertion: `mailglass_admin/lib/mailglass_admin/controllers/assets.ex` still references `mailglass-logo.svg` and no router route change is needed.
  </acceptance_criteria>
  <done>The admin static logo file is a sealed-flap, outlined, font-free asset at the stable served path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make the rendered admin logo theme-aware</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/components.ex,
    mailglass_admin/lib/mailglass_admin/operator/shell.ex,
    mailglass_admin/assets/css/app.css,
    mailglass_admin/priv/static/mailglass-logo.svg,
    brandbook/assets/logo-monochrome.svg,
    .planning/phases/92-surface-propagation/92-CONTEXT.md (D-10, D-11, D-12),
    .planning/phases/92-surface-propagation/92-RESEARCH.md (Admin wordmark finding),
    .planning/phases/92-surface-propagation/92-PATTERNS.md (Components.logo pattern)
  </read_first>
  <action>Update `Components.logo/1` for theme-safe rendering by replacing the `<img src="logo.svg" ...>` implementation with an inline `<svg>` that uses the same sealed-flap monochrome/currentColor path geometry as `mailglass_admin/priv/static/mailglass-logo.svg`. Preserve the public `attr :class` API by applying `class={@class}` to the `<svg>`, preserve accessible naming with `role="img"` and `aria-label="mailglass"`, and use `fill="currentColor"` paths so the logo inherits the surrounding `text-base-content` in both `mailglass-light` and `mailglass-dark`. Update the component module doc for `logo/1` to say the admin UI renders the sealed-flap lockup inline for theme-aware color while the `logo.svg` route remains served by `MailglassAdmin.Controllers.Assets`. Do not add new dependencies, new icon libraries, new CSS themes, or broad admin layout changes.</action>
  <verify>
    <automated>rg '<svg' mailglass_admin/lib/mailglass_admin/components.ex && rg 'currentColor' mailglass_admin/lib/mailglass_admin/components.ex && rg 'aria-label="mailglass"' mailglass_admin/lib/mailglass_admin/components.ex && ! rg '<img src="logo\\.svg"' mailglass_admin/lib/mailglass_admin/components.ex</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Components.logo/1` renders an inline `<svg` rather than `<img src="logo.svg"`.
    - Source assertion: `mailglass_admin/lib/mailglass_admin/components.ex` contains `currentColor`.
    - Source assertion: `mailglass_admin/lib/mailglass_admin/components.ex` contains `aria-label="mailglass"` or equivalent accessible naming.
    - Source assertion: `attr :class, :any, default: nil` remains present for `logo/1`.
    - Source assertion: `mailglass_admin/lib/mailglass_admin/operator/shell.ex` remains functionally unchanged unless the executor documents a concrete class-level need in the summary.
  </acceptance_criteria>
  <done>The admin UI renders a currentColor sealed-flap logo that inherits light/dark theme text color without relying on external SVG inheritance through img.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend logo regression tests and run admin verification</name>
  <files>mailglass_admin/test/mailglass_admin/bundle_test.exs</files>
  <read_first>
    mailglass_admin/test/mailglass_admin/bundle_test.exs,
    mailglass_admin/test/mailglass_admin/assets_test.exs,
    mailglass_admin/mix.exs,
    mailglass_admin/priv/static/mailglass-logo.svg,
    mailglass_admin/lib/mailglass_admin/components.ex,
    .planning/phases/92-surface-propagation/92-VALIDATION.md (SURF-03 validation map)
  </read_first>
  <action>Extend the existing `describe "priv/static/mailglass-logo.svg"` block in `mailglass_admin/test/mailglass_admin/bundle_test.exs` with assertions that read `@logo_path` and refute `<text`, `font-family`, and `letter-spacing`. Keep the existing under-20KB logo budget. Then run `cd mailglass_admin && mix verify.preview`. This gate must run after the static logo and component edits, because it compiles with warnings as errors, runs admin tests excluding flaky tests, rebuilds assets, and runs `git diff --exit-code priv/static/`.</action>
  <verify>
    <automated>rg '<text|font-family|letter-spacing' mailglass_admin/test/mailglass_admin/bundle_test.exs && cd mailglass_admin && mix verify.preview</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mailglass_admin/test/mailglass_admin/bundle_test.exs` reads `@logo_path` and refutes `<text`.
    - Source assertion: `mailglass_admin/test/mailglass_admin/bundle_test.exs` refutes `font-family`.
    - Source assertion: `mailglass_admin/test/mailglass_admin/bundle_test.exs` refutes `letter-spacing`.
    - CLI assertion: `cd mailglass_admin && mix verify.preview` exits 0.
    - CLI assertion: the `verify.preview` run includes `cmd git diff --exit-code priv/static/` and leaves `mailglass_admin/priv/static/` clean except for the intentional committed `mailglass-logo.svg` content change.
  </acceptance_criteria>
  <done>The admin logo replacement has regression coverage and the admin preview verification gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brand source asset -> shipped admin package | Brandbook paths are copied into `mailglass_admin/priv/static`, which ships in the Hex package. |
| Admin theme state -> logo rendering | The operator chrome changes `data-theme`; the logo must not disappear when dark chrome is active. |
| Static asset source -> compile-time embedded response | `Controllers.Assets` embeds `mailglass-logo.svg` at compile time and serves it as immutable SVG. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Spoofing / Tampering | `mailglass-logo.svg` | mitigate | Replace placeholder live text with sealed-flap outlined paths, keep stable filename, and add test coverage banning `<text>` and font dependencies. |
| T-92-05 | Denial of service / UX failure | Dark admin chrome | mitigate | Render the UI logo inline with currentColor so it inherits `text-base-content` in both `mailglass-light` and `mailglass-dark`. |
| T-92-06 | Repudiation | Admin bundle verification | mitigate | Run `cd mailglass_admin && mix verify.preview` after edits and require the bundle-clean gate to pass. |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No new package install is planned; existing Elixir/Tailwind/asset tooling is used through `mix verify.preview`. |
</threat_model>

<verification>
- `rg -n '<text|font-family|letter-spacing|url\\(|href=' mailglass_admin/priv/static/mailglass-logo.svg` prints no lines.
- `rg '<svg|currentColor|aria-label="mailglass"' mailglass_admin/lib/mailglass_admin/components.ex` exits 0.
- `rg '<text|font-family|letter-spacing' mailglass_admin/test/mailglass_admin/bundle_test.exs` exits 0.
- `cd mailglass_admin && mix verify.preview` exits 0.
</verification>

<success_criteria>
- SURF-03 is covered by a replaced admin wordmark, not a deferral.
- The admin logo no longer depends on live text or installed fonts.
- The running admin UI has a theme-safe sealed-flap identity in both light and dark chrome.
- The existing admin preview verification gate passes after the change.
</success_criteria>

<output>
Create `.planning/phases/92-surface-propagation/92-02-SUMMARY.md` when done.
</output>
