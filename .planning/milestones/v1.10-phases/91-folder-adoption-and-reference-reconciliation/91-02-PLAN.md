---
phase: 91-folder-adoption-and-reference-reconciliation
plan: 02
type: execute
wave: 1
depends_on: ["91-01"]
files_modified:
  - brandbook/
  - brandbook-fable/
  - .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md
autonomous: true
requirements: [FOLD-01, FOLD-03]
must_haves:
  truths:
    - "The repo-root `brandbook/` folder contains the approved fable artifact tree."
    - "`brandbook-fable/` no longer exists as a tracked or working-tree path."
    - "The old codex-only files are absent from the canonical `brandbook/` folder."
  artifacts:
    - path: "brandbook/brand-book.md"
      provides: "Canonical fable brand book text master"
    - path: "brandbook/index.html"
      provides: "Canonical fable self-contained HTML brand book"
    - path: "brandbook/assets/logo-primary.svg"
      provides: "Canonical fable primary lockup"
    - path: ".planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md"
      provides: "Preflight and Git move proof"
      contains: "Git Folder Adoption"
  key_links:
    - from: "brandbook/"
      to: "brandbook-fable/"
      via: "git mv after old brandbook removal"
      pattern: "git mv brandbook-fable brandbook"
    - from: ".planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh"
      to: "brandbook/"
      via: "adoption invariant checks"
      pattern: "brandbook/brand-book.md"
---

<objective>
Replace the active codex-era `brandbook/` tree with the approved fable artifacts using Git operations.

Purpose: FOLD-01 and D-01 through D-03 require a clean tracked replacement: old codex files are removed from the active tree, ignored leftovers are cleared, and `brandbook-fable/` is moved into `brandbook/` with Git preserving history.

Output: canonical `brandbook/` contains the fable book; `brandbook-fable/` is gone; preflight and move proof are recorded in `91-gate-evidence.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-CONTEXT.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-RESEARCH.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-PATTERNS.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md
@brandbook-fable/README.md
@brandbook-fable/brand-book.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Preflight ignored files before the Git move</name>
  <files>brandbook/, brandbook-fable/, .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md</files>
  <read_first>
    brandbook/README.md,
    brandbook-fable/README.md,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-CONTEXT.md (D-01, D-02, D-03),
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-RESEARCH.md (Pitfall 1)
  </read_first>
  <action>Run and record `git status --ignored --short --untracked-files=all -- brandbook brandbook-fable` in `91-gate-evidence.md` under `Ignored File Preflight`. Follow the `91-PATTERNS.md` deleted old `brandbook/**` analog for the exact ignored-file preflight pattern. The only known ignored paths allowed before cleanup are `!! brandbook-fable/.DS_Store`, `!! brandbook/.DS_Store`, and `!! brandbook/assets/.DS_Store`. If any other `??` or `!!` line appears under these folders, stop and record the unexpected path instead of moving files. Remove only the known `.DS_Store` leftovers from the working tree; do not stage or commit `.DS_Store` files.</action>
  <verify>
    <automated>! git status --ignored --short --untracked-files=all -- brandbook brandbook-fable | grep -E '^[?!]' | grep -v '\\.DS_Store$'</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: the preflight command output is recorded in `91-gate-evidence.md`.
    - CLI assertion: `git status --ignored --short --untracked-files=all -- brandbook brandbook-fable | grep -E '^[?!]' | grep -v '\\.DS_Store$'` prints no lines.
    - Source assertion: `git status --short -- brandbook brandbook-fable | grep '\\.DS_Store'` prints no lines.
    - Behavior assertion: no file outside the known `.DS_Store` paths is removed by this task.
  </acceptance_criteria>
  <done>Ignored/untracked preflight is recorded, known `.DS_Store` leftovers are cleared, and no unexpected untracked file blocks the Git move.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace old brandbook/ with the fable folder via Git</name>
  <files>brandbook/, brandbook-fable/, .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md</files>
  <read_first>
    brandbook/README.md,
    brandbook-fable/README.md,
    brandbook-fable/brand-book.md,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>Run the tracked replacement as Git operations, in this order: `git rm -r brandbook`; `rm -rf brandbook` to clear any remaining ignored directory entries after the tracked removal; `git mv brandbook-fable brandbook`. Follow the `91-PATTERNS.md` `brandbook/**` replacing `brandbook-fable/**` analog for preserving the fable artifact tree, and the old-only absence inventory for codex file removals. Record these commands and their success in `91-gate-evidence.md` under `Git Folder Adoption`. Do not copy files manually. Do not keep an in-tree codex archive. Do not edit `prompts/mailglass-brand-book.md`.</action>
  <verify>
    <automated>test -f brandbook/brand-book.md && test -f brandbook/index.html && test -f brandbook/assets/logo-primary.svg && test ! -e brandbook-fable && test -z "$(git ls-files brandbook-fable)" && test ! -e brandbook/assets/concepts && test ! -e brandbook/assets/options && test ! -e brandbook/brand-audit.md && test ! -e brandbook/logo-concepts.html && test ! -e brandbook/logo-concepts.md && test ! -e brandbook/logo-creative-brief.md && test ! -e brandbook/logo-options.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f brandbook/brand-book.md`, `test -f brandbook/index.html`, and `test -f brandbook/assets/logo-primary.svg` all exit 0.
    - CLI assertion: `test ! -e brandbook-fable` exits 0.
    - CLI assertion: `git ls-files brandbook-fable` prints no paths.
    - Source assertion: `brandbook/assets/concepts`, `brandbook/assets/options`, `brandbook/brand-audit.md`, `brandbook/logo-concepts.html`, `brandbook/logo-concepts.md`, `brandbook/logo-creative-brief.md`, and `brandbook/logo-options.md` do not exist.
    - CLI assertion: `git status --short -- brandbook brandbook-fable | grep '\\.DS_Store'` prints no lines.
  </acceptance_criteria>
  <done>The active tree has the fable book at `brandbook/`, no `brandbook-fable/`, no old codex-only artifacts, and no staged `.DS_Store` files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git index -> canonical brand artifacts | Removing and moving tracked files can accidentally lose fable artifacts or retain codex artifacts. |
| Ignored filesystem -> Git move | Ignored `.DS_Store` files can keep directories around and cause an incorrect nested move. |
| Local commits -> release automation | The folder-adoption commit must use `chore:` and avoid release-triggering Conventional Commit markers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | `brandbook/` and `brandbook-fable/` | mitigate | Preflight ignored files, remove tracked old `brandbook/`, clear leftover directory, then `git mv brandbook-fable brandbook`; verify fable files present and codex-only files absent. |
| T-91-02 | Repudiation | `91-gate-evidence.md` | mitigate | Record preflight and move command outputs so the adoption path is auditable. |
| T-91-03 | Denial of service / Operational risk | Commit subject | mitigate | Use a `chore:` commit subject for this plan and no `feat:`, `fix:`, `!`, `BREAKING CHANGE`, or `Release-As`. |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned or needed. |
</threat_model>

<verification>
- `test -f brandbook/brand-book.md`
- `test ! -e brandbook-fable`
- `test -z "$(git ls-files brandbook-fable)"`
- `git status --short -- brandbook brandbook-fable | grep '\\.DS_Store'` prints no lines.
</verification>

<success_criteria>
- FOLD-01 is satisfied for the file tree: the fable book is canonical at `brandbook/`, old codex files are removed from the active tree, and the former fable path is gone.
- The evidence record proves preflight and Git move mechanics.
- No Phase 92 or Phase 93 surface propagation work is introduced.
</success_criteria>

<output>
Create `.planning/phases/91-folder-adoption-and-reference-reconciliation/91-02-SUMMARY.md` when done.
</output>
