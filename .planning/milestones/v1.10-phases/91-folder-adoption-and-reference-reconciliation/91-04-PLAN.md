---
phase: 91-folder-adoption-and-reference-reconciliation
plan: 04
type: execute
wave: 3
depends_on: ["91-03"]
files_modified:
  - .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md
autonomous: true
requirements: [FOLD-01, FOLD-02, FOLD-03]
must_haves:
  truths:
    - "The re-pathed Phase 91 gate passes and prints `GATE-PASS`."
    - "Active reference sweeps prove no stale active `brandbook-fable/` or prompt-era source pointer remains outside explicit provenance paths."
    - "Phase 91 commits use only non-release-triggering subjects and no Release Please PR is open for this phase."
  artifacts:
    - path: ".planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md"
      provides: "Final gate output, reference sweep, and release-safety proof"
      contains: "GATE-PASS"
  key_links:
    - from: ".planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md"
      to: ".planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh"
      via: "verbatim gate output"
      pattern: "GATE-PASS"
    - from: ".planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md"
      to: "GitHub Release Please PR list"
      via: "`gh pr list --search` output"
      pattern: "Release Safety"
---

<objective>
Run the final Phase 91 verification and record the evidence that proves the canonical folder move did not break the v1.9 brandbook gate or trigger release automation.

Purpose: FOLD-03 and D-14 through D-15 require the re-pathed gate to pass on `brandbook/`, Phase 91 commit subjects to stay release-safe, and release-please PR absence to be explicitly verified without adding Phase 93 release-hardening work.

Output: `91-gate-evidence.md` contains final `GATE-PASS`, active reference sweep output, local commit-subject proof, and Release Please PR proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-CONTEXT.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-RESEARCH.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-VALIDATION.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-PATTERNS.md
@.planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh
@.planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md
@release-please-config.json
@.github/workflows/pr-title.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the final gate and active reference sweeps</name>
  <files>.planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md</files>
  <read_first>
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-CONTEXT.md (D-07, D-08, D-09, D-10, D-11),
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-VALIDATION.md,
    CLAUDE.md,
    mailglass_admin/docs/design-system.md,
    mailglass_admin/assets/css/app.css,
    brandbook/brand-book.md
  </read_first>
  <action>Run `bash .planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh` from the repo root and record the full output and exit code in `91-gate-evidence.md` under `Gate Run`. Read the `Phase base:` hash from `91-gate-evidence.md`, run `git diff --name-status "$PHASE_BASE" --`, and record the command/output under `Adoption Diff Scope`; the recorded proof must show only approved rename/reference/gate/planning evidence paths from the same explicit allowlist enforced by Check 9: `brandbook/**`; deleted or rename-source `brandbook-fable/**` only; `CLAUDE.md`; `mailglass_admin/docs/design-system.md`; `mailglass_admin/assets/css/app.css`; live planning files approved for Phase 91 (`.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONES.md`, `.planning/RETROSPECTIVE.md`); `.planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh`; `.planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md`; and Phase 91 planning artifacts under `.planning/phases/91-folder-adoption-and-reference-reconciliation/` matching `91-*-PLAN.md`, `91-*-SUMMARY.md`, `91-CONTEXT.md`, `91-DISCUSSION-LOG.md`, `91-RESEARCH.md`, `91-VALIDATION.md`, and `91-PATTERNS.md`. Run and record an active stale-reference sweep that excludes only explicit provenance paths: `.planning/milestones/**`, `.planning/research/**`, `.planning/phases/91-folder-adoption-and-reference-reconciliation/**`, `.planning/todos/**`, `prompts/**`, generated docs, and build outputs. The sweep must prove no active tracked file contains `brandbook-fable/`. Also run and record the active pointer sweep over `CLAUDE.md`, `mailglass_admin/docs/design-system.md`, `mailglass_admin/assets/css/app.css`, `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONES.md`, and `.planning/RETROSPECTIVE.md` proving no exact `prompts/mailglass-brand-book.md` active pointer remains.</action>
  <verify>
    <automated>bash .planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh | tee /tmp/phase91-gate.out && grep -x 'GATE-PASS' /tmp/phase91-gate.out && grep -n 'Adoption Diff Scope' .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md && grep -n 'git diff --name-status' .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md && ! rg --hidden -n 'brandbook-fable/' --glob '!.git/**' --glob '!.planning/milestones/**' --glob '!.planning/research/**' --glob '!.planning/phases/91-folder-adoption-and-reference-reconciliation/**' --glob '!.planning/todos/**' --glob '!prompts/**' --glob '!_build/**' --glob '!doc/**' --glob '!mailglass_admin/_build/**' --glob '!mailglass_admin/doc/**' --glob '!mailglass_inbound/_build/**' --glob '!mailglass_inbound/doc/**' --glob '!reference/**/_build/**' --glob '!reference/**/doc/**' && ! rg -n 'prompts/mailglass-brand-book\\.md' CLAUDE.md mailglass_admin/docs/design-system.md mailglass_admin/assets/css/app.css .planning/PROJECT.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/MILESTONES.md .planning/RETROSPECTIVE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: the gate exits 0 and its output contains a line exactly equal to `GATE-PASS`.
    - Source assertion: `91-gate-evidence.md` contains the final gate command, full output, exit code 0, and `GATE-PASS`.
    - Source assertion: `91-gate-evidence.md` contains the `Adoption Diff Scope` proof, including the recorded `Phase base:` hash, the `git diff --name-status "$PHASE_BASE" --` command, and output limited to the approved path allowlist.
    - CLI assertion: the active `rg --hidden -n 'brandbook-fable/'` sweep with the provenance excludes listed in the action prints no lines.
    - CLI assertion: `rg -n 'prompts/mailglass-brand-book\\.md' CLAUDE.md mailglass_admin/docs/design-system.md mailglass_admin/assets/css/app.css .planning/PROJECT.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/MILESTONES.md .planning/RETROSPECTIVE.md` prints no lines.
  </acceptance_criteria>
  <done>The final gate passes on canonical `brandbook/`, and active stale-reference sweeps are recorded with zero active hits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record release-safety proof</name>
  <files>.planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md</files>
  <read_first>
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md,
    .planning/phases/91-folder-adoption-and-reference-reconciliation/91-CONTEXT.md (D-14, D-15),
    release-please-config.json,
    .github/workflows/pr-title.yml,
    .github/workflows/ci.yml,
    CLAUDE.md
  </read_first>
  <action>Read the `Phase base:` hash recorded in `91-gate-evidence.md`, then inspect local non-merge commit subjects in `BASE..HEAD`. Record `git log --no-merges --format=%s BASE..HEAD` under `Release Safety`. The command must show only subjects beginning with `chore` or `docs`, optionally scoped, followed by a colon; no subject may begin with `feat`, `fix`, or `deps`; no subject may contain `!`, `BREAKING CHANGE`, or `Release-As`. Then run `gh pr list --state open --search 'release-please in:title' --limit 20 --json number,title --template '{{len .}}'` and record the output. Keep this plan limited to FOLD-01 through FOLD-03 evidence and release-safety proof.</action>
  <verify>
    <automated>BASE=$(sed -n 's/.*Phase base: `\\([0-9a-f][0-9a-f]*\\)`.*/\\1/p' .planning/phases/91-folder-adoption-and-reference-reconciliation/91-gate-evidence.md | head -1); test -n "$BASE"; git log --no-merges --format=%s "$BASE"..HEAD | tee /tmp/phase91-subjects.txt; awk 'NF && $0 !~ /^(chore|docs)(\\([^)]*\\))?: / {print; bad=1} END {exit bad}' /tmp/phase91-subjects.txt; ! grep -E '^(feat|fix|deps)(\\(|:)|!|BREAKING CHANGE|Release-As' /tmp/phase91-subjects.txt; gh pr list --state open --search 'release-please in:title' --limit 20 --json number,title --template '{{len .}}' | tee /tmp/phase91-release-pr-count.txt; grep -x '0' /tmp/phase91-release-pr-count.txt</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `git log --no-merges --format=%s BASE..HEAD` is recorded in `91-gate-evidence.md`.
    - CLI assertion: every non-empty subject in the recorded Phase 91 range matches `^(chore|docs)(\\([^)]*\\))?: `.
    - CLI assertion: no recorded subject contains `!`, `BREAKING CHANGE`, or `Release-As`.
    - CLI assertion: `gh pr list --state open --search 'release-please in:title' --limit 20 --json number,title --template '{{len .}}'` prints `0`, and that output is recorded.
  </acceptance_criteria>
  <done>Phase 91 release-safety evidence proves commit subjects are non-release-triggering and no open Release Please PR was created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation script -> repository state | The final gate must evaluate the moved canonical folder, not stale paths. |
| Documentation -> future agents/readers | Active reference sweeps must distinguish stale active pointers from provenance records. |
| Local commits/GitHub PRs -> release automation | Conventional Commit subjects and Release Please PRs are operational release triggers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | Final gate over `brandbook/` | mitigate | Run full phase-local `gate.sh` and require `GATE-PASS` after folder adoption and reference reconciliation. |
| T-91-02 | Spoofing / Repudiation | Active reference sweeps | mitigate | Record exact `rg` sweeps with explicit provenance excludes and zero active stale hits. |
| T-91-03 | Denial of service / Operational risk | Commit titles and Release Please PRs | mitigate | Verify local commit subjects from Phase base use only `chore:`/`docs:` forms and `gh pr list` reports zero open release-please PRs. |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned or needed. |
</threat_model>

<verification>
- `bash .planning/phases/91-folder-adoption-and-reference-reconciliation/gate.sh` exits 0 and prints `GATE-PASS`.
- Active stale-reference sweeps print no lines outside explicit provenance paths.
- Commit-subject and Release Please PR checks are recorded in `91-gate-evidence.md`.
</verification>

<success_criteria>
- FOLD-01, FOLD-02, and FOLD-03 are all proven by final evidence.
- The phase remains FOLD-only; Phase 92/93 work is not implemented here.
- Release automation is not triggered by Phase 91 commits.
</success_criteria>

<output>
Create `.planning/phases/91-folder-adoption-and-reference-reconciliation/91-04-SUMMARY.md` when done.
</output>
