---
phase: 93-hexdocs-wiring-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/assets/logo-mark.svg
  - brandbook/assets/favicon.svg
  - mix.exs
  - mailglass_admin/mix.exs
  - mailglass_inbound/mix.exs
autonomous: true
requirements: [HEXD-01, HEXD-02]
must_haves:
  truths:
    - "The two canonical brand SVGs carry explicit width/height matching their viewBox aspect (ex_doc 0.40.x requirement)"
    - "All three packages' docs/0 config references the canonical brandbook/ logo and favicon via relative paths (no per-package copies)"
    - "mix docs builds for all three packages, copies logo+favicon into doc/assets/, and emits no new warnings"
    - "Every edit lands as a non-release-triggering commit type; this plan cuts no Hex release"
  artifacts:
    - path: "brandbook/assets/logo-mark.svg"
      provides: "logo: source with explicit width=164 height=156"
      contains: 'width="164" height="156"'
    - path: "brandbook/assets/favicon.svg"
      provides: "favicon: source with explicit width=16 height=16"
      contains: 'width="16" height="16"'
    - path: "mix.exs"
      provides: "root docs/0 with logo:/favicon: brandbook/assets relative paths"
      contains: "brandbook/assets/logo-mark.svg"
    - path: "mailglass_admin/mix.exs"
      provides: "admin docs/0 with ../brandbook/assets/ logo:/favicon:"
      contains: "../brandbook/assets/logo-mark.svg"
    - path: "mailglass_inbound/mix.exs"
      provides: "inbound docs/0 with ../brandbook/assets/ logo:/favicon:"
      contains: "../brandbook/assets/logo-mark.svg"
  key_links:
    - from: "mix.exs docs/0"
      to: "brandbook/assets/logo-mark.svg"
      via: "ex_doc logo: relative path (root = repo root, no ../)"
      pattern: "logo:.*brandbook/assets/logo-mark.svg"
    - from: "mailglass_admin/mix.exs and mailglass_inbound/mix.exs docs/0"
      to: "brandbook/assets/logo-mark.svg"
      via: "ex_doc logo: relative path (siblings = ../brandbook)"
      pattern: "[.][.]/brandbook/assets/logo-mark.svg"
---

<objective>
Wire the sealed-flap brand into all three packages' HexDocs output: add the explicit
width/height attributes ex_doc 0.40.x requires on the two canonical brand SVGs, then add
logo/favicon keys to all three docs/0 configs pointing at the canonical brandbook/ assets via
relative paths. Prove it with a local mix docs build per package.

Purpose: HexDocs is the primary surface adopters see when they read the library's docs; the
sealed-flap mark must appear there. The wiring is inert on hexdocs.pm until each package's next
natural release (locked HexDocs-latency decision, D-06) — this plan publishes nothing.

Output: two SVGs gain width/height; three docs/0 gain logo/favicon; local mix docs x3 renders
the mark with no new warnings. Implements HEXD-01 + HEXD-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-hexdocs-wiring-and-release-hardening/93-CONTEXT.md
@.planning/phases/93-hexdocs-wiring-and-release-hardening/93-PATTERNS.md
@.planning/research/v1.10-brand-adoption/ADOPTION-MECHANICS.md

<interfaces>
Exact current shapes the executor edits (extracted from the codebase 2026-06-13).

brandbook/assets/logo-mark.svg root element (line 1) is viewBox-only today, ADD width/height.
The root svg carries: xmlns, viewBox="-12 32 164 156", role="img", aria-labelledby=...

brandbook/assets/favicon.svg root element (line 1) is viewBox-only today, ADD width/height.
The root svg carries: xmlns, viewBox="0 0 16 16", role="img", aria-labelledby=...

docs/0 function heads (no logo:/favicon:/assets: key exists in any of them today):
  mix.exs:355                   main: "getting-started", homepage_url:, source_url:, source_ref: "v#{@version}", ...
  mailglass_admin/mix.exs:214   main: "MailglassAdmin", source_url:, source_ref: "v" <> @version, extras: [...]
  mailglass_inbound/mix.exs:147 main: "MailglassInbound", source_url:, source_ref: "v" <> @version, extras: [...]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit width/height to the two canonical brand SVGs</name>
  <files>brandbook/assets/logo-mark.svg, brandbook/assets/favicon.svg</files>
  <read_first>
    - brandbook/assets/logo-mark.svg (line 1 root element)
    - brandbook/assets/favicon.svg (line 1 root element)
    - 93-PATTERNS.md section "brandbook/assets/logo-mark.svg + favicon.svg" (lines 226-242, quotes the exact current root elements and target attributes)
    - .planning/research/v1.10-brand-adoption/ADOPTION-MECHANICS.md section 1 "Critical gap" (why ex_doc 0.40.x needs explicit width/height on SVG logo/favicon)
  </read_first>
  <action>
    Edit the root svg element of brandbook/assets/logo-mark.svg in place to add width="164" height="156" (matches the viewBox -12 32 164 156 extent of 164x156 per D-02). Edit the root svg element of brandbook/assets/favicon.svg in place to add width="16" height="16" (matches viewBox 0 0 16 16 per D-02). Idiomatic placement is immediately after xmlns and before viewBox, or directly after viewBox; exact attribute ordering is your discretion per CONTEXT.md. Do NOT create ex_doc-only duplicate SVG copies and do NOT change any other attribute; these are the canonical assets and both are outlined-path SVGs, so there is no font-substitution concern. Existing img and link rel=icon embeds in brandbook/index.html and landing-page.html override or ignore intrinsic size, so adding these attributes is safe for current consumers. Commit type is non-bumping (chore: or docs:) per D-06.
  </action>
  <verify>
    <automated>grep -E 'width="164" height="156"' brandbook/assets/logo-mark.svg; grep -E 'width="16" height="16"' brandbook/assets/favicon.svg</automated>
  </verify>
  <acceptance_criteria>
    - grep -E 'width="164" height="156"' brandbook/assets/logo-mark.svg matches (exit 0).
    - grep -E 'width="16" height="16"' brandbook/assets/favicon.svg matches (exit 0).
    - grep -c 'viewBox="-12 32 164 156"' brandbook/assets/logo-mark.svg still returns 1 (viewBox untouched).
    - grep -c 'viewBox="0 0 16 16"' brandbook/assets/favicon.svg still returns 1 (viewBox untouched).
    - No new file created under brandbook/assets/: git status --porcelain brandbook/assets/ shows no untracked entries.
  </acceptance_criteria>
  <done>Both canonical SVGs carry explicit width/height matching their viewBox aspect; viewBoxes unchanged; no duplicate copies created.</done>
</task>

<task type="auto">
  <name>Task 2: Add logo/favicon keys to all three docs/0 configs</name>
  <files>mix.exs, mailglass_admin/mix.exs, mailglass_inbound/mix.exs</files>
  <read_first>
    - mix.exs around line 355 (root docs/0 head)
    - mailglass_admin/mix.exs around line 214 (admin docs/0 head)
    - mailglass_inbound/mix.exs around line 147 (inbound docs/0 head)
    - 93-PATTERNS.md sections for the three docs/0 (lines 77-148, quote current shapes and exact key/path values per package)
    - .planning/research/v1.10-brand-adoption/ADOPTION-MECHANICS.md section 1 "Path resolution" (root = repo root no ../; siblings = ../brandbook/...; ex_doc auto-copies into doc output so no :files change)
  </read_first>
  <action>
    Add two keyword keys, logo: and favicon:, to each of the three docs/0 keyword lists (per D-01/D-03/D-04; favicon is supported on all three in ex_doc 0.40.1). In root mix.exs docs/0 (line 355) use repo-root-relative paths with NO ../ prefix: logo: "brandbook/assets/logo-mark.svg" and favicon: "brandbook/assets/favicon.svg". In both mailglass_admin/mix.exs docs/0 (line 214) and mailglass_inbound/mix.exs docs/0 (line 147) use sibling-relative paths with the ../ prefix into the canonical brandbook/: logo: "../brandbook/assets/logo-mark.svg" and favicon: "../brandbook/assets/favicon.svg". Idiomatic placement is alongside main:/source_url: near the top of each keyword list, before the large extras:/skip_undefined_reference_warnings_on: blocks. Do NOT touch any package's :files allowlist (root mix.exs:345-353, admin :210, inbound :143); ex_doc auto-copies logo/favicon into doc output, so the publish-allowlist *-files.expected proofs stay green. Do NOT create per-package SVG copies. Commit type is non-bumping (docs: or chore:) per D-06; this plan cuts no Hex release.
  </action>
  <verify>
    <automated>grep -c 'logo:.*brandbook/assets/logo-mark.svg' mix.exs; grep -c '[.][.]/brandbook/assets/logo-mark.svg' mailglass_admin/mix.exs; grep -c '[.][.]/brandbook/assets/logo-mark.svg' mailglass_inbound/mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'logo:.*brandbook/assets/logo-mark.svg' mix.exs returns 1 and grep -c 'favicon:.*brandbook/assets/favicon.svg' mix.exs returns 1 (root, no ../).
    - grep -c '[.][.]/brandbook/assets/logo-mark.svg' mailglass_admin/mix.exs returns 1 and grep -c '[.][.]/brandbook/assets/favicon.svg' mailglass_admin/mix.exs returns 1 (sibling, with ../).
    - grep -c '[.][.]/brandbook/assets/logo-mark.svg' mailglass_inbound/mix.exs returns 1 and grep -c '[.][.]/brandbook/assets/favicon.svg' mailglass_inbound/mix.exs returns 1 (sibling, with ../).
    - No :files allowlist changed: git diff --unified=0 mix.exs mailglass_admin/mix.exs mailglass_inbound/mix.exs piped to grep -c 'files:' returns 0.
    - No per-package SVG copy created: git status --porcelain shows no untracked *.svg.
  </acceptance_criteria>
  <done>All three docs/0 configs reference the canonical brandbook/ logo and favicon via correct relative paths (root no ../, siblings with ../); no :files change; no SVG copies.</done>
</task>

<task type="auto">
  <name>Task 3: Prove logo+favicon render via local mix docs for all three packages</name>
  <files>mix.exs, mailglass_admin/mix.exs, mailglass_inbound/mix.exs</files>
  <read_first>
    - 93-VALIDATION.md "Per-Task Verification Map" row "Local mix docs proof x3" and "Test Infrastructure" Quick-run command
    - .planning/research/v1.10-brand-adoption/ADOPTION-MECHANICS.md section 1 (ex_doc auto-copies logo/favicon into doc/assets/ on build)
    - 93-CONTEXT.md D-07 (verify with a local mix docs build per package; confirm logo+favicon in doc/assets/, no new warnings) and D-08 (wire against ex_doc 0.40.1; ignore the pending 0.40.3 dependabot PR #77)
  </read_first>
  <action>
    Build docs locally for each of the three packages and confirm the wired logo/favicon are copied into the generated output and that no NEW warnings appear. From the repo root run mix docs and confirm doc/assets/logo.svg and doc/assets/favicon.svg exist; capture the build log and confirm it contains no warning lines that did not already exist before this plan (ex_doc copies the logo/favicon source into doc/assets/logo.svg and doc/assets/favicon.svg regardless of source filename). Repeat inside mailglass_admin/ (cd mailglass_admin then mix docs) and mailglass_inbound/ (cd mailglass_inbound then mix docs), each time confirming doc/assets/logo.svg and doc/assets/favicon.svg exist and the log has no new warnings. Do NOT run any mix command in reference/demo_app (swoosh lock drift pitfall). Do NOT commit the generated doc/ output; it is build artifact, not source. If the admin or inbound mix docs build resolves the sibling mailglass via the local path dep (dev/test default), that is expected and fine; the ../brandbook/ relative path resolves from each package dir against the monorepo checkout.
  </action>
  <verify>
    <automated>mix docs 2>&amp;1 | tee /tmp/d-root.log; ls doc/assets/logo.svg doc/assets/favicon.svg; grep -i warning /tmp/d-root.log; echo "expect no NEW warning lines above"</automated>
  </verify>
  <acceptance_criteria>
    - Root: mix docs exits 0; ls doc/assets/logo.svg doc/assets/favicon.svg both exist; no NEW warning lines in the build log.
    - Admin: cd mailglass_admin then mix docs exits 0; mailglass_admin/doc/assets/logo.svg and mailglass_admin/doc/assets/favicon.svg both exist; no new warnings.
    - Inbound: cd mailglass_inbound then mix docs exits 0; mailglass_inbound/doc/assets/logo.svg and mailglass_inbound/doc/assets/favicon.svg both exist; no new warnings.
    - No mix command run in reference/demo_app: git status --porcelain reference/demo_app/mix.lock shows no new drift introduced by this task.
    - Generated doc/ output is NOT staged: git status --porcelain shows no added doc/ paths.
  </acceptance_criteria>
  <done>All three packages build docs cleanly with the sealed-flap logo and favicon present in doc/assets/ and no new warnings; generated output not committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ex_doc build to doc output | ex_doc reads the logo/favicon source paths and copies them into doc/assets/. The only untrusted-input analog is the relative path resolving outside the package dir (../brandbook/). |
| package source to Hex tarball | The :files allowlist controls what ships to Hex; logo/favicon are auto-copied at doc-build time and are NOT in the tarball. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | docs/0 relative path resolves outside package dir | accept | ../brandbook/ is an intentional, committed monorepo path; docs only ever build from the monorepo checkout (D-05). No external input controls the path. |
| T-93-02 | Information Disclosure | :files allowlist accidentally widened to ship brandbook into the Hex tarball | mitigate | Acceptance criterion asserts no files: line changed in any mix.exs diff; ex_doc auto-copies at build time so no allowlist change is needed. |
| T-93-03 | Denial of Service | A new mix docs warning (missing/oversized SVG) silently degrades the published docs | mitigate | Task 3 fails on any NEW warning line in the build log for all three packages before commit. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; only SVG attribute edits, mix.exs keyword additions, and local doc builds against already-locked ex_doc 0.40.1. No new dependency introduced. |
</threat_model>

<verification>
- grep for width="164" height="156" in logo-mark.svg and width="16" height="16" in favicon.svg both match.
- All three docs/0 carry the correct relative logo/favicon paths (root no ../; siblings ../brandbook/...).
- mix docs builds cleanly for all three packages with doc/assets/logo.svg + doc/assets/favicon.svg present and no new warnings.
- No :files allowlist line changed; no SVG copies created; generated doc/ not committed.
- Every commit uses a non-release-triggering type (docs:/chore:); no release-please PR is created.
</verification>

<success_criteria>
- HEXD-01: the two canonical SVGs carry explicit width/height matching their viewBox; all three docs/0 reference the canonical brandbook/ logo+favicon via correct relative paths with no per-package copies and no :files change.
- HEXD-02: mix docs renders locally for all three packages with logo+favicon visible in doc/assets/ and no new warnings; all edits committed as non-release-triggering types; no Hex release cut.
</success_criteria>

<output>
Create `.planning/phases/93-hexdocs-wiring-and-release-hardening/93-01-SUMMARY.md` when done.
</output>
