---
phase: 27-release-install-closure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .github/workflows/post-publish-smoke.yml
  - .github/workflows/publish-hex.yml
  - .planning/phases/27-release-install-closure/27-02-EVIDENCE.md
autonomous: false
requirements: [REL-18]
must_haves:
  truths:
    - "post-publish-smoke.yml resolves the version-under-test from `github.event.release.tag_name` on the release-published path with no manual workaround required (D-27-05)."
    - "Both `publish-hex.yml` and `post-publish-smoke.yml` headers explicitly state `release.published` is canonical and `workflow_dispatch tag=...` is fallback-only (D-27-06), with version-agnostic phrasing replacing the stale `v0.3.0` references."
    - "The scheduled cron path remains an isolated independent health-check (D-27-07), unchanged in behavior."
    - "At least one rehearsal proof (Proof A or Proof B per RESEARCH.md §D2) is captured as a verification artifact at `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md`."
    - "The concurrency-group key on post-publish-smoke.yml line 23 is byte-identical to its pre-edit value (Pitfall 3)."
    - "The `cron-guard` JS body is byte-identical to its pre-edit value except for embedded comments (Pitfall 6 — do not rewrite working logic)."
  artifacts:
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "Updated header comment block on the `workflow_dispatch` trigger explicitly labeling release-event as canonical and dispatch as fallback-only, with version-agnostic phrasing."
      contains: "fallback"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Updated header comment block on the `workflow_dispatch` trigger with the same canonical/fallback labeling, version-agnostic."
      contains: "fallback"
    - path: ".planning/phases/27-release-install-closure/27-02-EVIDENCE.md"
      provides: "Rehearsal proof artifact (Proof A: `gh workflow run` URL/output OR Proof B: deferred-to-v0.4.0 statement with explicit forward-pointer)."
      contains: "Proof"
  key_links:
    - from: ".github/workflows/post-publish-smoke.yml header"
      to: "cron-guard job at line 34"
      via: "release.tag_name as canonical version source per D-27-05"
      pattern: "release\\.tag_name"
    - from: ".github/workflows/publish-hex.yml header"
      to: "checkout step ref resolution at lines 65/156/233"
      via: "github.event.inputs.tag || github.event.release.tag_name precedence"
      pattern: "github\\.event\\.(inputs\\.tag|release\\.tag_name)"
---

<objective>
Close REL-18 by hardening the comment blocks in both release-day workflows so
that future maintainers (and reviewers) can read the YAML header alone and
correctly identify `release.published` as the canonical version-source path
and `workflow_dispatch tag=...` as fallback-only (per D-27-05/06/07). Then
capture at least one rehearsal proof that the manual recovery path still
works (Proof A) OR document the deferred Proof B path against v0.4.0 ship.

Per RESEARCH.md §B5: the workflow LOGIC is already correct
(`cron-guard` already prefers `release.tag_name`; `publish-hex.yml` checkout
already prefers `github.event.inputs.tag || github.event.release.tag_name`).
This plan deliberately makes ZERO logic changes — only comment hardening +
rehearsal evidence — to minimize regression risk per Pitfall 3 and Pitfall 6.

Purpose: REL-18 was originally framed as a "version-resolution bug," but the
research clarifies the actual gap was (a) stale `v0.3.0`-specific comment
wording and (b) lack of an end-to-end rehearsal proof that exercises the
fallback dispatch path. Both are doc/ops work, not logic work.

Output: Updated YAML header comments, rehearsal evidence artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-release-install-closure/27-CONTEXT.md
@.planning/phases/27-release-install-closure/27-RESEARCH.md
@.planning/phases/27-release-install-closure/27-VALIDATION.md
@.planning/phases/27-release-install-closure/27-01-PLAN.md
@./CLAUDE.md
@.github/workflows/post-publish-smoke.yml
@.github/workflows/publish-hex.yml
@.planning/milestones/v0.3-phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md

<interfaces>
<!-- Current header structures, extracted verbatim per RESEARCH.md §B1, §B5. -->

`.github/workflows/post-publish-smoke.yml` (lines 1-25, current):

```yaml
name: post-publish-smoke

on:
  release:
    types: [published]
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:
    # Canonical fallback for v0.3.0 if publish succeeds but automatic smoke
    # fan-out does not occur: dispatch against the same reviewed core release
    # tag used for publish (e.g. `mailglass-v0.3.0`).
    inputs:
      tag:
        description: "Tag to smoke-test (e.g. mailglass-v0.3.0)"
        required: false
        type: string

permissions:
  contents: read
  issues: write

concurrency:
  group: post-publish-smoke-${{ github.event.inputs.tag || github.event.release.tag_name || github.ref }}
  cancel-in-progress: false
```

`.github/workflows/publish-hex.yml` (lines 1-15, current):

```yaml
name: publish-hex

on:
  release:
    types: [published]
  workflow_dispatch:
    # Canonical fallback for v0.3.0 if the release event does not fan out:
    # dispatch this workflow against the core release tag (e.g.
    # `mailglass-v0.3.0`), not `main`.
    inputs:
      tag:
        description: "Git tag/ref to publish from (canonical fallback: mailglass-v0.3.0)"
        required: true
        type: string
```

The `concurrency:` block at line 22-24 of post-publish-smoke.yml is BYTE-CRITICAL
per Pitfall 3. The `cron-guard` JS body (lines 34-128) is BYTE-CRITICAL except
for embedded comments per Pitfall 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale v0.3.0 wording in post-publish-smoke.yml workflow_dispatch comments</name>
  <files>.github/workflows/post-publish-smoke.yml</files>
  <read_first>
    - .github/workflows/post-publish-smoke.yml (full file — confirm current line numbers for trigger header lines 1-16, concurrency line 22-24, and the historical-note comment at lines 73-76)
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§B5 item 1, Pitfall 3, Pitfall 6)
    - .planning/phases/27-release-install-closure/27-CONTEXT.md (D-27-05, D-27-06, D-27-07 — the canonical/fallback/scheduled distinction the comments must reflect)
  </read_first>
  <action>
    Edit `.github/workflows/post-publish-smoke.yml` to replace the stale
    `v0.3.0`-specific wording in the `workflow_dispatch` header with version-
    agnostic phrasing that matches D-27-05/06/07. Make ONLY the changes below.
    Do NOT touch the `concurrency:` block (Pitfall 3). Do NOT touch the
    `cron-guard` JS body except as noted (Pitfall 6).

    Change 1 (workflow_dispatch comment block, currently lines 9-11):

    Replace these THREE lines:
    ```yaml
        # Canonical fallback for v0.3.0 if publish succeeds but automatic smoke
        # fan-out does not occur: dispatch against the same reviewed core release
        # tag used for publish (e.g. `mailglass-v0.3.0`).
    ```

    With these FIVE lines (insert version-agnostic canonical/fallback contract
    statement per D-27-05/06/07):
    ```yaml
        # Trigger contract (per Phase 27, D-27-05/06/07):
        #   - release: types: [published] is the CANONICAL release-day path; version
        #     is resolved from `github.event.release.tag_name`.
        #   - workflow_dispatch is FALLBACK-ONLY for manual recovery or rehearsal;
        #     supply the published tag (e.g. `mailglass-vX.Y.Z`) via `inputs.tag`.
        #   - schedule (`cron`) is an INDEPENDENT health-check that resolves the
        #     latest published release on its own; it must not gate release-day
        #     correctness.
    ```

    Change 2 (input description, currently line 14):

    Replace:
    ```yaml
            description: "Tag to smoke-test (e.g. mailglass-v0.3.0)"
    ```

    With (version-agnostic example):
    ```yaml
            description: "Tag to smoke-test (e.g. mailglass-vX.Y.Z); fallback path only"
    ```

    Change 3 (historical-note comment inside cron-guard JS, currently lines 73-76):

    Replace this comment block:
    ```javascript
                // Historical note from the 2026-04-28 rehearsal: an older
                // workflow_run-triggered path ended up resolving VERSION=main and
                // waited on `mix hex.info mailglass main`. Keep the manual fallback
                // tag-based so version resolution stays deterministic.
    ```

    With this updated comment block (preserves the lesson, updates the framing
    to post-Phase-27 contract):
    ```javascript
                // Historical note (2026-04-28 rehearsal, archived in
                // .planning/milestones/v0.3-phases/18-ship-v0-3-0/): an older
                // workflow_run-triggered path resolved VERSION=main and stalled on
                // `mix hex.info mailglass main`. Per Phase 27 D-27-05/06: the
                // fallback dispatch path stays tag-based so version resolution is
                // deterministic from the supplied input.
    ```

    Do NOT change ANY OTHER line. Do NOT change the `concurrency:` block at
    line 22-24. Do NOT change any branching logic in the `cron-guard` JS body.
    Do NOT rename steps, jobs, outputs, or env keys.
  </action>
  <verify>
    <automated>diff <(grep -c "concurrency:" .github/workflows/post-publish-smoke.yml) <(echo 1) &amp;&amp; grep -q "post-publish-smoke-\${{ github.event.inputs.tag || github.event.release.tag_name || github.ref }}" .github/workflows/post-publish-smoke.yml &amp;&amp; echo "concurrency-key preserved" || echo "FAIL: concurrency drift"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v0.3.0" .github/workflows/post-publish-smoke.yml` returns 0 (all stale version-specific phrasing removed).
    - `grep -c "Trigger contract" .github/workflows/post-publish-smoke.yml` returns 1.
    - `grep -c "CANONICAL release-day path" .github/workflows/post-publish-smoke.yml` returns 1.
    - `grep -c "FALLBACK-ONLY for manual recovery" .github/workflows/post-publish-smoke.yml` returns 1.
    - `grep -c "INDEPENDENT health-check" .github/workflows/post-publish-smoke.yml` returns 1.
    - `grep -c "mailglass-vX.Y.Z" .github/workflows/post-publish-smoke.yml` returns at least 1.
    - `grep -F "post-publish-smoke-\${{ github.event.inputs.tag || github.event.release.tag_name || github.ref }}" .github/workflows/post-publish-smoke.yml | wc -l | tr -d ' '` returns 1 (concurrency key byte-identical, Pitfall 3).
    - `grep -F "Phase 27 D-27-05/06" .github/workflows/post-publish-smoke.yml` returns 1 (updated historical-note comment present).
    - `grep -F "Historical note from the 2026-04-28" .github/workflows/post-publish-smoke.yml` returns 0 (old wording replaced).
    - `actionlint -ignore 'shellcheck:.*' .github/workflows/post-publish-smoke.yml` exits 0 (or matches whatever exit-zero result the repo's CI `actionlint.yml` uses).
  </acceptance_criteria>
  <done>Workflow header comments reflect post-Phase-27 contract; concurrency-group key byte-identical; cron-guard JS branching logic untouched (only its comments updated).</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale v0.3.0 wording in publish-hex.yml workflow_dispatch comments</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/publish-hex.yml (full file — confirm current line numbers for trigger header lines 1-15 and any other v0.3.0 mentions; verify the checkout `ref:` precedence at lines 65, 156, 233 is `${{ github.event.inputs.tag || github.event.release.tag_name }}`)
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§B3, §B5 item 1)
    - .planning/phases/27-release-install-closure/27-CONTEXT.md (D-27-05, D-27-06)
  </read_first>
  <action>
    Edit `.github/workflows/publish-hex.yml` to replace the stale
    `v0.3.0`-specific wording in the `workflow_dispatch` header. Make ONLY the
    changes below. Do NOT touch any job, step, or environment block. Do NOT
    change the `ref:` precedence on the checkout steps (already correct per
    RESEARCH.md §B3).

    Change 1 (workflow_dispatch comment block, currently lines 7-9):

    Replace these THREE lines:
    ```yaml
        # Canonical fallback for v0.3.0 if the release event does not fan out:
        # dispatch this workflow against the core release tag (e.g.
        # `mailglass-v0.3.0`), not `main`.
    ```

    With these FIVE lines:
    ```yaml
        # Trigger contract (per Phase 27, D-27-05/06):
        #   - release: types: [published] is the CANONICAL publish path;
        #     `github.event.release.tag_name` resolves the tag/ref to publish.
        #   - workflow_dispatch is FALLBACK-ONLY for manual recovery; supply the
        #     published tag (e.g. `mailglass-vX.Y.Z`) via `inputs.tag`. Never
        #     dispatch against `main` — version resolution is tag-based.
    ```

    Change 2 (input description, currently line 12):

    Replace:
    ```yaml
            description: "Git tag/ref to publish from (canonical fallback: mailglass-v0.3.0)"
    ```

    With:
    ```yaml
            description: "Git tag/ref to publish from; fallback-only (e.g. mailglass-vX.Y.Z)"
    ```

    Search for any OTHER `v0.3.0` literal in the file:
    ```sh
    grep -n "v0.3.0" .github/workflows/publish-hex.yml
    ```

    If any are found that are NOT inside a deliberately-historical commit-SHA
    pin or release-name string, replace each one with `vX.Y.Z` for example
    consistency. If they appear inside a step that references a real
    historical run/sha, leave them untouched and note them in the SUMMARY.

    Do NOT change ANY logic, step name, env key, secret reference, environment
    name, or ref precedence in the file. Do NOT change the `gate-ci-green`
    job's `actions/github-script` body (lines 121-143 per RESEARCH.md §B6).
  </action>
  <verify>
    <automated>grep -c "v0.3.0" .github/workflows/publish-hex.yml | grep -E '^[01]$' &amp;&amp; grep -q "Trigger contract" .github/workflows/publish-hex.yml &amp;&amp; echo "publish-hex.yml updated" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Canonical fallback for v0.3.0" .github/workflows/publish-hex.yml` returns 0.
    - `grep -c "Trigger contract" .github/workflows/publish-hex.yml` returns 1.
    - `grep -c "CANONICAL publish path" .github/workflows/publish-hex.yml` returns 1.
    - `grep -c "FALLBACK-ONLY for manual recovery" .github/workflows/publish-hex.yml` returns 1.
    - `grep -c "Never dispatch against \`main\`" .github/workflows/publish-hex.yml` returns 1 (the "never dispatch against main" guidance is present).
    - `grep -F "github.event.inputs.tag || github.event.release.tag_name" .github/workflows/publish-hex.yml | wc -l | tr -d ' '` returns at least 3 (the three checkout `ref:` occurrences at lines ~65, ~156, ~233 per RESEARCH.md §B3 are preserved).
    - `actionlint -ignore 'shellcheck:.*' .github/workflows/publish-hex.yml` exits 0 (matches CI's actionlint configuration).
  </acceptance_criteria>
  <done>publish-hex.yml header reflects post-Phase-27 contract; ref precedence unchanged on all three checkout steps; no remaining `v0.3.0` literal in trigger comments.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Capture rehearsal proof and write evidence artifact</name>
  <files>.planning/phases/27-release-install-closure/27-02-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§D2 — Proof A vs Proof B)
    - .planning/phases/27-release-install-closure/27-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/27-release-install-closure/27-01-PLAN.md (Plan 27-01 must have shipped first so the consumer-install step can succeed against the rehearsal tag)
  </read_first>
  <action>
    This is a checkpoint task that REQUIRES human action — the executor agent
    cannot complete REL-18's rehearsal proof without either invoking
    `gh workflow run` against the live GitHub Actions API (Proof A) or
    capturing a forward-pointer narrative (Proof B). Follow the procedure in
    `<how-to-verify>` below to write
    `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md`. The
    executor MAY invoke `gh` CLI commands automatically if the operator
    chooses Proof A; if Proof B is chosen the file write is fully autonomous.
    This task pauses for the operator's `approved` signal once the evidence
    file is in place per `<resume-signal>` below.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/27-release-install-closure/27-02-EVIDENCE.md; test -f "$F" && grep -q "Proof selected:" "$F" && grep -qE "Proof selected:\*\* (A|B)" "$F" && grep -qE "D-27-0[56]" "$F" && echo OK || echo "FAIL: evidence artifact missing or incomplete"'</automated>
  </verify>
  <what-built>
    YAML comment hardening on both release-day workflows (Tasks 1 + 2) plus an
    untouched-but-correct `cron-guard` JS body that already prefers
    `release.tag_name`. The remaining REL-18 obligation per RESEARCH.md §D2 is
    one rehearsal proof.
  </what-built>
  <how-to-verify>
    Pick exactly ONE of Proof A or Proof B and capture the output to
    `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md`.

    **Proof A (recommended — exercises the fallback dispatch path on an existing
    live tag):**

    1. Confirm Plan 27-01 has merged (its template fix must be live so the
       consumer-install step in post-publish-smoke.yml does not hit the
       Swoosh.ApiClient.Finch crash):
       ```sh
       gh pr list --search "fix(installer)" --state merged --limit 5
       ```
       Verify the most recent installer fix matches the Plan 27-01 commit.

    2. Trigger the rehearsal via fallback dispatch against the existing
       `mailglass-v0.3.2` tag:
       ```sh
       gh workflow run post-publish-smoke.yml -f tag=mailglass-v0.3.2
       ```

    3. Wait for the run to complete and capture the URL + outcome:
       ```sh
       sleep 30
       gh run list --workflow=post-publish-smoke.yml --limit 1 --json databaseId,status,conclusion,url,headBranch
       RUN_ID=$(gh run list --workflow=post-publish-smoke.yml --limit 1 --json databaseId --jq '.[0].databaseId')
       gh run watch "$RUN_ID"
       gh run view "$RUN_ID" --log | tail -200
       ```

    4. Confirm the `cron-guard` job output `version=0.3.2` and the
       `consumer-install` job exited 0. If `consumer-install` fails because
       the published 0.3.2 artifact does not yet have the REL-17 fix (it
       won't — REL-17 ships with v0.4.0), that is EXPECTED. The proof is
       that `cron-guard` correctly resolved version=0.3.2 from the
       `inputs.tag` path AND that `consumer-install` reached its
       `mix mailglass.install` step (i.e., the fallback dispatch path itself
       works end-to-end). Note this expected-failure caveat in the evidence
       artifact.

    5. Write the evidence artifact:
       ```
       .planning/phases/27-release-install-closure/27-02-EVIDENCE.md
       ```

       Contents (template):
       ```markdown
       # Phase 27 — REL-18 Rehearsal Evidence

       **Proof selected:** A (fallback dispatch against existing live tag)
       **Date:** YYYY-MM-DD
       **Run URL:** <gh run view URL from step 3>
       **Run ID:** <RUN_ID>

       ## cron-guard outputs
       - should_run: true
       - version: 0.3.2
       - source: workflow_dispatch inputs.tag

       ## consumer-install outcome
       - <pass/fail/expected-fail-with-reason>
       - If expected-fail: explain that v0.3.2 ships pre-REL-17 so the
         api_client crash is anticipated; the proof point is that the
         dispatch path reached this step at all, with version resolved
         deterministically.

       ## What this proves
       - D-27-06: workflow_dispatch tag=... is a working fallback recovery path.
       - D-27-05: indirectly — the same `cron-guard` job that handles
         release-event uses the same JS branching; release-event path will
         resolve `release.tag_name` analogously when v0.4.0 ships.
       - Pitfall 3: concurrency-group key was preserved (verified by Task 1
         acceptance criteria).

       ## What is still deferred
       - Proof B (release-event auto-triggered smoke run) is captured by the
         actual v0.4.0 ship event in milestone-close artifacts. This evidence
         is sufficient for Phase 27 closure per RESEARCH.md §D2.
       ```

    **Proof B (acceptable alternative — defer to v0.4.0 ship):**

    If the maintainer prefers to defer rehearsal until the v0.4.0 release
    actually fires (because v0.4.0 is imminent), write
    `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md` with this
    template instead:

    ```markdown
    # Phase 27 — REL-18 Rehearsal Evidence (Deferred Proof B)

    **Proof selected:** B (deferred to v0.4.0 release-event ship)
    **Date deferred:** YYYY-MM-DD
    **Forward-pointer:** v0.4.0 milestone-close ship phase will capture the
      auto-triggered release.published smoke run as the canonical proof.

    ## Why deferred
    - <rationale, e.g. "v0.4.0 is days away; running Proof A against v0.3.2
      would falsely fail consumer-install because v0.3.2 ships pre-REL-17.">

    ## Pre-shipping confidence
    - Workflow YAML comments hardened (Tasks 1+2 of plan 27-02).
    - cron-guard JS branching unchanged (already verified correct in
      RESEARCH.md §B1, §B5).
    - Concurrency-group key byte-identical (Task 1 acceptance criteria).

    ## At v0.4.0 ship, capture
    - Run URL of the release.published-triggered post-publish-smoke run.
    - cron-guard outputs (`version` matches v0.4.0).
    - consumer-install exit 0 (REL-17 fix is live in v0.4.0 artifact).
    ```

    Either Proof A or Proof B satisfies REL-18 per D-27-06 and RESEARCH.md
    §D2; both must produce the evidence file at the path above.
  </how-to-verify>
  <resume-signal>
    Type "approved" once `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md`
    exists with Proof A details (run URL + cron-guard outputs) OR Proof B
    deferred-statement (forward-pointer to v0.4.0 ship). Either is acceptable.
  </resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md` exists.
    - `grep -c "Proof selected:" .planning/phases/27-release-install-closure/27-02-EVIDENCE.md` returns 1.
    - File contains EITHER `Proof selected:** A` (with `Run URL:`, `cron-guard outputs`, and a `version: 0.3.2` line) OR `Proof selected:** B` (with `Forward-pointer:` referencing v0.4.0).
    - `grep -c "D-27-0[56]" .planning/phases/27-release-install-closure/27-02-EVIDENCE.md` returns at least 1 (evidence references the locked decisions it satisfies).
    - For Proof A: file references a real `gh run view` URL on github.com/szTheory/mailglass.
  </acceptance_criteria>
  <done>Evidence artifact committed; REL-18 closure can be claimed citing this artifact.</done>
</task>

</tasks>

<verification>
- `grep -c "v0.3.0" .github/workflows/post-publish-smoke.yml .github/workflows/publish-hex.yml | grep -v ':0$' | wc -l | tr -d ' '` returns 0 (no remaining stale version literals in either file's comments).
- Concurrency-group key on post-publish-smoke.yml line 23 is byte-identical to its pre-edit value (Pitfall 3): `grep -F "post-publish-smoke-\${{ github.event.inputs.tag || github.event.release.tag_name || github.ref }}" .github/workflows/post-publish-smoke.yml | wc -l` returns 1.
- `cron-guard` JS branching logic untouched per Pitfall 6: `git diff .github/workflows/post-publish-smoke.yml | grep -E "^[+-].*core\\.(setOutput|info)" | wc -l` returns 0 (no `+`/`-` diff lines on the JS branching primitives).
- actionlint exits 0 on both workflows (matches CI's `actionlint.yml` configuration).
- `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md` exists with either Proof A or Proof B captured.

**CHANGELOG hygiene (Pitfall 4):** Use `chore(release):` Conventional Commit
prefix for these workflow + evidence commits. Do NOT manually edit
CHANGELOG.md — Release Please handles the changelog from the commit messages.
</verification>

<success_criteria>
- REL-18 closed: both `post-publish-smoke.yml` and `publish-hex.yml` headers
  describe the canonical/fallback/scheduled trigger contract using
  version-agnostic phrasing per D-27-05/06/07.
- Concurrency-group key on post-publish-smoke.yml byte-identical to pre-edit
  (Pitfall 3).
- `cron-guard` JS branching logic unchanged (only embedded comments updated)
  per Pitfall 6.
- One rehearsal proof (Proof A or Proof B) captured in
  `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md`.
- No edits outside the two workflow files and the evidence artifact.
- All third-party GitHub Actions remain SHA-pinned (no action upgrades in this
  plan, per CLAUDE.md).
</success_criteria>

<output>
After completion, create
`.planning/phases/27-release-install-closure/27-02-SUMMARY.md` documenting:
- The exact diff of both workflow files (header comments only).
- Confirmation that the concurrency-group key was preserved (verbatim).
- Which proof (A or B) was selected and the evidence artifact path.
- Conventional Commit prefix used (`chore(release):`); explicit confirmation
  that CHANGELOG.md was NOT manually edited (Pitfall 4).
</output>
