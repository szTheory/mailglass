---
phase: 26-runtime-per-tenant-adapter-resolution
plan: "03"
type: execute
wave: 3
depends_on:
  - "26-01"
  - "26-02"
files_modified:
  - guides/multi-tenancy.md
  - README.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements:
  - TENANT-01
  - TENANT-02
  - TENANT-03
tags:
  - tenant
  - docs
  - routing
  - regression

must_haves:
  truths:
    - "The public docs describe the shipped runtime routing contract without implying hidden caches, failover, or mandatory multi-tenant setup."
    - "Examples show named adapter refs, the tenancy callback, and the unchanged single-tenant default path using real Mailglass APIs."
    - "Doc contract tests compile the new examples so the public guidance cannot drift from the shipped API."
  artifacts:
    - path: "guides/multi-tenancy.md"
      provides: "adopter-facing runtime routing guide with named adapter refs and callback examples"
      contains: "resolve_outbound_adapter_ref"
    - path: "README.md"
      provides: "concise public summary of runtime per-tenant adapter resolution"
      contains: "adapter_ref"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "compiled doc examples and wording checks for the new routing contract"
      contains: "multi-tenancy"
  key_links:
    - from: "guides/multi-tenancy.md"
      to: "lib/mailglass/tenancy.ex"
      via: "guide examples must use the actual callback and sentinel behavior"
      pattern: "resolve_outbound_adapter_ref|:default"
    - from: "guides/multi-tenancy.md"
      to: "lib/mailglass/config.ex"
      via: "docs must reflect the additive `adapters:` registry instead of inventing runtime registry APIs"
      pattern: "config :mailglass, adapters:"
    - from: "test/mailglass/docs_contract_test.exs"
      to: "guides/multi-tenancy.md"
      via: "contract tests compile and grep the new examples"
      pattern: "adapter_ref|resolve_outbound_adapter_ref"
---

<objective>
Finish Phase 26 with honest public docs and contract tests for the shipped routing surface.

Purpose: this phase changes a public configuration and tenancy seam. It is not complete if the guide still only covers tenant stamping, or if the README implies more magic than the runtime actually ships. The final plan turns the implementation into an adopter-usable documented feature and keeps the examples executable.

Output:
- updated multi-tenancy guide with runtime adapter-routing examples
- concise README mention of the new advanced routing surface
- docs contract coverage that compiles and pins the examples
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/26-runtime-per-tenant-adapter-resolution/26-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/26-runtime-per-tenant-adapter-resolution/26-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/26-runtime-per-tenant-adapter-resolution/26-PATTERNS.md
@/Users/jon/projects/mailglass/guides/multi-tenancy.md
@/Users/jon/projects/mailglass/config/runtime.exs
@/Users/jon/projects/mailglass/README.md
@/Users/jon/projects/mailglass/test/mailglass/docs_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 26-03-01: Update the multi-tenancy guide, runtime config example, and README for the shipped routing surface</name>
  <files>guides/multi-tenancy.md, config/runtime.exs, README.md</files>
  <read_first>
    - guides/multi-tenancy.md
    - config/runtime.exs
    - README.md
    - lib/mailglass/tenancy.ex
    - lib/mailglass/config.ex
    - lib/mailglass/outbound.ex
    - .planning/phases/26-runtime-per-tenant-adapter-resolution/26-CONTEXT.md
    - .planning/phases/26-runtime-per-tenant-adapter-resolution/26-RESEARCH.md
  </read_first>
  <action>
    Rewrite `guides/multi-tenancy.md` and refresh `config/runtime.exs` so they document the actual Phase 26 runtime-routing surface per D-26-19 through D-26-25. The guide and config example must cover:
    - the unchanged single-tenant default path using only `config :mailglass, adapter`
    - the additive `config :mailglass, adapters:` registry for named route refs
    - the `Mailglass.Tenancy.resolve_outbound_adapter_ref/1` callback with explicit `:default` fallback behavior
    - one example each for:
      - different ESP per tenant
      - same ESP family with different per-tenant credentials or subaccounts
      - same provider family with different stream/domain route refs
    - the async semantics: queue-time `adapter_ref` is persisted for determinism, but credentials still resolve from runtime config at dispatch time

    Keep the guide honest: do not mention failover, round-robin, cache invalidation, dynamic control-plane APIs, or any stateful adapter registry because those are explicitly deferred per D-26-16 and D-26-18. If the implementation from Plan 26-02 rejects raw `opts[:adapter]` on queued paths, document that plainly and point adopters to `adapter_ref` for queued overrides.

    `config/runtime.exs` should become the canonical example surface for named route refs and runtime credential lookup. Keep it minimal and honest: no failover, no cache, no dynamic control-plane claims, no secret persistence.

    Update `README.md` with a concise public-facing note that runtime per-tenant adapter resolution is now supported through tenancy callbacks plus named adapter refs, while the zero-config single-tenant default remains unchanged.
  </action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "resolve_outbound_adapter_ref|adapter_ref|config :mailglass, adapters:" guides/multi-tenancy.md config/runtime.exs` returns the shipped routing contract.
    - `rg -n "different ESP|credentials|stream|domain" guides/multi-tenancy.md` returns the three common-case examples requested in D-26-22.
    - `rg -n "failover|round-robin|registry process|cache" guides/multi-tenancy.md config/runtime.exs README.md` does not introduce deferred-scope promises.
    - `rg -n "single-tenant|adapter_ref|tenancy callback" README.md` returns the concise README summary.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The public docs now describe the actual runtime routing contract and its boundaries without inventing extra infrastructure or degrading the single-tenant story.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 26-03-02: Lock the docs against API drift with contract coverage</name>
  <files>test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - test/mailglass/docs_contract_test.exs
    - guides/multi-tenancy.md
    - README.md
    - lib/mailglass/tenancy.ex
    - lib/mailglass/config.ex
    - lib/mailglass/outbound.ex
    - .planning/phases/26-runtime-per-tenant-adapter-resolution/26-RESEARCH.md
  </read_first>
  <action>
    Expand `test/mailglass/docs_contract_test.exs` so the Phase 26 guide and README become load-bearing.

    The docs contract tests must prove:
    - the main code example in `guides/multi-tenancy.md` containing `resolve_outbound_adapter_ref/1` parses and compiles
    - the guide includes the additive `config :mailglass, adapters:` example surface
    - the guide still shows the single-tenant default path
    - the README mention of runtime per-tenant routing stays aligned to the shipped public terms (`tenancy`, `adapter_ref`, named refs) and does not claim failover/load-balancing behavior

    Keep the tests in the existing docs-contract style: extract code blocks, parse them, and assert on specific phrases or option names. Do not replace the docs suite with broad snapshot text assertions.
  </action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "resolve_outbound_adapter_ref|adapter_ref|adapters:" test/mailglass/docs_contract_test.exs` returns explicit Phase 26 doc checks.
    - `rg -n "single-tenant|failover|round-robin" test/mailglass/docs_contract_test.exs` returns both positive and negative wording assertions.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The runtime routing docs compile, the wording is pinned to the shipped API, and future edits cannot quietly reintroduce deferred-scope promises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> adopter configuration | Guides and README shape how adopters wire routing, secrets, and async overrides |
| Code examples -> compiled public contract | Shipped snippets become de facto API promises |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Repudiation | async routing semantics | mitigate | Document queue-time `adapter_ref` persistence and dispatch-time credential lookup explicitly so operators can reason about retries and config changes. |
| T-26-10 | Tampering | docs scope drift | mitigate | Exclude failover, load balancing, caches, and runtime registry APIs from the guide and pin that absence in docs contract tests per D-26-16 and D-26-18. |
| T-26-11 | Regression | single-tenant adoption path | mitigate | Keep the zero-config default example in the guide and README so advanced routing docs do not imply new required setup per D-26-05 and D-26-25. |
</threat_model>

<verification>
- mix test test/mailglass/docs_contract_test.exs --warnings-as-errors
</verification>

<success_criteria>
Phase 26 ends with honest public docs for the new routing contract and contract tests that compile the examples and pin the scope boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/26-runtime-per-tenant-adapter-resolution/26-03-SUMMARY.md`
</output>
