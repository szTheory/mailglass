---
phase: 22-operator-data-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "22-01"
  - "22-02"
files_modified:
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - test/support/admin_case.ex
  - mailglass_admin/test/support/live_view_case.ex
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
tags:
  - admin
  - test
  - verification
  - accessibility

must_haves:
  truths:
    - "The operator surface has first-party LiveView tests covering filter state, row selection, timeline rendering, suppression copy, and empty states."
    - "Tests assert the exact UI copy and interaction contract called out in `22-UI-SPEC.md`."
    - "Admin test support is extended through existing shared harness files rather than ad hoc helpers."
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "operator-surface interaction coverage"
      contains: "No recent deliveries"
    - path: "test/support/admin_case.ex"
      provides: "shared admin test support for the operator surface"
      contains: "Mailglass.AdminCase"
  key_links:
    - from: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "interaction tests cover operator params and selection behavior"
      pattern: "live\\("
    - from: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      to: ".planning/phases/22-operator-data-foundation/22-UI-SPEC.md"
      via: "literal copy and contract assertions"
      pattern: "Reversible in a later phase|Immutable by policy"
---

<objective>
Lock the operator data foundation with high-signal LiveView verification and any small shared-test-harness extensions required to exercise the new screen cleanly.

Purpose: Phase 22 should not finish with only backend tests and a compiled UI. It needs interaction-level proof that the operator surface preserves URL state, renders required copy, and stays read-only.

Output:
- `mailglass_admin/test/mailglass_admin/operator_live_test.exs`
- minimal shared test-harness support updates if required
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/phases/22-operator-data-foundation/22-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/22-operator-data-foundation/22-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/phases/22-operator-data-foundation/22-UI-SPEC.md
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/operator_live.ex
@/Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/preview_live_test.exs
@/Users/jon/projects/mailglass/test/support/admin_case.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 22-03-01: Add operator LiveView interaction coverage</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/preview_live_test.exs
    - mailglass_admin/lib/mailglass_admin/operator_live.ex
    - .planning/phases/22-operator-data-foundation/22-UI-SPEC.md
    - test/support/admin_case.ex
  </read_first>
  <action>
    Create `mailglass_admin/test/mailglass_admin/operator_live_test.exs` following the interaction style used by `preview_live_test.exs`. Cover at minimum:
    - default screen with no delivery selected
    - recent deliveries empty state
    - filter interaction and URL-backed state preservation
    - selecting a delivery and rendering summary + timeline + suppression card
    - no timeline events state
    - suppression reversible vs immutable copy
    - absence of replay or suppression-mutation controls in Phase 22

    Use literal-string assertions for the approved copy from `22-UI-SPEC.md`.
  </action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "No recent deliveries|Select a delivery to inspect its event timeline and suppression state|No delivery events have been recorded for this item yet|Reversible in a later phase|Immutable by policy" mailglass_admin/test/mailglass_admin/operator_live_test.exs` returns the required assertions.
    - `rg -n "render_click|render_change|live\\(" mailglass_admin/test/mailglass_admin/operator_live_test.exs` shows real LiveView interaction coverage.
    - `rg -n "replay|remove suppression|recent auth|recent-auth" mailglass_admin/test/mailglass_admin/operator_live_test.exs` returns only negative assertions or no matches, not Phase 24 behavior tests.
    - `mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The operator screen has first-party interaction tests that lock its copy, state flow, and read-only boundary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-03-02: Extend shared admin test support only where needed</name>
  <files>test/support/admin_case.ex, mailglass_admin/test/support/live_view_case.ex</files>
  <read_first>
    - test/support/admin_case.ex
    - mailglass_admin/test/support/live_view_case.ex
    - mailglass_admin/test/mailglass_admin/preview_live_test.exs
  </read_first>
  <action>
    If operator tests need shared fixtures, endpoint helpers, or imports beyond the current admin harness, extend the existing support files minimally. Keep the additions specific to reusable admin LiveView testing and avoid introducing a second bespoke harness just for the operator page.
  </action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/admin_case.ex` remains the primary shared admin-case entry point.
    - Any edits to `mailglass_admin/test/support/live_view_case.ex` are support-oriented and do not hardcode operator-only business logic.
    - `mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The operator tests run through the established admin harness without regressing the preview LiveView test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → operator LiveView | Fixtures and params simulate operator interaction with the new screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Repudiation / Verification Gap | operator screen | mitigate | Add explicit interaction tests for required copy, state transitions, and read-only boundary so the phase cannot pass on compile-only evidence. |
| T-22-10 | Accessibility Drift | selected-row/timeline UI | mitigate | Tests should assert presence of selection and state text seams that back semantic rendering rather than relying only on visual treatment. |
| T-22-11 | Regression | existing preview admin surface | mitigate | Run operator tests alongside `preview_live_test.exs` and keep harness changes minimal and shared. |
</threat_model>

<verification>
- mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs --warnings-as-errors
- mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs --warnings-as-errors
</verification>

<success_criteria>
The operator surface is protected by interaction-level tests and any supporting harness changes preserve the existing preview LiveView contract.
</success_criteria>

<output>
After completion, create `.planning/phases/22-operator-data-foundation/22-03-SUMMARY.md`
</output>
