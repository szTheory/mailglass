---
phase: 23-production-admin-mount-and-step-up-auth
plan: "02"
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/mount.ex
  - mailglass_admin/lib/mailglass_admin/auth.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/test/mailglass_admin/auth_test.exs
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/test/support/live_view_case.ex
  - mailglass_admin/test/support/endpoint_case.ex
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-05
tags:
  - admin
  - auth
  - operator
  - liveview

must_haves:
  truths:
    - "The production operator surface has a dedicated `on_mount` auth seam instead of reusing preview discovery behavior."
    - "Future destructive operator actions have one server-side recent-auth authorization contract to call."
    - "The current read-only operator UI remains intact while gaining explicit actor/auth context for later phases."
    - "No hard dependency on Sigra, `phx.gen.auth`, or any adopter-specific auth stack is introduced."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/mount.ex"
      provides: "operator-only LiveView mount auth hook"
      contains: "def on_mount"
    - path: "mailglass_admin/lib/mailglass_admin/auth.ex"
      provides: "generic recent-auth authorization contract"
      contains: "authorize"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "operator surface wired to the new auth seam"
      contains: "assign"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/mount.ex"
      to: "mailglass_admin/lib/mailglass_admin/router.ex"
      via: "operator `live_session` invokes the new mount hook"
      pattern: "Operator.Mount"
    - from: "mailglass_admin/lib/mailglass_admin/auth.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "future replay/removal handlers use one server-side authorization seam"
      pattern: "authorize|recent"
    - from: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "mount/auth non-regression coverage pins operator behavior"
      pattern: "unauthorized|recent|operator"
---

<objective>
Introduce the operator-specific auth and recent-auth seams that make the production mount safe now and make later destructive actions enforceable server-side.

Purpose: Phase 23 should not ship replay or suppression-removal behavior yet, but it must land the mount/auth contract those later actions will depend on. The critical outcome is a generic, adopter-owned authorization seam, not a hard-coded auth stack.

Output:
- new operator mount hook
- new generic auth/recent-auth contract
- operator LiveView wired to the auth seam without adding destructive controls
- tests covering authorized/unauthorized mount behavior and read-only non-regression
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/23-production-admin-mount-and-step-up-auth/23-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/23-production-admin-mount-and-step-up-auth/23-PATTERNS.md
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/router.ex
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/preview/mount.ex
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/operator_live.ex
@/Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/operator_live_test.exs
@/Users/jon/projects/mailglass/lib/mailglass/optional_deps/sigra.ex
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/optional_deps/phoenix_live_reload.ex
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 23-02-01: Add operator mount and generic authorization contract</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/mount.ex, mailglass_admin/lib/mailglass_admin/auth.ex, mailglass_admin/test/mailglass_admin/auth_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/preview/mount.ex
    - mailglass_admin/lib/mailglass_admin/router.ex
    - .planning/phases/23-production-admin-mount-and-step-up-auth/23-RESEARCH.md
    - .planning/phases/23-production-admin-mount-and-step-up-auth/23-PATTERNS.md
    - lib/mailglass/optional_deps/sigra.ex
  </read_first>
  <action>
    Create `mailglass_admin/lib/mailglass_admin/operator/mount.ex` as the operator-specific `on_mount/4` hook. It should follow the same LiveView mount control-flow shape as `Preview.Mount`, but enforce operator access instead of preview discovery. The hook should consume only the whitelisted operator session data produced in Plan 23-01 and either continue with explicit auth-related assigns or halt/redirect/raise in a controlled way when the adopter-owned auth contract says access is denied.

    Create `mailglass_admin/lib/mailglass_admin/auth.ex` as the library-owned generic authorization seam. Keep it stack-agnostic: no compile-time references to `Sigra`, `MyAppWeb.UserAuth`, or any adopter module. The contract should cover at least:
    - operator-page access authorization
    - recent-auth/destructive-action authorization for future replay/removal handlers
    - a normalized actor/auth-context return shape suitable for audit metadata later

    A behaviour, callback contract, or equivalent explicit adapter seam is acceptable. The main requirement is that later plans can call one server-side mailglass-owned contract instead of inventing custom checks per action.

    Add `mailglass_admin/test/mailglass_admin/auth_test.exs` as Wave 0 contract proof for this seam. It should assert the normalized result shapes for success, `:unauthorized`, and `:stale_auth`, plus the minimal actor metadata contract defined in `23-RESEARCH.md`.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/auth_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def on_mount" mailglass_admin/lib/mailglass_admin/operator/mount.ex` returns one mount hook.
    - `rg -n "authorize|recent|actor" mailglass_admin/lib/mailglass_admin/auth.ex` returns the generic auth contract surface.
    - `rg -n "unauthorized|stale_auth|subject_id|tenant_id|recent_auth_at" mailglass_admin/test/mailglass_admin/auth_test.exs` returns auth contract coverage.
    - `rg -n "Sigra|UserAuth|Pow|Guardian" mailglass_admin/lib/mailglass_admin/auth.ex mailglass_admin/lib/mailglass_admin/operator/mount.ex` does not introduce hard adopter-stack coupling.
    - `cd mailglass_admin && mix test test/mailglass_admin/auth_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The operator mount is auth-aware and later destructive actions have a single stack-agnostic authorization seam to call.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 23-02-02: Wire the operator LiveView and tests to the auth seam without adding mutations</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/test/support/live_view_case.ex, mailglass_admin/test/support/endpoint_case.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs
    - mailglass_admin/test/support/live_view_case.ex
    - mailglass_admin/test/support/endpoint_case.ex
    - mailglass_admin/lib/mailglass_admin/operator/mount.ex
    - mailglass_admin/lib/mailglass_admin/auth.ex
    - .planning/phases/22-operator-data-foundation/22-VERIFICATION.md
  </read_first>
  <action>
    Update `mailglass_admin/lib/mailglass_admin/operator_live.ex` so the read-only operator page consumes the new auth seam and exposes stable auth-related state needed for future phases. Keep all Phase 22 data loading and read-only rendering intact.

    The LiveView should:
    - assume the new operator mount hook has already run
    - preserve the URL-backed filter/selection flow
    - expose any actor/auth-context assigns needed for future audit-aware actions
    - avoid shipping replay or suppression-removal handlers in this phase

    Update the shared admin test harness in `live_view_case.ex` and `endpoint_case.ex` as needed so production operator auth scenarios are executable in Wave 2 rather than deferred to a later plan.

    Expand `mailglass_admin/test/mailglass_admin/operator_live_test.exs` to cover:
    - authorized operator mount path still renders the Phase 22 read-only contract
    - unauthorized or missing-auth context is rejected or redirected through the new mount/auth seam
    - no replay/removal controls are introduced yet
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/auth_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "actor|recent|auth" mailglass_admin/lib/mailglass_admin/operator_live.ex` returns explicit auth-context wiring.
    - `rg -n "operator|auth" mailglass_admin/test/support/live_view_case.ex mailglass_admin/test/support/endpoint_case.ex` returns harness support for the new operator path.
    - `rg -n "unauthorized|forbidden|recent" mailglass_admin/test/mailglass_admin/operator_live_test.exs` returns mount/auth coverage.
    - `rg -n "Replay|Remove suppression" mailglass_admin/test/mailglass_admin/operator_live_test.exs` still shows negative assertions only.
    - `cd mailglass_admin && mix test test/mailglass_admin/auth_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The operator LiveView participates in the new auth seam and retains its read-only scope, with tests pinning both access control and non-regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter auth state -> operator mount hook | App-owned authentication data crosses into library-owned LiveView mount logic |
| LiveView event handlers -> future destructive actions | Operator-triggered events will eventually cross into state-changing domain logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Spoofing | operator access | mitigate | Make operator access go through an explicit mount auth seam rather than implicit route placement alone. |
| T-23-06 | Elevation of Privilege | future replay/removal events | mitigate | Introduce one server-side recent-auth authorization contract before mutation handlers exist. |
| T-23-07 | Tampering | auth integration | mitigate | Keep the contract stack-agnostic and whitelisted; do not expose raw adopter session/user structs as the public seam. |
| T-23-08 | Regression | read-only operator UI | mitigate | Preserve Phase 22 data/view behavior and pin it with operator LiveView tests. |
</threat_model>

<verification>
- cd mailglass_admin && mix test test/mailglass_admin/auth_test.exs --warnings-as-errors
- cd mailglass_admin && mix test test/mailglass_admin/auth_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors
</verification>

<success_criteria>
The operator surface is mounted through an auth-aware seam, future destructive actions have a server-side recent-auth contract to call, and the current read-only UI remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/23-production-admin-mount-and-step-up-auth/23-02-SUMMARY.md`
</output>
