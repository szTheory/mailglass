---
phase: 23-production-admin-mount-and-step-up-auth
plan: "03"
type: execute
wave: 3
depends_on:
  - "23-01"
  - "23-02"
files_modified:
  - mailglass_admin/README.md
  - mailglass_admin/test/mailglass_admin/router_test.exs
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/test/mailglass_admin/preview_live_test.exs
  - mailglass_admin/test/mailglass_admin/auth_test.exs
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-05
tags:
  - admin
  - docs
  - tests
  - verification

must_haves:
  truths:
    - "The README no longer claims `mailglass_admin` lacks any prod-mountable operator surface once Phase 23 lands."
    - "Docs clearly separate dev preview mounting from production operator mounting and keep auth ownership with the adopter."
    - "Regression coverage proves preview still works while the production operator mount requires the new auth/session seam."
    - "The phase finishes with verification evidence aligned to the new router/auth contract, not just compile success."
  artifacts:
    - path: "mailglass_admin/README.md"
      provides: "separate dev-preview and production-operator mounting guidance"
      contains: "production operator"
    - path: "mailglass_admin/test/mailglass_admin/router_test.exs"
      provides: "preview and operator route contract coverage"
      contains: "preview"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "operator auth and read-only non-regression coverage"
      contains: "operator"
  key_links:
    - from: "mailglass_admin/README.md"
      to: "mailglass_admin/lib/mailglass_admin/router.ex"
      via: "docs must match the actual preview/operator route APIs"
      pattern: "mailglass_admin_routes|mailglass_operator"
    - from: "mailglass_admin/test/support/live_view_case.ex"
      to: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      via: "shared harness provides adopter-auth context for operator tests"
      pattern: "operator|auth"
    - from: "mailglass_admin/test/mailglass_admin/router_test.exs"
      to: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      via: "route-contract tests and mount-behavior tests prove the same operator boundary"
      pattern: "operator"
---

<objective>
Finish Phase 23 with adopter-facing docs and verification that make the production operator contract credible.

Purpose: Phase 23 is not complete if the code gains a production mount/auth seam but the README still says the package has no prod admin surface, or if test support cannot prove the new boundary. This plan turns the architecture change into an adopter-usable documented feature with verifiable regressions.

Output:
- updated README and mount guidance
- test harness support for production operator auth scenarios
- final verification commands and evidence for preview/operator non-regression
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/23-production-admin-mount-and-step-up-auth/23-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/23-production-admin-mount-and-step-up-auth/23-PATTERNS.md
@/Users/jon/projects/mailglass/mailglass_admin/README.md
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/router.ex
@/Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/operator_live.ex
@/Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/router_test.exs
@/Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/operator_live_test.exs
@/Users/jon/projects/mailglass/mailglass_admin/test/support/live_view_case.ex
@/Users/jon/projects/mailglass/mailglass_admin/test/support/endpoint_case.ex
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 23-03-01: Update adopter docs for preview vs production operator mounting</name>
  <files>mailglass_admin/README.md</files>
  <read_first>
    - mailglass_admin/README.md
    - mailglass_admin/lib/mailglass_admin/router.ex
    - .planning/phases/23-production-admin-mount-and-step-up-auth/23-RESEARCH.md
  </read_first>
  <action>
    Rewrite the relevant sections of `mailglass_admin/README.md` so they accurately describe the post-Phase-23 package:
    - keep a dev-preview mount section for `mailglass_admin_routes/2` or its final preview equivalent
    - add a production operator mount section with an example Phoenix scope/pipeline shape
    - state clearly that auth ownership and recent-auth UX remain adopter-owned
    - document that replay/removal actions are not shipped yet even though the auth seam is now present

    Remove stale claims that the package ships no prod-mountable admin surface if those claims are no longer true. Keep warnings precise rather than marketing-heavy.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/router_test.exs test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "production operator|dev preview|recent authentication|adopter" mailglass_admin/README.md` returns the new guidance.
    - `rg -n "does NOT ship|prod-mountable admin surface" mailglass_admin/README.md` no longer contradicts Phase 23 reality.
    - `cd mailglass_admin && mix test test/mailglass_admin/router_test.exs test/mailglass_admin/preview_live_test.exs --warnings-as-errors` still exits 0 after doc-aligned contract checks.
  </acceptance_criteria>
  <done>The README explains exactly how to mount preview vs production operator routes and who owns auth.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 23-03-02: Finalize regression and verification coverage for the new boundary</name>
  <files>mailglass_admin/test/mailglass_admin/router_test.exs, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/test/mailglass_admin/preview_live_test.exs, mailglass_admin/test/mailglass_admin/auth_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/router_test.exs
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs
    - mailglass_admin/test/mailglass_admin/preview_live_test.exs
    - mailglass_admin/test/mailglass_admin/auth_test.exs
    - .planning/phases/22-operator-data-foundation/22-VERIFICATION.md
  </read_first>
  <action>
    Add or refine coverage so the Phase 23 verification story proves:
    - preview route behavior still works
    - production operator route mounts through the new auth/session seam
    - unauthorized access is rejected predictably
    - the read-only operator surface from Phase 22 still renders correctly after the auth changes
    - the auth seam contract remains callable and returns the locked `:unauthorized` / `:stale_auth` outcomes

    Keep the tests first-party and deterministic. Do not defer the phase’s proof to manual browser inspection alone.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/router_test.exs test/mailglass_admin/operator_live_test.exs test/mailglass_admin/preview_live_test.exs test/mailglass_admin/auth_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "preview|operator|unauthorized|recent|stale_auth" mailglass_admin/test/mailglass_admin/router_test.exs mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs mailglass_admin/test/mailglass_admin/auth_test.exs` returns Phase 23 boundary coverage.
    - `cd mailglass_admin && mix test test/mailglass_admin/router_test.exs test/mailglass_admin/operator_live_test.exs test/mailglass_admin/preview_live_test.exs test/mailglass_admin/auth_test.exs --warnings-as-errors` exits 0.
    - Phase 22 read-only copy assertions remain present; the tests add auth-boundary coverage rather than replacing operator-surface coverage wholesale.
  </acceptance_criteria>
  <done>The phase has testable proof that preview survives, operator auth works, and the read-only UI contract still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/examples -> adopter configuration | Docs shape how adopters wire authentication and route scopes in production |
| shared test harness -> route/auth proofs | Test support must reflect the real contract or verification becomes meaningless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | Repudiation | adopter docs | mitigate | Keep auth ownership, recent-auth expectations, and non-goals explicit in README examples. |
| T-23-10 | Regression | shared test harness | mitigate | Extend the existing harness instead of creating ad hoc operator-only helpers that bypass real route/auth paths. |
| T-23-11 | False confidence | verification | mitigate | Require route + operator test passes, not just compile or doc edits. |
</threat_model>

<verification>
- cd mailglass_admin && mix test test/mailglass_admin/router_test.exs test/mailglass_admin/operator_live_test.exs test/mailglass_admin/preview_live_test.exs test/mailglass_admin/auth_test.exs --warnings-as-errors
- cd mailglass_admin && mix compile --warnings-as-errors
</verification>

<success_criteria>
Phase 23 ends with accurate adopter docs and automated proof that preview and production operator mounts both behave correctly under the new auth/session contract.
</success_criteria>

<output>
After completion, create `.planning/phases/23-production-admin-mount-and-step-up-auth/23-03-SUMMARY.md`
</output>
