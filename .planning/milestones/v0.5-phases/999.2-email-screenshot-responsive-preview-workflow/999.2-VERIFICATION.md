# Phase 999.2 Verification

- Phase: `999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog`
- Verifier run date: `2026-05-27`
- Scope: Validate plans `999.2-01`, `999.2-02`, and `999.2-03` against repository implementation and fresh verification commands.
- Status: `passed`

## Inputs Reviewed

- `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-01-PLAN.md`
- `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-02-PLAN.md`
- `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-03-PLAN.md`
- `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-01-SUMMARY.md`
- `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-02-SUMMARY.md`
- `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-03-SUMMARY.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`

## Fresh Verification Commands

1. `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs test/mailglass_admin/preview/capture_matrix_test.exs test/mix/tasks/mailglass_admin.preview.capture_test.exs test/mailglass_admin/preview/capture_manifest_test.exs --warnings-as-errors`
   - Result: pass (`19 tests, 0 failures`)
2. `cd mailglass_admin && mix help mailglass_admin.preview.capture`
   - Result: pass (task is discoverable; options/docs rendered)
3. `mix mailglass.docs.check`
   - Result: pass (`[mailglass.docs.check] OK — Tier 1 docs match the stability contract.`)
4. `mix test test/mailglass/docs_contract_test.exs test/mailglass/docs_check_task_test.exs --warnings-as-errors`
   - Result: pass (`26 tests, 0 failures, 1 skipped`)
5. `bash scripts/check_preview_capture_checkpoint.sh --help`
   - Result: pass (validator script is executable and exposes contract-check CLI)

## Plan Coverage Evidence

### Plan 01: Deterministic preview URL state + matrix foundations

- URL parsing and fallback helpers are implemented in `mailglass_admin/lib/mailglass_admin/preview_live.ex` (`parse_device_width_param/1`, `parse_theme_param/1`) and wired through `normalize_capture_url_state/2` and `sync_patch_capture_url_state/1`.
- Scenario routing keeps width/theme state canonical in `mailglass_admin/lib/mailglass_admin/preview/sidebar.ex` via `scenario_path/4`.
- Deterministic matrix primitives exist in:
  - `mailglass_admin/lib/mailglass_admin/preview/capture_state.ex`
  - `mailglass_admin/lib/mailglass_admin/preview/capture_matrix.ex`
- Matrix sorting and skip metadata (`:no_previews`, `:discovery_error`, `:invalid_reflection`) are present in `CaptureMatrix.build_matrix/2`.
- Regression coverage is present and passing in:
  - `mailglass_admin/test/mailglass_admin/preview_live_test.exs`
  - `mailglass_admin/test/mailglass_admin/preview/capture_matrix_test.exs`

### Plan 02: Mix capture workflow + deterministic manifest contract

- Mix task exists and is discoverable in `mailglass_admin/lib/mix/tasks/mailglass_admin.preview.capture.ex` with strict `OptionParser.parse/2` and fail-fast validation for unknown flags, widths, themes, and base URL.
- Chromium invocation is argument-list based (`System.cmd/3`) and deterministic in `mailglass_admin/lib/mailglass_admin/preview/chromium.ex` with `--headless`, `--virtual-time-budget`, and `--window-size`.
- Manifest/checkpoint writer exists in `mailglass_admin/lib/mailglass_admin/preview/capture_manifest.ex` with:
  - `schema_version` = `preview_capture.v1`
  - `claim_boundary` = `preview-pipeline confidence only; not cross-client parity`
  - deterministic ordering via `Enum.sort_by/2`
  - `sha256` identity/file hashing
- Task and manifest tests are present and passing:
  - `mailglass_admin/test/mix/tasks/mailglass_admin.preview.capture_test.exs`
  - `mailglass_admin/test/mailglass_admin/preview/capture_manifest_test.exs`
- Bounded docs language and workflow usage are present in:
  - `guides/preview.md`
  - `mailglass_admin/README.md`

### Plan 03: Advisory CI lane + checkpoint contract + docs boundary enforcement

- Advisory CI lane is present in `.github/workflows/ci.yml` as `preview_capture_advisory`, including:
  - `mix mailglass_admin.preview.capture`
  - checkpoint validation step (`scripts/check_preview_capture_checkpoint.sh`)
  - artifact upload for screenshots + manifest + checkpoint
- Advisory-vs-required boundary is documented in `MAINTAINING.md`; `Preview Capture Advisory` is listed under advisory checks and not required branch-protection contexts.
- Checkpoint validator script exists in `scripts/check_preview_capture_checkpoint.sh` and enforces schema version, claim boundary, matrix count, dimensions, and screenshot-path consistency.
- Docs contract checks enforce preview boundary language in `lib/mix/tasks/mailglass.docs.check.ex`.
- Enforcement tests are present and passing in:
  - `test/mailglass/docs_contract_test.exs`
  - `test/mailglass/docs_check_task_test.exs`

## Residual Risk

- The full GitHub Actions `preview_capture_advisory` job was not executed end-to-end in this local verifier run; validation here confirms configuration, script wiring, and supporting tests, but not remote runner behavior under live CI load.
- Non-blocking warnings were observed during local tests (boundary warnings in admin test support and missing optional OTLP exporter warning). These did not affect phase acceptance criteria and did not fail verification commands.

## Verdict

Phase `999.2` implementation is evidenced across code, docs, and CI configuration for all three plans, and targeted verification commands pass. Final status: `passed`.
