---
phase: "999.2"
plan: "03"
title: "Advisory CI Artifact Lane And Boundary Contract Enforcement"
type: execute
wave: 3
depends_on:
  - "999.2-02"
files_modified:
  - ".github/workflows/ci.yml"
  - "MAINTAINING.md"
  - "scripts/check_preview_capture_checkpoint.sh"
  - "lib/mix/tasks/mailglass.docs.check.ex"
  - "test/mailglass/docs_contract_test.exs"
  - "test/mailglass/docs_check_task_test.exs"
requirements:
  - "TBD (backlog phase)"
autonomous: true
must_haves:
  truths:
    - "CI screenshot lane is advisory evidence only and must not be promoted as branch-protection truth."
    - "CI lane publishes deterministic capture artifacts (screenshots + manifest + checkpoint) and fails advisory job when checkpoint contract is broken."
    - "Docs contract checks enforce explicit bounded language: preview-pipeline confidence only, not cross-client parity."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Advisory preview capture job with artifact upload and checkpoint verification"
      contains: "preview_capture_advisory"
    - path: "scripts/check_preview_capture_checkpoint.sh"
      provides: "Deterministic checkpoint/manifest contract validator used by CI"
      contains: "preview_capture.v1"
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Tier-1 docs boundary checks for preview confidence language"
      contains: "guides/preview.md"
  key_links:
    - from: ".planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-CONTEXT.md"
      to: ".github/workflows/ci.yml"
      via: "D-06/D-08 require deterministic trust artifacts and canonical CI environment."
      pattern: "deterministic artifacts|advisory CI"
    - from: ".planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-UI-SPEC.md"
      to: "lib/mix/tasks/mailglass.docs.check.ex"
      via: "UI copy contract mandates bounded claim wording."
      pattern: "preview pipeline confidence only|does not claim cross-client parity"
---

<objective>
Close phase 999.2 by operationalizing advisory CI artifact evidence and contract-checking the docs language boundary so the workflow remains useful, deterministic, and honestly scoped.

Purpose: ensure screenshot evidence is consistently produced in CI while preventing accidental escalation of claims beyond preview-pipeline confidence.
Output: advisory CI lane, checkpoint validator script, and docs/tests that fail on boundary-language drift.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-CONTEXT.md
@.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-RESEARCH.md
@.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-PATTERNS.md
@.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-UI-SPEC.md
@.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-VALIDATION.md
@.github/workflows/ci.yml
@MAINTAINING.md
@lib/mix/tasks/mailglass.docs.check.ex
@test/mailglass/docs_contract_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add advisory CI preview-capture lane with artifact upload</name>
  <read_first>
    - .planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-CONTEXT.md
    - .planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-RESEARCH.md
    - .github/workflows/ci.yml
    - MAINTAINING.md
    - mailglass_admin/test/support/operator_browser_server.ex
  </read_first>
  <files>.github/workflows/ci.yml, MAINTAINING.md</files>
  <action>Add a `preview_capture_advisory` job to `ci.yml` that boots deterministic preview capture prerequisites, runs `mix mailglass_admin.preview.capture` in a deterministic mode, and uploads screenshot + manifest/checkpoint artifacts using a pinned upload-artifact action SHA. Keep the lane explicitly advisory by documenting it in `MAINTAINING.md` under advisory checks and avoid adding it to required branch-protection truth sections.</action>
  <acceptance_criteria>
    - `ci.yml` contains a dedicated `preview_capture_advisory` job.
    - Job uploads capture artifacts (screenshots, manifest, checkpoint) on each run.
    - `MAINTAINING.md` lists the new lane under advisory checks.
    - No required-checks list is expanded to include preview capture.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "preview_capture_advisory|mailglass_admin\\.preview\\.capture|upload-artifact" .github/workflows/ci.yml</automated>
    <automated>rg -n "advisory signal|Preview Capture Advisory|not branch-protection truth" MAINTAINING.md</automated>
    <automated>! rg -n "required contexts.*Preview Capture Advisory|Support Contract Core.*Preview Capture Advisory" MAINTAINING.md</automated>
  </verify>
  <done>CI now emits screenshot evidence as advisory artifacts without changing branch-protection truth boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce deterministic checkpoint contract in CI lane</name>
  <read_first>
    - .planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-RESEARCH.md
    - .planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-PATTERNS.md
    - scripts/check_credo_suppressions.sh
    - scripts/check_dialyzer_ignore.sh
  </read_first>
  <files>scripts/check_preview_capture_checkpoint.sh, .github/workflows/ci.yml</files>
  <action>Create `scripts/check_preview_capture_checkpoint.sh` that validates deterministic capture contract outputs before upload: required files exist, manifest contains `schema_version: preview_capture.v1`, checkpoint contains non-zero matrix count and deterministic dimensions, and claim-boundary text is present. Wire this script into the advisory CI job so artifact upload only occurs when checkpoint validation passes.</action>
  <acceptance_criteria>
    - New checkpoint validator script exists and is executable.
    - Script validates manifest/checkpoint schema and required keys with actionable failures.
    - CI advisory job runs the validator prior to artifact upload.
    - CI logs show explicit checkpoint validation step label.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "preview_capture\\.v1|claim_boundary|matrix_count|widths|themes" scripts/check_preview_capture_checkpoint.sh</automated>
    <automated>rg -n "check_preview_capture_checkpoint\\.sh" .github/workflows/ci.yml</automated>
    <automated>bash scripts/check_preview_capture_checkpoint.sh --help</automated>
  </verify>
  <done>Artifact production is now contract-checked, making advisory evidence deterministic and machine-verifiable.</done>
</task>

<task type="auto">
  <name>Task 3: Add docs boundary-language contract checks and tests</name>
  <read_first>
    - .planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-UI-SPEC.md
    - guides/preview.md
    - mailglass_admin/README.md
    - lib/mix/tasks/mailglass.docs.check.ex
    - test/mailglass/docs_contract_test.exs
    - test/mailglass/docs_check_task_test.exs
  </read_first>
  <files>lib/mix/tasks/mailglass.docs.check.ex, test/mailglass/docs_contract_test.exs, test/mailglass/docs_check_task_test.exs</files>
  <action>Extend docs contract enforcement so `guides/preview.md` and `mailglass_admin/README.md` must include bounded wording (`preview-pipeline confidence only`) and must not contain parity-overreach phrases (`cross-client parity`, `guaranteed client parity`). Add/adjust ExUnit coverage in docs contract tests and docs-check task tests to fail on missing boundary text or parity-overreach language.</action>
  <acceptance_criteria>
    - `mailglass.docs.check` includes preview-guide boundary tokens and forbidden parity tokens.
    - Docs contract tests assert required bounded language in both preview docs surfaces.
    - Docs contract tests fail if parity-overreach phrase is introduced.
    - `mix mailglass.docs.check` and docs contract tests pass.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "guides/preview\\.md|preview-pipeline confidence only|cross-client parity" lib/mix/tasks/mailglass.docs.check.ex</automated>
    <automated>rg -n "preview-pipeline confidence only|cross-client parity|guaranteed client parity" test/mailglass/docs_contract_test.exs test/mailglass/docs_check_task_test.exs</automated>
    <automated>mix mailglass.docs.check</automated>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/docs_check_task_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Boundary-language drift is now mechanically blocked by both docs-check task rules and test coverage.</done>
</task>
</tasks>

<threat_model>
## STRIDE Threat Register

| Category | Threat | Surface | Mitigation in this plan | Verification hook |
|---|---|---|---|---|
| Spoofing | CI artifact claims are mistaken for required trust gates | workflow/docs messaging | Mark lane and docs explicitly as advisory, keep required-check list unchanged | `MAINTAINING.md` required-vs-advisory assertions |
| Tampering | Uploaded artifacts do not match deterministic contract | artifact generation + upload | Run checkpoint validator before upload; enforce schema and key invariants | `check_preview_capture_checkpoint.sh` in CI job |
| Repudiation | Future maintainer cannot verify what CI actually proved | checkpoint evidence | Store machine-readable manifest/checkpoint in uploaded artifact bundle | job artifact contents + validator key checks |
| Information Disclosure | Artifact bundle leaks sensitive/non-preview data claims | docs/task boundary | Contract checks enforce preview-only claim and forbid parity overreach wording | `mailglass.docs.check` + docs contract tests |
| Denial of Service | Capture lane flakes and blocks delivery velocity | CI posture | Keep lane advisory and separate from branch-protection truth | advisory list in `MAINTAINING.md` + no required-check update |
| Elevation of Privilege | Docs drift into de facto API guarantee language | docs surfaces | Explicit required/forbidden token checks for preview boundary language | `mix mailglass.docs.check` + docs tests |
</threat_model>

<verification>
- `rg -n "preview_capture_advisory|check_preview_capture_checkpoint\\.sh|upload-artifact" .github/workflows/ci.yml`
- `bash scripts/check_preview_capture_checkpoint.sh --help`
- `mix mailglass.docs.check`
- `mix test test/mailglass/docs_contract_test.exs test/mailglass/docs_check_task_test.exs --warnings-as-errors`
- `rg -n "Preview Capture Advisory|advisory signal|not branch-protection truth" MAINTAINING.md`
</verification>

<success_criteria>
Wave 3 ships an advisory CI capture lane with deterministic checkpoint validation and enforceable docs-language boundaries, preserving the explicit claim that this workflow improves preview-pipeline confidence only.
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog/999.2-03-SUMMARY.md`.
</output>
