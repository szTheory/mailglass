---
phase: 999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog
plan: "02"
subsystem: ui
tags: [mix-task, preview-capture, chromium, manifest-contract, docs]

requires:
  - phase: 999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog
    provides: Deterministic PreviewLive URL state and matrix helpers from Plan 01
provides:
  - One-command deterministic screenshot capture workflow for maintainers
  - Schema-versioned manifest/checkpoint contract with stable ordering and bounded claim language
  - Maintainer/adopter documentation for capture usage and output artifact expectations
affects: [preview-guides, admin-readme, screenshot-pipeline, ci-artifact-contract]

tech-stack:
  added: []
  patterns: [strict option-parser task CLI, deterministic manifest encoding, no-shell-eval chromium invocation]

key-files:
  created:
    - mailglass_admin/lib/mix/tasks/mailglass_admin.preview.capture.ex
    - mailglass_admin/lib/mailglass_admin/preview/chromium.ex
    - mailglass_admin/lib/mailglass_admin/preview/capture_manifest.ex
    - mailglass_admin/test/mix/tasks/mailglass_admin.preview.capture_test.exs
    - mailglass_admin/test/mailglass_admin/preview/capture_manifest_test.exs
  modified:
    - mailglass_admin/mix.exs
    - guides/preview.md
    - mailglass_admin/README.md

key-decisions:
  - Keep the primary workflow no-Node by default via direct Chromium CLI invocation through `System.cmd/3`.
  - Write deterministic manifest/checkpoint artifacts for both full capture and dry-run paths so CI/local contracts match.
  - Keep trust language explicit and bounded to preview-pipeline confidence only.

patterns-established:
  - "Capture task contract: strict CLI parsing, explicit invalid-option errors, deterministic matrix ordering."
  - "Manifest contract: `preview_capture.v1` plus tuple-sorted capture identities with per-entry sha256."

requirements-completed: ["TBD (backlog phase)"]

duration: 6 min
completed: 2026-05-27
---

# Phase 999.2 Plan 02: Mix Capture Workflow And Deterministic Manifest Contract Summary

**`mailglass_admin.preview.capture` now ships a deterministic Chromium-based capture workflow with versioned manifest/checkpoint outputs and bounded trust language in maintainer-facing docs.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-27T11:13:00Z
- **Completed:** 2026-05-27T11:19:25Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments
- Added `mix mailglass_admin.preview.capture` with strict option parsing, deterministic matrix planning, actionable invalid-input errors, and explicit dry-run behavior.
- Added `MailglassAdmin.Preview.Chromium` with argument-list `System.cmd/3` invocation, deterministic headless flags (`--headless`, `--virtual-time-budget`, fixed `--window-size`), and explicit binary discovery failure messaging.
- Added `MailglassAdmin.Preview.CaptureManifest` to write deterministic `manifest.json` and `checkpoint.json` with schema `preview_capture.v1`, bounded claim language, sorted identity rows, and sha256 fields.
- Wired dry-run and full capture paths to emit the same deterministic contract artifacts, then added regression coverage for ordering and byte-identical repeated dry-runs.
- Updated `guides/preview.md` and `mailglass_admin/README.md` with concrete commands, matrix dimensions, artifact paths, and explicit "preview-pipeline confidence only; not cross-client parity" boundaries.

## Task Commits

Each task was committed atomically:

1. **Task 1: Build `mailglass_admin.preview.capture` mix task with strict CLI and Chromium runner** - `80259ac` (feat)
2. **Task 2: Implement deterministic manifest/checkpoint contract and regression tests** - `3a29b76` (feat)
3. **Task 3: Document maintainer workflow and bounded claim language** - `07af05e` (docs)

**Plan metadata:** `(this commit)`

## Files Created/Modified
- `mailglass_admin/lib/mix/tasks/mailglass_admin.preview.capture.ex` - Added strict maintainer CLI workflow, deterministic matrix orchestration, and dry-run/full-run behavior.
- `mailglass_admin/lib/mailglass_admin/preview/chromium.ex` - Added deterministic Chromium command builder and binary discovery wrapper.
- `mailglass_admin/lib/mailglass_admin/preview/capture_manifest.ex` - Added deterministic schema-versioned manifest/checkpoint writer with stable ordering and sha256 fields.
- `mailglass_admin/mix.exs` - Added preferred test env mapping for `mailglass_admin.preview.capture`.
- `mailglass_admin/test/mix/tasks/mailglass_admin.preview.capture_test.exs` - Added CLI validation and dry-run contract tests.
- `mailglass_admin/test/mailglass_admin/preview/capture_manifest_test.exs` - Added deterministic ordering and repeated-generation byte-equality tests.
- `guides/preview.md` - Added capture workflow docs, matrix defaults, and bounded trust language.
- `mailglass_admin/README.md` - Added maintainer-facing capture command and artifact contract documentation.

## Decisions Made
- Kept Chromium invocation as a direct argument list (`System.cmd/3`) rather than shell-joined strings to preserve injection safety and deterministic flags.
- Chose deterministic identity hashes for dry-run manifest rows so repeated dry-runs with identical inputs produce byte-identical artifacts.
- Applied the same bounded claim text in both manifest metadata and docs to prevent drift into cross-client parity language.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `mix help mailglass_admin.preview.capture` initially ran before the new task module had been compiled in that shell context. Resolved by compiling first, then re-running help verification.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Maintainers now have one deterministic command that can run locally or in CI and emit machine-readable artifact contracts.
- Docs and artifact schemas are aligned, so phase-level trust claims stay explicitly bounded to preview pipeline confidence.

## Self-Check: PASSED

---
*Phase: 999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog*
*Completed: 2026-05-27*
