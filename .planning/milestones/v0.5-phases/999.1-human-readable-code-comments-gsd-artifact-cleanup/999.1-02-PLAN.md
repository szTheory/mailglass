---
phase: "999.1"
plan: "02"
title: "Tier 1/2 Cross-Package Comment Cleanup"
type: execute
wave: 2
depends_on:
  - "999.1-01"
files_modified:
  - "lib/mailglass/**/*.ex"
  - "mailglass_admin/lib/**/*.ex"
  - "mailglass_inbound/lib/**/*.ex"
requirements:
  - "TBD (backlog phase)"
autonomous: true
must_haves:
  truths:
    - "Wave 2 is inventory-driven and coverage-complete: every remaining scoped source file with planning tokens is rewritten until full-file scans are zero for core/admin/inbound paths."
    - "Cross-package comment tone is consistent and behavior-anchored (`why`, failure mode, operator outcome)."
    - "No runtime logic, signatures, schemas, or telemetry event shapes change."
  artifacts:
    - path: "lib/mailglass/telemetry.ex"
      provides: "PII-safe telemetry rationale without planning shorthand"
      contains: "must never include recipient or message-content PII"
    - path: "lib/mailglass/events/reconciler.ex"
      provides: "Replay/idempotency rationale without phase provenance"
      contains: "idempotent no-op"
    - path: "mailglass_inbound/lib/mailglass_inbound/optional_deps.ex"
      provides: "Optional-dependency gateway rationale in plain language"
      contains: "optional dependencies through gateway modules only"
  key_links:
    - from: ".planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md"
      to: "lib/mailglass/webhook/ingest.ex"
      via: "Tier 1 high-density hotspots are executed before long-tail sweeps"
      pattern: "Tier 1|webhook/ingest|telemetry"
    - from: ".planning/PROJECT.md"
      to: "lib/mailglass/telemetry.ex"
      via: "No-PII telemetry invariant stays explicit while removing planning IDs"
      pattern: "Never PII"
---

<objective>
Complete the inventory-driven Tier 1 and Tier 2 rewrite across core, admin, and inbound source paths using explicit, invariant-preserving language and zero planning-token drift.

Purpose: finish broad readability cleanup after Tier 0 pattern lock by burning down all remaining scoped token matches before Wave 3 guard enforcement.
Output: all remaining matched files in `lib/mailglass/**/*.ex`, `mailglass_admin/lib/**/*.ex`, and `mailglass_inbound/lib/**/*.ex` rewritten with invariant anchors and zero full-file token matches.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-PATTERNS.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-VALIDATION.md
@.planning/PROJECT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute inventory-driven residual core sweep with explicit invariant anchors</name>
  <read_first>
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-PATTERNS.md
    - lib/mailglass
  </read_first>
  <files>lib/mailglass/**/*.ex</files>
  <action>Run `rg -l --glob "*.ex" "D-\\d+|Phase\\s+\\d+(?:\\.\\d+)?|Plan\\s+\\d+(?:\\.\\d+)?|\\[ASSUMED|REQ-[A-Z0-9-]+|\\bGSD\\b" lib/mailglass` and treat every returned path as mandatory rewrite input for this task. Remove matched planning tokens from full file contents while preserving existing behavior and invariant meaning. Anchor rewritten commentary with these exact phrases in the specified files: `Telemetry metadata must never include recipient or message-content PII.` in `lib/mailglass/telemetry.ex`; `Replay duplicates are handled as an idempotent no-op, never a re-dispatch.` in `lib/mailglass/events/reconciler.ex`; `Match on %Mailglass.Error{} structs, never message strings.` in `lib/mailglass/mailable.ex`; `Raise on invalid configuration so misconfigured mailers fail fast at boot.` in `lib/mailglass/config.ex`.</action>
  <acceptance_criteria>
    - Full-file token scan returns zero matches for all `lib/mailglass/**/*.ex` files.
    - Required invariant anchor phrases exist in the four designated files.
    - `mix compile --warnings-as-errors` passes after the Tier 1 rewrite.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Telemetry metadata must never include recipient or message-content PII\." lib/mailglass/telemetry.ex</automated>
    <automated>rg -n "Replay duplicates are handled as an idempotent no-op, never a re-dispatch\." lib/mailglass/events/reconciler.ex</automated>
    <automated>rg -n "Match on %Mailglass.Error\{\} structs, never message strings\." lib/mailglass/mailable.ex</automated>
    <automated>rg -n "Raise on invalid configuration so misconfigured mailers fail fast at boot\." lib/mailglass/config.ex</automated>
    <automated>! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Tier 1 core files are rewritten for readability with explicit invariants preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Execute inventory-driven admin/inbound full sweep and keep operator/optional-dep semantics explicit</name>
  <read_first>
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-PATTERNS.md
    - mailglass_admin/lib
    - mailglass_inbound/lib
  </read_first>
  <files>mailglass_admin/lib/**/*.ex, mailglass_inbound/lib/**/*.ex</files>
  <action>Run `rg -l --glob "*.ex" "D-\\d+|Phase\\s+\\d+(?:\\.\\d+)?|Plan\\s+\\d+(?:\\.\\d+)?|\\[ASSUMED|REQ-[A-Z0-9-]+|\\bGSD\\b" mailglass_admin/lib mailglass_inbound/lib` and rewrite every returned file until no matches remain. Keep edits comment/docstring-focused with no runtime behavior changes, and add exact invariant anchors: `Route optional dependencies through gateway modules only so --no-optional-deps builds stay clean.` in `mailglass_inbound/lib/mailglass_inbound/optional_deps.ex`; `Inbound telemetry metadata must never include raw payload or recipient PII.` in `mailglass_inbound/lib/mailglass_inbound/telemetry.ex`; `Prune operations must remain tenant-scoped and operator-confirmed before destructive actions.` in `mailglass_inbound/lib/mailglass_inbound/internal/prune.ex`; `PubSub topics remain prefixed to avoid cross-application collisions.` in `mailglass_admin/lib/mailglass_admin/pub_sub/topics.ex`.</action>
  <acceptance_criteria>
    - Full-file token scan returns zero matches for all `mailglass_admin/lib/**/*.ex` and `mailglass_inbound/lib/**/*.ex` files.
    - Each required anchor phrase exists in the specified file.
    - `mix compile --no-optional-deps --warnings-as-errors` passes after the Tier 2 rewrite.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Route optional dependencies through gateway modules only so --no-optional-deps builds stay clean\." mailglass_inbound/lib/mailglass_inbound/optional_deps.ex</automated>
    <automated>rg -n "Inbound telemetry metadata must never include raw payload or recipient PII\." mailglass_inbound/lib/mailglass_inbound/telemetry.ex</automated>
    <automated>rg -n "Prune operations must remain tenant-scoped and operator-confirmed before destructive actions\." mailglass_inbound/lib/mailglass_inbound/internal/prune.ex</automated>
    <automated>rg -n "PubSub topics remain prefixed to avoid cross-application collisions\." mailglass_admin/lib/mailglass_admin/pub_sub/topics.ex</automated>
    <automated>! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" mailglass_admin/lib mailglass_inbound/lib</automated>
    <automated>mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <done>Tier 2 cross-package cleanup is complete with durable operator and optional-dependency guidance preserved.</done>
</task>
</tasks>

<threat_model>
## Threat Register

| Threat ID | Severity | Threat | Mitigation in this plan |
|-----------|----------|--------|--------------------------|
| TM-9991-04 | high | Invariant drift in replay/telemetry/optional-deps guidance during full-scope burn-down | Required anchor phrases enforce non-PII telemetry, idempotent replay, and optional-deps gateway invariants in specific files while inventory-driven sweep expands coverage. |
| TM-9991-05 | high | Misleading rewrites in operator surfaces causing unsafe mental models | Admin/inbound task mandates tenant-scoped and operator-confirmed wording for prune/destructive guidance. |
| TM-9991-06 | medium | Unnoticed behavior edits hidden in large documentation-focused diff | Acceptance criteria enforce full-file token scans plus compile gates while keeping edits comment/docstring-focused and behavior-preserving. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors`
- `mix compile --no-optional-deps --warnings-as-errors`
- `mix credo --strict`
- `! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass mailglass_admin/lib mailglass_inbound/lib`
</verification>

<success_criteria>
Tier 1 and Tier 2 complete a zero-match burn-down across all scoped core/admin/inbound `.ex` files, while key safety and operator invariants remain explicit and behavior-anchored.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-02-SUMMARY.md`.
</output>
