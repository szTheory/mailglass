---
phase: 999.1
slug: human-readable-code-comments-gsd-artifact-cleanup-backlog
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-27
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (`mix test`) + Credo |
| **Config file** | `mix.exs`, `.credo.exs`, `test/test_helper.exs` |
| **Quick run command** | `mix compile --warnings-as-errors && mix credo --strict` |
| **Full suite command** | `mix compile --no-optional-deps --warnings-as-errors && mix credo --strict && mix test --warnings-as-errors` |
| **Estimated runtime** | ~240 seconds |

---

## Sampling Rate

- **After every task commit:** Run `mix compile --warnings-as-errors && mix credo --strict`
- **After every plan wave:** Run `mix compile --no-optional-deps --warnings-as-errors && mix credo --strict && mix test --warnings-as-errors`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 300 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.1-01-01 | 01 | 1 | TBD (backlog phase) | — | Preserve invariant comments while removing planning IDs | lint + grep | `rg -n --glob "*.ex" "D-\\d+|Phase\\s+\\d|Plan\\s+\\d|\\[ASSUMED|REQ-|GSD" lib mailglass_admin/lib mailglass_inbound/lib && mix credo --strict` | ✅ | ⬜ pending |
| 999.1-02-01 | 02 | 2 | TBD (backlog phase) | — | Add drift guard to block new planning-artifact comments | lint | `mix credo --strict` | ❌ W0 | ⬜ pending |
| 999.1-03-01 | 03 | 3 | TBD (backlog phase) | — | Final sweep keeps behavior unchanged with no lint/test regressions | full suite | `mix compile --no-optional-deps --warnings-as-errors && mix credo --strict && mix test --warnings-as-errors` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `credo_checks/no_planning_artifact_comments.ex` — custom check skeleton and test coverage
- [ ] `test/credo_checks/no_planning_artifact_comments_test.exs` — enforce expected allow/deny patterns

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Safety/correctness comments retain intent after rewrite | Phase 999.1 context D-05 | Human judgement needed for semantic equivalence | Review Tier 0 files and confirm invariant-preservation checklist items are still explicitly documented in plain language |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 300s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
