---
phase: "999.1"
plan: "01"
title: "Tier 0 Comment Rewrite With Invariant Lock"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/mailglass/**/*.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
  - "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
requirements:
  - "TBD (backlog phase)"
autonomous: true
must_haves:
  truths:
    - "Tier 0 plus full core-package cleanup is inventory-driven: all planning artifact tokens (`D-*`, `Phase *`, `Plan *`, `[ASSUMED ...]`, `REQ-*`, `GSD`) are removed from `lib/mailglass/**/*.ex` and Tier 0 cross-package files owned by this plan."
    - "Security and correctness invariants stay explicit in plain language after rewrite."
    - "Phase scope remains comment/docstring only with no runtime behavior changes."
  artifacts:
    - path: "lib/mailglass/webhook/plug.ex"
      provides: "Verify-before-tenant and fail-closed signature commentary without planning shorthand"
      contains: "Verify signature before tenant lookup"
    - path: "lib/mailglass/outbound.ex"
      provides: "Adapter/transaction boundary and idempotency commentary without phase IDs"
      contains: "outside the database transaction"
  key_links:
    - from: ".planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md"
      to: "lib/mailglass/webhook/plug.ex"
      via: "Context D-05 invariant preservation is translated into concrete hotspot comment edits"
      pattern: "verify-before-tenant|security|correctness"
    - from: ".planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md"
      to: "lib/mailglass/outbound.ex"
      via: "Tier 0 hotspot cleanup starts with highest-risk invariant-bearing files"
      pattern: "Tier 0|adapter call stays outside DB transaction"
---

<objective>
Execute inventory-driven Tier 0 plus full core-package rewrite so source commentary reads as human rationale instead of planning chronology, while preserving load-bearing security/correctness intent.

Purpose: remove distracting planning-artifact language from Tier 0 hotspots, complete zero-token cleanup for `lib/mailglass/**/*.ex`, and hand off explicit residual admin/inbound inventory for Wave 2.
Output: Tier 0 and core-package files with planning tokens removed via full-file checks and invariant statements rewritten in plain behavior-anchored language.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-PATTERNS.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-VALIDATION.md
@.planning/PROJECT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Rewrite ingress-boundary comments in core and inbound Tier 0 files</name>
  <read_first>
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-PATTERNS.md
    - lib/mailglass/webhook/plug.ex
    - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  </read_first>
  <files>lib/mailglass/webhook/plug.ex, mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex</files>
  <action>In both ingress plugs, replace planning-ID comments with explicit invariant language and insert these exact phrases where the invariant is documented: `Verify signature before tenant lookup to fail closed on spoofed payloads.` and `Signature failures are terminal typed errors; do not recover in the plug.` Remove any token matching `D-\\d+`, `Phase \\d+(?:\\.\\d+)?`, `Plan \\d+(?:\\.\\d+)?`, `[ASSUMED`, `REQ-[A-Z0-9-]+`, or `GSD` from full file contents in these two files so scoped `rg` checks return zero.</action>
  <acceptance_criteria>
    - `lib/mailglass/webhook/plug.ex` contains `Verify signature before tenant lookup to fail closed on spoofed payloads.`
    - `mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex` contains `Verify signature before tenant lookup to fail closed on spoofed payloads.`
    - Both files contain `Signature failures are terminal typed errors; do not recover in the plug.`
    - Neither file contains planning artifact tokens anywhere in file contents.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Verify signature before tenant lookup to fail closed on spoofed payloads\." lib/mailglass/webhook/plug.ex mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex</automated>
    <automated>rg -n "Signature failures are terminal typed errors; do not recover in the plug\." lib/mailglass/webhook/plug.ex mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex</automated>
    <automated>! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass/webhook/plug.ex mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex</automated>
  </verify>
  <done>Ingress-boundary comments are human-readable and keep verify-first/fail-closed invariants explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Complete inventory-driven core sweep and lock Tier 0 outbound/provider/admin anchors</name>
  <read_first>
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
    - lib/mailglass
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  </read_first>
  <files>lib/mailglass/**/*.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <action>Run `rg -l --glob "*.ex" "D-\\d+|Phase\\s+\\d+(?:\\.\\d+)?|Plan\\s+\\d+(?:\\.\\d+)?|\\[ASSUMED|REQ-[A-Z0-9-]+|\\bGSD\\b" lib/mailglass` to generate the mandatory core rewrite inventory, then remove all matched tokens from every listed core file plus `mailglass_admin/lib/mailglass_admin/inbound_live.ex`. Keep edits comment/docstring focused with no runtime logic changes, and anchor invariant wording with exact text: in `lib/mailglass/outbound.ex` add `Provider adapter delivery runs outside the database transaction to avoid pool starvation and lock amplification.` and `Idempotency keys make duplicate dispatch attempts a database-level no-op.`; in `lib/mailglass/webhook/providers/ses.ex` add `Treat signature verification failures as non-recoverable input trust failures.`; in `mailglass_admin/lib/mailglass_admin/inbound_live.ex` add `Tenant-scoped operator actions must never widen visibility outside the current tenant.`. Capture residual admin/inbound inventory for Plan 02 via `rg -l --glob "*.ex" ... mailglass_admin/lib mailglass_inbound/lib` and include counts in `999.1-01-SUMMARY.md`.</action>
  <acceptance_criteria>
    - `lib/mailglass/outbound.ex` contains both required outbound invariant phrases.
    - `lib/mailglass/webhook/providers/ses.ex` contains the required verification-failure phrase.
    - `mailglass_admin/lib/mailglass_admin/inbound_live.ex` contains the required tenant-scoping phrase.
    - Full-file token scan returns zero matches for `lib/mailglass/**/*.ex`, `mailglass_admin/lib/mailglass_admin/inbound_live.ex`, and `mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex`.
    - `999.1-01-SUMMARY.md` includes residual Wave 2 inventory counts for `mailglass_admin/lib/**/*.ex` and `mailglass_inbound/lib/**/*.ex`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Provider adapter delivery runs outside the database transaction to avoid pool starvation and lock amplification\." lib/mailglass/outbound.ex</automated>
    <automated>rg -n "Idempotency keys make duplicate dispatch attempts a database-level no-op\." lib/mailglass/outbound.ex</automated>
    <automated>rg -n "Treat signature verification failures as non-recoverable input trust failures\." lib/mailglass/webhook/providers/ses.ex</automated>
    <automated>rg -n "Tenant-scoped operator actions must never widen visibility outside the current tenant\." mailglass_admin/lib/mailglass_admin/inbound_live.ex</automated>
    <automated>! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass mailglass_admin/lib/mailglass_admin/inbound_live.ex mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex</automated>
    <automated>rg -n "Residual Wave 2 inventory: admin=\d+, inbound=\d+" .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-01-SUMMARY.md</automated>
  </verify>
  <done>Tier 0 comments now explain behavior and invariants directly, without GSD provenance markers.</done>
</task>
</tasks>

<threat_model>
## Threat Register

| Threat ID | Severity | Threat | Mitigation in this plan |
|-----------|----------|--------|--------------------------|
| TM-9991-01 | high | Accidental removal of security/correctness invariant comments during cleanup | Required invariant phrase insertions are explicit and CLI-verified in ingress/outbound/provider/admin hotspots. |
| TM-9991-02 | high | Misleading rewrites that invert or soften fail-closed behavior | Rewrite actions require exact fail-closed wording for verify-before-tenant and non-recoverable signature paths. |
| TM-9991-03 | medium | Scope creep from comment cleanup into runtime changes during broad core sweep | Tasks lock runtime behavior, and acceptance is enforced by scoped full-file token checks plus compile/lint gates. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors`
- `mix credo --strict`
- `! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass mailglass_admin/lib/mailglass_admin/inbound_live.ex mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex`
</verification>

<success_criteria>
Tier 0 hotspots and all `lib/mailglass/**/*.ex` source files are zero-match clean for planning tokens, explicit security/correctness intent is preserved in plain language, and residual admin/inbound inventory is handed to Plan 02 with no behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-01-SUMMARY.md`.
</output>
