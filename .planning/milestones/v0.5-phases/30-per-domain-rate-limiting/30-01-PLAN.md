---
phase: 30-per-domain-rate-limiting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/mailglass/rate_limiter.ex, lib/mailglass/config.ex, lib/mailglass/outbound.ex]
autonomous: true
requirements: [RATE-01]
must_haves:
  truths:
    - "System throttles outbound dispatch based on {tenant, recipient_domain}."
    - "System throttles outbound dispatch based on global {recipient_domain}."
    - "System throttles outbound dispatch based on {sender_domain}."
    - "Transactional messages always bypass all rate limits (D-24)."
  artifacts:
    - path: "lib/mailglass/rate_limiter.ex"
      provides: "Multi-bucket rate limiting logic"
    - path: "lib/mailglass/config.ex"
      provides: "Configuration schema for multiple bucket types"
  key_links:
    - from: "lib/mailglass/outbound.ex"
      to: "lib/mailglass/rate_limiter.ex"
      via: "RateLimiter.check(msg)"
---

<objective>
Refactor the rate limiter to support multiple bucket types (tenant-recipient, global-recipient, and sender-domain) to provide better reputation protection for operators.

Purpose: Operators need to limit throughput at different granularities to satisfy ISP requirements and protect sender domain reputation.
Output: A multi-bucket rate limiter integrated into the outbound dispatch pipeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/mailglass/rate_limiter.ex
@lib/mailglass/config.ex
@lib/mailglass/outbound.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Config schema for multi-bucket rate limiting</name>
  <files>lib/mailglass/config.ex</files>
  <action>
    Update the `:rate_limit` key in `@schema` to support three sub-keys: `:tenant_recipient`, `:global_recipient`, and `:sender_domain`.
    Each sub-key should have `:default` and `:overrides` (following the existing pattern).
    Maintain backward compatibility during validation if possible, or provide a clean migration path in `new!/1` to wrap old-style config into `:tenant_recipient`.
  </action>
  <verify>
    <automated>mix test test/mailglass/config_test.exs</automated>
  </verify>
  <done>Config schema supports multi-bucket definitions and validates correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multi-bucket check in RateLimiter</name>
  <files>lib/mailglass/rate_limiter.ex</files>
  <behavior>
    - `check(msg)` returns `:ok` if all buckets have tokens.
    - `check(msg)` returns `{:error, %RateLimitError{}}` if ANY bucket is depleted.
    - `:transactional` stream bypasses ALL checks.
    - Keys in ETS should be prefixed or structured to avoid collisions: `{:tenant_recipient, tenant_id, domain}`, `{:global_recipient, domain}`, `{:sender, domain}`.
  </behavior>
  <action>
    Refactor `Mailglass.RateLimiter.check/3` into `check/1` which takes a `%Mailglass.Message{}`.
    Implement internal helpers `check_tenant_recipient/1`, `check_global_recipient/1`, and `check_sender/1`.
    Extract domains from the message: recipient domain from `msg.swoosh_email.to` and sender domain from `msg.swoosh_email.from`.
    Maintain the existing atomic ETS update logic using `:ets.update_counter/4`.
  </action>
  <verify>
    <automated>mix test test/mailglass/rate_limiter_test.exs</automated>
  </verify>
  <done>RateLimiter supports multiple buckets and passes tests.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate new RateLimiter.check/1 into Outbound pipeline</name>
  <files>lib/mailglass/outbound.ex</files>
  <action>
    Update all calls to `RateLimiter.check/3` in `Mailglass.Outbound` (in `do_send`, `do_deliver_later`, and `preflight_single`) to call `RateLimiter.check(msg)` instead.
    Ensure that the error return `{:error, %RateLimitError{}}` is correctly handled by the `with` blocks and persisted when necessary.
  </action>
  <verify>
    <automated>mix test test/mailglass/outbound_test.exs</automated>
  </verify>
  <done>Outbound dispatch uses the multi-bucket rate limiter during preflight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter -> Mailglass | Adopter provides Message and Config; Mailglass enforces limits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service | RateLimiter (ETS) | mitigate | Use `{tenant, domain}` keys to ensure ETS table growth is proportional to active delivery targets. |
| T-30-02 | Bypass | RateLimiter | accept | Transactional bypass is a documented design choice (D-24). |
| T-30-03 | Collision | RateLimiter (ETS) | mitigate | Use tagged tuples `{:type, ...}` as ETS keys to prevent different bucket types from colliding. |
</threat_model>

<success_criteria>
1. All three rate limit types (tenant-recipient, global-recipient, sender-domain) can be configured independently.
2. Exhausting any one of the three buckets results in a `RateLimitError`.
3. Transactional mail remains unthrottled.
</success_criteria>

<output>
After completion, create `.planning/phases/30-per-domain-rate-limiting/30-01-SUMMARY.md`
</output>
