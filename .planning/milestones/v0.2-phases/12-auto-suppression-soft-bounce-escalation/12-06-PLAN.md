---
phase: 12-auto-suppression-soft-bounce-escalation
plan: 06
type: execute
wave: 4
depends_on: [12-03, 12-05]
files_modified:
  - lib/mailglass/suppression.ex
  - lib/mailglass/suppression/entry.ex
  - lib/mailglass/migrations/postgres/v03.ex
  - guides/webhooks.md
  - test/mailglass/suppression/entry_test.exs
  - test/mailglass/suppression_test.exs
autonomous: true
requirements: [SUPP-05]

must_haves:
  truths:
    - Complaint suppressions are permanent at both the API and Postgres layers
    - Generic removal rejects `:complaint` and `:unsubscribe` rows while still permitting removable reasons such as `:hard_bounce`, `:manual`, and `:policy`
    - The operator docs explain the GDPR delete-source / keep-suppression pattern
  artifacts:
    - path: lib/mailglass/suppression.ex
      provides: public `remove/2` API and structured rejection behavior for permanent reasons
    - path: lib/mailglass/migrations/postgres/v03.ex
      provides: complaint permanence CHECK constraint
    - path: guides/webhooks.md
      provides: operator documentation for permanent complaint suppression handling
  key_links:
    - from: lib/mailglass/suppression.ex
      to: lib/mailglass/suppression/entry.ex
      via: removal rules keyed by `Entry.__reasons__/0` and permanence semantics
---

<objective>
Enforce permanent complaint suppression and document the operator contract.

Purpose: Make complaint suppressions structurally non-expiring and non-removable so compliance behavior cannot be bypassed by API mistakes or manual cleanup.
Output: New public `Mailglass.Suppression.remove/2` API, Postgres CHECK constraint for complaint permanence, tests, and operator documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/RESEARCH.md
@lib/mailglass/suppression.ex
@lib/mailglass/suppression/entry.ex
@lib/mailglass/migrations/postgres/v03.ex
@test/mailglass/suppression/entry_test.exs
@guides/webhooks.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public removal API and permanent-reason guards</name>
  <files>lib/mailglass/suppression.ex, test/mailglass/suppression_test.exs</files>
  <read_first>
    - lib/mailglass/suppression.ex
    - lib/mailglass/suppression/entry.ex
    - lib/mailglass/error.ex
    - test/mailglass/suppression_test.exs
  </read_first>
  <action>
    Introduce a public `Mailglass.Suppression.remove/2` API because the requirements and roadmap already reference it and the repo does not currently expose it.
    Make the API reject `reason: :complaint` and `reason: :unsubscribe` with a structured mailglass error shape consistent with the repo's existing hierarchy, and allow removal for `:hard_bounce`, escalation-created rows, `:manual`, and `:policy`.
    Keep removal explicit and auditable; do not add any silent delete paths or implicit re-consent behavior.
    Add tests asserting complaint and unsubscribe removals fail while removable reasons still delete successfully.
  </action>
  <acceptance_criteria>
    - `lib/mailglass/suppression.ex` contains `def remove(`
    - `lib/mailglass/suppression.ex` contains `:complaint`
    - `lib/mailglass/suppression.ex` contains `:unsubscribe`
    - `test/mailglass/suppression_test.exs` contains a rejection assertion for complaint removal
    - `mix test test/mailglass/suppression_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/suppression_test.exs</automated>
  </verify>
  <done>The repo has a real public removal API with explicit permanent-reason rejection semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce complaint permanence in Postgres and document the operator rule</name>
  <files>lib/mailglass/suppression/entry.ex, lib/mailglass/migrations/postgres/v03.ex, guides/webhooks.md, test/mailglass/suppression/entry_test.exs</files>
  <read_first>
    - lib/mailglass/suppression/entry.ex
    - lib/mailglass/migrations/postgres/v01.ex
    - lib/mailglass/migrations/postgres/v03.ex
    - test/mailglass/suppression/entry_test.exs
    - guides/webhooks.md
  </read_first>
  <action>
    Add the exact Postgres CHECK constraint required by Phase 12: `CHECK (reason != 'complaint' OR expires_at IS NULL)`.
    Mirror the same rule in `Mailglass.Suppression.Entry.changeset/1` so complaint rows with expiry fail before hitting Postgres.
    Add tests that prove a complaint row with `expires_at` is rejected and non-complaint rows can still expire normally.
    Update `guides/webhooks.md` with the operator-facing GDPR note: source delivery/webhook payload data may be deleted per retention or erasure policy, but the complaint suppression row remains as the durable compliance block.
  </action>
  <acceptance_criteria>
    - `lib/mailglass/migrations/postgres/v03.ex` contains `reason != 'complaint' OR expires_at IS NULL`
    - `lib/mailglass/suppression/entry.ex` contains complaint expiry validation logic
    - `guides/webhooks.md` contains `complaint` and `suppression`
    - `mix test test/mailglass/suppression/entry_test.exs test/mailglass/suppression_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/suppression/entry_test.exs test/mailglass/suppression_test.exs</automated>
  </verify>
  <done>Complaint permanence is enforced in both Elixir and Postgres and is documented for operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/API removal calls -> durable compliance state | Manual cleanup tools must not be able to erase complaint suppression accidentally |
| App-layer validation -> DB invariant | Postgres must still reject invalid complaint expiry data if app-layer checks are bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | suppression removal | mitigate | Reject complaint and unsubscribe removal in the public API |
| T-12-09 | Tampering | complaint expiry | mitigate | Enforce `reason != 'complaint' OR expires_at IS NULL` in both changeset and Postgres |
</threat_model>

<verification>
`mix test test/mailglass/suppression/entry_test.exs test/mailglass/suppression_test.exs` passes, `Mailglass.Suppression.remove/2` exists, and `lib/mailglass/migrations/postgres/v03.ex` contains the complaint-permanence CHECK constraint.
</verification>

<success_criteria>
Complaint suppressions cannot be given an expiry or removed through the generic API, and the operator docs explain why the suppression row outlives deletable source evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-suppression-soft-bounce-escalation/12-06-SUMMARY.md`
</output>
