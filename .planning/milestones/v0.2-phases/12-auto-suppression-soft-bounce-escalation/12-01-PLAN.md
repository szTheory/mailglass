---
phase: 12-auto-suppression-soft-bounce-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/suppression/auto_suppress.ex
  - lib/mailglass/webhook/ingest.ex
  - credo_checks/multi_event_first_in_webhook_ingest.ex
  - test/mailglass/properties/webhook_suppression_convergence_test.exs
  - test/credo_checks/multi_event_first_in_webhook_ingest_test.exs
autonomous: true
requirements: [SUPP-01]

must_haves:
  truths:
    - Webhook ingest appends the event row before any suppression write, and replaying the same suppression-causing events does not change final suppression state
    - Hard bounces, complaints, and unsubscribes auto-project into suppressions only when a linked delivery provides the authoritative recipient and stream
    - Unsubscribes suppress only the originating stream, while complaints and hard bounces suppress the full address
    - Orphan events are skipped deliberately instead of guessing provider-specific recipient data
  artifacts:
    - path: lib/mailglass/suppression/auto_suppress.ex
      provides: centralized event-to-scope and event-to-reason translation plus linked-delivery suppression projection
    - path: lib/mailglass/webhook/ingest.ex
      provides: flat Multi extension adding `{:auto_suppress, idx}` after `{:projector_apply, idx}` while keeping event append first
    - path: credo_checks/multi_event_first_in_webhook_ingest.ex
      provides: lint enforcement for event-first webhook ingest ordering
    - path: test/mailglass/properties/webhook_suppression_convergence_test.exs
      provides: replay convergence coverage for suppression-causing webhook sequences
  key_links:
    - from: lib/mailglass/webhook/ingest.ex
      to: lib/mailglass/suppression/auto_suppress.ex
      via: `Multi.run({:auto_suppress, idx}, ...)` immediately after `{:projector_apply, idx}`
    - from: lib/mailglass/suppression/auto_suppress.ex
      to: lib/mailglass/outbound/delivery.ex
      via: delivery-backed recipient and stream lookup for D-12-02, D-12-03, and D-12-04
    - from: credo_checks/multi_event_first_in_webhook_ingest.ex
      to: lib/mailglass/webhook/ingest.ex
      via: AST rule that rejects suppression steps placed before the event append step
---

<objective>
Deliver the event-first suppression projection contract for webhook ingest.

Purpose: Phase 12 depends on treating `mailglass_events` as the source of truth and `mailglass_suppressions` as a replay-safe projection, so the first plan locks the translation rules, ordering invariant, and orphan skip path before async or operator tooling builds on top.
Output: `Mailglass.Suppression.AutoSuppress`, webhook ingest integration, a dedicated Credo guard, and replay-convergence tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/RESEARCH.md
@.planning/phases/11-rfc-8058-list-unsubscribe/11-CONTEXT.md
@lib/mailglass/webhook/ingest.ex
@lib/mailglass/webhook/providers/postmark.ex
@lib/mailglass/webhook/providers/sendgrid.ex
@lib/mailglass/suppression/entry.ex
@lib/mailglass/suppression_store/ecto.ex
@lib/mailglass/outbound/delivery.ex
@test/mailglass/properties/webhook_idempotency_convergence_test.exs

<interfaces>
From lib/mailglass/webhook/ingest.ex:
```elixir
Events.append_multi(acc, event_step_name(idx), fn _changes -> ... end)
|> Multi.run({:projector_categorize, idx}, fn _repo, changes -> ... end)
|> Multi.run({:projector_apply, idx}, fn repo, changes -> ... end)
```

From lib/mailglass/suppression/entry.ex:
```elixir
scope :: :address | :domain | :address_stream
reason :: :manual | :policy | :hard_bounce | :complaint | :unsubscribe
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add centralized auto-suppression projection and wire it into webhook ingest</name>
  <read_first>
    - lib/mailglass/webhook/ingest.ex
    - lib/mailglass/suppression/entry.ex
    - lib/mailglass/suppression_store/ecto.ex
    - lib/mailglass/outbound/delivery.ex
    - .planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
  </read_first>
  <files>lib/mailglass/suppression/auto_suppress.ex, lib/mailglass/webhook/ingest.ex, test/mailglass/properties/webhook_suppression_convergence_test.exs</files>
  <behavior>
    - `:unsubscribed` maps to suppression `reason: :unsubscribe` and `scope: :address_stream` using the linked delivery stream per D-12-02 and D-12-07
    - `:complained` maps to suppression `reason: :complaint` and `scope: :address` per D-12-03 and D-12-08
    - hard `:bounced` maps to suppression `reason: :hard_bounce` and `scope: :address` per D-12-04 and D-12-07
    - orphan events return an explicit skip result instead of parsing provider-specific recipient fields
  </behavior>
  <action>Create `Mailglass.Suppression.AutoSuppress` as the single translation layer for D-12-01 through D-12-09, with one public function that receives the normalized event plus linked delivery context and returns either a concrete suppression candidate or an explicit skip. Extend the existing flat `Mailglass.Webhook.Ingest` Multi by appending `Multi.run({:auto_suppress, idx}, ...)` immediately after `{:projector_apply, idx}`; keep `Events.append_multi/3` first, do not nest a second Multi, and use the existing suppression insert path with `on_conflict: :nothing` so replays converge. Derive recipient and stream from the linked delivery only; if no delivery is linked, skip auto-suppression deliberately so later reconciliation or resync can rebuild from authoritative data instead of provider payload guesses. Seed `test/mailglass/properties/webhook_suppression_convergence_test.exs` in this task with the sequence fixture builders and replay assertions needed by the verify step.</action>
  <acceptance_criteria>
    - `rg -n 'Multi.run\\(\\{:auto_suppress, idx\\}' lib/mailglass/webhook/ingest.ex` returns exactly one match
    - `rg -n ':complained.*:complaint|:unsubscribed.*:unsubscribe|:bounced.*:hard_bounce' lib/mailglass/suppression/auto_suppress.ex` returns centralized mappings
    - `rg -n 'orphan|skip' lib/mailglass/suppression/auto_suppress.ex` shows an explicit orphan skip path
    - `mix test --warnings-as-errors test/mailglass/properties/webhook_suppression_convergence_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mailglass/properties/webhook_suppression_convergence_test.exs -x</automated>
  </verify>
  <done>Webhook ingest produces replay-safe suppression rows for linked hard-bounce, complaint, and unsubscribe events without violating the event-first ledger invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce event-first ordering with a dedicated Credo check and focused tests</name>
  <read_first>
    - credo_checks/stream_policy_consistent.ex
    - test/credo_checks/stream_policy_consistent_test.exs
    - lib/mailglass/webhook/ingest.ex
    - .planning/STATE.md
  </read_first>
  <files>credo_checks/multi_event_first_in_webhook_ingest.ex, test/credo_checks/multi_event_first_in_webhook_ingest_test.exs</files>
  <behavior>
    - Credo flags any suppression insert or enqueue step that appears before the event append step in `Mailglass.Webhook.Ingest`
    - The check allows the flat `event -> projector_categorize -> projector_apply -> auto_suppress` shape
    - Tests prove both the violation case and the allowed case
  </behavior>
  <action>Implement `Mailglass.Credo.MultiEventFirstInWebhookIngest` under `credo_checks/` using the same AST traversal style as the existing custom checks. Encode the project correction from `.planning/STATE.md` directly into the rule: event append must remain first, and any `{:auto_suppress, idx}` or future suppression-related step before that append is a lint failure. Add `test/credo_checks/multi_event_first_in_webhook_ingest_test.exs` using `Credo.Test.Case` with one fixture that passes for the sanctioned Phase 12 order and one fixture that fails when suppression precedes the event step.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Mailglass.Credo.MultiEventFirstInWebhookIngest' credo_checks/multi_event_first_in_webhook_ingest.ex` returns one match
    - `rg -n 'append_multi|auto_suppress' test/credo_checks/multi_event_first_in_webhook_ingest_test.exs` returns both passing and failing fixture coverage
    - `mix test --warnings-as-errors test/credo_checks/multi_event_first_in_webhook_ingest_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/credo_checks/multi_event_first_in_webhook_ingest_test.exs -x</automated>
  </verify>
  <done>Phase 12 has a lint-level guard that prevents regressions to suppression-before-event ordering in webhook ingest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verified webhook event -> suppression projection | Normalized provider events cross into durable suppression writes |
| Linked delivery -> suppression scope | Delivery recipient and stream data determine whether suppression is address-wide or stream-specific |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `lib/mailglass/webhook/ingest.ex` | mitigate | Keep `Events.append_multi/3` first and reject any suppression-first ordering with `Mailglass.Credo.MultiEventFirstInWebhookIngest` |
| T-12-02 | Information Disclosure | `lib/mailglass/suppression/auto_suppress.ex` | mitigate | Derive recipient and stream from linked deliveries only, and skip orphan events instead of reading provider-specific payload fields |
| T-12-03 | Repudiation | suppression projection | mitigate | Use centralized event-to-reason translation plus `on_conflict: :nothing` so replayed webhooks converge on auditable suppression state |
</threat_model>

<verification>
Run `mix test --warnings-as-errors test/mailglass/properties/webhook_suppression_convergence_test.exs test/credo_checks/multi_event_first_in_webhook_ingest_test.exs -x`.
</verification>

<success_criteria>
SUPP-01 is executable: webhook ingest projects suppression rows from linked deliveries, preserves event-first ordering, skips orphan guesses, and proves replay convergence under property coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-suppression-soft-bounce-escalation/12-01-SUMMARY.md`
</output>
