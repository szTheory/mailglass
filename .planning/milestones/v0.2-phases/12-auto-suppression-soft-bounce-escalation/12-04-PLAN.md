---
phase: 12-auto-suppression-soft-bounce-escalation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/suppression.ex
  - lib/mailglass/suppression_store/ecto.ex
  - priv/repo/migrations/20260428121000_enforce_complaint_permanence.exs
  - test/mailglass/suppression_test.exs
  - test/mailglass/suppression_store/ecto_test.exs
  - test/mailglass/suppression/remove_test.exs
autonomous: true
requirements: [SUPP-04, SUPP-05]

must_haves:
  truths:
    - Pre-send suppression checks return structured `%Mailglass.Error{type: :suppressed}` details and emit only PII-safe telemetry
    - A complaint or unsubscribe suppression cannot be removed through the generic public API
    - Complaint suppressions cannot carry an expiry, and Postgres enforces that invariant even if application code regresses
    - Hard-bounce, escalation-derived, manual, and policy suppressions remain removable through explicit audited paths
  artifacts:
    - path: lib/mailglass/suppression.ex
      provides: tightened pre-send behavior and public removal contract
    - path: lib/mailglass/suppression_store/ecto.ex
      provides: persistence behavior respecting permanence and structured suppression details
    - path: priv/repo/migrations/20260428121000_enforce_complaint_permanence.exs
      provides: Postgres constraint keeping `reason = 'complaint'` rows non-expiring
    - path: test/mailglass/suppression/remove_test.exs
      provides: complaint and unsubscribe removal rejection coverage
  key_links:
    - from: lib/mailglass/suppression.ex
      to: lib/mailglass/suppression_store/ecto.ex
      via: structured pre-send block responses and removal gating
    - from: lib/mailglass/suppression_store/ecto.ex
      to: priv/repo/migrations/20260428121000_enforce_complaint_permanence.exs
      via: application-layer behavior backed by a matching Postgres constraint
    - from: lib/mailglass/suppression.ex
      to: test/mailglass/suppression_test.exs
      via: telemetry assertions for `[:mailglass, :suppression, :auto_added, :stop]` and `[:mailglass, :suppression, :pre_send_blocked, :stop]`
---

<objective>
Tighten the public suppression API so permanence and pre-send behavior are enforceable, auditable, and PII-safe.

Purpose: Auto-added suppression rows only help if send-time checks default-deny correctly and non-reversible consent or complaint records cannot be relaxed accidentally in code or SQL.
Output: structured pre-send blocking, removal-policy enforcement, complaint permanence constraint, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/RESEARCH.md
@lib/mailglass/suppression.ex
@lib/mailglass/suppression_store/ecto.ex
@lib/mailglass/suppression/entry.ex
@test/mailglass/suppression_test.exs
@test/mailglass/suppression_store/ecto_test.exs

<interfaces>
From lib/mailglass/suppression/entry.ex:
```elixir
reason :: :manual | :policy | :hard_bounce | :complaint | :unsubscribe
```

From lib/mailglass/suppression.ex:
```elixir
check_before_send(...)
remove(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten pre-send suppression blocking and telemetry</name>
  <read_first>
    - lib/mailglass/suppression.ex
    - lib/mailglass/suppression_store/ecto.ex
    - test/mailglass/suppression_test.exs
    - .planning/PROJECT.md
  </read_first>
  <files>lib/mailglass/suppression.ex, lib/mailglass/suppression_store/ecto.ex, test/mailglass/suppression_test.exs</files>
  <behavior>
    - Matching suppressions return `%Mailglass.Error{type: :suppressed, detail: %{reason: atom, source: atom, expires_at: DateTime.t() | nil}}`
    - Telemetry emits `[:mailglass, :suppression, :auto_added, :stop]` and `[:mailglass, :suppression, :pre_send_blocked, :stop]`
    - Telemetry metadata remains whitelisted and contains no recipient, email, subject, or body fields
  </behavior>
  <action>Update `Mailglass.Suppression.check_before_send/1` and its storage-facing helpers so suppression matches default-deny with structured public errors for SUPP-04, rather than opaque or partial failure data. Preserve the project's closed error-contract style, emit exactly the two telemetry event names called out in the roadmap, and keep metadata limited to non-PII fields such as tenant ID, scope, reason, source, and expiry presence. Expand `test/mailglass/suppression_test.exs` to assert the exact error shape, that all send paths consume the richer detail without string matching, and that telemetry handlers never receive blacklisted metadata keys.</action>
  <acceptance_criteria>
    - `rg -n ':pre_send_blocked|:auto_added' lib/mailglass/suppression.ex test/mailglass/suppression_test.exs` returns the new telemetry names
    - `rg -n 'type: :suppressed|expires_at|source|reason' lib/mailglass/suppression.ex test/mailglass/suppression_test.exs` returns the structured error contract
    - `mix test --warnings-as-errors test/mailglass/suppression_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mailglass/suppression_test.exs -x</automated>
  </verify>
  <done>Pre-send suppression checks are explicit, structured, and instrumented with PII-safe telemetry only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce complaint permanence and generic removal rejection</name>
  <read_first>
    - lib/mailglass/suppression.ex
    - lib/mailglass/suppression_store/ecto.ex
    - test/mailglass/suppression_store/ecto_test.exs
    - .planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
  </read_first>
  <files>lib/mailglass/suppression.ex, lib/mailglass/suppression_store/ecto.ex, priv/repo/migrations/20260428121000_enforce_complaint_permanence.exs, test/mailglass/suppression_store/ecto_test.exs, test/mailglass/suppression/remove_test.exs</files>
  <behavior>
    - `:complaint` rows reject any expiry and remain non-removable per D-12-16
    - `:unsubscribe` rows are non-removable through the generic public API per D-12-17
    - `:hard_bounce`, escalation-derived, `:manual`, and `:policy` rows remain removable per D-12-18
  </behavior>
  <action>Add `priv/repo/migrations/20260428121000_enforce_complaint_permanence.exs` implementing the Postgres constraint `CHECK (reason != 'complaint' OR expires_at IS NULL)` and update the Ecto store path so complaint re-inserts cannot introduce an expiry through an upsert conflict path. In `Mailglass.Suppression.remove/2`, reject both `:complaint` and `:unsubscribe` through structured `%Mailglass.Error{}` responses, while preserving explicit audited removal for `:hard_bounce`, escalation-derived rows marked by their provenance, `:manual`, and `:policy`. Add `test/mailglass/suppression_store/ecto_test.exs` assertions for the constraint and upsert behavior plus a new `test/mailglass/suppression/remove_test.exs` covering allowed and rejected removal cases using the `:complaint` reason atom per D-12-08, not the outdated `:complained` wording.</action>
  <acceptance_criteria>
    - `rg -n "reason != 'complaint' OR expires_at IS NULL" priv/repo/migrations/20260428121000_enforce_complaint_permanence.exs` returns the exact constraint
    - `rg -n ':complaint|:unsubscribe|:hard_bounce|:manual|:policy' test/mailglass/suppression/remove_test.exs` returns removal-policy coverage for allowed and rejected reasons
    - `mix test --warnings-as-errors test/mailglass/suppression_store/ecto_test.exs test/mailglass/suppression/remove_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mailglass/suppression_store/ecto_test.exs test/mailglass/suppression/remove_test.exs -x</automated>
  </verify>
  <done>`:complaint` permanence is enforced in both SQL and application code, `:unsubscribe` remains non-removable through the generic API, and allowed removals stay explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suppression lookup -> send pipeline | Durable suppression state blocks outbound mail before send |
| Public removal API -> suppression store | Operator actions can attempt to relax or delete durable suppression records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Information Disclosure | suppression telemetry | mitigate | Emit only whitelisted metadata and assert blacklisted PII keys are absent in tests |
| T-12-11 | Tampering | `Mailglass.Suppression.remove/2` | mitigate | Reject `:complaint` and `:unsubscribe` via structured errors while preserving explicit remove paths for allowed reasons |
| T-12-12 | Elevation of Privilege | complaint permanence | mitigate | Back application-layer rejection with a Postgres `CHECK (reason != 'complaint' OR expires_at IS NULL)` constraint |
</threat_model>

<verification>
Run `mix test --warnings-as-errors test/mailglass/suppression_test.exs test/mailglass/suppression_store/ecto_test.exs test/mailglass/suppression/remove_test.exs -x`.
</verification>

<success_criteria>
SUPP-04 and SUPP-05 are satisfied: suppression matches block sends with structured, PII-safe behavior, and complaint permanence plus generic-removal rejection are enforced in code and Postgres.
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-suppression-soft-bounce-escalation/12-04-SUMMARY.md`
</output>
