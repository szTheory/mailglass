---
phase: 12-auto-suppression-soft-bounce-escalation
plan: 05
type: execute
wave: 3
depends_on: [12-01]
files_modified:
  - lib/mailglass/suppression.ex
  - lib/mailglass/errors/suppressed_error.ex
  - lib/mailglass/outbound.ex
  - test/mailglass/suppression_test.exs
  - test/mailglass/outbound/preflight_test.exs
autonomous: true
requirements: [SUPP-04]

must_haves:
  truths:
    - Pre-send suppression blocks remain default-deny and surface structured reason, source, and expiry detail without leaking recipient PII
    - The repo preserves its real public error hierarchy by enriching `Mailglass.SuppressedError` context instead of inventing a nonexistent `%Mailglass.Error{}` parent struct
    - Suppression telemetry remains whitelist-safe and emits the new auto-added / pre-send-blocked signals
  artifacts:
    - path: lib/mailglass/suppression.ex
      provides: enriched pre-send result shaping and new suppression telemetry events
    - path: lib/mailglass/errors/suppressed_error.ex
      provides: detail-carrying structured error context for suppression hits
    - path: test/mailglass/suppression_test.exs
      provides: regression coverage for context shape and telemetry metadata
  key_links:
    - from: lib/mailglass/suppression.ex
      to: lib/mailglass/errors/suppressed_error.ex
      via: enriched `context` keys `reason`, `source`, and `expires_at`
---

<objective>
Tighten the pre-send suppression surface and telemetry contract.

Purpose: Make suppression hits explicit, detail-rich, and telemetry-visible while staying consistent with the repo's existing `Mailglass.SuppressedError` hierarchy and PII rules.
Output: Enriched suppression facade behavior, updated error struct context, and regression tests for preflight and telemetry.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/RESEARCH.md
@lib/mailglass/suppression.ex
@lib/mailglass/errors/suppressed_error.ex
@lib/mailglass/outbound.ex
@test/mailglass/suppression_test.exs
@test/mailglass/outbound/preflight_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich suppression-hit errors without changing the repo's public hierarchy</name>
  <files>lib/mailglass/suppression.ex, lib/mailglass/errors/suppressed_error.ex, test/mailglass/suppression_test.exs, test/mailglass/outbound/preflight_test.exs</files>
  <read_first>
    - lib/mailglass/suppression.ex
    - lib/mailglass/errors/suppressed_error.ex
    - lib/mailglass/error.ex
    - test/mailglass/suppression_test.exs
    - test/mailglass/outbound/preflight_test.exs
  </read_first>
  <action>
    Keep the public result shape repo-native: `{:error, %Mailglass.SuppressedError{}}`.
    Extend the suppression-hit context to include `reason`, `source`, and `expires_at` in addition to `tenant_id` and `stream`, while still excluding address and other recipient-identifying fields.
    If needed, add helper constructors or formatting branches in `Mailglass.SuppressedError` so the struct stays the single public exception type for suppression preflight failures.
    Update tests to assert the enriched context is present and that the preflight path in `Mailglass.Outbound` still halts before adapter work.
  </action>
  <acceptance_criteria>
    - `lib/mailglass/errors/suppressed_error.ex` contains `reason`
    - `lib/mailglass/errors/suppressed_error.ex` contains `source`
    - `lib/mailglass/errors/suppressed_error.ex` contains `expires_at`
    - `test/mailglass/suppression_test.exs` asserts context keys for `reason`, `source`, and `expires_at`
    - `mix test test/mailglass/suppression_test.exs test/mailglass/outbound/preflight_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/suppression_test.exs test/mailglass/outbound/preflight_test.exs</automated>
  </verify>
  <done>Suppression blocks remain structured and default-deny while carrying enough non-PII detail for callers and tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit suppression telemetry for auto-add and pre-send-blocked paths</name>
  <files>lib/mailglass/suppression.ex, lib/mailglass/webhook/ingest.ex, test/mailglass/suppression_test.exs</files>
  <read_first>
    - lib/mailglass/suppression.ex
    - lib/mailglass/webhook/ingest.ex
    - test/mailglass/suppression_test.exs
  </read_first>
  <action>
    Add telemetry emits for `[:mailglass, :suppression, :auto_added, :stop]` when Plan 12-01 inserts a suppression row and `[:mailglass, :suppression, :pre_send_blocked, :stop]` when `check_before_send/1` returns a suppression error.
    Keep telemetry metadata whitelist-safe: include tenant, scope, reason, source class, and expiry presence when useful, but exclude recipient address and body-derived content.
    Update tests to attach handlers and assert the new event names and metadata keys.
  </action>
  <acceptance_criteria>
    - `lib/mailglass/suppression.ex` contains `[:mailglass, :suppression, :pre_send_blocked, :stop]`
    - `lib/mailglass/webhook/ingest.ex` or `lib/mailglass/suppression/auto_suppress.ex` contains `[:mailglass, :suppression, :auto_added, :stop]`
    - `test/mailglass/suppression_test.exs` attaches handlers for the new event names
    - `mix test test/mailglass/suppression_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/suppression_test.exs</automated>
  </verify>
  <done>Suppression add/block paths emit explicit telemetry without violating the repo's PII rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suppression hit -> caller-visible error | The public error contract must reveal enough detail for diagnosis without leaking recipient data |
| Internal suppression events -> telemetry | Observability signals must remain non-PII and safe for broad ingestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | suppression telemetry | mitigate | Restrict metadata to tenant, scope, reason, source class, and expiry presence only |
</threat_model>

<verification>
`mix test test/mailglass/suppression_test.exs test/mailglass/outbound/preflight_test.exs` passes and the enriched suppression context contains `reason`, `source`, and `expires_at` without address fields.
</verification>

<success_criteria>
Pre-send suppression is still fail-closed, now carries actionable structured context, and emits explicit PII-safe telemetry for both block and auto-add paths.
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-suppression-soft-bounce-escalation/12-05-SUMMARY.md`
</output>
