---
phase: 12-auto-suppression-soft-bounce-escalation
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - lib/mailglass/suppression/resync.ex
  - lib/mix/tasks/mailglass.suppressions.resync.ex
  - test/mailglass/suppression/resync_test.exs
  - test/mix/tasks/mailglass.suppressions.resync_test.exs
autonomous: true
requirements: [SUPP-03]

must_haves:
  truths:
    - `mix mailglass.suppressions.resync` fails fast without `--tenant-id`
    - Dry-run and apply modes use the same candidate-selection path and report stable counts for `scanned`, `would_insert`, and `existing`
    - Resync rebuilds suppressions from tenant-scoped event history without relying on ambient tenancy defaults
    - Resync preserves the same event-to-reason and scope rules as webhook auto-suppression
  artifacts:
    - path: lib/mailglass/suppression/resync.ex
      provides: shared tenant-scoped candidate selection and projection apply/dry-run logic
    - path: lib/mix/tasks/mailglass.suppressions.resync.ex
      provides: strict CLI wrapper requiring `--tenant-id` and ISO-8601 window flags
    - path: test/mix/tasks/mailglass.suppressions.resync_test.exs
      provides: CLI contract and tenant-scope coverage
  key_links:
    - from: lib/mix/tasks/mailglass.suppressions.resync.ex
      to: lib/mailglass/suppression/resync.ex
      via: shared projection path used for both dry-run and apply
    - from: lib/mailglass/suppression/resync.ex
      to: lib/mailglass/tenancy.ex
      via: explicit `Tenancy.with_tenant/2` and `Tenancy.scope/2` scoping, never ambient `Tenancy.current/0`
    - from: lib/mailglass/suppression/resync.ex
      to: lib/mailglass/suppression/auto_suppress.ex
      via: reuse of centralized event-to-reason and scope translation from Plan 12-01
---

<objective>
Ship the tenant-scoped suppression rebuild path for operator repair.

Purpose: Auto-suppression deliberately skips orphan events and late-linked deliveries, so Phase 12 needs one strict, idempotent repair command that replays authoritative event history per tenant and never risks cross-tenant scans.
Output: shared resync service, strict Mix task, and automated CLI plus projection coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/RESEARCH.md
@lib/mailglass/tenancy.ex
@lib/mailglass/events/reconciler.ex
@lib/mix/tasks/mailglass.reconcile.ex
@lib/mix/tasks/mailglass.gen.unsubscribe.ex
@lib/mailglass/suppression/auto_suppress.ex

<interfaces>
From lib/mailglass/events/reconciler.ex:
```elixir
Mailglass.Repo.all(Tenancy.scope(query, tid))
```

From lib/mix/tasks/mailglass.reconcile.ex:
```elixir
OptionParser.parse(argv, strict: [...])
Mix.raise(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tenant-scoped suppression resync as a shared service</name>
  <read_first>
    - lib/mailglass/tenancy.ex
    - lib/mailglass/events/reconciler.ex
    - lib/mailglass/suppression/auto_suppress.ex
    - .planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
  </read_first>
  <files>lib/mailglass/suppression/resync.ex, test/mailglass/suppression/resync_test.exs</files>
  <behavior>
    - The default scan window is the last 90 days per D-12-26
    - `--from` and `--to` map to ISO-8601 boundaries on the same candidate path used by dry-run and apply
    - Reads and writes are explicitly tenant-scoped per D-12-21 and D-12-25
    - Orphan events stay skipped until linked delivery data is available, matching Plan 12-01 behavior
  </behavior>
  <action>Create `Mailglass.Suppression.Resync` as the canonical projection helper for SUPP-03. Reuse Plan 12-01's centralized event-to-reason and scope translation instead of re-encoding mappings, select only suppression-causing events inside an explicitly provided tenant scope, and thread the same candidate list through both dry-run and apply so D-12-23 holds exactly. Implement default last-90-days boundaries, ISO-8601 overrides, explicit `Tenancy.with_tenant/2` plus `Tenancy.scope/2`, and idempotent insertion with counts for `scanned`, `would_insert`, `inserted`, `existing`, and `skipped_orphan`. Add `test/mailglass/suppression/resync_test.exs` to prove identical candidate selection across dry-run/apply and to reject any unscoped query shortcut.</action>
  <acceptance_criteria>
    - `rg -n 'Tenancy\\.with_tenant|Tenancy\\.scope' lib/mailglass/suppression/resync.ex` returns explicit tenant stamping and query scoping
    - `rg -n '90|from|to|would_insert|skipped_orphan' lib/mailglass/suppression/resync.ex` returns the window and reporting fields
    - `mix test --warnings-as-errors test/mailglass/suppression/resync_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mailglass/suppression/resync_test.exs -x</automated>
  </verify>
  <done>The library has one tenant-scoped, idempotent resync service that dry-run and apply mode both reuse without divergence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the resync service through a strict operator Mix task</name>
  <read_first>
    - lib/mix/tasks/mailglass.reconcile.ex
    - lib/mix/tasks/mailglass.gen.unsubscribe.ex
    - lib/mailglass/suppression/resync.ex
    - .planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
  </read_first>
  <files>lib/mix/tasks/mailglass.suppressions.resync.ex, test/mix/tasks/mailglass.suppressions.resync_test.exs</files>
  <behavior>
    - `--tenant-id` is mandatory and unknown flags fail loudly
    - Default output is concise, while `--verbose` adds detail and `--dry-run` reports `would_insert`
    - The task does not rely on `Tenancy.current/0` or any unscoped Repo path
  </behavior>
  <action>Implement `Mix.Tasks.Mailglass.Suppressions.Resync` with strict `OptionParser` validation modeled on the existing mailglass Mix tasks. Require `--tenant-id`, accept only `--from`, `--to`, `--dry-run`, and `--verbose`, route both dry-run and apply through `Mailglass.Suppression.Resync`, and return structured task failures for missing tenant IDs or invalid ISO-8601 values instead of ad hoc strings. Add `test/mix/tasks/mailglass.suppressions.resync_test.exs` that captures shell output and asserts the exact contract for missing tenant ID, dry-run count reporting, and verbose mode while also proving the task cannot succeed through ambient tenancy defaults.</action>
  <acceptance_criteria>
    - `rg -n 'tenant-id|dry-run|verbose|from|to' lib/mix/tasks/mailglass.suppressions.resync.ex` returns only the supported flags
    - `rg -n 'would_insert|scanned|existing' test/mix/tasks/mailglass.suppressions.resync_test.exs` returns output-contract assertions
    - `mix test --warnings-as-errors test/mix/tasks/mailglass.suppressions.resync_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mix/tasks/mailglass.suppressions.resync_test.exs -x</automated>
  </verify>
  <done>Operators can run one strict, tenant-required resync command that reports stable counts and cannot leak across tenants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI input -> tenant-scoped resync | User-supplied flags choose tenant and time window for projection rebuild |
| Tenant-scoped queries -> suppression inserts | Historical event reads drive durable suppression writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | `lib/mix/tasks/mailglass.suppressions.resync.ex` | mitigate | Require `--tenant-id` and reject any ambient-tenant fallback or unscoped run path |
| T-12-08 | Tampering | `lib/mailglass/suppression/resync.ex` | mitigate | Reuse the same candidate-selection path for dry-run and apply so operator previews match applied results |
| T-12-09 | Repudiation | resync reporting | mitigate | Emit deterministic `scanned`, `would_insert`, `inserted`, `existing`, and `skipped_orphan` counts for every run |
</threat_model>

<verification>
Run `mix test --warnings-as-errors test/mailglass/suppression/resync_test.exs test/mix/tasks/mailglass.suppressions.resync_test.exs -x`.
</verification>

<success_criteria>
SUPP-03 is satisfied with a strict, per-tenant resync command that rebuilds suppression state from authoritative event history without cross-tenant leakage or dry-run/apply drift.
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-suppression-soft-bounce-escalation/12-03-SUMMARY.md`
</output>
