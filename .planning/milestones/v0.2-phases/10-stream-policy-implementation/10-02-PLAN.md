---
phase: 10-stream-policy-implementation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/mailglass/errors/stream_policy_error.ex
  - lib/mailglass/stream.ex
  - test/mailglass/stream_test.exs
autonomous: true
requirements: [STREAM-02]

must_haves:
  truths:
    - Sending a message that violates stream policy (e.g. `:bulk` without `mailable` set) raises a structured error tuple
    - Existing `with :ok <- Stream.policy_check(msg)` call sites require zero modification
  artifacts:
    - path: lib/mailglass/errors/stream_policy_error.ex
      provides: StreamPolicyError struct implementing Mailglass.Error
    - path: lib/mailglass/stream.ex
      provides: policy_check/1 returning error tuples for violations
  key_links:
    - from: lib/mailglass/stream.ex
      to: lib/mailglass/errors/stream_policy_error.ex
      via: Return of `{:error, %StreamPolicyError{}}` instead of raise
---

<objective>
Implement the runtime StreamPolicy stage.

Purpose: Replace the no-op seam in `stream.ex` with actual runtime validation that returns a structured error without breaking existing outbound pipeline caller expectations.
Output: `Mailglass.StreamPolicyError` struct, an updated `Mailglass.Stream.policy_check/1`, and corresponding unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-stream-policy-implementation/10-PATTERNS.md
@.planning/phases/10-stream-policy-implementation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Mailglass.StreamPolicyError</name>
  <files>lib/mailglass/errors/stream_policy_error.ex</files>
  <action>
    Create `Mailglass.StreamPolicyError` using the error struct design pattern found in `10-PATTERNS.md` (analog: `RateLimitError`).
    Implement the `@behaviour Mailglass.Error`. Ensure it supports `type: :stream_policy_violated` and includes a `:detail` field (`%{rule: atom(), suggestion: String.t()}`).
  </action>
  <verify>
    <automated>elixirc lib/mailglass/errors/stream_policy_error.ex</automated>
  </verify>
  <done>The error struct is defined and complies with `Mailglass.Error` behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement real policy_check/1</name>
  <files>lib/mailglass/stream.ex, test/mailglass/stream_test.exs</files>
  <behavior>
    - `%Message{stream: :bulk, mailable: nil}` returns `{:error, %StreamPolicyError{...}}`
    - Valid messages return `:ok`
    - Telemetry is emitted on all paths (including errors)
  </behavior>
  <action>
    Update `Mailglass.Stream.policy_check/1`. Replace the no-op with real validation logic.
    For instance, enforce that `:bulk` stream messages MUST have a `mailable` associated (so that `Feedback-ID` can be populated later).
    If invalid, return `{:error, Mailglass.StreamPolicyError.new(:stream_policy_violated, detail: %{rule: :bulk_requires_mailable, suggestion: "..."})}`. Do NOT raise directly, to allow `with` macros to intercept the tuple smoothly. Ensure `emit_telemetry` is called appropriately on success and failure.
    Add basic unit tests to `test/mailglass/stream_test.exs` verifying that `policy_check/1` succeeds for valid streams and fails correctly for invalid combinations.
  </action>
  <verify>
    <automated>mix test test/mailglass/stream_test.exs</automated>
  </verify>
  <done>policy_check/1 enforces runtime constraints, returns {:error, struct} on violation, and is verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline Stage | `policy_check/1` guards the boundary between Mailable builder output and adapter send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Elevation of Privilege | Stream | mitigate | Prevent sending bulk messages through anonymous / untraceable pipelines by mandating `mailable` on `:bulk` |
</threat_model>

<verification>
`mix test` passes; `Mailglass.StreamPolicyError` works identically to other error structs.
</verification>

<success_criteria>
`Mailglass.Stream.policy_check/1` returns an error tuple for `:bulk` streams missing `mailable`, preserving caller contract.
</success_criteria>

<output>
After completion, create `.planning/phases/10-stream-policy-implementation/10-02-SUMMARY.md`
</output>