---
phase: 10-stream-policy-implementation
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/mailglass/config.ex
  - lib/mailglass/compliance.ex
  - test/mailglass/compliance_test.exs
autonomous: true
requirements: [STREAM-04]

must_haves:
  truths:
    - Feedback-ID format auto-populated as `{sender_id}:{mailable}:{tenant_id}:{stream}` when feedback_id is configured
    - stream slot reflects runtime stream value
  artifacts:
    - path: lib/mailglass/config.ex
      provides: `feedback_id` schema field
    - path: lib/mailglass/compliance.ex
      provides: Logic to inject the Feedback-ID header into the message
  key_links:
    - from: lib/mailglass/compliance.ex
      to: lib/mailglass/config.ex
      via: Reading the `feedback_id` config at runtime
---

<objective>
Update configuration and compliance injection to support the RFC-compatible Feedback-ID header, integrating the runtime stream value.

Purpose: Enable email providers to bucket reputation appropriately by tenant, mailable, and stream, allowing adopters to identify deliverability issues granularly.
Output: Updated `Mailglass.Config` schema, updated `Mailglass.Compliance` header injection logic, and supporting tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-stream-policy-implementation/10-PATTERNS.md
@.planning/phases/10-stream-policy-implementation/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add feedback_id to Config</name>
  <files>lib/mailglass/config.ex</files>
  <behavior>
    - `feedback_id` can be a string or nil
    - defaults to nil
  </behavior>
  <action>
    Update `Mailglass.Config` schema.
    Add `feedback_id` with `type: {:or, [:string, nil]}`, `default: nil`, and an appropriate `doc` string.
  </action>
  <verify>
    <automated>elixirc lib/mailglass/config.ex</automated>
  </verify>
  <done>Schema updated and compiles successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject Feedback-ID header via Compliance</name>
  <files>lib/mailglass/compliance.ex, test/mailglass/compliance_test.exs</files>
  <behavior>
    - If `feedback_id` config is nil, no header injected.
    - If `feedback_id` is set, injects `Feedback-ID: {sender_id}:{mailable}:{tenant_id}:{stream}`.
    - Missing tenant_id interpolates as `"default"`.
    - Missing mailable interpolates as `"unknown"`.
  </behavior>
  <action>
    Add a new function `maybe_add_feedback_id/1` (or similar) that accepts a `%Mailglass.Message{}`.
    Read `Application.get_env(:mailglass, :feedback_id)`. If present, format the Feedback-ID value using the message's `tenant_id` (fallback to "default"), `mailable` (extract module name or fallback to "unknown"), and `stream`.
    Update the message's inner `%Swoosh.Email{}` headers.
    Ensure existing `put_header_if_absent` logic is followed so adopters can override Feedback-ID manually if they choose. Reference the Header Injection Pattern and Put Header If Absent Pattern from `lib/mailglass/compliance.ex` in `10-PATTERNS.md`.
    Add test cases to `test/mailglass/compliance_test.exs`. Mock the application environment for `feedback_id` config, testing that the `maybe_add_feedback_id/1` logic yields `{sender_id}:{mailable}:{tenant_id}:{stream}` cleanly, handles fallbacks, and respects explicit overrides.
  </action>
  <verify>
    <automated>mix test test/mailglass/compliance_test.exs</automated>
  </verify>
  <done>Feedback-ID is automatically appended to the email headers when configured and correctly tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Systems | Feedback-ID is parsed by receiving ISPs to bucket reputation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Compliance | mitigate | Ensure Feedback-ID header is not overwritten if the adopter has explicitly provided one manually |
</threat_model>

<verification>
Unit tests assert the correct construction of the Feedback-ID header with all slots populated correctly.
</verification>

<success_criteria>
`Mailglass.Config` validates the `feedback_id` setting, and `Mailglass.Compliance` injects a correctly formatted string using the runtime stream value.
</success_criteria>

<output>
After completion, create `.planning/phases/10-stream-policy-implementation/10-04-SUMMARY.md`
</output>