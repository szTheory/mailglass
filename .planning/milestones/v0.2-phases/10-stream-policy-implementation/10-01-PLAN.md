---
phase: 10-stream-policy-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/stream.ex
  - lib/mailglass/message.ex
  - test/mailglass/stream_test.exs
  - test/mailglass/message_test.exs
autonomous: true
requirements: [STREAM-01]

must_haves:
  truths:
    - A mailable declared `use Mailglass.Mailable, stream: :bulk` has `%Message{stream: :bulk}` stamped at `Message.new_from_use/2`
    - Runtime `%Message{stream: :bulk}` assignment also works via `put_stream/2`
    - `:transactional`, `:operational`, `:bulk` are the only accepted atoms for the stream field
  artifacts:
    - path: lib/mailglass/stream.ex
      provides: Stream atom definitions and valid?/1 guard
    - path: lib/mailglass/message.ex
      provides: put_stream/2 setter and strict new_from_use/2 stamping
  key_links:
    - from: lib/mailglass/message.ex
      to: lib/mailglass/stream.ex
      via: valid?/1 guard in setter and builder functions
---

<objective>
Implement the core stream atom set and strict runtime setters for message streams.

Purpose: Provide the foundation for stream policy by ensuring `Mailglass.Message` structs can only ever carry a valid stream atom (`:transactional`, `:operational`, or `:bulk`), both at compile-time generation and runtime mutation.
Output: Updated `Mailglass.Stream` and `Mailglass.Message` modules enforcing the closed set, complete with basic unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-stream-policy-implementation/10-PATTERNS.md
@.planning/phases/10-stream-policy-implementation/RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
From lib/mailglass/message.ex:
```elixir
  @type stream :: :transactional | :operational | :bulk
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define closed stream atom set</name>
  <files>lib/mailglass/stream.ex, test/mailglass/stream_test.exs</files>
  <behavior>
    - `valid?(:transactional)` returns true
    - `valid?(:bulk)` returns true
    - `valid?(:operational)` returns true
    - `valid?(:other)` returns false
  </behavior>
  <action>
    Add a module attribute `@streams [:transactional, :operational, :bulk]` to `Mailglass.Stream`.
    Implement a `valid?(stream)` function (or a guard macro `is_stream(s)`) that returns true if the atom is in the closed set.
    Use the analog `lib/mailglass/rate_limiter.ex` as a reference for structuring this policy check component.
    Write tests in `test/mailglass/stream_test.exs` verifying `valid?/1` correctly identifies valid and invalid streams.
  </action>
  <verify>
    <automated>mix test test/mailglass/stream_test.exs</automated>
  </verify>
  <done>Stream atoms are centrally defined, verifiable, and unit tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce stream on Message build and mutate</name>
  <files>lib/mailglass/message.ex, test/mailglass/message_test.exs</files>
  <behavior>
    - `put_stream(%Message{}, :bulk)` returns an updated message
    - `put_stream(%Message{}, :invalid)` raises FunctionClauseError or ArgumentError
    - `new_from_use/2` raises if `:stream` opts is not valid
  </behavior>
  <action>
    Add `put_stream(%__MODULE__{} = msg, stream)` to `Mailglass.Message`.
    Use the `Mailglass.Stream.valid?/1` check (or guard) to restrict `stream` to the 3 allowed atoms.
    Update `new_from_use/2` to enforce the same rule on the extracted `stream` opt (defaulting to `:transactional`). Refer to the core pattern in `lib/mailglass/message.ex` from `10-PATTERNS.md` for `new_from_use/2` stamping.
    Write tests in `test/mailglass/message_test.exs` asserting that valid stream mutations succeed and invalid ones fail appropriately.
  </action>
  <verify>
    <automated>mix test test/mailglass/message_test.exs</automated>
  </verify>
  <done>A Message can only be built or mutated with a valid stream atom, verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module API | Validates adopter input (stream atoms) to prevent invalid state entering the outbound pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Message | mitigate | Enforce `stream` setter guards to reject arbitrary atom injection that could bypass policy stages |
</threat_model>

<verification>
`mix test` passes without breaking existing calls, and new strict setters reject invalid stream atoms.
</verification>

<success_criteria>
`Mailglass.Message.put_stream/2` and `new_from_use/2` strictly enforce `:transactional`, `:operational`, `:bulk`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-stream-policy-implementation/10-01-SUMMARY.md`
</output>