---
phase: 10-stream-policy-implementation
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - credo_checks/stream_policy_consistent.ex
  - test/credo_checks/stream_policy_consistent_test.exs
autonomous: true
requirements: [STREAM-03]

must_haves:
  truths:
    - `mix credo --strict` catches a `:transactional` mailable with tracking enabled
    - `mix credo --strict` catches a mailable with tracking enabled that defaults to transactional (missing stream set)
  artifacts:
    - path: credo_checks/stream_policy_consistent.ex
      provides: Custom credo check logic
    - path: test/credo_checks/stream_policy_consistent_test.exs
      provides: Unit tests for the credo check
  key_links:
    - from: credo_checks/stream_policy_consistent.ex
      to: AST (Macro.traverse)
      via: Finding `use Mailglass.Mailable` opts
---

<objective>
Implement the `StreamPolicyConsistent` custom Credo check (LINT-13).

Purpose: Enforce stream policy rules at compile-time by analyzing Mailable definitions. This catches invalid combinations (like tracking enabled on a `:transactional` stream) and instances where a mailable intended for tracking forgets to explicitly declare a `:bulk` or `:operational` stream (defaulting incorrectly to `:transactional`).
Output: A new `Mailglass.Credo.StreamPolicyConsistent` check that can be wired into `.credo.exs`, complete with tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-stream-policy-implementation/10-PATTERNS.md
@.planning/phases/10-stream-policy-implementation/RESEARCH.md

See `credo_checks/no_tracking_on_auth_stream.ex` for the exact AST traversal pattern to follow.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StreamPolicyConsistent Credo check and tests</name>
  <files>credo_checks/stream_policy_consistent.ex, test/credo_checks/stream_policy_consistent_test.exs</files>
  <behavior>
    - `use Mailglass.Mailable, tracking: [opens: true]` -> raises LINT-13 (missing explicit non-transactional stream)
    - `use Mailglass.Mailable, stream: :transactional, tracking: true` -> raises LINT-13
    - `use Mailglass.Mailable, stream: :bulk, tracking: true` -> OK
  </behavior>
  <action>
    Implement `Mailglass.Credo.StreamPolicyConsistent` following the AST-walking structure in `Mailglass.Credo.NoTrackingOnAuthStream`.
    Target `use Mailglass.Mailable` definitions.
    Rule 1: If `tracking:` opts exist, check if `stream:` opts exist.
    Rule 2: If `stream:` is missing or explicitly `:transactional`, emit a Credo warning issue noting that tracking requires an explicit `:bulk` or `:operational` stream.
    Create `test/credo_checks/stream_policy_consistent_test.exs`. Write tests parsing AST strings to verify that `Mailglass.Credo.StreamPolicyConsistent` correctly flags tracking without explicit stream (defaulting to `:transactional`) or explicit `:transactional` streams, while passing valid tracking + `:bulk` configurations.
  </action>
  <verify>
    <automated>mix test test/credo_checks/stream_policy_consistent_test.exs</automated>
  </verify>
  <done>The credo check module is implemented, identifies rule violations correctly, and is backed by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static Analysis | Lint check prevents non-compliant mailable code from compiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | Credo | mitigate | Prevent PII leak via tracking pixel on auth/transactional emails by enforcing stream separation at compile-time |
</threat_model>

<verification>
File compiles successfully and tests pass confirming correct issue generation.
</verification>

<success_criteria>
`Mailglass.Credo.StreamPolicyConsistent` is built and correctly mimics the AST-traversal pattern of existing checks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-stream-policy-implementation/10-03-SUMMARY.md`
</output>