---
phase: 10-stream-policy-implementation
plan: 05
type: execute
wave: 3
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - test/mailglass/stream_test.exs
autonomous: true
requirements: [STREAM-01, STREAM-02]

must_haves:
  truths:
    - StreamData property test ensures stream stamping and extraction round-trips correctly across random valid/invalid permutations.
  artifacts:
    - path: test/mailglass/stream_test.exs
      provides: StreamData property tests for policy validations
  key_links:
    - from: test/mailglass/stream_test.exs
      to: lib/mailglass/stream.ex
      via: Exhaustive StreamData sequence testing
---

<objective>
Provide comprehensive property tests for Stream Policy Implementation.

Purpose: Guarantee that all invalid configurations, mutations, and deliveries are caught predictably and that valid flows work. Property-based tests guarantee the resilience of the pipeline against odd or randomized data conditions.
Output: Completed test suite demonstrating requirement coverage via StreamData.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-stream-policy-implementation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stream boundary property tests</name>
  <files>test/mailglass/stream_test.exs</files>
  <action>
    Update `test/mailglass/stream_test.exs` to include a `StreamData` property test. 
    Ensure the test generates random but valid combinations of `tenant_id`, `mailable`, and `stream` atoms, verifying that `policy_check/1` correctly allows valid combinations and specifically rejects `%Message{stream: :bulk, mailable: nil}` with `{:error, %StreamPolicyError{}}`. Use generators to ensure robust coverage across many edge cases.
  </action>
  <verify>
    <automated>mix test test/mailglass/stream_test.exs</automated>
  </verify>
  <done>Stream validation tests correctly assert valid/invalid boundaries using StreamData.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing boundary | Ensures test infrastructure adequately verifies the integrity of previous tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Denial of Service | Test Suite | accept | StreamData configurations can slow test execution; accepted as the value of exhaustive permutations offsets minor speed regressions during testing |
</threat_model>

<verification>
`mix test` passes with property tests executing smoothly.
</verification>

<success_criteria>
All stream policy code paths have robust property test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/10-stream-policy-implementation/10-05-SUMMARY.md`
</output>