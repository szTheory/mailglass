---
phase: 13-v0-2-release-ceremony
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md
  - MAINTAINING.md
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
autonomous: true
requirements: [REL-16]

must_haves:
  truths:
    - The protected-ref publish ceremony is explicit, reversible only within documented limits, and aligned with GitHub Environment approval
    - Post-publish smoke verifies both Hex indexing and HexDocs availability for `mailglass` and `mailglass_admin`
    - The maintainer has one canonical action path for manual publish/smoke fallback if release-triggered automation partially fails
  artifacts:
    - path: .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md
      provides: live publish and smoke evidence for the real `0.2.0` cut
    - path: MAINTAINING.md
      provides: final publish-day runbook and fallback steps
    - path: .github/workflows/publish-hex.yml
      provides: protected publish contract and optional manual invocation path
    - path: .github/workflows/post-publish-smoke.yml
      provides: post-publish verification contract
  key_links:
    - from: MAINTAINING.md
      to: .github/workflows/publish-hex.yml
      via: maintainer execution steps and environment approval expectations
    - from: .github/workflows/post-publish-smoke.yml
      to: Hex + HexDocs
      via: index and docs verification for both packages
---

<objective>
Finalize the irreversible release-day publish and smoke ceremony.

Purpose: The last plan should focus only on protected publish execution, post-publish verification, and the exact maintainer fallback path.
Output: Real `0.2.0` publish evidence plus the final publish/smoke workflow contract and runbook.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-v0-2-release-ceremony/13-CONTEXT.md
@.planning/phases/13-v0-2-release-ceremony/RESEARCH.md
@.planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md
@.planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@MAINTAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute or directly monitor the real protected-ref `0.2.0` publish and capture evidence</name>
  <files>.planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md, MAINTAINING.md, .github/workflows/publish-hex.yml</files>
  <action>
    Perform or directly monitor the real `0.2.0` publish ceremony on the final tag/ref using the canonical path decided in `13-04-REHEARSAL.md`.
    Approve the GitHub Environment when required, confirm both package publishes complete (or are skipped only by idempotency after a successful publish), and record the actual tag/ref, publish run URL(s), approver, and package outcomes in `.planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md`.
    Make the runbook explicit about the publish entrypoint, the required GitHub Environment approval, the expected package order, the idempotency guards, and the exact manual fallback if the release event does not produce the intended publish run.
    Align the final runbook wording with the canonical fallback decision captured in `13-04-REHEARSAL.md`.
    Update `publish-hex.yml` only as needed to match the documented contract; keep the existing release-trigger plus `workflow_dispatch` model rather than inventing a separate path.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^Tag: .+" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^Publish run URL: https://github.com/.+/actions/runs/[0-9]+" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^Environment approval: .+" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^mailglass publish result: (published|already-published)$|^mailglass_admin publish result: (published|already-published)$" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "hex-publish|workflow_dispatch" MAINTAINING.md .github/workflows/publish-hex.yml</automated>
  </verify>
  <done>The real `0.2.0` publish ceremony is executed or directly observed, and the maintainer has durable evidence of the tag, run, approval, and package-publish outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify live Hex indexing, live HexDocs, and post-publish smoke for both packages</name>
  <files>.planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md, .github/workflows/post-publish-smoke.yml, MAINTAINING.md</files>
  <action>
    Run or directly monitor the post-publish smoke path for the real release and confirm the success signals required by REL-16: `mix hex.info` for both packages, live HexDocs availability for both packages, and the fresh-host install/boot proof.
    Record the smoke run URL(s), `mix hex.info` results, HexDocs HTTP 200 checks, and final status in `.planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md`.
    If release-trigger ambiguity remains in practice, use the runbook's `workflow_dispatch` fallback against the final tag and record that invocation explicitly.
    Keep the smoke contract boring and observable.
  </action>
  <verify>
    <automated>rg -n "^Smoke run URL: https://github.com/.+/actions/runs/[0-9]+" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^mailglass hex.info: Released:" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^mailglass_admin hex.info: Released:" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^mailglass HexDocs: HTTP 200$" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^mailglass_admin HexDocs: HTTP 200$" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "^Fresh-host smoke: passed$" .planning/phases/13-v0-2-release-ceremony/13-05-PUBLISH-EVIDENCE.md && rg -n "mix hex.info|hexdocs\\.pm/mailglass|hexdocs\\.pm/mailglass_admin|workflow_dispatch" MAINTAINING.md .github/workflows/post-publish-smoke.yml</automated>
  </verify>
  <done>The live release is publicly observable on Hex and HexDocs, and the post-publish smoke path has recorded evidence for both packages and the fresh-host proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publish workflow -> public package availability | Partial publish success can leave one package, Hex index state, or HexDocs state out of sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Denial of Service | package indexing / docs build | mitigate | Explicit waits and smoke verification for Hex and HexDocs on both packages |
| T-13-11 | Repudiation | manual fallback operation | mitigate | Put exact fallback commands and verification points in the maintainer runbook |
</threat_model>

<verification>
The final release evidence file records the real publish run, environment approval, both-package Hex visibility, both-package HexDocs 200s, and fresh-host smoke success for `0.2.0`.
</verification>

<success_criteria>
REL-16 is fully satisfied at release-day execution time with a real protected publish, live smoke verification, and durable release evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/13-v0-2-release-ceremony/13-05-SUMMARY.md`
</output>
