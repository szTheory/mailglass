---
phase: 13-v0-2-release-ceremony
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/upgrade/v0_2_test.exs
  - test/mailglass/install/install_idempotency_test.exs
  - test/mailglass/install/install_first_preview_smoke_test.exs
  - test/fixtures
  - .github/workflows/post-publish-smoke.yml
autonomous: true
requirements: [REL-14]

must_haves:
  truths:
    - Phase 13 proves the narrow but strict adopter contract: fresh host install, happy-path v0.1 upgrade, and one sentinel ambiguous-case warning path
    - Non-ambiguous v0.1 fixture patterns upgrade with zero manual edits
    - Ambiguous cases are not silently rewritten and emit `IO.warn` with migration-guide guidance
  artifacts:
    - path: test/mailglass/upgrade/v0_2_test.exs
      provides: load-bearing codemod fixture coverage for happy-path and ambiguous-case behavior
    - path: test/mailglass/install/install_first_preview_smoke_test.exs
      provides: committed fresh-host preview endpoint boot/respond proof
    - path: test/fixtures
      provides: committed adopter-fixture inputs and expected outputs
    - path: .github/workflows/post-publish-smoke.yml
      provides: alignment between release-day smoke and the committed fresh-host proof
  key_links:
    - from: test/mailglass/upgrade/v0_2_test.exs
      to: lib/mix/tasks/mailglass.upgrade.v0_2.ex
      via: codemod behavior and warning contract
    - from: .github/workflows/post-publish-smoke.yml
      to: committed fixture tests
      via: fresh-host smoke should reuse the same public install/compile/boot expectations
---

<objective>
Prove the adopter walkthrough contract before release day.

Purpose: REL-14 is the main trust gate for `0.2.0`; the release is not credible unless the codemod and install path are proven against committed fixtures, not just described in docs.
Output: Fresh-host proof, happy-path v0.1 upgrade fixture proof, and a sentinel ambiguous-case proof with warning assertions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-v0-2-release-ceremony/13-CONTEXT.md
@.planning/phases/13-v0-2-release-ceremony/RESEARCH.md
@.planning/phases/13-v0-2-release-ceremony/13-PATTERNS.md
@lib/mix/tasks/mailglass.install.ex
@lib/mix/tasks/mailglass.upgrade.v0_2.ex
@test/mailglass/upgrade/v0_2_test.exs
@test/mailglass/install/install_idempotency_test.exs
@.github/workflows/post-publish-smoke.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add committed v0.1 adopter upgrade fixtures and happy-path assertions</name>
  <files>test/mailglass/upgrade/v0_2_test.exs, test/fixtures</files>
  <action>
    Extend the v0.2 upgrade tests with a committed v0.1 adopter fixture that uses the supported setter patterns called out by the roadmap and context.
    Assert the fixture upgrades with zero manual edits for non-ambiguous cases and still compiles or parses cleanly after the codemod output is applied.
    Keep the fixture narrow and explicit; do not attempt a broad compatibility matrix across arbitrary host customizations.
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade/v0_2_test.exs</automated>
  </verify>
  <done>The repo has a committed proof that the supported v0.1 surface upgrades cleanly without manual edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the sentinel ambiguous-case proof and align fresh-host smoke</name>
  <files>test/mailglass/upgrade/v0_2_test.exs, test/mailglass/install/install_idempotency_test.exs, test/mailglass/install/install_first_preview_smoke_test.exs, .github/workflows/post-publish-smoke.yml</files>
  <action>
    Add one sentinel ambiguous-case fixture proving the codemod does not silently rewrite unsupported patterns, emits `IO.warn`, and includes the migration-guide URL explicitly.
    Add or extend a committed repo-local fresh-host smoke proof that exercises install -> compile -> preview boot -> `/dev/mail/` response, using `test/mailglass/install/install_first_preview_smoke_test.exs` or an equally direct local test seam.
    Align the release-day smoke shape with the committed test contract so release-day smoke and local/CI fixture proofs are checking the same install/compile/preview expectations.
    Preserve the narrow release promise: fresh host, happy path, and one ambiguous sentinel.
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade/v0_2_test.exs test/mailglass/install/install_idempotency_test.exs test/mailglass/install/install_first_preview_smoke_test.exs</automated>
  </verify>
  <done>The ambiguous-case warning contract is load-bearing with the migration-guide URL explicitly asserted, and the release-day smoke path is aligned with a committed local preview-boot proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codemod claims -> adopter source tree | Incorrect rewrite or silent ambiguity would damage user code during migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | codemod output | mitigate | Committed fixture tests for happy-path rewrite correctness |
| T-13-04 | Repudiation | ambiguous-case handling | mitigate | Assert `IO.warn` and escape-hatch guidance instead of silent no-op or silent rewrite |
</threat_model>

<verification>
`mix test test/mailglass/upgrade/v0_2_test.exs test/mailglass/install/install_idempotency_test.exs test/mailglass/install/install_first_preview_smoke_test.exs` passes with committed happy-path, ambiguous-case, and fresh-host preview proofs.
</verification>

<success_criteria>
REL-14 is satisfied with a committed proof of the promised migration contract.
</success_criteria>

<output>
After completion, create `.planning/phases/13-v0-2-release-ceremony/13-02-SUMMARY.md`
</output>
