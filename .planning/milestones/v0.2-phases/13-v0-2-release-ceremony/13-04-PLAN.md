---
phase: 13-v0-2-release-ceremony
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-03]
files_modified:
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass_admin-files.expected
  - .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md
  - lib/mix/tasks/mailglass.publish.check.ex
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
  - MAINTAINING.md
autonomous: true
requirements: [REL-16]

must_haves:
  truths:
    - The repo can rehearse the `0.2.0` release cut on the current pinned Release Please v4 path without changing controllers mid-ceremony
    - The stale publish allowlists are updated or intentionally narrowed so prepublish checks represent the real v0.2 package contents
    - Release-trigger ambiguity is resolved either by a tested token path or by an explicit `workflow_dispatch` fallback contract in the runbook
    - Release PR diff review is treated as a required ceremony step because the repo uses custom dep-pin sync behavior
  artifacts:
    - path: .planning/publish/mailglass-files.expected
      provides: current package allowlist for core
    - path: .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md
      provides: deterministic rehearsal evidence, run URLs, trigger-path decision, and fallback result
    - path: .github/workflows/release-please.yml
      provides: rehearsed release PR/tag creation path
    - path: MAINTAINING.md
      provides: canonical rehearsal and fallback contract
  key_links:
    - from: lib/mix/tasks/mailglass.publish.check.ex
      to: .planning/publish/mailglass-files.expected
      via: tarball allowlist and size-budget enforcement
    - from: .github/workflows/release-please.yml
      to: .github/workflows/publish-hex.yml
      via: trigger semantics or explicit manual fallback path
---

<objective>
Rehearse the release mechanics and remove known preflight surprises before the real cut.

Purpose: REL-16 should not first discover stale tarball allowlists or trigger ambiguity during the irreversible publish ceremony.
Output: Updated package allowlists, rehearsed release path, and explicit maintainer fallback documentation for publish/smoke invocation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-v0-2-release-ceremony/13-CONTEXT.md
@.planning/phases/13-v0-2-release-ceremony/RESEARCH.md
@.planning/publish/mailglass-files.expected
@.planning/publish/mailglass_admin-files.expected
@lib/mix/tasks/mailglass.publish.check.ex
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@release-please-config.json
@.release-please-manifest.json
@MAINTAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale publish allowlists and make prepublish checks represent reality</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass_admin-files.expected, lib/mix/tasks/mailglass.publish.check.ex</files>
  <action>
    Resolve the currently observed `mix mailglass.publish.check --package mailglass` failure by updating the expected file lists or package exclusions so the allowlists reflect the actual Phase 9-12 v0.2 package surface.
    Preserve tarball-size and package-file strictness; do not "fix" the failure by broadly weakening the gate.
    Verify whether any admin allowlist updates are also required and keep the prepublish command authoritative for release-day use.
  </action>
  <verify>
    <automated>mix mailglass.publish.check --package mailglass && mix mailglass.publish.check --package mailglass_admin</automated>
  </verify>
  <done>The prepublish gate is current and trustworthy instead of failing on stale expected file lists.</done>
</task>

<task type="auto">
  <name>Task 2: Rehearse Release Please and publish/smoke trigger semantics on the existing v4 path</name>
  <files>.planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml, .github/workflows/post-publish-smoke.yml, MAINTAINING.md</files>
  <action>
    Rehearse the linked-version release flow on the current pinned v4 controller and resolve the ambiguity caused by `secrets.GITHUB_TOKEN` for downstream publish/smoke invocation.
    Either prove the intended trigger chain works as-is, switch to an approved token path that preserves the current ceremony, or explicitly declare `workflow_dispatch` as the canonical fallback in the runbook and workflow comments.
    Make review of the generated release PR diff a required runbook step, and document the fallback path if Release Please creates the release/tag but downstream workflows do not fire.
    Record the rehearsal outcome in `.planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md`, including the exact rehearsal ref/tag, run URLs or screenshots/log references, whether downstream workflows fired automatically, and the final canonical fallback decision for `0.2.0`.
    Keep the v5 upgrade out of scope.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md && rg -n "^Rehearsal ref: .+" .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md && rg -n "^Release Please run URL: https://github.com/.+/actions/runs/[0-9]+" .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md && rg -n "^Publish rehearsal run URL: https://github.com/.+/actions/runs/[0-9]+" .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md && rg -n "^Outcome: (automatic-trigger-confirmed|workflow_dispatch-fallback-required)$" .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md && rg -n "^Fallback decision: (automatic release chain|workflow_dispatch for 0.2.0)$" .planning/phases/13-v0-2-release-ceremony/13-04-REHEARSAL.md && rg -n "workflow_dispatch|fallback|release PR diff" MAINTAINING.md .github/workflows/release-please.yml .github/workflows/publish-hex.yml .github/workflows/post-publish-smoke.yml</automated>
  </verify>
  <done>The release path has a rehearsed, documented invocation story and no unresolved ambiguity about what the maintainer should do if downstream workflows fail to trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release PR/tag creation -> publish/smoke invocation | A partial automation chain can leave the repo in an unclear state near release time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Denial of Service | stale allowlist gate | mitigate | Update expected package contents before release day and keep the gate strict |
| T-13-08 | Tampering | custom linked-version sync | mitigate | Mandatory release PR diff review and rehearsed dep-pin sync verification |
| T-13-09 | Repudiation | workflow-trigger ambiguity | mitigate | Rehearse and document one canonical fallback path instead of improvising at release time |
</threat_model>

<verification>
`mix mailglass.publish.check --package mailglass`, `mix mailglass.publish.check --package mailglass_admin`, and the recorded rehearsal artifact prove the current pinned release path plus canonical fallback contract.
</verification>

<success_criteria>
REL-16 preflight work is complete: package checks are current, release rehearsal is boring, and trigger ambiguity has an explicit answer.
</success_criteria>

<output>
After completion, create `.planning/phases/13-v0-2-release-ceremony/13-04-SUMMARY.md`
</output>
