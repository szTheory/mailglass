---
phase: 13-v0-2-release-ceremony
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - mailglass_admin/CHANGELOG.md
  - MAINTAINING.md
autonomous: true
requirements: [REL-13]

must_haves:
  truths:
    - The `mailglass` `0.2.0` changelog entry leads with a maintainer-written migration front door instead of a raw generated ledger
    - The public entry explicitly covers breaking changes, `mix mailglass.upgrade.v0_2`, minimum dependency matrix, ambiguous-case escape hatch, and rollback
    - `mailglass_admin` gets a shorter coordinated entry that states whether adopters need action beyond the core upgrade
  artifacts:
    - path: CHANGELOG.md
      provides: curated `mailglass` `0.2.0` release narrative plus categorized ledger
    - path: mailglass_admin/CHANGELOG.md
      provides: sibling-package coordinated release note
    - path: MAINTAINING.md
      provides: runbook wording that matches rollback language used in the changelog
  key_links:
    - from: CHANGELOG.md
      to: lib/mix/tasks/mailglass.upgrade.v0_2.ex
      via: exact upgrade contract and ambiguous-case wording
    - from: CHANGELOG.md
      to: guides/upgrading-from-v0_1.md
      via: changelog is the front door; guide is the deep dive
---

<objective>
Write the v0.2 release narrative that an adopter can trust.

Purpose: REL-13 is satisfied only if the changelog acts as the migration front door for `~> 0.2`, not just as a commit dump.
Output: Curated `0.2.0` changelog entries for core and admin plus matching maintainer rollback language.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-v0-2-release-ceremony/13-CONTEXT.md
@.planning/phases/13-v0-2-release-ceremony/RESEARCH.md
@.planning/phases/13-v0-2-release-ceremony/13-PATTERNS.md
@CHANGELOG.md
@mailglass_admin/CHANGELOG.md
@guides/upgrading-from-v0_1.md
@lib/mix/tasks/mailglass.upgrade.v0_2.ex
@MAINTAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the `mailglass` `0.2.0` changelog entry as the migration front door</name>
  <files>CHANGELOG.md</files>
  <action>
    Add the `0.2.0` entry in `CHANGELOG.md` using the repo's curated `0.1.0` narrative pattern, not the `0.1.1` generated-ledger-first pattern.
    Put a short maintainer summary at the top, then explicit sections for breaking changes, exact upgrade path, dependency/runtime floor, ambiguous-case `update_swoosh/2` escape hatch, rollback, and immediate behavior changes adopters will feel.
    Keep the categorized `Added` / `Changed` / `Fixed` ledger under the curated top section instead of replacing it.
    Ensure every migration claim matches the actual behavior of `mix mailglass.upgrade.v0_2`; do not imply automatic rewriting for ambiguous cases.
  </action>
  <verify>
    <automated>rg -n "mix mailglass\\.upgrade\\.v0_2|update_swoosh/2|rollback|breaking" CHANGELOG.md</automated>
  </verify>
  <done>The `mailglass` changelog gives an adopter everything needed to decide whether and how to migrate to `0.2.0`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the coordinated `mailglass_admin` release note and align maintainer rollback wording</name>
  <files>mailglass_admin/CHANGELOG.md, MAINTAINING.md</files>
  <action>
    Add a concise `mailglass_admin` `0.2.0` entry that clearly states it is version-coordinated with `mailglass` core and whether adopters need any standalone admin action.
    Update `MAINTAINING.md` only as needed so rollback and maintainer recovery wording matches the public release story and does not promise cleanup of arbitrary dirty worktrees.
  </action>
  <verify>
    <automated>rg -n "mailglass_admin|coordinated|rollback|workflow_dispatch" mailglass_admin/CHANGELOG.md MAINTAINING.md</automated>
  </verify>
  <done>The sibling package release note is coordinated and the maintainer runbook uses the same rollback contract the public changelog advertises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal upgrade reality -> public release messaging | Overstated changelog claims would create migration risk and user distrust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | release messaging | mitigate | Tie every changelog claim to the actual codemod behavior and published upgrade guide |
| T-13-02 | Repudiation | rollback contract | mitigate | Keep rollback language explicit and maintainer-operational rather than hand-wavy |
</threat_model>

<verification>
The changelog entries and maintainer runbook contain the required migration sections and wording, and the final prepublish checks are deferred to Plan `13-04`, which updates the stale package allowlists before release-day verification.
</verification>

<success_criteria>
REL-13 is satisfied with curated, migration-first release notes for both packages.
</success_criteria>

<output>
After completion, create `.planning/phases/13-v0-2-release-ceremony/13-01-SUMMARY.md`
</output>
