---
phase: 08-release-engineering-hardening
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - .github/workflows/advisory-matrix.yml
  - .github/workflows/ci.yml
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
  - .github/workflows/release-please.yml
  - .github/workflows/dependency-review.yml
  - .github/workflows/actionlint.yml
  - .github/workflows/pr-title.yml
  - .github/workflows/provider-live.yml
  - mix.lock
  - test/installer/install_idempotency_test.exs
autonomous: false
requirements: [REL-06, REL-07, REL-08, REL-09]
must_haves:
  truths:
    - "Advisory Matrix CI completes successfully on Elixir 1.17 (DB setup + 1.17-compat compile fixes)"
    - "The 2 install_idempotency tests are unskipped and pass"
    - "Managed-snippet drift detection in install_idempotency tests catches installer-output divergence"
    - "All 6 closed Dependabot PRs are re-tested against current mix.lock and merged"
    - "All third-party GitHub Actions SHA pins are refreshed for 2026-Q2"
    - "Dependabot still watches both mix.lock and .github/workflows/ (CI-06 maintenance)"
  artifacts:
    - path: ".github/workflows/advisory-matrix.yml"
      provides: "Advisory matrix with Postgres service + 1.17-compat compile fixes"
      contains: "postgres:"
    - path: ".github/workflows/ci.yml"
      provides: "Refreshed 2026-Q2 SHA pins for all third-party uses:"
      contains: "uses:"
    - path: "test/installer/install_idempotency_test.exs"
      provides: "Unskipped idempotency tests + drift-detection assertions"
      contains: "describe"
  key_links:
    - from: "advisory-matrix.yml services block"
      to: "Postgres health check + ecto.create"
      via: "services.postgres + wait-for-postgres step"
      pattern: "pg_isready"
    - from: "Dependabot PR re-batch"
      to: "merged to main"
      via: "gh pr merge --squash after re-test"
      pattern: "dependabot"
---

<objective>
Four debt items that all touch `.github/workflows/*.yml`:

- REL-06: Fix Advisory Matrix CI — DB setup (Postgres service) + Elixir 1.17 compile failures.
- REL-07: Unskip the 2 install_idempotency tests + add managed-snippet drift detection.
- REL-08: Re-batch and merge the 6 closed Dependabot PRs (setup-beam, checkout, cache, sigra, dependency-review-action, actionlint).
- REL-09: Refresh ALL third-party GitHub Actions SHA pins for 2026-Q2.

This plan has a checkpoint task (Dependabot PR re-batch) — the user merges the 6 closed PRs after Claude validates locally. autonomous: false.

Purpose: Close v0.1.2 dependency / advisory-CI debt before Phase 8's gates flip in 08-05 and 08-06.

Output: Updated workflow YAMLs, unskipped install_idempotency tests, 6 merged Dependabot PRs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-engineering-hardening/08-CONTEXT.md
@.planning/phases/08-release-engineering-hardening/08-PATTERNS.md
@./CLAUDE.md
@.github/workflows/ci.yml
@.github/workflows/advisory-matrix.yml

<interfaces>
DB-setup pattern from ci.yml (08-PATTERNS.md §advisory-matrix.yml lines 905-934):

```
services:
  postgres:
    image: postgres:16-alpine
    env:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - 5432:5432
    options: >-
      --health-cmd pg_isready
      --health-interval 10s
      --health-timeout 5s
      --health-retries 5
env:
  MIX_ENV: test
  POSTGRES_HOST: localhost
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
```

Wait-for-postgres + create-DB step:
```
- name: Wait for postgres + create test DB
  env:
    PGPASSWORD: postgres
  run: |
    until pg_isready -h localhost -U postgres; do sleep 1; done
    mix ecto.create -r Mailglass.TestRepo --quiet
```

The 6 closed Dependabot PRs to re-batch (REL-08 + REQUIREMENTS.md REL-08):
- setup-beam, actions/checkout, actions/cache, sigra, actions/dependency-review-action, rhysd/actionlint-go

Per CLAUDE.md "All third-party GitHub Actions pinned to commit SHA":
```
uses: actions/checkout@<40-char-sha>  # v4.x
```
The trailing comment with the human-readable tag is mandatory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix advisory-matrix.yml — Postgres service + 1.17 compile-compat guards (REL-06)</name>
  <files>.github/workflows/advisory-matrix.yml, .github/workflows/provider-live.yml</files>
  <read_first>
    - .github/workflows/advisory-matrix.yml (entire file)
    - .github/workflows/provider-live.yml (entire file)
    - .github/workflows/ci.yml (find the existing DB-setup block to mirror, ~lines 116-156)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md REL-06
    - .planning/phases/08-release-engineering-hardening/08-PATTERNS.md §advisory-matrix.yml (lines 901-936)
    - .planning/todos/pending/2026-04-26-advisory-matrix-failing-db-setup-and-1-17-compat.md
    - Most-recent failing advisory-matrix run logs: `gh run list --workflow=advisory-matrix.yml --status=failure --limit 5` then `gh run view <id> --log` to extract the exact 1.17 compile error
  </read_first>
  <action>
    Part A — Add Postgres service block to every job in `advisory-matrix.yml` that runs tests. Use the exact YAML shown in the `<interfaces>` block above (services + env + wait-for-postgres step). Place the wait-for-postgres step BEFORE the `mix test` step.

    Part B — Fix the Elixir 1.17 compile failure. Use `gh run view <id> --log` to find the exact module + function. Likely candidates:
    - 1.18-only stdlib call (e.g., `Macro.Env.lookup_aliases/1`) → guard with `if function_exported?(Macro.Env, :lookup_aliases, 1) do ... else ... end`
    - 1.18-only macro feature → branch on `Version.match?(System.version(), ">= 1.18.0")` at compile time
    - New sigil/syntax 1.17 cannot parse → reformat or guard

    The 1.18+ behavior MUST be unchanged. Add a comment at each guard site:
    `# Elixir 1.17 compat — REL-06 advisory-matrix support. Remove when 1.17 drops out.`

    Part C — Apply the same DB-setup fix to `provider-live.yml` IF it has the same gap. Read the file first; only edit if needed. provider-live is advisory-only (nightly cron, never blocks PRs).

    DO NOT modify the matrix entries (Elixir/OTP versions). DO NOT touch SHA pins in this task — that's Task 4.
  </action>
  <verify>
    <automated>actionlint .github/workflows/advisory-matrix.yml && actionlint .github/workflows/provider-live.yml && grep -q "postgres:16-alpine" .github/workflows/advisory-matrix.yml && grep -q "pg_isready" .github/workflows/advisory-matrix.yml && grep -q "mix ecto.create" .github/workflows/advisory-matrix.yml
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/advisory-matrix.yml` exits 0
    - `actionlint .github/workflows/provider-live.yml` exits 0
    - `grep -c "postgres:16-alpine" .github/workflows/advisory-matrix.yml` >= 1
    - `grep -c "pg_isready" .github/workflows/advisory-matrix.yml` >= 1
    - `grep -c "mix ecto.create" .github/workflows/advisory-matrix.yml` >= 1
    - Every Elixir 1.17 compat guard site has a `# Elixir 1.17 compat` comment
    - Triggering advisory-matrix manually via `gh workflow run advisory-matrix.yml` produces a green run on the 1.17 row (verify after merge)
  </acceptance_criteria>
  <done>
    advisory-matrix.yml runs cleanly on Elixir 1.17 with Postgres service available; provider-live.yml is also fixed if needed; actionlint passes both files; 1.17 compat guards are annotated with the required comment for future removal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unskip install_idempotency tests + add managed-snippet drift detection (REL-07)</name>
  <files>test/installer/install_idempotency_test.exs</files>
  <read_first>
    - test/installer/install_idempotency_test.exs (entire file — find the 2 `@tag :skip` annotations)
    - test/installer/ (sibling files — find the manifest snapshot files; usually in `test/installer/snapshots/` or `test/fixtures/installer/`)
    - lib/mailglass/installer/ (the installer modules — understand what "managed snippet" means)
    - .planning/todos/pending/ (search for any installer-related drift TODO if present)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md REL-07
  </read_first>
  <action>
    Part A — Locate the 2 skipped tests. Run:
    ```
    grep -n "@tag :skip\|@moduletag :skip" test/installer/install_idempotency_test.exs
    ```

    For each skipped test, read the `describe`/`test` block to understand what it verifies. The two skips were placed during v0.1 ship because they were either flaky or asserted state the installer didn't yet stabilize. Validate the test logic is still relevant; if so, remove the `@tag :skip` line.

    Part B — Add managed-snippet drift detection. The "managed snippet" is the marker-block comment the installer writes into adopter files (typically `# BEGIN MAILGLASS MANAGED ... # END MAILGLASS MANAGED`). The drift test should:

    1. Run the installer against a fixture host project
    2. Read the generated marker block
    3. Compare against a checked-in expected snippet (golden file)
    4. Fail with a brand-voice message if they diverge:
       `"Managed snippet drifted from snapshot. Run `mix mailglass.install --update-goldens` (or equivalent) to refresh, or fix the installer template."`

    If the existing skipped test already implements this comparison, just unskip. If it doesn't, add a new `describe "managed-snippet drift detection"` block following the existing test patterns in the same file.

    DO NOT modify the installer source itself — REL-07 is a test-only change. If the existing installer output already drifts from the manifest, that's a real bug to be filed, NOT silently re-snapshot.

    Part C — Run `mix test test/installer/install_idempotency_test.exs` locally (5 random-seed runs minimum to confirm non-flakiness). All tests must pass.
  </action>
  <verify>
    <automated>! grep -E "^\s*@tag :skip|^\s*@moduletag :skip" test/installer/install_idempotency_test.exs && for i in 1 2 3 4 5; do mix test test/installer/install_idempotency_test.exs --seed $RANDOM || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*@tag :skip|^\s*@moduletag :skip" test/installer/install_idempotency_test.exs` == 0
    - 5 consecutive `mix test test/installer/install_idempotency_test.exs --seed <random>` runs all pass
    - At least one test in the file asserts managed-snippet drift detection (grep for "drift" or "snapshot" or "managed snippet" in the test names)
    - Negative test (manual): introduce a one-byte change to the managed-snippet template; rerun the test; it should FAIL with a brand-voice "drifted" message. Revert before commit.
  </acceptance_criteria>
  <done>
    Both previously-skipped install_idempotency tests run and pass (5 consecutive random-seed runs); managed-snippet drift detection is in place; a synthetic drift triggers a clear failure message.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh all third-party GitHub Actions SHA pins for 2026-Q2 (REL-09)</name>
  <files>.github/workflows/ci.yml, .github/workflows/publish-hex.yml, .github/workflows/post-publish-smoke.yml, .github/workflows/release-please.yml, .github/workflows/dependency-review.yml, .github/workflows/actionlint.yml, .github/workflows/pr-title.yml, .github/workflows/advisory-matrix.yml, .github/workflows/provider-live.yml</files>
  <read_first>
    - All files in `files_modified` (every `.github/workflows/*.yml` file — enumerate every `uses:` line)
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md REL-09 + D-08-26 (release-please-action SHA stays pinned at v4.4.1 — DO NOT bump to v5.0.0)
    - ./CLAUDE.md "All third-party GitHub Actions pinned to commit SHA"
    - https://github.com/<owner>/<repo>/releases for each action — find the latest 2026-Q2 release
  </read_first>
  <action>
    For each `uses: <owner>/<repo>@<ref>` line across ALL workflow files:

    1. Identify the action's latest 2026-Q2 release (look up via `gh api repos/<owner>/<repo>/releases?per_page=20` or browse releases page).
    2. Resolve the release tag to its commit SHA: `gh api repos/<owner>/<repo>/git/ref/tags/<tag>` returns `object.sha`. If the tag is annotated, follow with `gh api repos/<owner>/<repo>/git/tags/<sha>` to get the commit SHA.
    3. Update the `uses:` line to `uses: <owner>/<repo>@<40-char-sha>  # <tag>` format. The trailing comment with the human-readable tag is MANDATORY (CLAUDE.md).

    EXCEPTIONS:
    - `googleapis/release-please-action` — KEEP pinned at the v4.4.1 SHA `5c625bfb5d1ff62eadeeb3772007f7f66fdcf071` per D-08-26. v5.0.0 upgrade is deferred to Phase 13. Add `# v4.4.1 — DO NOT bump to v5 until Phase 13 release ceremony (per .planning/phases/08-release-engineering-hardening/08-CONTEXT.md D-08-26)` as a comment above this `uses:` line.

    The 6 Dependabot-tracked actions take precedence (Task 4 will batch-merge their PRs); for those, use whatever SHAs Dependabot recommends in the PR body if newer than what you find via `gh api`.

    DO NOT bump non-third-party `uses:` lines (e.g., local composite actions). Only third-party actions get the SHA refresh.

    DO NOT modify the action's input parameters (`with:` blocks) — that's a behavior change, not a SHA refresh. If a 2026-Q2 release introduces a breaking change requiring `with:` updates, flag it in the SUMMARY and STOP — open a separate spike.
  </action>
  <verify>
    <automated>for f in .github/workflows/*.yml; do actionlint "$f" || exit 1; done && ! grep -E '^\s*uses: [^@]+@[a-zA-Z0-9._-]+\s*$' .github/workflows/*.yml | grep -vE '#\s*v?[0-9]' && grep -q "5c625bfb5d1ff62eadeeb3772007f7f66fdcf071" .github/workflows/release-please.yml</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint` exits 0 on every workflow file
    - Every third-party `uses:` line has the form `uses: <owner>/<repo>@<40-char-sha>  # <tag>` (40-hex SHA + trailing tag comment)
    - `grep -c "5c625bfb5d1ff62eadeeb3772007f7f66fdcf071" .github/workflows/release-please.yml` >= 1 (release-please-action SHA unchanged per D-08-26)
    - Triggering ci.yml manually after the SHA refresh still produces a green run
    - No `with:` block was modified (SHA refresh only — no behavior changes)
  </acceptance_criteria>
  <done>
    All third-party `uses:` lines pinned to fresh 2026-Q2 SHAs with trailing tag comments; release-please-action stays at v4.4.1; actionlint passes every file; ci.yml still produces green runs after the refresh.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Re-batch + merge the 6 closed Dependabot PRs (REL-08)</name>
  <files>mix.lock</files>
  <read_first>
    - .planning/phases/08-release-engineering-hardening/08-CONTEXT.md REL-08
    - The 6 closed Dependabot PRs (use `gh pr list --state=closed --author=app/dependabot --limit 30` to find them)
  </read_first>
  <action>
    Claude pre-flight (automated):

    1. Identify the 6 closed Dependabot PRs:
       `gh pr list --state=closed --author=app/dependabot --limit 30 --json number,title,headRefName | jq '.[] | select(.title | test("setup-beam|checkout|cache|sigra|dependency-review|actionlint"))'`
    2. For each PR (one at a time):
       - `gh pr reopen <num>`
       - `gh pr checkout <num>`
       - `git rebase main`
       - `mix deps.get && mix compile --warnings-as-errors && mix test`
       - If green: `git push --force-with-lease`
       - Wait for CI: `gh pr checks <num> --watch`
       - If CI green: leave PR open in "ready" state for the user to merge.
       - If CI fails: comment on the PR with the failure summary; mark as blocked in the SUMMARY.

    User action required (szTheory-only — push to main / merge):

    1. For each of the 6 PRs that Claude marked "ready": `gh pr merge <num> --squash --delete-branch`
    2. After all merges, run `mix deps.get && mix test` locally on `main` to confirm no regression.
    3. Confirm `mix.lock` post-merge matches the SHA refresh from Task 3 (no drift).

    Brand voice: any failure summary follows "Delivery blocked: <specific reason>" pattern.

    What-built recap: Tasks 1-3 already shipped (advisory-matrix fix, idempotency tests unskipped, SHA pins refreshed). Task 4 closes REL-08 by re-batching the 6 stale Dependabot PRs.
  </action>
  <verify>
    <automated>gh pr list --state=closed --author=app/dependabot --limit 30 --json number,mergedAt,title | jq -e '[.[] | select(.title | test("setup-beam|checkout|cache|sigra|dependency-review|actionlint")) | select(.mergedAt != null)] | length >= 6'</automated>
  </verify>
  <how-to-verify>
    Verification checklist for the user:

    1. `gh pr list --state=merged --author=app/dependabot --limit 30` shows all 6 PRs as merged (not just closed)
    2. `git log --oneline main -20` shows the 6 squash-merge commits
    3. `mix deps.get && mix test` on local `main` exits 0
    4. `mix.lock` SHA changes are consistent with Task 3 workflow SHA pins (no drift)
  </how-to-verify>
  <resume-signal>Type "merged" once all 6 PRs are merged + main is green; or "blocked: <pr-num> <reason>" if a specific PR cannot be merged.</resume-signal>
  <done>
    All 6 stale Dependabot PRs are merged to main; `gh pr list --state=merged --author=app/dependabot` shows them merged (not just closed); `mix test` on main passes; `mix.lock` post-merge is consistent with Task 3 workflow SHA pins.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot PR -> main | Dependabot is a trusted bot; PRs go through normal CI; auto-merge requires required checks to pass. |
| Third-party action @SHA -> CI runtime | SHA pinning is the trust mechanism; CLAUDE.md requires every `uses:` to be SHA-pinned with trailing tag comment. |
| advisory-matrix Postgres service -> mix ecto.create | Service is local to the runner; credentials are postgres/postgres; ephemeral (per-run lifecycle). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Third-party action publishes a malicious release | mitigate | SHA-pinning prevents the workflow from picking up unreviewed updates; Dependabot watches the lockfile and opens PRs. |
| T-08-04-02 | Elevation of privilege | release-please-action v5.0.0 introduces Node 24 runtime | mitigate | Pin stays at v4.4.1 SHA per D-08-26; v5 upgrade deferred to Phase 13 release ceremony. |
| T-08-04-03 | Tampering | Reopened Dependabot PR has merge-conflict resolution that introduces unwanted changes | mitigate | Each PR is rebased + force-pushed before merge; CI re-runs against the rebased state; only merge after green. |
| T-08-04-04 | Information disclosure | advisory-matrix logs leak Postgres credentials | accept | Credentials are ephemeral per-run; `postgres/postgres` is the documented dev-default; never used in prod. |
| T-08-04-05 | Tampering | install_idempotency drift detection silently re-snapshots a real bug | mitigate | The drift test FAILS on divergence (does not auto-snapshot); user must explicitly run the snapshot-refresh path. |
</threat_model>

<verification>
- `for f in .github/workflows/*.yml; do actionlint "$f"; done` exits 0 for every file.
- Triggering advisory-matrix manually produces a green run including the Elixir 1.17 row.
- `mix test test/installer/install_idempotency_test.exs --seed <random>` passes 5 times in a row.
- All 6 Dependabot PRs are merged to main; `gh pr list --state=closed --author=app/dependabot` shows them as merged (not just closed).
- `grep -c "5c625bfb5d1ff62eadeeb3772007f7f66fdcf071" .github/workflows/release-please.yml` >= 1 (v4.4.1 pin retained).
</verification>

<success_criteria>
- advisory-matrix.yml + provider-live.yml run cleanly with Postgres service + 1.17 compat guards.
- The 2 install_idempotency tests are unskipped, pass 5 random-seed runs in a row, and assert managed-snippet drift detection.
- All 6 closed Dependabot PRs are merged to main after Claude rebase + green CI.
- All third-party `uses:` lines have fresh 2026-Q2 SHA pins with trailing tag comments.
- release-please-action remains at v4.4.1 SHA per D-08-26.
- Dependabot still watches both `mix.lock` and `.github/workflows/`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-engineering-hardening/08-04-SUMMARY.md`. Include:
- The exact 1.17 compile-compat guard sites (file + line + library function)
- A table of all third-party `uses:` lines with `before SHA` -> `after SHA` (and the corresponding tag)
- Confirmation that the release-please-action SHA is unchanged (D-08-26 deferred to Phase 13)
- The 6 Dependabot PR numbers + merge commit SHAs
- Confirmation that `mix.lock` post-merge matches what Task 3 committed (no drift)
</output>
