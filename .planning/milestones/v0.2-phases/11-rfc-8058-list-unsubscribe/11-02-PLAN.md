---
phase: 11-rfc-8058-list-unsubscribe
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - lib/mailglass/compliance.ex
  - credo_checks/require_atomic_unsubscribe_headers.ex
  - .credo.exs
  - test/mailglass/compliance_test.exs
  - test/credo_checks/require_atomic_unsubscribe_headers_test.exs
autonomous: true
requirements: [UNSUB-02]

must_haves:
  truths:
    - Bulk messages always receive both unsubscribe headers together or neither header at all
    - Transactional messages never receive unsubscribe headers
    - The outbound pipeline has one message-aware compliance entrypoint that preserves stream context until header injection is complete
  artifacts:
    - path: lib/mailglass/compliance.ex
      provides: message-aware compliance wrapper plus `inject_unsubscribe_headers/2` as the sole unsubscribe header writer
    - path: credo_checks/require_atomic_unsubscribe_headers.ex
      provides: lint enforcement against non-atomic or ad hoc unsubscribe header writes
  key_links:
    - from: lib/mailglass/compliance.ex
      to: lib/mailglass/compliance/unsubscribe.ex
      via: URL generation and atomic header injection
    - from: credo_checks/require_atomic_unsubscribe_headers.ex
      to: lib/mailglass/compliance.ex
      via: AST rule that only the dedicated injection path may write `List-Unsubscribe` headers
---

<objective>
Add the atomic, stream-aware unsubscribe header path without overloading the existing `%Swoosh.Email{}`-only compliance primitive.

Purpose: Resolve the current seam in `Mailglass.Compliance` explicitly: generic RFC headers stay email-level, while unsubscribe injection moves through a message-aware wrapper that can inspect stream and operational opt-in state.
Output: Updated compliance pipeline, dedicated atomic injection function, custom Credo check, and unit/lint coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-rfc-8058-list-unsubscribe/11-CONTEXT.md
@.planning/phases/11-rfc-8058-list-unsubscribe/RESEARCH.md
@.planning/phases/11-rfc-8058-list-unsubscribe/11-PATTERNS.md
@lib/mailglass/compliance.ex
@lib/mailglass/message.ex
@lib/mailglass/compliance/unsubscribe.ex

<interfaces>
From lib/mailglass/compliance.ex:
```elixir
@spec add_rfc_required_headers(Swoosh.Email.t()) :: Swoosh.Email.t()
@spec maybe_add_feedback_id(Mailglass.Message.t()) :: Mailglass.Message.t()
```

From lib/mailglass/message.ex:
```elixir
@type stream :: :transactional | :operational | :bulk
def update_swoosh(message, fun)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce the message-aware compliance wrapper and atomic injection path</name>
  <files>lib/mailglass/compliance.ex, test/mailglass/compliance_test.exs</files>
  <behavior>
    - `%Mailglass.Message{stream: :bulk}` gets both `List-Unsubscribe` and `List-Unsubscribe-Post`
    - `%Mailglass.Message{stream: :transactional}` gets neither unsubscribe header
    - `:operational` only injects when the mailable/config explicitly opts in
    - Existing `add_rfc_required_headers/1` remains the `%Swoosh.Email{}`-only primitive for generic RFC headers
  </behavior>
  <action>
    Extend `Mailglass.Compliance` by introducing a new message-aware entrypoint for outbound pipeline use instead of forcing stream-dependent logic into `add_rfc_required_headers/1`.
    Keep `add_rfc_required_headers/1` focused on the generic RFC headers it already owns, then apply feedback-id and unsubscribe logic on `%Mailglass.Message{}` after stream context is available.
    Implement `inject_unsubscribe_headers/2` as the only code path allowed to set either unsubscribe header, and make it atomic: it must write both headers to the inner `%Swoosh.Email{}` together or write neither.
    Preserve `put_header_if_absent` semantics so adopters can still pre-set the pair intentionally, but do not permit any secondary code path to set only one header.
  </action>
  <verify>
    <automated>mix test test/mailglass/compliance_test.exs</automated>
  </verify>
  <done>The compliance pipeline has a single message-aware unsubscribe seam and produces correct stream-conditional headers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce atomic unsubscribe headers with a custom Credo check</name>
  <files>credo_checks/require_atomic_unsubscribe_headers.ex, .credo.exs, test/credo_checks/require_atomic_unsubscribe_headers_test.exs</files>
  <behavior>
    - Direct writes of `List-Unsubscribe` or `List-Unsubscribe-Post` outside `inject_unsubscribe_headers/2` are flagged
    - AST paths that set one header without the other are flagged
    - The dedicated compliance injection function is accepted
  </behavior>
  <action>
    Add `Mailglass.Credo.RequireAtomicUnsubscribeHeaders` following the repo's existing stateful AST traversal pattern.
    Register it in `.credo.exs`.
    Detect both string-literal header names and common helper-call shapes so the rule catches header-by-header writes in library code.
    Scope the check to prevent false positives in tests that assert on headers rather than mutating them.
  </action>
  <verify>
    <automated>mix test test/credo_checks/require_atomic_unsubscribe_headers_test.exs && mix credo --strict</automated>
  </verify>
  <done>Lint fails whenever unsubscribe headers are set outside the one atomic injection path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Message struct -> compliance injection | Stream, mailable, and operational opt-in flags cross into the header mutation seam |
| Source code -> lint gate | Credo must block accidental half-configured RFC 8058 implementations before runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | `Mailglass.Compliance` | mitigate | Keep unsubscribe header writes behind one message-aware function that owns both headers together |
| T-11-05 | Repudiation | compliance call sites | mitigate | Add a custom Credo rule so future edits cannot silently bypass the atomic injection seam |
| T-11-06 | Information Disclosure | transactional messages | mitigate | Explicitly branch on `message.stream` so `:transactional` mail never advertises an unsubscribe flow |
</threat_model>

<verification>
`mix test test/mailglass/compliance_test.exs`, `mix test test/credo_checks/require_atomic_unsubscribe_headers_test.exs`, and `mix credo --strict` all pass.
</verification>

<success_criteria>
`Mailglass.Compliance` has one message-aware unsubscribe injection path, and the codebase cannot drift back to ad hoc header mutation without failing lint.
</success_criteria>

<output>
After completion, create `.planning/phases/11-rfc-8058-list-unsubscribe/11-02-SUMMARY.md`
</output>
