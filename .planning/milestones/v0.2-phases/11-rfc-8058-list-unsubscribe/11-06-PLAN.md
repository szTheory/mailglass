---
phase: 11-rfc-8058-list-unsubscribe
plan: 06
type: tdd
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - test/mailglass/properties/unsubscribe_property_test.exs
  - test/mailglass/properties/unsubscribe_post_idempotency_property_test.exs
autonomous: true
requirements: [UNSUB-05]

must_haves:
  truths:
    - Rotation, expiry, replay, and stream-conditional header behavior hold across generated inputs rather than only single examples
    - Unsubscribe URLs and token flows do not create open-redirect or SSRF seams
    - Replayed POST requests converge to the same durable state as a single POST
  artifacts:
    - path: test/mailglass/properties/unsubscribe_property_test.exs
      provides: token rotation, expiry, URL security, and header presence property coverage
    - path: test/mailglass/properties/unsubscribe_post_idempotency_property_test.exs
      provides: POST replay convergence property coverage
  key_links:
    - from: test/mailglass/properties/unsubscribe_property_test.exs
      to: lib/mailglass/compliance/unsubscribe.ex
      via: generated mint/verify/url/header assertions
    - from: test/mailglass/properties/unsubscribe_post_idempotency_property_test.exs
      to: lib/mailglass/compliance/unsubscribe_controller.ex
      via: generated replay requests against the mounted POST endpoint
---

<objective>
Lock the unsubscribe flow behind StreamData properties instead of one-off example tests.

Purpose: RFC 8058 edge cases are exactly the kind of stateful, replay-heavy behavior where property tests provide the most leverage. This plan is isolated as TDD so red/green cycles stay focused on the highest-risk invariants.
Output: Property suites for rotation, expiry, URL safety, stream-conditional headers, and POST replay convergence.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-rfc-8058-list-unsubscribe/RESEARCH.md
@test/mailglass/tracking/token_rotation_test.exs
@test/mailglass/tracking/open_redirect_test.exs
@test/mailglass/properties/webhook_idempotency_convergence_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED/GREEN token, URL, and header property suite</name>
  <files>test/mailglass/properties/unsubscribe_property_test.exs</files>
  <action>
    Write failing StreamData properties first for:
    1. mint -> verify succeeds across current-secret rotation when the old raw secret remains in `previous_secrets`
    2. expired tokens fail with the structured outcome expected by the controller
    3. generated unsubscribe URLs never accept unsafe redirect targets and fail before 900-byte overflow
    4. `:bulk` always yields both unsubscribe headers, `:transactional` yields neither, and `:operational` respects opt-in
    Then drive the production code to green without widening payload shape beyond `delivery_id`.
  </action>
  <verify>
    <automated>mix test test/mailglass/properties/unsubscribe_property_test.exs</automated>
  </verify>
  <done>Generated inputs prove the core unsubscribe service and atomic header logic honor rotation, expiry, URL safety, and stream rules.</done>
</task>

<task type="auto">
  <name>Task 2: RED/GREEN POST replay convergence property suite</name>
  <files>test/mailglass/properties/unsubscribe_post_idempotency_property_test.exs</files>
  <action>
    Write a failing property first that mounts the unsubscribe router/controller in a test router, generates replay counts for valid unsubscribe POST requests, and proves N POSTs converge to the same durable state as 1 POST.
    Follow the sandbox and truncation discipline from `test/mailglass/properties/webhook_idempotency_convergence_test.exs`.
    Assert HTTP 200 on first-write and replay paths and exactly one durable `:unsubscribed` event per delivery idempotency key.
  </action>
  <verify>
    <automated>mix test test/mailglass/properties/unsubscribe_post_idempotency_property_test.exs</automated>
  </verify>
  <done>Replay-heavy generated inputs prove one-click POST is structurally idempotent and convergence-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated inputs -> token and URL services | Property tests explore attacker-shaped tokens, oversized identifiers, and replay counts |
| Generated POST sequences -> controller and event ledger | Replay behavior must converge under untrusted repetition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering | token verification | mitigate | Property-test rotation, tamper, and expiry edges instead of relying on one example per branch |
| T-11-17 | Denial of Service | POST replay path | mitigate | Property-test replay convergence so repeated mailbox-provider POSTs stay 200 and idempotent |
| T-11-18 | Information Disclosure | URL builder | mitigate | Property-test URL-safety constraints to keep unsafe redirect or oversized-link behavior from shipping |
</threat_model>

<verification>
`mix test test/mailglass/properties/unsubscribe_property_test.exs` and `mix test test/mailglass/properties/unsubscribe_post_idempotency_property_test.exs` both pass.
</verification>

<success_criteria>
UNSUB-05 is covered by generative tests that prove rotation, expiry, URL safety, stream gating, and replay convergence across broad input sets.
</success_criteria>

<output>
After completion, create `.planning/phases/11-rfc-8058-list-unsubscribe/11-06-SUMMARY.md`
</output>
