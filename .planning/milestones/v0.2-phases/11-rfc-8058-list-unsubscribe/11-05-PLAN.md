---
phase: 11-rfc-8058-list-unsubscribe
plan: 05
type: execute
wave: 4
depends_on: [11-01, 11-04]
files_modified:
  - lib/mix/tasks/mailglass.gen.unsubscribe.ex
  - test/mix/tasks/mailglass.gen.unsubscribe_test.exs
autonomous: true
requirements: [UNSUB-04]

must_haves:
  truths:
    - `mix mailglass.gen.unsubscribe` prints a complete install checklist and copies zero files
    - The printed router contract is exactly `import Mailglass.Router` plus `mailglass_router_routes "/mailglass"`, producing `/mailglass/unsubscribe/:token`
    - The generator reports path and router prerequisites using the same unsubscribe config and collision rules the runtime mount uses
    - Running the task does not mutate adopter source code or generate scaffolding
  artifacts:
    - path: lib/mix/tasks/mailglass.gen.unsubscribe.ex
      provides: read-only generator task with strict CLI parsing and checklist output
    - path: test/mix/tasks/mailglass.gen.unsubscribe_test.exs
      provides: zero-write and output-contract coverage
  key_links:
    - from: lib/mix/tasks/mailglass.gen.unsubscribe.ex
      to: lib/mailglass/router.ex
      via: reuse of the mount-path and collision validation story
    - from: lib/mix/tasks/mailglass.gen.unsubscribe.ex
      to: lib/mailglass/config.ex
      via: printed config snippets and mount-path instructions
---

<objective>
Deliver the no-copy unsubscribe generator task as a read-only installation checklist.

Purpose: The phase context explicitly rejects code generation here. The task should help adopters wire the feature correctly while preserving the repo's no-copy rule and surfacing mount-path/collision issues early.
Output: `mix mailglass.gen.unsubscribe` plus tests proving it is read-only and instruction-complete.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-rfc-8058-list-unsubscribe/11-CONTEXT.md
@lib/mix/tasks/mailglass.install.ex
@lib/mailglass/router.ex
@lib/mailglass/config.ex

<interfaces>
From lib/mix/tasks/mailglass.install.ex:
```elixir
{opts, rest, invalid} = OptionParser.parse(...)
validate_cli!(rest, invalid)
Mix.shell().info(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the read-only unsubscribe checklist task</name>
  <files>lib/mix/tasks/mailglass.gen.unsubscribe.ex, test/mix/tasks/mailglass.gen.unsubscribe_test.exs</files>
  <behavior>
    - Running the task prints config snippets, router macro usage, and a UAT recipe
    - Running the task creates or modifies zero files
    - Unknown options or positional args fail loudly
  </behavior>
  <action>
    Create `Mix.Tasks.Mailglass.Gen.Unsubscribe` using the same strict CLI parsing style as `Mix.Tasks.Mailglass.Install`.
    Print sections for compliance config, the canonical `mailglass_router_routes "/mailglass"` mount, GET/POST UAT, and DKIM verification follow-up.
    Do not call installer or file-write helpers; this task must be terminal output only, per the user's hard constraint and locked generator decision.
    Include the exact macro and config snippets that match 11-01 and 11-04 so adopters are not handed divergent instructions, including the default `/mailglass/unsubscribe/:token` route shape.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.gen.unsubscribe_test.exs --only output_contract</automated>
  </verify>
  <done>The generator task is instruction-only, strict on CLI misuse, and aligned with the actual runtime config and mount contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove zero-write behavior and route preflight warnings</name>
  <files>test/mix/tasks/mailglass.gen.unsubscribe_test.exs</files>
  <behavior>
    - The task never writes files to disk
    - The task surfaces route-collision warnings or guidance using the same path story as the router macro
    - Output includes a concrete POST replay check and GET confirmation-page check
  </behavior>
  <action>
    Add task tests that capture shell output and assert the absence of any created files or installer operations.
    Where feasible, reuse the router collision helper or route-reflection approach so the printed guidance matches actual mount behavior rather than a string-only heuristic.
    Assert the output includes a browser GET check, one-click POST check, replay POST check, and reminder that the task intentionally copies zero files.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.gen.unsubscribe_test.exs</automated>
  </verify>
  <done>The generator's instruction contract and zero-write invariant are enforced by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer CLI input -> generator task | Untrusted CLI flags and router assumptions enter a task that prints install guidance |
| Generator output -> host app wiring | Incorrect instructions would create broken unsubscribe mounts or unsafe assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering | mix task behavior | mitigate | Keep the task read-only and fail on unknown args so it cannot drift into silent code generation |
| T-11-14 | Repudiation | printed setup guidance | mitigate | Generate instructions from the same config and router contract used by runtime code |
| T-11-15 | Denial of Service | collision guidance | mitigate | Surface route-preflight warnings so adopters do not ship a shadowed unsubscribe mount unknowingly |
</threat_model>

<verification>
`mix test test/mix/tasks/mailglass.gen.unsubscribe_test.exs` passes, proving the task prints actionable instructions and performs zero file writes.
</verification>

<success_criteria>
`mix mailglass.gen.unsubscribe` is a strict, read-only setup assistant that stays aligned with the actual unsubscribe config and router contract.
</success_criteria>

<output>
After completion, create `.planning/phases/11-rfc-8058-list-unsubscribe/11-05-SUMMARY.md`
</output>
