---
phase: 11-rfc-8058-list-unsubscribe
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-03]
files_modified:
  - lib/mailglass/router.ex
  - test/mailglass/router/unsubscribe_router_test.exs
autonomous: true
requirements: [UNSUB-04]

must_haves:
  truths:
    - Adopters can mount unsubscribe routes through a dedicated core router macro
    - The canonical public contract is `import Mailglass.Router` plus `mailglass_router_routes "/mailglass"`, which generates `GET` and `POST` routes at `/mailglass/unsubscribe/:token`
    - The mounted GET and POST paths always match the configured unsubscribe mount path
    - Route collisions fail loudly before a host app ships a shadowed unsubscribe endpoint
  artifacts:
    - path: lib/mailglass/router.ex
      provides: unsubscribe router macro, option validation, mount-path sourcing, and collision detection
    - path: test/mailglass/router/unsubscribe_router_test.exs
      provides: compile-time and route-reflection verification for path generation and collision failures
  key_links:
    - from: lib/mailglass/router.ex
      to: lib/mailglass/config.ex
      via: compile-time mount-path lookup routed through `Mailglass.Config`
    - from: lib/mailglass/router.ex
      to: lib/mailglass/compliance/unsubscribe_controller.ex
      via: generated GET and POST routes
---

<objective>
Mount the unsubscribe controller through a repo-native router macro and make route collisions load-bearing.

Purpose: The unsubscribe path must be deterministic for URL generation and safe for adopters to mount without silently shadowing or diverging from the configured route path.
Output: Core `mailglass_router_routes/2` macro, compile-time collision detection grounded in Phoenix's accumulated `:phoenix_routes` attribute, and route-reflection tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-rfc-8058-list-unsubscribe/11-CONTEXT.md
@.planning/phases/11-rfc-8058-list-unsubscribe/RESEARCH.md
@lib/mailglass/webhook/router.ex
@mailglass_admin/lib/mailglass_admin/router.ex
@test/mailglass/webhook/router_test.exs

<interfaces>
From lib/mailglass/webhook/router.ex:
```elixir
defmacro mailglass_webhook_routes(path, opts \\ [])
```

From mailglass_admin/lib/mailglass_admin/router.ex:
```elixir
@opts_schema [...]
defp validate_opts!(opts)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unsubscribe router macro with compile-time collision detection</name>
  <files>lib/mailglass/router.ex, test/mailglass/router/unsubscribe_router_test.exs</files>
  <behavior>
    - The macro generates exactly one GET and one POST route for the unsubscribe controller
    - The default public mount `mailglass_router_routes "/mailglass"` generates `/mailglass/unsubscribe/:token`
    - The macro sources the default mount path from `Mailglass.Config`, not from direct compile_env calls in the router module
    - Existing adopter routes that already claim the same GET or POST path raise `ArgumentError` at compile time
  </behavior>
  <action>
    Create `Mailglass.Router` following the webhook/admin router style, with `mailglass_router_routes/2` as the public macro and `NimbleOptions` validation for public opts such as helper prefix and any explicit path override used for tests.
    Route any compile-time config read through a `Mailglass.Config` accessor so the router module itself never calls `Application.compile_env*` directly.
    Implement route-collision detection in the macro by inspecting `Module.get_attribute(__CALLER__.module, :phoenix_routes)` before defining the unsubscribe routes. Phoenix registers `@phoenix_routes` with `accumulate: true` inside `Phoenix.Router` and later compiles `__routes__/0` from that same attribute, so this is a verified compile-time mechanism rather than speculative router introspection.
    The collision rule must check both verbs independently so a pre-existing GET or POST shadow is enough to fail the mount.
  </action>
  <verify>
    <automated>mix test test/mailglass/router/unsubscribe_router_test.exs --only macro_contract</automated>
  </verify>
  <done>The router macro mounts deterministic unsubscribe routes and refuses shadowed paths at compile time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify mount-path consistency and collision failures through route reflection</name>
  <files>test/mailglass/router/unsubscribe_router_test.exs</files>
  <behavior>
    - `__routes__/0` reflects the configured unsubscribe GET and POST paths exactly
    - Helper names match the public macro contract
    - Compile-string tests prove collision detection for pre-existing GET and POST routes separately
  </behavior>
  <action>
    Model the test suite after `test/mailglass/webhook/router_test.exs`.
    Include clean-router cases, custom helper-prefix cases, and compile-string collision cases for both GET and POST.
    Add one assertion that a later mount after a conflicting earlier route fails specifically because the earlier route already exists in `:phoenix_routes`, proving the compile-time mechanism used by the macro.
    Add one path-consistency test that proves the same default mount used by `mailglass_router_routes "/mailglass"` is what `Mailglass.Compliance.Unsubscribe.unsubscribe_url/2` builds against, preventing divergent-path regressions.
  </action>
  <verify>
    <automated>mix test test/mailglass/router/unsubscribe_router_test.exs</automated>
  </verify>
  <done>Route generation, helper naming, path consistency, and collision detection are all mechanically verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-router source -> macro expansion | Adopter routing code invokes a library macro that injects executable routes |
| Configured path -> generated routes | Compile-time path sourcing must stay in sync with runtime URL generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | router mount | mitigate | Source default mount path through `Mailglass.Config` and assert path consistency in tests |
| T-11-11 | Denial of Service | route collision | mitigate | Raise at compile time when adopter routes already occupy the unsubscribe GET or POST path |
| T-11-12 | Spoofing | helper/path drift | mitigate | Reflect generated routes in tests so URL builder and router macro cannot silently diverge |
</threat_model>

<verification>
`mix test test/mailglass/router/unsubscribe_router_test.exs` passes, including compile-string collision cases driven by Phoenix's accumulated `:phoenix_routes` attribute and path-consistency assertions.
</verification>

<success_criteria>
Adopters have a safe unsubscribe router macro that uses Phoenix's verified compile-time route accumulator to prevent silent shadows or drift from the configured mount path.
</success_criteria>

<output>
After completion, create `.planning/phases/11-rfc-8058-list-unsubscribe/11-04-SUMMARY.md`
</output>
