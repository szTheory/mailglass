---
phase: 11-rfc-8058-list-unsubscribe
plan: 07
type: execute
wave: 5
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05]
files_modified:
  - guides/unsubscribe.md
  - guides/dkim-setup.md
  - mix.exs
  - test/mailglass/docs/unsubscribe_guide_test.exs
autonomous: true
requirements: [UNSUB-06]

must_haves:
  truths:
    - Adopters have a start-to-finish unsubscribe setup guide aligned with the shipped config, router, controller, and generator behavior
    - DKIM guidance explicitly calls out that both unsubscribe headers must be covered in `h=`
    - Guide snippets and setup claims are load-bearing through an automated docs smoke test, not just `mix docs`
    - ESP-specific caveats, especially the SendGrid historical gap, are documented with concrete verification steps
  artifacts:
    - path: guides/unsubscribe.md
      provides: setup walkthrough, generator usage, built-in GET page behavior, lifecycle hook guidance, and troubleshooting
    - path: guides/dkim-setup.md
      provides: RFC 8058 DKIM `h=` guidance and ESP-specific verification notes
    - path: mix.exs
      provides: docs extras/nav wiring for the new or updated guides
    - path: test/mailglass/docs/unsubscribe_guide_test.exs
      provides: docs/smoke coverage for unsubscribe guide snippets, mount contract, and generator claims
  key_links:
    - from: guides/unsubscribe.md
      to: lib/mix/tasks/mailglass.gen.unsubscribe.ex
      via: generator-backed setup and UAT instructions
    - from: test/mailglass/docs/unsubscribe_guide_test.exs
      to: guides/unsubscribe.md
      via: load-bearing assertions for the published setup contract and example snippets
    - from: guides/dkim-setup.md
      to: RFC 8058 header injection
      via: requirement that both `List-Unsubscribe` and `List-Unsubscribe-Post` be signed
---

<objective>
Document the operational contract for RFC 8058 unsubscribe and DKIM verification.

Purpose: Phase 11 is incomplete if adopters can ship the code but not verify DKIM coverage, route wiring, rotation handling, and the built-in confirmation behavior in their own environment.
Output: Unsubscribe guide, updated DKIM guide, published-doc wiring, and a docs smoke test that makes the setup contract load-bearing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-rfc-8058-list-unsubscribe/11-CONTEXT.md
@.planning/phases/11-rfc-8058-list-unsubscribe/RESEARCH.md
@guides
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the unsubscribe adopter guide and docs smoke test</name>
  <files>guides/unsubscribe.md, test/mailglass/docs/unsubscribe_guide_test.exs</files>
  <action>
    Write `guides/unsubscribe.md` as the canonical adopter walkthrough for RFC 8058 setup.
    Cover the compliance config, router macro, `mix mailglass.gen.unsubscribe`, built-in GET confirmation page, redirect escape hatch, lifecycle hook usage, POST replay expectations, and concrete UAT steps.
    Call out explicitly that the generator prints instructions only and copies zero files.
    Include a rotation playbook showing how `previous_secrets` keeps in-flight links valid during secret-key-base rotation.
    Create `test/mailglass/docs/unsubscribe_guide_test.exs` to assert the published guide keeps the canonical `mailglass_router_routes "/mailglass"` mount, the `/mailglass/unsubscribe/:token` route shape, the zero-copy generator claim, and the documented UAT steps load-bearing.
  </action>
  <verify>
    <automated>mix test test/mailglass/docs/unsubscribe_guide_test.exs && mix docs</automated>
  </verify>
  <done>The unsubscribe guide is complete enough for an adopter to wire, test, and troubleshoot the feature without guessing at hidden behavior, and its critical setup contract is enforced by docs smoke tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update DKIM guidance and docs navigation for RFC 8058</name>
  <files>guides/dkim-setup.md, mix.exs</files>
  <action>
    Update `guides/dkim-setup.md` so it explicitly requires both `List-Unsubscribe` and `List-Unsubscribe-Post` in DKIM `h=` for RFC 8058 flows.
    Document the Postmark happy-path expectation and the SendGrid historical `List-Unsubscribe-Post` caveat noted in project state, with a concrete instruction to verify headers in ESP diagnostics before rollout.
    Update `mix.exs` docs extras or grouping only as needed so the unsubscribe and DKIM guidance both appear in published docs.
  </action>
  <verify>
    <automated>mix docs</automated>
  </verify>
  <done>Published docs surface both the unsubscribe walkthrough and RFC 8058-specific DKIM verification guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library docs -> adopter rollout | Incorrect or incomplete docs can cause compliant code to be deployed with broken DKIM or mount wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19 | Repudiation | operational docs | mitigate | Document the exact setup, replay, rotation, and DKIM verification steps so adopters can prove correct rollout |
| T-11-20 | Information Disclosure | ESP caveats | mitigate | Call out provider-specific header-signing gaps so adopters do not assume RFC 8058 compliance from code alone |
</threat_model>

<verification>
`mix test test/mailglass/docs/unsubscribe_guide_test.exs && mix docs` succeeds with the new unsubscribe walkthrough and DKIM guide updates wired into the published docs set.
</verification>

<success_criteria>
UNSUB-06 is satisfied with docs that match the shipped Phase 11 behavior, including DKIM `h=` verification and secret-rotation guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-rfc-8058-list-unsubscribe/11-07-SUMMARY.md`
</output>
