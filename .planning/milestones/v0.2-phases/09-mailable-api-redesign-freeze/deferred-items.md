- **mix test failures**: The test suite has 41 failures mostly related to `Postgrex.Error ERROR XX000 (internal_error) cache lookup failed for type 1774172`. This is likely an environmental issue with Postgres dropping types mid-flight or cache invalidation issues between Ecto and Postgres during tests. There is also one regular failure in `test/mailglass/events/reconciler_test.exs:35`. These issues are pre-existing or out-of-scope for the `09-02` plan, which is strictly about updating the Mailable Macro.