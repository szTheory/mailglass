---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 02
type: execute
wave: 2
depends_on: [153-01]
files_modified:
  - dev/mailglass/generated_host/host_template.ex
  - dev/mailglass/generated_host/journey.ex
  - dev/mailglass/generated_host/checkpoint.ex
  - test/generated_host/sync_async_parity_test.exs
autonomous: true
requirements: [ADOPT-02]
must_haves:
  truths:
    - "An unstamped SingleTenant caller sends the same supported message synchronously and asynchronously as tenant `default`, with wire-equivalent captured provider input (D-06, D-07)."
    - "Async delivery is performed by a normally supervised, positive-concurrency `mailglass_outbound` Oban queue, never inline, manual, drained, or direct-perform execution (D-06)."
    - "The async job visibly progresses through polling and successful durable settlement scrubs private content while retaining public delivery/event evidence (D-08)."
  artifacts:
    - path: dev/mailglass/generated_host/host_template.ex
      provides: "Host-owned public capture adapter, sample mailable, Repo, and normal Oban supervision source"
    - path: test/generated_host/sync_async_parity_test.exs
      provides: "Real-host provider-input and lifecycle oracle"
  key_links:
    - from: "Mailglass.deliver_later/2 in generated host"
      to: "normal Oban mailglass_outbound producer"
      via: "persisted job consumed by supervised polling"
      pattern: "mailglass_outbound"
    - from: "polled Oban worker"
      to: "host capture adapter and Mailglass lifecycle rows"
      via: "public adapter callback plus qualified host-owned DB observations"
      pattern: "capture"
---

<objective>
Expand the tracer into truthful sync/real-Oban async parity and lifecycle evidence (D-06 through D-08).

Purpose: Prove the durable path adopters actually run rather than a test-worker simulation.
Output: Generated host capture adapter, normal Oban supervision, parity/lifecycle stages, and behavioral tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-01-SUMMARY.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-01-SUMMARY.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-04-SUMMARY.md
@lib/mailglass/outbound/worker.ex
@lib/mailglass/outbound/dispatch_outcome.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Send one sync and one normally polled async message</name>
  <files>dev/mailglass/generated_host/host_template.ex, dev/mailglass/generated_host/journey.ex, test/generated_host/sync_async_parity_test.exs</files>
  <behavior>
    - "The host starts Repo, Endpoint, and Oban with `queues: [mailglass_outbound: 1]` and no testing/inline/manual mode (D-06)."
    - "Public sync and async calls are unstamped and persist tenant `default`; the capture adapter receives equal supported recipient/from/reply-to/subject/header/body/stream/tag/metadata/attachment/provider-option fields after excluding generated identifiers/timing (D-07)."
    - "The async checkpoint observes an inserted job and a later terminal completion/capture without invoking a worker or drain function itself (D-06, D-08)."
  </behavior>
  <action>Create the real-host RED oracle and commit `test(153-02): require normal Oban sync async parity`. Extend the host template with an adopter-owned capture adapter implementing only the documented public adapter contract, a supported sample Mailable, deterministic host-owned capture storage, Endpoint, and normal Oban child. Extend Journey with bounded polling that samples qualified job/capture/lifecycle state; it must never invoke `Worker.perform/1`, `Oban.drain_queue`, or Oban testing modes. Normalize only timing/provider-generated fields before comparing the full supported provider input. Commit `feat(153-02): prove normally polled provider input parity`.</action>
  <verify>
    <automated>mix test test/generated_host/sync_async_parity_test.exs --warnings-as-errors &amp;&amp; DEP_MODE=local bash scripts/generated_host_proof.sh --stage async-parity</automated>
  </verify>
  <done>The host proves unstamped default-tenant sync/async wire equivalence and a producer-driven job transition with no test execution shortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove durable settlement and private-content scrub</name>
  <files>dev/mailglass/generated_host/journey.ex, dev/mailglass/generated_host/checkpoint.ex, test/generated_host/sync_async_parity_test.exs</files>
  <behavior>
    - "After accepted async dispatch, Delivery and non-sensitive Event facts remain while the matching private Payload is a contentless scrubbed tombstone (D-08)."
    - "The checkpoint records only state, counts, stable hashed sentinels, and transition order—not message/provider content."
  </behavior>
  <action>Add failing post-settlement lifecycle assertions before implementation and commit `test(153-02): require async settlement scrub evidence`. Use only host-owned Repo/catalog queries against documented tables and public schema configuration; do not load Mailglass private modules into the generated host. Poll to a bounded deadline for the durable terminal facts, prove the payload content columns are cleared, retain the public projection/ledger, and add hashed transition/count fields to the checkpoint. Commit `feat(153-02): record durable async lifecycle settlement`.</action>
  <verify>
    <automated>DEP_MODE=local bash scripts/generated_host_proof.sh --stage async-parity &amp;&amp; bash scripts/check_generated_host_proof.sh --checkpoint tmp/generated-host-proof/checkpoint.json</automated>
  </verify>
  <done>The same public async request has provider dispatch, job transition, durable success facts, and private payload scrub in one validated proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host public send API → Oban | A queued result is untrusted until a real normal-mode producer processes the persisted job. |
| adapter input → capture proof | Provider input may contain PII and must be compared in memory but emitted only as hashes/results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-153-05 | Spoofing | async success evidence | high | mitigate | Observe inserted and terminal job/capture facts under normal producer polling; ban direct execution shortcuts (D-06). |
| T-153-06 | Tampering | sync/async parity | high | mitigate | Field-complete normalized comparison against one host-owned adapter oracle (D-07). |
| T-153-07 | Information Disclosure | captured provider input | high | mitigate | In-memory comparison plus hashes/counts only in checkpoint (D-08). |
| T-153-08 | Repudiation | payload scrub | medium | mitigate | Qualified before/after lifecycle observations bound to delivery/job sentinel hashes. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Status |
|---|---|---|---|
| REQ | ADOPT-02 | Default-tenant sync/real-Oban async equivalent input | COVERED |
| CONTEXT | D-06..D-08 | Active polling, parity, lifecycle/scrub | COVERED |
| RESEARCH | Pattern 2 | Polling-observed asynchronous assertion | COVERED |

<verification>
Run the real local host async-parity stage and validate both behavioral assertions and sanitized checkpoint.
</verification>

<success_criteria>
The generated host—not repo test support—proves the complete real durable happy path.
</success_criteria>

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-02-SUMMARY.md` when done.
</output>
