---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 03
type: execute
wave: 3
depends_on: [153-02]
files_modified:
  - dev/mailglass/generated_host/journey.ex
  - dev/mailglass/generated_host/host_template.ex
  - dev/mailglass/generated_host/checkpoint.ex
  - test/generated_host/negative_controls_test.exs
autonomous: true
requirements: [ADOPT-03]
must_haves:
  truths:
    - "Missing dependency/instance/canonical queue and wrong-queue controls each fail before queued success or any job, delivery, event, payload, capture, or background task appears (D-09)."
    - "Missing migration, wrong schema, and schema-version drift fail with bounded actionable readiness/operation errors without public-search-path rescue (D-10)."
    - "Zero/multiple recipients and unsupported payload/provider options fail before rendering or every durable/provider effect, with explicit unchanged before/after observations (D-11)."
  artifacts:
    - path: test/generated_host/negative_controls_test.exs
      provides: "Independent zero-effect queue/schema/input control matrix"
    - path: dev/mailglass/generated_host/checkpoint.ex
      provides: "Per-control before/after and reason-class evidence"
  key_links:
    - from: "each isolated control invocation"
      to: "qualified DB/job/capture/render/task observations"
      via: "fresh sentinel/schema and before/after snapshot"
      pattern: "negative"
---

<objective>
Make the generated-host gate fail closed and non-vacuous across queue, schema, recipient, and payload drift (D-09 through D-11).

Purpose: Prevent false green adoption proof when prerequisites or inputs are wrong.
Output: Isolated negative-control matrix with bounded failures and zero-effect evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-02-SUMMARY.md
@lib/mailglass/config.ex
@lib/mailglass/optional_deps/oban.ex
@lib/mailglass/outbound/preflight.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate queue and schema drift controls</name>
  <files>dev/mailglass/generated_host/journey.ex, dev/mailglass/generated_host/host_template.ex, test/generated_host/negative_controls_test.exs</files>
  <behavior>
    - "Separate invocations cover dependency missing, Oban instance unavailable, canonical queue absent, and wrong queue; each returns its bounded reason class and unchanged effect snapshot (D-09)."
    - "Separate invocations cover migration missing, configured schema wrong, and version behind/ahead; public decoy tables/version cannot satisfy them (D-10)."
  </behavior>
  <action>Add the failing independent-process matrix and commit `test(153-03): require queue and schema zero-effect controls`. Parameterize the shared runner with a closed control-name set and create a fresh database/schema/sentinel for each invocation. For the dependency-missing case, reuse the shipped isolated production-graph technique from Phase 150 rather than source-purging modules in a live VM. For instance/queue controls, change only the host's valid runtime config. For schema controls, omit migration, point at a different validated prefix with public decoys, or mutate the explicit version marker in the disposable database. Capture bounded error class and before/after counts for Oban jobs, deliveries, events, payloads, captures, and supervised tasks. Commit `feat(153-03): add isolated queue and schema controls`.</action>
  <verify>
    <automated>mix test test/generated_host/negative_controls_test.exs --only control_family:queue_schema --warnings-as-errors &amp;&amp; DEP_MODE=local bash scripts/generated_host_proof.sh --stage negative-controls --family queue-schema</automated>
  </verify>
  <done>Every queue/schema control fails for the intended reason and all effect counters remain exactly unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject invalid recipient and payload shapes before effects</name>
  <files>dev/mailglass/generated_host/journey.ex, dev/mailglass/generated_host/checkpoint.ex, test/generated_host/negative_controls_test.exs</files>
  <behavior>
    - "Zero, to+cc, duplicate-recipient, and broader multi-recipient shapes reject before render/durable/provider effects (D-11)."
    - "Unsupported attachment/payload/provider-option terms and over-limit JSON reject before any job or persistent Mailglass fact (D-11)."
    - "Checkpoint success requires an explicit zero delta for every observed effect category, not only an error result."
  </behavior>
  <action>Write the RED input-control matrix and mutation checks, commit `test(153-03): require input zero-effect controls`, then drive every case through the generated host's public outbound entrypoint. Instrument only the host-owned mailable render counter and capture store plus qualified database/job/task observations. Extend the manifest validator so a negative control without all before/after fields or with any nonzero delta is invalid. Commit `feat(153-03): enforce input controls before all effects`.</action>
  <verify>
    <automated>mix test test/generated_host/negative_controls_test.exs --only control_family:input --warnings-as-errors &amp;&amp; DEP_MODE=local bash scripts/generated_host_proof.sh --stage negative-controls --family input &amp;&amp; bash scripts/check_generated_host_proof.sh --checkpoint tmp/generated-host-proof/checkpoint.json</automated>
  </verify>
  <done>All invalid recipient/payload controls return bounded failures with explicit zero render/job/durable/capture/task deltas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host configuration/input → public Mailglass boundary | Untrusted configuration and messages must fail before any observable effect. |
| default search path → configured schema | Public decoys must not satisfy readiness or operation checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-153-09 | Spoofing | negative-control success | high | mitigate | Fresh isolated invocation plus complete before/after zero-effect vector (D-09, D-11). |
| T-153-10 | Tampering | public schema fallback | high | mitigate | Qualified configured-prefix checks with adversarial public decoys (D-10). |
| T-153-11 | Denial of Service | unbounded polling/control | medium | mitigate | Closed controls and bounded deadlines with retained diagnosis on failure. |
| T-153-12 | Information Disclosure | error/checkpoint context | medium | mitigate | Closed reason classes and counts/hashes only. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Status |
|---|---|---|---|
| REQ | ADOPT-03 | Queue/schema/input negative controls | COVERED |
| CONTEXT | D-09..D-11 | Independent failures plus zero effects | COVERED |
| RESEARCH | Pattern 3 | One negative control per isolated host invocation | COVERED |

<verification>
Run both negative-control families and validate the merged checkpoint against deliberate nonzero-delta mutations.
</verification>

<success_criteria>
No configuration or input control can report success from a decoy, pre-existing row, or partial side effect.
</success_criteria>

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-03-SUMMARY.md` when done.
</output>
