---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 08
type: execute
wave: 8
depends_on: [153-07]
files_modified:
  - .github/workflows/post-publish-smoke.yml
  - test/mailglass/publish/post_publish_smoke_contract_test.exs
  - scripts/verify_release_proof.exs
  - test/scripts/release_proof_verifier_test.exs
  - .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
autonomous: false
requirements: [REL-17]
must_haves:
  truths:
    - "D-23: Publication occurs only through the protected release workflow after all Plan 07 proof is green for the exact candidate SHA; credentials may pause that publication but cannot weaken or skip gates."
    - "D-22: Exact published Hex versions repeat the complete generated-host journey and match the resolver-approved release set."
    - "Before publication, a blocking human checkpoint displays and confirms the ledger-bound candidate SHA/tag, protected workflow/environment, resolver-selected packages, and exact target versions."
    - "The final evidence ledger names immutable package versions, checksums, workflow runs, HexDocs URLs, and every journey stage outcome."
  artifacts:
    - path: .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
      provides: Auditable prepublication, publication, and exact-Hex postpublication evidence
    - path: scripts/verify_release_proof.exs
      provides: Fail-closed workflow, environment, candidate, package, version, checksum, and ledger verifier
  key_links:
    - from: .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
      to: .github/workflows/publish-hex.yml
      via: Candidate SHA and protected publication run URL
    - from: .github/workflows/post-publish-smoke.yml
      to: Hex.pm exact versions
      via: Resolver-approved immutable version outputs
    - from: Hex.pm exact versions
      to: scripts/generated_host_proof.sh
      via: DEP_MODE=hex full downstream journey
---

<objective>
Perform the one-way protected publication only after complete reversible proof, then verify the exact public Hex artifacts end to end (D-22, D-23).

Purpose: Make the irreversible release action the final, narrow step and leave an independently auditable record of what users can actually install.
Output: Protected package publication and completed exact-Hex release proof ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-CONTEXT.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RESEARCH.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-07-SUMMARY.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
@.planning/release-target.json
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind exact-Hex workflow and release evidence to the approved candidate</name>
  <files>.github/workflows/post-publish-smoke.yml, test/mailglass/publish/post_publish_smoke_contract_test.exs, scripts/verify_release_proof.exs, test/scripts/release_proof_verifier_test.exs, .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</files>
  <reversibility rating="reversible">Workflow contracts, verifier fixtures, and the prepublication ledger can be changed before any immutable package is published.</reversibility>
  <behavior>
    - D-22: The post-publish workflow consumes only exact resolver-approved versions and runs every canonical generated-host stage under `DEP_MODE=hex`.
    - The verifier reads the run identity from the ledger and accepts only workflow path `.github/workflows/publish-hex.yml`, workflow name `publish-hex`, protected environment `hex-publish`, approved deployment status, and the ledger candidate SHA/tag.
    - Resolver-selected package names, release-target exact versions, tag dereferences, workflow publish jobs, downloaded Hex versions, archive checksums, and ledger values must agree exactly; any omission, extra, mismatch, unapproved environment, floating version, or path/git dependency fails closed.
  </behavior>
  <action>Write failing workflow and verifier fixture tests first. Implement the exact-Hex post-publish wiring in this task and invoke only `bash scripts/generated_host_proof.sh` as the canonical runner. Implement `scripts/verify_release_proof.exs` so it obtains the protected run database ID and URL from `153-RELEASE-PROOF.md`, queries GitHub for that run and its deployments, and binds evidence to the exact workflow path/name, `hex-publish` environment approval/protection, candidate SHA, annotated or lightweight tag dereference, resolver-selected package set, release-target exact versions, publish job set, Hex archive versions/checksums, and ledger fields. Do not accept a caller-supplied arbitrary successful run ID as proof. Make post-publish smoke reject local/path/git dependencies and floating constraints, use bounded Hex/HexDocs propagation retries, execute every canonical journey stage, and append observed stage evidence. Commit RED as `test(153-08): define ledger-bound protected release proof` and GREEN as `ci(153-08): bind exact Hex proof to protected release`.</action>
  <verify>
    <automated>mix test test/mailglass/publish/post_publish_smoke_contract_test.exs test/scripts/release_proof_verifier_test.exs --seed 0</automated>
  </verify>
  <done>D-22 exact-Hex wiring and the proof verifier are ready before publication; fixture tests reject every workflow, environment, SHA/tag, package, version, checksum, or ledger mismatch.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Authorize the irreversible protected Hex publication for the exact candidate recorded in `153-RELEASE-PROOF.md`.</decision>
  <context>Plan 07 and Task 1 have completed every reversible gate. Before continuing, present the concrete candidate SHA and tag, workflow path/name, protected `hex-publish` environment and approval state, resolver-selected packages, exact target versions, candidate package checksums, protected-CI URLs, and successful local journey/package checks from the ledger. This checkpoint authorizes only those displayed values; any later drift returns execution to the prepublication gates.</context>
  <options>
    <option id="publish-exact-candidate">
      <name>Publish exact candidate</name>
      <pros>Continues through the protected workflow with the displayed immutable candidate and package set.</pros>
      <cons>Hex publication is a one-way external action and cannot be treated as reversible after completion.</cons>
    </option>
    <option id="stop-before-publication">
      <name>Stop before publication</name>
      <pros>Leaves every completed change and proof reversible while preserving the ledger for later review.</pros>
      <cons>REL-17 and Phase 153 remain incomplete until the exact candidate is approved and published.</cons>
    </option>
  </options>
  <resume-signal>Select `publish-exact-candidate` to authorize only the displayed ledger-bound release, or `stop-before-publication`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Publish and verify the exact approved public artifacts</name>
  <files>.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</files>
  <precondition>The immediately preceding checkpoint approved `publish-exact-candidate`, and the ledger values still byte-for-byte match the approved candidate SHA/tag, protected workflow/environment, resolver-selected package set, exact versions, candidate checksums, and green prepublication evidence.</precondition>
  <reversibility rating="one-way">The protected workflow publishes immutable versions to Hex; no executor action can restore the unpublished external state.</reversibility>
  <action>Run the verifier in prepublication mode and halt on any difference from the checkpointed ledger. Trigger only `.github/workflows/publish-hex.yml` for the approved tag/package set, require workflow name `publish-hex`, candidate head SHA equal to the approved tag dereference, and environment `hex-publish` with its recorded approval/protection event. Never invoke local `mix hex.publish`, supply an unrelated run ID, change tags/targets, use dispatch overrides, bypass protection, or publish an unselected package. If credentials require authentication, pause and resume this same protected run without weakening a gate. After its exact publish jobs succeed, record the run database ID/URL, deployment/environment approval, job IDs, versions, Hex checksums, and tag/SHA association. Let post-publish smoke run `DEP_MODE=hex bash scripts/generated_host_proof.sh --stage all`; verify archive contents, no local dependency, install/compile, migration, actively polling Oban parity/lifecycle, zero-effect negatives, signed feedback, one-click suppression, production readiness/operator auth, executable docs, registry visibility, and HexDocs. Run the ledger-bound verifier in complete mode and leave REL-17 failed on any propagation or evidence mismatch per D-22 and D-23.</action>
  <verify>
    <automated>mix run scripts/verify_release_proof.exs -- --ledger .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md --workflow-path .github/workflows/publish-hex.yml --workflow-name publish-hex --environment hex-publish --stage complete</automated>
  </verify>
  <done>D-22 and D-23 are satisfied: the checkpoint-approved candidate alone was published through the exact protected workflow/environment, package/version/checksum/ledger identities agree, every exact-Hex journey stage and HexDocs pass, and no success is inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected workflow → Hex | Credentialed automation creates immutable public package versions. |
| Hex registry → generated host | Public archives and metadata become executable downstream dependencies. |
| workflow telemetry → evidence ledger | External run state is converted into durable release assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-29 | Elevation of Privilege | Protected publication | critical | mitigate | Blocking decision checkpoint plus exact workflow-path/name and `hex-publish` environment approval/protection verification; no local or bypass publish path. |
| T-153-30 | Tampering | Published artifacts | critical | mitigate | Record and compare exact versions, archive checksums, package contents, and immutable tags. |
| T-153-31 | Repudiation | Release proof ledger | high | mitigate | Preserve workflow URLs, job IDs, timestamps, SHA, commands, and observed outputs for every assertion. |
| T-153-32 | Spoofing | Registry propagation | high | mitigate | Resolve exact versions from Hex, compare installed application metadata, and fail on any path override or mismatch. |
| T-153-SC | Tampering | Package installs | high | mitigate | Install only exact already-audited project packages from Hex; verify checksums and package contents before executing the journey. |
</threat_model>

<verification>
- Workflow/verifier fixture tests reject mismatched identity, environment approval, SHA/tag, package, version, checksum, or ledger values before the checkpoint.
- The checkpoint displays and approves one concrete ledger-bound release candidate before the one-way task.
- `scripts/verify_release_proof.exs --stage complete` proves the protected workflow published only resolver-selected exact versions from the recorded candidate SHA and that the exact-Hex journey/HexDocs are green.
</verification>

<success_criteria>
- REL-17 is complete only after protected publication and exact-Hex downstream proof.
- D-23 keeps the irreversible action behind every reversible gate and allows only credential/authentication pauses.
- D-22 records the same complete journey against immutable public versions, with no local fallback.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Protected release follows complete local proof | COVERED — checkpoint and Task 3. |
| GOAL | Exact public packages pass the complete fresh-host journey | COVERED — Tasks 1 and 3. |
| REQ | REL-17 | COVERED — Tasks 1-3. |
| RESEARCH | Separate reversible validation from irreversible publication | COVERED — Task 1, blocking checkpoint, then one-way Task 3. |
| CONTEXT | D-22 | COVERED — Tasks 1 and 3. |
| CONTEXT | D-23 | COVERED — checkpoint and Task 3. |

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-08-SUMMARY.md` when done.
</output>
