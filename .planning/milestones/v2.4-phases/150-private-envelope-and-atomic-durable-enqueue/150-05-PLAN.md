---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "05"
type: execute
wave: 5
depends_on:
  - 150-04
files_modified:
  - lib/mailglass/optional_deps.ex
  - lib/mailglass/optional_deps/oban.ex
  - lib/mailglass/outbound.ex
  - lib/mailglass/application.ex
  - lib/mailglass/config.ex
  - docs/api_stability.md
  - guides/getting-started.md
  - guides/jobs.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements:
  - ENVL-01
  - ENVL-06
  - ENVL-07
  - ENVL-08
must_haves:
  truths:
    - "D-12/D-13/ENVL-06/ENVL-07: every active core source/API/adopter claim says selected `:oban` is durable and fail-closed, while TaskSupervisor runs only when explicitly selected and is non-durable."
    - "D-14/ENVL-06: `Mailglass.OptionalDeps` and `Mailglass.OptionalDeps.Oban` describe availability/readiness as gateway primitives, never as permission for outbound adapter substitution."
    - "D-10/ENVL-08: all current outbound queue examples in the touched source/docs use canonical `mailglass_outbound`; the inbound sibling's historical/current bounded fallback contract remains unchanged and out of this phase."
    - "D-01/D-03/ENVL-01: public stability and adopter guidance describe private recoverable transport state without creating a payload viewer, sent-message archive, or public Payload API promise."
    - "A single focused source-contract suite reads every active seam found by the repository sweep and fails non-vacuously if an unavailable-Oban automatic-downgrade claim or stale queue token is reintroduced."
  artifacts:
    - path: lib/mailglass/optional_deps.ex
      provides: "Framework-wide optional gateway pattern with an explicit Oban exception to generic degraded-fallback language"
    - path: lib/mailglass/optional_deps/oban.ex
      provides: "Truthful availability/readiness/insert documentation and canonical tenancy middleware queue example"
    - path: lib/mailglass/outbound.ex
      provides: "Public deliver_later/deliver_many/dispatch documentation matching the fail-closed runtime"
    - path: docs/api_stability.md
      provides: "Stable private envelope, ID-only job, typed readiness, explicit TaskSupervisor, and legacy-reader contract"
    - path: test/mailglass/docs_contract_test.exs
      provides: "One executable source/adopter contract over every active contradictory seam"
  key_links:
    - from: test/mailglass/docs_contract_test.exs
      to: lib/mailglass/optional_deps.ex
      via: "source-region extraction asserts the root gateway namespace does not promise outbound fallback"
      pattern: "OptionalDeps"
    - from: test/mailglass/docs_contract_test.exs
      to: lib/mailglass/outbound.ex
      via: "public-doc extraction ties deliver_later/deliver_many/dispatch claims to explicit adapter behavior"
      pattern: "deliver_later"
    - from: test/mailglass/docs_contract_test.exs
      to: lib/mailglass/outbound/worker.ex
      via: "all outbound production queue snippets compare with Worker.queue/0"
      pattern: "Worker\\.queue"
    - from: guides/jobs.md
      to: lib/mailglass/config.ex
      via: "adopter guidance names production_readiness/0 and labels explicit TaskSupervisor non-durable"
      pattern: "production_readiness"
  prohibitions:
    - statement: "Current outbound source/API/adopter guidance must not present Task.Supervisor as an automatic fallback for missing or unavailable Oban."
      status: unverified
      flagged: true
    - statement: "Public guidance must not turn private transport payloads into an adopter-facing archive or admin viewing promise."
      status: unverified
      flagged: true
---

<objective>
Remove and mechanically lock every active outbound fallback contradiction found by the repository-wide sweep, then publish the explicit durable/non-durable and private/public contracts consistently per D-01/D-03/D-10/D-12 through D-14.

Purpose: Prevent runtime truth from drifting across source moduledocs, public API docs, stability policy, and adopter guidance one file at a time.
Output: One coherent source/adopter claim set and one focused executable contract covering all active seams.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VALIDATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-03-SUMMARY.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-04-SUMMARY.md
@lib/mailglass/optional_deps.ex
@lib/mailglass/optional_deps/oban.ex
@lib/mailglass/outbound.ex
@lib/mailglass/application.ex
@lib/mailglass/config.ex
@docs/api_stability.md
@guides/getting-started.md
@guides/jobs.md
@test/mailglass/docs_contract_test.exs
</context>

## Fallback sweep classification

The tracked `.ex`, `.exs`, and `.md` sweep found these active outbound contradictions, all owned by Task 150-05-01: `lib/mailglass/optional_deps.ex`, `lib/mailglass/optional_deps/oban.ex`, `lib/mailglass/outbound.ex`, `lib/mailglass/application.ex`, `lib/mailglass/config.ex`, and the current async stability section in `docs/api_stability.md`. Current adopter guides do not consistently state the new executable boundary and are included for positive contract completion. The old fallback wording in `.planning/milestones/**`, milestone/research archives, changelogs, inbound sibling source/docs, and `PROJECT.md` provenance records describes shipped historical or distinct inbound behavior; preserve it. Unrelated webhook-maintenance cron fallback wording is also not the outbound `deliver_later` adapter contract.

## Artifacts this phase produces

- Corrected active source documentation in the optional-dependency namespace, Oban gateway, Outbound public API, application warnings/comments, and Config async-adapter entry.
- Corrected current API stability and adopter guidance for private Payload, exact ID-only Worker args, canonical queue, typed fail-closed Oban, explicit non-durable TaskSupervisor, and narrow pre-v2.4 compatibility.
- One tagged contract helper/test that enumerates every active seam rather than relying on a whole-repository negative grep.

<tasks>

<task type="auto" tdd="true">
  <name>Task 150-05-01: Clean and lock every active outbound fallback claim</name>
  <reversibility rating="costly">D-12/D-13 change an established adopter-facing behavior claim; keeping runtime and public guidance synchronized is required for a safe compatibility transition.</reversibility>
  <files>lib/mailglass/optional_deps.ex, lib/mailglass/optional_deps/oban.ex, lib/mailglass/outbound.ex, lib/mailglass/application.ex, lib/mailglass/config.ex, docs/api_stability.md, guides/getting-started.md, guides/jobs.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>Read D-01/D-03/D-10/D-12/D-13/D-14/D-15/D-16; Plan 03's final `ready?/1`, `insert/4`, Worker queue, payload-first/legacy behavior; Plan 04's production-readiness and warning behavior; every file in the Fallback sweep classification; the current Phase 150/stability sections of `docs/api_stability.md`; and existing file/block extraction helpers in `docs_contract_test.exs`.</read_first>
  <behavior>
    - Root OptionalDeps docs describe generic gateways without assigning Oban-selected outbound work to a degraded adapter; the Oban gateway documents `available?/0` as detection, `ready?/1` as canonical readiness, and `insert/4` as fail-closed.
    - Outbound moduledoc plus `deliver_later/2`, `deliver_many/2`, and `dispatch_by_id/1` docs describe explicit adapter selection: durable Oban failure returns typed error/no queued work, explicit TaskSupervisor is non-durable.
    - Application comments/warnings and Config schema docs match Plan 04 exactly and do not imply an automatic adapter change.
    - Stability/Getting Started/Jobs guidance names the private transport boundary, exact Worker identifiers, canonical queue, callable readiness seam, explicit non-durable TaskSupervisor, and narrow legacy reader without Phase 151 or admin/archive promises.
    - Contract-helper mutation fixtures prove the check rejects both an automatic-downgrade sentence and a stale outbound queue token before all real regions pass.
  </behavior>
  <action>Create the focused contract test first and tag it `@tag phase_150_task: "t150_05_01"`. Define an explicit manifest of the six active contradictory source/stability seams plus the two adopter guides, extract only moduledoc/public-function/config/Phase-150 sections, and assert positive semantics for D-10/D-12/D-13/D-14 rather than scanning historical files. Add mutation fixtures that make the helper fail for an unavailable-Oban adapter substitution claim and for the old outbound queue token so the gate is non-vacuous. Then reconcile every manifested region in one pass: make the root OptionalDeps pattern explicitly allow gateways to fail closed; document Oban `available?/0` only as dependency detection, `ready?/1` as configured canonical readiness, and `insert/4` as transactional failure rather than unchanged-Multi success; use `mailglass_outbound` in its middleware example; update Outbound's three public async/dispatch regions, Application comments/warnings, and Config's `:async_adapter` docs to the exact explicit-selection contract; and update stability/Getting Started/Jobs with the private Payload, exact `delivery_id` plus `mailglass_tenant_id` args, production readiness, canonical queue, and explicit non-durable TaskSupervisor. Preserve historical/archive/provenance and inbound sibling fallback text, preserve unrelated webhook cron/manual-maintenance wording, make no Phase 151 lifecycle/outcome promise, and expose no Payload viewer or public Payload API.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs --only phase_150_task:t150_05_01 --warnings-as-errors</automated>
  </verify>
  <done>Every active outbound source/API/adopter claim from the targeted repository sweep agrees with the executable fail-closed runtime, and one non-vacuous focused test protects all seams together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime adapter semantics → source/API documentation | A stale comment or public doc can advertise non-durable work as durable success. |
| private transport implementation → compatibility promise | Documentation can accidentally stabilize Payload as a public archive/viewing API. |
| repository history/inbound sibling → active outbound contract sweep | Legitimate historical or different-package fallback text must not be rewritten or used as a false failure signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-16 | Repudiation / Denial of Service | active outbound fallback claims | high | mitigate | Enumerated region manifest, positive explicit-selection assertions, and non-vacuous contradictory fixture tests. |
| T-150-17 | Tampering | canonical queue documentation | high | mitigate | Compare all manifested outbound queue tokens to Worker.queue/0 and reject the stale value fixture. |
| T-150-18 | Information Disclosure | Payload/stability guidance | medium | mitigate | Describe only private transport semantics and assert no archive/admin-view/public-Payload promise in the Phase 150 regions. |
| T-150-19 | Repudiation | historical/inbound classification | low | accept | Preserve immutable provenance and separately shipped inbound behavior; exclude only by explicit path/section manifest rather than deletion. |
| T-150-SC | Tampering | package installs | high | mitigate | No install task exists; any dependency deviation requires package-legitimacy review. |
</threat_model>

## Edge Coverage Resolution — 15/15

| Requirement | Probe item | Resolution location |
|---|---|---|
| ENVL-01 | unclassified | Plan 01 private-store tracer; Plan 02 recursive leakage; Plan 05 public-boundary contract |
| ENVL-02 | boundary | Plan 01 explicit supported-field allowlist and required-field failures |
| ENVL-02 | adjacency | Plan 01 ordered/duplicate collection round trips |
| ENVL-02 | empty | Plan 01 optional-empty versus required-empty cases |
| ENVL-02 | ordering | Plan 01 collection order and deterministic key normalization |
| ENVL-02 | precision | Plan 01 exact integers, finite numbers, non-finite rejection |
| ENVL-04 | boundary | Plan 02 preparation/route/dump ordering |
| ENVL-04 | empty | Plan 02 no refill from assigns/process/config |
| ENVL-04 | encoding | Plans 01/03 exact attachment/body/digest reconstruction |
| ENVL-04 | precision | Plans 01/03 exact version/digest/adapter-ref retry facts |
| ENVL-05 | unclassified | Plan 02 four-part success and two forced rollback points |
| ENVL-06 | unclassified | Plan 03 full typed readiness/insertion failure matrix; Plan 05 source contract |
| ENVL-07 | unclassified | Plan 04 executable rejection; Plan 05 explicit non-durable source/adopter contract |
| ENVL-08 | empty | Plan 03 missing/empty queue readiness failure |
| ENVL-08 | encoding | Plans 03-05 canonical source-of-truth/runtime/docs checks |

No edge item remains unresolved. RESEARCH A1 remains explicitly bounded to configured-instance/queue readiness plus successful insertion; Phase 153 owns active-consumer proof.

## Prohibition Probe Breadcrumbs

- Generic injection/search-path, tenant access control, and persisted-term/atom safety remain covered by the Phase 150 STRIDE mitigations and are not duplicated as project-specific prohibitions.
- The private-payload-no-archive/admin and no-durable-TaskSupervisor-misrepresentation prohibitions remain descriptor-less, flagged, and unverified until execution.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Complete private recoverable envelope plus atomic durable queue work or no queued work | 01-05 | COVERED | Tracer → atomic enqueue → payload-first readiness → production preflight → source/adopter contract |
| REQ | ENVL-01 | Private payload and ID-only args | 01-02,05 | COVERED | D-01/D-02/D-10 runtime and public boundary |
| REQ | ENVL-02 | Full supported-field round trip | 01-02 | COVERED | Codec plus public-path integration |
| REQ | ENVL-04 | Prepared immutable envelope and route | 01-03 | COVERED | Enqueue ordering and payload-first worker |
| REQ | ENVL-05 | Four-part atomic commit | 01-02 | COVERED | V06 plus forced rollback |
| REQ | ENVL-06 | Oban fail closed | 03,05 | COVERED | Full readiness/insertion matrix and all active source claims |
| REQ | ENVL-07 | TaskSupervisor explicit/non-durable | 04-05 | COVERED | Callable Config preflight, boot separation, and source/adopter contract |
| REQ | ENVL-08 | Canonical queue/no drift | 03-05 | COVERED | Worker source, runtime, production, and source/adopter checks |
| CONTEXT | D-01, D-02, D-03 | Private payload/public projection/lifecycle fields | 01-02,05 | COVERED | No admin/snapshot expansion |
| CONTEXT | D-04, D-05, D-06, D-07, D-08 | Immutable explicit envelope codec, attachments, safe JSON | 01-03 | COVERED | Every decision cited in tasks/truths |
| CONTEXT | D-09, D-10, D-11 | Atomic single/job args/atomic batch | 02-03,05 | COVERED | Per-envelope Multi and exact documented identifiers |
| CONTEXT | D-12, D-13, D-14 | Fail-closed Oban/explicit Task/gateway | 03-05 | COVERED | No bare optional calls and one all-seam cleanup contract |
| CONTEXT | D-15, D-16 | Narrow forward-only legacy compatibility | 01,03,05 | COVERED | No false backfill or complete-envelope claim |
| RESEARCH | — | JSONB payload with version + SHA-256 digest | 01 | COVERED | Resolved recommendation retained |
| RESEARCH | — | Payload-first worker and configured queue readiness | 03-05 | COVERED | Active polling remains Phase 153 |
| RESEARCH | — | Failure injection, migration/prefix/no-backfill tests | 01-02 | COVERED | Wave 0 plus database constraint injectors |
| VALIDATION | — | Nine mapped tasks, task-scoped samplers, and Wave 0 seams | 01-05 | COVERED | Every task has a sub-30-second-target sampler; broad checks are wave gates |

Deferred Phase 151 work, out-of-v2.4 items, historical/provenance artifacts, and the inbound sibling's distinct contract are exclusions, not gaps.

<verification>
After Task 150-05-01 run its focused command. At the Wave 5/phase gate run `mix docs && mix mailglass.docs.check && mix test test/mailglass/outbound/envelope_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/deliver_many_test.exs test/mailglass/outbound/worker_test.exs test/mailglass/migration_test.exs test/mailglass/schema_prefix_hardening_test.exs test/mailglass/application_test.exs test/mailglass/config_test.exs test/mailglass/docs_contract_test.exs test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors && mix compile --no-optional-deps --warnings-as-errors`, then run `mix test --warnings-as-errors` as the full phase gate.
</verification>

<success_criteria>
- All seven ENVL requirement IDs and D-01 through D-16 retain executable plan coverage.
- All 15 deterministic edge rows remain resolved with no silent drop.
- Every active outbound fallback contradiction found by the tracked-file sweep is assigned to Task 150-05-01 and protected by one focused contract.
- No plan implements Phase 151 or rewrites historical/inbound provenance.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-05-SUMMARY.md` when done.
</output>
