---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/outbound/envelope.ex
  - lib/mailglass/outbound/payload.ex
  - lib/mailglass/outbound.ex
  - lib/mailglass/migrations/postgres.ex
  - lib/mailglass/migrations/postgres/v06.ex
  - test/mailglass/outbound/envelope_test.exs
  - test/mailglass/migration_test.exs
  - test/mailglass/schema_prefix_hardening_test.exs
autonomous: true
requirements:
  - ENVL-01
  - ENVL-02
  - ENVL-04
  - ENVL-05
must_haves:
  truths:
    - "D-01/D-02/ENVL-01 [unclassified resolved]: one prepared message round-trips through one tenant-scoped private Payload while Delivery/Event/job/log/telemetry surfaces receive no envelope content."
    - "D-04/D-05/ENVL-02 [boundary]: V1 accepts exactly the documented prepared surface and preserves the native sole-recipient to/cc/bcc discriminator."
    - "D-05/ENVL-02 [adjacency]: ordered address/header/tag/attachment collections and adjacent duplicate values retain their documented order and multiplicity."
    - "D-05/ENVL-02 [empty]: supported optional nil/empty bodies and empty tags, metadata, attachments, and provider options round-trip without inventing or dropping values; required identity fields fail closed when empty."
    - "D-05/D-08/ENVL-02 [ordering/precision]: deterministic key normalization is order-independent, integers remain exact, finite JSON numbers round-trip, and non-finite/unsupported numeric terms fail before persistence."
    - "D-07/ENVL-04 [boundary/encoding]: attachment bytes are read once before persistence, base64-encoded with an explicit marker, and reconstruct byte-for-byte without retaining a path or upload term."
    - "D-03/D-16/ENVL-05: V06 creates an empty, reversible, prefix-qualified payload table and indexes without backfilling incomplete legacy metadata."
  artifacts:
    - path: lib/mailglass/outbound/envelope.ex
      provides: "Explicit V1 allowlisted dump/load and integrity codec"
      exports: ["dump/2", "load/1", "digest/1", "version/0"]
    - path: lib/mailglass/outbound/payload.ex
      provides: "Private tenant/delivery-scoped payload schema and lookup"
      exports: ["changeset/2", "from_envelope/4", "fetch_for_delivery/2"]
    - path: lib/mailglass/migrations/postgres/v06.ex
      provides: "Reversible prefixed mailglass_outbound_payloads DDL"
      exports: ["up/1", "down/1"]
    - path: test/mailglass/outbound/envelope_test.exs
      provides: "Wave 0 fidelity, safety, attachment, and persistence tracer"
  key_links:
    - from: lib/mailglass/outbound/envelope.ex
      to: lib/mailglass/outbound/payload.ex
      via: "Payload.from_envelope/4 stores the normalized map, version, and deterministic SHA-256 digest"
      pattern: "Envelope\\.(version|digest)"
    - from: lib/mailglass/outbound/payload.ex
      to: lib/mailglass/migrations/postgres/v06.ex
      via: "schema fields exactly match the prefixed payload table DDL"
      pattern: "mailglass_outbound_payloads"
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/outbound/envelope.ex
      via: "public deliver_later prepares/selects route, dumps once, and inserts Payload before the existing canonical job"
      pattern: "Envelope\.dump"
  prohibitions:
    - statement: "The private outbound payload must not silently become an adopter-visible sent-message archive, admin viewing source, or public metadata surface."
      status: unverified
      flagged: true
---

<objective>
Create the private-envelope tracer: one public durable request is prepared and route-selected, explicitly encoded, committed with a private Payload and canonical job, then tenant-scoped, integrity-checked, and reconstructed without content loss or public leakage per D-01 through D-09 and D-16.

Purpose: Prove the highest-risk format and schema boundary before atomic enqueue and worker expansion depend on it.
Output: `Envelope`, `Payload`, reversible V06 DDL, and Wave 0 codec/migration/prefix tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-PATTERNS.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VALIDATION.md
@.planning/phases/149-first-send-contract-foundation/149-04-SUMMARY.md
@lib/mailglass/outbound/delivery.ex
@lib/mailglass/schema.ex
@lib/mailglass/migrations/postgres.ex
@lib/mailglass/migrations/postgres/v05.ex
@deps/swoosh/lib/swoosh/email.ex
@deps/swoosh/lib/swoosh/attachment.ex
</context>

<assumption_delta_decision>
Recommendation-first resolution: adopt RESEARCH A2 by storing `envelope_version` plus a lowercase SHA-256 digest over the codec's deterministic canonical representation. For RESEARCH A1, readiness means the optional dependency and configured Oban instance exist, the canonical queue is configured, and insertion succeeds; active polling is deliberately not claimed here because Phase 153 owns generated-host consumption proof. This narrows implementation evidence without weakening D-12.
</assumption_delta_decision>

## Artifacts this phase produces

- `Mailglass.Outbound.Envelope.version/0`, `dump/2`, `load/1`, and `digest/1` in `lib/mailglass/outbound/envelope.ex`.
- `Mailglass.Outbound.Payload` with fields `id`, `tenant_id`, `delivery_id`, `envelope_version`, `envelope_digest`, `envelope`, `scrubbed_at`, `expires_at`, `inserted_at`, and `updated_at`; functions `changeset/2`, `from_envelope/4`, and `fetch_for_delivery/2`.
- `Mailglass.Migrations.Postgres.V06.up/1` and `down/1`; table `mailglass_outbound_payloads`; indexes `mailglass_outbound_payloads_delivery_id_idx`, `mailglass_outbound_payloads_tenant_delivery_idx`, and `mailglass_outbound_payloads_expires_at_idx`.
- A production `Mailglass.Outbound.deliver_later/2` happy path whose ordered Multi includes the new `:payload` operation before the existing canonical `:job` operation.
- `test/mailglass/outbound/envelope_test.exs` as the Wave 0 codec/persistence contract.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 150-01-01: Round-trip one prepared envelope through private storage</name>
  <reversibility rating="costly">D-01/D-03/D-05 establish a versioned stored envelope and reversible V06 schema; changing them later needs a coordinated forward migration, while the explicit version boundary prevents a published-contract break.</reversibility>
  <files>lib/mailglass/outbound/envelope.ex, lib/mailglass/outbound/payload.ex, lib/mailglass/outbound.ex, lib/mailglass/migrations/postgres/v06.ex, test/mailglass/outbound/envelope_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-01 through D-08 and D-16; `150-RESEARCH.md` Summary, Pattern 1, Pattern 2, Open Question 2, and Wave 0 gaps; `150-PATTERNS.md` assignments for envelope/payload/V06; `lib/mailglass/outbound/delivery.ex` schema/changeset conventions; `lib/mailglass/schema.ex`; `lib/mailglass/migrations/postgres.ex`; `lib/mailglass/migrations/postgres/v05.ex`; and the installed Swoosh Email/Attachment structs.</read_first>
  <behavior>
    - Happy path: public `deliver_later/2` prepares a fully populated post-Phase-149 Message, selects its adapter ref, dumps it, and commits exactly one Delivery/Event/Payload/canonical Job; the Payload reloads under the same tenant/delivery, verifies its digest, and reconstructs every D-05 field.
    - Privacy: only the Payload contains subject, bodies, headers, address tuples, attachment bytes, and provider options; test inspection of the codec error context contains no content or address.
    - Attachment: data-backed and readable path-backed attachments reconstruct exact bytes and attributes after the source path changes; unreadable/malformed forms return `%Mailglass.SendError{type: :serialization_failed}` before an insert.
    - Safety: functions, PIDs, references, arbitrary structs, assigns/private state, unknown fields, excessive depth/items/bytes, non-UTF-8 JSON strings, and non-finite numbers fail with a bounded stable `reason_class`; loading keys never creates atoms from persisted input.
  </behavior>
  <action>Start by creating `test/mailglass/outbound/envelope_test.exs`, tag this task's cases with `@tag phase_150_task: "t150_01_01"`, and make one real public `deliver_later/2` request fail because the private envelope/Payload operation does not exist; the test may invoke V06 directly in its isolated setup until Task 150-01-02 advances the migration runner. Implement an explicit string-key envelope-version-one codec per D-04/D-05/D-06/D-08, not Erlang term serialization: allowlist sender, native sole-recipient field/address, reply-to, subject, headers, html/text, stream, ordered tags, public Mailglass metadata, persisted adapter ref, materialized attachments, and recursively bounded JSON provider options. Normalize atom/string JSON keys deterministically while rejecting collisions, unknown terms, and atom creation on load. Encode attachment bytes as base64 with a format marker and preserve filename/content type/disposition/CID/headers per D-07; rescue file errors into non-PII reason classes and never persist paths/uploads. Implement deterministic canonical encoding and `:crypto.hash(:sha256, bytes)` for the lowercase digest. Create private `Payload` using `Mailglass.Schema`, changeset validation, one-row-per-delivery uniqueness, tenant-scoped `fetch_for_delivery/2`, and digest verification before `Envelope.load/1`; do not export it from the root facade or expose it to admin. Implement exact V06 DDL: prefix-qualified `mailglass_outbound_payloads` with UUID primary key; non-null text `tenant_id`; non-null UUID `delivery_id` referencing the prefixed `mailglass_deliveries` with `on_delete: :delete_all`; non-null integer `envelope_version`; non-null text `envelope_digest`; non-null JSONB/map `envelope`; nullable `scrubbed_at` and `expires_at` UTC microsecond timestamps; and non-null inserted/updated UTC microsecond timestamps. Add the named unique delivery index and tenant+delivery index, plus exactly `index(:mailglass_outbound_payloads, [:expires_at], name: :mailglass_outbound_payloads_expires_at_idx, where: "expires_at IS NOT NULL", prefix: prefix)` so the expiry index is prefix-qualified and partial with the exact predicate `expires_at IS NOT NULL`; pass `prefix:` to every table/reference/index operation. In `Outbound`, after existing preparation and adapter-route resolution, dump once and extend the current single-send Multi with a real `:payload` insert between `:event_queued` and `:job`; keep job args unchanged. Retain this public happy path as production code and retain the failing-first test; Task 150-01-02 wires the runner and migration hardening, while Plan 02 expands privacy, rollback, and batch cases.</action>
  <acceptance_criteria>
    - D-05 fidelity assertion: sender, native recipient field, reply-to, subject, headers, HTML, plaintext, stream, tags, metadata, adapter ref, attachments, and safe provider options compare equal after the public durable stored round trip.
    - D-06/D-08 safety assertion: rejected terms produce `:serialization_failed` with only a stable reason class/field category and do not create atoms, payload rows, or leak values.
    - D-07 TOCTOU assertion: the reconstructed attachment uses enqueue-time bytes even after a source file is changed or removed.
    - D-01/D-03 storage assertion: the row is tenant scoped, one per delivery, versioned, digest-verified, and carries explicit Phase-151 lifecycle timestamp fields.
    - Migration assertion: every DDL operation is explicitly prefixed and V06 performs no historical-row rewrite.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/envelope_test.exs --only phase_150_task:t150_01_01 --warnings-as-errors</automated>
  </verify>
  <done>One complete supported public durable request commits Delivery/Event/Payload/canonical Job and survives a private-store round trip with exact version-one fidelity, tenant isolation, deterministic integrity, and attachment materialization per D-01 through D-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 150-01-02: Prove reversible prefix-safe V06 migration behavior</name>
  <reversibility rating="costly">V06 is forward-only for adopter data and later phases depend on its columns, while its explicit down path remains safe before payload rows are relied on.</reversibility>
  <files>lib/mailglass/migrations/postgres/v06.ex, lib/mailglass/migrations/postgres.ex, test/mailglass/migration_test.exs, test/mailglass/schema_prefix_hardening_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-03/D-16, `150-VALIDATION.md` Wave 0 migration gaps, `150-PATTERNS.md` V06 assignment, all of `lib/mailglass/migrations/postgres/v05.ex`, the prefix migration describe in `test/mailglass/migration_test.exs`, and scratch-schema/hostile-search-path setup in `test/mailglass/schema_prefix_hardening_test.exs`.</read_first>
  <behavior>
    - Up from V05 creates only the payload table and three named indexes in the supplied schema; public-schema decoys stay untouched under a hostile search path.
    - Up preserves every pre-existing Delivery metadata byte and creates zero payload rows—there is no lossy/falsely complete legacy backfill per D-16.
    - Down from V06 drops only the supplied schema's indexes/table; V05 objects and decoys survive; up→down→up succeeds.
    - A forced DDL failure rolls back the migration transaction without advancing the schema migration version or leaving a partial table/index set.
  </behavior>
  <action>Advance `Mailglass.Migrations.Postgres.@current_version` from 5 to 6, then extend the existing scratch-prefix harness, never the configured baseline schema. Tag every new V06 case with `@tag phase_150_task: "t150_01_02"`. Assert the exact V06 columns, nullability, foreign key target/delete action, unique/secondary indexes, and the prefix-qualified `mailglass_outbound_payloads_expires_at_idx` catalog definition whose predicate normalizes to `expires_at IS NOT NULL`, plus the current version. Verify the predicate after up, its absence after down, and its exact restoration after the second up. Seed a legacy queued Delivery with old reconstruction metadata before running V06 and prove its metadata is byte-identical afterward and payload count is zero. Set a hostile `search_path` with decoy objects in public, exercise direct V06 up/down and the version runner, and verify every object resolves only under the configured prefix. Add a controlled collision or invalid-DDL case inside the migration transaction to prove rollback and version-table honesty. Keep tests `async: false` and clean scratch schemas in `on_exit`.</action>
  <acceptance_criteria>
    - Exact DDL assertion: all ten fields, one foreign key, and all three named indexes match Task 150-01-01; PostgreSQL catalog inspection proves `mailglass_outbound_payloads_expires_at_idx` is in the supplied prefix with predicate `expires_at IS NOT NULL` after each up and is absent after down.
    - Prefix assertion: table, constraint, reference, and every index land in the supplied schema with no ambient-search-path dependency.
    - Forward-only assertion: V06 creates no payload rows and changes no Delivery metadata.
    - Reversibility assertion: V06 down removes only V06 objects and a second up succeeds.
    - Rollback assertion: a failed migration leaves neither partial V06 objects nor a falsely advanced version.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/migration_test.exs test/mailglass/schema_prefix_hardening_test.exs --only phase_150_task:t150_01_02 --warnings-as-errors</automated>
  </verify>
  <done>V06 is exact, reversible, transactionally honest, prefix-safe under hostile search paths, and performs no lossy legacy backfill per D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| prepared Message/adapter ref → Envelope codec | Rich adopter/provider terms become durable JSON. |
| attachment path/upload → persisted bytes | Mutable local filesystem content crosses into retry-stable storage. |
| tenant worker context → Payload lookup | A stable delivery ID must not authorize another tenant's private content. |
| migration prefix → PostgreSQL objects | Ambient search path must not redirect private-table DDL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-01 | Information Disclosure | Payload/Envelope/public surfaces | high | mitigate | D-01/D-02 private table, root non-export, tenant-scoped reads, and tests proving content absent from public surfaces/error context. |
| T-150-02 | Tampering / Denial of Service | `Envelope.load/1` | high | mitigate | Explicit version/field allowlist, bounded recursion/size, digest verification, string dispatch without persisted-to-atom conversion, and hostile term tests. |
| T-150-03 | Tampering / Information Disclosure | attachment materialization | high | mitigate | D-07 one-time bounded read, byte encoding marker, no retained path/upload, and mutate/delete-after-enqueue TOCTOU proof. |
| T-150-04 | Elevation of Privilege | `Payload.fetch_for_delivery/2` | high | mitigate | Require both restored tenant ID and delivery ID through `Tenancy.scope/1`; cross-tenant negative test returns no payload. |
| T-150-05 | Tampering | V06 prefix resolution | high | mitigate | Validate/thread prefix on every table/reference/index operation and run hostile-search-path plus rollback tests. |
| T-150-SC | Tampering | package installs | high | mitigate | No install task exists; any dependency deviation stops for package-legitimacy audit, slopcheck, and blocking review of `[ASSUMED]`/`[SUS]`. |
</threat_model>

<verification>
Run each focused command after its task. At wave completion run `mix test test/mailglass/outbound/envelope_test.exs test/mailglass/migration_test.exs test/mailglass/schema_prefix_hardening_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- The V1 private envelope and exact V06 table exist with tenant scope, integrity, and no public exposure.
- All ENVL-02 boundary, adjacency, empty, ordering, encoding, and precision cases are automated.
- V06 up/down, lock rollback, hostile search path, and no-backfill behavior are green.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-01-SUMMARY.md` when done.
</output>
