---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "03"
type: execute
wave: 3
depends_on:
  - 150-02
files_modified:
  - lib/mailglass/outbound.ex
  - lib/mailglass/outbound/worker.ex
  - lib/mailglass/optional_deps/oban.ex
  - test/mailglass/outbound/deliver_later_test.exs
  - test/mailglass/outbound/worker_test.exs
autonomous: true
requirements:
  - ENVL-04
  - ENVL-06
  - ENVL-08
must_haves:
  truths:
    - "D-04/ENVL-04: worker retries restore tenant then load/digest-check the immutable Payload and never render, read assigns/process state, or reselect a changed route."
    - "D-12/ENVL-06 [unclassified resolved]: explicit :oban returns `%Mailglass.SendError{type: :adapter_failure}` with a bounded reason_class for dependency absent, instance unusable, canonical queue absent/wrong, or insert failure, and creates no queued work."
    - "D-10/D-12/ENVL-08 [empty]: missing queue configuration is a readiness failure rather than an assumed default."
    - "D-10/D-12/ENVL-08 [encoding]: atom/string/config representations are normalized only for comparison; the sole accepted worker queue identity is canonical `mailglass_outbound`, and drift fails closed."
    - "D-14: all optional Oban module/config/query/insert calls remain inside `Mailglass.OptionalDeps.Oban`, preserving no-optional-deps compilation."
    - "D-15/D-16: payload-first reads fall back only for recognizably pre-v2.4 queued rows with old metadata keys; they never promote incomplete metadata into a V1 Payload or claim complete fidelity."
  artifacts:
    - path: lib/mailglass/optional_deps/oban.ex
      provides: "Fail-closed Oban readiness and transaction-safe optional gateway"
      exports: ["ready?/1", "insert/4"]
    - path: lib/mailglass/outbound/worker.ex
      provides: "Canonical queue helper and payload-first worker dispatch entry"
      exports: ["queue/0", "new/1", "perform/1"]
    - path: lib/mailglass/outbound.ex
      provides: "Payload-first dispatch-by-id with isolated legacy queued reader"
  key_links:
    - from: lib/mailglass/outbound/worker.ex
      to: lib/mailglass/outbound.ex
      via: "tenant middleware calls payload-first dispatch_by_id"
      pattern: "TenancyMiddleware\.wrap_perform"
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/outbound/payload.ex
      via: "tenant-scoped Payload fetch precedes any legacy metadata branch"
      pattern: "Payload\.fetch_for_delivery"
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/optional_deps/oban.ex
      via: "explicit :oban selection checks readiness before dumping/transaction"
      pattern: "OptionalDeps\.Oban\.ready"
  prohibitions:
    - statement: "Explicit durable Oban selection must not silently degrade to Task.Supervisor or report queued work when readiness or insertion is uncertain."
      status: unverified
      flagged: true
---

<objective>
Make durable execution payload-first and readiness truthful: the worker reconstructs immutable tenant-scoped V1 input, legacy queued work has a narrow prefix-safe bridge, and explicit Oban selection fails closed on every unusable or drifted state per D-04/D-10/D-12/D-14 through D-16.

Purpose: Ensure persisted work is both recoverable and honestly runnable before documentation advertises durability.
Output: Payload-first worker path, isolated legacy reader, canonical queue helper, and fail-closed optional Oban gateway.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-PATTERNS.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VALIDATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-02-SUMMARY.md
@lib/mailglass/outbound/worker.ex
@lib/mailglass/optional_deps/oban.ex
@test/mailglass/outbound/worker_test.exs
</context>

<assumption_delta_decision>
Apply the research recommendation: `ready?/1` proves optional dependency availability, resolves the configured Oban instance without raising, and confirms its queue configuration contains `Worker.queue/0`; successful insertion remains the transactional proof that the producer path is usable. It does not claim an actively polling worker—Phase 153 owns that stronger generated-host evidence.
</assumption_delta_decision>

## Artifacts this phase produces

- `Mailglass.Outbound.Worker.queue/0` returning the canonical atom `:mailglass_outbound` without changing `new/1` options or args.
- `Mailglass.OptionalDeps.Oban.ready?/1` returning `:ok | {:error, reason_class}` for `:dependency_unavailable`, `:instance_unavailable`, and `:canonical_queue_unavailable`; strengthened prefix-aware `insert/4` never returns an unchanged Multi for explicit durable use.
- Package-private `load_payload_prepared/2` and `load_legacy_pre_v24_queued_message/1` seams in `lib/mailglass/outbound.ex`.
- Worker/readiness regression cases for route/template/config mutation, cross-tenant lookup, version/digest failure, legacy identification, queue drift, and no-optional-deps compilation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 150-03-01: Dispatch immutable payload input under restored tenancy</name>
  <reversibility rating="costly">D-04/D-15 establish payload-first worker recovery and a bounded legacy compatibility seam inherited by Phase 151.</reversibility>
  <files>lib/mailglass/outbound.ex, lib/mailglass/outbound/worker.ex, test/mailglass/outbound/worker_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-04/D-10/D-12/D-13/D-15/D-16; `150-RESEARCH.md` payload-first example and Pitfall 5; `150-PATTERNS.md` Worker assignment; Plan 01 Payload/Envelope interfaces; the `Mailglass.Outbound` moduledoc plus `deliver_later/2`, `deliver_many/2`, and `dispatch_by_id/1` docs; current metadata rehydration helpers; Worker `perform/1`; and all manual Oban setup/legacy assertions in `worker_test.exs`.</read_first>
  <behavior>
    - A job with only delivery/tenant IDs restores tenant, fetches that tenant's Payload, verifies version/digest, reconstructs prepared input, and dispatches its persisted adapter ref and message fields.
    - Mutating template code, renderer/routing config, adapter default, process assigns, and source attachment path after enqueue does not alter worker input.
    - Reusing a valid delivery ID under another tenant cannot read the payload or private values.
    - A no-Payload queued row is eligible for legacy reconstruction only when its pre-v2.4 metadata marker set is present; no modern/corrupt/unsupported-version row is silently reconstructed or backfilled.
  </behavior>
  <action>Make Worker expose `queue/0` as the single canonical queue value while retaining compile-time `use Oban.Worker, queue: :mailglass_outbound`, max attempts, uniqueness, and exact args. In `dispatch_by_id/1`, retain tenant-scoped Delivery lookup, then fetch Payload using the restored tenant and delivery ID before any metadata helper. Verify digest/version and reconstruct through Envelope; dispatch the stored adapter reference and message without calling Renderer, preparation, route resolution, assigns, or current config. Isolate current metadata rehydration behind `load_legacy_pre_v24_queued_message/1`, gated by queued status, missing Payload, and the complete recognizable old reconstruction-key set; it may preserve old behavior but must return an explicitly legacy result internally, create no Payload, and make no complete envelope-version-one fidelity claim per D-15/D-16. Do not implement Phase 151's missing/corrupt/scrubbed terminal classification—return typed/bounded errors through existing worker result normalization. Add manual Oban tests tagged `@tag phase_150_task: "t150_03_01"` with `async: false`, unique sentinels, and config/file mutation between enqueue and perform. Plan 05 owns the repository-wide D-12/D-13 source-documentation cleanup after this runtime behavior is complete.</action>
  <acceptance_criteria>
    - Immutable-input assertion: post-enqueue template/route/config/path mutations do not change captured provider input.
    - Tenant assertion: cross-tenant delivery ID lookup cannot return Payload or leak a private sentinel.
    - Payload-first assertion: a modern payload-backed row never consults legacy reconstruction metadata.
    - Legacy assertion: only identified old queued rows use the isolated reader; no backfill/V1 claim occurs.
    - Worker-contract assertion: args remain exactly `delivery_id` and `mailglass_tenant_id`; queue remains `:mailglass_outbound`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/worker_test.exs --only phase_150_task:t150_03_01 --warnings-as-errors</automated>
  </verify>
  <done>Worker retries consume the immutable private envelope under restored tenant context, with a narrow forward-only bridge for identified old queued rows per D-04/D-15/D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 150-03-02: Fail closed on unusable Oban and canonical queue drift</name>
  <reversibility rating="costly">D-12/D-14 make readiness behavior part of the durable public promise while keeping exact Oban calls replaceable behind the gateway.</reversibility>
  <files>lib/mailglass/outbound.ex, lib/mailglass/outbound/worker.ex, lib/mailglass/optional_deps/oban.ex, test/mailglass/outbound/deliver_later_test.exs, test/mailglass/outbound/worker_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-10/D-12/D-13/D-14; `150-RESEARCH.md` Open Question 1, fail-closed example, and Pitfalls 3/4; `150-PATTERNS.md` OptionalDeps assignment; the `Mailglass.OptionalDeps.Oban` moduledoc and docs for `available?/0`, prefix-aware `insert/4`, and `insert_all/1`; `enqueue_via_async_adapter/3`; Worker options; and env/Oban configuration restoration patterns in both test files.</read_first>
  <behavior>
    - Explicit :oban with dependency unavailable, unregistered/missing instance, missing queues, or a queue set not containing canonical mailglass_outbound returns adapter_failure and creates no four-part work.
    - A canonical configured queue plus successful transactional insertion passes readiness even though this phase does not claim an active poller.
    - Job insertion failure maps to bounded adapter_failure and rolls back all facts; there is no Task.Supervisor dispatch or warning containing private data.
    - The project compiles with optional dependencies removed; all Oban references remain gateway/conditional-worker confined.
  </behavior>
  <action>Extend `Mailglass.OptionalDeps.Oban` with `ready?/1`, where the required input is the canonical queue atom supplied as `Worker.queue/0` by Outbound; the gateway resolves the default configured Oban instance and checks that instance's queue configuration. Inside the gateway only, check module availability, resolve/query the configured Oban instance safely, normalize configured queue keys without creating atoms, and return stable reason classes for dependency, instance, and canonical-queue failures. Strengthen the prefix-aware transactional `insert/4` from Plan 02 so absence cannot silently return the input Multi on an explicit durable path. In `enqueue_via_async_adapter/3`, branch exactly on `:task_supervisor` versus `:oban`; run readiness before envelope transaction for `:oban`, map all readiness/insertion errors to `%Mailglass.SendError{type: :adapter_failure}` with only `reason_class`, and remove the implicit available?-based Task downgrade per D-12. Replace the existing test whose name promises the same queued return regardless of Oban availability with explicit-adapter cases: TaskSupervisor retains its queued non-durable result, while unavailable selected Oban returns typed failure and no work. Add all negative configuration and insertion-failure cases under `@tag phase_150_task: "t150_03_02"`, restoring Application/Oban state in `on_exit`. Keep direct Oban calls absent outside the gateway/conditional Worker. Plan 05 owns source-documentation cleanup for this gateway and every related public/current seam once the final runtime contract exists. The no-optional-dependencies compilation proof runs at the Wave 3 gate because it is broader than the sub-30-second per-task sampler.</action>
  <acceptance_criteria>
    - ENVL-06 matrix assertion: dependency absent, instance absent/unusable, queue missing/wrong, and insertion failure each return typed adapter_failure and zero queued work.
    - ENVL-08 assertion: readiness derives the accepted identity from Worker.queue/0 and rejects stale `:mailglass`, string/atom drift, missing, and empty queue configurations.
    - No-fallback assertion: none of the explicit :oban failures invokes AsyncAdapter dispatch or Task.Supervisor.
    - Privacy assertion: reason context/log/telemetry contains only bounded classifications and no config secrets or private envelope values.
    - Optional-dependency assertion: `mix compile --no-optional-deps --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/worker_test.exs --only phase_150_task:t150_03_02 --warnings-as-errors</automated>
  </verify>
  <done>Explicit Oban selection is truthful and fail-closed across every readiness/insertion failure, with one canonical queue and no hidden Task.Supervisor downgrade per D-10/D-12/D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ID-only Oban job → tenant-scoped Payload | Untrusted persisted identifiers request private message recovery. |
| persisted JSON → runtime structs | Stored keys/versions/digest become executable dispatch input. |
| host Oban configuration → durable-success claim | Optional runtime/queue state determines whether queued means runnable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-09 | Information Disclosure / Elevation of Privilege | payload-first worker lookup | high | mitigate | Restore tenant from ID-only args, query by tenant+delivery, verify digest/version, and test cross-tenant denial. |
| T-150-10 | Tampering | persisted key/version loading | high | mitigate | Envelope string-key allowlist, no atom creation, exact V1/digest checks before reconstruction. |
| T-150-11 | Repudiation / Denial of Service | Oban readiness/enqueue | high | mitigate | D-12 typed fail-closed matrix, canonical queue comparison, transactional insert rollback, and no fallback assertion. |
| T-150-12 | Information Disclosure | readiness errors/logging | medium | mitigate | Stable reason_class allowlist only; tests refute queue config secrets and envelope sentinels. |
| T-150-SC | Tampering | package installs | high | mitigate | No install task exists; optional integration uses the existing audited dependency only. |
</threat_model>

<verification>
Run focused checks after each task. At wave completion run `mix test test/mailglass/outbound/envelope_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/deliver_many_test.exs test/mailglass/outbound/worker_test.exs --warnings-as-errors && mix compile --no-optional-deps --warnings-as-errors`.
</verification>

<success_criteria>
- Worker execution is immutable, payload-first, tenant-scoped, and exact-version/digest checked.
- Explicit Oban selection never degrades and every readiness/insertion failure produces no queued work.
- Canonical queue identity and optional-dependency isolation are executable contracts.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-03-SUMMARY.md` when done.
</output>
