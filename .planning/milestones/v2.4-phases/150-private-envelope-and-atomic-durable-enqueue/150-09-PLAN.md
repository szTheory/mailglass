---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "09"
type: execute
wave: 6
depends_on:
  - 150-05
files_modified:
  - scripts/no_optional_deps_runtime_smoke.sh
  - test/runtime/no_optional_deps_public_send.exs
  - mix.exs
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements:
  - ENVL-06
must_haves:
  truths:
    - "D-12/D-14/ENVL-06: a separately compiled and executed runtime has no Oban or conditional Worker module loaded, yet the public deliver_later/2 path returns adapter_failure/dependency_unavailable rather than raising."
    - "D-12/ENVL-06: the dependency-free public-send probe leaves Delivery, Event, Payload, Oban job, Fake-adapter delivery, and Task.Supervisor dispatch counts unchanged."
    - "D-14/ENVL-06: the genuine no-optional-dependencies runtime probe is a repeatable local alias and a required Postgres-backed CI step, not a source inspection or compile-only claim."
  artifacts:
    - path: scripts/no_optional_deps_runtime_smoke.sh
      provides: "Isolated no-optional-dependencies compile-and-run harness"
    - path: test/runtime/no_optional_deps_public_send.exs
      provides: "Public deliver_later typed-result and zero durable/queue/provider effects runtime assertions"
    - path: .github/workflows/ci.yml
      provides: "Required CI execution of the dependency-free public-send proof"
  key_links:
    - from: scripts/no_optional_deps_runtime_smoke.sh
      to: test/runtime/no_optional_deps_public_send.exs
      via: "isolated MIX_BUILD_PATH compiles without optional dependencies, then runs the probe without recompiling or dependency checks"
      pattern: "no-optional-deps"
    - from: .github/workflows/ci.yml
      to: mix.exs
      via: "Postgres-backed required job invokes the named no-optional runtime alias"
      pattern: "verify.no_optional_runtime"
---

<objective>
Close the fail-closed runtime evidence gap by compiling Mailglass without optional dependencies in an isolated artifact, executing a valid public `deliver_later/2` call in that same Oban-free runtime, and proving the typed dependency error has zero durable or adapter effects per D-12/D-14.

Purpose: Upgrade the current compile-only evidence into the genuine runtime behavior promised to adopters.
Output: An isolated runtime probe, local alias, and required CI execution.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VERIFICATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-REVIEW.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-REVIEW-FIX.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-03-SUMMARY.md
@lib/mailglass/outbound.ex
@lib/mailglass/optional_deps/oban.ex
@mix.exs
@.github/workflows/ci.yml
@scripts/preflight_postgres.sh
@test/support/oban_helpers.ex

<interfaces>
`mix compile --no-optional-deps` produces the candidate artifact under an isolated `MIX_BUILD_PATH`, but `mix run` is not an acceptable launcher because Mix may add dependency paths from the normal project graph. The proof launcher must instead invoke `elixir` directly from an empty temporary working directory with `ERL_LIBS`, `ERL_AFLAGS`, `ERL_FLAGS`, `ELIXIR_ERL_OPTIONS`, `MIX_BUILD_PATH`, and `MIX_DEPS_PATH` unset, adding only the isolated artifact's allowlisted `lib/*/ebin` directories via repeated `-pa`. The required `support_contract_core` CI job already provisions PostgreSQL and creates the test database, making it the bounded existing lane for this runtime probe.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 150-09-01: Execute the public durable-send failure contract in a genuinely Oban-free runtime</name>
  <reversibility rating="reversible">The probe and CI wiring are internal verification surfaces and can move without changing the public runtime contract.</reversibility>
  <files>scripts/no_optional_deps_runtime_smoke.sh, test/runtime/no_optional_deps_public_send.exs, mix.exs, .github/workflows/ci.yml</files>
  <read_first>Read CR-02 and the no-optional behavior item in `150-VERIFICATION.md`; D-12/D-14; `enqueue_via_async_adapter/3`; `OptionalDeps.Oban.ready?/1`; Mix's existing isolated `_build/ci_fast` pattern; `ci.full` and `support_contract_core` job setup; and existing test DB migration/Oban-table setup.</read_first>
  <behavior>
    - Before launch, the harness derives the complete optional-application denylist from source-controlled `Mailglass.MixProject.project()[:deps]` entries whose options set `optional: true`, asserts that list contains `oban`, derives the isolated artifact's `lib/*/ebin` path manifest, proves every entry resolves under the temporary build root, and rejects every derived optional application root (currently Oban, OpenTelemetry, MJML, gen_smtp, Sigra, Phoenix LiveReload, and Igniter).
    - In the launched process, the probe audits `:code.get_path/0`, asserts every project/dependency ebin comes from the isolated build manifest, asserts `:code.lib_dir/1` reports no root for every optional application above, and asserts `Code.ensure_loaded?(Oban)`, `Code.ensure_loaded?(Oban.Worker)`, and `Code.ensure_loaded?(Mailglass.Outbound.Worker)` are all false before configuring or calling Mailglass.
    - A valid one-recipient, valid-body public Message sent with explicit `async_adapter: :oban` returns `{:error, %Mailglass.SendError{type: :adapter_failure, context: %{reason_class: :dependency_unavailable}}}` and does not raise or fall through to Task.Supervisor.
    - Schema-qualified before/after counts for Deliveries, Events, Payloads, and `public.oban_jobs` (when that host table exists) are identical; Fake deliveries and supervised Task dispatch effects remain zero. The ordinary bounded public failure telemetry span may occur and is not misclassified as queued work.
    - The script fails if the isolated build path contains an optional root, if the direct launcher sees any normal project/dependency ebin, if Oban is loadable, if required tables/config are unavailable, or if any assertion is not measured.
  </behavior>
  <action>Create executable `scripts/no_optional_deps_runtime_smoke.sh` with `mktemp -d`, a cleanup trap, and explicit temporary build/run directories; never clean or overwrite the developer's normal `_build`. Before building, use a controller-only Mix invocation to read the source-controlled `Mailglass.MixProject.project()[:deps]` tuples and write the application names whose dependency options contain `optional: true` to a temporary denylist; this controller is not the proof process. Fail if the list is empty or does not contain `oban`, so parser/config drift cannot weaken the proof. In `MIX_ENV=test`, compile into the temporary build root with `mix compile --no-optional-deps --warnings-as-errors`. For every derived optional application—including the current `oban`, `opentelemetry`, `mjml`, `gen_smtp`, `sigra`, `phoenix_live_reload`, and `igniter` set—fail if a corresponding directory exists under the isolated build's `lib/`. Build a deterministic launcher manifest from every remaining isolated `lib/*/ebin`, reject missing/non-directory entries and any realpath outside the temporary build root, and pass both manifests to the probe in environment variables. Change into an empty temporary run directory and launch the absolute probe path with the system `elixir` executable, not Mix: unset `ERL_LIBS`, `ERL_AFLAGS`, `ERL_FLAGS`, `ELIXIR_ERL_OPTIONS`, `MIX_BUILD_PATH`, and `MIX_DEPS_PATH`, then supply only one `-pa` argument per manifest ebin. This direct process may retain only OTP/Elixir standard-library paths plus the explicit isolated manifest; it has no normal project `_build` or dependency-source ebin path.

In `test/runtime/no_optional_deps_public_send.exs`, parse the path manifest, derived optional-app manifest, and temporary build root from the environment before starting applications. Audit `:code.get_path/0`: every non-OTP/non-Elixir ebin must exactly match the manifest and resolve under the temporary build root; fail on the normal repository `_build`, `deps/*/ebin`, current-directory beam roots, or any unmanifested ebin. For every source-controlled denied optional application, require `:code.lib_dir(app) == {:error, :bad_name}` (or the runtime-equivalent missing-app result); conversion from the bounded source-controlled app-name manifest may create atoms, but persisted/runtime input must not. Additionally require `Code.ensure_loaded?(Oban)`, `Code.ensure_loaded?(Oban.Worker)`, and `Code.ensure_loaded?(Mailglass.Outbound.Worker)` to be false. These assertions run before reading configuration, starting Mailglass, or probing behavior, so an accidental Oban path cannot produce a green result.

Load `config/config.exs` with `Config.Reader.read!` using `env: :test`, apply the returned application environment, start only the required non-optional application closure from the manifest, start `Mailglass.TestRepo`, and run the existing Mailglass migration API so the required private/public tables exist before baseline counts. Query schema-qualified Delivery/Event/Payload counts and use `to_regclass('public.oban_jobs')` to record either a missing table or its count; checkout Fake as a provider negative control and snapshot `Task.Supervisor.children(Mailglass.TaskSupervisor)`. Build a valid default-tenant transactional Message with one recipient and a body, invoke public `Mailglass.Outbound.deliver_later(message, async_adapter: :oban)` in this same pruned process, and assert the exact `%Mailglass.SendError{type: :adapter_failure, context: %{reason_class: :dependency_unavailable}}`. Recheck every durable/job/provider/task observation byte-for-byte/count-for-count. Permit only the normal bounded public failure telemetry span and assert it has no PII or queued-success claim. Do not mock `available?/0`, purge modules from a normal runtime, invoke `mix run`, or count compilation as runtime evidence.

Add `verify.no_optional_runtime` to `mix.exs` with preferred `:test` environment and point it at the shell harness. Add a named step to the existing required Postgres-backed `support_contract_core` CI job after test DB creation; keep the compile-only `compile_no_optional_deps` leaf unchanged because it intentionally has no database service. If runtime invocation exposes a source defect, stop and report it rather than broadening this plan's file ownership; CR-02's production fix is already present and this plan owns proof only.</action>
  <verify>
    <automated>MIX_ENV=test mix verify.no_optional_runtime</automated>
  </verify>
  <done>A separate runtime with Oban genuinely absent invokes the public send API, returns the stable dependency_unavailable SendError, and proves zero durable/queue/provider/Task fallback effects; the proof is repeatable locally and required in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| normal dependency cache → isolated runtime artifact | Cached Oban beams must not create a false-green dependency-free proof. |
| public send API → optional gateway | The caller must receive a typed failure before any queue or fallback effect. |
| runtime probe → PostgreSQL/Fake/task observations | Every claimed zero durable, queue, provider, and fallback effect must be measured without hiding missing inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-30 | Repudiation | no-optional runtime harness | high | mitigate | Direct `elixir` launcher from an empty directory, isolated allowlisted `-pa` manifest, optional-root denylist, in-process code-path/application/module absence assertions, and required CI invocation. |
| T-150-31 | Denial of Service | public `deliver_later/2` without Oban | high | mitigate | Exact typed-result assertion in the dependency-free process; any raise fails the script. |
| T-150-32 | Tampering / Repudiation | zero-effects claim | high | mitigate | Schema-qualified before/after facts plus adapter and Task.Supervisor counters; missing tables/config fail explicitly. |
| T-150-SC | Tampering | package installs | high | mitigate | The harness uses the existing lock and installs no new package. |
</threat_model>

## Source Coverage Audit — Gap Closure Set 150-06 through 150-09

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Complete private recoverable envelope plus atomic queue work or no queued work | 06-09 plus completed 01-05 | COVERED | Failed fidelity and all four behavior-unverified proofs are assigned. |
| REQ | ENVL-01 | Private payload and ID-only args | completed 01-05 | COVERED | Verifier marked satisfied; 09 rechecks zero effects only. |
| REQ | ENVL-02 | Full supported-field round trip and explicit rejection | 06 | COVERED | adapter_ref, nil semantics, ordered duplicates, JSON bounds, non-finite rejection, attachments. |
| REQ | ENVL-04 | Immutable prepared envelope/route before retry | 06,08 | COVERED | decoded route plus genuine queued-worker mutation proof. |
| REQ | ENVL-05 | Atomic durable facts and prefix-safe legacy path | completed 01-05, 07 | COVERED | Atomicity already verified; 07 closes exact V06 lifecycle evidence. |
| REQ | ENVL-06 | Oban absent/unusable fails closed | completed 03-05, 09 | COVERED | 09 executes the public API in a truly dependency-free runtime. |
| REQ | ENVL-07 | TaskSupervisor explicit/non-durable | completed 04-05 | COVERED | Verifier marked satisfied; no reimplementation. |
| REQ | ENVL-08 | Canonical queue/no drift | completed 03-05 | COVERED | Verifier marked satisfied; retained by 08/09 harnesses. |
| CONTEXT | D-01, D-02, D-03 | Private boundary/schema/lifecycle fields | completed 01-05, 07 | COVERED | 07 verifies exact private schema; lifecycle remains Phase 151. |
| CONTEXT | D-04, D-05, D-06, D-07, D-08 | Immutable complete bounded codec and attachments | 06,08 | COVERED | Every failed codec/retry item has executable proof. |
| CONTEXT | D-09, D-10, D-11 | Atomic single/batch enqueue and ID-only jobs | completed 02-05, 08-09 | COVERED | Existing atomic proof retained; real job/runtime negative controls added. |
| CONTEXT | D-12, D-13, D-14 | Fail-closed Oban, explicit Task, optional gateway | completed 03-05, 09 | COVERED | Runtime absence proof closes the remaining verifier item. |
| CONTEXT | D-15, D-16 | Narrow forward-only legacy compatibility | completed 03-05, 07 | COVERED | Exact no-backfill/down/up evidence; no false V1 promotion. |
| RESEARCH | — | Explicit JSONB V1 codec, materialized attachments, bounded validation | 06 | COVERED | Implements original Wave 0 contract that execution under-tested. |
| RESEARCH | — | Payload-first immutable worker and configured route | 08 | COVERED | Real queued job with changed live state. |
| RESEARCH | — | V06 failure/prefix/no-backfill tests | 07 | COVERED | Dedicated hostile catalog lifecycle. |
| RESEARCH | — | Optional dependency compile/runtime boundary | 09 | COVERED | Separate Oban-free runtime, not compile/source inference. |

Phase 151 wire-equivalence, outcome classification, payload scrubbing/retention/recovery, and missing/corrupt terminal handling remain explicit exclusions. Phase 153 retains generated clean-host and actively polling consumer proof.

<verification>
Run the focused alias. At the full gap-closure gate run `mix test test/mailglass/outbound/envelope_test.exs test/mailglass/v06_migration_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/worker_test.exs --warnings-as-errors && MIX_ENV=test mix verify.no_optional_runtime`, followed by `mix test --warnings-as-errors` after all Wave 6/7 plans complete.
</verification>

<success_criteria>
- The public API is invoked in an artifact that can prove Oban and Worker are absent.
- The exact stable dependency_unavailable result and zero effects are measured.
- Local and CI gates execute the proof without mutating the normal build tree.
- The complete GOAL/REQ/RESEARCH/CONTEXT audit has no unplanned Phase 150 verifier item and no Phase 151/153 scope theft.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-09-SUMMARY.md` when done.
</output>
