---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "08"
type: execute
wave: 7
depends_on:
  - 150-06
files_modified:
  - lib/mailglass/outbound.ex
  - test/mailglass/outbound/worker_test.exs
autonomous: true
gap_closure: true
requirements:
  - ENVL-04
must_haves:
  truths:
    - "D-04/ENVL-04: a real public Oban enqueue captures rendered content and adapter_ref before the async boundary, and Worker.perform/1 later sends those stored values after live template/assign and tenancy-route state changes."
    - "D-04/ENVL-04: the queued worker performs no render/template callback and no tenancy route-selection callback during retry."
    - "D-04/D-05/ENVL-04: modern payload-backed dispatch uses the adapter_ref returned by the decoded V1 envelope and fails closed if the public Delivery projection disagrees."
  artifacts:
    - path: test/mailglass/outbound/worker_test.exs
      provides: "Real queued-job retry proof with mutable live template and route negative controls"
    - path: lib/mailglass/outbound.ex
      provides: "Payload-decoded route consumption without re-render or route reselection"
  key_links:
    - from: test/mailglass/outbound/worker_test.exs
      to: lib/mailglass/outbound.ex
      via: "public deliver_later creates the Oban job, then Worker.perform dispatches its private payload after live-state mutation"
      pattern: "deliver_later.*Worker\\.perform"
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/outbound/payload.ex
      via: "dispatch_by_id consumes the decoded message and persisted adapter_ref from Payload"
      pattern: "load_payload_prepared"
---

<objective>
Close the immutable-retry evidence gap with a real queued Oban job: enqueue through the public API, mutate live template/assign and tenancy-route state, then invoke the worker and prove it sends the stored rendered payload through the originally persisted route per D-04.

Purpose: Replace Task.Supervisor-adjacent confidence and source inspection with the exact queued-worker retry behavior Phase 150 promises.
Output: One non-vacuous worker integration test and any narrow decoded-route wiring correction it exposes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VERIFICATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-03-SUMMARY.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-06-SUMMARY.md
@lib/mailglass/outbound.ex
@lib/mailglass/outbound/worker.ex
@lib/mailglass/outbound/payload.ex
@test/mailglass/outbound/worker_test.exs
@test/mailglass/outbound/deliver_later_test.exs
@test/support/outbound_route_test_support.ex

<interfaces>
`Outbound.deliver_later/2` with explicit `async_adapter: :oban` creates a real disabled-mode Oban job after readiness. `Worker.perform/1` restores `mailglass_tenant_id` and calls `Outbound.dispatch_by_id/1`. Plan 150-06 makes payload load return both the reconstructed Message and persisted adapter_ref; this plan proves that complete value is authoritative at retry time.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 150-08-01: Prove a queued worker retry ignores changed live render and route state</name>
  <reversibility rating="costly">D-04 is the durable retry contract on which Phase 151 outcome handling builds; changing it would alter which provider input an already queued job sends.</reversibility>
  <files>lib/mailglass/outbound.ex, test/mailglass/outbound/worker_test.exs</files>
  <read_first>Read the immutable-retry behavior item in `150-VERIFICATION.md`, D-04/D-05, Plan 06's decoded-envelope interface, `Outbound.do_deliver_later/2`, `dispatch_by_id/1`, `load_payload_prepared/1`, `resolve_persisted_adapter/1`, Worker.perform/1, the disabled Oban setup in `deliver_later_test.exs`, and RouteRecordingAdapter.</read_first>
  <behavior>
    - Public `deliver_later/2` against a real disabled-mode Oban instance inserts one canonical job whose args contain only delivery and tenant IDs.
    - Enqueue renders a unique original subject/body from mutable test state and selects route A; after commit the test changes the same state to a different subject/body and switches the live tenancy/default route to B.
    - Invoking `Worker.perform/1` with the persisted job sends the original subject/body/attachment marker through route A, emits no route-B delivery, and leaves render/route-selection callback counters unchanged during perform.
    - Tampering the Delivery projection's adapter_ref after enqueue while leaving the private envelope unchanged fails closed before either route is called, proving the private V1 route is checked rather than ignored.
  </behavior>
  <action>Add one tagged `async: false` integration describe under `@tag phase_150_task: "t150_08_01"`. Start a real `{Oban, testing: :disabled, repo: TestRepo, queues: [mailglass_outbound: 10]}` instance, use the public `Outbound.deliver_later/2` entry, and fetch the inserted `%Oban.Job{}` from the actual `oban_jobs` table rather than constructing job args by hand. Define bounded test-local mutable render/route controls using an Agent or ETS owner cleaned in `on_exit`; the enqueue input must derive unique original rendered fields from that state, and route selection must record its invocation. After enqueue, mutate both controls and application tenancy/adapter configuration, then call `Mailglass.Outbound.Worker.perform(job)`. Assert captured adapter input contains the old subject/body and route-A marker, the callbacks were not reinvoked, and no route-B send occurred. Add the Delivery adapter_ref mismatch negative control and assert a bounded serialization/adapter error with zero adapter calls. If Plan 06's decoded route is not yet authoritative, adjust only `dispatch_by_id/1`/`load_payload_prepared/1` so modern Payload returns `{prepared_message, persisted_adapter_ref}`, checks projection equality, and resolves that persisted ref; keep the existing narrow legacy branch on the Delivery projection. Do not classify worker failures, alter Oban retry return semantics, scrub Payload, or define missing/corrupt terminal states—those remain Phase 151.</action>
  <verify>
    <automated>mix test test/mailglass/outbound/worker_test.exs --only phase_150_task:t150_08_01 --warnings-as-errors</automated>
  </verify>
  <done>A real ID-only Oban job retries from immutable private content and the persisted V1 route after live render/assign/tenancy state changes, with negative controls proving neither live seam was consulted per D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live template/assign state → async retry | Mutable executable application state must not influence an already queued message. |
| live tenancy routing → persisted adapter_ref | A changed route must not redirect an already queued delivery. |
| public Delivery projection → private envelope | Projection tampering must not silently override the private durable route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-27 | Tampering | worker message reconstruction | high | mitigate | Real public enqueue, post-commit state mutation, callback counters, and exact captured adapter input. |
| T-150-28 | Elevation of Privilege | retry route resolution | high | mitigate | Persisted V1 adapter_ref is authoritative and projection mismatch fails before adapter resolution. |
| T-150-29 | Repudiation | hand-built worker test jobs | medium | mitigate | Fetch and perform the actual Oban job inserted by the public enqueue path; assert exact ID-only args. |
| T-150-SC | Tampering | package installs | high | mitigate | No package install occurs. |
</threat_model>

<verification>
Run the focused sampler, then `mix test test/mailglass/outbound/envelope_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/worker_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- The proof begins at public durable enqueue and executes the actual stored Oban job.
- Changed live body/assign and route state are explicit negative controls, not source-level assumptions.
- The originally rendered bytes and persisted adapter_ref reach the adapter, and projection mismatch fails closed.
- Phase 151 outcome classification and payload lifecycle scope remain untouched.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-08-SUMMARY.md` when done.
</output>
