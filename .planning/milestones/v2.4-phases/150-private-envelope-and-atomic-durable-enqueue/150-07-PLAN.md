---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "07"
type: execute
wave: 6
depends_on:
  - 150-05
files_modified:
  - lib/mailglass/migrations/postgres/v06.ex
  - test/mailglass/v06_migration_test.exs
autonomous: true
gap_closure: true
requirements:
  - ENVL-05
must_haves:
  truths:
    - "D-03/D-16/ENVL-05: V06 up creates only the exact prefix-qualified private payload table, foreign key, and three named indexes in the supplied schema under a hostile search_path."
    - "D-16/ENVL-05: V06 creates zero payload rows and preserves pre-v2.4 Delivery metadata byte-for-byte rather than claiming a complete backfill."
    - "D-16/ENVL-05: V06 down removes only V06 objects from the supplied schema, leaves V05 objects/public decoys intact, and a second up restores the exact catalog state without partial objects."
  artifacts:
    - path: lib/mailglass/migrations/postgres/v06.ex
      provides: "Exact reversible prefix-qualified payload DDL"
      exports: ["up/1", "down/1"]
    - path: test/mailglass/v06_migration_test.exs
      provides: "Dedicated hostile-search-path V05→V06→down→up catalog and no-backfill proof"
  key_links:
    - from: test/mailglass/v06_migration_test.exs
      to: lib/mailglass/migrations/postgres/v06.ex
      via: "direct version-module execution in a guarded scratch prefix with catalog-qualified assertions"
      pattern: "Postgres\\.V06"
---

<objective>
Turn the present-but-unverified V06 migration into exact executable evidence: prefix-safe V05→V06 creation, catalog-precise indexes/predicate, zero legacy backfill, clean rollback, and repeatable re-up under a hostile search path per D-03/D-16.

Purpose: Remove migration confidence that currently rests only on `current_version() == 6` and source inspection.
Output: One dedicated V06 lifecycle test and any DDL corrections that proof exposes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-PATTERNS.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VERIFICATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-01-SUMMARY.md
@lib/mailglass/migrations/postgres/v05.ex
@lib/mailglass/migrations/postgres/v06.ex
@test/mailglass/migration_test.exs
@test/mailglass/schema_prefix_hardening_test.exs
@test/support/sandbox_ownership.ex

<interfaces>
`Mailglass.Migrations.Postgres.V06.up/1` and `down/1` accept `prefix:` and are normal Ecto migration callbacks. `Mailglass.TestSupport.SandboxOwnership.scratch_schema!/2` guards scratch targets, while `unsandboxed_module/1` and the existing migration test's unique-version cleanup pattern permit DDL without sharing the configured baseline schema.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 150-07-01: Prove the exact hostile-path V06 lifecycle and no-backfill contract</name>
  <reversibility rating="costly">D-03/D-16 define adopter database objects inherited by later phases; corrections remain reversible through V06 down before payload lifecycle work ships.</reversibility>
  <files>lib/mailglass/migrations/postgres/v06.ex, test/mailglass/v06_migration_test.exs</files>
  <read_first>Read the V06 behavior-unverified item in `150-VERIFICATION.md`, D-03/D-16, V05 and V06 in full, the scratch-prefix describe in `migration_test.exs`, and `SandboxOwnership` scratch/unsandboxed/search-path helpers. Do not use or drop the configured baseline schema.</read_first>
  <behavior>
    - Starting from V05 in a unique guarded scratch schema, with the session search_path set to a decoy schema and public, V06 creates `mailglass_outbound_payloads` only in the supplied scratch schema.
    - Catalog inspection proves exactly ten columns with expected PostgreSQL types/nullability, a delivery foreign key targeting the same prefix with cascade delete, the unique delivery index, tenant+delivery index, and partial expires index whose normalized predicate is `expires_at IS NOT NULL`.
    - A legacy queued Delivery containing every old reconstruction metadata key remains byte-identical after V06 and the new payload table count is zero.
    - Down removes the payload table, foreign key, and all three V06 indexes only from the scratch prefix; V05 Delivery/Event/Suppression objects and same-named public/decoy objects survive; a second up recreates the exact original catalog signature.
  </behavior>
  <action>Create `test/mailglass/v06_migration_test.exs` as an `async: false` DDL module, tag the lifecycle with `@tag phase_150_task: "t150_07_01"`, acquire the repository's sanctioned unsandboxed mode, and guard a unique scratch prefix with `SandboxOwnership.scratch_schema!/2`. Build only V01-through-V05 prerequisites in that scratch prefix using the existing version modules/runner pattern, seed a queued Delivery with a JSON metadata sentinel, create same-named decoy table/index objects outside the target prefix, then force the checked-out session search_path away from the scratch schema before invoking V06 directly. Query `pg_namespace`, `pg_class`, `pg_attribute`, `pg_constraint`, `pg_index`, and `pg_get_expr` with explicit schema predicates; compare a normalized catalog signature before down and after the second up. Assert zero payload rows and compare the legacy metadata through PostgreSQL's JSONB text/canonical representation so no Elixir map normalization can hide a rewrite. Execute down, assert all V06 target-prefix objects absent while V05 and decoys remain, then execute up again. Cleanup may CASCADE only the uniquely guarded scratch/decoy schemas created by this test and must restore the prior search path. If catalog assertions expose a real DDL mismatch, correct only `v06.ex` by threading `prefix:` into the faulty operation or matching the locked names/predicate; do not add data rewrites, lifecycle policy, or Phase 151 columns.</action>
  <verify>
    <automated>mix test test/mailglass/v06_migration_test.exs --only phase_150_task:t150_07_01 --warnings-as-errors</automated>
  </verify>
  <done>A dedicated runtime test proves the exact V06 table/FK/index/predicate catalog, prefix isolation under hostile search_path, zero legacy backfill, clean down, and identical re-up per D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| migration prefix → PostgreSQL catalog | Ambient search_path and same-named decoys must not redirect private payload DDL. |
| legacy Delivery rows → V06 | Existing incomplete metadata must remain untouched and must not be promoted into a private V1 payload. |
| V06 down → adopter schema | Rollback must remove only V06-owned target-prefix objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-24 | Tampering | V06 prefix resolution | high | mitigate | Hostile search_path, public/decoy collisions, and schema-qualified catalog assertions for table/FK/index objects. |
| T-150-25 | Information Disclosure / Tampering | legacy metadata | high | mitigate | Byte/canonical comparison around V06 plus zero-payload-row assertion; no backfill code permitted. |
| T-150-26 | Denial of Service | V06 down/up lifecycle | medium | mitigate | V05-survival assertions, exact post-re-up signature, and cleanup limited to guarded scratch schemas. |
| T-150-SC | Tampering | package installs | high | mitigate | No package install occurs. |
</threat_model>

<verification>
Run the focused sampler, then `mix test test/mailglass/v06_migration_test.exs test/mailglass/migration_test.exs test/mailglass/schema_prefix_hardening_test.exs --warnings-as-errors` as the migration regression gate.
</verification>

<success_criteria>
- V06-specific behavior is exercised at runtime rather than inferred from the runner version.
- Exact target-prefix columns, FK, indexes, and partial predicate are catalog-proven under hostile search_path.
- Legacy metadata is unchanged, payload count is zero, and down→up leaves no partial or foreign-prefix state.
- Phase 151 scrubbing, retention, recovery, and terminal-state work remains absent.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-07-SUMMARY.md` when done.
</output>
