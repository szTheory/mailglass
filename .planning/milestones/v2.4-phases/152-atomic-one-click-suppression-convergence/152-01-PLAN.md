---
phase: 152-atomic-one-click-suppression-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/compliance/unsubscribe_convergence.ex
  - lib/mailglass/compliance/unsubscribe_controller.ex
  - test/mailglass/compliance/unsubscribe_controller_test.exs
autonomous: true
requirements: [UNSUB-07, UNSUB-08, UNSUB-11]
must_haves:
  truths:
    - "A valid POST atomically converges one canonical unsubscribe event and one immutable address_stream suppression per D-04 through D-06."
    - "Invalid, expired, tampered, and missing targets remain indistinguishable byte-empty 200 privacy no-ops per D-03."
    - "A real convergence failure returns byte-empty 500 and commits neither half of the pair per D-07 and D-17."
    - "A legacy event-only or suppression-only state is repaired to the complete canonical pair."
  artifacts:
    - path: "lib/mailglass/compliance/unsubscribe_convergence.ex"
      provides: "Flat prefix-explicit atomic convergence service and canonical result classification"
    - path: "lib/mailglass/compliance/unsubscribe_controller.ex"
      provides: "HTTP trust classification and stable success/failure mapping"
    - path: "test/mailglass/compliance/unsubscribe_controller_test.exs"
      provides: "First-convergence, conflict, incomplete-pair, privacy, and rollback evidence"
  key_links:
    - from: "lib/mailglass/compliance/unsubscribe_controller.ex"
      to: "lib/mailglass/compliance/unsubscribe_convergence.ex"
      via: "trusted persisted Delivery passed after the narrow opaque-ID lookup and tenant restoration"
      pattern: "UnsubscribeConvergence"
    - from: "lib/mailglass/compliance/unsubscribe_convergence.ex"
      to: "Mailglass.Events and Mailglass.Suppression.Entry"
      via: "one Ecto.Multi using Repo.multi_opts on inserts and canonical refetches"
      pattern: "Repo\\.multi_opts"
---

<objective>
Deliver the production transaction spine for atomic one-click suppression convergence.

Purpose: Make the hardest invariant true first: every valid one-click POST commits or reuses the complete canonical event/suppression pair without ambient-schema dependence or false conflict failures.
Output: A tested convergence service, controller wiring, and rollback/repair evidence for UNSUB-07, UNSUB-08, and UNSUB-11.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-CONTEXT.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-RESEARCH.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-PATTERNS.md
@lib/mailglass/compliance/unsubscribe_controller.ex
@lib/mailglass/events.ex
@lib/mailglass/repo.ex
@lib/mailglass/suppression/auto_suppress.ex
@lib/mailglass/suppression/entry.ex
@test/mailglass/compliance/unsubscribe_controller_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and implement the flat atomic convergence path</name>
  <files>test/mailglass/compliance/unsubscribe_controller_test.exs, lib/mailglass/compliance/unsubscribe_convergence.ex, lib/mailglass/compliance/unsubscribe_controller.ex</files>
  <behavior>
    - Test first: a valid POST derives tenant, normalized recipient, and originating stream only from the persisted Delivery per D-01, D-02, and D-08; it returns status 200 with a byte-empty body and persists one canonical `unsubscribe:&lt;delivery_id&gt;` event plus one immutable `address_stream`/`unsubscribe` suppression.
    - Test first: invalid, tampered, expired, and missing targets each return the same byte-empty 200 and write nothing per D-03 and D-18.
    - Test first: canonical conflict refetch returns persisted rows instead of treating `on_conflict: :nothing` as a failure per D-05; the suppression `inserted_at` insert/conflict sentinel is pinned against the current Ecto/Postgrex adapter before it gates effects.
    - Test first: the immutable suppression stores `source: "compliance:one_click"` and only bounded metadata keys `delivery_id`, `event_id`, and `event_type`; it stores no token or private message content.
  </behavior>
  <action>Create `Mailglass.Compliance.UnsubscribeConvergence.run/1` around one flat `Ecto.Multi`, accepting only a trusted persisted Delivery. Build event and suppression identities from that Delivery, use `Events.append_multi/3` for the canonical delivery idempotency key, and use the existing `Suppression.Entry.changeset/1` normalization/scope coupling. Construct the immutable suppression with exact `source: "compliance:one_click"` and bounded metadata `%{"delivery_id" => delivery.id, "event_id" => canonical_event.id, "event_type" => "unsubscribed"}`; never include the token, recipient in metadata, or private message content. Insert it conflict-safely and refetch canonical rows after either conflict. First pin the current adapter's suppression result sentinel: because `id` is client-generated and `inserted_at` is DB-defaulted/read-after-writes, use `inserted_at` only after the focused test proves it is nil on `DO NOTHING`; otherwise encapsulate the verified deterministic result without changing the `created`/`already_converged` contract. Pass `Repo.multi_opts(...)` explicitly to every insert and every callback-repo query per D-15; do not rely on connection `search_path` or use mutable admin replacement semantics. Return a tagged result that distinguishes `created`, `already_converged`, and failure while retaining both canonical rows. Wire the existing controller only after its audited opaque-ID Delivery lookup and tenant restoration per D-02; keep token/target privacy outcomes and the public route/body unchanged per D-03/D-18. Implement after the focused assertions fail.</action>
  <verify>
    <automated>mix test test/mailglass/compliance/unsubscribe_controller_test.exs --warnings-as-errors</automated>
  </verify>
  <done>A valid first POST traverses route → trusted Delivery → one transaction → canonical event and suppression, while all privacy no-ops remain exact empty 200 responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close conflict, incomplete-pair, and rollback branches</name>
  <files>test/mailglass/compliance/unsubscribe_controller_test.exs, lib/mailglass/compliance/unsubscribe_convergence.ex, lib/mailglass/compliance/unsubscribe_controller.ex</files>
  <behavior>
    - A pre-existing event with no suppression and a pre-existing suppression with no event each converge to exactly one complete pair; the completing request reports `created`.
    - A pair that already exists reports `already_converged`; canonical refetches return the persisted event/suppression on uniqueness conflicts per D-05/D-06.
    - Deterministic failure injected after either attempted insert rolls back the entire pair, returns byte-empty 500, and is never classified as a privacy success per D-07/D-17.
  </behavior>
  <action>Add deterministic, test-only injection seams at named Multi steps and write the failing incomplete-pair and rollback cases before production changes. Derive `created` from whether this transaction inserted either previously absent durable fact so legacy half-state repair is not stranded; derive `already_converged` only when both facts pre-existed. Preserve a flat Multi and force every conflict lookup through the exact tenant/address/scope/stream or delivery/idempotency identity using explicit prefix options. Map genuine transaction/convergence error to stable status 500 with an empty body; keep the privacy no-op branch separate. Do not emit lifecycle or broadcast effects in this plan—the transaction result is the sole durable output and Plan 02 owns created-only post-commit effects.</action>
  <verify>
    <automated>mix test test/mailglass/compliance/unsubscribe_controller_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Both legacy incomplete states repair atomically, uniqueness conflicts converge to canonical rows, and every injected failure proves zero partial durable mutation plus non-200 response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| one-click HTTP → token/Delivery resolution | Untrusted token and POST data cross into the narrowly audited opaque-ID lookup. |
| trusted Delivery → tenant transaction | Persisted tenant/address/stream facts cross into prefix-scoped event and suppression writes. |
| Ecto.Multi → PostgreSQL uniqueness | Concurrent inserts arbitrate canonical durable identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-152-01 | Tampering / Elevation | unsubscribe controller scope derivation | high | mitigate | Verify signed token, use only persisted Delivery tenant/address/stream per D-01/D-02/D-08, and retain indistinguishable privacy no-ops per D-03. |
| T-152-02 | Tampering / Repudiation | event/suppression convergence | high | mitigate | One flat prefix-explicit Multi, database unique identities, canonical conflict refetch, and incomplete-pair repair per D-04..D-06/D-15. |
| T-152-03 | Information Disclosure | HTTP error classification | medium | mitigate | Keep invalid/expired/missing targets on exact empty 200 while genuine DB failures use empty 500 per D-03/D-07. |
| T-152-04 | Denial of Service / Tampering | transaction failure path | high | mitigate | Named failure injection proves all-or-nothing rollback and no false success per D-17. |

</threat_model>

<verification>
Run `mix test test/mailglass/compliance/unsubscribe_controller_test.exs --warnings-as-errors`, then the Phase 152 focused gate in `152-VALIDATION.md` for Plan 01.
</verification>

<success_criteria>
- UNSUB-07 is demonstrated through the public POST and canonical database rows.
- UNSUB-08 conflict/replay foundations converge without duplicate rows.
- UNSUB-11 rollback and explicit-prefix behavior are encoded in the transaction spine.
</success_criteria>

<output>
Create `.planning/phases/152-atomic-one-click-suppression-convergence/152-01-SUMMARY.md` when done.
</output>
