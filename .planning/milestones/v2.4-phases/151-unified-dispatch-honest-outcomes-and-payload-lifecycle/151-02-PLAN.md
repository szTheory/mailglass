---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/outbound/dispatch_outcome.ex
  - lib/mailglass/adapters/swoosh.ex
  - test/mailglass/outbound/dispatch_outcome_test.exs
  - test/mailglass/adapters/swoosh_test.exs
autonomous: true
requirements: [DISP-01]
must_haves:
  truths:
    - "D-05/D-10/DISP-01: every result is accepted or one closed retryable, terminal, or uncertain outcome based only on structured evidence."
    - "D-06..D-09: 4xx is terminal, 5xx retryable, unknown/ambiguous acceptance uncertain, and public facts contain only allowlisted reason/correlation context."
    - "D-04: custom adapters retain their callback and conservatively map opaque errors without a breaking redesign."
  artifacts:
    - {path: lib/mailglass/outbound/dispatch_outcome.ex, provides: "closed outcome constructor/classifier/safe projection"}
    - {path: lib/mailglass/adapters/swoosh.ex, provides: "structured Swoosh evidence without callback change"}
  key_links:
    - {from: lib/mailglass/adapters/swoosh.ex, to: lib/mailglass/outbound/dispatch_outcome.ex, via: "bounded structured SendError context", pattern: "reason_class"}
---
<objective>Create the closed, privacy-bounded classification contract required by DISP-01 and D-04..D-10.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-CONTEXT.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-RESEARCH.md
@lib/mailglass/adapter.ex
@lib/mailglass/adapters/swoosh.ex
@docs/api_stability.md
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 151-02-01: Classify Swoosh evidence without changing the adapter contract</name>
  <reversibility rating="costly">D-05 becomes the internal control-flow vocabulary used by workers and persistence.</reversibility>
  <files>lib/mailglass/outbound/dispatch_outcome.ex, lib/mailglass/adapters/swoosh.ex, test/mailglass/outbound/dispatch_outcome_test.exs, test/mailglass/adapters/swoosh_test.exs</files>
  <read_first>Read D-04..D-10, the research mapping table/open question, `Mailglass.Adapter` callback, Swoosh `raw_deliver/2` and its current reason classes, and all adapter tests.</read_first>
  <behavior>
    - Accepted results normalize without losing provider response compatibility.
    - Structured HTTP 400..499 maps terminal and 500..599 maps retryable.
    - timeout, exit, malformed/opaque custom errors, or evidence not proving pre-acceptance defaults uncertain.
    - Local pre-dispatch typed errors can be terminal; arbitrary atoms/maps/text cannot assert certainty.
  </behavior>
  <action>Add `Mailglass.Outbound.DispatchOutcome` as a closed internal struct/tagged contract with constructors, exhaustive class validation, and a `safe_projection/1` that allowlists atom reason classes plus bounded correlation availability/identifier. Keep `Mailglass.Adapters.Swoosh.deliver/2` callback-compatible; expose only additive structured evidence through existing typed error context, remove raw provider body/exception text from the new outcome projection, and never parse `Exception.message/1`, `inspect/1`, or provider strings to choose a class per D-05/D-09. Preserve existing public error compatibility while treating unknown custom-adapter evidence conservatively per D-04/D-10. Tag tests `t151_02_01` and add a source assertion that the classifier contains no text-matching calls.</action>
  <verify><automated>mix test test/mailglass/outbound/dispatch_outcome_test.exs test/mailglass/adapters/swoosh_test.exs --only phase_151_task:t151_02_01 --warnings-as-errors</automated></verify>
  <done>All supported evidence maps into the closed D-05 classes with safe context and opaque cases remain uncertain.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| provider/custom adapter → outcome | Untrusted response/error material controls retry behavior. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-03 | Tampering / Repudiation | outcome classifier | high | mitigate | Closed constructors, structured-evidence table, exhaustive tests, no text matching. |
| T-151-04 | Information Disclosure | safe outcome projection | high | mitigate | Allowlists and sentinel/raw-body exclusion tests. |
| T-151-SC | Tampering | package supply chain | high | mitigate | Existing dependencies only. |
</threat_model>
<verification>Run focused tests, then `mix test test/mailglass/outbound/dispatch_outcome_test.exs test/mailglass/adapters/swoosh_test.exs --warnings-as-errors`.</verification>
<success_criteria>DISP-01 has one closed, conservative, non-sensitive classification contract.</success_criteria>
<output>Create `151-02-SUMMARY.md` in the phase directory.</output>

