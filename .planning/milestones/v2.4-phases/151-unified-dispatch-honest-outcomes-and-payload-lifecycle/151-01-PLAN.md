---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/outbound.ex
  - test/mailglass/outbound/wire_equivalence_test.exs
autonomous: true
requirements: [ENVL-03]
must_haves:
  truths:
    - "D-01/D-02/ENVL-03: sync and real Oban execution hand the same fully prepared one-recipient message and persisted adapter route to one canonical provider seam."
    - "D-03: parity covers native recipient placement, ordered duplicate headers, attachments, subject/bodies, Mailglass metadata/tags, and JSON-safe provider options."
    - "D-04: the public adapter callback and return contract remain compatible."
  artifacts:
    - {path: lib/mailglass/outbound.ex, provides: "canonical prepared-message dispatch seam"}
    - {path: test/mailglass/outbound/wire_equivalence_test.exs, provides: "sync versus actual queued-job adapter-input oracle"}
  key_links:
    - {from: lib/mailglass/outbound.ex, to: Mailglass.Adapter.deliver/2, via: "one dispatch_prepared/call_adapter path", pattern: "dispatch_prepared"}
---

<objective>
Prove ENVL-03 through one production dispatch seam and an actual sync-versus-Oban captured-input tracer per D-01 through D-04.

Purpose: Remove authoring drift before outcome and lifecycle behavior builds on the handoff.
Output: Canonical prepared dispatch and deep wire-equivalence regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-CONTEXT.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-PATTERNS.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-08-SUMMARY.md
@lib/mailglass/outbound.ex
@test/mailglass/outbound/worker_test.exs
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 151-01-01: Prove one sync/Oban provider-input path</name>
  <reversibility rating="costly">D-01/D-04 alter a shared internal dispatch seam behind a stable public callback.</reversibility>
  <files>lib/mailglass/outbound.ex, test/mailglass/outbound/wire_equivalence_test.exs</files>
  <read_first>Read D-01..D-04, the Pattern Map `outbound.ex` assignment, Phase 150 immutable-worker summary, `do_send_after_preflight/2`, `dispatch_by_id/1`, `dispatch_prepared/2`, `call_adapter/2`, and existing Fake/manual-Oban helpers.</read_first>
  <behavior>
    - The same fully featured supported message sent synchronously and by performing the exact job inserted through `deliver_later/2` produces deeply equal captured adapter messages/options.
    - Comparison includes ordered and duplicate collections and allows only timing/provider-generated response values.
    - Sync does not persist a Payload merely to share the seam; async still loads Envelope/Payload and its persisted adapter route.
  </behavior>
  <action>Write the tagged failing integration test first. Extract one package-private prepared-message dispatch helper used by the in-memory sync path and payload-backed `dispatch_by_id/1`; keep rendering/preparation before this helper, adapter I/O outside all Repo transactions, and async route authority in the stored `adapter_ref` per D-01/D-02. Preserve `Mailglass.Adapter.deliver/2` and all public return shapes per D-04. Use an actual ID-only job from public `deliver_later/2`, not constructed worker args. Compare every D-03 field with private unique sentinels and assert those sentinels remain absent from Delivery/Event/job surfaces.</action>
  <verify><automated>mix test test/mailglass/outbound/wire_equivalence_test.exs --only phase_151_task:t151_01_01 --warnings-as-errors</automated></verify>
  <done>The actual sync and durable async paths share the canonical adapter handoff and satisfy the complete D-03 equality oracle.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| prepared message → adapter | Private content crosses to the configured provider adapter. |
| Payload → worker message | Persisted private input becomes runtime provider input. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-01 | Tampering | sync/async handoff | high | mitigate | Deep actual-job parity test over the complete supported shape. |
| T-151-02 | Information Disclosure | public durable surfaces | high | mitigate | Sentinel absence assertions for Delivery/Event/job while adapter capture remains complete. |
| T-151-SC | Tampering | package supply chain | high | mitigate | No package installation or lockfile change. |
</threat_model>

<verification>Run the focused tracer, then `mix test test/mailglass/outbound/wire_equivalence_test.exs test/mailglass/outbound/worker_test.exs --warnings-as-errors`.</verification>
<success_criteria>One real provider-input seam is covered end to end without public adapter breakage or a sync Payload.</success_criteria>
<output>Create `.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-01-SUMMARY.md`.</output>

