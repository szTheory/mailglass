---
phase: 149-first-send-contract-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - lib/mailglass/outbound/preflight.ex
  - test/mailglass/outbound/preflight_test.exs
  - test/mailglass/outbound/deliver_later_test.exs
autonomous: true
requirements:
  - FIRST-03
  - FIRST-04
  - FIRST-07
must_haves:
  truths:
    - "D-04/FIRST-03: Exactly one total entry across to, cc, and bcc is accepted; nil/empty collections count as zero, and equal duplicate addresses still count as two entries and are rejected."
    - "D-04/FIRST-03: Recipient ordering never affects the outcome because preflight rejects every count other than one without selecting, merging, reordering, or dropping an address."
    - "D-03/FIRST-04: Recipient rejection completes before render telemetry/work, suppression, rate-limit consumption, persistence, job insertion, or provider dispatch."
    - "D-05/FIRST-07: Nil, empty, whitespace-only, unsupported, or invalid-encoding body shapes fail before a Delivery row or job exists; one supported nonblank HTML and/or plaintext binary is accepted."
    - "D-06/FIRST-07: Envelope/body failures use SendError type :preflight_rejected with only bounded reason classes, counts, and body-state atoms in context."
  artifacts:
    - path: lib/mailglass/outbound/preflight.ex
      provides: "Complete recipient cardinality and body-shape matrix"
      exports: ["run/1"]
    - path: test/mailglass/outbound/preflight_test.exs
      provides: "Pure matrix plus sync no-side-effect and non-PII error proof"
    - path: test/mailglass/outbound/deliver_later_test.exs
      provides: "Async zero-row/zero-job proof for rejected input"
  key_links:
    - from: lib/mailglass/outbound/preflight.ex
      to: lib/mailglass/outbound.ex
      via: "Preflight returns before all existing effectful `with` stages"
      pattern: "Preflight\\.run"
    - from: test/mailglass/outbound/preflight_test.exs
      to: lib/mailglass/errors/send_error.ex
      via: "Struct/type/context assertions for preflight_rejected"
      pattern: "preflight_rejected"
  prohibitions:
    - "flagged-unverified: Validation must not select a primary recipient, deduplicate equal addresses, or discard cc/bcc entries."
    - "flagged-unverified: Public error context must not contain addresses, subject, headers, HTML, plaintext, or inspected message content."
    - "flagged-unverified: Recipient fan-out remains outside the current milestone."
---

<objective>
Expand the proven shared preflight into the complete recipient and body rejection contract, including deterministic boundary/empty/ordering/encoding cases and proof that rejection precedes every side effect per D-03 through D-06.

Purpose: Make invalid input safely boring: one typed bounded error, no silent data loss, no quota use, no rendering, and no durable/provider artifacts.
Output: Complete pure validator behavior plus sync/async negative-control coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/149-first-send-contract-foundation/149-CONTEXT.md
@.planning/phases/149-first-send-contract-foundation/149-RESEARCH.md
@.planning/phases/149-first-send-contract-foundation/149-PATTERNS.md
@.planning/phases/149-first-send-contract-foundation/149-VALIDATION.md
@docs/api_stability.md
@.planning/phases/149-first-send-contract-foundation/149-01-SUMMARY.md
@lib/mailglass/outbound/preflight.ex
@lib/mailglass/outbound.ex
@lib/mailglass/errors/send_error.ex
@test/mailglass/outbound/preflight_test.exs
@test/mailglass/outbound/deliver_later_test.exs
</context>

## Artifacts this phase produces

- Recipient contract inside `Mailglass.Outbound.Preflight.run/1`: total cardinality is `length(List.wrap(to) ++ List.wrap(cc) ++ List.wrap(bcc))`, accepted only at exactly one.
- Body contract inside `Mailglass.Outbound.Preflight.run/1`: supported nonblank HTML and/or plaintext body, with HTML restricted to binary or 1-arity function and text restricted to valid UTF-8 binary.
- Bounded error context keys `:reason_class`, `:recipient_count`, and `:body_state` on the existing `%Mailglass.SendError{type: :preflight_rejected}` surface; no recipient/body values.

<tasks>

<task type="auto" tdd="true">
  <name>Task 149-02-01: Enforce exact native envelope cardinality</name>
  <reversibility rating="costly">Exactly-one cardinality is an adopter-visible send contract; undoing it would change accepted envelopes and all later persistence assumptions.</reversibility>
  <files>lib/mailglass/outbound/preflight.ex, test/mailglass/outbound/preflight_test.exs</files>
  <read_first>Read `149-CONTEXT.md` D-03/D-04/D-06, FIRST-03 in `REQUIREMENTS.md`, `149-RESEARCH.md` recipient aggregation example and anti-patterns, the Swoosh field use in `lib/mailglass/outbound.ex`, `lib/mailglass/errors/send_error.ex`, and current Plan 01 `Preflight.run/1` plus its tests.</read_first>
  <behavior>
    - Test 1: `{to: [one], cc: [], bcc: []}`, `{to: [], cc: [one], bcc: []}`, and `{to: [], cc: [], bcc: [one]}` each pass without moving the address between fields.
    - Test 2: nil/empty lists and zero total recipients return `%SendError{type: :preflight_rejected, context: %{reason_class: :recipient_count_invalid, recipient_count: 0}}`.
    - Test 3: Every two-or-more combination across and within to/cc/bcc rejects, including two byte-equal duplicate addresses; input collection order and contents remain unchanged.
    - Test 4: Boundary counts 0, 1, and 2 prove the exact threshold; a larger count proves no fixed maximum/overflow branch or primary-recipient selection.
  </behavior>
  <action>Implement the D-04 recipient validator using native Swoosh `to`, `cc`, and `bcc` collections. Treat nil as empty with `List.wrap/1`, concatenate all entries, and branch only on the integer total: one passes, every other count returns `Mailglass.SendError.new(:preflight_rejected, context: %{reason_class: :recipient_count_invalid, recipient_count: count})` per D-06. Do not parse, normalize, deduplicate, sort, select, or mutate mailbox entries. Add a table-driven matrix that asserts the original `%Swoosh.Email{}` fields are unchanged on both pass and failure, and assert the error context keys are exactly the bounded set rather than inspecting content.</action>
  <acceptance_criteria>
    - Behavior assertion: zero rejects, one anywhere passes, and two-or-more in all cross-field combinations reject.
    - Equality/ordering assertion: equal duplicate addresses remain two recipients and input field order is untouched.
    - Error assertion: `:recipient_count` is an integer; no recipient/address/content key or value appears in context.
    - Test command: `mix test test/mailglass/outbound/preflight_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/preflight_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIRST-03 is exhaustively enforced without implicit fan-out, selection, deduplication, or address loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 149-02-02: Reject before rendering and every outbound side effect</name>
  <files>test/mailglass/outbound/preflight_test.exs, test/mailglass/outbound/deliver_later_test.exs</files>
  <read_first>Read FIRST-04 and FIRST-07, `149-CONTEXT.md` D-03/D-05/D-06, `149-RESEARCH.md` Pitfalls 2-4 and Validation Architecture, the spec-less edge-probe disposition and planned Wave 2 verification work in `149-VALIDATION.md`, the existing render telemetry helpers/tests in `test/mailglass/renderer_test.exs`, Fake checkout API usages, RateLimiter test seams, Delivery/Event queries, and existing async adapter setup in `deliver_later_test.exs`.</read_first>
  <behavior>
    - Test 1: Zero- and multi-recipient sync rejection emits no render event, consumes no rate-limit token, inserts no Delivery/Event row, and records no Fake adapter delivery.
    - Test 2: The same rejected async input inserts no Delivery/Event row and zero Oban jobs through the stable optional-Oban test seam; Task.Supervisor mode also records no task/Fake delivery.
    - Test 3: HTML/text nil, empty, or Unicode-whitespace-only combinations reject as body_state `:empty`; unsupported HTML arity/value, nonbinary text, and invalid UTF-8 reject as body_state `:unsupported`.
    - Test 4: A one-recipient message with only nonblank text, only supported nonblank HTML, or both bodies passes the pure gate; Unicode plaintext is preserved byte-for-byte rather than normalized.
  </behavior>
  <action>Finish D-05 body validation in `Preflight.run/1`: define body presence as a valid UTF-8 binary whose `String.trim/1` result is non-empty; HTML additionally supports a 1-arity function, while plaintext is binary-only. Accept when either body is supported and present. Return the existing D-06 SendError with context `%{reason_class: :body_invalid, body_state: :empty | :unsupported}` for all other shapes, without embedding values or exception messages. Add negative-control integration tests proving D-03 ordering across render telemetry, RateLimiter state, Delivery/Event persistence, Oban job count, Task.Supervisor/Fake dispatch, and both public sync/async paths. Use `Mailglass.TestSupport.SandboxOwnership.with_app_env!/2` and `on_exit` to restore configuration. If the existing Oban seam cannot count jobs, add a test-local assertion helper in the modified async test file; do not change queue storage or job arguments. Replace permissive `case` branches in touched preflight tests with exact assertions so invalid behavior cannot pass either way.</action>
  <acceptance_criteria>
    - Source assertion: body validation completes inside the pure preflight before Renderer and all effectful stages.
    - Boundary assertion: nil/empty/Unicode-whitespace-only bodies reject; one supported nonblank body passes; unsupported type/arity/encoding rejects.
    - Side-effect assertion: invalid recipient/body cases prove zero rendering, limiter consumption, rows, jobs/tasks, and Fake deliveries.
    - Test command: `mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIRST-04 and FIRST-07 reject every invalid envelope/body shape through a bounded typed error before any observable side effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Swoosh envelope/body → shared preflight | Untrusted adopter-authored native fields enter the outbound contract. |
| preflight result → renderer/limits/storage/queue/provider | Acceptance enables all costly and durable work. |
| typed failure → adopter/logging surface | Bounded error metadata crosses a public observability boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-149-02 | Tampering | recipient aggregation | high | mitigate | Count every native to/cc/bcc entry and reject counts other than one without mutation, selection, or deduplication. |
| T-149-03 | Denial of Service | outbound stage ordering | high | mitigate | Negative controls prove rejection before render, limiter, rows, jobs/tasks, and adapter dispatch. |
| T-149-04 | Information Disclosure | SendError context | high | mitigate | Exact-key tests allow only reason class, count, and body-state atoms; exclude all PII/content fields and values. |
| T-149-SC | Tampering | package installs | high | mitigate | This plan has no install task; any install deviation must stop for the package-legitimacy audit, slopcheck, and a blocking human decision for `[ASSUMED]` or `[SUS]`. |
</threat_model>

<verification>
Run each focused command after its task. At wave completion run `mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/renderer_test.exs test/mailglass/tenancy_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- FIRST-03 covers empty, exact-boundary, duplicate-equality, cross-field, and ordering cases.
- FIRST-04 proves rejection precedes every named effect with positive negative controls.
- FIRST-07 covers body emptiness/encoding/type boundaries and non-PII typed failures.
</success_criteria>

<output>
Create `.planning/phases/149-first-send-contract-foundation/149-02-SUMMARY.md` when done.
</output>
