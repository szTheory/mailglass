---
phase: "52"
plan: "03"
type: execute
wave: 3
depends_on:
  - "52-02"
files_modified:
  - "reference/host_app/SCOPE.md"
  - "reference/host_app/README.md"
  - "test/reference_host/scope_lock_contract_test.exs"
autonomous: true
requirements:
  - HOST-03
must_haves:
  truths:
    - "Reference host scope is explicitly bounded by an allowlist and non-goals that match v1.3 locks."
    - "Scope boundary language is enforced in deterministic tests, not only prose."
    - "Out-of-scope constraints remain explicit: no provider matrix broadening, no `SEED-003` promotion, no `gen_smtp` expansion, no second-product growth."
  artifacts:
    - path: "reference/host_app/SCOPE.md"
      provides: "Canonical proof-scope allowlist and non-goals contract for the maintained host"
      contains: "## Non-Goals"
    - path: "test/reference_host/scope_lock_contract_test.exs"
      provides: "Machine-enforced scope lock guardrail for HOST-03"
      contains: "assert"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "reference/host_app/SCOPE.md"
      via: "phase-level non-goals map directly into host scope contract language"
      pattern: "Provider-matrix broadening|SEED-003|gen_smtp"
    - from: "reference/host_app/SCOPE.md"
      to: "test/reference_host/scope_lock_contract_test.exs"
      via: "required and forbidden scope tokens are asserted for fail-closed behavior"
      pattern: "In Scope|Non-Goals|Deferred"
---

<objective>
Lock HOST-03 by codifying and enforcing reference-host proof scope, allowlist, and non-goals so the host cannot drift into a second product.

Purpose: Implement D-06 and D-07 while honoring out-of-scope constraints from ROADMAP/REQUIREMENTS and preserving milestone focus.
Output: `SCOPE.md` + scope lock contract tests + docs-boundary check linkage for deterministic enforcement.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-CONTEXT.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-RESEARCH.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-PATTERNS.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-VALIDATION.md
@reference/host_app/README.md
@lib/mix/tasks/mailglass.docs.check.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Author `SCOPE.md` with explicit allowlist, non-goals, and deferred items</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/52-trust-scope-lock-reference-host-baseline/52-CONTEXT.md
    - reference/host_app/README.md
  </read_first>
  <files>reference/host_app/SCOPE.md, reference/host_app/README.md</files>
  <action>Create `reference/host_app/SCOPE.md` with exact headings `## In Scope`, `## Non-Goals`, and `## Deferred`. Under `## In Scope`, include only trust-proof baseline items: `install`, `preview`, `send`, `webhook ingest`, `operator troubleshooting`. Under `## Non-Goals`, include verbatim lock tokens: `Provider-matrix broadening`, `SEED-003-ecosystem-integrations promotion`, `gen_smtp listener expansion`, and `second product surface`. Under `## Deferred`, include `OPS-01/OPS-02 smoke reliability closure remains outside Phase 52`. Add a README pointer line: `Scope contract: see reference/host_app/SCOPE.md`.</action>
  <acceptance_criteria>
    - `SCOPE.md` contains all required headings and required lock tokens verbatim.
    - README contains the exact scope pointer line.
    - `SCOPE.md` language limits scope to trust-proof baseline without adding new feature commitments.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^## In Scope$" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "^## Non-Goals$" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "^## Deferred$" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "install" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "preview" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "send" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "webhook ingest" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "operator troubleshooting" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "Provider-matrix broadening" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "SEED-003-ecosystem-integrations promotion" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "gen_smtp listener expansion" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "second product surface" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "OPS-01/OPS-02 smoke reliability closure remains outside Phase 52" reference/host_app/SCOPE.md</automated>
    <automated>rg -n "Scope contract: see reference/host_app/SCOPE.md" reference/host_app/README.md</automated>
  </verify>
  <done>The reference host has a precise, testable scope contract aligned with v1.3 trust-proof boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce scope lock with deterministic contract tests</name>
  <read_first>
    - .planning/phases/52-trust-scope-lock-reference-host-baseline/52-PATTERNS.md
    - lib/mix/tasks/mailglass.docs.check.ex
    - reference/host_app/SCOPE.md
  </read_first>
  <files>test/reference_host/scope_lock_contract_test.exs</files>
  <action>Create `test/reference_host/scope_lock_contract_test.exs` with required token assertions for all headings and lock phrases from Task 1 and forbidden token assertions for expansion language: `multi-provider matrix`, `new product feature`, `marketing email`, `gen_smtp roadmap in v1.3`, and `SEED-003 included in this phase`. Add one required token assertion for `Scope contract: see reference/host_app/SCOPE.md` in `reference/host_app/README.md` to keep scope-pointer enforcement inside HOST-03 scope. Use failure messages prefixed with `HOST-03`.</action>
  <acceptance_criteria>
    - Scope contract test exists and enforces required and forbidden tokens.
    - Test output identifies scope drift with `HOST-03` messages.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/reference_host/scope_lock_contract_test.exs --warnings-as-errors</automated>
    <automated>rg -n "HOST-03" test/reference_host/scope_lock_contract_test.exs</automated>
    <automated>rg -n "reference/host_app/SCOPE.md" test/reference_host/scope_lock_contract_test.exs</automated>
    <automated>rg -n "Scope contract: see reference/host_app/SCOPE.md" reference/host_app/README.md</automated>
    <automated>rg -n "multi-provider matrix" test/reference_host/scope_lock_contract_test.exs</automated>
    <automated>rg -n "new product feature" test/reference_host/scope_lock_contract_test.exs</automated>
    <automated>rg -n "marketing email" test/reference_host/scope_lock_contract_test.exs</automated>
    <automated>rg -n "gen_smtp roadmap in v1.3" test/reference_host/scope_lock_contract_test.exs</automated>
    <automated>rg -n "SEED-003 included in this phase" test/reference_host/scope_lock_contract_test.exs</automated>
  </verify>
  <done>Scope lock is enforced by code and docs contract tests so non-goal drift is blocked during review and CI.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scope documentation -> milestone execution scope | Written scope must constrain implementation choices, not merely describe intent. |
| phase-level non-goals -> reference host evolution | Deferred/out-of-scope items must not leak into Phase 52 host work. |
| docs language -> public contract interpretation | Reference host docs must not be interpreted as contract guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-09 | Tampering | `reference/host_app/SCOPE.md` scope statements | mitigate | Enforce required headings and required lock tokens with fail-closed tests. |
| T-52-10 | Elevation of Privilege | scope creep into out-of-scope product work | mitigate | Assert forbidden expansion tokens in `scope_lock_contract_test.exs`. |
| T-52-11 | Repudiation | inability to prove why added work is out of scope | mitigate | Tie `SCOPE.md` and scope-lock checks to HOST-03-labeled failure messages. |
| T-52-12 | Misrepresentation | scope pointer drift between README and scope contract | mitigate | Require `Scope contract: see reference/host_app/SCOPE.md` in README and assert it in scope-lock verification. |
</threat_model>

<verification>
- `mix test test/reference_host/scope_lock_contract_test.exs --warnings-as-errors`
- `rg -n "^## In Scope$" reference/host_app/SCOPE.md`
- `rg -n "^## Non-Goals$" reference/host_app/SCOPE.md`
- `rg -n "^## Deferred$" reference/host_app/SCOPE.md`
- `rg -n "Provider-matrix broadening" reference/host_app/SCOPE.md`
- `rg -n "SEED-003-ecosystem-integrations promotion" reference/host_app/SCOPE.md`
- `rg -n "gen_smtp listener expansion" reference/host_app/SCOPE.md`
- `rg -n "second product surface" reference/host_app/SCOPE.md`
- `rg -n "Scope contract: see reference/host_app/SCOPE.md" reference/host_app/README.md`
</verification>

<success_criteria>
HOST-03 is satisfied with an explicit and machine-enforced scope allowlist/non-goals contract that prevents the reference host from expanding beyond the v1.3 trust-proof boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/52-trust-scope-lock-reference-host-baseline/52-03-SUMMARY.md`
</output>
