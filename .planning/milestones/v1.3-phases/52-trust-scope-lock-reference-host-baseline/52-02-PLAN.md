---
phase: "52"
plan: "02"
type: execute
wave: 2
depends_on:
  - "52-01"
files_modified:
  - "reference/host_app/lib/mailglass_reference_host_web/router.ex"
  - "reference/host_app/config/runtime.exs"
  - "reference/host_app/README.md"
  - "test/reference_host/public_seams_contract_test.exs"
autonomous: true
requirements:
  - HOST-02
must_haves:
  truths:
    - "Reference host integration is wired only through stable public Mailglass seams."
    - "No internal Mailglass, MailglassAdmin, or MailglassInbound modules are referenced from the host."
    - "Boundary assertions fail closed if forbidden seams or copied provider internals appear."
  artifacts:
    - path: "reference/host_app/lib/mailglass_reference_host_web/router.ex"
      provides: "Concrete host wiring through public admin/operator and ingress seams"
      contains: "mailglass_admin_routes"
    - path: "test/reference_host/public_seams_contract_test.exs"
      provides: "Deterministic seam-boundary contract proof for HOST-02"
      contains: "refute"
  key_links:
    - from: "docs/api_stability.md"
      to: "test/reference_host/public_seams_contract_test.exs"
      via: "stable/internal split becomes executable required/forbidden seam checks"
      pattern: "stable|internal|Mailglass.deliver/2"
    - from: "mailglass_admin/docs/api_stability.md"
      to: "reference/host_app/lib/mailglass_reference_host_web/router.ex"
      via: "router macros are mounted only through documented admin seams"
      pattern: "mailglass_admin_routes/2|mailglass_operator_routes/2"
---

<objective>
Enforce HOST-02 by making the reference host prove public-seam-only integration with deterministic fail-closed boundary tests.

Purpose: Lock D-02 and D-03 so the trust host demonstrates adopter integration without coupling to package internals or copied provider code.
Output: host wiring references only stable seams + `public_seams_contract_test.exs` required/forbidden checks.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-CONTEXT.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-RESEARCH.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-PATTERNS.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-VALIDATION.md
@docs/api_stability.md
@mailglass_admin/docs/api_stability.md
@mailglass_inbound/docs/api_stability.md
@reference/host_app/lib/mailglass_reference_host_web/router.ex
@reference/host_app/config/runtime.exs
@reference/host_app/README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire the host only through documented stable Mailglass seams</name>
  <read_first>
    - docs/api_stability.md
    - mailglass_admin/docs/api_stability.md
    - mailglass_inbound/docs/api_stability.md
    - reference/host_app/lib/mailglass_reference_host_web/router.ex
    - reference/host_app/config/runtime.exs
  </read_first>
  <files>reference/host_app/lib/mailglass_reference_host_web/router.ex, reference/host_app/config/runtime.exs, reference/host_app/README.md</files>
  <action>Update host router/runtime/docs so integration references only these exact public seams: `Mailglass.deliver/2`, `Mailglass.deliver!/2`, `Mailglass.deliver_later/2`, `MailglassAdmin.Router.mailglass_admin_routes/2`, `MailglassAdmin.Router.mailglass_operator_routes/2`, and `MailglassInbound.Ingress.Plug`. Add explicit README boundary line `Public seam boundary: this host does not call Mailglass internal modules or provider internals.` Remove or refuse any reference to forbidden namespaces: `Mailglass.Repo`, `Mailglass.Outbound.Projector`, `Mailglass.OptionalDeps`, `MailglassAdmin.Operator.Mount`, `MailglassInbound.Ingress.Providers`.</action>
  <acceptance_criteria>
    - Router/runtime/docs contain the exact allowed seam tokens listed in the action.
    - README contains the explicit public-seam boundary line verbatim.
    - No forbidden namespace tokens remain in host files.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Mailglass\.deliver/2|Mailglass\.deliver!/2|Mailglass\.deliver_later/2|mailglass_admin_routes/2|mailglass_operator_routes/2|MailglassInbound\.Ingress\.Plug" reference/host_app/lib/mailglass_reference_host_web/router.ex reference/host_app/config/runtime.exs reference/host_app/README.md</automated>
    <automated>! rg -n "Mailglass\.Repo|Mailglass\.Outbound\.Projector|Mailglass\.OptionalDeps|MailglassAdmin\.Operator\.Mount|MailglassInbound\.Ingress\.Providers" reference/host_app</automated>
  </verify>
  <done>The reference host integration surface is constrained to stable public seams and explicitly rejects internal-module coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-closed contract tests for allowed and forbidden seam tokens</name>
  <read_first>
    - .planning/phases/52-trust-scope-lock-reference-host-baseline/52-PATTERNS.md
    - test/mailglass/docs_contract_test.exs
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    - reference/host_app/lib/mailglass_reference_host_web/router.ex
  </read_first>
  <files>test/reference_host/public_seams_contract_test.exs</files>
  <action>Create `test/reference_host/public_seams_contract_test.exs` using required/forbidden token assertions. Required tokens: `Mailglass.deliver/2`, `Mailglass.deliver!/2`, `Mailglass.deliver_later/2`, `mailglass_admin_routes/2`, `mailglass_operator_routes/2`, `MailglassInbound.Ingress.Plug`, and `Public seam boundary: this host does not call Mailglass internal modules or provider internals.` Forbidden tokens: `Mailglass.Repo`, `Mailglass.Outbound.Projector`, `Mailglass.OptionalDeps`, `MailglassAdmin.Operator.Mount`, `MailglassInbound.Ingress.Providers`, `defmodule MailglassInbound.Ingress.Providers`, and `copied provider internals`. Fail with explicit HOST-02 message if any assertion fails.</action>
  <acceptance_criteria>
    - Contract test exists and checks all required and forbidden tokens listed in the action.
    - Failure messages include `HOST-02` so drift is obvious in CI output.
    - Test passes with warnings-as-errors.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/reference_host/public_seams_contract_test.exs --warnings-as-errors</automated>
    <automated>rg -n "HOST-02|Mailglass\.deliver/2|Mailglass\.deliver!/2|Mailglass\.deliver_later/2|MailglassInbound\.Ingress\.Plug|Mailglass\.Repo|Mailglass\.Outbound\.Projector|Mailglass\.OptionalDeps|MailglassAdmin\.Operator\.Mount|MailglassInbound\.Ingress\.Providers" test/reference_host/public_seams_contract_test.exs</automated>
  </verify>
  <done>HOST-02 is mechanically enforced by deterministic boundary tests that fail closed on internal seam drift.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host wiring -> package contract surface | The host must demonstrate only what adopters can safely rely on as stable API seams. |
| docs claims -> executable assertions | Boundary claims are trustworthy only when enforced by tests. |
| provider internals -> host codebase | Internal provider logic must never be copied into the host artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Elevation of Privilege | host access to internal modules | mitigate | Define forbidden namespace token list and fail tests when internal modules appear. |
| T-52-06 | Tampering | boundary documentation drift | mitigate | Mirror README boundary sentence in required-token contract tests. |
| T-52-07 | Repudiation | claim that host uses only public seams | mitigate | Require both positive seam assertions and negative forbidden assertions in `public_seams_contract_test.exs`. |
| T-52-08 | Information Disclosure | accidental exposure of provider internals in host source | mitigate | Refute copied-provider markers and forbidden provider module names via deterministic grep/test checks. |
</threat_model>

<verification>
- `mix test test/reference_host/public_seams_contract_test.exs --warnings-as-errors`
- `rg -n "Mailglass\.deliver/2|Mailglass\.deliver!/2|Mailglass\.deliver_later/2|mailglass_admin_routes/2|mailglass_operator_routes/2|MailglassInbound\.Ingress\.Plug" reference/host_app/lib/mailglass_reference_host_web/router.ex reference/host_app/config/runtime.exs reference/host_app/README.md`
- `! rg -n "Mailglass\.Repo|Mailglass\.Outbound\.Projector|Mailglass\.OptionalDeps|MailglassAdmin\.Operator\.Mount|MailglassInbound\.Ingress\.Providers|copied provider internals" reference/host_app`
</verification>

<success_criteria>
HOST-02 is satisfied because the reference host demonstrates only stable public integration seams and CI fails immediately if internal modules or provider internals are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/52-trust-scope-lock-reference-host-baseline/52-02-SUMMARY.md`
</output>
