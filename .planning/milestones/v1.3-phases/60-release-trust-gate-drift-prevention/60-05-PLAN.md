---
phase: 60-release-trust-gate-drift-prevention
plan: 05
type: gap_closure
wave: fix
depends_on: ["60-VERIFICATION"]
files_modified:
  - reference/host_app/mix.exs
  - reference/host_app/mix.lock
  - scripts/check_clean_baseline_hex_only.sh
  - test/mailglass/publish/ci_trust_lane_contract_test.exs
autonomous: true
requirements: [EVID-02, EVID-03]
must_haves:
  truths:
    - "reference/host_app pins mailglass/mailglass_admin/mailglass_inbound to the 1.3/1.3/0.3 release line"
    - "reference/host_app lock resolves those sibling packages from Hex at 1.3.0/1.3.0/0.3.0"
    - "the clean-baseline guard fails if the siblings are Hex-sourced but stale"
  artifacts:
    - path: "reference/host_app/mix.exs"
      provides: "Current clean-baseline dependency constraints"
      contains: "{:mailglass, \"~> 1.3\"}"
    - path: "reference/host_app/mix.lock"
      provides: "Hex lock entries for the current release line"
      contains: "\"mailglass\": {:hex, :mailglass, \"1.3.0\""
    - path: "scripts/check_clean_baseline_hex_only.sh"
      provides: "Hex-source and expected-version guard for clean-baseline/published trust lanes"
      contains: "1.3.0"
---

# Phase 60 Plan 05: Close Reference Host Version Drift

## Objective

Close the blocker found in `60-VERIFICATION.md`: the clean-baseline reference host is Hex-sourced, but it is still pinned and locked to `mailglass` 1.2.0, `mailglass_admin` 1.2.0, and `mailglass_inbound` 0.2.0. Phase 60 needs the 1.3/1.3/0.3 release line so the clean-baseline and published trust lanes prove the current release.

## Tasks

### Task 1: Bump and refresh the reference host sibling deps

Files:
- `reference/host_app/mix.exs`
- `reference/host_app/mix.lock`

Action:
- Change the three reference host sibling constraints to:
  - `{:mailglass, "~> 1.3"}`
  - `{:mailglass_admin, "~> 1.3"}`
  - `{:mailglass_inbound, "~> 0.3"}`
- From `reference/host_app`, run a scoped dependency refresh for only those siblings:
  - `mix deps.update mailglass mailglass_admin mailglass_inbound`
- Review the lock diff. Accept only those three sibling updates plus resolver-required patch churn that is explicitly called out in the summary.

Verification:
- `rg -n '{:mailglass, "~> 1.3"}|{:mailglass_admin, "~> 1.3"}|{:mailglass_inbound, "~> 0.3"}' reference/host_app/mix.exs`
- `rg -n '"mailglass": \{:hex, :mailglass, "1.3.0"|"mailglass_admin": \{:hex, :mailglass_admin, "1.3.0"|"mailglass_inbound": \{:hex, :mailglass_inbound, "0.3.0"' reference/host_app/mix.lock`

### Task 2: Make the clean-baseline guard version-specific

Files:
- `scripts/check_clean_baseline_hex_only.sh`
- `test/mailglass/publish/ci_trust_lane_contract_test.exs`

Action:
- Extend the guard so it asserts both source and version:
  - `mailglass` resolves via `:hex` at `1.3.0`
  - `mailglass_admin` resolves via `:hex` at `1.3.0`
  - `mailglass_inbound` resolves via `:hex` at `0.3.0`
- Preserve the current path/CWD behavior, because CI invokes it from `reference/host_app`.
- Add or extend a contract test so the guard's expected-version behavior cannot drift back to Hex-source-only.

Verification:
- `(cd reference/host_app && bash ../../scripts/check_clean_baseline_hex_only.sh)`
- `MIX_ENV=test mix test test/mailglass/publish/ci_trust_lane_contract_test.exs`

### Task 3: Re-run Phase 60 verification slice

Run:
- `MIX_ENV=test mix test test/mailglass/publish/ci_trust_lane_contract_test.exs test/mailglass/publish/post_publish_smoke_contract_test.exs test/mailglass/publish/maintaining_release_gate_contract_test.exs test/scripts/required_checks_test.exs test/mailglass/install/install_first_preview_smoke_test.exs`
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/post-publish-smoke.yml')); print('OK')"`
- `actionlint .github/workflows/ci.yml .github/workflows/post-publish-smoke.yml`

## Success Criteria

- `60-VERIFICATION.md` can be updated from `gaps_found` to `complete`.
- No manual UAT is required.
- The remaining live post-publish checks stay documented as runtime residuals, not local blockers.
