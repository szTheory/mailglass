---
phase: 60-release-trust-gate-drift-prevention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/host_app/mix.exs
  - reference/host_app/mix.lock
autonomous: true
requirements: [EVID-02, EVID-03]
must_haves:
  truths:
    - "reference/host_app resolves all three siblings from Hex at 1.3.0/1.3.0/0.3.0 (not path deps)"
    - "scripts/check_clean_baseline_hex_only.sh passes from reference/host_app (all 3 siblings :hex-sourced)"
    - "The Hex-baseline lanes in Plans 02/03 can resolve the newly-shipped sibling code"
  artifacts:
    - path: "reference/host_app/mix.exs"
      provides: "Sibling dep pins bumped to ~> 1.3 / ~> 1.3 / ~> 0.3"
      contains: "{:mailglass, \"~> 1.3\"}"
    - path: "reference/host_app/mix.lock"
      provides: "Resolved sibling entries at 1.3.0/1.3.0/0.3.0, all :hex-sourced"
      contains: "\"mailglass\": {:hex, :mailglass, \"1.3.0\""
  key_links:
    - from: "reference/host_app/mix.lock"
      to: "scripts/check_clean_baseline_hex_only.sh"
      via: "Hex-source guard reads the lock for :hex on all 3 siblings"
      pattern: ":hex, :mailglass(_admin|_inbound)?, \"(1|0)\\.3\\.0\""
---

<objective>
Bump the reference host's three sibling package pins from `~> 1.2`/`~> 1.2`/`~> 0.2` to
`~> 1.3`/`~> 1.3`/`~> 0.3` and refresh its lock to the live Hex versions
(`mailglass` 1.3.0, `mailglass_admin` 1.3.0, `mailglass_inbound` 0.3.0). This is the Wave-1
enabler: it makes `reference/host_app` resolve the newly-shipped sibling code from Hex so the
clean-baseline lane (Plan 02) and the published-version trust journey (Plan 03) build against
current code instead of stale 1.2.0/0.2.0.

Purpose: Both Hex-baseline trust lanes (EVID-02 in `ci.yml`, EVID-03 in `post-publish-smoke.yml`)
must orchestrate a Hex-sourced reference-host build. Until the pins move to the published 1.3.x
line, those lanes would either fail to resolve or prove stale code. Per CONTEXT D-02.
Output: An edited `reference/host_app/mix.exs` (3 pins) and a scoped-refreshed
`reference/host_app/mix.lock` (only the 3 sibling entries move; no transitive churn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-release-trust-gate-drift-prevention/60-RESEARCH.md

<interfaces>
<!-- Current pins (reference/host_app/mix.exs:32-34): -->
<!--   {:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"} -->
<!-- Current lock entries (reference/host_app/mix.lock:20-22) are :hex-sourced at 1.2.0/1.2.0/0.2.0. -->
<!-- Live Hex targets [VERIFIED 2026-05-29]: mailglass 1.3.0, mailglass_admin 1.3.0, mailglass_inbound 0.3.0. -->
<!-- Hex-source guard: scripts/check_clean_baseline_hex_only.sh defaults LOCK_PATH=mix.lock relative to CWD -->
<!-- (RESEARCH Pitfall 2), so it MUST be invoked from reference/host_app or given an explicit lock path. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump the three sibling pins and scoped-refresh the lock</name>
  <files>reference/host_app/mix.exs, reference/host_app/mix.lock</files>
  <read_first>
    - reference/host_app/mix.exs (the `deps/0` list, lines 32-34 hold the three sibling pins)
    - reference/host_app/mix.lock (current sibling entries, lines 20-22; confirm :hex source at 1.2.0/1.2.0/0.2.0)
    - .planning/phases/60-release-trust-gate-drift-prevention/60-RESEARCH.md (Standard Stack "Dep pin bump", Pitfall 8 mix.lock integration drift)
    - .planning/phases/60-release-trust-gate-drift-prevention/60-CONTEXT.md (D-02)
  </read_first>
  <action>
    Edit `reference/host_app/mix.exs` `deps/0`: change `{:mailglass, "~> 1.2"}` to `{:mailglass, "~> 1.3"}`,
    `{:mailglass_admin, "~> 1.2"}` to `{:mailglass_admin, "~> 1.3"}`, and `{:mailglass_inbound, "~> 0.2"}`
    to `{:mailglass_inbound, "~> 0.3"}`. Do NOT touch the other deps (`phoenix`, `phoenix_ecto`,
    `ecto_sql`, `postgrex`, `jason`, `plug_cowboy`). Then refresh the lock SCOPED — run
    `mix deps.update mailglass mailglass_admin mailglass_inbound` from `reference/host_app/`
    (NOT `mix deps.get`/`mix deps.update --all`, per Pitfall 8 + the project mix.lock-resolution memory).
    Review the resulting `git diff reference/host_app/mix.lock`: only the `mailglass`, `mailglass_admin`,
    and `mailglass_inbound` entries should move (1.2.0->1.3.0, 1.2.0->1.3.0, 0.2.0->0.3.0, including the
    inner `{:mailglass, "1.3.0", ...}` constraint inside the admin/inbound entries). If ANY unrelated
    transitive dependency line changes, surface it in the SUMMARY and do not silently accept it.
    All three sibling entries must remain `:hex`-sourced — never `:path` or `:git`.
  </action>
  <verify>
    <automated>cd reference/host_app && grep -q '{:mailglass, "~> 1.3"}' mix.exs && grep -q '{:mailglass_admin, "~> 1.3"}' mix.exs && grep -q '{:mailglass_inbound, "~> 0.3"}' mix.exs && bash ../../scripts/check_clean_baseline_hex_only.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `reference/host_app/mix.exs` contains the exact strings `{:mailglass, "~> 1.3"}`, `{:mailglass_admin, "~> 1.3"}`, `{:mailglass_inbound, "~> 0.3"}` and no remaining `~> 1.2` / `~> 0.2` sibling pin.
    - `reference/host_app/mix.lock` `mailglass` entry is `{:hex, :mailglass, "1.3.0", ...}`; `mailglass_admin` is `{:hex, :mailglass_admin, "1.3.0", ...}` and its inner pin reads `{:mailglass, "1.3.0", ...}`; `mailglass_inbound` is `{:hex, :mailglass_inbound, "0.3.0", ...}` and its inner pin reads `{:mailglass, "1.3.0", ...}`.
    - `git diff --name-only reference/host_app/mix.lock` shows changes confined to the three sibling entries (no unrelated transitive line churn); any churn is flagged in the SUMMARY.
    - `bash scripts/check_clean_baseline_hex_only.sh` run with CWD `reference/host_app` exits 0 (all three siblings positively `:hex`-sourced).
  </acceptance_criteria>
  <done>
    The reference host pins and lock target the live 1.3.x sibling line, all three resolve from Hex,
    the Hex-source guard passes from `reference/host_app/`, and the lock diff is confined to the
    three sibling entries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry -> reference host build | Sibling packages are first-party, self-published by this monorepo to its own Hex account; not third-party untrusted input. |
| mix.lock -> CI trust lanes | The lock is the source of truth the Hex-source guard inspects to assert clean-baseline resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `reference/host_app/mix.lock` refresh pulling unrelated transitive churn | mitigate | Scoped `mix deps.update mailglass mailglass_admin mailglass_inbound`; diff reviewed; only 3 sibling entries may move (RESEARCH Pitfall 8). |
| T-60-02 | Spoofing | Registry confusion on the three sibling packages | accept | Self-published first-party packages on this repo's own Hex account; live versions confirmed via hex.pm API (RESEARCH Package Legitimacy Audit — not applicable). |
| T-60-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager install in this plan; only a version-pin bump on first-party self-published siblings. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `cd reference/host_app && bash ../../scripts/check_clean_baseline_hex_only.sh` exits 0.
- `git diff reference/host_app/mix.lock` confined to the three sibling entries.
- `cd reference/host_app && MIX_ENV=dev mix deps.get && mix compile` resolves and compiles against Hex-sourced 1.3.x (optional local confirmation that Plans 02/03 will have a buildable host).
</verification>

<success_criteria>
- Three sibling pins read `~> 1.3` / `~> 1.3` / `~> 0.3` in `reference/host_app/mix.exs`.
- Lock resolves to 1.3.0/1.3.0/0.3.0, all `:hex`-sourced, no transitive churn.
- Hex-source guard passes from `reference/host_app/`.
</success_criteria>

<output>
Create `.planning/phases/60-release-trust-gate-drift-prevention/60-01-SUMMARY.md` when done.
</output>
