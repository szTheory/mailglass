---
phase: "57"
plan: "01"
title: "Canonical Trust Runner Command and Deterministic Stage Flow"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mix.exs"
  - "lib/mix/tasks/mailglass.trust.run.ex"
  - "test/reference_host/trust_runner_command_contract_test.exs"
  - "reference/host_app/README.md"
autonomous: true
requirements:
  - JOUR-01
must_haves:
  truths:
    - "One canonical command is the only supported entrypoint for trust-journey orchestration across local and CI surfaces."
    - "Runner stage ordering is deterministic and explicitly encoded as install -> preview -> send -> webhook_ingest -> operator_troubleshooting."
    - "Phase 57 does not claim signed-negative webhook or non-happy-path operator diagnosis completeness (deferred to Phase 58)."
  artifacts:
    - path: "lib/mix/tasks/mailglass.trust.run.ex"
      provides: "Deterministic trust-runner command implementation and stage contract"
      contains: "install, preview, send, webhook_ingest, operator_troubleshooting"
    - path: "mix.exs"
      provides: "Single canonical command alias used by local and CI wrappers"
      contains: "verify.reference_host.journey"
    - path: "test/reference_host/trust_runner_command_contract_test.exs"
      provides: "Fail-closed command/stage contract assertions"
      contains: "JOUR-01"
  key_links:
    - from: ".planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md"
      to: "lib/mix/tasks/mailglass.trust.run.ex"
      via: "D-01/D-02 single entrypoint and deterministic stage ordering"
      pattern: "one canonical runner entrypoint|install -> preview -> send -> webhook ingest -> operator troubleshooting"
    - from: ".planning/research/ARCHITECTURE.md"
      to: "mix.exs"
      via: "single trust runner command used by local and CI"
      pattern: "Define one trust runner that both humans and CI call"
---

<objective>
Implement JOUR-01 by introducing one deterministic trust-runner command that executes the full reference-host trust journey with stable stage keys and explicit phase boundary language.

Purpose: remove orchestration drift by making one command the only supported trust entrypoint for local and CI invocations.
Output: canonical verify alias + runner task + command contract tests + reference-host docs pointer.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-RESEARCH.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-PATTERNS.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-VALIDATION.md
@mix.exs
@reference/host_app/README.md
@test/reference_host/boot_contract_test.exs
@test/reference_host/public_seams_contract_test.exs
@test/reference_host/scope_lock_contract_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add one canonical trust-runner command entrypoint and deterministic stage pipeline</name>
  <read_first>
    - .planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
    - .planning/phases/57-deterministic-trust-runner-fixtures/57-RESEARCH.md
    - mix.exs
    - lib/mix/tasks/mailglass.publish.check.ex
    - lib/mix/tasks/mail.doctor.ex
  </read_first>
  <files>mix.exs, lib/mix/tasks/mailglass.trust.run.ex</files>
  <action>Add one canonical command alias in `mix.exs` named `verify.reference_host.journey` with explicit preferred env `:test`. Implement `Mix.Tasks.Mailglass.Trust.Run` that executes the exact stage sequence `install`, `preview`, `send`, `webhook_ingest`, `operator_troubleshooting` in fixed order and emits deterministic stage records (stable stage keys, explicit status values, no random identifiers). Support concrete options `--checkpoint-out`, `--host-root`, and `--dry-run`. Fail with explicit runner errors when any required stage signal is missing; do not silently skip stages.</action>
  <acceptance_criteria>
    - `mix.exs` contains `verify.reference_host.journey` in both alias list and preferred env mapping.
    - `mailglass.trust.run` task module exists and includes the exact five-stage sequence in deterministic order.
    - Task supports `--checkpoint-out`, `--host-root`, and `--dry-run` option parsing.
    - Running `mix verify.reference_host.journey --dry-run` exits successfully and emits all five stage keys.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "verify.reference_host.journey" mix.exs</automated>
    <automated>rg -n "defmodule Mix\\.Tasks\\.Mailglass\\.Trust\\.Run|install|preview|send|webhook_ingest|operator_troubleshooting|checkpoint_out|host_root|dry_run" lib/mix/tasks/mailglass.trust.run.ex</automated>
    <automated>mix help mailglass.trust.run</automated>
    <automated>mix verify.reference_host.journey --dry-run</automated>
  </verify>
  <done>The repo has exactly one deterministic trust-runner command entrypoint suitable for local and CI reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-closed command contract tests and explicit deferred-scope guardrails</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
    - test/reference_host/boot_contract_test.exs
    - reference/host_app/README.md
  </read_first>
  <files>test/reference_host/trust_runner_command_contract_test.exs, reference/host_app/README.md</files>
  <action>Create `test/reference_host/trust_runner_command_contract_test.exs` that asserts required stage tokens for JOUR-01 (`install`, `preview`, `send`, `webhook_ingest`, `operator_troubleshooting`) and asserts deferred-scope language that Phase 58 owns signed-negative webhook proof and scripted non-happy-path diagnosis (`JOUR-03`, `JOUR-04`). Update `reference/host_app/README.md` with one canonical command example line `mix verify.reference_host.journey` and one explicit defer line that signed-negative and non-happy-path diagnosis are deferred to Phase 58.</action>
  <acceptance_criteria>
    - Contract test exists and contains required token assertions for all five stage keys.
    - Contract test includes deferred-scope assertions for `JOUR-03` and `JOUR-04`.
    - `reference/host_app/README.md` contains `mix verify.reference_host.journey`.
    - `reference/host_app/README.md` contains explicit "deferred to Phase 58" language for signed-negative and non-happy-path proof.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/reference_host/trust_runner_command_contract_test.exs --warnings-as-errors</automated>
    <automated>rg -n "install|preview|send|webhook_ingest|operator_troubleshooting|JOUR-03|JOUR-04|Phase 58" test/reference_host/trust_runner_command_contract_test.exs</automated>
    <automated>rg -n "mix verify.reference_host.journey|Phase 58|signed|non-happy-path" reference/host_app/README.md</automated>
  </verify>
  <done>JOUR-01 command semantics and phase-boundary constraints are mechanically enforced by deterministic tests and docs tokens.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command invocation -> journey execution | Runner invocation must always execute the same ordered trust stages. |
| phase-57 claims -> milestone scope | Command/docs must not imply Phase 58 negative-path work is already complete. |
| local use -> CI wrapper reuse | CI wrappers must call the canonical command rather than duplicating orchestration logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | stage order/keys drift | mitigate | hard-code ordered stage list and pin in contract tests |
| T-57-02 | Repudiation | ambiguous runner completion claims | mitigate | explicit stage-status outputs and fail-closed missing-stage errors |
| T-57-03 | Spoofing | alternative command surfaces diverge from canonical flow | mitigate | one verify alias + docs token enforcing canonical command |
| T-57-04 | Misrepresentation | phase-58 deferred work implied as complete | mitigate | required deferred-scope assertions in tests/docs |
</threat_model>

<verification>
- `mix help mailglass.trust.run`
- `mix verify.reference_host.journey --dry-run`
- `mix test test/reference_host/trust_runner_command_contract_test.exs --warnings-as-errors`
- `rg -n "verify.reference_host.journey" mix.exs`
- `rg -n "install|preview|send|webhook_ingest|operator_troubleshooting" lib/mix/tasks/mailglass.trust.run.ex`
</verification>

<success_criteria>
JOUR-01 is satisfied at the command-contract layer: one canonical deterministic runner command exists, executes stable trust stages, and explicitly preserves the Phase 57/58 boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/57-deterministic-trust-runner-fixtures/57-01-SUMMARY.md`
</output>
