---
phase: "57"
plan: "02"
title: "Deterministic Fixtures and Trust Checkpoint Contract"
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - "lib/mailglass/reference_host/trust_checkpoint.ex"
  - "test/support/reference_host/trust_runner_fixtures.ex"
  - "scripts/check_trust_runner_checkpoint.sh"
  - "test/reference_host/trust_runner_fixture_contract_test.exs"
  - "test/reference_host/trust_runner_checkpoint_contract_test.exs"
  - "MAINTAINING.md"
autonomous: true
requirements:
  - JOUR-02
  - JOUR-01
must_haves:
  truths:
    - "Fixture IDs, scenario ordering, and checkpoint ordering remain stable across local reruns and CI."
    - "Trust checkpoint output is machine-readable, schema-versioned, and includes bounded-claim language."
    - "Downstream trust lanes consume Phase 57 checkpoint semantics without inferring Phase 58 signed-negative/non-happy-path completion."
  artifacts:
    - path: "lib/mailglass/reference_host/trust_checkpoint.ex"
      provides: "Schema-versioned trust checkpoint encoder with deterministic ordering and hash output"
      contains: "trust_runner.v1"
    - path: "scripts/check_trust_runner_checkpoint.sh"
      provides: "Executable checkpoint validator for deterministic schema/boundary/order assertions"
      contains: "schema_version"
    - path: "test/reference_host/trust_runner_checkpoint_contract_test.exs"
      provides: "Repeatability and schema contract tests for runner artifacts"
      contains: "claim_boundary"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/preview/capture_manifest.ex"
      to: "lib/mailglass/reference_host/trust_checkpoint.ex"
      via: "schema_version + claim_boundary + sorted deterministic hash contract pattern"
      pattern: "schema_version|claim_boundary|matrix_sha256"
    - from: "scripts/check_preview_capture_checkpoint.sh"
      to: "scripts/check_trust_runner_checkpoint.sh"
      via: "executable validator shape with explicit fail-closed messages"
      pattern: "missing checkpoint|schema_version|claim_boundary"
---

<objective>
Implement JOUR-02 by introducing deterministic fixture/checkpoint contracts that produce stable machine-readable trust evidence for local reruns and future CI trust lanes.

Purpose: make trust assertions reproducible and objectively verifiable across environments.
Output: deterministic fixture source, trust checkpoint encoder, validation script, repeatability tests, and downstream handoff docs.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-RESEARCH.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-PATTERNS.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-VALIDATION.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-01-PLAN.md
@mailglass_admin/lib/mailglass_admin/preview/capture_manifest.ex
@scripts/check_preview_capture_checkpoint.sh
@mailglass_admin/test/mailglass_admin/preview/capture_manifest_test.exs
@MAINTAINING.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement deterministic fixture catalog and schema-versioned trust checkpoint encoder</name>
  <read_first>
    - .planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
    - .planning/phases/57-deterministic-trust-runner-fixtures/57-RESEARCH.md
    - mailglass_admin/lib/mailglass_admin/preview/capture_manifest.ex
    - test/reference_host/trust_runner_command_contract_test.exs
  </read_first>
  <files>test/support/reference_host/trust_runner_fixtures.ex, lib/mailglass/reference_host/trust_checkpoint.ex</files>
  <action>Create a deterministic fixture module `Mailglass.ReferenceHost.TrustRunnerFixtures` with stable scenario IDs and fixed stage fixture ordering for `install`, `preview`, `send`, `webhook_ingest`, and `operator_troubleshooting`. Create checkpoint encoder module `Mailglass.ReferenceHost.TrustCheckpoint` with explicit schema `trust_runner.v1`, bounded claim text `reference-host trust-journey confidence only; signed-negative webhook and non-happy-path diagnosis are deferred to Phase 58`, deterministic ordered `checkpoints` rows, and aggregate hash field `checkpoint_sha256` derived from ordered stage rows.</action>
  <acceptance_criteria>
    - Fixture module exports deterministic stage fixture list with stable IDs and stable sort order.
    - Checkpoint encoder module contains schema `trust_runner.v1` and bounded claim text including `deferred to Phase 58`.
    - Checkpoint rows are sorted deterministically before hash calculation.
    - Encoder output includes `schema_version`, `claim_boundary`, `checkpoint_count`, `checkpoint_sha256`, and `checkpoints`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "defmodule Mailglass\\.ReferenceHost\\.TrustRunnerFixtures|install|preview|send|webhook_ingest|operator_troubleshooting" test/support/reference_host/trust_runner_fixtures.ex</automated>
    <automated>rg -n "defmodule Mailglass\\.ReferenceHost\\.TrustCheckpoint|trust_runner\\.v1|claim_boundary|checkpoint_sha256|checkpoint_count|checkpoints" lib/mailglass/reference_host/trust_checkpoint.ex</automated>
    <automated>rg -n "deferred to Phase 58|signed-negative webhook|non-happy-path" lib/mailglass/reference_host/trust_checkpoint.ex</automated>
  </verify>
  <done>Deterministic fixture and checkpoint schema foundations are in place for reproducible trust assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable checkpoint validator and deterministic repeatability contract tests</name>
  <read_first>
    - scripts/check_preview_capture_checkpoint.sh
    - mailglass_admin/test/mailglass_admin/preview/capture_manifest_test.exs
    - lib/mailglass/reference_host/trust_checkpoint.ex
    - test/support/reference_host/trust_runner_fixtures.ex
  </read_first>
  <files>scripts/check_trust_runner_checkpoint.sh, test/reference_host/trust_runner_fixture_contract_test.exs, test/reference_host/trust_runner_checkpoint_contract_test.exs</files>
  <action>Create `scripts/check_trust_runner_checkpoint.sh` that validates trust checkpoint files for required keys (`schema_version`, `claim_boundary`, `checkpoint_count`, `checkpoint_sha256`, ordered `checkpoints`) and exact required stage set (`install`, `preview`, `send`, `webhook_ingest`, `operator_troubleshooting`). Add fixture contract test asserting stable IDs and order. Add checkpoint contract test that runs `mix verify.reference_host.journey --dry-run --checkpoint-out tmp/mailglass_trust_runner/checkpoint-1.json` and again for `checkpoint-2.json`, then asserts normalized checkpoint payload equality and matching `checkpoint_sha256` values.</action>
  <acceptance_criteria>
    - Checkpoint validator script exits non-zero when required keys or stage set are missing.
    - Fixture contract test verifies deterministic fixture IDs/order.
    - Checkpoint contract test verifies two-run deterministic equality and hash stability.
    - All tests pass with warnings-as-errors.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check_trust_runner_checkpoint.sh --help</automated>
    <automated>mix test test/reference_host/trust_runner_fixture_contract_test.exs --warnings-as-errors</automated>
    <automated>mix test test/reference_host/trust_runner_checkpoint_contract_test.exs --warnings-as-errors</automated>
    <automated>rg -n "schema_version|claim_boundary|checkpoint_count|checkpoint_sha256|webhook_ingest|operator_troubleshooting" scripts/check_trust_runner_checkpoint.sh</automated>
  </verify>
  <done>Deterministic trust checkpoint contract is executable and repeatability is proven by automated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Document downstream checkpoint handoff contract for future trust lanes</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
    - MAINTAINING.md
    - scripts/check_trust_runner_checkpoint.sh
  </read_first>
  <files>MAINTAINING.md</files>
  <action>Add a concise `Trust runner checkpoint handoff` section to `MAINTAINING.md` documenting canonical command `mix verify.reference_host.journey`, default checkpoint artifact path (`tmp/mailglass_trust_runner/checkpoint.json`), required keys (`schema_version`, `claim_boundary`, `checkpoint_count`, `checkpoint_sha256`, `checkpoints`), and explicit note that Phase 58 extends this contract with signed-negative and non-happy-path semantics rather than redefining Phase 57 stage keys.</action>
  <acceptance_criteria>
    - `MAINTAINING.md` contains canonical command and checkpoint path.
    - `MAINTAINING.md` lists required checkpoint keys exactly.
    - `MAINTAINING.md` states Phase 58 extension boundary explicitly.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Trust runner checkpoint handoff|mix verify.reference_host.journey|tmp/mailglass_trust_runner/checkpoint.json|schema_version|claim_boundary|checkpoint_count|checkpoint_sha256|Phase 58" MAINTAINING.md</automated>
  </verify>
  <done>Downstream CI/release lane work can consume stable Phase 57 checkpoint semantics without contract ambiguity.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture source -> checkpoint evidence | Fixture identity must remain deterministic or trust evidence becomes flaky. |
| checkpoint artifact -> CI/release consumption | Artifact schema and ordering must be stable and machine-validated. |
| phase-57 checkpoint semantics -> phase-58 extension | Deferred phase behavior must extend the contract without rewriting baseline stage semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Tampering | fixture IDs/order | mitigate | fixture contract test pins IDs/order and fails on drift |
| T-57-06 | Repudiation | checkpoint integrity | mitigate | deterministic hash + required-key validator script |
| T-57-07 | Information Disclosure | checkpoint over-claims | mitigate | bounded claim text explicitly limits trust assertion scope |
| T-57-08 | Elevation of Privilege | downstream lanes assume phase-58 guarantees | mitigate | explicit defer language in checkpoint and maintenance docs |
</threat_model>

<verification>
- `mix test test/reference_host/trust_runner_fixture_contract_test.exs --warnings-as-errors`
- `mix test test/reference_host/trust_runner_checkpoint_contract_test.exs --warnings-as-errors`
- `bash scripts/check_trust_runner_checkpoint.sh --checkpoint tmp/mailglass_trust_runner/checkpoint.json`
- `rg -n "trust_runner.v1|claim_boundary|checkpoint_sha256" lib/mailglass/reference_host/trust_checkpoint.ex`
- `rg -n "mix verify.reference_host.journey|checkpoint_sha256|Phase 58" MAINTAINING.md`
</verification>

<success_criteria>
JOUR-02 is satisfied: fixtures and checkpoints are deterministic across reruns, checkpoint semantics are machine-verifiable, and downstream trust lanes have a stable handoff contract.
</success_criteria>

<output>
After completion, create `.planning/phases/57-deterministic-trust-runner-fixtures/57-02-SUMMARY.md`
</output>
