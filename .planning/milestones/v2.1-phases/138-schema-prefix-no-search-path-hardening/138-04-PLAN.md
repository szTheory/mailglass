---
phase: 138-schema-prefix-no-search-path-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 138-01
  - 138-02
  - 138-03
files_modified:
  - mix.exs
  - .github/workflows/advisory-matrix.yml
autonomous: true
requirements:
  - GATE-01
  - GATE-02
must_haves:
  truths:
    - "A single focused command runs the hostile no-search-path runtime proofs and the static prefix guard."
    - "The dual-schema advisory matrix is documented as a broad canary, not the fail-closed proof."
    - "Source assertions can prove the focused lane and canary wording exist."
  artifacts:
    - path: "mix.exs"
      provides: "`mix verify.schema_prefix` preferred env and alias"
      contains: "verify.schema_prefix"
    - path: ".github/workflows/advisory-matrix.yml"
      provides: "Canary wording for core and inbound dual-schema advisory jobs"
      contains: "verify.schema_prefix"
  key_links:
    - from: "mix.exs"
      to: "test/mailglass/schema_prefix_hardening_test.exs"
      via: "verify.schema_prefix runs hostile runtime proof"
      pattern: "schema_prefix_hardening_test"
    - from: "mix.exs"
      to: "test/mailglass/credo/raw_repo_prefix_contract_test.exs"
      via: "verify.schema_prefix runs static guard test and strict Credo"
      pattern: "raw_repo_prefix_contract_test"
    - from: "mix.exs"
      to: "mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs"
      via: "verify.schema_prefix delegates inbound contract proof"
      pattern: "schema_prefix_contract_test"
---

<objective>
Wire the focused verification lane and document the advisory matrix correctly: `mix verify.schema_prefix` is the fail-closed Phase 138 proof, while the dual-schema matrix remains broad canary coverage.

Purpose: GATE-01 and GATE-02 make the hardening repeatable and prevent the broad matrix from being mistaken for the proof that catches search_path-dependent runtime calls.
Output: Mix alias wiring plus canary/proof comments in the advisory matrix workflow.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-RESEARCH.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-VALIDATION.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
@mix.exs
@.github/workflows/advisory-matrix.yml
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Produced in plan |
|------|---------------|------------------|
| Test module | `Mailglass.SchemaPrefixHardeningTest` in `test/mailglass/schema_prefix_hardening_test.exs` | 138-01 |
| Test helper module | `Mailglass.SchemaPrefixHardeningTest.PrefixedWrapperMigration` | 138-01 |
| Test tag | `:schema_prefix` module/test tag for focused runtime proofs | 138-01 |
| Static check module | `Mailglass.Credo.RawRepoPrefixContract` in `credo_checks/raw_repo_prefix_contract.ex` | 138-03 |
| Static check test module | `Mailglass.Credo.RawRepoPrefixContractTest` | 138-03 |
| Inbound contract test module | `MailglassInbound.SchemaPrefixContractTest` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Internal.Replay` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Execution` | 138-02 |
| Helper function | `schema_opts/0` in `Mix.Tasks.Mailglass.Inbound.Replay` | 138-02 |
| Mix alias | `mix verify.schema_prefix` | 138-04 |
| CI/documentation wording | Advisory matrix comments naming the dual-schema matrix as a broad canary and `mix verify.schema_prefix` as the focused proof | 138-04 |

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Runtime reads/writes touching mailglass tables target configured schema without search_path help | 138-01, 138-02, 138-03, 138-04 | COVERED | Hostile runtime tests, inbound raw-repo opts, static guard, and focused alias together satisfy the phase goal. |
| REQ | SCHEMA-01 | `Mailglass.Webhook.Replay` projection update hostile-path proof | 138-01 | COVERED | Task 1 creates proof and source fix. |
| REQ | SCHEMA-02 | Unsubscribe replay/idempotency conflict lookup hostile-path proof | 138-01 | COVERED | Task 2 creates proof and source fix. |
| REQ | SCHEMA-03 | Raw repo calls and callbacks are prefixed/facade-routed/allowlisted with recurrence guard | 138-03 | COVERED | Remaining projection updates plus custom Credo guard. |
| REQ | SCHEMA-04 | Inbound repo-option extension points default to facade and prove raw-repo prefix contract | 138-02 | COVERED | Raw repo tests and `schema_opts/0` changes. |
| REQ | GATE-01 | Focused schema-prefix verification lane | 138-04 | COVERED | This plan adds `mix verify.schema_prefix`. |
| REQ | GATE-02 | Dual-schema advisory matrix remains a canary | 138-04 | COVERED | This plan updates workflow comments and source assertions. |
| RESEARCH | - | Keep broad dual-schema advisory matrix as canary, not only proof | 138-04 | COVERED | Workflow comments clarify canary status. |
| RESEARCH | - | Focused alias runs hostile runtime proofs plus static guard | 138-04 | COVERED | Mix alias runs all Phase 138 focused tests and strict Credo. |
| RESEARCH | - | No new packages; no package install checkpoint required | all | COVERED | No install tasks in this phase. |
| RESEARCH | - | Schema push detection | all | COVERED | No ORM schema, migration, collection, entity, Prisma, Drizzle, Supabase, or TypeORM files are modified; no schema push task is injected. |
| CONTEXT | - | No phase CONTEXT.md exists | all | COVERED | Binding decisions come from ROADMAP, REQUIREMENTS, STATE, RESEARCH, VALIDATION, and PATTERNS. |
| STATE | v2.1 locked decision | Explicit Ecto prefix is correctness mechanism; search_path is only host/test convenience | 138-01, 138-02, 138-03 | COVERED | Verification distinguishes hostile proof from search_path-backed matrix. |
| STATE | v2.1 locked decision | Surgical hostile lane and static guard instead of whole-suite no-search-path migration | 138-01, 138-03, 138-04 | COVERED | `verify.schema_prefix` is focused and does not require fixture sweep. |

<tasks>

<task type="auto">
  <name>Task 1: Add focused verify.schema_prefix alias</name>
  <files>mix.exs</files>
  <read_first>
    @mix.exs
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-VALIDATION.md
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
    @test/mailglass/schema_prefix_hardening_test.exs
    @test/mailglass/credo/raw_repo_prefix_contract_test.exs
    @mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs
  </read_first>
  <action>In `mix.exs`, add `"verify.schema_prefix": :test` to `cli/0` preferred envs beside the other semantic verify aliases. Add a semantic alias named `"verify.schema_prefix"` near the other `verify.*` aliases. The alias must run, in order: `test test/mailglass/schema_prefix_hardening_test.exs --only schema_prefix --warnings-as-errors`, `test test/mailglass/credo/raw_repo_prefix_contract_test.exs --warnings-as-errors`, `credo --strict`, and `cmd --cd mailglass_inbound mix test test/mailglass_inbound/schema_prefix_contract_test.exs --warnings-as-errors`. Keep the lane focused; do not add the full dual-schema advisory matrix or full suite to this alias.</action>
  <verify>
    <automated>mix verify.schema_prefix</automated>
    <automated>mix run -e 'aliases = Mix.Project.config()[:aliases]; unless Keyword.has_key?(aliases, :"verify.schema_prefix"), do: raise("missing verify.schema_prefix alias")'</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` preferred envs includes `"verify.schema_prefix": :test`.
    - `mix.exs` aliases include `"verify.schema_prefix"` with the three focused proof files and `mix credo --strict`.
    - The alias delegates to `mailglass_inbound` for `schema_prefix_contract_test.exs`.
    - `mix verify.schema_prefix` exits 0 after plans 138-01 through 138-03 are complete.
  </acceptance_criteria>
  <done>GATE-01 has a single focused local/CI-ready command for Phase 138 schema-prefix proof.</done>
</task>

<task type="auto">
  <name>Task 2: Mark dual-schema advisory matrix as canary coverage</name>
  <files>.github/workflows/advisory-matrix.yml</files>
  <read_first>
    @.github/workflows/advisory-matrix.yml
    @.planning/milestones/v2.0-MILESTONE-AUDIT.md
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-RESEARCH.md
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-VALIDATION.md
  </read_first>
  <action>Update only comments in `.github/workflows/advisory-matrix.yml` around the core `Run advisory full suite` step and the inbound `Run inbound dual-schema advisory full suite` step. State that these jobs are broad canaries because the test harness aligns `Config.schema/0` and also sets connection search_path for the matrix schema, so they are not the fail-closed no-search-path proof for this milestone. Name `mix verify.schema_prefix` as the focused proof that runs hostile no-search-path runtime tests and the static raw-repo prefix guard. Do not change triggers, matrix entries, package versions, or run commands.</action>
  <verify>
    <automated>rg -n "verify\\.schema_prefix|focused no-search-path|broad canary|search_path" .github/workflows/advisory-matrix.yml mix.exs</automated>
    <automated>mix verify.schema_prefix</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/advisory-matrix.yml` comments for both core and inbound dual-schema jobs include `mix verify.schema_prefix`.
    - The comments use the terms `broad canary` and `focused no-search-path` to distinguish GATE-02 semantics.
    - Workflow job names, triggers, matrix values, and run commands are unchanged.
    - The automated source assertion and `mix verify.schema_prefix` both exit 0.
  </acceptance_criteria>
  <done>GATE-02 is source-documented: the advisory matrix remains broad canary coverage, and the focused alias is the fail-closed proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer/CI -> verification lane | The alias must run the focused proof and not silently rely on broad canary coverage. |
| Advisory matrix -> release confidence | Advisory jobs must not be misrepresented as hostile no-search-path proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-14 | Repudiation | `mix verify.schema_prefix` | mitigate | Alias runs explicit test files and strict Credo so proof evidence is reproducible from one command. |
| T-138-15 | Tampering | `mix.exs` alias wiring | mitigate | Source assertion checks alias presence; `mix verify.schema_prefix` executes the referenced lane. |
| T-138-16 | Information Disclosure | Verification output | mitigate | Alias runs tests/credo only; new tests avoid PII-bearing diagnostics. |
| T-138-17 | Spoofing | Advisory matrix comments | mitigate | Comments explicitly call the matrix a broad canary and name the focused alias as the proof. |
</threat_model>

<verification>
Run `mix verify.schema_prefix` and `rg -n "verify\\.schema_prefix|focused no-search-path|broad canary|search_path" .github/workflows/advisory-matrix.yml mix.exs`.
</verification>

<success_criteria>
GATE-01 and GATE-02 are satisfied: the focused lane is executable, and the broad dual-schema matrix is documented as canary coverage rather than the only proof.
</success_criteria>

<output>
Create `.planning/phases/138-schema-prefix-no-search-path-hardening/138-04-SUMMARY.md` when done.
</output>
