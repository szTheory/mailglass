---
phase: 138-schema-prefix-no-search-path-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 138-01
  - 138-02
files_modified:
  - lib/mailglass/adapters/fake.ex
  - lib/mailglass/webhook/reconciler.ex
  - credo_checks/raw_repo_prefix_contract.ex
  - test/mailglass/credo/raw_repo_prefix_contract_test.exs
  - .credo.exs
autonomous: true
requirements:
  - SCHEMA-03
must_haves:
  truths:
    - "Fake adapter and webhook reconciler projection updates carry explicit prefix opts."
    - "Production raw repo calls and Multi callbacks touching mailglass tables are either prefixed, facade-routed, or explicitly allowlisted."
    - "A custom Credo guard blocks recurrence under `mix credo --strict`."
  artifacts:
    - path: "lib/mailglass/adapters/fake.ex"
      provides: "Explicit prefix opts for fake adapter projection Multi update"
      contains: "Mailglass.Repo.multi_opts()"
    - path: "lib/mailglass/webhook/reconciler.ex"
      provides: "Explicit prefix opts for reconciler projection Multi updates"
      contains: "Repo.multi_opts()"
    - path: "credo_checks/raw_repo_prefix_contract.ex"
      provides: "Static recurrence guard for raw repo prefix contract"
      contains: "Mailglass.Credo.RawRepoPrefixContract"
    - path: "test/mailglass/credo/raw_repo_prefix_contract_test.exs"
      provides: "Regression tests for the static guard"
      contains: "Mailglass.Credo.RawRepoPrefixContractTest"
    - path: ".credo.exs"
      provides: "Credo registration for RawRepoPrefixContract"
      contains: "Mailglass.Credo.RawRepoPrefixContract"
  key_links:
    - from: ".credo.exs"
      to: "credo_checks/raw_repo_prefix_contract.ex"
      via: "extra_checks registration"
      pattern: "RawRepoPrefixContract"
    - from: "credo_checks/raw_repo_prefix_contract.ex"
      to: "lib/mailglass/webhook/replay.ex"
      via: "guard permits Repo.multi_opts() fixed in 138-01"
      pattern: "Repo\\.multi_opts"
---

<objective>
Close the remaining SCHEMA-03 callback gaps and add a static guard so new raw repo/Multi table operations cannot silently reintroduce search_path-dependent behavior.

Purpose: The runtime fixes from 138-01 and 138-02 solve named paths; this plan makes recurrence fail under the project lint gate and includes fake adapter plus webhook reconciler projection updates.
Output: Two source fixes, a registered custom Credo check, and guard tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-RESEARCH.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-VALIDATION.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
@lib/mailglass/repo.ex
@lib/mailglass/adapters/fake.ex
@lib/mailglass/webhook/reconciler.ex
@lib/mailglass/outbound.ex
@credo_checks/no_unscoped_tenant_query_in_lib.ex
@credo_checks/no_schema_prefix_attribute.ex
@test/mailglass/credo/no_schema_prefix_attribute_test.exs
@test/mailglass/credo/checks_have_tests_test.exs
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Produced in plan |
|------|---------------|------------------|
| Test module | `Mailglass.SchemaPrefixHardeningTest` in `test/mailglass/schema_prefix_hardening_test.exs` | 138-01 |
| Test helper module | `Mailglass.SchemaPrefixHardeningTest.PrefixedWrapperMigration` | 138-01 |
| Test tag | `:schema_prefix` module/test tag for focused runtime proofs | 138-01 |
| Static check module | `Mailglass.Credo.RawRepoPrefixContract` in `credo_checks/raw_repo_prefix_contract.ex` | 138-03 |
| Static check test module | `Mailglass.Credo.RawRepoPrefixContractTest` | 138-03 |
| Inbound contract test module | `MailglassInbound.SchemaPrefixContractTest` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Internal.Replay` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Execution` | 138-02 |
| Helper function | `schema_opts/0` in `Mix.Tasks.Mailglass.Inbound.Replay` | 138-02 |
| Mix alias | `mix verify.schema_prefix` | 138-04 |
| CI/documentation wording | Advisory matrix comments naming the dual-schema matrix as a broad canary and `mix verify.schema_prefix` as the focused proof | 138-04 |

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Runtime reads/writes touching mailglass tables target configured schema without search_path help | 138-01, 138-02, 138-03, 138-04 | COVERED | Hostile runtime tests, inbound raw-repo opts, static guard, and focused alias together satisfy the phase goal. |
| REQ | SCHEMA-01 | `Mailglass.Webhook.Replay` projection update hostile-path proof | 138-01 | COVERED | Task 1 creates proof and source fix. |
| REQ | SCHEMA-02 | Unsubscribe replay/idempotency conflict lookup hostile-path proof | 138-01 | COVERED | Task 2 creates proof and source fix. |
| REQ | SCHEMA-03 | Raw repo calls and callbacks are prefixed/facade-routed/allowlisted with recurrence guard | 138-03 | COVERED | This plan owns remaining projection updates and the static guard. |
| REQ | SCHEMA-04 | Inbound repo-option extension points default to facade and prove raw-repo prefix contract | 138-02 | COVERED | Raw repo tests and `schema_opts/0` changes. |
| REQ | GATE-01 | Focused schema-prefix verification lane | 138-04 | COVERED | `mix verify.schema_prefix` runs runtime and static proofs. |
| REQ | GATE-02 | Dual-schema advisory matrix remains a canary | 138-04 | COVERED | Workflow comments and source assertions name canary semantics. |
| RESEARCH | - | Include fake adapter and webhook reconciler projection updates under SCHEMA-03 | 138-03 | COVERED | Both projection Multi updates get explicit opts. |
| RESEARCH | - | Use custom Credo check instead of shell-only grep for raw repo recurrence guard | 138-03 | COVERED | Check follows existing custom Credo pattern. |
| RESEARCH | - | No new packages; no package install checkpoint required | all | COVERED | No install tasks in this phase. |
| RESEARCH | - | Schema push detection | all | COVERED | No ORM schema, migration, collection, entity, Prisma, Drizzle, Supabase, or TypeORM files are modified; no schema push task is injected. |
| CONTEXT | - | No phase CONTEXT.md exists | all | COVERED | Binding decisions come from ROADMAP, REQUIREMENTS, STATE, RESEARCH, VALIDATION, and PATTERNS. |
| STATE | v2.1 locked decision | Explicit Ecto prefix is correctness mechanism; search_path is only host/test convenience | 138-01, 138-02, 138-03 | COVERED | Static guard enforces explicit opts. |
| STATE | v2.1 locked decision | Surgical hostile lane and static guard instead of whole-suite no-search-path migration | 138-01, 138-03, 138-04 | COVERED | Guard is scoped to production call shapes. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prefix remaining production projection Multi updates</name>
  <files>lib/mailglass/adapters/fake.ex, lib/mailglass/webhook/reconciler.ex</files>
  <read_first>
    @lib/mailglass/adapters/fake.ex
    @lib/mailglass/webhook/reconciler.ex
    @lib/mailglass/outbound.ex
    @lib/mailglass/repo.ex
    @test/mailglass/adapters/fake_test.exs
    @test/mailglass/webhook/reconciler_test.exs
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
  </read_first>
  <behavior>
    - SCHEMA-03: Fake adapter `Ecto.Multi.update(:delivery, ...)` passes `Mailglass.Repo.multi_opts()` as operation opts.
    - SCHEMA-03: Both webhook reconciler `Multi.update(:projection, ...)` branches pass `Repo.multi_opts()` as operation opts.
  </behavior>
  <action>In `lib/mailglass/adapters/fake.ex`, convert the projection update inside `trigger_event/3` from the 3-argument `Ecto.Multi.update(:delivery, fn ...)` form to the 4-argument form with `Mailglass.Repo.multi_opts()` as the final argument. In `lib/mailglass/webhook/reconciler.ex`, update both compiled branches of `attempt_reconcile/1` so `Multi.update(:projection, fn _changes -> Projector.update_projections(delivery, orphan) end, Repo.multi_opts())` is used. Preserve append-only event semantics: do not update the orphan event row and do not move broadcasts before transaction completion.</action>
  <verify>
    <automated>mix test test/mailglass/adapters/fake_test.exs test/mailglass/webhook/reconciler_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mailglass/adapters/fake.ex` has no 3-argument projection `Ecto.Multi.update(:delivery, fn ...)` call.
    - Both projection updates in `lib/mailglass/webhook/reconciler.ex` pass `Repo.multi_opts()`.
    - Existing fake adapter and reconciler tests pass with warnings as errors.
  </acceptance_criteria>
  <done>Fake adapter and webhook reconciler projection writes are no longer search_path-dependent raw Multi operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add and register RawRepoPrefixContract Credo guard</name>
  <files>credo_checks/raw_repo_prefix_contract.ex, test/mailglass/credo/raw_repo_prefix_contract_test.exs, .credo.exs</files>
  <read_first>
    @credo_checks/no_unscoped_tenant_query_in_lib.ex
    @credo_checks/no_schema_prefix_attribute.ex
    @test/mailglass/credo/no_schema_prefix_attribute_test.exs
    @test/mailglass/credo/checks_have_tests_test.exs
    @.credo.exs
    @lib/mailglass/webhook/replay.ex
    @lib/mailglass/compliance/unsubscribe_controller.ex
    @mailglass_inbound/lib/mailglass_inbound/internal/replay.ex
    @mailglass_inbound/lib/mailglass_inbound/execution.ex
    @mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
  </read_first>
  <behavior>
    - SCHEMA-03 RED: The guard flags a lowercase raw callback repo call such as `repo.update(changeset)` when it touches a configured mailglass table and lacks prefix opts.
    - SCHEMA-03 GREEN: The guard allows `repo.update(changeset, Repo.multi_opts())`, inbound `repo.one(query, schema_opts())`, facade-routed `Mailglass.Repo` or `MailglassInbound.Repo` calls, and files outside configured production path prefixes.
  </behavior>
  <action>Create `credo_checks/raw_repo_prefix_contract.ex` as `Mailglass.Credo.RawRepoPrefixContract` using the custom-check skeleton and AST traversal style from existing Credo checks. Scope it to production path prefixes `lib/mailglass/` and `mailglass_inbound/lib/`. Detect raw lowercase repo calls for `one`, `one!`, `get`, `get!`, `all`, `update`, `insert`, and `delete_all`, plus `Ecto.Multi.update` / `Multi.update` projection operations, when the call shape touches mailglass schemas and lacks an explicit prefix contract. Treat these as compliant: final opts containing `prefix:`, `Repo.multi_opts()`, `Mailglass.Repo.multi_opts()`, or local `schema_opts()`; facade module calls through `Mailglass.Repo` or `MailglassInbound.Repo`; and configured schema-agnostic allowlist entries. Add `test/mailglass/credo/raw_repo_prefix_contract_test.exs` with tests for a bad raw callback update, a bad raw callback read, allowed `Repo.multi_opts()`, allowed inbound `schema_opts()`, allowed facade calls, and ignored files outside path scope. Register `{Mailglass.Credo.RawRepoPrefixContract, [...]}` in `.credo.exs` with schema modules for core (`Delivery`, `Event`, `Suppression.Entry`, `WebhookEvent`) and inbound (`InboundRecord`, `InboundEvidence`, `ExecutionRun`). Keep the guard focused on production files to avoid migrations/tests and broad fixture noise.</action>
  <verify>
    <automated>mix test test/mailglass/credo/raw_repo_prefix_contract_test.exs --warnings-as-errors</automated>
    <automated>mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `credo_checks/raw_repo_prefix_contract.ex` defines `Mailglass.Credo.RawRepoPrefixContract`.
    - `test/mailglass/credo/raw_repo_prefix_contract_test.exs` covers failing and allowed call shapes named in the behavior block.
    - `.credo.exs` registers the check, and `test/mailglass/credo/checks_have_tests_test.exs` remains satisfiable.
    - `mix credo --strict` runs the new guard and exits 0 after the source fixes from plans 138-01, 138-02, and Task 1.
  </acceptance_criteria>
  <done>SCHEMA-03 has a registered static recurrence guard with tests and a green strict Credo run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw repo callback -> mailglass table | Callback repos bypass the facade and must receive per-operation prefix opts. |
| Static guard -> developer workflow | The guard must block real recurrence without creating noisy false positives that developers bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-09 | Tampering | `Mailglass.Adapters.Fake.trigger_event/3` | mitigate | Add `Mailglass.Repo.multi_opts()` to the projection `Ecto.Multi.update/4` operation. |
| T-138-10 | Tampering | `Mailglass.Webhook.Reconciler.attempt_reconcile/1` | mitigate | Add `Repo.multi_opts()` to both projection `Multi.update/4` operations. |
| T-138-11 | Tampering | `Mailglass.Credo.RawRepoPrefixContract` | mitigate | AST-based guard flags unprefixed production raw repo/Multi call shapes and allows only explicit contracts. |
| T-138-12 | Denial of Service | Static guard false positives | mitigate | Scope to production path prefixes and tested schema-touching call shapes; allow schema-agnostic entries explicitly. |
| T-138-13 | Information Disclosure | Guard/test failure messages | mitigate | Messages name function/call shape and file line only; no PII-bearing payload inspection. |
</threat_model>

<verification>
Run `mix test test/mailglass/adapters/fake_test.exs test/mailglass/webhook/reconciler_test.exs --warnings-as-errors`, `mix test test/mailglass/credo/raw_repo_prefix_contract_test.exs --warnings-as-errors`, and `mix credo --strict`.
</verification>

<success_criteria>
SCHEMA-03 is satisfied: known raw projection gaps are fixed and recurrence is blocked by a registered, tested static guard.
</success_criteria>

<output>
Create `.planning/phases/138-schema-prefix-no-search-path-hardening/138-03-SUMMARY.md` when done.
</output>
