---
phase: 138-schema-prefix-no-search-path-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/schema_prefix_hardening_test.exs
  - lib/mailglass/webhook/replay.ex
  - lib/mailglass/compliance/unsubscribe_controller.ex
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "Webhook replay updates the configured-schema delivery projection when the active DB connection search_path excludes that schema."
    - "Unsubscribe replay/idempotency conflict lookup reads the existing configured-schema event when the active DB connection search_path excludes that schema."
    - "The hostile runtime proof checks configured-schema rows directly and proves matching public-schema rows were not touched."
  artifacts:
    - path: "test/mailglass/schema_prefix_hardening_test.exs"
      provides: "Focused hostile no-search-path runtime proofs for SCHEMA-01 and SCHEMA-02"
      contains: "Mailglass.SchemaPrefixHardeningTest"
    - path: "lib/mailglass/webhook/replay.ex"
      provides: "Explicit prefix opts for raw Multi callback projection update"
      contains: "Repo.multi_opts()"
    - path: "lib/mailglass/compliance/unsubscribe_controller.ex"
      provides: "Explicit prefix opts for raw Multi callback conflict lookup"
      contains: "repo.one!"
  key_links:
    - from: "lib/mailglass/webhook/replay.ex"
      to: "lib/mailglass/repo.ex"
      via: "raw callback repo.update uses Repo.multi_opts()"
      pattern: "repo\\.update\\(.*Repo\\.multi_opts\\(\\)"
    - from: "lib/mailglass/compliance/unsubscribe_controller.ex"
      to: "lib/mailglass/repo.ex"
      via: "raw callback repo.one! uses Repo.multi_opts()"
      pattern: "repo\\.one!\\(.*Repo\\.multi_opts\\(\\)"
---

<objective>
Fix and prove the two named runtime failures from Phase 138: `Mailglass.Webhook.Replay` projection updates and unsubscribe replay/idempotency conflict lookups must target `Mailglass.Config.schema()` even when the DB connection search_path excludes that schema.

Purpose: SCHEMA-01 and SCHEMA-02 are the load-bearing user-visible runtime proofs for this phase; without these hostile-path tests, the broad advisory matrix can stay false-green.
Output: Hostile no-search-path ExUnit coverage plus explicit `Repo.multi_opts()` use at the raw callback call sites.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-RESEARCH.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-VALIDATION.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
@lib/mailglass/repo.ex
@lib/mailglass/webhook/replay.ex
@lib/mailglass/compliance/unsubscribe_controller.ex
@test/mailglass/schema_isolation_immutability_test.exs
@test/mailglass/webhook/replay_test.exs
@test/mailglass/compliance/unsubscribe_controller_test.exs
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Produced in plan |
|------|---------------|------------------|
| Test module | `Mailglass.SchemaPrefixHardeningTest` in `test/mailglass/schema_prefix_hardening_test.exs` | 138-01 |
| Test helper module | `Mailglass.SchemaPrefixHardeningTest.PrefixedWrapperMigration` | 138-01 |
| Test tag | `:schema_prefix` module/test tag for focused runtime proofs | 138-01 |
| Static check module | `Mailglass.Credo.RawRepoPrefixContract` in `credo_checks/raw_repo_prefix_contract.ex` | 138-03 |
| Static check test module | `Mailglass.Credo.RawRepoPrefixContractTest` | 138-03 |
| Inbound contract test module | `MailglassInbound.SchemaPrefixContractTest` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Internal.Replay` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Execution` | 138-02 |
| Helper function | `schema_opts/0` in `Mix.Tasks.Mailglass.Inbound.Replay` | 138-02 |
| Mix alias | `mix verify.schema_prefix` | 138-04 |
| CI/documentation wording | Advisory matrix comments naming the dual-schema matrix as a broad canary and `mix verify.schema_prefix` as the focused proof | 138-04 |

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Runtime reads/writes touching mailglass tables target configured schema without search_path help | 138-01, 138-02, 138-03, 138-04 | COVERED | Hostile runtime tests, inbound raw-repo opts, static guard, and focused alias together satisfy the phase goal. |
| REQ | SCHEMA-01 | `Mailglass.Webhook.Replay` projection update hostile-path proof | 138-01 | COVERED | Task 1 creates proof and source fix. |
| REQ | SCHEMA-02 | Unsubscribe replay/idempotency conflict lookup hostile-path proof | 138-01 | COVERED | Task 2 creates proof and source fix. |
| REQ | SCHEMA-03 | Raw repo calls and callbacks are prefixed/facade-routed/allowlisted with recurrence guard | 138-03 | COVERED | Remaining projection updates plus custom Credo guard. |
| REQ | SCHEMA-04 | Inbound repo-option extension points default to facade and prove raw-repo prefix contract | 138-02 | COVERED | Raw repo tests and `schema_opts/0` changes. |
| REQ | GATE-01 | Focused schema-prefix verification lane | 138-04 | COVERED | `mix verify.schema_prefix` runs runtime and static proofs. |
| REQ | GATE-02 | Dual-schema advisory matrix remains a canary | 138-04 | COVERED | Workflow comments and source assertions name canary semantics. |
| RESEARCH | - | Include fake adapter and webhook reconciler projection updates under SCHEMA-03 | 138-03 | COVERED | Both projection Multi updates get explicit opts. |
| RESEARCH | - | Use explicit Ecto `prefix:` and do not rely on connection search_path | 138-01, 138-02, 138-03 | COVERED | Plans use `Repo.multi_opts()` and inbound `schema_opts/0`. |
| RESEARCH | - | No new packages; no package install checkpoint required | all | COVERED | No install tasks in this phase. |
| RESEARCH | - | Schema push detection | all | COVERED | No ORM schema, migration, collection, entity, Prisma, Drizzle, Supabase, or TypeORM files are modified; no schema push task is injected. |
| CONTEXT | - | No phase CONTEXT.md exists | all | COVERED | Binding decisions come from ROADMAP, REQUIREMENTS, STATE, RESEARCH, VALIDATION, and PATTERNS. |
| STATE | v2.1 locked decision | Explicit Ecto prefix is correctness mechanism; search_path is only host/test convenience | 138-01, 138-02, 138-03 | COVERED | Tasks reject search_path as the fix mechanism. |
| STATE | v2.1 locked decision | Surgical hostile lane and static guard instead of whole-suite no-search-path migration | 138-01, 138-03, 138-04 | COVERED | Focused proofs and guard only; fixture sweep remains out of phase scope. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and fix Webhook.Replay projection prefixing</name>
  <files>test/mailglass/schema_prefix_hardening_test.exs, lib/mailglass/webhook/replay.ex</files>
  <read_first>
    @test/mailglass/schema_isolation_immutability_test.exs
    @test/mailglass/webhook/replay_test.exs
    @lib/mailglass/webhook/replay.ex
    @lib/mailglass/webhook/ingest.ex
    @lib/mailglass/repo.ex
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
  </read_first>
  <behavior>
    - SCHEMA-01 RED: With `Application.put_env(:mailglass, :schema, "mailglass")`, `:persistent_term` reset, and the active TestRepo connection forced to `SET search_path TO public`, `Mailglass.Webhook.Replay.execute/1` must replay a stored Postmark delivery event and update the `mailglass.mailglass_deliveries` row.
    - The same proof must assert the matching row in `public.mailglass_deliveries` is absent or unchanged, so the test cannot pass by accidentally writing to `public`.
  </behavior>
  <action>Create `test/mailglass/schema_prefix_hardening_test.exs` with module `Mailglass.SchemaPrefixHardeningTest`, `use ExUnit.Case, async: false`, and `@moduletag :schema_prefix`. Reuse the DDL setup/teardown shape from `Mailglass.SchemaIsolationImmutabilityTest`: set `Mailglass.Config.schema/0` to `"mailglass"`, erase `{Mailglass.Config, :schema}`, put `Ecto.Adapters.SQL.Sandbox` in `:auto`, drop the `mailglass` schema, run an inline `PrefixedWrapperMigration` that calls `Mailglass.Migration.up(prefix: "mailglass", repo: Mailglass.TestRepo)`, and restore the prior schema plus Sandbox `:manual` in `on_exit`. For the replay proof, force the active connection to `SET search_path TO public` before calling `Mailglass.Webhook.Replay.execute/1`. Build the delivery and webhook_event rows in the configured schema using the `Mailglass.Repo` facade or fully qualified `mailglass.*` SQL, not unprefixed `TestRepo.insert`. Assert `Replay.execute/1` returns `{:ok, result}` with `status: :replayed`, `new_event_count: 1`, and the expected delivery id. Assert by fully qualified SQL that `mailglass.mailglass_deliveries` has the projected event state and that `public.mailglass_deliveries` does not contain the same id. After the test fails for the current raw callback update, update `lib/mailglass/webhook/replay.ex` inside `update_projections_for_each/3`: assign `Projector.update_projections(delivery, inserted_event)` to a changeset variable and call the raw callback repo as `repo.update(changeset, Repo.multi_opts())`, matching `lib/mailglass/webhook/ingest.ex`. Do not fix this by changing connection search_path or by moving the projection update outside the transaction.</action>
  <verify>
    <automated>mix test test/mailglass/schema_prefix_hardening_test.exs --only schema_prefix --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/mailglass/schema_prefix_hardening_test.exs` contains `Mailglass.SchemaPrefixHardeningTest`, `@moduletag :schema_prefix`, `PrefixedWrapperMigration`, and a replay test that runs after `SET search_path TO public`.
    - The replay test asserts configured-schema delivery projection state through fully qualified SQL or facade reads with `prefix: "mailglass"` and asserts the public schema is not the write target.
    - `lib/mailglass/webhook/replay.ex` uses `Repo.multi_opts()` on the raw `repo.update` call inside the `{:projector_apply, idx}` callback.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>SCHEMA-01 is proven by a hostile no-search-path runtime test, and the replay projection callback uses explicit prefix opts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove and fix unsubscribe conflict lookup prefixing</name>
  <files>test/mailglass/schema_prefix_hardening_test.exs, lib/mailglass/compliance/unsubscribe_controller.ex</files>
  <read_first>
    @test/mailglass/compliance/unsubscribe_controller_test.exs
    @lib/mailglass/compliance/unsubscribe_controller.ex
    @lib/mailglass/repo.ex
    @mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
    @.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
  </read_first>
  <behavior>
    - SCHEMA-02 RED: Under the same `"mailglass"` configured schema and `SET search_path TO public`, two POST unsubscribe requests for the same delivery token must return 200 and leave exactly one configured-schema `:unsubscribed` event.
    - The second POST must exercise `canonical_event/3` with the conflict sentinel path and read the existing configured-schema row.
  </behavior>
  <action>Extend `Mailglass.SchemaPrefixHardeningTest` with a focused unsubscribe replay/idempotency proof. Define the minimal Phoenix router/endpoint test modules inside the test file using the same route shape as `Mailglass.Compliance.UnsubscribeControllerTest`: `post "/mailglass/unsubscribe/:token", Mailglass.Compliance.UnsubscribeController, :unsubscribe`. In setup, configure `:mailglass, :compliance` with that endpoint, a valid host/scheme/mount_path, no redirect, and `Mailglass.Lifecycle.Noop`, then restore app env in `on_exit`. Create a delivery in the configured schema, sign it with `Mailglass.Compliance.Unsubscribe.sign_token/1`, force `SET search_path TO public`, then POST twice. Assert both responses are 200, the configured-schema event count for `type = 'unsubscribed'` and idempotency key `unsubscribe:<delivery.id>` is exactly 1, and the matching public-schema event count is 0. After the test fails for the current raw lookup, update `canonical_event/3` in `lib/mailglass/compliance/unsubscribe_controller.ex` so the conflict branch builds the query and calls `repo.one!(query, Repo.multi_opts())`. Keep the lookup inside the transaction callback so lifecycle composition and idempotency semantics stay unchanged.</action>
  <verify>
    <automated>mix test test/mailglass/schema_prefix_hardening_test.exs --only schema_prefix --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The unsubscribe hostile-path test triggers two POSTs for one token after the active connection search_path excludes `mailglass`.
    - The test proves one configured-schema event and zero public-schema events for the unsubscribe idempotency key.
    - `lib/mailglass/compliance/unsubscribe_controller.ex` calls `repo.one!` with `Repo.multi_opts()` in the conflict lookup branch.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>SCHEMA-02 is proven by a hostile no-search-path runtime test, and unsubscribe conflict lookup uses explicit prefix opts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB connection -> configured schema | Runtime code must not let ambient connection search_path choose mailglass table targets. |
| Test diagnostics -> developer output | New failure messages must not expose recipient, subject, headers, body, or raw payload data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering | `Mailglass.Webhook.Replay.update_projections_for_each/3` | mitigate | Pass `Repo.multi_opts()` to the raw callback `repo.update/2` and prove it with a hostile no-search-path replay test. |
| T-138-02 | Information Disclosure | `Mailglass.Compliance.UnsubscribeController.canonical_event/3` | mitigate | Pass `Repo.multi_opts()` to the raw callback `repo.one!/2` so conflict lookup cannot read a same-id row from the wrong schema. |
| T-138-03 | Tampering | Direct SQL in hostile tests | mitigate | Use constant schema name `"mailglass"` from the test fixture and/or `Mailglass.Config.schema/0`; never interpolate user-controlled identifiers. |
| T-138-04 | Information Disclosure | ExUnit failure diagnostics | mitigate | Assert IDs/counts/status atoms only; do not log or inspect recipient, subject, headers, body, or raw payload fields. |
</threat_model>

<verification>
Run `mix test test/mailglass/schema_prefix_hardening_test.exs --only schema_prefix --warnings-as-errors`.
</verification>

<success_criteria>
SCHEMA-01 and SCHEMA-02 pass under a connection search_path that excludes the configured schema, and both source files carry explicit raw-repo prefix opts.
</success_criteria>

<output>
Create `.planning/phases/138-schema-prefix-no-search-path-hardening/138-01-SUMMARY.md` when done.
</output>
