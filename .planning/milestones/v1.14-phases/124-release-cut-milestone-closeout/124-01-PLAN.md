---
phase: 124-release-cut-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/mix.exs
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass-publish-summary.json
  - .planning/publish/mailglass_admin-files.expected
  - .planning/publish/mailglass_admin-publish-summary.json
  - .planning/publish/mailglass_inbound-files.expected
  - .planning/publish/mailglass_inbound-publish-summary.json
  - .planning/milestones/v1.14-ROADMAP.md
  - .planning/milestones/v1.14-REQUIREMENTS.md
  - .planning/milestones/v1.14-MILESTONE-AUDIT.md
  - .planning/milestones/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/RETROSPECTIVE.md
  - .planning/STATE.md
autonomous: false
requirements:
  - REL-01
  - REL-02

must_haves:
  truths:
    - "All three Hex packages (mailglass 1.10.0, mailglass_admin 1.10.0, mailglass_inbound 1.5.2) are live on Hex (D-01, D-02)"
    - "mailglass_inbound carries the dep {mailglass, == 1.10.0} (D-02, D-07, PROJECT D-13)"
    - "mix deps.get resolves all three packages from Hex in a fresh Phoenix app (REL-01)"
    - "post-publish-smoke passes (or only the known #32 hackney false-positive is red) (D-13)"
    - "origin/main carries the full 126-commit v1.14 body rebased atop the lone dependabot #88 commit, pushed by fast-forward (never --force) (D-04, D-05, D-06)"
    - "Milestone v1.14 is audited (15/15 requirements status: passed), archived, and tagged v1.14 (D-12)"
  artifacts:
    - path: "mailglass_inbound/mix.exs"
      provides: "Inbound exact-pin re-pinned to the new core version; the fix(inbound): commit that makes RP fold a paired inbound 1.5.2 release into the linked PR"
      contains: "== 1.10.0"
    - path: ".planning/milestones/v1.14-ROADMAP.md"
      provides: "Archived milestone roadmap"
    - path: ".planning/milestones/v1.14-REQUIREMENTS.md"
      provides: "Archived milestone requirements"
    - path: ".planning/milestones/v1.14-MILESTONE-AUDIT.md"
      provides: "Per-requirement pass/fail matrix at true scope (7 phases 118-124, 15 requirements)"
      contains: "status: passed"
  key_links:
    - from: ".planning/publish/mailglass_admin-files.expected"
      to: "prepublish-summary job in publish-hex.yml"
      via: "mix mailglass.publish.check"
      pattern: "mix mailglass.publish.check"
    - from: "fix(inbound): re-pin commit on origin/main"
      to: "Release Please linked release PR"
      via: "RP re-score after body reaches origin"
      pattern: "== 1.10.0"
    - from: "rebased origin/main HEAD SHA"
      to: "gate-ci-green in publish-hex.yml"
      via: "a green ci.yml run must exist on that SHA"
      pattern: "ci.yml"
---

<objective>
Release cut and milestone closeout for mailglass v1.14 — Operator IA & Lived-Experience Redesign.

Purpose: Move the v1.14 redesign body (Phases 118–123, the 126-commit body already built and verified on local `main`) from `main` to Hex adopters as a linked-version MINOR release — mailglass 1.10.0 / mailglass_admin 1.10.0 (core+admin linked, currently 1.9.0) with mailglass_inbound dragged via a deliberate D-07 exact-pin re-pin to `{:mailglass, "== 1.10.0"}` shipping as a PATCH 1.5.2 (currently 1.5.1) — then audit and archive the v1.14 milestone.

This phase writes essentially NO product/source code. The ONLY source edit is the inbound mix.exs re-pin. It reconciles a divergent origin (rebase + fast-forward push), lands the fix(inbound): commit so RP folds a paired inbound release into the linked PR, verifies the regenerated Release Please PR, gates on a maintainer go/no-go, confirms Hex publish, runs smoke, audits 15 requirements, and archives. Plan it like the Phase 117 / Phase 108 release ceremonies (this is a near-verbatim retarget).

Output: All three packages live on Hex at target versions; inbound pinned to mailglass == 1.10.0; consumer + post-publish smoke green; milestone audit and git tag v1.14.

LIVE STATE (verified at plan time — use these facts):
- Current versions: mailglass 1.9.0 / mailglass_admin 1.9.0 / mailglass_inbound 1.5.1 (pinned {:mailglass, "== 1.9.0"}).
- Target versions: mailglass 1.10.0 / mailglass_admin 1.10.0 (MINOR, D-01) / mailglass_inbound 1.5.2 (PATCH, D-02), inbound re-pinned {:mailglass, "== 1.10.0"}.
- NO Release Please PR is open yet — it regenerates AFTER the v1.14 body reaches origin/main, and should self-score 1.10.0/1.10.0 from the now-visible admin feat commits (D-03). Release-As: 1.10.0 is a FALLBACK only, applied in Wave 1 if RP scores a patch despite the body being on origin.
- ORIGIN DIVERGENCE (the one structural delta vs 117): local `main` (HEAD d46e4310 at plan time) is 126 commits AHEAD of and 1 BEHIND origin/main (63c3f4c3). origin carries one commit local lacks: `63c3f4c3 chore(deps): bump actions/checkout from 6.0.3 to 7.0.0 (#88)`, which modified ci.yml itself. Reconcile by `git fetch origin && git rebase origin/main` then a plain fast-forward `git push origin main`. NEVER --force (D-04, D-05, D-06).
- THE inbound release will NOT auto-cut: the RP sed pin-sync lands as a non-releasable chore on the PR branch. A deliberate `fix(inbound): re-pin to == 1.10.0` commit must be on origin/main BEFORE the RP PR merges, so RP folds the inbound 1.5.2 bump into the SAME linked release PR (D-07). publish-admin needs publish-inbound — a missing inbound release blocks admin publish.
- Publish allowlist is NOT stale this cycle (D-08): comm diffs show zero deltas. Still run `mix mailglass.publish.check --package <pkg>` as the fail-closed Wave-0 confirmation, but expect a clean no-op.
- Reference baseline pin-bump is OUT OF SCOPE; baselines pin ~> 1.0 which resolves any 1.x bump. Do not touch the 5-file baseline-coupling set (D-10).
- Milestone audit scope is 7 phases (118–124) and 15 requirements: METHOD-01/02, STORY-01/02, SHELL-01/02/03, DELIV-01, INB-01, PREV-01, COH-01/02, SEED-003, REL-01/02 (REL has only TWO IDs this milestone). Do NOT trust gsd-sdk milestone.complete — it sweeps 999.x backlog dirs and inflates the matrix. Hand-count to true scope (D-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/CLAUDE.md
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/124-release-cut-milestone-closeout/124-CONTEXT.md

Canonical references (READ before acting — CONTEXT.md mandates these):
- .planning/milestones/v1.13-phases/117-release-cut-milestone-closeout/117-01-PLAN.md (the closest precedent — the 6-wave ceremony this plan retargets from 1.8.0 to 1.10.0)
- .planning/milestones/v1.12-phases/108-release-cut-milestone-closeout/108-01-PLAN.md (the canonical 6-wave template)
- MAINTAINING.md (Release Runbook 5 steps, Retract Decision Tree, required vs. advisory lanes, Publish Summary Snapshot Protocol, Release-As: fallback ~line 296)
- .github/workflows/release-please.yml (sed dep-pin sync step / PINS array ~lines 139–263, auto-merge arming, cron self-heal)
- .github/workflows/publish-hex.yml (gate-ci-green SHA-bound green-CI requirement, publish ordering core → inbound → admin, publish-admin needs [publish-core, publish-inbound], idempotency guards, fallback dispatch, prepublish-summary)
- .github/workflows/post-publish-smoke.yml (smoke job chain, inbound compatibility check)
- scripts/consumer_install_smoke.sh (DEP_MODE=hex consumer smoke — REL-01 Hex-resolution proof)
- mailglass_inbound/mix.exs (~lines 114–136 — exact-pin + fix(inbound): re-pin idiom + recovery note)
</context>

<artifacts_produced>
## Artifacts This Phase Produces

This is a release-ceremony + closeout phase. No new source symbols, components, or product docs. Artifacts:

- **The rebased + pushed origin/main HEAD**: the full 126-commit v1.14 body replayed atop the lone dependabot #88 commit, fast-forward pushed (never --force). This rebased HEAD SHA is what RP tags and what gate-ci-green requires a green ci.yml run on (D-04, D-05).
- **The `fix(inbound): re-pin to == 1.10.0` commit** on origin/main (the deliberate trigger that makes RP fold a paired inbound 1.5.2 release into the linked PR — D-07).
- **(If needed) regenerated publish allowlist snapshots** (committed as `chore:`): the six `.planning/publish/*` files. Expected a clean no-op this cycle (D-08) — only commit if `mix mailglass.publish.check` actually rewrites them.
- **Published Hex versions**: mailglass 1.10.0, mailglass_admin 1.10.0, mailglass_inbound 1.5.2 (paired via the D-07 fix(inbound): drag).
- **Archive docs**: `.planning/milestones/v1.14-ROADMAP.md`, `.planning/milestones/v1.14-REQUIREMENTS.md`, `.planning/milestones/v1.14-MILESTONE-AUDIT.md`, and appended `.planning/RETROSPECTIVE.md`.
- **Updated planning docs**: `MILESTONES.md`, `PROJECT.md`, `ROADMAP.md`, `STATE.md`.
- **The `v1.14` git tag** (a milestone marker, distinct from the RP-created `*-v1.10.0` Hex release tags).
</artifacts_produced>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 0 — Pre-flight hygiene (reversible) — D-05, D-08
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1 (Wave 0): Confirm green CI on local main + fail-closed publish.check no-op (REL-01, D-05, D-08)</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-files.expected, .planning/publish/mailglass_admin-publish-summary.json, .planning/publish/mailglass_inbound-files.expected, .planning/publish/mailglass_inbound-publish-summary.json</files>

  <read_first>
    MAINTAINING.md — "Publish Summary Snapshot Protocol" section (mix mailglass.publish.check,
    regeneration steps, commit message guidance) + required-vs-advisory lane list.

    CRITICAL (CLAUDE.md "Things Not To Do" + CONTEXT D-08): Do NOT run
    `mix compile --no-optional-deps --force` on the shared _build during this step — it pollutes
    build artifacts and the /inbound route is compile-time-gated and order-sensitive. Use only
    `mix mailglass.publish.check --package <pkg>`. Per the demo_app swoosh-lock-drift gotcha, if any
    mix invocation re-bumps a reference mix.lock, `git checkout` that lock before committing — never
    commit transitive drift here.

    Note (D-09): phoenix_storybook lives ONLY in reference/demo_app/mix.exs as {:phoenix_storybook,
    "~> 1.2", only: :dev}; it is absent from mailglass_admin/mix.exs and the demo_app is never
    published — it is structurally incapable of appearing in any Hex tarball. No allowlist action
    is needed for it.
  </read_first>

  <action>
    Step 0.1 — CI green confirmation on local main (REL-01 evidence):
    Run `gh run list --workflow ci.yml --branch main --limit 8` from repo root. Locate the last
    non-docs commit run (the last commit touching lib/, guides/, mailglass_admin/lib/, or
    mailglass_inbound/lib/ — docs(state): commits skip ci.yml entirely via path filters). Confirm
    all required lanes are green: "Support Contract Core (Elixir 1.18 / OTP 27)", "Support Contract
    Admin (Elixir 1.18 / OTP 27)", "Compile No Optional Deps (Elixir 1.18 / OTP 27)", "Trust Lane
    Repo Head (Elixir 1.18 / OTP 27)", "Installer Host Smoke". Note: origin/main is frozen at the
    dependabot HEAD so the freshest origin runs predate the v1.14 body — the authoritative green
    proof on the released ancestry comes from the post-rebase ci.yml run in Task 2 (D-05) and the
    PR checks in Task 4.

    Step 0.2 — Clean worktree check:
    Run `git status`. The allowlist snapshot captures untracked files, so the working tree must be
    clean before running publish.check. The user may carry dirty .planning/ WIP — if so, note it but
    do not regenerate against a dirty source tree (source must be clean).

    Step 0.3 — Fail-closed publish.check (expected clean no-op this cycle, D-08):
    From repo root run each in turn:
      `mix mailglass.publish.check --package mailglass`
      `mix mailglass.publish.check --package mailglass_admin`
      `mix mailglass.publish.check --package mailglass_inbound`
    Each MUST exit 0. CONTEXT D-08 states the allowlist is NOT stale this cycle (comm diffs show zero
    deltas across all three *-files.expected snapshots, zero untracked files under admin lib/priv/docs),
    so expect NO diff to the .planning/publish/* files. Run `git status .planning/publish/` to confirm.

    Step 0.4 — Commit ONLY if a snapshot actually changed:
    If (contrary to D-08) any .planning/publish/* file was rewritten, review the diff
    (`git diff .planning/publish/`), confirm no reference mix.lock drift snuck in (`git checkout` any
    drifted lock first), stage ONLY the changed .planning/publish/* files, and commit:
    `chore: refresh publish allowlist snapshots for v1.10.0 release`
    (chore: does NOT trigger a Release Please version bump — intentional.) If nothing changed (the
    expected D-08 path), make NO commit and proceed to Task 2.
  </action>

  <verify>
    <automated>mix mailglass.publish.check --package mailglass && mix mailglass.publish.check --package mailglass_admin && mix mailglass.publish.check --package mailglass_inbound</automated>
  </verify>

  <acceptance_criteria>
    `gh run list --workflow ci.yml --branch main --limit 8` shows a completed run where required
    lanes ("Support Contract Core", "Support Contract Admin", "Compile No Optional Deps", "Trust
    Lane Repo Head", "Installer Host Smoke") are all green on the last non-docs SHA.

    `mix mailglass.publish.check --package mailglass` exits 0.
    `mix mailglass.publish.check --package mailglass_admin` exits 0.
    `mix mailglass.publish.check --package mailglass_inbound` exits 0.

    `git status --porcelain .planning/publish/` is empty (the expected D-08 clean no-op), OR — if a
    snapshot legitimately changed — `git show --stat HEAD` shows only .planning/publish/ files with a
    "chore: refresh publish allowlist snapshots" message and NO reference mix.lock files staged.
  </acceptance_criteria>

  <done>
    Green CI confirmed on local main. publish.check exits 0 for all three packages with a clean no-op
    (D-08); no allowlist commit was needed (or, if a snapshot legitimately drifted, a scoped chore:
    commit was made). Source tree clean and ready for the rebase reconciliation in Task 2.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 1 — Reconcile origin (rebase + ff push), land fix(inbound):,
              regenerate + verify the RP PR (reversible) — D-03..D-07
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 2 (Wave 1): Rebase onto origin/main, land fix(inbound): re-pin == 1.10.0, fast-forward push, confirm green ci.yml on the new HEAD (REL-01, D-04/D-05/D-06/D-07)</name>
  <files>mailglass_inbound/mix.exs</files>

  <read_first>
    CONTEXT D-04/D-05/D-06 (origin-divergence reconciliation) and D-07 (inbound paired release).
    mailglass_inbound/mix.exs ~lines 114–136 — the {:mailglass, "== <core>"} exact-pin line and the
    fix(inbound): re-pin idiom / recovery note.
    .github/workflows/release-please.yml — the sed/PINS dep-pin sync step (it rewrites admin AND
    inbound core pins on the PR branch automatically) + auto-merge arming.
    CLAUDE.md — "Reconcile planner branch to main" + the never-`--force` rule. Confirm a direct
    `git push origin main` (fast-forward) is permitted by branch protection before relying on it.

    HARD RULE: NEVER `git push --force` (CONTEXT D-04/D-06). If a non-fast-forward rejection appears
    after rebasing, origin advanced again — re-fetch and re-rebase, never force. Stale .planning/ SHA
    references after the rebase are cosmetic — do not "fix" them by force-pushing old SHAs.
  </read_first>

  <action>
    Step 1.1 — Re-fetch and rebase as a unit, close to push time (D-04, D-06):
    `git fetch origin && git rebase origin/main`
    Local main carries the 126-commit v1.14 body (zero merge commits); origin/main carries the lone
    dependabot commit `63c3f4c3 chore(deps): bump actions/checkout from 6.0.3 to 7.0.0 (#88)` which
    modified ci.yml itself. The rebase replays the 126 body commits atop #88 → clean linear history
    (the shape RP and the runbook assume). Expect zero conflicts (the body does not touch
    .github/workflows/ci.yml's checkout pin). If conflicts appear, resolve preserving BOTH the
    dependabot ci.yml change and the body changes, then `git rebase --continue`.

    Step 1.2 — Land the deliberate fix(inbound): re-pin (D-07 — the inbound release will NOT auto-cut):
    Edit `mailglass_inbound/mix.exs`: change the core dep pin from `{:mailglass, "== 1.9.0"}` to
    `{:mailglass, "== 1.10.0"}`. (Leave the inbound `@version` at 1.5.1 — Release Please computes the
    1.5.2 bump from the fix(inbound): commit; do NOT hand-bump @version.) Commit:
    `git add mailglass_inbound/mix.exs`
    `git commit -m "fix(inbound): re-pin to mailglass == 1.10.0"`
    Rationale to encode in the body (Claude's discretion on exact wording, CONTEXT): a fix(inbound):
    commit is the documented idiom that RP auto-scores to a PATCH (1.5.1 → 1.5.2) and folds into the
    SAME linked release PR, so publish-admin's `needs: [publish-core, publish-inbound]` is satisfied.
    Land it on this rebased main BEFORE the push so it reaches origin BEFORE the RP PR merges (D-07).

    Step 1.3 — Fast-forward push (NEVER --force, D-04):
    `git push origin main`
    Because the replayed commits descend from current origin HEAD (#88), this is an ordinary
    fast-forward — no force needed. If a non-fast-forward rejection appears, origin advanced again
    (another dependabot merge): re-run Step 1.1 (`git fetch origin && git rebase origin/main`),
    re-confirm the fix(inbound): commit is still present (`git log --oneline -5`), and re-push. NEVER
    force.

    Step 1.4 — Confirm ci.yml runs and goes GREEN on the new origin/main HEAD (D-05 — load-bearing):
    The push is human-authored (admin direct push), so it DOES trigger ci.yml (the "no ci.yml runs
    found for SHA" anti-recursion gap bites only the later bot-auto-merged release SHA, covered by
    gate-ci-green's self-heal — not this push). Watch it:
    `gh run list --workflow ci.yml --branch main --limit 3`
    Wait for the run on the new HEAD to complete and confirm the required lanes (Support Contract
    Core, Support Contract Admin, Compile No Optional Deps, Trust Lane Repo Head, Installer Host
    Smoke) are GREEN. The rebased HEAD SHA is what RP will tag and what gate-ci-green requires a
    green ci.yml run on — this MUST be green before authorizing the RP-PR merge in Wave 2.
  </action>

  <verify>
    <automated>test "$(git rev-parse origin/main)" = "$(git rev-parse HEAD)" && grep -q '== 1.10.0' mailglass_inbound/mix.exs && git log --oneline -8 | grep -q 'fix(inbound): re-pin to mailglass == 1.10.0'</automated>
  </verify>

  <acceptance_criteria>
    `git rev-parse origin/main` equals `git rev-parse HEAD` (the full v1.14 body + fix(inbound):
    re-pin reached origin via fast-forward).

    `grep '{:mailglass, "== 1.10.0"}' mailglass_inbound/mix.exs` returns a match (the inbound exact-pin
    is re-pinned to the new core version).

    `git log --oneline -8` contains a commit `fix(inbound): re-pin to mailglass == 1.10.0`, and that
    commit is on origin/main (`git log origin/main --oneline -8` shows it).

    `git reflog` / `git log` show NO force-push was used (the push was a fast-forward; origin HEAD is a
    descendant of #88 `63c3f4c3`).

    `gh run list --workflow ci.yml --branch main --limit 3` shows a completed run on the new origin HEAD
    with required lanes green.
  </acceptance_criteria>

  <done>
    Local main rebased onto origin/main (#88), the fix(inbound): re-pin == 1.10.0 commit landed,
    fast-forward pushed (no force). origin/main == local HEAD. ci.yml is green on the new HEAD — the
    SHA RP will tag is gate-ci-green-eligible. The push has triggered Release Please to regenerate the
    linked release PR (verified next task).
  </done>
</task>


<task type="auto">
  <name>Task 3 (Wave 1): Verify the regenerated Release Please PR — 1.10.0/1.10.0 + paired inbound 1.5.2 with == 1.10.0 pin, CHANGELOGs, required CI, auto-merge armed (REL-01, D-03/D-07/D-11)</name>
  <files></files>

  <read_first>
    .github/workflows/release-please.yml — sed/PINS sync step that rewrites mailglass_admin/mix.exs
    and mailglass_inbound/mix.exs dep pins on the PR branch; auto-merge arming; cron self-heal.
    MAINTAINING.md — Step 2 "Review the release PR diff before merge" (load-bearing dep-pin sync) +
    the Release-As: fallback (~line 296).
    CONTEXT D-03 (push-and-re-score, NOT Release-As; Release-As: 1.10.0 fallback only if RP scores a
    patch) and D-11 Wave 1 criteria.
  </read_first>

  <action>
    Step 1.5 — Wait for Release Please to regenerate the linked PR (D-03):
    The Task-2 push fired release-please.yml. Wait for it:
    `gh run list --workflow release-please.yml --limit 3` (watch the latest run to success). Then
    locate the regenerated "chore: release main" PR:
    `gh pr list --search "chore: release main" --state open --json number,title,headRefName`
    Record its PR number (referred to below as PR_N). If RP did not open/update the PR within ~10 min
    of the push completing, dispatch it: `gh workflow run release-please.yml` (no inputs) and re-check.

    Step 1.6 — Verify manifest + @version bumps are 1.10.0 (D-01, D-03):
    `gh pr diff PR_N` — confirm the regenerated PR shows:
    - `.release-please-manifest.json`: `"."` (core) = "1.10.0", `"mailglass_admin"` = "1.10.0",
      `"mailglass_inbound"` = "1.5.2".
    - `mix.exs` has `@version "1.10.0"` (core)
    - `mailglass_admin/mix.exs` has `@version "1.10.0"`
    - `mailglass_inbound/mix.exs` has `@version "1.5.2"`
    FALLBACK (D-03): if core/admin scored a PATCH (e.g. 1.9.1) despite the body being on origin,
    apply `Release-As: 1.10.0` per MAINTAINING.md ~line 296 (land an empty commit carrying the
    `Release-As: 1.10.0` footer on origin/main, then re-dispatch release-please.yml). Do NOT reach
    for Release-As unless the natural re-score is demonstrably a patch — D-03 makes it a fallback only.

    Step 1.7 — Verify the inbound paired release is present in the SAME PR (D-07 — BLOCKING):
    Confirm PR_N includes the inbound 1.5.2 version bump AND CHANGELOG entry (driven by the Task-2
    fix(inbound): commit). If inbound is ABSENT from the regenerated PR, the fix(inbound): commit did
    not reach origin before RP ran — STOP, confirm the commit is on origin/main (`git log origin/main
    --oneline | grep 'fix(inbound)'`), and re-dispatch release-please.yml. Do NOT merge an
    inbound-less RP PR — publish-admin `needs: [publish-core, publish-inbound]` would block the admin
    publish.

    Step 1.8 — Verify the inbound + admin dep pins (D-07, PROJECT D-13):
    `gh pr diff PR_N -- mailglass_inbound/mix.exs | grep '== 1.10.0'` — must return a `+` line
    containing `{:mailglass, "== 1.10.0"}` (already landed by Task 2; the RP sed step keeps it synced).
    `gh pr diff PR_N -- mailglass_admin/mix.exs | grep '== 1.10.0'` — must return a `+` line containing
    `{:mailglass, "== 1.10.0"}` (RP sed step).

    Step 1.9 — Review CHANGELOGs (REL-01):
    `gh pr diff PR_N -- CHANGELOG.md` (core), `... -- mailglass_admin/CHANGELOG.md`,
    `... -- mailglass_inbound/CHANGELOG.md`. Confirm a `## [1.10.0]` heading for core+admin and a
    `## [1.5.2]` heading for inbound. Light hand-curation is at Claude's discretion (CONTEXT) —
    RP-generated entries are sufficient for a routine ceremony.

    Step 1.10 — Check required CI + auto-merge (D-11):
    `gh pr checks PR_N` — all REQUIRED lanes must be reporting and green (advisory lanes — Operator
    Browser Gate, Preview Capture / Demo Browser Evidence Advisory — may be yellow/red without
    blocking; D-13). The checks must be REPORTING (not "no checks"). If any required lane is red,
    investigate before the Wave-2 checkpoint.
    `gh pr view PR_N --json autoMergeRequest` — the field must be non-null (RP arms it). If null:
    `gh pr merge PR_N --auto --squash`.

    Do NOT approve or merge yet. Task 4 is the irreversible maintainer go/no-go checkpoint.
  </action>

  <verify>
    <automated>PR_N=$(gh pr list --search "chore: release main" --state open --json number -q '.[0].number') && gh pr diff "$PR_N" -- mailglass_inbound/mix.exs | grep -q '== 1.10.0' && gh pr diff "$PR_N" | grep -q '1.10.0' && gh pr diff "$PR_N" | grep -q '1.5.2'</automated>
  </verify>

  <acceptance_criteria>
    A single open "chore: release main" PR exists (PR_N).

    `gh pr diff PR_N` shows manifest "1.10.0" for `.` (core) and "mailglass_admin", and "1.5.2" for
    "mailglass_inbound"; @version "1.10.0" in mix.exs and mailglass_admin/mix.exs; @version "1.5.2"
    in mailglass_inbound/mix.exs. (If a Release-As: 1.10.0 fallback was needed, the diff still ends at
    these values.)

    `gh pr diff PR_N -- mailglass_inbound/mix.exs | grep '== 1.10.0'` returns a `+` line containing
    `{:mailglass, "== 1.10.0"}`.
    `gh pr diff PR_N -- mailglass_admin/mix.exs | grep '== 1.10.0'` returns a `+` line containing
    `{:mailglass, "== 1.10.0"}`.

    `gh pr diff PR_N -- CHANGELOG.md | grep '## \[1.10.0\]'` returns a match; the admin CHANGELOG diff
    has `## [1.10.0]`; the inbound CHANGELOG diff has `## [1.5.2]`.

    `gh pr checks PR_N` shows all required lanes green (or orange while waiting; not red on required).
    `gh pr view PR_N --json autoMergeRequest` returns a non-null autoMergeRequest.
  </acceptance_criteria>

  <done>
    The regenerated linked release PR is verified: core+admin 1.10.0, paired inbound 1.5.2 in the SAME
    PR, inbound re-pin == 1.10.0, admin pin == 1.10.0, CHANGELOG entries for all three packages,
    required CI green, auto-merge armed. (Release-As: 1.10.0 fallback applied only if RP scored a patch.)
    Ready for the maintainer go/no-go.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 2 — Merge + publish (IRREVERSIBLE — maintainer go/no-go) — D-11
     ═══════════════════════════════════════════════════════════════════ -->

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (Wave 2): MAINTAINER GO/NO-GO — authorize merge of the linked release PR (IRREVERSIBLE after Hex publish)</name>
  <files></files>

  <action>
    HARD HUMAN GATE — no autonomous action. The executor PAUSES here and presents the go/no-go to the
    maintainer (see what-built / how-to-verify / resume-signal below). The executor does not approve,
    merge, or dispatch any publish on its own. After the maintainer types "approved" and the publish
    fan-out is observed starting in Actions, proceed to Task 5 (Wave 3). If "blocked: [reason]", halt
    the ceremony and report. This is the single irreversible authorization point (D-11 Wave 2).
  </action>

  <what-built>
    Wave 0 confirmed green CI on local main and ran publish.check fail-closed (clean no-op, D-08).
    Wave 1 reconciled the divergent origin by rebasing the 126-commit v1.14 body atop the lone
    dependabot #88 commit, landed the deliberate fix(inbound): re-pin == 1.10.0 (D-07), fast-forward
    pushed (NO force, D-04), and confirmed ci.yml is green on the new origin HEAD (D-05). That push
    re-scored the regenerated Release Please PR to mailglass 1.10.0 / mailglass_admin 1.10.0 /
    mailglass_inbound 1.5.2 (D-03), with the inbound re-pin {:mailglass, "== 1.10.0"} and admin pin
    both verified, and CHANGELOG entries for all three packages. Required CI is green and auto-merge
    is armed. The pipeline is fully hands-free once this checkpoint passes.

    IRREVERSIBILITY WARNING: After mailglass 1.10.0 publishes to Hex, Hex.pm allows revert only within
    60 minutes AND only if zero downloads have occurred. After that window, only `mix hex.retire`
    (deprecation) or a patch release remain. This is the one human go/no-go gate in the ceremony.
  </what-built>

  <how-to-verify>
    1. Visit the open "chore: release main" PR (PR_N) on https://github.com/szTheory/mailglass/pulls.
    2. Confirm it is the REGENERATED PR (versions 1.10.0/1.10.0/1.5.2, not a stale snapshot).
    3. Confirm the Files Changed tab shows:
       - mailglass @version "1.10.0", mailglass_admin @version "1.10.0", mailglass_inbound @version "1.5.2"
       - mailglass_inbound/mix.exs dep: `{:mailglass, "== 1.10.0"}` (in the + lines)
       - mailglass_admin/mix.exs dep: `{:mailglass, "== 1.10.0"}`
       - CHANGELOG entries under ## [1.10.0] (core+admin) / ## [1.5.2] (inbound) look reasonable for
         the v1.14 operator IA & lived-experience redesign
    4. Confirm required CI checks are green on the PR.
    5. If satisfied: APPROVE — auto-merge will squash-merge the PR; the hourly cron or a manual
       `gh workflow run release-please.yml` will tag the commit and fire the publish fan-out.

    After approving, monitor the Actions tab for:
    - release-please.yml completing (tag creation, GitHub Release)
    - publish-hex.yml fan-out triggered by `release: published`
    - prepublish-summary → gate-ci-green → publish-core → publish-inbound → publish-admin
      (publish-admin needs BOTH publish-core AND publish-inbound — D-07)

    STALL RECOVERY (documented, NOT the default path — from MAINTAINING.md):
    - Anti-recursion stall (PR merged but no tag): `gh workflow run release-please.yml` (no inputs).
      A bot-merged release SHA gets no ci.yml run, so gate-ci-green can block with "no ci.yml runs
      found for SHA" — gate-ci-green now self-heals (dispatches ci.yml on the tag, waits ~30 min).
    - publish fan-out failed after tag: re-dispatch from the RELEASE TAG, NEVER from main:
      `gh workflow run publish-hex.yml --ref mailglass-v1.10.0 --field tag=mailglass-v1.10.0 --field dry_run=false --field package=all`
  </how-to-verify>

  <resume-signal>
    Type "approved" after authorizing the merge and observing that the publish fan-out has started
    (publish-hex.yml run visible in Actions), or type "blocked: [reason]" to halt and describe what
    you found in the PR diff.
  </resume-signal>

  <verify>
    <human-check>Maintainer reviewed the linked release PR (versions 1.10.0/1.10.0/1.5.2, inbound re-pin == 1.10.0, CHANGELOGs, required CI green) and typed "approved"; publish-hex.yml fan-out is visible in the Actions tab.</human-check>
  </verify>

  <done>
    Maintainer authorized the merge of the linked release PR; publish-hex.yml fan-out has started
    (visible in Actions), or the ceremony was halted with a documented "blocked: [reason]".
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 3 — Confirm Hex live (read-only) — REL-01, D-11
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 5 (Wave 3): Confirm all three packages live on Hex at target versions + correct inbound pin (REL-01, D-07/D-11)</name>
  <files></files>

  <read_first>
    .github/workflows/publish-hex.yml — publish job ordering: publish-core → publish-inbound →
    publish-admin; publish-admin needs [publish-core, publish-inbound]; idempotency guards
    (mix hex.info check before publish); advisory lane list (do not block on Operator Browser Gate /
    Demo Browser Evidence Advisory).
    MAINTAINING.md — Step 3 "Monitor the hands-free publish fan-out".
  </read_first>

  <action>
    Step 3.1 — Watch the publish fan-out in Actions:
    `gh run list --workflow publish-hex.yml --limit 3` to find the run triggered by `release:
    published`. Monitor jobs: prepublish-summary → gate-ci-green → publish-core → publish-inbound →
    publish-admin. All must succeed (or skip via idempotency — already-published is success). Advisory
    jobs may fail without blocking. NOTE: publish-admin will not start until BOTH publish-core and
    publish-inbound succeed (D-07) — a missing inbound release would stall admin here.

    Step 3.2 — Verify core on Hex:
    `mix hex.info mailglass 1.10.0`. Wait for Hex indexing if needed (up to ~5 min; publish-core has a
    built-in index wait). Output must contain "Released:".

    Step 3.3 — Verify admin on Hex:
    `mix hex.info mailglass_admin 1.10.0`. Output must contain "Released:".

    Step 3.4 — Verify inbound on Hex with the correct pin (REL-01, D-07/PROJECT D-13):
    `mix hex.info mailglass_inbound 1.5.2`. Output must contain "Released:" AND must show the
    dependency `mailglass == 1.10.0`. Confirm:
    `mix hex.info mailglass_inbound 1.5.2 | grep 'mailglass'` returns `mailglass == 1.10.0`.

    Step 3.5 — HexDocs spot check (may lag Hex by 5–10 min):
    - https://hexdocs.pm/mailglass/1.10.0/
    - https://hexdocs.pm/mailglass_admin/1.10.0/
    - https://hexdocs.pm/mailglass_inbound/1.5.2/
    Each URL must return HTTP 200 (not 404 / redirect to latest).

    NOTE: the 60-minute revert window starts when publish-core completes (Step 3.2 green). Set a
    timer — Task 6 (consumer smoke) must complete within this window.
  </action>

  <verify>
    <automated>mix hex.info mailglass 1.10.0 | grep -q 'Released:' && mix hex.info mailglass_admin 1.10.0 | grep -q 'Released:' && mix hex.info mailglass_inbound 1.5.2 | grep -q 'Released:'</automated>
  </verify>

  <acceptance_criteria>
    `mix hex.info mailglass 1.10.0` output contains "Released:".
    `mix hex.info mailglass_admin 1.10.0` output contains "Released:".
    `mix hex.info mailglass_inbound 1.5.2` output contains "Released:" AND
    `mix hex.info mailglass_inbound 1.5.2 | grep 'mailglass'` returns `mailglass == 1.10.0`.
    All three publish-hex.yml jobs (publish-core, publish-inbound, publish-admin) show
    `completed: success` or `completed: skipped` (idempotent re-run) in `gh run view`.
  </acceptance_criteria>

  <done>
    mailglass 1.10.0, mailglass_admin 1.10.0, mailglass_inbound 1.5.2 confirmed live on Hex. Inbound
    dependency shows mailglass == 1.10.0. 60-minute revert window timer started.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 4 — Resolution + smoke (within 60-minute window) — REL-01, D-13
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 6 (Wave 4): Consumer smoke (DEP_MODE=hex) + post-publish-smoke green within the 60-minute window (REL-01, D-13)</name>
  <files></files>

  <read_first>
    scripts/consumer_install_smoke.sh — DEP_MODE=hex path; OPS-01 guard (no hackney/finch in
    mix.lock); INCLUDE_INBOUND flag; inbound version var.
    .github/workflows/post-publish-smoke.yml — smoke job chain; inbound compatibility check (grep for
    mailglass == 1.10.0, sets include_inbound=true); known #32 hackney false-positive (distinguish
    from a real regression).
    MAINTAINING.md — Step 4 (60-minute window manual smoke) + the Retract Decision Tree.
    CONTEXT D-13 — issue #32 swoosh/hackney OPS-01 false-positive and advisory lanes going red are
    NON-BLOCKERS; any OTHER red is a real regression.
  </read_first>

  <action>
    Step 4.1 — Run consumer smoke within the 60-minute window (REL-01 Hex-resolution proof):
    From repo root:
    `VERSION=1.10.0 VERSION_INBOUND=1.5.2 DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh`
    The smoke creates a fresh phx.new sandbox, injects == 1.10.0 deps, runs mix deps.get, mix
    mailglass.install, mix compile --warnings-as-errors, boots, and hits GET /dev/mail/ → 200.
    Exit 0 = pass.

    Step 4.2 — Verify mix deps.get Hex resolution (REL-01):
    Confirm the smoke's `mix deps.get` step resolved mailglass from Hex at 1.10.0 and mailglass_inbound
    from Hex at 1.5.2 (visible in script output). This is the REL-01 Hex-resolution proof.

    Step 4.3 — Monitor post-publish-smoke (REL-01):
    `gh run list --workflow post-publish-smoke.yml --limit 3` to find the run triggered by `release:
    published`. Monitor: cron-guard → wait-for-index → wait-for-hexdocs → consumer-install →
    published-trust-journey → retracted-check. If no run fired within ~10 min of publish, dispatch:
    `gh workflow run post-publish-smoke.yml --field tag=mailglass-v1.10.0`
    All jobs must pass.

    Step 4.4 — D-13 known-noise classification (NON-BLOCKER):
    The issue #32 swoosh/hackney OPS-01 false-positive in `consumer-install` (output containing
    "hackney" / "missing hackney dependency") is an ACCEPTABLE non-blocker, as are advisory lanes
    going red. If that is the only red, document that it matches #32 and proceed. ANY OTHER red is a
    real regression — evaluate it against the MAINTAINING.md Retract Decision Tree. Do not accept any
    non-#32 failure as noise.

    Step 4.5 — 60-minute decision (Retract Decision Tree):
    If both consumer smoke (4.1) and post-publish-smoke (4.3) pass (or only #32 noise is red), proceed
    to Task 7. If a real (non-#32) regression is found within 60 minutes of publish:
    - Zero downloads: `mix hex.publish --revert <version>` for affected packages.
    - Non-zero downloads or past 60 min: evaluate the Retract rules and open a patch release PR.
  </action>

  <verify>
    <automated>VERSION=1.10.0 VERSION_INBOUND=1.5.2 DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh</automated>
  </verify>

  <acceptance_criteria>
    `scripts/consumer_install_smoke.sh` (with VERSION=1.10.0 VERSION_INBOUND=1.5.2 DEP_MODE=hex
    INCLUDE_INBOUND=true) exits 0 AND its `mix deps.get` output shows mailglass 1.10.0 and
    mailglass_inbound 1.5.2 fetched from Hex.

    `gh run view` of post-publish-smoke.yml shows all jobs passed, OR the only red job is
    `consumer-install` with output containing "hackney" (issue #32 false-positive, acceptable
    non-blocker per D-13). No other failure is accepted as noise.
  </acceptance_criteria>

  <done>
    Consumer smoke passed within the 60-minute window; Hex resolution of all three packages confirmed.
    Post-publish smoke passed (or only the #32 hackney noise red). Retract-window decision: proceed.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 5 — Milestone closeout — REL-02, D-12
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 7 (Wave 5): Milestone audit (7 phases / 15 reqs), archive, git tag v1.14 (REL-02, D-12)</name>
  <files>.planning/milestones/v1.14-ROADMAP.md, .planning/milestones/v1.14-REQUIREMENTS.md, .planning/milestones/v1.14-MILESTONE-AUDIT.md, .planning/milestones/MILESTONES.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/RETROSPECTIVE.md, .planning/STATE.md</files>

  <read_first>
    STATE.md — the v1.13 closeout precedent (2026-06-21) and the milestone.complete count-inflation
    gotcha. 117-01-PLAN.md Task 6 for the exact archive sequence + audit-file shape.
    .planning/REQUIREMENTS.md — the 14-requirement traceability table (all Complete except REL-01/02,
    which this phase closes) — this is the authoritative scope source for the audit.

    SCOPE (D-12): the milestone audit is 7 phases (118–124) and 15 requirements. The 15 REQ-IDs are
    (verify verbatim): METHOD-01, METHOD-02, STORY-01, STORY-02, SHELL-01, SHELL-02, SHELL-03,
    DELIV-01, INB-01, PREV-01, COH-01, COH-02, SEED-003 (NOT SEED-01), REL-01, REL-02. NOTE: REL has
    only TWO IDs this milestone (unlike v1.13's three). The 14 IDs in REQUIREMENTS.md PLUS the
    deferred-but-tracked SEED-003 give the 15-item audit scope per D-12.

    COUNT-INFLATION WARNING (D-12): Do NOT use `gsd-sdk milestone.complete` raw counts for the audit.
    The CLI counts ALL .planning/phases/ directories including leftover 999.1, 999.2 backlog dirs.
    Manually use the true v1.14 scope: 7 phases (118–124), 15 requirements.
  </read_first>

  <action>
    Step 5.1 — Stability/structural contract baseline check:
    Run the repo's contract gate (e.g. `mix verify.stability_contract` or `mix verify.support_contract`,
    whichever MAINTAINING.md names) from repo root to confirm the shipped v1.10.0 codebase passes all
    contract checks. (If the demo/reference path re-bumps a mix.lock, `git checkout` it after.)

    Step 5.2 — Archive ROADMAP:
    `cp .planning/ROADMAP.md .planning/milestones/v1.14-ROADMAP.md`

    Step 5.3 — Archive REQUIREMENTS:
    `cp .planning/REQUIREMENTS.md .planning/milestones/v1.14-REQUIREMENTS.md`

    Step 5.4 — Write MILESTONE-AUDIT (REL-02, D-12):
    Create `.planning/milestones/v1.14-MILESTONE-AUDIT.md`. Include:
    - Header: "# v1.14 Milestone Audit — Operator IA & Lived-Experience Redesign"
    - Audit date (actual)
    - Verdict line: `status: passed`
    - True scope stats (CORRECTED, not from gsd-sdk): 7 phases (118–124), 15 requirements, packages
      shipped mailglass 1.10.0 / mailglass_admin 1.10.0 / mailglass_inbound 1.5.2
    - Per-requirement pass/fail table: ALL 15 REQ-IDs marked `status: passed` — METHOD-01, METHOD-02,
      STORY-01, STORY-02, SHELL-01, SHELL-02, SHELL-03, DELIV-01, INB-01, PREV-01, COH-01, COH-02,
      SEED-003, REL-01, REL-02
    - Phase table: 118–124 all Complete
    - Evidence references: published Hex URLs (1.10.0/1.10.0/1.5.2), post-publish-smoke run URL,
      release PR URL

    Step 5.5 — Update MILESTONES.md:
    Add a v1.14 row with CORRECTED counts:
    `✅ v1.14 Operator IA & Lived-Experience Redesign — 7 phases (118–124), 15 reqs, shipped YYYY-MM-DD — mailglass 1.10.0 / mailglass_admin 1.10.0 / mailglass_inbound 1.5.2 (see milestones/v1.14-MILESTONE-AUDIT.md)`

    Step 5.6 — Update PROJECT.md:
    Advance current-milestone notation to v1.14 shipped; update the "Current state" line to reflect
    1.10.0/1.10.0/1.5.2 as the current live Hex versions.

    Step 5.7 — Update ROADMAP.md:
    Move Phase 124 to Complete; flip the v1.14 milestone entry to ✅ SHIPPED; move v1.14 into the
    shipped/archived milestone history list at the top; clear the active-milestone section (or stub
    it with "no active milestone — see /gsd-new-milestone for next"). Update the Progress table row
    for Phase 124. (Use scoped Edits — never Write the whole ROADMAP.)

    Step 5.8 — Remove REQUIREMENTS.md (or replace with stub), following the v1.12/v1.13 precedent:
    `git rm .planning/REQUIREMENTS.md` (it is archived in v1.14-REQUIREMENTS.md), or replace with a
    one-line stub: "# Requirements — awaiting next milestone definition".

    Step 5.9 — Append RETROSPECTIVE entry:
    Append a v1.14 section to `.planning/RETROSPECTIVE.md`:
    - Milestone summary (7 phases, top-down JTBD/IA-led redesign + adversarial persona-critic loop + release)
    - What worked: method inversion (judgment-level persona critics catching IA/redundancy gates can't);
      surface-by-surface biggest-impact-first ordering; inherited-floor-green-only-forward ratchet;
      rebase-reconcile-then-ff-push for the divergent origin; deliberate fix(inbound): paired-release idiom
    - Key lesson (the structural delta this cycle): origin had diverged (126 ahead / 1 behind a lone
      dependabot ci.yml commit) — rebase onto origin/main then fast-forward push, NEVER --force; the
      rebased HEAD is what RP tags and gate-ci-green requires green ci.yml on
    - Key lesson: the inbound release does NOT auto-cut from the RP sed pin-sync — a deliberate
      fix(inbound): re-pin commit must reach origin BEFORE the RP PR merges, or publish-admin
      (needs publish-inbound) blocks
    - Release outcome: 1.10.0/1.10.0/1.5.2 live; first linked-version cut since 1.8.0/1.8.0/1.4.0 (v1.13)

    Step 5.10 — Update STATE.md (docs(state): commit reflecting Phase 124 complete, v1.14 closed).

    Step 5.11 — Commit all milestone artifacts:
    `git add .planning/milestones/v1.14-ROADMAP.md .planning/milestones/v1.14-REQUIREMENTS.md .planning/milestones/v1.14-MILESTONE-AUDIT.md .planning/milestones/MILESTONES.md .planning/PROJECT.md .planning/ROADMAP.md .planning/RETROSPECTIVE.md .planning/STATE.md`
    Plus the REQUIREMENTS.md removal (`git rm .planning/REQUIREMENTS.md`) if taking that path.
    Commit: `docs(state): v1.14 complete — mailglass 1.10.0/1.10.0/1.5.2 on Hex`

    Step 5.12 — Tag v1.14 (REL-02):
    `git tag v1.14` then `git push origin v1.14`.
    NOTE: the git tag v1.14 is a MILESTONE marker, distinct from the RP-created Hex release tags
    (mailglass-v1.10.0, mailglass_admin-v1.10.0, mailglass_inbound-v1.5.2).

    Step 5.13 — Push the commit: `git push origin main`.
  </action>

  <verify>
    <automated>ls .planning/milestones/v1.14-MILESTONE-AUDIT.md && grep -q 'status: passed' .planning/milestones/v1.14-MILESTONE-AUDIT.md && git tag -l v1.14 | grep -q v1.14</automated>
  </verify>

  <acceptance_criteria>
    `ls .planning/milestones/v1.14-*` returns at least three files: v1.14-ROADMAP.md,
    v1.14-REQUIREMENTS.md, v1.14-MILESTONE-AUDIT.md.

    `grep 'status: passed' .planning/milestones/v1.14-MILESTONE-AUDIT.md` returns a match.
    The audit file lists all 15 REQ-IDs (spot-check:
    `grep -E 'METHOD-01|STORY-02|SHELL-03|DELIV-01|INB-01|PREV-01|COH-02|SEED-003|REL-01|REL-02' .planning/milestones/v1.14-MILESTONE-AUDIT.md`
    returns matches; and `grep -c 'status: passed' .planning/milestones/v1.14-MILESTONE-AUDIT.md` is at
    least 15) and states "7 phases" (not inflated by 999.x dirs).

    `grep 'v1.14' .planning/milestones/MILESTONES.md` returns a row containing "1.10.0", "7 phases", and "15 reqs".

    `git tag -l v1.14` returns "v1.14" AND `git ls-remote --tags origin v1.14` returns the tag ref on origin.
  </acceptance_criteria>

  <done>
    Milestone v1.14 audited (7 phases 118–124, 15/15 requirements status: passed,
    1.10.0/1.10.0/1.5.2 on Hex). Three archive files created under .planning/milestones/. MILESTONES.md,
    PROJECT.md, ROADMAP.md, RETROSPECTIVE.md, STATE.md updated; REQUIREMENTS.md removed; v1.14 tag on
    origin. v1.14 milestone is CLOSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Hex publish | Irreversible: once mailglass 1.10.0 is on Hex and 60 min pass, it cannot be unpublished |
| RP PR diff → maintainer review | Load-bearing PR (dep-pin sed sync inside); must be human-reviewed before auto-merge fires |
| publish-hex.yml → hex-publish environment | HEX_API_KEY scoped to the hex-publish GitHub Environment; never exposed to PR jobs |
| local main → origin/main push | The rebase + fast-forward push re-scores the RP PR; a force-push or partial push could publish an unintended version or break linear history |
| fix(inbound): commit → RP linked PR | The inbound paired release exists ONLY because this commit reaches origin before the PR merges; absence blocks publish-admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-01 | Tampering | .release-please-manifest.json | high | mitigate | Never hand-edit; let Release Please manage; MAINTAINING.md Step 2 prohibits manual edits; Task 3 verifies the regenerated values rather than authoring them |
| T-124-02 | Information Disclosure | HEX_API_KEY secret | high | mitigate | Key scoped to the hex-publish GitHub Environment only; never visible to PR jobs (CLAUDE.md Commit & Branch Conventions) |
| T-124-03 | Repudiation | Hex publish origin | medium | mitigate | publish-hex.yml gated on gate-ci-green; publish only from the release tag (never main); all publish runs logged in GitHub Actions |
| T-124-04 | Tampering | rebase + force-push corrupting origin history / publishing an unintended version | critical | mitigate | HARD never-`--force` rule (D-04/D-06); Task 2 acceptance gates on a fast-forward push (origin HEAD descends from #88) and `origin/main == HEAD`; Task 3 re-verifies 1.10.0/1.10.0/1.5.2 before the Wave-2 human go/no-go authorizes the irreversible merge |
| T-124-05 | Denial of Service | inbound release absent → publish-admin (needs publish-inbound) stalls | high | mitigate | Task 2 lands the deliberate fix(inbound): re-pin BEFORE the push (D-07); Task 3 Step 1.7 is a BLOCKING gate that refuses to merge an inbound-less PR; Task 5 confirms publish-inbound succeeded before publish-admin |
| T-124-06 | Denial of Service | Anti-recursion stall (no publish fan-out after bot-merge) | low | accept | Self-healed by hourly cron + gate-ci-green auto-dispatch; manual recovery via `gh workflow run release-please.yml`; acceptable latency ≤ 60 min |
| T-124-07 | Elevation of Privilege | Dispatch publish-hex.yml from main | medium | mitigate | Workflow docs + MAINTAINING.md state "never dispatch from main"; always use `--ref mailglass-v1.10.0 --field tag=mailglass-v1.10.0` |
| T-124-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no new packages; the ceremony uses the existing mix toolchain only (the sole source edit is the inbound mix.exs pin string — no new application attack surface) |
</threat_model>

<verification>
Full phase gate (all must pass before Phase 124 is marked Complete):

- Wave 0: `mix mailglass.publish.check --package {mailglass,mailglass_admin,mailglass_inbound}` each exit 0 (clean no-op expected, D-08); green CI on local main (D-05).
- Wave 1: `git rev-parse origin/main == HEAD` via fast-forward (NO force); `mailglass_inbound/mix.exs` re-pinned `== 1.10.0` with a `fix(inbound):` commit on origin (D-07); ci.yml green on the new HEAD (D-05); regenerated RP PR shows 1.10.0/1.10.0 + paired inbound 1.5.2 with `== 1.10.0` pins, CHANGELOG `## [1.10.0]` / `## [1.5.2]`, required CI green, auto-merge armed (D-03/D-11). Release-As: 1.10.0 fallback only if RP scored a patch.
- Wave 2: maintainer go/no-go approved; publish fan-out visible in Actions (or stall recovery applied) (D-11).
- Wave 3: `mix hex.info mailglass 1.10.0` / `mailglass_admin 1.10.0` / `mailglass_inbound 1.5.2` each contain "Released:"; inbound shows `mailglass == 1.10.0` (REL-01, D-07).
- Wave 4: `consumer_install_smoke.sh` exits 0 with DEP_MODE=hex VERSION=1.10.0 VERSION_INBOUND=1.5.2; post-publish-smoke passes (or only #32 hackney noise red) (REL-01, D-13).
- Wave 5: `ls .planning/milestones/v1.14-*` returns 3 files; MILESTONE-AUDIT.md `status: passed` with all 15 REQ-IDs and 7-phase scope; `git tag -l v1.14` + `git ls-remote --tags origin v1.14` confirm the tag (REL-02, D-12).
</verification>

<success_criteria>
1. mailglass 1.10.0 is live on Hex: `mix hex.info mailglass 1.10.0` contains "Released:".
2. mailglass_admin 1.10.0 is live on Hex: `mix hex.info mailglass_admin 1.10.0` contains "Released:".
3. mailglass_inbound 1.5.2 is live with the correct pin: `mix hex.info mailglass_inbound 1.5.2 | grep mailglass` returns `mailglass == 1.10.0` (D-07/PROJECT D-13).
4. mix deps.get resolves all three from Hex (consumer smoke passes — REL-01).
5. Post-publish smoke passes (REL-01), or only the known #32 hackney false-positive is red (D-13).
6. origin/main carries the rebased v1.14 body via fast-forward (no force-push), and the fix(inbound): re-pin reached origin before the RP PR merged (D-04/D-07).
7. v1.14 milestone is archived: `.planning/milestones/v1.14-MILESTONE-AUDIT.md` exists with all 15 requirements `status: passed`, scope corrected to 7 phases / 15 reqs (not inflated by 999.x dirs) (D-12).
8. git tag v1.14 exists on origin (REL-02).
</success_criteria>

<output>
Create `.planning/phases/124-release-cut-milestone-closeout/124-01-SUMMARY.md` when done.

Summary must include:
- Hex versions confirmed live (with `mix hex.info` output excerpts): 1.10.0/1.10.0/1.5.2
- Inbound pin verification (`mailglass == 1.10.0`)
- Origin reconciliation: rebase onto #88 + fast-forward push evidence (no force-push), new HEAD SHA, green ci.yml run on it
- The fix(inbound): re-pin commit SHA and confirmation it reached origin before the RP PR merged
- Consumer smoke exit code + GET /dev/mail/ result + REL-01 Hex-resolution evidence
- post-publish-smoke run URL + outcome (and any #32 noise classification)
- Milestone audit: scope counts (7 phases, 15 reqs), all 15 REQ-IDs confirmed status: passed
- Git tag v1.14: `git tag -l v1.14` and push confirmation
- Any stall recovery or Release-As: fallback steps taken
</output>
