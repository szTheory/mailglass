---
phase: 123-cross-surface-coherence-ratchet-re-arm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/docs/ui-baseline-scores.json
  - mailglass_admin/e2e/judgment.spec.js
  - .planning/research/v1.14/MILESTONE-SEED.md
autonomous: true
requirements: [COH-02]
must_haves:
  truths:
    - "ratchet_baseline_test.exs passes: current promoted over prior under distinct run_ids, no cell regresses (only-forward), schema_version stays 3"
    - "judgment.spec.js disclaimer comments reflect armed status (no 'arming is Phase 123' / 'NOT yet added to a required CI lane' text)"
    - "The floor gate inventory in MILESTONE-SEED records the two judgment gates (gate count ~26 → ~28)"
    - "Committed mailglass_admin/priv/static/app.css is byte-unchanged (no asset rebuild committed)"
  artifacts:
    - "mailglass_admin/docs/ui-baseline-scores.json (promoted: prior=old current, current=fresh 54-cell re-score under new run_id)"
    - "mailglass_admin/e2e/judgment.spec.js (de-disclaimed comments)"
    - ".planning/research/v1.14/MILESTONE-SEED.md (gate count updated to include the two judgment gates)"
  key_links:
    - "ui-baseline-scores.json current/prior blocks ← anti-vacuity run_id guard + compare_baselines/2 in ratchet_baseline_test.exs"
    - "judgment.spec.js gates already globbed by playwright.config.cjs testDir './e2e' → run in required operator_browser_gate CI lane (ci.yml:643-714)"
---

<objective>
Re-score the 54-cell aesthetic ratchet only-forward and arm the two judgment gates into the documented floor — the mechanical COH-02 deliverable.

Purpose: Promote the redesigned-surface scores over the held floor under distinct run_ids (anti-vacuity guard is load-bearing) and convert the two already-green judgment gates from "drafted" to "armed" in the floor record, so the redesign's headline IA fixes cannot silently regress before the Phase 124 release cut.

Output: A promoted+re-scored ui-baseline-scores.json that passes ratchet_baseline_test.exs only-forward; de-disclaimed judgment.spec.js comments; an updated floor gate count in MILESTONE-SEED.md.

This is a JSON + comment + doc edit only. NO HEEx change. NO committed asset-bundle rebuild (D-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md

@mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
@mailglass_admin/docs/ui-baseline-scores.json
@mailglass_admin/e2e/judgment.spec.js
@mailglass_admin/playwright.config.cjs
@.planning/research/v1.14/MILESTONE-SEED.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote + re-score the 54-cell aesthetic ratchet baseline (D-01/D-02/D-03)</name>
  <files>mailglass_admin/docs/ui-baseline-scores.json</files>
  <read_first>
    - mailglass_admin/docs/ui-baseline-scores.json (the promotion target: current.run_id "2026-06-20-phase-116", prior.run_id "2026-06-16-phase-103", schema_version 3)
    - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs (@surfaces line 26 = ["deliveries","inbound","preview"]; @pillars; @themes; anti-vacuity guard lines 85-92; compare_baselines/2 lines 97-124; the "D-07 procedure" moduledoc lines 12-14)
    - .planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md (D-01/D-02/D-03 full text)
  </read_first>
  <action>
    Promote and re-score ui-baseline-scores.json as a JSON-only edit. Steps:
    (1) Copy the existing `current` block verbatim into `prior` (the held floor becomes the comparison baseline). Discard the old `prior` block (its run_id "2026-06-16-phase-103" is superseded — only-forward keeps the most-recent committed as prior).
    (2) Set the new `current.run_id` to a fresh distinct value encoding this phase — use "2026-06-28-phase-123" (must differ from prior's run_id, which is now "2026-06-20-phase-116"; the anti-vacuity guard at ratchet_baseline_test.exs:85-92 fails loudly if they match).
    (3) Run the LLM "D-07 procedure" 54-cell re-score to fill the new `current.surfaces` block: exactly the 3 record-surfaces deliveries/inbound/preview × 6 pillars (Spacing/Radius/Color/Type/Elevation/Motion+A11y) × 3 themes (light/dark/system) = 54 cells. Score each cell an integer in 1..4 (1=non-conformant, 2=significant-gaps, 3=mostly-conformant, 4=excellent) against the pillar rubric (design-system.md:104-121, named in the JSON's pillar_rubric field) judged on the post-119-122 redesigned surfaces. Every cell must MEET-OR-BEAT the prior cell (prior is the previous all-4s/preview-Motion-4 floor) — if any genuine re-assessment scores below prior, STOP and surface the regression in the SUMMARY rather than weakening the score or the assertion (D-03).
    (4) Do NOT add an `overview` key — @surfaces is fixed to the 3 record-surfaces and silently ignores extras (D-02). Keep `schema_version: 3` and the `pillar_rubric`/`grade_scale` metadata fields unchanged.
    Do NOT run `mix assets.build` (D-12). This task touches only the JSON.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs` exits 0
    - ui-baseline-scores.json: prior.run_id != current.run_id (jq '.prior.run_id != .current.run_id' returns true)
    - ui-baseline-scores.json: current.run_id == "2026-06-28-phase-123" (or another distinct phase-123-encoding string)
    - ui-baseline-scores.json: schema_version == 3
    - All 54 cells present in BOTH prior and current blocks, every score an integer 1..4
    - No `overview` key exists under current.surfaces or prior.surfaces
    - `git diff --stat mailglass_admin/priv/static/` shows zero changes (no asset rebuild committed)
  </acceptance_criteria>
  <done>ratchet_baseline_test.exs passes only-forward with promoted+re-scored 54 cells under a distinct current run_id; priv/static untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the two judgment gates — de-disclaim comments + record in floor inventory (D-04/D-05/D-14)</name>
  <files>mailglass_admin/e2e/judgment.spec.js, .planning/research/v1.14/MILESTONE-SEED.md</files>
  <read_first>
    - mailglass_admin/e2e/judgment.spec.js (stale disclaimer comment blocks at lines 11-18, the NOTE at :75, the NOTE at :102; the live test( bodies for nav-active-correctness :76-93 and no-nav-duplication :103-113)
    - mailglass_admin/playwright.config.cjs (testDir "./e2e" globs judgment.spec — confirms gates already run in the required operator_browser_gate lane; no ci.yml edit needed)
    - .planning/research/v1.14/MILESTONE-SEED.md (the "~26 conformance gates" / idempotent-floor inventory line near line 76)
    - .planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md (D-04/D-05/D-14)
  </read_first>
  <action>
    <!-- planner-discipline-allow: arming is Phase 123 -->
    <!-- planner-discipline-allow: NOT yet added to a required CI lane -->
    Arm the two judgment gates as a documentation/registration step (they ALREADY run green in the required operator_browser_gate CI lane — there is nothing to wire). Two edits:
    (a) In mailglass_admin/e2e/judgment.spec.js, update the stale disclaimer comments to reflect armed status. Specifically: rewrite the header comment block (lines 11-18) that describes the gates as `test.fixme` drafts flipped in 119 and "armed into the ratchet floor in Phase 123" so it reads as the present armed state (the gates ARE live `test(` and ARE in the required lane). Remove both inline NOTE lines (currently at :75 and :102) that read "This gate is NOT yet added to a required CI lane — that arming is Phase 123 (D-13)." Replace each with a one-line note stating the gate is armed and runs in the required operator_browser_gate lane via playwright.config.cjs testDir glob. Do NOT change the describe block name's meaning, the test names, or any assertion body — only the explanatory comments.
    (b) In .planning/research/v1.14/MILESTONE-SEED.md, update the idempotent-floor inventory line so the gate count reflects the two newly-armed judgment gates: change "~26 conformance gates (mailglass_admin/scripts/check-conformance.sh)" to record ~28 gates total, naming the two armed judgment gates (nav-active-correctness, no-nav-duplication) as running in the operator_browser_gate Playwright lane (NOT in check-conformance.sh, which is grep-based and cannot read rendered nav state — D-05).
    Do NOT edit ci.yml (no lane to add — would risk double-running). Do NOT add the gates to check-conformance.sh. gate-self-test.yml is a meta-workflow that proves the Tests gate blocks PRs and has no per-gate registry — leave it unchanged.
    D-14 paired-test discipline: the disclaimer string "arming is Phase 123" appears ONLY inside judgment.spec.js (verified: no voice_test.exs or e2e spec greps it). Confirm no other file greps any comment string you change before committing.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -rL "arming is Phase 123" e2e/judgment.spec.js >/dev/null && ! grep -q "arming is Phase 123" e2e/judgment.spec.js && grep -q "operator_browser_gate" e2e/judgment.spec.js && echo ARMED</automated>
  </verify>
  <acceptance_criteria>
    - judgment.spec.js contains NO "arming is Phase 123" string
    - judgment.spec.js contains NO "NOT yet added to a required CI lane" string
    - judgment.spec.js still contains both live tests: `grep -c 'test("' e2e/judgment.spec.js` >= 2 (the two gate bodies are unchanged)
    - MILESTONE-SEED.md floor inventory records ~28 gates and names nav-active-correctness + no-nav-duplication
    - `git grep -l "arming is Phase 123"` returns no source files outside .planning/ (paired-test discipline confirmed)
    - ci.yml, check-conformance.sh, gate-self-test.yml are unchanged (git diff shows no changes to them)
  </acceptance_criteria>
  <done>judgment.spec.js comments reflect armed-and-running status; MILESTONE-SEED floor inventory counts the two judgment gates; no ci.yml/conformance/gate-self-test wiring touched; gate test bodies byte-identical.</done>
</task>

</tasks>

<artifacts_produced>
This plan produces these precise edits:
- **mailglass_admin/docs/ui-baseline-scores.json** — `prior` ← old `current` block (run_id "2026-06-20-phase-116"); `current` ← fresh 54-cell re-score under new `current.run_id "2026-06-28-phase-123"`; schema_version stays 3; no `overview` key.
- **mailglass_admin/e2e/judgment.spec.js** — header comment block (lines 11-18) rewritten to armed status; both inline NOTE disclaimers (:75, :102) replaced with armed-and-running notes citing operator_browser_gate; ZERO assertion/test-body changes.
- **.planning/research/v1.14/MILESTONE-SEED.md** — idempotent-floor inventory line updated from "~26 conformance gates" to ~28 total, naming the two armed judgment gates.
- **NOT touched:** mailglass_admin/priv/static/app.css (no rebuild), ci.yml, check-conformance.sh, gate-self-test.yml, docs/axe-baseline.json (D-13 holds only-forward).
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| re-score → committed baseline | An LLM-authored score block becomes the only-forward floor; a too-generous score weakens future regression detection |
| comment edit → grepped test assertions | A changed comment string could silently invalidate a paired test that greps it (Pitfall-2) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-01 | Tampering | ui-baseline-scores.json re-score | medium | mitigate | anti-vacuity run_id guard + compare_baselines/2 only-forward assertion fail loudly on vacuous/regressing scores; meet-or-beat enforced by ratchet_baseline_test.exs |
| T-123-02 | Tampering | judgment.spec.js comment edit | low | mitigate | D-14 paired-test check — confirmed "arming is Phase 123" is grepped by no other file; verify before commit |
| T-123-03 | Repudiation | committed asset bundle | medium | mitigate | D-12 — do NOT run/commit mix assets.build; verify priv/static byte-unchanged in acceptance criteria |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs` exits 0 (only-forward, anti-vacuity, range, coverage all green)
- judgment.spec.js de-disclaimed; gate test bodies unchanged; runs in required lane (already green — no new run added)
- MILESTONE-SEED floor inventory reflects the two armed judgment gates (~28)
- `git diff --stat mailglass_admin/priv/static/` empty
</verification>

<success_criteria>
COH-02 mechanical floor satisfied: ratchet baseline re-scored only-forward under distinct run_ids with zero regressions; the two judgment gates armed (de-disclaimed + recorded in the floor inventory) while remaining green in the required CI lane; no asset bundle committed.
</success_criteria>

<output>
Create `.planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-01-SUMMARY.md` when done
</output>
