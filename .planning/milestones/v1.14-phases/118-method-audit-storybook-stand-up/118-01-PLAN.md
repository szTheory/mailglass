---
phase: 118-method-audit-storybook-stand-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - reference/demo_app/mix.exs
  - reference/demo_app/mix.lock
  - reference/demo_app/lib/mailglass_demo_web/storybook.ex
  - reference/demo_app/lib/mailglass_demo_web/router.ex
  - reference/demo_app/storybook/
autonomous: true
requirements: [METHOD-01, STORY-01]
must_haves:
  truths:
    - "The screenshot cache dir is git-ignored: `git check-ignore .planning/research/v1.14/.cache/screenshots/x.png` exits 0 BEFORE any screenshot harness runs."
    - "A dev-only phoenix_storybook surface mounts at /dev/storybook in the demo app and renders admin primitives styled by the committed app.css sandbox bundle."
    - "The committed mailglass_admin/priv/static/app.css is byte-identical (no rebuild): `mix verify.preview` git-diff on priv/static/ exits 0."
    - "phoenix_storybook is only: :dev — adopters never install it; nothing dev-only lands in mailglass_admin/lib/."
  artifacts:
    - ".gitignore entry covering .planning/research/v1.14/.cache/ (the screenshot evidence cache)"
    - "reference/demo_app/lib/mailglass_demo_web/storybook.ex (hand-written PhoenixStorybook backend, NOT mix phx.gen.storybook scaffold)"
    - "reference/demo_app/storybook/**/*.story.exs (initial foundations→primitives story inventory)"
    - "reference/demo_app/mix.exs gains {:phoenix_storybook, \"~> 1.2\", only: :dev}; router gains storybook_assets() + live_storybook dev-only"
  key_links:
    - "css_path points at the already-served committed admin bundle route /dev/mail/css-<hash> — a <link> URL string, NO new asset build"
    - "Each theme-sensitive variation sets data-theme=\"mailglass-light|mailglass-dark\" at the template level (NOT the storybook color_mode class picker)"
  prohibitions:
    - "DO NOT run `mix phx.gen.storybook` unmodified — it scaffolds a watcher + storybook.css/js build path (D-07). Hand-write the backend module."
    - "DO NOT edit mailglass_admin/assets/css/app.css OR the committed priv/static/app.css to add a class→data-theme alias — trips TokenParityTest + the priv/static drift gate (D-08)."
    - "DO NOT place the backend module or any *.story.exs under mailglass_admin/lib/ — the :files glob would tarball them to adopters and break their compile (D-06)."
    - "DO NOT add a js_path / esbuild watcher to the demo dev.exs — stories are static markup (D-07)."
    - "DO NOT rebuild or commit mailglass_admin/priv/static/app.css."
---

<objective>
Stand up the dev-only phoenix_storybook review surface in the demo app, and land the Wave-0 `.gitignore`
prerequisite that the screenshot harness (Plan 03) depends on.

Purpose: STORY-01 needs an interactive component review surface that renders admin primitives on-brand
using the committed `app.css` as its sandbox stylesheet — without touching the zero-Node adopter-facing
asset pipeline. The `.gitignore` entry must land first so the gsd commit seam never sweeps the ~80-120
evidence PNGs the persona-critic harness produces.
Output: a mounted `/dev/storybook` surface, an initial story inventory, and a git-ignored screenshot cache.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-method-audit-storybook-stand-up/118-CONTEXT.md
@.planning/phases/118-method-audit-storybook-stand-up/118-RESEARCH.md
@.planning/research/v1.14/STRESS-TEST-PROMPT.md
@reference/demo_app/mix.exs
@reference/demo_app/lib/mailglass_demo_web/router.ex
@reference/demo_app/config/dev.exs
@mailglass_admin/lib/mailglass_admin/controllers/assets.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .gitignore entry for the screenshot evidence cache (Wave-0 prereq)</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` (root) — confirm there is no existing `.planning` / `.cache` / `screenshot` rule (RESEARCH Pitfall 1 verified none exists).
    - 118-RESEARCH.md "Pitfall 1" — the cache dir is NOT actually ignored today; `git check-ignore` returns NOT IGNORED.
  </read_first>
  <action>
    Per D-02 and RESEARCH Pitfall 1: the screenshot cache `.planning/research/v1.14/.cache/screenshots/` is
    NOT git-ignored today (verified: `git check-ignore` returns not-ignored; no `.gitignore` exists under
    `.planning/`). Add a rule to the ROOT `.gitignore` that ignores the per-milestone research cache so the
    gsd commit seam (known to sweep all dirty `.planning/` paths) cannot stage the evidence PNGs. Use a
    pattern that covers the v1.14 cache and future milestone caches under `.planning/research/` — e.g. a
    rule matching `.cache/` nested under `.planning/research/`. Keep screenshots as evidence-only attachments
    per D-02 (they are explicitly NOT a pixel-diff baseline — pixel-diff is out of scope). Do NOT ignore any
    tracked artifact (e.g. DEFECT-REGISTER.md, story files) — scope the rule narrowly to the `.cache` dir.
  </action>
  <verify>
    <automated>git check-ignore -v .planning/research/v1.14/.cache/screenshots/x.png</automated>
  </verify>
  <acceptance_criteria>
    `git check-ignore -v .planning/research/v1.14/.cache/screenshots/x.png` exits 0 and prints the matching
    `.gitignore` rule. No tracked file under `.planning/research/v1.14/` (e.g. MILESTONE-SEED.md,
    STRESS-TEST-PROMPT.md, a future DEFECT-REGISTER.md) becomes ignored — verify with
    `git check-ignore .planning/research/v1.14/DEFECT-REGISTER.md` exiting non-zero (not ignored).
  </acceptance_criteria>
  <done>The screenshot cache path is git-ignored; sibling tracked research artifacts remain tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Add phoenix_storybook only: :dev + hand-written backend module + dev-only router mount</name>
  <files>reference/demo_app/mix.exs, reference/demo_app/mix.lock, reference/demo_app/lib/mailglass_demo_web/storybook.ex, reference/demo_app/lib/mailglass_demo_web/router.ex</files>
  <read_first>
    - `reference/demo_app/mix.exs` — `deps/0` (where the dep is added) and `elixirc_paths/1`.
    - `reference/demo_app/lib/mailglass_demo_web/router.ex` — the existing dev-only `/dev` scope mounting `mailglass_admin_routes("/mail", ...)`.
    - `mailglass_admin/lib/mailglass_admin/controllers/assets.ex` — `css_hash/0` (line ~99) emits the bundle hash; the served route is `get "/css-:md5", ...` under the admin mount (`/dev/mail`).
    - 118-RESEARCH.md Pattern 1 + the css_path note + Open Question 1 (the exact `css_path` literal).
    - phoenix_storybook setup + sandboxing docs (cited in 118-CONTEXT.md canonical_refs): https://phoenix-storybook.hexdocs.pm/setup.html , https://phoenix-storybook.hexdocs.pm/sandboxing.html
  </read_first>
  <action>
    Per D-06/D-07/D-09: add `{:phoenix_storybook, "~> 1.2", only: :dev}` to `reference/demo_app/mix.exs`
    `deps/0`, then `cd reference/demo_app && mix deps.get` to resolve it (this writes the new entry +
    transitive deps into `reference/demo_app/mix.lock`). Hand-write a backend module
    `MailglassDemoWeb.Storybook` using `use PhoenixStorybook` with `otp_app: :mailglass_demo`, a
    `content_path` pointing at a new `reference/demo_app/storybook` dir, and `sandbox_class: "mg-admin-root"`
    (the class the admin shell roots use). Wire `css_path` to the already-served committed admin bundle: an
    absolute URL-string `"/dev/mail/css-" <> MailglassAdmin.Controllers.Assets.css_hash()` — this is just a
    `<link>` URL, NOT a filesystem path and NOT a new build (D-07). OMIT `js_path` entirely (static stories,
    no esbuild watcher). In the demo router, add `import PhoenixStorybook.Router`, a `storybook_assets()`
    call (serves storybook's own prebuilt explorer assets shipped in the hex package), and
    `live_storybook "/storybook", backend_module: MailglassDemoWeb.Storybook` inside the EXISTING dev-only
    `/dev` scope (so it is never prod-reachable — V4 access-control mitigation). Do NOT run
    `mix phx.gen.storybook` (D-07: it scaffolds a watcher + storybook.css/js path). Do NOT touch
    `mailglass_admin/` at all. Confirm `cd reference/demo_app && mix compile` succeeds.

    mix.lock policy: commit ONLY the intentional new `phoenix_storybook` entry + its genuinely-new transitive
    deps; do NOT commit pre-existing transitive drift (e.g. swoosh re-bumps) per the project's mix.lock
    integration-resolution policy.
  </action>
  <verify>
    <automated>cd reference/demo_app && mix compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    `reference/demo_app/mix.exs` declares `{:phoenix_storybook, "~> 1.2", only: :dev}`. `mix deps.get` +
    `mix compile` succeed. `reference/demo_app/lib/mailglass_demo_web/storybook.ex` exists as a hand-written
    `use PhoenixStorybook` backend with `sandbox_class: "mg-admin-root"`, `css_path` pointing at
    `/dev/mail/css-<hash>` (via `MailglassAdmin.Controllers.Assets.css_hash/0`), and NO `js_path`. The router
    mounts `live_storybook "/storybook", ...` + `storybook_assets()` inside the dev-only `/dev` scope. NOTHING
    is added under `mailglass_admin/lib/`. `grep -rn "phx.gen.storybook" reference/demo_app` returns nothing
    (scaffold not run). `reference/demo_app/config/dev.exs` has no new storybook watcher.
  </acceptance_criteria>
  <done>Storybook compiles and is mounted dev-only with the committed bundle as its sandbox CSS; no scaffold watcher; admin lib untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Author initial *.story.exs inventory with template-level data-theme bridge</name>
  <files>reference/demo_app/storybook/</files>
  <read_first>
    - `mailglass_admin/e2e/structural.spec.js:19-44` — the gallery's primitive + state enumeration (nav_link, nav_pill, tenant_chip, theme_picker, stat_card and their states) — mirror this inventory.
    - `mailglass_admin/lib/mailglass_admin/components.ex` — the public primitive function signatures (`nav_link/1`, `nav_pill/1`, `tenant_chip/1`, `theme_picker/1`, `stat_card/1`) used as `def function`.
    - `mailglass_admin/lib/mailglass_admin/operator/shell.ex` (~line 219) — confirm components read `data-theme="mailglass-light|mailglass-dark"` on the `mg-admin-root` root.
    - 118-RESEARCH.md Pattern 2 (data-theme bridge) + Assumption A2 (per-variation wrapper-attribute API; pin from v1.2 Story docs).
  </read_first>
  <action>
    Per STORY-01 + Claude's-discretion story inventory (guided by STRESS-TEST-PROMPT fractal scope:
    foundations → primitives → groups → pages): author an initial set of `*.story.exs` files under
    `reference/demo_app/storybook/` covering the foundations + the five named admin primitives the gallery
    already enumerates (`nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, `stat_card`), each as a
    `use PhoenixStorybook.Story, :component` whose `function` points at the public
    `MailglassAdmin.Components.<primitive>/1`. Use the v1.0+ vocabulary: `Variation`/`VariationGroup`,
    `*.story.exs` (D-09 — ignore pre-1.0 "Story"/"StoryGroup" tutorials). For each theme-sensitive component
    define PAIRED light/dark variations whose rendered root carries `data-theme="mailglass-light"` /
    `data-theme="mailglass-dark"` set at the TEMPLATE/variation level (D-08 bridge) — pin the exact v1.2
    per-variation wrapper mechanism (template option vs a container element in the variation slot) from the
    v1.2 `PhoenixStorybook.Story` docs during this task. Cover the stress states the gallery already exercises
    (normal / long-label / long-value / empty / error / disabled / loading where applicable). Start small
    (foundations + primitives); groups/pages can expand in later phases — stories are review aids, not gates.
    NEVER add a class→data-theme CSS rule to bridge themes (D-08 prohibition).
  </action>
  <verify>
    <automated>cd reference/demo_app && mix compile 2>&1 | tail -5 && ls storybook</automated>
  </verify>
  <acceptance_criteria>
    `reference/demo_app/storybook/` contains at least the five primitive `*.story.exs` files (plus a
    foundations story), each compiling clean and pointing `function` at the public
    `MailglassAdmin.Components.<primitive>/1`. Theme-sensitive components have paired light/dark variations
    that set `data-theme="mailglass-light|mailglass-dark"` at the template level (verify with
    `grep -rn "mailglass-light\|mailglass-dark" reference/demo_app/storybook` returning hits). No
    class→data-theme CSS alias was added anywhere. `mix compile` in the demo app succeeds.
  </acceptance_criteria>
  <done>Initial foundations+primitives story inventory compiles and bridges themes via template-level data-theme; ready for the persona-critic harness to walk in Plan 03.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

| Path / Symbol | Kind | Notes |
|---|---|---|
| `.gitignore` (rule for `.planning/research/**/.cache/`) | edit | git-ignores the screenshot evidence cache (D-02) |
| `MailglassDemoWeb.Storybook` (`reference/demo_app/lib/mailglass_demo_web/storybook.ex`) | new module | hand-written `use PhoenixStorybook` backend; `sandbox_class: "mg-admin-root"`; `css_path → /dev/mail/css-<hash>`; no `js_path` |
| `reference/demo_app/storybook/**/*.story.exs` | new files | initial foundations+primitives story inventory with template-level `data-theme` bridge |
| `reference/demo_app/mix.exs` dep entry | edit | `{:phoenix_storybook, "~> 1.2", only: :dev}` |
| `reference/demo_app/mix.lock` entry | edit | new phoenix_storybook + transitive deps (intentional only) |
| `live_storybook "/storybook"` + `storybook_assets()` mount | router edit | dev-only `/dev` scope in `reference/demo_app/lib/mailglass_demo_web/router.ex` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI host → adopter package | phoenix_storybook must never reach the shipped `mailglass`/`mailglass_admin` packages |
| prod request path → storybook route | the storybook explorer exposes component internals; must be dev-only-reachable |

## STRIDE Threat Register (ASVS L1; block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-01 | Information disclosure | phoenix_storybook explorer reachable in prod | medium | mitigate | `only: :dev` dep (not compiled in prod) + mount strictly inside the demo's dev-only `/dev` router scope (D-06); the dep is absent from `mailglass_admin/mix.exs` `:files` |
| T-118-02 | Tampering | dev-only storybook module/stories tarballed to adopters | medium | mitigate | backend module + `*.story.exs` live in the DEMO app, never under `mailglass_admin/lib/` (the `:files` glob would ship them and break adopter compile) (D-06) |
| T-118-SC | Tampering | `mix deps.get` pulls phoenix_storybook (supply chain) | low | accept | RESEARCH Package Legitimacy Audit: `phoenix_storybook` is the canonical Phoenix storybook (phenixdigital, 1.59M downloads, OK verdict) — not [ASSUMED]/[SUS]/[SLOP]; no human-verify checkpoint required |
</threat_model>

<verification>
- `git check-ignore -v .planning/research/v1.14/.cache/screenshots/x.png` exits 0 (Task 1).
- `cd reference/demo_app && mix compile` succeeds with storybook wired (Tasks 2-3).
- `cd mailglass_admin && mix verify.preview` exits 0 — committed `priv/static/app.css` is byte-identical (no rebuild). This is the load-bearing zero-Node / TokenParityTest guard for D-07/D-08.
- `grep -rn "phx.gen.storybook" reference/demo_app` returns nothing (no scaffold watcher).
- No file added under `mailglass_admin/lib/`; no edit to `mailglass_admin/assets/css/app.css` or `priv/static/app.css`.
</verification>

<success_criteria>
- The screenshot cache dir is git-ignored before any harness runs.
- A dev-only phoenix_storybook surface mounts at `/dev/storybook`, styled by the committed `app.css` sandbox bundle, with an initial foundations+primitives story inventory bridging themes via template-level `data-theme`.
- The committed `priv/static/app.css` is unchanged (`mix verify.preview` clean); phoenix_storybook is `only: :dev`; nothing dev-only lands in `mailglass_admin/lib/`.
</success_criteria>

<output>
Create `.planning/phases/118-method-audit-storybook-stand-up/118-01-SUMMARY.md` when done
</output>
