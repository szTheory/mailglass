---
phase: 119-app-shell-nav-overview-redesign
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - mailglass_admin/e2e/operator.spec.js
  - mailglass_admin/e2e/judgment.spec.js
autonomous: true
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "operator.spec.js no longer asserts the deleted operator-overview-nav block is visible; it asserts the Overview health + drill-through structure instead."
    - "The nav-active-correctness judgment gate is a real test (not test.fixme) and passes: Overview active, Deliveries inactive via attribute OMISSION (not aria-current=false)."
    - "The no-nav-duplication judgment gate is a real test (not test.fixme) and passes: zero operator-overview-nav elements on a populated Overview."
    - "The operator browser gate and judgment gate are both green on a green-only-forward floor."
  artifacts:
    - "mailglass_admin/e2e/operator.spec.js (VERIF-02 rewritten: operator-overview-nav assertion removed; title de-references 'navigation CTAs')"
    - "mailglass_admin/e2e/judgment.spec.js (both gates flipped test.fixme→test; line-87 assertion fixed to not.toHaveAttribute aria-current page)"
  key_links:
    - "judgment.spec.js nav-active-correctness gate ← depends on 119-01 Overview nav_link emitting aria-current=page and active={@view} omitting it on Deliveries."
    - "judgment.spec.js no-nav-duplication gate ← depends on 119-01 deleting the operator-overview-nav block."
    - "operator.spec.js VERIF-02 rewrite ← depends on 119-01 deleting operator-overview-nav (else the assertion + the gate's toHaveCount(0) directly contradict)."
---

<objective>
Land the mandatory paired test updates (D-09) that MUST move in the same phase as the 119-01 code
deletion, on a green-only-forward floor. This is a Wave-2 plan: it runs only after 119-01 ships the
Overview/shell/nav code, because every assertion here asserts the 119-01 end-state.

Two non-trivial edits (NOT naive flips):
1. Rewrite `operator.spec.js` VERIF-02 so it stops asserting the now-deleted `operator-overview-nav`
   block is visible (else the operator browser gate goes red).
2. Flip BOTH `judgment.spec.js` gates `test.fixme`→`test` AND fix the `aria-current="false"` assertion
   to `not.toHaveAttribute("aria-current","page")` — the real `nav_link` OMITS the attribute when
   inactive, so a bare flip without the assertion fix turns the gate red.

Purpose: arm the headline-defect regression coverage (false-active-nav, nav-duplication) so the
keystone fixes are provably green, and keep the inherited v1.13 + operator browser floor green.

Output: edited `operator.spec.js` + `judgment.spec.js`; the operator browser gate and judgment gate
both green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/119-app-shell-nav-overview-redesign/119-CONTEXT.md
@.planning/phases/119-app-shell-nav-overview-redesign/119-RESEARCH.md
@.planning/phases/119-app-shell-nav-overview-redesign/119-PATTERNS.md
@.planning/phases/119-app-shell-nav-overview-redesign/119-01-PLAN.md

@mailglass_admin/e2e/operator.spec.js
@mailglass_admin/e2e/judgment.spec.js
@mailglass_admin/lib/mailglass_admin/components.ex
</context>

<artifacts_this_phase_produces>
- `operator.spec.js` VERIF-02 (the "operator overview landing" test, ~`:352-368`): the
  `operator-overview-nav` `toBeVisible()` assertion (~`:367`) removed; test title no longer claims
  "navigation CTAs"; retains the `operator-overview` + `operator-overview-health` assertions and (from
  119-01 Task 0) the drill-through + orientation-strip assertions.
- `judgment.spec.js`: `nav-active-correctness` and `no-nav-duplication` are real `test(...)` (no longer
  `test.fixme(...)`); line ~`:87` changed from `toHaveAttribute("aria-current","false")` to
  `not.toHaveAttribute("aria-current","page")` (or `getByRole("link", { name: "Deliveries", current: false })`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (D-09): Rewrite operator.spec.js VERIF-02 — drop the deleted-block assertion</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <action>
The "operator overview landing has health cards and navigation CTAs" test (~`:352`) currently asserts
`await expect(page.getByTestId("operator-overview-nav")).toBeVisible();` (~`:367`). 119-01 deleted that
block, so this assertion now contradicts reality (and directly opposes `judgment.spec.js`'s
`toHaveCount(0)` — Pitfall 2).

1. DELETE the `operator-overview-nav` `toBeVisible()` assertion line (and its `// Navigation CTAs container`
   comment).
2. Update the test title to drop "navigation CTAs" — e.g. "operator overview landing has health cards
   and drill-through links" (it now covers triage health, not signpost CTAs).
3. KEEP the `operator-overview` and `operator-overview-health` `toBeVisible()` assertions and the test
   scaffolding (viewport, `openOperator`, the tenant-scoped `page.goto` to the Overview, the
   "Operator overview" heading assertion).
4. Retain the Wave-0 drill-through + orientation-strip assertions added in 119-01 Task 0 — they now
   pass against the shipped code.

Do not add new behavior here beyond removing the stale assertion and the Task-0 scaffolds graduating to
green; this is the VERIF-02 reconciliation only.
  </action>
  <verify>
    <automated>cd mailglass_admin && ! grep -q 'operator-overview-nav' e2e/operator.spec.js && npm run test:operator-browser -- -g "operator overview"</automated>
  </verify>
  <done>operator.spec.js no longer references operator-overview-nav; the "operator overview landing" test passes against the 119-01 code, asserting the health row + drill-through links + empty-pane orientation.</done>
</task>

<task type="auto">
  <name>Task 2 (D-09): Flip + fix the two judgment gates</name>
  <files>mailglass_admin/e2e/judgment.spec.js</files>
  <action>
Two drafted gates currently sit as `test.fixme(...)` (~`:77` nav-active-correctness, ~`:102`
no-nav-duplication). 119-01 shipped the end-state they assert; arm them.

1. Flip `test.fixme(` → `test(` for BOTH gates (the `nav-active-correctness` gate ~`:77` and the
   `no-nav-duplication` gate ~`:102`).
2. REQUIRED assertion fix (Pitfall 3 — not optional): the `nav-active-correctness` gate at ~`:87`
   asserts `await expect(deliveriesLink).toHaveAttribute("aria-current", "false")`. The real `nav_link`
   emits `aria-current={@active && "page"}`, so when Deliveries is INACTIVE the attribute is boolean
   `false`, which Phoenix OMITS entirely (unit-confirmed: `components_test.exs` `refute html =~ aria-current="page"`).
   An `aria-current="false"` assertion fails because the attribute is absent. Change line ~`:87` to:
   `await expect(deliveriesLink).not.toHaveAttribute("aria-current", "page");`
   (equivalently `getByRole("link", { name: "Deliveries", current: false })`). Leave the
   `overviewLink` assertion (`toHaveAttribute("aria-current", "page")`) as-is — Overview IS active.
3. Do not otherwise rewrite the gate bodies — they already assert the correct end-state (Overview has
   `aria-current="page"`; populated Overview has zero `operator-overview-nav`). Keep the `openOverview`
   helper and viewport setup.
4. These gates are NOT yet added to any required CI lane — that arming is Phase 123 (D-13). This task
   only makes them real green tests in the suite.
  </action>
  <verify>
    <automated>cd mailglass_admin && ! grep -q 'aria-current", "false"' e2e/judgment.spec.js && ! grep -q 'test.fixme' e2e/judgment.spec.js && npm run test:operator-browser -- e2e/judgment.spec.js</automated>
  </verify>
  <done>Both judgment gates are real test() (no test.fixme); the aria-current="false" assertion is replaced by not.toHaveAttribute aria-current page; both gates pass against the 119-01 code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan — edits Playwright specs, no application surface |

This plan edits test files only. **No new attack surface** — no auth, input, route, or data boundary
is touched. No package installs (zero new deps; `T-119-SC` accepted — no `[ASSUMED]`/`[SUS]` packages).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-T1 | (none — test-only) | e2e specs | low | accept | Test edits cannot widen runtime attack surface; the gates strengthen regression coverage of the SHELL-01/02 end-state. |
</threat_model>

<verification>
- `cd mailglass_admin && npm run test:operator-browser` is green, including the rewritten VERIF-02 and both flipped judgment gates.
- `grep` confirms `operator-overview-nav` is gone from `operator.spec.js` and `aria-current", "false"` / `test.fixme` are gone from `judgment.spec.js`.
- Full phase gate (run before /gsd-verify-work): `cd mailglass_admin && mix test && npm run test:operator-browser` green; re-shoot the persona cells (DEFECT-REGISTER seam) to confirm only-forward improvement on the 320/375 mobile floors (D-MOBILE-INFODUMP) and light/dark/system parity (D-THEME-PARITY guardrail).
</verification>

<success_criteria>
- D-09 satisfied: the deleted block's VERIF-02 assertion is removed and both judgment gates are real, green tests (with the required line-87 assertion fix).
- The operator browser gate and judgment gate are green on a green-only-forward floor; no inherited v1.13 gate regresses.
</success_criteria>

<output>
Create `.planning/phases/119-app-shell-nav-overview-redesign/119-02-SUMMARY.md` when done.
</output>
