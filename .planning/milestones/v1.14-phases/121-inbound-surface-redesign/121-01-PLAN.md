---
phase: 121-inbound-surface-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
  - mailglass_admin/test/mailglass_admin/voice_test.exs
autonomous: true
requirements: [INB-01]

must_haves:
  truths:
    - "On a genuine no-data Inbound view (no records, no active filters, no filter errors), the render tree is a SINGLE calm pane only: inbound-empty-truly + orientation strip (surface=:inbound). The inbound-filters toolbar, the 'Open record' CTA, the Inbound.Overview health strip, and the entire inbound-master-detail grid (and therefore the 'Select an InboundMessage…' helper nested inside it) are ALL withheld (D-02)."
    - "On a no-match Inbound view (no records, filters active OR tenant has inbound history), the inbound-filters toolbar IS rendered (Clear-filters escape preserved), the health strip and master-detail grid render per existing behavior, and the orientation strip is NOT rendered (D-03)."
    - "On a populated Inbound view (records present), the inbound-filters toolbar IS rendered, the health strip and master-detail grid render, and the orientation strip is NOT rendered — it is removed from the populated-but-unselected detail column is_nil(@detail) branch (D-04)."
    - "The populated-but-unselected detail column still renders the 'Select an InboundMessage to inspect its Mailbox routing…' inbound-empty-detail helper (D-04 — proven positively, not just removed from no-data)."
    - "The genuine no-data state exposes no tenant-scope-widening control; the inbound-filters toolbar (the only scope-widening vector) is withheld there (D-05)."
    - "records_list.ex genuine no-data body reads 'No InboundMessages have been recorded yet.' (D-07 noun-discipline fix); the no-match body 'No records match the current filters.' is unchanged."
    - "RecordsList data_state (:error / :permission_denied / :stale) is reachable from inbound_live.ex — the attr is passed from the LiveView, no longer dead code (D-09)."
  artifacts:
    - "mailglass_admin/lib/mailglass_admin/inbound_live.ex (else-branch wrapped in a top-level cond: genuine-no-data renders single-calm-pane only; no-match/populated render filters toolbar + health strip + master-detail; orientation strip removed from is_nil(@detail) detail column; data_state wired through to records_list)"
    - "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (empty_body(:truly_empty) noun fix to 'No InboundMessages have been recorded yet.')"
    - "mailglass_admin/test/mailglass_admin/inbound_live_test.exs (orientation-on-populated assertion removed/relocated; genuine-no-data + populated-unselected assertions corrected; D-07 copy string updated)"
    - "mailglass_admin/test/mailglass_admin/voice_test.exs (LD-12 orientation-tip assertion relocated off the no-match mount to a genuine-no-data mount or component render)"
  key_links:
    - "inbound_live.ex else-branch (~382-533) reads empty_state_for/2 truth (@records == [] and not filters_active?(@filter_params) and @filter_errors == %{}) to discriminate no-data vs no-match vs populated — NO new flag"
    - "inbound_live.ex genuine-no-data branch renders ONLY inbound-empty-truly (RecordsList records={[]} empty_state={:truly_empty}) + Shell.orientation_strip surface={:inbound}; it withholds the inbound-filters section, the Overview health strip, and the inbound-master-detail grid (and therefore inbound-empty-detail) entirely"
    - "inbound_live.ex passes data_state={...} into RecordsList.records_list so the existing error/permission_denied/stale branches (records_list.ex:43-73) become reachable"
---

<objective>
Apply the shipped Phase 120 empty-pane-only IA pattern (operator_live.ex:488-522) verbatim to the Inbound
branch of `inbound_live.ex`. Wrap the `else` branch (~382-533, inside the `@tenant_state in [:select_required,
:none]` else) in a top-level `cond` driven by the EXISTING `empty_state_for/2` truth — never a new flag. In
genuine no-data (`@records == [] and not filters_active?(@filter_params) and @filter_errors == %{}`) render a
SINGLE calm pane only — `inbound-empty-truly` (RecordsList with `records={[]}` `empty_state={:truly_empty}`)
+ `Shell.orientation_strip surface={:inbound}` — and WITHHOLD the `inbound-filters` toolbar, the "Open record"
CTA, the `Inbound.Overview` health strip, and the entire `inbound-master-detail` grid (and therefore the
`inbound-empty-detail` "Select an InboundMessage…" helper, which lives inside that grid) per D-02/D-05. In
no-match and populated states, render the filters toolbar + health strip + master-detail per existing behavior
(D-03), with the orientation strip removed from the populated-but-unselected detail column `is_nil(@detail)`
branch (D-04) and the `inbound-empty-detail` helper RETAINED in that column (D-04).

Also: wire the dormant `data_state` attr through to `RecordsList.records_list` so the existing
error/permission_denied/stale branches (records_list.ex:43-73) become reachable from the LiveView (D-09 —
net-new wiring, not a copy of a shipped call-site; neither operator_live.ex nor inbound_live.ex passes
data_state today). Apply the one-line noun-discipline copy fix at records_list.ex (D-07). Update the
co-located ExUnit (`inbound_live_test.exs`) AND `voice_test.exs` in the same plan so the assertions that go RED
the instant the strip becomes empty-pane-only are corrected before they break (D-15, Pitfall-2 same-phase
paired-test rule).

Purpose: Convert the Inbound surface — literally the as-built "before" picture of Deliveries (orientation
strip always-visible below a populated table; no single-calm-pane no-data gate; dormant data_state) — into the
streamlined, least-surprise, three-state IA that 120 shipped for Deliveries.

Output: Render-condition-gated Inbound branch + data_state wiring + D-07 copy fix + corrected ExUnit/voice
assertions. Zero new components, zero new tokens, zero new Tailwind classes (render-condition wrappers reuse
classes already in the committed bundle — D-18, no mix assets.build).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-inbound-surface-redesign/121-CONTEXT.md
@.planning/phases/121-inbound-surface-redesign/121-PATTERNS.md
@.planning/phases/121-inbound-surface-redesign/121-UI-SPEC.md

# The shipped Phase 120 Deliveries shape to mirror (the no-data/populated cond split)
@mailglass_admin/lib/mailglass_admin/operator_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap the Inbound else-branch in the no-data/no-match/populated cond + relocate the orientation strip (D-01..D-06, D-08)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (render/1 ~345-536; the else-branch 382-533; filters section 383-419; health strip 421-426; master-detail 428-524; the redundant orientation strip at the is_nil(@detail) branch :483; the inbound-empty-detail helper 484-491; empty_state_for/2 659-670; filters_active?/1 672-685; tenant_has_inbound_history? 687-702)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex:488-615 (the SHIPPED Deliveries cond split to mirror verbatim: genuine-no-data 490-509, true-> branch 510 onward, the is_nil(@selected_delivery) branch 602-615 which has NO orientation strip)
    - .planning/phases/121-inbound-surface-redesign/121-PATTERNS.md (Pattern Assignments: inbound_live.ex render else-branch → top-level cond)
  </read_first>
  <action>
    In inbound_live.ex render/1, wrap the entire `<% else %>` body (the `inbound-filters` section through the
    ReplayModal seam, ~383-532) in a top-level `<%= cond do %>` exactly mirroring operator_live.ex:489-522.

    Branch 1 (genuine no-data), guard `@records == [] and not filters_active?(@filter_params) and @filter_errors == %{}`:
    render ONLY (a) a `<section data-testid="inbound-deliveries-empty-pane" class="card min-w-0 rounded-box border border-base-300 bg-base-200 p-0">` wrapping `<RecordsList.records_list records={[]} page_meta={@records_page_meta} previous_page_path={...} next_page_path={...} selected_record={nil} empty_state={:truly_empty} />` (mirror the operator-deliveries-empty-pane section at operator_live.ex:496-508, using the existing inbound pagination_path call args from :454-455), then (b) `<MailglassAdmin.Operator.Shell.orientation_strip surface={:inbound} />`. Keep the `@filter_errors == %{}` guard so an in-flight invalid filter is NOT misclassified as no-data (the 120 trap at operator_live.ex:490).

    Branch 2 (`<% true -> %>`): the CURRENT body — the `inbound-filters` section (383-419), the health-strip div (421-426, `Overview.overview summary={@inbound_summary}`), and the `inbound-master-detail` grid (428-524) — moved verbatim under this branch. Inside the detail column's nested cond, DELETE the line `<MailglassAdmin.Operator.Shell.orientation_strip surface={:inbound} />` from the `is_nil(@detail) ->` branch (currently :483) so the populated-but-unselected view no longer shows the strip (D-04). KEEP the `inbound-empty-detail` "Select an InboundMessage to inspect its Mailbox routing…" helper div (484-491) intact — it is the correct column-fill affordance, not orientation. Mirror operator_live.ex:602-615 which keeps only the helper, no strip.

    Leave the ReplayModal focus span + `<ReplayModal.replay_modal .../>` (527-532) where they are (inside the `true ->` branch, since the modal only opens on a selected record). Do NOT touch the orientation_strip component copy in shell.ex — only the render CONDITION changes (D-08). Do NOT push gating into records_list.ex — it already renders the empty PANE; the toolbar/health-strip/grid live in inbound_live.ex. Reuse only classes already in the committed app.css (Deliveries uses operator-deliveries-empty-pane with identical classes) — add NO new Tailwind utility (D-18); do NOT run mix assets.build.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c "orientation_strip surface={:inbound}" lib/mailglass_admin/inbound_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "orientation_strip surface={:inbound}" lib/mailglass_admin/inbound_live.ex` returns exactly 1 (the strip now renders only in the genuine-no-data branch; removed from the is_nil(@detail) branch).
    - `grep -c "data-testid=\"inbound-empty-detail\"" lib/mailglass_admin/inbound_live.ex` returns 1 (the "Select an InboundMessage…" helper is retained, D-04).
    - `grep -c "cond do" lib/mailglass_admin/inbound_live.ex` increases by 1 vs. before (a new top-level cond wraps the else-branch).
    - The genuine-no-data guard string `@records == [] and not filters_active?(@filter_params) and @filter_errors == %{}` is present verbatim in inbound_live.ex.
    - `mix compile --warnings-as-errors` exits 0.
    - `git diff --stat mailglass_admin/priv/static/app.css` shows no change (no assets.build run — D-18).
  </acceptance_criteria>
  <done>The Inbound else-branch is a top-level cond: genuine-no-data renders inbound-deliveries-empty-pane + orientation strip only; the true-> branch renders filters toolbar + health strip + master-detail; the orientation strip is removed from the is_nil(@detail) detail column; the inbound-empty-detail helper is retained; compile is clean; app.css unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the dormant data_state through to RecordsList + apply the D-07 noun copy fix</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/records_list.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (the data_state attr 43-45; the error/permission_denied/stale/empty cond 55-102; empty_body/1 359-367 — the :truly_empty clause at 364-365 is the D-07 target)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (the two RecordsList.records_list call-sites: the no-data pane from Task 1 and the populated list card ~451-458; assign_inbound_state/3 ~543-567 and clear_surface_state/1 ~574-586 where socket assigns are set)
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs:1098-1160 (the component-level data_state tests proving error/permission_denied/stale already work — confirming D-09 is wiring, not new UI)
  </read_first>
  <action>
    D-07 copy fix: in records_list.ex change `empty_body(:truly_empty)` (currently :364-365) from
    "No records have been recorded yet." to "No InboundMessages have been recorded yet." (brandbook noun
    discipline — the card header already says "Recent InboundMessages"). Leave `empty_body(:filtered)` ("No
    records match the current filters.") and `empty_body(:no_tenant)` unchanged.

    D-09 wiring: pass a `data_state` value into both `RecordsList.records_list` call-sites in inbound_live.ex so
    the existing error/permission_denied/stale branches (records_list.ex:55-73) become reachable. Add a
    `@data_state` assign to the socket (default `nil`, set in assign_inbound_state/3 and reset to `nil` in
    clear_surface_state/1) and thread it through as `data_state={@data_state}` on the populated list-card
    call-site. Derive its value from the existing failure signals already in scope: route a load/authorization
    failure to `:error` / `:permission_denied` consistent with how `@detail_error` is already computed
    (detail_error_for/2). Do NOT invent a new error-detection path or a new tenant query — reuse the signals the
    LiveView already has; `nil` remains the normal flow (records or the legacy empty branches render). This is
    wiring an existing primitive, NOT building new error UI (the data-state-error/permission-denied/stale panes
    and their copy already exist and are tested). The genuine-no-data pane from Task 1 passes
    `empty_state={:truly_empty}` (data_state stays nil there so the empty branch renders).

    Verify-don't-rebuild: confirm (do not re-author) that responsive table↔card duality, mask_recipient,
    format_datetime(nil)→"Pending", and truncate+title already live in records_list.ex. Add NO new Tailwind
    class; do NOT run mix assets.build (D-18).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c "No InboundMessages have been recorded yet." lib/mailglass_admin/inbound/records_list.ex && grep -c "data_state=" lib/mailglass_admin/inbound_live.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "No InboundMessages have been recorded yet." lib/mailglass_admin/inbound/records_list.ex` returns 1.
    - `grep -c "No records have been recorded yet." lib/mailglass_admin/inbound/records_list.ex` returns 0 (the old noun-drift string is gone).
    - `grep "No records match the current filters." lib/mailglass_admin/inbound/records_list.ex` still present (no-match body unchanged).
    - `grep -c "data_state=" lib/mailglass_admin/inbound_live.ex` is at least 1 (the attr is passed from the LiveView; previously 0).
    - `mix compile --warnings-as-errors` exits 0.
    - `git diff --stat mailglass_admin/priv/static/app.css` shows no change.
  </acceptance_criteria>
  <done>records_list.ex genuine-no-data body uses the InboundMessage noun; data_state is wired from inbound_live.ex into RecordsList so error/permission_denied/stale are reachable; no-match copy unchanged; compile clean; app.css untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Same-phase paired ExUnit + voice_test updates (D-15 Pitfall-2 trap)</name>
  <files>mailglass_admin/test/mailglass_admin/inbound_live_test.exs, mailglass_admin/test/mailglass_admin/voice_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs:53-113 (the V5 no-selection test at 53-75 asserts inbound-orientation visible on a POPULATED seed_matched! view at :74 — goes RED; the truly-empty test at 99-113 asserts clear_filters_count==1 and the D-07 copy string at :108-109)
    - mailglass_admin/test/mailglass_admin/voice_test.exs:133-159 (the canonical COPY-LD test mounts a NO-MATCH view via provider=no-such-provider and asserts the LD-12 orientation tip at :146 — goes RED because the strip is now genuine-no-data only; LD-16 helper :151 survives because no-match keeps the master-detail grid)
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs:1455+ (the clear_filters_count/1 helper)
  </read_first>
  <behavior>
    - Test 1 (populated-unselected, inbound_live_test.exs ~53-75): after seed_matched! (a POPULATED view), inbound-orientation MUST be ABSENT (count 0 / refute), the inbound-empty-detail "Select an InboundMessage…" helper MUST still be present (D-04 positive proof), inbound-master-detail present.
    - Test 2 (genuine no-data, inbound_live_test.exs ~99-113): a tenant with no inbound history asserts the D-07 body "No InboundMessages have been recorded yet."; the inbound-filters toolbar is WITHHELD (assert no `data-testid="inbound-filters"` toolbar in the no-data render); inbound-orientation IS present (count 1). Adjust the clear_filters_count expectation to match the withheld toolbar (the toolbar Clear-filters button is gone in genuine no-data; the in-pane reset is filtered-only, so the genuine-no-data count is 0).
    - Test 3 (voice_test.exs ~133-159): the LD-12 orientation tip is asserted against a genuine-no-data mount (no provider filter, a tenant with no history) OR via render_component(&Shell.orientation_strip/1, surface: :inbound) — NOT the no-match mount; keep LD-16 (select prompt) and LD-03 (filtered-empty body) asserted against the surfaces where they legitimately render.
  </behavior>
  <action>
    Update inbound_live_test.exs and voice_test.exs so the assertions that go RED under the Task-1 render
    conditions are corrected IN THIS PHASE (D-15). In the V5 no-selection test (~53-75): remove the line
    `assert html =~ ~s(data-testid="inbound-orientation")` (:74) and replace it with a positive proof that the
    populated-but-unselected view does NOT render the strip (`refute html =~ ~s(data-testid="inbound-orientation")`)
    while the `inbound-empty-detail` helper IS still present (the existing :67-68 select-prompt assertion already
    covers this — keep it). In the truly-empty test (~99-113): update the body assertion to the D-07 string "No
    InboundMessages have been recorded yet."; assert `inbound-orientation` is present (count 1) and the
    `inbound-filters` toolbar section is withheld; correct the `clear_filters_count(html)` expectation from 1 to
    the value matching the withheld toolbar (the genuine-no-data render no longer has the toolbar Clear-filters
    button). In voice_test.exs canonical-COPY-LD test (~133-159): relocate the LD-12 orientation-tip assertion off
    the no-match `provider=no-such-provider` mount — either mount a genuine-no-data view (no provider filter, a
    tenant with no history) for LD-12, or assert LD-12 via `render_component(&Shell.orientation_strip/1, surface: :inbound)`;
    keep LD-16 and LD-03 on a no-match mount where they legitimately render. Do NOT weaken any masking/no-leak
    assertion. Run the inbound suite + voice_test to confirm green.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs test/mailglass_admin/voice_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mailglass_admin/inbound_live_test.exs test/mailglass_admin/voice_test.exs` reports 0 failures.
    - `grep -c "data-testid=\"inbound-orientation\"" test/mailglass_admin/inbound_live_test.exs` no longer asserts presence on the populated seed_matched! view (the line is removed/inverted to refute on that view).
    - inbound_live_test.exs asserts "No InboundMessages have been recorded yet." (the D-07 string) in the truly-empty test.
    - voice_test.exs no longer asserts the LD-12 orientation tip on the `provider=no-such-provider` no-match mount; LD-12 is asserted via a genuine-no-data mount or render_component.
    - No masking/no-leak/banned-word assertion is weakened or removed.
  </acceptance_criteria>
  <done>The inbound ExUnit + voice_test assertions match the Task-1 render conditions (strip empty-pane-only, D-07 copy, toolbar withheld in no-data); both files pass with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → tenant-scoped Inbound data | The filters toolbar is the only control that can change which tenant's data is in scope; withholding it in genuine no-data removes the scope-widening vector |
| LiveView render condition → empty-state IA | A misclassified state (e.g. in-flight invalid filter treated as no-data) could withhold the recovery toolbar an operator needs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-01 | Elevation of Privilege | inbound_live.ex genuine-no-data branch (D-05) | high | mitigate | Withhold the inbound-filters toolbar in genuine no-data so the empty state exposes NO tenant-scope-widening control; FiltersForm.fields exposes only outcome/window (never a tenant control); :no_tenant handled upstream by tenant_selector. Asserting guard: the Plan 04 judgment gate `inbound-filters` count 0 on no-data + the ExUnit toolbar-withheld assertion in Task 3. |
| T-121-02 | Tampering | empty_state_for/2 discriminator (D-01) | medium | mitigate | Keep the `@filter_errors == %{}` guard so an in-flight invalid filter is not misclassified as no-data (would wrongly withhold the recovery toolbar). Asserting guard: Task 1 verbatim guard string + Task 3 no-match keeps-toolbar coverage. |
| T-121-03 | Information Disclosure | RecordsList no-leak (existing) | medium | accept | Cross-tenant id/recipient leak is already refuted by existing inbound_live_test.exs assertions (V1 isolation, blank-tenant) — Task 3 must not weaken them. No new disclosure surface is introduced by render-condition gating. |
</threat_model>

<verification>
- `cd mailglass_admin && mix compile --warnings-as-errors` exits 0.
- `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs test/mailglass_admin/voice_test.exs` reports 0 failures.
- `git diff --stat mailglass_admin/priv/static/app.css` shows no change (no assets.build — D-18).
- `grep -c "orientation_strip surface={:inbound}" mailglass_admin/lib/mailglass_admin/inbound_live.ex` returns 1.
</verification>

<success_criteria>
The Inbound surface renders three streamlined IA states (genuine-no-data single calm pane; no-match toolbar+grid; populated toolbar+grid) driven by the existing empty_state_for/2 truth; the orientation strip is empty-pane-only; data_state is wired and reachable; the D-07 noun fix is applied; ExUnit + voice_test pass with the corrected paired assertions; the committed app.css is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/121-inbound-surface-redesign/121-01-SUMMARY.md` when done.
</output>
