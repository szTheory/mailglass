---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/router.ex
  - mailglass_inbound/test/mailglass_inbound/router_test.exs
  - mailglass_inbound/test/mailglass_inbound/rate_limiter_test.exs
autonomous: true
requirements: [INB-06]
must_haves:
  truths:
    - "Public route/2 declarations accept only recursively validated literal mailbox/options AST and preserve declaration order/source metadata."
    - "Variables, calls, interpolation, captures, and side-effecting quoted expressions fail at compile time without executing."
    - "The Phase 156 inbound rate-limit table still proves bounded cardinality, expiry, fail-closed overflow, and restart ownership per D-08."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/router.ex"
      provides: "Literal-only route declaration decoder"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/router.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/router/route.ex"
      via: "decoded literals pass through existing value/NimbleOptions validation"
      pattern: "validate_route_opts"
---

<objective>
Close compile-time route execution while pinning the already-established inbound ETS lifecycle constraint per D-07 and D-08.

Purpose: Provider configuration must be data, never executable caller code, and Phase 157 must retain Phase 156's attacker-key bounds.
Output: Literal AST decoder, adversarial macro tests, and inbound rate-limit non-regression proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-PATTERNS.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-06-SUMMARY.md
@mailglass_inbound/lib/mailglass_inbound/router.ex
@mailglass_inbound/lib/mailglass_inbound/router/route.ex
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Compile one literal route without evaluating caller code</name>
  <files>mailglass_inbound/lib/mailglass_inbound/router.ex, mailglass_inbound/test/mailglass_inbound/router_test.exs</files>
  <behavior>
    - INB-06/D-07: literal mailbox aliases, keyword lists, strings, nil, lists/tuples of literals, and literal regex sigils compile to the same Route values/order/source metadata.
    - Variables, local/remote calls, interpolation, captures, and arbitrary quoted expressions raise a precise compile-time argument error.
    - A side-effect sentinel remains untouched when rejected AST is compiled.
  </behavior>
  <action>Write compile-string acceptance and side-effect rejection tests first. Replace unrestricted caller-AST evaluation with a recursive literal decoder that explicitly expands and validates a mailbox alias/module and recognizes only the allowed literal shapes, then feed decoded values through the existing route option/matcher validation. Preserve public declaration syntax, ordering, file/line metadata, matcher semantics, and generated-router compatibility.</action>
  <verify><automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/router_test.exs test/mailglass_inbound/router/matcher_test.exs --warnings-as-errors</automated></verify>
  <done>All valid public declarations remain equivalent and every executable AST class fails before side effects.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Pin inbound rate-limit lifecycle bounds</name>
  <files>mailglass_inbound/test/mailglass_inbound/rate_limiter_test.exs</files>
  <behavior>
    - D-08: unique inbound keys hit the configured cap, live-table overflow denies, idle expiry admits, and owner restart recreates the table.
    - No new ETS table or supervisor is introduced by Phase 157.
  </behavior>
  <action>Add a focused Phase 157 regression around the shipped Phase 156 owner/config seam only where coverage is absent. Keep deterministic injected clock/barrier assertions and do not broaden production code when the established behavior already meets D-08.</action>
  <verify><automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/rate_limiter_test.exs --warnings-as-errors</automated></verify>
  <done>The literal-router change cannot regress the bounded inbound attacker-key state inherited from Phase 156.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Host source AST → route macro | Compile-time input can otherwise execute arbitrary code. |
| Request key → inbound ETS | Unique attacker keys consume memory. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-09 | Elevation of Privilege | route macro | high | mitigate | Literal whitelist and side-effect negative controls. |
| T-157-10 | Denial of Service | inbound rate limiter | high | mitigate | Retain bounded owner admission/expiry/restart proof. |
</threat_model>

<verification>Run both focused suites, inbound support contract, formatter, and diff check.</verification>
<success_criteria>INB-06 is complete, D-08 remains true for inbound rate state, and no public route syntax or admin behavior changes.</success_criteria>
<output>Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-04-SUMMARY.md` when done.</output>
