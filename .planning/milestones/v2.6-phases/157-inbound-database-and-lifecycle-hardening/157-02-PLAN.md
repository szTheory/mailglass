---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 02
type: execute
wave: 2
depends_on: [157-01]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher/retry.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex
  - mailglass_inbound/test/mailglass_inbound/s3_fetcher_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs
autonomous: true
requirements: [INB-02, INB-03]
must_haves:
  truths:
    - "SES checks S3 object metadata against a configurable 40 MiB default before requesting the body and rejects oversized adapter bodies defensively."
    - "Only closed transient S3 outcomes retry; authorization, missing, oversized, malformed, and unknown exhausted outcomes retain permanent s3_fetch_failed classification."
    - "S3 remains an optional dependency and the no-optional compilation path stays green."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex"
      provides: "Metadata-first bounded retrieval contract"
    - path: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher/retry.ex"
      provides: "Closed finite retry classification"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex"
      via: "authenticated verified request resolves S3 content after tenant scope"
      pattern: "S3Fetcher"
---

<objective>
Make authenticated SES S3 resolution metadata-first, size-bounded, and classification-honest per D-04 and D-05.

Purpose: Prevent oversized materialization and false transient acknowledgements while retaining the existing finite retry seam.
Output: Extended optional adapter contract, 40 MiB default gate, closed retry matrix, and focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-RESEARCH.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-01-SUMMARY.md
@mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex
@mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex
@mailglass_inbound/lib/mailglass_inbound/s3_fetcher/retry.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one oversized SES object before body retrieval</name>
  <files>mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex, mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex, mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex, mailglass_inbound/test/mailglass_inbound/s3_fetcher_test.exs</files>
  <behavior>
    - INB-02/D-04: a HEAD result above 40 MiB causes a typed permanent rejection and the fake records zero GET calls.
    - The configurable byte limit is validated and a body exceeding the declared/allowed size is rejected after GET as defense in depth.
    - Missing optional ExAws modules compile cleanly and return the existing typed unavailable behavior.
  </behavior>
  <action>Write the call-order/count and exact-boundary tests first. Extend the fetcher/gateway contract with metadata retrieval, implement ExAws HeadObject without eager body loading, validate the configurable maximum with a 40 MiB default, and gate GET on content length. Retain the post-fetch byte-size guard for dishonest/custom adapters. Preserve the existing optional-dependency checks and test injection seams.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/s3_fetcher_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Oversized metadata prevents body retrieval, exact-limit content succeeds, and an oversized returned body fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve closed transient and permanent outcomes through exhaustion</name>
  <files>mailglass_inbound/lib/mailglass_inbound/s3_fetcher/retry.ex, mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex, mailglass_inbound/test/mailglass_inbound/s3_fetcher_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs</files>
  <behavior>
    - INB-03/D-05: transport, timeout, throttling, 5xx, and known not-ready outcomes retry only within the configured attempt budget.
    - Authorization, missing, oversized, malformed, and unknown outcomes are permanent and do not retry.
    - Exhausting an unknown outcome returns the permanent s3_fetch_failed family rather than a not-ready label.
  </behavior>
  <action>Build a table-driven red test matrix with injected attempt acknowledgements, then centralize S3 classification into a finite mapping used before and after retry. Preserve finite attempts/backoff injection and PII-safe outer errors. Ensure the verified-request content-resolution stage receives only closed typed outcomes that the Plug can route to transient response or durable terminal evidence.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/s3_fetcher_test.exs test/mailglass_inbound/ingress/ses_provider_test.exs --warnings-as-errors &amp;&amp; mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <done>The complete S3 outcome matrix proves finite retry and stable permanent classification, including unknown exhaustion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated SES envelope → S3 | Provider-controlled bucket/key can trigger remote work and allocation. |
| Optional adapter → inbound pipeline | Adapter metadata/body and errors are not trusted to be bounded or classified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-04 | Denial of Service | S3 retrieval | high | mitigate | HEAD-before-GET, 40 MiB default, validated override, and post-fetch byte guard. |
| T-157-05 | Repudiation | retry classification | high | mitigate | Closed table-driven classification and permanent unknown-exhaustion proof. |
</threat_model>

<verification>
Run the focused S3/SES tests and inbound no-optional compile; finish with formatting and diff checks.
</verification>

<success_criteria>
INB-02 and INB-03 hold without AWS credentials, public configuration remains additive, and no admin/operator UI is touched.
</success_criteria>

<output>
Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-02-SUMMARY.md` when done.
</output>
