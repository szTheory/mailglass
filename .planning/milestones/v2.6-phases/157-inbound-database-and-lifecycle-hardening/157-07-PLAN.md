---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 07
type: execute
wave: 2
depends_on: [157-06]
files_modified:
  - lib/mailglass/suppression/resync.ex
  - test/mailglass/suppression/resync_test.exs
  - test/mix/tasks/mailglass.suppressions.resync_test.exs
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "Suppression resync keyset-pages by occurred_at/id, deduplicates each page, bulk-loads existing state, and upserts bounded chunks."
    - "Dry-run, candidate, existing, missing, duplicate, and error totals retain documented meaning across page boundaries."
    - "A real write error halts honestly without loading the full time window or silently changing prior committed pages."
  artifacts:
    - path: "lib/mailglass/suppression/resync.ex"
      provides: "Bounded keyset/bulk suppression ledger rebuild"
  key_links:
    - from: "lib/mailglass/suppression/resync.ex"
      to: "lib/mailglass/suppression_store.ex"
      via: "each page uses the positional bulk capability from Plan 06"
      pattern: "check_many|bulk"
---

<objective>
Convert suppression resync into a bounded, resumable database flow per D-12.

Purpose: Production windows must not load or query one row at a time while observable totals and failure semantics stay stable.
Output: Keyset paging, page dedupe, bulk existing reads, bounded upserts, integration/task proof.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-06-SUMMARY.md
@lib/mailglass/suppression/resync.ex
@lib/mailglass/suppression_store.ex
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Resync one multi-page ledger with bounded bulk work</name>
  <files>lib/mailglass/suppression/resync.ex, test/mailglass/suppression/resync_test.exs</files>
  <behavior>
    - DATA-05/D-12: stable `(occurred_at, id)` cursors visit equal-timestamp events exactly once across small pages.
    - Candidate keys dedupe before one bounded bulk read and bounded conflict-safe upsert per chunk.
    - Dry-run and write mode preserve existing result-map totals, candidate summaries, tenant scope, duplicate treatment, and error behavior.
  </behavior>
  <action>Write forced-small-page Postgres tests first with equal timestamps, duplicates straddling pages, mixed existing/missing rows, dry-run, and injected write failure. Replace whole-window load/per-row reads with stable keyset pagination, page-local candidate dedupe, Plan 06 bulk capability, and bounded conflict-safe upserts. Accumulate existing public result fields without retaining all rows; halt on a real write error and report it through the existing shape.</action>
  <verify><automated>mix test test/mailglass/suppression/resync_test.exs --warnings-as-errors</automated></verify>
  <done>Multi-page resync converges with bounded query/write counts and unchanged totals in dry-run and write modes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Preserve the public resync Mix task contract</name>
  <files>test/mix/tasks/mailglass.suppressions.resync_test.exs</files>
  <behavior>
    - Existing CLI options/output/exit semantics invoke the bounded implementation.
    - A multi-page run and write failure remain visible and deterministic.
  </behavior>
  <action>Extend the existing task contract tests to exercise bounded page options through the public task without changing its flags or output vocabulary. Keep all implementation in the service module and the Mix task thin.</action>
  <verify><automated>mix test test/mix/tasks/mailglass.suppressions.resync_test.exs --warnings-as-errors</automated></verify>
  <done>The public task transparently uses the bounded resync and reports success/failure exactly as before.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Historical ledger → resync worker | Large tenant windows can amplify memory and queries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-16 | Denial of Service | resync | high | mitigate | Keyset pages, dedupe, bounded reads/upserts, count assertions. |
| T-157-17 | Repudiation | resync totals/errors | medium | mitigate | Cross-page deterministic totals and fail-visible write tests. |
</threat_model>
<verification>Run service/task tests, formatter, and diff check.</verification>
<success_criteria>DATA-05 is complete with bounded memory/query/write work and unchanged public semantics.</success_criteria>
<output>Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-07-SUMMARY.md` when done.</output>
