---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/webhook/providers/ses/cert_cache.ex
  - lib/mailglass/webhook/providers/ses/cert_cache/table_owner.ex
  - lib/mailglass/webhook/providers/ses.ex
  - lib/mailglass/webhook/providers/mailgun_replay_cache.ex
  - lib/mailglass/webhook/providers/mailgun_replay_cache/table_owner.ex
  - test/mailglass/webhook/providers/ses/cert_cache_test.exs
  - test/mailglass/webhook/providers/ses_test.exs
  - test/mailglass/webhook/providers/mailgun_test.exs
autonomous: true
requirements: [INB-01, INB-07]
must_haves:
  truths:
    - "A cold burst for one trusted SES certificate URL performs one bounded fetch, while distinct misses cannot exceed a small global in-flight cap."
    - "Certificate positive/negative entries and Mailgun replay tokens expire and cannot exceed explicit cardinality limits under unique attacker keys."
    - "SNS trust-policy validation precedes every network request, which has finite connect/request timeout and response-byte limits."
  artifacts:
    - path: "lib/mailglass/webhook/providers/ses/cert_cache.ex"
      provides: "Shared bounded SES certificate retrieval service for core and inbound"
    - path: "lib/mailglass/webhook/providers/mailgun_replay_cache/table_owner.ex"
      provides: "Owned finite replay-token lifecycle"
  key_links:
    - from: "lib/mailglass/webhook/providers/ses.ex"
      to: "lib/mailglass/webhook/providers/ses/cert_cache.ex"
      via: "all certificate misses pass through trust-checked bounded single-flight"
      pattern: "CertCache"
---

<objective>
Bound every shared pre-verification certificate and replay-cache resource per D-03 and D-08.

Purpose: Unique untrusted keys and cold bursts must not create unbounded ETS, network, response, or concurrent work.
Output: Shared bounded certificate service, finite Mailgun replay cache, deterministic concurrency/lifecycle tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-RESEARCH.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-06-SUMMARY.md
@lib/mailglass/webhook/providers/ses/cert_cache.ex
@lib/mailglass/webhook/providers/ses/cert_cache/table_owner.ex
@lib/mailglass/webhook/providers/ses/trust_policy.ex
@lib/mailglass/webhook/providers/mailgun_replay_cache.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verify a cold SES burst through one bounded shared fetch</name>
  <reversibility rating="costly">D-03 changes the shared core-owned certificate service used by both independently released packages while retaining its API.</reversibility>
  <files>lib/mailglass/webhook/providers/ses/cert_cache.ex, lib/mailglass/webhook/providers/ses/cert_cache/table_owner.ex, lib/mailglass/webhook/providers/ses.ex, test/mailglass/webhook/providers/ses/cert_cache_test.exs, test/mailglass/webhook/providers/ses_test.exs</files>
  <behavior>
    - INB-01/D-03: simultaneous misses for one validated URL release behind a barrier and cause exactly one HTTP fetch; all waiters receive the same closed result.
    - Distinct URLs cannot exceed configured global in-flight or entry limits; saturation fails closed without extra network calls.
    - Positive and short negative entries expire deterministically; timeout, oversize, malformed PEM, and untrusted URL/path results never become successful cache entries.
  </behavior>
  <action>Add injected HTTP, clock, and release-barrier tests first. Evolve the existing owner into the single-flight coordinator with conservative validated internal defaults for entries, in-flight count, positive/negative TTL, connect/request timeout, and maximum certificate bytes. Keep TrustPolicy ahead of the coordinator/network boundary, retain lazy plus scheduled expiry, and make overflow deterministic/fail closed. Bound response accumulation before PEM parsing. Preserve cache APIs, table supervision, cryptographic verification, telemetry privacy, and inbound reuse of the core service.</action>
  <verify>
    <automated>mix test test/mailglass/webhook/providers/ses/cert_cache_test.exs test/mailglass/webhook/providers/ses_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Cold-burst, global saturation, negative-cache, expiry, timeout, response-limit, and trust-policy tests are deterministic and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound Mailgun replay-token cardinality and expiry</name>
  <files>lib/mailglass/webhook/providers/mailgun_replay_cache.ex, lib/mailglass/webhook/providers/mailgun_replay_cache/table_owner.ex, test/mailglass/webhook/providers/mailgun_test.exs</files>
  <behavior>
    - INB-07/D-08: unique replay tokens cannot grow the owned ETS table beyond its configured cap.
    - Expired tokens are swept/admission-reclaimed; a full active table rejects new tokens without evicting live replay protection.
    - Restart recreates the canonical table and preserves fail-closed replay behavior.
  </behavior>
  <action>Write small-cap, injected-clock, sweep, full-active, and owner-restart tests first. Apply the Phase 156 owner-mediated admission pattern to the existing replay table, retaining its named table/API and token semantics. Configure bounded defaults and deterministic active-table overflow without creating a new supervisor or unowned ETS table.</action>
  <verify>
    <automated>mix test test/mailglass/webhook/providers/mailgun_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Mailgun replay protection has explicit capacity, expiry, overflow, and restart proof while existing outcomes remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SNS URL → HTTPS client | An untrusted envelope attempts to select a certificate network target. |
| Attacker key → shared ETS | Unique URLs/tokens consume node memory and coordination capacity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-06 | Spoofing | SES certificate fetch | critical | mitigate | Existing strict trust policy before bounded retrieval and cryptographic verification. |
| T-157-07 | Denial of Service | cert/replay caches | high | mitigate | Single-flight, global in-flight cap, entry cap, expiry, negative cache, finite bytes/timeouts. |
| T-157-08 | Tampering | replay admission | medium | mitigate | Live tokens are never evicted to admit attacker keys; overflow fails closed. |
</threat_model>

<verification>
Run the focused suites, `mix compile --no-optional-deps --warnings-as-errors`, formatter, and diff check; concurrency tests use barriers, not sleeps.
</verification>

<success_criteria>
INB-01 and the shared-cache portion of INB-07 are bounded under hostile cardinality while core/inbound APIs and package independence remain intact.
</success_criteria>

<output>
Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-03-SUMMARY.md` when done.
</output>
