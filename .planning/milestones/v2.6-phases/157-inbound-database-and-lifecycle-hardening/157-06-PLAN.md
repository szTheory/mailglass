---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/suppression_store.ex
  - lib/mailglass/suppression_store/ecto.ex
  - lib/mailglass/suppression_store/ets.ex
  - lib/mailglass/outbound.ex
  - test/mailglass/suppression_store/ecto_test.exs
  - test/mailglass/suppression_store/ets_test.exs
  - test/mailglass/outbound/preflight_test.exs
  - test/mailglass/outbound/deliver_many_test.exs
autonomous: true
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "Suppression stores may return positional bulk results while legacy check/2-only stores remain compatible through bounded chunks."
    - "Batch delivery deduplicates lookup keys, performs bounded store work, and maps results back to exact input order/outcomes."
    - "Ecto suppression queries compare normalized typed parameters to bare indexed columns with tenant/expiry semantics unchanged."
  artifacts:
    - path: "lib/mailglass/suppression_store.ex"
      provides: "Optional positional bulk capability and bounded fallback"
    - path: "lib/mailglass/outbound.ex"
      provides: "Order-preserving bounded batch suppression preflight"
  key_links:
    - from: "lib/mailglass/outbound.ex"
      to: "lib/mailglass/suppression_store.ex"
      via: "unique keys are checked in bounded chunks and expanded positionally"
      pattern: "check_many|bulk"
---

<objective>
Make suppression preflight bulk-capable and index-friendly without breaking stores or batch outcomes per D-10 and D-11.

Purpose: Eliminate one-query-per-message amplification while preserving every public result and input position.
Output: Optional bulk contract/fallback, Ecto/ETS implementations, bounded preflight, conformance tests.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-PATTERNS.md
@lib/mailglass/suppression_store.ex
@lib/mailglass/suppression_store/ecto.ex
@lib/mailglass/outbound.ex
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Resolve one mixed suppression batch positionally</name>
  <reversibility rating="costly">D-11 adds an optional public behaviour capability, but existing callbacks and stores remain valid.</reversibility>
  <files>lib/mailglass/suppression_store.ex, lib/mailglass/suppression_store/ecto.ex, lib/mailglass/suppression_store/ets.ex, test/mailglass/suppression_store/ecto_test.exs, test/mailglass/suppression_store/ets_test.exs</files>
  <behavior>
    - DATA-03/D-11: bulk-capable stores return one check result per input position, including duplicates and mixed suppressed/error/not-suppressed outcomes.
    - A legacy store exporting only check/2 and record/2 uses bounded chunks and preserves identical positional results.
    - D-10: Ecto direct-column query remains tenant/stream/domain/expiry correct and EXPLAIN/query-AST proof rejects indexed-side function casts.
  </behavior>
  <action>Write shared store conformance tests first. Add an optional callback/capability probe to `Mailglass.SuppressionStore` plus a configurable bounded chunk fallback over `check/2`. Implement native Ecto and ETS bulk paths by deduplicating query keys and reconstructing positional outputs. Normalize/cast parameters before the Ecto query and compare bare indexed address/scope columns; preserve existing results, tenant scope, expiration, and custom-store compatibility.</action>
  <verify><automated>mix test test/mailglass/suppression_store/ecto_test.exs test/mailglass/suppression_store/ets_test.exs --warnings-as-errors</automated></verify>
  <done>Native and legacy stores pass the same positional contract with bounded work and direct indexed predicates.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bound delivery preflight queries without changing outcomes</name>
  <files>lib/mailglass/outbound.ex, test/mailglass/outbound/preflight_test.exs, test/mailglass/outbound/deliver_many_test.exs</files>
  <behavior>
    - DATA-04: duplicate recipient/scope keys are checked once per bounded chunk, then exact single-batch order and per-message result semantics are restored.
    - Mixed success/suppressed/error batches match existing halt/continue and durable-batch behavior.
    - Query/callback count grows by chunks, not messages.
  </behavior>
  <action>Add instrumented-store query-count and parity tests first. Refactor preflight to form stable unique keys, call the capability helper in bounded chunks, and map positional results to original messages without altering error precedence, idempotency keys, durable transaction boundaries, or public return shapes established in Phase 156.</action>
  <verify><automated>mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_many_test.exs --warnings-as-errors</automated></verify>
  <done>Large and duplicate-heavy batches use bounded suppression work and return byte-for-byte/equality-compatible ordered outcomes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Batch recipients → suppression store | Caller-controlled cardinality drives database/cache work. |
| Custom store → core preflight | Third-party implementations may lack the new optional capability. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-14 | Denial of Service | suppression preflight | high | mitigate | Deduplicate, chunk, native bulk query, and query-count tests. |
| T-157-15 | Tampering | positional result mapping | high | mitigate | Position-preserving contract and duplicate/mixed-outcome parity tests. |
</threat_model>
<verification>Run focused stores/preflight/batch tests, root no-optional compile, formatter, and diff check.</verification>
<success_criteria>DATA-03/04 are complete with additive store compatibility, bounded queries, exact outcomes, and no admin/operator changes.</success_criteria>
<output>Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-06-SUMMARY.md` when done.</output>
