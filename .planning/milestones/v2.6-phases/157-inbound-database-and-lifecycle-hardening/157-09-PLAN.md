---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 09
type: execute
wave: 4
depends_on: [157-05, 157-08]
files_modified:
  - scripts/generated_ecto_host_proof.sh
  - test/scripts/generated_ecto_host_proof_test.exs
  - test/mailglass/shipped_migration_divergence_test.exs
  - mailglass_inbound/test/mailglass_inbound/migrations_test.exs
  - docs/migration-policy.md
  - test/mailglass/docs_migration_smoke_test.exs
autonomous: true
requirements: [DATA-08]
must_haves:
  truths:
    - "A generated Phoenix/Ecto/Postgres host upgrades populated core V05 and inbound V01 schemas through generated wrappers, resumes backfill, verifies indexes/raw bytes, and rolls back additive versions."
    - "Generated wrappers carry required transaction attributes and out-of-transaction concurrent-index policy rather than hiding unsafe DDL in package history."
    - "All shipped core V01-V05 and inbound V01 migration bytes match their pre-phase hashes; no proof may pass when the real host path is skipped."
  artifacts:
    - path: "scripts/generated_ecto_host_proof.sh"
      provides: "Generated-host populated upgrade/backfill/rollback proof"
    - path: "test/mailglass/shipped_migration_divergence_test.exs"
      provides: "Byte-immutability contract for shipped migrations"
    - path: "docs/migration-policy.md"
      provides: "Executable future populated-table migration policy"
  key_links:
    - from: "scripts/generated_ecto_host_proof.sh"
      to: "core V06 and inbound V02 generated wrappers"
      via: "Host.Repo migration/rollback against populated prior-version schemas"
      pattern: "upgrade|rollback"
---

<objective>
Prove the forward migration policy against a real populated generated host and freeze shipped history per D-15.

Purpose: Unit DDL tests cannot certify generated-wrapper attributes, resumable data compatibility, concurrent-index execution, or immutable prior migrations.
Output: Expanded generated-host upgrade journey, anti-vacuity/hash contracts, and adopter-facing migration policy.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-RESEARCH.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-05-SUMMARY.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-08-SUMMARY.md
@scripts/generated_ecto_host_proof.sh
@test/scripts/generated_ecto_host_proof_test.exs
@test/mailglass/shipped_migration_divergence_test.exs
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Upgrade a populated generated host through inbound V02 and core V06</name>
  <precondition>PostgreSQL is reachable through the generated-host script's existing allowlisted scratch DATABASE_URL contract.</precondition>
  <files>scripts/generated_ecto_host_proof.sh, test/scripts/generated_ecto_host_proof_test.exs, mailglass_inbound/test/mailglass_inbound/migrations_test.exs</files>
  <behavior>
    - DATA-08/D-15: Host.Repo begins at real core V05/inbound V01 with populated legacy rows, public generators produce fresh upgrade wrappers, and Ecto applies V06/V02.
    - The journey interrupts/resumes bounded SHA-256 backfill, verifies mixed-row dedupe, raw signed body persistence, and expected indexes/query plans, then rolls only additive versions back safely.
    - Wrapper source carries transaction/lock attributes required for concurrent indexes and bounded statements; removing any execute/apply/assertion anchor fails anti-vacuity tests.
  </behavior>
  <action>Extend the existing isolated generated-host proof rather than adding a parallel fake harness. Populate prior-version tables through Host.Repo, run both public upgrade generators with explicit repo/prior versions, assert generated timestamps/source/transaction attributes, migrate, exercise the actual backfill/dedupe/raw-body paths, and roll back to prior anchors without hand-written replacement DDL. Where concurrent indexes require transaction-disabled wrappers, prove their generated attribute and invalid-index recovery/idempotent rerun. Strengthen mutation-based source contracts so skipped generation, execution, backfill, index, or rollback cannot pass.</action>
  <verify><automated>mix test test/scripts/generated_ecto_host_proof_test.exs --warnings-as-errors &amp;&amp; MAILGLASS_PATH="$PWD" bash scripts/generated_ecto_host_proof.sh</automated></verify>
  <done>A real generated host proves populated upgrades, resumable compatibility, index/raw evidence, idempotent rerun, and rollback to V05/V01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze shipped migrations and codify the forward policy</name>
  <files>test/mailglass/shipped_migration_divergence_test.exs, docs/migration-policy.md, test/mailglass/docs_migration_smoke_test.exs</files>
  <behavior>
    - Core V01-V05 and inbound V01 byte hashes match the repository baseline captured before Phase 157 implementation.
    - New populated-table policy requires additive expand/contract, bounded lock/statement timeouts, resumable backfill, concurrent indexes outside transactions, invalid-index recovery, and later contract releases.
    - The guide points to public generators/generated-host proof and preserves package independence/no-admin scope.
  </behavior>
  <action>Before implementation diff can obscure provenance, pin the current shipped module byte hashes in the existing divergence contract and add V06/V02 only to the forward-version catalog, never to the immutable historical baseline. Create the focused migration-policy document with exact operational sequencing, wrapper attributes, recovery/rerun commands, and rollback boundaries exercised by Task 1, and extend the docs migration smoke contract to require it. State explicitly that core and inbound migrations remain independent and that no admin/operator schema or UI participates.</action>
  <verify><automated>mix test test/mailglass/shipped_migration_divergence_test.exs test/scripts/generated_ecto_host_proof_test.exs --warnings-as-errors &amp;&amp; mix test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors</automated></verify>
  <done>Historical migration bytes are enforced and the documented forward policy exactly matches generated-host execution.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Generated wrapper → adopter DB | Forward/rollback DDL executes against populated host storage. |
| Repository edits → shipped history | Historical migration changes could silently diverge installed schemas. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-21 | Tampering | shipped migrations | critical | mitigate | Pre-phase byte hashes and divergence test across both packages. |
| T-157-22 | Denial of Service | populated upgrade | high | mitigate | Bounded timeouts/backfill, concurrent-index wrapper attributes, real-host proof. |
| T-157-23 | Repudiation | generated-host certification | high | mitigate | Mutation-based anti-vacuity anchors for generation, execution, data, index, rollback. |
</threat_model>
<verification>Run the actual Postgres generated-host journey plus contracts/docs tests, both package migration suites, formatter, shell syntax, and diff check.</verification>
<success_criteria>DATA-08 and D-15 are executable in a real host; shipped migrations remain byte-identical; package/public/admin boundaries remain locked.</success_criteria>
<output>Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-09-SUMMARY.md` when done.</output>
