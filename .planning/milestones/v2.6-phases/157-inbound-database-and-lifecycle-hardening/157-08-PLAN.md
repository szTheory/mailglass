---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/migration.ex
  - lib/mailglass/migrations/postgres.ex
  - lib/mailglass/migrations/postgres/v06.ex
  - lib/mailglass/webhook/webhook_event.ex
  - lib/mailglass/webhook/pruner.ex
  - lib/mailglass/webhook/ingest.ex
  - lib/mailglass/webhook/plug.ex
  - test/mailglass/migration_test.exs
  - test/mailglass/webhook/pruner_test.exs
  - test/mailglass/webhook/ingest_test.exs
  - mailglass_inbound/test/mailglass_inbound/internal/prune_test.exs
autonomous: true
requirements: [DATA-06, DATA-07]
must_haves:
  truths:
    - "Core and inbound retention use finite indexed SKIP LOCKED batches, serialize each run, and commit batches independently."
    - "Webhook ingest parses verified JSON once, bulk-loads matching deliveries once per bounded provider batch, and preserves exact signed bytes immutably."
    - "Core V06 adds only forward schema/index support; V01-V05 and inbound shipped V01 remain unchanged."
  artifacts:
    - path: "lib/mailglass/migrations/postgres/v06.ex"
      provides: "Additive raw-body and retention-index expansion"
    - path: "lib/mailglass/webhook/pruner.ex"
      provides: "Serialized bounded core pruning"
    - path: "lib/mailglass/webhook/ingest.ex"
      provides: "Parse-once, bulk-load webhook projection"
  key_links:
    - from: "lib/mailglass/webhook/plug.ex"
      to: "lib/mailglass/webhook/ingest.ex"
      via: "verified decoded value and byte-exact raw body enter ingest together"
      pattern: "ingest_multi"
    - from: "lib/mailglass/webhook/pruner.ex"
      to: "lib/mailglass/migrations/postgres/v06.ex"
      via: "candidate ordering matches additive age/source/status indexes"
      pattern: "SKIP LOCKED"
---

<objective>
Ship the core database lifecycle tracer: bounded indexed retention plus parse-once, bulk-loaded webhook evidence per D-13 and D-14.

Purpose: Stored-data growth must not turn retention or provider batches into unbounded locks, scans, queries, or lossy evidence.
Output: Core V06 expansion, bounded pruner, inbound index proof, exact raw-body field, bulk webhook lookup.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-RESEARCH.md
@lib/mailglass/migrations/postgres/v05.ex
@lib/mailglass/webhook/pruner.ex
@mailglass_inbound/lib/mailglass_inbound/internal/prune.ex
@lib/mailglass/webhook/ingest.ex
@lib/mailglass/webhook/plug.ex

<interfaces>
Append V06 through the existing core migration facade/dispatcher; never alter V01-V05. Preserve `Mailglass.Webhook.Pruner` public and optional-Oban surfaces, telemetry names/counts, `Webhook.Ingest.ingest_multi/3`, provider response semantics, duplicate/no-op/orphan behavior, final delivery status selection, and post-commit broadcasts.
</interfaces>
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prune one populated core webhook table in indexed locked batches</name>
  <reversibility rating="one-way">D-13 adds persisted indexes through V06; reverting their shape after release requires another migration.</reversibility>
  <files>lib/mailglass/migrations/postgres/v06.ex, lib/mailglass/webhook/pruner.ex, test/mailglass/webhook/pruner_test.exs, mailglass_inbound/test/mailglass_inbound/internal/prune_test.exs</files>
  <behavior>
    - DATA-06/D-13: core selects finite ordered candidate ids under FOR UPDATE SKIP LOCKED, serializes each run, deletes/commits bounded batches, and reports exact totals.
    - Concurrent pruners do not double-delete/block indefinitely; infinity keeps its established behavior.
    - V06 adds matching source/status/age/id indexes and EXPLAIN confirms candidate queries use them; inbound child-first pruning retains equivalent index-backed behavior.
  </behavior>
  <action>Write core concurrency/small-batch/rollback/EXPLAIN, generated-wrapper attribute, and inbound index non-regression tests first. Append V06 with the retention indexes shared with Task 2 and exercise it directly in this task. For populated-host indexes, use the existing generated-wrapper convention to emit `CREATE INDEX CONCURRENTLY` outside a DDL transaction with its required transaction/migration-lock attributes; keep lock/statement settings bounded for transactional DDL. Refactor core pruner on the existing inbound pattern: checked-out session, package-specific advisory lock in try/after, finite candidate subquery with SKIP LOCKED, independent batch commits, configured prefix, and bounded telemetry. Preserve optional Oban stubs and infinity behavior. Do not modify shipped versions.</action>
  <verify><automated>mix test test/mailglass/migration_test.exs test/mailglass/webhook/pruner_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/internal/prune_test.exs --warnings-as-errors)</automated></verify>
  <done>Both retention paths have deterministic concurrency and index-plan proof with finite batches and immutable shipped migrations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist exact signed webhook bytes with one parse and bulk delivery load</name>
  <files>lib/mailglass/migrations/postgres/v06.ex, lib/mailglass/webhook/webhook_event.ex, lib/mailglass/webhook/ingest.ex, lib/mailglass/webhook/plug.ex, test/mailglass/webhook/ingest_test.exs</files>
  <behavior>
    - DATA-07/D-14: Plug passes authoritative cached raw bytes plus one decoded value; a byte sentinel survives unchanged in the immutable binary field.
    - A bounded multi-event provider payload performs one delivery-state load keyed by provider/message ids, then preserves duplicate/no-op/orphan/projector/final-status behavior.
    - Parsing or bulk-load failure rolls back consistently and no raw bytes/provider content enter logs or telemetry.
  </behavior>
  <action>Add parse-count, query-count, byte-exact, mixed-delivery, duplicate/orphan, rollback, and broadcast-after-commit tests first. Use V06's additive immutable binary raw-signed-body field/index support. Decode verified JSON once at the Plug/ingest boundary, derive events from that value, deduplicate provider/message keys, issue bounded bulk state queries, and map state in memory. Keep existing transaction lock/statement timeouts, tenant prefix, projection order, outcome shapes, and broadcasts.</action>
  <verify><automated>mix test test/mailglass/webhook/ingest_test.exs --warnings-as-errors</automated></verify>
  <done>Webhook batches prove one parse, bounded bulk delivery reads, exact immutable signed bytes, and unchanged projection outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose V06 through the stable core migration facade</name>
  <files>lib/mailglass/migration.ex, lib/mailglass/migrations/postgres.ex, test/mailglass/migration_test.exs</files>
  <behavior>
    - Fresh installs reach V06, V05 upgrades apply only V06, and rollback returns to V05.
    - Explicit repo/prefix/version selection and generated-wrapper contracts from Phase 155 remain unchanged.
  </behavior>
  <action>After both V06 consumers are green directly, append V06 to the stable core dispatcher/facade and add fresh, V05-upgrade, rerun, and rollback tests. Preserve all shipped V01-V05 modules and Phase 155 selection/error behavior.</action>
  <verify><automated>mix test test/mailglass/migration_test.exs --warnings-as-errors</automated></verify>
  <done>The public core migration path exposes V06 without rewriting any historical version.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Retention worker → populated DB | Destructive selection/deletion crosses durable storage. |
| Verified provider bytes → projection | Signed evidence and attacker-sized event batches enter persistence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-18 | Tampering | retention delete | high | mitigate | Indexed scoped candidate ids, advisory serialization, locked batches, transaction tests. |
| T-157-19 | Denial of Service | prune/ingest | high | mitigate | Finite batches, SKIP LOCKED, one parse, bounded bulk query. |
| T-157-20 | Repudiation | raw webhook evidence | high | mitigate | Preserve byte-exact immutable body and commit-coupled projection tests. |
</threat_model>
<verification>Run focused core/inbound suites, migration tests, schema-prefix gate, formatter, and diff check.</verification>
<success_criteria>DATA-06/07 are complete with bounded indexed work, exact evidence, immutable shipped migrations, and unchanged public/admin surfaces.</success_criteria>
<output>Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-08-SUMMARY.md` when done.</output>
