---
phase: 159-raise-and-simplify-engineering-gates
plan: 04
type: execute
wave: 4
depends_on: [159-02, 159-03]
files_modified: [mix.exs, mailglass_inbound/mix.exs, config/coverage_baselines.*, scripts/check_coverage_floor.*, test/scripts/coverage_floor_contract_test.exs, .github/workflows/ci.yml]
autonomous: true
requirements: [QUAL-06]
---
<objective>Measure canonical core/inbound coverage on the pinned CI toolchain, then enforce non-decreasing package floors and critical-path contracts.</objective>
<context>@.planning/phases/159-raise-and-simplify-engineering-gates/159-{CONTEXT,RESEARCH,PATTERNS}.md @test/support/suite_floor.ex</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Add deterministic coverage collection without a fabricated threshold</name><files>mix.exs, mailglass_inbound/mix.exs, scripts/check_coverage_floor.*, test/scripts/coverage_floor_contract_test.exs</files><action>Write parser/mutation tests first. Configure both packages independently and emit machine-readable coverage for canonical schema/package commands. A no-baseline mode reports measurement only and cannot be called a pass/floor.</action><verify><automated>mix test test/scripts/coverage_floor_contract_test.exs --warnings-as-errors</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: Record CI-toolchain measurements and promote floors</name><action>After green pinned-toolchain measurements for core public/mailglass and inbound axes, commit the exact observed literal baselines plus an exact critical-path command manifest. Reject lower coverage, missing artifacts, changed toolchain, or absent critical contracts; never derive a replacement baseline from the current run. Record the measurement commands and raw artifacts so independent verification can reproduce the chosen floors without a manual checkpoint.</action><verify><automated>mix test test/scripts/coverage_floor_contract_test.exs --warnings-as-errors</automated></verify></task>
</tasks>
<threat_model><table><tr><th>Threat</th><th>Mitigation</th></tr><tr><td>Coverage regression masked by a fresh measurement</td><td>Human-approved pinned baselines and missing-artifact failure.</td></tr></table></threat_model>
<success_criteria>Core and inbound have measured—not invented—floors and named critical contracts.</success_criteria>
