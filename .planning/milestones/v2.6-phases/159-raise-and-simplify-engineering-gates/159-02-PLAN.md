---
phase: 159-raise-and-simplify-engineering-gates
plan: 02
type: execute
wave: 2
depends_on: [159-01]
files_modified: [.formatter.exs, mailglass_inbound/.formatter.exs, .github/actions/setup-beam-mix/action.yml, .github/workflows/ci.yml, test/scripts/format_scope_contract_test.exs, test/scripts/setup_action_contract_test.exs]
autonomous: true
requirements: [QUAL-01, QUAL-05, QUAL-10]
---
<objective>Establish one formatted baseline and package-scoped locked CI setup without changing job identities or package boundaries.</objective>
<context>@.planning/phases/159-raise-and-simplify-engineering-gates/159-{CONTEXT,RESEARCH,PATTERNS}.md @.formatter.exs @mix.exs @mailglass_inbound/mix.exs</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Lock formatter scope before mechanical normalization</name><files>.formatter.exs, mailglass_inbound/.formatter.exs, test/scripts/format_scope_contract_test.exs</files><action>Create a red scope/mutation contract proving root formatting covers core and inbound inputs and preserves package-local formatter options. Then format only agreed Phase-159 baseline files; do not touch admin/operator behavior or intermix product edits.</action><verify><automated>mix test test/scripts/format_scope_contract_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted &amp;&amp; (cd mailglass_inbound &amp;&amp; mix format --check-formatted)</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: Centralize explicit locked package setup</name><files>.github/actions/setup-beam-mix/action.yml, .github/workflows/ci.yml, test/scripts/setup_action_contract_test.exs</files><action>Build a narrow composite action with explicit package path, lockfile, Mix environment/build path and cache namespace; it must run `mix deps.get --check-locked`. Mutation tests reject wildcard lock hashes, missing package identity, or reused incompatible cache keys. Keep Postgres, browsers, and publish credentials in calling jobs.</action><verify><automated>mix test test/scripts/setup_action_contract_test.exs test/scripts/ci_parity_drift_test.exs --warnings-as-errors</automated></verify></task>
</tasks>
<threat_model><table><tr><th>Threat</th><th>Mitigation</th></tr><tr><td>Unlocked/cross-package artifacts</td><td>Explicit package/lock/environment cache inputs and mutation tests.</td></tr></table></threat_model>
<success_criteria>Formatting is one baseline and setup sharing cannot blur sibling dependency graphs.</success_criteria>
