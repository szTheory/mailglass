---
phase: 159-raise-and-simplify-engineering-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/quality/ci_policy.exs
  - scripts/ci_green_policy.sh
  - test/support/ci_policy.ex
  - test/scripts/ci_green_policy_test.exs
  - test/scripts/required_checks_test.exs
  - test/scripts/lane_classification_drift_test.exs
autonomous: true
requirements: [QUAL-03, QUAL-04, QUAL-10]
must_haves:
  truths:
    - "A checked-in policy distinguishes today's active required leaves, the complete target required behaviors, and explicitly advisory lanes without changing CI topology yet."
    - "Missing, failed, duplicate, malformed, or advisory-as-required evidence cannot produce a green aggregate."
    - "Every policy test has a negative control, and all current job/check display names remain stable."
    - "No admin/operator UI source or behavior changes."
  artifacts:
    - path: "config/quality/ci_policy.exs"
      provides: "Single current/target/advisory lane classification with stable check identities"
    - path: "test/scripts/ci_green_policy_test.exs"
      provides: "Fail-closed evaluator fixtures and mutation controls"
    - path: "test/scripts/lane_classification_drift_test.exs"
      provides: "Non-vacuous required/advisory classification contract"
  key_links:
    - from: "test/support/ci_policy.ex"
      to: "config/quality/ci_policy.exs"
      via: "strict manifest parsing and set validation"
      pattern: "active_required|target_required|advisory"
---

<objective>
Build the policy tracer before changing CI topology: version the current and intended lane inventory, keep the current aggregate green, and prove the future promotion cannot omit required proof or admit advisory evidence.
</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-raise-and-simplify-engineering-gates/159-CONTEXT.md
@.planning/phases/159-raise-and-simplify-engineering-gates/159-RESEARCH.md
@.planning/phases/159-raise-and-simplify-engineering-gates/159-PATTERNS.md
@scripts/ci_green_policy.sh
@.github/workflows/ci.yml
</context>

<interfaces>The public protected check remains `CI Green`; existing job IDs and display names are preserved. This plan records target topology but does not add target leaves to `.github/workflows/ci.yml`. The target inventory must cover formatting; warning/no-optional compiles; deterministic core/inbound suites; core/inbound support contracts; Mix tasks; Credo/conformance; core/inbound Dialyzer; docs; Hex/dependency audits; trust; installer smoke/golden proof. Browser, demo, preview, admin visual, next-toolchain, provider-live, clean-baseline, and publish-only evidence is advisory.</interfaces>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Add a strict current/target/advisory policy manifest</name>
  <reversibility rating="low">The manifest is data-only until Plan 06 atomically promotes the repaired target.</reversibility>
  <files>config/quality/ci_policy.exs, test/support/ci_policy.ex, test/scripts/required_checks_test.exs, test/scripts/lane_classification_drift_test.exs</files>
  <read_first>.github/workflows/ci.yml, scripts/ci_green_policy.sh, test/scripts/required_checks_test.exs, test/scripts/ci_parity_drift_test.exs</read_first>
  <behavior>
    - Parse one literal manifest with unique stable IDs and explicit `active_required`, `target_required`, and `advisory` membership.
    - The active set exactly matches current `CI Green` inputs; the target set names every QUAL-03 behavior even when its implementation is not yet promotable.
    - Target and advisory sets are disjoint; unknown, duplicate, empty, or unclassified lanes fail.
    - A mutated omitted required behavior and an advisory promotion both make their fixtures fail.
  </behavior>
  <action>Write red parser/set fixtures first, then add the smallest data module/helper. Keep `promotion_ready: false` so later plans may repair lanes without making the current aggregate depend on unfinished work. Do not edit workflow topology or reuse job display strings as unstable parsing keys.</action>
  <verify><automated>mix test test/scripts/required_checks_test.exs test/scripts/lane_classification_drift_test.exs --warnings-as-errors</automated></verify>
  <done>The repository has a complete target inventory and an exact active inventory, both tested without changing any protected check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the existing fail-closed aggregate protocol</name>
  <files>scripts/ci_green_policy.sh, test/scripts/ci_green_policy_test.exs</files>
  <read_first>scripts/ci_green_policy.sh, test/scripts/ci_green_policy_test.exs</read_first>
  <behavior>
    - Missing, duplicate, failed, cancelled, skipped, malformed, or unknown required evidence fails.
    - Passing advisory evidence cannot substitute for any failed/missing required result.
    - Success requires every active required result exactly once and preserves the existing invocation protocol.
  </behavior>
  <action>Add failing fixtures before tightening the evaluator. Keep it a pure result evaluator and preserve its current stable output/check identity; policy classification remains in the manifest helper.</action>
  <verify><automated>mix test test/scripts/ci_green_policy_test.exs --warnings-as-errors</automated></verify>
  <done>The current aggregate is fail-closed and the future promotion has non-vacuous policy tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Workflow results → aggregate evaluator | Untrusted status/name inputs claim merge proof. |
| Policy manifest → workflow topology | Recorded intent can drift from actual jobs. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-159-01 | Tampering | result set | high | mitigate | Exact-set parsing plus missing/duplicate/malformed mutation fixtures. |
| T-159-02 | Spoofing | advisory lane | high | mitigate | Disjoint classification and advisory-as-required negative control. |
| T-159-03 | Repudiation | target inventory | medium | mitigate | Versioned stable IDs and complete QUAL-03 behavior mapping. |
</threat_model>

<verification>Run all three focused tests and `git diff --check`; prove at least one intentional required omission and one advisory promotion fail. Confirm `.github/workflows/ci.yml` and `mailglass_admin/**` are unchanged in this plan.</verification>
<success_criteria>The policy tracer is complete before topology changes: current CI still works, the full target is explicit, and false-green paths have negative controls.</success_criteria>
<output>Create `.planning/phases/159-raise-and-simplify-engineering-gates/159-01-SUMMARY.md`.</output>
