---
phase: 159-raise-and-simplify-engineering-gates
plan: 03
type: execute
wave: 3
depends_on: [159-01, 159-02]
files_modified: [config/test_exceptions.*, scripts/check_test_exceptions.*, test/scripts/test_exceptions_contract_test.exs, test/mailglass/**, mailglass_inbound/test/**]
autonomous: true
requirements: [QUAL-09]
---
<objective>Replace unaccountable skips, flaky exclusions, and readiness sleeps with an expiring registry and deterministic acknowledgements.</objective>
<context>@.planning/phases/159-raise-and-simplify-engineering-gates/159-{CONTEXT,RESEARCH,PATTERNS}.md @test/support/suite_floor.ex</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Inventory and validate test exceptions bidirectionally</name><files>config/test_exceptions.*, scripts/check_test_exceptions.*, test/scripts/test_exceptions_contract_test.exs</files><action>Define records with source, kind, owner, reason, expiry and category. Red tests must reject missing source entries, stale records, expired dates, unknown kinds and unregistered `@tag :skip`/`:flaky` or sleep sites. Preserve explicit clock/TTL/liveness categories rather than pretending all sleeps are equivalent.</action><verify><automated>mix test test/scripts/test_exceptions_contract_test.exs --warnings-as-errors</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: Remove readiness guessing from selected critical tests</name><files>test/mailglass/**, mailglass_inbound/test/**</files><action>For each selected async critical path, make the old sleep red/flaky scenario observable with a task reply, monitor, telemetry event or mailbox acknowledgement, then remove the sleep. Retain only registered clock/TTL/liveness waits with bounded rationale. Do not change runtime/admin UI behavior.</action><verify><automated>mix test test/mailglass/outbound test/mailglass/webhook mailglass_inbound/test/mailglass_inbound/async_execution_test.exs --warnings-as-errors</automated></verify></task>
</tasks>
<threat_model><table><tr><th>Threat</th><th>Mitigation</th></tr><tr><td>Silent test loss or timing flakes</td><td>Bidirectional expiry registry plus observable completion signals.</td></tr></table></threat_model>
<success_criteria>Every exemption is owned and expiring; readiness completion is asserted rather than slept for.</success_criteria>
