---
phase: 160-certification-documentation-and-release
plan: 03
type: execute
wave: 2
depends_on: [160-01, 160-02]
files_modified:
  - scripts/reconcile_release_versions.exs
  - test/scripts/reconcile_release_versions_test.exs
  - .planning/release-target.json
  - mix.exs
  - mailglass_admin/mix.exs
  - mailglass_inbound/mix.exs
  - .release-please-manifest.json
  - CHANGELOG.md
  - mailglass_admin/CHANGELOG.md
  - mailglass_inbound/CHANGELOG.md
  - .planning/publish/mailglass-publish-summary.json
  - .planning/publish/mailglass_admin-publish-summary.json
  - .planning/publish/mailglass_inbound-publish-summary.json
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "A credential-free preflight compares live Hex releases, all three package manifests, dependency constraints, and release-please state before any candidate exists."
    - "Known repository/live drift is classified explicitly; absence, malformed responses, retired releases, or disagreement fail closed."
    - "No next semantic version is guessed: candidate versions come only from reviewed release automation output in a later authorized plan."
  artifacts:
    - path: "scripts/reconcile_release_versions.exs"
      provides: "Pure live/repository version reconciliation and release-target writer"
    - path: "test/scripts/reconcile_release_versions_test.exs"
      provides: "Offline fixtures for drift, missing, malformed, retired, and equality cases"
    - path: ".planning/release-target.json"
      provides: "Credential-free exact candidate contract consumed by protected workflows"
  key_links:
    - from: "scripts/reconcile_release_versions.exs"
      to: ".release-please-manifest.json"
      via: "exact three-package comparison"
      pattern: "mailglass_inbound"
  prohibitions:
    - "No invented next version, tag, merge, release, or Hex publication."
    - "No weakening independent inbound version/constraint semantics."
    - "No acceptance of a failed or unparseable Hex lookup as version zero."
---

<objective>Close REL-03's current-version ambiguity with a tested, fail-closed reconciliation seam before release automation proposes any next versions.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@mix.exs
@mailglass_admin/mix.exs
@mailglass_inbound/mix.exs
@.release-please-manifest.json
@.github/workflows/publish-hex.yml</context>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Implement offline-tested live-versus-repository reconciliation</name>
<files>scripts/reconcile_release_versions.exs, test/scripts/reconcile_release_versions_test.exs</files>
<read_first>mix.exs, mailglass_admin/mix.exs, mailglass_inbound/mix.exs, .release-please-manifest.json</read_first>
<behavior>- Fixtures reproduce repository core/admin 2.4.0 versus live 2.4.1 and repository inbound 2.1.1 versus live 2.1.2 as drift, never as an upgrade target.
- Empty, missing, duplicate, malformed, retired, and conflicting package records fail closed.
- Equal repository/live versions pass, and core/admin/inbound remain three explicit package records.</behavior>
<action>Write pure fixture-driven tests first, then implement a script with `--fixture` and live read-only modes. Parse exact versions from Mix manifests and release-please state; query Hex read-only without credentials in live mode. Emit a structured drift report and refuse to write a release target until all current manifests agree with live published truth.</action>
<verify><automated>mix test test/scripts/reconcile_release_versions_test.exs --warnings-as-errors</automated></verify>
<done>Current drift and every fail-closed edge are reproducible without network access.</done>
<acceptance_criteria>The script never derives a next version and cannot pass on missing or contradictory evidence.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 2: Reconcile manifest truth and produce a version-neutral release target contract</name>
<files>mix.exs, mailglass_admin/mix.exs, mailglass_inbound/mix.exs, .release-please-manifest.json, .planning/release-target.json, CHANGELOG.md, mailglass_admin/CHANGELOG.md, mailglass_inbound/CHANGELOG.md, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-publish-summary.json, .planning/publish/mailglass_inbound-publish-summary.json, test/scripts/reconcile_release_versions_test.exs</files>
<read_first>scripts/reconcile_release_versions.exs, release-please-config.json, .github/workflows/publish-hex.yml</read_first>
<action>Run the live read-only reconciliation. Update repository version declarations, release-please manifest, changelog release entries, and all three publish-summary version fields only to match exact versions and artifacts proven already published on Hex and the immutable historical release tag; do not advance beyond live or copy unrelated historical-tag source. Preserve core/admin linkage and inbound independence. Write `.planning/release-target.json` as an inactive versioned schema containing the exact package set, reconciled baselines, required evidence identifiers, nullable Release Please proposal head/source identity, nullable publishable-content digest (explicitly excluding `.planning/release-target.json`), nullable final tag SHA, `candidate_versions: null`, and separate capture/authorization/publication states. Add tests that reject activation while any candidate field is absent or differs from reviewed automation output.</action>
<verify><automated>mix run scripts/reconcile_release_versions.exs --check-live &amp;&amp; mix test test/scripts/reconcile_release_versions_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/stability_contract_test.exs --warnings-as-errors) &amp;&amp; (cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/stability_contract_test.exs --warnings-as-errors)</automated></verify>
<done>Repository and live published baselines agree exactly, while the release target remains inactive and version-neutral.</done>
<acceptance_criteria>REL-03 baseline reconciliation is complete before candidate creation; no future version has been selected.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Hex API → repository manifests | Remote metadata influences release readiness. |
| Release automation output → release target | Unreviewed versions must not authorize publication. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-07 | Spoofing | Hex metadata | high | mitigate | HTTPS read-only query, exact package names, semantic parsing, and fixture tests. |
| T-160-08 | Tampering | release target | high | mitigate | Inactive target, exact three-package schema, no candidate inference. |
| T-160-09 | Repudiation | version reconciliation | medium | mitigate | Structured report records repository/live sources and exact comparison. |
</threat_model>

## Artifacts this phase produces
- `reconcile_release_versions.exs` CLI modes: `--fixture`, `--check-live`, and inactive-target validation.
- `.planning/release-target.json` schema with explicit core/admin/inbound baselines, null candidates, proposal/content/final identities, and separate authorization state.

<verification>Run offline mutation tests, live read-only check, and package stability contracts.</verification>
<success_criteria>REL-03 has exact current truth and no speculative candidate version.</success_criteria>
<output>Create `.planning/phases/160-certification-documentation-and-release/160-03-SUMMARY.md` when done.</output>
