---
phase: 158-simplify-architecture-without-breaking-adopters
plan: 03
type: execute
wave: 2
depends_on: [158-01]
files_modified:
  - lib/mailglass/ports/pub_sub.ex
  - lib/mailglass/pub_sub.ex
  - lib/mailglass/outbound/projector.ex
  - mailglass_inbound/lib/mailglass_inbound/ports/core.ex
  - mailglass_inbound/lib/mailglass_inbound/optional_deps.ex
  - mailglass_inbound/lib/mailglass_inbound/mime.ex
  - mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  - mailglass_inbound/lib/mailglass_inbound/execution/worker.ex
  - mailglass_inbound/mix.exs
  - test/scripts/architecture_boundary_test.exs
  - mailglass_inbound/test/mailglass_inbound/architecture_port_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs
  - mailglass_inbound/test/mailglass_inbound/mime_test.exs
  - mailglass_inbound/test/mailglass_inbound/internal/doctor_test.exs
autonomous: true
requirements: [ARCH-03, ARCH-06]
must_haves:
  truths:
    - "Inbound production code reaches core only through a small declared allowlist of capability ports/pure primitives."
    - "Safe post-commit PubSub broadcast has one core owner; inbound no longer copies an Outbound implementation detail."
    - "Each package retains its own optional dependency gateway, worker, application env, and release boundary."
    - "Inbound no longer reads core application env or imports core optional-dependency implementations, and the obsolete middleware warning suppression is removed."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/ports/core.ex"
      provides: "Inbound-owned adapter over declared core capabilities"
    - path: "mailglass_inbound/test/mailglass_inbound/architecture_port_test.exs"
      provides: "Production edge inventory and declared-port contract"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ports/core.ex"
      via: "post-commit broadcast invokes declared capability rather than Outbound.Projector internals"
      pattern: "Ports.Core"
---

<objective>Replace broad sibling implementation coupling with named, minimal core capabilities while preserving separate package ownership.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-CONTEXT.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-PATTERNS.md
@mailglass_inbound/lib/mailglass_inbound/optional_deps.ex
@mailglass_inbound/lib/mailglass_inbound/execution.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
@lib/mailglass/outbound/projector.ex
@lib/mailglass/pub_sub.ex</context>
<interfaces>Inbound keeps its own `:mailglass_inbound` config, Repo façade, optional dependency gateways, workers, and public router/mailbox/Plug contracts. Core remains free of `MailglassInbound.*` production references. Keep `Mailglass.Identifier.validate!/2`, `Mailglass.Tenancy`, typed errors, and declared PubSub functionality only where their concrete capabilities are required; do not export a root implementation bag.</interfaces>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Inventory production sibling edges and move safe broadcast to one declared core capability</name>
<files>test/scripts/architecture_boundary_test.exs, mailglass_inbound/test/mailglass_inbound/architecture_port_test.exs, lib/mailglass/ports/pub_sub.ex, lib/mailglass/pub_sub.ex, lib/mailglass/outbound/projector.ex, mailglass_inbound/lib/mailglass_inbound/ports/core.ex, mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
<behavior>- ARCH-03/06: a production-source inventory classifies every inbound `Mailglass.*` edge as allowed pure primitive, declared port, or prohibited implementation import; tests/generators are excluded deliberately, not accidentally.
- Inbound inserted-record broadcast retains PII-safe payload, post-commit timing, and best-effort failure semantics without calling/copying `Outbound.Projector.safe_broadcast/2`.
- A synthetic undeclared import and a core-to-inbound edge fail the architecture contracts.</behavior>
<action>Write inventory/negative-control tests first. Add a small core-owned safe-broadcast capability with domain-neutral input/output and use it from an inbound-owned Core port adapter. Move the duplicated defensive broadcast behavior out of Outbound.Projector only after characterizing its rescue/exit/log behavior. Keep topic construction package-specific and preserve current inbound payload/status timing. Do not make inbound import the full Config, Repo, Outbound, or Projector implementation; update the allowlist only for the narrow declared capability.</action>
<verify><automated>mix test test/scripts/architecture_boundary_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/architecture_port_test.exs test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors)</automated></verify>
<done>One shared behavior has a single core owner, inbound uses a declared narrow seam, and the guard proves it can reject leakage.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Isolate job/optional integration from core implementation leakage</name>
<files>mailglass_inbound/lib/mailglass_inbound/ports/core.ex, mailglass_inbound/lib/mailglass_inbound/optional_deps.ex, mailglass_inbound/lib/mailglass_inbound/mime.ex, mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex, mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex, mailglass_inbound/lib/mailglass_inbound/execution/worker.ex, mailglass_inbound/mix.exs, mailglass_inbound/test/mailglass_inbound/architecture_port_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs, mailglass_inbound/test/mailglass_inbound/mime_test.exs, mailglass_inbound/test/mailglass_inbound/internal/doctor_test.exs, test/scripts/architecture_boundary_test.exs</files>
<behavior>- ARCH-03/06: inbound job execution consumes only the minimal declared tenancy/job-context capability required by its worker.
- Inbound MIME/doctor use an inbound-owned GenSMTP optional gateway, and inbound persistence obtains the core suppression capability without reading `Application.get_env(:mailglass, ...)` or naming a concrete core store implementation.
- `mix compile --no-optional-deps --warnings-as-errors` stays green in both independent projects without broadening suppression lists.
- The no-optional build succeeds after deleting the existing `Mailglass.Oban.TenancyMiddleware` `no_warn_undefined` entry; a source negative control rejects reintroducing core optional-gateway imports or sibling app-env reads.
- Inbound ExAws/Oban gateways remain inbound-local and core optional integrations remain core-local.</behavior>
<action>Characterize worker, MIME/doctor, suppression persistence, and no-optional behavior first. Replace the direct core Oban middleware import with a small compatibility port/value or sibling-local adapter, move GenSMTP availability/decoding behind the inbound-owned optional gateway, and replace the inbound read of core `:suppression_store` app env/concrete default with a declared narrow core capability. Remove the now-obsolete `Mailglass.Oban.TenancyMiddleware` warning suppression from inbound `mix.exs`. Keep worker job shape, tenant propagation, retry/dispatch semantics, MIME errors, suppression outcomes, and optional-dependency absence behavior stable. The inventory may allow explicitly named pure helpers, typed public errors, migration primitives, and provider-verification capabilities, but must reject raw sibling app-env reads, another package's optional gateway, broad Config/Repo/Outbound implementation imports, and undeclared modules. Reject any solution that adds blanket `no_warn_undefined` entries or moves S3/provider feature ownership into core.</action>
<verify><automated>mix compile --no-optional-deps --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix compile --no-optional-deps --warnings-as-errors &amp;&amp; mix test test/mailglass_inbound/architecture_port_test.exs test/mailglass_inbound/mailbox_execution_test.exs test/mailglass_inbound/ingress/persist_test.exs test/mailglass_inbound/mime_test.exs test/mailglass_inbound/internal/doctor_test.exs --warnings-as-errors)</automated></verify>
<done>Package independence is proven under optional-dependency absence and every runtime sibling integration has a named owner.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Inbound → core capability | A sibling can otherwise acquire unrelated root implementation access. |
| Post-commit persistence → PubSub | Broadcast failure must not alter committed durable state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-158-04 | Elevation of Privilege | sibling port | high | mitigate | explicit production allowlist and negative import controls. |
| T-158-05 | Repudiation | broadcast | medium | mitigate | retain post-commit payload/timing characterization. |
| T-158-06 | Denial of Service | optional dependency loading | medium | mitigate | both no-optional compiles and package-local gateway proof. |
</threat_model>
<verification>Run focused architecture/ingress/worker tests, both no-optional compiles, both xref commands, and formatter. No admin/operator paths may be modified.</verification>
<success_criteria>ARCH-03 and ARCH-06 establish explicit inbound/core ports and one-owner shared logic without package collapse or optional-dependency leakage.</success_criteria>
<output>Create `.planning/phases/158-simplify-architecture-without-breaking-adopters/158-03-SUMMARY.md` when done.</output>
