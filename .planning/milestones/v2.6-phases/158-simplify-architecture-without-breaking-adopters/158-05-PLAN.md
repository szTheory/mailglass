---
phase: 158-simplify-architecture-without-breaking-adopters
plan: 05
type: execute
wave: 3
depends_on: [158-02, 158-03]
files_modified:
  - lib/mailglass/webhook/pipeline.ex
  - lib/mailglass/webhook/plug.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/pipeline.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  - test/mailglass/webhook/plug_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs
autonomous: true
requirements: [ARCH-05]
must_haves:
  truths:
    - "Core webhook and inbound Plug init/call contracts, provider options, response status/body policy, and telemetry names remain stable."
    - "Both Plug façades adapt Conn to a closed package-local pipeline outcome; verification still precedes tenant/database work."
    - "Persistence commits before best-effort broadcast; provider I/O remains outside durable transactions."
  artifacts:
    - path: "lib/mailglass/webhook/pipeline.ex"
      provides: "Core webhook internal outcome pipeline"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/pipeline.ex"
      provides: "Inbound internal outcome pipeline"
  key_links:
    - from: "lib/mailglass/webhook/plug.ex"
      to: "lib/mailglass/webhook/pipeline.ex"
      via: "stable Plug maps closed outcome to existing HTTP response"
      pattern: "Pipeline"
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/pipeline.ex"
      via: "stable Plug delegates verified inbound lifecycle"
      pattern: "Pipeline"
---

<objective>Turn the public webhook and inbound Plugs into thin adapters over explicit package-local pipelines without changing security or acknowledgement semantics.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-CONTEXT.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-PATTERNS.md
@lib/mailglass/webhook/plug.ex
@lib/mailglass/webhook/ingest.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
@mailglass_inbound/lib/mailglass_inbound/execution.ex</context>
<interfaces>Keep `Mailglass.Webhook.Plug.init/1`/`call/2` and `MailglassInbound.Ingress.Plug.init/1`/`call/2` exactly callable with existing provider/options/body-reader wiring. Preserve all current 200/401/422/429/500 decisions, typed errors, PII-safe response/telemetry behavior, verification-before-tenant ordering, durable terminal evidence acknowledgement, post-commit broadcast, and execution semantics. Pipelines are package-local internals, not a shared mega pipeline.</interfaces>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Extract the core webhook outcome pipeline behind its unchanged Plug</name>
<files>lib/mailglass/webhook/pipeline.ex, lib/mailglass/webhook/plug.ex, test/mailglass/webhook/plug_test.exs</files>
<behavior>- ARCH-05: successful and rejected core webhooks preserve raw-body verification, config/provider resolution, tenant ordering, normalized ingest, response status/body, telemetry, and post-commit broadcast.
- Pipeline returns a closed domain outcome; the public Plug alone maps it to Plug.Conn.
- A negative spy proves no tenant lookup/ingest occurs after invalid signature and no broadcast precedes commit.</behavior>
<action>Write ordered characterization tests first, including invalid signature, config failure, duplicate/orphan/success outcomes, and a commit/broadcast sentinel. Extract request/config/verification/tenant/ingest stages into `Webhook.Pipeline`; keep provider crypto and `Ingest` ownership unchanged. Keep response rendering and `Plug.Conn` handling at the public edge. Do not combine this with inbound persistence or introduce shared provider pipeline abstractions.</action>
<verify><automated>mix test test/mailglass/webhook/plug_test.exs test/mailglass/webhook/ingest_test.exs --warnings-as-errors</automated></verify>
<done>Core webhook public behavior is characterized and unchanged while its orchestration has one explicit internal owner.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Extract the inbound verified-request outcome pipeline behind its unchanged Plug</name>
<files>mailglass_inbound/lib/mailglass_inbound/ingress/pipeline.ex, mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
<behavior>- ARCH-05: all providers preserve public response/status policy, including replay/control-plane no-ops, signature rejection, rate limit, terminal S3 evidence acknowledgement, persistence failure, and execution/broadcast behavior.
- Authentication remains before tenant resolution; authenticated SES content/terminal evidence keeps its Phase 157 order.
- Pipeline stages use existing explicit VerifiedRequest/Persist/Execution contracts rather than process state or a new public callback.</behavior>
<action>Expand existing ordered spies into characterization tests before extracting. Create `Ingress.Pipeline` around current explicit verified-request branches and make the Plug only adapt Conn/provider opts, telemetry envelope, and response. Keep all legacy provider callback adapters at the existing edge. Preserve rate-limit position, terminal durable-evidence-before-ack rule, PII-safe logs/metadata, and best-effort post-commit broadcast through the declared core port from Plan 03.</action>
<verify><automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/ingress/ses_provider_test.exs test/mailglass_inbound/persistence_test.exs --warnings-as-errors</automated></verify>
<done>Inbound Plug is a stable public adapter over a closed pipeline, with all Phase 157 security/durability ordering preserved.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Provider HTTP → verification | Untrusted signed bytes and headers must authenticate before scoped work. |
| Durable persistence → response/broadcast | Acknowledgement and realtime notification must follow committed state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-158-10 | Spoofing | pipeline reorder | high | mitigate | verification-before-tenant negative spies. |
| T-158-11 | Repudiation | terminal/evidence response | high | mitigate | durable-before-ack characterization. |
| T-158-12 | Information Disclosure | HTTP/log mapping | medium | mitigate | preserve closed status/body/telemetry assertions. |
</threat_model>
<verification>Run focused core and inbound Plug suites, no-optional compiles, xref guard, formatter, and static diff scope. Do not alter UI/router mounting behavior.</verification>
<success_criteria>ARCH-05 delivers thin public Plugs, explicit package-local pipelines, preserved security/durability ordering, and zero public ingress contract breakage.</success_criteria>
<output>Create `.planning/phases/158-simplify-architecture-without-breaking-adopters/158-05-SUMMARY.md` when done.</output>
