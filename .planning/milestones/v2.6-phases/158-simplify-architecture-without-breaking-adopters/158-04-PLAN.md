---
phase: 158-simplify-architecture-without-breaking-adopters
plan: 04
type: execute
wave: 3
depends_on: [158-02, 158-03]
files_modified:
  - lib/mailglass/outbound.ex
  - lib/mailglass/outbound/preflight.ex
  - lib/mailglass/outbound/dispatch.ex
  - lib/mailglass/outbound/persistence.ex
  - lib/mailglass/outbound/routes.ex
  - test/mailglass/outbound_test.exs
  - test/mailglass/outbound/preflight_test.exs
  - test/mailglass/stability_contract_test.exs
autonomous: true
requirements: [ARCH-04]
must_haves:
  truths:
    - "Mailglass.Outbound keeps every existing public verb, arity, return shape, and documented synchronous/asynchronous behavior."
    - "Preflight, route/registry resolution, persistence Multi construction, and dispatch each have one internal owner behind the façade."
    - "Provider I/O remains outside transactions and durable/Task/Oban semantics remain unchanged."
  artifacts:
    - path: "lib/mailglass/outbound/preflight.ex"
      provides: "Owned delivery validation/render/suppression/rate-limit pipeline"
    - path: "lib/mailglass/outbound/persistence.ex"
      provides: "Owned delivery/event/payload/job persistence composition"
    - path: "lib/mailglass/outbound/dispatch.ex"
      provides: "Owned adapter and async dispatch operations"
  key_links:
    - from: "lib/mailglass/outbound.ex"
      to: "lib/mailglass/outbound/preflight.ex"
      via: "public send/deliver façade delegates after telemetry envelope"
      pattern: "Preflight"
    - from: "lib/mailglass/outbound.ex"
      to: "lib/mailglass/outbound/persistence.ex"
      via: "façade preserves transactional result mapping while collaborator builds writes"
      pattern: "Persistence"
---

<objective>Make Outbound a thin, stable public façade over clearly owned internal delivery collaborators.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-CONTEXT.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-RESEARCH.md
@lib/mailglass/outbound.ex
@lib/mailglass/outbound/delivery.ex
@lib/mailglass/outbound/projector.ex
@lib/mailglass/outbound/async_adapter.ex
@test/mailglass/outbound_test.exs</context>
<interfaces>Do not alter `Mailglass.Outbound.send/2`, `deliver/2`, `deliver!/2`, `deliver_later/2`, `deliver_many/2`, `deliver_many!/2`, or `dispatch_by_id/1`; root delegates and documented return/error structs remain stable. Internal collaborators are not newly documented adopter APIs. Preserve tenant/rate/suppression ordering, exact delivery status semantics, atomic Ecto.Multi/Oban behavior, error privacy, telemetry names, and post-commit broadcasts.</interfaces>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Extract the synchronous delivery tracer behind unchanged Outbound verbs</name>
<files>lib/mailglass/outbound.ex, lib/mailglass/outbound/preflight.ex, lib/mailglass/outbound/persistence.ex, lib/mailglass/outbound/dispatch.ex, lib/mailglass/outbound/routes.ex, test/mailglass/outbound_test.exs, test/mailglass/outbound/preflight_test.exs</files>
<behavior>- ARCH-04: `send/2`/`deliver/2` preserve rendered output, preflight rejection, idempotency, delivery/event ordering, adapter call timing, status/error mapping, and post-commit broadcast.
- Characterization proves adapter delivery is still outside DB transactions and a persistence failure cannot report a sent delivery.
- Each moved responsibility has one internal module owner; façade retains only public coercion/telemetry/result presentation.</behavior>
<action>Write characterization tests for sync success, each preflight rejection, adapter failure, and transaction/broadcast order before moving code. Extract cohesive collaborators with opaque internal inputs/results; keep the façade's public argument coercion and telemetry envelope. Do not move database schemas, provider adapters, or UI-facing code. Preserve explicit Runtime/Config accessors rather than reintroducing direct app-env reads.</action>
<verify><automated>mix test test/mailglass/outbound_test.exs test/mailglass/outbound/preflight_test.exs --warnings-as-errors</automated></verify>
<done>One end-to-end send remains identical at the public edge while preflight, routing, persistence, and dispatch have independent internal ownership.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Move async, batch, and replay paths through the same owned collaborators</name>
<files>lib/mailglass/outbound.ex, lib/mailglass/outbound/dispatch.ex, lib/mailglass/outbound/persistence.ex, lib/mailglass/outbound/routes.ex, test/mailglass/outbound_test.exs, test/mailglass/stability_contract_test.exs</files>
<behavior>- ARCH-04: async Oban/Task fallback, batch ordering/suppression outcomes, dispatch-by-id rehydration, registry routes, retry classes, and public return shapes remain unchanged.
- No duplicated private delivery policy remains in the façade after expansion.
- Existing public stability inventory remains green without adding new promised internal collaborators.</behavior>
<action>Add missing characterization cases first, then route async/batch/replay branches through the Task 1 collaborators. Preserve optional-Oban gates, bounded fallback refusal, private payload lifecycle, closed decoder/error behavior, and configuration override precedence. Keep line-count reduction non-goal: retain a small façade wrapper wherever it is required to preserve v2 coercion or telemetry semantics.</action>
<verify><automated>mix test test/mailglass/outbound_test.exs test/mailglass/outbound test/mailglass/stability_contract_test.exs --warnings-as-errors</automated></verify>
<done>All delivery modes share explicit owners behind the stable façade, with no new adopter-facing internal API.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Public message → preflight | Tenant, suppression, rate-limit, and rendering policy decides whether dispatch is safe. |
| Persistence → provider adapter | DB commit and external provider I/O have different atomicity/security boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-158-07 | Tampering | extraction order | high | mitigate | characterization of event/status/transaction order before refactor. |
| T-158-08 | Information Disclosure | error/telemetry path | high | mitigate | retain existing privacy-safe error and telemetry assertions. |
| T-158-09 | Denial of Service | async fallback | medium | mitigate | preserve bounded Task/Oban admission behavior. |
</threat_model>
<verification>Run focused outbound/stability suites, root no-optional compile, xref guard, formatter, and diff scope. No `mailglass_admin/**` or operator code edits.</verification>
<success_criteria>ARCH-04 holds for every public Outbound route: internal ownership is explicit, public behavior is characterized and stable, and no vanity churn is introduced.</success_criteria>
<output>Create `.planning/phases/158-simplify-architecture-without-breaking-adopters/158-04-SUMMARY.md` when done.</output>
