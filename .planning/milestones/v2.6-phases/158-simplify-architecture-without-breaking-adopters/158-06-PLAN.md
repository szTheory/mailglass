---
phase: 158-simplify-architecture-without-breaking-adopters
plan: 06
type: execute
wave: 4
depends_on: [158-04, 158-05]
files_modified:
  - test/scripts/architecture_boundary_test.exs
  - test/reference_host/public_seams_contract_test.exs
  - test/mailglass/stability_contract_test.exs
  - mailglass_inbound/test/mailglass_inbound/architecture_port_test.exs
  - mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs
  - .github/workflows/ci.yml
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-06]
must_haves:
  truths:
    - "The protected code-validation path runs the fail-closed cycle/package-edge contract without changing CI topology beyond this requirement."
    - "Core and inbound independently compile with no optional dependencies and retain their existing public/stability seams."
    - "A clean full validation demonstrates the refactor changed no admin/operator UI behavior or package release boundary."
  artifacts:
    - path: "test/scripts/architecture_boundary_test.exs"
      provides: "Final anti-vacuous architecture contract exercised by CI"
    - path: "test/reference_host/public_seams_contract_test.exs"
      provides: "Cross-package public façade inventory regression proof"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "test/scripts/architecture_boundary_test.exs"
      via: "existing required code lane executes architecture proof"
      pattern: "architecture_boundary_test"
---

<objective>Close Phase 158 with integration evidence that architecture contracts are enforced, packages remain independently usable, and public façades did not drift.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-CONTEXT.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-VALIDATION.md
@test/scripts/architecture_boundary_test.exs
@test/reference_host/public_seams_contract_test.exs
@.github/workflows/ci.yml</context>
<interfaces>No public v2 module/function/macro/callback removal, rename, return-shape change, config-key change, package collapse, provider expansion, or admin/operator UI change. CI modification is narrowly limited to executing the architecture proof in an existing required code-validation lane; Phase 159 owns broad gate topology work.</interfaces>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Wire the architecture contract into an existing required code lane</name>
<files>test/scripts/architecture_boundary_test.exs, .github/workflows/ci.yml</files>
<behavior>- ARCH-01: the existing required code path runs both xref checks and production-edge guard; any cycle, guard execution failure, or forbidden edge fails the job.
- The architecture test proves its negative-control mechanism still fails; CI cannot pass by skipping or swallowing its result.
- No required-check identity, lane classification, unrelated job, or admin/browser workflow is renamed/restructured.</behavior>
<action>First add a source/YAML contract proving the chosen existing required lane invokes the architecture command and fails on non-zero exit. Then make the smallest CI step/alias addition needed to execute the checked-in test/command. Keep the CI job name, required-check identity, dependency graph, cache setup, and advisory lanes unchanged. Add a mutation-style test that removes or neutralizes the invocation and proves the source contract fails.</action>
<verify><automated>mix test test/scripts/architecture_boundary_test.exs test/scripts/required_checks_test.exs --warnings-as-errors</automated></verify>
<done>The architecture gate is executable merge proof, not documentation or an advisory-only local command.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Run package independence and public compatibility final evidence</name>
<files>test/reference_host/public_seams_contract_test.exs, test/mailglass/stability_contract_test.exs, mailglass_inbound/test/mailglass_inbound/architecture_port_test.exs, mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs</files>
<behavior>- ARCH-01..06: core and inbound each compile with `--no-optional-deps --warnings-as-errors`, have zero compile-connected cycles, and pass package-edge contracts.
- Existing core Config/Outbound and inbound Plug/router/mailbox public inventory remains present; no private collaborator is accidentally promoted to stable API.
- The final changed-file scope contains no `mailglass_admin/**`, operator UI, migration, provider expansion, or package-manifest collapse work.</behavior>
<action>Strengthen final stability/public-seam contracts only where previous extraction exposed a missing assertion. Run clean independent compilation from both roots and retain failure diagnostics rather than adding suppression. Add a static scope test or phase diff check that rejects forbidden Phase 158 UI/operator paths, while allowing core/inbound consumer compatibility tests. Do not advance requirements, phase status, release versions, or generated-host certification here.</action>
<verify><automated>mix compile --no-optional-deps --warnings-as-errors &amp;&amp; mix xref graph --format cycles --label compile-connected &amp;&amp; mix test test/scripts/architecture_boundary_test.exs test/reference_host/public_seams_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix compile --no-optional-deps --warnings-as-errors &amp;&amp; mix xref graph --format cycles --label compile-connected &amp;&amp; mix test test/mailglass_inbound/architecture_port_test.exs test/mailglass_inbound/stability_contract_test.exs --warnings-as-errors)</automated></verify>
<done>Phase acceptance is evidenced by independent compilation, anti-vacuous architecture enforcement, and unchanged public seams/scope fences.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local architecture proof → protected CI | A passing local command is insufficient unless an existing required lane executes it. |
| Refactored internal modules → adopter public API | Internal collaborators can accidentally change/export a stable surface. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-158-13 | Tampering | CI gate | high | mitigate | YAML/source negative control and existing required-lane execution. |
| T-158-14 | Repudiation | public compatibility | high | mitigate | stability/public-seam contracts in both packages. |
| T-158-15 | Elevation of Privilege | scope creep | medium | mitigate | final changed-path guard excluding UI/operator/package-collapse work. |
</threat_model>
<verification>Run the Task 2 command, both full `mix ci` commands, both formatter checks, `git diff --check`, and the phase changed-path scope guard. If a full suite reveals unrelated dirty-worktree changes, report them; do not revert them.</verification>
<success_criteria>All ARCH requirements have final automated evidence in an existing required lane, independent package/no-optional proof, stable public v2 façades, and zero admin/operator UI edits.</success_criteria>
<output>Create `.planning/phases/158-simplify-architecture-without-breaking-adopters/158-06-SUMMARY.md` when done.</output>
