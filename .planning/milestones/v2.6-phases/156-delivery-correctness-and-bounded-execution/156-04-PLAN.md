---
phase: 156-delivery-correctness-and-bounded-execution
plan: 04
type: execute
wave: 4
depends_on: [156-03]
files_modified:
  - lib/mailglass/adapters/swoosh.ex
  - lib/mailglass/errors/send_error.ex
  - lib/mailglass/outbound.ex
  - lib/mailglass/outbound/worker.ex
  - lib/mailglass/tracking/plug.ex
  - docs/api_stability.md
  - test/mailglass/adapters/swoosh_test.exs
  - test/mailglass/error_test.exs
  - test/mailglass/outbound/worker_test.exs
  - test/mailglass/outbound/telemetry_test.exs
  - test/mailglass/tracking/plug_test.exs
autonomous: true
requirements: [EXEC-05, EXEC-06, EXEC-07]
must_haves:
  truths:
    - "Transport errors, timeouts, HTTP 429, and HTTP 5xx are transient; ordinary HTTP 4xx are permanent; no arbitrary text determines retry class."
    - "The Oban worker retries transient failures and discards permanent failures."
    - "Serialized and persisted errors omit provider response bodies, recipient, subject, headers, and rendered message content."
    - "Each provider dispatch emits one authoritative span, and tracking emits recorded only after ledger success while HTTP responses remain fail-open."
  artifacts:
    - path: "lib/mailglass/adapters/swoosh.ex"
      provides: "Closed provider-aware retry mapping and response-body-free errors"
    - path: "lib/mailglass/outbound/worker.ex"
      provides: "Retry/discard outcome driven by SendError.retry_class"
    - path: "lib/mailglass/tracking/plug.ex"
      provides: "Outcome-coupled success/failure telemetry with unchanged HTTP behavior"
  key_links:
    - from: "lib/mailglass/adapters/swoosh.ex"
      to: "lib/mailglass/errors/send_error.ex"
      via: "finite status/reason mapping sets retry_class"
      pattern: "retry_class"
    - from: "lib/mailglass/outbound/worker.ex"
      to: "lib/mailglass/errors/send_error.ex"
      via: "worker return follows closed transient/permanent classification"
      pattern: "retryable?"
    - from: "lib/mailglass/tracking/plug.ex"
      to: "Mailglass.Events"
      via: "append result selects recorded or failed telemetry before unchanged response"
      pattern: "Events.append"
---

<objective>
Make provider retry, error privacy, dispatch telemetry, and tracking telemetry tell the truth end to end.

Purpose: Stop permanent retry storms, serialized provider-body leaks, duplicate spans, and false tracking-success signals.
Output: Closed retry classification, privacy-safe errors, correct Oban outcomes, one span, truthful tracking telemetry, and stability documentation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-CONTEXT.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-RESEARCH.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-VALIDATION.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-03-SUMMARY.md
@lib/mailglass/errors/send_error.ex
@lib/mailglass/adapters/swoosh.ex
@lib/mailglass/outbound/worker.ex
@lib/mailglass/tracking/plug.ex

<interfaces>
Plan 156-03 adds `%Mailglass.SendError{retry_class: :transient | :permanent | nil}` and `:dispatch_unavailable` while Jason continues encoding only `type`, `message`, and `context`. `Mailglass.Outbound.dispatch_by_id/1` remains the worker entry point. Confirm the exact locked Oban 2.23 discard return from local dependency source before implementing the permanent branch.
</interfaces>
</context>

<resolved_research_questions>
- Swoosh has no evidenced adapter-specific exceptional 4xx in current scope. Map ordinary 400..499 permanently; add no exception without an adapter-specific source and regression test.
</resolved_research_questions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Classify provider outcomes and keep all error representations private</name>
  <reversibility rating="costly">D-08/D-09 tighten retry behavior and the documented additive error contract across adapters and durable workers.</reversibility>
  <files>lib/mailglass/adapters/swoosh.ex, lib/mailglass/errors/send_error.ex, lib/mailglass/outbound/worker.ex, docs/api_stability.md, test/mailglass/adapters/swoosh_test.exs, test/mailglass/error_test.exs, test/mailglass/outbound/worker_test.exs</files>
  <behavior>
    - D-08: timeout, transport, 429, and 500..599 errors carry `retry_class: :transient`; ordinary 400..499 carry `:permanent`; malformed/unknown outcomes use a closed explicit default rather than reason text.
    - Worker success returns `:ok`, transient failure returns Oban's retry form, and permanent failure returns the locked Oban discard form.
    - D-09: a unique provider-body/recipient/subject/rendered-body sentinel is absent from context, exception message, Jason output, and persisted `last_error`.
  </behavior>
  <action>Add the full status/reason decision table, worker outcomes, and multi-representation sentinel tests before production edits. Make `SendError.retryable?/1` follow the field's finite value rather than treating every adapter failure alike. In Swoosh, set retry class from exact known status/reason shapes; remove response-body preview construction and body-derived causes, retaining only allowlisted provider module, numeric status, and finite reason class. Have the worker use the documented Oban 2.23 permanent-discard return and retry only transient errors; fail closed for malformed classifications. Update `docs/api_stability.md` to record the additive type/field, unchanged JSON fields, decision table, and privacy rule without changing other public contracts.</action>
  <verify>
    <automated>mix test test/mailglass/error_test.exs test/mailglass/adapters/swoosh_test.exs test/mailglass/outbound/worker_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The provider decision table drives worker retry/discard exactly, and sensitive sentinels cannot enter any serializable or persisted error representation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit one dispatch span and outcome-coupled tracking telemetry</name>
  <files>lib/mailglass/adapters/swoosh.ex, lib/mailglass/outbound.ex, lib/mailglass/tracking/plug.ex, test/mailglass/outbound/telemetry_test.exs, test/mailglass/adapters/swoosh_test.exs, test/mailglass/tracking/plug_test.exs</files>
  <behavior>
    - D-11: one `dispatch_by_id` provider call produces exactly one dispatch start/stop pair, including the Swoosh adapter path.
    - D-10: successful open/click append emits the existing recorded event; append error or raise emits a distinct failed event and never recorded.
    - Valid tracking URLs retain their 200 GIF or 302 redirect response on ledger failure; failure telemetry contains only delivery ID, tenant ID, and a finite failure class.
  </behavior>
  <action>Strengthen telemetry tests first with handler acknowledgements and an injected event-ledger seam; do not use sleeps. Keep the authoritative provider span in `Mailglass.Outbound.call_adapter/2` and remove Swoosh's wrapper so direct adapter tests no longer claim a second end-to-end span. Refactor open/click recording to branch on `Events.append/1`: emit recorded only on success, emit separate privacy-safe failed telemetry on returned errors and rescued failures, then preserve existing fail-open HTTP response status/body/location. Use a finite failure class rather than exception messages or provider/message data.</action>
  <verify>
    <automated>mix test test/mailglass/outbound/telemetry_test.exs test/mailglass/adapters/swoosh_test.exs test/mailglass/tracking/plug_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Swoosh dispatch has one span, tracking telemetry matches ledger truth, failure metadata is bounded/PII-free, and caller-visible tracking behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider response → SendError/DB | Untrusted remote status/body enters application error handling and persistence. |
| Ledger result → telemetry/HTTP | Persistence failure must be observable without leaking data or breaking tracking responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-10 | Denial of Service | provider retry policy | high | mitigate | Closed status/reason classification and worker retry/discard matrix. |
| T-156-11 | Information Disclosure | SendError serialization/persistence | high | mitigate | Allowlist construction plus context/message/JSON/last_error sentinel tests. |
| T-156-12 | Repudiation | dispatch/tracking telemetry | medium | mitigate | Exactly-one span assertions and ledger-outcome-coupled event assertions. |
</threat_model>

<verification>
Run both focused tasks, `mix format --check-formatted`, and `mix compile --no-optional-deps --warnings-as-errors`.
</verification>

<success_criteria>
EXEC-05, EXEC-06, and EXEC-07 are covered by deterministic provider, worker, serialization, persistence, span, and tracking tests with no admin/operator change.
</success_criteria>

<output>
Create `.planning/phases/156-delivery-correctness-and-bounded-execution/156-04-SUMMARY.md` when done.
</output>
