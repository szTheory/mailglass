---
phase: 156-delivery-correctness-and-bounded-execution
plan: 06
type: execute
wave: 6
depends_on: [156-05]
files_modified:
  - lib/mailglass/rate_limiter/atomic_bucket.ex
  - lib/mailglass/rate_limiter/table_owner.ex
  - mailglass_inbound/lib/mailglass_inbound/rate_limiter/table_owner.ex
  - test/mailglass/rate_limiter_test.exs
  - test/mailglass/rate_limiter_supervision_test.exs
  - mailglass_inbound/test/mailglass_inbound/rate_limiter_test.exs
autonomous: true
gap_closure: true
requirements: [EXEC-01]
must_haves:
  truths:
    - "After either rate-limit owner loses and recreates its named ETS table, subsequent callers converge without crashing the owner."
    - "Legitimate contention never turns available tokens into false denials: after restart, one 50-token bucket grants exactly 50 of 100 barrier-released callers in core and inbound."
    - "Contention handling is bounded, never overspends, retains fixed-point refill state, and does not spin indefinitely."
    - "Both TableOwner module docs truthfully describe table ownership, serialized admission/contention fallback, CAS hot-path work, sweeps, and restart behavior."
    - "The combined inbound limiter/execution focused suite remains green across repeated fresh runs."
  artifacts:
    - path: "lib/mailglass/rate_limiter/atomic_bucket.ex"
      provides: "Bounded CAS fast path with correctness-preserving owner fallback under contention"
    - path: "lib/mailglass/rate_limiter/table_owner.ex"
      provides: "Core absent-table recovery plus serialized contended consumption"
    - path: "mailglass_inbound/lib/mailglass_inbound/rate_limiter/table_owner.ex"
      provides: "Inbound parity for lifecycle recovery and contended consumption"
    - path: "test/mailglass/rate_limiter_test.exs"
      provides: "Core restart-plus-100-caller exact-capacity regression"
    - path: "mailglass_inbound/test/mailglass_inbound/rate_limiter_test.exs"
      provides: "Inbound restart-plus-100-caller exact-capacity and combined-suite regression"
  key_links:
    - from: "lib/mailglass/rate_limiter/atomic_bucket.ex"
      to: "lib/mailglass/rate_limiter/table_owner.ex"
      via: "bounded CAS exhaustion delegates one contended consume request to the owner"
      pattern: "consume_contended"
    - from: "mailglass_inbound/lib/mailglass_inbound/rate_limiter/table_owner.ex"
      to: "lib/mailglass/rate_limiter/atomic_bucket.ex"
      via: "inbound owner uses the shared serialized transition after ensuring its package-local table exists"
      pattern: "AtomicBucket"
---

<objective>
Close the remaining EXEC-01 lifecycle/contention gap without changing either public limiter facade or weakening bounded state.

Purpose: Remove deterministic false denials seen in the combined inbound suite while keeping the private fixed-point CAS engine exact, bounded, and fail closed for real exhaustion.
Output: Lifecycle-safe owners, bounded serialized contention fallback, truthful owner docs, restart/concurrency regressions, and repeated combined-suite proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-CONTEXT.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-RESEARCH.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-VALIDATION.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-VERIFICATION.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-01-SUMMARY.md
@lib/mailglass/rate_limiter/atomic_bucket.ex
@lib/mailglass/rate_limiter/table_owner.ex
@mailglass_inbound/lib/mailglass_inbound/rate_limiter/table_owner.ex

<interfaces>
The public compatibility contracts remain `Mailglass.RateLimiter.check/1`, `check/3`, and `MailglassInbound.RateLimiter.check/3`, returning the existing `RateLimitError` shapes. Table names, configuration keys/defaults, telemetry, transactional bypass, bucket ordering, fixed-point scale, one-hour idle expiry, 60-second sweep, and 100,000-key cap remain unchanged. The internal owner message protocol may add a contended-consume request; no new adopter API is introduced.
</interfaces>
</context>

<gap_evidence>
The verifier observed 46 and 49 successes where an integrated inbound 50-capacity run required exactly 50. Isolated execution passed. `AtomicBucket` currently converts its 32nd lost CAS into `:denied`, and both owner `handle_call/3` paths can perform ETS operations while their named table is absent. No overspend was observed; the defect is false denial plus lifecycle fragility.
</gap_evidence>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Recover the core table and consume contended state without false denial</name>
  <files>lib/mailglass/rate_limiter/atomic_bucket.ex, lib/mailglass/rate_limiter/table_owner.ex, test/mailglass/rate_limiter_test.exs, test/mailglass/rate_limiter_supervision_test.exs</files>
  <behavior>
    - Killing the core owner removes the table; after the supervisor registers the replacement owner/table, 100 barrier-released checks against a 50-capacity single constrained bucket yield exactly 50 successes and 50 normal rate-limit errors.
    - Deleting the table immediately before owner admission does not crash the owner; it recreates the table with the canonical ETS options and the next caller succeeds or receives only a genuine limit/capacity denial.
    - A caller that loses the bounded CAS budget is resolved once through the owner from the latest bucket tuple; it is denied only when that tuple has less than one token or bounded key admission genuinely fails.
    - Fractional remainder, monotonic timestamp, last-seen value, maximum key count, and no-overspend invariants survive the fallback.
  </behavior>
  <action>Write the owner-restart plus 100-caller/50-capacity barrier test and absent-table owner-call regression first. Preserve the normal exact-match CAS fast path and its finite retry budget. Replace the terminal contention denial with one `GenServer.call` to a new owner contended-consume operation. In the owner, ensure/recreate the named table with the same canonical options before every admission, contended consume, and sweep ETS access. For the contention fallback, atomically `:ets.take/2` the latest bucket row while the owner mailbox serializes missing-key admission; compute exactly one transition through shared `AtomicBucket` fixed-point logic, then always reinsert the resulting tuple before replying. A caller that read before the take must lose its CAS and re-read/queue, preventing overspend; queued owner calls cannot race the temporary absence. Bound the operation to the existing CAS attempts plus one owner call and one lifecycle recovery attempt—no recursive spin, sleep, random backoff, or unconditional success. Update the core TableOwner docs to describe CAS ownership, serialized admission/contended fallback, sweeping, and table recreation instead of the stale no-callback/update-counter description. Preserve all public APIs and EXEC-02 bounds.</action>
  <verify>
    <automated>mix test test/mailglass/rate_limiter_test.exs test/mailglass/rate_limiter_supervision_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Core restarts safely and deterministically grants exactly the configured 50 tokens under 100-way contention, with no crash, overspend, false contention denial, or unbounded retry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror lifecycle-safe fallback in inbound and prove the combined suite repeatedly</name>
  <files>mailglass_inbound/lib/mailglass_inbound/rate_limiter/table_owner.ex, mailglass_inbound/test/mailglass_inbound/rate_limiter_test.exs</files>
  <behavior>
    - Inbound owner restart/table recreation followed by 100 barrier-released checks against a 50-capacity tenant bucket yields exactly 50 successes on every run.
    - The recipient and sender-domain buckets remain deliberately non-constraining in the regression, so the assertion measures only the contended tenant bucket.
    - The complete inbound limiter/fallback/decoder focused command passes repeatedly in one shell invocation, catching the prior integrated lifecycle interaction.
    - Inbound TableOwner docs state the same engine protocol while preserving its package-local table/config ownership.
  </behavior>
  <action>Add the inbound restart-plus-exact-capacity regression first, using monitored owner replacement, explicit table availability, a ready/go barrier, and deterministic task cleanup rather than sleeps. Mirror core's absent-table `ensure_table` and contended-consume handlers while continuing to read only `:mailglass_inbound` owner configuration and use `:mailglass_inbound_rate_limit`. Update the inbound TableOwner docs to reflect real callbacks, shared CAS, serialized fallback, sweep, and recreation. Run the combined focused inbound files five consecutive times with shell fail-fast behavior so any recurrence fails the task; do not weaken exact counts, allow multiple end states, change public errors, or absorb the Phase 159 repository-format deferral.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; for run in 1 2 3 4 5; do mix test test/mailglass_inbound/rate_limiter_test.exs test/mailglass_inbound/async_execution_test.exs test/mailglass_inbound/mailbox_execution_test.exs test/mailglass_inbound/worker_test.exs --warnings-as-errors || exit 1; done</automated>
  </verify>
  <done>Inbound lifecycle and contention match core exactly, and five repeated combined focused runs each prove 50—not 46/49—successful admissions with all companion execution tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent callers → bucket tuple | Legitimate schedulers compete to update finite capacity; fallback must not over-admit or spuriously deny. |
| Owner lifecycle → named ETS table | A crashed/deleted table can turn routine access into `:badarg` or reset state during admission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-15 | Denial of Service | CAS contention exhaustion | high | mitigate | Finite CAS attempts followed by one owner-serialized latest-state transition and exact-capacity regressions. |
| T-156-16 | Elevation of Privilege | temporary row removal | high | mitigate | `:ets.take/2` occurs inside the serialized owner protocol; stale CAS loses and queued admissions wait until reinsertion. |
| T-156-17 | Denial of Service | absent named table | high | mitigate | Canonical idempotent table creation before every owner ETS operation plus real restart/recreation tests. |
</threat_model>

<verification>
Run the core focused command, the five-run combined inbound command, both no-optional warning-as-error compiles, and `git diff --check`. Repository-wide inbound formatting remains deferred exactly as recorded in verification and is not a closure criterion here.
</verification>

<success_criteria>
EXEC-01 moves from blocked to satisfied: core and inbound survive real owner recreation and repeatedly admit exact configured capacity under contention without overspend, false contention denial, unbounded spinning, public API drift, or UI work.
</success_criteria>

<output>
Create `.planning/phases/156-delivery-correctness-and-bounded-execution/156-06-SUMMARY.md` when done.
</output>
