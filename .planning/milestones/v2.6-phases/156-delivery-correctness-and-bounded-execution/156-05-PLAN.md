---
phase: 156-delivery-correctness-and-bounded-execution
plan: 05
type: execute
wave: 5
depends_on: [156-04]
files_modified:
  - lib/mailglass/webhook/provider_name.ex
  - lib/mailglass/webhook/replay.ex
  - mailglass_inbound/lib/mailglass_inbound/execution.ex
  - mailglass_inbound/lib/mailglass_inbound/execution/worker.ex
  - test/mailglass/webhook/replay_test.exs
  - mailglass_inbound/test/mailglass_inbound/mailbox_execution_test.exs
  - mailglass_inbound/test/mailglass_inbound/worker_test.exs
autonomous: true
requirements: [EXEC-08]
must_haves:
  truths:
    - "Stored webhook provider strings map only to the five supported core provider atoms, and invalid values fail without allocating atoms."
    - "Stored inbound provider strings map only to the four supported inbound provider atoms."
    - "Inbound worker source strings map only to documented source atoms; malformed job args return a closed error/discard outcome."
  artifacts:
    - path: "lib/mailglass/webhook/provider_name.ex"
      provides: "Private finite core provider encoder/decoder"
    - path: "mailglass_inbound/lib/mailglass_inbound/execution.ex"
      provides: "Finite inbound persisted-provider normalization"
    - path: "mailglass_inbound/lib/mailglass_inbound/execution/worker.ex"
      provides: "Finite job-source decoding"
  key_links:
    - from: "lib/mailglass/webhook/replay.ex"
      to: "lib/mailglass/webhook/provider_name.ex"
      via: "replay result decodes stored provider before returning an atom"
      pattern: "ProviderName"
    - from: "mailglass_inbound/lib/mailglass_inbound/execution/worker.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/execution.ex"
      via: "only decoded source/provider values enter execution"
      pattern: "source_from_args"
---

<objective>
Close persisted and job value conversion to explicit finite sets without changing successful provider/source results.

Purpose: Remove atom-table exhaustion paths at database and background-job trust boundaries.
Output: Private finite decoders and atom-count regression tests for core replay and inbound execution.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-CONTEXT.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-RESEARCH.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-VALIDATION.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-04-SUMMARY.md
@lib/mailglass/webhook/replay.ex
@mailglass_inbound/lib/mailglass_inbound/execution.ex
@mailglass_inbound/lib/mailglass_inbound/execution/worker.ex

<interfaces>
Core webhook provider set is `postmark`, `sendgrid`, `mailgun`, `ses`, and `resend`. Inbound provider set is `postmark`, `sendgrid`, `mailgun`, and `ses`. Existing inbound enqueue emits source with `Atom.to_string/1`; supported execution sources must be taken from existing schema/tests and mapped literally, preserving `fresh` as the default only when the field is absent, not when an arbitrary value is supplied.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Decode stored core and inbound providers through finite maps</name>
  <files>lib/mailglass/webhook/provider_name.ex, lib/mailglass/webhook/replay.ex, mailglass_inbound/lib/mailglass_inbound/execution.ex, test/mailglass/webhook/replay_test.exs, mailglass_inbound/test/mailglass_inbound/mailbox_execution_test.exs</files>
  <behavior>
    - D-12: every supported stored provider returns its established atom in replay/message projections.
    - Unknown strings return the path's existing typed/closed failure and never reach downstream normalization.
    - After warming the test path, processing hundreds of unique invalid provider strings leaves `:erlang.system_info(:atom_count)` unchanged.
  </behavior>
  <action>Add supported-value, invalid-value, and warmed atom-count tests before production edits. Create private `Mailglass.Webhook.ProviderName` literal encode/decode clauses for the five core providers and use it in replay result construction, propagating an existing replay failure classification for invalid stored data. Replace inbound persisted-provider conversion with literal clauses for its four supported providers and return `:invalid_job_args`/the established load failure instead of constructing a message with an invalid atom. Preserve successful result shapes and independent package/provider sets; do not sweep UI, installer, or compile-time atom conversions.</action>
  <verify>
    <automated>mix test test/mailglass/webhook/replay_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/mailbox_execution_test.exs --warnings-as-errors)</automated>
  </verify>
  <done>Core replay and inbound persisted-message reconstruction accept exactly their supported providers, reject everything else, and prove invalid strings allocate no atoms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decode inbound job sources as a closed set</name>
  <files>mailglass_inbound/lib/mailglass_inbound/execution/worker.ex, mailglass_inbound/test/mailglass_inbound/worker_test.exs</files>
  <behavior>
    - Every source currently emitted by inbound enqueue round-trips to its established atom.
    - Missing source retains the documented `:fresh` default; unknown/non-binary source fails as invalid job args and does not execute work.
    - A warmed high-cardinality invalid-source loop leaves atom count unchanged.
  </behavior>
  <action>Write the source decision table and atom-count regression first. Replace job `source_from_args/1` conversion with literal pattern clauses for the sources actually emitted by the existing enqueue/replay flows. Return a closed decoding error for unknown values and make `perform/2` stop before `Execution.execute/2`; use the locked Oban invalid/permanent outcome convention established by Plan 156-04 where applicable. Keep job argument names, uniqueness configuration, tenancy wrapper, and successful execution result unchanged.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/worker_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Inbound background execution accepts only documented source strings, rejects corrupt jobs before execution, and cannot grow the atom table from job input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database/job JSON → BEAM atom table | Persisted strings are attacker-influenced and atoms are not garbage-collected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-13 | Denial of Service | provider/source decoding | high | mitigate | Literal finite mappings, closed invalid results, and warmed atom-count regression tests. |
| T-156-14 | Tampering | corrupt stored/job values | medium | mitigate | Stop replay/execution before downstream work when decoding fails. |
</threat_model>

<verification>
Run the core and inbound focused decoder suites and both package warning-as-error compiles.
</verification>

<success_criteria>
EXEC-08 is proven at every identified persisted/job conversion site, with supported values preserved and arbitrary atom allocation eliminated.
</success_criteria>

<output>
Create `.planning/phases/156-delivery-correctness-and-bounded-execution/156-05-SUMMARY.md` when done.
</output>
