---
phase: 156-delivery-correctness-and-bounded-execution
plan: 02
type: execute
wave: 2
depends_on: [156-01]
files_modified:
  - lib/mailglass/optional_deps/oban.ex
  - lib/mailglass/outbound.ex
  - test/mailglass/outbound/deliver_many_test.exs
autonomous: true
requirements: [EXEC-03]
must_haves:
  truths:
    - "A durable batch exposes queued deliveries only after delivery rows, queued events, rendered private metadata, and Oban jobs commit together."
    - "A job-build or job-insert failure rolls back every write from the attempted batch and returns a typed error."
    - "An idempotent replay returns the existing deliveries without inserting another event or job."
  artifacts:
    - path: "lib/mailglass/optional_deps/oban.ex"
      provides: "Optional-dependency-safe Oban insert-all operation for an Ecto.Multi"
    - path: "lib/mailglass/outbound.ex"
      provides: "Single-transaction durable batch orchestration and post-commit return projection"
    - path: "test/mailglass/outbound/deliver_many_test.exs"
      provides: "PostgreSQL/Oban atomicity, rollback, private metadata, and replay proof"
  key_links:
    - from: "lib/mailglass/outbound.ex"
      to: "lib/mailglass/optional_deps/oban.ex"
      via: "the durable batch adds jobs to the same Ecto.Multi before Repo.multi/1"
      pattern: "insert_all"
    - from: "lib/mailglass/outbound.ex"
      to: "Mailglass.Repo"
      via: "one Repo.multi/1 is the sole commit boundary for delivery, event, metadata, and job"
      pattern: "Repo.multi"
---

<objective>
Make the durable batch path atomic from persisted delivery projection through runnable Oban job.

Purpose: Eliminate the false queued result and stranded delivery state caused by committing delivery/event data before unchecked job insertion.
Output: Multi-capable Oban gateway, one-transaction batch dispatch, and rollback/idempotency integration proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-CONTEXT.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-RESEARCH.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-VALIDATION.md
@lib/mailglass/outbound.ex
@lib/mailglass/optional_deps/oban.ex
@test/mailglass/outbound/deliver_many_test.exs

<interfaces>
`Mailglass.Outbound.deliver_many/2` remains `{:ok, [%Delivery{}]} | {:error, Mailglass.Error.t()}`. `Mailglass.Repo.multi/1` is the host-repo transaction facade. Existing `Mailglass.OptionalDeps.Oban.insert/3` adds a single job builder to a Multi; extend the gateway with the corresponding named insert-all Multi form while retaining existing `insert_all/1` compatibility. The current private rendered payload is stored in each delivery's metadata (`rendered_html`, `rendered_text`, `subject`, `headers`); this phase must make that existing write atomic and must not invent a new table.
</interfaces>
</context>

<resolved_research_questions>
- The checkout has no separate private outbound-payload schema. The existing v2 private dispatch payload is the rendered delivery metadata rehydrated by `dispatch_by_id/1`; include that delivery write in the transaction and assert its rollback.
</resolved_research_questions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Commit one durable batch and its jobs through one Ecto.Multi</name>
  <reversibility rating="costly">D-05 changes the durable batch commit boundary, but keeps the published Outbound return contract and existing tables.</reversibility>
  <files>lib/mailglass/optional_deps/oban.ex, lib/mailglass/outbound.ex, test/mailglass/outbound/deliver_many_test.exs</files>
  <behavior>
    - D-05: two valid messages produce two queued delivery rows with rendered private metadata, two queued events, and two `mailglass_outbound` jobs after one successful transaction.
    - A deterministic failure in the named Oban Multi step returns `{:error, %Mailglass.Error{}}` and leaves delivery, event, and job counts unchanged, including no persisted rendered metadata.
    - Mailglass-owned inner Multi operations retain `Repo.multi_opts()` so non-public schema prefixes work.
  </behavior>
  <action>Write the Postgres/Oban tracer and injected failure test before changing production. Extend `Mailglass.OptionalDeps.Oban` with a conditionally safe Multi `insert_all` gateway matching locked Oban 2.23's supported signature and preserving the no-optional-dependencies build. Refactor the durable branch of `deliver_many/2` so it selects Oban before persistence, builds delivery rows, queued event rows, existing rendered private metadata, and job changes inside one `Ecto.Multi`, then calls `Repo.multi/1` once. Build jobs only from rows actually inserted by this transaction. Preserve all prefix options, event/idempotency fields, public return shapes, telemetry spans, and per-message preflight failures. Convert any Multi step failure through the existing `to_error/1` boundary; never acknowledge queued state before the transaction succeeds.</action>
  <verify>
    <automated>mix test test/mailglass/outbound/deliver_many_test.exs --warnings-as-errors</automated>
  </verify>
  <done>A real durable batch commits every required record and job together, while an induced job-step failure proves complete rollback and an honest typed error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve replay idempotency inside the atomic path</name>
  <files>lib/mailglass/outbound.ex, test/mailglass/outbound/deliver_many_test.exs</files>
  <behavior>
    - Replaying an identical durable batch returns the existing delivery projections in input correspondence without adding queued events or jobs.
    - A mixed replay/new batch creates an event and job only for the newly inserted row while returning both existing and new deliveries.
    - The returned delivery list remains deterministic and does not derive queue success from a discarded side-effect result.
  </behavior>
  <action>Add replay and mixed-batch tests before refining the transaction. Use the rows returned from `insert_all(..., returning: true)` as the sole source for event and job creation, then fetch the complete result set after commit for the established idempotent response. Maintain input-result association when inputs mix conflicts and inserts. Remove the post-commit durable `enqueue_batch_jobs/1` branch once no caller uses it; leave Task-supervisor routing available for Plan 156-03. Do not change single-delivery `deliver_later/2`, delivered content, recipient cardinality, or any admin/operator surface.</action>
  <verify>
    <automated>mix test test/mailglass/outbound/deliver_many_test.exs test/mailglass/outbound/delivery_idempotency_key_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Identical and mixed durable batch replays are atomic, deterministic, and do not duplicate events or jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mailglass transaction → Oban tables | The host repository must commit framework and queue state as one database unit. |
| Idempotent input → persisted queue | Replays must not create duplicate delivery attempts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-04 | Repudiation | durable batch acknowledgement | high | mitigate | Return queued only from a successful Multi containing the named Oban step. |
| T-156-05 | Tampering | batch rollback/prefix | high | mitigate | Use one host-repo transaction and retain `Repo.multi_opts()` on every Mailglass write. |
| T-156-06 | Denial of Service | idempotent replay | medium | mitigate | Generate events/jobs only for transaction-inserted rows and test mixed conflicts. |
</threat_model>

<verification>
Run the focused durable-batch tests with PostgreSQL and Oban available, then run `mix compile --no-optional-deps --warnings-as-errors` to prove the optional gateway remains safe.
</verification>

<success_criteria>
EXEC-03 is proven by successful atomic persistence, deterministic job-step rollback, and no duplicate replay enqueue, with the existing additive v2 API intact.
</success_criteria>

<output>
Create `.planning/phases/156-delivery-correctness-and-bounded-execution/156-02-SUMMARY.md` when done.
</output>
