---
phase: 155-restore-adopter-and-ci-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/migration_generator.ex
  - lib/mix/tasks/mailglass.gen.migration.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex
  - test/mix/tasks/mailglass_gen_migration_test.exs
  - mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs
autonomous: true
requirements: [ADOPT-01, ADOPT-02, ADOPT-03, ADOPT-04]
must_haves:
  truths:
    - "Fresh core and inbound generation emits real up/0 and down/0 package wrappers, never copied or toy DDL."
    - "A caller can select a configured Ecto repo explicitly, while inference works only for exactly one configured repo."
    - "Offline upgrade generation writes a new file, preserves existing migration bytes, and rejects --upgrade --from 0 with guidance to use initial generation."
  artifacts:
    - path: "lib/mailglass/migration_generator.ex"
      provides: "Shared additive generator contract for repo resolution, source rendering, and collision-safe writes"
    - path: "test/mix/tasks/mailglass_gen_migration_test.exs"
      provides: "Core fresh/offline-upgrade CLI acceptance matrix"
    - path: "mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs"
      provides: "Inbound parity acceptance matrix"
  key_links:
    - from: "lib/mix/tasks/mailglass.gen.migration.ex"
      to: "lib/mailglass/migration_generator.ex"
      via: "package specification passed to the shared generator"
      pattern: "MigrationGenerator"
    - from: "mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex"
      to: "lib/mailglass/migration_generator.ex"
      via: "inbound depends on core and uses the same option/repo contract"
      pattern: "Mailglass.MigrationGenerator"
---

<objective>
Establish the production-quality tracer for migration generation: one configured repo can receive genuine core and inbound wrappers, and an offline upgrade can be generated without altering migration history.

Purpose: Replace the false toy/regex generator path with the smallest shared additive contract that later live-upgrade, repair, and generated-host proof can extend.
Output: Shared generator seam, corrected core/inbound Mix tasks, and focused CLI tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@lib/mailglass/migration.ex
@mailglass_inbound/lib/mailglass_inbound/migration.ex
@lib/mix/tasks/mailglass.gen.migration.ex
@mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex

<interfaces>
Existing public wrapper calls are `Mailglass.Migration.up/1`, `Mailglass.Migration.down/1`, `MailglassInbound.Migration.up/1`, and `MailglassInbound.Migration.down/1`, each with zero-arity defaults. Core current schema version is exposed by `Mailglass.Migrations.Postgres.current_version/0`; inbound exposes the same function from its runner.
</interfaces>
</context>

<resolved_research_questions>
- `--upgrade --from 0` is invalid. Version zero means the package anchor is absent, so the caller must run initial generation without `--upgrade`; no upgrade file is written.
- Legacy recognition is not implemented in this plan and may not infer from table name or semantics; Plan 155-04 is bound to the exact historical generator bytes only.
</resolved_research_questions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate fresh core and inbound wrappers for one real configured repo</name>
  <files>lib/mailglass/migration_generator.ex, lib/mix/tasks/mailglass.gen.migration.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex, test/mix/tasks/mailglass_gen_migration_test.exs, mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs</files>
  <behavior>
    - One configured `:ecto_repos` entry is inferred and its existing module atom determines both the Ecto migration namespace and migrations directory.
    - `--repo MyApp.Repo` selects that repo only when it is already present in the configured repo list; zero repos, multiple repos without the flag, or an unconfigured string fail before any write and do not create atoms from CLI input.
    - Core source delegates `up/0` and `down/0` to `Mailglass.Migration`; inbound delegates to `MailglassInbound.Migration`; both contain no package table DDL.
    - Re-running initial generation reports unchanged and preserves the existing wrapper byte-for-byte.
  </behavior>
  <action>Create `Mailglass.MigrationGenerator` as an internal core-owned helper because inbound already depends on core and core must never depend on inbound. Give it an explicit package specification containing the task name, install suffix, wrapper module suffix, migration facade, and current-version callback; keep filesystem/repo selection generic. Resolve `Mix.Project.config()[:app]`, read that host application's `:ecto_repos`, compare `--repo` text against `inspect/1` of existing configured modules, and use Ecto's repo migration-path convention instead of the current `mix.exs` regex. Change both tasks to strict parsing of the additive flag vocabulary while this task executes initial mode only. Initial mode must emit the documented stable wrappers and treat an existing install wrapper as immutable/idempotent. Preserve package direction: inbound may call core's helper, never the reverse. Add tests first using isolated temporary Mix projects/config and deterministic clock/path injection so assertions do not sleep or depend on wall-clock ordering.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass_gen_migration_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix test test/mix/tasks/mailglass_inbound_gen_migration_test.exs --warnings-as-errors)</automated>
  </verify>
  <done>Fresh core and inbound tasks generate the real package wrappers for an explicitly selected or uniquely inferable configured repo, all ambiguous/unconfigured cases write nothing, and reruns leave existing files unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add collision-safe offline upgrade generation and the zero-version refusal</name>
  <files>lib/mailglass/migration_generator.ex, lib/mix/tasks/mailglass.gen.migration.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex, test/mix/tasks/mailglass_gen_migration_test.exs, mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs</files>
  <behavior>
    - `--upgrade --from N` accepts only an integer in each package's `initial_version..(current_version - 1)` range and generates a distinct timestamped upgrade file.
    - The generated `up/0` targets the package current version and `down/0` bakes the validated prior version into `down(version: N)`.
    - `--upgrade --from 0` is invalid and tells the caller to run initial generation without `--upgrade`.
    - `--from` without `--upgrade`, non-integers, negative/current/future versions, mutually exclusive operation flags, and timestamp collisions fail before changing any file.
  </behavior>
  <action>Expand the shared operation validator and both strict Mix tasks with offline upgrade mode. Generate a fresh suffix such as `mailglass_upgrade` / `mailglass_inbound_upgrade`; do not search for, replace, or rewrite an install or prior upgrade file. Render the prior version as a validated integer literal in the rollback call. Lock the approved final choice that version zero is not an upgrade source: return actionable initial-generation guidance. Since inbound is currently at schema version 1, its offline upgrade range is empty and every `--from` value must fail honestly until a later inbound schema version exists; do not invent a fake upgrade. Keep `--repair-legacy` recognized but fail with a message that the repair path is not available until Plan 155-04 rather than interpreting it as another operation.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass_gen_migration_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_inbound &amp;&amp; mix test test/mix/tasks/mailglass_inbound_gen_migration_test.exs --warnings-as-errors) &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Offline upgrades are additive and reversible to the validated prior version; every invalid combination—including `--upgrade --from 0`—fails before writes with precise next-step guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI text → configured repo module | Untrusted strings must select an existing configured module without atom creation or guessing. |
| Generator → host migration directory | Writes must never alter adopter-owned/applied history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-01 | Tampering | repo selection | high | mitigate | Match text only against configured repo module strings and fail on zero/many/unrecognized candidates. |
| T-155-02 | Tampering | migration writer | high | mitigate | Use exclusive collision checks and immutable initial/upgrade behavior with byte-preservation tests. |
| T-155-03 | Elevation of Privilege | CLI module input | medium | mitigate | Never convert arbitrary `--repo` text into a new atom. |
</threat_model>

<verification>
Run both focused task suites and inspect their temporary-host fixtures to prove exact source, repo ambiguity failure, no-write invalid cases, and byte-preserving reruns.
</verification>

<success_criteria>
ADOPT-01/02 and the offline portions of ADOPT-03/04 are executable through both public Mix tasks with no shipped migration edits and no admin changes.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-01-SUMMARY.md` when done.
</output>
