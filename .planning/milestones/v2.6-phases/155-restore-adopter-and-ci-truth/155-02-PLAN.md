---
phase: 155-restore-adopter-and-ci-truth
plan: 02
type: execute
wave: 2
depends_on: [155-01]
files_modified:
  - lib/mailglass/migration_version_error.ex
  - lib/mailglass/migration.ex
  - lib/mailglass/migrations/postgres.ex
  - test/mailglass/migration_test.exs
  - mailglass_inbound/lib/mailglass_inbound/migration.ex
  - mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex
  - mailglass_inbound/test/mailglass_inbound/migrations_test.exs
autonomous: true
requirements: [ADOPT-06]
must_haves:
  truths:
    - "Migration version zero is returned only when the package anchor relation is genuinely absent."
    - "Query errors, missing or malformed comments, unexpected result shapes, and impossible version ranges fail closed and remain distinguishable."
    - "Core and inbound retain independent anchors and identical outcome semantics."
  artifacts:
    - path: "lib/mailglass/migration_version_error.ex"
      provides: "Typed additive failure carrying package, repo/prefix context, and reason"
    - path: "lib/mailglass/migrations/postgres.ex"
      provides: "Fail-closed core catalog classification"
    - path: "mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex"
      provides: "Fail-closed inbound catalog classification"
  key_links:
    - from: "lib/mailglass/migration.ex"
      to: "lib/mailglass/migrations/postgres.ex"
      via: "public migrated_version/1 preserves absent=0 and raises typed failures"
      pattern: "migrated_version"
    - from: "mailglass_inbound/lib/mailglass_inbound/migration.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex"
      via: "independent inbound anchor classification"
      pattern: "migrated_version"
---

<objective>
Make package migration metadata tell the truth: absence remains version zero, while corruption, impossible metadata, and database faults stop the caller.

Purpose: Live upgrade and repair must never mistake an unavailable or corrupt database for a fresh installation.
Output: Shared typed error plus core and inbound fail-closed version readers with real and injected-result coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-01-SUMMARY.md
@lib/mailglass/migration.ex
@lib/mailglass/migrations/postgres.ex
@test/mailglass/migration_test.exs
@mailglass_inbound/lib/mailglass_inbound/migration.ex
@mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex
@mailglass_inbound/test/mailglass_inbound/migrations_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the core anchor reader distinguish absence from failure</name>
  <files>lib/mailglass/migration_version_error.ex, lib/mailglass/migration.ex, lib/mailglass/migrations/postgres.ex, test/mailglass/migration_test.exs</files>
  <behavior>
    - No catalog row for `mailglass_events` returns 0.
    - A single numeric comment in `initial_version..current_version` returns that integer.
    - Nil/blank/non-numeric comments, multiple/unexpected rows, negative/zero/out-of-range comments, and repo query errors raise `Mailglass.MigrationVersionError` with a stable reason.
    - Existing `up/1` and `down/1` propagate those failures without executing DDL.
  </behavior>
  <action>Add an additive `Mailglass.MigrationVersionError` exception with reason atoms suitable for assertions and a composed, PII-free actionable message. Refactor the core Postgres reader into an explicit three-way outcome internally—absent, valid version, or error—while preserving the public `migrated_version/1` success type (`0` or valid integer) and raising the typed error for every non-absence failure. Match exact Postgrex result shapes, parse with `Integer.parse/1` rather than a partial conversion, and validate the complete range. Add an injected query-result seam for exhaustive unit cases plus live scratch-schema tests proving absent versus valid comment; do not rescue database exceptions into absence. Update public docs/spec text so zero no longer promises to cover errors.</action>
  <verify>
    <automated>mix test test/mailglass/migration_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Core returns zero only for a verified missing anchor and stops before DDL for every malformed, impossible, or unavailable catalog state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same fail-closed contract to the independent inbound anchor</name>
  <files>mailglass_inbound/lib/mailglass_inbound/migration.ex, mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex, mailglass_inbound/test/mailglass_inbound/migrations_test.exs</files>
  <behavior>
    - No catalog row for `mailglass_inbound_records` returns 0.
    - Valid inbound metadata returns the independent inbound version.
    - Every malformed/unavailable/impossible result raises the shared typed error tagged for inbound.
    - Core anchor presence cannot satisfy or contaminate inbound classification.
  </behavior>
  <action>Mirror the tested core classification in inbound while preserving its separate `mailglass_inbound_records` anchor and package identity. Reuse the core exception because inbound already depends on core, but keep the query and version range in the inbound runner. Add cross-anchor and exhaustive injected/live cases to `migrations_test.exs`; retain all existing Ecto.Migrator up/down transition assertions.</action>
  <verify>
    <automated>(cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/migrations_test.exs --warnings-as-errors) &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Core and inbound expose the same fail-closed metadata semantics while retaining independent anchors and current-version bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL catalog → migration control flow | Catalog rows/comments and query errors decide whether destructive DDL may run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-04 | Tampering | migrated_version readers | high | mitigate | Exact result-shape parsing, full integer/range validation, and typed fail-closed propagation. |
| T-155-05 | Repudiation | error classification | medium | mitigate | Preserve distinct stable reason atoms so absence cannot be confused with query or metadata failure. |
| T-155-06 | Information Disclosure | exception messages | low | mitigate | Include package/reason and actionable guidance without query payloads, credentials, or row contents. |
</threat_model>

<verification>
Focused core and inbound suites cover every result class, real absent/present catalog states, and no-DDL propagation.
</verification>

<success_criteria>
ADOPT-06 is satisfied for both package anchors without changing any shipped migration.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-02-SUMMARY.md` when done.
</output>
