---
phase: 155-restore-adopter-and-ci-truth
plan: 05
type: execute
wave: 5
depends_on: [155-03, 155-04]
files_modified:
  - scripts/generated_ecto_host_proof.sh
  - test/scripts/generated_ecto_host_proof_test.exs
  - .github/workflows/ci.yml
  - test/scripts/required_checks_test.exs
autonomous: true
requirements: [ADOPT-01, ADOPT-02, ADOPT-03, ADOPT-05, ADOPT-06]
must_haves:
  truths:
    - "A stock generated Phoenix/Ecto/Postgres host invokes both real Mix generators, migrates their wrappers, and persists a delivery through its own Repo."
    - "The same host rolls generated package wrappers back and proves package relations are removed without repo-local TestRepo or hand-written package DDL."
    - "The existing required Installer Host Smoke lane runs this proof without changing its public identity or adding a new lane."
  artifacts:
    - path: "scripts/generated_ecto_host_proof.sh"
      provides: "Deterministic real Ecto adopter journey"
    - path: "test/scripts/generated_ecto_host_proof_test.exs"
      provides: "Anti-vacuity contract preventing no-Ecto, handwritten-DDL, or compile-only regression"
    - path: ".github/workflows/ci.yml"
      provides: "Existing installer lane Postgres service and real-host proof invocation"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/generated_ecto_host_proof.sh"
      via: "Installer Host Smoke step with Postgres service and path dependencies"
      pattern: "generated_ecto_host_proof"
---

<objective>
Turn the generator contract into real adopter evidence inside a generated Ecto host and the existing required installer lane.

Purpose: Unit/golden tests cannot prove Ecto executes generated wrappers, persists real data, or reverses schema changes.
Output: Generated-host script, anti-vacuity contract, and existing-lane wiring.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-03-SUMMARY.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-04-SUMMARY.md
@scripts/consumer_install_smoke.sh
@.github/workflows/ci.yml
@test/scripts/required_checks_test.exs
@lib/mailglass/outbound/delivery.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove generated wrappers migrate, persist, and roll back in a stock Ecto host</name>
  <precondition>PostgreSQL is reachable through DATABASE_URL and `mix phx.new` is available or installable by the existing CI setup.</precondition>
  <files>scripts/generated_ecto_host_proof.sh, test/scripts/generated_ecto_host_proof_test.exs</files>
  <behavior>
    - The script creates a stock Phoenix host with Ecto enabled and a real host Repo; it does not use `--no-ecto`, `Mailglass.TestRepo`, or handwritten package DDL.
    - The generated host explicitly configures `config :mailglass, repo: Host.Repo` and `config :mailglass_inbound, repo: Host.Repo`; no package-global test repo or inferred fallback owns execution.
    - It adds path dependencies for core and inbound, runs both public generators with `--repo`, runs `mix ecto.migrate`, and asserts both version anchors report their current versions.
    - It persists a valid `Mailglass.Outbound.Delivery` through `Host.Repo`, reloads/counts it from Postgres, rolls inbound and core wrappers back through `Host.Repo`, and asserts package relations are absent.
  </behavior>
  <action>Create a strict, temp-directory-isolated shell journey parallel to—not a replacement for—the existing no-Ecto preview smoke. Generate a Phoenix app with Ecto, inject local path dependencies without copying repository fixtures, configure `Host.Repo` from `DATABASE_URL`, and write both `config :mailglass, repo: Host.Repo` and `config :mailglass_inbound, repo: Host.Repo` before compilation or task execution. Invoke both public migration tasks with `--repo Host.Repo`, then use normal `mix ecto.migrate -r Host.Repo` and matching `mix ecto.rollback -r Host.Repo` commands so the generated host repo is explicit throughout. Persist a delivery with `Host.Repo` and the real Mailglass schema/changeset, then query it back before rollback. Assert generated source delegates to package façades and contains no copied package table definitions. Add an ExUnit source-contract test that pins both package config entries, explicit task repo flags, Host.Repo migrate/rollback commands, persistence/reload, and relation-absence checks; mutation-based negative controls must fail when any anchor is removed.</action>
  <verify>
    <automated>mix test test/scripts/generated_ecto_host_proof_test.exs --warnings-as-errors &amp;&amp; MAILGLASS_PATH="$PWD" bash scripts/generated_ecto_host_proof.sh</automated>
  </verify>
  <done>A newly generated Ecto host executes both package-generated wrappers against Postgres, persists and reloads a delivery, and rolls the generated migrations back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the real Ecto proof inside the existing Installer Host Smoke lane</name>
  <files>.github/workflows/ci.yml, test/scripts/required_checks_test.exs, test/scripts/generated_ecto_host_proof_test.exs</files>
  <behavior>
    - `installer_host_smoke` retains display name `Installer Host Smoke` and remains a required leaf.
    - The job provisions PostgreSQL 16 with a health check and invokes both the existing preview/install smoke and the new Ecto-host proof.
    - Removing the proof step, disabling Ecto, or changing the required lane identity fails a root meta-test.
  </behavior>
  <action>Add a Postgres 16 service and bounded health settings to the existing `installer_host_smoke` job, then add the generated Ecto proof as another step using the working tree path and service URL. Preserve the job id, display name, required-lane registry, and existing consumer smoke unchanged. Extend required-check/source contracts to assert the job contains both proofs and that the Ecto proof is not guarded away for code changes. Do not create a new job or touch admin UI/code.</action>
  <verify>
    <automated>mix test test/scripts/generated_ecto_host_proof_test.exs test/scripts/required_checks_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>The protected installer leaf now proves both existing install/boot behavior and real Ecto migration/persistence/rollback behavior under one unchanged identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated host → local package source | The proof must consume public tasks/facades, not repository-only fixtures. |
| Generated Repo → CI Postgres service | Real persistence and DDL must be observed through the host's own Ecto configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-13 | Repudiation | generated-host proof | high | mitigate | Anti-vacuity contract pins generation, migrate, persistence reload, rollback, and absence assertions. |
| T-155-14 | Tampering | proof dependencies | medium | mitigate | Use path deps into the working tree and ban repo-local TestRepo/handwritten DDL in the contract. |
| T-155-15 | Denial of Service | CI Postgres/host setup | low | mitigate | Existing job timeout plus service health check and temp cleanup bound failure. |
</threat_model>

<verification>
Run the source-contract test locally and the complete generated-host script against Postgres; CI repeats it in the preserved required installer lane.
</verification>

<success_criteria>
ADOPT-01 has real Ecto evidence, and the generated-host path exercises the repo/generator/version contracts from ADOPT-02/03/05/06 without admin changes.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-05-SUMMARY.md` when done.
</output>
