---
phase: 155-restore-adopter-and-ci-truth
plan: 03
type: execute
wave: 3
depends_on: [155-01, 155-02]
files_modified:
  - lib/mailglass/migration_generator.ex
  - lib/mailglass/migration.ex
  - lib/mix/tasks/mailglass.gen.migration.ex
  - mailglass_inbound/lib/mailglass_inbound/migration.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex
  - test/mix/tasks/mailglass_gen_migration_test.exs
  - mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs
autonomous: true
requirements: [ADOPT-03, ADOPT-04, ADOPT-06]
must_haves:
  truths:
    - "Live --upgrade starts the selected repo, reads honest package metadata, and creates a fresh rollback-aware wrapper only from a valid older applied version."
    - "Absent, current, malformed, impossible, and query-failure live states write no migration."
    - "Offline --from and live inspection cannot conflict or silently override one another."
  artifacts:
    - path: "lib/mailglass/migration_generator.ex"
      provides: "Live/offline prior-version resolution with one validated outcome"
    - path: "test/mix/tasks/mailglass_gen_migration_test.exs"
      provides: "Core live-upgrade acceptance and failure matrix"
    - path: "mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs"
      provides: "Inbound live-upgrade parity and current-version refusal"
  key_links:
    - from: "lib/mailglass/migration_generator.ex"
      to: "lib/mailglass/migration.ex"
      via: "selected repo passed to migrated_version/1 and used by the facade for adapter dispatch and catalog query under Ecto.Migrator.with_repo/3"
      pattern: "repo.*migrated_version|migrator"
---

<objective>
Complete upgrade generation by connecting the generator to honest live metadata without weakening the offline contract or rewriting history.

Purpose: Adopters need a deterministic live upgrade path whose rollback target is the version actually applied.
Output: Live core/inbound upgrade wiring and no-write failure coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-01-SUMMARY.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-02-SUMMARY.md
@lib/mailglass/migration_generator.ex
@lib/mailglass/migration.ex
@mailglass_inbound/lib/mailglass_inbound/migration.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive a live core upgrade from the selected repo's applied version</name>
  <files>lib/mailglass/migration_generator.ex, lib/mailglass/migration.ex, lib/mix/tasks/mailglass.gen.migration.ex, test/mix/tasks/mailglass_gen_migration_test.exs</files>
  <behavior>
    - `--upgrade` without `--from` starts/uses the selected repo, reads the core version, and writes a new wrapper whose rollback target is that version.
    - When `config :mailglass, repo: OtherRepo` differs from `--repo Host.Repo`, `Host.Repo` is authoritative for adapter resolution and the migrated-version query; `OtherRepo` is not called.
    - A verified absent anchor emits initial-generation guidance and writes nothing.
    - Current-version, malformed metadata, impossible metadata, startup/query failure, and file collision all write nothing and preserve every existing migration byte.
  </behavior>
  <action>Use `Ecto.Migrator.with_repo/3` around the selected configured repo for live inspection and pass that exact repo into `Mailglass.Migration.migrated_version/1`; do not fall back to package-global repo configuration. Change the public facade's internal adapter selection to resolve from the explicit `:repo` in the provided options before considering configured defaults, and pass the same repo to the Postgres reader, so one repo owns startup, adapter choice, and query. Preserve existing zero-arity/configured-repo behavior when no explicit repo is supplied. Normalize the live result through the same prior-version range validator as offline mode. Add a test with `config :mailglass, repo: OtherRepo` and selected `Host.Repo` whose adapters/query spies differ, proving only `Host.Repo` chooses the runner and receives the catalog query; also cover injected startup/query failures deterministically.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass_gen_migration_test.exs --warnings-as-errors</automated>
  </verify>
  <done>A valid older core installation produces one new rollback-aware upgrade wrapper; every other live state leaves the migration directory unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give inbound the identical live-upgrade contract</name>
  <files>lib/mailglass/migration_generator.ex, mailglass_inbound/lib/mailglass_inbound/migration.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex, mailglass_inbound/test/mix/tasks/mailglass_inbound_gen_migration_test.exs</files>
  <behavior>
    - Inbound live inspection targets the independent inbound anchor through the selected repo.
    - When `config :mailglass_inbound, repo: OtherRepo` differs from `--repo Host.Repo`, `Host.Repo` is authoritative for inbound adapter resolution and catalog query.
    - At inbound current version 1, absent and current states both refuse upgrade for distinct reasons and create no file.
    - Future older valid inbound states use the same rendered rollback contract without changing the task API.
  </behavior>
  <action>Wire the inbound package callbacks into the live prior-version resolver and update the inbound facade with the same explicit-repo-first adapter/query seam as core. Test independent anchor selection with application config pointing at a different repo and prove the selected repo alone determines the adapter and receives the query. Preserve today’s honest empty upgrade range: do not manufacture a migration when current version equals initial version. Test that `--from` remains the only offline path, that `--from 0` stays invalid, and that live faults propagate as actionable no-write failures.</action>
  <verify>
    <automated>(cd mailglass_inbound &amp;&amp; mix test test/mix/tasks/mailglass_inbound_gen_migration_test.exs --warnings-as-errors) &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Both packages share one live/offline upgrade contract and never convert absence or failure into an upgrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selected host repo → generated rollback literal | Live database state controls generated migration behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-07 | Tampering | live upgrade resolver | high | mitigate | Reuse validated configured repo and typed version reader; reject every non-older outcome before writes. |
| T-155-08 | Repudiation | offline/live mode selection | medium | mitigate | Strict mutually exclusive operation/source matrix with explicit tests and messages. |
| T-155-09 | Denial of Service | repo startup/query | low | mitigate | Bounded Ecto startup/query path with immediate propagated failure and no filesystem mutation. |
</threat_model>

<verification>
Focused Mix-task suites prove valid live core generation, honest inbound no-op range, and byte-identical history on every failure.
</verification>

<success_criteria>
ADOPT-03/04 live and offline paths agree on one validated prior version, and ADOPT-06 failures stop generation.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-03-SUMMARY.md` when done.
</output>
