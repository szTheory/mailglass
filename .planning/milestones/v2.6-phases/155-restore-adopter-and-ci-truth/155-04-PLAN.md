---
phase: 155-restore-adopter-and-ci-truth
plan: 04
type: execute
wave: 4
depends_on: [155-02, 155-03]
files_modified:
  - lib/mailglass/migration.ex
  - lib/mailglass/migration_generator.ex
  - lib/mailglass/migrations/legacy_toy.ex
  - lib/mix/tasks/mailglass.gen.migration.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex
  - test/mix/tasks/mailglass_legacy_repair_test.exs
autonomous: true
requirements: [ADOPT-05, ADOPT-06]
must_haves:
  truths:
    - "Repair recognizes only the exact byte-for-byte historical core toy generator output, including its host-app-derived `App.Repo.Migrations.MailglassInstall` module."
    - "Repair refuses ambiguous source, altered catalog shape, any populated toy table, unavailable metadata, and every inbound invocation before writing or executing destructive work."
    - "A valid repair is additive: it preserves the legacy file, creates a new timestamped migration, upgrades to the real schema, and rolls back to the exact toy state."
  artifacts:
    - path: "lib/mailglass/migrations/legacy_toy.ex"
      provides: "Exact-source/catalog/emptiness preflight and reversible toy-to-package migration operations"
    - path: "test/mix/tasks/mailglass_legacy_repair_test.exs"
      provides: "Real Postgres positive proof and destructive negative controls"
  key_links:
    - from: "lib/mix/tasks/mailglass.gen.migration.ex"
      to: "lib/mailglass/migrations/legacy_toy.ex"
      via: "repair preflight before additive wrapper creation"
      pattern: "LegacyToy"
    - from: "generated repair wrapper"
      to: "lib/mailglass/migration.ex"
      via: "additive repair up/down facade preserving rollback"
      pattern: "repair_legacy"
---

<objective>
Provide the single approved repair route for the historical core toy migration while refusing every ambiguous or populated state.

Purpose: Recover known early adopter output without ever guessing at provenance or destroying real data.
Output: Exact legacy recognizer, reversible repair operations, CLI wiring, and hostile Postgres tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-02-SUMMARY.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-03-SUMMARY.md
@lib/mix/tasks/mailglass.gen.migration.ex
@lib/mailglass/migration.ex
@lib/mailglass/migrations/postgres.ex
</context>

<resolved_research_questions>
- The sole recognized legacy signature is the exact byte sequence produced by the current pre-Phase-155 core generator, including the old app-name-derived `App.Repo.Migrations.MailglassInstall` module, whitespace, newlines, `change/0`, and body. Any variation is ambiguous and repair refuses it.
- `--upgrade --from 0` remains invalid here as well; repair is a distinct explicit operation and cannot be used to reinterpret an absent anchor as an upgrade.
</resolved_research_questions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair one exact empty core toy installation and roll it back</name>
  <reversibility rating="costly">The additive wrapper is reversible, but changing its shipped source later would require coordinated adopter migration handling.</reversibility>
  <files>lib/mailglass/migrations/legacy_toy.ex, lib/mailglass/migration.ex, lib/mailglass/migration_generator.ex, lib/mix/tasks/mailglass.gen.migration.ex, test/mix/tasks/mailglass_legacy_repair_test.exs</files>
  <behavior>
    - The only accepted source is the byte-exact historical body emitted by the pre-Phase-155 core generator: `App.Repo.Migrations.MailglassInstall` derived by the old `mix.exs` app-name rule, `change/0`, `mailglass_events`, `tenant_id :string`, and `timestamps(type: :utc_datetime_usec)` with the generator's exact whitespace/newlines.
    - The live table must match the exact expected column/type/null/default/index/constraint signature and contain zero rows.
    - Successful generation preserves the toy source file and writes a distinct repair wrapper; running it up creates the current real package schema and running it down restores the exact empty toy schema.
  </behavior>
  <action>Create a core-owned `Mailglass.Migrations.LegacyToy` seam with separate source fingerprint, catalog signature, zero-row preflight, and Ecto migration operations. Preserve the literal historical generator template before replacing it: derive its module by the old host-app-name rule (`App.Repo.Migrations.MailglassInstall`), require the explicitly selected/inferred repo to be that same historical `App.Repo`, and accept no AST-equivalent, whitespace-normalized, renamed, expanded, alternate-repo, or hand-authored variant. The task must start that configured repo, validate source and live state completely, then and only then write a new timestamped repair wrapper calling additive `Mailglass.Migration.repair_legacy_up/0` and `repair_legacy_down/0` functions. Up must use restrictive, fully qualified operations to remove only the proven empty toy relation and run the package migration; down must roll the package to zero before reconstructing only the exact toy relation. Never edit/delete the historical source and never use cascade behavior. Test the complete up/down cycle against a scratch Postgres schema.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass_legacy_repair_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The exact empty historical toy can be repaired and rolled back through a new migration while its original file remains byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove repair refuses every ambiguous, populated, or unsupported state</name>
  <files>lib/mailglass/migrations/legacy_toy.ex, lib/mailglass/migration_generator.ex, lib/mix/tasks/mailglass.gen.migration.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.gen.migration.ex, test/mix/tasks/mailglass_legacy_repair_test.exs</files>
  <behavior>
    - Any byte change to the historical source, multiple candidate files, missing file, selected repo other than the old app-derived `App.Repo`, unexpected column/default/index/constraint, or populated row fails and writes nothing.
    - Query/startup failure, missing/malformed metadata, and an already-real package anchor fail distinctly and write nothing.
    - `mix mailglass.inbound.gen.migration --repair-legacy` always refuses because no recognized inbound toy signature exists.
  </behavior>
  <action>Add table-driven negative controls that snapshot the migration directory and catalog before invocation and assert both remain unchanged after every refusal. Include a populated table containing a sentinel row and prove it survives. Make source ambiguity and database ambiguity separate actionable error reasons. Lock the approved final choice that this exact core body is the sole recognized legacy signature; do not broaden recognition based on filename, table name, parsed semantics, or plausible released variants. Wire inbound's recognized flag to an explicit unsupported/no-known-signature error rather than ignoring it.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass_legacy_repair_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Every non-exact or unsafe repair attempt fails closed, preserves data/files, and explains whether source, catalog, population, metadata, or package support blocked it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host migration source + live catalog → destructive repair | Both independent proofs must agree before any file or DDL mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-10 | Tampering | legacy recognizer | critical | mitigate | Require byte-exact source, exact catalog signature, and zero rows; fail on any ambiguity. |
| T-155-11 | Denial of Service | repair rollback | medium | mitigate | Fully qualified RESTRICT operations and tested up/down reconstruction with no cascade. |
| T-155-12 | Repudiation | repair errors | medium | mitigate | Stable distinct reasons and sentinel-preservation negative controls prove no destructive attempt occurred. |
</threat_model>

<verification>
The focused real-Postgres suite must prove one exact success and a mutation matrix in which every ambiguous/populated/fault state preserves both source bytes and sentinel data.
</verification>

<success_criteria>
ADOPT-05 is satisfied only for the approved historical core signature, and ADOPT-06 failure classes remain fail-closed throughout repair.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-04-SUMMARY.md` when done.
</output>
