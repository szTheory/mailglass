---
phase: 155-restore-adopter-and-ci-truth
plan: 06
type: execute
wave: 6
depends_on: [155-05]
files_modified:
  - scripts/ci_green_policy.sh
  - test/scripts/ci_green_policy_test.exs
  - .github/workflows/ci.yml
  - test/scripts/required_checks_test.exs
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "The protected public check remains named CI Green."
    - "CI Green fails unless change detection itself succeeds and reports exactly code=true or code=false."
    - "For code=true every required leaf must be exactly success; a skipped, failed, cancelled, missing, or unknown result fails."
    - "Only successful code=false classification permits required leaves to be skipped; failure/cancellation/unknown results still fail."
  artifacts:
    - path: "scripts/ci_green_policy.sh"
      provides: "Deterministic fail-closed aggregate decision table"
    - path: "test/scripts/ci_green_policy_test.exs"
      provides: "Executable detector/code/docs negative and positive controls"
    - path: ".github/workflows/ci.yml"
      provides: "CI Green direct dependency on changes and exact policy inputs"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci_green_policy.sh"
      via: "changes result/output plus every required leaf result"
      pattern: "ci_green_policy"
    - from: "test/scripts/required_checks_test.exs"
      to: "test/support/ci_lanes.ex"
      via: "needs minus structural changes dependency set-equals required leaves"
      pattern: "required_leaf"
---

<objective>
Make the stable protected `CI Green` signal fail closed whenever classification or required code proof did not actually run.

Purpose: A green aggregate must mean that code-path proof executed successfully, with docs-only skips accepted only after a successful detector result.
Output: Executable aggregate policy, workflow wiring, and regression meta-tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-05-SUMMARY.md
@.github/workflows/ci.yml
@test/scripts/required_checks_test.exs
@test/support/ci_lanes.ex
@test/support/ci_yaml.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fail CI Green when detector or one required code lane is not successful</name>
  <files>scripts/ci_green_policy.sh, test/scripts/ci_green_policy_test.exs, .github/workflows/ci.yml</files>
  <behavior>
    - Detector result other than `success` fails regardless of leaf results.
    - Successful detector with `code=true` passes only when every declared required leaf result is exactly `success`.
    - One skipped code leaf fails and names that leaf; missing/unknown/failed/cancelled results also fail.
    - Missing, empty, or non-boolean code output fails.
  </behavior>
  <action>Write a strict shell policy accepting the detector result, code output, and explicit `job_id=result` pairs; reject malformed/duplicate inputs and produce a stable failing summary naming each unacceptable result. Test the decision table directly from ExUnit using `System.cmd/3`, beginning with detector failure and one skipped required code leaf. In `ci_green`, add `changes` as a direct need and invoke the policy with `needs.changes.result`, `needs.changes.outputs.code`, and every existing required leaf result. Preserve job id/name and `if: always()`. Do not infer docs-only from skipped leaves; only the detector output decides the branch.</action>
  <verify>
    <automated>mix test test/scripts/ci_green_policy_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Detector failure, invalid classification, or any non-success code leaf makes CI Green fail with an attributable message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Permit only successfully classified docs-only skips and preserve lane-set truth</name>
  <files>scripts/ci_green_policy.sh, test/scripts/ci_green_policy_test.exs, .github/workflows/ci.yml, test/scripts/required_checks_test.exs</files>
  <behavior>
    - Successful detector with `code=false` accepts required leaves that are `success` or `skipped` and rejects failure/cancelled/missing/unknown.
    - `ci_green.needs` contains `changes` exactly once as structural input; after removing that key, its display names set-equal `Mailglass.CILanes.required_lanes/0` with anti-vacuity.
    - Mutating away `changes`, a required leaf, or the policy invocation makes a meta-test fail.
    - Protected branch-check identity remains exactly `CI Green` plus `Guard Release Trigger`.
  </behavior>
  <action>Complete the docs-only decision table and exhaustive negative controls. Update `required_checks_test.exs` so it separately asserts the structural `changes` dependency and compares only the remaining needs against required leaves; do not weaken the existing set-equality or permanently-disabled guards. Add mutation tests for detector dependency deletion, required-leaf deletion, skipped code lane, failed detector, successful docs-only skips, and docs-only failure. Keep `Mailglass.CILanes.required_lanes/0` unchanged because `changes` is structural, not a required leaf/check identity.</action>
  <verify>
    <automated>mix test test/scripts/ci_green_policy_test.exs test/scripts/required_checks_test.exs test/scripts/lane_classification_drift_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Docs-only skips pass only behind successful explicit `code=false`; every false-green mutation fails while `CI Green` identity and required leaf registry remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub `needs` context → protected aggregate | Potentially absent/skipped/failed results determine the public merge signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-16 | Repudiation | CI Green aggregate | high | mitigate | Require detector success, closed boolean output, and exact result policy with executable negative controls. |
| T-155-17 | Tampering | needs/leaf registry | high | mitigate | Assert structural `changes` separately and set-equality for every required leaf; mutation tests fail on deletion. |
| T-155-18 | Denial of Service | docs-only policy | low | accept | A malformed or unknown GitHub result intentionally blocks merge; fail-closed recovery is rerun/fix, not silent success. |
</threat_model>

<verification>
Run the policy, required-check, and lane-classification suites together; all positive branches and injected false-green cases must behave according to the decision table.
</verification>

<success_criteria>
QUAL-02 is satisfied: the unchanged protected aggregate cannot pass on detector failure or skipped required code work, and successful docs-only classification remains supported.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-06-SUMMARY.md` when done.
</output>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe generate, upgrade, repair, rollback plus honest skipped-code CI signal | 01-06 | COVERED | End-to-end migration capability plus aggregate enforcement. |
| REQ | ADOPT-01 | Real generated core/inbound wrappers in Ecto host | 01, 05 | COVERED | Source contract and real Postgres execution. |
| REQ | ADOPT-02 | Explicit repo; inference only for exactly one repo | 01 | COVERED | Existing-module selection, no arbitrary atom conversion. |
| REQ | ADOPT-03 | Fresh timestamped upgrade with prior-version rollback | 01, 03, 05 | COVERED | Offline/live generation and host execution. |
| REQ | ADOPT-04 | Valid older offline version only | 01, 03 | COVERED | Includes approved zero refusal. |
| REQ | ADOPT-05 | Exact known toy repair, non-destructive fail-closed | 04, 05 | COVERED | Exact bytes + catalog + emptiness and real lifecycle. |
| REQ | ADOPT-06 | Absent anchor distinct from malformed/query failure | 02-04 | COVERED | Typed core/inbound outcomes consumed by generation/repair. |
| REQ | QUAL-02 | CI Green fails on detector failure or skipped code lane | 06 | COVERED | Executable closed decision table and meta-tests. |
| RESEARCH | — | Shared internal generator seam, inbound→core only | 01 | COVERED | Parameterized package callbacks preserve dependency direction. |
| RESEARCH | — | Additive files; applied migrations immutable | 01, 03, 04 | COVERED | Collision/no-write/byte-preservation gates. |
| RESEARCH | — | Real stock Ecto/Postgres host, migrate/persist/rollback | 05 | COVERED | Existing Installer Host Smoke identity retained. |
| RESEARCH | — | `changes` structural need separate from required leaves | 06 | COVERED | Registry set-equality remains non-vacuous. |
| CONTEXT | migration generation | Documented wrappers, additive flags, exact repo inference and rollback | 01, 03 | COVERED | No reduced CLI surface. |
| CONTEXT | legacy/metadata safety | Exact toy only; ambiguous/populated/malformed/query states fail | 02, 04 | COVERED | Approved final exact-signature choice is explicit. |
| CONTEXT | CI truth | Stable name, detector dependency, exact code-lane success, docs-only skips | 06 | COVERED | Fail-closed decision table. |
| CONTEXT | deferred ideas | Runtime/data/architecture/quality expansion/release certification | — | EXCLUDED | Explicitly deferred to Phases 156-160. |
| PROJECT | scope lock | Additive-only; no admin/operator UI changes; no shipped migration rewrites | 01-06 | COVERED | Every plan preserves the fence. |
