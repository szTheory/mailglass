---
phase: 155-restore-adopter-and-ci-truth
plan: 07
type: execute
wave: 7
depends_on: [155-05, 155-06]
files_modified:
  - lib/mailglass/migrations/postgres.ex
  - mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex
  - mailglass_inbound/test/mailglass_inbound/migrations_test.exs
  - scripts/generated_ecto_host_proof.sh
  - test/scripts/generated_ecto_host_proof_test.exs
autonomous: true
gap_closure: true
requirements: [ADOPT-01]
must_haves:
  truths:
    - "In a shared configured non-public schema, rolling either package down to zero removes only that package's objects while sibling and host-owned objects survive."
    - "A package-managed schema stays present while any object remains, and the final package rollback may remove it only after it is empty."
    - "The public schema and create_schema: false paths retain their existing no-schema-drop behavior, and every attempted schema drop remains RESTRICT-only."
    - "The real generated Host.Repo proof runs two fresh isolated journeys so both inbound-first and core-first rollback orders prove sibling/schema survival followed by final empty-schema removal."
  artifacts:
    - path: "lib/mailglass/migrations/postgres.ex"
      provides: "Core teardown that treats a non-empty shared schema as a safe skip"
    - path: "mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex"
      provides: "Inbound teardown with matching shared-schema behavior"
    - path: "mailglass_inbound/test/mailglass_inbound/migrations_test.exs"
      provides: "Isolated Postgres coverage for both core/inbound rollback orders and host sentinels"
    - path: "scripts/generated_ecto_host_proof.sh"
      provides: "Passing generated-host migration, persistence, and shared-schema rollback journey"
  key_links:
    - from: "lib/mailglass/migrations/postgres.ex"
      to: "PostgreSQL schema ownership boundary"
      via: "full down-to-zero conditionally attempts DROP SCHEMA ... RESTRICT and skips only dependent-object refusal"
      pattern: "DROP SCHEMA.*RESTRICT"
    - from: "mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex"
      to: "lib/mailglass/migrations/postgres.ex"
      via: "identical lifecycle policy with package-specific table teardown"
      pattern: "dependent_objects_still_exist|2BP01"
    - from: "scripts/generated_ecto_host_proof.sh"
      to: "both package migration runners"
      via: "two generated Host.Repo journeys reverse wrapper generation order so each package is rolled back first once"
      pattern: "core_first|inbound_first"
---

<objective>
Close the generated-host rollback gap by making both package runners coexist safely in one configured non-public schema.

Purpose: ADOPT-01 is blocked because the first package rollback currently aborts when the sibling still occupies the shared schema, even though RESTRICT correctly prevents destructive deletion.
Output: Symmetric core/inbound teardown semantics, isolated rollback-order coverage, and a passing real generated-host proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-CONTEXT.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-RESEARCH.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VALIDATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-VERIFICATION.md
@.planning/phases/155-restore-adopter-and-ci-truth/155-05-SUMMARY.md
@lib/mailglass/migration.ex
@lib/mailglass/migrations/postgres.ex
@mailglass_inbound/lib/mailglass_inbound/migration.ex
@mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex
@mailglass_inbound/test/mailglass_inbound/migrations_test.exs
@scripts/generated_ecto_host_proof.sh
@test/scripts/generated_ecto_host_proof_test.exs

<interfaces>
Both public facades pass the configured prefix into `Postgres.down/1`. Each runner's `change/3` reaches `maybe_drop_schema/1` only after a full rollback targets version zero. `with_defaults/2` keeps `create_schema: false` for `public`, defaults it to true for a non-public prefix, and honors an explicit override. The existing drop uses `DROP SCHEMA IF EXISTS <quoted-prefix> RESTRICT`; this plan preserves that statement as the only schema-deletion primitive.
</interfaces>
</context>

<source_coverage>

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe generated migration apply, upgrade, repair, rollback, and truthful CI proof | 155-01..07 | COVERED | Plan 07 closes the only failed rollback truth. |
| REQ | ADOPT-01 | Real generated core/inbound wrappers apply and roll back | 155-01, 155-05, 155-07 | COVERED | Plan 07 owns shared-schema rollback closure. |
| REQ | ADOPT-02..06 | Repo selection, upgrades, repair, and metadata truth | 155-01..05 | COVERED | Already verified; unchanged by this gap plan. |
| REQ | QUAL-02 | CI Green fails closed | 155-06 | COVERED | Already verified; unchanged by this gap plan. |
| RESEARCH | — | Public facade wrappers execute through Ecto/Host.Repo | 155-05, 155-07 | COVERED | Real-host proof remains authoritative. |
| RESEARCH | — | Preserve immutable shipped migrations and RESTRICT safety | 155-01..07 | COVERED | Runner orchestration changes; no VNN migration is edited. |
| CONTEXT | — | Initial/upgrade/repair, repo, metadata, and CI locked decisions | 155-01..06 | COVERED | Existing plans implement these decisions; Plan 07 does not reopen them. |
| CONTEXT | — | Prefer the smallest deterministic generated-host proof | 155-07 | COVERED | Only the two runners, one isolated integration suite, and existing proof pair change. |

</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Roll core and inbound down independently without claiming shared schema ownership</name>
  <reversibility rating="costly">Both independently released packages must retain matching teardown semantics, so reverting one runner without the other would restore order-dependent adopter failures.</reversibility>
  <files>lib/mailglass/migrations/postgres.ex, mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex, mailglass_inbound/test/mailglass_inbound/migrations_test.exs</files>
  <behavior>
    - In an isolated package-managed non-public schema containing both packages, core-first rollback removes core relations while inbound relations and the schema remain; inbound can then roll back and remove the now-empty schema.
    - The inverse isolated order removes inbound relations first while core relations and the schema remain; core can then roll back and remove the now-empty schema.
    - A host-owned sentinel object survives either package rollback and the final package rollback; the schema remains until the host removes its object.
    - The public-prefix path and explicit `create_schema: false` path never attempt schema deletion.
    - A non-empty-schema refusal is the only swallowed schema-drop outcome; permission, syntax, connection, and other database errors still fail the migration.
  </behavior>
  <action>Write the isolated Postgres cases first in the inbound package, which already depends on core and can therefore exercise both public migration facades against one `MailglassInbound.TestRepo`. Use a distinct scratch schema and fresh Ecto migration versions per case; drive package up/down through real `Ecto.Migrator` runner contexts, assert each package's complete relation set rather than only its anchor, and clean test fixtures with test-only CASCADE solely in `on_exit`. Cover core-first and inbound-first with no host object so the second rollback proves the now-empty package-managed schema may disappear. Add a host-sentinel variant that proves both package object sets are removed but an unrelated table and the schema survive. Then change both runners' `maybe_drop_schema/1` paths symmetrically: retain identifier validation, the existing `create_schema` lifecycle switch, and an actual PostgreSQL `DROP SCHEMA IF EXISTS ... RESTRICT` attempt, but catch only SQLSTATE 2BP01/dependent-object refusal inside the database operation so sibling or host objects turn schema removal into a safe no-op. Do not pre-enumerate object types, do not delete or rename non-package objects, do not edit any shipped `VNN` migration, and let every other PostgreSQL error abort normally.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/migrations_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Both rollback orders complete in isolated Postgres schemas, each package removes only its own relations, host sentinels survive, and schema removal occurs only after RESTRICT confirms emptiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make both authoritative generated Host.Repo rollback orders pass</name>
  <precondition>PostgreSQL is reachable through DATABASE_URL using a base database name in the script's `mailglass_generated_ecto_host_` scratch namespace, and the configured role may create/drop the two derived scratch databases.</precondition>
  <files>scripts/generated_ecto_host_proof.sh, test/scripts/generated_ecto_host_proof_test.exs</files>
  <behavior>
    - A fresh inbound-first journey generates core then inbound, rolls inbound back first, proves only inbound relations disappeared while core relations and the shared schema remain, then rolls core back and proves package relations plus the now-empty schema are absent.
    - A separate fresh core-first journey generates inbound then core, rolls core back first, proves only core relations disappeared while inbound relations and the shared schema remain, then rolls inbound back and proves package relations plus the now-empty schema are absent.
    - Each journey uses its own generated Phoenix host directory and its own derived allowlisted scratch database, migrates through Host.Repo, and persists/reloads real package data before rollback.
    - Static contract coverage fails when either journey invocation, either generation order, either first-rollback package-specific assertion set, or either final schema-absence assertion is removed.
  </behavior>
  <action>Strengthen the source-contract test first, then parameterize the real script around a closed `core_first | inbound_first` journey function rather than duplicating unstructured shell blocks. Treat the caller's validated `DATABASE_URL` as a base: replace only its final database path component, preserving connection authority and query parameters, to derive two distinct names that remain inside `mailglass_generated_ecto_host_` and carry unambiguous order suffixes. Give each invocation a separate child of the script-owned `mktemp` directory, generate a fresh Phoenix/Ecto host there, configure the same shared `mailglass` prefix, and create/drop only that journey's derived database. Select generator invocation order explicitly—core then inbound for inbound-first rollback, inbound then core for core-first rollback—so ordinary `mix ecto.rollback --step 1` drives the requested first package through the real generated wrapper. In each journey, run both generated wrappers up, verify both anchors, and persist/reload real data. After the first rollback, assert the rolled-back package's full relation set is absent, the sibling package's anchor/data relations remain, and `to_regnamespace('mailglass')` is non-null. After the second rollback, assert both packages' relations and `to_regnamespace('mailglass')` are null. Preserve the current wrapper-source checks, private scratch ownership, URL namespace refusal, Host.Repo-only execution, strict shell mode, and cleanup on partial failure. Extend required snippets plus mutation controls to pin both journey calls, both opposing generation orders, both package-specific intermediate states, and both final empty-schema states; source checks supplement rather than replace execution.</action>
  <verify>
    <automated>mix test test/scripts/generated_ecto_host_proof_test.exs --warnings-as-errors &amp;&amp; MAILGLASS_PATH="$PWD" bash scripts/generated_ecto_host_proof.sh</automated>
  </verify>
  <done>Two fresh generated Phoenix/Ecto/Postgres hosts execute opposing rollback orders through Host.Repo; each first rollback preserves only the sibling and schema, and each second rollback removes the final package objects and empty package-managed schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package runner → shared adopter schema | Package-owned teardown executes beside sibling and host-owned database objects. |
| Generated wrapper → Host.Repo/PostgreSQL | Public migration facades issue destructive DDL through the adopter's configured repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-155-19 | Tampering | core/inbound schema teardown | high | mitigate | Delete only each runner's enumerated package relations and preserve PostgreSQL RESTRICT as the sole schema-drop authority. |
| T-155-20 | Denial of Service | first package rollback | high | mitigate | Convert only dependent-object schema-drop refusal into a safe skip so a sibling install cannot abort rollback. |
| T-155-21 | Elevation of Privilege | schema-drop error handling | medium | mitigate | Catch only SQLSTATE 2BP01 inside the database operation; permission and all other failures propagate. |
| T-155-22 | Repudiation | generated-host proof | medium | mitigate | Execute two isolated Host.Repo journeys and make both order-specific intermediate/final assertion sets load-bearing through mutation controls. |
</threat_model>

<verification>
Run the isolated inbound-package migration suite first, then the root source contract and real generated-host script; the script must derive and clean two dedicated PostgreSQL scratch databases from the validated base URL. Finish with `mix format --check-formatted`, `bash -n scripts/generated_ecto_host_proof.sh`, and `git diff --check`.
</verification>

<success_criteria>
ADOPT-01 is no longer order-dependent: both package runners preserve sibling/host objects in a shared non-public schema, a final teardown can remove an empty package-managed schema, public behavior remains unchanged, and both real generated-host rollback orders exit successfully.
</success_criteria>

<output>
Create `.planning/phases/155-restore-adopter-and-ci-truth/155-07-SUMMARY.md` when done.
</output>
