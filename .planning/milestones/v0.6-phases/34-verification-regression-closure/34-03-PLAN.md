---
phase: "34"
plan: "03"
type: execute
wave: 2
depends_on:
  - "34-01"
  - "34-02"
files_modified:
  - "scripts/verify_support_contract.sh"
  - ".github/workflows/ci.yml"
  - ".github/workflows/advisory-matrix.yml"
  - "MAINTAINING.md"
  - "test/mailglass/docs_contract_test.exs"
autonomous: true
requirements:
  - MAT-03
user_setup:
  - service: github
    why: "Align branch protection with the new explicit support-contract required checks"
    dashboard_config:
      - task: "Require `Compile No Optional Deps (Elixir 1.18 / OTP 27)`, `Support Contract Core (Elixir 1.18 / OTP 27)`, and `Support Contract Admin (Elixir 1.18 / OTP 27)`; remove any required reference to `Tests (Elixir 1.18 / OTP 27)` and `Operator Browser Gate (Elixir 1.18 / OTP 27 / Node 22)` if either exists, because browser coverage remains advisory in Phase 34."
        location: "GitHub repository settings -> Branches -> Branch protection rules -> Required status checks"
must_haves:
  truths:
    - "Maintainers have one honest repo-root entrypoint that orchestrates the three required MAT-03 buckets without pretending to be a monolithic repo test suite."
    - "PR-required workflow jobs expose the core support-contract bundle, the admin/operator support-contract bundle, and compile-without-optional-deps as separate always-run checks."
    - "Broader full-suite and provider-compatibility coverage stay advisory, and provider-live remains an explicit advisory canary."
  artifacts:
    - path: "scripts/verify_support_contract.sh"
      provides: "Thin repo-root orchestrator over the required Phase 34 verification authorities"
      contains: "mix verify.support_contract.core"
    - path: ".github/workflows/ci.yml"
      provides: "Explicit required jobs for core support contract, admin support contract, and compile-without-optional-deps"
      contains: "Support Contract Core"
    - path: ".github/workflows/advisory-matrix.yml"
      provides: "Repurposed advisory workflow with meaningful non-required signal"
      contains: "Provider Compatibility Advisory"
    - path: "MAINTAINING.md"
      provides: "Documented required-versus-advisory verification posture for maintainers"
      contains: "verify_support_contract.sh"
  key_links:
    - from: "scripts/verify_support_contract.sh"
      to: "mix.exs"
      via: "root support-contract alias invocation"
      pattern: "verify\\.support_contract\\.core"
    - from: "scripts/verify_support_contract.sh"
      to: "mailglass_admin/mix.exs"
      via: "package-local admin support-contract alias invocation"
      pattern: "verify\\.support_contract\\.admin"
    - from: ".github/workflows/ci.yml"
      to: "scripts/verify_support_contract.sh"
      via: "job commands mirror the same three-bucket contract"
      pattern: "Support Contract Core|Support Contract Admin|Compile No Optional Deps"
    - from: "test/mailglass/docs_contract_test.exs"
      to: "MAINTAINING.md"
      via: "docs assertions pin required-versus-advisory wording"
      pattern: "Support Contract Core|Provider Compatibility Advisory|Provider Live Advisory"
---

<objective>
Wire the package-local authorities into an honest repo-root maintainer entrypoint and an explicit CI contract, while repurposing duplicate advisory workflow signal into something meaningful.

Purpose: Per D-34-01 through D-34-17, Phase 34 must expose the real release gate as three separate required buckets, keep provider-live advisory, and stop presenting duplicated broad runs as the production-maturity truth.
Output: A thin repo-root verification script, explicit required CI jobs, a repurposed advisory workflow, and maintainer-facing docs/tests that lock the required-versus-advisory posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-verification-regression-closure/34-CONTEXT.md
@.planning/phases/34-verification-regression-closure/34-RESEARCH.md
@.planning/phases/34-verification-regression-closure/34-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/advisory-matrix.yml
@.github/workflows/provider-live.yml
@MAINTAINING.md
@test/mailglass/docs_contract_test.exs
@.planning/phases/34-verification-regression-closure/34-01-PLAN.md
@.planning/phases/34-verification-regression-closure/34-02-PLAN.md

<interfaces>
From .github/workflows/ci.yml:
```yaml
compile_no_optional_deps:
  name: Compile No Optional Deps (Elixir 1.18 / OTP 27)
```

From .github/workflows/provider-live.yml:
```yaml
provider_live:
  name: Provider Live Advisory (Elixir 1.18 / OTP 27)
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create the honest repo-root entrypoint and rewire required versus advisory workflows</name>
  <files>scripts/verify_support_contract.sh, .github/workflows/ci.yml, .github/workflows/advisory-matrix.yml</files>
  <read_first>scripts/verify_support_contract.sh, .github/workflows/ci.yml, .github/workflows/advisory-matrix.yml, .github/workflows/provider-live.yml, .planning/phases/34-verification-regression-closure/34-CONTEXT.md, .planning/phases/34-verification-regression-closure/34-RESEARCH.md, .planning/phases/34-verification-regression-closure/34-01-PLAN.md, .planning/phases/34-verification-regression-closure/34-02-PLAN.md</read_first>
  <acceptance_criteria>
    - `scripts/verify_support_contract.sh` exists, starts with `#!/usr/bin/env bash` and `set -euo pipefail`, and runs `mix verify.support_contract.core`, `cd mailglass_admin && mix verify.support_contract.admin`, and `mix compile --no-optional-deps --warnings-as-errors`.
    - `.github/workflows/ci.yml` contains jobs named `Support Contract Core (Elixir 1.18 / OTP 27)` and `Support Contract Admin (Elixir 1.18 / OTP 27)`.
    - `.github/workflows/ci.yml` keeps `Compile No Optional Deps (Elixir 1.18 / OTP 27)` as a separate required job.
    - `.github/workflows/ci.yml` does not use conditional `if:` guards or tag-only selectors for the required support-contract jobs.
    - `operator_browser_gate` is explicitly treated as advisory browser evidence and not as part of the required three-bucket `MAT-03` contract.
    - `.github/workflows/advisory-matrix.yml` no longer runs `mix test --warnings-as-errors --exclude provider_live`.
    - `.github/workflows/advisory-matrix.yml` contains meaningful advisory jobs named `Core Full Suite Advisory (Elixir 1.18 / OTP 27)` and `Provider Compatibility Advisory (Elixir 1.18 / OTP 27)`.
  </acceptance_criteria>
  <action>Create `scripts/verify_support_contract.sh` as the thin honest repo-root entrypoint from D-34-02 and D-34-07. The script must run exactly these three required buckets in sequence: `mix verify.support_contract.core`, `(cd mailglass_admin && mix verify.support_contract.admin)`, and `mix compile --no-optional-deps --warnings-as-errors`. In `.github/workflows/ci.yml`, replace the old broad required `tests` contract with two explicit required jobs named `Support Contract Core (Elixir 1.18 / OTP 27)` and `Support Contract Admin (Elixir 1.18 / OTP 27)` that call the package-local authorities from Plans 34-01 and 34-02; keep `Compile No Optional Deps (Elixir 1.18 / OTP 27)` as the third required bucket. Keep `operator_browser_gate` in CI only as advisory browser evidence and remove it from the required-branch-protection truth set if it is currently required. Do not implement the required jobs with `if:` conditionals, `--only` tag gates, or a single merged repo-root `mix test`. In `.github/workflows/advisory-matrix.yml`, repurpose the workflow so it carries non-required signal with two explicit jobs: `Core Full Suite Advisory (Elixir 1.18 / OTP 27)` running `mix test --warnings-as-errors`, and `Provider Compatibility Advisory (Elixir 1.18 / OTP 27)` running `mix verify.provider_compatibility`. Leave `.github/workflows/provider-live.yml` advisory-only and unpromoted to CI-required status.</action>
  <verify>
    <automated>bash scripts/verify_support_contract.sh && actionlint .github/workflows/ci.yml .github/workflows/advisory-matrix.yml && rg -n "Support Contract Core|Support Contract Admin|Core Full Suite Advisory|Provider Compatibility Advisory" .github/workflows/ci.yml .github/workflows/advisory-matrix.yml scripts/verify_support_contract.sh && ! rg -n --fixed-strings "mix test --warnings-as-errors --exclude provider_live" .github/workflows/advisory-matrix.yml</automated>
  </verify>
  <done>The repo now has one honest local orchestrator plus explicit required and advisory workflow separation that matches the Phase 34 verification philosophy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document and lock the required-versus-advisory verification contract</name>
  <files>MAINTAINING.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>MAINTAINING.md, test/mailglass/docs_contract_test.exs, .planning/phases/34-verification-regression-closure/34-CONTEXT.md, .planning/phases/34-verification-regression-closure/34-RESEARCH.md, .github/workflows/ci.yml, .github/workflows/advisory-matrix.yml, .github/workflows/provider-live.yml, scripts/verify_support_contract.sh</read_first>
  <acceptance_criteria>
    - `MAINTAINING.md` mentions `scripts/verify_support_contract.sh`.
    - `MAINTAINING.md` names the three required buckets exactly as `Support Contract Core`, `Support Contract Admin`, and `Compile No Optional Deps`.
    - `MAINTAINING.md` states that `Core Full Suite Advisory`, `Provider Compatibility Advisory`, and `Provider Live Advisory` are advisory, not branch-protection truth.
    - `test/mailglass/docs_contract_test.exs` asserts the exact required-versus-advisory wording added to `MAINTAINING.md`.
  </acceptance_criteria>
  <action>Update `MAINTAINING.md` so the maintainer-facing verification story matches D-34-01 through D-34-17 exactly: `scripts/verify_support_contract.sh` is the honest repo-root entrypoint, the required PR truth is the three-bucket contract `Support Contract Core`, `Support Contract Admin`, and `Compile No Optional Deps`, and the advisory lanes are `Core Full Suite Advisory`, `Provider Compatibility Advisory`, and `Provider Live Advisory`. Extend `test/mailglass/docs_contract_test.exs` with exact string assertions against `MAINTAINING.md` so future edits cannot quietly collapse the distinction back into a monolithic or duplicated test story. Keep the wording explicit that provider-live is a cron/workflow-dispatch canary and not a merge blocker.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The documented verification contract is now precise, honest, and regression-tested against future wording drift.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-root maintainer entrypoint -> package-local authorities | A convenience entrypoint must not overclaim ownership of sibling-package verification. |
| CI required jobs -> branch protection truth | Workflow job naming and scope determine what maintainers believe PR green actually means. |
| Maintainer docs -> operational behavior | Documentation must not drift away from the actual required-versus-advisory CI posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering / Misrepresentation | `scripts/verify_support_contract.sh`, `.github/workflows/ci.yml` | mitigate | Keep the repo-root entrypoint thin and the required CI contract split into the exact three explicit buckets from D-34-04. |
| T-34-08 | Repudiation | `.github/workflows/advisory-matrix.yml`, `MAINTAINING.md` | mitigate | Give advisory lanes unique names and docs wording so maintainers can distinguish broad/advisory signal from branch-protection truth. |
| T-34-09 | Denial of Service | `.github/workflows/ci.yml` | accept | Required jobs remain always-run and explicit even if they cost extra CI minutes; honesty is the higher priority in this phase. |
</threat_model>

<verification>
- `bash scripts/verify_support_contract.sh`
- `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors`
- `rg -n "Support Contract Core|Support Contract Admin|Compile No Optional Deps|Core Full Suite Advisory|Provider Compatibility Advisory|Provider Live Advisory|verify_support_contract\\.sh" .github/workflows/ci.yml .github/workflows/advisory-matrix.yml MAINTAINING.md test/mailglass/docs_contract_test.exs scripts/verify_support_contract.sh`
</verification>

<success_criteria>
Maintainers can point to one honest local entrypoint, three explicit required PR checks, and clearly named advisory lanes without overclaiming what the green checks mean.
</success_criteria>

<output>
After completion, create `.planning/phases/34-verification-regression-closure/34-03-SUMMARY.md`
</output>
