---
phase: "34"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mix.exs"
  - "test/support/citext_probe.ex"
  - "test/mailglass/test_support/citext_probe_test.exs"
autonomous: true
requirements:
  - MAT-03
must_haves:
  truths:
    - "Maintainers can run one explicit root-package support-contract bundle without pretending it is the whole sibling-package repo gate."
    - "The root-package bootstrap probe fails loudly after bounded retries instead of returning a false green."
    - "Deterministic provider-compatibility coverage exists as a separate signal from the required support-contract gate."
  artifacts:
    - path: "mix.exs"
      provides: "Explicit root-package verification aliases for the core support-contract bundle and deterministic provider compatibility bundle"
      contains: "verify.support_contract.core"
    - path: "test/support/citext_probe.ex"
      provides: "Honest bounded citext probe that raises on exhausted retries"
      contains: "raise"
    - path: "test/mailglass/test_support/citext_probe_test.exs"
      provides: "Regression coverage for probe success and exhausted-retry failure"
      contains: "citext probe exhausted"
  key_links:
    - from: "mix.exs"
      to: "test/mailglass/docs_contract_test.exs"
      via: "explicit core support-contract alias file list"
      pattern: "verify\\.support_contract\\.core"
    - from: "mix.exs"
      to: "test/mailglass/webhook/providers/postmark_test.exs"
      via: "deterministic provider-compatibility alias file list"
      pattern: "verify\\.provider_compatibility"
    - from: "test/support/citext_probe.ex"
      to: "test/mailglass/test_support/citext_probe_test.exs"
      via: "probe retry contract covered by focused unit tests"
      pattern: "citext probe exhausted"
---

<objective>
Make the root `mailglass` package an honest authority for the core support-contract gate, and remove the false-green bootstrap behavior that currently undermines trust in root-package verification.

Purpose: Per D-34-02 through D-34-09, the root package must own its own verification contract and bootstrap honesty without collapsing admin ownership into a fake repo-wide test abstraction.
Output: Explicit root-package aliases for the core support-contract bundle and deterministic provider compatibility bundle, plus bounded citext-probe behavior with regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-verification-regression-closure/34-CONTEXT.md
@.planning/phases/34-verification-regression-closure/34-RESEARCH.md
@.planning/phases/34-verification-regression-closure/34-PATTERNS.md
@mix.exs
@test/support/citext_probe.ex
@test/mailglass/docs_contract_test.exs
@test/mailglass/docs/operator_incident_support_guide_test.exs
@test/mailglass/operator/support_summary_test.exs
@test/mailglass/webhook/telemetry_test.exs
@test/mailglass/telemetry_test.exs
@test/mailglass/webhook/replay_test.exs
@test/mailglass/webhook/reconciler_test.exs
@test/mailglass/adapter_test.exs
@test/mailglass/adapters/swoosh_test.exs
@test/mailglass/webhook/providers/postmark_test.exs
@test/mailglass/webhook/providers/sendgrid_test.exs
@test/mailglass/webhook/providers/mailgun_test.exs
@test/mailglass/webhook/providers/resend_test.exs
@test/mailglass/webhook/providers/ses_test.exs
@test/mailglass/webhook/providers/ses/cert_cache_test.exs
@test/mailglass/webhook/plug_mailgun_test.exs
@test/mailglass/webhook/plug_ses_test.exs
@test/mailglass/webhook/providers/resend_webhook_plug_test.exs

<interfaces>
From test/support/citext_probe.ex:
```elixir
@spec run(keyword()) :: :ok
def run(opts \\ [])
```

From mix.exs:
```elixir
"verify.docs.contract": [
  "test test/mailglass/docs_contract_test.exs --warnings-as-errors"
]
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit root-package aliases for the required core bundle and deterministic provider compatibility bundle</name>
  <files>mix.exs</files>
  <read_first>mix.exs, .planning/phases/34-verification-regression-closure/34-CONTEXT.md, .planning/phases/34-verification-regression-closure/34-RESEARCH.md, test/mailglass/docs_contract_test.exs, test/mailglass/docs/operator_incident_support_guide_test.exs, test/mailglass/operator/support_summary_test.exs, test/mailglass/webhook/telemetry_test.exs, test/mailglass/telemetry_test.exs, test/mailglass/webhook/replay_test.exs, test/mailglass/webhook/reconciler_test.exs, test/mailglass/adapter_test.exs, test/mailglass/adapters/swoosh_test.exs, test/mailglass/webhook/providers/postmark_test.exs, test/mailglass/webhook/providers/sendgrid_test.exs, test/mailglass/webhook/providers/mailgun_test.exs, test/mailglass/webhook/providers/resend_test.exs, test/mailglass/webhook/providers/ses_test.exs, test/mailglass/webhook/providers/ses/cert_cache_test.exs, test/mailglass/webhook/plug_mailgun_test.exs, test/mailglass/webhook/plug_ses_test.exs, test/mailglass/webhook/providers/resend_webhook_plug_test.exs</read_first>
  <acceptance_criteria>
    - `mix.exs` contains a `verify.support_contract.core` alias.
    - `verify.support_contract.core` runs exactly these root files in one invocation: `test/mailglass/docs_contract_test.exs`, `test/mailglass/docs/operator_incident_support_guide_test.exs`, `test/mailglass/operator/support_summary_test.exs`, `test/mailglass/webhook/telemetry_test.exs`, `test/mailglass/telemetry_test.exs`, `test/mailglass/webhook/replay_test.exs`, `test/mailglass/webhook/reconciler_test.exs`.
    - `mix.exs` contains a `verify.provider_compatibility` alias.
    - `verify.provider_compatibility` runs an explicit deterministic file list and does not use `--only provider_live`, `--exclude provider_live`, or tag-only selection.
  </acceptance_criteria>
  <action>In `mix.exs`, add `verify.support_contract.core` per D-34-03, D-34-04, and D-34-05 as one explicit `mix test ... --warnings-as-errors` invocation over this exact bundle: `test/mailglass/docs_contract_test.exs test/mailglass/docs/operator_incident_support_guide_test.exs test/mailglass/operator/support_summary_test.exs test/mailglass/webhook/telemetry_test.exs test/mailglass/telemetry_test.exs test/mailglass/webhook/replay_test.exs test/mailglass/webhook/reconciler_test.exs`. In the same aliases block, add `verify.provider_compatibility` for the deterministic advisory lane using this exact file list: `test/mailglass/adapter_test.exs test/mailglass/adapters/swoosh_test.exs test/mailglass/webhook/providers/postmark_test.exs test/mailglass/webhook/providers/sendgrid_test.exs test/mailglass/webhook/providers/mailgun_test.exs test/mailglass/webhook/providers/resend_test.exs test/mailglass/webhook/providers/ses_test.exs test/mailglass/webhook/providers/ses/cert_cache_test.exs test/mailglass/webhook/plug_mailgun_test.exs test/mailglass/webhook/plug_ses_test.exs test/mailglass/webhook/providers/resend_webhook_plug_test.exs`. Keep root `mix test` available as the broad full-suite authority for `mailglass` per D-34-02, but do not label either alias as a repo-wide truth gate.</action>
  <verify>
    <automated>mix verify.support_contract.core && mix verify.provider_compatibility</automated>
  </verify>
  <done>The root package exposes explicit, non-vacuous local commands for the required support-contract bundle and the separate deterministic provider-compatibility advisory bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the root citext probe fail honestly after bounded retries</name>
  <files>test/support/citext_probe.ex, test/mailglass/test_support/citext_probe_test.exs</files>
  <read_first>test/support/citext_probe.ex, .planning/phases/34-verification-regression-closure/34-CONTEXT.md, .planning/phases/34-verification-regression-closure/34-PATTERNS.md, test/test_helper.exs, test/support/data_case.ex, test/support/mailer_case.ex</read_first>
  <acceptance_criteria>
    - `test/support/citext_probe.ex` no longer ends the retry loop with `:ok` on exhaustion.
    - `test/support/citext_probe.ex` raises an exception message containing `citext probe exhausted for Mailglass.TestRepo after`.
    - `test/mailglass/test_support/citext_probe_test.exs` contains one success-path assertion and one exhausted-retry assertion.
    - The public `run/1` API remains package-local and callable from existing root helpers without introducing a shared cross-package helper.
  </acceptance_criteria>
  <action>Refactor `Mailglass.TestSupport.CitextProbe` per D-34-06 through D-34-09 so retry exhaustion is explicit instead of silently returning `:ok`. Preserve the public `run/1` entrypoint used by `test/test_helper.exs`, `DataCase`, and `MailerCase`, but extract the retry loop far enough that `test/mailglass/test_support/citext_probe_test.exs` can drive it deterministically with a test-only probe function or stub repo. On exhaustion, raise with an exact message shaped like `citext probe exhausted for Mailglass.TestRepo after N attempts`; do not add blanket rescue/retry behavior outside the bounded probe loop, and do not move ownership out of the root package helpers.</action>
  <verify>
    <automated>mix test test/mailglass/test_support/citext_probe_test.exs --warnings-as-errors && mix verify.support_contract.core</automated>
  </verify>
  <done>The root bootstrap seam is now truthful: a poisoned bootstrap fails immediately with a clear message, and that behavior is covered by focused regression tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root test bootstrap -> CI truth | A local bootstrap helper determines whether required core verification can be trusted. |
| Deterministic provider tests -> advisory signal | Deterministic provider compatibility results must stay distinct from the required support-contract gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering / Misrepresentation | `test/support/citext_probe.ex` | mitigate | Raise on exhausted retries with an exact message so bootstrap failure cannot masquerade as green per D-34-06 and D-34-09. |
| T-34-02 | Repudiation | `mix.exs` aliases | mitigate | Use explicit file lists for `verify.support_contract.core` and `verify.provider_compatibility` so required and advisory signals are auditable and non-vacuous per D-34-04 and D-34-05. |
| T-34-03 | Denial of Service | `test/support/citext_probe.ex` | accept | Keep retries bounded to the current package-local probe budget; a clear hard failure is preferred over hidden recovery loops. |
</threat_model>

<verification>
- `mix verify.support_contract.core`
- `mix verify.provider_compatibility`
- `mix test test/mailglass/test_support/citext_probe_test.exs --warnings-as-errors`
- `rg -n "verify\\.support_contract\\.core|verify\\.provider_compatibility|citext probe exhausted" mix.exs test/support/citext_probe.ex test/mailglass/test_support/citext_probe_test.exs`
</verification>

<success_criteria>
The root package can prove its support-critical coverage with one explicit alias, its provider-compatibility advisory signal with another, and its bootstrap seam now fails honestly instead of silently swallowing exhaustion.
</success_criteria>

<output>
After completion, create `.planning/phases/34-verification-regression-closure/34-01-SUMMARY.md`
</output>
