---
phase: "32"
plan: "02"
type: execute
wave: 2
depends_on:
  - "32-01"
files_modified:
  - "mailglass_admin/lib/mailglass_admin/operator/repair_state.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
  - "mailglass_admin/lib/mailglass_admin/operator_live.ex"
  - "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
  - "test/mailglass/operator/timeline_test.exs"
  - "test/mailglass/webhook/replay_test.exs"
autonomous: true
requirements:
  - MAT-01
must_haves:
  truths:
    - "Operator-facing replay availability is described only as ready, choice required, or unavailable."
    - "Replay outcomes are described as requested, completed, or failed, with a separate new work or no change effect."
    - "Replay audit facts remain visible and distinct in the timeline without leaking raw event atoms as final UI wording."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/repair_state.ex"
      provides: "Shared presenter for replay availability, outcome, and effect wording"
      exports: ["availability_label/1", "outcome_label/1", "effect_label/1"]
    - path: "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
      provides: "Delivery header that uses presenter wording for replay hints and latest replay summary"
      contains: "Last replay:"
    - path: "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
      provides: "Timeline rendering that keeps replay audit rows visually distinct while using standardized labels"
      contains: "Replay audit"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/repair_state.ex"
      via: "availability and latest replay summary mapping"
      pattern: "RepairState\\."
    - from: "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/repair_state.ex"
      via: "timeline metadata summary and label mapping"
      pattern: "RepairState\\."
    - from: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "flash and rendered copy assertions"
      pattern: "completed|new work|no change|choice required|unavailable"
---

<objective>
Operator replay copy is standardized through one presenter seam so availability, outcome, and material effect stay honest and consistent.

Purpose: Replace scattered wording logic with one repair-state vocabulary while preserving replay audit durability and keeping replay as the only delivery-detail manual repair action.
Output: A shared repair-state presenter, updated UI surfaces, and focused wording regressions in the existing tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md
@.planning/phases/32-replay-reconcile-hardening/32-RESEARCH.md
@.planning/phases/32-replay-reconcile-hardening/32-PATTERNS.md
@.planning/phases/32-replay-reconcile-hardening/32-VALIDATION.md
@mailglass_admin/lib/mailglass_admin/operator/detail_header.ex
@mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
@mailglass_admin/lib/mailglass_admin/operator/timeline.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@test/mailglass/webhook/replay_test.exs

<interfaces>
From mailglass_admin/lib/mailglass_admin/operator/detail_header.ex:
```elixir
attr :delivery, :map, required: true
attr :replay_targets, :map, default: nil
attr :latest_replay, :map, default: nil
```

From mailglass_admin/lib/mailglass_admin/operator/timeline.ex:
```elixir
defp replay_event?(type),
  do: type in [:webhook_replay_requested, :webhook_replay_succeeded, :webhook_replay_failed]
```

From test/mailglass/webhook/replay_test.exs:
```elixir
assert result.status == :replayed
assert result.status == :noop
[requested] = replay_events_for(webhook_event.id, :webhook_replay_requested)
[succeeded] = replay_events_for(webhook_event.id, :webhook_replay_succeeded)
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Introduce the shared repair-state presenter and wire every replay UI surface through it</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/repair_state.ex, mailglass_admin/lib/mailglass_admin/operator/detail_header.ex, mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex, mailglass_admin/lib/mailglass_admin/operator/timeline.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <read_first>mailglass_admin/lib/mailglass_admin/operator/detail_header.ex, mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex, mailglass_admin/lib/mailglass_admin/operator/timeline.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, .planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md, .planning/phases/32-replay-reconcile-hardening/32-PATTERNS.md</read_first>
  <action>Create `MailglassAdmin.Operator.RepairState` as the single presenter seam for D-32-12 and D-32-16 through D-32-21. Map replay target facts to `ready`, `choice required`, and `unavailable`; map replay audit facts to `requested`, `completed`, and `failed`; and map completed replay effects to `new work` or `no change`. Update header hints, latest replay summaries, modal body copy, timeline metadata summaries, and success/error flashes to use the presenter. Keep the `Replay audit` badge and distinct visual treatment, keep replay as the only delivery-detail CTA per D-32-06 and D-32-07, and do not add any per-delivery reconcile action.</action>
  <acceptance_criteria>`mailglass_admin/lib/mailglass_admin/operator/repair_state.ex` exists and is referenced by header, modal, timeline, or LiveView code; `rg -n "choice required|new work|no change|completed|Replay audit" mailglass_admin/lib/mailglass_admin/operator` finds the standardized vocabulary; `rg -n "Reconcile" mailglass_admin/lib/mailglass_admin/operator/detail_header.ex mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` does not show a new delivery-detail reconcile CTA.</acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Every replay-facing operator surface now derives wording from one presenter seam, and the UI distinguishes availability, outcome, and effect without exposing raw audit atoms as final copy.</done>
</task>

<task type="auto">
  <name>Task 2: Lock wording and replay audit behavior in the existing focused tests</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs, test/mailglass/operator/timeline_test.exs, test/mailglass/webhook/replay_test.exs</files>
  <read_first>mailglass_admin/test/mailglass_admin/operator_live_test.exs, test/mailglass/operator/timeline_test.exs, test/mailglass/webhook/replay_test.exs, mailglass_admin/lib/mailglass_admin/operator/repair_state.ex, mailglass_admin/lib/mailglass_admin/operator/timeline.ex</read_first>
  <action>Expand the current tests instead of adding new test suites. In `operator_live_test.exs`, assert the rendered copy uses `completed` plus `new work` or `no change` and uses `choice required`/`unavailable` availability copy. In `timeline_test.exs`, add replay-audit timeline assertions that keep audit rows distinct while mapping metadata through presenter-level wording. In `replay_test.exs`, keep the durable facts unchanged but assert the `:replayed` vs `:noop` outcome contract that the presenter relies on. Do not rename ledger atoms or collapse requested/completed/failed into a single status string.</action>
  <acceptance_criteria>`mailglass_admin/test/mailglass_admin/operator_live_test.exs` contains assertions for `completed`, `new work`, and `no change`; `test/mailglass/operator/timeline_test.exs` contains replay audit summary assertions; `test/mailglass/webhook/replay_test.exs` still asserts requested/succeeded/failed audit facts and distinguishes `:replayed` from `:noop`.</acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors && cd /Users/jon/projects/mailglass && mix test test/mailglass/operator/timeline_test.exs test/mailglass/webhook/replay_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The operator copy contract and the replay ledger contract are both locked through the existing focused test files.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable replay audit facts -> operator-facing copy | Internal ledger atoms and metadata are translated into human-facing text. |
| Replay result -> LiveView flash/timeline summary | A backend outcome is surfaced to an operator and can overclaim if mapped incorrectly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Repudiation | `mailglass_admin/lib/mailglass_admin/operator/repair_state.ex` | mitigate | Map replay results to `completed` plus an explicit effect label so no-op replays do not read as unconditional success. |
| T-32-05 | Information Disclosure | `mailglass_admin/lib/mailglass_admin/operator/timeline.ex` | mitigate | Keep raw ledger event atoms and failure metadata behind presenter-level wording and preserve the PII-free audit summary shape. |
| T-32-06 | Tampering / Misrepresentation | `mailglass_admin/test/mailglass_admin/operator_live_test.exs` | mitigate | Lock the standardized vocabulary in the existing UI tests so later copy changes cannot silently reintroduce guessed, overclaiming, or reconcile-like semantics. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors`
- `mix test test/mailglass/operator/timeline_test.exs test/mailglass/webhook/replay_test.exs --warnings-as-errors`
- `rg -n "ready|choice required|unavailable|requested|completed|failed|new work|no change" mailglass_admin/lib/mailglass_admin/operator mailglass_admin/test/mailglass_admin/operator_live_test.exs test/mailglass/operator/timeline_test.exs`
</verification>

<success_criteria>
Replay remains the only delivery-detail repair action, but every replay hint, flash, and timeline summary now uses one stable vocabulary that is auditable and low-claiming.
</success_criteria>

<output>
After completion, create `.planning/phases/32-replay-reconcile-hardening/32-02-SUMMARY.md`
</output>
