---
phase: "32"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mailglass_admin/lib/mailglass_admin/auth.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex"
  - "mailglass_admin/lib/mailglass_admin/operator_live.ex"
  - "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
  - "mailglass_admin/test/support/endpoint_case.ex"
autonomous: true
requirements:
  - MAT-01
must_haves:
  truths:
    - "Operators can request replay only after tenant-safe target resolution and action-time authorization."
    - "Stale-auth and unauthorized replay attempts stop before any replay audit rows are appended."
    - "Ambiguous replayable webhook rows require an explicit operator choice instead of an implicit guess."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex"
      provides: "Shared destructive-action authorization helper on the existing auth seam"
      exports: ["authorize/4"]
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Replay event flow that resolves, authorizes, and then executes"
      contains: "confirm_replay"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "Focused regression coverage for stale-auth, exact-target, and ambiguity replay paths"
      contains: "returns a stale-auth error without performing replay"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex"
      via: "authorize(:destructive_action, ...)"
      pattern: "DestructiveAction\\.authorize"
    - from: "mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex"
      to: "mailglass_admin/lib/mailglass_admin/auth.ex"
      via: "MailglassAdmin.Auth.authorize/3"
      pattern: "Auth\\.authorize\\(.*:destructive_action"
    - from: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "LiveView replay flow assertions"
      pattern: "stale-auth|target choice|replay audit rows"
---

<objective>
Operators can only trigger replay through the existing destructive-action authorization seam, after the exact replay target is resolved.

Purpose: Close the highest-risk replay support gaps first by enforcing the tenant-safe resolve -> authorize -> execute order and by documenting the adopter-owned recent-auth contract.
Output: A shared destructive-action helper, updated replay event flow, and focused LiveView regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md
@.planning/phases/32-replay-reconcile-hardening/32-RESEARCH.md
@.planning/phases/32-replay-reconcile-hardening/32-PATTERNS.md
@.planning/phases/32-replay-reconcile-hardening/32-VALIDATION.md
@mailglass_admin/lib/mailglass_admin/auth.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/test/mailglass_admin/operator_live_test.exs
@mailglass_admin/test/support/endpoint_case.ex

<interfaces>
From mailglass_admin/lib/mailglass_admin/auth.ex:
```elixir
@type action :: :operator_access | :destructive_action | atom()
@callback authorize(action(), context :: map()) :: result()
@spec authorize(module(), action(), map()) :: {:ok, %{actor: actor(), assigns: map()}} | failure()
```

From mailglass_admin/lib/mailglass_admin/operator_live.ex:
```elixir
with %{selected_delivery: %{id: delivery_id, tenant_id: tenant_id} = delivery} <- socket.assigns,
     {:ok, target} <- selected_replay_target(socket.assigns.replay_targets, socket.assigns.replay_selected_target_id),
     {:ok, socket} <- authorize_replay(socket, delivery, target),
     {:ok, result} <- Replay.execute(%{tenant_id: tenant_id, delivery_id: delivery_id, webhook_event_id: target.webhook_event_id, actor: socket.assigns.operator_actor}) do
```

From mailglass_admin/test/support/endpoint_case.ex:
```elixir
@max_age_seconds 900
def authorize(:destructive_action, %{actor: %{recent_auth_at: recent_auth_at}}) when is_struct(recent_auth_at, DateTime) do
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract the shared destructive-action authorization seam for replay</name>
  <files>mailglass_admin/lib/mailglass_admin/auth.ex, mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/test/support/endpoint_case.ex</files>
  <read_first>mailglass_admin/lib/mailglass_admin/auth.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/test/support/endpoint_case.ex, .planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md, .planning/phases/32-replay-reconcile-hardening/32-PATTERNS.md</read_first>
  <action>Create `MailglassAdmin.Operator.DestructiveAction` and move the server-side `Auth.authorize(adapter, :destructive_action, %{actor, delivery, replay_target})` call behind it so Phase 32 follows D-32-01, D-32-02, and D-32-03 exactly. Keep replay target resolution in `OperatorLive`, but require the order `selected_replay_target/2 -> DestructiveAction.authorize/4 -> Replay.execute/1`, and keep stale-auth or unauthorized exits returning early with no replay audit append per D-32-05. Update the public auth documentation/example to describe a 15-minute freshness window as an adopter example, not a library-owned constant, per D-32-04.</action>
  <acceptance_criteria>`mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex` exists and exports `authorize/4`; `mailglass_admin/lib/mailglass_admin/operator_live.ex` calls `DestructiveAction.authorize` before `Replay.execute`; `mailglass_admin/lib/mailglass_admin/auth.ex` or `mailglass_admin/test/support/endpoint_case.ex` contains a 15-minute recent-auth example without introducing any new public auth action atom.</acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Replay authorization is centralized on the existing `:destructive_action` seam, exact-target resolution still happens first, and stale-auth handling remains adopter-owned and action-time enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the existing LiveView replay tests around denial and ambiguity</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex, .planning/phases/32-replay-reconcile-hardening/32-VALIDATION.md</read_first>
  <action>Expand `MailglassAdmin.OperatorLiveTest` instead of creating a new test module. Keep the current replay fixtures, then lock D-32-05 and D-32-11 through assertions that stale-auth denial leaves `replay_audit_rows_for/1` empty, exact-target replay auto-selects only the single candidate case, and ambiguous target sets keep confirm disabled until a concrete webhook row is selected. Do not add reconcile UI assertions or any per-delivery reconcile CTA coverage because D-32-06 and D-32-07 keep replay as the only delivery-detail repair action.</action>
  <acceptance_criteria>`mailglass_admin/test/mailglass_admin/operator_live_test.exs` contains focused assertions for stale-auth no-audit behavior, exact-target auto-selection, and ambiguous target explicit selection; grep for `replay_audit_rows_for`, `Choose one webhook target`, and `Recent authentication is required.` returns matching tests in this file.</acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The highest-risk replay operator flows are locked in the existing LiveView test file without introducing parallel harnesses.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView event -> destructive replay action | Untrusted operator input crosses into replay authorization and execution. |
| Session actor -> adopter auth module | Session data is normalized before a sensitive action is authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Spoofing / Elevation of Privilege | `mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex` | mitigate | Call `MailglassAdmin.Auth.authorize/3` with `:destructive_action` at event time, after exact target resolution and before any replay work or audit writes. |
| T-32-02 | Elevation of Privilege | `mailglass_admin/lib/mailglass_admin/operator_live.ex` | mitigate | Preserve tenant-scoped target lookup and pass the resolved delivery + replay target context into authorization before `Replay.execute/1`. |
| T-32-03 | Tampering | `mailglass_admin/test/mailglass_admin/operator_live_test.exs` | mitigate | Lock the explicit-choice branch so multiple replayable rows remain `choice required` and never auto-select a webhook row. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors`
- `rg -n "DestructiveAction\\.authorize|:destructive_action|15-minute|900" mailglass_admin/lib/mailglass_admin/auth.ex mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex mailglass_admin/lib/mailglass_admin/operator_live.ex mailglass_admin/test/support/endpoint_case.ex`
</verification>

<success_criteria>
Replay requests in the operator UI now share one action-time destructive-action helper, authorization denials stop before audit writes, and exact-target replay remains explicit under ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/32-replay-reconcile-hardening/32-01-SUMMARY.md`
</output>
