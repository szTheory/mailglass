---
phase: "32"
plan: "03"
type: execute
wave: 3
depends_on:
  - "32-02"
files_modified:
  - "lib/mailglass/application.ex"
  - "lib/mailglass/webhook/reconciler.ex"
  - "lib/mix/tasks/mailglass.reconcile.ex"
  - "guides/webhooks.md"
  - "guides/webhook-troubleshooting.md"
  - "test/mailglass/webhook/reconciler_test.exs"
  - "test/mix/tasks/mailglass_reconcile_test.exs"
autonomous: true
requirements:
  - MAT-01
must_haves:
  truths:
    - "Maintenance reconciliation works through one canonical function whether Oban is present or absent."
    - "Operators can understand whether reconcile linked new work or left items still unmatched without implying an exact per-delivery repair button."
    - "Docs, runtime warnings, and tests describe the same Oban-backed and Oban-less reconcile fallback contract."
  artifacts:
    - path: "lib/mailglass/webhook/reconciler.ex"
      provides: "Canonical reconcile function compiled for both Oban-present and Oban-absent installs"
      exports: ["available?/0", "reconcile/2"]
    - path: "lib/mix/tasks/mailglass.reconcile.ex"
      provides: "Manual reconcile entrypoint aligned with the runtime fallback contract"
      contains: "mix mailglass.reconcile"
    - path: "test/mix/tasks/mailglass_reconcile_test.exs"
      provides: "Regression coverage for Oban-present and Oban-absent reconcile task behavior"
      contains: "mailglass.reconcile"
  key_links:
    - from: "lib/mailglass/application.ex"
      to: "lib/mix/tasks/mailglass.reconcile.ex"
      via: "runtime warning text describing the fallback path"
      pattern: "mix mailglass\\.reconcile"
    - from: "lib/mix/tasks/mailglass.reconcile.ex"
      to: "lib/mailglass/webhook/reconciler.ex"
      via: "canonical reconcile invocation"
      pattern: "Mailglass\\.Webhook\\.Reconciler\\.reconcile"
    - from: "guides/webhook-troubleshooting.md"
      to: "test/mix/tasks/mailglass_reconcile_test.exs"
      via: "documented fallback behavior mirrored by automated tests"
      pattern: "Oban|system cron|still unmatched|linked"
---

<objective>
The reconcile fallback story becomes truthful and testable end to end, especially when Oban is not installed.

Purpose: Treat the current Oban-less mismatch as first-class scope so maintenance reconciliation stays usable, append-only, and accurately documented without broadening the operator UI.
Output: A canonical reconcile seam for all runtime modes, updated warnings/docs, and focused fallback regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md
@.planning/phases/32-replay-reconcile-hardening/32-RESEARCH.md
@.planning/phases/32-replay-reconcile-hardening/32-PATTERNS.md
@.planning/phases/32-replay-reconcile-hardening/32-VALIDATION.md
@lib/mailglass/application.ex
@lib/mailglass/webhook/reconciler.ex
@lib/mix/tasks/mailglass.reconcile.ex
@guides/webhooks.md
@guides/webhook-troubleshooting.md

<interfaces>
From lib/mailglass/webhook/reconciler.ex:
```elixir
@spec available?() :: true
def available?, do: true

@spec reconcile(String.t() | nil, pos_integer()) ::
        {:ok, %{scanned: non_neg_integer(), linked: non_neg_integer()}}
```

From lib/mix/tasks/mailglass.reconcile.ex:
```elixir
if Mailglass.Webhook.Reconciler.available?() do
  {:ok, %{scanned: scanned, linked: linked}} =
    Mailglass.Webhook.Reconciler.reconcile(tenant_id, batch_size)
else
  exit({:shutdown, 1})
end
```

From lib/mailglass/application.ex:
```elixir
Logger.warning("""
[mailglass] Webhook orphan reconciliation AND retention pruning require :oban.
Without Oban: orphan events will accumulate until you run
`mix mailglass.reconcile`
""")
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Make reconcile one canonical maintenance path in both Oban-present and Oban-absent installs</name>
  <files>lib/mailglass/application.ex, lib/mailglass/webhook/reconciler.ex, lib/mix/tasks/mailglass.reconcile.ex</files>
  <read_first>lib/mailglass/application.ex, lib/mailglass/webhook/reconciler.ex, lib/mix/tasks/mailglass.reconcile.ex, .planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md, .planning/phases/32-replay-reconcile-hardening/32-RESEARCH.md</read_first>
  <action>Refactor `Mailglass.Webhook.Reconciler` so the canonical `reconcile/2` function exists even when Oban is absent, while the Oban worker-specific pieces remain optional-dep gated. Keep append-only linking semantics and D-32-08/D-32-10 intact, but remove the current contradiction where `mix mailglass.reconcile` fails in the very environment the warning tells adopters to use it. Update the application warning and the mix task output so they describe the same fallback behavior: Oban enables scheduled background runs, while the manual CLI path still performs reconcile work and reports linked vs still-unmatched outcomes without pretending there is a per-delivery reconcile action.</action>
  <acceptance_criteria>`lib/mailglass/webhook/reconciler.ex` exports `reconcile/2` regardless of Oban presence; `lib/mix/tasks/mailglass.reconcile.ex` calls `Mailglass.Webhook.Reconciler.reconcile/2` without exiting solely because Oban is absent; `lib/mailglass/application.ex` warning text matches the new fallback contract and still mentions `mix mailglass.reconcile` and `mix mailglass.webhooks.prune`.</acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/webhook/reconciler_test.exs test/mix/tasks/mailglass_reconcile_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Reconcile behavior is defined once, optional-dep safe, and callable from the maintenance CLI in both runtime modes.</done>
</task>

<task type="auto">
  <name>Task 2: Align reconcile docs and focused regression tests with the fallback contract</name>
  <files>guides/webhooks.md, guides/webhook-troubleshooting.md, test/mailglass/webhook/reconciler_test.exs, test/mix/tasks/mailglass_reconcile_test.exs</files>
  <read_first>guides/webhooks.md, guides/webhook-troubleshooting.md, test/mailglass/webhook/reconciler_test.exs, lib/mailglass/webhook/reconciler.ex, lib/mix/tasks/mailglass.reconcile.ex, .planning/phases/32-replay-reconcile-hardening/32-VALIDATION.md</read_first>
  <action>Update the existing webhook docs so the maintenance story explicitly distinguishes background-first reconcile from the operator replay flow, per D-32-06 through D-32-10 and D-32-17. Document the Oban-backed schedule, the Oban-less CLI/system-cron fallback, and the truthful effect language (`linked` or `still unmatched`) without adding any delivery-detail reconcile CTA. Extend `test/mailglass/webhook/reconciler_test.exs` for append-only and idempotent reconcile behavior, and create `test/mix/tasks/mailglass_reconcile_test.exs` to lock both Oban-present and Oban-absent task output/exit semantics instead of creating a parallel test harness elsewhere.</action>
  <acceptance_criteria>`guides/webhooks.md` and `guides/webhook-troubleshooting.md` both mention the same Oban-backed and system-cron fallback contract; `test/mix/tasks/mailglass_reconcile_test.exs` exists and covers Oban-present plus Oban-absent behavior; `test/mailglass/webhook/reconciler_test.exs` continues asserting append-only `:reconciled` semantics and idempotent linking.</acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/webhook/reconciler_test.exs test/mix/tasks/mailglass_reconcile_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Operators and maintainers get one consistent reconcile contract across runtime warnings, docs, and automated tests, with no new operator-facing reconcile button.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime environment -> optional reconcile worker | Oban may or may not be installed, but maintenance reconciliation must remain truthful and safe. |
| Maintenance CLI -> append-only reconcile service | A manual operator/cron entrypoint triggers database writes and audit events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Denial of Service | `lib/mailglass/webhook/reconciler.ex` | mitigate | Keep one canonical `reconcile/2` path that runs with or without Oban so orphan linking does not depend on a missing worker runtime. |
| T-32-08 | Tampering / Repudiation | `test/mailglass/webhook/reconciler_test.exs` | mitigate | Preserve append-only `:reconciled` events, idempotency-key protection, and no direct orphan-row mutation through focused integration tests. |
| T-32-09 | Information Disclosure / Misconfiguration | `guides/webhooks.md`, `guides/webhook-troubleshooting.md`, `lib/mailglass/application.ex` | mitigate | Keep runtime warnings and docs aligned on the fallback contract so operators do not infer a false delivery-level reconcile action or unsafe global maintenance semantics. |
</threat_model>

<verification>
- `mix test test/mailglass/webhook/reconciler_test.exs test/mix/tasks/mailglass_reconcile_test.exs --warnings-as-errors`
- `rg -n "mailglass\\.reconcile|system cron|linked|still unmatched|Oban" lib/mailglass/application.ex lib/mix/tasks/mailglass.reconcile.ex guides/webhooks.md guides/webhook-troubleshooting.md`
</verification>

<success_criteria>
Reconcile stays a background-first maintenance action, but the Oban-less fallback finally works and is described consistently everywhere the operator or maintainer sees it.
</success_criteria>

<output>
After completion, create `.planning/phases/32-replay-reconcile-hardening/32-03-SUMMARY.md`
</output>
