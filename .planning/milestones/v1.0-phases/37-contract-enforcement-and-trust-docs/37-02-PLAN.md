---
phase: "37"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mailglass_admin/docs/operator-trust.md"
  - "mailglass_admin/docs/api_stability.md"
  - "mailglass_admin/README.md"
  - "mailglass_admin/mix.exs"
  - "guides/operator-incident-support.md"
  - "mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs"
  - "mailglass_admin/test/mailglass_admin/router_test.exs"
  - "mailglass_admin/test/mailglass_admin/auth_test.exs"
  - "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
autonomous: true
requirements:
  - PROOF-04
must_haves:
  truths:
    - "Adopters can read one canonical admin trust doc that names the stable router/auth/session/replay semantics without treating UI implementation details as stable."
    - "The admin README and API stability inventory point to the canonical trust doc instead of competing with it."
    - "The proof surface stays semantic and code-backed through router/auth/operator tests rather than DOM-shape assertions."
  artifacts:
    - path: "mailglass_admin/docs/operator-trust.md"
      provides: "Canonical admin trust contract for router, auth, session, and replay semantics"
      contains: "MailglassAdmin.Router"
    - path: "mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs"
      provides: "Deterministic docs proof for the canonical admin trust doc"
      contains: "canonical admin trust doc"
    - path: "mailglass_admin/README.md"
      provides: "Tier 1 pointer to the canonical admin trust doc with narrow stable-vs-internal wording"
      contains: "operator trust"
  key_links:
    - from: "mailglass_admin/docs/operator-trust.md"
      to: "mailglass_admin/lib/mailglass_admin/router.ex"
      via: "stable router macros, documented options, and session whitelist semantics"
      pattern: "mailglass_admin_routes|mailglass_operator_routes|subject_id|tenant_id|recent_auth_at"
    - from: "mailglass_admin/docs/operator-trust.md"
      to: "mailglass_admin/lib/mailglass_admin/auth.ex"
      via: "adopter-owned auth seam and `authorize/2` action semantics"
      pattern: "authorize|operator_access|destructive_action"
    - from: "mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs"
      to: "mailglass_admin/docs/operator-trust.md"
      via: "exact section and wording assertions for the canonical trust contract"
      pattern: "replay|reconcile|destructive_action|subject_id"
---

<objective>
Publish the canonical admin trust contract and wire it to the already-shipped router, auth, session, and replay semantics.

Purpose: Satisfy PROOF-04 with one stable semantic story and executable proof, while preserving freedom to change DOM, component, and LiveView internals.
Output: A canonical admin trust doc, aligned README/API-stability/docs config pointers, and targeted proof tests anchored to router/auth/operator semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-PATTERNS.md
@mailglass_admin/README.md
@mailglass_admin/docs/api_stability.md
@mailglass_admin/mix.exs
@mailglass_admin/lib/mailglass_admin/router.ex
@mailglass_admin/lib/mailglass_admin/auth.ex
@mailglass_admin/lib/mailglass_admin/operator/mount.ex
@mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@guides/operator-incident-support.md
@mailglass_admin/test/mailglass_admin/router_test.exs
@mailglass_admin/test/mailglass_admin/auth_test.exs
@mailglass_admin/test/mailglass_admin/operator_live_test.exs

<interfaces>
From mailglass_admin/lib/mailglass_admin/router.ex:
```elixir
mailglass_admin_routes(path, opts \\ [])
mailglass_operator_routes(path, opts \\ [])
```

From mailglass_admin/lib/mailglass_admin/auth.ex:
```elixir
@type action :: :operator_access | :destructive_action | atom()
@callback authorize(action(), context :: map()) :: result()
def authorize(module, action, context)
```

From guides/operator-incident-support.md:
```markdown
Replay is an operator action against one exact stored webhook event
Reconcile is background-first backlog maintenance
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create the canonical admin trust doc and align Tier 1 pointers</name>
  <files>mailglass_admin/docs/operator-trust.md, mailglass_admin/docs/api_stability.md, mailglass_admin/README.md, mailglass_admin/mix.exs, guides/operator-incident-support.md</files>
  <read_first>mailglass_admin/README.md, mailglass_admin/docs/api_stability.md, mailglass_admin/mix.exs, .planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md, mailglass_admin/lib/mailglass_admin/router.ex, mailglass_admin/lib/mailglass_admin/auth.ex, mailglass_admin/lib/mailglass_admin/operator/mount.ex, mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, guides/operator-incident-support.md</read_first>
  <acceptance_criteria>
    - `mailglass_admin/docs/operator-trust.md` exists and contains the headings `## Stable seams`, `## Session contract`, `## Authorization timing`, `## Replay semantics`, and `## Intentionally internal`.
    - `mailglass_admin/docs/operator-trust.md` explicitly names `MailglassAdmin.Router` macros, `MailglassAdmin.Auth.authorize/2`, `subject_id`, optional `tenant_id`, optional `auth_method`, optional `recent_auth_at`, `:operator_access`, and `:destructive_action`.
    - `mailglass_admin/docs/operator-trust.md` explicitly states that LiveView modules, component modules, DOM/CSS shape, event names, modal details, and internal assign names are not stable.
    - `mailglass_admin/README.md` points to `docs/operator-trust.md` as the canonical trust doc.
    - `mailglass_admin/mix.exs` exposes `docs/operator-trust.md` in ExDoc extras/groups.
    - `guides/operator-incident-support.md` uses replay-vs-reconcile wording consistent with the new trust doc.
  </acceptance_criteria>
  <action>Create `mailglass_admin/docs/operator-trust.md` as the canonical admin trust contract required by D-37-12 through D-37-15. Structure it with exact sections for stable seams, session contract, authorization timing, replay semantics, and intentionally internal details. In the stable sections, document these precise promises: `MailglassAdmin.Router` mount macros and documented options are the stable mount seam; `MailglassAdmin.Auth.authorize/2` is the stable adopter-owned auth seam; the operator session contract includes required `subject_id` and optional `tenant_id`, `auth_method`, and `recent_auth_at`; mount-time authorization uses `:operator_access`; action-time authorization uses `:destructive_action`; replay is exact-target, tenant-scoped, ledger-audited, and can honestly result in `new work` or `no change`. In the intentionally-internal section, name LiveView modules, component modules, DOM/CSS shape, event names, modal layout/details, and internal assign names as non-contract surfaces. Then update `mailglass_admin/README.md`, `mailglass_admin/docs/api_stability.md`, and `mailglass_admin/mix.exs` so they point to this doc instead of duplicating the full trust story. Align `guides/operator-incident-support.md` so replay and reconcile use the same exact distinctions as the trust doc. Do not document selectors, component APIs, or internal module names as stable.</action>
  <verify>
    <automated>cd mailglass_admin && mix docs --warnings-as-errors >/tmp/mailglass-admin-phase37-docs.log && rg -n "^## Stable seams$|^## Session contract$|^## Authorization timing$|^## Replay semantics$|^## Intentionally internal$|MailglassAdmin\\.Router|MailglassAdmin\\.Auth\\.authorize/2|subject_id|tenant_id|auth_method|recent_auth_at|operator_access|destructive_action|LiveView modules|DOM/CSS shape|new work|no change" docs/operator-trust.md README.md docs/api_stability.md mix.exs && cd /Users/jon/projects/mailglass && rg -n "Replay is|Reconcile is|new work|no change" guides/operator-incident-support.md</automated>
  </verify>
  <done>The admin trust story now has one canonical package-local document, and every Tier 1/admin pointer surface routes adopters to that same semantic contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic docs proof and tighten semantic tests around the stable admin seams</name>
  <files>mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs, mailglass_admin/test/mailglass_admin/router_test.exs, mailglass_admin/test/mailglass_admin/auth_test.exs, mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>mailglass_admin/test/mailglass_admin/router_test.exs, mailglass_admin/test/mailglass_admin/auth_test.exs, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/docs/operator-trust.md, mailglass_admin/lib/mailglass_admin/router.ex, mailglass_admin/lib/mailglass_admin/auth.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, guides/operator-incident-support.md</read_first>
  <acceptance_criteria>
    - `mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs` exists and contains a test description mentioning `canonical admin trust doc`.
    - `mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs` asserts the doc mentions `subject_id`, `operator_access`, `destructive_action`, `new work`, and `no change`.
    - `mailglass_admin/test/mailglass_admin/router_test.exs`, `auth_test.exs`, or `operator_live_test.exs` contain focused assertions that back the documented stable semantics rather than DOM-shape details.
    - `cd mailglass_admin && mix test test/mailglass_admin/operator_trust_doc_test.exs test/mailglass_admin/router_test.exs test/mailglass_admin/auth_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>Create `mailglass_admin/test/mailglass_admin/operator_trust_doc_test.exs` as the dedicated docs proof for PROOF-04. Assert the exact promise-level text from Task 1: stable router macros, session-key semantics, mount-time `:operator_access`, action-time `:destructive_action`, exact-target replay, and replay outcomes `new work` / `no change`. Then review the existing router/auth/operator tests and add only the focused assertions needed to keep the docs anchored to the stable semantics: router option/session whitelist behavior in `router_test.exs`, auth result normalization and stale-auth handling in `auth_test.exs`, and replay/action-time semantics in `operator_live_test.exs`. Keep those tests seam-centered and semantic; do not add assertions on selectors, component structure, CSS classes, or internal LiveView module names.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_trust_doc_test.exs test/mailglass_admin/router_test.exs test/mailglass_admin/auth_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The canonical admin trust doc is now guarded by deterministic docs assertions and the existing semantic admin test seams.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin trust docs -> adopter auth and routing code | Adopters wire router/auth/session behavior into production apps from these docs. |
| Trust docs -> operator actions | The docs frame when replay is safe and what it means. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering / Misrepresentation | `mailglass_admin/docs/operator-trust.md` | mitigate | Prove exact stable-seam wording with dedicated docs tests and align README/API-stability pointers to one canonical source. |
| T-37-05 | Elevation of Privilege | router/auth/session docs | mitigate | Document the explicit session whitelist plus mount-time and action-time authorization timing so adopters do not assume broader implicit auth behavior. |
| T-37-06 | Information Disclosure | internal UI details | mitigate | Explicitly classify DOM/component/LiveView details as internal so examples do not accidentally freeze private implementation. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_trust_doc_test.exs test/mailglass_admin/router_test.exs test/mailglass_admin/auth_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix docs --warnings-as-errors >/tmp/mailglass-admin-phase37-docs.log`
- `rg -n "operator-trust|subject_id|tenant_id|auth_method|recent_auth_at|operator_access|destructive_action|new work|no change|Intentionally internal" mailglass_admin/docs/operator-trust.md mailglass_admin/README.md mailglass_admin/docs/api_stability.md mailglass_admin/mix.exs guides/operator-incident-support.md`
</verification>

<success_criteria>
Adopters can rely on one semantic admin trust contract for router, auth, session, and replay behavior, and that contract stays explicitly separate from internal UI implementation details.
</success_criteria>

<output>
After completion, create `.planning/phases/37-contract-enforcement-and-trust-docs/37-02-SUMMARY.md`
</output>
