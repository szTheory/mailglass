---
phase: "38"
plan: "02"
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - ".github/workflows/post-publish-smoke.yml"
  - "test/mailglass/install/install_first_preview_smoke_test.exs"
  - "test/mailglass/install/install_first_send_smoke_test.exs"
  - "guides/getting-started.md"
  - "guides/upgrading-to-v1_0.md"
  - "test/mailglass/docs_migration_smoke_test.exs"
  - ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md"
  - ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md"
autonomous: true
requirements:
  - RELS-01
  - RELS-02
must_haves:
  truths:
    - "Maintainers can prove the canonical released-package install path still uses a fresh `phx.new --no-ecto --no-mailer --install` host, boots cleanly, and returns HTTP 200 from `/dev/mail/`."
    - "The fresh-host proof explicitly verifies the generated `config :swoosh, :api_client, false` sentinel."
    - "Maintainers can also execute the documented first-send workflow in a separate disposable Ecto-backed host that mirrors `guides/getting-started.md` without replacing the fast smoke gate."
    - "Maintainers can rehearse the canonical latest `0.3.x -> 1.0` upgrade path with strict docs and stability gates, and `Mailglass.Message.new/2` remains release-blocking for warnings-as-errors adopters."
  artifacts:
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "Canonical released-package smoke workflow for install rehearsal"
      contains: "mix phx.new sandbox --module Sandbox --app sandbox --no-ecto --no-mailer --install"
    - path: "test/mailglass/install/install_first_preview_smoke_test.exs"
      provides: "Repo-local mirror of the release-day fresh-host install contract"
      contains: "config :swoosh, :api_client, false"
    - path: "test/mailglass/install/install_first_send_smoke_test.exs"
      provides: "Secondary executable proof for the documented Ecto-backed first-send workflow"
      contains: "Mailglass.deliver"
    - path: "guides/upgrading-to-v1_0.md"
      provides: "Canonical strict upgrade rehearsal contract"
      contains: "mix verify.stability_contract"
    - path: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md"
      provides: "Supporting detailed install and upgrade rehearsal log with concrete results from phase execution"
      contains: "## Upgrade rehearsal proof"
    - path: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md"
      provides: "Canonical proof bundle updated with concrete install and upgrade rehearsal evidence"
      contains: "## Release rehearsal evidence"
  key_links:
    - from: ".github/workflows/post-publish-smoke.yml"
      to: "test/mailglass/install/install_first_preview_smoke_test.exs"
      via: "workflow and test mirror the same add deps -> install -> compile -> boot -> GET /dev/mail/ contract"
      pattern: "mix mailglass\\.install|GET /dev/mail/|api_client, false"
    - from: "guides/getting-started.md"
      to: "test/mailglass/install/install_first_send_smoke_test.exs"
      via: "secondary first-send rehearsal mirrors the documented Ecto-backed welcome-mail flow"
      pattern: "mix ecto\\.migrate|Mailglass\\.deliver|subject\\(\"Welcome\"\\)"
    - from: "guides/upgrading-to-v1_0.md"
      to: "test/mailglass/docs_migration_smoke_test.exs"
      via: "strict upgrade guidance is executable and stays recommendation-first"
      pattern: "verify\\.docs\\.migration|verify\\.stability_contract|Mailglass\\.Message\\.new/2"
    - from: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md"
      to: ".github/workflows/post-publish-smoke.yml"
      via: "record captures the canonical run URL and output fields for the released-package smoke"
      pattern: "workflow run URL|/dev/mail/"
    - from: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md"
      to: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md"
      via: "supporting rehearsal log feeds the canonical coherent proof bundle required by D-38-15"
      pattern: "Release rehearsal evidence|Install rehearsal|Upgrade rehearsal"
---

<objective>
Lock the canonical install and upgrade rehearsal evidence around the already-documented `v1.0` paths.

Purpose: Satisfy RELS-01 and RELS-02 per D-38-04 through D-38-13 by keeping the fast no-Ecto released-package host as the default release gate while recording a strict guide-backed upgrade rehearsal lane.
Output: A hardened post-publish smoke/workflow contract, a populated install/upgrade rehearsal record with concrete results, and targeted guide/tests updates that keep the canonical install and upgrade paths honest.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-CONTEXT.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-RESEARCH.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-PATTERNS.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-VALIDATION.md
@MAINTAINING.md
@.github/workflows/post-publish-smoke.yml
@test/mailglass/install/install_first_preview_smoke_test.exs
@mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs
@guides/getting-started.md
@guides/upgrading-to-v1_0.md
@guides/upgrading-from-v0_1.md
@guides/migration-from-swoosh.md
@test/mailglass/docs_migration_smoke_test.exs

<interfaces>
From .github/workflows/post-publish-smoke.yml:
```yaml
- name: Generate Phoenix host project
  run: mix phx.new sandbox --module Sandbox --app sandbox --no-ecto --no-mailer --install
- name: Run mix mailglass.install
- name: Compile, fail on warnings
- name: Boot endpoint and curl /dev/mail/
```

From test/mailglass/install/install_first_preview_smoke_test.exs:
```elixir
assert runtime =~ "config :swoosh, :api_client, false"
assert workflow =~ "Run mix mailglass.install"
assert workflow =~ "GET /dev/mail/ → HTTP ${STATUS}"
```

From guides/upgrading-to-v1_0.md / test/mailglass/docs_migration_smoke_test.exs:
```elixir
assert canonical =~ "mix verify.docs.migration"
assert canonical =~ "mix verify.stability_contract"
assert canonical =~ "Mailglass.Outbound.send/2"
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Keep the released-package install rehearsal fast and add an explicit first-send proof lane</name>
  <files>.github/workflows/post-publish-smoke.yml, test/mailglass/install/install_first_preview_smoke_test.exs, test/mailglass/install/install_first_send_smoke_test.exs, guides/getting-started.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md</files>
  <read_first>.github/workflows/post-publish-smoke.yml, test/mailglass/install/install_first_preview_smoke_test.exs, mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs, guides/getting-started.md, MAINTAINING.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-CONTEXT.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-RESEARCH.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-PATTERNS.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md, test/support/installer_fixture_helpers.ex</read_first>
  <acceptance_criteria>
    - `.github/workflows/post-publish-smoke.yml` still generates the host with `--no-ecto --no-mailer --install`.
    - The workflow or its mirrored test explicitly verifies `config :swoosh, :api_client, false` after `mix mailglass.install`.
    - The workflow still runs `mix deps.get`, `mix mailglass.install`, `mix compile --warnings-as-errors`, boots the endpoint, and expects `GET /dev/mail/` to return HTTP 200.
    - `test/mailglass/install/install_first_send_smoke_test.exs` exists and exercises the complete documented first-send flow from `guides/getting-started.md` in one clean consumer host using released Hex packages, with the admin mount seam present in the same app: `mix deps.get`, `mix mailglass.install`, admin routes mounted, `mix ecto.migrate`, compile a welcome mailable, and assert `Mailglass.deliver()` succeeds or the documented preview/send path completes.
    - `38-02-REHEARSAL-EVIDENCE.md` contains an `## Install rehearsal proof` section and an `## First-send workflow proof` section populated with concrete values for `Tag`, `Workflow run URL`, `Compile result`, `Swoosh sentinel result`, ``GET /dev/mail/`` status, first-send command outcomes, and first-send outcome.
    - `38-01-PREPUBLISH-PROOF.md` is updated with a `## Release rehearsal evidence` section that summarizes the concrete install and first-send outcomes from this task and links to `38-02-REHEARSAL-EVIDENCE.md` as supporting detail.
    - `guides/getting-started.md` remains aligned with the message-first install story and does not reintroduce raw `Swoosh.Email.to` as the canonical first step.
  </acceptance_criteria>
  <action>Update `.github/workflows/post-publish-smoke.yml` and `test/mailglass/install/install_first_preview_smoke_test.exs` so the canonical release-window install proof remains exactly the D-38-04 / D-38-05 path and preserves the exact workflow/test mirror pattern in `38-PATTERNS.md`: fresh `phx.new --no-ecto --no-mailer --install` host, add `mailglass` and `mailglass_admin`, run `mix deps.get`, run `mix mailglass.install`, run `mix compile --warnings-as-errors`, boot, and verify `GET /dev/mail/` returns HTTP 200. Add one explicit sentinel verification for D-38-06: confirm the generated runtime config contains `config :swoosh, :api_client, false`. Then add `test/mailglass/install/install_first_send_smoke_test.exs` as the resolved RELS-01 end-to-end proof lane in one clean consumer host using released Hex packages: create a disposable Ecto-enabled Phoenix fixture app, add `mailglass` and `mailglass_admin` from Hex, run `mix deps.get`, run `mix mailglass.install`, mount the admin preview routes in the same app, run `mix ecto.migrate`, define the documented welcome mailable from `guides/getting-started.md`, and verify the first-send workflow completes through `Mailglass.deliver()` or the exact documented preview/send path. This one host must prove released-package install, admin mount, and first-send together. Run both rehearsal lanes during phase execution and commit `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md` with concrete values from those runs: tag/version used, command outcomes, `/dev/mail/` status, sentinel result, admin mount result, first-send command outcomes, first-send result, and any rehearsal run URLs available. In the same task, append those concrete results into the canonical `38-01-PREPUBLISH-PROOF.md` `## Release rehearsal evidence` section, using `38-02-REHEARSAL-EVIDENCE.md` only as the supporting detailed log. Keep the fast no-Ecto lane as the canonical release-window gate, but make the released-package/admin/first-send consumer-host proof explicit and executable.</action>
  <verify>
    <automated>actionlint .github/workflows/post-publish-smoke.yml && mix test test/mailglass/install/install_first_preview_smoke_test.exs test/mailglass/install/install_first_send_smoke_test.exs --warnings-as-errors && rg -n "no-ecto --no-mailer --install|config :swoosh, :api_client, false|Run mix mailglass.install|Compile, fail on warnings|GET /dev/mail/|First-send workflow proof|Mailglass.deliver|mix ecto.migrate|Command outcome|Workflow run URL" .github/workflows/post-publish-smoke.yml test/mailglass/install/install_first_preview_smoke_test.exs test/mailglass/install/install_first_send_smoke_test.exs guides/getting-started.md .planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md && rg -n "Release rehearsal evidence|Install rehearsal|First-send workflow proof|38-02-REHEARSAL-EVIDENCE.md" .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md</automated>
  </verify>
  <done>The canonical released-package install rehearsal remains fast, strict, and explicit, and RELS-01 now also has a separate executable first-send proof lane that mirrors the public getting-started story.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn the canonical `0.3.x -> 1.0` upgrade lane into a strict rehearsal record</name>
  <files>guides/upgrading-to-v1_0.md, test/mailglass/docs_migration_smoke_test.exs, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md</files>
  <read_first>guides/upgrading-to-v1_0.md, guides/upgrading-from-v0_1.md, guides/migration-from-swoosh.md, test/mailglass/docs_migration_smoke_test.exs, .planning/phases/36-deprecation-and-compatibility-contract/36-CONTEXT.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-CONTEXT.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-RESEARCH.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-PATTERNS.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md</read_first>
  <acceptance_criteria>
    - `guides/upgrading-to-v1_0.md` names `mix verify.docs.migration`, `mix verify.stability_contract`, `mix docs --warnings-as-errors`, and `cd mailglass_admin && mix docs --warnings-as-errors` as the strict upgrade rehearsal gate.
    - `guides/upgrading-to-v1_0.md` explicitly marks `Mailglass.Message.new/2` as release-blocking for strict adopters under `--warnings-as-errors`.
    - `guides/upgrading-to-v1_0.md` keeps raw `%Swoosh.Email{}` delivery and `Mailglass.Outbound.send/2` documented as transitional compatibility paths, not as the preferred `1.x` posture.
    - `38-02-REHEARSAL-EVIDENCE.md` contains an `## Upgrade rehearsal proof` section populated with concrete values for `Version pair`, `Strict gate commands`, `Warning outcome`, `mix docs --warnings-as-errors result`, `cd mailglass_admin && mix docs --warnings-as-errors result`, `Fallback path used`, and `Admin docs build result`.
    - `38-01-PREPUBLISH-PROOF.md` is updated with the concrete upgrade rehearsal outcomes inside its `## Release rehearsal evidence` section and links to `38-02-REHEARSAL-EVIDENCE.md` for supporting detail.
    - `test/mailglass/docs_migration_smoke_test.exs` asserts the strict gate commands and the `Mailglass.Message.new/2` release-blocking wording.
  </acceptance_criteria>
  <action>Keep `guides/upgrading-to-v1_0.md` as the single authority per D-38-08 through D-38-13 and extend it with the exact strict rehearsal commands already mirrored in `38-PATTERNS.md`: `mix verify.docs.migration`, `mix verify.stability_contract`, `mix docs --warnings-as-errors`, and `cd mailglass_admin && mix docs --warnings-as-errors`. Make the guide explicit that `Mailglass.Message.new/2` is release-blocking for warnings-as-errors adopters, while raw `%Swoosh.Email{}` delivery and `Mailglass.Outbound.send/2` remain compatibility-lane bridges only. In `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md`, add a dedicated upgrade-proof section and populate it with concrete evidence from the actual strict rehearsal run: version pair, each command executed, warning outcome, both docs-build results, fallback path used, and any run URLs available for the rehearsal. In the same task, append those concrete upgrade results into `38-01-PREPUBLISH-PROOF.md` `## Release rehearsal evidence`, keeping the proof bundle authoritative and the phase-local rehearsal doc as supporting detail. Extend `test/mailglass/docs_migration_smoke_test.exs` so it fails if the guide stops naming those commands, stops flagging `Mailglass.Message.new/2`, or starts normalizing compatibility bridges as the preferred `1.x` story.</action>
  <verify>
    <automated>mix test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors && mix verify.docs.migration && mix verify.stability_contract && mix docs --warnings-as-errors && cd mailglass_admin && mix docs --warnings-as-errors && cd .. && rg -n "mix verify.docs.migration|mix verify.stability_contract|mix docs --warnings-as-errors|Mailglass.Message.new/2|Mailglass.Outbound.send/2|Fallback path used|Admin docs build result|Command outcome|Version pair" guides/upgrading-to-v1_0.md .planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-REHEARSAL-EVIDENCE.md test/mailglass/docs_migration_smoke_test.exs && rg -n "Release rehearsal evidence|Upgrade rehearsal|38-02-REHEARSAL-EVIDENCE.md|docs --warnings-as-errors" .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md</automated>
  </verify>
  <done>The canonical upgrade guide now doubles as a strict rehearsal contract, and the release-blocking warning posture is executable and documented.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| released package -> fresh Phoenix host | The public package is validated from a consumer app rather than from local path dependencies. |
| upgrade guide -> strict adopter workflow | The documented migration path directly affects whether warnings-as-errors adopters can adopt `v1.0` safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering | `.github/workflows/post-publish-smoke.yml`, `install_first_preview_smoke_test.exs` | mitigate | Keep the workflow and repo-local smoke test byte-aligned on the canonical fresh-host contract, including the Swoosh sentinel and `/dev/mail/` HTTP 200. |
| T-38-05 | Repudiation | `guides/upgrading-to-v1_0.md`, `docs_migration_smoke_test.exs` | mitigate | Require exact strict-gate commands and release-blocking warning text in the canonical upgrade guide, then enforce them in docs migration tests. |
| T-38-06 | Denial of Service | install and upgrade rehearsal scope | accept | Keep the default install gate on the fast no-Ecto host and keep any deeper first-send lane secondary so release-window feedback stays fast and reliable. |
</threat_model>

<verification>
- `mix test test/mailglass/install/install_first_preview_smoke_test.exs --warnings-as-errors`
- `mix test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors`
- `mix verify.docs.migration`
- `mix verify.stability_contract`
</verification>

<success_criteria>
The repo has one committed install/upgrade rehearsal artifact, the canonical fast install smoke remains intact, and the latest `0.3.x -> 1.0` upgrade path is strict enough to block a release when warning-producing legacy paths remain.
</success_criteria>

<output>
After completion, create `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-02-SUMMARY.md`
</output>
