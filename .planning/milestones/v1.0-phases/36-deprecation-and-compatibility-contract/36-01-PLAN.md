---
phase: 36-deprecation-and-compatibility-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/compatibility-and-deprecations.md
  - README.md
  - mailglass_admin/README.md
  - MAINTAINING.md
  - docs/api_stability.md
  - mailglass_admin/docs/api_stability.md
  - mailglass_admin/docs/compatibility-and-deprecations.md
  - mix.exs
  - mailglass_admin/mix.exs
autonomous: true
requirements:
  - COMPAT-01
  - COMPAT-02
must_haves:
  truths:
    - "Adopters can find one canonical `1.x` compatibility and deprecation policy instead of piecing it together from multiple docs."
    - "The documented support matrix stays narrow and matches repo-proven floors, required lanes, and sibling-package truth."
    - "Core and admin contract inventories stay focused on stable/internal surface classification while pointing outward to the compatibility policy."
  artifacts:
    - path: "guides/compatibility-and-deprecations.md"
      provides: "Canonical `1.x` versioning, deprecation, support-matrix, and exception policy"
      min_lines: 120
    - path: "README.md"
      provides: "Root adopter pointer to the canonical compatibility policy"
      min_lines: 200
    - path: "mailglass_admin/README.md"
      provides: "Matched-sibling admin pointer to the same canonical compatibility policy"
      min_lines: 120
    - path: "mix.exs"
      provides: "Root ExDoc navigation exposing the canonical compatibility guide"
      contains: "guides/compatibility-and-deprecations.md"
    - path: "mailglass_admin/docs/compatibility-and-deprecations.md"
      provides: "Admin-local pointer page that exposes the canonical compatibility guide without duplicating policy text"
      contains: "guides/compatibility-and-deprecations"
    - path: "mailglass_admin/mix.exs"
      provides: "Admin ExDoc navigation pointing to the local compatibility pointer page"
      contains: "compatibility-and-deprecations"
  key_links:
    - from: "README.md"
      to: "guides/compatibility-and-deprecations.md"
      via: "canonical compatibility pointer"
      pattern: "compatibility-and-deprecations"
    - from: "mailglass_admin/README.md"
      to: "guides/compatibility-and-deprecations.md"
      via: "matched-sibling compatibility pointer"
      pattern: "compatibility-and-deprecations"
    - from: "guides/compatibility-and-deprecations.md"
      to: "mix.exs"
      via: "runtime floor and optional-lane support matrix"
      pattern: "1\\.18|1\\.8|1\\.1|3\\.13|Postgres 14"
---

<objective>
Publish the canonical `1.x` compatibility/deprecation policy and support matrix, then make every Tier 1 entrypoint point to that one guide.

Purpose: Satisfy COMPAT-01 and COMPAT-02 with one narrow, repo-truth compatibility promise that does not blur into the Phase 35 stability inventories.
Output: A canonical compatibility guide, aligned README/maintainer pointers, and ExDoc navigation that exposes the root guide directly in `mailglass` and through an admin-local pointer page in `mailglass_admin`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-CONTEXT.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-RESEARCH.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-PATTERNS.md
@.planning/phases/35-stability-contract-audit/35-01-PLAN.md
@.planning/phases/35-stability-contract-audit/35-02-PLAN.md
@.planning/phases/35-stability-contract-audit/35-03-PLAN.md
@README.md
@mailglass_admin/README.md
@MAINTAINING.md
@docs/api_stability.md
@mailglass_admin/docs/api_stability.md
@mix.exs
@mailglass_admin/mix.exs
@scripts/verify_support_contract.sh
@.github/workflows/ci.yml

<interfaces>
From mix.exs:
```elixir
version: @version,
elixir: "~> 1.18",
deps: [
  {:phoenix, "~> 1.8"},
  {:phoenix_live_view, "~> 1.1"},
  {:ecto, "~> 3.13"},
  {:ecto_sql, "~> 3.13"},
  {:postgrex, "~> 0.22"}
]
```

From mailglass_admin/mix.exs:
```elixir
version: @version,
elixir: "~> 1.18",
defp mailglass_dep do
  if System.get_env("MIX_PUBLISH") == "true" do
    {:mailglass, "== 0.3.2"}
  else
    {:mailglass, path: "..", override: true}
  end
end
```

From scripts/verify_support_contract.sh:
```bash
mix verify.support_contract.core
cd "$ROOT_DIR/mailglass_admin"
mix verify.support_contract.admin
cd "$ROOT_DIR"
mix compile --no-optional-deps --warnings-as-errors
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical compatibility and deprecation guide</name>
  <files>guides/compatibility-and-deprecations.md</files>
  <action>Create `guides/compatibility-and-deprecations.md` as the single canonical adopter-facing policy per D-36-01 through D-36-17, D-36-21, and D-36-25. The guide must stay separate from the Phase 35 stability inventories and must explicitly define the two-lane model from D-36-10: stable lane (`Mailglass.deliver*`, native `Mailglass.Message` setters, `Mailglass.Message.update_swoosh/2`) versus compatibility lane (small retained legacy bridges only). Include explicit patch/minor/major guarantees, the narrow security/correctness exception clause from D-36-13 and D-36-17, warnings-as-errors guidance from D-36-21, and a support matrix tied to repo truth only: Elixir `~> 1.18`, OTP `27+`, Phoenix `~> 1.8`, LiveView `~> 1.1`, Ecto/Ecto SQL `~> 3.13`, Postgres 14+, matched `mailglass_admin` release lines, `mailglass_inbound` excluded per D-36-07, and optional-dependency lanes documented as supported integrations when present per D-36-08. Use `mix.exs`, `mailglass_admin/mix.exs`, `scripts/verify_support_contract.sh`, and `.github/workflows/ci.yml` as the proof sources for each row. Do not promise broader runtime, database, or sibling-package support than those files prove.</action>
  <verify>
    <automated>rg -n "stable lane|compatibility lane|patch releases|minor releases|major releases|warnings-as-errors|Elixir `~> 1\\.18`|OTP `27\\+`|Phoenix `~> 1\\.8`|LiveView `~> 1\\.1`|Ecto / Ecto SQL `~> 3\\.13`|Postgres 14\\+|mailglass_inbound" guides/compatibility-and-deprecations.md</automated>
  </verify>
  <done>The repo has one canonical compatibility policy and support matrix, and every policy claim is traceable to current package metadata or CI/support-contract truth.</done>
</task>

<task type="auto">
  <name>Task 2: Point Tier 1 docs and package docs to the canonical guide</name>
  <files>README.md, mailglass_admin/README.md, MAINTAINING.md, docs/api_stability.md, mailglass_admin/docs/api_stability.md, mailglass_admin/docs/compatibility-and-deprecations.md, mix.exs, mailglass_admin/mix.exs</files>
  <action>Update the adopter and maintainer entrypoints so they all point to the same canonical guide per D-36-02 while preserving Phase 35’s separation between stability inventory and compatibility policy. `README.md` and `mailglass_admin/README.md` should link to the canonical guide and summarize only the narrow high-level posture, not restate the full policy. `MAINTAINING.md` should reference the canonical policy when describing release/retract posture and support-contract truth, keeping the required-versus-advisory split honest. `docs/api_stability.md` and `mailglass_admin/docs/api_stability.md` should add short outward links for compatibility/deprecation/upgrade behavior but stay inventory-shaped. For `mailglass_admin` HexDocs, do not depend on a repo-root extra path that may not be publish-safe; instead create `mailglass_admin/docs/compatibility-and-deprecations.md` as a short package-local pointer page that declares the repo-root guide as canonical and links to it without duplicating policy prose. Surface the root guide directly in `mix.exs` and the admin pointer page in `mailglass_admin/mix.exs`, then prove both docs builds succeed. Do not move compatibility inventory into the stability docs, and do not add Phase 37 heavy enforcement language.</action>
  <verify>
    <automated>mix docs --warnings-as-errors >/tmp/mailglass-phase36-docs.log && cd mailglass_admin && mix docs --warnings-as-errors >/tmp/mailglass-admin-phase36-docs.log && cd .. && rg -n "compatibility-and-deprecations|matched release|support matrix|upgrade guide" README.md mailglass_admin/README.md MAINTAINING.md docs/api_stability.md mailglass_admin/docs/api_stability.md mailglass_admin/docs/compatibility-and-deprecations.md mix.exs mailglass_admin/mix.exs && rg -n "compatibility-and-deprecations|Compatibility|Deprecation" doc/**/*.html mailglass_admin/doc/**/*.html</automated>
  </verify>
  <done>Every Tier 1 doc and both package docs configurations point to one canonical compatibility policy, while the admin package uses a publish-safe local pointer page and the stability inventories remain narrowly scoped to stable/internal surface classification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopters | Compatibility docs define what production adopters expect to remain stable through `1.x`. |
| metadata/CI truth -> support matrix | Runtime-floor and support-lane claims are only trustworthy if they match manifest and CI reality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | T | guides/compatibility-and-deprecations.md | mitigate | Build the support matrix from current `mix.exs`, `mailglass_admin/mix.exs`, CI lanes, and support-contract script truth so aspirational compatibility claims do not enter the contract. |
| T-36-02 | I | README.md / mailglass_admin/README.md | mitigate | Replace fragmented policy text with canonical-guide pointers so adopters do not receive contradictory package compatibility promises. |
| T-36-03 | R | docs/api_stability.md | mitigate | Keep stability inventories inventory-shaped and point outward to compatibility policy so stable-surface classification and lifecycle policy remain independently auditable. |
</threat_model>

<verification>
Confirm the canonical guide exists, both package READMEs point to it, the stability inventories still remain surface inventories, and the support-matrix rows match the current package manifests and required CI/support lanes.
</verification>

<success_criteria>
Adopters and maintainers can find one narrow, explicit `1.x` compatibility promise and support matrix, and that promise does not exceed what the repo actually proves.
</success_criteria>

<output>
After completion, create `.planning/phases/36-deprecation-and-compatibility-contract/36-01-SUMMARY.md`
</output>
