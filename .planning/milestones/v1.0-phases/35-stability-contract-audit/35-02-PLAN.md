---
phase: 35-stability-contract-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - docs/api_stability.md
  - mailglass_admin/docs/api_stability.md
  - mailglass_admin/README.md
  - mailglass_admin/mix.exs
  - mailglass_admin/lib/mailglass_admin.ex
  - mailglass_admin/lib/mailglass_admin/router.ex
  - mailglass_admin/lib/mailglass_admin/auth.ex
  - mailglass_admin/lib/mailglass_admin/operator/mount.ex
autonomous: true
requirements:
  - LOCK-02
  - LOCK-03
must_haves:
  truths:
    - "Adopters can identify the exact `mailglass_admin` v1.x stable seams."
    - "Admin UI implementation details are explicitly marked internal rather than implied contract."
    - "Auth, router, and operator-service semantics are documented as the narrow stable admin surface."
  artifacts:
    - path: "docs/api_stability.md"
      provides: "Canonical admin stability inventory and exclusions"
      min_lines: 250
    - path: "mailglass_admin/docs/api_stability.md"
      provides: "Package-local canonical admin stability inventory surfaced in admin docs"
      min_lines: 120
    - path: "mailglass_admin/README.md"
      provides: "Adopter-facing admin contract guidance"
      min_lines: 120
    - path: "mailglass_admin/mix.exs"
      provides: "ExDoc curation that surfaces the admin contract in generated package docs"
      contains: "docs: docs()"
    - path: "mailglass_admin/lib/mailglass_admin/router.ex"
      provides: "Point-of-use router contract framing"
      contains: "defmacro mailglass_admin_routes"
  key_links:
    - from: "mailglass_admin/README.md"
      to: "mailglass_admin/docs/api_stability.md"
      via: "stable admin contract reference"
      pattern: "api_stability"
    - from: "mailglass_admin/lib/mailglass_admin/router.ex"
      to: "mailglass_admin/lib/mailglass_admin/auth.ex"
      via: "auth callback contract"
      pattern: "MailglassAdmin\\.Auth"
    - from: "mailglass_admin/lib/mailglass_admin/operator/mount.ex"
      to: "mailglass_admin/lib/mailglass_admin/auth.ex"
      via: "authorize"
      pattern: "Auth\\.authorize"
---

<objective>
Define the narrow `mailglass_admin` v1.x contract and make the stable-vs-internal admin boundary visible in both canonical docs and point-of-use package docs.

Purpose: Satisfy LOCK-02 and complete the admin half of LOCK-03 without freezing DOM or LiveView internals.
Output: Admin contract inventory plus aligned package and source-module docs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-stability-contract-audit/35-RESEARCH.md
@.planning/phases/35-stability-contract-audit/35-01-PLAN.md
@docs/api_stability.md
@mailglass_admin/docs/api_stability.md
@mailglass_admin/README.md
@mailglass_admin/mix.exs
@mailglass_admin/lib/mailglass_admin.ex
@mailglass_admin/lib/mailglass_admin/router.ex
@mailglass_admin/lib/mailglass_admin/auth.ex
@mailglass_admin/lib/mailglass_admin/operator/mount.ex

<interfaces>
From mailglass_admin/lib/mailglass_admin.ex:
```elixir
use Boundary, deps: [Mailglass], exports: [Router]
@spec version() :: String.t()
def version, do: @version
```

From mailglass_admin/lib/mailglass_admin/router.ex:
```elixir
@doc since: "0.1.0"
defmacro mailglass_admin_routes(path, opts \\ [])

@doc since: "0.1.0"
defmacro mailglass_operator_routes(path, opts \\ [])
```

From mailglass_admin/lib/mailglass_admin/auth.ex:
```elixir
@callback authorize(action(), context :: map()) :: result()
@spec authorize(module(), action(), map()) :: {:ok, %{actor: actor(), assigns: map()}} | failure()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the canonical inventory with the admin contract and exclusions</name>
  <read_first>
    - docs/api_stability.md
    - mailglass_admin/mix.exs
    - mailglass_admin/README.md
    - mailglass_admin/lib/mailglass_admin.ex
    - mailglass_admin/lib/mailglass_admin/router.ex
    - .planning/phases/35-stability-contract-audit/35-RESEARCH.md
  </read_first>
  <files>docs/api_stability.md, mailglass_admin/docs/api_stability.md, mailglass_admin/README.md, mailglass_admin/mix.exs</files>
  <action>Add the `mailglass_admin` section to the shared inventory and create a package-local admin contract artifact per LOCK-02 and LOCK-03. `mailglass_admin/docs/api_stability.md` must become the canonical admin contract page for generated package docs. Enumerate the stable admin seams as router macros, documented route options, `MailglassAdmin.Auth` callback contract, operator mount/auth semantics, and operator read-model or service seams adopters may directly call or configure. In the same document, the shared inventory, and `mailglass_admin/README.md`, explicitly mark LiveView modules, component modules, layouts, DOM/CSS shape, preview assigns plumbing, and internal mount hooks as internal implementation details. Update `mailglass_admin/mix.exs` docs configuration so the package-local contract page is exposed through ExDoc extras/module grouping rather than README-only discoverability. Keep the admin promise narrow and semantic; do not freeze DOM or component APIs and do not introduce Phase 37 trust-doc enforcement language.</action>
  <acceptance_criteria>
    - `docs/api_stability.md` includes a dedicated `mailglass_admin` contract section with stable and internal classifications.
    - `mailglass_admin/docs/api_stability.md` exists and is referenced from `mailglass_admin/mix.exs` docs configuration.
    - `mailglass_admin/README.md` points to the canonical admin contract and explicitly says DOM/component/LiveView internals are not part of the stable surface.
    - `mailglass_admin/mix.exs` curates generated docs so the admin contract page is visible in package-local docs navigation.
    - The updated docs describe router, auth, and operator semantics as stable without promising preview/operator implementation modules.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mailglass_admin|stable|internal|DOM|LiveView|Router|Auth|operator" docs/api_stability.md mailglass_admin/docs/api_stability.md mailglass_admin/README.md mailglass_admin/mix.exs && cd mailglass_admin && mix docs --warnings-as-errors >/tmp/mailglass-admin-phase35-docs.log && rg -n "api_stability|Stability|Contract" doc/**/*.html doc/dist/search_data-*.js</automated>
  </verify>
  <done>The canonical contract, package-local admin contract page, generated docs config, and adopter-facing admin README agree on the narrow stable admin seam and clearly exclude implementation details.</done>
</task>

<task type="auto">
  <name>Task 2: Align admin source docs with the narrow public boundary</name>
  <read_first>
    - mailglass_admin/lib/mailglass_admin.ex
    - mailglass_admin/lib/mailglass_admin/router.ex
    - mailglass_admin/lib/mailglass_admin/auth.ex
    - mailglass_admin/lib/mailglass_admin/operator/mount.ex
    - docs/api_stability.md
  </read_first>
  <files>mailglass_admin/lib/mailglass_admin.ex, mailglass_admin/lib/mailglass_admin/router.ex, mailglass_admin/lib/mailglass_admin/auth.ex, mailglass_admin/lib/mailglass_admin/operator/mount.ex</files>
  <action>Update point-of-use module docs so the code itself reflects the same narrow contract per LOCK-02 and LOCK-03. `MailglassAdmin` should keep `Router` as the only public top-level export and explain `version/0` as a stable package helper if retained. `MailglassAdmin.Router` should continue documenting macro options as the public contract, while `MailglassAdmin.Auth` should read as the stable adopter-owned behaviour seam. `MailglassAdmin.Operator.Mount` should remain internal in docs even if exported for framework wiring. Add missing `since` metadata where a stable admin seam lacks it, but do not rename or redesign the runtime surface.</action>
  <acceptance_criteria>
    - `mailglass_admin/lib/mailglass_admin.ex` and `router.ex` explicitly distinguish stable public contract from internal implementation details.
    - `mailglass_admin/lib/mailglass_admin/auth.ex` reads as the stable adopter-owned auth seam, while `operator/mount.ex` remains clearly internal.
    - Any stable admin helper kept public in these files carries visible `since` metadata.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@doc since:|stable|internal|public|auth seam|Router" mailglass_admin/lib/mailglass_admin.ex mailglass_admin/lib/mailglass_admin/router.ex mailglass_admin/lib/mailglass_admin/auth.ex mailglass_admin/lib/mailglass_admin/operator/mount.ex</automated>
  </verify>
  <done>Admin source docs match the canonical contract and no longer leave maintainers to infer whether reachable modules are stable or internal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin docs -> adopters | Admin docs define which seams adopters may safely integrate with. |
| router/auth seams -> UI internals | Stable semantic seams must stay separate from DOM and LiveView implementation details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | T | docs/api_stability.md | mitigate | Classify admin seams explicitly so internal LiveView/component modules are not accidentally promoted to public API. |
| T-35-05 | I | mailglass_admin/README.md | mitigate | State that DOM, CSS, and implementation modules remain internal to avoid leaking a broader support promise than maintainable. |
| T-35-06 | E | mailglass_admin/lib/mailglass_admin/operator/mount.ex | mitigate | Keep internal mount hooks documented as internal-only even though the runtime must export them for framework wiring. |
</threat_model>

<verification>
Run the task-level grep command and confirm the canonical inventory, README, and source module docs all describe the same admin boundary.
</verification>

<success_criteria>
Adopters can identify the stable `mailglass_admin` router/auth/operator seams without depending on DOM, component, or LiveView internals.
</success_criteria>

<output>
After completion, create `.planning/phases/35-stability-contract-audit/35-02-SUMMARY.md`
</output>
