---
milestone: v1.2
audit_type: checkpoint
scope: built-phases-only
phases_in_scope: ["044.5", "45", "46", "47"]
phases_unbuilt: ["48", "49", "50", "50.5", "51"]
audited: 2026-05-24
status: tech_debt
verdict: foundation-solid-continue-to-48
scores:
  requirements: 32/32   # in-scope only (4 built phases)
  phases: 4/4
  integration: 8/8 seams, 3/3 flows
  nyquist: 2/4 compliant (2 partial)
gaps:
  requirements: []      # no unsatisfied requirements in checkpoint scope
  integration: []       # all cross-phase seams wired
  flows: []             # all 3 E2E flows complete
nyquist_partial:
  - phase: "044.5"
    state: "VALIDATION.md draft; nyquist_compliant: false; wave_0_complete: false"
    action: "/gsd-validate-phase 44.5 (bookkeeping catch-up; phase goal already verified)"
  - phase: "45"
    state: "VALIDATION.md draft; nyquist_compliant: false; wave_0_complete: false"
    action: "/gsd-validate-phase 45 (bookkeeping catch-up; phase passed verification round 2)"
tech_debt:
  - phase: "044.5"
    routed_to: "Phase 51"
    items:
      - "release-please-config.json sticky `release-as: \"1.0.0\"` override must be removed or every future release force-pins 1.0.0 (deferred-items #6)"
      - "post-publish-smoke.yml consumer-install heredoc writes wrong dep version when smoke-testing inbound (workflow bug, not a package defect) (deferred-items #8)"
      - "Operator Browser Gate CI lane dropped to advisory; re-strict (deferred-items #9-10)"
      - "Editorial anchor comment lines in root + inbound mix.exs (Release-As trailer anchors) — cosmetic cleanup (deferred-items #7)"
      - "mailglass_inbound/.gitignore missing _build/ deps/ _publish_check/ (deferred-items #1)"
      - "stability_contract_test.exs:74 stale assertions referencing mailglass_inbound 0.3.2 (pre-existing red, deferred-items #4)"
  - phase: "45"
    routed_to: "follow-up polish (non-blocking, latent)"
    items:
      - "telemetry_event_convention.ex span_wrapper_name? matches any 'span'-prefixed name — latent, no such fn in tree (WR-02 follow-up)"
      - "no_pii_in_response_body.ex bare-variable body-arg rule can over-fire on future safe shapes — latent (WR-01 follow-up)"
      - "no_pii_in_response_body.ex def_head? single-line edge case soundness hole — latent (WR-03 follow-up)"
  - phase: "46"
    routed_to: "non-blocking info"
    items:
      - "DateTime.utc_now() for received_at instead of injectable Clock (IN-02, cosmetic test-determinism nit)"
  - phase: "environment"
    routed_to: "Phase 51 / known flake"
    items:
      - "Bare `mix test` ~57 Oban-migration failures in verification worktrees (test-DB/migration env gap, not a code regression)"
non_debt_documented_deviations:
  - phase: "044.5"
    item: "STATE.md transition (human_needed item) — resolved organically; STATE now shows completed_phases: 4, Phase 47 done"
  - phase: "47"
    item: "assert_inbound outcome/routing arities (/0,/1 vs SC2 /1,/2) — macro-name-encoded per D-47 discretion; behavioral contract met"
  - phase: "47"
    item: "MailboxCase sync-execution structural race-prevention (D-47-12/13) supersedes the HI-01 :async_execution_impl snapshot wording — key does not exist; honored as documented"
---

# v1.2 Checkpoint Audit — Built Phases (044.5, 45, 46, 47)

**Milestone:** v1.2 "Inbound Production Confidence" — **mid-flight, 4 of 11 phases built.**
**Audit type:** Checkpoint (NOT a pre-archive milestone audit). Scope is the 4 completed
phases only; phases 48-51 are not yet built and are excluded by design, not flagged as gaps.
**Audited:** 2026-05-24
**Verdict:** **Foundation solid — continue to Phase 48.** Status `tech_debt`: no critical
blockers, all in-scope requirements satisfied, all cross-phase seams wired; accumulated debt
is documented and already routed (mostly to Phase 51), plus 2 phases need Nyquist
bookkeeping catch-up.

## Why this is a checkpoint, not the milestone audit

`/gsd-audit-milestone` is normally a pre-archive gate. v1.2 is only 36% complete. Running the
full gate now would flag ~26 unbuilt requirements (phases 48-51) as "unsatisfied" — true but
noise. This checkpoint instead verifies that the **foundation phases are individually sound and
correctly wired together** before Phase 48 (Admin LiveView) builds on top of them. The canonical
`v1.2-MILESTONE-AUDIT.md` is intentionally left for the real end-of-milestone audit after Phase 51.

## Requirements Coverage (in-scope: 32/32 satisfied)

3-source cross-reference (phase VERIFICATION.md ↔ REQUIREMENTS.md traceability ↔ checkbox state).
All three sources agree for every in-scope REQ-ID.

| Phase | REQ-IDs | Count | VERIFICATION | Traceability | Checkboxes | Final |
|-------|---------|-------|--------------|--------------|------------|-------|
| 044.5 | CLOSE-06 | 1 | passed* | Complete (2026-05-07) | [x] | **satisfied** |
| 45 | TELE-01..08, MIME-01, MIME-02, MIME-04 | 11 | passed (round 2) | all Complete | all [x] | **satisfied** |
| 46 | MGUN-01..04, SESI-01..05 | 9 | passed | all Complete | all [x] | **satisfied** |
| 47 | ITEST-01..07, IGEN-01..04 | 11 | passed | all Complete | all [x] | **satisfied** |

\* Phase 044.5 VERIFICATION.md status is `human_needed`, but the human item was a STATE.md
bookkeeping transition, not the phase goal. CLOSE-06 (live Hex publish + branch-protection
evidence) was verified 13/13 + 5/5 SCs. The STATE.md transition has since resolved organically
(STATE now reports `completed_phases: 4`).

**No orphaned requirements** in scope. **No unsatisfied requirements** in scope → the FAIL gate
does not trip. The remaining 26 milestone REQ-IDs (MIME-03→49, MGUN-05/SESI-06→50, IADM-01..07→48,
CLOSE-01..05→51, plus OPS/DOCS/ALIVE/TEST families) belong to unbuilt phases and are correctly
out of checkpoint scope.

## Phase Verifications (4/4 passed)

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 044.5 Release Ceremony | passed (goal) | 13/13 truths + 5/5 SCs | 1 STATE.md bookkeeping deviation, since resolved |
| 45 Telemetry + Idempotency | passed | 8/8 (after gap-closure round 2) | Closed prior inert-guard + WR-02 blockers |
| 46 Mailgun + SES Ingress | passed | 13/13 | 2 BLOCKER + 6 WARNING review findings all fixed |
| 47 Test Helpers + Generators | passed | 12/12 | 2 intentional ROADMAP-wording deviations (documented) |

## Cross-Phase Integration (PASS — 8/8 seams, 3/3 flows)

Verified by gsd-integration-checker against live source.

**Wiring seams — all WIRED:**
1. 46→45 MIME: `mailgun.ex:137` / `ses.ex:88` call `MailglassInbound.MIME.parse/1` (never-raise).
2. 46→45 replay cache: `mailgun.ex:47` reuses core `MailgunReplayCache`; no duplicate GenServer in inbound app.
3. 46→45 optional-dep gateway: `OptionalDeps.ExAwsS3` mirrors `GenSmtp`; `.credo.exs:61-62` gates bare `ExAws`; no bare API call outside gateway.
4. 46→core seam: inbound `ses.ex:142` calls core `verify_envelope!/2`; outbound `verify!/3` intact.
5. 46 plug single-switch: one `[:postmark,:sendgrid,:mailgun,:ses]` switch, 3-variant verify result, dual SignatureError rescue → 401.
6. 47→46 real seams: `Test.Ingress`/`Fixtures` drive REAL per-provider verify!/normalize; SES primes real CertCache + S3Fetcher.Fake.
7. 47→production write path: `Test.Ingress` calls real `Persist.persist/2` + `Execution.execute/2` (sync), never `dispatch/2`.
8. 47 generator chain: `gen.mailbox` reuses `gen.inbound_route`'s `add_route/4`; generated code compiles + testable via MailboxCase.

**E2E flows — all COMPLETE:**
- E2E-1 Inbound ingress (45+46): POST → allowlist → verify! → normalize → `%InboundMessage{}` → persist+dedupe → execute → mailbox dispatch; forgery → 401 no-recovery.
- E2E-2 Adopter test DX (47 over 45+46): `use MailboxCase` → `Fixtures.build_*` → `Test.Ingress` → real write path → `assert_inbound_*`; replay converges to 1 record + 1 fresh run.
- E2E-3 Generator scaffold (47): `gen.inbound_router` → `gen.mailbox` → `gen.inbound_route` idempotent → compiles → exercisable under MailboxCase.

## Nyquist Coverage (2/4 compliant)

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status | Action |
|-------|---------------|-------------------|-----------------|--------|--------|
| 044.5 | exists (draft) | false | false | PARTIAL | `/gsd-validate-phase 44.5` |
| 45 | exists (draft) | false | false | PARTIAL | `/gsd-validate-phase 45` |
| 46 | exists (approved) | true | true | COMPLIANT | — |
| 47 | exists (validated) | true | true | COMPLIANT | — |

Phases 44.5 and 45 passed verification but their VALIDATION.md is still `draft` —
bookkeeping lag, the same pattern flagged in the v1.0 carry-forward notes. Both are
catch-up candidates for `/gsd-validate-phase`; neither blocks Phase 48.

## Tech Debt Summary

All accumulated debt is documented and routed. None blocks Phase 48.

- **Routed to Phase 51** (already in scope per STATE.md): release-please sticky `release-as`
  override, post-publish-smoke consumer-install version bug, Operator Browser Gate re-strict,
  mix.exs anchor-comment cleanup, inbound `.gitignore`, stale `stability_contract_test.exs`
  0.3.2 assertions, branch-protection install (CLOSE-02), `mix test` Oban-migration env race.
- **Follow-up polish (latent, Phase 45)**: three custom-Credo-check robustness gaps
  (`telemetry_event_convention` over-broad name match; `no_pii_in_response_body` over-fire +
  def-head edge case) — none fire on today's code; `mix credo --strict` exits 0.
- **Non-blocking info (Phase 46)**: `DateTime.utc_now()` vs injectable Clock (IN-02).

## Bottom line

The v1.2 foundation (release pipeline + telemetry/MIME/idempotency + Mailgun/SES ingress +
test DX/generators) is **individually verified and correctly integrated**. Nothing in the
4 built phases blocks Phase 48. Recommended next action: **continue to Phase 48 (Inbound Admin
LiveView)**, optionally clearing the 2 Nyquist bookkeeping catch-ups first.

---
_Checkpoint audit generated 2026-05-24 by /gsd-audit-milestone (scoped to built phases)._
