---
context: release-ceremony
phase: v1.3-release (between phases 59 and 60)
status: paused-release-in-flight
last_updated: 2026-05-28T18:56:32.892Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Release-PR "no checks reported" → BLOCKED | release-please's custom dep-pin sync pushes the PR branch with `GITHUB_TOKEN`; GitHub anti-recursion means that push does NOT trigger `ci.yml`, so the release PR shows no required checks and branch protection blocks the merge. | advisory | Dispatch `ci.yml` on the release PR head ref (`gh workflow run ci.yml --ref release-please--branches--main`) so the 4 required checks run + report on the PR head SHA. The future auto-merge workflow MUST do this automatically. |

<current_state>
The **v1.3 `1.3.0` release is in flight and shipping itself.** Release Please **PR #42**
(`release-please--branches--main`, "chore: release main") cuts `mailglass` + `mailglass_admin`
**1.3.0** (linked) and `mailglass_inbound` **0.3.0**. Auto-merge is **ARMED (squash)** on #42,
repo `allow_auto_merge` is enabled, and `ci.yml` was dispatched on the release branch
(run 26595500746). When its 4 required checks go green, GitHub auto-merges #42 → Release Please
tags + GitHub Release → `publish-hex` fans out **core → inbound → admin** → post-publish smoke.

`main` is fully green and release-clean. Phase 59 is complete (Trust Lane enforced, 4/4 branch checks).
</current_state>

<completed_work>
- Phase 59 complete; `main` fully green; Trust Lane Repo Head a required check.
- `RELEASE_PLEASE_TOKEN` wired into release-please.yml (commit 8fa3025).
- `release-as` → 1.3.0 for core+admin (commit 184d393).
- Release Please PR #42 opened; auto-merge armed (squash); CI dispatched (run 26595500746).
</completed_work>

<remaining_work>
1. PR #42 auto-merges on green → triggers publish (automated).
2. Approve the `hex-publish` env deployment if it has required reviewers (else auto-publishes).
3. Verify `mailglass`/`mailglass_admin` 1.3.0 + `mailglass_inbound` 0.3.0 live on Hex; post-publish smoke green.
4. Build the going-forward **auto-merge workflow**: auto-arm `--auto` + auto-dispatch `ci.yml` on every Release-Please PR (handles the anti-pattern above).
5. **Phase 60** (Release Trust Gate / EVID-03 + OPS smoke debt) → **Phase 61** (Docs Boundary).
6. Green-main cleanup **GM-03..06** (drop inbound `--seed 0` pin via real test isolation; narrow dialyzer specs; FUTR traceability; branch-protection tooling dedup).
</remaining_work>

<decisions_made>
- Armed auto-merge on the release PR (shift-left automation) but **left the `hex-publish` env approval intact** — public publishing is irreversible and CLAUDE.md locks it behind human approval.
- Local main's v1.3 ("Adopter Trust Proof") is canonical; origin's divergent v1.3 ("Release Discipline & Repo Truth", `fd4f3c9`) was reconciled in and re-filed as the **v1.4 backlog**.
</decisions_made>

<blockers>
- None. Only the (intentional) `hex-publish` env approval stands between the merge and a live publish.
</blockers>

## Required Reading (in order)
1. `gh pr view 42` — confirm release PR state (likely merged by the time you resume).
2. `.planning/backlog/v1.4-release-discipline-repo-truth.md` — GM-01..06 + RH/RELH/TRUTH (the v1.4 milestone).
3. Memory `project_v1_3_release_and_milestone_state.md` — milestone/fork context (auto-loaded).

## Infrastructure State
- PR #42: auto-merge ARMED (squash); repo `allow_auto_merge=true`.
- `ci.yml` dispatched on `release-please--branches--main` (run 26595500746) — in progress at pause.
- Auth: release uses `RELEASE_PLEASE_TOKEN` secret; publish uses `HEX_API_KEY` behind the `hex-publish` env.

<context>
Everything is teed up so the release lands without babysitting. If you resume and PR #42 is already
merged + 1.3.0 is on Hex, the release shipped — move to building the auto-merge workflow (task 4)
and then Phase 60. If the publish stalled on env approval, approve it.
</context>

<next_action>
Start with: `gh pr view 42 --json state,mergedAt` and `gh run list --workflow publish-hex.yml --limit 3`.
If merged + published → verify Hex + smoke, then build the Release-Please auto-merge workflow (task 4).
If awaiting env approval → approve the `hex-publish` deployment. Then proceed to Phase 60.
</next_action>
