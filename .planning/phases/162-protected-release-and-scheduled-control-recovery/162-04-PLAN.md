---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 04
type: execute
wave: 2
depends_on: [162-01]
files_modified:
  - .github/workflows/post-publish-smoke.yml
  - test/mailglass/publish/post_publish_smoke_contract_test.exs
autonomous: true
requirements: [AUTO-05]
must_haves:
  truths:
    - "D-07: Post-publish proof accepts only an exact immutable target whose tracked publication state and public facts pass all expected tags-to-one-SHA plus authorized content-digest validation."
    - "D-08: An `authorized` / `publication: not_started` scheduled target reports a bounded `blocked` result and exits non-success without substituting `main`, treating a release-event no-op as proof, or forcing publication."
    - statement: "AUTO-05 resolved edge rule: the top-level scheduled result for an authorized but unpublished target is `blocked`, because the applicable schedule ran but its completed-target precondition was unsatisfied."
      verification: backstop
      status: flagged
  artifacts:
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "Bounded scheduled target-resolution result, summary, and artifact"
      contains: "post-publish-resolution"
    - path: "test/mailglass/publish/post_publish_smoke_contract_test.exs"
      provides: "Scheduled unpublished-target result and immutable-ref regression contract"
      contains: "publication"
  key_links:
    - from: ".planning/release-target.json"
      to: "post-publish-resolution.json"
      via: "policy-owned completed-target resolution with explicit blocked unpublished outcome"
      pattern: "completed-versions|status|reason"
    - from: "resolved exact target"
      to: "scripts/check_post_publish_target.sh"
      via: "target SHA, three exact versions, all tags, and content digest"
      pattern: "--target-ref|--core|--admin|--inbound"
  assumptions:
    - "RESOLVED (AUTO-05): scheduled authorized/not-started is standardized as top-level `blocked` across result JSON, logs, summary, reconciliation, and plan because no completed immutable target exists."
  prohibitions:
    - statement: "MUST NOT force, imply, or manufacture a publication solely to make post-publish automation or milestone closeout appear successful."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Give scheduled post-publish recovery an explicit bounded result for an unpublished target while preserving exact immutable publication proof.

Purpose: Satisfy AUTO-05 and D-07/D-08 without publishing, weakening target validation, or adding a fallback ref.
Output: Narrow post-publish resolution reporting and executable workflow-contract coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-RESEARCH.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-PATTERNS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-01-SUMMARY.md
@.github/workflows/post-publish-smoke.yml
@scripts/release_policy.exs
@scripts/check_post_publish_target.sh
@scripts/verify_published_release.sh
@test/mailglass/publish/post_publish_smoke_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report scheduled unpublished targets before failing closed</name>
  <reversibility rating="costly">The result artifact becomes operational evidence consumed by final reconciliation, but it is an internal workflow contract with no package API or persisted-data migration.</reversibility>
  <files>test/mailglass/publish/post_publish_smoke_contract_test.exs, .github/workflows/post-publish-smoke.yml</files>
  <read_first>
    - .github/workflows/post-publish-smoke.yml
    - test/mailglass/publish/post_publish_smoke_contract_test.exs
    - scripts/release_policy.exs
    - scripts/check_post_publish_target.sh
    - scripts/verify_published_release.sh
    - .planning/release-target.json
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
  </read_first>
  <behavior>
    - "A scheduled `authorized` / `publication: not_started` target writes `blocked` with exact lifecycle/source identity and exits non-success before consumer jobs."
    - "A completed schedule or protected dispatch reaches the existing exact tag/SHA/content-digest guard; release events remain successful no-ops."
    - "The result contains `status`, `reason`, `event_name`, `run_id`, `ledger_status`, `publication_status`, `target_ref`, `core`, `admin`, and `inbound`."
  </behavior>
  <action>Write failing contract cases first for scheduled authorized/not-started classification, result-before-exit ordering, summary/artifact agreement, and immutable-ref protection. In `.github/workflows/post-publish-smoke.yml`, modify the existing `resolve-completed-target` reporting seam so schedule resolution writes `$RUNNER_TEMP/post-publish-resolution.json` before ending non-success when `completed-versions` rejects the authorized unpublished ledger. Standardize that expected state as `blocked` with fields `status`, `reason`, `event_name`, `run_id`, `ledger_status`, `publication_status`, `target_ref`, `core`, `admin`, and `inbound`; add `if: always()` summary and SHA-pinned artifact upload reading that exact file. Preserve release-event no-op behavior, schedule-only `completed-versions`, dispatch identity comparison, immutable workflow/target checkouts, exact SemVer/SHA validation, `scripts/check_post_publish_target.sh`, all-tags-one-SHA/content-digest verification, permissions, triggers, concurrency, and downstream job gates. Do not derive a target from the current branch, latest Hex data, a release event, or another workflow run.</action>
  <verify>
    <automated>mix test test/mailglass/publish/post_publish_smoke_contract_test.exs test/scripts/verify_published_release_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The scheduled authorized/not-started fixture asserts `blocked`, exact reason/lifecycle fields, artifact creation, matching summary, and non-success exit.
    - Completed schedule and protected dispatch still call `check_post_publish_target.sh` with exact target SHA and three versions.
    - Tests retain all immutable checkout, identity-key equality, all-tags-one-SHA, authorized content-digest, and no-current-branch substitution assertions per D-07.
    - Release events remain successful no-ops and cannot satisfy scheduled or consumer proof per D-08.
    - No release, package publication, tag mutation, permission broadening, dependency addition, or topology redesign occurs.
  </acceptance_criteria>
  <done>Post-publish automation truthfully distinguishes unpublished blocked state from exact immutable published proof without manufacturing a release.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/post-publish-smoke.yml` — transient `post-publish-resolution.json`, matching job summary, and uploaded result artifact.
- Result fields: `status`, `reason`, `event_name`, `run_id`, `ledger_status`, `publication_status`, `target_ref`, `core`, `admin`, `inbound`.
- `test/mailglass/publish/post_publish_smoke_contract_test.exs` — scheduled unpublished classification, result ordering, summary/artifact, and immutable-target assertions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule/dispatch input → target resolution | Trigger type determines whether only completed or exact authorized target state is accepted. |
| protected workflow control → target checkout | Caller-selected refs cannot own or replace protected policy. |
| Git tags/Hex facts → consumer proof | All packages must bind to one immutable target and authorized digest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-13 | Elevation of Privilege | target checkout | critical | mitigate | Keep protected workflow SHA as policy owner and compare every dispatch identity field before target proof. |
| T-162-14 | Tampering | tag/package set | high | mitigate | Preserve all expected tags resolving to one exact SHA and authorized content digest/version checks. |
| T-162-15 | Spoofing / Tampering | fallback target | high | mitigate | Schedule accepts only policy `completed-versions`; never substitute current branch, latest release, or event tag. |
| T-162-16 | Repudiation | unpublished scheduled outcome | medium | mitigate | Write machine result before non-success and render/upload the same file with event/run identity. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | Preserve existing GitHub/Hex credential use and permissions; `test/mailglass/publish/post_publish_smoke_contract_test.exs` ensures inaccessible policy/public facts cannot produce completed-target success. |
| V4 Access Control | Applicable | That workflow contract keeps schedule limited to completed policy targets and dispatch bound to exact authorized identities; release-event no-op never gains consumer-proof authority. |
| V5 Input Validation | Applicable | `test/mailglass/publish/post_publish_smoke_contract_test.exs` requires exact lifecycle, SemVer, target SHA, all expected tags on one SHA, and no current-branch/latest substitution. |
| V6 Cryptography | Applicable | `test/mailglass/publish/post_publish_smoke_contract_test.exs` preserves the authorized SHA-256 content-digest call to `scripts/check_post_publish_target.sh`; no replacement scheme is introduced. |
</threat_model>

<verification>
Run the focused post-publish and publication-verifier tests. Inspect the workflow diff to confirm exact-target checks, permissions, triggers, and downstream job conditions remain intact.
</verification>

<success_criteria>
- AUTO-05 has explicit bounded unpublished and exact-published outcomes.
- D-07/D-08 and the flagged AUTO-05 classification assumption are encoded in tests and result fields.
- No release is forced and no target/authority invariant is weakened.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-04-SUMMARY.md` when done.
</output>
