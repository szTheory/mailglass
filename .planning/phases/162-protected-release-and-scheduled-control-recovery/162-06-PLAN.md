---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 06
type: execute
wave: 4
depends_on: [162-05]
files_modified:
  - .github/workflows/release-please.yml
  - test/scripts/release_trigger_recovery_test.exs
autonomous: true
gap_closure: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "D-04: Push, hourly schedule, and digest-free dispatch remain proposal-only; the existing protected exact candidate-digest dispatch remains the sole merge/tag/release boundary."
    - "D-06 / AUTO-03: Every proposal capture path that reaches worktree creation publishes its real status, reason, proposal head, source SHA, candidate digest, and captured flag before cleanup completes."
    - "A successful capture produces a pass proposal-control artifact, while an exact identity mismatch produces blocked rather than defaulting to cannot-check."
    - "AUTO-03 adjacency: only exact identity equality passes; a near or unequal candidate, source, or content identity remains blocked."
    - "AUTO-03 empty-input: missing proposal evidence remains an explicit blocked result and an unavailable acquisition remains cannot-check."
    - "AUTO-03 ordering: one EXIT handler emits outputs and removes the temporary worktree without either responsibility replacing the other."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Chained proposal-capture output emission and worktree cleanup on every exit path"
      contains: "emit_capture_outputs"
    - path: "test/scripts/release_trigger_recovery_test.exs"
      provides: "Executable full capture-step fixtures for pass and identity mismatch outcomes"
      contains: "proposal capture"
  key_links:
    - from: ".github/workflows/release-please.yml capture-proposal shell"
      to: "steps.capture-proposal.outputs"
      via: "one guarded EXIT handler that performs cleanup and emits final fields"
      pattern: "trap.*EXIT"
    - from: "steps.capture-proposal.outputs"
      to: "release-proposal-control-result.json"
      via: "the existing proposal-control writer consumes the actual capture status and identities"
      pattern: "CAPTURE_STATUS|CAPTURE_REASON|PROPOSAL_HEAD|SOURCE_SHA|CANDIDATE_DIGEST"
  assumptions:
    - statement: "FLAGGED (AUTO-01 unclassified): the already-verified reconciliation remains complete only when all canonical D-01 sources retain rows; this workflow-only closure must not rewrite that evidence."
      status: unverified
    - statement: "FLAGGED (AUTO-02 adjacency/empty/ordering): distinct equal identities, explicit empty categories, and stable evidence ordering remain governed by the executed reconciliation contract and are preservation constraints here."
      status: unverified
    - statement: "FLAGGED (AUTO-04 unclassified): the already-verified repository-hygiene result map, summary, artifact, and control/schedule provenance remain untouched by this closure."
      status: unverified
  prohibitions:
    - statement: "MUST NOT grant ordinary release-please triggers merge, tag, publish, or protected-dispatch authority."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
    - statement: "MUST NOT delete or rewrite Phase 161 evidence or the append-only Phase 162 reconciliation to make capture appear successful."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Close the release-please output-loss gap by making proposal capture cleanup and output publication one end-to-end, behaviorally tested exit path.

Purpose: Restore truthful AUTO-03 pass/blocked/cannot-check reporting per D-04 and D-06 without changing release authority or topology.
Output: A corrected capture trap in `release-please.yml` and executable capture-path regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-02-SUMMARY.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-05-SUMMARY.md
@.github/workflows/release-please.yml
@test/scripts/release_trigger_recovery_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute proposal capture through output emission, cleanup, and result serialization</name>
  <files>.github/workflows/release-please.yml, test/scripts/release_trigger_recovery_test.exs</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-02-SUMMARY.md
    - .github/workflows/release-please.yml
    - test/scripts/release_trigger_recovery_test.exs
  </read_first>
  <behavior>
    - "Pass fixture: the full extracted capture step reaches worktree creation, exits successfully, removes the worktree, emits `result_status=pass`, `result_reason=proposal_captured`, all immutable identities, and `captured=true`; the unchanged result writer serializes pass."
    - "Mismatch fixture: the same extracted capture step reaches worktree creation, fails the exact identity comparison, removes the worktree, emits `blocked` plus `proposal_identity_mismatch` and the observed identities; the result writer serializes blocked."
    - "Unavailable or missing evidence retains the existing cannot-check or blocked semantics, and none of the fixtures invokes protected merge, tagging, release creation, publication, or protected dispatch."
  </behavior>
  <action>Start RED by replacing the isolated writer-only confidence with a test that extracts and executes the complete `Capture Release Please proposal identity without activation` `run:` body, followed by the existing proposal-control writer. Build a disposable fixture directory with command shims and minimal policy/ledger files that drive two paths through the real shell: a successful captured/authorized identity and a proposal/content identity mismatch, both after `git worktree add`. The shims may replace external GitHub and compilation effects, but must log invocations and preserve the workflow script's actual variable assignments, comparisons, EXIT handling, output writes, cleanup call, exit status, and result-writer consumption. Assert the temporary worktree is removed, `GITHUB_OUTPUT` contains exactly the final capture fields (including `captured=true` only on pass), and the resulting JSON is pass or blocked with the real identities and reason; also assert no logged command crosses the D-04 authority boundary. Then modify the capture step so `candidate_root` is safely initialized before trap registration and one cleanup-and-output function is the sole EXIT handler. That handler must conditionally remove an actually-created worktree and always invoke `emit_capture_outputs`, preserving the original exit status. Remove the later trap replacement. Keep the proposal queries, exact identity/digest/policy checks, `continue-on-error`, result writer, summary, artifact upload, non-pass failure, permissions, triggers, and protected-dispatch steps otherwise unchanged per D-04/D-06. Preserve the executed evidence contracts: do not rewrite the append-only blocked narrative per D-01, infer authorization or availability per D-02, change a recorded PR/branch/check disposition or delete retained evidence per D-03, substitute control proof for scheduled proof per D-05, weaken exact immutable post-publish identity per D-07, or convert unpublished/no-op state into consumer proof or a forced release per D-08.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The test executes the full capture shell through worktree creation for both pass and identity-mismatch fixtures; it does not merely execute the downstream JSON writer.
    - Pass emits `pass`, `proposal_captured`, full proposal/source/digest fields, and `captured=true`; mismatch emits `blocked`, `proposal_identity_mismatch`, and no captured flag.
    - Both paths prove worktree cleanup occurred and prove the writer JSON exactly reflects the emitted capture fields instead of its fallback defaults.
    - One EXIT handler owns both guarded cleanup and output emission, and the original command exit status remains observable.
    - Existing tests continue proving ordinary triggers cannot merge, tag, publish, create a release, or invoke protected dispatch per D-04.
  </acceptance_criteria>
  <done>Release-please reports the real proposal-only capture outcome after worktree creation, with cleanup intact and no authority expansion.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/release-please.yml` — existing `capture-proposal` step with one guarded cleanup/output EXIT handler; no new authority-bearing symbol or flag.
- `test/scripts/release_trigger_recovery_test.exs` — executable full-capture harness/fixtures for post-worktree pass and identity-mismatch paths and their downstream result JSON.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub proposal metadata → capture outputs | Untrusted or unavailable PR identities must remain exact, bounded evidence rather than authorization. |
| capture step → proposal result writer | Output loss or fallback substitution can misrepresent a known pass or block as cannot-check. |
| ordinary trigger → protected release operations | Proposal reporting must never grant merge/tag/release/publish authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-21 | Repudiation | capture EXIT handling | high | mitigate | Execute pass and mismatch paths after worktree creation and require exact outputs plus cleanup from one EXIT handler. |
| T-162-22 | Tampering | capture-to-result fields | high | mitigate | Assert the result JSON equals emitted status, reason, and immutable identities rather than fallback values. |
| T-162-23 | Elevation of Privilege | ordinary release-please triggers | high | mitigate | Preserve D-04 trigger/permission/step guards and assert the executed fixture log contains no merge, tag, release, publish, or protected-dispatch operation. |
| T-162-24 | Denial of Service | temporary worktree cleanup | medium | mitigate | Guard cleanup by a nonempty created path and prove both successful and failing paths remove it without replacing output emission. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | Existing `GITHUB_TOKEN` use and permissions remain unchanged; fixtures replace network acquisition without adding credentials. |
| V4 Access Control | Applicable | `release_trigger_recovery_test.exs` continues to prove protected exact-digest dispatch is the only authority crossing. |
| V5 Input Validation | Applicable | Executed fixtures cover exact 40/64-character identities, equality mismatch, missing evidence, and status serialization. |
| V6 Cryptography | Applicable | Existing content-digest comparison is exercised and unchanged; no digest scheme is introduced. |
</threat_model>

<verification>
Run the focused release-trigger contract and confirm both extracted capture fixtures traverse worktree creation, EXIT handling, and result serialization. Then run `mix test test/scripts/phase_162_release_reconciliation_test.exs test/scripts/release_trigger_recovery_test.exs --seed 0` to preserve the verified AUTO-01/AUTO-02 evidence boundary.
</verification>

<success_criteria>
- AUTO-03's failed truth is restored for successful and identity-mismatch capture paths.
- D-04 and D-06 remain test-enforced with no workflow topology, trigger, permission, or authority expansion.
- AUTO-01, AUTO-02, and AUTO-04 verified artifacts remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-06-SUMMARY.md` when done.
</output>
