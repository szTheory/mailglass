---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 02
type: execute
wave: 2
depends_on: [162-01]
files_modified:
  - .github/workflows/release-please.yml
  - test/scripts/release_trigger_recovery_test.exs
autonomous: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "D-04: Push, schedule, and digest-free dispatch remain proposal-only; only the existing protected exact candidate-digest dispatch can merge, tag, or create a release."
    - "D-05: Release-please control and schedule results preserve distinct `event_name` and `run_id` evidence; a control dispatch never satisfies scheduled proof."
    - "D-06: Proposal capture produces one bounded machine-readable result and a summary derived from that result for `pass`, `blocked`, or `cannot-check`."
    - "AUTO-03 adjacency: only byte-exact candidate-digest, proposal-head, source-SHA, and content-digest equality crosses the protected gate; a near match or mismatch remains proposal-only and blocked."
    - "AUTO-03 empty-input: an absent candidate digest selects proposal-only behavior; missing proposal rows produce an explicit inspectable result without release authority."
    - "AUTO-03 ordering: trigger provenance and candidate validation are evaluated before any protected operation, and result evidence is persisted before a non-pass exit."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Proposal-only control/schedule result artifact and matching summary"
      contains: "release-proposal-control-result"
    - path: "test/scripts/release_trigger_recovery_test.exs"
      provides: "Static and executable proposal-only result/authority contract"
      contains: "proposal-only"
  key_links:
    - from: "Capture Release Please proposal identity without activation"
      to: "release-proposal-control-result.json"
      via: "one result writer records event, run, identities, status, and reason before exit"
      pattern: "status|reason|event_name|run_id"
    - from: "release-proposal-control-result.json"
      to: "GITHUB_STEP_SUMMARY and uploaded artifact"
      via: "jq renders the already-written result without recomputing status"
      pattern: "release-proposal-control-result"
  assumptions:
    - statement: "Spec-less authority/evidence probes are never omitted: each attempted candidate, required-check, permission, trigger, and result-artifact assertion is recorded as pass, blocked, cannot-check, or pending with its source and recovery condition."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
  prohibitions:
    - statement: "MUST NOT portray a proposal-only run as a completed release or imply that an ordinary trigger can cross the protected boundary."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Make release-please proposal capture truthful and inspectable for control and scheduled entry points while preserving the protected exact-digest authority boundary.

Purpose: Satisfy AUTO-03 and D-04 through D-06 without redesigning workflow topology or enabling any new release action.
Output: Narrow release-please reporting changes and executable regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-RESEARCH.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-PATTERNS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-01-SUMMARY.md
@.github/workflows/release-please.yml
@scripts/release_policy.exs
@test/scripts/release_trigger_recovery_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit proposal-only capture results without expanding authority</name>
  <reversibility rating="costly">The workflow result artifact and summary are operational contracts consumed by later live evidence, but remain internal and can be changed through coordinated workflow/test edits.</reversibility>
  <files>test/scripts/release_trigger_recovery_test.exs, .github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml
    - test/scripts/release_trigger_recovery_test.exs
    - scripts/release_policy.exs
    - scripts/release_policy_content_digest.sh
    - .planning/release-target.json
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
  </read_first>
  <behavior>
    - "A valid proposal capture writes `pass`; an established identity/policy mismatch writes `blocked`; missing or inaccessible remote evidence writes `cannot-check`."
    - "Each result contains `status`, `reason`, `event_name`, `run_id`, `proposal_head`, `source_sha`, and `candidate_digest`, then drives both summary and artifact."
    - "Push, hourly schedule, and empty-digest dispatch cannot invoke merge/tag/release commands even when proposal capture succeeds."
  </behavior>
  <action>Extend `test/scripts/release_trigger_recovery_test.exs` first with static assertions and executable fake-`gh` fixtures for successful proposal capture, capture mismatch, unavailable GitHub evidence, and a complete probe ledger. Modify only the existing proposal capture/reporting seam in `.github/workflows/release-please.yml`: create `$RUNNER_TEMP/release-proposal-control-result.json` with fields `status`, `reason`, `event_name`, `run_id`, `proposal_head`, `source_sha`, and `candidate_digest`; classify confirmed policy/identity mismatch as `blocked`, acquisition failure as `cannot-check`, and a post-change scheduled observation that has not elapsed as `pending` evidence that cannot satisfy success. Write every attempted candidate, required-check, permission, trigger, and result-artifact probe to the bounded evidence surface; a probe that cannot run must remain `cannot-check` with recovery detail, and no probe item may be silently dropped. Write the result before returning nonzero for non-pass. Add an `if: always()` summary step that renders this JSON and an `if: always()` SHA-pinned upload step named `release-proposal-control-result-${{ github.run_id }}`. Keep trigger blocks, permissions, checkout/control target, exact SHA/digest/version checks, protected-dispatch condition, protected merge/release steps, and ordinary auto-merge disarm unchanged per D-04. Do not add packages, permissions, triggers, merge/tag/publish calls, or a second policy owner.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs test/scripts/release_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Tests execute successful, mismatch, and unavailable-evidence proposal captures and assert exact `pass`, `blocked`, and `cannot-check` result strings.
    - Result JSON and summary expose the same status/reason and retain separate `event_name`/`run_id` provenance per D-05/D-06.
    - Empty candidate digest, push, and schedule remain proposal-only; no test fixture finds merge/tag/release authority outside the existing protected-dispatch chain.
    - Near-equal or mismatched digests/SHAs fail closed before protected operations; exact protected comparisons remain unchanged.
    - Non-pass proposal capture writes and uploads inspectable evidence before the workflow exits nonzero.
    - Every spec-less probe item is present with source and status; `cannot-check` and `pending` remain non-success evidence and no item is silently omitted.
  </acceptance_criteria>
  <done>Every ordinary release-please entry point reports a bounded proposal-only outcome while the exact protected dispatch remains the sole release-authority path.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/release-please.yml` — transient `release-proposal-control-result.json`, summary rendering, and `release-proposal-control-result-${{ github.run_id }}` artifact upload.
- Result fields: `status`, `reason`, `event_name`, `run_id`, `proposal_head`, `source_sha`, `candidate_digest`.
- `test/scripts/release_trigger_recovery_test.exs` — capture-mismatch and evidence-unavailable executable fixtures plus authority-preservation assertions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ordinary trigger → release workflow | Untrusted trigger provenance must remain proposal-only. |
| GitHub PR API → candidate capture | Remote PR identities and availability are evidence, not authority. |
| protected dispatch input → merge/tag/release | Exact candidate digest crosses a high-impact authorization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-05 | Elevation of Privilege | ordinary triggers | critical | mitigate | Preserve D-04 trigger guards and prove no merge/tag/release operations are reachable outside protected exact-digest dispatch. |
| T-162-06 | Tampering | candidate identities | high | mitigate | Retain exact digest/SHA/version checks and classify mismatch before any protected operation. |
| T-162-07 | Repudiation | control versus schedule evidence | high | mitigate | Persist event name and run ID in the machine result; never substitute dispatch for schedule per D-05. |
| T-162-08 | Information Disclosure | workflow token | medium | mitigate | Keep existing least-privilege permissions and sanitize artifact fields; do not serialize tokens or raw environment. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | Preserve the existing GitHub token boundary and permissions; fake-`gh` cases in `test/scripts/release_trigger_recovery_test.exs` prove missing/inaccessible authentication becomes `cannot-check`, never success. |
| V4 Access Control | Applicable | `test/scripts/release_trigger_recovery_test.exs` proves push, schedule, and digest-free dispatch cannot reach merge/tag/release operations and only exact protected dispatch can cross the gate. |
| V5 Input Validation | Applicable | The same workflow contract validates exact candidate digest, proposal head, source SHA, event, and run provenance; mismatch fixtures fail closed as `blocked`. |
| V6 Cryptography | Applicable | Reuse the existing content-digest comparison without changing its algorithm; `test/scripts/release_policy_contract_test.exs` preserves exact digest equality before protected operations. |
</threat_model>

<verification>
Run the focused release workflow tests and inspect the diff to confirm trigger blocks, permissions, protected-dispatch conditions, and protected operations are byte-preserved except for result reporting.
</verification>

<success_criteria>
- AUTO-03 produces truthful proposal-only results for every existing ordinary entry point.
- D-04 through D-06 and all AUTO-03 adjacency/empty/ordering predicates are test enforced.
- No new authority, dependency, permission, trigger, or workflow topology is introduced.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-02-SUMMARY.md` when done.
</output>
