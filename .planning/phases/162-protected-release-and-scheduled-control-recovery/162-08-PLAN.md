---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 08
type: execute
wave: 5
depends_on: [162-06, 162-07]
files_modified:
  - .github/workflows/release-please.yml
  - test/scripts/release_trigger_recovery_test.exs
autonomous: true
gap_closure: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "D-04 / AUTO-03: An hourly schedule with an empty candidate digest and no open release proposal finishes successfully with one bounded `pending` / `no_open_proposal` result; it does not enter proposal capture or any protected operation."
    - "D-04: Push, schedule, and digest-free dispatch remain proposal-only; only the existing protected exact candidate-digest dispatch can merge, tag, publish, or create a release."
    - "D-05: The idle-schedule result retains its real `schedule` event name and run ID and cannot be substituted by a manual dispatch."
    - "D-06: Scheduled proposal discovery, capture, result JSON, summary, upload, and the final success gate preserve one truthful bounded outcome rather than converting expected absence into a failed policy block."
    - "AUTO-03 adjacency: zero open proposals selects pending, exactly one proceeds to capture, and multiple proposals still reach the existing blocked classification."
    - "AUTO-03 empty-input: the executable fixture passes an actually empty `CANDIDATE_DIGEST` and an argument-sensitive `gh pr list` response containing zero proposals."
    - "AUTO-03 ordering: proposal discovery runs after the proposal-only release action/sibling sync and before capture; the result is serialized and uploaded before the final gate evaluates pass or pending."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Argument-bounded scheduled proposal discovery and successful idle pending control flow"
      contains: "no_open_proposal"
    - path: "test/scripts/release_trigger_recovery_test.exs"
      provides: "Executable empty-digest/no-open-proposal schedule regression fixture"
      contains: "no_open_proposal"
  key_links:
    - from: "scheduled proposal discovery outputs"
      to: "capture-proposal condition"
      via: "`should_capture` skips capture only for an established zero-proposal scheduled observation"
      pattern: "should_capture|no_open_proposal"
    - from: "scheduled proposal discovery outputs"
      to: "release-proposal-control-result.json"
      via: "the existing result writer consumes discovery status/reason when capture was intentionally skipped"
      pattern: "DISCOVERY_STATUS|DISCOVERY_REASON|CAPTURE_OUTCOME"
    - from: "release-proposal-control-result.json"
      to: "final proposal-control gate"
      via: "only `pass` and the bounded idle `pending` status finish successfully after summary/upload"
      pattern: "pass|pending"
  assumptions:
    - "FLAGGED (AUTO-01 unclassified): the executed reconciliation remains complete only while every D-01 source row remains append-only; this workflow repair does not edit reconciliation evidence."
    - "FLAGGED (AUTO-02 adjacency/empty/ordering): distinct release identities, explicit empty evidence categories, and stable disposition ordering remain governed by the executed reconciliation contract and are preservation constraints here."
    - "FLAGGED (AUTO-05 unclassified): every post-publish resolver outcome remains serialized by Plan 07; this release-please-only repair does not alter post-publish control flow."
  prohibitions:
    - statement: "MUST NOT portray the absence of an open proposal as a completed release, release authorization, or evidence that protected publication occurred."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
    - statement: "MUST NOT grant push, schedule, or digest-free dispatch merge, tag, publish, release-creation, or protected-dispatch authority."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Close the idle-schedule release blocker with one end-to-end scheduled path: discover that no open proposal exists, skip capture, serialize `pending`, upload the bounded evidence, and finish successfully.

Purpose: Restore truthful AUTO-03 scheduled behavior per D-04 through D-06 without expanding authority or changing protected release policy.
Output: A narrow scheduled-proposal discovery seam in `release-please.yml` and an executable empty-digest/no-proposal fixture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-06-SUMMARY.md
@.github/workflows/release-please.yml
@test/scripts/release_trigger_recovery_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute an idle scheduled release run through discovery, pending evidence, upload, and success</name>
  <reversibility rating="costly">The status and reason are retained operational evidence consumed by summaries and later reconciliation, but changing them requires only coordinated workflow and contract-test edits.</reversibility>
  <files>.github/workflows/release-please.yml, test/scripts/release_trigger_recovery_test.exs</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-06-SUMMARY.md
    - .github/workflows/release-please.yml
    - test/scripts/release_trigger_recovery_test.exs
  </read_first>
  <behavior>
    - "Idle schedule: empty candidate digest plus an exact zero-row open-proposal query writes `pending` / `no_open_proposal`, leaves proposal/source/digest identities empty, uploads the result, and exits zero."
    - "Active schedule: one open proposal proceeds through the existing capture shell and retains its pass/blocked/cannot-check semantics."
    - "Ambiguous or unavailable discovery never becomes pending success: multiple proposals remain blocked through capture and a failed GitHub query remains cannot-check/nonzero."
    - "No idle-schedule fixture command invokes protected merge, tagging, release creation, package publication, push, or protected dispatch."
  </behavior>
  <action>Start RED in `test/scripts/release_trigger_recovery_test.exs` with a behavioral test that extracts and executes the real `Detect already-tagged release PR`, new scheduled-proposal discovery, `Write proposal-only release control result`, and final proposal-control gate shell bodies in their workflow order. Drive the test with `EVENT_NAME=schedule`, an actually empty `CANDIDATE_DIGEST`, and a fake `gh` that rejects any invocation except `pr list --head release-please--branches--main --base main --state open --json number,headRefOid,baseRefOid`, then returns `[]`. Assert preflight writes `should_run=true`, discovery writes `should_capture=false`, `result_status=pending`, and `result_reason=no_open_proposal`, capture is skipped, the decoded JSON retains `schedule` plus the run ID and empty immutable identity fields, summary/upload ordering remains before the gate, and the gate exits zero only for this bounded pending outcome. Make the fake record argv and assert its log contains no D-04 authority operation; add companion one-row and discovery-failure assertions so the test cannot green by ignoring query arguments or treating every skip as pending. Then add `Discover scheduled Release Please proposal before capture` with id `scheduled-proposal` after the proposal-only action and sibling synchronization but before `capture-proposal`. Run it only for schedules with an empty digest, query the exact open head/base pair, and emit `should_capture`, `result_status`, and `result_reason`: zero rows is `false`/`pending`/`no_open_proposal`; one row is `true` and continues to the existing capture; query failure is `false`/`cannot-check`/`github_evidence_unavailable`; multiple rows continue to the existing capture so its established `multiple_open_proposals` block remains authoritative. Extend the capture condition to honor `should_capture` only for the applicable empty-digest schedule. Feed discovery status/reason into the existing result writer when capture is skipped, emit both final `status` and `reason` as writer outputs, and retain its one JSON-to-summary/upload path. Give the final gate both outputs and make its exact success predicate `status == pass` OR (`status == pending` AND `reason == no_open_proposal`); blocked, cannot-check, and every other pending reason remain nonzero. Preserve triggers, permissions, protected-main checkouts, release action, exact protected-dispatch gates, capture identity/digest checks, ordinary auto-merge disarm, and all D-04 authority boundaries byte-for-byte outside this control-flow seam.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The test executes the actual empty-digest scheduled preflight, argument-sensitive open-proposal discovery, result writer, and final gate; no default candidate digest or argument-agnostic fake is used.
    - Zero proposals produce exactly `pending` / `no_open_proposal`, empty proposal identities, a decodable artifact, matching summary status/reason, and a zero final-gate exit.
    - One proposal still reaches capture; multiple proposals remain blocked; discovery acquisition failure remains cannot-check and nonzero.
    - Discovery occurs after proposal creation/synchronization and before capture, while result write and upload occur before the final success/non-pass gate per AUTO-03 ordering.
    - Existing pass and identity-mismatch post-worktree fixtures remain green, and ordinary triggers still cannot merge, tag, publish, create a release, push a protected result, or invoke protected dispatch per D-04.
  </acceptance_criteria>
  <done>An idle hourly release schedule reports successful bounded pending evidence instead of failing as `proposal_missing`, while every authority and non-pass boundary remains intact.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/release-please.yml` — new step id `scheduled-proposal` with outputs `should_capture`, `result_status`, and `result_reason`; `proposal-control-result` gains a `reason` step output, and the existing JSON field `reason` gains the exact value `no_open_proposal`.
- `test/scripts/release_trigger_recovery_test.exs` — executable empty-digest/no-proposal scheduled fixture plus argument-sensitive one-proposal and unavailable-discovery controls.
- New files: none.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled event → proposal discovery | Expected absence must be established by the exact GitHub query, not assumed from an empty digest. |
| discovery outputs → capture/result writer | Only a proven zero-proposal schedule may skip capture as successful pending. |
| ordinary trigger → protected release operations | Pending evidence must not confer merge, tag, publish, release, or protected-dispatch authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-29 | Elevation of Privilege | idle scheduled result | critical | mitigate | Preserve every D-04 protected gate and execute an argument-logging fixture that rejects any merge/tag/release/publish/protected-dispatch call. |
| T-162-30 | Spoofing / Tampering | open-proposal discovery | high | mitigate | Require the exact head/base/state query and distinguish zero, one, multiple, and acquisition-failure shapes before capture selection. |
| T-162-31 | Repudiation | pending artifact | high | mitigate | Persist event name, run ID, exact pending reason, and empty proposal identities in the same JSON rendered and uploaded before the final gate. |
| T-162-32 | Denial of Service | normal idle schedule | medium | mitigate | Accept only the serialized `pending` idle outcome as successful; blocked and cannot-check remain nonzero. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V2 Authentication | Applicable | A failed authenticated GitHub query becomes cannot-check/nonzero; the fixture proves it cannot be reclassified as absence. |
| V4 Access Control | Applicable | `release_trigger_recovery_test.exs` preserves protected exact-digest dispatch as the sole authority crossing and rejects authority operations in idle schedule execution. |
| V5 Input Validation | Applicable | The fixture validates empty digest, exact query argv, zero/one/multiple proposal cardinality, event provenance, and allowed result statuses. |
| V6 Cryptography | Applicable | Existing candidate/content digest validation remains unchanged and is bypassed only because an established idle schedule has no candidate to validate. |
</threat_model>

<verification>
Run the focused release-trigger suite, then run `mix test test/scripts/phase_162_release_reconciliation_test.exs test/scripts/release_trigger_recovery_test.exs --seed 0` to preserve append-only reconciliation and protected-authority coverage. Local fixtures prove behavior; do not claim they are an observed GitHub scheduled run.
</verification>

<success_criteria>
- AUTO-03's empty/no-proposal scheduled blocker is closed by executable runtime-shape coverage.
- D-04 through D-06 remain enforced with one truthful artifact and no authority expansion.
- Blocked/cannot-check schedules still fail; only pass and established idle pending finish successfully.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-08-SUMMARY.md` when done.
</output>
