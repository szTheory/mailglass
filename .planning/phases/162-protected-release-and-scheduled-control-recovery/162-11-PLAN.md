---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 11
type: execute
wave: 6
depends_on: [162-09]
files_modified:
  - dev/mix/tasks/mailglass.repo.hygiene.ex
  - test/mix/tasks/mailglass.repo.hygiene_test.exs
autonomous: true
gap_closure: true
requirements: [AUTO-04]
must_haves:
  truths:
    - "D-06 / AUTO-04: A zero-exit `gh run list` response that is invalid JSON or not a JSON list becomes a bounded `cannot-check` CI result with recovery detail instead of raising before report serialization."
    - "The malformed-success path flows through the existing aggregate, text, JSON, CLI exit, Actions summary, and artifact contract; the JSON result remains decodable and the CLI remains nonzero."
    - "Valid list responses retain exact checkout-SHA, returned `headSha`, completed status, and success-conclusion validation; an empty list or valid nonmatching run remains policy `blocked`."
    - "A nonzero GitHub CLI result remains `cannot-check` with its command error, distinct from malformed zero-exit evidence and from a confirmed policy block."
  artifacts:
    - path: "dev/mix/tasks/mailglass.repo.hygiene.ex"
      provides: "Non-raising GitHub run-list decode and list-shape classification at the CI evidence boundary"
      contains: "Jason.decode"
    - path: "test/mix/tasks/mailglass.repo.hygiene_test.exs"
      provides: "Malformed and non-list zero-exit GitHub fixtures through JSON/nonzero CLI reporting"
      contains: "malformed"
  key_links:
    - from: "zero-exit gh run-list bytes"
      to: "ci_state check"
      via: "Jason.decode/1 plus explicit list-shape validation"
      pattern: "Jason.decode|is_list"
    - from: "ci_state cannot-check"
      to: "CLI JSON and Actions evidence"
      via: "the existing audit result map, external status serializer, nonzero exit, and artifact-first renderer"
      pattern: "cannot_check|external_status|repo-hygiene.json"
  assumptions:
    - "FLAGGED (AUTO-01 unclassified): the executed release reconciliation remains append-only and this Mix task/test repair changes no release evidence."
    - "FLAGGED (AUTO-02 adjacency): equal release identities remain independent reconciliation rows."
    - "FLAGGED (AUTO-02 empty): explicit empty disposition categories remain distinct from unavailable remote evidence."
    - "FLAGGED (AUTO-02 ordering): stable reconciliation ordering remains unchanged."
    - "FLAGGED (AUTO-03 adjacency): exact candidate-digest authority remains governed by the release workflow and parallel Plan 10."
    - "FLAGGED (AUTO-03 empty): empty proposal behavior remains governed by the executed scheduled fixture and parallel Plan 10's protected lifecycle fixture."
    - "FLAGGED (AUTO-03 ordering): protected validation/release and proposal-only ordering remain outside this Mix task."
    - "FLAGGED (AUTO-04 unclassified): malformed successful remote bytes are treated as unavailable evidence; no stronger semantic classification is inferred."
    - "FLAGGED (AUTO-05 unclassified): immutable post-publish proof remains unchanged."
  prohibitions:
    - statement: "MUST NOT crash, emit partial output, or report pass/blocked when successful GitHub CLI bytes cannot be decoded as the expected run-list array."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
    - statement: "MUST NOT weaken exact-SHA run validation or convert nonzero GitHub CLI failure into confirmed absence."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Close the repository-hygiene malformed-success blocker by converting invalid zero-exit GitHub run-list output into the existing bounded cannot-check evidence path.

Purpose: Satisfy D-06 and AUTO-04 at the external CLI trust boundary without changing workflow topology, result schema, safe-action behavior, or policy precedence.
Output: Non-raising decode/list validation in `ci_state/1` and executable JSON/nonzero CLI regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-03-SUMMARY.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-09-SUMMARY.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
@test/mix/tasks/mailglass.repo.hygiene_test.exs
@.github/workflows/repo-hygiene.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace malformed successful GitHub bytes through bounded JSON and nonzero CLI evidence</name>
  <reversibility rating="reversible">This replaces one internal bang decode with explicit result classification and adds fixtures; it changes no persisted schema, public API, permission, or remote state.</reversibility>
  <files>dev/mix/tasks/mailglass.repo.hygiene.ex, test/mix/tasks/mailglass.repo.hygiene_test.exs</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-03-SUMMARY.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-09-SUMMARY.md
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - test/mix/tasks/mailglass.repo.hygiene_test.exs
    - .github/workflows/repo-hygiene.yml
  </read_first>
  <behavior>
    - "Malformed zero-exit response: fake `gh` returns invalid JSON with exit 0; `Hygiene.audit/1` returns `ci_state: cannot_check`, and `Hygiene.run([\"--check\", \"--format\", \"json\"])` emits decodable cannot-check JSON before exiting nonzero."
    - "Wrong-shape zero-exit response: valid JSON object/null/scalar is rejected because the run-list contract requires a list; it follows the same cannot-check and recovery-detail path."
    - "Nonzero response: the existing unavailable-query fixture remains cannot-check with its command error and nonzero CLI policy."
    - "Valid list response: matching completed-success remains pass; empty, wrong-head, incomplete, or failed list remains blocked."
  </behavior>
  <action>Start RED in `test/mix/tasks/mailglass.repo.hygiene_test.exs` by feeding the existing argument-validating fake `gh` an invalid JSON response with exit 0, then exercise both `Hygiene.audit/1` and the real JSON CLI boundary. Assert `ci_state` and aggregate are `:cannot_check`, details name the malformed/unexpected GitHub response and give a bounded retry/inspection recovery action, emitted stdout is valid JSON with top-level and `ci_state` status `cannot-check`, and `--check --format json` exits nonzero. Add a valid-but-non-list JSON case to pin the list-shape contract. Keep the existing nonzero, matching, empty-list, wrong-head, incomplete, and failed-run cases in the same focused suite. If needed, extend `run_hygiene/2` with options or add a narrowly named test helper so the CLI path uses the same fake response rather than hand-encoding a result map. Then replace only the `ci_state/1` successful-command `Jason.decode!/1` pipeline with `Jason.decode/1` and explicit `is_list/1` handling. For `{:ok, runs}` where `runs` is a list, retain `List.first/1` and every exact SHA/completed/success policy branch. For decode error or any decoded non-list value, return `cannot_check(:ci_state, ...)` with the checkout SHA, a bounded diagnostic/recovery detail, and no raw exception. Preserve nonzero-command cannot-check handling, cannot-check-over-blocked-over-pass precedence, external spelling, reason selection, JSON/text serializers, CLI exits, `--apply` behavior, and `.github/workflows/repo-hygiene.yml` unchanged per D-06.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Invalid JSON returned with exit 0 does not raise and produces a `ci_state` cannot-check result with checkout SHA plus actionable recovery detail.
    - Valid JSON that is not a list follows the same bounded cannot-check path; it is not treated as an empty run list or policy block.
    - The real JSON CLI emits one decodable result whose aggregate and `ci_state` spell `cannot-check`, then exits nonzero before the workflow's existing always-run summary/upload consumers.
    - Nonzero GitHub CLI output remains cannot-check; valid empty, mismatched, incomplete, or failed run lists remain blocked; exact matching completed-success remains pass.
    - No workflow, permission, result field, safe-action authority, dirty-work behavior, dependency, API, schema, or post-publish timeout change is introduced.
  </acceptance_criteria>
  <done>Malformed successful GitHub output is bounded as inspectable cannot-check evidence instead of escaping the Mix task before JSON and nonzero policy are produced.</done>
</task>

</tasks>

## Artifacts this phase produces

- `dev/mix/tasks/mailglass.repo.hygiene.ex` — `ci_state/1` uses `Jason.decode/1` and explicit list-shape validation; no new production function, status, result field, or CLI option.
- `test/mix/tasks/mailglass.repo.hygiene_test.exs` — malformed-zero-exit and non-list-zero-exit fixtures through `audit/1` and the real JSON CLI boundary; optional test-local helper/options only.
- New paths, files, external API capabilities, workflow artifacts, schema fields, and packages: none.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Existing controls report truthful bounded outcomes | 01-11 | COVERED | Plan 11 bounds malformed successful remote evidence. |
| REQ | AUTO-01 | Evidence-backed release narrative | 01, 05 | COVERED | Executed and preserved unchanged. |
| REQ | AUTO-02 | Explicit protected/retire/retain dispositions | 01, 05 | COVERED | Executed and preserved unchanged. |
| REQ | AUTO-03 | Proposal-only results without authority expansion | 02, 05, 06, 08, 10 | COVERED | Parallel Plan 10 closes the protected lifecycle blocker. |
| REQ | AUTO-04 | Inspectable three-state hygiene with control/schedule evidence | 03, 05, 09, 11 | COVERED | Plan 11 closes malformed successful GitHub output. |
| REQ | AUTO-05 | Exact immutable post-publish outcome | 04, 05, 07 | COVERED | Executed and preserved unchanged. |
| RESEARCH | — | Mix task owns the result; Actions renders the same JSON | 11 | COVERED | Repair occurs inside `ci_state/1`; workflow is unchanged. |
| RESEARCH | — | Remote unavailability is cannot-check, not absence | 11 | COVERED | Decode/shape failures join the bounded unavailable-evidence path. |
| RESEARCH | — | No package, service, workflow, or API surface addition | 11 | COVERED | Existing Jason, GitHub CLI, and ExUnit seams only; `COVERAGE.md` remains unchanged. |
| CONTEXT | D-01, D-02, D-03 | Append-only blocked reconciliation and safe dispositions | 01, 05; preserved by 11 | COVERED | No release evidence/state mutation. |
| CONTEXT | D-04 | Protected authority boundary | 02, 05, 06, 08, 10; preserved by 11 | COVERED | Outside Mix task. |
| CONTEXT | D-05 | Separate control/scheduled provenance | 03, 05, 09; preserved by 11 | COVERED | Exact checkout SHA remains in details. |
| CONTEXT | D-06 | Bounded agreeing human/machine outcomes | 11 | COVERED | Malformed bytes reach the existing one-map serializer. |
| CONTEXT | D-07, D-08 | Immutable post-publish proof; no forced release | 04, 05, 07; preserved by 11 | COVERED | Outside changed files. |

Descriptor-less probe coverage: AUTO-04's unclassified malformed-success edge is a direct truth with executable audit and CLI fixtures. AUTO-01 unclassified, AUTO-02 adjacency/empty/ordering, AUTO-03 adjacency/empty/ordering, and AUTO-05 unclassified remain explicit flagged preservation assumptions. No unresolved probe is silently promoted to a verified truth. Deferred Phase 163 timeouts, Phase 164 closeout, dirty-work preservation, post-publish polling budget, and SEED-006 remain excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub CLI bytes → JSON decoder | Exit 0 does not make untrusted bytes syntactically or structurally valid. |
| decoded run list → CI policy | Only a list with a matching immutable run may satisfy CI truth. |
| Mix result → Actions summary/artifact | All failure shapes must reach one bounded serializable result map. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-41 | Tampering | GitHub run-list response | high | mitigate | Use non-raising decode plus explicit list validation before inspecting any run. |
| T-162-42 | Denial of Service | `ci_state/1` decode boundary | high | mitigate | Convert malformed successful bytes into cannot-check and prove the full JSON/nonzero CLI path. |
| T-162-43 | Spoofing | returned CI identity | high | mitigate | Preserve exact checkout SHA/returned headSha and terminal-success comparisons for valid lists. |
| T-162-44 | Repudiation | cannot-check evidence | medium | mitigate | Retain checkout SHA and bounded diagnostic/recovery details in the serialized check. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|---|---|---|
| V2 Authentication | Applicable | Auth/command failure remains distinct cannot-check evidence and cannot become confirmed absence. |
| V4 Access Control | Applicable | Existing read-only workflow permissions and safe-action authority remain unchanged. |
| V5 Input Validation | Applicable | ExUnit covers invalid JSON, non-list JSON, empty list, wrong SHA, incomplete/failed run, and exact completed-success input. |
| V6 Cryptography | N/A | Existing Git SHA identity comparison is preserved; no digest primitive or cryptographic contract changes. |
</threat_model>

<verification>
Run the focused hygiene suite, then run `mix test test/scripts/release_trigger_recovery_test.exs test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0` as the parallel Wave 6 blocker gate. Confirm malformed and non-list zero-exit fixtures emit valid cannot-check JSON and nonzero exits without altering the existing workflow artifact renderer.
</verification>

<success_criteria>
- AUTO-04's malformed zero-exit GitHub response blocker is closed by executable audit and CLI regression coverage.
- D-06's pass/blocked/cannot-check result, JSON, summary, artifact, and nonzero policies remain aligned.
- No secondary warning, workflow topology, release authority, API surface, dependency, schema, or timeout work enters scope.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-11-SUMMARY.md` when done.
</output>
