---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 13
type: execute
wave: 7
depends_on: [162-11]
files_modified:
  - dev/mix/tasks/mailglass.repo.hygiene.ex
  - test/mix/tasks/mailglass.repo.hygiene_test.exs
autonomous: true
gap_closure: true
requirements: [AUTO-04]
must_haves:
  truths:
    - "D-01: The append-only release reconciliation remains unchanged by this read-only hygiene repair."
    - "D-02: Unavailable remote evidence remains cannot-check rather than inferred absence."
    - "D-03: Existing PR, branch, check, and retained-evidence dispositions remain unchanged."
    - "D-04: Proposal-only and protected-dispatch authority boundaries remain unchanged."
    - "D-05: Control and applicable scheduled-run evidence remain separate."
    - "D-06: AUTO-04 successful `gh pr list` bytes are decoded without raising and must be a JSON list before repository-hygiene classifies open-PR policy."
    - "D-07: Exact immutable post-publish recovery remains unchanged."
    - "D-08: Unpublished/no-op states remain bounded and cannot force publication."
    - "Malformed JSON and valid non-list JSON become aggregate `cannot-check` evidence with a retry/inspection reason, decodable JSON output, and the existing nonzero exit."
    - "Valid empty lists remain pass and valid nonempty lists remain blocked with their existing open-count/details behavior."
  artifacts:
    - path: "dev/mix/tasks/mailglass.repo.hygiene.ex"
      provides: "Non-raising, list-shaped PR evidence classification in pull_requests/1"
      contains: "defp pull_requests(repo)"
    - path: "test/mix/tasks/mailglass.repo.hygiene_test.exs"
      provides: "Malformed and non-list successful PR-list regression through audit and JSON CLI boundaries"
      contains: "bounds malformed and non-list successful PR responses"
  key_links:
    - from: "gh pr list zero-exit stdout"
      to: "pull_requests/1 check result"
      via: "Jason.decode/1 plus is_list/1 shape guard"
      pattern: "Jason.decode\(json\)|when is_list\(prs\)"
    - from: "pull_requests/1 cannot-check result"
      to: "CLI JSON, Actions summary, and retained workflow artifact"
      via: "existing audit/1 result map and external-status serialization"
      pattern: "cannot_check\(:pull_requests|cannot-check"
  assumptions:
    - statement: "FLAGGED (AUTO-01 unclassified): the executed D-01 reconciliation remains complete and this task must not rewrite its evidence."
      status: unverified
      verification: backstop
    - statement: "FLAGGED (AUTO-02 adjacency/empty/ordering): existing evidence identity and disposition contracts remain unchanged."
      status: unverified
      verification: backstop
    - statement: "FLAGGED (AUTO-03 adjacency/empty/ordering): existing proposal identity equality, idle-schedule empty handling, and protected/proposal path ordering remain unchanged."
      status: unverified
      verification: backstop
    - statement: "FLAGGED (AUTO-04 unclassified): a zero process exit is transport success only; semantic PR evidence requires decodable list-shaped JSON."
      status: unverified
      verification: backstop
    - statement: "FLAGGED (AUTO-05 unclassified): exact immutable post-publish proof remains independent of this hygiene repair."
      status: unverified
      verification: backstop
  prohibitions:
    - statement: "MUST NOT interpret malformed or non-list PR data as an empty list, a passing check, or a complete policy block."
      status: unverified
      flagged: true
    - statement: "MUST NOT let external PR-list decoding escape before the aggregate JSON result, summary, and artifact can be produced."
      status: unverified
      flagged: true
---

<objective>
Close the remaining repository-hygiene error path by tracing malformed successful PR-list bytes through the existing three-state result map, JSON CLI, and nonzero exit.

Purpose: Restore AUTO-04's inspectable bounded outcome per D-06 without changing repository policy, workflow topology, or release authority.
Output: Non-raising PR-list shape validation and focused audit/CLI regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-11-SUMMARY.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
@test/mix/tasks/mailglass.repo.hygiene_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace malformed successful PR bytes through bounded cannot-check JSON</name>
  <files>dev/mix/tasks/mailglass.repo.hygiene.ex, test/mix/tasks/mailglass.repo.hygiene_test.exs</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-11-SUMMARY.md
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - test/mix/tasks/mailglass.repo.hygiene_test.exs
  </read_first>
  <behavior>
    - "Malformed fixture: zero-exit `gh pr list` stdout that cannot decode produces a `pull_requests` cannot-check result, aggregate cannot-check, a retry/inspection message, decodable JSON, and `{:shutdown, 1}`."
    - "Non-list fixture: zero-exit JSON object, scalar, or null produces the same bounded cannot-check contract rather than being treated as no open PRs."
    - "Valid fixtures: `[]` remains pass; a list with one PR remains blocked and retains the existing `open_count` and `prs` details."
  </behavior>
  <action>Start RED in `test/mix/tasks/mailglass.repo.hygiene_test.exs` with the test named `bounds malformed and non-list successful PR responses as cannot-check JSON evidence`. Extend `with_hygiene_environment/3` or its fake-`gh` helper with a PR-specific response and exit seam while preserving the current exact `gh run list` fixture. Exercise at least malformed bytes and a valid JSON object through both `Hygiene.audit/1` and `run_hygiene(..., ["--check", "--format", "json"])`; assert the `pull_requests` check and aggregate serialize as `cannot-check`, include a bounded inspect/retry recovery message, and exit nonzero. Add explicit valid-empty and valid-nonempty assertions so equality at list length zero remains pass and one open PR remains blocked. Then replace `Jason.decode!/1` inside `pull_requests/1` with the same `Jason.decode/1` plus list-shape case pattern already established in `ci_state/1`: only `{:ok, prs} when is_list(prs)` reaches `Enum.empty?/1`, `length/1`, and `pr_message/1`; valid non-list and decode-error branches call `cannot_check(:pull_requests, ...)` with distinct unexpected/malformed diagnostics and no inferred PR count. Preserve `audit/1` precedence, result schema, text/JSON status spelling, nonzero non-pass exit, `.github/workflows/repo-hygiene.yml`, and all D-06 summary/artifact wiring. Per D-01 through D-05, do not alter release evidence, dispositions, triggers, schedule provenance, or authority; per D-07/D-08, do not touch post-publish recovery.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Malformed and non-list zero-exit PR-list fixtures return `pull_requests: cannot_check` and aggregate `cannot_check` without raising.
    - The JSON CLI output is decodable, spells both relevant statuses `cannot-check`, carries an inspect/retry reason, and exits with `{:shutdown, 1}`.
    - Valid `[]` remains a passing PR check; a valid one-element list remains blocked with `open_count: 1` and the original PR details.
    - Existing malformed CI-run, detached-SHA, three-state precedence, summary, and artifact tests remain green.
    - No workflow, permission, dependency, external API surface, schema, release policy, merge, tag, publish, or protected-dispatch change.
  </acceptance_criteria>
  <done>Every successful PR-list command response produces either a valid list-based policy result or bounded cannot-check evidence that reaches the existing JSON/artifact boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- `dev/mix/tasks/mailglass.repo.hygiene.ex` — modified private `pull_requests/1` with `Jason.decode/1` list-shape branches; no new public symbol, field, flag, schema, or API.
- `test/mix/tasks/mailglass.repo.hygiene_test.exs` — new `bounds malformed and non-list successful PR responses as cannot-check JSON evidence` regression and PR-specific fake-`gh` fixture controls.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub CLI PR response → Mix task | Zero exit establishes command completion, not valid JSON or a list-shaped PR observation. |
| Mix check → aggregate result | Unavailable evidence must outrank policy conclusions and remain cannot-check. |
| aggregate result → CLI/workflow artifact | The bounded result must survive serialization and nonzero exit without a pre-render exception. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-13-01 | Tampering | pull_requests/1 external JSON boundary | high | mitigate | Decode without raising, require a list, and reject malformed/non-list success bytes as cannot-check. |
| T-162-13-02 | Repudiation | repository-hygiene JSON evidence | high | mitigate | Exercise malformed paths through the real JSON CLI and require decodable status/reason/check evidence before nonzero exit. |
| T-162-13-03 | Denial of Service | Jason decoding in pull_requests/1 | medium | mitigate | Remove the bang decoder so unexpected remote bytes cannot abort aggregate rendering. |
| T-162-13-04 | Elevation of Privilege | release and repository controls | low | accept | The change is confined to read-only PR evidence classification and does not touch workflow permissions or release operations. |
| T-162-13-SC | Tampering | package/action supply chain | low | accept | No package install or action reference change is planned; existing dependencies and pins remain unchanged. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V4 Access Control | Applicable | Read-only evidence handling cannot authorize merge, tag, release, publish, or protected dispatch. |
| V5 Input Validation | Applicable | JSON syntax and top-level list shape are both validated before list operations. |
| V6 Cryptography | Not applicable | No digest or cryptographic behavior changes in this plan. |
</threat_model>

<verification>
Run `mix test test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0`, then run `mix test test/scripts/release_trigger_recovery_test.exs test/mix/tasks/mailglass.repo.hygiene_test.exs --seed 0` as the combined Wave 7 boundary gate. Run `git diff --check` for the two owned files.
</verification>

<success_criteria>
- AUTO-04's remaining malformed-success PR observation becomes inspectable cannot-check evidence rather than an exception.
- D-06's existing result-map, summary, artifact, and nonzero-exit contracts remain the sole reporting path.
- Valid PR lists retain their established pass/blocked semantics and no release or repository authority expands.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Explain and safely disposition blocked release state with truthful bounded controls | 162-01..13 | COVERED | Plans 12 and 13 close the only two current verification blockers. |
| REQ | AUTO-01 | Evidence-backed release narrative | 162-01, 162-05 | COVERED | Verified and untouched. |
| REQ | AUTO-02 | Safe PR/branch/check dispositions | 162-01, 162-05 | COVERED | Verified and untouched. |
| REQ | AUTO-03 | Proposal-only controls without authority expansion | 162-02, 162-06, 162-08, 162-10, 162-12 | COVERED | Plan 12 closes dispatcher authorization. |
| REQ | AUTO-04 | Three-state repository-hygiene evidence | 162-03, 162-09, 162-11, 162-13 | COVERED | Plan 13 closes malformed successful PR-list handling. |
| REQ | AUTO-05 | Exact immutable post-publish proof | 162-04, 162-07 | COVERED | Verified and untouched. |
| RESEARCH | — | Existing policy owner, proposal-only authority, immutable targets, no dependencies | 162-01..12 | COVERED | Existing implementations remain authoritative. |
| RESEARCH | — | Three-state artifact-first reporting and remote-input validation | 162-03, 162-11, 162-13 | COVERED | Plan 13 applies the established CI boundary pattern to PR data. |
| CONTEXT | D-01, D-02, D-03 | Append-only reconciliation and safe release dispositions | 162-01, 162-05 | COVERED | Preserved by explicit task constraints. |
| CONTEXT | D-04, D-05, D-06 | Bounded control/schedule authority and evidence | 162-02, 162-03, 162-05, 162-06, 162-08..13 | COVERED | Plan 13 preserves controls while closing the error path. |
| CONTEXT | D-07, D-08 | Exact immutable published target or bounded unpublished outcome | 162-04, 162-07 | COVERED | Verified and untouched. |

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-13-SUMMARY.md` when done.
</output>
