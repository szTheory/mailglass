---
phase: 162-protected-release-and-scheduled-control-recovery
plan: 10
type: execute
wave: 6
depends_on: [162-08]
files_modified:
  - .github/workflows/release-please.yml
  - test/scripts/release_trigger_recovery_test.exs
autonomous: true
gap_closure: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "D-04 / AUTO-03: A protected exact-candidate dispatch may complete merge and release without subsequently entering proposal-only discovery, capture, result writing, upload, or failure gating."
    - "D-06: Push, schedule, and digest-free dispatch retain the existing bounded proposal-control JSON, summary, upload, and pass/pending/non-pass policy; only the protected candidate-digest lifecycle is excluded from that proposal-only tail."
    - "AUTO-03 adjacency: only a nonempty exact candidate digest selects the protected lifecycle; an empty digest remains an ordinary proposal-only run and a near-match still fails the existing protected validation before merge."
    - "AUTO-03 empty-input: after a successful protected merge leaves zero open release proposals, that zero-row state cannot become `proposal_missing` or fail the already-completed protected release."
    - "AUTO-03 ordering: exact protected validation precedes merge and release, while proposal-only capture/result gating applies only to ordinary triggers and therefore cannot run after protected completion."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Mutually exclusive protected-release and proposal-only control tails"
      contains: "github.event.inputs.candidate_digest == ''"
    - path: "test/scripts/release_trigger_recovery_test.exs"
      provides: "Executable protected merge/release followed by zero-proposal lifecycle regression"
      contains: "post-merge"
  key_links:
    - from: "protected-dispatch and protected-merge outputs"
      to: "release action"
      via: "the existing exact candidate-digest authorization, content, and merge-tree gates"
      pattern: "protected-dispatch|protected-merge|candidate_digest"
    - from: "candidate-digest trigger classification"
      to: "capture-proposal, proposal-control-result, summary, upload, and final gate"
      via: "one consistent proposal-only predicate excludes the protected lifecycle"
      pattern: "candidate_digest == ''"
  assumptions:
    - "FLAGGED (AUTO-01 unclassified): the executed append-only release reconciliation remains complete and unchanged by this workflow-only repair."
    - "FLAGGED (AUTO-02 adjacency): equal release identities remain separate reconciliation rows; this plan changes no identity or disposition semantics."
    - "FLAGGED (AUTO-02 empty): empty disposition categories remain explicit `NONE-*` evidence rather than inferred absence."
    - "FLAGGED (AUTO-02 ordering): reconciliation ordering remains category then bytewise immutable identity."
    - "FLAGGED (AUTO-04 unclassified): the executed repository-hygiene three-state contract remains unchanged by this release workflow repair."
    - "FLAGGED (AUTO-05 unclassified): the executed immutable post-publish resolver remains the exhaustive publication-proof contract."
  prohibitions:
    - statement: "MUST NOT let proposal-only reporting retroactively fail a protected release that already passed exact validation, merge, and release creation."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
    - statement: "MUST NOT broaden merge, tag, release, publish, or protected-dispatch authority to push, schedule, or digest-free manual runs."
      status: unverified
      flagged: true
      disposition: block_execution_until_task_assertion_passes
---

<objective>
Close the protected-dispatch lifecycle blocker by making the protected exact-digest release tail mutually exclusive with proposal-only capture and result gating.

Purpose: Preserve D-04/D-06 and AUTO-03 while ensuring a normal post-merge zero-proposal observation cannot turn a completed protected release into a false failure.
Output: Narrow workflow predicates and an executable end-to-end lifecycle regression in the existing release recovery suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-06-SUMMARY.md
@.planning/phases/162-protected-release-and-scheduled-control-recovery/162-08-SUMMARY.md
@.github/workflows/release-please.yml
@test/scripts/release_trigger_recovery_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a completed protected release past the post-merge zero-proposal state</name>
  <reversibility rating="reversible">The change is confined to internal workflow step predicates and an ExUnit fixture; reverting restores the prior control flow without data migration or public-contract change.</reversibility>
  <files>.github/workflows/release-please.yml, test/scripts/release_trigger_recovery_test.exs</files>
  <read_first>
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-VERIFICATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-06-SUMMARY.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-08-SUMMARY.md
    - .github/workflows/release-please.yml
    - test/scripts/release_trigger_recovery_test.exs
  </read_first>
  <behavior>
    - "Protected lifecycle: one exact open proposal passes the existing candidate validation, protected merge removes it, release creation succeeds, and the subsequent zero-proposal state does not invoke proposal-only capture or its final gate."
    - "Ordinary lifecycle: push, schedule, and digest-free dispatch still reach the existing proposal-control writer, summary, upload, and final gate with unchanged pass/blocked/cannot-check/pending semantics."
    - "Authority regression: only the nonempty exact candidate-digest path may invoke protected merge/release operations; ordinary fixtures continue to prove those operations absent."
  </behavior>
  <action>Start RED in `test/scripts/release_trigger_recovery_test.exs` with an executable protected-dispatch lifecycle fixture. Reuse the existing workflow block extractors and fake command logging, and add `run_protected_dispatch_lifecycle/1` only if a focused helper is needed. Seed the fixture with exactly one open release proposal, execute the protected merge/release sequence so the fake GitHub state transitions to zero open proposals, then execute the workflow's post-release selection boundary and prove `capture-proposal`, `proposal-control-result`, its summary/upload, and the final proposal gate are not selected. Assert the command log proves protected merge and release occurred before the zero-row state, and that no post-merge proposal query can yield `proposal_missing`. Keep the existing pre-merge exact identity, required-check, content-digest, and merge-tree assertions in the lifecycle rather than replacing them with a static string-only check. Then update `.github/workflows/release-please.yml` so proposal capture and every proposal-only result consumer share the digest-empty predicate per D-04: push, schedule, and digest-free dispatch continue unchanged, while a nonempty protected candidate digest skips the entire proposal-only tail after the existing release action. Do not change triggers, permissions, action pins, protected validation, merge mechanics, release action inputs, scheduled discovery semantics, or the ordinary non-pass exit policy per D-04/D-06.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The new fixture executes an exact protected merge/release lifecycle, transitions its fake PR query from one row to zero rows, and finishes successfully without executing proposal-only capture/result/final-gate code.
    - The fixture fails if the protected candidate-digest run can reach `proposal_missing` after merge, or if any proposal-only tail step loses the consistent digest-empty predicate.
    - Existing executable proposal pass, identity-mismatch, unavailable-evidence, and scheduled zero/one/multiple-proposal fixtures remain green.
    - The protected dispatch remains the sole merge/release authority; ordinary trigger fixtures still log no protected operation per D-04.
    - No workflow topology, permission, action pin, package, API, schema, release ledger, tag, or publication behavior changes beyond the mutually exclusive tail predicates.
  </acceptance_criteria>
  <done>A completed exact-digest protected release cannot be converted into a false proposal-missing failure by the normal post-merge zero-proposal state.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/release-please.yml` — consistent digest-empty predicates on the existing proposal-only capture, result writer, summary, upload, and final gate; no new step, output, field, trigger, or permission.
- `test/scripts/release_trigger_recovery_test.exs` — executable protected-dispatch post-merge zero-proposal fixture and optional test-local `run_protected_dispatch_lifecycle/1` helper.
- New production symbols, result fields, fixture paths, and files: none.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Blocked release is safely explained while controls report bounded truth | 01-10 | COVERED | Plan 10 removes the false failure after a completed protected release. |
| REQ | AUTO-01 | Evidence-backed release narrative | 01, 05 | COVERED | Executed and preserved unchanged. |
| REQ | AUTO-02 | Explicit protected/retire/retain dispositions | 01, 05 | COVERED | Executed and preserved unchanged. |
| REQ | AUTO-03 | Proposal-only ordinary results without authority expansion | 02, 05, 06, 08, 10 | COVERED | Plan 10 closes protected post-merge lifecycle interference. |
| REQ | AUTO-04 | Three-state hygiene evidence | 03, 05, 09, 11 | COVERED | Parallel Plan 11 repairs malformed successful output. |
| REQ | AUTO-05 | Exact immutable post-publish outcome | 04, 05, 07 | COVERED | Executed and preserved unchanged. |
| RESEARCH | — | Existing protected topology and ReleasePolicy remain authoritative | 10 | COVERED | Only proposal-tail predicates and existing tests change. |
| RESEARCH | — | No package, service, workflow, or API surface addition | 10 | COVERED | Existing workflow and ExUnit seams only; `COVERAGE.md` remains unchanged. |
| CONTEXT | D-01, D-02, D-03 | Append-only blocked reconciliation and safe dispositions | 01, 05; preserved by 10 | COVERED | No evidence or release-state mutation. |
| CONTEXT | D-04 | Ordinary triggers stay proposal-only; protected dispatch stays unique | 10 | COVERED | Mutually exclusive control tails preserve the authority boundary. |
| CONTEXT | D-05 | Control and scheduled evidence remain distinct | 02, 05, 08; preserved by 10 | COVERED | No provenance substitution. |
| CONTEXT | D-06 | Bounded agreeing outcomes | 10 | COVERED | Proposal result tail remains unchanged for its applicable entries. |
| CONTEXT | D-07, D-08 | Immutable post-publish proof; no forced release | 04, 05, 07; preserved by 10 | COVERED | Outside changed files. |

Descriptor-less probe coverage: AUTO-03 adjacency, empty, and ordering are direct truths with an executable lifecycle fixture. AUTO-01 unclassified, AUTO-02 adjacency/empty/ordering, AUTO-04 unclassified, and AUTO-05 unclassified remain explicit flagged preservation assumptions. No unresolved probe is treated as verified by assertion alone. Deferred Phase 163 timeouts, Phase 164 closeout, dirty-work preservation, post-publish polling budget, and SEED-006 remain excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow dispatch input → protected authority | Only a validated nonempty exact digest may select merge/release authority. |
| protected merge/release → proposal-only reporting | A completed authority path must not be reclassified by an inapplicable open-proposal query. |
| ordinary trigger → proposal result artifact | Existing bounded evidence and non-pass behavior must remain intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-37 | Elevation of Privilege | trigger classification | critical | mitigate | Preserve exact protected validation and assert ordinary triggers cannot invoke merge, tag, release, publish, or protected dispatch. |
| T-162-38 | Tampering / Repudiation | post-merge result | high | mitigate | Make protected and proposal-only tails mutually exclusive and execute a one-proposal → merge/release → zero-proposal regression. |
| T-162-39 | Denial of Service | final proposal gate | high | mitigate | Exclude the protected lifecycle from an inapplicable proposal-only failure gate while retaining all ordinary non-pass exits. |
| T-162-40 | Spoofing | candidate digest | high | mitigate | Retain exact 64-hex digest, proposal head/source, required-check, content-digest, and merge-tree validation before protected operations. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|---|---|---|
| V2 Authentication | Applicable | Existing GitHub token boundaries remain unchanged; fixture uses no credential or permission broadening. |
| V4 Access Control | Applicable | Exact candidate-digest validation remains the sole authority crossing and ordinary paths retain negative authority assertions. |
| V5 Input Validation | Applicable | The lifecycle fixture distinguishes empty from exact nonempty digest and retains exact identity/digest validation. |
| V6 Cryptography | Applicable | Existing SHA-256 content digest comparison is unchanged and remains required before protected merge/release. |
</threat_model>

<verification>
Run the focused release-trigger suite, then run `mix test test/scripts/phase_162_release_reconciliation_test.exs test/scripts/release_trigger_recovery_test.exs --seed 0`. Inspect the new command log assertion to confirm protected merge/release precede the zero-proposal state and no proposal-only tail step runs afterward.
</verification>

<success_criteria>
- AUTO-03's protected post-merge zero-proposal blocker is closed by an executable lifecycle regression.
- D-04 authority and D-06 ordinary bounded-result behavior remain unchanged.
- No secondary warning, release mutation, API surface, dependency, schema, or workflow-topology work enters scope.
</success_criteria>

<output>
Create `.planning/phases/162-protected-release-and-scheduled-control-recovery/162-10-SUMMARY.md` when done.
</output>
