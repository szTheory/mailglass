---
phase: 68-realistic-b2b-saas-fixtures
plan: "02"
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex
  - reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex
  - reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex
  - reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "Preview surfaces expose deterministic invite/auth, billing, and operational scenarios with realistic B2B SaaS Ops copy."
    - "Later browser evidence can identify preview scenarios from public scenario keys and message content without relying on DOM internals."
  artifacts:
    - path: reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex
      provides: "Deterministic account-invite and magic-link preview scenarios."
    - path: reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex
      provides: "Deterministic receipt and payment-failure preview scenarios."
    - path: reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex
      provides: "Deterministic usage-alert and incident-update preview scenarios."
    - path: reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs
      provides: "Message-level contract coverage for all six preview scenarios."
  key_links:
    - from: reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex
      to: lib/mailglass/message.ex
      via: "Mailglass.Message.put_function/2 and `swoosh_email` subject/body fields"
      pattern: "Message\\.put_function"
    - from: reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs
      to: test/mailglass/demo_data_test.exs
      via: "root wrapper wildcard"
      pattern: "test/mailglass_demo/\\*\\.exs"
---

<objective>
Enrich the demo preview mailers and add public-seam scenario tests for DATA-04.

Purpose: make the six preview scenarios realistic enough for later browser evidence while keeping assertions on public `Mailglass.Message` behavior instead of DOM shape.
Output: deeper `preview_props` data and message copy in the three demo mailers plus a dedicated preview-scenario contract test.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-CONTEXT.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-RESEARCH.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-PATTERNS.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-VALIDATION.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-01-PLAN.md
@reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex
@reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex
@reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex
@lib/mailglass/message.ex
@test/mailglass/message_test.exs

<interfaces>
From the existing demo mailers:

```elixir
def preview_props do
  [
    invite_admin: %{...},
    magic_link: %{...}
  ]
end
```

```elixir
def preview_props do
  [
    receipt_paid: %{...},
    payment_failed: %{...}
  ]
end
```

```elixir
def preview_props do
  [
    usage_alert: %{...},
    incident_update: %{...}
  ]
end
```

From `lib/mailglass/message.ex` and `test/mailglass/message_test.exs`:

```elixir
defstruct [:swoosh_email, :stream, :mailable, :mailable_function, ...]
def html_body(t(), String.t()) :: t()
def text_body(t(), String.t()) :: t()
def put_function(t(), atom()) :: t()
```

Public fields already asserted elsewhere:

```elixir
updated.swoosh_email.subject
updated.swoosh_email.html_body
updated.swoosh_email.text_body
msg.mailable_function
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen the six preview scenarios with concrete B2B SaaS Ops props and copy</name>
  <read_first>
reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex
reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex
reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex
lib/mailglass/message.ex
test/mailglass/message_test.exs
.planning/phases/68-realistic-b2b-saas-fixtures/68-CONTEXT.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-RESEARCH.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-PATTERNS.md
  </read_first>
  <files>reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex, reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex, reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex</files>
  <action>Follow the exact mailer-component analogs from `68-PATTERNS.md` for each modified file: `reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex` keeps the existing `use Mailglass.Mailable` + `Message` pipeline and deterministic `preview_props/0` list order, `reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex` keeps the operational-stream preview family and `Message.put_function/2` tagging pattern, and `reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex` keeps the two-scenario preview family and B2B operational-copy pipeline. Enrich the existing mailers per D-08, D-09, and D-10 without changing module names, scenario family names, or public `Mailglass.Message` pipelines. Keep `AccountMailer.preview_props/0` ordered as `invite_admin`, `magic_link`, and add exact deterministic fields `requested_by: "Chrome on macOS"` and `requested_at: "2026-06-01 14:48 UTC"` to `magic_link`; keep `BillingMailer.preview_props/0` ordered as `receipt_paid`, `payment_failed`, and add exact fields `billing_period: "May 2026"` and `plan: "Scale"` to `receipt_paid` plus `amount_due: "$248.00"`, `card_last4: "4242"`, and `retry_at: "2026-06-02 09:00 ET"` to `payment_failed`; keep `OperationsMailer.preview_props/0` ordered as `usage_alert`, `incident_update`, and add exact fields `projected_overage: "$38.00"` to `usage_alert` plus `impacted_feature: "Inbound routing trace"` and `next_update: "15 minutes"` to `incident_update`. Update the HTML and text bodies so those exact values appear in public message content, while keeping the subjects exactly `Sam Rivera invited you to Northstar Ops`, `Your Northstar Ops sign-in link`, `Receipt INV-2026-0601 for Northstar Ops`, `Payment action needed for Northstar Ops`, `Northstar Ops email usage reached 85%`, and `INC-4421 is monitoring`. Keep copy calm, concrete, and operational; do not introduce marketing language or private MailglassAdmin assumptions.</action>
  <verify>
    <automated>mix test test/mailglass/demo_data_test.exs</automated>
  </verify>
  <acceptance_criteria>
- The three mailers preserve the existing six scenario keys and the exact key order described in the action.
- `magic_link`, `receipt_paid`, `payment_failed`, `usage_alert`, and `incident_update` now expose the exact deterministic fields named in the action.
- Source assertions show the six exact subject strings named in the action.
- Public message bodies include the new scenario-identifying tokens (`Chrome on macOS`, `2026-06-01 14:48 UTC`, `May 2026`, `Scale`, `$248.00`, `4242`, `$38.00`, `Inbound routing trace`, `15 minutes`).
  </acceptance_criteria>
  <done>The demo preview scenarios are realistic, deterministic, and still built only through public `Mailglass.Mailable` and `Mailglass.Message` seams.</done>
</task>

<task type="auto">
  <name>Task 2: Add message-level preview scenario contract coverage</name>
  <read_first>
reference/demo_app/lib/mailglass_demo_web/mailers/account_mailer.ex
reference/demo_app/lib/mailglass_demo_web/mailers/billing_mailer.ex
reference/demo_app/lib/mailglass_demo_web/mailers/operations_mailer.ex
lib/mailglass/message.ex
test/mailglass/message_test.exs
test/mailglass/demo_data_test.exs
.planning/phases/68-realistic-b2b-saas-fixtures/68-CONTEXT.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-PATTERNS.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-VALIDATION.md
  </read_first>
  <files>reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs</files>
  <action>Create `reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs` using the role-match analog `reference/demo_app/test/mailglass_demo_web/page_controller_security_test.exs` from `68-PATTERNS.md`: keep one focused behavior per test, deterministic assertions, and no helper-heavy DOM coupling. For the modified file itself, mirror the assertion boundary implied by the exact mailer analogs in `68-PATTERNS.md` by checking only `preview_props/0`, `message.mailable_function`, and public `message.swoosh_email` fields. Create `reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs` as plain `ExUnit.Case` coverage for DATA-04 per D-09, D-14, D-15, and D-16. Add focused tests that assert each mailer’s `preview_props/0` returns the exact scenario key order and deterministic prop values from Task 1, then call `invite_admin/1`, `magic_link/1`, `receipt_paid/1`, `payment_failed/1`, `usage_alert/1`, and `incident_update/1` with those props and assert the public message fields: `message.mailable_function` equals the scenario atom, `message.swoosh_email.subject` equals the exact subject string, and `message.swoosh_email.html_body` and `message.swoosh_email.text_body` include the scenario-identifying tokens from Task 1. Keep the assertions at public message-field level only; do not inspect preview LiveView HTML, selector shape, or MailglassAdmin internals. The new file must live under `reference/demo_app/test/mailglass_demo/` so the root wrapper from Plan 01 automatically picks it up via `test/mailglass_demo/*.exs`.</action>
  <verify>
    <automated>mix test test/mailglass/demo_data_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs` exists and covers all six scenario functions.
- The test file asserts exact `preview_props/0` key order for account, billing, and operations mailers.
- The test file asserts `message.mailable_function`, `message.swoosh_email.subject`, `message.swoosh_email.html_body`, and `message.swoosh_email.text_body` for each scenario.
- `mix test test/mailglass/demo_data_test.exs` exits `0` and includes the new preview test through the wildcard wrapper.
  </acceptance_criteria>
  <done>Preview scenario coverage fails closed on scenario drift while staying within the public Mailglass message contract and out of DOM-shape territory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo mailer modules -> preview and browser-evidence surfaces | Scenario props and copy become visible in preview UI and later automation artifacts. |
| public message fields -> verification claims | Tests can over-claim stability if they assert beyond `Mailglass.Message` public fields. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Blocked By | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|------------|-----------------|
| T-68-05 | low | Information Disclosure | Demo mailer preview props and message copy | mitigate | Task 1 synthetic deterministic props plus Task 2 message-level tests | Keep all recipients, invoice IDs, incident IDs, and workspace details synthetic and deterministic; forbid live customer data or secrets in preview props. |
| T-68-06 | medium | Repudiation | Preview scenario tests | mitigate | Task 2 public-message-field-only assertions plus D-16 scope guard | Assert only public `Mailglass.Message` fields and exact scenario keys so the proof is honest about what it covers; explicitly avoid DOM-shape or admin-internal assertions per D-16. |
| T-68-07 | medium | Tampering | Mailer function tagging and scenario order | mitigate | Task 1 fixed key order/subjects plus Task 2 exact order/content assertions | Pin the six exact scenario atoms and key order in tests so later refactors cannot silently rename, reorder, or flatten preview stories. |

High-severity threats: none. Rationale: the phase only enriches synthetic preview content and public-message tests inside the demo app; it introduces no new auth boundary, no external data ingestion path, and no secret-bearing workflow.
</threat_model>

<verification>
- Repo-root canonical quick gate: `mix test test/mailglass/demo_data_test.exs`
- Repo-root wave gate: `mix test`
- Demo-app targeted fallback: `cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/*.exs --warnings-as-errors`
</verification>

<success_criteria>
- All six preview scenarios have deterministic props and realistic B2B SaaS Ops copy.
- `reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs` locks scenario key order and public message content.
- `mix test test/mailglass/demo_data_test.exs` and `mix test` both pass.
</success_criteria>

<output>
Create `.planning/phases/68-realistic-b2b-saas-fixtures/68-02-SUMMARY.md` when done
</output>
