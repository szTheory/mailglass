---
phase: 68-realistic-b2b-saas-fixtures
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/lib/mailglass_demo/demo_data.ex
  - reference/demo_app/test/mailglass_demo/demo_data_reset_test.exs
  - test/mailglass/demo_data_test.exs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Maintainer can run `mix demo.reset` and get the same named Northstar outbound and inbound corpus on every run."
    - "Outbound operator surfaces have deterministic invite, auth, billing, alert, suppression, webhook, and replayable evidence rows."
    - "Inbound operator surfaces have deterministic accept, bounce, reject, no-match, and replay lineage rows that preserve stored-truth semantics."
  artifacts:
    - path: reference/demo_app/lib/mailglass_demo/demo_data.ex
      provides: "Deterministic Northstar fixture corpus for outbound, inbound, suppression, webhook, and replay stories."
    - path: reference/demo_app/test/mailglass_demo/demo_data_reset_test.exs
      provides: "Named fixture contract assertions for reset determinism and scenario completeness."
    - path: test/mailglass/demo_data_test.exs
      provides: "Repo-root quick validation wrapper for Phase 68 demo data."
  key_links:
    - from: reference/demo_app/mix.exs
      to: reference/demo_app/priv/repo/seeds.exs
      via: "\"demo.reset\" alias"
      pattern: "\"demo.reset\": \\[\"run priv/repo/seeds.exs\"\\]"
    - from: reference/demo_app/priv/repo/seeds.exs
      to: reference/demo_app/lib/mailglass_demo/demo_data.ex
      via: "MailglassDemo.DemoData.reset!/0"
      pattern: "MailglassDemo\\.DemoData\\.reset!"
    - from: test/mailglass/demo_data_test.exs
      to: reference/demo_app/test/mailglass_demo
      via: "System.cmd nested Mix test invocation"
      pattern: "mix test test/mailglass_demo/\\*\\.exs --warnings-as-errors"
---

<objective>
Deliver the deterministic Northstar fixture corpus and repo-root quick validation seam for Phase 68.

Purpose: satisfy DATA-01 through DATA-03 with a resettable demo corpus that later dashboard and browser-evidence phases can trust without adding new Mailglass public API.
Output: expanded `MailglassDemo.DemoData` scenario set, stronger reset contract assertions, and a root `mix test test/mailglass/demo_data_test.exs` quick gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-CONTEXT.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-RESEARCH.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-PATTERNS.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-VALIDATION.md
@.planning/phases/67-demo-app-foundation/67-03-SUMMARY.md
@reference/demo_app/lib/mailglass_demo/demo_data.ex
@reference/demo_app/test/mailglass_demo/demo_data_reset_test.exs
@reference/demo_app/priv/repo/seeds.exs
@reference/demo_app/mix.exs
@test/reference_host/compile_smoke_test.exs

<interfaces>
From `reference/demo_app/lib/mailglass_demo/demo_data.ex`:

```elixir
def tenant_id, do: @tenant

def reset! do
  truncate!()
  seed_outbound!()
  seed_inbound!()
  :ok
end
```

From `mailglass_inbound/lib/mailglass_inbound/inbound_records.ex`:

```elixir
insert_inbound_record(map(), keyword()) :: {:ok, InboundRecord.t()} | {:error, Ecto.Changeset.t()}
insert_inbound_evidence(map(), keyword()) :: {:ok, InboundEvidence.t()} | {:error, Ecto.Changeset.t()}
insert_execution_run(map(), keyword()) :: {:ok, ExecutionRun.t()} | {:error, Ecto.Changeset.t()}
```

From `mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex`:

```elixir
@sources [:fresh, :replay]
@outcomes [:no_match, :accept, :ignore, :reject, :bounce, :failed]
```

From `lib/mailglass/outbound/delivery.ex` and `lib/mailglass/suppression/entry.ex`:

```elixir
@status_values [:queued, :sent, :dispatched, :failed, :suppressed]
@streams [:transactional, :operational, :bulk]
@reasons [:hard_bounce, :complaint, :unsubscribe, :manual, :policy, :invalid_recipient]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the deterministic outbound and inbound scenario corpus</name>
  <read_first>
reference/demo_app/lib/mailglass_demo/demo_data.ex
reference/demo_app/priv/repo/seeds.exs
reference/demo_app/mix.exs
.planning/phases/68-realistic-b2b-saas-fixtures/68-CONTEXT.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-RESEARCH.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-PATTERNS.md
  </read_first>
  <files>reference/demo_app/lib/mailglass_demo/demo_data.ex</files>
  <action>Deepen `MailglassDemo.DemoData` in place per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-11, D-12, and D-13. Keep `reset!/0` as `truncate! -> seed_outbound! -> seed_inbound!` and keep the `TRUNCATE TABLE ... RESTART IDENTITY CASCADE` list scoped to the existing seven demo Mailglass tables only. Seed exactly six outbound stories with fixed `provider_message_id` values `pm-demo-invite-001`, `pm-demo-magic-link-001`, `pm-demo-receipt-001`, `pm-demo-payment-failed-001`, `sg-demo-usage-001`, and `sg-demo-incident-001`; use `mailable` values tied to `AccountMailer.invite_admin`, `AccountMailer.magic_link`, `BillingMailer.receipt_paid`, `BillingMailer.payment_failed`, `OperationsMailer.usage_alert`, and `OperationsMailer.incident_update`; create exactly two `WebhookEvent` rows with `provider_event_id` values `demo-receipt-delivery` and `demo-usage-bounce`; keep one suppression-linked incident story with `status: :suppressed` and `last_event_type: :suppressed`; and keep all addresses synthetic under `*.example` or `*.local`. Seed exactly four inbound records with fixed `provider_message_id` values `mg-demo-support-001`, `mg-demo-refund-001`, `pm-demo-spam-001`, and `pm-inbound-demo-nomatch-001`; preserve stored-truth semantics by inserting one record/evidence pair per story through `InboundRecords.insert_inbound_record/2`, `insert_inbound_evidence/2`, and `insert_execution_run/2`; and produce the exact execution matrix `support -> {:fresh, :accept} + {:replay, :accept}`, `refund -> {:fresh, :bounce, "mailbox_full"} + {:replay, :bounce, "mailbox_full"}`, `spam -> {:fresh, :reject, "spam"}`, `no_match -> {:fresh, :no_match, nil}`. Put scenario identity in delivery metadata, suppression metadata, and inbound headers or metadata using exact scenario names `invite_admin`, `magic_link`, `receipt_paid`, `payment_failed`, `usage_alert`, `incident_update`, `support_reply`, `refund_request`, `spam_reject`, and `inbound_no_match`. Do not add new public Mailglass APIs, provider breadth work, schema rebuild flows, or DOM-facing evidence logic.</action>
  <verify>
    <automated>cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/demo_data_reset_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/lib/mailglass_demo/demo_data.ex` contains the ten exact scenario identifiers named in the action.
- Source assertions show the six outbound `provider_message_id` values and four inbound `provider_message_id` values exactly as specified.
- Source assertions show `demo-receipt-delivery` and `demo-usage-bounce` as the only seeded webhook `provider_event_id` values.
- `TRUNCATE TABLE` remains limited to `mailglass_inbound_replay_runs`, `mailglass_inbound_evidence`, `mailglass_inbound_records`, `mailglass_webhook_events`, `mailglass_events`, `mailglass_suppressions`, and `mailglass_deliveries`.
- The inbound execution seed path still routes through `InboundRecords.insert_execution_run/2` and includes explicit `:fresh` and `:replay` rows instead of duplicate inbound-record inserts.
  </acceptance_criteria>
  <done>The demo seed path produces a richer deterministic corpus without changing the reset entrypoint, public Mailglass API surface, or replay semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Lock Phase 68 data determinism with named scenario assertions and a repo-root quick gate</name>
  <read_first>
reference/demo_app/test/mailglass_demo/demo_data_reset_test.exs
reference/demo_app/lib/mailglass_demo/demo_data.ex
test/reference_host/compile_smoke_test.exs
.planning/phases/68-realistic-b2b-saas-fixtures/68-CONTEXT.md
.planning/phases/68-realistic-b2b-saas-fixtures/68-VALIDATION.md
  </read_first>
  <files>reference/demo_app/test/mailglass_demo/demo_data_reset_test.exs, test/mailglass/demo_data_test.exs</files>
  <action>Upgrade the reset contract test per D-14, D-15, and D-16 so it proves fixture completeness by named scenario, not counts alone. Keep `MailglassDemo.DemoDataResetTest` under the demo app and extend its snapshot helpers to assert the exact counts `deliveries: 6`, `events: 11`, `inbound: 4`, `suppressions: 1`, `inbound_evidence: 4`, and `inbound_replay_runs: 6`; assert the exact ordered outbound message-id list `pm-demo-invite-001`, `pm-demo-magic-link-001`, `pm-demo-payment-failed-001`, `pm-demo-receipt-001`, `sg-demo-incident-001`, `sg-demo-usage-001`; assert exact webhook provider-event IDs `demo-receipt-delivery` and `demo-usage-bounce`; assert the exact suppression tuple for the incident-update story; and assert an execution matrix keyed by inbound `provider_message_id`, `source`, `outcome`, and `outcome_reason` that matches the four inbound stories from Task 1. Create `test/mailglass/demo_data_test.exs` as the canonical root quick gate, modeled on `test/reference_host/compile_smoke_test.exs`, using `System.cmd/3` with `cd: @demo_app`, `stderr_to_stdout: true`, and `MIX_ENV=test` to run `mix ecto.create`, `mix ecto.migrate`, and `mix test test/mailglass_demo/*.exs --warnings-as-errors` inside `reference/demo_app`. Keep this wrapper scoped to demo-app fixture tests only, and do not add assertions against MailglassAdmin HTML, LiveView DOM, or private package module names.</action>
  <verify>
    <automated>mix test test/mailglass/demo_data_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/test/mailglass_demo/demo_data_reset_test.exs` asserts the exact counts, outbound message IDs, webhook provider-event IDs, suppression tuple, inbound message IDs, and inbound execution matrix named in the action.
- `test/mailglass/demo_data_test.exs` exists and uses `System.cmd/3` to run `reference/demo_app` tests from the repo root.
- `test/mailglass/demo_data_test.exs` targets `mix test test/mailglass_demo/*.exs --warnings-as-errors` so later Phase 68 preview tests are automatically included.
- `mix test test/mailglass/demo_data_test.exs` exits `0`.
  </acceptance_criteria>
  <done>Phase 68 has an executable root quick gate and a reset contract test that will fail closed if scenario richness, determinism, or replay semantics drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer command -> demo database | `mix demo.reset` and the root quick gate can destroy and recreate demo rows. |
| seeded synthetic corpus -> operator/preview surfaces | Demo rows become visible in operator, preview, and later browser-evidence views. |
| fixture helpers -> inbound replay ledger | Seed logic can misstate replay semantics if it inserts the wrong record/evidence/run shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | `reference/demo_app/lib/mailglass_demo/demo_data.ex` | mitigate | Keep the `TRUNCATE TABLE` list explicit and limited to the seven demo Mailglass tables; forbid schema rebuild or broad SQL resets in task acceptance criteria. |
| T-68-02 | Information Disclosure | Seeded delivery, suppression, and inbound metadata | mitigate | Use only synthetic `*.example` and `*.local` addresses, fixed tenant `northstar`, and deterministic scenario metadata; reject any real tenant/user data in tests. |
| T-68-03 | Repudiation | Inbound execution lineage | mitigate | Seed one record/evidence pair per inbound story and assert exact `:fresh`/`:replay` execution rows keyed by `provider_message_id` so replay never masquerades as a fresh receive. |
| T-68-04 | Denial of Service | `test/mailglass/demo_data_test.exs` nested Mix wrapper | mitigate | Scope the wrapper to `reference/demo_app` and `test/mailglass_demo/*.exs` only, with deterministic `mix ecto.create`/`mix ecto.migrate` prerequisites and no arbitrary shell path expansion. |
</threat_model>

<verification>
- Quick gate: `mix test test/mailglass/demo_data_test.exs`
- Wave gate: `mix test`
- Targeted fallback while triaging demo-only failures: `cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/*.exs --warnings-as-errors`
</verification>

<success_criteria>
- `mix demo.reset` still routes through `priv/repo/seeds.exs` and reseeds the same named Northstar dataset on repeated runs.
- The seeded corpus includes deterministic outbound, webhook, suppression, inbound, and replay stories matching DATA-01 through DATA-03.
- `mix test test/mailglass/demo_data_test.exs` passes from the repo root.
</success_criteria>

<output>
Create `.planning/phases/68-realistic-b2b-saas-fixtures/68-01-SUMMARY.md` when done
</output>
