---
phase: "40"
plan: "03"
type: execute
wave: 3
depends_on:
  - "40-01"
  - "40-02"
files_modified:
  - "mailglass_inbound/README.md"
  - "mailglass_inbound/docs/api_stability.md"
  - "mailglass_inbound/docs/postmark_ingress.md"
  - "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/postmark_provider_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs"
autonomous: true
requirements:
  - INGRESS-01
  - STORE-01
must_haves:
  truths:
    - "Adopters and operators can read one honest Phase 40 story: Postmark ingress and replayable storage are real now, while mailbox execution is still deferred."
    - "The package stability inventory stays narrow and does not accidentally promote internal provider or persistence seams to the stable public contract."
    - "Contract-proof tests cover parse, rejection, insert, and duplicate paths so Phase 40 claims are mechanically enforced."
  artifacts:
    - path: "mailglass_inbound/docs/postmark_ingress.md"
      provides: "Canonical adopter/operator doc for Postmark ingress setup and storage semantics"
      contains: "Postmark"
    - path: "mailglass_inbound/docs/api_stability.md"
      provides: "Updated contract inventory that keeps ingress internals internal and stable public surface narrow"
      contains: "stable"
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Automated proof that docs do not overstate execution or public-provider extensibility"
      contains: "api_stability"
  key_links:
    - from: "mailglass_inbound/docs/postmark_ingress.md"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      via: "docs show the real mount/config path and body-reader requirement"
      pattern: "body_reader|Plug"
    - from: "mailglass_inbound/docs/api_stability.md"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex"
      via: "stability inventory keeps the provider seam internal rather than public"
      pattern: "internal|stable"
    - from: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      to: "mailglass_inbound/docs/postmark_ingress.md"
      via: "docs-contract proof guards honest Phase 40 claims"
      pattern: "mailbox execution|duplicate|replay"
---

<objective>
Publish the honest Phase 40 documentation and contract-proof lane for Postmark ingress and replayable persistence.

Purpose: Deliver D-40-03, D-40-05, D-40-18, D-40-19, D-40-20, and D-40-24 by documenting the shipped ingress/storage slice accurately and proving the package claims match the implemented parse, storage, duplicate, and rejection behavior.
Output: Updated README and stability inventory, a dedicated Postmark ingress guide, and docs/contract tests that lock the Phase 40 posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-CONTEXT.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-RESEARCH.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-VALIDATION.md
@.planning/phases/39-inbound-package-foundation/39-03-PLAN.md
@mailglass_inbound/docs/api_stability.md
@mailglass_inbound/lib/mailglass_inbound/inbound_message.ex
@mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_record.ex
@mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_evidence.ex

<interfaces>
From mailglass_inbound/docs/api_stability.md:
```markdown
### `stable`
- `MailglassInbound`
- `MailglassInbound.InboundMessage`
- `MailglassInbound.Router`
- `MailglassInbound.Mailbox`
```

From mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_evidence.ex:
```elixir
field :raw_payload, :map, default: %{}, redact: true
field :raw_headers, :map, default: %{}
field :verification_facts, :map, default: %{}
field :parse_warnings, :map, default: %{}
field :attachment_blobs, :map, default: %{}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Publish honest Phase 40 Postmark ingress and stability docs</name>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/api_stability.md, mailglass_inbound/docs/postmark_ingress.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <behavior>
    - Test 1: docs describe one obvious mount/config path, including the inbound `Plug.Parsers` `:body_reader` requirement and the package-local ingress Plug per D-40-21.
    - Test 2: docs explain canonical row plus evidence row persistence, duplicate handling, and replay honesty without claiming raw MIME reconstruction or fresh-ingress replay writes per D-40-15 through D-40-18.
    - Test 3: stability docs keep provider seams, persistence modules, and route-proof internals out of the stable public contract while stating clearly that mailbox execution is deferred to Phase 41+ per D-40-03, D-40-05, and D-40-20.
  </behavior>
  <action>Update `mailglass_inbound/README.md` and `mailglass_inbound/docs/api_stability.md`, and add `mailglass_inbound/docs/postmark_ingress.md`, so the public story exactly matches the shipped Phase 40 slice. Document one recommended mount/config path for the inbound Plug and body reader, the verify-first request ordering, the normalized `%InboundMessage{}` contract, the canonical-plus-evidence storage truth, explicit duplicate/rejection outcomes, and the narrow route-compatibility proof. Keep the stable contract narrow: the adopter-facing ingress and routing surface is public, but provider internals, persistence wiring, and replay implementation details stay internal per D-40-05. State plainly that mailbox execution, async runners, and broader provider parity are not part of Phase 40 per D-40-03 and D-40-20.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The shipped docs are honest about what Phase 40 does and does not deliver, and the stability inventory does not widen the stable public contract from Phase 39.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock Phase 40 claims with parse, storage, duplicate, and rejection proof</name>
  <files>mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/postmark_provider_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs</files>
  <behavior>
    - Test 1: provider and plug proof cover successful verification/normalization plus auth/config rejection paths for `INGRESS-01`.
    - Test 2: persistence proof covers inserted and duplicate outcomes, canonical/evidence storage truth, and no fresh-ingress `ReplayRun` writes for `STORE-01`.
    - Test 3: docs-contract proof fails if docs claim mailbox execution, public provider extensibility, widened matcher semantics, or replay-as-fresh-receive behavior.
  </behavior>
  <action>Extend the existing provider, plug, persistence, and docs-contract tests into one coherent Phase 40 proof lane named by `40-VALIDATION.md`. The tests must collectively prove parse success, explicit rejection, transactional storage, duplicate collapse, and route-compatibility-only behavior. Make the docs-contract assertions strict enough that any claim of mailbox execution, public provider behaviour support, widened `%InboundMessage{}` fields, or replay disguised as a fresh receive breaks the lane immediately. Keep proof focused on the Phase 40 contract; do not pull in Phase 41 mailbox execution or Phase 42 async semantics.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/postmark_provider_test.exs test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/ingress/persist_test.exs test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The Phase 40 claims are mechanically enforced across docs and tests, with no room for accidental scope creep into execution or public-provider extensibility.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package docs -> adopters and operators | Readers will treat docs as the contract and act on configuration, storage, and replay claims. |
| contract tests -> future changes | The proof lane determines whether future edits silently widen stable surfaces or overstate Phase 40 behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Repudiation | docs and stability inventory | mitigate | Make docs explicitly state that Phase 40 delivers ingress plus replayable storage only, with mailbox execution deferred, per D-40-03 and D-40-20. |
| T-40-08 | Tampering | docs-contract and integration proof lanes | mitigate | Add strict tests that fail when docs or proofs widen `%InboundMessage{}`, expose internal provider seams as public, or claim replay/matcher/execution behavior outside the Phase 40 contract per D-40-05 and D-40-24. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/postmark_provider_test.exs test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/ingress/persist_test.exs test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
</verification>

<success_criteria>
Phase 40 ships with an honest public story, a still-narrow stable contract, and automated proof that parse, storage, duplicate, and rejection behavior match the documented ingress-and-replayable-persistence slice.
</success_criteria>

<output>
After completion, create `.planning/phases/40-postmark-ingress-and-replayable-persistence/40-03-SUMMARY.md`
</output>
