---
phase: 119-app-shell-nav-overview-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/e2e/operator.spec.js
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]

must_haves:
  truths:
    - "On the Overview route the sidebar marks Overview active (aria-current=page) and Deliveries inactive (attribute omitted)."
    - "An always-visible Overview nav_link (sidebar) and nav_pill (mobile) point at the bare operator root with no ?view=."
    - "The operator-overview-nav 'Navigate' card block is gone from the Overview branch."
    - "Recent failures and Active suppressions stat cards are wrapped in drill-through links to Deliveries filtered by status=failed / status=suppressed; the wrap preserves tenant scope."
    - "The orientation strip renders only in the all-clear/empty Overview state and is suppressed when health needs attention; the gate never raises on a nil support_summary."
    - "The Overview subtitle is a triage line (attention vs all-clear), never 'Oops' and never 'Navigate to <surface>'."
  artifacts:
    - "mailglass_admin/lib/mailglass_admin/operator/shell.ex (active enum extended to [:overview,:deliveries,:inbound]; Overview nav_link + nav_pill; surface_paths returns :overview)"
    - "mailglass_admin/lib/mailglass_admin/operator_live.ex (active={@view}; subtitle predicate; deleted nav block; drill-through link wrappers; empty-pane-only orientation gate)"
    - "mailglass_admin/e2e/operator.spec.js (Wave-0 drill-through + orientation-strip assertion scaffolds)"
  key_links:
    - "operator_live.ex render passes active={@view} into Shell.shell — the single SHELL-01 seam (one caller, all surfaces)."
    - "Overview health stat cards → build_path/4 filtered Deliveries URLs (status=failed / status=suppressed) preserving @filter_params tenant_id."
    - "surface_paths/4 returns :overview (bare root) threaded into shell as @overview_path for both nav_link and nav_pill."
---

<objective>
Land the SHELL-01/02/03 code changes for the keystone Overview/shell/nav redesign in a single
tightly-coupled plan (all three requirements touch `shell.ex` + `operator_live.ex`). This is a
**surgical correction**, not a rebuild: extend one enum, clone two existing nav instances, swap one
literal, delete one redundant block, wrap two presentational cards in existing-style links, gate one
strip render, and rewrite one subtitle.

Purpose: Convert the #1-pain operator landing from a homepage-that-signposts-elsewhere into a real
triage destination with correct location awareness and zero redundant chrome — the cleaned-up IA /
component / microcopy / motion pattern set every later v1.14 surface inherits.

Output: Edited `shell.ex`, `operator_live.ex`, and Wave-0 test scaffolds in `operator.spec.js`. The
paired gate flips/fixes (D-09) land in 119-02 (Wave 2) once this code exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/119-app-shell-nav-overview-redesign/119-CONTEXT.md
@.planning/phases/119-app-shell-nav-overview-redesign/119-RESEARCH.md
@.planning/phases/119-app-shell-nav-overview-redesign/119-UI-SPEC.md
@.planning/phases/119-app-shell-nav-overview-redesign/119-PATTERNS.md

@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/operator/support_cards.ex
@mailglass_admin/lib/mailglass_admin/components.ex
</context>

<artifacts_this_phase_produces>
New / changed symbols introduced by this plan (downstream consumers and 119-02 depend on these exact names):

- `shell.ex` `attr(:active, ...)` enum value `:overview` (added; enum becomes `[:overview, :deliveries, :inbound]`).
- `shell.ex` `attr(:overview_path, :string, required: true)` — new shell attr (bare operator root href).
- `shell.ex` `surface_paths/4` return key `:overview` — the bare root path (`root <> query`).
- `shell.ex` new sidebar `Components.nav_link` instance — `label="Overview"`, `icon="hero-chart-bar"`, `href={@overview_path}`, `active={@active == :overview}` (no `:if` gate; first child of the sidebar `<nav>`).
- `shell.ex` new mobile `Components.nav_pill` instance — `label="Overview"`, `href={@overview_path}`, `active={@active == :overview}` (no `:if` gate; first child of the mobile `<nav>`).
- `operator_live.ex` `active={@view}` at the shell render (replaces literal `active={:deliveries}`).
- `operator_live.ex` `overview_path={@overview_path}` shell attr pass-through + `@overview_path` assign (from `surface_paths` `:overview`).
- `operator_live.ex` drill-through `<.link patch={...}>` wrappers around the "Recent failures" and "Active suppressions" stat cards (with `aria-label`).
- `operator_live.ex` triage subtitle `cond` (attention / all-clear lines).
- `operator_live.ex` null-guarded empty-pane orientation gate: `@support_summary && all_clear?(@support_summary) && @suppression_count in [0, nil]`.
- `operator.spec.js` Wave-0 assertions: drill-through link hrefs (`status=failed`, `status=suppressed`) + orientation-strip presence keyed to all-clear vs attention.

Removed: the `data-testid="operator-overview-nav"` block (the two "Navigate" cards) and the signpost subtitle.
</artifacts_this_phase_produces>

<icon_and_asset_decision>
**Overview nav icon — use `hero-chart-bar` (no rebuild).** Verified facts (do not rediscover mid-flight):
- `hero-squares-2x2` and `hero-home` are **NOT embedded** in `mailglass_admin/assets/vendor/heroicons-inline.js`. Using them renders a blank icon with no compile/test error (the heroicons-inline landmine).
- `hero-chart-bar` IS embedded (`heroicons-inline.js:28`) and reads as "at-a-glance metrics/health" — semantically apt for a triage Overview and satisfies the UI-SPEC's argument against `hero-home`.
- This avoids the embed + `mix assets.build` + TokenParityTest landmine (D-12) entirely.

**No asset rebuild in this plan.** Every Tailwind class used is already in the committed `priv/static/app.css`:
`block`, `rounded-box`, `mg-focus-ring`, `hover:border-primary` (confirmed present), `transition-colors`,
`ease-out`, `duration-(--duration-fast)`, `.motion-reveal`. **Introduce no new utility class.** If, against
this guidance, any genuinely new class is introduced, STOP and follow the D-12 rebuild protocol
(run `mix test test/mailglass_admin/token_parity_test.exs` BEFORE committing any rebuilt bundle —
a fresh build emits raw-inline daisyUI theme blocks that break TokenParityTest).
</icon_and_asset_decision>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Scaffold the missing e2e assertions in operator.spec.js</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <action>
Add the two MISSING Wave-0 e2e assertions the phase needs (RESEARCH "Wave 0 Gaps"), WITHOUT yet
removing the VERIF-02 `operator-overview-nav` assertion (that rewrite is 119-02 Task 1, paired with
the gate flips). These scaffolds assert the end-state the Task 1/2 code will satisfy.

1. In the existing "operator overview landing" test (currently `operator.spec.js:352`, tenant-scoped
   Overview already navigated), add assertions that the failures and suppressions stat cards are
   drill-through links into filtered Deliveries: locate the failures card
   (`data-testid="operator-overview-health-failures"`) and assert its enclosing `<a>` has an `href`
   containing `status=failed`; same for `operator-overview-health-suppressions` containing
   `status=suppressed`. Use `page.getByTestId(...).locator("xpath=ancestor::a[1]")` (or a wrapping
   `[data-testid="operator-overview-health-failures-link"]` if Task 2 adds one — prefer asserting the
   href on the link wrapper Task 2 creates). Keep the assertion resilient: match `status=failed`
   substring, not exact full URL.
2. Add a second focused test (or extend a state-specific test) asserting the orientation strip is
   empty-pane-only: on an all-clear / zero-data tenant the orientation strip is visible; on an
   attention tenant (non-zero failures) it is absent. Reference the orientation strip by a stable
   selector — if none exists, Task 2 must add `data-testid="operator-overview-orientation"` to the
   conditional strip wrapper; assert that testid `toBeVisible()` in the all-clear cell and
   `toHaveCount(0)` in the attention cell. Use the existing persona/tenant fixtures the operator
   browser suite already seeds (do not invent new seed data).

Do NOT touch line 367's `operator-overview-nav` assertion in this task — leaving it intact keeps the
suite internally consistent until 119-02 deletes both the block and the assertion together. These new
scaffolds will themselves go red until Task 2 ships; that is the intended RED state for the Wave-1
code to satisfy.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -n 'status=failed' e2e/operator.spec.js && grep -n 'status=suppressed' e2e/operator.spec.js && grep -n 'operator-overview-orientation' e2e/operator.spec.js</automated>
  </verify>
  <done>operator.spec.js contains drill-through href assertions for status=failed and status=suppressed, plus an orientation-strip presence assertion keyed to all-clear vs attention. The existing line-367 operator-overview-nav assertion is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (SHELL-01): Nav active-state fix + Overview nav identity</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <behavior>
    - shell_test: rendering the shell with active={:overview} marks the Overview nav_link with aria-current="page" and omits aria-current on Deliveries/Inbound (per components_test.exs:260 omission semantics).
    - shell_test: rendering with active={:deliveries} marks Deliveries active and the new Overview link inactive (no regression to the existing case).
    - shell_test: the Overview nav_link and nav_pill href equals @overview_path (bare root, no view= param), and the Overview items render unconditionally (not gated by inbound_available?).
    - components_test: nav_link enabled branch emits aria-current; inactive omits it (existing test — must stay green).
  </behavior>
  <action>
Implement D-01/D-02/D-03/D-08 (SHELL-01). Per PATTERNS Patterns 1, 2, 3 and Shared Patterns A & C —
clone existing in-file analogs; do NOT rewrite the shell active logic (it is already correct at
`shell.ex` `@active == :deliveries` and `components.ex` `aria-current={@active && "page"}`).

In `shell.ex`:
1. Extend the active attr enum (currently `attr(:active, :atom, values: [:deliveries, :inbound], required: true)`)
   to `values: [:overview, :deliveries, :inbound]` (add `:overview` first). Analog: the sibling 3-value
   `values:` enum already in the module.
2. Add `attr(:overview_path, :string, required: true)` alongside the existing `:deliveries_path` /
   `:inbound_path` attrs.
3. Extend `surface_paths/4` to also return `overview: root <> query` in its map (the bare operator
   root — `root <> query` is already computed for the deliveries/inbound keys; just add the `:overview`
   key returning the unqualified root).
4. In the sidebar `<nav aria-label="Operator sections">`, add an Overview `Components.nav_link` as the
   FIRST child (above Deliveries): `label="Overview"`, `icon="hero-chart-bar"`, `href={@overview_path}`,
   `active={@active == :overview}`. OMIT the `:if={@inbound_available?}` gate — Overview is always shown.
5. In the mobile `<nav class="... md:hidden">`, add an Overview `Components.nav_pill` as the FIRST child
   (above Deliveries): `label="Overview"`, `href={@overview_path}`, `active={@active == :overview}`.
   `nav_pill` has NO `icon` attr (confirmed) — do not add one. Omit the `:if` gate.

In `operator_live.ex`:
6. Replace the literal `active={:deliveries}` at the shell render (currently the single `active={...}`
   call site, ~`:349`) with `active={@view}`. `@view` already resolves to `:overview`/`:deliveries`/`:inbound`
   and already drives the title — this single swap fixes the false-active bug for ALL surfaces (overview,
   deliveries, AND inbound; there is one shell render, not a separate inbound caller).
7. Add `overview_path={@overview_path}` to the shell render attrs (next to `deliveries_path`/`inbound_path`).
8. Thread `@overview_path` into assigns. Both `surface_paths/4` call sites (the render-time assign at
   ~`:333` and the `assign_overview_state` path at ~`:828`) currently pass a hardcoded `:deliveries`
   active and assign `paths.deliveries`/`paths.inbound`; add `overview_path: paths.overview` (render
   site, in the `assign(assigns, ...)` map) and `|> assign(:overview_path, paths.overview)` (the
   socket-pipe site). Confirm `surface_paths` is called identically at both sites.

Active state stays non-color-alone (existing accent left-border + font-bold weight on the active
nav_link/nav_pill) and holds in light/dark/system — no CSS change.

Extend `test/mailglass_admin/operator/shell_test.exs` to cover the `:overview` active resolution
(aria-current on Overview, omitted on Deliveries) and the always-shown Overview nav items — clone the
existing `aria-current nav resolution` describe-block assertions.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs test/mailglass_admin/components_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Overview nav_link + nav_pill render unconditionally pointing at @overview_path; active={@view} replaces the literal; surface_paths returns :overview; shell_test covers :overview active resolution and passes. components_test stays green (aria-current omission semantics intact).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (SHELL-02): Overview triage — delete Navigate block, drill-through health, empty-pane orientation</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <behavior>
    - operator_live_test (overview render): the rendered Overview HTML does NOT contain data-testid="operator-overview-nav" (block deleted).
    - operator_live_test: the "Recent failures" stat card is wrapped in a link whose patch contains status=failed; "Active suppressions" wrapped in a link whose patch contains status=suppressed; both preserve the tenant_id from @filter_params.
    - operator_live_test: "Orphan backlog" keeps the existing support_cards focus drilldown (no new status= filter link); "Overall status" has no link wrapper.
    - operator_live_test: with a nil @support_summary the overview render does NOT raise (the empty-pane orientation gate is null-guarded) and the orientation strip is suppressed (treated as attention/unavailable).
    - operator_live_test: in the all-clear state (support_summary all-zero AND suppression_count in [0,nil]) the orientation strip renders (data-testid="operator-overview-orientation"); in the attention state it is absent.
  </behavior>
  <action>
Implement D-04/D-05/D-06/D-07 (SHELL-02). Per PATTERNS Patterns 4, 5, 6 — wrap existing primitives in
existing-style links; never mutate `stat_card`.

0. PRE-CHECK the no-rebuild asset assumption BEFORE editing (defuses the D-12 landmine up-front rather
   than discovering it via TokenParityTest after): `cd mailglass_admin && grep -q 'hover:border-primary' priv/static/app.css`.
   If present (expected — the tenant_selector wrap at `shell.ex` already uses it), proceed with no rebuild.
   If ABSENT, `hover:border-primary` is a new variant → follow the D-12 protocol (`mix assets.build`,
   then run TokenParityTest, commit the surviving bundle in the SAME commit) before continuing.
1. DELETE the `operator-overview-nav` block entirely (the `<div data-testid="operator-overview-nav">`
   containing the "Navigate" `<h2>` and the two "View Deliveries" / "View Inbound" cards). It duplicates
   the always-visible sidebar (D-NAV-DUP). Keep the surrounding `operator-overview` +
   `operator-overview-health` structure.
2. Wrap the "Recent failures" `Components.stat_card` in a drill-through link at the call site. Use the
   established wrap-a-block pattern (analog: the existing `<.link patch={build_path(...)}>` wrappers and
   the tenant_selector wrap with `mg-focus-ring ... hover:border-primary`):
   patch = `build_path(@base_path, Map.put(@filter_params, "view", "deliveries") |> Map.put("status", "failed"), nil, @dark_chrome)`;
   wrapper class = `"block mg-focus-ring rounded-box hover:border-primary transition-colors ease-out duration-(--duration-fast)"`;
   add `aria-label="View recent failures in Deliveries"` and a `data-testid="operator-overview-health-failures-link"`.
   The 4-arg `build_path` form is correct (the 5th `support_state` arg defaults). `:failed` is already
   a valid `@status_values` member — no filter plumbing.
3. Wrap "Active suppressions" the same way with `status` = `"suppressed"`,
   `aria-label="View active suppressions in Deliveries"`, `data-testid="operator-overview-health-suppressions-link"`.
4. Do NOT wrap "Orphan backlog" (keep the existing `support_cards.ex` `phx-value-focus="orphan_backlog"`
   focus drilldown — no Deliveries `status=` filter maps to orphans; inventing one 404s). Do NOT wrap
   "Overall status" (it is a summary readout, not actionable).
5. Do NOT embed an `<a>`/`<button>` inside `stat_card` (it stays a presentational `<article>`); the
   wrapping link is nested-interactive-clean because the card has no inner interactive element (D-06).
   Do NOT add classes to `components.ex` — the hover escalation lives on the wrapper.
6. Make the orientation strip empty-pane-only (D-07). The strip currently renders unconditionally at the
   top of the Overview branch (`<MailglassAdmin.Operator.Shell.orientation_strip surface={:deliveries} />`).
   Gate it with the NULL-SAFE predicate (analog: support_cards.ex `@support_summary && <predicate>`):
   render the strip only when `@support_summary && all_clear?(@support_summary) && @suppression_count in [0, nil]`.
   CRITICAL: `all_clear?/1` does `summary.failed_ingest.count == 0 and ...` and RAISES on nil — the
   `@support_summary &&` guard MUST come first. Treat a nil summary as attention/unavailable (strip
   suppressed). Wrap the conditional `orientation_strip` call in an OUTER element you control in
   `operator_live.ex` — `<div data-testid="operator-overview-orientation">…</div>` — so the testid lands
   on the wrapper, NOT inside the byte-frozen strip. (Required, not optional: the strip's copy is
   byte-frozen by D-10, so the testid cannot live in `shell.ex`; a green Task-0 grep against the spec
   file must not be able to mask a non-wired assertion.) The orientation strip's own copy
   (`copy_for(:deliveries)` in `shell.ex`) stays BYTE-FROZEN — only the render condition changes (D-10).
7. When health needs attention (not all-clear, or suppressions present, or nil summary), the health
   stat_card row is the primary content and the strip is absent. Keep the existing severity-ordered
   4-card grid order (failures → orphans → suppressions → overall) — it already matches severity descent;
   do NOT reorder.

Add/extend `test/mailglass_admin/operator_live_test.exs` assertions per the behavior block (render the
overview branch in all-clear, attention, and nil-summary states; assert no `operator-overview-nav`,
the two drill-through links with correct `status=` + tenant scope, and the orientation gate).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs test/mailglass_admin/token_parity_test.exs --warnings-as-errors</automated>
  </verify>
  <done>operator-overview-nav block deleted; failures/suppressions stat cards wrapped in tenant-scope-preserving build_path links (status=failed/suppressed); orphan/overall unwrapped; orientation strip empty-pane-only with a null-safe gate that never raises on nil summary; operator_live_test covers all three states; TokenParityTest green (bundle undisturbed).</done>
</task>

<task type="auto">
  <name>Task 3 (SHELL-03): Triage microcopy + motion confirmation</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <action>
Implement D-10/D-11 (SHELL-03). Per UI-SPEC Copywriting Contract + RESEARCH "Subtitle predicate".

1. Rewrite the Overview subtitle. The shell render currently sets the subtitle via an `if @view == :overview`
   that emits the signpost line "A task-oriented overview ... Navigate to Deliveries to inspect individual
   sends." Replace the overview branch with a triage `cond` (null-safe, same guard as the orientation gate):
     - `@view != :overview` → keep the existing Deliveries subtitle
       ("Prove what happened to a message — inspect its event timeline, suppression state, and replay history.").
     - `@support_summary && all_clear?(@support_summary) && @suppression_count in [0, nil]` →
       "Your delivery system is healthy."
     - true → "Your delivery system needs attention."
   Banned: "Oops" (any form) and "Navigate to <surface>" — the sidebar already navigates. No boilerplate
   ("A task-oriented overview of…").
2. Add the all-clear calm-state copy inside the Overview branch: when in the all-clear state (same
   guarded predicate), render a single `<p class="text-body text-secondary">Your delivery system is
   healthy — nothing needs your attention right now.</p>` ABOVE the (empty-pane-only) orientation strip.
   This collapses the all-clear Overview to a calm, short summary instead of a full-height info-dump
   (D-MOBILE-INFODUMP). Do NOT render this `<p>` in the attention state.
3. Confirm motion uses existing tokens ONLY (D-11): the nav active-change uses the existing
   `transition-colors ease-out duration-(--duration-fast)` already on `nav_link`; any Overview reveal
   uses the existing `.motion-reveal` token. Add NO new `@keyframes`, no `ease-in`, no height/width
   transitions, no JS animation hooks. Reduced-motion neutralization is inherited.

Keep the page `<h1>` "Operator overview" and the "Health" `<h2>` as-is (do not introduce an `<h3>`
above Health — preserve heading hierarchy). Extend `shell_test.exs` (or `operator_live_test.exs`)
to assert the subtitle picks the correct triage line per state and never contains "Oops" or
"Navigate to".
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs test/mailglass_admin/operator_live_test.exs --warnings-as-errors && test "$(grep -c '@keyframes' assets/css/app.css)" = "$(git show HEAD:mailglass_admin/assets/css/app.css | grep -c '@keyframes')"</automated>
  </verify>
  <done>Overview subtitle is a state-driven triage line (attention / all-clear), never "Oops"/"Navigate to"; all-clear renders a single calm `<p>`; no new @keyframes added to app.css; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → LiveView (URL params) | `tenant_id` / `status` / `view` query params cross here; already validated server-side |

This phase is a UI/LiveView template surgical correction — **no new attack surface**. No new auth,
input handling, route, or data boundary is introduced. The drill-through links are built with the
existing `build_path/4` helper from the already-valid closed-set `@status_values`; no user-controlled
query construction is added.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-01 | Information disclosure | drill-through `<.link>` → filtered Deliveries | low | mitigate | `build_path/4` merges existing `@filter_params` (which carries `tenant_id`); the read model re-scopes server-side. Verify the failures/suppressions links do not drop `tenant_id` (asserted in 119-01 Task 2 tests). |
| T-119-02 | Tampering / input validation | `status` drill-through param | low | accept | `:failed`/`:suppressed` are members of the closed-set `@status_values`; no free-text param is introduced. An out-of-set `status=` is rejected by the existing `normalize_enum_filter` cast. |
| T-119-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (zero new deps; RESEARCH Package Legitimacy Audit = N/A). No `[ASSUMED]`/`[SUS]` packages — no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs test/mailglass_admin/components_test.exs test/mailglass_admin/operator_live_test.exs test/mailglass_admin/token_parity_test.exs --warnings-as-errors` is green.
- No new Tailwind utility class introduced → `priv/static/app.css` is byte-unchanged (no `mix assets.build`, no bundle commit). TokenParityTest confirms the bundle is undisturbed.
- No new `@keyframes` in `assets/css/app.css` (D-11 motion lock).
- The two paired-gate edits (D-09) are intentionally deferred to 119-02 (Wave 2): the Wave-0 scaffolds in this plan go RED until Task 2 ships, then GREEN; the `operator-overview-nav` VERIF-02 assertion at `operator.spec.js:367` and the `judgment.spec.js` fixme→test flips land in 119-02 alongside the assertion fix.
</verification>

<success_criteria>
- SHELL-01: Overview route highlights Overview (aria-current=page), Deliveries inactive; `active={@view}` replaces the literal; Overview nav_link + nav_pill always visible at the bare root.
- SHELL-02: `operator-overview-nav` deleted; failures/suppressions are tenant-scope-preserving drill-through links into filtered Deliveries; orphan keeps the support_cards focus; orientation strip is empty-pane-only with a null-safe gate.
- SHELL-03: subtitle is a state-driven triage line (no "Oops"/"Navigate to"); all-clear is a calm single line; motion reuses existing tokens with zero new CSS.
</success_criteria>

<output>
Create `.planning/phases/119-app-shell-nav-overview-redesign/119-01-SUMMARY.md` when done.
</output>
