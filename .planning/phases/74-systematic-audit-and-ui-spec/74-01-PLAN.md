---
phase: 74-systematic-audit-and-ui-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/scripts/ui-audit.sh
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "scripts/ui-audit.sh captures the full 390/768/1440 x light/dark x 3-surface matrix"
    - "The capture run writes PNGs only under gitignored tmp/ui-audit/, never priv/static/"
    - "74-UI-SPEC.md is confirmed to satisfy every AUDIT-02 acceptance criterion (frozen, not re-authored)"
  artifacts:
    - path: "mailglass_admin/scripts/ui-audit.sh"
      provides: "Extended viewport-matrix capture tooling (390/768/1440 x light/dark x 3 surfaces)"
      contains: "390"
  key_links:
    - from: "mailglass_admin/scripts/ui-audit.sh"
      to: "tmp/ui-audit/"
      via: "OUT default writes screenshots to gitignored tmp"
      pattern: "tmp/ui-audit"
---

<objective>
Extend the ad-hoc visual-audit tooling (`scripts/ui-audit.sh`) to capture the full audit matrix the gap register depends on — 390/768/1440px viewports x light/dark themes x the three operator/preview surfaces — and produce the before-baseline PNG set (gitignored). Then verify the already-frozen `74-UI-SPEC.md` satisfies every AUDIT-02 acceptance criterion (it is `status: approved` and comprehensive — this task confirms, it does NOT re-author).

Purpose: The gap register (Plan 03) needs a deterministic, per-viewport screenshot matrix as its evidence source. The 390px mobile pass (Pitfall 15) is the failure mode this extension exists to prevent. AUDIT-02 verification closes the requirement without touching the frozen contract.

Output: Extended `scripts/ui-audit.sh`; a captured PNG set under `tmp/ui-audit/` (gitignored, never committed); a confirmation note that AUDIT-02 is satisfied.

CONSTRAINT (ROADMAP success criterion 5): Zero production code changes. The ONLY executable artifact touched is `scripts/ui-audit.sh` (audit/test tooling, NOT production code). No file under `mailglass_admin/lib/` changes. No file under `priv/static/` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-CONTEXT.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md

<interfaces>
<!-- Current ui-audit.sh shape. The extension must iterate viewports and themes -->
<!-- around the existing per-surface shot() calls, preserving URL-driven state. -->

From mailglass_admin/scripts/ui-audit.sh:
- `PORT` default 4015; `BASE=http://localhost:${PORT}`; `TENANT=northstar`
- `OUT="${AGENT_BROWSER_SCREENSHOT_DIR:-tmp/ui-audit}"` (gitignored by .gitignore `/tmp/`)
- `shot() { url, name }` — opens URL via `agent-browser open`, sleeps 1, then `agent-browser screenshot --full "$OUT/$name.png"`
- Existing surfaces captured by URL: `/dev/mail/` (Preview), `/ops/mail/?tenant_id=…` (Deliveries/Operator landing, light+dark), `/ops/mail/inbound?tenant_id=…` (Inbound, light+dark)
- State is URL-driven (`?tenant_id=…&theme=dark`) — viewport is set via the agent-browser CLI, not the URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ui-audit.sh for the 390/768/1440 x light/dark x 3-surface matrix</name>
  <files>mailglass_admin/scripts/ui-audit.sh</files>
  <read_first>
    - mailglass_admin/scripts/ui-audit.sh (the file being modified — current shot() helper, OUT default, surface URLs)
    - mailglass_admin/docs/design-system.md:123-150 (visual audit loop: the matrix is screen x theme x viewport x state; tmp/ui-audit gitignore contract; deep-link Known Limitations note that explains why direct deep-URL loads can be unstyled)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md "Per-Surface Acceptance Checklists" (the 390/768/1440 x light/dark dimensions the matrix must cover) and "Surfaces in Scope" table
    - .planning/research/v1.7-admin-ui-polish/PITFALLS.md (Pitfall 15 — 390px audit gap is the failure mode this task prevents)
  </read_first>
  <action>
    Modify ONLY `mailglass_admin/scripts/ui-audit.sh` (audit tooling — not production code). Generalize the capture loop so it iterates a `VIEWPORTS` list of exactly `390 768 1440` (widths, in px) and both themes (light + `theme=dark`) across the three surfaces already present: Preview (`/dev/mail/`), Operator/Deliveries landing (`/ops/mail/?tenant_id=${TENANT}`, entered via the existing `/demo/login?return_to=…` session step), and Inbound (`/ops/mail/inbound?tenant_id=${TENANT}`).

    For each (surface x viewport x theme) cell, set the browser viewport width via the agent-browser CLI before the `shot` call (set a matching height, e.g. 900, sufficient for `--full` page capture), and name the PNG deterministically as `{surface}-{viewport}-{theme}.png` (e.g. `deliveries-390-dark.png`, `inbound-768-light.png`, `preview-1440-light.png`). Keep the existing URL-driven state approach (do NOT drive clicks). Keep `OUT="${AGENT_BROWSER_SCREENSHOT_DIR:-tmp/ui-audit}"` so output stays gitignored — NEVER change OUT to anything under `priv/static/` (would trip the bundle gate).

    Preserve the existing preconditions comment block (demo app booted on $PORT, seeded, Postgres reachable). Add the viewport/theme dimensions to the header comment. Add a short note that the matrix is the before-baseline source for `74-GAP-REGISTER.md` and that PNGs stay gitignored per D-06. Do not promote any of this to CI (non-deterministic pixels — local/ad-hoc only, per D-07). Use only POSIX/bash constructs already idiomatic to the script (it is `set -euo pipefail`). No fenced implementation here — the file edit is the implementation.
  </action>
  <verify>
    <automated>bash -n mailglass_admin/scripts/ui-audit.sh && grep -q '390' mailglass_admin/scripts/ui-audit.sh && grep -q '768' mailglass_admin/scripts/ui-audit.sh && grep -q '1440' mailglass_admin/scripts/ui-audit.sh && grep -q 'tmp/ui-audit' mailglass_admin/scripts/ui-audit.sh && ! grep -q 'priv/static' mailglass_admin/scripts/ui-audit.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n mailglass_admin/scripts/ui-audit.sh` exits 0 (script is syntactically valid)
    - `grep '390' mailglass_admin/scripts/ui-audit.sh` matches (390px mobile pass present)
    - Script references all three viewports 390, 768, 1440 (grep each)
    - Script iterates both light and dark themes for each surface (grep `theme=dark`)
    - `grep 'tmp/ui-audit' mailglass_admin/scripts/ui-audit.sh` matches and `grep 'priv/static' mailglass_admin/scripts/ui-audit.sh` returns nothing (output stays gitignored, bundle gate untouched)
    - `git diff --name-only` shows changes ONLY within `mailglass_admin/scripts/` and `.planning/` (no `mailglass_admin/lib/` file changed)
    - `git diff --exit-code mailglass_admin/priv/static/` exits 0 (no bundle pollution)
  </acceptance_criteria>
  <done>scripts/ui-audit.sh captures the full 390/768/1440 x light/dark x 3-surface matrix to gitignored tmp/ui-audit/ with deterministic PNG names; no production code or bundle file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Run the capture and confirm 74-UI-SPEC.md satisfies AUDIT-02</name>
  <files>mailglass_admin/scripts/ui-audit.sh, tmp/ui-audit/ (gitignored output), .planning/phases/74-systematic-audit-and-ui-spec/74-01-SUMMARY.md</files>
  <read_first>
    - mailglass_admin/scripts/ui-audit.sh (the extended script from Task 1)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md (the FROZEN AUDIT-02 deliverable — read fully; do NOT modify it)
    - .planning/REQUIREMENTS.md (AUDIT-02 acceptance criteria — taxonomy table resolving the three-way badge_class conflict, support-card hierarchy, empty/error/loading inventory, motion matrix, per-surface checklists)
    - .planning/research/v1.7-admin-ui-polish/FEATURES.md (canonical status taxonomy — the single source of truth the UI-SPEC table must resolve against)
  </read_first>
  <action>
    Attempt to run the extended `mailglass_admin/scripts/ui-audit.sh` to populate `tmp/ui-audit/` with the before-baseline PNG matrix. If the `agent-browser` CLI, demo app, or Postgres is not available in the environment, do NOT fail the task — record that the capture is reproducible-on-demand via the script and that the committed durable artifact is the path references (D-06: PNG binaries are never committed; the inventory/path references are). The PNG paths are deterministic from Task 1's naming, so the gap register (Plan 03) can cite them whether or not the binaries are present in this session.

    Then VERIFY (do not re-author) that the frozen `74-UI-SPEC.md` satisfies every AUDIT-02 acceptance criterion. Produce a short verification subsection appended to the plan's SUMMARY (NOT to the UI-SPEC) checking off: (a) canonical status->color taxonomy table exists and resolves all three-way badge_class conflicts including the phantom `:suppressed`; (b) support-card primary/secondary hierarchy layout present; (c) empty/error/loading state inventory present; (d) motion assignment matrix present; (e) per-surface acceptance checklists present; (f) `status: approved` frontmatter present. If any criterion is unmet, STOP and report — but per the orchestrator grounding the UI-SPEC is already comprehensive, so the expected outcome is all-satisfied. Do not edit `74-UI-SPEC.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md && grep -q '^status: approved' .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md && grep -q 'Canonical Status-Badge Taxonomy Table' .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md && grep -q 'Support-Card Primary/Secondary Hierarchy' .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md && grep -q 'Motion Assignment Matrix' .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md && grep -q 'Empty / Error / Loading State Inventory' .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `74-UI-SPEC.md` exists with `status: approved` frontmatter (grep `^status: approved`)
    - The UI-SPEC contains the canonical status-badge taxonomy table, support-card hierarchy spec, empty/error/loading inventory, and motion assignment matrix (grep each heading)
    - The verification note is recorded in the plan SUMMARY (NOT appended to the UI-SPEC) — `git diff --name-only` shows `74-UI-SPEC.md` is UNCHANGED
    - If the capture ran, PNGs exist only under `tmp/ui-audit/` (gitignored); if it could not run, the SUMMARY records the script as the reproducible source
    - `git diff --exit-code mailglass_admin/priv/static/` exits 0
  </acceptance_criteria>
  <done>The before-baseline capture is run (or recorded as reproducible-on-demand); 74-UI-SPEC.md is confirmed to satisfy AUDIT-02 with zero edits to the frozen contract.</done>
</task>

</tasks>

<threat_model>
This is an evidence-only docs/tooling plan. No new attack surface: no production code, no new routes, no new inputs, no new dependencies. The capture tooling is local/ad-hoc and never promoted to CI.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Information disclosure | Captured screenshots may show seed PII (recipients/subjects) | mitigate | PNGs stay gitignored in tmp/ui-audit/ (D-06); never committed; never written under priv/static/ |

No high-severity threats. No package installs in this phase — `T-74-SC` (supply-chain) is not applicable.
</threat_model>

<verification>
- `bash -n mailglass_admin/scripts/ui-audit.sh` exits 0
- Script iterates viewports 390, 768, 1440 and both themes across all three surfaces
- Output path remains gitignored `tmp/ui-audit/`; no `priv/static/` reference
- `74-UI-SPEC.md` is unchanged and confirmed to satisfy AUDIT-02
- `git diff --name-only` stays within `mailglass_admin/scripts/` and `.planning/`
</verification>

<success_criteria>
- The audit script captures the full 390/768/1440 x light/dark x 3-surface matrix (390px mobile pass included)
- PNGs land only in gitignored tmp/ui-audit/; bundle gate is clean
- AUDIT-02 is confirmed satisfied by the frozen 74-UI-SPEC.md without re-authoring it
- Zero production-code (lib/) and zero bundle (priv/static/) changes
</success_criteria>

<output>
Create `.planning/phases/74-systematic-audit-and-ui-spec/74-01-SUMMARY.md` when done
</output>
