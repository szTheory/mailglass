---
phase: 74-systematic-audit-and-ui-spec
plan: 03
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "A scored gap register exists with stable GAP-NN row IDs covering surface x light/dark x viewport(390/768/1440) x state"
    - "An explicit 390px mobile pass is present for each of the three surfaces"
    - "All 5 badge_class call sites (not just the headline 3) are enumerated, including the two latent detail_header.ex copies"
    - "The deep-link unstyled-CSS bug is filed as a gap row with disposition 'defer to Phase 79'"
  artifacts:
    - path: ".planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md"
      provides: "Scored gap register — the anti-churn citation gate for Phases 75-78"
      min_lines: 60
      contains: "GAP-"
  key_links:
    - from: "74-GAP-REGISTER.md"
      to: "74-UI-SPEC.md"
      via: "pillar + severity rubric cited from the UI-SPEC Gap Register Dimension Reference"
      pattern: "pillar"
    - from: "74-GAP-REGISTER.md"
      to: "mailglass_admin/lib (badge_class call sites)"
      via: "every grep -rln badge_class hit enumerated as component:line rows"
      pattern: "badge_class"
---

<objective>
Produce `74-GAP-REGISTER.md` — the AUDIT-01 scored gap register and the anti-churn citation gate for the whole milestone. Each row carries a stable `GAP-NN` ID and records `{surface, component:line, pillar (one of the 6 from design-system.md:104-121), severity 1-5, evidence PNG path, fix sketch}`. The register covers surface x light/dark x viewport (390/768/1440) x state — including an explicit 390px mobile pass (Pitfall 15). It enumerates ALL FIVE `badge_class/1` call sites (correcting the CONTEXT "three-way" headline), and files the deep-link unstyled-CSS bug as a row with recommended disposition "defer to Phase 79" (D-11).

Purpose: Stable GAP-NN IDs are the mechanism (D-02) that makes the ROADMAP anti-churn gate mechanically enforceable — no build task in Phases 75-78 ships without citing a gap row at severity >= 3. Missing the two latent `detail_header.ex` badge copies would let Phase 76's consolidation silently change a status color (Pitfall 5) — enumerating all five call sites is the prevention.

Output: `.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md`.

CONSTRAINT (ROADMAP success criterion 5): Zero production code changes. The gap register READS the production files at the cited lines but changes none of them. No file under `mailglass_admin/lib/` and no `priv/static/` file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-CONTEXT.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-01-SUMMARY.md

<interfaces>
<!-- The six conformance pillars (design-system.md:104-121) — pillar dimension: -->
<!--   1 Spacing/size  2 Radius  3 Color  4 Type  5 Elevation/stacking  6 Motion+A11y -->
<!-- Severity rubric (UI-SPEC "Gap Register Dimension Reference", D-04/D-05): -->
<!--   5 blocks usage/a11y + blocks Phase 79 closeout; 4 visible regression + blocks closeout; -->
<!--   3 meaningful inconsistency (minimum citable); 2 minor drift; 1 cosmetic. -->

ALL FIVE badge_class/1 call sites (grounded from `grep -rln badge_class mailglass_admin/lib`):
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex:80-84  (named in CONTEXT D-12; phantom :suppressed at :83)
  - mailglass_admin/lib/mailglass_admin/operator/timeline.ex:130-135       (named in D-12; returns full "badge …" string)
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex:97-101     (named in D-12; singular :accept/:reject/:bounce)
  - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex:81-85    (LATENT — NOT in D-12; replicates phantom :suppressed at :84)
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex:142-146   (LATENT — NOT in D-12; replicates singular :accept/:reject/:bounce)

Deep-link bug source: mailglass_admin/docs/design-system.md:141-150 (Known Limitations — relative asset URL resolves against deep path; hard refresh on deep URL loads unstyled; stable asset-serving seam).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 74-GAP-REGISTER.md header — schema, pillar set, severity rubric</name>
  <files>.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md</files>
  <read_first>
    - mailglass_admin/docs/design-system.md:104-150 (the 6 conformance pillars verbatim — pillar dimension source D-04; the Known Limitations deep-link note D-11)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md "Gap Register Dimension Reference" (the exact 6 pillars + the 1-5 severity rubric anchored to Phase 79 closeout; D-04/D-05) and "Per-Surface Acceptance Checklists" (the dimensions each surface row is scored against)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-CONTEXT.md (D-02 stable GAP-NN IDs mandatory; D-04/D-05 severity; Claude's-discretion on column ordering within the AUDIT-01 schema)
    - .planning/REQUIREMENTS.md (AUDIT-01 acceptance criterion — the {surface, component:line, pillar, severity 1-5, evidence PNG, fix sketch} schema + explicit 390px pass)
  </read_first>
  <action>
    Create `.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md` with a header section that defines, self-contained (no external rubric file — D-04):

    1. The row schema: a Markdown table whose columns are exactly `GAP-NN | surface | component:line | pillar | severity | evidence PNG | fix sketch` (column ordering is Claude's discretion within this set — choose readable). Every row's first cell is a stable `GAP-NN` ID starting at `GAP-01` (D-02 — these IDs are the citation gate, never renumber once written).
    2. The six pillars enumerated verbatim from design-system.md:104-121: (1) Spacing/size, (2) Radius, (3) Color, (4) Type, (5) Elevation/stacking, (6) Motion + A11y. Each gap row's `pillar` MUST be one of these six — do not invent a parallel taxonomy.
    3. The severity rubric copied from the UI-SPEC: 5 = blocks usage/a11y + blocks Phase 79 closeout; 4 = visible quality regression + blocks closeout; 3 = meaningful inconsistency (minimum citable in a build task); 2 = minor drift; 1 = cosmetic.
    4. A one-line statement of the anti-churn contract: build tasks in Phases 75-78 must cite a row at severity >= 3.
    5. A note that the matrix dimension is surface x light/dark x viewport (390/768/1440) x state, and that evidence PNG paths reference the gitignored `tmp/ui-audit/{surface}-{viewport}-{theme}.png` set captured by the extended ui-audit.sh (Plan 01) — binaries never committed (D-06).
    6. A header line stating zero production code changed in this phase (the cited files are read-only).

    Tables and prose only — no fenced implementation code.
  </action>
  <verify>
    <automated>test -f .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md && grep -q 'GAP-01' .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md && grep -qi 'severity' .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md && grep -qi 'pillar' .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `74-GAP-REGISTER.md` exists with a defined row schema containing the exact AUDIT-01 fields (surface, component:line, pillar, severity, evidence PNG, fix sketch)
    - The six pillars are enumerated and match design-system.md:104-121 (no invented pillar)
    - The 1-5 severity rubric is present and anchored to Phase 79 closeout (sev 4/5 block closeout; sev >= 3 citable)
    - The header states the anti-churn contract and the gitignored PNG evidence convention (D-06)
    - `git diff --name-only` shows changes ONLY within `.planning/`
  </acceptance_criteria>
  <done>The gap register header defines the GAP-NN schema, the six verbatim pillars, and the Phase-79-anchored severity rubric — the structural frame the rows fill.</done>
</task>

<task type="auto">
  <name>Task 2: Populate gap rows — all 5 badge_class copies, 390px pass, deep-link defer row</name>
  <files>.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (read lines around 80-84 — phantom :suppressed at :83, :dispatched grouped with success)
    - mailglass_admin/lib/mailglass_admin/operator/timeline.ex (lines around 130-135 — full "badge badge-outline …" string; replay events all mapped to badge-error)
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (lines around 97-101 — singular :accept/:reject/:bounce)
    - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex (lines around 81-85 — LATENT copy; phantom :suppressed at :84)
    - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex (lines around 142-146 — LATENT copy; singular :accept/:reject/:bounce)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md "Canonical Status-Badge Taxonomy Table" (the resolved conflicts each badge row's fix sketch must reference) + "Per-Surface Acceptance Checklists" (the 390px dimension per surface)
    - mailglass_admin/docs/design-system.md:141-150 (deep-link Known Limitations — the gap row's evidence and rationale)
    - .planning/research/v1.7-admin-ui-polish/PITFALLS.md (Pitfall 5 silent color change, Pitfall 15 390px, Pitfall 16 deep-link — each maps to specific rows)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-01-SUMMARY.md (the deterministic PNG path names to cite as evidence)
  </read_first>
  <action>
    Populate the gap register rows under the header from Task 1. Continue the stable `GAP-NN` sequence (no renumbering). Required rows:

    BADGE-CONSOLIDATION rows (pillar = Color; these are the Pitfall-5 prevention and the Phase 76 DS-01 citation targets). File a row for EACH of the five badge_class call sites, with component:line being the exact path:line:
      - deliveries_list.ex:83 — phantom `:suppressed`->badge-warning with no canonical row (fix sketch: route through unified status_badge; phantom atom eliminated per UI-SPEC Conflict 1). Severity >= 3.
      - timeline.ex:130-135 — full "badge …" string divergence + replay events all badge-error (fix sketch: unified atom owns base `badge`; correct replay semantics per UI-SPEC Conflicts 3+4). Severity >= 3.
      - records_list.ex:97-99 — singular inbound atoms (fix sketch: normalize to past-tense before status_badge per UI-SPEC Conflict 2). Severity >= 3.
      - operator/detail_header.ex:84 — LATENT duplicate of the phantom `:suppressed` (fix sketch: this copy MUST also be deleted in Phase 76 or the consolidation silently misses it — explicitly flag as the Pitfall-5 latent failure mode). Severity >= 3.
      - inbound/detail_header.ex:142-144 — LATENT duplicate of the singular inbound atoms (same Pitfall-5 flag). Severity >= 3.
    Add a one-line callout that `grep -rln badge_class mailglass_admin/lib` returns FIVE files, not the three named in CONTEXT D-12 — the two detail_header.ex copies are latent.

    390px MOBILE rows (pillar = Spacing/size or as scored): file at least one explicit 390px row PER surface (Operator/Deliveries, Inbound, Preview) — e.g. master-detail collapse to single column, touch targets >= min-h-11, orientation-strip legibility at 390px. Evidence PNG = the `{surface}-390-{theme}.png` paths. This is the Pitfall-15 pass AUDIT-01 mandates.

    SUPPORT-CARD / hierarchy + token / motion rows: file rows for the flat 2x2 support-card grid (no triage signal — pillar Spacing/size or Elevation; Phase 76 DS-03 target), raw type tokens / off-grid gaps / faux-bold where observed (pillar Type/Spacing; DS-02 targets), and the motion-reveal re-fire (operator_live.ex detail pane lacks record-keyed id; pillar Motion+A11y; MOTION-01 target). Score so every build requirement (IA/DS/MOTION/SEED) has at least one citable severity >= 3 row to anchor to (anti-churn contract).

    DEEP-LINK row (D-11, mandatory): file exactly one row for the deep-link unstyled-CSS bug. component:line = the asset-serving seam referenced in design-system.md:141-150. pillar = Elevation/stacking or as scored. Severity set so it does not falsely block closeout before its decision (per D-05, sev 4/5 block Phase 79). In the fix-sketch column, record recommended disposition: "Defer to Phase 79 (VERIF-04); formal in-scope/deferred decision owned by Phase 75 (IA-04)". The row text MUST contain "Phase 79".

    Each row's evidence column cites a concrete `tmp/ui-audit/…png` path (gitignored — D-06). Tables/prose only.
  </action>
  <verify>
    <automated>F=.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md; for s in deliveries_list.ex timeline.ex records_list.ex operator/detail_header.ex inbound/detail_header.ex; do grep -q "$s" "$F" || { echo "MISSING $s"; exit 1; }; done; grep -q '390' "$F" && grep -q 'Phase 79' "$F" && grep -Eq 'GAP-0[1-9]|GAP-1[0-9]' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every row has a unique `GAP-NN` id (no duplicate IDs; `grep -oE 'GAP-[0-9]+' 74-GAP-REGISTER.md | sort | uniq -d` returns nothing)
    - All FIVE badge_class call sites are enumerated by path: deliveries_list.ex, timeline.ex, records_list.ex, operator/detail_header.ex, inbound/detail_header.ex (the two detail_header.ex latent copies are explicitly present — cross-check against `grep -rln badge_class mailglass_admin/lib` returning 5 files)
    - At least one row with viewport 390 exists for each of the three surfaces (explicit 390px mobile pass — Pitfall 15)
    - Exactly one deep-link gap row exists whose fix-sketch references "Phase 79" as the deferral target (D-11)
    - Every badge-consolidation, 390px, support-card, token, and motion gap row used as a build-phase anchor is severity >= 3 (citable under the anti-churn contract)
    - Every row's pillar is one of the six from design-system.md:104-121
    - `git diff --name-only` shows changes ONLY within `.planning/`; `git diff --exit-code mailglass_admin/priv/static/` exits 0; no `mailglass_admin/lib/` file changed
  </acceptance_criteria>
  <done>The gap register is fully populated with stable GAP-NN rows covering all 5 badge_class copies, an explicit 390px pass per surface, the support-card/token/motion gaps, and the deep-link defer-to-Phase-79 row — every build requirement has a severity >= 3 anchor.</done>
</task>

</tasks>

<threat_model>
Evidence-only docs plan. No new attack surface: no production code, no new routes/inputs/deps. Production files are read-only at the cited lines.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03 | Information disclosure | Gap rows reference screenshots that may show seed PII | mitigate | Gap register cites PNG paths only (gitignored, D-06); quotes only structural/aggregate strings and class names — no raw recipient/subject PII |

No high-severity threats. No package installs — `T-74-SC` not applicable.
</threat_model>

<verification>
- `74-GAP-REGISTER.md` exists with stable GAP-NN ids and the AUDIT-01 column schema
- All 5 badge_class call sites enumerated (including the 2 latent detail_header.ex copies)
- 390px pass present for each of the 3 surfaces
- Deep-link row present with "defer to Phase 79" disposition
- Every build-anchor row is severity >= 3; every pillar is one of the canonical six
- `git diff --name-only` stays within `.planning/`; bundle gate clean
</verification>

<success_criteria>
- A scored gap register exists covering surface x light/dark x viewport(390/768/1440) x state with stable GAP-NN ids
- The anti-churn citation gate is mechanically usable (severity >= 3 rows per build requirement)
- The Pitfall-5 latent-copy failure mode is closed by enumerating all 5 badge_class sites
- The deep-link bug is filed with a Phase 79 deferral disposition
- Zero production-code and zero bundle changes
</success_criteria>

<output>
Create `.planning/phases/74-systematic-audit-and-ui-spec/74-03-SUMMARY.md` when done
</output>
