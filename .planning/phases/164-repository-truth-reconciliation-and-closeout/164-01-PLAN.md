---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
  - test/scripts/phase_164_repository_truth_test.exs
  - scheduled-control-sweep.json
autonomous: true
requirements: [TRTH-02]
must_haves:
  truths:
    - "D-05/D-08/TRTH-02: A maintainer can trace the root scheduled-control-sweep.json from its locked digest and stale identity to exactly one remove disposition before the file is removed."
    - "D-06: The tracer preserves durable scheduled-control proof as tracked, discoverable evidence."
  artifacts:
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv"
      provides: "Machine-checkable disposition ledger schema and first complete D-08 row"
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Wave 0 ledger schema and stale-root negative/positive contract"
  key_links:
    - from: "scheduled-control-sweep.json"
      to: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv"
      via: "locked SHA-256, no-producer/no-consumer evidence, and remove disposition"
      pattern: "331810b4b1724452f0e2707c800230e52fabea01c3773d362b3a1240040ece7e"
  flagged_assumptions:
    - "TRTH-02 adjacency probe unresolved: when two audited subjects or ignore patterns are textually equal or touch, the executor must not assume whether they merge; the ledger uniqueness contract and observed subject inventory must surface the case."
    - "TRTH-02 empty probe unresolved: behavior for an empty audit, a single audited subject, or a missing/null ledger input is not specified by a phase SPEC and remains explicitly flagged."
    - "TRTH-02 ordering probe unresolved: stable ordering for equal comparison keys is not specified by a phase SPEC and remains explicitly flagged."
  prohibitions:
    - statement: "D-07/D-08: MUST NOT add an ignore rule to conceal the stale root scheduled-control output or substitute concealment for its locked evidence-backed removal."
      status: unverified
      flagged: true
---

<objective>
Prove the Phase 164 architecture end to end with one real stale-artifact disposition.

Purpose: Establish the tracked-ledger → executable contract → evidence-backed cleanup path before expanding the audit.
Output: A production ledger schema, its Wave 0 test, and removal of only the D-08-identified stale root file.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv
@scripts/scheduled_control_evidence.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the locked stale sweep through ledger, contract, and removal</name>
  <reversibility rating="costly">The ledger header becomes a shared contract for later plans and changing it requires coordinated test, wrapper, and evidence-row updates.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv, test/scripts/phase_164_repository_truth_test.exs, scheduled-control-sweep.json</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv
    - scripts/scheduled_control_evidence.sh
    - .github/scheduled-controls.json
    - .gitignore
    - scheduled-control-sweep.json
  </read_first>
  <behavior>
    - Test 1: the TSV header exposes stable_id, subject, kind, producer, state, authority, reproducibility, currentness, durable_consumer, evidence, disposition, and rationale.
    - Test 2: the D-08 row names scheduled-control-sweep.json once, carries the locked SHA-256, records stale generated output with no repository producer/consumer for the root filename, and uses disposition remove.
    - Test 3: a duplicate subject, blank required field, or disposition outside retain/update/archive/remove/ignore fails the parser contract.
    - Test 4: no repository ignore file gains a scheduled-control-sweep.json exclusion and the root file is absent after its digest is verified.
  </behavior>
  <action>Start RED by creating `Mailglass.Scripts.Phase164RepositoryTruthTest` with a TSV parser and a temporary malformed-ledger fixture. Define `164-TRUTH-DISPOSITION.tsv` using the twelve-column schema delegated by D-05 and selected from the Phase 161 TSV analog; use one stable row ID for the root sweep. Before removal, compute SHA-256 using `shasum -a 256 scheduled-control-sweep.json` and require exact equality with D-08's locked digest. Record the file as untracked stale generated output, producer `scripts/scheduled_control_evidence.sh sweep` only by content shape (no root-path producer), no durable consumer, evidence references to D-08/current Phase 162 scheduled proof, and disposition `remove`; then remove only that exact root file. Per D-06/D-07, leave all six ignore files unchanged and preserve `.planning`, release, publish, scheduled-control, and generated-host evidence.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_repository_truth_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 164-TRUTH-DISPOSITION.tsv` contains exactly the twelve D-05 fields named in the behavior block, in a deterministic tab-separated order.
    - The test asserts one and only one `scheduled-control-sweep.json` subject row, `remove`, and SHA-256 `331810b4b1724452f0e2707c800230e52fabea01c3773d362b3a1240040ece7e`.
    - The executor computes the digest before deletion and halts without deleting when the file is absent or the digest differs.
    - `test ! -e scheduled-control-sweep.json` passes after the verified removal, and `git diff --` across all six ignore files is empty for this task.
  </acceptance_criteria>
  <done>The first audited subject has a complete, tested, evidence-backed disposition and only the exact D-08 stale root output is removed.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv` — new fields `stable_id`, `subject`, `kind`, `producer`, `state`, `authority`, `reproducibility`, `currentness`, `durable_consumer`, `evidence`, `disposition`, and `rationale`; first stable row for `scheduled-control-sweep.json`.
- `test/scripts/phase_164_repository_truth_test.exs` — new test module `Mailglass.Scripts.Phase164RepositoryTruthTest` and TSV parsing/validation helpers.
- `scheduled-control-sweep.json` — removed only after locked digest verification; no CLI flag, API, schema, dependency, or ignore pattern is added.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untracked generated file → durable ledger | File identity must match the locked digest before deletion. |
| Ledger prose → cleanup authority | Only a complete singular disposition authorizes the named removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-01 | Tampering | scheduled-control-sweep.json identity | high | mitigate | Compute SHA-256 before deletion and require D-08's exact digest. |
| T-164-02 | Repudiation | disposition ledger row | high | mitigate | Test mandatory fields, unique subject, evidence, and exact disposition enum. |
| T-164-03 | Tampering | ignore rules | high | mitigate | Assert all six ignore files are unchanged and no proof-hiding pattern is added. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile changes occur. |
</threat_model>

<verification>
Run the focused Wave 0 ledger contract, confirm the locked row, confirmed removal, and unchanged ignore files, then run `mix ci.fast` at the end of Wave 1.
</verification>

<success_criteria>
- The tracer gives maintainers one real, reviewable artifact disposition from authority to cleanup.
- Missing/changed identity fails before mutation.
- Durable proof remains visible and no broad ignore is introduced.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less: concealing the D-08 stale root output instead of recording and removing its locked identity is a repository-specific evidence-integrity prohibition and remains flagged-unverified in `must_haves.prohibitions`.
- Recalled downstream: Plan 164-04 expands this narrow tracer constraint into the phase-wide durable-proof concealment/destruction prohibition.
- Dropped with breadcrumb: duplicate rows, blank fields, invalid dispositions, and digest mismatch are routine contract/edge failures covered by the tracer's automated test.

## Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts and ignore rules describe actual repository state | 164-01 | COVERED |
| REQ | TRTH-02 | Evidence-backed classification and junk-only removal | 164-01 | COVERED |
| CONTEXT | D-05, D-06, D-07, D-08 | Ledger contract, durable proof, narrow ignores, locked stale sweep | 164-01 | COVERED |
| RESEARCH | tracer recommendation | Tracked schema-tested ledger plus locked root cleanup | 164-01 | COVERED |
| PATTERNS | TSV/D-08 analog | Phase 161 format and scheduled sweep producer shape | 164-01 | COVERED |
| VALIDATION | 164-01-01 tracer Wave 0 contract | Create the repository-truth test before ledger/removal implementation | 164-01 | COVERED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-01-SUMMARY.md` when done.
</output>
