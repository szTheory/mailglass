---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 36
type: execute
wave: 33
depends_on: [164-35]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/TRTH-03: Plan 164-35 implementation and its summary reach protected main through normal integration before any replacement proposal is authoritative."
    - "D-11/TRTH-03: The exact integrated main SHA has a successful attempt-one normally triggered push CI run selected without dispatch or rerun substitution."
    - "TRTH-03: The handoff contains one full main SHA and one numeric CI run ID whose independently read fields agree on workflow, event, attempt, branch, head SHA, status, and conclusion."
  artifacts: []
  key_links:
    - from: "164-35-SUMMARY.md"
      to: "protected main"
      via: "normal protected integration"
      pattern: "164-35-SUMMARY.md"
    - from: "protected main SHA"
      to: "attempt-one CI run"
      via: "independent exact-SHA normal-push field validation"
      pattern: "CI|push|attempt"
  flagged_assumptions:
    - "TRTH-03 ordering probe: if multiple runs exist, only the exact-SHA attempt-one normal push run is admissible; equal timestamps or ordering do not substitute identity."
  prohibitions:
    - statement: "D-11: Manual dispatch, rerun, alternate SHA, or direct protected-main bypass MUST NOT substitute for normal exact-main integration evidence."
      status: unverified
      flagged: true
    - statement: "T-164-109: Terminal evidence MUST NOT be produced at this integration checkpoint or followed by later tracked work."
      status: unverified
      flagged: true
---

<objective>
Place the complete authority-chain repair on protected main and obtain its exact normal CI identity before external approval.

Purpose: Ensure the replacement proposal binds code already accepted by the repository's normal protected integration path.
Output: A checkpoint handoff containing the exact protected-main SHA and successful attempt-one push CI run ID.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-35-PLAN.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-35-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@test/scripts/phase_164_closeout_test.exs
@mix.exs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Normally integrate the repair and attest exact attempt-one CI</name>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-35-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - mix.exs
  </read_first>
  <precondition>Plan 164-35 and `164-35-SUMMARY.md` are committed, the focused authority lane and repository CI lane pass, and the working tree contains no uncommitted Plan 164-35 source change.</precondition>
  <action>Rerun the exact focused authority-closure and repository CI aliases before pausing. Ask the maintainer to integrate the complete Plan 164-35 implementation and summary using the repository's normal protected-main workflow; the executor does not directly push protected main, bypass protection, auto-merge, dispatch workflows, or rerun jobs. After fetching origin, require the canonical checkout on branch `main`, HEAD equal to `refs/remotes/origin/main`, and two consecutive stable empty porcelain observations. Wait for the normally triggered attempt-one `CI` push run for that exact SHA to complete successfully. Independently read the run's database ID, workflow name, event, attempt, branch, head SHA, status, and conclusion and require exact agreement. Preserve pending or failed state and keep the checkpoint open if any field or authority is unavailable. Do not run installed-boundary verification or either finalization mode.</action>
  <instructions>Use the repository's normal protected integration workflow for the complete Plan 164-35 commit set. After the merge, fetch origin and supply only the exact clean main SHA plus the database ID of its successful attempt-one normal push CI run.</instructions>
  <verification>Confirm branch `main`; HEAD equals fetched origin/main; two porcelain reads are empty; the run is workflow `CI`, event `push`, attempt one, branch `main`, the same head SHA, completed, and successful.</verification>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.authority_closure &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; either alias selects zero tests; repository CI includes an external-host group; the working tree has invalid diff whitespace; or source behavior differs from the Plan 164-35 summary.</fails_when>
  </verify>
  <acceptance_criteria>
    - Plan 164-35 implementation and summary are present at the exact protected-main SHA.
    - HEAD, fetched origin/main, and the CI run's head SHA are identical full OIDs.
    - CI is workflow `CI`, event `push`, attempt one, branch `main`, completed, and successful.
    - No proposal approval, installed replacement, schedule operation, completion metadata, or terminal evidence is produced.
  </acceptance_criteria>
  <reversibility rating="costly">Protected-main integration remains recoverable through the repository's normal revert path, but reversing it requires another protected change and CI cycle.</reversibility>
  <done>The implemented authority closure is on protected main with exact successful normal attempt-one CI evidence.</done>
  <resume-signal>Provide `main_sha=&lt;40-lowercase-hex&gt; ci_run_id=&lt;numeric-database-id&gt;` only after the protected-main and CI checks above pass; otherwise report the exact pending or failed authority and keep the checkpoint open.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- New repository files or symbols: none.
- External proposal, approval, installation, rollback, report, or completion metadata: none.
- Checkpoint output: exact `main_sha` and exact normal attempt-one `ci_run_id` values for Plan 164-37.

<threat_model>
## ASVS Level

OWASP ASVS L1; bypassed protected integration or substituted CI identity is high severity and blocks progression.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local commits → protected main | Local success is insufficient until the normal protected workflow accepts the exact repair. |
| Protected main → GitHub CI | Run metadata must identify the exact main SHA and normal attempt-one push execution. |
| CI handoff → proposal | Later approval may consume only the independently validated SHA/run pair. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-138 | Spoofing | protected-main identity | high | mitigate | Require canonical branch plus exact HEAD/fetched-origin equality and stable porcelain. |
| T-164-139 | Repudiation | CI provenance | high | mitigate | Independently validate workflow, event, attempt, branch, SHA, status, conclusion, and numeric run ID. |
| T-164-140 | Elevation of Privilege | integration controls | high | mitigate | Use a blocking human-action checkpoint and the normal protected workflow without bypass or direct push. |
| T-164-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs. |
</threat_model>

<verification>
Repository checks run before the checkpoint. External completion requires clean exact protected main and independently validated attempt-one normal push CI for that same SHA.
</verification>

<success_criteria>
- Protected main contains the complete Plan 164-35 source and summary.
- Exact attempt-one normal CI succeeds for that SHA.
- The checkpoint yields a bounded SHA/run handoff and performs no later lifecycle stage.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational state follows normal protected repository authority | 164-36 Task 1 | COVERED |
| REQ | TRTH-03 | Exact-main normal integration evidence | Task 1 | COVERED |
| CONTEXT | D-09/D-10 | Durable change precedes exact-main proof | Task 1 | COVERED |
| CONTEXT | D-11 | Exact normal attempt-one CI without operator substitution | Task 1 | COVERED |
| RESEARCH | — | Fail-closed external authority handoff | Task 1 | COVERED |
| VERIFICATION | Missing protected integration evidence for repairs | Exact main SHA and CI run checkpoint | Task 1 | COVERED |
| EDGE | TRTH-03 ordering | Identity fields dominate timestamp/run ordering | must_haves.flagged_assumptions | COVERED-FLAGGED |
| PROHIBITION | No bypass/manual substitute or terminal proof | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-36-SUMMARY.md` with the exact checkpoint SHA and CI run ID. Continue to Plan 164-37; do not run proposal approval outside that plan, install bytes, or run either finalization mode.
</output>
