---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 38
type: execute
wave: 35
depends_on: [164-37]
files_modified:
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TRTH-03: Only the exact Plan 164-37 approved loader blob replaces the installed predecessor, after exact predecessor bytes are preserved at the approved rollback identity."
    - "TRTH-03: Direct installed `--version` and ancestry-aware `--self-check` prove digest, mode, source OID, terminal range 01–39, and a runnable physical Mix/Elixir/Erlang closure in the exact child environment."
    - "D-09/D-10/D-11/TRTH-03: Controlled-host proof reads the real approval and installed object while repository CI remains hermetic and external-host tests remain explicitly excluded."
    - "D-09/D-10/D-11: Installation readiness is non-terminal and leaves ordinary verification, completion metadata, final protected-main evidence, and T-164-109 pending."
  artifacts:
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Observed active installation, rollback provenance, controlled-host result, and pending terminal order"
  key_links:
    - from: "Plan 164-37 approval record"
      to: "installed loader object"
      via: "authenticated Git blob materialization, digest/mode checks, and atomic same-directory rename"
      pattern: "installation_source_oid|source_sha256"
    - from: "installed loader object"
      to: "test/scripts/phase_164_closeout_test.exs"
      via: "real controlled-host alias with direct version/self-check and exact-child runtime probe"
      pattern: "phase_164_installed_production_boundary"
    - from: "rollback object"
      to: "prior active approval"
      via: "exact predecessor digest and mode recorded before replacement"
      pattern: "prior_sha256|rollback"
  flagged_assumptions:
    - "TRTH-03 adjacency probe: approved current and preserved prior objects remain distinct identities even if paths are adjacent or bytes later compare equal."
    - "TRTH-03 empty probe: absent approval, rollback, installed object, direct probe, or selected host test is failure."
  prohibitions:
    - statement: "D-09/D-10/D-11: Installation MUST NOT consume checkout bytes, tuple drift, or unapproved future source."
      status: unverified
      flagged: true
    - statement: "D-09/D-10/D-11: Installed readiness MUST NOT substitute for ordinary verification, protected exact-main evidence, or terminal finalization."
      status: unverified
      flagged: true
    - statement: "T-164-109: No-later-write terminal evidence MUST NOT be produced before Plan 164-39 and all later lifecycle steps finish."
      status: unverified
      flagged: true
---

<!-- plan-criteria-allow: R6 - git diff --check intentionally validates the executor's current uncommitted task changes before commit. -->

<objective>
Recoverably install the exact approved authority-chain repair and prove the real installed runtime closure without entering terminal lifecycle.

Purpose: Establish installed provenance for the repaired loader only after protected integration and explicit tuple approval.
Output: Approved installed bytes, verified rollback, non-vacuous controlled-host proof, and a truthful readiness record.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-37-PLAN.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-37-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@scripts/finalize_phase_164.sh
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the predecessor and atomically install approved source bytes</name>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-37-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  </read_first>
  <precondition>The exact Plan 164-37 proposal and approval records pass the proposal-boundary alias, and the installed predecessor still matches the approved prior identity.</precondition>
  <action>Consume only the persisted Plan 164-37 approval as authority data. Revalidate exact proposal/approval parity, approved protected source OID and CI run, authenticated source blob/digest, physical trusted executable identities, exact-child runtime probe outputs/digest, installed predecessor lstat/digest/mode/stat identity, destination, and rollback absence. First atomically publish exact predecessor bytes at the approved rollback identity as a regular non-symlink mode-0400 object and re-read its digest. Materialize the approved loader Git blob into a private file, verify its digest and absolute shebang, set mode 0500, then atomically rename within the destination directory. Re-read installed lstat/mode/digest before executing it. Run direct `--version`, then `--self-check` with the canonical repository and approved installation source OID; self-check must report installation ancestry, 01–39 terminal range, and the same successful physical Mix/Elixir/OTP probe. Run the real controlled-host alias. If any post-replacement assertion fails, atomically restore the already verified predecessor and report non-success. On success, write the exact observed installed and rollback identities to the readiness section of `164-FINALIZATION.md`; Task 2 completes the lane/lifecycle reconciliation. Do not use checkout bytes and do not invoke a phase number, pre-verification mode, terminal mode, dispatch, rerun, release, or publication operation.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.installed_boundary</automated>
    <fails_when>Non-zero exit; the controlled-host selection is empty; approval or live tuple drifts; rollback is absent, symlinked, wrong-mode, or wrong-digest; installed bytes are absent, symlinked, wrong-mode, or wrong-digest; direct version/self-check fails; source ancestry or range differs; exact-child runtime probe differs; or terminal evidence is created.</fails_when>
  </verify>
  <acceptance_criteria>
    - Verified predecessor bytes exist at the approved rollback identity before installed mutation.
    - Installed bytes are exactly the approved Git blob as a regular non-symlink mode-0500 object.
    - Direct installed inspection proves source ancestry, 01–39 range, and runnable physical BEAM closure.
    - Automatic restoration is armed for every post-install failure.
  </acceptance_criteria>
  <reversibility rating="reversible">The exact predecessor is verified and published before atomic replacement, enabling deterministic restoration.</reversibility>
  <done>The approved loader is installed recoverably and its real child runtime closure passes direct self-check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record non-terminal installed provenance and preserve lane separation</name>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-37-SUMMARY.md
    - test/scripts/phase_164_closeout_test.exs
    - mix.exs
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <behavior>
    - Test 1: the installed-boundary alias reads the real Plan 164-37 approval and installed object and selects a nonzero controlled-host set.
    - Test 2: direct installed proof validates Erlang identity plus normalized Mix, Elixir, OTP, and probe-digest agreement.
    - Test 3: repository CI excludes proposal and installed-host groups while retaining the repository-only movement/runtime negatives.
    - Test 4: lifecycle text labels the result installed readiness only and orders Plan 164-39 reconciliation before ordinary verifier, completion metadata, protected main, exact remote evidence, and terminal execution.
  </behavior>
  <action>Rerun the real installed-boundary alias and the repository CI lane as separate authorities. Update `164-FINALIZATION.md` with the active approval digest, protected installation OID/CI run, installed digest/mode/stat identity, rollback digest/mode/stat identity, exact physical runtime identities and normalized probe outputs, direct version/self-check result, and selected host-test count. Retain the former approval/installation only as explicit bounded provenance per D-02. Mark Plan 164-38 as installation readiness, keep Plan 164-39 reconciliation and every terminal prerequisite pending, and keep T-164-109 open. Do not modify validation/security/ledger records in this task; Plan 164-39 owns that final tracked reconciliation.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.installed_boundary &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; either lane is empty; controlled-host proof does not read the real active tuple; repository CI touches host state; prior provenance becomes a second active authority; lifecycle text claims terminal/completion state; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - Active installed proof is bound to the Plan 164-37 approval and actual installed object.
    - Prior approval and rollback remain recoverable historical provenance, not current authority.
    - Repository-only and controlled-host lanes both pass with disjoint selections.
    - Plan 164-39, ordinary verification, completion metadata, protected-main final evidence, and terminal capture remain pending.
  </acceptance_criteria>
  <reversibility rating="reversible">The tracked readiness record can be corrected independently; installed rollback remains available.</reversibility>
  <done>The active installed authority is proven and documented without making a terminal or completion claim.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md` — active Plan 164-37 approval/install/rollback/probe readiness record.
- Installed loader object — external regular non-symlink mode-0500 exact approved bytes at the approved destination identity.
- Rollback object — external regular non-symlink mode-0400 exact predecessor bytes at the approved rollback identity.
- New terminal report or completion metadata: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; unapproved installation, unrecoverable replacement, false installed proof, and premature terminal execution are high severity and block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Approval record → external install | Only the exact approved Git blob and tuple may mutate installed state. |
| Installed predecessor → rollback | Recoverability must be established before replacement. |
| Installed command → controlled-host test | Direct execution must prove actual mode/digest/ancestry and runtime closure. |
| Installed readiness → terminal lifecycle | Readiness is not ordinary verification or exact-main terminal evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-146 | Tampering | installed replacement | high | mitigate | Revalidate exact approval and atomically install only authenticated commit bytes. |
| T-164-147 | Repudiation | rollback provenance | high | mitigate | Publish and verify digest-addressed mode-0400 predecessor bytes before mutation. |
| T-164-148 | Spoofing | active installed proof | high | mitigate | Directly verify real approval, installed digest/mode/OID ancestry, range, and exact-child runtime output. |
| T-164-149 | Elevation of Privilege | terminal execution | high | mitigate | Invoke inspection modes only and preserve later lifecycle gates. |
| T-164-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs. |
</threat_model>

<verification>
Run direct installed inspection through the non-vacuous controlled-host alias, then separately run repository CI with external groups excluded. Confirm the tracked record remains readiness-only.
</verification>

<success_criteria>
- Exact approved bytes are installed only after verified rollback publication.
- Real installed self-check proves the repaired authority chain and physical runtime closure.
- Repository CI remains hermetic and terminal lifecycle remains pending.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational state records the actual supported installed authority | 164-38 Tasks 1–2 | COVERED |
| REQ | TRTH-03 | Real installed reproducible closeout authority | Tasks 1–2 | COVERED |
| CONTEXT | D-02 | One active authority with bounded prior provenance | Task 2 | COVERED |
| CONTEXT | D-09–D-11 | External installed proof remains distinct from terminal proof | Tasks 1–2 | COVERED |
| RESEARCH | — | Exact approval, rollback-first mutation, direct installed verification | Task 1 | COVERED |
| VERIFICATION | Installed approval/provenance missing | Exact Plan 164-37 install plus controlled-host proof | Tasks 1–2 | COVERED |
| EDGE | TRTH-03 adjacency/empty | Active/prior identities remain distinct; missing object/test fails | must_haves / Tasks 1–2 | COVERED-FLAGGED |
| PROHIBITION | No checkout install or premature terminal claim | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-38-SUMMARY.md` with exact installed/rollback/probe evidence. Continue to Plan 164-39; do not run ordinary completion or either finalization mode yet.
</output>
