---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 29
type: execute
wave: 27
depends_on: [164-28]
files_modified:
  - test/scripts/phase_164_closeout_test.exs
  - test/test_helper.exs
  - test/scripts/ci_parity_drift_test.exs
  - test/scripts/scheduled_control_evidence_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: The controlled-host alias reads the real mode-0500 installed executable and exact mode-0400 Plan 164-27 approval record, proves their digest and installation-OID ancestry, and runs the installed executable's own --self-check."
    - "TRTH-03: Missing, symlinked, non-regular, wrong-mode, malformed, duplicate-key, digest-mismatched, non-ancestor, or unsuccessful installed authority fails the controlled-host suite; an empty selection is not green."
    - "TRTH-03: Disposable repository/source-loader attacks remain repository-only tests, and no protected, repository-only, or unfiltered full-suite entry point can collect the controlled-host tag."
    - "TRTH-03: Equal approval and installed identities are accepted only when every tuple field agrees; adjacent or duplicate records remain distinct failures, and validation order is stable from path/type/mode through schema/digest/ancestry/self-check."
  artifacts:
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Real installed-command/Plan-164-27 approval proof separated from disposable source-loader attacks"
    - path: "test/test_helper.exs"
      provides: "Default controlled-host tag exclusion with explicit --only inclusion preserved"
    - path: "test/scripts/ci_parity_drift_test.exs"
      provides: "Expanded alias/workflow full-suite collection contract"
    - path: "test/scripts/scheduled_control_evidence_test.exs"
      provides: "Protected-job and explicit controlled-host alias separation contract"
  key_links:
    - from: "test/scripts/phase_164_closeout_test.exs"
      to: "Plan 164-27 approval tuple and installed executable"
      via: "exact schema, lstat/mode, SHA-256, git cat-file, merge-base ancestry, and direct --self-check assertions"
      pattern: "phase_164_installed_production_boundary"
    - from: "test/test_helper.exs"
      to: "all Mix aliases and .github/workflows/ci.yml full-suite commands"
      via: "default ExUnit exclusion plus explicit controlled-host inclusion"
      pattern: "phase_164_installed_production_boundary"
  flagged_assumptions:
    - "TRTH-01 unclassified probe: no TRTH-01 implementation changes are needed because the verifier already marked current and historical guidance satisfied; Plan 164-34 will only correct evidence statements affected by these repairs."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT treat controlled-host readiness tests as terminal protected-main evidence."
      status: unverified
      flagged: true
    - statement: "T-164-109: terminal evidence MUST NOT be followed by a tracked write; terminal capture remains pending and is not run by this plan."
      status: unverified
      flagged: true
---

<objective>
Make controlled-host proof exercise the actual Plan 164-27 installation and make host-only collection impossible from every repository/full-suite path.

Purpose: Replace fixture-labeled production proof with the real authority boundary while keeping protected CI hermetic.
Output: Fail-closed installed tuple assertions, repository-only hostile fixtures, and an exhaustive full-suite isolation contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
@test/scripts/phase_164_closeout_test.exs
@test/test_helper.exs
@test/scripts/ci_parity_drift_test.exs
@mix.exs
@.github/workflows/ci.yml
</context>

<assumption_delta_decision>
Primary noun: Phase 164 installed authority tuple. Decision: no-change — “alternate” remains a hostile-input case; this repair retains one exact installed executable/approval identity and introduces no plural authority architecture.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the Plan 164-27 approval tuple through the real installed self-check</name>
  <files>test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: the host-tagged test lstat-checks the real installed command and Plan 164-27 approval as regular non-symlinks at modes 0500 and 0400 before reading either.
    - Test 2: the approval parser accepts exactly the 19-key record_version=1, phase_plan=164-27 schema once each and binds destination, install_mode, approval_status, source OID, source digest, all seven executable identities, prior object, and rollback fields.
    - Test 3: installed SHA-256 and the committed loader blob at installation_source_oid equal source_sha256, and that OID is an ancestor of current HEAD.
    - Test 4: direct execution of the real installed file with --self-check exits 0 and reports the approved installation OID, current OID, loader digest, executable path, mode 0500, and terminal range.
    - Test 5: each path/type/mode/schema/digest/ancestry/self-check failure is nonzero and bounded; no missing-host conditional or skip converts it to green.
  </behavior>
  <action>Start RED by replacing the five fixture-backed cases inside the `phase 164 installed production boundary` describe with a real-host tracer. Add exact module attributes for the installed command and Plan 164-27 approval record, plus narrowly named helpers `parse_install_approval!/1`, `assert_regular_mode!/2`, `sha256_file!/1`, and `invoke_installed_self_check/2`. Parse the approval without shell evaluation; reject malformed lines, blank keys/values, duplicate keys, unknown/missing keys, and any value that disagrees with the immutable tuple recorded in `164-27-SUMMARY.md`. Use lstat before read/hash, trusted Git object reads for `scripts/mailglass_finalize_phase_loader.mjs`, and `git merge-base --is-ancestor` for provenance. Invoke the installed executable directly so its approved shebang participates, then assert every named self-check output field and nonzero test selection. Preserve Plan 164-27 as the active tuple for this tracer; do not mutate the approval, installation, rollback bytes, or repository state, and do not invoke pre-verification or terminal mode.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.installed_boundary</automated>
    <fails_when>Non-zero exit; zero selected tests; the real installed file or approval is not read; any exact schema/digest/OID/mode/self-check assertion is absent; or output contains an ExUnit skip.</fails_when>
  </verify>
  <acceptance_criteria>
    - The controlled-host block has at least one active test and no disposable repository fixture creation.
    - The actual installed path and Plan 164-27 approval are authenticated fail-closed before self-check.
    - Approval bytes are parsed as data and never sourced or evaluated by a shell.
    - Self-check output is asserted field-by-field, including installation OID ancestry and the installed path/mode.
  </acceptance_criteria>
  <done>The named controlled-host alias proves the real Plan 164-27 installation non-vacuously.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exclude host-only collection from every repository and protected full suite</name>
  <files>test/scripts/phase_164_closeout_test.exs, test/test_helper.exs, test/scripts/ci_parity_drift_test.exs, test/scripts/scheduled_control_evidence_test.exs</files>
  <read_first>
    - mix.exs
    - test/test_helper.exs
    - .github/workflows/ci.yml
    - test/scripts/phase_164_closeout_test.exs
    - test/scripts/ci_parity_drift_test.exs
    - test/scripts/scheduled_control_evidence_test.exs
    - test/support/suite_floor.ex
    - test/scripts/suite_floor_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: default ExUnit configuration excludes phase_164_installed_production_boundary on public and non-public schema axes, while the explicit --only alias includes it.
    - Test 2: all former disposable foreign-repository, moving-HEAD, missing-pair, retired-extension, and override attacks remain active under repository-only coverage.
    - Test 3: fixture invocation discovers Node from the test environment and contains no Jon-specific Node-version path.
    - Test 4: recursive expansion of every Mix alias and every CI workflow run command finds no full-root suite capable of collecting the host tag.
    - Test 5: negative controls removing the default exclusion or substituting an unfiltered synthetic alias/job make the contract fail.
  </behavior>
  <action>Move the five disposable source-loader attacks out of the host-only describe into a clearly named repository-only describe under the existing repository fixture tag, retaining every marker and failure assertion. Replace `invoke_production_loader/3` and `invoke_immutable_loader/3` use of the versioned absolute Node path with a checked `System.find_executable("node")` result for disposable fixture execution; the production installed tracer continues to invoke the installed file directly. Configure `test/test_helper.exs` with a base exclusion for `:phase_164_installed_production_boundary` and merge `:public_only` only on the non-public schema axis so later `ExUnit.configure/1` calls cannot erase the host exclusion. In `CIParityDriftTest`, add `full_suite_alias_commands/1`, `workflow_full_suite_commands/1`, and `host_only_collection_impossible?/2` (or equivalently explicit pure helpers) that recursively expand every alias key, parse every `.github/workflows/ci.yml` run command, classify unfiltered/full-root `mix test` entry points, and prove each inherits the default exclusion or carries the exact explicit exclusion; allow only the named controlled-host alias to use `--only`. Extend the scheduled-control contract to preserve the unchanged protected call sites. Do not delete attacks, broaden exclusions, change CI jobs/triggers, or treat an absent host as a pass.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/ci_parity_drift_test.exs test/scripts/scheduled_control_evidence_test.exs test/scripts/phase_164_closeout_test.exs --exclude phase_164_installed_production_boundary --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; zero tests; any disposable attack is host-tagged or skipped; any full-suite alias/job can collect the host tag; a negative control stays green; the versioned Jon-specific Node invocation remains; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - Host-only exclusion is the default for every root test process, including protected `mix test` and `ci.full`.
    - Explicit controlled-host inclusion remains one exact named alias and is not called by CI.
    - The contract enumerates all aliases/jobs rather than checking only `verify.ci_lane_contract`.
    - Repository-only attacks remain non-vacuous and the full required lane passes without reading external installed state.
  </acceptance_criteria>
  <done>Host-specific proof cannot leak into protected or repository-only full suites, while all disposable attacks remain covered.</done>
</task>

</tasks>

## Artifacts this phase produces

- `test/scripts/phase_164_closeout_test.exs` — `parse_install_approval!/1`, `assert_regular_mode!/2`, `sha256_file!/1`, `invoke_installed_self_check/2`, real installed tracer, and repository-only hostile attack group.
- `test/test_helper.exs` — default `phase_164_installed_production_boundary` exclusion merged with schema-axis exclusions.
- `test/scripts/ci_parity_drift_test.exs` — full-suite alias/job expansion and negative controls.
- `test/scripts/scheduled_control_evidence_test.exs` — protected-job/host-alias separation assertions.
- New files or runtime flags: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; false installed proof and host-state leakage into protected CI are high severity and block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| External approval/install → controlled-host ExUnit | Mutable filesystem objects must match the immutable Plan 164-27 tuple before their contents count as evidence. |
| Repository test process → host-only tag | Unfiltered local/protected suites must not collect machine-specific assertions. |
| Disposable Git fixtures → repository-only tests | Hostile source-loader behavior remains tracked test coverage but cannot impersonate the actual installation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-110 | Spoofing | installed authority proof | high | mitigate | Authenticate path/type/mode, exact approval schema, digest, committed blob, ancestry, and direct self-check. |
| T-164-111 | Repudiation | controlled-host alias | high | mitigate | Require nonzero selected tests and exact self-check output; missing host state remains nonzero. |
| T-164-112 | Denial of Service | protected/full suites | high | mitigate | Default-exclude the host tag and mechanically expand every alias/job entry point. |
| T-164-113 | Tampering | disposable attack coverage | medium | mitigate | Keep all five hostile fixtures active under repository-only selection with marker assertions. |
| T-164-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs; existing Node is discovered only for disposable tests. |
</threat_model>

<verification>
Run the real controlled-host alias separately, then the focused isolation contracts and complete repository-only required alias. Do not run either finalization mode.
</verification>

<success_criteria>
- The actual Plan 164-27 installation is proven through its immutable tuple and own self-check.
- All host-only tests are default-excluded from every repository/protected full suite.
- Disposable source-loader attacks remain active repository-only coverage without a hard-coded Node-version path.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational evidence describes the actual supported installed boundary | 164-29 | COVERED |
| REQ | TRTH-03 | Reproducible closeout authority and hermetic protected proof | Tasks 1–2 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, canonical host, exact protected evidence | Tasks 1–2 | COVERED |
| RESEARCH | — | Exact identity and fail-closed component composition | Tasks 1–2 | COVERED |
| VERIFICATION | Gap 1 | Real installed executable and Plan 164-27 tuple | Task 1 | COVERED |
| VERIFICATION | Gap 2 | Default exclusion, no hard-coded Node path, exhaustive suite contract | Task 2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact tuple equality, hard missing states, stable validation order | Tasks 1–2 | COVERED |
| EDGE | TRTH-01 unclassified | Explicit no-change assumption | must_haves.flagged_assumptions | COVERED-FLAGGED |
| PROHIBITION | D-09/D-10/D-11 and T-164-109 | Descriptor-less lifecycle prohibitions retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-29-SUMMARY.md` when done. Do not run canonical pre-verification or terminal finalization.
</output>
