---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 12
type: execute
wave: 10
depends_on: [164-11]
files_modified: []
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/TRTH-03: A fresh ignored report is regenerated in canonical tmp for the exact clean protected main containing the tracked freshness repair."
    - "D-11/TRTH-03: The report independently revalidates the normal successful push CI run and naturally scheduled authoritative control evidence against the same exact SHA."
    - "D-12/TRTH-03: The canonical complete ledger passes through the shared validator and stable porcelain remains empty after every report write."
    - "The final verifier checks report identity, status, component provenance, ignore attribution, and canonical cleanliness independently of the closeout command's exit code."
  artifacts:
    - path: "tmp/phase-164-closeout/report.json"
      provides: "Fresh volatile exact-main closeout evidence for the protected repair SHA"
  key_links:
    - from: "164-11-SUMMARY.md main_sha and ci_run_id"
      to: "tmp/phase-164-closeout/report.json"
      via: "production closeout invocation followed by independent jq and Git assertions"
      pattern: "head_sha|origin_main_sha|ci_run_id|components|status"
    - from: "report.components.scheduled"
      to: "scripts/scheduled_control_evidence.sh"
      via: "successful authoritative sweep with registry-specific freshness and exact provenance"
      pattern: "expected_main_sha|evidence_valid|source_run|workflow_sha"
    - from: "report.components.ledger"
      to: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv"
      via: "scripts/validate_repository_truth.exs against the canonical exact-one ledger"
      pattern: "validate_repository_truth.exs|complete_authoritative_disposition_ledger"
  prohibitions:
    - statement: "D-09 through D-12: MUST NOT edit tracked evidence, weaken a gate, dispatch or rerun automation, increase configured ages, or accept stale/mismatched/incomplete evidence to manufacture a pass report."
      status: unverified
      flagged: true
---

<objective>
Regenerate and independently verify the final volatile repository-truth report.

Purpose: Close TRTH-03 only after the tracked repair, protected integration, normal push CI, and natural scheduled evidence are all exact and current.
Output: Fresh ignored report for the Plan 164-11 protected SHA/run pair.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
@scripts/validate_repository_truth.exs
@scripts/scheduled_control_evidence.sh
@scripts/closeout_repository_truth.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and independently verify exact-main closeout proof</name>
  <precondition>Plan 164-11 completed with a recorded 40-hex `main_sha`, numeric normal-push `ci_run_id`, and authoritative natural scheduled evidence for that same protected SHA.</precondition>
  <reversibility rating="reversible">The ignored report is volatile and can be regenerated after any later protected-main change without rewriting Git history or a published contract.</reversibility>
  <files>tmp/phase-164-closeout/report.json</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/validate_repository_truth.exs
    - scripts/scheduled_control_evidence.sh
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_repository_truth_test.exs
    - test/scripts/phase_164_closeout_test.exs
    - test/scripts/scheduled_control_evidence_test.exs
    - .github/scheduled-controls.json
  </read_first>
  <action>Extract the exact `main_sha` and `ci_run_id` recorded by Plan 164-11, fetch origin, and independently require canonical branch main, local HEAD equal to that SHA, origin/main equal to that SHA, and empty stable porcelain including untracked files. Re-run the focused contracts and production ledger validator. Invoke `scripts/closeout_repository_truth.sh` with `/Users/jon/projects/mailglass`, the canonical Phase 164 ledger, the exact normal push CI run ID, and `tmp/phase-164-closeout/report.json`, supplying only read-only GitHub repository context needed by the scheduled sweep. Inspect the report whether the command exits zero or nonzero. Independently require the report's repo, branch, head/origin SHA, CI run ID, final status/reason, canonical ledger component, normal CI component, and scheduled component to agree with the live inputs. For every scheduled control, reassert evidence-valid, schedule event, completed status, main branch, exact head SHA, exact workflow SHA, and allowed result status; rely on the authoritative sweep verdict for registry-specific freshness rather than recomputing a blanket age. Confirm `git check-ignore -v` attributes the report and components to the existing root tmp rule, then recheck stable porcelain. Preserve any truthful pending/cannot-check/blocked report and halt instead of mutating tracked files or remote controls.</action>
  <verify>
    <automated>summary=.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md; report=tmp/phase-164-closeout/report.json; main_sha=$(grep -Eo 'main_sha=[0-9a-f]{40}' "$summary" | head -1 | cut -d= -f2); ci_run_id=$(grep -Eo 'ci_run_id=[1-9][0-9]*' "$summary" | head -1 | cut -d= -f2); test -n "$main_sha" &amp;&amp; test -n "$ci_run_id" &amp;&amp; test -s "$report" &amp;&amp; jq -e --arg repo "/Users/jon/projects/mailglass" --arg sha "$main_sha" --arg run "$ci_run_id" '.status == "pass" and .reason == "all_authorities_exact_and_current" and .repo == $repo and .branch == "main" and .head_sha == $sha and .origin_main_sha == $sha and .ci_run_id == $run and .components.git.status == "pass" and .components.ledger.status == "pass" and .components.ci.status == "pass" and .components.scheduled.status == "pass" and .components.scheduled.evidence_valid == true' "$report" &amp;&amp; git check-ignore -q "$report" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)"</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 164-11 summary supplies one parseable `main_sha` and `ci_run_id`; both are independently matched to live Git, CI, and report data.
    - The report is newly captured under the canonical ignored tmp boundary for the exact protected SHA containing Plan 164-10.
    - The ledger component uses `scripts/validate_repository_truth.exs` against the exact canonical ledger and passes complete exact-one disposition validation.
    - CI is the recorded normal push run with exact headSha, terminal completed status, and success conclusion.
    - The scheduled component is the successful authoritative sweep for the same SHA; each control retains exact event, branch, status, workflow-SHA, evidence-valid provenance, and allowed result checks.
    - Daily evidence accepted inside 129600 seconds is not rejected by a second three-hour calculation, and no configured maximum age is changed.
    - `git check-ignore -v` attributes the report to the existing root tmp rule and final stable porcelain remains empty after all writes.
    - No tracked snapshot, dispatch, rerun, merge, force-push, release, publish, authorization, workflow, registry, dependency, or ignore-rule change occurs.
  </acceptance_criteria>
  <done>The independently checked ignored report proves the repaired repository's exact current protected, CI, scheduled-control, ledger, and cleanliness state.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tmp/phase-164-closeout/report.json` — regenerated volatile `schema`, `captured_at`, canonical repository identity, exact SHA/run identity, component evidence, final `status`, and `reason`.
- No tracked snapshot, source symbol, CLI flag, API, database schema, UI, dependency, workflow, release, tag, publication, authorization, registry value, or ignore rule.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checkpoint handoff → live Git/GitHub state | Recorded values are independently revalidated rather than trusted by provenance alone. |
| Component JSON → final quiet verdict | Every identity, provenance, status, and result envelope must agree before pass. |
| Ignored report writes → canonical cleanliness | Runtime evidence must remain under the existing narrow ignore boundary and leave stable porcelain empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-44 | Spoofing | main and CI identity | high | mitigate | Re-fetch and require exact summary/live/report SHA and normal CI run equality. |
| T-164-45 | Tampering | scheduled and ledger evidence | high | mitigate | Re-run authoritative validators and independently assert exact component provenance and statuses. |
| T-164-46 | Repudiation | final quiet verdict | high | mitigate | Retain timestamped component sources/reasons in the ignored report and verify its fields independently. |
| T-164-47 | Elevation of Privilege | remote evidence controls | high | mitigate | Use read-only observation only; prohibit dispatch, rerun, merge, release, publish, and authority mutation. |
| T-164-48 | Tampering | workspace cleanliness | medium | mitigate | Require canonical ignored output and empty stable porcelain after all writes. |
| T-164-SC | Tampering | package supply chain | low | accept | No package or action installation occurs. |
</threat_model>

<verification>
Run the focused Phase 164 contracts, production ledger validator, exact documented closeout command, independent JSON identity/provenance assertions, ignore attribution, and post-write stable porcelain.
</verification>

<success_criteria>
- The report is fresh for the exact protected-main SHA containing the tracked repair.
- Normal CI and naturally scheduled evidence independently agree with that SHA.
- The canonical repository stays clean and the volatile report remains ignored.
</success_criteria>

## Prohibition Recall Audit

- Weakening tracked evidence, increasing ages, or operating remote controls to manufacture pass remains descriptor-less and flagged-unverified.
- Recalled prohibitions from Plans 164-08 through 164-11 remain binding: durable proof stays visible, canonical identities cannot be substituted, and external evidence must arise normally.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Documentation, tracked artifacts, ignore rules, and final evidence describe actual supported and operational state | 164-01..12 | COVERED |
| REQ | TRTH-01 | Current maintainer/package guidance agrees with settled authority | 164-02, 164-03, 164-06, 164-07 | COVERED-EXISTING |
| REQ | TRTH-02 | Complete evidence-backed artifact/ignore classification | 164-01, 164-04, 164-08, 164-09 | COVERED-EXISTING |
| REQ | TRTH-03 | Reproducible exact-main clean closeout with correct per-control freshness | 164-05..12 | COVERED |
| CONTEXT | D-01, D-02, D-03, D-04 | Layered current documentation, history boundary, manifest truth, one supported runbook | 164-02, 164-03 | COVERED-EXISTING |
| CONTEXT | D-05, D-06, D-07, D-08 | Complete ledger, durable proof, narrow ignores, locked stale sweep removal | 164-01, 164-04, 164-08 | COVERED-EXISTING |
| CONTEXT | D-09, D-10, D-11, D-12 | Volatile report, canonical cleanliness, exact evidence, complete authoritative ledger | 164-05..12 | COVERED |
| RESEARCH | Existing tools, dual proof, no dependency/service/workflow/release expansion | 164-05..12 | COVERED |
| VERIFICATION | Original three gaps | Shared ledger, canonical boundaries, and final exact-main report | 164-08, 164-09, 164-12 | COVERED |
| VERIFICATION | Freshness gap | Tracked repair, protected integration, natural exact-SHA schedules, regenerated report | 164-10, 164-11, 164-12 | COVERED |
| PROHIBITION | No false, forced, or authority-expanded proof | 164-10..12 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-12-SUMMARY.md` when done; do not commit `tmp/phase-164-closeout/report.json`.
</output>
