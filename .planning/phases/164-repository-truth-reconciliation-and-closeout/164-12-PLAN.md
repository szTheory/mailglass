---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 12
type: execute
wave: 10
depends_on: [164-11]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - "D-09/D-10/TRTH-03: Before ordinary goal verification, the complete tracked implementation through Plan 164-11 is normally integrated on protected main and sampled without a later implementation change."
    - "D-11/TRTH-03: `/finalize-phase 164 --pre-verification` proves that implementation SHA with attempt-1 normal push CI and attempt-1 natural scheduled evidence."
    - "The ignored pre-verification report is evidence for the verifier, not the terminal report; Plan 164-12 SUMMARY and later completion metadata intentionally invalidate it for final-state purposes."
  artifacts:
    - path: "tmp/phase-164-closeout/pre-verification-report.json"
      provides: "Ignored live operational proof for the protected implementation SHA consumed by ordinary phase verification"
  key_links:
    - from: "164-11-SUMMARY.md on protected main"
      to: "/finalize-phase 164 --pre-verification"
      via: "explicit protected integration checkpoint"
      pattern: "pre-verification"
    - from: "pre-verification-report.json raw sources"
      to: "164-VERIFICATION.md"
      via: "verifier independently checks exact identity/provenance and permits only the later 164-12 summary metadata delta"
      pattern: "components.ci.source|components.scheduled.source"
  prohibitions:
    - statement: "MUST NOT represent pre-verification evidence as terminal evidence, dispatch/rerun workflows, accept attempts greater than 1, push/merge automatically, alter authority/ages, or edit tracked files during capture."
      status: unverified
      flagged: true
---

<objective>
Supply the non-circular live evidence ordinary Phase 164 verification needs.

Purpose: Prove the protected implementation before the verifier writes tracked completion metadata, while reserving the terminal capture for the later final metadata SHA.
Output: An ignored pre-verification report and raw sources for the exact protected implementation SHA.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
@scripts/finalize_phase_164.sh
@scripts/closeout_repository_truth.sh
@.github/scheduled-controls.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Integrate the implementation and capture exact pre-verification evidence</name>
  <precondition>Plans 164-10 and 164-11 are locally complete with summaries and all focused/full verification green.</precondition>
  <reversibility rating="reversible">The capture is ignored and regenerable; protected integration uses the repository's normal reviewed path and is not performed by the finalizer.</reversibility>
  <files>tmp/phase-164-closeout/pre-verification-inputs.json, tmp/phase-164-closeout/pre-verification-report.json</files>
  <action>Pause for normal protected integration of all tracked Phase 164 implementation and summaries through `164-11-SUMMARY.md`. Do not push directly to protected main, auto-merge, dispatch, or rerun automation. Once local branch `main`, HEAD, and fetched `origin/main` are the same clean SHA, wait for its normal attempt-1 push CI and natural attempt-1 scheduled controls. Run `ASDF_NODEJS_VERSION=22.14.0 gsd --print --no-session "/finalize-phase 164 --pre-verification"`. Preserve any truthful pending/cannot-check/blocked result and wait for naturally produced evidence rather than operating workflow controls. On success, independently inspect the ignored report and raw sources per `164-FINALIZATION.md`. Record the exact implementation SHA and evidence paths in the tracked Plan 164-12 SUMMARY only after the capture; explicitly state that this summary makes the capture non-terminal but does not change implementation behavior.</action>
  <instructions>
    1. Integrate the completed implementation and `164-10-SUMMARY.md`/`164-11-SUMMARY.md` through the repository's normal protected review path; the executor does not push, merge, dispatch, or rerun.
    2. In `/Users/jon/projects/mailglass`, fetch `origin main` and require branch `main`, local HEAD equal to `origin/main`, and empty `git status --porcelain=v1 --untracked-files=all`.
    3. Wait for normal attempt-1 push CI and natural attempt-1 schedules for that exact SHA, then run `ASDF_NODEJS_VERSION=22.14.0 gsd --print --no-session "/finalize-phase 164 --pre-verification"`.
    4. Run the independent raw-source verification below. If any control is pending, cannot-check, blocked without valid policy evidence, stale, mismatched, or attempt greater than 1, leave the checkpoint open; do not operate a remote control to change the result.
  </instructions>
  <verification>report=tmp/phase-164-closeout/pre-verification-report.json; inputs=tmp/phase-164-closeout/pre-verification-inputs.json; test -s "$report" &amp;&amp; test -s "$inputs" &amp;&amp; main_sha=$(jq -er '.main_sha | select(test("^[0-9a-f]{40}$"))' "$inputs") &amp;&amp; ci_run_id=$(jq -er '.ci_run_id | tostring | select(test("^[1-9][0-9]*$"))' "$inputs") &amp;&amp; ci_source=$(jq -er '.components.ci.source' "$report") &amp;&amp; scheduled_source=$(jq -er '.components.scheduled.source' "$report") &amp;&amp; case "$(cd "$(dirname "$ci_source")" &amp;&amp; pwd -P)/$(basename "$ci_source")" in /Users/jon/projects/mailglass/tmp/phase-164-closeout/*) ;; *) exit 1 ;; esac &amp;&amp; case "$(cd "$(dirname "$scheduled_source")" &amp;&amp; pwd -P)/$(basename "$scheduled_source")" in /Users/jon/projects/mailglass/tmp/phase-164-closeout/*) ;; *) exit 1 ;; esac &amp;&amp; jq -e --arg sha "$main_sha" --arg run "$ci_run_id" '.databaseId | tostring == $run and .workflowName == "CI" and .attempt == 1 and .event == "push" and .headBranch == "main" and .headSha == $sha and .status == "completed" and .conclusion == "success"' "$ci_source" &amp;&amp; jq -e --arg sha "$main_sha" --slurpfile registry .github/scheduled-controls.json '.status == "pass" and .evidence_valid == true and .expected_main_sha == $sha and ([.controls[].control] | sort) == ($registry[0].controls | map(.id) | sort) and all(.controls[]; .evidence_valid == true and .source_run.attempt == 1 and .source_run.event == "schedule" and .source_run.head_branch == "main" and .source_run.status == "completed" and .source_run.head_sha == $sha and .result.workflow_sha == $sha and (.result.reason | type == "string" and length &gt; 0) and (.result.payload_sha256 | test("^[0-9a-f]{64}$")) and (.result.artifact_archive_digest | test("^sha256:[0-9a-f]{64}$")) and (.result.status == "pass" or .result.status == "blocked"))' "$scheduled_source" &amp;&amp; test "$(git rev-parse HEAD)" = "$main_sha" &amp;&amp; test "$(git rev-parse refs/remotes/origin/main)" = "$main_sha" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)"</verification>
  <verify>
    <automated>report=tmp/phase-164-closeout/pre-verification-report.json; test -s "$report" &amp;&amp; jq -e '.status == "pass" and .components.ci.status == "pass" and .components.scheduled.status == "pass" and (.components.ci.source | type == "string" and length &gt; 0) and (.components.scheduled.source | type == "string" and length &gt; 0)' "$report" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)"</automated>
  </verify>
  <resume-signal>Resume after the protected implementation SHA has a successful `/finalize-phase 164 --pre-verification` capture; provide the exact SHA and report path. If evidence is pending, leave the checkpoint open and report the natural control still awaited.</resume-signal>
  <acceptance_criteria>
    - Protected main contains every implementation artifact and summary through Plan 164-11 before capture.
    - The command selected no caller-provided run ID and accepted only attempt-1 CI/scheduled evidence for that SHA.
    - Raw CI and scheduled sources pass the independent verification contract.
    - Stable porcelain remains empty; only ignored pre-verification runtime files are written.
    - The later verifier may accept this report for implementation behavior only when the diff from its SHA to verifier HEAD contains no implementation file and is limited to the Plan 164-12 summary/metadata handoff.
  </acceptance_criteria>
  <done>Ordinary phase verification has fresh protected evidence for the complete implementation and can run without depending on terminal post-completion capture.</done>
</task>

</tasks>

<verification>
Confirm the exact protected implementation SHA, attempt-1 raw CI/scheduled provenance, ignored report location, and empty stable porcelain. Ordinary verification must independently re-read the report sources rather than trust this checkpoint's exit code.
</verification>

<success_criteria>
- Phase verification has fresh operational evidence before phase.complete.
- The checkpoint does not claim terminal final-state evidence.
- No remote control or tracked implementation mutation is used to manufacture a pass.
</success_criteria>

## Prohibition Recall Audit

- Protected integration is a blocking human action through the normal reviewed path; it is not authority for direct push or automatic merge.
- Attempts greater than 1, manual dispatch/rerun, age expansion, and treating the pre-verification report as terminal remain flagged-unverified.

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-12-SUMMARY.md` after the checkpoint succeeds. The subsequent verifier must inspect the pre-verification report, then normal phase.complete metadata must be integrated before the terminal `/finalize-phase 164` rerun.
</output>
