---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 41
type: execute
wave: 38
depends_on: [164-40]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Completed-plan files_modified metadata is structurally parsed regardless of key ordering, while only explicit [] means no modified files."
    - "Missing or malformed completed-plan metadata returns a stable tagged API error and a bounded exit-1 CLI diagnostic."
    - "An existing non-Git repository root is rejected without a MatchError or stack trace, while a plain authority directory remains valid."
  artifacts:
    - path: scripts/validate_repository_truth.exs
      provides: "Structural plan metadata parsing and tagged Git-root validation"
    - path: test/scripts/phase_164_repository_truth_test.exs
      provides: "Public API and standalone CLI regressions for metadata and non-Git roots"
  key_links:
    - from: scripts/validate_repository_truth.exs
      to: "completed PLAN.md frontmatter"
      via: "plan_files_modified/1 structural parser"
    - from: scripts/validate_repository_truth.exs
      to: "git rev-parse/ls-files"
      via: "early Git identity validation and tagged command errors"
  flagged_assumptions:
    - "TRTH-02 adjacency: files_modified may be adjacent to or separated from autonomous by other frontmatter keys; both forms must parse identically."
    - "TRTH-02 empty: only the explicit YAML value [] denotes an empty files_modified list."
    - "TRTH-02 ordering: files_modified may appear in any frontmatter key order before the closing delimiter."
  prohibitions:
    - "Contractual or forensic proof MUST NOT be hidden or reclassified to manufacture a quiet repository."
---

<objective>
Close the remaining validator fail-open paths for completed-plan metadata and non-Git roots per TRTH-02.

Purpose: Repository-truth results must derive from structurally valid evidence and return stable bounded diagnostics for invalid roots or metadata.
Output: Structural frontmatter parsing, tagged Git failures, and public API/CLI regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-40-SUMMARY.md
@scripts/validate_repository_truth.exs
@test/scripts/phase_164_repository_truth_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Parse one reordered completed-plan frontmatter path end to end</name>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - scripts/validate_repository_truth.exs — `phase_artifacts/2`, `plan_files_modified/1`, `audit_subjects/2`, and CLI error rendering
    - test/scripts/phase_164_repository_truth_test.exs — existing temporary-plan and standalone CLI fixture conventions
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md — CR-02 missing/malformed metadata counterexamples
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md — D-05 ledger completeness and D-12 exact-one disposition contracts
  </read_first>
  <behavior>
    - "A completed plan whose files_modified key is followed by intervening frontmatter keys yields its exact path list for D-05/D-12 ledger completeness."
    - "A completed plan with no files_modified key returns `{:error, {:plan_metadata_missing, relative_plan, \"files_modified\"}}`."
    - "A malformed files_modified value returns `{:error, {:plan_metadata_malformed, relative_plan, reason}}`."
  </behavior>
  <action>Replace the regex-boundary implementation with a structural line/state parser limited to the opening YAML frontmatter. Make `plan_files_modified/1` a public, tagged-result seam and thread its `{:ok, paths}` or `{:error, tag}` through `phase_artifacts/2`, `audit_subjects/2`, and `main/1` without collapsing errors. Accept scalar dash-list entries as paths, ignore unrelated keys regardless of order, and accept an empty result only when the source explicitly contains `files_modified: []`. In `test/scripts/phase_164_repository_truth_test.exs`, reuse the mapped `test/scripts/phase_162_release_reconciliation_test.exs` structure: read each fixture once, parse its record set once, assert nonempty input before field checks, and require one unique identity tuple for each expected subject with exact missing or malformed tags. Render stable relative plan paths and bounded diagnostics so D-05 ledger coverage and D-12 exact-one disposition cannot fail open.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_plan_metadata --warnings-as-errors --no-deps-check</automated>
    <fails_when>The command exits nonzero, a reordered valid list is lost, explicit [] is rejected, or missing/malformed metadata lacks its stable tag.</fails_when>
  </verify>
  <done>One reordered completed-plan path crosses fixture file, public parser, audit composition, and standalone CLI with exact tagged outcomes.</done>
  <acceptance_criteria>
    - `plan_files_modified/1` no longer uses the `files_modified`-to-`autonomous` regex boundary.
    - Reordered and non-adjacent valid frontmatter returns the exact declared path list.
    - Explicit `files_modified: []` returns `{:ok, []}`; missing and malformed values return distinct stable tags.
    - CLI failures exit 1, mention the relative plan and tag meaning, and contain neither `MatchError` nor a stack trace.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject non-Git roots and exhaust the metadata shape matrix</name>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - scripts/validate_repository_truth.exs — `ensure_repository/1`, `tracked_subjects/1`, `phase_artifacts/2`, and `main/1`
    - test/scripts/phase_164_repository_truth_test.exs — repository-root versus authority-root fixtures and CLI assertions
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md — CR-02 and CR-03 exact reproductions
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md — fail-closed Git identity guidance
  </read_first>
  <behavior>
    - "An existing directory that is not a Git work tree returns `{:error, {:invalid_git_repository, root}}` before ls-files."
    - "A non-Git authority directory remains accepted for independent ignore-policy fixtures."
    - "Nested/reordered lists parse; blank, scalar, malformed indentation, duplicate-key, missing-delimiter, and unterminated lists fail with stable metadata tags."
  </behavior>
  <action>Split directory validation from repository identity: validate `repo_root` with `git rev-parse --is-inside-work-tree` (and a stable tagged command result), while validating `authority_root` only as the directory used for policy files. Remove every `{output, 0} = System.cmd(...)` assumption reachable by untrusted fixture roots and thread tagged Git errors. Expand parser regressions across adjacency, emptiness, and ordering so no malformed completed-plan metadata silently becomes an empty artifact list. Preserve the mapped `test/scripts/phase_162_release_reconciliation_test.exs` exact-one regression pattern by parsing each metadata fixture once, asserting the valid matrix is nonempty and subject identities are unique, then checking each invalid shape against its precise failure tag rather than a generic non-success.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_git_identity --only phase_164_plan_metadata --warnings-as-errors --no-deps-check</automated>
    <fails_when>The command exits nonzero, a non-Git repository reaches ls-files, an arbitrary authority directory is rejected, or any malformed metadata case is treated as empty.</fails_when>
  </verify>
  <done>Git identity and all required completed-plan metadata shapes fail closed through both public API and standalone CLI surfaces.</done>
  <acceptance_criteria>
    - Public API returns `{:error, {:invalid_git_repository, root}}` for an existing non-Git repository root.
    - Standalone invocation for that root exits exactly 1 with one bounded diagnostic and no exception text.
    - Authority-root-only policy fixtures continue to work without requiring authority_root to be a Git repository.
    - Tests explicitly cover adjacent, non-adjacent, reordered, explicit-empty, missing, and malformed metadata.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- `RepositoryTruthLedger.plan_files_modified/1` — public structural completed-plan frontmatter parser.
- `{:plan_metadata_missing, relative_plan, "files_modified"}` — stable missing-key error contract.
- `{:plan_metadata_malformed, relative_plan, reason}` — stable malformed-value error contract.
- `{:invalid_git_repository, root}` — stable non-Git-root error contract.
- Tagged `:phase_164_plan_metadata` and `:phase_164_git_identity` public API/CLI regression groups.
</artifacts_produced>

<threat_model>
## ASVS Level

OWASP ASVS L1; high-severity metadata or Git-root failures block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| PLAN.md bytes → evidence enumeration | Tracked files can contain missing, reordered, or malformed metadata |
| caller path → Git subprocess | An existing directory may not be a Git work tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-158 | Tampering | `plan_files_modified/1` | high | mitigate | Structurally parse the opening frontmatter and reject missing/malformed metadata with stable tags. |
| T-164-159 | Denial of Service | Git subprocess handling | medium | mitigate | Validate Git identity early and replace MatchError paths with bounded tagged errors. |
| T-164-160 | Information Disclosure | standalone CLI | low | mitigate | Emit relative bounded diagnostics without exception/stack output. |
| T-164-SC | Tampering | package supply chain | high | accept | No npm, pip, or cargo package install occurs in this plan. |
</threat_model>

<verification>
- Run `mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_plan_metadata --only phase_164_git_identity --warnings-as-errors`.
- Failure condition: nonzero exit, a missing/malformed case becomes empty success, or the non-Git CLI emits exception text.
</verification>

<success_criteria>
- Completed-plan metadata is structural, order-independent, and fail-closed.
- Non-Git repository roots produce tagged API and bounded CLI failures without weakening authority-root fixtures.
- No completion-only state or terminal finalization is advanced.
</success_criteria>

## Source Coverage Audit

| Source | Item | Planned implementation | Coverage |
|---|---|---|---|
| GOAL | Tracked artifacts describe actual state | Structural evidence enumeration | COVERED |
| REQ | TRTH-02 | Tasks 1–2 | COVERED |
| CONTEXT | D-05, D-12 | Fail-closed plan-derived ledger completeness | COVERED |
| RESEARCH | Structural metadata and Git identity | Parser and early validation | COVERED |
| VERIFICATION | CR-02, CR-03 | Metadata and non-Git regressions | COVERED |
| EDGE | TRTH-02 adjacency/empty/ordering | Explicit matrix and flagged assumptions | COVERED-FLAGGED |
| PROHIBITION | Preserve contractual/forensic proof | Descriptor-less transparency constraint | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-41-SUMMARY.md` after both tasks pass. Do not update completion metadata or invoke terminal finalization.
</output>
