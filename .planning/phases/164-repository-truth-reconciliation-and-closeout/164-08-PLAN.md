---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 08
type: execute
wave: 6
depends_on: [164-07]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
autonomous: true
gap_closure: true
requirements: [TRTH-02]
must_haves:
  truths:
    - "D-05/D-12/TRTH-02: One reusable validator enforces the authoritative twelve-column ledger schema, exact currentness enum, stale-outcome rule, unique subjects, and complete audited-subject inventory."
    - "D-06/D-07/TRTH-02: Every required tracked proof path and every non-comment rule from all six ignore files is present exactly once, independent of ledger row order."
    - "An empty, incomplete, duplicate-subject, malformed-currentness, or stale-retain ledger fails closed with a specific nonzero result."
  artifacts:
    - path: "scripts/validate_repository_truth.exs"
      provides: "Shared ledger parser and repository-derived audited-subject validator"
      exports: ["Mailglass.RepositoryTruthLedger.parse/1", "Mailglass.RepositoryTruthLedger.audit_subjects/1", "Mailglass.RepositoryTruthLedger.validate/2"]
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Positive authoritative-ledger and adversarial parser/inventory contracts"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv"
      provides: "Authoritative ledger updated for the shared validator and preserved verification evidence"
  key_links:
    - from: "test/scripts/phase_164_repository_truth_test.exs"
      to: "scripts/validate_repository_truth.exs"
      via: "Code.require_file and direct calls to the production parser/validator"
      pattern: "RepositoryTruthLedger.*(parse|audit_subjects|validate)"
    - from: "scripts/validate_repository_truth.exs"
      to: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv"
      via: "exact schema, enum, semantic, and repository-derived completeness checks"
      pattern: "current|historical|stale"
  flagged_assumptions:
    - "TRTH-02 adjacency: subject identity is exact text after TSV parsing; adjacent or equal-looking rows are never merged, and an exact duplicate subject is rejected."
    - "TRTH-02 empty: an empty audit or header-only ledger cannot satisfy D-05/D-12 and is rejected rather than treated as vacuously complete."
    - "TRTH-02 ordering: the authoritative ledger remains diff-friendly, but validity is set-based and must not depend on the order of otherwise unique rows."
  prohibitions:
    - statement: "D-06/D-07: MUST NOT conceal, drop, or silently exempt contractual, forensic, planning, release, publish, scheduled-control, or generated-host proof to make ledger completeness pass."
      status: unverified
      flagged: true
---

<objective>
Extract the repository-truth contract into one production validator that both tests and closeout can consume.

Purpose: Remove the split-brain parser that let a shallow ledger pass while the focused test enforced stronger rules.
Output: Shared validator, adversarial tests, and authoritative ledger rows for all newly durable Phase 164 evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-04-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
@test/scripts/phase_164_repository_truth_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the exact ledger parser and audited-subject inventory</name>
  <reversibility rating="costly">The validator becomes the shared contract used by focused tests and the final closeout gate, so changing its interface requires coordinated callers.</reversibility>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-01-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-04-PLAN.md
    - .gitignore
    - mailglass_admin/.gitignore
    - mailglass_inbound/.gitignore
    - reference/demo_app/.gitignore
    - reference/host_app/.gitignore
    - test/example/.gitignore
  </read_first>
  <behavior>
    - Test 1: `parse/1` accepts only the exact twelve-column header, twelve fields per row, nonblank required fields, dispositions retain/update/archive/remove/ignore, and currentness current/historical/stale.
    - Test 2: forged prefix values such as current-forged and stale-looking variants fail exact-enum validation.
    - Test 3: stale plus retain/ignore fails; stale plus update/archive/remove is eligible when all other fields are complete.
    - Test 4: duplicate adjacent subjects, header-only input, missing audited subjects, and a one-row incomplete ledger fail.
    - Test 5: `audit_subjects/1` derives ignore subjects from all six committed ignore files, durable tracked proof from Git, Phase 164 plan-owned artifacts, and the authoritative verification artifact; validation is invariant to row ordering.
  </behavior>
  <action>Write the adversarial cases RED, then extract the parser and audited-subject construction from `Mailglass.Scripts.Phase164RepositoryTruthTest` into `scripts/validate_repository_truth.exs` as `Mailglass.RepositoryTruthLedger`. Provide pure `parse/1` plus repository-aware `audit_subjects/1` and `validate/2`, and a CLI entry that exits nonzero with a specific error category. Enforce exact equality for `currentness` against `current`, `historical`, and `stale`; apply the stale-outcome rule only to the exact `stale` value. Derive the required inventory from the six ignore files, the existing durable proof list, `git ls-files .planning/publish`, Phase 164 plan `files_modified` entries, and `164-VERIFICATION.md`. Require every derived subject exactly once while allowing no duplicate subject anywhere. Have the ExUnit module require and exercise the production module rather than retaining a second parser. Keep row validation independent of ordering and do not add a package or dependency.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_repository_truth_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - The focused test imports `Mailglass.RepositoryTruthLedger` from `scripts/validate_repository_truth.exs`; no second `parse_ledger`/`row_from_fields` implementation remains in the test module.
    - Exact currentness mutations, stale-retain, duplicate adjacent subjects, header-only input, one-row input, and removal of each required subject class return errors under test.
    - Shuffling valid ledger rows does not change the validation result.
    - `mix run scripts/validate_repository_truth.exs -- --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv` is the production validation seam and exits zero only for the complete ledger.
  </acceptance_criteria>
  <done>The focused contract and production closeout have one exact, complete ledger authority to share.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile authoritative ledger rows with the extracted validator</name>
  <reversibility rating="reversible">Ledger rows are tracked evidence and can be corrected through review without changing the fixed schema.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/validate_repository_truth.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-PLAN.md
    - test/scripts/phase_164_repository_truth_test.exs
  </read_first>
  <action>Run the extracted inventory against the authoritative ledger and add or correct only the rows it identifies as missing or factually stale. Add complete tracked-evidence rows for `scripts/validate_repository_truth.exs` and the preserved `164-VERIFICATION.md`; correct the existing closeout script, closeout test, and closeout contract rows from their pre-implementation planned state to their actual tracked/current state. Preserve D-08's stable ID, digest, and remove outcome byte-for-byte in meaning, preserve every ignore-rule subject, and do not renumber unrelated stable IDs. Each changed row must name an actual producer, authority, durable consumer or explicit none, evidence reference, exact currentness enum, and one D-12 disposition.</action>
  <verify>
    <automated>mix run scripts/validate_repository_truth.exs -- --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; mix test test/scripts/phase_164_repository_truth_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - The production validator reports no missing audited subject, duplicate subject, blank field, invalid currentness, invalid disposition, or stale-without-outcome error.
    - `scripts/validate_repository_truth.exs` and `164-VERIFICATION.md` each have exactly one complete tracked/current retain row.
    - Existing subjects for `scripts/closeout_repository_truth.sh`, `test/scripts/phase_164_closeout_test.exs`, and `164-CLOSEOUT.md` report their actual tracked state rather than a planned/untracked state.
    - The D-08 subject still has digest `331810b4b1724452f0e2707c800230e52fabea01c3773d362b3a1240040ece7e` and disposition `remove`.
  </acceptance_criteria>
  <done>The authoritative ledger passes the same complete contract the final closeout will invoke.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/validate_repository_truth.exs` — new module `Mailglass.RepositoryTruthLedger`; functions `parse/1`, `audit_subjects/1`, and `validate/2`; CLI flags `--repo` and `--ledger`.
- `test/scripts/phase_164_repository_truth_test.exs` — adversarial cases for exact currentness, stale outcomes, duplicates, empty/incomplete ledgers, missing inventory, and row-order invariance.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv` — new tracked rows for the validator and `164-VERIFICATION.md`, plus corrected actual-state fields for existing closeout artifacts.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md` — preserved unchanged as authoritative workflow evidence and tracked by the planning commit; Plan 164-08 only inventories it.
- No API, database schema, UI, dependency, workflow, or external-service integration is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TSV bytes → repository truth verdict | Malformed or semantically forged rows must not be interpreted as complete evidence. |
| Git/index/ignore state → audited subject inventory | A hand-maintained subset must not omit durable proof or ignore rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-22 | Tampering | currentness/disposition fields | high | mitigate | Exact enum parsing plus stale-outcome tests in the shared production validator. |
| T-164-23 | Repudiation | audited-subject completeness | high | mitigate | Derive required subjects from Git, plans, proof paths, verification evidence, and six ignore files. |
| T-164-24 | Tampering | duplicate/adjacent ledger rows | medium | mitigate | Reject exact duplicate subjects independent of adjacency or row ordering. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run the production validator and focused ExUnit contract; then run `mix format --check-formatted scripts/validate_repository_truth.exs test/scripts/phase_164_repository_truth_test.exs` and `git diff --check`.
</verification>

<success_criteria>
- One implementation defines ledger validity for tests and closeout.
- Exact enum and complete-inventory adversarial fixtures fail closed.
- The authoritative ledger describes the actual tracked Phase 164 evidence set.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less: concealment or silent exemption of durable proof remains flagged-unverified under `must_haves.prohibitions`.
- Relevant edge probes are surfaced and resolved explicitly in `must_haves.flagged_assumptions` and Task 1 behavior; no unrelated collection semantics are invented.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts and ignore rules describe actual repository state | 164-08 | COVERED |
| REQ | TRTH-02 | Complete evidence-backed classification and discoverable proof | 164-08 | COVERED |
| CONTEXT | D-05, D-06, D-07, D-12 | Full ledger fields, durable proof, narrow ignores, exact-one outcome | 164-08 | COVERED |
| RESEARCH | one-row ledger / two-authority proof | Shared machine-checkable ledger validator | 164-08 | COVERED |
| VERIFICATION | gap 1 and exact-currentness warning | Authoritative path, complete inventory, exact enum, negative fixtures | 164-08 | COVERED |
| EDGE | TRTH-02 adjacency / empty / ordering | Exact duplicate rejection, non-vacuous input, set-based validation | 164-08 | COVERED-ASSUMPTION |
| PROHIBITION | Preserve durable proof visibility | Descriptor-less and flagged-unverified | 164-08 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md` when done.
</output>
