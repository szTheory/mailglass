---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 10
type: execute
wave: 8
depends_on: [164-09]
files_modified:
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-09/D-11/TRTH-03: Closeout consumes the successful authoritative scheduled-control sweep verdict without applying a second blanket age window to each control."
    - "D-11/TRTH-03: Daily repo-hygiene and post-publish evidence older than three hours remains acceptable when the registry-specific 129600-second freshness authority has already accepted it."
    - "D-11/TRTH-03: Exact main SHA, schedule event, main branch, completed status, workflow SHA, evidence-valid provenance, and allowed pass/policy-blocked result checks remain mandatory."
    - "No configured max_age_seconds value, workflow authority, result-status policy, or provenance identity contract is broadened or weakened."
  artifacts:
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Closeout scheduled-component consumer that trusts the authoritative per-control freshness decision and retains identity/provenance validation"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Runtime regression contract for registry-specific freshness plus exact identity/provenance rejection"
  key_links:
    - from: "scripts/closeout_repository_truth.sh"
      to: "scripts/scheduled_control_evidence.sh"
      via: "sweep exit/status/evidence_valid is the sole freshness decision consumed by closeout"
      pattern: "scheduled_control_evidence.sh sweep|evidence_valid"
    - from: "test/scripts/phase_164_closeout_test.exs"
      to: "scripts/closeout_repository_truth.sh"
      via: "sourceable scheduled-report predicate exercised with authoritative sweep fixtures"
      pattern: "scheduled_report_is_acceptable"
  prohibitions:
    - statement: "D-09/D-11: MUST NOT increase registry freshness limits, manually dispatch or rerun a workflow, change workflow authority, or remove exact SHA/event/branch/status/workflow-SHA/provenance checks to obtain a pass."
      status: unverified
      flagged: true
---

<objective>
Repair the closeout freshness consumer through a focused red-green contract.

Purpose: Eliminate the duplicate three-hour policy that rejects evidence the authoritative per-control sweep has already found current.
Output: Tracked closeout-script and ExUnit repair ready for normal protected integration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-09-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
@.github/scheduled-controls.json
@scripts/scheduled_control_evidence.sh
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make authoritative per-control freshness flow end to end</name>
  <reversibility rating="reversible">The repair is confined to one internal Bash predicate and its focused contract; reverting restores the prior consumer without migrating data or changing a published interface.</reversibility>
  <files>scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-09-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - .github/scheduled-controls.json
    - scripts/scheduled_control_evidence.sh
    - scripts/closeout_repository_truth.sh
    - test/scripts/scheduled_control_evidence_test.exs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - RED 1: A sweep fixture for one exact 40-hex main SHA, with all three controls evidence-valid and daily control updated_at values older than three hours but younger than their configured 129600-second limits, is accepted by the production closeout predicate.
    - RED 2: Mutating expected_main_sha, source_run.head_sha, source_run.event, source_run.head_branch, source_run.status, result.workflow_sha, control evidence_valid, or the allowed result status makes that same fixture fail.
    - RED 3: A top-level pending/cannot-check/non-evidence-valid sweep fails even when its nested controls otherwise resemble valid evidence.
    - GREEN: The consumer delegates freshness to the successful authoritative sweep and retains every existing identity, terminal-state, provenance, and policy-result assertion.
  </behavior>
  <action>First add the runtime regression cases against production code and run them to record RED. Extract only the scheduled-report acceptance predicate in `scripts/closeout_repository_truth.sh` into a sourceable `scheduled_report_is_acceptable(report_path, expected_sha)` shell function; wrap the existing CLI body in `main` with a standard `BASH_SOURCE` execution guard so ExUnit can call that predicate without collecting live components. Build the positive fixture dynamically with release-please evidence inside its 10800-second registry window and both daily controls older than three hours but inside 129600 seconds; mark it as the successful, evidence-valid output of the authoritative sweep. Change the scheduled component branch to call this predicate after `scripts/scheduled_control_evidence.sh sweep` succeeds. Remove only the consumer's duplicated timestamp comparison. Keep the top-level exact expected-main identity and successful authoritative verdict, plus each control's evidence-valid flag, scheduled event, completed status, main branch, exact head SHA, exact workflow SHA, and pass or evidence-backed policy-blocked result shape. Do not edit `.github/scheduled-controls.json`, `scripts/scheduled_control_evidence.sh`, workflows, authorization, dispatch behavior, or any configured age value. Run the focused suite through GREEN, then refactor only for shell clarity while preserving behavior.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/closeout_repository_truth.sh</automated>
  </verify>
  <acceptance_criteria>
    - The focused contract executes `scheduled_report_is_acceptable` from the production script; it does not duplicate the production jq predicate in ExUnit.
    - The positive fixture contains both daily controls with timestamps beyond three hours and is accepted because the authoritative sweep verdict is successful and evidence-valid.
    - Each exact identity/event/branch/status/workflow-SHA/provenance mutation is rejected independently under test.
    - `scripts/closeout_repository_truth.sh` no longer calculates one age threshold across every nested control.
    - `.github/scheduled-controls.json` still declares 10800 for release-please and 129600 for each daily control, and `scripts/scheduled_control_evidence.sh` remains the only code that compares control timestamps with those registry values.
    - No manual dispatch, rerun, workflow-authority, release, publish, ignore-rule, package, dependency, or unrelated file change is introduced.
  </acceptance_criteria>
  <done>The tracked closeout repair accepts authoritative registry-specific freshness while failing closed on every existing exact identity and provenance invariant.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/closeout_repository_truth.sh` — sourceable `scheduled_report_is_acceptable(report_path, expected_sha)` plus guarded `main`, with registry freshness delegated to the authoritative sweep.
- `test/scripts/phase_164_closeout_test.exs` — dynamic authoritative-sweep fixture and adversarial exact identity/provenance mutations.
- No registry value, workflow, authorization rule, dispatch path, release path, ignore rule, dependency, API, schema, or UI artifact changes.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authoritative scheduled sweep → closeout consumer | A valid per-control freshness result must survive composition without being replaced by a contradictory local age policy. |
| Sweep JSON bytes → quiet verdict | Malformed or spoofed SHA/event/branch/status/workflow/provenance fields must not gain a pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-36 | Tampering | duplicated freshness predicate | high | mitigate | Delegate age comparison exclusively to the registry-driven sweep and regression-test daily evidence beyond three hours. |
| T-164-37 | Spoofing | scheduled run identity | high | mitigate | Retain exact expected-main, head SHA, event, branch, terminal status, and workflow-SHA assertions with one-field mutation tests. |
| T-164-38 | Repudiation | evidence-valid provenance/result | high | mitigate | Require successful sweep, top-level and per-control evidence validity, and the established allowed result envelope. |
| T-164-39 | Elevation of Privilege | GitHub workflow controls | medium | mitigate | Modify no workflow, dispatch, rerun, authorization, release, or publication surface. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run the focused closeout and scheduled-control contracts, Bash syntax validation, `mix format --check-formatted test/scripts/phase_164_closeout_test.exs`, and `git diff --check`.
</verification>

<success_criteria>
- The TDD contract fails before the repair and passes after it.
- Registry-specific freshness is authoritative at the closeout boundary.
- All exact identity and provenance checks remain enforced.
</success_criteria>

## Prohibition Recall Audit

- Manual evidence manufacture, configured-age increases, and workflow-authority changes remain descriptor-less and flagged-unverified.
- The repair changes only the duplicate consumer policy; it does not reinterpret a policy-blocked result or relax protected identity.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence describes the repository's actual operational state | 164-10 | COVERED |
| REQ | TRTH-03 | Rerunnable closeout composes explained scheduled outcomes without false rejection | 164-10 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Volatile proof, exact canonical identity, exact CI/scheduled provenance | 164-10 | COVERED |
| CONTEXT | D-01..D-08, D-12 | Completed documentation and ledger decisions remain unchanged | 164-01..09 | COVERED-EXISTING |
| RESEARCH | Existing scheduled verifier remains authority; no new service/dependency | 164-10 | COVERED |
| VERIFICATION | Freshness-gap repair | Registry-specific accepted ages and retained exact provenance | 164-10 | COVERED |
| PROHIBITION | No manufactured evidence or authority expansion | 164-10 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md` when done.
</output>
