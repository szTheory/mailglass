---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 23
type: execute
wave: 21
depends_on: [164-22]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low
user_setup:
  - service: local-command-installation
    why: "Approve the exact immutable identity before the executor places a trust-root executable outside the mutable repository"
    dashboard_config:
      - task: "Review the captured OID, SHA-256, destination, prior-destination disposition, rollback path/digest when applicable, and mode 0500; the executor performs the installation after approval"
        location: "Plan 164-23 blocking-human checkpoint"
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: `/Users/jon/.local/bin/mailglass-finalize-phase` is a regular non-symlink executable outside `/Users/jon/projects/mailglass` whose bytes equal the captured committed loader blob."
    - "TRTH-03: Installation records the source commit OID and SHA-256, rejects a dirty or noncanonical source checkout, and replaces no unexpected destination object."
    - "TRTH-03: Any eligible prior Mailglass loader is preserved byte-for-byte at a private mode-0400 rollback path with verified digest before replacement."
  artifacts:
    - path: "/Users/jon/.local/bin/mailglass-finalize-phase"
      provides: "User-installed pre-evaluation authority executable (external operational state, sourced from tracked Git bytes)"
    - path: "/Users/jon/.local/share/mailglass/rollback/"
      provides: "Conditional private recoverable copy and provenance for a replaced prior Mailglass loader"
  key_links:
    - from: "captured commit:scripts/mailglass_finalize_phase_loader.mjs"
      to: "/Users/jon/.local/bin/mailglass-finalize-phase"
      via: "private temporary extraction, byte/digest comparison, non-symlink destination check, mode-0500 atomic install"
      pattern: "git show|shasum -a 256|install -m 0500"
  flagged_assumptions:
    - "TRTH-03 empty/ordering probes: installation must have one non-empty committed source blob and completes verification before any installed invocation; an absent or pre-existing unexpected destination pauses rather than being overwritten."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT install loader bytes read directly from a dirty working tree or overwrite a symlink/unexpected destination."
      status: unresolved
      flagged: true
---

<objective>
Install the authenticated loader outside the checkout and verify its exact provenance.

Purpose: Cross the approved external trust-boundary step required before the old project extension can be replaced operationally.
Output: One user-owned mode-0500 installed executable byte-identical to a captured Git commit blob.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the exact immutable installation identity</name>
  <reversibility rating="reversible">This preflight reads committed bytes and destination metadata without changing the installed command.</reversibility>
  <files>No files modified; source is scripts/mailglass_finalize_phase_loader.mjs and the proposed destination is /Users/jon/.local/bin/mailglass-finalize-phase</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>From `/Users/jon/projects/mailglass`, require the canonical physical root, an empty stable porcelain result, and one full `HEAD^{commit}` OID. Resolve `/Users/jon/.local/bin` physically and prove it is outside the repository. Inspect the proposed destination without following it: absence is acceptable; a symlink, directory, non-regular object, or regular file whose digest differs from the captured committed loader blob is a hard stop. Thus an eligible prior destination has immutable provenance at the captured installation OID rather than a spoofable content marker. For an eligible prior loader, record its SHA-256 and mode and derive a collision-resistant private rollback path beneath `/Users/jon/.local/share/mailglass/rollback/`; preflight names but does not create that backup. Extract `scripts/mailglass_finalize_phase_loader.mjs` from the captured OID into a mode-0600 private temporary file, require non-empty bytes, compute its SHA-256, re-check checkout HEAD and porcelain, validate every approval-tuple field, then remove the file and directory and assert both are absent. Present the exact installation OID, source digest, destination, destination disposition, intended mode 0500, and conditional prior digest/mode/rollback path as one approval tuple. Do not alter the destination or run any loader mode during this preflight.</action>
  <verify>
    <automated>set -eu; repo=/Users/jon/projects/mailglass; dest=/Users/jon/.local/bin/mailglass-finalize-phase; test "$(cd "$(git -C "$repo" rev-parse --show-toplevel)" &amp;&amp; pwd -P)" = "$repo"; test -z "$(git -C "$repo" status --porcelain=v1 --untracked-files=all)"; install_oid="$(git -C "$repo" rev-parse --verify 'HEAD^{commit}')"; test "${#install_oid}" = 40; case "$install_oid" in *[!0-9a-f]*) exit 1;; esac; dest_parent="$(cd "$(dirname "$dest")" &amp;&amp; pwd -P)"; case "$dest_parent/" in "$repo/"*) exit 1;; esac; tmp_root="$(mktemp -d "${TMPDIR:-/tmp}/mailglass-install-preflight.XXXXXX")"; tmp_loader="$tmp_root/loader"; trap 'rm -f "$tmp_loader"; rmdir "$tmp_root" 2&gt;/dev/null || true' EXIT; git -C "$repo" show "$install_oid:scripts/mailglass_finalize_phase_loader.mjs" &gt;"$tmp_loader"; chmod 0600 "$tmp_loader"; test -s "$tmp_loader"; source_digest="$(shasum -a 256 "$tmp_loader" | awk '{print $1}')"; test "${#source_digest}" = 64; disposition=absent; prior_digest=none; prior_mode=none; rollback_path=none; if test -e "$dest" || test -L "$dest"; then test ! -L "$dest"; test -f "$dest"; prior_digest="$(shasum -a 256 "$dest" | awk '{print $1}')"; test "$prior_digest" = "$source_digest"; disposition=eligible-committed-loader; prior_mode="$(stat -f '%Lp' "$dest")"; rollback_path="/Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.$prior_digest"; fi; printf 'installation_source_oid=%s\nsource_sha256=%s\ndestination=%s\ndestination_disposition=%s\nprior_sha256=%s\nprior_mode=%s\nrollback_path=%s\ninstall_mode=0500\n' "$install_oid" "$source_digest" "$dest" "$disposition" "$prior_digest" "$prior_mode" "$rollback_path"; test "$(git -C "$repo" rev-parse --verify 'HEAD^{commit}')" = "$install_oid"; test -z "$(git -C "$repo" status --porcelain=v1 --untracked-files=all)"; rm -f "$tmp_loader"; rmdir "$tmp_root"; trap - EXIT; test ! -e "$tmp_root"; git -C "$repo" diff --check</automated>
    <fails_when>The checkout is noncanonical or dirty; the OID is not full/stable; the committed loader blob is absent; destination containment/type/provenance is unsafe; the approval tuple is incomplete; any finalization dispatch occurs; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - One approval tuple records a full stable source OID, committed-blob SHA-256, external destination, destination disposition, intended mode 0500, and conditional prior digest/mode/rollback path.
    - The tuple derives from `git show <captured-oid>:scripts/mailglass_finalize_phase_loader.mjs`, never checkout bytes.
    - Preflight leaves the destination unchanged and removes its private temporary.
  </acceptance_criteria>
  <done>The exact external installation proposed to the user is immutable, bounded, and fully identified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact trust-root installation tuple</name>
  <files>No files modified; approval applies only to the Task 1 OID/digest/path/mode tuple</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>Pause before mutating user-global executable state and display the Task 1 full source OID, SHA-256, exact destination `/Users/jon/.local/bin/mailglass-finalize-phase`, intended mode 0500, current destination disposition, and conditional prior digest/mode/rollback path. Approval is scoped to that exact tuple. Any mismatch, moving HEAD, dirty source, unsafe destination identity, or request to change the destination returns to Task 1 rather than being inferred.</action>
  <what-built>An authenticated, non-mutating installation proposal for a pre-evaluation Phase 164 authority command.</what-built>
  <how-to-verify>Confirm that the displayed source is the committed Plan 164-21 loader, the destination is the approved user-owned external command path, mode is 0500, no unexpected prior object will be replaced, and any prior eligible loader has an exact private rollback destination.</how-to-verify>
  <verification>The executor rechecks that the exact OID is still checkout HEAD, porcelain remains empty, the committed blob still hashes to the displayed SHA-256, and destination disposition has not changed.</verification>
  <resume-signal>Reply `approved` to authorize only the displayed tuple, or describe any mismatch to stop without installation.</resume-signal>
  <instructions>
    Review the exact OID, digest, destination, destination disposition, conditional rollback provenance, and mode shown by the executor.
    Approve only when all five fields match the authorized external trust-root boundary.
  </instructions>
  <verify>
    <automated>approved_oid="$(git rev-parse --verify 'HEAD^{commit}')" &amp;&amp; test "${#approved_oid}" = 40 &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)" &amp;&amp; git show "$approved_oid:scripts/mailglass_finalize_phase_loader.mjs" | shasum -a 256</automated>
    <fails_when>The approval-time OID is not full or differs from Task 1; porcelain is nonempty; the committed blob digest differs; destination disposition changed; or any installation/finalization dispatch occurs before approval.</fails_when>
  </verify>
  <acceptance_criteria>
    - Human approval names the exact immutable tuple and is never auto-approved.
    - Any changed component invalidates the approval and leaves the destination untouched.
  </acceptance_criteria>
  <done>The exact trust-root mutation is explicitly approved or the plan stops without changing external state.</done>
</task>

<task type="auto">
  <name>Task 3: Atomically install and self-check the approved loader</name>
  <reversibility rating="reversible">Before replacement, exact prior bytes are preserved with verified digest at the approved private rollback path; rollback can atomically restore those bytes and remove the newly installed file.</reversibility>
  <files>External operational state only: /Users/jon/.local/bin/mailglass-finalize-phase and conditional /Users/jon/.local/share/mailglass/rollback/mailglass-finalize-phase.&lt;prior-sha256&gt;</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
  </read_first>
  <action>Repeat the full preflight from scratch and require every value to equal the Task 2 approved tuple. Create the user command directory and rollback directory with user-only writable permissions if absent. Extract the approved committed blob to a private temporary and verify its digest. If an eligible prior loader exists, copy its bytes without following links to the approved rollback path with mode 0400, verify the backup digest equals the approved prior digest before replacement, and refuse to overwrite an existing rollback object with different bytes. Immediately re-check checkout HEAD, porcelain, and destination disposition. Atomically install only the approved blob at `/Users/jon/.local/bin/mailglass-finalize-phase` with mode 0500; never follow or overwrite an unexpected object. Verify the installed digest, regular non-symlink identity, external containment, and mode, then run the Plan 164-21-owned `--version` and `--self-check --repo /Users/jon/projects/mailglass --expected-source-oid &lt;approved-full-oid&gt;` branches. Those branches must not spawn Bash or collect evidence. Remove every installation temporary. Record exact machine-readable `installation_source_oid`, `source_sha256`, `destination`, `mode`, `prior_destination_disposition`, `prior_sha256`, and `rollback_path` fields in `164-23-SUMMARY.md`, plus the verified atomic rollback command and cleanup rule: restoration consumes only the digest-verified private backup and removes it only after restored destination bytes and mode match the recorded prior state.</action>
  <verify>
    <automated>test -f /Users/jon/.local/bin/mailglass-finalize-phase &amp;&amp; test -x /Users/jon/.local/bin/mailglass-finalize-phase &amp;&amp; test ! -L /Users/jon/.local/bin/mailglass-finalize-phase &amp;&amp; test "$(stat -f '%Lp' /Users/jon/.local/bin/mailglass-finalize-phase)" = 500 &amp;&amp; /Users/jon/.local/bin/mailglass-finalize-phase --version &amp;&amp; /Users/jon/.local/bin/mailglass-finalize-phase --self-check --repo /Users/jon/projects/mailglass --expected-source-oid "$(git -C /Users/jon/projects/mailglass rev-parse --verify 'HEAD^{commit}')"</automated>
    <fails_when>Any tuple component changed after approval; the destination is absent, non-executable, symlinked, or not mode 0500; source/destination bytes differ; self-check observes another OID, dirty state, wrong range, repository-contained installation, or any Bash/finalization dispatch.</fails_when>
  </verify>
  <acceptance_criteria>
    - The destination is one regular non-symlink file outside the repository with exact mode 0500.
    - Installed bytes equal the approved commit blob byte-for-byte and share the approved SHA-256.
    - When a prior loader is replaced, its private mode-0400 backup exists with the approved prior digest before destination replacement, and the summary gives a verified atomic restore-and-cleanup procedure.
    - `--version` and `--self-check` pass without Bash, pre-verification, terminal finalization, or evidence collection.
    - The summary records the full approved provenance tuple for Plan 164-24.
  </acceptance_criteria>
  <done>The authoritative loader is installed outside mutable checkout evaluation with exact approved provenance and is ready for production-boundary regression.</done>
</task>

</tasks>

## Artifacts this phase produces

- `/Users/jon/.local/bin/mailglass-finalize-phase` — external user-installed mode-0500 authority executable.
- Checkpoint evidence — exact source commit OID, SHA-256, destination identity/mode, prior-destination disposition, and conditional rollback provenance (recorded in `164-23-SUMMARY.md`, not a new tracked runtime snapshot).

<threat_model>
## ASVS Level

OWASP ASVS L1; installation provenance and destination substitution are high-severity trust-root threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Captured Git blob → private install temporary | Only immutable committed bytes may enter installation. |
| Private temporary → user executable path | Destination identity, containment, permissions, and replacement provenance must be checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-84 | Tampering | loader source during installation | high | mitigate | Extract by captured OID, re-check HEAD/porcelain, and require source/destination digest equality. |
| T-164-85 | Elevation of Privilege | destination path | high | mitigate | Reject symlink/non-regular/unexpected targets, require outside-repository containment, and install mode 0500. |
| T-164-86 | Repudiation | install provenance | medium | mitigate | Record exact OID, SHA-256, path, mode, and prior destination disposition in the summary. |
| T-164-86A | Tampering | prior-loader rollback | high | mitigate | Preserve eligible prior bytes at a private mode-0400 path and verify their digest before replacement; restore atomically before cleanup. |
| T-164-SC | Tampering | external dependency | low | accept | Installation introduces no third-party package; it copies authenticated project source into a user-owned command path. |
</threat_model>

<verification>
Verify exact file type/mode, run non-dispatching version and self-check modes, and record immutable provenance. No finalization mode runs.
</verification>

<success_criteria>
- Installed executable is outside the checkout and byte-identical to one committed source blob.
- Installation cannot follow or overwrite an unexpected destination.
- Replacement preserves a byte-verifiable rollback path for the prior eligible loader.
- Non-dispatching self-check proves exact provenance and supported Phase 164 range.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational command exists in its documented trusted state | 164-23 | COVERED |
| REQ | TRTH-03 | Reproducible trusted finalization entry | 164-23 Tasks 1–3 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Installed dual-proof authority before mutable checkout execution | 164-23 Tasks 1–3 | COVERED |
| RESEARCH | exact identity and volatile final proof | Immutable source install without final report capture | 164-23 Tasks 1–3 | COVERED |
| VERIFICATION | gap 3 | Trusted installed command exists before use | 164-23 Tasks 1–3 | COVERED |
| EDGE | TRTH-03 empty/ordering | Non-empty source and install-before-use order | 164-23 flagged assumption/task | COVERED-FLAGGED |
| PROHIBITION | No dirty source or destination substitution | Descriptor-less unresolved constraint retained | 164-23 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md` with the exact OID/digest/path/mode evidence. Do not run finalization.
</output>
