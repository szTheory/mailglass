---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 27
type: execute
wave: 25
depends_on: [164-26]
files_modified:
  - test/scripts/phase_164_closeout_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low
user_setup:
  - service: local-command-reinstallation
    why: "Approve replacement of the installed Phase 164 trust root with the exact canonical-repository and trusted-toolchain loader bytes from Plan 164-26"
    dashboard_config:
      - task: "Review the source OID/digest, pinned interpreter and tool identities, destination, prior installed digest/mode/stat identity, rollback path, and mode 0500"
        location: "Plan 164-27 blocking-human checkpoint"
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: The external installed command is byte-identical to the tracked hardened loader at one approved ancestor commit and starts with the approved absolute Node interpreter."
    - "TRTH-03: Reinstallation replaces only the exact previously approved regular non-symlink loader, preserves its bytes at a private digest-addressed rollback path, and rejects all post-approval drift."
    - "TRTH-03: The installed command passes version, ancestry-aware self-check, canonical-repository rejection, forged-PATH rejection, and controlled-host boundary tests before becoming operationally ready."
    - "D-09/TRTH-03: Neither pre-verification nor terminal finalization runs during installation; terminal evidence remains ordered after ordinary verification and protected completion metadata."
  artifacts:
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Approval-tuple and installed hardened-authority readiness contract"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Explicit reinstall prerequisite and unchanged terminal lifecycle boundary"
    - path: "scripts/mailglass_finalize_phase_loader.mjs"
      provides: "Tracked immutable source whose committed bytes are the only permitted installation input"
  key_links:
    - from: "captured commit:scripts/mailglass_finalize_phase_loader.mjs"
      to: "external installed command target"
      via: "approved digest tuple, private extraction, mode-0500 atomic rename, and post-install self-check"
      pattern: "installation_source_oid|source_sha256|install_mode"
    - from: "installed command"
      to: "Plan 164-26 trust contracts"
      via: "the explicit controlled-host alias after provenance and ancestry validation"
      pattern: "verify.phase_164.installed_boundary"
  flagged_assumptions:
    - "TRTH-03 empty probe: missing proposal, approval, source blob, trusted tool, destination, or rollback evidence stops before mutation or invocation."
    - "TRTH-03 ordering probe: tracked readiness contracts commit first, approval precedes mutation, installation verification precedes any later protected-main or terminal evidence step."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT install checkout bytes, auto-approve a changed tuple, or run terminal capture from an unintegrated installation commit."
      status: unresolved
      flagged: true
---

<objective>
Install the hardened loader through a new explicit immutable approval and prove the actual external authority is ready.

Purpose: Ensure the supported command uses the canonical-repository, trusted-toolchain, and ancestry repairs before any protected-main closeout attempt.
Output: Tracked reinstall contracts, an approved recoverable external replacement, and controlled-host proof of the installed bytes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-26-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@test/scripts/phase_164_closeout_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the hardened tracked source to one reinstall-readiness contract</name>
  <files>test/scripts/phase_164_closeout_test.exs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-26-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: reinstall preflight requires the committed Plan 164-26 loader source, exact 01–28 range, canonical repository/origin constants, absolute interpreter/tool identities, and ancestry self-check.
    - Test 2: the current installed file and Plan 164-23 approval are treated as the exact prior object, not silently overwritten or mistaken for current source.
    - Test 3: lifecycle documentation requires a new Plan 164-27 approval/install before the command is called ready and continues to forbid pre-verification or terminal execution in this plan.
  </behavior>
  <action>Write a tracked `phase_164_reinstall_contract` test over source, current Plan 164-23 installation provenance, and lifecycle text. Require the hardened source to expose the loader-owned canonical path/origin, absolute interpreter/tool identities, sanitized child environment, installation-ancestry gate, and exact 01–28 range before an upgrade can be proposed. Update `164-FINALIZATION.md` to state that the older Plan 164-23 installed digest is superseded operationally only after Plan 164-27's exact approval, atomic replacement, rollback preservation, and controlled-host checks succeed. Preserve the earlier approval as immutable provenance and preserve the post-summary → ordinary verifier → protected completion metadata → exact-main terminal order. Do not change product docs, release authority, required CI topology, or claim the new external installation exists.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_reinstall_contract --warnings-as-errors --no-deps-check &amp;&amp; node --check scripts/mailglass_finalize_phase_loader.mjs &amp;&amp; git diff --check</automated>
    <fails_when>The tag selects zero tests; source trust constants or ancestry checks are absent; lifecycle text treats old bytes as current, claims installation/terminal evidence, changes ordering, any test or syntax check fails, or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - A non-vacuous tracked test identifies every Plan 164-26 security repair required in the installed bytes.
    - The Plan 164-23 tuple remains preserved as prior provenance and replacement input.
    - Lifecycle documentation names Plan 164-27 as the only reinstall authority and leaves terminal evidence pending.
  </acceptance_criteria>
  <done>The repository commits a precise reinstall contract before any external mutation is proposed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact hardened-loader replacement tuple</name>
  <files>scripts/mailglass_finalize_phase_loader.mjs, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-26-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  </read_first>
  <action>From the clean canonical checkout, capture one full `HEAD^{commit}` OID after Task 1's tracked commit. Extract `scripts/mailglass_finalize_phase_loader.mjs` from that OID, never from the worktree, and calculate its SHA-256. Verify its first line names the exact absolute Node interpreter approved by the Plan 164-26 source contract and verify every configured absolute tool path resolves to the expected safe regular executable identity. Inspect the current external destination and Plan 164-23 approval without following symlinks; require the destination to be the exact approved regular mode-0500 prior loader with matching digest and stat identity. Derive a new digest-addressed private rollback path and persist a mode-0400 Plan 164-27 proposal containing source OID/digest, destination/mode, interpreter/tool identities, prior approval digest, prior loader digest/mode/stat identity, and rollback target. Recheck source HEAD/porcelain, committed blob, trusted tools, and prior destination after proposal publication. Display the complete persisted tuple and pause. Only an explicit `approved` response may create the byte-identical mode-0400 approval record; any drift invalidates approval before mutation.</action>
  <what-built>A non-mutating proposal binding the hardened committed loader, trusted toolchain, exact prior installation, and recoverable replacement path.</what-built>
  <how-to-verify>Confirm the source OID contains Plan 164-26, the digest differs from the superseded loader as expected, the interpreter and tool paths are absolute and trusted, the destination is the exact current loader, and the rollback path is private and digest-addressed.</how-to-verify>
  <verification>The executor rereads the persisted mode-0400 approval, proves it is an exact proposal copy plus one approval field, and revalidates every source, tool, destination, and rollback input before Task 3.</verification>
  <resume-signal>Reply `approved` to authorize only the displayed Plan 164-27 tuple, or describe any mismatch to stop without replacement.</resume-signal>
  <instructions>Approve only the exact committed source OID/digest, absolute interpreter/tool identities, destination, prior-object identity, rollback path, and mode shown.</instructions>
  <verify>
    <automated>set -eu; proposal=/Users/jon/.local/share/mailglass/checkpoints/164-27-install-proposal.env; approval=/Users/jon/.local/share/mailglass/checkpoints/164-27-install-approval.env; test -f "$proposal"; test ! -L "$proposal"; test -f "$approval"; test ! -L "$approval"; test "$(stat -f '%Lp' "$proposal")" = 400; test "$(stat -f '%Lp' "$approval")" = 400; test "$(grep -c '^approval_status=approved$' "$approval")" = 1; rg -n '^installation_source_oid=[0-9a-f]{40}$|^source_sha256=[0-9a-f]{64}$|^destination=/Users/jon/.local/bin/mailglass-finalize-phase$|^install_mode=0500$|^node_executable=/|^git_executable=/|^bash_executable=/|^prior_sha256=[0-9a-f]{64}$|^rollback_path=/' "$approval"</automated>
    <fails_when>The proposal or approval is absent, symlinked, not mode 0400, malformed, not explicitly approved, missing any source/tool/prior/rollback identity, differs from the displayed tuple, or any external destination mutation occurred before approval.</fails_when>
  </verify>
  <acceptance_criteria>
    - Human approval is scoped to every immutable source, trusted-tool, destination, prior-object, and rollback field.
    - The approval record is regular, non-symlink, mode 0400, and cannot be reconstructed from later mutable observations.
    - Any changed field stops with the existing installed command untouched.
  </acceptance_criteria>
  <done>The hardened external replacement is explicitly authorized, or execution stops before mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Atomically replace, self-check, and attack the installed authority</name>
  <precondition>The exact Plan 164-27 approval record exists at mode 0400 and still matches clean canonical HEAD, the committed loader blob, trusted tool identities, and the current installed destination.</precondition>
  <reversibility rating="reversible">The exact prior loader is preserved at the approved private rollback path and can be atomically restored if post-install checks fail.</reversibility>
  <files>scripts/mailglass_finalize_phase_loader.mjs, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-26-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <action>Consume only the persisted Plan 164-27 approval record. Revalidate clean canonical HEAD, committed source digest, exact trusted tool identities, current destination lstat/digest/mode/stat identity, and rollback absence before mutation. Copy the prior installed bytes to the approved private rollback target with mode 0400, fsync/atomically publish where supported by the existing Plan 164-23 protocol, and verify its digest. Materialize the approved committed source blob privately, require the approved absolute Node shebang and digest, then atomically replace the destination as a regular non-symlink mode-0500 file. Re-read and hash the installed bytes. Run `--version`, ancestry-aware `--self-check` against the canonical repository and approved installation OID, and `mix verify.phase_164.installed_boundary`; the controlled-host suite must include foreign-repository and forged-PATH rejection before any accepted canonical execution. If any post-install check fails, atomically restore the verified rollback bytes and report non-success. Do not run `164 --pre-verification` or terminal `164` against the canonical checkout.</action>
  <verify>
    <automated>set -eu; approval=/Users/jon/.local/share/mailglass/checkpoints/164-27-install-approval.env; installed=/Users/jon/.local/bin/mailglass-finalize-phase; oid=$(sed -n 's/^installation_source_oid=//p' "$approval"); digest=$(sed -n 's/^source_sha256=//p' "$approval"); test -f "$installed"; test ! -L "$installed"; test "$(stat -f '%Lp' "$installed")" = 500; test "$(shasum -a 256 "$installed" | awk '{print $1}')" = "$digest"; "$installed" --version; "$installed" --self-check --repo /Users/jon/projects/mailglass --expected-source-oid "$oid"; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.installed_boundary</automated>
    <fails_when>The approval/current tuple drifts; rollback bytes are absent or mismatched; installation is symlinked, wrong-mode, or digest-mismatched; version/self-check fails; installation OID is not an ancestor; the controlled-host alias selects zero tests or any attack reaches its marker; or canonical finalization runs.</fails_when>
  </verify>
  <acceptance_criteria>
    - Installed bytes equal the approved committed blob and execute under the approved absolute interpreter.
    - The prior installed loader is preserved byte-for-byte at the private approved rollback path before replacement.
    - Self-check proves source digest, current digest, trusted tool identities, and installation-OID ancestry.
    - The explicit installed-boundary alias passes foreign repository, forged Git/PATH, exact-history, moving-HEAD, and hostile checkout attacks non-vacuously.
    - No canonical pre-verification or terminal report is created.
  </acceptance_criteria>
  <done>The supported installed command is the approved hardened authority and is ready for later protected-main lifecycle gates.</done>
</task>

</tasks>

## Artifacts this phase produces

- `test/scripts/phase_164_closeout_test.exs` — `phase_164_reinstall_contract` plus updated installed-boundary approval assertions.
- `164-FINALIZATION.md` — Plan 164-27 reinstall prerequisite and unchanged terminal ordering.
- External checkpoint targets — mode-0400 Plan 164-27 proposal/approval records used only for explicit installation authorization.
- External installed command target — mode-0500 byte-identical hardened loader with a private verified rollback copy.

<threat_model>
## ASVS Level

OWASP ASVS L1; unauthorized or stale external executable replacement is high severity and blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked commit blob → external proposal | Only immutable Git bytes and validated tool identities may enter approval. |
| Human approval → external destination | Approval authorizes one exact replacement tuple, not a path or future version generally. |
| Prior installed command → rollback store | Existing trusted bytes must remain recoverable before replacement. |
| Updated installed command → canonical repository | Readiness tests may inspect/attack the boundary but may not capture canonical final evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-100 | Tampering | installation source | high | mitigate | Extract by approved commit OID, compare digest/shebang/tool identities, never install checkout bytes. |
| T-164-101 | Elevation of Privilege | destination replacement | high | mitigate | Blocking human approval, lstat/stat/digest rechecks, exact prior identity, atomic replacement. |
| T-164-102 | Repudiation | prior installation provenance | high | mitigate | Preserve old approval and digest-addressed mode-0400 rollback bytes before mutation. |
| T-164-103 | Spoofing | installed readiness | high | mitigate | Version, ancestry-aware self-check, and explicit controlled-host adversarial suite. |
| T-164-104 | Tampering | terminal evidence ordering | high | mitigate | Forbid canonical pre-verification/terminal invocation during install and assert no report claim. |
| T-164-SC | Tampering | external source movement | high | mitigate | Package legitimacy is not applicable; source is an authenticated in-repository Git blob and approval pins its exact digest. |
</threat_model>

<verification>
Verify tracked reinstall contracts, persisted approval identity, installed digest/mode/version/self-check, and the named controlled-host attack suite. Canonical finalization remains excluded.
</verification>

<success_criteria>
- The hardened tracked loader is the actual installed command after explicit approval.
- Prior installed bytes remain recoverable and all tuple drift fails closed.
- Installed boundary attacks pass without running canonical evidence capture.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Supported operational command matches tracked repository truth | 164-27 | COVERED |
| REQ | TRTH-03 | Reproducible installed finalization authority | 164-27 Tasks 1–3 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, canonical workspace, exact protected evidence ordering | 164-27 Tasks 1–3 | COVERED |
| RESEARCH | durable-vs-volatile proof and exact identity | Immutable approved reinstall with no terminal report | 164-27 | COVERED |
| VERIFICATION | CR-02/CR-03/WR-01 operational boundary | Install and attack the repaired bytes | 164-27 Task 3 | COVERED |
| EDGE | TRTH-03 empty/ordering | Missing tuple hard-stop and approval-before-mutation order | 164-27 | COVERED-FLAGGED |
| PROHIBITION | No checkout install, auto-approval, or early terminal capture | Descriptor-less unresolved constraint retained | 164-27 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-27-SUMMARY.md` when done, recording the exact approval/installed/rollback results. Do not run canonical finalization.
</output>
