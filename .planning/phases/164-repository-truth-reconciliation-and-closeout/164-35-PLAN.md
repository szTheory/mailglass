---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 35
type: execute
wave: 32
depends_on: [164-34]
files_modified:
  - scripts/mailglass_finalize_phase_loader.mjs
  - scripts/finalize_phase_164.sh
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_closeout_test.exs
  - mix.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: The loader authenticates one commit, passes that exact authority OID through every Bash layer, and every layer rejects a repository identity that differs before evidence collection begins."
    - "TRTH-03: The trusted runtime closure contains physical Mix, Elixir, and Erlang executables that run successfully in the same sanitized child environment used for finalization."
    - "D-09/D-10/D-11/TRTH-03: Trusted Git, Bash, GitHub CLI, jq, Mix, Node, and Elixir call sites consume loader-provided MAILGLASS_* identities instead of caller-selected command resolution."
    - "TRTH-03 edge contract: a missing authority OID, missing runtime tool, equal-but-differently-authenticated commit, or reordered repository state fails closed; one exact unchanged identity proceeds."
  artifacts:
    - path: "scripts/mailglass_finalize_phase_loader.mjs"
      provides: "Authenticated physical runtime closure and authority-OID handoff"
    - path: "scripts/finalize_phase_164.sh"
      provides: "Expected-authority enforcement around fetch, closeout, and final revalidation"
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Pinned-tool and expected-main-sha closeout execution"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Deterministic sanitized-runtime and post-authentication repository-movement regressions"
    - path: "mix.exs"
      provides: "Named repository-only, proposal-boundary, and installed-boundary verification lanes"
  key_links:
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "scripts/finalize_phase_164.sh"
      via: "required authenticated authority OID positional argument plus constructed child environment"
      pattern: "authorityOid|MAILGLASS_ERL"
    - from: "scripts/finalize_phase_164.sh"
      to: "scripts/closeout_repository_truth.sh"
      via: "required expected-main-sha argument and MAILGLASS_* command identities"
      pattern: "expected.*sha|MAILGLASS_MIX"
    - from: "test/scripts/phase_164_closeout_test.exs"
      to: "mix.exs"
      via: "non-vacuous named authority-closure and external-boundary tags"
      pattern: "phase_164_authority_closure"
  flagged_assumptions:
    - "TRTH-03 adjacency probe: adjacent commits never merge into one authority; byte equality does not permit OID substitution."
    - "TRTH-03 empty probe: an absent OID, tool identity, or probe output is invalid rather than an empty success."
    - "TRTH-03 ordering probe: validation order is fixed from loader capture through final closeout recheck, and equal observations preserve the authenticated OID rather than selecting by timing."
  prohibitions:
    - statement: "D-09/D-10/D-11: Mutable or caller-selected repository bytes MUST NOT establish or replace finalization authority."
      status: unverified
      flagged: true
    - statement: "D-09/D-10/D-11: Repository-only tests or installed readiness MUST NOT be presented as terminal exact-main proof."
      status: unverified
      flagged: true
    - statement: "T-164-109: Terminal evidence MUST NOT be followed by a tracked write; terminal evidence remains absent during implementation."
      status: unverified
      flagged: true
---

<!-- plan-criteria-allow: R6 - git diff --check intentionally validates the executor's current uncommitted task changes before commit. -->

<objective>
Close the executable authority chain from authenticated loader commit through the actual Mix/Elixir/Erlang runtime and final closeout observation.

Purpose: Remove both remaining code-level paths by which validated authority can diverge from executed authority.
Output: Production loader/finalizer/closeout wiring plus deterministic fail-closed regressions and named verification lanes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-34-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@scripts/finalize_phase_164.sh
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
@mix.exs
@.tool-versions
</context>

<assumption_delta_decision>
NO CHANGE — D-02 and D-09 retain one primary current finalization authority. Any “alternate” executable or commit in a regression is hostile input, not a second supported path.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one physical BEAM toolchain through the exact child environment</name>
  <files>scripts/mailglass_finalize_phase_loader.mjs, scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - .tool-versions
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
  </read_first>
  <behavior>
    - RED 1: the two approved asdf shim identities fail when invoked inside the loader's current sanitized child PATH, so the fixture must reject that tuple before Bash dispatch.
    - RED 2: versioned physical Mix and Elixir executables for Elixir 1.19.5-otp-28 plus the physical Erlang 28.4.1 `erl` executable run in the constructed child environment and report Mix, Elixir, and OTP versions.
    - RED 3: removing Mix, Elixir, or Erlang from the exact child closure, substituting a shim, adding an unsafe path, or producing empty/incompatible version output stops before the dispatch marker.
    - RED 4: every trust-sensitive closeout call resolves through the corresponding MAILGLASS_* identity while the same fixture still produces one valid repository-only component path.
  </behavior>
  <action>Start with the failing process-level cases. Resolve the already installed versioned physical executables using the asdf installation lookup for Elixir 1.19.5-otp-28 and Erlang 28.4.1, append the exact `mix`, `elixir`, and `erl` executable names, then subject all three to the loader's existing lstat/realpath/type/owner/mode authentication. The tracked `.tool-versions` entries are unavailable on this host, so record that observed mismatch in test diagnostics and do not infer authority from those absent versions. Replace the shim tuple with the resolved physical identities, add `TRUSTED_TOOLS.ERL` and `MAILGLASS_ERL`, and remove forwarded ASDF selector variables from the child environment. Build child PATH only from directories of authenticated tools plus the loader's existing platform fallbacks. Implement and export `probeTrustedRuntime(tools, childEnv)`; before Bash dispatch and during `--self-check`, use it to run direct Mix/Elixir version probes and a noninteractive OTP-release probe in that exact child environment, requiring bounded nonempty compatible outputs and exposing normalized results plus a probe digest for later proposal binding. Refactor `scripts/closeout_repository_truth.sh` so all Git, Bash, GitHub CLI, jq, Mix, Node, and Elixir invocations use required loader-provided MAILGLASS_* identities; keep incidental platform utilities within the authenticated environment. Preserve D-09 through D-11 read-only behavior and exact report semantics.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --exclude phase_164_installed_production_boundary --exclude phase_164_proposal_boundary --warnings-as-errors</automated>
    <fails_when>Non-zero exit; the repository-only selection is empty; a shim tuple dispatches Bash; the physical runtime probe fails inside the exact child environment; a missing runtime member passes; or any hostile caller command is observed.</fails_when>
  </verify>
  <acceptance_criteria>
    - The loader authenticates physical Mix, Elixir, and Erlang members as one runnable closure.
    - The exact child environment used for dispatch is also the environment proven by the runtime probe.
    - Closeout trust-sensitive calls consume the pinned MAILGLASS_* identities.
    - No proposal, approval, installation, protected-main claim, or terminal evidence is produced.
  </acceptance_criteria>
  <reversibility rating="reversible">The tool tuple and environment construction are tracked source changes that can be reverted before external installation.</reversibility>
  <done>One production-quality repository-only path reaches the closeout seam with a physically runnable, authenticated child toolchain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry the authenticated authority OID through Bash and reject later repository movement</name>
  <files>scripts/mailglass_finalize_phase_loader.mjs, scripts/finalize_phase_164.sh, scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs, mix.exs</files>
  <read_first>
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/finalize_phase_164.sh
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - mix.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
  </read_first>
  <behavior>
    - RED 1: a fixture advances clean HEAD after the loader's last identity capture but before Bash begins; execution exits nonzero before any component-evidence or finalizer marker.
    - RED 2: a missing, malformed, nonexistent, or non-matching expected authority OID is rejected at the first shell boundary.
    - RED 3: unchanged HEAD equal to fetched origin/main and to the authenticated OID reaches the existing pre-verification fixture path.
    - RED 4: exact history requires numbered PLAN/SUMMARY pairs 01 through 39; missing, extra, duplicate, or reordered identities fail deterministically.
    - RED 5: repository-only CI excludes both real proposal and installed-host tags, while each dedicated alias selects a nonzero contract set.
  </behavior>
  <action>Pass the loader's full authenticated `authorityOid` as a required positional argument to `scripts/finalize_phase_164.sh`; shift mode parsing explicitly and reject any missing or malformed value. In the finalizer, compare the supplied OID to HEAD and fetched origin/main before evidence work, immediately after fetch, immediately before invoking `scripts/closeout_repository_truth.sh`, and at final revalidation. Pass the same value to closeout as required `--expected-main-sha` input; closeout compares it to its initial and final repository observations and records it in the existing report identity fields. Extend the immutable-loader fixture so its copied source replaces the trusted Git identity with a safe fixture-owned delegating executable. The delegate detects the loader's second exact `rev-parse HEAD`, captures and returns that old value, commits a clean fixture advance before returning control, and thereby moves the repository in the inter-process gap without relying on caller PATH or a production test bypass. Require failure before component output and marker files. Advance loader, shell, documentation-contract fixtures, and exact-history assertions to 01–39. Add named `verify.phase_164.authority_closure` and `verify.phase_164.proposal_boundary` aliases, and keep the real proposal and installed-host groups excluded from repository CI. Update the existing installed-boundary schema contract to require Erlang identity and normalized Mix version, Elixir version, OTP release, and probe-digest fields so later Plans can bind them without changing protected implementation.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.authority_closure &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract &amp;&amp; node --check scripts/mailglass_finalize_phase_loader.mjs &amp;&amp; bash -n scripts/finalize_phase_164.sh scripts/closeout_repository_truth.sh &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; either named selection is empty; repository CI includes an external-host tag; post-authentication HEAD movement reaches component evidence; expected-OID validation is absent at a shell boundary; exact history differs from 01–39; syntax fails; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - The one OID whose bytes were authenticated by Node is the required identity consumed by both Bash scripts.
    - Deterministic repository movement between Node authentication and Bash startup fails before evidence collection.
    - Positive fixtures retain current exact-main behavior under the fixed identity.
    - All 01–39 plan names are reserved by the tracked plan set and exact-history tests require the complete paired range.
  </acceptance_criteria>
  <reversibility rating="reversible">The authority handoff, tests, and aliases are tracked code with no external-state mutation.</reversibility>
  <done>The authenticated source commit remains authoritative across the process boundary and every later repository observation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/mailglass_finalize_phase_loader.mjs` — `TRUSTED_TOOLS.ERL`, `MAILGLASS_ERL`, exported `probeTrustedRuntime`, exact-child runtime probing, and authenticated-OID dispatch.
- `scripts/finalize_phase_164.sh` — required authority-OID parameter and repeated exact-main enforcement.
- `scripts/closeout_repository_truth.sh` — `--expected-main-sha` input and pinned MAILGLASS_* command use.
- `test/scripts/phase_164_closeout_test.exs` — sanitized-runtime, missing-member, shim, and post-authentication movement regressions.
- `mix.exs` — `verify.phase_164.authority_closure` and `verify.phase_164.proposal_boundary` aliases plus external-tag exclusion.
- External installation, approval, terminal report, or completion metadata: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; spoofed executable identity, commit/checkout time-of-check-time-of-use, and premature terminal proof are high severity and block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated loader → child process | A constructed environment and one captured commit cross from Node into Bash. |
| Versioned runtime files → Mix/Elixir execution | Elixir scripts depend on Erlang lookup inside the exact child PATH. |
| Finalizer → live repository | Every observation must remain equal to the OID whose source files were authenticated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-133 | Spoofing | BEAM runtime closure | high | mitigate | Authenticate physical Mix, Elixir, and Erlang identities and execute version probes in the exact child environment. |
| T-164-134 | Tampering | child environment | high | mitigate | Construct PATH only from authenticated tool directories and remove inherited ASDF selectors. |
| T-164-135 | Tampering | Node-to-Bash authority handoff | high | mitigate | Pass the authenticated full OID as required data through both shell layers. |
| T-164-136 | Elevation of Privilege | closeout command resolution | high | mitigate | Route trust-sensitive calls through required MAILGLASS_* identities. |
| T-164-137 | Repudiation | race regression evidence | high | mitigate | Advance a fixture repository after loader capture and prove bounded failure before evidence markers. |
| T-164-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs. |
</threat_model>

<verification>
Run the non-vacuous authority-closure lane, full repository CI lane with external groups excluded, JavaScript and Bash syntax checks, and diff hygiene. Do not run proposal publication, installation, pre-verification, or terminal finalization.
</verification>

<success_criteria>
- The exact sanitized child environment can execute the complete physical BEAM runtime closure.
- The loader-authenticated OID is mandatory and unchanged through final closeout revalidation.
- Moving repository state and hostile command resolution fail before evidence collection.
- Exact history and named lanes are ready for protected integration and later external-boundary proof.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence reflects actual executable authority | 164-35 Tasks 1–2 | COVERED |
| REQ | TRTH-03 | Reproducible canonical closeout under exact executable/repository authority | Tasks 1–2 | COVERED |
| CONTEXT | D-01–D-04 | One current supported path remains primary; historical guidance unchanged | objective / assumption delta | COVERED |
| CONTEXT | D-09–D-11 | Dual proof, canonical exact main, and fail-closed provenance | Tasks 1–2 | COVERED |
| RESEARCH | — | Exact identity, sanitize-before-use, authenticate-before-execute, bounded failure | Tasks 1–2 | COVERED |
| VERIFICATION | Gap: asdf shims in child PATH | Physical Mix/Elixir/Erlang closure proven in exact child environment | Task 1 | COVERED |
| VERIFICATION | Gap: discarded authorityOid | OID passed through Bash and movement race rejected | Task 2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Distinct OIDs stay distinct; missing values fail; authenticated ordering is stable | must_haves / Task 2 | COVERED-FLAGGED |
| PROHIBITION | Mutable authority and premature terminal proof | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-35-SUMMARY.md` when done. Continue to Plan 164-36; do not publish approval, install bytes, update completion metadata, or run either finalization mode.
</output>
