---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 05
type: execute
wave: 3
depends_on: [164-04]
files_modified:
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_closeout_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
autonomous: true
requirements: [TRTH-03]
must_haves:
  truths:
    - "D-09/TRTH-03: One rerunnable command writes a volatile report under existing ignored tmp/ and composes existing hygiene, preservation, exact-CI, scheduled-control, and ledger authorities."
    - "D-10: Quiet requires canonical /Users/jon/projects/mailglass on main, HEAD equal to origin/main, and empty stable porcelain including untracked files."
    - "D-11: Quiet is returned only after exact-main protected CI succeeds and every scheduled/recovery outcome has complete, current, identity-matched evidence."
    - "D-12: Quiet requires the complete exact-one disposition ledger gate."
  artifacts:
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Rerunnable fail-closed closeout CLI"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Wave 0 fixture-backed identity and failure-precedence contract"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md"
      provides: "Stable usage and evidence contract without a committed final-state snapshot"
  key_links:
    - from: "scripts/closeout_repository_truth.sh"
      to: "existing repository evidence tools"
      via: "preserved exit status plus normalized single JSON report"
      pattern: "repo.hygiene|verify_workspace_evidence|scheduled_control_evidence|ci_monitor"
  flagged_assumptions:
    - "TRTH-03 adjacency probe unresolved: when two control/evidence observations are equal or share a boundary identity, no SPEC defines merge/collision semantics; the wrapper must expose rather than silently coalesce them."
    - "TRTH-03 empty probe unresolved: no SPEC defines quiet behavior for zero controls, one control, or missing/null source results; the wrapper's fail-closed test contract does not silently resolve this planner assumption."
    - "TRTH-03 ordering probe unresolved: stable ordering for equal control or evidence keys is not specified by a phase SPEC and remains explicitly flagged."
  prohibitions:
    - statement: "MUST NOT label the repository quiet when exact-main, protected-check, scheduled/recovery provenance, artifact agreement, or disposition evidence is pending, cannot-check, stale, malformed, mismatched, missing, or unexplained."
      status: unverified
      flagged: true
---

<objective>
Create the rerunnable, non-self-referential quiet-repository verdict.

Purpose: Turn the durable ledger and existing volatile authorities into one inspectable fail-closed result without adding a service or workflow.
Output: Closeout shell command, fixture-backed contract test, and stable usage document.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-04-SUMMARY.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
@scripts/verify_workspace_evidence.sh
@scripts/scheduled_control_evidence.sh
@scripts/ci_monitor.cjs
@.github/scheduled-controls.json
@.github/workflows/ci.yml
@test/scripts/workspace_evidence_contract_test.exs
@test/scripts/scheduled_control_evidence_test.exs
@test/scripts/required_checks_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose exact-main authorities into a fixture-proven report</name>
  <reversibility rating="costly">The CLI/result fields become the documented maintainer seam; undo requires coordinated script, tests, and usage-document changes.</reversibility>
  <files>scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - dev/mix/tasks/mailglass.repo.hygiene.ex
    - scripts/verify_workspace_evidence.sh
    - scripts/scheduled_control_evidence.sh
    - scripts/ci_monitor.cjs
    - .github/scheduled-controls.json
    - .github/workflows/ci.yml
    - test/scripts/workspace_evidence_contract_test.exs
    - test/scripts/scheduled_control_evidence_test.exs
    - test/scripts/required_checks_test.exs
  </read_first>
  <behavior>
    - Test 1: a disposable main repo with HEAD == origin/main, empty porcelain, passing hygiene/ledger/preservation, exact-SHA successful CI, and evidence-valid scheduled controls emits status pass and exit 0.
    - Test 2: non-main, divergent SHA, tracked/untracked dirt, duplicate/missing ledger evidence, or wrong CI headSha emits non-pass and nonzero.
    - Test 3: malformed/unavailable external JSON becomes cannot-check and nonzero; no error-suppressing fallback can coerce it to pass.
    - Test 4: pending/cannot-check/stale/mismatched scheduled evidence is non-pass; an already-defined blocked control is eligible only when event, run, workflow SHA, head SHA, artifact digest, and evidence_valid are complete.
    - Test 5: report carries schema, captured_at, repo, branch, head_sha, origin_main_sha, ci_run_id, component results, final status/reason, and source output paths.
  </behavior>
  <action>Start RED with disposable Git repositories and PATH-stubbed `gh`/`mix` fixture commands following `workspace_evidence_contract_test.exs`; include malformed, wrong-identity, pending, cannot-check, and policy-blocked fixtures. Implement `scripts/closeout_repository_truth.sh` as Bash with `set -euo pipefail` and CLI `--repo PATH --ledger PATH --ci-run-id ID --output PATH`. Resolve all paths from the supplied repo. Validate branch `main`, local HEAD and `refs/remotes/origin/main` as 40-hex and equal, and `git status --porcelain=v1 --untracked-files=all` empty. Compose, without reimplementing policy, `mix mailglass.repo.hygiene --check --format json`, Phase 161 static evidence verification, the Phase 164 ledger contract, `node scripts/ci_monitor.cjs inspect &lt;run-id&gt;`, and `scripts/scheduled_control_evidence.sh sweep --output &lt;component&gt;`. Decode and shape-check every JSON object; bind CI and scheduled expected SHA to the same HEAD. Preserve `cannot-check` precedence, then pending/unexplained blocked, then blocked, then pass. Write the aggregate atomically to the requested path even on non-pass, and exit nonzero for every status except pass. Do not dispatch, rerun, merge, publish, or mutate GitHub/Hex/Git state.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior case runs against a disposable repository or explicit stub fixture and asserts both JSON status/reason and process exit code.
    - The pass fixture proves non-vacuity by mutating each required source independently and observing nonzero failure.
    - The wrapper accepts only a positive integer CI run ID and rejects missing/unknown CLI arguments with exit 2.
    - Component files and final report are written beneath the supplied output directory/path using temporary files and rename; malformed output is retained as cannot-check evidence.
    - No network call occurs in focused tests, and production execution uses only the existing repository tools listed in the action.
  </acceptance_criteria>
  <done>The closeout command emits one machine-readable, exact-identity, fail-closed verdict over all existing authorities.</done>
</task>

<task type="auto">
  <name>Task 2: Document the stable post-merge usage contract</name>
  <reversibility rating="reversible">The usage document can be corrected without changing the report CLI contract or live evidence.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-RELEASE-RECONCILIATION.md
    - MAINTAINING.md
    - .gitignore
  </read_first>
  <action>Create `164-CLOSEOUT.md` as the durable usage/evidence contract, not a captured final snapshot, per D-09. State the exact post-protected-merge invocation from `/Users/jon/projects/mailglass`, require the exact normally triggered CI run ID for current main, write the report to `tmp/phase-164-closeout/report.json` under the existing `/tmp/` ignore, and enumerate the machine fields/pass conditions and non-pass precedence. Link to `164-TRUTH-DISPOSITION.tsv`, Phase 161 preservation proof, Phase 162 scheduled/release evidence, and the current maintainer path. State that each run's timestamp/SHA/report is volatile and must not be committed as canonical final state.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - The document contains the exact CLI arguments `--repo`, `--ledger`, `--ci-run-id`, and `--output` and uses `tmp/phase-164-closeout/report.json`.
    - It enumerates D-10 through D-12 pass conditions and explicitly makes pending/cannot-check/stale/malformed/mismatched evidence non-pass.
    - It labels the report volatile/untracked and does not embed a claimed final HEAD, CI run, scheduled run, timestamp, or pass snapshot.
  </acceptance_criteria>
  <done>Maintainers have an exact rerun recipe and understand the durable-ledger versus volatile-report boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/closeout_repository_truth.sh` — new CLI flags `--repo`, `--ledger`, `--ci-run-id`, `--output`; JSON fields `schema`, `captured_at`, `repo`, `branch`, `head_sha`, `origin_main_sha`, `ci_run_id`, `components`, `status`, and `reason`.
- `test/scripts/phase_164_closeout_test.exs` — new module `Mailglass.Scripts.Phase164CloseoutTest`, disposable-repository helpers, and source fixture/stub helpers.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md` — stable command and evidence contract.
- Runtime-only `tmp/phase-164-closeout/report.json` plus component JSON/text files; these remain untracked under the existing `/tmp/` rule.
- No external API/SDK, database schema, dependency, workflow, release authority, or new ignore pattern is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git/GitHub command output → aggregate verdict | External bytes may be malformed, stale, or identity-mismatched and must fail closed. |
| Existing component statuses → final status | Precedence must preserve unknown/pending outcomes and evidence-valid policy blocks. |
| Volatile report → durable repository | Runtime evidence must stay untracked and must not become self-referential truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-12 | Spoofing | exact main / CI run identity | high | mitigate | Require branch, local/remote SHA equality, exact run ID, and matching CI head SHA. |
| T-164-13 | Tampering | scheduled artifact provenance | high | mitigate | Reuse verifier and require event/run/workflow/head/artifact bindings. |
| T-164-14 | Repudiation | aggregate status precedence | high | mitigate | Fixture-test malformed/unavailable/pending/blocked/pass branches and preserve source outputs. |
| T-164-15 | Elevation of Privilege | release/control operations | high | mitigate | Wrapper is read-only and exposes no dispatch, rerun, merge, publish, or authorization operation. |
| T-164-SC | Tampering | package supply chain | low | accept | No install task or dependency change occurs. |
</threat_model>

<verification>
Run the complete fixture-backed closeout contract and `mix ci.fast`; do not use a live report as test setup.
</verification>

<success_criteria>
- The rerunnable report proves every D-10 through D-12 condition from exact identities.
- Unknown or incomplete evidence cannot pass.
- The durable usage contract never commits a self-invalidating final snapshot.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less: falsely claiming quiet on incomplete/mismatched evidence is a repository-specific transparency prohibition and remains flagged-unverified.
- Dropped with breadcrumb: authorization bypass and sensitive-data leakage are canon security/privacy candidates covered by the STRIDE register and `$gsd-secure-phase`, not minted here.
- Dropped with breadcrumb: null input, equal-key order, duplicate rows, command failures, and malformed JSON are routine engineering/edge candidates covered by flagged assumptions and fixture tests.

## Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Rerunnable final evidence describes actual operational state | 164-05 | COVERED |
| REQ | TRTH-03 | Clean canonical, exact protected CI, explained controls, all dispositions | 164-05 | COVERED |
| CONTEXT | D-09, D-10, D-11, D-12 | Dual proof, clean definition, exact evidence, ledger completeness | 164-05 | COVERED |
| RESEARCH | composed local closeout command | Reuse existing tools and volatile report | 164-05 | COVERED |
| PATTERNS | fail-closed shell/fixture analog | Existing scripts and disposable Git fixtures | 164-05 | COVERED |
| VALIDATION | 164-05-01/164-05-02 Wave 0 and docs contract | Create the closeout test before wrapper code and reuse it for the stable usage contract | 164-05 | COVERED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-05-SUMMARY.md` when done.
</output>
