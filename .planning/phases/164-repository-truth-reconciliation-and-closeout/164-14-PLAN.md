---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 14
type: execute
wave: 12
depends_on: [164-13]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TRTH-01, TRTH-02, TRTH-03]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/TRTH-01: The full maintainer and package contracts still agree with executable protected-release authority, manifests, and explicitly historical guidance at the tracked repair SHA."
    - "D-05/D-06/D-07/D-08/D-12/TRTH-02: The production validator accepts only the authoritative complete ledger, including all six ignore inventories and durable proof, at the tracked repair SHA."
    - "D-09/D-10/TRTH-03: The complete tracked gap repair is normally integrated on protected main before volatile evidence is sampled, with canonical HEAD equal to fetched origin/main and stable porcelain empty."
    - "D-11/TRTH-03: Pre-verification evidence uses the exact current protected-main SHA, attempt-one normal push CI, and complete naturally scheduled attempt-one control evidence; exact equal identities pass while any collision, missing evidence, or equal-sort tie with conflicting provenance fails."
    - "D-09/TRTH-03: The pre-verification capture is not terminal proof; the tracked summary/verifier/completion metadata must land before the separate terminal finalizer regenerates evidence at the then-current main SHA with no later tracked commit."
  artifacts:
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Tracked authority for the pre-verification versus terminal evidence boundary"
    - path: "scripts/finalize_phase_164.sh"
      provides: "Tracked exact-main evidence selector and independent raw-source validator"
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Tracked canonical closeout composition invoked by the finalizer"
  key_links:
    - from: "164-13-SUMMARY.md on protected main"
      to: "scripts/finalize_phase_164.sh"
      via: "normal protected integration followed by the tracked pre-verification command"
      pattern: "--pre-verification"
    - from: "scripts/finalize_phase_164.sh"
      to: ".github/scheduled-controls.json"
      via: "exact registry-set, attempt-one, event, head/workflow SHA, status, and digest validation"
      pattern: "source_run.attempt == 1|expected_main_sha"
    - from: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      to: "terminal finalizer invocation"
      via: "post-verifier/post-completion no-tracked-write boundary"
      pattern: "Terminal operational proof"
  flagged_assumptions:
    - "TRTH-01 unclassified: no phase SPEC classifies another edge; focused maintainer/package contracts plus exact protected evidence are treated as the defensible proof surface."
  prohibitions:
    - statement: "Protected or terminal evidence must not be manufactured through a caller-selected run, manual dispatch, rerun, alternate SHA, expanded freshness age, forced publication, authority change, or a tracked write after capture."
      status: unverified
      flagged: true
---

<objective>
Integrate the tracked regression repair normally and refresh exact protected-main evidence without crossing the terminal proof boundary.

Purpose: Replace stale volatile evidence with exact-SHA proof for ordinary verification while keeping the final post-completion report non-circular.
Output: A successful ignored pre-verification capture for the integrated repair SHA and an explicit handoff to terminal finalization after tracked completion metadata.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-13-SUMMARY.md
@scripts/finalize_phase_164.sh
@scripts/closeout_repository_truth.sh
@scripts/validate_repository_truth.exs
@.github/scheduled-controls.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Integrate the regression lock and capture exact protected pre-verification evidence</name>
  <precondition>Plan 164-13 is complete with a tracked summary; its tagged and complete focused suites are green; the user's unrelated `.tool-versions` modification has been resolved or preserved outside stable porcelain by the user without executor mutation.</precondition>
  <reversibility rating="reversible">The evidence capture is ignored and regenerable; normal protected integration is externally governed and the task grants no bypass or remote-mutation authority.</reversibility>
  <files>None — this task modifies no tracked source file; its runtime evidence is written only to the ignored location defined by `scripts/finalize_phase_164.sh`.</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-13-SUMMARY.md
    - scripts/finalize_phase_164.sh
    - scripts/closeout_repository_truth.sh
    - scripts/validate_repository_truth.exs
    - .github/scheduled-controls.json
    - test/scripts/phase_164_repository_truth_test.exs
    - test/scripts/phase_164_closeout_test.exs
    - test/mailglass/publish/maintaining_release_gate_contract_test.exs
    - test/mailglass/docs_contract_test.exs
  </read_first>
  <instructions>
    1. Run the tagged and complete focused contracts plus the production ledger validator before requesting integration.
    2. Preserve the user's unrelated `.tool-versions` change; wait until the user has resolved or preserved it outside stable porcelain instead of modifying it.
    3. Integrate Plan 164-13 and its summary through the normal protected path, then fetch `origin main` and prove the physical canonical checkout, branch, HEAD, remote SHA, and stable porcelain conditions.
    4. Wait for the normal attempt-one push CI and natural attempt-one scheduled controls for that exact SHA; run the tracked pre-verification finalizer and independently inspect its raw CI and scheduled sources.
    5. If any evidence is pending, blocked without the already-defined policy basis, cannot-check, stale, incomplete, mismatched, or from a later attempt, leave the checkpoint open without operating remote controls.
  </instructions>
  <action>First rerun the two tagged Plan 164-13 groups, the full focused documentation/ledger/closeout suite, the production authoritative-ledger CLI, Bash syntax, and formatting. Pause for normal protected integration of the tracked Plan 164-13 repair and its summary; the executor does not push directly to protected main or operate merge controls. After fetching `origin main`, require the canonical physical checkout, branch `main`, one exact 40-hex identity shared by HEAD and `refs/remotes/origin/main`, and empty stable porcelain. Wait for the normally triggered attempt-one push CI and naturally produced attempt-one scheduled-control evidence for that exact SHA. Run the tracked pre-verification mode through the project-local finalizer command. Preserve truthful pending, blocked, or cannot-check evidence and leave the checkpoint open until the existing authorities naturally satisfy D-11; do not dispatch, rerun, relax registry ages, change workflow authority, or substitute another identity. Independently read the generated report and both raw sources, checking exact report/input SHA equality, the selected numeric CI run, CI workflow/event/attempt/branch/head/status/conclusion, complete registry-set equality, and every scheduled control's workflow name, numeric run ID, attempt, schedule event, main branch, completed status, exact head/workflow SHA, evidence validity, nonblank allowed result, payload digest, and archive digest. Equal sort keys never override identity checks. Record the repair SHA and evidence identities in `164-14-SUMMARY.md` only after the capture, and state that this tracked summary intentionally makes the report non-terminal. Ordinary verification must replace the stale `164-VERIFICATION.md` result, integrate its passing result and completion metadata normally, and only then may the separate terminal mode in `164-FINALIZATION.md` regenerate the final ignored report at the new exact main SHA with no later tracked write.</action>
  <verification>Run the automated gate, then independently compare the generated inputs/report identities and raw sources against the exact fetched main SHA and `.github/scheduled-controls.json`. Success requires empty stable porcelain after capture and a passing aggregate report whose underlying CI and scheduled records each satisfy the listed attempt, event, identity, status, and digest predicates.</verification>
  <verify>
    <automated>mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs test/mailglass/publish/maintaining_release_gate_contract_test.exs test/mailglass/docs_contract_test.exs --warnings-as-errors --no-deps-check &amp;&amp; elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; ASDF_NODEJS_VERSION=22.14.0 gsd --print --no-session "/finalize-phase 164 --pre-verification"</automated>
    <fails_when>Any focused contract or authoritative-ledger check exits non-zero; the finalizer cannot resolve clean canonical protected main; exact attempt-one normal CI or complete natural scheduled evidence is absent/mismatched; or the pre-verification report is non-pass.</fails_when>
  </verify>
  <acceptance_criteria>
    - All TRTH-01 documentation/package contracts and all TRTH-02 ledger/ignore contracts pass at the tracked repair SHA.
    - Protected main contains Plan 164-13 tests and summary before capture; HEAD and fetched origin/main are the same 40-hex SHA and stable porcelain is empty.
    - The finalizer selects the CI run itself and accepts only attempt-one workflow `CI`, normal push, main branch, exact SHA, completed success.
    - The scheduled evidence contains exactly the registry controls and every item passes the independent attempt/event/identity/status/reason/digest contract; missing or equal-sorted conflicting items cannot pass.
    - No remote control is dispatched/rerun and no authority, age, release, tag, publication, or application behavior changes.
    - `164-14-SUMMARY.md` labels the capture pre-verification-only; terminal mode runs only after a passing refreshed verifier and all tracked completion metadata are on protected main, and produces no later tracked result.
  </acceptance_criteria>
  <resume-signal>Resume only after Plan 164-13 is on protected main and pre-verification mode succeeds for that exact SHA; provide the exact SHA, selected CI run ID, and the finalizer's generated report location. If natural evidence is not ready, report the specific pending/non-pass authority and keep the checkpoint open.</resume-signal>
  <done>Ordinary verification has fresh exact-SHA evidence for all tracked gap repairs, and the terminal finalization handoff remains causally valid.</done>
</task>

</tasks>

## Artifacts this phase produces

- No tracked symbol, script option, function, field, or new source file path is created by this plan.
- The existing finalizer regenerates ignored runtime inputs, normalized report, and raw component evidence at the integrated repair SHA; these remain outside `files_modified` and are never committed.
- `164-14-SUMMARY.md` is the executor's required tracked execution summary and explicitly records only pre-verification evidence, never terminal proof.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked repair → protected main | Local success is not exact-main proof until normal protected integration completes. |
| GitHub CI/scheduled bytes → pre-verification verdict | Caller choice, reruns, stale identities, incomplete sets, and malformed provenance must remain non-authoritative. |
| Pre-verification metadata → terminal closeout | A later tracked summary/verifier/completion commit invalidates terminal identity and requires a new post-completion capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-49 | Spoofing | protected-main and CI identity | high | mitigate | Fetch and require canonical branch/HEAD/origin equality; finalizer selects and independently validates attempt-one exact-SHA normal push CI. |
| T-164-50 | Tampering | scheduled evidence set/provenance | high | mitigate | Require exact registry set plus per-control attempt, event, workflow, SHA, status, reason, and digest checks. |
| T-164-51 | Repudiation | pre-verification versus terminal boundary | high | mitigate | Track the lifecycle contract and label the summary-caused SHA change; terminal capture occurs only after completion metadata with no later tracked write. |
| T-164-52 | Elevation of Privilege | remote workflow/release controls | high | mitigate | Observation-only checkpoint forbids dispatch, rerun, protection bypass, authority change, and forced publication. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Before the checkpoint, rerun the complete focused Phase 164 contracts and production ledger validator. At the checkpoint, require exact clean protected main and independently verify the selected attempt-one CI and complete natural scheduled sources. After ordinary verification and completion metadata integrate, follow the tracked terminal-finalization contract and permit no later tracked write.
</verification>

<success_criteria>
- All three TRTH requirements have current local contract evidence at the integrated repair SHA.
- The refreshed pre-verification report proves exact protected main rather than the stale SHA in the old verifier report.
- The terminal report is regenerated only after tracked closeout metadata reaches protected main and remains the final operation.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and flagged-unverified: manufacturing protected or terminal evidence through selected/rerun/manual/alternate identities, age expansion, forced publication, authority change, or later tracked writes is a repository-specific transparency and operational-safety constraint.
- Dropped with breadcrumb: generic credential exposure, API response injection, and path traversal remain canonical security concerns covered by the threat model and `$gsd-secure-phase`.
- Dropped as routine engineering: missing files, empty inputs, equality, ordering, and command failures are explicit acceptance/failure cases.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence describes the repository's actual supported and operational state | 164-14 | COVERED |
| REQ | TRTH-01 | Current maintainer/package guidance agrees with authorities | 164-14 focused contracts | COVERED |
| REQ | TRTH-02 | Complete authoritative dispositions and narrow ignore truth | 164-13 plus 164-14 production validation | COVERED |
| REQ | TRTH-03 | Fresh exact-main CI/scheduled/cleanliness closeout proof | 164-14 plus terminal non-plan gate | COVERED |
| CONTEXT | D-01, D-02, D-03, D-04 | Layered documentation/package authority | 164-14 focused contracts | COVERED |
| CONTEXT | D-05, D-06, D-07, D-08, D-12 | Complete ledger, durable proof, narrow ignores, locked stale removal | 164-13 and 164-14 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, exact clean canonical main, exact protected/scheduled provenance | 164-14 | COVERED |
| RESEARCH | Two-phase closeout proof and existing-tool composition | Protected pre-verification plus post-completion terminal capture | 164-14 | COVERED |
| VERIFICATION | Gap 1 | Complete semantic authoritative-ledger enforcement | 164-13 Task 1, 164-14 validation | COVERED |
| VERIFICATION | Gap 2 | Canonical repo and post-write cleanliness | 164-13 Task 2, 164-14 canonical capture | COVERED |
| VERIFICATION | Gap 3 | Replace stale volatile evidence after tracked integration | 164-14 and terminal non-plan gate | COVERED |
| EDGE | TRTH-01 unclassified | Visible flagged assumption, backed by focused contracts | 164-14 | COVERED-FLAGGED |
| EDGE | TRTH-03 adjacency/empty/ordering | Exact identity/set equality, missing-evidence non-pass, stable selection with full provenance | 164-14 | COVERED-ASSUMPTION |
| PROHIBITION | Manufactured/nonterminal proof represented as final | Descriptor-less flagged-unverified transparency/safety item | 164-14 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-14-SUMMARY.md` after the protected pre-verification capture succeeds. Then ordinary verification must replace the stale verifier result and normal completion metadata must integrate before the separate terminal finalizer runs; do not create a tracked terminal-evidence summary.
</output>
