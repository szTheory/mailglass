---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 26
type: execute
wave: 24
depends_on: [164-25]
files_modified:
  - scripts/mailglass_finalize_phase_loader.mjs
  - scripts/finalize_phase_164.sh
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: Production finalization selects only the loader-owned canonical physical path `/Users/jon/projects/mailglass` with normalized origin identity `szTheory/mailglass` before any repository dependency is enumerated or executed."
    - "D-09/D-10/D-11/TRTH-03: Node startup, Git authentication, Bash dispatch, and every downstream executable run through validated absolute tool identities and a sanitized child environment rather than caller PATH."
    - "TRTH-03: A foreign repository and an internally consistent forged Git/PATH implementation are rejected before repository-supplied Bash or any marker executes."
    - "TRTH-03: Self-check accepts an installation OID only when it is an ancestor of the current authority OID, even when unrelated history contains identical loader bytes."
    - "D-12/TRTH-03: The authenticated terminal history is the exact PLAN/SUMMARY pair set 01 through 28 and cannot shrink or grow implicitly."
  artifacts:
    - path: "scripts/mailglass_finalize_phase_loader.mjs"
      provides: "Canonical-repository and trusted-toolchain bootstrap before authenticated dependency execution"
      exports: ["main", "validateCanonicalRepository", "validateTrustedToolchain", "installationOidIsAncestor"]
    - path: "scripts/finalize_phase_164.sh"
      provides: "Downstream finalizer consuming loader-provided absolute tools and sanitized environment"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Foreign repository, forged Git/PATH, unrelated-history, and exact 01-28 regression matrix"
  key_links:
    - from: "installed loader-owned constants"
      to: "/Users/jon/projects/mailglass and szTheory/mailglass"
      via: "physical-root equality plus normalized origin identity before dependencyManifest"
      pattern: "CANONICAL_REPOSITORY|EXPECTED_REPOSITORY|remote get-url origin"
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "Git and Bash subprocesses"
      via: "validated absolute executable paths and an allowlisted child environment"
      pattern: "TRUSTED_GIT|TRUSTED_BASH|spawnSync"
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "scripts/finalize_phase_164.sh and transitive tools"
      via: "absolute tool variables plus a fixed sanitized PATH with caller injection removed"
      pattern: "MAILGLASS_GIT|MAILGLASS_GH|MAILGLASS_JQ|MAILGLASS_MIX|MAILGLASS_NODE|MAILGLASS_ELIXIR"
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "scripts/finalize_phase_164.sh"
      via: "a mechanically checked shared terminal range requiring exact PLAN/SUMMARY pairs 01 through 28"
      pattern: "TERMINAL_LAST_PLAN.*28|terminal_last_plan=28|exact 01-28 PLAN/SUMMARY set"
  flagged_assumptions:
    - "TRTH-03 adjacency probe: repository path and remote identity must both match; equality of only one authority attribute cannot satisfy the pair."
    - "TRTH-03 empty probe: missing origin, missing tool, empty output, or an empty allowlisted environment is a hard non-dispatch result."
    - "TRTH-03 ordering probe: canonical path, origin, and trusted tools are authenticated before OID capture; dependency authentication completes before the single Bash dispatch."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT let mutable or caller-selected executable or repository bytes establish finalization authority."
      status: unresolved
      flagged: true
    - statement: "D-12: MUST NOT infer the authorized Phase 164 history from whichever numbered artifacts survive."
      status: unresolved
      flagged: true
---

<objective>
Make the installed finalizer's first trust decision canonical and independent of caller-controlled repository and executable lookup.

Purpose: Close CR-02, CR-03, and WR-01 at the loader-owned pre-evaluation boundary.
Output: Canonical repository pinning, validated absolute tool selection, sanitized child execution, ancestry validation, and adversarial regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-SECURITY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-25-SUMMARY.md
@scripts/mailglass_finalize_phase_loader.mjs
@scripts/finalize_phase_164.sh
@test/scripts/phase_164_closeout_test.exs
</context>

<assumption_delta_decision>
Primary noun: canonical repository authority. Decision: no-change — alternate repository and phase inputs are attack fixtures; the supported identity stays the single canonical Mailglass checkout and Phase 164.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace canonical repository identity through one trusted Git authentication path</name>
  <reversibility rating="costly">The loader-owned repository and tool identities coordinate installed bytes, tests, installation approval, and terminal operations.</reversibility>
  <files>scripts/mailglass_finalize_phase_loader.mjs, scripts/finalize_phase_164.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-24-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: direct production CLI invocation selects the compiled canonical physical repository independent of current working directory and rejects any different real path before tree enumeration.
    - Test 2: origin URL normalization accepts only the existing SSH/HTTPS spellings of `github.com/szTheory/mailglass` and rejects absent, extra, lookalike, or foreign identities before dependency authentication.
    - Test 3: a fake `git` placed first on caller PATH is never invoked and cannot forge a repository root, OID, tree, index, or blob response.
    - Test 4: tests may exercise the accepted flow only through an explicitly constructed test-only loader configuration; production CLI constants cannot be overridden through environment variables or arguments.
    - Test 5: the loader and shell expose one mechanically checked terminal contract with `TERMINAL_LAST_PLAN = 28` and `terminal_last_plan=28`; both require exactly one regular authenticated PLAN and SUMMARY for every number 01 through 28, reject either missing member with the member-specific error, and never infer the range from surviving files.
  </behavior>
  <action>Start RED by replacing the positive arbitrary-repository installed-production case with a rejection case and adding a fake-Git program that returns an internally consistent forged root/OID/tree/blob story plus a marker. In `scripts/mailglass_finalize_phase_loader.mjs`, move repository selection ahead of all Git-derived discovery: production CLI must resolve the compiled `/Users/jon/projects/mailglass` physical path, require the expected canonical path, and use a loader-owned normalized-origin predicate for `szTheory/mailglass` before calling `captureAuthorityCommit`, `phaseDirectoryAtCommit`, or `dependencyManifest`. Do not accept a repository path, origin, tool path, or override from caller argv/environment. Replace bare Git execution with a validated absolute Git identity from the fixed production toolchain. Expose a narrow injected configuration only to imported test helpers or a generated test-only loader copy so disposable accepted-flow fixtures can name their own canonical path, origin, and trusted tools without weakening direct production CLI. Update the loader's authoritative numbered range to `TERMINAL_LAST_PLAN = 28`, require the exact 56-path set formed by one PLAN and one SUMMARY for every number 01–28, and update its bounded mismatch error to name the exact 01–28 PLAN/SUMMARY set; missing, additional, adjacent, duplicate, and singleton artifacts remain fail-closed per D-12. In `scripts/finalize_phase_164.sh`, deliberately set `terminal_last_plan=28` and retain explicit `seq -w "$terminal_first_plan" "$terminal_last_plan"` iteration over 01–28: for every number require the authenticated `164-NN-PLAN.md` and `164-NN-SUMMARY.md` as regular files, preserve the exact member-specific `missing terminal plan 164-NN-PLAN.md` and `missing terminal summary 164-NN-SUMMARY.md` errors, and never derive completeness from a surviving glob. Add a dedicated non-vacuous loader/shell agreement regression that parses both sources, asserts both terminal constants equal 28, exercises missing PLAN and missing SUMMARY cases within 01–28, checks their exact errors, and fails if either implementation accepts a range other than 01–28. Preserve phase-164-only parsing, captured-OID object reads, exact stage-0 checks, private materialization, moving-HEAD rejection, and bounded output.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_canonical_loader --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_loader_shell_terminal_contract --warnings-as-errors --no-deps-check &amp;&amp; node --check scripts/mailglass_finalize_phase_loader.mjs &amp;&amp; /opt/homebrew/bin/bash -n scripts/finalize_phase_164.sh &amp;&amp; git diff --check</automated>
    <fails_when>Either tag selects zero tests; loader and shell terminal constants differ or are not 28; either path accepts anything other than exact PLAN/SUMMARY pairs 01–28; a missing PLAN or SUMMARY lacks its exact member-specific error; a foreign path or origin reaches tree enumeration or Bash; the forged Git marker exists; caller argv/environment can replace a production authority constant; Node or Bash syntax fails; any test exits nonzero; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - Production CLI contains one canonical physical repository path and one normalized expected GitHub repository identity owned by the loader.
    - Canonical path and origin checks complete before the first repository dependency enumeration or repository-supplied executable read.
    - The current positive foreign-repository expectation is removed and replaced by fail-before-marker behavior.
    - A PATH-prepended forged Git implementation is observably unused.
    - Accepted disposable execution exists only behind explicit test configuration inaccessible to direct production invocation.
    - The loader sets `TERMINAL_LAST_PLAN = 28`, rejects any numbered history other than the exact PLAN/SUMMARY pair set 01–28, and reports the bounded exact-set mismatch contract.
    - The shell sets `terminal_last_plan=28`, iterates every number 01–28, requires both authenticated members as regular files, emits the exact missing-plan or missing-summary error for the absent member, and does not infer completeness from surviving artifacts.
    - A runnable non-vacuous regression proves loader/shell agreement on 28 and on the exact 01–28 iteration and error behavior.
  </acceptance_criteria>
  <done>The loader's first authenticated Git path can originate only from the canonical Mailglass repository and trusted Git executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry the validated toolchain and installation ancestry through private Bash dispatch</name>
  <reversibility rating="costly">The trusted executable allowlist and child environment are shared across every terminal component command.</reversibility>
  <files>scripts/mailglass_finalize_phase_loader.mjs, scripts/finalize_phase_164.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/finalize_phase_164.sh
    - scripts/closeout_repository_truth.sh
    - scripts/scheduled_control_evidence.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: production startup uses an installation-pinned absolute Node interpreter rather than `/usr/bin/env`, and Git/Bash spawn calls use validated absolute executable identities.
    - Test 2: Bash and every transitive child receive an allowlisted environment and sanitized PATH that excludes the caller's prefix while preserving required GitHub/Mix/Elixir operation.
    - Test 3: fake `git`, `bash`, `gh`, `jq`, `mix`, `node`, and `elixir` commands on caller PATH write no marker during rejected or accepted fixture paths.
    - Test 4: `--self-check` rejects an installation OID from unrelated history even when its loader blob is byte-identical to current and installed bytes.
    - Test 5: missing, symlinked, non-regular, unexpectedly owned, or group/world-writable trusted executable identity fails before repository authentication or Bash.
  </behavior>
  <action>Extend RED with adversarial caller-PATH programs for every executable named by the loader/finalizer chain and an unrelated-history fixture containing byte-identical loader source. Define the production toolchain as explicit absolute identities for the installation-pinned Node interpreter, Git, Bash, GitHub CLI, jq, Mix, and Elixir plus only the system utilities genuinely used by the authenticated scripts. Validate each configured identity before repository discovery using lstat/realpath, regular-file, ownership, and unsafe-mode rules appropriate to the fixed system/Homebrew/asdf locations; reject missing or unexpected identities with bounded diagnostics. Use absolute Git and Bash paths at their direct spawn sites. Pass only a constructed child environment containing necessary stable locale/temp/home/auth variables, fixed tool variables, `GH_HOST=github.com`, and a sanitized PATH assembled from the validated tool locations; do not spread the caller environment or preserve its PATH prefix. Update `scripts/finalize_phase_164.sh` to call the loader-provided absolute Git/GitHub/jq/Mix/Node/Elixir identities at trust-sensitive seams and ensure authenticated helper scripts inherit the same sanitized environment. In `selfCheck`, after both full OIDs exist, use trusted Git `merge-base --is-ancestor <installation_oid> <current_oid>` and fail before reporting provenance when it is false. Keep current exact-byte comparisons as separate checks; ancestry cannot substitute for byte identity. Preserve D-09/D-10/D-11 read-only finalization, exact-main checks, no dispatch/rerun/release authority, and private-root cleanup.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_trusted_toolchain --warnings-as-errors --no-deps-check &amp;&amp; node --check scripts/mailglass_finalize_phase_loader.mjs &amp;&amp; /opt/homebrew/bin/bash -n scripts/finalize_phase_164.sh scripts/closeout_repository_truth.sh scripts/verify_workspace_evidence.sh scripts/scheduled_control_evidence.sh &amp;&amp; git diff --check</automated>
    <fails_when>The tag selects zero tests; any forged-tool marker exists; any trust-sensitive spawn uses caller PATH; an unsafe configured tool passes validation; unrelated installation history passes self-check; required absolute tools or sanitized variables are absent; syntax fails; a command exits nonzero; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - The installed file's shebang names the approved absolute Node interpreter and contains no environment-based interpreter lookup.
    - Git and Bash are invoked by validated absolute identity from the loader.
    - The child environment is an allowlist, and its PATH cannot include caller-prepended directories.
    - GitHub CLI, jq, Mix, Node, and Elixir resolution inside the authenticated chain comes from validated loader-owned identities.
    - Every fake-tool attack is non-vacuous and proves its marker remains absent.
    - Self-check requires both exact byte equality and installation-OID ancestry to current authority OID.
    - No product, API, schema, dependency, workflow-topology, or release-authority behavior changes.
  </acceptance_criteria>
  <done>Caller cwd, PATH, foreign Git objects, and unrelated installation history cannot replace the installed finalization trust root.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/mailglass_finalize_phase_loader.mjs` — canonical repository constants, trusted-toolchain validation, sanitized environment builder, ancestry gate, and exact 01–28 history.
- `scripts/finalize_phase_164.sh` — absolute-tool consumers and sanitized downstream execution contract.
- `test/scripts/phase_164_closeout_test.exs` — `phase_164_canonical_loader` and `phase_164_trusted_toolchain` adversarial groups.

<threat_model>
## ASVS Level

OWASP ASVS L1; caller-selected repository/executable substitution is high severity and blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller cwd → installed loader | Current directory is untrusted and cannot select authority repository bytes. |
| Caller environment/PATH → Node/Git/Bash | Startup and direct trust-oracle executables must be loader-owned absolute identities. |
| Loader → authenticated shell chain | Only an allowlisted environment and validated tool identities cross into private Bash execution. |
| Installation OID → current repository history | Byte equality alone does not prove the recorded installation commit belongs to current authority history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-95 | Spoofing | canonical repository selection | high | mitigate | Pin physical path and normalized origin in loader-owned code before dependency enumeration. |
| T-164-96 | Elevation of Privilege | ambient executable lookup | high | mitigate | Pin Node startup, validate absolute Git/Bash/tool identities, and sanitize child environment/PATH. |
| T-164-97 | Tampering | forged Git object responses | high | mitigate | Never invoke caller PATH Git; run an internally consistent fake-Git attack and require no marker. |
| T-164-98 | Repudiation | installation provenance | high | mitigate | Require merge-base ancestry in addition to installed/current byte equality. |
| T-164-99 | Denial of Service | missing trusted host tool | medium | mitigate | Fail before repository authentication with bounded diagnostic; installation preflight verifies readiness. |
| T-164-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs; existing host executables are identity-validated. |
</threat_model>

<verification>
Run both named adversarial groups, Node/Bash syntax checks, and diff hygiene. Do not invoke canonical pre-verification or terminal finalization before the updated loader is reinstalled.
</verification>

<success_criteria>
- Only the canonical physical Mailglass repository and expected origin can supply authenticated authority bytes.
- Caller PATH cannot substitute any trust-sensitive executable.
- Installed-source provenance requires both byte equality and commit ancestry.
- Exact Phase 164 terminal history expands deliberately through Plan 28.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational state is described and enforced by actual repository authority | 164-26 | COVERED |
| REQ | TRTH-03 | Reproducible canonical exact-main finalization | 164-26 Tasks 1–2 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, canonical checkout, and exact protected evidence | 164-26 Tasks 1–2 | COVERED |
| CONTEXT | D-12 | Exact 01–28 numbered artifact authority | 164-26 Task 1 | COVERED |
| RESEARCH | exact identity and fail-closed component composition | Loader-owned repository/tool bootstrap | 164-26 | COVERED |
| VERIFICATION | CR-02 | Reject foreign/caller-selected repository authority | 164-26 Task 1 | COVERED |
| VERIFICATION | CR-03 | Reject ambient PATH and forged Git/tool substitution | 164-26 Task 2 | COVERED |
| VERIFICATION | WR-01 | Require installation-OID ancestry | 164-26 Task 2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Paired identity, hard missing states, fixed authentication order | 164-26 | COVERED-FLAGGED |
| PROHIBITION | No caller-selected mutable authority or inferred history | Descriptor-less unresolved constraints retained | 164-26 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-26-SUMMARY.md` when done. The changed tracked loader is not operational authority until Plan 164-27 completes its explicit reinstall checkpoint.
</output>
