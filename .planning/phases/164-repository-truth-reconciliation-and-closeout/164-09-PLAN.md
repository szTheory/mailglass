---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 09
type: execute
wave: 7
depends_on: [164-08]
files_modified:
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_closeout_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
autonomous: true
gap_closure: true
requirements: [TRTH-02, TRTH-03]
must_haves:
  truths:
    - "D-10/TRTH-03: `--repo` resolves exactly to /Users/jon/projects/mailglass; every other checkout, worktree, symlink target, or disposable repository is rejected before evidence collection."
    - "D-09/D-10/TRTH-03: `--output` resolves beneath canonical `tmp/`, is covered by the existing root ignore rule, and a passing run rechecks stable porcelain after every report/component write."
    - "D-05/D-12/TRTH-02: `--ledger` resolves exactly to the authoritative Phase 164 TSV inside the canonical repository and closeout invokes the shared full validator from Plan 164-08."
    - "Malformed currentness, stale-retain, missing subjects, incomplete ledgers, arbitrary ledgers, noncanonical repositories, and non-ignored outputs cannot produce a pass report."
  artifacts:
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Canonical-path-bound, authoritative-ledger-bound, output-cleanliness-safe closeout CLI"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Negative integration contract for repository, ledger, and output bypasses"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md"
      provides: "Accurate stable contract for enforced canonical paths and post-write cleanliness"
  key_links:
    - from: "scripts/closeout_repository_truth.sh"
      to: "scripts/validate_repository_truth.exs"
      via: "production CLI invocation against the exact canonical ledger"
      pattern: "validate_repository_truth.exs.*--repo.*--ledger"
    - from: "scripts/closeout_repository_truth.sh"
      to: "/Users/jon/projects/mailglass/tmp/phase-164-closeout/report.json"
      via: "resolved canonical tmp path, git-check-ignore gate, atomic writes, and final porcelain recheck"
      pattern: "git check-ignore|status --porcelain=v1"
  flagged_assumptions:
    - "TRTH-03 adjacency: repository, ledger, and output containment use resolved path identity; lexical prefix adjacency such as `mailglass-other` must not count as containment."
    - "TRTH-03 empty: missing or empty path arguments and missing source results remain usage/cannot-check failures, never vacuous success."
    - "TRTH-03 ordering: final cleanliness is checked after all output/component writes, so a pre-write clean observation cannot override later dirt."
  prohibitions:
    - statement: "D-09/D-10: MUST NOT manufacture a quiet verdict by selecting another checkout, an arbitrary ledger, a non-ignored destination, or by sampling cleanliness before the command's own writes."
      status: unverified
      flagged: true
---

<objective>
Bind the closeout CLI to the one canonical repository, ledger, and ignored output boundary it claims to certify.

Purpose: Ensure no caller-controlled path can turn a disposable or newly dirtied workspace into a quiet verdict.
Output: Hardened closeout command, adversarial integration tests, and corrected maintainer contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md
@scripts/validate_repository_truth.exs
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject noncanonical repository, ledger, and output paths</name>
  <reversibility rating="costly">These path identities are the documented maintainer contract and changing them requires coordinated script, tests, and usage guidance.</reversibility>
  <files>scripts/closeout_repository_truth.sh, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - .gitignore
  </read_first>
  <behavior>
    - Test 1: a disposable clean `main`, a sibling path with the canonical name as prefix, and a symlink resolving elsewhere are rejected as noncanonical before component collection.
    - Test 2: any ledger other than the resolved canonical `164-TRUTH-DISPOSITION.tsv` is rejected, including a copied complete ledger and a one-row arbitrary ledger.
    - Test 3: malformed-currentness, stale-retain, missing-subject, and header-only mutations fail through the shared production validator.
    - Test 4: output outside canonical `tmp/`, lexical-prefix escape, or a path not covered by the existing root `/tmp/` ignore is rejected before write.
    - Test 5: after all atomic component/report writes, nonempty stable porcelain prevents pass; ignored `tmp/phase-164-closeout` output does not dirty porcelain.
  </behavior>
  <action>Write the bypass cases RED, then harden `scripts/closeout_repository_truth.sh` before any `mkdir`, component command, or report write. Resolve `--repo` with physical path semantics and require exact equality to `/Users/jon/projects/mailglass`. Construct the sole accepted ledger path from that resolved repository and require the resolved `--ledger` to equal it. Resolve the output parent safely, require path-component containment beneath `$repo/tmp`, and use `git -C "$repo" check-ignore` to prove the final output and component directory are covered by the existing root rule. Replace the partial AWK gate with the Plan 164-08 production validator and preserve its nonzero result as ledger `cannot-check`. Retain the initial Git identity check, then repeat `git status --porcelain=v1 --untracked-files=all` after every component and final-report write; a nonempty final result must make the git component/final verdict non-pass. Restructure the old disposable positive fixture into negative path tests and pure component-status assertions; live positive exact-main proof belongs only in Plan 164-11. Do not add a hidden test bypass, alternate canonical-root flag, or new ignore rule.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/closeout_repository_truth.sh</automated>
  </verify>
  <acceptance_criteria>
    - Invoking the command with a disposable repository returns nonzero before its stubbed `mix`, `node`, workspace, or scheduled scripts are called.
    - A copied valid ledger and a one-row ledger at any non-authoritative path return nonzero; the command invokes `scripts/validate_repository_truth.exs` only with the canonical ledger.
    - Exact-currentness, stale-retain, missing-subject, and incomplete-ledger mutations are asserted through the shared validator, not a duplicated test-only parser.
    - Canonical repo plus a tracked-root or other non-ignored output path is rejected without creating that output.
    - Source and behavioral assertions prove the final porcelain sample occurs after the report rename and participates in the returned verdict.
  </acceptance_criteria>
  <done>Caller-controlled paths and post-check writes can no longer produce a false quiet result.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the durable closeout contract to match enforced behavior</name>
  <reversibility rating="reversible">The documentation can be corrected locally while the executable path gates remain authoritative.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - scripts/closeout_repository_truth.sh
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_closeout_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .gitignore
  </read_first>
  <action>Revise `164-CLOSEOUT.md` so its exact invocation uses canonical `/Users/jon/projects/mailglass`, the canonical Phase 164 ledger path, and `tmp/phase-164-closeout/report.json`. State that these are enforced identities, not examples. Document the shared full-ledger validator, exact currentness enum, audited-subject completeness, existing root `/tmp/` ignore requirement, and the final post-write porcelain check. Preserve the durable-ledger versus volatile-report boundary and do not embed a current SHA, run ID, timestamp, or pass snapshot.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - The document names the exact canonical repository, exact canonical ledger, and ignored output path used by the script.
    - It states that arbitrary repositories/ledgers and non-ignored outputs are rejected and that cleanliness is sampled after writes.
    - It retains no claim that the CLI accepts a caller-selected checkout or equivalent ledger.
    - It contains no captured final SHA, CI run, timestamp, or durable pass assertion.
  </acceptance_criteria>
  <done>The stable maintainer contract truthfully describes the hardened closeout boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/closeout_repository_truth.sh` — strengthened semantics for existing flags `--repo`, `--ledger`, and `--output`; exact canonical constants/path checks; shared-validator invocation; post-write porcelain verdict.
- `test/scripts/phase_164_closeout_test.exs` — new negative cases for noncanonical repo, sibling/symlink identity, arbitrary ledger, exact-currentness/stale/incomplete ledger, non-ignored output, and post-write dirt.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md` — corrected enforced-path and post-write cleanliness contract.
- No new CLI flag, API, database schema, UI, dependency, workflow, ignore rule, or external integration is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller paths → canonical closeout authority | Repository, ledger, and output paths are untrusted until resolved and matched to locked identities. |
| Command writes → clean-workspace verdict | The command's own output must remain ignored and final cleanliness must be sampled after writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-25 | Spoofing | `--repo` identity | high | mitigate | Resolve physical path and require exact canonical equality before component execution. |
| T-164-26 | Tampering | `--ledger` authority | high | mitigate | Require exact canonical ledger path and invoke the shared full validator. |
| T-164-27 | Repudiation | output/porcelain ordering | high | mitigate | Require ignored canonical tmp output and recheck porcelain after atomic writes. |
| T-164-28 | Elevation of Privilege | closeout operations | high | mitigate | Preserve read-only remote behavior; expose no dispatch, rerun, merge, publish, or authority mutation. |
| T-164-SC | Tampering | package supply chain | low | accept | No install task, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run both focused Phase 164 contracts, Bash syntax validation, formatting checks for the modified ExUnit file, and `git diff --check`.
</verification>

<success_criteria>
- Only the canonical repository and authoritative ledger can reach evidence collection.
- Only ignored canonical tmp output can be written.
- A pass cannot leave the canonical workspace dirty.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less: manufacturing quiet through caller-controlled identity or observation ordering remains flagged-unverified under `must_haves.prohibitions`.
- The TRTH-03 adjacency, empty, and ordering probes are applied only to relevant resolved-path and sampling-order boundaries.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence describes the actual canonical repository | 164-09 | COVERED |
| REQ | TRTH-02 | Complete authoritative disposition evidence reaches closeout | 164-09 | COVERED |
| REQ | TRTH-03 | Reproducible clean canonical closeout | 164-09 | COVERED |
| CONTEXT | D-05, D-09, D-10, D-12 | Authoritative ledger, dual proof, exact canonical path, complete dispositions | 164-09 | COVERED |
| RESEARCH | fail-closed composed command | Reuse shared validator and existing ignored tmp output | 164-09 | COVERED |
| VERIFICATION | gaps 1 and 2 | Bind all paths, full ledger, negative cases, post-write cleanliness | 164-09 | COVERED |
| EDGE | TRTH-03 adjacency / empty / ordering | Path-component containment, non-vacuous args, post-write sampling | 164-09 | COVERED-ASSUMPTION |
| PROHIBITION | No false quiet verdict | Descriptor-less and flagged-unverified | 164-09 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-09-SUMMARY.md` when done.
</output>
