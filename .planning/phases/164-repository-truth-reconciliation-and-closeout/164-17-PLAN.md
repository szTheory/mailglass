---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 17
type: execute
wave: 15
depends_on: [164-16]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
  - .gsd/extensions/finalize-phase/index.ts
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02, TRTH-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05/D-06/D-12/TRTH-02: A ledger row whose state is tracked passes only when its exact subject is both a regular file and a member of the target repository's Git index."
    - "TRTH-02: Direct repository-truth CLI misuse, including no arguments, unknown options, or one missing required option, prints a bounded diagnostic and exits nonzero without affecting module use from ExUnit."
    - "D-09/D-10/D-11/TRTH-03: The finalize-phase extension proves the exact relative finalizer exists in HEAD, rejects staged-new and any index/worktree divergence, and executes an immutable private materialization of the authenticated HEAD blob rather than mutable checkout bytes."
    - "TRTH-03: An unchanged committed finalizer reaches the existing shell gate with the requested mode, while every failed authentication case stops before any finalizer-controlled marker can be written."
  artifacts:
    - path: "scripts/validate_repository_truth.exs"
      provides: "Exact Git-index membership enforcement for tracked ledger rows and fail-closed standalone CLI argument handling"
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Disposable-repository committed-versus-untracked regression and standalone CLI misuse contract"
    - path: ".gsd/extensions/finalize-phase/index.ts"
      provides: "HEAD-blob-authenticated finalizer dispatcher with private immutable execution materialization"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Behavioral staged-new, unstaged-modified, and unchanged-committed extension dispatch regressions"
  key_links:
    - from: "scripts/validate_repository_truth.exs"
      to: "the repository Git index"
      via: "exact literal pathspec passed after -- to git ls-files --error-unmatch"
      pattern: "ls-files.*--error-unmatch.*--"
    - from: ".gsd/extensions/finalize-phase/index.ts"
      to: "HEAD:<relative-finalizer>"
      via: "Git object lookup, checkout/index equality gate, private blob materialization, and bash execution of only that materialization"
      pattern: "HEAD:|diff.*HEAD|mkdtemp|writeFile|pi.exec"
  flagged_assumptions:
    - "TRTH-01 unclassified probe: no new documentation or package-truth behavior is implicated; the executed 164-16 whole-document contract remains the authoritative satisfied disposition and is rerun only in the plan-level regression suite."
    - "TRTH-02 adjacency probe: subject identity remains exact full-string equality; Git receives one literal subject after --, so prefix-adjacent or pathspec-like names cannot satisfy another row."
    - "TRTH-02 empty probe: empty/header-only ledgers remain rejected, and the standalone command with no arguments is now an explicit usage failure rather than silent success."
    - "TRTH-02 ordering probe: tracked membership is checked independently for each parsed row and remains invariant to ledger row order."
    - "TRTH-03 adjacency probe: the authenticated Git object is addressed by the exact repository-relative finalizer path; sibling phase directories and prefix matches remain non-authoritative."
    - "TRTH-03 empty probe: absent HEAD blobs, missing finalizer files, and empty/invalid command arguments fail before execution."
    - "TRTH-03 ordering probe: HEAD/object and index/worktree authentication must complete before materialization or bash dispatch; cleanup runs after dispatch without changing which bytes were authorized."
  prohibitions:
    - statement: "D-05/D-06/D-12: MUST NOT represent an untracked regular file as tracked repository proof."
      status: unresolved
      flagged: true
    - statement: "D-09/D-10/D-11: MUST NOT execute finalizer bytes that are absent from or differ from the authenticated HEAD object."
      status: unresolved
      flagged: true
---

<objective>
Close the two remaining Phase 164 trust-anchor defects and the bounded validator CLI warning.

Purpose: Make tracked-artifact classification and finalizer execution derive from immutable Git authority under adversarial but valid repository states.
Output: Exact-index ledger validation, fail-closed direct CLI handling, HEAD-blob-authenticated finalizer dispatch, and behavioral regressions at both production seams.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-15-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-16-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a tracked ledger claim through exact Git-index evidence</name>
  <reversibility rating="reversible">The change tightens one internal validator predicate and its CLI wrapper while preserving the twelve-column ledger and public repository contract.</reversibility>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
  </read_first>
  <behavior>
    - Test 1: in a disposable Git repository, a complete ledger row naming a committed regular file with `state=tracked` validates successfully.
    - Test 2: after removing that same path from the index while leaving its regular working-tree file intact, production `RepositoryTruthLedger.validate/2` returns a tracked-membership error naming the exact subject.
    - Test 3: a prefix-adjacent path or a subject containing pathspec metacharacters cannot satisfy the exact literal tracked-path check.
    - Test 4: invoking `elixir scripts/validate_repository_truth.exs` with no arguments, an unknown option, or only one required option exits nonzero and emits a bounded usage/error diagnostic; the canonical `--repo` plus `--ledger` invocation still prints the existing valid signal and exits zero.
    - Test 5: requiring the script from ExUnit exposes `Mailglass.RepositoryTruthLedger` without accidentally running or halting the standalone CLI path.
  </behavior>
  <action>Start RED by adding a disposable-repository regression around the production `Mailglass.RepositoryTruthLedger.validate/2` seam, not a test-local membership helper. Construct the smallest repository/ledger fixture that preserves the production parser's canonical constraints, commit the tracked subject for the positive control, then remove only its index entry while retaining the regular file for the negative control reproduced in `164-VERIFICATION.md`. Tighten `ensure_tracked_subjects_exist/2` per D-05/D-06/D-12 so every `state=tracked` row requires both `File.regular?/1` and exact index membership obtained from `git ls-files --error-unmatch -- <one literal subject>` in the supplied repository; preserve specific error categories for missing files, command failure, and untracked subjects, and do not use a shell-interpolated pathspec. Add subprocess tests for canonical direct invocation and each invalid argument shape. Refactor the bottom-level runner only as needed so direct script execution always parses arguments and fails closed while `Code.require_file/2` remains side-effect-free; retain `parse/1`, `audit_subjects/1`, and `validate/2` signatures and do not change the ledger schema, canonical relationships, D-08 row, ignore inventory, or closeout caller flags.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_trust_anchor --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv</automated>
    <fails_when>The tagged group selects zero tests or exits nonzero; an untracked regular file validates as tracked; exact literal path identity is not enforced; invalid direct invocation exits zero or produces no diagnostic; requiring the module triggers CLI termination; or the canonical ledger no longer reports valid.</fails_when>
  </verify>
  <acceptance_criteria>
    - The tagged test creates and commits a regular file, proves validation passes, runs `git rm --cached` for that exact file without deleting it, proves `File.regular?/1` remains true, and proves production validation now returns a non-success tracked-membership result.
    - The production implementation invokes Git with an argv list, `cd: repo_root`, `--error-unmatch`, and `--` before exactly one subject; it does not concatenate the subject into a shell command.
    - Every `state=tracked` row must satisfy file-type and index-membership checks; non-tracked row semantics remain unchanged.
    - No-argument, unknown-option, missing-repo, and missing-ledger standalone calls each exit nonzero with a stable diagnostic, while canonical invocation exits zero with `repository truth ledger: valid`.
    - The existing parser, inventory, canonical relationship hashes, D-08 evidence, disposition enum, and row-order invariance contracts remain green.
  </acceptance_criteria>
  <done>Repository truth can no longer certify a working-tree-only file as tracked proof, and direct CLI misuse is observably fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authenticate and execute only the committed finalizer blob</name>
  <reversibility rating="costly">The extension's dispatch sequence is the protected closeout entry contract; changing it later requires coordinated GSD-runtime, Git-fixture, and lifecycle regression updates.</reversibility>
  <files>.gsd/extensions/finalize-phase/index.ts, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .gsd/extensions/finalize-phase/index.ts
    - .gsd/extensions/finalize-phase/extension-manifest.json
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: a finalizer newly staged in a disposable repository but absent from HEAD is rejected before its marker payload executes.
    - Test 2: an unstaged modification to an otherwise committed finalizer is rejected before the modified marker payload executes.
    - Test 3: an index-staged modification to a committed finalizer is rejected before the staged or working-tree payload executes.
    - Test 4: an unchanged committed finalizer receives the repository root and optional `--pre-verification` mode through the real registered handler and writes its expected benign marker.
    - Test 5: the successful path executes a private materialization whose bytes equal `git show HEAD:<relative-finalizer>`, not the checkout pathname, and the private directory is removed on both success and failure.
  </behavior>
  <action>Start RED by replacing the source-string-only dispatch assertion with a behavioral harness around the real registered extension handler. Use disposable Git repositories and a fake `ExtensionAPI` that records `registerCommand` and delegates `pi.exec` to real bounded subprocesses; invoke the registered handler for staged-new, unstaged-modified, staged-modified, and unchanged committed finalizers, and use distinct marker payloads so the test proves which bytes did or did not execute. Harden `.gsd/extensions/finalize-phase/index.ts` per D-09/D-10/D-11: derive one exact repository-relative finalizer path, prove `HEAD:<relative-finalizer>` exists as a blob, retain exact index membership, and reject any index or working-tree difference from HEAD before dispatch. Read the verified blob through Git, create a mode-0700 private temporary directory outside the checkout, write only those authenticated bytes to a non-writable executable file, execute Bash against that private path with `repoRoot` and the existing mode arguments, and remove the private directory in `finally` on every result. Preserve bounded output, print-mode nonzero behavior, phase-directory containment, positive-integer parsing, and the shell finalizer's own clean-tree/evidence gates. Do not run `/finalize-phase 164`, alter protected evidence, relax stable-porcelain checks, add a dependency, or change release/workflow authority.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_trust_anchor --warnings-as-errors --no-deps-check &amp;&amp; git diff --check</automated>
    <fails_when>The tagged group selects zero tests or exits nonzero; staged-new, staged-modified, or unstaged-modified bytes can write their marker; the unchanged committed blob does not execute with the exact arguments; Bash receives the checkout finalizer path; temporary materialization survives success/failure; existing error propagation regresses; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - The behavioral harness invokes the actual handler captured from `registerCommand`; a source-text grep is not the sole proof for any staged/modified/unchanged case.
    - `HEAD:<relative-finalizer>` absence and both staged and unstaged divergence return nonzero before Bash dispatch and before any hostile marker is created.
    - The positive fixture proves the committed bytes execute once, receive the disposable repository root plus requested mode, and do not execute a different working-tree payload.
    - Bash is invoked with the private materialized pathname, never the resolved working-tree finalizer; the materialized bytes originate from the authenticated HEAD blob.
    - Private materialization uses restrictive directory/file permissions and is removed in a `finally` path after successful and failed Bash execution.
    - The extension retains the existing path-containment, exact phase-directory count, bounded-tail, notification, `result.code`, and print-mode exit contracts.
    - No terminal or pre-verification Phase 164 capture is run during implementation; terminal finalization remains downstream of passing verification and protected completion metadata.
  </acceptance_criteria>
  <done>The protected command can execute only finalizer bytes authenticated to HEAD, and behavioral tests prove hostile checkout/index mutations cannot run first.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/validate_repository_truth.exs` — tightens `Mailglass.RepositoryTruthLedger.validate/2` and its tracked-subject helper to require exact Git-index membership; adds/refines a direct CLI entry function without changing `parse/1`, `audit_subjects/1`, `validate/2`, `--repo`, or `--ledger`.
- `test/scripts/phase_164_repository_truth_test.exs` — adds the `phase_164_trust_anchor` disposable-repository membership and direct CLI argument regressions.
- `.gsd/extensions/finalize-phase/index.ts` — adds HEAD blob authentication, checkout/index equality enforcement, private immutable materialization, and guaranteed cleanup while retaining the `finalize-phase` command signature.
- `test/scripts/phase_164_closeout_test.exs` — adds the `phase_164_trust_anchor` real-handler harness and staged-new, staged-modified, unstaged-modified, unchanged-committed, and cleanup regressions.
- No new API, schema, dependency, workflow, release authority, package version, evidence-age rule, or terminal evidence artifact is created.

<threat_model>
## ASVS Level

OWASP ASVS L1. The high-severity evidence-spoofing and pre-gate code-execution threats block completion until the behavioral regressions pass at the production seams.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Ledger state claim → Git index | A regular working-tree file is not tracked proof unless its exact subject is present in the target repository index. |
| Working tree/index → protected finalizer dispatcher | Mutable or newly staged bytes must not execute before the authenticated shell gates. |
| HEAD Git object → temporary executable | Only bytes read from the exact committed blob may be materialized, executed, and then removed. |
| CLI arguments → repository-truth verdict | Missing or unknown arguments must be observable non-success rather than a silent green process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-61 | Spoofing | `state=tracked` ledger rows | high | mitigate | Require regular-file type plus exact literal `git ls-files --error-unmatch -- <subject>` membership and disposable-repository regression. |
| T-164-62 | Elevation of Privilege | finalize-phase extension dispatch | high | mitigate | Authenticate `HEAD:<relative-finalizer>`, reject staged/unstaged divergence, and execute only the private committed-blob materialization. |
| T-164-63 | Tampering | temporary finalizer materialization | high | mitigate | Use restrictive permissions, source bytes only from Git, never dispatch the checkout pathname, and clean up in `finally`. |
| T-164-64 | Repudiation | standalone validator invocation | medium | mitigate | Make every invalid invocation emit a bounded diagnostic and nonzero status under subprocess tests. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run both non-vacuous `phase_164_trust_anchor` groups first. Then run the complete focused Phase 164 suite with `ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs test/mailglass/publish/maintaining_release_gate_contract_test.exs test/mailglass/docs_contract_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check`, the canonical standalone validator invocation, `mix format --check-formatted scripts/validate_repository_truth.exs test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs`, and `git diff --check`. Any nonzero exit, zero selected trust-anchor tests, or high-severity threat regression blocks completion. Do not invoke `/finalize-phase 164`; refresh ordinary verification only after this plan and its summary are integrated, then preserve the existing protected-main completion-metadata and terminal-finalization sequence.
</verification>

<success_criteria>
- Exact Git index membership, not filesystem existence alone, supports every tracked ledger claim.
- Standalone validator misuse cannot exit successfully without a verdict.
- Finalizer bytes absent from or divergent from HEAD cannot execute through the extension.
- The unchanged committed finalizer path remains operational through private HEAD-blob execution and existing fail-closed shell gates.
- TRTH-01 remains satisfied and untouched; TRTH-02 and TRTH-03 gain direct behavioral proof for the verifier's two failed trust boundaries.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and unresolved: representing an untracked regular file as tracked proof is a repository-specific transparency prohibition from D-05/D-06/D-12 and the authoritative verifier.
- Kept descriptor-less and unresolved: executing finalizer bytes absent from or different from HEAD is a repository-specific operational-safety prohibition from D-09/D-10/D-11 and the authoritative verifier.
- Canon-referral breadcrumb: generic command injection, path traversal, and temporary-file attacks remain ASVS/security-review concerns; their phase-specific manifestations are concretely mitigated in the threat model without minting extra prohibitions.
- Dropped as routine engineering: missing arguments, empty input, duplicate rows, subprocess errors, and cleanup failures are explicit behavior and acceptance cases rather than values/safety prohibitions.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts and final evidence describe actual supported and operational state | 164-17 | COVERED |
| REQ | TRTH-01 | Current maintainer/package guidance | Executed baseline 164-16; full contract rerun | SATISFIED-UNCHANGED |
| REQ | TRTH-02 | Evidence-backed tracked/generated classification | 164-17 Task 1 | COVERED |
| REQ | TRTH-03 | Reproducible trustworthy final closeout authority | 164-17 Task 2 | COVERED |
| CONTEXT | D-01 through D-04 | Layered current/historical documentation and package truth | Executed baseline 164-03/16 | SATISFIED-UNCHANGED |
| CONTEXT | D-05, D-06, D-12 | Evidence-backed exact-one tracked disposition truth | 164-17 Task 1 | COVERED |
| CONTEXT | D-07, D-08 | Narrow ignores and locked stale-root removal | Executed baseline 164-01/04/15 | SATISFIED-UNCHANGED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, exact clean canonical state, exact protected evidence | 164-17 Task 2 | COVERED |
| RESEARCH | Git index is authority for tracked state; volatile proof executes established controls | 164-17 Tasks 1-2 | COVERED |
| PATTERNS | Shared production validator and project-local finalizer extension seams | 164-17 Tasks 1-2 | COVERED |
| VERIFICATION | CR-02 untracked regular file accepted as tracked | 164-17 Task 1 | COVERED |
| VERIFICATION | CR-01 mutable working-tree finalizer executes before gates | 164-17 Task 2 | COVERED |
| VERIFICATION | WR-01 invalid/no-argument validator exits zero silently | 164-17 Task 1 | COVERED |
| EDGE | TRTH-01 unclassified | Existing whole-document contract retained and rerun | 164-17 flagged assumption | COVERED-FLAGGED |
| EDGE | TRTH-02 adjacency / empty / ordering | Literal exact subject, non-vacuous CLI/ledger, per-row order independence | 164-17 Task 1 and flagged assumptions | COVERED-ASSUMPTION |
| EDGE | TRTH-03 adjacency / empty / ordering | Exact relative HEAD object, absence fail-closed, authenticate-before-dispatch | 164-17 Task 2 and flagged assumptions | COVERED-ASSUMPTION |
| PROHIBITION | False tracked proof | Descriptor-less unresolved safety/transparency constraint | 164-17 Task 1 | COVERED-FLAGGED |
| PROHIBITION | Unauthenticated finalizer execution | Descriptor-less unresolved operational-safety constraint | 164-17 Task 2 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-17-SUMMARY.md` when done. After the plan summary and refreshed passing verifier/completion metadata reach protected main, run the separately governed terminal finalizer with no later tracked write.
</output>
