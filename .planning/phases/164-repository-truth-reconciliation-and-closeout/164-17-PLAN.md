---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 17
type: execute
wave: 15
depends_on: [164-16]
files_modified:
  - scripts/validate_repository_truth.exs
  - test/scripts/phase_164_repository_truth_test.exs
  - .gsd/extensions/finalize-phase/index.ts
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02, TRTH-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05/D-06/D-12/TRTH-02: A ledger row whose state is tracked passes only when its exact subject is both a regular file and the sole exact path returned from the target repository's Git index under literal-pathspec semantics."
    - "TRTH-02: Direct repository-truth CLI misuse, including no arguments, unknown options, or one missing required option, prints a bounded diagnostic and exits nonzero without affecting module use from ExUnit."
    - "D-09/D-10/D-11/TRTH-03: The finalize-phase extension authenticates both the phase shim and its first gate-bearing downstream executable in HEAD, rejects staged-new and any index/worktree divergence at either link, and directly executes an immutable private materialization of the authenticated downstream HEAD blob rather than mutable checkout bytes."
    - "TRTH-03: An unchanged committed shim plus unchanged committed downstream finalizer reaches the existing shell gate with the requested mode, while staged or unstaged downstream mutation and every other failed authentication case stop before any finalizer-controlled marker can be written."
  artifacts:
    - path: "scripts/validate_repository_truth.exs"
      provides: "Exact Git-index membership enforcement for tracked ledger rows and fail-closed standalone CLI argument handling"
    - path: "test/scripts/phase_164_repository_truth_test.exs"
      provides: "Disposable-repository committed-versus-untracked, pathspec-metacharacter, prefix-adjacency, exact-returned-path, and standalone CLI misuse contract"
    - path: ".gsd/extensions/finalize-phase/index.ts"
      provides: "HEAD-authenticated phase-shim-to-gate-script dispatcher with private immutable execution of the downstream gate-bearing blob"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Behavioral shim and downstream-script regressions, including unchanged shim with staged/unstaged downstream mutation and immutable committed downstream execution"
  key_links:
    - from: "scripts/validate_repository_truth.exs"
      to: "the repository Git index"
      via: "git --literal-pathspecs ls-files --error-unmatch followed by exact single-path stdout equality"
      pattern: "--literal-pathspecs.*ls-files.*--error-unmatch.*--"
    - from: ".gsd/extensions/finalize-phase/index.ts"
      to: "HEAD:.planning/.../164-FINALIZE.sh and HEAD:scripts/finalize_phase_164.sh"
      via: "Git object lookup and checkout/index equality at both links, followed by private materialization and direct Bash execution of only the authenticated gate-bearing script"
      pattern: "HEAD:|diff.*HEAD|mkdtemp|writeFile|pi.exec"
  flagged_assumptions:
    - "TRTH-01 unclassified probe: no new documentation or package-truth behavior is implicated; the executed 164-16 whole-document contract remains the authoritative satisfied disposition and is rerun only in the plan-level regression suite."
    - "TRTH-02 adjacency probe: subject identity remains exact full-string equality; Git runs in literal-pathspec mode for one subject and its stdout must equal that subject exactly, so prefix-adjacent or pathspec-metacharacter names cannot satisfy another row."
    - "TRTH-02 empty probe: empty/header-only ledgers remain rejected, and the standalone command with no arguments is now an explicit usage failure rather than silent success."
    - "TRTH-02 ordering probe: tracked membership is checked independently for each parsed row and remains invariant to ledger row order."
    - "TRTH-03 adjacency probe: both authenticated Git objects are addressed by exact repository-relative paths for the phase shim and downstream gate-bearing script; sibling phase directories and prefix matches remain non-authoritative."
    - "TRTH-03 empty probe: either absent HEAD blob, either missing finalizer file, and empty/invalid command arguments fail before execution."
    - "TRTH-03 ordering probe: HEAD/object and index/worktree authentication for both the shim and downstream gate script must complete before materialization or Bash dispatch; cleanup runs after dispatch without changing which downstream bytes were authorized."
  prohibitions:
    - statement: "D-05/D-06/D-12: MUST NOT represent an untracked regular file as tracked repository proof."
      status: unresolved
      flagged: true
    - statement: "D-09/D-10/D-11: MUST NOT execute finalizer bytes that are absent from or differ from the authenticated HEAD object."
      status: unresolved
      flagged: true
---

<objective>
Close the two remaining Phase 164 trust-anchor defects and the bounded validator CLI warning.

Purpose: Make tracked-artifact classification and finalizer execution derive from immutable Git authority under adversarial but valid repository states.
Output: Literal-pathspec exact-index ledger validation, fail-closed direct CLI handling, full shim-to-gate-script HEAD authentication with private downstream execution, and behavioral regressions at both production seams.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-RESEARCH.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-PATTERNS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-15-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-16-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a tracked ledger claim through exact Git-index evidence</name>
  <reversibility rating="reversible">The change tightens one internal validator predicate and its CLI wrapper while preserving the twelve-column ledger and public repository contract.</reversibility>
  <files>scripts/validate_repository_truth.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-08-SUMMARY.md
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
  </read_first>
  <behavior>
    - Test 1: in a disposable Git repository, a complete ledger row naming a committed regular file with `state=tracked` validates successfully.
    - Test 2: after removing that same path from the index while leaving its regular working-tree file intact, production `RepositoryTruthLedger.validate/2` returns a tracked-membership error naming the exact subject.
    - Test 3: a prefix-adjacent path or a subject containing pathspec metacharacters cannot satisfy the tracked-path check because Git is explicitly placed in literal-pathspec mode and the command's sole returned path must equal the ledger subject byte-for-byte.
    - Test 4: invoking `elixir scripts/validate_repository_truth.exs` with no arguments, an unknown option, or only one required option exits nonzero and emits a bounded usage/error diagnostic; the canonical `--repo` plus `--ledger` invocation still prints the existing valid signal and exits zero.
    - Test 5: requiring the script from ExUnit exposes `Mailglass.RepositoryTruthLedger` without accidentally running or halting the standalone CLI path.
  </behavior>
  <action>Start RED by adding a disposable-repository regression around the production `Mailglass.RepositoryTruthLedger.validate/2` seam, not a test-local membership helper. Construct the smallest repository/ledger fixture that preserves the production parser's canonical constraints, commit the tracked subject for the positive control, then remove only its index entry while retaining the regular file for the negative control reproduced in `164-VERIFICATION.md`. Add committed fixtures whose names contain Git pathspec metacharacters and whose names are prefix-adjacent, and prove each ledger row is decided only by its byte-for-byte subject. Tighten `ensure_tracked_subjects_exist/2` per D-05/D-06/D-12 so every `state=tracked` row requires both `File.regular?/1` and exact index membership obtained with argv equivalent to `git --literal-pathspecs ls-files --error-unmatch -- <subject>` in the supplied repository. Parse the successful command output as exactly one returned repository-relative path and require that path to equal the requested subject exactly; treat zero, multiple, prefix-only, or otherwise unequal returned paths as non-membership defense in depth. Preserve specific error categories for missing files, Git command failure, and untracked or identity-mismatched subjects, and do not use a shell-interpolated pathspec. Add subprocess tests for canonical direct invocation and each invalid argument shape. Refactor the bottom-level runner only as needed so direct script execution always parses arguments and fails closed while `Code.require_file/2` remains side-effect-free; retain `parse/1`, `audit_subjects/1`, and `validate/2` signatures and do not change the ledger schema, canonical relationships, D-08 row, ignore inventory, or closeout caller flags.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs --only phase_164_trust_anchor --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv</automated>
    <fails_when>The tagged group selects zero tests or exits nonzero; an untracked regular file validates as tracked; Git pathspec expansion remains enabled; returned-path output is not asserted as exactly one path equal to the subject; a metacharacter or prefix-adjacent fixture satisfies the wrong row; invalid direct invocation exits zero or produces no diagnostic; requiring the module triggers CLI termination; or the canonical ledger no longer reports valid.</fails_when>
  </verify>
  <acceptance_criteria>
    - The tagged test creates and commits a regular file, proves validation passes, runs `git rm --cached` for that exact file without deleting it, proves `File.regular?/1` remains true, and proves production validation now returns a non-success tracked-membership result.
    - The production implementation invokes Git with an argv list, `cd: repo_root`, global `--literal-pathspecs`, `ls-files`, `--error-unmatch`, and `--` before exactly one subject; it does not concatenate the subject into a shell command.
    - A successful Git exit is insufficient by itself: normalized command output must contain exactly one path and that path must equal the requested subject exactly; zero, multiple, prefix-adjacent, or unequal results fail the membership predicate.
    - Production regressions cover a literal subject containing pathspec metacharacters and a prefix-adjacent tracked path, proving neither is expanded or confused with another index entry.
    - Every `state=tracked` row must satisfy file-type and index-membership checks; non-tracked row semantics remain unchanged.
    - No-argument, unknown-option, missing-repo, and missing-ledger standalone calls each exit nonzero with a stable diagnostic, while canonical invocation exits zero with `repository truth ledger: valid`.
    - The existing parser, inventory, canonical relationship hashes, D-08 evidence, disposition enum, and row-order invariance contracts remain green.
  </acceptance_criteria>
  <done>Repository truth can no longer certify a working-tree-only file as tracked proof, and direct CLI misuse is observably fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authenticate and execute only the committed finalizer blob</name>
  <reversibility rating="costly">The extension's dispatch sequence is the protected closeout entry contract; changing it later requires coordinated GSD-runtime, Git-fixture, and lifecycle regression updates.</reversibility>
  <files>.gsd/extensions/finalize-phase/index.ts, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .gsd/extensions/finalize-phase/index.ts
    - .gsd/extensions/finalize-phase/extension-manifest.json
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: a phase shim newly staged in a disposable repository but absent from HEAD is rejected before its marker payload executes.
    - Test 2: with the phase shim unchanged and committed, an unstaged modification to the committed `scripts/finalize_phase_164.sh` is rejected before the modified downstream marker payload executes.
    - Test 3: with the phase shim unchanged and committed, an index-staged modification to the committed downstream script is rejected before the staged or working-tree payload executes.
    - Test 4: an unchanged committed shim plus unchanged committed downstream script receives the repository root and optional `--pre-verification` mode through the real registered handler, enters the downstream script's first shell gate, and writes only its expected benign fixture marker.
    - Test 5: the successful path executes a private materialization whose bytes equal `git show HEAD:scripts/finalize_phase_164.sh`, not either checkout pathname; authentication covers both exact HEAD blobs and the private directory is removed on success and failure.
  </behavior>
  <action>Start RED by replacing the source-string-only dispatch assertion with a behavioral harness around the real registered extension handler. Use disposable Git repositories and a fake `ExtensionAPI` that records `registerCommand` and delegates `pi.exec` to real bounded subprocesses. Keep the phase shim unchanged and committed while independently applying staged and unstaged hostile marker mutations to `scripts/finalize_phase_164.sh`; retain staged-new and modified-shim cases, and include an unchanged-both positive control, so the test proves the transitive executable bytes that did or did not run. Harden `.gsd/extensions/finalize-phase/index.ts` per D-09/D-10/D-11 by deriving the exact repository-relative phase-shim path and the exact `scripts/finalize_phase_164.sh` downstream path, proving both `HEAD:<path>` objects exist as blobs, retaining exact index membership for each with global Git literal-pathspec mode and exact single returned-path equality, and using literal-pathspec semantics for every Git comparison that accepts either path. Reject any index or working-tree difference from HEAD at either link before dispatch. The authenticated phase shim remains the command-selection contract, but do not execute it against the mutable repository path it embeds. Instead, read the downstream gate-bearing script's verified blob through Git, create a mode-0700 private temporary directory outside the checkout, write only those authenticated downstream bytes to a non-writable executable file, and execute Bash directly against that private path with `repoRoot` and the existing mode arguments. Remove the private directory in `finally` on every result. Preserve bounded output, print-mode nonzero behavior, phase-directory containment, positive-integer parsing, and every clean-tree/evidence gate in `scripts/finalize_phase_164.sh`. Do not run `/finalize-phase 164`, alter protected evidence, relax stable-porcelain checks, add a dependency, or change release/workflow authority.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_trust_anchor --warnings-as-errors --no-deps-check &amp;&amp; git diff --check</automated>
    <fails_when>The tagged group selects zero tests or exits nonzero; an unchanged shim allows staged or unstaged downstream-script bytes to write their hostile marker; staged-new or modified shim bytes execute; either authenticated link is absent from the asserted HEAD/index/worktree chain; the unchanged committed downstream blob does not enter its first gate with the exact arguments; Bash receives either checkout finalizer path; temporary materialization survives success/failure; existing error propagation regresses; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - The behavioral harness invokes the actual handler captured from `registerCommand`; a source-text grep is not the sole proof for any staged/modified/unchanged case.
    - Absence of either `HEAD:.planning/.../164-FINALIZE.sh` or `HEAD:scripts/finalize_phase_164.sh`, and staged or unstaged divergence at either exact path, returns nonzero before Bash dispatch and before any hostile marker is created.
    - Dedicated regressions keep the committed phase shim unchanged while staging and unstaging hostile changes to `scripts/finalize_phase_164.sh`; neither downstream payload executes, so a self-contained shim fixture cannot mask the transitive bypass.
    - The positive fixture proves the unchanged committed shim and downstream bytes authenticate, the downstream blob executes once, receives the disposable repository root plus requested mode, and reaches its first gate without executing a different working-tree payload.
    - Bash is invoked with the private materialized downstream pathname, never the resolved working-tree shim or downstream path; the materialized bytes originate from the authenticated `HEAD:scripts/finalize_phase_164.sh` blob.
    - Private materialization uses restrictive directory/file permissions and is removed in a `finally` path after successful and failed Bash execution.
    - The extension retains the existing path-containment, exact phase-directory count, bounded-tail, notification, `result.code`, and print-mode exit contracts.
    - No terminal or pre-verification Phase 164 capture is run during implementation; terminal finalization remains downstream of passing verification and protected completion metadata.
  </acceptance_criteria>
  <done>The protected command authenticates the full shim-to-gate-script chain and can execute only the downstream gate-bearing bytes from HEAD; behavioral tests prove hostile staged or unstaged downstream mutations cannot run through an unchanged shim.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/validate_repository_truth.exs` — tightens `Mailglass.RepositoryTruthLedger.validate/2` and its tracked-subject helper to use literal-pathspec Git-index membership plus exact returned-path equality; adds/refines a direct CLI entry function without changing `parse/1`, `audit_subjects/1`, `validate/2`, `--repo`, or `--ledger`.
- `test/scripts/phase_164_repository_truth_test.exs` — adds the `phase_164_trust_anchor` disposable-repository membership, pathspec-metacharacter, prefix-adjacency, returned-path identity, and direct CLI argument regressions.
- `.gsd/extensions/finalize-phase/index.ts` — authenticates the exact phase shim and downstream gate-bearing script against HEAD/index/worktree, privately materializes only the downstream HEAD blob, executes it directly, and guarantees cleanup while retaining the `finalize-phase` command signature.
- `test/scripts/phase_164_closeout_test.exs` — adds the `phase_164_trust_anchor` real-handler harness and proves an unchanged shim cannot transitively execute staged or unstaged downstream-script mutation, alongside staged-new/modified shim, unchanged-chain, exact-argument, and cleanup regressions.
- No new API, schema, dependency, workflow, release authority, package version, evidence-age rule, or terminal evidence artifact is created.

<threat_model>
## ASVS Level

OWASP ASVS L1. The high-severity evidence-spoofing and pre-gate code-execution threats block completion until the behavioral regressions pass at the production seams.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Ledger state claim → Git index | A regular working-tree file is not tracked proof unless literal-pathspec lookup returns exactly its subject and no other target-repository index path. |
| Working tree/index → protected finalizer dispatcher | Mutable or newly staged phase-shim and downstream-script bytes must not execute before the authenticated shell gates. |
| HEAD shim and downstream Git objects → temporary executable | Both links must authenticate, and only bytes read from the exact committed downstream gate-bearing blob may be materialized, executed, and then removed. |
| CLI arguments → repository-truth verdict | Missing or unknown arguments must be observable non-success rather than a silent green process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-61 | Spoofing | `state=tracked` ledger rows | high | mitigate | Require regular-file type, `git --literal-pathspecs ls-files --error-unmatch -- <subject>`, exact single returned-path equality, and metacharacter/prefix-adjacency disposable-repository regressions. |
| T-164-62 | Elevation of Privilege | finalize-phase extension dispatch | high | mitigate | Authenticate exact HEAD blobs and index/worktree equality for both the phase shim and `scripts/finalize_phase_164.sh`, then directly execute only a private materialization of the downstream committed blob. |
| T-164-63 | Tampering | downstream finalizer and temporary materialization | high | mitigate | Keep the shim unchanged in staged/unstaged downstream mutation regressions, use restrictive permissions, source executable bytes only from the downstream Git blob, never dispatch either checkout pathname, and clean up in `finally`. |
| T-164-64 | Repudiation | standalone validator invocation | medium | mitigate | Make every invalid invocation emit a bounded diagnostic and nonzero status under subprocess tests. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
  <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs test/mailglass/publish/maintaining_release_gate_contract_test.exs test/mailglass/docs_contract_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix format --check-formatted scripts/validate_repository_truth.exs test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs &amp;&amp; git diff --check</automated>
  <fails_when>Any command exits nonzero; either `phase_164_trust_anchor` task verification selected zero tests; literal path identity or full shim-to-downstream authentication regresses; or a high-severity threat remains reproducible. Do not invoke `/finalize-phase 164`; refresh ordinary verification only after this plan and its summary are integrated, then preserve the existing protected-main completion-metadata and terminal-finalization sequence.</fails_when>
</verification>

<success_criteria>
- Literal-pathspec Git index membership with exact single returned-path equality, not filesystem existence or pathspec matching alone, supports every tracked ledger claim.
- Standalone validator misuse cannot exit successfully without a verdict.
- Phase-shim or downstream gate-script bytes absent from or divergent from HEAD cannot execute through the extension, including when the shim is unchanged but the downstream script is staged or unstaged modified.
- The unchanged committed shim-to-downstream chain remains operational through private execution of the downstream HEAD blob and its existing fail-closed shell gates.
- TRTH-01 remains satisfied and untouched; TRTH-02 and TRTH-03 gain direct behavioral proof for the verifier's two failed trust boundaries.
</success_criteria>

## Prohibition Recall Audit

- Kept descriptor-less and unresolved: representing an untracked regular file as tracked proof is a repository-specific transparency prohibition from D-05/D-06/D-12 and the authoritative verifier.
- Kept descriptor-less and unresolved: executing finalizer bytes absent from or different from HEAD is a repository-specific operational-safety prohibition from D-09/D-10/D-11 and the authoritative verifier.
- Canon-referral breadcrumb: generic command injection, path traversal, and temporary-file attacks remain ASVS/security-review concerns; their phase-specific manifestations are concretely mitigated in the threat model without minting extra prohibitions.
- Dropped as routine engineering: missing arguments, empty input, duplicate rows, subprocess errors, and cleanup failures are explicit behavior and acceptance cases rather than values/safety prohibitions.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Tracked artifacts and final evidence describe actual supported and operational state | 164-17 | COVERED |
| REQ | TRTH-01 | Current maintainer/package guidance | Executed baseline 164-16; full contract rerun | SATISFIED-UNCHANGED |
| REQ | TRTH-02 | Evidence-backed tracked/generated classification using literal path identity and exact returned-path equality | 164-17 Task 1 | COVERED |
| REQ | TRTH-03 | Reproducible trustworthy final closeout authority across the shim-to-downstream execution chain | 164-17 Task 2 | COVERED |
| CONTEXT | D-01 through D-04 | Layered current/historical documentation and package truth | Executed baseline 164-03/16 | SATISFIED-UNCHANGED |
| CONTEXT | D-05, D-06, D-12 | Evidence-backed exact-one tracked disposition truth | 164-17 Task 1 | COVERED |
| CONTEXT | D-07, D-08 | Narrow ignores and locked stale-root removal | Executed baseline 164-01/04/15 | SATISFIED-UNCHANGED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, exact clean canonical state, exact protected evidence, and immutable execution through the first gate-bearing executable | 164-17 Task 2 | COVERED |
| RESEARCH | Git index is authority for tracked state; volatile proof executes established controls | 164-17 Tasks 1-2 | COVERED |
| PATTERNS | Shared production validator and project-local finalizer extension seams | 164-17 Tasks 1-2 | COVERED |
| VERIFICATION | CR-02 untracked regular file accepted as tracked | 164-17 Task 1 | COVERED |
| VERIFICATION | CR-01 mutable working-tree finalizer executes before gates | 164-17 Task 2 | COVERED |
| VERIFICATION | WR-01 invalid/no-argument validator exits zero silently | 164-17 Task 1 | COVERED |
| EDGE | TRTH-01 unclassified | Existing whole-document contract retained and rerun | 164-17 flagged assumption | COVERED-FLAGGED |
| EDGE | TRTH-02 adjacency / empty / ordering | Literal-pathspec exact subject plus exact returned-path equality, non-vacuous CLI/ledger, per-row order independence | 164-17 Task 1 and flagged assumptions | COVERED-ASSUMPTION |
| EDGE | TRTH-03 adjacency / empty / ordering | Exact relative HEAD objects for shim and downstream, either-object absence fail-closed, full-chain authentication before direct downstream dispatch | 164-17 Task 2 and flagged assumptions | COVERED-ASSUMPTION |
| PROHIBITION | False tracked proof | Descriptor-less unresolved safety/transparency constraint | 164-17 Task 1 | COVERED-FLAGGED |
| PROHIBITION | Unauthenticated finalizer execution | Descriptor-less unresolved operational-safety constraint | 164-17 Task 2 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-17-SUMMARY.md` when done. After the plan summary and refreshed passing verifier/completion metadata reach protected main, run the separately governed terminal finalizer with no later tracked write.
</output>
