---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 25
type: execute
wave: 23
depends_on: [164-24]
files_modified:
  - mix.exs
  - test/scripts/phase_164_closeout_test.exs
  - test/scripts/scheduled_control_evidence_test.exs
  - test/scripts/ci_parity_drift_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TRTH-03: The generic required `verify.ci_lane_contract` lane runs from tracked repository state without requiring `/Users/jon` installation or approval files."
    - "TRTH-03: Fixture-backed immutable-loader tests remain required-CI coverage, while tests of the actual installed command run only through one explicit controlled-host alias."
    - "D-09/D-11/TRTH-03: Excluding the controlled-host tag does not exclude repository-source finalizer, exact-SHA, scheduled-control, or fail-closed fixture contracts."
  artifacts:
    - path: "mix.exs"
      provides: "Hermetic required-lane alias plus an explicit controlled-host installed-boundary alias"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Separate fixture-backed required coverage and installed-host coverage with non-overlapping tags"
    - path: "test/scripts/ci_parity_drift_test.exs"
      provides: "Required-lane regression proving the generic alias cannot select host-only finalization tests"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix.exs verify.ci_lane_contract"
      via: "the existing required CI job continues to invoke the same semantic alias"
      pattern: "mix verify\\.ci_lane_contract"
    - from: "mix.exs verify.ci_lane_contract"
      to: "test/scripts/phase_164_closeout_test.exs"
      via: "directory auto-discovery with only the controlled-host tag excluded"
      pattern: "test test/scripts/.*exclude phase_164_installed_production_boundary"
  flagged_assumptions:
    - "TRTH-03 adjacency probe: fixture-backed and installed-host groups remain distinct execution authorities even though they share one source file; neither group substitutes for the other."
    - "TRTH-03 empty probe: both aliases must select a nonzero named test set, and zero selected tests fails their contracts."
    - "TRTH-03 ordering probe: required repository tests may run in any ExUnit order; the controlled-host alias remains separately and explicitly invoked after installation."
  prohibitions:
    - statement: "D-09/D-11: MUST NOT make a protected required lane depend on one maintainer's untracked installed command or approval record."
      status: unresolved
      flagged: true
---

<objective>
Restore hermetic protected CI while preserving explicit production-installed boundary proof.

Purpose: Remove the maintainer-local prerequisite from the required repository lane without weakening any repository-source regression.
Output: A required alias that excludes only the controlled-host tag, a named opt-in installed-boundary alias, and drift contracts for both scopes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-24-SUMMARY.md
@mix.exs
@.github/workflows/ci.yml
@test/scripts/phase_164_closeout_test.exs
</context>

<assumption_delta_decision>
Primary noun: finalization test authority. Decision: no-change — the detected “alternate phase” text is a hostile-input case, not a second supported identity; Phase 164 remains the only supported finalization target.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the required CI alias through repository-only finalizer coverage</name>
  <files>mix.exs, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-REVIEW.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-24-PLAN.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-24-SUMMARY.md
    - mix.exs
    - test/scripts/phase_164_closeout_test.exs
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - Test 1: `mix verify.ci_lane_contract` selects the repository fixture tests but does not execute any assertion that reads the absolute installed loader or Plan 164-23 approval file.
    - Test 2: the installed-production group remains active and non-vacuous under one explicit `mix verify.phase_164.installed_boundary` command.
    - Test 3: no source-level skip or conditional pass converts an unavailable installed command into green required CI.
  </behavior>
  <action>Start RED with an alias contract that reproduces CR-01 by identifying the host-only `phase_164_installed_production_boundary` tag inside the directory-selected `test/scripts/` scope. Change `verify.ci_lane_contract` in `mix.exs` to retain directory auto-discovery while excluding exactly that tag. Add `verify.phase_164.installed_boundary` as the sole alias selecting exactly that tag from `test/scripts/phase_164_closeout_test.exs`. Keep all source-loader and disposable-repository groups untagged or under their existing required tags so protected CI continues to exercise immutable commit authentication, exact numbered history, repository truth, scheduled evidence, and failure precedence. Do not add CI jobs, workflow triggers, environment fallbacks, or success-on-missing-host behavior; the separation is an execution-scope correction within D-09/D-11 and the fixed CI topology.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --exclude phase_164_installed_production_boundary --warnings-as-errors --no-deps-check &amp;&amp; git diff --check</automated>
    <fails_when>The repository-source run selects zero tests, reaches either absolute host file, executes an installed-production case, reports a failure, or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - The generic required-lane alias retains `test/scripts/` directory discovery and excludes exactly the installed-production tag.
    - A distinct alias names the controlled-host tag explicitly and contains no generic directory-only invocation.
    - Fixture-backed loader, ledger, CI, and scheduled-control tests remain selected by the required scope.
    - Missing external installed state can neither fail nor silently satisfy the generic required lane because that state is outside its selected test authority.
  </acceptance_criteria>
  <done>The required repository lane executes its Phase 164 contracts without touching maintainer-local installed state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock required-versus-controlled-host scope in CI drift contracts</name>
  <files>test/scripts/scheduled_control_evidence_test.exs, test/scripts/ci_parity_drift_test.exs, mix.exs</files>
  <read_first>
    - mix.exs
    - .github/workflows/ci.yml
    - test/scripts/scheduled_control_evidence_test.exs
    - test/scripts/ci_parity_drift_test.exs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: the existing required CI job still calls `mix verify.ci_lane_contract` and never calls the controlled-host alias.
    - Test 2: the required alias carries the exact installed-production exclusion and the controlled-host alias carries the matching exact inclusion.
    - Test 3: removing either side of the alias pair, broadening the exclusion, or moving fixture-backed tests behind the host tag makes the contract fail.
  </behavior>
  <action>Extend the existing CI-parity and scheduled-control source contracts instead of creating a second CI policy model. Assert the unchanged `.github/workflows/ci.yml` call site, the exact required-lane exclusion, the exact controlled-host inclusion, and a nonzero set of source-loader/fixture tests outside the host-only describe tag. Make the contract inspect parsed alias/task text narrowly enough that a broad `test/scripts` exclusion or an empty opt-in alias fails. Preserve the directory-glob rationale for all other `test/scripts` files and do not alter required lane count, workflow topology, release authority, or scheduled-control behavior.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/ci_parity_drift_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.ci_lane_contract</automated>
    <fails_when>Either focused contract fails, the required alias is absent from the existing CI job, the host alias enters protected CI, the exclusion is broader than the exact tag, the generic lane selects host-only assertions, or the required lane exits nonzero.</fails_when>
  </verify>
  <acceptance_criteria>
    - CI source and Mix aliases form one mechanically checked required-to-hermetic mapping.
    - The controlled-host production group remains runnable by a named command and is not skipped or deleted.
    - The complete generic required lane succeeds using repository-tracked inputs only.
    - Required lane count, names, topology, and release policy remain unchanged.
  </acceptance_criteria>
  <done>Future alias or CI drift cannot silently reintroduce maintainer-local files into protected required CI.</done>
</task>

</tasks>

## Artifacts this phase produces

- `mix.exs` — `verify.ci_lane_contract` repository-only scope and `verify.phase_164.installed_boundary` controlled-host scope.
- `test/scripts/phase_164_closeout_test.exs` — explicit separation of fixture-backed and installed-production test authority.
- `test/scripts/ci_parity_drift_test.exs` — required-lane hermeticity regression.
- `test/scripts/scheduled_control_evidence_test.exs` — unchanged CI call-site and alias-scope contract.

<threat_model>
## ASVS Level

OWASP ASVS L1; high-severity CI hermeticity and false-green evidence threats block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub runner → repository tests | A protected runner has tracked checkout state but no maintainer-local installed command or approval record. |
| Controlled maintainer host → installed boundary suite | External installed bytes are tested only when that host-specific alias is invoked explicitly. |
| CI workflow → Mix alias | The existing required job delegates its exact test scope to one versioned semantic alias. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-91 | Denial of Service | required CI lane | high | mitigate | Exclude only the installed-production tag from the generic directory scope and run the complete alias in verification. |
| T-164-92 | Repudiation | installed-boundary evidence | high | mitigate | Preserve a named explicit alias with nonzero tagged tests; do not convert missing host state into a pass. |
| T-164-93 | Tampering | CI/Mix scope mapping | high | mitigate | Source contracts bind the unchanged CI call site to exact include/exclude alias semantics. |
| T-164-94 | Elevation of Privilege | CI topology | low | accept | No job, trigger, permission, release, or protected-check topology changes are introduced. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install or dependency change occurs. |
</threat_model>

<verification>
Run the repository-only Phase 164 closeout suite, focused CI/source contracts, the complete required alias, and diff hygiene. The controlled-host alias remains available but is not a required-runner prerequisite.
</verification>

<success_criteria>
- Required CI is hermetic and non-vacuous.
- Installed production evidence remains separately runnable and cannot be mistaken for required-runner evidence.
- Existing CI topology and all non-host repository contracts remain intact.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Required operational evidence reflects a runnable repository state | 164-25 | COVERED |
| REQ | TRTH-03 | Reproducible closeout proof can pass protected CI without local files | 164-25 Tasks 1–2 | COVERED |
| CONTEXT | D-09, D-11 | Dual proof and exact protected checks remain authoritative | 164-25 Tasks 1–2 | COVERED |
| RESEARCH | fixture-backed fail-closed tests and exact identity | Required fixture suite retained; installed suite isolated | 164-25 | COVERED |
| VERIFICATION | CR-01 | Generic required lane must not require absolute host files | 164-25 Tasks 1–2 | COVERED |
| EDGE | TRTH-03 adjacency/empty/ordering | Distinct aliases, nonzero selection, order-independent test execution | 164-25 | COVERED-FLAGGED |
| PROHIBITION | No maintainer-local protected-CI prerequisite | Descriptor-less unresolved constraint retained | 164-25 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-25-SUMMARY.md` when done. Do not run terminal finalization.
</output>
