---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 24
type: execute
wave: 22
depends_on: [164-23]
files_modified:
  - test/scripts/phase_164_closeout_test.exs
  - test/mailglass/docs_contract_test.exs
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
autonomous: true
gap_closure: true
requirements: [TRTH-01, TRTH-02, TRTH-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TRTH-03: The actually installed executable passes the moving-HEAD, middle-pair deletion, terminal-pair deletion, and extension-self-mutation subprocess regressions without evaluating checkout extension bytes."
    - "TRTH-01/TRTH-02: Complete current documentation and canonical ledger contracts remain green after the authority migration."
    - "D-09/D-10/D-11: Validation and lifecycle records describe observed installed-boundary results without claiming pre-verification or terminal evidence."
  artifacts:
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Production-installed boundary regression using the checkpointed executable"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md"
      provides: "Current proof map through Plan 24 with non-vacuous installed-loader results"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Final truthful lifecycle record leaving terminal capture pending"
  key_links:
    - from: "/Users/jon/.local/bin/mailglass-finalize-phase"
      to: "test/scripts/phase_164_closeout_test.exs"
      via: "real subprocess execution after checkpointed digest/self-check verification"
      pattern: "phase_164_installed_production_boundary"
    - from: "observed regression output"
      to: "164-VALIDATION.md and 164-FINALIZATION.md"
      via: "named commands, actual selected counts, and explicit non-terminal status"
      pattern: "moving HEAD|paired deletion|self-mutation|pending"
  flagged_assumptions:
    - "TRTH-01 unclassified probe remains explicitly flagged: complete doc contracts are the only asserted behavior; no new package-version semantics are inferred."
    - "TRTH-02 adjacency/empty/ordering remain the exact ledger semantics already enforced by the canonical production validator and rerun here."
    - "TRTH-03 adjacency/empty/ordering are the exact set/identity/authenticate-before-dispatch behaviors exercised through the installed production boundary."
  prohibitions:
    - statement: "D-09/D-10/D-11: MUST NOT treat a passing test-local import as proof that the installed production authority excludes checkout extension mutation."
      status: unresolved
      flagged: true
    - statement: "D-09: MUST NOT represent this regression/record reconciliation plan as terminal closeout evidence."
      status: unresolved
      flagged: true
---

<objective>
Prove the installed production boundary and reconcile the final automated lifecycle record.

Purpose: Demonstrate that all three authoritative verifier attacks fail through the command maintainers will actually run.
Output: Real installed-command subprocess evidence plus current validation and finalization records.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
@test/scripts/phase_164_closeout_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regress all three attacks through the installed production executable</name>
  <precondition>`164-23-SUMMARY.md` contains exactly one `installation_source_oid=&lt;40-lowercase-hex&gt;` and `source_sha256=&lt;64-lowercase-hex&gt;` record; `/Users/jon/.local/bin/mailglass-finalize-phase --self-check --repo /Users/jon/projects/mailglass --expected-source-oid &lt;recorded-installation-oid&gt;` succeeds before Task 1 edits, and both the installed file and the loader blob at the newly captured current `HEAD^{commit}` hash to the recorded digest without requiring current HEAD to equal the installation OID.</precondition>
  <reversibility rating="reversible">This task extends fixture coverage around an already installed command without changing its authority contract.</reversibility>
  <files>test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - scripts/finalize_phase_164.sh
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <behavior>
    - Test 1: invoke the actual installed path against an accepted disposable repository and prove private downstream execution uses the captured commit.
    - Test 2: move HEAD between authenticated Git calls and prove the installed process exits nonzero before downstream execution.
    - Test 3: delete both Plan/Summary 10, then 20, then 24 in separate captured commits and prove each exact-set authentication fails.
    - Test 4: create an assume-unchanged hostile copy of the former project-local extension which would write a marker if evaluated; invoke the installed executable and prove the marker remains absent.
    - Test 5: the positive and every negative fixture verify the immutable installation OID/digest from Plan 164-23, separately capture the current fixture commit, and require that current commit's loader blob to remain byte-identical without equating the two commit OIDs; every private root is cleaned afterward.
  </behavior>
  <action>Before changing tracked test bytes, parse the exact `installation_source_oid` and `source_sha256` fields from `164-23-SUMMARY.md`, run the installed loader's non-dispatching self-check against that immutable installation OID, capture the now-current full commit OID, and prove independently that both the installed executable and `&lt;current-oid&gt;:scripts/mailglass_finalize_phase_loader.mjs` hash to the recorded digest. Retain the installation OID and newly captured execution-authority OID as separate provenance fields; a Plan 164-23 summary commit is expected to advance HEAD and is valid when the loader blob is unchanged. Extend the named `phase_164_installed_production_boundary` group to call `/Users/jon/.local/bin/mailglass-finalize-phase` directly, not the tracked source path or a test-local import. Reuse the disposable Git fixture and real Git/Bash subprocess patterns from Plans 164-17 through 164-21. Assert the checkpointed installed digest and immutable installation-OID self-check before each matrix, then separately capture the disposable repository's execution-authority commit. Exercise one positive private dispatch, deterministic moving HEAD, paired deletions for 10/20/24, and a hostile assume-unchanged recreation of `.gsd/extensions/finalize-phase/index.ts`; the hostile module must contain an observable marker payload and remain unevaluated. Require every negative result to contain a bounded reason, record no Bash call/marker, and leave no authority root. Preserve canonical finalization as excluded; all runs target disposable fixtures or self-check mode.</action>
  <verify>
    <automated>summary=.planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md; test "$(grep -c '^installation_source_oid=' "$summary")" = 1 &amp;&amp; test "$(grep -c '^source_sha256=' "$summary")" = 1 &amp;&amp; install_oid="$(sed -n 's/^installation_source_oid=//p' "$summary")" &amp;&amp; source_digest="$(sed -n 's/^source_sha256=//p' "$summary")" &amp;&amp; test "${#install_oid}" = 40 &amp;&amp; test "${#source_digest}" = 64 &amp;&amp; current_oid="$(git rev-parse --verify 'HEAD^{commit}')" &amp;&amp; current_digest="$(git show "$current_oid:scripts/mailglass_finalize_phase_loader.mjs" | shasum -a 256 | awk '{print $1}')" &amp;&amp; installed_digest="$(shasum -a 256 /Users/jon/.local/bin/mailglass-finalize-phase | awk '{print $1}')" &amp;&amp; test "$current_digest" = "$source_digest" &amp;&amp; test "$installed_digest" = "$source_digest" &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/scripts/phase_164_closeout_test.exs --only phase_164_installed_production_boundary --warnings-as-errors --no-deps-check &amp;&amp; git diff --check</automated>
    <fails_when>The summary lacks exactly one parseable installation OID/digest; installed bytes or the newly captured current-commit loader blob differ from the recorded digest; the tag selects zero tests; moving HEAD or any paired deletion reaches Bash; hostile extension mutation writes its marker; a private root survives; canonical finalization runs; any command exits nonzero; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - Test output proves the absolute installed executable, not repository source, was the process under test.
    - Moving HEAD and deletion of middle pair 10, locked baseline terminal pair 20, or current terminal pair 24 each fail before Bash.
    - The self-mutated former extension contains a real marker payload but is never evaluated and cannot decide output or exit status.
    - The positive fixture executes only captured-commit private bytes and removes its authority root.
    - Evidence retains distinct immutable installation and current execution-authority OIDs while proving both commits contain byte-identical loader source.
    - The group selects at least five active behavioral cases with no skip used to hide an unavailable installation.
  </acceptance_criteria>
  <done>The actual installed authority defeats every verifier-reproduced attack, including extension self-mutation, through observable subprocess behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile validation and lifecycle records with observed installed-boundary proof</name>
  <reversibility rating="reversible">These evidence maps can be corrected locally if observed results change and do not execute finalization.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VALIDATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-21-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-22-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-23-SUMMARY.md
    - test/scripts/phase_164_closeout_test.exs
    - test/mailglass/docs_contract_test.exs
  </read_first>
  <action>After Task 1 produces observed non-vacuous results, update `164-VALIDATION.md` with Plan 164-21 through 164-24 task mappings, exact commands, threat IDs, selected/excluded/failure counts, installed OID/digest evidence, and the three closed verifier attacks. Update `164-FINALIZATION.md` only where necessary to match the installed executable actually proved. Extend the stable docs contract to require the recorded lifecycle distinctions without hard-coding current package version literals; derive any current compatibility expectation from manifests, thereby resolving WR-01 only where this touched contract already covers it. Explicitly state that no task in Plans 21–24 ran canonical pre-verification or terminal finalization and that ordinary verification plus protected completion-metadata integration still precede the final installed command run. Do not mark ROADMAP/REQUIREMENTS complete or write a committed final report.</action>
  <verify>
    <automated>make toolchain CMD='mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs test/mailglass/publish/maintaining_release_gate_contract_test.exs test/mailglass/docs_contract_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors' &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 elixir scripts/validate_repository_truth.exs --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; node --check scripts/mailglass_finalize_phase_loader.mjs &amp;&amp; bash -n scripts/finalize_phase_164.sh scripts/closeout_repository_truth.sh &amp;&amp; git diff --check</automated>
    <fails_when>Any command exits nonzero; the focused suite selects no tests or has failures; the canonical ledger is invalid; Node/Bash syntax fails; validation omits a new task/attack/result; docs retain a touched hard-coded current package major instead of deriving it; terminal evidence is claimed; or whitespace errors remain.</fails_when>
  </verify>
  <acceptance_criteria>
    - Validation maps all Plans 21–24 tasks, requirements, threats, commands, failure directions, and actual results.
    - All three current verifier gaps are mapped to named active installed-boundary regressions.
    - Finalization documentation matches the installed path, exact source provenance, captured OID, exact numbered range, and private dispatch behavior.
    - Any touched current package compatibility assertion derives its expectation from the package manifests; historical constraints remain unchanged.
    - Records explicitly leave canonical pre-verification and terminal finalization pending after ordinary verification and protected completion metadata.
  </acceptance_criteria>
  <done>Phase 164's automated proof map truthfully records closure of the three trust-boundary gaps without crossing the separately governed terminal capture.</done>
</task>

</tasks>

## Artifacts this phase produces

- `phase_164_installed_production_boundary` — real installed-executable subprocess group covering accepted execution, moving HEAD, pair deletion, and extension self-mutation.
- `164-VALIDATION.md` — Plan 21–24 proof map and observed results.
- `164-FINALIZATION.md` — confirmed installed-authority lifecycle with terminal evidence still pending.
- `test/mailglass/docs_contract_test.exs` — manifest-derived current compatibility assertion where WR-01 overlaps the touched contract.

<threat_model>
## ASVS Level

OWASP ASVS L1; tests must exercise the production installed boundary, and documentation may not overstate closure.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Installed executable → disposable repository | Production authority behavior is observed without canonical finalization side effects. |
| Hostile checkout extension → installed command | Checkout code must remain unevaluated even when hidden from ordinary status. |
| Test results → validation records | Only observed non-vacuous outputs may become closure claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-87 | Spoofing | test subject identity | high | mitigate | Assert absolute installed path and checkpoint digest before running the production-boundary matrix. |
| T-164-88 | Elevation of Privilege | hostile former extension | high | mitigate | Use an executable marker payload and prove direct installed invocation never imports it. |
| T-164-89 | Repudiation | validation/finalization records | high | mitigate | Record named commands and actual counts; retain explicit non-terminal status. |
| T-164-90 | Tampering | current documentation contract | medium | mitigate | Run whole-current-region and manifest-derived docs tests; preserve historical boundaries. |
| T-164-SC | Tampering | installed executable | high | mitigate | Re-run checkpointed source-OID/digest self-check before production-boundary tests. |
</threat_model>

<verification>
Run the production-installed attack matrix, complete focused Phase 164 tests via the proven toolchain command, canonical ledger validator, Node/Bash syntax, and diff hygiene. Canonical finalization remains excluded.
</verification>

<success_criteria>
- Actual installed bytes defeat all three verifier attacks.
- Complete Phase 164 contracts remain green and non-vacuous.
- Validation and finalization records match observed behavior and preserve terminal ordering.
- No product/API/schema/workflow/dependency/release-authority expansion occurs.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Maintainers can rely on final documentation/artifacts/evidence | 164-24 | COVERED |
| REQ | TRTH-01 | Current docs remain contract-checked | 164-24 Task 2 | COVERED |
| REQ | TRTH-02 | Canonical ledger remains exact and discoverable | 164-24 Task 2 | COVERED |
| REQ | TRTH-03 | All three attacks fail through installed production authority | 164-24 Task 1 | COVERED |
| CONTEXT | D-01 through D-04 | Current/historical prose and manifest-derived truth preserved | 164-24 Task 2 | COVERED |
| CONTEXT | D-05 through D-08, D-12 | Ledger/removal/durable proof remains valid | 164-24 Task 2 | COVERED |
| CONTEXT | D-09 through D-11 | Installed authority and non-terminal lifecycle proof | 164-24 Tasks 1–2 | COVERED |
| RESEARCH | exact identity / dual proof / fail closed | Production installed regression and lifecycle map | 164-24 | COVERED |
| VERIFICATION | gaps 1–3 | Moving HEAD, exact set, extension self-mutation | 164-24 Task 1 | COVERED |
| VERIFICATION | WR-01 | Derive touched current compatibility expectations | 164-24 Task 2 | COVERED-WHERE-TOUCHED |
| EDGE | all seven fallback rows | Authored as explicit exact behavior or flagged assumption | Plans 164-21, 22, 24 | COVERED-FLAGGED |
| PROHIBITION | Mutable authority / hollow completeness / false terminal claim | Descriptor-less unresolved constraints retained | 164-24 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-24-SUMMARY.md` when done. Then re-run ordinary Phase 164 verification; terminal installed-command finalization remains after protected completion metadata.
</output>
