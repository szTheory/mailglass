---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 11
type: execute
wave: 9
depends_on: [164-10]
files_modified:
  - .gitignore
  - .gsd/extensions/finalize-phase/extension-manifest.json
  - .gsd/extensions/finalize-phase/index.ts
  - scripts/finalize_phase_164.sh
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
  - scripts/ci_monitor.cjs
  - scripts/scheduled_control_evidence.sh
  - scripts/closeout_repository_truth.sh
  - test/scripts/phase_164_closeout_test.exs
  - test/scripts/phase_164_repository_truth_test.exs
  - test/scripts/scheduled_control_evidence_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-02, TRTH-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "D-09/D-10/TRTH-03: GSD exposes a durable project-local `/finalize-phase 164` boundary that runs after all ordinary tracked phase outputs have reached protected main."
    - "D-07/TRTH-02: Only the finalize-phase extension is re-included beneath `.gsd`; databases, exec logs, and all other local runtime state remain ignored."
    - "D-11/TRTH-03: Finalization automatically selects an exact-SHA, attempt-1 successful normal push CI run and consumes only attempt-1 naturally scheduled evidence; it accepts no caller-selected run ID and triggers no workflow."
    - "D-09/D-12/TRTH-03: Final capture writes ignored runtime evidence only, independently validates raw CI and scheduled sources, and fails if HEAD or stable porcelain changes."
    - "Ordinary phase verification proves the finalization capability and may complete tracked GSD metadata before the external-state gate; the later gate supplies terminal operational proof without another tracked write."
  artifacts:
    - path: ".gsd/extensions/finalize-phase/extension-manifest.json"
      provides: "Versioned community extension manifest declaring only the finalize-phase command"
    - path: ".gsd/extensions/finalize-phase/index.ts"
      provides: "Generic project-local command dispatcher for tracked phase finalizers"
    - path: "scripts/finalize_phase_164.sh"
      provides: "Phase-specific no-tracked-output protected-main evidence finalizer"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Maintainer lifecycle contract for the post-completion gate"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Runtime and contract coverage for the extension and finalizer"
  key_links:
    - from: ".gsd/extensions/finalize-phase/index.ts"
      to: "scripts/finalize_phase_164.sh"
      via: "numeric phase discovery, tracked-script check, and pi.exec"
      pattern: "registerCommand|finalize-phase|pi.exec"
    - from: "scripts/finalize_phase_164.sh"
      to: "scripts/closeout_repository_truth.sh"
      via: "automatic exact push-CI selection followed by ignored closeout capture"
      pattern: "event.*push|headSha|closeout_repository_truth.sh"
    - from: "tmp/phase-164-closeout/report.json"
      to: "raw CI and scheduled component sources"
      via: "independent jq verification of paths and provenance"
      pattern: "components.ci.source|components.scheduled.source"
  prohibitions:
    - statement: "D-09/D-11: MUST NOT dispatch or rerun automation, accept run attempt greater than 1, accept a caller-selected CI identity, push/merge/commit, alter workflow authority, increase configured ages, or write tracked state during finalization."
      status: unverified
      flagged: true
---

<objective>
Install a real post-completion GSD finalization boundary and bind Phase 164 to it.

Purpose: Let normal plan summaries and phase metadata finish first, then capture exact protected-main evidence without invalidating it with a later tracked commit.
Output: A versioned project-local command, a Phase 164 finalizer, tested lifecycle documentation, and complete repository-truth dispositions.

Integration budget: This intentionally spans the extension loader, attempt-provenance schema, shell finalizer, documentation, ignore boundary, and ledger because the attempt-1 identity must flow atomically from GitHub observation through authoritative sweep, closeout consumption, and terminal verification. Each concern remains isolated in its own task commit and focused test gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
@/Users/jon/.agents/skills/create-gsd-extension/SKILL.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
@.gitignore
@.github/scheduled-controls.json
@scripts/ci_monitor.cjs
@scripts/scheduled_control_evidence.sh
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
@test/scripts/phase_164_repository_truth_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Version the project-local finalize-phase command without exposing GSD runtime state</name>
  <reversibility rating="reversible">The extension is isolated beneath one project-local directory and the narrow ignore exception can be reverted with it.</reversibility>
  <files>.gitignore, .gsd/extensions/finalize-phase/extension-manifest.json, .gsd/extensions/finalize-phase/index.ts, test/scripts/phase_164_closeout_test.exs, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
    - .gitignore
    - test/scripts/phase_164_repository_truth_test.exs
    - test/scripts/phase_164_closeout_test.exs
    - /Users/jon/.agents/skills/create-gsd-extension/SKILL.md
    - /Users/jon/.agents/skills/create-gsd-extension/workflows/create-extension.md
    - /Users/jon/.agents/skills/create-gsd-extension/references/custom-commands.md
    - /Users/jon/.agents/skills/create-gsd-extension/references/key-rules-gotchas.md
  </read_first>
  <behavior>
    - RED 1: The manifest must identify a community extension compatible with GSD 2.29+ and declare exactly one command, `finalize-phase`.
    - RED 2: `/finalize-phase` accepts one positive integer phase plus only the optional literal `--pre-verification`; it rejects empty, nonnumeric, decimal, path-like, extra, or unknown arguments before executing anything.
    - RED 3: A valid phase resolves exactly one tracked `.planning/phases/<phase>-*/<phase>-FINALIZE.sh`; missing, ambiguous, untracked, or non-file targets fail closed.
    - RED 4: The command invokes the finalizer with `pi.exec`, reports bounded stdout/stderr through `ctx.ui.notify`, and surfaces a nonzero command result as an error.
    - RED 5: Git ignore behavior exposes only `.gsd/extensions/finalize-phase/` while `.gsd/gsd.db`, `.gsd/exec/`, and sibling extension/runtime paths remain ignored.
  </behavior>
  <action>Write the extension and ignore-boundary assertions first. Replace the broad `/.gsd/` rule with the minimum ordered rules needed to keep `.gsd/*` ignored, re-include the `extensions` parent, keep arbitrary extension siblings ignored, and re-include only `extensions/finalize-phase/`. Create the required manifest and TypeScript entry point. Register only `finalize-phase`; parse one positive integer phase token plus at most the exact optional `--pre-verification` flag; discover a single matching phase directory and `<phase>-FINALIZE.sh`; prove both paths are inside the repository and the finalizer is tracked at HEAD; then call `pi.exec("bash", [finalizer, repoRoot, ...validatedMode], {cwd: repoRoot})`. Do not use child_process, direct shell interpolation, event hooks, tools, stateful details, or dialog APIs. Truncate displayed output to a bounded tail and use `ctx.ui.notify` for success/error. The command itself must not modify Git, GitHub, planning files, or runtime evidence.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs test/scripts/phase_164_repository_truth_test.exs --warnings-as-errors --no-deps-check &amp;&amp; git check-ignore -q .gsd/gsd.db .gsd/exec/probe .gsd/extensions/other/index.ts &amp;&amp; test -z "$(git check-ignore .gsd/extensions/finalize-phase/index.ts .gsd/extensions/finalize-phase/extension-manifest.json)" &amp;&amp; test "$(ASDF_NODEJS_VERSION=22.14.0 gsd --version)" = "2.80.0"</automated>
  </verify>
  <acceptance_criteria>
    - The extension directory contains `index.ts` and `extension-manifest.json`; manifest `provides` matches the one registered command and contains no empty capability arrays.
    - The entry point imports `ExtensionAPI`, registers during load, uses `pi.exec`, and handles noninteractive UI safely without dialogs.
    - The pinned local `ASDF_NODEJS_VERSION=22.14.0` launcher reports GSD 2.80.0; the real extension load/invocation is deferred to Task 3, after Task 2 has created and committed the tracked phase finalizer.
    - Invalid or ambiguous phase inputs cannot execute a path.
    - Only the named extension is unignored; current `.gsd` runtime files and unknown sibling extensions remain ignored.
    - The repository-truth ledger contract detects and requires dispositions for the changed and added ignore rules.
  </acceptance_criteria>
  <done>The missing lifecycle command is durable, project-scoped, safely dispatched, and does not expose local GSD state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Phase 164 no-tracked-output finalizer</name>
  <reversibility rating="costly">The script becomes the terminal closeout procedure, but it is read-only toward remote state and writes only regenerable ignored evidence.</reversibility>
  <files>scripts/finalize_phase_164.sh, scripts/ci_monitor.cjs, scripts/scheduled_control_evidence.sh, scripts/closeout_repository_truth.sh, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md, test/scripts/phase_164_closeout_test.exs, test/scripts/scheduled_control_evidence_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
    - .github/scheduled-controls.json
    - scripts/ci_monitor.cjs
    - scripts/scheduled_control_evidence.sh
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - test/scripts/scheduled_control_evidence_test.exs
    - /Users/jon/.codex/gsd-core/workflows/execute-plan.md
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md
  </read_first>
  <behavior>
    - RED 1: Pre-verification mode requires clean exact protected main plus summaries for implementation Plans 01-11, but does not require Plan 12 summary, passed VERIFICATION, completed ROADMAP, or completed TRTH requirements. Terminal mode additionally requires every numbered plan summary, ROADMAP completion, TRTH-01..03 completion, and passed final verification.
    - RED 2: CI selection accepts only `workflowName == "CI"`, `event == "push"`, `attempt == 1`, `headBranch == "main"`, exact head SHA, completed status, and success conclusion; no run ID is accepted as input.
    - RED 3: Closeout writes only beneath ignored `tmp/phase-164-closeout/` and independently reads the raw paths named by `.components.ci.source` and `.components.scheduled.source`.
    - RED 4: Raw CI and each exact registered scheduled control are rejected on any attempt, event, SHA, branch, terminal status, workflow SHA, evidence-valid, result-envelope, or retained-artifact provenance mismatch.
    - RED 5: Any HEAD change or nonempty stable porcelain after capture invalidates the result.
  </behavior>
  <action>Write sourceable-function tests and fixture JSON first. Extend `scripts/ci_monitor.cjs` list/view fields with `attempt`. Extend scheduled-control verification to persist GitHub API `run_attempt` as `source_run.attempt`, require positive integer attempt 1 in authoritative evidence, and update its fixtures/contracts. Retain that attempt-1 check in the closeout scheduled predicate. Implement `scripts/finalize_phase_164.sh` with `set -euo pipefail`, one canonical repo argument, the optional literal `--pre-verification`, a standard `BASH_SOURCE` main guard, and no mutation except `git fetch origin main` plus ignored tmp writes. Both modes require branch main, successful fetch, HEAD equal origin/main, and empty stable porcelain. Pre-verification mode requires summaries for tracked implementation Plans 01-11 and writes `pre-verification-inputs.json` plus `pre-verification-report.json`; terminal mode requires summaries for every numbered plan, completed ROADMAP and TRTH-01..03, passed VERIFICATION, and writes `finalization-inputs.json` plus `report.json`. Query exact-SHA CI with fixed `gh run list` arguments including `attempt`, select the newest attempt-1 qualifying run deterministically, and persist only `main_sha` plus `ci_run_id`. Invoke `scripts/closeout_repository_truth.sh` with the canonical ledger and selected ignored report path. Whether closeout passes or fails, inspect the report and resolve the two raw source paths; require they canonicalize beneath that capture's ignored components directory. Independently validate raw CI fields including attempt 1 and the scheduled sweep's top-level pass/evidence-valid/exact-SHA fields, exact registry control set, and every control's attempt 1, schedule event, main branch, completed status, exact head/workflow SHA, evidence validity, nonblank reason, allowed pass or evidence-backed blocked result, 64-hex payload digest, and sha256 artifact archive digest. Delegate all age decisions to the successful authoritative sweep. Recheck HEAD and porcelain at the end. Add a tracked `<phase>-FINALIZE.sh` shim that execs the production script and mode. Document the pre-verification checkpoint, ordinary verifier, and post-completion operational-proof split in `164-FINALIZATION.md` and link it from `164-CLOSEOUT.md`. The script must never dispatch, rerun, push, merge, commit, publish, release, or edit tracked files.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check &amp;&amp; bash -n scripts/finalize_phase_164.sh .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh &amp;&amp; mix format --check-formatted test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - No CLI or extension argument can choose a CI run or remote workflow action.
    - The exact attempt-1 normal push CI identity is selected and independently rechecked from raw bytes.
    - Authoritative scheduled evidence persists `source_run.attempt` and both closeout and finalizer require attempt 1, so reruns cannot satisfy the gate.
    - The finalizer trusts registry-specific freshness only after a successful evidence-valid sweep and independently retains every identity/provenance/result check.
    - `finalization-inputs.json`, `report.json`, and component sources are ignored; no summary or tracked result is produced by the terminal invocation.
    - A blocked/pending/cannot-check report is preserved truthfully and produces a nonzero finalizer result.
    - `164-FINALIZATION.md` says Plan 12 supplies pre-verification evidence for the implementation SHA, the normal verifier checks that evidence/capability before phase.complete, and `/finalize-phase 164` supplies terminal evidence after completion metadata is integrated.
    - A real GSD load/invocation is performed during execution; before protected integration it must fail the appropriate precondition rather than contacting or mutating remote controls.
  </acceptance_criteria>
  <done>Phase 164 can be finalized after tracked completion with exact live evidence and no self-invalidating commit.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the extension, finalizer, lifecycle contract, and ignore rules in the authoritative ledger</name>
  <reversibility rating="reversible">Ledger rows are normal tracked evidence and can be corrected under the existing exact-one contract.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv, test/scripts/phase_164_repository_truth_test.exs</files>
  <read_first>
    - .gitignore
    - .gsd/extensions/finalize-phase/extension-manifest.json
    - .gsd/extensions/finalize-phase/index.ts
    - scripts/finalize_phase_164.sh
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZE.sh
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
  </read_first>
  <action>Add exactly one complete tracked/current retain disposition for each new durable extension/finalizer artifact and reconcile every changed/added non-comment `.gitignore` rule, preserving exact-one set equality. Explain that the narrow `.gsd` exceptions expose only the versioned extension while sibling runtime state stays ignored. Keep ignored finalization inputs/report represented by the existing `/tmp/` ignore-rule disposition, not as durable artifact rows. Preserve all existing stable IDs and D-08 semantics.</action>
  <verify>
    <automated>mix run scripts/validate_repository_truth.exs -- --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check &amp;&amp; git diff --check &amp;&amp; runtime_log=$(mktemp) &amp;&amp; trap 'rm -f "$runtime_log"' EXIT &amp;&amp; ! ASDF_NODEJS_VERSION=22.14.0 gsd --print --no-session "/finalize-phase 164 --pre-verification" &gt;"$runtime_log" 2&gt;&amp;1 &amp;&amp; grep -F "stable porcelain is not empty" "$runtime_log"</automated>
  </verify>
  <acceptance_criteria>
    - Every new tracked artifact and every changed/added ignore rule has exactly one complete disposition.
    - The production validator reports no missing subject, duplicate, blank field, invalid enum, or stale-without-outcome error.
    - Runtime evidence remains ignored/regenerable while the command, finalizer, and lifecycle contract remain tracked/discoverable.
    - With Tasks 1-2 already committed, pinned GSD 2.80.0 loads the real extension and invokes `/finalize-phase 164 --pre-verification`; current Task 3 dirt fails at the expected stable-porcelain precondition before GitHub observation.
  </acceptance_criteria>
  <done>The new GSD lifecycle boundary is covered by the same exact-one truth authority as the rest of Phase 164.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.gsd/extensions/finalize-phase/` — tracked project-local command and manifest.
- `scripts/finalize_phase_164.sh` plus the phase shim — terminal ignored-evidence finalizer.
- `164-FINALIZATION.md` and updated closeout guidance — durable lifecycle contract.
- Updated ledger/tests — exact-one coverage for artifacts and the narrow `.gsd` ignore boundary.

<verification>
Run focused extension/finalizer/ledger contracts, Bash syntax, ExUnit formatting, `git diff --check`, and one real GSD command invocation that fails closed before protected completion.
</verification>

<success_criteria>
- The finalizer extension loads and dispatches only a tracked phase finalizer.
- Phase 164 pre-verification and terminal captures select only attempt-1 exact normal CI and independently prove attempt-1 raw scheduled provenance.
- Ordinary GSD completion metadata precedes final capture; no tracked write follows it.
</success_criteria>

## Prohibition Recall Audit

- The only ignore-rule change is the minimum versioning boundary for the named project-local extension; all `.gsd` runtime data remains ignored.
- Manual/rerun/historical evidence, attempts greater than 1, caller-selected CI IDs, age expansion, authority changes, and tracked post-capture writes remain descriptor-less and flagged-unverified.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Pre-verification evidence unblocks truthful phase verification; terminal evidence describes actual state after tracked closeout metadata | 164-11, 164-12, post-completion gate | COVERED |
| REQ | TRTH-02 | Every new artifact and changed ignore rule has exact-one disposition | 164-11 Task 3 | COVERED |
| REQ | TRTH-03 | Reproducible attempt-1 exact-main closeout with independent raw-source proof | 164-10, 164-11 Tasks 1-2, 164-12 | COVERED |
| CONTEXT | D-07, D-09, D-10, D-11, D-12 | Narrow ignores, dual proof, exact canonical identity, exact evidence, complete ledger | 164-11 | COVERED |
| RESEARCH | Two-phase durable-versus-volatile proof | Versioned command plus post-completion ignored capture | 164-11 | COVERED |
| PROHIBITION | No manufactured or self-invalidating evidence | 164-11 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md` when done. That tracked implementation summary is the protected-integration precondition for Plan 164-12's `/finalize-phase 164 --pre-verification` checkpoint. After the verifier and phase.complete metadata are integrated, the terminal `/finalize-phase 164` invocation creates no SUMMARY or tracked output.
</output>
