---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 11
type: execute
wave: 9
depends_on: [164-10]
files_modified:
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
  - test/scripts/phase_164_closeout_test.exs
autonomous: true
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/TRTH-03: No normal executor plan claims final exact-main evidence, because every such plan necessarily writes a tracked SUMMARY after its task."
    - "D-09/D-10/TRTH-03: The post-execution gate runs only after every Phase 164 SUMMARY plus ROADMAP, STATE, REQUIREMENTS, and VERIFICATION update has itself reached protected main."
    - "D-11/TRTH-03: After that final tracked SHA receives normal push CI and naturally scheduled exact-SHA evidence, volatile main_sha and ci_run_id flow through ignored runtime state into the ignored final report."
    - "D-11/TRTH-03: Independent verification reads the raw files named by report.components.ci.source and report.components.scheduled.source and proves their exact event, SHA, branch, terminal status, workflow SHA, evidence-valid provenance, and allowed result envelope."
    - "No tracked commit, SUMMARY, ROADMAP edit, REQUIREMENTS edit, verification rewrite, dispatch, rerun, authority change, or configured-age increase occurs after final capture."
  artifacts:
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Tracked lifecycle contract for the post-execution, post-metadata, no-later-commit finalization gate"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md"
      provides: "Maintainer closeout contract linked to the post-execution boundary"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Contract tests for finalization ordering, ignored identity state, raw-source verification, and no-later-tracked-write rule"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv"
      provides: "Exact-one durable disposition for the tracked finalization contract"
  key_links:
    - from: "GSD phase.complete tracking commit on protected main"
      to: "164-FINALIZATION.md gate"
      via: "explicit post-execution precondition; never a phase-plan-index task"
      pattern: "ROADMAP.md|STATE.md|REQUIREMENTS.md|VERIFICATION.md"
    - from: "tmp/phase-164-closeout/finalization-inputs.json"
      to: "tmp/phase-164-closeout/report.json"
      via: "ignored main_sha and normal ci_run_id inputs"
      pattern: "main_sha|ci_run_id"
    - from: "report.components.ci.source and report.components.scheduled.source"
      to: "independent finalization assertions"
      via: "jq reads the raw referenced JSON files, not normalized component status alone"
      pattern: "databaseId|event|headSha|expected_main_sha|source_run|workflow_sha|evidence_valid"
  prohibitions:
    - statement: "D-09/D-11: MUST NOT run final capture inside a normal executor plan, commit any tracked metadata afterward, dispatch or rerun automation, alter workflow authority, or increase any configured max age."
      status: unverified
      flagged: true
---

<objective>
Install the lifecycle boundary that can truthfully produce final exact-main evidence.

Purpose: Separate ordinary tracked plan execution from the no-later-commit volatile capture that GSD's normal SUMMARY lifecycle cannot express.
Output: A tested tracked finalization contract, corrected closeout guidance, and complete ledger disposition.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
@.github/scheduled-controls.json
@scripts/ci_monitor.cjs
@scripts/scheduled_control_evidence.sh
@scripts/closeout_repository_truth.sh
@test/scripts/phase_164_closeout_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and contract-test the post-execution finalization gate</name>
  <reversibility rating="costly">The lifecycle boundary becomes the maintainer closeout contract; changing its ordering later requires coordinated documentation, tests, and operational practice, but no data migration or public API break.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md, .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md, test/scripts/phase_164_closeout_test.exs</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
    - .github/scheduled-controls.json
    - scripts/ci_monitor.cjs
    - scripts/scheduled_control_evidence.sh
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - /Users/jon/.codex/gsd-core/workflows/execute-plan.md
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md
  </read_first>
  <behavior>
    - Test 1: The finalization contract states that it is not a PLAN and cannot run until all Phase 164 SUMMARY files and phase.complete updates to ROADMAP, STATE, REQUIREMENTS, and VERIFICATION are integrated on protected main.
    - Test 2: The contract carries main_sha and ci_run_id only in ignored `tmp/phase-164-closeout/finalization-inputs.json`, then regenerates ignored `report.json` after normal push CI and naturally scheduled exact-SHA evidence.
    - Test 3: Independent verification resolves both raw source paths from the report and validates CI event/headSha/status/conclusion plus scheduled expected SHA, per-control event/branch/status/head/workflow SHA/evidence_valid/result envelope.
    - Test 4: The contract forbids a tracked write after final capture and requires a complete re-capture cycle after any later protected-main commit.
  </behavior>
  <action>Write the finalization-document assertions RED in `test/scripts/phase_164_closeout_test.exs`, then create `164-FINALIZATION.md` as an explicit GSD post-execution lifecycle gate rather than a numbered PLAN. Document why a normal plan is invalid: execute-plan always creates its tracked SUMMARY after task completion, and execute-phase later writes VERIFICATION plus phase.complete updates to ROADMAP, STATE, and REQUIREMENTS. Make the gate precondition require those tracked artifacts to be complete, committed, normally integrated into protected main, present in the fetched final SHA, and followed by zero pending tracked changes. After that SHA receives a normal successful push CI run and naturally scheduled exact-SHA controls, write only `main_sha` and `ci_run_id` to ignored `tmp/phase-164-closeout/finalization-inputs.json`; invoke the production closeout command to regenerate ignored `report.json`. Specify an independent verification block that obtains `ci_source` and `scheduled_source` with `jq -er` from the report, proves both paths resolve beneath canonical ignored tmp, and reads those raw JSON files. Require raw CI `databaseId`, `workflowName`, `event`, `headBranch`, `headSha`, `status`, and `conclusion` to match the input and normal push contract. Require raw scheduled sweep `status == pass`, `evidence_valid == true`, exact `expected_main_sha`, and the exact registered control set; for every control require evidence_valid, schedule event, main branch, completed status, exact head/workflow SHA, nonblank reason, result pass or blocked, a 64-hex payload digest, and a sha256 artifact archive digest. Trust the authoritative sweep for registry-specific ages and never add a second timestamp threshold. End the lifecycle without producing a SUMMARY or any tracked write; any later commit invalidates the report and restarts normal CI, natural schedules, capture, and independent verification. Update `164-CLOSEOUT.md` to point maintainers to this boundary and distinguish it from normal plan execution.</action>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - `164-FINALIZATION.md` explicitly names the tracked artifacts that must already be on protected main and states that it is outside phase-plan-index execution.
    - The contract refuses capture while a Phase 164 plan lacks its SUMMARY, phase tracking is not finalized, local HEAD differs from origin/main, or stable porcelain is nonempty.
    - Volatile identities exist only in ignored `finalization-inputs.json` and are independently matched to Git, raw CI source, raw scheduled source, and final report.
    - Independent CI verification reads `.components.ci.source`; it does not infer event/SHA/status from `.components.ci.status`.
    - Independent scheduled verification reads `.components.scheduled.source`; it does not assert a field on the normalized scheduled component that the report does not contain.
    - The scheduled raw-source predicate checks the exact three registry control IDs and every event/branch/status/head/workflow-SHA/evidence-valid/result/provenance field without recalculating freshness.
    - The contract requires no tracked commit after final capture and requires full re-capture after any later protected-main commit.
    - Manual dispatch, rerun, alternate SHA, force-push, release, publication, authority change, registry edit, or max-age increase is prohibited.
  </acceptance_criteria>
  <done>Maintainers have a tested lifecycle boundary that can run after all GSD metadata and produce independently verified final evidence without invalidating itself.</done>
</task>

<task type="auto">
  <name>Task 2: Add the finalization contract to the authoritative ledger</name>
  <reversibility rating="reversible">The ledger row is tracked evidence and can be corrected through normal review while preserving the fixed schema and exact-one subject rule.</reversibility>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv
    - scripts/validate_repository_truth.exs
    - test/scripts/phase_164_repository_truth_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
  </read_first>
  <action>Add exactly one complete tracked/current retain row for `164-FINALIZATION.md` under D-05/D-06/D-12. Name the Phase 164 post-execution finalization contract as producer, D-09 through D-12 as authority, the post-execution closeout gate as durable consumer, Plan 164-11 plus its focused contract as evidence, and explain that the tracked document defines the no-later-commit lifecycle while the identities/report remain ignored runtime artifacts. Preserve all existing stable IDs and D-08 evidence semantics; do not add ledger rows for ignored `finalization-inputs.json` or `report.json` unless the shared validator's repository-derived audited-subject contract requires their existing ignore-rule subject rather than an artifact row.</action>
  <verify>
    <automated>mix run scripts/validate_repository_truth.exs -- --repo /Users/jon/projects/mailglass --ledger /Users/jon/projects/mailglass/.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv &amp;&amp; mix test test/scripts/phase_164_repository_truth_test.exs test/scripts/phase_164_closeout_test.exs --warnings-as-errors --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - `164-FINALIZATION.md` has exactly one complete tracked/current retain row with a unique subject and one valid disposition.
    - The production validator reports no missing subject, duplicate, blank field, invalid enum, or stale-without-outcome error.
    - The row distinguishes the tracked lifecycle contract from the ignored volatile identity and report files.
    - Existing D-08 digest/removal evidence and all prior artifact/ignore dispositions retain their meaning.
  </acceptance_criteria>
  <done>The post-execution lifecycle contract is durable, discoverable, and covered by the same exact-one repository-truth authority as every other Phase 164 artifact.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md` — tracked post-execution lifecycle gate with preconditions, capture steps, raw-source independent verification, invalidation rule, and no-tracked-output contract.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md` — current maintainer guidance linked to finalization rather than a self-invalidating numbered plan.
- `test/scripts/phase_164_closeout_test.exs` — finalization ordering and raw-source contract assertions.
- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-TRUTH-DISPOSITION.tsv` — exact-one durable row for the finalization contract.
- The later gate produces ignored `tmp/phase-164-closeout/finalization-inputs.json` and `tmp/phase-164-closeout/report.json`; Plan 164-11 itself does not create or certify those volatile files.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GSD tracked completion state → post-execution gate | Final capture must occur only after every normal SUMMARY and phase-tracking write is integrated. |
| Ignored identity state → closeout invocation | SHA/run inputs must remain exact and cannot be substituted from tracked stale prose. |
| Normalized report components → raw source JSON | Independent verification must inspect the referenced authoritative bytes rather than trust wrapper labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-40 | Repudiation | final capture ordering | high | mitigate | Gate on all summaries and phase.complete tracking artifacts already present at protected HEAD; forbid later tracked writes. |
| T-164-41 | Spoofing | volatile SHA/run inputs | high | mitigate | Store ignored inputs and independently match them to Git, raw CI, raw scheduled evidence, and report. |
| T-164-42 | Tampering | normalized component status | high | mitigate | Resolve and validate raw `.components.ci.source` and `.components.scheduled.source` files. |
| T-164-43 | Elevation of Privilege | remote evidence acquisition | high | mitigate | Permit passive normal push/schedule observation only; prohibit dispatch, rerun, authority changes, and publication. |
| T-164-44 | Tampering | freshness registry | high | mitigate | Consume the authoritative sweep verdict and preserve 10800/129600/129600 without a duplicate age predicate. |
| T-164-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency, action, or lockfile change occurs. |
</threat_model>

<verification>
Run the focused closeout/scheduled contracts, production ledger validator, formatting checks for the modified ExUnit file, and `git diff --check`. The volatile finalization gate runs only after normal execute-phase metadata and protected integration, outside this plan.
</verification>

<success_criteria>
- Normal plan execution ends with all tracked lifecycle instructions committed before capture.
- The post-execution gate independently validates raw CI and scheduled source files.
- The final capture creates no tracked artifact and cannot be invalidated by a later GSD SUMMARY.
</success_criteria>

## Prohibition Recall Audit

- A normal executor plan cannot own final capture because its required SUMMARY is a later tracked write; the lifecycle contract states this limitation explicitly.
- Manual/rerun/historical evidence, age expansion, authority changes, and any tracked write after capture remain descriptor-less and flagged-unverified.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence describes actual repository state after all tracked closeout metadata | 164-11 finalization contract | COVERED |
| REQ | TRTH-03 | Reproducible exact-main clean closeout with independent raw-source proof | 164-10, 164-11 finalization contract | COVERED |
| CONTEXT | D-09, D-10, D-11, D-12 | Dual proof, exact canonical main, exact evidence, complete ledger | 164-05..11 | COVERED |
| CONTEXT | D-01..D-08 | Completed documentation/artifact decisions remain unchanged | 164-01..09 | COVERED-EXISTING |
| RESEARCH | Two-phase durable-versus-volatile proof | Post-execution boundary after all tracked metadata | 164-11 | COVERED |
| VERIFICATION | Freshness gap | Repair before protected final SHA, normal CI, natural schedules, ignored report | 164-10 plus finalization gate | COVERED |
| PROHIBITION | No manufactured or self-invalidating evidence | 164-11 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md` when done. This SUMMARY completes tracked plan execution; it is a precondition of the later `164-FINALIZATION.md` gate and is never written after final capture.
</output>
