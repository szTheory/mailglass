---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 11
type: execute
wave: 9
depends_on: [164-10]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/TRTH-03: The tracked Plan 164-10 repair is integrated through protected main before any final volatile state is sampled."
    - "D-11/TRTH-03: The exact then-current protected main SHA receives its normal push CI run, which is terminal and successful for that same SHA."
    - "D-11/TRTH-03: All three registered controls naturally schedule for that exact SHA and the authoritative sweep accepts each control under its unchanged registry-specific freshness window."
    - "Manual dispatch, rerun, alternate SHA, configured-age change, workflow-authority change, or publication is never accepted as evidence."
  artifacts:
    - path: "scripts/closeout_repository_truth.sh"
      provides: "Protected-main integrated registry-freshness consumer"
    - path: "test/scripts/phase_164_closeout_test.exs"
      provides: "Protected-main integrated focused regression contract"
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md"
      provides: "Exact observed main_sha and normal push ci_run_id handoff for final report generation"
  key_links:
    - from: "Plan 164-10 tracked repair"
      to: "protected main"
      via: "normal protected integration followed by normal push CI"
      pattern: "event.*push|headSha|completed|success"
    - from: "protected main SHA"
      to: "scripts/scheduled_control_evidence.sh sweep"
      via: "naturally scheduled exact-SHA runs for all registered controls"
      pattern: "event.*schedule|expected_main_sha|evidence_valid"
  prohibitions:
    - statement: "D-09/D-11: MUST NOT dispatch, rerun, force-push, publish, alter configured ages, change workflow authority, or substitute historical evidence during the external wait."
      status: unverified
      flagged: true
---

<objective>
Integrate the tracked freshness repair and wait for normal exact-SHA external evidence.

Purpose: Preserve the required order: tracked repair, protected main, normal push CI, natural schedules, then final report.
Output: Revalidated `main_sha` and normal `ci_run_id` inputs for Plan 164-12.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
@.github/workflows/ci.yml
@.github/scheduled-controls.json
@scripts/ci_monitor.cjs
@scripts/scheduled_control_evidence.sh
@scripts/closeout_repository_truth.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Integrate repair and observe normal CI plus natural schedules</name>
  <reversibility rating="costly">Any later protected-main commit invalidates the observed SHA/run/control set and requires another complete natural evidence cycle, although no data or published interface is migrated.</reversibility>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FRESHNESS-GAP.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-10-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CLOSEOUT.md
    - scripts/closeout_repository_truth.sh
    - test/scripts/phase_164_closeout_test.exs
    - scripts/scheduled_control_evidence.sh
    - test/scripts/scheduled_control_evidence_test.exs
    - .github/scheduled-controls.json
    - .github/workflows/ci.yml
    - scripts/ci_monitor.cjs
  </read_first>
  <action>Confirm the committed Plan 164-10 repair passed its focused contracts, then integrate that tracked commit through the repository's normal protected path. Fetch `origin/main` into `/Users/jon/projects/mailglass`, fast-forward canonical `main`, and require local HEAD and origin/main to be the same 40-hex SHA containing the repair. Observe the normally triggered `CI` workflow for that exact SHA and accept only event `push`, exact headSha, terminal `completed`, and conclusion `success`. Then wait for release-please, repo-hygiene, and post-publish-smoke to run naturally with event `schedule` at that exact SHA. Run the authoritative sweep with `GITHUB_REPOSITORY=szTheory/mailglass`; require a pass/evidence-valid report for the same expected_main_sha and all three registered controls under the unchanged 10800/129600/129600 registry values. Do not dispatch, rerun, publish, merge an unrelated change, modify workflows, change authorization, or edit any configured age to advance. Record the immutable `main_sha` and `ci_run_id` in the completion summary only after every assertion agrees.</action>
  <what-built>The TDD-proven closeout freshness repair from Plan 164-10, which consumes registry-authoritative freshness and preserves all exact identity/provenance checks.</what-built>
  <how-to-verify>
    1. Confirm the Plan 164-10 implementation commit is an ancestor of protected `origin/main`.
    2. Require canonical branch `main`, local HEAD equal to origin/main, and stable porcelain empty.
    3. Inspect the exact normal push CI run with `scripts/ci_monitor.cjs`; reject manual or historical runs.
    4. Run `GITHUB_REPOSITORY=szTheory/mailglass bash scripts/scheduled_control_evidence.sh sweep --output tmp/phase-164-closeout/scheduled-checkpoint.json` only after GitHub naturally emits all three exact-SHA scheduled runs.
    5. Keep the checkpoint blocked for pending, cannot-check, stale, mismatched, malformed, or missing evidence.
  </how-to-verify>
  <verify>
    <automated>mix test test/scripts/phase_164_closeout_test.exs test/scripts/scheduled_control_evidence_test.exs --warnings-as-errors --no-deps-check &amp;&amp; test "$(git branch --show-current)" = main &amp;&amp; test "$(git rev-parse HEAD)" = "$(git rev-parse refs/remotes/origin/main)" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)"</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 164-10 implementation commit and its normal protected integration are ancestors of the supplied `main_sha`.
    - `main_sha` is 40-hex and equals fetched canonical HEAD and origin/main.
    - `ci_run_id` identifies the normal push CI workflow with exact matching headSha, terminal completed status, and success conclusion.
    - The authoritative sweep reports pass/evidence-valid for release-please, repo-hygiene, and post-publish-smoke at the same exact SHA after naturally scheduled runs.
    - `.github/scheduled-controls.json` still contains max ages 10800, 129600, and 129600; no workflow or authority file changes participate.
    - The summary records `main_sha=&lt;40-hex&gt;` and `ci_run_id=&lt;databaseId&gt;` for deterministic Plan 164-12 consumption.
    - No manual dispatch, rerun, alternate SHA, force-push, release, publication, authorization change, or freshness-limit increase is accepted.
  </acceptance_criteria>
  <resume-signal>Provide `main_sha=&lt;40-hex&gt; ci_run_id=&lt;databaseId&gt;` only after the normal CI and naturally scheduled exact-SHA evidence satisfy every acceptance criterion.</resume-signal>
  <done>The protected repair SHA, normal successful push CI run, and naturally scheduled authoritative control evidence are ready for independent final closeout.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md` — exact `main_sha` and normal `ci_run_id` handoff plus natural scheduled-evidence observations.
- `tmp/phase-164-closeout/scheduled-checkpoint.json` — ignored diagnostic output from the authoritative sweep; never committed as durable proof.
- No new production symbol, field, CLI flag, API, schema, UI, dependency, workflow, release, tag, publication, authorization, or ignore rule.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked repair commit → protected main | Final evidence is invalid until the exact repair is normally integrated. |
| GitHub run metadata → evidence handoff | Wrong events, SHAs, reruns, or historical runs could spoof current evidence. |
| Natural scheduler timing → authoritative sweep | Delayed schedules must remain pending rather than trigger a policy or authority change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-40 | Spoofing | protected-main SHA and CI run | high | mitigate | Require exact local/origin equality and normal push event/headSha/status/conclusion. |
| T-164-41 | Elevation of Privilege | integration and schedule acquisition | high | mitigate | Use protected integration and passive observation only; prohibit dispatch, rerun, authority changes, and publication. |
| T-164-42 | Repudiation | scheduled evidence timing/provenance | high | mitigate | Block until the authoritative registry-driven sweep validates all naturally scheduled exact-SHA controls. |
| T-164-43 | Tampering | freshness configuration | high | mitigate | Assert the unchanged 10800/129600/129600 registry values before handoff. |
| T-164-SC | Tampering | package supply chain | low | accept | No package or action installation occurs. |
</threat_model>

<verification>
Require the focused contracts, exact protected Git identity, normal push CI metadata, unchanged registry values, and a fresh authoritative sweep over natural exact-SHA scheduled runs.
</verification>

<success_criteria>
- The tracked repair precedes all volatile evidence.
- CI is normal, terminal, successful, and exact-SHA.
- All three controls arise naturally and pass the authoritative per-control freshness gate.
</success_criteria>

## Prohibition Recall Audit

- Manual, rerun, historical, alternate-identity, forced-publication, age-expansion, and authority-changing substitutes remain descriptor-less and flagged-unverified.
- The checkpoint observes a costly-to-refresh historical fact but introduces no one-way implementation decision.

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Final evidence samples the actual protected repository state | 164-11 | COVERED |
| REQ | TRTH-03 | Protected main, normal CI, and explained natural scheduled outcomes precede report | 164-11 | COVERED |
| CONTEXT | D-09, D-10, D-11 | Dual proof, exact canonical main, exact CI/control evidence | 164-11 | COVERED |
| RESEARCH | Two-phase closeout proof | Integrate durable repair before volatile capture | 164-11 | COVERED |
| VERIFICATION | Freshness-gap integration sequence | Protected repair, normal push CI, natural exact-SHA schedules | 164-11 | COVERED-CHECKPOINT |
| PROHIBITION | No manufactured external evidence | 164-11 | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-11-SUMMARY.md` when done.
</output>
