---
phase: 164-repository-truth-reconciliation-and-closeout
plan: 37
type: execute
wave: 34
depends_on: [164-36]
files_modified:
  - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
autonomous: false
gap_closure: true
requirements: [TRTH-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10/D-11/TRTH-03: One immutable proposal binds the exact Plan 164-36 protected-main OID, normal CI run, authenticated loader blob, physical runtime closure, installed predecessor, destination, and absent new rollback identity."
    - "TRTH-03: The proposal records successful Mix, Elixir, and OTP probes produced inside the exact loader child environment, not merely executable path validation."
    - "TRTH-03 edge contract: proposal and approval are exact-copy ordered; missing fields, duplicate keys, empty values, path aliases, tuple drift, or reordered status data fail closed."
    - "D-02/D-09: Human approval authorizes only the displayed exact tuple; it does not authorize future source, another toolchain, installation, or finalization."
  artifacts:
    - path: ".planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md"
      provides: "Pending exact replacement proposal/approval identity and non-terminal lifecycle handoff"
  key_links:
    - from: "164-36-SUMMARY.md"
      to: "Plan 164-37 proposal record"
      via: "exact protected-main SHA and numeric attempt-one CI run ID"
      pattern: "main_sha|ci_run_id"
    - from: "scripts/mailglass_finalize_phase_loader.mjs"
      to: "Plan 164-37 proposal record"
      via: "authenticated source blob plus exact-child Mix/Elixir/OTP probe output"
      pattern: "MAILGLASS_ERL|terminal_range=01-39"
    - from: "Plan 164-37 proposal record"
      to: "Plan 164-37 approval record"
      via: "byte-exact copy plus one terminal approval_status line"
      pattern: "approval_status=approved"
  flagged_assumptions:
    - "TRTH-03 empty probe: absent proposal, approval, OID, tool, version output, prior identity, destination, or rollback field is invalid."
    - "TRTH-03 ordering probe: approval is exactly proposal bytes followed by one status line; equivalent unordered key/value sets are not accepted."
  prohibitions:
    - statement: "D-09/D-10/D-11: Human approval MUST NOT install bytes, change protected controls, run finalization, or broaden authority beyond the exact displayed tuple."
      status: unverified
      flagged: true
    - statement: "T-164-109: Proposal or approval evidence MUST NOT be labeled terminal or substitute for the final no-later-write capture."
      status: unverified
      flagged: true
---

<!-- plan-criteria-allow: R6 - git diff --check intentionally validates the executor's current uncommitted task changes before commit. -->

<objective>
Publish and obtain explicit human approval for one exact protected-main loader and physically runnable toolchain tuple.

Purpose: Replace the invalid shim-bound approval without allowing path, commit, or environment drift between review and installation.
Output: An immutable proposal, its exact human-approved copy, and a tracked non-terminal lifecycle record.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-VERIFICATION.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-36-SUMMARY.md
@.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
@scripts/mailglass_finalize_phase_loader.mjs
@test/scripts/phase_164_closeout_test.exs
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the exact physical-toolchain replacement proposal</name>
  <files>.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md</files>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-36-SUMMARY.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-CONTEXT.md
  </read_first>
  <precondition>`164-36-SUMMARY.md` contains a full protected-main SHA and numeric attempt-one normal CI run ID, and current HEAD descends from that exact SHA.</precondition>
  <action>Consume the Plan 164-36 SHA/run handoff as data and independently revalidate its protected-main and CI field agreement. Read the loader only as the authenticated Git blob at that OID, verify its digest and absolute Node shebang, require terminal range 01–39, and extract all eight trusted tool identities including physical Mix, Elixir, and Erlang. Re-run lstat/realpath/type/owner/mode validation for every identity. Use the loader's exported child-environment builder and runtime probe to capture normalized Mix version, Elixir version, OTP release, and a digest over the exact ordered probe output. Build a strict versioned proposal containing proposal schema, installation source OID/digest, protected CI run ID, destination/mode, all trusted executable identities, the three runtime versions and probe digest, prior approval digest, installed predecessor digest/mode/stat identity, new rollback identity and verified absence, and terminal_last_plan=39. Inherit destination/prior/rollback semantics only from the active approved tuple; do not accept caller overrides. Atomically publish a regular non-symlink mode-0400 proposal under the Plan 164-37 proposal identity, read it back, validate exact key order/uniqueness/type/nonemptiness and all live facts, then run the named proposal-boundary alias. Update `164-FINALIZATION.md` with proposal digest, protected SHA/run, normalized runtime outputs, approval pending, and every later stage still pending.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.proposal_boundary &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; the proposal-boundary selection is empty; the real proposal is absent, symlinked, writable beyond mode 0400, malformed, unordered, duplicated, empty, or stale; protected SHA/CI differs; a tool is a shim or unsafe; exact-child runtime probing fails; prior/destination/rollback identity drifts; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - The proposal binds one authenticated protected-main loader blob and the exact successful CI run.
    - Physical Mix, Elixir, and Erlang identities and successful exact-child probe outputs are immutable proposal fields.
    - Prior installation and absent rollback are revalidated before publication.
    - Installation, finalization, workflow operation, and completion metadata remain unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">The proposal is non-mutating authority data and can be abandoned without changing installed bytes.</reversibility>
  <done>One immutable, fully revalidated Plan 164-37 replacement tuple is ready for exact human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve exactly the displayed immutable proposal</name>
  <read_first>
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-36-SUMMARY.md
    - .planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md
    - scripts/mailglass_finalize_phase_loader.mjs
    - test/scripts/phase_164_closeout_test.exs
  </read_first>
  <precondition>The Plan 164-37 proposal-boundary alias passes against the exact published proposal and the installed predecessor is unchanged.</precondition>
  <action>Re-run proposal verification immediately before prompting. Display every proposal field in recorded order, its SHA-256 digest, the protected-main SHA and CI run, physical Mix/Elixir/Erlang identities, normalized runtime probe outputs, installed predecessor identity, destination, rollback identity/absence, and the precise mutation Plan 164-38 would perform. State that approval is tuple-specific, installation is recoverable, finalization is excluded, and any changed field requires a new proposal. Wait for the exact response `approved`. Only after that response, revalidate every live field again and atomically publish a regular non-symlink mode-0400 approval whose bytes are exactly the proposal followed by one `approval_status=approved` line. Read back and verify exact-copy-plus-status form. If the response differs or any fact drifts, publish no approval and stop. Do not replace the installed object, create rollback bytes, invoke either finalization mode, or change tracked completion metadata.</action>
  <what-built>An immutable Plan 164-37 proposal binding protected source/CI, physical runtime identities and exact-child probe output, installed predecessor, destination, and rollback facts.</what-built>
  <how-to-verify>Review every ordered field and the proposal digest displayed by the executor. Confirm the physical Mix/Elixir/Erlang identities and normalized versions are expected, the source/CI pair is Plan 164-36's exact protected result, and the predecessor/destination/rollback tuple is the intended recoverable replacement. Respond exactly `approved` only for that tuple.</how-to-verify>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix verify.phase_164.proposal_boundary &amp;&amp; git diff --check</automated>
    <fails_when>Non-zero exit; the proposal tuple or digest differs from the displayed data; protected CI, source, toolchain probe, prior object, destination, or rollback fact drifts; the response is not exact; an approval is not exact-copy-plus-status mode 0400; installed/rollback state mutates; or diff whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - The maintainer sees and approves the complete immutable proposal, including physical runtime probe results.
    - Approval bytes equal proposal bytes plus exactly one final approved-status line.
    - Revalidation occurs both before the prompt and immediately before approval publication.
    - No installation, rollback creation, finalization, dispatch, rerun, release, or terminal claim occurs.
  </acceptance_criteria>
  <reversibility rating="reversible">Approval authorizes only the next recoverable install; abandoning the tuple leaves current installed state intact.</reversibility>
  <done>The exact Plan 164-37 proposal is human-approved without changing executable or terminal state.</done>
  <resume-signal>Respond exactly `approved` to authorize publication of this exact approval record, or provide any other response to reject it without side effects.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-FINALIZATION.md` — exact proposal identity, runtime probe facts, approval status, and later-stage pending order.
- Plan 164-37 proposal record — external mode-0400 immutable tuple at the established checkpoint authority.
- Plan 164-37 approval record — external mode-0400 exact proposal copy plus one approval-status line, created only after explicit approval.
- New installed object, rollback bytes, final report, or completion metadata: none.

<threat_model>
## ASVS Level

OWASP ASVS L1; proposal drift, ambiguous approval, and approval of an unusable runtime closure are high severity and block installation.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Protected-main handoff → proposal | Exact source/CI identity becomes immutable install authority data. |
| Authenticated tools → runtime probe | Path validation is insufficient unless the exact child environment can execute the closure. |
| Proposal → human approval | The maintainer authorizes one displayed byte-exact tuple. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-141 | Spoofing | proposal source/CI tuple | high | mitigate | Bind full source OID/digest and independently validated attempt-one normal CI run. |
| T-164-142 | Tampering | runtime tool tuple | high | mitigate | Bind physical path identities, normalized version outputs, and exact-child probe digest. |
| T-164-143 | Repudiation | human approval | high | mitigate | Display ordered fields and accept one exact response for exact-copy-plus-status publication. |
| T-164-144 | Elevation of Privilege | approval scope | high | mitigate | Separate approval from installation and exclude finalization/workflow authority. |
| T-164-145 | Tampering | proposal/approval files | high | mitigate | Atomically publish regular non-symlink mode-0400 records and read back exact bytes. |
| T-164-SC | Tampering | package supply chain | low | accept | No package installation or dependency change occurs. |
</threat_model>

<verification>
The named external proposal lane must non-vacuously verify the real record and all live facts before and after review. Approval remains a blocking human checkpoint and creates no executable or terminal evidence.
</verification>

<success_criteria>
- The new tuple is protected-main, exact-CI, exact-source, physical-tool, exact-child-probe, and predecessor bound.
- Approval is explicit and byte-exact.
- Installed and terminal state remain unchanged until their separately ordered plans.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Operational records truthfully describe proposed authority | 164-37 Tasks 1–2 | COVERED |
| REQ | TRTH-03 | Approved reproducible closeout authority | Tasks 1–2 | COVERED |
| CONTEXT | D-02 | One primary current path, prior tuple only as bounded provenance | Tasks 1–2 | COVERED |
| CONTEXT | D-09–D-11 | Durable/external proof separation and exact-main provenance | Tasks 1–2 | COVERED |
| RESEARCH | — | Immutable tuple, exact identity, recoverability-first mutation | Task 1 | COVERED |
| VERIFICATION | Invalid shim-bound approval | Replacement binds physical runnable closure | Task 1 | COVERED |
| VERIFICATION | Approval required before install | Explicit exact-tuple checkpoint | Task 2 | COVERED |
| EDGE | TRTH-03 empty/ordering | Missing fields fail; exact ordered proposal-copy contract | must_haves / Tasks 1–2 | COVERED-FLAGGED |
| PROHIBITION | Approval cannot broaden authority or become terminal | Descriptor-less constraints retained | must_haves.prohibitions | COVERED-FLAGGED |

<output>
Create `.planning/phases/164-repository-truth-reconciliation-and-closeout/164-37-SUMMARY.md` with proposal/approval digests and exact approved field values. Continue to Plan 164-38; do not execute installation or finalization here.
</output>
