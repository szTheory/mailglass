---
phase: 116-fixtures-idempotent-ratchet-arm
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements: [RATCHET-03]
user_setup: []

must_haves:
  truths:
    - "The interaction pillar is a set of BINARY pass/fail Playwright gates (not a 7th LLM-scored pillar) — the failing test name IS the diagnosis"
    - "Invariant 1 panel-above-scrim: a centroid elementFromPoint hit-test returns the dialog panel (or descendant), not the scrim, for every surface x theme"
    - "Invariant 2 scroll-chaining: scrolling an overlay to its end leaves window.scrollY of the background unchanged"
    - "Invariant 3 focus-restore: closing the overlay returns document.activeElement to the trigger element"
    - "Invariant 4 layout-jump/CLS: a region's getBoundingClientRect().height delta loading-vs-loaded is <= threshold (<=8px; 0px for synchronous surfaces)"
  artifacts:
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Four binary interaction-invariant gates parameterized across deliveries/inbound/preview x light/dark/system, extending the existing assertPanelAboveScrim seam"
      contains: "assertPanelAboveScrim"
  key_links:
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin (live surfaces)"
      via: "page.evaluate elementFromPoint / scrollY / activeElement / getBoundingClientRect — no screenshots, no pixel diff"
      pattern: "elementFromPoint"
---

<objective>
Add the ratchet's interaction pillar (RATCHET-03, interaction half): four BINARY pass/fail
Playwright gates — panel-above-scrim hit-test, scroll-chaining/overscroll-contain,
focus-restore-to-trigger, and layout-jump/CLS — each parameterized across deliveries/inbound/preview
x light/dark/system.

Purpose: These are true/false runtime DOM properties an LLM scoring a static PNG cannot observe and
a 1-4 aesthetic rubric must not be able to trade away. The failing test name is the diagnosis. They
extend the existing `assertPanelAboveScrim` seam in `structural.spec.js` rather than inventing new
infrastructure, and run in Wave 1 independent of the cohort. (The cohort-dependent EXTENSION of
panel-above-scrim against fjordline-aps/northstar data lands in the Bucket-A plan 116-05.)

Output: Four binary interaction gates in `structural.spec.js`, screenshot-free, no pixel-diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-CONTEXT.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-RESEARCH.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-UI-SPEC.md

# Existing seam this plan extends (read before implementing):
@mailglass_admin/e2e/structural.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel-above-scrim + scroll-chaining binary gates</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (assertPanelAboveScrim ~L528/1077 — the existing centroid elementFromPoint hit-test; the explicit-system pattern ~L1110-1120; existing emulateMedia theme drive and how the replay/inbound/preview overlays are opened)
    - 116-RESEARCH.md "Interaction pillar extension (clone assertPanelAboveScrim, structural.spec.js L528)" + "Don't Hand-Roll" (elementFromPoint hit-test, not screenshot/pixel diff)
    - 116-UI-SPEC.md "Interaction Invariants" Invariant 1 + Invariant 2 (exact contracts)
  </read_first>
  <action>
    Extend structural.spec.js (D-01) with the panel-above-scrim invariant parameterized across the three
    overlay surfaces (deliveries replay dialog, inbound modal, preview panel) x light/dark/system: reuse /
    generalize assertPanelAboveScrim so the centroid elementFromPoint hit-test asserts hit === el ||
    el.contains(hit) for every combination. Add the scroll-chaining/overscroll-contain invariant: with the
    overlay open, scroll it to scrollTop = scrollHeight and assert window.scrollY of the background document
    is unchanged (no scroll-chaining); assert at most one element in the overlay subtree has a vertical
    scrollbar. Drive theme via emulateMedia + the app's 3-wrapper (matching the existing system pattern).
    All assertions are binary pass/fail with a descriptive label so the test name diagnoses the failure.
    Screenshot-free; no pixel diff.
  </action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "panel above scrim|scroll-chaining|overscroll" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Panel-above-scrim passes for deliveries/inbound/preview x light/dark/system (binary)
    - Scroll-chaining: background window.scrollY unchanged after scrolling overlay to its end; <=1 scrollbar in overlay subtree
  </acceptance_criteria>
  <done>Two binary interaction gates (panel-above-scrim, scroll-chaining) pass across all surface x theme combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Focus-restore + layout-jump/CLS binary gates</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (existing focus-keep-on-patch ~L1575 + reduced-motion getAnimations() ~L2418; the replay trigger id=replay-open-btn referenced in STATE.md as the JS.focus return target)
    - 116-UI-SPEC.md "Interaction Invariants" Invariant 3 (focus restore to trigger) + Invariant 4 (CLS, threshold <=8px, 0px for synchronous surfaces) + "Motion Contract" (measure the SETTLED state via waitForLoadState networkidle, not a mid-animation frame; MOTION-GATE bans height/width/padding transitions)
    - 116-RESEARCH.md interaction pillar example (focus-restore: assert document.activeElement === trigger; CLS: capture getBoundingClientRect().height loading vs loaded, assert delta <= threshold)
  </read_first>
  <action>
    Add the focus-restore invariant: open the replay modal on deliveries (trigger id=replay-open-btn or the
    equivalent per STATE.md [Phase ?]), close it via phx-remove / JS.hide, and assert document.activeElement
    === the trigger element, parameterized x light/dark/system. Add the layout-jump/CLS invariant: capture a
    content region's getBoundingClientRect().height in the loading state and in the settled state, and assert
    |loaded - loading| <= threshold; pick a concrete threshold <= 8px (Claude's Discretion per CONTEXT, but
    must be meaningful) and assert 0px for synchronous surfaces (inbound mount). Measure the settled state via
    waitForLoadState('networkidle') (or an explicit settle wait), NOT a mid-animation frame, so intentional
    reveal motion does not trip the assertion (Motion Contract). Binary pass/fail; the test name is the
    diagnosis. Note: A21 (CLS) and A22 (skeleton-on-synchronous) get their dedicated Bucket-A regression
    guards in plan 116-05 — this task establishes the interaction-pillar gates; do not duplicate, cross-cite.
  </action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "focus.restore|layout.jump|CLS" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Focus-restore: document.activeElement === trigger after the replay modal closes, x light/dark/system
    - CLS: region height delta loading-vs-loaded <= threshold (<=8px), 0px on the synchronous inbound mount
  </acceptance_criteria>
  <done>Two binary interaction gates (focus-restore, layout-jump/CLS) pass; the four-invariant interaction pillar is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard/pointer → overlay surfaces | Overlays must keep focus and pointer hit-testing inside the panel (no scrim interception, no focus loss) — a correctness/accessibility boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; the axe devDep is installed in plan 116-02 with its legitimacy checkpoint. |
| T-116-06 | Denial of service | overlay focus trap / scroll lock | mitigate | Binary gates assert focus restores to the trigger and background scroll is not chained — preventing a keyboard user from being stranded behind an open overlay. |
| T-116-07 | Tampering | LLM aesthetic score trading away interaction defects | mitigate | These invariants are binary pass/fail (not part of the 1-4 rubric) so a panel-behind-scrim or focus-loss defect cannot be "scored away" (D-01 rationale). |
</threat_model>

<verification>
- `cd mailglass_admin && npm run test:operator-browser -- --grep "panel above scrim|scroll-chaining|focus.restore|layout.jump|CLS"` green
- All four invariants pass across deliveries/inbound/preview x light/dark/system where applicable
- No screenshots / pixel diffs introduced
</verification>

<success_criteria>
- Four binary interaction gates pass: panel-above-scrim, scroll-chaining, focus-restore, CLS (RATCHET-03)
- Each is true/false; the failing test name is the diagnosis (D-01)
- Screenshot-free, no pixel-diff
</success_criteria>

<output>
Create `.planning/phases/116-fixtures-idempotent-ratchet-arm/116-03-SUMMARY.md` when done.
Record: the chosen CLS threshold value, the focus-restore trigger id used, and the exact `--grep`
labels (so plan 116-05's A21/A22 guards and plan 116-06's full-matrix run can reference them).
</output>
