---
phase: 116-fixtures-idempotent-ratchet-arm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/package.json
  - mailglass_admin/package-lock.json
  - mailglass_admin/e2e/axe-baseline.spec.js
  - mailglass_admin/docs/axe-baseline.json
  - mailglass_admin/test/mailglass_admin/axe_baseline_test.exs
autonomous: true
requirements: [RATCHET-03]
user_setup: []

must_haves:
  truths:
    - "@axe-core/playwright ^4.11.2 is a test-only devDep in mailglass_admin/ and is never imported into priv/static bundles"
    - "A producer spec regenerates the axe baseline current block: 3 surfaces x 3 themes = 9 cells, each {total, rules}, where total = sum of failing-node counts and rules maps rule-id -> node count"
    - "The system theme cell is genuinely distinct from light: the producer sets BOTH app theme = system AND emulateMedia colorScheme:dark so the media-query branch is exercised"
    - "[role=dialog] overlay violations fold into the surface that opened them (no 4th surface) — the 9-cell count is invariant"
    - "axe_baseline_test.exs fails closed: schema_version==1, all 9 cells present in both prior and current, prior.run_id != current.run_id, meet-or-beat per cell total AND per rule-id count"
  artifacts:
    - path: "mailglass_admin/docs/axe-baseline.json"
      provides: "9-cell WCAG 2.2 AA violation baseline (schema_version 1), sibling to ui-baseline-scores.json"
      contains: "schema_version"
    - path: "mailglass_admin/e2e/axe-baseline.spec.js"
      provides: "Playwright producer regenerating current.violations via AxeBuilder.withTags([...wcag22aa]).analyze()"
      contains: "AxeBuilder"
    - path: "mailglass_admin/test/mailglass_admin/axe_baseline_test.exs"
      provides: "Fail-closed ExUnit comparator (near-clone of ratchet_baseline_test.exs) with per-rule diff"
      contains: "schema_version"
  key_links:
    - from: "mailglass_admin/e2e/axe-baseline.spec.js"
      to: "mailglass_admin/docs/axe-baseline.json"
      via: "producer writes current.violations + a fresh run_id"
      pattern: "axe-baseline.json"
    - from: "mailglass_admin/test/mailglass_admin/axe_baseline_test.exs"
      to: "mailglass_admin/docs/axe-baseline.json"
      via: "__DIR__-relative docs/ path; Jason.decode!; compare_axe/2 meet-or-beat + per-rule diff"
      pattern: "axe-baseline.json"
---

<objective>
Add the WCAG 2.2 AA axe-violation JSON baseline (RATCHET-03, axe half): a test-only
`@axe-core/playwright` devDep, a Playwright producer that regenerates the 9-cell `current` block,
the committed `docs/axe-baseline.json`, and a fail-closed ExUnit comparator cloned from the proven
`ratchet_baseline_test.exs`.

Purpose: Establish a screenshot-free, no-pixel-diff accessibility ratchet — counts + per-rule
breakdown that make a regression diagnosable as `"inbound.dark: color-contrast 0 -> 2 (REGRESSION)"`.
This is the durable enforcement artifact for the project's WCAG 2.2 AA commitment and the A16-system
contrast-parity guard. It runs in Wave 1 (independent of the cohort) so the comparator exists for
the Wave 3 re-score.

Output: `axe-baseline.json` (schema 1), `e2e/axe-baseline.spec.js` (producer),
`axe_baseline_test.exs` (comparator), the test-only npm devDep + committed lockfile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-CONTEXT.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-RESEARCH.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-UI-SPEC.md

# Templates this plan clones (read before implementing):
@mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
@mailglass_admin/docs/ui-baseline-scores.json
@mailglass_admin/e2e/structural.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @axe-core/playwright test-only devDep</name>
  <files>mailglass_admin/package.json, mailglass_admin/package-lock.json</files>
  <read_first>
    - mailglass_admin/package.json (existing scripts: test:operator-browser uses --workers=1; @playwright/test ^1.59.1 pin)
    - 116-RESEARCH.md "Package Legitimacy Audit" (verdict OK — official Deque package, peer playwright-core >=1.0.0 satisfied by @playwright/test ^1.59.1, bundles axe-core ~4.11.4 with the wcag22aa rule pack) and "Compatibility verdict"
    - 116-UI-SPEC.md "Registry Safety" + "Zero-Node asset pipeline constraint"
  </read_first>
  <action>
    Install the only net-new dependency of the whole phase (D-05) as a TEST-ONLY devDep in
    mailglass_admin/ ONLY (NOT reference/demo_app/assets — axe stays admin-only per RESEARCH "Does axe
    live in the demo harness too?"): run `cd mailglass_admin && npm install --save-dev @axe-core/playwright@^4.11.2`
    (resolves 4.11.3). It must NEVER be imported by any file feeding priv/static/app.css — it is a
    Playwright-only devDep with zero runtime/app-bundle impact (Zero-Node asset pipeline constraint).
    Commit the resulting package.json + package-lock.json changes. Legitimacy is pre-verified in RESEARCH
    (official dequelabs/axe-core-npm source, mature 4.x line, no postinstall) — no further approval needed.

    Confirm the existing `mix mailglass_admin.assets.build` still produces a bit-identical priv/static bundle
    (the axe devDep must not perturb it).
  </action>
  <verify>
    <automated>cd mailglass_admin && npm ls @axe-core/playwright 2>&1 | grep -q '@axe-core/playwright@4.11' && echo "AXE_DEP_OK" || echo "AXE_DEP_MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - @axe-core/playwright@4.11.x present in mailglass_admin/ devDependencies; lockfile committed
    - Dep is test-only; not referenced by any priv/static asset source
  </acceptance_criteria>
  <done>The axe scanner is available to the admin Playwright harness as a test-only devDep.</done>
</task>

<task type="auto">
  <name>Task 2: Axe producer spec + committed 9-cell baseline JSON</name>
  <files>mailglass_admin/e2e/axe-baseline.spec.js, mailglass_admin/docs/axe-baseline.json</files>
  <read_first>
    - 116-RESEARCH.md "PRIMARY: @axe-core/playwright Wire Shape" (AxeBuilder API; violations[].nodes.length = the count; summarize() producing {total, rules}), "Code Examples" (producer spec + example axe-baseline.json shape), "Light / dark / system axis — emulateMedia" + Pitfall 1 (system cell must set app-theme=system AND emulateMedia colorScheme:dark, else it duplicates light)
    - mailglass_admin/e2e/structural.spec.js (existing emulateMedia + 3-theme wrapper patterns; surface URLs for deliveries/inbound/preview; how a [role=dialog] overlay is opened)
    - 116-UI-SPEC.md "Accessibility Contract" (9-cell shape, overlay folding, scan config)
  </read_first>
  <action>
    Create e2e/axe-baseline.spec.js (CommonJS to match playwright.config.cjs convention) per D-05/RESEARCH
    Code Examples: require AxeBuilder from "@axe-core/playwright"; for each surface in
    {deliveries, inbound, preview} x theme in {light, dark, system}, set the theme (light/dark via
    emulateMedia colorScheme; system via app theme=system AND emulateMedia colorScheme:dark so the
    media-query branch is genuinely exercised — Pitfall 1), navigate, open the surface's [role=dialog]
    overlay and FOLD its violations into the same surface cell (do NOT create a 4th surface — D-03), run
    AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']).analyze(), and
    summarize violations into {total, rules} where total = sum of v.nodes.length and rules[v.id] += v.nodes.length
    with deterministic sorted rule-id keys (stable committed diff). total counts FAILING NODES, not
    violations.length (RESEARCH "Counting decision" — node count is the stricter floor D-03 requires).

    Write docs/axe-baseline.json with schema_version: 1 and the 9-cell current block (and a prior block —
    on first establishment, prior may mirror current's structure with a DISTINCT prior run_id so the
    comparator's anti-vacuity guard is satisfiable; the real promotion happens in plan 116-06). The
    aspirational target is zero violations across all 9 cells after Bucket-A a11y fixes land; commit the
    actual measured counts. Screenshot-free, no pixel-diff.
  </action>
  <verify>
    <automated>cd mailglass_admin && node -e "const b=require('./docs/axe-baseline.json'); const s=['deliveries','inbound','preview'],t=['light','dark','system']; if(b.schema_version!==1)throw new Error('schema!=1'); for(const sf of s)for(const th of t){const c=b.current.violations[sf]&&b.current.violations[sf][th]; if(!c||typeof c.total!=='number'||typeof c.rules!=='object')throw new Error('missing '+sf+'.'+th)} console.log('AXE_BASELINE_9CELLS_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - axe-baseline.json has schema_version 1 and all 9 surface x theme {total, rules} cells in current
    - Producer spec uses .withTags([...wcag22aa]) and folds overlay violations into the opening surface
    - system cell driven by app-theme=system + emulateMedia colorScheme:dark (distinct from light)
  </acceptance_criteria>
  <done>A committed 9-cell axe baseline exists, regenerable by a screenshot-free Playwright producer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-closed ExUnit axe comparator (clone of ratchet_baseline_test.exs)</name>
  <files>mailglass_admin/test/mailglass_admin/axe_baseline_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs (FULL — the template: __DIR__-relative docs/ path L24, schema_version assert L40-44, all-cells-present coverage L46-62 with is_nil fail-closed, prior.run_id != current.run_id anti-vacuity L87-89, compare_baselines/2 L97-124)
    - 116-RESEARCH.md "Axe Comparator Clone Pattern (D-04)" (the compare_axe/2 structure: missing-cell fail-closed, total meet-or-beat, per-rule diff that fails closed on a rising rule count OR a new rule-id even under a flat total)
    - 116-CONTEXT.md D-04
  </read_first>
  <behavior>
    - assert schema_version == 1 (distinct from the score baseline's 3).
    - All 9 surface x theme cells present in BOTH prior and current; a missing cell is an error, never coerced to total:0.
    - assert prior.run_id != current.run_id (anti-vacuity).
    - Per cell: current.total > prior.total is a REGRESSION (message "inbound.dark: total P -> C (REGRESSION)").
    - Per rule-id: current.rules[rid] > (prior.rules[rid] || 0) is a REGRESSION even if total is flat (catches a rule swap); message "inbound.dark: color-contrast 0 -> 2 (REGRESSION)".
  </behavior>
  <action>
    Create test/mailglass_admin/axe_baseline_test.exs as a near-clone of ratchet_baseline_test.exs (D-04).
    Use the identical __DIR__-relative docs/ path trick to reach axe-baseline.json (sibling of
    ui-baseline-scores.json). Lift verbatim: the schema_version assertion (== 1), the all-cells-present
    coverage test over the 9 surface x theme cells with the is_nil fail-closed guard, and the
    prior.run_id != current.run_id anti-vacuity assertion. Implement compare_axe/2 per RESEARCH "Axe
    Comparator Clone Pattern": fail closed on a missing cell; flag current.total > prior.total; then a
    per-rule diff that flags any rule whose count rises OR any rule-id present in current but absent in
    prior with count > 0 (pcount defaults to 0) — so a rule swap under a flat total still regresses.
    Produce exactly the D-03 message shape.
  </action>
  <verify>
    <automated>cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/axe_baseline_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - axe_baseline_test.exs passes against the committed baseline (schema 1, 9 cells, distinct run_ids, no regression)
    - A deliberately injected rising rule count or new rule-id under a flat total fails the comparator (verify by construction / a fixture scenario)
  </acceptance_criteria>
  <done>A fail-closed ExUnit comparator enforces meet-or-beat axe counts + per-rule diffs in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → admin test toolchain | The net-new `@axe-core/playwright` devDep is fetched from npm; must be the official Deque package and stay test-only |
| committed baseline JSON → CI gate | `axe-baseline.json` is the durable artifact; a tampered/forgotten promotion must fail the comparator, not pass vacuously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-SC | Tampering | npm install (`@axe-core/playwright@^4.11.2`) | mitigate | Package legitimacy pre-verified in 116-RESEARCH "Package Legitimacy Audit": official `dequelabs/axe-core-npm` source, mature 4.x line, millions of downloads/wk, peer `playwright-core >= 1.0.0` satisfied, no postinstall scripts. Verdict OK (not `[ASSUMED]`). Commit a pinned lockfile. |
| T-116-04 | Tampering | axe-baseline.json promotion | mitigate | Comparator asserts `prior.run_id != current.run_id` — a forgotten current->prior promotion fails loudly (anti-vacuity), never a silent self-compare. |
| T-116-05 | Spoofing | system theme cell | mitigate | system cell must set app-theme=system AND emulateMedia colorScheme:dark so it exercises the real media-query branch and cannot silently duplicate the light cell (Pitfall 1). |
</threat_model>

<verification>
- `cd mailglass_admin && npm ls @axe-core/playwright` shows 4.11.x; lockfile committed
- `cd mailglass_admin && npm run test:operator-browser` (or scoped) runs the axe producer green
- `cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/axe_baseline_test.exs` passes
- `mix mailglass_admin.assets.build` still bit-identical (axe devDep does not perturb the bundle)
</verification>

<success_criteria>
- 9-cell `axe-baseline.json` (schema 1) committed + regenerable by a screenshot-free producer (RATCHET-03)
- Fail-closed comparator: schema/coverage/anti-vacuity/meet-or-beat + per-rule diff (D-04)
- system cell genuinely distinct from light (Pitfall 1 avoided)
- axe devDep is test-only and does not touch the asset bundle
</success_criteria>

<output>
Create `.planning/phases/116-fixtures-idempotent-ratchet-arm/116-02-SUMMARY.md` when done.
Record: the measured per-cell violation counts (input to plan 116-06 re-score), the resolved axe
version, and confirmation the asset bundle stayed bit-identical.
</output>
