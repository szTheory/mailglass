---
phase: 116-fixtures-idempotent-ratchet-arm
plan: 05
type: execute
wave: 2
depends_on: ["116-01", "116-02", "116-03"]
files_modified:
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/test/mailglass_admin/bucket_a_coverage_test.exs
  - .planning/research/v1.13/BUCKET-A-LEDGER.md
autonomous: true
requirements: [RATCHET-05]
user_setup: []

must_haves:
  truths:
    - "All 24 Bucket-A usability defects (A1..A24) have a live regression guard — ~18 cite an existing green guard from phases 109-115, 6 are net-new (A3, A4/A23, A16-system, A21, A22, A11)"
    - "The 6 net-new guards land: A3 hover-on-non-interactive-empty-state (on the helios-void no-data fixture), A4/A23 floating-element-vs-btn-primary overlap, A16-system dark-contrast parity, A21 loading CLS, A22 skeleton-on-synchronous-surface, A11 table-overuse audit + count-must-not-increase"
    - "bucket_a_coverage_test.exs is an executable manifest mapping each A-NN -> {guard_kind, locator, status} that ASSERTS each cited guard physically exists (gate name in check-conformance.sh; testid/title literal in e2e/*.spec.js; fixture testid) and fails closed on a stale citation"
    - "The ledger obeys the stable-ID / never-delete / downgraded-not-deleted contract; a regression reopens a row, the row is never removed"
    - "BUCKET-A-LEDGER.md is a human mirror, never the source of truth"
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/bucket_a_coverage_test.exs"
      provides: "Executable fail-closed manifest: A-NN -> guard existence assertion for all 24 defects"
      contains: "bucket_a"
    - path: ".planning/research/v1.13/BUCKET-A-LEDGER.md"
      provides: "Human-readable mirror of the manifest (stable-ID, never-delete)"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Net-new Playwright guards for A3, A4/A23, A16-system, A21, A22 (+ panel-above-scrim extension on cohort data)"
      contains: "btn-primary"
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "A11 table-count-must-not-increase grep inventory gate"
      contains: "table"
  key_links:
    - from: "mailglass_admin/test/mailglass_admin/bucket_a_coverage_test.exs"
      to: "mailglass_admin/scripts/check-conformance.sh"
      via: "manifest asserts each cited gate NAME literal physically exists in the script (fail-closed on rename/delete)"
      pattern: "check-conformance"
    - from: "mailglass_admin/test/mailglass_admin/bucket_a_coverage_test.exs"
      to: "mailglass_admin/e2e/structural.spec.js"
      via: "manifest asserts each cited data-testid / title string literal physically exists in the spec files"
      pattern: "structural.spec"
---

<objective>
Close all 24 enumerated Bucket-A usability defects (RATCHET-05), each with an individually
verifiable regression guard, as an AUDIT (verify-and-lock) not author-all-24: ~18 cite an existing
green guard from phases 109-115; 6 are net-new (A3, A4/A23, A16-system, A21, A22, A11). The durable
artifact is an executable manifest that fails closed on a stale citation.

Purpose: This is the milestone's accountability ledger — it makes "all 24 closed" checkable in CI
rather than a prose claim, and keeps it honest over time (a renamed/deleted guard fails the manifest).
The 6 net-new guards depend on the RATCHET-01 cohort (helios-void no-data surface, long-ID/non-ASCII
data) so this plan lands in Wave 2 after the cohort, axe baseline, and interaction pillar exist to cite.

Output: 6 net-new guards across `structural.spec.js` + `check-conformance.sh`, the executable
`bucket_a_coverage_test.exs` manifest, and the `BUCKET-A-LEDGER.md` human mirror.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-CONTEXT.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-RESEARCH.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-UI-SPEC.md
@.planning/research/v1.13/PITFALLS.md
@.planning/RATCHET-GAP-REGISTER.md

# Existing seams this plan extends + summaries of upstream plans:
@mailglass_admin/scripts/check-conformance.sh
@mailglass_admin/e2e/structural.spec.js
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-01-SUMMARY.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-02-SUMMARY.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Net-new Playwright guards A3, A4/A23, A16-system, A21, A22</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - .planning/research/v1.13/PITFALLS.md Bucket A — the exact "Guard/test" lines for A3, A4, A21, A22, A23 (and A16 for system parity)
    - 116-UI-SPEC.md "Bucket-A Defect Closure Contracts" (A3 hover-only-on-interactive on the no-data surface; A4/A23 floating-element rect vs btn-primary rect; A16-system axe_system.total <= axe_dark.total; A21 getBoundingClientRect height delta <= threshold, 0px synchronous; A22 zero .mg-skeleton on synchronous inbound mount)
    - 116-01-SUMMARY.md (helios-void no-data fixture reachability), 116-03-SUMMARY.md (interaction-pillar grep labels / CLS threshold to reuse), 116-02-SUMMARY.md (axe system vs dark cells for A16-system)
    - mailglass_admin/e2e/structural.spec.js (existing emulateMedia, getBoundingClientRect, .mg-skeleton phx-loading/phx-connected patterns)
  </read_first>
  <action>
    Author the 5 net-new Playwright guards in structural.spec.js (D-11), each with a stable, citeable
    test title / data-testid (the manifest in Task 3 asserts the literal exists):

    - A3: on the helios-void no-data fixture (direct URL), enumerate elements carrying a hover-derived CSS
      transition and assert each is a,button,[role=button],[phx-click],[tabindex] — decorative empty-state
      elements must NOT have hover transitions.
    - A4/A23: for each open floating element (theme menu, tooltip, flash notification), assert its
      getBoundingClientRect does NOT intersect any btn-primary rect on the same surface.
    - A16-system: assert contrast parity for theme=system + prefers-color-scheme:dark vs explicit dark — at
      minimum axe_system.total <= axe_dark.total per surface (reference the plan 116-02 axe system/dark cells);
      if a direct Playwright contrast assertion is used, mirror the existing contrast-matrix pattern.
    - A21: capture a region's getBoundingClientRect().height loading-vs-loaded; assert |delta| <= the CLS
      threshold from plan 116-03 (0px for synchronous surfaces); measure the settled state (networkidle), not
      a mid-animation frame.
    - A22: assert synchronous surfaces (inbound mount) show zero .mg-skeleton elements in the settled DOM
      (extends the existing "loading contract remains synchronous" test).

    Run on the cohort fixtures (helios-void for A3, the deliveries/inbound surfaces for the rest). Keep all
    assertions binary/structural — no screenshots, no pixel diff. Cross-cite (do not duplicate) the plan
    116-03 interaction-pillar CLS gate.
  </action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "A3|A4|A23|A16|A21|A22|hover|skeleton|floating|contrast parity" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - A3 passes on the helios-void no-data fixture (only interactive elements have hover transitions)
    - A4/A23 passes (no floating element overlaps a btn-primary rect)
    - A16-system passes (system contrast parity with dark; axe_system.total <= axe_dark.total)
    - A21 passes (CLS height delta <= threshold; 0px synchronous); A22 passes (no skeleton on synchronous inbound mount)
  </acceptance_criteria>
  <done>The 5 net-new Playwright Bucket-A guards pass against the cohort fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: A11 table-overuse grep inventory gate</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - mailglass_admin/scripts/check-conformance.sh (existing named gates: STATCARD-GATE, ICON-EXISTS-GATE, Z-INDEX-GATE, FOCUS-RING-GATE, PHASE112-SHELL-GATE, GAP-GATE, SPACE-GATE, GROUP-GATE, MOTION-GATE, DATA-STATE-GATE — match the grep-gate style; note comment-text discipline: an acceptance literal must not appear verbatim in lib/ files the gate negative-greps)
    - .planning/research/v1.13/PITFALLS.md A11 (grep inventory: count <table occurrences in lib/, require each maps to a justification line; meet-or-beat — count must not increase without justification) + 116-UI-SPEC.md A11 contract
  </read_first>
  <action>
    Add an A11 table-overuse gate to check-conformance.sh: count <table occurrences in mailglass_admin/lib/
    and fail if the count rises above its Phase-116-start value (a count-must-not-increase floor, baked as a
    constant in the gate). Filter out comment lines so header prose cannot self-invalidate the count
    (grep -v '^#' style hygiene; never a bare ==0 on an unfiltered file). The per-<table> justification rows
    live in the Bucket-A manifest (Task 3) — this gate is the count tripwire. Give the gate a stable name
    (e.g. TABLE-OVERUSE-GATE) so the manifest can cite it.
  </action>
  <verify>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - check-conformance.sh gains a named table-overuse gate that fails if the lib/ <table> count rises above the recorded floor
    - The gate filters comment lines; no bare ==0 on unfiltered files
    - Full check-conformance.sh still exits 0
  </acceptance_criteria>
  <done>A11 table overuse is locked by a count-must-not-increase grep inventory gate.</done>
</task>

<task type="auto">
  <name>Task 3: Executable Bucket-A coverage manifest + human ledger mirror</name>
  <files>mailglass_admin/test/mailglass_admin/bucket_a_coverage_test.exs, .planning/research/v1.13/BUCKET-A-LEDGER.md</files>
  <read_first>
    - .planning/research/v1.13/PITFALLS.md Bucket A (ALL 24 rows A1..A24 + each row's proposed Guard/test — the source of truth for which existing guard each defect maps to)
    - .planning/RATCHET-GAP-REGISTER.md (the stable-ID / never-delete / downgraded-not-deleted contract this manifest mirrors)
    - 116-CONTEXT.md D-12 + 116-RESEARCH.md Pitfall 4 (stale citation must fail closed — assert the cited string literal physically exists)
    - mailglass_admin/scripts/check-conformance.sh (the exact gate-name literals to cite) + 116-03-SUMMARY.md / 116-04-SUMMARY.md (the e2e test-title / testid literals to cite for the Playwright-guarded defects)
  </read_first>
  <behavior>
    - The manifest enumerates all 24 defects A1..A24, each mapped to {guard_kind in [:grep_gate,:playwright_testid,:playwright_title,:axe,:fixture], locator, status}.
    - For each :grep_gate citation, the test asserts the gate-name literal is present in check-conformance.sh (read the file, assert String.contains?).
    - For each :playwright_testid / :playwright_title citation, the test asserts the literal is present in the relevant e2e/*.spec.js file.
    - For each :axe citation, the test asserts the axe baseline / axe_baseline_test.exs reference exists.
    - For each :fixture citation, the test asserts the fixture testid / persona literal is present.
    - A stale citation (renamed gate, deleted testid) FAILS the test (fail-closed) — proving the ledger cannot drift silent.
    - All 24 rows have status indicating a live guard; a regression reopens (downgrades) a row, the row is never deleted.
  </behavior>
  <action>
    Create test/mailglass_admin/bucket_a_coverage_test.exs as the executable manifest (D-12). Encode all 24
    Bucket-A defects A1..A24 with their guard mapping (cite the EXISTING green guards from phases 109-115 for
    the ~18 already-covered defects — e.g. A1->Z-INDEX-GATE + panel-above-scrim; A7->GAP-GATE/SPACE-GATE;
    A12->STATCARD-GATE; A17->theme-picker test; A18->tab aria-selected test; A20->ICON-EXISTS-GATE; A24->the
    no-bare-dash empty-state test — read PITFALLS for each row's precise guard) and the 6 net-new guards from
    Tasks 1-2 + plans 116-02/03 (A3, A4/A23, A16-system, A21, A22, A11). For each, ASSERT the cited guard
    physically exists by reading the cited file and checking the literal is present (gate name in
    check-conformance.sh; test title / data-testid in the e2e spec; axe ref; fixture testid). Fail closed on
    absence (RESEARCH Pitfall 4). Govern the rows by the RATCHET-GAP-REGISTER stable-ID / never-delete /
    downgraded-not-deleted contract.

    DISCIPLINE: do not paste any negative-grep acceptance literal verbatim into a lib/ file. Keep citation
    string literals inside the test module only (it is a test/ file, not greppped by the lib/-scoped gates).

    Write .planning/research/v1.13/BUCKET-A-LEDGER.md as a thin human-readable mirror of the manifest (the
    24 rows + guard kind + status), clearly marked as a mirror, NEVER the source of truth.
  </action>
  <verify>
    <automated>cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/bucket_a_coverage_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 24 defects A1..A24 are enumerated with a guard mapping and a live status
    - The manifest fails closed when a cited gate name / testid / title literal is absent (verify by a documented negative scenario)
    - BUCKET-A-LEDGER.md mirrors the 24 rows and is marked not-the-source-of-truth
  </acceptance_criteria>
  <done>An executable, fail-closed manifest proves all 24 Bucket-A defects have a live regression guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage claim → CI enforcement | The "24 closed" claim must be machine-checked; a stale citation must fail rather than pass vacuously |
| floating overlays → primary CTAs | A4/A23 enforce that floats never obscure a primary action (a usability/accessibility boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; axe devDep handled in plan 116-02 with its legitimacy checkpoint. |
| T-116-10 | Repudiation | stale Bucket-A citation | mitigate | bucket_a_coverage_test.exs asserts each cited guard literal physically exists; a renamed/deleted guard fails closed (defeats silent ledger drift — RESEARCH Pitfall 4). |
| T-116-11 | Tampering | check-conformance.sh table count | mitigate | A11 gate bakes a count floor and filters comment lines so header prose cannot self-invalidate the inventory. |
</threat_model>

<verification>
- `cd mailglass_admin && npm run test:operator-browser -- --grep "A3|A4|A16|A21|A22|hover|skeleton|floating"` green
- `cd mailglass_admin && bash scripts/check-conformance.sh` exits 0 (incl. the new table-overuse gate)
- `cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/bucket_a_coverage_test.exs` green
- Manifest fails closed when a cited guard literal is removed (negative check)
</verification>

<success_criteria>
- All 24 Bucket-A defects (A1..A24) have a live regression guard (RATCHET-05)
- 6 net-new guards authored (A3, A4/A23, A16-system, A21, A22, A11); ~18 cite existing green guards
- The executable manifest fails closed on a stale citation (D-12); the ledger never deletes a row
</success_criteria>

<output>
Create `.planning/phases/116-fixtures-idempotent-ratchet-arm/116-05-SUMMARY.md` when done.
Record: the final A-NN -> guard map, the table-count floor value, and any defect whose guard
status is downgraded (so plan 116-06's phase gate accounts for it).
</output>
