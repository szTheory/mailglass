---
phase: 116-fixtures-idempotent-ratchet-arm
plan: 06
type: execute
wave: 3
depends_on: ["116-01", "116-02", "116-03", "116-04", "116-05"]
files_modified:
  - reference/demo_app/assets/e2e/cohort.spec.js
  - mailglass_admin/docs/ui-baseline-scores.json
  - mailglass_admin/docs/axe-baseline.json
autonomous: true
requirements: [RATCHET-04]
user_setup: []

must_haves:
  truths:
    - "The full matrix runs INCLUDING >=1 Playwright run against rich reference/demo_app data (the persona cohort) — closing the lab-passes-but-ugly gap"
    - "The demo cohort spec extends the existing reference/demo_app/assets/e2e harness (config + /demo/evidence/reset token already shipped) — no new harness, OperatorBrowserServer is NOT bent"
    - "After all fixtures/gallery/pillars/defect-fixes land, the 54-cell ui-baseline-scores.json is re-scored: current is promoted to prior, a fresh current is generated, all 54 cells meet-or-beat with zero regressions and a distinct run_id"
    - "The 9-cell axe baseline is likewise promoted (current -> prior) with a fresh distinct run_id and meet-or-beat green"
    - "All THREE baseline comparators are green simultaneously: 54-cell ratchet, 9-cell axe, Bucket-A manifest"
  artifacts:
    - path: "reference/demo_app/assets/e2e/cohort.spec.js"
      provides: "Playwright spec asserting the persona cohort renders correctly on demo surfaces (tenant picker visible, edge values render) after /demo/evidence/reset"
      contains: "reset"
    - path: "mailglass_admin/docs/ui-baseline-scores.json"
      provides: "Re-scored 54-cell baseline: current promoted to prior, fresh current, zero regression"
      contains: "run_id"
    - path: "mailglass_admin/docs/axe-baseline.json"
      provides: "Promoted 9-cell axe baseline: current -> prior, fresh run_id, meet-or-beat"
      contains: "run_id"
  key_links:
    - from: "reference/demo_app/assets/e2e/cohort.spec.js"
      to: "reference/demo_app (demo seed)"
      via: "beforeEach POST /demo/evidence/reset with x-mailglass-demo-reset-token re-runs DemoData.reset! -> Personas.seed!"
      pattern: "demo/evidence/reset"
    - from: "mailglass_admin/docs/ui-baseline-scores.json"
      to: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      via: "promoted prior/current with distinct run_ids; compare_baselines/2 meet-or-beat green"
      pattern: "run_id"
---

<objective>
Run the full matrix INCLUDING >=1 run against rich `reference/demo_app` data, then promote
`current -> prior` and re-score with all gates green (RATCHET-04): the keystone close of v1.13. This
is the milestone's ONLY full pillar re-score — it must run LAST, after the cohort, gallery,
interaction pillar, axe baseline, and 24 Bucket-A guards all land, so the green-gates re-score
reflects the completed work.

Purpose: Prove the admin design system survives realistic multi-tenant data (closing the
"lab-passes-but-ugly" gap that survived v1.11), then ratchet the baseline forward idempotently —
meet-or-beat across all 54 aesthetic cells + all 9 axe cells, zero regressions, with the anti-vacuity
run_id promotion that makes a forgotten re-score fail loudly.

Output: a demo cohort Playwright spec, a promoted+re-scored 54-cell `ui-baseline-scores.json`, a
promoted 9-cell `axe-baseline.json`, all three comparators green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-CONTEXT.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-RESEARCH.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-UI-SPEC.md

# Templates + upstream summaries (read before implementing):
@mailglass_admin/docs/ui-baseline-scores.json
@mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
@reference/demo_app/assets/e2e/demo.spec.js
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-01-SUMMARY.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-02-SUMMARY.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-03-SUMMARY.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-04-SUMMARY.md
@.planning/phases/116-fixtures-idempotent-ratchet-arm/116-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo cohort Playwright run against rich demo_app data</name>
  <files>reference/demo_app/assets/e2e/cohort.spec.js</files>
  <read_first>
    - reference/demo_app/assets/e2e/demo.spec.js (the existing harness: beforeEach POSTs /demo/evidence/reset with header x-mailglass-demo-reset-token: $DEMO_EVIDENCE_RESET_TOKEN — the seed-reset seam) + reference/demo_app/assets/playwright.config.cjs (testDir ./e2e, @playwright/test ^1.59.1, webServer runs mix ecto.setup && mix phx.server; the destructive reset means keep serial — do NOT set fullyParallel:true)
    - 116-CONTEXT.md D-10 (extend the existing demo suite; no new harness; do NOT bend OperatorBrowserServer) + 116-RESEARCH.md "Demo Harness Extension" + Open Question 1 (confirm DemoData.reset! incl Personas.seed! runs both at boot AND on the reset-token POST) + Assumption A5
    - 116-01-SUMMARY.md (whether DemoData.reset! seeds the cohort at boot + on reset)
    - 116-UI-SPEC.md "Tenant Picker Visibility Contract" + "RATCHET-01 persona render behaviors"
  </read_first>
  <action>
    Create reference/demo_app/assets/e2e/cohort.spec.js extending the existing demo harness (D-10 — NO new
    infra): reuse the beforeEach /demo/evidence/reset token flow, then assert against the rich cohort data:
    the tenant picker is visible (>=2 selectable tenants: northstar + fjordline-aps) and helios-void is NOT
    a selectable option; the fjordline-aps single delivery renders with the long-ID truncated (no horizontal
    overflow), non-ASCII names "Bjørn Hansen"/"山田太郎" rendered verbatim, the nil reject_reason showing NO
    reject-reason badge; the northstar high-count lifecycle renders without overflow/collapse; a
    helios-void-scoped direct URL renders the empty state, not a crash. This is the RATCHET-04 "≥1 run against
    rich demo_app data" coverage requirement — structural assertions, not an axe baseline (axe stays
    admin-only per RESEARCH). Keep the suite serial (the destructive reset races a shared DB across parallel
    workers). First confirm Personas.seed! runs on the reset-token POST (Open Question 1 / A5); if not, wire
    reset! to seed the cohort. Revert any reference/demo_app mix.lock swoosh drift before committing (MEMORY).
  </action>
  <verify>
    <automated>cd reference/demo_app/assets && DEMO_EVIDENCE_RESET_TOKEN="${DEMO_EVIDENCE_RESET_TOKEN:-test-token}" npm run test:e2e -- --grep "cohort" 2>&1 | tail -25; git -C /Users/jon/projects/mailglass/reference/demo_app checkout mix.lock 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - cohort.spec.js passes: tenant picker visible (northstar+fjordline-aps), helios-void absent
    - fjordline-aps edge values render correctly (long-ID truncated, non-ASCII verbatim, nil reject_reason no badge); northstar high-count renders without collapse; helios-void direct URL empty state not a crash
    - Suite stays serial; no reference/demo_app mix.lock drift committed
  </acceptance_criteria>
  <done>A Playwright run against rich demo_app cohort data passes, closing the lab-passes-but-ugly gap.</done>
</task>

<task type="auto">
  <name>Task 2: Promote + re-score the 54-cell aesthetic baseline (zero regression)</name>
  <files>mailglass_admin/docs/ui-baseline-scores.json</files>
  <read_first>
    - mailglass_admin/docs/ui-baseline-scores.json (current shape: schema_version 3, prior + current blocks each 3 surfaces x 6 pillars x 3 themes; current run_id "2026-06-16-phase-103")
    - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs (the comparator that enforces: schema 3, all 54 cells in both blocks, scores 1..4, prior.run_id != current.run_id anti-vacuity, compare_baselines/2 no-regression)
    - mailglass_admin/docs/design-system.md (the 1-4 pillar rubric + D-07 scoring procedure) + 116-CONTEXT.md D-02 (matrix STAYS 54 cells, schema 3 untouched; this is the milestone's ONLY full re-score)
  </read_first>
  <action>
    Perform the milestone's ONLY full pillar re-score (D-02, RATCHET-04). Promote the existing current block
    to prior, then regenerate a fresh current by re-scoring all 54 cells (3 surfaces x 6 pillars x 3 themes)
    via the established D-07 LLM scoring procedure against the now-completed work (cohort + gallery matrix +
    interaction pillar + Bucket-A fixes all landed). Assign the new current a DISTINCT run_id (e.g.
    "2026-06-20-phase-116") so the anti-vacuity guard is satisfied. Every cell must meet-or-beat its prior
    (zero regression) — if any cell would regress, fix the underlying surface before committing (do NOT lower
    a score to pass). Keep schema_version at 3; do NOT add a pillar or bump the schema. The interaction
    pillar (plan 116-03) and axe baseline (plan 116-02) are SEPARATE primitives — they are NOT folded into
    this 54-cell matrix.
  </action>
  <verify>
    <automated>cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/ratchet_baseline_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ui-baseline-scores.json: current promoted to prior; fresh current with a distinct run_id; schema_version stays 3
    - All 54 cells present in both blocks, scores 1..4, zero regression (meet-or-beat)
    - ratchet_baseline_test.exs green
  </acceptance_criteria>
  <done>The 54-cell aesthetic baseline is idempotently ratcheted forward with zero regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Promote the 9-cell axe baseline + confirm all three comparators green</name>
  <files>mailglass_admin/docs/axe-baseline.json</files>
  <read_first>
    - mailglass_admin/docs/axe-baseline.json (the 9-cell schema-1 baseline established in plan 116-02) + mailglass_admin/test/mailglass_admin/axe_baseline_test.exs (the comparator) + mailglass_admin/e2e/axe-baseline.spec.js (the producer)
    - 116-02-SUMMARY.md (the measured per-cell violation counts) + 116-CONTEXT.md D-03/D-04 (meet-or-beat + per-rule diff; anti-vacuity run_id)
  </read_first>
  <action>
    Promote the 9-cell axe baseline (RATCHET-04): re-run the producer (e2e/axe-baseline.spec.js) against the
    completed work to regenerate current.violations, copy the prior current block to prior, and assign the new
    current a DISTINCT run_id (e.g. "axe-2026-06-20-phase-116"). All 9 cells must meet-or-beat (current.total
    <= prior.total per cell AND no rule-id count rises / no new rule-id) — if a cell would regress, fix the
    underlying a11y issue before committing (do NOT relax the baseline). Then run all THREE comparators
    together and confirm simultaneous green: the 54-cell ratchet (Task 2), the 9-cell axe baseline, and the
    Bucket-A manifest (plan 116-05). This is the phase gate per 116-VALIDATION.md — run on main, not a
    worktree (deps/_build gitignored). Scope the mix test run to avoid the ~57 unrelated Oban worktree
    failures (MEMORY).
  </action>
  <verify>
    <automated>cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/axe_baseline_test.exs test/mailglass_admin/ratchet_baseline_test.exs test/mailglass_admin/bucket_a_coverage_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - axe-baseline.json: current promoted to prior; fresh current with a distinct run_id; meet-or-beat per cell + per rule-id
    - All THREE comparators green simultaneously: ratchet_baseline_test, axe_baseline_test, bucket_a_coverage_test
  </acceptance_criteria>
  <done>The axe baseline is promoted; all three fail-closed comparators are green — the keystone ratchet-arm is armed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| re-score promotion → CI ratchet | A forgotten current->prior promotion must fail the anti-vacuity guard, not pass a vacuous self-compare |
| demo reset token → demo seed | The /demo/evidence/reset token gates re-seeding; the cohort must be present after reset for the RATCHET-04 run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; the axe devDep was installed in plan 116-02 with its legitimacy checkpoint. |
| T-116-12 | Repudiation | forgotten re-score promotion | mitigate | Both comparators assert prior.run_id != current.run_id — a forgotten promotion fails loudly (anti-vacuity), never a silent self-compare. |
| T-116-13 | Tampering | lowering a score/baseline to pass | mitigate | Meet-or-beat is only-forward; the plan mandates fixing the underlying surface, never lowering a prior score or relaxing the axe floor, to keep the ratchet honest. |
</threat_model>

<verification>
- `cd reference/demo_app/assets && npm run test:e2e -- --grep "cohort"` green (rich demo data run)
- `cd mailglass_admin && MIX_ENV=test mix test test/mailglass_admin/ratchet_baseline_test.exs test/mailglass_admin/axe_baseline_test.exs test/mailglass_admin/bucket_a_coverage_test.exs` all green
- `cd mailglass_admin && npm run test:operator-browser` full matrix green (run on main, not a worktree)
- ui-baseline-scores.json + axe-baseline.json both promoted with distinct run_ids; zero regression
</verification>

<success_criteria>
- >=1 Playwright run against rich demo_app cohort data passes (RATCHET-04)
- 54-cell aesthetic baseline + 9-cell axe baseline both promoted (current->prior) and meet-or-beat with distinct run_ids
- All three fail-closed comparators green simultaneously; the keystone ratchet-arm is armed
</success_criteria>

<output>
Create `.planning/phases/116-fixtures-idempotent-ratchet-arm/116-06-SUMMARY.md` when done.
Record: the new run_ids for both baselines, any cell that needed an underlying fix to meet-or-beat,
and confirmation all three comparators ran green on main (input to Phase 117 release cut).
</output>
