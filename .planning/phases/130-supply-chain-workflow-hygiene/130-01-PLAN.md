---
phase: 130-supply-chain-workflow-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - lib/mix/tasks/mailglass.publish.check.ex
  - test/support/ci_lanes.ex
  - test/scripts/ci_parity_drift_test.exs
  - test/mailglass/publish/audit_allowlist_test.exs
autonomous: true
requirements:
  - SUPPLY-01
  - SUPPLY-03
must_haves:
  truths:
    - "mix deps.audit runs advisory (non-blocking) on PR in a lane named 'Deps Audit Advisory (Elixir 1.18 / OTP 27)' — the Advisory-naming convention ensures gate-ci-green's isAdvisory() pattern treats it as non-blocking at publish time too (D-LD-10, LD-4, SUPPLY-01)."
    - "The publish gate (publish.check.ex) has a dedicated Step 14 'run deps.audit' that calls mix deps.audit in the tarball audit root using the @accepted_advisories allowlist — a non-allowlisted advisory hard-blocks delivery, per D-LD-4 (SUPPLY-01)."
    - "Every accepted-advisory entry in @accepted_advisories has an OSV-staleness check on every publish.check run: stale (withdrawn upstream) → hard block; OSV outage → fail-open with a loud stderr warning; active → logged notice (SUPPLY-03)."
    - "The new 'Deps Audit Advisory' lane is registered in Mailglass.CILanes @advisory_lanes_ci, covered by a matcher in ci_parity_drift_test.exs, and in the mix ci alias — the MIXCI-03 parity-drift test and GATE-03 meta-tests stay green (D-LD-10, SUPPLY-01)."
    - "ExUnit tests in audit_allowlist_test.exs prove: (a) a non-allowlisted deps.audit finding blocks publish, (b) allowlisted advisories are accepted, (c) OSV returns withdrawn → block, (d) OSV network error → fail-open."
  artifacts:
    - "mix.exs: {:mix_audit, ~> 2.1, only: [:dev, :test], runtime: false} dep + deps.audit step in ci alias"
    - ".github/workflows/ci.yml: deps_audit_advisory job (continue-on-error: true, name matching Advisory() pattern)"
    - "lib/mix/tasks/mailglass.publish.check.ex: verify_deps_audit/1, check_osv_advisory_staleness/0, osv_get/1 functions; Step 14 and OSV step wired into execute_package/2"
    - "test/support/ci_lanes.ex: 'Deps Audit Advisory (Elixir 1.18 / OTP 27)' in @advisory_lanes_ci"
    - "test/scripts/ci_parity_drift_test.exs: matcher_for entry + matcher_lanes MapSet entry for new lane"
    - "test/mailglass/publish/audit_allowlist_test.exs: new describe blocks for deps.audit and OSV staleness"
  key_links:
    - "Advisory lane name MUST contain 'Advisory (' to match isAdvisory() regex in publish-hex.yml gate-ci-green — a mis-named lane falls into the non-required/non-advisory bucket and blocks publish (Pitfall 1)."
    - "CILanes registration is a three-file atomic: ci_lanes.ex + ci_parity_drift_test.exs matcher + ci_parity_drift_test.exs matcher_lanes MapSet — missing any one fails the MIXCI-03 anti-vacuity guard (Pitfall 2)."
    - "mix deps.audit output format may differ from mix hex.audit — write unaccepted_deps_audit_findings/1 as a peer function (not reusing the hex.audit parser) after verifying the actual output format by running locally (Pitfall 5)."
    - "OSV API fail-open must cover ALL error paths: network error, timeout, non-200, parse error — the try/rescue in osv_get/1 is the safety net (Pitfall 6)."
---

<objective>
Add the `mix deps.audit` advisory PR lane and publish-gate block (SUPPLY-01), plus the OSV-staleness forcing function for the cowlib accepted-advisory allowlist (SUPPLY-03). This is the Elixir-code-heavy plan — all new ExUnit test seams live here.

Purpose: Close the v1.14-advisory-wave gap. Under an unfixable transitive CVE wave, `mix deps.audit` on PR must NEVER red an open PR, while a real unfixable advisory must be detectable and hard-block at publish. The OSV-staleness check ensures an accepted allowlist entry doesn't silently outlive its upstream advisory.

Output: New `:mix_audit` dep in mix.exs, a new `deps_audit_advisory` advisory CI lane in ci.yml, two new publish.check.ex steps (Step 14 deps.audit + OSV staleness), CILanes registration, and ExUnit tests proving the PR-vs-publish asymmetry and the fail-open OSV guarantee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-supply-chain-workflow-hygiene/130-CONTEXT.md
@.planning/phases/130-supply-chain-workflow-hygiene/130-RESEARCH.md
@.planning/research/milestone-cicd/SYNTHESIS.md
</context>

<interface_context>
## Key identifiers and invariants — read before touching each file

### publish.check.ex step-wiring contract
- The existing step chain is at lines ~146-170. `step/6` signature: `step(counts, :update | :unchanged, package, "step name", ctx, &verify_fn/1)`.
- Step 13 ("run hex.audit") is at line 159. Insert Step 14 ("run deps.audit") immediately after (new `{counts, ctx} = step(...)` line).
- The OSV-staleness call wires into `execute_package/2` in TWO places: (a) loud warning call before the step chain (every run), and (b) a new step ("verify osv advisory freshness") added after Step 14.
- `verify_audit/1` (lines 1038-1074) is the structural model for `verify_deps_audit/1`. `verify_deps_audit/1` must call `unaccepted_deps_audit_findings/1` (a NEW peer function — see Pitfall 5 note below) rather than reusing `unaccepted_audit_findings/1` (which was calibrated for `mix hex.audit`'s EEF-CVE ID format).
- `fail_step/2` signature: `fail_step("step name", "Delivery blocked: <brand-voice message>")`.
- `@accepted_advisories` keys are the advisory IDs used in OSV lookups: `"EEF-CVE-2026-43966"` and `"EEF-CVE-2026-43969"`.

### mix deps.audit output format (SUPPLY-01 research open question A4)
Run `mix deps.audit` locally AFTER adding the dep to confirm output format. The mix_audit 2.1.5 tool uses the elixir-security-advisories DB which uses GHSA IDs. Expected line format: `  <pkg> <version> - GHSA-xxxx-yyyy-zzzz  <description>` (different separator and ID scheme from `mix hex.audit`'s `EEF-CVE-*` format). Write `unaccepted_deps_audit_findings/1` with a regex anchored to the ACTUAL format you observe. Add a test case in the new describe block using fixture output that matches the real format.

### OSV API endpoint and fail-open (SUPPLY-03 research open question A3)
Before implementing `osv_get/1`, test the endpoint: `curl -s "https://api.osv.dev/v1/vulns/EEF-CVE-2026-43966"` — if it returns data, the ID resolves natively. If it 404s, try looking up the OSV alias from `mix hex.audit` output (the `aka: CVE-*` line in hex.audit advisory output may carry the canonical OSV ID). If neither resolves, `osv_get/1` will always return `{:error, {:http_status, 404}}` which is the fail-open path — acceptable (logs warning, does not block). Document the resolution in the SUMMARY.

The `check_osv_advisory_staleness/0` function iterates `@accepted_advisories` keys. For each key `id`:
- Call `osv_get("https://api.osv.dev/v1/vulns/#{id}")`.
- On `{:ok, body}`: decode JSON; if `"withdrawn"` key present → `{:stale, id, withdrawn_at}`; else → `{:active, id}`.
- On `{:error, reason}`: `{:error, id, reason}` — this is the fail-open path.
- `try/rescue` wraps the `:httpc` call with timeout `5_000` ms.

### CILanes registration (Pitfall 2 — three-file atomic)
1. `test/support/ci_lanes.ex` `@advisory_lanes_ci` — append `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"`.
2. `test/scripts/ci_parity_drift_test.exs` `matcher_for/1` — add a clause: `"Deps Audit Advisory (Elixir 1.18 / OTP 27)" => &any_step?(&1, "deps.audit")`.
3. `test/scripts/ci_parity_drift_test.exs` `matcher_lanes` MapSet — add `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"` to the hardcoded set (lines ~180-196).

### mix ci alias
Add `"deps.audit"` as a string entry to the `ci:` alias list in `mix.exs`, positioned after `"hex.audit"` (line 368).

### ci.yml advisory lane (SUPPLY-01 research Pitfall 1)
The job MUST be named exactly `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"` (with `Advisory (` substring) so `isAdvisory()` in publish-hex.yml classifies it as non-blocking. Job key: `deps_audit_advisory`. The job must have `continue-on-error: true`. Use `.tool-versions` version-file (Phase 129 pattern — not hardcoded `elixir-version`/`otp-version`). The cache key shape (Phase 129) is `mix-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-${{ env.MIX_ENV || 'dev' }}-${{ hashFiles('**/mix.lock') }}` with matching `restore-keys`. The job needs `needs: [changes]` and `if: needs.changes.outputs.code == 'true'` to match the advisory pattern of other advisory lanes in ci.yml. Steps: checkout → setup-beam → cache → deps.get → deps.audit (no --ignore-unfixed flag; the lane is advisory so a red result is expected under an advisory wave and is non-blocking via continue-on-error).
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Add mix_audit dep to mix.exs and wire deps.audit into mix ci alias</name>
  <files>mix.exs</files>
  <read_first>
    - `mix.exs` lines 355-376 (the `ci:` alias list, specifically the `"hex.audit"` line at ~368).
    - `mix.exs` `defp deps` block (grep `{:ex_doc` or `{:bandit` for placement context near dev/test-only deps).
  </read_first>
  <action>
    Add `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}` to the `defp deps` list in `mix.exs`. Place it near other dev/test-only tools (e.g., after `{:ex_doc, ...}` or grouped with lint tools). The dep is dev/test-only and `runtime: false` so it does not ship to adopters (zero adopter impact).

    In the `ci:` alias list (lines ~357-376), add `"deps.audit"` as a string immediately after `"hex.audit"` (currently at line 368). The ordering places the supply-chain scan next to the existing hex security scan for cohesion.

    No other alias changes — `ci.fast`, `ci.setup`, or `ci.browser` do not need `deps.audit`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q 'mix_audit' mix.exs && grep -qE '"deps\.audit"' mix.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}` appears in the `deps` list.
    - `"deps.audit"` appears in the `ci:` alias list immediately after `"hex.audit"`.
    - `mix deps.get` completes without error (the dep resolves on Hex).
  </acceptance_criteria>
  <done>mix_audit is declared as a dev/test-only dep and wired into the mix ci alias for local parity.</done>
</task>

<task type="auto">
  <name>Task 2: Add deps_audit_advisory advisory CI lane to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — grep `hex_audit` to find the existing `hex_audit` advisory job block (~528-551). The new job follows the same pattern.
    - `.github/workflows/ci.yml` — grep `actions/checkout@` to find the pinned SHA in use (use the SAME SHA, do not change it).
    - `.github/workflows/ci.yml` — grep `erlef/setup-beam@` to find the pinned SHA in use (use the SAME SHA).
    - `.github/workflows/ci.yml` — grep `actions/cache@` to find the pinned SHA (use the SAME SHA).
    - `.github/workflows/ci.yml` — grep `changes.outputs.code` to confirm the `changes` job output key name (used in `needs` + `if` condition).
  </read_first>
  <action>
    Add a new job `deps_audit_advisory` to `.github/workflows/ci.yml` immediately after the existing `hex_audit` job block. The job must have:
    - `name: Deps Audit Advisory (Elixir 1.18 / OTP 27)` — the Advisory-naming convention (contains `Advisory (`) is REQUIRED so publish-hex.yml `isAdvisory()` classifies it as non-blocking (Pitfall 1).
    - `runs-on: ubuntu-latest`
    - `needs: [changes]`
    - `if: needs.changes.outputs.code == 'true'` (same guard as adjacent advisory lanes)
    - `continue-on-error: true` (advisory: never reds a PR or publish gate)
    - Steps using the SAME action SHA pins already present in ci.yml for `actions/checkout@`, `erlef/setup-beam@`, and `actions/cache@` — do NOT introduce new or different SHAs.
    - `erlef/setup-beam@` with: `version-file: .tool-versions`, `version-type: strict` (Phase 129 pattern — no hardcoded elixir-version/otp-version).
    - `actions/cache@` with path `deps`, key `mix-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-${{ env.MIX_ENV || 'dev' }}-${{ hashFiles('**/mix.lock') }}`, restore-keys `mix-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-${{ env.MIX_ENV || 'dev' }}-` (Phase 129 toolchain-hashed key shape).
    - Step `run: mix deps.get`
    - Step `run: mix deps.audit`

    The job must NOT be added to `ci_green.needs` (it is advisory, not required).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q 'deps_audit_advisory' .github/workflows/ci.yml && grep -q 'Deps Audit Advisory (Elixir 1.18 / OTP 27)' .github/workflows/ci.yml && grep -q 'continue-on-error: true' .github/workflows/ci.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Job `deps_audit_advisory` with `name: Deps Audit Advisory (Elixir 1.18 / OTP 27)` is present.
    - `continue-on-error: true` is set on the job.
    - `version-file: .tool-versions` is used in setup-beam (no hardcoded elixir-version/otp-version).
    - Cache key uses the Phase 129 toolchain-hashed shape.
    - The job is NOT in `ci_green.needs`.
    - All Action SHAs match the existing pins in ci.yml (no new SHA introduced).
  </acceptance_criteria>
  <done>The advisory deps.audit PR lane is in ci.yml, non-blocking, and correctly named for the isAdvisory() pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add verify_deps_audit and OSV-staleness to publish.check.ex + register CILanes + add tests</name>
  <files>lib/mix/tasks/mailglass.publish.check.ex, test/support/ci_lanes.ex, test/scripts/ci_parity_drift_test.exs, test/mailglass/publish/audit_allowlist_test.exs</files>
  <read_first>
    - `lib/mix/tasks/mailglass.publish.check.ex` lines 58-65 (`@accepted_advisories`) and 146-170 (step chain in execute_package/2).
    - `lib/mix/tasks/mailglass.publish.check.ex` lines 1038-1106 (`verify_audit/1` and `unaccepted_audit_findings/1`) — structural model to follow.
    - `test/support/ci_lanes.ex` lines 59-70 (`@advisory_lanes_ci` list).
    - `test/scripts/ci_parity_drift_test.exs` lines 88-130 (`matcher_for/1` function) and lines 179-196 (`matcher_lanes` MapSet).
    - `test/mailglass/publish/audit_allowlist_test.exs` (full file — existing test structure).
    - Run `mix deps.audit` locally after Task 1 completes to capture the actual output format for the `unaccepted_deps_audit_findings/1` regex.
    - Run `curl -s "https://api.osv.dev/v1/vulns/EEF-CVE-2026-43966"` to verify the OSV endpoint resolves the EEF-CVE ID (research open question A3).
  </read_first>
  <behavior>
    - `verify_deps_audit/1`: non-allowlisted deps.audit finding → `fail_step("run deps.audit", "Delivery blocked: ...")`; allowlisted advisories → ctx map updated with audit output; clean (exit 0) → ctx map updated.
    - `check_osv_advisory_staleness/0`: withdrawn advisory → `{:stale, id, date}`; active advisory → `{:active, id}`; network/parse error → `{:error, id, reason}` (fail-open path).
    - OSV warning (every run): emits to stderr even for active advisories ("allowlist entry EEF-CVE-... confirmed active in OSV").
    - OSV block (publish step only): `{:stale, _, _}` → `fail_step("verify osv advisory freshness", "Delivery blocked: ...")`.
    - OSV fail-open: `{:error, _, _}` → `IO.puts(:stderr, "[publish.check] WARNING: OSV API unavailable for #{id} — skipping staleness check (fail-open)")` and continue.
    - ExUnit: `unaccepted_deps_audit_findings/1` test: non-allowlisted GHSA finding in deps.audit format → returns non-empty list; allowlisted EEF-CVE finding → returns empty list.
    - ExUnit OSV staleness: `{:stale, id, date}` from mock → stale result returned; `{:error, id, :network}` from mock → error result returned (no block signaled).
  </behavior>
  <action>
    STEP A — Run `mix deps.audit` locally (after Task 1 adds the dep via `mix deps.get`) to capture the actual output format. Note the advisory line format. Write `unaccepted_deps_audit_findings/1` with a regex anchored to the ACTUAL output format observed. If it uses GHSA IDs, the regex should capture `{pkg, ghsa_id}` tuples and filter against the allowlist (which uses EEF-CVE IDs). If the IDs differ, the function will never suppress a deps.audit advisory unless we cross-reference — document this in code comments and in the SUMMARY. Add `@doc false` to make it unit-testable.

    STEP B — Add two new private functions to `publish.check.ex`:

    1. `check_osv_advisory_staleness/0` — iterates `Map.keys(@accepted_advisories)`, calls `osv_get/1` for each, returns a list of `{:active | :stale | :error, id, ...}` tuples.

    2. `osv_get/1` — wraps an `:httpc.request/4` call with `try/rescue`. URL arg is a string; convert to charlist for `:httpc`. Timeout: 5_000 ms. Returns `{:ok, body_string}` on HTTP 200, `{:error, reason}` on all other paths. The `rescue` clause catches any exception and returns `{:error, :exception}`.

    STEP C — Wire into `execute_package/2`:
    - Before the step chain (loud-warn-always): call `check_osv_advisory_staleness/0`, emit `IO.puts(:stderr, ...)` for each result (active = notice, stale = warning, error = warning with fail-open note). This call NEVER blocks.
    - After the Step 13 `hex.audit` step: add Step 14 `{counts, ctx} = step(counts, :update, package, "run deps.audit", ctx, &verify_deps_audit/1)`.
    - After Step 14: add Step 15 `{counts, ctx} = step(counts, :update, package, "verify osv advisory freshness", ctx, &verify_osv_freshness/1)`. The `verify_osv_freshness/1` function calls `check_osv_advisory_staleness/0`, fails on `{:stale, _, _}`, warns and continues on `{:error, _, _}`, and returns updated ctx on `{:active, _}`.

    STEP D — CILanes registration (three-file atomic, Pitfall 2):
    - `test/support/ci_lanes.ex`: append `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"` to the `@advisory_lanes_ci` list.
    - `test/scripts/ci_parity_drift_test.exs` `matcher_for/1`: add `"Deps Audit Advisory (Elixir 1.18 / OTP 27)" => &any_step?(&1, "deps.audit")` clause.
    - `test/scripts/ci_parity_drift_test.exs` `matcher_lanes` MapSet: add `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"` to the hardcoded set at lines ~180-196.

    STEP E — ExUnit tests in `test/mailglass/publish/audit_allowlist_test.exs`. Add two new `describe` blocks:

    Block 1: `"unaccepted_deps_audit_findings/1"` — test cases using fixture strings in the ACTUAL mix deps.audit output format (captured in Step A). Include: (a) a non-allowlisted advisory finding returns a non-empty list; (b) a clean scan returns [].

    Block 2: `"check_osv_advisory_staleness/0 (fail-open behavior)"` — use an application-level trick or a wrapper to test without live HTTP: extract the JSON-decode + stale-check logic into a testable function `classify_osv_response/2` that takes `(advisory_id, response_body)` and returns `{:active | :stale, id, ...}`. Test `classify_osv_response/2` directly: (a) body with `"withdrawn"` key → `{:stale, _, _}`; (b) body without `"withdrawn"` key → `{:active, _}`; (c) malformed JSON → `{:error, _, :parse_error}`. This avoids live HTTP in tests while proving the classification logic.

    Brand-voice error messages in fail_step calls: "Delivery blocked: mix deps.audit reported non-accepted issues for [pkg] ([advisory IDs]). ..." and "Delivery blocked: accepted advisory [id] has been withdrawn upstream (fixed as of [date]). Remove it from @accepted_advisories and bump the dep."
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix test test/mailglass/publish/audit_allowlist_test.exs test/scripts/ci_parity_drift_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mailglass/publish/audit_allowlist_test.exs` is green with new test cases for `unaccepted_deps_audit_findings/1` and OSV staleness classification.
    - `mix test test/scripts/ci_parity_drift_test.exs` is green (MIXCI-03 parity drift test passes with the new lane registered).
    - `grep -c "Deps Audit Advisory" test/support/ci_lanes.ex` returns 1.
    - `grep -c "deps.audit" lib/mix/tasks/mailglass.publish.check.ex` is non-zero (Step 14 and verify function exist).
    - `grep -c "osv\|OSV" lib/mix/tasks/mailglass.publish.check.ex` is non-zero (OSV functions exist).
    - Brand-voice fail messages contain "Delivery blocked:" prefix (not "Oops" or generic strings).
    - OPEN QUESTION A3 resolved and documented in SUMMARY: whether `EEF-CVE-2026-43966` resolves on the OSV API; if 404, fail-open path verified by curl or test.
    - OPEN QUESTION A4 resolved and documented in SUMMARY: actual `mix deps.audit` output format; `unaccepted_deps_audit_findings/1` regex anchored to the real format.
  </acceptance_criteria>
  <done>publish.check.ex has Step 14 (deps.audit block) and the OSV-staleness forcing function wired; CILanes registration is atomic across all three files; ExUnit tests prove the advisory-block and fail-open behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OSV.dev API → publish.check.ex | External HTTP call crosses here. A malicious or unavailable response must not block a security patch (fail-open is the safety contract). |
| mix deps.audit → publish.check.ex allowlist | Advisory findings cross here at publish time; non-allowlisted items must hard-block delivery. |
| ci.yml advisory lane → publish gate | A mis-named advisory lane could accidentally be treated as blocking by gate-ci-green (isAdvisory() naming contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-01 | Tampering | advisory lane naming (`isAdvisory()` pattern) | high | mitigate | Lane name MUST contain `Advisory (` substring (per D-LD-4 + Pitfall 1); verify with grep gate in acceptance_criteria. |
| T-130-02 | Denial of Service | OSV API outage blocking a security patch release | high | mitigate | Fail-open in `osv_get/1` via `try/rescue` — network error, timeout, non-200, parse error all return `{:error, _, _}` and are logged but never block (LD-4). |
| T-130-03 | Elevation of Privilege | vacuous CILanes coverage — advisory lane added to ci.yml but not registered in CILanes | medium | mitigate | Three-file atomic registration (ci_lanes.ex + matcher_for + matcher_lanes MapSet); MIXCI-03 anti-vacuity guard catches missing matchers. |
| T-130-04 | Information Disclosure | PII in OSV/audit telemetry or log output | low | accept | No PII involved — only dep names, versions, and advisory IDs are logged. CLAUDE.md telemetry constraint is satisfied. |
| T-130-SC | Tampering | npm/pip/cargo installs | low | accept | No npm/pip/cargo installs — only one Hex dep (mix_audit 2.1.5, legitimacy-audited in RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
Phase-level (run after all tasks complete):

1. `mix test test/mailglass/publish/audit_allowlist_test.exs test/scripts/ci_parity_drift_test.exs --warnings-as-errors` — both test modules green.

2. `grep -q 'Deps Audit Advisory (Elixir 1.18 / OTP 27)' .github/workflows/ci.yml && grep -q 'continue-on-error: true' .github/workflows/ci.yml && echo OK` — advisory lane present and non-blocking.

3. `grep -q 'mix_audit' mix.exs && grep -qE '"deps\.audit"' mix.exs && echo OK` — dep and alias wired.

4. `grep -c 'osv_get\|check_osv_advisory_staleness\|verify_osv_freshness' lib/mix/tasks/mailglass.publish.check.ex` — returns 3+ (all OSV functions present).

5. `mix test test/scripts/required_checks_test.exs` — GATE-03 required-checks test still green (new advisory lane is NOT in the required set).

6. Push `phase/130` branch and confirm CI: `deps_audit_advisory` job appears as non-blocking advisory; it does NOT appear in `ci_green.needs` check.
</verification>

<success_criteria>
- A simulated non-allowlisted advisory in `unaccepted_deps_audit_findings/1` test returns a non-empty list (publish would block) — proves the PR-vs-publish asymmetry is testable without a real CVE.
- The `deps_audit_advisory` CI lane name matches `/ Advisory \(/.test(jobName)` in publish-hex.yml gate-ci-green.
- `mix test test/scripts/ci_parity_drift_test.exs` green — the new lane is registered and has a matcher.
- OSV fail-open test: `{:error, id, :network}` response → staleness check returns error tuple, no block signaled.
- All brand-voice error messages use "Delivery blocked: [Noun] [past-tense verb]: [specific cause]" form.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (Plan 01)

- **`mix.exs`**: `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}` dep; `"deps.audit"` in `ci:` alias.
- **`.github/workflows/ci.yml`**: `deps_audit_advisory` advisory job (non-blocking, `Advisory (` naming convention, version-file, Phase 129 cache key shape).
- **`lib/mix/tasks/mailglass.publish.check.ex`**: `verify_deps_audit/1`, `unaccepted_deps_audit_findings/1`, `check_osv_advisory_staleness/0`, `osv_get/1`, `classify_osv_response/2`, `verify_osv_freshness/1`; Step 14 + Step 15 in `execute_package/2`; loud-warn-always OSV call before step chain.
- **`test/support/ci_lanes.ex`**: `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"` in `@advisory_lanes_ci`.
- **`test/scripts/ci_parity_drift_test.exs`**: `matcher_for/1` clause + `matcher_lanes` MapSet entry for the new lane.
- **`test/mailglass/publish/audit_allowlist_test.exs`**: two new `describe` blocks for `unaccepted_deps_audit_findings/1` and `classify_osv_response/2`.
</artifacts_produced>

<output>
Create `.planning/phases/130-supply-chain-workflow-hygiene/130-01-SUMMARY.md` when done.
</output>
