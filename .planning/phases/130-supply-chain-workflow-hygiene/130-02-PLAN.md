---
phase: 130-supply-chain-workflow-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - .github/workflows/actionlint.yml
  - .github/workflows/advisory-matrix.yml
  - mix.exs
autonomous: true
requirements:
  - SUPPLY-02
  - SUPPLY-04
  - SUPPLY-05
must_haves:
  truths:
    - "dependabot.yml watches the mailglass_admin and mailglass_inbound sibling mix.lock files on a weekly schedule; the frozen reference/ baselines are NOT watched (SUPPLY-02, per D-LD reference-baseline-coupling)."
    - "actionlint.yml contains a dependency-review-action step (SHA-pinned v5.0.0) that runs advisory (continue-on-error: true) on pull_request events only, and all third-party action SHAs in the file are commit SHAs with version comments (CLAUDE.md non-negotiable, SUPPLY-04)."
    - "advisory-matrix.yml has a new core_latest_elixir_advisory job for Elixir 1.19 / OTP 28 that runs push+cron only (if: github.event_name != 'pull_request'), never runs on PR, is never required, and carries the LD-13 floor-coincidence invariant as a workflow comment (SUPPLY-05)."
    - "The 1.19/OTP28 advisory job is named 'Core Full Suite Advisory (Elixir 1.19 / OTP 28)' so isAdvisory() in publish-hex.yml classifies it as non-blocking (contains 'Advisory ('), and its cache key includes the matrix toolchain dimension to prevent cross-toolchain artifact bleed (Pitfall 4)."
  artifacts:
    - ".github/dependabot.yml: two new mix ecosystem entries for /mailglass_admin and /mailglass_inbound (weekly schedule, no reference/ entries)"
    - ".github/workflows/actionlint.yml: dependency-review step (actions/dependency-review-action@a1d282b... # v5.0.0, continue-on-error: true, fail-on-severity: high)"
    - ".github/workflows/advisory-matrix.yml: core_latest_elixir_advisory job (if: github.event_name != 'pull_request', matrix elixir 1.19 / otp 28, LD-13 comment)"
  key_links:
    - "reference/host_app and reference/demo_app must NOT appear in dependabot.yml — they are frozen deterministic baselines; adding them would trigger the coordinated 5-file change trap (reference-baseline-coupling constraint)."
    - "The 1.19/OTP28 job MUST have if: github.event_name != 'pull_request' at the JOB level — GitHub Actions does not support per-matrix-row if conditions; without this guard the row runs on every PR (SUPPLY-05 invariant)."
    - "The dependency-review-action step must have continue-on-error: true (LD-4: advisory-on-PR philosophy) and be restricted to pull_request events only (the action fails without a PR diff context on push/dispatch runs)."
    - "All third-party action SHAs must be commit SHAs (not tags like @v5) per CLAUDE.md; Dependabot watches .github/workflows/ and will send update PRs going forward."
---

<objective>
Add the three pure YAML/config guards: dependabot sibling coverage (SUPPLY-02), the dependency-review-action advisory step in actionlint.yml (SUPPLY-04), and the 1.19/OTP28 latest-Elixir advisory matrix row with the LD-13 floor-coincidence invariant documented (SUPPLY-05). This plan touches only YAML files — no Elixir code, no test changes, no mix.exs — and can run fully in parallel with Plan 01.

Purpose: Complete the supply-chain perimeter. Dependabot keeps the two active sibling packages' deps current; dependency-review catches newly introduced vulnerable deps in PRs; the 1.19/OTP28 advisory row gives early warning when new Elixir/OTP breaks the library, without ever blocking a PR or the publish gate.

Output: Three YAML files updated. Zero new action SHAs beyond the one new dep-review pin. Zero product code change (D-23 infra-only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-supply-chain-workflow-hygiene/130-CONTEXT.md
@.planning/phases/130-supply-chain-workflow-hygiene/130-RESEARCH.md
@.planning/research/milestone-cicd/SYNTHESIS.md
</context>

<interface_context>
## Key identifiers and invariants — read before touching each file

### dependabot.yml current state
Current file (2 entries, 5 lines): one `mix` entry at `directory: "/"` (weekly), one `github-actions` entry at `directory: "/"` (weekly). Both use `interval: "weekly"` with no `open-pull-requests-limit` override (inherits default of 5). No `labels:` or `reviewers:` overrides. New sibling entries MUST match this convention exactly.

Directories to add: `/mailglass_admin` and `/mailglass_inbound` (both `package-ecosystem: "mix"`, `interval: "weekly"`). Do NOT add entries for `reference/host_app` or `reference/demo_app` — those are frozen baselines per the reference-baseline-coupling constraint.

### actionlint.yml current state
Existing workflow has: `on: pull_request: branches: [main], paths: [...]` + `workflow_dispatch:`. Permissions: `contents: read`. Steps: `actions/checkout@<sha>`, `rhysd/actionlint@914e7df21a07ef503a81201c76d2b11c789d3fca  # v1.7.12`, plus a Tests-gate check step.

The new `dependency-review-action` step must:
- Use SHA `a1d282b36b6f3519aa1f3fc636f609c47dddb294` with comment `# v5.0.0` (CLAUDE.md: all third-party actions SHA-pinned).
- Have `continue-on-error: true` (LD-4 advisory-on-PR: must never block a PR or an unfixable-wave PR).
- Have `fail-on-severity: high` (under `with:`) — catches newly introduced HIGH+ vulnerabilities without being excessively noisy for MEDIUM/LOW.
- Be wrapped in `if: github.event_name == 'pull_request'` at the STEP level — the `dependency-review-action` requires a PR diff context and will fail on `workflow_dispatch` runs without it.
- Permissions: `contents: read` is sufficient for scan-only mode (no PR comment posting).

### advisory-matrix.yml current state
Current triggers: `push: branches: [main]`, `pull_request: branches: [main]`, `schedule: cron '21 4 * * *'`, `workflow_dispatch:`. Current matrix: single `elixir: "1.18" / otp: "27"` row across two jobs: `core_full_suite_advisory` and `provider_compatibility_advisory`.

SUPPLY-05 requires ONLY a `core` advisory row for 1.19/OTP28 — NOT a `provider_compatibility_advisory` row (provider tests require live sandbox credentials; OTP28 compatibility is out of scope for Phase 130).

The 1.19/OTP28 row must live in a NEW job definition (separate from the existing `core_full_suite_advisory` job) with `if: github.event_name != 'pull_request'` at the job level. GitHub Actions does not support per-row if conditions in a matrix — a job-level if is the only mechanism.

### 1.19/OTP28 job naming contract
Job name: `Core Full Suite Advisory (Elixir ${{ matrix.elixir }} / OTP ${{ matrix.otp }})` with matrix `include: [{elixir: "1.19", otp: "28"}]`. The rendered name `Core Full Suite Advisory (Elixir 1.19 / OTP 28)` contains `Advisory (` which matches `isAdvisory()` in publish-hex.yml — confirming a red 1.19 run never blocks publish.

### 1.19/OTP28 cache key
Use `mix-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('**/mix.lock') }}` for the new job's cache key. This is toolchain-parameterized (prevents cross-toolchain artifact bleed per Pitfall 4). advisory-matrix.yml is EXEMPT from the Phase 129 `.tool-versions` canonical cache pattern (its purpose is to test SPECIFIC named toolchains); use the matrix dims directly.

### setup-beam for 1.19/OTP28 (research open question A2)
Use bare `elixir: "1.19"` and `otp: "28"` in the `with:` block for setup-beam. The advisory-matrix.yml is EXEMPT from the `version-file` contract — it intentionally names specific toolchains. If setup-beam cannot find `"1.19"` or `"28"` on ubuntu-latest, the advisory row will fail at setup (which is advisory-acceptable — red advisory, not a blocked PR or publish). Document this assumption in the SUMMARY.

### LD-13 floor-coincidence invariant (SYNTHESIS.md LD-13 verbatim)
Add this comment block in the new `core_latest_elixir_advisory` job header in advisory-matrix.yml:

```
# LD-13 floor-coincidence invariant:
# This row tests the LATEST Elixir/OTP line (advisory, non-blocking, push+cron only).
# INVARIANT: whenever the required pin advances past 1.18, either add a 1.18 floor
# row to the required lane OR raise the declared `elixir:` floor in mix.exs —
# never let the tested version outrun the declared `~> 1.18` floor.
# See .planning/research/milestone-cicd/SYNTHESIS.md LD-13.
```

Also add a brief comment in `mix.exs` near the `elixir: "~> 1.18"` project floor declaration referencing LD-13.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependabot sibling entries for mailglass_admin and mailglass_inbound</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - `.github/dependabot.yml` (full file — only 5 lines; confirm exact indentation and structure before adding entries).
  </read_first>
  <action>
    Add two `mix` ecosystem entries to `.github/dependabot.yml` for the sibling packages. Each entry must use `package-ecosystem: "mix"`, the respective directory, and `interval: "weekly"` matching the existing convention. Preserve the existing two entries unchanged.

    New entries:
    - `directory: "/mailglass_admin"`, `package-ecosystem: "mix"`, `schedule.interval: "weekly"`
    - `directory: "/mailglass_inbound"`, `package-ecosystem: "mix"`, `schedule.interval: "weekly"`

    Do NOT add entries for `reference/host_app` or `reference/demo_app`. Do NOT add any `open-pull-requests-limit`, `labels:`, or `reviewers:` overrides (none exist in the current entries; new entries must match the existing minimal style).

    After the edit, the file should have exactly 4 `directory:` lines: `"/"` (mix), `"/"` (github-actions), `"/mailglass_admin"`, `"/mailglass_inbound"`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -c 'directory:' .github/dependabot.yml | grep -qx '4' && grep -q 'mailglass_admin' .github/dependabot.yml && grep -q 'mailglass_inbound' .github/dependabot.yml && grep -v 'reference' .github/dependabot.yml | grep -qc 'mix' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'directory:' .github/dependabot.yml` returns 4.
    - `grep 'mailglass_admin' .github/dependabot.yml` matches (admin entry present).
    - `grep 'mailglass_inbound' .github/dependabot.yml` matches (inbound entry present).
    - No `reference` appears in `dependabot.yml` (frozen baselines excluded).
    - Existing two entries unchanged (no structural drift).
  </acceptance_criteria>
  <done>dependabot.yml watches the two active sibling package locks on a weekly schedule; the frozen reference/ baselines remain unwatched.</done>
</task>

<task type="auto">
  <name>Task 2: Add dependency-review advisory step to actionlint.yml</name>
  <files>.github/workflows/actionlint.yml</files>
  <read_first>
    - `.github/workflows/actionlint.yml` (full file — confirm existing steps, current SHA pins for checkout + actionlint, and permissions block).
  </read_first>
  <action>
    Add a new step to the job in `.github/workflows/actionlint.yml` after the existing `rhysd/actionlint` step (and any Tests-gate check step). The new step:

    Name: `Dependency review`
    Uses: `actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0`
    continue-on-error: true
    if: `github.event_name == 'pull_request'`
    with:
      fail-on-severity: high

    The `if: github.event_name == 'pull_request'` step condition is required because `dependency-review-action` requires a PR diff context — it will fail on `workflow_dispatch` runs without it. `continue-on-error: true` enforces LD-4 (advisory-on-PR: never blocks a PR under an unfixable-wave).

    Do NOT change the existing `rhysd/actionlint` SHA or any other existing step. Do NOT add `pull-requests: write` to the permissions block — `contents: read` is sufficient for scan-only mode. All existing SHA pins must remain unchanged.

    The CLAUDE.md non-negotiable "All third-party GitHub Actions pinned to commit SHA" is satisfied by using the full SHA `a1d282b36b6f3519aa1f3fc636f609c47dddb294` with the `# v5.0.0` comment. Dependabot (watching `.github/workflows/`) will send update PRs when v5.1+ releases.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q 'dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294' .github/workflows/actionlint.yml && grep -q 'continue-on-error: true' .github/workflows/actionlint.yml && grep -q 'github.event_name' .github/workflows/actionlint.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'dependency-review-action@a1d282b' .github/workflows/actionlint.yml` matches (SHA-pinned v5.0.0).
    - `grep 'continue-on-error: true' .github/workflows/actionlint.yml` matches (advisory, non-blocking).
    - `grep "github.event_name == 'pull_request'" .github/workflows/actionlint.yml` matches (step-level guard for PR-only context).
    - `grep 'fail-on-severity: high' .github/workflows/actionlint.yml` matches (severity threshold configured).
    - Existing `rhysd/actionlint@914e7df` SHA is unchanged.
    - No `pull-requests:` permission added to the permissions block.
  </acceptance_criteria>
  <done>actionlint.yml has a SHA-pinned advisory dependency-review step that scans PR dep changes for HIGH+ vulnerabilities without blocking the PR.</done>
</task>

<task type="auto">
  <name>Task 3: Add 1.19/OTP28 advisory matrix row to advisory-matrix.yml + document LD-13 invariant</name>
  <files>.github/workflows/advisory-matrix.yml, mix.exs</files>
  <read_first>
    - `.github/workflows/advisory-matrix.yml` (full file — confirm the existing `core_full_suite_advisory` job structure: services block, env block, steps including checkout SHA, setup-beam SHA, cache pattern, and the `mix test` invocation). The new job is a structural copy of this job with key differences.
    - `mix.exs` — grep `elixir: "~> 1.18"` to find the project floor declaration line for the LD-13 comment.
  </read_first>
  <action>
    Add a new job `core_latest_elixir_advisory` to `.github/workflows/advisory-matrix.yml` after the existing `core_full_suite_advisory` job and before `provider_compatibility_advisory`. The new job must have:

    1. `name: Core Full Suite Advisory (Elixir ${{ matrix.elixir }} / OTP ${{ matrix.otp }})` — the `Advisory (` substring in the rendered name (`Core Full Suite Advisory (Elixir 1.19 / OTP 28)`) matches the `isAdvisory()` pattern in publish-hex.yml.

    2. `if: github.event_name != 'pull_request'` at the JOB level — this is the only way to prevent a matrix row from running on PR events (GitHub Actions does not support per-row if conditions). This ensures the 1.19 row NEVER runs on PR, NEVER blocks a PR, and NEVER appears in a PR's required checks context.

    3. `strategy.matrix.include: [{elixir: "1.19", otp: "28"}]` with `fail-fast: false`.

    4. Same `services:` block as `core_full_suite_advisory` (Postgres) — read it from the existing job and copy exactly.

    5. Same `env:` block as `core_full_suite_advisory` — read and copy exactly.

    6. Steps: same `actions/checkout@<sha>`, `erlef/setup-beam@<sha>` structure AS in `core_full_suite_advisory`, BUT with the following setup-beam differences:
       - Do NOT use `version-file: .tool-versions` (advisory-matrix.yml is EXEMPT from the Phase 129 version-file contract — its purpose is to test SPECIFIC named toolchains).
       - Use `elixir-version: ${{ matrix.elixir }}` and `otp-version: ${{ matrix.otp }}` (the matrix drives the toolchain for advisory rows).
       - Use the SAME `erlef/setup-beam@<sha>` pin already in the file.

    7. Cache key: `mix-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('**/mix.lock') }}` with restore-keys `mix-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-`. This includes the matrix toolchain dimension to prevent cross-toolchain artifact bleed (Pitfall 4). Do NOT use the Phase 129 `.tool-versions` hash here.

    8. Same `mix deps.get` and `mix test` steps as `core_full_suite_advisory`.

    9. LD-13 comment block (verbatim from the interface_context above) in the job header, BEFORE the `name:` line or immediately after it as a YAML comment.

    Do NOT add a `provider_compatibility_advisory` 1.19 row — only the `core` advisory row is in scope for SUPPLY-05.

    Do NOT remove the `pull_request:` trigger from the top-level `on:` block — the existing `core_full_suite_advisory` job running on PRs is acceptable and provides value. Only the new job uses the `if:` guard.

    In `mix.exs`, add a brief inline comment on the `elixir: "~> 1.18"` project floor line (or the line immediately above it) referencing the LD-13 invariant: something like `# LD-13: when the required pin advances past 1.18, add a 1.18 floor row to the required lane or raise this floor.`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q 'core_latest_elixir_advisory' .github/workflows/advisory-matrix.yml && grep -q "github.event_name != 'pull_request'" .github/workflows/advisory-matrix.yml && grep -q '"1.19"' .github/workflows/advisory-matrix.yml && grep -q '"28"' .github/workflows/advisory-matrix.yml && grep -q 'LD-13' .github/workflows/advisory-matrix.yml && grep -q 'LD-13' mix.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'core_latest_elixir_advisory' .github/workflows/advisory-matrix.yml` matches.
    - `grep "github.event_name != 'pull_request'" .github/workflows/advisory-matrix.yml` matches at the job level.
    - `grep '"1.19"' .github/workflows/advisory-matrix.yml` and `grep '"28"'` both match.
    - `grep 'LD-13' .github/workflows/advisory-matrix.yml` matches (invariant comment present in the workflow).
    - `grep 'LD-13' mix.exs` matches (floor reference comment present).
    - The job name pattern `Core Full Suite Advisory (Elixir ${{ matrix.elixir }} / OTP ${{ matrix.otp }})` renders as `Core Full Suite Advisory (Elixir 1.19 / OTP 28)` — contains `Advisory (`.
    - The existing `core_full_suite_advisory` and `provider_compatibility_advisory` jobs are unchanged.
    - The setup-beam in the new job uses `elixir-version: ${{ matrix.elixir }}` / `otp-version: ${{ matrix.otp }}` (not version-file — advisory-matrix.yml is EXEMPT from Phase 129 version-file contract).
    - Cache key includes `${{ matrix.elixir }}-${{ matrix.otp }}` to scope per-toolchain (no cross-toolchain bleed).
    - OPEN QUESTION A2 resolved and documented in SUMMARY: whether setup-beam v1.24.x resolves `"1.19"` + `"28"` on ubuntu-latest; if not, what version string format is required.
  </acceptance_criteria>
  <done>advisory-matrix.yml has a 1.19/OTP28 advisory job that never runs on PR, is never required, and documents the LD-13 floor-coincidence invariant; mix.exs floor declaration references LD-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependabot PRs → sibling mix.lock | Dependabot opens PRs against sibling packages; reference/ baselines must remain excluded or the coordinated-5-file-change trap triggers. |
| actions/dependency-review-action → PR manifest diff | External GitHub-maintained action scans PR changes; must be advisory (continue-on-error) to honor LD-4. |
| 1.19/OTP28 advisory row → required-checks gate | A mis-named advisory job or a missing job-level if: guard could let the 1.19 row accidentally block PRs or the publish gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-05 | Tampering | dependabot.yml reference/ exclusion | high | mitigate | No `reference/host_app` or `reference/demo_app` entries added; acceptance_criteria grep verifies `reference` absent from the file. |
| T-130-06 | Denial of Service | dependency-review-action blocking PRs under an advisory wave | high | mitigate | `continue-on-error: true` on the step; LD-4 advisory-on-PR invariant (a red dep-review never blocks a PR). |
| T-130-07 | Denial of Service | 1.19/OTP28 row running on PR and blocking | high | mitigate | Job-level `if: github.event_name != 'pull_request'` is the ONLY mechanism for matrix-level PR exclusion; verified in acceptance_criteria. |
| T-130-08 | Tampering | SHA un-pinned dependency-review-action | high | mitigate | Pin to `a1d282b36b6f3519aa1f3fc636f609c47dddb294` (v5.0.0) per CLAUDE.md; Dependabot watches `.github/workflows/` for drift. |
| T-130-09 | Tampering | cross-toolchain cache bleed (OTP27 artifacts restored for OTP28 job) | medium | mitigate | Cache key for 1.19 row includes `${{ matrix.elixir }}-${{ matrix.otp }}` dims (Pitfall 4 avoidance). |
| T-130-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager installs in this plan — pure YAML edits only. |
</threat_model>

<verification>
Phase-level (run after all tasks complete):

1. `grep -c 'directory:' .github/dependabot.yml | grep -qx '4' && echo OK` — exactly 4 directory entries.

2. `grep -q 'dependency-review-action@a1d282b' .github/workflows/actionlint.yml && grep -q 'continue-on-error: true' .github/workflows/actionlint.yml && echo OK` — advisory dep-review step is present and non-blocking.

3. `grep -q 'core_latest_elixir_advisory' .github/workflows/advisory-matrix.yml && grep -q "event_name != 'pull_request'" .github/workflows/advisory-matrix.yml && grep -q 'LD-13' .github/workflows/advisory-matrix.yml && echo OK` — 1.19 job present with PR guard and invariant comment.

4. `grep -q 'reference' .github/dependabot.yml && echo FAIL || echo OK` — reference/ baselines NOT in dependabot.yml.

5. Push `phase/130` branch and confirm CI: `core_latest_elixir_advisory` job does NOT appear in the PR checks list; it appears in the push/cron run list but never as required.
</verification>

<success_criteria>
- `grep -c 'directory:' .github/dependabot.yml` returns 4 (root mix + root github-actions + admin + inbound); no reference/ entry.
- `grep 'dependency-review-action@a1d282b' .github/workflows/actionlint.yml` matches; `continue-on-error: true` is set.
- `grep "github.event_name != 'pull_request'" .github/workflows/advisory-matrix.yml` matches at the `core_latest_elixir_advisory` job level.
- `grep 'Advisory (' .github/workflows/advisory-matrix.yml` matches the new job name (isAdvisory() pattern satisfied).
- LD-13 invariant comment is present in advisory-matrix.yml and referenced in mix.exs.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (Plan 02)

- **`.github/dependabot.yml`**: Two new `mix` ecosystem entries for `/mailglass_admin` and `/mailglass_inbound` (weekly schedule, no reference/ entries).
- **`.github/workflows/actionlint.yml`**: `dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0` step (continue-on-error: true, PR-only step condition, fail-on-severity: high).
- **`.github/workflows/advisory-matrix.yml`**: `core_latest_elixir_advisory` job (if: github.event_name != 'pull_request', matrix elixir 1.19 / otp 28, matrix-parameterized cache key, LD-13 invariant comment block).
- **`mix.exs`**: LD-13 invariant comment near the `elixir: "~> 1.18"` floor declaration.
</artifacts_produced>

<output>
Create `.planning/phases/130-supply-chain-workflow-hygiene/130-02-SUMMARY.md` when done.
</output>
