---
phase: 044.5-v1-0-1-1-release-ceremony
plan: 02
subsystem: release-engineering
tags: [release, hex, ci-workflow, mailglass_inbound, runbook]
requires:
  - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-CONTEXT.md (D-44.5-03)
  - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 3)
  - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (publish-hex.yml clone instructions, post-publish-smoke.yml inbound additions, MAINTAINING.md edits)
  - mailglass_inbound/mix.exs (@version source for runtime read)
  - lib/mix/tasks/mailglass.publish.check.ex (--package mailglass_inbound support)
provides:
  - publish-hex.yml accepts `package=mailglass_inbound` and `package=all` (default `all`); legacy `both` removed
  - publish-inbound job parallel-safe with publish-admin under `needs: [publish-core]`
  - post-publish-smoke.yml exposes `cron-guard.outputs.version_inbound` (runtime read of mailglass_inbound/mix.exs)
  - Three parallel inbound smoke steps (wait-for-index, wait-for-hexdocs, retracted-check)
  - consumer-install adds {:mailglass_inbound, "== <version_inbound>"} dep without invoking `mix mailglass.install`
  - MAINTAINING.md runbook reflects three-package fan-out (`package=all`, parallel admin+inbound, locked versions)
affects:
  - .github/workflows/publish-hex.yml (367 lines after; +88 -3)
  - .github/workflows/post-publish-smoke.yml (522 lines after; +78 -1)
  - MAINTAINING.md (189 lines after; +4 -4)
tech-stack:
  added: []
  patterns:
    - GitHub Actions `outputs:` block + step-level `id:` referencing pattern (cron-guard exposes version_inbound by referencing read-inbound-version step)
    - Conditional fan-out via per-job `if:` block negative-form exclusion (`package != 'mailglass_admin' && package != 'mailglass_inbound'`)
    - Idempotency guard via `mix hex.info <pkg> <ver> | grep -q "Released:"` (cloned from publish-admin)
    - MIX_PUBLISH=true environment swap (cloned from publish-admin's mailglass_dep selector pattern)
    - Optional dep entry pattern in Elixir heredoc (skip inbound if version_inbound is empty/unresolved)
key-files:
  created: []
  modified:
    - .github/workflows/publish-hex.yml
    - .github/workflows/post-publish-smoke.yml
    - MAINTAINING.md
decisions:
  - D-44.5-03 implemented end-to-end (3-of-3 commits): inbound rides the canonical Environment-gated workflow; no manual one-shot publish; HEX_API_KEY stays off the maintainer machine
  - cron-guard version_inbound resolution uses Option B (runtime read) per RESEARCH.md A6 — future-proof for inbound 0.2.0+ without each-release bumps
  - publish-inbound runs parallel with publish-admin (both `needs: [publish-core]`) — faster feedback inside the 60-minute revert window per CONTEXT.md "Claude's Discretion" line 113
  - Pre-publish check for mailglass_admin step preserved verbatim (per plan instruction); admin runs whenever package != 'mailglass' (which is correct because `all`, `mailglass_admin`, `mailglass_inbound` all pass that filter, but the publish-admin job's `if:` block handles the fanout strictly)
  - MAINTAINING.md sandbox install snippet uses `== 1.0.0` / `== 0.1.0` literal pins per plan Edit 3 (not the actual `0.3.2` inbound version) — matches the v1.0/1.1 ceremony's locked versions described in CONTEXT.md
metrics:
  duration_ms: ~1200000
  completed_at: 2026-05-07T10:53:01Z
  tasks_completed: 3
  files_modified: 3
---

# Phase 044.5 Plan 02: v1.0/1.1 Release Ceremony — Commit B (publish-inbound + smoke parity + MAINTAINING) Summary

Wired `mailglass_inbound` into the canonical Environment-gated Hex publish pipeline alongside `mailglass` and `mailglass_admin`, extended `post-publish-smoke.yml` with three parallel inbound smoke steps (cron-guard now exposes a runtime-resolved `version_inbound`), and updated the MAINTAINING.md runbook to reflect the three-package fan-out (`package=all`, locked versions, "core then admin and inbound in parallel"). Last-mile workflow plumbing — inbound's docs-contract-respecting smoke (deps.get + compile-clean, no installer) is preserved.

## Files Modified

| File | Lines (after) | Net Change | Insertion Points |
|------|---------------|------------|------------------|
| `.github/workflows/publish-hex.yml` | 367 | +88 −3 | input enum (line 23-32); prepublish-summary inbound step (after admin step); publish-core `if:` block extended; publish-admin `if:` block extended; new `publish-inbound` job (after publish-admin) |
| `.github/workflows/post-publish-smoke.yml` | 522 | +78 −1 | cron-guard `outputs:` block (`version_inbound` added); cron-guard checkout + `read-inbound-version` step inserted before `guard`; wait-for-index inbound parallel step; wait-for-hexdocs inbound parallel step; retracted-check inbound parallel step; consumer-install Elixir heredoc updated |
| `MAINTAINING.md` | 189 | +4 −4 | Lines 147-149 (Package order narrative + Idempotency note + Fallback path); Line 157 (sandbox install snippet) |

## Per-Task Commits

| Task | Description | Commit |
|------|-------------|--------|
| 1 | publish-hex.yml: input enum (`all`/`mailglass_inbound`), prepublish-summary inbound step, publish-core/admin `if:` block updates, new publish-inbound job | `9083e16` |
| 2 | post-publish-smoke.yml: cron-guard version_inbound output (runtime read), three parallel inbound smoke steps, consumer-install dep | `2a36e93` |
| 3 | MAINTAINING.md: package=all, three-package fan-out narrative, sandbox install snippet | `b2f44a9` |

## Verification Output

### actionlint

```
$ actionlint .github/workflows/publish-hex.yml
(exit 0)

$ actionlint .github/workflows/post-publish-smoke.yml
(exit 0)
```

### `mix mailglass.publish.check --package mailglass_inbound` (last 20 lines)

```
[create] build and unpack tarball for mailglass_inbound
[create] capture file list for mailglass_inbound
[unchanged] compare allowlist for mailglass_inbound
[unchanged] check denylist for mailglass_inbound
[update] check tarball size for mailglass_inbound
[update] tarball size 110682 bytes (0.11 MB)
[unchanged] check required files for mailglass_inbound
[unchanged] check CHANGELOG section for mailglass_inbound
[unchanged] check mix metadata for mailglass_inbound
[unchanged] check dependency shapes for mailglass_inbound
[unchanged] check linked-version constraint for mailglass_inbound
[unchanged] check prod deps resolution for mailglass_inbound
[unchanged] compile tarball in isolation for mailglass_inbound
[update] run hex.audit for mailglass_inbound
[update] capture hex.outdated advisory for mailglass_inbound
[update] write reviewer summary for mailglass_inbound
[update] clean unpacked tarball for mailglass_inbound
Pre-publish check result for mailglass_inbound: create=2 update=5 unchanged=9 conflict=0
(exit 0)
```

No regression — the in-tree `mix mailglass.publish.check --package mailglass_inbound` task continues to run end-to-end.

### `must_haves` truth coverage (grep verification)

`.github/workflows/publish-hex.yml`:

```
$ grep -c '^  publish-inbound:' .github/workflows/publish-hex.yml
1
$ grep -cE '^[[:space:]]+- mailglass_inbound$' .github/workflows/publish-hex.yml
1
$ grep -cE '^[[:space:]]+- all$' .github/workflows/publish-hex.yml
1
$ grep -cE '^[[:space:]]+- both$' .github/workflows/publish-hex.yml
0
$ grep -c 'default: all' .github/workflows/publish-hex.yml
1
$ grep -c 'default: both' .github/workflows/publish-hex.yml
0
$ grep -c 'Pre-publish check for mailglass_inbound' .github/workflows/publish-hex.yml
1
$ grep -c 'mix mailglass.publish.check --package mailglass_inbound' .github/workflows/publish-hex.yml
1
$ grep -c 'mix hex.info mailglass_inbound' .github/workflows/publish-hex.yml
1
$ grep -c 'working-directory: mailglass_inbound' .github/workflows/publish-hex.yml
3
$ grep -c 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd' .github/workflows/publish-hex.yml
4
$ grep -c 'erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93' .github/workflows/publish-hex.yml
4
$ grep -c 'actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae' .github/workflows/publish-hex.yml
4
$ grep -c "package != 'mailglass_inbound'" .github/workflows/publish-hex.yml
2
```

Pinned action SHAs preserved at 4× (one occurrence per job: prepublish-summary, publish-core, publish-admin, publish-inbound) — exceeds the ≥3 acceptance threshold.

`.github/workflows/post-publish-smoke.yml`:

```
$ grep -c 'version_inbound' .github/workflows/post-publish-smoke.yml
11
$ grep -c 'Wait for mailglass_inbound index' .github/workflows/post-publish-smoke.yml
1
$ grep -c 'Wait for mailglass_inbound HexDocs' .github/workflows/post-publish-smoke.yml
1
$ grep -c 'Check mailglass_inbound not retracted' .github/workflows/post-publish-smoke.yml
1
$ grep -c 'mailglass_inbound' .github/workflows/post-publish-smoke.yml
19
$ grep -E 'mailglass_inbound.*mix mailglass.install' .github/workflows/post-publish-smoke.yml | wc -l
0
$ grep -c 'cron-guard.outputs.version_inbound' .github/workflows/post-publish-smoke.yml
4
$ grep -c '{:mailglass_inbound, \"== #{version_inbound}\"' .github/workflows/post-publish-smoke.yml
1
```

`MAINTAINING.md`:

```
$ grep -c 'package=all' MAINTAINING.md
1
$ grep -c 'package=both' MAINTAINING.md
0
$ grep -c 'mailglass_inbound' MAINTAINING.md
5
$ grep -c 'in parallel against the newly live' MAINTAINING.md
1
$ grep -c 'mailglass-v1.0.0' MAINTAINING.md
1
$ grep -c 'All three publish steps check' MAINTAINING.md
1
$ rg "[\x{1F300}-\x{1F9FF}]" MAINTAINING.md | wc -l
0
```

All 11 `must_haves.truths` met. All 3 `must_haves.artifacts` exist with the required `contains:` markers.

### git diff scope (MAINTAINING.md)

`git diff` confirms only line ranges 144-149 and 154-159 modified — Retract Decision Tree (lines 76-94) and Required Checks (lines 26-43) are unchanged.

## Threat Disposition Status

| Threat ID | Disposition | Status | Evidence |
|-----------|-------------|--------|----------|
| T-44.5-02-01 (Tampering — `if:` block drift silently excludes inbound on `package=all`) | mitigate | mitigated | `actionlint` exits 0 on publish-hex.yml; `grep -c "package != 'mailglass_inbound'"` returns 2 (publish-core + publish-admin both excluding explicit-inbound dispatch); `grep -c '^  publish-inbound:'` returns 1 (job declared); fanout shape: `release` event runs all three; `package=all` runs all three; `package=mailglass_inbound` runs only inbound; `package=mailglass` runs only core; `package=mailglass_admin` runs only admin |
| T-44.5-02-02 (Information Disclosure — HEX_API_KEY exposure if Environment gate omitted) | mitigate | mitigated | publish-inbound job preserves `environment: hex-publish` verbatim from publish-admin; `grep -c 'environment: hex-publish'` returns 3 across publish-core, publish-admin, publish-inbound; HEX_API_KEY only injected when Environment reviewer approves |
| T-44.5-02-03 (Tampering — pinned action SHA drift during clone) | mitigate | mitigated | `grep -c 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd'` returns 4; `grep -c 'erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93'` returns 4; `grep -c 'actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae'` returns 4 — all preserved verbatim across the four jobs |
| T-44.5-02-04 (Availability — version_inbound not resolved → smoke timeout) | mitigate | mitigated | cron-guard now reads `mailglass_inbound/mix.exs` at the checked-out tag and exposes `version_inbound` via step output; downstream jobs reference `${{ needs.cron-guard.outputs.version_inbound }}`; if the file is absent at the ref or empty, the new step writes empty `version_inbound=` and the three downstream inbound steps skip with `exit 0` (no smoke timeout) |
| T-44.5-02-05 (Tampering — consumer-install invokes `mix mailglass.install` for inbound, violating docs contract) | accept (with explicit anti-pattern check) | accepted, no regression | `grep -E 'mailglass_inbound.*mix mailglass.install'` returns 0 — the docs-contract-respecting pattern (deps.get + compile-clean, no installer) is preserved. The Elixir heredoc adds inbound to deps when version_inbound is non-empty, then the existing `mix deps.get` and `mix compile --warnings-as-errors` steps cover it |
| T-44.5-02-06 (Tampering — `if:` fanout silently excludes inbound on `package=all`) | mitigate | mitigated | publish-inbound `if:` block has the explicit four-clause shape from PATTERNS.md: handles `release` event, `package=all` (via negative form `package != 'mailglass' && package != 'mailglass_admin'`), explicit `package=mailglass_inbound`, and skipped-core fallback. Plan 04 dry-run dispatch (Wave 3) will exercise all three jobs end-to-end |

## Deviations from Plan

**1. [Rule 3 — Build dependency] mix deps.get pre-flight**
- **Found during:** Task 1 verification
- **Issue:** `mix mailglass.publish.check --package mailglass_inbound` failed initially because root deps were not installed in the worktree.
- **Fix:** Ran `mix deps.get` once at the worktree root before re-running the verify command. No file modifications; transient runtime state only.
- **Files modified:** None (deps fetched into `_build/` and `deps/` — already in `.gitignore`).
- **Commit:** N/A (no source change).

**2. [Note — text fidelity] sandbox install snippet uses plan-specified `== 0.1.0` for inbound, not actual mix.exs version**
- **Observation:** `mailglass_inbound/mix.exs` at this SHA pins `@version "0.3.2"`, but the plan's Edit 3 target text specifies `{:mailglass_inbound, "== 0.1.0"}` per CONTEXT.md's locked v1.0/1.1 ceremony scope.
- **Resolution:** Followed the plan exactly. The MAINTAINING.md sandbox install snippet describes the post-ceremony locked-publish state per D-44.5-04 step 5, not the pre-ceremony in-tree version. This is consistent with the plan's stated behavior and the v1.0/1.1 release narrative; `mix mailglass.publish.check` does not regress because it reads the live `mix.exs` value (0.3.2) — independent of the doc string.
- **Files modified:** N/A (in-line with plan text).

No other deviations. No `Rule 4` architectural changes triggered. No checkpoints encountered.

## Authentication Gates

None encountered.

## Self-Check: PASSED

- `.github/workflows/publish-hex.yml` exists at the expected path: FOUND
- `.github/workflows/post-publish-smoke.yml` exists at the expected path: FOUND
- `MAINTAINING.md` exists at the expected path: FOUND
- Commit `9083e16` (Task 1) found in `git log`: FOUND
- Commit `2a36e93` (Task 2) found in `git log`: FOUND
- Commit `b2f44a9` (Task 3) found in `git log`: FOUND
- All `must_haves.truths` (11) backed by grep evidence above: VERIFIED
- All `must_haves.artifacts` (3) with `contains:` markers: VERIFIED
- All `must_haves.key_links` (3) traceable via the exact patterns specified: VERIFIED

## Proposed Conventional Commit Message (overall PR)

```
feat(release-engineering): add publish-inbound job + post-publish-smoke inbound parity

Extends the canonical Hex publish pipeline to handle mailglass_inbound
alongside mailglass + mailglass_admin per D-44.5-03:

- publish-hex.yml: input enum gains mailglass_inbound + all (default
  all; legacy both removed); prepublish-summary runs publish.check for
  inbound; new publish-inbound job clones publish-admin with parallel
  sequencing (needs: [publish-core]); publish-core + publish-admin if:
  blocks updated to exclude explicit-inbound dispatch.
- post-publish-smoke.yml: cron-guard exposes version_inbound (runtime
  read of mailglass_inbound/mix.exs); wait-for-index, wait-for-hexdocs,
  retracted-check each gain a parallel inbound step keyed on
  VERSION_INBOUND; consumer-install adds {:mailglass_inbound, "== ..."}
  to sandbox deps without invoking mix mailglass.install (inbound's
  docs contract enforces manual setup only).
- MAINTAINING.md: package=both → package=all; fan-out narrative
  reflects core, then admin and inbound in parallel; sandbox install
  snippet ships locked 1.0.0 / 1.0.0 / 0.1.0 versions.

All pinned action SHAs preserved. actionlint exits 0 on both workflows.
```

## Next Step

**Plan 03 (Commit C — curated CHANGELOG + `docs/upgrade-from-0.x.md`)** lands on `main`. After Plan 03 + the prereq Plan 01 (release-please.yml jq query rewrite) ship, Plan 04's `dry_run=true package=all` rehearsal can exercise all three packages — including `mailglass_inbound` — through the same gated workflow exactly once before the live publish in Plan 05.
