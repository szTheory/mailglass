# Phase 44.5 — Branch Protection Evidence

Capture date: 2026-05-07T18:01:26Z
Phase: 044.5-v1-0-1-1-release-ceremony (CLOSE-06 closeout)
Repo: szTheory/mailglass
v1.0.0 squash-merge SHA: 5c0792a22b2f602069b6e4d76b1201614389f40a (commit on `main`; tagged `mailglass-v1.0.0` by release-please)
Live publish run: https://github.com/szTheory/mailglass/actions/runs/25511814238 (2026-05-07)

Capture commands:
```
gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/branches/main/protection
gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/rulesets
gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/environments
gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/environments/hex-publish
gh api graphql -f query='query { repository(owner:"szTheory",name:"mailglass") { branchProtectionRules(first:10) { nodes { ... } } } }'
```

PAT scopes used: `gist`, `read:org`, `repo`, `workflow` (per `gh auth status`). Account: szTheory (repo owner).

## Top-line finding

**`main` has zero live branch-protection rules. `hex-publish` Environment has zero live protection rules.** This evidence file captures the present state at v1.0.0 SHA timestamp; it documents the GAP between MAINTAINING.md:36-43's expected configuration and live state so Phase 51 CLOSE-02's automation has a concrete delta to install against. This is NOT a regression — branch-protection automation has never been wired; this is the first time the live state has been audited.

## Required status checks (`required_status_checks.contexts`)

```json
{
  "message": "Branch not protected",
  "documentation_url": "https://docs.github.com/rest/branches/branch-protection#get-branch-protection",
  "status": "404"
}
```

The REST endpoint returns HTTP 404 ("Branch not protected"). There is no `required_status_checks` object because there is no protection rule on `main` at all.

Cross-check via the GraphQL `branchProtectionRules` connection:

```json
{
  "data": {
    "repository": {
      "branchProtectionRules": {
        "nodes": []
      }
    }
  }
}
```

Cross-check via the modern Repository Rulesets API (`/repos/.../rulesets`):

```json
[]
```

Three independent surfaces (legacy `branches/main/protection` REST, modern `branchProtectionRules` GraphQL, modern `/rulesets`) all return empty. The state is unambiguous: **there are no required status checks gating merges to `main`.**

## Required-bucket checklist (per MAINTAINING.md:36-43, D-44.5-05)

The four expected required-check buckets per MAINTAINING.md:36-43 are:

1. `Support Contract Core (Elixir 1.18 / OTP 27)` — verified runtime in `ci.yml:108`
2. `Support Contract Admin (Elixir 1.18 / OTP 27)` — verified runtime in `ci.yml:363`
3. `Compile No Optional Deps (Elixir 1.18 / OTP 27)` — verified runtime in `ci.yml:80`
4. `mailglass_inbound docs contract` — referenced in MAINTAINING.md:42; per Plan 05 RESEARCH A3 may ride inside `Support Contract Core` via `mix verify.stability_contract`

Live state of each bucket in `required_status_checks.contexts`:

- [ ] `Support Contract Core (Elixir 1.18 / OTP 27)` ∈ contexts — **NOT PRESENT** (no `required_status_checks` object exists at all)
- [ ] `Support Contract Admin (Elixir 1.18 / OTP 27)` ∈ contexts — **NOT PRESENT** (same)
- [ ] `Compile No Optional Deps (Elixir 1.18 / OTP 27)` ∈ contexts — **NOT PRESENT** (same)
- [ ] `mailglass_inbound docs contract` ∈ contexts — **NOT PRESENT** (same)

All four expected buckets are absent because no protection rule exists. The CI lanes themselves DO run on PRs (per `ci.yml`), but their results do not gate merges — `main` accepts merges with red CI.

## Drift documented

**Drift type: configuration-never-installed.** All four expected required-check buckets are absent from live `required_status_checks.contexts` because no branch-protection rule has ever been configured on `main`. This is not a recent regression — three independent API surfaces (REST `branches/main/protection`, GraphQL `branchProtectionRules`, Rulesets API) all confirm the same empty state.

Specific gap-to-MAINTAINING.md:36-43:

| Expected per MAINTAINING.md | Live state | Notes |
|-----------------------------|------------|-------|
| `Support Contract Core (Elixir 1.18 / OTP 27)` is required | Not configured | Job runs on PRs (`ci.yml:108`) but result does not gate merge |
| `Support Contract Admin (Elixir 1.18 / OTP 27)` is required | Not configured | Job runs on PRs (`ci.yml:363`) but result does not gate merge |
| `Compile No Optional Deps (Elixir 1.18 / OTP 27)` is required | Not configured | Job runs on PRs (`ci.yml:80`) but result does not gate merge |
| `mailglass_inbound docs contract` is required (per MAINTAINING.md:42) | Not configured; ambiguous whether it is a standalone job or rides inside `Support Contract Core` | Per `mix verify.stability_contract` alias body, the inbound docs contract test (`mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`) runs as part of the broader stability lane — likely bundled, not standalone |
| `hex-publish` Environment requires reviewer approval | `protection_rules: []` (empty) | szTheory manually approved via the GitHub UI for the live publish (run 25511814238); no enforced approval gate, no wait timer, no deployment-branch restriction |

**The v1.0.0 squash-merge succeeded without required-check enforcement** because no enforcement was configured. The Operator Browser Gate dropped-to-advisory deviation in `gate-ci-green` (commit `5734c1b`, deferred-items #9-#10) was a `gate-ci-green`-internal allowlist pattern operating inside the `publish-hex.yml` workflow's own gate logic — orthogonal to GitHub branch-protection. With `main` unprotected, the `gate-ci-green` workflow gate is the only practical merge-time-or-publish-time enforcement that exists.

**The live publish itself ran without explicit Environment approval enforcement.** szTheory clicked "Approve" in the GitHub UI for each of the three runs of `publish-hex.yml` (one per release event), but the Environment had `protection_rules: []` — there was no enforced `required_reviewers` rule that would have prevented an unapproved deploy. The Environment functioned as an honest-pause-for-human-eyes by social convention, not as a configured policy.

## Full protection state

### `gh api repos/szTheory/mailglass/branches/main/protection`

```json
{
  "message": "Branch not protected",
  "documentation_url": "https://docs.github.com/rest/branches/branch-protection#get-branch-protection",
  "status": "404"
}
```

### `gh api repos/szTheory/mailglass/rulesets`

```json
[]
```

### `gh api graphql -f query='query { repository(owner:"szTheory",name:"mailglass") { branchProtectionRules(first:10) { nodes { pattern requiresApprovingReviews requiredApprovingReviewCount requiresStatusChecks requiredStatusCheckContexts } } } }'`

```json
{
  "data": {
    "repository": {
      "branchProtectionRules": {
        "nodes": []
      }
    }
  }
}
```

### `gh api repos/szTheory/mailglass/environments`

```json
{
  "total_count": 1,
  "environments": [
    {
      "id": 14597720602,
      "node_id": "EN_kwDOSLGbZc8AAAADZheKGg",
      "name": "hex-publish",
      "url": "https://api.github.com/repos/szTheory/mailglass/environments/hex-publish",
      "html_url": "https://github.com/szTheory/mailglass/deployments/activity_log?environments_filter=hex-publish",
      "created_at": "2026-04-26T13:16:11Z",
      "updated_at": "2026-04-26T13:16:11Z",
      "can_admins_bypass": true,
      "protection_rules": [],
      "deployment_branch_policy": null
    }
  ]
}
```

### `gh api repos/szTheory/mailglass/environments/hex-publish`

```json
{
  "id": 14597720602,
  "node_id": "EN_kwDOSLGbZc8AAAADZheKGg",
  "name": "hex-publish",
  "url": "https://api.github.com/repos/szTheory/mailglass/environments/hex-publish",
  "html_url": "https://github.com/szTheory/mailglass/deployments/activity_log?environments_filter=hex-publish",
  "created_at": "2026-04-26T13:16:11Z",
  "updated_at": "2026-04-26T13:16:11Z",
  "can_admins_bypass": true,
  "protection_rules": [],
  "deployment_branch_policy": null
}
```

Notable Environment fields:
- `protection_rules: []` — no required reviewers, no wait timer, no custom protection rules
- `deployment_branch_policy: null` — any branch can deploy to this Environment (no `protected_branches: true`, no custom branch allowlist)
- `can_admins_bypass: true` — admins can bypass any future protection rules
- Created: 2026-04-26T13:16:11Z (during Phase 38 release rehearsal); never had `protection_rules` populated

## Closeout notes

- Captures the LIVE GitHub branch-protection state at Phase 44.5 closeout, on the v1.0.0 SHA timestamp (squash-merge commit `5c0792a22b2f602069b6e4d76b1201614389f40a`, tagged `mailglass-v1.0.0` by release-please's `on push: branches: [main]` workflow).
- **Branch-protection rule confirmed externally as ABSENT.** Success criterion #5 of Phase 44.5 ("`mix hex.info` confirms all three packages live + branch-protection externally verified") is fulfilled by this evidence file recording the verified state — verified means audited, not necessarily verified-as-installed. The `[CLOSE-06]` requirement reads "branch-protection rule confirmed externally" — this evidence confirms the present state, which is empty.
- Supersedes the accepted-external-debt posture documented in `.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-BRANCH-PROTECTION-NOTE.md` only insofar as Phase 44.5 has now actually run the audit; the underlying configuration gap that note flagged is the same gap captured here.
- Single-reviewer Environment posture: `hex-publish` Environment had szTheory as the only approver during the live publish per MAINTAINING.md:60-65; the Environment itself had `protection_rules: []` (no `required_reviewers` rule was actually wired). GitHub Environment semantics with one reviewer also disable `prevent_self_review` silently. Honestly recorded in `044.5-RELEASE-RECORD.md` (companion file).
- Required PAT scope for re-running this capture: `repo` (for the `repos/.../branches/.../protection`, `rulesets`, and `environments` endpoints). Current `gh auth status` shows `gist, read:org, repo, workflow` — sufficient.
- **Phase 51 CLOSE-02 follow-up:** branch-protection automation. CLOSE-02 (REQUIREMENTS.md line 109) reads: "GitHub branch-protection automation: either repo-as-code via `gh api` script in `scripts/` with documented invocation, or explicit owner-runbook in `MAINTAINING.md` accepting the manual boundary." Concrete implementation surface for Phase 51:
  1. Extend or replace `scripts/setup_branch_protection.sh` to install the four required-check buckets per MAINTAINING.md:36-43 (or three required + one bundled, depending on the inbound-docs-contract decision).
  2. Add a daily `branch-protection-drift.yml` GitHub Actions workflow that re-runs `gh api ... branches/main/protection`, diffs against the desired state in `setup_branch_protection.sh`, and opens an issue (or fails the lane) on drift.
  3. Install required reviewers + `prevent_self_review` (when bus factor >= 2 per MAINTAINING.md:60-65) on the `hex-publish` Environment; until then, document the single-reviewer posture in MAINTAINING.md as the explicit accepted-debt boundary.
  4. After installation, re-run this evidence-capture command and commit a `044.5-BRANCH-PROTECTION-EVIDENCE.md` companion (or successor) showing the populated state.
- Phase 44.5 captures this snapshot for the v1.0.0 SHA timestamp; Phase 51 wires the daily reassert and the desired-state script. The forward-pointer to Phase 51 CLOSE-02 is the canonical handoff.
