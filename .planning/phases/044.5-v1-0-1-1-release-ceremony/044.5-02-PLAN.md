---
phase: 044.5-v1-0-1-1-release-ceremony
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
  - MAINTAINING.md
autonomous: true
requirements: [CLOSE-06]
requirements_addressed: [CLOSE-06]
decisions_implemented: [D-44.5-03]

must_haves:
  truths:
    - "publish-hex.yml input enum includes `mailglass_inbound` and `all`; default is `all`"
    - "publish-hex.yml has a `prepublish-summary` step for `mailglass_inbound` mirroring the existing admin step"
    - "publish-hex.yml has a `publish-inbound` job cloned from `publish-admin` with `needs: [publish-core]`, parallel-safe with admin"
    - "publish-inbound job uses `working-directory: mailglass_inbound`, `MIX_PUBLISH=true`, idempotency guard via `mix hex.info mailglass_inbound`"
    - "publish-inbound job preserves all pinned action SHAs verbatim from publish-admin clone"
    - "post-publish-smoke.yml `wait-for-index`, `wait-for-hexdocs`, `retracted-check` each have a parallel `mailglass_inbound` step"
    - "post-publish-smoke.yml `cron-guard` step exposes a `version_inbound` output (resolved at runtime by reading `mailglass_inbound/mix.exs`)"
    - "post-publish-smoke.yml `consumer-install` adds `{:mailglass_inbound, \"== <version_inbound>\"}` to the sandbox deps without invoking `mix mailglass.install` for inbound"
    - "MAINTAINING.md updated: `package=both` → `package=all`; fan-out narrative reflects 'core, then admin and inbound in parallel'"
    - "MAINTAINING.md sandbox install snippet adds `{:mailglass_inbound, \"== 0.1.0\"}`"
    - "`actionlint` exits 0 on both publish-hex.yml and post-publish-smoke.yml"
  artifacts:
    - path: ".github/workflows/publish-hex.yml"
      provides: "Three-job publish pipeline (publish-core, publish-admin, publish-inbound) with `all` enum support"
      contains: "publish-inbound:"
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "Three-package post-publish smoke (wait-for-index, wait-for-hexdocs, consumer-install, retracted-check) with version_inbound output"
      contains: "version_inbound"
    - path: "MAINTAINING.md"
      provides: "Updated runbook for three-package fan-out with `package=all`"
      contains: "package=all"
  key_links:
    - from: "publish-hex.yml workflow_dispatch input.package=all"
      to: "publish-core + publish-admin + publish-inbound `if:` blocks"
      via: "input enum + per-job conditional fanout"
      pattern: "github.event.inputs.package == 'all'"
    - from: "publish-inbound job"
      to: "Hex.pm mailglass_inbound 0.1.0"
      via: "`mix hex.publish` with MIX_PUBLISH=true"
      pattern: "mix hex.publish"
    - from: "post-publish-smoke.yml cron-guard.outputs.version_inbound"
      to: "wait-for-index + wait-for-hexdocs + retracted-check inbound steps"
      via: "GitHub Actions outputs/inputs"
      pattern: "needs.cron-guard.outputs.version_inbound"
---

<objective>
Commit B — publish-hex.yml inbound job + post-publish-smoke.yml parallel
inbound steps + MAINTAINING.md surgical edits (D-44.5-03).

Implement the workflow extensions that bring `mailglass_inbound` into the
canonical release pipeline. Full automation parity with the admin path — no
manual one-shot. After this plan lands, `dry_run=true package=all` rehearsal
in Plan 04 can exercise all three packages through the same gated workflow.

Purpose: D-44.5-03 explicitly rejects manual first-cut publish (HEX_API_KEY
exposure to maintainer machine + unrehearsed ceremony at highest blast radius
moment). The bulk of scaffolding is already done — `mix mailglass.publish.check`
is inbound-aware, `.planning/publish/mailglass_inbound-files.expected` is
committed, `mailglass_inbound/mix.exs:58-64` already has the MIX_PUBLISH swap.
This plan wires the last mile.

Output:
- 3 files modified (1 commit, 1 PR)
- `actionlint` clean on both modified workflow YAMLs
- `mix mailglass.publish.check --package mailglass_inbound` still passes (no regression)
- ready for Plan 03 (Commit C — curated CHANGELOG + upgrade doc) to land on `main`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-CONTEXT.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-VALIDATION.md
@CLAUDE.md
@MAINTAINING.md

<interfaces>
<!-- Key contracts the executor needs verbatim. -->

publish-hex.yml input enum (lines 23-31) — current shape:
```yaml
package:
  description: "Package(s) to publish"
  required: false
  type: choice
  options:
    - mailglass
    - mailglass_admin
    - both
  default: both
```

publish-hex.yml prepublish-summary admin step (lines 101-106) — analog for new inbound step:
```yaml
- name: Pre-publish check for mailglass_admin
  if: ${{ github.event.inputs.package != 'mailglass' }}
  run: |
    echo "## mailglass_admin" >> "$GITHUB_STEP_SUMMARY"
    set -o pipefail
    mix mailglass.publish.check --package mailglass_admin 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"
```

publish-hex.yml publish-admin job (lines 210-282) — exact line range to clone for publish-inbound. Pinned actions (preserve verbatim):
- `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2`
- `erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93  # v1.24.0` (elixir 1.18 / otp 27)
- `actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5`

publish-inbound `if:` block (target shape, per RESEARCH.md Pattern 3 lines 472-487):
```yaml
if: |
  always() &&
  (
    (github.event_name == 'workflow_dispatch' &&
     github.event.inputs.package == 'mailglass_inbound' &&
     needs.publish-core.result == 'skipped') ||
    (github.event_name == 'workflow_dispatch' &&
     github.event.inputs.package != 'mailglass' &&
     github.event.inputs.package != 'mailglass_admin' &&
     needs.publish-core.result == 'success') ||
    (github.event_name == 'release' &&
     needs.publish-core.result == 'success')
  )
```

post-publish-smoke.yml wait-for-index admin step (lines 167-184) — analog for new inbound step:
```yaml
- name: Wait for mailglass_admin index
  env:
    VERSION: ${{ needs.cron-guard.outputs.version }}
  run: |
    if [ -z "${VERSION}" ]; then
      echo "No version resolved; skipping."
      exit 0
    fi
    DEADLINE=$(( SECONDS + 300 ))
    until mix hex.info mailglass_admin "${VERSION}" >/dev/null 2>&1; do
      if [ $SECONDS -ge $DEADLINE ]; then
        echo "Smoke aborted: Hex.pm did not index mailglass_admin ${VERSION} within 5 minutes."
        exit 1
      fi
      echo "Waiting for Hex.pm to index mailglass_admin ${VERSION}..."
      sleep 30
    done
    echo "Hex.pm indexed mailglass_admin ${VERSION}."
```

post-publish-smoke.yml consumer-install `entries` heredoc (lines 271-284 area) — current shape inlines admin + core; target adds inbound dep:
```elixir
entries = "      {:mailglass, \"== #{version}\"},\n      {:mailglass_admin, \"== #{version}\"},\n      {:mailglass_inbound, \"== #{version_inbound}\"},\n"
```

MAINTAINING.md current state (specific edits per PATTERNS.md lines 562-610):
- line 149: `package=both` → `package=all`
- lines 147-148: "core then admin" narrative → "core, then admin and inbound in parallel"
- lines 154-159: sandbox install adds `{:mailglass_inbound, "== 0.1.0"}` dep
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend publish-hex.yml — input enum, prepublish-summary inbound step, publish-inbound job clone, sibling `if:` block updates</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/publish-hex.yml (full file — 282 lines; especially lines 23-31 input enum, 95-106 prepublish-summary, 108-146 gate-ci-green, 148-208 publish-core, 210-282 publish-admin)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Pattern 3 lines 461-549 — exact clone diff with four substantive changes)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 317-477 — line-by-line clone instructions table; lines 1242-1258 idempotency guard pattern; lines 1214-1228 pinned-actions pattern)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-CONTEXT.md (D-44.5-03 lines 53-67)
    - lib/mix/tasks/mailglass.publish.check.ex (lines 60-75 to confirm `--package mailglass_inbound` is accepted)
  </read_first>
  <action>
    Four surgical extensions to `.github/workflows/publish-hex.yml`. Apply in
    this order so the workflow remains valid YAML at every intermediate
    state.

    **Extension 1 — Input enum** (lines 23-31). Replace the `options:` list
    and `default:`:

    Before:
    ```yaml
    options:
      - mailglass
      - mailglass_admin
      - both
    default: both
    ```

    After:
    ```yaml
    options:
      - mailglass
      - mailglass_admin
      - mailglass_inbound
      - all
    default: all
    ```

    Rationale: `both` is replaced by `all` because two of two became three of
    three. Keeping `both` would silently exclude inbound on existing
    maintainer muscle memory (RESEARCH.md Pattern 3 paragraph "1").

    **Extension 2 — Prepublish-summary inbound step**. Locate the existing
    `Pre-publish check for mailglass_admin` step (lines 101-106) inside the
    `prepublish-summary` job. Insert a new step IMMEDIATELY after it
    (preserve the admin step verbatim):

    ```yaml
    - name: Pre-publish check for mailglass_inbound
      if: ${{ github.event.inputs.package != 'mailglass' && github.event.inputs.package != 'mailglass_admin' }}
      run: |
        echo "## mailglass_inbound" >> "$GITHUB_STEP_SUMMARY"
        set -o pipefail
        mix mailglass.publish.check --package mailglass_inbound 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"
    ```

    The `if:` condition runs when package is `all` or `mailglass_inbound`,
    skipping for explicit `mailglass`-only or `mailglass_admin`-only
    dispatches.

    **A5 sanity check (RESEARCH.md):** the `prepublish-summary` job has a
    Postgres service for core (lines 43-56 area). Per RESEARCH.md A5,
    `mix mailglass.publish.check --package mailglass_inbound` does NOT need
    Postgres. Inbound's publish.check has no live-DB dependency. The new
    step rides the same job runner — no service changes needed.

    **Extension 3 — Update sibling `if:` blocks for `all` enum value**.

    Two existing `if:` blocks need patches so the `all` value behaves
    consistently:

    `publish-core` job `if:` (lines 151-153 area). Current shape excludes
    explicit admin-only dispatch. Add explicit inbound exclusion:

    Before (semantics):
    ```yaml
    if: |
      always() &&
      (
        (github.event_name == 'workflow_dispatch' &&
         github.event.inputs.package != 'mailglass_admin') ||
        (github.event_name == 'release')
      )
    ```

    After:
    ```yaml
    if: |
      always() &&
      (
        (github.event_name == 'workflow_dispatch' &&
         github.event.inputs.package != 'mailglass_admin' &&
         github.event.inputs.package != 'mailglass_inbound') ||
        (github.event_name == 'release')
      )
    ```

    `publish-admin` job `if:` (lines 213-230 area). Current second clause is
    `package != 'mailglass'`. Add explicit inbound exclusion so explicit
    inbound-only dispatch doesn't accidentally publish admin:

    Before (the second OR clause):
    ```yaml
    (github.event_name == 'workflow_dispatch' &&
     github.event.inputs.package != 'mailglass' &&
     needs.publish-core.result == 'success') ||
    ```

    After:
    ```yaml
    (github.event_name == 'workflow_dispatch' &&
     github.event.inputs.package != 'mailglass' &&
     github.event.inputs.package != 'mailglass_inbound' &&
     needs.publish-core.result == 'success') ||
    ```

    Preserve the first OR clause (`package == 'mailglass_admin' && needs
    skipped`) and the third OR clause (`release event`) verbatim.

    Search for the literal string `'both'` in the file's `if:` blocks. The
    current code has no `'both'` literal in `if:` conditionals (only in
    defaults and comments) — confirm via grep before continuing. If any
    `'both'` literal IS found in an `if:` block, replace with `'all'`.

    **Extension 4 — Clone `publish-admin` job → `publish-inbound`**. Insert
    immediately after `publish-admin`'s closing brace (after line 282).

    Use the line-by-line clone table in PATTERNS.md (lines 446-457) as the
    transformation map. The full new job body:

    ```yaml
    publish-inbound:
      runs-on: ubuntu-latest
      needs: [publish-core]
      if: |
        always() &&
        (
          (github.event_name == 'workflow_dispatch' &&
           github.event.inputs.package == 'mailglass_inbound' &&
           needs.publish-core.result == 'skipped') ||
          (github.event_name == 'workflow_dispatch' &&
           github.event.inputs.package != 'mailglass' &&
           github.event.inputs.package != 'mailglass_admin' &&
           needs.publish-core.result == 'success') ||
          (github.event_name == 'release' &&
           needs.publish-core.result == 'success')
        )
      environment: hex-publish
      steps:
        - name: Checkout
          uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
          with:
            ref: ${{ github.event.inputs.tag || github.event.release.tag_name }}
        - name: Set up OTP + Elixir
          uses: erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93  # v1.24.0
          with:
            elixir-version: "1.18"
            otp-version: "27"
        - name: Cache deps
          uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5
          with:
            path: deps
            key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
        - name: Install deps (MIX_PUBLISH=true → resolves :mailglass as Hex package)
          working-directory: mailglass_inbound
          env:
            MIX_PUBLISH: "true"
          run: mix deps.get
        - name: Read mailglass_inbound version from mix.exs
          id: version
          working-directory: mailglass_inbound
          run: |
            VERSION=$(grep -E '@version "[0-9]' mix.exs | head -1 | sed -E 's/.*"([^"]+)".*/\1/')
            echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
        - name: Skip if version already on Hex
          id: idempotency
          run: |
            VERSION="${{ steps.version.outputs.version }}"
            if mix hex.info mailglass_inbound "${VERSION}" 2>/dev/null | grep -q "Released:"; then
              echo "Version ${VERSION} of mailglass_inbound already on Hex — skipping publish (idempotency guard)."
              echo "skip=true" >> "$GITHUB_OUTPUT"
            fi
        - name: Publish mailglass_inbound to Hex.pm
          if: steps.idempotency.outputs.skip != 'true'
          working-directory: mailglass_inbound
          run: |
            if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
              mix hex.publish --dry-run
            else
              mix hex.publish --yes
            fi
          env:
            HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
            MIX_PUBLISH: "true"
    ```

    **Critical — preserve every pinned action SHA verbatim** from
    publish-admin. Do NOT bump action versions during this ceremony per
    Phase 13 D-13-21..D-13-26 deferred posture.

    Sequencing guarantee: `needs: [publish-core]`. Inbound depends on core
    1.0.0 being indexed (mailglass_inbound/mix.exs:60 pins
    `{:mailglass, "== 1.0.0"}`). publish-admin and publish-inbound are
    parallel-safe — no inter-dependency. Per CONTEXT.md "Claude's Discretion"
    line 113 + RESEARCH.md Open Question 4 recommendation: **parallel** for
    faster feedback inside the 60-minute window.

    Single file edit. End-state must be valid YAML and pass actionlint.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; actionlint .github/workflows/publish-hex.yml &amp;&amp; grep -c '^  publish-inbound:' .github/workflows/publish-hex.yml | grep -q '^1$' &amp;&amp; grep -c '    - mailglass_inbound$' .github/workflows/publish-hex.yml | grep -q '^1$' &amp;&amp; grep -c '    - all$' .github/workflows/publish-hex.yml | grep -q '^1$' &amp;&amp; grep -c 'Pre-publish check for mailglass_inbound' .github/workflows/publish-hex.yml | grep -q '^1$' &amp;&amp; grep -c 'mix mailglass.publish.check --package mailglass_inbound' .github/workflows/publish-hex.yml | grep -q '^1$' &amp;&amp; grep -c 'working-directory: mailglass_inbound' .github/workflows/publish-hex.yml | grep -qE '^[2-9]$'</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/publish-hex.yml` exits 0
    - `grep -c '^  publish-inbound:' .github/workflows/publish-hex.yml` returns `1` (new job declared at correct indent)
    - `grep -cE '^[[:space:]]+- mailglass_inbound$' .github/workflows/publish-hex.yml` returns `1` (input enum option added)
    - `grep -cE '^[[:space:]]+- all$' .github/workflows/publish-hex.yml` returns `1` (input enum option added)
    - `grep -cE '^[[:space:]]+- both$' .github/workflows/publish-hex.yml` returns `0` (legacy `both` enum option removed)
    - `grep -c 'default: all' .github/workflows/publish-hex.yml` returns `1` (default updated)
    - `grep -c 'default: both' .github/workflows/publish-hex.yml` returns `0` (legacy default removed)
    - `grep -c 'Pre-publish check for mailglass_inbound' .github/workflows/publish-hex.yml` returns `1`
    - `grep -c 'mix mailglass.publish.check --package mailglass_inbound' .github/workflows/publish-hex.yml` returns `1`
    - `grep -c 'mix hex.info mailglass_inbound' .github/workflows/publish-hex.yml` returns `1` (idempotency guard)
    - `grep -c 'working-directory: mailglass_inbound' .github/workflows/publish-hex.yml` returns at least `2` (deps install + version read + publish — minimum 2 occurrences in new job; 4 expected if all working-directory lines present)
    - `grep -c 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd' .github/workflows/publish-hex.yml` returns at least `3` (preserved across publish-core, publish-admin, publish-inbound)
    - `grep -c 'erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93' .github/workflows/publish-hex.yml` returns at least `3` (preserved)
    - `grep -c "package != 'mailglass_inbound'" .github/workflows/publish-hex.yml` returns at least `2` (publish-core + publish-admin `if:` blocks excluding explicit inbound dispatch)
    - `mix mailglass.publish.check --package mailglass_inbound` exits 0 (no regression in publish-check tooling)
  </acceptance_criteria>
  <done>
    publish-hex.yml accepts `package=all` and `package=mailglass_inbound`
    enum values; runs prepublish-summary for inbound; clones publish-admin
    shape into publish-inbound job with surgically-correct working-directory,
    idempotency guard, and `MIX_PUBLISH=true` env. All pinned action SHAs
    preserved. publish-core + publish-admin sibling `if:` blocks updated to
    handle explicit-inbound dispatch correctly. `actionlint` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend post-publish-smoke.yml — version_inbound output, parallel inbound steps in wait-for-index/wait-for-hexdocs/retracted-check, consumer-install adds inbound dep</name>
  <files>.github/workflows/post-publish-smoke.yml</files>
  <read_first>
    - .github/workflows/post-publish-smoke.yml (full file — especially lines 39-135 cron-guard, 137-184 wait-for-index, 186-228 wait-for-hexdocs, 230-326 consumer-install, 328-367 retracted-check)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Pattern 4 lines 551-589, Pitfall 3 lines 755-767 — cron-guard version_inbound rationale)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 480-554 — wait-for-index inbound step recipe + cron-guard extension + consumer-install heredoc edit)
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs (lines 91-106 — confirms inbound rejects installer framing; consumer-install MUST NOT call `mix mailglass.install` for inbound)
  </read_first>
  <action>
    Four extensions to `.github/workflows/post-publish-smoke.yml`. Applied in
    order so `version_inbound` is available before downstream jobs reference
    it.

    **Extension 1 — cron-guard outputs `version_inbound`** (Pitfall 3
    mitigation, RESEARCH.md A6 — recommended Option B: runtime read from
    `mailglass_inbound/mix.exs`).

    Locate the `cron-guard` job's `outputs:` block (lines 35-37 area). Add
    `version_inbound` output:

    Before:
    ```yaml
    outputs:
      should_run: ${{ steps.guard.outputs.should_run }}
      version: ${{ steps.guard.outputs.version }}
    ```

    After:
    ```yaml
    outputs:
      should_run: ${{ steps.guard.outputs.should_run }}
      version: ${{ steps.guard.outputs.version }}
      version_inbound: ${{ steps.guard.outputs.version_inbound }}
    ```

    Inside cron-guard's `steps:` block, add a `Read mailglass_inbound version`
    step BEFORE the existing `guard` step. This step reads
    `mailglass_inbound/mix.exs` from the checked-out tag and writes the
    version to `$GITHUB_OUTPUT`. If the existing cron-guard does NOT already
    `actions/checkout`, add a checkout step pinned to the same SHA used in
    publish-hex.yml (`actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2`).

    Add to the `guard` step's github-script body (or as a new bash step
    immediately after checkout, before `guard`):

    ```yaml
    - name: Read mailglass_inbound version
      id: read-inbound-version
      run: |
        VERSION_INBOUND=$(grep -E '@version "[0-9]' mailglass_inbound/mix.exs | head -1 | sed -E 's/.*"([^"]+)".*/\1/')
        echo "version_inbound=${VERSION_INBOUND}" >> "$GITHUB_OUTPUT"
    ```

    Then in the `guard` step's github-script body (or wherever
    `core.setOutput('version', ...)` is called), add:

    ```javascript
    core.setOutput('version_inbound', '${{ steps.read-inbound-version.outputs.version_inbound }}');
    ```

    OR, simpler, change the `outputs:` block reference from
    `${{ steps.guard.outputs.version_inbound }}` to
    `${{ steps.read-inbound-version.outputs.version_inbound }}` and skip the
    setOutput-from-script roundtrip.

    **Pick whichever pattern is consistent with how the existing
    `version` output is wired** — read the existing cron-guard implementation
    first and mirror exactly.

    **Extension 2 — `wait-for-index` parallel inbound step**. Insert
    immediately after the existing `Wait for mailglass_admin index` step
    (around line 184). Note: `VERSION_INBOUND` env var name distinct from
    `VERSION` because the version line differs from core/admin:

    ```yaml
    - name: Wait for mailglass_inbound index
      env:
        VERSION_INBOUND: ${{ needs.cron-guard.outputs.version_inbound }}
      run: |
        if [ -z "${VERSION_INBOUND}" ]; then
          echo "No inbound version resolved; skipping."
          exit 0
        fi
        DEADLINE=$(( SECONDS + 300 ))
        until mix hex.info mailglass_inbound "${VERSION_INBOUND}" >/dev/null 2>&1; do
          if [ $SECONDS -ge $DEADLINE ]; then
            echo "Smoke aborted: Hex.pm did not index mailglass_inbound ${VERSION_INBOUND} within 5 minutes."
            exit 1
          fi
          echo "Waiting for Hex.pm to index mailglass_inbound ${VERSION_INBOUND}..."
          sleep 30
        done
        echo "Hex.pm indexed mailglass_inbound ${VERSION_INBOUND}."
    ```

    **Extension 3 — `wait-for-hexdocs` parallel inbound step**. Mirror the
    existing admin step (lines 211-228 area). Replace `mix hex.info` with
    `curl -fsI https://hexdocs.pm/mailglass_inbound/${VERSION_INBOUND}/`
    pattern:

    ```yaml
    - name: Wait for mailglass_inbound HexDocs
      env:
        VERSION_INBOUND: ${{ needs.cron-guard.outputs.version_inbound }}
      run: |
        if [ -z "${VERSION_INBOUND}" ]; then
          echo "No inbound version resolved; skipping."
          exit 0
        fi
        DEADLINE=$(( SECONDS + 300 ))
        until curl -fsI "https://hexdocs.pm/mailglass_inbound/${VERSION_INBOUND}/" >/dev/null 2>&1; do
          if [ $SECONDS -ge $DEADLINE ]; then
            echo "Smoke aborted: HexDocs did not publish mailglass_inbound ${VERSION_INBOUND} within 5 minutes."
            exit 1
          fi
          echo "Waiting for HexDocs to publish mailglass_inbound ${VERSION_INBOUND}..."
          sleep 30
        done
        echo "HexDocs published mailglass_inbound ${VERSION_INBOUND}."
    ```

    **Extension 4 — `retracted-check` parallel inbound step**. Mirror the
    existing pattern (lines 356-367 area). Third step for
    `mailglass_inbound`:

    ```yaml
    - name: Check mailglass_inbound not retracted
      env:
        VERSION_INBOUND: ${{ needs.cron-guard.outputs.version_inbound }}
      run: |
        if [ -z "${VERSION_INBOUND}" ]; then
          echo "No inbound version resolved; skipping."
          exit 0
        fi
        if mix hex.info mailglass_inbound "${VERSION_INBOUND}" 2>/dev/null | grep -q "Retired"; then
          echo "Smoke aborted: mailglass_inbound ${VERSION_INBOUND} is retracted."
          exit 1
        fi
        echo "mailglass_inbound ${VERSION_INBOUND} is not retracted."
    ```

    **Extension 5 — `consumer-install` adds inbound dep**. Locate the
    Elixir-script `entries` heredoc (lines 271-284 area) that inlines the
    deps into the sandbox `mix.exs`. Update to include inbound:

    Before:
    ```elixir
    entries = "      {:mailglass, \"== #{version}\"},\n      {:mailglass_admin, \"== #{version}\"},\n"
    ```

    After:
    ```elixir
    entries = "      {:mailglass, \"== #{version}\"},\n      {:mailglass_admin, \"== #{version}\"},\n      {:mailglass_inbound, \"== #{version_inbound}\"},\n"
    ```

    The `version_inbound` variable is read from `cron-guard.outputs.version_inbound`
    at the top of the consumer-install Elixir script (mirror however
    `version` is currently bound — typically `version = System.get_env("VERSION")`
    or similar).

    **Critical — DO NOT add `mix mailglass.install` for inbound.** Inbound's
    docs contract test
    (`mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs:91-106`)
    explicitly rejects installer framing — manual setup only. The smoke for
    inbound is "deps.get + compile-clean", NOT installer-driven. Per
    RESEARCH.md anti-pattern line 709: do NOT use `mix mailglass.install`
    flow on `mailglass_inbound` in sandbox smoke. Per A6: do NOT wire
    `MailglassInbound.Router` into the sandbox endpoint either — the narrow
    gate is "dep added, deps.get clean, compile clean".

    Single file edit. End-state must be valid YAML and pass actionlint.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; actionlint .github/workflows/post-publish-smoke.yml &amp;&amp; grep -c 'version_inbound' .github/workflows/post-publish-smoke.yml | grep -qE '^[5-9]$|^[1-9][0-9]+$' &amp;&amp; grep -c 'Wait for mailglass_inbound index' .github/workflows/post-publish-smoke.yml | grep -q '^1$' &amp;&amp; grep -c 'mailglass_inbound' .github/workflows/post-publish-smoke.yml | grep -qE '^[5-9]$|^[1-9][0-9]+$' &amp;&amp; ! grep -E 'mix mailglass.install.*mailglass_inbound|mailglass_inbound.*mix mailglass.install' .github/workflows/post-publish-smoke.yml</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/post-publish-smoke.yml` exits 0
    - `grep -c 'version_inbound' .github/workflows/post-publish-smoke.yml` returns at least `5` (cron-guard output declaration + 3 downstream jobs reference it + 1 consumer-install reference; minimum 5 occurrences)
    - `grep -c 'Wait for mailglass_inbound index' .github/workflows/post-publish-smoke.yml` returns `1`
    - `grep -c 'Wait for mailglass_inbound HexDocs' .github/workflows/post-publish-smoke.yml` returns `1`
    - `grep -c 'Check mailglass_inbound not retracted' .github/workflows/post-publish-smoke.yml` returns `1`
    - `grep -c 'mailglass_inbound' .github/workflows/post-publish-smoke.yml` returns at least `5` (multiple references across cron-guard + 3 jobs + consumer-install)
    - `grep -E 'mailglass_inbound.*mix mailglass.install' .github/workflows/post-publish-smoke.yml` returns no matches (anti-pattern check — inbound MUST NOT be installer-driven)
    - `grep -c 'cron-guard.outputs.version_inbound' .github/workflows/post-publish-smoke.yml` returns at least `3` (referenced by wait-for-index, wait-for-hexdocs, retracted-check)
    - The consumer-install `entries` heredoc contains `{:mailglass_inbound, \"== #{version_inbound}\"}` (or equivalent format) — verify by inspecting the consumer-install job's Elixir script
  </acceptance_criteria>
  <done>
    post-publish-smoke.yml exposes `version_inbound` from cron-guard
    (Pitfall 3 closed); wait-for-index/wait-for-hexdocs/retracted-check
    each have an inbound parallel step keyed on `VERSION_INBOUND`;
    consumer-install adds the inbound dep without invoking
    `mix mailglass.install` (anti-pattern avoided); actionlint exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update MAINTAINING.md runbook — `package=both` → `package=all`, fan-out narrative, sandbox install snippet</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (lines 26-189, especially 36-43 required-check buckets, 60-75 single-reviewer Environment, 76-94 Retract Decision Tree, 108-189 Release Runbook, 147-149 fan-out narrative + fallback, 154-159 sandbox install snippet)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 558-612 — three specific edits for MAINTAINING.md)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-CONTEXT.md (D-44.5-03 line 65 — runbook updates mandate)
  </read_first>
  <action>
    Three surgical edits to `MAINTAINING.md`. Do NOT restructure. Do NOT
    rewrite the Retract Decision Tree (lines 76-94) or the Required Checks
    section (lines 26-43) — they remain authoritative.

    **Edit 1 — line ~149 — Fallback path `package=both` → `package=all`:**

    Find the current text:

    > **Fallback path:** If the Release Please tag/release exists but
    > `publish-hex` did not fan out, dispatch
    > `.github/workflows/publish-hex.yml` manually (with `package=both` and
    > `dry_run=false`). **Do not dispatch from `main`**. Always use the
    > reviewed release tag (for `0.3.0`: `mailglass-v0.3.0`) so the publish
    > run is pinned to the exact commit Release Please tagged.

    Replace `package=both` with `package=all`. Update the example tag from
    `0.3.0`/`mailglass-v0.3.0` to `1.0.0`/`mailglass-v1.0.0`. Result:

    > **Fallback path:** If the Release Please tag/release exists but
    > `publish-hex` did not fan out, dispatch
    > `.github/workflows/publish-hex.yml` manually (with `package=all` and
    > `dry_run=false`). **Do not dispatch from `main`**. Always use the
    > reviewed release tag (for `1.0.0`: `mailglass-v1.0.0`) so the publish
    > run is pinned to the exact commit Release Please tagged.

    **Edit 2 — lines ~147-148 — Fan-out narrative reflects three packages
    in parallel:**

    Find:

    > - **Package order:** The workflow guarantees `mailglass` (core)
    >   publishes first, then `mailglass_admin` publishes against the newly
    >   live core.
    > - **Idempotency:** Both publish steps check `mix hex.info` first and
    >   skip the publish command if the version is already live, making
    >   the workflow safe to retry.

    Replace with:

    > - **Package order:** The workflow guarantees `mailglass` (core)
    >   publishes first; once core is indexed, `mailglass_admin` and
    >   `mailglass_inbound` publish in parallel against the newly live
    >   core.
    > - **Idempotency:** All three publish steps check `mix hex.info`
    >   first and skip the publish command if the version is already
    >   live, making the workflow safe to retry.

    **Edit 3 — lines ~154-159 — Sandbox install snippet adds inbound dep
    + reflects 1.0.0 cut versions:**

    Find:

    ```bash
    mix archive.install hex phx_new --force
    mix phx.new sandbox --no-ecto --no-mailer --install
    cd sandbox
    # add {:mailglass, "~> 0.3"}, {:mailglass_admin, "~> 0.3"} to deps
    mix deps.get && mix mailglass.install && mix compile --warnings-as-errors
    mix phx.server  # visit http://localhost:4000/dev/mail/
    ```

    Replace with:

    ```bash
    mix archive.install hex phx_new --force
    mix phx.new sandbox --no-ecto --no-mailer --install
    cd sandbox
    # add {:mailglass, "== 1.0.0"}, {:mailglass_admin, "== 1.0.0"}, {:mailglass_inbound, "== 0.1.0"} to deps
    mix deps.get && mix mailglass.install && mix compile --warnings-as-errors
    mix phx.server  # visit http://localhost:4000/dev/mail/
    ```

    Note: `mix mailglass.install` is core+admin scope only; inbound is
    deps.get + compile-clean only (matches Plan 02 Task 2 consumer-install
    posture and inbound's docs_contract_test).

    Three edits total. Surgical text changes only. Preserve all other
    runbook content verbatim. No emojis. Brand voice: clear, exact,
    confident, warm.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; grep -c 'package=all' MAINTAINING.md | grep -q '^1$' &amp;&amp; grep -c 'package=both' MAINTAINING.md | grep -q '^0$' &amp;&amp; grep -c 'mailglass_inbound' MAINTAINING.md | grep -qE '^[2-9]$' &amp;&amp; grep -c 'in parallel against the newly live' MAINTAINING.md | grep -q '^1$' &amp;&amp; grep -c 'mailglass-v1.0.0' MAINTAINING.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'package=all' MAINTAINING.md` returns `1`
    - `grep -c 'package=both' MAINTAINING.md` returns `0` (legacy literal removed)
    - `grep -c 'mailglass_inbound' MAINTAINING.md` returns at least `2` (sandbox install snippet + fan-out narrative)
    - `grep -c 'in parallel against the newly live' MAINTAINING.md` returns `1` (fan-out narrative updated)
    - `grep -c 'mailglass-v1.0.0' MAINTAINING.md` returns `1` (fallback path example tag updated)
    - `grep -c 'All three publish steps check' MAINTAINING.md` returns `1` (idempotency note updated)
    - No emojis introduced; `rg "[\\x{1F300}-\\x{1F9FF}]" MAINTAINING.md` returns no matches
    - `rg "D-\\d{2,3}|LINT-\\d{2}" MAINTAINING.md` returns no NEW matches (planning IDs not leaked into runbook)
    - Retract Decision Tree (lines 76-94) and Required Checks section (lines 26-43) unchanged — verify by `git diff` showing only the three targeted edit ranges modified
  </acceptance_criteria>
  <done>
    MAINTAINING.md reflects the three-package fan-out reality. Fallback
    `package=all` documented. Sandbox install snippet ships the locked
    versions (1.0.0 / 1.0.0 / 0.1.0). Other authoritative sections
    untouched. Brand voice preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workflow_dispatch` input → publish-hex.yml `if:` conditionals | User-supplied `package=` enum value drives which jobs run; misclassification publishes wrong artifact |
| GitHub Environment `hex-publish` reviewer approval → HEX_API_KEY injection | Single chokepoint where secret meets the publish step; breakage publishes from main or skips review |
| Workflow runner → Hex.pm registry | `mix hex.publish` crosses adopter trust boundary; tarball contents become permanent |
| post-publish-smoke.yml runner → sandbox phx.new app → Hex/HexDocs | Smoke verifies the artifact is consumable; failure = revert/retire decision tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44.5-02-01 | Tampering | Workflow YAML correctness drift after publish-inbound clone (mismatched `if:` blocks silently exclude inbound on `package=all`) | mitigate | Task 1 explicitly updates publish-core + publish-admin `if:` blocks AND adds publish-inbound with the four-clause shape from PATTERNS.md. `actionlint` exits 0 (acceptance). The `if:` fanout for `package=all` runs core, then admin and inbound in parallel after core indexes. |
| T-44.5-02-02 | Information Disclosure | HEX_API_KEY exposure if publish-inbound job omits Environment gate (D-44.5-03 decisive factor) | mitigate | Task 1 publish-inbound clone preserves `environment: hex-publish` verbatim (acceptance: grep for environment line). Same Environment gates all three jobs (single approval covers core + admin + inbound). Workflow path is the canonical posture; no manual `mix hex.publish` from maintainer machine. |
| T-44.5-02-03 | Tampering | Pinned action version drift introduced during clone (e.g., bumping `actions/checkout` to v6.x.x latest) | mitigate | Task 1 acceptance: `grep -c 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd'` returns at least `3` (preserved across all 3 jobs). Same for `setup-beam` and `cache` SHAs. Per CLAUDE.md: all third-party Actions pinned to commit SHA; Dependabot watches workflow versions. |
| T-44.5-02-04 | Availability | post-publish-smoke version_inbound not resolved → smoke times out (Pitfall 3) | mitigate | Task 2 extends cron-guard with `version_inbound` output via runtime read of `mailglass_inbound/mix.exs` (Option B — future-proof for inbound 0.2.0+). All three downstream jobs reference `cron-guard.outputs.version_inbound`. Default-empty handling: `if [ -z "${VERSION_INBOUND}" ]` → skip with exit 0, not error. |
| T-44.5-02-05 | Tampering | consumer-install accidentally invokes `mix mailglass.install` for inbound, violating inbound's docs contract | accept (with explicit anti-pattern check) | Task 2 acceptance: `grep -E 'mailglass_inbound.*mix mailglass.install'` returns no matches. Inbound's docs_contract_test:91-106 enforces the manual-setup-only contract; this plan honors it by adding the dep + deps.get + compile-clean only. |
| T-44.5-02-06 | Tampering | `if:` block fanout silently excludes inbound when `package=all` (most likely failure mode of this plan) | mitigate | Task 1 acceptance: `grep -c "package != 'mailglass_inbound'"` returns at least `2` (publish-core + publish-admin both excluding explicit-inbound dispatch). publish-inbound `if:` includes the `'all'`-handling clause via the negative form `package != 'mailglass' && package != 'mailglass_admin'` plus explicit `'mailglass_inbound'` and `release` clauses. Plan 04 dry-run dispatch (Wave 3) exercises all three jobs with `package=all` — final correctness check. |

Block on: high severity. T-44.5-02-01 and T-44.5-02-02 are HIGH (would publish wrong artifact or leak HEX_API_KEY); both have grep-verifiable acceptance criteria + `actionlint` gate.
</threat_model>

<verification>
- `actionlint .github/workflows/publish-hex.yml` exits 0
- `actionlint .github/workflows/post-publish-smoke.yml` exits 0
- `grep` checks confirm publish-inbound job exists with correct `working-directory`, idempotency guard, and `MIX_PUBLISH=true` env
- `grep` checks confirm input enum has `mailglass_inbound` and `all` (default `all`); legacy `both` removed
- `grep` checks confirm publish-core + publish-admin `if:` blocks exclude explicit-inbound dispatch
- `grep` checks confirm pinned action SHAs preserved (checkout, setup-beam, cache appear at least 3× each in publish-hex.yml)
- `grep` checks confirm post-publish-smoke.yml has `version_inbound` output declared and 3 downstream parallel inbound steps
- `grep` checks confirm consumer-install does NOT invoke `mix mailglass.install` for inbound
- `grep` checks confirm MAINTAINING.md updated for `package=all`, three-package fan-out, sandbox install snippet
- `mix mailglass.publish.check --package mailglass_inbound` exits 0 (no regression)
</verification>

<success_criteria>
Phase 44.5 success criterion #3 ("the existing release-rehearsal artifacts
from v1.0 milestone are converted from rehearsal to live; the protected
GitHub Environment publish flow is exercised for real") is structurally
unblocked by this plan. Live publish happens in Plan 04. The
`dry_run=true package=all` rehearsal in Plan 04 cannot run until this plan
ships.

Plan-local success: 3 files modified. publish-hex.yml accepts the new enum
value `mailglass_inbound` and `all`; clones publish-admin shape into
publish-inbound with parallel-safe sequencing. post-publish-smoke.yml exposes
`version_inbound` and runs three parallel inbound smoke steps.
MAINTAINING.md reflects three-package fan-out. `actionlint` exits 0 on both
workflows. CLOSE-06 mechanically further unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-02-SUMMARY.md`
documenting:
- Files modified with line counts and the exact insertion points
- `actionlint` output for both workflow YAMLs
- `mix mailglass.publish.check --package mailglass_inbound` output (last 20 lines)
- `grep` verification output (actual command results) confirming all `must_haves` truths
- Status of T-44.5-02-01 through T-44.5-02-06 threats (mitigated/verified)
- Proposed Conventional Commit message:

  ```
  feat(release-engineering): add publish-inbound job + post-publish-smoke inbound parity

  Extends the canonical Hex publish pipeline to handle mailglass_inbound
  alongside mailglass + mailglass_admin per D-44.5-03:

  - publish-hex.yml: input enum gains mailglass_inbound + all (default
    all; legacy both removed); prepublish-summary runs publish.check for
    inbound; new publish-inbound job clones publish-admin with parallel
    sequencing (needs: [publish-core]); publish-core + publish-admin if:
    blocks updated to exclude explicit-inbound dispatch.
  - post-publish-smoke.yml: cron-guard exposes version_inbound (runtime
    read of mailglass_inbound/mix.exs); wait-for-index, wait-for-hexdocs,
    retracted-check each gain a parallel inbound step keyed on
    VERSION_INBOUND; consumer-install adds {:mailglass_inbound, "== ..."}
    to sandbox deps without invoking mix mailglass.install (inbound's
    docs contract enforces manual setup only).
  - MAINTAINING.md: package=both → package=all; fan-out narrative
    reflects core, then admin and inbound in parallel; sandbox install
    snippet ships locked 1.0.0 / 1.0.0 / 0.1.0 versions.

  All pinned action SHAs preserved. actionlint exits 0 on both workflows.
  ```

- Pointer to Plan 03 (Commit C — curated CHANGELOG + upgrade-from-0.x.md) as next step
</output>
