---
phase: 044.5-v1-0-1-1-release-ceremony
plan: 05
type: execute
wave: 4
depends_on: [044.5-01, 044.5-02, 044.5-03, 044.5-04]
files_modified:
  - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CLOSE-06]
requirements_addressed: [CLOSE-06]
decisions_implemented: [D-44.5-05]

must_haves:
  truths:
    - "`gh auth status` confirms admin scope (or `gh auth refresh -s repo` was run) before capture"
    - "`gh api repos/szTheory/mailglass/branches/main/protection` returns 200; JSON captured to `044.5-BRANCH-PROTECTION-EVIDENCE.md`"
    - "Required-bucket checklist documents which of the 4 expected buckets are present in `required_status_checks.contexts` and which are bundled inside `Support Contract Core` via `verify.stability_contract`"
    - "Drift between MAINTAINING.md:36-43 expected buckets and live state is documented inline (or 'No drift detected.')"
    - "`.planning/MILESTONES.md` v1.0 entry appends a 'Live publish: 2026-05-XX' bullet with pointer to `044.5-RELEASE-RECORD.md`"
    - "`.planning/PROJECT.md` 'Current State' section reflects live Hex versions (mailglass 1.0.0 / mailglass_admin 1.0.0 / mailglass_inbound 0.1.0) and points to the release record"
    - "`.planning/STATE.md` Current Position updated to reflect Phase 44.5 complete; Phase 45 unblocked"
    - "CLOSE-06 marked closed in `.planning/REQUIREMENTS.md` (or its source-of-truth equivalent) with pointer to this phase's evidence"
    - "Phase 51 CLOSE-02 follow-up note explicit: branch-protection automation (gh api script + drift workflow) lands later"
  artifacts:
    - path: ".planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md"
      provides: "First evidence-capture file in the project; gh api JSON snapshot at v1.0.0 SHA timestamp"
      contains: "required_status_checks"
    - path: ".planning/MILESTONES.md"
      provides: "v1.0 entry appended with live publish date + pointer to release record"
      contains: "Live publish:"
    - path: ".planning/PROJECT.md"
      provides: "Current State updated to reflect live publish + link to release record"
      contains: "1.0.0"
    - path: ".planning/STATE.md"
      provides: "Phase 44.5 marked complete; Phase 45 unblocked"
      contains: "44.5"
  key_links:
    - from: "gh api repos/szTheory/mailglass/branches/main/protection"
      to: "044.5-BRANCH-PROTECTION-EVIDENCE.md"
      via: "JSON capture + checklist of required buckets"
      pattern: "required_status_checks"
    - from: "044.5-BRANCH-PROTECTION-EVIDENCE.md"
      to: "Phase 51 CLOSE-02 (automation follow-up)"
      via: "Closeout-notes section explicit forward-pointer"
      pattern: "Phase 51 CLOSE-02"
    - from: ".planning/STATE.md Current Position"
      to: "Phase 45 (Inbound Telemetry + Idempotency Foundation)"
      via: "release-cadence rule unblocks v1.2 implementation"
      pattern: "Phase 45"
---

<objective>
Wave 4 — CLOSE-06 closeout. Capture branch-protection evidence at the
v1.0.0 SHA timestamp (D-44.5-05). Update MILESTONES.md, PROJECT.md, and
STATE.md to reflect the live publish. Mark CLOSE-06 closed.

Per D-44.5-05: branch-protection verification happens AFTER live publish
(not before) because publish-hex.yml's `gate-ci-green` already respects
branch protection at the SHA level — pre-publish external verify is
redundant.

This plan is also the only place the project state transitions from
"v1.2 planning, blocked on Phase 44.5" → "Phase 44.5 complete, Phase 45
unblocked, v1.2 implementation can begin."

Phase 51 CLOSE-02 follows up with automation (`gh api` desired-state script
+ daily reassert via `branch-protection-drift.yml`). Phase 44.5 captures
the snapshot for the v1.0.0 SHA timestamp.

Output:
- 4 files modified (1 new evidence file + 3 state updates; 1 commit, 1 PR)
- CLOSE-06 closed
- Project ready to begin Phase 45 (v1.2 implementation)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-CONTEXT.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-VALIDATION.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-04-SUMMARY.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md
@CLAUDE.md
@MAINTAINING.md
@scripts/setup_branch_protection.sh

<interfaces>
<!-- Critical contracts for this plan. -->

Expected required-check buckets (per MAINTAINING.md:36-43 + D-44.5-05):
1. `Support Contract Core (Elixir 1.18 / OTP 27)` — verified runtime in ci.yml:108
2. `Support Contract Admin (Elixir 1.18 / OTP 27)` — verified runtime in ci.yml:363
3. `Compile No Optional Deps (Elixir 1.18 / OTP 27)` — verified runtime in ci.yml:80
4. `mailglass_inbound docs contract` — referenced in MAINTAINING.md:42 but the canonical CI job name is unclear; per A3 assumption in RESEARCH.md, may be bundled inside `Support Contract Core` via `verify.stability_contract`

Open question 3 from RESEARCH.md (line 1193-1195) is RESOLVED here by inspection: capture actual `gh api` JSON; if 4th bucket is bundled inside core, document the discrepancy as drift to be reconciled in Phase 51.

Evidence capture command (RESEARCH.md Pattern 7 lines 660-688):
```bash
gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/branches/main/protection > /tmp/branch-protection.json

{
  echo "# Phase 44.5 — Branch Protection Evidence"
  echo
  echo "Capture date: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
  echo "Phase: 044.5-v1-0-1-1-release-ceremony (CLOSE-06 closeout)"
  echo "Capture command:"
  echo "  gh api -H \"Accept: application/vnd.github+json\" repos/szTheory/mailglass/branches/main/protection"
  echo
  echo "## Required status checks (\`required_status_checks.contexts\`)"
  echo
  echo "\`\`\`json"
  jq '.required_status_checks' /tmp/branch-protection.json
  echo "\`\`\`"
  echo
  echo "## Required-bucket checklist (per MAINTAINING.md:36-43, D-44.5-05)"
  echo
  echo "<filled by manual review of captured JSON>"
  echo
  echo "## Drift documented"
  echo
  echo "<filled by manual review>"
  echo
  echo "## Full protection state"
  echo
  echo "\`\`\`json"
  cat /tmp/branch-protection.json
  echo "\`\`\`"
  echo
  echo "## Closeout notes"
  echo
  echo "- Captures the LIVE GitHub branch-protection state at Phase 44.5 closeout."
  echo "- Supersedes the accepted-external-debt posture documented in 38-03-BRANCH-PROTECTION-NOTE.md."
  echo "- Phase 51 CLOSE-02 will follow up with automated reassert via branch-protection-drift.yml + scripts/setup_branch_protection.sh updates."
  echo "- Required PAT scope: \`Administration: Read\` (typical for repo owners via \`gh auth status\`); if \`gh api\` returns 403, run \`gh auth refresh -s repo\` first."
} > .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md

rm /tmp/branch-protection.json
```

`.planning/MILESTONES.md` v1.0 entry append (per PATTERNS.md lines 802-819 + 826):
```markdown
- **Live publish: 2026-05-XX** — `mailglass` 1.0.0, `mailglass_admin` 1.0.0, `mailglass_inbound` 0.1.0 published to Hex.pm via Phase 44.5 ceremony. See `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md`.
```

`.planning/PROJECT.md` "Current State" updates (per PATTERNS.md lines 826-846 + RESEARCH.md):
- Replace "Current package versions in repo: ... 0.3.2 ..." with "Current package versions on Hex: mailglass 1.0.0 / mailglass_admin 1.0.0 / mailglass_inbound 0.1.0 (live as of 2026-05-XX via Phase 44.5)"
- Replace "Release posture: repo proof and rehearsal artifacts are complete; the live v1.0 publish still follows the Phase 38 checklist..." with "Release posture: live publish complete. Phase 44.5 ceremony record at .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md. Branch-protection evidence at 044.5-BRANCH-PROTECTION-EVIDENCE.md."

`.planning/STATE.md` Current Position update:
- Phase: from "44.5 (planning — not started)" → "44.5 complete; Phase 45 next"
- Plan: from `—` → "5/5 plans complete"
- Status: short note that v1.0/1.1 release ceremony shipped; Phase 45 (Inbound Telemetry + Idempotency Foundation) is the next phase per ROADMAP

`.planning/REQUIREMENTS.md` CLOSE-06 (line 113):
- Change `- [ ] **CLOSE-06**:` to `- [x] **CLOSE-06**:` (mark closed)
- Append a closure note inline or in a "Closed Requirements" section: `(closed 2026-05-XX via Phase 44.5; see 044.5-RELEASE-RECORD.md and 044.5-BRANCH-PROTECTION-EVIDENCE.md)`

Phase 51 CLOSE-02 forward-pointer is mandatory per D-44.5-05 — the
branch-protection automation work is explicitly OUT of Phase 44.5 scope.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight `gh auth status` admin scope check</name>
  <what-built>
    Capturing `gh api repos/szTheory/mailglass/branches/main/protection`
    requires admin PAT permissions (`Administration: Read`). Local `gh` CLI
    auth typically has this for repo owners (szTheory is repo owner per
    MAINTAINING.md:60-65), but PAT scopes can drift if a recent
    `gh auth login` was scoped narrower.

    A 403 response from the `gh api` call would mean the executor cannot
    capture evidence and the user must run `gh auth refresh -s repo`
    before continuing.
  </what-built>
  <how-to-verify>
    Run:

    ```bash
    gh auth status
    # Expect output like:
    #   Logged in to github.com account szTheory (...)
    #   Active account: true
    #   Git operations protocol: ...
    #   Token: gho_*****
    #   Token scopes: 'admin:public_key', 'gist', 'read:org', 'repo', 'workflow'
    ```

    The token MUST have the `repo` scope at minimum. If `repo` is missing
    OR if `gh auth status` reports "not logged in", run:

    ```bash
    gh auth refresh -s repo
    ```

    Then re-run `gh auth status` to confirm.

    Test the actual API call BEFORE the evidence capture to fail fast:

    ```bash
    gh api repos/szTheory/mailglass/branches/main/protection 2>&1 | head -20
    # Expect: a JSON response starting with `{`
    # If you see "HTTP 403" or "Resource not accessible by integration",
    # auth is insufficient — refresh PAT scope before continuing.
    ```

    Acceptance: `gh api repos/szTheory/mailglass/branches/main/protection`
    exits 0 and returns JSON.
  </how-to-verify>
  <resume-signal>Type "auth-ok" if `gh api .../branches/main/protection` returns JSON. Type "auth-failed" + describe error otherwise.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture branch-protection evidence to 044.5-BRANCH-PROTECTION-EVIDENCE.md</name>
  <files>.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Pattern 7 lines 651-701 — full evidence capture recipe)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 1070-1142 — target shape for the new file; this is a NEW PATTERN with no exact analog in the repo)
    - scripts/setup_branch_protection.sh (lines 36-77 — desired-state shape for context)
    - MAINTAINING.md (lines 36-43 — required-check buckets list)
  </read_first>
  <action>
    Create the new file
    `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md`
    by capturing live `gh api` output and adding the required-bucket
    checklist + drift narrative.

    **Step 1 — Capture raw JSON:**
    ```bash
    CAPTURE_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    TMPFILE=$(mktemp)
    gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/branches/main/protection > "${TMPFILE}"
    ```

    **Step 2 — Inspect captured contexts to fill the checklist:**
    ```bash
    jq -r '.required_status_checks.contexts[]' "${TMPFILE}"
    ```

    For each of the four expected buckets (per MAINTAINING.md:36-43), check
    if the captured contexts list contains an exact match or a fuzzy
    match. Fill the checklist in the evidence file accordingly.

    **Step 3 — Compose the evidence file** by writing the following
    structure (substitute captured values):

    ```markdown
    # Phase 44.5 — Branch Protection Evidence

    Capture date: <CAPTURE_DATE from step 1>
    Phase: 044.5-v1-0-1-1-release-ceremony (CLOSE-06 closeout)
    Capture command:
      gh api -H "Accept: application/vnd.github+json" repos/szTheory/mailglass/branches/main/protection

    ## Required status checks (`required_status_checks.contexts`)

    ```json
    <jq '.required_status_checks' from captured JSON>
    ```

    ## Required-bucket checklist (per MAINTAINING.md:36-43, D-44.5-05)

    - [<x or space>] `Support Contract Core (Elixir 1.18 / OTP 27)` ∈ contexts
    - [<x or space>] `Support Contract Admin (Elixir 1.18 / OTP 27)` ∈ contexts
    - [<x or space>] `Compile No Optional Deps (Elixir 1.18 / OTP 27)` ∈ contexts
    - [<x or space>] `mailglass_inbound docs contract` ∈ contexts (or documented as "rides inside Support Contract Core via verify.stability_contract")

    ## Drift documented

    <Inline narrative describing any bucket missing or named differently than expected. Examples:
    - "No drift detected. All 4 buckets present in contexts."
    - "Bucket 4 (`mailglass_inbound docs contract`) not present as a standalone context. Per ci.yml audit, the inbound docs contract test runs as part of `verify.stability_contract` invoked by `Support Contract Core`. MAINTAINING.md:42's framing is aspirational; Phase 51 CLOSE-02 will reconcile by either splitting out the bucket or updating MAINTAINING.md to reflect the bundle."
    - "Bucket 1 (`Support Contract Core`) is named `Support Contract Core (Elixir 1.18 / OTP 27)` in contexts (matches expected name verbatim)."
    >

    ## Full protection state

    ```json
    <full captured JSON, jq-formatted>
    ```

    ## Closeout notes

    - Captures the LIVE GitHub branch-protection state at Phase 44.5 closeout.
    - Branch-protection rule confirmed externally; success criterion #5 of Phase 44.5 fulfilled.
    - Supersedes the accepted-external-debt posture documented in `.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-BRANCH-PROTECTION-NOTE.md`.
    - **Phase 51 CLOSE-02 follow-up:** automation reassert via `branch-protection-drift.yml` and `scripts/setup_branch_protection.sh` updates. Phase 44.5 captures the snapshot for the v1.0.0 SHA timestamp; Phase 51 wires the daily reassert.
    - Required PAT scope: `Administration: Read` (typical for repo owners via `gh auth status`); if `gh api` returns 403, run `gh auth refresh -s repo` first.
    - Single-reviewer Environment posture: `hex-publish` Environment has only `szTheory` as approver per MAINTAINING.md:60-65; no `prevent_self_review` per GitHub Environment semantics; honestly recorded in `044.5-RELEASE-RECORD.md` (companion file).
    ```

    **Step 4 — Clean up temp file:**
    ```bash
    rm "${TMPFILE}"
    ```

    **Substitution discipline:**
    - `<CAPTURE_DATE>` = ISO 8601 UTC from step 1
    - The "Required status checks" code block contains
      `jq '.required_status_checks' <captured>` output (typically 30-50
      lines including the contexts array)
    - The "Full protection state" code block contains the full `gh api`
      response (~100-200 lines)
    - The required-bucket checklist uses `[x]` for each bucket present,
      `[ ]` for missing
    - The "Drift documented" section is human-written narrative based on
      what the actual captured contexts contain; it MUST address each of
      the four expected buckets (present-with-exact-name / present-with-different-name /
      missing-but-bundled / missing-and-genuinely-absent)

    Single new file. Markdown.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; test -f .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md &amp;&amp; head -1 .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md | grep -q '^# Phase 44.5 — Branch Protection Evidence' &amp;&amp; grep -c 'required_status_checks' .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md | grep -qE '^[1-9]$' &amp;&amp; grep -c 'Phase 51 CLOSE-02' .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md | grep -qE '^[1-9]$' &amp;&amp; grep -c 'Capture date:' .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md | grep -q '^1$' &amp;&amp; grep -c 'Required-bucket checklist' .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md` exits 0
    - First line: `# Phase 44.5 — Branch Protection Evidence`
    - `grep -c 'Capture date: 2026-' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns `1` (with substituted ISO 8601 timestamp)
    - `grep -c 'required_status_checks' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns at least `1` (JSON code block + checklist label)
    - `grep -c 'Phase 51 CLOSE-02' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns at least `1` (forward-pointer to automation follow-up)
    - `grep -c 'Required-bucket checklist' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns `1`
    - `grep -c 'Drift documented' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns `1`
    - `grep -c 'Full protection state' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns `1`
    - `grep -c 'Closeout notes' 044.5-BRANCH-PROTECTION-EVIDENCE.md` returns `1`
    - At least one of `[x]` or `[ ]` checkbox markers present (the checklist filled with state)
    - At least 50 total lines (full JSON + narrative): `wc -l 044.5-BRANCH-PROTECTION-EVIDENCE.md | awk '{print $1}' | xargs -I{} test {} -ge 50`
    - JSON code blocks parse: `awk '/```json/,/```$/' 044.5-BRANCH-PROTECTION-EVIDENCE.md | grep -v '^```' | jq . > /dev/null` exits 0
  </acceptance_criteria>
  <done>
    Evidence file exists with captured `gh api` JSON, required-bucket
    checklist, drift narrative, and Phase 51 CLOSE-02 forward-pointer.
    Branch-protection state at the v1.0.0 SHA timestamp is permanently
    recorded.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update MILESTONES.md, PROJECT.md, STATE.md, and REQUIREMENTS.md to reflect live publish + close CLOSE-06</name>
  <files>
    .planning/MILESTONES.md,
    .planning/PROJECT.md,
    .planning/STATE.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/MILESTONES.md (lines 64-74 — current v1.0 entry)
    - .planning/PROJECT.md (lines 7-22 — Current State section)
    - .planning/STATE.md (lines 25-31 — Current Position section)
    - .planning/REQUIREMENTS.md (line 113 — CLOSE-06 entry)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 797-845 — exact edits for MILESTONES + PROJECT)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md (created in Plan 04 — pull the live publish date from here)
  </read_first>
  <action>
    Surgical updates across four planning artifacts. Use the actual live
    publish date from `044.5-RELEASE-RECORD.md` (replace `2026-05-XX`
    placeholders with the captured `Approval timestamp` date portion).

    **Edit 1 — `.planning/MILESTONES.md` v1.0 entry:**

    Locate the v1.0 Stability Lock entry (around lines 64-74). Append a new
    bullet at the END of the existing `**Key accomplishments:**` list:

    Before:
    ```markdown
    **Key accomplishments:**

    - Canonical core/admin stability inventories with explicit stable / internal / sibling-package-only classification, backed by compiled-doc and docs-surface verification.
    - ...
    - Accepted closeout debt: partial Nyquist bookkeeping for Phase 35, non-blocking boundary warnings in support-contract lanes, manual GitHub branch-protection verification.
    ```

    After:
    ```markdown
    **Key accomplishments:**

    - Canonical core/admin stability inventories with explicit stable / internal / sibling-package-only classification, backed by compiled-doc and docs-surface verification.
    - ...
    - Accepted closeout debt: partial Nyquist bookkeeping for Phase 35, non-blocking boundary warnings in support-contract lanes, manual GitHub branch-protection verification.
    - **Live publish: <YYYY-MM-DD>** — `mailglass` 1.0.0, `mailglass_admin` 1.0.0, `mailglass_inbound` 0.1.0 published to Hex.pm via Phase 44.5 ceremony. See `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md`.
    ```

    Surgical append. Do NOT restructure existing entries. Mirror the
    bullet-style of existing `Key accomplishments`.

    **Edit 2 — `.planning/PROJECT.md` "Current State":**

    Locate the "Current State" section (around lines 7-22). Update the
    package-versions and release-posture lines:

    Before (lines 13-15 area):
    ```markdown
    - Planning milestone closed: 6 phases (39-44), 17 plans (12 product + 5 audit-gap closure), all complete
    - Current package versions in repo: `mailglass` 0.3.2 / `mailglass_admin` 0.3.2 / `mailglass_inbound` 0.1.0 (initial)
    - v1.1 milestone audit re-run on 2026-05-07 with `status: passed` after Phase 43 + 44 closeout
    - Release posture: repo proof and rehearsal artifacts are complete; the live `v1.0` publish still follows the Phase 38 checklist and external closeout steps
    ```

    After:
    ```markdown
    - Planning milestone closed: 6 phases (39-44), 17 plans (12 product + 5 audit-gap closure), all complete
    - **Current package versions on Hex: `mailglass` 1.0.0 / `mailglass_admin` 1.0.0 / `mailglass_inbound` 0.1.0** (live as of <YYYY-MM-DD> via Phase 44.5)
    - v1.1 milestone audit re-run on 2026-05-07 with `status: passed` after Phase 43 + 44 closeout
    - Release posture: live publish complete. Phase 44.5 ceremony record at `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md`. Branch-protection evidence at `044.5-BRANCH-PROTECTION-EVIDENCE.md`.
    ```

    Preserve "What This Is", "Codebase characteristics", "Open issues / debt"
    sections unchanged.

    **Edit 3 — `.planning/STATE.md` Current Position:**

    Locate the "Current Position" section (around lines 25-31). Update:

    Before:
    ```markdown
    Phase: 44.5 (planning — not started) — **release ceremony BLOCKING Phase 45 implementation**
    Plan: —
    Status: Roadmap drafted (2026-05-07); v1.0/1.1 release ceremony inserted as Phase 44.5 to ship 4 milestones of unreleased Hex work before v1.2 implementation begins
    Last activity: 2026-05-07 — Inserted Phase 44.5 (v1.0/1.1 release) and Phase 50.5 (v1.2 release) into roadmap; CLOSE-06 reassigned from Phase 51 → Phase 44.5
    ```

    After:
    ```markdown
    Phase: 45 (next) — Phase 44.5 (v1.0/1.1 release ceremony) complete; v1.2 implementation unblocked
    Plan: —
    Status: Live publish complete (mailglass 1.0.0 / mailglass_admin 1.0.0 / mailglass_inbound 0.1.0). CLOSE-06 closed via Phase 44.5. Release-cadence rule observed: each milestone closes with a release ceremony before the next milestone implementation begins.
    Last activity: <YYYY-MM-DD> — Phase 44.5 shipped v1.0/1.1 release; CLOSE-06 closed; branch-protection evidence captured (Phase 51 CLOSE-02 will follow up with automation).
    ```

    Update the `## v1.2 Phase Plan` table row for Phase 44.5 from
    `0/TBD | Not started — BLOCKING Phase 45` to `5/5 | Complete — shipped <YYYY-MM-DD>`.

    Update the front-matter `progress` block:
    - `completed_phases`: increment by 1 (44.5 now complete)
    - `completed_plans`: increment by 5 (Plans 01-05 of Phase 44.5)
    - `percent`: recalculate

    Update `last_updated` to current ISO 8601 UTC timestamp.

    **Edit 4 — `.planning/REQUIREMENTS.md` close CLOSE-06:**

    Locate line 113:

    Before:
    ```markdown
    - [ ] **CLOSE-06**: v1.0 live Hex publish closeout is coordinated (tarball published, branch-protection rule confirmed externally, MILESTONES.md updated)
    ```

    After:
    ```markdown
    - [x] **CLOSE-06**: v1.0 live Hex publish closeout coordinated (closed <YYYY-MM-DD> via Phase 44.5; see `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md` and `044.5-BRANCH-PROTECTION-EVIDENCE.md`)
    ```

    Use `[x]` (lowercase x in brackets) to mark closed. Preserve any
    surrounding closeout-debt section structure.

    **Substitution:**
    - Replace `<YYYY-MM-DD>` with the actual live publish date from the
      `044.5-RELEASE-RECORD.md` Approval timestamp (date portion only,
      not the full ISO 8601 timestamp with time).
    - Use the same date across all four edits (consistency).

    Four file edits total. Surgical text changes only. Brand voice
    preserved. No emojis.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; grep -c 'Live publish: 2026-' .planning/MILESTONES.md | grep -q '^1$' &amp;&amp; grep -c '044.5-RELEASE-RECORD.md' .planning/MILESTONES.md | grep -q '^1$' &amp;&amp; grep -c 'Current package versions on Hex' .planning/PROJECT.md | grep -q '^1$' &amp;&amp; grep -c 'mailglass_inbound. 0.1.0' .planning/PROJECT.md | grep -qE '^[1-9]$' &amp;&amp; grep -c '^- \[x\] \*\*CLOSE-06\*\*' .planning/REQUIREMENTS.md | grep -q '^1$' &amp;&amp; grep -c 'Phase: 45 (next)' .planning/STATE.md | grep -q '^1$' &amp;&amp; grep -c '5/5' .planning/STATE.md | grep -qE '^[1-9]$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Live publish: 2026-' .planning/MILESTONES.md` returns `1` (new bullet appended with substituted date)
    - `grep -c '044.5-RELEASE-RECORD.md' .planning/MILESTONES.md` returns at least `1` (pointer present)
    - `.planning/MILESTONES.md` v1.0 section structure preserved: existing key-accomplishment bullets present unchanged
    - `grep -c 'Current package versions on Hex' .planning/PROJECT.md` returns `1` (line updated from "in repo" to "on Hex")
    - `grep -c 'mailglass.* 1.0.0' .planning/PROJECT.md` returns at least `1` (mailglass version updated)
    - `grep -c '044.5-BRANCH-PROTECTION-EVIDENCE.md' .planning/PROJECT.md` returns at least `1` (pointer present)
    - `grep -c '^- \[x\] \*\*CLOSE-06\*\*' .planning/REQUIREMENTS.md` returns `1` (CLOSE-06 marked closed)
    - `grep -c '^- \[ \] \*\*CLOSE-06\*\*' .planning/REQUIREMENTS.md` returns `0` (open CLOSE-06 line removed)
    - `grep -c 'Phase: 45 (next)' .planning/STATE.md` returns `1` (Current Position updated)
    - `grep -c '5/5.*Complete' .planning/STATE.md` returns at least `1` (table updated)
    - `.planning/STATE.md` front-matter `last_updated` is a valid ISO 8601 timestamp newer than the previous value
    - No emojis introduced anywhere: `rg '[\\x{1F300}-\\x{1F9FF}\\x{2600}-\\x{27BF}]' .planning/MILESTONES.md .planning/PROJECT.md .planning/STATE.md .planning/REQUIREMENTS.md` returns no NEW matches
    - No D-XX or LINT-XX leakage: `rg "D-\\d{2,3}|LINT-\\d{2}" .planning/PROJECT.md` returns no NEW matches in the updated lines (existing references like D-09, D-13, D-22 in PROJECT.md are pre-existing decision pointers and may remain — `git diff` shows only the targeted edit-range changed)
  </acceptance_criteria>
  <done>
    All four planning artifacts reflect the live publish reality.
    MILESTONES.md v1.0 entry adds the live-publish bullet. PROJECT.md
    Current State shows live Hex versions + release record pointer.
    STATE.md Current Position transitions to "Phase 45 next; Phase 44.5
    complete". REQUIREMENTS.md marks CLOSE-06 closed with closure pointer.
    Project state ready for Phase 45 implementation.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final repo-truth lane verification + propose closeout commit message</name>
  <files></files>
  <read_first>
    - mix.exs (lines 252-258 — verify.stability_contract alias body)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-VALIDATION.md
  </read_first>
  <action>
    Final pass: confirm the repo-truth lane stays green after the four
    state updates, propose the closeout Conventional Commit message for
    user approval.

    **Validation runs (each must exit 0):**

    1. `mix mailglass.docs.check` — Tier 1 docs leak detector (no
       `D-XX`/`LINT-XX` leaked into the updated planning artifacts; not
       expected to leak since planning artifacts are not adopter-facing,
       but `mailglass.docs.check`'s scope confirms this).

    2. `mix verify.stability_contract` — full repo-truth lane (or
       documented Postgres-skip subset).

    3. `mix hex.info mailglass 1.0.0`, `mix hex.info mailglass_admin 1.0.0`,
       `mix hex.info mailglass_inbound 0.1.0` — final live-state
       confirmation; each MUST return `Released:` (no surprise retraction).

    4. `curl -fsI https://hexdocs.pm/mailglass/1.0.0/`,
       `curl -fsI https://hexdocs.pm/mailglass_admin/1.0.0/`,
       `curl -fsI https://hexdocs.pm/mailglass_inbound/0.1.0/` — each
       returns HTTP 200.

    5. Git status check: confirm only the four expected planning files
       (and the new evidence file) show as modified/new. No unintended
       changes.

    **Conventional Commit message** (use this verbatim when staging the
    Plan 05 changes; surface to user):

    ```text
    docs(release): close CLOSE-06 — branch-protection evidence + state updates

    Captures Phase 44.5 closeout per D-44.5-05:

    - 044.5-BRANCH-PROTECTION-EVIDENCE.md: gh api JSON snapshot of
      repos/szTheory/mailglass/branches/main/protection at the v1.0.0
      SHA timestamp. Required-bucket checklist documents which of the
      4 expected buckets per MAINTAINING.md:36-43 are present in
      contexts and which (if any) are bundled inside Support Contract
      Core via verify.stability_contract. Phase 51 CLOSE-02 follows up
      with automation reassert.
    - .planning/MILESTONES.md: appends "Live publish: <date>" bullet to
      the v1.0 entry with pointer to 044.5-RELEASE-RECORD.md.
    - .planning/PROJECT.md: Current State updated to reflect live Hex
      versions (1.0.0 / 1.0.0 / 0.1.0) and pointer to release record +
      branch-protection evidence.
    - .planning/STATE.md: Current Position transitions Phase 44.5 →
      Phase 45 (Inbound Telemetry + Idempotency Foundation); v1.2
      implementation unblocked per release-cadence rule.
    - .planning/REQUIREMENTS.md: marks CLOSE-06 closed with closure
      pointer to Phase 44.5 evidence.

    All three packages live on Hex.pm; HexDocs URLs return 200; 60-min
    revert window has expired without revert needed (recorded in
    044.5-RELEASE-RECORD.md). Phase 44.5 (v1.0/1.1 release ceremony)
    complete; the 4-milestone gap between v0.3.2 and 1.0.0 is closed.
    ```

    Do NOT commit. Surface the message to the user.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; mix mailglass.docs.check &amp;&amp; mix hex.info mailglass 1.0.0 | grep -q 'Released:' &amp;&amp; mix hex.info mailglass_admin 1.0.0 | grep -q 'Released:' &amp;&amp; mix hex.info mailglass_inbound 0.1.0 | grep -q 'Released:' &amp;&amp; curl -fsI https://hexdocs.pm/mailglass/1.0.0/ &amp;&amp; curl -fsI https://hexdocs.pm/mailglass_admin/1.0.0/ &amp;&amp; curl -fsI https://hexdocs.pm/mailglass_inbound/0.1.0/</automated>
  </verify>
  <acceptance_criteria>
    - `mix mailglass.docs.check` exits 0
    - `mix verify.stability_contract` exits 0 OR executor summary documents Postgres-skip subset
    - `mix hex.info mailglass 1.0.0` returns `Released:`
    - `mix hex.info mailglass_admin 1.0.0` returns `Released:`
    - `mix hex.info mailglass_inbound 0.1.0` returns `Released:`
    - HexDocs URLs return HTTP 200 for all three packages
    - `git status` shows exactly the four expected files modified/new (`.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-BRANCH-PROTECTION-EVIDENCE.md`, `.planning/MILESTONES.md`, `.planning/PROJECT.md`, `.planning/STATE.md`, plus `.planning/REQUIREMENTS.md` if updated separately)
    - Executor summary surfaces the proposed Conventional Commit message verbatim
    - No files committed (executor surfaces the diff + message; user commits)
  </acceptance_criteria>
  <done>
    Repo-truth lane green. All three packages confirmed live on Hex with
    HexDocs available. Closeout commit message ready for user. CLOSE-06
    structurally closed: evidence captured, state updated, requirement
    checkbox flipped, Phase 51 CLOSE-02 forward-pointer recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gh api repos/.../branches/main/protection` → live GitHub state | Authenticated read; reveals branch-protection configuration; PAT scope sensitivity |
| Captured JSON → `044.5-BRANCH-PROTECTION-EVIDENCE.md` | Evidence file lands in repo; permanent audit-trail artifact |
| `.planning/STATE.md`/`PROJECT.md`/`MILESTONES.md` updates → next phase invocation | Phase 45 / `gsd-plan-phase` reads STATE.md to determine current position; staleness blocks v1.2 implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44.5-05-01 | Tampering | Branch-protection drift between Phase 44.5 evidence capture and Phase 51 automation | mitigate | Task 2 captures snapshot at the v1.0.0 SHA timestamp. Phase 51 CLOSE-02 ships `branch-protection-drift.yml` daily reassert + `scripts/setup_branch_protection.sh` updates. The 044.5-BRANCH-PROTECTION-EVIDENCE.md "Closeout notes" explicitly forward-points to Phase 51. |
| T-44.5-05-02 | Information Disclosure | Branch-protection JSON includes admin team membership / Environment reviewer details | accept | The captured JSON is committed to a private-or-public repo (mailglass repo policy: TBD; if public, the JSON contents are already visible via the GitHub UI to anyone with read access). No new disclosure surface introduced. |
| T-44.5-05-03 | Tampering | STATE.md `last_updated` not bumped → next gsd-plan-phase invocation reads stale state | mitigate | Task 3 acceptance: `front-matter last_updated` is a valid ISO 8601 timestamp newer than the previous value. Visible in `git diff` review. |
| T-44.5-05-04 | Tampering | CLOSE-06 marked closed in REQUIREMENTS.md but evidence files don't exist (race condition between Plan 04 and Plan 05) | mitigate | Plan 05 `depends_on: [044.5-04]` enforces Plan 04 completion. Plan 04 acceptance: `044.5-RELEASE-RECORD.md` exists. Plan 05 references that file in its updates; if the file is missing, Plan 05 fails. |
| T-44.5-05-05 | Repudiation | Drift narrative in evidence file is incomplete (e.g., bucket 4 is "missing" but the narrative doesn't explain whether it's bundled-inside-core or genuinely-absent) | mitigate | Task 2 action explicitly requires the drift narrative to address each of the four expected buckets with one of: present-with-exact-name / present-with-different-name / missing-but-bundled / missing-and-genuinely-absent. Resume signal in checkpoint is "auth-ok"; no checkpoint for narrative completeness, but Task 2 acceptance criteria includes `grep -c 'Drift documented'` which catches the section presence. Human review at PR time catches narrative quality. |

Block on: high severity. T-44.5-05-01 is HIGH (would leave automation gap if forward-pointer is missing); mitigated by explicit Phase 51 CLOSE-02 reference in Task 2.
</threat_model>

<verification>
- `gh auth status` confirms admin scope (Task 1 checkpoint)
- `gh api repos/szTheory/mailglass/branches/main/protection` returns 200 + JSON
- `044.5-BRANCH-PROTECTION-EVIDENCE.md` exists with capture date, contexts JSON, required-bucket checklist filled, drift narrative, full protection state JSON, closeout notes (Phase 51 CLOSE-02 forward-pointer)
- `.planning/MILESTONES.md` v1.0 entry has "Live publish: <date>" bullet
- `.planning/PROJECT.md` Current State reflects live Hex versions
- `.planning/STATE.md` Current Position transitions to Phase 45; front-matter `last_updated` bumped
- `.planning/REQUIREMENTS.md` CLOSE-06 marked `[x]` with closure pointer
- `mix hex.info <pkg> <ver>` returns `Released:` for all three packages
- HexDocs URLs return HTTP 200 for all three
- `mix mailglass.docs.check` exits 0
- Executor summary surfaces the proposed Conventional Commit closeout message
</verification>

<success_criteria>
Phase 44.5 success criteria #4 and #5 are LIVE after this plan:

- #4: `mix hex.info` confirms all three packages live; PROJECT.md
  "Current State" updated; MILESTONES.md v1.0 entry adds live-publish date.
- #5: GitHub branch-protection rule verified externally via `gh api`
  capture; closes the v1.0 carry-forward "manual GitHub branch-protection
  verification" item; partially fulfills CLOSE-02 (the automation work in
  Phase 51 supplements this with a `gh api` script).

CLOSE-06 closed.

Phase 44.5 (v1.0/1.1 Release Ceremony) complete. The 4-milestone-deep gap
between v0.3.2 and 1.0.0 is closed. Phase 45 (Inbound Telemetry +
Idempotency Foundation) is unblocked per the release-cadence rule.

Plan-local success: 4 (or 5, including REQUIREMENTS.md) files modified.
Branch-protection evidence captured. Project state transitioned. CLOSE-06
checkbox flipped. Closeout commit message ready.
</success_criteria>

<output>
After completion, create `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-05-SUMMARY.md`
documenting:
- `gh auth status` output (Task 1)
- `044.5-BRANCH-PROTECTION-EVIDENCE.md` summary: required-bucket checklist
  state, drift narrative gist (Task 2)
- Diffs of the four state-file updates (Task 3): show the before/after for
  MILESTONES.md, PROJECT.md, STATE.md, REQUIREMENTS.md
- Final repo-truth lane confirmations (Task 4): `mix hex.info` outputs,
  HexDocs HTTP 200 confirmations, `mix mailglass.docs.check` exit 0
- The proposed closeout Conventional Commit message verbatim
- Status of T-44.5-05-01 through T-44.5-05-05 threats (mitigated/verified)
- Closing pointer: "Phase 44.5 (v1.0/1.1 Release Ceremony) complete.
  CLOSE-06 closed. Phase 45 (Inbound Telemetry + Idempotency Foundation)
  is the next phase per ROADMAP.md."
- Pointer to Phase 51 CLOSE-02 (branch-protection automation reassert) as
  the final piece of CLOSE-06's branch-protection story.
</output>
