---
phase: 044.5-v1-0-1-1-release-ceremony
plan: 03
type: execute
wave: 2
depends_on: [044.5-01, 044.5-02]
files_modified:
  - CHANGELOG.md
  - mailglass_admin/CHANGELOG.md
  - mailglass_inbound/CHANGELOG.md
  - docs/upgrade-from-0.x.md
  - mix.exs
autonomous: true
requirements: [CLOSE-06]
requirements_addressed: [CLOSE-06]
decisions_implemented: [D-44.5-02]

must_haves:
  truths:
    - "CHANGELOG.md has a placeholder front-matter paragraph above the existing top entry that links to `docs/upgrade-from-0.x.md` (survives release-please prepend per Pitfall 1)"
    - "mailglass_admin/CHANGELOG.md does NOT yet have a `## [1.0.0]` section (sibling note added during Release-PR hand-edit, not on `main`)"
    - "mailglass_inbound/CHANGELOG.md `## [0.1.0]` entry contains a first-publish framing paragraph + 0.x stability disclaimer pointing to `guides/compatibility-and-deprecations.md`"
    - "`docs/upgrade-from-0.x.md` exists and contains the bundle-context narrative modeled on `guides/upgrading-from-v0_1.md`"
    - "`docs/upgrade-from-0.x.md` is registered in `mix.exs` `docs.extras` list and in the `Guides` group of `groups_for_extras`"
    - "`docs/upgrade-from-0.x.md` contains zero `D-XX` or `LINT-XX` literal markers (mix mailglass.docs.check leak-free)"
    - "`mix mailglass.docs.check` exits 0"
    - "`mix docs --warnings-as-errors` exits 0 at root, in mailglass_admin/, and in mailglass_inbound/"
    - "Brand voice preserved: clear, exact, confident, warm; no emojis; no `Oops!` framing"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Adopter-facing front-matter paragraph above top entry pointing to upgrade doc"
      contains: "docs/upgrade-from-0.x.md"
    - path: "mailglass_inbound/CHANGELOG.md"
      provides: "First-publish framing + 0.x stability disclaimer in [0.1.0] entry"
      contains: "first Hex appearance"
    - path: "docs/upgrade-from-0.x.md"
      provides: "Bundle-context companion to canonical upgrade-to-v1_0.md (v0.5 + v0.6 + v1.0 + v1.1 deep-dive)"
      contains: "# Upgrading from v0.x to v1.0"
      min_lines: 80
    - path: "mix.exs"
      provides: "docs.extras registers docs/upgrade-from-0.x.md so HexDocs publishes it"
      contains: "docs/upgrade-from-0.x.md"
  key_links:
    - from: "CHANGELOG.md front-matter paragraph"
      to: "docs/upgrade-from-0.x.md"
      via: "markdown link `[docs/upgrade-from-0.x.md](docs/upgrade-from-0.x.md)`"
      pattern: "\\[docs/upgrade-from-0\\.x\\.md\\]"
    - from: "mix.exs docs.extras"
      to: "HexDocs build of `mailglass` package"
      via: "ExDoc extras registration"
      pattern: "docs/upgrade-from-0\\.x\\.md"
    - from: "mailglass_inbound/CHANGELOG.md [0.1.0] entry"
      to: "guides/compatibility-and-deprecations.md (0.x stability disclaimer)"
      via: "markdown link"
      pattern: "guides/compatibility-and-deprecations\\.md"
---

<objective>
Commit C — Curated CHANGELOG headers + `docs/upgrade-from-0.x.md` companion +
`mix.exs` docs.extras registration (D-44.5-02).

Implement the hybrid CHANGELOG curation strategy. Per RESEARCH.md Pattern 5
(force-push avoidance), the structure for D-44.5-02 lands across two
moments:

1. **On `main` in Commit C (this plan):** create `docs/upgrade-from-0.x.md`
   with the deep-dive bundle narrative; add a non-section-headed
   adopter-facing front-matter paragraph at the top of `CHANGELOG.md`
   linking to the upgrade doc; add the curated [0.1.0] framing to
   `mailglass_inbound/CHANGELOG.md`; register the upgrade doc in `mix.exs`
   `docs.extras`. **Do NOT add curated `## [1.0.0]` section content to
   `CHANGELOG.md` or `mailglass_admin/CHANGELOG.md` here** — release-please
   prepends those sections itself.

2. **On the open Release-Please PR (during Plan 04 ceremony):** hand-edit
   the auto-generated `## [1.0.0]` section in CHANGELOG.md to insert the
   four-milestone curated subsections (v0.5 / v0.6 / v1.0 / v1.1) BETWEEN
   the section heading and the `### Features` subsection; add the short
   coordinated sibling note to `mailglass_admin/CHANGELOG.md`'s [1.0.0]
   entry. Merge the PR immediately.

Purpose: Path C (hybrid curated header + auto ledger) per D-44.5-02 — the
only working precedent in this repo. Adopters who hit `hexdocs.pm` see the
curated narrative first; auditors get the full release-please-generated
ledger preserved below. Pitfall 1 (force-push clobber) is avoided by
landing the durable companion doc + front-matter on `main` and confining
hand-edits to a coordinated window.

Output:
- 5 files modified (1 commit, 1 PR)
- `mix mailglass.docs.check` exits 0 (no `D-XX`/`LINT-XX` leakage)
- `mix docs --warnings-as-errors` exits 0 at root + mailglass_admin/ + mailglass_inbound/
- `docs/upgrade-from-0.x.md` ships as ExDoc extra so it appears on hexdocs.pm/mailglass
- ready for Plan 04 (Wave 3 — live ceremony) once Commits A, B, C land on `main`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-CONTEXT.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-VALIDATION.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-01-SUMMARY.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-02-SUMMARY.md
@CLAUDE.md
@guides/upgrading-from-v0_1.md
@guides/upgrading-to-v1_0.md
@guides/compatibility-and-deprecations.md

<interfaces>
<!-- Key contracts the executor needs verbatim. -->

CHANGELOG.md current state (lines 1-7 header, 8-62 v0.3.x raw-ledger, 63-164 v0.2.0 curated precedent — the only working precedent in this repo for hybrid curated+ledger).

mix.exs `docs.extras` list (current shape, lines 344-348 area — the upgrade guides that already live here):
```elixir
"guides/multi-tenancy.md",
"guides/telemetry.md",
"guides/testing.md",
"guides/upgrading-from-v0_1.md",
"guides/migration-from-swoosh.md",
```

mix.exs `groups_for_extras` (lines 359-373 area) — the `Guides` group is the target for `docs/upgrade-from-0.x.md`.

guides/upgrading-from-v0_1.md (full file, 184 lines) — exact shape model per CONTEXT.md line 50. Section structure:
1. Title + canonical-positioning paragraphs (lines 1-9)
2. `## Before/After Examples` with code blocks (lines 16-54)
3. `## Codemod Walkthrough` 5-numbered-steps with bash blocks (lines 67-109)
4. `## Ambiguous Cases / Recipes`
5. `## New in v0.2`
6. `## Dependency Matrix` bullet list (lines 152-163)
7. `## Troubleshooting` `### h3 subsection per issue` (lines 165-180)
8. Closing `*Last updated: ...*` italic line (line 184)

guides/compatibility-and-deprecations.md (lines 161, 197) — already excludes `mailglass_inbound` from the 1.x compatibility promise. The curated [0.1.0] framing in `mailglass_inbound/CHANGELOG.md` MUST link here for the 0.x stability disclaimer.

Brand voice (CLAUDE.md `## Brand & Voice`): clear, exact, confident, warm. No emojis. No "Oops!" framing. Failure messages: `Delivery blocked: ...`. Direct word ("preview" not "experience the full rendering lifecycle").

Banned content patterns (mix mailglass.docs.check Tier 1 leak detector — verified leak-free at end of plan):
- `\bD-\d{2,3}\b` (e.g., `D-44.5-01`)
- `\bLINT-\d{2}\b` (e.g., `LINT-05`)

The new `docs/upgrade-from-0.x.md` and the CHANGELOG front-matter MUST NOT contain these.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/upgrade-from-0.x.md (bundle-context companion modeled on guides/upgrading-from-v0_1.md)</name>
  <files>docs/upgrade-from-0.x.md</files>
  <read_first>
    - guides/upgrading-from-v0_1.md (full file, 184 lines — section structure model)
    - guides/upgrading-to-v1_0.md (full file — canonical 0.x → 1.0 upgrade authority that this doc complements, NOT replaces)
    - guides/compatibility-and-deprecations.md (lines 161, 197 — inbound exclusion text)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Example 6 lines 1042-1135 — full skeleton + filename location decision)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 849-961 — section structure to mirror + brand voice constraints)
    - .planning/MILESTONES.md (lines 64-74 v1.0 entry, plus the v0.5 / v0.6 / v1.1 entries — reference material for "What 1.0.0 bundles" section)
    - .planning/milestones/v0.5-MILESTONE-AUDIT.md (existence check; if it exists, link to it)
    - .planning/milestones/v0.6-MILESTONE-AUDIT.md (existence check; if it exists, link to it)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md (existence check; if it exists, link to it)
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md (existence check; if it exists, link to it)
    - CLAUDE.md (Brand & Voice section)
    - MAINTAINING.md (lines 154-159 — sandbox install commands, source for the "Sandbox Verification Recipe" section)
  </read_first>
  <action>
    Create the new file `docs/upgrade-from-0.x.md`. Modeled on
    `guides/upgrading-from-v0_1.md` shape per CONTEXT.md line 50 + RESEARCH.md
    Example 6.

    **File location:** `docs/upgrade-from-0.x.md` (NOT `guides/upgrade-from-0.x.md`)
    per CONTEXT.md line 50 explicit spec. The path is registered in `mix.exs`
    `docs.extras` in Task 4.

    **Section structure** (mirror analog 1:1 with section name adaptations):

    ```markdown
    # Upgrading from v0.x to v1.0 (bundle: v0.5 → v1.1)

    This guide is the bundle-context companion to
    [`upgrading-to-v1_0.md`](../guides/upgrading-to-v1_0.md). Use this guide
    to understand WHAT changed across the four-milestone bundle (v0.5 + v0.6
    + v1.0 + v1.1) and HOW to verify the upgrade against your sandbox app.
    Use [`upgrading-to-v1_0.md`](../guides/upgrading-to-v1_0.md) for the
    strict-CI canonical migration steps.

    For the focused `v0.1 → v0.2` setter rewrite specifically, see
    [`upgrading-from-v0_1.md`](../guides/upgrading-from-v0_1.md).

    ## Who this guide is for

    Adopters on any `~> 0.3` version of `mailglass` and `mailglass_admin` who:

    - shipped before the v1.0 cut date
    - want to migrate to the `~> 1.0` line
    - are willing to verify against a sandbox app before upgrading production

    ## What 1.0.0 bundles

    Four shipped milestones land in this single 1.0.0 cut:

    | Milestone | Phases | Key adopter-facing change |
    |-----------|--------|---------------------------|
    | v0.5 Adoption Hardening | 28-31 | `Mailglass.TestAssertions`, idempotent installer |
    | v0.6 Production Maturity | 32-34 | `RateLimiter`, Mailgun/SES providers |
    | v1.0 Stability Lock | 35-38 | Compatibility guide, upgrade-to-v1_0.md, deprecations |
    | v1.1 Inbound Core Slice | 39-44 | `mailglass_inbound` (separate 0.x package) |

    ## Before/After Examples

    [3-5 code blocks showing highest-impact diffs across the bundle. Pick from:
    - `Mailglass.Message.new/2` deprecation warning + replacement
    - Per-domain rate limiting opt-in
    - TestAssertions usage upgrade
    - mix.exs deps lock change `{:mailglass, "~> 0.3"}` → `{:mailglass, "~> 1.0"}`
    Each example: a "Before (0.x)" code block and an "After (1.0)" code block.]

    ## Bundle Migration Walkthrough

    1. Update your dependencies:
       ```elixir
       def deps do
         [
           {:mailglass, "~> 1.0"},
           {:mailglass_admin, "~> 1.0"},
           # OPTIONAL — add only if you want inbound message routing:
           {:mailglass_inbound, "~> 0.1"}
         ]
       end
       ```
    2. Fetch dependencies: `mix deps.get`
    3. (If still on `~> 0.1` setter API) Run `mix mailglass.upgrade.v0_2 --apply`
    4. Compile: `mix compile --warnings-as-errors` — `Mailglass.Message.new/2`
       will warn; migrate per
       [`upgrading-to-v1_0.md`](../guides/upgrading-to-v1_0.md).
    5. Verify your usage matches the canonical guide:
       `mix verify.docs.migration`
    6. Run your test suite.

    ## Sibling Packages

    - `mailglass_admin` 1.0.0 — linked release; bump together with core
    - `mailglass_inbound` 0.1.0 — first Hex publish; opt-in if you handle
      inbound mail; ships on a separate 0.x version line per
      [`compatibility-and-deprecations.md`](../guides/compatibility-and-deprecations.md)

    ## Sandbox Verification Recipe

    Verify end-to-end against a fresh Phoenix app:

    ```bash
    mix archive.install hex phx_new --force
    mix phx.new sandbox --no-ecto --no-mailer --install
    cd sandbox
    # Edit mix.exs deps to add:
    #   {:mailglass, "== 1.0.0"},
    #   {:mailglass_admin, "== 1.0.0"},
    #   {:mailglass_inbound, "== 0.1.0"}
    mix deps.get
    mix mailglass.install
    mix compile --warnings-as-errors
    mix phx.server &
    curl -fs http://localhost:4000/dev/mail/    # expect HTTP 200
    ```

    If the sandbox passes, your upgrade should be safe.

    ## Dependency Matrix

    - `mailglass`: `~> 1.0`
    - `mailglass_admin`: `~> 1.0`
    - `mailglass_inbound`: `~> 0.1` (optional)
    - `phoenix`: `~> 1.8`
    - `phoenix_live_view`: `~> 1.1`
    - Elixir `~> 1.18` / OTP 27+

    ## Rollback

    Treat the upgrade as a git-clean operation. Run from a disposable branch
    or clean working tree so `git diff` shows exactly what changed. Discard
    with `git restore .` if the diff is wrong.

    ## Troubleshooting

    ### Linked siblings drift between releases

    [Specific guidance: `mailglass` and `mailglass_admin` share a version
    line; if you see `mix deps.get` complain about mismatched versions, you
    likely have one declared at a different constraint than the other.
    Align both at `~> 1.0`.]

    ### `mailglass_inbound` not on the 1.x line

    [Specific guidance: inbound is intentionally on `~> 0.1` because the
    inbound API is still stabilizing. Add or omit it independently of
    `mailglass` core; it does not lock-step with the 1.x line.]

    ### `Mailglass.Message.new/2` warning

    [Specific guidance: this is the deprecation bridge. See
    `upgrading-to-v1_0.md` for the migration path. The warning is
    informational, not blocking.]

    ## What this guide does NOT cover

    - API-level migration of specific code paths — see
      [`upgrading-to-v1_0.md`](../guides/upgrading-to-v1_0.md).
    - Detailed deprecation contract — see
      [`compatibility-and-deprecations.md`](../guides/compatibility-and-deprecations.md).
    - Inbound integration — see `mailglass_inbound/README.md` (separate
      package).

    ---

    *Last updated: 2026-05-XX (Phase 44.5 v1.0/1.1 release ceremony).*
    ```

    **Critical guardrails:**

    1. **NO `D-XX` or `LINT-XX` literal markers anywhere.** This file is
       adopter-facing and ships to hexdocs.pm. `mix mailglass.docs.check`
       Tier 1 leak detector enforces this. Verified in Task 5.

    2. **Brand voice** (CLAUDE.md): clear, exact, confident, warm. No
       emojis. No "Oops!" framing. Direct word: "preview" not "experience
       the full rendering lifecycle". Errors composed: `Delivery blocked:
       <reason>`.

    3. **Cross-references** use relative paths (`../guides/...`) because
       the file lives in `docs/` and the canonical guides live in
       `guides/`. ExDoc resolves these correctly when both are registered
       in `docs.extras`.

    4. **MILESTONE-AUDIT links:** if any of the four
       `.planning/milestones/v0.X-MILESTONE-AUDIT.md` files do NOT exist,
       OMIT the link rather than ship a broken reference. Use the live
       check from `read_first`.

    5. **Length:** target 80-150 lines after substituting the actual code
       block contents. Mirror the analog's density (the v0.1 → v0.2 doc is
       184 lines).

    Single new file. Markdown.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; test -f docs/upgrade-from-0.x.md &amp;&amp; head -1 docs/upgrade-from-0.x.md | grep -q '^# Upgrading from v0.x to v1.0' &amp;&amp; ! rg "D-\\d{2,3}|LINT-\\d{2}" docs/upgrade-from-0.x.md &amp;&amp; grep -q 'compatibility-and-deprecations' docs/upgrade-from-0.x.md &amp;&amp; grep -q 'upgrading-to-v1_0.md' docs/upgrade-from-0.x.md &amp;&amp; grep -q 'mix archive.install hex phx_new' docs/upgrade-from-0.x.md &amp;&amp; wc -l docs/upgrade-from-0.x.md | awk '{print $1}' | xargs -I{} test {} -ge 80</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/upgrade-from-0.x.md` exits 0 (file exists)
    - `head -1 docs/upgrade-from-0.x.md` reads `# Upgrading from v0.x to v1.0` (or close variant including bundle scope)
    - `wc -l docs/upgrade-from-0.x.md` reports at least `80` lines
    - `rg "D-\\d{2,3}|LINT-\\d{2}" docs/upgrade-from-0.x.md` returns NO matches (zero D-XX or LINT-XX literals)
    - `grep -c 'compatibility-and-deprecations' docs/upgrade-from-0.x.md` returns at least `1` (linked for 0.x stability disclaimer)
    - `grep -c 'upgrading-to-v1_0.md' docs/upgrade-from-0.x.md` returns at least `2` (referenced as canonical authority in multiple sections)
    - `grep -c 'mix archive.install hex phx_new' docs/upgrade-from-0.x.md` returns `1` (sandbox install recipe)
    - `grep -c '## Sandbox Verification Recipe' docs/upgrade-from-0.x.md` returns `1` (named section per CONTEXT success criterion #2)
    - `grep -c '## Bundle Migration Walkthrough' docs/upgrade-from-0.x.md` returns `1`
    - `grep -c '## Dependency Matrix' docs/upgrade-from-0.x.md` returns `1`
    - `grep -c '## Troubleshooting' docs/upgrade-from-0.x.md` returns `1`
    - No emojis present: `rg '[\x{1F300}-\x{1F9FF}\x{2600}-\x{27BF}]' docs/upgrade-from-0.x.md` returns no matches
    - No `Oops!` literal: `grep -c 'Oops!' docs/upgrade-from-0.x.md` returns `0`
  </acceptance_criteria>
  <done>
    `docs/upgrade-from-0.x.md` exists, mirrors the
    `guides/upgrading-from-v0_1.md` shape, covers the four-milestone bundle
    with adopter-facing focus. Brand voice preserved. Zero D-XX / LINT-XX
    leakage. Cross-references use correct relative paths. Ready for ExDoc
    registration in Task 4.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add adopter-facing front-matter paragraph at top of CHANGELOG.md (Pitfall 1 force-push avoidance)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-22 — current header + top of v0.3.x entry; this is where the front-matter paragraph goes)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Pattern 5 lines 591-618 — empirically-safer pattern: durable front-matter on `main`, hand-edit on PR for the `## [1.0.0]` section)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 645-749 — curated section structure for hand-edit reference, NOT for landing on main)
  </read_first>
  <action>
    Insert a non-section-headed adopter-facing paragraph BETWEEN the
    Keep-a-Changelog header (typically lines 1-7) and the existing top
    `## [...]` entry (typically line 8 onward). This paragraph survives
    release-please's prepend behavior because it lives ABOVE all `## [...]`
    section headings and release-please's prepend logic targets section
    headings.

    **Why this pattern (Pitfall 1 mitigation per RESEARCH.md Pattern 5
    lines 591-618):** release-please regenerates CHANGELOG.md by prepending
    a new `## [1.0.0]` section above the existing top entry. Hand-editing
    the auto-generated `## [1.0.0]` section directly on the open Release PR
    is fragile (force-push clobber risk if any commit lands on `main`
    between the hand-edit and merge). The robust pattern is:
    - Land durable adopter-facing prose on `main` ABOVE the section
      headings (this task) — release-please cannot clobber it because it
      doesn't touch this region.
    - On the open Release PR, do a fast hand-edit to insert the curated
      four-milestone subsections INSIDE the auto-generated `## [1.0.0]`
      block (Plan 04 task; immediate merge to minimize clobber window).

    **Concrete shape to insert** (paragraphs only — NO `##` or `###`
    headings; this is prose adjacent to but above the section ledger):

    ```markdown
    # Changelog

    All notable changes to `mailglass` are documented here.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    Mailglass 1.0.0 bundles four shipped milestones (v0.5 + v0.6 + v1.0 + v1.1) into
    the first stable release. If you ship on `~> 0.3`, read the
    [bundle upgrade companion at `docs/upgrade-from-0.x.md`](docs/upgrade-from-0.x.md)
    for the deep-dive walkthrough across all four milestones, then follow
    [`guides/upgrading-to-v1_0.md`](guides/upgrading-to-v1_0.md) for the strict-CI
    canonical migration steps. Sibling packages: `mailglass_admin` 1.0.0 (linked
    release) and `mailglass_inbound` 0.1.0 (first Hex publish; separate 0.x
    version line per [`guides/compatibility-and-deprecations.md`](guides/compatibility-and-deprecations.md)).

    ## [Unreleased]
    ...
    ```

    Notes:
    - Preserve the existing Keep-a-Changelog header lines exactly.
    - Insert the new paragraph BEFORE the first `## [...]` heading.
    - The paragraph references `docs/upgrade-from-0.x.md` (created in
      Task 1) as the durable pointer.
    - The paragraph also names sibling packages (admin 1.0.0, inbound 0.1.0
      on separate line) so adopters skimming the file get the bundle shape
      immediately.
    - Do NOT add a `## [1.0.0]` section here. release-please prepends that
      itself when it opens the PR. Hand-edit happens on the PR (Plan 04).
    - Do NOT modify the existing `## [...]` entries below.

    Single file edit. Markdown.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; grep -c 'docs/upgrade-from-0.x.md' CHANGELOG.md | grep -qE '^[1-9]$' &amp;&amp; grep -c 'mailglass_inbound. 0.1.0' CHANGELOG.md | grep -qE '^[1-9]$' &amp;&amp; grep -c 'guides/compatibility-and-deprecations.md' CHANGELOG.md | grep -qE '^[1-9]$' &amp;&amp; ! rg "D-\\d{2,3}|LINT-\\d{2}" CHANGELOG.md | head -1 &amp;&amp; awk '/^## \[/{exit} {print}' CHANGELOG.md | grep -q 'Mailglass 1.0.0 bundles four shipped milestones'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'docs/upgrade-from-0.x.md' CHANGELOG.md` returns at least `1` (link to upgrade companion present)
    - `grep -c 'guides/compatibility-and-deprecations.md' CHANGELOG.md` returns at least `1` (link to inbound 0.x stability disclaimer present)
    - `grep -c 'guides/upgrading-to-v1_0.md' CHANGELOG.md` returns at least `1` (link to canonical migration authority present)
    - `awk '/^## \\[/{exit} {print}' CHANGELOG.md | grep -c 'Mailglass 1.0.0 bundles four shipped milestones'` returns `1` — meaning the new paragraph lives ABOVE the first `## [...]` heading (Pitfall 1 force-push avoidance pattern)
    - The first `## [...]` section heading is unchanged (no new `## [1.0.0]` section landed on main; that's release-please's job): `grep -B1 'Unreleased\|0.3.2\|0.2.0' CHANGELOG.md | head -10` shows the existing entries unchanged
    - `rg "D-\\d{2,3}|LINT-\\d{2}" CHANGELOG.md` produces NO new matches (no planning-ID leakage)
    - No emojis introduced: `rg '[\\x{1F300}-\\x{1F9FF}]' CHANGELOG.md` returns no matches
    - File length grew by approximately 6-10 lines (the front-matter paragraph + spacing)
  </acceptance_criteria>
  <done>
    `CHANGELOG.md` has a durable adopter-facing paragraph above all
    `## [...]` entries, linking to `docs/upgrade-from-0.x.md`,
    `guides/upgrading-to-v1_0.md`, and
    `guides/compatibility-and-deprecations.md`. release-please's prepend
    behavior cannot clobber it. The `## [1.0.0]` section is deliberately
    absent on `main` — release-please will prepend it when opening the PR.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Curate mailglass_inbound/CHANGELOG.md [0.1.0] entry — first-publish framing + 0.x stability disclaimer</name>
  <files>mailglass_inbound/CHANGELOG.md</files>
  <read_first>
    - mailglass_inbound/CHANGELOG.md (full file — already has [0.1.0] header from Plan 01 / Commit A; this task adds the curated framing prose)
    - guides/compatibility-and-deprecations.md (lines 161, 197 — text to link to)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 167-231 — target shape with first-publish framing)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Example 4 lines 938-962)
  </read_first>
  <action>
    Insert a curated framing paragraph IMMEDIATELY after the
    `## [0.1.0] - 2026-05-XX` header (which Plan 01 / Commit A already
    landed) and BEFORE the existing `### Added` subsection.

    The framing paragraph sits between the section heading and the bullet
    list, just like the v0.2.0 sibling note in
    `mailglass_admin/CHANGELOG.md:39-46`.

    **Concrete content to insert:**

    ```markdown
    ## [0.1.0] - 2026-05-XX

    `mailglass_inbound` 0.1.0 is the first Hex appearance of the inbound sibling
    package. It ships on a separate, unlinked 0.x version line per
    [`guides/compatibility-and-deprecations.md`](../guides/compatibility-and-deprecations.md).
    The 1.x compatibility promise applies to `mailglass` core and `mailglass_admin`
    only; `mailglass_inbound` remains 0.x while the inbound API surface
    stabilizes.

    ### Added

    - Canonical manual setup documentation for the `mailglass_inbound` package.
    - Postmark and SendGrid provider guides with contract-tested durability and replay wording.
    - Oban-backed async execution with bounded Task.Supervisor fallback semantics.

    ### Changed

    - Repo-root verification now treats inbound docs and sibling-package release proof as release-blocking truth.
    ```

    **Critical preservations:**
    - The `## [0.1.0] - 2026-05-XX` header (already landed in Plan 01 / Commit A).
    - The existing `### Added` and `### Changed` bullet lists — preserve their content verbatim.
    - The Keep-a-Changelog header at the top of the file (lines 1-6).

    **Brand voice constraint:** clear, exact, confident, warm. The phrase
    "first Hex appearance" is intentional — direct, specific, accurate.
    Avoid "first official release" (ambiguous; the package was registered
    as a release artifact in v1.1 but never published to Hex) or
    "introducing" (cute, not technical).

    **Cross-reference path:** `../guides/compatibility-and-deprecations.md`
    (relative path from `mailglass_inbound/CHANGELOG.md` to root
    `guides/`). Do NOT use absolute or repository-prefixed paths.

    Single file edit. Markdown.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; grep -c 'first Hex appearance' mailglass_inbound/CHANGELOG.md | grep -q '^1$' &amp;&amp; grep -c '../guides/compatibility-and-deprecations.md' mailglass_inbound/CHANGELOG.md | grep -q '^1$' &amp;&amp; grep -c 'unlinked 0.x version line' mailglass_inbound/CHANGELOG.md | grep -q '^1$' &amp;&amp; grep -c '## \[0.1.0\]' mailglass_inbound/CHANGELOG.md | grep -q '^1$' &amp;&amp; grep -c '### Added' mailglass_inbound/CHANGELOG.md | grep -qE '^[1-9]$' &amp;&amp; ! rg "D-\\d{2,3}|LINT-\\d{2}" mailglass_inbound/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'first Hex appearance' mailglass_inbound/CHANGELOG.md` returns `1`
    - `grep -c '../guides/compatibility-and-deprecations.md' mailglass_inbound/CHANGELOG.md` returns `1` (relative-path link present)
    - `grep -c 'unlinked 0.x version line' mailglass_inbound/CHANGELOG.md` returns `1` (or close variant naming the unlinked posture)
    - `grep -c '^## \[0\.1\.0\]' mailglass_inbound/CHANGELOG.md` returns `1` (section header preserved from Plan 01)
    - `grep -c '^### Added' mailglass_inbound/CHANGELOG.md` returns at least `1` (subsection preserved)
    - `grep -c '^### Changed' mailglass_inbound/CHANGELOG.md` returns at least `1` (subsection preserved)
    - `grep -c 'Canonical manual setup documentation for the' mailglass_inbound/CHANGELOG.md` returns `1` (existing bullet preserved)
    - `rg "D-\\d{2,3}|LINT-\\d{2}" mailglass_inbound/CHANGELOG.md` returns NO matches
    - No emojis introduced
  </acceptance_criteria>
  <done>
    `mailglass_inbound/CHANGELOG.md` `## [0.1.0]` entry leads with the
    first-publish framing + 0.x stability disclaimer pointing to the
    compatibility guide. Existing Added/Changed bullets preserved. Brand
    voice intact. Zero planning-ID leakage.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Register docs/upgrade-from-0.x.md in mix.exs docs.extras + Guides group</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (root, lines 331-373 area — `docs/0` returns ExDoc config; `extras` list around 344-348; `groups_for_extras` around 359-373)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-PATTERNS.md (lines 616-642 — exact one-line edits)
  </read_first>
  <action>
    Two surgical edits to `mix.exs`. Both inside the `docs/0` private
    function (the ExDoc config block).

    **Edit 1 — `extras` list:** Locate the list of guides currently
    registered (around lines 344-348). The list ends with
    `"guides/migration-from-swoosh.md",` (or similar terminator). Append
    the new entry RIGHT AFTER the upgrading-from-v0_1.md line so the two
    upgrade guides appear adjacent in the source:

    Before:
    ```elixir
    "guides/multi-tenancy.md",
    "guides/telemetry.md",
    "guides/testing.md",
    "guides/upgrading-from-v0_1.md",
    "guides/migration-from-swoosh.md",
    ```

    After:
    ```elixir
    "guides/multi-tenancy.md",
    "guides/telemetry.md",
    "guides/testing.md",
    "guides/upgrading-from-v0_1.md",
    "guides/migration-from-swoosh.md",
    "docs/upgrade-from-0.x.md",
    ```

    Preserve existing entries verbatim (commas, trailing comma if present,
    indentation matches).

    **Edit 2 — `groups_for_extras` Guides group:** Locate the
    `groups_for_extras:` keyword inside `docs/0` (around lines 359-373).
    Find the `Guides` group entry — it's typically a list of file paths or
    a regex matching `guides/*.md`. Add `"docs/upgrade-from-0.x.md"` to
    that list so the upgrade companion appears in the same Guides
    navigation group on hexdocs.pm.

    Two patterns are common:

    **Pattern A — explicit list of files:**
    Before:
    ```elixir
    Guides: [
      "guides/installation.md",
      "guides/upgrading-from-v0_1.md",
      "guides/upgrading-to-v1_0.md",
      ...
    ],
    ```

    After:
    ```elixir
    Guides: [
      "guides/installation.md",
      "guides/upgrading-from-v0_1.md",
      "guides/upgrading-to-v1_0.md",
      "docs/upgrade-from-0.x.md",
      ...
    ],
    ```

    **Pattern B — regex that matches `guides/*.md` only:**
    The new file lives in `docs/`, not `guides/`, so a `~r{^guides/}` regex
    will NOT pick it up. Either:
    - Add an explicit string entry alongside the regex
      (`Guides: [~r{^guides/}, "docs/upgrade-from-0.x.md"]`), OR
    - Broaden the regex to match both directories
      (`Guides: ~r{^(guides|docs)/}`).

    **Read the actual current shape first** and apply whichever pattern
    matches. Mirror the position of `guides/upgrading-from-v0_1.md` in the
    Guides group so the navigation pairs upgrade guides together.

    **What to preserve:** every other key in the `docs/0` return map
    (`main`, `source_url`, `extra_section`, `formatters`, `groups_for_*`
    other than Guides). Surgical text-only edits.

    Single file edit. Elixir source.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; grep -c '"docs/upgrade-from-0.x.md"' mix.exs | grep -qE '^[1-9]$' &amp;&amp; mix docs --warnings-as-errors &amp;&amp; cd mailglass_admin &amp;&amp; mix docs --warnings-as-errors &amp;&amp; cd ../mailglass_inbound &amp;&amp; mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"docs/upgrade-from-0.x.md"' mix.exs` returns at least `1` — preferably `2` (extras list + Guides group)
    - `mix docs --warnings-as-errors` exits 0 at root (resolves the new extras path; no broken references)
    - `cd mailglass_admin && mix docs --warnings-as-errors` exits 0 (sibling docs build remains green)
    - `cd mailglass_inbound && mix docs --warnings-as-errors` exits 0 (sibling docs build remains green)
    - The generated `doc/` directory contains an entry for the new file:
      `test -f doc/upgrade-from-0-x.html` OR
      `test -f doc/docs/upgrade-from-0.x.html` — depending on ExDoc's path-flattening behavior; one of these must exist after a successful `mix docs` run
    - `mix.exs` syntactically valid: `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>
    `mix.exs` registers `docs/upgrade-from-0.x.md` as an ExDoc extra in the
    Guides group. `mix docs --warnings-as-errors` exits 0 at root and in
    both sibling packages. The upgrade companion will appear on
    hexdocs.pm/mailglass alongside the existing upgrade guides.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Validate Commit C locally — docs.check leak-free, full repo-truth lane green, propose Conventional Commit message</name>
  <files></files>
  <read_first>
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-VALIDATION.md (Per-Task Verification Map row for Plan 03)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RESEARCH.md (Phase Requirements → Test Map line 1243 — D-44.5-02 narrative correctness)
    - mix.exs (lines 252-258 — verify.stability_contract alias body)
    - lib/mix/tasks/mailglass.docs.check.ex (banned-pattern list — verify the literal patterns checked)
  </read_first>
  <action>
    Validate Commit C across the full docs gate before requesting commit.
    Surface the proposed Conventional Commit message.

    **Validation runs (each must exit 0):**

    1. `mix mailglass.docs.check` — Tier 1 docs leak detector. Asserts no
       banned patterns (`\bD-\d{2,3}\b`, `\bLINT-\d{2}\b`) anywhere in
       adopter-facing surfaces.

    2. `mix docs --warnings-as-errors` (root) — ExDoc build for `mailglass`
       package. Resolves `docs/upgrade-from-0.x.md` and all cross-references.

    3. `cd mailglass_admin && mix docs --warnings-as-errors` — sibling
       docs build remains green after the parent's mix.exs change.

    4. `cd mailglass_inbound && mix docs --warnings-as-errors` — sibling
       docs build remains green after the inbound CHANGELOG curation.

    5. `mix verify.stability_contract` — full repo-truth lane. Asserts the
       hybrid CHANGELOG + new upgrade doc + sibling-CHANGELOG curation pass
       all release-blocking checks. (If Postgres unavailable locally, run
       the docs-only subset: `mix mailglass.docs.check && mix docs
       --warnings-as-errors` plus the two sibling docs builds — document
       Postgres-skip in the executor summary.)

    6. Visual smoke: `rg -c "D-\\d{2,3}|LINT-\\d{2}" docs/upgrade-from-0.x.md
       CHANGELOG.md mailglass_inbound/CHANGELOG.md` returns `0:` for each
       file.

    **Conventional Commit message** (surface verbatim to user):

    ```text
    docs(release): curate CHANGELOG front-matter + create upgrade-from-0.x.md (Commit C)

    Lands the durable curation surfaces for the v1.0/1.1 release ceremony
    per D-44.5-02 hybrid posture:

    - docs/upgrade-from-0.x.md: new bundle-context companion modeled on
      guides/upgrading-from-v0_1.md. Covers v0.5 + v0.6 + v1.0 + v1.1
      across one adopter-facing walkthrough with sandbox verification
      recipe matching MAINTAINING.md.
    - CHANGELOG.md: adds an adopter-facing paragraph above the existing
      top entry pointing to the upgrade companion + canonical guide +
      compatibility doc. Lives above all section headings so
      release-please's prepend cannot clobber it.
    - mailglass_inbound/CHANGELOG.md: curates the [0.1.0] entry with
      first-publish framing + 0.x stability disclaimer linking to
      guides/compatibility-and-deprecations.md.
    - mix.exs: registers docs/upgrade-from-0.x.md in docs.extras and the
      Guides group so HexDocs publishes it on hexdocs.pm/mailglass.

    The auto-generated ## [1.0.0] sections in CHANGELOG.md and
    mailglass_admin/CHANGELOG.md are deliberately NOT added on main —
    release-please prepends them when it opens the Release PR. Hand-edit
    of those sections happens on the PR per the v0.2.0 ceremony precedent
    (Phase 13), with immediate merge to minimize force-push clobber risk.

    All adopter-facing surfaces leak-free per mix mailglass.docs.check;
    mix docs --warnings-as-errors green at root + admin + inbound.
    ```

    Do NOT commit anything in this task. Only validate + surface the
    message string.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass &amp;&amp; mix mailglass.docs.check &amp;&amp; mix docs --warnings-as-errors &amp;&amp; cd mailglass_admin &amp;&amp; mix docs --warnings-as-errors &amp;&amp; cd ../mailglass_inbound &amp;&amp; mix docs --warnings-as-errors &amp;&amp; cd .. &amp;&amp; ! rg "D-\\d{2,3}|LINT-\\d{2}" docs/upgrade-from-0.x.md CHANGELOG.md mailglass_inbound/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix mailglass.docs.check` exits 0
    - `mix docs --warnings-as-errors` exits 0 (root)
    - `cd mailglass_admin && mix docs --warnings-as-errors` exits 0
    - `cd mailglass_inbound && mix docs --warnings-as-errors` exits 0
    - `mix verify.stability_contract` exits 0 OR executor summary documents Postgres-skip with subset rationale
    - `rg "D-\\d{2,3}|LINT-\\d{2}" docs/upgrade-from-0.x.md CHANGELOG.md mailglass_inbound/CHANGELOG.md` returns NO matches across all three files
    - Executor summary surfaces the proposed Conventional Commit message verbatim
    - No files committed (executor surfaces the diff + message; user commits)
  </acceptance_criteria>
  <done>
    Commit C validated locally. `mix mailglass.docs.check` is leak-free.
    `mix docs --warnings-as-errors` exits 0 at root and in both sibling
    packages. Proposed commit message ready for user. Wave 2 complete; Plan
    04 (Wave 3 — live ceremony) unblocked once Commits A, B, C all land on
    `main`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter on hexdocs.pm → docs/upgrade-from-0.x.md | Adopters ingest the upgrade narrative as the source of truth for migrating; misleading content = broken upgrades |
| `main` branch → release-please open PR (force-push regenerates CHANGELOG.md) | Hand-edits to CHANGELOG.md sections on the open PR are vulnerable to clobber if any commit lands on main between hand-edit and merge |
| `mix mailglass.docs.check` → adopter-facing surfaces | Tier 1 leak detector enforces no internal D-XX or LINT-XX markers cross into hexdocs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44.5-03-01 | Tampering | Curated CHANGELOG hand-edit clobbered by release-please force-push (Pitfall 1) | mitigate | This plan deliberately splits curation: durable adopter-facing paragraph lives ABOVE all section headings on `main` (immune to release-please's prepend); the per-section curated `## [1.0.0]` four-milestone subsections happen on the open Release PR in Plan 04 with immediate merge (per Phase 13 v0.2.0 precedent). Pitfall 1 fully mitigated structurally. |
| T-44.5-03-02 | Information Disclosure | `D-XX` or `LINT-XX` planning IDs leaking into adopter-facing files (CHANGELOG, upgrade doc, inbound CHANGELOG) | mitigate | Task 5 runs `mix mailglass.docs.check` (Tier 1 leak detector with banned-pattern list); acceptance criteria `rg "D-\d{2,3}|LINT-\d{2}"` returns no matches across all three files. Task 1, 2, 3 explicitly forbid these patterns in their action. |
| T-44.5-03-03 | Tampering | Cross-reference rot — `docs/upgrade-from-0.x.md` references files that don't exist (e.g., a missing milestone audit) | mitigate | Task 1 read_first explicitly checks for existence of `.planning/milestones/v0.X-MILESTONE-AUDIT.md` files; missing files are OMITTED rather than shipped as broken refs. `mix docs --warnings-as-errors` (Task 4 acceptance) catches broken in-doc cross-references. |
| T-44.5-03-04 | Tampering | Sibling docs builds break after root mix.exs change (mailglass_admin or mailglass_inbound have different docs.extras shape and depend on shared config) | mitigate | Task 4 acceptance runs `mix docs --warnings-as-errors` in BOTH sibling directories. If the extras change in root mix.exs cascades, the sibling docs builds catch it. |
| T-44.5-03-05 | Tampering | Brand voice drift — "Oops!" / cute / overhyped tone leaks into adopter-facing surfaces despite CLAUDE.md rule | accept (with grep gates) | Task 1, 2, 3 acceptance: `grep -c 'Oops!'` returns `0` per file; emoji range regex returns 0 matches. Brand voice is enforced at acceptance gate. Subjective tone is human-judged at PR review. |

Block on: high severity. T-44.5-03-01 and T-44.5-03-02 are HIGH (would publish broken upgrade narrative or leak internal IDs); both have grep-verifiable acceptance criteria + `mix mailglass.docs.check` gate.
</threat_model>

<verification>
- `test -f docs/upgrade-from-0.x.md` exits 0 with at least 80 lines
- `mix mailglass.docs.check` exits 0
- `mix docs --warnings-as-errors` exits 0 at root, in mailglass_admin/, in mailglass_inbound/
- `grep` checks confirm CHANGELOG.md adopter-facing paragraph lives ABOVE all `## [...]` section headings
- `grep` checks confirm `docs/upgrade-from-0.x.md` registered in mix.exs docs.extras
- `grep` checks confirm `mailglass_inbound/CHANGELOG.md` [0.1.0] entry has first-publish framing + compatibility-and-deprecations link
- `rg` checks confirm zero `D-XX`/`LINT-XX` literal markers across all three modified docs files
- No emojis introduced
- Executor summary surfaces the proposed Conventional Commit message
</verification>

<success_criteria>
Phase 44.5 success criterion #2 ("CHANGELOG entries for all three packages
document the bundled v0.5 + v0.6 + v1.0 + v1.1 milestone work with REQ-ID
references; the upgrade path doc `docs/upgrade-from-0.x.md` is verified
end-to-end against a sandbox app") is structurally seeded by this plan. The
upgrade doc exists on `main`. The four-milestone REQ-ID-citing subsections
land on the Release PR in Plan 04. The sandbox-verification recipe is in
the upgrade doc and is exercised in Plan 04 Wave 3.

Plan-local success: 5 files modified. Pitfall 1 (force-push clobber)
structurally avoided via durable-paragraph-above-section-headings pattern.
`mix mailglass.docs.check` leak-free. `mix docs --warnings-as-errors` green
at all three docs surfaces. Plan 04 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-03-SUMMARY.md`
documenting:
- Files modified with line counts
- `mix mailglass.docs.check` output
- `mix docs --warnings-as-errors` output for root + mailglass_admin + mailglass_inbound
- `grep` verification output (actual command results) for all `must_haves` truths
- `rg "D-\d{2,3}|LINT-\d{2}"` results for all three modified docs files (proof of leak-free)
- Status of T-44.5-03-01 through T-44.5-03-05 threats (mitigated/verified)
- The proposed Conventional Commit message verbatim
- Pointer to Plan 04 (Wave 3 — live ceremony) as next step, noting that Plan 04 cannot start until Commits A, B, C have all landed on `main` and release-please opens the v1.0.0 PR
</output>
