---
phase: 165-reconcile-terminal-proof-and-milestone-archive-ordering
plan: 04
type: execute
wave: 2
depends_on:
  - 165-01
files_modified: []
autonomous: false
requirements: []
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No installed terminal executable is created or changed until the user approves the exact source OID, SHA-256, destination, mode, predecessor disposition, runtime closure, and rollback tuple (D-16)."
    - "After approval, the installed bytes match the approved source and the installed-only hostile boundary passes without changing the historical Phase 164 command (D-02, D-12)."
    - "Installation performs no remote mutation, dependency change, workflow operation, release, publication, PR closure, or tracked repository write (D-10, D-17)."
  artifacts:
    - scripts/mailglass_finalize_milestone_loader.mjs
    - scripts/finalize_milestone_v2_7.sh
    - test/scripts/phase_165_milestone_finalizer_test.exs
  key_links:
    - "approved installation-proposal JSON -> atomic external installation -> installed byte digest assertion"
    - "installed executable -> `phase_165_installed_production_boundary` -> readiness-only v2.7 probe"
---

<objective>
Obtain exact approval for, then install and verify, the separate v2.7 terminal executable.

Purpose: Cross the repository-to-host trust boundary only with the locked approval tuple and rollback controls required by D-16.
Output: An approved external installed mirror whose bytes and runtime closure match the tracked source, with no tracked repository output.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-CONTEXT.md
@.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-RESEARCH.md
@.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-01-SUMMARY.md
@scripts/mailglass_finalize_milestone_loader.mjs
@scripts/finalize_milestone_v2_7.sh
@test/scripts/phase_165_milestone_finalizer_test.exs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the exact external installation tuple</name>
  <read_first>
    - .planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-PATTERNS.md
    - scripts/mailglass_finalize_milestone_loader.mjs
    - scripts/finalize_milestone_v2_7.sh
    - test/scripts/phase_165_milestone_finalizer_test.exs
    - .planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-01-SUMMARY.md
  </read_first>
  <what-built>The repository-local v2.7 loader, finalizer, hostile fixtures, and an exact read-only installation proposal.</what-built>
  <how-to-verify>Review the displayed source OID, SHA-256, destination, mode, predecessor disposition, runtime closure, rollback tuple, and explicit exclusion of the Phase 164 command.</how-to-verify>
  <action>
Follow the installed-boundary authentication and exact-tuple pattern mapped by `165-PATTERNS.md` to `scripts/mailglass_finalize_phase_loader.mjs`, while keeping the external destination outside tracked file assignments. Run only the read-only installation proposal for destination `/Users/jon/.local/bin/mailglass-finalize-milestone`; immediately before emitting it, `lstat` that exact path and do not create, replace, chmod, back up, or delete it. Accept only absence, with disposition `create`, or one safe regular non-symlink file; for the latter capture exact SHA-256, mode, uid/gid owner, atomic backup/replace behavior, and executable rollback. Reject all other predecessor kinds and unsafe owner/mode states. Present one exact JSON tuple containing the tracked source blob OID, source SHA-256, destination, requested mode 0500, predecessor identity and disposition, every physical runtime path/version/digest in the closure, and the rollback tuple. Show that `/Users/jon/.local/bin/mailglass-finalize-phase 164` is outside the proposal and remains unchanged per D-02. Ask the user to approve or reject this exact tuple. This is the first of exactly two lifecycle checkpoints required by D-16.
  </action>
  <verify>
    <automated>node scripts/mailglass_finalize_milestone_loader.mjs --installation-proposal --destination /Users/jon/.local/bin/mailglass-finalize-milestone | jq -e '.source_oid and .source_sha256 and .destination == "/Users/jon/.local/bin/mailglass-finalize-milestone" and .mode == "0500" and (.predecessor.disposition == "create" or (.predecessor.disposition == "backup_replace" and .predecessor.sha256 and .predecessor.mode and .predecessor.uid != null and .predecessor.gid != null)) and (.runtime_closure | type == "array" and length &gt; 0) and .rollback' &gt;/dev/null</automated>
    <fails_when>The proposal exits non-zero, any required tuple field is absent, the source is not the tracked loader/finalizer revision, the mode differs from 0500, the Phase 164 command is targeted, or the proposal performs a filesystem mutation.</fails_when>
    <human-check>Approve only the displayed exact tuple; any changed OID, digest, destination, mode, predecessor disposition, runtime closure, or rollback requires a fresh proposal and fresh approval.</human-check>
  </verify>
  <acceptance_criteria>
    - "The user has explicitly approved the exact seven-part installation tuple (D-16)."
    - "No external executable changed before approval."
    - "The Phase 164 installed authority is excluded and unchanged (D-02)."
  </acceptance_criteria>
  <done>The unchanged seven-part tuple has explicit user approval and no installation mutation preceded it.</done>
  <resume-signal>Reply `approve exact installation tuple` to authorize Task 2, or reject it with the field that must change.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the approved bytes atomically and prove the installed-only boundary</name>
  <files>scripts/mailglass_finalize_milestone_loader.mjs, scripts/finalize_milestone_v2_7.sh, test/scripts/phase_165_milestone_finalizer_test.exs</files>
  <read_first>
    - .planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-PATTERNS.md
    - scripts/mailglass_finalize_milestone_loader.mjs
    - scripts/finalize_milestone_v2_7.sh
    - test/scripts/phase_165_milestone_finalizer_test.exs
    - .planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-01-SUMMARY.md
  </read_first>
  <reversibility rating="costly">The external replacement is rollback-capable, but only the exact approved tuple may cross the host boundary and any failure must restore its recorded predecessor.</reversibility>
  <action>
Implement the host mutation with the installed-loader analog and the shared authentication, bounded-error, secret-environment, and evidence-separation patterns explicitly assigned in `165-PATTERNS.md` to `scripts/mailglass_finalize_phase_loader.mjs` and `test/scripts/phase_164_closeout_test.exs`. After approval, re-lstat the destination and reject any drift from the approved predecessor tuple before feeding the unchanged proposal to `--install-approved`; stage beside the approved destination, verify source and staged digests, apply mode 0500, atomically rename, fsync where supported, and re-read installed bytes and runtime closure. On any failure, execute the approved rollback tuple and report the restored predecessor. Run the installed-boundary alias in readiness mode only: it must authenticate bytes and prove hostile installed-byte mismatch rejection without issuing the terminal `v2.7` report. Do not modify tracked files, the historical Phase 164 command, dependencies, workflows, remotes, releases, publications, PRs, CI topology, or product surfaces (D-02, D-10, D-12, D-17).
  </action>
  <verify>
    <automated>mix verify.phase_165.installed_boundary &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)"</automated>
    <fails_when>The installed-boundary suite exits non-zero, approved and installed digests differ, the byte-mismatch fixture is accepted, terminal report dispatch occurs, rollback fails after an induced installation error, or repository porcelain is non-empty.</fails_when>
  </verify>
  <acceptance_criteria>
    - "Installed bytes, mode, destination, and runtime closure exactly match the approved proposal."
    - "Installed-byte mismatch fails closed before terminal dispatch (D-12)."
    - "Repository porcelain remains empty and the Phase 164 installed command is unchanged (D-02)."
  </acceptance_criteria>
  <done>The approved bytes are installed atomically, the installed-only boundary passes, rollback is proven, and tracked porcelain stays empty.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 165-04 local inventory)

- **New or modified tracked implementation paths/symbols:** none; the Plan 01 loader/finalizer/tests are read-only inputs to this host-boundary plan.
- **Executor-owned generated tracked output:** `.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-04-SUMMARY.md`, recording the approved tuple and readiness result without embedding credentials.
- **External runtime output:** `/Users/jon/.local/bin/mailglass-finalize-milestone`, created when absent or atomically replacing one approved safe regular predecessor; its exact bytes, mode, owner, runtime closure, and rollback are checkpoint-bound rather than tracked artifacts.
- **External recovery output:** a digest-addressed predecessor backup only when the approved tuple's disposition is `backup_replace`; absence uses disposition `create` and needs no predecessor backup.
- **Ignored runtime output:** none; installed-boundary readiness must not emit the terminal milestone report.
- **Archived/workflow outputs:** none; audit and archive remain deferred to the Plan 05 post-completion runbook.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked source -> external executable | Approved repository bytes are copied into a host command location. |
| predecessor -> replacement -> rollback | Existing host state must remain recoverable across atomic replacement. |

## STRIDE Threat Register (OWASP ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-04-T01 | Tampering | installed executable bytes | high | mitigate | Blocking human approval plus pre/post SHA-256, full source OID, atomic rename, and exact mode. |
| T-165-04-E01 | Elevation of privilege | runtime closure | high | mitigate | Pin physical executables and versions/digests; use mode 0500 and no ambient runtime lookup. |
| T-165-04-D01 | Denial of service | failed replacement | medium | mitigate | Capture predecessor disposition and test executable rollback before replacement. |
| T-165-04-R01 | Repudiation | installation authorization | high | mitigate | Persist the exact approved proposal tuple in execution evidence and reject any drift. |
</threat_model>

<verification>
Generate and approve the exact installation tuple, perform the atomic installation, compare installed bytes with the approved source, run the installed-only boundary alias, and assert clean repository porcelain.
</verification>

<success_criteria>
The separate v2.7 installed command exists only after exact approval, matches approved bytes and runtime closure, is rollback-capable, and has not emitted terminal evidence.
</success_criteria>

<output>
Create `.planning/phases/165-reconcile-terminal-proof-and-milestone-archive-ordering/165-04-SUMMARY.md` when done.
</output>
