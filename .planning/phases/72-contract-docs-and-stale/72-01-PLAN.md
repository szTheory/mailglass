---
phase: 72-contract-docs-and-stale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/jobs.md
  - guides/compatibility-and-deprecations.md
  - MAINTAINING.md
  - lib/mix/tasks/mailglass.docs.check.ex
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - PROOF-02

must_haves:
  truths:
    - "guides/jobs.md describes mailglass_inbound as having its own independent stable 1.0 contract, not as outside the v1.x stability promise (D-11: primary Phase 72 public surface; D-12: known stale site)"
    - "guides/compatibility-and-deprecations.md support matrix row says independent 1.0 contract instead of excluded from the 1.x compatibility promise (D-11: primary Phase 72 public surface; D-12: known stale site)"
    - "guides/compatibility-and-deprecations.md DX inventory support-until horizons say Through mailglass_inbound 1.x instead of 0.x (D-11: primary Phase 72 public surface; D-12: known stale site)"
    - "MAINTAINING.md JTBD step 5 routes readers to mailglass_inbound/docs/api_stability.md instead of conditional outside-v1.x language (D-11: primary Phase 72 public surface; D-12: known stale site)"
    - "mix mailglass.docs.check exits 0 on the corrected public docs"
    - "mix test test/mailglass/docs_contract_test.exs passes with updated freshness stamp and MAINTAINING assertions"
    - "mailglass_inbound/docs/api_stability.md remains the canonical contract inventory; edited docs summarize and route readers there without becoming competing inventories (D-04)"
    - "All stale-phrase guards in this plan use exact token matching, not regex or dynamic parsing (D-06)"
    - "Guards use 1.0 for inbound's own contract and do not ban 1.x mentions in DX inventory horizons where they describe future semantic-break policy (D-07)"
    - "Replacement wording makes the adopter workflow end-to-end obvious without blurring framework semantics with provider implementation details (D-14)"
    - "Provider stability is kept at the MailglassInbound.Ingress.Plug option/semantic level; provider modules remain internal (D-15)"
  artifacts:
    - path: "guides/jobs.md"
      provides: "Corrected inbound stability header and footer"
      contains: "independent stable `1.0` contract"
    - path: "guides/compatibility-and-deprecations.md"
      provides: "Corrected support matrix row and DX inventory horizons"
      contains: "independent `1.0` contract"
    - path: "MAINTAINING.md"
      provides: "Corrected JTBD step 5"
      contains: "independent `1.0` contract"
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Flipped README.md required/forbidden tokens + compatibility guide forbidden token"
      contains: "excluded from the `1.x` compatibility promise"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "Updated freshness stamp and MAINTAINING assertions"
      contains: "independent stable `1.0` contract"
  key_links:
    - from: "lib/mix/tasks/mailglass.docs.check.ex"
      to: "README.md"
      via: "@tier1_surface_rules required/forbidden check"
      pattern: "mailglass_inbound.*stable 1\\.0"
    - from: "test/mailglass/docs_contract_test.exs"
      to: "guides/jobs.md"
      via: "File.read! freshness assertion"
      pattern: "assert jobs =~ \"independent stable"
---

<objective>
Correct the five stale public-doc claim sites in guides/jobs.md,
guides/compatibility-and-deprecations.md, and MAINTAINING.md so all three
describe mailglass_inbound as having its own independent stable 1.0 contract
(not as outside or excluded from the v1.x stability promise). Simultaneously
flip the corresponding executable guards in mailglass.docs.check.ex and
test/mailglass/docs_contract_test.exs so the proof seams enforce the corrected
posture instead of the stale one.

Purpose: DOC-01, DOC-02, and PROOF-02 close the gap between Phase 66's
release-position decision (mailglass_inbound 1.0.0 ships with its own contract)
and the public prose/tests that still describe inbound as outside the v1.x
stability promise. Passing these checks is the minimum correctness bar before
Phase 73 records Hex/HexDocs evidence.

Output:
- guides/jobs.md with updated date, header, and footer
- guides/compatibility-and-deprecations.md with corrected support matrix row,
  three DX inventory horizon cells, and "What this guide does not promise" note
- MAINTAINING.md with corrected JTBD step 5
- lib/mix/tasks/mailglass.docs.check.ex with updated @tier1_surface_rules for
  README.md (remove stale required token, add two correct required tokens, add
  one forbidden token) and compatibility guide (add forbidden + required tokens)
- test/mailglass/docs_contract_test.exs with updated freshness/stability test
  and new MAINTAINING assertions
</objective>

<notes>
Implementation constraints honored by all tasks in this plan:

- D-04: mailglass_inbound/docs/api_stability.md remains the canonical contract
  inventory. The docs edited here summarize and route readers there; they do not
  become competing inventories.
- D-06: Stale-phrase guards use exact token matching for known bad claims (e.g.,
  "excluded from the `1.x` compatibility promise", "outside the `v1.x` stability
  promise"). Dynamic parsing is used only for version pins and manifest values.
- D-07: Guards do not ban every mention of `1.x` around inbound. "1.0" is used
  for inbound's own contract to avoid triggering refute_over_claims! regex; "1.x"
  appears in DX inventory horizons where it correctly describes future inbound
  semantic-break policy.
- D-14: Wording follows the Rails Action Mailbox / Laravel Mail pattern: end-to-end
  adopter workflow is obvious, provider implementation details are not blurred with
  framework semantics.
- D-15: Wording follows Anymail's honesty about normalized support; provider
  stability is kept at the MailglassInbound.Ingress.Plug option/semantic level
  without matrix sprawl.
</notes>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-PATTERNS.md

<interfaces>
<!-- Key patterns extracted from source files. Read these before touching anything. -->

From test/mailglass/docs_contract_test.exs lines 356-363 (CURRENT stale test to replace):
  test "freshness stamp and inbound stability boundary are present" do
    jobs = File.read!("guides/jobs.md")
    assert jobs =~ "Current as of 2026-05-23"
    assert jobs =~ "mailglass` and `mailglass_admin`"
    assert jobs =~ "outside the"
    assert jobs =~ "`v1.x` stability promise"
  end

From test/mailglass/docs_contract_test.exs lines 171-202 (CURRENT MAINTAINING test):
  test "MAINTAINING.md pins the required versus advisory verification contract" do
    maintaining = File.read!("MAINTAINING.md")
    -- many assertions --
    assert maintaining =~ "JTBD Docs Refresh Protocol"
    assert maintaining =~ "guides/jobs.md"
    assert maintaining =~ "Always refresh the internal map first"
    assert maintaining =~ "guides/compatibility-and-deprecations.md"
  end

From lib/mix/tasks/mailglass.docs.check.ex lines 70-86 (CURRENT stale README.md rules):
  "README.md" => %{
    required: [
      "docs/api_stability.md",
      "guides/compatibility-and-deprecations.md",
      "guides/upgrading-to-v1_0.md",
      "mix mailglass.install",
      "mailglass_inbound` is outside the `v1.x` stability promise"   # STALE - must move to forbidden
    ],
    forbidden: ["~> 0.1", "~> 0.2", "verify.phase_07", "v0.1 in development", "v0.3 public surface"]
  },

From lib/mix/tasks/mailglass.docs.check.ex lines 251-263 (CURRENT compatibility guide rules):
  "guides/compatibility-and-deprecations.md" => %{
    required: [
      "stable lane", "compatibility lane", "warnings-as-errors",
      "mailglass_inbound", "mailglass_inbound/docs/api_stability.md",
      "Reachability is not a compatibility promise.",
      "## Inbound deprecation-DX inventory", "Support-until horizon", "Proof artifact"
    ],
    forbidden: ["Phase 37", "v0.1 in development"]
  },

From mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs lines 502-512
(tokens that must survive the compatibility guide edit -- DO NOT REMOVE):
  assert compatibility =~ "mailglass_inbound/docs/api_stability.md"
  assert compatibility =~ "stable/internal/deferred source"
  assert compatibility =~ "Reachability is not a compatibility promise."
  assert compatibility =~ "## mailglass_inbound compatibility"
  assert compatibility =~ "## Inbound deprecation-DX inventory"
  assert compatibility =~ "| Surface | Bridge or replacement | Warning or migration channel | ..."

From 72-PATTERNS.md — refute_over_claims! guard to avoid triggering:
  Regex: ~r/mailglass_inbound.*1\.x.*(stability|stable|compatibility)/i
  Use "1.0" not "1.x" when describing inbound's own contract.
  "independent `1.0` contract" is safe; "1.x compatibility" tied to inbound is not.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct stale prose in guides/jobs.md, guides/compatibility-and-deprecations.md, and MAINTAINING.md</name>
  <files>guides/jobs.md, guides/compatibility-and-deprecations.md, MAINTAINING.md</files>
  <read_first>
    - guides/jobs.md (full file — identify exact lines 3-6 header and lines 416-419 footer)
    - guides/compatibility-and-deprecations.md (full file — identify line 166 support matrix row, lines 217-219 DX inventory horizons, and line ~225 "What this guide does not promise" bullet)
    - MAINTAINING.md lines 110-125 (JTBD Docs Refresh Protocol step 5 at lines 117-118)
    - .planning/phases/72-contract-docs-and-stale/72-PATTERNS.md (exact replacement wording for all five stale sites)
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs lines 475-524 (tokens that must survive compatibility guide edit)
  </read_first>
  <behavior>
    - guides/jobs.md header must not contain "outside the `v1.x` stability promise"
    - guides/jobs.md header must contain "independent stable `1.0` contract" and "mailglass_inbound/docs/api_stability.md"
    - guides/jobs.md header date must be "Current as of 2026-06-02"
    - guides/jobs.md footer must not contain "outside the `v1.x` stability promise" or "v1.2 work is expanding"
    - guides/jobs.md footer must route to mailglass_inbound/docs/api_stability.md
    - guides/compatibility-and-deprecations.md line 166 must contain "independent `1.0` contract" not "excluded from the `1.x` compatibility promise"
    - guides/compatibility-and-deprecations.md three DX inventory horizon cells must say "Through `mailglass_inbound` `1.x`" not "0.x"
    - guides/compatibility-and-deprecations.md "What this guide does not promise" bullet distinguishes "not covered by this guide" from "not stable"
    - guides/compatibility-and-deprecations.md must preserve: "mailglass_inbound/docs/api_stability.md", "stable/internal/deferred source", "Reachability is not a compatibility promise.", "## mailglass_inbound compatibility", "## Inbound deprecation-DX inventory", the DX header row string
    - MAINTAINING.md lines 117-118 must not say "while it remains outside the `v1.x`"
    - MAINTAINING.md must say "independent `1.0` contract" and route to "mailglass_inbound/docs/api_stability.md" in step 5
    - Replacement wording uses "1.0" not "1.x" for inbound's own contract to avoid triggering refute_over_claims! regex
  </behavior>
  <action>
    Edit guides/jobs.md header (lines 3-6): Change date to "Current as of 2026-06-02". Replace the "it remains **outside the `v1.x` stability promise** for now" clause with "; it has its own independent stable `1.0` contract documented in `mailglass_inbound/docs/api_stability.md`." (per D-01, D-03, D-16 and PATTERNS.md replacement direction).

    Edit guides/jobs.md footer paragraph (lines 416-419): Replace the entire paragraph starting "Today it ships verified ingress" through "production-capable and still hardening." with a two-sentence replacement that names mailglass_inbound 1.0.0, its independent stable 1.0 contract, and routes to mailglass_inbound/docs/api_stability.md. Use the exact replacement text from PATTERNS.md "Stale footer paragraph" section (the backtick-linked form).

    Edit guides/compatibility-and-deprecations.md line 166: Replace the `mailglass_inbound` row cell "excluded from the `1.x` compatibility promise in this milestone" with "independent `1.0` contract; see `mailglass_inbound/docs/api_stability.md`" and update the proof artifact column to "`mailglass_inbound/docs/api_stability.md`" (per RESEARCH.md Example 1, D-02, D-03).

    Edit guides/compatibility-and-deprecations.md three DX inventory horizon cells (lines 217-219, one per row): Replace every occurrence of "Through `mailglass_inbound` `0.x`; semantic break requires a major release position decision" with "Through `mailglass_inbound` `1.x`; semantic break requires a deprecation bridge or a `mailglass_inbound` major-version change" (per RESEARCH.md Example 2, D-02).

    Edit guides/compatibility-and-deprecations.md "What this guide does not promise" bullet (line ~225): Replace the bullet that says "support for `mailglass_inbound` within the `1.x` contract covered here" with a bullet that distinguishes coverage boundary from stability, routing readers to mailglass_inbound/docs/api_stability.md (per PATTERNS.md "`What this guide does not promise` section" and D-03).

    Edit MAINTAINING.md lines 117-118: Replace "keep inbound summarized separately while it remains outside the `v1.x`\n     promise" with "keep inbound summarized separately, noting its own independent `1.0`\n     contract and routing readers to `mailglass_inbound/docs/api_stability.md`" (per D-01, D-03 and PATTERNS.md "MAINTAINING.md" section).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -c "outside the \`v1\.x\` stability promise" guides/jobs.md guides/compatibility-and-deprecations.md MAINTAINING.md | grep -v ':0' | wc -l | grep -q '^0$' && echo STALE_PHRASES_GONE || echo STALE_PHRASES_REMAIN</automated>
  </verify>
  <done>
    - guides/jobs.md contains "Current as of 2026-06-02", "independent stable `1.0` contract", and "mailglass_inbound/docs/api_stability.md" in header; footer routes to api_stability.md
    - guides/compatibility-and-deprecations.md does not contain "excluded from the `1.x` compatibility promise"; contains "independent `1.0` contract"
    - guides/compatibility-and-deprecations.md does not contain "Through `mailglass_inbound` `0.x`"; contains "Through `mailglass_inbound` `1.x`"
    - MAINTAINING.md does not contain "while it remains outside the `v1.x`"; contains "independent `1.0` contract"
    - All tokens that inbound docs_contract_test.exs pins in the compatibility guide are still present
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip executable guards in mailglass.docs.check.ex and docs_contract_test.exs</name>
  <files>lib/mix/tasks/mailglass.docs.check.ex, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - lib/mix/tasks/mailglass.docs.check.ex lines 70-90 (current README.md @tier1_surface_rules — the stale required entry is at line 77)
    - lib/mix/tasks/mailglass.docs.check.ex lines 251-264 (current compatibility guide @tier1_surface_rules)
    - test/mailglass/docs_contract_test.exs lines 356-363 (current stale freshness test)
    - test/mailglass/docs_contract_test.exs lines 171-202 (MAINTAINING test — add two assertions here)
    - .planning/phases/72-contract-docs-and-stale/72-PATTERNS.md "lib/mix/tasks/mailglass.docs.check.ex" section (exact token values to add/move)
    - README.md (verify "**`mailglass_inbound`** (inbound routing; stable 1.0)" and "`mailglass_inbound` has its own stable `1.0` contract inventory" are present — Phase 71 put them there)
  </read_first>
  <behavior>
    - mix mailglass.docs.check exits 0 after this task (currently fails because it requires the stale README token Phase 71 already removed)
    - mix mailglass.docs.check fails if "mailglass_inbound` is outside the `v1.x` stability promise" reappears in README.md (stale phrase now forbidden)
    - mix mailglass.docs.check fails if "excluded from the `1.x` compatibility promise" reappears in compatibility guide (new forbidden token)
    - mix mailglass.docs.check requires "independent `1.0` contract" in compatibility guide (new required token)
    - mix test test/mailglass/docs_contract_test.exs passes (freshness test checks corrected date and wording, MAINTAINING test checks corrected step 5)
    - No new test files created; all changes are in-place edits to existing files
  </behavior>
  <action>
    Edit lib/mix/tasks/mailglass.docs.check.ex @tier1_surface_rules "README.md" entry:
    (1) Remove the stale required token "mailglass_inbound` is outside the `v1.x` stability promise" from the required: list.
    (2) Add two new required tokens to the required: list: "**`mailglass_inbound`** (inbound routing; stable 1.0)" and "`mailglass_inbound` has its own stable `1.0` contract inventory". Both of these tokens are already in README.md per docs_contract_test.exs lines 43-44.
    (3) Add the removed stale phrase "mailglass_inbound` is outside the `v1.x` stability promise" to the forbidden: list so any future regression fails the check.

    Edit lib/mix/tasks/mailglass.docs.check.ex @tier1_surface_rules "guides/compatibility-and-deprecations.md" entry:
    (1) Add "independent `1.0` contract" to the required: list (asserts the corrected support matrix posture from Task 1).
    (2) Add "excluded from the `1.x` compatibility promise" to the forbidden: list (pins the stale phrase as a regression guard).

    Edit test/mailglass/docs_contract_test.exs "freshness stamp and inbound stability boundary are present" test (lines 356-363):
    Replace the four assertions with: assert jobs =~ "Current as of 2026-06-02", assert jobs =~ "mailglass` and `mailglass_admin`", assert jobs =~ "independent stable `1.0` contract", assert jobs =~ "mailglass_inbound/docs/api_stability.md", refute jobs =~ "outside the `v1.x` stability promise". (per PATTERNS.md Example 4 and RESEARCH.md Proof Seam 2).

    Edit test/mailglass/docs_contract_test.exs "MAINTAINING.md pins" test (lines 171-202):
    Add two new assertions inside this existing test after the existing maintaining assertions:
    refute maintaining =~ "while it remains outside the `v1.x`"
    assert maintaining =~ "independent `1.0` contract"
    (per PATTERNS.md "test/mailglass/docs_contract_test.exs" coverage gap section).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix mailglass.docs.check && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>
    - mix mailglass.docs.check exits 0
    - mix test test/mailglass/docs_contract_test.exs --warnings-as-errors exits 0
    - @tier1_surface_rules for README.md does not contain the stale required token; contains the two new required tokens; contains the stale phrase in forbidden
    - @tier1_surface_rules for guides/compatibility-and-deprecations.md forbidden list includes "excluded from the `1.x` compatibility promise"; required list includes "independent `1.0` contract"
    - docs_contract_test.exs freshness test asserts "Current as of 2026-06-02" and "independent stable `1.0` contract"; refutes "outside the `v1.x` stability promise"
    - docs_contract_test.exs MAINTAINING test refutes "while it remains outside the `v1.x`" and asserts "independent `1.0` contract"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system reads in Mix task | mailglass.docs.check.ex reads public doc files from disk at task-run time |
| Test file assertions | docs_contract_test.exs reads file contents at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No external packages; all changes are in-place Elixir/Markdown edits |
| T-72-01-01 | Tampering | guides/compatibility-and-deprecations.md | accept | In-repository Markdown; no untrusted input; changes are doc corrections, not code paths |
| T-72-01-02 | Information Disclosure | docs_contract_test.exs | accept | Test files read local Markdown; no PII or credentials touched; no network I/O |
</threat_model>

<verification>
After both tasks complete:

1. mix mailglass.docs.check exits 0
2. mix test test/mailglass/docs_contract_test.exs --warnings-as-errors exits 0
3. grep "outside the \`v1\.x\` stability promise" guides/jobs.md guides/compatibility-and-deprecations.md MAINTAINING.md returns no matches
4. grep "excluded from the \`1\.x\` compatibility promise" guides/compatibility-and-deprecations.md returns no matches
5. grep "Through \`mailglass_inbound\` \`0\.x\`" guides/compatibility-and-deprecations.md returns no matches
6. grep "independent \`1\.0\` contract" guides/compatibility-and-deprecations.md returns at least one match
7. grep "independent stable \`1\.0\` contract" guides/jobs.md returns at least one match
8. grep "independent \`1\.0\` contract" MAINTAINING.md returns at least one match
</verification>

<success_criteria>
- guides/jobs.md does not contain "outside the `v1.x` stability promise"; contains "Current as of 2026-06-02" and "independent stable `1.0` contract"
- guides/compatibility-and-deprecations.md does not contain "excluded from the `1.x` compatibility promise" or "Through `mailglass_inbound` `0.x`"; contains "independent `1.0` contract" and "Through `mailglass_inbound` `1.x`"
- MAINTAINING.md does not contain "while it remains outside the `v1.x`"; contains "independent `1.0` contract"
- mix mailglass.docs.check exits 0 (stale required token removed, two correct tokens added to required, stale phrase moved to forbidden, compatibility guide forbidden token added)
- mix test test/mailglass/docs_contract_test.exs --warnings-as-errors exits 0
</success_criteria>

<output>
Create `.planning/phases/72-contract-docs-and-stale/72-01-SUMMARY.md` when done.
</output>
