---
phase: 72-contract-docs-and-stale
plan: 03
type: execute
wave: 2
depends_on:
  - 72-01
  - 72-02
files_modified:
  - mailglass_inbound/mix.exs
  - .planning/publish/mailglass_inbound-publish-summary.json
  - test/mailglass/stability_contract_test.exs
autonomous: true
requirements:
  - PROOF-02

must_haves:
  truths:
    - "mailglass_inbound/mix.exs source_ref_pattern is mailglass_inbound-v%{version} (not mailglass-sibling-group-v%{version})"
    - ".planning/publish/mailglass_inbound-publish-summary.json source_ref_pattern field reflects the corrected value"
    - "stability_contract_test.exs asserts summary source_ref_pattern == mailglass_inbound-v%{version}"
    - "mix verify.stability_contract exits 0 (all three lanes green)"
  artifacts:
    - path: "mailglass_inbound/mix.exs"
      provides: "Corrected source_ref_pattern pointing to inbound-specific tag"
      contains: "mailglass_inbound-v%{version}"
    - path: ".planning/publish/mailglass_inbound-publish-summary.json"
      provides: "Regenerated publish summary with corrected source_ref_pattern"
      contains: "mailglass_inbound-v%{version}"
    - path: "test/mailglass/stability_contract_test.exs"
      provides: "Assertion that guards against source_ref_pattern regression"
      contains: "source_ref_pattern"
  key_links:
    - from: "test/mailglass/stability_contract_test.exs"
      to: ".planning/publish/mailglass_inbound-publish-summary.json"
      via: "json!/1 helper reads the publish summary"
      pattern: "summary\\[\"source_ref_pattern\"\\] == \"mailglass_inbound-v%\\{version\\}\""
---

<objective>
Correct the source_ref_pattern metadata in mailglass_inbound/mix.exs from the
stale sibling-group tag pattern ("mailglass-sibling-group-v%{version}") to the
package-specific tag pattern ("mailglass_inbound-v%{version}"). Regenerate the
publish summary via the existing mix mailglass.publish.check lane. Add a
stability_contract_test.exs assertion to pin the corrected value against future
regression.

Purpose: The source_ref_pattern controls which Git tag HexDocs uses to link
source files. "mailglass-sibling-group-v%{version}" would resolve to the
sibling-group tag (which does not exist for inbound's independent release path);
"mailglass_inbound-v%{version}" resolves to the inbound-specific tag
mailglass_inbound-v1.0.0 that MAINTAINING.md documents as the correct dispatch
ref. D-09 directs this pre-publish correction. PROOF-02 requires the corrected
value to be asserted in the stability-contract test.

Output:
- mailglass_inbound/mix.exs with corrected source_ref_pattern
- .planning/publish/mailglass_inbound-publish-summary.json regenerated (run
  through the existing mix mailglass.publish.check lane, never hand-edited)
- test/mailglass/stability_contract_test.exs with one new assertion added to
  the existing "inbound 1.0 release preflight truth is exact" test block
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-01-SUMMARY.md
@/Users/jon/projects/mailglass/.planning/phases/72-contract-docs-and-stale/72-02-SUMMARY.md

<interfaces>
<!-- Key patterns from source files for this plan -->

From mailglass_inbound/mix.exs lines 118-130 (CURRENT stale value):
  defp package do
    [
      name: "mailglass_inbound",
      licenses: ["MIT"],
      description: @description,
      source_ref_pattern: "mailglass-sibling-group-v%{version}",   # STALE — change to "mailglass_inbound-v%{version}"
      links: %{...},
      files: ~w(lib docs priv .formatter.exs mix.exs README* CHANGELOG* LICENSE*)
    ]
  end

From mailglass_inbound/mix.exs docs() (DO NOT CHANGE this line):
  source_ref: "v" <> @version,   # stays as-is: "v1.0.0"

From test/mailglass/stability_contract_test.exs lines 116-146 (current test block — add ONE assertion):
  test "inbound 1.0 release preflight truth is exact across source and publish evidence" do
    manifest = json!(".release-please-manifest.json")
    summary = json!(".planning/publish/mailglass_inbound-publish-summary.json")
    ...
    assert summary["source_ref"] == "v#{expected_version}"           # existing — keep
    # ADD AFTER THIS LINE:
    # assert summary["source_ref_pattern"] == "mailglass_inbound-v%{version}"
    assert summary["mailglass_inbound_publish_pin"] == "== #{expected_core_version}"   # keep, exists after
    ...
  end

From test/mailglass/stability_contract_test.exs line 26 (json! helper — do not add a second one):
  defp json!(path), do: path |> File.read!() |> Jason.decode!()

Publish summary regeneration command (repo-native, do not hand-edit the JSON):
  mix mailglass.publish.check --package mailglass_inbound

Note: Pitfall 4 in RESEARCH.md — run mix mailglass.publish.check BEFORE adding the
source_ref_pattern assertion to the test, so the summary is regenerated first and
the assertion has a valid JSON field to check.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix source_ref_pattern in mix.exs, regenerate publish summary, add stability assertion</name>
  <files>mailglass_inbound/mix.exs, .planning/publish/mailglass_inbound-publish-summary.json, test/mailglass/stability_contract_test.exs</files>
  <read_first>
    - mailglass_inbound/mix.exs lines 115-135 (package/0 and docs/0 functions — identify source_ref_pattern line and confirm source_ref line is NOT changed)
    - test/mailglass/stability_contract_test.exs lines 116-148 (full "inbound 1.0 release preflight truth is exact" test block — find the summary["source_ref"] assertion line to insert after)
    - .planning/publish/mailglass_inbound-publish-summary.json (current state — confirms stale "mailglass-sibling-group-v%{version}" value before running the regeneration command)
    - .planning/phases/72-contract-docs-and-stale/72-PATTERNS.md "mailglass_inbound/mix.exs" section (corrected value and what must not change)
    - .planning/phases/72-contract-docs-and-stale/72-PATTERNS.md ".planning/publish/mailglass_inbound-publish-summary.json" section (regeneration instruction)
  </read_first>
  <behavior>
    - mailglass_inbound/mix.exs package/0 source_ref_pattern value is "mailglass_inbound-v%{version}"
    - mailglass_inbound/mix.exs docs/0 source_ref value is unchanged ("v" <> @version)
    - .planning/publish/mailglass_inbound-publish-summary.json source_ref_pattern field is "mailglass_inbound-v%{version}" (regenerated by mix mailglass.publish.check, not hand-edited)
    - test/mailglass/stability_contract_test.exs contains assert summary["source_ref_pattern"] == "mailglass_inbound-v%{version}" inside the existing "inbound 1.0 release preflight truth is exact" test block
    - mix test test/mailglass/stability_contract_test.exs --warnings-as-errors exits 0
    - mix verify.stability_contract exits 0
  </behavior>
  <action>
    Step 1 — Edit mailglass_inbound/mix.exs: In the package/0 function, change the single line source_ref_pattern: "mailglass-sibling-group-v%{version}" to source_ref_pattern: "mailglass_inbound-v%{version}". Do not alter source_ref: "v" <> @version in the docs/0 function.

    Step 2 — Regenerate publish summary: Run the command "mix mailglass.publish.check --package mailglass_inbound" from the repository root. This regenerates .planning/publish/mailglass_inbound-publish-summary.json with the corrected source_ref_pattern. Do not hand-edit the JSON file; let the task write it.

    Step 3 — Add stability assertion: In test/mailglass/stability_contract_test.exs, locate the assert summary["source_ref"] == "v#{expected_version}" line (line ~140). Add the following assertion immediately after it on the next line: assert summary["source_ref_pattern"] == "mailglass_inbound-v%{version}". Maintain consistent indentation (six spaces, matching surrounding lines).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q '"mailglass_inbound-v%{version}"' mailglass_inbound/mix.exs && grep -q '"source_ref_pattern": "mailglass_inbound-v%{version}"' .planning/publish/mailglass_inbound-publish-summary.json && mix test test/mailglass/stability_contract_test.exs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>
    - mailglass_inbound/mix.exs contains source_ref_pattern: "mailglass_inbound-v%{version}"
    - .planning/publish/mailglass_inbound-publish-summary.json contains "source_ref_pattern": "mailglass_inbound-v%{version}" (regenerated, not hand-edited)
    - test/mailglass/stability_contract_test.exs contains assert summary["source_ref_pattern"] == "mailglass_inbound-v%{version}"
    - mix test test/mailglass/stability_contract_test.exs --warnings-as-errors exits 0
    - mix verify.stability_contract exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix task execution | mix mailglass.publish.check reads mix.exs and writes publish summary JSON |
| JSON parsing in test | stability_contract_test.exs parses publish summary at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No external packages; changes are metadata correction + test assertion |
| T-72-03-01 | Tampering | mailglass_inbound/mix.exs source_ref_pattern | accept | Package metadata only; controls HexDocs source-link tag; no runtime security surface |
| T-72-03-02 | Information Disclosure | .planning/publish/mailglass_inbound-publish-summary.json | accept | Publish snapshot; no credentials or PII; regenerated by repo-native task |
</threat_model>

<verification>
After task completes:

1. grep "mailglass_inbound-v%{version}" mailglass_inbound/mix.exs returns one match (source_ref_pattern line)
2. grep "mailglass-sibling-group" mailglass_inbound/mix.exs returns no matches
3. grep '"source_ref_pattern": "mailglass_inbound-v%{version}"' .planning/publish/mailglass_inbound-publish-summary.json returns one match
4. grep 'source_ref_pattern' test/mailglass/stability_contract_test.exs returns one match (the new assertion)
5. mix test test/mailglass/stability_contract_test.exs --warnings-as-errors exits 0
6. mix verify.stability_contract exits 0
</verification>

<success_criteria>
- mailglass_inbound/mix.exs source_ref_pattern is "mailglass_inbound-v%{version}" (not "mailglass-sibling-group-v%{version}")
- .planning/publish/mailglass_inbound-publish-summary.json "source_ref_pattern" field is "mailglass_inbound-v%{version}"
- test/mailglass/stability_contract_test.exs contains assert summary["source_ref_pattern"] == "mailglass_inbound-v%{version}"
- mix test test/mailglass/stability_contract_test.exs --warnings-as-errors exits 0
- mix verify.stability_contract exits 0 (all three lanes: docs_contract, stability_contract, inbound docs_contract)
</success_criteria>

<output>
Create `.planning/phases/72-contract-docs-and-stale/72-03-SUMMARY.md` when done.
</output>
