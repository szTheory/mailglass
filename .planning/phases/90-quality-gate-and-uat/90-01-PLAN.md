---
phase: 90-quality-gate-and-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/90-quality-gate-and-uat/gate.sh
  - .planning/phases/90-quality-gate-and-uat/90-gate-evidence.md
  - .planning/phases/90-quality-gate-and-uat/90-01-CHECKPOINT.md
autonomous: false
requirements: [GATE-01, GATE-02, GATE-03]
must_haves:
  truths:
    - "A scripted gate passes: all SVGs XML-parse (xmllint), tokens.json JSON-parses, every href/src resolves locally, zero external URLs, zero planning-process vocabulary in the folder, zero font-family in assets/*.svg, no background plate behind any mark (documented square social-avatar exception), size budgets hold (folder <= 500 KB, index.html <= 150 KB, no file > 100 KB)"
    - "Browser-rendered evidence (screenshots to an ignored tmp dir) confirms light/dark rendering and 16px favicon legibility, and nothing outside brandbook-fable/ and .planning/ changed"
    - "The maintainer completes an A/B walkthrough (brandbook/index.html vs brandbook-fable/index.html) and signs off the milestone"
  artifacts:
    - path: ".planning/phases/90-quality-gate-and-uat/gate.sh"
      provides: "Consolidated 9-check scripted gate (GATE-01 + scope check), re-runnable as one unit"
      min_lines: 60
    - path: ".planning/phases/90-quality-gate-and-uat/90-gate-evidence.md"
      provides: "Full gate run log(s) + browser evidence index with per-screenshot verdicts"
      min_lines: 40
    - path: ".planning/phases/90-quality-gate-and-uat/90-01-CHECKPOINT.md"
      provides: "One-screen A/B walkthrough package + maintainer sign-off record (GATE-03)"
      contains: "DIF-12"
  key_links:
    - from: ".planning/phases/90-quality-gate-and-uat/90-gate-evidence.md"
      to: ".planning/phases/90-quality-gate-and-uat/gate.sh"
      via: "verbatim tee'd run output ending in GATE-PASS"
      pattern: "GATE-PASS"
    - from: ".planning/phases/90-quality-gate-and-uat/90-01-CHECKPOINT.md"
      to: ".planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md"
      via: "the 12 differentiators reproduced as the UAT checklist"
      pattern: "DIF-0[1-9]|DIF-1[0-2]"
---

<objective>
Prove `brandbook-fable/` is standalone, clean, and A/B-ready with one consolidated
scripted gate (GATE-01) plus browser-rendered evidence (GATE-02), then hand the
maintainer the side-by-side A/B walkthrough and pause for sign-off (GATE-03) —
the milestone's final human step.

Purpose: this gate re-proves, on the final state and in one place, every check
Phases 86-89 enforced piecemeal — and closes the v1.8 root failures for good
(planning-language leakage, unvalidated contrast, undemonstrated dark mode,
font-dependent wordmarks).

Output: `gate.sh` + `90-gate-evidence.md` (gate runs + screenshot verdicts) in the
phase dir, `90-01-CHECKPOINT.md` awaiting maintainer sign-off. `brandbook-fable/`
is touched ONLY if a gate failure forces a fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-quality-gate-and-uat/90-CONTEXT.md
@.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md
@.planning/phases/87-logo-tournament/87-decision-record.md
</context>

<fix_loop_rule>
ANY gate or evidence failure: fix it in `brandbook-fable/`, then re-run the FULL
gate from check 1 (one consolidated `bash gate.sh` re-run, never a single-check
re-run) and append the complete new run to `90-gate-evidence.md` labeled
"Run 2", "Run 3", etc. Browser evidence affected by a fix is re-captured and
re-read. No git branching — work on main. Do NOT commit `brandbook-fable/` fixes
mixed with the user's unrelated WIP; commit only this phase's files plus any
fable fix files.
</fix_loop_rule>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Consolidated scripted gate (GATE-01) — gate.sh + first recorded run</name>
  <files>.planning/phases/90-quality-gate-and-uat/gate.sh, .planning/phases/90-quality-gate-and-uat/90-gate-evidence.md</files>
  <read_first>
    - .planning/phases/90-quality-gate-and-uat/90-CONTEXT.md (the 9 locked checks)
    - .planning/phases/89-collateral-specimens-copy/89-01-PLAN.md Task 4 verify block (the established denylist regex + size/scope command forms — the precedent to extend, not replace)
    - .planning/phases/87-logo-tournament/87-decision-record.md asset table (social-avatar.svg / social-avatar-dark.svg are the documented plate exception; note actual filenames differ from the Phase 85 manifest: logo-monochrome.svg, social-avatar.svg, social-avatar-dark.svg, logo-typemark.svg)
  </read_first>
  <action>
    Write `.planning/phases/90-quality-gate-and-uat/gate.sh` (bash, lives in the phase
    dir per CONTEXT discretion — NEVER inside brandbook-fable/). Structure: each check
    prints "CHECK-N PASS" or "CHECK-N FAIL: detail" and sets a failure flag; the script
    runs ALL checks before exiting (no early exit, so one run reports everything), exits
    non-zero on any failure, and prints the sentinel GATE-PASS only when all 9 pass.
    Run from repo root. The 9 checks, with exact command forms:

    1. SVG parse: for f in brandbook-fable/assets/*.svg brandbook-fable/examples/*.svg; do xmllint --noout "$f"; done — all must pass (xmllint is at /usr/bin/xmllint). Also assert the expected inventory: exactly 8 files in assets/ and 6 in examples/ (4 SVGs + 2 HTML).

    2. tokens parse: python3 -m json.tool brandbook-fable/tokens.json > /dev/null

    3. Reference integrity + zero external URLs:
       a. Local resolution — for each of the 3 HTML files (index.html, examples/landing-page.html, examples/email-template.html): extract every (href|src)="..." value with grep -Eo, skip values starting with "#", "data:", or "mailto:", resolve each against the FILE'S OWN directory (examples pages use ../tokens.css, ../assets/...), and test -f the result. Every reference must resolve.
       b. External — all must count 0:
          grep -rEn '(href|src|xlink:href)="https?://' brandbook-fable/
          grep -rEin 'url\([\"'"'"']?https?:' brandbook-fable/
          grep -rEin '@import' brandbook-fable/
          grep -rEn 'fetch\(|XMLHttpRequest|<script src' brandbook-fable/ --include='*.html'
          (xmlns namespace URIs are identifiers, not fetches — the href/src/url() patterns above deliberately do not match them; do NOT use a bare https?:// grep, it false-positives on xmlns.)

    4. Process-vocabulary denylist — the established Phase 88/89 regex VERBATIM as the base, extended with the CONTEXT terms under word boundaries:
       BASE (copy exactly): phase|milestone|roadmap|tournament|codex|gsd|REQ-[0-9A-Z]|BOOK-0|DIF-[0-9]|CDX-|COLL-0|COPY-0|FOUND-0|LOGO-0|GATE-0|TODO|FIXME|lorem|placeholder|draft
       EXTENSION (append with |): \bplans?\b|checkpoint|\bTBD\b|option-|variant-|\bbaseline\b
       Command shape: grep -riE '<BASE>|<EXTENSION>' brandbook-fable/ | grep -viE 'align-items:[[:space:]]*baseline' | wc -l → must be 0.
       The single grep -v filter is the CSS-keyword false-positive exclusion (index.html uses the functional CSS value align-items: baseline twice; rewriting working layout CSS to dodge a denylist word would be wrong — same rationale as decision [79-01] text-base-content). Document this exclusion in a comment inside gate.sh AND in the evidence file. NO other exclusions are permitted; any other hit is a real leak to fix in the source file.

    5. Live text / fonts in SVG assets — both must be 0 across brandbook-fable/assets/*.svg AND brandbook-fable/examples/*.svg (re-proving the Phase 87/89 gates, never weakening them):
       grep -c '<text' per file; grep -ic 'font-family' per file

    6. No background plate behind any mark (structural): every assets/*.svg EXCEPT social-avatar.svg and social-avatar-dark.svg must contain zero <rect> elements (verified-current state: marks/lockups/favicon are path+circle only). The two avatars must each contain EXACTLY one <rect width="240" height="240"> — the inherently-square documented exception per 87-decision-record.md.

    7. Size budgets: [ "$(du -sk brandbook-fable | awk '{print $1}')" -le 500 ]; [ "$(wc -c < brandbook-fable/index.html)" -le 153600 ]; [ -z "$(find brandbook-fable -type f -size +100k)" ]

    8. Favicon: [ "$(grep -Eoc '<(path|rect|circle|ellipse|polygon|polyline|line)\b' brandbook-fable/assets/favicon.svg)" ] total shape-element count <= 3 (use grep -Eo ... | wc -l), and grep -c 'viewBox="0 0 16 16"' >= 1.

    9. Scope / frozen baseline (commit-range check):
       a. Working tree clean outside .planning/: git status --porcelain | grep -vE '^.. \.planning/' → empty (this phase's own evidence files may be in flight under .planning/).
       b. Frozen codex untouched: git diff --quiet 09a84dd4 -- brandbook/ AND git status --porcelain brandbook/ → empty.
       c. v1.9 range clean: git diff --name-only 09a84dd4..HEAD | grep -vE '^(brandbook-fable/|\.planning/)' | wc -l → 0 (covers phases 85-90, strictly stronger than the required 88-90 window).

    Then run it: bash .planning/phases/90-quality-gate-and-uat/gate.sh 2>&1 | tee /tmp/gate-run-1.txt
    Create `90-gate-evidence.md` with: header (date, HEAD sha, gate.sh path), the
    documented align-items:baseline exclusion note, and the verbatim Run 1 output.
    On any FAIL: apply the fix-loop rule (fix in brandbook-fable/, full re-run,
    append as Run N) until a run ends in GATE-PASS.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && bash .planning/phases/90-quality-gate-and-uat/gate.sh > /tmp/gate-verify.txt 2>&1 && grep -q 'GATE-PASS' /tmp/gate-verify.txt && [ "$(grep -c 'CHECK-[0-9] PASS' /tmp/gate-verify.txt)" -ge 9 ] && e=.planning/phases/90-quality-gate-and-uat/90-gate-evidence.md && [ -f "$e" ] && grep -q 'GATE-PASS' "$e" && grep -qi 'align-items' "$e" && echo T1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - gate.sh implements all 9 CONTEXT checks, runs every check per invocation, exits 0 only on full pass, prints GATE-PASS sentinel
    - The denylist uses the Phase 88/89 regex verbatim as its base plus the word-bounded CONTEXT extension; the only filter is the documented align-items:baseline CSS exclusion
    - 90-gate-evidence.md contains at least one complete verbatim run ending in GATE-PASS (plus any failing runs that preceded it, labeled Run N)
    - brandbook-fable/ modified only if a check failed; brandbook/ untouched
    - T1-PASS printed
  </acceptance_criteria>
  <done>gate.sh exists, all 9 checks pass in one full run, GATE-PASS run recorded verbatim in 90-gate-evidence.md</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Browser-rendered evidence (GATE-02) — screenshots read + evidence index</name>
  <files>.planning/phases/90-quality-gate-and-uat/90-gate-evidence.md</files>
  <read_first>
    - .planning/phases/89-collateral-specimens-copy/89-visual-audit.md (the established agent-browser/Playwright audit pattern: viewport set, theme toggle, READ each shot)
    - brandbook-fable/assets/favicon.svg (it adapts pane color to OS dark via prefers-color-scheme per [87-02] — dark capture needs colorScheme emulation, not the page toggle)
  </read_first>
  <action>
    Capture screenshots with the installed agent-browser (Playwright-driven) to
    `/tmp/fable-90-evidence/` — outside the repo, so inherently untracked per the
    "ignored tmp dir" requirement. Open pages from file:// (zero network is the point).
    Required set (8 captures):
    1. index-1440-light.png — brandbook-fable/index.html at 1440px width, light theme
    2. index-1440-dark.png — same page after activating the page-level theme toggle (data-theme="dark"); confirm the toggle re-skins specimens, contrast matrix, gallery
    3. index-390-light.png — 390px viewport (mobile reflow, no horizontal overflow)
    4. landing-1440-light.png and landing-1440-dark.png — examples/landing-page.html, both themes via its toggle
    5. email-600.png — examples/email-template.html at 600px viewport
    6. favicon-16-light.png and favicon-16-dark.png — build a tiny throwaway harness HTML in /tmp/fable-90-evidence/ (NOT in the repo) that shows the favicon twice: an img at exactly 16px CSS size and an 8x magnified copy with image-rendering: pixelated, side by side, referencing the asset by absolute file:// path. Capture once normally (light) and once with emulated prefers-color-scheme: dark (agent-browser color-scheme emulation) to exercise the favicon's own media query.

    READ every screenshot (Read tool on each PNG). Verdict per shot: what it shows and
    whether rendering is correct (legible favicon silhouette at 16px in BOTH schemes,
    dark theme actually re-skinned everything visible, mobile not broken, email
    brand-true at 600px). Append to 90-gate-evidence.md a "Browser Evidence (GATE-02)"
    section: an index table of filename | page + viewport + theme | verdict, plus a
    one-line note that /tmp/fable-90-evidence/ is untracked tmp.
    Any rendering defect: apply the fix-loop rule — fix in brandbook-fable/, re-run the
    FULL gate.sh (append as a new run), re-capture and re-read the affected shots.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && [ "$(ls /tmp/fable-90-evidence/*.png 2>/dev/null | wc -l)" -ge 8 ] && e=.planning/phases/90-quality-gate-and-uat/90-gate-evidence.md && for s in index-1440-light index-1440-dark index-390 landing-1440-light landing-1440-dark email-600 favicon-16-light favicon-16-dark; do grep -q "$s" "$e" || { echo "MISSING EVIDENCE ROW: $s"; exit 1; }; done && [ "$(git status --porcelain /tmp 2>/dev/null | wc -l)" -eq 0 ] && git check-ignore -q /tmp/fable-90-evidence 2>/dev/null; [ "$(git status --porcelain | grep -c 'fable-90-evidence')" -eq 0 ] && echo T2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 8 screenshots exist in /tmp/fable-90-evidence/ and each was READ (verdicts are observations, not assumptions)
    - Evidence index in 90-gate-evidence.md has one row per shot with an explicit verdict; favicon confirmed legible at 16px in light AND emulated-dark
    - No screenshot or harness file landed inside the repo
    - T2-PASS printed
  </acceptance_criteria>
  <done>8 screenshots in /tmp/fable-90-evidence/ all READ; evidence index rows with verdicts in 90-gate-evidence.md; favicon legible at 16px in both color schemes</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Maintainer A/B walkthrough package + sign-off pause (GATE-03)</name>
  <files>.planning/phases/90-quality-gate-and-uat/90-01-CHECKPOINT.md</files>
  <read_first>
    - .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md (the 12 DIF rows — checklist source, quote the differentiator names verbatim)
  </read_first>
  <action>
    Author `.planning/phases/90-quality-gate-and-uat/90-01-CHECKPOINT.md` — a ONE-SCREEN
    walkthrough the maintainer can follow without reading anything else:
    1. The two file:// paths to open side by side: /Users/jon/projects/mailglass/brandbook/index.html (codex, frozen at 09a84dd4) vs /Users/jon/projects/mailglass/brandbook-fable/index.html (fable).
    2. The 12 differentiators as a checkbox checklist (DIF-01..DIF-12 from the brief), each with one line on WHERE to look in the fable book (e.g. DIF-03 → header toggle, watch every specimen re-skin; DIF-05 → Contrast Matrix section, ratios recompute on toggle; DIF-11 → Logo System 16px chips + the favicon evidence verdicts).
    3. "What to compare" in 4-5 bullets: toggle both books to dark; check the codex wordmark vs fable outlined wordmark (codex degrades without Avenir Next); compare component galleries (static pictures vs live states); open one example page from each from file://; squint-test both favicons at tab size.
    4. Gate status line: link the GATE-PASS run + browser evidence in 90-gate-evidence.md.
    5. Sign-off block: status: awaiting-maintainer-signoff, empty verdict + punch-list fields, date.
    Then STOP. Do not proceed past this checkpoint, do not mark GATE-03 complete, do not
    close the phase. The maintainer's recorded sign-off closes GATE-03; punch-list items
    are fixed in-phase (fix-loop rule: fix in brandbook-fable/, full gate.sh re-run
    recorded, evidence refreshed) and re-presented BEFORE close.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && c=.planning/phases/90-quality-gate-and-uat/90-01-CHECKPOINT.md && [ -f "$c" ] && [ "$(grep -Ec 'DIF-0[1-9]|DIF-1[0-2]' "$c")" -ge 12 ] && grep -q 'brandbook/index.html' "$c" && grep -q 'brandbook-fable/index.html' "$c" && grep -qi 'awaiting-maintainer-signoff' "$c" && grep -q '90-gate-evidence.md' "$c" && echo T3-READY</automated>
  </verify>
  <decision>A/B verdict on the fable brand book vs the frozen codex baseline — the v1.9 milestone's final human step (GATE-03)</decision>
  <context>GATE-01 and GATE-02 are mechanically proven (see 90-gate-evidence.md). Only the maintainer can judge whether fable beats codex on craft. Winner ADOPTION (folder rename, README propagation) is deferred to a future milestone — this sign-off only closes v1.9.</context>
  <options>
    <option id="approve">
      <name>Sign off — fable is A/B-ready and beats the codex baseline</name>
      <pros>Closes GATE-03 and the v1.9 milestone</pros>
      <cons>None — adoption remains a separate future decision</cons>
    </option>
    <option id="punch-list">
      <name>Conditional — sign off after listed fixes</name>
      <pros>Specific defects get fixed in-phase, full gate re-run, then re-presented</pros>
      <cons>Phase stays open until the punch list clears</cons>
    </option>
  </options>
  <resume-signal>Reply "approve", or "punch list:" followed by numbered items (recorded in 90-01-CHECKPOINT.md; fixes happen in-phase before close)</resume-signal>
  <done>90-01-CHECKPOINT.md presented; maintainer verdict recorded (approve, or punch list fixed in-phase + re-presented + approved); GATE-03 closed only by that recorded sign-off</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brandbook-fable/ → public repo readers | shipped artifacts must carry zero process/internal vocabulary and zero network behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Information Disclosure | all brandbook-fable files | mitigate | Check 4 denylist (extended, word-bounded, single documented CSS exclusion) → zero hits |
| T-90-02 | Tampering | HTML/SVG/CSS artifacts | mitigate | Check 3 zero external URLs/fetch/script-src/@import; pages proven from file:// in Task 2 |
| T-90-03 | Tampering | frozen brandbook/ baseline | mitigate | Check 9 git diff vs 09a84dd4 + commit-range scope check |
| T-90-SC | Tampering | package installs | accept | No npm/pip/cargo/hex installs in this phase — bash + xmllint + python3 stdlib + installed agent-browser only |
</threat_model>

<verification>
- gate.sh full run ends in GATE-PASS; every run (including failures) recorded verbatim in 90-gate-evidence.md
- 8 screenshots captured to /tmp/fable-90-evidence/, READ, and indexed with verdicts
- 90-01-CHECKPOINT.md presents the A/B package and the phase is PAUSED at awaiting-maintainer-signoff
</verification>

<success_criteria>
- GATE-01: scripted gate passes all 9 checks on the final brandbook-fable/ state
- GATE-02: browser evidence confirms light/dark rendering + 16px favicon legibility in both color schemes; nothing outside brandbook-fable/ + .planning/ changed since 09a84dd4
- GATE-03: maintainer sign-off recorded in 90-01-CHECKPOINT.md (phase does NOT close before it; punch-list items fixed in-phase first)
</success_criteria>

<output>
Create `.planning/phases/90-quality-gate-and-uat/90-01-SUMMARY.md` when done (after maintainer sign-off, including punch-list resolution if any)
</output>
