---
phase: 123-cross-surface-coherence-ratchet-re-arm
plan: 03
type: execute
wave: 2
depends_on: ["123-01", "123-02"]
files_modified:
  - .planning/research/v1.14/DEFECT-REGISTER.md
autonomous: true
requirements: [COH-01]
must_haves:
  truths:
    - "Every catalogued defect in DEFECT-REGISTER.md has its Status flipped from CATALOGUED to RESOLVED/FIXED (or HELD for the guardrail findings) with a maintainer sign-off note, backed by the green automated floor"
    - "The persona-critic re-run verdict is recorded in DEFECT-REGISTER.md using the five hats + three personas as fixed verbatim vocabulary, judged against the Apple-deliberate-IA bar"
    - "The persona re-shoot is evidence only — screenshots stay gitignored, no new ratchet cell, MailglassDemo.Personas.spec/0 untouched"
    - "No new automated coherence-score gate is added"
  artifacts:
    - ".planning/research/v1.14/DEFECT-REGISTER.md (per-finding Status closures + appended coherence sign-off section)"
  key_links:
    - "DEFECT-REGISTER closeout ← green automated floor (ratchet 54-cell + 9-cell axe + 24-item Bucket-A + persona-drift + the two armed judgment gates from Plan 01)"
    - "persona-critic re-run ← reference/demo_app/assets/e2e/persona-screenshots.spec.js producer against make demo (evidence only)"
---

<objective>
Prove cross-surface coherence by re-running the adversarial persona-critic harness across all four surfaces and recording the verdict + per-defect closures in DEFECT-REGISTER.md — the COH-01 coherence-proof deliverable.

Purpose: Coherence is a human-judgment property the MILESTONE-SEED says gates cannot express. The auditable evidence Phase 124's milestone audit cites for COH-01 is the adversarial persona-critic verdict + maintainer sign-off + the green automated floor. This plan closes the single traceable ledger (DEFECT-REGISTER.md) rather than spawning a sibling audit doc.

Output: Per-finding Status closures + an appended coherence sign-off section in DEFECT-REGISTER.md. NO new automated gate. NO new persona cells. NO Personas.spec/0 change.

Runs after Plan 01 (floor confirmed green) and Plan 02 (review surfaces finalized) so the verdict can cite both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md

@.planning/research/v1.14/DEFECT-REGISTER.md
@.planning/research/v1.14/STRESS-TEST-PROMPT.md
@.planning/research/v1.14/MILESTONE-SEED.md
@reference/demo_app/assets/e2e/persona-screenshots.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the persona-critic harness as coherence evidence (D-06/D-07)</name>
  <files>.planning/research/v1.14/.cache/screenshots (gitignored evidence only — not committed)</files>
  <read_first>
    - reference/demo_app/assets/e2e/persona-screenshots.spec.js (the re-shoot producer — the rerun command + cell matrix)
    - .planning/research/v1.14/STRESS-TEST-PROMPT.md (the binding Apple-deliberate-IA persona-critic rubric — redundancy/earns-its-place/least-surprise/hierarchy/microcopy/cross-surface-consistency; do not dilute)
    - .planning/research/v1.14/DEFECT-REGISTER.md (the catalogued findings + their cells; the five hats vocabulary at lines 24-25, three personas at lines 26-27)
    - .planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md (D-06/D-07 + specifics: the five hats and three personas are fixed verbatim vocabulary)
  </read_first>
  <action>
    Re-run the adversarial persona-critic harness across all four surfaces (Overview/shell, Deliveries, Inbound, Preview) as EVIDENCE for the coherence verdict — not a new ratchet cell (D-07). Steps:
    (1) Boot the demo (make demo) and re-run the existing persona-screenshots producer in one pass to capture the post-119-122 visual delta. The producer is reference/demo_app/assets/e2e/persona-screenshots.spec.js; the rerun invocation is the one documented in DEFECT-REGISTER.md near line 294 (a DEMO_BASE_URL + DEMO_EVIDENCE_RESET_TOKEN env plus npx playwright test against e2e/persona-screenshots.spec.js). Screenshots land in the gitignored .planning/research/v1.14/.cache/screenshots/ cache — working evidence only, never committed, never a pixel-diff baseline (118 D-02).
    (2) Do NOT add new persona cells and do NOT touch MailglassDemo.Personas.spec/0 (reference/persona_spec/personas.ex) — the persona-drift-guard treats it as single source of truth and any change fails by triplication drift (D-07). If the demo or producer cannot run in this environment, record that fact and base the verdict on the existing .cache screenshots plus the green automated floor — do NOT block on a re-shoot.
    (3) Adversarially walk each surface against the STRESS-TEST-PROMPT rubric using the five hats verbatim (dev-evaluator, library-integrator, maintainer-debugging, operator/on-call-SRE-under-stress, security-reviewer) and three personas verbatim (northstar, fjordline-aps, helios-void), judged against the Apple-deliberate-IA bar — cross-surface spacing/hierarchy/IA/microcopy/motion consistency, not just WCAG nits. Capture each surface's verdict for Task 2 to record.
    This task produces evidence + a verdict for Task 2; it commits no code and no screenshots.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && (git status --porcelain reference/persona_spec/personas.ex | grep -q . && echo "FAIL personas.ex changed" || echo "OK personas-untouched")</automated>
  </verify>
  <acceptance_criteria>
    - reference/persona_spec/personas.ex (MailglassDemo.Personas.spec/0) is unchanged (git status shows no modification)
    - No screenshots staged for commit (.planning/research/v1.14/.cache/ stays gitignored)
    - A per-surface coherence verdict exists (captured for Task 2), framed with the five hats + three personas verbatim against the Apple-deliberate-IA bar
    - No new automated gate / test file added
  </acceptance_criteria>
  <done>Persona-critic harness re-run (or, if unrunnable here, verdict grounded in existing .cache evidence + green floor); Personas.spec/0 + screenshots untouched/uncommitted; a per-surface verdict captured.</done>
</task>

<task type="auto">
  <name>Task 2: Close out DEFECT-REGISTER — flip Status fields + append the coherence sign-off (D-06/D-14)</name>
  <files>.planning/research/v1.14/DEFECT-REGISTER.md</files>
  <read_first>
    - .planning/research/v1.14/DEFECT-REGISTER.md (every finding's `- **Status:** CATALOGUED ...` line: D-NAV-ACTIVE :101, D-NAV-DUP :127, D-ORIENT-REDUNDANT :150, D-OVERVIEW-SIGNPOST :174, D-FILTERS-ON-EMPTY :197, D-LABEL-TRIPLING :213, D-MOBILE-INFODUMP :226, D-THEME-PARITY :240 guardrail, D-STORYBOOK-BRAND :258, D-STORYBOOK-STALE-BOOT :274)
    - .planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md (D-06: single-ledger closure, no sibling COHERENCE-AUDIT.md; D-14 paired-test discipline)
    - The Task 1 per-surface verdict (the sign-off content)
  </read_first>
  <action>
    Record the coherence proof in DEFECT-REGISTER.md as the single traceable ledger (D-06 — chosen over a fresh sibling COHERENCE-AUDIT.md). Two edits:
    (a) Flip each finding's `Status:` line. For the headline + IA defects resolved by 119-122 (D-NAV-ACTIVE, D-NAV-DUP, D-ORIENT-REDUNDANT, D-OVERVIEW-SIGNPOST, D-FILTERS-ON-EMPTY, D-LABEL-TRIPLING, D-MOBILE-INFODUMP) change `CATALOGUED (fix = Phase NNN)` to `RESOLVED (Phase NNN) — verified Phase 123 cross-surface coherence re-run; backed by green floor` with a brief per-finding maintainer sign-off note citing the resolving phase + the persona-critic verdict. For the two guardrail/cosmetic findings: D-THEME-PARITY → `HELD (guardrail) — light/dark/system parity confirmed across all four surfaces Phase 123; 54-cell + 9-cell axe floor green`; D-STORYBOOK-BRAND → `RESOLVED (accepted dev-only cosmetic, Phase 123 D-09) — indigo explorer chrome accepted; no dep CSS / Node build`; D-STORYBOOK-STALE-BOOT → `RESOLVED (docs, Phase 123 D-10) — caveat added to guides/run-the-demo.md`.
    (b) Append a short "## Phase 123 — Cross-surface coherence sign-off" section at the end of the file recording: the per-surface verdict from Task 1 across all four surfaces; the five hats + three personas used verbatim; the explicit statement that coherence is backed by the green automated floor (ratchet 54-cell + 9-cell axe + 24-item Bucket-A + persona-drift + the two armed judgment gates) and the persona-critic verdict, NOT a new automated coherence-score gate (D-06); and a maintainer sign-off line. Format is the executor's discretion (inline-vs-appended-section) per D-06; keep it within the single DEFECT-REGISTER ledger.
    D-14: confirm no voice_test.exs or e2e spec greps any DEFECT-REGISTER copy (it is a .planning research doc, not a tested string — low risk; verify).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && ! grep -q "CATALOGUED" .planning/research/v1.14/DEFECT-REGISTER.md && grep -q "Cross-surface coherence sign-off" .planning/research/v1.14/DEFECT-REGISTER.md && echo CLOSED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CATALOGUED" .planning/research/v1.14/DEFECT-REGISTER.md` returns 0 (every finding's Status flipped)
    - DEFECT-REGISTER.md contains a "Phase 123 — Cross-surface coherence sign-off" section
    - The sign-off section names the five hats verbatim (dev-evaluator, library-integrator, maintainer-debugging, operator/on-call-SRE-under-stress, security-reviewer) and the three personas verbatim (northstar, fjordline-aps, helios-void)
    - The sign-off explicitly states no new automated coherence-score gate was added and cites the green floor
    - No sibling COHERENCE-AUDIT.md file created (closure stays in DEFECT-REGISTER.md)
    - No source/test file changed by this task (only DEFECT-REGISTER.md)
  </acceptance_criteria>
  <done>Every DEFECT-REGISTER finding's Status is closed (RESOLVED/HELD) with a sign-off note; a Phase 123 coherence sign-off section is appended with verbatim hats/personas + green-floor citation; single-ledger, no new gate.</done>
</task>

</tasks>

<artifacts_produced>
This plan produces these precise edits:
- **.planning/research/v1.14/DEFECT-REGISTER.md** — all 10 findings' `Status:` lines flipped from CATALOGUED to RESOLVED/HELD with per-finding maintainer sign-off notes; a new appended "## Phase 123 — Cross-surface coherence sign-off" section recording the per-surface verdict, the five hats + three personas verbatim, the green-floor citation, and a maintainer sign-off line.
- **Evidence (uncommitted):** re-shot screenshots under the gitignored .planning/research/v1.14/.cache/screenshots/ cache.
- **NOT touched:** reference/persona_spec/personas.ex (MailglassDemo.Personas.spec/0 — D-07), any test/gate file (no new automated coherence gate — D-06), no sibling COHERENCE-AUDIT.md.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| persona spec → drift guard | Editing Personas.spec/0 fails the triplication drift guard; it is the single source of truth |
| coherence claim → audit trail | Phase 124's milestone audit cites this ledger; a "looks coherent" claim without the hats/personas/floor citation is not auditable |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-07 | Tampering | MailglassDemo.Personas.spec/0 | medium | mitigate | D-07 — do NOT touch personas.ex; acceptance criterion asserts zero git change; drift guard fails loudly otherwise |
| T-123-08 | Repudiation | coherence audit trail | high | mitigate | D-06 — sign-off uses verbatim hats/personas + cites the green floor; auditable evidence, not a bare claim; single ledger |
| T-123-09 | Information disclosure | gitignored screenshot cache | low | accept | screenshots are gitignored working evidence (118 D-02); never committed |
</threat_model>

<verification>
- `grep -c "CATALOGUED" .planning/research/v1.14/DEFECT-REGISTER.md` returns 0
- DEFECT-REGISTER.md carries the Phase 123 coherence sign-off with verbatim hats/personas + green-floor citation
- reference/persona_spec/personas.ex unchanged; no screenshots committed; no new gate/test file
</verification>

<success_criteria>
COH-01 coherence-proof deliverable satisfied: the adversarial persona-critic re-run verdict + per-defect Status closures + maintainer sign-off are recorded in the single DEFECT-REGISTER ledger, backed by the green automated floor, using the fixed five-hats/three-personas vocabulary against the Apple-deliberate-IA bar — with no new automated coherence gate, no new persona cells, and Personas.spec/0 untouched.
</success_criteria>

<output>
Create `.planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-03-SUMMARY.md` when done
</output>
