---
phase: 123-cross-surface-coherence-ratchet-re-arm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/storybook/foundations.story.exs
  - reference/demo_app/storybook/primitives
  - guides/run-the-demo.md
autonomous: true
requirements: [COH-01]
must_haves:
  truths:
    - "The storybook story inventory (foundations + the 5 primitive stories) is consistent with the shipped admin primitives in components.ex; any primitive the 119-122 redesigns introduced/changed has a story"
    - "/dev/mail/gallery (gallery_live.ex) is byte-unchanged"
    - "The indigo phoenix_storybook explorer chrome is recorded as accepted dev-only cosmetic (no dep CSS edit, no Node build)"
    - "The run-the-demo DX documents the storybook-needs-fresh-make-demo-boot caveat"
  artifacts:
    - "guides/run-the-demo.md (D-STORYBOOK-STALE-BOOT caveat added to Troubleshooting)"
    - "Storybook finalization record (D-09 accepted-indigo + D-11 inventory verdict) captured in this plan's SUMMARY"
    - "Any new reference/demo_app/storybook/primitives/*.story.exs for a 119-122-introduced primitive (only if one is missing)"
  key_links:
    - "storybook stories ← MailglassAdmin.Components primitive function defs (nav_link/nav_pill/tenant_chip/theme_picker/stat_card)"
    - "guides/run-the-demo.md ← reference/demo_app/README.md links to it for troubleshooting"
---

<objective>
Finalize the dev-only storybook + /dev/mail/gallery review surfaces so they are consistent with the shipped UI — the COH-01 review-surface deliverable.

Purpose: Verify the storybook story inventory matches the post-119-122 primitives, accept the indigo explorer chrome as dev-only cosmetic (no dep CSS / no Node build), document the stale-boot DX caveat, and keep the gallery specimen surface byte-unchanged. This is what "finalized" means: consistent with the redesign, not exhaustive.

Output: Verified/extended story inventory; a DX caveat in guides/run-the-demo.md; a finalization record in the SUMMARY. NO HEEx change, NO asset rebuild, NO gallery edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md

@reference/demo_app/storybook/foundations.story.exs
@reference/demo_app/lib/mailglass_demo_web/storybook.ex
@guides/run-the-demo.md
@brandbook/brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Story-inventory completeness check + accept indigo explorer chrome (D-08/D-09/D-11)</name>
  <files>reference/demo_app/storybook/primitives, reference/demo_app/storybook/_primitives.index.exs, reference/demo_app/storybook/foundations.story.exs</files>
  <read_first>
    - reference/demo_app/storybook/foundations.story.exs (the :page story documenting the sandbox + theme-bridge contract)
    - reference/demo_app/storybook/primitives/nav_link.story.exs, nav_pill.story.exs, tenant_chip.story.exs, stat_card.story.exs, theme_picker.story.exs (the 5 existing primitive stories)
    - reference/demo_app/storybook/_primitives.index.exs (the primitives index)
    - mailglass_admin/lib/mailglass_admin/components.ex (primitive function defs: nav_link, nav_pill, tenant_chip, theme_picker, stat_card, card — to compare against the story set)
    - .planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md (D-08/D-09/D-11) + .planning/research/v1.14/DEFECT-REGISTER.md (D-STORYBOOK-BRAND entry, lines 246-258)
  </read_first>
  <action>
    Verify and, only if needed, extend the storybook story inventory so it is consistent with the shipped admin primitives — "finalized" means consistent with the redesign, not exhaustive (D-11). Steps:
    (1) Cross-reference the 5 existing primitive stories (nav_link, nav_pill, tenant_chip, stat_card, theme_picker) against the primitive function defs in mailglass_admin/lib/mailglass_admin/components.ex. For each existing story, confirm its `function`, `variations`, and theme-bridge template still match the current component signature/behavior (attribute names, active/disabled handling, theme keying). Fix any story whose variation attributes drifted from the current component contract.
    (2) Determine whether the 119-122 redesigns introduced or changed any NEW operator/admin primitive that is a brand primitive (a reusable theme-sensitive component like the existing five) but lacks a story. Only add a `*.story.exs` (under reference/demo_app/storybook/primitives/, registered in _primitives.index.exs, following the nav_pill.story.exs shape: `use PhoenixStorybook.Story, :component`, `function/0`, light+dark template-level data-theme bridge, representative variations) if such a primitive exists. Do NOT add stories for generic containers (e.g. `card`) or for every component — the foundations doc fixes the brand-primitive set at the five; over-scoping is explicitly out of scope (D-11). If the inventory is already complete, record that verdict in the SUMMARY and add NO files.
    (3) D-09: Accept the indigo phoenix_storybook explorer chrome as-is. It is the dep's prebuilt explorer-shell CSS (priv/static/css/phoenix_storybook-*.css), distinct from the sandbox we already style via css_path. Do NOT edit the dep CSS and do NOT add a Node/esbuild storybook.css build (both violate the zero-Node adopter guarantee and 118 D-07). If a clean config-only explorer-theme hook exists in the installed phoenix_storybook version, you MAY set the title/accent via config only — but do NOT build or edit CSS to chase it. Record the accept-indigo decision in the SUMMARY's finalization record.
    (4) gallery_live.ex stays byte-unchanged (D-08) — it is the ratchet specimen surface; do not touch it.
    Do NOT run `mix assets.build` (D-12).
  </action>
  <verify>
    <automated>cd reference/demo_app && mix compile --warnings-as-errors 2>&1 | tail -5; git diff --stat -- mailglass_admin/lib/mailglass_admin/gallery_live.ex mailglass_admin/priv/static/</automated>
  </verify>
  <acceptance_criteria>
    - Every reference/demo_app/storybook/primitives/*.story.exs registered in _primitives.index.exs compiles (`cd reference/demo_app && mix compile --warnings-as-errors` exits 0)
    - The story set covers each shipped brand primitive; SUMMARY states the inventory verdict (complete as-is, OR names each newly added story + the 119-122 primitive it covers)
    - `git diff --stat mailglass_admin/lib/mailglass_admin/gallery_live.ex` shows zero changes (D-08 byte-unchanged)
    - No phoenix_storybook dep CSS edited; no Node/esbuild build added (git diff shows no changes under reference/demo_app/deps/ or any new assets build config)
    - `git diff --stat mailglass_admin/priv/static/` empty (no asset rebuild committed)
    - SUMMARY records the accept-indigo D-09 finalization decision
  </acceptance_criteria>
  <done>Story inventory verified consistent with shipped primitives (extended only if a 119-122 primitive was missing); indigo explorer chrome accepted as dev-only cosmetic in the finalization record; gallery + priv/static untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Document the storybook stale-boot DX caveat (D-10/D-14)</name>
  <files>guides/run-the-demo.md</files>
  <read_first>
    - guides/run-the-demo.md (the Troubleshooting section, lines 118-131 — the least-surprise home for a boot caveat)
    - reference/demo_app/README.md (links to guides/run-the-demo.md for troubleshooting)
    - .planning/research/v1.14/DEFECT-REGISTER.md (D-STORYBOOK-STALE-BOOT entry, lines 260-274 — the exact failure mode: /dev/storybook 500s with PhoenixStorybook.Router UndefinedFunctionError under hot-reload until a fresh make demo)
    - .planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-CONTEXT.md (D-10/D-14)
  </read_first>
  <action>
    Add the D-STORYBOOK-STALE-BOOT caveat to the run-the-demo DX as a docs-only note (D-10). Add a new Troubleshooting bullet in guides/run-the-demo.md (within the "## Troubleshooting" section, lines 118+) explaining: the /dev/storybook live-route needs a fresh `make demo` (or `docker restart`) after the phoenix_storybook dep was added — a demo container already running from before that commit cannot hot-reload the live_storybook macro and returns a 500 (PhoenixStorybook.Router UndefinedFunctionError); the operator surfaces hot-reload fine, only the storybook live-route needs a clean boot. Keep the voice on-brand (clear, exact, a thoughtful maintainer — match the existing Troubleshooting bullets' tone). No code change.
    D-14 paired-test discipline: confirm no voice_test.exs or e2e spec greps the exact copy you add (the surface is a guides/*.md doc, not a tested string — low risk, but verify before committing).
  </action>
  <verify>
    <automated>grep -qi "storybook" guides/run-the-demo.md && grep -qi "make demo\|restart\|fresh" guides/run-the-demo.md && echo CAVEAT-PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - guides/run-the-demo.md Troubleshooting section contains a bullet naming /dev/storybook + the fresh-make-demo/restart requirement after the dep was added
    - No code files changed by this task (only guides/run-the-demo.md)
    - `git grep` of any voice_test.exs / e2e spec shows no assertion greps the new doc copy (paired-test discipline confirmed)
  </acceptance_criteria>
  <done>guides/run-the-demo.md documents the storybook-needs-fresh-boot caveat as a Troubleshooting bullet; no code change; no paired test invalidated.</done>
</task>

</tasks>

<artifacts_produced>
This plan produces these precise edits:
- **reference/demo_app/storybook/primitives/*.story.exs** — only IF a 119-122-introduced brand primitive lacks a story (added following the nav_pill.story.exs shape + registered in _primitives.index.exs); otherwise the inventory is verified complete and NO files are added.
- **reference/demo_app/storybook/*.story.exs** — any drifted variation attributes corrected to match the current component contract (only if drift found).
- **guides/run-the-demo.md** — new Troubleshooting bullet for the D-STORYBOOK-STALE-BOOT caveat (storybook live-route needs fresh make demo after the dep was added).
- **SUMMARY finalization record** — D-09 accept-indigo decision + D-11 story-inventory verdict (complete-as-is or the named additions).
- **NOT touched:** mailglass_admin/lib/mailglass_admin/gallery_live.ex (D-08 byte-unchanged), phoenix_storybook dep CSS (D-09), mailglass_admin/priv/static/app.css (D-12), no Node build added.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| story variation attrs → component contract | A story whose attributes drift from the shipped component misrepresents the primitive to reviewers |
| doc copy → grepped assertions | New DX copy could collide with a grepping test (Pitfall-2) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-04 | Spoofing | storybook stories vs shipped primitives | low | mitigate | cross-reference each story against components.ex defs; mix compile --warnings-as-errors gates structural correctness |
| T-123-05 | Tampering | gallery_live.ex specimen surface | medium | mitigate | D-08 — gallery stays byte-unchanged; acceptance criterion asserts zero git diff |
| T-123-06 | Tampering | committed asset bundle | medium | mitigate | D-12 — no mix assets.build; priv/static byte-unchanged asserted |
</threat_model>

<verification>
- `cd reference/demo_app && mix compile --warnings-as-errors` exits 0 (all stories compile)
- gallery_live.ex + priv/static byte-unchanged (zero git diff)
- guides/run-the-demo.md documents the stale-boot caveat
- SUMMARY carries the D-09 accept-indigo + D-11 inventory finalization record
</verification>

<success_criteria>
COH-01 review-surface deliverable satisfied: storybook story inventory consistent with the shipped primitives (extended only where a 119-122 primitive was missing); indigo explorer chrome accepted as dev-only cosmetic; stale-boot DX caveat documented; gallery byte-unchanged; no dep CSS / Node build / asset rebuild.
</success_criteria>

<output>
Create `.planning/phases/123-cross-surface-coherence-ratchet-re-arm/123-02-SUMMARY.md` when done
</output>
