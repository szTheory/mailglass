---
phase: 156-delivery-correctness-and-bounded-execution
plan: 03
type: execute
wave: 3
depends_on: [156-02]
files_modified:
  - lib/mailglass/application.ex
  - lib/mailglass/errors/send_error.ex
  - lib/mailglass/outbound.ex
  - lib/mailglass/outbound/async_adapter/task_supervisor.ex
  - mailglass_inbound/lib/mailglass_inbound/application.ex
  - mailglass_inbound/lib/mailglass_inbound/execution.ex
  - test/mailglass/application_test.exs
  - test/mailglass/error_test.exs
  - test/mailglass/outbound/deliver_later_test.exs
  - test/mailglass/outbound/deliver_many_test.exs
  - mailglass_inbound/test/mailglass_inbound/async_execution_test.exs
autonomous: true
requirements: [EXEC-04]
must_haves:
  truths:
    - "Core and inbound Task supervisors admit at most ten concurrent children by default."
    - "Single and batch core fallback return success only for an accepted spawn; saturation or supervisor failure returns `Mailglass.SendError` with type `:dispatch_unavailable`."
    - "Inbound fallback reports the same typed unavailable result instead of raw supervisor reasons or queued success."
    - "Spawn refusal leaves a truthful failed projection/event rather than a queued row with no executing task."
  artifacts:
    - path: "lib/mailglass/errors/send_error.ex"
      provides: "Additive dispatch-unavailable type and retry_class field without JSON-shape change"
    - path: "lib/mailglass/outbound/async_adapter/task_supervisor.ex"
      provides: "Explicit result-bearing core fallback admission"
    - path: "mailglass_inbound/lib/mailglass_inbound/execution.ex"
      provides: "Honest inbound fallback admission normalization"
  key_links:
    - from: "lib/mailglass/outbound.ex"
      to: "lib/mailglass/outbound/async_adapter/task_supervisor.ex"
      via: "single and batch fallbacks branch on every dispatch return"
      pattern: "AsyncAdapter.dispatch"
    - from: "lib/mailglass/application.ex"
      to: "Mailglass.TaskSupervisor"
      via: "supervisor child spec declares max_children: 10"
      pattern: "max_children"
    - from: "mailglass_inbound/lib/mailglass_inbound/execution.ex"
      to: "MailglassInbound.TaskSupervisor"
      via: "inbound result reports best-effort queued only after accepted start_child"
      pattern: "start_child"
---

<objective>
Make non-durable Task-supervisor fallback bounded and truthful for core and inbound under saturation and supervisor failure.

Purpose: Prevent overload from consuming unbounded processes or being misreported as queued work.
Output: Ten-child supervisors, additive typed dispatch errors, result-aware fallback paths, and deterministic barrier saturation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-CONTEXT.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-RESEARCH.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-VALIDATION.md
@.planning/phases/156-delivery-correctness-and-bounded-execution/156-02-SUMMARY.md
@lib/mailglass/outbound/async_adapter.ex
@lib/mailglass/outbound/async_adapter/task_supervisor.ex
@lib/mailglass/errors/send_error.ex
@mailglass_inbound/lib/mailglass_inbound/execution.ex

<interfaces>
`Mailglass.Outbound.AsyncAdapter.dispatch/2` returns `{:ok, pid()} | {:error, term()}` for the Task-supervisor implementation. The published outbound operations retain their existing success/error outer shapes. `Mailglass.SendError` currently serializes only `type`, `message`, and `context`; D-07 adds the struct field `retry_class` but that field must remain excluded from JSON so existing serialized fields are unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject the eleventh core fallback dispatch honestly</name>
  <reversibility rating="costly">D-06/D-07 tighten a public failure contract additively and bound a shared runtime resource.</reversibility>
  <files>lib/mailglass/application.ex, lib/mailglass/errors/send_error.ex, lib/mailglass/outbound.ex, lib/mailglass/outbound/async_adapter/task_supervisor.ex, test/mailglass/application_test.exs, test/mailglass/error_test.exs, test/mailglass/outbound/deliver_later_test.exs, test/mailglass/outbound/deliver_many_test.exs</files>
  <behavior>
    - D-06: ten children held on a receive barrier are accepted; the next single delivery returns `{:error, %Mailglass.SendError{type: :dispatch_unavailable}}` without reporting queued.
    - A missing/dead supervisor and an injected `start_child` error produce the same typed failure rather than raising or being discarded.
    - Batch fallback inspects each spawn: admitted entries remain queued/best-effort, refused entries become failed with a failure event and serializable dispatch-unavailable error.
    - D-07: `SendError.__types__/0` includes `:dispatch_unavailable`; the struct accepts `retry_class: :transient | :permanent | nil`; Jason output retains exactly `type`, `message`, and `context`.
  </behavior>
  <action>Write barrier-based single/batch saturation tests and the additive error contract test first. Configure `Mailglass.TaskSupervisor` with `max_children: 10`. Add `:dispatch_unavailable` and validated `retry_class` construction to `Mailglass.SendError`, defaulting to nil and preserving its existing encoded fields. Make the Task-supervisor adapter normalize `Task.Supervisor.start_child/2` returns and supervisor exits into explicit errors. In both `enqueue_task_supervisor/3` and the batch fallback, inspect every dispatch result before returning a delivery as queued. On refusal, create the typed error, persist the delivery projection as failed with the existing failed-event path, and return the public error for single delivery or the failed per-message delivery for a batch. Use release messages and `on_exit` cleanup for all held tasks; replace permissive queued-or-sent/sleep assertions in the edited paths with deterministic acknowledgements.</action>
  <verify>
    <automated>mix test test/mailglass/application_test.exs test/mailglass/error_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/deliver_many_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The eleventh core task, dead supervisor, and injected spawn error are never reported queued, and every failed admission has a typed additive error plus truthful persisted state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the ten-child and typed failure contract to inbound</name>
  <files>mailglass_inbound/lib/mailglass_inbound/application.ex, mailglass_inbound/lib/mailglass_inbound/execution.ex, mailglass_inbound/test/mailglass_inbound/async_execution_test.exs</files>
  <behavior>
    - D-06: `MailglassInbound.TaskSupervisor` declares `max_children: 10`, and a real held-child barrier rejects the eleventh dispatch.
    - An accepted inbound spawn preserves `{status: :queued, mode: :task_supervisor, durability: :best_effort}`.
    - Saturation, injected error, exit, or absent supervisor returns `{:error, %Mailglass.SendError{type: :dispatch_unavailable}}` with privacy-safe reason class and no queued claim.
  </behavior>
  <action>Add real-supervisor barrier tests and injected failure/exit tests first, preserving the existing fake execution seam. Set the inbound Task supervisor's default child cap to ten. Normalize all fallback admission failures through the additive core `Mailglass.SendError` contract because inbound already depends on core, while retaining inbound's public execution facade and best-effort success metadata. Catch only the supervisor admission boundary; do not swallow failures from accepted task execution, change durable Oban routing, or couple inbound startup to the core supervisor.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/async_execution_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Inbound admits no more than ten fallback tasks and distinguishes accepted best-effort execution from every spawn-unavailable outcome through the same typed additive error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → Task supervisor | Work admission consumes node processes and may fail under local saturation. |
| Spawn result → persisted/public status | A rejected task must not become an unverifiable queued claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-07 | Denial of Service | core/inbound Task supervisors | high | mitigate | Default `max_children: 10` and real barrier saturation tests. |
| T-156-08 | Repudiation | fallback acknowledgement | high | mitigate | Inspect every spawn result and persist/return dispatch-unavailable on refusal. |
| T-156-09 | Information Disclosure | dispatch failure context | medium | mitigate | Context contains only a finite reason class and excludes work payload/recipient fields. |
</threat_model>

<verification>
Run both focused fallback suites and `mix format --check-formatted`; no saturation assertion may use a sleep or accept multiple final states.
</verification>

<success_criteria>
EXEC-04 and the D-07 additive contract required by it are proven in core and inbound without removing Task fallback or changing durable/default routing.
</success_criteria>

<output>
Create `.planning/phases/156-delivery-correctness-and-bounded-execution/156-03-SUMMARY.md` when done.
</output>
