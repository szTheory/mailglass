---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "02"
type: execute
wave: 2
depends_on:
  - 150-01
files_modified:
  - lib/mailglass/outbound.ex
  - lib/mailglass/optional_deps/oban.ex
  - test/mailglass/outbound/deliver_later_test.exs
  - test/mailglass/outbound/deliver_many_test.exs
autonomous: true
requirements:
  - ENVL-01
  - ENVL-02
  - ENVL-04
  - ENVL-05
must_haves:
  truths:
    - "D-04/ENVL-04 [boundary]: preflight, render, compliance/tracking preparation, delivery-ID stamping, and adapter-route selection finish before envelope dump or any database write."
    - "D-04/ENVL-04 [empty]: a prepared message with supported empty optional collections needs no template assigns/process state after enqueue and retries never refill omitted values from live configuration."
    - "D-04/ENVL-04 [encoding/precision]: the exact V1 version, adapter ref, digest, rendered Unicode/body bytes, and attachment bytes selected at enqueue are the immutable retry input."
    - "D-09/ENVL-05 [unclassified resolved]: Delivery, queued Event, Payload, and canonical Oban Job commit in one ordered prefix-aware Multi or all roll back."
    - "D-11/ENVL-05: every eligible `deliver_many/2` item reuses the same per-envelope transaction while preserving ordered per-message public results and replay semantics."
    - "D-01/D-02/D-10/ENVL-01: new Delivery metadata contains only adopter public metadata, Event/job/log/telemetry contain no private content, and job args are exactly delivery_id plus mailglass_tenant_id."
  artifacts:
    - path: lib/mailglass/outbound.ex
      provides: "Reusable four-part atomic durable enqueue for single and batch APIs"
    - path: test/mailglass/outbound/deliver_later_test.exs
      provides: "Success/privacy/order and payload/job failure rollback proof"
    - path: test/mailglass/outbound/deliver_many_test.exs
      provides: "Per-envelope atomic batch and ordered result proof"
  key_links:
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/outbound/envelope.ex
      via: "dump occurs after prepare_outbound_message and resolve_async_adapter_ref"
      pattern: "Envelope\.dump"
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/outbound/payload.ex
      via: "`:payload` Multi insert is ordered after queued Event and before `:job`"
      pattern: "event_queued.*payload.*job"
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/optional_deps/oban.ex
      via: "Oban job changeset is added inside the same Repo.multi transaction"
      pattern: "OptionalDeps\.Oban\.insert"
  prohibitions:
    - statement: "A durable request must not report queued if any one of Delivery, queued Event, private Payload, or Oban Job is absent."
      status: unverified
      flagged: true
---

<objective>
Wire the private envelope into the real durable APIs so single and batch sends commit Delivery, queued Event, Payload, and canonical job atomically per envelope, with no private reconstruction data on public surfaces per D-01/D-02/D-04/D-09 through D-11.

Purpose: Close the stranded-work and false-queued windows in the actual public enqueue paths.
Output: One reusable atomic enqueue helper and deterministic rollback/privacy integration tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-PATTERNS.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VALIDATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-01-SUMMARY.md
@lib/mailglass/outbound.ex
@test/mailglass/outbound/deliver_later_test.exs
@test/mailglass/outbound/deliver_many_test.exs
</context>

## Artifacts this phase produces

- A package-private `Mailglass.Outbound` atomic enqueue helper that accepts a prepared message, persisted adapter ref, and dumped envelope.
- Named `Ecto.Multi` operations `:delivery`, `:event_queued`, `:payload`, and `:job` in that exact order.
- Test-only database failure injectors `with_payload_insert_failure/1` and `with_oban_job_insert_failure/1` in `deliver_later_test.exs` using temporary prefixed CHECK constraints, not production flags.
- Per-envelope `deliver_many/2` orchestration replacing `insert_batch/1`, `enqueue_batch_jobs/1`, and post-commit job insertion for Oban-selected work.

<tasks>

<task type="auto" tdd="true">
  <name>Task 150-02-01: Commit one durable request as four inseparable facts</name>
  <reversibility rating="costly">D-09 defines the durable enqueue ordering relied on by worker and recovery code, though the helper remains package-private.</reversibility>
  <files>lib/mailglass/outbound.ex, lib/mailglass/optional_deps/oban.ex, test/mailglass/outbound/deliver_later_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-01/D-02/D-04/D-09/D-10; `150-RESEARCH.md` Pattern 3 and Pitfall 1; `150-PATTERNS.md` Outbound assignment; Plan 01 summary/interfaces; `do_deliver_later/2`, `enqueue_via_async_adapter/3`, `enqueue_oban/3`, `base_delivery_attrs/3`, `prepare_outbound_message/1`, current `Mailglass.OptionalDeps.Oban.insert/3`, `Mailglass.Repo.multi_opts/1`, and installed Oban 2.23 `Oban.insert/4`, `Oban.Engine.insert_job/5`, and `Oban.Repo.default_options/1`/dispatch behavior; then read existing Oban assertions in the modified test.</read_first>
  <behavior>
    - Success: a fully supported public `deliver_later/2` request creates exactly one Delivery, queued Event, Payload, and `mailglass_outbound` job, returning queued only after commit.
    - Ordering: preflight/render/preparation/route selection and envelope dump complete before the Multi; the four writes execute Delivery→Event→Payload→Job with prefix opts on every Mailglass table operation.
    - Privacy: Delivery metadata equals adopter metadata minus internal delivery-id transport state; queued Event normalized/raw/metadata and job args contain no subject/body/header/address/attachment/provider-option values.
    - Failure: force Payload insertion and Oban job insertion failures independently; each returns a typed bounded error and leaves zero matching Delivery/Event/Payload/Job rows.
  </behavior>
  <action>Start with integration assertions tagged `@tag phase_150_task: "t150_02_01"` and database-level failure injection helpers in the `async: false` test module. Each helper temporarily installs a prefixed CHECK constraint that rejects the target fixture's Payload or canonical Oban job and removes it in guaranteed cleanup, so production code gains no test switch. Harden the Plan 01 tracer helper into the sole durable single-send boundary: preserve Phase 149 ordering, require dump only after `prepare_outbound_message/1` and `resolve_async_adapter_ref/2`, and retain the ordered `Ecto.Multi` of Delivery changeset with `Repo.multi_opts()`, `Events.append_multi(:event_queued, ...)`, Payload changeset with `Repo.multi_opts()`, and gateway Oban insertion named `:job`; call `Repo.multi/1` once. Per D-09, extend the optional gateway to `Mailglass.OptionalDeps.Oban.insert/4` with `(multi, operation_name, job_builder, opts)` and have Outbound call exactly `OptionalDeps.Oban.insert(multi, :job, job_builder, Repo.multi_opts())`; inside the optional-dependency boundary forward to installed Oban 2.23's supported `Oban.insert(multi, operation_name, job_builder, opts)`. This is the exact prefix path: the active Multi supplies the host Repo to Oban through `Oban.Engine.insert_job/5`, while `Repo.multi_opts()` supplies `prefix: Mailglass.Config.schema()` and Oban's repo dispatch merges those per-call opts over its configured defaults before inserting `Oban.Job`. Do not rely on transaction opts, ambient `search_path`, or an unprefixed `Oban.insert/3`. Per D-02, change `base_delivery_attrs/3` to keep only adopter-supplied public metadata while ensuring internal `delivery_id` is not copied as adopter metadata; remove rendered_html/rendered_text/subject/headers/recipient_field writes. Keep event payload empty and job args exactly D-10's two string keys. Map serialization and transaction failures to closed SendError types with bounded reason_class, and return queued only from the four-key success result.</action>
  <acceptance_criteria>
    - Success assertion: exactly one row/fact exists in each of the four stores and all share delivery/tenant identity.
    - Atomicity assertion: injected Payload and job failures each leave zero matching rows in all four stores, including zero canonical `mailglass_outbound` jobs in the configured prefix.
    - Privacy assertion: recursive inspection of Delivery, Event, job args, captured logs, and telemetry metadata finds none of the unique private fixture values.
    - Order assertion: adapter selection and envelope dump happen before the Multi, while provider dispatch remains outside and absent from it.
    - Prefix assertion: Delivery, Event, Payload, and the Oban Job insert each receive the configured Mailglass prefix; with a hostile `search_path` and decoy `public.oban_jobs` (or an equivalent disjoint decoy schema), catalog-qualified queries prove the canonical `mailglass_outbound` job exists only in the configured prefix on success, while forced job failure leaves no matching row in the configured prefix or decoy/public schema.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/deliver_later_test.exs --only phase_150_task:t150_02_01 --warnings-as-errors</automated>
  </verify>
  <done>A single durable request either returns one fully recoverable four-part queued result or produces no durable queued work at all per D-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 150-02-02: Reuse per-envelope atomicity for deliver_many</name>
  <reversibility rating="costly">D-11 changes batch persistence from bulk post-commit insertion to per-envelope transactions while preserving the published per-message result contract.</reversibility>
  <files>lib/mailglass/outbound.ex, test/mailglass/outbound/deliver_many_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-11, `150-RESEARCH.md` Pitfall 1 and State of the Art batch row, `150-PATTERNS.md` batch guidance, current `do_deliver_many/2`, `preflight_single/1`, `insert_batch/1`, `enqueue_batch_jobs/1`, and all result ordering/idempotency assertions in `deliver_many_test.exs`.</read_first>
  <behavior>
    - Two eligible messages produce two independent four-part commits and results remain in input order.
    - One serialization/route/job failure produces that message's typed failed result without rolling back a different eligible envelope and without leaving any partial facts for the failed item.
    - Idempotency replays return the existing delivery result without inserting a duplicate Payload/Event/job or re-enqueueing settled work.
    - Native cc/bcc selection and all V1 fields survive each batch item's envelope, with no whole-batch atomicity claim.
  </behavior>
  <action>Tag this expansion's cases with `@tag phase_150_task: "t150_02_02"`. Refactor each eligible routed item to invoke the same atomic helper from Task 150-02-01 with its own dumped envelope. Preserve input-position/result semantics by carrying indices through preflight/routing/enqueue and rebuilding the ordered result list. Remove the Oban path's `insert_batch/1` plus post-commit `enqueue_batch_jobs/1` stranded-work window; do not use `Oban.insert_all/1` for durable items. Preserve public failed-delivery result shape and idempotency behavior: an existing delivery is returned without duplicating Event/Payload/job, while a new eligible item commits independently. Explicit `:task_supervisor` remains a separate non-durable branch for Plan 04 documentation/readiness; do not broaden the transaction into an all-or-nothing batch.</action>
  <acceptance_criteria>
    - D-11 atomicity assertion: every newly queued batch result has exactly one Delivery/Event/Payload/job; a failed item has none.
    - Public-contract assertion: result cardinality/order and typed failed entries match the input list.
    - Replay assertion: repeated idempotency keys do not duplicate any of the four facts or resend settled work.
    - Scope assertion: no provider dispatch, recipient fan-out, or whole-batch transaction is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/outbound/deliver_many_test.exs --only phase_150_task:t150_02_02 --warnings-as-errors</automated>
  </verify>
  <done>Each eligible batch item owns an independent four-part atomic enqueue and ordered public result, eliminating D-11's post-commit stranded-work window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| prepared envelope → transaction | Validated private content is separated from public projection/event/job facts. |
| Ecto.Multi → Mailglass and Oban tables | Four local writes must cross one commit boundary under the configured prefix. |
| batch item → per-envelope transaction | One failed item must not create partial work or corrupt other result positions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-06 | Information Disclosure | `base_delivery_attrs/3`, Event, job/log/telemetry | high | mitigate | D-01/D-02/D-10 allowlisted public metadata and recursive sentinel tests across every non-Payload surface. |
| T-150-07 | Repudiation / Denial of Service | four-part enqueue Multi | high | mitigate | D-09 one transaction, named ordered operations, forced Payload/job failures, and zero-row rollback assertions. |
| T-150-08 | Tampering | batch result association | medium | mitigate | Carry stable input index and delivery/tenant identity through per-envelope helper; ordered mixed-result and replay tests. |
| T-150-SC | Tampering | package installs | high | mitigate | No install task exists; package additions require the legitimacy gate and blocking review before execution. |
</threat_model>

<verification>
After each task run its focused suite. At wave completion run `mix test test/mailglass/outbound/envelope_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/outbound/deliver_many_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- Single and batch durable APIs reuse one per-envelope atomic boundary.
- Private values appear only in Payload, and job args remain the exact D-10 identifiers.
- Every injected failure disproves false durable success with zero partial records.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-02-SUMMARY.md` when done.
</output>
