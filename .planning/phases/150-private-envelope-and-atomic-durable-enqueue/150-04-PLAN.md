---
phase: 150-private-envelope-and-atomic-durable-enqueue
plan: "04"
type: execute
wave: 4
depends_on:
  - 150-03
files_modified:
  - lib/mailglass/application.ex
  - lib/mailglass/config.ex
  - guides/production-go-live-checklist.md
  - test/mailglass/application_test.exs
  - test/mailglass/config_test.exs
  - test/mailglass/docs_migration_smoke_test.exs
autonomous: true
requirements:
  - ENVL-07
  - ENVL-08
must_haves:
  truths:
    - "D-12/D-13/ENVL-07 [unclassified resolved]: only explicit `async_adapter: :task_supervisor` selects the non-durable development/test path, and `Mailglass.Config.production_readiness/0` rejects it with a typed bounded ConfigError."
    - "D-10/D-12/D-13/ENVL-07: `Mailglass.Config.production_readiness/0` passes explicit `:oban` only when `Mailglass.OptionalDeps.Oban.ready?/1` accepts canonical `:mailglass_outbound`; ordinary `validate_at_boot!/0` does not impose this production-only gate."
    - "D-10/D-13/ENVL-08: the production go-live configuration and its executable smoke contract use only `mailglass_outbound` for the outbound worker queue."
    - "D-12/ENVL-08: missing, empty, or wrong canonical queue state fails readiness rather than being accepted as durable production configuration."
  artifacts:
    - path: lib/mailglass/config.ex
      provides: "Explicit production-readiness preflight over current async adapter and canonical Oban readiness"
      exports: ["production_readiness/0"]
    - path: lib/mailglass/application.ex
      provides: "Truthful boot behavior and warnings kept separate from the production-only preflight"
    - path: guides/production-go-live-checklist.md
      provides: "Canonical production queue and callable readiness instructions"
    - path: test/mailglass/config_test.exs
      provides: "Executable rejection/pass/dev-boot separation proof"
  key_links:
    - from: lib/mailglass/config.ex
      to: lib/mailglass/optional_deps/oban.ex
      via: "production_readiness/0 delegates explicit :oban readiness for canonical :mailglass_outbound"
      pattern: "OptionalDeps\\.Oban\\.ready"
    - from: lib/mailglass/application.ex
      to: lib/mailglass/config.ex
      via: "ordinary boot invokes validate_at_boot!/0 but never the production-only readiness seam"
      pattern: "validate_at_boot"
    - from: guides/production-go-live-checklist.md
      to: lib/mailglass/config.ex
      via: "go-live steps invoke production_readiness/0 after configuring the canonical queue"
      pattern: "production_readiness"
  prohibitions:
    - statement: "Production readiness must not accept explicit Task.Supervisor or an Oban instance lacking the canonical queue."
      status: unverified
      flagged: true
---

<objective>
Implement the production-readiness boundary: explicit TaskSupervisor is rejected, explicit Oban must prove the canonical configured queue, and ordinary development/test boot remains usable per D-10/D-12/D-13.

Purpose: Make the durable go-live decision executable without imposing production-only policy on routine application startup.
Output: `Mailglass.Config.production_readiness/0`, corrected application warnings, canonical production checklist, and focused runtime/config tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-RESEARCH.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-VALIDATION.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-03-SUMMARY.md
@lib/mailglass/application.ex
@lib/mailglass/config.ex
@guides/production-go-live-checklist.md
@test/mailglass/application_test.exs
@test/mailglass/config_test.exs
@test/mailglass/docs_migration_smoke_test.exs
</context>

## Artifacts this phase produces

- `Mailglass.Config.production_readiness/0`, returning `:ok | {:error, %Mailglass.ConfigError{}}` from the current explicit async-adapter choice and Plan 03 readiness result.
- Application warnings that distinguish unavailable durable Oban from explicitly selected non-durable TaskSupervisor without changing ordinary boot strictness.
- Copy-paste production configuration using `queues: [mailglass_outbound: ...]`, explicit `async_adapter: :oban`, and the callable readiness preflight.
- Focused config/application and production-smoke tests tagged for sub-30-second task sampling.

<tasks>

<task type="auto" tdd="true">
  <name>Task 150-04-01: Enforce the explicit production-readiness boundary</name>
  <reversibility rating="costly">D-10/D-13 are adopter-facing operational contracts; changing the canonical queue or production durability claim requires coordinated runtime and documentation migration.</reversibility>
  <files>lib/mailglass/application.ex, lib/mailglass/config.ex, test/mailglass/application_test.exs, test/mailglass/config_test.exs</files>
  <read_first>Read `150-CONTEXT.md` D-10/D-12/D-13/D-14; Plan 03 summary's `Mailglass.OptionalDeps.Oban.ready?/1` result contract; the `:async_adapter` schema entry and `validate_at_boot!/0`; all `maybe_warn_missing_oban/0` branches/messages; environment restoration patterns in `config_test.exs`; and boot-warning coverage in `application_test.exs`.</read_first>
  <behavior>
    - Explicit `async_adapter: :task_supervisor` returns `{:error, %Mailglass.ConfigError{type: :invalid, context: %{key: :async_adapter, reason_class: :non_durable_async_adapter}}}` and does not query Oban readiness.
    - Explicit `async_adapter: :oban` with a running default Oban instance configured with canonical `mailglass_outbound` at positive concurrency returns `:ok`.
    - Missing dependency/instance or missing, empty, or wrong queue maps the gateway's bounded reason class into a non-PII ConfigError and never passes.
    - `validate_at_boot!/0` and `Application.start/2` remain separate from the production preflight; explicit TaskSupervisor is still usable for development/test boot.
  </behavior>
  <action>Implement public `Mailglass.Config.production_readiness/0 :: :ok | {:error, Mailglass.ConfigError.t()}` per D-12/D-13. Read the schema-valid current `Application.get_env(:mailglass, :async_adapter, :oban)`: return the exact non-durable-adapter ConfigError for `:task_supervisor`; for `:oban`, call `Mailglass.OptionalDeps.Oban.ready?(:mailglass_outbound)` and map `:dependency_unavailable`, `:instance_unavailable`, and `:canonical_queue_unavailable` to context containing only `key: :async_adapter` and `reason_class`. Keep `validate_at_boot!/0` unchanged in strictness. Update application branching and warnings so unavailable explicitly selected Oban says durable sends fail closed, while selected TaskSupervisor says it is non-durable and rejected by production readiness; do not call the preflight from ordinary boot. Add `@tag phase_150_task: "t150_04_01"` cases; make global-state modules `async: false`, snapshot and restore Application/Oban state in `on_exit`, start a real default-name manual Oban for the passing case, and exercise missing/wrong/empty queue negatives.</action>
  <verify>
    <automated>mix test test/mailglass/config_test.exs test/mailglass/application_test.exs --only phase_150_task:t150_04_01 --warnings-as-errors</automated>
  </verify>
  <done>D-13 has a callable production gate that rejects explicit TaskSupervisor and accepts only canonical-ready Oban, while routine development/test boot remains unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 150-04-02: Make the production checklist exercise the readiness contract</name>
  <files>guides/production-go-live-checklist.md, test/mailglass/docs_migration_smoke_test.exs</files>
  <read_first>Read D-10/D-12/D-13, the production-readiness behavior from Task 150-04-01, the current Oban queue example in `guides/production-go-live-checklist.md`, and the generated/config snippets plus registration assertions in `docs_migration_smoke_test.exs`.</read_first>
  <behavior>
    - The production snippet selects `async_adapter: :oban`, configures only `mailglass_outbound`, and invokes `Mailglass.Config.production_readiness/0` before go-live.
    - A canonical-ready fixture passes; missing/wrong queue and explicit TaskSupervisor fixtures fail with the documented typed result.
    - The existing async migration example remains one-recipient/nonblank-body valid and selects its intended adapter explicitly.
  </behavior>
  <action>Update the production checklist per D-10/D-12/D-13 to use `queues: [mailglass_outbound: positive_integer]`, explicit `async_adapter: :oban`, and a preflight step matching on `Mailglass.Config.production_readiness/0`. State only its bounded typed readiness result and keep active-consumer proof assigned to Phase 153. Add `@tag phase_150_task: "t150_04_02"` structural smoke assertions that extract the production config/readiness block, compare the queue token to `Mailglass.Outbound.Worker.queue/0`, and prove wrong/missing queue fixtures are rejected. Keep the migration example explicitly configured and valid under the Phase 149 one-recipient/body contract.</action>
  <verify>
    <automated>mix test test/mailglass/docs_migration_smoke_test.exs --only phase_150_task:t150_04_02 --warnings-as-errors</automated>
  </verify>
  <done>The production go-live path is copy-pasteable, canonical-queue exact, and mechanically tied to the executable D-13 readiness seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| deployment preflight → runtime adapter/Oban configuration | A production-ready claim must distinguish explicit non-durable execution from a canonical configured durable queue. |
| ordinary application boot → production policy | Development/test startup must not silently inherit a deployment-only rejection gate. |
| production guide → adopter host configuration | Copy-paste queue and adapter values determine whether durable work is consumable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-150-13 | Repudiation / Denial of Service | `Config.production_readiness/0` | high | mitigate | Execute D-13 against explicit adapter plus Plan 03 readiness and expose only bounded ConfigError reason classes. |
| T-150-14 | Repudiation | application boot separation | high | mitigate | Test that ordinary boot invokes schema validation but never production readiness, while warning text accurately identifies the selected path. |
| T-150-15 | Denial of Service | production queue configuration | high | mitigate | Derive the checklist queue from Worker.queue/0 and reject absent, empty, or drifted configuration in focused smoke tests. |
| T-150-SC | Tampering | package installs | high | mitigate | No install task exists; any dependency deviation requires package-legitimacy review. |
</threat_model>

<verification>
Run each focused command after its task. At the Wave 4 gate run `mix test test/mailglass/config_test.exs test/mailglass/application_test.exs test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- Production readiness rejects explicit TaskSupervisor and every unusable/canonical-queue failure with bounded typed errors.
- Canonical-ready explicit Oban passes without claiming an actively polling worker.
- Ordinary dev/test boot remains usable and separate from the production-only gate.
</success_criteria>

<output>
Create `.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-04-SUMMARY.md` when done.
</output>
