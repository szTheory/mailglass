---
phase: "33"
plan: "03"
type: execute
wave: 3
depends_on:
  - "33-01"
  - "33-02"
files_modified:
  - "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
  - "mailglass_admin/lib/mailglass_admin/operator/repair_state.ex"
  - "mailglass_admin/lib/mailglass_admin/operator_live.ex"
  - "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
autonomous: true
requirements:
  - MAT-02
must_haves:
  truths:
    - "The existing operator delivery detail view shows read-only support cues backed by the support-summary read model."
    - "Operator UI copy keeps provider lifecycle facts, replay audit facts, and reconcile facts visibly distinct."
    - "Each support cue drills to a concrete selected delivery, webhook row, or durable timeline/audit fact instead of ending at an aggregate count."
    - "Overview-style operator surfaces are more privacy-minimized than today without making the selected-delivery detail unusable for support."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      provides: "Read-only support cue cards for failed ingest, orphan backlog, replay outcomes, and reconcile facts"
      contains: "operator-support-cards"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Delivery-centric operator flow that loads support summaries alongside selected-delivery detail"
      contains: "support_summary"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "Regression coverage for support cards, truthful copy, exemplar-oriented cues, and masked overview fields"
      contains: "operator-support-cards"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "lib/mailglass/operator/support_summary.ex"
      via: "tenant-scoped support-summary assignment during detail loading"
      pattern: "SupportSummary\\.summarize_tenant"
    - from: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/repair_state.ex"
      via: "shared wording for replay and reconcile effect labels"
      pattern: "RepairState\\."
    - from: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "card exemplar actions patch filter state or select a concrete delivery/webhook fact drilldown"
      pattern: "push_patch|selected_delivery|provider_event_id|event_id"
    - from: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "rendered support-card, exemplar drilldown, timeline-label, and privacy assertions"
      pattern: "operator-support-cards|Replay audit|Reconcile fact|provider_event_id|event_id"
---

<objective>
Wire the new support facts into the existing operator delivery surface without turning it into a dashboard product or weakening tenant/privacy boundaries.

Purpose: Per D-33-03, D-33-10, D-33-17, and D-33-20, operators should get delivery-centric support cues inside `mailglass_admin`, but only as narrow read-only cards and labels that point to concrete facts instead of speculative fleet-wide health.
Output: Support-card components, LiveView wiring, clearer timeline fact distinctions, stronger overview masking, and focused LiveView regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-observability-incident-support/33-CONTEXT.md
@.planning/phases/33-observability-incident-support/33-RESEARCH.md
@.planning/phases/33-observability-incident-support/33-PATTERNS.md
@.planning/phases/33-observability-incident-support/33-VALIDATION.md
@.planning/phases/33-observability-incident-support/33-01-PLAN.md
@.planning/phases/33-observability-incident-support/33-02-PLAN.md
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
@mailglass_admin/lib/mailglass_admin/operator/detail_header.ex
@mailglass_admin/lib/mailglass_admin/operator/timeline.ex
@mailglass_admin/lib/mailglass_admin/operator/repair_state.ex
@mailglass_admin/test/mailglass_admin/operator_live_test.exs
@lib/mailglass/operator/support_summary.ex
@lib/mailglass/operator/replay_history.ex
@lib/mailglass/operator/timeline.ex

<interfaces>
From lib/mailglass/operator/support_summary.ex:
```elixir
def summarize_tenant(%{tenant_id: tenant_id, window_hours: window_hours})
```

From mailglass_admin/lib/mailglass_admin/operator_live.ex:
```elixir
socket
|> assign(:selected_delivery, selected_delivery)
|> assign(:timeline_events, load_timeline(filter_params, selected_delivery))
|> assign(:replay_history, replay_history)
```

From mailglass_admin/lib/mailglass_admin/operator/repair_state.ex:
```elixir
def latest_replay_summary(replay)
def effect_label(outcome)
def replay_event_label(type)
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add read-only support cards and privacy-minimized overview cues to the operator surface</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/support_cards.ex, mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/operator/detail_header.ex, mailglass_admin/lib/mailglass_admin/operator/timeline.ex, mailglass_admin/lib/mailglass_admin/operator/repair_state.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <behavior>
    - Test 1: Selecting a delivery renders support cards for failed ingest, orphan backlog, replay outcomes, and reconcile facts using the support-summary read model.
    - Test 2: Each card exposes an exemplar drilldown that selects the concrete delivery or patches the current operator filters to reveal the corresponding webhook row or replay/reconcile fact.
    - Test 3: Replay audit rows and reconcile facts are labeled distinctly from provider lifecycle events in the timeline and support copy.
    - Test 4: Delivery-list rows are more privacy-minimized than today while the selected-delivery detail still preserves enough exact identity for authorized support work.
  </behavior>
  <action>Keep the work inside the existing operator mount per D-33-01 through D-33-03. Create a read-only `SupportCards` component and place it in the detail column after `DetailHeader` and before `Timeline`, using `Mailglass.Operator.SupportSummary.summarize_tenant/1` from `OperatorLive` rather than querying from templates. For D-33-11 and D-33-22, every card must expose a concrete exemplar path: failed-ingest cards should patch operator filters or an anchor state to reveal the exact webhook row by `provider_event_id`/row ID; replay-outcome cards should jump to the matching replay audit entry in the selected delivery timeline/history; orphan/reconcile cards should either select the exemplar delivery or patch to the relevant unmatched/linked fact view using durable event IDs. Extend `RepairState` before inventing new copy so replay `failed / no change / new work` and reconcile `linked / still unmatched pressure` language stays aligned with 33-01 and Phase 32. Update `Timeline` to visually distinguish reconcile facts from replay audit rows instead of leaving `:reconciled` as an unlabeled generic event. Strengthen privacy posture by masking recipient-like identifiers in overview list rows by default per D-33-17, while keeping the selected detail truthful and support-usable per D-33-16. Do not add search, exports, notes, new actions, or any separate observability dashboard per D-33-02 and D-33-19.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The operator surface now exposes narrow, truthful support cues and clearer fact labeling without becoming a second dashboard product or leaking extra PII in overview surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand LiveView regression coverage for support cards, wording, and privacy posture</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <behavior>
    - Test 1: The rendered operator detail includes the support-card container and card copy for failed ingest, orphan backlog, replay outcomes, and reconcile facts.
    - Test 2: Card exemplar controls patch the LiveView state or selection so the operator lands on the concrete webhook row, replay audit row, or reconcile fact claimed by the card.
    - Test 3: The timeline shows distinct replay-audit and reconcile-fact labels, not one generic repair status.
    - Test 4: Overview rows mask the recipient by default while selected-delivery detail retains the exact recipient for authorized support context.
  </behavior>
  <action>Expand `MailglassAdmin.OperatorLiveTest` rather than creating a second LiveView suite. Seed webhook rows, replay audit rows, and reconcile facts that exercise every support-card bucket, then assert exact rendered copy and `data-testid` markers. Keep the tests delivery-centric: the cue cards should only appear once a delivery is selected, and their counts/exemplars must reflect the selected tenant and current window. Add explicit assertions that activating an exemplar control updates the selected delivery or LiveView patch/filter state so the promised webhook row, replay audit row, or reconcile fact is actually visible after the interaction. Add explicit assertions that the UI does not claim real-time completeness or provider-side truth beyond the read model, and keep replay as the only destructive operator action in this surface.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors && cd /Users/jon/projects/mailglass && mix test test/mailglass/operator/support_summary_test.exs test/mailglass/operator/timeline_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Support-card UI behavior, fact labels, and privacy-minimized overview behavior are locked through the existing operator LiveView regression seam.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant-scoped support summary -> LiveView UI | Backend support facts become visible to authorized operators. |
| Overview delivery list -> human-identifying data | List rows present identity cues in a broader glanceable surface than selected detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Information Disclosure | `mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex`, `support_cards.ex` | mitigate | Mask overview recipient-like fields by default, avoid raw payload/webhook-body rendering, and keep only tenant-scoped IDs/counts/timestamps in support cards per D-33-15 and D-33-17. |
| T-33-08 | Tampering / Misrepresentation | `mailglass_admin/lib/mailglass_admin/operator/timeline.ex`, `repair_state.ex` | mitigate | Render separate labels for provider lifecycle, replay audit, and reconcile facts so the UI does not imply that provider retries, replay, and reconcile are the same workflow per D-33-08 and D-33-21. |
| T-33-09 | Elevation of Privilege | `mailglass_admin/lib/mailglass_admin/operator_live.ex` | mitigate | Reuse the existing authenticated operator mount, load only tenant-scoped summaries, and keep the new support surface read-only with no added action paths per D-33-18. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors`
- `mix test test/mailglass/operator/support_summary_test.exs test/mailglass/operator/timeline_test.exs --warnings-as-errors`
- `rg -n "operator-support-cards|Replay audit|Reconcile fact|provider_event_id|event_id|push_patch|mask" mailglass_admin/lib/mailglass_admin/operator mailglass_admin/test/mailglass_admin/operator_live_test.exs`
</verification>

<success_criteria>
Authorized operators can open one delivery in the existing `mailglass_admin` surface, see truthful support cues backed by durable facts, and distinguish provider, replay, and reconcile evidence without broader dashboard scope or extra privacy leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/33-observability-incident-support/33-03-SUMMARY.md`
</output>
