---
phase: "33"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "guides/operator-incident-support.md"
  - "guides/telemetry.md"
  - "guides/webhook-troubleshooting.md"
  - "guides/webhooks.md"
  - "mailglass_admin/README.md"
  - "test/mailglass/docs/operator_incident_support_guide_test.exs"
  - "test/mailglass/docs_contract_test.exs"
autonomous: true
requirements:
  - MAT-02
must_haves:
  truths:
    - "Operators have one canonical incident guide that starts from real support symptoms and routes into the shipped delivery, webhook, replay, and reconcile evidence model."
    - "Telemetry docs only name event families and metadata keys that the current code actually emits, with no PII-bearing examples."
    - "Operator docs, telemetry docs, and webhook docs use the same exact distinctions for provider lifecycle facts, replay facts, and reconcile facts."
  artifacts:
    - path: "guides/operator-incident-support.md"
      provides: "Canonical operator incident guide with symptom-first entrypoints and stage drilldowns"
      contains: "Mailglass can tell you this"
    - path: "guides/telemetry.md"
      provides: "Current telemetry contract reference for outbound, webhook ingest, orphan, duplicate, and reconcile events"
      contains: "[:mailglass, :webhook, :ingest"
    - path: "test/mailglass/docs/operator_incident_support_guide_test.exs"
      provides: "Doc contract coverage for symptom-first structure, honesty notes, and stage-specific guidance"
      contains: "operator incident support guide"
  key_links:
    - from: "guides/operator-incident-support.md"
      to: "guides/telemetry.md"
      via: "incident checklist references exact telemetry event names and metadata keys"
      pattern: "mailglass, :webhook|mailglass, :outbound"
    - from: "guides/operator-incident-support.md"
      to: "guides/webhooks.md"
      via: "stage drilldowns reuse the same replay and reconcile contract wording"
      pattern: "Replay|reconcile|still unmatched|new work|no change"
    - from: "test/mailglass/docs/operator_incident_support_guide_test.exs"
      to: "guides/operator-incident-support.md"
      via: "guide-structure and wording assertions"
      pattern: "customer says the email never arrived|mailglass can tell you this"
---

<objective>
Phase 33 starts by fixing the operator-facing contract: one canonical incident guide, current telemetry terminology, and aligned admin/support wording.

Purpose: Per D-33-01 through D-33-09 and D-33-23, later support cues must build on truthful, stable language rather than the current drift across telemetry, webhook, and admin docs.
Output: A canonical operator incident guide, corrected telemetry reference, tightened webhook docs, aligned admin README wording, and docs-contract tests that lock the support language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/33-observability-incident-support/33-CONTEXT.md
@.planning/phases/33-observability-incident-support/33-RESEARCH.md
@.planning/phases/33-observability-incident-support/33-PATTERNS.md
@.planning/phases/33-observability-incident-support/33-VALIDATION.md
@.planning/phases/32-replay-reconcile-hardening/32-CONTEXT.md
@guides/telemetry.md
@guides/webhook-troubleshooting.md
@guides/webhooks.md
@mailglass_admin/README.md
@lib/mailglass/webhook/telemetry.ex
@lib/mailglass/telemetry.ex
@lib/mailglass/webhook/reconciler.ex
@lib/mix/tasks/mailglass.reconcile.ex
@test/mailglass/docs_contract_test.exs
@test/support/docs_helpers.ex

<interfaces>
From lib/mailglass/webhook/telemetry.ex:
```elixir
[:mailglass, :webhook, :ingest, :start | :stop | :exception]
[:mailglass, :webhook, :signature, :verify, :start | :stop | :exception]
[:mailglass, :webhook, :normalize, :stop]
[:mailglass, :webhook, :orphan, :stop]
[:mailglass, :webhook, :duplicate, :stop]
[:mailglass, :webhook, :reconcile, :start | :stop | :exception]
```

From lib/mailglass/telemetry.ex:
```elixir
[:mailglass, :render, :message, :start | :stop | :exception]
[:mailglass, :outbound, :send, :start | :stop | :exception]
[:mailglass, :outbound, :dispatch, :start | :stop | :exception]
```

From test/support/docs_helpers.ex:
```elixir
def extract_code_blocks(path)
def extract_block_after_heading(path, heading)
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Rewrite the support docs into one canonical operator incident surface</name>
  <files>guides/operator-incident-support.md, guides/telemetry.md, guides/webhook-troubleshooting.md, guides/webhooks.md, mailglass_admin/README.md, test/mailglass/docs/operator_incident_support_guide_test.exs</files>
  <behavior>
    - Test 1: The canonical guide exposes symptom-first entrypoints for "customer says the email never arrived", "provider is retrying or timing out", "orphan backlog is growing", and "replay completed but nothing changed".
    - Test 2: Every stage drilldown explicitly separates provider lifecycle facts, replay facts, and reconcile facts, with "mailglass can tell you this / cannot tell you this" honesty notes.
    - Test 3: Telemetry docs use only shipped event paths and whitelist-safe metadata keys; no stale `[:mailglass, :deliver]` or `[:mailglass, :reconcile]` references remain.
  </behavior>
  <action>Start by creating `test/mailglass/docs/operator_incident_support_guide_test.exs` with the failing structure and wording assertions from the behavior block so the task's `<automated>` command is runnable as soon as Task 1 completes. Then create `guides/operator-incident-support.md` as the canonical playbook required by D-33-01, D-33-05, D-33-06, D-33-07, D-33-08, and D-33-09. Rewrite `guides/telemetry.md` directly from `Mailglass.Telemetry` and `Mailglass.Webhook.Telemetry`, then tighten `guides/webhooks.md`, `guides/webhook-troubleshooting.md`, and `mailglass_admin/README.md` so they reference the same shipped support model per D-33-04 and D-33-21. Keep `guides/webhook-troubleshooting.md` as a short operator entry shim that points into the canonical guide and exact webhook reference sections instead of re-implementing the whole support story. Include safe optional integration recipes for LiveDashboard / OpenTelemetry / Sentry / Honeycomb style handlers per D-33-13 and D-33-23, but do not imply any backend is required and do not introduce any new dashboard promise per D-33-02.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/docs/operator_incident_support_guide_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The docs surface has one canonical incident guide, current telemetry names, and aligned operator wording with no stale observability claims or PII-bearing examples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add docs-contract coverage for the Phase 33 support language</name>
  <files>test/mailglass/docs/operator_incident_support_guide_test.exs, test/mailglass/docs_contract_test.exs</files>
  <behavior>
    - Test 1: The canonical guide contains the symptom-first headings, stage drilldowns, and honesty notes locked by MAT-02.
    - Test 2: The general docs contract rejects stale telemetry names and ensures the operator/admin docs reference replay and reconcile with the current vocabulary.
  </behavior>
  <action>Extend the existing docs-contract seam instead of adding ad hoc grep scripts. Build on the guide-contract file created in Task 1 by expanding `test/mailglass/docs/operator_incident_support_guide_test.exs` with any additional exact-support prompts needed for MAT-02, then update `test/mailglass/docs_contract_test.exs` so the telemetry and admin docs reject stale event paths, require current webhook/reconcile wording, and keep the support surface aligned with the existing codebase. Keep the assertions text-level and fast; do not add integration tests that spin up LiveView or parse unrelated guides.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/docs/operator_incident_support_guide_test.exs test/mailglass/webhook/telemetry_test.exs test/mailglass/telemetry_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Phase 33's documentation contract is executable: stale telemetry names, vague repair wording, or missing honesty sections now fail tests immediately.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator action | Operators rely on docs to decide whether to replay, reconcile, or escalate. |
| Documentation -> telemetry backend wiring | Adopters copy support recipes into their own observability stack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `guides/telemetry.md`, `guides/operator-incident-support.md` | mitigate | Keep all examples and metadata references inside the existing whitelist posture per D-33-15; forbid recipient/body/payload examples in docs-contract tests. |
| T-33-02 | Tampering / Misrepresentation | `guides/operator-incident-support.md`, `guides/webhooks.md` | mitigate | Explicitly separate provider lifecycle, replay, and reconcile facts per D-33-08 and D-33-21 so operators are not told a no-op replay or backlog sweep "fixed" delivery state. |
| T-33-03 | Elevation of Privilege | `mailglass_admin/README.md` | mitigate | Keep the admin/operator docs inside adopter-owned auth and recent-auth posture; do not promise unauthenticated support routes or a separate incident console per D-33-02 and D-33-18. |
</threat_model>

<verification>
- `mix test test/mailglass/docs_contract_test.exs test/mailglass/docs/operator_incident_support_guide_test.exs --warnings-as-errors`
- `mix test test/mailglass/webhook/telemetry_test.exs test/mailglass/telemetry_test.exs --warnings-as-errors`
- `rg -n "\\[:mailglass, :deliver\\]|\\[:mailglass, :reconcile\\]|metadata.function" guides/telemetry.md guides/webhooks.md guides/webhook-troubleshooting.md mailglass_admin/README.md test/mailglass/docs_contract_test.exs`
</verification>

<success_criteria>
Operators and adopters now have one truthful support reference surface, telemetry docs match shipped code, and the docs/tests lock the exact support vocabulary Phase 33 will reuse in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/33-observability-incident-support/33-01-SUMMARY.md`
</output>
