---
phase: "33"
plan: "02"
type: execute
wave: 2
depends_on:
  - "33-01"
files_modified:
  - "lib/mailglass/operator/support_summary.ex"
  - "test/mailglass/operator/support_summary_test.exs"
autonomous: true
requirements:
  - MAT-02
must_haves:
  truths:
    - "Support cues are derived from tenant-scoped webhook rows and append-only ledger facts, not a new mutable incident model."
    - "The support summary distinguishes failed ingest facts, orphan backlog facts, replay outcomes, and reconcile facts without flattening them into one status."
    - "Each cue exposes an exemplar identifier or durable fact that later UI work can drill into instead of stopping at an aggregate count."
  artifacts:
    - path: "lib/mailglass/operator/support_summary.ex"
      provides: "Tenant-scoped support summary read model for failed ingest, orphan backlog, replay outcomes, and reconcile facts"
      exports: ["summarize_tenant/1"]
    - path: "test/mailglass/operator/support_summary_test.exs"
      provides: "Query-contract coverage for counts, exemplars, windows, and tenant isolation"
      contains: "summarize_tenant"
  key_links:
    - from: "lib/mailglass/operator/support_summary.ex"
      to: "lib/mailglass/webhook/webhook_event.ex"
      via: "failed-ingest and latest-webhook exemplar queries"
      pattern: "WebhookEvent"
    - from: "lib/mailglass/operator/support_summary.ex"
      to: "lib/mailglass/events/event.ex"
      via: "orphan backlog, replay outcome, and reconcile fact queries"
      pattern: "needs_reconciliation|webhook_replay|:reconciled"
    - from: "test/mailglass/operator/support_summary_test.exs"
      to: "lib/mailglass/operator/support_summary.ex"
      via: "tenant-scoped count/exemplar assertions"
      pattern: "failed_ingest|orphan_backlog|replay_outcomes|reconcile_facts"
---

<objective>
Introduce the durable support-summary seam the UI will consume, backed by real webhook and ledger facts rather than synthetic incident state.

Purpose: Per D-33-10 through D-33-14 and D-33-22, the operator surface needs truthful "what needs attention now?" cues with exemplars, but those cues must come from tenant-scoped read models instead of ad hoc LiveView queries or mutable incident records.
Output: A new `Mailglass.Operator.SupportSummary` read model and focused regression coverage for its counts, exemplars, tenant scoping, and time-window behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-observability-incident-support/33-CONTEXT.md
@.planning/phases/33-observability-incident-support/33-RESEARCH.md
@.planning/phases/33-observability-incident-support/33-PATTERNS.md
@.planning/phases/33-observability-incident-support/33-VALIDATION.md
@.planning/phases/33-observability-incident-support/33-01-PLAN.md
@lib/mailglass/events/event.ex
@lib/mailglass/operator/deliveries.ex
@lib/mailglass/operator/timeline.ex
@lib/mailglass/operator/replay_history.ex
@lib/mailglass/webhook/webhook_event.ex
@lib/mailglass/webhook/reconciler.ex
@test/mailglass/operator/timeline_test.exs
@test/mailglass/webhook/reconciler_test.exs
@test/mailglass/webhook/replay_test.exs

<interfaces>
From lib/mailglass/events/event.ex:
```elixir
field(:type, Ecto.Enum, values: @event_types)
field(:needs_reconciliation, :boolean, default: false)
field(:metadata, :map, default: %{})
```

From lib/mailglass/webhook/webhook_event.ex:
```elixir
field(:status, Ecto.Enum, values: [:received, :processing, :succeeded, :failed, :dead])
field(:provider, :string)
field(:provider_event_id, :string)
field(:received_at, :utc_datetime_usec)
```

From lib/mailglass/operator/replay_history.ex:
```elixir
def list_delivery_replay_history(%{tenant_id: tenant_id, delivery_id: delivery_id})
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create the tenant-scoped support-summary read model</name>
  <files>lib/mailglass/operator/support_summary.ex, test/mailglass/operator/support_summary_test.exs</files>
  <behavior>
    - Test 1: `summarize_tenant/1` returns separate sections for failed ingest, orphan backlog, replay outcomes, and reconcile facts.
    - Test 2: Each section returns counts plus at least one exemplar identifier or durable timestamp that the UI can drill into.
    - Test 3: The read model stays tenant-scoped and time-window-bounded; foreign-tenant data and out-of-window facts are excluded.
  </behavior>
  <action>Start by creating `test/mailglass/operator/support_summary_test.exs` with failing assertions for the four support buckets, tenant isolation, and exemplar/time-window behavior so the task's `<automated>` command is runnable as soon as Task 1 completes. Then create `Mailglass.Operator.SupportSummary` with a narrow public API such as `summarize_tenant(%{tenant_id: tenant_id, window_hours: hours})`. Back the summary with repo queries over `Mailglass.Webhook.WebhookEvent` and `Mailglass.Events.Event`, not transient telemetry snapshots or a new incident table, per D-33-10 and D-33-14. Return four explicit buckets using the Phase 33 vocabulary from 33-01: `failed_ingest`, `orphan_backlog`, `replay_outcomes`, and `reconcile_facts`. For replay outcomes, split counts into failed / no change / new work using the existing replay audit metadata contract. For reconcile, report linked facts from `:reconciled` events and still-unmatched pressure from current orphan backlog instead of inventing per-run state. Include exemplar IDs and timestamps so D-33-22 is satisfied without building a dashboard.</action>
  <verify>
    <automated>mix test test/mailglass/operator/support_summary_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The codebase now has one durable, tenant-safe support-summary seam that expresses real support posture without a mutable incident model or fake real-time guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock support-summary semantics with focused read-model tests</name>
  <files>test/mailglass/operator/support_summary_test.exs</files>
  <behavior>
    - Test 1: Failed webhook rows with `:failed` or `:dead` status count correctly and expose the latest exemplar row.
    - Test 2: Orphan backlog reports current unresolved count and oldest age without mutating the orphan rows.
    - Test 3: Replay outcomes and reconcile facts remain distinct, with `replayed`, `noop`, and `:reconciled` facts asserted separately.
  </behavior>
  <action>Build on the dedicated test seam created in Task 1 by filling out `test/mailglass/operator/support_summary_test.exs` with the full fixture matrix. Use the existing fixture style from `timeline_test.exs`, `replay_test.exs`, and `reconciler_test.exs`: append real ledger events, insert real webhook rows, and assert exact count/exemplar results for one tenant and one foreign tenant. Keep the tests data-level and fast; they should not boot LiveView or duplicate operator rendering assertions that belong in Plan 33-03.</action>
  <verify>
    <automated>mix test test/mailglass/operator/support_summary_test.exs test/mailglass/webhook/replay_test.exs test/mailglass/webhook/reconciler_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Support-summary behavior is pinned to durable facts, tenant scoping, and the existing replay/reconcile semantics before any UI consumes it.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant-scoped read model -> operator support cues | Database facts are summarized into human-facing support indicators. |
| Mutable webhook rows -> append-only ledger facts | The support summary combines two storage classes with different trust semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Information Disclosure | `lib/mailglass/operator/support_summary.ex` | mitigate | Query only IDs, statuses, timestamps, counts, and provider names needed for support cues; do not surface `raw_payload`, recipient fields, or redact-protected webhook bodies per D-33-15. |
| T-33-05 | Tampering / Misrepresentation | `lib/mailglass/operator/support_summary.ex` | mitigate | Keep failed ingest, replay outcome, orphan backlog, and reconcile facts as separate buckets so operators cannot confuse provider retries with replay or backlog recovery per D-33-08 and D-33-21. |
| T-33-06 | Elevation of Privilege | `test/mailglass/operator/support_summary_test.exs` | mitigate | Lock strict tenant scoping in tests so summary queries never aggregate across tenants or imply a cross-tenant support console. |
</threat_model>

<verification>
- `mix test test/mailglass/operator/support_summary_test.exs --warnings-as-errors`
- `mix test test/mailglass/webhook/replay_test.exs test/mailglass/webhook/reconciler_test.exs --warnings-as-errors`
- `rg -n "failed_ingest|orphan_backlog|replay_outcomes|reconcile_facts|summarize_tenant" lib/mailglass/operator/support_summary.ex test/mailglass/operator/support_summary_test.exs`
</verification>

<success_criteria>
The repo has a durable support-summary read model that returns truthful, exemplar-oriented support facts for one tenant and one time window, ready for the existing operator surface to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/33-observability-incident-support/33-02-SUMMARY.md`
</output>
