---
phase: 63-inbound-contract-inventory-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/docs/api_stability.md
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03]
must_haves:
  truths:
    - "Adopters can identify stable inbound runtime, testing, operator, telemetry, and error-contract seams from `mailglass_inbound/docs/api_stability.md`."
    - "Adopters can distinguish stable semantic seams from reachable internal modules such as provider implementations, replay internals, route structs, and worker or queue details."
    - "Adopters can see an explicit deferred-capability list so replay APIs, provider extension APIs, matcher expansion, lifecycle callbacks, fan-out, synthetic UI, `gen_smtp`, and ecosystem integrations are not promoted accidentally."
  artifacts:
    - path: "mailglass_inbound/docs/api_stability.md"
      provides: "Canonical inbound stable/testing/internal/deferred inventory"
      contains: "## Contract Posture"
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Fail-closed docs-contract assertions for the Phase 63 inventory and over-claim boundaries"
      contains: "mix mailglass.inbound.doctor"
  key_links:
    - from: "mailglass_inbound/docs/api_stability.md"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      via: "stable provider support and verify-first ingress semantics"
      pattern: "MailglassInbound\\.Ingress\\.Plug|verify before tenant"
    - from: "mailglass_inbound/docs/api_stability.md"
      to: "mailglass_inbound/lib/mailglass_inbound/telemetry.ex"
      via: "documented stable telemetry families and PII-safe metadata posture"
      pattern: "mailglass_inbound, :ingress|mailglass_inbound, :prune"
    - from: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      to: "mailglass_inbound/docs/api_stability.md"
      via: "literal docs-contract token assertions and refutes"
      pattern: "public replay API|provider extension API|mix mailglass\\.inbound\\.(doctor|replay|prune)"
---

<objective>
Reconcile the canonical inbound stability inventory with the shipped runtime so
the public contract is semantics-first, provider support is documented through
`MailglassInbound.Ingress.Plug`, and internal or deferred capabilities are
named explicitly rather than inferred from reachability.

Purpose: Implement D-01 through D-09 without widening the inbound public API.
Output: An updated `mailglass_inbound/docs/api_stability.md` plus inbound
docs-contract assertions that fail closed on future over-claims or omissions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-RESEARCH.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-VALIDATION.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-PATTERNS.md
@.planning/milestones/v1.1-phases/39-inbound-package-foundation/39-03-SUMMARY.md
@.planning/milestones/v1.3-phases/61-docs-contract-boundary-enforcement/61-03-SUMMARY.md
@docs/api_stability.md
@mailglass_admin/docs/api_stability.md
@mailglass_admin/docs/operator-trust.md
@mailglass_inbound/docs/api_stability.md
@mailglass_inbound/docs/inbound-mailgun.md
@mailglass_inbound/docs/inbound-operator.md
@mailglass_inbound/docs/inbound-ses.md
@mailglass_inbound/docs/inbound-testing.md
@mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex
@mailglass_inbound/lib/mailglass_inbound/telemetry.ex
@mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
@mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
@mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
@mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
@mix.exs

<interfaces>
Current stable ingress seam:
- `MailglassInbound.Ingress.Plug.init/1` accepts `provider` values `:postmark`, `:sendgrid`, `:mailgun`, and `:ses`.
- `MailglassInbound.Ingress.Plug.call/2` documents verify-before-tenant behavior, canonical-plus-raw persistence before execution dispatch, duplicate acknowledgement from durable receive truth, and replay staying distinct from fresh receipt.

Current internal provider seam:
- `MailglassInbound.Ingress.Provider` is `@moduledoc false`.
- It declares mixed-arity `verify!/2` and `verify!/3` callbacks plus `normalize/2`; this is implementation support, not an adopter-facing provider API.

Current stable operator semantics:
- `mix mailglass.inbound.doctor` exposes exit codes `0`, `1`, and `2`, plus `--strict`, `--format json`, and `--verbose`.
- `mix mailglass.inbound.replay` requires `--tenant`, uses `[y/N]` confirmation unless `--yes`, and replays stored truth rather than guessing across tenants.
- `mix mailglass.inbound.prune` requires typed `yes` unless `--yes`, runs synchronously with or without Oban, and treats `MailglassInbound.Prune.Worker` as optional scheduling support.

Current stable telemetry families:
- `[:mailglass_inbound, :ingress, :request, :start | :stop | :exception]`
- `[:mailglass_inbound, :route, :match, :start | :stop | :exception]`
- `[:mailglass_inbound, :persist, :record, :start | :stop | :exception]`
- `[:mailglass_inbound, :execution, :run, :start | :stop | :exception]`
- `[:mailglass_inbound, :ingress, :rate_limit, :start | :stop | :exception]`
- `[:mailglass_inbound, :ingress, :suppression_flag, :start | :stop | :exception]`
- `[:mailglass_inbound, :prune, :sweep, :start | :stop | :exception]`

Root verification lane:
- `mix.exs` `verify.stability_contract` already runs `cmd --cd mailglass_inbound mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`, then `mailglass.docs.check`, then `compile --no-optional-deps --warnings-as-errors`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the inbound stability inventory around the locked semantic taxonomy</name>
  <files>mailglass_inbound/docs/api_stability.md</files>
  <read_first>
    - mailglass_inbound/docs/api_stability.md
    - docs/api_stability.md
    - mailglass_admin/docs/api_stability.md
    - mailglass_admin/docs/operator-trust.md
    - mailglass_inbound/docs/inbound-mailgun.md
    - mailglass_inbound/docs/inbound-operator.md
    - mailglass_inbound/docs/inbound-ses.md
    - mailglass_inbound/docs/inbound-testing.md
    - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
    - mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex
    - mailglass_inbound/lib/mailglass_inbound/telemetry.ex
    - mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
    - mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    - mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-RESEARCH.md
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-VALIDATION.md
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-PATTERNS.md
  </read_first>
  <action>
    Rewrite `mailglass_inbound/docs/api_stability.md` per D-01 and D-02 so the
    intro and contract posture explicitly say the contract is the inventory in
    this file, not ExDoc visibility, generated docs reachability, or module
    reachability. Keep the four buckets `stable`, `testing`, `internal`, and
    `deferred`.

    In the `stable` inventory, document provider support through
    `MailglassInbound.Ingress.Plug` semantics and options per D-03: the stable
    public statement must name `provider: :postmark | :sendgrid | :mailgun |
    :ses`, verify-before-tenant semantics, canonical-plus-raw persistence
    before execution dispatch, duplicate acknowledgement from durable receive
    truth, and replay staying distinct from fresh receipt. Do not present
    `MailglassInbound.Ingress.Provider` or any
    `MailglassInbound.Ingress.Providers.*` module as public API per D-04.

    Expand the `stable` inventory to name the shipped non-runtime semantic seams
    required by D-05 and D-07: `mix mailglass.inbound.doctor`,
    `mix mailglass.inbound.replay`, `mix mailglass.inbound.prune`, the PII-safe
    telemetry families listed in `MailglassInbound.Telemetry`, and the stable
    error-contract seams `MailglassInbound.MIMEError`,
    `MailglassInbound.SignatureError`, and `MailglassInbound.S3FetchError`
    including closed `:type` sets and serialization exclusions. Phrase operator
    stability at the command-behavior level only: exit semantics, tenant guard,
    replay-over-stored-truth, confirmation tiers, prune destructiveness, and
    documented options. Do not freeze UI, DOM, queue names, worker args, or
    job structs.

    Make the `internal` and `deferred` sections explicit per D-06, D-08, and
    D-09. The internal list must name `MailglassInbound.Ingress.Provider`,
    `MailglassInbound.Ingress.Providers.Postmark`,
    `MailglassInbound.Ingress.Providers.Sendgrid`,
    `MailglassInbound.Ingress.Providers.Mailgun`,
    `MailglassInbound.Ingress.Providers.SES`,
    `MailglassInbound.Internal.Doctor`,
    `MailglassInbound.Internal.Replay`,
    `MailglassInbound.Internal.Prune`, `MailglassInbound.Execution`,
    `MailglassInbound.Execution.Worker`, `MailglassInbound.Prune.Worker`,
    `MailglassInbound.Router.Route`, persistence modules under
    `MailglassInbound.InboundRecords.*`, queue names, retry tuning, worker args,
    direct Oban job shapes, and admin or operator UI implementation details.
    The deferred list must name public replay API and rerouting controls,
    public provider extension API, public worker or queue contracts, matcher
    expansion beyond recipient/subject/header, lifecycle callbacks beyond
    `process/1`, multi-route fan-out, synthetic inbound development UI,
    `gen_smtp`, and ecosystem integrations.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/docs/api_stability.md` contains `stable`, `testing`, `internal`, and `deferred` headings under `## Contract Posture`.
    - The intro contains the exact tokens `ExDoc visibility` and `module reachability` in a sentence that says they do not define the contract.
    - The stable inventory contains `MailglassInbound.Ingress.Plug`, `mix mailglass.inbound.doctor`, `mix mailglass.inbound.replay`, `mix mailglass.inbound.prune`, `MailglassInbound.MIMEError`, `MailglassInbound.SignatureError`, and `MailglassInbound.S3FetchError`.
    - The stable inventory contains `:postmark`, `:sendgrid`, `:mailgun`, and `:ses` as `MailglassInbound.Ingress.Plug` provider semantics rather than provider-module APIs.
    - The stable inventory contains the telemetry-family strings `[:mailglass_inbound, :ingress, :request`, `[:mailglass_inbound, :route, :match`, `[:mailglass_inbound, :persist, :record`, `[:mailglass_inbound, :execution, :run`, `[:mailglass_inbound, :ingress, :rate_limit`, `[:mailglass_inbound, :ingress, :suppression_flag`, and `[:mailglass_inbound, :prune, :sweep`.
    - The internal inventory contains `MailglassInbound.Ingress.Provider`, `MailglassInbound.Ingress.Providers.Postmark`, `MailglassInbound.Ingress.Providers.Sendgrid`, `MailglassInbound.Ingress.Providers.Mailgun`, `MailglassInbound.Ingress.Providers.SES`, `MailglassInbound.Internal.Doctor`, `MailglassInbound.Internal.Replay`, `MailglassInbound.Internal.Prune`, `MailglassInbound.Execution.Worker`, `MailglassInbound.Prune.Worker`, `MailglassInbound.Router.Route`, `queue names`, and `worker args`.
    - The deferred inventory contains `public replay API`, `provider extension API`, `multi-route fan-out`, `lifecycle callbacks`, `synthetic inbound development UI`, `gen_smtp`, and `ecosystem integrations`.
    - `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    `mailglass_inbound/docs/api_stability.md` becomes the canonical semantics-first
    inventory for shipped stable seams, reachable internals, and deferred
    inbound capabilities without widening the public API surface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten inbound docs-contract assertions around the reconciled inventory</name>
  <files>mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <read_first>
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    - mailglass_inbound/docs/api_stability.md
    - mailglass_inbound/docs/inbound-operator.md
    - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
    - mailglass_inbound/lib/mailglass_inbound/telemetry.ex
    - mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
    - mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    - mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
    - mix.exs
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-RESEARCH.md
    - .planning/phases/63-inbound-contract-inventory-reconciliation/63-VALIDATION.md
  </read_first>
  <action>
    Update `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`
    so the quick inbound docs lane proves the Phase 63 inventory per D-01,
    D-03, D-05, D-08, and D-09. Keep the suite package-local; do not move this
    coverage to root docs tests.

    Add focused assertions that `mailglass_inbound/docs/api_stability.md`
    includes the new stable operator/task tokens, telemetry-family strings, the
    explicit semantics-first wording about ExDoc visibility and reachability,
    the stable `Ingress.Plug` provider options `:postmark`, `:sendgrid`,
    `:mailgun`, and `:ses`, and the explicit internal-module list for provider,
    replay, worker, queue, and route-struct details. Add explicit deferred-list
    assertions for `public replay API`, `provider extension API`, matcher
    expansion, lifecycle callbacks, `multi-route fan-out`, synthetic inbound
    development UI, `gen_smtp`, and ecosystem integrations.

    Pair those positive assertions with refutes that block public-surface
    over-claims such as provider module APIs, stable worker or queue contracts,
    replay-as-fresh wording, or any phrasing that implies ExDoc visibility
    defines stability. Keep the existing root-lane assertion that
    `mix verify.stability_contract` runs the inbound docs-contract test, and
    preserve unrelated README, SendGrid, and operator-trust coverage unless a
    token must change because the inventory wording changed in Task 1.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` contains a dedicated assertion block or tests that read `mailglass_inbound/docs/api_stability.md` and pin the Phase 63 inventory tokens.
    - The test file asserts the presence of `mix mailglass.inbound.doctor`, `mix mailglass.inbound.replay`, `mix mailglass.inbound.prune`, `:postmark`, `:sendgrid`, `:mailgun`, `:ses`, and the telemetry-family prefixes from `MailglassInbound.Telemetry`.
    - The test file asserts the presence of `MailglassInbound.Ingress.Provider`, `MailglassInbound.Internal.Doctor`, `MailglassInbound.Internal.Replay`, `MailglassInbound.Internal.Prune`, `MailglassInbound.Prune.Worker`, `MailglassInbound.Router.Route`, `provider extension API`, `multi-route fan-out`, `gen_smtp`, and `ecosystem integrations`.
    - The test file refutes over-claim phrases for provider module APIs, stable worker or queue contracts, replay-as-fresh semantics, and any wording that makes ExDoc visibility the contract.
    - The test file still asserts that root `mix.exs` runs `cmd --cd mailglass_inbound mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` inside `verify.stability_contract`.
    - `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    The inbound quick docs-contract lane fails closed on missing or misleading
    Phase 63 stability-inventory wording, so later docs and release phases can
    build on a canonical verified inventory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical inventory -> adopter integration decisions | Misleading docs could make adopters treat internal modules or unstable flows as supported public contract. |
| Operator/replay wording -> tenant-scoped recovery behavior | If replay or prune semantics are described imprecisely, operators can infer unsafe cross-tenant or fresh-receipt behavior. |
| Telemetry/error wording -> external observability and incident tooling | Over-claiming telemetry or error semantics can leak incorrect expectations about PII, verification, or recovery behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Spoofing | `mailglass_inbound/docs/api_stability.md` ingress/provider wording | mitigate | Per D-03 and D-04, document shipped provider support only through `MailglassInbound.Ingress.Plug` semantics and keep provider modules internal, then pin the wording in `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs`. |
| T-63-02 | Elevation of Privilege | Replay and operator contract wording | mitigate | Per D-05 through D-07, state `mix mailglass.inbound.replay --tenant` as the stable tenant guard, keep replay internals and worker or queue details internal, and pin replay-over-stored-truth and confirmation semantics in the docs-contract test. |
| T-63-03 | Information Disclosure | Telemetry and error-contract claims | mitigate | Document only the shipped PII-safe telemetry families and closed error-contract seams, and refute wording that implies raw payload, UI, or internal serialization details are public contract. |
| T-63-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs or dependency changes occur in this plan. |
</threat_model>

<verification>
- After each task: `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- Final phase gate: `mix verify.stability_contract`
</verification>

<success_criteria>
- `mailglass_inbound/docs/api_stability.md` names stable runtime, testing, operator, telemetry, and error-contract seams without promoting internal provider or operator modules.
- Existing provider support is documented through `MailglassInbound.Ingress.Plug` behavior and options, not provider-module APIs.
- Internal and deferred lists explicitly include replay internals, worker or queue details, route structs, provider modules, matcher expansion, lifecycle callbacks, fan-out, synthetic UI, `gen_smtp`, and ecosystem integrations.
- The inventory uses the same semantics-first contract language as core and admin: stable means explicit contract, not ExDoc visibility or module reachability.
- `mix verify.stability_contract` is the final green gate.
</success_criteria>

<output>
Create `.planning/phases/63-inbound-contract-inventory-reconciliation/63-01-SUMMARY.md` when done
</output>
