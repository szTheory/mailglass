---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 05
type: execute
wave: 3
depends_on: [157-01, 157-02]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/migration.ex
  - mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex
  - mailglass_inbound/lib/mailglass_inbound/migrations/postgres/v02.ex
  - mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_evidence.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  - mailglass_inbound/test/mailglass_inbound/migrations_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs
autonomous: true
requirements: [INB-04, DATA-01, DATA-02]
must_haves:
  truths:
    - "New and legacy inbound evidence dedupe throughout a resumable SHA-256 expand/backfill transition without editing V01."
    - "Fingerprint queries compare stored indexed columns directly, prefer SHA-256, and fall back to legacy MD5 only for unbackfilled rows."
    - "Every authenticated permanent failure commits tenant-scoped replayable evidence before the Plug acknowledges; unauthenticated failures create none."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/migrations/postgres/v02.ex"
      provides: "Additive SHA-256 and durable terminal-evidence schema/index transition"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex"
      provides: "Dual-write/read, bounded backfill, and terminal evidence transaction"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex"
      via: "terminal acknowledgement follows committed evidence result"
      pattern: "terminal|failure"
---

<objective>
Ship the inbound persistence tracer that keeps old/new MIME rows compatible and makes permanent acknowledgements replayable per D-06, D-09, and D-10.

Purpose: Existing installations must upgrade without dedupe gaps, and authenticated terminal failures must never disappear behind a successful acknowledgement.
Output: Append-only V02 migration, dual path, bounded backfill, durable terminal evidence, and Postgres/Plug proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-RESEARCH.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-01-SUMMARY.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-02-SUMMARY.md
@mailglass_inbound/lib/mailglass_inbound/migrations/postgres/v01.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
@mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_evidence.ex

<interfaces>
V01 remains byte-for-byte immutable. V02 is appended through the existing `MailglassInbound.Migration` → `Migrations.Postgres` dispatcher. Existing public Plug/replay/task façades and constraint error families stay compatible. Caller-supplied prefix remains authoritative.
</interfaces>
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Dedupe a mixed old/new MIME stream through SHA-256</name>
  <reversibility rating="one-way">D-09 adds persisted digest/evidence schema consumed by future releases; changing it later requires another compatibility migration.</reversibility>
  <files>mailglass_inbound/lib/mailglass_inbound/migrations/postgres/v02.ex, mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_evidence.ex, mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex, mailglass_inbound/test/mailglass_inbound/migrations_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs</files>
  <behavior>
    - DATA-01/D-09: V01→V02 adds nullable explicit 32-byte SHA-256 state/indexes, new evidence dual-writes it, and bounded resumable backfill converges legacy rows.
    - Mixed legacy/new redeliveries dedupe before, during, and after backfill; fresh V02 installs and rollback to V01 remain valid.
    - DATA-02/D-10: query-shape/EXPLAIN proof shows the new fingerprint column is directly compared and the legacy stored fingerprint is consulted only when SHA-256 is null.
  </behavior>
  <action>Write mixed-row, rerun/resume, direct-V02 upgrade/rollback, constraint translation, and query-plan tests first. Append V02 without altering V01: add explicit binary SHA-256 state and compatible provider-scoped indexes plus any additive terminal-evidence fields chosen for Task 3. Dual-write SHA-256, directly query stored fingerprint columns with tenant/provider scope, prefer SHA-256 and gate legacy fallback on null new state. Implement bounded primary-key/keyset backfill commits that are safe to rerun. Use bounded lock/statement settings and keep destructive contract/removal out of this phase.</action>
  <verify><automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/migrations_test.exs test/mailglass_inbound/ingress/persist_test.exs --warnings-as-errors</automated></verify>
  <done>Fresh and upgraded schemas dedupe mixed rows, bounded backfill resumes, direct-index proof passes, and V01 is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch V02 through fresh and upgrade migration façades</name>
  <files>mailglass_inbound/lib/mailglass_inbound/migration.ex, mailglass_inbound/lib/mailglass_inbound/migrations/postgres.ex, mailglass_inbound/test/mailglass_inbound/migrations_test.exs</files>
  <behavior>
    - D-09: public fresh installation reaches V02 and upgrade from V01 applies only V02; rollback returns to V01 without rewriting the applied wrapper.
    - Caller prefix/repo/version selection and Phase 155 generated-wrapper contracts remain unchanged.
  </behavior>
  <action>Wire V02 into the existing stable migration facade and version dispatcher after the direct migration behavior is green. Add fresh, V01-upgrade, rerun, and rollback dispatch tests using the existing explicit-repo/prefix conventions; preserve V01 bytes and all Phase 155 error/version semantics.</action>
  <verify><automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/migrations_test.exs --warnings-as-errors</automated></verify>
  <done>The public inbound migration path reaches V02 safely for fresh and existing hosts and rolls back only the additive version.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit replayable authenticated dead evidence before acknowledgement</name>
  <files>mailglass_inbound/lib/mailglass_inbound/inbound_records/inbound_evidence.ex, mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex, mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/test/mailglass_inbound/ingress/persist_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
  <behavior>
    - INB-04/D-06: authenticated permanent S3/routing/content failure stores tenant, byte-exact signed request, authenticated facts/envelope, closed failure class, and replay context before terminal acknowledgement.
    - A forced database failure returns retryable 500 and never sends the terminal acknowledgement; successful evidence is loadable by the existing replay path end to end.
    - Authentication failures remain 401 and create no tenant evidence.
  </behavior>
  <action>Use the smallest additive internal representation supported by existing evidence/replay tables and the V02 expansion from Task 1; do not add operator UI. Add transaction-failure ordering and replay tests first. Implement a tenant-scoped terminal-evidence transaction from the explicit verified request, storing only required durable raw/fact/context fields and a closed atom/string class. Make the Plug choose terminal acknowledgement only after commit; preserve transient 500 and PII-safe response/log/telemetry behavior.</action>
  <verify><automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/ingress/persist_test.exs test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors</automated></verify>
  <done>Permanent authenticated failures are replayable after acknowledgement, commit failures keep provider redelivery alive, and unauthenticated requests leave no evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Verified request → tenant database | Authenticated provider material becomes durable tenant evidence. |
| Old schema/data → new code | Mixed digest representations coexist during upgrade. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-11 | Repudiation | permanent acknowledgement | high | mitigate | Commit replayable evidence before terminal response; DB failure remains retryable. |
| T-157-12 | Tampering | MIME dedupe | high | mitigate | SHA-256 dual write/read, tenant/provider scope, unique indexes, mixed-row tests. |
| T-157-13 | Information Disclosure | dead evidence | high | mitigate | Tenant scope, private schema, closed outward errors, no raw logging/telemetry. |
</threat_model>

<verification>Run focused inbound migration/persistence/Plug suites, support contract, no-optional compile, formatter, and diff check.</verification>
<success_criteria>INB-04 and DATA-01/02 are complete with immutable V01, replayable terminal evidence, and no public/admin behavior change.</success_criteria>
<output>Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-05-SUMMARY.md` when done.</output>
