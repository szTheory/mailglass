---
phase: 157-inbound-database-and-lifecycle-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/ingress/verified_request.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  - mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs
autonomous: true
requirements: [INB-05]
must_haves:
  truths:
    - "An authenticated inbound request flows from verification through normalization as one explicit value carrying exact signed bytes and verification facts."
    - "SES authentication completes before tenant resolution and provider content retrieval, with no production ambient-process handoff or refetch."
    - "Existing public Plug and provider configuration contracts remain additive-compatible and mailglass_inbound remains independently releasable."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/verified_request.ex"
      provides: "Private explicit verified-request value"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      provides: "Ordered verify, tenant, resolve-content, normalize, persist pipeline"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/verified_request.ex"
      via: "verification returns the explicit value"
      pattern: "VerifiedRequest"
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex"
      via: "one canonical internal callback path with additive legacy adapters"
      pattern: "verify"
---

<objective>
Replace SES's ambient verification handoff with one production end-to-end verified-request pipeline per D-01 and D-02.

Purpose: Keep exact authenticated request material and provider facts explicit so later bounded retrieval and durable-failure paths cannot refetch, reorder, or lose them.
Output: Private verified value, canonical provider contract, ordered Plug pipeline, and regression proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-CONTEXT.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-RESEARCH.md
@.planning/phases/157-inbound-database-and-lifecycle-hardening/157-PATTERNS.md
@mailglass_inbound/lib/mailglass_inbound/ingress/request.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex

<interfaces>
Preserve `MailglassInbound.Ingress.Plug.init/1` and `call/2`, provider configuration keys, response codes, telemetry names, and legacy provider modules. The new verified value is private and carries the existing `Request`, exact `raw_body`, decoded authenticated envelope/facts, warnings, and resolved provider content. Legacy provider callbacks remain callable only through explicit adapters; internal production flow uses one canonical verified-value contract.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one SES request through verification and normalization</name>
  <reversibility rating="costly">D-01 changes the shared internal provider contract across all inbound providers while preserving their public edge.</reversibility>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/verified_request.ex, mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex, mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex, mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs</files>
  <behavior>
    - INB-05/D-01: SES verification returns one explicit value containing byte-exact signed body, decoded authenticated envelope, facts/warnings, and later resolved MIME; normalization consumes that value exactly once.
    - A caller process pre-seeded with stale ambient values cannot alter the verified result, and a normalization call cannot trigger a second S3 fetch.
    - Existing provider modules and supported callback arities compile through additive adapters without making legacy arity the canonical internal path.
  </behavior>
  <action>Write failing SES provider tests first. Add a private `%MailglassInbound.Ingress.VerifiedRequest{}` with enforced keys and opaque provider facts/content fields. Refactor SES to return and transform this value explicitly, removing production ambient state and fallback content retrieval. Update the provider behaviour around one canonical verify/normalize contract; keep supported legacy callbacks as thin adapters using explicit arguments so custom providers retain v2 compatibility. Keep raw bodies and provider errors out of inspect/log/telemetry metadata.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/ingress/ses_provider_test.exs --warnings-as-errors</automated>
  </verify>
  <done>SES verification and normalization prove a single explicit value owns exact bytes, facts, warnings, and resolved content with no ambient coupling or refetch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Order the Plug pipeline around authentication and tenant scope</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
  <behavior>
    - D-02: signature authentication happens before tenant resolution; tenant resolution happens before authenticated provider content retrieval.
    - Unauthenticated failures remain 401 and perform neither tenant resolution nor content retrieval.
    - Existing successful response, typed failure response, telemetry span, and PII-safe static response behavior remains unchanged.
  </behavior>
  <action>Add ordered spies and negative-path assertions first, then make the Plug pass the verified value through authentication, tenant resolution, content resolution, normalization, and persistence. Keep exactly one ingress telemetry span and existing closed response policy. Do not move broad Plug responsibility into a new public facade; Phase 158 owns architecture-wide factoring.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/ingress/ses_provider_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Tests prove the ordered pipeline and public Plug behavior while unauthenticated work stops before tenant/provider I/O.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider HTTP → verifier | Untrusted exact bytes and headers cross into authentication. |
| Verified request → tenant/provider I/O | Only authenticated facts may authorize scoped work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-157-01 | Spoofing | ingress pipeline | high | mitigate | Authentication precedes tenant resolution and retrieval, with ordered negative tests. |
| T-157-02 | Tampering | verified request handoff | high | mitigate | One explicit value retains exact bytes/facts and eliminates ambient stale state/refetch. |
| T-157-03 | Information Disclosure | telemetry/errors | medium | mitigate | Preserve static response bodies and closed metadata allowlists. |
</threat_model>

<verification>
Run both focused inbound suites, then `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` and `mix format --check-formatted`.
</verification>

<success_criteria>
INB-05 is observable through the unchanged Plug/provider façades, D-01/D-02 are implemented exactly, and no admin/operator UI or package-boundary file changes.
</success_criteria>

<output>
Create `.planning/phases/157-inbound-database-and-lifecycle-hardening/157-01-SUMMARY.md` when done.
</output>
