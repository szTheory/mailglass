---
phase: 66-release-position-decision
plan: 02
type: execute
wave: 2
depends_on:
  - 66-01
files_modified:
  - mailglass_inbound/CHANGELOG.md
  - mailglass_inbound/mix.exs
  - .release-please-manifest.json
  - mailglass_inbound/README.md
  - .planning/publish/mailglass_inbound-publish-summary.json
  - .planning/phases/66-release-position-decision/66-VERIFICATION.md
  - .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "The chosen candidate version is consistent across `mailglass_inbound/mix.exs`, `.release-please-manifest.json`, the README install pin, and the publish-summary evidence."
    - "Inbound release notes explain adopter action, verification commands, behavior changes, operator-impacting changes, compatibility posture, and stable/internal/deferred boundaries without becoming a second canonical contract."
    - "Phase planning state continues to block broad feature-growth work until the release-position decision is recorded and Phase 66 is closed."
  artifacts:
    - path: "mailglass_inbound/mix.exs"
      provides: "Chosen candidate version truth for the inbound package"
    - path: ".release-please-manifest.json"
      provides: "Manifest-level version truth for the chosen inbound candidate"
    - path: "mailglass_inbound/CHANGELOG.md"
      provides: "Operational release notes for the chosen candidate version"
    - path: ".planning/publish/mailglass_inbound-publish-summary.json"
      provides: "Candidate-version publish-proof summary refreshed by `mailglass.publish.check`"
    - path: ".planning/STATE.md"
      provides: "Explicit feature-growth gate and next-step release-governance posture"
  key_links:
    - from: "mailglass_inbound/mix.exs"
      to: ".release-please-manifest.json"
      via: "candidate version parity"
      pattern: "mailglass_inbound"
    - from: "mailglass_inbound/README.md"
      to: "mailglass_inbound/mix.exs"
      via: "install pin major/minor matches chosen package version"
      pattern: "~>"
    - from: "mailglass_inbound/CHANGELOG.md"
      to: "mailglass_inbound/docs/api_stability.md"
      via: "compatibility-routing links rather than duplicate contract prose"
      pattern: "api_stability\\.md"
    - from: ".planning/publish/mailglass_inbound-publish-summary.json"
      to: ".planning/phases/66-release-position-decision/66-VERIFICATION.md"
      via: "final candidate-version evidence capture"
      pattern: "manifest_version|source_ref|version"
---

<objective>
Apply the chosen release truth, publish sober release notes, rerun the release gates on the candidate version, and keep future feature-growth blocked until the decision is recorded.

Purpose: Finish REL-01 through REL-03 without changing product scope: one chosen candidate version, one operational changelog story, refreshed publish proof, and explicit planning-state guardrails.
Output: Candidate-version truth in package files, final release-note prose, refreshed publish-summary evidence, and updated planning-state gate language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/66-release-position-decision/66-CONTEXT.md
@.planning/phases/66-release-position-decision/66-RESEARCH.md
@.planning/phases/66-release-position-decision/66-VALIDATION.md
@.planning/phases/66-release-position-decision/66-PATTERNS.md
@.planning/phases/66-release-position-decision/66-RELEASE-POSITION.md
@.planning/phases/66-release-position-decision/66-VERIFICATION.md
@mailglass_inbound/docs/api_stability.md
@guides/compatibility-and-deprecations.md

<interfaces>
Current version-truth triad:
```text
mailglass_inbound/mix.exs            -> @version "0.3.0"
.release-please-manifest.json        -> "mailglass_inbound": "0.3.0"
.planning/publish/...publish-summary -> version/manifest_version/source_ref == 0.3.0
```

Release-automation invariants to preserve:
```text
release-please keeps `mailglass_inbound` as its own component
release-please workflow syncs README major.minor pins from manifest values
publish-hex keeps `workflow_dispatch` as the documented fallback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the chosen candidate version and operational release notes</name>
  <read_first>
    .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md
    .planning/phases/66-release-position-decision/66-VERIFICATION.md
    mailglass_inbound/CHANGELOG.md
    mailglass_inbound/mix.exs
    .release-please-manifest.json
    mailglass_inbound/README.md
    mailglass_inbound/docs/api_stability.md
    guides/compatibility-and-deprecations.md
    release-please-config.json
    .github/workflows/release-please.yml
    .github/workflows/publish-hex.yml
    .planning/publish/mailglass_inbound-publish-summary.json
  </read_first>
  <files>mailglass_inbound/CHANGELOG.md, mailglass_inbound/mix.exs, .release-please-manifest.json, mailglass_inbound/README.md</files>
  <action>Apply the candidate selected in `66-RELEASE-POSITION.md`. On the D-01 green path, set `mailglass_inbound` to `1.0.0`; on the D-02 blocker path, set it to one final explicit `0.4.0` confidence release because D-10 fixes current truth at `0.3.0`. Update `mailglass_inbound/mix.exs` `@version`, the `.release-please-manifest.json` `mailglass_inbound` entry, and the `mailglass_inbound/README.md` install snippet major/minor so they match the chosen version. Keep the existing `{:mailglass, "== 1.3.0"}` publish pin and current release-please topology unless source truth changed independently; Phase 66 must not relink inbound into the core/admin linked-versions group per D-11. Rewrite `mailglass_inbound/CHANGELOG.md` with D-06 sober operational release notes: adopter action required, exact verification commands, behavior changes since `0.3.0`, operator-impacting changes, compatibility posture, and explicit stable/internal/deferred boundaries. Per D-07 and D-08, summarize the compatibility posture plainly but route canonical guarantee truth to `mailglass_inbound/docs/api_stability.md` and `guides/compatibility-and-deprecations.md` instead of duplicating a second contract in changelog prose.</action>
  <acceptance_criteria>
    - `mailglass_inbound/mix.exs`, `.release-please-manifest.json`, and the `mailglass_inbound/README.md` install snippet all reflect the same chosen candidate version and major/minor pin.
    - `mailglass_inbound/CHANGELOG.md` includes adopter action required, both required verification commands, behavior changes since `0.3.0`, operator-impacting changes, compatibility posture, and explicit stable/internal/deferred boundaries.
    - The changelog links to `mailglass_inbound/docs/api_stability.md` and `guides/compatibility-and-deprecations.md` for canonical compatibility truth instead of restating a second contract.
    - No new release automation path is introduced and no release-please linked-version topology is broadened.
  </acceptance_criteria>
  <verify>
    <automated>jq -r '.["mailglass_inbound"]' .release-please-manifest.json && rg -n '@version "|~> 1\\.0|~> 0\\.4|adopter action required|mix verify\\.stability_contract|mix mailglass\\.publish\\.check --package mailglass_inbound|api_stability\\.md|compatibility-and-deprecations\\.md' mailglass_inbound/mix.exs mailglass_inbound/README.md mailglass_inbound/CHANGELOG.md</automated>
  </verify>
  <done>The repo has one chosen inbound candidate version, one aligned README pin, and one operational changelog story that does not overstate the compatibility contract.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh publish proof and keep feature-growth blocked in planning state</name>
  <read_first>
    .planning/phases/66-release-position-decision/66-VERIFICATION.md
    .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md
    .planning/phases/66-release-position-decision/66-VALIDATION.md
    .planning/publish/mailglass_inbound-publish-summary.json
    mailglass_inbound/mix.exs
    .planning/STATE.md
    .planning/ROADMAP.md
  </read_first>
  <files>.planning/publish/mailglass_inbound-publish-summary.json, .planning/phases/66-release-position-decision/66-VERIFICATION.md, .planning/phases/66-release-position-decision/66-RELEASE-POSITION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Per D-05, rerun `mix verify.stability_contract` and `mix mailglass.publish.check --package mailglass_inbound` after Task 1 so the final evidence matches the chosen candidate version, not only the pre-bump `0.3.0` state. Let `mix mailglass.publish.check --package mailglass_inbound` refresh `.planning/publish/mailglass_inbound-publish-summary.json`, then update `66-VERIFICATION.md` with the final candidate-version results and publish-summary fields. Reconcile `66-RELEASE-POSITION.md` against those final results: keep the candidate green and ready if both commands pass, or pivot to the D-02 final `0.4.0` path with explicit blocker evidence if the candidate exposes a real release blocker after versioning. Update `.planning/STATE.md` and `.planning/ROADMAP.md` so REL-03 stays explicit: Phase 66 is the release-governance gate, broad feature-growth remains blocked until this decision is complete, and the next-step posture after Phase 66 is release ceremony or maintenance rather than new scope. When editing roadmap/state, restrict the change to Phase 66 and the feature-growth gate; do not normalize unrelated historical bookkeeping drift.</action>
  <acceptance_criteria>
    - `.planning/publish/mailglass_inbound-publish-summary.json` `version`, `manifest_version`, and `source_ref` match the chosen candidate version.
    - `66-VERIFICATION.md` records the final candidate-version results for both required commands, not only the initial `0.3.0` preflight.
    - `66-RELEASE-POSITION.md` status matches the final candidate-version evidence and names blockers explicitly if fallback is required.
    - `.planning/STATE.md` and `.planning/ROADMAP.md` contain explicit Phase 66 release-position gating language and do not open broad feature-growth work.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.stability_contract && mix mailglass.publish.check --package mailglass_inbound && jq -r '.version, .manifest_version, .source_ref' .planning/publish/mailglass_inbound-publish-summary.json && rg -n "release-position decision|feature-growth|release ceremony|maintenance" .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <done>The chosen candidate version has fresh publish-proof evidence, the binary release-position artifact matches that evidence, and planning state still blocks broad feature-growth until Phase 66 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release-position artifact -> version-truth files | A governance decision is being translated into package versions, README pins, and changelog prose. |
| candidate release files -> publish proof | The chosen candidate must survive the existing publish-check lane without contradicting manifest or README truth. |
| planning state -> future milestone selection | State/roadmap wording controls whether later work reopens broad feature growth prematurely. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-66-04 | Tampering | high | candidate version truth | mitigate | yes | Per D-09 and D-10, keep `mailglass_inbound/mix.exs`, `.release-please-manifest.json`, README major/minor pins, and publish-summary fields aligned to one chosen candidate; blocking acceptance is Task 2’s combined automated verify command plus `jq` parity check. |
| T-66-05 | Repudiation | high | `mailglass_inbound/CHANGELOG.md` release notes | mitigate | yes | Per D-06 through D-08, require sober operational sections, the exact verification commands, and canonical compatibility links so the changelog cannot become a contradictory second contract; blocking acceptance is Task 1’s `rg` verify command. |
| T-66-06 | Tampering | high | feature-growth gate in `.planning/STATE.md` / `.planning/ROADMAP.md` | mitigate | yes | Per REL-03, keep explicit Phase 66 release-position gate wording and do not open new feature-growth scope; blocking acceptance is Task 2’s `rg` verify command over state and roadmap. |
| T-66-07 | Spoofing | medium | release automation fallback story | mitigate | no | Preserve the existing release-please plus `workflow_dispatch` fallback narrative and do not introduce a second publish path or linked-version topology change. |
</threat_model>

<verification>
- `jq -r '.["mailglass_inbound"]' .release-please-manifest.json`
- `rg -n '@version "|~> 1\\.0|~> 0\\.4|adopter action required|mix verify\\.stability_contract|mix mailglass\\.publish\\.check --package mailglass_inbound|api_stability\\.md|compatibility-and-deprecations\\.md' mailglass_inbound/mix.exs mailglass_inbound/README.md mailglass_inbound/CHANGELOG.md`
- `mix verify.stability_contract`
- `mix mailglass.publish.check --package mailglass_inbound`
- `jq -r '.version, .manifest_version, .source_ref' .planning/publish/mailglass_inbound-publish-summary.json`
- `rg -n "release-position decision|feature-growth|release ceremony|maintenance" .planning/STATE.md .planning/ROADMAP.md`
</verification>

<success_criteria>
- The chosen inbound candidate version is coherent across package code, manifest truth, README install pins, and publish-summary evidence.
- Release notes are operational and bounded, and they point to canonical compatibility sources instead of becoming a second contract.
- Planning artifacts keep broad feature-growth blocked until the release-position decision is recorded and Phase 66 is closed.
</success_criteria>

<output>
Create `.planning/phases/66-release-position-decision/66-02-SUMMARY.md` when done
</output>
