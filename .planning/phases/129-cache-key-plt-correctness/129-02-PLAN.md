---
phase: 129-cache-key-plt-correctness
plan: 02
type: execute
wave: 2
depends_on:
  - 129-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CACHE-02
must_haves:
  truths:
    - "The dialyzer job's PLT cache key is toolchain-scoped from `.tool-versions` (reusing Plan 01's hash) and points at the PLT path that actually exists under the job's MIX_ENV."
    - "A stale/corrupt PLT is evicted and rebuilt by the workflow (Bandit two-step: continue-on-error dialyzer, then an eviction+rebuild step gated on the first step's failure), and the job still reds on a genuine type error."
    - "Dialyzer remains advisory/non-required — it is NOT added to the CI-Green needs / branch-protection set this phase (LD-7 sequencing)."
  artifacts:
    - ".github/workflows/ci.yml (dialyzer job: PLT cache block + two-step self-heal)"
  key_links:
    - "The dialyzer job sets MIX_ENV: test, so the runtime PLT lives under `_build/test/*.plt`, NOT `_build/dev/*.plt` — the cache `path:`, the eviction `rm -rf`, and the rebuild must all target the correct env's PLT dir or the self-heal is a no-op."
    - "Reuse Plan 01's `${{ hashFiles('.tool-versions') }}` toolchain dim in the plt- key so a toolchain bump busts the PLT cache too (depends_on 129-01)."
---

<objective>
Give the Dialyzer PLT cache Bandit-style self-healing eviction: a corrupted/stale PLT is evicted and rebuilt by the workflow instead of wedging the job, while a genuine type error still reds the job (§P1-A, LD-7, CACHE-02). Toolchain-scope the PLT cache key to the `.tool-versions` hash Plan 01 introduced.

Purpose: This is the precondition LD-7 requires BEFORE Dialyzer can later be promoted toward the required set. This phase delivers the eviction MECHANISM ONLY — it does NOT promote Dialyzer to required.

Output: The `ci.yml` dialyzer job rewritten — PLT cache block (toolchain-scoped key, correct MIX_ENV PLT path) + a two-step Dialyzer run (continue-on-error, then gated evict+rebuild). No product code, no schema change (D-23). No OTP/Elixir bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-cache-key-plt-correctness/129-CONTEXT.md
@.planning/research/milestone-cicd/SYNTHESIS.md
@.planning/research/milestone-cicd/CICD-RELEASE-HARDENING.md
@.github/workflows/ci.yml
</context>

<interface_context>
Current dialyzer job (`ci.yml` ~466-524), verified:
- `name: Dialyzer (Elixir 1.18 / OTP 27)` — display-name contract label, DO NOT rename (scope fence).
- Job `env:` sets `MIX_ENV: test`. So the runtime PLT is built under `_build/test/`, NOT `_build/dev/`.
- Current PLT cache block (~510-516): `path: _build/dev/*.plt`, `key: ${{ runner.os }}-dialyzer-plt-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}`, `restore-keys: ${{ runner.os }}-dialyzer-plt-${{ matrix.otp }}-${{ matrix.elixir }}-`. NOTE the `path:` is `_build/dev` while the job runs `MIX_ENV: test` — a latent path mismatch this plan corrects.
- Current Dialyzer run (~523-524): a single `- name: Run Dialyzer` / `run: mix dialyzer` step (no continue-on-error, no eviction).
- The deps-cache block (~503-509) is Plan 01's responsibility — Plan 01 runs first (wave 1); this plan MUST NOT touch it.
- `mix.exs` dialyzer config: no explicit `plt_file`, so the PLT lands in `_build/$MIX_ENV/*.plt` (confirmed: locally `_build/test/dialyxir_*.plt` and `_build/dev/dialyxir_*.plt` both exist; on CI with MIX_ENV:test the active one is under `_build/test/`).

Bandit self-heal pattern to adopt (§P1-A, adapted to this repo's MIX_ENV:test and the `.tool-versions` hash):
- PLT cache: `path: _build/test/*.plt`; `key: plt-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-${{ hashFiles('**/mix.lock') }}`; `restore-keys: plt-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-` (same toolchain-hash prefix, never a broad `plt-` restore-key).
- Step 1: `- name: Run Dialyzer` / `id: dialyzer` / `continue-on-error: true` / `run: mix dialyzer`.
- Step 2: `- name: Evict stale PLT + rebuild` / `if: steps.dialyzer.outcome == 'failure'` / `run: rm -rf _build/test/*.plt && mix dialyzer --plt && mix dialyzer`. The re-run's exit code is the step's (and job's) real result — a genuine type error reds; only PLT-staleness self-heals.
- The existing `- name: Verify ignore-file docs (shell gate)` step (~519-522, `bash scripts/check_dialyzer_ignore.sh`) stays — keep it before the Dialyzer run.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain-scope the PLT cache key and fix its MIX_ENV path</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `ci.yml` dialyzer job (~466-524), especially the PLT cache block (~510-516) and the job `env: MIX_ENV: test`.
    - PLT-path note in <interface_context> (MIX_ENV:test → `_build/test/*.plt`).
    - Plan 01's `.tool-versions` hash convention (`${{ hashFiles('.tool-versions') }}`).
  </read_first>
  <action>
    Rewrite ONLY the `- name: Cache PLT` block in the dialyzer job. Keep the `actions/cache@<sha>` SHA pin unchanged; edit the `with:` body:
    - `path: _build/test/*.plt` (match the job's `MIX_ENV: test`; the old `_build/dev/*.plt` was caching a dir the test-env run doesn't produce).
    - `key: plt-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-${{ hashFiles('**/mix.lock') }}` (toolchain dim from the single source, reusing Plan 01's hash — replaces the `${{ matrix.otp }}-${{ matrix.elixir }}` literals).
    - `restore-keys: plt-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-` (same toolchain-hash prefix; keep `plt-` distinct from the deps `mix-` prefix so the two caches never collide).

    Do NOT touch the deps-cache block (~503-509) — Plan 01 owns it. Do NOT rename the job `name:`. Do NOT touch `mix.exs`.
  </action>
  <verify>
    <automated>bash -c 'grep -n "path: _build/test/\*.plt" .github/workflows/ci.yml >/dev/null && grep -n "plt-.*hashFiles(..\.tool-versions..)" .github/workflows/ci.yml >/dev/null && ! grep -n "path: _build/dev/\*.plt" .github/workflows/ci.yml && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - The PLT cache `path:` is `_build/test/*.plt` (matches MIX_ENV:test); no `_build/dev/*.plt` PLT path remains.
    - The PLT cache key + restore-key are toolchain-scoped via `${{ hashFiles('.tool-versions') }}`; no `${{ matrix.otp }}`/`${{ matrix.elixir }}` literal remains in the PLT key.
    - The `plt-` prefix is distinct from the deps `mix-` prefix; the deps-cache block is unchanged; the job `name:` is unchanged; the Action SHA pin is unchanged.
  </acceptance_criteria>
  <done>The PLT cache is toolchain-scoped from the single source and points at the PLT the test-env job actually builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Bandit two-step self-healing Dialyzer run</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `ci.yml` dialyzer job run steps (~519-524): the ignore-file shell gate + the single `Run Dialyzer` step.
    - Bandit two-step pattern in <interface_context> (continue-on-error + gated evict/rebuild; final result must reflect the real Dialyzer outcome).
    - Scope fence: do NOT add the dialyzer job to the required `CI Green` needs / branch-protection set (LD-7 — mechanism only, no promotion).
  </read_first>
  <action>
    Replace the single `- name: Run Dialyzer` / `run: mix dialyzer` step with the Bandit two-step self-heal, keeping the preceding `- name: Verify ignore-file docs (shell gate)` step intact and before it:

    Step 1 — `- name: Run Dialyzer` with `id: dialyzer`, `continue-on-error: true`, `run: mix dialyzer`. (continue-on-error lets a stale-PLT failure fall through to the eviction step instead of failing the job immediately.)

    Step 2 — `- name: Evict stale PLT + rebuild` with `if: steps.dialyzer.outcome == 'failure'`, `run: rm -rf _build/test/*.plt && mix dialyzer --plt && mix dialyzer`. This deletes the stale PLT, rebuilds it, and re-runs Dialyzer. The re-run's exit status is the step's result and (since it is NOT continue-on-error) the job's result — so a genuine type error still reds the job; only PLT-staleness self-heals.

    Brand/voice: any step name copy stays specific and plain ("Evict stale PLT + rebuild"), no cute phrasing (CLAUDE.md voice).

    Do NOT promote the dialyzer job into `ci-green` `needs` or branch protection. Do NOT rename the job. Keep the `rm -rf` targeting `_build/test/*.plt` to match Task 1's path.
  </action>
  <verify>
    <automated>bash -c 'grep -n "id: dialyzer" .github/workflows/ci.yml >/dev/null && grep -n "continue-on-error: true" .github/workflows/ci.yml >/dev/null && grep -n "steps.dialyzer.outcome == .failure." .github/workflows/ci.yml >/dev/null && grep -n "rm -rf _build/test/\*.plt" .github/workflows/ci.yml >/dev/null && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Dialyzer runs as two steps: step 1 `id: dialyzer` + `continue-on-error: true` + `mix dialyzer`; step 2 gated `if: steps.dialyzer.outcome == 'failure'` doing `rm -rf _build/test/*.plt && mix dialyzer --plt && mix dialyzer`.
    - The eviction `rm -rf` path matches the Task 1 PLT path (`_build/test/*.plt`).
    - The final job conclusion still reflects the real Dialyzer result (step 2 is not continue-on-error) — a genuine type error reds.
    - Dialyzer is NOT added to the required CI-Green needs / branch-protection set.
  </acceptance_criteria>
  <done>A stale/corrupt PLT self-heals via evict+rebuild; a real type error still fails the job; Dialyzer stays advisory.</done>
</task>

<task type="auto">
  <name>Task 3: Document a reproducible PLT-corruption recovery proof</name>
  <files>.planning/phases/129-cache-key-plt-correctness/129-02-SUMMARY.md</files>
  <read_first>
    - The two-step self-heal added in Task 2.
    - Verification posture in 129-CONTEXT.md §Specifics: prove eviction recovers via an intentional PLT-corruption run — a real, observed recovery, not a claim.
  </read_first>
  <action>
    Produce a documented, reproducible proof that the eviction self-heals — recorded in the plan SUMMARY (not a shipped test; LD-9 retired the grep-police, and a corruption-recovery proof is an operational check, not a unit test).

    Provide BOTH a local proof recipe and the CI-observable evidence path:
    - Local recipe: with MIX_ENV=test, build the PLT (`MIX_ENV=test mix dialyzer --plt`), corrupt it (`printf 'garbage' > _build/test/<the>.plt` or truncate it), run `MIX_ENV=test mix dialyzer` (observe it fail on a corrupt PLT), then run the exact eviction command (`rm -rf _build/test/*.plt && mix dialyzer --plt && mix dialyzer`) and observe it recover to green. Record the observed commands + outcomes in the SUMMARY.
    - CI evidence: on the `phase/129` run, the dialyzer job logs show the step-1 outcome and, when a PLT is stale, the step-2 "Evict stale PLT + rebuild" step running and passing. Note where in the Actions log this is visible.

    ALSO record the negative control in the SUMMARY: confirm a GENUINE type error (not PLT staleness) still reds the job — describe the reasoning (step 2 re-run is not continue-on-error, so a real Dialyzer error survives the rebuild and fails the job). A one-off local demonstration (introduce a temporary type error, observe red, revert) is encouraged but the reasoning + the two-step structure is the minimum evidence.
  </action>
  <verify>
    <human-check>SUMMARY records an observed local corruption→recovery run (commands + outcomes) AND identifies the CI log location for the eviction step; the negative-control reasoning (real type error still reds) is documented.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY documents a reproducible PLT-corruption→eviction→recovery proof with observed commands/outcomes (not a claim).
    - The CI-log evidence location for the self-heal step is identified.
    - The negative control (genuine type error still reds the job) is documented.
  </acceptance_criteria>
  <done>There is a real, reproducible, recorded proof that the PLT self-heals and that a genuine type error is not masked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLT cache artifact → dialyzer job | A restored PLT built under a different toolchain or a corrupt PLT crosses into the type-check step; the self-heal converts a wedged job into an evict+rebuild. |
| eviction step → job conclusion | The gated re-run must faithfully surface a genuine type error (not swallow it) — the correctness boundary of the self-heal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-04 | Tampering (masked failure) | continue-on-error Dialyzer step | high | mitigate | Only step 1 is continue-on-error; the step-2 rebuild+re-run is NOT, so a genuine type error still reds the job. Negative control documented (Task 3). |
| T-129-05 | Denial of Service (no-op self-heal) | wrong PLT path | high | mitigate | `path:` + `rm -rf` target `_build/test/*.plt` to match MIX_ENV:test (Task 1) — the old `_build/dev` path would make the eviction a no-op. |
| T-129-06 | Elevation (premature promotion) | Dialyzer → required set | medium | accept | Scope fence LD-7: Dialyzer stays advisory this phase; promotion is a later, separate decision. Not added to CI-Green needs. |
| T-129-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs (infra-only YAML edits + a doc proof). No new deps. |
</threat_model>

<verification>
Phase-level (CI-observable, self-verify shift-left):
1. `phase/129` CI: the dialyzer job runs the two-step self-heal; the PLT cache key carries the `.tool-versions` toolchain hash (visible in the cache-restore log line) and caches `_build/test/*.plt`.
2. Reproducible corruption proof (Task 3): an intentionally corrupt PLT is evicted and rebuilt to green — recorded with observed commands/outcomes.
3. Negative control: a genuine type error still reds the dialyzer job (documented reasoning + optional local demo).
4. Dialyzer is NOT in the required CI-Green needs / branch-protection set (LD-7 sequencing preserved).
</verification>

<success_criteria>
- The PLT cache is toolchain-scoped from `.tool-versions` and points at the MIX_ENV:test PLT.
- A stale/corrupt PLT self-heals via the Bandit two-step evict+rebuild; a genuine type error still reds the job.
- Dialyzer remains advisory (not promoted to required).
- The corruption-recovery proof is real and reproducible; phase/129 CI green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- **`ci.yml` dialyzer job — PLT cache block** rewritten: `path: _build/test/*.plt`, key `plt-${{ runner.os }}-${{ hashFiles('.tool-versions') }}-${{ hashFiles('**/mix.lock') }}`.
- **`ci.yml` dialyzer job — two-step self-heal**: step `id: dialyzer` (`continue-on-error: true`) + step `Evict stale PLT + rebuild` (`if: steps.dialyzer.outcome == 'failure'`).
- **PLT-corruption recovery proof** recorded in `129-02-SUMMARY.md` (operational proof, not a shipped test).
</artifacts_produced>

<output>
Create `.planning/phases/129-cache-key-plt-correctness/129-02-SUMMARY.md` when done.
</output>