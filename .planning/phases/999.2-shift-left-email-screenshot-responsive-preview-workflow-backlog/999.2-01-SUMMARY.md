---
phase: 999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog
plan: "01"
subsystem: ui
tags: [liveview, preview, capture-matrix, deterministic]

requires:
  - phase: 999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog
    provides: PreviewLive scenario render truth and preview_props/0 discovery seams
provides:
  - Deterministic URL-state parsing/fallback for preview width and theme capture entrypoints
  - Deterministic capture-state and capture-matrix primitives with stable ordering
  - Regression tests for URL state behavior and matrix determinism/skip metadata
affects: [preview-live, screenshot-workflow, wave-2-capture-task]

tech-stack:
  added: []
  patterns: [allowlisted query param parsing, tuple-sorted deterministic matrix generation]

key-files:
  created:
    - mailglass_admin/lib/mailglass_admin/preview/capture_state.ex
    - mailglass_admin/lib/mailglass_admin/preview/capture_matrix.ex
    - mailglass_admin/test/mailglass_admin/preview/capture_matrix_test.exs
  modified:
    - mailglass_admin/lib/mailglass_admin/preview_live.ex
    - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
    - mailglass_admin/test/mailglass_admin/preview_live_test.exs

key-decisions:
  - Keep initial scenario mounts non-redirecting while still canonicalizing width/theme through scenario links and toggle-driven push_patch updates.
  - Represent discovery failures and :no_previews as explicit skipped metadata rather than hard-failing matrix generation.

patterns-established:
  - "Preview URL state pattern: width/theme query params parsed via allowlists with deterministic fallback."
  - "Capture matrix pattern: stable tuple sort by mailable/scenario/width/theme with reproducible output."

requirements-completed: ["TBD (backlog phase)"]

duration: 4 min
completed: 2026-05-27
---

# Phase 999.2 Plan 01: Deterministic Preview URL State And Capture Matrix Foundations Summary

**PreviewLive now accepts deterministic width/theme URL state and the admin preview surface now has deterministic capture matrix primitives over scenario x width x theme.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-27T11:08:00Z
- **Completed:** 2026-05-27T11:12:59Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Added strict `width`/`theme` URL parsing in `PreviewLive` with deterministic fallbacks (`768`, `mailglass-light`) and URL sync on device/theme interactions.
- Preserved preview state continuity by passing current width/theme through scenario links and keeping URL state replayable for capture entrypoints.
- Added deterministic capture helpers (`CaptureState`/`CaptureMatrix`) with tuple-sorted output and explicit skipped metadata for discovery edge cases.
- Added regression tests for valid/invalid URL behavior, canonical URL sync, deterministic matrix ordering, and skip metadata handling.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make PreviewLive state deterministic and URL-canonical for capture entrypoints** - `738a6af` (feat)
2. **Task 2: Add deterministic capture-state and matrix helper modules with ordering tests** - `b89b140` (feat)

**Plan metadata:** `(this commit)`

## Files Created/Modified
- `mailglass_admin/lib/mailglass_admin/preview_live.ex` - Added URL param parsing, fallback normalization, and canonical URL sync on interactive state changes.
- `mailglass_admin/lib/mailglass_admin/preview/sidebar.ex` - Preserved width/theme query state when selecting scenarios from the sidebar.
- `mailglass_admin/test/mailglass_admin/preview_live_test.exs` - Added URL-state regression coverage for valid params, invalid fallback, and canonical sync.
- `mailglass_admin/lib/mailglass_admin/preview/capture_state.ex` - Added canonical capture-state struct, dimensions, URL builder, and sort key.
- `mailglass_admin/lib/mailglass_admin/preview/capture_matrix.ex` - Added deterministic matrix generation with tuple sorting and skipped-entry metadata.
- `mailglass_admin/test/mailglass_admin/preview/capture_matrix_test.exs` - Added repeated-run determinism and skip-metadata tests.

## Decisions Made
- Avoided mount-time redirect canonicalization because it interfered with existing LiveView test entrypoints; canonical URL state is now enforced via scenario links and interaction patches instead.
- Kept matrix output resilient by treating `:no_previews` and discovery errors as explicit skipped records.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Initial canonicalization-by-redirect approach caused `live/2` mount redirect failures in existing `PreviewLive` tests. Resolved by moving canonicalization to link/event URL sync while preserving deterministic fallback behavior.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Wave 1 foundations are in place for Wave 2 mix-task orchestration to consume deterministic matrix entries directly.
- No blockers found for composing capture task orchestration over `CaptureMatrix.build_matrix/2`.

## Self-Check: PASSED

---
*Phase: 999.2-shift-left-email-screenshot-responsive-preview-workflow-backlog*
*Completed: 2026-05-27*
