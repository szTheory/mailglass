---
phase: 07-installer-ci-cd-docs
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - release-please-config.json
  - .release-please-manifest.json
  - .github/scripts/check_hex_tarballs.sh
  - mix.exs
  - mailglass_admin/mix.exs
  - LICENSE
  - CHANGELOG.md
autonomous: true
requirements:
  - CI-04
  - CI-05
  - CI-07
must_haves:
  truths:
    - "Release Please is configured in manifest mode with linked versions for `mailglass` and `mailglass_admin`."
    - "Hex publish path is isolated to protected release flow and GitHub Environment approval."
    - "Publish workflow does not run on pull requests and never exposes `HEX_API_KEY` to PR jobs."
    - "Tarball content and size checks are blocking before publish (<500KB core, <2MB admin)."
    - "Root release artifacts expected by package whitelists (`LICENSE`, `CHANGELOG.md`) are present."
  artifacts:
    - path: "release-please-config.json"
      provides: "CI-04 linked-version release automation"
      contains: "\"linked-versions\""
    - path: ".release-please-manifest.json"
      provides: "coordinated version state for root and admin packages"
      contains: "\"mailglass_admin\""
    - path: ".github/workflows/publish-hex.yml"
      provides: "CI-07 protected ref + environment gated publish workflow"
      contains: "environment:"
    - path: ".github/scripts/check_hex_tarballs.sh"
      provides: "CI-05 tarball whitelist and size enforcement"
      contains: "500000"
---

<objective>
Implement secure release automation and protected publish controls so coordinated core/admin releases can ship safely without leaking publish credentials.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Configure Release Please with linked versions for root and admin packages</name>
  <files>
    - release-please-config.json
    - .release-please-manifest.json
    - .github/workflows/release-please.yml
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/CLAUDE.md
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-CONTEXT.md
  </read_first>
  <action>
    Add Release Please configuration in manifest mode:
    - packages:
      - `"."` with release type `elixir`
      - `"mailglass_admin"` with release type `elixir`
    - plugin list includes `"linked-versions"`
    - `"separate-pull-requests": false`

    Add release workflow:
    - trigger on push to `main`
    - least-privilege permissions for release-please operations
    - no publish secret usage in this workflow.
  </action>
  <acceptance_criteria>
    - `test -f release-please-config.json && test -f .release-please-manifest.json` exits 0.
    - `rg "\"linked-versions\"" release-please-config.json` returns one match.
    - `rg "\"separate-pull-requests\"\\s*:\\s*false" release-please-config.json` returns one match.
    - `rg "\"mailglass_admin\"" .release-please-manifest.json release-please-config.json` returns matches in both files.
    - `test -f .github/workflows/release-please.yml` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add protected `publish-hex.yml` workflow with environment gate and publish ordering</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/CLAUDE.md
    - /Users/jon/projects/mailglass/mix.exs
    - /Users/jon/projects/mailglass/mailglass_admin/mix.exs
  </read_first>
  <action>
    Create `publish-hex.yml` with concrete controls:
    - trigger on release publication (or protected tag flow only)
    - no `pull_request` trigger
    - workflow/job `environment` uses protected environment (for example `hex-publish`)
    - publish order:
      1. build/check/publish `mailglass`
      2. build/check/publish `mailglass_admin`
    - environment secret usage limited to publish jobs (`HEX_API_KEY`)
    - permissions restricted to required minimum (`contents: read` plus publish-specific needs)
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/publish-hex.yml` exits 0.
    - `rg "pull_request" .github/workflows/publish-hex.yml` returns no matches.
    - `rg "environment:\\s*hex-publish|environment:\\s*name:\\s*hex-publish" .github/workflows/publish-hex.yml` returns one or more matches.
    - `rg "HEX_API_KEY" .github/workflows/publish-hex.yml` returns matches only in publish job steps.
    - `rg "mailglass_admin" .github/workflows/publish-hex.yml` returns match after core publish step (ordered sequence).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add tarball whitelist/size checks and align package artifacts</name>
  <files>
    - .github/scripts/check_hex_tarballs.sh
    - mix.exs
    - mailglass_admin/mix.exs
    - LICENSE
    - CHANGELOG.md
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/mix.exs
    - /Users/jon/projects/mailglass/mailglass_admin/mix.exs
  </read_first>
  <action>
    Add a blocking script `check_hex_tarballs.sh` that:
    - builds tarballs for root and admin packages
    - validates allowed file sets per package whitelist
    - enforces size caps:
      - `mailglass` < `500000` bytes
      - `mailglass_admin` < `2000000` bytes

    Align package metadata and required root artifacts:
    - ensure `LICENSE` exists at repo root
    - ensure root `CHANGELOG.md` exists
    - ensure `mix.exs` / `mailglass_admin/mix.exs` package file lists include those artifacts where expected.
  </action>
  <acceptance_criteria>
    - `test -f .github/scripts/check_hex_tarballs.sh` exits 0.
    - `rg "500000|2000000" .github/scripts/check_hex_tarballs.sh` returns both thresholds.
    - `test -f LICENSE && test -f CHANGELOG.md` exits 0.
    - `rg "LICENSE|CHANGELOG\\.md" mix.exs mailglass_admin/mix.exs` returns matches in package file lists.
    - `.github/scripts/check_hex_tarballs.sh` exits 0 locally.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
security_enforcement: true
security_asvs_level: 1
security_block_on: high

Any HIGH threat below is release-blocking for this plan until mitigated and acceptance checks pass.

| Threat ID | Severity | Category | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-07-05-A | high | Information Disclosure | Hex publish credentials | block | Publish workflow uses protected environment approvals; no PR trigger; `HEX_API_KEY` scoped to publish jobs only. |
| T-07-05-B | high | Tampering | Version drift between root/admin | block | Release Please linked-versions config + single coordinated release PR. |
| T-07-05-C | high | Tampering | Package content/size drift | block | Blocking tarball validation script enforces whitelist and byte budgets before publish. |
| T-07-05-D | medium | Repudiation | Missing release artifacts | mitigate | Root `LICENSE` and `CHANGELOG.md` committed and validated by package list checks. |
</threat_model>

<verification>
- `test -f release-please-config.json && test -f .release-please-manifest.json`
- `test -f .github/workflows/release-please.yml && test -f .github/workflows/publish-hex.yml`
- `rg "\"linked-versions\"" release-please-config.json`
- `rg "pull_request" .github/workflows/publish-hex.yml`
- `.github/scripts/check_hex_tarballs.sh`
</verification>

<success_criteria>
- Coordinated release automation for root and admin packages is in place.
- Publish path is protected, environment-gated, and not reachable from PR workflows.
- Tarball content/size budgets are automatically enforced before publish.
- Required release artifacts are present and included in package metadata.
</success_criteria>
