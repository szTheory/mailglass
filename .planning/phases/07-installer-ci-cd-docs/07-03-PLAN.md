---
phase: 07-installer-ci-cd-docs
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - mix.exs
  - README.md
  - guides/getting-started.md
  - guides/authoring-mailables.md
  - guides/components.md
  - guides/preview.md
  - guides/webhooks.md
  - guides/multi-tenancy.md
  - guides/telemetry.md
  - guides/testing.md
  - guides/migration-from-swoosh.md
  - test/mailglass/docs_contract_test.exs
  - test/mailglass/docs_migration_smoke_test.exs
  - test/support/docs_helpers.ex
  - MAINTAINING.md
  - CONTRIBUTING.md
  - SECURITY.md
  - CODE_OF_CONDUCT.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - BRAND-02
  - BRAND-03
must_haves:
  truths:
    - "ExDoc is configured as canonical docs spine with `main: \"getting-started\"`, `homepage_url`, complete extras, and stable source links."
    - "All nine required guides exist and each ends with a runnable end-to-end section."
    - "Docs contracts compile snippets and assert referenced mix tasks/routes/config APIs exist."
    - "Raw Swoosh -> mailglass migration parity is covered by a dedicated smoke integration test included in Phase 07 verification aliases."
    - "Maintainer docs (`MAINTAINING.md`, `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`) ship at repo root."
    - "Docs and installer-facing copy follow direct wording and specific error/log style."
  artifacts:
    - path: "mix.exs"
      provides: "DOCS-01 ExDoc config with extras/groups and warnings-as-errors compatibility"
      contains: "main: \"getting-started\""
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "DOCS-04 executable docs contract checks"
      contains: "defmodule Mailglass.DocsContractTest"
    - path: "test/mailglass/docs_migration_smoke_test.exs"
      provides: "DOCS-03 migration parity smoke test from raw Swoosh host to mailglass"
      contains: "defmodule Mailglass.DocsMigrationSmokeTest"
    - path: "guides/migration-from-swoosh.md"
      provides: "DOCS-03 migration guide with testable parity flow"
      contains: "Migration from raw Swoosh"
    - path: "SECURITY.md"
      provides: "DOCS-05 disclosure policy for signature bypass and tenant isolation issues"
      contains: "webhook signature bypass"
---

<objective>
Ship trustworthy docs as executable contracts. This plan turns README/guides/ExDoc into an enforced interface that stays synchronized with real tasks, config, and routes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Expand ExDoc configuration and guide information architecture</name>
  <files>
    - mix.exs
    - README.md
    - guides/getting-started.md
    - guides/authoring-mailables.md
    - guides/components.md
    - guides/preview.md
    - guides/webhooks.md
    - guides/multi-tenancy.md
    - guides/telemetry.md
    - guides/testing.md
    - guides/migration-from-swoosh.md
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/.planning/ROADMAP.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-CONTEXT.md
    - /Users/jon/projects/mailglass/guides/webhooks.md
    - /Users/jon/projects/mailglass/README.md
    - /Users/jon/projects/mailglass/mix.exs
  </read_first>
  <action>
    Update `mix.exs` docs configuration with concrete fields:
    - `main: "getting-started"`
    - `homepage_url`
    - `source_url`
    - `source_ref` from project version/tag
    - extras list with all nine guide paths
    - grouped extras/module groups for navigation.

    Ensure README Quick Start points to the docs spine and uses real task names (`mix mailglass.install`, `mix verify.phase_07`).

    Add docs-focused verification aliases in `mix.exs`:
    - `verify.docs.contract`
    - `verify.docs.migration`
    - ensure `verify.phase_07` composition includes the migration parity check alias.

    Create/update the nine guide files so each includes:
    - setup prerequisites
    - runnable command/code examples
    - explicit final section heading: `## End-to-End Example`
  </action>
  <acceptance_criteria>
    - `rg "main: \"getting-started\"" mix.exs` returns one match.
    - `rg "homepage_url" mix.exs` returns one match.
    - `rg "guides/(getting-started|authoring-mailables|components|preview|webhooks|multi-tenancy|telemetry|testing|migration-from-swoosh)\\.md" mix.exs` finds all 9 paths in docs extras.
    - `for f in guides/getting-started.md guides/authoring-mailables.md guides/components.md guides/preview.md guides/webhooks.md guides/multi-tenancy.md guides/telemetry.md guides/testing.md guides/migration-from-swoosh.md; do test -f "$f" || exit 1; done` exits 0.
    - `rg "^## End-to-End Example$" guides/*.md` returns at least 9 matches.
    - `rg "verify\\.docs\\.contract|verify\\.docs\\.migration|verify\\.phase_07" mix.exs` returns all three aliases.
    - `mix docs --warnings-as-errors` exits 0.
    - `test -f doc/llms.txt || test -f doc/dist/llms.txt` exits 0 after docs generation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement docs-contract test suite for snippets/tasks/routes/config</name>
  <files>
    - test/support/docs_helpers.ex
    - test/mailglass/docs_contract_test.exs
    - test/mailglass/docs_migration_smoke_test.exs
    - mix.exs
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-RESEARCH.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-PATTERNS.md
    - /Users/jon/projects/mailglass/README.md
    - /Users/jon/projects/mailglass/mix.exs
  </read_first>
  <action>
    Add `Mailglass.DocsContractTest` with concrete checks:
    - extract fenced quick-start snippets from `README.md` and selected guides, parse with `Code.string_to_quoted/1`
    - assert referenced tasks exist via `Mix.Task.get/1` for:
      - `mailglass.install`
      - `mailglass.reconcile`
      - `mailglass.webhooks.prune`
      - `verify.phase_07`
    - validate config examples through `Mailglass.Config.new!/1`
    - validate docs route examples include `mailglass_admin_routes` and `mailglass_webhook_routes` names that exist in codebase.

    Add helper module `test/support/docs_helpers.ex` for fenced-block extraction by heading label to keep test module concise.

    Add dedicated migration parity smoke test:
    - `test/mailglass/docs_migration_smoke_test.exs` defines `Mailglass.DocsMigrationSmokeTest`
    - starts from a raw Swoosh-style host fixture baseline
    - executes documented migration steps from `guides/migration-from-swoosh.md`
    - asserts parity smoke contract: migrated host compiles, required mailglass tasks/routes resolve, and no migration-step command fails.
  </action>
  <acceptance_criteria>
    - `rg "defmodule Mailglass\\.DocsContractTest" test/mailglass/docs_contract_test.exs` returns one match.
    - `rg "defmodule Mailglass\\.DocsMigrationSmokeTest" test/mailglass/docs_migration_smoke_test.exs` returns one match.
    - `rg "Code\\.string_to_quoted|Mix\\.Task\\.get|Mailglass\\.Config\\.new!" test/mailglass/docs_contract_test.exs` returns matches for all three contract types.
    - `rg "mailglass\\.install|mailglass\\.reconcile|mailglass\\.webhooks\\.prune|verify\\.phase_07" test/mailglass/docs_contract_test.exs` finds all required task names.
    - `rg "migration-from-swoosh\\.md|raw Swoosh|parity" test/mailglass/docs_migration_smoke_test.exs` returns one or more matches.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
    - `mix test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors` exits 0.
    - `mix verify.docs.migration` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add maintainer/security policy docs and enforce brand wording</name>
  <files>
    - MAINTAINING.md
    - CONTRIBUTING.md
    - SECURITY.md
    - CODE_OF_CONDUCT.md
    - README.md
    - guides/getting-started.md
    - guides/authoring-mailables.md
    - guides/components.md
    - guides/preview.md
    - guides/webhooks.md
    - guides/multi-tenancy.md
    - guides/telemetry.md
    - guides/testing.md
    - guides/migration-from-swoosh.md
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/CLAUDE.md
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-CONTEXT.md
  </read_first>
  <action>
    Add root maintainer docs with concrete content:
    - `MAINTAINING.md`: release flow, required checks, snapshot update protocol
    - `CONTRIBUTING.md`: local setup, verify commands, commit/PR expectations
    - `SECURITY.md`: disclosure channel, SLA windows, severity rubric; explicitly mention webhook signature bypass and tenant-isolation leaks as critical classes
    - `CODE_OF_CONDUCT.md`: contributor behavior policy and reporting path

    Run a direct-wording pass across README/guides:
    - prefer `preview` terminology
    - avoid inflated marketing prose
    - keep error/log examples specific and operational.
  </action>
  <acceptance_criteria>
    - `test -f MAINTAINING.md && test -f CONTRIBUTING.md && test -f SECURITY.md && test -f CODE_OF_CONDUCT.md` exits 0.
    - `rg "webhook signature bypass|tenant isolation" SECURITY.md` returns at least 2 matches.
    - `rg "Oops|experience the full rendering lifecycle" README.md guides/*.md MAINTAINING.md CONTRIBUTING.md SECURITY.md CODE_OF_CONDUCT.md` returns no matches.
    - `rg "\\bpreview\\b" README.md guides/*.md | wc -l` returns at least 9.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
security_enforcement: true
security_asvs_level: 1
security_block_on: high

Any HIGH threat below is release-blocking for this plan until mitigated and acceptance checks pass.

| Threat ID | Severity | Category | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-07-03-A | high | Tampering | Docs examples vs runtime behavior | block | Docs-contract tests must compile snippets and validate real task/config/route references in CI. |
| T-07-03-B | high | Information Disclosure | Security reporting gaps | block | `SECURITY.md` must document disclosure path and explicitly cover signature bypass + tenant isolation classes. |
| T-07-03-C | medium | Repudiation | Ambiguous docs wording | mitigate | Brand voice checks remove vague/error-prone wording and require direct terminology. |
</threat_model>

<verification>
- `mix docs --warnings-as-errors`
- `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors`
- `mix test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors`
- `mix verify.docs.migration`
- `for f in MAINTAINING.md CONTRIBUTING.md SECURITY.md CODE_OF_CONDUCT.md; do test -f "$f" || exit 1; done`
- `rg "main: \"getting-started\"" mix.exs`
- `rg "^## End-to-End Example$" guides/*.md`
</verification>

<success_criteria>
- ExDoc and guide IA are complete and compile without warnings.
- Docs contracts execute and fail fast on API/task/config drift.
- Migration guide is present and backed by executable raw Swoosh -> mailglass parity smoke coverage in phase verification aliases.
- Root maintainer/security docs are shipped and brand-voice consistent.
</success_criteria>
