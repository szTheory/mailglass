---
phase: 07-installer-ci-cd-docs
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - test/example/README.md
  - test/example/.gitignore
  - test/support/installer_fixture_helpers.ex
  - test/mailglass/install/install_golden_test.exs
  - test/mailglass/install/install_idempotency_test.exs
  - test/mailglass/install/install_first_preview_smoke_test.exs
  - mix.exs
autonomous: true
requirements:
  - INST-03
  - INST-04
must_haves:
  truths:
    - "Installer behavior is protected by golden snapshot tests over a fixture host tree under `test/example/`."
    - "Second-run idempotency is enforced by automated assertions that zero target files change."
    - "Conflict sidecar behavior is explicitly tested with user-edited managed regions."
    - "`mix verify.phase_07` and focused installer aliases exist and are wired into executable checks."
  artifacts:
    - path: "test/mailglass/install/install_golden_test.exs"
      provides: "INST-03 golden-diff regression guard"
      contains: "defmodule Mailglass.Install.GoldenTest"
    - path: "test/mailglass/install/install_idempotency_test.exs"
      provides: "INST-02/INST-03 rerun no-op and conflict-sidecar assertions"
      contains: "defmodule Mailglass.Install.IdempotencyTest"
    - path: "test/mailglass/install/install_first_preview_smoke_test.exs"
      provides: "time-to-first-preview smoke check for Phase goal"
      contains: "defmodule Mailglass.Install.FirstPreviewSmokeTest"
    - path: "mix.exs"
      provides: "INST-04 verify aliases"
      contains: "verify.phase_07"
---

<objective>
Lock installer behavior with executable fixture tests and phase verify aliases. This plan makes installer drift reviewable in PRs and enforces idempotency as a merge-blocking contract.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add fixture-host test harness for installer golden snapshots</name>
  <files>
    - test/example/README.md
    - test/example/.gitignore
    - test/support/installer_fixture_helpers.ex
    - test/mailglass/install/install_golden_test.exs
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-CONTEXT.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-RESEARCH.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-PATTERNS.md
    - /Users/jon/projects/mailglass/test/support/fixtures.ex
  </read_first>
  <action>
    Build a reusable installer fixture harness:

    - `test/support/installer_fixture_helpers.ex` with functions:
      - `new_fixture_root!(name)` (copies fixture tree to temp dir)
      - `run_install!(fixture_root, argv)`
      - `snapshot_tree!(fixture_root)` (sorted path/content digest)
      - `normalize_snapshot(snapshot)` with narrow tokenization only:
        - `<TMP_PATH>`
        - `<MIGRATION_TS>`
        - `<SECRET>`
    - `test/mailglass/install/install_golden_test.exs`:
      - test fresh install snapshot against committed golden expectation
      - test `--no-admin` snapshot branch separately
      - explicit helper to refresh snapshots through one command path (`MIX_INSTALLER_ACCEPT_GOLDEN=1`)

    Keep snapshots human-reviewable: plaintext tree + per-file blocks, not hash-only blobs.
  </action>
  <acceptance_criteria>
    - `test -f test/support/installer_fixture_helpers.ex` passes.
    - `rg "new_fixture_root!|run_install!|snapshot_tree!|normalize_snapshot" test/support/installer_fixture_helpers.ex` finds all helper functions.
    - `rg "MIX_INSTALLER_ACCEPT_GOLDEN" test/mailglass/install/install_golden_test.exs` returns one match.
    - `rg "<TMP_PATH>|<MIGRATION_TS>|<SECRET>" test/support/installer_fixture_helpers.ex` returns all three normalization tokens.
    - `mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add idempotency/conflict integration tests for reruns</name>
  <files>
    - test/mailglass/install/install_idempotency_test.exs
    - test/support/installer_fixture_helpers.ex
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-VALIDATION.md
    - /Users/jon/projects/mailglass/.planning/phases/07-installer-ci-cd-docs/07-CONTEXT.md
    - /Users/jon/projects/mailglass/lib/mailglass/installer/apply.ex
    - /Users/jon/projects/mailglass/lib/mailglass/installer/conflict.ex
  </read_first>
  <action>
    Create `install_idempotency_test.exs` with concrete scenarios:

    1. first install then immediate second install -> zero file diffs under fixture root
    2. edit managed block in fixture `router.ex` then rerun -> target file untouched and sidecar created
    3. rerun with `--force` after managed drift -> overwrite allowed, sidecar not created

    Assert sidecar file naming prefix `.mailglass_conflict_` and include reason marker in sidecar body.
  </action>
  <acceptance_criteria>
    - `rg "defmodule Mailglass\\.Install\\.IdempotencyTest" test/mailglass/install/install_idempotency_test.exs` returns one match.
    - `rg "\\.mailglass_conflict_" test/mailglass/install/install_idempotency_test.exs` returns at least one match.
    - `rg "--force" test/mailglass/install/install_idempotency_test.exs` returns at least one match.
    - `mix test test/mailglass/install/install_idempotency_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add first-preview smoke test and phase verify aliases</name>
  <files>
    - test/mailglass/install/install_first_preview_smoke_test.exs
    - mix.exs
  </files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/ROADMAP.md
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
    - /Users/jon/projects/mailglass/mix.exs
  </read_first>
  <action>
    Add:

    - `install_first_preview_smoke_test.exs`:
      - executes installer in fixture host
      - applies minimal mailable scaffold script
      - asserts generated preview route and template compile path exist
      - captures elapsed milliseconds and asserts `< 300_000`
    - `mix.exs` aliases:
      - `verify.installer.golden`
      - `verify.installer.idempotency`
      - `verify.installer.smoke`
      - `verify.phase_07` composed from focused aliases (not a kitchen-sink single command literal)
      - treat this task as wave-1 ownership of Phase 07 alias scaffolding in `mix.exs`; downstream docs alias additions happen in Plan 03 after this plan completes
  </action>
  <acceptance_criteria>
    - `rg "defmodule Mailglass\\.Install\\.FirstPreviewSmokeTest" test/mailglass/install/install_first_preview_smoke_test.exs` returns one match.
    - `rg "300_000|< 300_000" test/mailglass/install/install_first_preview_smoke_test.exs` returns one match.
    - `rg "verify\\.installer\\.golden|verify\\.installer\\.idempotency|verify\\.installer\\.smoke|verify\\.phase_07" mix.exs` returns all aliases.
    - `mix test test/mailglass/install/install_first_preview_smoke_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
security_enforcement: true
security_asvs_level: 1
security_block_on: high

Any HIGH threat below is release-blocking for this plan until its mitigation is present and corresponding acceptance checks pass.

| Threat ID | Severity | Category | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-07-02-A | high | Tampering | Installer rerun paths | block | Dedicated idempotency tests must assert second-run no-op and conflict-sidecar behavior; no implicit pass by snapshot update. |
| T-07-02-B | high | Repudiation | Golden snapshot updates | block | Snapshot regeneration must require explicit env var (`MIX_INSTALLER_ACCEPT_GOLDEN=1`) and produce human-readable diff output. |
| T-07-02-C | medium | Denial of Service | First-preview setup UX | mitigate | Smoke test enforces under-5-minute threshold with bounded fixture setup. |
</threat_model>

<verification>
- `mix test test/mailglass/install/install_golden_test.exs --warnings-as-errors`
- `mix test test/mailglass/install/install_idempotency_test.exs --warnings-as-errors`
- `mix test test/mailglass/install/install_first_preview_smoke_test.exs --warnings-as-errors`
- `mix verify.installer.golden`
- `mix verify.installer.idempotency`
- `mix verify.installer.smoke`
</verification>

<success_criteria>
- Installer behavior changes surface as explicit golden diffs in PRs.
- Idempotency and conflict-sidecar contracts are enforced by integration tests.
- Phase-specific verify aliases exist and execute focused checks.
- Fixture smoke test proves the first-preview path stays inside the phase goal budget.
</success_criteria>
