---
phase: 139-admin-asset-first-load-deep-link-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 139-01
files_modified:
  - mailglass_admin/e2e/admin-assets.spec.js
autonomous: true
requirements:
  - AAU-02
  - AAU-03
  - AAU-04
  - GATE-03
must_haves:
  truths:
    - "Direct browser hard loads request stylesheet and font assets from the effective mount root with 200 responses and expected content types."
    - "The same direct-load browser proof passes for alternate preview, operator, and inbound mount roots from plan 139-01."
    - "Computed-style assertions prove CSS/fonts are applied after page.goto loads without screenshots or pixel diffs."
  artifacts:
    - path: "mailglass_admin/e2e/admin-assets.spec.js"
      provides: "Focused Playwright network and computed-style proof"
      exports: ["collectAssetResponses", "assertDirectAssetLoad", "assertTokenBackedStyles"]
    - path: "mailglass_admin/test/support/endpoint_case.ex"
      provides: "Alternate mount routes consumed by browser proof"
      contains: "mailglass_admin_operator_alt"
  key_links:
    - from: "mailglass_admin/e2e/admin-assets.spec.js"
      to: "mailglass_admin/test/support/endpoint_case.ex"
      via: "Browser direct-load route cases include default and alternate macro mounts"
      pattern: "alt/dev/console|secure/console"
    - from: "mailglass_admin/e2e/admin-assets.spec.js"
      to: "mailglass_admin/playwright.config.cjs"
      via: "Existing serialized operator browser server runs the focused spec"
      pattern: "test:operator-browser"
    - from: "mailglass_admin/e2e/admin-assets.spec.js"
      to: "mailglass_admin/assets/css/app.css"
      via: "CSS @font-face relative URLs must resolve against mount-rooted stylesheet URL"
      pattern: "fonts/"
---

<objective>
Create the browser half of GATE-03 for AAU-02, AAU-03, and AAU-04. The spec performs direct `page.goto` hard loads, fails on stylesheet/font request failures or non-200 responses, checks content types and mount-rooted asset paths per D-06, and asserts token-backed computed styling per D-07 without screenshots or pixel diffs per D-08.

Purpose: prove the first HTML hrefs from plan 139-01 work in a real browser, including CSS-relative font resolution and alternate mount roots.
Output: focused Playwright spec included by the existing serialized `test:operator-browser` gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/139-admin-asset-first-load-deep-link-proof/139-CONTEXT.md
@.planning/phases/139-admin-asset-first-load-deep-link-proof/139-RESEARCH.md
@.planning/phases/139-admin-asset-first-load-deep-link-proof/139-PATTERNS.md
@.planning/phases/139-admin-asset-first-load-deep-link-proof/139-UI-SPEC.md
@.planning/phases/139-admin-asset-first-load-deep-link-proof/139-01-SUMMARY.md
@mailglass_admin/e2e/operator.spec.js
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/e2e/gallery-matrix.spec.js
@mailglass_admin/playwright.config.cjs
@mailglass_admin/package.json
@mailglass_admin/test/support/operator_browser_server.ex
@mailglass_admin/test/support/endpoint_case.ex
@mailglass_admin/assets/css/app.css
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Produced in plan |
|------|---------------|------------------|
| Test-only route scope | `/alt/dev/console` in `MailglassAdmin.TestAdopter.Router` | 139-01 |
| Test-only route scope | `/secure/console` in `MailglassAdmin.TestAdopter.Router` | 139-01 |
| Live session name | `:mailglass_admin_preview_alt` | 139-01 |
| Live session name | `:mailglass_admin_operator_alt` | 139-01 |
| Test file | `mailglass_admin/test/mailglass_admin/admin_asset_url_test.exs` | 139-01 |
| Test module | `MailglassAdmin.AdminAssetUrlTest` | 139-01 |
| Test helper | `stylesheet_href!/1` | 139-01 |
| Test helper | `operator_conn/1` | 139-01 |
| Test helper | `assert_stylesheet_href_rooted!/3` | 139-01 |
| Test case | First-HTML stylesheet href route matrix for preview/operator/inbound/gallery/query/alternate roots | 139-01 |
| Browser spec file | `mailglass_admin/e2e/admin-assets.spec.js` | 139-02 |
| Browser helper | `loginOperatorForAssetRoute(page, targetPath)` | 139-02 |
| Browser helper | `collectAssetResponses(page, expectedMountRoot)` | 139-02 |
| Browser helper | `assertDirectAssetLoad(page, routeCase)` | 139-02 |
| Browser helper | `assertTokenBackedStyles(page, routeCase)` | 139-02 |
| Browser test cases | `admin asset hard load: <surface>` route cases | 139-02 |

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Prove admin CSS/font asset URLs are mount-aware on first HTML load and hard refresh across deep links and alternate mount roots | 139-01, 139-02 | COVERED | This plan consumes the first-HTML proof and validates browser network/style behavior. |
| REQ | AAU-01 | First HTML emits mount-rooted stylesheet hrefs | 139-01 | COVERED | Dependency plan covers fast href assertions. |
| REQ | AAU-02 | Hard refreshes and direct deep links load CSS/fonts with 200 responses and expected content types | 139-02 | COVERED | Task 1 captures stylesheet/font responses for direct loads. |
| REQ | AAU-03 | Same proof passes for arbitrary alternate mount path without public router macro options | 139-01, 139-02 | COVERED | This plan exercises alternate routes created by plan 01. |
| REQ | AAU-04 | Browser gate fails on CSS/font 404s and asserts token-backed computed styling | 139-02 | COVERED | Task 1 handles network failures; Task 2 handles computed styles. |
| REQ | GATE-03 | Admin URL robustness has fast LiveView/Conn-level assertions and serialized browser proof | 139-01, 139-02 | COVERED | Existing `test:operator-browser` runs Playwright with `--workers=1`. |
| RESEARCH | - | Use response status/content-type checks because Playwright requestfailed does not catch HTTP 404 responses | 139-02 | COVERED | Task 1 inspects every stylesheet/font response. |
| RESEARCH | - | Use direct page.goto for every route; do not rely on in-app navigation | 139-02 | COVERED | Each route case gets a direct navigation after auth setup when needed. |
| RESEARCH | - | Wait for fonts and assert computed styles so unstyled HTML cannot pass | 139-02 | COVERED | Task 2 adds token-backed computed-style helpers. |
| RESEARCH | - | Existing Playwright dependency is locked; no install/upgrade | 139-02 | COVERED | No package files are modified. |
| RESEARCH | - | Schema push detection | all | COVERED | Phase sources contain no ORM schema paths; no schema push task is injected. |
| CONTEXT | D-01 | Preserve MountPathHook -> MountPath.base/1 -> Layouts.css_url/1 | 139-01, 139-02 | COVERED | Browser proof validates the existing emitted href behavior. |
| CONTEXT | D-02 | Continue macro-scoped asset routes; no duplicate nested routes, CDN, base tag, or redirects | 139-01, 139-02 | COVERED | Browser URL assertions expect macro-scoped paths under each mount root. |
| CONTEXT | D-03 | Expand fast first-HTML assertions across route matrix | 139-01 | COVERED | Dependency plan. |
| CONTEXT | D-04 | Prove alternate mount roots through test-only support | 139-01, 139-02 | COVERED | This plan includes alternate preview/operator/inbound route cases. |
| CONTEXT | D-05 | Assert rooted hrefs and reject bare relative hrefs | 139-01 | COVERED | Dependency plan. |
| CONTEXT | D-06 | Add targeted Playwright asset-loading proof with direct page.goto | 139-02 | COVERED | Task 1. |
| CONTEXT | D-07 | Assert token-backed computed styles after direct loads | 139-02 | COVERED | Task 2. |
| CONTEXT | D-08 | Keep browser proof screenshot-free and pixel-diff-free | 139-02 | COVERED | Task 2 prohibits screenshot and pixel-diff assertions. |
| UI-SPEC | - | Verification-only UI work; no visual redesign | all | COVERED | Browser proof observes existing UI only. |

Scope fences honored: Phase 140 documentation/backlog reconciliation, broader UI verification discipline, CDN/host asset pipelines, duplicate nested asset routes, base tags, redirects, and public router macro options are not planned in Phase 139 because the phase context excludes them.

<tasks>

<task type="auto">
  <name>Task 1: Add direct-load CSS and font network proof</name>
  <files>mailglass_admin/e2e/admin-assets.spec.js</files>
  <read_first>
    @mailglass_admin/e2e/admin-assets.spec.js
    @mailglass_admin/e2e/operator.spec.js
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/e2e/gallery-matrix.spec.js
    @mailglass_admin/playwright.config.cjs
    @mailglass_admin/package.json
    @mailglass_admin/test/support/operator_browser_server.ex
    @mailglass_admin/test/support/endpoint_case.ex
    @mailglass_admin/assets/css/app.css
    @.planning/phases/139-admin-asset-first-load-deep-link-proof/139-01-SUMMARY.md
    @.planning/phases/139-admin-asset-first-load-deep-link-proof/139-CONTEXT.md
    @.planning/phases/139-admin-asset-first-load-deep-link-proof/139-PATTERNS.md
  </read_first>
  <action>Create `mailglass_admin/e2e/admin-assets.spec.js` with `@playwright/test`. Define explicit route cases named with the prefix `admin asset hard load:` for preview index, preview query, preview scenario, preview error, gallery, operator query, inbound query, alternate preview, alternate operator, and alternate inbound. For operator/inbound cases, authenticate with the existing `/ops/browser-reset` and `/ops/browser-login` support routes first, then attach asset tracking and perform a direct `page.goto(targetPath)` to the target route so the proof is a hard load. Implement `collectAssetResponses(page, expectedMountRoot)` to subscribe before navigation to both `requestfailed` and `response`; filter `request.resourceType()` to `stylesheet` and `font`; fail on any filtered failed request; assert every filtered response has status 200; assert stylesheet content types include `text/css`; assert font content types include `font/woff2`; assert every stylesheet URL path is under `<expectedMountRoot>/css-`; assert every font URL path is under `<expectedMountRoot>/fonts/`; assert at least one stylesheet and at least one font response are observed for each route case. Keep each route case as an independent Playwright test so cached assets from one surface cannot mask another surface. Do not add screenshots, pixel diffs, package installs, package upgrades, router options, duplicate asset routes, redirects, or client-side asset rewriting per D-06 and D-08.</action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "admin asset hard load"</automated>
    <automated>rg -n "admin asset hard load|requestfailed|resourceType\\(\\)|content-type|font/woff2|text/css|/alt/dev/console|/secure/console" mailglass_admin/e2e/admin-assets.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - The spec has independent direct `page.goto` proof for default preview/operator/inbound/gallery routes and alternate preview/operator/inbound routes.
    - The spec listens for both `requestfailed` and `response` before each target navigation.
    - CSS and font responses fail the test on non-200 status, wrong content type, wrong origin, or a pathname outside the expected mount root.
    - Each route case observes at least one stylesheet and at least one self-hosted font response.
    - The focused Playwright grep exits 0 through the existing serialized `test:operator-browser` script.
  </acceptance_criteria>
  <done>AAU-02 and the network half of AAU-04 are proven in a real browser for default and alternate mounts.</done>
</task>

<task type="auto">
  <name>Task 2: Add token-backed computed-style assertions</name>
  <files>mailglass_admin/e2e/admin-assets.spec.js</files>
  <read_first>
    @mailglass_admin/e2e/admin-assets.spec.js
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/e2e/operator.spec.js
    @mailglass_admin/e2e/gallery-matrix.spec.js
    @mailglass_admin/assets/css/app.css
    @.planning/phases/139-admin-asset-first-load-deep-link-proof/139-UI-SPEC.md
    @.planning/phases/139-admin-asset-first-load-deep-link-proof/139-RESEARCH.md
  </read_first>
  <action>In the same focused spec, add `assertTokenBackedStyles(page, routeCase)` and call it after the network assertions for every route case. Wait for `document.fonts.ready`, assert `document.fonts.check()` succeeds for the Inter body font, assert the computed `body` font family contains `Inter`, assert a visible route-specific anchor exists (`preview-shell`, gallery heading, operator heading, inbound heading, or another stable visible root from the existing specs), assert the first visible heading uses the existing bold heading weight, and assert a semantic background or surface color resolves to one of the existing token-backed RGB values from the UI-SPEC rather than browser defaults. For dark-theme query cases, also assert the root `data-theme` is `mailglass-dark` before checking dark token colors. Keep the proof screenshot-free and pixel-diff-free per D-07 and D-08; do not modify CSS, HEEx, tokens, component markup, or text unless the network proof uncovers a real asset application bug that cannot be honestly proven otherwise.</action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "admin asset hard load"</automated>
    <automated>rg -n "assertTokenBackedStyles|document\\.fonts\\.ready|document\\.fonts\\.check|getComputedStyle|fontFamily|backgroundColor|fontWeight" mailglass_admin/e2e/admin-assets.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Every `admin asset hard load:` route case calls the computed-style assertion after direct navigation and network validation.
    - The computed-style assertion waits for font readiness and proves Inter-family CSS/font application.
    - The assertion checks at least one semantic token-backed color or surface value and one heading/body typography value.
    - The spec contains no screenshot or pixel-diff assertion.
    - The focused Playwright command exits 0 and is included by the existing full `npm run test:operator-browser` script.
  </acceptance_criteria>
  <done>AAU-04 and the serialized browser half of GATE-03 are complete without redesigning the admin UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> admin routes | Direct deep-link navigations resolve CSS and font URLs through browser URL rules. |
| Browser session setup -> operator routes | The proof authenticates through existing test support before direct operator/inbound hard loads. |
| Playwright network events -> verification result | HTTP 404 responses complete as responses, so the test must inspect status and content type. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-06 | Repudiation | `admin-assets.spec.js` network proof | mitigate | Collect request failures and all stylesheet/font responses; assert status, content type, and URL path with route labels. |
| T-139-07 | Denial of Service | CSS/font asset loading | mitigate | Tests fail when CSS or font assets 404, return wrong content type, or resolve under nested LiveView paths. |
| T-139-08 | Information Disclosure | Asset URL origin/path | mitigate | Assert asset URLs stay same-origin and under the expected macro-scoped mount root. |
| T-139-09 | Elevation of Privilege | Operator/inbound browser setup | mitigate | Use existing `/ops/browser-login` session setup, then direct-load protected targets; do not add auth-bypass browser routes. |
| T-139-10 | Tampering | Computed style proof | mitigate | Assert token-backed computed values after `page.goto` so unstyled HTML cannot satisfy the browser gate. |
| T-139-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install or upgrade task is planned; use existing locked Playwright dev dependency only. |
</threat_model>

<verification>
Run `cd mailglass_admin && npm run test:operator-browser -- --grep "admin asset hard load"`. The full serialized browser gate `cd mailglass_admin && npm run test:operator-browser` must include the new spec because it lives under `mailglass_admin/e2e`.
</verification>

<success_criteria>
Direct hard loads for required default and alternate admin routes have no CSS/font request failures, every CSS/font response is 200 with the expected content type and mount-rooted URL, and token-backed computed styles prove the admin is styled after direct navigation.
</success_criteria>

<output>
Create `.planning/phases/139-admin-asset-first-load-deep-link-proof/139-02-SUMMARY.md` when done.
</output>
