---
phase: 48-inbound-admin-liveview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/mix.exs
  - mailglass_admin/config/test.exs
  - mailglass_admin/test/test_helper.exs
  - mailglass_admin/test/support/endpoint_case.ex
  - mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex
  - mailglass_admin/lib/mailglass_admin/pub_sub/topics.ex
  - mailglass_admin/lib/mailglass_admin/pub_sub/topics_test.exs
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_inbound/lib/mailglass_inbound/router/matcher.ex
  - mailglass_inbound/test/mailglass_inbound/router/matcher_test.exs
  - mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex
  - mailglass_inbound/lib/mailglass_inbound/internal/operator/timeline.ex
  - mailglass_inbound/lib/mailglass_inbound/internal/operator/detail.ex
  - mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs
autonomous: true
requirements: [IADM-01, IADM-04, IADM-05]
user_setup: []

must_haves:
  truths:
    - "mailglass_admin compiles with mailglass_inbound present (path dep, dev/test) AND with --no-optional-deps (inbound stripped, gateway elided)"
    - "InboundLive tests can insert InboundRecord + InboundEvidence + ExecutionRun fixtures against the admin test DB"
    - "Router.Matcher.explain/2 returns a per-clause verdict list whose overall verdict equals matches_route?/2 for every route×message"
    - "The inbound read-model returns [] for a blank tenant and never returns another tenant's rows"
    - "MailglassAdmin.PubSub.Topics.inbound_record_inserted/1 produces the identical string as MailglassInbound.PubSub.Topics.inbound_record_inserted/1"
    - "mask_recipient/1 is a single public definition in MailglassAdmin.Components, called by both deliveries_list and the new inbound components"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex"
      provides: "Runtime-gated gateway for all inbound access from admin"
      contains: "Code.ensure_loaded?(MailglassInbound)"
    - path: "mailglass_inbound/lib/mailglass_inbound/router/matcher.ex"
      provides: "explain/2 reflection reusing the existing defp predicates"
      contains: "def explain"
    - path: "mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex"
      provides: "Tenant-required-or-empty inbound list read-model"
      contains: "Tenancy.scope"
    - path: "mailglass_admin/lib/mailglass_admin/pub_sub/topics.ex"
      provides: "Consumer-side inbound_record_inserted/1 builder"
      contains: "inbound_record_inserted"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex"
      to: "MailglassInbound.Internal.Operator.Records"
      via: "apply/3 wrapper inside the gateway"
      pattern: "apply\\("
    - from: "mailglass_inbound/lib/mailglass_inbound/router/matcher.ex"
      to: "matches_matcher?/2"
      via: "explain/2 reuses the private predicates in-module"
      pattern: "matches_matcher\\?"
---

<objective>
Wave 0: lay every cross-package seam and test-infra bolt that the InboundLive surface depends on, so Waves 1-2 are pure UI/wiring work against contracts that already exist and are unit-tested.

This plan ships three things, none of which touch `InboundLive`:
1. **Dependency + test-infra wiring** (the mandatory Wave 0 from RESEARCH Pitfall 3 / VALIDATION) — the floating optional inbound dep, the inbound-repo test config, inbound migrations in the admin test bootstrap, and the synthetic-router `:inbound_router` opt. Without this, no InboundLive test can insert a fixture.
2. **Package-local seams in `mailglass_inbound`** — `Router.Matcher.explain/2` reflection (IADM-04) and the `@moduledoc false` `Internal.Operator.*` read-model (records / timeline / detail), both tenant-required-or-empty.
3. **Admin-side seams** — the `OptionalDeps.MailglassInbound` runtime gateway, the consumer-side `inbound_record_inserted/1` topic builder (IADM-05), and the promotion of `mask_recipient/1` to a public `Components` helper (one audited PII-masking definition).

Purpose: every downstream plan calls these contracts; building them first (with their own unit/property tests) removes the "scavenger hunt" and lets Wave 1 clone the OperatorLive shell against ready seams.
Output: a compiling admin (both with and without inbound), a green inbound reflection property test (V3), a green read-model tenancy test (V1 unit half), and a topic-parity test (V8).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-inbound-admin-liveview/48-CONTEXT.md
@.planning/phases/48-inbound-admin-liveview/48-RESEARCH.md
@.planning/phases/48-inbound-admin-liveview/48-PATTERNS.md
@.planning/phases/48-inbound-admin-liveview/48-VALIDATION.md

<interfaces>
<!-- Contracts the executor builds against. Extracted from codebase + RESEARCH. -->

Matcher predicates to REUSE (mailglass_inbound/lib/mailglass_inbound/router/matcher.ex):
- `def matches_route?(%Route{}, %InboundMessage{})` — public, l.31 — AND across recipient/subject/headers
- `defp matches_headers?(headers, normalized_headers)` — l.37 — Enum.all? over header clauses, Map.get(name, []) + Enum.any?
- `defp matches_matcher?(nil, _value)` → true (wildcard) — l.45
- `defp matches_matcher?(_matcher, nil)` → false (nil actual fails) — l.46
- `defp matches_matcher?(%Regex{} = m, v) when is_binary(v)` → Regex.match?(m, v) — l.47
- `defp matches_matcher?(m, v) when is_binary(m) and is_binary(v)` → m == v — l.48

Route struct (router/route.ex:14): `defstruct [:mailbox, :recipient, :subject, headers: []]` — 4 fields; matcher ∈ nil | String.t() | Regex.t()
InboundMessage.headers (inbound_message.ex:52): `%{name => [String.t()]}` — header "actual" is a LIST; missing header → []
Routes reflection entry point (router.ex:64-72): `__mailglass_inbound_routes__/0`

Read-model analog (lib/mailglass/operator/deliveries.ex:18-50): fetch_tenant_id! raises on blank (l.55-58); `where r.tenant_id == ^tenant_id` + maybe_filter_* + order_by desc + limit + select map; then `|> Tenancy.scope(tenant_id) |> Repo.all()` (l.48-49)
Tenancy (lib/mailglass/tenancy.ex:269-273): `Mailglass.Tenancy.scope(query, tenant_id)` — inbound has ZERO usage today; this plan introduces the first
Host-repo facade (mailglass_inbound/lib/mailglass_inbound/repo.ex): `all/2`, `one/3`, `get/3` — raises if `config :mailglass_inbound, :repo` unset

ExecutionRun reuse (mailglass_inbound/lib/mailglass_inbound/internal/replay.ex):
- `load_record/2` l.30-38 (by id only, NO tenant scope), `load_evidence/2` l.40-48
- `resolve_mailbox/2` / `latest_matched_fresh_run/2` l.50-82 (matched-mailbox resolution)
- `latest_fresh_run` query l.84-93: `from(run in ExecutionRun, where: run.inbound_record_id == ^id, order_by: [desc: run.inserted_at])`
ExecutionRun outcomes (execution_run.ex:76-77): `__outcomes__/0` → `[:no_match, :accept, :ignore, :reject, :bounce, :failed]`
Both ExecutionRun AND ReplayRun map to table "mailglass_inbound_replay_runs" — timeline reads ExecutionRun (has :no_match + source), NOT ReplayRun (Pitfall 7)

Gateway pattern (mailglass_admin/lib/mailglass_admin/optional_deps/phoenix_live_reload.ex): `if Code.ensure_loaded?(Dep) do defmodule ... @compile {:no_warn_undefined, [...]}; def available?, do: true end end`; callers guard via `Code.ensure_loaded?(__MODULE__)`. Core Oban gate precedent: lib/mailglass.ex:55-60
Topic to mirror (mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex:33-34): `def inbound_record_inserted(tenant_id) when is_binary(tenant_id), do: "mailglass:inbound:" <> tenant_id`
mask_recipient trio to promote (mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex:98-121): `mask_recipient/1` (l.98-100) + `mask_email/2` (l.107-112) + `mask_value/1` (l.114-121)
mix.exs FLOATING dep analog (mailglass_admin/mix.exs `mailglass_dep/0`): MIX_PUBLISH branch returns `{:mailglass, "== 1.0.0"}`, else path dep — mirror STRUCTURE but FLOATING (`"~> 0.2"`, never `==`)
elixirc_options no_warn_undefined (mailglass_admin/mix.exs ~l.69-71): existing `[no_warn_undefined: [Phoenix.LiveReloader]]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-package dependency + admin test-infra wiring</name>
  <files>mailglass_admin/mix.exs, mailglass_admin/config/test.exs, mailglass_admin/test/test_helper.exs, mailglass_admin/test/support/endpoint_case.ex</files>
  <read_first>
    - mailglass_admin/mix.exs (the file being modified) — `deps/0`, `mailglass_dep/0` MIX_PUBLISH branch (~l.126-132), `elixirc_options` (~l.69-71)
    - mailglass_admin/test/test_helper.exs (the file being modified) — the existing core-repo `with_repo`/pool-override migration block
    - mailglass_admin/config/test.exs (the file being modified) — existing core repo config
    - mailglass_admin/test/support/endpoint_case.ex (the file being modified) — the synthetic adopter router's `mailglass_operator_routes` invocation
    - ANALOG: mailglass_inbound/config/test.exs + mailglass_inbound/test/test_helper.exs — how inbound configures its own repo + runs its own migrations (the pool-override dance to mirror)
    - SOURCE OF TRUTH: 48-RESEARCH.md Pitfall 3 (steps a-d) + 48-VALIDATION.md "Wave 0 Requirements"; 48-CONTEXT.md D-48-01 (FLOATING, never ==; out of PINS) + D-48-07 (:inbound_router opt)
  </read_first>
  <behavior>
    - Test: `cd mailglass_admin && mix compile --warnings-as-errors` succeeds with inbound present (path dep loads MailglassInbound)
    - Test: `cd mailglass_admin && mix compile --no-optional-deps --warnings-as-errors` succeeds with inbound stripped (no bare reference, dep is optional)
    - Test: an InboundLive-suite fixture insert of `MailglassInbound.InboundRecords.InboundRecord` against `MailglassAdmin.TestRepo` succeeds (relation exists; `config :mailglass_inbound, :repo` resolves) — proven by Task 4's records_test inserting a row
  </behavior>
  <action>
    In `mailglass_admin/mix.exs`: add a plain floating entry to `deps/0` resolved through a NEW private `mailglass_inbound_dep/0` helper that mirrors `mailglass_dep/0`'s MIX_PUBLISH branch STRUCTURE but FLOATING — `{:mailglass_inbound, "~> 0.2", optional: true}` when `MIX_PUBLISH=="true"`, else `{:mailglass_inbound, path: "../mailglass_inbound", optional: true}`. NEVER use `==` (D-48-01, Pitfall 4: a `==` pin writes an unsatisfiable cross-line version into a 0.2.x package). Add as a SEPARATE deps line; leave `mailglass_dep/0` and its `== X.Y.Z` shape untouched (it is the release-please sed anchor with a REL-05 exit-1 guard). Do NOT add the inbound dep to the release-please `PINS` array in `.github/workflows/release-please.yml`. Leave `elixirc_options` as-is (the gateway handles no_warn_undefined; the optional dep makes the bare references in the gateway compile-clean either way).
    In `mailglass_admin/config/test.exs`: add `config :mailglass_inbound, :repo, MailglassAdmin.TestRepo` so the inbound host-repo facade points at the admin test repo.
    In `mailglass_admin/test/test_helper.exs`: ALSO run inbound migrations against the same DB before the sandbox mode is set — `Path.join(:code.priv_dir(:mailglass_inbound), "repo/migrations")` — mirroring the existing core-repo `with_repo` pool-override dance (start with `DBConnection.ConnectionPool` during migration, restore `Sandbox` after). Both core and inbound tables must exist in `MailglassAdmin.TestRepo`'s DB.
    In `mailglass_admin/test/support/endpoint_case.ex`: add the `inbound_router:` opt to the synthetic adopter's `mailglass_operator_routes` invocation, pointing at a small test router module that declares at least one inbound route (so the routing-trace card in Wave 2 has routes to render). If no such test router exists, define a minimal one with a recipient/subject/header route under `test/support`.
  </action>
  <acceptance_criteria>
    - `cd mailglass_admin && mix compile --warnings-as-errors` exits 0 (inbound present via path dep)
    - `cd mailglass_admin && mix compile --no-optional-deps --warnings-as-errors` exits 0 (V4 precondition; inbound stripped)
    - `mailglass_admin/config/test.exs` contains `config :mailglass_inbound, :repo, MailglassAdmin.TestRepo` (grep: `grep -c 'config :mailglass_inbound, :repo' mailglass_admin/config/test.exs` ≥ 1)
    - `mailglass_admin/test/test_helper.exs` references `:code.priv_dir(:mailglass_inbound)` for migrations (grep with `grep -v '^#'` ≥ 1)
    - `mix.exs` inbound dep uses `optional: true` and NEVER `{:mailglass_inbound, "== ` (grep: `grep -c '{:mailglass_inbound, "==' mailglass_admin/mix.exs` == 0)
    - `.github/workflows/release-please.yml` PINS array does NOT contain `mailglass_inbound` (grep: `grep 'PINS' -A4 .github/workflows/release-please.yml | grep -c mailglass_inbound` == 0)
    - `test/support/endpoint_case.ex` passes `inbound_router:` to `mailglass_operator_routes` (grep `grep -v '^#'` ≥ 1)
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors && mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <done>Admin compiles both with inbound (path dep) and without (--no-optional-deps); inbound test repo configured + migrated; synthetic router threads :inbound_router; inbound dep is floating + optional + out of PINS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router.Matcher.explain/2 reflection + property test (IADM-04 seam)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/router/matcher.ex, mailglass_inbound/test/mailglass_inbound/router/matcher_test.exs</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/router/matcher.ex (the file being modified) — `matches_route?/2` (l.31), `matches_headers?/2` (l.37), the four `matches_matcher?/2` clauses (l.45-48)
    - mailglass_inbound/lib/mailglass_inbound/router/route.ex — the 4-field Route struct (l.14) + matcher type
    - mailglass_inbound/lib/mailglass_inbound/inbound_message.ex — `headers :: %{name => [String.t()]}` (l.52), `envelope_recipient`, `subject`
    - mailglass_inbound/test/mailglass_inbound/router/matcher_test.exs (the file being modified, if exists) — existing matcher test patterns; StreamData is already a test dep
    - SOURCE OF TRUTH: 48-RESEARCH.md "Routing-Trace Reflection" (the explain/2 shape + the V3 equivalence property); 48-CONTEXT.md D-48-06 (reuse predicates, single source of truth); 48-PATTERNS.md matcher.ex section
  </read_first>
  <behavior>
    - Test (V3 property): for ALL route×message combos over the 3 matcher kinds (nil/exact/regex) × {present, absent, nil} actuals × header AND-semantics, `Enum.all?(explain(route, msg), fn t -> elem(t, tuple_size(t) - 1) end) == matches_route?(route, msg)`
    - Test (example): a regex-recipient route, an exact-recipient route, a wildcard (nil) route, and a missing-header route each produce the correct per-clause pass?/fail verdicts
    - Test: header "actual" is the LIST `Map.get(message.headers, name, [])`; a missing header yields `[]` and fails unless the matcher is nil
  </behavior>
  <action>
    Add a `@doc false` `explain/2` to `MailglassInbound.Router.Matcher` (co-located so it calls the existing `defp matches_matcher?/2` and the same header logic as `matches_headers?/2` — single source of truth, D-48-06). Return a per-clause verdict list: `{:recipient, route.recipient, message.envelope_recipient, matches_matcher?(route.recipient, message.envelope_recipient)}`, `{:subject, route.subject, message.subject, matches_matcher?(route.subject, message.subject)}`, then for each `{name, matcher}` in `route.headers` a `{:header, name, matcher, actual, Enum.any?(actual, &matches_matcher?(matcher, &1))}` where `actual = Map.get(message.headers, name, [])`. Do NOT copy the equality/regex logic — call `matches_matcher?/2`. The last element of every tuple is the clause pass? boolean. Spec the `@type` (clause_verdict). The exact tuple shape is Claude's discretion per D-48-06 provided the last element is the pass? boolean and the reuse rule holds.
    Add the V3 property test plus example tests to `matcher_test.exs`: generate routes (4 fields, 3 matcher kinds) and messages (present/absent/nil actuals, header lists), assert `Enum.all?(explain(...), &last) == matches_route?(...)`.
  </action>
  <acceptance_criteria>
    - `MailglassInbound.Router.Matcher.explain/2` exists and is `@doc false` (grep: `grep -c 'def explain' mailglass_inbound/lib/mailglass_inbound/router/matcher.ex` ≥ 1)
    - explain/2 reuses `matches_matcher?/2` (grep `grep -v '^#' .../matcher.ex | grep -c 'matches_matcher?'` shows calls inside explain, not a re-implementation of `Regex.match?`/`==` inside explain — no second `Regex.match?` outside the existing l.47 clause)
    - V3 property holds: `cd mailglass_inbound && mix test test/mailglass_inbound/router/matcher_test.exs --seed 0` green, including the `Enum.all?(explain) == matches_route?` equivalence assertion
    - The header verdict's `actual` is a list (`Map.get(headers, name, [])`); a missing-header example test asserts `[]` actual + fail verdict
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/router/matcher_test.exs --seed 0</automated>
  </verify>
  <done>explain/2 ships in Router.Matcher reusing the in-module predicates; the V3 equivalence property + example tests are green; the overall verdict always equals matches_route?/2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inbound read-model (Internal.Operator.{Records,Timeline,Detail}) + tenancy test (IADM-01 seam)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex, mailglass_inbound/lib/mailglass_inbound/internal/operator/timeline.ex, mailglass_inbound/lib/mailglass_inbound/internal/operator/detail.ex, mailglass_inbound/test/mailglass_inbound/internal/operator/records_test.exs</files>
  <read_first>
    - lib/mailglass/operator/deliveries.ex — l.18-50 the tenant-required + Tenancy.scope/2 + projection shape to clone; l.55-58 fetch_tenant_id! raise-on-blank
    - lib/mailglass/operator/timeline.ex — timeline read-model precedent
    - lib/mailglass/tenancy.ex — `scope/2` (l.269-273)
    - mailglass_inbound/lib/mailglass_inbound/internal/replay.ex — l.30-38 load_record, l.40-48 load_evidence, l.50-82 resolve_mailbox/latest_matched_fresh_run, l.84-93 latest_fresh_run ExecutionRun query (the query shapes to mirror, ADD tenant where + Tenancy.scope)
    - mailglass_inbound/lib/mailglass_inbound/inbound_records/{inbound_record,inbound_evidence,execution_run}.ex — schemas; ExecutionRun.__outcomes__/0 (execution_run.ex:76-77); inbound_evidence redact:true fields
    - mailglass_inbound/lib/mailglass_inbound/repo.ex — host-repo facade all/2, one/3, get/3
    - mailglass_inbound/lib/mailglass_inbound/fixtures.ex — code-built fixtures (default tenant_id) for the test
    - SOURCE OF TRUTH: 48-CONTEXT.md D-48-04 (@moduledoc false Internal.Operator.*, tenant-required-or-empty), D-48-05 (replay is NOT tenant-scoped — detail read-model adds scoping so admin can gate); 48-PATTERNS.md internal/operator sections; 48-RESEARCH.md Pitfall 7 (ExecutionRun not ReplayRun)
  </read_first>
  <behavior>
    - Test (V1 unit): `Records.list_records(%{tenant_id: ""}, [])` (or the blank-tenant path) returns `[]`
    - Test (V1 unit): insert an InboundRecord for tenant A and one for tenant B; a tenant-A query returns ONLY tenant-A rows (cross-tenant isolation)
    - Test: outcome filter casts against `ExecutionRun.__outcomes__/0`; an unknown outcome value is rejected/ignored, not passed to SQL
    - Test: Detail returns canonical record + evidence + matched mailbox/outcome from the latest FRESH ExecutionRun; Timeline returns ALL ExecutionRun rows (fresh + replay) chronologically — sourced from ExecutionRun, never ReplayRun
  </behavior>
  <action>
    Create three `@moduledoc false` modules under `mailglass_inbound/lib/mailglass_inbound/internal/operator/` (D-48-04 — data access lives in the owning package, NOT admin):
    - `Records` — mirror `Mailglass.Operator.Deliveries` (deliveries.ex:18-50): `fetch_tenant_id!` raising on blank; `InboundRecord |> where(r.tenant_id == ^tenant_id) |> maybe_filter_provider |> maybe_filter_outcome (join/subquery on ExecutionRun) |> maybe_filter_window |> order_by desc received_at |> limit |> select map |> Tenancy.scope(tenant_id) |> MailglassInbound.Repo.all()`. The `Tenancy.scope/2` line is load-bearing (inbound's first usage). Outcome filter allow-list = `ExecutionRun.__outcomes__/0`.
    - `Timeline` — mirror `Internal.Replay`'s `latest_fresh_run` query (replay.ex:84-93) but for ALL runs of a record: drop `limit: 1`, ascending `executed_at` for chronological display, ADD `tenant_id` where + `Tenancy.scope/2`. Select `source`, `mailbox`, `outcome`, `outcome_reason`, `executed_at`, `id`. Read `ExecutionRun`, NEVER `ReplayRun` (Pitfall 7).
    - `Detail` — reuse the query shapes in `Internal.Replay` (load_record l.30-38, load_evidence l.40-48, resolve_mailbox/latest_matched_fresh_run l.50-82) but ADD the tenant where-clause + `Tenancy.scope/2` (replay loads by id only — that is why admin tenant-gates before replay, D-48-05). Return canonical `InboundRecord` + `InboundEvidence` + matched mailbox/outcome from the latest FRESH ExecutionRun.
    All three return `[]`/`nil` for a blank/missing tenant. Add a tenancy + outcome-cast test in `records_test.exs` using `MailglassInbound.Fixtures` + `InboundRecords.insert_*`.
  </action>
  <acceptance_criteria>
    - All three modules are `@moduledoc false` (grep: `grep -c '@moduledoc false' mailglass_inbound/lib/mailglass_inbound/internal/operator/records.ex` ≥ 1, same for timeline + detail)
    - Each module calls `Tenancy.scope` (grep `grep -v '^#' .../records.ex | grep -c 'Tenancy.scope'` ≥ 1; same for timeline, detail)
    - Queries run through `MailglassInbound.Repo` facade, not a bare `Repo` (grep ≥ 1 `MailglassInbound.Repo` per module)
    - Timeline + Detail read `ExecutionRun`, NOT `ReplayRun` (grep: `grep -c 'ReplayRun' mailglass_inbound/lib/mailglass_inbound/internal/operator/timeline.ex` == 0)
    - V1 unit half green: `cd mailglass_inbound && mix test test/mailglass_inbound/internal/operator/records_test.exs --seed 0` — blank tenant → [], tenant-A query excludes tenant-B rows, outcome cast against `__outcomes__/0`
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/internal/operator/records_test.exs --seed 0</automated>
  </verify>
  <done>Records/Timeline/Detail read-models exist as @moduledoc false, tenant-required-or-empty, Tenancy.scope/2 on every query, through the Repo facade, reading ExecutionRun; cross-tenant isolation + blank-tenant-empty proven by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Admin seams — gateway, topic builder, mask_recipient/1 promotion (IADM-05 + arch)</name>
  <files>mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex, mailglass_admin/lib/mailglass_admin/pub_sub/topics.ex, mailglass_admin/lib/mailglass_admin/pub_sub/topics_test.exs, mailglass_admin/lib/mailglass_admin/components.ex, mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/optional_deps/phoenix_live_reload.ex — the conditional-compile gateway envelope to clone (Code.ensure_loaded? wrapper, @compile no_warn_undefined, available?/0 -> true)
    - lib/mailglass.ex — l.55-60 the Oban `Code.ensure_loaded?` call-site gate precedent
    - mailglass_admin/lib/mailglass_admin/pub_sub/topics.ex (the file being modified) — the existing `admin_reload/0` builder shape (@spec, @doc since)
    - mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex — l.33-34 `inbound_record_inserted/1` → `"mailglass:inbound:" <> tenant_id` (the string to match EXACTLY, V8)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex — l.98-100 mask_recipient/1, l.107-112 mask_email/2, l.114-121 mask_value/1 (the trio to promote)
    - mailglass_admin/lib/mailglass_admin/components.ex (the file being modified) — flash/1, icon/1 (reusable as-is); the promotion target for mask_recipient
    - SOURCE OF TRUTH: 48-CONTEXT.md D-48-02 (Boundary decl UNCHANGED), D-48-03 (available?/0 gates the whole surface), D-48-11 (admin topic builder = identical string), D-48-13 (promote mask_recipient — one definition); 48-PATTERNS.md Optional-Dep Gateway + PubSub + PII Masking shared patterns; credo_checks/prefixed_pub_sub_topics.ex (LINT-06)
  </read_first>
  <behavior>
    - Test (V8): `MailglassAdmin.PubSub.Topics.inbound_record_inserted(t) == MailglassInbound.PubSub.Topics.inbound_record_inserted(t)` for an arbitrary tenant_id
    - Test: `MailglassAdmin.Components.mask_recipient/1` masks an email (one public definition); `deliveries_list.ex` no longer defines its own private `mask_recipient/1`
    - Test: the gateway exposes `available?/0` and `apply/3` wrappers for read-model/reflection/replay; no bare `MailglassInbound.*` reference appears in any admin module outside the gateway (V4 precondition)
  </behavior>
  <action>
    Create `MailglassAdmin.OptionalDeps.MailglassInbound` cloning `phoenix_live_reload.ex`: wrap the whole `defmodule` in `if Code.ensure_loaded?(MailglassInbound) do ... end`, declare `@compile {:no_warn_undefined, [MailglassInbound, MailglassInbound.Internal.Operator.Records, MailglassInbound.Internal.Operator.Timeline, MailglassInbound.Internal.Operator.Detail, MailglassInbound.Router.Matcher, MailglassInbound.Internal.Replay]}`, expose `available?/0 -> true`. House the `apply/3` call wrappers here too (e.g. `list_records/2`, `timeline/2`, `detail/2`, `explain/2`, `replay/2`) so InboundLive never references `MailglassInbound.*` directly. Leave the Boundary decl in `mailglass_admin.ex` UNCHANGED (D-48-02 — listing an absent app emits `unknown_dep` and breaks the no-optional-deps lane).
    In `MailglassAdmin.PubSub.Topics`: add `inbound_record_inserted/1` producing the IDENTICAL string `"mailglass:inbound:" <> tenant_id` (D-48-11; LINT-06 — build through the function, never a literal at a call site), following the existing `admin_reload/0` @spec/@doc-since shape. Add a `topics_test.exs` parity assertion against `MailglassInbound.PubSub.Topics.inbound_record_inserted/1` (V8).
    In `MailglassAdmin.Components`: promote `mask_recipient/1` + its `mask_email/2` + `mask_value/1` helpers from `deliveries_list.ex:98-121` to public `Components` functions (ONE audited definition, D-48-13). Update `deliveries_list.ex` to call `MailglassAdmin.Components.mask_recipient/1` and delete its private copies (no duplication). `Components.flash/1` and `Components.icon/1` stay as-is.
  </action>
  <acceptance_criteria>
    - Gateway wrapped in `Code.ensure_loaded?(MailglassInbound)` (grep: `grep -c 'Code.ensure_loaded?(MailglassInbound)' mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex` ≥ 1) with `@compile {:no_warn_undefined` + `def available?`
    - Boundary decl unchanged: `grep -c 'use Boundary, deps: \[Mailglass\], exports: \[Router\]' mailglass_admin/lib/mailglass_admin.ex` ≥ 1 (no `MailglassInbound` added to deps:)
    - V8 parity green: `cd mailglass_admin && mix test test/mailglass_admin/pub_sub/topics_test.exs` asserts the two builders return the identical string
    - `mask_recipient/1` is public in Components and private definition removed from deliveries_list: `grep -c 'def mask_recipient' mailglass_admin/lib/mailglass_admin/components.ex` ≥ 1 AND `grep -c 'defp mask_recipient' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex` == 0
    - LINT-06 passes: `cd mailglass_admin && mix credo --strict` reports no PrefixedPubSubTopics violation (V9)
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/pub_sub/topics_test.exs && mix credo --strict</automated>
  </verify>
  <done>Gateway exists (conditional-compile + apply/3 wrappers + available?/0); Boundary decl untouched; admin topic builder string-matches inbound (V8 green); mask_recipient/1 is one public Components definition; LINT-06 clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (no inbound) → admin (inbound present) | The optional-dep edge; the no-optional-deps lane must compile with inbound stripped |
| admin → inbound data | All inbound access crosses through the runtime `apply/3` gateway — no compile-time / Boundary edge |
| operator (tenant A) → inbound rows (any tenant) | The read-model is the first tenant-scoping seam for inbound; a blank/foreign tenant must never return rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Information Disclosure | Internal.Operator.Records/Timeline/Detail | mitigate | Every query applies `Mailglass.Tenancy.scope/2` + a `tenant_id` where-clause; blank tenant returns `[]`/`nil` (V1 unit test asserts cross-tenant isolation) |
| T-48-02 | Tampering | mix.exs inbound dep / release-please PINS | mitigate | Floating `optional: true` dep, never `==`; kept OUT of the PINS array so the Phase 50.5 sed-pin cannot write an unsatisfiable cross-line version (D-48-01) |
| T-48-03 | Elevation | Boundary decl in mailglass_admin.ex | mitigate | Leave Boundary `deps:` UNCHANGED; route inbound through runtime `apply/3` so an absent app never emits `unknown_dep` and the no-optional-deps lane stays green (D-48-02) |
| T-48-04 | Information Disclosure | mask_recipient/1 promotion | mitigate | One audited public PII-masking definition in Components; deliveries_list calls it (no second, drifting copy) |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | Zero external package installs this phase; the only dep edge is the first-party sibling `mailglass_inbound` (RESEARCH Package Legitimacy Audit: N/A for first-party) |
</threat_model>

<verification>
- `cd mailglass_admin && mix compile --warnings-as-errors` (inbound present) AND `mix compile --no-optional-deps --warnings-as-errors` (inbound stripped) both green — V4 precondition.
- `cd mailglass_inbound && mix test test/mailglass_inbound/router/matcher_test.exs --seed 0` — V3 reflection property green.
- `cd mailglass_inbound && mix test test/mailglass_inbound/internal/operator/records_test.exs --seed 0` — V1 unit (tenancy isolation) green.
- `cd mailglass_admin && mix test test/mailglass_admin/pub_sub/topics_test.exs` — V8 topic parity green.
- `cd mailglass_admin && mix credo --strict` — LINT-06 (V9) clean.
- Wave merge gate: `cd mailglass_admin && mix verify.preview` AND `cd mailglass_inbound && mix test --seed 0`.
</verification>

<success_criteria>
- Admin compiles with AND without inbound (the optional-dep contract holds).
- InboundLive-suite fixtures can be inserted against the admin test DB (inbound repo configured + migrated).
- `explain/2` exists and its overall verdict equals `matches_route?/2` for all combos (V3).
- The read-model is tenant-required-or-empty with cross-tenant isolation proven (V1 unit).
- Admin and inbound topic builders return the identical string (V8); LINT-06 clean (V9).
- `mask_recipient/1` is one public Components definition.
</success_criteria>

<output>
Create `.planning/phases/48-inbound-admin-liveview/48-01-SUMMARY.md` when done
</output>
