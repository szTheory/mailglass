---
phase: 48-inbound-admin-liveview
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/inbound/timeline.ex
  - mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/destructive_action.ex
  - mailglass_admin/lib/mailglass_admin/router.ex
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
  - mailglass_admin/test/support/inbound_fixtures.ex
autonomous: true
requirements: [IADM-01, IADM-02, IADM-07]
user_setup: []

must_haves:
  truths:
    - "Operator visits /admin/inbound (operator live_session, Auth gate), selects a tenant, sees a paginated/filterable list of inbound records"
    - "A blank/missing tenant renders the empty-state copy and never leaks another tenant's record id or recipient"
    - "Selecting a record shows the canonical %InboundMessage{} summary, matched mailbox + outcome, and a chronological timeline of ExecutionRun rows (fresh + replay)"
    - "Filters (tenant, provider, mailbox outcome, time window, search) live in the URL and survive refresh/back/forward"
    - "/admin/inbound and its nav link are gated by available?/0 — absent inbound means the route/nav no-op"
    - "The detail header reads suppression_flagged defensively (Map.get) and never raises KeyError"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Master/detail InboundLive shell cloned from OperatorLive"
      min_lines: 120
      contains: "load_inbound_records"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      provides: "Inbound records list component"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex"
      provides: "Detail header with defensive suppression_flagged read"
      contains: "Map.get"
    - path: "mailglass_admin/lib/mailglass_admin/router.ex"
      provides: ":inbound_router opt + live /inbound route in operator live_session"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      to: "MailglassAdmin.OptionalDeps.MailglassInbound"
      via: "apply/3 gateway for list/timeline/detail — never a bare MailglassInbound.* reference"
      pattern: "OptionalDeps.MailglassInbound"
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      to: "tenant-required-or-empty"
      via: "load_inbound_records(%{\"tenant_id\" => \"\"}) -> []"
      pattern: "tenant_id.*=>.*\"\""
    - from: "mailglass_admin/lib/mailglass_admin/router.ex"
      to: "MailglassAdmin.InboundLive"
      via: "live \"/inbound\" inside the operator live_session"
      pattern: "live \"/inbound\""
---

<objective>
Wave 1: clone the shipped OperatorLive master/detail surface into `MailglassAdmin.InboundLive` + `MailglassAdmin.Inbound.*` sibling components, and wire `/admin/inbound` into the existing operator `live_session` with a nav link — all gated by `available?/0`. This is the non-novel bulk of the phase: the list, detail header, timeline, filters, replay modal chrome, and the inbound destructive-action guard, plus the tenant-required-or-empty gate. The two NET-NEW cards (routing-trace, evidence) and the replay confirm flow + live updates are Wave 2.

Purpose: stand up a working, tenant-safe, Auth-gated inbound observability surface with list + detail + timeline that an operator can navigate, against the Wave 0 seams (gateway, read-model, mask_recipient). It is the "blueprint" Wave 2 finishes.
Output: a routable, testable InboundLive with list/detail/timeline, the replay modal chrome (open/close; confirm flow lands in Wave 2), and the inbound DestructiveAction guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-inbound-admin-liveview/48-CONTEXT.md
@.planning/phases/48-inbound-admin-liveview/48-UI-SPEC.md
@.planning/phases/48-inbound-admin-liveview/48-PATTERNS.md
@.planning/phases/48-inbound-admin-liveview/48-RESEARCH.md
@.planning/phases/48-inbound-admin-liveview/48-VALIDATION.md
@.planning/phases/48-inbound-admin-liveview/48-01-SUMMARY.md

<interfaces>
<!-- Wave 0 seams this plan consumes (from 48-01) + OperatorLive analogs to clone. -->

Wave 0 gateway (mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex):
- `available?/0` — guard the module body / route / nav via `Code.ensure_loaded?(MailglassAdmin.OptionalDeps.MailglassInbound)` then `.available?/0`
- `apply/3` wrappers: `list_records/2`, `timeline/2`, `detail/2`, `explain/2`, `replay/2` — InboundLive calls THESE, never `MailglassInbound.*` directly

Wave 0 read-model contracts (mailglass_inbound/.../internal/operator/):
- Records.list_records — tenant-required-or-empty, select map projection
- Timeline — ALL ExecutionRun rows for a record, chronological (source/mailbox/outcome/outcome_reason/executed_at/id)
- Detail — canonical InboundRecord + InboundEvidence + matched mailbox/outcome (latest fresh run)

OperatorLive analogs to clone (mailglass_admin/lib/mailglass_admin/operator_live.ex):
- handle_params URL-state l.68-117 (swap delivery_id → inbound_id; base path default → /inbound)
- handle_event "select_delivery" → "select_inbound" l.98-103 (push_patch build_path)
- tenant gate `load_deliveries(%{"tenant_id" => ""}), do: []` l.338 (the load-bearing security head)
- filter normalize + cast_enum allow-list l.326-336, 487-495 (inbound casts against ExecutionRun.__outcomes__/0: [:no_match,:accept,:ignore,:reject,:bounce,:failed])
- assign_*_state aggregation l.408-427 (list + selected + timeline + detail in one pass via apply/3)
- render shell l.190-314 (root data-theme div → main max-w-7xl → flash band → filter card → lg:grid-cols-[minmax(22rem,28rem)_1fr] split → modal outside main)

Components to clone as MailglassAdmin.Inbound.* siblings (mailglass_admin/lib/mailglass_admin/operator/):
- deliveries_list.ex → records_list.ex (empty-state copy from UI-SPEC; selected-row border-l-4 + aria-current/aria-selected; meta line `tenant · PROVIDER · matched-mailbox-or-"no match" · received_at`; call Components.mask_recipient/1)
- detail_header.ex → detail_header.ex (<dl> grid: Tenant/Provider/From-masked/Subject/Received/Matched-mailbox; suppression_flagged via Map.get; Replay button row, disabled when outcome :no_match)
- timeline.ex → timeline.ex (ExecutionRun rows NOT ReplayRun; source badge Fresh/Replay = badge badge-outline; outcome→dot color per UI-SPEC Color table)
- filters_form.ex → filters_form.ex (tenant/provider/outcome-select-over-__outcomes__/window-24h-7d-30d/search; min-h-11; label text-xs font-bold uppercase tracking-[0.08em] text-secondary)
- replay_modal.ex → replay_modal.ex SIMPLIFIED (DELETE the `case @replay_targets` block + target_card/confirm_enabled? helpers l.36-142; single confirmation body, Confirm replay always enabled when open)
- destructive_action.ex → destructive_action.ex (action :destructive_action → :replay_inbound; context key :delivery → :inbound_record; D-48-10 sibling, never reuse outbound verbatim)

Auth seam (mailglass_admin/lib/mailglass_admin/auth.ex:33,55): `action :: :operator_access | :destructive_action | atom()` — arbitrary atoms accepted, NO new auth surface
Operator Mount assigns (mailglass_admin/lib/mailglass_admin/operator/mount.ex:21,33-38): :operator_actor + :operator_auth (adapter: opts[:auth]); InboundLive reads socket.assigns.operator_auth[:adapter]
Router macro (mailglass_admin/lib/mailglass_admin/router.ex): @operator_opts_schema l.143; __operator_session__/2 l.291-302; on_mount {Operator.Mount, opts} l.244; operator live_session block l.246-251; @compile no_warn_undefined l.87-94
Promoted PII masking: MailglassAdmin.Components.mask_recipient/1 (from Wave 0)
Fixtures: MailglassInbound.Fixtures + InboundRecords.insert_* (seed matched/no_match/replay rows)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone inbound sibling components (list, detail header, timeline, filters, replay-modal chrome, destructive-action)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/records_list.ex, mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex, mailglass_admin/lib/mailglass_admin/inbound/timeline.ex, mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex, mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/lib/mailglass_admin/inbound/destructive_action.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex — list component analog (attr/empty-state l.10-69; selected-row l.31-40,71-78); note mask_recipient is now in Components (Wave 0)
    - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex — header analog (<dl> grid l.14-57; action row l.59-76)
    - mailglass_admin/lib/mailglass_admin/operator/timeline.ex — timeline analog (l.13-65 structure; replay badge l.122-126; dot color l.148-154)
    - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex — filters analog (field structure l.13-91; <option> loop l.49-55)
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex — modal analog (chrome l.17-35,89-102; the case @replay_targets block l.36-87 + helpers l.109-142 to DELETE)
    - mailglass_admin/lib/mailglass_admin/operator/destructive_action.ex — guard analog (authorize seam l.15-31)
    - mailglass_admin/lib/mailglass_admin/components.ex — Components.mask_recipient/1 (promoted Wave 0), flash/1, icon/1
    - mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex — __outcomes__/0 (l.76-77); source/outcome/outcome_reason/executed_at fields
    - SOURCE OF TRUTH (copy verbatim): 48-UI-SPEC.md Component Inventory + Copywriting Contract + Color (outcome→class) tables; 48-PATTERNS.md per-component sections; 48-RESEARCH.md Pitfall 2 (suppression_flagged Map.get) + Pitfall 7 (ExecutionRun not ReplayRun)
  </read_first>
  <behavior>
    - Test: records_list renders the empty-state copy "No inbound records match these filters. Clear the filters or wait for the next inbound message." when given []
    - Test: detail_header renders without raising for a record that has NO :suppression_flagged field (Map.get default false → indicator omitted, never KeyError)
    - Test: detail_header Replay button is disabled when the displayed outcome is :no_match
    - Test: timeline renders ExecutionRun rows with a Fresh/Replay source badge and outcome→dot color per the UI-SPEC Color table
    - Test: filters_form outcome select offers exactly ExecutionRun.__outcomes__/0 options
    - Test: replay_modal renders the single confirmation body (UI-SPEC destructive-confirmation copy) with Confirm replay always enabled when open — no target-selection branch
  </behavior>
  <action>
    Clone the six `operator/*` components into `mailglass_admin/lib/mailglass_admin/inbound/` as `MailglassAdmin.Inbound.*` siblings (D-48-13 "sibling, not refactor" — do NOT parameterize the outbound modules):
    - `records_list.ex`: clone attr/empty-state/row structure. Empty-state heading "No inbound records" + body (UI-SPEC). Selected-row `border-l-4 border-primary bg-base-100` + `aria-current`/`aria-selected`; unselected `border-l-4 border-transparent bg-base-200 hover:bg-base-100`. Call `MailglassAdmin.Components.mask_recipient/1` (NOT a private copy). Meta line `tenant · PROVIDER · matched-mailbox-or-"no match" · received_at`. Record id in mono, outcome badge.
    - `detail_header.ex`: clone header + `<dl>` grid (Tenant, Provider, From via Components.mask_recipient/1, Subject, Received, Matched mailbox). CRITICAL (Pitfall 2): read the suppression flag as `Map.get(record, :suppression_flagged, false)` — NEVER `record.suppression_flagged` (KeyError; field does not exist until Phase 49). Render the IOPS-05 copy "Sender suppressed: this message was flagged, not bounced, to preserve diagnostic signal." only when truthy. Clone the bottom Replay action row (`btn btn-error min-h-11`); DISABLE the button when the displayed outcome is `:no_match` (Pitfall 1 — replay always fails there).
    - `timeline.ex`: clone the vertical `<ol>` chrome. Rows are `ExecutionRun` (NOT ReplayRun — Pitfall 7), chronological by `executed_at`. `source: :replay` → `badge badge-outline` rendering Fresh/Replay text. Show outcome label + `outcome_reason` inline + `executed_at`/run id in mono. Outcome→dot color per UI-SPEC: `:accept`→bg-success, `:no_match`→bg-warning, `:reject|:bounce|:failed`→bg-error, `:ignore`→bg-secondary.
    - `filters_form.ex`: clone field structure. Inbound filters: tenant (text), provider (text), mailbox outcome (select over `ExecutionRun.__outcomes__/0`), time window (24h/7d/30d select), search (text). All `min-h-11`; label `text-xs font-bold uppercase tracking-[0.08em] text-secondary`. Reuse the `<option>` loop.
    - `replay_modal.ex`: clone modal chrome (overlay, `max-w-2xl rounded-box shadow-2xl` card, Close, Cancel/`Confirm replay btn btn-error min-h-11`). SIMPLIFY — DELETE the entire `case @replay_targets` block + `target_card`/`confirm_enabled?` helpers (IADM-03 — single target). Single confirmation body using the UI-SPEC destructive-confirmation copy; `Confirm replay` always enabled when open.
    - `destructive_action.ex`: clone the authorize seam but RE-SHAPE the context (D-48-10): action `:replay_inbound`; context key `:inbound_record` (NEVER `:delivery` — adopter Auth may pattern-match `%Mailglass.Outbound.Delivery{}`). Ride the existing `Auth.authorize/3` `atom()` action type (no new auth surface). Default denial copy "Replay blocked: this action is not authorized for the current operator."
  </action>
  <acceptance_criteria>
    - All six modules exist under `mailglass_admin/lib/mailglass_admin/inbound/` namespaced `MailglassAdmin.Inbound.*`
    - detail_header reads the flag defensively: `grep -c 'Map.get(record, :suppression_flagged' mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex` ≥ 1 AND `grep -c 'record.suppression_flagged' .../detail_header.ex` == 0
    - replay_modal has NO `@replay_targets` branch: `grep -c 'replay_targets' mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex` == 0
    - destructive_action passes `:inbound_record` not `:delivery`: `grep -c ':inbound_record' .../inbound/destructive_action.ex` ≥ 1 AND `grep -c 'inbound_record under :delivery\|:delivery =>' .../inbound/destructive_action.ex` == 0
    - components call the promoted masker, not a private copy: `grep -c 'defp mask_recipient' mailglass_admin/lib/mailglass_admin/inbound/records_list.ex` == 0
    - timeline reads ExecutionRun not ReplayRun: `grep -c 'ReplayRun' mailglass_admin/lib/mailglass_admin/inbound/timeline.ex` == 0
    - `cd mailglass_admin && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Six MailglassAdmin.Inbound.* sibling components ship; suppression read is defensive; replay modal is single-target; destructive guard uses :inbound_record; masking is the one promoted definition; timeline reads ExecutionRun.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InboundLive shell — URL-state, tenant gate, list/detail/timeline aggregation via the gateway</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound_live.ex, mailglass_admin/test/support/inbound_fixtures.ex, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex — the full shell to clone: handle_params l.68-117, select event l.98-103, tenant gate l.338, filter normalize/cast_enum l.326-336/487-495, assign_*_state l.408-427, render shell l.190-314
    - mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex (Wave 0) — available?/0 + apply/3 wrappers (list_records/timeline/detail) — call THESE
    - mailglass_admin/lib/mailglass_admin/operator/mount.ex — l.21,33-38 the assigns InboundLive reads (:operator_actor, :operator_auth)
    - mailglass_admin/lib/mailglass_admin/inbound/*.ex (Task 1) — the components to render
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs — the LiveView test pattern (lazy_html DOM) to mirror
    - mailglass_inbound/lib/mailglass_inbound/fixtures.ex + inbound_records.ex insert_* — seed helpers for the new test fixtures module
    - SOURCE OF TRUTH: 48-UI-SPEC.md Screen Contract + Interaction Contract + Copywriting Contract (empty/no-selection/error copy verbatim); 48-CONTEXT.md D-48-03 (available?/0 gate), D-48-04/05 (read-model + tenant gate); 48-VALIDATION.md V1, V5(masking half)
  </read_first>
  <behavior>
    - Test (V1): `live(conn, "/inbound")` with a blank tenant renders the empty-state copy, asserts an empty list, and leaks NO other-tenant record id or recipient
    - Test: selecting a record (push_patch with inbound_id) renders the detail header + timeline for that record in place (no remount); URL carries inbound_id + filters
    - Test: filters (provider/outcome/window/search) are reflected in the URL via handle_params and survive a re-mount
    - Test (V5 masking half): the list + detail render the masked recipient by default; `refute html =~ raw_recipient`, `assert html =~ masked`
    - Test: no-record-selected renders "Select an inbound record to inspect its routing, execution timeline, and raw source."
  </behavior>
  <action>
    Create `MailglassAdmin.InboundLive` cloning `operator_live.ex` mechanics (Claude's discretion on exact assign shapes per D-48 — clone OperatorLive). Swap `delivery_id`→`inbound_id`, default base path `/operator`→`/inbound`. Gate the module body / all data calls behind `Code.ensure_loaded?(MailglassAdmin.OptionalDeps.MailglassInbound)` + `available?/0` (D-48-03; mirror the core Oban gate pattern).
    - `handle_params` URL-state (clone l.68-117): assign base_path, filter_params, filter_form (`to_form(..., as: :filters)`), and the aggregated inbound state.
    - Tenant-required-or-empty (clone l.338): `defp load_inbound_records(%{"tenant_id" => ""}), do: []` head BEFORE any data call; the populated head routes through `apply(gateway, :list_records, [...])` — NEVER a bare `MailglassInbound.*` reference. Mirror for timeline/detail.
    - Filter normalization + enum cast (clone l.326-336/487-495): cast the outcome filter against `ExecutionRun.__outcomes__/0` via the `cast_enum/2` allow-list (V5 input-validation guard). Window 24h/7d/30d. Search free text.
    - `assign_*_state` aggregation (clone l.408-427): load list + selected record + timeline + detail in one pass through the gateway apply/3 wrappers.
    - Render shell (clone l.190-314): root `<div data-theme="mailglass-light">` → `<main class="mx-auto max-w-7xl ...">` → page header → flash band → filter card (Task 1 filters_form) → `lg:grid-cols-[minmax(22rem,28rem)_1fr]` master/detail split (records_list left; detail header + timeline right) → replay modal outside `<main>`. Detail-pane order: header → timeline → (routing-trace + evidence are Wave 2 slots, leave the placement points). Use the exact UI-SPEC empty/no-selection/error copy.
    - `select_inbound` event (clone l.98-103): push_patch to build_path with inbound_id + current filters.
    Create `test/support/inbound_fixtures.ex` seeding `InboundRecord` + `InboundEvidence` + `ExecutionRun` (matched / no_match / replay) via `MailglassInbound.Fixtures` + `InboundRecords.insert_*`. Add `inbound_live_test.exs` covering V1 (blank-tenant empty + no cross-tenant leak), selection, URL filters, masking-by-default (V5 half), and no-selection copy.
  </action>
  <acceptance_criteria>
    - InboundLive gates on the gateway: `grep -c 'OptionalDeps.MailglassInbound' mailglass_admin/lib/mailglass_admin/inbound_live.ex` ≥ 1 AND no bare `MailglassInbound.` reference escapes the gateway: `grep -c 'MailglassInbound\.' mailglass_admin/lib/mailglass_admin/inbound_live.ex` == 0 (only the gateway module name `OptionalDeps.MailglassInbound` is allowed; verify the grep counts bare `MailglassInbound.` excluding the gateway path)
    - Tenant gate present: `grep -c 'load_inbound_records(%{"tenant_id" => ""})' mailglass_admin/lib/mailglass_admin/inbound_live.ex` ≥ 1
    - V1 + masking + selection green: `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` — blank-tenant empty-state + no other-tenant id/recipient in HTML; masked recipient by default (`refute html =~ raw_recipient`)
    - no-selection copy rendered verbatim: test asserts `html =~ "Select an inbound record to inspect its routing, execution timeline, and raw source."`
    - `test/support/inbound_fixtures.ex` seeds matched + no_match + replay rows (grep for all three outcome atoms ≥ 1 each)
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>InboundLive renders the master/detail shell, list/detail/timeline via the gateway apply/3 (no bare inbound ref), tenant-required-or-empty with no cross-tenant leak, URL-as-state filters, masked-by-default recipient; fixtures seed matched/no_match/replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Router wiring — :inbound_router opt + /inbound route in operator live_session + nav link (IADM-07)</name>
  <files>mailglass_admin/lib/mailglass_admin/router.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/router.ex (the file being modified) — @operator_opts_schema l.143; __operator_session__/2 l.291-302; on_mount {Operator.Mount, opts} l.244; operator live_session block l.246-251; @compile no_warn_undefined l.87-94; existing nav/route wiring
    - mailglass_admin/lib/mailglass_admin/operator/mount.ex — l.21 the Mount hook receives full opts (surfaces :inbound_router to assigns)
    - mailglass_admin/lib/mailglass_admin/optional_deps/mailglass_inbound.ex (Wave 0) — available?/0 for the route/nav gate
    - SOURCE OF TRUTH: 48-CONTEXT.md D-48-07 (:inbound_router opt, no global singleton), D-48-12 (mount in operator live_session, Auth gate, NOT dev-preview), D-48-03 (available?/0 gates route + nav); 48-PATTERNS.md "Router Macro Threading"; nimble_options @operator_opts_schema for opt validation
  </read_first>
  <behavior>
    - Test: when inbound is available, the operator live_session exposes a `live "/inbound"` route resolving to MailglassAdmin.InboundLive
    - Test: the :inbound_router opt is validated by @operator_opts_schema (type :atom) and threaded into both __operator_session__ and the Operator.Mount on_mount tuple
    - Test: /inbound mounts in the SAME operator live_session (Operator.Mount + Auth gate), NOT the dev-preview session — unauthenticated access is gated by the existing Auth plug (no new auth surface)
  </behavior>
  <action>
    Extend the existing `mailglass_operator_routes/2` macro in `router.ex`:
    - Add `:inbound_router` (type `:atom`) to `@operator_opts_schema` (l.143) — validated by nimble_options; no global Application config singleton (D-48-07).
    - Thread `:inbound_router` into both `__operator_session__/2` (l.291-302) AND the `{MailglassAdmin.Operator.Mount, opts}` on_mount tuple (l.244) so the Mount hook surfaces it to assigns alongside `:auth` (it already receives the full opts).
    - Add `live "/inbound", MailglassAdmin.InboundLive, :index` INSIDE the existing operator `live_session` (l.246-251) — same session, same `Operator.Mount` + Auth gate (D-48-12; mounting in the dev-preview session would bypass Auth and leak tenant data). Gate the route emission via `available?/0` (D-48-03) so it no-ops when inbound is absent.
    - Add a nav link to the existing admin nav, also gated by `available?/0`.
    - Add `MailglassAdmin.InboundLive` to the existing `@compile {:no_warn_undefined, [...]}` (router.ex:87-94) so the --no-optional-deps lane stays clean.
    Leave the Boundary decl untouched (D-48-02).
  </action>
  <acceptance_criteria>
    - `:inbound_router` is in the opts schema: `grep -c ':inbound_router' mailglass_admin/lib/mailglass_admin/router.ex` ≥ 2 (schema + threading)
    - `/inbound` live route present in the operator live_session: `grep -c 'live "/inbound", MailglassAdmin.InboundLive' mailglass_admin/lib/mailglass_admin/router.ex` ≥ 1
    - route/nav gated by available?/0 (grep `grep -v '^#' router.ex | grep -c 'available?'` ≥ 1) or `Code.ensure_loaded?(MailglassAdmin.OptionalDeps.MailglassInbound)`
    - Boundary decl unchanged: `grep -c 'use Boundary, deps: \[Mailglass\], exports: \[Router\]' mailglass_admin/lib/mailglass_admin.ex` ≥ 1
    - `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` green (route resolves; mounts in operator session) AND `mix compile --no-optional-deps --warnings-as-errors` exits 0 (V4)
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors && mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <done>/admin/inbound routes to InboundLive inside the operator live_session (Auth-gated, not dev-preview); :inbound_router opt is validated + threaded to assigns; route + nav are available?/0-gated; --no-optional-deps stays green; Boundary unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → /admin/inbound | Must be gated by the existing Auth plug via the operator live_session (no new auth surface) |
| operator (tenant A) → inbound list/detail | Tenant-required-or-empty; a blank/foreign tenant must render the empty state and leak no rows |
| client URL params → InboundLive | Filter params (provider/outcome/window/search/tenant_id/inbound_id) are untrusted; outcome cast against an allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Elevation | router.ex /inbound route | mitigate | Route mounts in the operator `live_session` (Operator.Mount → Auth gate), NOT the dev-preview session (D-48-12); additionally gated by `available?/0` (D-48-03) |
| T-48-06 | Information Disclosure | InboundLive load_inbound_records / detail / timeline | mitigate | `load_inbound_records(%{"tenant_id" => ""}) -> []` head + read-model `Tenancy.scope/2`; LiveView test asserts no other-tenant id/recipient in HTML (V1) |
| T-48-07 | Information Disclosure | records_list / detail_header rendering | mitigate | Recipient/sender masked by default via `Components.mask_recipient/1`; `refute html =~ raw_recipient` (V5 masking half) |
| T-48-08 | Tampering / DoS | URL filter params | mitigate | `normalize_filter_params` + `cast_enum/2` allow-list against `ExecutionRun.__outcomes__/0` (V5 input validation); unknown outcome rejected, never passed to SQL |
| T-48-09 | Information Disclosure | detail_header suppression_flagged read | mitigate | `Map.get(record, :suppression_flagged, false)` — defensive; never raises and never renders a leaked field (Pitfall 2) |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No external package installs this phase (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` — V1 (tenant gate), V5 masking half, selection, URL filters green.
- `cd mailglass_admin && mix compile --no-optional-deps --warnings-as-errors` — V4 stays green after route/nav/components added.
- `cd mailglass_admin && mix credo --strict` — LINT-06 (V9) clean.
- Wave merge gate: `cd mailglass_admin && mix verify.preview` (full admin gate incl. no-optional-deps lane + bundle-drift). UI-SPEC says zero new tokens — expect no `priv/static/` diff; if a class pulls a new utility, run `mix mailglass_admin.assets.build` and commit the bundle.
</verification>

<success_criteria>
- /admin/inbound is reachable, Auth-gated via the operator live_session, available?/0-gated (IADM-07).
- The master/detail list + detail header + timeline render against the Wave 0 read-model through the gateway (IADM-01, IADM-02 list/detail/timeline half).
- Blank/missing tenant renders the empty state with no cross-tenant leak (V1).
- Recipient masked by default (V5 masking half); suppression read is defensive (no KeyError).
- Filters live in the URL and survive refresh (IADM-01).
- --no-optional-deps lane (V4) and LINT-06 (V9) stay green.
</success_criteria>

<output>
Create `.planning/phases/48-inbound-admin-liveview/48-02-SUMMARY.md` when done
</output>
