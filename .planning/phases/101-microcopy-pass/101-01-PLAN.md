---
phase: 101-microcopy-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
autonomous: true
requirements:
  - COPY-01

must_haves:
  truths:
    - "The replay-success flash on inbound_live.ex line 219 uses 'InboundMessage's timeline' not 'message's timeline'"
    - "The no-selection error flash on inbound_live.ex line 227 matches the LD-16 locked string verbatim"
    - "The subtitle on inbound_live.ex line 289 uses 'InboundMessage' not 'received message'"
    - "No residual put_flash or subtitle copy violations remain in inbound_live.ex beyond what COPY-LD already settled"
    - "All already-applied LD strings (LD-01..14, LD-16 at line 385) remain verbatim and untouched"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Three residual string fixes (lines 219, 227, 289)"
      contains: "this InboundMessage's timeline"
  key_links:
    - from: "inbound_live.ex:217-220"
      to: "COPY-LD-13 (replay modal confirmation)"
      via: "put_flash :info"
      pattern: "InboundMessage's timeline"
    - from: "inbound_live.ex:224-228"
      to: "COPY-LD-16 (select-record prompt)"
      via: "put_flash :error"
      pattern: "Select an InboundMessage to inspect its Mailbox routing"
    - from: "inbound_live.ex:289"
      to: "COPY-LD-07 (InboundMessage domain noun in subtitle)"
      via: "subtitle= HEEx attribute"
      pattern: "InboundMessage"
---

<objective>
Apply the three residual COPY-LD string fixes in `mailglass_admin/lib/mailglass_admin/inbound_live.ex`
that the surface phases (98/99/100) missed when updating rendered-pane copy but skipped
`put_flash` and subtitle strings, then sweep all LiveView flash/subtitle sites for any
additional non-conformant strings the dossier's pane-biased inventory may have missed.

Purpose: Close the gap between the rendered-pane LD strings already applied and the flash/subtitle
strings still using generic domain terms, so every copy site in the inbound surface satisfies
the "thoughtful maintainer" voice and seven domain nouns rule before the conformance check
(Plan 02) asserts them.

Output: Updated `inbound_live.ex` with three string edits; a final source-sweep finding attached
as a comment in the plan SUMMARY confirming no additional violations.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/101-microcopy-pass/101-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply three residual COPY-LD string fixes in inbound_live.ex</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>

  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (READ IN FULL — must not miss any put_flash or subtitle site)
    - .planning/research/v1.11/MICROCOPY.md (THE SPEC — the LOCKED DECISION table at lines 484–504 is authoritative for exact strings; consult it before each edit)
    - .planning/phases/101-microcopy-pass/101-CONTEXT.md (D-04, D-05, D-06, D-07 scope)
  </read_first>

  <action>
    Make exactly three string edits in `mailglass_admin/lib/mailglass_admin/inbound_live.ex`.
    Apply each locked string VERBATIM from the COPY-LD table — do NOT rephrase.

    EDIT 1 — D-05 / COPY-LD-13 (replay-success flash, ~line 219):
    Current: "Replay recorded. A new replay run was appended to this message's timeline."
    Replace with: "Replay recorded. A new replay run was appended to this InboundMessage's timeline."
    Rationale: "message" is the generic noun; in the inbound context the domain noun is "InboundMessage".

    EDIT 2 — D-04 / COPY-LD-16 (no-selection error flash, ~line 227):
    Current: "Select an inbound record to inspect its routing, execution timeline, and raw source."
    Replace with: "Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence."
    This is the COPY-LD-16 locked string verbatim. Note: the already-correct rendered-pane string
    at line 385 uses this same text and must NOT be changed.

    EDIT 3 — D-06 / COPY-LD-07 (subtitle attribute, ~line 289):
    Current: subtitle="See why a received message routed the way it did — execution timeline, routing trace, and raw evidence."
    Replace with: subtitle="See why an InboundMessage routed the way it did — execution timeline, routing trace, and raw evidence."
    Rationale: "received message" is not a domain noun; "InboundMessage" is the seven-noun-correct term
    for inbound context (COPY-LD-07 pattern: use domain nouns on operator/inbound surfaces).

    ALSO perform the D-07 source sweep before committing:
    Scan ALL `put_flash` sites and `subtitle=` attributes in inbound_live.ex, operator_live.ex,
    and preview_live.ex for any copy that uses banned standalone "Email", "Status", "Notification",
    "Oops", "whoops", "uh oh", or "something went wrong", or uses generic "message" / "inbound record"
    where a domain noun is required.

    Sweep findings to report (from pre-read):
    - operator_live.ex:278 subtitle "Prove what happened to a message" — per COPY-LD-07 analysis:
      "happened to a message" is the compound usage acceptable in operator context (not standalone noun
      violation). Flag in SUMMARY but do NOT change (not in COPY-LD locked decisions, changing risks
      regression, outside Phase 101 locked scope).
    - operator_live.ex:225 put_flash "Select a delivery before replaying a webhook." — conformant
      (uses "delivery", cause-naming pattern). No change needed.
    - preview_live.ex put_flash strings: "Scenario not found", "Reloaded: …" — developer surface
      LD-08 exception (technical precision). No change needed.
    - No other violations found.

    CONSTRAINTS:
    - Do NOT change lines other than the three targets.
    - Do NOT change the rendered-pane LD-16 string at line 385 (already correct, do not touch).
    - No CSS changes; no bundle rebuild needed (string-only edits in Elixir source).
    - PII preserved: replay modal headings use mask_recipient/1 (not touched by these edits).
  </action>

  <verify>
    <automated>
      grep -n "InboundMessage's timeline" mailglass_admin/lib/mailglass_admin/inbound_live.ex
      grep -n "Select an InboundMessage to inspect its Mailbox routing" mailglass_admin/lib/mailglass_admin/inbound_live.ex
      grep -n "See why an InboundMessage routed" mailglass_admin/lib/mailglass_admin/inbound_live.ex
      grep -n "this message's timeline" mailglass_admin/lib/mailglass_admin/inbound_live.ex
      grep -n "inbound record to inspect" mailglass_admin/lib/mailglass_admin/inbound_live.ex
      grep -n "received message routed" mailglass_admin/lib/mailglass_admin/inbound_live.ex
    </automated>
    Expected: first three greps each return one matching line; last three greps return no output.
  </verify>

  <acceptance_criteria>
    - inbound_live.ex contains exact string "Replay recorded. A new replay run was appended to this InboundMessage's timeline." (per D-05 / COPY-LD-13)
    - inbound_live.ex contains exact string "Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence." in a put_flash call (per D-04 / COPY-LD-16; distinct from the already-correct line 385)
    - inbound_live.ex contains exact string "See why an InboundMessage routed the way it did — execution timeline, routing trace, and raw evidence." in the subtitle= attribute (per D-06 / COPY-LD-07)
    - inbound_live.ex does NOT contain "this message's timeline" (old string gone)
    - inbound_live.ex does NOT contain "inbound record to inspect its routing" in a put_flash call (old LD-16 violation gone)
    - inbound_live.ex does NOT contain "See why a received message routed" (old subtitle gone)
    - Line 385 is unchanged: still contains "Select an InboundMessage to inspect its Mailbox routing, execution timeline, and raw evidence." (rendered-pane, not a flash — must not be double-edited)
    - No other files are modified
    - cd mailglass_admin && mix compile --warnings-as-errors exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source → compiled template | String literals in HEEx/Elixir are compile-time constants; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Information Disclosure | flash strings containing recipient data | accept | PII minimization preserved: replay modal headings use mask_recipient/1; the three edited strings contain no email address, subject, or recipient data |
| T-101-SC | Tampering | no package installs in this plan | accept | Copy-string edits only; no npm/pip/cargo installs; no supply-chain surface |

Surface: text-string changes in one Elixir source file. Negligible threat surface. No input handling, auth, network, or data-flow changes. The one standing invariant: PII minimization is preserved — the edited flash strings do not introduce unmasked recipient/email/PII into rendered output.
</threat_model>

<verification>
Run after task completes:

```
cd mailglass_admin && mix compile --warnings-as-errors
```

Expected: exits 0 with no warnings. No bundle rebuild needed (no CSS or asset changes).

Cross-check D-02 already-applied strings are untouched by inspecting one representative LD per surface:
```
grep -n "No Deliveries match your filters" mailglass_admin/lib/mailglass_admin/operator
grep -n "No InboundMessages match these filters" mailglass_admin/lib/mailglass_admin/inbound
grep -n "Preview the first Mailable" mailglass_admin/lib/mailglass_admin/preview_live.ex
```
Each should return at least one match (confirming already-applied strings were not disturbed).
</verification>

<success_criteria>
Three residual string violations in inbound_live.ex corrected verbatim per COPY-LD-13 (replay flash),
COPY-LD-16 (no-selection flash), and COPY-LD-07 (subtitle). Compilation clean. No already-applied
LD strings disturbed. D-07 sweep confirms no additional violations across operator/inbound/preview
LiveView flash and subtitle sites.
</success_criteria>

<output>
Create `.planning/phases/101-microcopy-pass/101-01-SUMMARY.md` when done.
Include in SUMMARY: the three exact edits made with before/after strings, and the D-07 sweep
findings (other put_flash / subtitle sites reviewed and disposition for each).
</output>
