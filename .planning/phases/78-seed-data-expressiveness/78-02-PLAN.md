---
phase: 78-seed-data-expressiveness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/test/support/operator_fixtures.ex
  - mailglass_admin/e2e/operator.spec.js
autonomous: true
requirements:
  - SEED-01
  - SEED-02

must_haves:
  truths:
    - "The browser-tenant inbound operator has at least one seeded inbound record, navigable at /ops/mail/inbound?tenant_id=browser-tenant"
    - "Clicking the first inbound row navigates to a URL containing inbound_id="
    - "The inbound detail pane renders with id='inbound-detail-{inbound_id}' visible"
    - "operator.spec.js line 254 test.skip is removed and the 4-step inbound assertion executes"
    - "operator.spec.js delivery row indices 0–3 are unchanged (selected=0, noop=1, ambiguous=2, exact=3)"
    - "operator.spec.js passes with no NEW failures (pre-existing line 104 failure excluded per D-10)"
  artifacts:
    - path: "mailglass_admin/test/support/operator_fixtures.ex"
      provides: "browser-tenant inbound seed: one inbound record + evidence + :accept execution run at hours_ago(10) or older"
      contains:
        - "mailglass_inbound_records"
        - "mailglass_inbound_evidence"
        - "mailglass_inbound_replay_runs"
    - path: "mailglass_admin/e2e/operator.spec.js"
      provides: "Un-skipped inbound detail pane id-presence test at line 254"
      does_not_contain:
        - "test.skip(\"inbound detail pane carries record-keyed id"
  key_links:
    - from: "operator_fixtures.ex seed_browser_scenario!"
      to: "mailglass_inbound_records table"
      via: "raw SQL INSERT via TestRepo"
      pattern: "mailglass_inbound_records"
    - from: "operator.spec.js test (line ~254)"
      to: "/ops/mail/inbound?tenant_id=browser-tenant"
      via: "page.goto + click nth(0)"
      pattern: "inbound-detail-"
    - from: "operator_fixtures.ex reset!"
      to: "GET /ops/browser-reset"
      via: "operator.spec.js openOperator"
      pattern: "reset!"
---

<objective>
Seed one inbound record in operator_fixtures.ex for the browser-tenant scenario, then remove the
test.skip wrapper from operator.spec.js:254 and implement the 4-step inbound detail pane
id-presence assertion. Both changes land in the same commit (D-09 same-commit contract).

Purpose: Un-skip the MOTION-02 regression gate at operator.spec.js:254 that Phase 77 added
but could not activate because OperatorFixtures seeds zero inbound records (SEED-01 / GAP-13).
This is the depth half of D-01 (operator_fixtures.ex owns depth, not breadth). Row indices 0–3
for replay tests are protected by inserting the inbound record at hours_ago(10) or older (D-07).

Output: Modified operator_fixtures.ex (adds 3 raw SQL INSERT helpers or direct calls for
inbound_records/evidence/replay_runs tables, plus a call in seed_browser_scenario!).
Modified operator.spec.js (remove test.skip wrapper, implement 4-step assertion).
No schema changes. No bundle rebuild.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/78-seed-data-expressiveness/78-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/78-seed-data-expressiveness/78-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/78-seed-data-expressiveness/78-VALIDATION.md
@/Users/jon/projects/mailglass/.planning/phases/74-systematic-audit-and-ui-spec/74-ASSERTION-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inbound seed to operator_fixtures.ex (browser-tenant) + un-skip operator.spec.js:254</name>

  <read_first>
    - mailglass_admin/test/support/operator_fixtures.ex — full file: @tenant_id "browser-tenant", existing helpers (insert_delivery!, insert_event!, insert_webhook_event!, insert_suppression!, insert_linked_event!), seed_browser_scenario! body, reset! query, hours_ago/1 helper (DateTime.add(DateTime.utc_now(), -N, :hour))
    - mailglass_inbound/priv/repo/migrations/20260506163000_create_mailglass_inbound_storage_foundation.exs — exact column set for mailglass_inbound_records (id uuid PK, tenant_id text NOT NULL, provider text NOT NULL, provider_message_id text, message_id text, envelope_recipient text, from jsonb[], to jsonb[], cc jsonb[], bcc jsonb[], reply_to jsonb[], subject text, headers map, sent_at utc_datetime_usec, received_at utc_datetime_usec NOT NULL, text_body text, html_body text, attachments jsonb[], inserted_at, updated_at), mailglass_inbound_evidence (id uuid PK, tenant_id text NOT NULL, provider text NOT NULL, inbound_record_id uuid FK NOT NULL, raw_payload map, raw_headers map, raw_mime bytea, verification_facts map, parse_warnings map, attachment_blobs map, inserted_at, updated_at), mailglass_inbound_replay_runs (id uuid PK, tenant_id text NOT NULL, replay_id text nullable, mailbox text nullable, outcome text, outcome_reason text, failure map NOT NULL DEFAULT {}, executed_at utc_datetime_usec NOT NULL, metadata map NOT NULL DEFAULT {}, inbound_record_id uuid FK NOT NULL, inbound_evidence_id uuid FK NOT NULL, source text NOT NULL DEFAULT 'replay', inserted_at, updated_at)
    - mailglass_inbound/priv/repo/migrations/20260506210000_generalize_replay_runs_to_execution_lineage.exs — confirms source column added with default 'replay', replay_id and mailbox made nullable
    - mailglass_inbound/priv/repo/migrations/20260525000000_add_suppression_flagged_to_inbound_records.exs — check if suppression_flagged column added to mailglass_inbound_records
    - mailglass_inbound/priv/repo/migrations/20260523120000_add_mailgun_fingerprint_index.exs — check if fingerprint columns added to inbound_records or evidence
    - mailglass_admin/test/support/operator_fixtures.ex:214-241 — raw SQL insert pattern for insert_webhook_event! (model for inbound SQL inserts: Ecto.Adapters.SQL.query! with positional params)
    - mailglass_admin/e2e/operator.spec.js:242-268 — full skip block to be removed; exact navigation URL and assertion targets (operator-inbound-row testid, inbound_id URL param, #inbound-detail-${inboundId} locator)
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex:36 — data-testid="inbound-record-row" (note: spec uses operator-inbound-row — verify which testid name the spec block uses vs what the template emits)
    - mailglass_admin/e2e/operator.spec.js:262 — test uses getByTestId("operator-inbound-row") — verify this matches the actual testid in records_list.ex OR update the spec to use "inbound-record-row"
    - mailglass_admin/lib/mailglass_admin/inbound/inbound_live.ex — confirm the inbound detail pane id attribute format: id={"inbound-detail-#{@detail.record.id}"} (Phase 77 Plan 01 added this)
    - operator_fixtures.ex reset! query — confirm it currently truncates outbound tables only; must be extended to include mailglass_inbound_replay_runs, mailglass_inbound_evidence, mailglass_inbound_records
  </read_first>

  <action>
  This task implements the operator-fixtures depth seed and the operator.spec.js un-skip together
  (D-09 same-commit contract). Both changes must be committed together.

  Gap-register citation: GAP-13 (support-card zero-state — without an inbound record the
  browser scenario cannot exercise the inbound detail pane motion gate).

  PART A — operator_fixtures.ex changes:

  1. Extend reset! to truncate inbound tables. Update the TRUNCATE query to include the three
     inbound tables in dependency order (runs before records before evidence is NOT correct
     for CASCADE — use RESTART IDENTITY CASCADE which handles FK order automatically):
     "TRUNCATE TABLE mailglass_inbound_replay_runs, mailglass_inbound_evidence, mailglass_inbound_records, mailglass_webhook_events, mailglass_events, mailglass_suppressions, mailglass_deliveries RESTART IDENTITY CASCADE"

  2. Read all migration files listed in read_first to assemble the EXACT column list for each
     inbound table before writing any SQL. Specifically check:
     - mailglass_inbound_records: does it have a suppression_flagged boolean column? (20260525 migration)
     - mailglass_inbound_records: does it have fingerprint columns? (20260523 migration)
     If yes to either, include those columns in the INSERT (with appropriate null/default values).

  3. Add private helpers for inbound inserts. Model them exactly on insert_webhook_event! (raw SQL
     via Ecto.Adapters.SQL.query! with TestRepo). Add three private functions:

     insert_inbound_record!(attrs):
     - Generates UUID id = Ecto.UUID.generate()
     - Inserts into mailglass_inbound_records with all NOT NULL columns
     - Required columns: id (uuid), tenant_id, provider, received_at, from (jsonb array), to (jsonb array), cc (jsonb array, default []), bcc (jsonb array, default []), reply_to (jsonb array, default []), headers (map, default {}), attachments (jsonb array, default []), inserted_at, updated_at
     - Optional columns with null default: provider_message_id, message_id, envelope_recipient, subject, sent_at, text_body, html_body
     - Include suppression_flagged (false) and any fingerprint columns found in migrations
     - Returns a map with at least %{id: uuid_string, tenant_id: ..., subject: ...}

     insert_inbound_evidence!(inbound_record_id):
     - Generates UUID id = Ecto.UUID.generate()
     - Inserts into mailglass_inbound_evidence
     - Required: id, tenant_id, provider, inbound_record_id (FK), raw_payload ({}), raw_headers ({}), verification_facts ({}), parse_warnings ({}), attachment_blobs ({}), inserted_at, updated_at
     - Optional with null: raw_mime
     - Returns a map with at least %{id: uuid_string}

     insert_inbound_run!(inbound_record_id, inbound_evidence_id):
     - Generates UUID id = Ecto.UUID.generate()
     - Inserts into mailglass_inbound_replay_runs
     - Required: id, tenant_id, inbound_record_id (FK), inbound_evidence_id (FK), source ('fresh'), executed_at (DateTime.utc_now()), failure ({}), metadata ({}), inserted_at, updated_at
     - outcome: 'accept', mailbox: 'Mailglass.Example.BrowserMailbox' (a plausible mailbox name)
     - replay_id: nil (nullable per migration), outcome_reason: nil
     - Returns the run id or :ok

  4. Add a call in seed_browser_scenario! AFTER all existing delivery inserts, at the END:
     inbound_record = insert_inbound_record!(%{
       tenant_id: @tenant_id,
       provider: "postmark",
       provider_message_id: "pm_browser_inbound_001",
       envelope_recipient: "support@browser-scenario.example",
       subject: "Browser scenario support request",
       from: [%{"address" => "user@browser-test.example"}],
       to: [%{"address" => "support@browser-scenario.example"}],
       received_at: hours_ago(10)   ← CRITICAL: must be older than hours_ago(6) (browser-other delivery)
     })
     inbound_evidence = insert_inbound_evidence!(inbound_record.id)
     insert_inbound_run!(inbound_record.id, inbound_evidence.id)

     The received_at: hours_ago(10) value ensures this inbound record sorts below any delivery
     row in the operator delivery list. The inbound list has a separate sort
     (desc: received_at, inserted_at) so this record will be index 0 in the inbound list.
     Row indices 0–3 in the DELIVERY list are unaffected (separate list, separate sort,
     separate table). D-07 row-index stability is preserved.

  5. The raw SQL for jsonb array columns (from, to, cc, bcc, reply_to, attachments) must use
     the correct PostgreSQL type cast. For empty arrays: pass [] as Jason-encoded JSON.
     For the from array: pass [%{"address" => "user@browser-test.example"}] as Jason-encoded JSON.
     Use the pattern from insert_webhook_event!: pass Elixir maps/lists and let
     Ecto.Adapters.SQL.query! handle encoding via the postgrex type encoder.
     If postgrex does not auto-encode {:array, :map}, cast explicitly in SQL with $N::jsonb.

  PART B — operator.spec.js changes:

  1. Read the existing test.skip block (lines ~242-268) carefully. Note the testid used:
     getByTestId("operator-inbound-row") — verify this matches the HEEx testid.
     Reading records_list.ex:36 confirms the actual testid is "inbound-record-row".
     The spec block uses "operator-inbound-row" — this is a discrepancy. Use "inbound-record-row"
     in the un-skipped test to match the actual DOM testid (the comment above the skip block
     describes the intent but the testid in the code body may differ from what's in the DOM).

  2. Remove the test.skip wrapper. Replace:
     test.skip("inbound detail pane carries record-keyed id [SKIP: requires inbound seed in browser scenario]", async ({ page }) => {
       // Phase 78 seed expansion is the gate to enable this test.
       // See OperatorFixtures.seed_browser_scenario!() — zero inbound records are seeded.
     with:
     test("inbound detail pane carries record-keyed id", async ({ page }) => {

  3. Implement the 4-step assertion body (per the comment in the skip block and the
     RESEARCH.md SEED-02 table entry at operator.spec.js:254):
     a. setViewportSize({ width: 1280, height: 900 }) — already in the skip block
     b. await openOperator(page) — already in the skip block
     c. Navigate to inbound surface: await page.goto(`/ops/mail/inbound?tenant_id=${tenantId}`)
        — already in the skip block
     d. Click the first inbound row using "inbound-record-row" testid (not "operator-inbound-row"):
        await page.getByTestId("inbound-record-row").nth(0).click();
     e. Wait for URL to contain inbound_id= then read it:
        await expect(page).toHaveURL(/inbound_id=/);
        const inboundId = new URL(page.url()).searchParams.get("inbound_id");
        expect(inboundId).toBeTruthy();
     f. Assert the detail pane id attribute:
        await expect(page.locator(`#inbound-detail-${inboundId}`)).toBeVisible();

  4. Remove or update the comment block above the test that refers to "Phase 78 seed expansion
     is the gate" — replace with a brief MOTION-02 citation comment consistent with the
     delivery detail pane test above it.

  5. Verify the updated test name no longer contains "[SKIP:...]" so it runs normally.

  Do NOT modify any other test blocks in operator.spec.js. Do NOT change delivery row indices.
  Do NOT attempt to fix the pre-existing line 104 "exact replay flow" failure (D-10).
  </action>

  <verify>
    <automated>
    cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs
    </automated>
    <human-check>
    Run: npm run test:operator-browser from mailglass_admin/
    Confirm: the inbound detail pane test now runs (no longer skipped); it passes.
    Confirm: delivery row tests (exact=3, ambiguous=2, noop=1, selected=0) still pass.
    Confirm: pre-existing line 104 failure count has NOT increased (same 1 known failure).
    Run: mix verify.preview from mailglass_admin/ — confirm exits 0 (bundle bit-identical).
    </human-check>
  </verify>

  <done>
    - operator_fixtures.ex reset! truncates mailglass_inbound_replay_runs, mailglass_inbound_evidence, mailglass_inbound_records (in addition to existing outbound tables)
    - operator_fixtures.ex seed_browser_scenario! inserts one inbound record + evidence + execution run for browser-tenant at received_at: hours_ago(10) or older
    - operator.spec.js test.skip removed; test renamed to "inbound detail pane carries record-keyed id"
    - operator.spec.js inbound test uses getByTestId("inbound-record-row").nth(0) (matches actual DOM testid)
    - operator.spec.js inbound test asserts: URL contains inbound_id=; #inbound-detail-{inboundId} is visible
    - npm run test:operator-browser: inbound detail pane test passes; delivery row indices 0–3 tests pass; no new failures introduced (pre-existing line 104 failure remains excluded per D-10)
    - mix verify.preview exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundary exists in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; all changes are raw SQL seed helpers in a test support file and a Playwright spec edit. No registry audit required. |

**Security enforcement finding:** This plan is NOT APPLICABLE for security enforcement. All changes are to test support files (`operator_fixtures.ex`) and a Playwright spec (`operator.spec.js`). No production code paths are modified. No authentication, authorization, input validation, or cryptographic operations are touched. Seeded records use synthetic demo identifiers. ASVS categories V2/V3/V4/V5/V6 do not apply. No high-severity threats → does not block.
</threat_model>

<verification>
After the task in this plan:

1. `cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes
2. `cd /Users/jon/projects/mailglass/mailglass_admin && npm run test:operator-browser` — all tests pass except the known pre-existing line 104 failure; the inbound detail pane test runs and passes; no delivery row index regressions
3. Confirm `test.skip` is absent from operator.spec.js (grep: `grep -c 'test.skip' mailglass_admin/e2e/operator.spec.js` returns 0)
4. Confirm inbound record testid in spec matches DOM: `grep "inbound-record-row" mailglass_admin/e2e/operator.spec.js` returns at least one match
5. `cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.preview` exits 0 (data-only + spec-only change — priv/static/ bit-identical)
</verification>

<success_criteria>
- operator_fixtures.ex seeds one inbound record + evidence + execution run for browser-tenant at received_at: hours_ago(10) or older (SEED-01 / D-01 / GAP-13)
- operator_fixtures.ex reset! includes inbound tables in the TRUNCATE (no orphan rows on re-run)
- operator.spec.js test.skip removed from the inbound detail pane test (SEED-02 / D-09)
- operator.spec.js inbound test passes: navigates to /ops/mail/inbound?tenant_id=browser-tenant, clicks first inbound row, asserts #inbound-detail-{inbound_id} visible
- Delivery row indices 0–3 unchanged: selected=0 (hours_ago(1)), noop=1 (hours_ago(2)), ambiguous=2 (hours_ago(2)), exact=3 (hours_ago(2)); new inbound record at hours_ago(10) does not affect delivery list (separate table, separate sort) (D-07)
- Pre-existing operator.spec.js:104 failure not introduced or altered (D-10)
- reference/demo_app/mix.exs and reference/host_app/mix.exs version pins unchanged (D-09)
- mix verify.preview exits 0 (bundle clean)
</success_criteria>

<output>
Create `.planning/phases/78-seed-data-expressiveness/78-02-SUMMARY.md` when done.
</output>
