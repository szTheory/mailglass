---
phase: 78-seed-data-expressiveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/lib/mailglass_demo/demo_data.ex
autonomous: true
requirements:
  - SEED-01
  - SEED-02

must_haves:
  truths:
    - "Every event-timeline badge atom is exercisable: :queued, :sent, :rejected, :failed, :bounced, :deferred, :delivered, :autoresponded, :opened, :clicked, :complained, :unsubscribed, :subscribed, :unknown"
    - "All 6 inbound outcome badges are seeded: :no_match, :accept, :reject, :bounce, :ignore, :failed"
    - "The orphan-backlog Tier-1 support card renders: orphan_backlog.count > 0"
    - "The failed-ingest Tier-1 support card renders: failed_ingest.count > 0"
    - "Replay-outcomes Tier-1 card renders all 3 branches: replayed/noop/failed"
    - "Reconcile-facts Tier-2 shows BOTH branches: reconciled_count > 0 AND still_unmatched_count > 0"
    - "Truncation stress rows are present: recipient local-part >= 80 chars, inbound subject >= 150 chars"
    - "Empty-result state is reachable via a sibling tenant with zero rows"
    - "demo.spec.js continues to pass: reset endpoint ok(), >= 1 delivery row, >= 1 inbound row, AccountMailer visible"
  artifacts:
    - path: "reference/demo_app/lib/mailglass_demo/demo_data.ex"
      provides: "Northstar breadth seed: 8 missing event-type deliveries, 2 missing inbound outcomes, support-card branches, truncation stress rows, empty-tenant constant"
      contains:
        - "event!(*, :queued,"
        - "event!(*, :rejected,"
        - "event!(*, :autoresponded,"
        - "event!(*, :opened,"
        - "event!(*, :clicked,"
        - "event!(*, :complained,"
        - "event!(*, :unsubscribed,"
        - "event!(*, :subscribed,"
        - "event!(*, :webhook_replay_succeeded,"
        - "event!(*, :webhook_replay_failed,"
        - "event!(*, :reconciled,"
        - "needs_reconciliation: true"
        - "@empty_tenant"
        - "execution_failure:"
        - ":ignore"
  key_links:
    - from: "demo_data.ex seed_outbound!"
      to: "Event.changeset/1 via event!/4"
      via: "Repo.insert!"
      pattern: "event!/4"
    - from: "demo_data.ex seed_inbound!"
      to: "InboundRecords.insert_execution_run/1"
      via: "execution_failure: key"
      pattern: "execution_failure:"
    - from: "demo_data.ex reset!"
      to: "POST /demo/evidence/reset → DemoData.reset!()"
      via: "demo.spec.js beforeEach"
      pattern: "reset!"
---

<objective>
Expand demo_data.ex with the northstar-tenant breadth seed: the 8 missing Anymail event-type
deliveries, the 2 missing inbound outcomes (:ignore and :failed), all 4 currently-absent
support-card branches (orphan backlog, failed-ingest, replay outcomes × 3, reconcile-facts × 2),
truncation stress rows, and the empty-tenant constant.

Purpose: Make every screen state in the admin dashboard reachable by a seeded URL per SEED-01
(GAP-13 zero-state, GAP-16 empty states). demo.spec.js requires zero assertion changes because
its invariants are minimum-count not exact-count (SEED-02 satisfied without spec edits).

Output: Modified demo_data.ex with ~60–80 additional lines of seed data across seed_outbound!
and seed_inbound! functions, plus an @empty_tenant module attribute and the public empty_tenant_id/0
function. No new files. No bundle rebuild. No schema changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/78-seed-data-expressiveness/78-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/78-seed-data-expressiveness/78-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/78-seed-data-expressiveness/78-VALIDATION.md
@/Users/jon/projects/mailglass/.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand seed_outbound! with 8 missing event-type deliveries + support-card branch seeds</name>

  <read_first>
    - reference/demo_app/lib/mailglass_demo/demo_data.ex — full file: @now timestamp, existing delivery!/1, event!/4, webhook!/6 builders, minutes_ago/1 helper, seed_outbound! body, seed_inbound! body, truncate! table list
    - lib/mailglass/operator/support_summary.ex — summarize_tenant/1, failed_ingest_summary (queries WebhookEvent.status in [:failed, :dead] using @default_window_hours 24), orphan_backlog_summary (needs_reconciliation: true, delivery_id: nil, NOT EXISTS reconciled_from_event_id), reconcile_facts_summary (reconciled_count via :reconciled events, still_unmatched via unresolved_orphans_query), replay_outcomes_summary (:webhook_replay_succeeded + metadata["outcome"], :webhook_replay_failed)
    - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex — Tier-1 conditions (failed_ingest.count > 0, orphan_backlog.count > 0, replay_outcomes nonzero), Tier-2 conditions (zero-state compact row), reconcile_facts branch conditions
    - lib/mailglass/events/event.ex — @anymail_event_types list (confirm :queued, :rejected, :autoresponded, :opened, :clicked, :complained, :unsubscribed, :subscribed, :unknown, :reconciled, :webhook_replay_succeeded, :webhook_replay_failed all accepted by Event.changeset/1)
    - lib/mailglass/webhook/reconciler.ex:161-170 — canonical :reconciled event metadata shape (reconciled_from_event_id, reconciled_provider, reconciled_provider_event_id)
    - lib/mailglass/operator/deliveries.ex:29-33 — delivery sort order (desc: last_event_at, inserted_at, id) confirming minutes_ago(120+) rows sort after existing rows (newest existing last_event_at is minutes_ago(5) for incident_update)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex:44 — truncate class on recipient column
  </read_first>

  <action>
  Append to seed_outbound! in demo_data.ex (after the incident_update block, before the end of the function) — insert all new breadth rows at minutes_ago(130+) so they sort below existing rows.

  Gap-register citation: GAP-13 (support-card zero-state hierarchy), GAP-16 (empty states / missing badge atoms). Per D-03, seed Event rows of each missing type.

  Sequence of additions (all new deliveries use provider_message_id prefixes like "pm-demo-badge-TYPENAME-001" and minutes_ago offsets >= 130, spread by 2 minutes per delivery group):

  1. MISSING EVENT-TYPE DELIVERIES (8 deliveries + events). Each delivery gets a minimal 2-event lifecycle (sent + the target type). Use delivery!/1 with status: :dispatched or :failed as appropriate, provider: "postmark" or "sendgrid" alternating. Assign strictly decreasing last_event_at values starting at minutes_ago(132) and stepping back by 2 per delivery:

     a. :queued delivery — last_event_at: minutes_ago(132). Event: event!(d, :queued, minutes_ago(133), %{"provider" => "postmark", "source" => "api"}). Represents a message accepted but not yet relayed.

     b. :rejected delivery — last_event_at: minutes_ago(134). Events: event!(d, :sent, ...), event!(d, :rejected, minutes_ago(134), %{"provider" => "postmark", "classification" => "domain_block"}). Status :failed.

     c. :autoresponded delivery — last_event_at: minutes_ago(136). Events: event!(d, :sent, ...), event!(d, :autoresponded, minutes_ago(136), %{"provider" => "sendgrid", "classification" => "out_of_office"}). Status :sent.

     d. :opened delivery — last_event_at: minutes_ago(138). Events: event!(d, :delivered, ...), event!(d, :opened, minutes_ago(138), %{"provider" => "postmark", "user_agent" => "Outlook/16.0"}). Status :sent.

     e. :clicked delivery — last_event_at: minutes_ago(140). Events: event!(d, :delivered, ...), event!(d, :clicked, minutes_ago(140), %{"provider" => "postmark", "url" => "https://demo.example/cta"}). Status :sent.

     f. :complained delivery — last_event_at: minutes_ago(142). Events: event!(d, :delivered, ...), event!(d, :complained, minutes_ago(142), %{"provider" => "sendgrid", "classification" => "spam_complaint"}). Status :failed.

     g. :unsubscribed delivery — last_event_at: minutes_ago(144). Events: event!(d, :delivered, ...), event!(d, :unsubscribed, minutes_ago(144), %{"provider" => "sendgrid", "classification" => "list_unsubscribe"}). Status :sent.

     h. :subscribed delivery — last_event_at: minutes_ago(146). Event: event!(d, :subscribed, minutes_ago(146), %{"provider" => "sendgrid", "classification" => "list_subscribe"}). Note: :subscribed renders as badge-outline fallback per UI-SPEC Conflict 1 — this is correct and expected.

     i. :unknown delivery — last_event_at: minutes_ago(148). Event: event!(d, :unknown, minutes_ago(148), %{"provider" => "postmark", "raw_type" => "ProprietaryEvent"}). Renders badge-outline.

  2. TRUNCATION STRESS DELIVERY (per D-08). One delivery with recipient local-part of exactly 82 chars: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa@northstar-stress.example" (adjust count to get exactly 82 'a' chars before @). last_event_at: minutes_ago(150). Events: event!(d, :sent, ...), event!(d, :delivered, minutes_ago(150), ...). mask_recipient/1 preserves length so the truncate column will overflow.

  3. REPLAY-OUTCOME EVENTS (per D-05). Attach to the receipt delivery (already seeded, has a webhook). Add three event!/4 calls on any existing delivery (use receipt for the first two, usage_alert for the third):
     - event!(receipt, :webhook_replay_succeeded, minutes_ago(121), %{"provider" => "postmark", "outcome" => "replayed", "webhook_event_id" => "demo-replay-wh-001"})
     - event!(receipt, :webhook_replay_succeeded, minutes_ago(122), %{"provider" => "postmark", "outcome" => "noop", "webhook_event_id" => "demo-replay-wh-002"})
     - event!(usage_alert, :webhook_replay_failed, minutes_ago(123), %{"provider" => "sendgrid", "failure_reason" => "webhook_event_not_found"})

  4. ORPHAN EVENTS (per D-06). Two distinct orphan events for both reconcile-facts branches. These call Event.changeset/1 directly (not event!/4 which requires a delivery). Insert via Repo.insert!:

     Orphan A (reconciled — will be pointed to by a :reconciled event):
     - tenant_id: @tenant, delivery_id: nil, type: :sent, occurred_at: minutes_ago(128), needs_reconciliation: true
     - idempotency_key: "demo-orphan-reconciled-001"
     - metadata: %{"provider" => "sendgrid", "provider_event_id" => "sg-orphan-a", "webhook_event_id" => "00000000-0000-0000-0000-000000000001", "provider_message_id" => "sg-orphan-msg-001"}
     - normalized_payload: %{}
     Capture as orphan_a = ...

     Orphan B (still unmatched — no :reconciled event will point to it):
     - tenant_id: @tenant, delivery_id: nil, type: :sent, occurred_at: minutes_ago(129), needs_reconciliation: true
     - idempotency_key: "demo-orphan-unmatched-001"
     - metadata: %{"provider" => "sendgrid", "provider_event_id" => "sg-orphan-b", "webhook_event_id" => "00000000-0000-0000-0000-000000000002", "provider_message_id" => "sg-orphan-msg-002"}
     - normalized_payload: %{}

     :reconciled event (per D-06 / reconciler.ex:161-170 canonical shape) — attach to any existing delivery (use invite):
     - tenant_id: @tenant, delivery_id: invite.id, type: :reconciled, occurred_at: minutes_ago(127)
     - idempotency_key: "reconciled:" <> to_string(orphan_a.id)
     - metadata: %{"reconciled_from_event_id" => to_string(orphan_a.id), "reconciled_provider" => "sendgrid", "reconciled_provider_event_id" => "sg-demo-reconciled-event-001"}
     - normalized_payload: %{}
     This makes reconcile_facts.reconciled_count > 0. Orphan B remains unmatched (still_unmatched_count > 0).

  5. FAILED-INGEST WEBHOOK (per support-card analysis — requires WebhookEvent with status: :failed). Call webhook!/6 with a status override:
     - Use the webhook! builder but override status to :failed (or pass "failed" string — builder stringifies atoms). Add one more clause to webhook! or call WebhookEvent.changeset directly:
       %{tenant_id: @tenant, provider: "sendgrid", provider_event_id: "sg-demo-failed-ingest-001", event_type_raw: "Bounce", event_type_normalized: "bounced", status: :failed, raw_payload: %{"RecordType" => "Bounce", "error" => "parse_failure"}, received_at: minutes_ago(124), processed_at: minutes_ago(124)} |> WebhookEvent.changeset() |> Repo.insert!()
     Note: The current webhook! builder always sets status: :succeeded. Instead of modifying the private builder signature, insert directly via WebhookEvent.changeset/1 inline — match the existing webhook!/6 pattern.

  6. EMPTY-TENANT constant. Add at the top of the module (near @tenant):
     @empty_tenant "empty-tenant"
     def empty_tenant_id, do: @empty_tenant
     No rows are seeded for the empty tenant. All operator queries are tenant-scoped so navigating with ?tenant_id=empty-tenant reaches every empty/zero state (empty deliveries list, empty inbound list, all-zero support cards) without any data.

  All new rows must be within the @default_window_hours 24 window — minutes_ago(120..150) is 2–2.5 hours, well within the 24-hour window (per RESEARCH Pitfall 5).

  seed_outbound! must bind orphan_a before the :reconciled event call and bind invite before seed_outbound! ends (invite is already bound at the top of seed_outbound!; make sure invite is accessible in scope when :reconciled event is inserted — move the reconcile block to after invite is bound, or capture invite.id earlier).

  Do NOT modify mix.exs version pins (D-09). Do NOT modify demo.spec.js (its invariants are minimum-count; breadth-only additions never reduce rows below 1 so no assertion value changes are needed — SEED-02 satisfied).
  </action>

  <verify>
    <automated>
    # From reference/demo_app/ — compile check first, then ExUnit, then e2e
    cd /Users/jon/projects/mailglass/reference/demo_app && mix compile --warnings-as-errors
    cd /Users/jon/projects/mailglass/reference/demo_app && mix test test/mailglass_demo/ 2>/dev/null || true
    </automated>
    <human-check>
    Run from mailglass_admin/: mix test test/mailglass_admin/operator_live_test.exs
    Confirm: test output shows Tier-1 support cards present (failed_ingest, orphan_backlog, replay_outcomes nonzero).
    Run from reference/demo_app/assets/: npm run test:e2e
    Confirm: all 3 demo tests pass (reset ok, AccountMailer visible, delivery row + inbound row exist).
    </human-check>
  </verify>

  <done>
    - demo_data.ex compiles without warnings
    - 8 missing event-type deliveries present: :queued, :rejected, :autoresponded, :opened, :clicked, :complained, :unsubscribed, :subscribed (+ :unknown) each with at least one event!/4 call of that type
    - 3 replay-outcome events present: :webhook_replay_succeeded with outcome="replayed", :webhook_replay_succeeded with outcome="noop", :webhook_replay_failed
    - 2 orphan events present (needs_reconciliation: true, delivery_id: nil) — orphan A pointed to by a :reconciled event; orphan B unmatched
    - 1 WebhookEvent with status: :failed inserted (failed-ingest branch)
    - Truncation stress delivery present with recipient local-part >= 80 chars
    - @empty_tenant "empty-tenant" module attr + empty_tenant_id/0 function present
    - mix test operator_live_test.exs green (support-card Tier-1 branches render for orphan_backlog, failed_ingest, replay_outcomes; reconcile_facts shows both branches)
    - npm run test:e2e green (3/3 tests pass; no assertion value changes were needed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand seed_inbound! with :ignore and :failed outcome rows + truncation stress inbound record</name>

  <read_first>
    - reference/demo_app/lib/mailglass_demo/demo_data.ex — inbound_run!/6 builder (outcome, outcome_reason, mailbox params), inbound_record!/1 builder, inbound_evidence!/2 builder, seed_inbound! body
    - mailglass_inbound/lib/mailglass_inbound/inbound_records.ex:69-101 — normalize_execution_attrs/1: execution_failure key path for :failed outcome; :ignore outcome requires mailbox present string + failure: %{}
    - mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex — validate_outcome_shape/1: :ignore requires present_string?(mailbox) and map_size(failure) == 0; :failed requires map_size(failure) > 0
    - mailglass_admin/lib/mailglass_admin/components.ex:126-129 — normalize_inbound_outcome/1: :accept→:accepted, :ignore→:ignore, :reject→:rejected, :bounce→:bounced, :failed→:failed_ingest; confirm :ignore maps to badge atom :ignore
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex:50 — truncate class on subject column
    - reference/demo_app/lib/mailglass_demo/demo_data.ex — @now fixed timestamp base for minutes_ago/1 (records inserted via inbound_record!/1 use received_at: minutes_ago(5) default — update to minutes_ago for new inbound records so they sort correctly: inbound sort is desc: received_at, inserted_at, so new records at received_at: minutes_ago(30) and minutes_ago(35) will appear below existing records at minutes_ago(5))
  </read_first>

  <action>
  Append to seed_inbound! in demo_data.ex (after the no_match block, before the end of the function) — per D-04.

  Gap-register citation: GAP-16 (empty states — inbound outcome badges :ignore and :failed_ingest not reachable without these seeds).

  1. :ignore inbound outcome record (per D-04, Pattern 6 in RESEARCH). The :ignore outcome requires: mailbox present string, failure: %{}, no outcome_reason. The existing inbound_run!/6 builder passes outcome_reason as the 6th arg; for :ignore pass nil for outcome_reason and pass "MailglassDemoWeb.Inbound.SpamMailbox" as mailbox. The normalize_execution_attrs/1 in InboundRecords does NOT normalize :ignore from a special key — pass outcome: :ignore directly via the inbound_run!/6 builder which calls insert_execution_run with the attrs including outcome: :ignore and mailbox: "MailglassDemoWeb.Inbound.SpamMailbox".

     Insert record:
     - subject: "RE: Your request has been noted" (plausible auto-ignore subject)
     - from: %{"address" => "autoresponder@partner-crm.example"}
     - envelope_recipient: "support@demo.mailglass.local"
     - received_at: minutes_ago(30) — overrides the default minutes_ago(5) so this record sorts after existing records (inbound sort is desc: received_at so minutes_ago(30) < minutes_ago(5) in the list order, meaning it appears lower)
     - Override inbound_record!/1 call to pass received_at explicitly: merge %{received_at: minutes_ago(30)} into the attrs map before calling InboundRecords.insert_inbound_record

     Note: inbound_record!/1 hardcodes received_at: minutes_ago(5) in its defaults. Either:
     (a) Call InboundRecords.insert_inbound_record directly with received_at: minutes_ago(30), or
     (b) Pass received_at: minutes_ago(30) in the attrs — check if inbound_record!/1 merges attrs OVER defaults or defaults OVER attrs. Looking at the code: Map.merge(attrs, defaults) — this means defaults WIN over attrs keys. Therefore pass received_at via a second argument, or call InboundRecords.insert_inbound_record directly for the new records with explicit received_at.
     Use InboundRecords.insert_inbound_record directly (not the private inbound_record!/1 wrapper) for the two new records so received_at can be controlled.

     Execution run: inbound_run!(ignore_record, ignore_evidence, :fresh, :ignore, "MailglassDemoWeb.Inbound.SpamMailbox")

  2. :failed inbound outcome record (per D-04, Pattern 5 in RESEARCH). The :failed outcome requires execution_failure: %{non-empty map} passed to insert_execution_run (NOT via inbound_run! builder which doesn't accept execution_failure). Use InboundRecords.insert_execution_run directly:

     Record:
     - subject: A subject of >= 150 chars for truncation stress. Example: "This is a very long subject line for a vendor notification that was not expected and has exceeded the maximum subject line length that the truncation column can display without ellipsis overflow occurring here" (count to ensure >= 150 chars)
     - from: %{"address" => "noreply@vendor-platform.example"}
     - envelope_recipient: "support@demo.mailglass.local"
     - received_at: minutes_ago(35)
     Use InboundRecords.insert_inbound_record with explicit received_at.

     Evidence: InboundRecords.insert_inbound_evidence with standard demo facts.

     Execution run via direct InboundRecords.insert_execution_run call (not inbound_run!/6):
     {:ok, _run} = InboundRecords.insert_execution_run(%{
       tenant_id: @tenant,
       inbound_record_id: failed_record.id,
       inbound_evidence_id: failed_evidence.id,
       source: :fresh,
       executed_at: minutes_ago(36),
       metadata: %{"demo" => true},
       execution_failure: %{"reason" => "parse_error", "provider" => "mailgun"}
     })

     This uses the execution_failure: key which normalize_execution_attrs/1 maps to outcome: :failed + failure: map, satisfying validate_outcome_shape/1 (failure map_size > 0).

     This record also serves as the truncation stress inbound row (subject >= 150 chars), satisfying D-08's inbound subject requirement in the same record.

  The two new inbound records are NOT inserted via inbound_record!/1 (which hardcodes received_at). Use the InboundRecords module API directly. The demo_data.ex already aliases MailglassInbound.InboundRecords — no new alias needed.

  Do NOT use inbound_run!/6 for the :failed outcome (that builder doesn't pass execution_failure). Call insert_execution_run directly.
  </action>

  <verify>
    <automated>
    cd /Users/jon/projects/mailglass/reference/demo_app && mix compile --warnings-as-errors
    </automated>
    <human-check>
    Run: mix test test/mailglass_admin/operator_live_test.exs from mailglass_admin/
    Run: npm run test:e2e from reference/demo_app/assets/
    Confirm: inbound-record-row.first() exists in demo, :ignore and :failed inbound outcome badges reachable via northstar tenant inbound list, truncation stress inbound subject >= 150 chars present.
    </human-check>
  </verify>

  <done>
    - demo_data.ex compiles without warnings after both Task 1 and Task 2 changes
    - seed_inbound! has at least 6 inbound records covering all outcomes: :no_match, :accept, :reject, :bounce, :ignore, :failed
    - :ignore record present: inbound_run!/6 call with outcome: :ignore, mailbox: "MailglassDemoWeb.Inbound.SpamMailbox"
    - :failed record present: InboundRecords.insert_execution_run call with execution_failure: %{"reason" => "parse_error"...}
    - truncation stress inbound subject >= 150 chars present
    - npm run test:e2e: 3/3 tests pass (inbound-record-row.first() still exists — there are now >= 6 inbound records)
    - mix verify.preview from mailglass_admin/ exits 0 (data-only change — priv/static/ bit-identical)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundary exists in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; all changes are pure Elixir seed data edits to existing files. No registry audit required. |

**Security enforcement finding:** This plan is NOT APPLICABLE for security enforcement. All changes are read-only seed-data inserts to test/dev seed files (`demo_data.ex`). No authentication, authorization, input validation, or cryptographic operations are touched. All seeded recipients, subjects, and identifiers are synthetic demo data — not real PII. ASVS categories V2/V3/V4/V5/V6 do not apply. No high-severity threats → does not block.
</threat_model>

<verification>
After both tasks in this plan:

1. `cd /Users/jon/projects/mailglass/reference/demo_app && mix compile --warnings-as-errors` exits 0
2. `cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes — support-card Tier-1 branches render for orphan_backlog, failed_ingest, replay_outcomes; reconcile_facts shows both reconciled and still_unmatched branches
3. `cd /Users/jon/projects/mailglass/reference/demo_app/assets && npm run test:e2e` — 3/3 tests pass (minimum-count invariants satisfied)
4. `cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.preview` exits 0 — data-only change confirmed bundle bit-identical
5. Verify `@empty_tenant "empty-tenant"` and `empty_tenant_id/0` present in demo_data.ex
6. Verify demo.spec.js is UNCHANGED (no edits needed — SEED-02 satisfied without spec changes)
</verification>

<success_criteria>
- demo_data.ex seeds all 14 Anymail event-type badges (8 new: :queued, :rejected, :autoresponded, :opened, :clicked, :complained, :unsubscribed, :subscribed; plus :unknown — SEED-01 / GAP-16)
- All 6 inbound outcomes seeded: :no_match, :accept, :reject, :bounce, :ignore, :failed (SEED-01 / GAP-16)
- Support-card Tier-1 branches reachable: orphan_backlog, failed_ingest, replay_outcomes × 3, reconcile_facts × 2 branches (SEED-01 / GAP-13)
- Truncation stress rows: recipient local-part >= 80 chars in seed_outbound!, inbound subject >= 150 chars in seed_inbound! (SEED-01 / D-08)
- Empty-tenant constant exported as empty_tenant_id/0 (SEED-01 / D-02)
- demo.spec.js UNCHANGED — no assertion values modified (SEED-02 / D-09)
- reference/demo_app/mix.exs and reference/host_app/mix.exs version pins UNCHANGED (D-09)
- mix verify.preview exits 0 (no bundle rebuild needed — data-only change)
</success_criteria>

<output>
Create `.planning/phases/78-seed-data-expressiveness/78-01-SUMMARY.md` when done.
</output>
