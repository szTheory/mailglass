---
phase: 161-canonical-workspace-and-evidence-preservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md
autonomous: true
requirements: [WSPC-01, WSPC-02]
must_haves:
  truths:
    - "D-01/D-04: One tracked pre-mutation ledger names the canonical root and every Git-registered linked worktree."
    - "D-05/D-06: The canonical root is recorded as clean `main` with a timestamped live upstream count, the fixed seven-commit v2.6/v2.7 semantic range, and an explicit non-release-clean verdict until upstream settlement."
    - "WSPC-01 adjacency/order: equal object IDs or overlapping ranges retain separate identity rows, ordered by category and bytewise identity, while shared evidence may be cross-referenced."
    - "WSPC-01 empty-input: every enumerator records an explicit zero count and `none found` result when its category is empty; omission never means empty."
    - statement: "WSPC-01 coverage is complete only when fresh worktree, stash, ref, divergent-range, release-leftover, and selected unreachable-object outputs reconcile to ledger rows."
      verification: backstop
  artifacts:
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      provides: "Timestamped pre-mutation Git/workspace evidence ledger and canonical-main explanation"
      contains: "Pre-Mutation Snapshot"
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md"
      provides: "Executable requirement-to-evidence sampling contract"
      contains: "Per-Task Verification Map"
  key_links:
    - from: "git worktree list --porcelain -z"
      to: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      via: "one WT row per porcelain worktree record"
      pattern: "WT-"
    - from: "git rev-list --left-right --count '@{upstream}...HEAD'"
      to: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      via: "timestamped canonical-main divergence evidence"
      pattern: "Canonical Main"
  assumptions:
    - "FLAGGED (WSPC-02 unclassified): the live ahead count is capture-time evidence and will change as Phase 161 artifacts are committed; the immutable claim is the identified seven-commit semantic subrange, not a permanently fixed total count."
  prohibitions:
    - statement: "MUST NOT present missing, unreadable, or stale Git evidence as an empty or settled category."
      status: resolved
      verification: judgment
    - statement: "MUST NOT portray canonical `main` as release-clean merely because its working tree is clean."
      status: resolved
      verification: judgment
---

<objective>
Create the production pre-mutation evidence tracer: one ledger that proves the canonical `main` path and spans every local Git/workspace evidence category before any cleanup command is eligible.

Purpose: Satisfy WSPC-01 and WSPC-02 while implementing D-01 and D-03 through D-06 with a reproducible, reviewable baseline.
Output: `161-WORKSPACE-INVENTORY.md` and a finalized Wave-0 validation map.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-PATTERNS.md
@MAINTAINING.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
@.planning/release-target.json
@.planning/milestones/v2.6-phases/160-certification-documentation-and-release/160-06-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Capture canonical `main` and the complete linked-worktree path end to end</name>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md (create if absent; otherwise preserve prior captured evidence)
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PATTERNS.md
    - MAINTAINING.md
    - dev/mix/tasks/mailglass.repo.hygiene.ex
  </read_first>
  <action>From `/Users/jon/projects/mailglass`, first record `git rev-parse HEAD`, UTC capture time, and a phase-base marker; do not run a state-changing Git command. Create the diffable ledger with schema columns `ID`, `category`, `identity/path`, `observed state`, `content/unique-work evidence`, `reachability evidence`, `evidence ref`, `disposition`, `preservation/handoff`, `permitted next action`, and `outcome`. Per D-04, make `/Users/jon/projects/mailglass` on `main` the sole `CANONICAL` row. Capture fixed-argument outputs for `git status --short --branch`, `git branch --show-current`, `git rev-parse --abbrev-ref --symbolic-full-name '@{upstream}'`, `git rev-list --left-right --count '@{upstream}...HEAD'`, and `git log --left-right --cherry-mark --oneline '@{upstream}...HEAD'`. Per D-05, identify the locked seven-commit v2.6 archive/retrospective and v2.7 initialization subrange separately from later planning commits and report the live capture-time total rather than rewriting history. Per D-06, label the root non-release-clean while any upstream drift remains. Run `git worktree list --porcelain -z`, decode each NUL-delimited record without reading `.git` internals, then run quoted `git -C <exact-path> status --short --branch` and `git -C <exact-path> rev-parse HEAD` for every record. Emit one `WT-*` row per registered worktree, including clean/dirty, branch/detached, locked/prunable flags, and exact HEAD. Include full command, concise captured output, exit code, and capture time beneath the matrix.</action>
  <verify>
    <automated>test -f .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md &amp;&amp; test "$(git worktree list --porcelain | rg -c '^worktree ')" -eq "$(rg -c '^\| WT-[^|]+\|' .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md)" &amp;&amp; rg -n 'CANONICAL|Canonical Main|non-release-clean|seven-commit|git worktree list --porcelain -z' .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - The inventory contains exactly one `CANONICAL` row naming `/Users/jon/projects/mailglass`, branch `main`, upstream, live ahead/behind values, capture HEAD/time, and non-release-clean verdict per D-04/D-06.
    - The locked seven-commit semantic range is listed by commit identity and distinguished from later Phase 161 commits per D-05.
    - The number of `WT-*` rows equals the fresh porcelain worktree count, and every path has status plus HEAD evidence.
    - No cleanup, ref creation, checkout, reset, merge, removal, prune, or force operation occurs in this task.
  </acceptance_criteria>
  <done>The canonical root and every registered worktree are reproducibly represented in a committed pre-mutation ledger, proving the phase architecture from Git porcelain to durable evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the snapshot across stash, ref, range, release, and object categories</name>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md, .planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - MAINTAINING.md
    - .planning/release-target.json
    - .planning/milestones/v2.6-phases/160-certification-documentation-and-release/160-06-SUMMARY.md
  </read_first>
  <action>Complete D-01 before any cleanup by appending stable, category-scoped evidence in deterministic category/bytewise-identity order. Capture every stash with `git stash list --format='%gd%x00%H%x00%P%x00%gs'` plus quoted `git stash show --stat --include-untracked <selector>` and patch-summary evidence. Capture relevant `refs/heads`, `refs/remotes`, and `refs/tags` with `git for-each-ref --format='%(refname)%00%(objectname)%00%(upstream)%00%(upstream:track)'`, then record symmetric counts and cherry-marked logs for each release/archive/divergent range against canonical `main`; equal OIDs remain separate ref rows. Inventory local release proof individually: `.planning/release-target.json`, every tracked `.planning/publish/*-publish-summary.json`, the Phase 160 release summary, and modified copies in linked worktrees, recording hashes/status/diff references but handing remote interpretation to Phase 162 per D-09. Run `git fsck --no-reflogs --unreachable --no-dangling`; record the command's total counts by object type and create `OBJ-*` rows for every unreachable commit plus any tree/blob needed to explain an inventoried dirty/stash/release candidate. Per D-03/D-07, mark all dirty, stash, detached, unreachable, and uncertain items `assessment pending` with `retain` or `handoff`, never `remove`. For any empty category, add an explicit zero-count `NONE-*` row and captured successful output. Update `161-VALIDATION.md` so finalized task IDs/waves match Plans 01-04, Wave 0 points at this completed schema/snapshot, and `wave_0_complete` becomes true only after the reconciliation commands pass.</action>
  <verify>
    <automated>inventory=.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md; test "$(git stash list | wc -l | tr -d ' ')" -eq "$(rg -c '^\| STASH-[^|]+\|' "$inventory")" &amp;&amp; rg -n 'git for-each-ref|git fsck --no-reflogs --unreachable --no-dangling|release-target.json|publish-summary.json|Phase 162|NONE-' "$inventory" &amp;&amp; rg -n '161-01-01|161-01-02|wave_0_complete: true' .planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - WSPC-01/D-01 categories each have fresh command output, a count, and rows or an explicit `NONE-*` zero row.
    - Every current stash has one `STASH-*` row with selector, OID, parents, content summary, and evidence reference.
    - Relevant local/remote refs and each divergent range have separate stable-ordered rows even when OIDs or evidence are equal.
    - Every unreachable commit reported at capture has an `OBJ-*` row; additional trees/blobs are selected only when needed to explain an inventoried candidate.
    - Release ledgers and publish summaries are classified as evidence and unresolved remote meaning is handed to Phase 162 per D-09.
    - Dirty, detached, stash, unreachable, and uncertain rows are not cleanup-eligible before Plan 02 assessment.
  </acceptance_criteria>
  <done>The tracked pre-mutation snapshot covers every WSPC-01 category, handles empty/equal/order edges explicitly, and maps validation to the final execution plan.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md` — ledger schema, canonical-main record, `WT-*`, `STASH-*`, `REF-*`, `RANGE-*`, `REL-*`, `OBJ-*`, and explicit `NONE-*` contracts.
- `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-VALIDATION.md` — finalized Plan 01-04 sampling map and Wave-0 completion state.
- Contract: capture-time facts are immutable evidence blocks; later recaptures append a new timestamp/HEAD rather than silently rewriting old results.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem/Git metadata → ledger | Local paths, refs, object IDs, and command output cross into durable evidence. |
| linked worktree → canonical root | Dirty or detached state outside the root must be represented without executing it or treating it as disposable. |
| local release proof → Phase 162 handoff | Local files are evidence; remote meaning remains outside this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-01 | Tampering / Denial of Service | unique local work | high | mitigate | D-03/D-07 classify dirty, detached, stash, and unreachable candidates as preservation candidates before mutation. |
| T-161-02 | Tampering | inventory completeness | high | mitigate | Reconcile stable porcelain/ref/fsck counts to one row per identity and explicit zero rows. |
| T-161-03 | Repudiation | release proof | medium | mitigate | Hash and inventory each ledger/summary separately; route remote interpretation to Phase 162. |
| T-161-04 | Tampering | shell/ref parsing | medium | mitigate | Use fixed quoted arguments, NUL-delimited porcelain, and no direct `.git` parsing. |
</threat_model>

<verification>
Run the Plan 01 automated checks, then compare every fresh category count with the corresponding ledger row count. Confirm `git status --short --branch` remains clean apart from the committed planning artifacts and that no cleanup mutation occurred before the inventory commit.
</verification>

<success_criteria>
- WSPC-01 and WSPC-02 are represented by a committed, reproducible pre-mutation snapshot.
- D-01 and D-03 through D-07 are visible in row states and canonical-main semantics.
- Every applicable fallback edge has a truth/acceptance criterion; WSPC-02's unclassified live-count ambiguity remains explicitly flagged.
</success_criteria>

<output>
Create `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-01-SUMMARY.md` when done.
</output>
