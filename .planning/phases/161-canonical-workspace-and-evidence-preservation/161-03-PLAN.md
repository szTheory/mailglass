---
phase: 161-canonical-workspace-and-evidence-preservation
plan: 03
type: execute
wave: 3
depends_on: [161-02]
files_modified:
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv
  - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-verify-preservation-reconciliation.sh
autonomous: true
requirements: [WSPC-04]
must_haves:
  truths:
    - "D-08: Every unique or uncertain candidate selected for later removal resolves through a named `preserve/*` ref or a concrete documented handoff before cleanup begins."
    - "D-07: Retained and handed-off items remain reachable in their recorded location; preservation never silently consumes a stash or release artifact."
    - statement: "WSPC-04 preservation proof is complete only when every archive/remove prerequisite resolves to the recorded OID or committed dirty-content tree."
      verification: backstop
    - "WSPC-04 reconciliation fails closed when the eligible row set or preservation-required set is empty, a source row is missing/duplicated, a ref resolves to a different OID, or a handoff lacks concrete recovery fields."
  artifacts:
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md"
      provides: "Preservation manifest mapping candidates to recoverable refs or handoffs"
      contains: "Preservation Manifest"
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv"
      provides: "Machine-checkable one-to-one archive/remove preservation eligibility and recovery mapping"
      contains: "source_row"
    - path: ".planning/phases/161-canonical-workspace-and-evidence-preservation/161-verify-preservation-reconciliation.sh"
      provides: "Shared fail-closed partial/complete reconciliation verifier used before cleanup"
      contains: "complete"
  key_links:
    - from: "disposition preservation prerequisite"
      to: "refs/heads/preserve/* or Phase 162 handoff"
      via: "verified OID/content mapping in the preservation manifest"
      pattern: "preserve/phase-161-"
    - from: "archive/remove ledger rows"
      to: "preservation refs or concrete handoffs"
      via: "exactly one row per source identity in 161-PRESERVATION-RECONCILIATION.tsv"
      pattern: "required\t"
  assumptions:
    - "FLAGGED (WSPC-04 unclassified): an approved cleanup candidate means a row with disposition `remove`, complete `EVID-*` support, and a verified preservation/handoff prerequisite; no other notion of approval authorizes mutation."
  prohibitions:
    - statement: "MUST NOT make recoverability depend only on a temporary worktree path, reflog, or unreachable loose object that ordinary maintenance may later prune."
      status: resolved
      verification: judgment
    - statement: "MUST NOT preserve evidence by overwriting an existing ref, consuming a stash entry, or changing canonical `main` history."
      status: resolved
      verification: judgment
---

<objective>
Materialize the recoverability boundary for every unique or uncertain candidate before any approved cleanup is attempted.

Purpose: Implement WSPC-04 and D-07/D-08 using named refs or explicit handoffs while leaving canonical `main` and original evidence intact.
Output: Verified preservation refs/commits, a complete preservation manifest, and a fail-closed row-level reconciliation file.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-02-SUMMARY.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
@.planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
@MAINTAINING.md
@dev/mix/tasks/mailglass.repo.hygiene.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create collision-safe recoverable refs for ref-addressable candidates</name>
  <reversibility rating="costly">Creating preservation refs is cheap to undo, but selecting the wrong target can invalidate later cleanup evidence across multiple workspace rows.</reversibility>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md, .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv, .planning/phases/161-canonical-workspace-and-evidence-preservation/161-verify-preservation-reconciliation.sh</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - MAINTAINING.md
    - dev/mix/tasks/mailglass.repo.hygiene.ex
  </read_first>
  <action>Before mutation, assert each target row still resolves to its assessed OID and canonical `main` remains the recorded branch/HEAD apart from expected plan commits. Create `161-PRESERVATION-RECONCILIATION.tsv` with the exact tab-separated header `source_row disposition evidence_ref preservation_requirement mechanism expected_oid target observed_oid handoff_location blocking_condition permitted_next_action status`. Enumerate every non-sentinel inventory row whose disposition is `archive` or `remove` exactly once. Mark every `archive` row `required`; mark a `remove` row `required` whenever its evidence is unique or uncertain, and permit `not-required` only when its cited content plus reachability/patch evidence proves no unique recoverable value under the resolved research rule. Use `mechanism=none,status=verified` for evidence-backed `not-required` rows and `mechanism=pending,status=pending` only for required non-ref-addressable rows that Task 2 must convert to a concrete handoff or preservation commit. Create `161-verify-preservation-reconciliation.sh` as the single verifier consumed by both this plan and Plan 04. It accepts `partial|complete`, inventory path, and TSV path; validates the exact header; extracts only archive/remove item-row IDs; fails if the eligible or required count is zero; compares sorted source-ID sets for equality; rejects duplicate TSV source IDs; checks each TSV disposition against its inventory row and requires a nonempty `EVID-*` reference. In both modes it requires at least one `mechanism=ref` row, a full `refs/heads/preserve/phase-161-*` target, a full 40-hex expected/observed OID, and exact `git rev-parse <full-ref>^{commit}` equality. Partial mode additionally permits only required `pending/pending` rows and evidence-backed `not-required/none/verified` rows. Complete mode rejects pending rows, requires every required row to use exactly one `ref` or `handoff` mechanism, and requires each handoff to have a stable `HANDOFF-*` target plus nonempty location/evidence anchor, blocking condition, permitted next action, and matching concrete inventory handoff record. The script exits nonzero on every missing file, malformed row, zero-set, mismatch, or unknown value; it must not suppress command errors or supply defaults. For every required commit-addressable row, derive a descriptive collision-safe full ref `refs/heads/preserve/phase-161-<category>-<stable-slug>-<yyyymmdd>`; check nonexistence with `git show-ref --verify --quiet <full-ref>` and stop on collision rather than overwrite. Create the ref with the fixed target OID using `git branch <preserve/name> <oid>`. For stash entries, point the preservation branch at the recorded stash commit OID and leave the stash entry present. For unreachable commits, point the preservation branch at the exact recorded commit. For branch/detached HEAD candidates, point at the assessed OID. Record one `mechanism=ref` reconciliation row with the full ref, full expected OID, observed OID, and `verified` status. Append the same source row, full ref, expected OID, actual `git rev-parse <ref>`, creation command/exit, and timestamp to the Preservation Manifest. Do not move, reset, merge, delete, force-update, or push any ref; canonical `main` must remain unchanged.</action>
  <verify>
    <automated>bash .planning/phases/161-canonical-workspace-and-evidence-preservation/161-verify-preservation-reconciliation.sh partial .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv &amp;&amp; rg -n 'Preservation Manifest|expected OID|actual OID' .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - Every ref-addressable archive/remove preservation prerequisite has a unique `refs/heads/preserve/phase-161-*` ref resolving to the assessed OID.
    - The reconciliation contains exactly one row for every archive/remove ledger row, has nonzero eligible and required counts, and rejects duplicate or missing source rows.
    - Existing refs are never overwritten; a name collision stops execution and is recorded for replanning.
    - Stash entries remain listed after their commit OIDs gain preservation refs.
    - Canonical `main` branch identity and pre-task HEAD are unchanged by ref creation.
  </acceptance_criteria>
  <done>Every ref-addressable unique or uncertain candidate has a verified named recovery anchor without consuming its original representation.</done>
</task>

<task type="auto">
  <name>Task 2: Commit dirty evidence safely or bind it to a concrete handoff</name>
  <reversibility rating="costly">A preservation commit is recoverable, but committing the wrong dirty file set would create a misleading archive contract used by cleanup.</reversibility>
  <files>.planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md, .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv</files>
  <read_first>
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-verify-preservation-reconciliation.sh
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-CONTEXT.md
    - .planning/phases/161-canonical-workspace-and-evidence-preservation/161-RESEARCH.md
    - MAINTAINING.md
    - .planning/release-target.json
    - .planning/milestones/v2.6-phases/160-certification-documentation-and-release/160-06-SUMMARY.md
  </read_first>
  <action>For each dirty worktree that is a later `remove` candidate, re-run quoted status and binary diff checks and require exact agreement with its assessed `EVID-*` block. Create a collision-safe `preserve/phase-161-worktree-<stable-slug>-<yyyymmdd>` branch at its current HEAD using `git -C <exact-path> switch -c <name>`, stage only the inventoried tracked/untracked paths, verify `git diff --cached --name-status` equals the ledger path set, and commit with `chore(161): preserve <stable-slug> workspace evidence`. Record parent, commit OID, tree OID, file list, and a post-commit clean status in the Preservation Manifest, then update that source row's single TSV record to `mechanism=ref`, its full expected/observed commit OID, full ref target, and `status=verified`. If a candidate cannot be safely committed without altering unrelated content, do not clean or remove it: retain the worktree and update its single TSV record to `mechanism=handoff` with a stable `HANDOFF-*` target plus nonempty exact path/HEAD/diff-hash location, blocking condition, permitted next action, and `status=verified`; record those same concrete fields in the inventory handoff table. For release artifacts, retain or hand off their local proof and do not decide PR #222/check/tag/Hex meaning per D-09. Finish by reconciling every archive/remove row one-to-one against the TSV. Every `required` row must have exactly one exact-OID ref or one concrete handoff; every `not-required` row must cite evidence proving no unique recoverable value. Append preservation coverage totals with eligible, required, ref, handoff, not-required, and pending counts; pending must be zero.</action>
  <verify>
    <automated>bash .planning/phases/161-canonical-workspace-and-evidence-preservation/161-verify-preservation-reconciliation.sh complete .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md .planning/phases/161-canonical-workspace-and-evidence-preservation/161-PRESERVATION-RECONCILIATION.tsv &amp;&amp; rg -n 'Preservation Coverage|pending: 0' .planning/phases/161-canonical-workspace-and-evidence-preservation/161-WORKSPACE-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - Every dirty remove candidate is either cleanly committed on a named preservation branch with exact file/tree evidence or changed to retain/handoff with a concrete blocker.
    - Preservation commits include only paths enumerated in the source row's evidence block.
    - Every archive/remove row has a resolving named ref/commit or concrete handoff and preservation coverage reports pending zero.
    - Every concrete handoff has an exact location or evidence anchor, blocking condition, and permitted next action in both the TSV and inventory; headings or narrative alone cannot satisfy the gate.
    - Remote PR, checks, tags, Hex, and scheduled automation remain explicitly unresolved for Phase 162 per D-09.
    - No cleanup removal, stash drop/clear, branch deletion, reset, force action, garbage collection, or history rewrite occurs in this plan.
  </acceptance_criteria>
  <done>All unique or uncertain work that could be affected by later cleanup is independently recoverable or explicitly retained/handed off.</done>
</task>

</tasks>

## Artifacts this phase produces

- `refs/heads/preserve/phase-161-<category>-<stable-slug>-<yyyymmdd>` — collision-safe refs resolving to inventoried source OIDs.
- Preservation commits `chore(161): preserve <stable-slug> workspace evidence` for dirty remove candidates whose exact file set can be safely committed.
- Updated `161-WORKSPACE-INVENTORY.md` Preservation Manifest with source row, expected/actual OID, tree/file evidence, handoff, and zero-pending coverage.
- `161-PRESERVATION-RECONCILIATION.tsv` — one machine-checkable row per archive/remove source identity, with a fail-closed exact-OID ref, concrete handoff, or evidence-backed not-required determination.
- `161-verify-preservation-reconciliation.sh` — the single partial/complete row-level verifier reused by Plan 04 before cleanup and sign-off.
- Contract: existing refs and original stash entries are never overwritten or consumed during preservation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assessed OID → preservation ref | The ref must bind the exact assessed object, not a moving symbolic name. |
| dirty filesystem → preservation commit | Only inventoried paths may cross into the committed recovery tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-01 | Tampering / Denial of Service | unique local work | high | mitigate | Exact-OID refs and exact-file preservation commits precede cleanup eligibility. |
| T-161-02 | Tampering | inventory completeness | high | mitigate | Zero-pending preservation coverage reconciles every archive/remove row. |
| T-161-03 | Repudiation | release proof | medium | mitigate | Preserve local artifacts and name Phase 162 questions without remote conclusions. |
| T-161-04 | Tampering | shell/ref ambiguity | medium | mitigate | Full refs, exact OIDs/paths, collision checks, and fixed Git argument lists. |
</threat_model>

<verification>
Resolve every manifest ref to its expected OID, compare every preservation commit's file list/tree with its source evidence, confirm original stash entries remain, and prove canonical `main` was not reset, rewritten, merged, or force-updated.
</verification>

<success_criteria>
- WSPC-04's preservation-before-removal half is complete for every affected candidate.
- D-07/D-08 are mechanically evidenced by named refs/commits or concrete handoffs.
- The WSPC-04 unclassified approval assumption remains visible and constrains Plan 04.
</success_criteria>

<output>
Create `.planning/phases/161-canonical-workspace-and-evidence-preservation/161-03-SUMMARY.md` when done.
</output>
