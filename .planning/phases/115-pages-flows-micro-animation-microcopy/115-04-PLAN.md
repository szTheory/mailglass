---
phase: 115-pages-flows-micro-animation-microcopy
plan: 04
type: execute
wave: 2
depends_on: ["115-01", "115-02"]
files_modified:
  - mailglass_admin/e2e/flows.spec.js
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03]
must_haves:
  truths:
    - "ONE new e2e/flows.spec.js walks the 5-path taxonomy (happy/error/boundary/edge/advanced) per surface by SEEDED URL, reusing existing helpers, with ZERO new fixtures (D-02)"
    - "The single obvious top operator action per surface (Operator=Replay delivery, Inbound=Replay inbound message, Preview=Render scenario) stays visible/dominant and survives 320px in all three themes — IA is verified, not re-architected (D-01)"
    - "Full walk = 3 surfaces x 5 paths x {320px} x {system}: each asserts correct landmark/testid visible + no horizontal overflow on root AND master-detail + the single-h1 invariant (D-03a)"
    - "Overlay subset = operator + inbound replay modal x {320}: panel-above-scrim + Escape closes + background scrollY unchanged (D-03b)"
    - "Theme-parity spot-check = 1 happy + 1 overlay path x {light,dark,system} x {320}: assertTextContrastAA passes (D-03c)"
    - "structural.spec.js touched responsive tests lower the floor 390->320 and patch overflow, but DO NOT promote the 320 cell into the ratchet baseline (D-04)"
    - "structural motion block asserts transformOrigin center for centered modals, theme label transitionProperty excludes color, getAnimations() empty after data-theme swap, and a :hover state layer transitionDuration <= 0.1s (D-09)"
    - "Assertions only: getComputedStyle/boundingBox/scrollWidth-clientWidth<=1/elementFromPoint/data-theme/getAnimations — NEVER toHaveScreenshot (D-03)"
  artifacts:
    - path: "mailglass_admin/e2e/flows.spec.js"
      provides: "5-path x 3-surface flow walk at 320/system + overlay subset + light/dark/system contrast spot-check"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "390->320 floor lowering on touched responsive tests; motion-block origin/theme-swap/state-layer assertions"
  key_links:
    - from: "flows.spec.js"
      to: "the three live surfaces by seeded URL"
      via: "openOperator/openInbound/openPreview* + seeded URL params (delivery_id=does-not-exist, status=queued, tenant_id=browser-empty) — no new fixtures"
      pattern: "openOperator|openInbound|openPreview"
  prohibitions:
    - "Do NOT add fixtures — flow-proof only against what is already seeded (D-02)"
    - "Do NOT use toHaveScreenshot / any pixel-diff (D-03)"
    - "Do NOT promote the 320 cell into the permanent ratchet baseline / current->prior re-score (Phase 116, D-04)"
    - "Do NOT add the full component x state x theme x viewport gallery matrix or axe-JSON baseline (Phase 116, D-13)"
    - "Do NOT wire live :permission_denied/:stale triggers — render-only via URL state-probe / gallery (D-05)"
    - "Do NOT add routes or touch brandbook/ (D-13)"
---

<objective>
Prove FLOW-01/02/03 with ONE new `e2e/flows.spec.js` 5-path walk and the `structural.spec.js` 320-floor + motion-block additions — deterministic, no pixel-diff, zero new fixtures.

Purpose: Demonstrate every happy/error/boundary/edge/advanced path on the three live surfaces works at 320px/system with the single-h1 invariant and no overflow; prove the overlay interaction contract (panel-above-scrim, Escape, no scroll-chaining); spot-check contrast across light/dark/system; and assert the origin-aware / theme-swap motion contracts structurally.
Output: new flows.spec.js; structural.spec.js touched responsive tests lowered to 320 + motion-block origin/theme-swap/state-layer assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- New file: `e2e/flows.spec.js` (5-path x 3-surface walk + overlay subset + contrast spot-check).
- New/changed test cases in `structural.spec.js`: touched responsive surface tests lowered from 390 to 320; motion-block assertions for `transformOrigin` (center for centered modals), theme-label `transitionProperty` excluding color, `getAnimations()` empty after `data-theme` swap, and `:hover` state-layer `transitionDuration <= 0.1s`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create e2e/flows.spec.js — 5-path x 3-surface walk at 320/system + overlay subset + contrast spot-check</name>
  <files>mailglass_admin/e2e/flows.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (PRIMITIVE_VIEWPORTS ~19-23 incl 320; helpers openOperator ~47 / openInbound ~77 / openPreviewEmpty ~90 / openPreviewIndex ~100 / openPreviewScenario ~106 / openPreviewError ~116; assertNoElementHorizontalOverflow ~496; assertPanelAboveScrim + assertTextContrastAA ~271-569; openOperatorReplayModal ~561 / openInboundReplayModal ~595; state-by-URL probes ~812-835 — the seeded URLs to reuse)
    - mailglass_admin/e2e/operator.spec.js (viewport call convention ~74, ~316; per-file login convention — no shared module)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-02 one new spec, reuse helpers, zero fixtures; D-03 bounded matrix a/b/c; the flow-path taxonomy table in specifics)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md (single-h1 invariant; overlay interaction contract; theme-parity spot-check; primary CTA per surface)
  </read_first>
  <action>
    Create `e2e/flows.spec.js`. Match the current per-file convention (duplicate the login/helper pattern from operator.spec.js / structural.spec.js — no shared module exists; Claude's discretion on test names). Reuse the existing helpers; add NO new fixtures — drive flows with the already-seeded URL params used at structural.spec.js ~812-835 (e.g. `delivery_id=does-not-exist`, `status=queued`, `tenant_id=browser-empty`, scenario/error preview params).
    (a) FULL WALK — for each of the 3 surfaces (Operator `/ops/mail`, Inbound `/ops/mail/inbound`, Preview `/dev/mail`) x the 5 paths from the taxonomy table (happy / error / boundary / edge / advanced) at viewport {320x900} with the {system} theme: assert the correct landmark/testid is visible per path; assert `assertNoElementHorizontalOverflow` on BOTH the root and the master-detail container (`scrollWidth - clientWidth <= 1`); assert exactly ONE `h1` is present on the surface (single-h1 invariant; orientation-strip headings stay h2/h3); and on the happy path assert the single primary CTA per surface (Operator=`Replay delivery`, Inbound=`Replay inbound message`, Preview=`Render scenario`) is visible and is the one accent/`btn-primary` action at 320 (D-01 IA verification).
    (b) OVERLAY SUBSET — operator + inbound replay modal at {320}: open via `openOperatorReplayModal`/`openInboundReplayModal`; assert `assertPanelAboveScrim` (elementFromPoint hit-test, panel above scrim); press Escape and assert the modal closes; capture `window.scrollY` before opening and after scrolling inside the modal and assert it is unchanged (scroll-chaining guard from the overscroll-contain in Plan 02).
    (c) THEME-PARITY SPOT-CHECK — 1 happy path + 1 overlay path x {light, dark, system} x {320}: assert `assertTextContrastAA` passes; read `data-theme` to confirm the theme axis. Use NO `toHaveScreenshot` anywhere — assertions are limited to getComputedStyle / boundingBox / scrollWidth-clientWidth / elementFromPoint / data-theme / getAnimations.
    Map taxonomy paths to seeded URLs per the CONTEXT specifics table (Operator: happy=select row->timeline; error=`delivery_id=does-not-exist`; boundary=no-results empty+reset / no-tenant; edge=long/non-ASCII truncate + pagination; advanced=replay modal. Inbound + Preview analogously).
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test e2e/flows.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npx playwright test e2e/flows.spec.js` passes; the file contains ~15 full-walk tests (3 surfaces x 5 paths) plus the overlay subset and contrast spot-check.
    - Source assertion: `grep -c "toHaveScreenshot" e2e/flows.spec.js` returns 0.
    - Source assertion: flows.spec.js declares no new fixture seeding — it reuses `openOperator`/`openInbound`/`openPreview*` and the seeded URL params only.
    - Behavior assertion: each full-walk test asserts a single `h1` and `scrollWidth - clientWidth <= 1` on root and master-detail at 320.
  </acceptance_criteria>
  <done>flows.spec.js proves the 5-path x 3-surface walk at 320/system + overlay subset + contrast spot-check, zero fixtures, no pixel-diff.</done>
</task>

<task type="auto">
  <name>Task 2: Lower touched structural responsive tests 390->320 and add motion-block origin/theme-swap/state-layer assertions</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (responsive surface tests ~654-919 hardcoding `width: 390`; motion block ~1208-1268; state-probe ~812-835; PRIMITIVE_VIEWPORTS ~19-23)
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex + inbound/replay_modal.ex (centered panels — transformOrigin must resolve to center)
    - mailglass_admin/lib/mailglass_admin/components.ex (theme-picker label — transitionProperty must exclude color after Plan 01)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-04 lower floor but DO NOT rebaseline; D-09 structural motion assertions)
  </read_first>
  <action>
    In the responsive surface tests this phase touches (~654-919), lower the hardcoded `390` viewport width to `320` and patch any overflow the lower floor surfaces (assert `scrollWidth - clientWidth <= 1` and >=44px targets at 320). Per D-04, ONLY lower the floor + patch overflow in the tests you touch — do NOT promote the 320 cell into the permanent ratchet baseline and do NOT touch the `current -> prior` re-score (Phase 116 owns that).
    Extend the motion block (~1208) with assertions (no pixel-diff): open a centered replay modal and assert `getComputedStyle(panel).transformOrigin` resolves to center (the centered modals omit `--mg-origin`); for any header-anchored overlay added in Plan 02, assert its transformOrigin resolves to the top edge. Assert the theme-picker label's `transitionProperty` excludes `color`/`background-color` (Plan 01 removed the transition). Toggle `data-theme` via `page.evaluate` and assert `getAnimations()` on the root subtree is empty immediately after the swap (theme swap never animates). Assert a `:hover` state-layer element still reports `transitionDuration <= 0.1s` (state layers survive — Plan 02).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q "transformOrigin" e2e/structural.spec.js && grep -q "getAnimations" e2e/structural.spec.js && npx playwright test e2e/structural.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the touched responsive tests use `320` (not `390`) for the viewport width; `grep -c "width: 390" e2e/structural.spec.js | grep -v '^#'` is reduced only for the tests this phase touches (untouched tests may keep 390 — see CONTEXT note: only lower the floor in tests you touch).
    - Source assertion: the motion block asserts `transformOrigin` (center for centered modals), `transitionProperty` excludes color for the theme label, `getAnimations()` empty after a `data-theme` swap, and a `:hover` state-layer `transitionDuration <= 0.1s`.
    - CLI: `npx playwright test e2e/structural.spec.js` passes; `grep -c "toHaveScreenshot" e2e/structural.spec.js` returns 0.
  </acceptance_criteria>
  <done>Touched responsive tests run at the 320 floor (not rebaselined) and the motion block structurally proves origin/theme-swap/state-layer contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded test data → flow assertions | Tests drive only already-seeded URLs; no new fixture or external input crosses into the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-07 | Information disclosure | permission_denied render path (probed by URL) | mitigate | The permission_denied specimen is render-only (D-05); the live cross-tenant trigger stays deferred — flows.spec.js never asserts a live deny that could leak resource existence |
| T-115-08 | Tampering | npm/pip/cargo installs | accept | No package installs; Playwright already present, zero-Node toolchain for the app, no new deps |
</threat_model>

<verification>
- `npx playwright test e2e/flows.spec.js` and `npx playwright test e2e/structural.spec.js` both pass.
- `grep -c toHaveScreenshot` returns 0 in both spec files.
- Touched responsive tests run at 320; motion block asserts origin/theme-swap/state-layer.
</verification>

<success_criteria>
- FLOW-01/02/03 proven structurally: 5-path x 3-surface walk at 320/system with single-h1 + no overflow; overlay subset (panel-above-scrim, Escape, no scroll-chaining); contrast across light/dark/system; origin-aware/theme-swap motion asserted — all deterministic, zero fixtures, no pixel-diff, and the 320 cell is NOT promoted to the baseline.
</success_criteria>

<output>
Create `.planning/phases/115-pages-flows-micro-animation-microcopy/115-04-SUMMARY.md` when done.
</output>