---
phase: 115-pages-flows-micro-animation-microcopy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/assets/css/app.css
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
autonomous: true
requirements: [FLOW-02, FLOW-03]
must_haves:
  truths:
    - ".motion-overlay declares transform-origin: var(--mg-origin, center) — re-parameterizes the existing scale(0.98->1), no new animated property (D-07, MOTION-LD-04/10)"
    - "Centered confirm modals (operator/inbound replay_modal) keep the default center origin — they OMIT the --mg-origin var (D-07)"
    - "Header-anchored overlays/toasts (if any rendered) set a fixed author-time keyword inline style --mg-origin, never computed, no getBoundingClientRect, no phx-hook (D-07)"
    - "State-layer (:hover/:focus) color transitions opt in at the fast token (<=100ms); bulk theme-driven color/border transitions are OFF by default (D-08)"
    - "The modal scroll container has overscroll-behavior: contain (scroll-chaining guard) (D-04)"
    - "Reduced-motion already neutralizes the origin var via the existing @media block — no extra rule added (D-08)"
  artifacts:
    - path: "mailglass_admin/assets/css/app.css"
      provides: ".motion-overlay transform-origin var; state-layer transition scoping; overscroll-contain utility if needed; committed CSS bundle rebuilt"
    - path: "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
      provides: "overscroll-behavior: contain on the modal scroll container; stays center (omits --mg-origin)"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex"
      provides: "overscroll-behavior: contain; stays center (omits --mg-origin)"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Inline --mg-origin keyword on any header-anchored/toast overlay trigger (centered modal omits it)"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Inline --mg-origin keyword on any header-anchored/toast overlay trigger (centered modal omits it)"
  key_links:
    - from: ".motion-overlay (app.css)"
      to: "overlay panels (replay modals + any header overlay)"
      via: "transform-origin: var(--mg-origin, center); per-surface inline --mg-origin keyword (centered modals omit -> center)"
      pattern: "transform-origin:\\s*var\\(--mg-origin"
  prohibitions:
    - "Do NOT compute origin at runtime — no getBoundingClientRect, no phx-hook, no client JS (D-07 zero-hook)"
    - "Do NOT add a new animated property — only re-parameterize the existing scale (MOTION-LD-04/10)"
    - "Do NOT add transition-colors on data-theme-driven root chrome (D-08; the theme-picker label removal lives in Plan 01)"
    - "Do NOT add a reduced-motion rule for the origin var — the existing @media block already covers it (D-08)"
    - "Do NOT touch brandbook/ tokens, recipient-facing templates, or add fixtures/routes (D-13)"
---

<objective>
Land the FLOW-03 micro-animation pass: origin-aware overlays and theme-switch suppression, within the MOTION-LD locks.

Purpose: Make overlays scale from an intentional author-time origin (centered modals stay center) via a single CSS custom property and inline keyword — zero JS, no new animated property. Scope color transitions to state layers only so the LiveView server-side theme re-render never flashes an animated color change. Add the scroll-chaining guard on modal scroll containers for 320px.
Output: `.motion-overlay` origin var + state-layer transition scoping in app.css (bundle rebuilt); overscroll-contain on both replay modals; inline `--mg-origin` keywords on overlay triggers (centered modals omit).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- New CSS custom property usage: `--mg-origin` consumed by `.motion-overlay` as `transform-origin: var(--mg-origin, center)`.
- New/changed CSS: state-layer-scoped color transition (`:hover`/`:focus` opt-in at the fast token), `overscroll-behavior: contain` on the modal scroll container; rebuilt committed `priv/static` CSS bundle.
- Changed HEEx: inline `style="--mg-origin: ..."` keyword on any header-anchored/toast overlay trigger; centered replay modals deliberately OMIT the var (default center).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add origin var + state-layer transition scoping + overscroll-contain in app.css, rebuild bundle</name>
  <files>mailglass_admin/assets/css/app.css</files>
  <read_first>
    - mailglass_admin/assets/css/app.css (.motion-overlay keyframe ~312-335; reduced-motion block ~392-400; --duration-* / --ease-* tokens; focus-ring ~260)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-07 origin var; D-08 inverted default / state-layer opt-in; D-04 overscroll-contain)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md (Motion M1/M2; duration tokens table)
  </read_first>
  <action>
    In app.css, add to the `.motion-overlay` rule (~333) the declaration `transform-origin: var(--mg-origin, center);` so the existing `mg-overlay` scale keyframe scales from the author-declared origin (default center). Add NO new keyframe and NO new animated property.
    Add the state-layer color-transition scoping (D-08 inverted default): provide a state-layer-only transition utility/rule so that ONLY `:hover`/`:focus` interactive elements transition color, at the fast token (`transition: color var(--duration-instant)` or the equivalent `<= 0.1s` fast token, MOTION-LD-06). Name the utility per Claude's discretion (e.g. `.mg-state-layer`); do NOT declare `transition-colors` on any `data-theme`-driven root chrome. Do NOT add a reduced-motion rule for the origin var — the existing `@media (prefers-reduced-motion: reduce)` block already neutralizes it.
    Add `overscroll-behavior: contain;` to the modal scroll container styling (either as a utility class consumed by the replay modals in Task 2, or scoped to the overlay panel scroll region) so background scroll does not chain at 320px.
    Rebuild the committed CSS bundle (run the project's asset build so `priv/static` reflects the CSS change) — CI runs `git diff --exit-code` on `priv/static`, so the rebuilt bundle MUST be committed.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -Eq "transform-origin:\s*var\(--mg-origin" assets/css/app.css && grep -q "overscroll-behavior: contain" assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E "transform-origin:\s*var\(--mg-origin, center\)" assets/css/app.css` matches inside the `.motion-overlay` rule.
    - Source assertion: a state-layer transition rule exists scoped to `:hover`/`:focus` using a fast token (<= 0.1s); no `transition-colors` appears on `data-theme` root chrome.
    - Source assertion: `grep -F "overscroll-behavior: contain" assets/css/app.css` returns a match.
    - CLI: after the asset build, `git diff --exit-code` on `priv/static` is clean only once the rebuilt bundle is committed (the rebuilt bundle is part of the change).
  </acceptance_criteria>
  <done>.motion-overlay scales from var(--mg-origin, center); color transitions are state-layer-scoped; overscroll-contain available; committed CSS bundle rebuilt.</done>
</task>

<task type="auto">
  <name>Task 2: Apply overscroll-contain to both replay modals; confirm they stay center (omit --mg-origin)</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex, mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (panel `motion-overlay mg-layer-overlay-panel mx-auto my-4 w-full max-w-2xl` ~33; close buttons ~51/99; confirm ~105)
    - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (panel `motion-overlay mg-layer-overlay-panel w-full max-w-2xl` ~37; confirm ~67)
    - mailglass_admin/assets/css/app.css (the overscroll-contain utility added in Task 1)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-07 centered modals keep center / omit var; D-04 overscroll-contain on modal scroll container)
  </read_first>
  <action>
    Both replay modals are centered (`mx-auto`/`max-w-2xl`), so per D-07 they MUST keep the default `center` origin: do NOT add any `style="--mg-origin: ..."` to either panel — omitting the var resolves to `center` via the Task-1 fallback.
    Add the scroll-chaining guard to each modal's scroll container: apply the overscroll-contain utility class from Task 1 (or `style="overscroll-behavior: contain"`) to the scrollable region of each `.motion-overlay` panel so background `scrollY` stays unchanged when scrolling inside the modal at 320px. Inbound replay_modal currently lacks `mx-auto` — ensure it is still horizontally centered at 320 (add `mx-auto` if missing) without changing its origin (still omit the var → center).
    Do not alter the existing `phx-click="close_replay"` Escape/close behavior or testids (`operator-replay-modal`, `inbound-replay-modal`, the confirm testids).
  </action>
  <verify>
    <automated>cd mailglass_admin && ! grep -q -- "--mg-origin" lib/mailglass_admin/operator/replay_modal.ex && ! grep -q -- "--mg-origin" lib/mailglass_admin/inbound/replay_modal.ex && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: neither replay_modal.ex contains `--mg-origin` (centered modals keep center).
    - Source assertion: each replay modal's scroll region carries the overscroll-contain utility/style.
    - Behavior assertion: existing modal testids and `close_replay` wiring are unchanged; `mix compile --warnings-as-errors` passes.
  </acceptance_criteria>
  <done>Both replay modals keep center origin (no var) and guard scroll-chaining via overscroll-contain.</done>
</task>

<task type="auto">
  <name>Task 3: Inline --mg-origin keyword on header-anchored/toast overlay triggers in the live views</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (overlay open at ~247 assign replay_modal_open?; surface composition)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (overlay open at ~239)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-07 origin by surface role: header-anchored = top/top right, toast = top right, centered modal = center/omit; never computed)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md (Motion M1 origin-by-surface-role table)
  </read_first>
  <action>
    Audit the overlay surfaces actually rendered by operator_live.ex and inbound_live.ex. For any header-anchored overlay (e.g. a header-anchored popover/menu) add a fixed author-time inline `style="--mg-origin: top right"` (or `top` for a top-edge-anchored panel) on the `.motion-overlay` element; for any toast use `style="--mg-origin: top right"`. The two replay modals are centered and MUST remain without the var (handled in Task 2). If the only overlays in these surfaces are the centered replay modals, make NO change here and record that in the SUMMARY (the centered-modal default is the correct intentional origin) — do not invent a new overlay. Never compute the keyword; it is a fixed surface-role keyword.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: any header-anchored/toast `.motion-overlay` in the two live views carries a literal `style="--mg-origin: ..."` with a fixed keyword (`top`, `top right`); no `--mg-origin` is computed from JS or `getBoundingClientRect`.
    - Behavior assertion: if the surfaces render only the centered replay modals, no `--mg-origin` is added and the SUMMARY records that the centered default is intentional.
    - Behavior assertion: `mix compile --warnings-as-errors` passes.
  </acceptance_criteria>
  <done>Every non-centered overlay declares a fixed author-time origin keyword; centered modals stay center; nothing is computed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-declared origin → CSS rendering | Origin is a fixed authored keyword; no untrusted/runtime input crosses into transform-origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03 | Tampering | --mg-origin value source | mitigate | Origin is a fixed author-time keyword in HEEx, never computed from client input — no injection surface (D-07) |
| T-115-04 | Tampering | npm/pip/cargo installs | accept | No package installs; CSS + HEEx edits only, zero-Node asset build |
</threat_model>

<verification>
- `grep -E "transform-origin:\s*var\(--mg-origin" assets/css/app.css` matches inside `.motion-overlay`.
- Neither replay modal contains `--mg-origin` (stay center); both carry overscroll-contain.
- No `transition-colors` on `data-theme` root chrome; state-layer transition present at fast token.
- `mix compile --warnings-as-errors` clean; rebuilt CSS bundle committed (`git diff --exit-code` on priv/static clean).
</verification>

<success_criteria>
- FLOW-03: origin-aware overlays via `var(--mg-origin, center)` with centered modals at center; theme-switch never animates color (state-layer-scoped transitions); reduced-motion neutralized by the existing block; overscroll-contain guards 320px scroll-chaining. No new animated property, zero hooks.
</success_criteria>

<output>
Create `.planning/phases/115-pages-flows-micro-animation-microcopy/115-02-SUMMARY.md` when done.
</output>