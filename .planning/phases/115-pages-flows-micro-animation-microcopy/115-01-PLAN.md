---
phase: 115-pages-flows-micro-animation-microcopy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/test/mailglass_admin/voice_test.exs
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-04]
must_haves:
  truths:
    - "Permission-denied copy is byte-identical heading for deliveries and inbound (Access restricted) and never names the missing permission or a specific tenant — no existence leak (D-10)"
    - "Stale copy shape is the locked Data may be out of date / Showing {Noun}s as of {HH:MM}. Refresh to load the latest. with an illustrative timestamp, no live trigger (D-05/D-10)"
    - "The three tenant failure modes (no tenant chosen, no access, no tenants exist) stay lexically distinct, each with its own template + testid (D-06)"
    - "Sole-tenant mount auto-selects and renders NO picker (picker testid absent) (D-06)"
    - "Deliveries load-error body uses the cause-naming recovery form Delivery data unavailable / could not be loaded (D-11)"
    - "Theme-picker label carries no always-on transition-colors so theme swap never animates color (D-08)"
    - "voice_test asserts @banned_words absent AND the locked verbatim strings present across new state + tenant 0/1/>=2 cases (D-12)"
    - "Header cluster, mono ID cells, and tenant copy survive 320px without horizontal overflow (D-04)"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      provides: "Cause-naming load-error copy (D-11); permission/stale verbatim copy"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      provides: "Inbound permission/stale/error verbatim copy (D-10)"
    - path: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      provides: "Verbatim tenant copy + 320px header flex-wrap + mono truncate cells"
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "data_state copy polish + theme-picker transition-colors removal (D-08)"
    - path: "mailglass_admin/test/mailglass_admin/voice_test.exs"
      provides: "New permission/stale/tenant state cases + verbatim-string positive assertions"
  key_links:
    - from: "voice_test.exs"
      to: "components.ex data_state + shell.ex tenant_selector"
      via: "render_component / LiveView-nav forcing each state, asserting strip_scripts(html) banned-free + verbatim present"
      pattern: "Access restricted|Data may be out of date|No tenants available"
  prohibitions:
    - "Do NOT wire live :permission_denied or :stale triggers — copy/render pass only (D-05)"
    - "Do NOT add fixtures (D-13)"
    - "Do NOT add a ban grep for standalone Email/Status/Notification — Status is a legitimate <th> header; enforce domain nouns as positive render assertions only (D-12)"
    - "Do NOT add routes, touch brandbook/ tokens, or recipient-facing email templates (D-13)"
    - "Do NOT promote the 320 cell into the ratchet baseline (D-04)"
---

<objective>
Land the FLOW-04 microcopy pass and the FLOW-01/02 320px shell-layout patches on the three live surfaces.

Purpose: Replace generic/near-miss copy with the locked verbatim strings (cause-naming, thoughtful-maintainer voice, seven domain nouns), keep the three tenant failure modes lexically distinct, remove the theme-picker color transition so theme swaps never animate, and make the header cluster + mono cells survive 320px. Prove it with extended voice_test cases.
Output: Updated copy + 320 layout patches in deliveries_list/records_list/shell/components; new voice_test state cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md
@.planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- New/changed microcopy strings/headings: `Access restricted`, `Data may be out of date`, `No tenants available`, `Select a tenant`, `Delivery data unavailable`, `Record data unavailable`, `Scoped to {tenant}` (sole-tenant chip) — verbatim from the locked table.
- Changed test cases: new `voice_test.exs` cases forcing `data_state` `:permission_denied`/`:stale` and tenant 0/1/>=2 mounts, asserting both `@banned_words`-free and verbatim-present, plus sole-tenant picker-absent.
- Changed CSS class usage: removal of `transition-colors` from the theme-picker label (no new utility; the FLOW-03 inverted-default for theme chrome).
- Changed layout: header flex-wrap cluster + `mono min-w-0 truncate` + `title` ID cells re-asserted at 320px (no new token, no new layout system).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace deliveries + inbound list state copy with locked verbatim strings</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/inbound/records_list.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (state branches ~49-99; generic error string ~53-54; the `Status` th at ~106 — leave it)
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (error/permission_denied/stale branches ~59-71)
    - mailglass_admin/lib/mailglass_admin/components.ex (data_state/1 ~410-465 — confirm it takes title/body; do not change its signature)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (locked microcopy table in specifics; D-05, D-10, D-11, D-12)
  </read_first>
  <action>
    In deliveries_list.ex set the load-error data_state to title "Delivery data unavailable" and body "Delivery data could not be loaded. Refresh the page or adjust the filters, then try again." (D-11 — replaces the ~53-54 generic "There was a problem loading deliveries. Try refreshing the page."). Set permission_denied to title "Access restricted" body "You do not have access to this tenant's mail operations. Ask an administrator to grant access." Set stale to title "Data may be out of date" body "Showing Deliveries as of 14:32. Refresh to load the latest." (14:32 is the illustrative author-time timestamp; no live trigger per D-05). Leave the `Status` th at ~106 untouched.
    In records_list.ex set error to title "Record data unavailable" body "Record data could not be loaded. Refresh the page or adjust the filters, then try again."; permission_denied to title "Access restricted" body "You do not have access to this tenant's inbound routing. Ask an administrator to grant access." (byte-identical heading to deliveries; never names the permission or tenant — no existence leak); stale to title "Data may be out of date" body "Showing InboundMessages as of 14:32. Refresh to load the latest."
    Do not add a Refresh affordance wired to a live event (deferred per D-05); leave any existing recovery affordance as the inert illustrative shape it already is.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -qF "Delivery data could not be loaded. Refresh the page or adjust the filters, then try again." lib/mailglass_admin/operator/deliveries_list.ex && grep -qF "You do not have access to this tenant's inbound routing. Ask an administrator to grant access." lib/mailglass_admin/inbound/records_list.ex && grep -qF "Showing Deliveries as of 14:32. Refresh to load the latest." lib/mailglass_admin/operator/deliveries_list.ex && ! grep -qF "There was a problem loading deliveries" lib/mailglass_admin/operator/deliveries_list.ex</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Access restricted" lib/mailglass_admin/operator/deliveries_list.ex` and the same over `lib/mailglass_admin/inbound/records_list.ex` each return >= 1; the two permission_denied sub-copy strings differ only by `mail operations` vs `inbound routing`.
    - Source assertion: `grep -F "There was a problem loading deliveries" lib/mailglass_admin/operator/deliveries_list.ex` returns nothing.
    - Behavior assertion: the permission_denied sub-copy contains no tenant id, no permission name, no resource-exists/absent distinction.
  </acceptance_criteria>
  <done>Deliveries + inbound list states render the locked verbatim copy; the generic "problem loading" offender is gone; the `Status` th is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align shell tenant copy verbatim, patch 320px header/cells, remove theme-picker transition</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (header flex cluster ~243; tenant_selector copy ~305-356; mono truncate link cell ~340)
    - mailglass_admin/lib/mailglass_admin/components.ex (no-tenant chip ~306; theme-picker label `transition-colors ease-out duration-(--duration-fast)` ~337 — the always-on transition to remove per D-08)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (locked microcopy table; D-04 320 patch list; D-06 three distinct tenant modes; D-08 theme transition removal)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-UI-SPEC.md (Responsive Contract 320 patches; Motion M2 inverted default)
  </read_first>
  <action>
    In shell.ex tenant_selector make copy byte-identical to the locked table: no-tenants heading "No tenants available" / body "This operator does not have a tenant with mail activity yet. Send a Message with a tenant_id, or check the host tenant scope." (capital M in "Send a Message"); select-a-tenant heading "Select a tenant" / body "Choose a tenant to inspect its Deliveries and inbound routing. Tenant scope stays in the URL so refreshes and shared links keep the same view." (capital D in "Deliveries"); per-tenant link label stays "Select tenant". Keep all three modes as distinct templates with distinct testids; do not merge them.
    320px patches (no new layout system, D-04): confirm the ~243 header is `flex flex-wrap items-center ... gap-sm` so tenant-chip + theme-picker + nav-pills wrap at 320; add `min-w-0` to any non-wrapping flex child that could force overflow. Ensure every `mono` ID/tenant cell uses `min-w-0 truncate` + a native `title` attribute (the link cell at ~340 already does — apply the same to any mono cell missing it).
    In components.ex theme-picker label (~337), remove the `transition-colors ease-out duration-(--duration-fast)` classes from the label so theme-driven color changes do not animate (D-08 inverted default). Do NOT add a transition to any `data-theme`-driven root chrome. State-layer (`:hover`/`:focus`) transitions elsewhere are unaffected.
    Where the auto-selected single tenant renders, apply the optional sole-tenant chip copy "Scoped to {tenant}" (the picker must NOT render in the sole-tenant case — asserted in Task 3).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -qF "Send a Message with a tenant_id" lib/mailglass_admin/operator/shell.ex && grep -qF "inspect its Deliveries and inbound routing" lib/mailglass_admin/operator/shell.ex && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -F "Send a Message with a tenant_id" lib/mailglass_admin/operator/shell.ex` returns the no-tenant body verbatim; `grep -F "inspect its Deliveries and inbound routing" lib/mailglass_admin/operator/shell.ex` returns the select-a-tenant body verbatim.
    - Source assertion: the theme-picker label string in components.ex (~337) no longer contains `transition-colors`.
    - Behavior assertion: `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>Tenant copy is byte-identical to the locked table across all three distinct modes; the header cluster + mono cells are 320-safe; the theme-picker label has no color transition.</done>
</task>

<task type="auto">
  <name>Task 3: Extend voice_test with new state + tenant cases asserting banned-free and verbatim-present</name>
  <files>mailglass_admin/test/mailglass_admin/voice_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/voice_test.exs (@banned_words ~25; strip_scripts ~183; surface coverage ~91-157; the phoenix.mjs "noops" false-positive note ~32-35)
    - mailglass_admin/lib/mailglass_admin/components.ex (data_state/1 ~410-465 — testids per kind)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (tenant_selector + testids)
    - .planning/phases/115-pages-flows-micro-animation-microcopy/115-CONTEXT.md (D-12 ban verification = rendered-HTML voice test + positive verbatim assertions; sole-tenant picker absent)
  </read_first>
  <action>
    Add render_component / LiveView-nav cases that force each new state and assert against `strip_scripts(html)`:
    (a) data_state :permission_denied (deliveries + inbound) and :stale (deliveries + inbound) — for each, assert the existing `@banned_words` loop finds none AND the locked verbatim heading/body strings are present (e.g. "Access restricted", "You do not have access to this tenant's mail operations", "Data may be out of date", "Showing Deliveries as of 14:32. Refresh to load the latest.").
    (b) tenant 0 / 1 / >=2 mounts: 0 → assert "No tenants available" + "Send a Message with a tenant_id" present and banned-free; >=2 → assert "Select a tenant" + "inspect its Deliveries and inbound routing" present and the per-tenant "Select tenant" link present; 1 (sole-tenant) → assert the picker testid is ABSENT (proves auto-select per D-06) and, if the chip renders, "Scoped to" present.
    Enforce the domain-noun rules as POSITIVE render assertions only (e.g. asserting the domain nouns appear where intended) — do NOT add any ban grep for standalone Email/Status/Notification (D-12). Match the existing per-test convention in the file; reuse strip_scripts.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `mix test test/mailglass_admin/voice_test.exs` passes with the new cases.
    - Test assertion: a forced :permission_denied render asserts `@banned_words` absent AND "Access restricted" present.
    - Test assertion: the sole-tenant case asserts the picker testid is absent (auto-select proven).
    - Source assertion: voice_test.exs contains no grep/assertion banning standalone `Status` (the <th> header survives).
  </acceptance_criteria>
  <done>voice_test passes with new permission/stale/tenant cases proving banned-free + verbatim-present + sole-tenant picker-absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (authenticated) → tenant-scoped read models | A signed-in operator must not learn that a forbidden tenant's resources exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Information disclosure | permission_denied copy (deliveries/inbound) | mitigate | Copy is byte-identical regardless of resource existence; never names the permission or a specific tenant (D-10) — no existence leak |
| T-115-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure copy/template edits, zero-Node |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/mailglass_admin/voice_test.exs` passes.
- All locked verbatim strings present via grep; generic "problem loading" offender absent.
- Theme-picker label has no `transition-colors`.
</verification>

<success_criteria>
- FLOW-04 copy is byte-identical to the locked table across deliveries/inbound/shell; three tenant modes lexically distinct with distinct testids; permission-denied leak-free; theme-picker color transition removed (D-08); voice_test green.
</success_criteria>

<output>
Create `.planning/phases/115-pages-flows-micro-animation-microcopy/115-01-SUMMARY.md` when done.
</output>