---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "05"
type: execute
wave: 3
depends_on: [151-04]
files_modified:
  - lib/mailglass/config.ex
  - lib/mailglass/outbound/payload_lifecycle.ex
  - lib/mailglass/outbound/payload_pruner.ex
  - test/mailglass/config_test.exs
  - test/mailglass/outbound/payload_lifecycle_test.exs
autonomous: true
requirements: [PRIV-02, PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-16/D-17: defaults are terminal 14d, uncertain 30d, legacy 14d, batch 500; all are finite, validated, documented config facts."
    - "D-18: `prune(tenant_id: tenant_id)` requires a nonblank explicit tenant, processes at most one deterministic prefix-safe batch for only that tenant, and leaves explanatory tombstones."
    - "D-16/D-19/D-20: terminal/discarded/abandoned use 14d, uncertain 30d, legacy 14d; recovery never invents legacy content or resends uncertainty."
  artifacts:
    - {path: lib/mailglass/outbound/payload_lifecycle.ex, provides: "retention policy and recovery eligibility"}
    - {path: lib/mailglass/outbound/payload_pruner.ex, provides: "bounded tombstone-preserving prune API"}
  key_links:
    - {from: lib/mailglass/outbound/payload_pruner.ex, to: lib/mailglass/outbound/payload.ex, via: "scoped deterministic CAS transitions", pattern: "prune_batch"}
---
<objective>Implement the finite retention/recovery policy and one-batch tombstone prune core.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-RESEARCH.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-04-SUMMARY.md
@lib/mailglass/config.ex
@lib/mailglass/webhook/pruner.ex
</context>
<assumption_delta_decision>Adopt the research defaults exactly: `outbound_payload_retention` terminal_days 14, uncertain_days 30, legacy_days 14, prune_batch_size 500. The 30-day uncertainty window and 500-row batch are [ASSUMED] policy defaults, deliberately configurable and covered at boundaries; no code evidence warrants adjustment.</assumption_delta_decision>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 151-05-01: Expire one bounded batch into non-content tombstones</name>
  <reversibility rating="costly">D-16/D-17 set operational defaults, but adopters can override them without data migration.</reversibility>
  <files>lib/mailglass/config.ex, lib/mailglass/outbound/payload_lifecycle.ex, lib/mailglass/outbound/payload_pruner.ex, test/mailglass/config_test.exs, test/mailglass/outbound/payload_lifecycle_test.exs</files>
  <read_first>Read D-16..D-20, research recommended policy/assumptions, Config NimbleOptions `webhook_retention` pattern, Clock, Webhook.Pruner, Plan 04 lifecycle APIs, and Phase 150 prefix tests.</read_first>
  <behavior>
    - Defaults and positive overrides validate; zero, negative, infinity, unknown keys, and invalid batch values fail configuration validation.
    - `prune(tenant_id: binary)` rejects missing/blank tenant and selects no more than configured batch size by expiry/id for exactly that tenant and configured prefix.
    - Multi-tenant fixtures prove pruning tenant A cannot select/count/mutate tenant B under hostile search_path and same-ID decoys.
    - Terminal, discarded, and abandoned retain 14 days; uncertain retains 30; legacy retains 14; each transitions to expired idempotently and preserves its bounded reason.
    - Recoverable is claimable, dispatching is acceptance-uncertain on observation, scrubbed/expired are contentless no-ops, and recovery never reconstructs legacy metadata.
  </behavior>
  <action>Add the exact research config subtree and validation. Keep the complete nine-state retention/recovery matrix in non-Oban `PayloadLifecycle`. Define the public core as `PayloadPruner.prune(tenant_id: tenant_id)` with a required validated nonblank tenant—no ambient/default tenant and no all-tenant sweep. Use `Clock.utc_now/0`, tenant predicate plus `Tenancy.scope/2`, configured prefix, deterministic expiry/id limit, and CAS content-bearing terminal/discarded/abandoned/uncertain/legacy rows to expired tombstones while retaining identity/version/digest/original reason/timestamps. Recoverable is never retention-pruned while retry-safe, dispatching is never blindly expired as retry-safe, and scrubbed/expired are idempotent no-ops. Do not use unbounded delete/loop or metadata reconstruction. Test every matrix row, exact cutoffs, batch 500 override, repeated calls, and two tenants plus a hostile decoy prefix.</action>
  <verify><automated>mix test test/mailglass/config_test.exs test/mailglass/outbound/payload_lifecycle_test.exs --only phase_151_task:t151_05_01 --warnings-as-errors</automated></verify>
  <done>Private non-success content has finite policy and each invocation safely removes at most one bounded batch while preserving explanation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| operator config → prune query | Retention values control destructive content removal and query size. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-11 | Denial of Service | prune sweep | high | mitigate | Positive max batch, indexed deterministic selection, one batch per call. |
| T-151-12 | Elevation of Privilege | prune scope | high | mitigate | Required explicit tenant argument, tenant predicate, Tenancy.scope, prefix opts, multi-tenant hostile-path tests. |
| T-151-13 | Tampering | recovery policy | medium | mitigate | Closed state transitions; uncertain and legacy cannot enter blind resend. |
| T-151-SC | Tampering | package supply chain | high | mitigate | No installs. |
</threat_model>
<verification>Run focused config/lifecycle tests, then `mix test test/mailglass/outbound/payload_lifecycle_test.exs test/mailglass/config_test.exs --warnings-as-errors`.</verification>
<success_criteria>PRIV-02 has finite defaults, bounded safe pruning, and honest recovery semantics.</success_criteria>
<output>Create `151-05-SUMMARY.md` in the phase directory.</output>
