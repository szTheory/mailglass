---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "06"
type: execute
wave: 4
depends_on: [151-05]
files_modified:
  - lib/mailglass/outbound/payload_pruner_worker.ex
  - lib/mix/tasks/mailglass.outbound.payloads.prune.ex
  - test/mailglass/outbound/payload_pruner_test.exs
autonomous: true
requirements: [PRIV-02]
must_haves:
  truths:
    - "D-18/PRIV-02: optional scheduled and manual entrypoints invoke exactly the same one-batch prune API and report bounded counts."
    - "The package compiles without Oban; the Mix task remains usable without Oban and never silently claims scheduled cleanup."
  artifacts:
    - {path: lib/mailglass/outbound/payload_pruner_worker.ex, provides: "conditionally compiled maintenance worker/stub"}
    - {path: lib/mix/tasks/mailglass.outbound.payloads.prune.ex, provides: "Oban-independent manual prune task"}
  key_links:
    - {from: lib/mix/tasks/mailglass.outbound.payloads.prune.ex, to: lib/mailglass/outbound/payload_pruner.ex, via: "same public prune function", pattern: "PayloadPruner.prune"}
---
<objective>Expose bounded payload pruning through honest optional Oban and universal manual operations paths.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-05-SUMMARY.md
@lib/mailglass/webhook/pruner.ex
@lib/mix/tasks/mailglass.webhooks.prune.ex
@test/mailglass/webhook/pruner_test.exs
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 151-06-01: Run the same bounded sweep with and without Oban</name>
  <files>lib/mailglass/outbound/payload_pruner_worker.ex, lib/mix/tasks/mailglass.outbound.payloads.prune.ex, test/mailglass/outbound/payload_pruner_test.exs</files>
  <read_first>Read D-18, research Open Question 2 and optional-runtime pattern, Plan 05 API, Webhook.Pruner conditional compilation, existing prune Mix task, and no-optional-runtime harness conventions.</read_first>
  <behavior>
    - Scheduled worker uses `:mailglass_maintenance`, ID-free args, and invokes one prune batch.
    - Mix task invokes the same API and succeeds when Oban is not loaded, printing counts without identifiers/private data.
    - Optional worker exposes honest availability and compilation contains no unguarded Oban reference.
  </behavior>
  <action>Conditionally compile an Oban worker on the existing maintenance queue plus an availability stub, keeping delivery queue/readiness untouched. Make both worker and Mix task call `PayloadPruner.prune/1`; unlike the older webhook task, the manual path must not require Oban because the prune core is non-Oban. Accept only bounded operational selectors supported by Plan 05, reject unknown CLI options, and print aggregate counts/reason classes only. Add worker/manual parity, no-recursive-sweep, private sentinel, invalid-option, and no-Oban compile/run tests tagged `t151_06_01`; retain `@compile {:no_warn_undefined, ...}` where required.</action>
  <verify><automated>mix test test/mailglass/outbound/payload_pruner_test.exs --only phase_151_task:t151_06_01 --warnings-as-errors &amp;&amp; MIX_ENV=test mix verify.no_optional_runtime</automated></verify>
  <done>Operators can safely run one bounded prune batch manually everywhere or schedule the identical path when Oban is present.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| operator CLI/Oban job → prune API | External operational invocation selects a private-data mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-14 | Spoofing / Tampering | maintenance entrypoints | medium | mitigate | Closed options and both entrypoints call the same tested library API. |
| T-151-15 | Information Disclosure | task/telemetry output | high | mitigate | Aggregate allowlisted counts only; sentinel exclusion tests. |
| T-151-SC | Tampering | package supply chain | high | mitigate | Optional existing dependency only; no install. |
</threat_model>
<verification>Run the sampler and isolated no-optional-runtime proof.</verification>
<success_criteria>Manual pruning is universal, scheduled pruning is compile-optional, and neither can exceed one bounded batch.</success_criteria>
<output>Create `151-06-SUMMARY.md` in the phase directory.</output>

