---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "08"
type: execute
wave: 6
depends_on: [151-07]
files_modified:
  - lib/mailglass/outbound.ex
  - lib/mailglass/outbound/worker.ex
  - lib/mailglass/outbound/dispatch_outcome.ex
  - test/mailglass/outbound/worker_test.exs
  - guides/jobs.md
  - guides/getting-started.md
  - guides/compatibility-and-deprecations.md
  - docs/api_stability.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
gap_closure: true
requirements: [DISP-02, PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-07/D-13/PRIV-04: every missing Payload, including a historical queued Delivery whose public metadata resembles the former pre-v2.4 shape, settles with the distinct `legacy_payload_missing` terminal reason and never reaches an adapter."
    - "D-09/D-15/PRIV-03: a private metadata sentinel is never copied into a Message, Payload, Event, Delivery error projection, log, or adapter input; the original Delivery metadata remains unchanged as provenance."
    - "D-19: legacy cleanup preserves Delivery/Event history but has no dispatch-compatibility exception; the authoritative ROADMAP PRIV-04 fail-closed contract supersedes the former narrow reader promise."
    - "First and repeated Oban executions return the same terminal cancellation, create at most one terminal Event, and create no private Payload or envelope."
  artifacts:
    - {path: lib/mailglass/outbound.ex, provides: "fail-closed no-Payload settlement with no metadata reconstruction path"}
    - {path: lib/mailglass/outbound/dispatch_outcome.ex, provides: "distinct closed legacy/missing-payload terminal reason"}
    - {path: test/mailglass/outbound/worker_test.exs, provides: "first/repeat job, atomic settlement, adapter-negative, and private-sentinel regression oracle"}
    - {path: test/mailglass/docs_contract_test.exs, provides: "executable no-legacy-dispatch documentation and source contract"}
  key_links:
    - {from: lib/mailglass/outbound.ex, to: lib/mailglass/outbound/dispatch_outcome.ex, via: "Payload.claim/2 not-found becomes the distinct terminal outcome before adapter resolution", pattern: "legacy_payload_missing"}
    - {from: lib/mailglass/outbound/worker.ex, to: lib/mailglass/outbound.ex, via: "first and repeated ID-only jobs receive the same cancel result", pattern: "dispatch_by_id|cancel"}
    - {from: test/mailglass/outbound/worker_test.exs, to: lib/mailglass/adapters/fake.ex, via: "private sentinel and zero-delivery negative control", pattern: "Fake.deliveries"}
---

<objective>
Close verification blocker `cc1a237f` by removing the historical no-Payload metadata dispatch exception and making every missing Payload an atomic, idempotent, operator-actionable terminal fact.

Purpose: Enforce the ROADMAP/PRIV-04 privacy boundary uniformly across modern and historical queued Deliveries without erasing public audit history.
Output: Fail-closed runtime settlement, real worker negative controls, and corrected executable compatibility/operations guidance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/150-private-envelope-and-atomic-durable-enqueue/150-CONTEXT.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-CONTEXT.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-RESEARCH.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-04-SUMMARY.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-07-SUMMARY.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-VERIFICATION.md
@lib/mailglass/outbound.ex
@lib/mailglass/outbound/worker.ex
@lib/mailglass/outbound/dispatch_outcome.ex
@test/mailglass/outbound/worker_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 151-08-01: Fail closed an actual historical no-Payload worker path</name>
  <reversibility rating="costly">Removing the Phase-150 legacy dispatch bridge changes historical queued-job behavior, but ROADMAP PRIV-04 and the explicit gap decision already lock the safer contract.</reversibility>
  <files>lib/mailglass/outbound.ex, lib/mailglass/outbound/worker.ex, lib/mailglass/outbound/dispatch_outcome.ex, test/mailglass/outbound/worker_test.exs</files>
  <read_first>Read ROADMAP Phase 151 success criterion 5 and PRIV-04 first; then D-07/D-09/D-13/D-15/D-19, the Phase-150 D-15/D-16 provenance, verification blocker cc1a237f, `dispatch_by_id/1`, `claim_payload/1`, the legacy overload of `dispatch_claimed_payload/2`, `persist_outcome_multi/3`, `DispatchOutcome`'s closed reason list, Worker error mapping, and existing Phase-151 terminal-route idempotence tests.</read_first>
  <behavior>
    - RED first: create a queued historical Delivery with no Payload and a unique private sentinel in every formerly recognized metadata field; the first ID-only Oban worker execution returns `{:cancel, :legacy_payload_missing}`.
    - The same first execution atomically changes Delivery to terminal failed and appends exactly one failed Event whose bounded projection exposes `legacy_payload_missing`; forced Event failure rolls back the Delivery update.
    - A repeated job returns the identical cancellation without another Event, Delivery mutation, Payload/envelope creation, adapter call, or retry result.
    - Both executions leave the historical Delivery metadata byte-for-byte unchanged for provenance, while the sentinel is absent from Event payloads, last-error projection, adapter captures, telemetry/log capture, and every newly created record.
    - A queued Delivery with no Payload receives the same `legacy_payload_missing` terminal fact whether it is historical or a broken current invariant; metadata shape or content never changes classification.
    - Source-contract assertions prove the former loader, rehydrator, message builder, and associated recipient/header reconstruction helpers no longer exist or participate in dispatch.
  </behavior>
  <action>Write the worker integration and rollback tests before production changes. Per authoritative PRIV-04 and D-13, change `Payload.claim/2` not-found handling so it produces the closed terminal `:legacy_payload_missing` outcome before route resolution or adapter I/O. Apply that reason to every already-queued Delivery with no private Payload, whether historical or a broken current invariant, and derive it only from the absent private row plus Delivery identity—not by reading or validating metadata fields. Persist the reason in both Delivery's bounded last-error projection and Event normalized payload using one prefix-aware `Ecto.Multi`, and make the transition compare-and-set/idempotent so repeats observe the settled fact without appending another Event. Preserve the original Delivery metadata unchanged and do not insert any Payload. Remove `load_legacy_pre_v24_queued_message/1`, `rehydrate_message/1`, `build_rehydrated_message/2`, their recipient/header reconstruction helpers, the `:legacy` dispatch overload, and Worker compatibility clauses that existed solely for that send path. Keep provider I/O outside transactions, exact ID-only args, tenancy restoration, queue options, all extant Payload-backed behavior, and public adapter callback compatibility. D-19's retained obligation is cleanup/history only: the ROADMAP PRIV-04 safety contract and this gap directive supersede its prior dispatch-reader wording.</action>
  <verify>
    <automated>mix test test/mailglass/outbound/worker_test.exs --only phase_151_task:t151_08_01 --warnings-as-errors</automated>
  </verify>
  <done>First and repeat no-Payload jobs cancel with one atomic `legacy_payload_missing` settlement, no private fabrication, and zero adapter calls regardless of metadata shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 151-08-02: Retire the legacy dispatch promise from executable guidance</name>
  <reversibility rating="costly">D-23 publishes the corrected operator and compatibility contract that replaces the former no-Payload dispatch promise.</reversibility>
  <files>guides/jobs.md, guides/getting-started.md, guides/compatibility-and-deprecations.md, docs/api_stability.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>Read D-19/D-23, PRIV-03/PRIV-04, the Phase-151 jobs lifecycle matrix, getting-started async paragraph, compatibility legacy cleanup section and safety/correctness exception, API stability outbound-preflight and dispatch/privacy sections, plus the existing `t151_07_01` docs contract.</read_first>
  <behavior>
    - Contract tests reject every claim that recognizable pre-v2.4 no-Payload rows can be read, reconstructed, or dispatched from Delivery metadata.
    - Jobs and getting-started guidance say any queued Delivery without a private Payload settles terminally, preserves public Delivery/Event history, never invokes the adapter, and requires explicit operator re-authoring/re-enqueueing from an authoritative private source if sending is still desired.
    - Compatibility guidance records the retired bridge as provenance, explains the PRIV-04 safety/correctness correction, and does not promise a dispatch grace window; finite retention still applies only to actual private legacy Payload content/tombstones.
    - API stability keeps `Mailglass.Adapter` callback compatibility while stating that historical queue dispatch compatibility does not override the private payload boundary.
  </behavior>
  <action>Extend the docs contract first so current reader promises fail. Reconcile all four active documents with the runtime tracer: historical no-Payload work settles to the distinct operator-actionable terminal reason, preserves Delivery/Event audit history, does not fabricate a private envelope, and never calls an adapter. Preserve the 14-day `legacy_days` policy only for real content-bearing legacy Payload rows and their cleanup/tombstone lifecycle, not as permission to dispatch public metadata. In the compatibility guide, explicitly use the existing security/correctness exception to record why the bridge was retired and what an operator must do from an authoritative private source. In API stability, distinguish stable adapter callback shape from unsafe historical job behavior; this is not a callback break. Keep at-least-once, uncertainty, retention, tenant-pruning, and no-optional-runtime guidance unchanged.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs --only phase_151_task:t151_08_02 --warnings-as-errors &amp;&amp; MIX_ENV=test mix verify.support_contract.core</automated>
  </verify>
  <done>Every active adoption/operations/compatibility surface describes the same no-Payload fail-closed behavior and executable contracts reject revival of the metadata dispatch bridge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Public Delivery metadata → provider dispatch | Historical public projection data is untrusted as private provider input. |
| Repeated Oban job → terminal settlement | At-least-once execution may re-enter after the first terminal commit. |
| Runtime behavior → adopter guidance | Compatibility wording can cause operators to expect or revive unsafe dispatch behavior. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-19 | Information Disclosure | historical metadata dispatch | high | mitigate | Terminalize before adapter resolution; sentinel proves no Message/Payload/Event/error/log/adapter copy. |
| T-151-20 | Tampering | repeated job settlement | high | mitigate | Prefix-aware atomic compare-and-set creates one Delivery/Event fact and stable cancellation on repeats. |
| T-151-21 | Repudiation | operator diagnosis | medium | mitigate | Distinct closed reason is present in bounded Delivery and Event projections with preserved original metadata provenance. |
| T-151-22 | Denial of Service | historical retries | medium | mitigate | First and repeated executions cancel; no branch returns retryable for absent Payload. |
| T-151-23 | Spoofing | metadata shape | medium | mitigate | Metadata keys cannot opt a row into legacy dispatch or change its reason classification. |
| T-151-SC | Tampering | package supply chain | high | mitigate | No package installs or dependency changes are permitted in this plan. |
</threat_model>

<verification>
Run the focused runtime and docs samplers, then the complete gate without weakening or skipping any lane:

`mix test test/mailglass/outbound/worker_test.exs test/mailglass/outbound/payload_lifecycle_test.exs test/mailglass/outbound/dispatch_outcome_test.exs test/mailglass/docs_contract_test.exs --warnings-as-errors &amp;&amp; MIX_ENV=test mix verify.support_contract.core &amp;&amp; MIX_ENV=test mix verify.no_optional_runtime &amp;&amp; mix test --warnings-as-errors`

Also review the production diff to confirm only planning-approved runtime/tests/docs changed and `.planning/config.json` remains untouched.
</verification>

<success_criteria>
- Verification blocker cc1a237f is falsified by an end-to-end private-sentinel test rather than a source-only assertion.
- Every missing Payload fails closed; historical metadata can never become provider input.
- Delivery and Event settle atomically once; first and repeated jobs cancel identically without adapter I/O or Payload creation.
- Support-contract, no-optional-runtime, focused Phase-151, and full-suite gates all pass.
</success_criteria>

## Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Same prepared input, honest outcomes, bounded private content | 01-08 | COVERED | Plan 08 closes the sole verification blocker without changing the verified seams. |
| REQ | ENVL-03 | Wire-equivalent sync/Oban input | 01 | COVERED | Unchanged actual-job oracle. |
| REQ | DISP-01 | Closed structural outcomes | 02,08 | COVERED | Plan 08 adds one bounded terminal reason. |
| REQ | DISP-02 | Only retryable work retries | 04,08 | COVERED | Missing Payload always cancels, including repeats. |
| REQ | DISP-03 | Uncertain reconciliation, no resend | 04,07 | COVERED | Unchanged. |
| REQ | DISP-04 | Honest provider boundary | 07 | COVERED | Unchanged. |
| REQ | PRIV-01 | Atomic successful scrub | 03-04 | COVERED | Unchanged. |
| REQ | PRIV-02 | Finite retention/prune/recovery | 05-07,08 | COVERED | Legacy retention now applies only to actual private Payload content/tombstones. |
| REQ | PRIV-03 | Public metadata privacy and explicit legacy strategy | 01,04,05,07,08 | COVERED | Plan 08 removes metadata-to-adapter reconstruction and documents operator recovery. |
| REQ | PRIV-04 | Every invalid/missing Payload fails closed without reconstruction | 03-05,08 | COVERED | TDD tracer covers historical and modern no-Payload rows plus repeats. |
| RESEARCH | — | Provider I/O outside transactions; closed reason projection | 02,04,08 | COVERED | Plan 08 terminalizes before provider I/O and reuses bounded projection. |
| RESEARCH | — | Atomic prefix-aware Delivery/Event settlement | 04,08 | COVERED | No-Payload path has no Payload write but settles public facts in one Multi. |
| RESEARCH | — | Private sentinel and real worker negative controls | 01,04,08 | COVERED | Plan 08 adds historical metadata sentinel, rollback, repeat, and zero-adapter/Payload controls. |
| RESEARCH | — | Finite legacy lifecycle without invented envelope | 05-08 | COVERED | Cleanup remains; public-metadata dispatch is retired. |
| RESEARCH | — | Optional dependency isolation and support/full gates | 06-08 | COVERED | Final gate includes support, no-optional, and full suite. |
| CONTEXT | D-01..D-12 | Canonical seam and honest structural outcomes | 01-04 | COVERED | Existing verified plans remain authoritative. |
| CONTEXT | D-13..D-15 | Distinct fail-closed states, privacy, idempotence | 04,08 | COVERED | Plan 08 closes the historical exception. |
| CONTEXT | D-16..D-18 | Finite retention and pruning | 05-07 | COVERED | Unchanged. |
| CONTEXT | D-19 | Narrow reader versus no-invention boundary | 08 | COVERED | ROADMAP PRIV-04/user directive supersedes dispatch compatibility; history/cleanup provenance remains. |
| CONTEXT | D-20 | No blind resend | 04,07,08 | COVERED | Missing Payload cancels first and repeat. |
| CONTEXT | D-21..D-23 | Honest executable guidance | 07-08 | COVERED | Docs contract is corrected with runtime behavior. |

<output>Create `151-08-SUMMARY.md` in the phase directory.</output>
