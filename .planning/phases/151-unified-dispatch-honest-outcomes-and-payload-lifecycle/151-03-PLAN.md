---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/migrations/postgres/v07.ex
  - lib/mailglass/migrations/postgres.ex
  - test/mailglass/v07_migration_test.exs
  - test/mailglass/migration_test.exs
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-04]
must_haves:
  truths:
    - "D-12/D-16: V07 can represent recoverable, dispatching, scrubbed, expired, and terminal-retained tombstones with a bounded reason and claim time."
    - "D-14/D-18: V07 is prefix-qualified, reversible, hostile-search-path safe, and preserves V06 rows/content during upgrade."
  artifacts:
    - {path: lib/mailglass/migrations/postgres/v07.ex, provides: "prefix-safe payload lifecycle columns/constraints/indexes"}
    - {path: test/mailglass/v07_migration_test.exs, provides: "V06→V07 hostile-path lifecycle proof"}
  key_links:
    - {from: lib/mailglass/migrations/postgres.ex, to: lib/mailglass/migrations/postgres/v07.ex, via: "current version dispatcher", pattern: "@current_version 7"}
---
<objective>Add the reversible storage vocabulary required for atomic scrubbing, CAS claims, and bounded retention.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-CONTEXT.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-RESEARCH.md
@lib/mailglass/migrations/postgres/v06.ex
@test/mailglass/v06_migration_test.exs
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 151-03-01: Prove the V07 lifecycle schema under a hostile search path</name>
  <reversibility rating="one-way">D-12/D-16 add adopter-persisted lifecycle state; undo after use requires reconciling live tombstones and claims.</reversibility>
  <files>lib/mailglass/migrations/postgres/v07.ex, lib/mailglass/migrations/postgres.ex, test/mailglass/v07_migration_test.exs, test/mailglass/migration_test.exs</files>
  <read_first>Read D-11..D-19, research payload states/runtime inventory, V06 and its lifecycle test, Postgres dispatcher, migration wrapper conventions, and scratch-schema helpers.</read_first>
  <behavior>
    - V06 rows upgrade without content rewrite and become recoverable.
    - State/reason/claim fields and transition constraints distinguish all D-13 terminal facts while allowing envelope removal only for non-recoverable tombstones.
    - Exact target-prefix catalog appears under hostile search_path; down removes only V07-owned objects and re-up is identical.
  </behavior>
  <action>Create V07 with text lifecycle state constrained to the closed values, bounded nullable reason class, `claimed_at`, and any content-nullability/check change needed for tombstones; keep version/digest/tenant/delivery identifiers. Add an index supporting deterministic expired content-bearing batch selection. Thread `prefix = opts[:prefix]` through every DDL operation and exact down reversal. Bump the internal current version and update migration assertions/wrapper expectations. Build a direct V06→V07→down→up scratch-prefix test with decoys and hostile search_path, exact catalog checks, seeded recoverable content preservation, and no invented legacy payload. The decision is already locked by D-12/D-16, so record the one-way rating without re-asking.</action>
  <verify><automated>mix test test/mailglass/v07_migration_test.exs test/mailglass/migration_test.exs --only phase_151_task:t151_03_01 --warnings-as-errors</automated></verify>
  <done>V07 provides an exact reversible catalog for lifecycle claims and contentless tombstones without weakening prefix safety.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| migration prefix → catalog | Ambient search_path must not redirect lifecycle DDL. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-05 | Elevation of Privilege / Tampering | V07 DDL | high | mitigate | Explicit prefix on every object plus hostile-path/decoy proof. |
| T-151-06 | Information Disclosure | tombstone constraint | high | mitigate | Database constraint requires recoverable content and contentless terminal tombstones. |
| T-151-SC | Tampering | package supply chain | high | mitigate | No installs. |
</threat_model>
<verification>Run focused migration tests, then the V06/V07/migration suites together.</verification>
<success_criteria>The schema can safely represent every required lifecycle fact and CAS claim.</success_criteria>
<output>Create `151-03-SUMMARY.md` in the phase directory.</output>

