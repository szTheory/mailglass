---
phase: 151-unified-dispatch-honest-outcomes-and-payload-lifecycle
plan: "04"
type: execute
wave: 2
depends_on: [151-01, 151-02, 151-03]
files_modified:
  - lib/mailglass/outbound.ex
  - lib/mailglass/outbound/worker.ex
  - lib/mailglass/outbound/payload.ex
  - test/mailglass/outbound/worker_test.exs
  - test/mailglass/outbound/payload_lifecycle_test.exs
autonomous: true
requirements: [DISP-02, DISP-03, PRIV-01, PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-06/D-07/D-08/DISP-02/DISP-03: only retryable returns an Oban error; terminal and uncertain persist distinct actionable facts and cancel without resend."
    - "D-11/D-12/PRIV-01: async success updates Delivery, appends the ledger event, and removes recoverable Payload bytes in one prefix-aware transaction."
    - "D-13..D-15/PRIV-04: missing, corrupt, unsupported, expired, scrubbed, route-mismatched, and in-flight payloads fail closed without metadata reconstruction or double dispatch."
    - "D-09/PRIV-03: new public state contains only allowlisted reason/correlation facts and ID-only job args remain exact."
  artifacts:
    - {path: lib/mailglass/outbound/payload.ex, provides: "tenant-scoped claim and CAS settlement/tombstone changesets"}
    - {path: lib/mailglass/outbound/worker.ex, provides: "exhaustive closed outcome to Oban result mapping"}
    - {path: lib/mailglass/outbound.ex, provides: "claim → external I/O → atomic settlement orchestration"}
  key_links:
    - {from: lib/mailglass/outbound/worker.ex, to: lib/mailglass/outbound/dispatch_outcome.ex, via: "exhaustive class match", pattern: "retryable|terminal|uncertain"}
    - {from: lib/mailglass/outbound.ex, to: lib/mailglass/outbound/payload.ex, via: "short claim and settlement transactions around provider I/O", pattern: "Payload.*claim|Payload.*scrub"}
---
<objective>Turn the Phase 151 tracer and storage vocabulary into honest, race-safe durable execution and atomic success scrubbing.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-CONTEXT.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-RESEARCH.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-01-SUMMARY.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-02-SUMMARY.md
@.planning/phases/151-unified-dispatch-honest-outcomes-and-payload-lifecycle/151-03-SUMMARY.md
@lib/mailglass/outbound/worker.ex
@lib/mailglass/outbound/payload.ex
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 151-04-01: Claim once, call outside transactions, and atomically scrub success</name>
  <reversibility rating="costly">D-11/D-15 define the concurrency and success-settlement protocol across Payload, Delivery, and Event.</reversibility>
  <files>lib/mailglass/outbound.ex, lib/mailglass/outbound/payload.ex, test/mailglass/outbound/payload_lifecycle_test.exs</files>
  <read_first>Read D-11..D-15, research Patterns 3/4 and Pitfalls 2/3/5, Plan 01 canonical seam, V07 contract, Payload fetch, `persist_dispatched_multi/3`, Repo Multi options, and projector optimistic locking.</read_first>
  <behavior>
    - One tenant-scoped short transaction changes recoverable to dispatching; provider I/O occurs after commit.
    - A second short CAS transaction settles accepted work by updating Delivery, appending dispatched, and making Payload a contentless scrubbed tombstone.
    - Forced failure of any settlement step rolls back all three facts; broadcast occurs only after commit.
    - Concurrent/repeated claims produce at most one provider call; a stranded dispatching claim becomes acceptance-uncertain, not retry-safe.
  </behavior>
  <action>Add tenant predicate plus `Tenancy.scope/2` to claim reads and `Repo.multi_opts()` to every write. Implement an atomic recoverable→dispatching claim guarded by lifecycle state/lock semantics; return a closed already-claimed outcome to losers. Call the Plan 01 canonical adapter seam only after the claim transaction ends. Extend the existing success Multi, rather than adding a later write, to CAS the claimed Payload to scrubbed, set envelope to nil/contentless while retaining IDs/version/digest and `scrubbed_at`, update the optimistically locked Delivery, and append the existing dispatched event. Treat stale settlement as a bounded outcome and never issue a second provider call. Add failure injection, query-capture proving no adapter call is inside Repo transaction, repeated/concurrent worker, tenant, and hostile-prefix tests tagged `t151_04_01`.</action>
  <verify><automated>mix test test/mailglass/outbound/payload_lifecycle_test.exs --only phase_151_task:t151_04_01 --warnings-as-errors</automated></verify>
  <done>Durable success is a claim/external-call/atomic-scrub protocol with one provider call and no partial public/private state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 151-04-02: Persist terminal and uncertain outcomes and map Oban exhaustively</name>
  <files>lib/mailglass/outbound.ex, lib/mailglass/outbound/worker.ex, lib/mailglass/outbound/payload.ex, test/mailglass/outbound/worker_test.exs, test/mailglass/outbound/payload_lifecycle_test.exs</files>
  <read_first>Read D-05..D-10/D-13/D-14/D-20, DispatchOutcome exports from Plan 02, worker `perform/1`, Phase 150 legacy gate, Delivery/Event existing `:failed` and `:deferred` values, and public/private sentinel tests.</read_first>
  <behavior>
    - Retryable returns `{:error, safe_reason}` and retains recoverable content with a finite retry horizon.
    - Terminal persists failed/actionable safe reason and returns `{:cancel, safe_reason}`.
    - Uncertain persists deferred/needs-reconciliation plus safe correlation context and returns cancel; rerun never calls the adapter.
    - Every payload failure has a distinct bounded reason; modern work never falls back to Delivery metadata.
  </behavior>
  <action>Replace `terminal_payload_error?/1` and generic retry behavior with exhaustive DispatchOutcome matching. Persist terminal through an idempotent CAS settlement and uncertain through the existing `:deferred` ledger vocabulary with `needs_reconciliation: true`; keep arbitrary raw error text out of Delivery/Event/log/telemetry. Map missing, integrity, unsupported version, expired, scrubbed, route mismatch, and in-flight/claimed states to distinct D-13 reason classes. Permit the Phase 150 legacy reader only for recognizably old queued Delivery rows missing any modern payload marker, and never create a complete envelope from public metadata. Preserve conditional Worker compilation, canonical queue/options, restored tenancy, and exact two ID-only args. Add actual Oban return and no-second-adapter-call tests tagged `t151_04_02`.</action>
  <verify><automated>mix test test/mailglass/outbound/worker_test.exs test/mailglass/outbound/payload_lifecycle_test.exs --only phase_151_task:t151_04_02 --warnings-as-errors</automated></verify>
  <done>Only positively retryable work retries; terminal and uncertain states settle honestly and every invalid payload is actionable and fail-closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| worker IDs → tenant Payload | Persisted identifiers select private content. |
| provider acknowledgement → local settlement | External acceptance cannot join the DB transaction. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-151-07 | Tampering | claim/settle race | high | mitigate | Tenant-scoped CAS, optimistic lock, concurrent negative control, idempotent settlement. |
| T-151-08 | Repudiation | acceptance uncertainty | high | mitigate | Persist reconciliation-required deferred event and cancel automatic resend. |
| T-151-09 | Information Disclosure | lifecycle outcomes | high | mitigate | Safe projection allowlist and private sentinel exclusion across all public surfaces. |
| T-151-10 | Elevation of Privilege | prefix/tenant lookup | high | mitigate | Tenant predicate, Tenancy.scope, per-step Repo.multi_opts, hostile-prefix test. |
| T-151-SC | Tampering | package supply chain | high | mitigate | No installs. |
</threat_model>
<verification>After each sampler, run all Phase 151 focused runtime files plus Phase 150 worker/envelope regressions.</verification>
<success_criteria>DISP-02/03 and PRIV-01/03/04 are executable under races, rollback, hostile prefixes, and private sentinels.</success_criteria>
<output>Create `151-04-SUMMARY.md` in the phase directory.</output>
