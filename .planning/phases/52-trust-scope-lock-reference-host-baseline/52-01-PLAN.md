---
phase: "52"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "reference/host_app/mix.exs"
  - "reference/host_app/config/config.exs"
  - "reference/host_app/config/dev.exs"
  - "reference/host_app/config/runtime.exs"
  - "reference/host_app/lib/mailglass_reference_host/application.ex"
  - "reference/host_app/lib/mailglass_reference_host/repo.ex"
  - "reference/host_app/lib/mailglass_reference_host_web/router.ex"
  - "reference/host_app/priv/repo/migrations/20260527000000_create_mailglass_reference_baseline.exs"
  - "reference/host_app/.env.example"
  - "reference/host_app/README.md"
  - "test/reference_host/boot_contract_test.exs"
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "A committed maintained Phoenix reference host exists at `reference/host_app` and is separate from `test/example` fixture-only content."
    - "The host boot path is Ecto-capable and reproducible from clean checkout using only documented commands."
    - "Host setup docs pin published package constraints and do not require path dependencies."
  artifacts:
    - path: "reference/host_app/README.md"
      provides: "Canonical adopter-facing clean-checkout setup contract for the maintained host baseline"
      contains: "mix deps.get"
    - path: "test/reference_host/boot_contract_test.exs"
      provides: "Deterministic boot contract proof for HOST-01"
      contains: "test "
  key_links:
    - from: "reference/host_app/README.md"
      to: "test/reference_host/boot_contract_test.exs"
      via: "README promises are mechanically enforced by required command and token assertions"
      pattern: "mix deps.get|mix ecto.create|mix ecto.migrate|mix compile --warnings-as-errors"
---

<objective>
Create the HOST-01 maintained reference-host baseline artifact and prove it boots from clean checkout with documented setup and published package constraints.

Purpose: Lock D-01, D-04, and D-05 by creating one thin committed host artifact suitable for the v1.3 trust journey while preserving fixture and smoke-lane boundaries.
Output: `reference/host_app` scaffold + setup docs + boot contract tests proving deterministic clean-checkout startup.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-CONTEXT.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-RESEARCH.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-PATTERNS.md
@.planning/phases/52-trust-scope-lock-reference-host-baseline/52-VALIDATION.md
@.planning/PROJECT.md
@test/example/README.md
@test/mailglass/install/install_first_preview_smoke_test.exs
@.github/workflows/post-publish-smoke.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold `reference/host_app` as the committed maintained baseline host</name>
  <read_first>
    - .planning/phases/52-trust-scope-lock-reference-host-baseline/52-CONTEXT.md
    - .planning/phases/52-trust-scope-lock-reference-host-baseline/52-RESEARCH.md
    - test/example/README.md
    - test/mailglass/install/install_first_preview_smoke_test.exs
  </read_first>
  <files>reference/host_app/mix.exs, reference/host_app/config/config.exs, reference/host_app/config/dev.exs, reference/host_app/config/runtime.exs, reference/host_app/lib/mailglass_reference_host/application.ex, reference/host_app/lib/mailglass_reference_host/repo.ex, reference/host_app/lib/mailglass_reference_host_web/router.ex, reference/host_app/priv/repo/migrations/20260527000000_create_mailglass_reference_baseline.exs, reference/host_app/.env.example</files>
  <action>Create the committed host app at `reference/host_app` (not under `test/example`). Set `mix.exs` dependencies to published package constraints only: `{:mailglass, "~> 1.2"}`, `{:mailglass_admin, "~> 1.2"}`, and `{:mailglass_inbound, "~> 0.2"}` with no `path:` entries. Add Ecto/Postgres baseline wiring (`MailglassReferenceHost.Repo`, `config :mailglass_reference_host, ecto_repos: [...]`) and one migration named `20260527000000_create_mailglass_reference_baseline.exs` that creates a sentinel table `mailglass_reference_baseline` with `inserted_at` timestamp. Keep this host thin: no new feature surfaces, no provider-matrix branches, no `gen_smtp`, and no copied package internals.</action>
  <acceptance_criteria>
    - `reference/host_app/mix.exs` exists and includes `{:mailglass, "~> 1.2"}`, `{:mailglass_admin, "~> 1.2"}`, and `{:mailglass_inbound, "~> 0.2"}`.
    - `reference/host_app/mix.exs` contains no `path:` dependency entries for Mailglass packages.
    - Repo and migration files exist and compile under Elixir with warnings-as-errors.
    - Host path is `reference/host_app` and no new baseline host is added under `test/example`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "{:mailglass, \"~> 1.2\"}" reference/host_app/mix.exs</automated>
    <automated>rg -n "{:mailglass_admin, \"~> 1.2\"}" reference/host_app/mix.exs</automated>
    <automated>rg -n "{:mailglass_inbound, \"~> 0.2\"}" reference/host_app/mix.exs</automated>
    <automated>! rg -n "{:mailglass[^\\n]*path:|{:mailglass_admin[^\\n]*path:|{:mailglass_inbound[^\\n]*path:" reference/host_app/mix.exs</automated>
    <automated>test -f reference/host_app/lib/mailglass_reference_host/repo.ex && test -f reference/host_app/priv/repo/migrations/20260527000000_create_mailglass_reference_baseline.exs</automated>
  </verify>
  <done>The maintained host baseline artifact exists in the canonical location with published-version constraints and Ecto-capable wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Document clean-checkout setup and enforce HOST-01 with boot contract tests</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/52-trust-scope-lock-reference-host-baseline/52-VALIDATION.md
    - reference/host_app/mix.exs
  </read_first>
  <files>reference/host_app/README.md, test/reference_host/boot_contract_test.exs</files>
  <action>Write `reference/host_app/README.md` with one canonical setup lane containing exact commands: `cd reference/host_app`, `mix deps.get`, `mix ecto.create`, `mix ecto.migrate`, `mix compile --warnings-as-errors`, and `mix phx.server`. Include exact boundary statement `Maintained trust-proof host artifact (not a fixture seed)` and `test/example remains fixture-only`. Add `test/reference_host/boot_contract_test.exs` that reads README and asserts required tokens (`mix deps.get`, `mix ecto.create`, `mix ecto.migrate`, `mix compile --warnings-as-errors`, `mix phx.server`, `published package constraints`) plus refutes forbidden drift tokens (`path: "../"`, `test/example baseline`, `provider matrix`).</action>
  <acceptance_criteria>
    - README includes every required setup command and both boundary statements verbatim.
    - Boot contract test exists and includes required+forbidden token assertions.
    - HOST-01 verification command passes with `--warnings-as-errors`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/reference_host/boot_contract_test.exs --warnings-as-errors</automated>
    <automated>rg -n "Maintained trust-proof host artifact \(not a fixture seed\)" reference/host_app/README.md</automated>
    <automated>rg -n "test/example remains fixture-only" reference/host_app/README.md</automated>
    <automated>rg -n "mix deps.get" reference/host_app/README.md</automated>
    <automated>rg -n "mix ecto.create" reference/host_app/README.md</automated>
    <automated>rg -n "mix ecto.migrate" reference/host_app/README.md</automated>
    <automated>rg -n "mix compile --warnings-as-errors" reference/host_app/README.md</automated>
    <automated>rg -n "mix phx.server" reference/host_app/README.md</automated>
  </verify>
  <done>Adopters have one deterministic maintained-host setup path, and drift from that contract fails closed in tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository checkout -> reference host bootstrap | A clean clone must produce the same baseline boot behavior on every machine. |
| maintained host artifact -> fixture seed | Maintained host and test fixture semantics must remain explicitly separate. |
| release smoke lane -> deep trust baseline | New host baseline must not replace or weaken the fast no-ecto release smoke gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `reference/host_app/mix.exs` dependency source | mitigate | Enforce published package constraints and reject `path:` dependencies via contract assertions. |
| T-52-02 | Spoofing | `reference/host_app` identity vs fixture artifacts | mitigate | Require explicit README token `Maintained trust-proof host artifact (not a fixture seed)` and fixture-boundary token. |
| T-52-03 | Repudiation | setup reproducibility claims | mitigate | Encode exact setup commands in README and assert them in `boot_contract_test.exs`. |
| T-52-04 | Denial of Service | phase scope hijacked by smoke debt remediation | mitigate | Keep fast smoke lane untouched; track OPS debt separately per D-08 and refute off-scope expansion tokens. |
</threat_model>

<verification>
- `mix test test/reference_host/boot_contract_test.exs --warnings-as-errors`
- `cd reference/host_app && mix deps.get && mix ecto.create && mix ecto.migrate && mix compile --warnings-as-errors`
- `rg -n "{:mailglass, \"~> 1.2\"}|{:mailglass_admin, \"~> 1.2\"}|{:mailglass_inbound, \"~> 0.2\"}" reference/host_app/mix.exs`
- `rg -n "Maintained trust-proof host artifact \(not a fixture seed\)|test/example remains fixture-only" reference/host_app/README.md`
</verification>

<success_criteria>
HOST-01 is satisfied with a committed maintained `reference/host_app` baseline that boots from clean checkout using documented commands and published package constraints, while preserving fixture and smoke-lane boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/52-trust-scope-lock-reference-host-baseline/52-01-SUMMARY.md`
</output>
