---
phase: "41"
plan: "03"
type: execute
wave: 3
depends_on:
  - "41-01"
  - "41-02"
files_modified:
  - "mailglass_inbound/lib/mailglass_inbound/internal/replay.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex"
  - "mailglass_inbound/lib/mailglass_inbound/inbound_records.ex"
  - "mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex"
  - "mailglass_inbound/priv/repo/migrations/20260506220000_add_sendgrid_fingerprint_and_replay_contract_fields.exs"
  - "mailglass_inbound/README.md"
  - "mailglass_inbound/docs/api_stability.md"
  - "mailglass_inbound/docs/sendgrid_ingress.md"
  - "mailglass_inbound/test/mailglass_inbound/replay_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs"
autonomous: true
requirements:
  - INGRESS-02
  - STORE-02
must_haves:
  truths:
    - "SendGrid duplicate ingress collapses on provider-specific raw-MIME fingerprinting rather than overloading `provider_message_id`."
    - "Replay reruns stored canonical/evidence truth against the originally matched mailbox by default and does not pretend to be a fresh receive."
    - "Docs and proof lanes make the second-provider and execution story honest without widening the stable public surface."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/internal/replay.ex"
      provides: "Package-local replay seam that reuses stored inbound truth and appends replay execution lineage"
      contains: "def replay"
    - path: "mailglass_inbound/priv/repo/migrations/20260506220000_add_sendgrid_fingerprint_and_replay_contract_fields.exs"
      provides: "Provider-specific duplicate anchor and execution-lineage schema support"
      contains: "create unique_index"
    - path: "mailglass_inbound/docs/sendgrid_ingress.md"
      provides: "Canonical adopter/operator doc for SendGrid ingress, mailbox execution semantics, and replay honesty"
      contains: "SendGrid"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/internal/replay.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/inbound_records.ex"
      via: "replay reuses stored canonical and evidence truth rather than re-ingesting provider payloads"
      pattern: "inbound_record|inbound_evidence"
    - from: "mailglass_inbound/docs/sendgrid_ingress.md"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      via: "docs show the real SendGrid mount/config and execution behavior"
      pattern: "SendGrid|replay|mailbox"
---

<objective>
Extend replay, persistence, and second-provider contract proof without re-receive ambiguity.

Purpose: Deliver D-41-10 through D-41-13 and D-41-23 through D-41-25 by making SendGrid duplicate handling provider-specific, replay truthful, and the shipped second-provider story mechanically enforced across docs and tests.
Output: A package-local replay seam, provider-specific SendGrid dedupe support, docs, and proof lanes that lock the Phase 41 posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-CONTEXT.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-RESEARCH.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-PATTERNS.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-01-PLAN.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-02-PLAN.md
@mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
@mailglass_inbound/lib/mailglass_inbound/inbound_records.ex
@mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex
@mailglass_inbound/test/mailglass_inbound/replay_test.exs
@mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
@mailglass_inbound/docs/api_stability.md

<interfaces>
From mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex:
```elixir
schema "mailglass_inbound_execution_runs" do
  field :source, Ecto.Enum, values: [:fresh, :replay]
  field :mailbox, :string
  field :outcome, Ecto.Enum, values: [:no_match, :accept, :ignore, :reject, :bounce, :failed]
end
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add provider-specific SendGrid dedupe and truthful replay execution</name>
  <files>mailglass_inbound/lib/mailglass_inbound/internal/replay.ex, mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex, mailglass_inbound/lib/mailglass_inbound/inbound_records.ex, mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex, mailglass_inbound/priv/repo/migrations/20260506220000_add_sendgrid_fingerprint_and_replay_contract_fields.exs, mailglass_inbound/test/mailglass_inbound/replay_test.exs, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
  <behavior>
    - Test 1: SendGrid duplicate fresh ingress collapses on `(tenant_id, provider, raw_mime_fingerprint)` without overloading `provider_message_id`.
    - Test 2: duplicate fresh ingress does not execute the mailbox a second time.
    - Test 3: replay loads stored canonical record plus evidence row, defaults to the mailbox identity recorded by the most recent fresh matched execution lineage row for that record, and appends a `source: :replay` execution row instead of re-ingesting provider data.
    - Test 4: records whose fresh history only contains `:no_match` or predates execution-lineage capture fail with an explicit operator-facing replay error rather than rerouting silently.
    - Test 5: replay does not re-normalize provider payloads by default.
  </behavior>
  <action>Add the SendGrid-specific duplicate anchor at the persistence boundary, ideally by storing or deriving a deterministic raw-MIME fingerprint in evidence or a linked internal field and enforcing uniqueness scoped by tenant/provider. Preserve the current Postmark duplicate path untouched. Introduce one package-local replay seam, not a new stable public API, that loads stored canonical and evidence truth, resolves the default mailbox from the most recent fresh matched execution lineage row for that record, invokes the same internal execution runner used for fresh ingress, and appends a replay execution row. If no prior matched mailbox exists, or the historical record predates execution-lineage capture, return an explicit replay error instead of rerouting silently. Replay must not insert a new inbound record, must not create a fake fresh receive, and must not silently re-normalize provider payloads by default.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/replay_test.exs test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors</automated>
  </verify>
  <done>SendGrid duplicate truth is provider-specific and replay is an honest rerun over stored inbound truth rather than a fake new receive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish honest second-provider docs and contract proof</name>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/api_stability.md, mailglass_inbound/docs/sendgrid_ingress.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs, mailglass_inbound/test/mailglass_inbound/replay_test.exs</files>
  <behavior>
    - Test 1: docs describe the real SendGrid ingress setup, including the raw-MIME requirement and the Phase 41 shared-secret/basic-auth posture.
    - Test 2: docs state clearly that persistence happens before mailbox execution, and that execution outcomes do not drive provider retries.
    - Test 3: docs and contract tests reject any claim that replay is re-ingest, that signed multipart verification shipped, that replay reroutes silently, or that provider internals became public API.
  </behavior>
  <action>Update the package README and `docs/api_stability.md`, and add `docs/sendgrid_ingress.md`, so the public story matches the shipped Phase 41 slice precisely. Document the SendGrid-specific ingress caveat, the second-provider normalization posture, post-commit mailbox execution, duplicate semantics, and replay honesty. Keep replay documented as an operator/package capability, not as a newly widened stable public API. Tighten docs-contract assertions so future drift cannot reintroduce “replay as fresh receive,” “signed SendGrid verification supported,” silent replay rerouting, or public-provider-extensibility claims.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/replay_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The shipped second-provider and replay story is honest, mechanically enforced, and still keeps the stable public surface narrow.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider retries/manual resend -> duplicate receive truth | External delivery retries can re-hit ingress and must not create new receive truth or a second mailbox run. |
| stored inbound truth -> replay | Replay must operate over immutable stored truth and remain distinguishable from fresh provider ingress. |
| docs -> adopters and operators | Readers will treat docs as contract, so claims about security, replay, and public surfaces must stay exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | Repudiation | SendGrid duplicate collapse and replay lineage | mitigate | Use provider-specific fingerprint dedupe and append explicit `source: :replay` lineage so retries and replays are never confused with fresh receives. |
| T-41-08 | Tampering | replay implementation | mitigate | Replay must load stored canonical/evidence truth and default to original mailbox execution rather than re-normalizing or rerouting silently. |
| T-41-09 | Information Disclosure / Contract drift | docs and stability inventory | mitigate | Keep provider internals internal and document the exact supported security and replay posture with strict docs-contract tests. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/replay_test.exs test/mailglass_inbound/ingress/plug_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs test/mailglass_inbound/replay_test.exs --warnings-as-errors`
</verification>

<success_criteria>
Phase 41 ships with truthful SendGrid duplicate handling, honest replay semantics over stored inbound truth, and docs/tests that lock the second-provider execution story without widening the stable public contract.
</success_criteria>

<output>
After completion, create `.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-03-SUMMARY.md`
</output>
