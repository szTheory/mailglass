---
phase: "41"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
  - "mailglass_inbound/lib/mailglass_inbound/ingress/request.ex"
  - "mailglass_inbound/test/mailglass_inbound/ingress/sendgrid_provider_test.exs"
  - "mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs"
autonomous: true
requirements:
  - INGRESS-02
must_haves:
  truths:
    - "Authentic SendGrid inbound requests can enter `mailglass_inbound` through one first-party Plug path without claiming signed multipart verification support."
    - "Verified SendGrid requests normalize into the already-locked `%MailglassInbound.InboundMessage{}` shape using raw MIME plus provider envelope metadata."
    - "Misconfigured SendGrid ingress that omits the raw MIME `email` part fails with explicit operator-helpful outcomes and does not continue into persistence."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex"
      provides: "Sealed SendGrid verifier/normalizer that returns canonical inbound message plus evidence facts"
      contains: "def verify!"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/request.ex"
      provides: "Provider input shape that can carry either raw-body JSON or parsed multipart ingress facts honestly"
      contains: "defstruct"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      provides: "Shared ingress choke point extended to support SendGrid"
      contains: "provider_module(:sendgrid)"
    - path: "mailglass_inbound/test/mailglass_inbound/ingress/sendgrid_provider_test.exs"
      provides: "Automated proof of auth, raw MIME requirement, and canonical normalization discipline"
      contains: "test "
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex"
      via: "SendGrid ingress verifies and normalizes through the sealed provider seam"
      pattern: "verify|normalize"
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/inbound_message.ex"
      via: "provider output is constrained to the locked canonical struct"
      pattern: "InboundMessage"
---

<objective>
Implement SendGrid inbound parse verification and normalization into the canonical `InboundMessage`.

Purpose: Deliver D-41-01 through D-41-09 by adding a truthful SendGrid ingress path that handles multipart/raw-MIME provider variance without widening the stable public contract or overstating signed-verification support.
Output: A sealed SendGrid provider, ingress Plug support, and tests proving auth, MIME requirements, and canonical normalization.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-CONTEXT.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-RESEARCH.md
@.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-PATTERNS.md
@.planning/phases/40-postmark-ingress-and-replayable-persistence/40-01-PLAN.md
@mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/request.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/providers/postmark.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
@mailglass_inbound/lib/mailglass_inbound/inbound_message.ex

<interfaces>
From mailglass_inbound/lib/mailglass_inbound/inbound_message.ex:
```elixir
@type t :: %__MODULE__{
        tenant_id: String.t() | nil,
        provider: provider() | nil,
        provider_message_id: String.t() | nil,
        message_id: String.t() | nil,
        envelope_recipient: String.t() | nil,
        from: [address()],
        to: [address()],
        cc: [address()],
        bcc: [address()],
        reply_to: [address()],
        subject: String.t() | nil,
        headers: %{optional(String.t()) => [String.t()]},
        sent_at: DateTime.t() | nil,
        received_at: DateTime.t() | nil,
        text_body: String.t() | nil,
        html_body: String.t() | nil,
        attachments: [attachment()]
      }
```

From mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex:
```elixir
@callback verify!(request :: MailglassInbound.Ingress.Request.t(), config :: map()) :: map()
@callback normalize(request :: MailglassInbound.Ingress.Request.t()) :: map()
```

Planned addition in `mailglass_inbound/lib/mailglass_inbound/ingress/request.ex`:
```elixir
defstruct [
  :provider,
  :raw_body,
  :headers,
  :params,
  :raw_mime,
  :content_type
]
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the sealed SendGrid provider around explicit auth and raw-MIME-first normalization</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex, mailglass_inbound/lib/mailglass_inbound/ingress/request.ex, mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex, mailglass_inbound/test/mailglass_inbound/ingress/sendgrid_provider_test.exs</files>
  <behavior>
    - Test 1: SendGrid verification uses the Phase 41 shared-secret/basic-auth posture and fails closed on missing or invalid credentials.
    - Test 2: normalization requires the raw `email` MIME part and returns an explicit misconfiguration error when it is missing.
    - Test 3: canonical output keeps `provider_message_id` nil, maps RFC `Message-ID` only to `message_id`, and sources `envelope_recipient` from the provider envelope.
    - Test 4: provider-only multipart fields, spam/auth verdicts, attachment blobs, and raw MIME stay in evidence rather than widening `%InboundMessage{}`.
  </behavior>
  <action>Create an explicit provider input seam for inbound ingress, preferably `MailglassInbound.Ingress.Request`, so the provider contract can carry either Postmark-style raw-body JSON or SendGrid multipart facts without pretending both providers share the same raw-body shape. Update `MailglassInbound.Ingress.Provider` to accept that request struct. Then implement `MailglassInbound.Ingress.Providers.Sendgrid` as a sealed internal provider module parallel to the Postmark provider. Verification for this phase must follow D-41-01 through D-41-04 exactly: support one explicit shared-secret/basic-auth posture, do not claim signed multipart verification, and fail closed on unverifiable requests. Implement normalization around the parsed multipart params, extracted raw `email` MIME field, and provider envelope metadata per D-41-06 through D-41-09. Parse the MIME into the already-locked `%MailglassInbound.InboundMessage{}` shape, preserve the struct boundary, and keep provider-only facts in evidence. If the ingress is configured without raw MIME delivery, reject with operator-helpful guidance instead of silently degrading.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/sendgrid_provider_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The package has a truthful SendGrid verifier/normalizer that handles multipart variance while preserving the locked canonical message contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the ingress Plug to support SendGrid without widening the adopter-facing path</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
  <behavior>
    - Test 1: the Plug accepts `provider: :sendgrid`, builds the new provider request shape from multipart params plus raw MIME, verifies first, and only then resolves tenant context.
    - Test 2: SendGrid auth failure, missing MIME payload, tenant failure, and config failure map to explicit HTTP outcomes.
    - Test 3: successful SendGrid ingress produces the same compact internal handoff shape used by persistence without executing mailboxes yet.
  </behavior>
  <action>Extend `MailglassInbound.Ingress.Plug` so it can serve Postmark and SendGrid through one honest first-party seam. Preserve the verify-first ordering from Phase 40, but make the multipart seam explicit: for SendGrid, the Plug must construct the new provider request struct from parsed multipart params, the extracted `email` part, and request headers, then hand that request to `MailglassInbound.Ingress.Providers.Sendgrid`. Keep the adopter-facing story as one mount path with one clear SendGrid configuration lane. Do not add signed verification branches, mailbox execution, or duplicate/persistence logic in this plan beyond the existing handoff contract.</action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/ingress/sendgrid_provider_test.exs --warnings-as-errors</automated>
  </verify>
  <done>SendGrid requests can enter through the first-party ingress Plug, verify and normalize honestly, and stop at the same internal handoff boundary Phase 40 established.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external SendGrid multipart request -> ingress plug | Untrusted network input crosses into package code and must be authenticated before tenant, persistence, or routing work happens. |
| provider multipart fields -> canonical message | Provider-specific data is converted into the stable inbound contract that downstream routing and mailbox logic will trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Spoofing | `MailglassInbound.Ingress.Providers.Sendgrid.verify!/3` | mitigate | Enforce the explicit shared-secret/basic-auth phase posture and fail closed on missing or bad credentials. |
| T-41-02 | Tampering | SendGrid multipart/raw-MIME parsing | mitigate | Require the raw `email` MIME part and reject misconfigured best-effort parsing so canonical truth is derived from the honest source of record. |
| T-41-03 | Information Disclosure | canonical normalization | mitigate | Keep multipart-only facts, spam/auth verdicts, raw MIME, and attachment blobs in evidence rather than the public struct. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/sendgrid_provider_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/plug_test.exs test/mailglass_inbound/ingress/sendgrid_provider_test.exs --warnings-as-errors`
</verification>

<success_criteria>
`mailglass_inbound` can accept authentic SendGrid inbound requests through one honest ingress path and produce the locked canonical `%InboundMessage{}` without widening the public contract or pretending signed multipart verification is already supported.
</success_criteria>

<output>
After completion, create `.planning/phases/41-sendgrid-ingress-and-mailbox-routing/41-01-SUMMARY.md`
</output>
