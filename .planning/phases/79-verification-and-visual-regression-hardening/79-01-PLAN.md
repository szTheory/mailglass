---
phase: 79-verification-and-visual-regression-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/docs/design-system.md
autonomous: true
requirements:
  - VERIF-03

must_haves:
  truths:
    - "bash mailglass_admin/scripts/check-conformance.sh exits 0 on the current codebase"
    - "The design-system.md audit-loop section explicitly describes the before/after LLM-critique ritual — how to compare against the Phase 74 baseline and which GAP rows should show improvement"
    - "git diff --exit-code mailglass_admin/priv/static/ exits 0 (bundle clean)"
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "Five-gate conformance grep script (BADGE, TYPE, BOLD, GAP, HEX)"
      contains: "set -euo pipefail"
    - path: "mailglass_admin/docs/design-system.md"
      provides: "Expanded audit-loop ritual prose with before/after comparison instructions"
      contains: "Phase 74 baseline"
  key_links:
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/lib/ --include='*.ex'"
      via: "grep -rE with --include='*.ex'"
      pattern: "grep -rE.*mailglass_admin/lib"
    - from: "mailglass_admin/docs/design-system.md"
      to: "6-pillar rubric"
      via: "audit-loop section cross-reference"
      pattern: "Phase 74 baseline"
---

<objective>
Create the committed conformance shell script that encodes the five design-system gates, and expand the design-system.md audit-loop section to make the before/after LLM-critique ritual an explicit, repeatable procedure. Both deliverables satisfy VERIF-03.

Purpose: Permanently commit the five inline greps from Phase 76-06 into a runnable, CI-safe script; and document the screenshot→LLM-critique comparison loop clearly enough that any maintainer can reproduce it from scratch.
Output: `mailglass_admin/scripts/check-conformance.sh` (new, executable); `mailglass_admin/docs/design-system.md` (audit-loop section expanded, lines ~123–139).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md

<interfaces>
<!-- Analog script to copy structure from exactly -->
<!-- scripts/check_motion_conformance.sh (full 39 lines) -->

Key structural invariants:
  - shebang: #!/usr/bin/env bash
  - set -euo pipefail (line 2 after header comments)
  - LIB="mailglass_admin/lib" (no trailing slash)
  - errors=0 counter; errors=$((errors + 1)) per failure
  - on-failure: print matches (NO --quiet), echo FAIL: to stderr, increment counter
  - final block: if [[ $errors -gt 0 ]]; then echo FAIL: ...; exit 1; fi
  - success: echo "OK: design-system conformance clean."

Five gate names and grep patterns (from 76-06-SUMMARY.md):
  BADGE-GATE: grep -rE 'defp badge_class' "$LIB" --include="*.ex"
  TYPE-GATE:  grep -rE 'text-(sm|base|xs)' "$LIB" --include="*.ex" | grep -v 'text-base-content'
  BOLD-GATE:  grep -rE 'font-(medium|semibold)' "$LIB" --include="*.ex"
  GAP-GATE:   grep -rE 'gap-(3|4|6)' "$LIB" --include="*.ex"
  HEX-GATE:   grep -rE '#[0-9a-fA-F]{3,6}' "$LIB" --include="*.ex"

text-base-content Footgun-6 exclusion: TYPE-GATE pipes through `grep -v 'text-base-content'`
This is mandatory — without it every file using text-base-content produces a false failure.

Bundle-clean gate (already wired in verify.preview alias):
  git diff --exit-code mailglass_admin/priv/static/

Design-system.md audit loop prose at lines ~123–139 (expand in-place):
  Existing bullet: "or hand them to a multimodal model with this checklist as the rubric"
  → expand to make the before/after comparison step explicit:
    - Reference the Phase 74 baseline (gitignored tmp/ui-audit/ at the time of the audit)
    - Name which GAP rows the comparison should show improved:
        GAP-01/03/05/06 (badge consolidation), GAP-13 (support-card hierarchy),
        GAP-07 (390px orientation strip), GAP-21 (h1/h2 hierarchy)
    - State the comparison rubric: the 6-pillar checklist already present in lines 104–121
    - Note the IA change at /ops/mail/ (now lands on Operator Overview, not Deliveries list)
      so reviewers don't mistake it for a regression
  Voice: "thoughtful maintainer" — specific, composed, imperative. Match existing bullet-point
  format. No new tooling. Prose only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mailglass_admin/scripts/check-conformance.sh</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - scripts/check_motion_conformance.sh (full analog — copy structure exactly)
    - .planning/phases/76-component-library-and-design-system-hardening/76-06-SUMMARY.md (lines 70–77 — five gate names and results that confirm zero violations on current codebase)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md (Pattern Assignments section — five gate implementations verbatim)
  </read_first>
  <action>
Create `mailglass_admin/scripts/check-conformance.sh` mirroring the structure of `scripts/check_motion_conformance.sh` exactly. The new script is scoped to `LIB="mailglass_admin/lib"` with `--include="*.ex"` on every grep (no CSS file — design-system gates apply only to HEEx in .ex files).

Header comment block: cite VERIF-03, D-07, Phase 76-06 as the source of the five gate patterns. Reference that `text-base-content` is a DaisyUI semantic color token (Footgun-6) and must be excluded from the TYPE-GATE.

Five gates in order (use these exact gate-label names matching 76-06-SUMMARY labels):

BADGE-GATE: grep for `defp badge_class` — must return zero results (proves `Components.status_badge/1` is the only status→color definition). FAIL message: "BADGE-GATE — defp badge_class found; route through Components.status_badge/1"

TYPE-GATE: grep for `text-(sm|base|xs)` then pipe `| grep -v 'text-base-content'` — the exclusion is mandatory. FAIL message: "TYPE-GATE — raw text-scale utility found (use text-label/body/heading/display)"

BOLD-GATE: grep for `font-(medium|semibold)` — must return zero results. FAIL message: "BOLD-GATE — faux-bold token found (use font-bold or default only)"

GAP-GATE: grep for `gap-(3|4|6)` — must return zero results. FAIL message: "GAP-GATE — off-grid gap token found (use gap-sm/md/lg)"

HEX-GATE: grep for `#[0-9a-fA-F]{3,6}` — must return zero results. FAIL message: "HEX-GATE — hard-coded hex color found (use semantic tokens)"

On any failure: print the matching lines to stdout (no --quiet), echo the FAIL: label to stderr, increment `errors`.

Final block: if errors > 0, exit 1 with summary count. On all clean: `echo "OK: design-system conformance clean."`

After creating the file, run `chmod +x mailglass_admin/scripts/check-conformance.sh` then run `bash mailglass_admin/scripts/check-conformance.sh` from the repo root. The script MUST exit 0 — validate by running, not by grep-proof (per `feedback_validate_credo_by_running_it`).

Also run `git diff --exit-code mailglass_admin/priv/static/` to confirm the bundle is still clean entering this phase.
  </action>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance.sh && git diff --exit-code mailglass_admin/priv/static/</automated>
  </verify>
  <acceptance_criteria>
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0 and prints "OK: design-system conformance clean."
    - Running it again produces the same result (idempotent)
    - The script file exists at `mailglass_admin/scripts/check-conformance.sh` and is executable
    - Each of the five gates is present in the script: BADGE-GATE, TYPE-GATE (with grep -v text-base-content), BOLD-GATE, GAP-GATE, HEX-GATE
    - The TYPE-GATE grep includes `| grep -v 'text-base-content'` — visible in the file
    - `git diff --exit-code mailglass_admin/priv/static/` exits 0 (bundle not dirtied)
  </acceptance_criteria>
  <done>check-conformance.sh exists, is executable, exits 0 on current codebase, and bundle-clean gate also exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Expand design-system.md audit-loop ritual prose (D-02)</name>
  <files>mailglass_admin/docs/design-system.md</files>
  <read_first>
    - mailglass_admin/docs/design-system.md (read the full file — especially lines 104–159: the 6-pillar rubric, the current audit-loop section ~123–139, and the Known Limitations / GAP-22 block ~141–159)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md (lines 52–62: severity rubric; lines 117–122: sev-4 rows GAP-01/03/05/06; line 155: GAP-13)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-PATTERNS.md (design-system.md section: what D-02 requires and voice guidance)
    - .planning/phases/79-verification-and-visual-regression-hardening/79-RESEARCH.md (Area 1: Audit-Matrix Re-Run — existing prose description, what expansion is needed)
  </read_first>
  <action>
Edit the "Visual audit loop" section of `mailglass_admin/docs/design-system.md` (lines ~123–139) to make the before/after LLM-critique comparison a documented, repeatable ritual. This is prose-only — no new tooling, no script changes.

The existing bullet reads: "or hand them to a multimodal model with this checklist as the rubric: accent overuse? faux-bold? non-flat shadow? off-grid spacing? contrast ≥ 4.5:1?"

Expand this (or add a new adjacent bullet) to cover:

1. Before/after comparison step: The Phase 74 baseline represents the pre-v1.7 state. When comparing, open the Phase 74 PNG set (stored in the maintainer's local `tmp/ui-audit/` at baseline time, or regenerated from the Phase 74 git state) alongside the current run. Do not commit either PNG set.

2. Which GAP rows should show visible improvement in the comparison:
   - GAP-01/03/05/06: badge color consistency (badge colors now come from `Components.status_badge/1`; no phantom `:suppressed`, no `:badge-error` for all replay types)
   - GAP-13: support-card hierarchy (Tier 1 full cards for non-zero/actionable; Tier 2 compact row for zero states)
   - GAP-07: 390px orientation strip visible on deliveries surface
   - GAP-21: single h1 "Operator overview" heading on landing

3. The IA change at `/ops/mail/`: the deliveries URL now lands on the Operator Overview, not the Deliveries list. This is intentional — a reviewer comparing deliveries-at-landing screenshots should expect a different page, not flag it as a regression.

4. The comparison rubric: reference the 6-pillar checklist already defined above this section (lines 104–121) as the scoring framework. The multimodal model prompt: supply the current screenshot, the Phase 74 baseline screenshot, the 6-pillar rubric, and the GAP row list above; ask for a per-pillar before/after score and a list of remaining issues if any.

Match the existing voice exactly: imperative, specific, "thoughtful maintainer." No fluffy adjectives. Use the existing bullet-point structure. Do not insert headings or structural changes outside the audit-loop section.

After editing, confirm the section reads coherently with the surrounding prose and the 6-pillar rubric cross-reference is accurate.
  </action>
  <verify>
    <automated>grep -c "Phase 74 baseline" mailglass_admin/docs/design-system.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 74 baseline" mailglass_admin/docs/design-system.md` returns >= 1
    - The audit-loop section explicitly names at least GAP-01, GAP-07, GAP-13 as gap rows to check improvement on
    - The section notes the IA change at /ops/mail/ (Operator Overview on landing)
    - The 6-pillar rubric is referenced as the comparison scoring framework
    - No fenced code blocks, no new tooling references, no script additions — prose only
    - The Known Limitations / GAP-22 block (~141–159) is untouched
  </acceptance_criteria>
  <done>design-system.md audit-loop section is expanded with explicit before/after comparison ritual; prose matches existing voice and does not introduce new tooling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell script → filesystem | check-conformance.sh reads mailglass_admin/lib/ and exits; no network, no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — shell script and doc edit only |

**Minimal threat surface assessment:** This plan creates a read-only shell script (grep + exit) and edits documentation prose. No new auth surfaces, no new network endpoints, no new data surfaces. No PII is touched (telemetry/fixture invariant unaffected). No stable seam is touched (asset-serving seam, router macro, Auth behaviour all untouched). No ASVS L1 categories require new mitigations.
</threat_model>

<verification>
From repo root:

```
bash mailglass_admin/scripts/check-conformance.sh
git diff --exit-code mailglass_admin/priv/static/
grep -c "Phase 74 baseline" mailglass_admin/docs/design-system.md
```

All three must succeed (exit 0 or count ≥ 1). Then run the full suite:

```
cd mailglass_admin && mix test --seed 0 --warnings-as-errors
```
</verification>

<success_criteria>
- check-conformance.sh exists, is executable, exits 0 on current codebase
- All five gates (BADGE, TYPE with text-base-content exclusion, BOLD, GAP, HEX) are present
- Bundle-clean gate (`git diff --exit-code mailglass_admin/priv/static/`) exits 0
- design-system.md audit-loop section is expanded with explicit before/after comparison ritual
- mix test --seed 0 passes (no regressions)
</success_criteria>

<output>
Create `.planning/phases/79-verification-and-visual-regression-hardening/79-01-SUMMARY.md` when done
</output>
