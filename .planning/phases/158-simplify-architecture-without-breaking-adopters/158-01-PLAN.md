---
phase: 158-simplify-architecture-without-breaking-adopters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/runtime.ex
  - lib/mailglass/config.ex
  - lib/mailglass/adapters/fake.ex
  - test/scripts/architecture_boundary_test.exs
  - test/mailglass/runtime_test.exs
  - test/mailglass/config_test.exs
  - mix.exs
  - mailglass_inbound/mix.exs
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-04]
must_haves:
  truths:
    - "Core and inbound xref graphs have no compile-connected cycles, and a checked-in guard fails for a forbidden package edge or compile-cycle fixture."
    - "One additive Mailglass.Runtime value validates/caches the first core configuration path while Mailglass.Config preserves the existing schema/accessor behavior."
    - "The existing Config and Outbound public surfaces stay untouched; no admin/operator UI file is modified."
  artifacts:
    - path: "lib/mailglass/runtime.ex"
      provides: "Opaque validated core runtime value and explicit cache lifecycle"
    - path: "test/scripts/architecture_boundary_test.exs"
      provides: "Fail-closed, non-vacuous compile-cycle and package-direction contract"
  key_links:
    - from: "lib/mailglass/config.ex"
      to: "lib/mailglass/runtime.ex"
      via: "stable Config accessor delegates to validated runtime value"
      pattern: "Mailglass.Runtime"
    - from: "test/scripts/architecture_boundary_test.exs"
      to: "mix xref graph --format cycles --label compile-connected"
      via: "command output is interpreted as a gate, with a negative control"
      pattern: "compile-connected"
---

<objective>
Deliver the architecture tracer: a current configuration request enters one validated Runtime value through the unchanged Config façade, while the repository gains an executable guard that catches the observed compile cycle and forbidden package direction.

Purpose: Make the first ownership improvement observable end to end before wider extraction.
Output: Runtime tracer, compatibility characterization, cycle removal, and anti-vacuity architecture gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-CONTEXT.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-RESEARCH.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-PATTERNS.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-VALIDATION.md
@lib/mailglass/config.ex
@lib/mailglass/adapters/fake.ex
@lib/mailglass/events.ex
@lib/mailglass/repo.ex
</context>

<interfaces>
Keep all `Mailglass.Config` public functions, application-env keys/defaults, validation exception types, schema cache invalidation behavior relied on by tests, and `Mailglass.Outbound` public verbs/return values. `Mailglass.Runtime` is additive/internal and is not a required new adopter API. Core must not import `MailglassInbound.*`; inbound keeps its independent Mix project. Do not modify `mailglass_admin/**` or operator UI source.
</interfaces>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Characterize architecture contracts, add a non-vacuous guard, and remove the live core cycle</name>
  <reversibility rating="costly">The checked-in dependency guard becomes a maintenance contract; it must accurately encode allowed package direction before CI consumes it.</reversibility>
  <files>test/scripts/architecture_boundary_test.exs, mix.exs, mailglass_inbound/mix.exs, lib/mailglass/config.ex, lib/mailglass/adapters/fake.ex, test/mailglass/config_test.exs</files>
  <behavior>
    - ARCH-01: root and inbound `mix xref graph --format cycles --label compile-connected` commands produce no cycles after the change.
    - The guard rejects a fixture/mutated core-to-inbound production reference and rejects a synthetic compile-connected SCC; it also proves an allowed declared inbound-to-core port passes.
    - Current Config defaults, invalid-config errors, and `:persistent_term` reset behavior are characterized before moving implementation.
  </behavior>
  <action>Write the architecture guard and its negative controls first. It must inspect only production source for package direction, run both Mix xref commands, and fail closed on command/parser error or non-empty cycle output; do not use grep-only “clean tree” proof. Then break the actual five-file core SCC by removing Config's compile-time default dependency on the Fake/Ecto implementation (defer the runtime/default resolution through the validated Runtime/Config boundary) while retaining the exact effective default and all documented Config behavior. Keep Boundary declarations explicit; do not silence cycles through xref exclusions or additional `no_warn_undefined` entries.</action>
  <verify><automated>mix test test/scripts/architecture_boundary_test.exs test/mailglass/config_test.exs --warnings-as-errors &amp;&amp; mix xref graph --format cycles --label compile-connected &amp;&amp; (cd mailglass_inbound &amp;&amp; mix xref graph --format cycles --label compile-connected)</automated></verify>
  <done>The observed SCC is removed, the guard is provably capable of failing, and existing Config behavior is characterized rather than assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce the first validated Runtime-to-Config compatibility path</name>
  <files>lib/mailglass/runtime.ex, lib/mailglass/config.ex, test/mailglass/runtime_test.exs, test/mailglass/config_test.exs</files>
  <behavior>
    - ARCH-02: one Runtime constructor validates the same core app env/defaults used by the tracer; Config delegates its migrated accessor without return-shape drift.
    - A cold cache, invalid config, and an explicit test env override either reload or fail exactly as characterized; no stale configuration is silently served.
    - Runtime exposes no mutable raw app-env map to inbound or adopter code.
  </behavior>
  <action>Write Runtime/Config parity tests first. Add an opaque `%Mailglass.Runtime{}` plus explicit bootstrap/current/reset-for-test seam following the existing schema-cache hygiene. Move one complete, low-risk configuration slice (schema plus its validation/default) behind Runtime and make the existing Config accessor delegate. Preserve boot behavior and the public `Mailglass.Config` function; do not force callers to adopt Runtime or bulk-migrate unrelated configuration reads in this tracer.</action>
  <verify><automated>mix test test/mailglass/runtime_test.exs test/mailglass/config_test.exs --warnings-as-errors</automated></verify>
  <done>A real caller reaches validated configuration through Runtime while the v2 Config façade remains indistinguishable at the exercised edge.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Application environment → Runtime cache | Misconfiguration and test overrides cross into process-global validated state. |
| Core source → inbound source | A reverse import would break independently released package direction. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-158-01 | Tampering | Runtime cache | high | mitigate | Invalid/cold/override characterization and explicit reset/bootstrap lifecycle. |
| T-158-02 | Denial of Service | compiler graph | medium | mitigate | xref SCC gate with negative-control proof; no suppression workaround. |
| T-158-03 | Elevation of Privilege | sibling boundary | high | mitigate | production-only reverse-edge guard and declared allowlist. |
</threat_model>

<verification>Run focused architecture/Runtime/Config tests, both xref commands, no-optional compiles in both roots, and formatter checks. The new guard must be tested with an intentional failing fixture before it protects CI.</verification>
<success_criteria>ARCH-01 has executable, non-vacuous evidence; ARCH-02/04 have one production-quality Runtime façade tracer; no public v2 or admin/operator UI surface changes.</success_criteria>
<output>Create `.planning/phases/158-simplify-architecture-without-breaking-adopters/158-01-SUMMARY.md` when done.</output>
