---
phase: 158-simplify-architecture-without-breaking-adopters
plan: 02
type: execute
wave: 2
depends_on: [158-01]
files_modified:
  - lib/mailglass/runtime.ex
  - lib/mailglass/config.ex
  - lib/mailglass/application.ex
  - lib/mailglass/repo.ex
  - lib/mailglass/clock.ex
  - lib/mailglass/suppression.ex
  - lib/mailglass/tracking.ex
  - test/mailglass/runtime_test.exs
  - test/mailglass/config_test.exs
  - test/mailglass/application_test.exs
autonomous: true
requirements: [ARCH-02, ARCH-04]
must_haves:
  truths:
    - "Core configuration is validated once into a complete Runtime value at boot, with explicit cold-cache/test-reset behavior."
    - "Existing Mailglass.Config and application-env façades keep their keys, defaults, return values, and failure semantics."
    - "Runtime consumers do not re-read or independently validate the same core configuration slice."
  artifacts:
    - path: "lib/mailglass/runtime.ex"
      provides: "Complete validated core Runtime owner"
    - path: "lib/mailglass/config.ex"
      provides: "Stable compatibility delegates over Runtime"
  key_links:
    - from: "lib/mailglass/application.ex"
      to: "lib/mailglass/runtime.ex"
      via: "Runtime bootstraps before supervisors start"
      pattern: "Runtime"
---

<objective>Expand the tracer into the one authoritative core Runtime configuration owner without changing any v2 Config contract.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-CONTEXT.md
@.planning/phases/158-simplify-architecture-without-breaking-adopters/158-RESEARCH.md
@lib/mailglass/runtime.ex
@lib/mailglass/config.ex
@lib/mailglass/application.ex
@test/support/sandbox_ownership.ex</context>
<interfaces>`Mailglass.Config` remains the stable public configuration façade. Preserve `config :mailglass` shape, adapter registry normalization, schema opt-out, boot errors, test cache invalidation, and all public accessors. Inbound continues to validate only `:mailglass_inbound` configuration.</interfaces>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Make Runtime the single validated owner of core configuration</name>
<files>lib/mailglass/runtime.ex, lib/mailglass/config.ex, lib/mailglass/application.ex, test/mailglass/runtime_test.exs, test/mailglass/config_test.exs, test/mailglass/application_test.exs</files>
<behavior>- ARCH-02: Runtime validates full known core config/defaults once at boot and exposes typed/narrow accessors.
- Config calls return the same values/errors for adapter registry, schema, tenancy, tracking, compliance, rate-limit, and webhook slices.
- Invalid config never warms a partial cache; cache reset plus Application env override cannot leak prior values.</behavior>
<action>Write parity and failure-matrix tests first. Move full-schema validation, normalization, and cache lifecycle into Runtime, preserving `Config.new!/1` where it is a public/tested validation API as a delegating compatibility entry. Change Application startup to warm Runtime before children. Convert Config accessors to Runtime delegates in small reviewed groups; avoid a new public struct dependency in consumers. Use one explicit test-only reset seam instead of direct arbitrary persistent-term writes.</action>
<verify><automated>mix test test/mailglass/runtime_test.exs test/mailglass/config_test.exs test/mailglass/application_test.exs --warnings-as-errors</automated></verify>
<done>Runtime is the only full core app-env validation/cache owner and every existing Config contract remains equivalent.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Route core runtime consumers through narrow Runtime/Config accessors</name>
<files>lib/mailglass/repo.ex, lib/mailglass/clock.ex, lib/mailglass/suppression.ex, lib/mailglass/tracking.ex, test/mailglass/runtime_test.exs, test/mailglass/config_test.exs</files>
<behavior>- ARCH-02: migrated runtime consumers do not directly read the same `:mailglass` config slice after Runtime owns it.
- Repo prefix/repo absence, clock injection, suppression store selection, and tracking endpoint behavior retain their current externally visible results.</behavior>
<action>Add characterization tests for each migrated read before changing it. Replace direct application-env access only for slices already represented in Runtime/Config; leave intentionally test-only/injected operational seams documented for later ownership rather than masking them. Preserve per-call explicit overrides, prefix precedence, typed ConfigError, and runtime (not compile-time) configurability.</action>
<verify><automated>mix test test/mailglass/runtime_test.exs test/mailglass/config_test.exs test/mailglass/repo_prefix_test.exs test/mailglass/tracking_test.exs --warnings-as-errors</automated></verify>
<done>Core consumers use one validated source of truth without changing call-site override or public façade behavior.</done>
</task>
</tasks>
<verification>Run focused Runtime/Config/Repo/Tracking tests, root no-optional compile, xref guard, and formatter. Do not touch inbound configuration or UI files.</verification>
<success_criteria>ARCH-02 is complete for core: one Runtime owner, stable Config façade, explicit test cache lifecycle, and no broad public runtime migration.</success_criteria>
<output>Create `.planning/phases/158-simplify-architecture-without-breaking-adopters/158-02-SUMMARY.md` when done.</output>
