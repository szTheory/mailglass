---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 04
type: execute
wave: 4
depends_on: [153-03]
files_modified:
  - dev/mailglass/generated_host/host_template.ex
  - dev/mailglass/generated_host/journey.ex
  - dev/mailglass/generated_host/checkpoint.ex
  - test/generated_host/http_journey_test.exs
autonomous: true
requirements: [ADOPT-04]
must_haves:
  truths:
    - "A real signed provider webhook crosses the generated host HTTP router, verifies before persistence, and produces the durable event/ledger result after commit (D-12)."
    - "A real delivery-derived signed one-click URL receives two RFC 8058 HTTP POSTs that both return the privacy response and converge to one canonical event/suppression pair (D-13)."
    - "A later same-tenant/address/stream public send is suppressed while transactional and unrelated-stream controls remain sendable (D-14)."
  artifacts:
    - path: dev/mailglass/generated_host/host_template.ex
      provides: "Adopter-owned HTTP router and signature/auth configuration"
    - path: test/generated_host/http_journey_test.exs
      provides: "Signed webhook and one-click HTTP integration oracle"
  key_links:
    - from: "HTTP webhook request"
      to: "published ingress/router plug"
      via: "host router configured with real signing secret"
      pattern: "postmark"
    - from: "delivery-derived one-click URL"
      to: "public outbound preflight"
      via: "POST replay then new matching/isolated sends"
      pattern: "one_click"
---

<objective>
Exercise the feedback and unsubscribe journey exclusively through generated-host HTTP and public send boundaries (D-12 through D-14).

Purpose: Prove real adopter routing, signature, transaction, replay, privacy, and enforcement wiring.
Output: HTTP host template, feedback/one-click stages, and durable/scoped assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-03-SUMMARY.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-VERIFICATION.md
@reference/host_app/lib/mailglass_reference_host_web/router.ex
@lib/mailglass/compliance/unsubscribe_controller.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ingest signed provider feedback through HTTP</name>
  <files>dev/mailglass/generated_host/host_template.ex, dev/mailglass/generated_host/journey.ex, test/generated_host/http_journey_test.exs</files>
  <behavior>
    - "A correctly signed HTTP request to the adopter-configured provider route returns the documented status and commits one durable feedback event/ledger result (D-12)."
    - "A forged signature fails before tenant resolution/durable mutation/capture, providing a zero-effect control without exposing the secret or payload."
  </behavior>
  <action>Write RED HTTP positive/forged tests and commit `test(153-04): require signed feedback over host HTTP`. Add the public provider ingress/router mount and host-configured signing secret to HostTemplate. Send requests with `curl` or a normal HTTP client to the booted generated endpoint; do not source-load repo proof modules, inject fake persistence/execution, or call internal ingest functions. Poll qualified durable facts only after the HTTP response commits and emit hashes/counts only. Commit `feat(153-04): prove signed feedback through adopter router`.</action>
  <verify>
    <automated>mix test test/generated_host/http_journey_test.exs --only journey:feedback --warnings-as-errors &amp;&amp; DEP_MODE=local bash scripts/generated_host_proof.sh --stage feedback</automated>
  </verify>
  <done>Valid feedback commits through the real HTTP route, forged feedback has zero effects, and neither path uses a private/test seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay one-click HTTP and prove exact suppression scope</name>
  <files>dev/mailglass/generated_host/journey.ex, dev/mailglass/generated_host/checkpoint.ex, test/generated_host/http_journey_test.exs</files>
  <behavior>
    - "The journey derives the signed URL from an originating stream delivery, POSTs the RFC 8058 form twice over HTTP, and observes two byte-empty successes with one canonical event/suppression pair (D-13)."
    - "A subsequent matching public send is rejected before capture; transactional and unrelated-stream sends reach capture (D-14)."
  </behavior>
  <action>Add the failing replay/enforcement oracle and commit `test(153-04): require HTTP one-click convergence and scope`. Obtain the one-click URL from the actual generated-host delivery/message surface and POST the required body twice. Assert exact response status/body, canonical row counts, then call the public outbound boundary for matching, transactional, and unrelated-stream messages. Check zero capture growth only for matching scope. Extend the manifest with response/body-length, pair counts, and scoped send outcomes without the URL/token/address. Commit `feat(153-04): prove one-click replay and scoped enforcement`.</action>
  <verify>
    <automated>mix test test/generated_host/http_journey_test.exs --only journey:one_click --warnings-as-errors &amp;&amp; DEP_MODE=local bash scripts/generated_host_proof.sh --stage feedback-unsubscribe &amp;&amp; bash scripts/check_generated_host_proof.sh --checkpoint tmp/generated-host-proof/checkpoint.json</automated>
  </verify>
  <done>HTTP replay converges privately and only the intended tenant/address/stream scope is blocked on the next public send.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internet-shaped HTTP request → webhook router | Signature verification must precede tenant/durable work. |
| signed one-click URL → suppression mutation | Tokens are untrusted and private; replay must converge atomically and scope exactly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-153-13 | Spoofing | provider webhook | high | mitigate | Real signed/forged HTTP controls through published router (D-12). |
| T-153-14 | Tampering | one-click scope | high | mitigate | Delivery-derived URL, exact pair counts, matching/transactional/other-stream sends (D-13, D-14). |
| T-153-15 | Repudiation | replay convergence | medium | mitigate | Two response observations plus canonical durable cardinality after commit. |
| T-153-16 | Information Disclosure | signature/token evidence | high | mitigate | Never serialize secret, raw request, address, URL, or token; retain hashes/status/counts only. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Status |
|---|---|---|---|
| REQ | ADOPT-04 | Signed feedback and one-click enforcement | COVERED |
| CONTEXT | D-12..D-14 | HTTP-only feedback, replay, exact suppression scope | COVERED |
| RESEARCH | Pattern 4 | Public router proof | COVERED |

<verification>
Run the feedback and feedback-unsubscribe host stages against a booted endpoint and validate sanitized evidence.
</verification>

<success_criteria>
Both external HTTP contracts are proven end to end, including forgery, replay, privacy, and scope controls.
</success_criteria>

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-04-SUMMARY.md` when done.
</output>
