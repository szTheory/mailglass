---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/mailglass.gen.migration.ex
  - test/mailglass/shipped_migration_divergence_test.exs
  - scripts/generated_host_proof.sh
  - dev/mailglass/generated_host/journey.ex
  - dev/mailglass/generated_host/host_template.ex
  - dev/mailglass/generated_host/checkpoint.ex
  - scripts/check_generated_host_proof.sh
  - test/generated_host/journey_contract_test.exs
autonomous: true
requirements: [ADOPT-01]
must_haves:
  truths:
    - "A new stock Phoenix/Ecto/Postgres host, not a repository reference host, installs package-shaped local artifacts through public package APIs (D-01, D-02, D-03, D-04)."
    - "The generated wrapper runs the complete current Mailglass migration chain in a unique non-public schema and records the current migration version there only (D-05)."
    - "The proof emits a bounded, deterministic, credential- and PII-free checkpoint that fails closed when an expected stage or package identity is missing."
  artifacts:
    - path: scripts/generated_host_proof.sh
      provides: "Disposable stock-host lifecycle and local/Hex dependency-mode entrypoint"
      contains: "mix phx.new"
    - path: lib/mix/tasks/mailglass.gen.migration.ex
      provides: "Public complete migration wrapper generator"
      contains: "Mailglass.Migration.up"
    - path: dev/mailglass/generated_host/checkpoint.ex
      provides: "Bounded proof manifest encoder"
    - path: scripts/check_generated_host_proof.sh
      provides: "Fail-closed manifest validator"
  key_links:
    - from: scripts/generated_host_proof.sh
      to: dev/mailglass/generated_host/journey.ex
      via: "fresh-host creation followed by host-owned journey execution"
      pattern: "generated_host"
    - from: "generated host migration"
      to: lib/mailglass/migration.ex
      via: "generated Ecto wrapper calls public Mailglass.Migration.up/0 and down/0"
      pattern: "Mailglass\\.Migration\\.(up|down)"
---

<objective>
Establish the production-quality generated-host tracer: package-shaped local install, complete public migration, boot, and sanitized checkpoint evidence (D-01 through D-05).

Purpose: Prove the package and schema architecture before expanding the host journey.
Output: Shared generated-host runner, host template/orchestrator, complete migration wrapper, and manifest validator.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-CONTEXT.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RESEARCH.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-PATTERNS.md
@lib/mailglass/migration.ex
@lib/mailglass/migrations/postgres.ex
@scripts/consumer_install_smoke.sh

<interfaces>
`Mailglass.Migration.up(opts \\ [])`, `down(opts \\ [])`, and `migrated_version(opts \\ [])` are the public schema boundary. The generated wrapper must use `up/0` and `down/0`, allowing `Mailglass.Config.schema/0` to supply the configured prefix. `Mailglass.Migrations.Postgres.current_version/0` is internal and may be observed only by repository tests, never generated-host code.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one package-shaped stock host through complete schema migration</name>
  <files>lib/mix/tasks/mailglass.gen.migration.ex, test/mailglass/shipped_migration_divergence_test.exs, scripts/generated_host_proof.sh, dev/mailglass/generated_host/journey.ex, dev/mailglass/generated_host/host_template.ex</files>
  <behavior>
    - "RED: the generator emits `up/0` and `down/0` delegating to the public migration facade and a stock Ecto host creates every V01-V07 table plus the exact version in one unique non-public schema (D-05)."
    - "RED: local mode builds and unpacks package tarballs/allowlisted contents before injecting path dependencies; no generated dependency points at the repository working tree, git, `test/support`, or private source (D-02, D-03)."
    - "RED: the fresh host is created with Ecto/Postgres enabled, migrates, boots, and records package lock identities (D-01, D-04)."
  </behavior>
  <action>Write the failing migration and runner contract first and commit it as `test(153-01): require complete generated-host migration tracer`. Replace `migration_body/0` with the documented stable `Mailglass.Migration.up/0` and `down/0` wrapper, retaining strict argument parsing and idempotent wrapper discovery. Implement `scripts/generated_host_proof.sh` with `set -euo pipefail`, `DEP_MODE=local|hex`, explicit validated work/artifact directories, deterministic cleanup plus opt-in keep-on-failure, and stock `mix phx.new` without `--no-ecto`. In local mode, run the established package checks/builds, unpack package-shaped core/admin/inbound artifacts, and inject only those extracted directories; in Hex mode require exact versions and reject path/git dependency lock entries. Have `HostTemplate` write only adopter-owned Repo/config/application/router/proof sources after generation, and have `Journey` run install, generated wrapper, create/migrate, qualified catalog/version assertions, and boot. Generated code may call documented public APIs only per D-03. Commit the passing implementation as `feat(153-01): add package-shaped generated-host migration tracer`.</action>
  <verify>
    <automated>mix test test/mailglass/shipped_migration_divergence_test.exs test/generated_host/journey_contract_test.exs --warnings-as-errors &amp;&amp; DEP_MODE=local KEEP_HOST_ON_FAILURE=false bash scripts/generated_host_proof.sh --stage migrate</automated>
  </verify>
  <done>A clean generated host resolves extracted package artifacts, runs the complete public wrapper into its unique schema, proves no Mailglass tables/version leaked to public, boots, and commits the RED/GREEN pair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratchet the checkpoint schema and privacy boundary</name>
  <files>dev/mailglass/generated_host/checkpoint.ex, scripts/check_generated_host_proof.sh, test/generated_host/journey_contract_test.exs</files>
  <behavior>
    - "The manifest binds schema version, dependency mode, source SHA, package lock identities, database/schema sentinel hashes, ordered stage outcomes, command hashes, and overall status."
    - "The validator rejects absent/duplicate/out-of-order stages, unhashed recipient/body/token/secret material, credentials, database URLs, path/git dependencies in Hex mode, and a success claim containing a failed stage."
  </behavior>
  <action>Start with failing manifest/validator mutation cases and commit `test(153-01): define generated-host checkpoint contract`. Implement a deterministic JSON encoder and shell validator using allowlisted keys and SHA-256 hashes; preserve diagnostic IDs/counts but never serialize raw recipient, message, captured provider input, webhook secret/signature, one-click token/URL, environment, or database URL. Make the runner validate before reporting success or uploading evidence. Commit `feat(153-01): enforce bounded generated-host proof manifests`.</action>
  <verify>
    <automated>mix test test/generated_host/journey_contract_test.exs --warnings-as-errors &amp;&amp; bash scripts/check_generated_host_proof.sh --checkpoint tmp/generated-host-proof/checkpoint.json</automated>
  </verify>
  <done>A successful tracer produces a validator-approved, deterministic, PII-free checkpoint; each mutated privacy or stage-integrity fixture fails nonzero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository packages → disposable host | Only package-allowlisted artifacts and public compile/runtime APIs may cross. |
| configured schema → Postgres catalogs | A hostile/default search path must not redirect migration or version observations. |
| host observations → retained proof | Evidence must be useful without exposing message, recipient, token, credential, or database data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-153-01 | Tampering | local dependency source | high | mitigate | Build/unpack package-shaped artifacts, inspect lock source identity, and ban repository-root/test-support loading (D-02, D-03). |
| T-153-02 | Tampering | schema migration | high | mitigate | Qualified catalog checks, unique non-public schema, complete table inventory, and version assertion (D-05). |
| T-153-03 | Spoofing | shallow reference-host success | high | mitigate | Require fresh `phx.new`, executable migrate/boot stages, and package lock identity (D-01). |
| T-153-04 | Information Disclosure | checkpoint JSON | high | mitigate | Allowlisted hashed manifest plus mutation tests and blocking validator. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Status |
|---|---|---|---|
| GOAL | — | Clean production-shaped host and drift-blocking proof | COVERED |
| REQ | ADOPT-01 | Install, schema-isolated migration, public boot | COVERED |
| CONTEXT | D-01..D-05 | Fresh host, package boundary, package family, full schema | COVERED |
| RESEARCH | W0 | Generated-host runner, manifest validator, migration wrapper repair | COVERED |

<verification>
Run the focused ExUnit contracts and the real local package-shaped migrate stage; validate its emitted checkpoint.
</verification>

<success_criteria>
The thinnest package-to-host-to-Postgres path is executable and honest before any later runtime proof is added.
</success_criteria>

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-01-SUMMARY.md` when done.
</output>
