---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 07
type: execute
wave: 7
depends_on: [153-06]
files_modified:
  - scripts/resolve_release_packages.exs
  - test/scripts/release_package_resolver_test.exs
  - .planning/release-target.json
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - test/scripts/linked_release_concurrency_test.exs
  - test/scripts/release_trigger_recovery_test.exs
  - .github/workflows/post-publish-smoke.yml
  - test/mailglass/publish/post_publish_smoke_contract_test.exs
  - .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
autonomous: true
requirements: [REL-17]
must_haves:
  truths:
    - "D-19: The changed-package resolver derives the exact core/admin/inbound release set from package-owned source and packaged-document changes since each package tag."
    - "D-20: Core and admin are selected together when their public compatibility surface changes; inbound remains independently releasable."
    - "D-21: Protected publication cannot begin until the package-shaped local journey, repository gates, package checks, release-target agreement, and candidate-SHA CI are green."
    - "D-22: The post-publish workflow reuses the same generated-host journey with exact published Hex versions rather than maintaining a weaker smoke path."
  artifacts:
    - path: scripts/resolve_release_packages.exs
      provides: Deterministic changed-package and linked-release selection
    - path: .planning/release-target.json
      provides: Machine-readable package/version target constrained by resolver output
    - path: .github/workflows/publish-hex.yml
      provides: Protected prepublication gate and ordered package publication inputs
    - path: .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
      provides: Candidate SHA, target, gate, and workflow evidence ledger
  key_links:
    - from: scripts/resolve_release_packages.exs
      to: .planning/release-target.json
      via: Exact set comparison before release execution
    - from: .github/workflows/publish-hex.yml
      to: dev/mailglass/generated_host_proof.sh
      via: DEP_MODE=local package-shaped prepublication journey
    - from: .github/workflows/post-publish-smoke.yml
      to: dev/mailglass/generated_host_proof.sh
      via: Exact-version DEP_MODE=hex invocation
---

<objective>
Make release selection and every prepublication gate deterministic and wire exact-Hex verification without publishing yet (D-19 through D-22).

Purpose: Remove hardcoded package assumptions and prove the candidate exactly as a downstream host will consume it before the one-way release step.
Output: Changed-package resolver, protected workflow gates, shared local/Hex journey wiring, and initialized evidence ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-CONTEXT.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RESEARCH.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-06-SUMMARY.md
@.planning/phases/archive/148-package-owned-release-automation-and-pre-publish-acceptance-gate/148-RELEASE-PROOF.md
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@scripts/consumer_install_smoke.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the exact release package set</name>
  <files>scripts/resolve_release_packages.exs, test/scripts/release_package_resolver_test.exs, .planning/release-target.json</files>
  <behavior>
    - D-19: Changes are compared from each package's most recent tag and selected only when package-owned source, metadata, or packaged documentation changed.
    - D-20: A public core/admin compatibility change selects both packages; an inbound-only change selects inbound without core/admin.
    - Unrelated repository files select no package, missing or ambiguous tag ancestry fails closed, and target entries missing from or extra to the derived set are rejected.
  </behavior>
  <action>Write table-driven resolver tests first using temporary git histories that cover core-only, admin-only/public-coupling, inbound-only, packaged-doc, unrelated-file, first-release, divergent-tag, and target-mismatch cases. Implement a deterministic Elixir resolver whose allowlists follow actual package manifests and package ownership, whose comparison base is each package's own latest reachable version tag, and whose linked-release rule represents the public core/admin compatibility surface. Keep inbound independent. Emit stable JSON for workflow consumption and validate `.planning/release-target.json` has exactly the derived packages while leaving release-please responsible for the version values. Do not infer releases from one global path list or hardcode this phase's versions. Commit RED as `test(153-07): define changed-package resolution` and GREEN as `feat(153-07): resolve release packages deterministically`.</action>
  <verify>
    <automated>mix test test/scripts/release_package_resolver_test.exs --seed 0</automated>
  </verify>
  <done>D-19 and D-20 are proven across package-owned docs/source, linked core/admin, independent inbound, tag ancestry, and exact target-set agreement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put all reversible candidate gates ahead of publication</name>
  <files>.github/workflows/release-please.yml, .github/workflows/publish-hex.yml, test/scripts/linked_release_concurrency_test.exs, test/scripts/release_trigger_recovery_test.exs, .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</files>
  <behavior>
    - D-21: The publish workflow cannot reach a Hex publication job until the resolver agrees with the release target, the local package-shaped journey passes, `mix ci` passes, package checks pass, and protected CI is green for the exact candidate SHA.
    - Release-please output and dispatch recovery preserve the exact selected package set without duplicate or concurrent publication.
    - The evidence ledger records candidate SHA, derived set, target versions, commands, timestamps, and protected workflow URLs before publication.
  </behavior>
  <action>Extend the workflow contract tests first. Wire the resolver into release-please and publish inputs, retain the established concurrency and recovery protections, and make the publish job depend on an explicit prepublish-summary job that runs the full `DEP_MODE=local` generated-host journey, repository CI, package checks for every selected package, exact target agreement, and protected candidate-SHA status. Initialize `153-RELEASE-PROOF.md` with a reproducible evidence table and record only observed results. Ensure all gates in this task are reversible and complete before any Hex credential or publication step is invoked. Do not add a manual `mix hex.publish`, tag bypass, or branch-protection bypass. Commit RED as `test(153-07): define protected prepublish gate` and GREEN as `ci(153-07): gate publication on complete candidate proof`.</action>
  <verify>
    <automated>mix test test/scripts/linked_release_concurrency_test.exs test/scripts/release_trigger_recovery_test.exs --seed 0 &amp;&amp; DEP_MODE=local dev/mailglass/generated_host_proof.sh --stage all</automated>
  </verify>
  <done>D-21 is encoded in workflow dependencies and the proof ledger can show every local/package/protected-CI gate green for one immutable candidate SHA before publication is reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reuse the journey for exact-version Hex verification</name>
  <files>.github/workflows/post-publish-smoke.yml, test/mailglass/publish/post_publish_smoke_contract_test.exs, .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</files>
  <behavior>
    - D-22: Post-publish smoke receives exact core/admin/inbound versions from the protected release output and runs the same complete generated-host stages under `DEP_MODE=hex`.
    - The workflow rejects floating constraints, omitted selected packages, unexpected extra packages, and any local/path dependency in Hex mode.
    - The proof ledger has dedicated post-publish rows for registry visibility, package integrity, install/compile, migration, async/sync parity, negatives, HTTP, readiness/operator, docs, and HexDocs.
  </behavior>
  <action>Write the workflow contract tests first, then replace the shallow post-publish consumer path with an exact-version invocation of the shared Phase 153 journey. Pass the resolver-approved release outputs into `HEX_CORE_VERSION`, `HEX_ADMIN_VERSION`, and `HEX_INBOUND_VERSION` only when selected, reject non-exact constraints and local overrides, wait with bounded retries for Hex registry/package propagation, and preserve per-stage evidence in the proof ledger. This task wires and tests the post-publish verifier but does not trigger publication. Commit RED as `test(153-07): define exact Hex journey contract` and GREEN as `ci(153-07): reuse generated host journey after publish`.</action>
  <verify>
    <automated>mix test test/mailglass/publish/post_publish_smoke_contract_test.exs --seed 0</automated>
  </verify>
  <done>D-22 is ready for the protected release: exact published versions drive the same full journey and every expected post-publish evidence row is defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → release resolver | Tags and changed paths determine which public packages can be published. |
| repository workflow → Hex publication | Protected CI output crosses into a credentialed release environment. |
| release output → fresh Hex host | Exact versions fetched from a public registry become executable dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-25 | Tampering | Changed-package resolver | high | mitigate | Per-package tag bases, explicit ownership allowlists, linked-release tests, and exact target-set comparison fail closed. |
| T-153-26 | Repudiation | Release evidence | high | mitigate | Record immutable candidate SHA, commands, timestamps, outputs, and protected workflow URLs. |
| T-153-27 | Elevation of Privilege | Publish workflow | critical | mitigate | Publication job depends on protected prepublish summary and environment protection; bypass paths are contract-tested absent. |
| T-153-28 | Spoofing | Hex dependency selection | high | mitigate | Accept only exact resolver-approved versions and reject path/floating dependencies in Hex mode. |
| T-153-SC | Tampering | Package installs | high | mitigate | No new npm, pip, or cargo dependency is introduced; Elixir packages are built from existing audited manifests and verified by package checks. |
</threat_model>

<verification>
- `mix test test/scripts/release_package_resolver_test.exs test/scripts/linked_release_concurrency_test.exs test/scripts/release_trigger_recovery_test.exs test/mailglass/publish/post_publish_smoke_contract_test.exs --seed 0`
- `DEP_MODE=local dev/mailglass/generated_host_proof.sh --stage all`
- `mix ci` and package checks for exactly the resolver-selected package set.
- Protected CI status for the immutable candidate SHA is recorded green in `153-RELEASE-PROOF.md`.
</verification>

<success_criteria>
- REL-17 release selection is derived rather than manually guessed (D-19, D-20).
- Every reversible local/package/protected gate is green before a publication job is reachable (D-21).
- Exact Hex releases will execute the identical complete downstream journey (D-22).
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Package-shaped local proof gates the protected release | COVERED — Tasks 1-2. |
| GOAL | Exact published packages repeat the same journey | COVERED — Task 3. |
| REQ | REL-17 | COVERED — Tasks 1-3 and completed by Plan 08. |
| RESEARCH | Deterministic changed-package resolver and shared local/Hex journey | COVERED — Tasks 1-3. |
| CONTEXT | D-19 | COVERED — Task 1. |
| CONTEXT | D-20 | COVERED — Task 1. |
| CONTEXT | D-21 | COVERED — Task 2. |
| CONTEXT | D-22 | COVERED — Task 3. |

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-07-SUMMARY.md` when done.
</output>
