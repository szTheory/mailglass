---
phase: 153-generated-host-proof-docs-and-release-gate
plan: 08
type: execute
wave: 8
depends_on: [153-07]
files_modified:
  - .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
autonomous: true
requirements: [REL-17]
must_haves:
  truths:
    - "D-23: Publication occurs only through the protected release workflow after all Plan 07 proof is green for the exact candidate SHA; credentials may pause that publication but cannot weaken or skip gates."
    - "D-22: Exact published Hex versions repeat the complete generated-host journey and match the resolver-approved release set."
    - "The final evidence ledger names immutable package versions, checksums, workflow runs, HexDocs URLs, and every journey stage outcome."
  artifacts:
    - path: .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
      provides: Auditable prepublication, publication, and exact-Hex postpublication evidence
  key_links:
    - from: .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
      to: .github/workflows/publish-hex.yml
      via: Candidate SHA and protected publication run URL
    - from: .github/workflows/post-publish-smoke.yml
      to: Hex.pm exact versions
      via: Resolver-approved immutable version outputs
    - from: Hex.pm exact versions
      to: dev/mailglass/generated_host_proof.sh
      via: DEP_MODE=hex full downstream journey
---

<objective>
Perform the one-way protected publication only after complete reversible proof, then verify the exact public Hex artifacts end to end (D-22, D-23).

Purpose: Make the irreversible release action the final, narrow step and leave an independently auditable record of what users can actually install.
Output: Protected package publication and completed exact-Hex release proof ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-CONTEXT.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RESEARCH.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-07-SUMMARY.md
@.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md
@.planning/release-target.json
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the resolver-approved set through the protected workflow</name>
  <files>.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</files>
  <precondition>The Plan 07 release proof records the immutable candidate SHA, exact resolver/target agreement, complete local generated-host journey, `mix ci`, all selected package checks, security/validation gates, and protected candidate-SHA CI as green.</precondition>
  <action>Re-read the Plan 07 evidence ledger and mechanically assert every required row is green for one candidate SHA and the resolver-approved package/version set. Trigger only the repository's protected release workflow and monitor its concurrency, environment protection, ordered package publication, and immutable outputs. Do not invoke a local publish command, edit tags, alter the target after approval, bypass branch/environment protections, or publish a package outside the derived set. If the protected environment requires a credential or authentication refresh, pause at that authentication gate, resume the same workflow after the credential is supplied, and rerun no weaker path. Record workflow run URL, job IDs, exact versions, package checksums, timestamps, and immutable commit/tag associations in the ledger per D-23.</action>
  <verify>
    <automated>test -n "${PUBLISH_RUN_ID:-}" &amp;&amp; test "$(gh run view "$PUBLISH_RUN_ID" --json conclusion --jq .conclusion)" = success &amp;&amp; rg -n 'candidate SHA|exact version|checksum|workflow run' .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</automated>
  </verify>
  <done>D-23 is satisfied: only the protected workflow published exactly the resolver-approved versions after all candidate gates, and the ledger contains immutable publication evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Verify exact public Hex artifacts with the full downstream journey</name>
  <files>.planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</files>
  <precondition>The protected publication workflow reports successful publication of every resolver-selected exact version and no unselected package.</precondition>
  <reversibility rating="reversible">This task reads public registry artifacts and appends evidence; it does not mutate published packages.</reversibility>
  <action>Wait with the bounded post-publish retry policy for each exact version to become visible on Hex, then let the protected post-publish workflow run the shared generated-host journey under `DEP_MODE=hex` with those immutable versions. Verify archive checksum and contents, no path override, fresh install/compile, generated migration, Repo migration, sync/async parity through actively polling Oban, lifecycle persistence, zero-effect negatives, signed feedback, one-click replay/suppression, production preflight, authenticated operator mount, executable docs, and HexDocs visibility. Compare the installed application versions and package metadata to the release outputs. Append command outputs, workflow URL, Hex/HexDocs URLs, checksums, and per-stage results to the ledger. A propagation timeout or stage failure leaves REL-17 unverified and must not be recorded as success per D-22.</action>
  <verify>
    <automated>DEP_MODE=hex dev/mailglass/generated_host_proof.sh --stage all &amp;&amp; rg -n 'HexDocs|checksum|post-publish|DEP_MODE=hex' .planning/phases/153-generated-host-proof-docs-and-release-gate/153-RELEASE-PROOF.md</automated>
  </verify>
  <done>D-22 is satisfied against public Hex: the exact approved versions pass every shared downstream stage, HexDocs are reachable, and the completed ledger contains no inferred or unobserved success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected workflow → Hex | Credentialed automation creates immutable public package versions. |
| Hex registry → generated host | Public archives and metadata become executable downstream dependencies. |
| workflow telemetry → evidence ledger | External run state is converted into durable release assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-29 | Elevation of Privilege | Protected publication | critical | mitigate | Use only protected workflow/environment permissions after candidate-SHA gates; no local or bypass publish path. |
| T-153-30 | Tampering | Published artifacts | critical | mitigate | Record and compare exact versions, archive checksums, package contents, and immutable tags. |
| T-153-31 | Repudiation | Release proof ledger | high | mitigate | Preserve workflow URLs, job IDs, timestamps, SHA, commands, and observed outputs for every assertion. |
| T-153-32 | Spoofing | Registry propagation | high | mitigate | Resolve exact versions from Hex, compare installed application metadata, and fail on any path override or mismatch. |
| T-153-SC | Tampering | Package installs | high | mitigate | Install only exact already-audited project packages from Hex; verify checksums and package contents before executing the journey. |
</threat_model>

<verification>
- The protected publication run concludes successfully and the exact-version Hex journey passes every stage.
- The protected workflow shows only resolver-selected package versions published from the recorded candidate SHA.
- Exact public Hex packages and HexDocs are reachable and the complete shared journey is green.
</verification>

<success_criteria>
- REL-17 is complete only after protected publication and exact-Hex downstream proof.
- D-23 keeps the irreversible action behind every reversible gate and allows only credential/authentication pauses.
- D-22 records the same complete journey against immutable public versions, with no local fallback.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Protected release follows complete local proof | COVERED — Task 1. |
| GOAL | Exact public packages pass the complete fresh-host journey | COVERED — Task 2. |
| REQ | REL-17 | COVERED — Tasks 1-2. |
| RESEARCH | Separate reversible validation from irreversible publication | COVERED — explicit dependency and one-way reversibility marker. |
| CONTEXT | D-22 | COVERED — Task 2. |
| CONTEXT | D-23 | COVERED — Task 1. |

<output>
Create `.planning/phases/153-generated-host-proof-docs-and-release-gate/153-08-SUMMARY.md` when done.
</output>
