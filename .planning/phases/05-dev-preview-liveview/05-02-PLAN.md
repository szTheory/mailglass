---
phase: 05-dev-preview-liveview
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - mailglass_admin/.formatter.exs
  - mailglass_admin/.gitignore
  - mailglass_admin/mix.exs
  - mailglass_admin/config/config.exs
  - mailglass_admin/config/test.exs
  - mailglass_admin/config/dev.exs
  - mailglass_admin/lib/mailglass_admin.ex
  - mailglass_admin/README.md
  - mailglass_admin/CHANGELOG.md
  - mailglass_admin/LICENSE
autonomous: true
requirements:
  - PREV-01
must_haves:
  truths:
    - "`cd mailglass_admin && mix deps.get` resolves successfully in local-dev mode (path dep to ..)."
    - "When `MIX_PUBLISH=true`, the `:mailglass` dep tuple renders as `{:mailglass, \"== 0.1.0\"}` (pinned to mailglass core's current version), proving the linked-versions switch works."
    - "`mix_config_test.exs` (created in Plan 01) flips from RED to GREEN."
    - "`cd mailglass_admin && mix compile` succeeds for the empty package (no lib/*.ex referenced yet except lib/mailglass_admin.ex root module)."
  artifacts:
    - path: "mailglass_admin/mix.exs"
      provides: "Hex package definition with linked mailglass dep + package[:files] whitelist + boundary compiler + verify.phase_05 alias"
      contains: "@version"
      min_lines: 80
    - path: "mailglass_admin/config/config.exs"
      provides: "config :tailwind, version: \"4.1.12\" + default build profile"
      contains: "4.1.12"
    - path: "mailglass_admin/config/test.exs"
      provides: "Synthetic adopter endpoint config — secret_key_base, pubsub_server, live_view signing_salt"
      contains: "secret_key_base"
    - path: "mailglass_admin/lib/mailglass_admin.ex"
      provides: "Root module with Boundary declaration"
      contains: "use Boundary"
    - path: "mailglass_admin/README.md"
      provides: "Adopter-facing README with router mount idiom + dev-only guidance"
      min_lines: 30
    - path: "mailglass_admin/CHANGELOG.md"
      provides: "Release Please-compatible changelog starter"
  key_links:
    - from: "mailglass_admin/mix.exs"
      to: "../mix.exs (mailglass root)"
      via: "path dep switch"
      pattern: "path:"
---

<objective>
Stand up the `mailglass_admin/` nested-sibling Hex package skeleton per CONTEXT D-01..D-04. Wave 1 foundational scaffolding; does NOT ship features but DOES establish the load-bearing PREV-01 contract: `mailglass_admin/mix.exs` declares `{:mailglass, "== <pinned>"}` when publishing to Hex, and `{:mailglass, path: "..", override: true}` for local-dev.

Purpose: Everything else in Phase 5 mounts on top of this. The `mix.exs` linked-versions switch prevents sibling-version drift (DIST-01). `.formatter.exs`, config files, and the root module + Boundary declaration lock architectural boundaries before any feature code lands.
Output: A compilable empty Hex package with `mix deps.get` + `mix compile` working locally, `verify.phase_05` alias available, `mix_config_test.exs` (from Plan 01) passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dev-preview-liveview/05-CONTEXT.md
@.planning/phases/05-dev-preview-liveview/05-RESEARCH.md
@.planning/phases/05-dev-preview-liveview/05-PATTERNS.md
@mix.exs

<interfaces>
Existing mailglass core mix.exs is the style reference:

- `@version "0.1.0"` module attribute (line 4)
- `compilers: [:boundary | Mix.compilers()]` (line 14)
- `elixirc_paths(:test), do: ["lib", "test/support"]` pattern (line 35)
- `package[:files]` whitelist pattern (line 160)
- `"verify.phase_04":` alias pattern (lines 127-132)

Local mailglass `@version` is currently `"0.1.0"`. The new `mailglass_admin/mix.exs` MUST track this value exactly; linked-versions (Phase 7 Release Please) keeps them in sync post-v0.1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mailglass_admin/mix.exs + .formatter.exs + .gitignore</name>
  <files>mailglass_admin/mix.exs, mailglass_admin/.formatter.exs, mailglass_admin/.gitignore</files>
  <read_first>
    - /Users/jon/projects/mailglass/mix.exs (full file — style reference; Phase 5 CONTEXT D-02 switch is the ONE pattern not present in this analog)
    - /Users/jon/projects/mailglass/.formatter.exs (if present — style reference)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md section "mailglass_admin/mix.exs" lines 107-189 (mix.exs skeleton)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-CONTEXT.md D-01..D-04 (package layout + deps + files whitelist)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 157-199 (verified deps + versions)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-VALIDATION.md Wave 0 (the `verify.phase_05` alias that must exist for CI)
  </read_first>
  <action>
    Create `mailglass_admin/mix.exs` following the mailglass core pattern plus CONTEXT D-01..D-04 linked-versions switch.

    Required module attributes: `@version "0.1.0"` (MUST equal `../mix.exs` `@version` literal — read root mix.exs first to confirm); `@source_url "https://github.com/szTheory/mailglass"`; `@description "Mountable LiveView dashboard for mailglass — dev preview + admin."`

    Required `project/0` keys: `app: :mailglass_admin`, `version: @version`, `elixir: "~> 1.18"`, `elixirc_paths: elixirc_paths(Mix.env())`, `elixirc_options: elixirc_options()`, `compilers: [:boundary | Mix.compilers()]`, `start_permanent: Mix.env() == :prod`, `deps: deps()`, `aliases: aliases()`, `name: "MailglassAdmin"`, `description: @description`, `source_url: @source_url`, `homepage_url: @source_url`, `package: package()`, `docs: docs()`.

    Required `application/0`: `[extra_applications: [:logger]]` — no `mod:` entry (no supervision tree of our own at v0.1 per PATTERNS.md line 133).

    Required `elixirc_paths`: `(:test), do: ["lib", "test/support"]` + catch-all `do: ["lib"]`.

    Required `elixirc_options`: `[no_warn_undefined: [Phoenix.LiveReloader]]` per CONTEXT D-24 (phoenix_live_reload is dev-only optional).

    Required `deps/0` list (exact members):
    1. `mailglass_dep()` — a separate private function returning the dep tuple conditional on `System.get_env("MIX_PUBLISH")`:
       - When `MIX_PUBLISH` equals "true": `{:mailglass, "== " <> @version}` — per CONTEXT D-02 linked-versions
       - Otherwise: `{:mailglass, path: "..", override: true}` — local-dev path dep
    2. `{:phoenix, "~> 1.8"}`
    3. `{:phoenix_live_view, "~> 1.1"}`
    4. `{:phoenix_html, "~> 4.1"}`
    5. `{:plug, "~> 1.18"}`
    6. `{:nimble_options, "~> 1.1"}`
    7. `{:tailwind, "~> 0.4", only: :dev, runtime: false}` — per CONTEXT D-18
    8. `{:phoenix_live_reload, "~> 1.6", optional: true, only: :dev}` — per CONTEXT D-24
    9. `{:boundary, "~> 0.10", runtime: false}` — matches mailglass core
    10. `{:floki, "~> 0.38", only: :test}` — for brand + voice tests
    11. `{:jason, "~> 1.4", only: [:dev, :test]}`
    12. `{:credo, "~> 1.7", only: [:dev, :test], runtime: false}`
    13. `{:ex_doc, "~> 0.40", only: :dev, runtime: false}`

    Required `aliases/0` list — one alias `"verify.phase_05"` with steps (exact order):
    1. `"compile --no-optional-deps --warnings-as-errors"`
    2. `"test --warnings-as-errors --exclude flaky"`
    3. `"mailglass_admin.assets.build"` — landing in Plan 05
    4. `"cmd git diff --exit-code priv/static/"` — the PREV-06 gate

    At Plan 02 completion, `verify.phase_05` is expected to FAIL at step 2 (tests are RED from Plan 01) and step 3 (assets.build task not yet shipped). That is the intended RED state.

    Required `package/0` per CONTEXT D-04 (strict files whitelist — assets/ source EXCLUDED, priv/static/ INCLUDED):
    - `name: "mailglass_admin"`
    - `licenses: ["MIT"]`
    - `description: @description`
    - `links: %{"GitHub" => @source_url, "HexDocs" => "https://hexdocs.pm/mailglass_admin"}`
    - `files: ~w(lib priv/static .formatter.exs mix.exs README* CHANGELOG* LICENSE*)`

    Required `docs/0`: `main: "MailglassAdmin"`, `source_url: @source_url`, `source_ref: "v" <> @version`.

    Then create `mailglass_admin/.formatter.exs`:

        [
          import_deps: [:phoenix, :phoenix_live_view],
          plugins: [Phoenix.LiveView.HTMLFormatter],
          inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"]
        ]

    Then create `mailglass_admin/.gitignore` with standard Elixir entries — `/_build/`, `/cover/`, `/deps/`, `/doc/`, `/.fetch`, `erl_crash.dump`, `*.ez`, `/tmp/`, `*.log`, `/.elixir_ls/` — AND with two load-bearing comments at the end stating explicitly: "DO NOT ignore priv/static/ — CONTEXT D-04 requires it committed." and "DO NOT ignore assets/vendor/ — CONTEXT D-18 requires vendored daisyUI committed." These comments document the two anti-patterns that would silently break PREV-06 / CONTEXT D-04.

    Do NOT ignore `priv/static/` or `assets/vendor/` in the gitignore patterns.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/mix.exs && test -f mailglass_admin/.formatter.exs && test -f mailglass_admin/.gitignore && grep -q 'app: :mailglass_admin' mailglass_admin/mix.exs && grep -q 'mailglass_dep' mailglass_admin/mix.exs && grep -q 'MIX_PUBLISH' mailglass_admin/mix.exs && grep -q 'verify.phase_05' mailglass_admin/mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_admin/mix.exs` exists and defines `defmodule MailglassAdmin.MixProject`.
    - grep `app: :mailglass_admin` succeeds.
    - grep `mailglass_dep` succeeds (the private function exists).
    - grep `MIX_PUBLISH` succeeds.
    - grep `path: \"\\.\\.\"` or equivalent (path: "..") exists in mailglass_admin/mix.exs.
    - grep `{:phoenix_live_view, "~> 1.1"}` succeeds.
    - grep `{:tailwind, "~> 0.4", only: :dev, runtime: false}` succeeds.
    - grep `{:phoenix_live_reload, "~> 1.6", optional: true, only: :dev}` succeeds.
    - grep `verify.phase_05` succeeds.
    - grep `git diff --exit-code priv/static/` succeeds.
    - grep `files: ~w(lib priv/static` succeeds (exact package[:files] whitelist per D-04).
    - `.formatter.exs` contains `import_deps: [:phoenix, :phoenix_live_view]` and `Phoenix.LiveView.HTMLFormatter`.
    - `.gitignore` contains BOTH comments about `priv/static/` and `assets/vendor/` as committed directories.
    - `.gitignore` does NOT contain `priv/static` or `assets/vendor` as unqualified ignore patterns (grep for `^priv/static` or `^assets/vendor` returns no matches).
    - `@version` literal in mailglass_admin/mix.exs matches `@version` literal in `/Users/jon/projects/mailglass/mix.exs` (run `grep '@version' mix.exs mailglass_admin/mix.exs` and compare).
  </acceptance_criteria>
  <done>
    Hex package skeleton exists. `cd mailglass_admin && mix deps.get` resolves.
  </done>
</task>

<task type="auto">
  <name>Task 2: config/*, root module, README/CHANGELOG/LICENSE</name>
  <files>mailglass_admin/config/config.exs, mailglass_admin/config/test.exs, mailglass_admin/config/dev.exs, mailglass_admin/lib/mailglass_admin.ex, mailglass_admin/README.md, mailglass_admin/CHANGELOG.md, mailglass_admin/LICENSE</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 942-956 (exact config/config.exs contents for tailwind pinning per CONTEXT D-22)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md section "Boundary declaration" lines 867-881 (Boundary pattern for root module)
    - /Users/jon/projects/mailglass/lib/mailglass.ex lines 1-60 (style reference for root module moduledoc + Boundary)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-CONTEXT.md lines 196-207 (adopter-facing 4-line router mount idiom — must appear in README verbatim)
    - /Users/jon/projects/mailglass/mailglass_admin/test/support/endpoint_case.ex (from Plan 01 — the synthetic endpoint that requires config/test.exs wiring)
    - /Users/jon/projects/mailglass/LICENSE (if present — for the MIT text used in mailglass_admin/LICENSE)
  </read_first>
  <action>
    Create six support files.

    **`mailglass_admin/config/config.exs`** per CONTEXT D-22 + 05-RESEARCH.md lines 942-956:

        import Config

        # CONTEXT D-22: pin Tailwind version in the admin package's own config;
        # do not track latest blindly. daisyUI pin lives as a file-header comment
        # in assets/vendor/daisyui.js (Plan 05 ships that file).
        config :tailwind,
          version: "4.1.12",
          default: [
            args: ~w(
              --input=assets/css/app.css
              --output=priv/static/app.css
            ),
            cd: Path.expand("..", __DIR__)
          ]

        # Environment-specific config split out for clarity.
        if File.exists?(Path.join(__DIR__, "#{config_env()}.exs")) do
          import_config "#{config_env()}.exs"
        end

    **`mailglass_admin/config/test.exs`** — The EndpointCase from Plan 01 requires these keys to start `MailglassAdmin.TestAdopter.Endpoint`:

        import Config

        # Synthetic adopter endpoint for router + LiveView test coverage.
        # See test/support/endpoint_case.ex.
        config :mailglass_admin, MailglassAdmin.TestAdopter.Endpoint,
          http: [port: 4002],
          server: false,
          secret_key_base: String.duplicate("mailglass_admin_test_secret_key_base_0", 2),
          live_view: [signing_salt: "mailglass_admin_test_signing_salt_0123"],
          pubsub_server: MailglassAdmin.TestPubSub,
          render_errors: [formats: [html: MailglassAdmin.TestAdopter.ErrorHTML], layout: false]

    The `secret_key_base` literal MUST be at least 64 bytes (Phoenix enforces) — the construction above yields 72 chars. The `pubsub_server` name `MailglassAdmin.TestPubSub` matches the PubSub name the LiveReload test (Plan 06) broadcasts on.

    **`mailglass_admin/config/dev.exs`** — Stub for now:

        import Config
        # Reserved for maintainer-side dev configuration.
        # Tailwind build profile is already configured in config.exs.

    **`mailglass_admin/lib/mailglass_admin.ex`** — Root module with Boundary. Match the shape of `/Users/jon/projects/mailglass/lib/mailglass.ex` but scoped to admin:

        defmodule MailglassAdmin do
          @moduledoc \"\"\"
          Mountable LiveView dashboard for mailglass. Dev preview at v0.1; prod admin at v0.5.

          ## Quick start

          Add to your adopter app's `lib/my_app_web/router.ex`:

              import MailglassAdmin.Router

              if Application.compile_env(:my_app, :dev_routes) do
                scope "/dev" do
                  pipe_through :browser
                  mailglass_admin_routes "/mail"
                end
              end

          Restart `mix phx.server`, visit `/dev/mail`. Done.

          See `MailglassAdmin.Router.mailglass_admin_routes/2` for options.
          \"\"\"

          use Boundary,
            deps: [Mailglass],
            exports: [Router]

          @version Mix.Project.config()[:version]
          @doc \"\"\"
          Returns the package version string at compile time.
          \"\"\"
          @spec version() :: String.t()
          def version, do: @version
        end

    The `deps: [Mailglass]` is load-bearing: PATTERNS.md line 881 locks the renderer-purity rule — PreviewLive may call `Mailglass.Renderer.render/1` and `Mailglass.Message.*` builders but NOT `Mailglass.Outbound.deliver/2`. Boundary enforces at compile time once Plans 03-06 populate submodules.

    The `exports: [Router]` reflects that the router macro is the only public surface at v0.1 — every other submodule is internal. Phase 6's boundary contract test will verify.

    **`mailglass_admin/README.md`** — Adopter-facing README (markdown, ≥30 lines). Must contain:

    1. H1 `# mailglass_admin` + one-line tagline matching mailglass brand voice.
    2. Quick-install section with the EXACT 4-line router snippet from CONTEXT.md specifics lines 196-207:

            import MailglassAdmin.Router

            if Application.compile_env(:my_app, :dev_routes) do
              scope "/dev" do
                pipe_through :browser
                mailglass_admin_routes "/mail"
              end
            end

    3. `## LiveReload setup (optional)` section documenting the `config :my_app, MyAppWeb.Endpoint, live_reload: [notify: ["mailglass:admin:reload": [~r"lib/.*mailer.*\\.ex$"]]]` idiom verbatim (prefixed topic form — PATTERNS.md correction re line 699).
    4. `## preview_props/0 contract` section showing the canonical `def preview_props, do: [welcome_default: %{...}, welcome_enterprise: %{...}]` shape per CONTEXT D-11.
    5. `## License` section referencing MIT.
    6. NO emojis. Brand voice: "clear, exact, confident, warm, technical" — see `prompts/mailglass-brand-book.md` §5.

    **`mailglass_admin/CHANGELOG.md`** — Release Please-compatible starter:

        # Changelog

        All notable changes to `mailglass_admin` will be documented in this file.
        Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
        Versioning coordinated with `mailglass` core via Release Please linked-versions.

        ## [Unreleased]

        ### Added
        - Initial package skeleton (Phase 5 Plan 02).

    **`mailglass_admin/LICENSE`** — Copy the MIT text from `/Users/jon/projects/mailglass/LICENSE` verbatim if it exists; otherwise use the standard MIT license template with copyright holder "Jon Joubert" and year 2026. Per PROJECT.md D-02, the license is MIT across all sibling packages forever.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/config/config.exs && test -f mailglass_admin/config/test.exs && test -f mailglass_admin/config/dev.exs && test -f mailglass_admin/lib/mailglass_admin.ex && test -f mailglass_admin/README.md && test -f mailglass_admin/CHANGELOG.md && test -f mailglass_admin/LICENSE && grep -q '4.1.12' mailglass_admin/config/config.exs && grep -q 'secret_key_base' mailglass_admin/config/test.exs && grep -q 'use Boundary' mailglass_admin/lib/mailglass_admin.ex && grep -q 'mailglass_admin_routes' mailglass_admin/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All seven files exist at the listed paths.
    - `config/config.exs` contains the literal `version: "4.1.12"`.
    - `config/config.exs` defines `config :tailwind` with `default: [args: ...]` and `cd: Path.expand("..", __DIR__)` pointing at `mailglass_admin/`.
    - `config/test.exs` defines `config :mailglass_admin, MailglassAdmin.TestAdopter.Endpoint` with at minimum `secret_key_base`, `live_view`, and `pubsub_server` keys.
    - `config/test.exs` `pubsub_server` value is `MailglassAdmin.TestPubSub` (matches Plan 06's PubSub subscribe target).
    - `lib/mailglass_admin.ex` contains `use Boundary, deps: [Mailglass], exports: [Router]`.
    - `lib/mailglass_admin.ex` has a `@moduledoc` with at least 10 lines including the router mount idiom.
    - `lib/mailglass_admin.ex` defines `version/0` returning the Mix project version.
    - `README.md` contains the literal string `mailglass_admin_routes "/mail"`.
    - `README.md` contains the literal string `"mailglass:admin:reload"` (prefixed topic form for adopter LiveReload notify config).
    - `README.md` contains the literal string `preview_props` (zero-arity contract documentation).
    - `README.md` has no emoji characters (grep `[\x{1F300}-\x{1FAFF}]` or spot-check is sufficient).
    - `CHANGELOG.md` contains `## [Unreleased]`.
    - `LICENSE` contains `MIT` and `Permission is hereby granted`.
    - `cd mailglass_admin && mix compile` succeeds (the root module compiles; Boundary compiles with `deps: [Mailglass]`).
  </acceptance_criteria>
  <done>
    Package skeleton is complete: config, root module, README, CHANGELOG, LICENSE. `mix compile` works for the empty package.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mailglass_admin package ↔ Hex registry | Package manifest (mix.exs files whitelist + MIX_PUBLISH switch) is the supply-chain seam; bad pin lets sibling versions drift (DIST-01) or ships source that shouldn't be public. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Elevation of Privilege | Adopter mounts `mailglass_admin_routes` outside `:dev` pipeline by mistake | mitigate | README.md shows the canonical `if Application.compile_env(:my_app, :dev_routes) do ... end` wrapper with the 4-line mount; CONTEXT D-06 places enforcement on adopter (cannot be enforced library-side). This plan's mitigation is DOCUMENTATION — Plan 03's router macro enforces via absence of Mix.env() checks. |
| T-05-03 | Tampering | Committed bundle drift (priv/static/ out of sync with assets/) | mitigate | mix.exs `verify.phase_05` alias ends with `cmd git diff --exit-code priv/static/` — the CI merge gate per CONTEXT D-04. `.gitignore` comments prevent accidentally gitignoring the committed bundle. Plan 05 produces the actual bundle. |
</threat_model>

<verification>
- `cd /Users/jon/projects/mailglass/mailglass_admin && mix deps.get` returns exit code 0.
- `cd /Users/jon/projects/mailglass/mailglass_admin && mix compile` returns exit code 0 (only the root module compiles; no lib/mailglass_admin/*.ex exists yet outside the root).
- `cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/mix_config_test.exs` returns exit code 0 (the Plan 01 mix_config_test now has a real mix.exs to assert against).
- `MIX_PUBLISH=true cd /Users/jon/projects/mailglass/mailglass_admin && mix deps` output line for `:mailglass` contains `== 0.1.0` (confirming the linked-versions switch flips correctly).
</verification>

<success_criteria>
- `mailglass_admin/mix.exs` exists with the exact `app: :mailglass_admin`, `version: "0.1.0"`, `mailglass_dep/0` private function, `verify.phase_05` alias, and `package[:files]` whitelist.
- `mailglass_admin/.formatter.exs`, `.gitignore`, `config/config.exs`, `config/test.exs`, `config/dev.exs`, `lib/mailglass_admin.ex`, `README.md`, `CHANGELOG.md`, `LICENSE` all exist.
- `mix_config_test.exs` (from Plan 01) passes.
- `mix deps.get` + `mix compile` succeed in `mailglass_admin/`.
- Boundary compiles the root module with `deps: [Mailglass], exports: [Router]`.
- No `priv/static/` or `assets/vendor/` appears as an ignore pattern in `.gitignore`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dev-preview-liveview/05-02-SUMMARY.md` documenting:
- Exact `@version` value locked in (must match mailglass core)
- The chosen `pubsub_server` name (MailglassAdmin.TestPubSub) so Plan 06 broadcasts to the right target
- Any deviations from 05-PATTERNS.md mix.exs skeleton and why
- Known RED signals at Plan 02 completion (`verify.phase_05` fails at step 3 — `mailglass_admin.assets.build` task not yet shipped; this is expected).
</output>
