---
phase: 05-dev-preview-liveview
plan: 05
type: execute
wave: 1
depends_on: ["01", "02"]
files_modified:
  - mailglass_admin/assets/css/app.css
  - mailglass_admin/assets/vendor/daisyui.js
  - mailglass_admin/assets/vendor/daisyui-theme.js
  - mailglass_admin/assets/vendor/heroicons.js
  - mailglass_admin/priv/static/app.css
  - mailglass_admin/priv/static/mailglass-logo.svg
  - mailglass_admin/priv/static/fonts/inter-400.woff2
  - mailglass_admin/priv/static/fonts/inter-700.woff2
  - mailglass_admin/priv/static/fonts/inter-tight-400.woff2
  - mailglass_admin/priv/static/fonts/inter-tight-700.woff2
  - mailglass_admin/priv/static/fonts/ibm-plex-mono-400.woff2
  - mailglass_admin/priv/static/fonts/ibm-plex-mono-700.woff2
  - mailglass_admin/lib/mailglass_admin/controllers/assets.ex
  - mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex
  - mailglass_admin/lib/mix/tasks/mailglass_admin.assets.watch.ex
  - mailglass_admin/lib/mix/tasks/mailglass_admin.daisyui.update.ex
  - mailglass_admin/.gitattributes
autonomous: false
user_setup:
  - service: daisyui-vendored-sources
    why: "Vendored daisyUI 5 + Tailwind plugin and six font woff2 subsets need to be obtained as binary files before CSS can compile. The mix task provides the fetch path but the initial placement + font subsetting is a maintainer-time step."
    dashboard_config:
      - task: "Run `mix mailglass_admin.daisyui.update` from mailglass_admin/ to fetch vendor/daisyui.js and vendor/daisyui-theme.js from the daisyUI 5 GitHub releases page."
        location: "Terminal in mailglass_admin/ directory"
      - task: "Pre-subset the six woff2 font files (Inter 400/700, Inter Tight 400/700, IBM Plex Mono 400/700) using pyftsubset/glyphhanger for Latin + Latin-Ext coverage (~30KB each). Place in priv/static/fonts/. See 05-RESEARCH.md § Typography lines 1307-1315 for the exact pyftsubset command."
        location: "Maintainer local workstation; commit the woff2 files to git"
      - task: "Provide or vendor mailglass-logo.svg (brand book §7 glyph). Placeholder acceptable for v0.1 if logo asset not yet finalized."
        location: "mailglass_admin/priv/static/mailglass-logo.svg"
requirements:
  - PREV-05
  - PREV-06
  - BRAND-01
must_haves:
  truths:
    - "`mix mailglass_admin.assets.build` produces a minified `priv/static/app.css` from `assets/css/app.css` with zero Node toolchain required."
    - "The compiled `priv/static/app.css` contains every canonical brand hex value (Ink/Glass/Ice/Mist/Paper/Slate + Signal Amber/Error Crimson/Success Pine) and defines both `mailglass-light` and `mailglass-dark` daisyUI themes."
    - "`MailglassAdmin.Controllers.Assets` serves `priv/static/app.css` with `cache-control: public, max-age=31536000, immutable` and the content bytes embedded at compile time via `@external_resource` + `File.read!/1`."
    - "Font allowlist matches the six files shipped (inter-400/700, inter-tight-400/700, ibm-plex-mono-400/700) — path traversal via `/fonts/../etc/passwd` returns 404."
    - "Total `priv/static/` size stays under 800 KB, well below the 2 MB PREV-06 Hex tarball gate."
  artifacts:
    - path: "mailglass_admin/assets/css/app.css"
      provides: "Tailwind v4 input CSS with @theme brand palette + @plugin daisyui + @font-face declarations"
      contains: "@import \"tailwindcss\""
      min_lines: 80
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Compiled+minified CSS bundle (CI git-diff gate target)"
      min_lines: 1
    - path: "mailglass_admin/lib/mailglass_admin/controllers/assets.ex"
      provides: "Compile-time asset serving with immutable cache headers + font allowlist"
      contains: "cache-control"
      min_lines: 60
    - path: "mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex"
      provides: "Mix task: mix mailglass_admin.assets.build"
      contains: "tailwind"
    - path: "mailglass_admin/lib/mix/tasks/mailglass_admin.assets.watch.ex"
      provides: "Mix task: mix mailglass_admin.assets.watch (maintainer loop)"
      contains: "--watch"
    - path: "mailglass_admin/lib/mix/tasks/mailglass_admin.daisyui.update.ex"
      provides: "Mix task: refresh vendored daisyUI files from GitHub releases"
      contains: "httpc"
  key_links:
    - from: "mailglass_admin/assets/css/app.css"
      to: "mailglass_admin/priv/static/app.css"
      via: "mix mailglass_admin.assets.build (tailwind standalone compile)"
      pattern: "priv/static/app.css"
    - from: "mailglass_admin/lib/mailglass_admin/controllers/assets.ex"
      to: "mailglass_admin/priv/static/app.css"
      via: "@external_resource + File.read!/1 at compile time"
      pattern: "File.read!"
---

<objective>
Ship the complete no-Node asset pipeline per CONTEXT D-17..D-23 + 05-RESEARCH.md § Asset Pipeline. Three responsibilities bundled because they share the same trust-boundary surface (the committed bundle) and because tests depend on the compiled output + controller together:

1. **Input CSS + vendored daisyUI + fonts.** Author `assets/css/app.css` with Tailwind v4 + daisyUI 5 plugin directives + brand palette `@theme` + self-hosted `@font-face` declarations. Vendor daisyUI `.js` files (fetched via Mix task). Commit the six woff2 font subsets and logo SVG to `priv/static/`.

2. **Compiled bundle + Mix tasks.** `mix mailglass_admin.assets.build` produces minified `priv/static/app.css` via the `tailwind` Hex package (zero Node). `mix mailglass_admin.assets.watch` is the maintainer dev loop. `mix mailglass_admin.daisyui.update` refreshes vendored files. CI runs `git diff --exit-code priv/static/` per CONTEXT D-04 — drift between source and committed bundle fails merge.

3. **Compile-time asset controller.** `MailglassAdmin.Controllers.Assets` embeds `priv/static/app.css`, concatenated `phoenix.js` + `phoenix_live_view.js`, logo SVG, and serves with `cache-control: public, max-age=31536000, immutable`. Font allowlist prevents path traversal. Phoenix + LiveView JS are read from their `Application.app_dir/2` paths so they are NOT charged to mailglass_admin's Hex tarball.

Purpose: This is the plan that delivers PREV-05 (brand palette + typography + daisyUI 5 + Tailwind v4 no-Node) + PREV-06 (committed bundle + CI diff gate + <2 MB tarball) + the palette portion of BRAND-01. The LiveView (Plan 06) links the CSS + JS via the URLs this controller exposes.

Marked `autonomous: false`: Vendored daisyUI files, font woff2 subsets, and potentially the logo SVG require maintainer-time setup per `user_setup` above. Executor halts at the first checkpoint for human provisioning.

Output: `brand_test.exs` + `accessibility_test.exs` + `bundle_test.exs` + `assets_test.exs` (from Plan 01) flip from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dev-preview-liveview/05-CONTEXT.md
@.planning/phases/05-dev-preview-liveview/05-RESEARCH.md
@.planning/phases/05-dev-preview-liveview/05-PATTERNS.md
@.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md
@mailglass_admin/mix.exs
@mailglass_admin/config/config.exs

<interfaces>
Existing contracts (already shipped):

- `Mix.Task` behaviour — all three mix tasks `use Mix.Task` with `@impl Mix.Task def run/1`.
- `tailwind` Hex package — `Mix.Task.run("tailwind", ["default", "--minify"])` is the build invocation.
- `Plug.Conn` — standard `put_resp_header/3`, `send_resp/3`, `halt/1`.
- `:httpc.request/4` — stdlib HTTP client for the daisyui.update task.

Contracts this plan creates (consumed by Plan 03's Router macro + Plan 06's Layouts):

- `MailglassAdmin.Controllers.Assets.css_hash/0 :: String.t()` — returns the `:crypto.hash(:md5, @css)` hex-encoded string, computed at compile time.
- `MailglassAdmin.Controllers.Assets.js_hash/0 :: String.t()` — same for concatenated JS bundle.
- `MailglassAdmin.Controllers.Assets.init/1` + `call/2` — Plug-controller dispatch for actions `:css`, `:js`, `:font`, `:logo`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 0: Vendor daisyUI + subset fonts + place logo (maintainer-time setup)</name>
  <what-built>
    This task CANNOT be automated by Claude because:
    - daisyUI source files must be curled from a GitHub release URL (network access potentially required, and the Mix task that does this is defined IN THIS PLAN — chicken-and-egg on first run).
    - Font woff2 subsetting requires running `pyftsubset` (Python tool) locally on the maintainer's workstation with source TTF files.
    - The logo SVG is a brand asset the maintainer owns.
  </what-built>
  <how-to-verify>
    Before proceeding to subsequent tasks, the maintainer must produce these files at these paths:

    1. `mailglass_admin/assets/vendor/daisyui.js` — Fetched from `https://github.com/saadeghi/daisyui/releases/latest/download/daisyui.js`. Option A: run `mix mailglass_admin.daisyui.update` AFTER Task 3 of this plan lands the task module, then restart this checkpoint. Option B: curl manually:

           curl -L -o mailglass_admin/assets/vendor/daisyui.js https://github.com/saadeghi/daisyui/releases/latest/download/daisyui.js

       Prepend a file-header comment stating the daisyUI version and fetch date per CONTEXT D-22.

    2. `mailglass_admin/assets/vendor/daisyui-theme.js` — Same pattern, from `https://github.com/saadeghi/daisyui/releases/latest/download/daisyui-theme.js`.

    3. `mailglass_admin/assets/vendor/heroicons.js` — Copy verbatim from `installer/templates/phx_assets/heroicons.js.eex` in a Phoenix 1.8+ source checkout, strip the `.eex` expressions if any. If Phoenix source not available locally, fetch from `https://raw.githubusercontent.com/phoenixframework/phoenix/main/installer/templates/phx_assets/heroicons.js.eex`.

    4. `mailglass_admin/priv/static/fonts/` — Six woff2 files, Latin + Latin-Ext subsets (~30 KB each):
       - `inter-400.woff2`
       - `inter-700.woff2`
       - `inter-tight-400.woff2`
       - `inter-tight-700.woff2`
       - `ibm-plex-mono-400.woff2`
       - `ibm-plex-mono-700.woff2`

       Subsetting command per 05-RESEARCH.md lines 1307-1315:

           pyftsubset Inter-Regular.ttf \
             --unicodes="U+0000-00FF,U+0100-017F,U+0180-024F" \
             --flavor=woff2 \
             --output-file=priv/static/fonts/inter-400.woff2

       Note: the 2-weights-per-family lock per 05-UI-SPEC lines 71-79 supersedes 05-RESEARCH.md's earlier 3-weight mention. Ship exactly 6 font files, not 8.

    5. `mailglass_admin/priv/static/mailglass-logo.svg` — Brand book §7 logo glyph. Acceptable v0.1 placeholder: a minimal SVG rendering the word "mailglass" in Inter Tight 700 + Ink color. Under 20 KB.

    Verify each file exists:

        ls -la mailglass_admin/assets/vendor/daisyui.js mailglass_admin/assets/vendor/daisyui-theme.js mailglass_admin/assets/vendor/heroicons.js
        ls -la mailglass_admin/priv/static/fonts/{inter-400,inter-700,inter-tight-400,inter-tight-700,ibm-plex-mono-400,ibm-plex-mono-700}.woff2
        ls -la mailglass_admin/priv/static/mailglass-logo.svg
  </how-to-verify>
  <resume-signal>Type "vendored" when all nine files (3 vendor/*.js + 6 priv/static/fonts/*.woff2 + 1 priv/static/mailglass-logo.svg) exist at the paths above. Otherwise describe which are missing and why (e.g., "daisyUI 6 just dropped; pinning to 5.5.x release URL instead").</resume-signal>
  <files>mailglass_admin/assets/vendor/daisyui.js, mailglass_admin/assets/vendor/daisyui-theme.js, mailglass_admin/assets/vendor/heroicons.js, mailglass_admin/priv/static/fonts/inter-400.woff2, mailglass_admin/priv/static/fonts/inter-700.woff2, mailglass_admin/priv/static/fonts/inter-tight-400.woff2, mailglass_admin/priv/static/fonts/inter-tight-700.woff2, mailglass_admin/priv/static/fonts/ibm-plex-mono-400.woff2, mailglass_admin/priv/static/fonts/ibm-plex-mono-700.woff2, mailglass_admin/priv/static/mailglass-logo.svg</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above. Maintainer-only setup: vendor 3 daisyUI/Heroicons .js files, subset 6 woff2 fonts, place logo SVG. No automated work — checkpoint blocks until human signals "vendored".</action>
  <verify>
    <automated>test -f mailglass_admin/assets/vendor/daisyui.js && test -f mailglass_admin/assets/vendor/daisyui-theme.js && test -f mailglass_admin/assets/vendor/heroicons.js && test -f mailglass_admin/priv/static/mailglass-logo.svg && for f in inter-400 inter-700 inter-tight-400 inter-tight-700 ibm-plex-mono-400 ibm-plex-mono-700; do test -f mailglass_admin/priv/static/fonts/$f.woff2 || exit 1; done</automated>
  </verify>
  <done>All 10 vendored files exist at the paths above; subsequent tasks can compile assets/css/app.css through Tailwind without missing-plugin errors.</done>
</task>

<task type="auto">
  <name>Task 1: .gitattributes + assets/css/app.css (source CSS with brand palette)</name>
  <files>mailglass_admin/.gitattributes, mailglass_admin/assets/css/app.css</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 810-940 (the full verbatim app.css contents per CONTEXT D-17..D-22, with the font path caveat at line 940 correcting the CSS to use `url('./fonts/...')` relative paths)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md section "Color → Palette definition" lines 108-131 (canonical hex values)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md section "Visual DON'Ts" lines 163-171 (what must NOT appear in the CSS)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md section "Pitfall 3: Asset CI gate false positive on line endings" lines 1378-1391 (the .gitattributes requirement)
    - /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/brand_test.exs (from Plan 01 — the assertions this CSS must satisfy)
  </read_first>
  <action>
    Create `mailglass_admin/.gitattributes` to prevent CI false-positives on CRLF/LF drift per 05-RESEARCH.md Pitfall 3:

        priv/static/app.css text eol=lf
        priv/static/*.svg text eol=lf
        priv/static/fonts/*.woff2 binary
        assets/vendor/*.js text eol=lf
        assets/css/*.css text eol=lf

    Create `mailglass_admin/assets/css/app.css` by copying 05-RESEARCH.md lines 814-937 VERBATIM, with TWO corrections applied:

    1. **Font paths must be relative.** RESEARCH.md lines 814-937 show absolute `/dev/mail/fonts/...` paths. Per RESEARCH.md line 940, rewrite every `src: url('/dev/mail/fonts/...')` to `src: url('./fonts/...')`. The browser resolves relative to the `/<mount>/css-<hash>.css` URL, so `./fonts/inter-400.woff2` becomes `/<mount>/fonts/inter-400.woff2` at load time — works for ANY adopter mount path (PHX-02 prevention).

    2. **Two-weight font loading.** Per 05-UI-SPEC lines 71-79, only 400 + 700 weights are loaded per family. RESEARCH.md lines 885-927 show 3 weights for Inter (400/500/700), 2 for Inter Tight (600/700), and 2 for IBM Plex Mono (400/600). Collapse to exactly 2 weights per family: Inter 400/700, Inter Tight 400/700, IBM Plex Mono 400/700. Remove `inter-500.woff2`, `inter-tight-600.woff2`, `ibm-plex-mono-600.woff2` @font-face blocks. Replace with the 400/700 variants.

    Also update the "Palette to daisyUI" mapping per 05-UI-SPEC lines 108-122 (the UI-SPEC's canonical brand book §7.3 hex values for semantic colors supersede RESEARCH.md's earlier values):

    - `--color-warning: #A95F10` (Signal Amber — UI-SPEC) NOT `#C08A2B` (RESEARCH.md) — 05-UI-SPEC explicitly documents the correction as "prior draft used undocumented drift."
    - `--color-error: #B42318` (Error Crimson — UI-SPEC) NOT `#B04A3F` (RESEARCH.md).
    - `--color-success: #166534` (Success Pine — UI-SPEC) NOT `#5A8F4E` (RESEARCH.md).

    Keep the dark-theme Signal Amber/Error Crimson/Success Pine adjustments per 05-UI-SPEC lines 119-122 (`#E0A955`, `#D47368`, `#8BB77F`) — those are deliberate dark-surface legibility adjustments.

    Required CSS properties that MUST appear somewhere in the final file:

    - `@import "tailwindcss"` (with `source(none)` per RESEARCH.md line 819)
    - `@source "../css"` + `@source "../../lib"` (RESEARCH.md lines 821-822)
    - `@plugin "../vendor/daisyui"` block with `themes: false`
    - Two `@plugin "../vendor/daisyui-theme"` blocks: one `name: "mailglass-light"` with `default: true`, one `name: "mailglass-dark"`.
    - In the light theme:
      - `--color-base-100: #F8FBFD;` (Paper)
      - `--color-base-200: #EAF6FB;` (Mist)
      - `--color-base-300: #A6EAF2;` (Ice)
      - `--color-base-content: #0D1B2A;` (Ink)
      - `--color-primary: #277B96;` (Glass)
      - `--color-secondary: #5C6B7A;` (Slate)
      - `--color-accent: #0D1B2A;` (Ink)
      - `--color-warning: #A95F10;`
      - `--color-error: #B42318;`
      - `--color-success: #166534;`
      - `--depth: 0;`
      - `--noise: 0;`
    - Six `@font-face` declarations using `url('./fonts/<name>.woff2')` relative paths + `font-display: swap`.
    - `body { font-family: 'Inter', system-ui, -apple-system, sans-serif; }` and related typography defaults.

    Required EXCLUSIONS (must NOT appear anywhere in the file):
    - Any `backdrop-filter:` declaration
    - Any `box-shadow: inset` declaration
    - Any `url('https://...')` or `url('//...')` absolute external URL (self-hosted only per CONTEXT D-21)
    - Any `inter-500.woff2`, `inter-tight-600.woff2`, `ibm-plex-mono-600.woff2` reference (2-weights-per-family discipline)
    - Any hex value in the rejected drift set: `#C08A2B`, `#B04A3F`, `#5A8F4E` (superseded by canonical brand book §7.3).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/.gitattributes && test -f mailglass_admin/assets/css/app.css && grep -q 'tailwindcss' mailglass_admin/assets/css/app.css && grep -q 'mailglass-light' mailglass_admin/assets/css/app.css && grep -q 'mailglass-dark' mailglass_admin/assets/css/app.css && grep -q '#0D1B2A' mailglass_admin/assets/css/app.css && grep -q '#F8FBFD' mailglass_admin/assets/css/app.css && grep -q '#277B96' mailglass_admin/assets/css/app.css && ! grep -q 'backdrop-filter' mailglass_admin/assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `.gitattributes` contains `priv/static/app.css text eol=lf`.
    - `.gitattributes` contains `priv/static/fonts/*.woff2 binary`.
    - `assets/css/app.css` contains the literal string `@import "tailwindcss"`.
    - `app.css` contains `@plugin "../vendor/daisyui"` with `themes: false`.
    - `app.css` contains both `name: "mailglass-light"` and `name: "mailglass-dark"` theme plugin blocks.
    - `app.css` contains all six canonical brand hex values: `#0D1B2A`, `#277B96`, `#A6EAF2`, `#EAF6FB`, `#F8FBFD`, `#5C6B7A`.
    - `app.css` contains the canonical Signal colors: `#A95F10`, `#B42318`, `#166534`.
    - `app.css` contains `--depth: 0` AND `--noise: 0` in BOTH theme blocks.
    - `app.css` contains exactly six `@font-face` declarations (grep `-c '@font-face'` returns 6).
    - `app.css` contains `url('./fonts/inter-400.woff2')` (relative path form — NOT `/dev/mail/fonts/`).
    - `app.css` does NOT contain `backdrop-filter` (grep returns 0 matches).
    - `app.css` does NOT contain `box-shadow: inset` (grep returns 0).
    - `app.css` does NOT contain `#C08A2B`, `#B04A3F`, or `#5A8F4E` (rejected drift values).
    - `app.css` does NOT contain `inter-500.woff2`, `inter-tight-600.woff2`, or `ibm-plex-mono-600.woff2`.
  </acceptance_criteria>
  <done>
    Tailwind v4 + daisyUI 5 input CSS exists with brand palette + self-hosted 6-font setup; .gitattributes locks LF line endings to prevent CI false positives.
  </done>
</task>

<task type="auto">
  <name>Task 2: Three mix tasks (assets.build, assets.watch, daisyui.update)</name>
  <files>mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex, mailglass_admin/lib/mix/tasks/mailglass_admin.assets.watch.ex, mailglass_admin/lib/mix/tasks/mailglass_admin.daisyui.update.ex</files>
  <read_first>
    - /Users/jon/projects/mailglass/lib/mix/tasks/mailglass.reconcile.ex (the style reference — `use Boundary, classify_to:`, `@shortdoc`, `@moduledoc`, `use Mix.Task`, `@impl Mix.Task def run/1`)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 960-1001 (the three mix task bodies verbatim, including the daisyui.update httpc invocation)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md section "mailglass_admin/lib/mix/tasks" lines 660-696 (the Boundary + Mix.Task shell)
    - /Users/jon/projects/mailglass/mailglass_admin/config/config.exs (from Plan 02 — defines `config :tailwind, default: [args: ...]` which `Mix.Task.run("tailwind", ["default", ...])` dispatches to)
  </read_first>
  <action>
    Create three mix tasks. Each follows the pattern:

        defmodule Mix.Tasks.MailglassAdmin.Assets.Build do
          use Boundary, classify_to: MailglassAdmin

          @shortdoc "..."
          @moduledoc "..."

          use Mix.Task

          @impl Mix.Task
          def run(_argv) do
            ...
          end
        end

    **`mailglass_admin.assets.build.ex`** — Production build:

        @shortdoc "Build mailglass_admin CSS bundle (production, minified)"
        @moduledoc \"\"\"
        Compiles `mailglass_admin/assets/css/app.css` to `mailglass_admin/priv/static/app.css`
        via the `tailwind` Hex package — zero Node toolchain.

        ## Usage

            mix mailglass_admin.assets.build

        Run after editing `assets/css/app.css` or touching any HEEx under `lib/mailglass_admin/`.
        CI runs this followed by `git diff --exit-code priv/static/` per CONTEXT D-04 / PREV-06.
        \"\"\"
        use Mix.Task

        @impl Mix.Task
        def run(_argv) do
          Mix.Task.run("tailwind", ["default", "--minify"])
        end

    **`mailglass_admin.assets.watch.ex`** — Maintainer dev loop:

        @shortdoc "Watch mode for maintainer dev loop"
        @moduledoc \"\"\"
        Runs Tailwind in --watch mode. Recompiles priv/static/app.css on every
        save to assets/css/app.css or any HEEx source under lib/mailglass_admin/.

        ## Usage

            mix mailglass_admin.assets.watch
        \"\"\"
        use Mix.Task

        @impl Mix.Task
        def run(_argv) do
          Mix.Task.run("tailwind", ["default", "--watch"])
        end

    **`mailglass_admin.daisyui.update.ex`** — Refresh vendored daisyUI per 05-RESEARCH.md lines 979-1001:

        @shortdoc "Refresh vendored daisyUI .js files from GitHub releases"
        @moduledoc \"\"\"
        Curls the latest `daisyui.js` + `daisyui-theme.js` from the daisyUI GitHub
        releases page and writes them to `assets/vendor/`. Prepends a pin-comment
        with today's date for CHANGELOG traceability.

        ## Usage

            mix mailglass_admin.daisyui.update

        Review the diff, update CHANGELOG, commit. Supply-chain discipline: this is
        the single authorized path for refreshing vendored daisyUI. Dependabot does
        NOT cover vendored files; maintainer review on each diff is the control.
        \"\"\"
        use Mix.Task

        @daisyui_release "https://github.com/saadeghi/daisyui/releases/latest/download"

        @impl Mix.Task
        def run(_argv) do
          Application.ensure_all_started(:inets)
          Application.ensure_all_started(:ssl)

          [
            {"daisyui.js", @daisyui_release <> "/daisyui.js"},
            {"daisyui-theme.js", @daisyui_release <> "/daisyui-theme.js"}
          ]
          |> Enum.each(fn {name, url} ->
            path = Path.join(["assets", "vendor", name])
            Mix.shell().info("Downloading " <> name <> " ...")

            case :httpc.request(:get, {String.to_charlist(url), []}, [], body_format: :binary) do
              {:ok, {{_, 200, _}, _, body}} ->
                today = Date.utc_today() |> Date.to_iso8601()
                header = "// Fetched " <> today <> " from " <> url <> "\n"
                File.write!(path, header <> body)
                Mix.shell().info("  -> wrote " <> path)

              {:ok, {{_, status, _}, _, _}} ->
                Mix.raise("Unexpected HTTP status " <> Integer.to_string(status) <> " from " <> url)

              {:error, reason} ->
                Mix.raise("Failed to fetch " <> url <> ": " <> inspect(reason))
            end
          end)

          Mix.shell().info("Done. Review diff, update CHANGELOG, commit.")
        end

    Each mix task MUST include `use Boundary, classify_to: MailglassAdmin` so the compiler correctly attributes them to the admin package's boundary.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex && test -f mailglass_admin/lib/mix/tasks/mailglass_admin.assets.watch.ex && test -f mailglass_admin/lib/mix/tasks/mailglass_admin.daisyui.update.ex && grep -q 'Mix.Task.run("tailwind"' mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex && grep -q -- '--minify' mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex && grep -q -- '--watch' mailglass_admin/lib/mix/tasks/mailglass_admin.assets.watch.ex && grep -q 'httpc' mailglass_admin/lib/mix/tasks/mailglass_admin.daisyui.update.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_admin.assets.build.ex` exists with `defmodule Mix.Tasks.MailglassAdmin.Assets.Build`.
    - `assets.build` calls `Mix.Task.run("tailwind", ["default", "--minify"])`.
    - `mailglass_admin.assets.watch.ex` exists and calls `Mix.Task.run("tailwind", ["default", "--watch"])`.
    - `mailglass_admin.daisyui.update.ex` exists and uses `:httpc.request` to fetch from `https://github.com/saadeghi/daisyui/releases/latest/download`.
    - All three tasks have `use Boundary, classify_to: MailglassAdmin`, `use Mix.Task`, `@shortdoc`, `@moduledoc`, and `@impl Mix.Task def run/1`.
    - `cd mailglass_admin && mix help | grep mailglass_admin` lists all three tasks.
    - After Checkpoint 0's vendor files are in place: `cd mailglass_admin && mix mailglass_admin.assets.build` produces `priv/static/app.css` (non-empty file).
  </acceptance_criteria>
  <done>
    Three mix tasks shipped; `mix mailglass_admin.assets.build` produces a committed-ready CSS bundle.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run assets.build + MailglassAdmin.Controllers.Assets (compile-time asset serving)</name>
  <files>mailglass_admin/priv/static/app.css, mailglass_admin/lib/mailglass_admin/controllers/assets.ex</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md section "Pattern 2: Compile-time asset serving" lines 455-536 (the verbatim LiveDashboard assets controller)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md section "mailglass_admin/lib/mailglass_admin/controllers/assets.ex" lines 267-323 (the assets.ex template with font allowlist)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md lines 71-79 (the 6-file, 2-weights-per-family font loading lock)
    - /Users/jon/projects/mailglass/mailglass_admin/assets/css/app.css (from Task 1 — the input the build compiles from)
    - /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/assets_test.exs (from Plan 01 — the exact content-type/cache-control/font allowlist assertions this controller must satisfy)
    - /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/bundle_test.exs (from Plan 01 — size budget assertions the compiled bundle must satisfy)
  </read_first>
  <action>
    First, run `cd mailglass_admin && mix mailglass_admin.assets.build` to produce `priv/static/app.css`. Commit the resulting file (it's the PREV-06 CI gate target). If the build errors (e.g., daisyUI plugin incompatibility with Tailwind v4.1.12), follow the CONTEXT D-22 escape hatch: drop to raw Tailwind v4 with brand palette as `@theme` CSS variables (no daisyUI plugin) and annotate in the SUMMARY.

    Then write `mailglass_admin/lib/mailglass_admin/controllers/assets.ex` following 05-RESEARCH.md Pattern 2 lines 455-536 verbatim, adapted for mailglass_admin:

        defmodule MailglassAdmin.Controllers.Assets do
          @moduledoc false

          use Boundary, classify_to: MailglassAdmin

          import Plug.Conn

          # ---- CSS bundle (compile-time embedded) ----
          @css_path Path.join([:code.priv_dir(:mailglass_admin), "static", "app.css"])
          @external_resource @css_path
          @css File.read!(@css_path)
          @css_hash Base.encode16(:crypto.hash(:md5, @css), case: :lower)

          # ---- JS bundle: phoenix.js + phoenix_live_view.js ----
          # Read from the host's already-installed priv dirs so they are NOT
          # charged to mailglass_admin's Hex tarball.
          @phoenix_js Application.app_dir(:phoenix, "priv/static/phoenix.js")
          @phoenix_live_view_js Application.app_dir(:phoenix_live_view, "priv/static/phoenix_live_view.js")
          @external_resource @phoenix_js
          @external_resource @phoenix_live_view_js
          @js Enum.map_join([@phoenix_js, @phoenix_live_view_js], "\n", &File.read!/1)
          @js_hash Base.encode16(:crypto.hash(:md5, @js), case: :lower)

          # ---- Logo ----
          @logo_path Path.join([:code.priv_dir(:mailglass_admin), "static", "mailglass-logo.svg"])
          @external_resource @logo_path
          @logo File.read!(@logo_path)

          @doc false
          def css_hash, do: @css_hash
          @doc false
          def js_hash, do: @js_hash

          # Plug-controller dispatch
          def init(action) when action in [:css, :js, :font, :logo], do: action

          def call(conn, :css), do: serve(conn, @css, "text/css; charset=utf-8")
          def call(conn, :js),  do: serve(conn, @js,  "application/javascript; charset=utf-8")
          def call(conn, :logo), do: serve(conn, @logo, "image/svg+xml")

          def call(conn, :font) do
            name = conn.params["name"]

            with {:ok, path} <- resolve_font(name),
                 {:ok, bytes} <- File.read(path) do
              serve(conn, bytes, "font/woff2")
            else
              _ ->
                conn
                |> send_resp(404, "")
                |> halt()
            end
          end

          defp serve(conn, body, content_type) do
            conn
            |> put_resp_header("content-type", content_type)
            |> put_resp_header("cache-control", "public, max-age=31536000, immutable")
            |> send_resp(200, body)
            |> halt()
          end

          # Font allowlist — updated per 05-UI-SPEC lines 71-79 (2 weights per family only).
          # The RESEARCH.md allowlist at lines 518-526 included 500 and 600 weights; UI-SPEC
          # revision collapsed to 400/700 exclusively.
          @allowed_fonts ~w(
            inter-400.woff2 inter-700.woff2
            inter-tight-400.woff2 inter-tight-700.woff2
            ibm-plex-mono-400.woff2 ibm-plex-mono-700.woff2
          )

          defp resolve_font(name) when name in @allowed_fonts do
            {:ok, Path.join([:code.priv_dir(:mailglass_admin), "static", "fonts", name])}
          end

          defp resolve_font(_), do: :error
        end

    The six-member `@allowed_fonts` list mirrors the six woff2 files staged at Checkpoint 0. If the maintainer added a seventh weight during subsetting (e.g., Inter 500 for emphasis), the allowlist MUST be expanded too — but 05-UI-SPEC's 2-weights-per-family lock is the canonical contract; expand deliberately and update the brand test fixture.

    Expected test outcomes after this task:

    - `brand_test.exs`: palette hex values + depth/noise assertions GREEN.
    - `accessibility_test.exs`: contrast ratio assertions GREEN (pure math — depends only on the hex constants).
    - `bundle_test.exs`: `priv/static/app.css` exists + size < 150 KB + 6 font files + logo + total < 800 KB GREEN.
    - `assets_test.exs`: GET css/js/font/logo routes + path traversal 404 GREEN.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/priv/static/app.css && test -f mailglass_admin/lib/mailglass_admin/controllers/assets.ex && grep -q '@external_resource' mailglass_admin/lib/mailglass_admin/controllers/assets.ex && grep -q 'cache-control' mailglass_admin/lib/mailglass_admin/controllers/assets.ex && grep -q 'max-age=31536000' mailglass_admin/lib/mailglass_admin/controllers/assets.ex && grep -q '@allowed_fonts' mailglass_admin/lib/mailglass_admin/controllers/assets.ex</automated>
  </verify>
  <acceptance_criteria>
    - `priv/static/app.css` exists and is non-empty (File.stat size > 0).
    - `priv/static/app.css` size is < 150_000 bytes (CONTEXT D-23 envelope).
    - `priv/static/app.css` contains the canonical Ink hex `#0D1B2A` (verifies the build pulled the theme block through unchanged).
    - `lib/mailglass_admin/controllers/assets.ex` exists with `defmodule MailglassAdmin.Controllers.Assets`.
    - Controller contains `@external_resource @css_path`, `@external_resource @phoenix_js`, `@external_resource @phoenix_live_view_js`, `@external_resource @logo_path` (4 total).
    - Controller contains `@css File.read!(@css_path)` and `@css_hash Base.encode16(:crypto.hash(:md5, @css)`.
    - Controller's `@allowed_fonts` list contains exactly 6 entries: `inter-400.woff2`, `inter-700.woff2`, `inter-tight-400.woff2`, `inter-tight-700.woff2`, `ibm-plex-mono-400.woff2`, `ibm-plex-mono-700.woff2`.
    - Controller sets `cache-control: public, max-age=31536000, immutable` (grep the full header string).
    - Controller's `call(conn, :font)` uses `resolve_font/1` with allowlist membership check (no string concatenation with `conn.params["name"]` before the allowlist check).
    - `cd mailglass_admin && mix compile --warnings-as-errors` succeeds (Layouts module from Plan 03 now resolves `MailglassAdmin.Controllers.Assets.css_hash/0`).
    - `cd mailglass_admin && mix test test/mailglass_admin/brand_test.exs` passes all tests.
    - `cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs` passes all tests.
    - `cd mailglass_admin && mix test test/mailglass_admin/bundle_test.exs` passes all tests.
    - `cd mailglass_admin && mix test test/mailglass_admin/assets_test.exs` passes all tests.
    - After `mix mailglass_admin.assets.build`, `git diff --exit-code priv/static/` returns exit 0 (compiled bundle matches what's committed).
  </acceptance_criteria>
  <done>
    Compiled bundle committed. Controller serves CSS/JS/fonts/logo with immutable cache. Font allowlist rejects path traversal. Brand + accessibility + bundle + assets tests all GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored daisyUI sources ↔ compile-time embed | `assets/vendor/daisyui.js` is fetched from an external URL once and committed; every subsequent compile embeds those bytes via `@external_resource`. Supply-chain compromise at fetch time propagates through Hex releases. |
| Font filename param ↔ file read | `GET /fonts/:name` takes user input and uses it to construct a filesystem path. Must be structurally prevented from escaping `priv/static/fonts/`. |
| priv/static/ committed bundle ↔ source | Drift between `assets/css/app.css` source and `priv/static/app.css` compiled output silently ships stale CSS to adopters unless CI catches it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | `mailglass_admin/priv/static/` committed bundle vs source CSS drift | mitigate | `mix.exs` `verify.phase_05` alias (from Plan 02) runs `cmd git diff --exit-code priv/static/` after `mailglass_admin.assets.build`. `.gitattributes` (this plan, Task 1) locks LF line endings so CRLF-on-Windows doesn't false-positive the gate. |
| T-05-05 (new) | Tampering | Font filename path traversal via `/fonts/<attack>.woff2` | mitigate | Controller `resolve_font/1` uses `when name in @allowed_fonts` pattern guard — any filename outside the 6-item allowlist falls through to `:error` -> 404 response. `assets_test.exs` from Plan 01 explicitly tests `..%2F..%2Fetc%2Fpasswd`. |
| T-05-06 (new) | Tampering | Vendored daisyUI supply-chain compromise | mitigate (documentation-based) | `daisyui.update` Mix task writes the fetch-date + URL as a file-header comment for changelog/diff traceability. CONTEXT D-22 documents the escape-hatch (drop to raw Tailwind v4 + hand-crafted CSS) if daisyUI itself is compromised or breaks. Dependabot does NOT cover vendored files; maintainer diff review is the explicit control. |
</threat_model>

<verification>
- `cd mailglass_admin && mix mailglass_admin.assets.build` exits 0.
- `cd mailglass_admin && git diff --exit-code priv/static/` exits 0.
- `cd mailglass_admin && mix test test/mailglass_admin/brand_test.exs test/mailglass_admin/accessibility_test.exs test/mailglass_admin/bundle_test.exs test/mailglass_admin/assets_test.exs` exits 0.
- `du -sh mailglass_admin/priv/static/` reports < 800 KB.
- `ls mailglass_admin/priv/static/fonts/ | wc -l` returns 6.
</verification>

<success_criteria>
- Six woff2 font subsets in `priv/static/fonts/` (2 weights per family: Inter/Inter Tight/IBM Plex Mono).
- `priv/static/app.css` compiled from `assets/css/app.css` via `mix mailglass_admin.assets.build` (zero Node).
- `priv/static/app.css` contains all six brand palette hex values + canonical Signal colors + dual daisyUI themes + `--depth: 0` + `--noise: 0`.
- `priv/static/app.css` size < 150 KB; total `priv/static/` < 800 KB.
- No `backdrop-filter`, `box-shadow: inset`, or rejected-drift hex values anywhere in source or compiled CSS.
- `MailglassAdmin.Controllers.Assets` serves each asset with `cache-control: public, max-age=31536000, immutable`; font path traversal returns 404.
- Three `mix mailglass_admin.*` tasks shipped (assets.build, assets.watch, daisyui.update).
- `.gitattributes` prevents CRLF/LF drift from false-positiving the CI diff gate.
- All four asset-related test files (brand/accessibility/bundle/assets) are GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dev-preview-liveview/05-05-SUMMARY.md` documenting:
- Exact daisyUI version fetched + SHA256 of `assets/vendor/daisyui.js` (supply-chain traceability).
- Compiled CSS size (bytes) + each font file size (confirms against 05-UI-SPEC 2-weights-per-family lock).
- Any escape-hatch activation (CONTEXT D-22: dropped to raw Tailwind v4 because daisyUI plugin failed) — if so, note the deferred items for v0.2.
- CSS URL format chosen for layouts (relative vs absolute) and its coupling with Plan 03's Layouts module references to css_hash/0 / js_hash/0.
</output>
