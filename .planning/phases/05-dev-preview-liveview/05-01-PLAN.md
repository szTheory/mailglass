---
phase: 05-dev-preview-liveview
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - mailglass_admin/test/test_helper.exs
  - mailglass_admin/test/support/fixtures/mailables.ex
  - mailglass_admin/test/support/endpoint_case.ex
  - mailglass_admin/test/support/live_view_case.ex
  - mailglass_admin/test/mailglass_admin/mix_config_test.exs
  - mailglass_admin/test/mailglass_admin/router_test.exs
  - mailglass_admin/test/mailglass_admin/preview_live_test.exs
  - mailglass_admin/test/mailglass_admin/discovery_test.exs
  - mailglass_admin/test/mailglass_admin/assets_test.exs
  - mailglass_admin/test/mailglass_admin/brand_test.exs
  - mailglass_admin/test/mailglass_admin/accessibility_test.exs
  - mailglass_admin/test/mailglass_admin/bundle_test.exs
  - mailglass_admin/test/mailglass_admin/voice_test.exs
autonomous: true
requirements:
  - PREV-01
  - PREV-02
  - PREV-03
  - PREV-04
  - PREV-05
  - PREV-06
  - BRAND-01
must_haves:
  truths:
    - "All canonical planning docs (PROJECT.md L52, REQUIREMENTS.md L130, ROADMAP.md L22/L114/L119) read `preview_props/0` — the stale `/1` prose is eliminated."
    - "Every requirement in the Phase 5 validation map has a failing-by-default ExUnit test file at the path 05-VALIDATION.md specifies."
    - "The synthetic adopter Endpoint harness exists and is importable from test modules so router + LiveView tests can mount the real `mailglass_admin_routes/2` macro output."
    - "`mix test` inside `mailglass_admin/` compiles and runs (expect RED — feature plans drive GREEN)."
  artifacts:
    - path: "mailglass_admin/test/test_helper.exs"
      provides: "ExUnit bootstrap for the admin package"
      contains: "ExUnit.start"
    - path: "mailglass_admin/test/support/endpoint_case.ex"
      provides: "ConnTest harness wrapping the synthetic adopter endpoint so router tests exercise the real macro expansion"
      contains: "defmodule MailglassAdmin.EndpointCase"
    - path: "mailglass_admin/test/support/live_view_case.ex"
      provides: "Phoenix.LiveViewTest wrapper around the synthetic endpoint"
      contains: "import Phoenix.LiveViewTest"
    - path: "mailglass_admin/test/support/fixtures/mailables.ex"
      provides: "Three fixture mailable modules (happy / stub / broken)"
      contains: "defmodule MailglassAdmin.Fixtures.HappyMailer"
    - path: "mailglass_admin/test/mailglass_admin/router_test.exs"
      provides: "Router macro expansion + __session__/N isolation tests (RED by default)"
    - path: "mailglass_admin/test/mailglass_admin/preview_live_test.exs"
      provides: "LiveViewTest coverage for sidebar/tabs/device_toggle/dark_toggle/assigns_form/live_reload (RED by default, tagged)"
    - path: "mailglass_admin/test/mailglass_admin/brand_test.exs"
      provides: "Palette mapping + daisyUI theme CSS inspection"
    - path: "mailglass_admin/test/mailglass_admin/accessibility_test.exs"
      provides: "WCAG AA contrast ratio assertions for canonical brand pairs"
    - path: "mailglass_admin/test/mailglass_admin/bundle_test.exs"
      provides: "priv/static/app.css size budget + priv/static/ tree expectations"
    - path: "mailglass_admin/test/mailglass_admin/voice_test.exs"
      provides: "Brand voice lexicon enforcement against rendered HEEx"
    - path: "mailglass_admin/test/mailglass_admin/assets_test.exs"
      provides: "Compile-time asset serving contract (content-type, cache-control, allowlist)"
    - path: "mailglass_admin/test/mailglass_admin/mix_config_test.exs"
      provides: "mix.exs Hex-publish dep pin assertion (PREV-01)"
    - path: "mailglass_admin/test/mailglass_admin/discovery_test.exs"
      provides: "Explicit-list + graceful-failure discovery tests"
  key_links:
    - from: "mailglass_admin/test/support/endpoint_case.ex"
      to: "MailglassAdmin.Router.mailglass_admin_routes/2"
      via: "compiled-into-synthetic-endpoint router"
      pattern: "mailglass_admin_routes"
    - from: "mailglass_admin/test/support/fixtures/mailables.ex"
      to: "Mailglass.Mailable"
      via: "use Mailglass.Mailable"
      pattern: "use Mailglass.Mailable"
---

<objective>
Wave 0 bootstrap. Two purposes:

1. **Doc-fix sweep.** Rewrite the stale `preview_props/1` prose in PROJECT.md L52, REQUIREMENTS.md L130, and ROADMAP.md L22/L114/L119 to the canonical `preview_props/0` per CONTEXT D-10. The shipped code at `lib/mailglass/mailable.ex:111-112` has always been `/0`; the canonical planning docs must match the code before any Phase 5 code lands.

2. **Test infrastructure.** Stand up every file from 05-VALIDATION.md § "Wave 0 Requirements" in a RED-by-default state: test_helper, support harnesses (endpoint_case, live_view_case, fixtures), and one test file per PREV-/BRAND-01 requirement. All feature plans (02..06) drive these tests green. This is the Nyquist-compliance gate for the phase.

Purpose: Blocks all feature plans — no code lands until the test surface it must drive exists. The synthetic-endpoint harness is the load-bearing piece: it lets Plan 03's router macro test and Plan 06's LiveView test mount the real macro output against a real (but minimal) Phoenix Endpoint, without needing an adopter app.
Output: 3 doc-fix edits + 1 test_helper + 3 support files + 10 test files, all committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dev-preview-liveview/05-CONTEXT.md
@.planning/phases/05-dev-preview-liveview/05-RESEARCH.md
@.planning/phases/05-dev-preview-liveview/05-PATTERNS.md
@.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md
@.planning/phases/05-dev-preview-liveview/05-VALIDATION.md

<interfaces>
<!-- Contracts the test files assert against. Executors use these names verbatim. -->
<!-- All symbols are NOT YET IMPLEMENTED — tests must fail referencing them until feature plans land. -->

Expected public API surface (to be built by later plans):

```elixir
# Plan 03 creates this:
defmodule MailglassAdmin.Router do
  defmacro mailglass_admin_routes(path, opts \\ [])
  @doc false
  def __session__(conn_or_nil, opts)  # Oban-web-pattern whitelisted session callback
end

# Plan 04 creates this:
defmodule MailglassAdmin.Preview.Discovery do
  @spec discover(:auto_scan | [module()]) ::
          [{module(), [{atom(), map()}] | :no_previews | {:error, String.t()}}]
end

# Plan 05 creates this:
defmodule MailglassAdmin.Controllers.Assets do
  def init(action) when action in [:css, :js, :font, :logo]
  def call(conn, action)
  def css_hash/0, def js_hash/0
end

# Plan 05 creates this:
defmodule MailglassAdmin.PubSub.Topics do
  @spec admin_reload() :: String.t()
  # returns the literal "mailglass:admin:reload"
end

# Plan 06 creates this:
defmodule MailglassAdmin.PreviewLive, do: use Phoenix.LiveView
# LiveView routes: "/" => :index, "/:mailable/:scenario" => :show
```

Existing (already shipped, do not recreate):

```elixir
# lib/mailglass/mailable.ex:111-112, 150-156
@optional_callbacks preview_props: 0
@callback preview_props() :: [{atom(), map()}]
# @before_compile injects:
def __mailglass_mailable__, do: true

# lib/mailglass/renderer.ex
@spec render(Mailglass.Message.t()) :: {:ok, rendered} | {:error, Mailglass.TemplateError.t()}

# lib/mailglass/pub_sub/topics.ex
# Pattern to follow for new MailglassAdmin.PubSub.Topics module.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc-fix sweep — preview_props/1 → preview_props/0 in canonical docs</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/PROJECT.md (find the single occurrence at line 52 — the "preview_props/1" prose)
    - /Users/jon/projects/mailglass/.planning/REQUIREMENTS.md (find line 130 — PREV-03 prose)
    - /Users/jon/projects/mailglass/.planning/ROADMAP.md (find lines 22, 114, 119 — all three occurrences)
    - /Users/jon/projects/mailglass/lib/mailglass/mailable.ex lines 111-112 (the canonical shipped contract: `@optional_callbacks preview_props: 0` + `@callback preview_props() :: [{atom(), map()}]`)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-CONTEXT.md §Decisions D-10 (the specifics lock)
  </read_first>
  <action>
    Per CONTEXT D-10, rewrite every "preview_props/1" occurrence in the three canonical planning docs to "preview_props/0" to match shipped code at lib/mailglass/mailable.ex:111-112. Use the Edit tool exactly once per occurrence; do NOT rewrite whole files.

    Concrete edits (exact replacements):

    **PROJECT.md line 52** — Locate the `preview_props/1` token in the line that introduces the mailable preview contract. Replace that token with `preview_props/0`. No other changes on that line. Read the line with Read(offset=50, limit=5) first to confirm neighboring context is stable.

    **REQUIREMENTS.md line 130** — This is the PREV-03 bullet. It contains two occurrences of `preview_props/1`: once in `auto-discovered via \`preview_props/1\` callback` and once in `live-assigns form for each \`preview_props/1\` field`. Replace BOTH with `preview_props/0`. Do NOT touch the IDs cited in parens (TS-13, DF-01, DF-10).

    **ROADMAP.md** — Three occurrences:
    - Line 22: in the Phase 5 one-line summary (`auto-discovery via \`preview_props/1\``)
    - Line 114: in the Phase 5 Goal section
    - Line 119: in Success Criteria item 2 (`Every \`Mailglass.Mailable\` module that defines a \`preview_props/1\` callback ...`)

    Replace each with `preview_props/0`. Do NOT touch adjacent requirement IDs or phase numbers.

    Final check after all edits: `grep -rn "preview_props/1" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` must return zero matches. Shipped test at `test/mailglass/mailable_test.exs:131-135` already asserts `/0`; this doc sweep closes the prose/code gap.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -c "preview_props/1" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md 2>/dev/null | grep -v ":0$" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "preview_props/1" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns zero matches (exit code 1).
    - `grep -rn "preview_props/0" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns at least 6 matches (1 in PROJECT.md, 2 in REQUIREMENTS.md, 3 in ROADMAP.md).
    - `git diff --stat .planning/` shows exactly 3 files changed (PROJECT.md, REQUIREMENTS.md, ROADMAP.md) with only the preview_props edits (± line count ≤ 12).
    - No other line content (requirement IDs, phase headers, tables) changed — confirm via `git diff .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` spot-check.
  </acceptance_criteria>
  <done>
    Canonical docs match shipped code on `preview_props/0` arity. No grep leaks remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test infrastructure — test_helper + support/ harnesses + fixtures</name>
  <files>mailglass_admin/test/test_helper.exs, mailglass_admin/test/support/fixtures/mailables.ex, mailglass_admin/test/support/endpoint_case.ex, mailglass_admin/test/support/live_view_case.ex</files>
  <read_first>
    - /Users/jon/projects/mailglass/test/test_helper.exs (style reference — ExUnit.start, exclude/only tags, sandbox setup)
    - /Users/jon/projects/mailglass/test/support/mailer_case.ex (ExUnit.CaseTemplate shape for admin analog)
    - /Users/jon/projects/mailglass/test/mailglass/mailable_test.exs lines 5-27 (in-test fixture mailable pattern — defmodule SampleMailer do use Mailglass.Mailable, stream: :transactional ... end)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-VALIDATION.md § "Wave 0 Requirements" (the file list this task produces)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 760-775 (the session isolation test skeleton)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md §"mailglass_admin/test/mailglass_admin/router_test.exs" + §"discovery_test.exs"
  </read_first>
  <action>
    Stand up ExUnit + Phoenix.LiveViewTest test infrastructure for the new `mailglass_admin/` package. The feature plans in this phase drive these tests green.

    **`mailglass_admin/test/test_helper.exs`** — Minimal bootstrap:

    ```elixir
    ExUnit.start(exclude: [:skip])
    Application.ensure_all_started(:mailglass_admin)
    ```

    Do NOT start Ecto sandboxes — the admin package has no DB. Do NOT require a Phoenix Endpoint here; EndpointCase owns that.

    **`mailglass_admin/test/support/fixtures/mailables.ex`** — Three fixture modules, compiled into the test suite, each `use Mailglass.Mailable, stream: :transactional`:

    ```elixir
    defmodule MailglassAdmin.Fixtures.HappyMailer do
      use Mailglass.Mailable, stream: :transactional

      def preview_props do
        [
          welcome_default: %{user_name: "Ada", plan: :free, admin?: false},
          welcome_enterprise: %{user_name: "Babbage", plan: :enterprise, admin?: true}
        ]
      end

      def welcome_default(assigns) do
        new()
        |> from("no-reply@example.test")
        |> to("ada@example.test")
        |> subject("Welcome #{assigns.user_name}")
        |> html_body("<p>Hi #{assigns.user_name}</p>")
        |> text_body("Hi #{assigns.user_name}")
      end
    end

    defmodule MailglassAdmin.Fixtures.StubMailer do
      use Mailglass.Mailable, stream: :transactional
      # Deliberately no preview_props/0.
    end

    defmodule MailglassAdmin.Fixtures.BrokenMailer do
      use Mailglass.Mailable, stream: :transactional

      def preview_props do
        raise "boom — deliberate fixture raise for Discovery test coverage"
      end
    end
    ```

    These fixture modules must be at `test/support/fixtures/mailables.ex`; `mailglass_admin/mix.exs` (built in Plan 02) will add `elixirc_paths: ["lib", "test/support"]` for `:test`.

    **`mailglass_admin/test/support/endpoint_case.ex`** — Synthetic adopter-endpoint harness. Defines a MINIMAL Phoenix.Endpoint + Router pair that mounts `MailglassAdmin.Router.mailglass_admin_routes/2` so the router expansion and `__session__/N` can be exercised against a real endpoint. Shape:

    ```elixir
    defmodule MailglassAdmin.TestAdopter.Endpoint do
      use Phoenix.Endpoint, otp_app: :mailglass_admin

      plug Plug.Session,
        store: :cookie,
        key: "_mailglass_admin_test_session",
        signing_salt: "test-salt-01234567",
        same_site: "Lax"

      plug MailglassAdmin.TestAdopter.Router
    end

    defmodule MailglassAdmin.TestAdopter.Router do
      use Phoenix.Router
      import Phoenix.LiveView.Router
      import MailglassAdmin.Router

      pipeline :browser do
        plug :accepts, ["html"]
        plug :fetch_session
        plug :fetch_live_flash
        plug :put_root_layout, html: {MailglassAdmin.Layouts, :root}
        plug :protect_from_forgery
        plug :put_secure_browser_headers
      end

      scope "/dev" do
        pipe_through :browser
        mailglass_admin_routes "/mail"
      end
    end

    defmodule MailglassAdmin.EndpointCase do
      use ExUnit.CaseTemplate

      using do
        quote do
          import Plug.Conn
          import Phoenix.ConnTest
          alias MailglassAdmin.TestAdopter.Router.Helpers, as: Routes
          @endpoint MailglassAdmin.TestAdopter.Endpoint
        end
      end

      setup_all do
        {:ok, _} = Application.ensure_all_started(:phoenix)
        _ = MailglassAdmin.TestAdopter.Endpoint.start_link()
        :ok
      end

      setup do
        {:ok, conn: Phoenix.ConnTest.build_conn()}
      end
    end
    ```

    Required config wiring for the synthetic endpoint goes in `mailglass_admin/config/test.exs` (Plan 02 owns creation of that file; for now, the EndpointCase must document its expectation). Add a one-line header comment in endpoint_case.ex stating: `# Requires mailglass_admin/config/test.exs to set :mailglass_admin, MailglassAdmin.TestAdopter.Endpoint secret_key_base (Plan 02).`

    **`mailglass_admin/test/support/live_view_case.ex`** — Phoenix.LiveViewTest wrapper:

    ```elixir
    defmodule MailglassAdmin.LiveViewCase do
      use ExUnit.CaseTemplate

      using do
        quote do
          import Plug.Conn
          import Phoenix.ConnTest
          import Phoenix.LiveViewTest
          @endpoint MailglassAdmin.TestAdopter.Endpoint
        end
      end

      setup_all do
        {:ok, _} = Application.ensure_all_started(:phoenix)
        _ = MailglassAdmin.TestAdopter.Endpoint.start_link()
        :ok
      end

      setup do
        {:ok, conn: Phoenix.ConnTest.build_conn()}
      end
    end
    ```

    All modules must be syntactically valid Elixir — even though they reference `MailglassAdmin.Router.mailglass_admin_routes/2` which does not yet exist. That's intentional: the feature plan that builds the router is a prerequisite for this test suite to compile, so Plan 03 is wired to drive that compile green. At Wave 0 completion, `cd mailglass_admin && mix compile` is expected to fail with an undefined `MailglassAdmin.Router` reference — that's the RED state the phase gate opens with.
  </action>
  <verify>
    <automated>test -f /Users/jon/projects/mailglass/mailglass_admin/test/test_helper.exs && test -f /Users/jon/projects/mailglass/mailglass_admin/test/support/endpoint_case.ex && test -f /Users/jon/projects/mailglass/mailglass_admin/test/support/live_view_case.ex && test -f /Users/jon/projects/mailglass/mailglass_admin/test/support/fixtures/mailables.ex</automated>
  </verify>
  <acceptance_criteria>
    - Four files exist at the exact paths above.
    - `grep -q "ExUnit.start" mailglass_admin/test/test_helper.exs` succeeds.
    - `grep -q "defmodule MailglassAdmin.EndpointCase" mailglass_admin/test/support/endpoint_case.ex` succeeds.
    - `grep -q "defmodule MailglassAdmin.TestAdopter.Router" mailglass_admin/test/support/endpoint_case.ex` succeeds.
    - `grep -q "mailglass_admin_routes \"/mail\"" mailglass_admin/test/support/endpoint_case.ex` succeeds (verifies the macro call is wired).
    - `grep -q "import Phoenix.LiveViewTest" mailglass_admin/test/support/live_view_case.ex` succeeds.
    - `grep -q "defmodule MailglassAdmin.Fixtures.HappyMailer" mailglass_admin/test/support/fixtures/mailables.ex` succeeds.
    - `grep -q "defmodule MailglassAdmin.Fixtures.StubMailer" mailglass_admin/test/support/fixtures/mailables.ex` succeeds.
    - `grep -q "defmodule MailglassAdmin.Fixtures.BrokenMailer" mailglass_admin/test/support/fixtures/mailables.ex` succeeds.
    - BrokenMailer's `preview_props/0` calls `raise "boom` (grep `grep -q 'raise "boom' mailglass_admin/test/support/fixtures/mailables.ex`).
    - HappyMailer's preview_props returns at least two scenarios — grep for both `welcome_default:` and `welcome_enterprise:` in the fixtures file.
  </acceptance_criteria>
  <done>
    Four support files committed; each fixture module uses `use Mailglass.Mailable`; EndpointCase embeds the `mailglass_admin_routes "/mail"` macro call so the router test in Plan 03 exercises real macro output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ten RED-by-default test files per 05-VALIDATION.md requirement map</name>
  <files>mailglass_admin/test/mailglass_admin/mix_config_test.exs, mailglass_admin/test/mailglass_admin/router_test.exs, mailglass_admin/test/mailglass_admin/preview_live_test.exs, mailglass_admin/test/mailglass_admin/discovery_test.exs, mailglass_admin/test/mailglass_admin/assets_test.exs, mailglass_admin/test/mailglass_admin/brand_test.exs, mailglass_admin/test/mailglass_admin/accessibility_test.exs, mailglass_admin/test/mailglass_admin/bundle_test.exs, mailglass_admin/test/mailglass_admin/voice_test.exs</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-VALIDATION.md (full file — maps each requirement to the required `<automated>` command)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md § "Accessibility" lines 519-535 (the exact contrast pairs + numeric ratios the accessibility test asserts)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-UI-SPEC.md § "Copywriting Contract" lines 448-479 (the exact string constants the voice test matches)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 760-775 (router __session__ isolation test skeleton)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md §"discovery_test.exs" (the exact fixture-based discovery test shape)
    - /Users/jon/projects/mailglass/test/mailglass/mailable_test.exs (test-module style reference: `use ExUnit.Case, async: true`, descriptive `test "..."` names)
  </read_first>
  <action>
    Write ten ExUnit test files — RED by default — each mapped to the `<automated>` command in 05-VALIDATION.md. These files drive the feature plans; do NOT implement any production code.

    **Shape conventions:**
    - Each file: `use ExUnit.Case, async: true` (exception: tests that use EndpointCase/LiveViewCase use those templates, not `ExUnit.Case`).
    - Every test has a concrete assertion — no `flunk "TODO"` placeholders. The asserts MAY reference undefined modules; that is the RED signal.
    - Tag router + LiveView tests with ExUnit tags per the `--only <tag>` columns in 05-VALIDATION.md:

    ---

    **1. `mix_config_test.exs`** (PREV-01) — `use ExUnit.Case, async: true`:

    Assert:
    - `Mix.Project.config()[:app] == :mailglass_admin`
    - `Mix.Project.config()[:version]` matches `@version` attribute in mix.exs
    - When `System.get_env("MIX_PUBLISH") == "true"`, the `:mailglass` dep tuple is `{:mailglass, "== <exact_version>"}` (use `Mix.Project.config()[:deps]` — find the mailglass tuple, assert the version string matches `Mix.Project.config()[:version]` exactly).
    - When `MIX_PUBLISH` is unset/false, the mailglass dep tuple includes `path: ".."` and `override: true`.

    The test must flip `MIX_PUBLISH` via `System.put_env`/`System.delete_env` + re-read `mix.exs` via `Code.eval_file/1` into a temp module since `Mix.Project.config/0` is cached. Acceptable pattern: evaluate the deps function by re-reading mix.exs — see the hex_publish discussion in RESEARCH.md lines 158-183.

    ---

    **2. `router_test.exs`** (PREV-02) — `use MailglassAdmin.EndpointCase, async: false` + tag `@tag :session_isolation` on the `__session__/2` test:

    - `test "mailglass_admin_routes/2 expands into four asset routes and two LiveView routes"`: expects the synthetic `MailglassAdmin.TestAdopter.Router.__routes__/0` to contain at least: one `GET /dev/mail/css-:md5`, one `GET /dev/mail/js-:md5`, one `GET /dev/mail/fonts/:name`, one `GET /dev/mail/logo.svg`, one `LIVE /dev/mail`, one `LIVE /dev/mail/:mailable/:scenario`. Use `Enum.any?/2` on the routes list matching on `path:` + `verb:`.
    - `test "__session__/2 returns whitelisted keys and never leaks adopter cookies", %{conn: conn}`: verbatim from 05-RESEARCH.md lines 760-775 — stuff `"current_user_id" => 42` + `"csrf_token" => "secret"` into the session, call `MailglassAdmin.Router.__session__(conn, mailables: :auto_scan, live_session_name: :test_session)`, `refute Map.has_key?(session, "current_user_id")`, `refute Map.has_key?(session, "csrf_token")`, `assert Enum.sort(Map.keys(session)) == ["live_session_name", "mailables"]`.
    - `test "invalid opts raise ArgumentError at compile time"`: asserts a module containing `mailglass_admin_routes "/x", bogus: true` fails to compile — use `assert_raise ArgumentError, ~r/invalid opts for mailglass_admin_routes\/2/, fn -> Code.eval_string(...) end`.

    ---

    **3. `preview_live_test.exs`** (PREV-03 + PREV-04) — `use MailglassAdmin.LiveViewCase, async: false`:

    One test per ExUnit tag from 05-VALIDATION.md:
    - `@tag :sidebar` — `test "sidebar renders discovered mailables with scenarios, no-previews, and error states"`: pass `mailables: [MailglassAdmin.Fixtures.HappyMailer, MailglassAdmin.Fixtures.StubMailer, MailglassAdmin.Fixtures.BrokenMailer]` via session (via a test-specific endpoint configuration), `live/2` to `/dev/mail/`, assert rendered HTML contains "HappyMailer", "welcome_default", "StubMailer", "No previews defined", "BrokenMailer", and a warning badge element (class includes `badge-warning` or text "Error").
    - `@tag :tabs` — `test "HTML, Text, Raw, Headers tabs each render the correct artifact"`: live_mount the HappyMailer `welcome_default` scenario, assert the HTML tab renders an `<iframe` tag with `srcdoc=`; switch tabs via `render_click/2` on each tab button; assert Text tab contains the literal rendered `text_body`, Raw tab contains MIME boundary-looking content, Headers tab contains `Message-ID` and `Date` rows (auto-injected).
    - `@tag :device_toggle` — `test "device width toggle updates iframe width CSS inline"`: mount, find the iframe's `style` attribute, assert it contains `width: 768px` (initial default per 05-UI-SPEC line 184). Click `phx-value-width="375"` button, re-render, assert `width: 375px` appears. Repeat for 1024.
    - `@tag :dark_toggle` — `test "dark chrome toggle flips data-theme on wrapper"`: mount, assert wrapper has `data-theme="mailglass-light"`, click the dark toggle, re-render, assert `data-theme="mailglass-dark"`.
    - `@tag :assigns_form` — `test "assigns form re-renders preview on change"`: mount HappyMailer `welcome_default`, find the `name="assigns[user_name]"` input, push `assigns_changed` event with `%{"assigns" => %{"user_name" => "Grace"}}`, re-render, assert the iframe srcdoc contains "Hi Grace".
    - `@tag :live_reload` — `test "PreviewLive subscribes to mailglass:admin:reload and refreshes on broadcast"`: mount, grab the live_pid, broadcast `{:phoenix_live_reload, :ignored, "lib/my_app/user_mailer.ex"}` to the `"mailglass:admin:reload"` topic on the endpoint's pubsub, `:timer.sleep(50)`, re-render, assert flash shows "Reloaded: user_mailer.ex".

    All tests MUST use the literal PubSub topic string `"mailglass:admin:reload"` — never the unprefixed form.

    ---

    **4. `discovery_test.exs`** (PREV-03 discovery portion) — `use ExUnit.Case, async: true`:

    Three tests (verbatim from 05-PATTERNS.md lines 750-763):
    - `test "explicit list returns scenarios for healthy mailable"`: `assert [{MailglassAdmin.Fixtures.HappyMailer, scenarios}] = MailglassAdmin.Preview.Discovery.discover([MailglassAdmin.Fixtures.HappyMailer])`; assert `Keyword.keys(scenarios) == [:welcome_default, :welcome_enterprise]`.
    - `test "stub mailable yields :no_previews sentinel"`: `assert [{MailglassAdmin.Fixtures.StubMailer, :no_previews}] = Discovery.discover([MailglassAdmin.Fixtures.StubMailer])`.
    - `test "raising preview_props/0 yields {:error, formatted_stacktrace}"`: `assert [{MailglassAdmin.Fixtures.BrokenMailer, {:error, msg}}] = Discovery.discover([MailglassAdmin.Fixtures.BrokenMailer])`; `assert msg =~ "boom"`.
    - `test "auto_scan includes fixture mailables when their OTP app is loaded"`: call `Discovery.discover(:auto_scan)`, assert the returned list contains a tuple whose first element is `MailglassAdmin.Fixtures.HappyMailer`.
    - `test "explicit list with non-mailable module raises ArgumentError with actionable message"`: `assert_raise ArgumentError, ~r/use Mailglass\.Mailable/, fn -> Discovery.discover([Enum]) end`.

    ---

    **5. `assets_test.exs`** (PREV-06 controller contract) — `use MailglassAdmin.EndpointCase, async: true`:

    - `test "GET /dev/mail/css-<hash> returns app.css with content-type and immutable cache"`: build conn, GET the path at `"/dev/mail/css-#{MailglassAdmin.Controllers.Assets.css_hash()}"`, assert response status 200, content-type starts with "text/css", `cache-control: public, max-age=31536000, immutable`, response body is binary > 1000 bytes.
    - `test "GET /dev/mail/js-<hash> serves concatenated phoenix.js + phoenix_live_view.js"`: same pattern — assert body contains `"Phoenix"` (phoenix.js exports) and `"LiveView"` (phoenix_live_view.js exports).
    - `test "GET /dev/mail/fonts/inter-400.woff2 serves allowlisted font"`: assert 200 + content-type "font/woff2".
    - `test "GET /dev/mail/fonts/..%2F..%2Fetc%2Fpasswd returns 404"`: assert conn status 404 after path-traversal attempt (font allowlist rejects).
    - `test "GET /dev/mail/logo.svg serves logo with image/svg+xml content-type"`: assert 200 + content-type "image/svg+xml".

    ---

    **6. `brand_test.exs`** (PREV-05 palette) — `use ExUnit.Case, async: true`:

    - `test "compiled priv/static/app.css defines all six brand palette hex values"`: `File.read!(Application.app_dir(:mailglass_admin, "priv/static/app.css"))`, assert the string contains each canonical hex (lowercase or uppercase acceptable — use case-insensitive regex): `"#0D1B2A"`, `"#277B96"`, `"#A6EAF2"`, `"#EAF6FB"`, `"#F8FBFD"`, `"#5C6B7A"`.
    - `test "mailglass-light theme defines canonical daisyUI tokens"`: assert CSS contains literal substrings `--color-base-100: #F8FBFD`, `--color-primary: #277B96`, `--color-base-content: #0D1B2A` (per 05-UI-SPEC lines 113-122).
    - `test "mailglass-dark theme exists and is inverted"`: assert CSS contains `name: "mailglass-dark"` inside a `@plugin` block.
    - `test "compiled CSS bans glassmorphism primitives"`: assert CSS does NOT contain `backdrop-filter` or `box-shadow: inset`.
    - `test "depth and noise disabled"`: assert CSS contains `--depth: 0` and `--noise: 0` in both themes.

    ---

    **7. `accessibility_test.exs`** (PREV-05 contrast) — `use ExUnit.Case, async: true`:

    Define a private `contrast_ratio/2` helper using the standard WCAG 2.1 relative luminance formula — `(lighter + 0.05) / (darker + 0.05)`, with relative luminance as `0.2126 * R + 0.7152 * G + 0.0722 * B` after sRGB gamma expansion. Then assert (per 05-UI-SPEC lines 524-532):
    - Ink on Paper (`#0D1B2A` vs `#F8FBFD`) ≥ 15.0
    - Slate on Paper (`#5C6B7A` vs `#F8FBFD`) ≥ 4.5
    - Paper on Glass (`#F8FBFD` vs `#277B96`) ≥ 4.5 (passes AA for large text / UI)
    - Mist on Ink (`#EAF6FB` vs `#0D1B2A`) ≥ 15.0
    - Signal Amber on Paper (`#A95F10` vs `#F8FBFD`) ≥ 4.5
    - Error Crimson on Paper (`#B42318` vs `#F8FBFD`) ≥ 4.5
    - Success Pine on Paper (`#166534` vs `#F8FBFD`) ≥ 4.5

    Explicit NEGATIVE test: Glass on Paper body text (`#277B96` vs `#F8FBFD`) < 4.5 — documenting why Glass is not permitted as small body text.

    ---

    **8. `bundle_test.exs`** (PREV-06 size budget) — `use ExUnit.Case, async: true`:

    - `test "priv/static/app.css exists and is < 150KB"`: `%{size: size} = File.stat!(Application.app_dir(:mailglass_admin, "priv/static/app.css"))`; assert `size > 0 and size < 150_000`.
    - `test "priv/static/fonts/ contains exactly six woff2 files (Inter 400/700 + Inter Tight 400/700 + IBM Plex Mono 400/700)"`: list the fonts dir, assert sorted filenames are `["ibm-plex-mono-400.woff2", "ibm-plex-mono-700.woff2", "inter-400.woff2", "inter-700.woff2", "inter-tight-400.woff2", "inter-tight-700.woff2"]` — note the 2-weights-per-family lock from 05-UI-SPEC lines 71-79 (not 3).
    - `test "priv/static/mailglass-logo.svg exists and is under 20KB"`: stat + size check.
    - `test "total priv/static/ size is under 800KB"`: recursive file size sum < 800_000.

    ---

    **9. `voice_test.exs`** (BRAND-01) — `use MailglassAdmin.LiveViewCase, async: false`:

    - `test "banned exclamations are absent from rendered UI"`: mount `/dev/mail/`, grab rendered HTML, assert it does NOT contain `"Oops"`, `"Something went wrong"`, `"Whoops"`, `"Uh oh"` (case-insensitive via `String.downcase/1`).
    - `test "canonical brand copy strings appear verbatim"`: mount `/dev/mail/` with a session containing `StubMailer` and `BrokenMailer`, assert each of these literal strings appears somewhere in the rendered HTML (from 05-UI-SPEC Copywriting Contract table):
      - `"Mailers"` (sidebar heading)
      - `"Select a scenario from the sidebar to preview it."` (empty main pane)
      - `"No previews defined"` (stub mailable card)
      - `"preview_props/0 raised an error"` (error card heading — ONLY when BrokenMailer fixture is loaded)
    - `test "button labels use verb+noun form"`: mount a HappyMailer scenario, assert rendered buttons include `"Render preview"` and `"Reset assigns"` (not bare `"Render"` / `"Reset"`).
    - `test "LiveReload topic subscription info log fires exactly once per boot"` — SKIP for now with `@tag :skip` (depends on Plan 06's persistent_term gating).

    All assertions must reference the LITERAL strings from 05-UI-SPEC — copy verbatim.

    ---

    **Across all files:** Use `async: true` unless the test mounts the synthetic endpoint (EndpointCase/LiveViewCase force `async: false`). Each file compiles as standalone Elixir — references to `MailglassAdmin.Preview.Discovery`, `MailglassAdmin.Router`, `MailglassAdmin.Controllers.Assets`, etc. will be unresolved until feature plans land, which is the expected RED state.
  </action>
  <verify>
    <automated>for f in mix_config_test router_test preview_live_test discovery_test assets_test brand_test accessibility_test bundle_test voice_test; do test -f /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/$f.exs || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test files exist at `mailglass_admin/test/mailglass_admin/*.exs`.
    - Each file starts with `defmodule MailglassAdmin.` and ends with `end`.
    - `grep -q '@tag :session_isolation' mailglass_admin/test/mailglass_admin/router_test.exs` succeeds.
    - `grep -q '@tag :sidebar' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -q '@tag :tabs' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -q '@tag :device_toggle' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -q '@tag :dark_toggle' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -q '@tag :assigns_form' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -q '@tag :live_reload' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -q '"mailglass:admin:reload"' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds (prefixed form, never the unprefixed typo).
    - `grep -q 'Phoenix.LiveViewTest' mailglass_admin/test/mailglass_admin/preview_live_test.exs` succeeds.
    - `grep -c '#0D1B2A' mailglass_admin/test/mailglass_admin/brand_test.exs` returns at least 1.
    - `grep -q 'contrast_ratio' mailglass_admin/test/mailglass_admin/accessibility_test.exs` succeeds.
    - `grep -q '"Render preview"' mailglass_admin/test/mailglass_admin/voice_test.exs` succeeds.
    - `grep -q '"Reset assigns"' mailglass_admin/test/mailglass_admin/voice_test.exs` succeeds.
    - `grep -q '"No previews defined"' mailglass_admin/test/mailglass_admin/voice_test.exs` succeeds.
    - `grep -q 'refute Map.has_key?(session, "current_user_id")' mailglass_admin/test/mailglass_admin/router_test.exs` succeeds (session isolation test present).
    - `grep -q 'MailglassAdmin.Preview.Discovery' mailglass_admin/test/mailglass_admin/discovery_test.exs` succeeds.
    - Each test file contains at least one `test "..."` block (grep `^\s*test "` on each file).
  </acceptance_criteria>
  <done>
    Nine test files exist per 05-VALIDATION.md Wave 0 list. Every `<automated>` command referenced in 05-VALIDATION.md § Per-Task Verification Map now points at a real file (even if RED). Feature plans 02-06 drive them green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning-docs ↔ shipped-code | Canonical planning docs (PROJECT/REQUIREMENTS/ROADMAP) are trusted references for every downstream agent; drift between them and shipped code is a process-integrity risk (D-10 corrected this for preview_props arity). |
| test-support ↔ test-suites | Test harness code (support/*) is trusted by every test file; a bug here cascades across all admin test coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | mailglass_admin/priv/static/ committed bundle | mitigate (preparation) | bundle_test.exs asserts file existence + size budgets so when CI `git diff --exit-code priv/static/` lands in Plan 05/Phase 7, there is a RED/GREEN signal for every expected artifact. |
| T-05-04 | Denial of Service | Mailable discovery (raising preview_props/0) | mitigate (preparation) | BrokenMailer fixture + discovery_test.exs `test "raising preview_props/0 yields {:error, formatted_stacktrace}"` is the RED target Plan 04 drives GREEN via try/rescue in reflect/1. |
</threat_model>

<verification>
- `cd /Users/jon/projects/mailglass && grep -c "preview_props/1" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md 2>/dev/null | grep -v ':0$'` returns EMPTY (no docs still using `/1`).
- `ls -1 mailglass_admin/test/support/` shows: endpoint_case.ex, fixtures, live_view_case.ex (at least).
- `ls -1 mailglass_admin/test/support/fixtures/` shows: mailables.ex.
- `ls -1 mailglass_admin/test/mailglass_admin/*.exs | wc -l` returns 9.
- `cd mailglass_admin && mix compile 2>&1 | grep -q "MailglassAdmin.Router"` — expected to FAIL compile at this wave (RED state is correct; Plan 03 drives it GREEN).
</verification>

<success_criteria>
- Three canonical planning docs (PROJECT.md / REQUIREMENTS.md / ROADMAP.md) contain zero `preview_props/1` occurrences.
- `mailglass_admin/test/test_helper.exs` + 3 support files exist and contain the required module declarations.
- All 9 feature-driven test files exist at `mailglass_admin/test/mailglass_admin/*.exs` with the correct tags + literal string references from 05-UI-SPEC.
- Every `<automated>` entry in 05-VALIDATION.md maps to a file that exists on disk (may be RED — that's expected until later plans land).
- Fixture modules compile standalone via `use Mailglass.Mailable` — `cd mailglass_admin && mix compile test/support/fixtures/mailables.ex 2>&1 | grep -qv "error"` once mailglass_admin's mix.exs (Plan 02) is in place; for this plan, committing the files is sufficient.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dev-preview-liveview/05-01-SUMMARY.md` documenting:
- Exact doc-fix edits (file + line + before/after)
- Support module structure decisions (why the synthetic endpoint lives in EndpointCase vs separate module)
- Any test-harness deferred items (e.g., fixture subclasses that needed `@tag :skip` and why)
- Notes for Plan 02 executor about `mailglass_admin/config/test.exs` + `elixirc_paths(:test)` requirements the support files depend on.
</output>