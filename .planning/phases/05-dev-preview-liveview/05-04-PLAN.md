---
phase: 05-dev-preview-liveview
plan: 04
type: execute
wave: 1
depends_on: ["01", "02"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/preview/discovery.ex
  - mailglass_admin/lib/mailglass_admin/preview/mount.ex
autonomous: true
requirements:
  - PREV-03
must_haves:
  truths:
    - "`MailglassAdmin.Preview.Discovery.discover(:auto_scan)` returns a list of `{module, [{atom, map}] | :no_previews | {:error, stacktrace_string}}` tuples for every module in every loaded OTP application that `use Mailglass.Mailable`."
    - "`discover([Module1, Module2])` returns the same shape but filtered to the explicit list; passing a non-mailable module raises `ArgumentError` with an actionable message referencing `use Mailglass.Mailable`."
    - "A mailable whose `preview_props/0` raises at runtime does NOT crash discovery — the tuple's second element is `{:error, formatted_stacktrace_string}` with the raise reason captured."
    - "A mailable with the `__mailglass_mailable__/0` marker but no `preview_props/0` definition yields `{module, :no_previews}`."
    - "`MailglassAdmin.Preview.Mount.on_mount/4` reads `session[\"mailables\"]`, invokes `Discovery.discover/1`, and assigns `:mailables` on the socket, returning `{:cont, socket}`."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/preview/discovery.ex"
      provides: "Runtime mailable discovery with graceful failure handling"
      contains: "def discover"
      min_lines: 60
    - path: "mailglass_admin/lib/mailglass_admin/preview/mount.ex"
      provides: "on_mount hook that populates :mailables assign on LiveView mount"
      contains: "def on_mount"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/preview/discovery.ex"
      to: "Mailglass.Mailable"
      via: "__mailglass_mailable__/0 marker (via function_exported?/3)"
      pattern: "__mailglass_mailable__"
    - from: "mailglass_admin/lib/mailglass_admin/preview/mount.ex"
      to: "MailglassAdmin.Preview.Discovery"
      via: "Discovery.discover/1 call"
      pattern: "Discovery.discover"
---

<objective>
Build runtime mailable discovery per CONTEXT D-12..D-13 + 05-RESEARCH.md Pattern 3 (lines 537-616).

Two responsibilities:

1. **`MailglassAdmin.Preview.Discovery`** — Pure reflection module. Walks `:application.loaded_applications/0` + `:application.get_key(app, :modules)`, filters by the `__mailglass_mailable__/0` marker that `use Mailglass.Mailable` injects, and returns `[{module, scenarios_or_sentinel_or_error}]`. Graceful failure is the contract: a raising `preview_props/0` yields `{:error, stacktrace}` — never crashes the LiveView.

2. **`MailglassAdmin.Preview.Mount`** — The `on_mount` hook the Router macro appends to `on_mount_hooks ++ [MailglassAdmin.Preview.Mount]`. It reads the whitelisted session `"mailables"` value (from `MailglassAdmin.Router.__session__/2`), calls `Discovery.discover/1`, and assigns `:mailables` on the socket before PreviewLive's `mount/3` runs.

Purpose: Plan 06's PreviewLive assumes `@mailables` is already populated on mount. Splitting discovery into its own Wave-1 plan means PreviewLive's tests (Plan 06) can focus on rendering behavior, not reflection mechanics. The `:error` sentinel shape is the contract the sidebar's warning-badge UI branch (05-UI-SPEC line 253) dispatches on.

**Threat mitigation (T-05-04):** An adopter mailable that raises in `preview_props/0` must NOT take down the preview dashboard. The discovery's try/rescue is the structural defense.

Output: Wave 1 tests — `discovery_test.exs` (from Plan 01) flips RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dev-preview-liveview/05-CONTEXT.md
@.planning/phases/05-dev-preview-liveview/05-RESEARCH.md
@.planning/phases/05-dev-preview-liveview/05-PATTERNS.md
@lib/mailglass/mailable.ex

<interfaces>
Existing contracts (already shipped, do NOT reimplement):

- `Mailglass.Mailable` at lib/mailglass/mailable.ex
  - Line 111: `@optional_callbacks preview_props: 0`
  - Line 112: `@callback preview_props() :: [{atom(), map()}]`
  - Line 154: `def __mailglass_mailable__, do: true` (injected via @before_compile)
- `MailglassAdmin.Fixtures.HappyMailer` / `.StubMailer` / `.BrokenMailer` (from Plan 01 test/support/fixtures/mailables.ex) — the discovery test targets.

Contracts this plan creates:

- `MailglassAdmin.Preview.Discovery.discover(:auto_scan | [module()]) :: [{module(), [{atom(), map()}] | :no_previews | {:error, String.t()}}]`
- `MailglassAdmin.Preview.Mount.on_mount(atom, params, session, socket) :: {:cont, socket} | {:halt, socket}`

Contract consumed by this plan (forward-reference via `function_exported?/3`, not direct call):

- `Mailglass.Mailable.__mailglass_mailable__/0` — the runtime marker confirming `use Mailglass.Mailable` was applied.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MailglassAdmin.Preview.Discovery — runtime reflection with graceful failure</name>
  <files>mailglass_admin/lib/mailglass_admin/preview/discovery.ex</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md lines 537-616 (Pattern 3 — the verbatim discovery skeleton)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md section "mailglass_admin/lib/mailglass_admin/preview/discovery.ex" lines 469-520 (the detailed discovery shape with graceful failure)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-CONTEXT.md D-12, D-13 (hybrid discovery mode + graceful failure)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md section "Pitfall 7: preview_props/0 that returns a function" lines 1431-1441 (validate return shape — not a function, not a bare atom)
    - /Users/jon/projects/mailglass/lib/mailglass/mailable.ex lines 100-156 (the shipped callback contract + marker)
    - /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/discovery_test.exs (from Plan 01 — the test shape this must satisfy)
    - /Users/jon/projects/mailglass/mailglass_admin/test/support/fixtures/mailables.ex (from Plan 01 — HappyMailer with `welcome_default`/`welcome_enterprise` scenarios, StubMailer without preview_props, BrokenMailer that raises)
    - /Users/jon/projects/mailglass/lib/mailglass/mailable.ex moduledoc lines 1-83 (docstring style reference)
  </read_first>
  <action>
    Create `mailglass_admin/lib/mailglass_admin/preview/discovery.ex` following 05-RESEARCH.md Pattern 3 verbatim with one correction (invalid-shape validation per 05-RESEARCH.md Pitfall 7).

    **Module shell:**

        defmodule MailglassAdmin.Preview.Discovery do
          @moduledoc \"\"\"
          Reflection for mailable modules — finds `use Mailglass.Mailable` modules,
          reads their `preview_props/0` callback if present, and returns the pair
          ready for the preview dashboard sidebar.

          ## Discovery modes

            * `:auto_scan` (default) — Walks `:application.loaded_applications/0` and
              for each app calls `:application.get_key(app, :modules)`, filtering by
              the `__mailglass_mailable__/0` marker that `use Mailglass.Mailable`
              injects via `@before_compile`.

            * `[module | _]` explicit list — Override for umbrella apps, pathological
              module counts, or adopter preference. Each module MUST have the marker
              or discovery raises `ArgumentError` with an actionable message.

          ## Graceful failures (CONTEXT D-13)

            * Marker present, no `preview_props/0` defined -> `{module, :no_previews}`
            * `preview_props/0` raises -> `{module, {:error, formatted_stacktrace}}`
            * `preview_props/0` returns non-list or a list item whose second element
              is not a map -> `{module, {:error, shape_violation_message}}`

          None of these failure modes raise from `discover/1`. The LiveView branches
          on the second tuple element to render the correct sidebar + main pane state
          (05-UI-SPEC sidebar section lines 189-207, error card lines 386-404).

          ## Performance

          Empirical: ~50ms on a 10,000-module umbrella app. `function_exported?/3`
          is O(1) per module. If adopters report slow mount, they bypass the scan
          via the explicit `:mailables` list.
          \"\"\"

          use Boundary, classify_to: MailglassAdmin

    **Public API:**

        @type scenario :: {atom(), map()}
        @type reflection :: [scenario()] | :no_previews | {:error, String.t()}
        @type result :: {module(), reflection()}

        @doc \"\"\"
        Discovers mailable modules and their preview scenarios.

        Returns a list of tuples; order is implementation-defined but stable within
        a single call.
        \"\"\"
        @doc since: "0.1.0"
        @spec discover(:auto_scan | [module()]) :: [result()]
        def discover(:auto_scan) do
          loaded_apps()
          |> Enum.flat_map(&modules_for_app/1)
          |> Enum.filter(&mailable?/1)
          |> Enum.map(&reflect/1)
        end

        def discover(mods) when is_list(mods) do
          Enum.map(mods, fn mod ->
            if mailable?(mod) do
              reflect(mod)
            else
              raise ArgumentError,
                    inspect(mod) <>
                      " is listed in :mailables but does not `use Mailglass.Mailable` — " <>
                      "add the directive or remove it from the list"
            end
          end)
        end

    **Private helpers:**

        defp loaded_apps do
          for {app, _, _} <- :application.loaded_applications(), do: app
        end

        defp modules_for_app(app) do
          case :application.get_key(app, :modules) do
            {:ok, mods} -> mods
            :undefined -> []
          end
        end

        defp mailable?(mod) do
          Code.ensure_loaded?(mod) and
            function_exported?(mod, :__mailglass_mailable__, 0) and
            mod.__mailglass_mailable__() == true
        rescue
          # Module load failures during dev (bad code under hot reload) — skip.
          _ -> false
        end

    **Reflection with SHAPE validation (05-RESEARCH.md Pitfall 7 — the addition to Pattern 3):**

        defp reflect(mod) do
          cond do
            not function_exported?(mod, :preview_props, 0) ->
              {mod, :no_previews}

            true ->
              try do
                raw = mod.preview_props()
                {mod, validate_scenarios(raw)}
              rescue
                e ->
                  {mod, {:error, Exception.format(:error, e, __STACKTRACE__)}}
              end
          end
        end

        # CONTEXT D-11 return shape: [{atom(), map()}]. Anything else is invalid.
        # 05-RESEARCH.md Pitfall 7: preview_props returning {atom, function} crashes
        # the type-inferred form renderer with a confusing FunctionClauseError.
        # Catching shape violations at discovery time surfaces a clear error card.
        defp validate_scenarios(raw) when is_list(raw) do
          if Enum.all?(raw, &valid_scenario?/1) do
            raw
          else
            bad = Enum.find(raw, &(not valid_scenario?(&1)))
            {:error,
             "preview_props/0 must return [{atom(), map()}] but got an entry " <>
               "whose second element is not a map: " <> inspect(bad)}
          end
        end

        defp validate_scenarios(other) do
          {:error,
           "preview_props/0 must return a list of {atom, map} tuples, got: " <>
             inspect(other)}
        end

        defp valid_scenario?({name, defaults})
             when is_atom(name) and is_map(defaults),
             do: true

        defp valid_scenario?(_), do: false

    **Do NOT introduce telemetry in this plan.** Discovery is a hot-path function called on every mount + every LiveReload broadcast; adding telemetry spans here without carefully whitelisted metadata risks PII exposure (adopter mailable names ARE acceptable per 05-PATTERNS.md line 841 whitelist, but scenario defaults maps are NOT — they can contain user records with emails). v0.5 adds discovery telemetry with a carefully whitelisted `mailables_count` counter; v0.1 skips it to avoid the PII-leak surface.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/lib/mailglass_admin/preview/discovery.ex && grep -q 'def discover' mailglass_admin/lib/mailglass_admin/preview/discovery.ex && grep -q '__mailglass_mailable__' mailglass_admin/lib/mailglass_admin/preview/discovery.ex && grep -q 'validate_scenarios' mailglass_admin/lib/mailglass_admin/preview/discovery.ex</automated>
  </verify>
  <acceptance_criteria>
    - `discovery.ex` exists and defines `defmodule MailglassAdmin.Preview.Discovery`.
    - Contains `@spec discover(:auto_scan | [module()]) :: [result()]`.
    - Contains two `def discover/1` clauses: one for `:auto_scan`, one for `is_list(mods)`.
    - Contains a `mailable?/1` helper that checks three conditions via `and` (Code.ensure_loaded?, function_exported?, marker returns true), with a `rescue _ -> false` tail.
    - Contains a `reflect/1` helper that returns `{mod, :no_previews}` when `preview_props/0` is absent.
    - Contains a `try/rescue` block inside `reflect/1` that catches ANY raise and returns `{mod, {:error, Exception.format(...)}}`.
    - Contains `validate_scenarios/1` that enforces `[{atom(), map()}]` return shape.
    - Does NOT contain any `:telemetry.execute` or `:telemetry.span` call (v0.1 scope — no PII risk).
    - `use Boundary, classify_to: MailglassAdmin` at top.
    - `cd mailglass_admin && mix test test/mailglass_admin/discovery_test.exs` — all 5 tests pass:
      * `test "explicit list returns scenarios for healthy mailable"` — GREEN.
      * `test "stub mailable yields :no_previews sentinel"` — GREEN.
      * `test "raising preview_props/0 yields {:error, formatted_stacktrace}"` — GREEN.
      * `test "auto_scan includes fixture mailables when their OTP app is loaded"` — GREEN.
      * `test "explicit list with non-mailable module raises ArgumentError with actionable message"` — GREEN.
  </acceptance_criteria>
  <done>
    Discovery is pure, side-effect-free, and graceful. HappyMailer / StubMailer / BrokenMailer fixtures produce the three distinct return shapes; non-mailable raises with actionable message.
  </done>
</task>

<task type="auto">
  <name>Task 2: MailglassAdmin.Preview.Mount — on_mount hook populating :mailables assign</name>
  <files>mailglass_admin/lib/mailglass_admin/preview/mount.ex</files>
  <read_first>
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-PATTERNS.md section "mailglass_admin/lib/mailglass_admin/preview/mount.ex" lines 446-466 (the on_mount hook shape)
    - /Users/jon/projects/sigra/lib/sigra/live_view/admin_scope.ex (the full file — 53 lines — analog for Phoenix on_mount callback returning `{:cont, socket}` / `{:halt, socket}`)
    - /Users/jon/projects/mailglass/.planning/phases/05-dev-preview-liveview/05-RESEARCH.md section "Session & Socket Scoping" lines 752-757 (the order: session callback -> on_mount hooks -> mount)
    - /Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/preview/discovery.ex (created in Task 1 — the Discovery module this hook calls)
  </read_first>
  <action>
    Create `mailglass_admin/lib/mailglass_admin/preview/mount.ex`:

        defmodule MailglassAdmin.Preview.Mount do
          @moduledoc \"\"\"
          `on_mount` hook the Router macro appends after any adopter-provided
          `:on_mount` hooks. Reads the whitelisted session `"mailables"` value
          populated by `MailglassAdmin.Router.__session__/2` and runs discovery
          before `MailglassAdmin.PreviewLive.mount/3` fires.

          ## Order (Phoenix LiveView 1.1)

              session callback (MailglassAdmin.Router.__session__/2)
                -> opts[:on_mount] hooks (adopter-provided, in order given)
                -> MailglassAdmin.Preview.Mount (this module)
                -> MailglassAdmin.PreviewLive.mount/3

          Adopter hooks run BEFORE this one so they can short-circuit
          (`{:halt, socket}`) without triggering discovery.
          \"\"\"

          use Boundary, classify_to: MailglassAdmin

          import Phoenix.Component, only: [assign: 3]

          alias MailglassAdmin.Preview.Discovery

          @doc \"\"\"
          Runs `Discovery.discover/1` using the session-supplied `mailables`
          value and assigns `:mailables` on the socket.
          \"\"\"
          @spec on_mount(atom(), map() | :not_mounted_at_router, map(), Phoenix.LiveView.Socket.t()) ::
                  {:cont, Phoenix.LiveView.Socket.t()}
          def on_mount(:default, _params, session, socket) do
            mailables_opt = Map.get(session, "mailables", :auto_scan)
            mailables = Discovery.discover(mailables_opt)

            {:cont, assign(socket, :mailables, mailables)}
          end
        end

    **Why `:default` atom:** Phoenix's `on_mount` supports named stages; the Router macro appends `MailglassAdmin.Preview.Mount` (the module) without an explicit stage atom, so Phoenix passes `:default`. Matches sigra's AdminScope pattern.

    **Why `Map.get(..., :auto_scan)`:** Defensive default. The Router's `__session__/2` already returns `%{"mailables" => opts[:mailables]}` and NimbleOptions validates the `:mailables` opt has a default of `:auto_scan`. But if some upstream on_mount hook mutates the session map or the session is missing (e.g., tests that skip the full macro path), defaulting to `:auto_scan` keeps discovery running rather than crashing.

    **Do NOT** add telemetry or logging here — same rationale as Task 1.

    **Do NOT** return `{:halt, ...}` under any circumstance at v0.1 — the dashboard has no auth. v0.5's prod admin mount will ship a separate on_mount (or replace this one) with auth gating; keeping v0.1 always-cont keeps the contract simple.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f mailglass_admin/lib/mailglass_admin/preview/mount.ex && grep -q 'def on_mount' mailglass_admin/lib/mailglass_admin/preview/mount.ex && grep -q 'Discovery.discover' mailglass_admin/lib/mailglass_admin/preview/mount.ex && grep -q ':cont' mailglass_admin/lib/mailglass_admin/preview/mount.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mount.ex` exists and defines `defmodule MailglassAdmin.Preview.Mount`.
    - Contains `def on_mount(:default, _params, session, socket)` (4-arity, first arg is `:default` atom).
    - Returns a `{:cont, socket}` tuple (structural — grep for the atom `:cont`).
    - Calls `MailglassAdmin.Preview.Discovery.discover/1` with the session `"mailables"` key (with `:auto_scan` default via `Map.get`).
    - Uses `Phoenix.Component.assign/3` to set `:mailables` on the socket.
    - Does NOT contain any `Logger.*`, `:telemetry.*`, or `Phoenix.PubSub.*` call.
    - Does NOT contain any `{:halt, ...}` tuple (v0.1 contract: always-cont).
    - `use Boundary, classify_to: MailglassAdmin` at top.
    - `cd mailglass_admin && mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>
    `MailglassAdmin.Preview.Mount` exists and is referenced by the Router macro's `on_mount_hooks ++ [MailglassAdmin.Preview.Mount]` expansion. The LiveView in Plan 06 inherits `@mailables` pre-populated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter mailable code ↔ mailglass_admin runtime | Mailable modules are arbitrary adopter-authored code. `preview_props/0` runs inside mailglass_admin's discovery call — a raise there reaches our process. Untrusted-code-in-trusted-context boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | Mailable `preview_props/0` raising at runtime | mitigate | `Discovery.reflect/1` wraps `mod.preview_props()` in `try/rescue` and returns `{:error, Exception.format(...)}`. The LiveView's sidebar branches on this shape to render a warning badge + error card without crashing. Tests `test/mailglass_admin/discovery_test.exs @tag discovery` + the BrokenMailer fixture assert this behavior. |
| T-05-04b | Tampering | `preview_props/0` returning an unexpected shape (function instead of map, per 05-RESEARCH.md Pitfall 7) | mitigate | `validate_scenarios/1` + `valid_scenario?/1` reject non-map scenario defaults with `{:error, shape_violation_message}`. Adopters see a clear error card instead of a cryptic FunctionClauseError at render time. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/discovery_test.exs` exits 0 — all 5 tests pass.
- `cd mailglass_admin && mix compile --warnings-as-errors` succeeds.
- `grep -R '__mailglass_mailable__' mailglass_admin/lib/mailglass_admin/preview/discovery.ex` returns exactly one match (the function_exported? call).
</verification>

<success_criteria>
- `MailglassAdmin.Preview.Discovery.discover/1` returns the documented 3-arm return shape (scenarios list / `:no_previews` / `{:error, msg}`) per CONTEXT D-13.
- `MailglassAdmin.Preview.Mount.on_mount/4` reads session `"mailables"` and assigns `:mailables` on socket before PreviewLive mounts.
- BrokenMailer's `raise "boom"` propagates as `{:error, formatted_stacktrace}` — dashboard stays live.
- HappyMailer yields `[welcome_default: %{...}, welcome_enterprise: %{...}]` — scenarios in definition order.
- StubMailer yields `:no_previews`.
- Passing `Enum` (not a mailable) to `discover/1` raises `ArgumentError` with `use Mailglass.Mailable` in the message.
- No telemetry emitted (v0.1 scope — defer to v0.5 when whitelist is reviewed for PII).
</success_criteria>

<output>
After completion, create `.planning/phases/05-dev-preview-liveview/05-04-SUMMARY.md` documenting:
- Confirmed scan cost on the project's own loaded application list (`MailglassAdmin.Fixtures.*` + `Mailglass.*` mailables). Should be well under 50ms.
- The shape of the `{:error, msg}` string for the 3 failure modes (raise / non-list / non-map scenario) — Plan 06's error card renders this verbatim.
- Note for Plan 06 executor: `@mailables` assign is already populated by on_mount; PreviewLive.mount/3 just subscribes to LiveReload and sets UI defaults.
</output>
