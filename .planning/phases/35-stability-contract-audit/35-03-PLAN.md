---
phase: 35-stability-contract-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 35-01
  - 35-02
files_modified:
  - lib/mix/tasks/mailglass.install.ex
  - lib/mix/tasks/mailglass.reconcile.ex
  - lib/mix/tasks/mail.doctor.ex
  - lib/mix/tasks/mailglass.publish.check.ex
  - lib/mix/tasks/mailglass.docs.check.ex
  - lib/mix/tasks/mailglass.stability.check.ex
  - test/mailglass/docs_contract_test.exs
  - test/mailglass/stability_contract_test.exs
  - mailglass_admin/test/mailglass_admin/stability_contract_test.exs
autonomous: true
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
  - LOCK-04
must_haves:
  truths:
    - "Every stable public API in the Phase 35 contract inventory exposes truthful `since` or deprecation metadata at the point of use."
    - "Generated docs can surface the contract metadata instead of relying only on planning notes."
    - "Light docs-contract checks guard the new inventory wording without turning Phase 35 into heavy enforcement."
  artifacts:
    - path: "lib/mix/tasks/mailglass.install.ex"
      provides: "Stable task metadata visible in generated docs"
      contains: "defmodule Mix.Tasks.Mailglass.Install"
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Light docs drift checks aligned to the v1.x contract inventory"
      contains: "defmodule Mix.Tasks.Mailglass.Docs.Check"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "Docs contract regression coverage for the stability inventory"
      min_lines: 120
    - path: "test/mailglass/stability_contract_test.exs"
      provides: "Compiled-doc truth audit for core stable surfaces"
      min_lines: 80
    - path: "mailglass_admin/test/mailglass_admin/stability_contract_test.exs"
      provides: "Compiled-doc truth audit for admin stable surfaces"
      min_lines: 80
  key_links:
    - from: "lib/mix/tasks/mailglass.docs.check.ex"
      to: "README.md"
      via: "required tokens"
      pattern: "README\\.md"
    - from: "test/mailglass/docs_contract_test.exs"
      to: "docs/api_stability.md"
      via: "contract assertions"
      pattern: "api_stability"
    - from: "mix docs"
      to: "lib/mix/tasks/mailglass.install.ex"
      via: "generated ExDoc output"
      pattern: "Since"
---

<objective>
Complete the public metadata audit and align the existing light docs-contract checks to the new Phase 35 stability inventory.

Purpose: Satisfy LOCK-04 across the full stable public inventory and guard the documented contract without pulling Phase 37’s heavy enforcement work forward.
Output: Stable metadata on all public inventory items plus refreshed docs checks and exhaustive compiled-doc/docs-contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-stability-contract-audit/35-RESEARCH.md
@.planning/phases/34-verification-regression-closure/34-VERIFICATION.md
@lib/mix/tasks/mailglass.install.ex
@lib/mix/tasks/mailglass.reconcile.ex
@lib/mix/tasks/mail.doctor.ex
@lib/mix/tasks/mailglass.publish.check.ex
@lib/mix/tasks/mailglass.docs.check.ex
@lib/mix/tasks/mailglass.stability.check.ex
@test/mailglass/docs_contract_test.exs
@test/mailglass/stability_contract_test.exs
@mailglass_admin/test/mailglass_admin/stability_contract_test.exs

<interfaces>
From lib/mix/tasks/mailglass.docs.check.ex:
```elixir
@tier1_paths [...]
@tier1_surface_rules %{ ... }
def run(argv) do
```

From test/mailglass/docs_contract_test.exs:
```elixir
describe "README.md contract" do
describe "Task existence" do
describe "Guide contracts" do
```

From the stable public task modules:
```elixir
defmodule Mix.Tasks.Mailglass.Install do
  @moduledoc \"\"\"
  ## Usage
      mix mailglass.install
  \"\"\"
  def run(argv) do
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete point-of-use metadata for the full stable public inventory</name>
  <read_first>
    - lib/mix/tasks/mailglass.install.ex
    - lib/mix/tasks/mailglass.reconcile.ex
    - lib/mix/tasks/mail.doctor.ex
    - lib/mix/tasks/mailglass.publish.check.ex
    - lib/mix/tasks/mailglass.docs.check.ex
    - lib/mix/tasks/mailglass.stability.check.ex
    - .planning/phases/35-stability-contract-audit/35-RESEARCH.md
    - test/mailglass/stability_contract_test.exs
    - mailglass_admin/test/mailglass_admin/stability_contract_test.exs
  </read_first>
  <files>lib/mix/tasks/mailglass.install.ex, lib/mix/tasks/mailglass.reconcile.ex, lib/mix/tasks/mail.doctor.ex, lib/mix/tasks/mailglass.publish.check.ex, lib/mix/tasks/mailglass.docs.check.ex, lib/mix/tasks/mailglass.stability.check.ex, test/mailglass/stability_contract_test.exs, mailglass_admin/test/mailglass_admin/stability_contract_test.exs</files>
  <action>Add explicit documentation metadata for the full stable public inventory defined by Phase 35 and back it with compiled-doc verification per LOCK-04. This includes stable public Mix tasks, root entrypoint modules, stable functions, callbacks, types, and structs promised by the Phase 35 contract docs across both `mailglass` and `mailglass_admin`. Use `@moduledoc since:` where the module itself is the stable entrypoint and add `@doc since:`, `@typedoc since:`, or `@deprecated` where ExDoc needs the metadata on stable helpers, public callbacks/types, or intentionally supported escape hatches. Create `test/mailglass/stability_contract_test.exs` and `mailglass_admin/test/mailglass_admin/stability_contract_test.exs` as Wave 0 compiled-doc truth tests using `Code.fetch_docs/1`. The core test must assert exhaustive `:since` / deprecation coverage for every stable `Mailglass` API item named in the contract inventory; the admin test must do the same for `MailglassAdmin.Router`, `MailglassAdmin.Auth`, and every other stable admin seam documented in Phase 35. Keep changes documentation-oriented: no new runtime checks, no new CI gates, and no new surface classification logic beyond what the Phase 35 inventory already defines.</action>
  <acceptance_criteria>
    - Every stable public API item named in the Phase 35 contract inventory carries visible `since` metadata or explicit deprecation metadata in source.
    - Any still-supported deprecated stability escape hatch in the stable inventory is labeled as deprecated rather than left implicit.
    - `test/mailglass/stability_contract_test.exs` and `mailglass_admin/test/mailglass_admin/stability_contract_test.exs` exist and verify the full stable inventory via `Code.fetch_docs/1` rather than source grep.
    - Public callbacks/types in stable seams, including `MailglassAdmin.Auth`, are covered by the compiled-doc audit rather than left to representative sampling.
    - Runtime behavior remains unchanged aside from documentation metadata and wording.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@moduledoc since:|@doc since:|@deprecated" lib/mix/tasks/mailglass.install.ex lib/mix/tasks/mailglass.reconcile.ex lib/mix/tasks/mail.doctor.ex lib/mix/tasks/mailglass.publish.check.ex lib/mix/tasks/mailglass.docs.check.ex lib/mix/tasks/mailglass.stability.check.ex && mix test test/mailglass/stability_contract_test.exs --warnings-as-errors && cd mailglass_admin && mix test test/mailglass_admin/stability_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The full stable public inventory advertises provenance and deprecation status directly in source so ExDoc can surface that metadata exhaustively.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh light docs-contract verification for the stability inventory</name>
  <read_first>
    - lib/mix/tasks/mailglass.docs.check.ex
    - lib/mix/tasks/mailglass.stability.check.ex
    - test/mailglass/docs_contract_test.exs
    - docs/api_stability.md
    - README.md
    - mailglass_admin/README.md
    - .planning/phases/34-verification-regression-closure/34-VERIFICATION.md
  </read_first>
  <files>lib/mix/tasks/mailglass.docs.check.ex, lib/mix/tasks/mailglass.stability.check.ex, test/mailglass/docs_contract_test.exs, test/mailglass/stability_contract_test.exs, mailglass_admin/test/mailglass_admin/stability_contract_test.exs</files>
  <action>Retarget the existing light docs-contract surfaces to the new Phase 35 contract story per LOCK-01 through LOCK-04. Update `mailglass.docs.check` so its required/forbidden tokens stop encoding the stale v0.3 package story and instead require references to the canonical stability inventory plus the narrow admin boundary. Update `test/mailglass/docs_contract_test.exs` to assert the new contract entrypoints and wording. If `mailglass.stability.check` wording mentions outdated leak semantics or implicit exemptions, align the docs-facing messaging only; do not turn it into a full manifest/enforcement system yet. Keep `Code.fetch_docs/1`-backed truth in the new stability-contract tests and use `mix docs` output only as a final visibility check that the full stable inventory surfaces `Since` / deprecation text in generated docs.</action>
  <acceptance_criteria>
    - `mailglass.docs.check` no longer hard-codes the old `v0.3` contract story as the required Tier 1 surface.
    - `test/mailglass/docs_contract_test.exs` asserts the canonical stability inventory and the narrow admin boundary wording.
    - `test/mailglass/stability_contract_test.exs` and `mailglass_admin/test/mailglass_admin/stability_contract_test.exs` provide compiled-doc truth for the full stable inventory, not representative samples.
    - The plan’s automated verification proves generated docs expose `Since` or deprecation text for the full stable inventory and that `mailglass_admin` generated docs surface the package-local contract page.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors && cd mailglass_admin && mix test test/mailglass_admin/stability_contract_test.exs --warnings-as-errors && mix docs --warnings-as-errors >/tmp/mailglass-admin-phase35-docs.log && rg -n "api_stability|Stability|Contract" doc/**/*.html doc/dist/search_data-*.js && cd .. && mix mailglass.docs.check && mix docs >/tmp/mailglass-phase35-docs.log && rg -n "Since|Deprecated" doc/**/*.html</automated>
  </verify>
  <done>The Phase 35 contract is guarded by existing lightweight docs checks, and generated docs visibly expose provenance/deprecation metadata for the documented stable surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source docs -> generated docs | Metadata must survive into ExDoc output where adopters actually read it. |
| light docs checks -> release confidence | The checks should detect stale contract wording without claiming full Phase 37 enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | T | lib/mix/tasks/mailglass.docs.check.ex | mitigate | Retarget required/forbidden tokens to the Phase 35 contract so stale v0.3 language cannot silently return. |
| T-35-08 | R | test/mailglass/docs_contract_test.exs | mitigate | Assert canonical contract references in automated docs tests so maintainers have repo evidence for what was promised. |
| T-35-09 | I | generated ExDoc output | mitigate | Verify `mix docs` output shows `Since` or deprecation markers on representative stable surfaces instead of assuming metadata is visible. |
</threat_model>

<verification>
Run the full task-level command: docs contract test, docs drift check, then `mix docs` plus a grep for `Since` / `Deprecated` in generated HTML.
</verification>

<success_criteria>
Stable public APIs and task entrypoints visibly carry provenance/deprecation metadata, and the existing lightweight docs checks now guard the Phase 35 inventory rather than stale v0.3 messaging.
</success_criteria>

<output>
After completion, create `.planning/phases/35-stability-contract-audit/35-03-SUMMARY.md`
</output>
