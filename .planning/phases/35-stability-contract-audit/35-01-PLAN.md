---
phase: 35-stability-contract-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api_stability.md
  - README.md
  - lib/mailglass.ex
autonomous: true
requirements:
  - LOCK-01
  - LOCK-03
must_haves:
  truths:
    - "Adopters can open one canonical document and see the exact stable `mailglass` v1.x contract."
    - "Core root exports are classified so exported implementation helpers do not silently become promised API."
    - "The top-level adopter entrypoint points to the same contract inventory instead of stale v0.3 surface language."
  artifacts:
    - path: "docs/api_stability.md"
      provides: "Canonical v1.x core stability inventory and classification map"
      min_lines: 200
    - path: "README.md"
      provides: "Adopter-facing pointer to the canonical contract"
      min_lines: 150
    - path: "lib/mailglass.ex"
      provides: "Point-of-use root boundary framing for stable contract"
      contains: "defmodule Mailglass do"
  key_links:
    - from: "README.md"
      to: "docs/api_stability.md"
      via: "stability-contract reference"
      pattern: "api_stability"
    - from: "docs/api_stability.md"
      to: "lib/mailglass.ex"
      via: "root export classification"
      pattern: "Mailglass\\."
---

<objective>
Publish the core half of the v1.x stability contract so adopters can see exactly which `mailglass` modules, behaviours, mix tasks, telemetry families, structs, and documented fields are stable.

Purpose: Satisfy LOCK-01 and the core half of LOCK-03 without widening the promised surface.
Output: A canonical core contract inventory plus adopter-facing entry docs that point at it.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/35-stability-contract-audit/35-RESEARCH.md
@docs/api_stability.md
@README.md
@lib/mailglass.ex

<interfaces>
From lib/mailglass.ex:
```elixir
use Boundary,
  deps: [],
  exports: [
    Message,
    Telemetry,
    Config,
    Tenancy,
    Events,
    Webhook,
    Outbound,
    Operator.Deliveries,
    Operator.ReplayHistory,
    Operator.ReplayTargets,
    Operator.Timeline,
    Operator.Suppressions,
    Adapter,
    Adapters.Fake,
    Adapters.Swoosh,
    Mailable,
    Renderer
  ] ++ @oban_exports

@doc since: "0.1.0"
defdelegate deliver(msg, opts \\ []), to: Mailglass.Outbound
```

From docs/api_stability.md:
```markdown
# API Stability — mailglass
## Error Hierarchy
## Event Type Contract
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the canonical core stability inventory</name>
  <read_first>
    - docs/api_stability.md
    - lib/mailglass.ex
    - .planning/phases/35-stability-contract-audit/35-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>docs/api_stability.md</files>
  <action>Replace the v0.2 freeze framing with a v1.x stability-contract inventory per LOCK-01 and LOCK-03. Keep the existing closed-set sections that still define stable atoms and fields, but add a front section that explicitly classifies the reachable `Mailglass` surface into `stable`, `internal`, and `sibling-package-only`. The stable inventory must enumerate the root adopter API, stable behaviours, promised mix tasks, telemetry family names, stable structs/errors, and any documented field-level promises. Call out exported-but-not-promised helpers and operator/read-model modules explicitly so exported reachability does not imply contract status. Do not add Phase 37-style mechanical enforcement or broaden the contract to `mailglass_inbound`.</action>
  <acceptance_criteria>
    - `docs/api_stability.md` contains a v1.x-oriented contract heading and no longer claims a `v0.2 API Freeze Policy`.
    - The file contains explicit `stable`, `internal`, and `sibling-package-only` classifications for core surfaces.
    - The file names stable core categories covering modules/behaviours, mix tasks, telemetry families, and structs/documented fields.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "v1\\.x|stable|internal|sibling-package-only|telemetry|mix mailglass\\.|Mailglass\\." docs/api_stability.md</automated>
  </verify>
  <done>The canonical contract document gives adopters one exact place to identify the core `mailglass` v1.x promise and what is intentionally excluded from it.</done>
</task>

<task type="auto">
  <name>Task 2: Align core adopter entry docs to the canonical contract</name>
  <read_first>
    - README.md
    - lib/mailglass.ex
    - docs/api_stability.md
    - .planning/phases/35-stability-contract-audit/35-RESEARCH.md
  </read_first>
  <files>README.md, lib/mailglass.ex</files>
  <action>Update the adopter-facing README and the `Mailglass` moduledoc so they point readers to the canonical v1.x contract per LOCK-01 and LOCK-03. Remove stale surface language that still describes the packages as `v0.3` contracts, and replace any implication that all root exports are promised API with wording that the root boundary is classified in `docs/api_stability.md`. Keep the README focused on contract discovery and supported package posture; do not pull upgrade-policy or hard-enforcement content from Phases 36 or 37 into this plan.</action>
  <acceptance_criteria>
    - `README.md` references the canonical stability inventory and no longer presents the core package as a `v0.3` public surface.
    - `lib/mailglass.ex` moduledoc points to the canonical contract inventory for stable-vs-internal interpretation.
    - Both files preserve the existing product boundary and do not promise `mailglass_inbound` as part of the v1.x contract.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "api_stability|v1\\.x|v0\\.3 public surface|mailglass_inbound" README.md lib/mailglass.ex</automated>
  </verify>
  <done>Core adopter-facing entry docs and the root module doc agree on where the v1.x contract lives and stop advertising stale or ambiguous surface promises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopters | Contract docs shape what adopters depend on in production. |
| root exports -> public promise | Reachable modules can be mistaken for stable API unless classification is explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | T | docs/api_stability.md | mitigate | Classify each reachable core surface as `stable`, `internal`, or `sibling-package-only` so exported implementation helpers are not silently promoted to API. |
| T-35-02 | I | README.md | mitigate | Remove stale version/surface wording that could disclose an inaccurately broad or outdated support promise. |
| T-35-03 | R | lib/mailglass.ex | mitigate | Point the root moduledoc to the canonical contract so maintainers and adopters share one source of truth during disputes. |
</threat_model>

<verification>
Run the task-level grep command and confirm `docs/api_stability.md` is the single cited contract inventory from both `README.md` and `lib/mailglass.ex`.
</verification>

<success_criteria>
Adopters can find one canonical core contract inventory, and the top-level package docs no longer overclaim or rely on stale v0.3 surface language.
</success_criteria>

<output>
After completion, create `.planning/phases/35-stability-contract-audit/35-01-SUMMARY.md`
</output>
