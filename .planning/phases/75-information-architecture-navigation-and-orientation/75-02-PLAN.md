---
phase: 75-information-architecture-navigation-and-orientation
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/preview_live.ex
  - mailglass_admin/test/mailglass_admin/operator/shell_test.exs
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
  - mailglass_admin/test/mailglass_admin/preview_live_test.exs
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - IA-01
  - IA-03
must_haves:
  truths:
    - "Shell.orientation_strip/1 is a public def (not defp) in shell.ex, placed after defp flash_region/1"
    - "The component takes attr :surface, :atom, values: [:deliveries, :inbound, :preview], required: true"
    - "All three surfaces render the orientation strip with the correct per-surface frozen copy and testid"
    - "Deliveries orientation strip: testid deliveries-orientation, heading Deliveries, first bullet Email never arrived? Start here."
    - "Inbound orientation strip: testid inbound-orientation, heading Inbound, first bullet Message didn't route as expected? Inspect the routing trace."
    - "Preview orientation strip: testid preview-orientation, heading Preview, first bullet No mailables found? Define a mailable module in your app."
    - "The preview-empty-mailables testid is preserved (orientation strip supplements, not replaces)"
    - "The ul bullet list uses class text-label (not raw text-sm) — born token-clean"
    - "No motion classes (motion-reveal, phx-mounted) appear on the orientation strip"
    - "Bundle is rebuilt and priv/static/app.css is committed in the same change"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      provides: "Shell.orientation_strip/1 public function component"
      contains: "def orientation_strip"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Updated orientation call site, private defp removed"
      contains: "Shell.orientation_strip surface={:deliveries}"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Inbound orientation strip call"
      contains: "Shell.orientation_strip surface={:inbound}"
    - path: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      provides: "Preview orientation strip call alongside preserved empty state"
      contains: "Shell.orientation_strip surface={:preview}"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt bundle containing new token classes"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      via: "Shell.orientation_strip surface={:deliveries}"
      pattern: "Shell.orientation_strip"
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      via: "Shell.orientation_strip surface={:inbound}"
      pattern: "Shell.orientation_strip"
    - from: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      via: "Shell.orientation_strip surface={:preview}"
      pattern: "Shell.orientation_strip"
---

<objective>
Extract the private `defp orientation_strip(assigns)` from `operator_live.ex` into a public `Shell.orientation_strip/1` function component in `shell.ex`, wire all three surfaces (Deliveries, Inbound, Preview), update and un-skip the Wave 0 shell_test.exs stubs, and rebuild the admin bundle.

Purpose: Satisfies IA-01 — all three operator surfaces emit the shared orientation strip with per-surface frozen symptom-first copy (a COPY CHANGE, not a pure extraction: replaces "Start from the customer symptom" with the frozen terse table), appropriate testids, and token-clean markup. Addresses GAP-07, GAP-09, GAP-11 (390px orientation readability across all three surfaces).

Output: Public `Shell.orientation_strip/1` component; updated call sites in all three LiveViews; test coverage in shell_test.exs, operator_live_test.exs, inbound_live_test.exs, preview_live_test.exs; rebuilt priv/static/app.css.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-01-SUMMARY.md

<interfaces>
<!-- Key contracts. Extracted from codebase. -->

From mailglass_admin/lib/mailglass_admin/operator/shell.ex:
```elixir
# Line 102 — attr discriminator precedent:
attr :active, :atom, values: [:deliveries, :inbound], required: true

# Lines 278-302 — flash_region/1 placement anchor (new component goes AFTER this end):
attr :flash, :map, default: %{}
defp flash_region(assigns) do
  ~H"""..."""
end
# NEW orientation_strip/1 goes here (after line 302)
```

From mailglass_admin/lib/mailglass_admin/operator_live.ex:
```elixir
# Lines 254-256 — existing trigger and call site to update:
<div :if={is_nil(@selected_delivery)} class="mb-lg">
  <.orientation_strip />
</div>
# Change to: <Shell.orientation_strip surface={:deliveries} />
# (keep the :if condition and wrapping div)

# Lines 362-392 — private defp to REMOVE after extraction:
defp orientation_strip(assigns) do
  ~H"""
  <div class="rounded-box border border-base-300 bg-base-200 p-md" data-testid="operator-orientation">
    <div class="flex items-start gap-sm">
      <Components.icon name="hero-lifebuoy" class="mt-0.5 h-5 w-5 shrink-0 text-primary" />
      <div class="min-w-0">
        <h2 class="text-body font-bold text-base-content">Start from the customer symptom</h2>
        <ul class="mt-2 grid gap-1 text-sm text-secondary">
          <li>...</li>
        </ul>
      </div>
    </div>
  </div>
  """
end
```

From mailglass_admin/lib/mailglass_admin/inbound_live.ex:
```elixir
# Lines 330-338 — empty-detail branch to augment:
<% is_nil(@detail) -> %>
  <div data-testid="inbound-empty-detail" class="card rounded-box border border-base-300 bg-base-200 p-6">
    <h2 class="text-base font-bold text-base-content">
      Select an inbound record to inspect its routing, execution timeline, and raw source.
    </h2>
  </div>
# ADD Shell.orientation_strip surface={:inbound} BEFORE this div
```

From mailglass_admin/lib/mailglass_admin/preview_live.ex:
```elixir
# Lines 291-323 — zero-mailables branch to supplement (PRESERVE VERBATIM per D-06):
<% @mailables == [] -> %>
  <div data-testid="preview-empty-mailables" class="card mx-auto max-w-prose ...">
    <h2 class="mb-2 text-heading font-bold text-base-content">No mailables discovered</h2>
    ...
  </div>
# ADD Shell.orientation_strip surface={:preview} BEFORE this div (orientation strip above, preserved card below)
# Do NOT change the heading "No mailables discovered" (D-06 / Pitfall 5)
```

Frozen per-surface copy (verbatim — do NOT paraphrase):
- :deliveries — heading: "Deliveries"; bullets: "Email never arrived? Start here." / "Replay changed nothing? View the event timeline." / "Address keeps getting blocked? Check suppressions."
- :inbound — heading: "Inbound"; bullets: "Message didn't route as expected? Inspect the routing trace." / "No mailbox matched? Check the no-match record." / "Failed ingest? Review the provider signature log."
- :preview — heading: "Preview"; bullets: "No mailables found? Define a mailable module in your app." / "Mailable not showing? Ensure it's compiled." / "Preview not rendering? Check your template syntax."

Markup contract (token-clean):
- Container: rounded-box border border-base-300 bg-base-200 p-md, data-testid={"#{surface}-orientation"}
- Icon: hero-lifebuoy, mt-0.5 h-5 w-5 shrink-0 text-primary
- Heading: h2 class="text-body font-bold text-base-content"
- Tip list: ul class="mt-2 grid gap-1 text-label text-secondary" (text-label NOT text-sm)
- No motion-reveal, no phx-mounted
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract Shell.orientation_strip/1 public component and wire all three surfaces</name>
  <files>
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/lib/mailglass_admin/operator_live.ex
    mailglass_admin/lib/mailglass_admin/inbound_live.ex
    mailglass_admin/lib/mailglass_admin/preview_live.ex
    mailglass_admin/test/mailglass_admin/operator/shell_test.exs
    mailglass_admin/test/mailglass_admin/operator_live_test.exs
    mailglass_admin/test/mailglass_admin/inbound_live_test.exs
    mailglass_admin/test/mailglass_admin/preview_live_test.exs
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (full file — identify exact line after flash_region/1 ends, module close)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex lines 245-265 and 355-400 (orientation trigger, call site, private defp)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex lines 325-345 (is_nil(@detail) branch)
    - mailglass_admin/lib/mailglass_admin/preview_live.ex lines 285-330 (zero-mailables branch, preview-empty-mailables)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md §1 (orientation strip — full HEEx structure, copy table, placement)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-UI-SPEC.md § Orientation Strip Contract (frozen copy table, markup contract)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md D-01..D-06
    - mailglass_admin/test/mailglass_admin/operator/shell_test.exs (Wave 0 stubs from Plan 75-01 to un-skip)
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs lines 25-50 (existing no-selection test to understand assertion pattern)
    - mailglass_admin/test/mailglass_admin/preview_live_test.exs (full — understand existing structure for orientation addition)
  </read_first>
  <behavior>
    - Test 1 (shell_test.exs): Shell.orientation_strip/1 with surface={:deliveries} renders element with data-testid="deliveries-orientation"
    - Test 2 (shell_test.exs): Shell.orientation_strip/1 with surface={:inbound} renders element with data-testid="inbound-orientation"
    - Test 3 (shell_test.exs): Shell.orientation_strip/1 with surface={:preview} renders element with data-testid="preview-orientation"
    - Test 4 (shell_test.exs): The rendered deliveries orientation strip contains the text "Email never arrived? Start here."
    - Test 5 (shell_test.exs): The rendered markup for orientation_strip contains "text-label" class (not "text-sm") on the ul element
    - Test 6 (operator_live_test.exs): When no delivery is selected, the Deliveries surface renders deliveries-orientation testid
    - Test 7 (inbound_live_test.exs): When no detail is selected, the Inbound surface renders inbound-orientation testid
    - Test 8 (preview_live_test.exs): When mailables is empty, the Preview surface renders preview-orientation testid AND preview-empty-mailables testid (both present)
  </behavior>
  <action>
    Anti-churn gate citations: GAP-07 (Deliveries 390px orientation readability — lack of visible strip is the gap), GAP-09 (Inbound 390px orientation readability), GAP-11 (Preview 390px orientation readability). This task directly closes those three gap rows by implementing the shared component on all three surfaces.

    Step 1 — UN-SKIP tests in shell_test.exs (written in Plan 75-01 as @tag :skip stubs). Remove the @tag :skip annotations and write the test implementations using Phoenix.LiveViewTest render_component/2 for function components. For copy verification, assert `html =~ "Email never arrived? Start here."` (and inbound/preview equivalents). For class verification, assert `html =~ "text-label"` and `refute html =~ ~r/class="[^"]*text-sm/` on the new component output.

    Step 2 — ADD Shell.orientation_strip/1 to shell.ex. Placement: AFTER `defp flash_region(assigns)` function body (after its closing `end`), before the module's own closing `end`. Change `defp` to `def` for the public component. Add `attr :surface, :atom, values: [:deliveries, :inbound, :preview], required: true` before the function. Inside the function: compute `{heading, tips} = copy_for(@surface)` (or use a case statement directly in the HEEx — internal form is discretion). Replace `data-testid="operator-orientation"` with a dynamic testid computed from surface. Replace heading "Start from the customer symptom" with surface-specific frozen heading from the copy table. Replace bullets with surface-specific frozen bullets from the copy table. Change `text-sm` on the `<ul>` to `text-label`. Do NOT add motion-reveal or phx-mounted.

    Step 3 — UPDATE operator_live.ex. At lines 254-256: change `<.orientation_strip />` to `<Shell.orientation_strip surface={:deliveries} />`. Keep the surrounding `:if={is_nil(@selected_delivery)}` condition and `div class="mb-lg"` wrapper unchanged. DELETE the entire `defp orientation_strip(assigns)` function at lines 362-392 (confirmed source for the old private component).

    Step 4 — UPDATE inbound_live.ex. Inside the `<% is_nil(@detail) -> %>` branch: add `<Shell.orientation_strip surface={:inbound} />` BEFORE the existing `<div data-testid="inbound-empty-detail" ...>` div. Preserve the existing inbound-empty-detail div as-is.

    Step 5 — UPDATE preview_live.ex. Inside the `<% @mailables == [] -> %>` branch: add `<Shell.orientation_strip surface={:preview} />` BEFORE the existing `<div data-testid="preview-empty-mailables" ...>` block. DO NOT change the existing `"No mailables discovered"` heading (Pitfall 5 / D-06). The orientation strip and the existing empty-state card coexist in the branch.

    Step 6 — UPDATE tests. In operator_live_test.exs: the existing test "renders the default detail prompt when no delivery is selected" (line 16) — add an assertion that `deliveries-orientation` testid is present in the rendered HTML. In inbound_live_test.exs: the existing test "renders the no-selection prompt" (line 26) — add assertion for `inbound-orientation` testid. In preview_live_test.exs: add a new test or extend existing that asserts: `preview-orientation` testid is present AND `preview-empty-mailables` testid is also present (both must be in the rendered output at the same time).

    IMPORTANT — check Shell module alias usage: in operator_live.ex, inbound_live.ex, and preview_live.ex, confirm how `Shell` is referenced. If it is aliased as `alias MailglassAdmin.Operator.Shell` or used as a module attribute, use the same form. If it is referenced as `MailglassAdmin.Operator.Shell` directly, use the full module name. Read the existing module header before writing the call site.

    Do NOT rebuild the bundle in this task — that is Task 2.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix test mailglass_admin/test/mailglass_admin/operator/shell_test.exs mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs --seed 0</automated>
  </verify>
  <done>
    - grep -c "def orientation_strip" mailglass_admin/lib/mailglass_admin/operator/shell.ex returns 1 (public def)
    - grep -c "defp orientation_strip" mailglass_admin/lib/mailglass_admin/operator_live.ex returns 0 (private removed)
    - grep -c "Shell.orientation_strip surface={:deliveries}" mailglass_admin/lib/mailglass_admin/operator_live.ex returns 1
    - grep -c "Shell.orientation_strip surface={:inbound}" mailglass_admin/lib/mailglass_admin/inbound_live.ex returns 1
    - grep -c "Shell.orientation_strip surface={:preview}" mailglass_admin/lib/mailglass_admin/preview_live.ex returns 1
    - grep -c "preview-empty-mailables" mailglass_admin/lib/mailglass_admin/preview_live.ex returns at least 1 (preserved)
    - grep "Email never arrived" mailglass_admin/lib/mailglass_admin/operator/shell.ex returns the frozen copy verbatim
    - grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/shell.ex | grep -c "text-sm" returns 0 on the new orientation_strip function body (token-clean)
    - All specified tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild admin bundle and commit priv/static/</name>
  <files>
    mailglass_admin/priv/static/app.css
  </files>
  <read_first>
    - mailglass_admin/assets/css/app.css (confirm text-label and gap-sm/md/lg tokens are defined in @theme, so no new tokens added)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-RESEARCH.md §M-5 (bundle rebuild gate, new classes used)
    - CLAUDE.md § Things Not To Do #6 (bundle commit gate)
  </read_first>
  <action>
    Anti-churn gate citations: GAP-07, GAP-09, GAP-11 — the orientation strip's token-clean classes (`text-label`, `gap-sm`, `p-md`) must be included in the built bundle for the 390px acceptance check to pass with the correct rendering.

    From the mailglass_admin/ directory, run: `mix mailglass_admin.assets.build`

    After the build completes, run `git diff --exit-code mailglass_admin/priv/static/` to confirm the bundle has changed (it should, since new HEEx classes are now in the JIT scan). If `git diff --exit-code` exits 0 (no changes) it means the classes were already in the bundle — that is also acceptable and the gate passes.

    Stage and commit the `priv/static/app.css` (and any other priv/static/ file that changed) in a commit that says something like `build(admin): rebuild bundle for orientation_strip token-clean classes`. The bundle must be committed in the same PR as the HEEx changes (CLAUDE.md rule #6).

    Note: The `text-label` token is already confirmed present in STACK.md as part of the existing `@theme` block. The `hero-lifebuoy` icon is already vendored. All new classes used by orientation_strip/1 should already be in JIT scope since `lib/` is a `@source` in app.css. The rebuild may produce minimal diff or no diff if all classes were already emitted by other usages.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix mailglass_admin.assets.build 2>&1 | tail -5 && cd /Users/jon/projects/mailglass && git diff --exit-code mailglass_admin/priv/static/ || echo "BUNDLE_CHANGED_OK"</automated>
  </verify>
  <done>
    - mix mailglass_admin.assets.build exits 0
    - git diff --exit-code mailglass_admin/priv/static/ exits 0 (bundle committed) or BUNDLE_CHANGED_OK only if bundle is staged and committed in the same operation
    - The priv/static/app.css contains the compiled output (no git-dirty state on priv/static/ when the plan ends)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Shell.orientation_strip/1 | surface atom value is constrained by attr :surface, :atom, values: [...] — Phoenix validates at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-02-01 | Information Disclosure | orientation_strip/1 rendered copy | accept | Copy is static frozen strings — no dynamic data, no user input, no PII |
| T-75-02-02 | Tampering | attr :surface value | mitigate | Phoenix LiveView `attr` with `values:` list raises a compile-time warning if an undeclared value is passed; runtime invalid values use the default branch |
| T-75-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; bundle rebuild uses the already-installed Tailwind standalone binary |
</threat_model>

<verification>
After Wave 2 completes:
- mix test mailglass_admin/test/mailglass_admin/ --seed 0 exits 0 (all admin tests pass)
- grep -c "def orientation_strip" mailglass_admin/lib/mailglass_admin/operator/shell.ex returns 1
- grep -c "defp orientation_strip" mailglass_admin/lib/mailglass_admin/operator_live.ex returns 0
- grep "Email never arrived" mailglass_admin/lib/mailglass_admin/operator/shell.ex returns a match (verbatim frozen copy present)
- grep -v "^#" mailglass_admin/lib/mailglass_admin/operator/shell.ex | grep -c "text-sm" returns 0 on the orientation_strip function
- grep -c "preview-empty-mailables" mailglass_admin/lib/mailglass_admin/preview_live.ex returns at least 1
- git diff --exit-code mailglass_admin/priv/static/ exits 0 (bundle committed)
</verification>

<success_criteria>
- Shell.orientation_strip/1 is a public function component in shell.ex with attr :surface, :atom discriminator and frozen per-surface copy baked in
- All three surfaces (Deliveries, Inbound, Preview) call Shell.orientation_strip with the correct surface atom
- Preview empty state: orientation strip renders ALONGSIDE (above) the preserved preview-empty-mailables card — both testids present simultaneously
- The ul tip list uses text-label, not text-sm — the new shared component is born token-clean
- No motion-reveal or phx-mounted on the orientation strip
- All tests in shell_test.exs, operator_live_test.exs, inbound_live_test.exs, preview_live_test.exs pass
- admin bundle rebuilt and committed — git diff --exit-code mailglass_admin/priv/static/ exits 0
</success_criteria>

<output>
Create `.planning/phases/75-information-architecture-navigation-and-orientation/75-02-SUMMARY.md` when done.
</output>
