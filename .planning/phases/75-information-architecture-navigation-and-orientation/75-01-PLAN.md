---
phase: 75-information-architecture-navigation-and-orientation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/operator/suppressions.ex
  - test/mailglass/operator/suppressions_test.exs
  - mailglass_admin/test/mailglass_admin/operator/shell_test.exs
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
autonomous: true
requirements:
  - IA-02
must_haves:
  truths:
    - "count_active_suppressions/1 exists in core mailglass at lib/mailglass/operator/suppressions.ex and returns a non-negative integer for a given tenant_id"
    - "count_active_suppressions/1 only counts active entries (is_nil(expires_at) or expires_at > now) scoped to the given tenant"
    - "A test file exists for the new function with cases covering count correctness and active-only filtering"
    - "A shell_test.exs stub exists in mailglass_admin with an orientation_strip/1 describe block (Wave 0 gap closure)"
    - "operator_live_test.exs has describe blocks for Overview branch cases (no-tenant nudge, health counts, suppression degradation) (Wave 0 gap closure)"
  artifacts:
    - path: "lib/mailglass/operator/suppressions.ex"
      provides: "count_active_suppressions/1 additive function"
      contains: "def count_active_suppressions"
    - path: "test/mailglass/operator/suppressions_test.exs"
      provides: "test coverage for count_active_suppressions/1"
      contains: "count_active_suppressions"
    - path: "mailglass_admin/test/mailglass_admin/operator/shell_test.exs"
      provides: "Wave 0 stub — orientation_strip/1 test block"
      contains: "orientation_strip"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "Wave 0 stubs — Overview branch describe blocks"
      contains: "overview"
  key_links:
    - from: "lib/mailglass/operator/suppressions.ex"
      to: "mailglass/operator/suppressions_test.exs"
      via: "count_active_suppressions/1 public API"
      pattern: "count_active_suppressions"
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "lib/mailglass/operator/suppressions.ex"
      via: "runtime-module-indirection seam (Plan 75-03)"
      pattern: "suppression_count_module"
---

<objective>
Add the single cross-package read-model function `count_active_suppressions/1` to core `mailglass`, and scaffold the Wave 0 test stubs required before the later implementation plans can be verified.

Purpose: The Operator Overview (Plan 75-03) reads the active suppression count through a runtime-module-indirection seam. The function must exist in core before the Overview branch is wired. Wave 0 stubs establish test structure so Plans 75-02 and 75-03 can write passing tests against a known scaffold.

Output: One new additive function in `lib/mailglass/operator/suppressions.ex`; two new/extended test files in core and admin with describe-block stubs.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From lib/mailglass/operator/suppressions.ex (existing function to mirror):
```elixir
# Module-level aliases already present (lines 6-11) — no new imports needed
import Ecto.Query
alias Mailglass.Clock
alias Mailglass.Suppression.Entry
alias Mailglass.{Repo, Tenancy}

# Active-entry filter pattern at lines 26-28 (to mirror):
Entry
|> where([entry], entry.tenant_id == ^tenant_id)
|> where([entry], is_nil(entry.expires_at) or entry.expires_at > ^now)
|> Tenancy.scope(tenant_id)
# (existing also calls where_matches/order_by/limit/Repo.one — count variant omits those)

# Guard clause at line 58:
when is_binary(tenant_id) and tenant_id != ""

# Clock usage at line 24:
now = Clock.utc_now()
```

New function spec contract:
```elixir
@spec count_active_suppressions(String.t()) :: non_neg_integer()
def count_active_suppressions(tenant_id) when is_binary(tenant_id) and tenant_id != ""
```

From test/mailglass/operator/suppressions_test.exs (existing describe block, extend):
```elixir
defmodule Mailglass.Operator.SuppressionsTest do
  # existing describe "get_delivery_suppression_state/2" do ... end
  # ADD: describe "count_active_suppressions/1" do ... end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add count_active_suppressions/1 to core suppressions.ex</name>
  <files>lib/mailglass/operator/suppressions.ex, test/mailglass/operator/suppressions_test.exs</files>
  <read_first>
    - lib/mailglass/operator/suppressions.ex (full file — extract alias list, Clock usage at line 24, active-entry filter at lines 26-28, Tenancy.scope at line 50, guard at line 58)
    - test/mailglass/operator/suppressions_test.exs (full file — understand fixture/alias setup to mirror for new describe block)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-PATTERNS.md § Pattern 3 (count_active_suppressions/1 pattern)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-CONTEXT.md D-11, D-12, D-13
  </read_first>
  <behavior>
    - Test 1: count_active_suppressions/1 returns 0 for a tenant with no suppression entries
    - Test 2: count_active_suppressions/1 returns the correct integer count for a tenant with N active entries (is_nil(expires_at) entries counted)
    - Test 3: count_active_suppressions/1 excludes expired entries (expires_at in the past)
    - Test 4: count_active_suppressions/1 excludes entries belonging to a different tenant_id
    - Test 5: count_active_suppressions/1 counts entries with non-nil expires_at in the FUTURE as active
  </behavior>
  <action>
    Anti-churn gate citations: GAP-07, GAP-09, GAP-11 (orientation readability — suppression count is one of the three health counts the Overview surface requires to be readable at 390px; per D-18, 390px acceptance includes legible health counts). Also GAP-21 (a11y Overview subset — the suppression health card must render the count or the neutral degradation, not crash; this is the data foundation for that card).

    In test/mailglass/operator/suppressions_test.exs: add `describe "count_active_suppressions/1"` block with 5 test cases using the same fixture/factory pattern already in the file. Write the tests FIRST so they fail (RED). Tests must confirm: (1) 0 for empty tenant, (2) correct count for N active entries where `expires_at IS NULL`, (3) excludes entries where `expires_at < now`, (4) excludes entries with a different `tenant_id`, (5) counts entries where `expires_at > now` (future).

    In lib/mailglass/operator/suppressions.ex: add `count_active_suppressions/1` AFTER the existing `get_delivery_suppression_state/2` function body. Spec: `@spec count_active_suppressions(String.t()) :: non_neg_integer()`. Guard: `when is_binary(tenant_id) and tenant_id != ""`. Body: `now = Clock.utc_now()` then `Entry |> where([entry], entry.tenant_id == ^tenant_id) |> where([entry], is_nil(entry.expires_at) or entry.expires_at > ^now) |> Tenancy.scope(tenant_id) |> Repo.aggregate(:count, :id)`. No `where_matches`, no `order_by`, no `limit`, no `Repo.one` — those are delivery-match concerns. This function counts ALL active entries for the tenant. All required aliases (`Clock`, `Entry`, `Repo`, `Tenancy`) are already present. Do NOT add new imports or aliases.

    After GREEN: run `mix test test/mailglass/operator/suppressions_test.exs --seed 0` from the mailglass root. All tests must pass including the existing `get_delivery_suppression_state/2` tests.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix test test/mailglass/operator/suppressions_test.exs --seed 0</automated>
  </verify>
  <done>
    - lib/mailglass/operator/suppressions.ex contains `def count_active_suppressions` with `@spec count_active_suppressions(String.t()) :: non_neg_integer()`
    - test/mailglass/operator/suppressions_test.exs has a `describe "count_active_suppressions/1"` block with at least 5 tests
    - All tests in suppressions_test.exs pass (including pre-existing tests for get_delivery_suppression_state/2)
    - grep -c "def count_active_suppressions" lib/mailglass/operator/suppressions.ex returns 1
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for shell_test.exs and operator_live_test.exs Overview blocks</name>
  <files>
    mailglass_admin/test/mailglass_admin/operator/shell_test.exs
    mailglass_admin/test/mailglass_admin/operator_live_test.exs
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (full file — understand module/alias/describe setup to extend consistently)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-VALIDATION.md § Wave 0 Requirements (exact test cases needed)
    - .planning/phases/75-information-architecture-navigation-and-orientation/75-RESEARCH.md § Validation Architecture (per-task test map rows for overview branch, overview no-tenant, overview health, suppression degradation, aria-current)
  </read_first>
  <action>
    Anti-churn gate citations: GAP-21 (a11y Overview subset — the aria-current test belongs in shell_test.exs; this stub makes that test slot explicit before implementation), GAP-07/09/11 (390px orientation readability — overview no-tenant and health-count test cases are required verification gates for the orientation strip rendering at all health states before the 390px screenshot review).

    CREATE mailglass_admin/test/mailglass_admin/operator/shell_test.exs as a new file. This directory may not yet exist — create it. Module: `MailglassAdmin.Operator.ShellTest`. Use `use MailglassAdmin.ConnCase` (or the appropriate test case base used in operator_live_test.exs). Add two describe blocks as stubs:

    (a) `describe "orientation_strip/1"` with pending `@tag :skip` test stubs for: "renders deliveries-orientation testid with frozen copy", "renders inbound-orientation testid with frozen copy", "renders preview-orientation testid with frozen copy", "uses text-label not text-sm for bullet list". These run red/skip until Plan 75-02 implements the component.

    (b) `describe "aria-current nav resolution"` with a pending `@tag :skip` stub: "passes active={:deliveries} so nav_link emits aria-current=page on Overview". This runs red/skip until Plan 75-03 implements the Overview branch.

    NOTE: Use `@tag :skip` (not `flunk/1`) so the existing CI test suite remains green. The stubs are structural placeholders; Plan 75-02 will un-skip and implement them.

    EXTEND mailglass_admin/test/mailglass_admin/operator_live_test.exs: add a NEW `describe "Operator Overview branch"` block at the end of the module (inside the outer module, after existing describes). Add `@tag :skip` pending tests for: "bare /ops/mail/ renders h1 Operator overview (no selected delivery, no tenant)", "no-tenant Overview shows nudge copy not health row", "with-tenant Overview renders 4 health-count cards", "suppression count degradation renders em-dash in text-secondary when count errors", "?view=deliveries param shows Deliveries list not Overview". These will be un-skipped and implemented in Plan 75-03.

    The file mailglass_admin/test/mailglass_admin/operator/shell_test.exs does NOT currently exist — check the operator/ directory first and create it. Confirm the correct test helper module (look at how inbound_live_test.exs opens — should be `use MailglassAdmin.ConnCase` or similar).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && mix test mailglass_admin/test/mailglass_admin/operator/shell_test.exs mailglass_admin/test/mailglass_admin/operator_live_test.exs --seed 0 2>&1 | tail -20</automated>
  </verify>
  <done>
    - mailglass_admin/test/mailglass_admin/operator/shell_test.exs exists with describe blocks for orientation_strip/1 and aria-current nav resolution
    - operator_live_test.exs has a describe "Operator Overview branch" block with at least 5 @tag :skip stubs
    - mix test on both files exits 0 (skipped tests are not failures; existing tests remain green)
    - ls mailglass_admin/test/mailglass_admin/operator/shell_test.exs succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elixir test process → DB | count_active_suppressions/1 reads Postgres via Ecto; test DB is sandboxed per ExUnit.Case |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Information Disclosure | count_active_suppressions/1 return value | accept | Returns an aggregate integer count only; no recipient/address/subject fields exposed — CLAUDE.md PII telemetry rule satisfied at the source |
| T-75-02 | Spoofing | tenant_id argument | mitigate | Guard clause `when is_binary(tenant_id) and tenant_id != ""` + `Tenancy.scope/2` prevents cross-tenant data access; mirrors existing get_delivery_suppression_state/2 guard pattern |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; Package Legitimacy Gate not applicable |
</threat_model>

<verification>
After Wave 1 completes:
- mix test test/mailglass/operator/suppressions_test.exs --seed 0 exits 0 (all cases including new describe block)
- mix test mailglass_admin/test/mailglass_admin/ --seed 0 exits 0 (stubs are @tag :skip, not failures)
- grep -c "def count_active_suppressions" lib/mailglass/operator/suppressions.ex returns 1
- grep -c "describe.*count_active_suppressions" test/mailglass/operator/suppressions_test.exs returns 1
- ls mailglass_admin/test/mailglass_admin/operator/shell_test.exs succeeds
- grep -c "describe.*Operator Overview branch" mailglass_admin/test/mailglass_admin/operator_live_test.exs returns 1
</verification>

<success_criteria>
- count_active_suppressions/1 is present in core suppressions.ex, tenant-scoped, counting only active entries (is_nil(expires_at) or expires_at > now), using the existing filter pattern verbatim
- All 5 new test cases pass in suppressions_test.exs; no pre-existing tests broken
- Wave 0 test stubs exist in shell_test.exs and operator_live_test.exs; CI stays green (stubs are @tag :skip)
- No PII (recipient/address/subject) exposed by the new function — aggregate integer only
- No new imports or aliases added to suppressions.ex
</success_criteria>

<output>
Create `.planning/phases/75-information-architecture-navigation-and-orientation/75-01-SUMMARY.md` when done.
</output>
