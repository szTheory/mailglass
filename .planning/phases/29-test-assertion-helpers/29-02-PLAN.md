---
phase: 29-test-assertion-helpers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/webhook_case.ex
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - User can simulate a webhook payload arriving from a provider and verify the DB is updated.
    - User can test idempotency by re-processing the same webhook without duplicate side effects.
    - User can assert the final state of a delivery in the database.
  artifacts:
    - path: test/support/webhook_case.ex
      provides: "high-level webhook assertions"
      exports: ["assert_webhook_processed", "assert_webhook_idempotent", "assert_delivery_state", "assert_delivery_event_count"]
  key_links:
    - from: test/support/webhook_case.ex
      to: lib/mailglass/webhook/plug.ex
      via: "assert_webhook_processed simulates the plug lifecycle"
      pattern: "Mailglass.Webhook.Plug.call"
---

<objective>
Implement Webhook & Idempotency Assertions to provide high-level test helpers for webhook integration tests.

Purpose: Abstract the `Plug.Conn` construction and pipeline dispatch for end-users, enabling one-liner simulation of complex provider webhooks, idempotency checks, and database state assertions.
Output: New assertion macros/functions in `Mailglass.WebhookCase`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-assertion-helpers/RESEARCH.md
@.planning/phases/29-test-assertion-helpers/TEST_ASSERTION_ARCH_DECISION.md
</context>

<interfaces>
From test/support/webhook_case.ex:
```elixir
  # Existing functions we build upon:
  # mailglass_webhook_conn(provider, raw_body, opts \\ [])
  # stub_sendgrid_fixture("delivered")
  # assert_webhook_ingested(pattern_or_type \\ nil, timeout \\ 100)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add webhook processing and idempotency assertions</name>
  <files>
    - test/support/webhook_case.ex
    - test/support/webhook_case_test.exs
  </files>
  <behavior>
    - Test 1: `assert_webhook_processed(:sendgrid, "delivered")` dispatches the webhook and returns a 2xx conn, waiting for the pubsub broadcast.
    - Test 2: `assert_webhook_idempotent(:sendgrid, "delivered")` dispatches twice; first creates an event, second returns 2xx but creates no new event broadcast.
  </behavior>
  <action>
    Modify `test/support/webhook_case.ex`:
    - Add `assert_webhook_processed(provider, fixture_name, opts \\ [])`
      This should load the fixture via the dynamic `stub_#{provider}_fixture` macro or a case statement, build the conn with `mailglass_webhook_conn(provider, payload, opts)`, dispatch it through `Mailglass.Webhook.Plug.call(conn, Mailglass.Webhook.Plug.init([]))` (or equivalent router dispatch), assert status is 20x, and wait for `assert_webhook_ingested()`.
    - Add `assert_webhook_idempotent(provider, fixture_name, opts \\ [])`
      This should call `assert_webhook_processed` once, then call the Plug again with the exact same conn/payload, assert the status is 20x, but assert NO new PubSub event is broadcast (using `refute_receive`).
    
    Ensure to handle the `opts` carefully, perhaps passing `delivery_id` into the fixture template if the fixture supports substitution, or just passing `opts` to `mailglass_webhook_conn/3` for keys/signatures.
  </action>
  <verify>
    <automated>mix test test/support/webhook_case_test.exs</automated>
  </verify>
  <done>High-level webhook simulation and idempotency assertions exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add delivery state database assertions</name>
  <files>
    - test/support/webhook_case.ex
    - test/support/webhook_case_test.exs
  </files>
  <behavior>
    - Test 1: `assert_delivery_state(id, :delivered)` fetches from DB and asserts the `status` field.
    - Test 2: `assert_delivery_event_count(id, 1)` fetches from DB and asserts the length of the events array or count aggregate.
  </behavior>
  <action>
    Modify `test/support/webhook_case.ex`:
    - Add `assert_delivery_state(delivery_id, expected_status)`
      Use `Mailglass.Delivery.get/1` or `Mailglass.Repo.get!` to fetch the Delivery record and assert its status matches `expected_status`. Provide a clear ExUnit `flunk` or `assert x == y` message.
    - Add `assert_delivery_event_count(delivery_id, expected_count)`
      Fetch the delivery with its preloaded `events`, or run a direct query to count `Mailglass.DeliveryEvent` records for the delivery, and assert it equals `expected_count`.
  </action>
  <verify>
    <automated>mix test test/support/webhook_case_test.exs</automated>
  </verify>
  <done>Delivery database state assertions exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer test suite | Execution environment runs trusted code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02   | Repudiation | Test Assertions | accept | Test assertions operate only in the testing environment and don't execute in production. |
</threat_model>

<verification>
- Webhook tests can now be written in just a few lines using `assert_webhook_processed` and `assert_delivery_state`.
- Idempotency assertions correctly fail if duplicate events are generated.
</verification>

<success_criteria>
`TEST-02` is strictly met. Test cases clearly prove that webhooks and delivery DB states can be effortlessly tested without boilerplate Plug manipulation.
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-assertion-helpers/29-02-SUMMARY.md`
</output>