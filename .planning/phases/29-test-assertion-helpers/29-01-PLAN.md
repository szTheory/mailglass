---
phase: 29-test-assertion-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/message.ex
  - lib/mailglass/mailable.ex
  - lib/mailglass/test_assertions.ex
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - User can assert a mailable was dispatched with specific assigns.
    - User can instantiate a mailable with assigns directly via `new(assigns)`.
    - User can verify the text and HTML content of a generated message using assertion matchers.
  artifacts:
    - path: lib/mailglass/message.ex
      provides: "assigns storage"
      contains: "assigns: %{}"
    - path: lib/mailglass/mailable.ex
      provides: "frictionless assignment via new/1"
    - path: lib/mailglass/test_assertions.ex
      provides: "assigns and content assertions"
      exports: ["assert_mail_html_matches", "assert_mail_text_matches"]
  key_links:
    - from: lib/mailglass/mailable.ex
      to: lib/mailglass/message.ex
      via: "new/1 calls Message.assign/2"
      pattern: "Message.assign"
---

<objective>
Implement Outbound Delivery Assertions to allow developers to assert against domain data (assigns) instead of parsed HTML strings.

Purpose: Greatly improve developer ergonomics and testing resilience by making domain data a first-class testable artifact on the Message struct.
Output: Frictionless `new(assigns)` instantiation in Mailables and `:assigns` assertion support in `assert_mail_sent`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-assertion-helpers/RESEARCH.md
@.planning/phases/29-test-assertion-helpers/TEST_ASSERTION_ARCH_DECISION.md
</context>

<interfaces>
From lib/mailglass/message.ex:
```elixir
  @type t :: %__MODULE__{
          # existing fields
          swoosh_email: Swoosh.Email.t(),
          mailable: module() | nil,
          mailable_function: atom() | nil,
          tenant_id: String.t() | nil,
          stream: stream(),
          tags: [String.t()],
          metadata: %{atom() => term()},
          # NEW:
          assigns: %{atom() => term()}
        }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assigns to Message and Mailable API</name>
  <files>
    - lib/mailglass/message.ex
    - lib/mailglass/mailable.ex
    - test/mailglass/message_test.exs
  </files>
  <behavior>
    - Test 1: `Message.assign/3` adds a key-value to `assigns`.
    - Test 2: `Message.assign/2` merges a keyword list or map into `assigns`.
    - Test 3: A module using `Mailglass.Mailable` exposes `new/1` that initializes the message with the provided assigns (e.g. `MyMailer.new(user: "Jon") |> Map.get(:assigns)` returns `%{user: "Jon"}`).
  </behavior>
  <action>
    Modify `lib/mailglass/message.ex`:
    - Add `assigns: %{}` to the `defstruct`.
    - Add `assigns: %{atom() => term()}` to `@type t`.
    - Add `assign(%Message{} = msg, key, value)` and `assign(%Message{} = msg, attrs)` helper functions.

    Modify `lib/mailglass/mailable.ex`:
    - Update the `__using__` macro. Replace the existing `def new` with a function that accepts `assigns \\ []`.
    - `def new(assigns \\ [])` should call `Mailglass.Message.new_from_use(__MODULE__, @mailglass_opts)` and then pipe the result to `Mailglass.Message.assign(assigns)`.
  </action>
  <verify>
    <automated>mix test test/mailglass/message_test.exs</automated>
  </verify>
  <done>Message struct natively supports assigns, and Mailables can be instantiated via `new(user: user)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend TestAssertions with assigns and content matchers</name>
  <files>
    - lib/mailglass/test_assertions.ex
    - test/mailglass/test_assertions_test.exs
  </files>
  <behavior>
    - Test 1: `assert_mail_sent(assigns: %{user: "Jon"})` passes if a matching message is in the mailbox.
    - Test 2: `assert_mail_sent` fails with a clear message if `:assigns` don't match or are partially missing.
    - Test 3: `assert_mail_html_matches(msg, ~r/invoice_1234/)` passes when HTML body contains the regex.
    - Test 4: `assert_mail_text_matches(msg, "total")` passes when text body contains the string.
  </behavior>
  <action>
    Modify `lib/mailglass/test_assertions.ex`:
    - Extend `__match_keyword__/2`. Add a clause for `{:assigns, expected_assigns}`.
    - For `:assigns`, assert that the expected map is a subset of `msg.assigns` (using Map.merge or pattern matching), or enforce an exact match depending on the convention. A subset match (checking all provided expected assigns exist in the msg.assigns) is usually more ergonomic for tests. Example: `Enum.all?(expected_assigns, fn {k, v} -> Map.get(msg.assigns, k) == v end)`.
    - Add standalone functions `assert_mail_html_matches(msg, pattern)` and `assert_mail_text_matches(msg, pattern)` where pattern can be a binary or Regex. Use `ExUnit.Assertions.assert` and `String.contains?/2` or `Regex.match?/2`. Ensure clear flunk messages on failure.
  </action>
  <verify>
    <automated>mix test test/mailglass/test_assertions_test.exs</automated>
  </verify>
  <done>Developers can use `assert_mail_sent(assigns: %{...})` and the new content matchers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer test suite | Execution environment runs trusted code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01   | Repudiation | Test Assertions | accept | Test assertions operate only in the testing environment and don't execute in production. |
</threat_model>

<verification>
- `Mailglass.Message` now contains `assigns` map.
- Mailable constructor correctly populates assigns.
- Test suite verifies partial map matches for `:assigns` query in `assert_mail_sent`.
</verification>

<success_criteria>
`TEST-01` is strictly met. Test cases clearly prove that mailable testing avoids brittle HTML string parsing via domain assigns verification.
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-assertion-helpers/29-01-SUMMARY.md`
</output>