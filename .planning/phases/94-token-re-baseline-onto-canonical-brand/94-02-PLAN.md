---
phase: 94
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/test/mailglass_admin/token_parity_test.exs
  - mailglass_admin/test/mailglass_admin/accessibility_test.exs
  - mailglass_admin/test/mailglass_admin/brand_test.exs
  - mailglass_admin/mix.exs
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04

must_haves:
  truths:
    - "A fail-closed ExUnit test reads the compiled priv/static/app.css and asserts every daisyUI --color-* slot resolves to the oracle value from brandbook/tokens.json via var(--mg-*)"
    - "The parity test includes a structural assertion that no raw #hex literal appears on any --color-* line inside a daisyUI theme selector block"
    - "The parity test runs in the support_contract_admin CI lane (required, branch-protection)"
    - "Five new contrast assertions in accessibility_test.exs prove dark muted (#B8CAD4) and error (#E29089) pass AA 4.5:1 on Ink, primary-content is pinned, and both border roles are pinned sub-3:1 with a decorative-exemption docstring"
    - "brand_test.exs three literal-hex assertions updated to the two-tier var+hex form so they pass after the Wave 1 re-baseline"
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/token_parity_test.exs"
      provides: "Fail-closed parity test (TOKEN-04; structural TOKEN-01)"
      exports: ["MailglassAdmin.TokenParityTest"]
    - path: "mailglass_admin/test/mailglass_admin/accessibility_test.exs"
      provides: "5 new contrast tests (TOKEN-03)"
    - path: "mailglass_admin/test/mailglass_admin/brand_test.exs"
      provides: "Updated 3 assertions (TOKEN-01/02)"
    - path: "mailglass_admin/mix.exs"
      provides: "token_parity_test.exs in verify.support_contract.admin alias"
      contains: "token_parity_test.exs"
  key_links:
    - from: "token_parity_test.exs"
      to: "priv/static/app.css"
      via: "Application.app_dir(:mailglass_admin, 'priv') compiled-bundle seam"
    - from: "token_parity_test.exs"
      to: "brandbook/tokens.json"
      via: "__DIR__ three levels up + Path.expand"
    - from: "mix.exs verify.support_contract.admin"
      to: "token_parity_test.exs"
      via: "test path string in alias"
---

<objective>
Add the fail-closed token-parity ExUnit test and extend the accessibility + brand tests — all against the CURRENT compiled bundle. These tests are written in Wave 0 so they are committed GREEN against today's hex-literal bundle and turn RED when the Wave 1 app.css rewrite lands, closing the stale-bundle footgun for every future change.

Purpose: D-10 step 2 — test infra before the CSS rewrite. The parity test's structural assertion (no raw hex in theme selector) will fail against the current bundle; that half either lands alongside Wave 1 or the test is split. See task 2 for the exact commit strategy.

Output: one new test file (token_parity_test.exs), two updated test files (accessibility_test.exs extended, brand_test.exs 3 assertions updated), one updated mix.exs alias.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from existing test files — read these before implementing -->

From mailglass_admin/test/mailglass_admin/brand_test.exs:
- Module: MailglassAdmin.BrandTest
- Compiled-bundle seam (lines 15-23, copy verbatim for token_parity_test.exs):
    @css_path Path.join([Application.app_dir(:mailglass_admin, "priv"), "static", "app.css"])
    setup do; css = File.read!(@css_path); {:ok, css: css}; end
- Lines 44-52 (THREE literal-hex assertions to replace with two-tier var+hex form):
    assert lowered =~ "--color-base-100: #f8fbfd" or lowered =~ "--color-base-100:#f8fbfd"
    assert lowered =~ "--color-primary: #277b96" or lowered =~ "--color-primary:#277b96"
    assert lowered =~ "--color-base-content: #0d1b2a" or lowered =~ "--color-base-content:#0d1b2a"
  Replacement form per CONTEXT.md D-02 + PATTERNS.md §brand_test.exs:
    assert css =~ "--color-base-100: var(--mg-color-background)" or css =~ "--color-base-100:var(--mg-color-background)"
    assert String.downcase(css) =~ "--mg-color-background: #f8fbfd"
    (same two-tier pattern for primary/base-content)
- Lines 26-37 ("brand palette hex values" block): DO NOT TOUCH
- Lines 56-65 ("mailglass-dark theme" block): DO NOT TOUCH
- Lines 68-82 ("visual DON'Ts" block): DO NOT TOUCH

From mailglass_admin/test/mailglass_admin/accessibility_test.exs:
- Module: MailglassAdmin.AccessibilityTest
- Private functions (defp, already exist — do NOT redefine):
    defp contrast_ratio("#" <> _ = hex_a, "#" <> _ = hex_b) — WCAG 2.1 relative luminance
    defp luminance("#" <> hex), defp parse_hex, defp hex_pair, defp linearize
- Existing describe blocks: "canonical contrast pairs" (lines 15-43), "borderline pair" (lines 45-67)
- Two NEW describe blocks to add per PATTERNS.md §accessibility_test.exs:
    describe "dark-mode token fixes (TOKEN-03)" — 3 tests: muted #B8CAD4≥4.5, error #E29089≥4.5, primary-content pin
    describe "border role is intentionally sub-3:1 (WCAG 1.4.11 decorative exemption)" — 2 tests: light border < 3.0, dark border < 3.0

From mailglass_admin/test/mailglass_admin/bundle_test.exs:
- Failure message style: "app.css is #{size} bytes; PREV-06 budget is <150KB"
- Pattern: always include a self-serve fix command in assertion messages

Oracle path from RESEARCH.md §token_parity_test seam:
  @tokens_path Path.expand(Path.join([__DIR__, "..", "..", "..", "brandbook", "tokens.json"]))
  # Three levels up: test/mailglass_admin/ → test/ → mailglass_admin/ → monorepo root → brandbook/

Full @mapping from RESEARCH.md §Full Per-Slot daisyUI Mapping Table:
  Light theme changed slots: base-200 → --mg-color-surface-raised, base-300 → --mg-color-border, accent → --mg-color-accent (Glass #277B96), info → --mg-color-info-solid (#1D637A)
  Dark theme changed slots: base-300 → --mg-color-border (#315069), secondary → --mg-color-text-muted (#B8CAD4), secondary-content → --mg-color-text-inverse (#0D1B2A), accent → --mg-color-accent (#A6EAF2), accent-content → --mg-color-text-inverse, neutral → --mg-color-text-muted (#B8CAD4), neutral-content → --mg-color-text-inverse, error → --mg-color-error-solid (#E29089)

mix.exs verify.support_contract.admin alias (lines 189-191, current):
  "verify.support_contract.admin": [
    "test test/mailglass_admin/post_installer_smoke_test.exs ... test/mailglass_admin/auth_test.exs --warnings-as-errors"
  ]
  After: append "test/mailglass_admin/token_parity_test.exs" before "--warnings-as-errors"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token_parity_test.exs and extend accessibility_test.exs</name>
  <files>
    mailglass_admin/test/mailglass_admin/token_parity_test.exs
    mailglass_admin/test/mailglass_admin/accessibility_test.exs
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/brand_test.exs (full — copy compiled-bundle seam from lines 15-23; study module header + docstring style)
    - mailglass_admin/test/mailglass_admin/bundle_test.exs (full — study failure message style with self-serve fix)
    - mailglass_admin/test/mailglass_admin/accessibility_test.exs (full — read existing contrast_ratio/2 + luminance/1 defp implementations; identify append point after last describe block)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §token_parity_test.exs and §accessibility_test.exs
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md §token_parity_test seam and §Full Per-Slot daisyUI Mapping Table
    - brandbook/tokens.json (read once to understand the W3C token format — palette tier + color.light/dark semantic tier with {palette.x} aliases)
  </read_first>
  <action>
    Create mailglass_admin/test/mailglass_admin/token_parity_test.exs as a new file.

    Module: MailglassAdmin.TokenParityTest. Use `use ExUnit.Case, async: true`. @tag :token_parity as a convenience selector (not excluded from any CI lane).

    @moduledoc must state: (1) this reads compiled priv/static/app.css; (2) if it fails with "expected var(--mg-*) but got #hex" the bundle is stale — run `cd mailglass_admin && mix mailglass_admin.assets.build && git add priv/static/` and commit; (3) if it fails with a value mismatch, brandbook/tokens.json changed without resyncing app.css; (4) never hand-edit priv/static/app.css; (5) `actual` is what currently SHIPS.

    @css_path: copy verbatim from brand_test.exs lines 15-23 (Application.app_dir path construction).

    @tokens_path: `Path.expand(Path.join([__DIR__, "..", "..", "..", "brandbook", "tokens.json"]))` — three levels up from test/mailglass_admin/ to monorepo root.

    setup_all: assert `File.exists?(@tokens_path)` with message "tokens.json not found at #{@tokens_path} — run from mailglass_admin/"; decode the JSON with `Jason.decode!(File.read!(@tokens_path))`. Return `{:ok, tokens: decoded}`.

    setup (per-test): read @css_path via `File.read!` and make available as `%{css: css}` in test context.

    @mapping module attribute: a map of `{theme_name, daisyui_slot} => mg_token_name` encoding the FULL slot table from RESEARCH.md. Include ALL slots for both themes from the per-slot table (not just the changed ones — the full map is the contract). Use the token names from the "Target --mg-* token" column.

    Helper: a private `resolve_oracle(tokens, mg_token_name)` function that looks up the token value in the W3C format: check `tokens["color"]["light"][key]` or `tokens["color"]["dark"][key]` based on a convention, resolving `{palette.x}` aliases via `tokens["palette"][x]["$value"]`. The `mg_token_name` maps to the key by stripping `--mg-color-` prefix.

    Structural test ("no raw hex in --color-* lines in theme selectors"): search the compiled CSS for the pattern `[data-theme=mailglass-` to find theme block boundaries, then assert no `--color-[a-z-]+: #[0-9a-fA-F]` raw hex form appears within those theme blocks. Use a regex approach consistent with how brand_test.exs does string searches. Failure message: "TOKEN-01 violation: raw hex literal on a --color-* line inside a daisyUI theme block. All --color-* values must reference var(--mg-*). Run mix mailglass_admin.assets.build."

    Value-equality test: iterate the @mapping, for each `{theme, slot} => mg_token`, find the daisyUI theme block in the compiled CSS, extract the value for that slot, assert it is `var(mg_token)` (either spaced or unspaced colon). Then resolve mg_token against the inlined --mg-* declarations in the CSS and compare to the oracle value from tokens.json. Collect ALL mismatches before asserting (build a list, assert the list is empty with the full list as the message).

    Failure message per mismatch: "#{theme} #{slot}: expected var(#{mg_token}); actual: #{actual_value}. Bundle is stale or app.css hand-edited — run mix mailglass_admin.assets.build"

    NOTE on Wave 0 commit greenness: The structural assertion (no raw hex) WILL FAIL against the current compiled bundle (which still has raw hex values). The value-equality assertions WILL ALSO FAIL (current bundle has hex, not var(--mg-*)). This means token_parity_test.exs starts RED. Per D-10 step 2 and CONTEXT.md, the exact split of test commits is Claude's discretion — commit this test file as a "failing test for the re-baseline" in the same commit with Wave 1 (94-03-PLAN.md), or as a standalone commit clearly labeled "red test that Wave 1 makes green." Do NOT suppress the tests or add skip tags to make them appear green before the CSS rewrite.

    For accessibility_test.exs: append two new describe blocks AFTER the existing last describe block (after line 67 approximately). Do NOT modify any existing tests. Copy the exact describe/test structure from PATTERNS.md §accessibility_test.exs. The five tests use hardcoded hex literals (per the existing pattern in that file) with inline comments citing the token source (brandbook/tokens.css). The contrast_ratio/2 defp is already defined — call it directly.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - accessibility_test.exs: all existing tests pass AND the 5 new tests pass (muted #B8CAD4 ≥4.5:1 on Ink, error #E29089 ≥4.5:1 on Ink, primary-content Ink on Ice ≥4.5:1, light border #C7DCE5 on Paper < 3.0, dark border #315069 on Ink < 3.0)
    - token_parity_test.exs: file exists; module compiles without errors (`mix compile` in mailglass_admin green); tests are RED against current bundle (this is expected and correct — they will go green in Wave 1)
    - `cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs` exits 0
  </acceptance_criteria>
  <done>accessibility_test.exs has 5 new passing contrast tests (TOKEN-03 proof). token_parity_test.exs created with full @mapping, structural assertion, and value-equality assertions (will be red until Wave 1 CSS rewrite).</done>
</task>

<task type="auto">
  <name>Task 2: Update brand_test.exs three assertions and add token_parity_test.exs to mix.exs support_contract alias</name>
  <files>
    mailglass_admin/test/mailglass_admin/brand_test.exs
    mailglass_admin/mix.exs
  </files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/brand_test.exs (full — identify lines 44-52 exactly; confirm untouched sections at lines 26-37, 56-65, 68-82)
    - mailglass_admin/mix.exs lines 187-196 (verify.support_contract.admin alias current form)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §brand_test.exs and §mix.exs
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md §brand_test.exs seam
  </read_first>
  <action>
    In brand_test.exs, replace the three literal-hex assertions at approximately lines 44-52 with the two-tier var+hex form. Each assertion becomes a pair: (a) assert `var(--mg-*)` reference in the theme block, (b) assert the resolved hex from the inlined --mg-* declarations. The three replacements:

    Line 44-45 (--color-base-100 / Paper): replace with assertion for `--color-base-100: var(--mg-color-background)` (or without space) AND assert `String.downcase(css) =~ "--mg-color-background: #f8fbfd"` with message `--mg-color-background token (#F8FBFD Paper) must be inlined in compiled CSS`.

    Lines 47-48 (--color-primary / Glass): replace with assertion for `--color-primary: var(--mg-color-accent)` AND assert `String.downcase(css) =~ "--mg-color-accent: #277b96"` with message `--mg-color-accent token (#277B96 Glass) must be inlined in compiled CSS`.

    Lines 50-52 (--color-base-content / Ink): replace with assertion for `--color-base-content: var(--mg-color-text)` AND assert `String.downcase(css) =~ "--mg-color-text: #0d1b2a"` with message `--mg-color-text token (#0D1B2A Ink) must be inlined in compiled CSS`.

    Keep the variable name `lowered` or use `css` directly — check the actual test context binding from the file. These new assertions WILL FAIL against the current compiled bundle (hex not yet var form). That is correct — they will go green in Wave 1.

    NOTE: Lines 26-37 ("brand palette hex values"), lines 56-65 ("mailglass-dark theme"), and lines 68-82 ("visual DON'Ts") MUST NOT be changed.

    In mix.exs, update the `verify.support_contract.admin` alias string at lines 189-191: append `test/mailglass_admin/token_parity_test.exs` to the space-separated test path list, before `--warnings-as-errors`. Preserve the exact string format (single string in a list).
  </action>
  <verify>
    <automated>grep -n 'token_parity_test\|mg-color-background\|mg-color-accent\|mg-color-text' /Users/jon/projects/mailglass/mailglass_admin/test/mailglass_admin/brand_test.exs</automated>
    <automated>grep -n 'token_parity_test' /Users/jon/projects/mailglass/mailglass_admin/mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - brand_test.exs contains `var(--mg-color-background)`, `var(--mg-color-accent)`, `var(--mg-color-text)` in the updated assertions
    - brand_test.exs does NOT contain the old literal `--color-base-100: #f8fbfd`, `--color-primary: #277b96`, `--color-base-content: #0d1b2a` assertion patterns (they have been replaced)
    - brand_test.exs lines for the DON'Ts block (backdrop-filter, --depth, --noise) remain unchanged
    - mix.exs `verify.support_contract.admin` alias string contains `token_parity_test.exs`
    - `cd mailglass_admin && mix compile` exits 0 (no syntax errors introduced)
  </acceptance_criteria>
  <done>brand_test.exs three assertions updated to two-tier var+hex form (will fail until Wave 1). mix.exs alias updated to include token_parity_test.exs in the required support-contract lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → priv/static/app.css | File read at test time; no exec; path is repo-local |
| test suite → brandbook/tokens.json | File read via __DIR__-relative path; JSON parsed with Jason; no exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Tampering | token_parity_test.exs reading priv/static/app.css | accept | File is repo-committed; tests are read-only; no write path |
| T-94-05 | Information Disclosure | brandbook/tokens.json oracle path via __DIR__ | accept | tokens.json contains only color hex values — no PII, no secrets; file is repo-committed |
| T-94-06 | Denial of Service | parity test fails on stale bundle blocking CI | accept | This is intentional fail-closed behavior (TOKEN-04 requirement); the failure message names the self-serve fix |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; Jason is an existing dep |

**Assessment: LOW threat surface.** Pure ExUnit file-I/O + Jason parsing. No network, no user input, no PII, no auth. The intentional fail-closed behavior is a feature, not a risk.
</threat_model>

<verification>
After both tasks complete, run:

```
cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs
# Expected: all tests pass (including the 5 new TOKEN-03 contrast tests) — exit 0

cd mailglass_admin && mix compile
# Expected: exit 0 (token_parity_test.exs compiles cleanly)

grep 'token_parity_test' mailglass_admin/mix.exs
# Expected: appears in verify.support_contract.admin alias string
```

Note: brand_test.exs and token_parity_test.exs will be RED against the current bundle — this is correct and expected. They turn green in Wave 1 (94-03-PLAN.md).
</verification>

<success_criteria>
- token_parity_test.exs exists and compiles; structural + value-equality assertions are present (red against current bundle — correct)
- accessibility_test.exs has 5 new passing tests: muted text, error solid, primary-content (≥4.5:1), light border, dark border (< 3.0)
- brand_test.exs three assertions use two-tier var+hex form (red against current bundle — correct)
- mix.exs verify.support_contract.admin alias includes token_parity_test.exs
- commit message: `test(admin): add fail-closed token-parity test` (note: this commit may be bundled with Wave 1 commit if keeping each commit green is cleaner — per D-10 Claude's discretion)
</success_criteria>

<validation>
Dimension 8 proof commands from 94-VALIDATION.md:

| Requirement | Command | Expected result |
|-------------|---------|-----------------|
| TOKEN-04 (parity oracle + startup assert) | `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs` | RED against current bundle (expected; green after Wave 1) |
| TOKEN-03 (dark AA + border sub-3:1 pin) | `cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs` | exit 0 (5 new tests green immediately) |
| TOKEN-04 (CI lane wired) | `grep 'token_parity_test' mailglass_admin/mix.exs` | match found in verify.support_contract.admin |
| TOKEN-01/02 brand_test update | `cd mailglass_admin && mix test test/mailglass_admin/brand_test.exs` | RED (expected; green after Wave 1) |
</validation>

<output>
Create `.planning/phases/94-token-re-baseline-onto-canonical-brand/94-02-SUMMARY.md` when done.
</output>
