---
phase: 94
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/scripts/check-conformance-advisory.sh
  - scripts/check_motion_conformance.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - RATCHET-03

must_haves:
  truths:
    - "All five existing hard-closed conformance gates (BADGE/TYPE-base/BOLD/GAP/HEX) continue to exit 0 on the current codebase"
    - "check-conformance.sh runs in the credo_strict CI job (was DEAD — never wired)"
    - "A separate check-conformance-advisory.sh runs with continue-on-error in CI, logging TYPE-lg/xl and TRACK-[…] violations without blocking main"
    - "The THRASH_PATTERN in check_motion_conformance.sh catches layout-property transitions (width/spacing/margin/inset/top/right/bottom/left) and arbitrary transition-[layout] JIT forms"
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance-advisory.sh"
      provides: "Advisory TYPE-lg/xl and TRACK-[ gate script (exits 0)"
      contains: "exit 0"
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "Hard-closed 5-gate conformance script (unchanged exit contract)"
    - path: ".github/workflows/ci.yml"
      provides: "Two new steps in credo_strict job wiring both scripts"
      contains: "check-conformance-advisory.sh"
  key_links:
    - from: ".github/workflows/ci.yml credo_strict job"
      to: "mailglass_admin/scripts/check-conformance.sh"
      via: "bash step (hard, no continue-on-error)"
    - from: ".github/workflows/ci.yml credo_strict job"
      to: "mailglass_admin/scripts/check-conformance-advisory.sh"
      via: "bash step with continue-on-error: true"
---

<objective>
Wire the previously-DEAD `check-conformance.sh` into CI and tighten the two conformance scripts with new gate arms — all before the token re-baseline lands, so the re-baseline cannot regress silently. No CSS, no markup, no test files.

Purpose: D-10 mandates gates FIRST so every subsequent commit is automatically checked. The hard script's exit-1 contract must remain intact; advisory patterns go in a separate script so `continue-on-error: true` cannot accidentally soften the existing five hard gates.

Output: three modified files (check-conformance.sh TYPE-GATE extended on hard arm for `sm|xs` only — see task note; check_motion_conformance.sh THRASH_PATTERN extended; ci.yml two new steps) and one new file (check-conformance-advisory.sh with TYPE-lg/xl + TRACK-[ arms, exits 0).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md

<interfaces>
<!-- Authoritative gate script state — read before editing -->

From mailglass_admin/scripts/check-conformance.sh (full file, 89 lines):
- Lines 1-24: shebang + BASH_SOURCE anchor (SCRIPT_DIR / LIB / dir-assert / errors=0)
- Line 45 (TYPE-GATE grep, the ONLY line to change in this file):
    if grep -rEn 'text-(sm|xs)\b|text-base($|[^-])' "$LIB" --include="*.ex" 2>/dev/null; then
  NOTE per PATTERNS.md §check-conformance.sh: the `lg|xl|2xl…` arm is ADVISORY and goes in
  check-conformance-advisory.sh, NOT here. This file's TYPE-GATE stays as `text-(sm|xs)\b` only.
- Lines 84-89: fail-closed error-count block (keep verbatim)

From scripts/check_motion_conformance.sh (full file, 39 lines):
- Line 18 (THRASH_PATTERN, the ONLY line to change):
    THRASH_PATTERN='transition-height|transition-max-height|transition-padding|transition-all|duration-[3-9][0-9][0-9]|duration-[0-9]{4,}'
  Extended form (D-07 + RESEARCH.md §THRASH_PATTERN):
    THRASH_PATTERN='transition-(height|max-height|padding|width|spacing|margin|inset|top|right|bottom|left)\b|transition-all|transition-\[(width|height|margin|padding|inset|top|right|bottom|left)|duration-[3-9][0-9][0-9]|duration-[0-9]{4,}'
- Line 28 (Pass B EASE_PATTERN): DO NOT CHANGE — ease-in[^-] already covers bare ease-in.

From .github/workflows/ci.yml credo_strict job (lines 395-404, the insertion context):
      - name: Verify suppression docs (shell gate)
        run: bash scripts/check_credo_suppressions.sh
      - name: Verify motion conformance (shell gate)
        run: bash scripts/check_motion_conformance.sh
      - name: Run Credo strict
        run: mix credo --strict
  Insert two new steps BETWEEN "Verify motion conformance" and "Run Credo strict".
  Scripts are BASH_SOURCE-anchored and cwd-independent — run from repo root with no working-directory:.

check-conformance-advisory.sh structure (from PATTERNS.md §check-conformance-advisory.sh):
- BASH_SOURCE anchor identical to check-conformance.sh lines 22-24
- Two advisory arms (TYPE-lg/xl and TRACK-[): print WARN, do NOT increment an errors counter
- Script ALWAYS exits 0 — this is the critical difference from the hard script
- Phase 99 flip note in header comments
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create check-conformance-advisory.sh and extend check_motion_conformance.sh THRASH_PATTERN</name>
  <files>
    mailglass_admin/scripts/check-conformance-advisory.sh
    scripts/check_motion_conformance.sh
  </files>
  <read_first>
    - mailglass_admin/scripts/check-conformance.sh (full — copy BASH_SOURCE anchor from lines 22-24; study gate block shape from lines 27-48)
    - scripts/check_motion_conformance.sh (full — line 18 THRASH_PATTERN is the only edit)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §check-conformance-advisory.sh and §check_motion_conformance.sh
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md D-07 + D-08
  </read_first>
  <action>
    Create mailglass_admin/scripts/check-conformance-advisory.sh as a new file. It must be chmod +x (add execute bit via git). Structure mirrors check-conformance.sh but with one critical difference: this script exits 0 unconditionally.

    Header block: `#!/usr/bin/env bash` + a comment block explaining: advisory conformance gates for TYPE-GATE (text-lg/xl/2xl/3xl/4xl/5xl) and TRACK-GATE (tracking-[arbitrary]); these have known violations deferred to Phase 98/99; run with `continue-on-error: true` in CI until Phase 99 flips to hard-fail. Include the Phase 99 flip instruction and the known violation counts (5 TYPE sites, ~43 TRACK sites).

    After `set -euo pipefail`, add the BASH_SOURCE anchor (identical to check-conformance.sh lines 22-24): SCRIPT_DIR, LIB, dir-assertion (exit 2 if missing).

    Two advisory gate blocks (no errors counter, no exit 1):

    TYPE-GATE (advisory): `grep -rEn 'text-(lg|xl|2xl|3xl|4xl|5xl)\b' "$LIB" --include="*.ex" 2>/dev/null` — if it matches, print `WARN: TYPE-GATE (advisory) — text-lg/xl found; fix in Phase 98/99 (use text-heading/display tokens)` to stderr. Do not call `exit 1`. Do not use an errors counter.

    TRACK-GATE (advisory): `grep -rEn 'tracking-\[' "$LIB" --include="*.ex" 2>/dev/null` — if it matches, print `WARN: TRACK-GATE (advisory) — arbitrary tracking-[…] found; define --tracking-eyebrow token first, then migrate in Phase 98/99` to stderr. Same: no exit 1.

    End with `echo "OK: advisory conformance check complete (violations above are logged, not blocking)."` and explicit `exit 0`.

    For scripts/check_motion_conformance.sh: edit ONLY line 18 — replace the THRASH_PATTERN value with the extended form that adds layout-property named transitions and the arbitrary JIT form. The extended pattern is: `transition-(height|max-height|padding|width|spacing|margin|inset|top|right|bottom|left)\b|transition-all|transition-\[(width|height|margin|padding|inset|top|right|bottom|left)|duration-[3-9][0-9][0-9]|duration-[0-9]{4,}`. Do NOT touch line 28 (Pass B EASE_PATTERN — already correct per D-07).
  </action>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance-advisory.sh; echo "advisory exit: $?"</automated>
    <automated>bash scripts/check_motion_conformance.sh; echo "motion exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash mailglass_admin/scripts/check-conformance-advisory.sh` exits 0 AND prints WARN lines for the 5 TYPE sites and ~43 TRACK sites (violations are logged, not blocking)
    - `bash scripts/check_motion_conformance.sh` exits 0 (no THRASH violations in current codebase after extension — the new arms add coverage but should not catch existing code)
    - `check-conformance-advisory.sh` contains `exit 0` and does NOT contain `exit 1` or `errors=$((errors + 1))`
  </acceptance_criteria>
  <done>check-conformance-advisory.sh exists, is executable, exits 0, and prints advisory WARNs for the known violations. check_motion_conformance.sh THRASH_PATTERN extended, exits 0 on current codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Wire check-conformance.sh (hard) and check-conformance-advisory.sh (advisory) into ci.yml credo_strict job</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - .github/workflows/ci.yml lines 395-410 (insertion context: after "Verify motion conformance" step, before "Run Credo strict" step)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §ci.yml
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md D-06 + D-08
  </read_first>
  <action>
    Edit .github/workflows/ci.yml. In the `credo_strict` job, insert two new steps between the "Verify motion conformance (shell gate)" step and the "Run Credo strict" step. The insertion point is immediately after the `run: bash scripts/check_motion_conformance.sh` line.

    Step 1 (hard-fail): name "Verify design-system conformance (shell gate — hard-fail arms)", with a comment block explaining: wires the previously-DEAD check-conformance.sh (D-06, RATCHET-03); five hard-closed gates: BADGE / TYPE-base / BOLD / GAP / HEX; script is BASH_SOURCE-anchored, cwd-independent, run from repo root. Run command: `bash mailglass_admin/scripts/check-conformance.sh`. No `continue-on-error` key (hard-fail is the default).

    Step 2 (advisory): name "Verify design-system conformance (advisory arms — TYPE-lg/xl + TRACK)", with a comment block explaining: advisory until Phases 98/99 clean the 5 text-xl/lg and ~43 tracking-[0.08em] sites; continue-on-error keeps main green; flip to hard-fail in Phase 99 plan. Add `continue-on-error: true` at the step level (same indentation as `name` and `run`). Run command: `bash mailglass_admin/scripts/check-conformance-advisory.sh`.

    Match the existing step indentation and comment style exactly (6-space indent for step keys under the steps list, 8-space for `name`/`run`/`continue-on-error`). The existing "Verify motion conformance" step has a comment block (`# Fails CI if...`) — match that pattern.

    Do NOT change any other part of ci.yml.
  </action>
  <verify>
    <automated>grep -n 'check-conformance' /Users/jon/projects/mailglass/.github/workflows/ci.yml</automated>
    <automated>grep -n 'continue-on-error' /Users/jon/projects/mailglass/.github/workflows/ci.yml</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh && echo "hard exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml now contains two lines with `check-conformance` (hard + advisory scripts)
    - `continue-on-error: true` appears exactly once, on the advisory step
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0 on current codebase (all five existing hard gates are clean — zero violations)
    - The five original hard gates (BADGE/TYPE-base/BOLD/GAP/HEX) in check-conformance.sh are NOT modified in this task
  </acceptance_criteria>
  <done>ci.yml credo_strict job runs check-conformance.sh (hard) then check-conformance-advisory.sh (continue-on-error) between the motion step and `mix credo --strict`. Running `bash mailglass_admin/scripts/check-conformance.sh` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell scripts → lib/*.ex files | grep reads source files; no exec, no user input, no network |
| ci.yml → script invocations | CI executor calls bash; scripts are committed, not externally fetched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | check-conformance-advisory.sh (new file) | accept | Script is committed in git, same origin as the hard script; no runtime input paths; read-only grep on lib/ |
| T-94-02 | Denial of Service | advisory gate with continue-on-error | accept | Failure of advisory step does not block CI by design; worst case: advisory output is noisy, never silent-green |
| T-94-03 | Spoofing | advisory script exits 0 always | accept | Low risk: the advisory script's exit-0 contract is intentional and documented; the hard script keeps exit-1 for violations |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; bash + grep only |

**Assessment: LOW threat surface.** No user input, no network, no PII, no auth paths. The scripts are pure file-I/O grep gates. The only behavioral risk is the advisory/hard split being misconfigured — mitigated by Task 2's acceptance criterion that verifies `continue-on-error` appears exactly once and only on the advisory step.
</threat_model>

<verification>
Run from monorepo root after both tasks complete:

```
bash mailglass_admin/scripts/check-conformance.sh
# Expected: "OK: design-system conformance clean." + exit 0

bash mailglass_admin/scripts/check-conformance-advisory.sh
# Expected: WARN lines for TYPE and TRACK violations + exit 0

bash scripts/check_motion_conformance.sh
# Expected: "OK: motion conformance clean." + exit 0

grep -c 'check-conformance' .github/workflows/ci.yml
# Expected: 2 (one hard step, one advisory step)
```
</verification>

<success_criteria>
- check-conformance.sh exits 0 (five hard gates clean on current codebase)
- check-conformance-advisory.sh created, exits 0, logs WARN for known TYPE/TRACK violations
- check_motion_conformance.sh THRASH_PATTERN extended with layout-property transitions, exits 0
- ci.yml credo_strict job includes both new steps in correct order with correct continue-on-error placement
- commit message: `ci(admin): wire + tighten design-system conformance gates`
</success_criteria>

<validation>
Dimension 8 proof commands from 94-VALIDATION.md:

| Requirement | Command | Expected result |
|-------------|---------|-----------------|
| RATCHET-03 (wire dead gate + hard arms) | `bash mailglass_admin/scripts/check-conformance.sh` | exit 0, "OK: design-system conformance clean." |
| RATCHET-03 (advisory typography/tracking arms) | `bash mailglass_admin/scripts/check-conformance-advisory.sh` | exit 0, WARN lines for TYPE/TRACK |
| RATCHET-03 (THRASH layout-transition arms) | `bash scripts/check_motion_conformance.sh` | exit 0, "OK: motion conformance clean." |
| RATCHET-03 (CI wiring) | `grep -c 'check-conformance' .github/workflows/ci.yml` | 2 |
</validation>

<output>
Create `.planning/phases/94-token-re-baseline-onto-canonical-brand/94-01-SUMMARY.md` when done.
</output>
