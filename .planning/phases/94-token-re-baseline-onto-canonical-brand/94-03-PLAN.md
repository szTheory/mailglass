---
phase: 94
plan: 03
type: execute
wave: 2
depends_on:
  - "94-01"
  - "94-02"
files_modified:
  - mailglass_admin/assets/css/app.css
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-05
  - TOKEN-04

must_haves:
  truths:
    - "app.css opens with @import \"../../../brandbook/tokens.css\" before @import tailwindcss"
    - "Every --color-* line in both daisyUI theme blocks references var(--mg-*) — no raw hex literals remain in theme selectors"
    - "Seven token-role corrections land: base-200 light→surface-raised, base-300 light→border, accent light→Glass, info light→info-solid, base-300 dark→border, secondary dark→text-muted, error dark→error-solid (plus secondary-content/accent/accent-content/neutral/neutral-content dark fixes)"
    - "All -content slots explicitly set (no daisyUI auto-derivation)"
    - "priv/static/app.css is rebuilt by mix mailglass_admin.assets.build and committed"
    - "mix verify.preview exits 0 (git diff --exit-code priv/static/ is clean)"
    - "token_parity_test.exs, brand_test.exs, and accessibility_test.exs all pass green"
    - "No HEEx component markup changed in this phase"
  artifacts:
    - path: "mailglass_admin/assets/css/app.css"
      provides: "@import of brandbook/tokens.css + full var(--mg-*) rewrite of both theme blocks"
      contains: "@import \"../../../brandbook/tokens.css\""
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt compiled bundle with --mg-* declarations inlined"
      contains: "--mg-color-background"
  key_links:
    - from: "mailglass_admin/assets/css/app.css @import"
      to: "brandbook/tokens.css"
      via: "../../../brandbook/tokens.css (3 levels up from assets/css/, build cwd = mailglass_admin/)"
    - from: "priv/static/app.css [data-theme=mailglass-light]"
      to: "--mg-color-*"
      via: "var(--mg-*) references resolved at browser render time"
    - from: "token_parity_test.exs"
      to: "priv/static/app.css + brandbook/tokens.json"
      via: "compiled-bundle seam + oracle compare"
---

<objective>
Perform the CSS re-baseline: add the cross-package @import, rewrite all daisyUI --color-* slots to var(--mg-*) in both themes, rebuild the compiled bundle, and commit. This commit turns the Wave 0 red tests (token_parity_test.exs, brand_test.exs) green and proves the full pipeline end-to-end.

Purpose: D-01 + D-03 + D-09 from CONTEXT.md. The @import is the single source of truth; the slot rewrite corrects seven documented token-role bugs; the bundle commit is the only adopter-visible artifact.

Output: mailglass_admin/assets/css/app.css (source, two theme blocks fully rewritten), mailglass_admin/priv/static/app.css (rebuilt compiled bundle, committed). No HEEx files touched. mix verify.preview clean.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md

<interfaces>
<!-- Full slot mapping — the authoritative rewrite table for app.css -->
<!-- Source: 94-RESEARCH.md §Full Per-Slot daisyUI Mapping Table (VERIFIED 2026-06-13) -->

mailglass-LIGHT theme — full --color-* rewrite:
  --color-base-100: var(--mg-color-background)          /* was #F8FBFD — no value change, role correct */
  --color-base-200: var(--mg-color-surface-raised)      /* was #EAF6FB (Mist) — CORRECTED to #FFFFFF */
  --color-base-300: var(--mg-color-border)              /* was #A6EAF2 (Ice) — CORRECTED to #C7DCE5 */
  --color-base-content: var(--mg-color-text)            /* was #0D1B2A — no value change */
  --color-primary: var(--mg-color-accent)               /* was #277B96 — no value change */
  --color-primary-content: var(--mg-color-background)   /* was #F8FBFD — no value change */
  --color-secondary: var(--mg-color-text-muted)         /* was #5C6B7A — no value change */
  --color-secondary-content: var(--mg-color-background) /* was #F8FBFD — no value change */
  --color-accent: var(--mg-color-accent)                /* was #0D1B2A (Ink!) — CORRECTED to Glass #277B96 */
  --color-accent-content: var(--mg-color-text-inverse)  /* was #F8FBFD — reclassified */
  --color-neutral: var(--mg-color-text-muted)           /* was #5C6B7A — no value change */
  --color-neutral-content: var(--mg-color-background)   /* was #F8FBFD — no value change */
  --color-info: var(--mg-color-info-solid)              /* was #277B96 (Glass) — CORRECTED to #1D637A */
  --color-info-content: var(--mg-color-text-inverse)    /* ADD (was absent) */
  --color-success: var(--mg-color-success-solid)        /* was #166534 — no value change */
  --color-success-content: var(--mg-color-success-on-solid) /* ADD (was absent) */
  --color-warning: var(--mg-color-warning-solid)        /* was #A95F10 — no value change */
  --color-warning-content: var(--mg-color-warning-on-solid) /* ADD (was absent) */
  --color-error: var(--mg-color-error-solid)            /* was #B42318 — no value change */
  --color-error-content: var(--mg-color-error-on-solid) /* ADD (was absent) */

NON-COLOR SLOTS light (keep as literal values — do NOT rewrite to var()):
  --radius-selector: 0.25rem
  --radius-field: 0.25rem
  --radius-box: 0.5rem
  --border: 1px
  --depth: 0
  --noise: 0

mailglass-DARK theme — full --color-* rewrite:
  --color-base-100: var(--mg-color-background)          /* was #0D1B2A — no value change */
  --color-base-200: var(--mg-color-surface-raised)      /* was #152538 — no value change */
  --color-base-300: var(--mg-color-border)              /* was #1F3049 (overlay!) — CORRECTED to #315069 */
  --color-base-content: var(--mg-color-text)            /* was #EAF6FB — no value change */
  --color-primary: var(--mg-color-accent)               /* was #A6EAF2 (Ice) — no value change */
  --color-primary-content: var(--mg-color-text-inverse) /* was #0D1B2A — already passes AA 12.98:1 */
  --color-secondary: var(--mg-color-text-muted)         /* was #5C6B7A (3.18:1!) — CORRECTED to #B8CAD4 */
  --color-secondary-content: var(--mg-color-text-inverse) /* was #F8FBFD — CORRECTED to #0D1B2A on dark fills */
  --color-accent: var(--mg-color-accent)                /* was #277B96 (Glass/light!) — CORRECTED to Ice #A6EAF2 */
  --color-accent-content: var(--mg-color-text-inverse)  /* was #F8FBFD — CORRECTED to #0D1B2A */
  --color-neutral: var(--mg-color-text-muted)           /* was #5C6B7A (3.18:1!) — CORRECTED to #B8CAD4 */
  --color-neutral-content: var(--mg-color-text-inverse) /* was #F8FBFD — CORRECTED to #0D1B2A */
  --color-info: var(--mg-color-info-solid)              /* was #A6EAF2 (Ice) — no value change for dark */
  --color-info-content: var(--mg-color-info-on-solid)   /* ADD (was absent) */
  --color-success: var(--mg-color-success-solid)        /* was #8BB77F — no value change */
  --color-success-content: var(--mg-color-success-on-solid) /* ADD (was absent) */
  --color-warning: var(--mg-color-warning-solid)        /* was #E0A955 — no value change */
  --color-warning-content: var(--mg-color-warning-on-solid) /* ADD (was absent) */
  --color-error: var(--mg-color-error-solid)            /* was #D47368 (OFF-PALETTE!) — CORRECTED to #E29089 */
  --color-error-content: var(--mg-color-error-on-solid) /* ADD (was absent) */

NON-COLOR SLOTS dark (keep as literal values):
  Same as light: --radius-selector, --radius-field, --radius-box, --border, --depth, --noise

@import position: Insert BEFORE @import "tailwindcss" (i.e., the very first line of app.css).
@import comment to include:
  /* Resolves to: <monorepo-root>/brandbook/tokens.css (build cwd = mailglass_admin/)    */
  /* Standalone Tailwind v4.1.12 binary inlines all --mg-* declarations at build time.   */
  /* Adopting apps consume only priv/static/app.css (bundle); brandbook/ excluded from   */
  /* Hex tarball (mix.exs :files). Editing brandbook/tokens.css requires a bundle rebuild.*/

build command (TOKEN-05, D-09): cd mailglass_admin && mix mailglass_admin.assets.build
post-build gate: cd mailglass_admin && mix verify.preview

Brand_test.exs note: its updated assertions (from Plan 02) assert var(--mg-*) wiring AND the resolved hex in the --mg-* inlined block. The inlined block comes from the @import. After rebuild, the compiled bundle will have both the [data-theme=*] blocks (with var refs) and the --mg-* declarations from tokens.css. The test will be green.

Token_parity_test.exs note: after rebuild, the @mapping assertions will compare the inlined --mg-* values against tokens.json oracle. No code changes needed to the test — the rebuilt bundle is what makes it green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mailglass_admin/assets/css/app.css with @import and full var(--mg-*) slot rewrite</name>
  <files>
    mailglass_admin/assets/css/app.css
  </files>
  <read_first>
    - mailglass_admin/assets/css/app.css (FULL file read — understand the complete current structure including @theme block, both daisyUI plugin blocks, and any other @import or directives present; identify exact current slot names and order to preserve non-color slots)
    - brandbook/tokens.css (FULL file read — verify all --mg-color-* token names are present that the daisyUI slots reference; confirm light/dark blocks exist; confirm the file is self-contained CSS)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-PATTERNS.md §app.css
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-CONTEXT.md D-01 + D-03 + D-09
  </read_first>
  <action>
    Edit mailglass_admin/assets/css/app.css. This is a surgical rewrite of the daisyUI theme blocks only — all other parts of the file (@theme block, heroicons-inline @plugin, @layer, @utility declarations) are preserved verbatim.

    Step 1 — add @import at the top of the file (before the existing @import "tailwindcss" line):
    Insert the four-line comment block then `@import "../../../brandbook/tokens.css";` as the first CSS declaration. The resolved path comment is mandatory per the interface block above.

    Step 2 — rewrite mailglass-light theme block:
    Inside the `@plugin "../vendor/daisyui-theme" { name: "mailglass-light"; ... }` block, replace EVERY existing `--color-*: <value>` line with `--color-*: var(--mg-color-*)` per the full mapping in the interfaces block above. Add the four new -content slots (info-content, success-content, warning-content, error-content) that are absent from the current file. Keep ALL non-color slots (--radius-selector, --radius-field, --radius-box, --border, --depth, --noise) as their existing literal values — do NOT convert them to var(). Keep all other daisyUI plugin header declarations (name, default, prefersdark, color-scheme) unchanged.

    Step 3 — rewrite mailglass-dark theme block:
    Same process for `@plugin "../vendor/daisyui-theme" { name: "mailglass-dark"; ... }`. Rewrite all --color-* to var(--mg-*) per the dark mapping above. Add the four absent -content slots. Keep non-color slots as literals.

    Critical correctness check: after the rewrite, scan the theme blocks to confirm NO `--color-*: #` hex literal remains. The only #-containing values permitted are inside comments.

    Do NOT modify the @theme block (which holds type/spacing/elevation/easing custom properties). Do NOT modify any @layer or @utility rule. Do NOT touch app.css if it contains any HEEx-adjacent content (it should not — this is pure CSS).
  </action>
  <verify>
    <automated>grep -n 'var(--mg-' /Users/jon/projects/mailglass/mailglass_admin/assets/css/app.css | head -10</automated>
    <automated>grep -n '@import.*brandbook' /Users/jon/projects/mailglass/mailglass_admin/assets/css/app.css</automated>
    <automated>grep -En '--color-[a-z-]+: #[0-9a-fA-F]' /Users/jon/projects/mailglass/mailglass_admin/assets/css/app.css | grep -v '^.*:.*\/\*'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@import.*brandbook' mailglass_admin/assets/css/app.css` returns a match
    - `grep -En '--color-[a-z-]+: #[0-9a-fA-F]' mailglass_admin/assets/css/app.css` returns 0 lines (no raw hex in --color-* assignments outside comments)
    - `grep -c 'var(--mg-color-' mailglass_admin/assets/css/app.css` returns ≥ 38 (20 light slots + ~20 dark slots including the newly-added -content slots)
    - The @import line reads exactly `@import "../../../brandbook/tokens.css";` (3 levels up, correct path)
  </acceptance_criteria>
  <done>Source app.css rewritten: @import added, all --color-* slots in both themes now reference var(--mg-*), no raw hex remains in theme selectors, non-color slots preserved as literals.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild compiled bundle, verify parity + brand + contrast tests green, commit</name>
  <files>
    mailglass_admin/priv/static/app.css
  </files>
  <read_first>
    - mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex (verify the task name and any options)
    - mailglass_admin/test/mailglass_admin/token_parity_test.exs (understand what the test now checks — ensure the @mapping keys match the mg token names from the inlined --mg-* block in the compiled CSS)
    - mailglass_admin/test/mailglass_admin/brand_test.exs (verify updated assertions from Plan 02 are present)
    - .planning/phases/94-token-re-baseline-onto-canonical-brand/94-RESEARCH.md §Common Pitfalls (Pitfall 3: @import not inlined = grep --mg-color-background on compiled bundle)
  </read_first>
  <action>
    Step 1 — rebuild the bundle: `cd mailglass_admin && mix mailglass_admin.assets.build`. This runs the standalone Tailwind v4.1.12 binary with --minify, inlining all @import-ed declarations (including the brandbook/tokens.css --mg-* block) into priv/static/app.css.

    Step 2 — verify the @import was inlined: `grep '--mg-color-background' mailglass_admin/priv/static/app.css`. If this returns empty, the @import path is wrong — recount the ../ levels in app.css (should be 3) and rebuild. Do NOT proceed until the --mg-* declarations appear in the compiled bundle.

    Step 3 — run the full test suite against the newly-rebuilt bundle: `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs test/mailglass_admin/brand_test.exs test/mailglass_admin/accessibility_test.exs`. All tests must be green. If token_parity_test.exs fails with a value mismatch, read the error message — it names exactly which slot and expected vs actual value. Cross-check the @mapping in token_parity_test.exs against the mg token name in the compiled bundle (the inlined --mg-* declarations must exactly match the names used in the @mapping).

    Step 4 — run the bundle-clean gate: `cd mailglass_admin && mix verify.preview`. This rebuilds AND runs `git diff --exit-code priv/static/`. If the diff is non-empty, the bundle was not committed — commit priv/static/app.css before this step (or ensure the built artifact is staged). verify.preview checks the committed state, so `git add mailglass_admin/priv/static/app.css` must precede the final verify.preview call.

    Step 5 — run the hard conformance gate: `bash mailglass_admin/scripts/check-conformance.sh`. Must exit 0. If it fails due to a HEX-GATE hit (a hex value somehow in a lib/*.ex file), investigate — this plan should not have touched any .ex files.

    Step 6 — run support contract lane: `cd mailglass_admin && mix verify.support_contract.admin`. This calls the parity test as a required check. Must exit 0.

    Commit ordering (D-10 step 3): this is one commit covering the app.css source rewrite + rebuilt priv/static/app.css bundle. If the Wave 0 tests from Plan 02 were committed separately as red tests, this commit turns them green. If they were not yet committed, bundle them here. Commit message: `feat(admin): re-baseline app.css onto brandbook tokens`.
  </action>
  <verify>
    <automated>grep -c '\-\-mg-color-background' /Users/jon/projects/mailglass/mailglass_admin/priv/static/app.css</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs test/mailglass_admin/brand_test.exs test/mailglass_admin/accessibility_test.exs 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.preview 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.support_contract.admin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '--mg-color-background' mailglass_admin/priv/static/app.css` returns ≥ 1 (confirms @import was inlined)
    - `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs test/mailglass_admin/brand_test.exs test/mailglass_admin/accessibility_test.exs` exits 0 — ALL tests green
    - `cd mailglass_admin && mix verify.preview` exits 0 — git diff --exit-code priv/static/ is clean (bundle committed, no drift)
    - `cd mailglass_admin && mix verify.support_contract.admin` exits 0
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0
    - Zero HEEx .ex files modified (confirm with `git diff --name-only HEAD` — only assets/css/app.css and priv/static/app.css should appear)
    - `git diff --name-only HEAD` does NOT show any file under mailglass_admin/lib/
  </acceptance_criteria>
  <done>priv/static/app.css rebuilt with --mg-* declarations inlined; all parity + brand + contrast tests green; mix verify.preview exits 0; mix verify.support_contract.admin exits 0; no HEEx markup changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @import "../../../brandbook/tokens.css" | Resolves only at MAINTAINER build time (mix mailglass_admin.assets.build); not an adopter-visible path |
| priv/static/app.css | Pre-resolved bundle committed to git; adopters consume only this file; brandbook/ excluded from Hex tarball (mix.exs :files) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | Tampering | cross-package @import path traversal | accept | Path resolves only at maintainer build time; brandbook/ is in the same monorepo, not an external supply-chain artifact; adopters never see the @import or brandbook/; no path-traversal exposure to consumers |
| T-94-08 | Tampering | priv/static/app.css (committed bundle) | mitigate | verify.preview gate enforces git diff --exit-code priv/static/ — stale or hand-edited bundle breaks CI; parity test reads the committed artifact so stale bundle is self-reporting |
| T-94-09 | Spoofing | daisyUI var(--mg-*) resolution at browser render time | accept | Custom property resolution is standard CSS cascade; no eval/exec; tokens are color values only (no PII, no secrets) |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; Tailwind binary pinned at v4.1.12 in config.exs; Jason is an existing test dep |

**Assessment: LOW threat surface.** The cross-package @import is the one notable architectural decision — assessed as no supply-chain risk because brandbook/ is in the same git repo at maintainer scope, not an external dependency. Adopters receive only the pre-resolved priv/static/app.css in the Hex tarball. The threat is acknowledged and accepted with the reasoning from the security_threat_model block in the planning_context.
</threat_model>

<verification>
Final phase-level verification (all three plans complete):

```
# Gates
bash mailglass_admin/scripts/check-conformance.sh
# exit 0, "OK: design-system conformance clean."

bash mailglass_admin/scripts/check-conformance-advisory.sh
# exit 0, WARN lines for TYPE/TRACK violations (advisory only)

bash scripts/check_motion_conformance.sh
# exit 0, "OK: motion conformance clean."

# Tests
cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs test/mailglass_admin/brand_test.exs test/mailglass_admin/accessibility_test.exs
# exit 0, all passing

# Bundle-clean + full suite
cd mailglass_admin && mix verify.preview
# exit 0 (includes git diff --exit-code priv/static/ clean)

# Required CI lane
cd mailglass_admin && mix verify.support_contract.admin
# exit 0 (token_parity_test.exs now in this alias)

# No markup changed
git diff --name-only HEAD~3..HEAD | grep -v 'mailglass_admin/assets/css/app.css\|mailglass_admin/priv/static/app.css\|mailglass_admin/test/\|mailglass_admin/scripts/\|mailglass_admin/mix.exs\|scripts/check_motion_conformance.sh\|.github/workflows/ci.yml'
# no matches (only expected files changed)
```
</verification>

<success_criteria>
- mailglass_admin/assets/css/app.css opens with @import "../../../brandbook/tokens.css" (3 levels up)
- Both daisyUI theme blocks contain only var(--mg-*) references on --color-* lines; no raw hex
- Seven documented token-role corrections applied (base-200 light, base-300 light, accent light, info light, base-300 dark, secondary dark, error dark) plus secondary-content/accent/accent-content/neutral/neutral-content dark
- All -content slots explicitly set in both themes
- priv/static/app.css rebuilt and committed; mix verify.preview exits 0
- token_parity_test.exs, brand_test.exs, accessibility_test.exs all exit 0
- mix verify.support_contract.admin exits 0
- Zero .ex files in mailglass_admin/lib/ modified
- commit message: `feat(admin): re-baseline app.css onto brandbook tokens`
</success_criteria>

<validation>
Dimension 8 proof commands from 94-VALIDATION.md:

| Requirement | Command | Expected result |
|-------------|---------|-----------------|
| TOKEN-01 (no raw hex, --mg-* inlined) | `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs` | exit 0 (structural assertion green) |
| TOKEN-02 (light/dark slot remap) | `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs test/mailglass_admin/brand_test.exs` | exit 0 |
| TOKEN-03 (dark AA + border sub-3:1) | `cd mailglass_admin && mix test test/mailglass_admin/accessibility_test.exs` | exit 0 |
| TOKEN-04 (parity assertions) | `cd mailglass_admin && mix verify.support_contract.admin` | exit 0 |
| TOKEN-05 (bundle rebuilt + committed) | `cd mailglass_admin && mix verify.preview` | exit 0, git diff --exit-code priv/static/ clean |
| SC-4 (zero markup changes) | `git diff --name-only HEAD \| grep 'mailglass_admin/lib'` | no output |
</validation>

<output>
Create `.planning/phases/94-token-re-baseline-onto-canonical-brand/94-03-SUMMARY.md` when done.
</output>
