---
status: complete
phase: 167-truthful-documentation-and-the-standing-control
source:
  - 167-01-SUMMARY.md
  - 167-02-SUMMARY.md
  - 167-03-SUMMARY.md
  - 167-04-SUMMARY.md
started: 2026-09-18
updated: 2026-09-18
---

## Current Test

[testing complete]

## Tests

### 1. DOCS-03 — MAINTAINING.md exclude-paths count is generated, not proofread
expected: The prose count of `exclude-paths` entries is derived from release-please-config.json at test time, so a config edit that adds or removes an entry fails the test rather than silently making the doc false.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `MAINTAINING.md contract exclude-paths count in prose is derived from release-please-config.json`. Ran locally 2026-09-18 (`mix test test/mailglass/docs_contract_test.exs --seed 0`), 0 failures."
coverage_id: 167-01-D1

### 2. DOCS-03 — MAINTAINING.md no longer claims the publish fan-out is hands-free
expected: Corrected text names `required_reviewers` and the three approval stops on a linked core+admin+inbound release.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `MAINTAINING.md contract publish fan-out is described as gated, not hands-free`. 0 failures."
coverage_id: 167-01-D2

### 3. DOCS-03 — CONTRIBUTING.md drops the mandatory fix(inbound) floor-bump instruction
expected: Guidance describes the current bare `~> 2.0` sibling constraint; the unrelated `fix(inbound):` commit example further down the file survives untouched.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `CONTRIBUTING.md contract sibling pin guidance describes the bare ~> constraint, not a floor bump`. 0 failures."
coverage_id: 167-01-D3

### 4. DOCS-03 — MAINTAINING.md Release Close-Out runbook exists and every script it names is real
expected: A `## Release Close-Out` section names the five ledger states and both close-out script paths, and every `scripts/*.sh|.exs` path it tells the reader to run is proven to exist on disk.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `MAINTAINING.md contract close-out runbook names the ledger states and every script it tells the reader to run`. 0 failures."
coverage_id: 167-01-D4

### 5. DOCS-02 — CLAUDE.md no longer instructs an `==` exact sibling pin
expected: The Engineering DNA bullet names `~>` and the linked-versions plugin. The assertion is scoped to that bullet so an unrelated pre-existing `~>` elsewhere in the file cannot satisfy it vacuously.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `CLAUDE.md contract sibling pin guidance matches the ~> convention the sibling mix files carry`. 0 failures."
coverage_id: 167-02-D1

### 6. DOCS-02 — CLAUDE.md no longer claims the release PR auto-merges on green
expected: The corrected bullet names the disarmed auto-merge plus the protected candidate-digest dispatch, with the `required_reviewers` / three-approvals language intact.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `CLAUDE.md contract release PR merge path is described as disarmed plus protected dispatch`. 0 failures."
coverage_id: 167-02-D2

### 7. DOCS-06 — every upgrade guide on disk is linked from README.md
expected: README links guides/upgrading-to-v2_0.md, and the assertion generalizes over every `guides/upgrading-*.md` on disk so a future guide cannot go unlinked.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `README.md contract every upgrade guide on disk is linked from README.md`. Generalized over the glob, not a literal. 0 failures."
coverage_id: 167-02-D3

### 8. DOCS-06 — CHANGELOG 2.0.0 names the schema-isolation breaking change
expected: The 2.0.0 section names the PostgreSQL schema-isolation move (Phases 132-137), appended after — not replacing — the release-please-generated bullet.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `CHANGELOG.md contract 2.0.0 changelog entry names the schema-isolation breaking change`. 0 failures."
coverage_id: 167-02-D4

### 9. DOCS-04 — migration guide's sibling pins are derived, not hardcoded
expected: The guide's `~> X.Y` pins for mailglass and mailglass_admin equal `package_major_minor!/1` read from each mix.exs at test time, with the stale `~> 0.3` and `~> 1.6` pins refuted.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs:534 `Guide contracts migration-from-swoosh opens with the value-prop pitch before subordinate framing` — the derived-pin assertions live in this test body (lines 565-587), comparing dependency_constraint!/3 against package_major_minor!(\"mix.exs\") and (\"mailglass_admin/mix.exs\"). Verified the assertions exist, not just the test name. 0 failures."
coverage_id: 167-03-T1

### 10. DOCS-04 — the migration guide's pin is re-synced at release time
expected: release-please.yml's existing pin-resync sed loop covers guides/migration-from-swoosh.md and the file is listed in SYNC_PATHS, so the derived pin above can never go stale against a new release.
result: pass
source: self-verified
evidence: "`grep -c 'guides/migration-from-swoosh.md' .github/workflows/release-please.yml` = 2 — line 441 (the `for readme in README.md mailglass_admin/README.md guides/migration-from-swoosh.md` sed loop) and line 483 (SYNC_PATHS). `python3 yaml.safe_load` parses the workflow cleanly. Run locally 2026-09-18."
coverage_id: 167-03-T2

### 11. DOCS-04 — compatibility guide no longer claims an exact sibling-version pin
expected: compatibility-and-deprecations.md describes the `~>` constraint rather than an exact pin.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs:733 — `Guide contracts compatibility guide no longer claims an exact sibling-version pin`. 0 failures."
coverage_id: 167-03-T3

### 12. DOCS-05 — `css_inliner: :none` is rejected at validation
expected: `Mailglass.Config.new!/1` rejects `css_inliner: :none` with a message naming `:premailex`; `:premailex` acceptance and defaulting are unchanged. (The one product-code change v2.8 permits.)
result: pass
source: automated
evidence: "test/mailglass/config_test.exs — `new!/1 rejects css_inliner: :none — the key was accepted but never read`. 0 failures. The key was previously accepted and silently ignored — a claim the config made about itself that was not true."
coverage_id: 167-03-T4

### 13. DOCS-05 — outbound.ex moduledoc no longer claims orphan-queued reconciliation
expected: The moduledoc stops claiming the Reconciler resolves orphan `:queued` Delivery rows and states the gap explicitly instead.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `docs/api_stability.md contract outbound moduledoc does not claim queued-delivery reconciliation`. 0 failures."
coverage_id: 167-03-T5

### 14. DOCS-05 — api_stability.md's injected-forms list matches mailable.ex verbatim
expected: Items 5/8/12 of the `__using__` injected forms list are pinned against the mailable.ex source rather than transcribed by hand.
result: pass
source: automated
evidence: "test/mailglass/docs_contract_test.exs — `docs/api_stability.md contract injected __using__ forms list matches mailable.ex`. 0 failures."
coverage_id: 167-03-T6

### 15. DOCS-01 — check_state_md_pr_refs.sh gives a three-way live verdict, all three exit paths observed
expected: Every `#NNN` reference in STATE.md is audited against live GitHub, with pass (0), blocked (1) and cannot_check (2) each observably distinct per D-34.
result: pass
source: self-verified
evidence: |
  Run locally 2026-09-18, all three exit paths exercised:
  - exit 0: `bash scripts/check_state_md_pr_refs.sh` → `pass: 7 check(s)` — #222 (CLOSED), #129 (CLOSED), #257/#266/#267/#260/#263 (MERGED), each consistent with STATE.md's prose.
  - exit 1: fixture asserting `PR #222 is still open` → `blocked #222: #222 is described as open in STATE.md but GitHub reports CLOSED.`
  - exit 2: same fixture run under a PATH symlink farm built from the real PATH minus `gh` → `cannot-check #222: gh is unavailable or unauthenticated; #222 was not checked.`
  `--format json` also emits a well-formed verdict document.
coverage_id: 167-04-T1

### 16. DOCS-01 — STATE.md states only permanent facts and delegates ongoing state
expected: The prohibition on hardcoding an open-PR/open-issue snapshot is pinned offline in the required core lane, so it cannot regress silently.
result: pass
source: automated
evidence: "test/mailglass/state_md_contract_test.exs — 5 tests, run via the core lane. Confirmed the file carries exactly 5 tests and all pass. 0 failures."
coverage_id: 167-04-T2

### 17. STAND-01 — the three mix Dependabot entries are grouped, capped and weekly
expected: All three mix ecosystems group minor+patch updates, carry an open-PR cap, and run weekly — while majors and security advisories stay individually reviewable (never swept into the group).
result: pass
source: self-verified
evidence: |
  `python3 yaml.safe_load` structural assertion against .github/dependabot.yml, run locally 2026-09-18:
  - 3 mix entries — `/`, `/mailglass_admin`, `/mailglass_inbound`
  - each: interval=weekly, open-pull-requests-limit=3, groups=['mix-minor-patch'], update-types=['minor','patch']
  - `grep -c major` = 0 and `grep -c security-updates` = 0 — majors and advisories are excluded from the group by construction, so they still open as individually reviewable PRs.
coverage_id: 167-04-T3

## Summary

total: 17
passed: 17
issues: 0
pending: 0
blocked: 0
skipped: 0

## Gaps

[none]

## Notes

**Full-suite regression check.** Beyond the cited per-test refs, the complete core
suite was run locally at HEAD with the floor enforced:
`MAILGLASS_SUITE_FLOOR=1 mix test --seed 0` → **23 properties, 2182 tests, 0 failures**,
`scope: FULL SUITE (MAILGLASS_SUITE_FLOOR=1) — executed floor 1576, skipped ceiling 7
enforced`, `signature tally: already_shared=0, formatter_violations=0`. This matches the
measured zero-failure baseline; no Phase 167 regression is hiding behind a scoped lane.

**Three SUMMARY coverage entries carry an invalid `kind`.** `167-03-T2`, `167-04-T1` and
`167-04-T3` declare `verification[0].kind: manual`, which is not in the accepted enum
(`unit, integration, e2e, automated_ui, manual_procedural, other`). `uat.classify-coverage`
therefore refused to auto-pass them and routed all three to human checkpoints with
`reason: validation_failed`. This is a metadata typo in the execution record, not a defect
in the delivered work — each of the three was verified directly in this session (tests 10,
15, 17 above) and passes on its stated evidence. Filed rather than fixed, per the v2.8 rule
that anything outside the 17 requirements is filed, not fixed. The intended value is
`manual_procedural`.
