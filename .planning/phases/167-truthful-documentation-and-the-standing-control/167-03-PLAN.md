---
phase: 167-truthful-documentation-and-the-standing-control
plan: 03
type: execute
wave: 3
depends_on: ["167-02"]
files_modified:
  - guides/migration-from-swoosh.md
  - guides/compatibility-and-deprecations.md
  - .github/workflows/release-please.yml
  - test/mailglass/docs_contract_test.exs
  - lib/mailglass/config.ex
  - test/mailglass/config_test.exs
  - lib/mailglass/outbound.ex
  - docs/api_stability.md
autonomous: true
requirements: [DOCS-04, DOCS-05]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`test/mailglass/docs_contract_test.exs` contains no hardcoded version literal for `guides/migration-from-swoosh.md`; the expected pin is derived at test time from `package_major_minor!(\"mix.exs\")`."
    - "When the guide's `{:mailglass, \"~> X.Y\"}` equals `package_major_minor!(\"mix.exs\")` the test passes; when they differ by one minor it fails."
    - "If the migration guide's `{:mailglass, \"~> X.Y\"}` dependency block is missing entirely, the derived-constraint extraction flunks with a named message rather than silently passing."
    - "`.github/workflows/release-please.yml`'s pin-resync step rewrites `guides/migration-from-swoosh.md` and includes it in `SYNC_PATHS`, so the guide's pin is generated at release time rather than hand-edited."
    - "`guides/compatibility-and-deprecations.md` no longer claims published builds pin the exact sibling version; it states the current `~>` major.minor reality."
    - "`Mailglass.Config.new!(renderer: [css_inliner: :none])` raises `NimbleOptions.ValidationError`, and no `lib/`, `test/`, `reference/`, `guides/`, or README path outside `lib/mailglass/config.ex` and `test/mailglass/config_test.exs` references `css_inliner`."
    - "`lib/mailglass/outbound.ex`'s moduledoc states that orphan `:queued` Delivery rows are NOT currently auto-reconciled and that `Mailglass.Events.Reconciler` resolves a distinct failure class (orphan webhook events)."
    - "`docs/api_stability.md`'s injected-forms list matches `lib/mailglass/mailable.ex`'s actual `__using__/1`: the `Mailglass.Message` import, `new/0` and `new/1`, and the five-name `defoverridable`."
    - statement: "If the release-please pin resync is interrupted after editing the guide but before committing, the next proposal run re-derives and re-applies the same pin — the sed step is idempotent because it rewrites to a value derived from the manifest, not by incrementing."
      verification: backstop
  artifacts:
    - guides/migration-from-swoosh.md
    - guides/compatibility-and-deprecations.md
    - .github/workflows/release-please.yml
    - test/mailglass/docs_contract_test.exs
    - lib/mailglass/config.ex
    - test/mailglass/config_test.exs
    - lib/mailglass/outbound.ex
    - docs/api_stability.md
  key_links:
    - "`.release-please-manifest.json` `.[\".\"]` → `CORE_MM` → sed rewrite of `guides/migration-from-swoosh.md` → `SYNC_PATHS` commit"
    - "`mix.exs` `@version` → `package_major_minor!/1` → derived expected pin for the migration guide"
    - "`lib/mailglass/config.ex` `css_inliner` `{:in, [...]}` type → `NimbleOptions.ValidationError` → `test/mailglass/config_test.exs` assert_raise"
  prohibitions:
    - "No `lib/` file other than `lib/mailglass/config.ex` and `lib/mailglass/outbound.ex` is modified, and the `outbound.ex` change is moduledoc text only."
    - "`lib/mailglass/renderer.ex` is not modified — the `:none` key is rejected at validation, not honored."
    - "No existing sed rewrite, `SYNC_PATHS` entry, or guard in `.github/workflows/release-please.yml` is removed or loosened."
    - "No existing assertion in `test/mailglass/config_test.exs` or `test/mailglass/docs_contract_test.exs` is deleted or skipped; the `css_inliner: :none` acceptance case is inverted, not removed."
    - "No version literal is introduced into any docs-contract assertion for the migration guide."
---

<objective>
Dissolve the DOCS-04 two-file version lockstep permanently, and close the DOCS-05 gap between what the
code accepts/claims and what it does (DOCS-04, DOCS-05).

Purpose: `guides/migration-from-swoosh.md:31-32` and `docs_contract_test.exs:557-558` are the Phase 125
pin-drift shape — a hardcoded literal in two files that a human must remember to move together. Rather
than re-tightening that lockstep, this plan brings the guide under the release-time sed resync that
already generates the READMEs' pins, and converts the test to the dynamic helper pattern the README
contract test already uses. Separately, `css_inliner: :none` is accepted by validation and then never
read, and two docs describe behavior the code does not have.

Output: a generated guide pin, a dynamic test assertion, a narrowed NimbleOptions type with an
inverted unit test, and two corrected code-adjacent docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-PATTERNS.md
@.planning/phases/167-truthful-documentation-and-the-standing-control/167-02-SUMMARY.md
</context>

<execution_preamble>
Every `mix` invocation in this plan MUST be preceded in the same shell by:

export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27

Do not modify `.tool-versions`.
</execution_preamble>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dissolve the migration-guide version lockstep — release-time sed sync plus a dynamic test assertion, in ONE commit</name>
  <files>guides/migration-from-swoosh.md, guides/compatibility-and-deprecations.md, .github/workflows/release-please.yml, test/mailglass/docs_contract_test.exs</files>
  <precondition>`.release-please-manifest.json` contains a `"."` key holding the linked core version; the sed resync step reads it directly with no fallback chain.</precondition>
  <read_first>
    - guides/migration-from-swoosh.md (lines 25-45 — the `def deps do` block carrying `{:mailglass, "~> 2.5"}` and `{:mailglass_admin, "~> 2.5"}`)
    - test/mailglass/docs_contract_test.exs (lines 516-560 — the `migration-from-swoosh opens with the value-prop pitch` test containing the hardcoded assertion at ~:557-558; lines 14-26 — the README contract test showing the dynamic comparison to imitate; lines 1049-1074 — `package_major_minor!/1`, `current_compatibility_section!/2`, `dependency_constraint!/3`)
    - .github/workflows/release-please.yml (lines 395-500 in full — `CORE_VERSION`/`CORE_MM` derivation, the `for readme in README.md mailglass_admin/README.md` sed loop, the standalone inbound seds, and the `SYNC_PATHS=(...)` array and its `git diff --quiet` / `git add` usage)
    - guides/compatibility-and-deprecations.md (lines 200-218 — the `published builds pin the exact sibling version` bullet at ~:209-210)
    - mailglass_admin/mix.exs (~:151) and mailglass_inbound/mix.exs (~:149) — the actual `{:mailglass, "~> 2.0"}` declarations that falsify the "exact" claim
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (§ "DOCS-04 — the landmine, decisive disposition" — disposition (b) is locked; and Landmine 1 requiring both halves in the same commit)
  </read_first>
  <behavior>
    - Test 1: the migration guide's `{:mailglass, "~> X.Y"}` constraint equals `package_major_minor!("mix.exs")`, computed at test time.
    - Test 2: the migration guide's `{:mailglass_admin, "~> X.Y"}` constraint equals `package_major_minor!("mailglass_admin/mix.exs")`.
    - Test 3 (empty input): if the guide has no `{:mailglass, "~> X.Y"}` constraint at all, the extraction flunks with a message naming the guide path — it must not pass vacuously.
    - Test 4 (adjacency): a guide pinned to one minor below the manifest value fails; a guide pinned to exactly the manifest value passes.
  </behavior>
  <action>
<!-- planner-discipline-allow: exact sibling version -->
This task lands **three coupled changes in a single commit**. Splitting them across commits leaves
the guide un-generated while the test expects dynamic behavior that does not exist yet — that is the
deterministic red 167-RESEARCH.md Landmine 1 warns about. Do not split.

**Change A — the test becomes dynamic.** In `test/mailglass/docs_contract_test.exs`, inside the
existing `migration-from-swoosh opens with the value-prop pitch before subordinate framing` test,
delete the two hardcoded lines asserting a `~>` two-dot literal against the guide and replace them
with a derived comparison. Keep the two existing `refute` guards for the older stale pins (`~> 0.3`
and the 1.6 regex) untouched — those are historical drift-guards for versions that can never become
current again, and removing them would weaken a gate.

The migration guide does NOT have a `## Current package compatibility` heading, so
`current_compatibility_section!/2` does not apply. Reuse `dependency_constraint!/3` directly against
the guide's full body — its regex anchors on `\{:#{dependency},\s*"~>\s*(\d+\.\d+)` and flunks with a
named message when absent, which satisfies the empty-input behavior. Assert
`dependency_constraint!(migration, "mailglass", "guides/migration-from-swoosh.md") ==
package_major_minor!("mix.exs")` and the analogous `mailglass_admin` comparison against
`package_major_minor!("mailglass_admin/mix.exs")`. Introduce no new helper — these three already
exist at the bottom of the file.

Because the guide currently reads `~> 2.5` while `mix.exs` `@version` is on the 2.6 line, this
assertion will be RED until Change B's manual catch-up edit. Run it and capture the RED output as
evidence that the assertion is live.

**Change B — bring the guide's current text to the derived value.** Update the `def deps do` block in
`guides/migration-from-swoosh.md` so both `{:mailglass, "~> X.Y"}` and
`{:mailglass_admin, "~> X.Y"}` carry the current `package_major_minor!("mix.exs")` value. Read the
value from `mix.exs`'s `@version` — do not copy a version number out of any planning document. This
is the last time this edit is made by hand; Change C makes it generated.

**Change C — bring the guide under the release-time resync.** In
`.github/workflows/release-please.yml`, extend the existing pin-resync step (the one deriving
`CORE_MM` from `.release-please-manifest.json`):
- Add `guides/migration-from-swoosh.md` to the `for readme in README.md mailglass_admin/README.md`
  loop so both the `{:mailglass, "~> X.Y"}` and `{:mailglass_admin, "~> X.Y"}` seds apply to it. The
  existing sed regexes are already generic enough to match the guide's dep block verbatim — do not
  write a new regex.
- Add `guides/migration-from-swoosh.md` to the `SYNC_PATHS=(...)` array so the `git diff --quiet`
  check, the diff echo, and the `git add` all include it.
Change nothing else in that step. Do not touch the inbound-specific seds, the `INBOUND_CHANGED`
logic, the publish-summary `jq`, the `RELEASE_PLEASE_PAT` identity, or any guard.

**Change D — the unrelated compatibility-guide correction.** In
`guides/compatibility-and-deprecations.md` at ~:209-210, the bullet claiming published builds pin the
exact sibling version is false: both sibling `mix.exs` files declare a bare `{:mailglass, "~> 2.0"}`.
Rewrite that bullet to state that published builds pin the sibling's current major.minor with a
pessimistic `~>` constraint. Pin it with a scoped `refute` on the stale phrase plus a positive
assertion that the bullet now contains `~>` — add both to the existing compatibility-guide coverage
in `docs_contract_test.exs`. Negative-plus-positive is chosen here because the defect is one wrong
adjective and a bare refute would pass if the bullet were simply deleted.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*#' test/mailglass/docs_contract_test.exs | grep -c '2\.5'` returns `0` for any assertion targeting the migration guide — confirm by inspecting each remaining hit.
    - `grep -c 'dependency_constraint!(migration' test/mailglass/docs_contract_test.exs` returns at least `1`.
    - `grep -c 'guides/migration-from-swoosh.md' .github/workflows/release-please.yml` returns at least `2` — once in the sed loop, once in `SYNC_PATHS`.
    - `grep -c 'exact sibling version' guides/compatibility-and-deprecations.md` returns `0`.
    - The migration guide's `{:mailglass, "~> X.Y"}` value equals the output of `grep -oE '@version "[0-9]+\.[0-9]+' mix.exs | cut -d'"' -f2`.
    - `mix test test/mailglass/docs_contract_test.exs --warnings-as-errors` exits 0.
    - Adjacency mutation proof, run and discarded: edit the guide's `{:mailglass, "~> X.Y"}` down by one minor, re-run, confirm RED; delete the whole `def deps` block, re-run, confirm RED with the guide path named (not a vacuous pass); then restore. Both observed RED outputs are recorded in the commit body.
    - `git log -1 --name-only` shows `guides/migration-from-swoosh.md`, `.github/workflows/release-please.yml`, and `test/mailglass/docs_contract_test.exs` in the SAME commit.
    - `yq '.' .github/workflows/release-please.yml > /dev/null` (or `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release-please.yml'))"`) exits 0, proving the workflow still parses.
  </acceptance_criteria>
  <reversibility rating="costly">The sed extension executes only during a real release ceremony, so a defect in it is not observable until the next proposal is cut, and a bad rewrite would land inside a bot-authored release commit. Mitigated by reusing the byte-identical regex already proven at four existing call sites and by adding the path to `SYNC_PATHS` so the diff is echoed to the run log before commit. Not rated one-way: reverting is a two-line workflow edit, and the guide's text is recoverable from git.</reversibility>
  <done>The migration guide's pin is generated at release time and asserted dynamically at test time, so it can neither drift nor require a human to remember a two-file lockstep again — and the guide/test/workflow halves landed in one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject css_inliner: :none at validation and invert the acceptance test</name>
  <files>lib/mailglass/config.ex, test/mailglass/config_test.exs</files>
  <precondition>`grep -rn "css_inliner" lib/ test/ reference/ guides/ docs/ README.md mailglass_admin/ mailglass_inbound/` returns hits ONLY in `lib/mailglass/config.ex` and `test/mailglass/config_test.exs`; if any other path appears, halt and report before changing the schema.</precondition>
  <read_first>
    - lib/mailglass/config.ex (lines 75-100 — the `renderer:` subtree and the `css_inliner` key at ~:84-88; note the convention that the schema is declared BEFORE `@moduledoc` so `NimbleOptions.docs(@schema)` can interpolate)
    - lib/mailglass/renderer.ex (lines 60-90 and 230-245 — confirm `inline_css/1` unconditionally calls `Premailex.to_inline_css/1` and that `render/2` never threads renderer config into it; this file is NOT to be modified)
    - test/mailglass/config_test.exs (lines 10-40 — the `accepts valid opts unchanged and fills defaults` case at ~:17-21 and the adjacent `:invalid_backend` `assert_raise` case at ~:30-34 whose shape is the target)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (§ "DOCS-05 — decisive dispositions" (i): REJECT is locked, with the three-part rationale; and Landmine 4 requiring a re-grep immediately before landing)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-PATTERNS.md (the before/after pair for `config_test.exs`)
  </read_first>
  <behavior>
    - Test 1: `Mailglass.Config.new!(renderer: [css_inliner: :none])` raises `NimbleOptions.ValidationError`.
    - Test 2: `Mailglass.Config.new!(renderer: [css_inliner: :premailex, plaintext: false])` still succeeds and still fills defaults — the surviving half of the original acceptance case.
    - Test 3: the default remains `:premailex` when `renderer:` is omitted entirely.
    - Test 4: the raised message names `:premailex` as the accepted value, so an operator who set `:none` learns what to set instead.
  </behavior>
  <action>
Re-run the precondition grep first. 167-RESEARCH.md Landmine 4 requires confirming the key is still
dead surface immediately before the change, because Phase 166 executed after that grep was taken. If
any new reference exists outside the two named files, halt and report rather than proceeding.

**`lib/mailglass/config.ex`.** Narrow the `css_inliner` key's `type:` from the two-element
`{:in, [...]}` set to accept only `:premailex`. Leave `default: :premailex` unchanged. Update the
key's `doc:` string so the generated `NimbleOptions.docs(@schema)` output no longer implies a
disable-able backend — state that Premailex is the only supported inlining backend. Keep the schema
declared before `@moduledoc`, per the file's existing convention. This is the milestone's ONLY
permitted `lib/` behavior change; do not touch any other key, any other module, or
`lib/mailglass/renderer.ex`.

**`test/mailglass/config_test.exs`.** Two edits:
1. In the existing `accepts valid opts unchanged and fills defaults` test, replace the now-invalid
   `css_inliner: :none` in the valid-opts payload with `:premailex` and update the corresponding
   assertion. Do not delete the test — the `plaintext: false` half is still meaningful coverage.
2. Add a new test immediately adjacent to the `:invalid_backend` case, named
   `rejects css_inliner: :none — the key was accepted but never read`. Use `assert_raise
   NimbleOptions.ValidationError, fn -> Mailglass.Config.new!(renderer: [css_inliner: :none]) end`,
   mirroring the adjacent case byte-for-byte in shape. Capture the raised exception and assert its
   `message` contains `:premailex`, so the error meets the project's error-contract and brand/voice
   requirement that an error names the correct value rather than only rejecting the wrong one.

Record in the SUMMARY: this is a breaking change for any adopter who set `css_inliner: :none`. Blast
radius was measured as zero — the key appears in no adopter-facing guide, README, CHANGELOG entry, or
reference app — so no adopter was ever told it worked. Note that it is nonetheless a boot-time raise
for anyone who discovered it by reading the schema docs, and that the raise names `:premailex` so the
fix is one word.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/config_test.exs test/mailglass/renderer_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':premailex, :none' lib/mailglass/config.ex` returns `0`.
    - `grep -rn 'css_inliner' lib/ test/ reference/ guides/ docs/ README.md mailglass_admin/ mailglass_inbound/ | grep -vc 'lib/mailglass/config.ex\|test/mailglass/config_test.exs'` returns `0`.
    - `grep -c 'assert_raise NimbleOptions.ValidationError' test/mailglass/config_test.exs` returns strictly more than the pre-task count.
    - `git diff lib/` shows exactly one file changed in this task: `lib/mailglass/config.ex`.
    - `git diff lib/mailglass/renderer.ex` is empty.
    - `mix test test/mailglass/config_test.exs --warnings-as-errors` exits 0.
    - The lane that runs `config_test.exs` is named in the SUMMARY, confirmed by `grep -n 'core_deterministic_suite\|Core Full Suite' .github/workflows/ci.yml` — a pin that no lane executes is not a pin.
  </acceptance_criteria>
  <reversibility rating="costly">Re-adding `:none` to the `{:in, [...]}` set is a one-line revert, but once published this is a boot-time raise for any adopter who set the key. Blast radius was measured as zero across all adopter-facing surfaces, and both REQUIREMENTS.md and 167-RESEARCH.md pre-authorize the reject disposition, so no `checkpoint:decision` is inserted — the decision is already locked, not open.</reversibility>
  <done>No accepted config key is inert: `css_inliner: :none` now raises at validation with a message naming the supported value, pinned by a unit test that mirrors the file's existing rejection idiom.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct the outbound.ex reconciliation claim and the api_stability.md injected-forms list</name>
  <files>lib/mailglass/outbound.ex, docs/api_stability.md, test/mailglass/docs_contract_test.exs</files>
  <read_first>
    - lib/mailglass/outbound.ex (lines 30-60 — the moduledoc passage naming `Mailglass.Events.Reconciler` and `age >= 5min` at ~:47-50)
    - lib/mailglass/events/reconciler.ex (lines 50-80 — `find_orphans/1` and its `needs_reconciliation == true and is_nil(delivery_id)` predicate, proving it queries Events, not Deliveries)
    - lib/mailglass/mailable.ex (lines 120-160 — the actual `__using__/1` body: the `import Mailglass.Message, only: [...]` list, `def new(assigns \\ [])`, and the `defoverridable` name list)
    - docs/api_stability.md (lines 1105-1130 — the numbered "What is injected" list, items 5, 8, and 12)
    - test/mailglass/mailable_test.exs (lines 74-118 — the `Code.string_to_quoted` structural-assertion idiom and the inline comment, which 167-RESEARCH.md Landmine 5 confirms is ALREADY CORRECT and must not be "fixed")
    - test/mailglass/docs_contract_test.exs (for placement of the new describe block)
    - .planning/phases/167-truthful-documentation-and-the-standing-control/167-RESEARCH.md (Defect Ledger rows DOCS-05 (ii) and (iii), including the recommended corrected outbound.ex wording and the verbatim mailable.ex quotes)
  </read_first>
  <behavior>
    - Test 1: `docs/api_stability.md` does not contain the non-existent import form naming `Swoosh.Email` with an `except:` option.
    - Test 2: `docs/api_stability.md` contains `import Mailglass.Message, only: [to: 2` — the form actually injected.
    - Test 3: `docs/api_stability.md`'s `defoverridable` item names `new: 1` alongside `new: 0`.
    - Test 4 (cross-check): the same strings asserted in the doc are also present in `lib/mailglass/mailable.ex`, so the doc is pinned to the source rather than to a second hand-maintained literal.
    - Test 5: `lib/mailglass/outbound.ex` does not claim orphan `:queued` Delivery rows are reconcilable via `Mailglass.Events.Reconciler`, and positively states they are not currently auto-reconciled.
  </behavior>
  <action>
<!-- planner-discipline-allow: import Swoosh.Email, except: [new: 0] -->
**`lib/mailglass/outbound.ex` moduledoc.** Replace the sentence claiming orphan `:queued` Delivery
rows between Multi#1 and the adapter call are reconcilable via `Mailglass.Events.Reconciler` with age
>= 5min. `Mailglass.Events.Reconciler.find_orphans/1` queries orphan webhook **Event** rows
(`needs_reconciliation == true and is_nil(delivery_id)`) — a different failure class entirely, and
nothing in the codebase reconciles a Delivery stuck at `:queued` with no matching event. The
correction must be precise about the gap rather than silently deleting the claim: state that
`Mailglass.Events.Reconciler` resolves orphan webhook events only, that a Delivery stuck at `:queued`
is not currently auto-reconciled, and that this class requires manual operator intervention today.
Keep the surrounding "Adapter-call-in-transaction is a hard no" rationale intact — that part is
accurate and load-bearing. This is a moduledoc-text edit only: change no function, no clause, no
guard.

**`docs/api_stability.md` injected-forms list.** Correct three of the twelve numbered items against
`lib/mailglass/mailable.ex:124-157`:
- item 5 — replace the Swoosh import form with the actual
  `import Mailglass.Message, only: [to: 2, from: 2, subject: 2, html_body: 2, text_body: 2, header: 3, attach: 2, put_tag: 2]`
- item 8 — replace `def new/0` with `def new(assigns \\ [])`, noting it defines both `new/0` and `new/1`
- item 12 — replace the `defoverridable` list with `defoverridable new: 0, new: 1, render: 3, deliver: 2, deliver_later: 2`
Transcribe all three from `mailable.ex` itself, not from any planning document. Do not change the
"exactly 12 top-level AST forms" count or the LINT-05 budget text — those remain correct.
Do not modify `test/mailglass/mailable_test.exs`; its inline comment is already accurate and is
independent corroboration, not a second defect.

**Pins.** Add a new `describe "docs/api_stability.md contract"` block to
`test/mailglass/docs_contract_test.exs` with one test
`injected __using__ forms list matches mailable.ex`. Read both `docs/api_stability.md` and
`lib/mailglass/mailable.ex`. For each of the three corrected forms, assert the substring is present
in BOTH files — the doc assertion alone would be a second hand-maintained literal, whereas asserting
the same string against the source makes the doc provably a description of the code. Add one `refute`
for the stale Swoosh import form in the doc. This negative-plus-source-cross-check pairing is chosen
over a full AST-to-prose derivation, which 167-RESEARCH.md scopes out of a docs-correctness phase.

Add a second test `outbound moduledoc does not claim queued-delivery reconciliation` in the same
block (or a sibling `describe "lib moduledoc contract"`): `refute` the stale
`reconcilable via` + `Mailglass.Events.Reconciler` pairing in `lib/mailglass/outbound.ex`, and
`assert` the moduledoc positively contains wording establishing the gap (assert on a stable phrase
the executor lands, e.g. `not currently auto-reconciled`) so deleting the passage cannot pass.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test test/mailglass/docs_contract_test.exs test/mailglass/mailable_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Swoosh.Email, except' docs/api_stability.md` returns `0`.
    - `grep -c 'import Mailglass.Message, only: \[to: 2' docs/api_stability.md` returns at least `1`, and the same grep against `lib/mailglass/mailable.ex` also returns at least `1`.
    - `grep -c 'defoverridable new: 0, new: 1, render: 3, deliver: 2, deliver_later: 2' docs/api_stability.md` returns `1`, and the same grep against `lib/mailglass/mailable.ex` returns `1`.
    - `grep -c 'not currently auto-reconciled' lib/mailglass/outbound.ex` returns at least `1`.
    - `git diff lib/mailglass/outbound.ex` contains no changed line outside the `@moduledoc` heredoc — verify by inspecting the hunk headers.
    - `git diff test/mailglass/mailable_test.exs` is empty.
    - `mix test test/mailglass/docs_contract_test.exs test/mailglass/mailable_test.exs --warnings-as-errors` exits 0.
    - Source-coupling mutation proof, run and discarded: change one imported name in `lib/mailglass/mailable.ex`'s `import Mailglass.Message, only: [...]` list, re-run, confirm RED, then restore. The observed RED output is recorded in the commit body.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation text in a moduledoc and a markdown list plus two tests; no runtime behavior changes and nothing published is affected beyond regenerated HexDocs prose.</reversibility>
  <done>No moduledoc or public API document in scope describes behavior absent from the code, and each corrected claim is asserted against the source file it describes rather than against a second hand-maintained copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release automation → repository contents | The release-please sed resync runs with `RELEASE_PLEASE_PAT` and commits rewritten files to the release branch. Widening its file set widens what an automated, un-reviewed-at-write-time step may modify. |
| adopter config → library validation | `Mailglass.Config.new!/1` is the boundary where adopter-supplied options are accepted or rejected. |
| public API doc → adopter code | `docs/api_stability.md` is the contract adopters pattern-match their expectations against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-10 | Tampering | release-please.yml pin-resync file set | high | mitigate | Adding a path to a bot-committed sed loop expands what release automation rewrites without human review at write time. Mitigated three ways: the sed regexes are reused byte-identically from four existing proven call sites rather than newly written; the path is added to `SYNC_PATHS` so the diff is echoed to the run log and included in `git diff --quiet` before any commit; and Task 1 requires a YAML-parse check plus a same-commit landing so the change is reviewable as one unit. No guard, identity, or existing path is removed. |
| T-167-11 | Denial of Service | `css_inliner` validation narrowing | medium | mitigate | Rejecting a previously-accepted key is a boot-time raise for any adopter who set it — effectively an availability event on their deploy. Blast radius measured as zero across `lib/`, `test/`, `reference/`, `guides/`, `docs/`, and all three READMEs, and Task 2 carries a precondition re-grep that halts if that changed. The raised message names `:premailex` so recovery is a one-word edit, satisfying the project's error-as-public-contract convention. |
| T-167-12 | Spoofing | `docs/api_stability.md` injected-forms list | medium | mitigate | An API-stability document naming an import that does not exist invites adopters to build against a surface that was never there. Task 3 pins each corrected form against `lib/mailglass/mailable.ex` itself, so the document cannot drift from the macro without the docs contract test going red. |
| T-167-13 | Repudiation | `lib/mailglass/outbound.ex` orphan-delivery claim | medium | mitigate | The current moduledoc asserts a recovery mechanism that does not exist, which could lead an operator to believe a stuck `:queued` Delivery will self-heal and take no action. The correction states the gap explicitly and names manual intervention as the current answer, rather than deleting the sentence and leaving silence. |
| T-167-14 | Information Disclosure | corrected docs | low | accept | No secret, credential path, token name, or internal endpoint is added by any correction in this plan. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; no dependency is added to any `mix.exs` and `mix.lock` is not modified. The Package Legitimacy Gate is not applicable. |
</threat_model>

<verification>
- `export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix verify.support_contract.core` exits 0.
- `export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && mix test --warnings-as-errors` (full core suite) exits 0 — this is the lane that actually executes `test/mailglass/config_test.exs`, which `verify.support_contract.core`'s explicit file list does not include.
- `git diff --name-only lib/` lists exactly `lib/mailglass/config.ex` and `lib/mailglass/outbound.ex`.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml'))"` exits 0.
- `git diff` contains no deleted `assert`/`refute`/`assert_raise` line and no added `@tag :skip`.
</verification>

<success_criteria>
- The migration-guide version claim is generated at release time and asserted dynamically — the two-file lockstep no longer exists in either direction.
- `css_inliner: :none` raises, with the raise pinned by a unit test in a lane that provably runs it.
- Both code-adjacent doc corrections are asserted against the source files they describe.
- Exactly two `lib/` files changed, one of them moduledoc-only.
</success_criteria>

<artifacts_this_plan_produces>
New symbols created by this plan:

- `describe "docs/api_stability.md contract"` — new describe block in `test/mailglass/docs_contract_test.exs`
  - test `injected __using__ forms list matches mailable.ex`
  - test `outbound moduledoc does not claim queued-delivery reconciliation`
- test `rejects css_inliner: :none — the key was accepted but never read` — new test in `test/mailglass/config_test.exs`
- `guides/migration-from-swoosh.md` — new entry in the `for readme in ...` sed loop and in the `SYNC_PATHS` array of `.github/workflows/release-please.yml` (no new step, job, or workflow file)

No new script, Mix task, workflow file, dependency, or helper function is produced by this plan; the
migration-guide assertion reuses the existing `package_major_minor!/1` and `dependency_constraint!/3`
helpers.
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/167-truthful-documentation-and-the-standing-control/167-03-SUMMARY.md` when done.
</output>
