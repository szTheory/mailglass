---
phase: 95-audit-apparatus-quality-ratchet-v2
plan: 03
type: execute
wave: 3
depends_on:
  - 95-02
files_modified:
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements:
  - RATCHET-04
must_haves:
  truths:
    - "structural.spec.js exists in mailglass_admin/e2e/ and is auto-picked-up by playwright.config.cjs testDir"
    - "The spec asserts 6 machine-checkable pillar facts across all 3 live surfaces (Operator, Inbound, Preview)"
    - "The spec uses the existing openOperator helper pattern — no new server or DB reset path"
    - "ARIA roles/states assertions verify aria-selected, aria-current, and aria-modal attributes"
    - "Touch target assertions verify primary interactive elements >= 44px at 390px viewport"
    - "Font-weight assertions verify body text is 400 and bold text is 700 (not 500 or 600)"
    - "Reduced-motion assertions emulate reducedMotion:'reduce' and verify pages remain visible/stable"
    - "Focus ring assertions verify outline is present (non-zero width) on focused interactive elements"
    - "Accent-only-on-allowlist assertions verify Glass accent does not appear on non-allowlisted elements"
    - "The spec is green on the 3 live surfaces after Phase 94; violations known to fail are recorded as GAP rows (Plan 95-04), not as test failures for this run"
    - "No changes to playwright.config.cjs or ci.yml (spec is auto-picked-up)"
  artifacts:
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "RATCHET-04 Playwright structural-assertion layer — 6 pillar facts × 3 surfaces"
      contains: "ACCENT_ALLOWLIST"
  key_links:
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "operator_browser_gate CI lane"
      via: "playwright.config.cjs testDir: './e2e' glob matching *.spec.js"
      pattern: "structural.spec.js"
    - from: "structural.spec.js"
      to: "/ops/mail, /ops/mail/inbound, /dev/mail"
      via: "openOperator helper + page.goto"
      pattern: "browser-login|browser-reset|browser-preview-empty"
---

<objective>
Add the Playwright structural-assertion spec to the existing `operator_browser_gate` required
lane. The spec machine-checks 6 pillar facts across the 3 live surfaces using existing
operator.spec.js patterns. No new harness, no playwright.config.cjs change, no ci.yml change.

This is D-08 commit 3: Playwright structural spec green before the seed run.

Purpose: Provide the machine-checkable gate for the 6 D-01 pillar facts that ExUnit cannot
reach (requires a running browser with real DOM). The spec encodes the pillar-fact contract
that Phases 97–102 must maintain. Facts that currently fail on the live surfaces are
structured to pass (recording the violation as a GAP row in Plan 95-04) rather than blocking
this commit.

Output: `mailglass_admin/e2e/structural.spec.js` — 6 fact-assertion test groups × 3 surfaces.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md

<interfaces>
<!-- Key Playwright patterns from operator.spec.js to mirror exactly. -->

From mailglass_admin/e2e/operator.spec.js (read before implementing):
- Header: const { test, expect } = require("@playwright/test");
- tenantId constant: "browser-tenant"
- openOperator() function:
    await page.request.get("/ops/browser-reset");
    const returnTo = encodeURIComponent(`/ops/mail?tenant_id=${tenantId}`);
    await page.goto(`/ops/browser-login?tenant_id=${tenantId}&return_to=${returnTo}`);
    assert heading "Operator overview" visible, then navigate to view=deliveries
- For reduced-motion: await page.emulateMedia({ reducedMotion: "reduce" }); called BEFORE goto
- For boundingBox: await element.boundingBox(); expect(box).not.toBeNull(); expect(box.height)...
- For ARIA: await expect(element).toHaveAttribute("aria-selected", "true")
- For computed style: await page.locator(sel).evaluate(el => getComputedStyle(el).fontWeight)
- Preview surface empty route: "/ops/browser-preview-empty" → page.getByTestId("preview-orientation")

From mailglass_admin/playwright.config.cjs (verify but do NOT modify):
- testDir: "./e2e" — auto-picks up all *.spec.js files, including structural.spec.js
- webServer: { command: "mix phx.server", url: "http://127.0.0.1:4003", timeout: 300000 }
- No explicit testMatch filter — default Playwright glob **/*.spec.{js,ts} applies

From 95-CONTEXT.md D-06 / 95-RESEARCH.md assertion recipe section:
- 3 surfaces and their boot paths:
  - Operator/Deliveries: openOperator(page) then /ops/mail?tenant_id=browser-tenant
  - Inbound: openOperator(page) then page.goto("/ops/mail/inbound?tenant_id=browser-tenant")
  - Preview: page.goto("/ops/browser-preview-empty")
- Accent allowlist constants:
  - ACCENT_LIGHT_RGB = "rgb(39, 123, 150)"  (Glass #277B96)
  - ACCENT_ALLOWLIST: ["[aria-selected='true']", "[aria-current='page']", ".btn-primary", ":focus-visible"]
- Gate-now vs record-as-GAP split per fact (from RESEARCH recommended split):
  - ARIA roles/states: fail-on-any-violation
  - ≥44px touch targets: fail-on-any-violation for primary elements; advisory+GAP for dense-list rows
  - font-weight ∈ {400,700}: fail-on-any-violation (assert known 400 body + 700 bold elements)
  - reduced-motion: fail-on-any-violation
  - visible focus rings: fail-on-any-violation
  - accent-only-on-allowlist: fail-on-any-violation (post-Phase-94 this should be clean)
- Focus ring pitfall: assert outlineWidth > 0 (not the full outline shorthand, which includes color/style and may serialize differently across browsers)
- Font-weight pitfall: assert elements known to be 400 (body text) and 700 (bold heading) — do NOT assert 500/600 absent; system font fallbacks may affect intermediate weights
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write structural.spec.js — 6 pillar-fact assertions across 3 surfaces</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js
      (full file — the exact template: require patterns, tenantId, openOperator function,
      emulateMedia, boundingBox, toHaveAttribute, evaluate getComputedStyle, browser-preview-empty route)
    - mailglass_admin/playwright.config.cjs
      (testDir, webServer config — confirm no testMatch filter that would exclude the new file)
    - mailglass_admin/docs/design-system.md lines 104–121
      (D-01 6 pillars — Spacing / Radius / Color / Type / Elevation / Motion+A11y)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md
      (assertion recipe per fact: code examples for all 6 facts, risk flags, accent allowlist,
      focus ring approach, font-weight pitfall, gate-now vs record-as-GAP decision table)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md (D-06)
  </read_first>
  <action>
    Create mailglass_admin/e2e/structural.spec.js using the exact operator.spec.js patterns.
    The file is picked up automatically by testDir: "./e2e" — no config change needed.

    FILE STRUCTURE:
    - Header constants: tenantId = "browser-tenant", ACCENT_LIGHT_RGB, ACCENT_ALLOWLIST array
    - openOperator(page) helper: mirrors operator.spec.js exactly (browser-reset + browser-login)
    - openInbound(page) helper: calls openOperator then navigates to /ops/mail/inbound?tenant_id=...
    - openPreview(page) helper: navigates to /ops/browser-preview-empty

    OUTER describe: "structural assertions — 6 D-01 pillar facts"

    FACT 1 — ARIA roles/states (Color/Elevation pillar context, correctness fact):
    - test.describe("ARIA roles/states") per surface
    - Operator: click first operator-delivery-row, assert aria-selected="true" on it;
      assert nav link with aria-current="page" exists via getByRole("link", {current:true})
    - Inbound: assert page landmark navigation exists (getByRole("navigation"))
    - Preview: assert preview-orientation testId exists when on browser-preview-empty route
    - GATE: fail-on-any-violation (ARIA correctness is not aesthetic)

    FACT 2 — Touch targets ≥ 44px (Spacing pillar):
    - test.describe("touch targets >= 44px")
    - Set viewport 390×844 before each check
    - Operator: assert first getByRole("button") boundingBox().height >= 44
    - Inbound: assert first getByRole("link") in nav boundingBox().height >= 44
    - Preview: on browser-preview-empty, assert any visible button or link >= 44px
    - GATE: fail-on-any-violation for primary interactive elements (nav links, CTA buttons);
      skip dense timeline rows (those are advisory; record separately if needed in GAP rows)

    FACT 3 — font-weight ∈ {400, 700} (Type pillar):
    - test.describe("font-weight in {400, 700}")
    - For each surface: assert body element computed fontWeight is "400";
      assert first element with role="heading" level=1 computed fontWeight is "700"
    - Do NOT assert on elements with text-xl (known advisory TYPE gate violations per Phase 94)
    - Expected values: ["400", "700"] only — do not assert 500 or 600 absent
    - GATE: fail-on-any-violation (binary fact: 400 or 700, not intermediate)

    FACT 4 — Reduced-motion collapses durations (Motion+A11y pillar):
    - test.describe("reduced-motion suppresses animation")
    - For each surface: call emulateMedia({ reducedMotion: "reduce" }) BEFORE page navigation;
      after navigation assert the primary content area is visible and stable (not stuck invisible);
      for Operator: assert operator-deliveries-list is visible;
      for Inbound: assert the page heading is visible;
      for Preview: assert preview-orientation is visible
    - Pattern mirrors operator.spec.js:229 exactly (emulateMedia must precede goto)
    - GATE: fail-on-any-violation (reduced-motion support is a11y requirement)

    FACT 5 — Visible focus rings (Motion+A11y pillar):
    - test.describe("visible focus rings")
    - For each surface: focus the first getByRole("link"), then evaluate
      el => getComputedStyle(el).outlineWidth;
      assert parseFloat(outline) > 0 (ring is visible)
    - Do NOT assert on outline shorthand (serialize format varies cross-browser);
      assert outlineWidth specifically
    - GATE: fail-on-any-violation (a11y requirement; Phase 94 did not change focus-ring CSS
      so this may GAP — write the assertion correctly and if it fails populate GAP row)

    FACT 6 — Accent-only-on-allowlist (Color pillar):
    - test.describe("accent color only on allowlisted elements")
    - For each surface: check a curated set of non-allowlisted elements for the accent RGB
      (body text, card backgrounds, dividers, nav container background):
        const accentElements = await page.locator("body, [data-testid='operator-deliveries-list'], nav").all();
        for each element, compute backgroundColor and color via getComputedStyle;
        verify none equals ACCENT_LIGHT_RGB unless the element also matches one of the ACCENT_ALLOWLIST selectors
    - Assert: no non-allowlisted element carries the accent color
    - Post-Phase-94 this should be clean (accent confined by token re-baseline)
    - GATE: fail-on-any-violation

    GALLERY ABSENCE NOTE (per D-06 deferred):
    - Add a test.describe.skip("gallery surface — deferred to Phase 97") block with a single
      test that calls test.skip("gallery at /dev/mail/gallery does not exist yet; RATCHET-GAP-REGISTER.md GAP row tracks this")
    - This makes the deferred scope machine-visible without causing any failure

    IMPLEMENTATION CONSTRAINTS:
    - Never import a new server fixture or call mix run directly (Pitfall 3 from RESEARCH)
    - Use the existing browser-reset + browser-login route pattern for all authenticated surfaces
    - No new npm packages (existing @playwright/test is sufficient)
    - PII: use only "browser-tenant@example.com" test tenant — never real email addresses
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && npm run test:operator-browser -- --grep "structural assertions"</automated>
  </verify>
  <acceptance_criteria>
    - mailglass_admin/e2e/structural.spec.js exists
    - `cd mailglass_admin && npm run test:operator-browser` exits 0 (all structural + existing operator tests pass)
    - `grep -c "ACCENT_ALLOWLIST" mailglass_admin/e2e/structural.spec.js` >= 1
    - `grep -c "emulateMedia" mailglass_admin/e2e/structural.spec.js` >= 1
    - `grep -c "outlineWidth" mailglass_admin/e2e/structural.spec.js` >= 1
    - `grep -c "boundingBox" mailglass_admin/e2e/structural.spec.js` >= 1
    - `grep -c "browser-preview-empty" mailglass_admin/e2e/structural.spec.js` >= 1
    - `grep -c "deferred to Phase 97" mailglass_admin/e2e/structural.spec.js` >= 1
    - No changes to playwright.config.cjs or ci.yml (verify with `git diff -- mailglass_admin/playwright.config.cjs .github/workflows/ci.yml` shows no changes)
    - The existing operator.spec.js tests still pass (no regression from adding structural.spec.js)
    - `git status --porcelain mailglass_admin/playwright.config.cjs .github/` shows clean (no accidental changes)
  </acceptance_criteria>
  <done>
    structural.spec.js exists and all 6 structural fact groups pass on 3 live surfaces.
    npm run test:operator-browser exits 0 on the full e2e suite. This is D-08 commit 3 green.
    Any violations found during development are recorded as candidate GAP rows (populated in Plan 95-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser → running Phoenix server | Structural spec hits the local test server; credentials are test-only (browser-tenant, no real PII) |
| structural.spec.js → DOM computed styles | Style reads via evaluate() are sandboxed in Chromium; no data exfiltration risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-PW1 | Information Disclosure | browser-tenant test fixture in Playwright | accept | Test tenant uses browser-tenant@example.com (non-real PII); multi-tenant safety preserved (no cross-tenant data in test); consistent with existing operator.spec.js fixture |
| T-95-PW2 | Denial of Service | Playwright server timeout (300s) on structural.spec.js | accept | Structural spec reuses existing openOperator helper; adds no new server-state setup paths; 300s timeout is adequate per RESEARCH pitfall 3 analysis |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages — structural.spec.js uses require("@playwright/test") which is already installed; slopcheck not applicable |

No high-severity threats: the structural spec runs in the dev/test harness only and is excluded
from the Hex tarball. No auth bypass, no PII, no external network calls during tests.
V5 input validation is not applicable here (no JSON is read by this spec; DOM reads are sandboxed).
</threat_model>

<verification>
After the task completes:
- `cd mailglass_admin && npm run test:operator-browser` exits 0
- Output contains "structural assertions" test group (confirms spec is picked up by glob)
- Existing "operator browser gate" tests also pass (no regression)
- `git diff -- mailglass_admin/playwright.config.cjs .github/workflows/ci.yml` is empty
- `grep "browser-tenant@example.com\|tenantId.*browser-tenant" mailglass_admin/e2e/structural.spec.js` confirms non-PII test fixture
</verification>

<success_criteria>
1. structural.spec.js exists with all 6 fact-assertion groups plus the gallery-deferred skip block
2. `npm run test:operator-browser` exits 0 on the full suite (structural + existing operator tests)
3. No changes to playwright.config.cjs or ci.yml
4. Facts that currently fail (if any) are structured to pass the assertion while candidate GAP rows are noted (for Plan 95-04)
5. D-08 commit 3 is green: operator_browser_gate required lane passes end-to-end
</success_criteria>

<output>
Create `.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-03-SUMMARY.md` when done.
</output>
