---
phase: 95-audit-apparatus-quality-ratchet-v2
plan: 04
type: execute
wave: 4
depends_on:
  - 95-03
files_modified:
  - mailglass_admin/docs/ui-baseline-scores.json
  - .planning/RATCHET-GAP-REGISTER.md
autonomous: false
requirements:
  - RATCHET-01
  - RATCHET-02
  - RATCHET-05
user_setup:
  - service: agent-browser
    why: "ui-audit.sh captures the 18-cell PNG matrix using the agent-browser CLI"
    env_vars: []
    dashboard_config:
      - task: "Ensure agent-browser CLI is installed locally before running this plan"
        location: "Install per the agent-browser CLI documentation (local binary, not a Hex/npm dep)"
must_haves:
  truths:
    - "The 18-cell PNG matrix (3 surfaces × 3 viewports × 2 themes) has been captured via ui-audit.sh"
    - "PNGs are under tmp/ui-audit/ which is gitignored — no PNG is committed"
    - "All 36 surface×pillar×theme cells in ui-baseline-scores.json hold real LLM-scored integers (not placeholder 1s)"
    - "The ratchet_baseline_test.exs passes on the real scores (shape + range assertions green)"
    - "RATCHET-GAP-REGISTER.md has been populated with initial GAP-NN rows from the seed run"
    - "Each GAP row has all 9 columns filled: GAP-NN, surface, component:line, pillar, sev, evidence PNG, fix sketch, status (open), run_id, first_seen_run"
    - "run_id is '2026-06-13-phase-95-baseline' consistently across all first-seed GAP rows"
    - "At least one GAP row with sev≥3 exists (or is documented as 'none found') so the anti-churn contract is demonstrably active"
    - "git status tmp/ui-audit/ shows empty output (PNGs are gitignored)"
  artifacts:
    - path: "mailglass_admin/docs/ui-baseline-scores.json"
      provides: "D-03 real scored baseline — 36 cells, run_id 2026-06-13-phase-95-baseline"
      contains: "run_id"
    - path: ".planning/RATCHET-GAP-REGISTER.md"
      provides: "GAP-01..NN rows from Phase 95 seed run"
      contains: "2026-06-13-phase-95-baseline"
  key_links:
    - from: "mailglass_admin/scripts/ui-audit.sh"
      to: "tmp/ui-audit/{surface}-{vp}-{theme}.png"
      via: "agent-browser CLI screenshot capture"
      pattern: "tmp/ui-audit/"
    - from: "LLM scoring step"
      to: "mailglass_admin/docs/ui-baseline-scores.json"
      via: "multimodal subagent scoring PNGs against D-01 pillars"
      pattern: "surface.*pillar.*theme.*score"
    - from: ".planning/RATCHET-GAP-REGISTER.md"
      to: "downstream Phases 98–103 build tasks"
      via: "sev≥3 citation gate"
      pattern: "GAP-\\d+ | sev.*[345]"
---

<objective>
Run the apparatus once to seed the baseline: capture the 18-cell PNG matrix via ui-audit.sh,
score each surface×theme pair against the D-01 6-pillar rubric using a multimodal subagent,
write the real scores to `ui-baseline-scores.json`, confirm the ExUnit baseline assertion is
green on the real scores, and populate initial GAP-NN rows in RATCHET-GAP-REGISTER.md.

This is D-08 commit 4: the seed run that transitions the apparatus from "scaffold green"
to "measured baseline committed."

Purpose: Establish the Phase 95 baseline — the floor that Phase 103's meet-or-beat assertion
will require every cell to meet or exceed. Also seeds the GAP register with the first run's
findings, activating the anti-churn citation gate for downstream build phases.

Output:
- Updated `mailglass_admin/docs/ui-baseline-scores.json` with real LLM scores (run_id set)
- Updated `.planning/RATCHET-GAP-REGISTER.md` with populated GAP-01..NN rows
- PNGs remain in gitignored tmp/ui-audit/ — never committed
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-CONTEXT.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md
@.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-VALIDATION.md
@.planning/RATCHET-GAP-REGISTER.md
@mailglass_admin/docs/ui-baseline-scores.json

<interfaces>
<!-- LLM scoring procedure from 95-RESEARCH.md steps 1–5 -->

From mailglass_admin/scripts/ui-audit.sh (read before running):
- Variables: OUT defaults to tmp/ui-audit, SURFACES, VIEWPORTS (390/768/1440), THEMES (light/dark)
- Output: 18 PNG files at tmp/ui-audit/{surface}-{vp}-{theme}.png
- Prerequisite: reference demo server running on port 4015 (or the port ui-audit.sh targets)
- Do NOT use the port 4003 Playwright dev server; ui-audit.sh uses a separate server port

From mailglass_admin/docs/design-system.md lines 104–121 (D-01 pillar rubric):
- Scoring dimensions: Spacing / Radius / Color / Type / Elevation / Motion+A11y
- Grade scale (1–4): 1=non-conformant/severe, 2=significant-gaps, 3=mostly-conformant, 4=excellent
- Evidence for a score < 4 must be specific (component name, line reference, observed behavior)
  — this evidence becomes the GAP row's fix sketch

From .planning/RATCHET-GAP-REGISTER.md (header created by Plan 95-01):
- Column order: GAP-NN | surface | component:line | pillar | sev | evidence PNG | fix sketch | status | run_id | first_seen_run
- run_id format: 2026-06-13-phase-95-baseline
- Each score < 4 on any cell that has a visible UI cause → one GAP row
- first_seen_run = run_id (all Phase 95 rows are first-seen in this run)
- status = "open" for all Phase 95 rows

From mailglass_admin/docs/ui-baseline-scores.json (placeholder from Plan 95-02):
- Replace placeholder 1s with real scored integers
- Keep all metadata keys: run_id, schema_version, pillar_rubric, grade_scale
- Update run_id from "2026-06-13-phase-95-placeholder" to "2026-06-13-phase-95-baseline"

From .gitignore and mailglass_admin/.gitignore:
- /tmp/ rule covers tmp/ui-audit/ in both files
- docs/ui-baseline-scores.json is NOT under tmp/ and IS in the Hex tarball files list
- After the run: `git status tmp/ui-audit/` must show empty (PNGs are gitignored)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Boot demo server and capture 18-cell PNG matrix via ui-audit.sh</name>
  <what-built>
    The apparatus (GAP register schema, ExUnit baseline assertion, Playwright structural spec)
    is fully wired and green after Plans 95-01 through 95-03. Now the maintainer must run
    the local seed capture step that Claude cannot automate: starting the demo server and
    invoking ui-audit.sh with the agent-browser CLI.
  </what-built>
  <how-to-verify>
    Run these commands in order from the mailglass repo root:

    1. Boot the reference demo (requires agent-browser CLI installed locally):
       ```
       cd reference/demo_app
       mix ecto.create && mix ecto.migrate && mix run priv/repo/seeds.exs
       mix phx.server
       ```
       (Leave this terminal running. The server must stay up for the screenshot capture.)

    2. In a NEW terminal, from the mailglass repo root, run:
       ```
       bash mailglass_admin/scripts/ui-audit.sh
       ```
       Expected: 18 PNG files created at tmp/ui-audit/ (paths like tmp/ui-audit/deliveries-390-light.png).

    3. Verify PNG capture:
       ```
       ls tmp/ui-audit/*.png | wc -l
       ```
       Expected output: 18

    4. Verify PNGs are gitignored (no accidental commit):
       ```
       git status tmp/ui-audit/
       ```
       Expected: empty output (no files listed)

    When you have 18 PNG files confirmed, type "screenshots-ready".
  </how-to-verify>
  <resume-signal>Type "screenshots-ready" once 18 PNGs are confirmed in tmp/ui-audit/ and gitignored</resume-signal>
</task>

<task type="auto">
  <name>Task 2: LLM-score 18 cells against D-01 pillars and write ui-baseline-scores.json</name>
  <files>mailglass_admin/docs/ui-baseline-scores.json</files>
  <read_first>
    - mailglass_admin/docs/design-system.md lines 104–121
      (canonical D-01 6 pillars and their definitions — the scoring rubric)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md
      (LLM scoring procedure section, JSON schema example, 36-cell reconciliation,
      pitfall 1 about not conflating 18 PNGs with 36 graded cells)
    - mailglass_admin/docs/ui-baseline-scores.json
      (current placeholder — replace all score values, update run_id)
    - tmp/ui-audit/*.png
      (the 18 captured PNGs — read them as images to perform multimodal scoring)
  </read_first>
  <action>
    Score each surface×theme pair (6 pairs: deliveries-light, deliveries-dark, inbound-light,
    inbound-dark, preview-light, preview-dark) against all 6 D-01 pillars using the 1–4 scale.

    SCORING PROCEDURE:
    For each of the 6 surface×theme pairs:
    1. Load the 3 viewport PNGs for that pair (390, 768, 1440 width screenshots).
    2. For each of the 6 pillars, assign an integer score 1–4:
       - Spacing: consistent spacing rhythm, use of semantic spacing tokens
       - Radius: corner radius consistency, use of radius tokens
       - Color: semantic token use, contrast, WCAG AA compliance, no raw hex
       - Type: type scale correctness, weight (only 400/700), size tokens, no text-xl violations
       - Elevation: flat elevation (border-first), surface token use (surface-raised/sunken), no glassmorphism
       - Motion+A11y: focus rings visible, touch targets ≥44px, ARIA labels, reduced-motion support
    3. A score of 4 = excellent / fully on-brand; 3 = mostly conformant; 2 = significant gaps; 1 = non-conformant.
    4. Any score < 4 requires a specific evidence note (the component/element + the issue observed).
       These evidence notes become GAP rows in Task 3.

    WRITE THE RESULT:
    Update mailglass_admin/docs/ui-baseline-scores.json in-place:
    - Set "run_id" to "2026-06-13-phase-95-baseline"
    - Keep "schema_version": 1, "pillar_rubric", "grade_scale" unchanged
    - Replace all 36 score values with real scored integers in 1..4
    - All 3 surfaces × 6 pillars × 2 themes must have an integer (not null, not 0, not a string)

    AFTER WRITING: run the ExUnit baseline test to confirm it passes on the real scores:
    `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs`
    If any score is out of range (< 1 or > 4), correct the score before proceeding.

    PATH NOTE: write to mailglass_admin/docs/ui-baseline-scores.json (the D-03 canonical path).
    The ExUnit test reads from Path.join([__DIR__, "..", "..", "docs", "ui-baseline-scores.json"])
    which resolves to the same file. Confirm both paths agree before commit.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs` exits 0 (3 tests pass on the real scores)
    - `python3 -c "import json; d=json.load(open('mailglass_admin/docs/ui-baseline-scores.json')); scores=[d['surfaces'][s][p][t] for s in ['deliveries','inbound','preview'] for p in ['Spacing','Radius','Color','Type','Elevation','Motion+A11y'] for t in ['light','dark']]; assert all(isinstance(x,int) and 1<=x<=4 for x in scores), f'Bad scores: {[x for x in scores if not(isinstance(x,int) and 1<=x<=4)]}'; print(f'All {len(scores)} scores valid')"` prints "All 36 scores valid"
    - `grep '"run_id": "2026-06-13-phase-95-baseline"' mailglass_admin/docs/ui-baseline-scores.json` matches
    - `git status tmp/ui-audit/` shows no output (PNGs remain gitignored)
    - The JSON file does not contain "placeholder" in run_id
  </acceptance_criteria>
  <done>
    ui-baseline-scores.json contains 36 real LLM-scored integers in 1..4, run_id is the
    baseline ID, and the ExUnit test passes. D-08 commit 4 is half done (JSON committed).
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate initial GAP-NN rows in RATCHET-GAP-REGISTER.md from seed-run findings</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    - .planning/RATCHET-GAP-REGISTER.md
      (header + schema created by Plan 95-01 — the column order and anti-churn contract to continue)
    - mailglass_admin/docs/ui-baseline-scores.json
      (just scored — every cell with score < 4 is a candidate GAP row; evidence from scoring step)
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-RESEARCH.md
      (GAP register schema section, severity rubric, column descriptions)
    - mailglass_admin/e2e/structural.spec.js
      (check if any structural assertions revealed a pillar-fact violation during Task 1 of Plan 95-03;
      violations not yet gated that were noted in task acceptance criteria become GAP rows here)
    - tmp/ui-audit/*.png
      (reference for evidence PNG column: use the path format tmp/ui-audit/{surface}-{vp}-{theme}.png)
  </read_first>
  <action>
    Populate the ## Gaps section of .planning/RATCHET-GAP-REGISTER.md with the initial GAP rows.
    Source the rows from two inputs:
    1. LLM scoring findings: any surface×pillar×theme cell scored < 4 with a specific visible cause.
       A score of 3 ("mostly conformant") warrants a GAP row if there is a specific identified fix.
       A score of 2 or 1 always warrants a GAP row.
    2. Playwright structural spec findings: any violation found during Plan 95-03 that was noted
       as a candidate GAP (e.g. focus rings, touch targets on dense rows, etc.).
    3. Gallery absence: add exactly one GAP row for "gallery surface /dev/mail/gallery does not exist"
       with pillar=Motion+A11y (accessibility audit surface), sev=2 (minor — it's a Phase 97 deliverable),
       status=open, fix sketch="Build Phase 97 component gallery".

    COLUMN VALUES FOR ALL ROWS:
    - run_id: "2026-06-13-phase-95-baseline"
    - first_seen_run: "2026-06-13-phase-95-baseline"
    - status: "open"
    - evidence PNG: use the most relevant viewport PNG path (e.g. tmp/ui-audit/deliveries-390-light.png)
    - component:line: identify the relevant source file in mailglass_admin/lib/ (e.g.
      mailglass_admin_web/live/operator_live.ex, or the specific component file + approximate line);
      if line is approximate, use the filename only

    ROW ORDERING: assign sequential GAP-01, GAP-02, ... IDs. Stable once assigned — never renumber.

    SEVERITY RUBRIC (from register header):
    - 5: Accessibility failure / brand violation visible to every user
    - 4: Major design-system violation affecting core UX
    - 3: Significant gap (the anti-churn citation threshold — downstream phases MUST cite sev≥3)
    - 2: Minor inconsistency
    - 1: Cosmetic / polish

    ANTI-CHURN GATE CHECK:
    Ensure at least one row has sev≥3. If the surfaces are genuinely excellent (all scores 3+,
    no structural fact violations), document this explicitly: add a row for the gallery absence
    at sev=3 (Phase 97 must cite it when building the gallery) and/or any Type/Motion+A11y
    finding from the scoring (Type is expected to have sev≥3 findings based on the advisory
    text-xl violations known from Phase 94). The anti-churn gate must be demonstrably active.

    GALLERY ROW:
    Add a row: GAP-xx | all | mailglass_admin_web/router.ex | Motion+A11y | 2 | — |
    Add route /dev/mail/gallery (Phase 97, dev live_session only) | open | 2026-06-13-phase-95-baseline | 2026-06-13-phase-95-baseline

    After populating rows, verify the table is well-formed (balanced pipe characters, no
    row wrapping that breaks markdown table rendering).
  </action>
  <verify>
    <automated>grep -c "2026-06-13-phase-95-baseline" .planning/RATCHET-GAP-REGISTER.md && grep -c "GAP-0" .planning/RATCHET-GAP-REGISTER.md && grep -c "open" .planning/RATCHET-GAP-REGISTER.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/RATCHET-GAP-REGISTER.md contains at least 1 GAP-NN row (GAP-01 at minimum)
    - All rows have run_id = "2026-06-13-phase-95-baseline"
    - All rows have first_seen_run = "2026-06-13-phase-95-baseline"
    - All rows have status = "open"
    - At least one row has sev≥3 (so the anti-churn gate is demonstrably active for downstream phases)
    - The gallery absence is recorded as a GAP row (check `grep "gallery" .planning/RATCHET-GAP-REGISTER.md`)
    - Each row has all 9 columns filled (no empty cells other than evidence PNG which may be "—" if not applicable)
    - Markdown table is valid (grep -c "^| GAP" .planning/RATCHET-GAP-REGISTER.md returns >= 1)
    - `git diff --stat HEAD` after commit shows only ui-baseline-scores.json and RATCHET-GAP-REGISTER.md changed
  </acceptance_criteria>
  <done>
    RATCHET-GAP-REGISTER.md has initial GAP-01..NN rows seeded from the Phase 95 baseline run.
    At least one sev≥3 row exists. Gallery absence is recorded. All rows are open with the
    Phase 95 run_id. The D-08 commit 4 is complete: apparatus seeded, baseline committed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify complete phase gate — both required CI lanes green on the real scores</name>
  <files></files>
  <read_first>
    - .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-VALIDATION.md
      (full per-task verification map — confirm all ACTIVE rows are satisfied)
    - mailglass_admin/docs/ui-baseline-scores.json (just committed — confirm run_id is baseline)
  </read_first>
  <action>
    Run the two required CI lanes to confirm the complete Phase 95 apparatus is green:
    1. `cd mailglass_admin && mix verify.support_contract.admin` — must exit 0 (includes ratchet_baseline_test.exs on the real scores)
    2. `cd mailglass_admin && npm run test:operator-browser` — must exit 0 (includes structural.spec.js on 3 live surfaces)

    Then verify the gitignore gate:
    3. `git status tmp/ui-audit/` — must show empty output (no PNGs tracked)
    4. `git status --porcelain mailglass_admin/docs/ui-baseline-scores.json` — must show the file is committed (empty if already committed, or "M" if a change is staged)

    Then update 95-VALIDATION.md frontmatter: set nyquist_compliant: true and wave_0_complete: true.

    If either CI lane is red: diagnose and fix before updating VALIDATION.md. Common causes:
    - ratchet_baseline_test.exs red: a real score is outside 1..4 — correct the JSON
    - structural.spec.js red: a newly discovered violation on the live surfaces — decide gate-now
      vs add GAP row; if gate-now is incorrect (the apparatus is measuring, not requiring
      pre-fix perfection), adjust the assertion to pass and add a GAP row instead
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.support_contract.admin && npm run test:operator-browser && git status tmp/ui-audit/ && echo "PHASE_95_APPARATUS_GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix verify.support_contract.admin` exits 0
    - `cd mailglass_admin && npm run test:operator-browser` exits 0
    - `git status tmp/ui-audit/` shows empty output
    - Echo "PHASE_95_APPARATUS_GREEN" is reached (both gates passed sequentially)
    - 95-VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true
    - `grep "nyquist_compliant: true" .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-VALIDATION.md` matches
  </acceptance_criteria>
  <done>
    Both required CI lanes are green on the real baseline scores. PNGs are gitignored.
    The Phase 95 apparatus is fully stood up and the first seed run is committed. Phase 95 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ui-audit.sh PNG output → LLM scoring subagent | PNGs are local screenshots of the dev/test server; no PII in UI surfaces (test-tenant data uses mask_recipient/1 patterns) |
| LLM score integers → ui-baseline-scores.json → ExUnit | JSON is a committed repo artifact read at test time; structure validated by the ExUnit test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-V5 | Information Disclosure / Tampering | ui-baseline-scores.json read by ratchet_baseline_test.exs | mitigate | V5 input validation active: test validates schema_version == 1, all 36 cells present (integer keys), all scores in 1..4 integer range — malformed JSON or wrong types fail the test with a clear message |
| T-95-PII | Information Disclosure | Screenshots captured by ui-audit.sh | accept | Screenshots target the dev/test server with seed tenant data; mask_recipient/1 ensures no real recipient PII; screenshots are gitignored (tmp/ui-audit/) — never committed |
| T-95-05 | Tampering | RATCHET-GAP-REGISTER.md stable IDs | accept | Register is a planning artifact; stable-ID discipline enforced by documented review rule (anti-churn contract); no machine enforcement needed (PR review is the gate) |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — agent-browser CLI is a pre-existing local tool, not a new dep; slopcheck not applicable |

High-severity assessment: No high-severity threats apply. The one substantive V5 threat is
mitigated by the ExUnit shape/range assertions. PII is minimized (gitignored screenshots,
test-tenant seed data). The LLM scoring step is local and its only committed output is the
integer score JSON.
</threat_model>

<verification>
Phase 95 complete when ALL of the following are true:
1. `cd mailglass_admin && mix verify.support_contract.admin` exits 0 — ratchet_baseline_test.exs passes on real scores
2. `cd mailglass_admin && npm run test:operator-browser` exits 0 — structural.spec.js passes on 3 live surfaces
3. `git status tmp/ui-audit/` is empty — no PNG committed
4. `python3 -c "import json; d=json.load(open('mailglass_admin/docs/ui-baseline-scores.json')); print(d['run_id'])"` prints "2026-06-13-phase-95-baseline"
5. `grep -c "GAP-0" .planning/RATCHET-GAP-REGISTER.md` >= 1 — at least one data row
6. `grep -c "sev.*[345]" .planning/RATCHET-GAP-REGISTER.md` >= 1 — at least one sev≥3 row
7. `grep "nyquist_compliant: true" .planning/phases/95-audit-apparatus-quality-ratchet-v2/95-VALIDATION.md` matches
</verification>

<success_criteria>
1. 18-cell PNG matrix captured via ui-audit.sh — all 18 PNGs gitignored, none committed
2. All 36 surface×pillar×theme cells in ui-baseline-scores.json hold real LLM-scored integers in 1–4
3. run_id = "2026-06-13-phase-95-baseline" in the committed JSON
4. ratchet_baseline_test.exs is green on the real scores (verify.support_contract.admin passes)
5. structural.spec.js is green on 3 live surfaces (operator_browser_gate passes)
6. RATCHET-GAP-REGISTER.md has populated GAP-01..NN rows with at least one sev≥3 row
7. Gallery absence recorded as a GAP row
8. 95-VALIDATION.md: nyquist_compliant: true, wave_0_complete: true
9. D-08 commit 4 complete: apparatus seeded, baseline committed, both CI lanes green
</success_criteria>

<output>
Create `.planning/phases/95-audit-apparatus-quality-ratchet-v2/95-04-SUMMARY.md` when done.
</output>
