---
phase: 142-supply-chain-remediation-gating
plan: 04
type: execute
wave: 3
depends_on: ["142-03"]
files_modified:
  - .github/workflows/ci.yml
  - test/support/ci_lanes.ex
  - .github/workflows/publish-hex.yml
  - MAINTAINING.md
  - test/scripts/lane_classification_drift_test.exs
  - test/scripts/ci_parity_drift_test.exs
autonomous: true
requirements: [VULN-03]

must_haves:
  truths:
    - "Both `Hex Audit (Elixir 1.18 / OTP 27)` and `Deps Audit (Elixir 1.18 / OTP 27)` (renamed from `Deps Audit Advisory`) are members of `ci.yml`'s `ci_green.needs` AND `Mailglass.CILanes.required_lanes/0` — merge-gating, not merely publish-gating."
    - "`deps_audit_advisory`'s job no longer carries `continue-on-error: true` — a red finding on that job now fails the job (and, via `ci_green`, blocks the merge), not just warns."
    - "The nine D-05 edit sites land in exactly ONE commit — `ci.yml`'s `needs:` + result-loop, the `continue-on-error`/stale-comment deletion, `ci_lanes.ex`'s four bucket edits, `publish-hex.yml`'s three array edits, `MAINTAINING.md`'s two disposition rows, and the three drift-test hardcoded counts — never split across separate commits (D-04)."
    - "No HIGH-severity merge threshold is introduced anywhere in this commit — the gate still blocks on ANY non-allowlisted finding, matching `mailglass.publish.check`'s existing block-on-any semantics (D-08)."
    - "A `deps.audit` finding matching an allowlist entry ONLY by its GHSA alias (not by primary EEF-CVE id) is still suppressed post-promotion and does not block the merge — the D-02/F2 alias-matching mechanism from Wave 1 continues to hold once the lane becomes required. [edge: adjacency, VULN-03]"
    - "A directory whose audit is genuinely clean (root or `mailglass_inbound` today) contributes zero blocking findings to the promoted lane's exit code; an all-clean run across all three directories exits 0 without any directory's absence-of-findings being mistaken for an error. [edge: empty, VULN-03]"
    - "Because the nine-site edit lands as one atomic commit, the internal order in which the sites are edited within that commit is irrelevant to correctness — the drift meta-tests evaluate only the final committed file states, never an intermediate ordering. [edge: ordering, VULN-03]"
    - "`Mailglass.CILanes.all_classified_lanes/0` still totals exactly 24 entries after the promotion (5+4+13+2 -> 7+3+12+2 = 24) — only bucket membership moves, no job is dropped or duplicated."
  artifacts: []
  key_links:
    - "`ci.yml`'s `ci_green.needs:` list and its `for job_result in` shell-loop list must both gain `hex_audit`/`deps_audit_advisory` in the same commit — D-07: job-level `continue-on-error` forces `needs.*.result` to `success`, so BOTH the `needs:` addition AND the `continue-on-error` deletion are required for PR-merge blocking; neither alone works."
    - "`publish-hex.yml`'s `REQUIRED_LANES` gains both renamed/unchanged display names, and `ADVISORY_LANES`/`PUBLISH_GATING_LANES` lose them — `gate-ci-green`'s `classify/1` reads job.conclusion from the REST API, which `continue-on-error` does NOT mask; it is the array membership that determines whether publish blocks (D-07)."
    - "`MAINTAINING.md`'s disposition-table parser (`lane_classification_drift_test.exs`) bounds the `## Required Checks` section by splitting on `\"\\n## \"` and asserts exactly 24 rows — editing the `hex_audit`/`deps_audit_advisory` rows in place (not adding/removing a row) is what keeps that count exact."
  prohibitions: []
---

<objective>
Execute VULN-03's promotion — Hex Audit and Deps Audit become merge-gating — as ONE atomic commit
touching all nine sites D-05 enumerates, immediately after 142-03's checkpoint confirms Wave 1 is
genuinely green with cowlib in scope. Splitting this across commits would leave `main` with a red
publish-gating lane and a `gate-ci-green` whose arrays disagree with `ci_green.needs` — reproducing
the exact registry-disagreement defect Phase 141 just closed (D-04).

Purpose: ROADMAP criterion 2 — "a PR that introduces a new HIGH-severity advisory with an available
fix cannot merge, while a PR touching only the already-accepted cowlib advisories merges cleanly."
This plan is the single mechanical act that makes that true; 142-01/02/03 built and proved the
mechanism that makes it SAFE to do.

Output: `Hex Audit` and `Deps Audit` (renamed) are both `ci_green.needs` members and
`Mailglass.CILanes.required_lanes/0` members; `deps_audit_advisory` no longer carries
`continue-on-error: true`; `MAINTAINING.md`'s disposition table reflects the promotion;
`lane_classification_drift_test.exs`/`ci_parity_drift_test.exs`'s hardcoded bucket counts are
updated to match.

**Decisions implemented here:** D-04 (one atomic commit — locked, non-negotiable) · D-05 (the nine
exact edit sites) · D-06 (rename `Deps Audit Advisory` -> `Deps Audit`, `scripts/setup_branch_
protection.sh` NOT touched) · D-07 (`continue-on-error` deletion is mandatory for PR-merge blocking;
the classification-array move is what makes publish blocking) · D-08 (block on any finding, no HIGH
threshold) · D-09 (delete the stale `isAdvisory()` comment).

**THIS PLAN IS DELIBERATELY ONE TASK, NOT 2-3.** D-04 locks the nine-site edit as a single atomic
commit. The executor's default protocol commits per task; splitting this work across multiple tasks
would therefore split the commit too, reproducing the exact multi-registry-disagreement defect Phase
141 closed. One task, one commit, nine sites, all landing together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-supply-chain-remediation-gating/142-CONTEXT.md
@.planning/phases/142-supply-chain-remediation-gating/142-RESEARCH.md
@.planning/phases/142-supply-chain-remediation-gating/142-PATTERNS.md
@.planning/phases/141-lane-truth-foundation/141-CONTEXT.md
</context>

## Artifacts this phase produces

| Symbol | Kind | Created in |
|---|---|---|
| `Mailglass.SupplyChain.AcceptedAdvisories`, `mix mailglass.audit` | module/task | 142-01/02 |
| `Deps Audit (Elixir 1.18 / OTP 27)` | renamed `ci.yml`/`ci_lanes.ex`/`publish-hex.yml`/`MAINTAINING.md` display name (was `Deps Audit Advisory (Elixir 1.18 / OTP 27)`) | 142-04 (this plan) |
| `## Dependency Advisory Triage` | `MAINTAINING.md` section | 142-05 |

<tasks>

<task type="auto">
  <name>Task 1: The atomic promotion — nine sites, one commit</name>

  <files>
    .github/workflows/ci.yml,
    test/support/ci_lanes.ex,
    .github/workflows/publish-hex.yml,
    MAINTAINING.md,
    test/scripts/lane_classification_drift_test.exs,
    test/scripts/ci_parity_drift_test.exs
  </files>

  <read_first>
    - `.github/workflows/ci.yml:545-568` (`hex_audit`, unchanged by this task — already rewired in
      142-01), `:570-601` (`deps_audit_advisory` — this task's `name:`/`continue-on-error`/comment
      target), `:1146-1177` (`ci_green` — the `needs:` list AND the shell `for job_result in` loop,
      which must change together)
    - `test/support/ci_lanes.ex` (full file, already read during planning — reproduced verbatim
      below in `<action>`) — every one of `@required_lanes`, `@advisory_lanes_ci`,
      `@advisory_classified_lanes`, `@publish_gating_lanes` needs an edit
    - `.github/workflows/publish-hex.yml:204-261` — `REQUIRED_LANES`, `ADVISORY_LANES`,
      `PUBLISH_GATING_LANES` (the fourth array, `STRUCTURAL_LANES`, is untouched by this promotion)
    - `MAINTAINING.md:190-217` — the 24-row disposition table; rows for `deps_audit_advisory`
      (currently line ~197) and `hex_audit` (currently line ~210) both already carry disposition
      `promote` with the literal text "Phase 142/VULN-03 executes the promotion" (Phase 141 wrote
      this phase's own instruction into the table)
    - `test/scripts/lane_classification_drift_test.exs:67-99,135-153` — the three hardcoded counts:
      `ADVISORY_LANES` `== 4` (test at line ~71), `PUBLISH_GATING_LANES` `== 13` (line ~88),
      `REQUIRED_LANES` `== 5` (line ~143, inside the anti-vacuity test)
    - `test/scripts/ci_parity_drift_test.exs:161` — `Mailglass.CILanes.required_lanes()` `== 5`
    - `142-PATTERNS.md` § "`test/support/ci_lanes.ex` (MODIFY — Wave 2, D-05 sites 4a-4d)" and §
      "`.github/workflows/publish-hex.yml`, `test/scripts/lane_classification_drift_test.exs`,
      `test/scripts/ci_parity_drift_test.exs`, `mix.exs`, `MAINTAINING.md`" — the exact current bucket
      text to diff against
  </read_first>

  <precondition>
    `142-03-SUMMARY.md` records the live-verified evidence that `Hex Audit` is green with cowlib
    genuinely detected-and-suppressed, and that the negative control fails closed. Halt and report if
    that SUMMARY is missing or does not contain both excerpts — this task must not start on "142-01
    merged" alone (D-14).
  </precondition>

  <reversibility rating="costly">
    Promoting a lane from advisory/publish-gating to merge-gating has real, immediate impact: every
    open and future PR must now pass both `Hex Audit` and `Deps Audit`. Technically reversible (a
    follow-up commit can demote it), but not free — 142-03's checkpoint is the pre-flight mitigation;
    this task itself carries the residual "costly" rating because the impact lands the moment this
    commit merges, not gradually.
  </reversibility>

  <action>
    Make ALL nine edits below, then stage and commit them as ONE commit (D-04). Do not commit
    incrementally between edits.

    **1. `.github/workflows/ci.yml` — `ci_green`'s `needs:` and result-loop (D-05 site 1).**
    In the `needs:` list (`:1150-1155`), add `- hex_audit` and `- deps_audit_advisory` alongside the
    existing five entries. In the `for job_result in \` heredoc list (`:1160-1165`), add
    `"hex_audit=${{ needs.hex_audit.result }}" \` and `"deps_audit_advisory=${{
    needs.deps_audit_advisory.result }}" \` as two more backslash-continued lines. Both edits are
    required together — a `needs:` addition with no matching loop entry silently never checks the new
    job's result.

    **2. `.github/workflows/ci.yml` — delete `continue-on-error: true` (D-05 site 2, D-07).**
    Delete `continue-on-error: true` at `deps_audit_advisory`'s job level (`:581`). This is mandatory
    for PR-merge blocking: job-level `continue-on-error` forces `needs.*.result` to `success` in
    `ci_green`'s shell loop, regardless of the job's real `conclusion` — without this deletion, step 1
    above adds the job to `needs:` but it can never actually block a merge.

    **3. `.github/workflows/ci.yml` — delete the stale comment block (D-05 site 3, D-09).**
    Delete the 6-line `# Advisory-only: ...` comment (`:575-580`) sitting above the deleted
    `continue-on-error` line — it claims `isAdvisory()` matches on the `"Advisory ("` naming
    convention; Phase 141 D-04 deleted that function. Replace it with a short, accurate one-line
    comment: promoted to merge-gating (Phase 142/VULN-03); disposition recorded in `MAINTAINING.md`
    § "Required Checks".

    **4. `.github/workflows/ci.yml` — rename `deps_audit_advisory`'s display name (D-06).**
    Change `name: Deps Audit Advisory (Elixir 1.18 / OTP 27)` (`:571`) to
    `name: Deps Audit (Elixir 1.18 / OTP 27)`. The job KEY `deps_audit_advisory:` (`:570`) is
    UNCHANGED — only the human-readable `name:` value renames. `hex_audit`'s name is unchanged (it
    was never "Advisory"-named). `scripts/setup_branch_protection.sh` is NOT touched (D-06 — branch
    protection stays exactly `{CI Green, Guard Release Trigger}`).

    **5. `test/support/ci_lanes.ex` — the four bucket edits (D-05 sites 4a-4d).**
    - `@required_lanes`: add `"Hex Audit (Elixir 1.18 / OTP 27)"` and `"Deps Audit (Elixir 1.18 /
      OTP 27)"` (the RENAMED string — never re-add the old "Advisory" name here).
    - `@advisory_lanes_ci`: remove BOTH `"Hex Audit (Elixir 1.18 / OTP 27)"` and
      `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"`. Do not re-add the renamed form — a required
      lane's local-parity coverage is already guaranteed via `required_lanes()`'s union into
      `ci_parity_drift_test.exs`'s `all_lanes/0`; none of the five pre-existing required lanes appear
      in `@advisory_lanes_ci` either, so this keeps the pattern consistent.
    - `@advisory_classified_lanes`: remove `"Deps Audit Advisory (Elixir 1.18 / OTP 27)"` (its only
      member from this pair — `"Hex Audit ..."` was never in this bucket).
    - `@publish_gating_lanes`: remove `"Hex Audit (Elixir 1.18 / OTP 27)"` (its only member from this
      pair — `"Deps Audit Advisory ..."` was never in this bucket).
    Leave the module's `@moduledoc` "Intentional exclusions from the parity claim" list unchanged —
    neither lane was ever listed there.

    **6. `.github/workflows/publish-hex.yml` — the three array edits (D-05 site 5).**
    - `REQUIRED_LANES` (`:204-210`): add `'Hex Audit (Elixir 1.18 / OTP 27)'` and
      `'Deps Audit (Elixir 1.18 / OTP 27)'` (renamed).
    - `ADVISORY_LANES` (`:228-233`): remove `'Deps Audit Advisory (Elixir 1.18 / OTP 27)'`.
    - `PUBLISH_GATING_LANES` (`:238-252`): remove `'Hex Audit (Elixir 1.18 / OTP 27)'`.
    `STRUCTURAL_LANES` is untouched.

    **7. `MAINTAINING.md` — flip both disposition rows (D-05 site 6).**
    `deps_audit_advisory` row: display name -> `Deps Audit (Elixir 1.18 / OTP 27)`; classification
    `advisory` -> `required`; disposition `promote` -> `keep-with-reason`; reason -> "Promoted from
    advisory (Phase 142/VULN-03) and renamed from 'Deps Audit Advisory' to 'Deps Audit' — a
    merge-gating lane named 'Advisory' is the signal-honesty defect this milestone exists to fix
    (D-06). The shared allowlist (Phase 142/VULN-05) makes it trustworthy enough to merge-gate."
    `hex_audit` row: classification `publish-gating` -> `required`; disposition `promote` ->
    `keep-with-reason`; reason -> "Promoted from publish-gating (Phase 142/VULN-03): the shared
    allowlist (`Mailglass.SupplyChain.AcceptedAdvisories`, Phase 142/VULN-05) makes the lane
    trustworthy enough to merge-gate — a non-accepted advisory now blocks a PR, not just a Hex
    publish." The table stays exactly 24 rows (only these two rows' cells change).

    **8. `test/scripts/lane_classification_drift_test.exs` — three hardcoded counts (D-05 sites 7-8).**
    - The `ADVISORY_LANES` size assertion (currently `== 4`) becomes `== 3`.
    - The `PUBLISH_GATING_LANES` size assertion (currently `== 13`) becomes `== 12`.
    - The `REQUIRED_LANES` size assertion inside the anti-vacuity test (currently `== 5`) becomes
      `== 7`.
    Do not touch the `all_classified_lanes/0` totality test (`:229-241`) — it has no hardcoded number
    and will pass automatically once the four buckets above total 24 (7+3+12+2).

    **9. `test/scripts/ci_parity_drift_test.exs` — the anti-vacuity `required_lanes()` count (D-05
    site 9).** Change `assert length(Mailglass.CILanes.required_lanes()) == 5` (`:161`) to `== 7`.

    **Stage and commit all nine edits as ONE commit.** Suggested commit type: `ci(142-04):`.
  </action>

  <verify>
    <automated>MIX_ENV=test mix test test/scripts/ --warnings-as-errors && mix verify.ci_lane_contract</automated>
  </verify>

  <acceptance_criteria>
    - `MIX_ENV=test mix test test/scripts/ --warnings-as-errors` exits 0 — every drift meta-test
      (`lane_classification_drift_test.exs`, `ci_parity_drift_test.exs`, `required_checks_test.exs`)
      passes with the new counts.
    - `MIX_ENV=test mix test test/scripts/conformance_advisory_test.exs --warnings-as-errors` exits 0
      and the test refuting `continue-on-error: true` in the advisory step block still passes,
      unaffected by this promotion (sanity check that D-07's deletion didn't collaterally touch the
      unrelated `deps_audit_advisory`-adjacent conformance step).
    - `awk '/^  deps_audit_advisory:$/,/^  installer_golden_gate:$/' .github/workflows/ci.yml | grep -c "continue-on-error: true"`
      returns `0` — confined to the `deps_audit_advisory` job block specifically (not a whole-file
      count, since other jobs may legitimately keep `continue-on-error`).
    - `awk '/^  deps_audit_advisory:$/,/^  installer_golden_gate:$/' .github/workflows/ci.yml | grep -c "isAdvisory"`
      returns `0` — the stale comment is gone.
    - `grep -c "name: Deps Audit (Elixir 1.18 / OTP 27)" .github/workflows/ci.yml` returns `1`;
      `grep -c "Deps Audit Advisory" .github/workflows/ci.yml test/support/ci_lanes.ex
      .github/workflows/publish-hex.yml MAINTAINING.md` returns `0` (fully renamed everywhere, no
      leftover old name).
    - `mix verify.ci_lane_contract` exits 0.
    - `git log -1 --stat` shows exactly one new commit touching all six files in `<files>` — the
      atomic-commit requirement (D-04), verified structurally rather than assumed.
    - `git diff HEAD~1 -- scripts/setup_branch_protection.sh` is empty (D-06 — untouched).
    - `git diff HEAD~1 -- mix.exs test/scripts/ci_parity_drift_test.exs` shows no `severity` or
      `HIGH` threshold literal introduced anywhere (D-08 negative confirmation — grep the diff, not
      the file, since the file legitimately contains the word "HIGH" in comments/reason text from
      142-01 unrelated to this check).
  </acceptance_criteria>

  <done>
    `Hex Audit` and `Deps Audit` are both merge-gating: present in `ci.yml`'s `ci_green.needs`,
    present in `Mailglass.CILanes.required_lanes/0`, and no longer masked by `continue-on-error`. All
    drift meta-tests agree. The promotion landed as one atomic commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → merge-gating lane set | The exact boundary this task widens; a bug here either fails to block a bad PR or blocks every good one. |
| `ci_lanes.ex` ↔ `publish-hex.yml` ↔ `MAINTAINING.md` | Three surfaces this commit must move together — the registry-disagreement defect class Phase 141 closed and this promotion must not reopen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-05 | Denial of Service (self-inflicted) | `ci_green.needs`, `Mailglass.CILanes.required_lanes/0` | high | mitigate | 142-03's checkpoint (precondition of this task) already proved the promoted lane is genuinely green with cowlib in scope; this task's own `<precondition>` re-asserts that SUMMARY exists before starting. |
| T-142-08 | Tampering | the atomic-commit boundary itself | high | mitigate | D-04 is enforced structurally: one task, one commit, verified post-hoc via `git log -1 --stat` in `<acceptance_criteria>` rather than trusted by convention. |
| T-142-SC | Tampering | npm/pip/cargo installs | high | accept | Not applicable — no new dependency this phase. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/scripts/ --warnings-as-errors` exits 0.
- `mix verify.ci_lane_contract` exits 0.
- `mix ci.fast` green.
- Exactly one commit lands, touching all six files.
</verification>

<success_criteria>
- ROADMAP criterion 2 is met: both audit lanes are merge-gating; a PR with a new HIGH-severity
  advisory blocks; a PR touching only the already-accepted cowlib advisories merges cleanly.
- No lane other than these two changed classification (Phase 141's 24-row table is preserved
  everywhere else — this phase touches exactly the two `promote` rows it recorded).
</success_criteria>

<output>
Create `.planning/phases/142-supply-chain-remediation-gating/142-04-SUMMARY.md` when done. Record
the commit SHA and confirm `git log -1 --stat` shows the six-file atomic commit.
</output>
</content>
