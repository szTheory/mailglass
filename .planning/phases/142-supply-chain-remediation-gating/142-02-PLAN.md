---
phase: 142-supply-chain-remediation-gating
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VULN-02]

must_haves:
  truths:
    - "`gh pr list --repo szTheory/mailglass --state open --json number,author,autoMergeRequest` returns zero `app/dependabot`-authored entries with a non-null `autoMergeRequest`, OR each remaining one carries a recorded reason in this plan's SUMMARY."
    - "PR #96 (`bump igniter 0.8.1 -> 0.8.2`) is dispositioned as a rebase-or-close decision, never a blind re-trigger of auto-merge — it is the one PR with a genuine merge conflict (`mergeable: CONFLICTING`, `mergeStateStatus: DIRTY`), not merely a stale base (F6)."
    - "PRs #115 and #125 (both bumping `phoenix_live_view` to 1.2.8, in different sibling directories) are dispositioned independently — each may merge on its own — and are NOT treated as a duplicate pair to collapse into one action. [edge: adjacency, VULN-02]"
    - "The disposition step re-queries `gh pr list` live rather than trusting the frozen 13-PR count from CONTEXT.md/RESEARCH.md; if the live open-dependabot-PR set differs from the 13 named there, the discrepancy is investigated and recorded, not silently ignored. [edge: empty, VULN-02]"
    - "Where one PR is superseded by an already-merged sibling (e.g. #114 `credo` in `mailglass_admin` vs. the already-merged #78 `credo` in root), the disposition table records the superseding relationship BEFORE the superseded PR is closed, so a later reader can trace WHY, not just THAT. [edge: ordering, VULN-02]"
    - "Maintainer PR #132 (auto-merge armed, `mergeStateStatus: BEHIND`) is flagged as adjacent-but-out-of-scope in the SUMMARY and is NOT dispositioned as part of this plan — it is not a dependency PR."
  artifacts: []
  key_links:
    - "This plan touches zero repository source files — its artifact is the GitHub PR state itself plus this plan's own SUMMARY.md disposition table, per D-12's 'no new `.planning/` register' choice (the phase artifact IS the audit trail)."
  prohibitions:
    - requirement_id: VULN-02
      category: safety
      status: unverified
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT disposition the 13 dependabot PRs via a blanket merge-all or close-all action. Each PR's disposition (merge, or close with a recorded reason) must be individually decided and individually recorded — a single bulk `gh pr merge` loop with no per-PR review is exactly the failure mode Pitfall 4 and D-12 exist to prevent (RESEARCH.md `.planning/research/v2.2/PITFALLS.md:676-681`)."
---

<objective>
Disposition every open dependabot PR with auto-merge enabled as of 2026-07-28 — merged or closed
with a recorded reason, none left indeterminate (VULN-02) — one PR at a time, using live
`gh`-verified state rather than the frozen research snapshot.

Purpose: `.planning/research/v2.2/PITFALLS.md:676-681` and CONTEXT.md D-12 are explicit that blanket
processing is wrong in both directions: several PRs are visibly superseded by already-merged
siblings (stale-base merge risk), while others (e.g. `#108`, the SHA-pinned `erlef/setup-beam` bump)
are genuinely wanted. F6's live re-verification found `#96` carries a real merge conflict
(`CONFLICTING`/`DIRTY`), not just a stale base — it needs a rebase-or-close decision, not a blind
re-arm of auto-merge.

Output: 13 dependabot PRs (`#131, #130, #125, #124, #116, #115, #114, #112, #111, #108, #106, #96,
#95`) each merged or closed with a reason, recorded in this plan's SUMMARY.md disposition table.

**Decisions implemented here:** D-12 (exactly these 13 PRs, one at a time, recorded reasons, no
blanket action) · D-13 Wave 1 (pure GitHub-side work, zero file overlap with 142-01/142-03/142-04).

**This plan runs in parallel with 142-01** — same wave, disjoint file sets (this plan touches no
repository file at all).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-supply-chain-remediation-gating/142-CONTEXT.md
@.planning/phases/142-supply-chain-remediation-gating/142-RESEARCH.md
</context>

## Artifacts this phase produces

(Reproduced from 142-01 for the mandatory per-plan section; this plan itself creates no new
repository symbols — its output is GitHub PR state plus this plan's own SUMMARY.md.)

| Symbol | Kind | Created in |
|---|---|---|
| `Mailglass.SupplyChain.AcceptedAdvisories`, `mix mailglass.audit` | module/task | 142-01/02 |
| `Deps Audit (Elixir 1.18 / OTP 27)` | renamed display name | 142-04 |
| `## Dependency Advisory Triage` | `MAINTAINING.md` section | 142-05 |

## Live PR state (re-verified during planning, 2026-07-28)

`gh pr list --repo szTheory/mailglass --state open --json number,author,autoMergeRequest,mergeable,mergeStateStatus`
returned exactly the 13 dependabot PRs D-12 names, byte-identical to CONTEXT.md and F6:

| PR | mergeable | mergeStateStatus | Note |
|---|---|---|---|
| #131, #130, #125, #124, #116, #115, #114, #112, #111, #108, #106, #95 | MERGEABLE | BEHIND | stale base only |
| **#96** | **CONFLICTING** | **DIRTY** | genuine merge conflict — needs rebase or close, not a re-trigger |

Also present but explicitly OUT of VULN-02's scope: **#132** (maintainer PR, docs guides, auto-merge
armed, `BEHIND` — flag its state, do not disposition it here); **#129**, **#104** (maintainer PRs,
`CONFLICTING`/`DIRTY`, `autoMergeRequest: null` — irrelevant to VULN-02).

**Before acting, re-run the `gh pr list` command above** — this table is a planning-time snapshot;
treat it as the starting hypothesis, not ground truth for execution (must_haves' "empty" edge above).

## Known supersession/duplication signals (from RESEARCH, verify before acting)

- **#114** (`credo` bump in `mailglass_admin`) is visibly superseded by the already-merged **#78**
  (`credo` bump in root) — verify #78's merge status and the actual dependency delta before deciding
  merge vs. close.
- **#115** and **#125** both bump `phoenix_live_view` to `1.2.8`, but in DIFFERENT sibling
  directories (`mailglass_admin` and `mailglass_inbound` respectively, or similar — confirm exact
  directories via `gh pr view`) — these are independent PRs on independent lockfiles, not a
  duplicate pair. Disposition each on its own merits.
- **#108** (`erlef/setup-beam` bump) is genuinely wanted — the action is SHA-pinned in every
  workflow per CLAUDE.md's "All third-party GitHub Actions pinned to commit SHA" convention; a
  version bump PR here is exactly the intended Dependabot flow, not noise.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Disposition all 13 dependabot PRs individually</name>
  <what-built>
    Nothing built — this is a disposition pass over live GitHub state, requiring judgment RESEARCH.md
    and CONTEXT.md explicitly say an automated agent should not make unsupervised (Pitfall 4: several
    PRs are visibly superseded, one has a real conflict, one is a genuinely-wanted SHA-pin bump).
  </what-built>
  <how-to-verify>
    For each of the 13 PRs (`#131, #130, #125, #124, #116, #115, #114, #112, #111, #108, #106, #96,
    #95`):
    1. `gh pr view <N> --repo szTheory/mailglass` — read the title, base branch currency, and diff
       summary.
    2. Decide: **merge** (rebase/re-arm auto-merge if `BEHIND` but otherwise clean and wanted) or
       **close** (`gh pr close <N> --comment "<specific reason>"` — e.g. "superseded by #78", "base
       diverged too far from the 2026-07-28 remediation, re-open if still needed after a rebase",
       "duplicate of the already-merged X").
    3. For **#96** specifically: attempt a rebase (`gh pr checkout 96 && git rebase origin/main &&
       git push --force-with-lease`) OR close with a reason naming the conflict — do not re-trigger
       auto-merge on a `CONFLICTING` PR without resolving the conflict first.
    4. For **#108**: confirm it is a clean SHA-pin version bump for `erlef/setup-beam` and merge it —
       this is the "genuinely wanted" case Pitfall 4 warns against accidentally closing.
    5. Record every disposition (PR number, action, reason) in a table in this plan's SUMMARY.md.
    6. Confirm #132 is left untouched (out of scope) and note its state in the SUMMARY.
  </how-to-verify>
  <resume-signal>
    Paste the completed 13-row disposition table (PR, action, reason), or type "approved" once the
    table is recorded in SUMMARY.md.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm zero indeterminate dependabot PRs remain, write the disposition table</name>

  <files>.planning/phases/142-supply-chain-remediation-gating/142-02-SUMMARY.md</files>

  <read_first>
    - This plan's Task 1 output (the disposition decisions just made)
    - `.planning/phases/142-supply-chain-remediation-gating/142-VALIDATION.md` § "Manual-Only
      Verifications" — the VULN-02 row's exact re-query command
  </read_first>

  <action>
    Re-run `gh pr list --repo szTheory/mailglass --state open --json
    number,author,autoMergeRequest` and confirm zero `app/dependabot`-authored entries carry a
    non-null `autoMergeRequest` — OR, for any that remain open by deliberate choice (e.g. awaiting a
    maintainer follow-up), confirm each carries a recorded reason distinguishing it from an
    indeterminate state. Write the final disposition table into
    `142-02-SUMMARY.md` with columns `PR | Title | Action | Reason`, covering all 13 PRs plus a note
    on #132's out-of-scope state.
  </action>

  <verify>
    <automated>gh pr list --repo szTheory/mailglass --state open --json number,author,autoMergeRequest --jq '[.[] | select(.author.login == "dependabot[bot]" or .author.login == "app/dependabot") | select(.autoMergeRequest != null)] | length'</automated>
  </verify>

  <acceptance_criteria>
    - The `gh pr list` command above returns `0` — no dependabot PR remains auto-merge-armed and
      indeterminate — OR the SUMMARY table names a specific, recorded reason for each remaining one.
    - `142-02-SUMMARY.md` contains a disposition row for all 13 named PRs plus a note for #132.
    - No SUMMARY row reads a generic reason like "cleanup" or "housekeeping" — every reason names the
      specific fact driving the decision (superseded-by-PR-N, genuine conflict needing rebase,
      SHA-pinned action bump kept, base diverged N+ weeks, etc.).
  </acceptance_criteria>

  <done>
    Every one of the 13 dependabot PRs is merged or closed with an individually recorded reason;
    the live `gh pr list` re-query proves zero indeterminate auto-merge-armed dependabot PRs remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot PR → auto-merge | An automated bot proposes a merge; a stale base or genuine conflict crossing this boundary unreviewed is the risk this plan exists to close. |
| Maintainer judgment → `gh pr merge`/`close` | The only mitigation for "blanket processing" is a human decision per PR — this plan's entire task shape is that mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-03 | Tampering | dependabot auto-merge queue | high | mitigate | D-12's per-PR disposition, never blanket merge-all/close-all; F6 flags #96's genuine conflict specifically so it is never blindly re-armed. |
| T-142-07 | Repudiation | disposition record | medium | mitigate | Every action is recorded with a specific reason in SUMMARY.md — no PR closes or merges silently. |
</threat_model>

<verification>
- `gh pr list --repo szTheory/mailglass --state open --json number,author,autoMergeRequest` shows
  zero indeterminate dependabot entries, or each is explained in SUMMARY.md.
- `142-02-SUMMARY.md` exists with a complete 13-row (+ #132 note) disposition table.
</verification>

<success_criteria>
- ROADMAP criterion 4 is met: every dependabot PR left auto-merge-armed on 2026-07-28 is confirmed
  merged or closed with a recorded reason; none remains indeterminate.
- #96's genuine conflict is resolved deliberately, not by a blind re-trigger.
- #132 is flagged, not silently dispositioned as if it were a dependency PR.
</success_criteria>

<output>
Create `.planning/phases/142-supply-chain-remediation-gating/142-02-SUMMARY.md` when done, with the
full disposition table.
</output>
</content>
