---
phase: 142-supply-chain-remediation-gating
plan: 05
type: execute
wave: 4
depends_on: ["142-04"]
files_modified:
  - MAINTAINING.md
autonomous: true
requirements: [VULN-04]

must_haves:
  truths:
    - "`MAINTAINING.md` carries a `## Dependency Advisory Triage` section, placed after `## Required Checks` and adjacent to `## Security Response SLA` (D-11's hard placement constraint), naming who reads raw `mix hex.audit`/`mix deps.audit` output (not just the dependabot PR queue), how often, and the response expectation by severity."
    - "The section states plainly that Dependabot cannot auto-file a Hex transitive-dependency fix requiring a parent-package version bump — documented upstream Dependabot behavior, not a repo defect — and that reading raw audit output directly is therefore the only path for that class of advisory (the literal `hpax` precedent this milestone exists to close)."
    - "`lane_classification_drift_test.exs`'s disposition-table parser still finds exactly 24 rows after this edit — the new section landed OUTSIDE the `## Required Checks` table's bounds, not inside it."
    - "The cadence's severity-response numbers do not exceed what a single, unpaid maintainer can sustainably keep — matching the existing Security Response SLA's 'written to be kept rather than aspired to' ethos, not an aspirational commitment. [edge: concurrency, VULN-04]"
  artifacts: []
  key_links:
    - "The section MUST land after the `## Required Checks` heading's own section boundary — `lane_classification_drift_test.exs`'s `find_required_checks_section/1` bounds that section by splitting on the literal token `\"\\n## \"` and taking everything up to the NEXT such heading (`## Bus Factor & Continuity` today); inserting anywhere before that boundary — even after the 24-row table but before the next `## ` heading — would silently fold the new section's prose into the table parser's scanned range and could corrupt the row count."
  prohibitions:
    - requirement_id: VULN-04
      category: transparency
      status: unverified
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT state a triage response-time commitment in the new section more aggressive than a single maintainer can sustainably keep. The existing `## Security Response SLA` is explicit that its numbers are 'written to be kept rather than aspired to' — the new section must match that honesty, not read as marketing."
---

<objective>
Document VULN-04's triage cadence — who reads raw advisory-scanner output, how often, and the
response expectation by severity — as a new `## Dependency Advisory Triage` section in
`MAINTAINING.md`, placed exactly where D-11 requires so the section-boundary parser that guards the
24-row disposition table stays correct.

Purpose: dependabot only ever proposes a fix for a DIRECT dependency. The `hpax` case this milestone
opened on — a transitive advisory with no PR possible — has no automated signal path today; a human
has to read `mix hex.audit`/`mix deps.audit` output directly. VULN-04 makes that expectation
explicit, named, and scheduled, rather than an unwritten assumption.

Output: `MAINTAINING.md` § "## Dependency Advisory Triage".

**Decisions implemented here:** D-11 (placement after `## Required Checks`, adjacent to `##
Security Response SLA`, a hard constraint driven by the disposition-table parser's section-boundary
technique — not a stylistic preference).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/142-supply-chain-remediation-gating/142-CONTEXT.md
@.planning/phases/142-supply-chain-remediation-gating/142-04-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol | Kind | Created in |
|---|---|---|
| `Mailglass.SupplyChain.AcceptedAdvisories`, `mix mailglass.audit` | module/task | 142-01/02 |
| `Deps Audit (Elixir 1.18 / OTP 27)` | renamed display name | 142-04 |
| `## Dependency Advisory Triage` | `MAINTAINING.md` section (this plan) | 142-05 |

<tasks>

<task type="auto">
  <name>Task 1: Write the `## Dependency Advisory Triage` section</name>

  <files>MAINTAINING.md</files>

  <read_first>
    - `MAINTAINING.md:132` (`## Required Checks` heading), `:190-217` (the 24-row disposition table,
      already edited by 142-04), `:236` (`## Bus Factor & Continuity` — the NEXT `## ` heading after
      Required Checks, confirming the section's parsed bounds), `:296` (`## Security Response SLA`,
      the adjacent existing section whose SLA-number style to match), `:253` (`## Retract Decision
      Tree`, the section between Required Checks and Security Response SLA — confirms two other
      sections already sit in that gap, so a third insertion there is not unusual)
    - `test/scripts/lane_classification_drift_test.exs:595-626` — `find_required_checks_section/1`
      and `parse_disposition_table/1`: the section is bounded by splitting on `"\n## "` and taking
      the chunk starting with `"Required Checks"`, up to the NEXT `"\n## "` token. This is the exact
      mechanism D-11's placement constraint protects.
    - `142-CONTEXT.md` § D-11 (full text) — the four literal content requirements: who, what/how
      often, response-by-severity, and the explicit Dependabot-transitive-limitation statement
  </read_first>

  <action>
    Insert a new `## Dependency Advisory Triage` section into `MAINTAINING.md` immediately before
    `## Security Response SLA` (i.e. after `## Retract Decision Tree`'s content ends, right at the
    line preceding `## Security Response SLA`'s heading at `:296`) — this places it strictly after
    the `## Required Checks` section's own boundary (which ends at `## Bus Factor & Continuity`,
    `:236`), so the disposition-table parser's scanned range is untouched.

    Content, matching D-11's four literal requirements exactly:
    - **Opening paragraph:** advisories don't only arrive as a dependabot PR — a transitive
      dependency's advisory with no available parent-bump PR (the `hpax` case this milestone exists
      to close) requires reading raw audit output directly. State plainly: Dependabot cannot
      auto-file a Hex transitive-dependency fix that requires bumping a PARENT package's version
      constraint; this is documented upstream Dependabot behavior, not a repo defect, and it is why
      this section exists.
    - **Who:** the sole maintainer (`szTheory`) — cross-reference the existing "## Bus Factor &
      Continuity" section's single-maintainer framing rather than restating it.
    - **What:** run `mix mailglass.audit --kind hex` and `mix mailglass.audit --kind deps` locally
      (or read the `Hex Audit` / `Deps Audit` CI job logs on `main`) across all three Mix projects —
      not just the open dependabot PR queue, which only ever proposes a fix for a direct dependency.
    - **How often:** weekly, aligned with `.github/dependabot.yml`'s existing weekly schedule, plus
      whenever `Hex Audit` or `Deps Audit` goes red on `main` (now merge-gating per 142-04, so a red
      state is immediately visible).
    - **Response expectation by severity** — explicitly bounded to what a single maintainer can
      sustainably keep, matching `## Security Response SLA`'s "written to be kept rather than
      aspired to" ethos (this is the values/transparency prohibition above — do not over-promise):
      HIGH/CRITICAL within 14 days (patch or a dated, recorded allowlist exception — mirrors the
      existing "Mitigation or workaround for critical issues" SLA number); MEDIUM within 30 days;
      LOW at the next scheduled triage, no forced timeline.
    - **Missed-cycle guarantee** (VULN-04's concurrency edge): a missed cycle does not lose signal —
      the next `mix hex.audit`/`mix deps.audit` run re-surfaces every outstanding advisory from
      scratch, so nothing is silently dropped, only delayed. Note explicitly that mailglass is
      single-maintainer (cross-reference "## Bus Factor & Continuity"), so concurrent-maintainer
      triage races are out of scope for this cadence, consistent with the project's already-documented
      posture rather than a newly invented gap.
  </action>

  <verify>
    <automated>MIX_ENV=test mix test test/scripts/lane_classification_drift_test.exs --warnings-as-errors</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^## Dependency Advisory Triage" MAINTAINING.md` returns `1`.
    - `grep -A 30 "^## Dependency Advisory Triage" MAINTAINING.md | grep -c "cannot auto-file"`
      returns at least `1` — the Dependabot-transitive-limitation statement is present verbatim in
      spirit.
    - `grep -A 30 "^## Dependency Advisory Triage" MAINTAINING.md | grep -Ec "hex.audit|deps.audit"`
      returns at least `2` — both raw-output commands named.
    - `grep -A 40 "^## Dependency Advisory Triage" MAINTAINING.md | grep -Ec "HIGH|CRITICAL|MEDIUM|LOW"`
      returns at least `3` — response expectations are broken down by severity, not one blanket
      number.
    - `MIX_ENV=test mix test test/scripts/lane_classification_drift_test.exs --warnings-as-errors`
      exits 0 and, specifically, `"MAINTAINING.md's disposition table parses to exactly 24 non-empty
      rows"` passes — the new section did not land inside the parser's scanned range.
    - The new section's text appears strictly between `## Retract Decision Tree`'s content and `##
      Security Response SLA`'s heading: `awk '/^## Retract Decision Tree$/,/^## Security Response
      SLA$/' MAINTAINING.md | grep -c "^## Dependency Advisory Triage"` returns `1`.
  </acceptance_criteria>

  <done>
    `MAINTAINING.md` carries a complete, honest `## Dependency Advisory Triage` section satisfying
    all four of D-11's literal content requirements, placed so the 24-row disposition-table parser
    remains correct.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documented cadence → actual maintainer behavior | A documentation-only change; the risk is over-promising, not a code trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-02 | Information Disclosure / Repudiation (of due diligence) | `MAINTAINING.md` § "Dependency Advisory Triage" | medium | mitigate | The section explicitly names reading raw `mix hex.audit`/`mix deps.audit` output (not just the dependabot PR queue) as the only path for transitive advisories with no available PR — closing the `hpax` precedent's visibility gap. |
| T-142-09 | Repudiation (over-promising) | the SLA numbers in the new section | low | mitigate | Response times are bounded to match the existing Security Response SLA's "written to be kept" ethos — the minted prohibition above is the acceptance gate. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/scripts/lane_classification_drift_test.exs --warnings-as-errors`
  exits 0.
- `grep -A 20 '## Dependency Advisory Triage' MAINTAINING.md` shows who/what/how-often/response-by-
  severity/the-Dependabot-limitation, all present.
</verification>

<success_criteria>
- ROADMAP criterion 5 is met: a written triage cadence names who reads raw audit output, how often,
  and the response expectation by severity.
- The 24-row disposition table's parser is unaffected — a placement mistake here would be a
  self-inflicted regression of Phase 141's own machinery.
</success_criteria>

<output>
Create `.planning/phases/142-supply-chain-remediation-gating/142-05-SUMMARY.md` when done. This is
the phase's final plan — note in the SUMMARY that all five ROADMAP success criteria for Phase 142
are now satisfied, and confirm the folded todo (closed in 142-01) and the D-14 checkpoint evidence
(142-03) are both linked from here for a future milestone-audit read.
</output>
</content>
