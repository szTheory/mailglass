---
phase: 138-schema-prefix-no-search-path-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs
  - mailglass_inbound/lib/mailglass_inbound/internal/replay.ex
  - mailglass_inbound/lib/mailglass_inbound/execution.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
autonomous: true
requirements:
  - SCHEMA-04
must_haves:
  truths:
    - "Inbound replay extension points keep the facade as the default repo."
    - "When a raw repo is supplied to inbound replay/load/selector paths, every mailglass_inbound table read receives `prefix: MailglassInbound.Config.schema()`."
    - "The raw-repo prefix contract is tested without relying on ambient connection search_path."
  artifacts:
    - path: "mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs"
      provides: "Raw-repo extension-point contract tests"
      contains: "MailglassInbound.SchemaPrefixContractTest"
    - path: "mailglass_inbound/lib/mailglass_inbound/internal/replay.ex"
      provides: "Explicit prefix opts for raw repo one/2 calls"
      contains: "schema_opts"
    - path: "mailglass_inbound/lib/mailglass_inbound/execution.ex"
      provides: "Explicit prefix opts for raw repo get/3 calls"
      contains: "schema_opts"
    - path: "mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex"
      provides: "Explicit prefix opts for raw repo selector all/2 call"
      contains: "schema_opts"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/internal/replay.ex"
      to: "mailglass_inbound/lib/mailglass_inbound/config.ex"
      via: "schema_opts/0 returns prefix from MailglassInbound.Config.schema/0"
      pattern: "defp schema_opts"
    - from: "mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs"
      to: "mailglass_inbound/lib/mailglass_inbound/internal/replay.ex"
      via: "capture repo asserts opts on raw repo.one/2"
      pattern: "captured_opts"
---

<objective>
Make the inbound raw-repo extension-point contract explicit and tested: default callers stay on `MailglassInbound.Repo`, while supplied raw repos receive explicit schema prefix opts on table reads.

Purpose: SCHEMA-04 closes the facade-bypass risk for inbound replay and execution paths without changing public router/API behavior.
Output: Inbound contract tests plus local `schema_opts/0` helpers threaded through raw repo calls.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-RESEARCH.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-VALIDATION.md
@.planning/phases/138-schema-prefix-no-search-path-hardening/138-PATTERNS.md
@mailglass_inbound/lib/mailglass_inbound/repo.ex
@mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
@mailglass_inbound/lib/mailglass_inbound/internal/replay.ex
@mailglass_inbound/lib/mailglass_inbound/execution.ex
@mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
@mailglass_inbound/test/mailglass_inbound/repo_prefix_test.exs
@mailglass_inbound/test/mix/tasks/mailglass_inbound_replay_test.exs
</context>

## Artifacts this phase produces

| Kind | Symbol / path | Produced in plan |
|------|---------------|------------------|
| Test module | `Mailglass.SchemaPrefixHardeningTest` in `test/mailglass/schema_prefix_hardening_test.exs` | 138-01 |
| Test helper module | `Mailglass.SchemaPrefixHardeningTest.PrefixedWrapperMigration` | 138-01 |
| Test tag | `:schema_prefix` module/test tag for focused runtime proofs | 138-01 |
| Static check module | `Mailglass.Credo.RawRepoPrefixContract` in `credo_checks/raw_repo_prefix_contract.ex` | 138-03 |
| Static check test module | `Mailglass.Credo.RawRepoPrefixContractTest` | 138-03 |
| Inbound contract test module | `MailglassInbound.SchemaPrefixContractTest` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Internal.Replay` | 138-02 |
| Helper function | `schema_opts/0` in `MailglassInbound.Execution` | 138-02 |
| Helper function | `schema_opts/0` in `Mix.Tasks.Mailglass.Inbound.Replay` | 138-02 |
| Mix alias | `mix verify.schema_prefix` | 138-04 |
| CI/documentation wording | Advisory matrix comments naming the dual-schema matrix as a broad canary and `mix verify.schema_prefix` as the focused proof | 138-04 |

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Runtime reads/writes touching mailglass tables target configured schema without search_path help | 138-01, 138-02, 138-03, 138-04 | COVERED | Hostile runtime tests, inbound raw-repo opts, static guard, and focused alias together satisfy the phase goal. |
| REQ | SCHEMA-01 | `Mailglass.Webhook.Replay` projection update hostile-path proof | 138-01 | COVERED | Task 1 creates proof and source fix. |
| REQ | SCHEMA-02 | Unsubscribe replay/idempotency conflict lookup hostile-path proof | 138-01 | COVERED | Task 2 creates proof and source fix. |
| REQ | SCHEMA-03 | Raw repo calls and callbacks are prefixed/facade-routed/allowlisted with recurrence guard | 138-03 | COVERED | Remaining projection updates plus custom Credo guard. |
| REQ | SCHEMA-04 | Inbound repo-option extension points default to facade and prove raw-repo prefix contract | 138-02 | COVERED | Raw repo tests and `schema_opts/0` changes. |
| REQ | GATE-01 | Focused schema-prefix verification lane | 138-04 | COVERED | `mix verify.schema_prefix` runs runtime and static proofs. |
| REQ | GATE-02 | Dual-schema advisory matrix remains a canary | 138-04 | COVERED | Workflow comments and source assertions name canary semantics. |
| RESEARCH | - | Include fake adapter and webhook reconciler projection updates under SCHEMA-03 | 138-03 | COVERED | Both projection Multi updates get explicit opts. |
| RESEARCH | - | Inbound raw repo extension points need local schema opts | 138-02 | COVERED | This plan owns all three discovered inbound files. |
| RESEARCH | - | No new packages; no package install checkpoint required | all | COVERED | No install tasks in this phase. |
| RESEARCH | - | Schema push detection | all | COVERED | No ORM schema, migration, collection, entity, Prisma, Drizzle, Supabase, or TypeORM files are modified; no schema push task is injected. |
| CONTEXT | - | No phase CONTEXT.md exists | all | COVERED | Binding decisions come from ROADMAP, REQUIREMENTS, STATE, RESEARCH, VALIDATION, and PATTERNS. |
| STATE | v2.1 locked decision | Explicit Ecto prefix is correctness mechanism; search_path is only host/test convenience | 138-01, 138-02, 138-03 | COVERED | Tasks use explicit opts. |
| STATE | v2.1 locked decision | Surgical hostile lane and static guard instead of whole-suite no-search-path migration | 138-01, 138-03, 138-04 | COVERED | Focused proofs and guard only. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create inbound raw-repo prefix contract tests</name>
  <files>mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs</files>
  <read_first>
    @mailglass_inbound/test/mailglass_inbound/repo_prefix_test.exs
    @mailglass_inbound/test/mix/tasks/mailglass_inbound_replay_test.exs
    @mailglass_inbound/lib/mailglass_inbound/internal/replay.ex
    @mailglass_inbound/lib/mailglass_inbound/execution.ex
    @mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    @mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
  </read_first>
  <behavior>
    - SCHEMA-04 RED: `MailglassInbound.Internal.Replay.replay/2` with a supplied raw repo must pass `prefix: MailglassInbound.Config.schema()` to every `repo.one/2` read it performs.
    - SCHEMA-04 RED: `MailglassInbound.Execution.load/2` with a supplied raw repo must pass `prefix: MailglassInbound.Config.schema()` to both `repo.get/3` reads.
    - SCHEMA-04 RED: `Mix.Tasks.Mailglass.Inbound.Replay.run/2` selector resolution with a supplied raw repo must pass `prefix: MailglassInbound.Config.schema()` to `repo.all/2`.
  </behavior>
  <action>Create `mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs` with module `MailglassInbound.SchemaPrefixContractTest`, `use ExUnit.Case, async: false`, and the env/cache reset pattern from `MailglassInbound.RepoPrefixTest`. Add a process-capturing raw repo module that implements `one/2`, `get/3`, and `all/2`, records each opts list in process state, and returns queued structs when a test needs the code path to continue. Add tests for `Internal.Replay.replay/2`, `Execution.load/2`, and `Mix.Tasks.Mailglass.Inbound.Replay.run/2`. For replay, queue `%InboundRecord{}`, `%InboundEvidence{}`, and `%ExecutionRun{source: :fresh, outcome: :accept, mailbox: "MailglassInbound.TestMailbox"}` values and pass an execution stub that returns `{:ok, %{status: :replayed}}`. For execution load, queue a record and evidence for the two `get/3` calls. For mix task selector resolution, call `run(["--tenant", "tenant-a", "--dry-run", "--no-start"], repo: CaptureRepo)` and have `all/2` return one UUID so the task reports a dry run without invoking replay. Assert every captured opts list has `Keyword.get(opts, :prefix) == MailglassInbound.Config.schema()`. Keep assertions to IDs/counts/status atoms and avoid subject/body/header diagnostics.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/schema_prefix_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The new test file contains `MailglassInbound.SchemaPrefixContractTest` and a capture repo that records opts for `one/2`, `get/3`, and `all/2`.
    - Tests cover `MailglassInbound.Internal.Replay.replay/2`, `MailglassInbound.Execution.load/2`, and `Mix.Tasks.Mailglass.Inbound.Replay.run/2` with a supplied raw repo.
    - Tests fail before production changes because captured opts lack `:prefix`.
    - The automated command exits 0 after Task 2.
  </acceptance_criteria>
  <done>SCHEMA-04 has focused raw-repo extension-point tests for every discovered inbound path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread inbound schema opts through raw repo extension points</name>
  <files>mailglass_inbound/lib/mailglass_inbound/internal/replay.ex, mailglass_inbound/lib/mailglass_inbound/execution.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex</files>
  <read_first>
    @mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex
    @mailglass_inbound/lib/mailglass_inbound/repo.ex
    @mailglass_inbound/lib/mailglass_inbound/internal/replay.ex
    @mailglass_inbound/lib/mailglass_inbound/execution.ex
    @mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    @mailglass_inbound/test/mailglass_inbound/schema_prefix_contract_test.exs
  </read_first>
  <behavior>
    - `repo.one(query, schema_opts())`, `repo.get(schema, id, schema_opts())`, and `repo.all(query, schema_opts())` are used for raw repo extension points while default facade behavior remains unchanged.
  </behavior>
  <action>In `mailglass_inbound/lib/mailglass_inbound/internal/replay.ex`, add a local `defp schema_opts, do: [prefix: MailglassInbound.Config.schema()]` helper matching `Ingress.Persist`, then change `load_record/3`, `load_evidence/3`, `latest_matched_fresh_run/3`, and `latest_fresh_run/3` so each pipeline ends with `repo.one(schema_opts())`. In `mailglass_inbound/lib/mailglass_inbound/execution.ex`, add the same local helper and change both `repo.get(InboundRecord, inbound_record_id)` and `repo.get(InboundEvidence, inbound_evidence_id)` calls in `load/2` to pass `schema_opts()`. In `mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex`, add the same local helper and change selector resolution to call `repo.all(schema_opts())`. Keep `Keyword.get(opts, :repo, MailglassInbound.Repo)` defaults intact and do not remove raw-repo test injection support.</action>
  <verify>
    <automated>cd mailglass_inbound &amp;&amp; mix test test/mailglass_inbound/schema_prefix_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three inbound files defines a local `schema_opts/0` returning `[prefix: MailglassInbound.Config.schema()]`.
    - `Internal.Replay` has no remaining bare `repo.one()` calls.
    - `Execution.load/2` raw repo `get` calls pass opts.
    - Inbound replay mix task selector resolution passes opts to `repo.all/2`.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Inbound raw-repo extension points keep their facade default and enforce the explicit prefix contract when a raw repo is supplied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied repo -> inbound table reads | Raw repo options cross the facade boundary and must receive explicit schema opts. |
| CLI selectors -> database query | Mix task selectors are user input and must remain parameterized while receiving prefix opts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-05 | Tampering | `MailglassInbound.Internal.Replay` raw repo reads | mitigate | Add `schema_opts/0` and pass it to each `repo.one/2`; capture tests assert opts. |
| T-138-06 | Information Disclosure | `MailglassInbound.Execution.load/2` raw repo reads | mitigate | Add `schema_opts/0` and pass it to both `repo.get/3` calls to avoid cross-schema record confusion. |
| T-138-07 | Tampering | `mix mailglass.inbound.replay` selector resolution | mitigate | Keep Ecto query parameterization and pass `schema_opts/0` to `repo.all/2`. |
| T-138-08 | Information Disclosure | Inbound test diagnostics | mitigate | Contract tests assert prefixes/counts/statuses only; no subject/body/header/raw MIME output. |
</threat_model>

<verification>
Run `cd mailglass_inbound && mix test test/mailglass_inbound/schema_prefix_contract_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
SCHEMA-04 passes for all discovered inbound raw-repo extension points, and the default facade remains the default repo path.
</success_criteria>

<output>
Create `.planning/phases/138-schema-prefix-no-search-path-hardening/138-02-SUMMARY.md` when done.
</output>
