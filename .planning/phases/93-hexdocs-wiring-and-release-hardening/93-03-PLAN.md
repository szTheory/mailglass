---
phase: 93-hexdocs-wiring-and-release-hardening
plan: 03
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - .release-please-manifest.json
  - mix.exs
  - mailglass_admin/mix.exs
  - mailglass_inbound/mix.exs
  - .planning/STATE.md
  - CLAUDE.md
autonomous: true
requirements: [RELH-02]
must_haves:
  truths:
    - "Live Hex is confirmed at 1.6.2/1.6.2/1.3.1 before any version edit (D-13 external-dependency guard); if it contradicts, the blocker is recorded and reconciliation stops"
    - "The in-repo manifest, all three @version lines, and both core-dep pins are aligned to released reality 1.6.2/1.6.2/1.3.1 with inbound pinning core == 1.6.2 (already published, cannot red main)"
    - "The remote 1.6.x package tags are fetched and kept locally (real published releases, not deleted)"
    - ".planning release-state memory and CLAUDE.md current-state reflect the final version truth 1.6.2/1.6.2/1.3.1; all edits are non-release-triggering"
  artifacts:
    - path: ".release-please-manifest.json"
      provides: "manifest at released versions"
      contains: '1.6.2'
    - path: "mix.exs"
      provides: "core @version 1.6.2"
      contains: '@version "1.6.2"'
    - path: "mailglass_inbound/mix.exs"
      provides: "inbound @version 1.3.1 and core pin == 1.6.2"
      contains: '{:mailglass, "== 1.6.2"}'
    - path: ".planning/STATE.md"
      provides: "release-state truth corrected to 1.6.2/1.6.2/1.3.1"
      contains: '1.6.2'
  key_links:
    - from: ".release-please-manifest.json + 3 @version + 2 pins"
      to: "live Hex 1.6.2/1.6.2/1.3.1"
      via: "Hex-greater-than-in-repo catch-up reconciliation (no new release)"
      pattern: '1.6.2'
    - from: "mailglass_inbound/mix.exs and mailglass_admin/mix.exs core pin"
      to: "published core 1.6.2"
      via: "== 1.6.2 (published, safe; never pin an unpublished core)"
      pattern: '== 1.6.2'
---

<objective>
Reconcile the 1.6.x release aftermath. Live Hex is authoritative at 1.6.2 / 1.6.2 / 1.3.1
(research VERDICT flips CONTEXT.md's premise: the release-state memory was CORRECT; the in-repo
state at 1.6.1/1.6.1/1.3.0 is the STALE side). This plan advances the in-repo source of truth
to match what is already published, fetches and keeps the real remote 1.6.x tags, and corrects
.planning release-state docs.

Purpose: the repo currently lies about being on 1.6.1 while Hex shipped 1.6.2/1.6.2/1.3.1
(the accidental train RELH-01 hardens against). Making the in-repo manifest/@version/pins match
released reality is a CATCH-UP, not a new release. This phase cuts NO Hex release; inbound 1.3.1
already carries == 1.6.2 on Hex, so pinning the working tree to == 1.6.2 is safe (published core,
cannot red main).

Output: manifest + three @version + two pins aligned to 1.6.2/1.6.2/1.3.1; remote tags fetched
and kept; STATE.md + CLAUDE.md current-state corrected. Implements RELH-02. D-13 guard encoded
as the first task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-hexdocs-wiring-and-release-hardening/93-CONTEXT.md
@.planning/phases/93-hexdocs-wiring-and-release-hardening/93-RESEARCH.md
@.planning/phases/93-hexdocs-wiring-and-release-hardening/93-PATTERNS.md

<interfaces>
Current in-repo (STALE) state and reconciliation target (extracted/verified 2026-06-13).

.release-please-manifest.json (whole file, current):
  { ".": "1.6.1", "mailglass_admin": "1.6.1", "mailglass_inbound": "1.3.0" }
  TARGET: { ".": "1.6.2", "mailglass_admin": "1.6.2", "mailglass_inbound": "1.3.1" }

@version lines (all at line 4):
  mix.exs:4                     @version "1.6.1"  ->  "1.6.2"
  mailglass_admin/mix.exs:4     @version "1.6.1"  ->  "1.6.2"
  mailglass_inbound/mix.exs:4   @version "1.3.0"  ->  "1.3.1"

core-dep pins (both currently == 1.6.1 -> == 1.6.2):
  mailglass_admin/mix.exs:142   {:mailglass, "== 1.6.1"}  ->  {:mailglass, "== 1.6.2"}
  mailglass_inbound/mix.exs:127 {:mailglass, "== 1.6.1"}  ->  {:mailglass, "== 1.6.2"}

BINDING comment blocks to respect (quote, do NOT edit): mailglass_inbound/mix.exs lines 114-124
(the fix(inbound): dance rule) and mailglass_admin/mix.exs lines 126-139 (literal-not-interpolation
+ release-please sed sync). Only the "== 1.6.1" literals change.

Live Hex truth (mix hex.info, 2026-06-13): mailglass 1.6.2, mailglass_admin 1.6.2,
mailglass_inbound 1.3.1; inbound 1.3.1 deps {:mailglass, "== 1.6.2"}. Remote tags
mailglass-v1.6.1/v1.6.2 and mailglass_admin-v1.6.1/v1.6.2 exist on origin (un-fetched locally).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm live Hex truth (D-13 external-dependency guard) and fetch+keep remote tags</name>
  <files>.planning/phases/93-hexdocs-wiring-and-release-hardening/93-03-SUMMARY.md</files>
  <read_first>
    - 93-CONTEXT.md D-11, D-12, D-13 (investigate Hex live first; reconcile only if settled; record the blocker and STOP if not settled)
    - 93-RESEARCH.md Open Item 2 (the 3-way reconciliation table, "Verdict on D-13: the train HAS settled", and "Tag disposition: FETCH and KEEP, do NOT delete")
    - .planning/STATE.md "Sequencing notes" (RELH-02 potentially blocked-on-external)
  </read_first>
  <action>
    This is the binding D-13 gate; it MUST run and pass before any version edit in Task 2/3. Query live Hex for all three packages: mix hex.info mailglass, mix hex.info mailglass_admin, mix hex.info mailglass_inbound. Confirm the latest published versions are EXACTLY mailglass 1.6.2, mailglass_admin 1.6.2, mailglass_inbound 1.3.1, AND that inbound 1.3.1 deps include {:mailglass, "== 1.6.2"} (run mix hex.info mailglass_inbound 1.3.1 to read its deps). If ALL of these confirm, the train has settled (research VERDICT) and reconciliation proceeds. If live Hex does NOT confirm 1.6.2/1.6.2/1.3.1 (e.g. a newer 1.6.x/1.7.x has landed, or a version is missing/mid-flight), STOP: do NOT guess version truth, record the blocker in the SUMMARY per D-13 (what Hex actually shows vs. expected, dated), and do NOT proceed to Task 2/3. On the happy path, run git fetch --tags origin so the local checkout gains mailglass-v1.6.1, mailglass-v1.6.2, mailglass_admin-v1.6.1, mailglass_admin-v1.6.2 (disposition = fetch + KEEP; these are real published releases referenced by source_ref in the tarballs, do NOT delete). Note the harmless quirk to document later: mailglass_admin-v1.6.1 exists on git but admin 1.6.1 was never published to Hex (linked-version tagging artifact). Do NOT run any mix command in reference/demo_app (swoosh lock drift).
  </action>
  <verify>
    <automated>mix hex.info mailglass 2>&amp;1 | grep -E '^\s+1\.6\.2'; git fetch --tags origin 2&gt;&amp;1 | tail -1; git tag --list 'mailglass-v1.6.2' | grep -q . &amp;&amp; echo TAG_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - mix hex.info mailglass shows 1.6.2 as the latest release; mix hex.info mailglass_admin shows 1.6.2; mix hex.info mailglass_inbound shows 1.3.1.
    - mix hex.info mailglass_inbound 1.3.1 deps include {:mailglass, "== 1.6.2"} (inbound's published pin matches the reconciliation target).
    - On the happy path: git tag --list 'mailglass-v1.6.2' returns a non-empty result (remote tag fetched and kept), and no tag was deleted (git tag --list 'mailglass-v1.6.1' is also present).
    - If Hex does NOT confirm 1.6.2/1.6.2/1.3.1: the SUMMARY records the blocker (actual vs expected, dated) and Tasks 2-3 are NOT executed (D-13 stop-short).
  </acceptance_criteria>
  <done>Live Hex confirmed at 1.6.2/1.6.2/1.3.1 with inbound pinning == 1.6.2, remote 1.6.x tags fetched and kept; OR the unsettled-train blocker is recorded and the plan stops short per D-13.</done>
</task>

<task type="auto">
  <name>Task 2: Align in-repo manifest, @version, and core-dep pins to released 1.6.2/1.6.2/1.3.1</name>
  <files>.release-please-manifest.json, mix.exs, mailglass_admin/mix.exs, mailglass_inbound/mix.exs</files>
  <read_first>
    - .release-please-manifest.json (current 1.6.1/1.6.1/1.3.0)
    - mix.exs:4, mailglass_admin/mix.exs:4, mailglass_inbound/mix.exs:4 (the @version lines)
    - mailglass_admin/mix.exs:142 and mailglass_inbound/mix.exs:127 (the == 1.6.1 pins) plus the binding comments at admin 126-139 and inbound 114-124
    - 93-PATTERNS.md section ".release-please-manifest.json + 3 @version + 2 pins" (lines 178-222, target values, the binding comment to respect, ordering/commit-type discipline)
    - 93-RESEARCH.md Open Item 2 "Encoded reconciliation ACTION" Encoding A (lines 240-270) and "Ordering constraint" (never pin an unpublished core; == 1.6.2 is published, safe)
  </read_first>
  <action>
    Gated on Task 1 confirming the settled train. Apply Encoding A (align in-repo to released reality, no version bump intended; this catch-up makes release-please find no pending bump because the manifest will equal what is already tagged/published). Update .release-please-manifest.json to { ".": "1.6.2", "mailglass_admin": "1.6.2", "mailglass_inbound": "1.3.1" }. Update the three @version lines: core mix.exs:4 to @version "1.6.2", admin mix.exs:4 to @version "1.6.2", inbound mix.exs:4 to @version "1.3.1". Update both core-dep pins to the released core: admin mix.exs:142 and inbound mix.exs:127 to {:mailglass, "== 1.6.2"}. Change ONLY the version literals; do NOT delete or edit the binding comment blocks (admin 126-139, inbound 114-124). The pin edit is a CATCH-UP to an already-published artifact (inbound 1.3.1 on Hex already carries == 1.6.2; core 1.6.2 IS published), so it can ride a non-bumping commit and cannot red main; never pre-bump a pin to an unpublished core. Commit type is chore(release): per D-06 / research (e.g. "chore(release): align in-repo manifest + @version + dep pins to released 1.6.2/1.6.2/1.3.1"); this touches real package files (mix.exs/manifest), is non-bumping, and is not all-guarded, so it PASSES the new RELH-01 lint trivially. Do NOT run mix in reference/demo_app.
  </action>
  <verify>
    <automated>jq -r '."."' .release-please-manifest.json | grep -qx 1.6.2 &amp;&amp; jq -r '.mailglass_inbound' .release-please-manifest.json | grep -qx 1.3.1; grep -q '@version "1.6.2"' mix.exs; grep -q '@version "1.3.1"' mailglass_inbound/mix.exs; grep -q '{:mailglass, "== 1.6.2"}' mailglass_inbound/mix.exs; grep -q '{:mailglass, "== 1.6.2"}' mailglass_admin/mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - Manifest: jq -r '."."' is 1.6.2, .mailglass_admin is 1.6.2, .mailglass_inbound is 1.3.1; file is valid JSON.
    - @version: grep -q '@version "1.6.2"' mix.exs and mailglass_admin/mix.exs both pass; grep -q '@version "1.3.1"' mailglass_inbound/mix.exs passes.
    - Pins: grep -q '{:mailglass, "== 1.6.2"}' both mailglass_admin/mix.exs and mailglass_inbound/mix.exs; grep -c '== 1.6.1' across both files returns 0.
    - Binding comments intact: grep -c 'fix(inbound):' mailglass_inbound/mix.exs is unchanged from before the edit (comment not deleted).
    - No mix run in reference/demo_app: git status --porcelain reference/demo_app/mix.lock shows no new drift from this task.
  </acceptance_criteria>
  <done>Manifest, all three @version, and both core-dep pins equal released reality 1.6.2/1.6.2/1.3.1 with inbound/admin pinning == 1.6.2; binding comments untouched; committed as chore(release): (non-bumping).</done>
</task>

<task type="auto">
  <name>Task 3: Correct .planning release-state memory and CLAUDE.md current-state to 1.6.2/1.6.2/1.3.1</name>
  <files>.planning/STATE.md, CLAUDE.md</files>
  <read_first>
    - .planning/STATE.md (any assertion of 1.6.1/1.6.1/1.3.0 or stale current-version language)
    - CLAUDE.md "What This Is" current-state paragraph (the live-version reconciliation line that may still say 1.5.1 / 1.6.1)
    - 93-RESEARCH.md Open Item 2 "Tag disposition" (document the mailglass_admin-v1.6.1-without-Hex-publish quirk) and "What CONTEXT.md got backwards" (memory was right, in-repo was stale)
    - 93-PATTERNS.md "Non-bumping commit-type discipline" (docs(state): for STATE.md skips CI path filters)
  </read_first>
  <action>
    Gated on Task 1/2. Correct the .planning release-state truth to the final 1.6.2/1.6.2/1.3.1 wherever it is asserted stale. In .planning/STATE.md, update any current-version statement that reads 1.6.1/1.6.1/1.3.0 (or otherwise contradicts released reality) to 1.6.2/1.6.2/1.3.1, and add a Session Continuity / Accumulated Context note recording the RELH-02 reconciliation outcome: Hex was authoritative (memory correct, in-repo stale), in-repo advanced to 1.6.2/1.6.2/1.3.1, remote 1.6.x tags fetched + kept (not deleted), and the harmless mailglass_admin-v1.6.1-git-tag-without-Hex-publish quirk documented. In CLAUDE.md, update the "What This Is" current-state paragraph so the live-version line reflects 1.6.2/1.6.2/1.3.1 (do not rewrite unrelated prose; only correct the version-truth claim). Do NOT edit any .planning/milestones/ archive (frozen). Use commit type docs(state): for the STATE.md edit (skips CI path filters) and docs: for CLAUDE.md; both are non-bumping and a .planning-only docs(state): PR passes the new RELH-01 lint at its non-bump short-circuit.
  </action>
  <verify>
    <automated>grep -rn '1.6.1/1.6.1/1.3.0' .planning/STATE.md CLAUDE.md; echo "expect: no matches above"; grep -c '1.6.2' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - No stale composite version string remains: grep -rn '1.6.1/1.6.1/1.3.0' .planning/STATE.md CLAUDE.md returns no matches.
    - STATE.md asserts the final truth: grep -c '1.6.2' .planning/STATE.md is at least 1, and a Session Continuity/Accumulated Context note records the RELH-02 reconciliation outcome (Hex authoritative, tags fetched+kept, admin-v1.6.1 quirk documented).
    - CLAUDE.md current-state version line reflects 1.6.2/1.6.2/1.3.1 (grep for 1.6.2 in the "What This Is" region matches).
    - No .planning/milestones/ archive modified: git status --porcelain .planning/milestones/ is empty.
  </acceptance_criteria>
  <done>STATE.md and CLAUDE.md current-state assert the final version truth 1.6.2/1.6.2/1.3.1, the reconciliation note (incl. the admin-v1.6.1 tag quirk and fetch+keep disposition) is recorded, no archive touched; committed as docs(state):/docs:.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-repo pins to Hex dependency resolution | The admin/inbound core-dep pins are resolved at publish/CI time against Hex; a pin to an unpublished core version reds main (docs-contract / installer-smoke jobs fail to resolve). |
| live Hex to in-repo version truth | Hex is the authoritative source; the D-13 guard establishes it before any edit so reconciliation never runs against an unsettled train. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-08 | Denial of Service | Core-dep pin set to an unpublished core version, reds main on dependency resolution | mitigate | Task 1 confirms core 1.6.2 IS published on Hex before Task 2 sets == 1.6.2; the research "Ordering constraint" is encoded as the gate. The acceptance criteria assert the pin equals the published version. |
| T-93-09 | Tampering | Reconciliation run against an unsettled / mid-flight release train, guessing wrong version truth | mitigate | D-13 guard (Task 1) STOPS and records the blocker if live Hex does not confirm 1.6.2/1.6.2/1.3.1; Tasks 2-3 are gated on it. |
| T-93-10 | Repudiation | A hand-edit to manifest/@version that release-please later fights, churning versions | accept | Research verified release-please only rewrites manifest/@version when it CUTS a release PR; with the manifest already at released versions and no pending bump-triggering commits, it has nothing to cut. The sed sync step runs only if prs_created. Hand-edit is stable. |
| T-93-11 | Tampering | Accidental deletion of real published 1.6.x tags, orphaning HexDocs source links | mitigate | Disposition is fetch + KEEP (research); Task 1 fetches and the acceptance criteria assert both 1.6.1 and 1.6.2 tags remain present (no deletion). |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; only version-literal edits in manifest/mix.exs, a git fetch --tags, and .planning/CLAUDE.md doc corrections. No new dependency introduced. |
</threat_model>

<verification>
- Live Hex confirmed at 1.6.2/1.6.2/1.3.1 with inbound pinning == 1.6.2 BEFORE any edit (D-13 guard); blocker recorded and stop-short if not.
- Manifest, three @version, and both pins equal 1.6.2/1.6.2/1.3.1; binding comments intact; remote 1.6.x tags fetched and kept (not deleted).
- STATE.md + CLAUDE.md current-state assert the final truth; no .planning/milestones/ archive edited.
- All edits non-release-triggering (chore(release):/docs(state):/docs:); no release-please PR created; no Hex publish triggered.
</verification>

<success_criteria>
- RELH-02: the 1.6.x aftermath is reconciled to released reality 1.6.2/1.6.2/1.3.1 (manifest + @version + both pins), the real remote 1.6.x tags are fetched and kept (dispositioned, not deleted), the inbound exact-pin is at the released core == 1.6.2 via a non-bumping catch-up, and .planning release-state memory + CLAUDE.md reflect the final version truth. If the train were unsettled, the blocker would be recorded per D-13 instead (not the case at planning time; live Hex confirmed settled).
</success_criteria>

<output>
Create `.planning/phases/93-hexdocs-wiring-and-release-hardening/93-03-SUMMARY.md` when done. If Task 1's D-13 guard fails (Hex unsettled), the SUMMARY records the blocker and Tasks 2-3 are not executed.
</output>
