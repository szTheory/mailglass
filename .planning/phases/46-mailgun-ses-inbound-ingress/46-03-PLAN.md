---
phase: 46-mailgun-ses-inbound-ingress
plan: 03
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex
  - mailglass_inbound/lib/mailglass_inbound/optional_deps.ex
  - mailglass_inbound/mix.exs
  - .credo.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs
  - mailglass_inbound/test/mailglass_inbound/s3_fetcher_test.exs
autonomous: true
requirements: [SESI-01, SESI-02, SESI-04, SESI-05]
user_setup:
  - service: aws-ses
    why: "Real S3Fetcher.ExAwsS3 adapter fetches the MIME body from the adopter's S3 bucket; the Fake adapter is the test default so CI needs no setup"
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "ex_aws standard credential chain (env -> pod-identity -> instance/task role) — adopter wires their own AWS creds; no mailglass-specific config (D-46-15)"
must_haves:
  truths:
    - "An authentic SES SNS Notification verifies the X.509 signature via the reused core CertCache + TrustPolicy and returns {:ok, facts}"
    - "A forged SNS signature or a SigningCertURL/SubscribeURL failing TrustPolicy raises MailglassInbound.SignatureError -> 401, no record"
    - "A SubscriptionConfirmation with a TrustPolicy-valid SubscribeURL auto-confirms and returns {:control_plane, 200} -> 200 no-op, no record; a hijacked SubscribeURL is rejected"
    - "An Action:S3 Notification fetches the MIME body via the MailglassInbound.S3Fetcher behaviour; S3Fetcher.Fake is the test default; S3Fetcher.ExAwsS3 gates through MailglassInbound.OptionalDeps.ExAwsS3"
    - "S3 fetch uses a small bounded retry (2-3 attempts); on exhaustion it returns/raises MailglassInbound.S3FetchError and does NOT ack, so SNS redelivers"
    - "mix compile --no-optional-deps --warnings-as-errors stays green; no bare ExAws reference outside the gateway"
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex"
      provides: "SES inbound provider: SNS X.509 verify via core seam + control-plane no-op + S3/inline MIME extraction + bounded retry"
      contains: "verify_envelope!"
    - path: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex"
      provides: "Test-default S3 fetcher (fake-first, D-13)"
      contains: "@behaviour MailglassInbound.S3Fetcher"
    - path: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex"
      provides: "Real S3 fetcher behind the optional-dep gateway"
      contains: "MailglassInbound.OptionalDeps.ExAwsS3"
    - path: "mailglass_inbound/lib/mailglass_inbound/optional_deps.ex"
      provides: "MailglassInbound.OptionalDeps.ExAwsS3 gateway (inbound-local, D-46-14)"
      contains: "no_warn_undefined"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex"
      to: "Mailglass.Webhook.Providers.SES.verify_envelope!/2"
      via: "calls the core seam extracted in Plan 01, then drives its own dispatch"
      pattern: "verify_envelope!"
    - from: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex"
      to: "MailglassInbound.OptionalDeps.ExAwsS3.get_object"
      via: "all ExAws access flows through the gateway only"
      pattern: "OptionalDeps.ExAwsS3"
---

<objective>
Ship `MailglassInbound.Ingress.Providers.SES`: verify SNS X.509 via the reused core `verify_envelope!/2` seam (CertCache + TrustPolicy), auto-confirm `SubscriptionConfirmation` via TrustPolicy (`{:control_plane, 200}` no-op), extract the MIME body from `Action: S3` (primary) or SNS-inline `content` (secondary) and feed it to `MailglassInbound.MIME.parse/1`, with a small bounded GetObject retry guarded by `MailglassInbound.S3FetchError`. Ship the `S3Fetcher.Fake` test default and the `S3Fetcher.ExAwsS3` real adapter behind a new inbound-local `MailglassInbound.OptionalDeps.ExAwsS3` gateway, and add `:ex_aws`/`:ex_aws_s3` as optional deps.

Purpose: SES inbound is the second production provider. It depends on Plan 01's `verify_envelope!/2` seam, the widened `{:ok, facts}` / `{:control_plane, _}` contract, the four-provider allowlist, the `S3Fetcher` behaviour, and `MailglassInbound.S3FetchError`.

Output: `ingress/providers/ses.ex`, `s3_fetcher/fake.ex`, `s3_fetcher/ex_aws_s3.ex` (new); `MailglassInbound.OptionalDeps.ExAwsS3` gateway; `:ex_aws`/`:ex_aws_s3` optional deps in `mix.exs`; `.credo.exs` `gated_modules` entry; SES provider + S3 fetcher tests with code-built SNS envelopes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-CONTEXT.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-VALIDATION.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Use directly; no exploration. -->

REUSE — core SES seam (extracted in Plan 01) + caches/policy (do NOT re-supervise; D-46-02):
  Mailglass.Webhook.Providers.SES.verify_envelope!(raw_body :: binary, config :: map) :: {:ok, sns_payload :: map}  # raises Mailglass.SignatureError on failure
  Mailglass.Webhook.Providers.SES.dispatch_message_type/3  # control-plane: {:ok, :control_plane, :subscription_confirmed | :unsubscribe_confirmed}; auto-confirms via TrustPolicy
  Mailglass.Webhook.Providers.SES.CertCache.fetch_public_key(url) :: {:ok, term} | :miss     # ETS, process-global
  Mailglass.Webhook.Providers.SES.TrustPolicy.valid_cert_url?(url) :: boolean                # https-only, SNS-host allowlist (SSRF guard)
  Mailglass.Webhook.Providers.SES.TrustPolicy.valid_subscribe_url?(url) :: boolean

SES payload shape (D-46-12): verified payload Type == "Notification"; receipt.action.type == "S3" carries bucketName + objectKey (objectKey == mail.messageId). SNS-inline content path: Message JSON carries raw MIME (UTF-8 or Base64, <=150 KB).

BEHAVIOUR + ERRORS (created in Plan 01):
  MailglassInbound.S3Fetcher.fetch(bucket :: String.t(), key :: String.t(), opts :: keyword()) :: {:ok, binary()} | {:error, term()}
  MailglassInbound.S3FetchError   closed @types [:s3_object_not_ready, :s3_fetch_failed]
  MailglassInbound.SignatureError closed @types incl :subscribe_url_untrusted, :bad_signature

WIDENED contract (Plan 01) — SES verify! must return one of:
  {:ok, facts}  (Notification, persists)  |  {:control_plane, 200}  (Subscription/Unsubscribe, 200 no-op)   ; raise SignatureError on forgery

MIME parser (Phase 45): MailglassInbound.MIME.parse(raw :: binary) :: {:ok, repr} | {:error, MIMEError.t()}  # never raises

GATEWAY TEMPLATE — lib/mailglass/optional_deps/gen_smtp.ex (mirror EXACTLY):
  @compile {:no_warn_undefined, [:gen_smtp_client, :mimemail]}
  @doc since:; def available?, do: Code.ensure_loaded?(:gen_smtp_client)
  def decode(raw, opts) do {:ok, :mimemail.decode(...)} rescue e -> {:error, {:error, e}} catch :throw, r -> ...; :exit, r -> ... end

GATEWAY PRECEDENT (inbound-local) — mailglass_inbound/lib/mailglass_inbound/optional_deps.ex:
  MailglassInbound.OptionalDeps  (namespace moduledoc states inbound keeps its own gateway surface — D-46-14)
  MailglassInbound.OptionalDeps.Oban  @compile {:no_warn_undefined, [...]}; def available?, do: Code.ensure_loaded?(Oban)  (:12-71)

CONFIG-RESOLUTION SEAM — lib/mailglass/webhook/providers/ses.ex httpc_client/1 (:351-362): config-map-first then app-env. Mirror for s3_fetcher resolution defaulting to Fake in :test, ExAwsS3 otherwise.

S3 FETCH (D-46-15): ExAws.S3.get_object(bucket, key) |> ExAws.request() => {:ok, %{body: binary, ...}}  (extract :body; A1 — verify the key in the Fake-vs-real test)

.credo.exs gated_modules (:48-56): Oban => [Mailglass.OptionalDeps.Oban, MailglassInbound.OptionalDeps.Oban], GenSmtp => [...], :mimemail => ... ; included_path_prefixes already covers "mailglass_inbound/lib/" (:60)

mix.exs (mailglass_inbound) deps/0 (:65-82): {:gen_smtp, "~> 1.3", optional: true} (:76); project no_warn_undefined list (:57)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MailglassInbound.OptionalDeps.ExAwsS3 gateway + :ex_aws deps + .credo.exs gating</name>
  <files>mailglass_inbound/lib/mailglass_inbound/optional_deps.ex, mailglass_inbound/mix.exs, .credo.exs</files>
  <read_first>
    - lib/mailglass/optional_deps/gen_smtp.ex (the gateway shape to mirror EXACTLY: @compile no_warn_undefined, available?/0 via Code.ensure_loaded?, never-raise wrapper with rescue + catch :exit)
    - mailglass_inbound/lib/mailglass_inbound/optional_deps.ex (inbound-local gateway namespace + Oban precedent :12-71)
    - mailglass_inbound/mix.exs (deps/0 :65-82 with {:gen_smtp, ..., optional: true} :76; project no_warn_undefined :57; comment block :72-75)
    - .credo.exs (gated_modules :48-56; included_path_prefixes :60)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md ("optional_deps/ex_aws_s3.ex (gateway)" + "mix.exs — MODIFIED" + ".credo.exs — MODIFIED" + DRIFT #1)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md (Pattern 4 gateway; Stack table; Pitfall 5; D-46-14/15/20)
  </read_first>
  <action>
Add `MailglassInbound.OptionalDeps.ExAwsS3` to `mailglass_inbound/lib/mailglass_inbound/optional_deps.ex` as a second module in the same file (DRIFT #1 — there is no `optional_deps/oban.ex` file; Oban lives inside `optional_deps.ex`; follow that precedent). Mirror `Mailglass.OptionalDeps.GenSmtp` exactly (D-46-14): `@compile {:no_warn_undefined, [ExAws, ExAws.S3]}`; `@doc since: "0.2.0"` + `@spec available?() :: boolean()` / `def available?, do: Code.ensure_loaded?(ExAws.S3)`; a never-raise `@spec get_object(String.t(), String.t()) :: {:ok, map()} | {:error, term()}` wrapper that calls `ExAws.S3.get_object(bucket, key) |> ExAws.request()` wrapped in `rescue e -> {:error, {:error, e}}` + `catch :exit, reason -> {:error, {:exit, reason}}`. This gateway is the ONLY place that may name `ExAws`/`ExAws.S3` (Pitfall 5). Moduledoc records the STACK-lock departure (D-46-20) and the inbound-local placement rationale (D-46-14; SESI-04's literal `Mailglass.OptionalDeps.ExAwsS3` is an erratum — consumer lives in inbound).

In `mailglass_inbound/mix.exs` `deps/0`, add `{:ex_aws, "~> 2.7", optional: true}` and `{:ex_aws_s3, "~> 2.5", optional: true}` (D-46-15, D-46-20). Do NOT add `ExAws`/`ExAws.S3` to the project-level `no_warn_undefined` list (:57) — the gateway's own `@compile {:no_warn_undefined, [ExAws, ExAws.S3]}` covers it (same rationale as the gen_smtp comment :72-75). Record the first-optional-dep-since-v1.0-STACK-lock departure in the inbound CHANGELOG (D-46-20).

In `.credo.exs`, add to `gated_modules` (:48-56): `ExAws => MailglassInbound.OptionalDeps.ExAwsS3` and `ExAws.S3 => MailglassInbound.OptionalDeps.ExAwsS3` so `NoBareOptionalDepReference` flags any stray ExAws reference outside the gateway (`included_path_prefixes` already covers `mailglass_inbound/lib/` at :60). Cross-package aliasing of `Mailglass.Webhook.Providers.*` from inbound is not flagged by the path-scoped boundary check (D-46-04); keep the calls explicit and confirm no compile-time cycle is introduced (the `mix compile` verify guards this).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix deps.get && mix compile --no-optional-deps --warnings-as-errors</automated>
    <automated>cd /Users/jon/projects/mailglass && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound/optional_deps.ex` defines `MailglassInbound.OptionalDeps.ExAwsS3` with `@compile {:no_warn_undefined, [ExAws, ExAws.S3]}` and `def available?, do: Code.ensure_loaded?(ExAws.S3)`
    - `mailglass_inbound/mix.exs` declares `{:ex_aws, "~> 2.7", optional: true}` and `{:ex_aws_s3, "~> 2.5", optional: true}`
    - `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` exits 0
    - `mix credo --strict` reports no `NoBareOptionalDepReference` violations (gateway is the only ExAws reference)
  </acceptance_criteria>
  <done>The inbound-local ExAwsS3 gateway exists mirroring GenSmtp; ex_aws/ex_aws_s3 are optional deps; .credo.exs gates ExAws; both compile lanes + credo strict are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: S3Fetcher.Fake (test default) + S3Fetcher.ExAwsS3 (real, gated) + config-resolution seam + bounded retry</name>
  <files>mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex, mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex, mailglass_inbound/test/mailglass_inbound/s3_fetcher_test.exs</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex (the behaviour created in Plan 01: @callback fetch/3)
    - mailglass_inbound/lib/mailglass_inbound/optional_deps.ex (the ExAwsS3 gateway from Task 1 — the real adapter calls it, never ExAws directly)
    - mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex (closed @types [:s3_object_not_ready, :s3_fetch_failed] — created in Plan 01)
    - lib/mailglass/webhook/providers/ses.ex (httpc_client/1 :351-362 — config-map-then-app-env resolution to mirror for the fetcher module)
    - mailglass_inbound/lib/mailglass_inbound/optional_deps/... (Oban gated-call wrapper :60-70 pattern)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md ("s3_fetcher/fake.ex", "s3_fetcher/ex_aws_s3.ex", "Config-resolution seam", "No Analog Found" notes on fake + bounded retry)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md (Pattern 4 + Pattern 5 bounded retry; A1 ExAws body key; D-46-13/15/16)
  </read_first>
  <behavior>
    - Test: S3Fetcher.Fake.fetch(bucket, key, opts) returns a configured {:ok, binary} for a canned bucket/key
    - Test: S3Fetcher.Fake can be configured to return {:error, _} for the first N calls then {:ok, binary} — exercises the bounded-retry path
    - Test: S3Fetcher.ExAwsS3.fetch/3 extracts :body from the gateway's {:ok, %{body: binary}} and surfaces {:error, reason} unchanged; it names NO ExAws module directly (only the gateway)
    - Test: MailglassInbound.OptionalDeps.ExAwsS3.available?/0 is false in CI (dep absent); get_object/2 returns a tagged {:error, _} rather than raising when ExAws is unavailable
    - Test: bounded retry runs <=3 attempts; on exhaustion returns/raises MailglassInbound.S3FetchError :s3_object_not_ready (so SNS redelivers); a non-retryable error -> :s3_fetch_failed
  </behavior>
  <action>
Create `mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex` defining `MailglassInbound.S3Fetcher.Fake` (`@behaviour MailglassInbound.S3Fetcher`, fake-first per D-13/D-46-13). Dependency-free. Implement `fetch(bucket, key, opts)` resolving canned responses from a configured map (e.g. `Application.get_env(:mailglass_inbound, :s3_fetcher_fake, %{})` keyed by `{bucket, key}` or by `key`), supporting an "error first N calls" mode (a counter in an Agent/ETS or an opts-driven sequence) so the SESI-05 bounded-retry path is testable. This is the `:test`-default fetcher.

Create `mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex` defining `MailglassInbound.S3Fetcher.ExAwsS3` (`@behaviour MailglassInbound.S3Fetcher`). `fetch(bucket, key, _opts)` calls `MailglassInbound.OptionalDeps.ExAwsS3.get_object(bucket, key)` and matches `{:ok, %{body: body}} when is_binary(body) -> {:ok, body}` / `{:error, reason} -> {:error, reason}` (D-46-15). CRITICAL: this adapter must NOT name `ExAws`/`ExAws.S3` directly — all access goes through the gateway only, or the `--no-optional-deps --warnings-as-errors` lane breaks (Pitfall 5).

Add a config-resolution seam (used by the SES provider in Task 3) — implement it where the SES provider can call it (e.g. a private `s3_fetcher(config)` in `ses.ex`, or a small shared helper). Mirror `ses.ex httpc_client/1` (:351-362): prefer `config[:s3_fetcher]` (an atom module) then `Application.get_env(:mailglass_inbound, :ses, [])[:s3_fetcher]`, defaulting to `S3Fetcher.Fake` in `:test` and `S3Fetcher.ExAwsS3` otherwise (D-46-13).

Add the bounded-retry helper (D-46-16; RESEARCH Pattern 5) — a small `fetch_with_retry(fetcher, bucket, key, attempts)` with 2-3 attempts and short backoff (e.g. 250ms -> 1s -> 2s). On `{:ok, body}` return it. On exhaustion return/raise `MailglassInbound.S3FetchError.new(:s3_object_not_ready, ...)` (the caller in Task 3 turns this into a non-2xx so SNS redelivers — do NOT ack; the dedupe layer on messageId is the real safety net). A clearly non-retryable failure maps to `:s3_fetch_failed`. Keep retry counts small — do NOT build a large retry budget around an unconfirmed timing gap (S3 has strong read-after-write since Dec 2020). In tests, inject short/zero backoff so the suite stays fast.

Create/extend `mailglass_inbound/test/mailglass_inbound/s3_fetcher_test.exs` covering all five behaviors above, including the gateway `available?/0` false-in-CI assertion (SESI-04) and the bounded-retry-then-S3FetchError path (SESI-05).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/s3_fetcher_test.exs</automated>
    <automated>cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex` implements `@behaviour MailglassInbound.S3Fetcher` and supports an error-first-N-calls mode
    - `mailglass_inbound/lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex` references `MailglassInbound.OptionalDeps.ExAwsS3` and names no `ExAws` module directly
    - bounded retry runs at most 3 attempts and on exhaustion yields `MailglassInbound.S3FetchError` `:s3_object_not_ready`
    - `MailglassInbound.OptionalDeps.ExAwsS3.available?/0` returns false in CI; `get_object/2` returns `{:error, _}` (no raise) when ExAws is absent
    - `cd mailglass_inbound && mix test test/mailglass_inbound/s3_fetcher_test.exs` exits 0
    - `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Fake (test default) + real gated ExAwsS3 fetchers exist; the config-resolution seam defaults to Fake in test; bounded retry maps exhaustion to S3FetchError; no bare ExAws ref; no-optional-deps lane green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SES provider — SNS X.509 verify via core seam + control-plane no-op + S3/inline MIME extraction</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex, mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex (provider shape: struct-arity verify!(%Request{}, config), normalize(%Request{}), evidence map, MIME.parse usage)
    - lib/mailglass/webhook/providers/ses.ex (verify_envelope!/2 seam from Plan 01; dispatch_message_type/3 :137-188 control-plane reuse; CertCache/TrustPolicy calls)
    - lib/mailglass/webhook/providers/ses/trust_policy.ex (valid_cert_url?/1 :37, valid_subscribe_url?/1 :72 — SSRF guards to call directly)
    - mailglass_inbound/lib/mailglass_inbound/signature_error.ex + s3_fetch_error.ex (Plan 01 errors to raise)
    - mailglass_inbound/lib/mailglass_inbound/s3_fetcher/fake.ex (Task 2 — the test-default fetcher) + the s3_fetcher/config-resolution seam from Task 2
    - mailglass_inbound/lib/mailglass_inbound/mime.ex (MIME.parse/1 :108 — feed the fetched/inline body here)
    - mailglass_inbound/lib/mailglass_inbound/inbound_message.ex (canonical struct — do NOT widen for SES quirks)
    - mailglass_inbound/test/mailglass_inbound/ingress/sendgrid_provider_test.exs (provider-test ANALOG)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md ("ingress/providers/ses.ex" three-way dispatch + reuse-only calls + Notification->S3 fetch + config seam)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md (Pattern 2 SES seam; Pitfall 2 control-plane; Anti-Patterns; D-46-12/16/18)
  </read_first>
  <behavior>
    - Test: authentic SNS Notification (Action:S3) -> verify!/2 returns {:ok, facts}; the S3 body is fetched via the configured Fake fetcher and fed to MIME.parse/1
    - Test: forged SNS signature (or SigningCertURL failing TrustPolicy) -> raises MailglassInbound.SignatureError :bad_signature -> 401, no record
    - Test: SubscriptionConfirmation with a TrustPolicy-valid SubscribeURL -> {:control_plane, 200}; a hijacked SubscribeURL (fails valid_subscribe_url?) -> raises MailglassInbound.SignatureError :subscribe_url_untrusted
    - Test: UnsubscribeConfirmation -> {:control_plane, 200}
    - Test: SNS-inline content path (small message, no S3 action) -> decoded (UTF-8/Base64) and fed to MIME.parse/1 -> {:ok, facts}
    - Test: Fake fetcher returns :error for the first N calls -> bounded retry then S3FetchError; the provider surfaces it so the plug returns non-2xx (SNS redelivers), no record
    - Test: normalize/1 builds %InboundMessage{} + evidence (raw_payload = SNS envelope, raw_mime = fetched/inline body); provider_message_id = mail.messageId (== objectKey)
  </behavior>
  <action>
Create `mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex` (`@moduledoc false`, `@behaviour MailglassInbound.Ingress.Provider`). Aliases: `MailglassInbound.{InboundMessage, SignatureError, S3FetchError}`, `MailglassInbound.Ingress.Request`, `Mailglass.Webhook.Providers.SES`, `Mailglass.Webhook.Providers.SES.{TrustPolicy}` (CertCache is reached through `verify_envelope!`).

Implement `verify!(%Request{raw_body: raw_body} = request, %{} = config)` (struct arity, widened contract). Call `SES.verify_envelope!(raw_body, config)` (the Plan-01 seam) to verify the SNS X.509 signature and get `{:ok, sns_payload}`; a forgery raises `Mailglass.SignatureError` from core. RE-RAISE as `MailglassInbound.SignatureError` so the inbound plug's dual rescue maps it to 401 consistently (wrap the call: rescue core `Mailglass.SignatureError` and `raise MailglassInbound.SignatureError.new(:bad_signature, provider: :ses)`), satisfying D-46-19/SESI-01. Then drive a three-way dispatch on `sns_payload["Type"]` (mirror core `dispatch_message_type` :137-188): `"Notification"` -> continue to MIME extraction -> `{:ok, facts}` (facts e.g. `%{auth: :sns_x509}`); `"SubscriptionConfirmation"` / `"UnsubscribeConfirmation"` -> reuse `SES.dispatch_message_type(type, sns_payload, config)` (it auto-confirms via `TrustPolicy.valid_subscribe_url?/1` + constructs ConfirmSubscription from the signed TopicArn+Token) and return `{:control_plane, 200}` — NOT a raise/401 (D-46-06, Pitfall 2). A hijacked SubscribeURL fails TrustPolicy inside dispatch and raises a core SignatureError -> re-raise as `MailglassInbound.SignatureError.new(:subscribe_url_untrusted, provider: :ses)`.

For the Notification path, MIME extraction (D-46-12): decode the inner SES `Message` JSON; if `receipt.action.type == "S3"` -> read `bucketName` + `objectKey` (objectKey == `mail.messageId`) and call the resolved `S3Fetcher` (via the Task-2 config-resolution seam) wrapped in the Task-2 bounded retry. On retry exhaustion the provider must surface the failure so the plug returns NON-2xx (SNS redelivers) — do NOT swallow it into a 200; either let the `S3FetchError` propagate (the plug should treat a non-Signature exception as a 500) or return an error tuple the plug maps to 5xx. If `receipt.action.type` is the SNS-inline `content` path (secondary, <=150 KB), decode the inline `content` (UTF-8 or Base64) directly. Feed the resulting raw bytes into `MailglassInbound.MIME.parse/1` (never raises). Do NOT support SES client-side KMS-encrypted objects — that is scoped out and documented for Phase 50 (D-46-18); a ciphertext body simply parses as a degraded record.

Implement `normalize(%Request{} = request)` returning `%{message: %InboundMessage{}, evidence: map}`. Build `%InboundMessage{}` from the parsed MIME representation (reuse the SendGrid RFC-5322 helpers via the parsed repr) with `provider_message_id = mail.messageId`. Evidence map (SendGrid key shape): `raw_payload` = the SNS envelope, `raw_headers`, `raw_mime` = the fetched/inline body, `verification_facts: %{}`, `parse_warnings`, `attachment_blobs`. Do NOT widen the public `%InboundMessage{}` struct for SES quirks.

Create `mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs` covering all seven behaviors with code-built SNS envelopes (no `.eml`). Build a `sns_notification/1` helper producing a signed SNS envelope the core `verify_envelope!` accepts (use the existing SES test cert/fixture pattern from core's SES tests, or a self-signed cert primed into `CertCache` via `CertCache.put/3`). Configure `S3Fetcher.Fake` via the config-resolution seam (test default) with canned bodies and an error-first-N mode for the retry test.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/ses_provider_test.exs</automated>
    <automated>cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - authentic SNS Notification (Action:S3) -> `verify!/2` returns `{:ok, facts}`; S3 body fetched via the Fake fetcher and parsed by `MIME.parse/1`
    - forged SNS signature -> `MailglassInbound.SignatureError` raised -> 401, zero records
    - `SubscriptionConfirmation` with valid SubscribeURL -> `{:control_plane, 200}`, no record; hijacked SubscribeURL -> `MailglassInbound.SignatureError` `:subscribe_url_untrusted`
    - SNS-inline content path decodes (UTF-8/Base64) and feeds `MIME.parse/1` -> `{:ok, facts}`
    - Fake-error-first-N -> bounded retry then `MailglassInbound.S3FetchError` `:s3_object_not_ready`; provider surfaces it so no record is created
    - `ses.ex` calls `Mailglass.Webhook.Providers.SES.verify_envelope!` and names no ExAws module directly
    - `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/ses_provider_test.exs` exits 0 and `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>SES verify! reuses the core X.509 seam, re-raises forgery as MailglassInbound.SignatureError, returns control-plane 200 no-ops, extracts the MIME body from S3 (bounded retry) or inline content into %InboundMessage{} + evidence — all covered by code-built SNS envelope tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SES SNS POST -> SES provider | untrusted SNS JSON envelope + X.509 signature + claimed SigningCertURL/SubscribeURL crosses here |
| SES provider -> SigningCertURL / SubscribeURL | URLs influence outbound network I/O (SSRF / hijack surface) |
| SES provider -> adopter S3 bucket | provider-declared bucket/key fetched over the network |
| S3Fetcher.ExAwsS3 -> ExAws (optional dep) | optional runtime dep boundary; must stay gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-20 | Spoofing | SES verify (X.509) | mitigate | reuse core `verify_envelope!/2` (RSA-SHA1/256 via `:public_key.verify`); forgery raises -> re-raised as `MailglassInbound.SignatureError` :bad_signature -> 401 no-recovery |
| T-46-21 | SSRF | SigningCertURL | mitigate | `TrustPolicy.valid_cert_url?/1` (https-only, SNS-host allowlist) validated BEFORE network I/O inside `verify_envelope!` |
| T-46-22 | Spoofing/SSRF | SubscribeURL hijack | mitigate | `TrustPolicy.valid_subscribe_url?/1` host allowlist; ConfirmSubscription URL constructed from the SIGNED TopicArn+Token, not the raw SubscribeURL; hijack -> `MailglassInbound.SignatureError` :subscribe_url_untrusted |
| T-46-23 | Tampering | control-plane no-op | mitigate | Subscription/Unsubscribe return `{:control_plane, 200}` with NO InboundRecord; never persists state from a non-message notification (Pitfall 2 — without this SES inbound silently never works) |
| T-46-24 | DoS | unbounded S3 retry | mitigate | bounded retry (<=3 attempts, short backoff); on exhaustion `S3FetchError` :s3_object_not_ready + non-ack (SNS redelivers); idempotency on messageId is the real safety net (D-46-16) |
| T-46-25 | DoS | oversized S3 object | accept | SES caps at 40 MB; `get_object` into memory is acceptable; streaming deferred (D-46 deferred) |
| T-46-26 | Tampering | client-side KMS ciphertext | accept | out of scope (D-46-18); documented (Phase 50) that adopters use bucket-level SSE, not client-side KMS; ciphertext parses as a degraded record, never a crash |
| T-46-27 | Info Disclosure | evidence/telemetry/logs | mitigate | PII stays in tenant-scoped `inbound_evidence`; `S3FetchError`/`SignatureError` derive excludes `:cause`; telemetry stop-meta PII-free |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | `:ex_aws` + `:ex_aws_s3` added as OPTIONAL deps; RESEARCH Package Legitimacy Audit verified both on Hex (ex_aws 2.7.0, ex_aws_s3 2.5.9) — mature, active repos, [VERIFIED: Hex]; gateway-gated so `--no-optional-deps` lane stays green |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/s3_fetcher_test.exs test/mailglass_inbound/ingress/ses_provider_test.exs` green
- `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` green (gateway-gated ExAws; no bare references)
- `cd mailglass_inbound && mix compile --warnings-as-errors` green (with optional deps)
- `mix credo --strict` reports no `NoBareOptionalDepReference` violations
- Forged-SNS test asserts 401 + zero records; control-plane test asserts 200 + zero records; retry-exhaustion test asserts S3FetchError + no record
</verification>

<success_criteria>
- Authentic SNS Notification -> X.509 verified via reused core seam -> S3/inline MIME extracted -> {:ok, facts} (SESI-01)
- Forged signature / untrusted cert URL -> MailglassInbound.SignatureError -> 401, no record (SESI-01)
- SubscriptionConfirmation auto-confirms via TrustPolicy -> {:control_plane, 200} no-op; hijacked URL rejected (SESI-02)
- S3Fetcher.Fake is the test default; S3Fetcher.ExAwsS3 gates through MailglassInbound.OptionalDeps.ExAwsS3 (SESI-03/04)
- Bounded GetObject retry -> S3FetchError on exhaustion -> non-ack so SNS redelivers (SESI-05)
- mix compile --no-optional-deps --warnings-as-errors stays green
</success_criteria>

<output>
Create `.planning/phases/46-mailgun-ses-inbound-ingress/46-03-SUMMARY.md` when done
</output>
