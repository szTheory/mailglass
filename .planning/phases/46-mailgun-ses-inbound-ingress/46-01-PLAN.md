---
phase: 46-mailgun-ses-inbound-ingress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/signature_error.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex
  - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex
  - lib/mailglass/webhook/providers/ses.ex
  - mailglass_inbound/test/mailglass_inbound/signature_error_test.exs
  - mailglass_inbound/test/mailglass_inbound/s3_fetch_error_test.exs
  - mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs
  - docs/api_stability.md
autonomous: true
requirements: [MGUN-04, SESI-03]
must_haves:
  truths:
    - "MailglassInbound.SignatureError raises with no recovery on forged signatures and exposes a closed :type atom set"
    - "MailglassInbound.S3FetchError exposes the closed :type set [:s3_object_not_ready, :s3_fetch_failed]"
    - "The inbound plug allowlist accepts :postmark, :sendgrid, :mailgun, :ses as ONE init guard + ONE provider_module switch"
    - "The inbound plug do_call/2 dispatches a three-variant verify! return: {:ok, facts} persists, {:replay} is a 200 no-op, {:control_plane, status} is a 200 no-op"
    - "The inbound plug rescues BOTH Mailglass.SignatureError and MailglassInbound.SignatureError, both mapping to 401"
    - "Mailglass.Webhook.Providers.SES exposes verify_envelope!/2 returning the verified SNS payload; outbound verify!/3 keeps its existing return and outbound tests stay green"
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/signature_error.ex"
      provides: "Net-new no-recovery inbound signature error, closed-type"
      contains: "def __types__"
    - path: "mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex"
      provides: "Net-new closed-type S3 fetch error"
      contains: "def __types__"
    - path: "mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex"
      provides: "S3Fetcher behaviour @callback fetch/3"
      contains: "@callback fetch"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex"
      provides: "Widened verify! callback return-type union"
      contains: "control_plane"
    - path: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      provides: "Widened allowlist + 3-variant result case + dual SignatureError rescue"
      contains: ":mailgun"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex"
      to: "MailglassInbound.SignatureError"
      via: "rescue clause covering both signature error structs"
      pattern: "MailglassInbound.SignatureError"
    - from: "lib/mailglass/webhook/providers/ses.ex"
      to: "verify_envelope!/2"
      via: "verify!/3 refactored to call the extracted envelope-verify seam"
      pattern: "verify_envelope!"
---

<objective>
Build the shared foundation both inbound providers depend on: two net-new closed-type errors, the `S3Fetcher` behaviour contract, the widened `Ingress.Provider` callback, the widened `Ingress.Plug` (four-provider allowlist + three-variant result `case` + dual `SignatureError` rescue), and the SES `verify_envelope!/2` core seam.

Purpose: Plans 02 (Mailgun) and 03 (SES) are blocked until the result contract can express `{:replay}` and `{:control_plane, _}` no-ops, the plug rescues the net-new inbound `SignatureError`, and the SES X.509 verify is reusable from inbound. This plan delivers exactly that surface and nothing provider-specific.

Output: `signature_error.ex`, `s3_fetch_error.ex`, `s3_fetcher.ex` (new); widened `provider.ex` + `plug.ex`; extracted `verify_envelope!/2` in core `ses.ex`; contract tests for both errors; extended `plug_test.exs` for the four-provider allowlist + the widened-result branches; `api_stability.md` entries for the two new error type sets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-CONTEXT.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md
@.planning/phases/46-mailgun-ses-inbound-ingress/46-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree. Use directly; no exploration. -->

mailglass_inbound/lib/mailglass_inbound/mime_error.ex (the closed-type error TEMPLATE to mirror):
  @types [:inbound_mime_invalid, :gen_smtp_unavailable]
  @derive {Jason.Encoder, only: [:type, :message, :context]}   # excludes :cause — no PII leak
  defexception [:type, :message, :cause, :context]
  @doc since: "0.2.0"
  def __types__, do: @types
  @impl true ; def message(%__MODULE__{message: m}), do: m
  # Package-local: does NOT implement Mailglass.Error behaviour, does NOT join its @type union.

lib/mailglass/errors/signature_error.ex (the no-recovery SEMANTICS to mirror — :provider field, per-type messages):
  defexception [:type, :message, :cause, :context, :provider]
  # closed @types set; retryable?/0 -> false (no-recovery, CLAUDE.md #5 / D-22); new/2 builder validates type + formats message

mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex (CURRENT — to widen):
  @callback verify!(raw_body :: binary(), headers :: [{String.t(), String.t()}], config :: map()) :: map()
  @callback normalize(raw_body :: binary(), headers :: [{String.t(), String.t()}]) :: normalized_t()
  # SendGrid already uses the struct arity verify!(%Request{}, config); the @callback still types the legacy arity.

mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex (CURRENT — to widen):
  init/1: `unless provider in [:postmark, :sendgrid]` (plug.ex:36) raises ArgumentError
  do_call/2 (plug.ex:53-61): verification_facts = verify_request!(...); single persist-or-error path
  rescue (plug.ex:114-117): `e in SignatureError` — aliased to Mailglass.SignatureError ONLY (plug.ex:28)
  provider_module/1 (plug.ex:372-373): :postmark / :sendgrid clauses
  per-provider seams: build_request!/2 (178), resolve_config!/3 (217), verify_request!/3 (241), normalize_request!/2 (249)
  send_json/3 (plug.ex:364) — JSON response helper

lib/mailglass/webhook/plug.ex:119-161 (the PROVEN 3-variant case in core outbound — copy this shape):
  case verify!(...) do
    {:ok, :replay}            -> send_resp(conn, 200, "")   # no-op
    {:ok, :control_plane, _o} -> send_resp(conn, 200, "")   # no-op
    :ok                       -> resolve_tenant! -> normalize -> ingest
  end

lib/mailglass/webhook/providers/ses.ex (CURRENT verify!/3 — to refactor):
  verify!/3 (ses.ex:55-110): decode_payload! -> fetch SigningCertURL/SignatureVersion/Signature/Type
    -> TrustPolicy.valid_cert_url? -> fetch_public_key! (CertCache) -> build_canonical_string
    -> :public_key.verify -> dispatch_message_type(msg_type, payload, config) [LINE 109]
  Steps 55-106 are the "envelope verify"; line 109 is the dispatch.
  Existing return: :ok | {:ok, :control_plane, :subscription_confirmed | :unsubscribe_confirmed}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Net-new closed-type errors SignatureError + S3FetchError (+ contract tests + api_stability)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/signature_error.ex, mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex, mailglass_inbound/test/mailglass_inbound/signature_error_test.exs, mailglass_inbound/test/mailglass_inbound/s3_fetch_error_test.exs, docs/api_stability.md</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/mime_error.ex (the closed-type TEMPLATE: @types, @derive excluding :cause, defexception fields, __types__/0 with @doc since:, message/1)
    - lib/mailglass/errors/signature_error.ex (no-recovery SEMANTICS: :provider field, retryable?/0 -> false, new/2 builder, per-type format_message)
    - mailglass_inbound/test/mailglass_inbound/mime_error_test.exs (the contract-test ANALOG: __types__/0 closed-set assertion, field set, :cause JSON exclusion, package-local assertion)
    - docs/api_stability.md (where the locked :type atom sets live; find the inbound-error section to append to)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md ("Closed-type error discipline" + signature_error.ex / s3_fetch_error.ex sections)
  </read_first>
  <behavior>
    - Test 1: MailglassInbound.SignatureError.__types__/0 returns exactly [:bad_signature, :missing_header, :malformed_header, :timestamp_skew, :subscribe_url_untrusted]
    - Test 2: MailglassInbound.S3FetchError.__types__/0 returns exactly [:s3_object_not_ready, :s3_fetch_failed]
    - Test 3: Jason.encode! of either struct omits :cause (and SignatureError's :provider/:cause are not in the derive set) — no PII leak
    - Test 4: SignatureError.new(:not_a_type, []) raises ArgumentError (closed-set validation)
    - Test 5: both structs are package-local — they do NOT implement the Mailglass.Error behaviour
    - Test 6: __types__/0 of both matches the atoms documented in docs/api_stability.md
  </behavior>
  <action>
Create `mailglass_inbound/lib/mailglass_inbound/signature_error.ex` defining `MailglassInbound.SignatureError` (D-46-19). Mirror `mime_error.ex` structure, adding a `:provider` field like core `signature_error.ex`. `defexception [:type, :message, :cause, :context, :provider]`. `@derive {Jason.Encoder, only: [:type, :message, :context]}` (exclude `:cause` and `:provider` to keep the serialized output PII-safe, matching mime_error's three-key derive). Closed `@types [:bad_signature, :missing_header, :malformed_header, :timestamp_skew, :subscribe_url_untrusted]`. Add `@doc since: "0.2.0"` + `def __types__, do: @types`, plus `def message(%__MODULE__{message: m}), do: m`. Add a `new/2` builder mirroring core `signature_error.ex` `new/2`: accept `(type, opts)`, validate `type in @types` (raise ArgumentError otherwise), set `:provider` from `opts[:provider]`, format a per-type message. This is the no-recovery error (CLAUDE.md #5 / D-22) — carry NO retry affordance; do NOT add a `retryable?/0` returning true. Moduledoc must point to `docs/api_stability.md` and state it is package-local (does NOT implement `Mailglass.Error`).

Create `mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex` defining `MailglassInbound.S3FetchError` (D-46-17). Mirror `mime_error.ex` verbatim. Closed `@types [:s3_object_not_ready, :s3_fetch_failed]`. `defexception [:type, :message, :cause, :context]` (no `:provider`). `@derive {Jason.Encoder, only: [:type, :message, :context]}`. `@doc since: "0.2.0"` + `def __types__, do: @types` + `message/1`. Per-type moduledoc: `:s3_object_not_ready` = bounded GetObject retry exhausted (SNS redelivers); `:s3_fetch_failed` = non-retryable S3 error. Do NOT bolt these atoms onto core `Mailglass.Error`, `Mailglass.SignatureError`, or `MailglassInbound.MIMEError`.

Append both new type sets to `docs/api_stability.md` in the inbound-errors section (next to `MailglassInbound.MIMEError`), with `@since 0.2.0` notes (D-46-17, D-46-19). Record the new errors in the inbound CHANGELOG fragment if one exists.

Create `mailglass_inbound/test/mailglass_inbound/signature_error_test.exs` and `mailglass_inbound/test/mailglass_inbound/s3_fetch_error_test.exs`, each mirroring `mime_error_test.exs` and covering the six behaviors above.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound/signature_error.ex defines __types__/0` returning exactly `[:bad_signature, :missing_header, :malformed_header, :timestamp_skew, :subscribe_url_untrusted]`
    - `mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex defines __types__/0` returning exactly `[:s3_object_not_ready, :s3_fetch_failed]`
    - `Jason.encode!(%MailglassInbound.SignatureError{cause: :secret})` output contains no `cause` key
    - `cd mailglass_inbound && mix test test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs` exits 0
    - `docs/api_stability.md` contains both new closed-type sets annotated `@since 0.2.0`
  </acceptance_criteria>
  <done>Both errors exist as package-local closed-type structs with passing `__types__/0` contract tests; api_stability.md documents both type sets.</done>
</task>

<task type="auto">
  <name>Task 2: S3Fetcher behaviour + widened Ingress.Provider callback</name>
  <files>mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex, mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex (the behaviour to widen; @callback + @type idiom)
    - mailglass_inbound/lib/mailglass_inbound/ingress/request.ex (Request struct shape used by the struct-arity verify!)
    - mailglass_inbound/lib/mailglass_inbound/ingress/providers/sendgrid.ex (lines 11, 24 — already uses verify!(%Request{}, config); the migration target)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md ("s3_fetcher.ex (behaviour)" + "ingress/provider.ex (behaviour) — MODIFIED" + "Verify-first ingress + widened result dispatch")
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md (Pattern 3 + Pattern 4 for the exact tuple shape)
  </read_first>
  <action>
Create `mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex` defining the behaviour `MailglassInbound.S3Fetcher` with `@moduledoc false` and the single callback `@callback fetch(bucket :: String.t(), key :: String.t(), opts :: keyword()) :: {:ok, binary()} | {:error, term()}` (D-46-13). Copy the `@behaviour`/`@callback`/`@type` idiom from `ingress/provider.ex`. Behaviour only — no implementation (`S3Fetcher.Fake` and `S3Fetcher.ExAwsS3` are built in Plan 03).

Widen `MailglassInbound.Ingress.Provider`'s `verify!` `@callback` (D-46-07) to the struct arity with the three-variant union: `@callback verify!(request :: Request.t(), config :: map()) :: {:ok, verification_facts :: map()} | {:replay} | {:control_plane, http_status :: pos_integer()}`. Add a comment that forged input RAISES a `SignatureError` (core's or the new inbound one) and is NOT a tuple variant. Keep the `normalize` callback as-is (the plug's per-provider `normalize_request!/2` clauses handle arity differences). Do NOT edit Postmark/SendGrid source here — this is the contract widening only; the plug task and Plans 02/03 adapt callers. Leave a comment that Postmark continues to expose the legacy `verify!/3` arity (the plug dispatches per-provider, so a mixed-arity transition is acceptable within this phase).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound/s3_fetcher.ex` defines `@callback fetch(bucket :: String.t(), key :: String.t(), opts :: keyword()) :: {:ok, binary()} | {:error, term()}`
    - `mailglass_inbound/lib/mailglass_inbound/ingress/provider.ex` `verify!` `@callback` return type contains the `{:replay}` and `{:control_plane, ...}` variants
    - `cd mailglass_inbound && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>S3Fetcher behaviour exists; Provider.verify! callback expresses the three-variant union; package compiles warning-free.</done>
</task>

<task type="auto">
  <name>Task 3: Widen Ingress.Plug (4-provider allowlist + 3-variant result case + dual SignatureError rescue) and extract SES verify_envelope!/2</name>
  <files>mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex, lib/mailglass/webhook/providers/ses.ex, mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex (the plug to widen: init/1:33-42, do_call/2:53-133, rescue:114-117, alias:28, provider_module/1:372-373, build_request!:178, resolve_config!:217, verify_request!:241, normalize_request!:249, send_json:364)
    - lib/mailglass/webhook/plug.ex (lines 119-161 — the PROVEN 3-variant verify! result case to copy)
    - lib/mailglass/webhook/providers/ses.ex (verify!/3:55-110 — extract 55-106 as verify_envelope!/2; line 109 stays the dispatch call)
    - mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs (the plug test to extend with :mailgun/:ses allowlist + widened-result branch coverage)
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-PATTERNS.md ("ingress/plug.ex — MODIFIED" Changes 1-3 + "core ses.ex — MODIFIED (optional extract)")
    - .planning/phases/46-mailgun-ses-inbound-ingress/46-RESEARCH.md (Open Question 1 rec = rescue both; Open Question 2 rec = extract verify_envelope!/2; Anchor Drift #4 + #5)
  </read_first>
  <action>
**Change A — core SES seam extract (D-46-01 for SES; RESEARCH Open Question 2 rec).** In `lib/mailglass/webhook/providers/ses.ex`, factor `verify!/3`'s steps (decode -> `TrustPolicy.valid_cert_url?` -> `fetch_public_key!` -> `build_canonical_string` -> `:public_key.verify`, currently lines 55-106) into a new public `verify_envelope!(raw_body, config) :: {:ok, sns_payload :: map()}` that raises `Mailglass.SignatureError` on any failure exactly as today. Refactor `verify!/3` to call `verify_envelope!/2` then `dispatch_message_type(msg_type, payload, config)`, preserving its existing `:ok | {:ok, :control_plane, _}` return. The SNS body is JSON identically inbound and outbound (no Mailgun-style JSON coupling), so the blast radius is small. Outbound behavior MUST be unchanged: `verify!/3`'s public return and all SES outbound tests stay green — only the shared crypto seam is factored out; outbound's JSON decode + `%Event{}` normalization stay untouched (D-46-03). Add `@spec verify_envelope!(binary(), map()) :: {:ok, map()}` and a `@doc` noting it is the inbound-reuse seam. Fallback (only if a read shows the refactor risks outbound regressions): leave `verify!/3` intact and have Plan 03 call it + re-decode the Notification body — but the seam is the recommended path.

**Change B — allowlist (one switch, D-46-05, MGUN-04).** In `mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex`: change the `init/1` guard (plug.ex:36) from `provider in [:postmark, :sendgrid]` to `provider in [:postmark, :sendgrid, :mailgun, :ses]` and update the ArgumentError message. Add `:mailgun`/`:ses` clauses to `provider_module/1` (plug.ex:372-373) -> `MailglassInbound.Ingress.Providers.Mailgun` / `MailglassInbound.Ingress.Providers.SES` (created in Plans 02/03; reference by name — they exist before Wave 2 integration tests run). Add `:mailgun`/`:ses` clauses to `build_request!/2`, `resolve_config!/3`, `verify_request!/3`, `normalize_request!/2` in the existing per-provider clause style. `build_request!` mirrors SendGrid (`raw_body: conn.private[:raw_body]`, `params: conn.params`, content_type from header). `resolve_config!` reads `Application.get_env(:mailglass_inbound, :mailgun, [])` / `(:ses, [])` honoring the opts `:config` override; SES config map surfaces `:s3_fetcher` and optionally `cert_cache_ttl_seconds`; Mailgun config surfaces `:signing_key`. `verify_request!(:mailgun|:ses, request, config)` calls `provider_module(p).verify!(request, config)` (struct arity). `normalize_request!(:mailgun|:ses, request)` calls `provider_module(p).normalize(request)`.

**Change C — widen do_call/2 result contract (D-46-06; RESEARCH Pattern 3; Anchor Drift #5).** Replace the single `verification_facts = verify_request!(...)` line (plug.ex:57) with `case verify_request!(provider, request, config) do ... end` copying core's three-variant shape (lib/mailglass/webhook/plug.ex:119-161): `{:replay}` -> `send_json(conn, 200, %{status: "replay"})` + PII-free stop-meta `%{provider:, status: :replay}`, NO `InboundRecord`; `{:control_plane, _status}` -> `send_json(conn, 200, %{status: "control_plane"})` + stop-meta `%{provider:, status: :control_plane}`, NO `InboundRecord`; `{:ok, facts}` -> the EXISTING tenant -> normalize -> `build_handoff(normalized, provider, tenant_id, facts)` -> persist -> dispatch flow. Replay and control-plane are 200 no-ops — NEVER SignatureError/401 (providers retry-storm on non-200; RESEARCH Pitfall 2 + Anti-Patterns). Keep telemetry stop-meta PII-free.

**Change D — rescue BOTH SignatureError structs (D-46-19; Anchor Drift #4; RESEARCH Open Question 1 rec).** Add a `MailglassInbound.SignatureError` alias near plug.ex:28. Change `rescue e in SignatureError ->` (plug.ex:115) to `rescue e in [Mailglass.SignatureError, MailglassInbound.SignatureError] ->` so both map to the existing 401 body; `Atom.to_string(e.type)` still works (both structs expose `.type`). Leave Postmark/SendGrid raising core's error untouched (smallest blast radius, no behavior change to shipped v1.1 providers).

**Tests.** Extend `mailglass_inbound/test/mailglass_inbound/ingress/plug_test.exs`: (1) `Plug.init([provider: :mailgun])` and `init([provider: :ses])` do NOT raise; an unknown provider still raises ArgumentError. (2) Inject a stub provider via the opts seam and assert the widened `do_call` branches: a `{:replay}` return -> 200, no `InboundRecord` row; a `{:control_plane, 200}` return -> 200, no `InboundRecord` row; an `{:ok, facts}` return -> persists as today. (3) A stub that raises `MailglassInbound.SignatureError` -> 401 (dual rescue) and one that raises `Mailglass.SignatureError` -> 401 (existing path). Use the existing opts seams (`:persistence`, `:execution`, `:config`); do NOT require the real Mailgun/SES providers.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/ingress/plug_test.exs</automated>
    <automated>cd /Users/jon/projects/mailglass && mix test test/mailglass/webhook/</automated>
  </verify>
  <acceptance_criteria>
    - `Plug.init([provider: :mailgun])` and `Plug.init([provider: :ses])` return without raising; unknown provider raises ArgumentError
    - `{:replay}` verify return -> HTTP 200 and zero new InboundRecord rows (asserted in plug_test)
    - `{:control_plane, 200}` verify return -> HTTP 200 and zero new InboundRecord rows
    - a stub raising `MailglassInbound.SignatureError` -> HTTP 401; a stub raising `Mailglass.SignatureError` -> HTTP 401
    - `lib/mailglass/webhook/providers/ses.ex` defines `verify_envelope!/2`; `cd /Users/jon/projects/mailglass && mix test test/mailglass/webhook/` exits 0 (outbound SES unchanged)
    - `cd mailglass_inbound && mix test test/mailglass_inbound/ingress/plug_test.exs` exits 0
  </acceptance_criteria>
  <done>Plug accepts all four providers via one switch, dispatches the three-variant verify result, rescues both signature errors to 401, and core SES exposes verify_envelope!/2 with outbound tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider POST -> Ingress.Plug | untrusted multipart/SNS payload + claimed signature crosses here |
| Ingress.Plug response -> provider | response body + status code returned to the provider (PII egress + retry-control signal) |
| core SES seam <- inbound caller | the extracted `verify_envelope!/2` becomes a cross-package security primitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | Ingress.Plug rescue clause | mitigate | rescue covers BOTH `Mailglass.SignatureError` and `MailglassInbound.SignatureError` -> 401 no-recovery; a forged-signature path that raised the new error must NOT escape as a 500 (Anchor Drift #4) |
| T-46-02 | Tampering | do_call/2 result contract | mitigate | replay/control-plane are 200 no-ops with NO `InboundRecord`; only `{:ok, facts}` persists — a non-persisting verified outcome cannot create state |
| T-46-03 | Repudiation/DoS | Ingress.Plug response status | mitigate | replay + control-plane return 200 (never 401/500) so providers do not retry-storm; forgery returns 401 (no-recovery), transient persist failure returns 500 (provider retries) |
| T-46-04 | Info Disclosure | error structs + plug response/telemetry | mitigate | `SignatureError`/`S3FetchError` `@derive Jason.Encoder only [:type,:message,:context]` excludes `:cause`/`:provider`; plug stop-meta stays PII-free (provider/status only) |
| T-46-05 | Tampering | core `verify!/3` refactor | mitigate | `verify_envelope!/2` extraction must keep outbound `verify!/3` return + behavior identical; gated by `mix test test/mailglass/webhook/` staying green (regression guard on a security primitive) |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs in this plan (deps land in Plan 03); slopcheck + human checkpoint not required here |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs test/mailglass_inbound/ingress/plug_test.exs` green
- `cd mailglass_inbound && mix compile --warnings-as-errors` green
- `cd /Users/jon/projects/mailglass && mix test test/mailglass/webhook/` green (outbound SES regression guard for the seam extract)
- `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` green (no new optional-dep references introduced in this plan)
</verification>

<success_criteria>
- Two net-new closed-type errors exist with passing `__types__/0` contract tests + api_stability.md entries
- `S3Fetcher` behaviour + widened `Provider.verify!` callback compiled
- Plug allowlist accepts all four providers via one switch (MGUN-04)
- Plug do_call dispatches the three-variant result contract; replay + control-plane are 200 no-ops with no record
- Plug rescues both `SignatureError` structs to 401
- Core `SES.verify_envelope!/2` extracted with outbound tests green
</success_criteria>

<output>
Create `.planning/phases/46-mailgun-ses-inbound-ingress/46-01-SUMMARY.md` when done
</output>
