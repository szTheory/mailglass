---
phase: 167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev
plan: 02
subsystem: infra
tags: [release-please, requirements-docs, ci-controls]

requires:
  - phase: 167.1-01
    provides: "the shipped pending/proposal_awaiting_release_action verdict and both refuted-mechanism findings (F-01..F-05) this plan's prose corrections cite"
provides:
  - "CTRL-02/CTRL-03 requirement prose that names the measured mechanism (conflated should_run flag) instead of the refuted rate-limit diagnosis, with both Accept clauses byte-identical"
  - "167.1-POSTMERGE-EVIDENCE.md — a harvest procedure for both post-merge observations, every slot empty"
affects: [167.1-milestone-audit, gsd-verify-work-167.1]

actuals:
  tokens: 2200
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "harvest-document shape reused from 166-POSTMERGE-EVIDENCE.md: status line, per-requirement trigger + runnable commands + empty observation table, expected-verdicts-by-world-state table, refutation-signature list, closing non-inference note"

key-files:
  created:
    - .planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md
  modified:
    - .planning/REQUIREMENTS.md

key-decisions:
  - "CTRL-02's mechanism sentence now says release-preflight correctly answers 'should the action re-run' while the proposal-evidence steps were wrongly gated on that same flag -- naming Phase 167.1 as the fix, per D-06 and F-01/F-02."
  - "CTRL-03's *Note* now cites the measured 5000/5000 quota and points at the same conflated flag rather than a secondary rate limit, per F-04; its 'reducing cron frequency is not a fix' conclusion is preserved because it still holds under the corrected basis."
  - "Both *Accept:* clauses left byte-identical -- verified by exact grep-count before and after; only mechanism/note prose changed, checkbox state unchanged ([ ] for both)."
  - "Harvest doc's expected-verdict table cites Plan 01's shipped strings verbatim (pending/proposal_awaiting_release_action, blocked/multiple_open_proposals) rather than restating them from memory, so a future auditor can diff observed against predicted without re-deriving anything."

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "REQUIREMENTS.md no longer attributes CTRL-02/CTRL-03 failures to a GitHub rate limit; both Accept clauses stay byte-identical; both entries stay unchecked; diff touches only these two entries"
    verification:
      - kind: other
        ref: "grep -c 'trips the rate limit' / 'all 15 buckets read full' = 0; grep -c on both Accept-clause strings = 1 each; grep -c on both unchecked-box patterns = 1 each; git diff .planning/REQUIREMENTS.md scoped to CTRL-02/CTRL-03 lines only"
        status: pass
    human_judgment: false
  - id: D2
    description: "167.1-POSTMERGE-EVIDENCE.md exists with per-requirement trigger, runnable commands, expected-verdict table citing the shipped reason strings, refutation-signature list, and every observation slot empty"
    verification:
      - kind: other
        ref: "presence + section-heading grep for CTRL-02, CTRL-03, 'expected verdicts', 'would refute', and 'evidence-pending'"
        status: pass
    human_judgment: false
  - id: D3
    description: "CTRL-02 and CTRL-03 Accept clauses are actually met (a green post-merge push run; three agreeing push/schedule runs)"
    verification: []
    human_judgment: true
    rationale: "Both Accept clauses are post-merge observations on protected main this plan structurally cannot produce. The harvest document records the procedure only; filling its slots requires a real merge and real calendar-time CI runs that have not yet occurred."

duration: 12min
completed: 2026-09-18
status: complete
---

# Phase 167.1 Plan 02: Correct CTRL-02/CTRL-03 Prose and Write the Post-Merge Harvest Procedure Summary

**Replaced the refuted rate-limit diagnosis in CTRL-02/CTRL-03's requirement prose with the measured conflated-`should_run`-flag mechanism (D-06), and created `167.1-POSTMERGE-EVIDENCE.md` — a harvest procedure with empty observation slots for both requirements' post-merge Accept clauses.**

## Performance

- **Duration:** 12 min
- **Tasks:** 2
- **Files modified:** 2 (`.planning/REQUIREMENTS.md`, new `167.1-POSTMERGE-EVIDENCE.md`)

## Accomplishments

- CTRL-02's mechanism sentence no longer says "removing the redundant API work that trips the rate limit" — it now describes the actual defect (proposal-evidence steps gated on the same flag that correctly gates the release-please action itself) and names Phase 167.1 as the fix.
- CTRL-03's `*Note:*` no longer says "research verified this is a secondary rate limit (all 15 buckets read full during a 403)" — it now cites the 2026-09-18 audit's measured 5000/5000 quota and the same conflated-flag root cause, while preserving the still-true "reducing cron frequency is not a fix, not in scope" conclusion.
- Both `*Accept:*` clauses verified byte-identical before and after via exact grep-count on their literal text; both entries verified still unchecked (`- [ ]`); `git diff` confirmed to touch only these two requirement entries.
- `167.1-POSTMERGE-EVIDENCE.md` created with a per-requirement trigger, the exact `gh run list` / `gh run view` / `gh run download` commands to harvest each observation, an expected-verdict-by-world-state table citing Plan 01's shipped reason strings verbatim, a refutation-signature list, and a closing note that an empty slot is the truthful state and must never be filled by inference or a manufactured re-run.
- No observation slot in the harvest document contains a result. No workflow was dispatched, re-run, or inferred from a prior run to produce one.

## Task Commits

1. **Task 1: Correct the refuted rate-limit mechanism in CTRL-02 and CTRL-03 (D-06)** - `5f733317` (docs)
2. **Task 2: Write the post-merge harvest procedure with empty observation slots** - `4d0043c8` (docs)

_Plan metadata commit follows this SUMMARY._

## Files Created/Modified

- `.planning/REQUIREMENTS.md` — CTRL-02 mechanism sentence, CTRL-03 `*Note:*` line. Both `*Accept:*` clauses and both checkbox states untouched.
- `.planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md` (new) — the harvest procedure.

## Decisions Made

- See `key-decisions` in frontmatter. In short: name Phase 167.1 in the corrected sentences so a reader can trace the claim to the diff that made it true (per the plan's own instruction); preserve the "cron-frequency reduction is not a fix" conclusion under its corrected basis rather than dropping it, since that conclusion survives independently of which diagnosis was right.

## Deviations from Plan

None — plan executed exactly as written. Both must-have truths about the Accept clauses and checkbox state were verified mechanically (grep counts), not asserted from memory.

## Issues Encountered

None.

## CTRL-02/CTRL-03 status — explicitly NOT met by this plan

**CTRL-02 and CTRL-03 remain `Implemented, evidence pending`.** This plan corrects the
repository's own claim about *why* they were failing (D-06) and writes down *how* to collect
the evidence once it naturally exists. It does not and cannot supply that evidence:

- **CTRL-02's Accept clause** — "the merge of a `chore: release main` PR produces a green
  `release-please` push run" — requires a real proposal merge on protected `main` that has not
  happened.
- **CTRL-03's Accept clause** — three consecutive agreeing push/schedule runs — requires
  calendar time and real CI activity on `main` that has not happened.

The phase is expected to close `human_needed` for these two requirements, exactly as Phase 166
did before this gap-closing phase existed. That is the correct, truthful state — not a failure
of this plan.

## Next Phase Readiness

- `.planning/REQUIREMENTS.md` no longer contains the refuted rate-limit diagnosis anywhere.
- `167.1-POSTMERGE-EVIDENCE.md` is ready to be filled in by a future `/gsd-verify-work` or
  milestone audit once a real `chore: release main` merge and three real pushes (plus an
  aligned schedule run) have occurred on `main`.
- No blockers for closing Phase 167.1 as `human_needed` pending that future observation.

---
*Phase: 167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev*
*Completed: 2026-09-18*

## Self-Check: PASSED
