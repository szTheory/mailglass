---
status: partial
phase: 167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev
source: [167.1-VERIFICATION.md]
started: 2026-09-18
updated: 2026-09-18
---

## Current Test

[testing paused — 1 item outstanding: CTRL-02, blocked on a release ceremony that must not be manufactured]

## Tests

### 1. CTRL-02 harvest — green release-please push run after a `chore: release main` merge
expected: The run concludes `success` (not `skipped`) and the artifact carries `status=pending` with `reason=proposal_awaiting_release_action` or `reason=no_open_proposal`. Commands are in `167.1-POSTMERGE-EVIDENCE.md`: `gh run list --workflow=release-please.yml --branch=main --event=push --limit=1 --json databaseId,headSha,conclusion,createdAt`, then `gh run view <run-id> --json jobs`, then download and inspect the artifact.
result: blocked
blocked_by: prior-phase
reason: "Precondition now partially cleared — the fix reached protected `main` as f6131908 (PR #285, merged 2026-09-18T19:48Z). But CTRL-02's accept clause requires the green push run to follow **the merge of a `chore: release main` PR**, and #285 was a `fix(release):` PR, not a release proposal. The post-fix push run 35387947658 is therefore NOT CTRL-02 evidence and has deliberately not been recorded as such. CTRL-02 remains blocked on a real release proposal reaching a merge — a Hex release ceremony requiring human approval per CLAUDE.md (`hex-publish` required_reviewers, one per package) and its own reviewed ledger PR (`inactive` -> `captured` -> `authorized`). Must not be manufactured to fill this slot."

### 2. CTRL-03 harvest — three consecutive push runs plus an aligned schedule firing agree
expected: All three push runs conclude `success`, and `push` and `schedule` at the same SHA report the same status AND the same reason — no #278-style event-scoped split (same SHA, opposite verdict, split by `event`). Per-SHA comparison commands are in `167.1-POSTMERGE-EVIDENCE.md`.
result: pass
reason: "HARVESTED IN FULL 2026-09-18. All three facts in the verbatim accept clause are satisfied. (a) Three consecutive green push runs: 35387947658 @ f6131908, 35396271105 @ 004f6462, 35396363586 @ 14b76d82 — consecutive with no intervening push run, all `success`, all reporting status=pending / reason=proposal_awaiting_release_action. Each reproduces the decisive step pattern: `Discover…` success (un-gating engaged), `Capture…` skipped (D-03 ordering guard intact), `Fail non-pass…` success rather than skipped (a real pass, not a short-circuit). (b) push/schedule agreement at the same SHA: schedule 35391497513 @ f6131908 diffs byte-identical to push 35387947658 on {status, reason} AND on the full probes array; only event_name differs. No #278 signature. (c) `cannot-check` never reported as `pass`: each of the three runs carries exactly one cannot-check probe (required_checks) while the overall verdict stays `pending`; a bare `pass` is never emitted. All four refutation conditions absent across all three runs. PROVENANCE DISCLOSED: pushes 2 and 3 (004f6462, 14b76d82) originated from the same assistant session that harvested them — real work commits (evidence harvest; Phase 166 deferred-items disposition), not dispatches, re-runs or no-ops, with guard_release_trigger.sh PASS on the second. If a maintainer judges same-session pushes not to be independent traffic, rows 2-3 should be struck and the count restarted from the next externally-originated push. See the provenance section in 167.1-POSTMERGE-EVIDENCE.md."

## Summary

total: 2
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

[none — blocked items are prerequisite gates, not code issues]

## Notes

Both items are **natural-trigger, post-merge, calendar-time observations** on protected `main`.
They are not a shortfall of this phase: the structural gap named in the phase goal is fully
closed and verified (13/13 must-haves, 0 gaps). What remains is a real-world CI event outside
this phase's control.

Per the phase's binding constraints and the closing note in `167.1-POSTMERGE-EVIDENCE.md`, these
slots must **never** be filled from inference, from a pre-167.1 run, or from a dispatched
workflow re-run standing in for a natural trigger.

**Precondition — CLEARED 2026-09-18T19:48Z.** PR #285 merged the fix to protected `main` as
f6131908 (CI 34/34 green). Local `main` is now in sync (`0 0`). The fix is confirmed working
in production: the first post-fix push run and the first post-fix schedule run both concluded
`success`, against six consecutive pre-fix `failure` runs at dcbda58f — same workflow, same
world state, opposite verdict, with the fix as the only variable.

**CTRL-03 — CLOSED 2026-09-18.** All three accept-clause facts harvested and asserted
mechanically. See test 2 and the completion harvest log in `167.1-POSTMERGE-EVIDENCE.md`,
including the deliberate disclosure that pushes 2 and 3 originated from the harvesting session.

**Remaining unblocking sequence.** CTRL-02 alone. It needs a real `chore: release main` PR to
reach a merge, which is a human-approved release ceremony (per CLAUDE.md the `hex-publish`
environment requires one reviewer approval per package) with its own reviewed ledger PR. It is
NOT to be initiated to satisfy this slot. Then re-run `/gsd-verify-work 167.1` to harvest it.

**Doc defect found and fixed while harvesting.** The documented harvest command
`gh run download <run-id> -n release-proposal-control-result` does not work — artifact names
are run-id suffixed (`release-proposal-control-result-<run-id>`), so `gh` returns "no artifact
matches any of the names or patterns provided". Both harvest procedures in
`167.1-POSTMERGE-EVIDENCE.md` were corrected and a `gh api .../artifacts` name-resolution step
added. This surfaced only because the procedure was actually executed.

Phase verification correctly remains `human_needed`. Do not canonicalize it to `passed` while
these two slots are empty.
