---
status: partial
phase: 167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev
source: [167.1-VERIFICATION.md]
started: 2026-09-18
updated: 2026-09-18
---

## Current Test

[testing paused — 2 items outstanding, both blocked on an unfired natural trigger]

## Tests

### 1. CTRL-02 harvest — green release-please push run after a `chore: release main` merge
expected: The run concludes `success` (not `skipped`) and the artifact carries `status=pending` with `reason=proposal_awaiting_release_action` or `reason=no_open_proposal`. Commands are in `167.1-POSTMERGE-EVIDENCE.md`: `gh run list --workflow=release-please.yml --branch=main --event=push --limit=1 --json databaseId,headSha,conclusion,createdAt`, then `gh run view <run-id> --json jobs`, then download and inspect the artifact.
result: blocked
blocked_by: prior-phase
reason: "Trigger cannot have fired. Measured 2026-09-18: local `main` is 22 commits ahead of `origin/main`; the fix commit 588f3b8c (`fix(167.1-01): unblock proposal-discovery from should_run for the live world state`) is unpushed. `origin/main` HEAD is dcbda58f (the pre-167.1 v2.8 merge). The latest release-please push run on main is 35361151297 @ dcbda58f, conclusion=failure — a pre-fix run, explicitly disqualified as evidence by 167.1-POSTMERGE-EVIDENCE.md. No `chore: release main` PR has been merged since."

### 2. CTRL-03 harvest — three consecutive push runs plus an aligned schedule firing agree
expected: All three push runs conclude `success`, and `push` and `schedule` at the same SHA report the same status AND the same reason — no #278-style event-scoped split (same SHA, opposite verdict, split by `event`). Per-SHA comparison commands are in `167.1-POSTMERGE-EVIDENCE.md`.
result: blocked
blocked_by: prior-phase
reason: "Same unmet precondition as test 1. Zero post-fix pushes exist on protected `main`, so there cannot be three consecutive post-fix push runs, nor a schedule firing on a post-fix SHA to align against. The only push/schedule pair currently available (push 35361151297 / schedule 35368608097, both @ dcbda58f) is pre-fix and is already recorded in 167.1-CONTEXT.md F-04 as diagnostic context, not as evidence of the fix."

## Summary

total: 2
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

[none — blocked items are prerequisite gates, not code issues]

## Notes

Both items are **natural-trigger, post-merge, calendar-time observations** on protected `main`.
They are not a shortfall of this phase: the structural gap named in the phase goal is fully
closed and verified (13/13 must-haves, 0 gaps). What remains is a real-world CI event outside
this phase's control.

Per the phase's binding constraints and the closing note in `167.1-POSTMERGE-EVIDENCE.md`, these
slots must **never** be filled from inference, from a pre-167.1 run, or from a dispatched
workflow re-run standing in for a natural trigger.

**Precondition (re-measured this session, 2026-09-18).** Still unmet. `git rev-list
--left-right --count origin/main...HEAD` reports `0 22` — the fix has not reached protected
`main`, so neither trigger can have fired. `167.1-POSTMERGE-EVIDENCE.md` harvest tables remain
correctly empty; an empty slot is the truthful representation of an uncollected observation.

**Unblocking sequence.** (1) Ship the 22 local commits to `origin/main` via PR — this produces
the first post-fix push run. (2) CTRL-03 needs two further pushes plus an hourly schedule
firing landing on a SHA a push also observed. (3) CTRL-02 needs a real `chore: release main`
PR to reach a merge. Then re-run `/gsd-verify-work 167.1` to harvest.

Phase verification correctly remains `human_needed`. Do not canonicalize it to `passed` while
these two slots are empty.
