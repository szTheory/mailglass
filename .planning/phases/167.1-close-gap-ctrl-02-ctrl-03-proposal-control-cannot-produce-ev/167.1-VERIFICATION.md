---
phase: 167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev
verified: 2026-09-18T18:22:09Z
status: human_needed
score: 8/8 must-haves verified (structural gap closed); CTRL-02/CTRL-03 Accept clauses are post-merge observations, correctly left evidence-pending
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "CTRL-02 harvest — after the next merge of a chore: release main PR to protected main, run: gh run list --workflow=release-please.yml --branch=main --event=push --limit=1 --json databaseId,headSha,conclusion,createdAt; then gh run view <run-id> --json jobs --jq '.jobs[].steps[] | select(.name | test(\"Write proposal-only release control result|Fail non-pass proposal control result after evidence upload\")) | {name, conclusion}'; then download and inspect release-proposal-control-result.json"
    expected: "The run concludes success (not skipped) and the artifact carries status=pending with reason=proposal_awaiting_release_action or reason=no_open_proposal."
    why_human: "This is a natural-trigger post-merge observation on protected main that no pre-merge plan or verifier run can produce. The fix has not yet been merged to origin/main (local main is 19 commits ahead of origin/main as of this verification)."
  - test: "CTRL-03 harvest — after three consecutive pushes to main plus at least one aligned hourly schedule firing, compare push vs schedule verdicts at the same SHA per the commands in 167.1-POSTMERGE-EVIDENCE.md."
    expected: "All three push runs conclude success; push and schedule at the same SHA report the same status AND the same reason (no #278-style event-scoped split)."
    why_human: "Requires calendar time and real CI activity on protected main after merge. Cannot be simulated, inferred, or dispatched per the phase's own binding constraints."
---

# Phase 167.1: Close gap — CTRL-02/CTRL-03 proposal control cannot produce evidence Verification Report

**Phase Goal:** Close the structural gap that makes the release-proposal control unable to produce
evidence for CTRL-02 and CTRL-03, and make the repository's own requirement text true about why they
were failing.
**Verified:** 2026-09-18T18:22:09Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

This phase has two distinct, deliberately separated success bars, and both must be reported
separately per the phase's own design:

1. **The structural fix (verifiable now, pre-merge).** Did the plans actually decouple discovery
   from `should_run` and make the requirement prose true? — **Verified below, fully.**
2. **The CTRL-02/CTRL-03 Accept clauses (not verifiable now).** Both clauses are natural-trigger
   post-merge observations on protected `main`. Per the phase's own binding constraints ("never
   manufacture evidence... do not dispatch a workflow, do not re-run a job, do not infer a verdict
   from a prior run") these cannot and must not be marked met by this verification. They are
   correctly left evidence-pending, and I additionally confirmed the fix has not even reached
   protected `main` yet (`git fetch origin main` shows local `main` 19 commits ahead of
   `origin/main`, HEAD at `dcbda58f`), so no natural trigger has occurred at all. This routes the
   phase to `human_needed`, not `passed` and not `gaps_found`.

### Observable Truths (Plan-level must_haves, pre-merge)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `proposal-discovery`'s `if:` is un-gated from `should_run`, retaining only `candidate_digest == ''` | ✓ VERIFIED | `release-please.yml:517-518`: `if: ${{ github.event.inputs.candidate_digest == '' }}` — grepped directly, no `should_run` conjunct present |
| 2 | `should_run` is threaded into discovery's `env:` as `SHOULD_RUN`, and `steps.release-preflight.outputs` reference count stays at 7 | ✓ VERIFIED | `grep -c 'steps.release-preflight.outputs' release-please.yml` = 7 (measured directly); `env: SHOULD_RUN: ${{ steps.release-preflight.outputs.should_run }}` present at line 520 |
| 3 | `count==1` + `should_run=false` emits `pending`/`proposal_awaiting_release_action`; `should_run=true` keeps today's hand-off to capture byte-for-byte | ✓ VERIFIED | Read the live `case "$count"` arm (lines ~588-611): `if [ "${SHOULD_RUN:-}" != true ]; then should_capture=false; result_status=pending; result_reason=proposal_awaiting_release_action; fi` |
| 4 | `count>1` (`*)`) + `should_run=false` reproduces capture's own `blocked`/`multiple_open_proposals` verdict verbatim — no new admissible pass pair | ✓ VERIFIED | Read the `*)` arm directly: identical shape, `result_status=blocked`/`result_reason=multiple_open_proposals`; `blocked` absent from both pass predicates (confirmed by direct read of both predicate lines) |
| 5 | `cannot-check` never reaches the gate as a pass on any `should_run` value | ✓ VERIFIED | Both pass predicates read directly (lines 889, 917) — only `pass` or (`pending` AND (`no_open_proposal` OR `proposal_awaiting_release_action`)) pass; `cannot-check` structurally excluded; test "cannot-check and blocked never pass the gate under should_run=false" passes (27/27 suite green) |
| 6 | `capture-proposal` stays gated on `should_run == 'true'` — no pre-sibling-sync digest ever captured | ✓ VERIFIED | `release-please.yml:634-635`: `if: ${{ steps.release-preflight.outputs.should_run == 'true' && github.event.inputs.candidate_digest == '' && steps.proposal-discovery.outputs.should_capture != 'false' }}` — byte-identical to pre-task per SUMMARY's own grep comparison, confirmed by direct read |
| 7 | `proposal-discovery` still refuses the protected candidate-digest dispatch path | ✓ VERIFIED | Same `if:` line — `github.event.inputs.candidate_digest == ''` is the sole remaining gate, confirmed unique in file per REVIEW.md IN-01/IN-02 discussion and my own read |
| 8 | The two pass predicates remain literal twins, differing only in variable names (D-05) | ✓ VERIFIED | Lines 889 and 917 read directly — structurally identical modulo `$status`/`$reason` vs `$RESULT_STATUS`/`$RESULT_REASON`; dedicated D-05 parity test passes |

**Score:** 8/8 plan-level must-haves verified, 0 behavior-unverified (all eight are backed by both
static reads of the live file and passing runtime tests exercising the workflow's own extracted bash
bodies, not presence-only grep).

### Requirement-Text Truths (Plan 02, D-06)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 9 | No sentence in REQUIREMENTS.md still attributes the CTRL-02/CTRL-03 failure to a GitHub rate limit | ✓ VERIFIED | `grep -c 'trips the rate limit'` = 0, `grep -c 'all 15 buckets read full'` = 0 |
| 10 | CTRL-02's and CTRL-03's `*Accept:*` clauses are byte-identical to their pre-task state | ✓ VERIFIED | `git show 5f733317 -- .planning/REQUIREMENTS.md` — diff touches only the mechanism sentence (CTRL-02) and the `*Note:*` line (CTRL-03); both `*Accept:*` lines are untouched context lines in the diff |
| 11 | Both entries stay unchecked | ✓ VERIFIED | `grep -c '\[ \] \*\*CTRL-02\*\*'` = 1, same for CTRL-03 |
| 12 | The post-merge harvest procedure exists with every observation slot unfilled | ✓ VERIFIED | Read `167.1-POSTMERGE-EVIDENCE.md` in full — every "Observed evidence" table cell reads `_(empty)_`; no run ID, SHA, timestamp, or verdict is filled in anywhere |
| 13 | No task asserts, simulates, or manufactures a post-merge observation | ✓ VERIFIED | Read the full harvest doc and both SUMMARYs — explicit closing note ("must never be filled from inference... or from a workflow dispatched or re-run"); confirmed no `gh workflow run` / `gh run rerun` invocation appears anywhere in either plan's commits |

**Score:** 5/5 requirement-text must-haves verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.github/workflows/release-please.yml` | Repaired proposal-discovery path | ✓ VERIFIED | Exists, substantive, wired — read directly, matches every claim in both PLANs and the SUMMARY |
| `test/scripts/release_trigger_recovery_test.exs` | New tests pinning all 5 reachable world states + 2 guards | ✓ VERIFIED | 7 new named tests present (grepped), 27/27 passing at `--seed 0` |
| `.planning/REQUIREMENTS.md` | Corrected CTRL-02/CTRL-03 mechanism prose | ✓ VERIFIED | Diff scoped exactly to the two entries; Accept clauses untouched |
| `.planning/phases/.../167.1-POSTMERGE-EVIDENCE.md` | Harvest procedure, empty slots | ✓ VERIFIED | Exists, all required sections present, no harvested result |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `steps.release-preflight.outputs.should_run` | `proposal-discovery` `env: SHOULD_RUN` | env-threading | ✓ WIRED | Confirmed by direct read of line 520 |
| `steps.proposal-discovery.outputs.result_status`/`result_reason` | writer's override branch → artifact → both pass predicates | case-arm output → `$GITHUB_OUTPUT` → downstream steps | ✓ WIRED | Traced the full chain by reading the file; the e2e test drives the gate from the artifact's own persisted JSON, not hardcoded strings, per Plan 01 Task 2(e)'s explicit design |
| `with_idle_schedule_fixture`'s `SHOULD_RUN` env key | extracted bash bodies under `set -u` | fixture env map → `"${SHOULD_RUN:-}"` defaulted read | ✓ WIRED | 27/27 tests pass, including the tests that exercise this exact path |

### Behavioral Spot-Checks / Probe Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Workflow lints clean | `actionlint .github/workflows/release-please.yml` | clean, no findings | ✓ PASS |
| Workflow parses as YAML | `python3 -c "import yaml; yaml.safe_load(...)"` | `yaml ok` | ✓ PASS |
| Exactly two pass predicates | `grep -vn '^\s*#' ... \| grep '= pass \]'` | 2 hits (lines 889, 917) | ✓ PASS |
| Preflight-output reference count unchanged at 7 | `grep -c 'steps.release-preflight.outputs'` | 7 | ✓ PASS |
| Targeted test file green | `mix test test/scripts/release_trigger_recovery_test.exs --seed 0` | 27 tests, 0 failures | ✓ PASS |
| No debt markers introduced | `git diff dcbda58f..HEAD -- <files> \| grep -E 'TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER'` | no hits | ✓ PASS |
| Full-suite regression (already measured by orchestrator, re-confirmed via targeted file + REVIEW.md's independent 27/27 run) | `mix test --seed 0` | core 2189/0 failures (baseline 2182, +7 exactly matching this phase's new tests) | ✓ PASS |

I additionally ran a check not requested by the plan but material to the human-verification framing:
`git fetch origin main` shows local `main` is **19 commits ahead of `origin/main`** (HEAD at
`dcbda58f`). The 167.1 fix has not yet reached protected `main`, so no natural CTRL-02/CTRL-03 trigger
has occurred yet. This does not constitute a gap in this phase's own scope (merging to protected main
is outside both plans' `files_modified`) — it confirms the harvest document's empty slots are correct
and that `human_needed` is the right verdict, not a symptom of missed work.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| CTRL-02 | 167.1-01, 167.1-02 | Structural fix shipped (discovery decoupled from `should_run`); requirement prose corrected (D-06); Accept clause unchanged | Implemented, evidence pending (unchanged from Phase 166's classification, per design) | Fix verified in code + tests; Accept clause ("merge of a `chore: release main` PR produces a green push run") is a post-merge observation not yet triggerable — fix isn't even on protected `main` yet |
| CTRL-03 | 167.1-01, 167.1-02 | Same structural fix; `*Note:*` corrected to cite measured 5000/5000 quota instead of refuted rate-limit theory | Implemented, evidence pending | Same reasoning; three-consecutive-push + aligned-schedule observation requires calendar time on `main` |

Both requirement IDs declared in PLAN frontmatter (`CTRL-02`, `CTRL-03`) are accounted for. No
orphaned requirements: `REQUIREMENTS.md`'s traceability table (bottom of file) still lists CTRL-02 and
CTRL-03 under "Phase 166" rather than "Phase 166 / 167.1" — this table was generated 2026-09-17,
before Phase 167.1 was inserted into the roadmap, and neither plan's `files_modified` includes it.
This is a pre-existing, out-of-scope cosmetic drift, not a gap created by this phase; flagged as info,
not a blocker.

### Anti-Patterns Found

None. `git diff dcbda58f..HEAD` on the three phase-modified files was scanned for `TBD`/`FIXME`/`XXX`/
`TODO`/`HACK`/`PLACEHOLDER` and hardcoded-empty stub patterns — no hits. `REVIEW.md` (produced by a
prior code-review pass, independently verified here by re-reading the flagged lines) records 0
critical findings, 1 warning (WR-01: the two new/existing reason strings are duplicated literals
across producer/consumer sites with no single source of truth — a real maintainability risk but not a
functional defect, and explicitly not blocking per the review's own classification), and 2 info items
(unquoted bareword comparison; discovery now runs on every non-protected trigger — both documented as
non-issues).

### Human Verification Required

The two items below are exactly the post-merge observations described in
`167.1-POSTMERGE-EVIDENCE.md` — no additional manual steps have been invented:

### 1. CTRL-02 post-merge harvest

**Test:** After the next merge of a `chore: release main` PR to protected `main`, run:
```
gh run list --workflow=release-please.yml --branch=main --event=push --limit=1 --json databaseId,headSha,conclusion,createdAt
gh run view <run-id> --json jobs --jq '.jobs[].steps[] | select(.name | test("Write proposal-only release control result|Fail non-pass proposal control result after evidence upload")) | {name, conclusion}'
gh run download <run-id> -n release-proposal-control-result
cat release-proposal-control-result.json
```
**Expected:** Run concludes `success` (not `skipped`); artifact carries `status=pending` with
`reason=proposal_awaiting_release_action` or `reason=no_open_proposal`.
**Why human:** Natural-trigger post-merge observation on protected `main`. Cannot be produced,
simulated, or dispatched by this or any pre-merge verification. Confirmed the fix has not yet reached
`origin/main` (19 commits ahead locally as of this verification).

### 2. CTRL-03 post-merge harvest

**Test:** After three consecutive pushes to `main` plus at least one aligned hourly schedule firing,
compare push vs. schedule verdicts at the same SHA using the commands in
`167.1-POSTMERGE-EVIDENCE.md` § CTRL-03.
**Expected:** All three push runs conclude `success`; `push` and `schedule` at the same SHA report the
same `status` AND the same `reason` (watch for the #278 signature — same SHA, opposite verdict, split
by event — which would mean the fix regressed, not merely that evidence is still pending).
**Why human:** Requires calendar time and real CI activity on protected `main` after merge.

### Gaps Summary

No gaps in this phase's own deliverable. The structural fix (Plan 01) and the truthful requirement
prose plus harvest procedure (Plan 02) are both fully present, substantive, and wired, verified by
direct reads of the live workflow file, a passing targeted test run (27/27), a passing full-suite
regression (+7 tests over baseline matching the phase's own claim), clean `actionlint`/YAML parsing,
and an exact-diff check that the requirement-prose edit touched only the intended lines while leaving
both Accept clauses byte-identical. The two requirement IDs' Accept clauses remain — correctly, by the
phase's own explicit design and binding constraints — unobserved, because they describe events on
protected `main` that have not yet occurred (the fix itself is not yet merged to `origin/main`). This
routes the phase to `human_needed`, not `gaps_found`: there is nothing here for a planner to close: the
gap the phase named in its own goal is closed, and what remains is a real-world, time-bound event this
plan structurally could not and must not force.

---

_Verified: 2026-09-18T18:22:09Z_
_Verifier: Claude (gsd-verifier)_
