---
phase: 167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev
plan: 02
type: execute
wave: 2
depends_on: ["167.1-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md
autonomous: true
requirements: [CTRL-02, CTRL-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No sentence in REQUIREMENTS.md still attributes the CTRL-02 / CTRL-03 failure to a GitHub rate limit (D-06)."
    - "CTRL-02's and CTRL-03's `*Accept:*` clauses are byte-identical to their pre-task state — the acceptance bar is not moved (D-06, CONTEXT.md scope)."
    - "The post-merge harvest procedure for both Accept clauses is written down, with every observation slot left unfilled."
    - "No task in this plan asserts, simulates, or manufactures a post-merge observation."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md
  key_links:
    - "Plan 01's shipped reason string → the harvest document's expected-verdict table → the eventual UAT record."
    - "CTRL-02/CTRL-03 `*Accept:*` clauses → the harvest document's observation slots (one slot per clause)."
---

<objective>
Make the repository's own requirement text true about why CTRL-02/CTRL-03 were failing (D-06), and
write down the procedure for harvesting the post-merge evidence both Accept clauses demand — so the
observation can be collected cleanly once it naturally exists.

Purpose: the milestone stop line is "every claim the repo makes about itself is either true or tested."
Two requirement entries currently assert a mechanism the 2026-09-18 audit refuted. That is exactly the
DOCS-01 class of untruth this milestone exists to close.

Output: corrected CTRL-02 and CTRL-03 prose with their acceptance bars untouched, and
`167.1-POSTMERGE-EVIDENCE.md` — a harvest procedure with empty observation slots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-CONTEXT.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-POSTMERGE-EVIDENCE.md
@.planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-01-SUMMARY.md
</context>

<binding_constraints>

**The acceptance bar does not move.** Both `*Accept:*` clauses are correct and stay verbatim (D-06,
CONTEXT.md). Only mechanism prose changes. Capture each clause's exact bytes before editing and diff
them after.

**Never manufacture evidence.** Both Accept clauses are post-merge observations that no plan can force.
This plan writes the *procedure*, not the result. Every observation slot ships empty. Do not dispatch a
workflow, do not re-run a job, do not infer a verdict from a prior run.

**Out of scope, unchanged:** GREEN-05 Part 2 and CTRL-05 (genuine timed observations belonging to
Phase 166); PR #280; `.planning/release-target.json`; the `release-please.yml` stale comment block
already filed as tech debt.

**Checkbox state.** CTRL-02 and CTRL-03 stay unchecked. Their fix has shipped, but their acceptance is
unobserved, and an unchecked box is the truthful representation of that.

</binding_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Correct the refuted rate-limit mechanism in CTRL-02 and CTRL-03 (D-06)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - The `### Controls That Cannot Pass (CTRL)` section of `.planning/REQUIREMENTS.md`, in particular
      the already-corrected CTRL-01 entry immediately above CTRL-02 — it is the house shape to match:
      a `- [ ] **ID**:` bullet, a mechanism paragraph, an italic `*Accept:*` clause, optionally an
      italic `*Note:*` clause.
    - `167.1-CONTEXT.md` § Verified Findings F-01..F-05 — the corrected mechanism to write.
    - `167.1-01-SUMMARY.md` — for the reason string as actually shipped.
  </read_first>
  <action>
    Two entries currently assert a diagnosis the 2026-09-18 audit refuted by direct measurement (the
    API budget read 5000/5000; both the push and the schedule run at `dcbda58f` executed only two steps
    with discovery and capture skipped; the reds are 23 consecutive and begin at `7e9617b2`).

    **CTRL-02.** Its mechanism sentence ends with a trailing clause attributing the failure to redundant
    API work exhausting a GitHub quota. Replace that clause with the measured mechanism: `release-preflight`
    correctly reports that the release-please action should not re-run once `main`'s manifest tags all
    exist, but the proposal-evidence steps were gated on that same flag, so the control could not observe
    the repository it reports on; Phase 167.1 decouples discovery from that flag. Keep the preceding
    sentence about the restored tagged-PR preflight skip — that work shipped in Phase 166 and is true.
    Leave the `*Accept:*` line byte-identical.

    **CTRL-03.** Its italic `*Note:*` line asserts the same refuted diagnosis and draws a scope
    conclusion from it. Replace the line with the measured finding — the quota was not exhausted, the
    root cause was the conflated preflight flag, and reducing cron frequency remains not a fix and
    remains out of scope (that conclusion survives; only its stated basis was wrong). Leave CTRL-03's
    mechanism paragraph about classified retry with backoff alone: that mechanism shipped in Phase 166's
    Plan 04 and is true. Leave the `*Accept:*` line, including its bolded `cannot-check` clause,
    byte-identical.

    Both entries stay unchecked. Name Phase 167.1 in each corrected sentence so a reader can trace the
    claim to the diff that made it true. Touch no other requirement entry.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test "$(grep -c 'trips the rate limit' .planning/REQUIREMENTS.md)" = 0 && test "$(grep -c 'all 15 buckets read full' .planning/REQUIREMENTS.md)" = 0 && test "$(grep -c 'the merge of a `chore: release main` PR produces a green `release-please` push run' .planning/REQUIREMENTS.md)" = 1 && test "$(grep -c 'three consecutive pushes to `main` produce `success`' .planning/REQUIREMENTS.md)" = 1 && test "$(grep -c '\[ \] \*\*CTRL-02\*\*' .planning/REQUIREMENTS.md)" = 1 && test "$(grep -c '\[ \] \*\*CTRL-03\*\*' .planning/REQUIREMENTS.md)" = 1</automated>
  </verify>
  <done>
    Neither refuted mechanism claim survives anywhere in REQUIREMENTS.md; both `*Accept:*` clauses are
    still present verbatim and each appears exactly once; both entries are still unchecked; the
    corrected sentences name Phase 167.1 and describe the measured mechanism.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the post-merge harvest procedure with empty observation slots</name>
  <files>.planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/166-earned-greens-and-controls-that-can-pass/166-POSTMERGE-EVIDENCE.md` — the
      in-repo precedent for this document's shape: per-requirement sections, a stated trigger, the exact
      commands to run, and a slot for the harvested result.
    - `167.1-01-SUMMARY.md` — the shipped reason string, needed for the expected-verdict table.
  </read_first>
  <action>
    Create the harvest document. Its job is to make a future observation cheap and unambiguous, and to
    make it obvious that the observation has not happened yet.

    Open with a status line stating plainly that CTRL-02 and CTRL-03 are implemented and
    evidence-pending, that both Accept clauses are natural-trigger post-merge observations, and that
    nothing in this phase may force them.

    **Section — CTRL-02.** Trigger: the next merge of a `chore: release main` PR. Record the command
    that lists the resulting run and the command that reads its conclusion, plus which single step's
    outcome distinguishes a real pass from a skip. Expected verdict: the run concludes `success` and
    uploads `release-proposal-control-result.json`. Leave the observed run ID, SHA, timestamp and
    conclusion as empty slots.

    **Section — CTRL-03.** Trigger: three consecutive pushes to `main`, plus at least one hourly
    schedule firing at a SHA a push also observed. Record the commands to list both event types, the
    per-SHA comparison to perform, and the two facts to assert: all three push runs conclude `success`,
    and `push` and `schedule` at the same SHA report the same status **and** the same reason. Note the
    #278 signature to watch for — same SHA, opposite verdict, split by event. Leave a table with one row
    per observation and every cell empty.

    **Section — expected verdicts by world state.** A small table mapping each observable repository
    state to the status/reason the repaired control should now report and whether the gate passes: no
    open proposal; exactly one open proposal with the manifest tags already present (the state PR #280
    currently produces — cite the reason string exactly as shipped in Plan 01); more than one open
    proposal; and a GitHub API failure. State explicitly that the last two are expected to fail the
    gate, and that this is correct fail-closed behavior rather than a regression.

    **Section — what would refute the fix.** List the signatures that mean Plan 01 did not work: the
    artifact still reporting the blind `cannot-check` verdict after discovery ran; discovery showing as
    skipped on a run with no candidate digest; or push and schedule disagreeing at one SHA.

    **Closing note.** State that this document is filled in by a later `/gsd-verify-work` or milestone
    audit, that an empty slot is the truthful representation of an uncollected observation, and that the
    slots must never be filled from inference, from a pre-167.1 run, or from a dispatched re-run
    standing in for a natural trigger.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test -f .planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md && for s in CTRL-02 CTRL-03 "expected verdicts" "would refute"; do grep -qi "$s" .planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md || { echo "MISSING: $s"; exit 1; }; done && grep -qi 'evidence-pending' .planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-POSTMERGE-EVIDENCE.md && echo "harvest doc ok"</automated>
  </verify>
  <done>
    The harvest document exists with a per-requirement trigger, runnable commands, an expected-verdict
    table citing the shipped reason string, a refutation-signature list, and empty observation slots. No
    slot contains a harvested result, and no workflow was dispatched or re-run to produce one.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` → milestone auditors and release reviewers | requirement prose is read as the acceptance contract for a release-authorization control |
| harvest document → a future `/gsd-verify-work` or milestone audit | an observation slot is read as evidence that a fail-closed control was proven in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-08 | Tampering | CTRL-02 / CTRL-03 `*Accept:*` clauses | high | mitigate | Task 1 edits mechanism prose only; the verify gate counts each Accept clause's literal text and fails if either stops appearing exactly once, so a silently relaxed bar cannot merge. |
| T-167.1-09 | Repudiation | empty observation slots in the harvest document | medium | mitigate | Task 2 states in-document that a slot may never be filled from inference, from a pre-167.1 run, or from a dispatched stand-in; the status line names both requirements as evidence-pending so an auditor cannot mistake the document for evidence. |
| T-167.1-10 | Spoofing | requirement checkbox state | medium | mitigate | Both entries stay unchecked; the verify gate asserts the unchecked form is still present for each ID. |
| T-167.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this plan; no install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- Neither refuted mechanism claim survives in `.planning/REQUIREMENTS.md`.
- Both `*Accept:*` clauses appear exactly once, verbatim; both entries remain unchecked.
- `167.1-POSTMERGE-EVIDENCE.md` exists with all required sections and no harvested result.
- `git diff .planning/REQUIREMENTS.md` touches only the CTRL-02 and CTRL-03 entries.
</verification>

<success_criteria>
- The repository no longer asserts a refuted diagnosis for CTRL-02 or CTRL-03.
- The acceptance bar for both is provably unchanged.
- The post-merge observation is cheap to collect and impossible to confuse with an already-collected one.
</success_criteria>

<output>
Create `.planning/phases/167.1-close-gap-ctrl-02-ctrl-03-proposal-control-cannot-produce-ev/167.1-02-SUMMARY.md` when done.

State in the summary that CTRL-02 and CTRL-03 remain `Implemented, evidence pending` and that the phase
is expected to close `human_needed`. **Do not assert either Accept clause as met.**
</output>
