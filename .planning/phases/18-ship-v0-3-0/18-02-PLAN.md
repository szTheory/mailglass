---
phase: 18-ship-v0-3-0
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DELIV-04

must_haves:
  truths:
    - "`mailglass` `0.3.0` is live on Hex.pm and installable"
    - "`mailglass_admin` `0.3.0` is live on Hex.pm and installable"
    - "The release evidence records that the pre-publish summary was reviewed before environment approval"
    - "The release evidence records that CI was green on the tagged SHA before publish proceeded"
    - "Both HexDocs sites for `0.3.0` are reachable and the fresh-host smoke path passes"
    - "The release evidence file records the real tag, run URLs, approval outcome, and smoke results"
    - "`ROADMAP.md` and `PROJECT.md` are updated only after the release is publicly visible"
  artifacts:
    - path: ".planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md"
      provides: "durable factual log of the real publish and smoke ceremony, including tagged-SHA CI green and pre-publish summary review"
      contains: "Tag:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18 marked complete with final plan count"
      contains: "Phase 18: Ship v0.3.0"
    - path: ".planning/PROJECT.md"
      provides: "DELIV-04 marked complete and milestone state updated to reflect the shipped release"
      contains: "0.3.0"
  key_links:
    - from: ".planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md"
      to: ".github/workflows/publish-hex.yml"
      via: "publish run URL, tag, approval, package outcomes"
    - from: ".planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md"
      to: ".github/workflows/post-publish-smoke.yml"
      via: "smoke run URL, Hex visibility, HexDocs status, fresh-host proof"
---

<objective>
Execute or directly monitor the real `0.3.0` publish ceremony, capture durable evidence, and close the milestone truthfully.

Purpose: This plan owns the irreversible part of Phase 18. It should not reopen editorial questions; it should publish the reviewed release, verify public visibility, record what actually happened, and only then mark `DELIV-04` complete.

Output: A real `18-02-PUBLISH-EVIDENCE.md` file plus completion updates in `ROADMAP.md` and `PROJECT.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/18-ship-v0-3-0/18-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/18-ship-v0-3-0/18-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/18-ship-v0-3-0/18-01-SUMMARY.md
@/Users/jon/projects/mailglass/CHANGELOG.md
@/Users/jon/projects/mailglass/mailglass_admin/CHANGELOG.md
@/Users/jon/projects/mailglass/guides/webhooks.md
@/Users/jon/projects/mailglass/README.md
@/Users/jon/projects/mailglass/MAINTAINING.md
@/Users/jon/projects/mailglass/.github/workflows/publish-hex.yml
@/Users/jon/projects/mailglass/.github/workflows/post-publish-smoke.yml
@/Users/jon/projects/mailglass/lib/mix/tasks/mailglass.publish.check.ex
@/Users/jon/projects/mailglass/test/mailglass/install/install_first_preview_smoke_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute or directly monitor the protected `0.3.0` Hex publish</name>
  <files>.planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md</files>
  <action>
    Perform or directly monitor the real release ceremony on the final reviewed tag.

    Required evidence to record:
    - `Tag: mailglass-v0.3.0`
    - tagged SHA
    - tagged-SHA CI result / CI run URL
    - publish workflow URL
    - pre-publish summary reviewed before approval
    - environment approval outcome / approver
    - `mailglass` publish result (`published` or `already-published`)
    - `mailglass_admin` publish result (`published` or `already-published`)

    Use the existing `publish-hex.yml` path. If the release event does not fan out, use the documented `workflow_dispatch` fallback against the reviewed release tag, not `main`.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Tag: mailglass-v0\.3\.0$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Tagged SHA: [0-9a-f]{7,40}$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Tagged SHA CI: green$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^CI run URL: https://github.com/.+/actions/runs/[0-9]+' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Publish run URL: https://github.com/.+/actions/runs/[0-9]+' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Pre-publish summary reviewed: yes$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Environment approval: .+' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^mailglass publish result: (published|already-published)$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^mailglass_admin publish result: (published|already-published)$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md</automated>
  </verify>
  <done>The real `0.3.0` packages are published or explicitly confirmed already live, and the evidence file records the exact publish path.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Hex index, HexDocs, and fresh-host smoke within the release window</name>
  <files>.planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md</files>
  <action>
    Run or directly monitor the post-publish verification path and record factual results:
    - smoke workflow URL
    - `mix hex.info mailglass 0.3.0`
    - `mix hex.info mailglass_admin 0.3.0`
    - `mailglass` HexDocs HTTP 200
    - `mailglass_admin` HexDocs HTTP 200
    - fresh-host Phoenix install / compile / boot smoke result inside the 60-minute window

    Keep the evidence file literal and timestamped; do not summarize away the exact release proof.
  </action>
  <verify>
    <automated>rg -n '^Smoke run URL: https://github.com/.+/actions/runs/[0-9]+' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^mailglass hex.info: Released:' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^mailglass_admin hex.info: Released:' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^mailglass HexDocs: HTTP 200$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^mailglass_admin HexDocs: HTTP 200$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md && rg -n '^Fresh-host smoke: passed$' .planning/phases/18-ship-v0-3-0/18-02-PUBLISH-EVIDENCE.md</automated>
  </verify>
  <done>The public release is observable and the durable evidence file proves it.</done>
</task>

<task type="auto">
  <name>Task 3: Mark the milestone complete only after public proof exists</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
    After the publish evidence is complete:
    - update `ROADMAP.md` so Phase 18 is marked complete and the final plan count is reflected accurately
    - update `PROJECT.md` so `DELIV-04` is complete and the current-state / milestone notes reflect that `0.3.0` shipped
    - update `.planning/REQUIREMENTS.md` so the `DELIV-04` traceability row is marked `Complete`

    Do not mark completion before the package and docs URLs are live.
  </action>
  <verify>
    <automated>rg -n "Phase 18: Ship v0.3.0.*completed|\| 18\. Ship v0\.3\.0 \| 2/2 \| Complete \|" .planning/ROADMAP.md && rg -n "0\.3\.0 shipped|v0\.3\.0 shipped|DELIV-04 marked complete" .planning/PROJECT.md && rg -n "^\| DELIV-04 \| Phase 18 \| Complete \|$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The planning artifacts reflect reality: the public release is live and the milestone requirement is truly complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow success -> public package availability | A green workflow alone is not enough if Hex index or HexDocs lag or fail |
| Internal completion markers -> real release proof | Marking DELIV-04 complete before public visibility would falsify project state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Denial of Service | Hex index / HexDocs lag | mitigate | Require `mix hex.info`, HTTP 200 checks, and fresh-host smoke before completion |
| T-18-04 | Repudiation | release evidence | mitigate | Record tag, SHA, run URLs, approval, and package outcomes in a durable evidence file |
</threat_model>

<verification>
The evidence file contains real publish and smoke outcomes for `0.3.0`, and only then are `ROADMAP.md` and `PROJECT.md` advanced to completion.
</verification>

<success_criteria>
Phase 18 closes with real public proof: both Hex packages live, both HexDocs sites live, fresh-host smoke passed, and DELIV-04 marked complete truthfully.
</success_criteria>

<output>
After completion, create `.planning/phases/18-ship-v0-3-0/18-02-SUMMARY.md`
</output>
