---
phase: 118-method-audit-storybook-stand-up
plan: 03
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - reference/demo_app/assets/e2e/persona-screenshots.spec.js
  - .planning/research/v1.14/DEFECT-REGISTER.md
autonomous: true
requirements: [METHOD-01]
must_haves:
  truths:
    - "A screenshot seam reusing the EXISTING demo Playwright infra drives the running `make demo`, capturing page.screenshot() per prioritized surface×persona×viewport×theme×state cell into the git-ignored cache."
    - "Adversarial persona/JTBD critic agents (five hats) walk every admin surface — live make demo + storybook + gallery — and author one prioritized, severity-ranked, screenshot-backed defect register."
    - "Every register finding cites its exact cell (surface / persona / viewport / theme / state) + its screenshot path, judged against the binding STRESS-TEST-PROMPT.md rubric — not just WCAG nits."
    - "Screenshots land ONLY in the git-ignored cache (.planning/research/v1.14/.cache/screenshots/); none are committed."
  artifacts:
    - "reference/demo_app/assets/e2e/persona-screenshots.spec.js (screenshot seam reusing @playwright/test — NOT a new harness)"
    - ".planning/research/v1.14/DEFECT-REGISTER.md (the milestone-scoped hit-list; prioritized + severity-ranked)"
  key_links:
    - "Personas → routes: /ops/mail?tenant_id=<northstar|fjordline-aps|helios-void>; theme via ?theme= / emulateMedia; viewport via setViewportSize (reuse structural.spec.js applyThemeEmulation pattern)"
    - "Register lives at MILESTONE scope (.planning/research/v1.14/), NOT the phase dir, so Phases 119-123 consume it without reaching an archived phase dir (D-05)"
    - "Register MUST flag operator.spec.js:352-368 (asserts operator-overview-nav IS visible) as a test Phase 119 must update when the redundant card is removed (RESEARCH Pitfall 2)"
  prohibitions:
    - "DO NOT build a new screenshot harness — reuse the existing @playwright/test infra (D-01)."
    - "DO NOT duplicate Personas.seed! or add a new seed path — make demo already seeds the cohort; duplicating it risks the persona drift-guard (D-03)."
    - "DO NOT run the full Cartesian sweep (~1,620 cells) — author findings off a prioritized representative sample (~80-120 evidence shots), citing the exact cell per finding."
    - "DO NOT commit any screenshot PNG — they are git-ignored evidence only, NOT a pixel-diff baseline (D-02)."
    - "DO NOT screenshot any real-tenant instance — only the seeded synthetic personas (no real PII)."
    - "DO NOT fix any catalogued bug — this phase only catalogues; fixes are Phases 119-123."
    - "DO NOT write DEFECT-REGISTER.md into the phase dir — it is milestone-scoped (D-05)."
---

<objective>
Author the persona-critic harness — a screenshot seam over the running `make demo` plus the new
storybook + retained gallery — and emit the prioritized, severity-ranked, screenshot-backed
DEFECT-REGISTER.md that drives every later surface redesign.

Purpose: METHOD-01 is the keystone deliverable of v1.14 — the inverted, judgment-level review method.
Five adversarial hats walk the full viewport×theme×state matrix using the three named personas, judge
each surface against the binding STRESS-TEST-PROMPT.md rubric (redundancy / IA clarity / least-surprise
/ info-dump / crowding / hierarchy / microcopy / footguns / semantic icons / cross-surface consistency
— the questions no structural gate can ask), and produce the hit-list Phase 119 consumes.
Output: a reusable screenshot seam + the milestone-scoped DEFECT-REGISTER.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-method-audit-storybook-stand-up/118-CONTEXT.md
@.planning/phases/118-method-audit-storybook-stand-up/118-RESEARCH.md
@.planning/research/v1.14/STRESS-TEST-PROMPT.md
@.planning/research/v1.14/MILESTONE-SEED.md
@reference/demo_app/assets/playwright.config.cjs
@reference/persona_spec/personas.ex
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/e2e/operator.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the persona-critic screenshot seam reusing the existing Playwright infra</name>
  <files>reference/demo_app/assets/e2e/persona-screenshots.spec.js</files>
  <read_first>
    - `reference/demo_app/assets/playwright.config.cjs:1-8` — `baseURL`/`DEMO_BASE_URL`/`PORT` resolution (Docker in-network `http://demo:4015` vs host `http://127.0.0.1:4015`).
    - `mailglass_admin/e2e/operator.spec.js:17-40` — the login helpers (`/ops/browser-reset` + `/ops/browser-login`) and how an authenticated operator session is opened.
    - `mailglass_admin/e2e/structural.spec.js` `applyThemeEmulation`/`themeQuery` pattern (~688-704) — the established theme-emulation seam to reuse.
    - 118-RESEARCH.md Pattern 3 (screenshot seam) + Pitfall 4 (Docker base URL) + Pitfall 5 (persona auth + whether browser-* routes exist in :dev) + the sampling-strategy recommendation.
    - `reference/persona_spec/personas.ex` — northstar / fjordline-aps / helios-void cohort and what each stresses (many/high-count/error; one/long-ID/non-ASCII/null; zero-data).
  </read_first>
  <action>
    Per D-01/D-02/D-03/D-04: add a screenshot spec at `reference/demo_app/assets/e2e/persona-screenshots.spec.js`
    that REUSES the existing `@playwright/test` runner (NOT a new harness). It drives the running `make demo`
    via the config `baseURL` (resolve Docker vs host per Pitfall 4 — drive via `make demo-e2e` in-network OR
    set `DEMO_BASE_URL`/`PORT` for a host run). Iterate a PRIORITIZED representative sample (NOT the full
    ~1,620-cell Cartesian sweep — see sampling recommendation): anchor cells = each surface × {northstar,
    fjordline-aps, helios-void} × {375, 1440} × {light, dark}; targeted state cells where a persona naturally
    produces them (helios-void → empty/zero; northstar → error/high-count; fjordline-aps → long-ID/non-ASCII/
    null); spot-check system-theme + 320 + 768/1024 on the #1/#2 surfaces (App-shell+Overview, Deliveries).
    Route personas via `/ops/mail?tenant_id=<persona>` (+ the inbound/preview/gallery/storybook surfaces);
    theme via `?theme=` / `emulateMedia({colorScheme})`; viewport via `setViewportSize`. Authenticate per the
    operator.spec.js helper — FIRST resolve (Pitfall 5) whether `/ops/browser-*` helper routes exist in the
    `:dev` demo or whether to use `/demo/login?return_to=...`; document which path the seam uses. Save PNGs
    to `.planning/research/v1.14/.cache/screenshots/<surface>-<persona>-<vw>-<theme>-<state>.png` (the
    git-ignored cache from Plan 01 — verify the ignore is in effect before the first run). The seam is an
    evidence producer, NOT a pixel-diff baseline (D-02). Do NOT add a new seed path (D-03).
  </action>
  <verify>
    <automated>cd reference/demo_app/assets && npx playwright test e2e/persona-screenshots.spec.js --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `reference/demo_app/assets/e2e/persona-screenshots.spec.js` exists, parses/lists cleanly under the
    existing `@playwright/test` runner, and enumerates a prioritized cell sample (not the full Cartesian
    product) across the three named personas × the anchor viewport/theme square + targeted state cells. It
    writes PNGs only under `.planning/research/v1.14/.cache/screenshots/` (confirmed git-ignored). It reuses
    the existing login + theme-emulation helpers and adds NO new seed path. The login path used against the
    Docker demo is documented in a header comment (Pitfall 5 resolution).
  </acceptance_criteria>
  <done>A reusable, git-ignored-output screenshot seam drives the running demo across the prioritized persona/viewport/theme/state sample.</done>
</task>

<task type="auto">
  <name>Task 2: Run the five-hat walkthrough and author the prioritized, screenshot-backed DEFECT-REGISTER.md</name>
  <files>.planning/research/v1.14/DEFECT-REGISTER.md</files>
  <read_first>
    - `.planning/research/v1.14/STRESS-TEST-PROMPT.md` — the BINDING rubric (must not be diluted): the fractal audit scope A–G, the footgun list, the viewport/theme/state matrix, the expected severity-ranked output shape.
    - 118-CONTEXT.md D-04 (the five hats verbatim) + D-05 (register location + per-finding citation requirement) + the "specifics" section (Apple-like deliberate IA is the north star; use the five hats + three personas as fixed vocabulary).
    - 118-RESEARCH.md Pitfall 2 (the `operator.spec.js:352-368` contradiction to flag) + Pitfall 3 (the bug is the caller literal at `operator_live.ex:349`, shell is correct — name the precise site).
    - The known catalogued bug sites (118-CONTEXT.md integration points): `operator_live.ex:349`, `operator_live.ex:416-448`, `operator/shell.ex:216-244`, `operator/shell.ex:361-424`.
  </read_first>
  <action>
    Per METHOD-01 / D-04 / D-05: run the adversarial persona-critic walkthrough using the screenshot seam from
    Task 1. Drive `make demo`, capture the prioritized cell sample, read the images back, and — wearing each
    of the five hats verbatim (dev-evaluator, library-integrator, maintainer-debugging,
    operator/on-call-SRE-under-stress, security-reviewer) — judge every admin surface (live demo + the new
    `/dev/storybook` + the retained `/dev/mail/gallery`) against the STRESS-TEST-PROMPT.md rubric. Author
    `.planning/research/v1.14/DEFECT-REGISTER.md` (MILESTONE scope — sibling to MILESTONE-SEED.md /
    STRESS-TEST-PROMPT.md, NOT the phase dir, so 119-123 consume it without reaching an archived phase dir).
    The register MUST be prioritized + severity-ranked (Critical/High/Medium/Low), and EVERY finding MUST
    cite: surface, persona, viewport, theme, state, screenshot path, the rubric dimension it violates, and a
    concrete fix-direction + affected file/line where known. Frame findings against the Apple-like deliberate
    IA bar (redundancy / earns-its-place / least-surprise / info-dump-vs-streamlined), not just WCAG nits.
    Use the five hats + three personas as fixed traceable vocabulary. Explicitly register the known headline
    defects with precise sites: false-active-nav at `operator_live.ex:349` (note the SHELL is correct per
    Pitfall 3 — the bug is the caller literal); redundant Navigate cards at `operator_live.ex:416-448`. For
    the redundant-nav finding, FLAG that `operator.spec.js:352-368` (VERIF-02) currently asserts
    `operator-overview-nav` IS visible and that Phase 119 must update/remove it when deleting the card
    (Pitfall 2) — so 119 is not blindsided. Do NOT fix anything here (cataloguing only). Maintainer reviews
    the register at the phase-boundary checkpoint (per MILESTONE-SEED decision 2).
  </action>
  <verify>
    <automated>test -f .planning/research/v1.14/DEFECT-REGISTER.md && grep -ciE 'severity|critical|high|medium|low' .planning/research/v1.14/DEFECT-REGISTER.md && grep -c 'screenshots/' .planning/research/v1.14/DEFECT-REGISTER.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/research/v1.14/DEFECT-REGISTER.md` exists at milestone scope, is non-empty, prioritized +
    severity-ranked, and every finding cites surface/persona/viewport/theme/state + a screenshot path under
    `.cache/screenshots/`. The five hats and three personas appear as the traceable vocabulary. The known
    headline defects are registered with precise sites (`operator_live.ex:349` false-active-nav noting the
    shell is correct; `operator_live.ex:416-448` redundant cards). The register flags `operator.spec.js:352-368`
    as a Phase-119 update target (Pitfall 2). No screenshot PNG is staged for commit
    (`git status --porcelain .planning/research/v1.14/.cache/` shows nothing tracked). No catalogued bug is fixed.
  </acceptance_criteria>
  <done>The prioritized, screenshot-backed hit-list is authored at milestone scope and ready to drive Phase 119.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Path / Symbol | Kind | Notes |
|---|---|---|
| `reference/demo_app/assets/e2e/persona-screenshots.spec.js` | new file | screenshot seam reusing `@playwright/test`; output to the git-ignored cache only |
| `.planning/research/v1.14/DEFECT-REGISTER.md` | new file | milestone-scoped, prioritized, severity-ranked, screenshot-backed hit-list (D-05) |
| `.planning/research/v1.14/.cache/screenshots/*.png` | git-ignored evidence | NOT committed, NOT a pixel-diff baseline (D-02) |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot evidence → git history | synthetic-persona PNGs must stay out of version control |
| demo instance → screenshot seam | the seam must target the seeded synthetic personas, never a real-tenant instance |

## STRIDE Threat Register (ASVS L1; block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-05 | Information disclosure | screenshots committed to the repo | medium | mitigate | the `.cache/` dir is git-ignored (Plan 01 Task 1, verified before first run); register cites paths, never inlines images (D-02) |
| T-118-06 | Information disclosure | screenshots capture real PII | low | mitigate | personas are synthetic seed data (`make demo` cohort, no real PII); the seam targets only `/ops/mail?tenant_id=<persona>`, never a real-tenant instance (D-03) |

(No `high` threats: this plan produces dev-only evidence + a research artifact; nothing ships, no auth/crypto/data path is touched.)
</threat_model>

<verification>
- `npx playwright test e2e/persona-screenshots.spec.js --list` enumerates the prioritized cell sample (Task 1).
- `git check-ignore .planning/research/v1.14/.cache/screenshots/x.png` exits 0 BEFORE the first screenshot run (depends on Plan 01).
- `.planning/research/v1.14/DEFECT-REGISTER.md` exists, severity-ranked, each finding citing its cell + screenshot path.
- `git status --porcelain .planning/research/v1.14/.cache/` shows no tracked PNGs.
- No catalogued bug is fixed (no edit to `operator_live.ex` / `operator/shell.ex` / `operator.spec.js`).
</verification>

<success_criteria>
- A reusable screenshot seam drives the running demo across the prioritized persona/viewport/theme/state sample, writing only to the git-ignored cache.
- The milestone-scoped DEFECT-REGISTER.md is authored — prioritized, severity-ranked, screenshot-backed, judged against the binding STRESS-TEST-PROMPT.md rubric, with every finding citing its exact cell.
- The register flags the `operator.spec.js` contradiction for Phase 119 and names the precise headline-bug sites; nothing is fixed here.
</success_criteria>

<output>
Create `.planning/phases/118-method-audit-storybook-stand-up/118-03-SUMMARY.md` when done
</output>
