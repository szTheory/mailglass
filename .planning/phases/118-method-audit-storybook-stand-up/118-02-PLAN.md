---
phase: 118-method-audit-storybook-stand-up
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/e2e/judgment.spec.js
autonomous: true
requirements: [METHOD-02, STORY-02]
must_haves:
  truths:
    - "Two new judgment gates exist as Playwright rendered-DOM assertions in a sibling of structural.spec.js, expressed as test.fixme (skipped — neither red nor green) asserting the CORRECT end-state."
    - "The two gates assert: nav-active-correctness (on Overview, the :overview nav item carries aria-current=page and Deliveries is inactive) and no-nav-duplication (populated Overview does not render data-testid=operator-overview-nav)."
    - "The full v1.13 ratchet floor is verified green on clean main: ~26 conformance gates + 54-cell aesthetic + 9-cell axe + 24-item Bucket-A + persona drift-guard — a verification run ONLY, no re-score, no arming."
    - "/dev/mail/gallery (gallery_live.ex) is retained UNCHANGED — no testid churn, no drift-guard regression."
  artifacts:
    - "mailglass_admin/e2e/judgment.spec.js (NEW; two test.fixme drafted gates)"
    - "A verify-green log/evidence captured in the SUMMARY: each of the five floor artifacts run and green"
  key_links:
    - "The new gates plug into the existing accent-allowlist Playwright seam that keys off [aria-current='page'] (structural.spec.js:11-17)"
    - "The no-nav-duplication gate targets data-testid=operator-overview-nav (operator_live.ex:416) — the same testid the existing operator.spec.js:367 asserts IS visible (the Pitfall-2 contradiction to flag for Phase 119)"
  prohibitions:
    - "DO NOT weaken either assertion to pass on the live buggy UI — arm them green now and you bake the bug into the gate (D-12). They MUST be test.fixme asserting the correct end-state."
    - "DO NOT add judgment.spec.js to any required CI lane in Phase 118 (D-13 — drafted only; armed into the floor in Phase 123)."
    - "DO NOT fix the false-active-nav bug or remove the redundant Navigate cards here — that is Phase 119 (user-confirmed: do not front-load the redesign)."
    - "DO NOT modify operator.spec.js:352-368 (the opposing VERIF-02 test) — only FLAG it; Phase 119 updates it."
    - "DO NOT re-score the aesthetic ratchet or arm new gates into the floor — verify-green only (D-13)."
    - "DO NOT touch gallery_live.ex or any gallery testid (STORY-02 / D-10 — retained unchanged)."
---

<objective>
Draft the two new judgment-level regression gates (nav-active-correctness, no-nav-duplication) as
pending Playwright assertions, and prove the inherited v1.13 ratchet floor is green on clean main.

Purpose: METHOD-02 requires new gates that catch the headline IA bugs structural greps cannot (active
state is a RENDERED property; a hardcoded `active={:deliveries}` literal grep can't evaluate it). In
Phase 118 these gates are DRAFTED asserting the correct end-state — they turn green in Phase 119 (when
the bug is fixed) and are armed into the floor in Phase 123. This plan also verifies (no re-score) that
the five inherited floor artifacts are green, and confirms `/dev/mail/gallery` is retained unchanged.
Output: a drafted `judgment.spec.js` + a verify-green floor log in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-method-audit-storybook-stand-up/118-CONTEXT.md
@.planning/phases/118-method-audit-storybook-stand-up/118-RESEARCH.md
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/e2e/operator.spec.js
@mailglass_admin/scripts/check-conformance.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft judgment.spec.js — two pending rendered-DOM gates asserting the correct end-state</name>
  <files>mailglass_admin/e2e/judgment.spec.js</files>
  <read_first>
    - `mailglass_admin/e2e/structural.spec.js:1-50` — the sibling pattern: requires, `baseURL`, `ACCENT_ALLOWLIST` keying off `[aria-current='page']`, and the `openOperator` login helper (browser-reset + browser-login).
    - `mailglass_admin/e2e/operator.spec.js:17-40` (login helpers) and `:352-368` (the opposing VERIF-02 test that asserts `operator-overview-nav` IS visible).
    - `mailglass_admin/lib/mailglass_admin/operator_live.ex:349` (hardcoded `active={:deliveries}` — the bug) and `:416` (`data-testid="operator-overview-nav"` — the redundant card block).
    - 118-RESEARCH.md Pattern 4 (drafted judgment gates, `test.fixme` mechanism) + Pitfall 2 (the contradiction) + Pitfall 3 (shell is correct; only the caller literal is the bug).
  </read_first>
  <action>
    Per D-11/D-12: create `mailglass_admin/e2e/judgment.spec.js` as a sibling of `structural.spec.js`,
    reusing the same login/navigation helpers (an `openOverview(page)` that reaches the Overview view — the
    default `/ops/mail?tenant_id=...` with no `&view=deliveries`). Express BOTH gates as `test.fixme(...)`
    (skipped — Playwright reports them as skipped, never red/green; the body is not executed) so they do NOT
    run green against the live bug. Each gate's body asserts the CORRECT END-STATE:
      - nav-active-correctness: on the Overview route the Deliveries nav link does NOT carry
        `aria-current="page"`, AND an Overview nav item exists carrying `aria-current="page"`. (Today
        `operator_live.ex:349` hardcodes `active={:deliveries}` and there is no `:overview` nav item — so
        this MUST be `test.fixme`, not armed.)
      - no-nav-duplication: the populated Overview renders zero `data-testid="operator-overview-nav"`
        elements (`toHaveCount(0)`).
    Add a header comment on each gate explaining it turns green in Phase 119 (bug fixed) and is armed into
    the ratchet floor in Phase 123 (D-12). Do NOT add this spec to any required CI lane (D-13). Do NOT fix
    the bug or touch `operator_live.ex`. Do NOT modify `operator.spec.js`.

    Comment-text discipline: the conformance/voice gates do NOT scan `mailglass_admin/e2e/*.js`, but keep the
    header comments conceptual — describe the bug by reference (line numbers, behavior), do not paste literal
    banned-word strings.
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test e2e/judgment.spec.js --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `mailglass_admin/e2e/judgment.spec.js` exists with exactly two `test.fixme(...)` entries — one for
    nav-active-correctness, one for no-nav-duplication — each asserting the correct end-state (Deliveries NOT
    `aria-current="page"` on Overview + an Overview item that IS; and `operator-overview-nav` count 0). The
    spec lists/parses cleanly (`--list` enumerates both as fixme/skipped). The spec is NOT referenced by any
    CI required-lane config. `operator_live.ex` and `operator.spec.js` are unchanged. `grep -n "test.fixme"
    mailglass_admin/e2e/judgment.spec.js` returns two lines.
  </acceptance_criteria>
  <done>Two drafted, never-armed judgment gates assert the correct end-state and will flip green in Phase 119.</done>
</task>

<task type="auto">
  <name>Task 2: Verify-green the five inherited floor artifacts (no re-score, no arming) + confirm gallery unchanged</name>
  <files>mailglass_admin/e2e/judgment.spec.js</files>
  <read_first>
    - 118-CONTEXT.md D-13/D-14 — "inherit green" = verification run ONLY; the exact five floor artifacts.
    - `mailglass_admin/scripts/check-conformance.sh` — the ~26-gate floor (PRIMITIVE-DRIFT, FORM-DRIFT, STATCARD, ICON-EXISTS, BADGE, TYPE, BOLD, GAP, SPACE, GROUP, HEX, Z-INDEX, FOCUS-RING, SCOPE, TOKEN-SCOPE, PHASE112-SHELL, VOICE, RADIUS, SHADOW, BORDER, SIZE, MOTION, STATUS-BADGE, DATA-STATE, TABLE-OVERUSE).
    - Project memory: bare `mix test` has ~57 unrelated Oban failures in worktrees and `voice_test.exs` substring-matches dep JS — scope to the named baseline test files, do NOT run a bare full `mix test`.
  </read_first>
  <action>
    Per D-13/D-14: run a VERIFICATION-ONLY pass over the five inherited floor artifacts on clean main. Do NOT
    re-score the aesthetic ratchet, do NOT arm the new judgment gates, do NOT promote current→prior. Run each
    and capture green evidence into the SUMMARY:
      1. `bash mailglass_admin/scripts/check-conformance.sh` (~26 conformance gates) — exits 0.
      2. `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs` (54-cell aesthetic, schema_version 3).
      3. `cd mailglass_admin && mix test test/mailglass_admin/axe_baseline_test.exs` (9-cell axe baseline; the paired `e2e/axe-baseline.spec.js` is the Playwright producer — note if it needs a running browser server).
      4. `cd mailglass_admin && mix test test/mailglass_admin/bucket_a_coverage_test.exs` (24-item Bucket-A manifest, coupled TABLE_FLOOR=3).
      5. `cd mailglass_admin && mix test test/mailglass_admin/persona_drift_guard_test.exs test/mailglass_admin/persona_cohort_test.exs` (persona drift-guard cohort).
    Also confirm STORY-02: `gallery_live.ex` is byte-unchanged this phase (`git diff --stat
    mailglass_admin/lib/mailglass_admin/gallery_live.ex` shows no change) and the gallery's structural/drift
    tests remain green (they run within conformance + the baseline tests above). Capture any known flake/seed
    notes (e.g. axe producer needs the operator browser server; persona tests are deterministic). This task
    writes NO production code — its sole output is the verify-green evidence recorded in the SUMMARY. (The
    `files_modified` entry is the plan's only new file, judgment.spec.js, shared with Task 1 — this task adds
    nothing to it.)
  </action>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance.sh && cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs test/mailglass_admin/bucket_a_coverage_test.exs test/mailglass_admin/persona_drift_guard_test.exs test/mailglass_admin/persona_cohort_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    All five floor artifacts run green on clean main: `check-conformance.sh` exits 0 (~26 gates);
    `ratchet_baseline_test.exs` passes (54 cells, schema_version 3, no re-score); `axe_baseline_test.exs`
    passes (9 cells); `bucket_a_coverage_test.exs` passes (24 items, TABLE_FLOOR=3); `persona_drift_guard_test.exs`
    + `persona_cohort_test.exs` pass. `git diff --stat mailglass_admin/lib/mailglass_admin/gallery_live.ex`
    shows no change (STORY-02). No `prior`/`current` block in the aesthetic baseline was promoted (no re-score).
    The judgment gates are still `test.fixme` (not armed). Any known flake/seed caveat is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The inherited v1.13 floor is proven green (verify-only, no re-score, no arming); the gallery is unchanged.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

| Path / Symbol | Kind | Notes |
|---|---|---|
| `mailglass_admin/e2e/judgment.spec.js` | new file | two `test.fixme` drafted gates: nav-active-correctness + no-nav-duplication, asserting the correct end-state (D-11/D-12) |
| verify-green floor log (in SUMMARY) | evidence | the five D-14 floor artifacts run green; gallery unchanged (STORY-02) |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted gate → CI required lane | a drafted gate must NOT enter a blocking lane while asserting an unfixed end-state |

## STRIDE Threat Register (ASVS L1; block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-03 | Tampering | a weakened assertion bakes the live bug into the new gate | low | mitigate | gates are `test.fixme` asserting the CORRECT end-state, never armed green in 118 (D-12); not added to any required lane (D-13) |
| T-118-04 | Repudiation | "floor green" claimed without evidence | low | mitigate | each of the five floor artifacts is run and its green result captured in the SUMMARY (verify-only, no re-score) |

(No `high` threats: this plan adds a skipped test file and runs read-only verifications. No data path, auth, crypto, or shipped surface is touched.)
</threat_model>

<verification>
- `npx playwright test e2e/judgment.spec.js --list` enumerates exactly two fixme/skipped gates.
- `grep -n "test.fixme" mailglass_admin/e2e/judgment.spec.js` returns two lines.
- All five D-14 floor artifacts run green (conformance script + four ExUnit baseline test files + axe).
- `git diff --stat mailglass_admin/lib/mailglass_admin/gallery_live.ex` is empty (STORY-02).
- `operator_live.ex` and `operator.spec.js` are unchanged (no bug fix, no front-loaded redesign).
</verification>

<success_criteria>
- Two drafted judgment gates (`test.fixme`) assert the correct end-state and are NOT armed into any lane.
- The inherited v1.13 ratchet floor is verified green on clean main (no re-score, no arming).
- `/dev/mail/gallery` is retained unchanged with no drift-guard regression.
</success_criteria>

<output>
Create `.planning/phases/118-method-audit-storybook-stand-up/118-02-SUMMARY.md` when done
</output>