---
phase: 61-docs-contract-boundary-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/host_app/README.md
  - reference/host_app/SCOPE.md
  - test/reference_host/trust_runner_command_contract_test.exs
autonomous: true
requirements: [DOCB-01, DOCB-02]
must_haves:
  truths:
    - "Reference-host docs state that `reference/host_app` is usage-proof evidence only, not API-contract truth."
    - "Reference-host scope docs keep the host framed as a thin maintained baseline, not a second product surface or fixture seed."
    - "Reference-host contract tests fail closed if canonical stability links or usage-proof boundary wording drift."
  artifacts:
    - path: "reference/host_app/README.md"
      provides: "Reference-host usage-proof boundary and canonical contract-routing text"
      contains: "usage-proof evidence only"
    - path: "reference/host_app/SCOPE.md"
      provides: "Reference-host non-goals and canonical contract-truth routing"
      contains: "not API-contract truth"
    - path: "test/reference_host/trust_runner_command_contract_test.exs"
      provides: "Deterministic token assertions for reference-host boundary language"
      contains: "Phase boundary drift"
  key_links:
    - from: "test/reference_host/trust_runner_command_contract_test.exs"
      to: "reference/host_app/README.md"
      via: "File.read! + required_tokens assertions"
      pattern: "required_tokens"
    - from: "test/reference_host/trust_runner_command_contract_test.exs"
      to: "reference/host_app/SCOPE.md"
      via: "scope token assertions"
      pattern: "scope"
---

<objective>
Lock the reference-host docs to the Phase 61 posture: `reference/host_app` is
usage-proof evidence only, while stable guarantees route to canonical
`api_stability.md` inventories and executable contract checks.

Purpose: Implement D-01, D-02, D-03, and D-07 at the reference-host entrypoint
so the maintained host cannot be mistaken for public API truth.
Output: Updated `reference/host_app` docs plus deterministic reference-host
contract coverage in `test/reference_host/trust_runner_command_contract_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/61-docs-contract-boundary-enforcement/61-CONTEXT.md
@.planning/phases/61-docs-contract-boundary-enforcement/61-RESEARCH.md
@reference/host_app/README.md
@reference/host_app/SCOPE.md
@docs/api_stability.md
@mailglass_admin/docs/api_stability.md
@mailglass_inbound/docs/api_stability.md
@test/reference_host/trust_runner_command_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the reference-host entry docs to the usage-proof-only boundary</name>
  <files>reference/host_app/README.md, reference/host_app/SCOPE.md</files>
  <read_first>
    - reference/host_app/README.md
    - reference/host_app/SCOPE.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-CONTEXT.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-RESEARCH.md
    - docs/api_stability.md
    - mailglass_admin/docs/api_stability.md
    - mailglass_inbound/docs/api_stability.md
  </read_first>
  <action>
    Edit `reference/host_app/README.md` and `reference/host_app/SCOPE.md` so the
    reference host explicitly says it is usage-proof evidence only per D-01 and a
    thin maintained trust-proof baseline per D-02. Add exact canonical-routing
    text per D-03/D-07: point stable guarantees to `docs/api_stability.md`,
    `mailglass_admin/docs/api_stability.md`, `mailglass_inbound/docs/api_stability.md`,
    and `mix verify.stability_contract`. Keep the existing public-seam boundary,
    but add non-contract framing that route names, checkpoint details, and trust-runner
    implementation reachability are implementation details unless those stability
    inventories say otherwise. In `SCOPE.md`, add explicit statements that the
    host is not API-contract truth, not a second product surface, and not a
    fixture seed.
  </action>
  <verify>
    <automated>grep -q 'usage-proof evidence only' reference/host_app/README.md && grep -q 'mix verify.stability_contract' reference/host_app/README.md && grep -q 'docs/api_stability.md' reference/host_app/README.md && grep -q 'mailglass_admin/docs/api_stability.md' reference/host_app/README.md && grep -q 'mailglass_inbound/docs/api_stability.md' reference/host_app/README.md && grep -q 'not API-contract truth' reference/host_app/SCOPE.md && grep -q 'second product surface' reference/host_app/SCOPE.md && grep -q 'fixture seed' reference/host_app/SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - `reference/host_app/README.md` contains the literal phrase `usage-proof evidence only`.
    - `reference/host_app/README.md` routes guarantee semantics to `docs/api_stability.md`, `mailglass_admin/docs/api_stability.md`, `mailglass_inbound/docs/api_stability.md`, and `mix verify.stability_contract`.
    - `reference/host_app/README.md` keeps the existing public-seam boundary while adding non-contract framing for route names, checkpoint details, or trust-runner implementation details.
    - `reference/host_app/SCOPE.md` contains `not API-contract truth`, `second product surface`, and `fixture seed` in explicit non-goal or boundary language.
    - Neither file introduces provider-breadth, release-gate, or product-feature scope outside Phase 61.
  </acceptance_criteria>
  <done>
    The reference-host entry docs now make the usage-proof-only boundary explicit
    and route stable-guarantee truth to the canonical stability inventories and
    executable contract lane.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the reference-host boundary language in the existing contract test</name>
  <files>test/reference_host/trust_runner_command_contract_test.exs</files>
  <read_first>
    - test/reference_host/trust_runner_command_contract_test.exs
    - reference/host_app/README.md
    - reference/host_app/SCOPE.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-CONTEXT.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-RESEARCH.md
  </read_first>
  <behavior>
    - PASS when `reference/host_app/README.md` contains the usage-proof-only boundary and the canonical stability-routing tokens added in Task 1.
    - PASS when `reference/host_app/SCOPE.md` contains the non-product / non-fixture / non-contract boundary tokens added in Task 1.
    - FAIL when the reference-host docs drift back toward `API-contract truth`, `fixture seed`, or missing canonical stability links.
  </behavior>
  <action>
    Extend `test/reference_host/trust_runner_command_contract_test.exs` instead of
    creating a new test file, per D-05. Add a `@scope_path` constant and a focused
    assertion block that reads both `reference/host_app/README.md` and
    `reference/host_app/SCOPE.md`, then loops over required tokens for the new
    Phase 61 language. Reuse the existing `required_tokens` / `String.contains?/2`
    pattern. Add `refute` assertions for overreach phrases such as `API-contract truth`
    without negation and for reintroduced fixture-seed framing. Do not change the
    existing claim-boundary constant or the Phase 57/58 runner-command assertions.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/reference_host/trust_runner_command_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/reference_host/trust_runner_command_contract_test.exs` reads both `reference/host_app/README.md` and `reference/host_app/SCOPE.md`.
    - The test asserts the Phase 61 tokens from Task 1, including the three canonical `api_stability.md` paths and `mix verify.stability_contract`.
    - The test refutes reference-host overreach wording that would frame the host as contract truth or a fixture seed.
    - `MIX_ENV=test mix test test/reference_host/trust_runner_command_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Reference-host boundary language is enforced by the existing deterministic
    contract test, so future wording drift fails closed in CI-compatible local
    verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reference-host docs -> adopter interpretation | The maintained host docs can be misread as public API guarantee truth unless they explicitly route guarantees elsewhere. |
| Reference-host docs -> reference-host contract tests | Without deterministic tests, wording drift could silently weaken the usage-proof-only boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | `reference/host_app/README.md`, `reference/host_app/SCOPE.md` | mitigate | Add explicit usage-proof-only and non-product boundary text, then pin it in `test/reference_host/trust_runner_command_contract_test.exs`. |
| T-61-02 | Spoofing | Reference-host docs presented as canonical contract truth | mitigate | Route guarantees to `docs/api_stability.md`, `mailglass_admin/docs/api_stability.md`, `mailglass_inbound/docs/api_stability.md`, and `mix verify.stability_contract` per D-03. |
| T-61-03 | Repudiation | Reference-host boundary drift goes unverified | mitigate | Extend the existing reference-host ExUnit contract test so missing boundary tokens or reintroduced overreach phrases fail deterministically. |
| T-61-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/reference_host/trust_runner_command_contract_test.exs --warnings-as-errors`
- Grep proof that the new usage-proof and canonical-stability tokens are present in both reference-host docs
</verification>

<success_criteria>
- `reference/host_app/README.md` and `reference/host_app/SCOPE.md` explicitly frame the reference host as usage-proof evidence only and not API-contract truth.
- Reference-host docs route stable guarantees to the three canonical `api_stability.md` inventories and `mix verify.stability_contract`.
- `test/reference_host/trust_runner_command_contract_test.exs` fails closed on boundary-language drift.
</success_criteria>

<output>
Create `.planning/phases/61-docs-contract-boundary-enforcement/61-01-SUMMARY.md` when done
</output>
