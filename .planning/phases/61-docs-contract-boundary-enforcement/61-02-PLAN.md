---
phase: 61-docs-contract-boundary-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
  - guides/webhooks.md
  - guides/webhook-troubleshooting.md
  - mailglass_admin/docs/operator-trust.md
autonomous: true
requirements: [DOCB-01, DOCB-02]
must_haves:
  truths:
    - "Trust-entry docs outside `reference/host_app` route guarantee semantics to canonical stability inventories and executable contract lanes."
    - "Trust-entry docs keep internal names usable for troubleshooting only when they are explicitly framed as implementation detail, not stable API."
    - "Maintainer and operator docs do not treat reference-host or trust-runner implementation details as public contract truth."
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Maintainer-facing routing from trust artifacts to canonical contract truth"
      contains: "mix verify.stability_contract"
    - path: "guides/webhooks.md"
      provides: "Webhook guide boundary text linking to canonical core/inbound stability docs"
      contains: "implementation detail"
    - path: "guides/webhook-troubleshooting.md"
      provides: "Webhook troubleshooting shim that routes trust semantics to canonical contract sources"
      contains: "canonical"
    - path: "mailglass_admin/docs/operator-trust.md"
      provides: "Operator trust semantics that link to admin/core stability inventories without widening the stable surface"
      contains: "api_stability.md"
  key_links:
    - from: "guides/webhooks.md"
      to: "docs/api_stability.md"
      via: "Markdown link in trust-boundary introduction"
      pattern: "docs/api_stability.md"
    - from: "mailglass_admin/docs/operator-trust.md"
      to: "mailglass_admin/docs/api_stability.md"
      via: "Markdown link in stable-seams framing"
      pattern: "api_stability.md"
---

<objective>
Rewrite the trust-entry docs outside `reference/host_app` so they route guarantee
semantics to canonical stability inventories and executable contract lanes, while
keeping internal names available only as explicitly non-contract troubleshooting
context.

Purpose: Implement D-03, D-04, D-07, and D-08 across the broader trust-entry
surface without widening the public contract.
Output: Updated maintainer, webhook, troubleshooting, and operator-trust docs
that all point to canonical contract truth.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/61-docs-contract-boundary-enforcement/61-CONTEXT.md
@.planning/phases/61-docs-contract-boundary-enforcement/61-RESEARCH.md
@MAINTAINING.md
@guides/webhooks.md
@guides/webhook-troubleshooting.md
@mailglass_admin/docs/operator-trust.md
@docs/api_stability.md
@mailglass_admin/docs/api_stability.md
@mailglass_inbound/docs/api_stability.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route maintainer and webhook trust docs to canonical contract truth</name>
  <files>MAINTAINING.md, guides/webhooks.md, guides/webhook-troubleshooting.md</files>
  <read_first>
    - MAINTAINING.md
    - guides/webhooks.md
    - guides/webhook-troubleshooting.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-CONTEXT.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-RESEARCH.md
    - docs/api_stability.md
    - mailglass_inbound/docs/api_stability.md
  </read_first>
  <action>
    Edit `MAINTAINING.md`, `guides/webhooks.md`, and
    `guides/webhook-troubleshooting.md` per D-03/D-04/D-07/D-08. Add explicit
    trust-boundary text that the reference host and trust journey are usage-proof
    and release-evidence artifacts, while stable guarantees live in
    `docs/api_stability.md`, `mailglass_inbound/docs/api_stability.md`, and
    `mix verify.stability_contract`. In the webhook guide, add a short
    introduction or boundary note that provider modules, checkpoint details, and
    dev-only trust-runner internals are implementation details unless those
    canonical inventories say otherwise. In the troubleshooting shim, keep the
    page as an entry shim and add contract-routing text so incident semantics
    point to canonical inventories and executable contract verification rather
    than local page reachability.
  </action>
  <verify>
    <automated>grep -q 'mix verify.stability_contract' MAINTAINING.md && grep -q 'docs/api_stability.md' guides/webhooks.md && grep -q 'mailglass_inbound/docs/api_stability.md' guides/webhooks.md && grep -q 'implementation detail' guides/webhooks.md && grep -q 'canonical' guides/webhook-troubleshooting.md</automated>
  </verify>
  <acceptance_criteria>
    - `MAINTAINING.md` names `mix verify.stability_contract` as the executable contract lane for guarantee semantics.
    - `guides/webhooks.md` links to `docs/api_stability.md` and `mailglass_inbound/docs/api_stability.md`.
    - `guides/webhooks.md` explicitly says provider modules, checkpoint details, or trust-runner internals are implementation detail / non-contract context.
    - `guides/webhook-troubleshooting.md` keeps its entry-shim posture and adds canonical contract-routing language instead of implying the shim is contract truth.
    - The edits do not widen supported providers, trust-lane scope, or release policy beyond Phase 61.
  </acceptance_criteria>
  <done>
    Maintainer and webhook trust docs now point readers to canonical contract
    truth and use internal names only with explicit non-contract framing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten operator-trust wording around semantic seams versus implementation detail</name>
  <files>mailglass_admin/docs/operator-trust.md</files>
  <read_first>
    - mailglass_admin/docs/operator-trust.md
    - mailglass_admin/docs/api_stability.md
    - docs/api_stability.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-CONTEXT.md
    - .planning/phases/61-docs-contract-boundary-enforcement/61-RESEARCH.md
  </read_first>
  <action>
    Update `mailglass_admin/docs/operator-trust.md` so the intro or stable-seams
    framing explicitly routes stable guarantees to `mailglass_admin/docs/api_stability.md`
    and, where core/inbound semantics are referenced, to `docs/api_stability.md`
    or `mailglass_inbound/docs/api_stability.md`. Preserve the existing "semantic
    seams, not implementation freeze" posture, but strengthen it per D-08: if
    internal names such as LiveView modules, replay plumbing, or route evidence
    appear for troubleshooting context, mark them as implementation detail and
    not a stable API guarantee. Do not widen the set of stable seams listed in
    the doc.
  </action>
  <verify>
    <automated>grep -q 'mailglass_admin/docs/api_stability.md' mailglass_admin/docs/operator-trust.md && grep -q 'docs/api_stability.md' mailglass_admin/docs/operator-trust.md && grep -q 'implementation detail' mailglass_admin/docs/operator-trust.md</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_admin/docs/operator-trust.md` links to `mailglass_admin/docs/api_stability.md`.
    - `mailglass_admin/docs/operator-trust.md` links to the relevant core or inbound `api_stability.md` doc when it talks about shared semantics.
    - The doc contains explicit `implementation detail` or equivalent non-contract wording for internal names and UI plumbing.
    - The stable-seams list remains semantic and does not add new public router/auth/UI guarantees.
  </acceptance_criteria>
  <done>
    Operator-trust docs remain useful for troubleshooting but now route guarantee
    truth to canonical inventories instead of local implementation reachability.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trust-entry docs -> adopter/maintainer interpretation | These docs can be mistaken for the canonical contract unless they explicitly link to stable inventories and contract lanes. |
| Internal names in docs -> public-contract assumptions | Troubleshooting details can be read as API guarantees unless they are explicitly marked as implementation detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | `MAINTAINING.md`, `guides/webhooks.md`, `guides/webhook-troubleshooting.md`, `mailglass_admin/docs/operator-trust.md` | mitigate | Add explicit canonical-routing language so trust-entry docs cannot silently drift into contract-truth posture. |
| T-61-02 | Spoofing | Reference-host or internal implementation details presented as canonical guarantees | mitigate | Add `api_stability.md` links plus explicit implementation-detail framing wherever internal names are used. |
| T-61-03 | Repudiation | Trust-entry docs outside Tier 1 remain ambiguous and bypass later deterministic checks | mitigate | Update every D-04 trust-entry surface now so Plan 03 can enforce the exact wording via the existing docs checker and ExUnit contract tests. |
| T-61-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan. |
</threat_model>

<verification>
- Grep proof that the updated docs contain canonical `api_stability.md` links and executable contract-lane references
- Spot-check that internal names are paired with explicit non-contract / implementation-detail wording
</verification>

<success_criteria>
- `MAINTAINING.md`, `guides/webhooks.md`, `guides/webhook-troubleshooting.md`, and `mailglass_admin/docs/operator-trust.md` all route guarantee semantics to canonical stability inventories and executable contract lanes.
- Internal or troubleshooting-only names remain documented only with explicit non-contract framing.
- No doc in this plan widens the public API, provider scope, or release posture.
</success_criteria>

<output>
Create `.planning/phases/61-docs-contract-boundary-enforcement/61-02-SUMMARY.md` when done
</output>
