---
phase: 102-motion-micro-interaction-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true
wave_0: true

must_haves:
  truths:
    - "A grep gate fails CI if any lib/*.ex file transitions a layout property (height/width/padding/margin/top/left/right/bottom/max-height) or uses a stray ease-in outside the documented --ease-symmetric exception"
    - "A structural test asserts that under prefers-reduced-motion the computed animation/transition duration on a motion-carrying element is effectively zero"
    - "A structural test asserts enter/exit asymmetry is present (a phx-remove exit attribute or a 150ms computed exit duration) on the detail/overlay surface"
    - "All existing conformance gates and structural facts still pass (no regression)"
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "MOTION-GATE banning layout-property transitions and stray ease-in"
      contains: "MOTION-GATE"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "reduced-motion computed-duration assertion + enter/exit asymmetry assertion"
      contains: "reducedMotion"
  key_links:
    - from: "scripts/check-conformance.sh MOTION-GATE"
      to: "lib/**/*.ex transition utilities"
      via: "grep -rEn over $LIB --include=*.ex"
      pattern: "transition-\\(height|width|padding|margin"
    - from: "structural.spec.js reduced-motion test"
      to: ".motion-reveal computed style"
      via: "emulateMedia({reducedMotion:reduce}) + getComputedStyle"
      pattern: "animationDuration"
---

<objective>
Land the motion conformance and structural gates BEFORE any CSS/HEEx motion change, mirroring the Phase 94 tighten-then-change pattern, so the motion uplift in Plans 02–03 cannot regress silently.

Purpose: MOTION-02 requires the motion conformance gate to stay green and a structural reduced-motion assertion to pass. Today neither the grep gate nor the structural spec actually proves these (RESEARCH § Conformance Gate: FACT 4 asserts visibility only, no MOTION grep gate exists). This plan creates the proofs first; later plans implement against them.

Output: A new MOTION-GATE block in `check-conformance.sh`; an extended FACT 4 reduced-motion computed-duration assertion and a new enter/exit asymmetry assertion in `structural.spec.js`. No CSS or HEEx behavior changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md
@.planning/phases/102-motion-micro-interaction-pass/102-VALIDATION.md
@.planning/research/v1.11/MOTION.md

<interfaces>
<!-- The conformance gate is a fail-on-match grep loop. Each gate increments `errors`,
     prints FAIL to stderr, and a final `if [[ $errors -gt 0 ]]` exits 1.
     LIB is anchored to BASH_SOURCE: $LIB = mailglass_admin/lib. All gates scope to
     --include="*.ex" (HEEx lives in LiveView modules; no .heex partials). -->

Existing gate shape (check-conformance.sh):
  if grep -rEn '<pattern>' "$LIB" --include="*.ex" 2>/dev/null; then
    echo "FAIL: <NAME>-GATE — <message>" >&2
    errors=$((errors + 1))
  fi

Existing structural computed-style pattern (structural.spec.js FACT 5):
  const outlineWidth = await el.evaluate(e => getComputedStyle(e).outlineWidth);
  expect(parseFloat(outlineWidth)).toBeGreaterThan(0);

Existing reduced-motion test order (FACT 4): emulateMedia({reducedMotion:"reduce"}) MUST precede page navigation.
Helpers available in spec: openOperator(page), openInbound(page), openPreviewEmpty(page).
app.css reduced-motion block uses `0.01ms !important` (NOT 0ms) — assert ≤ ~0.05, never === 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MOTION-GATE to check-conformance.sh</name>
  <read_first>
    - mailglass_admin/scripts/check-conformance.sh (the file being modified — copy the existing gate idiom exactly: grep -rEn ... "$LIB" --include="*.ex", errors increment, FAIL message to stderr)
    - .planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md (§ Conformance Gate — the exact ban list and the --ease-symmetric carve-out)
    - .planning/research/v1.11/MOTION.md (MOTION-LD-01 ease-out-only, MOTION-LD-10 prohibited properties, MOTION-LD-05 --ease-symmetric exception)
    - mailglass_admin/assets/css/app.css (source-of-truth for token names; NOT scanned by this gate, which scopes to lib/*.ex)
    - mailglass_admin/docs/design-system.md (motion rules block — source-of-truth)
  </read_first>
  <action>
    Add a sixth gate named MOTION-GATE to check-conformance.sh, placed after HEX-GATE and before the final `if [[ $errors -gt 0 ]]` summary. It MUST follow the existing gate idiom exactly (grep -rEn over "$LIB" --include="*.ex", increment `errors`, FAIL message to stderr). The gate bans, per MOTION-LD-10, any Tailwind transition utility that animates a layout property: ban literal `transition-(height|width|padding|margin|top|left|right|bottom|max-height)` utilities AND arbitrary `transition-\[` JIT utilities naming those same layout properties. Add a SECOND grep within MOTION-GATE that bans a stray `ease-in\b` token in lib/*.ex (MOTION-LD-01 ease-out-only) while allowing the documented `--ease-symmetric` exception — match `ease-in` as a whole token but exclude `ease-in-out` and the var form `(--ease-symmetric)` so the one permitted symmetric crossfade does not trip it. Use a FAIL message that names the rule, e.g. "MOTION-GATE — layout-property transition found (animate transform/opacity only, MOTION-LD-10)" and "MOTION-GATE — stray ease-in found (ease-out only except --ease-symmetric, MOTION-LD-01)". Apply grep-gate hygiene: anchor tokens with word boundaries so `transition-colors`, `transition-all`, `transition-transform`, `transition-opacity`, and `ease-out`/`ease-in-out` are NOT matched. Validate by RUNNING the script against the current clean lib/ (project memory: validate-credo-by-running-it) — it MUST still exit 0 on today's code.
  </action>
  <verify>
    <automated>cd mailglass_admin && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh && grep -q 'MOTION-GATE' scripts/check-conformance.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && scripts/check-conformance.sh` exits 0 on the current (unchanged) lib/ tree
    - `grep -c 'MOTION-GATE' mailglass_admin/scripts/check-conformance.sh` returns ≥ 1
    - Negative proof (run once, do not commit the probe): temporarily adding a line containing `transition-height` to any lib/*.ex makes the script exit 1 with a MOTION-GATE FAIL message; revert the probe
    - Negative proof (run once, do not commit): a line containing `ease-in"` (not `ease-in-out`, not `--ease-symmetric`) makes the script exit 1; revert
    - `scripts/check-conformance-advisory.sh` still exits 0
  </acceptance_criteria>
  <done>MOTION-GATE present, exits 0 on clean tree, exits 1 on a layout-property-transition probe and on a stray ease-in probe; advisory gate unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Extend structural.spec.js with reduced-motion duration + enter/exit asymmetry assertions</name>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (the file being modified — FACT 4 at lines ~744-772 for the emulateMedia order, FACT 5 at ~779-824 for the getComputedStyle idiom; reuse helpers openOperator/openInbound/openPreviewEmpty)
    - .planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md (§ Conformance Gate — the exact computed-duration assertion shape and the 0.01ms-not-0 caveat)
    - .planning/research/v1.11/MOTION.md (MOTION-LD-09 reduced-motion snaps all, MOTION-LD-13 exit = 150ms)
    - mailglass_admin/assets/css/app.css (source-of-truth — reduced-motion block uses 0.01ms !important; .motion-reveal/.motion-timeline classes)
    - mailglass_admin/docs/design-system.md (motion vocabulary — source-of-truth)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the detail pane carrying motion-reveal at line ~466 — the asymmetry assertion targets this surface after Plan 03 adds phx-remove)
  </read_first>
  <action>
    Extend the FACT 4 "reduced-motion suppresses animation" describe block: add a test that, after `page.emulateMedia({ reducedMotion: "reduce" })` (BEFORE navigation) and `openOperator(page)`, locates the first `.motion-reveal` element (or `.motion-timeline > *` if no reveal is present in the operator overview), reads `getComputedStyle(el).animationDuration` and `transitionDuration` via `el.evaluate`, and asserts each `parseFloat(...) <= 0.05` (app.css uses `0.01ms !important`, so assert ≤ ~0.05s, NEVER === 0). This is the MOTION-02 structural proof. Add a SECOND new test (a separate describe block "enter/exit asymmetry") that opens the operator surface, selects a delivery so the detail pane renders, and asserts enter/exit asymmetry on the `#delivery-detail-*` element: assert it carries a `phx-remove` attribute (presence proof) — OR, if the implementation drives the exit via a computed transition, assert the computed exit `transitionDuration` ≈ 0.15s. Use a tolerant presence check (`getAttribute("phx-remove")` is non-null) so the assertion is satisfied once Plan 03 adds the exit. Because Plan 03 has not run yet, mark this asymmetry test with `test.fixme()` (or `test.skip` with a TODO citing 102-03) so Wave 1 stays green; Plan 03's closing task un-skips it. The reduced-motion duration test is NOT skipped — it must pass immediately against current CSS. Do not weaken FACT 4's existing visibility tests; add alongside them.
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "reduced-motion"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/structural.spec.js -g "reduced-motion"` passes, including the new computed-duration assertion (`animationDuration` and `transitionDuration` parseFloat ≤ 0.05 under reduced-motion)
    - `grep -c 'animationDuration' mailglass_admin/e2e/structural.spec.js` returns ≥ 1
    - `grep -c 'enter/exit asymmetry' mailglass_admin/e2e/structural.spec.js` returns ≥ 1 (the new describe block exists)
    - The asymmetry test is marked `fixme`/`skip` with an inline comment referencing 102-03 (so the full structural run stays green in Wave 1)
    - `npx playwright test e2e/structural.spec.js` reports no NEW failures versus the pre-change baseline (existing 6 facts still pass)
  </acceptance_criteria>
  <done>Reduced-motion computed-duration assertion passes now; enter/exit asymmetry test exists, scoped skipped pending 102-03; no existing structural fact regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a CI grep script and a Playwright spec. No runtime trust boundary is crossed; no data flows through the changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | check-conformance.sh gate logic | accept | Gate is presentational-conformance only; a weakened gate cannot exfiltrate data or alter runtime. Validated by running it (positive + negative probes) per acceptance criteria. |
| T-102-NB | — | motion/CSS/test surface | accept | Per 102-RESEARCH § Security Domain: no new trust boundary; motion carries no data; PII-minimization (`mask_recipient/1`) and multi-tenant scoping are untouched (no lib runtime code changes in this plan). |
</threat_model>

<verification>
- `cd mailglass_admin && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh` — exits 0 (MOTION-GATE present, clean tree)
- `cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "reduced-motion"` — passes
- `cd mailglass_admin && npx playwright test e2e/structural.spec.js` — no new failures vs baseline
</verification>

<success_criteria>
- MOTION-GATE bans layout-property transitions + stray ease-in; exits 0 on clean lib/, exits 1 on probes
- Structural reduced-motion computed-duration assertion passes against current CSS
- Enter/exit asymmetry test scaffolded (skipped pending 102-03)
- No regression in existing conformance gates or structural facts
</success_criteria>

<output>
Create `.planning/phases/102-motion-micro-interaction-pass/102-01-SUMMARY.md` when done
</output>
