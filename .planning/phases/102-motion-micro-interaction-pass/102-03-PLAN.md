---
phase: 102-motion-micro-interaction-pass
plan: 03
type: execute
wave: 3
depends_on: ["102-01", "102-02"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/preview_live.ex
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The operator and inbound detail panes fire a real exit transition (150ms ease-out, opacity+transform only) before LiveView removes them — enter/exit asymmetry per MOTION-LD-13"
    - "The replay modal overlay fires a 150ms scale-down + fade exit before removal (MOTION-LD-04)"
    - "Focus rings carry a token-named transition resolving through --duration-instant on the surfaces standardized by MOTION-LD-06"
    - "The preview empty-state CTA carries the .motion-reveal entrance (MOTION-LD-12) and remains an unconditionally-focusable element (GAP-02, already fixed Phase 100 — verify-only)"
    - "First-mount stagger (cap 8) and reduced-motion collapse remain intact (verify-only)"
    - "The enter/exit asymmetry structural assertion is un-skipped and passes; the bundle is rebuilt bit-clean and mix verify.preview is green"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "phx-remove exit on #delivery-detail-* pane"
      contains: "phx-remove"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "phx-remove exit on #inbound-detail-* pane"
      contains: "phx-remove"
    - path: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      provides: "motion-reveal on empty-state CTA"
      contains: "motion-reveal"
  key_links:
    - from: "operator_live.ex #delivery-detail pane"
      to: "JS.hide(transition: ...) exit"
      via: "phx-remove attribute, 150ms, opacity+transform only"
      pattern: "phx-remove=\\{JS.hide"
    - from: "structural.spec.js enter/exit asymmetry test"
      to: "#delivery-detail-* phx-remove attribute"
      via: "getAttribute presence / computed 150ms exit"
      pattern: "phx-remove"
---

<objective>
Add the HEEx enter/exit asymmetry and focus-transition uplift that consumes the tokens landed in Plan 02: real `phx-remove` exit transitions on the detail panes and modal overlays (MOTION-LD-04/13, 220ms in / 150ms out), the token-named focus transition (MOTION-LD-06), and the `.motion-reveal` entrance on the preview empty-state CTA (MOTION-LD-12). Un-skip the enter/exit asymmetry structural assertion, verify the already-fixed GAP-02/GAP-03 and the intact stagger/reduced-motion behavior, rebuild the bundle, and close the phase with `mix verify.preview`.

Purpose: This delivers the remaining MOTION-01 mechanics that require HEEx changes (exit asymmetry, focus transitions, MOTION-LD-12 reveal) and proves the MOTION-02 reduced-motion + gate posture end-to-end. It is the closing gate plan.

Output: Updated LiveView/component `.ex` files, the un-skipped structural assertion, and a rebuilt `priv/static/app.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md
@.planning/phases/102-motion-micro-interaction-pass/102-VALIDATION.md
@.planning/research/v1.11/MOTION.md

<interfaces>
<!-- Motion-carrying surfaces (from RESEARCH § Component Responsibilities), all use
     Phoenix.LiveView.JS (already imported in these modules) — NO client JS hook. -->

operator_live.ex:466  <div id={"delivery-detail-#{@selected_delivery.id}"} class="motion-reveal space-y-4">  (id-keyed entrance; needs phx-remove exit)
operator_live.ex:494-498  existing phx-mounted={JS.focus_first(...)} / phx-remove={JS.focus(...)} — sanctioned LiveView.JS usage pattern to mirror
inbound_live.ex:389   <div id={"inbound-detail-#{@detail.record.id}"} class="motion-reveal space-y-4">  (id-keyed entrance; needs phx-remove exit)
operator/replay_modal.ex:18  .motion-tab-swap backdrop ; :26  .motion-overlay panel  (entrance only; "Exit stays instant" — MOTION-LD-04 upgrade adds exit)
inbound/replay_modal.ex:22,27  same backdrop/overlay pattern
preview_live.ex:403-409  .link "Preview the first Mailable" CTA, already focus-visible:ring-2 focus-visible:ring-primary (GAP-02 fixed); needs .motion-reveal entrance (MOTION-LD-12)

LiveView.JS exit primitive (1.1.28): phx-remove={JS.hide(time: 150, transition: {"<transition-classes>", "<from>", "<to>"})}
  - time: literal integer ms MUST equal the CSS transition duration (Pitfall 3 desync); use 150 (MOTION-LD-13 exit)
  - transition utilities must be opacity/transform only (MOTION-LD-10): e.g. opacity-100→opacity-0, translate-y-0→translate-y-1, scale-100→scale-[0.98]
  - transition utility classes must be Tailwind literals the scanner sees, OR standard utilities Tailwind always emits (Pitfall 2)
Stagger (app.css:281-288) and reduced-motion block (app.css:292-300) are already conformant — verify only, do not rebuild.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phx-remove exit transitions to detail panes + modal overlays; standardize focus transition</name>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the file being modified — #delivery-detail pane ~466; mirror the existing JS.focus_first/JS.focus phx-mounted/phx-remove idiom at ~494-498)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (the file being modified — #inbound-detail pane ~389)
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (the file being modified — .motion-tab-swap backdrop ~18, .motion-overlay panel ~26)
    - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (the file being modified — same backdrop/overlay)
    - .planning/research/v1.11/MOTION.md (MOTION-LD-04 overlay exit 150ms scale-down+fade; MOTION-LD-13 exit=entrance×0.67=150ms; MOTION-LD-06 focus ≤100ms; MOTION-LD-10 opacity/transform only; MOTION-LD-11 mount-trigger)
    - .planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md (Pattern 1 exit mechanics; Pitfall 2 tree-shaking; Pitfall 3 time↔duration sync; Pitfall 5 keyframe-vs-transition — exits use JS.hide transition tuple, NOT reversed keyframe)
    - mailglass_admin/assets/css/app.css (source-of-truth — duration/easing tokens, .motion-* classes)
    - mailglass_admin/docs/design-system.md (motion vocabulary — source-of-truth)
  </read_first>
  <action>
    Add a `phx-remove={JS.hide(time: 150, transition: {...})}` exit to the operator `#delivery-detail-*` pane (operator_live.ex ~466) and the inbound `#inbound-detail-*` pane (inbound_live.ex ~389): the transition tuple must use ease-out + a 150ms duration utility and opacity/transform-only classes (from `opacity-100 translate-y-0` to `opacity-0 translate-y-1`) per MOTION-LD-13 and MOTION-LD-10. Keep `time: 150` numerically equal to the CSS transition duration (Pitfall 3). Do NOT reverse the keyframe entrance (Pitfall 5 — entrances stay as `.motion-reveal` keyframes; exits are JS-driven transitions). Add a `phx-remove` exit to the replay modal `.motion-overlay` panel in BOTH operator/replay_modal.ex (~26) and inbound/replay_modal.ex (~27): MOTION-LD-04 — `scale(1→0.98)` + `opacity(1→0)` at 150ms ease-out (`scale-100→scale-[0.98]`, `opacity-100→opacity-0`); the backdrop (.motion-tab-swap) may fade out at 150ms as well. Ensure all transition-tuple utility classes are Tailwind literals (Pitfall 2): prefer standard always-emitted utilities (`opacity-0`, `translate-y-1`, `scale-[0.98]`, `duration-150`, `ease-out`) or the named `.motion-*` classes. Standardize the focus-ring transition on the surfaces RESEARCH § Focus Transitions names (gallery_live.ex bare `transition-colors`, and any focus utility lacking a duration): apply `transition-colors ease-out duration-(--duration-instant)` so focus motion resolves ≤100ms (MOTION-LD-06); leave row-background hover on `duration-(--duration-fast)`. Do not introduce any layout-property transition (MOTION-GATE enforces). NOTE: this task changes literal class strings in HEEx, so the bundle must be rebuilt in Task 2.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q 'phx-remove={JS.hide' lib/mailglass_admin/operator_live.ex && grep -q 'phx-remove={JS.hide' lib/mailglass_admin/inbound_live.ex && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh && mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'phx-remove={JS.hide' mailglass_admin/lib/mailglass_admin/operator_live.ex` ≥ 1 and same for inbound_live.ex
    - The exit tuples animate opacity/transform only (grep the JS.hide transition strings; no height/width/max-height/padding/margin/top/left present)
    - `grep -c 'phx-remove' mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` ≥ 1 and same for inbound/replay_modal.ex
    - Focus transition standardized: `grep -c 'duration-(--duration-instant)' mailglass_admin/lib/mailglass_admin/gallery_live.ex` ≥ 1 (or the focus-carrying file named in RESEARCH § Focus Transitions)
    - `cd mailglass_admin && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh` exit 0 (MOTION-GATE clean — no layout-property transition, no stray ease-in)
    - `cd mailglass_admin && mix compile --no-optional-deps --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Detail panes + modal overlays carry MOTION-LD-04/13 exit transitions (opacity/transform only, 150ms, time-synced); focus transition standardized on --duration-instant; conformance clean; compiles warnings-as-errors.</done>
</task>

<task type="auto">
  <name>Task 2: MOTION-LD-12 reveal on preview CTA; verify GAP-02/03, stagger, reduced-motion; un-skip asymmetry assertion; rebuild bundle + verify.preview</name>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/preview_live.ex (the file being modified — empty-state CTA ~403-409, already focus-visible:ring-2; GAP-02 fixed Phase 100)
    - mailglass_admin/e2e/structural.spec.js (the file being modified — the enter/exit asymmetry describe block scaffolded skipped in 102-01; FACT 4 reduced-motion; FACT 5 focus)
    - .planning/research/v1.11/MOTION.md (MOTION-LD-12 preview CTA reveal + unconditional focusable; MOTION-LD-03 stagger cap 8; MOTION-LD-09 reduced-motion)
    - .planning/phases/102-motion-micro-interaction-pass/102-RESEARCH.md (§ Component Responsibilities — preview CTA needs .motion-reveal; Pattern 2 stagger verify-only; § Conformance Gate; Pitfall 1 bundle rebuild)
    - mailglass_admin/assets/css/app.css (source-of-truth — .motion-timeline stagger ~281-288, reduced-motion ~292-300; verify intact)
    - mailglass_admin/mix.exs (verify.preview alias ~183-188)
  </read_first>
  <action>
    Add the `.motion-reveal` class to the preview empty-state CTA in preview_live.ex (~403-409) per MOTION-LD-12 (entrance via the existing id-keyed/mount path; the `.link` already carries `focus-visible:ring-2 focus-visible:ring-primary` and is rendered for `first_previewable(@mailables)` — confirm GAP-02 stays satisfied: an unconditionally-focusable element exists in the empty-state). Do NOT reopen GAP-02 or GAP-03 and do NOT modify RATCHET-GAP-REGISTER.md — both are already fixed (Phase 100); this is verify-only beyond adding the reveal class. Verify (do not rebuild) that the first-mount stagger cap-at-8 (app.css:281-288) and the reduced-motion `@media` block (app.css:292-300) remain intact via grep. Un-skip the enter/exit asymmetry structural test scaffolded in 102-01 (remove the `fixme`/`skip`) now that Task 1 added the `phx-remove` exit, so it asserts the `#delivery-detail-*` pane carries `phx-remove` (or a computed 150ms exit). Then rebuild the bundle (`mix mailglass_admin.assets.build`) because Task 1 + this task added literal motion class strings, commit `priv/static/app.css` bit-clean, and run the closing phase gate `mix verify.preview` (compile + scoped test + assets.build + `git diff --exit-code priv/static/`). Use scoped commands, never bare `mix test` (project memory: ~57 Oban failures + voice_test "Oops" dep-JS false positive).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -q 'motion-reveal' lib/mailglass_admin/preview_live.ex && grep -q 'nth-child(8)' assets/css/app.css && grep -q 'prefers-reduced-motion' assets/css/app.css && npx playwright test e2e/structural.spec.js && mix verify.preview</automated>
  </verify>
  <acceptance_criteria>
    - The preview empty-state CTA carries `motion-reveal`: `grep -A6 'first_previewable' mailglass_admin/lib/mailglass_admin/preview_live.ex | grep -q 'motion-reveal'`
    - The CTA remains an unconditionally-rendered focusable `.link`/button with `focus-visible:ring-2` (GAP-02 intact); `RATCHET-GAP-REGISTER.md` is NOT modified (`git status` shows no change to it)
    - Stagger + reduced-motion intact: `grep -c 'nth-child(8)' mailglass_admin/assets/css/app.css` ≥ 1 and `grep -c 'prefers-reduced-motion: reduce' mailglass_admin/assets/css/app.css` ≥ 1
    - The enter/exit asymmetry structural test is un-skipped and passes: `npx playwright test e2e/structural.spec.js -g "asymmetry"` passes (no `fixme`/`skip` remaining on it)
    - `npx playwright test e2e/structural.spec.js` — all facts (incl. reduced-motion duration + asymmetry) pass
    - `cd mailglass_admin && mix verify.preview` exits 0 (compile + scoped test + assets.build + `git diff --exit-code priv/static/` clean)
  </acceptance_criteria>
  <done>Preview CTA carries MOTION-LD-12 reveal (GAP-02/03 untouched); stagger + reduced-motion verified intact; asymmetry assertion un-skipped and green; bundle rebuilt bit-clean; mix verify.preview green — phase closing gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | HEEx motion attributes (`phx-remove`, `.motion-reveal`, focus-transition classes) + a Playwright assertion + a bundle rebuild. `phx-remove`/`JS.hide` are presentational LiveView.JS commands; no new event handler, route, input, or data flow is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Tampering | phx-remove / JS.hide on detail pane + modal | accept | These are client-side presentational transitions issued by the server; they do not alter the LiveView event contract or any tenant-scoped query. Exit motion runs before LiveView removes a node it already decided to remove. |
| T-102-NB | — | motion/HEEx surface | accept | Per 102-RESEARCH § Security Domain: no new trust boundary; motion never carries data. PII-minimization (`mask_recipient/1`) on the detail panes and multi-tenant scoping are untouched — only presentational attributes/classes are added; verify.preview's scoped test suite (incl. operator_live_test, router_test, auth_test) gates regressions. |
</threat_model>

<verification>
- `cd mailglass_admin && scripts/check-conformance.sh && scripts/check-conformance-advisory.sh` — exit 0
- `cd mailglass_admin && npx playwright test e2e/structural.spec.js` — all facts pass (incl. reduced-motion duration + un-skipped asymmetry)
- `cd mailglass_admin && mix verify.preview` — exits 0 (compile + scoped test + assets.build + bundle-clean)
- `git status` — `RATCHET-GAP-REGISTER.md` unchanged
</verification>

<success_criteria>
- Detail panes + modal overlays fire MOTION-LD-04/13 exit transitions (opacity/transform, 150ms, time-synced)
- Focus transition standardized on --duration-instant (MOTION-LD-06)
- Preview empty-state CTA carries MOTION-LD-12 reveal; GAP-02/03 verified, RATCHET-GAP-REGISTER.md untouched
- Stagger (cap 8) + reduced-motion collapse verified intact
- Enter/exit asymmetry structural assertion un-skipped and green
- Bundle rebuilt bit-clean; mix verify.preview green (phase closing gate)
</success_criteria>

<output>
Create `.planning/phases/102-motion-micro-interaction-pass/102-03-SUMMARY.md` when done
</output>
