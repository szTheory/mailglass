# Phase 50.5: v1.2 Release Ceremony — Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-05-25
**Phase:** 50.5-v1-2-release-ceremony
**Mode:** assumptions
**Areas analyzed:** Version Computation Strategy, Pre-Ceremony Commit Sequence, CHANGELOG Curation Scope, Publish Pipeline

## Assumptions Presented

### Version Computation Strategy
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Replace `"release-as": "1.0.0"` → `"1.2.0"` in release-please-config.json; inbound computes 0.2.0 naturally | Confident | `release-please-config.json` lines 9+17 (sticky override confirmed); deferred-items.md item #6; 32 feat: commits touch `mailglass_inbound/` since 0.1.0 (git log verified) |

### Pre-Ceremony Commit Sequence
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Commit A (config swap + editorial comment removal) → Commit B (CHANGELOG curation) before release-please is triggered | Confident | D-44.5 locked sequence (044.5-CONTEXT.md); `mix.exs:5` and `mailglass_inbound/mix.exs:5` editorial comments confirmed; deferred-items.md item #7 |

### CHANGELOG Curation Scope
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Core 1.2.0 = brief single-section milestone summary; Admin 1.2.0 = short coordinated sibling note; Inbound 0.2.0 = full v1.2 narrative with all 9 REQ categories (current Unreleased covers partial Phase 46 only) | Confident | `CHANGELOG.md` Unreleased (1 entry); `mailglass_admin/CHANGELOG.md` (no Unreleased section); `mailglass_inbound/CHANGELOG.md` lines 8–82 (partial coverage); confirmed presence of MailboxCase, TestAssertions, Test.Ingress, Fixtures, RateLimiter, doctor/replay/prune tasks, InboundLive in `mailglass_inbound/lib/` and `mailglass_admin/lib/` |

### Publish Pipeline
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| No structural workflow changes needed; all Phase 44.5 infrastructure reused unchanged | Likely | `publish-hex.yml` line 31 (package=all default); `release-please.yml` lines 146-147 (PINS array); Phase 44.5 publish succeeded via this infrastructure |

## Corrections Made

No corrections — all assumptions confirmed by user.

## External Research Resolved

- **Topic:** Whether release-please computes 0.2.0 naturally for unlinked `mailglass_inbound`
- **Finding:** Yes — 32 `feat:` commits touch `mailglass_inbound/` since `mailglass_inbound-v0.1.0`; release-please's path-based commit filter picks these up and computes a minor bump (0.1.0 → 0.2.0). No `"release-as"` override needed for inbound.
- **Method:** `git log mailglass-v1.0.0..HEAD --oneline -- mailglass_inbound/ | grep feat` (verified directly from repo)
- **Confidence impact:** Publish Pipeline assumption elevated from Unclear to Likely (pipeline confirmed correct for inbound version computation)
