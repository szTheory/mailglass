---
phase: 50.5-v1-2-release-ceremony
plan: 03
type: execute
wave: 3
depends_on: ["50.5-01", "50.5-02"]
files_modified:
  - .planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
autonomous: false
requirements: []
user_setup:
  - service: github
    why: "Live publish ceremony requires GitHub Actions workflow_dispatch, Release PR merge, and hex-publish Environment approval"
    dashboard_config:
      - task: "Approve hex-publish GitHub Environment when the live publish run pauses for review"
        location: "GitHub Actions → publish-hex.yml run → Review deployments → hex-publish"
  - service: hex
    why: "Verify all three packages are live on Hex.pm and HexDocs after publish"
    dashboard_config:
      - task: "Confirm mailglass 1.2.0 / mailglass_admin 1.2.0 / mailglass_inbound 0.2.0 are indexed"
        location: "hex.pm/packages/<pkg> and hexdocs.pm/<pkg>"
must_haves:
  truths:
    - "release-please opens a 1.2.0 PR with manifest bumps (.→1.2.0, admin→1.2.0, inbound→0.2.0), both sibling dep pins synced to == 1.2.0, and curated CHANGELOG headers preserved"
    - "Dry-run dispatch (dry_run=true, package=all) on the PR head ref passes prepublish-summary for all three packages"
    - "All three packages publish live: mailglass 1.2.0, mailglass_admin 1.2.0, mailglass_inbound 0.2.0, confirmed by mix hex.info"
    - "Sandbox consumer install of ~> 1.2 / ~> 0.2 resolves and compiles clean within the 60-minute window"
    - "50.5-RELEASE-RECORD.md proof artifact exists; PROJECT.md Current State and MILESTONES.md v1.2 entry record the live-publish date"
  artifacts:
    - path: ".planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md"
      provides: "Live publish proof record (mirrors 044.5-RELEASE-RECORD.md shape)"
      contains: "mailglass-v1.2.0"
    - path: ".planning/MILESTONES.md"
      provides: "v1.2 milestone entry with live-publish date"
      contains: "v1.2"
  key_links:
    - from: "Release PR head ref"
      to: "publish-hex.yml dry-run dispatch"
      via: "workflow_dispatch dry_run=true package=all"
      pattern: "publish-hex"
    - from: "merged Release PR tag"
      to: "Hex.pm"
      via: "publish-hex.yml on release: published"
      pattern: "hex.publish"
---

<objective>
Run the live v1.2 release ceremony, reusing the Phase 44.5 publish infrastructure unchanged (D-50.5-04). After Commits A+B land on `main`, wait for release-please to open the 1.2.0 PR, verify the diff, dispatch the dry-run, merge, approve the live publish, validate adopter installability in the 60-minute window, then write the proof record and update close-out docs.

Purpose: Ship all v1.2 inbound work to Hex.pm so adopters can `{:mailglass, "~> 1.2"}` / `{:mailglass_inbound, "~> 0.2"}` and pick up Mailgun + SES ingress, admin LiveView, test helpers, and operator tooling — closing the v1.2 milestone with a published release. This is the institutionalization of the release-cadence rule.
Output: Three live Hex packages, a `v1.2.0` git tag, `50.5-RELEASE-RECORD.md`, and updated PROJECT.md + MILESTONES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-RESEARCH.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md
@.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md

<interfaces>
<!-- Ceremony posture is a repeat of Phase 44.5. All four 44.5 pitfalls have known in-repo solutions. -->

publish-hex.yml: workflow_dispatch inputs `dry_run` (true/false) and `package` (mailglass | mailglass_admin | mailglass_inbound | all). Default package=all.
  Jobs: prepublish-summary → gate-ci-green → publish-core → publish-admin + publish-inbound (parallel after core).
  Sibling MIX_PUBLISH dep-resolution steps are guarded with `if: dry_run != 'true'` (Pitfall 3 already fixed in 44.5 commit a0ba244) — they show "skipped" not "failed" in dry-run.
  gate-ci-green carries an ADVISORY_LANES allowlist including "Operator Browser Gate" (advisory since 44.5 commit 5734c1b; re-strict is Phase 51 scope, not this plan).

release-please.yml sync step (lines 133-178): PINS array reads CORE_VERSION from manifest .["."] and writes {:mailglass, "== <CORE_VERSION>"} to both mailglass_admin/mix.exs and mailglass_inbound/mix.exs automatically. No manual pin edits.

post-publish-smoke.yml: has a KNOWN consumer-install bug for mailglass_inbound (deferred-items.md item #8 — writes {:mailglass, "~> $VERSION"} using inbound's 0.2.0 instead of resolving core transitively). This is expected to fail; manual sandbox smoke substitutes as proof. Fix is Phase 51 scope.

MAINTAINING.md lines 108-189: the release runbook (60-minute window, retract decision tree, required check buckets). Sandbox recipe updated to ~> 1.2 / ~> 0.2 in Plan 01.

Target versions: mailglass 1.2.0, mailglass_admin 1.2.0, mailglass_inbound 0.2.0.
Tags release-please will create: mailglass-v1.2.0, mailglass_admin-v1.2.0, mailglass_inbound-v0.2.0.

50.5-RELEASE-RECORD.md structure (mirror 044.5): header block (release type, tags, squash-merge SHA, workflow run URLs, GitHub Release URLs, Hex URLs, HexDocs URLs, 60-min outcome) → ## Manual approvals and external checks → ## Sandbox install proof (60-minute window) → ## Dispatch attempt chronology (table) → ## Recovery commits applied during ceremony retries → ## Proof links → ## Notes.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Trigger release-please and verify the 1.2.0 Release PR diff</name>
  <files>(no file edits — gh CLI inspection of the release-please PR)</files>
  <what-built>
    Commits A (version force + allowlist refresh) and B (curated CHANGELOG headers) are on `main`. The orchestrator pushes them, which triggers the release-please GitHub App to compute the linked-group 1.2.0 cut (forced via release-as) and the natural mailglass_inbound 0.2.0 cut, and open a Release PR.
    Before this checkpoint, Claude must:
    - Confirm Commits A+B are pushed to main (`git log origin/main -3 --oneline` shows both ceremony commits).
    - Poll for the release-please PR: `gh pr list --label "autorelease: pending" --json number,headRefName,url` (or `gh pr list --search "release-please" --json number,url`). Capture the PR number and head ref.
  </what-built>
  <how-to-verify>
    Inspect the open release-please PR diff (`gh pr diff <PR#>`) and confirm ALL SIX of the following are present, exactly:
    1. `.release-please-manifest.json` bumped to `{".": "1.2.0", "mailglass_admin": "1.2.0", "mailglass_inbound": "0.2.0"}`
    2. `mailglass_admin/mix.exs` dep pin updated by the sync step to `{:mailglass, "== 1.2.0"}`
    3. `mailglass_inbound/mix.exs` dep pin updated by the sync step to `{:mailglass, "== 1.2.0"}`
    4. The curated CHANGELOG headers from Commit B are PRESERVED (the `## What's new in 1.2.0` core narrative, the inbound per-phase subsections, the admin coordinated-sibling note) — not clobbered by raw ledger
    5. `mix.exs` `@version` bumped to `"1.2.0"`
    6. `mailglass_inbound/mix.exs` `@version` bumped to `"0.2.0"`
    If any of the six is missing (especially #1 wrong versions, or #4 clobbered headers), STOP — do not proceed to dry-run. A clobbered header (#4) means Commit B did not land before the PR opened; a wrong version (#1) means the release-as force or inbound computation misfired.
  </how-to-verify>
  <read_first>
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md ("Release PR Verification Checklist")
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md (the dispatch chronology shows what good/bad PR states looked like in 44.5)
  </read_first>
  <action>Push Commits A+B to main (orchestrator-coordinated), poll for the release-please PR with `gh pr list`, capture its number + head ref, and run `gh pr diff <PR#>` to verify all six PR-diff checks listed in how-to-verify before approving. This is a blocking checkpoint — do not proceed to the dry-run until the diff is confirmed correct. Do not edit any files in this task.</action>
  <verify>
    <automated>gh pr diff "${RELEASE_PR:?set RELEASE_PR to the release-please PR number}" | grep -q '"mailglass_inbound": "0.2.0"' && gh pr diff "${RELEASE_PR}" | grep -q "What's new in 1.2.0"</automated>
  </verify>
  <acceptance_criteria>
    - The release-please PR is open and its number + head ref are captured for the dry-run dispatch
    - `gh pr diff <PR#>` shows manifest = `{".": "1.2.0", "mailglass_admin": "1.2.0", "mailglass_inbound": "0.2.0"}`
    - Both sibling mix.exs files show `{:mailglass, "== 1.2.0"}` in the diff
    - The curated `## What's new in 1.2.0` narrative is present in the PR's CHANGELOG.md diff
    - mix.exs @version shows "1.2.0" and mailglass_inbound/mix.exs @version shows "0.2.0" in the diff
  </acceptance_criteria>
  <done>The release-please PR is open with manifest 1.2.0/1.2.0/0.2.0, both sibling pins == 1.2.0, curated CHANGELOG headers preserved, and @version bumps correct; PR number + head ref captured for the dry-run.</done>
  <resume-signal>Type "approved" if all six PR-diff checks pass, or describe which check failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dry-run dispatch on the PR head ref and verify prepublish-summary</name>
  <files>(no file edits — gh CLI workflow dispatch + run inspection)</files>
  <what-built>
    Claude dispatches the publish workflow in dry-run mode against the release-please PR head ref:
    `gh workflow run publish-hex.yml --ref <PR-head-ref> -f dry_run=true -f package=all`
    Then polls the run to completion (`gh run list --workflow=publish-hex.yml --limit 1`, then `gh run watch <run-id>` or poll `gh run view <run-id>`), capturing the run ID and URL.
  </what-built>
  <how-to-verify>
    Read the run's GITHUB_STEP_SUMMARY (`gh run view <run-id> --log` or the run's summary in the Actions UI) and confirm:
    1. `prepublish-summary` is GREEN for all three packages — `mix mailglass.publish.check` passes for mailglass, mailglass_admin, AND mailglass_inbound (file counts, total size, CHANGELOG excerpt for the target version, top files all sane).
    2. `gate-ci-green` is GREEN (Operator Browser Gate may be advisory-accepted per the ADVISORY_LANES allowlist — that is expected and acceptable per 44.5 posture; any OTHER red lane is a stop).
    3. The sibling `publish-admin` and `publish-inbound` MIX_PUBLISH dep-resolution steps show as "skipped" (not "failed") — this confirms the dry-run guard is working (Pitfall 3 pre-resolved).
    If prepublish-summary fails on an allowlist mismatch, the Plan 01 allowlist refresh missed an entry — STOP and reconcile before proceeding. If the CHANGELOG check fails, the curated header for the target version section is missing — STOP.
  </how-to-verify>
  <read_first>
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-RESEARCH.md (Pitfalls 1, 3, 6 — allowlist, dry-run sibling resolution, CHANGELOG version-section check)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md (dispatch attempt chronology — what each failure mode looked like)
  </read_first>
  <action>Dispatch `gh workflow run publish-hex.yml --ref <PR-head-ref> -f dry_run=true -f package=all` against the release-please PR head ref captured in Task 1, poll the run to completion, capture the run ID + URL, and confirm the three how-to-verify checks (prepublish-summary green for all three packages, gate-ci-green green, sibling MIX_PUBLISH steps skipped). This is a blocking checkpoint — do not merge until the dry-run is green. Do not edit any files in this task.</action>
  <verify>
    <automated>gh run view "${DRYRUN_RUN_ID:?set DRYRUN_RUN_ID to the dry-run dispatch run id}" --json conclusion -q '.conclusion' | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run dispatch run ID and URL are captured (for the RELEASE-RECORD)
    - prepublish-summary step succeeds for all three packages (publish.check exit 0 each)
    - gate-ci-green succeeds (Operator Browser Gate advisory-accepted is OK; no other red lane)
    - publish-admin and publish-inbound MIX_PUBLISH steps show "skipped" under dry-run
  </acceptance_criteria>
  <done>The dry-run dispatch on the PR head ref is green for all three packages with sibling MIX_PUBLISH steps skipped; run ID + URL captured for the RELEASE-RECORD.</done>
  <resume-signal>Type "approved" if the dry-run is green for all three packages, or describe the failing step.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Merge the Release PR, approve the live publish, and start the 60-minute window</name>
  <files>(no file edits — irreversible publish via PR merge + GitHub Environment approval)</files>
  <what-built>
    This is the irreversible publish. Claude has confirmed (Tasks 1-2) the PR diff is correct and the dry-run is green for all three packages. Claude prepares the merge command and the approval guidance; the human performs the squash-merge and the hex-publish Environment approval (the Environment approval and the irreversible Hex publish are human-only gates per MAINTAINING.md).
    Claude does (automatable parts):
    - Re-confirm dry-run was green (Task 2 resume-signal).
    - Provide the squash-merge command: `gh pr merge <PR#> --squash` (or instruct the human to squash-merge in the UI).
    - After merge, identify the triggered `publish-hex.yml` run on the `release: published` event (`gh run list --workflow=publish-hex.yml --limit 3`) and capture the live run ID + URL.
    Human does (human-only, no CLI/API substitute):
    - Squash-merge the Release PR (or confirm Claude's `gh pr merge` if delegated).
    - Approve the `hex-publish` GitHub Environment deployment when the live run pauses for review (GitHub Actions → run → Review deployments → hex-publish).
  </what-built>
  <how-to-verify>
    After the live run completes, confirm all three publish jobs are GREEN:
    - publish-core: `mix hex.publish --yes mailglass 1.2.0`
    - publish-admin: `mix hex.publish --yes mailglass_admin 1.2.0`
    - publish-inbound: `mix hex.publish --yes mailglass_inbound 0.2.0`
    Confirm the three git tags exist: `mailglass-v1.2.0`, `mailglass_admin-v1.2.0`, `mailglass_inbound-v0.2.0` (`gh release list` or `git tag --list 'mailglass*v1.2.0' 'mailglass_inbound-v0.2.0'`).
    Start the literal 60-minute revert window timer (per MAINTAINING.md) at the moment publish-core indexes.
  </how-to-verify>
  <read_first>
    - MAINTAINING.md lines 108-189 (release runbook, 60-minute revert window, retract decision tree)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md ("Manual approvals and external checks" — approver + timestamp shape)
  </read_first>
  <action>Squash-merge the release-please PR (`gh pr merge <PR#> --squash`, or the human merges in the UI), then the human approves the hex-publish GitHub Environment deployment when the live publish run pauses for review (Environment approval and the irreversible Hex publish are human-only gates — no CLI substitute). Capture the squash-merge SHA and the live publish run ID + URL. Confirm publish-core/admin/inbound are all green and the three tags exist, then start the 60-minute revert timer at publish-core index. Do not edit any files in this task.</action>
  <verify>
    <automated>git ls-remote --tags origin | grep -q 'mailglass-v1.2.0' && git ls-remote --tags origin | grep -q 'mailglass_inbound-v0.2.0'</automated>
  </verify>
  <acceptance_criteria>
    - Release PR is squash-merged; squash-merge commit SHA captured
    - hex-publish Environment approved; live publish run ID + URL captured
    - publish-core, publish-admin, publish-inbound all green in the live run
    - Tags mailglass-v1.2.0, mailglass_admin-v1.2.0, mailglass_inbound-v0.2.0 exist
    - 60-minute window timer started at publish-core index
  </acceptance_criteria>
  <done>All three packages published live; the three v1.2.0/0.2.0 tags exist; squash-merge SHA and live run URL captured; the 60-minute revert window has started.</done>
  <resume-signal>Type "published" once all three publish jobs are green and the three tags exist, or describe the failure (and whether retract/recovery per MAINTAINING.md is needed).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify live packages and run the 60-minute sandbox install proof</name>
  <files>(no repo file edits — throwaway sandbox project built outside the repo)</files>
  <what-built>
    Within the 60-minute window, Claude validates adopter installability (the manual sandbox smoke substitutes for the known-broken post-publish-smoke inbound consumer-install job, deferred-items.md item #8).
    Claude does:
    - Confirm Hex indexing: `mix hex.info mailglass 1.2.0`, `mix hex.info mailglass_admin 1.2.0`, `mix hex.info mailglass_inbound 0.2.0` — each must report the version as Released.
    - Build a throwaway sandbox OUTSIDE the repo: `mix phx.new sandbox --no-ecto --no-mailer --install` (per the MAINTAINING.md recipe), add `{:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"}` to deps, run `mix deps.get` then `mix compile --warnings-as-errors`.
    - Capture the post-publish-smoke.yml run outcome (it will likely show the inbound consumer-install failure — record it as the known Phase 51 carry-forward, NOT a publish defect).
  </what-built>
  <how-to-verify>
    1. `mix hex.info mailglass 1.2.0`, `mix hex.info mailglass_admin 1.2.0`, `mix hex.info mailglass_inbound 0.2.0` all confirm the versions live (this is SC-4).
    2. The sandbox `mix deps.get` resolves all three packages from the live Hex registry with the locked versions in mix.lock.
    3. The sandbox `mix compile --warnings-as-errors` exits 0.
    4. HexDocs are built: hexdocs.pm/mailglass/1.2.0/, hexdocs.pm/mailglass_admin/1.2.0/, hexdocs.pm/mailglass_inbound/0.2.0/ resolve (may lag a few minutes).
    The decision is pass if the three packages resolve and compile clean. The post-publish-smoke inbound consumer-install failure is the known item #8 bug and does NOT fail this check (production artifacts are valid; the manual sandbox is the authoritative adopter proof).
  </how-to-verify>
  <read_first>
    - MAINTAINING.md lines 150-189 (sandbox recipe + 60-minute decision)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md ("Sandbox install proof (60-minute window)" — the exact decision/rationale/timestamp shape)
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/deferred-items.md (item #8 — the known smoke bug, so the executor records it correctly)
  </read_first>
  <action>Within the 60-minute window: confirm `mix hex.info mailglass 1.2.0` / `mailglass_admin 1.2.0` / `mailglass_inbound 0.2.0` each report Released; build a throwaway sandbox outside the repo with `mix phx.new sandbox --no-ecto --no-mailer --install`, add deps `{:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"}`, run `mix deps.get` then `mix compile --warnings-as-errors`; capture the post-publish-smoke run outcome and record the inbound consumer-install failure as the known item #8 carry-forward (not a publish defect). Record the 60-minute decision (pass, or revert per MAINTAINING.md if it failed). Do not edit any repo files in this task.</action>
  <verify>
    <automated>mix hex.info mailglass 1.2.0 2>&1 | grep -qi released && mix hex.info mailglass_inbound 0.2.0 2>&1 | grep -qi released</automated>
  </verify>
  <acceptance_criteria>
    - `mix hex.info mailglass 1.2.0` / `mailglass_admin 1.2.0` / `mailglass_inbound 0.2.0` each report the version as Released
    - Sandbox `mix deps.get` resolves mailglass 1.2.0 + mailglass_admin 1.2.0 + mailglass_inbound 0.2.0 from live Hex
    - Sandbox `mix compile --warnings-as-errors` exits 0
    - The post-publish-smoke outcome is captured (inbound consumer-install failure noted as known item #8, not a publish defect)
    - 60-minute decision recorded as pass (or revert per MAINTAINING.md if it failed)
  </acceptance_criteria>
  <done>All three packages are confirmed live on Hex and resolve + compile clean in a fresh sandbox within the 60-minute window; the 60-minute decision is recorded.</done>
  <resume-signal>Type "verified" if all three packages resolve + compile clean in the sandbox, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write 50.5-RELEASE-RECORD.md and update PROJECT.md + MILESTONES.md</name>
  <files>.planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md, .planning/PROJECT.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md (exact section structure to mirror — header block, manual approvals, sandbox proof, dispatch chronology table, recovery commits, proof links, notes)
    - .planning/PROJECT.md lines 11-30 (Current State section — confirm the "Current package versions on Hex" line to update)
    - .planning/MILESTONES.md (confirm there is NO v1.2 section yet — the Phase 44.5 entry under v1.0 is the format precedent; the file currently has v1.1 as the latest entry with no v1.2)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md (RELEASE-RECORD section structure + key differences from 044.5)
  </read_first>
  <action>
    Create .planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md mirroring the 044.5-RELEASE-RECORD.md structure with these concrete values: Release type "live (linked-version cut for v1.2 bundle)"; Tag `mailglass-v1.2.0`; Sibling tags `mailglass_admin-v1.2.0` ; `mailglass_inbound-v0.2.0`; the captured squash-merge SHA + URL; the live publish run URL with per-job outcomes (publish-core mailglass 1.2.0, publish-admin mailglass_admin 1.2.0, publish-inbound mailglass_inbound 0.2.0); the post-publish-smoke run URL; the three GitHub Release URLs; the three Hex URLs (hex.pm/packages/mailglass/1.2.0 etc.); the three HexDocs URLs (hexdocs.pm/mailglass/1.2.0/ etc.); 60-minute outcome. Include the `## Sandbox install proof (60-minute window)` section with the `mix phx.new sandbox` command, deps `{:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"}`, deps.get + compile outcomes, decision pass, rationale, timestamp. Include the `## Dispatch attempt chronology` table (the dry-run as attempt #1 and live publish as the final entry; expect fewer retries than 44.5 since all 44.5 pitfalls are pre-resolved). Include `## Recovery commits applied during ceremony retries` (note "None — all Phase 44.5 pitfalls pre-resolved" if the first dry-run succeeded). Include `## Proof links` pointing to 044.5-RELEASE-RECORD.md as predecessor and a carry-over tracker noting deferred-items.md item #6-successor (Phase 51 removes `"release-as": "1.2.0"`), item #8 (smoke bug), items #9-10 (browser gate). Include `## Notes` flagging Operator Browser Gate advisory (pre-existing) and the post-publish-smoke inbound consumer-install known bug.
    Update .planning/PROJECT.md "Current State" (lines 11-30): change the "Current package versions on Hex" line to `mailglass 1.2.0 / mailglass_admin 1.2.0 / mailglass_inbound 0.2.0` (live as of <publish date> via Phase 50.5); add a sentence noting v1.2 Inbound Production Confidence shipped to Hex with a pointer to 50.5-RELEASE-RECORD.md.
    Update .planning/MILESTONES.md: add a new `## v1.2 Inbound Production Confidence (Shipped: <date>)` section following the v1.1 entry's format, with a "Live publish: <date>" line naming the three versions and pointing to 50.5-RELEASE-RECORD.md, and key accomplishments summarizing the 9 REQ categories (TELE/MIME/MGUN/SESI/ITEST/IGEN/IADM/IOPS/IDOC).
    Stage and commit these three files with message `docs(50.5): record live v1.2 publish ceremony`. Do NOT sweep unrelated dirty .planning/ files into the commit — stage only these three paths explicitly.
  </action>
  <verify>
    <automated>test -f .planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md && grep -q 'mailglass-v1.2.0' .planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md && grep -q 'mailglass_inbound-v0.2.0' .planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md && grep -q '1.2.0' .planning/PROJECT.md && grep -q 'v1.2 Inbound Production Confidence' .planning/MILESTONES.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md exists and contains `mailglass-v1.2.0`, `mailglass_inbound-v0.2.0`, the live publish run URL, the three Hex URLs, and the sandbox install proof section
    - The RELEASE-RECORD does NOT overwrite 044.5-RELEASE-RECORD.md (separate file, both exist)
    - .planning/PROJECT.md Current State shows `mailglass 1.2.0 / mailglass_admin 1.2.0 / mailglass_inbound 0.2.0`
    - .planning/MILESTONES.md contains a `## v1.2 Inbound Production Confidence` section with a live-publish date and a pointer to 50.5-RELEASE-RECORD.md
    - A commit `docs(50.5): record live v1.2 publish ceremony` exists with only those three files staged
  </acceptance_criteria>
  <done>The live ceremony is documented with a proof record mirroring the 44.5 shape; PROJECT.md and MILESTONES.md reflect the live publish; the v1.2 milestone is closed with a published release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → Hex.pm | `mix hex.publish` is irreversible after the 60-minute window; publishing a wrong version or a tarball with unintended files is a public, permanent mistake |
| Release PR → main → tags | Merging the wrong PR state cuts wrong tags; the PR-diff verification (Task 1) is the gate |
| GitHub Environment → publish credentials | HEX_API_KEY lives in the hex-publish Environment, gated by human approval — never exposed to PR jobs |
| dry-run dispatch → release verification | The dry-run is the last safe rehearsal before irreversibility; skipping it forfeits the cheapest failure-detection point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.5-03-01 | Tampering | wrong version published to Hex | mitigate | Task 1 asserts manifest = exact target versions and @version bumps in the PR diff; Task 2 dry-run prepublish-summary confirms the CHANGELOG version section + tarball before merge; both are blocking checkpoints |
| T-50.5-03-02 | Information disclosure | tarball ships unintended files | mitigate | Plan 01 allowlists + Task 2 dry-run publish.check assert the tarball matches the allowlist exactly for all three packages before the irreversible publish |
| T-50.5-03-03 | Elevation of privilege | HEX_API_KEY exposure | mitigate | Publish runs only from the protected hex-publish GitHub Environment behind human approval (Task 3); dry-run on PR head never touches the key; posture unchanged from 44.5 (D-50.5-04) |
| T-50.5-03-04 | Spoofing | forged/incorrect release tags | mitigate | release-please creates tags from the merged manifest; Task 3 asserts the three expected tags exist; tag→Hex publish gated by Environment approval |
| T-50.5-03-05 | Repudiation | no audit trail for the irreversible cut | mitigate | 50.5-RELEASE-RECORD.md captures run URLs, SHAs, approver, dispatch chronology, sandbox proof — full paper trail mirroring 44.5 |
| T-50.5-03-06 | Denial of service | broken adopter install ships | accept | post-publish-smoke inbound consumer-install is a KNOWN workflow bug (item #8, Phase 51); manual sandbox install (Task 4) is the authoritative adopter-installability proof — production artifacts are valid |
| T-50.5-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — it merges/publishes already-audited code (RESEARCH.md Package Legitimacy Audit: no new deps). Sandbox `mix deps.get` resolves only the just-published, already-reviewed mailglass packages |
</threat_model>

<verification>
- release-please PR diff verified: manifest 1.2.0/1.2.0/0.2.0, sibling pins == 1.2.0, curated headers preserved, @version bumps correct
- Dry-run dispatch green for all three packages (prepublish-summary + gate-ci-green); sibling MIX_PUBLISH steps skipped
- Live publish: publish-core/admin/inbound all green; three tags created
- `mix hex.info` confirms mailglass 1.2.0 / mailglass_admin 1.2.0 / mailglass_inbound 0.2.0 live
- Sandbox `~> 1.2` / `~> 0.2` install resolves + compiles clean within 60 minutes
- 50.5-RELEASE-RECORD.md exists (does not overwrite 044.5); PROJECT.md + MILESTONES.md updated
</verification>

<success_criteria>
All three packages are live on Hex.pm at mailglass 1.2.0 / mailglass_admin 1.2.0 / mailglass_inbound 0.2.0 via the release-please linked-versions PR; CHANGELOGs document v1.2 by REQ-ID category with the inbound 0.x disclaimer; `mix hex.info` confirms all three; the v1.2.0 tag is pushed; the sandbox install proves adopter installability; and 50.5-RELEASE-RECORD.md + PROJECT.md + MILESTONES.md record the live publish — closing the v1.2 milestone with a published release per the release-cadence rule.
</success_criteria>

<output>
Create `.planning/phases/50.5-v1-2-release-ceremony/50.5-03-SUMMARY.md` when done. Record: the Release PR number + head ref, the dry-run run ID/URL, the live publish run ID/URL + squash-merge SHA, the three tags, the `mix hex.info` confirmations, the 60-minute sandbox decision, and the path to 50.5-RELEASE-RECORD.md.
</output>
