---
phase: 50.5-v1-2-release-ceremony
plan: 02
type: execute
wave: 2
depends_on: ["50.5-01"]
files_modified:
  - mailglass_inbound/CHANGELOG.md
  - CHANGELOG.md
  - mailglass_admin/CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "mailglass_inbound 0.2.0 [Unreleased] documents all of Phases 45-49 by REQ-ID category, including the Mailgun provider that was previously absent"
    - "Core CHANGELOG.md has a curated 'What's new in 1.2.0' narrative citing all 9 REQ-ID categories, with the OptionalDeps.GenSmtp entry preserved"
    - "mailglass_admin/CHANGELOG.md has a pre-authored [Unreleased] coordinated-sibling section (it had none before)"
    - "Inbound 0.2.0 entry contains the verbatim 0.x stability disclaimer callout (success criterion #3)"
  artifacts:
    - path: "mailglass_inbound/CHANGELOG.md"
      provides: "0.2.0 release notes covering Phases 45-49 + 0.x disclaimer"
      contains: "remains on the **0.x version line**"
    - path: "CHANGELOG.md"
      provides: "Curated 1.2.0 narrative header above release-please ledger"
      contains: "What's new in 1.2.0"
    - path: "mailglass_admin/CHANGELOG.md"
      provides: "[Unreleased] coordinated-sibling note shipping InboundLive"
      contains: "## [Unreleased]"
  key_links:
    - from: "mailglass_inbound/CHANGELOG.md"
      to: "guides/compatibility-and-deprecations.md"
      via: "0.x stability disclaimer link"
      pattern: "compatibility-and-deprecations"
    - from: "mailglass_admin/CHANGELOG.md"
      to: "mailglass_inbound 0.2.0"
      via: "requires {:mailglass_inbound, \"~> 0.2\"} note"
      pattern: "~> 0\\.2"
---

<objective>
Land pre-ceremony Commit B: pre-authored curated CHANGELOG headers for all three packages. These must reach `main` BEFORE release-please opens the 1.2.0 PR — release-please force-pushes the CHANGELOG on PR open, and only headers already in the commit history survive into the released artifact (Pitfall 2 from RESEARCH.md). The inbound 0.2.0 entry is the most content-heavy authoring task in this phase: it currently covers partial Phase 46 only and must grow to document all of Phases 45–49.

Purpose: Satisfies success criteria #2 (CHANGELOG entries by REQ-ID category) and #3 (inbound 0.x stability disclaimer) before the irreversible publish. Without curated headers landing here, adopters see only a raw commit ledger on hexdocs.
Output: One commit `docs(changelog): pre-author curated 1.2.0 / 0.2.0 release headers` editing the three CHANGELOG files. Not pushed in this plan — the orchestrator coordinates the A+B push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md

<interfaces>
<!-- Verified CHANGELOG states (disk-confirmed 2026-05-25). -->

CHANGELOG.md (core): Has a `## [Unreleased]` section with exactly ONE entry —
  `Mailglass.OptionalDeps.GenSmtp.decode/2` (the never-raising RFC 5322 MIME parse seam, @since "1.2.0").
  Below it is the existing curated `## [1.0.0]` four-subsection narrative (the structural precedent to mirror, but briefer for one milestone).
  The OptionalDeps.GenSmtp entry MUST be preserved verbatim in the new curated header.

mailglass_admin/CHANGELOG.md: Has NO `[Unreleased]` section — goes from the file header straight to `## [1.0.0]`.
  The `## [1.0.0]` entry's coordinated-sibling note (short, version-paired, points to core) is the shape to mirror.

mailglass_inbound/CHANGELOG.md: `## [Unreleased]` (lines 8-82) currently covers Phase 46 partial only —
  Signals (IOPS-05), MIMEError, SignatureError, S3FetchError, OptionalDeps.ExAwsS3, S3Fetcher Fake/ExAwsS3, SES provider, + a `### Dependencies` block (ex_aws/ex_aws_s3).
  MISSING and must be added: Phase 45 telemetry + MIME, Phase 46 Mailgun provider (absent despite being shipped), Phase 47 test helpers + generators, Phase 48 admin LiveView integration note, Phase 49 operator tooling.
  Note Deviation D-49-21: the suppression flag ships at `.signals.suppression_flagged`, NOT `.metadata.suppression_flagged`.

9 v1.2 REQ-ID categories: TELE, MIME, MGUN, SESI, ITEST, IGEN, IADM, IOPS, IDOC.

Required verbatim 0.x disclaimer (success criterion #3) — exact text in 50.5-PATTERNS.md "Required verbatim callout".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand mailglass_inbound/CHANGELOG.md [Unreleased] to cover Phases 45-49</name>
  <files>mailglass_inbound/CHANGELOG.md</files>
  <read_first>
    - mailglass_inbound/CHANGELOG.md (current [Unreleased] lines 8-82 — partial Phase 46 only; and the [0.1.0] narrative below it for shape)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md (the full target [Unreleased] structure with per-phase subsections and the verbatim 0.x disclaimer callout)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md (D-50.5-03 inbound scope — exact feature list per phase with REQ-IDs)
    - .planning/PROJECT.md "Current State" (authoritative per-phase feature descriptions for accurate prose, incl. Deviation D-49-21)
  </read_first>
  <action>
    Expand the existing `## [Unreleased]` section into the full 0.2.0 narrative. Lead with a one-paragraph summary that names the five phases of work and includes the verbatim 0.x stability disclaimer callout exactly as specified in 50.5-PATTERNS.md ("remains on the **0.x version line** — the 1.x stability promise applies to `mailglass` + `mailglass_admin` only. Conductor-style synthetic inbound dev tool, Cloudflare Email Routing, and `gen_smtp` listener are the pre-1.0 expansion targets. See [`guides/compatibility-and-deprecations.md`](../guides/compatibility-and-deprecations.md).").
    Organize the entry into per-phase subsections, PRESERVING the existing Phase 46 SES/error-struct/S3Fetcher/Dependencies content verbatim and ADDING the missing items:
    - "### Phase 45 — Telemetry + MIME (TELE-01..08, MIME-01..02, MIME-04)": telemetry spans at `[:mailglass_inbound, :ingress, :request, *]`, `[:route, :match, *]`, `[:execution, :run, *]`, `[:persist, :record, *]` with PII-free whitelisted metadata via `MailglassInbound.Telemetry`; `MailglassInbound.MIME` never-raising RFC 5322 parse seam over `Mailglass.OptionalDeps.GenSmtp.decode/2` returning `{:ok, _}` or `{:error, %MailglassInbound.MIMEError{}}`; StreamData 1000-replay convergence property (TELE-08).
    - "### Phase 46 — Mailgun + SES Providers (MGUN-01..04, SESI-01..05)": keep all existing SES + error-struct + S3Fetcher content, and ADD `MailglassInbound.Ingress.Providers.Mailgun` — HMAC-SHA256 ingress verifying the Mailgun signature, raising `MailglassInbound.SignatureError` on failure (no-recovery contract), dual body-mime/parsed mode.
    - "### Phase 47 — Test Helpers + Generators (ITEST-01..09, IGEN-01..04)": `MailglassInbound.MailboxCase` (ExUnit case, async: false, ETS sandbox); `MailglassInbound.TestAssertions` (4 matcher styles + outcome + routing + negative); `MailglassInbound.Test.Ingress` (real persist+sync-execute driver); `MailglassInbound.Fixtures` (code-built Postmark/SendGrid/Mailgun/SES-SNS payloads, no `.eml`/`.pem` on disk); generators `mix mailglass.gen.{mailbox,inbound_router,inbound_route}` (idempotent Sourceror-zipper edits, `--dry-run`).
    - "### Phase 48 — Admin LiveView Integration (IADM-01..07)": InboundLive ships via `mailglass_admin` 1.2.0 — adopters need `{:mailglass_admin, "~> 1.2"}` for the UI; the inbound package itself has no LiveView dependency.
    - "### Phase 49 — Runtime Operator Tooling (IOPS-01..05)": `mix mailglass.inbound.doctor` (three-state exit 0/1/2, DNS-free, `--strict`, `--format json`); `mix mailglass.inbound.replay` (tenant-scoped, `--tenant` REQUIRED, `--dry-run`, `--yes`); `mix mailglass.inbound.prune` (typed "yes" confirmation, `--dry-run`, `--yes`); `MailglassInbound.RateLimiter` (three-bucket: tenant/sender_domain/recipient); suppression signal at `.signals.suppression_flagged` (flag-only, no auto-bounce, Deviation D-49-21).
    Keep the existing `### Dependencies` block (ex_aws/ex_aws_s3) intact, and keep the `## 0.1.0 (2026-05-07)` section and everything below it untouched. Do NOT rename `[Unreleased]` to `[0.2.0]` — release-please does that when it opens the PR.
  </action>
  <verify>
    <automated>grep -q 'remains on the \*\*0.x version line\*\*' mailglass_inbound/CHANGELOG.md && grep -q 'Phase 45 — Telemetry' mailglass_inbound/CHANGELOG.md && grep -q 'Providers.Mailgun' mailglass_inbound/CHANGELOG.md && grep -q 'inbound.doctor' mailglass_inbound/CHANGELOG.md && grep -q 'signals.suppression_flagged' mailglass_inbound/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - mailglass_inbound/CHANGELOG.md [Unreleased] contains subsections for Phase 45, 46, 47, 48, and 49 (grep finds `Phase 45 — Telemetry`, `Phase 47 — Test`, `Phase 49 — Runtime`)
    - Contains `MailglassInbound.Ingress.Providers.Mailgun` (the previously-absent provider)
    - Contains the verbatim 0.x disclaimer (grep finds `remains on the **0.x version line**` and `compatibility-and-deprecations`)
    - Contains `signals.suppression_flagged` (Deviation D-49-21 correct accessor) and does NOT introduce a new `metadata.suppression_flagged` claim
    - The existing `## 0.1.0 (2026-05-07)` section and its content below are unchanged
    - The section header is still `## [Unreleased]` (not renamed to `[0.2.0]`)
  </acceptance_criteria>
  <done>Inbound 0.2.0 release notes document all five phases of v1.2 inbound work by REQ-ID category with the required 0.x stability disclaimer.</done>
</task>

<task type="auto">
  <name>Task 2: Author core CHANGELOG.md 1.2.0 curated narrative</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current [Unreleased] with the single OptionalDeps.GenSmtp entry; and the [1.0.0] four-subsection curated narrative below for structural shape)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md (core CHANGELOG target structure — single "What's new in 1.2.0" narrative + 9-category list)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md (D-50.5-03 core scope)
  </read_first>
  <action>
    Within the existing `## [Unreleased]` section, add a curated `## What's new in 1.2.0 (v1.2 Inbound Production Confidence)` narrative subsection ABOVE the existing `### Added` block. The narrative is a single milestone section (NOT the four-subsection style used for v1.0.0 which bundled four milestones). State that 1.2.0 ships production-ready inbound email processing, that the primary deliverable is `mailglass_inbound` 0.2.0 (link to mailglass_inbound/CHANGELOG.md), that the sibling `mailglass_admin` 1.2.0 ships the InboundLive admin UI, and that `mailglass_inbound` stays on the 0.x line (link to guides/compatibility-and-deprecations.md).
    Add a "### v1.2 REQ-ID categories shipped" list with adopter-facing one-liners for all 9 categories: TELE (TELE-01..08 telemetry spans + 1000-replay convergence), MIME (MIME-01..02,04 RFC 5322 parse seam via OptionalDeps.GenSmtp.decode/2 — the core addition this release), MGUN (MGUN-01..04 Mailgun HMAC ingress), SESI (SESI-01..05 SES SNS X.509 + S3), ITEST (ITEST-01..09 MailboxCase/TestAssertions/Test.Ingress/Fixtures), IGEN (IGEN-01..04 three generators), IADM (IADM-01..07 InboundLive via mailglass_admin 1.2.0), IOPS (IOPS-01..05 doctor/replay/prune + RateLimiter + suppression signals), IDOC (IDOC-01..06 six operator guides).
    PRESERVE the existing `### Added` block with the `Mailglass.OptionalDeps.GenSmtp.decode/2` entry verbatim — acknowledge it as the core code addition of this release within the narrative. Leave release-please's `### Features` / `### Bug Fixes` raw ledger (the `## [1.0.0]` section and below) entirely untouched as the audit trail. Do NOT rename `[Unreleased]` to `[1.2.0]` — release-please does that on PR open.
  </action>
  <verify>
    <automated>grep -q "What's new in 1.2.0" CHANGELOG.md && grep -q 'TELE-01..08' CHANGELOG.md && grep -q 'IDOC-01..06' CHANGELOG.md && grep -q 'OptionalDeps.GenSmtp.decode/2' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## What's new in 1.2.0 (v1.2 Inbound Production Confidence)`
    - The REQ-ID category list cites all 9: grep finds `TELE-01..08`, `MIME-01..02`, `MGUN-01..04`, `SESI-01..05`, `ITEST-01..09`, `IGEN-01..04`, `IADM-01..07`, `IOPS-01..05`, `IDOC-01..06`
    - The existing `Mailglass.OptionalDeps.GenSmtp.decode/2` entry is still present in CHANGELOG.md
    - The `## [1.0.0]` section and its ledger entries below are byte-for-byte unchanged
    - The section header is still `## [Unreleased]` (not renamed to `[1.2.0]`)
    - CHANGELOG.md links to `guides/compatibility-and-deprecations.md`
  </acceptance_criteria>
  <done>Core CHANGELOG leads with a curated single-milestone 1.2.0 narrative citing all 9 REQ categories, with the GenSmtp addition and the release-please ledger both preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-author mailglass_admin/CHANGELOG.md [Unreleased] coordinated-sibling note and stage Commit B</name>
  <files>mailglass_admin/CHANGELOG.md</files>
  <read_first>
    - mailglass_admin/CHANGELOG.md (confirm NO [Unreleased] section exists — file goes header → [1.0.0]; the [1.0.0] coordinated-sibling note is the shape to mirror)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md (admin target [Unreleased] block with exact sibling-note content)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md (D-50.5-03 admin scope + Commit B convention)
  </read_first>
  <action>
    Insert a new `## [Unreleased]` section between the file header (ends ~line 6) and the first `## [1.0.0]` entry. Content: a short coordinated-sibling note stating mailglass_admin 1.2.0 is version-paired (linked release) with mailglass 1.2.0; ships the InboundLive admin UI (`/inbound` route with list, detail, timeline, and routing-trace views; tenant-gated replay confirm modal); requires `{:mailglass_inbound, "~> 0.2"}` for the inbound UI surface; no breaking changes for existing admin users who do not use mailglass_inbound; points to the mailglass_inbound 0.2.0 CHANGELOG for the full inbound feature narrative. Add an `### Added` block naming `MailglassAdmin.InboundLive` (mountable LiveView for inbound, IADM-01..07, gated behind the MailglassInbound optional dep) and `MailglassAdmin.OptionalDeps.MailglassInbound` (optional-dep gateway). Add a `### Changed` block: "Stays version-paired with `mailglass` 1.2.0 release line." Do NOT rename to `[1.2.0]` — release-please does that on PR open. Leave the `## [1.0.0]` entries below untouched.
    Then stage exactly the three CHANGELOG files (mailglass_inbound/CHANGELOG.md, CHANGELOG.md, mailglass_admin/CHANGELOG.md) and commit with message `docs(changelog): pre-author curated 1.2.0 / 0.2.0 release headers`. Do NOT stage any other dirty files. Do NOT push — the orchestrator coordinates the A+B push before triggering release-please.
  </action>
  <verify>
    <automated>grep -q '## \[Unreleased\]' mailglass_admin/CHANGELOG.md && grep -q 'InboundLive' mailglass_admin/CHANGELOG.md && grep -q '~> 0.2' mailglass_admin/CHANGELOG.md && git log -1 --pretty=%s | grep -q 'pre-author curated'</automated>
  </verify>
  <acceptance_criteria>
    - mailglass_admin/CHANGELOG.md contains a `## [Unreleased]` section above the first `## [1.0.0]`
    - The [Unreleased] section contains `MailglassAdmin.InboundLive`, `MailglassAdmin.OptionalDeps.MailglassInbound`, and `{:mailglass_inbound, "~> 0.2"}`
    - The `## [1.0.0]` entries below are unchanged
    - `git diff --cached --name-only` (before commit) lists only the 3 CHANGELOG files
    - The Commit B commit exists with message `docs(changelog): pre-author curated 1.2.0 / 0.2.0 release headers`
  </acceptance_criteria>
  <done>Admin CHANGELOG has a pre-authored [Unreleased] coordinated-sibling note; all three CHANGELOG headers are committed (not pushed) so they survive release-please's force-push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated CHANGELOG headers → release-please force-push | Headers must be in commit history before PR open or they are clobbered; this is a correctness/audit boundary, not a security one |
| inbound 0.2.0 notes → adopter version expectations | The 0.x stability disclaimer is a contract communication; omitting it would misrepresent the API stability promise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.5-02-01 | Repudiation | release-please ledger preservation | mitigate | Acceptance criteria assert the `## [1.0.0]` ledger sections remain byte-for-byte unchanged — the audit trail is not lost when curated headers are added |
| T-50.5-02-02 | Information disclosure | CHANGELOG prose content | accept | CHANGELOGs are public adopter docs; prose names modules/REQ-IDs/behaviors only — no secrets, no PII, no signing keys; consistent with project brand-voice |
| T-50.5-02-03 | Spoofing | inbound 0.x stability claim | mitigate | Verbatim disclaimer callout asserted present (success criterion #3); links to the authoritative guides/compatibility-and-deprecations.md so the stability promise is not silently overstated |
| T-50.5-02-04 | Tampering | atomic Commit B scope | mitigate | `git diff --cached --name-only` asserted to list only the 3 CHANGELOG files; unrelated worktree WIP excluded |
| T-50.5-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — documentation edits only; RESEARCH.md Package Legitimacy Audit records "no new deps" |
</threat_model>

<verification>
- mailglass_inbound/CHANGELOG.md covers Phases 45-49 with the verbatim 0.x disclaimer and the Mailgun provider
- CHANGELOG.md has the curated 1.2.0 narrative with all 9 REQ categories; GenSmtp entry + [1.0.0] ledger preserved
- mailglass_admin/CHANGELOG.md has a pre-authored [Unreleased] coordinated-sibling section
- Commit B committed (not pushed) with only the three CHANGELOG files
</verification>

<success_criteria>
All three CHANGELOG files carry their curated 1.2.0/0.2.0 headers in commit history before release-please runs, satisfying success criteria #2 and #3. The headers will survive the force-push because they are committed on the branch the orchestrator pushes to main before triggering release-please.
</success_criteria>

<output>
Create `.planning/phases/50.5-v1-2-release-ceremony/50.5-02-SUMMARY.md` when done. Record: which phases/REQ-IDs were added to each CHANGELOG, confirmation the verbatim 0.x disclaimer is present, confirmation the existing ledgers were preserved, and the Commit B SHA.
</output>
