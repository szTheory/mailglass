# Phase 50.5: v1.2 Release Ceremony — Pattern Map

**Mapped:** 2026-05-25
**Files analyzed:** 10 files to be created or modified
**Analogs found:** 10 / 10

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `release-please-config.json` | config | — | `release-please-config.json` (Phase 44.5 edit history) | exact |
| `mix.exs` (line 5 removal) | config | — | `mailglass_inbound/mix.exs` line 5 (same editorial comment shape) | exact |
| `mailglass_inbound/mix.exs` (line 5 removal) | config | — | `mix.exs` line 5 (same editorial comment shape) | exact |
| `CHANGELOG.md` (curated 1.2.0 header) | doc | — | `CHANGELOG.md` `## [1.0.0]` curated narrative (2026-05-07 entry) | exact |
| `mailglass_admin/CHANGELOG.md` (add `[Unreleased]` + sibling note) | doc | — | `mailglass_admin/CHANGELOG.md` `## [1.0.0]` sibling note (lines 23–30) | exact |
| `mailglass_inbound/CHANGELOG.md` (`[Unreleased]` → full 0.2.0 content) | doc | — | `mailglass_inbound/CHANGELOG.md` existing `[Unreleased]` block + `## [0.1.0]` narrative (lines 112–130) | exact |
| `.planning/publish/mailglass-files.expected` (add 4 entries) | config | — | `.planning/publish/mailglass-files.expected` (current file; sorted-list convention) | exact |
| `.planning/publish/mailglass_admin-files.expected` (add 10 entries) | config | — | `.planning/publish/mailglass_admin-files.expected` (current file; sorted-list convention) | exact |
| `.planning/publish/mailglass_inbound-files.expected` (add 39 entries) | config | — | `.planning/publish/mailglass_inbound-files.expected` (current file; sorted-list convention) | exact |
| `.planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md` (new) | doc | — | `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md` | exact |

---

## Pattern Assignments

### `release-please-config.json` (config — version force update)

**Analog:** `release-please-config.json` (current file, lines 1–33)

**Current state** (lines 8–16):
```json
{
  "packages": {
    ".": {
      "package-name": "mailglass",
      "release-type": "elixir",
      "component": "mailglass",
      "changelog-path": "CHANGELOG.md",
      "release-as": "1.0.0"
    },
    "mailglass_admin": {
      "package-name": "mailglass_admin",
      "release-type": "elixir",
      "component": "mailglass_admin",
      "changelog-path": "CHANGELOG.md",
      "release-as": "1.0.0"
    },
    "mailglass_inbound": {
      "package-name": "mailglass_inbound",
      "release-type": "elixir",
      "component": "mailglass_inbound",
      "changelog-path": "CHANGELOG.md"
    }
  }
}
```

**Target state — Commit A change (two lines only):**
- Line 9: `"release-as": "1.0.0"` → `"release-as": "1.2.0"` (`.` package block)
- Line 16: `"release-as": "1.0.0"` → `"release-as": "1.2.0"` (`mailglass_admin` block)
- `mailglass_inbound` block: no change — no `"release-as"` field, 0.2.0 computed naturally from 32 `feat:` commits

**Critical rule:** Do NOT use per-commit `Release-As:` trailer for the linked group. The linked-versions plugin silently overrides trailers. The config `"release-as"` field is the only reliable force mechanism. This is Pitfall 5 from Phase 44.5, confirmed by commit `749e01d`.

---

### `mix.exs` (editorial comment removal — Commit A)

**Analog:** `mailglass_inbound/mix.exs` lines 1–6 (same structure, same comment pattern)

**Current state** (lines 4–6):
```elixir
@version "1.0.0"
# v1.0.0 cut — see Phase 044.5 release ceremony record
@source_url "https://github.com/szTheory/mailglass"
```

**Target state:**
```elixir
@version "1.0.0"
@source_url "https://github.com/szTheory/mailglass"
```

**Note:** `@version` stays `"1.0.0"` — release-please updates it to `"1.2.0"` in the Release PR. Do NOT pre-bump the version manually.

---

### `mailglass_inbound/mix.exs` (editorial comment removal — Commit A)

**Analog:** `mix.exs` lines 4–6 (same structure)

**Current state** (lines 4–6):
```elixir
@version "0.1.0"
# First Hex publish — see Phase 044.5 release ceremony record
@source_url "https://github.com/szTheory/mailglass"
```

**Target state:**
```elixir
@version "0.1.0"
@source_url "https://github.com/szTheory/mailglass"
```

**Note:** `@version` stays `"0.1.0"` and the dep pin at line 97 (`{:mailglass, "== 1.0.0"}`) stays as-is. The release-please.yml sync step (lines 133–178, `PINS` array) handles both updates automatically when the Release PR is generated.

---

### `CHANGELOG.md` (curated 1.2.0 header — Commit B)

**Analog:** `CHANGELOG.md` `## [1.0.0]` section starting at line 52 (2026-05-07 canonical entry with the four-subsection narrative — this is the structure to mirror, but briefer)

**Current `[Unreleased]` state** (lines 17–30) — this block becomes the curated 1.2.0 header:
```markdown
## [Unreleased]

### Added

- `Mailglass.OptionalDeps.GenSmtp.decode/2` — a never-raising RFC 5322 MIME
  parse seam over `:mimemail.decode/2` (gen_smtp 1.3.0)...
```

**Target `[Unreleased]` replacement — insert ABOVE the existing `[Unreleased]` line, converting it into the curated header:**

Structure per D-50.5-03:
```markdown
## [Unreleased]

## What's new in 1.2.0 (v1.2 Inbound Production Confidence)

`mailglass` 1.2.0 ships the v1.2 milestone: production-ready inbound email
processing via `mailglass_inbound` 0.2.0. The primary deliverable is the
`mailglass_inbound` sibling package — see its
[0.2.0 CHANGELOG](mailglass_inbound/CHANGELOG.md) for the full feature narrative.
Sibling: `mailglass_admin` 1.2.0 (linked release) ships the InboundLive admin UI.
`mailglass_inbound` remains on the 0.x version line — see
[`guides/compatibility-and-deprecations.md`](guides/compatibility-and-deprecations.md).

### v1.2 REQ-ID categories shipped

- **TELE (TELE-01..08)** — `:telemetry` spans at four inbound levels (ingress/route/execution/persist) with property-tested 1000-replay convergence proof
- **MIME (MIME-01..02, MIME-04)** — RFC 5322 MIME parse seam via `Mailglass.OptionalDeps.GenSmtp.decode/2` (the core addition in this release)
- **MGUN (MGUN-01..04)** — Mailgun inbound provider with HMAC-SHA256 ingress and dual body-mime/parsed mode
- **SESI (SESI-01..05)** — SES inbound provider with SNS X.509 verification and S3 receipt-rule extraction
- **ITEST (ITEST-01..09)** — `MailglassInbound.MailboxCase`, `TestAssertions`, `Test.Ingress`, `Fixtures` test helpers
- **IGEN (IGEN-01..04)** — `mix mailglass.gen.mailbox`, `mix mailglass.gen.inbound_router`, `mix mailglass.gen.inbound_route` generators
- **IADM (IADM-01..07)** — InboundLive admin UI shipped via `mailglass_admin` 1.2.0 (list/detail/timeline/routing-trace, tenant-gated replay)
- **IOPS (IOPS-01..05)** — `mix mailglass.inbound.doctor`, `.replay`, `.prune`; `MailglassInbound.RateLimiter`; suppression signals
- **IDOC (IDOC-01..06)** — six production operator guides (install, mailgun, ses, testing, operator, routing-debug)

### Added

- `Mailglass.OptionalDeps.GenSmtp.decode/2` — [existing Unreleased entry verbatim]

### Features / Bug Fixes

[release-please raw ledger preserved below — do not remove]
```

**Key rule:** The `## What's new in 1.2.0` narrative section is NOT the full four-subsection style used for v1.0.0. One milestone, one narrative section. The existing `[Unreleased]` GenSmtp entry is acknowledged in the `### Added` sub-section and preserved verbatim. Release-please's raw `### Features` / `### Bug Fixes` ledger remains below the curated header.

---

### `mailglass_admin/CHANGELOG.md` (add `[Unreleased]` + coordinated sibling note — Commit B)

**Analog:** `mailglass_admin/CHANGELOG.md` `## [1.0.0]` curated section (lines 23–34) — the coordinated sibling note shape to mirror

**Current state:** No `[Unreleased]` section exists. File goes from header directly to `## [1.0.0]...` at line 7.

**Template from `[1.0.0]` curated entry** (lines 23–34):
```markdown
## [1.0.0]...

`mailglass_admin` 1.0.0 is coordinated with `mailglass` 1.0.0 and shares the
1.x stability promise (per `guides/compatibility-and-deprecations.md`). There
is no admin-only migration story for the 1.0 cut: bump both linked sibling
packages together. See [`docs/upgrade-from-0.x.md`](../docs/upgrade-from-0.x.md)
for the bundled v0.5 → v1.1 upgrade story.

### Changed

- Stays version-paired with `mailglass` 1.0.0 release line.
```

**Target `[Unreleased]` to insert at line 7** (between the file header and the first `## [1.0.0]`):
```markdown
## [Unreleased]

`mailglass_admin` 1.2.0 is coordinated with `mailglass` 1.2.0 (linked release).
Ships the InboundLive admin UI: `/inbound` route with list, detail, timeline,
and routing-trace views; tenant-gated replay confirm modal. Requires
`{:mailglass_inbound, "~> 0.2"}` for the inbound UI surface. No breaking
changes for existing admin users who do not use `mailglass_inbound`. See
[`mailglass_inbound` 0.2.0 CHANGELOG](../mailglass_inbound/CHANGELOG.md) for
the full inbound feature narrative.

### Added

- `MailglassAdmin.InboundLive` — mountable LiveView for inbound message
  management (IADM-01..07); gated behind `MailglassInbound` optional dep.
- `MailglassAdmin.OptionalDeps.MailglassInbound` — optional-dep gateway for
  `mailglass_inbound`, following the project's gateway pattern.

### Changed

- Stays version-paired with `mailglass` 1.2.0 release line.
```

**Critical rule:** This `## [Unreleased]` section MUST land in Commit B, on `main`, before release-please opens the PR. Release-please force-pushes the CHANGELOG on PR open — pre-authoring on `main` before the PR opens is what preserves the curated header.

---

### `mailglass_inbound/CHANGELOG.md` (`[Unreleased]` full expansion — Commit B)

**Analog:** `mailglass_inbound/CHANGELOG.md` `## [0.1.0]` narrative (lines 112–130) — the shape to mirror; and `CHANGELOG.md` v1.0.0 multi-subsection structure for the per-phase breakdown style

**Current `[Unreleased]` state** (lines 8–82): Covers partial Phase 46 only — error structs (MIMEError, SignatureError, S3FetchError), OptionalDeps.ExAwsS3, S3Fetcher implementations, and SES provider. The Mailgun provider and all Phases 45/47/48/49 content are absent.

**Target `[Unreleased]` — expand in place to cover Phases 45–49:**

Structure:
```markdown
## [Unreleased]

`mailglass_inbound` 0.2.0 ships five phases of production-confidence work:
telemetry instrumentation, MIME parsing, the Mailgun provider, a full test
helper suite with generators, admin LiveView integration, and operator tooling.
`mailglass_inbound` remains on the **0.x version line** — the 1.x stability
promise applies to `mailglass` + `mailglass_admin` only. Conductor-style
synthetic inbound dev tool, Cloudflare Email Routing, and `gen_smtp` listener
are the pre-1.0 expansion targets. See
[`guides/compatibility-and-deprecations.md`](../guides/compatibility-and-deprecations.md).

### Phase 45 — Telemetry + MIME (TELE-01..08, MIME-01..02, MIME-04)

- `:telemetry` spans at `[:mailglass_inbound, :ingress, :request, :start/:stop/:exception]`,
  `[:route, :match, *]`, `[:execution, :run, *]`, `[:persist, :record, *]`
  with metadata whitelisted per core PII policy (no recipient/body/subject).
  `MailglassInbound.Telemetry` is the single attach-point module.
- `MailglassInbound.MIME` — RFC 5322 MIME parse seam via
  `Mailglass.OptionalDeps.GenSmtp.decode/2`; returns `{:ok, tuple}` or a
  `{:error, %MailglassInbound.MIMEError{}}`. Never raises. MIME-01, MIME-02,
  MIME-04.
- StreamData property test: 1000-replay convergence proof confirming telemetry
  handler failures do not propagate to business logic (TELE-08).

### Phase 46 — Mailgun + SES Providers (MGUN-01..04, SESI-01..05)

[existing Unreleased entries verbatim for SES + error structs, PLUS:]
- `MailglassInbound.Ingress.Providers.Mailgun` — HMAC-SHA256 ingress provider
  with dual body-mime/parsed mode. Verifies `X-Mailgun-Signature-V1` header;
  raises `MailglassInbound.SignatureError` on failure per no-recovery contract.
  Supports both raw MIME and pre-parsed Mailgun multipart payloads (MGUN-01..04).

### Phase 47 — Test Helpers + Generators (ITEST-01..09, IGEN-01..04)

- `MailglassInbound.MailboxCase` — ExUnit test case module (`async: false`,
  ETS sandbox). Use `use MailglassInbound.MailboxCase` in inbound tests
  (ITEST-01).
- `MailglassInbound.TestAssertions` — four assertion styles: exact match,
  pattern match, outcome assertion, routing assertion. `assert_routed_to/2`,
  `assert_mailbox_received/2`, `refute_mailbox_received/1` (ITEST-02..06).
- `MailglassInbound.Test.Ingress` — test ingress dispatch helper for bypassing
  the HTTP layer in unit tests (ITEST-07).
- `MailglassInbound.Fixtures` — in-memory fixture builder. No `.eml` files on
  disk; fixtures are constructed programmatically (ITEST-08, ITEST-09).
- `mix mailglass.gen.mailbox` — generates a `MyApp.Mailboxes.MyMailbox` module
  with `@behaviour MailglassInbound.Mailbox` (IGEN-01, IGEN-02).
- `mix mailglass.gen.inbound_router` — generates the router module for inbound
  routing configuration (IGEN-03).
- `mix mailglass.gen.inbound_route` — generates an individual route entry
  (IGEN-04).

### Phase 48 — Admin LiveView Integration (IADM-01..07)

- InboundLive shipped via `mailglass_admin` 1.2.0. Requires
  `{:mailglass_admin, "~> 1.2"}` for the admin UI. The inbound package itself
  has no LiveView dependency — the UI is entirely in `mailglass_admin`.
  See `mailglass_admin` 1.2.0 CHANGELOG for the full admin surface narrative.

### Phase 49 — Runtime Operator Tooling (IOPS-01..05)

- `mix mailglass.inbound.doctor` — three-state exit (0 = healthy, 1 = warnings,
  2 = errors). DNS-free checks. `--strict` flag promotes warnings to errors.
  `--format json` for CI integrations (IOPS-01).
- `mix mailglass.inbound.replay` — tenant-scoped message replay. `--tenant` is
  REQUIRED. `--dry-run` for preview, `--yes` for cron/CI (IOPS-02).
- `mix mailglass.inbound.prune` — typed "yes" confirmation for destructive prune.
  `--dry-run`, `--yes` flags. Bounded retention window (IOPS-03).
- `MailglassInbound.RateLimiter` — three-bucket rate limiter (tenant /
  sender_domain / recipient) with ETS-backed sliding window (IOPS-04).
- `MailglassInbound.InboundMessage.Signals` — framework-owned typed struct at
  `.signals.suppression_flagged`. Flag-only: message persists and reaches the
  mailbox even when flagged; no auto-bounce, no auto-suppression (IOPS-05,
  Deviation D-49-21: field is at `.signals.suppression_flagged` not
  `.metadata.suppression_flagged`).

### Dependencies

[existing Unreleased `### Dependencies` block verbatim — ex_aws + ex_aws_s3 entry]
```

**Required verbatim callout** (must appear in the 0.2.0 entry, per success criterion #3):
```markdown
`mailglass_inbound` remains on the **0.x version line** — the 1.x stability
promise applies to `mailglass` + `mailglass_admin` only. Conductor-style
synthetic inbound dev tool, Cloudflare Email Routing, and `gen_smtp` listener
are the pre-1.0 expansion targets. See
[`guides/compatibility-and-deprecations.md`](../guides/compatibility-and-deprecations.md).
```

---

### `.planning/publish/mailglass-files.expected` (add 4 entries — Commit A)

**Analog:** Current file `/Users/jon/projects/mailglass/.planning/publish/mailglass-files.expected` — one entry per line, alphabetically sorted within sections (lib/ entries in module path order, guides/ entries alphabetically)

**Verified missing entries** (confirmed by disk scan vs expected):
```
guides/jobs.md
lib/mix/tasks/mailglass.gen.inbound_route.ex
lib/mix/tasks/mailglass.gen.inbound_router.ex
lib/mix/tasks/mailglass.gen.mailbox.ex
```

**Insertion positions** (maintaining sort order):
- `guides/jobs.md` — insert after `guides/getting-started.md`, before `guides/migration-from-swoosh.md`
- `lib/mix/tasks/mailglass.gen.inbound_route.ex` — insert after `lib/mix/tasks/mailglass.gen.mailable.ex`, note: `inbound_route` sorts before `inbound_router` which sorts before `mailable` alphabetically — insert in `inbound_route`, `inbound_router`, `mailbox`, `mailable`, `migration`, `unsubscribe` order
- `lib/mix/tasks/mailglass.gen.inbound_router.ex` — same block
- `lib/mix/tasks/mailglass.gen.mailbox.ex` — same block

**Exact corrected `lib/mix/tasks/mailglass.gen.*` block order:**
```
lib/mix/tasks/mailglass.gen.inbound_route.ex
lib/mix/tasks/mailglass.gen.inbound_router.ex
lib/mix/tasks/mailglass.gen.mailbox.ex
lib/mix/tasks/mailglass.gen.mailable.ex
lib/mix/tasks/mailglass.gen.migration.ex
lib/mix/tasks/mailglass.gen.unsubscribe.ex
```

---

### `.planning/publish/mailglass_admin-files.expected` (add 10 entries — Commit A)

**Analog:** Current file `/Users/jon/projects/mailglass/.planning/publish/mailglass_admin-files.expected` — same one-per-line sorted convention

**Verified missing entries** (confirmed by disk scan vs expected):
```
lib/mailglass_admin/inbound/destructive_action.ex
lib/mailglass_admin/inbound/detail_header.ex
lib/mailglass_admin/inbound/evidence_card.ex
lib/mailglass_admin/inbound/filters_form.ex
lib/mailglass_admin/inbound/records_list.ex
lib/mailglass_admin/inbound/replay_modal.ex
lib/mailglass_admin/inbound/routing_trace.ex
lib/mailglass_admin/inbound/timeline.ex
lib/mailglass_admin/inbound_live.ex
lib/mailglass_admin/optional_deps/mailglass_inbound.ex
```

**Insertion positions** (maintaining sort order):
- All 8 `lib/mailglass_admin/inbound/*` entries insert after `lib/mailglass_admin/controllers/assets.ex`, before `lib/mailglass_admin/layouts.ex` (alphabetically: `controllers` < `inbound` < `layouts`)
- `lib/mailglass_admin/inbound_live.ex` inserts after the `lib/mailglass_admin/inbound/timeline.ex` block, before `lib/mailglass_admin/layouts.ex`
- `lib/mailglass_admin/optional_deps/mailglass_inbound.ex` inserts after `lib/mailglass_admin/optional_deps/phoenix_live_reload.ex` — wait: `mailglass_inbound` sorts BEFORE `phoenix_live_reload`, so insert before it

**Stale entries DO NOT exist:** The two `lib/mailglass_admin/layouts/*.html.heex` entries in the expected file are valid `.heex` files present on disk. Do not remove them.

---

### `.planning/publish/mailglass_inbound-files.expected` (add 39 entries — Commit A)

**Analog:** Current file `/Users/jon/projects/mailglass/.planning/publish/mailglass_inbound-files.expected` — same one-per-line sorted convention

**Verified missing entries** (confirmed by disk scan vs expected, 39 total):

New lib files (30 — verified on disk):
```
lib/mailglass_inbound/config.ex
lib/mailglass_inbound/fixtures.ex
lib/mailglass_inbound/inbound_message/signals.ex
lib/mailglass_inbound/ingress/providers/mailgun.ex
lib/mailglass_inbound/ingress/providers/ses.ex
lib/mailglass_inbound/internal/doctor.ex
lib/mailglass_inbound/internal/operator/detail.ex
lib/mailglass_inbound/internal/operator/records.ex
lib/mailglass_inbound/internal/operator/timeline.ex
lib/mailglass_inbound/internal/prune.ex
lib/mailglass_inbound/mailbox_case.ex
lib/mailglass_inbound/mime.ex
lib/mailglass_inbound/mime_error.ex
lib/mailglass_inbound/operator/formatter.ex
lib/mailglass_inbound/prune/worker.ex
lib/mailglass_inbound/pub_sub/topics.ex
lib/mailglass_inbound/rate_limiter.ex
lib/mailglass_inbound/rate_limiter/table_owner.ex
lib/mailglass_inbound/s3_fetch_error.ex
lib/mailglass_inbound/s3_fetcher.ex
lib/mailglass_inbound/s3_fetcher/ex_aws_s3.ex
lib/mailglass_inbound/s3_fetcher/fake.ex
lib/mailglass_inbound/s3_fetcher/retry.ex
lib/mailglass_inbound/signature_error.ex
lib/mailglass_inbound/telemetry.ex
lib/mailglass_inbound/test/ingress.ex
lib/mailglass_inbound/test_assertions.ex
lib/mix/tasks/mailglass.inbound.doctor.ex
lib/mix/tasks/mailglass.inbound.prune.ex
lib/mix/tasks/mailglass.inbound.replay.ex
```

New docs (6 — verified on disk):
```
docs/inbound-install.md
docs/inbound-mailgun.md
docs/inbound-operator.md
docs/inbound-routing-debug.md
docs/inbound-ses.md
docs/inbound-testing.md
```

New migrations (3 — verified on disk):
```
priv/repo/migrations/20260523120000_add_mailgun_fingerprint_index.exs
priv/repo/migrations/20260523130000_add_ses_fingerprint_index.exs
priv/repo/migrations/20260525000000_add_suppression_flagged_to_inbound_records.exs
```

**No stale entries** — all 38 current entries are confirmed present on disk.

**Sort position for `lib/mix/tasks/mailglass.inbound.*` entries:** Insert after the last `lib/mailglass_inbound/...` entry, before `mix.exs` (current file has `mix.exs` as the last lib entry before `priv/`). The three inbound mix tasks sort as: `doctor` < `prune` < `replay`.

**Additional `required_file_entries` consideration:** `mix mailglass.publish.check` line 1160 hardcodes only 3 required docs (`api_stability.md`, `postmark_ingress.md`, `sendgrid_ingress.md`). Consider adding the 6 Phase 50 docs to this list in Commit A as a belt-and-suspenders guard (per RESEARCH.md Open Question #2). This is in the file at `/Users/jon/projects/mailglass/lib/mix/tasks/mailglass.publish.check.ex` — verify line 1160 range before editing.

---

### `.planning/phases/50.5-v1-2-release-ceremony/50.5-RELEASE-RECORD.md` (new file — post-publish)

**Analog:** `.planning/phases/044.5-v1-0-1-1-release-ceremony/044.5-RELEASE-RECORD.md` (read in full above)

**Exact section structure to mirror:**

1. **Header block** (lines 1–30 pattern):
   ```
   Release type: live (linked-version cut for v1.2 bundle)
   Tag: mailglass-v1.2.0
   Sibling tags: mailglass_admin-v1.2.0 ; mailglass_inbound-v0.2.0
   Squash-merge commit: <SHA> (<URL>)
   Live publish workflow run URL: <URL>
     - prepublish-summary: <outcome>
     - gate-ci-green: <outcome>
     - publish-core: <outcome> (mix hex.publish --yes mailglass 1.2.0)
     - publish-admin: <outcome> (mix hex.publish --yes mailglass_admin 1.2.0)
     - publish-inbound: <outcome> (mix hex.publish --yes mailglass_inbound 0.2.0)
   Post-publish smoke run URL: <URL>
   GitHub Release URLs: (3 tags)
   Hex URLs: (3 packages × version)
   HexDocs URLs: (3 packages × version)
   Fallback path used: <yes/not used>
   60-minute outcome: <pass/fail>
   ```

2. **`## Manual approvals and external checks`** — approver, timestamp, branch-protection note

3. **`## Sandbox install proof (60-minute window)`** — `mix phx.new sandbox` command, deps used (`{:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"}`), `mix deps.get` outcome, `mix compile --warnings-as-errors` outcome, decision, rationale, timestamp

4. **`## Dispatch attempt chronology`** — table with columns `#`, `Run ID`, `URL`, `Outcome`, `What it taught us`. Phase 50.5 expects fewer retries than Phase 44.5 (all 44.5 pitfalls have known solutions). If the first dry-run succeeds, record it as attempt #1 and the live publish as the final entry.

5. **`## Recovery commits applied during ceremony retries`** — paper trail. If first dry-run succeeds, this section may be empty or note "None — all Phase 44.5 pitfalls pre-resolved."

6. **`## Proof links`** — predecessor (`044.5-RELEASE-RECORD.md`), CI evidence URLs, CHANGELOG sections, allowlist files, carry-over tracker (Phase 51 deferred items)

7. **`## Notes`** — advisory deviations. Minimally: Operator Browser Gate is advisory (pre-existing from `5734c1b`); post-publish-smoke inbound consumer-install bug is known pre-existing (deferred-items.md item #8); manual sandbox substituted.

**Key differences from 044.5 record:**
- Tags: `mailglass-v1.2.0` / `mailglass_admin-v1.2.0` / `mailglass_inbound-v0.2.0`
- Sandbox deps use `"~> 1.2"` and `"~> 0.2"` (not pinned `"== 1.0.0"`)
- Predecessor section points to `044.5-RELEASE-RECORD.md`
- Carry-over tracker now points to `deferred-items.md` items #6-successor (Phase 51 removes `"release-as": "1.2.0"`), #8, #9-10
- Does NOT overwrite `044.5-RELEASE-RECORD.md`

---

## Shared Patterns

### Allowlist File Format
**Source:** `.planning/publish/mailglass-files.expected` (all three files use identical format)
**Apply to:** All three `.planning/publish/*.expected` file edits

```
# One path per line, no trailing whitespace, no blank lines between entries.
# Sorted: top-level files first, then alphabetically by path within each section.
# Sections (implicit, by path prefix): top-level → docs/ → hex_metadata.config
#   → lib/ → mix.exs → priv/
# No comments in actual files — this is just the pattern description.
```

Rule: do not add glob patterns (e.g., `priv/static/fonts/*.woff2`) as path literals. Globs are excluded from the missing-report scan by `mix mailglass.publish.check` Rule 1 (commit `12002ea`). Only list concrete file paths.

### Commit A Convention
**Source:** D-50.5-02 + Phase 44.5 ceremony record
**Apply to:** Commit A message

```
chore(release-engineering): clean up post-1.0 publish recovery anchors + force 1.2.0 cut
```

Files in Commit A: `release-please-config.json`, `mix.exs`, `mailglass_inbound/mix.exs`, all three `.planning/publish/*.expected` files. (MAINTAINING.md sandbox recipe update optional — Claude's Discretion per RESEARCH.md Open Question #1.)

### Commit B Convention
**Source:** D-50.5-02 (ordering constraint)
**Apply to:** Commit B message

```
docs(changelog): pre-author curated 1.2.0 / 0.2.0 release headers
```

Files in Commit B: `CHANGELOG.md`, `mailglass_admin/CHANGELOG.md`, `mailglass_inbound/CHANGELOG.md`. Must land on `main` BEFORE release-please opens the PR.

### Release PR Verification Checklist
**Source:** `044.5-RELEASE-RECORD.md` + RESEARCH.md Architecture Patterns
**Apply to:** Live ceremony execution

When release-please opens the PR, verify the PR diff shows:
1. `.release-please-manifest.json` bumped to `{".": "1.2.0", "mailglass_admin": "1.2.0", "mailglass_inbound": "0.2.0"}`
2. `mailglass_admin/mix.exs` dep pin updated to `{:mailglass, "== 1.2.0"}` (sync step)
3. `mailglass_inbound/mix.exs` dep pin updated to `{:mailglass, "== 1.2.0"}` (sync step)
4. Curated CHANGELOG headers preserved (not replaced by raw ledger)
5. `mix.exs` `@version` bumped to `"1.2.0"`
6. `mailglass_inbound/mix.exs` `@version` bumped to `"0.2.0"`

---

## No Analog Found

All files in this phase have close analogs. No files require falling back to RESEARCH.md patterns alone.

---

## Metadata

**Analog search scope:** Repository root; `.planning/phases/044.5-v1-0-1-1-release-ceremony/`; `CHANGELOG.md`; `mailglass_admin/CHANGELOG.md`; `mailglass_inbound/CHANGELOG.md`; `.planning/publish/`; `release-please-config.json`; `mix.exs`; `mailglass_inbound/mix.exs`
**Files scanned:** 14 files read + disk-verified with `find` commands
**Pattern extraction date:** 2026-05-25

### Disk-Verified Allowlist Gap Summary

| Package | Current entries | Missing entries | New total |
|---|---|---|---|
| `mailglass` | 177 | 4 (3 gen tasks + `guides/jobs.md`) | 181 |
| `mailglass_admin` | 49 | 10 (InboundLive + optional_deps) | 59 |
| `mailglass_inbound` | 38 | 39 (30 lib + 6 docs + 3 migrations) | 77 |

All gaps verified by `find` against the actual filesystem — these are ground-truth counts, not estimates from RESEARCH.md.
