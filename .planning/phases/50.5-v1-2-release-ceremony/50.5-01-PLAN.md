---
phase: 50.5-v1-2-release-ceremony
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release-please-config.json
  - mix.exs
  - mailglass_inbound/mix.exs
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass_admin-files.expected
  - .planning/publish/mailglass_inbound-files.expected
  - lib/mix/tasks/mailglass.publish.check.ex
  - MAINTAINING.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "release-please-config.json forces 1.2.0 on both linked-group blocks (. and mailglass_admin)"
    - "Editorial recovery-anchor comments are removed from mix.exs:5 and mailglass_inbound/mix.exs:5"
    - "All three tarball allowlists match disk so mix mailglass.publish.check passes for all three packages"
    - "REL-05 dep-pin stability test stays green (no sync-step anchor regression)"
  artifacts:
    - path: "release-please-config.json"
      provides: "Version force 1.2.0 on linked group; inbound unchanged (0.2.0 natural)"
      contains: "\"release-as\": \"1.2.0\""
    - path: ".planning/publish/mailglass-files.expected"
      provides: "Core tarball allowlist with 4 new entries (3 gen tasks + guides/jobs.md)"
      min_lines: 181
    - path: ".planning/publish/mailglass_admin-files.expected"
      provides: "Admin tarball allowlist with 10 new InboundLive entries"
      min_lines: 58
    - path: ".planning/publish/mailglass_inbound-files.expected"
      provides: "Inbound tarball allowlist with 39 new entries (30 lib + 6 docs + 3 migrations)"
      min_lines: 77
  key_links:
    - from: "release-please-config.json"
      to: "release-please linked-versions plugin"
      via: "release-as field forces 1.2.0 cut"
      pattern: "release-as.*1\\.2\\.0"
    - from: ".planning/publish/*.expected"
      to: "mix mailglass.publish.check"
      via: "tarball file-set comparison"
      pattern: "publish.check"
---

<objective>
Land pre-ceremony Commit A: the release-engineering cleanup and version force that must reach `main` before release-please is triggered. This forces the linked group (`mailglass` + `mailglass_admin`) to cut 1.2.0 (skipping the 1.1.0 conventional commits would otherwise compute), lets `mailglass_inbound` compute 0.2.0 naturally, removes the stale 1.0.0-ceremony recovery anchors, and repairs all three tarball allowlists that have gone stale across five implementation phases (45–50).

Purpose: The dry-run dispatch in Wave 3 fails `mix mailglass.publish.check` unless the allowlists match disk and the version force is in place. This plan is the load-bearing prep that makes the live ceremony a repeat (not a discovery) of Phase 44.5.
Output: One atomic commit `chore(release-engineering): clean up post-1.0 publish recovery anchors + force 1.2.0 cut` containing the config force, two comment removals, three allowlist refreshes, an optional publish.check required-entries hardening, and the MAINTAINING.md sandbox-recipe version update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-RESEARCH.md
@.planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md

<interfaces>
<!-- Verified file states (disk-confirmed 2026-05-25). Executor should treat these as ground truth. -->

release-please-config.json (line 9 = `.` block; line 16 = mailglass_admin block):
  Both carry `"release-as": "1.0.0"`. The mailglass_inbound block (lines 18-23) has NO release-as field — leave it that way.
  The linked-versions plugin (lines 25-31) groups only `["mailglass", "mailglass_admin"]`.

mix.exs lines 4-6:
  @version "1.0.0"
  # v1.0.0 cut — see Phase 044.5 release ceremony record    <- remove this comment line only
  @source_url "..."
  Keep @version "1.0.0" — release-please bumps it to 1.2.0 in the Release PR. Do NOT pre-bump.

mailglass_inbound/mix.exs lines 4-6:
  @version "0.1.0"
  # First Hex publish — see Phase 044.5 release ceremony record    <- remove this comment line only
  @source_url "..."
  Keep @version "0.1.0". The dep pin at line 97 `{:mailglass, "== 1.0.0"}` stays as-is — release-please.yml sync step bumps it to "== 1.2.0" automatically.

mailglass_admin/mix.exs: has NO editorial comment. No change in this plan.

publish.check required_file_entries(:mailglass_inbound) at lib/mix/tasks/mailglass.publish.check.ex lines 1160-1167:
  Currently lists [".formatter.exs", "docs/api_stability.md", "docs/postmark_ingress.md", "docs/sendgrid_ingress.md"].

Allowlist format (all three .expected files): one path per line, alphabetically sorted by path, no globs as literals, no blank lines, no comments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force 1.2.0 in release-please-config.json and remove editorial recovery anchors</name>
  <files>release-please-config.json, mix.exs, mailglass_inbound/mix.exs, MAINTAINING.md</files>
  <read_first>
    - release-please-config.json (confirm both release-as fields read "1.0.0" before editing; line 9 = `.` block, line 16 = mailglass_admin block)
    - mix.exs (confirm line 5 is `# v1.0.0 cut — see Phase 044.5 release ceremony record`)
    - mailglass_inbound/mix.exs (confirm line 5 is `# First Hex publish — see Phase 044.5 release ceremony record`)
    - MAINTAINING.md line 212 (confirm sandbox recipe still references `== 1.0.0` / `== 0.1.0`)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-CONTEXT.md (D-50.5-01, D-50.5-02 — version force and editorial cleanup scope)
  </read_first>
  <action>
    In release-please-config.json, change `"release-as": "1.0.0"` to `"release-as": "1.2.0"` in the `.` package block (line 9) AND in the `mailglass_admin` block (line 16). Do NOT add a release-as field to the `mailglass_inbound` block — it computes 0.2.0 naturally from its 32 feat: commits since mailglass_inbound-v0.1.0 (D-50.5-01). Do NOT touch the linked-versions plugin block. Do NOT touch .release-please-manifest.json (release-please updates it when it opens the PR).
    In mix.exs, delete the single editorial comment line `# v1.0.0 cut — see Phase 044.5 release ceremony record` (between `@version "1.0.0"` and `@source_url`). Keep `@version "1.0.0"` unchanged — release-please bumps it. This resolves deferred-items.md item #7 (root mix.exs anchor).
    In mailglass_inbound/mix.exs, delete the single editorial comment line `# First Hex publish — see Phase 044.5 release ceremony record` (between `@version "0.1.0"` and `@source_url`). Keep `@version "0.1.0"` unchanged. Keep the dep pin `{:mailglass, "== 1.0.0"}` at line 97 unchanged — the release-please.yml sync step updates it to `"== 1.2.0"`. This resolves deferred-items.md item #7 (inbound mix.exs anchor).
    In MAINTAINING.md line 212, update the sandbox-smoke recipe deps from `{:mailglass, "== 1.0.0"}, {:mailglass_admin, "== 1.0.0"}, {:mailglass_inbound, "== 0.1.0"}` to `{:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"}` so the Wave 3 sandbox smoke matches the versions actually being published (Claude's Discretion per CONTEXT.md — two-line version-string update, no structural change).
  </action>
  <verify>
    <automated>grep -c '"release-as": "1.2.0"' release-please-config.json | grep -qx 2 && ! grep -q 'release ceremony record' mix.exs && ! grep -q 'release ceremony record' mailglass_inbound/mix.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - release-please-config.json contains exactly two occurrences of `"release-as": "1.2.0"` and zero occurrences of `"release-as": "1.0.0"`
    - The mailglass_inbound block in release-please-config.json still has NO release-as field
    - mix.exs contains `@version "1.0.0"` and does NOT contain the string `release ceremony record`
    - mailglass_inbound/mix.exs contains `@version "0.1.0"`, the dep pin `{:mailglass, "== 1.0.0"}`, and does NOT contain the string `release ceremony record`
    - MAINTAINING.md line 212 region contains `{:mailglass, "~> 1.2"}` and does NOT contain `== 1.0.0`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Linked group forces 1.2.0; inbound left to compute 0.2.0; both recovery-anchor comments gone; sandbox recipe uses current versions; compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh all three tarball allowlists to match disk and harden inbound required-entries</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass_admin-files.expected, .planning/publish/mailglass_inbound-files.expected, lib/mix/tasks/mailglass.publish.check.ex</files>
  <read_first>
    - .planning/publish/mailglass-files.expected (current 177-entry sorted allowlist — see existing sort convention)
    - .planning/publish/mailglass_admin-files.expected (current 48-entry sorted allowlist)
    - .planning/publish/mailglass_inbound-files.expected (current 38-entry sorted allowlist)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md (exact missing-entry lists with insertion positions + sort order, per-package)
    - lib/mix/tasks/mailglass.publish.check.ex lines 1153-1168 (required_file_entries clauses — inbound clause at 1160)
  </read_first>
  <action>
    Add the 4 missing entries to .planning/publish/mailglass-files.expected, inserted in correct alphabetical sort position: `guides/jobs.md` (after `guides/getting-started.md`); `lib/mix/tasks/mailglass.gen.inbound_route.ex`, `lib/mix/tasks/mailglass.gen.inbound_router.ex`, `lib/mix/tasks/mailglass.gen.mailbox.ex` (in the `lib/mix/tasks/mailglass.gen.*` block, sorting before `mailable`). Final gen-block order: inbound_route, inbound_router, mailbox, mailable, migration, unsubscribe.
    Add the 10 missing entries to .planning/publish/mailglass_admin-files.expected: the 8 `lib/mailglass_admin/inbound/*.ex` files (destructive_action, detail_header, evidence_card, filters_form, records_list, replay_modal, routing_trace, timeline — alphabetical, between `controllers/` and `layouts.ex`), then `lib/mailglass_admin/inbound_live.ex`, then `lib/mailglass_admin/optional_deps/mailglass_inbound.ex` (which sorts BEFORE `optional_deps/phoenix_live_reload.ex`). Do NOT remove the two existing `lib/mailglass_admin/layouts/*.html.heex` entries — they are valid on-disk .heex files.
    Add the 39 missing entries to .planning/publish/mailglass_inbound-files.expected: the 30 new lib files (config.ex, fixtures.ex, inbound_message/signals.ex, ingress/providers/mailgun.ex, ingress/providers/ses.ex, internal/doctor.ex, internal/operator/{detail,records,timeline}.ex, internal/prune.ex, mailbox_case.ex, mime.ex, mime_error.ex, operator/formatter.ex, prune/worker.ex, pub_sub/topics.ex, rate_limiter.ex, rate_limiter/table_owner.ex, s3_fetch_error.ex, s3_fetcher.ex, s3_fetcher/ex_aws_s3.ex, s3_fetcher/fake.ex, s3_fetcher/retry.ex, signature_error.ex, telemetry.ex, test/ingress.ex, test_assertions.ex, plus lib/mix/tasks/mailglass.inbound.{doctor,prune,replay}.ex), the 6 new docs (docs/inbound-{install,mailgun,operator,routing-debug,ses,testing}.md), and the 3 new migrations (priv/repo/migrations/20260523120000_add_mailgun_fingerprint_index.exs, 20260523130000_add_ses_fingerprint_index.exs, 20260525000000_add_suppression_flagged_to_inbound_records.exs). Maintain alphabetical sort within each path-prefix section (docs/ before lib/ before priv/). The three `lib/mix/tasks/mailglass.inbound.*` entries sort doctor < prune < replay. Do NOT add any glob-style entry (no `*`) — publish.check rejects literal globs as missing paths.
    In lib/mix/tasks/mailglass.publish.check.ex, extend the `required_file_entries(:mailglass_inbound)` list (lines 1160-1167) to add the 6 Phase 50 docs as belt-and-suspenders required entries: `docs/inbound-install.md`, `docs/inbound-testing.md`, `docs/inbound-operator.md`, `docs/inbound-mailgun.md`, `docs/inbound-ses.md`, `docs/inbound-routing-debug.md`. Keep the existing four entries.
  </action>
  <verify>
    <automated>mix mailglass.publish.check --package mailglass && mix mailglass.publish.check --package mailglass_admin && mix mailglass.publish.check --package mailglass_inbound</automated>
  </verify>
  <acceptance_criteria>
    - `mix mailglass.publish.check --package mailglass` exits 0
    - `mix mailglass.publish.check --package mailglass_admin` exits 0
    - `mix mailglass.publish.check --package mailglass_inbound` exits 0
    - .planning/publish/mailglass-files.expected contains `guides/jobs.md` and `lib/mix/tasks/mailglass.gen.mailbox.ex`
    - .planning/publish/mailglass_admin-files.expected contains `lib/mailglass_admin/inbound_live.ex` and `lib/mailglass_admin/optional_deps/mailglass_inbound.ex`
    - .planning/publish/mailglass_inbound-files.expected contains `lib/mailglass_inbound/telemetry.ex`, `docs/inbound-ses.md`, and `priv/repo/migrations/20260525000000_add_suppression_flagged_to_inbound_records.exs`
    - lib/mix/tasks/mailglass.publish.check.ex `required_file_entries(:mailglass_inbound)` contains `docs/inbound-routing-debug.md`
    - No .expected file contains a line with `*` (no literal globs)
  </acceptance_criteria>
  <done>All three packages pass publish.check against the actual tarball contents; the dry-run dispatch in Wave 3 will not fail on stale allowlists.</done>
</task>

<task type="auto">
  <name>Task 3: Validate dep-pin stability and stage Commit A</name>
  <files>(no new edits — verification + staging task)</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/mix_config_test.exs (REL-05 sed-anchor stability guard — confirm it asserts the `{:mailglass, "== <semver>"}` pin shape)
    - .planning/phases/50.5-v1-2-release-ceremony/50.5-PATTERNS.md (Commit A convention: message + exact file list)
  </read_first>
  <action>
    Run the REL-05 dep-pin stability test to confirm the release-please.yml sync step's sed anchors are intact and that nothing in this plan's edits broke the `{:mailglass, "== <semver>"}` pin shape. (This test guards the sync-step anchor that will bump both sibling pins to `== 1.2.0` when release-please opens the PR.) Then stage exactly the Commit A files for an atomic commit: release-please-config.json, mix.exs, mailglass_inbound/mix.exs, the three .planning/publish/*.expected files, lib/mix/tasks/mailglass.publish.check.ex, and MAINTAINING.md. Do NOT stage any other dirty files (the worktree has unrelated WIP under .planning/ and guides/jobs.md). Commit message: `chore(release-engineering): clean up post-1.0 publish recovery anchors + force 1.2.0 cut`. Do NOT push in this plan — the orchestrator coordinates the push of Commit A and Commit B together before release-please is triggered.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/mix_config_test.exs && cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mailglass_admin/mix_config_test.exs` (run from mailglass_admin/) exits 0 (REL-05 green)
    - `git diff --cached --name-only` lists only the 8 Commit A files (release-please-config.json, mix.exs, mailglass_inbound/mix.exs, the 3 .expected files, lib/mix/tasks/mailglass.publish.check.ex, MAINTAINING.md)
    - The Commit A commit exists on the current branch with message containing `force 1.2.0 cut`
  </acceptance_criteria>
  <done>REL-05 confirms the sync-step anchors are intact; Commit A is staged atomically with only the intended files and committed (not pushed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → release-please plugin | `release-as` field drives the version computed for the published tarball; an incorrect value mis-versions a public Hex release (irreversible) |
| allowlist → published tarball | `.expected` files gate which files ship; a stale allowlist either blocks the publish or (if loosened wrongly) could ship unintended files |
| local commit → main → release-please PR | Commit A content must land before the PR opens; ordering is load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.5-01-01 | Tampering | release-please-config.json version force | mitigate | Verify exactly two `"release-as": "1.2.0"` and zero `"1.0.0"`; inbound block has no release-as; `release-please --dry-run` confirmation deferred to Wave 3 PR review (manifest bumps verified in PR diff) |
| T-50.5-01-02 | Information disclosure | tarball allowlist edits | mitigate | Only add concrete on-disk paths verified to exist (disk-confirmed list in PATTERNS.md); no globs; publish.check exit-0 proves tarball matches allowlist exactly — no extra files smuggled in |
| T-50.5-01-03 | Tampering | dep-pin sync-step anchors | mitigate | REL-05 `mix_config_test.exs` asserts the `{:mailglass, "== <semver>"}` pin shape survives; run before commit |
| T-50.5-01-04 | Repudiation | atomic Commit A scope | mitigate | `git diff --cached --name-only` asserted to list only the 8 intended files; unrelated worktree WIP excluded |
| T-50.5-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — release-engineering config + allowlist edits only; RESEARCH.md Package Legitimacy Audit records "no new deps" |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0
- `mix mailglass.publish.check --package mailglass|mailglass_admin|mailglass_inbound` all exit 0
- `mix test test/mailglass_admin/mix_config_test.exs` (from mailglass_admin/) exits 0
- release-please-config.json: exactly two `"release-as": "1.2.0"`, zero `"1.0.0"`, inbound block has none
- No editorial recovery-anchor comments remain in mix.exs or mailglass_inbound/mix.exs
</verification>

<success_criteria>
Commit A exists on the branch (not pushed) with the version force, both comment removals, three refreshed allowlists, the inbound required-entries hardening, and the MAINTAINING.md sandbox-recipe update — and all three packages pass `mix mailglass.publish.check`, so the Wave 3 dry-run dispatch will not fail on stale prep.
</success_criteria>

<output>
Create `.planning/phases/50.5-v1-2-release-ceremony/50.5-01-SUMMARY.md` when done. Record: the exact allowlist entry counts before/after per package, the publish.check exit codes, the REL-05 result, and the Commit A SHA.
</output>
