---
phase: 166-earned-greens-and-controls-that-can-pass
plan: 02
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - .github/workflows/ci.yml
  - test/scripts/lane_classification_drift_test.exs
autonomous: true
requirements: [GREEN-03]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The required `core_deterministic_suite` lane log prints `scope: FULL SUITE (MAILGLASS_SUITE_FLOOR=1)` instead of a scoped-run / floor-not-evaluated line (GREEN-03)."
    - "`MAILGLASS_SUITE_FLOOR: \"1\"` appears exactly once in `.github/workflows/ci.yml`, in the `env:` block of the `Run deterministic core suite` step (D-09)."
    - "`test/scripts/lane_classification_drift_test.exs` gains a `@ci_yml_suite_floor_occurrences` constant plus an assertion test, an anti-vacuity test, and a negative-control test, mirroring the existing `advisory-matrix.yml` trio (D-08)."
    - "`@suite_floor_env_occurrences` stays 2 — the roadmap's 2-to-3 bump is wrong as written and must not be applied; that constant counts `advisory-matrix.yml` only (D-07)."
    - "The `ci.yml` env line and the extended guard land in the SAME commit; either alone reds the suite (D-38)."
    - "An unfiltered local `MAILGLASS_SUITE_FLOOR=1 mix test --warnings-as-errors` is green before the env line is pushed, because this lane (unlike the advisory legs) does not exclude `:requires_workspace` (D-10)."
    - "With the floor enabled the lane passes at today's committed numbers with NO constant re-pinning: executed floors 1576/1575 against roughly 2145 executed, and a skipped ceiling of 7 against 7 measured (edge: GREEN-03/boundary)."
    - "The two occurrence guards never read the same file: `@suite_floor_env_occurrences` counts `advisory-matrix.yml`, the new `@ci_yml_suite_floor_occurrences` counts `ci.yml` (edge: GREEN-03/adjacency)."
    - "The counted literal is exactly the string `MAILGLASS_SUITE_FLOOR: \"1\"` — a value change to \"0\" or \"true\" reds the guard rather than silently counting (edge: GREEN-03/precision)."
    - statement: "Anti-vacuity: an empty or unreadable `ci.yml` makes the new guard fail rather than count zero occurrences and pass."
      verification: backstop
    - statement: "The occurrence count is position-independent — an occurrence anywhere in `ci.yml` counts identically, so no ordering contract is implied."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - test/scripts/lane_classification_drift_test.exs
  key_links:
    - "`.github/workflows/ci.yml` `Run deterministic core suite` step `env:` block feeds `test/support/suite_floor.ex`'s runtime env read, which turns the floor from advisory to enforced."
    - "`test/scripts/lane_classification_drift_test.exs` `@ci_yml_path` (already declared in that module) is the file the new occurrence guard reads — reuse it, do not redeclare."
  prohibitions:
    - statement: "Do not add `continue-on-error` to `cron-guard`, and do not drop a cron from `post-publish-smoke.yml`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not delete `recheck_by`, `expired_entries/1`, or `unused_entries/1`."
      status: pending
      verification: flagged-unverified
    - statement: "Do not lower `max_runs` on the inbound property test or narrow its generator."
      status: pending
      verification: flagged-unverified
    - statement: "Do not re-pin `@executed_floors` or `@skipped_ceiling` in `test/support/suite_floor.ex`, and do not add a CLI `--exclude` to the deterministic lane, to make the newly-enforced floor pass — if it reds on a sandbox-ownership signature, fix the cause (D-10)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not bump `@suite_floor_env_occurrences` from 2 to 3 — it counts `advisory-matrix.yml` only and a bare bump reds an unchanged file (D-07)."
      status: pending
      verification: flagged-unverified
    - statement: "Do not widen any workflow permissions scope beyond the existing minimal grants."
      status: pending
      verification: flagged-unverified
---

<objective>
PR-2 of five (D-37). Turn on the anti-vacuity suite floor for the required `core_deterministic_suite`
lane, and extend the occurrence-count drift guard so the new env line cannot be silently deleted.

Purpose: the required lane currently reports "scoped run … floor not evaluated" — a green that never
checked whether the suite still runs what it claims to run. Enabling the floor without guarding the
env line would reproduce exactly the silent-disable failure mode the guard exists to prevent (D-08).

Output: one `env:` line in `ci.yml` and a mirrored guard trio in
`test/scripts/lane_classification_drift_test.exs`, landed in one commit.
</objective>

<toolchain_precondition>
Every `<automated>` command below already carries
`export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27`. The tracked
`.tool-versions` pins versions that are not installed locally; bare `mix` fails with a misleading
`corrupt atom table`. Do not "fix" `.tool-versions` — it is the correct CI contract.
</toolchain_precondition>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-CONTEXT.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-RESEARCH.md
@.planning/phases/166-earned-greens-and-controls-that-can-pass/166-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demonstrate the unfiltered suite-floor run is green locally before touching CI (D-10)</name>
  <files>none — measurement-only gate; no tracked file is modified</files>
  <read_first>
    - `test/support/suite_floor.ex` — the executed floors, the skipped ceiling, the known exclusion
      tag list, the growth nudge that is a warning rather than a failure, and the runtime env read.
      Locate each with `grep -n '@executed_floors\|@skipped_ceiling\|@known_exclusion_tags\|MAILGLASS_SUITE_FLOOR' test/support/suite_floor.ex`.
    - `.github/workflows/ci.yml` — the `core_deterministic_suite:` job and its
      `Run deterministic core suite` step; note it runs schema `public` with no CLI `--exclude`, so the
      effective exclusion set is exactly `test/test_helper.exs`'s base list.
    - `test/test_helper.exs` — the base exclusion list, to confirm every member is in
      `@known_exclusion_tags`.
  </read_first>
  <action>
    D-10 names the residual risk precisely: unlike the advisory legs, this lane does NOT exclude
    `:requires_workspace`, so the `already_shared` and `formatter_violations` tallies will be measured
    over a population the floor has never been enforced against.

    Run the unfiltered command locally first and capture the full suite-floor report block from the
    output: the executed count, the skipped count, the `already_shared` tally, the
    `formatter_violations` tally, and the printed scope line. Compare the observed executed count
    against the committed executed floors and the observed skipped count against the committed skipped
    ceiling, and record both comparisons in the SUMMARY.

    If the run reds on a sandbox-ownership signature (the known `already_shared` shape), STOP and fix
    the cause. Re-pinning the floors, lowering the ceiling, or adding a CLI `--exclude` are all
    prohibited diffs for this requirement — surface the blocker instead of relaxing the control.

    Make no file edits in this task. It is a measurement gate for Task 2.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && MAILGLASS_SUITE_FLOOR=1 mix test --warnings-as-errors 2>&1 | tail -40</automated>
    <fails_when>non-zero exit, or the output contains "floor not evaluated", or the ExUnit summary reports any failure count above 0, or the suite-floor report block is absent from the output</fails_when>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && MAILGLASS_SUITE_FLOOR=1 mix test --warnings-as-errors 2>&1 | grep -c 'FULL SUITE'</automated>
    <fails_when>stdout prints 0 — the full-suite scope line was never emitted, meaning the env var did not take effect</fails_when>
  </verify>
  <acceptance_criteria>
    - The run exits 0 with 0 failures under `MAILGLASS_SUITE_FLOOR=1`.
    - The output contains a scope line naming FULL SUITE and the `MAILGLASS_SUITE_FLOOR=1` value.
    - The observed executed count is greater than or equal to both committed executed floors, and the
      observed skipped count is less than or equal to the committed skipped ceiling; both numbers are
      recorded verbatim in the SUMMARY.
    - `git status --porcelain` shows no modified tracked files from this task.
  </acceptance_criteria>
  <done>The unfiltered floor-enforced run is proven green locally at today's committed constants, with the measured numbers recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Set MAILGLASS_SUITE_FLOOR in ci.yml and extend the occurrence guard to ci.yml — one commit</name>
  <files>.github/workflows/ci.yml, test/scripts/lane_classification_drift_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` — the `Run deterministic core suite` step's existing `env:` block
      (it already holds the timeout-evidence path and command entries). Locate with
      `grep -n 'Run deterministic core suite' .github/workflows/ci.yml`.
    - `test/scripts/lane_classification_drift_test.exs` — the module attributes near the top
      (`@repo_root`, `@ci_yml_path` — ALREADY DECLARED, reuse it; `@advisory_matrix_path`,
      `@suite_floor_env_entry`, `@suite_floor_env_occurrences`), the three-test `describe` block
      guarding the advisory matrix, and the `count_suite_floor_env_entries/1` helper at the bottom of
      the module. Locate each with `grep -n '@ci_yml_path\|@suite_floor_env\|count_suite_floor_env_entries\|describe "advisory-matrix' test/scripts/lane_classification_drift_test.exs`.
    - `166-PATTERNS.md` section on `test/scripts/lane_classification_drift_test.exs` — the trio quoted
      verbatim; this is the exact template to mirror.
  </read_first>
  <action>
    Both halves land in ONE commit (D-38): the guard asserts an occurrence count that only becomes
    correct once the env line exists, and the env line is only protected once the guard exists.

    (a) `.github/workflows/ci.yml`: add `MAILGLASS_SUITE_FLOOR: "1"` as an entry in the existing `env:`
    block of the `Run deterministic core suite` step inside the `core_deterministic_suite:` job. Use
    that exact literal, including the quotes around the 1 — `test/support/suite_floor.ex` compares
    against the string `"1"` exactly, and `@suite_floor_env_entry` counts the literal. Add it nowhere
    else in the file; the new guard's expected count is 1.

    (b) `test/scripts/lane_classification_drift_test.exs`: add a new module attribute
    `@ci_yml_suite_floor_occurrences 1` with a comment explaining it counts the single
    `core_deterministic_suite` full-suite step. Reuse the module's existing `@ci_yml_path` attribute —
    confirm by grep that it is already declared and do not redeclare it. Reuse the existing
    `@suite_floor_env_entry` literal (it is file-agnostic). Either parameterize
    `count_suite_floor_env_entries/1` to accept a path, or add a sibling
    `count_ci_yml_suite_floor_env_entries/1` that reads `@ci_yml_path` by default — whichever keeps
    the existing advisory-matrix tests byte-identical in behavior.

    Add a new `describe` block for the ci.yml guard containing the same three tests as the
    advisory-matrix trio: (1) the occurrence-count assertion against
    `@ci_yml_suite_floor_occurrences`; (2) an anti-vacuity test asserting the parsed `ci.yml` source is
    non-empty AND contains `@suite_floor_env_entry` at all; (3) a negative-control test that removes
    one occurrence from the in-memory source with a non-global replace and asserts the counting
    function reports one fewer.

    Do NOT touch `@suite_floor_env_occurrences` (D-07): it counts `advisory-matrix.yml` only, and a
    bare 2-to-3 bump would red an unchanged file. Do not touch `test/support/suite_floor.ex` at all.
  </action>
  <verify>
    <automated>export ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.18.4-otp-27 && cd /Users/jon/projects/mailglass && mix test test/scripts/lane_classification_drift_test.exs</automated>
    <fails_when>non-zero exit, or any failure count above 0 in the ExUnit summary</fails_when>
    <automated>cd /Users/jon/projects/mailglass && grep -c 'MAILGLASS_SUITE_FLOOR: "1"' .github/workflows/ci.yml</automated>
    <fails_when>stdout prints any number other than 1 — 0 means the env line is missing, more than 1 means the new occurrence guard's expected count is wrong</fails_when>
    <automated>cd /Users/jon/projects/mailglass && grep -v '^ *#' test/scripts/lane_classification_drift_test.exs | grep -c '@suite_floor_env_occurrences 2'</automated>
    <fails_when>stdout prints 0 — the advisory-matrix constant was changed, which D-07 prohibits</fails_when>
    <automated>cd /Users/jon/projects/mailglass && actionlint .github/workflows/ci.yml</automated>
    <fails_when>non-zero exit, or any diagnostic line printed referencing the `core_deterministic_suite` job</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/scripts/lane_classification_drift_test.exs` defines `@ci_yml_suite_floor_occurrences` and
      contains three new tests: an occurrence-count assertion, an anti-vacuity test, and a
      negative-control test, all targeting `ci.yml`.
    - `@suite_floor_env_occurrences` is still literally 2 in that file.
    - `MAILGLASS_SUITE_FLOOR: "1"` appears exactly once in `.github/workflows/ci.yml`, inside the
      `Run deterministic core suite` step's `env:` block.
    - `git show --stat --pretty=format: <this task's commit sha>` lists exactly
      `.github/workflows/ci.yml` and `test/scripts/lane_classification_drift_test.exs`.
    - `test/support/suite_floor.ex` is unmodified (`git diff --quiet -- test/support/suite_floor.ex`
      exits 0).
  </acceptance_criteria>
  <done>The required deterministic lane enforces its own anti-vacuity floor, and deleting the env line is now caught by a mirrored occurrence guard — both in one commit.</done>
  <reversibility rating="reversible">Removing the env line and the new guard block restores prior behavior; no published artifact is affected.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow env to test harness | `MAILGLASS_SUITE_FLOOR` crosses from workflow configuration into the enforcement path in `test/support/suite_floor.ex` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-04 | Tampering | silent deletion of the `MAILGLASS_SUITE_FLOOR` env line from `ci.yml` | high | mitigate | The new `@ci_yml_suite_floor_occurrences` guard plus its anti-vacuity and negative-control tests make deletion a deterministic red rather than a silent return to an unenforced floor. |
| T-166-05 | Tampering | weakening enforcement by re-pinning `@executed_floors` or lowering `@skipped_ceiling` instead of fixing a red | high | mitigate | Named as a plan prohibition; Task 1 measures the real numbers first so any later re-pin is visibly unjustified in diff, and `test/support/suite_floor.ex` is asserted unmodified. |
| T-166-06 | Elevation of Privilege | new workflow env entry | low | accept | An env var name/value pair in an existing step grants no new permission and reads no secret; the job's `permissions:` block is untouched. |
| T-166-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. 166-RESEARCH.md § Package Legitimacy Audit covers the phase's only dependency addition (`excoveralls`, plan 166-01) and rates it OK with no `[ASSUMED]`/`[SUS]` entries, so no blocking human legitimacy checkpoint is required here. |
</threat_model>

<verification>
- `mix test test/scripts/lane_classification_drift_test.exs` green.
- `MAILGLASS_SUITE_FLOOR=1 mix test --warnings-as-errors` green locally, unfiltered.
- `actionlint .github/workflows/ci.yml` clean.
- In the PR's own CI run, the `Core Deterministic Suite` lane log prints the FULL SUITE scope line —
  this is milestone exit criterion 2 and is observable pre-merge.
- Every diff is auditable as "made a control able to reach its own pass state": an unevaluated floor
  becomes an evaluated one, and a deletable env line becomes a guarded one. Nothing is relaxed.

## Post-Merge Verification (166-VALIDATION.md Manual-Only rows owned here)

None. GREEN-03's acceptance (the lane's printed scope line) is visible in this PR's own required-lane
run. Record that log line and the run URL in the SUMMARY.
</verification>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New symbols**
- `test/scripts/lane_classification_drift_test.exs`: module attribute
  `@ci_yml_suite_floor_occurrences` (value 1); a new `describe` block with three tests (occurrence
  assertion, anti-vacuity, negative control); either a parameterized
  `count_suite_floor_env_entries/1` or a new sibling counting helper reading `@ci_yml_path`.

**New configuration**
- `.github/workflows/ci.yml`: env entry `MAILGLASS_SUITE_FLOOR: "1"` in the
  `Run deterministic core suite` step of the `core_deterministic_suite` job.

**Unchanged by design**
- `test/support/suite_floor.ex` (no code change; the env var it already reads is simply now set).
- `@suite_floor_env_occurrences` stays 2.
</artifacts_this_phase_produces>

<flagged_assumptions>
- No SPEC.md exists, so edge predicates were authored from the deterministic probe report. Two
  GREEN-03 edges (`empty`, `ordering`) are recorded as `backstop` truths: a verifier that cannot
  confirm them with explicit evidence must abstain (`human_needed`, reason `insufficient_spec`),
  never silently pass.
- D-09 predicts no constant re-pinning is needed. Task 1 is the measurement that confirms or refutes
  it before any CI edit; a refutation is a blocker to surface, not a licence to re-pin.
</flagged_assumptions>

<success_criteria>
- GREEN-03: the required `core_deterministic_suite` lane prints the FULL SUITE scope line, and
  `lane_classification_drift_test.exs` is green in the same change.
- The env line is protected by an occurrence guard mirroring the advisory-matrix trio.
- No floor constant re-pinned, no exclusion added, no gate relaxed.
</success_criteria>

<output>
Create `.planning/phases/166-earned-greens-and-controls-that-can-pass/166-02-SUMMARY.md` when done.
</output>
