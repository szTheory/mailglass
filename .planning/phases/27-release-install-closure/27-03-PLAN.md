---
phase: 27-release-install-closure
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/MILESTONE-ARC.md
autonomous: true
requirements: [REL-17, REL-18]
must_haves:
  truths:
    - "Active planning state no longer describes Issue #25 / Issue #9 as carry-forward gaps; they appear only as historical evidence in archived locations (D-27-08)."
    - "REL-17 and REL-18 checkboxes in REQUIREMENTS.md flip to `[x]` and traceability rows show `Complete` (D-27-08)."
    - "PROJECT.md's `Active` checklist no longer lists `REL-17` / `REL-18` as open items."
    - "STATE.md's `Carry-forward to next milestone` block is rewritten so Issue #25 and Issue #9 are described as resolved-in-Phase-27, not as outstanding work."
    - "All four edits respect D-27-09: NO file under `.planning/milestones/v0.3-phases/**` is touched (those archived artifacts remain as historical evidence)."
    - "MILESTONE-ARC.md line 90 phrases the Issue #25 / Issue #9 carry-forward in past tense (closed in Phase 27) rather than as live milestone work."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REL-17 / REL-18 checkboxes flipped to `[x]`; traceability rows updated to `Complete`."
      contains: "REL-17"
    - path: ".planning/PROJECT.md"
      provides: "Line 124 checkbox for `REL-17` / `REL-18` flipped to `[x]`; line 73 milestone-goal line preserved (it's a goal statement, not an open todo)."
      contains: "REL-17"
    - path: ".planning/STATE.md"
      provides: "Carry-forward block rewritten so Issues #25/#9 are resolved-in-Phase-27 rather than active gaps."
      contains: "Issue #25"
    - path: ".planning/MILESTONE-ARC.md"
      provides: "Line 90 reframed in past tense (closed in Phase 27); the carry-forward bullet stays as historical record but is no longer an open item."
      contains: "Issue #25"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "REL-17 / REL-18 status column"
      via: "Status flip from `Pending` to `Complete`"
      pattern: "REL-1[78].*Complete"
    - from: ".planning/STATE.md Carry-forward to next milestone block"
      to: "post-Phase-27 closure narrative"
      via: "rewrite Issues #25/#9 entries"
      pattern: "Issue #(25|9).*Phase 27|resolved"
---

<objective>
Close the documentation half of Phase 27 per D-27-08 by updating four
active-state planning artifacts so Issue #25 and Issue #9 are no longer
described as carry-forward gaps in v0.4. Per D-27-09, do NOT touch any
archived milestone artifact under `.planning/milestones/v0.3-phases/**` —
those remain as historical evidence of the prior workaround.

Per RESEARCH.md §C1, the four files with active-state references are:
1. `.planning/STATE.md` (lines 66-69 carry-forward block + line 78 parenthetical)
2. `.planning/REQUIREMENTS.md` (lines 36-37 checkboxes + lines 84-85 traceability rows)
3. `.planning/PROJECT.md` (line 124 active checklist entry; line 73 is a milestone-goal statement and stays)
4. `.planning/MILESTONE-ARC.md` (line 90 — reframe in past tense)

This plan does not touch any code, workflow, or archived doc.

Purpose: D-27-08 explicitly requires "updating current planning/state
artifacts so the manual `workflow_dispatch tag=...` workaround is recorded
as historical evidence, not as an active requirement for shipping." This
plan operationalizes that closure step.

Output: Four cleanly-edited active-state planning files; no changes
elsewhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONE-ARC.md
@.planning/phases/27-release-install-closure/27-CONTEXT.md
@.planning/phases/27-release-install-closure/27-RESEARCH.md
@.planning/phases/27-release-install-closure/27-VALIDATION.md
@.planning/phases/27-release-install-closure/27-01-PLAN.md
@.planning/phases/27-release-install-closure/27-02-PLAN.md
@./CLAUDE.md

<interfaces>
Current active-state references to Issue #25 / Issue #9 (extracted per RESEARCH.md C1).

STATE.md lines 66-69, current "Carry-forward to next milestone" block:

```
### Carry-forward to next milestone

- **Issue #25** — post-publish-smoke fresh-host install crashes on missing `:hackney` (Swoosh ApiClient default in Phoenix 1.8). Recommended fix: `mix mailglass.install` writes `config :swoosh, :api_client, false` (or Finch). v0.4 candidate.
- **Issue #9** — chronic post-publish-smoke version-resolution bug. Sidestepped here via `workflow_dispatch tag=mailglass-v0.3.2`; structural fix still pending.
```

STATE.md line 78 (current parenthetical):

```
- Phase 18 (Ship v0.3.x) complete 2026-04-29 — shipped as v0.3.2 after 3-cycle CI recovery (PRs #20, #22 → #21 / 0.3.1 orphan; #23 → #24 / 0.3.2 shipped). DELIV-04 marked Complete; smoke contract gap (Issue #25) tracked for v0.4.
```

REQUIREMENTS.md lines 36-37 (current checkboxes):

```
- [ ] **REL-17**: Fresh-host install no longer crashes on missing `:hackney` / Swoosh API client defaults (`Issue #25`).
- [ ] **REL-18**: Post-publish smoke no longer depends on the current manual version-resolution workaround (`Issue #9`).
```

REQUIREMENTS.md lines 84-85 (current traceability rows):

```
| REL-17 | Phase 27 | Pending |
| REL-18 | Phase 27 | Pending |
```

PROJECT.md line 124 (current Active checklist entry):

```
- [ ] `REL-17` / `REL-18` — fresh-host install and post-publish smoke closure
```

PROJECT.md line 73 (current milestone goal — STAYS UNTOUCHED, it's a
historical goal statement, not an open todo):

```
- Release/install closure for `Issue #25` and `Issue #9`
```

MILESTONE-ARC.md line 90 (current — reframe in past tense):

```
- Carry-forward ship/install fixes from `Issue #25` and `Issue #9`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip REL-17 / REL-18 checkboxes and traceability rows in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — confirm current line numbers for REL-17 / REL-18 checkboxes lines 36-37 and traceability rows lines 84-85)
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§C1 — closure-edit list)
    - .planning/phases/27-release-install-closure/27-CONTEXT.md (D-27-08 — closure includes updating current state artifacts)
  </read_first>
  <action>
    Make exactly two edits in `.planning/REQUIREMENTS.md`.

    Edit 1 — lines 36-37, flip checkboxes from `[ ]` to `[x]`. Replace these
    two lines verbatim:

    ```markdown
    - [ ] **REL-17**: Fresh-host install no longer crashes on missing `:hackney` / Swoosh API client defaults (`Issue #25`).
    - [ ] **REL-18**: Post-publish smoke no longer depends on the current manual version-resolution workaround (`Issue #9`).
    ```

    With:

    ```markdown
    - [x] **REL-17**: Fresh-host install no longer crashes on missing `:hackney` / Swoosh API client defaults (closed in Phase 27; previously tracked as `Issue #25`).
    - [x] **REL-18**: Post-publish smoke no longer depends on the prior manual version-resolution workaround (closed in Phase 27; previously tracked as `Issue #9`).
    ```

    Edit 2 — traceability rows for REL-17 / REL-18 (lines 84-85). Replace
    these two lines verbatim:

    ```markdown
    | REL-17 | Phase 27 | Pending |
    | REL-18 | Phase 27 | Pending |
    ```

    With:

    ```markdown
    | REL-17 | Phase 27 | Complete |
    | REL-18 | Phase 27 | Complete |
    ```

    Do NOT change any other line in the file. Do NOT renumber sections. Do NOT
    edit any other requirement's checkbox or status column.
  </action>
  <verify>
    <automated>bash -c 'A=$(grep -c "^- \[x\] \*\*REL-1[78]\*\*" .planning/REQUIREMENTS.md); B=$(grep -cE "^\| REL-1[78] \| Phase 27 \| Complete \|" .planning/REQUIREMENTS.md); test "$A" = "2" -a "$B" = "2" && echo OK || echo "FAIL A=$A B=$B"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*REL-17\*\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "^- \[x\] \*\*REL-18\*\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -cE "^\| REL-17 \| Phase 27 \| Complete \|" .planning/REQUIREMENTS.md` returns 1.
    - `grep -cE "^\| REL-18 \| Phase 27 \| Complete \|" .planning/REQUIREMENTS.md` returns 1.
    - `grep -cE "^\| REL-1[78] \| Phase 27 \| Pending \|" .planning/REQUIREMENTS.md` returns 0.
    - `grep -c "^- \[ \] \*\*REL-1[78]\*\*" .planning/REQUIREMENTS.md` returns 0.
    - `git diff --stat .planning/REQUIREMENTS.md` shows no more than 4 line changes (2 checkbox flips + 2 traceability flips, no incidental edits).
  </acceptance_criteria>
  <done>Both REL-17 and REL-18 checkboxes show `[x]`, both traceability rows show `Complete`, no other rows touched.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the REL-17 / REL-18 active checklist entry in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 115-130 — confirm the `Active` checklist structure and current line 124 entry; also confirm line 73 is inside the `Current Milestone: v0.4 Operator Confidence > Target features` block, NOT inside the Active checklist)
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§C1 — explicitly says line 73 is a milestone goal that may stay untouched)
  </read_first>
  <action>
    Make exactly ONE edit in `.planning/PROJECT.md`.

    Edit — line 124 (Active checklist entry). Replace this single line verbatim:

    ```markdown
    - [ ] `REL-17` / `REL-18` — fresh-host install and post-publish smoke closure
    ```

    With:

    ```markdown
    - [x] `REL-17` / `REL-18` — fresh-host install and post-publish smoke closure (Phase 27)
    ```

    Do NOT touch line 73 (the milestone-goal bullet `- Release/install closure
    for ... Issue #25 and Issue #9`); per RESEARCH.md §C1 it documents the
    milestone goal, which remains true. Do NOT touch any other line.

    Do NOT add new sections. Do NOT reorder the checklist.
  </action>
  <verify>
    <automated>bash -c 'A=$(grep -c "^- \[x\] \`REL-17\` / \`REL-18\`" .planning/PROJECT.md); B=$(grep -c "^- \[ \] \`REL-17\` / \`REL-18\`" .planning/PROJECT.md); test "$A" = "1" -a "$B" = "0" && echo OK || echo "FAIL A=$A B=$B"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \`REL-17\` / \`REL-18\`" .planning/PROJECT.md` returns 1.
    - `grep -c "^- \[ \] \`REL-17\` / \`REL-18\`" .planning/PROJECT.md` returns 0.
    - `grep -c "Release/install closure for \`Issue #25\` and \`Issue #9\`" .planning/PROJECT.md` returns 1 (line 73 milestone goal preserved).
    - `git diff --stat .planning/PROJECT.md` shows exactly 1 changed line (the Active checklist flip + the appended `(Phase 27)` annotation).
  </acceptance_criteria>
  <done>Active checklist line 124 flipped to `[x]` with `(Phase 27)` annotation; line 73 milestone-goal preserved; no other edits.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite STATE.md carry-forward block and Phase 18 parenthetical</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full file — confirm current carry-forward block at lines 66-69 and Phase 18 parenthetical at line 78)
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§C1 — explicit closure-edit guidance)
    - .planning/phases/27-release-install-closure/27-CONTEXT.md (D-27-08 — workaround should appear as historical evidence, not active requirement)
  </read_first>
  <action>
    Make exactly TWO edits in `.planning/STATE.md`.

    Edit 1 — Carry-forward block (lines 66-69). Replace these four lines verbatim:

    ```markdown
    ### Carry-forward to next milestone

    - **Issue #25** — post-publish-smoke fresh-host install crashes on missing `:hackney` (Swoosh ApiClient default in Phoenix 1.8). Recommended fix: `mix mailglass.install` writes `config :swoosh, :api_client, false` (or Finch). v0.4 candidate.
    - **Issue #9** — chronic post-publish-smoke version-resolution bug. Sidestepped here via `workflow_dispatch tag=mailglass-v0.3.2`; structural fix still pending.
    ```

    With:

    ```markdown
    ### Carry-forward closure (resolved in Phase 27)

    - **Issue #25** — RESOLVED in Phase 27 (REL-17). The installer now writes `config :swoosh, :api_client, false` so a fresh `mix phx.new --no-mailer` host boots without `:finch` in deps. See `.planning/phases/27-release-install-closure/27-01-PLAN.md` and the historical narrative archived under `.planning/milestones/v0.3-phases/18-ship-v0-3-0/`.
    - **Issue #9** — RESOLVED in Phase 27 (REL-18). Workflow comments hardened so the canonical release-event path and fallback `workflow_dispatch tag=...` recovery path are explicitly documented in `.github/workflows/post-publish-smoke.yml` and `.github/workflows/publish-hex.yml`. Rehearsal evidence: `.planning/phases/27-release-install-closure/27-02-EVIDENCE.md`.
    ```

    Edit 2 — Phase 18 parenthetical (line 78). Replace this single line verbatim:

    ```markdown
    - Phase 18 (Ship v0.3.x) complete 2026-04-29 — shipped as v0.3.2 after 3-cycle CI recovery (PRs #20, #22 → #21 / 0.3.1 orphan; #23 → #24 / 0.3.2 shipped). DELIV-04 marked Complete; smoke contract gap (Issue #25) tracked for v0.4.
    ```

    With:

    ```markdown
    - Phase 18 (Ship v0.3.x) complete 2026-04-29 — shipped as v0.3.2 after 3-cycle CI recovery (PRs #20, #22 → #21 / 0.3.1 orphan; #23 → #24 / 0.3.2 shipped). DELIV-04 marked Complete; smoke contract gap (Issue #25) was carried forward to v0.4 and closed in Phase 27 (REL-17).
    ```

    Do NOT change any other line in STATE.md. Do NOT touch the frontmatter.
    Do NOT touch the Architectural Decisions list, Blockers, Deferred Items
    table, Performance Metrics, or any other section.
  </action>
  <verify>
    <automated>bash -c 'A=$(grep -c "Carry-forward closure (resolved in Phase 27)" .planning/STATE.md); B=$(grep -c "RESOLVED in Phase 27 (REL-17)" .planning/STATE.md); C=$(grep -c "RESOLVED in Phase 27 (REL-18)" .planning/STATE.md); D=$(grep -c "closed in Phase 27 (REL-17)" .planning/STATE.md); test "$A" = "1" -a "$B" = "1" -a "$C" = "1" -a "$D" = "1" && echo OK || echo "FAIL A=$A B=$B C=$C D=$D"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Carry-forward to next milestone" .planning/STATE.md` returns 0 (heading rewritten).
    - `grep -c "Carry-forward closure (resolved in Phase 27)" .planning/STATE.md` returns 1.
    - `grep -c "RESOLVED in Phase 27 (REL-17)" .planning/STATE.md` returns 1.
    - `grep -c "RESOLVED in Phase 27 (REL-18)" .planning/STATE.md` returns 1.
    - `grep -c "structural fix still pending" .planning/STATE.md` returns 0 (old framing removed).
    - `grep -c "v0.4 candidate" .planning/STATE.md` returns 0 (Issue #25 no longer marked as v0.4 candidate).
    - `grep -c "smoke contract gap (Issue #25) tracked for v0.4" .planning/STATE.md` returns 0 (parenthetical updated).
    - `grep -c "closed in Phase 27 (REL-17)" .planning/STATE.md` returns 1.
    - Phase 18 narrative still mentions PRs #20/#21/#22/#23/#24 (no incidental edits): `grep -c "PRs #20, #22 → #21 / 0.3.1 orphan; #23 → #24 / 0.3.2 shipped" .planning/STATE.md` returns 1.
  </acceptance_criteria>
  <done>Carry-forward block heading + bullets rewritten; Phase 18 parenthetical reframed; no incidental edits to other sections.</done>
</task>

<task type="auto">
  <name>Task 4: Reframe MILESTONE-ARC.md line 90 in past tense</name>
  <files>.planning/MILESTONE-ARC.md</files>
  <read_first>
    - .planning/MILESTONE-ARC.md (lines 75-105 — confirm the v0.4 milestone arc context around line 90)
    - .planning/phases/27-release-install-closure/27-RESEARCH.md (§C1 — line 90 already past-tense framing acceptable; review during Phase 27 closure)
  </read_first>
  <action>
    Make exactly ONE edit in `.planning/MILESTONE-ARC.md`.

    Edit — line 90 (the carry-forward bullet inside the v0.4 milestone arc).
    Replace this single line verbatim:

    ```markdown
    - Carry-forward ship/install fixes from `Issue #25` and `Issue #9`
    ```

    With:

    ```markdown
    - Carry-forward ship/install fixes from `Issue #25` and `Issue #9` (closed in Phase 27 — see REL-17/REL-18)
    ```

    This preserves the carry-forward bullet as historical record (per
    RESEARCH.md §C1 the line was already acceptable past-tense framing) but
    annotates it with the closure pointer so a reader scanning the v0.4 arc
    sees the resolution inline.

    Do NOT change ANY other line. Do NOT change the milestone goal text or
    surrounding bullets.
  </action>
  <verify>
    <automated>bash -c 'A=$(grep -c "Carry-forward ship/install fixes from \`Issue #25\` and \`Issue #9\` (closed in Phase 27 — see REL-17/REL-18)" .planning/MILESTONE-ARC.md); B=$(grep -c "^- Carry-forward ship/install fixes from \`Issue #25\` and \`Issue #9\`$" .planning/MILESTONE-ARC.md); test "$A" = "1" -a "$B" = "0" && echo OK || echo "FAIL A=$A B=$B"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(closed in Phase 27 — see REL-17/REL-18)" .planning/MILESTONE-ARC.md` returns 1.
    - `grep -c "^- Carry-forward ship/install fixes from \`Issue #25\` and \`Issue #9\`$" .planning/MILESTONE-ARC.md` returns 0 (the original un-annotated bullet is gone).
    - `grep -c "^- Carry-forward ship/install fixes from \`Issue #25\` and \`Issue #9\`" .planning/MILESTONE-ARC.md` returns 1 (the annotated form remains).
    - `git diff --stat .planning/MILESTONE-ARC.md` shows exactly 1 changed line.
  </acceptance_criteria>
  <done>Line 90 annotated with closure pointer; no other edits.</done>
</task>

<task type="auto">
  <name>Task 5: Final-state guard — verify no active-state file describes Issues #25/#9 as carry-forward gaps</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/MILESTONE-ARC.md</files>
  <read_first>
    - .planning/STATE.md (post-edit state)
    - .planning/REQUIREMENTS.md (post-edit state)
    - .planning/PROJECT.md (post-edit state)
    - .planning/MILESTONE-ARC.md (post-edit state)
    - .planning/phases/27-release-install-closure/27-VALIDATION.md (Per-Task Verification Map — last row's grep gate is the canonical D-27-08 check)
  </read_first>
  <action>
    Run a final-state guard that proves D-27-08 + D-27-09 are honored:

    1. Confirm NO active-state file (the four files this plan edits) frames
       Issue #25 or Issue #9 as an outstanding carry-forward gap. The text
       MAY still mention these issue numbers — but only inside historical /
       resolved / closed framing. Run:

       ```sh
       # Should match only the new "RESOLVED" / "closed in Phase 27" annotations
       grep -nE 'Issue #25|Issue #9' .planning/STATE.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/MILESTONE-ARC.md
       ```

       Inspect each match. Every match MUST be inside one of:
       - "RESOLVED in Phase 27 ..."
       - "closed in Phase 27 ..."
       - "previously tracked as `Issue #...`"
       - "Release/install closure for `Issue #25` and `Issue #9`" (PROJECT.md
         line 73 milestone goal — historical statement, allowed).

       Flag any match that frames either issue as still-active (e.g. "tracked
       for v0.4", "structural fix still pending", "v0.4 candidate", "[ ]"
       checkbox-form mention).

    2. Confirm D-27-09: NO file under `.planning/milestones/v0.3-phases/**`
       was edited:

       ```sh
       git diff --stat .planning/milestones/v0.3-phases/ | wc -l | tr -d ' '
       ```
       Must return 0.

    3. Confirm REQUIREMENTS.md traceability table is internally consistent:

       ```sh
       grep -cE "^\| REL-1[78] \| Phase 27 \| (Pending|Complete) \|" .planning/REQUIREMENTS.md
       ```
       Must return 2, AND the `Pending` count must be 0:

       ```sh
       grep -cE "^\| REL-1[78] \| Phase 27 \| Pending \|" .planning/REQUIREMENTS.md
       ```
       Must return 0.

    Do NOT modify any file in this task — it is a read-only verification
    pass. If any check fails, STOP and revise the relevant prior task before
    declaring this plan done.
  </action>
  <verify>
    <automated>bash -c 'PENDING=$(grep -cE "^\| REL-1[78] \| Phase 27 \| Pending \|" .planning/REQUIREMENTS.md); ARCHIVED_DIFF=$(git diff --stat .planning/milestones/v0.3-phases/ | wc -l | tr -d " "); BAD_FRAMING=$(grep -cE "v0.4 candidate|structural fix still pending|tracked for v0.4" .planning/STATE.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/MILESTONE-ARC.md); test "$PENDING" = "0" -a "$ARCHIVED_DIFF" = "0" -a "$BAD_FRAMING" = "0" && echo OK || echo "FAIL PENDING=$PENDING ARCHIVED_DIFF=$ARCHIVED_DIFF BAD_FRAMING=$BAD_FRAMING"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| REL-1[78] \| Phase 27 \| Pending \|" .planning/REQUIREMENTS.md` returns 0.
    - `grep -cE "v0.4 candidate|structural fix still pending|tracked for v0.4" .planning/STATE.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/MILESTONE-ARC.md` returns 0 (no active-gap framings remain anywhere in the four edited files).
    - `git diff --stat .planning/milestones/v0.3-phases/ | wc -l | tr -d ' '` returns 0 (D-27-09: no archived-milestone edits).
    - Every `Issue #25` / `Issue #9` mention in the four edited files is inside one of the allowed historical/resolved/closed framings (manual review during execution; document the per-line audit in 27-03-SUMMARY.md).
  </acceptance_criteria>
  <done>All four files report consistent post-Phase-27 state; archived milestone artifacts untouched; verification commands all green.</done>
</task>

</tasks>

<verification>
- `grep -c "^- \[x\] \*\*REL-17\*\*" .planning/REQUIREMENTS.md` returns 1.
- `grep -c "^- \[x\] \*\*REL-18\*\*" .planning/REQUIREMENTS.md` returns 1.
- `grep -cE "^\| REL-1[78] \| Phase 27 \| Complete \|" .planning/REQUIREMENTS.md` returns 2.
- `grep -cE "^\| REL-1[78] \| Phase 27 \| Pending \|" .planning/REQUIREMENTS.md` returns 0.
- `grep -c "Carry-forward closure (resolved in Phase 27)" .planning/STATE.md` returns 1.
- `grep -c "RESOLVED in Phase 27 (REL-17)" .planning/STATE.md` returns 1 and `(REL-18)` likewise returns 1.
- `grep -c "(closed in Phase 27 — see REL-17/REL-18)" .planning/MILESTONE-ARC.md` returns 1.
- `grep -cE "v0.4 candidate|structural fix still pending|tracked for v0.4" .planning/STATE.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/MILESTONE-ARC.md` returns 0.
- `git diff --stat .planning/milestones/v0.3-phases/ | wc -l | tr -d ' '` returns 0 (D-27-09: archived artifacts untouched).
- Use Conventional Commit prefix `docs(state):` for the resulting commit per CLAUDE.md (CI path filters skip docs(state) updates).
</verification>

<success_criteria>
- All four active-state planning artifacts (STATE.md, REQUIREMENTS.md,
  PROJECT.md, MILESTONE-ARC.md) describe Issue #25 / Issue #9 only as
  resolved-in-Phase-27 or as historical milestone goals; none frame them as
  outstanding carry-forward gaps.
- REL-17 and REL-18 checkboxes both `[x]`, both traceability rows `Complete`.
- D-27-09 honored: no edits under `.planning/milestones/v0.3-phases/**`.
- Final-state guard (Task 5) passes all three checks: PENDING=0,
  ARCHIVED_DIFF=0, BAD_FRAMING=0.
</success_criteria>

<output>
After completion, create
`.planning/phases/27-release-install-closure/27-03-SUMMARY.md` documenting:
- The exact diff hunks for each of the four edited files.
- The line-by-line audit from Task 5: every remaining `Issue #25` / `Issue #9`
  mention in the four files, plus the framing classification (RESOLVED /
  closed / historical-goal / previously-tracked).
- Confirmation that `git diff --stat .planning/milestones/v0.3-phases/`
  returned 0 lines (D-27-09).
- Conventional Commit prefix used: `docs(state):` per CLAUDE.md.
</output>
