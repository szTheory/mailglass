---
phase: "97"
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/preview/device_frame.ex
  - mailglass_admin/lib/mailglass_admin/preview/tabs.ex
  - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "All three device_frame buttons carry min-h-11 (44px) alongside btn-sm; aria-pressed not regressed"
    - "All four preview tab buttons carry focus-visible:ring-inset and aria-controls; content pane has role=tabpanel + aria-labelledby"
    - "HTML tab empty pane shows 'No HTML body' placeholder when @html_body is empty"
    - "sidebar summary and scenario link elements carry focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1"
    - "sidebar scenario active border changed from border-l-[3px] to border-l-2"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/preview/device_frame.ex"
      provides: "All three segmented control buttons with min-h-11"
      contains: "min-h-11"
    - path: "mailglass_admin/lib/mailglass_admin/preview/tabs.ex"
      provides: "Tab strip with aria-controls + tabpanel + focus rings + empty HTML pane"
      contains: "aria-controls"
    - path: "mailglass_admin/lib/mailglass_admin/preview/sidebar.ex"
      provides: "Focus rings on summary and scenario links; border-l-2 active scenario"
      contains: "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1"
  key_links:
    - from: "tabs.ex tab buttons"
      to: "tabpanel content divs"
      via: "aria-controls={tab-id} + role=tabpanel + aria-labelledby={tab-btn-id}"
      pattern: "aria-controls"
    - from: "sidebar.ex scenario_classes"
      to: "active scenario link"
      via: "border-l-2 border-primary"
      pattern: "border-l-2"
---

<objective>
Uplift the three Preview surface components: add `min-h-11` to device_frame segmented control buttons, add full ARIA tab panel contracts + focus rings + empty HTML placeholder to tabs, and add focus rings + border-l-2 fix to sidebar.

Purpose: Preview is a dev-only surface but these components still carry WCAG 2.4.7 focus-ring gaps and missing ARIA semantics (tabs need aria-controls + tabpanel). device_frame shares the `btn-sm` + `min-h-11` tension from the operator side. sidebar uses an arbitrary `border-l-[3px]` that the conformance gate would flag.
Output: Three modified preview component files; no bundle rebuild (plan 06).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key contracts from SUMMARY.md and 97-PATTERNS.md -->

STATE-LD-20 (device_frame): ADD min-h-11 to all three buttons alongside btn-sm. Current: class={["btn btn-sm join-item", button_classes(@device_width == 375)]}. After: class={["btn btn-sm min-h-11 join-item", ...]}. Verify compiled min-height. aria-pressed already implemented — do not regress.

STATE-LD-21 (tabs): ADD focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset to all four tab buttons. ADD id="tab-btn-{tab}" and aria-controls="tab-panel-{tab}" to each tab button. ADD id="tab-panel-{active_tab}" + role="tabpanel" + aria-labelledby="tab-btn-{active_tab}" to content div. ADD "No HTML body — this Mailable's template returned empty content." placeholder when @html_body == "". min-h-10 → min-h-11 on tab buttons (even dev-only, STATE-LD-21 decision specifies min-h-11 for the focus target; min-h-10 was the prior decision, but UI-SPEC §Accessibility row overrides to min-h-11 globally).

STATE-LD-22 (sidebar): ADD focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1 to <summary> and to <.link> elements. Change border-l-[3px] → border-l-2 in scenario_classes/4 (both active and inactive branches — both use border-l-[3px]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: device_frame — add min-h-11 to all three segmented control buttons</name>
  <files>mailglass_admin/lib/mailglass_admin/preview/device_frame.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/preview/device_frame.ex (read lines 26-58 — three button elements)
    - SUMMARY.md STATE-LD-20: "ADD min-h-11 to all three buttons alongside btn-sm. Phase 97 executor must verify min-h-11 overrides btn-sm in the compiled bundle."
    - mailglass_admin/priv/static/app.css — grep for .btn-sm min-height value
    - 97-PATTERNS.md section "device_frame min-h-11"
  </read_first>
  <action>
    Read device_frame.ex lines 26-58. Identify the three buttons (375, 768, 1024 widths, lines ~34, 43, 52).

    For each button, the class list is `class={["btn btn-sm join-item", button_classes(@device_width == 375)]}` (or similar). Add `min-h-11` to the static string: `class={["btn btn-sm min-h-11 join-item", button_classes(...)]}`.

    Apply to all three buttons.

    Check compiled CSS: grep app.css for the btn-sm min-height value. Same resolution rule as theme_toggle and evidence_card: if btn-sm overrides min-h-11 in the compiled output, drop `btn-sm` from all three buttons.

    Do NOT touch `aria-pressed`, `role`, `aria-label`, or `phx-click` on any button.
    Verify `role="group"` + `aria-label="Preview device width"` are still present on the wrapper container.
  </action>
  <verify>
    <automated>grep -c "min-h-11" mailglass_admin/lib/mailglass_admin/preview/device_frame.ex</automated>
    Expected: 3 (one per button).
    <automated>grep -c "aria-pressed" mailglass_admin/lib/mailglass_admin/preview/device_frame.ex</automated>
    Expected: ≥ 3 (not regressed).
  </verify>
  <done>All three device_frame buttons carry min-h-11 for 44px touch target. aria-pressed not regressed. Compiled min-height verified.</done>
</task>

<task type="auto">
  <name>Task 2: tabs — add ARIA tab contract, focus rings, empty HTML placeholder</name>
  <files>mailglass_admin/lib/mailglass_admin/preview/tabs.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/preview/tabs.ex (read lines 35-130 in full — tab buttons at 39-81, content div at ~84, tab_content/1 function variants)
    - SUMMARY.md STATE-LD-21: full ARIA spec including aria-controls, tabpanel, aria-labelledby, empty HTML body placeholder
    - 97-PATTERNS.md section "tabs focus ring and ARIA" for exact attribute placements and id naming pattern
  </read_first>
  <action>
    Read tabs.ex lines 35-130.

    For each of the four tab buttons (html, text, raw, headers — at lines ~45, 55, 65, 76):
      1. Add `id="tab-btn-{tab}"` (e.g., `id="tab-btn-html"`) to the button element.
      2. Add `aria-controls="tab-panel-{tab}"` (e.g., `aria-controls="tab-panel-html"`) to the button element.
      3. Add `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset` to the class list.
      4. Change `min-h-10` to `min-h-11` if present (STATE-LD-21 note in 97-PATTERNS.md: fix min-h-10 → min-h-11).
      5. Verify `role="tab"` is already present on each button; if not, add it.
      6. Verify `aria-selected={to_string(@active_tab == :html)}` (or the atom-specific equivalent) is present.

    On the content wrapper div (line ~84):
      1. Add `id={"tab-panel-" <> Atom.to_string(@active_tab)}` (or equivalent computed id).
      2. Add `role="tabpanel"`.
      3. Add `aria-labelledby={"tab-btn-" <> Atom.to_string(@active_tab)}`.
      4. If the div uses a hardcoded id like "preview-tab-...", rename to "tab-panel-..." to match aria-controls.

    In the `tab_content` function for the `:html` tab (lines ~107-120):
      Add a conditional: when `@html_body == ""` (or `@html_body == nil`), render instead of the iframe:
        A `<p>` with class `"text-body text-secondary py-lg text-center"` and text `"No HTML body — this Mailable's template returned empty content."`
      The iframe renders when `@html_body` is non-empty (existing behavior).

    Verify `motion-tab-swap` class is still present on the content wrapper (MOTION-LD-05).
  </action>
  <verify>
    <automated>grep -c "aria-controls" mailglass_admin/lib/mailglass_admin/preview/tabs.ex</automated>
    Expected: ≥ 4 (one per tab button).
    <automated>grep -c "role=\"tabpanel\"" mailglass_admin/lib/mailglass_admin/preview/tabs.ex</automated>
    Expected: ≥ 1.
    <automated>grep -c "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-inset" mailglass_admin/lib/mailglass_admin/preview/tabs.ex</automated>
    Expected: ≥ 4 (one per tab button).
    <automated>grep -c "No HTML body" mailglass_admin/lib/mailglass_admin/preview/tabs.ex</automated>
    Expected: 1 (empty HTML body placeholder copy).
    <automated>grep -c "motion-tab-swap" mailglass_admin/lib/mailglass_admin/preview/tabs.ex</automated>
    Expected: ≥ 1 (not regressed).
  </verify>
  <done>All four tab buttons have id, aria-controls, focus-visible:ring-inset, role=tab, aria-selected, min-h-11. Content div has id=tab-panel-*, role=tabpanel, aria-labelledby. Empty HTML body placeholder present. motion-tab-swap not regressed.</done>
</task>

<task type="auto">
  <name>Task 3: sidebar — focus rings on summary and link, border-l-[3px] → border-l-2</name>
  <files>mailglass_admin/lib/mailglass_admin/preview/sidebar.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex (read lines 40-152 — summary at ~75, scenario link at ~81, scenario_classes/4 private function)
    - SUMMARY.md STATE-LD-22: focus rings on summary and .link; border-l-[3px] → border-l-2 decision ("2px is closer to brand border-width discipline")
    - 97-PATTERNS.md section "sidebar focus ring and border-l assessment" for exact class string changes
  </read_first>
  <action>
    Read sidebar.ex lines 40-152.

    On the `<summary>` element (line ~75): add `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` to its class string. The existing classes are: `"flex items-center gap-2 px-3 py-2 min-h-11 text-body font-bold text-base-content cursor-pointer hover:bg-base-200 rounded transition-colors"`. Append the focus ring to this string.

    On the `<.link>` element (line ~81): add `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` to its static class string (not inside scenario_classes — the static string on the link element itself). The scenario_classes/4 function provides only the state-varying classes.

    In `scenario_classes/4` private function (both clauses): replace `border-l-[3px]` with `border-l-2` in BOTH clauses (active and inactive). The arbitrary `border-l-[3px]` value is banned by the conformance gate; `border-l-2` (2px) is the nearest Tailwind scale value and matches nav_link's `border-l-2` at shell.ex:207.

    Verify the `h1 "Mailers"` heading (sidebar.ex:43) is intact and uses appropriate token class.
    Verify `<details>/<summary>` HTML structure is preserved — do NOT replace with a custom accordion.
  </action>
  <verify>
    <automated>grep -c "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1" mailglass_admin/lib/mailglass_admin/preview/sidebar.ex</automated>
    Expected: ≥ 2 (one on summary, one on the .link element).
    <automated>grep -v "^#" mailglass_admin/lib/mailglass_admin/preview/sidebar.ex | grep -c "border-l-\[3px\]"</automated>
    Expected: 0 (arbitrary value removed from both scenario_classes clauses).
    <automated>grep -c "border-l-2" mailglass_admin/lib/mailglass_admin/preview/sidebar.ex</automated>
    Expected: ≥ 2 (active + inactive scenario_classes clauses).
    <automated>grep -c "<details\|<summary" mailglass_admin/lib/mailglass_admin/preview/sidebar.ex</automated>
    Expected: ≥ 2 (details/summary structure preserved).
  </verify>
  <done>summary element carries focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1. Scenario .link element carries the same focus ring. Both scenario_classes clauses use border-l-2 (border-l-[3px] absent). details/summary structure preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tabs iframe srcdoc | Preview iframe renders adopter email content. This plan does not change srcdoc handling — only adds a conditional empty state around it |
| sidebar .link patch target | scenario .link patches to adopter-defined Mailable/scenario paths. No change to href generation — only class strings modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05-01 | Tampering | tabs aria-controls id injection | accept | id values are derived from atom_to_string(@active_tab) — controlled atom set, not user input |
| T-97-05-02 | Information Disclosure | tabs empty HTML body placeholder | accept | Placeholder message is a static string; no adopter data is surfaced in the empty state |
| T-97-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure HEEx edits |
</threat_model>

<verification>
After all three tasks:

1. `grep -c "min-h-11" mailglass_admin/lib/mailglass_admin/preview/device_frame.ex` — must be 3
2. `grep -c "aria-controls" mailglass_admin/lib/mailglass_admin/preview/tabs.ex` — must be ≥ 4
3. `grep -c "role=\"tabpanel\"" mailglass_admin/lib/mailglass_admin/preview/tabs.ex` — must be ≥ 1
4. `grep -c "No HTML body" mailglass_admin/lib/mailglass_admin/preview/tabs.ex` — must be 1
5. `grep -c "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1" mailglass_admin/lib/mailglass_admin/preview/sidebar.ex` — must be ≥ 2
6. `grep -v "^#" mailglass_admin/lib/mailglass_admin/preview/sidebar.ex | grep -c "border-l-\[3px\]"` — must be 0
7. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5` — exit 0
</verification>

<success_criteria>
- device_frame: all three buttons carry min-h-11 at effective 44px; aria-pressed not regressed.
- tabs: four tab buttons have id, aria-controls, role=tab, focus ring, min-h-11; content div has role=tabpanel + aria-labelledby; HTML empty pane shows "No HTML body — this Mailable's template returned empty content." when @html_body is empty.
- sidebar: summary and scenario link elements carry focus-visible ring; border-l-[3px] replaced with border-l-2 in both scenario_classes clauses.
- mix compile --warnings-as-errors clean.
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-05-SUMMARY.md` when done.
</output>
