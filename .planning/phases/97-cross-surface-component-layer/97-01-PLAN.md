---
phase: "97"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "nav_link elements carry a visible focus ring in both themes at every width"
    - "nav_pill elements carry a visible focus ring in both themes at every width"
    - "theme_toggle min-h-11 (44px) is the effective compiled min-height"
    - "Orientation strip copy uses domain nouns: 'Delivery' not 'Email', 'Suppression list' not 'suppressions', 'InboundMessage' not 'Message'"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      provides: "Uplifted shell component — nav focus rings + theme_toggle touch target + orientation strip copy"
      contains: "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1"
  key_links:
    - from: "shell.ex nav_link"
      to: "ring-primary token"
      via: "focus-visible:ring-primary semantic class"
      pattern: "focus-visible:ring-primary"
    - from: "shell.ex orientation_strip"
      to: "copy_for(:deliveries)"
      via: "Delivery / Suppression list copy strings"
      pattern: "Delivery never arrived"
---

<objective>
Uplift `operator/shell.ex` shared-chrome components: add missing WCAG 2.4.7 focus rings to nav_link and nav_pill, verify theme_toggle touch target, and correct orientation_strip copy to use domain nouns.

Purpose: nav_link/nav_pill focus rings are absent (STATE-LD-06 — the only hard WCAG violation in shared shell components). Orientation strip copy uses "Email" and generic verbs that violate the seven-noun rule (COPY-LD-11/12).
Output: shell.ex with all four changes committed; no bundle rebuild yet (plan 06 handles that).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key contracts extracted from shell.ex (patterns only — no full file re-read during execution) -->

From shell.ex:204-213 (nav_link current class list):
  class={["flex min-h-11 items-center gap-sm rounded-field border-l-2 px-sm text-body transition-colors ease-out",
          "duration-(--duration-fast)",
          if(@active, do: "border-primary ...", else: "border-transparent ...")]}
  Note: focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1 IS ABSENT — add to static string.

From shell.ex:229-238 (nav_pill current class list):
  class={["flex min-h-11 items-center rounded-field px-sm text-body transition-colors ease-out duration-(--duration-fast)",
          if(@active, do: "bg-primary/10 font-bold ...", else: "text-secondary hover:...")]}
  Note: focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1 IS ABSENT — add to static string.

From shell.ex:264-267 (theme_toggle):
  class="btn btn-ghost btn-sm btn-square min-h-11"
  aria-label={if @dark_chrome, do: "Switch to light theme", else: "Switch to dark theme"}
  Note: btn-sm sets min-height 32px, min-h-11 sets 44px — verify which wins; drop btn-sm if it overrides.

From shell.ex:339, 341, 348 (orientation_strip copy_for/1 function):
  339: "Email never arrived? Start here."
  341: "Address keeps getting blocked? Check suppressions."
  348: "Message didn't route as expected? Inspect the routing trace."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible rings to nav_link and nav_pill</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (read lines 200-245 to confirm current class lists for nav_link and nav_pill before editing)
    - SUMMARY.md STATE-LD-06: "ADD focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1 — WCAG 2.4.7 compliance; currently missing"
    - SUMMARY.md DARK-LD-03: ring-primary resolves to Ice #A6EAF2 in dark (semantic, no raw color needed)
    - 97-PATTERNS.md section "nav_link focus ring" for the exact class-string placement
  </read_first>
  <action>
    Read shell.ex lines 200-245.

    In the nav_link class list (the line beginning "flex min-h-11 items-center gap-sm rounded-field border-l-2"):
    Append `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` to that static string — the focus-ring classes must live on the static class string, not inside the `if(@active, ...)` branch (the ring must appear in both active and inactive states).

    In the nav_pill class list (the line beginning "flex min-h-11 items-center rounded-field px-sm text-body"):
    Append `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` to that static string.

    Do NOT touch `aria-current={@active && "page"}` at shell.ex:205 and shell.ex:229 — verify both are still present after editing.

    Semantic token: `ring-primary` — never `ring-[#277B96]` or `ring-[#A6EAF2]` (STATE-LD-06, DARK-LD-03).
  </action>
  <verify>
    <automated>grep -c "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1" mailglass_admin/lib/mailglass_admin/operator/shell.ex</automated>
    Expected: 2 (one for nav_link, one for nav_pill).
    Also verify aria-current not removed: grep -c "aria-current" mailglass_admin/lib/mailglass_admin/operator/shell.ex — must be ≥ 2.
  </verify>
  <done>Both nav_link and nav_pill carry focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1 on their static class string. aria-current="page" not regressed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify theme_toggle min-h-11 compiled height + fix if needed</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (read lines 260-275 for theme_toggle)
    - SUMMARY.md STATE-LD-08: "btn-sm sets min-height 2rem (32px); min-h-11 sets min-height 2.75rem (44px). Phase 97 executor must verify the compiled min-height. If btn-sm overrides min-h-11, drop btn-sm."
    - 97-PATTERNS.md section "theme_toggle min-h-11" for resolution rule
    - mailglass_admin/priv/static/app.css — grep for .btn-sm to find compiled min-height value
  </read_first>
  <action>
    Read shell.ex lines 260-275. Confirm the current class is `btn btn-ghost btn-sm btn-square min-h-11`.

    Check compiled min-height: grep for `.btn-sm` in `mailglass_admin/priv/static/app.css` to find the compiled `min-height` value. Then check the Tailwind `min-h-11` output (should be `min-height: 2.75rem`).

    In Tailwind v4 / daisyUI v5, utility class specificity: if both `.btn-sm` and `min-h-11` set `min-height`, the later-declared rule in the stylesheet wins. Read app.css to determine ordering.

    Decision rule (per STATE-LD-08):
    - If `min-h-11` wins (44px is the compiled effective min-height): no change needed — leave class as-is.
    - If `btn-sm` wins (32px): remove `btn-sm` from the class string, leaving `class="btn btn-ghost btn-square min-h-11"`. `btn-square` is kept for icon sizing; `btn` base class controls height when `btn-sm` is absent.

    Preserve `aria-label={if @dark_chrome, do: "Switch to light theme", else: "Switch to dark theme"}` — do not regress.
  </action>
  <verify>
    <automated>grep "btn-sm\|min-h-11" mailglass_admin/lib/mailglass_admin/operator/shell.ex | grep "theme_toggle\|btn btn-ghost"</automated>
    Result must show `min-h-11` present. If `btn-sm` is also present, it must be because the compiled CSS confirms min-h-11 wins — otherwise btn-sm must be absent.
    Also: grep -c "Switch to light theme\|Switch to dark theme" mailglass_admin/lib/mailglass_admin/operator/shell.ex — must be ≥ 1.
  </verify>
  <done>theme_toggle aria-label preserved. Compiled effective min-height is 44px (either min-h-11 wins over btn-sm, or btn-sm has been dropped).</done>
</task>

<task type="auto">
  <name>Task 3: Replace orientation_strip copy with domain-noun strings</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (read lines 330-360 for orientation_strip copy_for/1 function)
    - SUMMARY.md COPY-LD-11: "Email never arrived? Start here." → "Delivery never arrived? Start here." / "Address keeps getting blocked? Check suppressions." → "Address keeps getting blocked? Review the Suppression list."
    - SUMMARY.md COPY-LD-12: "Message didn't route as expected? Inspect the routing trace." → "InboundMessage didn't route as expected? Inspect the routing trace."
    - 97-PATTERNS.md section "orientation_strip copy" for exact replacements
  </read_first>
  <action>
    Read shell.ex lines 330-360. Locate the `copy_for/1` private function (or the HEEx template section that renders orientation strip tips).

    Make exactly three string replacements:

    Line ~339: Replace `"Email never arrived? Start here."` with `"Delivery never arrived? Start here."` (per COPY-LD-11)

    Line ~341: Replace `"Address keeps getting blocked? Check suppressions."` with `"Address keeps getting blocked? Review the Suppression list."` (per COPY-LD-11)

    Line ~348: Replace `"Message didn't route as expected? Inspect the routing trace."` with `"InboundMessage didn't route as expected? Inspect the routing trace."` (per COPY-LD-12)

    No other changes in this file section. The hero-lifebuoy icon `text-primary` class (STATE-LD-09) must remain — verify it is not accidentally removed.
  </action>
  <verify>
    <automated>grep "Delivery never arrived\|Review the Suppression list\|InboundMessage didn't route" mailglass_admin/lib/mailglass_admin/operator/shell.ex | wc -l</automated>
    Expected: 3 matching lines.
    Banned strings must be gone: grep -c "Email never arrived\|Check suppressions\|Message didn't route" mailglass_admin/lib/mailglass_admin/operator/shell.ex — must be 0.
  </verify>
  <done>Three orientation_strip copy strings updated to domain-noun forms. "Email", "Check suppressions", and "Message" anti-patterns absent from shell.ex orientation strip section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component renders → browser DOM | All shell.ex changes are server-rendered HEEx; no client input processed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Tampering | shell.ex aria attributes | accept | Changes are additive (adding focus ring classes, not removing aria-current); grep gates in verify steps confirm aria-current not regressed |
| T-97-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — pure HEEx/Elixir edits |
</threat_model>

<verification>
After all three tasks:

1. `grep -c "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1" mailglass_admin/lib/mailglass_admin/operator/shell.ex` — output must be 2
2. `grep -c "aria-current" mailglass_admin/lib/mailglass_admin/operator/shell.ex` — must be ≥ 2
3. `grep "Delivery never arrived\|Review the Suppression list\|InboundMessage didn't route" mailglass_admin/lib/mailglass_admin/operator/shell.ex | wc -l` — must be 3
4. `grep -c "Email never arrived\|Check suppressions\b\|Message didn't route" mailglass_admin/lib/mailglass_admin/operator/shell.ex` — must be 0
5. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5` — must exit 0 (no new warnings)
</verification>

<success_criteria>
- nav_link and nav_pill each carry `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` using the semantic `ring-primary` token (never arbitrary ring color).
- theme_toggle effective compiled min-height is 44px; aria-label dynamic string preserved.
- Orientation strip tips read: "Delivery never arrived?", "Review the Suppression list.", "InboundMessage didn't route?" — all three domain-noun corrections applied.
- `mix compile --warnings-as-errors` clean after changes.
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-01-SUMMARY.md` when done.
</output>
