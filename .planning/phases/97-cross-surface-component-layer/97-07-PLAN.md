---
phase: "97"
plan: 07
type: execute
wave: 3
depends_on:
  - "97-01"
  - "97-02"
  - "97-03"
  - "97-04"
  - "97-05"
  - "97-06"
files_modified:
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "priv/static/app.css is bit-for-bit current with all component edits from plans 01-06"
    - "mix verify.preview exits 0 (git diff --exit-code priv/static/ is clean)"
    - "mix compile --warnings-as-errors exits 0"
  artifacts:
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt CSS bundle incorporating all Wave-1 class changes"
      contains: "focus-visible:ring-primary"
  key_links:
    - from: "shell.ex nav_link focus-visible:ring-primary"
      to: "priv/static/app.css compiled output"
      via: "mix mailglass_admin.assets.build"
      pattern: "ring-primary"
---

<objective>
Rebuild and commit `priv/static/app.css` after all Wave-1 component edits (plans 01-05) and Wave-2 gallery creation (plan 06). This is the mandatory bundle-clean gate before structural assertions can run.

Purpose: `verify.preview` runs `cmd git diff --exit-code priv/static/` — any uncommitted bundle reds CI. Every new Tailwind utility class added by plans 01-06 (focus-visible:ring-*, ring-primary/40, text-heading additions) must be compiled into app.css and the result committed.
Output: Committed `priv/static/app.css` with clean diff.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md

<interfaces>
<!-- Bundle-clean gate reference from mix.exs:183-188 -->

verify.preview alias calls two commands in sequence:
  1. mix mailglass_admin.assets.build — invokes the standalone Tailwind binary to rebuild app.css from all .ex/.heex source files
  2. cmd git diff --exit-code priv/static/ — fails if the rebuilt bundle differs from the committed bundle

The assets.build task is defined in mix.exs and calls the vendored Tailwind standalone binary directly (no Node toolchain). Output is written to mailglass_admin/priv/static/app.css.

Prior phase 92/94 pattern: after any component class change, rebuild + commit before verify.preview runs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild CSS bundle and commit (BLOCKING gate)</name>
  <files>mailglass_admin/priv/static/app.css</files>
  <read_first>
    - mailglass_admin/mix.exs (grep for "verify.preview\|assets.build" to confirm the exact task alias and command)
    - .planning/phases/97-cross-surface-component-layer/97-01-SUMMARY.md through 97-06-SUMMARY.md (confirm all prior plans are complete before running bundle rebuild)
  </read_first>
  <action>
    Confirm all plans 01-06 are complete (SUMMARYs exist for each).

    This task implements CONTEXT D-06 (after component edits, rebuild and COMMIT priv/static/app.css via the project asset build, gated by `git diff --exit-code priv/static/` in verify.preview — an uncommitted bundle reds CI).

    Run the asset build:
      `cd mailglass_admin && mix mailglass_admin.assets.build`

    This invokes the standalone Tailwind v4 binary to scan all .ex/.heex files in the admin package and regenerate `priv/static/app.css`. Zero Node toolchain involved.

    After the build completes, run the git diff check:
      `git diff --exit-code priv/static/`

    If the diff is non-empty (expected — the component edits added new utility classes to the source), stage and commit `priv/static/app.css`:
      `git add mailglass_admin/priv/static/app.css`
      `git commit -m "build(97): rebuild app.css bundle after Wave-1 component uplift"`

    After committing, run verify.preview to confirm the gate is clean:
      `cd mailglass_admin && mix verify.preview`

    Expected: mix verify.preview exits 0.

    If mix verify.preview exits non-zero due to a pre-existing issue NOT introduced by plans 01-06, diagnose and fix it before declaring this plan complete. Known pre-existing issues are documented in the project memory (voice_test "Oops" noise from Phoenix dep JS — exclude that from pass/fail).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix verify.preview 2>&1 | tail -5</automated>
    Expected: exit 0. The `git diff --exit-code priv/static/` sub-command must be clean.
    <automated>git diff --exit-code mailglass_admin/priv/static/app.css</automated>
    Expected: exit 0 (bundle committed — no outstanding diff).
  </verify>
  <done>priv/static/app.css rebuilt and committed. mix verify.preview exits 0. git diff --exit-code priv/static/ clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run targeted compile + mix test guards to confirm no regressions</name>
  <files></files>
  <read_first>
    - project memory "execute-phase worktrees impractical" — run targeted test commands, not bare mix test
    - project memory "Validate credo by running it" — run mix credo --strict to validate class/token conformance
  </read_first>
  <action>
    Run compile gate:
      `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -10`
    Expected: exit 0.

    Run credo strict to catch any arbitrary class values or conformance violations introduced:
      `cd mailglass_admin && mix credo --strict 2>&1 | tail -20`
    Expected: no new warnings about tracking-[...] or text-xl or ring-[...] or ease-in.

    Run the ExUnit token-parity and ratchet tests (targeted — not bare mix test):
      `cd mailglass_admin && mix test test/mailglass_admin/verify/ratchet_baseline_test.exs --seed 0 2>&1 | tail -15`
    Expected: exit 0 — 36-cell baseline unchanged (D-10: no gallery entry added).

    Run structural conformance checks:
      `cd mailglass_admin && mix test test/mailglass_admin/verify/ --seed 0 2>&1 | tail -15`
    Expected: exit 0.

    If any targeted test fails due to a Phase 97 regression (not a pre-existing issue), fix the root cause before declaring complete.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | grep -c "error:"</automated>
    Expected: 0.
    <automated>cd mailglass_admin && mix test test/mailglass_admin/verify/ratchet_baseline_test.exs --seed 0 2>&1 | grep -c "failures\b"</automated>
    Expected: 0.
  </verify>
  <done>mix compile --warnings-as-errors clean. Credo strict clean (no new arbitrary values). ratchet_baseline_test 36-cell assertion green. verify/ test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset build → priv/static/app.css | The Tailwind standalone binary scans source files and outputs CSS. No user input, no network access during build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-07-01 | Tampering | priv/static/app.css committed artifact | accept | Bundle is regenerated deterministically from source files by the vendored Tailwind binary. git diff confirms the committed artifact matches the built output. |
| T-97-07-SC | Tampering | npm/pip/cargo installs | accept | Asset build uses standalone Tailwind binary (no npm, no Node). No package installs in this plan. |
</threat_model>

<verification>
After both tasks:

1. `cd mailglass_admin && mix verify.preview 2>&1 | tail -3` — exit 0
2. `git diff --exit-code mailglass_admin/priv/static/app.css` — exit 0 (committed)
3. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | grep "error:" | wc -l` — 0
4. `cd mailglass_admin && mix test test/mailglass_admin/verify/ratchet_baseline_test.exs --seed 0 2>&1 | grep "failures" | wc -l` — 0
</verification>

<success_criteria>
- `mix verify.preview` exits 0 — the `git diff --exit-code priv/static/` gate is clean.
- Committed `priv/static/app.css` contains the compiled output of all Wave-1 + Wave-2 class additions (focus-visible:ring-*, ring-primary/40, text-heading, etc.).
- `mix compile --warnings-as-errors` clean.
- ratchet_baseline_test.exs 36-cell assertion green (ui-baseline-scores.json unchanged — D-10).
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-07-SUMMARY.md` when done.
</output>
