---
phase: "97"
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
autonomous: true
requirements:
  - COMP-02

must_haves:
  truths:
    - "replay_modal dialog carries aria-labelledby pointing at the h2 id"
    - "replay_modal dismisses on Escape key via phx-window-keydown"
    - "replay_modal h2 uses text-heading token and carries id='replay-modal-title'"
    - "replay_modal sub-copy uses the domain-noun COPY-LD-13 string"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
      provides: "WCAG 2.1 compliant modal dialog with aria-labelledby, keyboard dismiss, and domain-noun copy"
      contains: "aria-labelledby"
  key_links:
    - from: "replay_modal role=dialog"
      to: "replay-modal-title h2"
      via: "aria-labelledby='replay-modal-title'"
      pattern: "aria-labelledby=\"replay-modal-title\""
    - from: "replay_modal dialog"
      to: "close_replay handler"
      via: "phx-window-keydown='close_replay' phx-key='Escape'"
      pattern: "phx-window-keydown"
---

<objective>
Apply WCAG 2.1 Level AA accessibility additions to `operator/replay_modal.ex`: add `aria-labelledby` + `id` on h2, keyboard dismiss via Escape, focus trap wiring, h2 typography fix (text-lg → text-heading), and COPY-LD-13 sub-copy.

Purpose: replay_modal is the only interactive overlay in Phase 97 scope. It currently lacks `aria-labelledby`, keyboard dismissal, and focus management — all required by STATE-LD-17 and WCAG 2.1 success criterion 2.1.2. The h2 also uses the banned `text-lg` raw Tailwind class.
Output: replay_modal.ex fully a11y-compliant; no bundle rebuild in this plan.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key contracts extracted from replay_modal.ex -->

replay_modal.ex:20-24 (current dialog div):
  <div
    data-testid="operator-replay-modal"
    role="dialog"
    aria-modal="true"
    class="motion-overlay w-full max-w-2xl rounded-box border border-base-300 bg-base-100 p-6 shadow-overlay"
  >
  ADD: aria-labelledby="replay-modal-title"
  ADD: phx-key="Escape"
  ADD: phx-window-keydown="close_replay"

replay_modal.ex:27-29 (current h2):
  <h2 class="text-lg font-bold text-base-content">
    Replay webhook for {@delivery.recipient}
  </h2>
  CHANGE: text-lg → text-heading; ADD: id="replay-modal-title"

replay_modal.ex:29-32 (current sub-copy paragraph):
  Current: "Replay is delivery-detail initiated, tenant-scoped, and recorded in the append-only ledger."
  Correct (COPY-LD-13): "Re-dispatches the stored webhook request through Mailbox routing and records a new Event in the append-only ledger. Confirm to replay."

replay_modal.ex:100-103 (confirm button):
  class="btn btn-error" + "Confirm replay" — already correct per COPY-LD-07; do NOT change.

Focus trap: JS.focus_first on open and JS.focus back to trigger on close.
  These are LiveView.JS operations invoked from the parent OperatorLive event handler, not the modal component.
  The modal component owns aria-labelledby + Escape dismiss.
  The parent LiveView owns JS.focus commands (invoked in the phx-click="open_replay" and "close_replay" handler JS pipelines).
  Phase 97 scope: add the aria + keyboard attributes to the modal component;
  add JS.focus_first to the JS.show pipeline and JS.focus to the JS.hide pipeline in operator_live.ex or wherever the replay open/close events are handled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aria-labelledby, Escape dismiss, h2 id + typography, and COPY-LD-13 sub-copy</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (read lines 15-110 in full — need the complete modal structure before editing)
    - SUMMARY.md STATE-LD-17: "(1) Add id to h2 and aria-labelledby on dialog; (2) Add phx-key='Escape' phx-window-keydown='close_replay'; (3) Focus trap: JS.focus_first on open / JS.focus back on close."
    - SUMMARY.md COPY-LD-13: exact target sub-copy string for operator replay modal
    - 97-PATTERNS.md section "replay_modal a11y" for exact attribute placements
  </read_first>
  <action>
    Read replay_modal.ex lines 15-110.

    On the `role="dialog"` div (lines ~20-24): add three attributes after `aria-modal="true"`:
      `aria-labelledby="replay-modal-title"`
      `phx-key="Escape"`
      `phx-window-keydown="close_replay"`

    On the `h2` element (lines ~27-29):
      Add `id="replay-modal-title"` attribute.
      Change `text-lg` to `text-heading` (text-lg is a banned raw Tailwind class).

    On the sub-copy `<p>` (lines ~29-32, immediately after the h2): replace the paragraph text with:
      `"Re-dispatches the stored webhook request through Mailbox routing and records a new Event in the append-only ledger. Confirm to replay."`

    The confirm button (lines ~100-103) uses `"Confirm replay"` and `btn btn-error` — do NOT change these.

    Verify `data-testid="operator-replay-modal"` is still present on the dialog div.
    Verify `aria-modal="true"` is still present on the dialog div.
    Verify `class="motion-overlay ..."` is intact (MOTION-LD-04 overlay animation).
  </action>
  <verify>
    <automated>grep -c "aria-labelledby=\"replay-modal-title\"" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</automated>
    Expected: 1.
    <automated>grep -c "id=\"replay-modal-title\"" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</automated>
    Expected: 1.
    <automated>grep -c "phx-window-keydown=\"close_replay\"" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</automated>
    Expected: 1.
    <automated>grep -c "Re-dispatches the stored webhook" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</automated>
    Expected: 1.
    <automated>grep -c "text-lg" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</automated>
    Expected: 0 (text-lg banned; replaced with text-heading).
  </verify>
  <done>Dialog div has aria-labelledby, phx-window-keydown=close_replay, phx-key=Escape. h2 has id=replay-modal-title and uses text-heading. Sub-copy uses COPY-LD-13 string. Confirm button unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JS.focus_first / JS.focus focus-trap in parent LiveView open/close handlers</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (read the full file or grep for "open_replay" and "close_replay" event handlers and any JS.show/JS.hide pipeline that controls the modal)
    - 97-PATTERNS.md section "replay_modal focus trap" — explains that focus commands live in the parent LiveView, not the modal component
    - SUMMARY.md STATE-LD-17: "Focus trap: use Phoenix.LiveView.JS.focus_first on modal open and JS.focus back to the trigger on close."
  </read_first>
  <action>
    Grep operator_live.ex for `open_replay` and `close_replay` to locate the event handlers and any JS pipelines.

    Locate the JS pipeline that shows the replay modal (triggered by phx-click="open_replay" on the replay button). Pipe `JS.focus_first(to: "#operator-replay-modal")` after the JS.show call — this moves focus to the first focusable element inside the modal when it opens.

    Locate the JS pipeline or handle_event/3 that handles "close_replay". After hiding the modal, pipe `JS.focus(to: "#replay-btn-{delivery.id}")` — or use the replay button's `data-testid` or `id` selector to return focus to the element that triggered the modal. If the trigger button does not have an `id`, add one (e.g., `id={"replay-btn-#{@delivery.id}"}`).

    If the open/close is purely through socket assigns (not JS.show/hide), use a JS.dispatch or phx-hook to manage focus — but prefer LiveView.JS approach per the no-client-hook constraint.

    Do NOT add a client-side JS hook. Use Phoenix.LiveView.JS module only (CSS + LiveView.JS constraint per MOTION-LD-11).

    If operator_live.ex uses `phx-click={JS.show("#operator-replay-modal")}` style, chain: `JS.show("#operator-replay-modal") |> JS.focus_first(to: "#operator-replay-modal")`.
    For close: `JS.hide("#operator-replay-modal") |> JS.focus(to: "#replay-open-btn")` (adjust selector to match actual trigger id).
  </action>
  <verify>
    <automated>grep -c "JS.focus_first\|focus_first" mailglass_admin/lib/mailglass_admin/operator_live.ex</automated>
    Expected: ≥ 1.
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
    Expected: exit 0 — no new warnings from the JS module usage.
  </verify>
  <done>JS.focus_first wired on replay modal open. JS.focus (to trigger) wired on replay modal close. mix compile --warnings-as-errors clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replay modal dialog → user action | Confirm replay executes a write (webhook re-dispatch). Modal's two-step flow (open → confirm button) is the existing safety gate. No change to confirmation logic in this plan. |
| phx-window-keydown → close_replay handler | Escape key triggers close_replay event. Handler must NOT execute the replay action — it must close the modal. Verify the handle_event("close_replay", ...) handler only hides the modal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Elevation of Privilege | close_replay handler | mitigate | Verify close_replay handle_event only closes modal; does not call the replay dispatch function. grep for replay dispatch call site and confirm it is only in the confirm handler, not close_replay. |
| T-97-03-02 | Spoofing | aria-labelledby id | accept | id="replay-modal-title" is a static string; no user input can inject into it |
| T-97-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure HEEx/Elixir edits |
</threat_model>

<verification>
After both tasks:

1. `grep -c "aria-labelledby=\"replay-modal-title\"" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` — must be 1
2. `grep -c "phx-window-keydown=\"close_replay\"" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` — must be 1
3. `grep -c "text-lg" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` — must be 0
4. `grep -c "text-heading" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` — must be ≥ 1
5. `grep -c "Re-dispatches the stored webhook" mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex` — must be 1
6. `grep -c "JS.focus_first" mailglass_admin/lib/mailglass_admin/operator_live.ex` — must be ≥ 1
7. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5` — exit 0
</verification>

<success_criteria>
- `role="dialog"` div carries `aria-labelledby="replay-modal-title"`, `phx-key="Escape"`, `phx-window-keydown="close_replay"`.
- `h2` has `id="replay-modal-title"` and uses `text-heading` token (text-lg absent).
- Sub-copy reads: "Re-dispatches the stored webhook request through Mailbox routing and records a new Event in the append-only ledger. Confirm to replay."
- Confirm button "Confirm replay" with btn-error unchanged.
- JS.focus_first fires on modal open; JS.focus returns to trigger on close.
- close_replay handler closes modal only — does not dispatch replay action.
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-03-SUMMARY.md` when done.
</output>
