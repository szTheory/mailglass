---
phase: "97"
plan: 06
type: execute
wave: 2
depends_on:
  - "97-01"
  - "97-02"
  - "97-03"
  - "97-04"
  - "97-05"
files_modified:
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/lib/mailglass_admin/router.ex
autonomous: true
requirements:
  - GALLERY-01
  - GALLERY-02

must_haves:
  truths:
    - "GET /dev/mail/gallery returns 200 in the test server"
    - "GalleryLive renders one section per component from STATE-LD-01..22 (22 components)"
    - "Every specimen cell has data-testid='gallery-{component}-{state}'"
    - "Every specimen cell contains twin data-theme='mailglass-light' and data-theme='mailglass-dark' wrappers"
    - "Route is inside dev live_session only — never reachable under /ops"
    - "No DB access in GalleryLive mount or render"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      provides: "Dev-only component gallery LiveView with in-code specimen list"
      contains: "data-testid"
    - path: "mailglass_admin/lib/mailglass_admin/router.ex"
      provides: "Gallery route inside preview live_session"
      contains: "GalleryLive"
  key_links:
    - from: "router.ex preview live_session"
      to: "GalleryLive :index"
      via: "live \"/gallery\", MailglassAdmin.GalleryLive, :index"
      pattern: "GalleryLive"
    - from: "gallery_live.ex specimen list"
      to: "data-testid cells"
      via: "gallery-{component}-{state} testid scheme"
      pattern: "gallery-status_badge-delivered"
---

<objective>
Create `GalleryLive` and wire it into the router — the dev-only component gallery at `/dev/mail/gallery` rendering every component × every state × both themes from an in-code specimen list with stable `data-testid` cells.

Purpose: GALLERY-01 (the route and LiveView) and GALLERY-02 (stable testids) are the primary new deliverables of Phase 97. The gallery is the visual-audit capture surface for structural assertions. It depends on plans 01-05 having uplifted the components so specimens render correctly.
Output: gallery_live.ex + router.ex change; no DB access; no new hero icon glyphs; no bundle rebuild in this plan (plan 07).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key contracts from CONTEXT and PATTERNS for GalleryLive -->

CONTEXT D-01: Route is one line inside existing live_session :mailglass_admin_preview block in router.ex:219-225.
  live "/gallery", MailglassAdmin.GalleryLive, :index
  Note: /gallery (1 segment) does not collide with /:mailable/:scenario (2 segments).

CONTEXT D-02: Specimen list — in-code @specimens module attribute or defp, returning [{component_atom, state_label, assigns_map}] tuples. No DB access. No __preview_session__ assigns.

CONTEXT D-03: Twin data-theme wrappers per cell — NOT a toggle. Each cell has:
  <div data-theme="mailglass-light" ...>specimen</div>
  <div data-theme="mailglass-dark" ...>same specimen</div>

CONTEXT D-04: data-testid scheme = gallery-{component}-{state}
  (e.g. gallery-status_badge-delivered, gallery-nav_link-active, gallery-flash-error)
  Theme is on the ancestor [data-theme] wrapper — NOT in the testid.

CONTEXT D-07: No new hero icons needed — use only glyphs already embedded in heroicons-inline.js.

CONTEXT D-10: Do NOT add "gallery" to @surfaces in ratchet_baseline_test.exs or ui-baseline-scores.json. Gallery cells feed structural assertions only (GALLERY-02), not LLM-score baseline keys.

router.ex:87-95 @compile no_warn_undefined list:
  Add MailglassAdmin.GalleryLive to this list.

Specimen coverage required (all STATE-LD rows):
  status_badge: 22 atoms + phantom nil (23 specimens for this component alone — STATE-LD-05)
  nav_link: active, inactive (STATE-LD-06)
  nav_pill: active, inactive (STATE-LD-06)
  tenant_chip: with-tenant, no-tenant (STATE-LD-07)
  theme_toggle: light-mode, dark-mode (STATE-LD-08)
  orientation_strip: deliveries, inbound, preview (STATE-LD-09)
  shell: not a gallery specimen (shell is the full page layout — cannot be embedded in a gallery cell)
  deliveries_list: populated-unselected, populated-selected, empty (STATE-LD-11)
  detail_header: shown, absent (STATE-LD-12) — operator variant only for gallery
  filters_form: empty, filled (STATE-LD-13) — static assigns, no phx-submit needed
  support_cards: tier1-shown, tier1-hidden (STATE-LD-14) — static data
  suppression_card: present, absent (STATE-LD-15)
  timeline: populated, highlighted-event, empty (STATE-LD-16)
  replay_modal: closed (STATE-LD-17) — open states require live event; specimen is rest-closed
  routing_trace: empty, all-passing, first-failing (STATE-LD-18)
  evidence_card: no-evidence, redacted, revealed, denied (STATE-LD-19)
  device_frame: inactive-btn (STATE-LD-20) — static assigns, no phx-click
  tabs: inactive-tab (STATE-LD-21) — static assigns showing the tab strip
  sidebar: mailable-collapsed, mailable-expanded, scenario-active (STATE-LD-22) — static
  icon: rest (STATE-LD-01)
  logo: rest (STATE-LD-02)
  flash: error-kind, info-kind, success-kind, warning-kind (STATE-LD-03)
  badge: warning, stub (STATE-LD-04)

File location: lib/mailglass_admin/gallery_live.ex (flat next to preview_live.ex — per PATTERNS.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire gallery route in router.ex</name>
  <files>mailglass_admin/lib/mailglass_admin/router.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/router.ex (read lines 85-100 for @compile list; read lines 215-230 for preview live_session block)
    - CONTEXT D-01: exact placement — one line inside existing live_session block, after the two existing live lines
    - 97-PATTERNS.md section "router.ex" for exact @compile addition and live_session placement
  </read_first>
  <action>
    Read router.ex lines 85-100 and 215-230.

    In the @compile {:no_warn_undefined, [...]} list (lines ~87-95): add `MailglassAdmin.GalleryLive` as a new entry in the list. Do not remove any existing entries.

    In the preview live_session block (lines ~219-225), after the two existing `live` lines (`live "/", MailglassAdmin.PreviewLive, :index` and `live "/:mailable/:scenario", MailglassAdmin.PreviewLive, :show`), add:
      `live "/gallery", MailglassAdmin.GalleryLive, :index`

    This is a single line addition inside the existing `do...end` block. Do NOT create a new `live_session` block or a new `scope` block. The route resolves to `/dev/mail/gallery` because the preview live_session is mounted under `/dev/mail` in the adopter's router (per CONTEXT D-01).
  </action>
  <verify>
    <automated>grep -c "GalleryLive" mailglass_admin/lib/mailglass_admin/router.ex</automated>
    Expected: 2 (one in @compile list, one in live "/gallery").
    <automated>grep -c "live \"/gallery\"" mailglass_admin/lib/mailglass_admin/router.ex</automated>
    Expected: 1.
  </verify>
  <done>router.ex: GalleryLive in @compile no_warn_undefined list; live "/gallery" route inside preview live_session. No new session block or scope.</done>
</task>

<task type="auto">
  <name>Task 2: Create GalleryLive module with complete in-code specimen list and twin-theme render</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/preview_live.ex (read lines 1-55 for module header/use pattern; lines 57-83 for mount/assign pattern)
    - SUMMARY.md STATE-LD-01..22: full component × state matrix — every STATE-LD row that has gallery specimens listed
    - CONTEXT D-02, D-03, D-04: specimen list shape, twin-theme wrappers, testid scheme
    - 97-PATTERNS.md section "gallery_live.ex" for the module header, mount, specimens data structure, and render template
  </read_first>
  <action>
    Create the file at `mailglass_admin/lib/mailglass_admin/gallery_live.ex`.

    Module structure (per 97-PATTERNS.md):
      `defmodule MailglassAdmin.GalleryLive`
      `use MailglassAdmin, :live_view`
      Alias the component modules: Components, Operator.Shell, Operator.DeliveriesList, Operator.DetailHeader, Operator.FiltersForm, Operator.SupportCards, Operator.SuppressionCard, Operator.Timeline, Operator.ReplayModal, Inbound.RoutingTrace, Inbound.EvidenceCard, Preview.DeviceFrame, Preview.Tabs, Preview.Sidebar.

    Mount assigns (no DB, no mailable scan, no __preview_session__ access):
      `:page_title` = "mailglass — Component Gallery"
      `:specimens` = call `specimens()` private function

    Specimen list (`@specimens` module attribute returning keyword-list or list of {component_atom, state_label, assigns_map} tuples):
      Cover every STATE-LD-01..22 row per the interface notes above.
      For status_badge: all 22 atoms + phantom nil (23 entries).
      For components that cannot be shown in open/interactive state in a static specimen (replay_modal open, full deliveries list with live selection), use the rest/closed/static state only.

    Render function: implement a grouped render (one `<section>` per component, one cell per state).
    Each cell:
      - Outer `<div data-testid={"gallery-#{component}-#{state}"}>` — the stable ratchet anchor (CONTEXT D-04)
      - A label `<p>` showing `{component} — {state}` in text-label font-bold text-secondary
      - Twin wrapper divs:
          `<div data-theme="mailglass-light" ...>` — renders the specimen
          `<div data-theme="mailglass-dark" ...>` — renders the same specimen

    Use a private `render_specimen/2` (or component function) that pattern-matches on the component atom and dispatches to the appropriate component function call with the assigns_map.

    Page heading: `<h1 class="text-display font-bold text-base-content mb-lg">Component Gallery</h1>`
    Section headings: `<h2 class="text-heading font-bold text-base-content mb-lg">{ComponentName}</h2>` — use domain nouns and component names verbatim (not "Storybook", not "Email Status").

    Gallery page chrome: neutral `bg-base-100` page background, `bg-base-200 border border-base-300 rounded-box` per cell container — NO accent used in gallery chrome itself.

    No handle_params, no handle_event, no DB calls, no Repo usage.

    Do NOT add new hero icon glyphs to heroicons-inline.js — specimens reuse only already-embedded glyphs (CONTEXT D-07).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -10</automated>
    Expected: exit 0 — no warnings about undefined GalleryLive.
    <automated>grep -c "data-testid" mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    Expected: ≥ 1 (the data-testid= interpolation in the cell template).
    <automated>grep -c "data-theme=\"mailglass-light\"\|data-theme=\"mailglass-dark\"" mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    Expected: ≥ 2 (both theme wrappers in the cell template).
    <automated>grep -c "gallery-status_badge-delivered\|gallery-nav_link-active\|gallery-flash-error" mailglass_admin/lib/mailglass_admin/gallery_live.ex</automated>
    Expected: ≥ 1 (specimen tuples exist — could be in @specimens attribute or specimen list function).
    Verify no Ecto.Repo calls: grep -c "Repo\." mailglass_admin/lib/mailglass_admin/gallery_live.ex — must be 0.
  </verify>
  <done>gallery_live.ex compiles clean. Specimen list covers all STATE-LD-01..22 states. Each cell has data-testid=gallery-{component}-{state} and twin data-theme wrappers. No DB access. No new hero icon glyphs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /dev/mail/gallery route → browser | Route is inside preview live_session which is guarded by adopter's `if dev_routes` wrapping — dev-only by construction |
| GalleryLive specimen assigns | All assigns are hardcoded in the in-code specimen list — no user input, no DB data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06-01 | Elevation of Privilege | gallery route exposure | mitigate | Route is inside dev live_session only (CONTEXT D-01). Adopter's `if dev_routes` wrapping gates it to dev environment. Verify no ops-session live_session contains the route. |
| T-97-06-02 | Denial of Service | gallery specimen rendering | accept | Specimen list is finite (~50 cells); no DB query; no external call. Rendering is bounded. |
| T-97-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — new Elixir module only |
</threat_model>

<verification>
After both tasks:

1. `grep -c "GalleryLive" mailglass_admin/lib/mailglass_admin/router.ex` — must be 2
2. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | grep -i error | wc -l` — must be 0
3. `grep -c "data-testid" mailglass_admin/lib/mailglass_admin/gallery_live.ex` — must be ≥ 1
4. `grep -c "data-theme=\"mailglass-light\"\|data-theme=\"mailglass-dark\"" mailglass_admin/lib/mailglass_admin/gallery_live.ex` — must be ≥ 2
5. `grep -c "Repo\." mailglass_admin/lib/mailglass_admin/gallery_live.ex` — must be 0
6. `grep -c "gallery_live\|gallery-" mailglass_admin/lib/mailglass_admin/router.ex` — must be ≥ 1
</verification>

<success_criteria>
- GET /dev/mail/gallery resolves (route registered inside preview live_session; dev-only by adopter wrapping).
- GalleryLive mounts with in-code specimen list covering STATE-LD-01..22 component × state matrix.
- Every specimen cell has `data-testid="gallery-{component}-{state}"` plus twin `data-theme="mailglass-light"` and `data-theme="mailglass-dark"` wrappers.
- No DB access (`Repo.` absent from gallery_live.ex).
- `ui-baseline-scores.json` and `ratchet_baseline_test.exs @surfaces` unmodified (D-10).
- `mix compile --warnings-as-errors` clean.
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-06-SUMMARY.md` when done.
</output>
