---
phase: "97"
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/lib/mailglass_admin/operator/timeline.ex
  - mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex
  - mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "All 22 status_badge atoms (plus phantom fallback) resolve to correct daisyUI badge class per STATE-LD-05"
    - "icon/logo/flash/badge components carry required ARIA attributes"
    - "timeline highlighted event shows border-primary ring-1 ring-primary/40; dot classes match STATUS-LD-16 groups"
    - "routing_trace first-failing border-l-4 border-error present; no interactive states added"
    - "evidence_card reveal button effective min-height is 44px; revealed pre element uses correct token classes"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "Verified shared component ARIA and status_badge class correctness"
      contains: "badge-primary"
    - path: "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
      provides: "motion-timeline container + highlighted event border + correct dot classes"
      contains: "border-primary ring-1 ring-primary/40"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex"
      provides: "first-failing border-l-4 border-error; badge-outline badge-error on route-level badge"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex"
      provides: "reveal button with min-h-11; pre with correct token classes"
      contains: "min-h-11"
  key_links:
    - from: "components.ex status_badge"
      to: "status_class/1 dispatch table"
      via: "badge-primary / badge-success / badge-warning / badge-error / badge-outline"
      pattern: "badge-primary.*dispatched|badge-success.*delivered"
    - from: "timeline.ex event container"
      to: "highlighted border"
      via: "border-primary ring-1 ring-primary/40"
      pattern: "ring-primary/40"
---

<objective>
Verification + targeted fix pass on four component files: `components.ex` (icon/logo/flash/badge/status_badge ARIA and class correctness), `timeline.ex` (highlighted event + dot classes), `routing_trace.ex` (first-failing border + route badge — verify only), and `evidence_card.ex` (reveal button touch target + revealed pre classes).

Purpose: Most changes here are verify-then-fix-if-wrong (routing_trace and flash may need no edits). This plan groups the "light-touch verification + minor fix" components so heavier files get their own plans.
Output: Up to four modified files; no bundle rebuild (plan 06).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key contracts from SUMMARY.md for these components -->

STATE-LD-01 (icon): aria-hidden="true" always present. No interactive states.
STATE-LD-02 (logo): role="img" + aria-label="mailglass". No interactive states.
STATE-LD-03 (flash): role="status" + aria-live="polite" on container. motion-reveal on mount.
STATE-LD-04 (badge sidebar): badge-warning + badge-sm gap-1 for :warning; text-secondary text-label "—" for :stub.
STATE-LD-05 (status_badge): 22 atoms — dispatched/queued/sent→badge-primary; delivered/opened/clicked/accepted/webhook_replay_succeeded→badge-success; deferred/unsubscribed/no_match/reconciled→badge-warning; bounced/failed/rejected/complained/failed_ingest/webhook_replay_failed→badge-error; autoresponded/unknown/ignore/webhook_replay_requested→badge-outline. Phantom fallback: badge-outline.

STATE-LD-16 (timeline): motion-timeline on ol container; highlighted event: border-primary ring-1 ring-primary/40; dot classes by group: webhook_replay_failed→bg-error; webhook_replay_requested/succeeded→bg-warning; reconciled→bg-accent; all other→bg-primary.

STATE-LD-18 (routing_trace — verify only): first-failing→border-l-4 border-error px-3; route-level badge→badge-outline badge-error "No match".

STATE-LD-19 (evidence_card): reveal button: btn btn-ghost btn-sm min-h-11 px-4 (verify min-h-11 wins; drop btn-sm if not). Revealed pre: max-h-80 overflow-auto rounded-box border border-base-300 bg-base-100 text-label text-base-content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify components.ex ARIA attributes and status_badge class dispatch table</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/components.ex (read lines 45-280 — icon 45-49, logo 62-89, flash 102-111, badge 129-141, status_badge 196-280 incl status_class/1 and status_icon/1)
    - SUMMARY.md STATE-LD-01..05: per-component ARIA and class requirements
    - 97-UI-SPEC.md per-component state contract table for components.ex
  </read_first>
  <action>
    Read components.ex lines 45-280.

    Check icon (lines 45-49): `aria-hidden="true"` must be present on the icon span. If absent, add it.

    Check logo (lines 62-89): `role="img"` + `aria-label="mailglass"` must be present. If absent, add both.

    Check flash (lines 102-111): `role="status"` + `aria-live="polite"` must be on the container element. Verify `motion-reveal` class is on the flash region. If absent, add them.

    Check badge (lines 129-141): `:warning` variant uses `badge badge-warning badge-sm gap-1` + exclamation icon. `:stub` variant uses `text-secondary text-label` + "—" text. Both are display-only. Verify no interactive attributes were accidentally added.

    Check status_badge status_class/1 (lines ~196-250): verify ALL 22 atoms map to the correct daisyUI class per STATE-LD-05:
      - :dispatched, :queued, :sent → "badge-primary"
      - :delivered, :opened, :clicked, :accepted, :webhook_replay_succeeded → "badge-success"
      - :deferred, :unsubscribed, :no_match, :reconciled → "badge-warning"
      - :bounced, :failed, :rejected, :complained, :failed_ingest, :webhook_replay_failed → "badge-error"
      - :autoresponded, :unknown, :ignore, :webhook_replay_requested → "badge-outline"
      - phantom atoms (:suppressed, nil) → fallback "badge-outline"

    For any atom that maps to a wrong class, correct the defp status_class/1 clause.
    Verify aria-hidden="true" is present on status_badge icon spans within status_icon/1 (lines ~232-255).

    Only make changes for correctness failures found during the read. The goal is verify-then-fix, not rewrite.
  </action>
  <verify>
    <automated>grep -c "aria-hidden=\"true\"" mailglass_admin/lib/mailglass_admin/components.ex</automated>
    Expected: ≥ 1 (icon component).
    <automated>grep -c "role=\"img\"" mailglass_admin/lib/mailglass_admin/components.ex</automated>
    Expected: ≥ 1 (logo component).
    <automated>grep -c "role=\"status\"" mailglass_admin/lib/mailglass_admin/components.ex</automated>
    Expected: ≥ 1 (flash container).
    <automated>grep -c "badge-primary\|badge-success\|badge-warning\|badge-error\|badge-outline" mailglass_admin/lib/mailglass_admin/components.ex</automated>
    Expected: ≥ 5 (one clause per group in status_class/1).
  </verify>
  <done>icon has aria-hidden. logo has role=img + aria-label=mailglass. flash has role=status + aria-live=polite. status_badge: all 22 atoms + phantom fallback resolve to correct daisyUI class per STATE-LD-05.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix timeline highlighted event + dot classes</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/timeline.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/timeline.ex (read lines 14-71 — ol container, event_container_class/2, event_dot_class/1)
    - SUMMARY.md STATE-LD-16: "highlighted event: border-primary ring-1 ring-primary/40. Dot classes: webhook_replay_failed→bg-error; webhook_replay_requested/webhook_replay_succeeded→bg-warning; reconciled→bg-accent; all other→bg-primary."
    - 97-PATTERNS.md section "timeline verify"
  </read_first>
  <action>
    Read timeline.ex lines 14-71.

    Verify ol container (line ~30) has class `motion-timeline ...` (STATE-LD-16 + MOTION-LD-03). If absent, add `motion-timeline` to the ol class.

    Locate `event_container_class/2` (or equivalent logic that applies border classes to the highlighted event). When the highlight_id matches the event_id, the class must include `border-primary ring-1 ring-primary/40`. If this function does not exist or returns incorrect classes, add/fix it:
      defp event_container_class(highlight_id, event_id) when highlight_id == event_id, do: "border-primary ring-1 ring-primary/40"
      defp event_container_class(_highlight_id, _event_id), do: "border-base-300"

    Locate `event_dot_class/1` (or equivalent). Verify dot color mapping per STATE-LD-16:
      :webhook_replay_failed → include "bg-error"
      :webhook_replay_requested, :webhook_replay_succeeded → include "bg-warning"
      :reconciled → include "bg-accent"
      all other atoms → include "bg-primary"
    Fix any clause that returns wrong color.

    Semantic tokens only — never "bg-red-500" or hex colors. Each dot class must be a daisyUI semantic token (bg-error, bg-warning, bg-success, bg-primary, bg-accent).
  </action>
  <verify>
    <automated>grep -c "motion-timeline" mailglass_admin/lib/mailglass_admin/operator/timeline.ex</automated>
    Expected: ≥ 1.
    <automated>grep -c "ring-primary/40" mailglass_admin/lib/mailglass_admin/operator/timeline.ex</automated>
    Expected: ≥ 1 (highlighted event container).
    <automated>grep -c "bg-error\|bg-warning\|bg-primary\|bg-accent" mailglass_admin/lib/mailglass_admin/operator/timeline.ex</automated>
    Expected: ≥ 4 (dot class clauses).
  </verify>
  <done>motion-timeline on ol. Highlighted event returns border-primary ring-1 ring-primary/40. Dot classes map webhook_replay_failed→bg-error, webhook_replay_requested/succeeded→bg-warning, reconciled→bg-accent, other→bg-primary.</done>
</task>

<task type="auto">
  <name>Task 3: Verify routing_trace state classes + fix evidence_card reveal button and pre element</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex, mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex (read lines 31-101 — empty state, first-failing li, route-level badge)
    - mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex (read lines 29-113 — reveal button, pre element, denied state)
    - SUMMARY.md STATE-LD-18: routing_trace — first-failing: border-l-4 border-error px-3; route badge: badge-outline badge-error. No code changes expected if already correct.
    - SUMMARY.md STATE-LD-19: evidence_card reveal button btn-sm + min-h-11 tension; if btn-sm wins, drop btn-sm. Revealed pre: max-h-80 overflow-auto rounded-box border border-base-300 bg-base-100 text-label text-base-content.
    - 97-PATTERNS.md sections "routing_trace verify" and "evidence_card verify"
  </read_first>
  <action>
    routing_trace.ex — Read lines 31-101. This is a verify-only pass (per 97-PATTERNS.md — all required classes are already present):
    Verify empty-state prose present (e.g., "No inbound routes..." in text-body text-secondary).
    Verify first-failing clause `<li>` has class including `border-l-4 border-error px-3`.
    Verify route-level badge has `badge-outline badge-error`.
    If all three are correct, no changes needed. If any are wrong, fix to the STATE-LD-18 spec.

    evidence_card.ex — Read lines 29-113:
    Locate the reveal button (lines ~38-45). Check its class: should be `btn btn-ghost btn-sm min-h-11 px-4`.
    Resolve btn-sm vs min-h-11: grep mailglass_admin/priv/static/app.css for `.btn-sm` and `.min-h-11` declarations to determine which compiled rule wins.
    If btn-sm overrides min-h-11 (compiled min-height < 44px): drop `btn-sm`, leaving `btn btn-ghost min-h-11 px-4`.
    If min-h-11 wins (compiled min-height = 44px): no change to button class.

    Locate the revealed `<pre>` element (lines ~70-90). It must have class: `max-h-80 overflow-auto rounded-box border border-base-300 bg-base-100 text-label text-base-content`.
    If the pre uses raw CSS instead of token classes, fix it to the above class string.

    Verify the denied state element has `border-warning bg-warning/10 p-3 text-body text-base-content`.
  </action>
  <verify>
    <automated>grep -c "border-l-4 border-error" mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex</automated>
    Expected: ≥ 1.
    <automated>grep -c "badge-outline badge-error\|badge-error.*badge-outline" mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex</automated>
    Expected: ≥ 1.
    <automated>grep -c "min-h-11" mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex</automated>
    Expected: ≥ 1 (reveal button must carry min-h-11).
    <automated>grep -c "max-h-80 overflow-auto rounded-box" mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex</automated>
    Expected: ≥ 1 (revealed pre element).
  </verify>
  <done>routing_trace: first-failing border-l-4 border-error present; route badge has badge-outline badge-error; no interactive states added. evidence_card: reveal button carries min-h-11 at effective 44px; revealed pre has token class string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence_card pre element | Renders raw payload from inbound data. Pre element is server-rendered with {:safe, raw_payload} or plain text — verify no XSS vector is introduced by the pre class change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Information Disclosure | evidence_card revealed pre | accept | Raw payload display is an existing feature (STATE-LD-19 specifies it). No new data is surfaced; only the CSS class is being corrected |
| T-97-04-02 | Tampering | status_badge class dispatch | accept | status_class/1 is a pure function on atom inputs; no user-supplied data flows through it |
| T-97-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — verify-and-fix Elixir edits only |
</threat_model>

<verification>
After all three tasks:

1. `grep -c "aria-hidden=\"true\"\|role=\"img\"\|role=\"status\"" mailglass_admin/lib/mailglass_admin/components.ex` — must be ≥ 3
2. `grep -c "ring-primary/40" mailglass_admin/lib/mailglass_admin/operator/timeline.ex` — must be ≥ 1
3. `grep -c "motion-timeline" mailglass_admin/lib/mailglass_admin/operator/timeline.ex` — must be ≥ 1
4. `grep -c "border-l-4 border-error" mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex` — must be ≥ 1
5. `grep -c "min-h-11" mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex` — must be ≥ 1
6. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5` — exit 0
</verification>

<success_criteria>
- components.ex: icon has aria-hidden; logo has role=img+aria-label=mailglass; flash has role=status+aria-live=polite; all 22 status atoms map to correct daisyUI badge class; phantom fallback badge-outline present.
- timeline.ex: motion-timeline on ol; highlighted event: border-primary ring-1 ring-primary/40; dot class dispatch correct per STATUS-LD-16.
- routing_trace.ex: verify pass passes (all required classes already present — no regression introduced).
- evidence_card.ex: reveal button effective 44px min-height; revealed pre uses max-h-80 overflow-auto rounded-box border border-base-300 bg-base-100 text-label text-base-content.
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-04-SUMMARY.md` when done.
</output>
