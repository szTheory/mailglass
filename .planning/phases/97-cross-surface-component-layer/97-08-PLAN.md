---
phase: "97"
plan: 08
type: execute
wave: 4
depends_on:
  - "97-07"
files_modified:
  - mailglass_admin/e2e/structural.spec.js
  - .planning/RATCHET-GAP-REGISTER.md
autonomous: true
requirements:
  - GALLERY-02

must_haves:
  truths:
    - "e2e/structural.spec.js gallery describe block is un-skipped and runs real assertions"
    - "openGallery(page) helper navigates to /dev/mail/gallery and verifies h1"
    - "At least 5 getByTestId assertions cover status_badge, nav_link, flash, routing_trace, evidence_card gallery cells"
    - "Each cell asserts twin data-theme wrappers are visible"
    - "GAP-05 row in RATCHET-GAP-REGISTER.md is flipped to status: fixed"
  artifacts:
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Un-skipped gallery surface structural assertions"
      contains: "openGallery"
    - path: ".planning/RATCHET-GAP-REGISTER.md"
      provides: "GAP-05 flipped to fixed"
      contains: "fixed"
  key_links:
    - from: "structural.spec.js openGallery helper"
      to: "GalleryLive /dev/mail/gallery route"
      via: "page.goto('/dev/mail/gallery')"
      pattern: "openGallery"
    - from: "getByTestId('gallery-status_badge-delivered')"
      to: "GalleryLive specimen cell"
      via: "data-testid='gallery-status_badge-delivered'"
      pattern: "gallery-status_badge-delivered"
---

<objective>
Un-skip the reserved gallery block in `e2e/structural.spec.js`, add the `openGallery` helper, write real `getByTestId` visibility + twin-theme assertions, and flip `RATCHET-GAP-REGISTER.md` GAP-05 to `fixed`.

Purpose: GALLERY-02 requires stable `data-testid` cells that can be structurally asserted by the ratchet. The skipped block at structural.spec.js:421-428 was reserved for exactly this. The gallery structural assertions close the loop between plan 06 (specimen cells with testids) and the ratchet layer.
Output: Un-skipped gallery describe block with real assertions; GAP-05 marked fixed.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-UI-SPEC.md
@/Users/jon/projects/mailglass/.planning/phases/97-cross-surface-component-layer/97-PATTERNS.md
@/Users/jon/projects/mailglass/.planning/research/v1.11/SUMMARY.md

<interfaces>
<!-- Key patterns from structural.spec.js (patterns only — read the file before editing) -->

openPreview helper pattern (structural.spec.js:38-42):
  async function openPreview(page) {
    await page.goto("/ops/browser-preview-empty");
    await expect(page.getByTestId("preview-orientation")).toBeVisible();
  }

Mirror this as openGallery:
  async function openGallery(page) {
    await page.goto("/dev/mail/gallery");
    await expect(page.getByRole("heading", { name: "Component Gallery", level: 1 })).toBeVisible();
  }

Skipped block location: structural.spec.js:421-428 (test.describe.skip).
Replace the entire test.describe.skip block with a test.describe (no .skip).

Existing assertion patterns in the file:
  page.getByTestId("operator-delivery-row").first()  → toBeVisible()
  page.locator('[data-theme="mailglass-dark"]')       → exists in preview assertions

data-testid scheme (CONTEXT D-04):
  gallery-{component}-{state}
  e.g. gallery-status_badge-delivered, gallery-nav_link-active, gallery-flash-error

Twin-theme assertion pattern:
  const cell = page.getByTestId("gallery-status_badge-delivered");
  await expect(cell.locator('[data-theme="mailglass-light"]')).toBeVisible();
  await expect(cell.locator('[data-theme="mailglass-dark"]')).toBeVisible();

GAP-05 row location: RATCHET-GAP-REGISTER.md line ~138.
  Current status field: "open"
  Flip to: "fixed"
  Add close evidence: reference "97-08-SUMMARY.md" or "Phase 97 plan 08"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip gallery describe block and add real structural assertions</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/structural.spec.js (read lines 35-50 for openPreview helper pattern; read lines 415-435 for the skipped block to be replaced)
    - 97-PATTERNS.md section "structural.spec.js gallery block" for the exact openGallery helper and assertion set
    - 97-UI-SPEC.md Gallery Contract section for testid scheme and assertion targets
    - CONTEXT D-09: exact instruction to un-skip and add openGallery helper
  </read_first>
  <action>
    Read structural.spec.js lines 35-50 (for the openPreview pattern) and lines 415-435 (for the skipped gallery block).

    Add the `openGallery` helper function near `openPreview` (after line 42):
      async function openGallery(page) {
        await page.goto("/dev/mail/gallery");
        await expect(page.getByRole("heading", { name: "Component Gallery", level: 1 })).toBeVisible();
      }

    Replace the entire `test.describe.skip("gallery surface — deferred to Phase 97", ...)` block (lines ~421-428) with a real `test.describe("gallery surface — Phase 97", ...)` containing:

    Test 1 — gallery renders status_badge specimens for all 5 badge groups:
      await openGallery(page)
      assert getByTestId("gallery-status_badge-delivered") → toBeVisible() (badge-success group)
      assert getByTestId("gallery-status_badge-dispatched") → toBeVisible() (badge-primary group)
      assert getByTestId("gallery-status_badge-bounced") → toBeVisible() (badge-error group)
      assert getByTestId("gallery-status_badge-deferred") → toBeVisible() (badge-warning group)
      assert getByTestId("gallery-status_badge-autoresponded") → toBeVisible() (badge-outline group)

    Test 2 — gallery renders nav_link active and inactive states:
      await openGallery(page)
      assert getByTestId("gallery-nav_link-active") → toBeVisible()
      assert getByTestId("gallery-nav_link-inactive") → toBeVisible()

    Test 3 — gallery renders flash states:
      await openGallery(page)
      assert getByTestId("gallery-flash-error") → toBeVisible()
      assert getByTestId("gallery-flash-success") → toBeVisible()

    Test 4 — gallery twin-theme wrappers present per cell:
      await openGallery(page)
      const cell = page.getByTestId("gallery-status_badge-delivered")
      assert cell.locator('[data-theme="mailglass-light"]') → toBeVisible()
      assert cell.locator('[data-theme="mailglass-dark"]') → toBeVisible()

    Test 5 — gallery renders routing_trace and evidence_card inbound specimens:
      await openGallery(page)
      assert getByTestId("gallery-routing_trace-empty") → toBeVisible()
      assert getByTestId("gallery-evidence_card-redacted") → toBeVisible()

    All tests use `async ({ page }) =>` and await openGallery(page) at the start.
    No test.skip(), no test.describe.skip() in the new block.
  </action>
  <verify>
    <automated>grep -c "test.describe.skip" mailglass_admin/e2e/structural.spec.js</automated>
    Expected: 0 (the gallery skip is removed; verify no other skips were accidentally added).
    <automated>grep -c "openGallery" mailglass_admin/e2e/structural.spec.js</automated>
    Expected: ≥ 6 (one function definition + at least 5 call sites, one per test).
    <automated>grep -c "gallery-status_badge-delivered\|gallery-nav_link-active\|gallery-flash-error" mailglass_admin/e2e/structural.spec.js</automated>
    Expected: ≥ 3.
    <automated>grep -c "data-theme=\"mailglass-light\"\|data-theme=\"mailglass-dark\"" mailglass_admin/e2e/structural.spec.js</automated>
    Expected: ≥ 2 (twin-theme assertion in test 4).
  </verify>
  <done>Gallery describe block is un-skipped. openGallery helper defined. Five tests cover status_badge groups, nav_link states, flash states, twin-theme wrappers, and inbound specimens. test.describe.skip absent from the file.</done>
</task>

<task type="auto">
  <name>Task 2: Flip GAP-05 to fixed in RATCHET-GAP-REGISTER.md</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    - .planning/RATCHET-GAP-REGISTER.md (read lines 130-145 — GAP-05 row)
    - CONTEXT D-09: "Flip GAP-05 → fixed in RATCHET-GAP-REGISTER.md:138"
  </read_first>
  <action>
    Read RATCHET-GAP-REGISTER.md lines 130-145.

    Locate the GAP-05 row (line ~138). The row currently has `status: open`.

    Update the `status` field in the GAP-05 row from `open` to `fixed`.

    Add or update the `fix sketch` / evidence field to reference: "Phase 97 plan 08: gallery route created in router.ex; structural.spec.js gallery block un-skipped with 5 real assertions."

    Do NOT modify any other GAP rows (GAP-01 through GAP-04 status remain as-is — they are closed by downstream phases 98/99/100 per SUMMARY.md GAP Coverage Index).

    Format: keep the pipe-delimited table row intact; only change the status cell value.
  </action>
  <verify>
    <automated>grep "GAP-05" .planning/RATCHET-GAP-REGISTER.md | grep -c "fixed"</automated>
    Expected: 1.
    <automated>grep "GAP-04\|GAP-03\|GAP-02\|GAP-01" .planning/RATCHET-GAP-REGISTER.md | grep -c "fixed"</automated>
    Expected: 0 (other GAPs still open — not touched by Phase 97).
  </verify>
  <done>GAP-05 status field is "fixed" in RATCHET-GAP-REGISTER.md. GAP-01 through GAP-04 remain open (unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test → /dev/mail/gallery | The test server must expose the gallery route; the dev live_session must be active in test config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-08-01 | Tampering | structural.spec.js test skip removal | accept | Removing test.describe.skip only adds test coverage; it cannot introduce a false-green if the underlying route/testids don't exist |
| T-97-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs — JS test edits only |
</threat_model>

<verification>
After both tasks:

1. `grep -c "test.describe.skip" mailglass_admin/e2e/structural.spec.js` — must be 0
2. `grep -c "openGallery" mailglass_admin/e2e/structural.spec.js` — must be ≥ 6
3. `grep "GAP-05" .planning/RATCHET-GAP-REGISTER.md | grep -c "fixed"` — must be 1
4. Optional smoke (if test server is running): `cd mailglass_admin && npx playwright test e2e/structural.spec.js --grep "gallery" 2>&1 | tail -10` — all gallery tests pass.

Note: Playwright tests require the Phoenix dev server. If the server is not running, verify via grep gates only and note in SUMMARY that the Playwright run should be validated in the execute-phase browser session.
</verification>

<success_criteria>
- `test.describe.skip` gallery block replaced with `test.describe` + 5 real tests.
- `openGallery(page)` helper navigates to `/dev/mail/gallery` and asserts the h1 heading.
- Five tests cover: status_badge badge-group coverage, nav_link states, flash states, twin-theme wrappers per cell, inbound specimens.
- GAP-05 row in `RATCHET-GAP-REGISTER.md` has `status: fixed`.
- GAP-01 through GAP-04 remain open (Phase 98/99/100 scope).
</success_criteria>

<output>
Create `.planning/phases/97-cross-surface-component-layer/97-08-SUMMARY.md` when done.
</output>
