---
phase: 69-click
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
  - reference/demo_app/test/mailglass_demo_web/page_controller_dashboard_test.exs
autonomous: true
requirements:
  - DEMO-03
must_haves:
  truths:
    - "A maintainer can load `/` and immediately see a guided Northstar hub that links into the real preview, outbound operator, and inbound operator surfaces."
    - "The dashboard remains thin demo-app glue under `MailglassDemoWeb` and does not duplicate MailglassAdmin screens or add new public Mailglass API."
    - "The reset affordance is explicitly destructive and demo-only, and safe operator navigation remains constrained to local `/ops/mail*` paths."
  artifacts:
    - path: reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
      provides: "Controller-rendered click-around hub with real mounted-surface links and destructive reset copy."
    - path: reference/demo_app/test/mailglass_demo_web/page_controller_dashboard_test.exs
      provides: "Focused dashboard route/content/link coverage for DEMO-03 without relying on MailglassAdmin DOM internals."
  key_links:
    - from: reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
      to: reference/demo_app/lib/mailglass_demo/demo_data.ex
      via: "DemoData.summary/0"
      pattern: "summary = DemoData\\.summary\\(\\)"
    - from: reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
      to: /dev/mail
      via: "preview dashboard card href"
      pattern: "href=\"/dev/mail\""
    - from: reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
      to: /demo/login?return_to=/ops/mail?tenant_id=northstar
      via: "outbound operator card href"
      pattern: "href=\"/demo/login\\?return_to=/ops/mail\\?tenant_id=#\\{summary\\.tenant_id\\}\""
    - from: reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
      to: /demo/login?return_to=/ops/mail/inbound?tenant_id=northstar
      via: "inbound operator card href"
      pattern: "href=\"/demo/login\\?return_to=/ops/mail/inbound\\?tenant_id=#\\{summary\\.tenant_id\\}\""
  decision_refs:
    - id: D-01
      constraint: "Keep the dashboard implementation in `MailglassDemoWeb.PageController.home/2`."
    - id: D-02
      constraint: "Keep dashboard behavior under `MailglassDemoWeb`; do not move it into package source."
    - id: D-03
      constraint: "Treat the page as a hub into real preview/operator surfaces, not a second admin."
    - id: D-04
      constraint: "Keep the exact mounted-surface link targets for preview, outbound operator, and inbound operator."
    - id: D-05
      constraint: "Preserve simple demo-only login/session glue and safe `return_to` filtering."
    - id: D-06
      constraint: "Make reset wording explicitly destructive and demo-only."
    - id: D-10
      constraint: "Keep the page calm, guided, inspectable, and evidence-first."
    - id: D-11
      constraint: "Use Mailglass domain language such as preview, delivery, suppression, inbound record, evidence, routing trace, and replay."
    - id: D-12
      constraint: "Avoid marketing gloss, production account UI, analytics-dashboard styling, and stable-public-API claims."
    - id: D-13
      constraint: "Add focused verification for controller output, links, reset wording, and dashboard route coverage."
    - id: D-14
      constraint: "Do not pull Playwright screenshot/checkpoint hardening into this plan."
    - id: D-15
      constraint: "Do not make private MailglassAdmin DOM shape the proof surface for DEMO-03."
---

<objective>
Refine the existing controller-rendered home page into the Phase 69 click-around hub and add focused route/content coverage.

Purpose: satisfy DEMO-03 by guiding adopters into the real mounted Mailglass preview, outbound operator, and inbound operator surfaces without widening product surface area or borrowing Phase 70 browser-evidence scope.
Output: a more guided `PageController.home/2` dashboard plus a dedicated controller test that pins exact route targets, reset wording, and summary content.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/69-click/69-CONTEXT.md
@.planning/phases/69-click/69-RESEARCH.md
@.planning/phases/69-click/69-PATTERNS.md
@.planning/phases/69-click/69-VALIDATION.md
@.planning/phases/69-click/69-UI-SPEC.md
@.planning/phases/67-demo-app-foundation/67-03-SUMMARY.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-01-SUMMARY.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-02-SUMMARY.md
@reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
@reference/demo_app/lib/mailglass_demo_web/router.ex
@reference/demo_app/lib/mailglass_demo/demo_data.ex
@reference/demo_app/test/mailglass_demo_web/page_controller_security_test.exs

<interfaces>
From `reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex`:

```elixir
def home(conn, _params) do
  summary = DemoData.summary()
  html(conn, """...""")
end

def login(conn, params) do
  return_to = safe_return_to(Map.get(params, "return_to"))
  ...
  |> redirect(to: return_to)
end
```

From `reference/demo_app/lib/mailglass_demo/demo_data.ex`:

```elixir
def summary do
  %{
    tenant_id: @tenant,
    deliveries: Repo.aggregate(Delivery, :count),
    events: Repo.aggregate(Event, :count),
    inbound: count_table!("mailglass_inbound_records"),
    suppressions: Repo.aggregate(Entry, :count)
  }
end
```

From `reference/demo_app/lib/mailglass_demo_web/router.ex`:

```elixir
get("/", PageController, :home)
get("/demo/login", PageController, :login)
post("/demo/reset", PageController, :reset)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn `PageController.home/2` into the guided Northstar click-around hub</name>
  <read_first>
reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
reference/demo_app/lib/mailglass_demo_web/router.ex
reference/demo_app/lib/mailglass_demo/demo_data.ex
reference/demo_app/test/mailglass_demo_web/page_controller_security_test.exs
.planning/phases/69-click/69-CONTEXT.md
.planning/phases/69-click/69-RESEARCH.md
.planning/phases/69-click/69-PATTERNS.md
.planning/phases/69-click/69-UI-SPEC.md
  </read_first>
  <files>reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex</files>
  <action>Refine `MailglassDemoWeb.PageController.home/2` in place per D-01, D-02, and D-03; do not introduce a LiveView, helper component tree, or package-level dashboard abstraction. Preserve the existing controller-rendered HTML and inline `<style>` approach, keep `summary = DemoData.summary()` as the data source, and keep the current link topology exact per D-04: `/dev/mail`, `/demo/login?return_to=/ops/mail?tenant_id=#{summary.tenant_id}`, `/demo/login?return_to=/ops/mail/inbound?tenant_id=#{summary.tenant_id}`, and the existing `POST /demo/reset` form. Update the page copy and structure so the hub is more guided and evidence-first per D-10 through D-12: use the exact route labels `Preview mailables`, `Outbound operator`, `Inbound operator`, and `Reset seed data`; add a short operator-facing intro that says tenant `northstar` contains deterministic preview, delivery, suppression, inbound record, routing trace, and replay evidence; and add a compact “what to inspect” summary that mentions invite and sign-in, receipt and payment failure, usage alert and suppression, support reply, refund request, spam reject, no-match routing, and stored-truth replay. Keep the existing stats labels `Deliveries`, `Ledger Events`, `Inbound Records`, and `Suppressions`. Make the reset affordance explicit per D-06 by changing its visible card copy to the exact sentence `Destructive: truncates and reseeds deterministic demo evidence tables for tenant northstar.` Do not change `login/2`, `safe_return_to/1`, session key names, `authorized_evidence_reset?/1`, `/health`, `/demo/evidence/reset`, or the mounted operator routes.</action>
  <verify>
    <automated>cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo_web/page_controller_dashboard_test.exs test/mailglass_demo_web/page_controller_security_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex` still defines the dashboard in `home/2` and still assigns `summary = DemoData.summary()`.
- The rendered dashboard contains the exact visible route labels `Preview mailables`, `Outbound operator`, `Inbound operator`, and `Reset seed data`.
- The rendered dashboard contains the exact destructive sentence `Destructive: truncates and reseeds deterministic demo evidence tables for tenant northstar.`
- The rendered dashboard still links to `/dev/mail`, `/demo/login?return_to=/ops/mail?tenant_id=northstar`, and `/demo/login?return_to=/ops/mail/inbound?tenant_id=northstar`.
- No new routes, auth flows, LiveViews, or package-source dashboard modules are introduced.
  </acceptance_criteria>
  <done>The demo home page becomes a clearer click-around hub while remaining a thin controller page over the existing mounted Mailglass surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused dashboard route and content coverage without coupling to MailglassAdmin DOM</name>
  <read_first>
reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
reference/demo_app/lib/mailglass_demo_web/router.ex
reference/demo_app/test/mailglass_demo_web/page_controller_security_test.exs
.planning/phases/69-click/69-CONTEXT.md
.planning/phases/69-click/69-PATTERNS.md
.planning/phases/69-click/69-VALIDATION.md
  </read_first>
  <files>reference/demo_app/test/mailglass_demo_web/page_controller_dashboard_test.exs</files>
  <action>Create `reference/demo_app/test/mailglass_demo_web/page_controller_dashboard_test.exs` as the new Phase 69 route/content proof per D-13 through D-15. Follow the existing `MailglassDemo.ConnCase` request/assertion style from `page_controller_security_test.exs`, but keep proof bounded to controller HTML and route strings rather than MailglassAdmin internals. Add focused tests that: `GET /` returns `200`; the response contains `Northstar Ops`, `Preview mailables`, `Outbound operator`, `Inbound operator`, and `Reset seed data`; the response contains the stats labels `Deliveries`, `Ledger Events`, `Inbound Records`, and `Suppressions`; the response contains the exact hrefs `/dev/mail`, `/demo/login?return_to=/ops/mail?tenant_id=northstar`, and `/demo/login?return_to=/ops/mail/inbound?tenant_id=northstar`; and the response contains the exact destructive reset sentence from Task 1. Do not add assertions against LiveView test IDs, rendered operator list items, preview HTML internals, or browser selectors. Keep `reference/demo_app/test/mailglass_demo_web/page_controller_security_test.exs` unmodified unless an unavoidable compile failure requires a minimal adjustment; verification must still run it to preserve the existing safe-redirect and reset-token guarantees from D-05.</action>
  <verify>
    <automated>cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo_web/page_controller_dashboard_test.exs test/mailglass_demo_web/page_controller_security_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/test/mailglass_demo_web/page_controller_dashboard_test.exs` exists and uses `MailglassDemo.ConnCase`.
- The new test file asserts `GET /` returns `200`.
- The new test file asserts the exact three click-through targets and the exact destructive reset sentence named in Task 1.
- The test file does not reference MailglassAdmin DOM test IDs, LiveView internals, or Playwright selectors.
- `cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo_web/page_controller_dashboard_test.exs test/mailglass_demo_web/page_controller_security_test.exs --warnings-as-errors` exits `0`.
  </acceptance_criteria>
  <done>Phase 69 has explicit controller-level proof for the click-around hub while preserving the existing redirect and reset security assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser request -> `PageController.home/2` | Untrusted browser traffic hits the controller-rendered dashboard and its generated route targets. |
| dashboard card -> `/demo/login?return_to=...` | Click-through navigation crosses from public demo page into the demo auth/redirect seam for operator surfaces. |
| dashboard reset form -> destructive reseed action | A browser-visible control can trigger the destructive `POST /demo/reset` flow. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Blocked By | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|------------|-----------------|
| T-69-01 | medium | Spoofing | Dashboard operator links and `safe_return_to/1` seam | mitigate | Task 1 exact route preservation plus Task 2 targeted controller/security tests | Keep the dashboard pointing only at `/dev/mail` and local `/ops/mail*` paths, and re-run `page_controller_security_test.exs` so redirect filtering stays fail-closed. |
| T-69-02 | low | Tampering | `POST /demo/reset` affordance | mitigate | Task 1 exact destructive wording plus Task 2 content assertions | Make the reset control visibly destructive and demo-only so the UI does not conceal the truncation-and-reseed behavior. |
| T-69-03 | medium | Repudiation | Dashboard proof scope | mitigate | Task 2 controller-only assertions | Keep proof at controller HTML + route-target level and forbid MailglassAdmin DOM/test-id coupling so the plan does not over-claim Phase 69 coverage or stable private UI structure. |

High-severity threats: none. Rationale: this plan only refines a local demo controller and adds bounded controller tests; it introduces no new external auth surface, secret handling, or persistent privilege escalation path.
</threat_model>

<verification>
- `cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo_web/page_controller_dashboard_test.exs test/mailglass_demo_web/page_controller_security_test.exs --warnings-as-errors`
- `cd reference/demo_app && MIX_ENV=test mix test --warnings-as-errors`
</verification>

<success_criteria>
- `GET /` renders a guided hub for tenant `northstar` with exact links into preview, outbound operator, and inbound operator surfaces.
- The reset affordance explicitly states that it truncates and reseeds deterministic demo evidence tables.
- Controller-level tests prove route targets and copy without relying on MailglassAdmin DOM internals.
</success_criteria>

<output>
Create `.planning/phases/69-click/69-01-SUMMARY.md` when done
</output>
