---
phase: 69-click
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/README.md
  - reference/demo_app/test/mailglass_demo/docs_contract_test.exs
autonomous: true
requirements:
  - DX-03
must_haves:
  truths:
    - "The canonical demo docs start with a short compose quickstart and tell a fresh maintainer exactly where to click for preview, outbound operator, and inbound operator flows."
    - "The canonical demo docs explain the Northstar Ops persona/JTBD, the seeded outbound and inbound stories, destructive reset semantics, dependency mode, and the demo-vs-contract boundary."
    - "Docs proof stays text-based and explicitly does not promote demo DOM, selectors, routes, copy, or browser evidence artifacts into stable public API."
  artifacts:
    - path: reference/demo_app/README.md
      provides: "Canonical short quickstart plus persona/JTBD, seeded-data, and click-path documentation for Phase 69."
    - path: reference/demo_app/test/mailglass_demo/docs_contract_test.exs
      provides: "Executable docs contract coverage for DX-03 and the Phase 70 browser-evidence boundary."
  key_links:
    - from: reference/demo_app/README.md
      to: compose.demo.yml
      via: "Quickstart and reset commands"
      pattern: "docker compose -f compose\\.demo\\.yml"
    - from: reference/demo_app/README.md
      to: /dev/mail
      via: "what-to-click route list"
      pattern: "/dev/mail"
    - from: reference/demo_app/README.md
      to: /ops/mail and /ops/mail/inbound
      via: "demo login click-path routes"
      pattern: "/demo/login\\?return_to=/ops/mail"
    - from: reference/demo_app/test/mailglass_demo/docs_contract_test.exs
      to: reference/demo_app/README.md
      via: "README source assertions"
      pattern: "File\\.read!"
  decision_refs:
    - id: D-07
      constraint: "Keep `reference/demo_app/README.md` as the canonical quickstart and click guide."
    - id: D-08
      constraint: "Do not scatter canonical demo truth into root/admin docs in this plan."
    - id: D-09
      constraint: "Explain persona/JTBD, seeded outbound and inbound data, preview scenarios, reset behavior, Compose quickstart, dependency mode, and the demo-vs-contract boundary."
    - id: D-11
      constraint: "Use consistent Mailglass domain language."
    - id: D-12
      constraint: "Avoid marketing gloss and stable-public-API claims about demo routes, selectors, copy, or DOM shape."
    - id: D-13
      constraint: "Add focused verification for docs content."
    - id: D-14
      constraint: "Keep Playwright screenshots/checkpoints and hardened browser artifacts out of scope."
    - id: D-15
      constraint: "Do not depend on private MailglassAdmin DOM shape as docs proof."
---

<objective>
Make `reference/demo_app/README.md` the complete Phase 69 quickstart and click-path guide, then lock it with a docs contract test.

Purpose: satisfy DX-03 with one canonical doc that explains who the demo is for, what deterministic data exists, where to click, how reset works, and why the demo is adoption evidence rather than contract truth.
Output: expanded demo README content plus a focused `docs_contract_test.exs` that fails closed on route, reset, persona/JTBD, and boundary drift.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/69-click/69-CONTEXT.md
@.planning/phases/69-click/69-RESEARCH.md
@.planning/phases/69-click/69-PATTERNS.md
@.planning/phases/69-click/69-VALIDATION.md
@.planning/phases/67-demo-app-foundation/67-01-SUMMARY.md
@.planning/phases/67-demo-app-foundation/67-03-SUMMARY.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-01-SUMMARY.md
@.planning/phases/68-realistic-b2b-saas-fixtures/68-02-SUMMARY.md
@reference/demo_app/README.md
@reference/demo_app/lib/mailglass_demo/demo_data.ex
@reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
@reference/demo_app/assets/e2e/demo.spec.js

<interfaces>
From `reference/demo_app/README.md`:

```markdown
## Quickstart
...
- Demo dashboard: http://localhost:4015
- Preview: http://localhost:4015/dev/mail
- Outbound operator: http://localhost:4015/demo/login?return_to=/ops/mail?tenant_id=northstar
- Inbound operator: http://localhost:4015/demo/login?return_to=/ops/mail/inbound?tenant_id=northstar
```

From `reference/demo_app/lib/mailglass_demo/demo_data.ex`:

```elixir
def tenant_id, do: "northstar"
def summary do
  %{deliveries: ..., events: ..., inbound: ..., suppressions: ...}
end
```

From `reference/demo_app/assets/e2e/demo.spec.js`:

```javascript
test("dashboard links to preview and operator surfaces", ...)
test("outbound operator opens with seeded delivery evidence", ...)
test("inbound operator opens with seeded support mailbox evidence", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the demo README into the canonical quickstart and what-to-click guide</name>
  <read_first>
reference/demo_app/README.md
reference/demo_app/lib/mailglass_demo/demo_data.ex
reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
reference/demo_app/assets/e2e/demo.spec.js
.planning/phases/69-click/69-CONTEXT.md
.planning/phases/69-click/69-RESEARCH.md
.planning/phases/69-click/69-PATTERNS.md
  </read_first>
  <files>reference/demo_app/README.md</files>
  <action>Keep `reference/demo_app/README.md` as the only canonical demo quickstart per D-07. Do not edit the root `README.md`, `mailglass_admin` docs, or other package docs in this plan; D-08 requires canonical truth to stay here and any other pointers to remain brief and directional. Preserve `## Quickstart` as the first substantive section and keep the existing compose boot command, reset command, and `demo_e2e` command, but expand the file so DX-03 is complete and explicit. Add or strengthen the exact sections `## Quickstart`, `## Persona and JTBD`, `## Seeded data`, `## What to click`, and `## Dependency Mode`. Under `## Persona and JTBD`, keep Northstar Ops as the B2B SaaS operations team and explicitly say the demo exists to inspect preview mailables, confirm deliveries and suppressions, review inbound records and routing trace evidence, and replay stored truth without guessing. Under `## Seeded data`, add concrete bullets that name the deterministic preview stories `invite admin`, `magic link`, `receipt paid`, `payment failed`, `usage alert`, and `incident update`; the outbound operator stories `receipt delivery`, `payment failure bounce`, `usage alert bounce`, and `manual suppression`; and the inbound operator stories `support reply`, `refund request`, `spam reject`, `no-match route`, and `stored-truth replay`. Under `## What to click`, add an ordered list that starts at `http://localhost:4015`, then sends the user to `http://localhost:4015/dev/mail`, then `http://localhost:4015/demo/login?return_to=/ops/mail?tenant_id=northstar`, then `http://localhost:4015/demo/login?return_to=/ops/mail/inbound?tenant_id=northstar`, and finally calls out `mix demo.reset` or the dashboard reset button as the destructive way to restore the deterministic baseline. Keep the reset wording explicit with the exact sentence `Destructive note: this reset truncates seeded demo tables before reseeding preview, delivery, suppression, inbound record, evidence, routing trace, and replay data for tenant \`northstar\`.` Preserve the dependency-mode section, and end the boundary language with the exact sentence `This demo app is richer click-around evidence for maintainer and adopter validation. It does not define stable Mailglass API guarantees, and demo DOM, selectors, routes, and copy are not stable public API.`</action>
  <verify>
    <automated>cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/README.md` contains the exact section headers `## Quickstart`, `## Persona and JTBD`, `## Seeded data`, `## What to click`, and `## Dependency Mode`.
- The README contains the exact click-path URLs for the dashboard, preview, outbound operator, and inbound operator routes.
- The README contains the exact destructive reset sentence and the exact demo-vs-contract boundary sentence from the action.
- The README names the preview, outbound, and inbound seeded stories listed in the action.
- No root README or package-doc edits are required to satisfy DX-03.
  </acceptance_criteria>
  <done>The demo README stands alone as the canonical quickstart and click-path guide for a fresh maintainer or adopter reviewer.</done>
</task>

<task type="auto">
  <name>Task 2: Add a docs contract test that fails closed on quickstart, route, and boundary drift</name>
  <read_first>
reference/demo_app/README.md
reference/demo_app/test/test_helper.exs
reference/demo_app/test/mailglass_demo/mailer_preview_scenarios_test.exs
.planning/phases/69-click/69-CONTEXT.md
.planning/phases/69-click/69-PATTERNS.md
.planning/phases/69-click/69-VALIDATION.md
  </read_first>
  <files>reference/demo_app/test/mailglass_demo/docs_contract_test.exs</files>
  <action>Create `reference/demo_app/test/mailglass_demo/docs_contract_test.exs` as plain `ExUnit.Case` coverage that reads `reference/demo_app/README.md` and asserts the exact Phase 69 tokens from Task 1. Keep the verification boundary textual per D-13 through D-15: assert section headers, quickstart commands, route URLs, seeded-data story labels, the destructive reset sentence, the dependency-mode heading, `demo_browser_evidence.v1` as a future artifact label, and the exact boundary sentence `This demo app is richer click-around evidence for maintainer and adopter validation. It does not define stable Mailglass API guarantees, and demo DOM, selectors, routes, and copy are not stable public API.` Use simple `File.read!` plus exact-string assertions or tight regexes; do not parse HTML, invoke Playwright, or inspect MailglassAdmin DOM/test-id shape. The test file must live under `reference/demo_app/test/mailglass_demo/` so it can be run directly by Mix and remain independent from Phase 70 browser-evidence work.</action>
  <verify>
    <automated>cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
- `reference/demo_app/test/mailglass_demo/docs_contract_test.exs` exists and uses `ExUnit.Case`.
- The test file reads `reference/demo_app/README.md` with `File.read!`.
- The test file asserts the exact section headers, quickstart command, route URLs, seeded-story labels, destructive reset sentence, future artifact label `demo_browser_evidence.v1`, and the exact boundary sentence named in Task 1.
- The test file does not depend on browser automation, LiveView DOM, or MailglassAdmin test IDs.
- `cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/docs_contract_test.exs --warnings-as-errors` exits `0`.
  </acceptance_criteria>
  <done>DX-03 is protected by a small textual contract test instead of browser-depth or DOM-coupled proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer/adopter reader -> demo README | Readers rely on documentation to decide what the demo proves and how to navigate it. |
| README text -> future browser-evidence work | Phase 69 docs can accidentally over-claim DOM or screenshot stability that Phase 70 has not proven yet. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Blocked By | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|------------|-----------------|
| T-69-04 | medium | Repudiation | `reference/demo_app/README.md` boundary language | mitigate | Task 1 exact boundary sentence plus Task 2 docs contract assertions | Keep the README explicit that the demo is adoption evidence only and that DOM, selectors, routes, and copy are not stable public API. |
| T-69-05 | low | Tampering | Quickstart and click-path instructions | mitigate | Task 1 exact commands/routes plus Task 2 source assertions | Pin the compose command, reset command, and four exact click-path URLs in a test so later docs drift cannot silently misroute the maintainer. |
| T-69-06 | medium | Information Disclosure | Seeded-data and reset semantics prose | mitigate | Task 1 exact synthetic-story wording plus Task 2 reset/boundary assertions | Keep the docs scoped to synthetic Northstar stories and explicit destructive reset behavior; do not imply production tenants, hidden data, or broader provider/public-contract guarantees. |

High-severity threats: none. Rationale: this plan only updates maintainer-facing docs and a text-based contract test; it introduces no new auth surface, secret storage, or externally reachable processing path.
</threat_model>

<verification>
- `cd reference/demo_app && MIX_ENV=test mix test test/mailglass_demo/docs_contract_test.exs --warnings-as-errors`
- `cd reference/demo_app && MIX_ENV=test mix test --warnings-as-errors`
</verification>

<success_criteria>
- `reference/demo_app/README.md` starts with a short compose quickstart and clearly explains persona, JTBD, seeded data, what to click, reset semantics, dependency mode, and the demo boundary.
- A focused docs contract test fails closed if the quickstart routes, seeded-story labels, reset wording, or Phase 70 boundary drift.
- No browser-depth proof or private MailglassAdmin DOM dependence is required for DX-03.
</success_criteria>

<output>
Create `.planning/phases/69-click/69-02-SUMMARY.md` when done
</output>
