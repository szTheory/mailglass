---
phase: 60-release-trust-gate-drift-prevention
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
  - test/mailglass/publish/maintaining_release_gate_contract_test.exs
  - test/scripts/required_checks_test.exs
autonomous: true
requirements: [OPS-02, EVID-02]
must_haves:
  truths:
    - "MAINTAINING.md lists green trust evidence as an explicit release-gate item (Trust Lane Repo Head check + clean-baseline/published journey + trust-runner-* artifacts)"
    - "MAINTAINING.md Required Checks includes the Phase 59 Trust Lane Repo Head context"
    - "MAINTAINING.md no longer instructs a manual hex-publish approval gate in any of its three stale regions"
    - "A doc-contract test asserts the trust-evidence strings present and the three stale approval-gate strings absent"
    - "required_checks_test.exs explicitly refutes a clean-baseline entry in REQUIRED_CHECKS (locks D-04)"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Release-gate trust-evidence requirement + reconciled Required Checks + hands-free runbook"
      contains: "Trust Lane Repo Head (Elixir 1.18 / OTP 27)"
    - path: "test/mailglass/publish/maintaining_release_gate_contract_test.exs"
      provides: "Deterministic doc-contract guard for the OPS-02 release-gate language"
      contains: "MAINTAINING.md"
    - path: "test/scripts/required_checks_test.exs"
      provides: "D-04 hardening refute that the clean-baseline lane is absent from REQUIRED_CHECKS"
      contains: "Trust Lane Clean Baseline"
  key_links:
    - from: "test/mailglass/publish/maintaining_release_gate_contract_test.exs"
      to: "MAINTAINING.md"
      via: "File.read! + assert/refute on literal strings"
      pattern: "File\\.read!.*MAINTAINING\\.md"
    - from: "test/scripts/required_checks_test.exs"
      to: "scripts/setup_branch_protection.sh REQUIRED_CHECKS"
      via: "parse_required_checks/1 + refute clean-baseline membership"
      pattern: "refute MapSet.member"
---

<objective>
Satisfy OPS-02 by editing `MAINTAINING.md` and shipping a deterministic doc-contract test, plus harden
the D-04 (EVID-02) posture with an explicit `refute` in the REQUIRED_CHECKS drift test.

MAINTAINING.md edits (D-08/D-09/D-10):
(a) Add green trust evidence as an explicit release-gate item — the `Trust Lane Repo Head (Elixir 1.18 /
OTP 27)` required check + the new clean-baseline / published-version trust journey + the `trust-runner-*`
checkpoint artifacts — under the "Verify CI green" runbook step and the "Required Checks" section, and a
maintenance-cadence note that the post-publish trust journey must be green before milestone trust claims /
v1.3 closeout.
(b) Reconcile "Required Checks" to include the Phase 59 `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`
context now in `scripts/setup_branch_protection.sh` REQUIRED_CHECKS.
(c) Fix the THREE stale approval-gate regions (RESEARCH Pitfall 7 — line 24, the "Bus Factor & Continuity"
block at ~178-188, Release Runbook step 3 at ~260-266) and bump the stale `~> 1.2` smoke-step line at ~277.
Replace (don't just delete) the dead "Approve the hex-publish deployment" step with a "monitor the
hands-free publish fan-out" step, preserving the 5-step runbook numbering.

Doc-contract test (D-08, RESEARCH "ship it"): assert trust-evidence strings present and the three stale
approval-gate strings absent — converts OPS-02 from a docs-only claim into a local deterministic guard.

D-04 hardening: extend `required_checks_test.exs` with a `refute` that "Trust Lane Clean Baseline ..." is
NOT in REQUIRED_CHECKS, locking the clean-baseline lane out of branch protection against future drift.

Purpose: A release checklist that requires "green trust evidence" must not also instruct a maintainer to
click an approval that does not exist (the `hex-publish` environment has no required reviewers — hands-free
publish). Per CONTEXT D-08/D-09/D-10 and RESEARCH Open Question 2/3.
Output: Edited `MAINTAINING.md`, a new doc-contract test, and a hardened `required_checks_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/60-release-trust-gate-drift-prevention/60-RESEARCH.md

<interfaces>
<!-- MAINTAINING.md stale-region map (RESEARCH Pitfall 7, VERIFIED): -->
<!--   Line 24: "The `publish-hex` workflow is environment-gated and requires manual approval in the -->
<!--             GitHub Actions UI." -> FALSE, no required reviewers. -->
<!--   Lines 178-188 ("Bus Factor & Continuity"): "a GitHub Environment (`hex-publish`) with a single -->
<!--             required reviewer (`szTheory`)" + "one-eye pause". -> describes a gate that no longer exists. -->
<!--   Lines 260-266 (Release Runbook step 3): "Approve the `hex-publish` deployment in the GitHub -->
<!--             Environment UI ... BEFORE clicking Approve ... Record ... approver identity, and approval -->
<!--             timestamp." -> FALSE; no approval step. Replace with "monitor hands-free fan-out", keep 5 steps. -->
<!--   Line 277: "{:mailglass, "~> 1.2"}, {:mailglass_admin, "~> 1.2"}, {:mailglass_inbound, "~> 0.2"}" -> bump to ~> 1.3 / ~> 0.3. -->
<!--   Required Checks section (lines 123-176): currently lists Support Contract Core/Admin + Compile No -->
<!--             Optional Deps as the exact required contexts (144-147) but OMITS Trust Lane Repo Head. -->
<!--   Verify CI green runbook step (lines 241-249): lists the required release-truth buckets. -->
<!-- scripts/setup_branch_protection.sh REQUIRED_CHECKS (lines 17-22) currently holds 4 entries INCLUDING -->
<!--   "Trust Lane Repo Head (Elixir 1.18 / OTP 27)" and NOT any clean-baseline entry (D-04). -->
<!-- Doc-contract test precedent: test/mailglass/install/install_first_preview_smoke_test.exs:24-39 -->
<!--   (File.read! a file, assert literal strings). New test home: test/mailglass/publish/ -->
<!--   (module namespace Mailglass.Publish.*, e.g. existing Mailglass.Publish.InstallerGoldenCheckTest). -->
<!--   From test/mailglass/publish/, repo-root MAINTAINING.md is Path.expand("../../../MAINTAINING.md", __DIR__). -->
<!-- required_checks_test.exs: parse_required_checks/1 returns a MapSet of REQUIRED_CHECKS names; -->
<!--   @v1_0_lock_entries holds the 3 Phase-27 lock entries. Add a refute test for the clean-baseline name. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile MAINTAINING.md to the hands-free, trust-evidence-gated release flow</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (line 24; Required Checks 123-176 incl. exact contexts at 144-147; Bus Factor 178-188; Release Runbook 228-289 incl. Verify-CI-green step 241-249, stale step 3 at 260-266, `~> 1.2` smoke line at 277)
    - scripts/setup_branch_protection.sh (REQUIRED_CHECKS array lines 17-22 — the source of truth that now includes Trust Lane Repo Head; do NOT modify this script)
    - .github/workflows/ci.yml (the trust lane names: "Trust Lane Repo Head (Elixir 1.18 / OTP 27)" and, from Plan 02, "Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)" — for accurate reference; reference the artifact names trust-runner-repo-head/clean-baseline/published)
    - .planning/phases/60-release-trust-gate-drift-prevention/60-RESEARCH.md (Pitfall 7 three-region map; Open Question 2 runbook restructure; State of the Art "MAINTAINING.md" row)
    - .planning/phases/60-release-trust-gate-drift-prevention/60-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
    Edit `MAINTAINING.md` with these changes:
    (a) In the "Required Checks" section (around 123-147): add `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`
    to the exact required-contexts list (it is in `setup_branch_protection.sh` REQUIRED_CHECKS but missing
    here — D-09). Add a release-gate note that green trust evidence is required: the `Trust Lane Repo Head`
    required check, the clean-baseline + published-version trust journeys (the EVID-02/EVID-03 lanes), and
    the `trust-runner-repo-head` / `trust-runner-clean-baseline` / `trust-runner-published` checkpoint
    artifacts (D-08).
    (b) In the "Verify CI green" runbook step (around 241-249): add the `Trust Lane Repo Head (Elixir 1.18
    / OTP 27)` bucket to the required release-truth buckets, and add a maintenance-cadence note that the
    post-publish trust journey (the EVID-03 sentinel) must be GREEN before milestone trust claims / v1.3
    closeout (D-08).
    (c) Fix all THREE stale approval-gate regions to the hands-free reality (D-10, RESEARCH Pitfall 7):
    - Line 24: replace the "environment-gated and requires manual approval in the GitHub Actions UI"
      sentence with the hands-free fact (release-please auto-merges on green; `gate-ci-green` is the publish
      gate; the `hex-publish` environment has no required reviewers).
    - "Bus Factor & Continuity" (178-188): replace the "single required reviewer (`szTheory`)" / "one-eye
      pause" description with the hands-free posture; keep the legitimate bus-factor / Hex-transfer content.
    - Release Runbook step 3 (260-266): REPLACE (do not delete) the "Approve the `hex-publish` deployment"
      step with a "Monitor the hands-free publish fan-out" step (verify `gate-ci-green` passed and the
      publish jobs fanned out; keep package-order/idempotency/fallback sub-bullets); preserve the 5-step
      numbering and the 60-minute-timer step. Drop the "approver identity / approval timestamp" recording
      that references a non-existent approval.
    - Line 277: bump the smoke-step deps from `~> 1.2`/`~> 1.2`/`~> 0.2` to `~> 1.3`/`~> 1.3`/`~> 0.3`.
    Do NOT edit `scripts/setup_branch_protection.sh` (its REQUIRED_CHECKS already matches; the doc must
    catch up to it, not the reverse) and do NOT add a clean-baseline entry anywhere as a required check (D-04).
  </action>
  <verify>
    <automated>grep -q 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' MAINTAINING.md && grep -qE 'trust-runner-(repo-head|clean-baseline|published)' MAINTAINING.md && ! grep -q 'requires manual approval in the GitHub Actions UI' MAINTAINING.md && ! grep -q 'Approve the `hex-publish` deployment' MAINTAINING.md && ! grep -q 'single required reviewer' MAINTAINING.md && ! grep -q '~> 0.2' MAINTAINING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `MAINTAINING.md` contains the literal string `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` in the Required Checks section.
    - `MAINTAINING.md` references the `trust-runner-` checkpoint artifacts (matches `trust-runner-(repo-head|clean-baseline|published)`).
    - `MAINTAINING.md` contains a maintenance-cadence statement that the post-publish trust journey must be green before milestone trust claims / v1.3 closeout.
    - `MAINTAINING.md` does NOT contain `requires manual approval in the GitHub Actions UI` (line 24 region fixed).
    - `MAINTAINING.md` does NOT contain ``Approve the `hex-publish` deployment`` (runbook step 3 replaced with a monitor step; 5-step numbering preserved).
    - `MAINTAINING.md` does NOT contain `single required reviewer` (Bus Factor region fixed).
    - `MAINTAINING.md` does NOT contain `~> 0.2` (line 277 bumped to `~> 0.3`); the smoke-step deps read `~> 1.3` / `~> 1.3` / `~> 0.3`.
    - `scripts/setup_branch_protection.sh` is unchanged.
  </acceptance_criteria>
  <done>
    `MAINTAINING.md` requires green trust evidence as a release gate, lists Trust Lane Repo Head in Required
    Checks, describes the hands-free publish in all three formerly-stale regions, and the smoke-step deps
    read the 1.3.x line.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the OPS-02 doc-contract test and the D-04 REQUIRED_CHECKS refute</name>
  <files>test/mailglass/publish/maintaining_release_gate_contract_test.exs, test/scripts/required_checks_test.exs</files>
  <read_first>
    - test/mailglass/install/install_first_preview_smoke_test.exs (lines 24-39 — the File.read!/assert-string precedent to mirror)
    - test/mailglass/publish/installer_golden_check_test.exs (the `Mailglass.Publish.*` module-naming convention for the new test's namespace)
    - test/scripts/required_checks_test.exs (parse_required_checks/1 at 45-52; the two existing tests; @v1_0_lock_entries)
    - MAINTAINING.md (the post-Task-1 content the doc-contract test asserts against)
    - .planning/phases/60-release-trust-gate-drift-prevention/60-RESEARCH.md ("OPTIONAL OPS-02 doc-contract test" precedent block; Validation Architecture Wave-0 hardening item)
  </read_first>
  <behavior>
    - doc-contract test: PASSES after Task 1 — `MAINTAINING.md` contains `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` and a `trust-runner-(repo-head|clean-baseline|published)` reference; FAILS if the stale strings `requires manual approval in the GitHub Actions UI` or ``Approve the `hex-publish` deployment`` or `single required reviewer` reappear.
    - required_checks refute: PASSES with the current REQUIRED_CHECKS (which has no clean-baseline entry) — `Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)` is NOT a member; would FAIL if a clean-baseline entry were ever added to REQUIRED_CHECKS (locks D-04).
  </behavior>
  <action>
    Create `test/mailglass/publish/maintaining_release_gate_contract_test.exs` defining
    `Mailglass.Publish.MaintainingReleaseGateContractTest` (`use ExUnit.Case, async: true`). Read
    `MAINTAINING.md` via `Path.expand("../../../MAINTAINING.md", __DIR__)`. Assert the trust-evidence
    strings are present (`Trust Lane Repo Head (Elixir 1.18 / OTP 27)` and a `~r/trust-runner-(repo-head|clean-baseline|published)/`
    match) and `refute` the three stale approval-gate strings
    (`requires manual approval in the GitHub Actions UI`; `~r/Approve the `hex-publish` deployment/`;
    `single required reviewer`). Follow the precedent shape in RESEARCH "OPTIONAL OPS-02 doc-contract test".

    Then extend `test/scripts/required_checks_test.exs` with a new test
    ("clean-baseline lane is NOT a required branch-protection check (D-04)") that calls
    `parse_required_checks/1` on the script source and `refute MapSet.member?(array_set, "Trust Lane Clean
    Baseline (Elixir 1.18 / OTP 27)")`. Keep the two existing tests intact; reuse the private parser. Do NOT
    change `scripts/setup_branch_protection.sh`.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/mailglass/publish/maintaining_release_gate_contract_test.exs test/scripts/required_checks_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/mailglass/publish/maintaining_release_gate_contract_test.exs` defines `Mailglass.Publish.MaintainingReleaseGateContractTest`, reads `MAINTAINING.md` via `Path.expand("../../../MAINTAINING.md", __DIR__)`, asserts the two trust-evidence strings present and refutes the three stale approval-gate strings.
    - `test/scripts/required_checks_test.exs` has a new test that refutes membership of `Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)` in the parsed REQUIRED_CHECKS, and the two pre-existing tests still pass.
    - `MIX_ENV=test mix test test/mailglass/publish/maintaining_release_gate_contract_test.exs test/scripts/required_checks_test.exs` exits 0.
    - `scripts/setup_branch_protection.sh` is unchanged.
  </acceptance_criteria>
  <done>
    Two deterministic local guards exist: one fails if MAINTAINING.md loses the trust-evidence language or
    regains a stale approval-gate line; one fails if the clean-baseline lane is ever added to
    REQUIRED_CHECKS. Both pass against the current tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MAINTAINING.md prose -> maintainer action | The runbook is human-facing; stale instructions cause a maintainer to look for an approval that does not exist. |
| Doc-contract test -> CI | The only enforcement of the OPS-02 release-gate language is the ExUnit string-assert; without it, drift is undetected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-12 | Repudiation | OPS-02 release-gate language silently drifts / a stale approval-gate line reappears | mitigate | Doc-contract test asserts trust-evidence strings present + the three stale strings absent (RESEARCH "OPTIONAL OPS-02 doc-contract test"). |
| T-60-13 | Tampering | Clean-baseline lane silently added to REQUIRED_CHECKS, flipping D-04 | mitigate | `refute` in `required_checks_test.exs` fails if the clean-baseline name appears in REQUIRED_CHECKS (D-04 hardening). |
| T-60-14 | Information Disclosure | Maintainer doc describes a control stronger than reality | mitigate | All three stale regions reconciled to the honest hands-free posture (D-10). |
| T-60-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager install in this plan; docs + tests only. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/mailglass/publish/maintaining_release_gate_contract_test.exs test/scripts/required_checks_test.exs` exits 0.
- Manual grep proofs of the absent/present strings (see Task 1 verify) confirm the three stale regions are reconciled.
</verification>

<success_criteria>
- MAINTAINING.md requires green trust evidence, lists Trust Lane Repo Head in Required Checks, and has no stale approval-gate prose in any of the three regions; smoke-step deps read the 1.3.x line.
- Doc-contract test and the D-04 refute exist and pass against the current tree.
</success_criteria>

<output>
Create `.planning/phases/60-release-trust-gate-drift-prevention/60-04-SUMMARY.md` when done.
</output>
