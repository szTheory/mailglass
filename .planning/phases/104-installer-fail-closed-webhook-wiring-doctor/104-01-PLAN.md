---
phase: 104-installer-fail-closed-webhook-wiring-doctor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/install/install_fail_closed_test.exs
  - test/mailglass/install/mailglass_doctor_test.exs
autonomous: true
requirements: [INSTALL-01, INSTALL-02, INSTALL-03, INSTALL-04]

must_haves:
  truths:
    - "Both new test files exist, compile, and run under the installer lane"
    - "install_fail_closed_test.exs RED-asserts the {:error, {:unmanaged_parser_conflict, _}} tuple via a direct Apply.run/2 call (not a message string)"
    - "install_fail_closed_test.exs RED-asserts --force ordering (managed block byte index < unmanaged plug Plug.Parsers index) and artifact compile"
    - "mailglass_doctor_test.exs RED-asserts the doctor runner's 0/1/2 summary→exit mapping (wired / unwired / endpoint-missing)"
    - "Every conflict-seeding setup writes a bare plug Plug.Parsers with NO body_reader OUTSIDE the managed markers (no vacuous pass)"
  artifacts:
    - path: "test/mailglass/install/install_fail_closed_test.exs"
      provides: "Failing tests for INSTALL-01 (tuple + non-zero exit), INSTALL-02 (--force ordering)"
      contains: "unmanaged_parser_conflict"
    - path: "test/mailglass/install/mailglass_doctor_test.exs"
      provides: "Failing tests for INSTALL-03 (doctor 0/1/2 exit mapping)"
      contains: "Mailglass.Installer.Doctor"
  key_links:
    - from: "test/mailglass/install/install_fail_closed_test.exs"
      to: "Mailglass.Installer.Apply.run/2"
      via: "direct call inside File.cd!(fixture_root, ...)"
      pattern: "Apply\\.run"
    - from: "test/mailglass/install/mailglass_doctor_test.exs"
      to: "Mailglass.Installer.Doctor.run/1"
      via: "direct call inside File.cd!(fixture_root, ...)"
      pattern: "Doctor\\.run"
---

<objective>
Wave 0 (tests-first). Create the two failing ExUnit test files that pin Phase 104's
observable behaviors BEFORE any implementation exists, following the
`install_idempotency_test.exs` fixture pattern. These tests are RED on creation
(the `{:error, {:unmanaged_parser_conflict, _}}` return, the `--force` ordering, and
the `Mailglass.Installer.Doctor` runner all do not exist yet) and turn GREEN when
Plan 02 lands.

Purpose: The project requires tests-first for this phase (INSTALL-04, CONTEXT D-11..D-14,
104-VALIDATION.md Wave 0). Landing the contract tests first prevents the implementation
from drifting and encodes the seeding footgun and the struct/tuple-match DNA rule up front.

Output: `test/mailglass/install/install_fail_closed_test.exs` and
`test/mailglass/install/mailglass_doctor_test.exs`, both compiling and RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-CONTEXT.md
@.planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-RESEARCH.md
@.planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-PATTERNS.md
@.planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-VALIDATION.md

<interfaces>
<!-- Seams the tests call. Extracted from live source — no codebase exploration needed. -->

From test/support/installer_fixture_helpers.ex (module Mailglass.Test.InstallerFixtureHelpers):
  new_fixture_root!(name :: binary) :: fixture_root :: binary
    # copies test/support/example, ensures bare host skeleton, returns the temp root
  run_install!(fixture_root :: binary, argv :: [binary]) :: :ok
    # runs inside File.cd!(fixture_root, ...); builds Plan + calls Apply.run/2.
    # On {:error, reason} it RE-RAISES a RuntimeError embedding inspect(reason) in the
    # MESSAGE — the original tuple is NOT propagated (lines 41-43).
  assert_generated_artifacts_compile!(fixture_root :: binary) :: :ok
    # syntax-parses every generated .ex/.exs/.heex; raises naming the offending file (line 64)

  # host_endpoint/0 skeleton (lines 263-269) emits ONLY:
  #   defmodule ExampleWeb.Endpoint do
  #     use Phoenix.Endpoint, otp_app: :example
  #   end
  # NO plug Plug.Parsers — so the conflict must be SEEDED by overwriting the file.

From lib/mailglass/installer/plan.ex:
  Mailglass.Installer.Plan.build(opts :: keyword, ctx :: map) :: [Operation.t()]
    # ctx is %{oban_available?: boolean()}; in tests use
    # %{oban_available?: Mailglass.OptionalDeps.Oban.available?()}
  Mailglass.Installer.Plan.detect_otp_app() :: atom   # fixture resolves to :example

From lib/mailglass/installer/apply.ex:
  Mailglass.Installer.Apply.run(plan :: [Operation.t()], opts :: keyword) ::
    {:ok, map()} | {:error, term()}
    # AFTER Plan 02: returns {:error, {:unmanaged_parser_conflict, endpoint_path}}
    # on an unmanaged-parser conflict unless opts has force: true.

CONTRACT this plan's tests target (implemented by Plan 02 — they are RED until then):
  Mailglass.Installer.Doctor.run(opts :: keyword) ::
    %{summary: %{pass: n, warn: n, fail: n, cannot_diagnose: n}, findings: [map()]}
    # static scan of lib/<app>_web/endpoint.ex relative to cwd; called inside File.cd!(fixture_root).
    # summary→exit mapping (the doctor mix task's exit_code/2): cannot_diagnose>0 → 2,
    # fail>0 → 1, else 0.
</interfaces>

<seed_body>
<!-- The MANDATORY conflict seed (104-RESEARCH.md lines 207-216). NO body_reader, OUTSIDE
     managed markers — otherwise the test passes vacuously (seeding footgun). Write this
     into lib/example_web/endpoint.ex AFTER new_fixture_root!/1: -->
defmodule ExampleWeb.Endpoint do
  use Phoenix.Endpoint, otp_app: :example

  plug Plug.Parsers,
    parsers: [:urlencoded, :json],
    pass: ["*/*"],
    json_decoder: Jason
end
</seed_body>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing install_fail_closed_test.exs (INSTALL-01, INSTALL-02, INSTALL-04)</name>
  <files>test/mailglass/install/install_fail_closed_test.exs</files>
  <read_first>
    - test/mailglass/install/install_fail_closed_test.exs (the file being created)
    - test/mailglass/install/install_idempotency_test.exs (header + fixture pattern: `use ExUnit.Case, async: false`, `import Mailglass.Test.InstallerFixtureHelpers`, `new_fixture_root!/1` → `run_install!/2`)
    - test/support/installer_fixture_helpers.ex (lines 1-75 for `new_fixture_root!/1`, `run_install!/2`, `assert_generated_artifacts_compile!/1`; lines 263-269 for the bare `host_endpoint/0` skeleton)
    - .planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-PATTERNS.md (section "install_fail_closed_test.exs (CREATE)" — exact assertions)
    - .planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-RESEARCH.md (Pitfall 1 + the seeding footgun, lines 194-219)
    - CLAUDE.md (engineering DNA: match errors by struct/tuple, never message string)
  </read_first>
  <action>
    Create `test/mailglass/install/install_fail_closed_test.exs` as
    `defmodule Mailglass.Install.FailClosedTest`, `use ExUnit.Case, async: false`,
    `import Mailglass.Test.InstallerFixtureHelpers`.

    Provide a private helper that calls `new_fixture_root!/1` then `File.write!`-overwrites
    `lib/example_web/endpoint.ex` (joined to the fixture root) with the seed body from the
    `<seed_body>` context block — a bare `plug Plug.Parsers` with NO `body_reader`, OUTSIDE the
    managed markers. This seeding is mandatory; without it the conflict guard is never tripped
    and the tests pass vacuously (104-RESEARCH.md seeding footgun).

    Write three test cases:

    1. INSTALL-01 tuple-level (per D-12 / Pitfall 1): inside `File.cd!(fixture_root, fn -> ... end)`,
       build the plan with `Mailglass.Installer.Plan.build([], %{oban_available?: Mailglass.OptionalDeps.Oban.available?()})`
       and assert `{:error, {:unmanaged_parser_conflict, _path}} = Mailglass.Installer.Apply.run(plan, [])`.
       Match the TUPLE — never the message string. Do NOT route this assertion through `run_install!/2`
       (it re-raises a RuntimeError embedding only `inspect(reason)` in the message).

    2. INSTALL-01 task-level non-zero exit: `assert_raise` (RuntimeError is acceptable here — the
       raised exit is the contract surface) around `run_install!(fixture_root, [])`, and assert the
       raised message names the endpoint path `lib/example_web/endpoint.ex`. This is the ONE place a
       message assertion is allowed (the message IS the user-facing contract).

    3. INSTALL-02 `--force` ordering (per D-13 / Pitfall 4): `run_install!(fixture_root, ["--force"])`,
       then read the patched `lib/example_web/endpoint.ex`. Assert the managed start-marker
       (`Mailglass.Installer.Templates.endpoint_webhook_block_start()`) byte index is strictly LESS THAN
       the unmanaged `plug Plug.Parsers` byte index — use `:binary.match/2` on the seeded (unmanaged)
       occurrence. Then call `assert_generated_artifacts_compile!(fixture_root)`. Assert ORDERING, not
       mere success.

    These tests are EXPECTED TO FAIL on creation (the fail-closed return and `format_error` clause do
    not exist yet) — that is the RED state Wave 0 requires. No fenced implementation code goes in this
    action; follow the analog file structure in `<read_first>`.
  </action>
  <verify>
    <automated>mix test test/mailglass/install/install_fail_closed_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `test/mailglass/install/install_fail_closed_test.exs` exists and COMPILES (no syntax/alias errors).
    - `mix test test/mailglass/install/install_fail_closed_test.exs` RUNS and reports failures (RED) — the
      failures are assertion/behavior failures, NOT CompileError/UndefinedFunctionError on the test's own
      helpers (`new_fixture_root!`, `run_install!`, `Plan.build`, `Apply.run`, `Templates.endpoint_webhook_block_start`
      all already exist).
    - The tuple test contains a literal `{:error, {:unmanaged_parser_conflict,` pattern-match (grep:
      `grep -c 'unmanaged_parser_conflict' test/mailglass/install/install_fail_closed_test.exs` ≥ 1).
    - The `--force` test asserts a byte-index ordering via `:binary.match` AND calls
      `assert_generated_artifacts_compile!` (both present in the file).
    - The seed helper writes a `plug Plug.Parsers` with NO `body_reader` substring
      (the seed body contains no `body_reader` text).
  </acceptance_criteria>
  <done>install_fail_closed_test.exs compiles and is RED with the three INSTALL-01/02 contract assertions, seeded correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing mailglass_doctor_test.exs (INSTALL-03, INSTALL-04)</name>
  <files>test/mailglass/install/mailglass_doctor_test.exs</files>
  <read_first>
    - test/mailglass/install/mailglass_doctor_test.exs (the file being created)
    - test/mailglass/install/install_idempotency_test.exs (header + fixture pattern)
    - test/support/installer_fixture_helpers.ex (lines 1-75: `new_fixture_root!/1`, `run_install!/2`, `assert_generated_artifacts_compile!/1`)
    - .planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-PATTERNS.md (section "mailglass_doctor_test.exs (CREATE)" — the three cases + summary→exit mapping)
    - .planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-RESEARCH.md (Validation Architecture INSTALL-03 row; D-10 exit-code semantics)
  </read_first>
  <action>
    Create `test/mailglass/install/mailglass_doctor_test.exs` as
    `defmodule Mailglass.Install.MailglassDoctorTest`, `use ExUnit.Case, async: false`,
    `import Mailglass.Test.InstallerFixtureHelpers`.

    The tests target the CONTRACT `Mailglass.Installer.Doctor.run(opts) ::
    %{summary: %{pass, warn, fail, cannot_diagnose}, findings: [...]}` (implemented in Plan 02 —
    these tests are RED until then). Call the runner directly inside `File.cd!(fixture_root, fn -> ... end)`
    so `detect_otp_app/0` and the relative `lib/example_web/endpoint.ex` path resolve against the fixture
    (D-14). Prefer asserting the summary map (and, if cheap, the doctor mix task's `exit_code/2` mapping)
    over spawning a real OS exit.

    Write three test cases:

    1. INSTALL-03 wired → exit 0: `run_install!(fixture_root, [])` on a fresh fixture (the installer
       wires the managed `CachingBodyReader` block), then
       `summary = File.cd!(fixture_root, fn -> Mailglass.Installer.Doctor.run([]) end).summary`;
       assert `summary.fail == 0 and Map.get(summary, :cannot_diagnose, 0) == 0` (maps to exit 0).

    2. INSTALL-03 unwired → exit 1: use a fixture whose endpoint has NO `body_reader` /
       `Mailglass.Webhook.CachingBodyReader` (either a fresh `new_fixture_root!/1` WITHOUT running the
       installer, or strip the managed block after install). Assert `summary.fail > 0` and
       `Map.get(summary, :cannot_diagnose, 0) == 0` (maps to exit 1).

    3. INSTALL-03 cannot-diagnose → exit 2: delete `lib/example_web/endpoint.ex` from the fixture
       (so the static scan finds no endpoint). Assert `Map.get(summary, :cannot_diagnose, 0) > 0`
       (maps to exit 2).

    These tests are EXPECTED TO FAIL on creation (the `Mailglass.Installer.Doctor` module does not exist
    yet) — RED is correct. No fenced implementation code in this action; follow the analog structure in
    `<read_first>`.
  </action>
  <verify>
    <automated>mix test test/mailglass/install/mailglass_doctor_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `test/mailglass/install/mailglass_doctor_test.exs` exists and COMPILES.
    - `mix test test/mailglass/install/mailglass_doctor_test.exs` RUNS and reports failures (RED) — the
      expected failure is an `UndefinedFunctionError`/module-not-found for `Mailglass.Installer.Doctor.run/1`
      (that module is created in Plan 02), NOT a syntax error in the test's own helper calls.
    - The file contains assertions on all three states: `summary.fail == 0` AND `cannot_diagnose == 0`
      (wired/0), `summary.fail > 0` (unwired/1), and `cannot_diagnose > 0` (missing/2)
      (grep: `grep -c 'cannot_diagnose' test/mailglass/install/mailglass_doctor_test.exs` ≥ 2).
    - At least one assertion runs the runner inside `File.cd!(fixture_root, ...)`.
  </acceptance_criteria>
  <done>mailglass_doctor_test.exs compiles and is RED, asserting the 0/1/2 summary states against the Doctor runner contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none, this plan) | Test-only plan. No network, DB, auth, secrets, or untrusted input. Tests run offline against an install fixture in a temp directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | Vacuous test pass (conflict-seed missing `body_reader`/marker discipline) | mitigate | Seed helper writes a `plug Plug.Parsers` with NO `body_reader` OUTSIDE managed markers (acceptance criteria enforce the absent `body_reader` substring); a vacuous pass would not surface the unmanaged-parser conflict the phase exists to catch. |
| T-104-02 | Repudiation | Asserting the fail-closed error by message string (DNA violation) | mitigate | Tuple test pattern-matches `{:error, {:unmanaged_parser_conflict, _}}` via a direct `Apply.run/2` call; message assertion confined to the one task-level non-zero-exit case where the message IS the user contract. |
| T-104-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure stdlib + existing project modules (104-RESEARCH.md: "No Package Legitimacy Audit needed"). No `[ASSUMED]`/`[SUS]` packages. |

Note: this phase as a whole CLOSES a security/correctness footgun (a silent production webhook 401
from an unmanaged `Plug.Parsers` consuming the raw body before signature verification). This Wave 0
plan encodes the contract that proves the footgun is closed; it introduces no new attack surface.
</threat_model>

<verification>
- `mix test test/mailglass/install/install_fail_closed_test.exs` — compiles, runs, RED (behavior failures).
- `mix test test/mailglass/install/mailglass_doctor_test.exs` — compiles, runs, RED (Doctor module absent).
- Do NOT run bare `mix test` (unrelated Oban + `voice_test` "Oops" dep-JS flakes per project MEMORY).
- Both files import `Mailglass.Test.InstallerFixtureHelpers` and use `async: false`.
</verification>

<success_criteria>
- Both test files exist, compile, and are RED for the right reason (missing implementation, not test bugs).
- Conflict seeding follows the footgun rule (no `body_reader`, outside managed markers).
- INSTALL-01 tuple assertion matches the struct/tuple, never a message string.
- INSTALL-02 asserts `--force` ORDERING (byte index) + artifact compile, not mere success.
- INSTALL-03 asserts all three doctor states (0/1/2) on the runner's summary map.
</success_criteria>

<output>
Create `.planning/phases/104-installer-fail-closed-webhook-wiring-doctor/104-01-SUMMARY.md` when done
</output>
