---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
audit_blocker: B-1
decisions_addressed: [D-06, D-07]

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table flips ALL ~25 stale `Pending` rows enumerated in v0.1-MILESTONE-AUDIT.md lines 213-227 to `Complete (XX-YY)`"
    - "INST-01 and INST-03 rows STAY `Pending` (D-06 explicit hold — they only flip after Plan 06 installer fixes land)"
    - "All other Phase 7 rows (CI-01..07, DOCS-01..05, BRAND-02..03, INST-02, INST-04, TEST-04) flip to `Complete (07-NN)`"
    - "All Phase 3 rows flip to `Complete (03-NN)` per audit list"
    - "All Phase 4 rows flip per audit list (HOOK-01, HOOK-03..05 transition)"
    - "All Phase 5 rows (PREV-01..06 + BRAND-01) flip to `Complete (05-NN)`"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Up-to-date REQ traceability matrix"
      contains: "| INST-01 | Phase 7"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "v0.1-MILESTONE-AUDIT.md lines 213-227 enumerated list"
      via: "lifted REQ-IDs verbatim per D-06"
      pattern: "Complete \\(\\d{2}-\\d{2}"
---

<objective>
Flip the ~25 stale `Pending` rows in `.planning/REQUIREMENTS.md` traceability table to `Complete (XX-YY)` referencing the delivering plan. Per D-06: list is the audit's enumerated "REQ-IDs that are SATISFIED but not marked Complete" section (`v0.1-MILESTONE-AUDIT.md` lines 211-227). Per D-06: KEEP `INST-01` and `INST-03` as `Pending` until Plan 06 installer fixes land — those flips happen in Plan 06's commit.

Per D-07: bookkeeping commits stay separate from product-fix commits. This plan is its own commit.

Purpose: Audit close-out blocker B-1. Without this flip, the REQUIREMENTS.md trace table still shows ~25 satisfied features as `Pending`, defeating the milestone close-out signal.

Output: One markdown file edited in place. Pure textual edits in the traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip ~25 stale Pending rows in REQUIREMENTS.md traceability table per D-06</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 264-353) — the entire traceability table; identify each row that needs flipping
    - .planning/v0.1-MILESTONE-AUDIT.md (lines 211-227) — authoritative enumerated list of REQ-IDs to flip + which plan delivered them
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (line 43) — D-06 verbatim ("Keep INST-01 + INST-03 'Pending' until D-02 lands")
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 749-754) — pattern map for this textual flip
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` traceability table (lines 264-353 in current file). Apply ALL the following row flips. Each edit is purely textual: find the row matching `| <REQ-ID> | <Phase line> | Pending |` and replace `Pending` with `Complete (<XX-YY>)`.

    **REQ-IDs to flip (all from audit lines 213-227 — these are SATISFIED in the live codebase per audit verification):**

    Phase 3 rows (lift `XX-YY` plan numbers from audit's parenthetical citations):

    | REQ-ID | New status |
    |---|---|
    | AUTHOR-01 | Complete (03-04) |
    | TRANS-01 | Complete (03-02) |
    | TRANS-02 | Complete (03-02) |
    | TRANS-03 | Complete (03-02) |
    | TRANS-04 | Complete (03-02) |
    | SEND-01 | Complete (03-01) |
    | SEND-02 | Complete (03-03) |
    | SEND-03 | Complete (03-05) |
    | SEND-04 | Complete (03-03) |
    | SEND-05 | Complete (03-01) |
    | TRACK-01 | Complete (03-04) |
    | TRACK-03 | Complete (03-07, 03-08) |
    | TEST-01 | Complete (03-06) |
    | TEST-02 | Complete (03-06) |
    | TEST-05 | Complete (03-12) |

    Phase 4 rows (the audit notes HOOK-02 + HOOK-06 + HOOK-07 + TEST-03 are already flipped; these four need no edit. The remaining four DO need flipping per audit lines 221-222):

    | REQ-ID | New status |
    |---|---|
    | HOOK-01 | Complete (04-01, 04-02) |
    | HOOK-03 | Complete (04-02) |
    | HOOK-04 | Complete (04-03) |
    | HOOK-05 | Complete (04-02, 04-03) |

    Phase 5 rows (PREV-01..06 + BRAND-01 — all live per audit):

    | REQ-ID | New status |
    |---|---|
    | PREV-01 | Complete (05-02) |
    | PREV-02 | Complete (05-03) |
    | PREV-03 | Complete (05-04, 05-06) |
    | PREV-04 | Complete (05-04) |
    | PREV-05 | Complete (05-05, 05-06) |
    | PREV-06 | Complete (05-05) |
    | BRAND-01 | Complete (05-05) |

    Phase 7 rows (audit confirmed SATISFIED for: TEST-04, INST-02, INST-04, CI-01..07, DOCS-01..05, BRAND-02, BRAND-03 — but EXPLICITLY NOT INST-01, INST-03 per D-06):

    | REQ-ID | New status |
    |---|---|
    | TEST-04 | Complete (07-04) |
    | INST-02 | Complete (07-01) |
    | INST-04 | Complete (07-02) |
    | CI-01 | Complete (07-04) |
    | CI-02 | Complete (07-04) |
    | CI-03 | Complete (07-04) |
    | CI-04 | Complete (07-04, 07-05) |
    | CI-05 | Complete (07-04) |
    | CI-06 | Complete (07-04) |
    | CI-07 | Complete (07-04, 07-05) |
    | DOCS-01 | Complete (07-03) |
    | DOCS-02 | Complete (07-03) |
    | DOCS-03 | Complete (07-03) |
    | DOCS-04 | Complete (07-03) |
    | DOCS-05 | Complete (07-03) |
    | BRAND-02 | Complete (07-01, 07-03) |
    | BRAND-03 | Complete (07-03) |

    **HOLD — DO NOT FLIP per D-06:**

    | REQ-ID | Current state (preserve) |
    |---|---|
    | INST-01 | Pending |
    | INST-03 | Pending |

    These two stay `Pending`. They will flip to `Complete (07.1-06)` in Plan 06's commit (the installer-fix commit), per D-07 separation of bookkeeping vs product-fix commits.

    **Approach:**
    1. For each flip, find the row by exact REQ-ID match (e.g. `grep -n "^| AUTHOR-01 |" .planning/REQUIREMENTS.md` to locate).
    2. Edit ONLY the third column (status). Do NOT change the second column (phase name).
    3. Verify by reading the table back after edits — line counts must match (no rows added or removed; only third-column text changes).

    **Citations are honest:** if a REQ-ID was satisfied across multiple plans (e.g. TRACK-03 satisfied in plans 03-07 + 03-08), include both: `Complete (03-07, 03-08)`. The exact `XX-YY` mapping comes from audit lines 213-227 plus cross-reference with plan SUMMARY artifacts where the audit cites a single plan but the SUMMARY shows distribution.

    **Anti-pattern to avoid:** do NOT add a "Notes" column, do NOT add a "Verified at" column, do NOT add a `closure_plan` column. The current 3-column table format (`Requirement | Phase | Status`) is preserved.
  </action>
  <verify>
    <automated>grep -E "^\| [A-Z][A-Z0-9-]+ \| Phase [0-9]+" .planning/REQUIREMENTS.md | grep -c "Pending"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\| [A-Z][A-Z0-9-]+ \| Phase [0-9]+" .planning/REQUIREMENTS.md | grep -c "Pending"` returns exactly `2` (only INST-01 + INST-03 remain Pending; all others flipped)
    - `grep "^| INST-01 |" .planning/REQUIREMENTS.md | grep -F "| Pending |" | wc -l` returns `1` (held per D-06)
    - `grep "^| INST-03 |" .planning/REQUIREMENTS.md | grep -F "| Pending |" | wc -l` returns `1` (held per D-06)
    - `grep "^| AUTHOR-01 |" .planning/REQUIREMENTS.md | grep -F "Complete (03-04)" | wc -l` returns `1`
    - `grep "^| HOOK-01 |" .planning/REQUIREMENTS.md | grep -F "Complete (04-01" | wc -l` returns `1`
    - `grep "^| HOOK-04 |" .planning/REQUIREMENTS.md | grep -F "Complete (04-03)" | wc -l` returns `1`
    - `grep "^| PREV-01 |" .planning/REQUIREMENTS.md | grep -F "Complete (05-02)" | wc -l` returns `1`
    - `grep "^| PREV-06 |" .planning/REQUIREMENTS.md | grep -F "Complete (05-05)" | wc -l` returns `1`
    - `grep "^| BRAND-01 |" .planning/REQUIREMENTS.md | grep -F "Complete (05-05)" | wc -l` returns `1`
    - `grep "^| TEST-04 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-04)" | wc -l` returns `1`
    - `grep "^| CI-01 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-04)" | wc -l` returns `1`
    - `grep "^| CI-07 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-04, 07-05)" | wc -l` returns `1`
    - `grep "^| DOCS-01 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-03)" | wc -l` returns `1`
    - `grep "^| DOCS-05 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-03)" | wc -l` returns `1`
    - `grep "^| BRAND-02 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-01, 07-03)" | wc -l` returns `1`
    - `grep "^| INST-02 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-01)" | wc -l` returns `1`
    - `grep "^| INST-04 |" .planning/REQUIREMENTS.md | grep -F "Complete (07-02)" | wc -l` returns `1`
    - Row count unchanged: `grep -cE "^\| [A-Z][A-Z0-9-]+ \| Phase " .planning/REQUIREMENTS.md` returns `84` (84 REQ-IDs total per ROADMAP coverage line 354)
  </acceptance_criteria>
  <done>
    `.planning/REQUIREMENTS.md` traceability table has exactly 2 `Pending` rows remaining (INST-01 + INST-03 held per D-06). All other previously-Pending rows for Phases 3, 4, 5, 7 are flipped to `Complete (XX-YY)` with the plan citation matching `v0.1-MILESTONE-AUDIT.md` lines 211-227 enumeration. Row count is unchanged at 84. Phase 1, 2, 6 rows are untouched (they were already `Complete` before this edit).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md trace → milestone close-out logic | Trace table is the trust signal that requirements are landed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-05-01 | R (Repudiation) | Premature flip of INST-01 / INST-03 | mitigate | Per D-06, both rows are EXPLICITLY held `Pending` until Plan 06 installer fixes land. Acceptance criteria asserts BOTH stay Pending in this commit. Flipping early would be a false-positive milestone-close signal. |
| T-07.1-05-02 | T (Tampering) | Plan citation accuracy | mitigate | Citations lift directly from audit lines 213-227 enumeration. Where the audit cites a single plan (e.g. AUTHOR-01 cited as 03-04), the trace uses that plan number. Where audit cites multiple plans (e.g. TRACK-03), all are listed. No invented `XX-YY` numbers. |
| T-07.1-05-03 | I | Surfaced plan numbers | accept | Plan numbers (e.g. `03-04`) are project-internal references, no external surface affected (file is .planning/, removed from Hex tarball per Plan 01 D-23). |
</threat_model>

<verification>
After this task lands:
```bash
# Pending count must be exactly 2 (INST-01 + INST-03 only)
grep -E "^\| [A-Z][A-Z0-9-]+ \| Phase [0-9]+" .planning/REQUIREMENTS.md | grep -c "Pending"
# Expected: 2

# Total REQ-ID rows unchanged
grep -cE "^\| [A-Z][A-Z0-9-]+ \| Phase " .planning/REQUIREMENTS.md
# Expected: 84
```
</verification>

<success_criteria>
1. Exactly 2 rows in `.planning/REQUIREMENTS.md` traceability table contain `Pending` (INST-01 + INST-03 — held per D-06).
2. All other previously-Pending rows from `v0.1-MILESTONE-AUDIT.md` lines 213-227 are flipped to `Complete (XX-YY)`.
3. Total row count in traceability table is unchanged at 84.
4. No phase column was edited (only the third "Status" column).
5. No new columns were added to the table.
6. Phase 1, 2, 6 rows are untouched (already `Complete` before this edit).
7. Plan citations match the audit enumeration verbatim (single-plan citations match audit; multi-plan citations include all referenced plans).
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-05-SUMMARY.md`.
</output>
