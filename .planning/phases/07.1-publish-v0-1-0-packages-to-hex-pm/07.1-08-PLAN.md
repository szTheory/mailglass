---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - .github/workflows/publish-hex.yml
autonomous: true
requirements: []
audit_blocker: null
decisions_addressed: [D-12, D-13, D-14, D-15, D-16, D-25]

must_haves:
  truths:
    - "publish-hex.yml `if:` gate is the D-12 compound expression: `(workflow_dispatch) || (workflow_run + conclusion=success + head_branch matches release tag pattern)` — NOT the current bare `conclusion == 'success'`"
    - "publish-hex.yml has a `workflow_dispatch:` trigger with three inputs: `tag` (required string), `dry_run` (boolean default false), `package` (string choices `mailglass | mailglass_admin | both`, default `both`) per D-13"
    - "publish-hex.yml structure is FOUR sequential `needs:`-chained jobs (not one matrix): `prepublish-summary` → `gate-ci-green` → `publish-core` → `publish-admin` per D-14, D-15, D-16"
    - "`publish-admin` declares `needs: publish-core` AND has a Hex-index polling step (`until mix hex.info mailglass <ver> 2>/dev/null; do sleep 15; done`, max 5 min) per D-14"
    - "`prepublish-summary` runs `mix mailglass.publish.check` for both packages, writes summary to `$GITHUB_STEP_SUMMARY`, and is NOT environment-gated — the reviewer sees the summary BEFORE clicking Approve in the `hex-publish` environment per D-15"
    - "`gate-ci-green` uses `actions/github-script` to query `github.rest.actions.listWorkflowRuns` for the tagged SHA and asserts `conclusion === 'success'` for `ci.yml` per D-16 (this is also how D-25 priv/static/ freshness is enforced — main CI on the tagged SHA is the only assertion)"
    - "All 3rd-party Actions remain pinned to commit SHA per D-34 — no version-tag references introduced"
  artifacts:
    - path: ".github/workflows/publish-hex.yml"
      provides: "Hardened pre-publish CI pipeline (4 sequential jobs)"
      contains: "needs: publish-core"
  key_links:
    - from: "publish-hex.yml prepublish-summary job"
      to: "lib/mix/tasks/mailglass.publish.check.ex (Plan 07)"
      via: "mix mailglass.publish.check invocation that writes $GITHUB_STEP_SUMMARY"
      pattern: "mix mailglass.publish.check"
    - from: "publish-hex.yml gate-ci-green job"
      to: "ci.yml workflow run on tagged SHA"
      via: "github.rest.actions.listWorkflowRuns API query"
      pattern: "listWorkflowRuns"
    - from: "publish-hex.yml publish-admin job"
      to: "Hex.pm package index"
      via: "polling mix hex.info mailglass between core publish and admin publish"
      pattern: "mix hex.info mailglass"
---

<objective>
Harden `.github/workflows/publish-hex.yml` per D-12, D-13, D-14, D-15, D-16, D-25. This is in-place editing — DO NOT rewrite the file. Preserve every existing 3rd-party Action commit-SHA pin per D-34.

Per D-12: tighten the trigger gate from current `if: conclusion == 'success'` (which fires on every release-please success including PR-update runs) to a compound gate that ALSO requires the `head_branch` matches the linked-versions group tag pattern.

Per D-13: add `workflow_dispatch:` as permanent safety valve with `tag` + `dry_run` + `package` inputs.

Per D-14: serialize the matrix — replace `strategy.matrix` with `publish-core` followed by `publish-admin needs: publish-core`. Insert a Hex-index polling step between them so `mailglass_admin`'s `{:mailglass, "== <ver>"}` constraint can resolve.

Per D-15: move `mix mailglass.publish.check` summary emission to a SEPARATE prior job (no environment gate) so the reviewer sees the summary BEFORE clicking Approve. The publish jobs `needs:` this job.

Per D-16: require green `ci.yml` on the tagged SHA before publish runs. Use `actions/github-script` to query the workflow runs API. This is ALSO the D-25 enforcement mechanism for `priv/static/` freshness — main CI is the only thing asserting `git diff --exit-code` on the bundle.

Purpose: The current workflow could double-publish on first run (D-12 race), can't be re-run after partial failure without git surgery (D-13 missing safety valve), is a guaranteed race for sibling-package linked-versions (D-14 matrix parallelism), forces rubber-stamped Approve clicks (D-15 summary post-Approve), and skips the only assertion that the tagged commit is actually green on main CI (D-16). This plan closes all five gates in one PR while preserving SHA-pinning discipline.

Output: One CI YAML file edited in place. No new files. No existing Action SHAs changed (only re-pinning if a NEW Action is introduced — actionlint must still pass with `pin-github-action` rule on).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md
@.github/workflows/publish-hex.yml
@.github/workflows/provider-live.yml
@.github/workflows/release-please.yml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten trigger gate + add workflow_dispatch (D-12, D-13)</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/publish-hex.yml (full current file — 44 lines, single matrix-job structure)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (D-12 line 65, D-13 lines 67-71 — trigger gate + workflow_dispatch inputs)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 530-538 — D-12/D-13 implementation pattern)
    - .github/workflows/release-please.yml (release-please trigger reference; produces `mailglass-sibling-group-v*` tag pattern per D-11)
  </read_first>
  <action>
    Edit `.github/workflows/publish-hex.yml` `on:` block and the top-level `if:` gate.

    **Replace the current `on:` block (lines 3-7):**
    ```yaml
    on:
      workflow_run:
        workflows: ["release-please"]
        types:
          - completed
    ```

    **With this combined `on:` block (D-13 inputs included):**
    ```yaml
    on:
      workflow_run:
        workflows: ["release-please"]
        types:
          - completed
      workflow_dispatch:
        inputs:
          tag:
            description: "Git tag/ref to publish from (e.g. mailglass-sibling-group-v0.1.0)"
            required: true
            type: string
          dry_run:
            description: "Run mix hex.publish --dry-run instead of --yes"
            required: false
            type: boolean
            default: false
          package:
            description: "Package(s) to publish"
            required: false
            type: choice
            options:
              - both
              - mailglass
              - mailglass_admin
            default: both
    ```

    **Add concurrency block** (between `permissions:` and `jobs:`) so a manual dispatch during an automatic run does not double-publish:
    ```yaml
    concurrency:
      group: publish-hex-${{ github.ref }}
      cancel-in-progress: false
    ```
    Note: `cancel-in-progress: false` is intentional — once a publish job has started, it must complete (Hex publish is partially atomic; cancellation mid-step is dangerous).

    **Replace the current job-level `if:` gate** (current line 14: `if: ${{ github.event.workflow_run.conclusion == 'success' }}`) with the D-12 compound gate. Apply this gate to the FIRST job in the chain (which after Task 2 will be `prepublish-summary`); subsequent `needs:`-chained jobs inherit the gate transitively.

    ```yaml
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success' &&
       startsWith(github.event.workflow_run.head_branch, 'mailglass-sibling-group-v'))
    ```

    **Note for Task 2:** the current `strategy.matrix` block (lines 17-21) is REMOVED in Task 2's restructure. Task 1 only edits the `on:` block, adds `concurrency:`, and rewrites the `if:` gate; the rest of the file is reshaped in Task 2. So at the end of Task 1, the YAML still has a single `publish` job with the new `on:` triggers and new compound `if:` — Task 2 then splits that job into four.

    **Preserve verbatim:**
    - `permissions: contents: read` (lines 9-10)
    - All existing 3rd-party Action SHA pins (lines 24, 26, 31)

    **Anti-pattern to avoid:** do NOT change `permissions:` to `write` (the publish jobs only need `contents: read` since they push to Hex.pm, not back to the GitHub repo). Do NOT introduce a new top-level `env:` block — input env vars stay in step `env:` blocks per `provider-live.yml` convention.
  </action>
  <verify>
    <automated>bash -c "grep -F 'workflow_dispatch:' .github/workflows/publish-hex.yml && grep -F 'concurrency:' .github/workflows/publish-hex.yml && grep -F \"startsWith(github.event.workflow_run.head_branch, 'mailglass-sibling-group-v')\" .github/workflows/publish-hex.yml"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "workflow_dispatch:" .github/workflows/publish-hex.yml` returns 1
    - `grep -cF "type: string" .github/workflows/publish-hex.yml` returns at least 1 (D-13 `tag` input)
    - `grep -cF "type: boolean" .github/workflows/publish-hex.yml` returns at least 1 (D-13 `dry_run` input)
    - `grep -cF "type: choice" .github/workflows/publish-hex.yml` returns at least 1 (D-13 `package` input)
    - `grep -cF "default: both" .github/workflows/publish-hex.yml` returns 1 (D-13 default)
    - `grep -cF "concurrency:" .github/workflows/publish-hex.yml` returns 1
    - `grep -cF "cancel-in-progress: false" .github/workflows/publish-hex.yml` returns 1
    - `grep -cF "github.event_name == 'workflow_dispatch'" .github/workflows/publish-hex.yml` returns at least 1 (D-12 compound gate)
    - `grep -cF "startsWith(github.event.workflow_run.head_branch, 'mailglass-sibling-group-v')" .github/workflows/publish-hex.yml` returns 1 (D-12 tag-pattern guard)
    - `grep -cF "permissions:" .github/workflows/publish-hex.yml` returns 1 (preserved)
    - `grep -cF "actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683" .github/workflows/publish-hex.yml` returns at least 1 (existing SHA pin preserved)
    - `grep -cF "erlef/setup-beam@5304e04ea2b355f03681464e683d92e3b2f18451" .github/workflows/publish-hex.yml` returns at least 1 (existing SHA pin preserved)
  </acceptance_criteria>
  <done>
    `publish-hex.yml` `on:` block now declares both `workflow_run` and `workflow_dispatch` triggers, with `tag` (string, required) + `dry_run` (boolean, default false) + `package` (choice, default `both`) inputs. The job-level `if:` is the D-12 compound expression requiring tag-pattern match for `workflow_run` events. A `concurrency:` block prevents double-publish on overlapping triggers. All existing Action SHA pins preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure into four needs-chained jobs (D-14, D-15, D-16)</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/publish-hex.yml (post-Task-1 state — single `publish` job with new on:/if: but still matrix-shaped)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (D-14 lines 73-76, D-15 line 78, D-16 line 80)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 491-538 — full restructure pattern with provider-live.yml analog)
    - .github/workflows/provider-live.yml lines 52-127 (verbatim `actions/github-script` + `github.rest.*` analog for D-16 cross-workflow query and notify-on-failure issue creation pattern)
    - .github/workflows/ci.yml (read top-level workflow `name:` line — Task 2 step 2 needs the exact name to query via `listWorkflowRuns`)
    - lib/mix/tasks/mailglass.publish.check.ex (post-Plan 07 expansion — Task 2 step 1 invokes this with `--package both`)
  </read_first>
  <action>
    Restructure `.github/workflows/publish-hex.yml` `jobs:` block from current single matrix job into FOUR sequential jobs chained via `needs:`. Apply the D-12 compound gate from Task 1 ONLY to the first job (`prepublish-summary`); subsequent jobs inherit transitively via `needs:`.

    **Job 1 — `prepublish-summary` (D-15: NO environment gate; reviewer sees summary BEFORE Approve click):**

    Top-level `if:` is the D-12 compound gate from Task 1. Steps: `actions/checkout` (with `ref: ${{ github.event.inputs.tag || github.event.workflow_run.head_branch }}`) → `erlef/setup-beam` (Elixir 1.18 / OTP 27) → `actions/cache` (deps) → `mix deps.get` → `mix mailglass.publish.check --package mailglass` (gated `if: ${{ github.event.inputs.package != 'mailglass_admin' }}`) → `mix mailglass.publish.check --package mailglass_admin` (gated `if: ${{ github.event.inputs.package != 'mailglass' }}`).

    The Pre-publish check steps automatically write `$GITHUB_STEP_SUMMARY` (per Plan 07 D-17 step 15). Reviewer opening the workflow run page sees both summaries rendered BEFORE going to Environments → hex-publish → Approve.

    **Job 2 — `gate-ci-green` (D-16: require green ci.yml on tagged SHA; D-25: priv/static/ freshness via this gate):**

    Declares `needs: [prepublish-summary]`. Two `actions/github-script` steps:

    Step A — Resolve the SHA (use `context.payload.workflow_run?.head_sha || context.payload.inputs?.tag || context.sha`); call `github.rest.repos.getCommit({owner, repo, ref: sha})` to convert tag → SHA if needed; `core.setOutput("sha", commit.sha)`.

    Step B — Query `github.rest.actions.listWorkflowRuns({owner, repo, workflow_id: "ci.yml", head_sha: resolvedSha, per_page: 10})`. Assert `data.total_count > 0` (else `core.setFailed("Delivery blocked: no ci.yml runs found for SHA <sha>. Push the tagged commit and let CI complete before publishing.")`). Take the latest (`data.workflow_runs[0]`); assert `conclusion === "success"` (else `core.setFailed("Delivery blocked: ci.yml on SHA <sha> concluded with '<conclusion>', not 'success'. Run: <html_url>")`).

    This is the D-25 mechanism: `ci.yml` is the only workflow that runs `git diff --exit-code priv/static/` (per Phase 7 D-04). Asserting `ci.yml` succeeded on the tagged SHA transitively asserts the bundle was rebuilt and committed.

    **Job 3 — `publish-core` (environment-gated; D-14 sequential):**

    Declares `needs: [gate-ci-green]`, `if: ${{ github.event.inputs.package != 'mailglass_admin' }}`, `environment: hex-publish`. Steps: `actions/checkout` (with `ref: ${{ github.event.inputs.tag || github.event.workflow_run.head_branch }}`) → `erlef/setup-beam` → `actions/cache` → `mix deps.get` → "Read mailglass version from mix.exs" (id: `version`, run: `VERSION=$(grep -E '@version "[0-9]' mix.exs | head -1 | sed -E 's/.*"([^"]+)".*/\1/'); echo "version=${VERSION}" >> "$GITHUB_OUTPUT"`) → "Publish mailglass to Hex.pm" (run: `if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then mix hex.publish --dry-run; else mix hex.publish --yes; fi`; `env: HEX_API_KEY: ${{ secrets.HEX_API_KEY }}`) → "Wait for Hex.pm to index" (gated `if: ${{ github.event.inputs.dry_run != 'true' }}`, run: `VERSION="${{ steps.version.outputs.version }}"; DEADLINE=$(( SECONDS + 300 )); until mix hex.info mailglass "${VERSION}" 2>/dev/null; do if [ $SECONDS -ge $DEADLINE ]; then echo "Delivery blocked: Hex.pm did not index mailglass ${VERSION} within 5 minutes."; exit 1; fi; echo "Waiting for Hex.pm to index mailglass ${VERSION}..."; sleep 15; done; echo "Hex.pm indexed mailglass ${VERSION}."`).

    The `if:` filter on the job (`!= 'mailglass_admin'`) lets a `--package mailglass_admin` re-publish skip core entirely.

    **Job 4 — `publish-admin` (environment-gated; D-14 `needs: publish-core`):**

    Declares `needs: [publish-core]`, `environment: hex-publish`, and:
    ```yaml
    if: |
      always() &&
      (needs.publish-core.result == 'success' || needs.publish-core.result == 'skipped') &&
      github.event.inputs.package != 'mailglass'
    ```
    Steps: `actions/checkout` (with `ref: ${{ github.event.inputs.tag || github.event.workflow_run.head_branch }}`) → `erlef/setup-beam` → `actions/cache` → `mix deps.get` → "Publish mailglass_admin to Hex.pm" (`working-directory: mailglass_admin`, `env: HEX_API_KEY: ${{ secrets.HEX_API_KEY }}` and `MIX_PUBLISH: "true"`, run: `mix deps.get; if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then mix hex.publish --dry-run; else mix hex.publish --yes; fi`).

    The `MIX_PUBLISH=true` env var flips `mailglass_dep/0` (per `mailglass_admin/mix.exs` lines 86-92) so the published manifest declares `{:mailglass, "== <X>"}` instead of `path: ".."`. The `needs.publish-core.result == 'success' || 'skipped'` allows the `--package mailglass_admin` re-publish flow to proceed when core was skipped.

    **Anti-patterns to avoid:**
    - DO NOT replace `strategy.matrix` with `strategy.max-parallel: 1` — that is not serialization, just throttling. The audit-equivalent failure (admin publishes before core is indexed) still happens because GitHub Actions can pick either job first within a matrix. Use TWO separate jobs with `needs:` per D-14.
    - DO NOT put the `mix mailglass.publish.check` invocation inside the `publish-core` / `publish-admin` jobs — that defeats D-15 (summary appears AFTER Approve click). Keep it in `prepublish-summary`.
    - DO NOT add `concurrency: cancel-in-progress: true` — per Task 1 it is `false`. Mid-publish cancellation is dangerous.
    - DO NOT remove `environment: hex-publish` from `publish-core` / `publish-admin` — that is the human-Approve gate per D-27.
  </action>
  <verify>
    <automated>bash -c "grep -cE '^  prepublish-summary:|^  gate-ci-green:|^  publish-core:|^  publish-admin:' .github/workflows/publish-hex.yml | grep -q '^4$' && grep -F 'needs: [prepublish-summary]' .github/workflows/publish-hex.yml && grep -F 'needs: [gate-ci-green]' .github/workflows/publish-hex.yml && grep -F 'needs: [publish-core]' .github/workflows/publish-hex.yml && grep -F 'environment: hex-publish' .github/workflows/publish-hex.yml && grep -F 'MIX_PUBLISH' .github/workflows/publish-hex.yml && grep -F 'github.rest.actions.listWorkflowRuns' .github/workflows/publish-hex.yml && grep -F 'until mix hex.info mailglass' .github/workflows/publish-hex.yml"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^  prepublish-summary:|^  gate-ci-green:|^  publish-core:|^  publish-admin:" .github/workflows/publish-hex.yml` returns exactly 4
    - `grep -cF "needs: [prepublish-summary]" .github/workflows/publish-hex.yml` returns 1
    - `grep -cF "needs: [gate-ci-green]" .github/workflows/publish-hex.yml` returns 1
    - `grep -cF "needs: [publish-core]" .github/workflows/publish-hex.yml` returns 1
    - `grep -cF "environment: hex-publish" .github/workflows/publish-hex.yml` returns exactly 2 (publish-core + publish-admin)
    - `grep -cF "MIX_PUBLISH" .github/workflows/publish-hex.yml` returns at least 1 (publish-admin step env)
    - `grep -cF "github.rest.actions.listWorkflowRuns" .github/workflows/publish-hex.yml` returns 1 (D-16 query)
    - `grep -cF "until mix hex.info mailglass" .github/workflows/publish-hex.yml` returns 1 (D-14 polling)
    - `grep -cF "mix mailglass.publish.check --package mailglass" .github/workflows/publish-hex.yml` returns at least 1 (D-15 summary)
    - `grep -cF "mix mailglass.publish.check --package mailglass_admin" .github/workflows/publish-hex.yml` returns at least 1 (D-15 summary)
    - `grep -cF "secrets.HEX_API_KEY" .github/workflows/publish-hex.yml` returns exactly 2 (only inside `publish-core` and `publish-admin`, NEVER in `prepublish-summary` or `gate-ci-green` — secret minimization)
    - `grep -cF "strategy:" .github/workflows/publish-hex.yml` returns 0 (matrix removed per D-14)
    - `grep -cF "actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea" .github/workflows/publish-hex.yml` returns at least 1 (D-16 step uses pinned SHA)
    - `grep -cF "actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683" .github/workflows/publish-hex.yml` returns at least 3 (one per job that checks out: prepublish-summary, publish-core, publish-admin)
    - `grep -cF "erlef/setup-beam@5304e04ea2b355f03681464e683d92e3b2f18451" .github/workflows/publish-hex.yml` returns at least 3 (same)
    - YAML parses cleanly: `bash -c "command -v actionlint >/dev/null && actionlint .github/workflows/publish-hex.yml || echo 'actionlint not installed; skipping'"` exits 0 OR produces only warnings (actionlint must not exit non-zero).
    - The string `Delivery blocked:` appears at least twice in the file (once in the gate-ci-green core.setFailed messages per D-16) — `grep -cF 'Delivery blocked:' .github/workflows/publish-hex.yml` returns at least 2
  </acceptance_criteria>
  <done>
    `publish-hex.yml` is restructured into FOUR `needs:`-chained jobs in this exact order: `prepublish-summary` (no env gate, runs `mix mailglass.publish.check`) → `gate-ci-green` (no env gate, queries ci.yml run via `actions/github-script`) → `publish-core` (env: hex-publish, publishes mailglass + polls Hex index) → `publish-admin` (env: hex-publish, publishes mailglass_admin with `MIX_PUBLISH=true`). The matrix block is gone. All Action SHA pins preserved. `HEX_API_KEY` flows ONLY through the two environment-gated publish jobs. `Delivery blocked:` brand voice used in `core.setFailed` messages.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Hex.pm | HEX_API_KEY exposure here is unrecoverable; key is published-package-write-rights |
| release-please workflow_run → publish-hex | Untightened gate fires on every PR-update success, not just merge-to-main |
| GitHub Environment Approve click → publish | Reviewer needs informed signal BEFORE click, not after |
| Cross-workflow status query (ci.yml → publish-hex) | API tokens (GITHUB_TOKEN) scoped to repo; no cross-repo concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-08-01 | I (Information disclosure) | HEX_API_KEY leak via PR-job context | mitigate | `HEX_API_KEY` is referenced ONLY inside `publish-core` and `publish-admin` jobs (acceptance criteria asserts `grep -cF "secrets.HEX_API_KEY"` returns exactly 2). These jobs declare `environment: hex-publish` so the secret is environment-scoped per D-27. The `prepublish-summary` and `gate-ci-green` jobs run pre-environment-gate and never reference the secret. Plan 11 verifies the secret is NOT also a repo secret. |
| T-07.1-08-02 | T (Tampering) | Double-publish via untightened workflow_run gate (D-12) | mitigate | Compound `if:` on `prepublish-summary` requires `head_branch` matches `mailglass-sibling-group-v*` — release-please PR-update runs do not match this pattern (their head_branch is `release-please--branches--main--components--mailglass`), so the gate excludes them. Acceptance criteria asserts `startsWith(... 'mailglass-sibling-group-v')` present. `concurrency: group: publish-hex-${{ github.ref }}, cancel-in-progress: false` prevents overlapping runs from racing. |
| T-07.1-08-03 | E (Elevation of privilege) | workflow_dispatch arbitrary-tag publish | mitigate | `workflow_dispatch` requires Hex Environment approval (publish jobs declare `environment: hex-publish`). Maintainer review gate is the human check. Combined with D-25 enforcement (gate-ci-green requires green CI on the SHA), arbitrary publish is bounded by "tag must point at a commit that passed CI on main". Bus-factor risk acknowledged in MAINTAINING.md per Plan 02 D-30. |
| T-07.1-08-04 | T (Tampering) | priv/static/ stale-bundle ship via skipped CI gate | mitigate | D-25 mechanism: `gate-ci-green` queries `ci.yml` workflow runs for the tagged SHA via `github.rest.actions.listWorkflowRuns`; asserts `conclusion === 'success'`. ci.yml runs `git diff --exit-code priv/static/` (Phase 7 D-04), so green ci.yml ⇒ bundle is fresh. Cannot bypass: there is no other path to publish-core other than `needs: gate-ci-green`. |
| T-07.1-08-05 | I (Information disclosure) | Reviewer rubber-stamps Approve without seeing tarball contents | mitigate | D-15 mechanism: `prepublish-summary` runs BEFORE the environment gate; its `$GITHUB_STEP_SUMMARY` (Plan 07 D-17 step 15: file list, sizes, CHANGELOG excerpt, allowlist diff) is rendered on the workflow run page, visible BEFORE clicking Approve in Environments → hex-publish. Acceptance criteria asserts `mix mailglass.publish.check` runs in `prepublish-summary`, not in publish-core/publish-admin. |
| T-07.1-08-06 | T (Tampering) | mailglass_admin publishes before mailglass core indexed (sibling race) | mitigate | D-14 mechanism: `publish-admin needs: [publish-core]` (acceptance criteria asserts) AND `publish-core` has a Hex-index polling step (`until mix hex.info mailglass <ver>; do sleep 15; done`, max 5 min). The polling step is INSIDE publish-core, so publish-admin only runs after polling succeeds. Parallel-publish race that originally motivated D-14 cannot occur. |
| T-07.1-08-07 | R (Repudiation) | New 3rd-party Action introduced unpinned | mitigate | All Action references in this plan use existing SHA pins from the codebase (per PATTERNS.md "Shared Patterns: 3rd-party Actions SHA-pinning"). Acceptance criteria asserts at least 3 occurrences of each existing pinned SHA. Dependabot watches `.github/workflows/` per D-34. |
| T-07.1-08-08 | D (DoS) | Mid-publish cancellation corrupting Hex artifact | mitigate | `concurrency: cancel-in-progress: false` (Task 1, asserted in acceptance criteria). Once a publish job starts, overlapping triggers queue rather than cancel. |
</threat_model>

<verification>
After both tasks land:
```bash
# Four jobs in expected order
grep -nE "^  (prepublish-summary|gate-ci-green|publish-core|publish-admin):" .github/workflows/publish-hex.yml

# needs: chain present
grep -F "needs: [prepublish-summary]" .github/workflows/publish-hex.yml
grep -F "needs: [gate-ci-green]" .github/workflows/publish-hex.yml
grep -F "needs: [publish-core]" .github/workflows/publish-hex.yml

# D-12 compound gate
grep -F "github.event_name == 'workflow_dispatch'" .github/workflows/publish-hex.yml
grep -F "startsWith(github.event.workflow_run.head_branch, 'mailglass-sibling-group-v')" .github/workflows/publish-hex.yml

# D-13 inputs
grep -F "type: string" .github/workflows/publish-hex.yml
grep -F "type: boolean" .github/workflows/publish-hex.yml
grep -F "type: choice" .github/workflows/publish-hex.yml

# D-14 polling
grep -F "until mix hex.info mailglass" .github/workflows/publish-hex.yml

# D-16 cross-workflow query
grep -F "github.rest.actions.listWorkflowRuns" .github/workflows/publish-hex.yml

# Secret minimization
grep -cF "secrets.HEX_API_KEY" .github/workflows/publish-hex.yml
# Expected: 2 (only publish-core + publish-admin)

# Action SHA pinning preserved (every existing SHA still present)
grep -cF "actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683" .github/workflows/publish-hex.yml
# Expected: ≥ 3

# YAML lint (if actionlint installed)
command -v actionlint >/dev/null && actionlint .github/workflows/publish-hex.yml
```
</verification>

<success_criteria>
1. Four `needs:`-chained jobs in this order: `prepublish-summary` → `gate-ci-green` → `publish-core` → `publish-admin`. Matrix block is gone.
2. D-12 compound trigger gate is present on `prepublish-summary` requiring `head_branch` tag-pattern match for `workflow_run` events.
3. D-13 `workflow_dispatch:` trigger present with `tag` (string, required), `dry_run` (boolean, default false), `package` (choice, default `both`) inputs.
4. D-14 `publish-admin needs: [publish-core]` AND `publish-core` has Hex-index polling step (`until mix hex.info mailglass <ver>; do sleep 15; done`, max 5 min).
5. D-15 `prepublish-summary` runs `mix mailglass.publish.check` for both packages BEFORE the environment-gated publish jobs (no env gate on summary job).
6. D-16 `gate-ci-green` queries `ci.yml` workflow runs for the tagged SHA via `actions/github-script` + `github.rest.actions.listWorkflowRuns`; fails closed if not green.
7. D-25 enforcement: `gate-ci-green` is the only path to publish-core, so priv/static/ freshness inherits from main CI's `git diff --exit-code` check.
8. `HEX_API_KEY` referenced ONLY in the two environment-gated jobs (`secrets.HEX_API_KEY` count = 2).
9. All existing 3rd-party Action SHA pins preserved; no new unpinned Actions introduced.
10. `Delivery blocked:` brand-voice prefix used in `core.setFailed` messages.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-08-SUMMARY.md`.
</output>
