---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 06
subsystem: installer
tags: [installer, mailglass-install, golden-tests, smoke-tests, audit-blockers]
dependency_graph:
  requires: [07.1-01, 07.1-02, 07.1-03, 07.1-04, 07.1-05]
  provides: [installer-blockers-closed]
  affects:
    - test/support/installer_fixture_helpers.ex
    - lib/mailglass/installer/templates.ex
    - lib/mailglass/installer/plan.ex
    - test/example/README.md
    - mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs
    - .planning/REQUIREMENTS.md
tech_stack:
  added: []
  patterns: [golden-snapshot-testing, post-installer-smoke, real-apply-pipeline]
key_files:
  - lib/mailglass/installer/plan.ex
  - lib/mailglass/installer/templates.ex
  - test/support/installer_fixture_helpers.ex
  - mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs
decisions: [D-02, D-04]
metrics:
  duration: "≈90min (6 commits across 4 hours)"
  completed: "2026-04-25"
---

# Phase 07.1 Plan 06: Installer Audit-Blocker Closure Summary

Closed all five v0.1 milestone audit installer blockers (G-1, G-2, G-3, G-4, G-5) in the EXACT load-bearing sequence mandated by D-02. The order was non-negotiable per D-04: dropping the simulation FIRST forced the existing golden tests to fail honestly, exposing G-1 and G-3 before patches landed. Fixing templates first while simulation passed would have recreated the audit's exact failure mode.

## What Changed

Six commits, executed in the D-02 mandated sequence:

1. **`b53a7b2` — drive installer fixtures through real `Apply.run/2`** (D-04 anti-pattern fix). Removed the simulation layer in `test/support/installer_fixture_helpers.ex` so golden tests now exercise the real installer pipeline. This was the load-bearing first step — it caused G-1 and G-3 to fail loudly until the actual fixes landed.

2. **`70e69f0` — emit the adopter admin router mount snippet** (G-2). `lib/mailglass/installer/templates.ex` now emits the correct `mailglass_admin_routes "/mail"` mount snippet into the adopter's router so the dev preview is reachable post-install.

3. **`b247187` — wire webhook installer ops and migration generator** (G-4, G-5). `lib/mailglass/installer/plan.ex` now generates the webhook plug mount + the V01 migration generator op so adopters get a working webhook ingest path post-install.

4. **`fb32b4e` — refresh installer golden snapshots**. With the real pipeline now exercised end-to-end, the golden tests' baseline JSONs were regenerated to match honest output.

5. **`7c7c893` — add admin smoke coverage for post-install route resolution**. `mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs` exercises the post-install router compilation path so that `UndefinedFunctionError` on `mailglass_admin_routes` (literal G-1 regression) cannot recur in the test suite.

6. **`58264c1` — mark installer REQs complete after smoke and snapshot coverage**. Flipped `INST-01` and `INST-03` from `Pending` to `Complete (07.1-06)` in `.planning/REQUIREMENTS.md` per D-06's deferral.

## Deviations from Plan

None of substance — the load-bearing sequence per D-02/D-04 was followed exactly. Each commit is atomic and self-contained.

## Verification

- All five audit blockers G-1..G-5 marked closed in the milestone audit follow-up.
- Golden tests exercise real `Apply.run/2` pipeline ✓
- Post-install router smoke test asserts no `UndefinedFunctionError` on `mailglass_admin_routes` ✓
- `INST-01` and `INST-03` REQ rows now `Complete (07.1-06)` ✓
- `mix test` (Wave 2 milestone): green at the time these commits landed (CI red is a later regression — Plan 11 prereq).

## Self-Check

PASSED — all five installer audit blockers closed in the D-02 mandated sequence. Commits `b53a7b2`, `70e69f0`, `b247187`, `fb32b4e`, `7c7c893`, `58264c1` on `main`.
