---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: []
audit_blocker: null
decisions_addressed: [D-31, D-32, D-33]

must_haves:
  truths:
    - "CHANGELOG.md `## [0.1.0]` section is rewritten in mailglass voice as narrative-prefaced Keep-a-Changelog"
    - "CHANGELOG.md v0.1.0 entry has `### Added` and `### Security` sections ONLY (NO `### Known Limitations`)"
    - "CHANGELOG.md v0.1.0 entry leads with a 1-2 sentence narrative paragraph (~150 words max) before the bullet list"
    - "CHANGELOG.md preserves Keep-a-Changelog scaffold (lines 1-7) verbatim"
    - "CHANGELOG.md narrative does NOT contain forbidden phrases (Sorry!, Heads up!, Bear with us, Excited to announce)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v0.1.0 release narrative for HexDocs + GitHub Release"
      contains: "## [0.1.0]"
  key_links:
    - from: "CHANGELOG.md `## [0.1.0]` section"
      to: "GitHub Release narrative + HexDocs CHANGELOG page"
      via: "release-please publishes the section as the GitHub Release body verbatim (D-33)"
      pattern: "^## \\[0\\.1\\.0\\]"
---

<objective>
Rewrite the `## [0.1.0]` entry in `CHANGELOG.md` from feature-inventory bullet-list shape to narrative-prefaced Keep-a-Changelog format, in mailglass brand voice. Preserve the Keep-a-Changelog scaffold (lines 1-7). Per D-31 only `### Added` and `### Security` sections — NO `### Known Limitations` section because Phase 07.1 fixes the blockers cleanly. Per D-32 the narrative paragraph appears at the top of the entry; the GitHub Release v0.1.0 narrative will be this CHANGELOG entry verbatim PLUS one extra "why mailglass exists" framing paragraph (Plan 11 publish ceremony adds the framing paragraph at GitHub-Release-creation time, NOT in this file).

Purpose: Per CONTEXT this is the first impression an evaluator landing from search sees. Feature-inventory bullets without context land flat; narrative-prefaced Keep-a-Changelog answers "why is this here?" before "what's new?". Brand voice forbids panic, apology, and AI-marketing-speak ("Excited to announce") — they corrode the "thoughtful maintainer" signal.

Output: One markdown file rewritten in place, scaffold preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CHANGELOG.md v0.1.0 entry per D-31</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (entire 21-line file) — current scaffold (lines 1-7) and current v0.1.0 entry (lines 8-21) shape
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (lines 180-185) — D-31, D-32, D-33 verbatim
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 672-702) — pattern map for the in-place rewrite
    - .planning/PROJECT.md — D-19 brand-voice anchor ("thoughtful maintainer, clear, exact, confident not cocky, warm not cute, modern not trendy, technical not intimidating")
    - CLAUDE.md — Brand &amp; Voice section (front-page brand summary)
  </read_first>
  <action>
    PRESERVE lines 1-7 verbatim (the Keep-a-Changelog scaffold). REPLACE lines 8-21 (the current v0.1.0 entry) with a narrative-prefaced Keep-a-Changelog v0.1.0 entry.

    **Brand voice (apply to every sentence):** "thoughtful maintainer, clear, exact, confident not cocky, warm not cute, modern not trendy, technical not intimidating." Errors are specific and composed. Documentation prefers the direct word.

    **Forbidden phrases (do NOT use any of these):** `Sorry!`, `Heads up!`, `Bear with us`, `Excited to announce`, `Stay tuned`, `Without further ado`, `We're thrilled`, `Get ready`. No emoji. No exclamation marks except in code samples.

    **Narrative paragraph constraints (per D-31 + Claude's-discretion bullet 1 in CONTEXT):**
    - 1-2 sentences, under 150 words total
    - Answers "why is this here?" — what problem mailglass solves that Swoosh deliberately leaves out
    - Lands the value claim factually, not in marketing-pitch shape
    - Names the validation-release framing honestly

    **Section structure (D-31 mandate — exactly two sections):**
    - `### Added` — initial release highlights, grouped by capability area in concise prose-bullets (not raw feature inventory). Use "thoughtful maintainer" cadence: each bullet starts with the thing the adopter receives, not the thing the maintainer built.
    - `### Security` — anything security-relevant in v0.1 (signature verification, append-only ledger, suppression-list pre-send check, tracking opt-in default, no PII in telemetry).

    **Use this template literally** (substitute `<...>` placeholders with brand-voice prose; do not invent additional sections):

    ```markdown
    ## [0.1.0] - 2026-04-25

    <Narrative paragraph: 1-2 sentences. Frames mailglass as the "framework
    layer Swoosh leaves out" — HEEx-native components, append-only event
    ledger, multi-tenancy, normalized webhook events. Marks v0.1.0 as a
    validation release inviting feedback. Under 150 words total. Brand voice:
    factual, confident, no marketing-speak.>

    ### Added

    - <Capability bullet 1: HEEx-native component library (`<.container>`,
      `<.row>`, `<.button>`, etc.) with MSO Outlook VML fallbacks. No Node
      toolchain required at any point.>
    - <Capability bullet 2: Pure-function `Mailglass.Renderer.render/1` —
      HEEx → Premailex CSS inlining → Floki-derived plaintext.>
    - <Capability bullet 3: Append-only `mailglass_events` ledger with
      Postgres trigger raising SQLSTATE 45A01 on UPDATE/DELETE attempts.>
    - <Capability bullet 4: Multi-tenancy via `Mailglass.Tenancy` behaviour
      with `tenant_id` on every record from day one.>
    - <Capability bullet 5: Webhook ingest for Postmark + SendGrid with
      Anymail event taxonomy normalization, idempotent replay (UNIQUE on
      `(provider, provider_event_id)`), and orphan reconciliation.>
    - <Capability bullet 6: Send pipeline — `Mailable` → preflight
      (suppression + rate-limit + stream policy) → render → atomic
      `Multi(Delivery + Event + Worker enqueue)` → adapter dispatch.>
    - <Capability bullet 7: `Mailglass.Adapters.Fake` — stateful, time-
      advanceable test adapter with `assert_mail_sent/1` family of matchers.>
    - <Capability bullet 8: `mailglass_admin` dev preview LiveView — sidebar
      with auto-discovered mailables, HTML/Text/Raw/Headers tabs, device
      width and dark/light toggles. Brand-conformant UI (Ink/Glass/Ice/Mist/
      Paper/Slate palette, Inter + IBM Plex Mono typography).>
    - <Capability bullet 9: Twelve custom Credo checks enforcing domain rules
      at lint time — telemetry PII whitelist, tracking-off-by-default on
      auth-stream mailables, no-raw-Swoosh-deliver in lib code, prefixed
      PubSub topics, append-only event writes.>
    - <Capability bullet 10: `mix mailglass.install` for Phoenix 1.8 hosts —
      idempotent installer with `.mailglass_conflict_*` sidecars. Golden-diff
      CI snapshot test catches installer regressions.>
    - <Capability bullet 11: ExDoc with nine guides covering authoring,
      components, preview, webhooks, multi-tenancy, telemetry, testing, and
      migration from raw Swoosh + Phoenix.Swoosh.>

    ### Security

    - HMAC-verified webhook ingest. Postmark uses Basic Auth via
      `Plug.Crypto.secure_compare/2`; SendGrid uses ECDSA P-256 verification
      via OTP 27 `:public_key`. Forged signatures raise
      `Mailglass.SignatureError` with no recovery path and return 401.
    - Suppression-list check runs before every send. Recipients on the list
      cannot be re-sent to without explicit unblock through the suppression
      store.
    - Open and click tracking are off by default. Per-mailable opt-in
      required; the `NoTrackingOnAuthStream` Credo check raises at compile
      time on auth-context heuristics (`magic_link`, `password_reset`,
      `verify_email`, `confirm_account`).
    - Telemetry metadata is whitelisted to counts, statuses, IDs, and
      latencies. PII keys (`:to`, `:from`, `:body`, `:html_body`, `:subject`,
      `:headers`, `:recipient`, `:email`) are forbidden by the
      `NoPiiInTelemetryMeta` Credo check.
    - Click-rewriting tokens are signed via `Phoenix.Token` with rotation
      support. Target URLs live in the signed payload, not as a query param —
      the open-redirect CVE class is structurally unreachable.
    ```

    Replace each `<...>` placeholder with brand-voice prose that meets the constraints. Keep the bullet count and section structure exactly as shown — don't add `### Changed`, `### Deprecated`, `### Removed`, `### Fixed`, or `### Known Limitations` sections (D-31 explicitly forbids `### Known Limitations`; the others are not applicable to a 0.1.0 initial release).

    Use the date `2026-04-25` (Phase 07.1 ship date per current STATE.md) — NOT 2026-04-24 (the original entry's date, which predated this rewrite).
  </action>
  <verify>
    <automated>head -7 CHANGELOG.md &amp;&amp; grep -E "^##\s*\[?v?0\.1\.0\]?" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -7 CHANGELOG.md` matches the original 7-line scaffold byte-for-byte (compare to `git show HEAD:CHANGELOG.md | head -7` if needed)
    - `grep -E "^##\s*\[?v?0\.1\.0\]?" CHANGELOG.md | wc -l` returns at least `1` (D-17 step 7 publish.check regex match)
    - `grep -F "## [0.1.0]" CHANGELOG.md | wc -l` returns `1` (Keep-a-Changelog header shape preserved)
    - `grep -F "### Added" CHANGELOG.md | wc -l` returns at least `1` (Added section present)
    - `grep -F "### Security" CHANGELOG.md | wc -l` returns at least `1` (Security section present)
    - `grep -F "### Known Limitations" CHANGELOG.md | wc -l` returns `0` (D-31 forbidden section absent)
    - `grep -F "### Changed" CHANGELOG.md | wc -l` returns `0` (no changed section for initial release)
    - `grep -F "### Removed" CHANGELOG.md | wc -l` returns `0` (no removed section for initial release)
    - **Brand-voice grep (must be ABSENT):** `grep -E "Sorry!|Heads up!|Bear with us|Excited to announce|Stay tuned|Without further ado|We're thrilled|Get ready" CHANGELOG.md | wc -l` returns `0`
    - **Emoji check:** `grep -P "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{26FF}]|[\x{2700}-\x{27BF}]" CHANGELOG.md` returns nothing (no Unicode emoji)
    - `grep -F "Initial release of Mailglass core." CHANGELOG.md | wc -l` returns `0` (old feature-inventory phrasing replaced)
    - `wc -l CHANGELOG.md` returns at least `30` (rewrite is substantively longer than the original 21-line file)
    - The line immediately under `## [0.1.0]` is non-empty paragraph text (the narrative), NOT a `### Added` heading — verify with `awk '/^## \[0\.1\.0\]/{getline; getline; print; exit}' CHANGELOG.md` returning a non-empty non-heading line
  </acceptance_criteria>
  <done>
    CHANGELOG.md preserves lines 1-7 verbatim; the `## [0.1.0]` entry is rewritten as narrative-prefaced Keep-a-Changelog with one ~150-word narrative paragraph followed by `### Added` and `### Security` sections only; no forbidden brand-voice phrases or emoji; the regex `^##\s*\[?v?0\.1\.0\]?` matches (publish.check D-17 step 7 will be green).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG.md → public docs | Section is published as GitHub Release body verbatim (D-33) AND on HexDocs CHANGELOG page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-03-01 | T (Tampering / impression) | CHANGELOG narrative | mitigate | Brand-voice grep in `<acceptance_criteria>` blocks marketing-speak that would mismatch the "thoughtful maintainer" voice. Forbidden phrases enumerated explicitly. |
| T-07.1-03-02 | I (Information disclosure) | Security section | accept | Security bullets describe surface-level guarantees (HMAC verify, append-only ledger, opt-in tracking) — they are public design decisions documented in PROJECT.md, REQUIREMENTS.md, and CLAUDE.md already. No new disclosure. |
| T-07.1-03-03 | R (Repudiation) | Missing `### Known Limitations` | accept | Per D-31 explicit decision: this phase fixes the blockers (G-1..G-5) so v0.1.0 is shipped clean. Documenting "known limitations" against fixes that just landed would create a false impression that the release is broken. |
</threat_model>

<verification>
Pre-publish verification — these must all pass before Plan 11 (publish ceremony):
```bash
# Brand-voice
grep -E "Sorry!|Heads up!|Bear with us|Excited to announce|Stay tuned|Without further ado|We're thrilled|Get ready" CHANGELOG.md && echo "FAIL: forbidden phrase" || echo "voice clean"

# Section regex (publish.check D-17 step 7 will run this)
grep -E "^##\s*\[?v?0\.1\.0\]?" CHANGELOG.md

# Section bounds
grep -c "^### Added" CHANGELOG.md      # 1
grep -c "^### Security" CHANGELOG.md   # 1
grep -c "^### Known Limitations" CHANGELOG.md  # 0
```
</verification>

<success_criteria>
1. CHANGELOG.md preserves the Keep-a-Changelog scaffold (lines 1-7) byte-for-byte.
2. CHANGELOG.md `## [0.1.0]` entry is rewritten as narrative-prefaced Keep-a-Changelog.
3. Narrative paragraph (1-2 sentences, ≤150 words) appears immediately under the `## [0.1.0]` heading, before any `###` subsection.
4. Entry contains exactly `### Added` and `### Security` subsections — no other subsections.
5. `### Added` covers initial release highlights as concise prose-bullets (not raw feature inventory).
6. `### Security` covers HMAC verify, suppression check, tracking-off-by-default, telemetry PII whitelist, signed click tokens.
7. No forbidden brand-voice phrases (`Sorry!`, `Heads up!`, `Bear with us`, `Excited to announce`, `Stay tuned`, `Without further ado`, `We're thrilled`, `Get ready`).
8. No Unicode emoji.
9. Date stamp is `2026-04-25` (Phase 07.1 ship date), not `2026-04-24`.
10. The regex `^##\s*\[?v?0\.1\.0\]?` matches (publish.check D-17 step 7 verifier).
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-03-SUMMARY.md`.
</output>
