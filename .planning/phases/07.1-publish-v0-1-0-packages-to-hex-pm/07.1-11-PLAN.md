---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 11
type: execute
wave: 5
depends_on: [10]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: []
audit_blocker: null
decisions_addressed: [D-27, D-28, D-30, D-32, D-34, D-35]

must_haves:
  truths:
    - "GitHub Environment `hex-publish` configuration is verified to match D-27 table EXACTLY: required reviewer `szTheory`, prevent_self_review OFF, wait timer 0, deployment branch policy `mailglass-sibling-group-v*` tag pattern + `main` branch, environment secret `HEX_API_KEY` (NOT repo-scoped), environment variables none, custom protection rules none"
    - "`HEX_API_KEY` is environment-scoped (not repo-scoped) per D-27 — `gh api /repos/szTheory/mailglass/actions/secrets` shows NO repo-level entry; only the `hex-publish` environment scope contains it"
    - "Dependabot is enabled for `mix.lock` AND `.github/workflows/` per D-34 — verified at `/settings/security_analysis` or via `.github/dependabot.yml` containing both ecosystems"
    - "release-please PR (created by Plan 10 Task 3) is approved and merged by maintainer"
    - "GitHub Release `mailglass-sibling-group-v0.1.0` body is edited per D-32 to prepend a ~150-word brand-voice framing paragraph above the auto-generated CHANGELOG body, with `---` separator (Task 5)"
    - "`hex-publish` deployment is approved in GitHub Environment UI by maintainer (single-eye Approve per D-27 — prevent_self_review is silently disabled with one reviewer)"
    - "Within 60 minutes of publish: maintainer runs `mix mailglass.install` in a fresh Phoenix host as the literal final revert window per D-30 step 4"
    - "post-publish-smoke workflow (Plan 09) runs and exits green within 30 minutes of publish"
    - "`mix hex.info mailglass 0.1.0` and `mix hex.info mailglass_admin 0.1.0` both show v0.1.0 NOT retired"
    - "`.planning/STATE.md` is updated to mark milestone v0.1.0 as SHIPPED with publish timestamp"
    - "D-35 deferred items (GPG-signed tags, CodeQL, SBOM, Hex package signing) are confirmed STILL deferred at v0.1.0 — no scope creep into the ceremony"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated milestone state showing v0.1.0 SHIPPED"
      contains: "v0.1.0 SHIPPED"
  key_links:
    - from: "Plan 10's Release-As commit"
      to: "release-please PR"
      via: "release-please workflow auto-creates PR after Release-As push lands on main"
      pattern: "release-please bot PR"
    - from: "release-please PR merge"
      to: "publish-hex.yml workflow_run trigger"
      via: "Plan 08 D-12 compound gate fires on workflow_run + tag-pattern match"
      pattern: "workflow_run conclusion success + head_branch starts with mailglass-sibling-group-v"
    - from: "publish-hex.yml publish-core + publish-admin jobs"
      to: "Hex.pm published artifacts"
      via: "mix hex.publish --yes (Plan 08)"
      pattern: "Published mailglass 0.1.0 + mailglass_admin 0.1.0"
    - from: "Hex.pm publish completion"
      to: "post-publish-smoke.yml workflow_run trigger (Plan 09)"
      via: "automatic chain: publish-hex completed → post-publish-smoke fires"
      pattern: "post-publish-smoke green within 30 min"
---

<objective>
Execute the v0.1.0 publish ceremony. This plan is ENTIRELY manual — every task is `checkpoint:human-action` or `checkpoint:human-verify`. There is no executor automation here; Plans 01-10 have done all the autonomous work, and what remains is the irreducible human ceremony around a one-shot publish action.

Per D-27: verify the `hex-publish` Environment is configured EXACTLY per the locked table (single reviewer `szTheory`, prevent_self_review OFF — silently disabled with one reviewer, document honestly per Plan 02 D-30, 0-min wait, tag+main branch policy, env-scoped HEX_API_KEY, no env vars, no custom rules).

Per D-34: verify Dependabot is enabled for both `mix.lock` and `.github/workflows/` BEFORE publish — first publish is the worst possible time to discover missing security plumbing.

Per D-30 (step 4): within 60 minutes of publish, maintainer must run `mix mailglass.install` in a fresh Phoenix host. This is the LITERAL last revert window — Hex.pm allows `mix hex.publish --revert` only within the first 60 minutes after publish for unconsumed packages, and the 24-hour absolute bound thereafter. Any installer breakage discovered after 60 minutes requires `mix hex.retire` (a permanent record) instead of revert.

Per D-35: confirm scope discipline — the deferred items (GPG-signed git tags, CodeQL workflow, SBOM artifacts, Hex package signing) are STILL deferred at v0.1.0. The ceremony does NOT silently expand scope.

Purpose: This is the moment v0.1.0 ships. The ceremony enforces:
1. Pre-flight: settings audit + secret-scope verification + Dependabot verification (Tasks 1-3).
2. Approval: release-please PR review + merge → GitHub Release D-32 framing prepend → environment Approve click (Tasks 4-6).
3. Verification window: 60-min smoke install + post-publish-smoke green + retraction-state check (Tasks 7-9).
4. State capture: STATE.md milestone-shipped marker (Task 10).

Output: Zero source-code changes. ONE STATE.md update at the end. Ceremony tasks are GitHub-UI + shell-verification + maintainer-judgment.

**Scope note (Blocker 2 — D-30 step 5 boundary):** Per CONTEXT line 14 (Out of scope) and line 351 (Deferred Ideas: Marketing announcements), the Release Runbook step 5 (Forum post) is documented in MAINTAINING.md per Plan 02 D-30 but is NOT executed within this phase. The maintainer performs it on their own cadence post-publish; not a blocker for the milestone-shipped marker (Task 10). Plan 02 Task 1 acceptance criteria verifies that MAINTAINING.md flags this step as "post-publish, optional" so the runbook honestly reflects the phase's executed scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md
@MAINTAINING.md
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@.github/workflows/release-please.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify GitHub Environment `hex-publish` settings match D-27 table EXACTLY</name>
  <what-built>
    Plans 02 (MAINTAINING.md sections D-30) and 08 (publish-hex.yml hardening D-12..D-16) both depend on the `hex-publish` Environment being configured per D-27. This task is the pre-publish verification that the GitHub Environment UI matches the locked table.
  </what-built>
  <how-to-verify>
    Open https://github.com/szTheory/mailglass/settings/environments/hex-publish in a browser logged in as `szTheory`. Verify EACH of the following settings against D-27:

    | Setting | Expected Value | How to verify |
    |---|---|---|
    | Required reviewers | `szTheory` (single reviewer) | "Required reviewers" toggle ON; the user `szTheory` is the ONLY entry in the list |
    | Prevent self-review | **OFF** (either greyed-out by GitHub auto-disable for single-reviewer per D-27, OR operable-but-set-to-OFF; both states satisfy) | The "Prevent self-review" toggle is greyed out OR the toggle reads OFF and a tooltip explains "requires more than one reviewer". **Maintainer MUST note which of the two states was observed in their `approved:` resume signal** (e.g. "approved: ... Prevent self-review: greyed-out auto-disabled" or "approved: ... Prevent self-review: operable, set to OFF") |
    | Wait timer | 0 minutes | "Wait timer" field shows `0` |
    | Deployment branch policy | "Selected branches and tags" → both `mailglass-sibling-group-v*` (tag pattern) AND `main` (branch) entries exist | Click "Add deployment branch or tag rule" pattern; both entries should already exist |
    | Environment secrets | `HEX_API_KEY` listed (write API rights) | "Environment secrets" section shows `HEX_API_KEY` (value not displayed; only the name) |
    | Environment variables | None | "Environment variables" section is empty (no entries) |
    | Custom protection rules | None | "Deployment protection rules" section shows no custom rules |

    **Verification artifact (paste into Task resume signal):**

    Either capture a screenshot of the Environment settings page (full page, no value-redaction needed since the page does not display secret values), OR paste the following table back as a textual confirmation:

    ```
    Required reviewers: szTheory
    Prevent self-review: OFF (note observed state: <greyed-out auto-disabled> OR <operable, set to OFF>)
    Wait timer: 0
    Deployment branch policy: mailglass-sibling-group-v* + main
    Environment secrets: HEX_API_KEY
    Environment variables: (none)
    Custom protection rules: (none)
    ```

    **If ANY setting does not match:** STOP. Adjust in the GitHub UI before proceeding to Task 2. The most common mis-config is `Prevent self-review: ON` — GitHub silently disables it when only one reviewer is present, but the toggle position can be misleading. If in doubt, leave it OFF (per D-27 honest stance — see MAINTAINING.md "Bus Factor & Continuity" section per Plan 02 D-30).
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: <paste verification table or screenshot URL>` — all D-27 settings verified
    - `block: <reason>` — settings mismatch; describe what's wrong and what was changed (or what needs maintainer decision)
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Verify HEX_API_KEY is environment-scoped (NOT repo-scoped)</name>
  <what-built>
    Per D-27 "Environment secrets" row + Plan 08 threat model T-07.1-08-01: `HEX_API_KEY` MUST be ONLY in the `hex-publish` environment scope, NOT in the repository-wide secrets scope. Repo secrets are exposed to PR jobs (via `secrets.HEX_API_KEY`) which is the #1 secret-leak vector for Hex publish keys.

    Per memory `project_hex_api_key.md` (2026-04-25 maintainer note): the key is provisioned with write API rights; verify scope here.
  </what-built>
  <how-to-verify>
    Run BOTH of the following gh CLI checks:

    **Check 1 — Repository secrets must NOT contain HEX_API_KEY:**
    ```
    gh api /repos/szTheory/mailglass/actions/secrets --jq '.secrets[].name' | sort
    ```
    The output should list any repo-level secrets present (e.g. `GITHUB_TOKEN` is auto-managed and may appear). The string `HEX_API_KEY` MUST NOT appear in this list.

    **Check 2 — Environment `hex-publish` secrets MUST contain HEX_API_KEY:**
    ```
    gh api /repos/szTheory/mailglass/environments/hex-publish/secrets --jq '.secrets[].name' | sort
    ```
    The output MUST include `HEX_API_KEY` exactly.

    **If Check 1 shows `HEX_API_KEY` at the repo level:** STOP. This is the secret-leak vector D-27 explicitly forbids. Remove the repo-level secret in https://github.com/szTheory/mailglass/settings/secrets/actions BEFORE proceeding. Verify Check 2 still passes (the env-scoped copy should be untouched).

    **If Check 2 does NOT show HEX_API_KEY:** STOP. Add it to the `hex-publish` environment secrets in the GitHub UI (Settings → Environments → hex-publish → Add Secret), pasting the same write-rights API key from Hex.pm.

    **Cross-validation against MAINTAINING.md:** Plan 02 D-30 added a "Bus Factor & Continuity" section that documents this secret-scope policy. Re-read that section to confirm the documented policy matches what's actually configured.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: repo-scope clean, env-scope confirmed` — both checks passed
    - `fixed: removed repo-level HEX_API_KEY` — Check 1 found a leak; removed; re-verify
    - `block: <reason>` — something inconsistent; describe
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Verify Dependabot enabled for mix.lock + .github/workflows/ (D-34)</name>
  <what-built>
    Per D-34, Dependabot must monitor BOTH `mix.lock` (Hex deps) AND `.github/workflows/` (3rd-party Action SHA pins). Without this, the SHA-pinning convention (Plan 08 + Plan 09 + every existing workflow) silently rots.
  </what-built>
  <how-to-verify>
    Two equivalent verification paths — choose either.

    **Option A — Inspect `.github/dependabot.yml` directly:**
    ```
    cat .github/dependabot.yml
    ```
    The file must contain at least these two `updates:` entries:

    ```yaml
    updates:
      - package-ecosystem: "mix"
        directory: "/"
        schedule:
          interval: "weekly"
      - package-ecosystem: "github-actions"
        directory: "/"
        schedule:
          interval: "weekly"
    ```

    The file may have additional ecosystems (e.g. `mailglass_admin/` mix), but it MUST contain at least the root `mix` and root `github-actions` entries.

    **Option B — Check via the GitHub API:**
    ```
    gh api /repos/szTheory/mailglass/dependabot/alerts --jq 'length'
    ```
    Returns the count of alerts (which can be 0 if no vulnerabilities — that's fine; what matters is the API responds 200, which means Dependabot is active).

    Also verify at https://github.com/szTheory/mailglass/security/dependabot — the page should show "Dependabot is enabled" with both `mix` and `github-actions` ecosystems listed.

    **If Dependabot is NOT enabled or one of the ecosystems is missing:** STOP. Add `.github/dependabot.yml` (or edit it to add the missing ecosystem) in a separate PR BEFORE Task 4. Do not bundle this into the release-please PR or the publish ceremony.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: dependabot.yml has mix + github-actions ecosystems` — verified
    - `fixed: added missing ecosystem to dependabot.yml in PR #N` — gap fixed; will land before publish
    - `block: <reason>` — describe
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Approve and merge the release-please PR (created by Plan 10 Task 3)</name>
  <what-built>
    Plan 10 Task 3 pushed the `Release-As: 0.1.0` empty commit to main, which caused the release-please workflow to open a PR proposing v0.1.0. This task is the merge-ceremony for that PR.
  </what-built>
  <how-to-verify>
    1. Find the PR:
       ```
       gh pr list --state open --label "autorelease: pending"
       ```
       Should return 1 PR with title like `chore(main): release 0.1.0`.

    2. Inspect the diff:
       ```
       gh pr diff <PR_NUMBER>
       ```
       Verify the diff contains:
       - `.release-please-manifest.json`: `0.0.0` → `0.1.0` for both packages
       - Root `CHANGELOG.md`: release-please's auto-summary folded under (or appended to) the existing `## [0.1.0]` section that Plan 03 wrote
       - `mailglass_admin/CHANGELOG.md`: NEW v0.1.0 section (release-please-managed per D-31)

    3. Verify CI is green on the PR:
       ```
       gh pr checks <PR_NUMBER>
       ```
       All checks should be `pass`. If `ci.yml` is failing, do NOT merge — Plan 08's `gate-ci-green` job will block publish-hex anyway, and merging a red PR creates a worse forensic trail.

    4. Verify the CHANGELOG narrative is intact:
       ```
       gh pr diff <PR_NUMBER> -- CHANGELOG.md
       ```
       The pre-written Plan 03 narrative paragraph and Added/Security sections MUST still be present. If release-please overwrote them, do NOT merge — fix manually by editing the PR (push to release-please's branch) to restore the narrative.

    5. Merge using squash-merge (Conventional Commits enforced via PR title check):
       ```
       gh pr merge <PR_NUMBER> --squash --delete-branch
       ```
       OR via the GitHub UI: click "Squash and merge" → keep the PR title as the commit message → confirm.

    **Post-merge:** within ~30 seconds, release-please-action will create the linked-versions group tag `mailglass-sibling-group-v0.1.0` and push it to the repo. Verify:
    ```
    git fetch --tags
    git tag --list 'mailglass-sibling-group-v*'
    ```
    Should show `mailglass-sibling-group-v0.1.0`.

    **Post-tag:** the tag push triggers `publish-hex.yml` (Plan 08) — open https://github.com/szTheory/mailglass/actions and confirm `publish-hex` workflow run started for the tag.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: PR #N merged, tag mailglass-sibling-group-v0.1.0 created, publish-hex workflow started` — proceed to Task 5
    - `defer: CHANGELOG conflict, fixing PR manually` — fixing in-place, will resume
    - `block: <reason>` — describe (e.g. CI red on PR; release-please proposed wrong version)
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Edit auto-generated GitHub Release v0.1.0 to prepend D-32 framing paragraph</name>
  <what-built>
    Per D-32 (CONTEXT line 182): the GitHub Release narrative for v0.1.0 = CHANGELOG entry verbatim PLUS one extra "why mailglass exists" framing paragraph at the top (1-2 short paragraphs, ~150 words) PLUS optional asciinema/screenshot of `mix mailglass.install` running successfully.

    Plan 03 explicitly defers framing-paragraph creation to this plan. release-please-action auto-creates the GitHub Release after Task 4 merges with the bare CHANGELOG body — without this task, the framing paragraph is silently lost and an evaluator landing from search sees only the technical changelog. Per CONTEXT line 182, the ~10 min/release prose cost is worth it for evaluators landing from search.

    This task is `checkpoint:human-action` (not auto) because: (a) the framing prose requires brand-voice judgment (Claude's discretion per CONTEXT line 204, "follow brand voice; under 200 framing"); (b) the maintainer must vouch for what mailglass is/why it exists with their own voice, not delegate that to a template.
  </what-built>
  <how-to-verify>
    **The release exists already** — Task 4's release-please PR merge auto-created `mailglass-sibling-group-v0.1.0` with a bare CHANGELOG body. This task EDITS that release in place to prepend the framing.

    **Step 1 — fetch the auto-generated body:**
    ```
    gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' > /tmp/release-body.md
    ```

    **Step 2 — maintainer drafts a 1-2 paragraph "why mailglass exists" framing (~150 words) in `/tmp/release-framing.md`.**

    Brand voice anchor: `prompts/mailglass-brand-book.md` ("clear, exact, confident not cocky, warm not cute, modern not trendy, technical not intimidating — a thoughtful maintainer"). NO emoji. NO forbidden phrases (`Sorry!`, `Heads up!`, `Bear with us`, `Excited to announce`).

    Concrete framing skeleton (Claude's discretion per CONTEXT line 204 — adapt freely):

    - **Para 1 (what):** "mailglass is the transactional email framework Phoenix has been missing — HEEx-native components, LiveView preview, normalized webhook events (Anymail taxonomy), RFC 8058 List-Unsubscribe, append-only event ledger, multi-tenant routing. Composes on top of Swoosh; doesn't replace it."
    - **Para 2 (why now):** "v0.1.0 is the validation release. Honest scope: dev preview + send pipeline + webhook ingest. mailglass_inbound, prod admin dashboard, `mix mail.doctor` deliverability checks land in v0.5+. Forever MIT."

    Write to `/tmp/release-framing.md` (do NOT commit; this is one-shot prose). Word count target ~150 words; cap ~200.

    **Step 3 — compose the final body** (framing on top, `---` separator, then CHANGELOG body verbatim):
    ```
    cat /tmp/release-framing.md > /tmp/release-final.md
    echo -e "\n\n---\n" >> /tmp/release-final.md
    cat /tmp/release-body.md >> /tmp/release-final.md
    ```

    Open `/tmp/release-final.md` and visually proof-read. Confirm:
    - Framing is 1-2 paragraphs, ~150 words (use `wc -w /tmp/release-framing.md`).
    - Voice matches brand-book; no forbidden phrases; no emoji.
    - The `---` separator + CHANGELOG body verbatim are intact below.

    **Step 4 — replace the Release body:**
    ```
    gh release edit mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --notes-file /tmp/release-final.md
    ```

    **Step 5 — optional: attach asciinema cast or screenshot of `mix mailglass.install` running** (D-32 specifies "optional"):
    ```
    # Example (only if you have a recording):
    # gh release upload mailglass-sibling-group-v0.1.0 install-demo.cast --repo szTheory/mailglass
    ```
    Skip if no recording is available — D-32 marks this as optional.

    **Step 6 — verify the edit landed:**
    ```
    gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | head -20
    ```
    Confirm the first ~20 lines contain the framing paragraph (NOT the CHANGELOG version header).

    **The `approved:` resume signal MUST include the FULL FINAL release body pasted back** — this proves the maintainer read the final composed body, not just rubber-stamped a script invocation.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: <paste the FULL FINAL release body, framing + --- + CHANGELOG verbatim>` — release edited; proceed to Task 6
    - `defer: re-drafting framing for voice/length` — drafting in progress, will resume
    - `block: <reason>` — describe (e.g. release not auto-created by release-please; dispatch issue)
  </resume-signal>
  <acceptance_criteria>
    - **Body word count exceeds CHANGELOG-only floor:** `gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | wc -w` returns count exceeding (Plan 03 CHANGELOG `## [0.1.0]` body word count + 100). Use that floor as a sanity check that ~150 framing words actually landed on top of the CHANGELOG.
    - **Framing-on-top check:** `gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | head -c 200 | grep -F "mailglass"` returns at least one match (the framing paragraph mentions "mailglass" within the first 200 characters — proof the framing is at the top, not buried below the CHANGELOG).
    - **No forbidden phrases:** `gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | grep -E "Sorry!|Heads up!|Bear with us|Excited to announce"` returns empty (exit 1).
    - **Maintainer pasted full body back in approved signal:** the resume signal text is at least 200 words (proves the maintainer actually read the composed body, not just rubber-stamped).
    - **CHANGELOG verbatim preserved below:** `gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | grep -F "## [0.1.0]"` returns at least 1 (the CHANGELOG section header is still present below the framing + separator).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Approve `hex-publish` deployment in GitHub Environment UI</name>
  <what-built>
    Plan 08 restructured publish-hex.yml into 4 sequential jobs. After Task 4's tag push, the workflow runs through `prepublish-summary` → `gate-ci-green` → and then BLOCKS at the `hex-publish` Environment Approve gate before `publish-core` and `publish-admin`. This task is the maintainer Approve click.
  </what-built>
  <how-to-verify>
    1. Open the publish-hex workflow run:
       ```
       gh run list --workflow publish-hex.yml --limit 1 --json databaseId,status,conclusion
       ```
       Note the `databaseId`. Open `https://github.com/szTheory/mailglass/actions/runs/<DATABASE_ID>`.

    2. Verify `prepublish-summary` and `gate-ci-green` BOTH passed (green checkmark). If either failed:
       - `prepublish-summary` red → `mix mailglass.publish.check` failed; click into the job logs to see which D-17 step (Plan 07) failed; fix and re-run via `workflow_dispatch` with `--package both`
       - `gate-ci-green` red → ci.yml on the tagged SHA is not green; investigate ci.yml logs

    3. **Read the `prepublish-summary` step summary on the run page.** Per Plan 08 D-15, this is the WHOLE POINT of the separate-job split — the reviewer sees the file list, sizes, CHANGELOG excerpt, and allowlist diff BEFORE clicking Approve. Read it. Confirm:
       - File count is in the expected range (~50-200 files per package, depending on `priv/static/` contents)
       - Total size is well under 6 MB (Plan 07 D-17 step 5 hard limit)
       - CHANGELOG excerpt matches Plan 03's narrative
       - No surprising paths in the file list (no `.env*`, no `.elixir_ls/`, no `_build/` — Plan 07 D-17 step 4 denylist would have failed if so, but human-eye verify)

    4. **Set a literal 60-minute timer NOW** (before clicking Approve). Per D-30 step 4 + CONTEXT specifics line 332. The 60-minute window starts at the publish moment; Task 7 must complete within it.

    5. Click Approve:
       - Scroll to the run's pending deployments section.
       - Click "Review deployments" → check `hex-publish` → click Approve.

    6. Watch `publish-core` execute:
       - `mix hex.publish --yes` runs (or `--dry-run` if you triggered with that flag).
       - Hex-index polling step (Plan 08 D-14): `until mix hex.info mailglass 0.1.0 ...; do sleep 15; done` — should complete within 5 minutes.

    7. After `publish-core` is green, click Approve AGAIN for `publish-admin` (the environment gate fires on each gated job separately):
       - `MIX_PUBLISH=true mix hex.publish --yes` runs in `mailglass_admin/` directory.
       - Verifies linked-version constraint resolves against the just-published mailglass core.

    **All four jobs (prepublish-summary, gate-ci-green, publish-core, publish-admin) must conclude `success`** before proceeding to Task 7.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: publish-hex green, both packages on Hex.pm` — proceed to Task 7
    - `defer: re-running via workflow_dispatch after fix` — issue surfaced, retrying
    - `retract-needed: <reason>` — something is broken; trigger Task 9 retract policy IMMEDIATELY (within 60-min window)
    - `block: <reason>` — describe
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Within 60 minutes — run consumer install in fresh Phoenix host (D-30 step 4)</name>
  <what-built>
    Per D-30 step 4: the LAST revert window. `mix hex.publish --revert` is only available within the first 60 minutes after publish (and only if zero downloads have happened). After 60 min: `mix hex.retire` is the only recourse, which is a permanent record visible to all evaluators.

    This task is the maintainer's literal-timer install-test against the JUST-published Hex artifact (NOT the in-tree source — Plan 09's post-publish-smoke runs in CI; this task is the parallel maintainer-machine validation).
  </what-built>
  <how-to-verify>
    **The 60-minute clock started at Task 6's first Approve click.** Confirm time remaining:
    ```
    # Start time: when you clicked Approve in Task 6 step 5.
    # If unsure, check Hex.pm:
    mix hex.info mailglass 0.1.0
    # Look for "Published at: <timestamp>"
    ```

    **Procedure (run in a fresh tmp directory, NOT in the mailglass repo):**

    ```
    cd $(mktemp -d)
    mix archive.install hex phx_new --force
    mix phx.new sandbox --module Sandbox --app sandbox --no-ecto --no-mailer --install
    cd sandbox
    ```

    Edit `mix.exs` `defp deps do` to add:
    ```
    {:mailglass, "== 0.1.0"},
    {:mailglass_admin, "== 0.1.0"},
    ```

    Then:
    ```
    mix deps.get
    mix mailglass.install --yes
    mix compile --warnings-as-errors
    ```

    Verify:
    - `mix deps.get` resolves both packages without warnings.
    - `mix mailglass.install --yes` runs to completion (status verbs `[create]` / `[update]` printed; final `Install result: create=N update=M unchanged=K conflict=0` line; `conflict=0` is mandatory).
    - `mix compile --warnings-as-errors` exits 0 with NO `UndefinedFunctionError` in the output (the literal G-1 regression test on the live Hex artifact).
    - Boot the endpoint:
      ```
      mix phx.server &
      SERVER_PID=$!
      sleep 5
      curl -fsI http://localhost:4000/dev/mail/
      kill $SERVER_PID
      ```
    - The curl should return HTTP 200.

    **If ANY step fails AND the 60-minute window is still open:** trigger Task 9 retract policy immediately (`mix hex.publish --revert mailglass 0.1.0` and `mix hex.publish --revert mailglass_admin 0.1.0`) BEFORE the window closes.

    **If ANY step fails AFTER 60 minutes:** revert is no longer available. Use `mix hex.retire` per D-28 step 1 IF the failure is data-loss / security / signature-bypass / fails-to-compile. For lesser issues, plan a v0.1.1 patch within 7 days per D-28 step 2.

    **If all steps pass:** type `approved` to resume — Task 7 complete.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: install + compile + endpoint smoke all green within 60 min` — proceed to Task 8
    - `retract-triggered: revert issued for <pkg> within window` — within 60 min, reverted; investigate; restart at Plan 10 Task 3 with fixed manifest
    - `retire-needed: 60-min window closed; retiring per D-28` — file `mix hex.retire` per D-28 step 1; trigger v0.1.1 plan in next phase
    - `defer: minor issue, will patch in v0.1.1` — D-28 step 2/3 path; do not retire
    - `block: <reason>` — describe
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Verify post-publish-smoke workflow (Plan 09) completes green within 30 minutes</name>
  <what-built>
    Plan 09's `post-publish-smoke.yml` workflow_run trigger fires automatically when `publish-hex` completes (Task 6). The smoke runs the same 5-job pipeline against the published Hex artifact: wait-for-index → wait-for-hexdocs → consumer-install → retracted-check → notify-on-failure (only on failure).

    This task is the maintainer's confirmation that CI's automated smoke agrees with Task 7's local smoke.
  </what-built>
  <how-to-verify>
    1. Open the post-publish-smoke run:
       ```
       gh run list --workflow post-publish-smoke.yml --limit 1 --json databaseId,status,conclusion
       ```
       It should already be running (or completed) since the workflow_run trigger fires automatically after Task 6.

    2. Wait up to 30 minutes for completion. The pipeline involves:
       - `wait-for-index`: max 5 min per package
       - `wait-for-hexdocs`: max 10 min per package
       - `consumer-install`: ~5-10 min (Phoenix project generation + deps + compile + endpoint boot)
       - `retracted-check`: ~30s
       - `notify-on-failure`: only fires if anything above failed

       Total expected: 15-25 min wall-clock.

    3. Verify final conclusion is `success`:
       ```
       gh run view <DATABASE_ID> --json conclusion --jq '.conclusion'
       ```
       Expected: `"success"`.

    4. If `notify-on-failure` job ran (i.e. the workflow concluded `failure`):
       - Check https://github.com/szTheory/mailglass/issues for an open issue tagged `publish-smoke-failed`.
       - Inspect the failed job's logs.
       - Cross-reference with Task 7's local smoke result:
         - If Task 7 passed but post-publish-smoke failed → Hex.pm propagation lag; re-trigger via `workflow_dispatch` after another 10 min
         - If Task 7 also failed → the published artifact is genuinely broken; trigger Task 9 retract within 60-min window OR retire after

    5. Spot-check HexDocs:
       - Open https://hexdocs.pm/mailglass/0.1.0/ in a browser; confirm the page loads.
       - Open https://hexdocs.pm/mailglass_admin/0.1.0/ in a browser; confirm the page loads.
       - Confirm CLAUDE.md, MAINTAINING.md, CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md are present in the docs sidebar (per Plan 01 D-23/D-24 cleanup) but `.planning/ROADMAP.md` and `.planning/PROJECT.md` are NOT (per D-23 removal).
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: post-publish-smoke green, HexDocs spot-check passed` — proceed to Task 9
    - `defer: smoke transient failure, retrying via workflow_dispatch` — re-trying, will resume
    - `block: <reason>` — describe (e.g. consumer-install failed, propagating to Task 9 retract decision)
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Confirm both packages NOT retired (D-28 final state check)</name>
  <what-built>
    Final retraction-state check before declaring v0.1.0 SHIPPED. If anything in Tasks 7-8 triggered a retract (`mix hex.publish --revert`) or a retire (`mix hex.retire`), this task surfaces it and gates the milestone-shipped state appropriately.
  </what-built>
  <how-to-verify>
    Run BOTH:

    ```
    mix hex.info mailglass 0.1.0
    mix hex.info mailglass_admin 0.1.0
    ```

    Verify:
    - Both commands exit 0.
    - Neither output contains the word `Retired`.
    - Neither output contains `Reverted` or `Removed`.
    - The "Releases:" line for each shows `0.1.0` as a NORMAL release (no asterisk, no "(retired)" suffix).

    Cross-check via Hex.pm web UI:
    - Open https://hex.pm/packages/mailglass and verify v0.1.0 is the latest, NOT shown as retired.
    - Open https://hex.pm/packages/mailglass_admin and verify same.

    **If either is retired:** v0.1.0 ceremony is INCOMPLETE — the next phase (a v0.1.1 patch or v0.1.0 unretire after fix) takes over. Do NOT proceed to Task 10 (STATE.md update). Update STATE.md instead to reflect the retired-and-needs-followup state per D-28.

    **If both are clean:** proceed to Task 10.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved: both packages live and not retired` — proceed to Task 10
    - `block: <reason>` — describe (e.g. one or both retired)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 10: Update .planning/STATE.md to mark v0.1.0 milestone SHIPPED</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — milestone in progress; phase 07.1 underway)
    - .planning/ROADMAP.md (verify v0.1 milestone definition; mark phase 07.1 closed)
    - .planning/v0.1-MILESTONE-AUDIT.md (audit verdict; ensure all blockers G-1..G-5 + B-1 + V-05 + V-07 are now closed)
  </read_first>
  <action>
    Edit `.planning/STATE.md` in place. Add a new section (or update the existing milestone state section) reflecting:

    - **Milestone v0.1.0 SHIPPED** with timestamp = the publish moment captured from Task 6 / `mix hex.info mailglass 0.1.0` "Published at: <timestamp>"
    - **Phase 07.1 closed** — all 11 plans landed
    - **Audit blockers closed:**
      - G-1, G-2, G-3, G-4, G-5: closed by Plan 06 (installer fixes)
      - B-1: closed by Plan 05 (REQUIREMENTS.md trace flip)
      - V-05, V-07: closed by Plan 04 (VERIFICATION.md backfill)
    - **Hex artifacts published:**
      - mailglass v0.1.0 → https://hex.pm/packages/mailglass/0.1.0
      - mailglass_admin v0.1.0 → https://hex.pm/packages/mailglass_admin/0.1.0
    - **Next milestone:** v0.2 features (per ROADMAP — refer to roadmap entries beyond Phase 07.1)
    - **Deferred items still deferred at v0.1.0** (per D-35): GPG-signed git tags, CodeQL workflow, SBOM artifacts, Hex package signing, multi-owner Hex transition, GitHub Org migration

    Preserve existing STATE.md content; this is an APPEND or section-update, not a rewrite. The commit type for this edit is `docs(state):` per CLAUDE.md "Commit & Branch Conventions" — CI path filters skip these commits.

    **Commit format:**
    ```
    docs(state): mark v0.1.0 milestone SHIPPED

    Phase 07.1 closed. mailglass + mailglass_admin v0.1.0 published to Hex.pm
    on <timestamp>. All audit blockers (G-1..G-5, B-1, V-05, V-07) closed.
    ```

    **Anti-pattern to avoid:** do NOT rewrite the entire STATE.md from scratch — preserve project history, decisions, blockers sections. Only add or update the milestone state section.
  </action>
  <verify>
    <automated>bash -c "grep -F 'v0.1.0 SHIPPED' .planning/STATE.md && grep -F 'Phase 07.1' .planning/STATE.md && grep -F 'mailglass v0.1.0' .planning/STATE.md && grep -F 'mailglass_admin v0.1.0' .planning/STATE.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "v0.1.0 SHIPPED" .planning/STATE.md` returns at least 1
    - `grep -cF "Phase 07.1" .planning/STATE.md` returns at least 1 (closure noted)
    - `grep -cF "mailglass v0.1.0" .planning/STATE.md` returns at least 1
    - `grep -cF "mailglass_admin v0.1.0" .planning/STATE.md` returns at least 1
    - `grep -cE "G-1|G-2|G-3|G-4|G-5" .planning/STATE.md` returns at least 1 (audit blocker closure noted)
    - `grep -cE "V-05|V-07|B-1" .planning/STATE.md` returns at least 1 (bookkeeping closure noted)
    - The commit (when made) uses `docs(state):` type per CLAUDE.md convention
  </acceptance_criteria>
  <done>
    `.planning/STATE.md` reflects v0.1.0 SHIPPED with publish timestamp, phase 07.1 closure, all audit blockers closed, both Hex artifacts linked, deferred items confirmed still deferred. Commit type follows `docs(state):` convention.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer Approve click → publish-hex publish jobs | Single point of human gate before bytes go to Hex.pm forever |
| `mix hex.publish --revert` 60-min window → 24-hr absolute window → permanent record | Time-bounded recovery; missing the window converts revert to retire (permanent) |
| Maintainer-machine smoke → published artifact validation | Independent of CI smoke; catches HexDocs/index propagation issues that CI would miss |
| GitHub Environment HEX_API_KEY scope → publish-hex jobs | Env-scope limits secret exposure to only environment-gated jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-11-01 | E (Elevation of privilege) | Single-reviewer self-Approve bypass (D-27 prevent_self_review limitation) | mitigate | D-27 acknowledges this honestly: GitHub silently disables prevent_self_review when only 1 reviewer exists. MAINTAINING.md "Bus Factor & Continuity" section (Plan 02 D-30) documents the gap explicitly rather than pretending the gate is two-eyes. Compensating controls: (a) D-15 Plan 08 reviewer summary forces informed Approve click; (b) Task 7 60-min smoke window catches errors that slipped past human review; (c) Task 8 post-publish-smoke is automated and runs after Approve. |
| T-07.1-11-02 | I (Information disclosure) | HEX_API_KEY repo-scope leak | mitigate | Task 2 explicit verification: `gh api /repos/.../actions/secrets` MUST NOT contain HEX_API_KEY; only `gh api /repos/.../environments/hex-publish/secrets` MUST contain it. Pre-publish gate. |
| T-07.1-11-03 | T (Tampering) | Broken artifact ships, 60-min window passes before detection | mitigate | Task 7 mandates literal-timer maintainer smoke within 60 min. Plan 09 post-publish-smoke runs concurrently in CI. Two parallel detectors give ~30-min worst-case for failure detection. Within window: revert. Past window: retire per D-28 step 1. |
| T-07.1-11-04 | T (Tampering) | release-please PR overwrites Plan 03 CHANGELOG narrative | mitigate | Task 4 step 4 mandates inspecting CHANGELOG.md diff in PR. release-please's auto-summary defers to existing `## [<version>]` sections; if conflict, maintainer manually pushes a fix to release-please's branch BEFORE merging. |
| T-07.1-11-05 | E (Elevation of privilege) | workflow_dispatch arbitrary-tag publish post-ceremony | mitigate | Plan 08 Task 1 D-13 workflow_dispatch is gated by `environment: hex-publish` Approve. Same human gate as automatic flow. Cannot bypass. |
| T-07.1-11-06 | R (Repudiation) | STATE.md update missing or incomplete; future maintainer can't reconstruct ceremony state | mitigate | Task 10 acceptance criteria asserts specific tokens (`v0.1.0 SHIPPED`, `Phase 07.1`, both package names, audit blocker IDs) in STATE.md. `docs(state):` commit type per CLAUDE.md convention preserves the marker in git history. |
| T-07.1-11-07 | I (Information disclosure) | Surprising paths in tarball discovered AFTER publish | mitigate | Plan 07 D-17 step 4 denylist + step 3 allowlist is the pre-publish gate. Task 6 step 3 mandates HUMAN review of the prepublish-summary file list before Approve click. Two-stage defense (automated + human-eye). |
| T-07.1-11-08 | T (Tampering) | Dependabot disabled silently; 3rd-party Action SHA pins rot | mitigate | Task 3 explicit verification of `.github/dependabot.yml` containing both `mix` and `github-actions` ecosystems BEFORE publish. Pre-flight gate. |
| T-07.1-11-09 | E (Elevation of privilege) | Scope creep into D-35 deferred items during ceremony | mitigate | Task 10 STATE.md update explicitly lists D-35 deferred items as STILL deferred. Documents scope discipline in the milestone-shipped marker. |
</threat_model>

<verification>
After all 10 tasks complete:
```bash
# Both packages live on Hex.pm
mix hex.info mailglass 0.1.0
mix hex.info mailglass_admin 0.1.0

# HexDocs built
curl -fsI https://hexdocs.pm/mailglass/0.1.0/
curl -fsI https://hexdocs.pm/mailglass_admin/0.1.0/

# Tag in repo
git tag --list 'mailglass-sibling-group-v*'
# Expected: mailglass-sibling-group-v0.1.0

# D-32 framing landed on the GitHub Release (Task 5)
gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | head -c 200 | grep -F "mailglass"
# Expected: at least 1 match (framing in first 200 chars)
gh release view mailglass-sibling-group-v0.1.0 --repo szTheory/mailglass --json body --jq '.body' | grep -E "Sorry!|Heads up!|Bear with us|Excited to announce" && echo "FAIL: forbidden phrase" || echo "brand-voice clean"

# Workflows green
gh run list --workflow publish-hex.yml --limit 1 --json conclusion --jq '.[0].conclusion'
# Expected: "success"
gh run list --workflow post-publish-smoke.yml --limit 1 --json conclusion --jq '.[0].conclusion'
# Expected: "success"

# STATE.md updated
grep -F "v0.1.0 SHIPPED" .planning/STATE.md
grep -F "Phase 07.1" .planning/STATE.md

# Secret scope clean
gh api /repos/szTheory/mailglass/actions/secrets --jq '.secrets[].name' | grep -F "HEX_API_KEY" || echo "clean (HEX_API_KEY not in repo scope)"
gh api /repos/szTheory/mailglass/environments/hex-publish/secrets --jq '.secrets[].name' | grep -F "HEX_API_KEY"
# Expected: present in env scope, absent in repo scope
```
</verification>

<success_criteria>
1. GitHub Environment `hex-publish` settings verified to match D-27 table EXACTLY (Task 1).
2. `HEX_API_KEY` is environment-scoped only, NOT repo-scoped (Task 2).
3. Dependabot is enabled for both `mix.lock` and `.github/workflows/` (Task 3).
4. release-please PR (from Plan 10) is approved + squash-merged; tag `mailglass-sibling-group-v0.1.0` exists in repo (Task 4).
5. GitHub Release `mailglass-sibling-group-v0.1.0` body has the D-32 framing paragraph (~150 words, brand voice) prepended above the auto-generated CHANGELOG body, with `---` separator; framing mentions "mailglass" within the first 200 characters; no forbidden brand-voice phrases (Task 5).
6. `hex-publish` deployment Approved by maintainer in GitHub Environment UI; `publish-core` + `publish-admin` jobs both `success` (Task 6).
7. Within 60 minutes of publish, maintainer ran `mix mailglass.install` against the published artifact in a fresh Phoenix host; install + compile + endpoint smoke all green (Task 7).
8. post-publish-smoke workflow (Plan 09) completed `success` within 30 minutes; HexDocs spot-check passed (Task 8).
9. `mix hex.info mailglass 0.1.0` and `mix hex.info mailglass_admin 0.1.0` both show v0.1.0 NOT retired/reverted (Task 9).
10. `.planning/STATE.md` updated to mark v0.1.0 SHIPPED with timestamp, Phase 07.1 closed, all audit blockers (G-1..G-5, B-1, V-05, V-07) closed, deferred items per D-35 confirmed still deferred (Task 10).
11. Plan 11 commit uses `docs(state):` Conventional Commit type per CLAUDE.md convention.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-11-SUMMARY.md`.
</output>
