---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - lib/mix/tasks/mailglass.publish.check.ex
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass_admin-files.expected
autonomous: true
requirements: []
audit_blocker: null
decisions_addressed: [D-17, D-18]

must_haves:
  truths:
    - "`mix mailglass.publish.check --package mailglass` runs all 16 D-17 procedure steps in order and exits non-zero on any failure"
    - "`mix mailglass.publish.check --package mailglass_admin` runs the same 16-step procedure with `MIX_PUBLISH=true` set so `mailglass_dep/0` flips to the Hex constraint"
    - "Existing denylist (`_build/`, `deps/`, `.git/`, `.gsd/`, `.planning/`, `.claude/`) is PRESERVED as belt-and-suspenders alongside the new allowlist diff (D-17 step 4)"
    - "Allowlist diff reads `.planning/publish/<pkg>-files.expected` and fails loudly with a unified-diff body listing added/removed paths (D-17 step 3)"
    - "Tarball-size guardrails warn at 4 MB and fail at 6 MB total uncompressed (D-17 step 5; Hex hard limit 8 MB)"
    - "Reviewer summary written to `$GITHUB_STEP_SUMMARY` (when env var set) per D-17 step 15: package name, version delta, file count, total size, CHANGELOG section excerpt, top-10 files by size, full file list collapsed in `<details>`"
    - "All failure messages use the `Delivery blocked: ...` brand-voice prefix per D-18 with offending value + the exact fix to take"
    - "Status verbs `[create]`, `[update]`, `[unchanged]`, `[conflict]` are used consistent with `mix mailglass.install` output style (D-18)"
    - "Two NEW golden allowlist files (`.planning/publish/mailglass-files.expected` and `.planning/publish/mailglass_admin-files.expected`) exist, sorted, one path per line"
  artifacts:
    - path: "lib/mix/tasks/mailglass.publish.check.ex"
      provides: "Expanded 16-step pre-publish check pipeline (~150-300 LOC)"
      contains: "OptionParser.parse(argv, strict: [package: :string"
      not_contains: "IO.puts"
    - path: ".planning/publish/mailglass-files.expected"
      provides: "v0.1.0 baseline file allowlist for mailglass core tarball"
      contains: "mix.exs"
    - path: ".planning/publish/mailglass_admin-files.expected"
      provides: "v0.1.0 baseline file allowlist for mailglass_admin tarball"
      contains: "mix.exs"
  key_links:
    - from: "mix mailglass.publish.check"
      to: ".planning/publish/<pkg>-files.expected"
      via: "File.read! + sort + diff against unpacked file list"
      pattern: "files.expected"
    - from: "mix mailglass.publish.check (D-17 step 15)"
      to: "$GITHUB_STEP_SUMMARY"
      via: "File.write/3 of markdown summary"
      pattern: "GITHUB_STEP_SUMMARY"
    - from: "mix mailglass.publish.check"
      to: "publish-hex.yml prepublish-summary job (Plan 08 D-15)"
      via: "shell invocation in CI prior to environment-gated publish"
      pattern: "mix mailglass.publish.check"
---

<objective>
Expand `lib/mix/tasks/mailglass.publish.check.ex` from current ~58 LOC denylist-only to the full 16-step procedure mandated by D-17, plus generate the two NEW `.planning/publish/<pkg>-files.expected` golden allowlist files that step 3 reads. Per D-17 the existing denylist stays as belt-and-suspenders (step 4) — this is in-place expansion, NOT a rewrite.

Per D-18: all output adopts mailglass brand voice ("thoughtful maintainer, clear, exact, confident not cocky"). Status verbs `[create]/[update]/[unchanged]/[conflict]` mirror `mix mailglass.install`. Failure messages use `Delivery blocked: ...` prefix with the offending value AND the exact fix.

Purpose: Without this expansion, the pre-publish check is a single denylist regex that catches the audit's exact failure class only by accident. The 16-step procedure converts pre-publish into a defensible audit trail: allowlist diff (step 3) is the inverse of the audit's lesson — assert against EXPECTED, not deny against forbidden. The reviewer-summary (step 15) is what makes the GitHub Environment Approve click informed instead of rubber-stamped (Plan 08 D-15 wires this into the publish-hex.yml `prepublish-summary` job).

Output: One Mix task expanded in place + two new golden allowlist files committed to `.planning/publish/`. No CI files touched (Plan 08 wires this into publish-hex.yml).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md
@lib/mix/tasks/mailglass.publish.check.ex
@lib/mix/tasks/mailglass.reconcile.ex
@lib/mix/tasks/mailglass.install.ex
@mix.exs
@mailglass_admin/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand mix mailglass.publish.check.ex with the 16-step D-17 procedure</name>
  <files>lib/mix/tasks/mailglass.publish.check.ex</files>
  <read_first>
    - lib/mix/tasks/mailglass.publish.check.ex (current 58-LOC denylist-only implementation — preserve denylist as step 4)
    - lib/mix/tasks/mailglass.reconcile.ex (brand-voice + OptionParser strict + Mix.shell + exit({:shutdown, 1}) skeleton)
    - lib/mix/tasks/mailglass.install.ex lines 30-51 (real Mix task driver shape; status-verb output convention)
    - lib/mix/tasks/mailglass.install.ex lines 71-93, 102-107, 113-135 (status_label/1 + format_error/1 brand-voice analogs)
    - mix.exs (lines 201-208 package/0 block to introspect at runtime; lines 1-20 @version + @description)
    - mailglass_admin/mix.exs (lines 86-92 mailglass_dep/0 MIX_PUBLISH switch — D-17 step 10 verifies this resolves to `{:mailglass, "== <X>"}`)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (D-17 lines 84-101 verbatim 16-step procedure; D-18 lines 103-106 brand voice)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 263-360 brand-voice + status-verb + denylist-preservation pattern; lines 781-812 Mix-task structure)
  </read_first>
  <action>
    Rewrite `lib/mix/tasks/mailglass.publish.check.ex` to implement the full D-17 16-step pipeline. Target size: ~250-300 LOC. Structure follows `mailglass.reconcile.ex` skeleton: `use Boundary, classify_to: Mailglass`, `@shortdoc`, full `@moduledoc` with usage block, `OptionParser.parse(argv, strict: [package: :string, keep: :boolean])`, `Mix.Task.run("app.start")`, all I/O via `Mix.shell().info/1` and `Mix.shell().error/1` (NEVER `IO.puts`), `exit({:shutdown, 1})` on hard failure.

    **CLI surface (D-17):**
    ```
    mix mailglass.publish.check
    mix mailglass.publish.check --package mailglass
    mix mailglass.publish.check --package mailglass_admin
    mix mailglass.publish.check --package mailglass --keep
    ```
    - `--package` (`:string`, optional, choices `mailglass | mailglass_admin`) — when omitted at repo root, run for both packages sequentially.
    - `--keep` (`:boolean`, default `false`) — preserve `_publish_check/` for inspection (D-17 step 16).
    - Reject unknown args via `validate_cli!/2` mirroring `mailglass.install.ex` lines 53-69 — `Mix.raise("Delivery blocked: unknown args [...]")`.

    **Per-package pipeline (called once for each `--package` target). Each step prints `[create] <step>` / `[update] <step>` / `[unchanged] <step>` / `[conflict] <step>` status before its body runs, mirroring `mailglass.install.ex` `status_label/1`. On success print final `[unchanged] check complete` line. On hard failure print `[conflict] <step>` then `Mix.shell().error("Delivery blocked: ...")` then `exit({:shutdown, 1})`.**

    **Step 1 — Build + unpack tarball.**
    `cd <pkg_dir> && mix hex.build --unpack --output _publish_check/<pkg>`. For `mailglass_admin`, set `MIX_PUBLISH=true` first via `System.put_env("MIX_PUBLISH", "true")` BEFORE invoking `mix hex.build` so `mailglass_dep/0` flips to the Hex constraint. Restore the original env var afterwards in an `after` clause.
    Failure: `Delivery blocked: hex.build failed for <pkg>. Run \`mix hex.build\` locally to reproduce. Output: <captured>`.

    **Step 2 — File-list capture.**
    Walk the unpacked dir, collect relative paths + sizes. Sort lexicographically. Write to `_publish_check/<pkg>-files.actual`, one path per line.

    **Step 3 — Files allowlist diff (the inverse of the audit's lesson).**
    Read `.planning/publish/<pkg>-files.expected`. If absent: `Delivery blocked: missing golden allowlist .planning/publish/<pkg>-files.expected. Generate it by running \`mix mailglass.publish.check --package <pkg> --keep\` once and copying _publish_check/<pkg>-files.actual into place. Then commit.`
    Compare expected vs actual. If diff: `Delivery blocked: package files diff. Files added since last allowlist update: [<added paths>]. Files removed since last allowlist update: [<removed paths>]. Either update .planning/publish/<pkg>-files.expected or exclude in mix.exs :package :files.`
    Print the unified diff body BELOW the message so reviewer sees the exact paths.

    **Step 4 — Forbidden-files denylist (PRESERVE current behavior).**
    Patterns (extend current list per D-17 step 4): `~r/^_build/`, `~r/^deps/`, `~r/^\.git/`, `~r/^\.gsd/`, `~r/^\.planning/`, `~r/^\.claude/`, `~r/^\.elixir_ls/`, `~r/^cover/`, `~r/^node_modules/`, `~r/^priv\/native/`, `~r/\.env/`, `~r/\.DS_Store$/`, `~r/\.beam$/`, `~r/\.dump$/`. For `mailglass_admin` only: also `~r/^assets\//` (source CSS — only `priv/static/` ships per Phase 7 D-04).
    Failure: `Delivery blocked: forbidden files in tarball: [<list>]. Adjust mix.exs :package :files to exclude these paths.`

    **Step 5 — Tarball-size guardrails.**
    Compute total uncompressed bytes (sum of file sizes). Warn at 4 MB (`Mix.shell().info("[update] tarball size <N> bytes (warning: >4 MB)")`). Fail at 6 MB (`Delivery blocked: tarball size <N> bytes exceeds 6 MB threshold (Hex hard limit 8 MB). Trim package contents.`).

    **Step 6 — Required-files presence.**
    All packages: assert `LICENSE`, `README.md`, `CHANGELOG.md`, `mix.exs` present in unpacked dir. For `mailglass_admin` ALSO assert: `priv/static/app.css` present AND `File.stat!(...).size >= 1024` (catches empty-CSS regression where Tailwind didn't run); all `priv/static/fonts/*.woff2` present (count >= 1); `priv/static/mailglass-logo.svg` present.
    Failures: `Delivery blocked: missing required file <path>. Add to mix.exs :package :files OR rebuild assets (cd mailglass_admin/assets && bun run build) for priv/static/* targets.`

    **Step 7 — CHANGELOG section presence.**
    Read `<unpacked>/CHANGELOG.md`. Compute current version from `mix.exs` `@version` of the package being checked. Regex: `~r/^##\s*\[?v?#{Regex.escape(version)}\]?/m`. Assert at least one match. Locate the next `## ` heading (or EOF); assert the section body between is non-empty (>0 non-blank lines).
    Failure: `Delivery blocked: CHANGELOG.md does not contain a non-empty section for version <X>. Add a v<X> entry per CHANGELOG conventions.`

    **Step 8 — Mix metadata sanity.**
    Parse the unpacked `mix.exs` via `Code.string_to_quoted!/1`. Walk AST to extract:
    - `description` — present, length ≤ 300, NOT starting with whitespace, NOT ending with `.`
    - `package[:licenses]` exactly `["MIT"]`
    - `package[:links]` includes `"GitHub"` (and for `mailglass_admin` also `"HexDocs"`)
    - `source_url` matches `homepage_url` and starts with `https://github.com/`
    - `@version` is valid SemVer (regex `~r/^\d+\.\d+\.\d+(-[\w.+-]+)?$/`) AND matches `.release-please-manifest.json` entry for that package.

    For each violation: `Delivery blocked: mix.exs metadata <field> <reason>. Fix <pkg>/mix.exs and rebuild.`

    **Step 9 — Forbidden dep shapes.**
    From the parsed AST, walk `deps/0` returning. Fail on any keyword `path:`, `git:`, or `github:` in any dep tuple.
    Failure: `Delivery blocked: forbidden dep keyword <:path|:git|:github> in <dep_name>. Hex packages must declare published-version constraints only.`
    For `mailglass_admin` the `:mailglass` dep MUST be of form `{:mailglass, "== <X>"}` (no `path:`) since `MIX_PUBLISH=true` was set in step 1 — failure here means `mailglass_dep/0` switch did not flip.

    **Step 10 — `mailglass_admin`-only: linked-version constraint validation.**
    Skip for `--package mailglass`. For `mailglass_admin`: extract the `:mailglass` dep from the AST. Assert it equals `{:mailglass, "== <X>"}` where `<X>` exactly matches the published `mailglass` package's `@version` (read from root `mix.exs`).
    Failure: `Delivery blocked: mailglass_admin's mailglass dep is "== <X>" but mailglass core declares @version "<Y>". Run release-please to re-link versions, then re-publish.`

    **Step 11 — No `:dev`/`:test` deps leak.**
    Create `tmp_dir = "_publish_check/<pkg>-deps-only"`. Copy unpacked `mix.exs` to `tmp_dir/mix.exs`. `cd tmp_dir && MIX_ENV=prod mix deps.get` — assert exit 0. (This validates the published manifest resolves cleanly on prod-only.)
    Failure: `Delivery blocked: prod deps.get failed for <pkg>. The published mix.exs has unresolvable :dev or :test deps in :prod context. Output: <captured>.`

    **Step 12 — Compile-from-tarball.**
    `cd _publish_check/<pkg> && mix compile --no-optional-deps --warnings-as-errors` in a fresh `MIX_HOME` (set via `System.put_env("MIX_HOME", System.tmp_dir!() <> "/mix-publish-check-<pkg>")`). Restore `MIX_HOME` after.
    Failure: `Delivery blocked: tarball failed to compile in isolation. <output tail>. The published artifact is broken — likely elixirc_paths(:test) leak or missing lib/ files.`

    **Step 13 — `mix hex.audit` clean.**
    `cd <pkg_dir> && mix hex.audit`. Assert exit 0.
    Failure: `Delivery blocked: mix hex.audit reported issues for <pkg>. <output>.`

    **Step 14 — `mix hex.outdated --within-requirements` (advisory).**
    `cd <pkg_dir> && mix hex.outdated --within-requirements`. NEVER fail on this. Capture stdout, append to summary as a `## Outdated (advisory)` block.

    **Step 15 — Reviewer summary.**
    If `System.get_env("GITHUB_STEP_SUMMARY")` is set, write a markdown summary via `File.write!(summary_path, content, [:append])`:
    ```
    ## Pre-publish check: <pkg> v<X>

    | Field | Value |
    |---|---|
    | Package | <pkg> |
    | Version delta | v<PREV> → v<X> |
    | File count | <N> |
    | Total size | <bytes> (<MB>) |

    ### CHANGELOG excerpt
    <first 30 lines of v<X> CHANGELOG section>

    ### Top 10 files by size
    | Path | Bytes |
    |---|---|
    | ... | ... |

    <details><summary>Full file list (<N>)</summary>

    ```
    <one path per line, sorted>
    ```

    </details>
    ```
    Compute `<PREV>` from `.release-please-manifest.json` if present; else write `none → v<X>` (first release case).

    **Step 16 — Cleanup.**
    Unless `--keep` was passed, `File.rm_rf!("_publish_check/<pkg>")`. With `--keep`: `Mix.shell().info("[unchanged] _publish_check/<pkg> retained for inspection")`.

    **Final output line per package:**
    ```
    Pre-publish check result for <pkg>: create=<N> update=<M> unchanged=<K> conflict=0
    ```
    Mirror `mailglass.install.ex` lines 102-107 `print_totals/2` shape. Counts come from how many steps emitted each status verb.

    **Anti-patterns to avoid (D-18):**
    - NO emoji anywhere in output.
    - NO "Sorry!", "Heads up!", "Bear with us", "Excited to announce" — neutral, declarative.
    - NEVER use `IO.puts` — always `Mix.shell().info/1` or `Mix.shell().error/1`.
    - DO NOT remove the existing denylist (step 4 PRESERVES it as belt-and-suspenders per D-17).
    - DO NOT skip step 11 / 12 even though they're slow — they're the only thing that catches `elixirc_paths(:test)` leaks.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 &amp;&amp; mix help mailglass.publish.check &amp;&amp; grep -F 'OptionParser.parse' lib/mix/tasks/mailglass.publish.check.ex &amp;&amp; grep -F 'Delivery blocked:' lib/mix/tasks/mailglass.publish.check.ex &amp;&amp; grep -cE '\[create\]|\[update\]|\[unchanged\]|\[conflict\]' lib/mix/tasks/mailglass.publish.check.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 (no warnings introduced)
    - `mix help mailglass.publish.check` returns the new `@moduledoc` with usage block including `--package` and `--keep` flags
    - `grep -F "OptionParser.parse(argv, strict: [package: :string, keep: :boolean]" lib/mix/tasks/mailglass.publish.check.ex` returns 1 line
    - `grep -cF "Delivery blocked:" lib/mix/tasks/mailglass.publish.check.ex` returns at least 10 (one per failure mode minimum across the 13 fail-able steps)
    - `grep -cE '"\[create\]"|"\[update\]"|"\[unchanged\]"|"\[conflict\]"' lib/mix/tasks/mailglass.publish.check.ex` returns at least 4 (status-verb labels defined)
    - `grep -cF "IO.puts" lib/mix/tasks/mailglass.publish.check.ex` returns 0 (no `IO.puts` allowed; brand-voice via `Mix.shell()`)
    - `grep -F "MIX_PUBLISH" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (step 1 sets it for mailglass_admin)
    - `grep -F "GITHUB_STEP_SUMMARY" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (step 15)
    - `grep -F "hex.audit" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (step 13)
    - `grep -F "hex.outdated" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (step 14)
    - `grep -F "_build" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (denylist preserved per D-17 step 4)
    - `grep -F ".elixir_ls" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (denylist extended per D-17 step 4)
    - `grep -F ".planning/publish" lib/mix/tasks/mailglass.publish.check.ex` returns at least 1 line (allowlist read path per D-17 step 3)
    - `wc -l lib/mix/tasks/mailglass.publish.check.ex` returns ≥ 200 lines (was 58; expansion mandatory per D-17)
    - `grep -F "use Boundary" lib/mix/tasks/mailglass.publish.check.ex` returns 1 line (Mix-task structure per PATTERNS.md)
  </acceptance_criteria>
  <done>
    `lib/mix/tasks/mailglass.publish.check.ex` implements all 16 D-17 steps in order with brand-voice output (D-18). The existing denylist is preserved as step 4 (belt-and-suspenders). The task accepts `--package` and `--keep` flags via OptionParser strict. All output uses `Mix.shell()`; no `IO.puts`. All failure messages prefix `Delivery blocked:` with offending value + actionable fix. Status verbs `[create]`, `[update]`, `[unchanged]`, `[conflict]` are used consistently. `mix compile --warnings-as-errors` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate the two NEW .planning/publish/&lt;pkg&gt;-files.expected golden allowlists</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass_admin-files.expected</files>
  <read_first>
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (D-17 step 3 lines 89-90 — generation strategy)
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 836-841 — "No Analog Found" entry documents the generation strategy)
    - mix.exs (root `package/0` :files declaration to know what mailglass tarball SHOULD contain)
    - mailglass_admin/mix.exs (`package/0` :files declaration for mailglass_admin)
  </read_first>
  <action>
    These two files are the v0.1.0 baseline allowlists that Task 1's step-3 reads. Generation strategy (per PATTERNS.md "No Analog Found"):

    **Generation procedure (executor runs this):**
    1. Verify Task 1 has shipped (`mix help mailglass.publish.check` shows `--keep` flag).
    2. Run for `mailglass` core:
       ```
       mix mailglass.publish.check --package mailglass --keep
       ```
       This will FAIL at step 3 (no golden file present yet — step 3 says missing-allowlist error). That's expected on first run.
    3. The `--keep` flag preserves `_publish_check/mailglass/` and `_publish_check/mailglass-files.actual`. Copy the actual file into the golden location:
       ```
       mkdir -p .planning/publish
       cp _publish_check/mailglass-files.actual .planning/publish/mailglass-files.expected
       ```
    4. Repeat for `mailglass_admin`:
       ```
       mix mailglass.publish.check --package mailglass_admin --keep
       cp _publish_check/mailglass_admin-files.actual .planning/publish/mailglass_admin-files.expected
       ```
    5. Sort each file lexicographically (`sort -o .planning/publish/mailglass-files.expected .planning/publish/mailglass-files.expected` and same for admin) so subsequent diffs are deterministic.
    6. Sanity-check the contents: each file must contain `mix.exs`, `LICENSE`, `README.md`, `CHANGELOG.md`. For `mailglass_admin-files.expected`, also `priv/static/app.css` and `priv/static/mailglass-logo.svg`.
    7. Cleanup: `rm -rf _publish_check`.

    **What these files ARE:** committed-to-git source-of-truth allowlists. Each row is a single relative path inside the unpacked tarball. Sorted. One path per line. No comments, no headers, no trailing whitespace.

    **Why `.planning/publish/` (Claude's discretion per CONTEXT D-17 step 3):** keeps publish-bookkeeping co-located with other planning artifacts; explicitly excluded from the Hex tarball by Plan 01's denylist + Plan 07 step 4 denylist (`.planning/` is forbidden), so these golden files never ship to adopters.

    **Why this is NOT in the Mix task:** the golden file is the assertion target; if the Mix task generated it on every run, allowlist drift would be silent. Treating the file as committed code makes any drift a reviewable diff in PR.

    **Anti-pattern to avoid:** do NOT hand-author these files based on `package/0` :files inspection — `mix hex.build --unpack` is the source of truth (it expands wildcards, applies hex.exs file resolution rules, etc.). The audit's exact failure mode (G-2/G-5: simulation-vs-reality drift) recurs if this generation step is bypassed.
  </action>
  <verify>
    <automated>test -f .planning/publish/mailglass-files.expected &amp;&amp; test -f .planning/publish/mailglass_admin-files.expected &amp;&amp; grep -F "mix.exs" .planning/publish/mailglass-files.expected &amp;&amp; grep -F "mix.exs" .planning/publish/mailglass_admin-files.expected &amp;&amp; grep -F "LICENSE" .planning/publish/mailglass-files.expected &amp;&amp; grep -F "priv/static/app.css" .planning/publish/mailglass_admin-files.expected</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/publish/mailglass-files.expected` returns 0
    - `test -f .planning/publish/mailglass_admin-files.expected` returns 0
    - `grep -cF "mix.exs" .planning/publish/mailglass-files.expected` returns at least 1
    - `grep -cF "LICENSE" .planning/publish/mailglass-files.expected` returns at least 1
    - `grep -cF "README.md" .planning/publish/mailglass-files.expected` returns at least 1
    - `grep -cF "CHANGELOG.md" .planning/publish/mailglass-files.expected` returns at least 1
    - `grep -cF "mix.exs" .planning/publish/mailglass_admin-files.expected` returns at least 1
    - `grep -cF "priv/static/app.css" .planning/publish/mailglass_admin-files.expected` returns at least 1
    - `grep -cF "priv/static/mailglass-logo.svg" .planning/publish/mailglass_admin-files.expected` returns at least 1
    - Both files are sorted: `sort -c .planning/publish/mailglass-files.expected` exits 0; same for admin.
    - Neither file contains forbidden paths: `grep -E "^(_build|deps|\.git|\.gsd|\.planning|\.claude|\.elixir_ls)" .planning/publish/mailglass-files.expected | wc -l` returns 0; same for admin.
    - Running `mix mailglass.publish.check --package mailglass` (with the goldens in place) exits 0 (full pipeline passes against the v0.1.0 baseline).
    - Running `mix mailglass.publish.check --package mailglass_admin` exits 0.
  </acceptance_criteria>
  <done>
    Both golden allowlist files exist under `.planning/publish/`, sorted, contain the required-files baseline (LICENSE, README.md, CHANGELOG.md, mix.exs everywhere; priv/static/app.css and mailglass-logo.svg additionally for admin), and no forbidden paths leak in. With them in place, `mix mailglass.publish.check --package mailglass` and `--package mailglass_admin` both exit 0 against the v0.1.0 tarball state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-publish CI → Hex.pm publish gate | Pre-publish check is the LAST programmatic assertion before the artifact ships forever |
| Tarball file list → adopter machines | Anything in the tarball is downloaded by every adopter; secret leak via tarball is unrecoverable |
| Mix task output → reviewer summary | Reviewer-summary informs the human Approve click; misleading output enables silent footgun |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-07-01 | I (Information disclosure) | Tarball file disclosure (e.g. .env, .planning/, .claude/, .elixir_ls/, *.beam) | mitigate | Step 4 denylist (preserved per D-17) catches forbidden patterns; step 3 allowlist diff catches NEW paths not in golden. Two-layer defense: a new forbidden path that bypasses the denylist regex still fails the allowlist diff because it's not in `.expected`. Acceptance criteria asserts both layers present. |
| T-07.1-07-02 | T (Tampering) | Allowlist drift via uncommitted goldens | mitigate | `.expected` files are committed source-of-truth; any drift is a reviewable PR diff. Step 3 fails closed (missing golden = `Delivery blocked` + actionable regen instruction); cannot be silently bypassed. |
| T-07.1-07-03 | E (Elevation of privilege) | Compile-from-tarball masking elixirc_paths(:test) leaks | mitigate | Step 12 (`cd _publish_check/<pkg> && mix compile --no-optional-deps --warnings-as-errors` in fresh `MIX_HOME`) compiles from the published artifact in isolation. Catches the class where in-tree compile passes because `lib/` + `test/` co-mingle, but tarball compile fails because `test/` was excluded. |
| T-07.1-07-04 | T (Tampering) | mailglass_admin linked-version race | mitigate | Step 10 asserts `mailglass_admin`'s `:mailglass` dep equals `{:mailglass, "== <root @version>"}` exactly. Catches release-please linked-versions plugin misfire where admin pins a version core hasn't published. |
| T-07.1-07-05 | D (DoS) | Tarball size DoS on Hex.pm | accept | Hex hard-limits at 8 MB; we warn at 4 MB and fail at 6 MB (step 5). Treating Hex's own DoS protection as load-bearing is acceptable. |
| T-07.1-07-06 | I (Information disclosure) | HEX_API_KEY exposure via Mix task output | accept | The Mix task itself never reads `HEX_API_KEY`. Only the `mix hex.publish` invocation in publish-hex.yml reads it. Mix task output is safe to surface in reviewer summary; no secret material flows through. (Plans 08/10/11 own HEX_API_KEY scope mitigation.) |
| T-07.1-07-07 | R (Repudiation) | Forbidden dep shapes (`path:`, `git:`) shipping in published artifact | mitigate | Step 9 walks `mix.exs` AST and fails on any `path:`/`git:`/`github:` keyword. The audit's exact failure class for sibling-package mis-publish (`mailglass_dep/0` MIX_PUBLISH switch failing to flip) is caught here AND at step 10. |
</threat_model>

<verification>
After both tasks land:
```bash
# Mix task expanded
wc -l lib/mix/tasks/mailglass.publish.check.ex
# Expected: ≥ 200

# Goldens exist
test -f .planning/publish/mailglass-files.expected && echo OK
test -f .planning/publish/mailglass_admin-files.expected && echo OK

# Full pipeline passes against v0.1.0 baseline
mix mailglass.publish.check --package mailglass
mix mailglass.publish.check --package mailglass_admin

# Brand voice + status verbs present
grep -cF "Delivery blocked:" lib/mix/tasks/mailglass.publish.check.ex
# Expected: ≥ 10
grep -cE '"\[create\]"|"\[update\]"|"\[unchanged\]"|"\[conflict\]"' lib/mix/tasks/mailglass.publish.check.ex
# Expected: ≥ 4

# No IO.puts
grep -cF "IO.puts" lib/mix/tasks/mailglass.publish.check.ex
# Expected: 0
```
</verification>

<success_criteria>
1. `lib/mix/tasks/mailglass.publish.check.ex` implements all 16 D-17 steps in order, ~250-300 LOC.
2. Existing denylist is preserved as step 4 (PATTERNS.md + D-17 mandate).
3. Allowlist diff (step 3) reads `.planning/publish/<pkg>-files.expected` and fails loudly with offending paths + actionable fix.
4. Reviewer summary (step 15) writes to `$GITHUB_STEP_SUMMARY` when env var set.
5. All output uses brand voice: `Delivery blocked: ...` prefix on failures; status verbs `[create]/[update]/[unchanged]/[conflict]`; no emoji; no apology; no `IO.puts`.
6. Both `.planning/publish/<pkg>-files.expected` golden files exist, sorted, contain required-files baseline, no forbidden paths.
7. Full pipeline passes against v0.1.0 tarball state for both packages.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-07-SUMMARY.md`.
</output>
