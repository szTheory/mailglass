---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
  - SECURITY.md
autonomous: true
requirements: []
audit_blocker: null
decisions_addressed: [D-26, D-27, D-28, D-29, D-30, D-34]

must_haves:
  truths:
    - "MAINTAINING.md contains FOUR new sections (Bus Factor & Continuity, Retract Decision Tree, Security Response SLA, Release Runbook) appended after existing content"
    - "MAINTAINING.md acknowledges single-maintainer reality + silently-disabled `prevent_self_review` per D-27 (no two-eyes pretense)"
    - "MAINTAINING.md contains the 5-line retract decision tree from D-28 verbatim"
    - "MAINTAINING.md Release Runbook explicitly mentions the literal 60-minute revert window per CONTEXT specifics"
    - "SECURITY.md does NOT contain the placeholder string `security@example.com`"
    - "SECURITY.md SLA claims 72h ack / 14d critical (not 24h/7d) per D-29"
    - "All new prose is brand-voice-conformant: no `Sorry!`, no `Heads up!`, no `Bear with us`, no `Excited to announce`, no emoji"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Bus factor, retract policy, SLA, runbook"
      min_lines: 100
      contains: "## Bus Factor &amp; Continuity"
    - path: "SECURITY.md"
      provides: "Real disclosure address + honest SLA"
      contains: "72 hours"
  key_links:
    - from: "MAINTAINING.md Release Runbook step 4"
      to: "Hex.pm 60-min mix hex.publish --revert window"
      via: "literal 60-minute timer note"
      pattern: "60[ -]min"
    - from: "MAINTAINING.md Bus Factor section"
      to: "D-27 honest disclosure of disabled prevent_self_review"
      via: "explicit mention of single-reviewer GitHub UX"
      pattern: "single[- ]?(maintainer|reviewer)"
---

<objective>
Append four governance sections to `MAINTAINING.md` (Bus Factor & Continuity, Retract Decision Tree, Security Response SLA, Release Runbook) per D-30 and replace the placeholder email + aspirational SLA in `SECURITY.md` per D-29. All prose follows mailglass brand voice ("thoughtful maintainer, clear, exact, confident not cocky") with the explicit anti-pattern list from D-31.

Purpose: First-release table-stakes security artifacts (D-34). Documents the single-maintainer reality honestly (D-27 silently-disabled `prevent_self_review` workaround) instead of pretending the gate is two-eyes. Bounds the retract behavior so future patches don't accidentally retire packages. Sets a literal 60-minute revert-window timer that the publish ceremony (Plan 11) operationalizes.

Output: Two markdown files updated in place. `MAINTAINING.md` grows from ~29 lines to ~100+ lines; `SECURITY.md` rewrites the disclosure paragraph in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md
@.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md
@MAINTAINING.md
@SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append four sections to MAINTAINING.md per D-30</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (entire file — 29 lines) so executor sees current 3-section structure (Release Flow, Snapshot Update Protocol, Required Checks) and preserves verbatim
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (lines 142-176) — D-26 through D-30 verbatim
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 631-654) — pattern map for this surgical append
  </read_first>
  <action>
    PRESERVE the existing 29 lines of MAINTAINING.md verbatim. APPEND the four new sections below, in this exact order, after the current `## Required Checks` section. Use the prose templates below — they encode D-26..D-30 verbatim where they specify exact content, with brand-voice rewrites where Claude's discretion is allowed (D-30 short-form discretion bullets).

    Brand-voice anchor (apply throughout): "thoughtful maintainer, clear, exact, confident not cocky, warm not cute, modern not trendy, technical not intimidating." NO emoji. NO "Sorry!", "Heads up!", "Bear with us", "Excited to announce." Use status verbs (`create`, `update`, `unchanged`, `conflict`) and specific actionable failure messages where applicable.

    **Section to append (verbatim — copy these markdown blocks literally, only adjusting Claude's-discretion prose where indicated):**

    ```markdown

    ## Bus Factor &amp; Continuity

    Mailglass is single-maintainer at v0.1. The release pipeline is gated on a GitHub
    Environment (`hex-publish`) with a single required reviewer (`szTheory`). When a
    GitHub Environment has only one reviewer, GitHub silently disables the
    `prevent_self_review` setting — the gate is effectively a one-eye pause, not a
    two-eyes review. This is documented honestly here rather than presented as a
    stronger control than it is. Multi-owner Hex transition is deferred to v0.5,
    when production adopters exist (D-26 rationale: at v0.1 the asymmetry of a
    co-owner being able to `mix hex.publish` from their own machine bypassing
    GitHub governance is a worse footgun than the bus-factor risk it solves).

    If `szTheory` is unreachable for more than 30 days, the community can request a
    Hex.pm package transfer by opening a public issue titled
    `Maintainer-unreachable: requesting Hex transfer` on
    https://github.com/szTheory/mailglass/issues — Hex.pm's public maintainer-transfer
    process can be initiated from there.

    ## Retract Decision Tree

    Five rules. Bias toward patch over retract — three retractions in your first six
    months tells evaluators "don't bet on this lib."

    1. **Data-loss / security / signature bypass / fails to compile.**
       Run `mix hex.retire <pkg> <ver> security|invalid --message "<140 chars>"`
       AND ship `<ver+1>` immediately.
    2. **User-visible breakage with workaround.**
       Do NOT retire. Patch within 7 days. Add a CHANGELOG entry.
    3. **Cosmetic / docs / non-runtime.**
       Do NOT retire. Roll into next planned patch.
    4. **Published less than 60 minutes ago AND zero downloads.**
       Run `mix hex.publish --revert <ver>` (only window where unpublish works —
       also bounded by Hex.pm's 24-hour initial-release window).
    5. **Already retired and false alarm.**
       Run `mix hex.retire <pkg> <ver> --unretire`.

    ## Security Response SLA

    Single-maintainer numbers, written to be kept rather than aspired to.

    - **Acknowledgement of report:** within 72 hours.
    - **Mitigation or workaround for critical issues:** within 14 days.
    - **Public security advisory:** published alongside the fix.

    Critical issue classes are listed in `SECURITY.md` (`## Critical Classes`).
    Reports go through the disclosure address documented there or via GitHub
    Private Vulnerability Reporting if no email is reachable.

    ## Release Runbook

    Five steps. Step 4 has a literal 60-minute timer — that is the last revert
    window before the published artifact becomes permanent.

    1. **Verify CI green on `main` for the SHA to be released.**
       Check `actions/workflows/ci.yml` — required because publish-hex.yml gates
       on this SHA via the `gate-ci-green` job (per Plan 08, D-16).
    2. **Merge the release-please PR.**
       Squash-merge keeps the changelog history linear. The merge commit is what
       release-please tags as `mailglass-sibling-group-v<version>`.
    3. **Approve the `hex-publish` deployment in the GitHub Environment UI.**
       Review the pre-publish summary in the workflow run page (rendered by the
       `prepublish-summary` job per D-15) BEFORE clicking Approve. Verify the
       file count, total size, CHANGELOG excerpt, and top files all match
       expectations.
    4. **Within 60 minutes of publish: smoke-install in a fresh Phoenix app.**
       Set a literal timer when approving the deployment.
       Run:

           mix archive.install hex phx_new --force
           mix phx.new sandbox --no-ecto --no-mailer --install
           cd sandbox
           # add {:mailglass, "~> 0.1"}, {:mailglass_admin, "~> 0.1"} to deps
           mix deps.get &amp;&amp; mix mailglass.install --yes &amp;&amp; mix compile --warnings-as-errors
           mix phx.server  # visit http://localhost:4000/dev/mail/

       If anything fails AND the publish was less than 60 minutes ago AND zero
       downloads have happened, the Retract Decision Tree rule 4
       (`mix hex.publish --revert`) is reachable. After 60 minutes the only
       options are retire-then-patch (rule 1) or patch-only (rule 2).

       The post-publish-smoke workflow (`.github/workflows/post-publish-smoke.yml`,
       Plan 09) runs the same smoke automatically — but it does not respect the
       60-minute window. Run the manual smoke during the window regardless.
    5. **Post the release link to Elixir Forum #libraries section** (post-publish,
       optional — performed by maintainer on their own cadence; not gated by
       Phase 07.1's milestone-shipped marker per CONTEXT line 14 / line 351).
       Body equals the GitHub Release narrative (CHANGELOG entry verbatim plus
       one framing paragraph for 0.x.0 minor bumps; verbatim CHANGELOG only for
       patches).
    ```

    Do NOT modify the existing three sections (Release Flow, Snapshot Update Protocol, Required Checks). Append starting on a new line AFTER the current end of file.
  </action>
  <verify>
    <automated>wc -l MAINTAINING.md &amp;&amp; grep -c "^## " MAINTAINING.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l MAINTAINING.md` returns at least `100` (was 29; +four sections of substantive content)
    - `grep -c "^## " MAINTAINING.md` returns exactly `7` (3 original + 4 new)
    - `grep -F "## Bus Factor &amp; Continuity" MAINTAINING.md | wc -l` returns `1`
    - `grep -F "## Retract Decision Tree" MAINTAINING.md | wc -l` returns `1`
    - `grep -F "## Security Response SLA" MAINTAINING.md | wc -l` returns `1`
    - `grep -F "## Release Runbook" MAINTAINING.md | wc -l` returns `1`
    - `grep -F "60 minute" MAINTAINING.md | wc -l` returns at least `1` OR `grep -F "60-minute" MAINTAINING.md | wc -l` returns at least `1` (literal timer note)
    - `grep -F "prevent_self_review" MAINTAINING.md | wc -l` returns at least `1` (honest disclosure)
    - `grep -F "single-maintainer" MAINTAINING.md | wc -l` returns at least `1` (Bus Factor honesty)
    - `grep -F "## Release Flow" MAINTAINING.md | wc -l` returns `1` (existing section preserved)
    - `grep -F "## Snapshot Update Protocol" MAINTAINING.md | wc -l` returns `1` (existing section preserved)
    - `grep -F "## Required Checks" MAINTAINING.md | wc -l` returns `1` (existing section preserved)
    - **Brand-voice grep (forbidden phrases must be ABSENT):** `grep -E "Sorry!|Heads up!|Bear with us|Excited to announce" MAINTAINING.md | wc -l` returns `0`
    - **Emoji check:** `grep -P "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{26FF}]|[\x{2700}-\x{27BF}]" MAINTAINING.md` returns nothing (no Unicode emoji)
    - `grep -F "mix hex.retire" MAINTAINING.md | wc -l` returns at least `2` (D-28 retract+unretire commands present)
    - `grep -F "mix hex.publish --revert" MAINTAINING.md | wc -l` returns at least `1` (D-28 rule 4)
    - **Forum-post scope honesty (Blocker 2):** `grep -F "Post the release link to Elixir Forum" MAINTAINING.md | wc -l` returns at least `1` (Release Runbook step 5 line present) AND `grep -F "post-publish, optional" MAINTAINING.md | wc -l` returns at least `1` (the line is annotated as post-publish-optional, per CONTEXT line 14 Out of scope + line 351 Deferred Ideas: Marketing announcements — the runbook honestly reflects that step 5 is not executed within Phase 07.1)
  </acceptance_criteria>
  <done>
    MAINTAINING.md preserves its three original sections verbatim AND has four new H2 sections appended in this order: Bus Factor &amp; Continuity, Retract Decision Tree, Security Response SLA, Release Runbook. Single-maintainer reality + silently-disabled prevent_self_review acknowledged. The literal 60-minute revert-window timer note is in the Release Runbook step 4. No forbidden brand-voice phrases or emoji.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder email + SLA in SECURITY.md per D-29</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (entire file — 29 lines) so executor sees current shape; PRESERVE Critical Classes + Disclosure Process sections verbatim
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-CONTEXT.md (line 165) — D-29 verbatim
    - .planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-PATTERNS.md (lines 657-668) — pattern map
  </read_first>
  <action>
    Replace the `## Reporting a Vulnerability` section (lines 11-15 of current `SECURITY.md`) IN PLACE. Per D-29 the placeholder `security@example.com` falls back to GitHub Private Vulnerability Reporting — there is no real disclosure email yet. Use this verbatim replacement:

    ```markdown
    ## Reporting a Vulnerability

    Do not open a public GitHub issue for security reports. Use GitHub's Private
    Vulnerability Reporting on this repository:
    https://github.com/szTheory/mailglass/security/advisories/new — this routes the
    report directly to the maintainer without exposing it.

    Single-maintainer SLA, written to be kept:

    - Acknowledgement of report: within 72 hours.
    - Mitigation or workaround for critical issues: within 14 days.
    - Public security advisory: published alongside the fix.
    ```

    PRESERVE the rest of the file verbatim:
    - `# Security Policy` H1 (line 1) — keep
    - `## Supported Versions` section + table (lines 3-9) — keep
    - `## Critical Classes` section (lines 17-22) — keep verbatim
    - `## Disclosure Process` section (lines 24-29) — keep verbatim

    Brand-voice (D-31 anchor): no `Sorry!`, no `Heads up!`, no apology, no emoji.
  </action>
  <verify>
    <automated>wc -l SECURITY.md &amp;&amp; grep -c "security@example.com" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "security@example.com" SECURITY.md | wc -l` returns `0` (placeholder removed)
    - `grep -F "Private Vulnerability Reporting" SECURITY.md | wc -l` returns at least `1` (PVR fallback per D-29)
    - `grep -F "https://github.com/szTheory/mailglass/security/advisories/new" SECURITY.md | wc -l` returns `1`
    - `grep -F "72 hours" SECURITY.md | wc -l` returns at least `1` (D-29 ack SLA)
    - `grep -F "14 days" SECURITY.md | wc -l` returns at least `1` (D-29 critical mitigation SLA)
    - `grep -F "24 hours" SECURITY.md | wc -l` returns `0` (old aspirational SLA removed)
    - `grep -F "7 days" SECURITY.md | wc -l` returns `0` (old aspirational mitigation removed)
    - `grep -F "## Critical Classes" SECURITY.md | wc -l` returns `1` (preserved)
    - `grep -F "## Disclosure Process" SECURITY.md | wc -l` returns `1` (preserved)
    - `grep -F "## Supported Versions" SECURITY.md | wc -l` returns `1` (preserved)
    - **Brand-voice grep:** `grep -E "Sorry!|Heads up!|Bear with us|Excited to announce" SECURITY.md | wc -l` returns `0`
  </acceptance_criteria>
  <done>
    SECURITY.md replaces `security@example.com` with the GitHub Private Vulnerability Reporting URL; SLA paragraph rewrites 24h/7d aspirational numbers to honest 72h ack / 14d critical / advisory-with-fix. The Supported Versions, Critical Classes, and Disclosure Process sections remain untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MAINTAINING.md → public docs | Honest single-maintainer disclosure shapes adopter expectations |
| SECURITY.md → public reporting channel | Determines how vulnerabilities reach the maintainer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-02-01 | I (Information disclosure) | SECURITY.md placeholder email | mitigate | D-29 fix: replace `security@example.com` with the real GitHub PVR URL. Placeholder addresses can route reports to attackers who register the domain or simply lose the report. PVR is in-platform and routes to repo admin only. |
| T-07.1-02-02 | R (Repudiation) | MAINTAINING.md silently-disabled prevent_self_review | mitigate | D-27 honest disclosure. Documenting the gap defeats a "we have two-eyes review" repudiation claim — adopters know the gate is one-eye and can compensate (extra CI checks, signed tags later, etc.). |
| T-07.1-02-03 | T | Retract Decision Tree | mitigate | D-28 explicit five-rule tree bounds the retract behavior; rule 4 (`mix hex.publish --revert` only within 60 min + zero downloads) prevents accidental unpublish on actively-used releases. |
| T-07.1-02-04 | I | MAINTAINING.md Bus Factor section | accept | Discloses single-maintainer reality publicly. This is a feature, not a bug — adopters need to know to plan their dependency strategy. The existing GitHub repo already exposes this fact via commit history. |
</threat_model>

<verification>
After both tasks land:
```bash
# All grep checks above pass
wc -l MAINTAINING.md SECURITY.md
grep -E "Sorry!|Heads up!|Bear with us|Excited to announce|security@example.com" MAINTAINING.md SECURITY.md && echo "FAIL: forbidden phrase present" || echo "brand-voice clean"
```
</verification>

<success_criteria>
1. MAINTAINING.md has 7 H2 sections: 3 original (Release Flow, Snapshot Update Protocol, Required Checks) + 4 new (Bus Factor & Continuity, Retract Decision Tree, Security Response SLA, Release Runbook).
2. MAINTAINING.md mentions the literal 60-minute revert-window timer in the Release Runbook step 4.
3. MAINTAINING.md acknowledges single-maintainer reality + silently-disabled prevent_self_review honestly.
4. MAINTAINING.md retract decision tree contains all 5 rules from D-28 (security retire, patch within 7 days, no-retire for cosmetic, 60-min publish revert, unretire).
5. SECURITY.md does NOT contain `security@example.com`.
6. SECURITY.md disclosure section uses GitHub Private Vulnerability Reporting URL.
7. SECURITY.md SLA paragraph claims 72h ack / 14d critical (not 24h/7d).
8. SECURITY.md preserves Supported Versions, Critical Classes, and Disclosure Process sections verbatim.
9. No forbidden brand-voice phrases (`Sorry!`, `Heads up!`, `Bear with us`, `Excited to announce`) in either file.
10. No Unicode emoji in either file.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-publish-v0-1-0-packages-to-hex-pm/07.1-02-SUMMARY.md`.
</output>
