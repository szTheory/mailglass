---
phase: 07.1-publish-v0-1-0-packages-to-hex-pm
plan: 01
subsystem: release
tags: [hexdocs, mix-exs, source-ref-pattern]
dependency_graph:
  requires: []
  provides: [hex-tarball-cleanup]
  affects:
    - mix.exs
    - mailglass_admin/mix.exs
tech_stack:
  added: []
  patterns: [hex-extras-cleanup, source-ref-pattern]
key_files:
  - mix.exs
  - mailglass_admin/mix.exs
decisions: [D-11, D-23, D-24]
metrics:
  duration: "≈15min"
  completed: "2026-04-25"
---

# Phase 07.1 Plan 01: HexDocs Cleanup + source_ref_pattern Summary

Surgical cleanup of `mix.exs` (root) and `mailglass_admin/mix.exs` so the Hex tarball / HexDocs reflect what actually ships, and so HexDocs `View Source` links resolve to the real linked-versions group tag.

## What Changed

- Stripped `.planning/ROADMAP.md` and `.planning/PROJECT.md` from root `mix.exs` `extras`, `groups_for_extras`, and `skip_undefined_reference_warnings_on` (D-23). These are project-internal planning artifacts and were never in `package[:files]`, so HexDocs `View Source` links would 404.
- Removed the entire `Planning:` group from `groups_for_extras`.
- Preserved `CLAUDE.md` in `extras` + `Overview` group per D-24 (advanced-adopter / contributor reference).
- Added `source_ref_pattern: "mailglass-sibling-group-v%{version}"` to `package/0` in BOTH `mix.exs` and `mailglass_admin/mix.exs` per D-11. HexDocs links now resolve to the actual tag produced by release-please's linked-versions plugin (e.g. `mailglass-sibling-group-v0.1.0`), instead of the per-package `v%{version}` default that is never created.
- Single commit: `c3b43d6` — 2 files changed, 3 insertions(+), 9 deletions(-).

## Deviations from Plan

None — exact textual edits per plan instructions.

## Verification

- `grep -cF "source_ref_pattern" mix.exs mailglass_admin/mix.exs` → 1 each ✓
- `grep -cF "ROADMAP.md\|PROJECT.md" mix.exs` → 0 ✓
- `grep -cF "CLAUDE.md" mix.exs` → 1 ✓ (preserved per D-24)
- `mix compile --warnings-as-errors` ✓ (no impact on compile)

## Self-Check

PASSED — both mix.exs files cleaned per D-23/D-24, source_ref_pattern in place per D-11. Commit `c3b43d6` on `main`.
