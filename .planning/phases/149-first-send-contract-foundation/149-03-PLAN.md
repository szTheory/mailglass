---
phase: 149-first-send-contract-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["149-02"]
files_modified:
  - lib/mailglass/config.ex
  - lib/mailglass/renderer.ex
  - test/mailglass/renderer_test.exs
  - test/mailglass/outbound_test.exs
  - test/mailglass/outbound/deliver_later_test.exs
  - mailglass_admin/test/mailglass_admin/preview_live_test.exs
autonomous: true
requirements:
  - FIRST-05
  - FIRST-06
must_haves:
  truths:
    - "D-08/FIRST-05: Explicit valid Unicode plaintext is preserved byte-for-byte, including when HTML also exists; text-only mail remains non-empty and sendable."
    - "D-09/FIRST-05: HTML-only mail gains generated plaintext only when renderer.plaintext is true; false leaves text_body absent and never deletes explicit text."
    - "D-10/FIRST-06: renderer.css_inliner :premailex runs the current inliner and :none skips only inlining while HEEx rendering and data-mg-* stripping still occur."
    - "D-07/FIRST-06: Direct render, sync send, async preparation, and preview all consume Mailglass.Renderer and expose the same body/config semantics."
  artifacts:
    - path: lib/mailglass/config.ex
      provides: "Validated Mailglass.Config.renderer/0 accessor for published renderer options"
      exports: ["renderer/0"]
    - path: lib/mailglass/renderer.ex
      provides: "Single body-precedence and config-dispatch implementation"
      exports: ["render/2", "to_plaintext/1"]
    - path: test/mailglass/renderer_test.exs
      provides: "Direct body/config truth table"
    - path: test/mailglass/outbound_test.exs
      provides: "Sync/async consumer parity regression"
    - path: mailglass_admin/test/mailglass_admin/preview_live_test.exs
      provides: "Preview renderer parity regression"
  key_links:
    - from: lib/mailglass/renderer.ex
      to: lib/mailglass/config.ex
      via: "Renderer reads validated `Config.renderer/0` once per render"
      pattern: "Config\\.renderer"
    - from: mailglass_admin/lib/mailglass_admin/preview_live.ex
      to: lib/mailglass/renderer.ex
      via: "Existing PreviewLive build_and_render path calls Mailglass.Renderer.render/1 unchanged"
      pattern: "Renderer\\.render"
  prohibitions:
    - "flagged-unverified: Preview, sync, and async paths must not grow their own plaintext or CSS-inlining implementation."
    - "flagged-unverified: Explicit plaintext must not be normalized, regenerated, or overwritten."
    - "flagged-unverified: This plan must not claim Phase 151 wire equivalence or dispatch-outcome convergence."
---

<objective>
Make `Mailglass.Renderer` honor the published body precedence and renderer switches once, then prove direct rendering, sync send, async preparation, and preview observe that one implementation per D-07 through D-10.

Purpose: Ensure the message adopters preview is the message Mailglass prepares to send, without overwriting authored content or forking rendering policy.
Output: Validated renderer config accessor, body-aware renderer pipeline, and cross-consumer parity regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/149-first-send-contract-foundation/149-CONTEXT.md
@.planning/phases/149-first-send-contract-foundation/149-RESEARCH.md
@.planning/phases/149-first-send-contract-foundation/149-PATTERNS.md
@.planning/phases/149-first-send-contract-foundation/149-VALIDATION.md
@guides/authoring-mailables.md
@guides/preview.md
@.planning/phases/149-first-send-contract-foundation/149-01-SUMMARY.md
@lib/mailglass/config.ex
@lib/mailglass/renderer.ex
@mailglass_admin/lib/mailglass_admin/preview_live.ex
@test/mailglass/renderer_test.exs
@test/mailglass/outbound_test.exs
@mailglass_admin/test/mailglass_admin/preview_live_test.exs
</context>

## Artifacts this phase produces

- `Mailglass.Config.renderer/0`: validated keyword accessor returning `plaintext: boolean()` and `css_inliner: :premailex | :none` defaults from the existing schema.
- Renderer body precedence: explicit text authoritative; text-only pass-through; conditional HTML-to-text generation; HTML-only CSS branch; `data-mg-*` stripping after either CSS mode.
- Config-isolated direct/sync/async/preview parity tests that restore application environment after each case.

<tasks>

<task type="auto" tdd="true">
  <name>Task 149-03-01: Implement renderer-owned body precedence and switches</name>
  <reversibility rating="costly">The published renderer switches and body precedence are adopter-visible across every rendering consumer, so a later semantic change coordinates docs and all call paths.</reversibility>
  <files>lib/mailglass/config.ex, lib/mailglass/renderer.ex, test/mailglass/renderer_test.exs</files>
  <read_first>Read `149-CONTEXT.md` D-07 through D-10, FIRST-05/FIRST-06, `149-RESEARCH.md` Pattern 2 and renderer body branch, `149-PATTERNS.md` Renderer and Config assignments, `lib/mailglass/config.ex` schema plus existing validated accessors (`theme/0`, `tracking/0`, `compliance/0`), and all current direct-render behavior/error tests in `renderer_test.exs`.</read_first>
  <behavior>
    - Test 1: Nonblank explicit plaintext survives render byte-for-byte for text-only and HTML+text messages, including Unicode/grapheme content; text-only leaves HTML absent.
    - Test 2: HTML-only with `renderer: [plaintext: true]` generates text; with false it leaves text absent. Empty plaintext is treated as absent for generation, while nonblank explicit text is never deleted in either setting.
    - Test 3: `css_inliner: :premailex` retains current inline-CSS behavior; `:none` leaves authored style declarations uninlined while still rendering HEEx and stripping all `data-mg-*` attributes.
    - Test 4: Direct invalid HTML retains `%TemplateError{}` behavior distinct from outbound preflight SendError.
  </behavior>
  <action>Add `Mailglass.Config.renderer/0` following existing accessors: return `validated_config() |> Keyword.fetch!(:renderer)` so default values and NimbleOptions validation stay centralized; Renderer must not call `Application.get_env` directly. Refactor `Renderer.render/2` inside the existing `Telemetry.render_span/2` into body-aware branches per D-08/D-09: render HTML only when present, retain a nonblank explicit `text_body` exactly, derive via `to_plaintext/1` only for HTML-only/blank-text input when `plaintext: true`, and allow text-only output without manufacturing HTML. Dispatch only the CSS stage per D-10: `:premailex` calls existing `inline_css/1`; `:none` returns rendered HTML unchanged, then both paths run `strip_mg_attributes/1`. Keep direct-render invalid HTML on TemplateError and keep Renderer free of Outbound/Repo/process dependencies. Add isolated tests that capture/restore all `:mailglass` env using the sanctioned SandboxOwnership helper; remove no existing plaintext-strategy coverage.</action>
  <acceptance_criteria>
    - Source assertion: Renderer gets both switches only through `Mailglass.Config.renderer/0` and owns all body/config branching.
    - Behavior assertion: explicit text is byte-for-byte stable; text-only is sendable; generated text obeys the boolean; CSS inlining obeys the enum without bypassing HEEx or attribute stripping.
    - Error assertion: direct invalid HTML still returns TemplateError, leaving outbound SendError mapping to Preflight.
    - Test command: `mix test test/mailglass/renderer_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/renderer_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIRST-05 body semantics and both published switches are implemented once in Renderer with direct tests for all branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 149-03-02: Prove sync, async, and preview renderer parity</name>
  <files>test/mailglass/outbound_test.exs, test/mailglass/outbound/deliver_later_test.exs, mailglass_admin/test/mailglass_admin/preview_live_test.exs</files>
  <read_first>Read `149-CONTEXT.md` D-07 and FIRST-06, the renderer call sites in `lib/mailglass/outbound.ex` and `mailglass_admin/lib/mailglass_admin/preview_live.ex`, existing Fake-adapter sent-message assertions in `outbound_test.exs`, prepared async assertions/setup in `outbound/deliver_later_test.exs`, the HTML/Text tab test and scenario setup in `preview_live_test.exs`, and `149-VALIDATION.md` Wave 0 config-isolation requirement.</read_first>
  <behavior>
    - Test 1: With plaintext disabled, direct-render truth from Task 1 is reproduced in the sync Fake-adapter message and the async prepared/persisted rendering seam without asserting Phase 151 provider wire equivalence.
    - Test 2: With explicit text present, sync and async preparation retain the same exact text despite plaintext generation setting.
    - Test 3: Preview HTML/Text tabs show the same generated-versus-absent/preserved text behavior for plaintext true and false.
    - Test 4: Preview with css_inliner `:none` shows rendered but non-inlined HTML with `data-mg-*` stripped; `:premailex` shows the inlined counterpart.
  </behavior>
  <action>Extend the existing outbound and LiveView tests; do not modify `PreviewLive` because it already delegates to `Mailglass.Renderer.render/1`. In sync tests assert the message recorded by `Mailglass.Adapters.Fake`; for async assert only the already-observable prepared/persisted rendering state, not eventual wire equivalence or dispatch outcomes reserved for Phase 151. In preview, toggle existing scenarios/tabs and assert semantic HTML/text markers under both renderer configurations. Wrap every env mutation with `Mailglass.TestSupport.SandboxOwnership.with_app_env!(:mailglass)` (or the file's established equivalent) and restore the prior config/cache in `on_exit`; make the files `async: false` where global config requires it. Use distinctive authored/generated strings so a test cannot pass by matching unrelated page chrome.</action>
  <acceptance_criteria>
    - Consumer assertion: sync, async preparation, and preview each exhibit the same Renderer truth table as direct render.
    - Architecture assertion: no rendering/config branch is added to Outbound or PreviewLive.
    - Isolation assertion: renderer configuration and relevant caches are restored after every test case.
    - Test command: `mix test test/mailglass/renderer_test.exs test/mailglass/outbound_test.exs test/mailglass/outbound/deliver_later_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/renderer_test.exs test/mailglass/outbound_test.exs test/mailglass/outbound/deliver_later_test.exs mailglass_admin/test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIRST-06 is proven across all four named consumers without a preview/outbound rendering fork or Phase 151 claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| validated application config → Renderer | Published booleans/enums control content transformation. |
| adopter-authored bodies → rendered Message | Explicit content crosses HEEx/plaintext/CSS transformations. |
| core Renderer → admin preview/outbound consumers | One transformed message is exposed by multiple surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-149-04 | Tampering | `Mailglass.Renderer.render/2` | high | mitigate | Truth-table tests preserve explicit text, constrain generation, and prove CSS switch behavior across consumers. |
| T-149-05 | Information Disclosure | PreviewLive rendered output | medium | mitigate | Preview continues consuming the same Renderer output; no additional body copying/logging/error context is introduced. |
| T-149-06 | Repudiation | divergent renderer consumers | medium | mitigate | Direct/sync/async/preview regression tests bind every consumer to one renderer-owned configuration contract. |
| T-149-SC | Tampering | package installs | high | mitigate | This plan has no install task; any install deviation must stop for the package-legitimacy audit, slopcheck, and a blocking human decision for `[ASSUMED]` or `[SUS]`. |
</threat_model>

<verification>
Run each focused command after its task. At wave completion, after Plan 149-02 also lands, run `mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/renderer_test.exs test/mailglass/tenancy_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- FIRST-05 explicit/plaintext-only/HTML-only semantics are exact, including empty and Unicode cases.
- FIRST-06 switches have identical observable effects in direct, sync, async-preparation, and preview paths.
- Renderer remains the sole implementation point and all env-changing tests are isolated.
</success_criteria>

<output>
Create `.planning/phases/149-first-send-contract-foundation/149-03-SUMMARY.md` when done.
</output>
