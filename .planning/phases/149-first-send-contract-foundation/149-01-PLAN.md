---
phase: 149-first-send-contract-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mailglass/outbound/preflight.ex
  - lib/mailglass/outbound.ex
  - test/mailglass/boundary_test.exs
  - test/mailglass/outbound/preflight_test.exs
  - test/mailglass/outbound/deliver_later_test.exs
  - test/mailglass/tenancy_test.exs
autonomous: true
requirements:
  - FIRST-01
  - FIRST-02
must_haves:
  truths:
    - "D-01/FIRST-01: An unstamped caller configured with Mailglass.Tenancy.SingleTenant can synchronously send one valid message as tenant \"default\"."
    - "D-01/FIRST-01: The same caller can select the existing durable async path and the returned/persisted delivery is owned by tenant \"default\"."
    - "D-02/FIRST-02: Missing, empty, or lost custom-tenant context fails with Mailglass.TenancyError rather than falling back to \"default\"."
    - "Every accepted resolver path places the resolved tenant identity on the Message before suppression, rate limiting, persistence, queue ownership, or routing reads it."
    - "Mailglass.Outbound.Preflight is Boundary-legal as an unexported internal child of Mailglass.Outbound, whose dependency list remains exactly [Mailglass]."
  artifacts:
    - path: lib/mailglass/outbound/preflight.ex
      provides: "Internal pure Mailglass.Outbound.Preflight.run/1 tenant normalization and initial envelope/body gate"
      exports: ["run/1"]
    - path: lib/mailglass/outbound.ex
      provides: "Sync, async, and batch entry-point convergence on shared preflight"
    - path: test/mailglass/outbound/preflight_test.exs
      provides: "Default and custom tenancy contract proof through the public sync path"
    - path: test/mailglass/boundary_test.exs
      provides: "Boundary declaration proof that Preflight remains an internal Outbound child"
    - path: test/mailglass/outbound/deliver_later_test.exs
      provides: "Default-tenant async selection and ownership proof"
  key_links:
    - from: lib/mailglass/outbound.ex
      to: lib/mailglass/outbound/preflight.ex
      via: "do_send/2, do_deliver_later/2, and preflight_single/1 call Preflight.run/1 first"
      pattern: "Preflight\\.run"
    - from: lib/mailglass/outbound/preflight.ex
      to: lib/mailglass/tenancy.ex
      via: "resolver-sensitive use of Tenancy.current/0 versus Tenancy.tenant_id!/0"
      pattern: "Tenancy\\.(current|tenant_id!)"
  prohibitions:
    - "flagged-unverified: Custom tenancy must not inherit the SingleTenant \"default\" fallback."
    - "flagged-unverified: The tracer must not redesign private envelope persistence, Oban argument content, or atomic enqueue owned by Phase 150."
---

<objective>
Establish the production-quality first-send tracer: one valid unstamped SingleTenant message crosses shared preflight, rendering, persistence, and sync/async selection with tenant identity `"default"`, while custom tenancy remains fail-closed per D-01 and D-02.

Purpose: Prove the phase's hardest architectural constraint before expanding the validation matrix: a single resolver-aware gate must normalize tenancy and run before every effectful outbound stage.
Output: Internal `Mailglass.Outbound.Preflight`, convergence wiring in `Mailglass.Outbound`, and focused sync/async tenancy regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/149-first-send-contract-foundation/149-CONTEXT.md
@.planning/phases/149-first-send-contract-foundation/149-RESEARCH.md
@.planning/phases/149-first-send-contract-foundation/149-PATTERNS.md
@.planning/phases/149-first-send-contract-foundation/149-VALIDATION.md
@docs/api_stability.md
@lib/mailglass/outbound.ex
@lib/mailglass/tenancy.ex
@lib/mailglass/errors/tenancy_error.ex
@test/mailglass/outbound/preflight_test.exs
@test/mailglass/outbound/deliver_later_test.exs
</context>

<assumption_delta_decision>
Primary noun: tenant identity (`tenant_id`). Decision: no-change. Resolver selection is configuration, while both SingleTenant and custom tenancy resolve the same tenant identity consumed by Message, persistence, queue ownership, and routing. The invariant is that every accepted resolver path supplies that identity; custom missing, empty, invalid, or unrestorable context fails closed.
</assumption_delta_decision>

## Artifacts this phase produces

- `Mailglass.Outbound.Preflight.run/1` in `lib/mailglass/outbound/preflight.ex`: internal pure contract returning `{:ok, normalized_message}` or a typed error before effects.
- Resolver-sensitive behavior: only configured `Mailglass.Tenancy.SingleTenant` may normalize an unstamped message to tenant `"default"`; custom resolvers require a non-empty stamped tenant identity.
- `Mailglass.Outbound` wiring that makes the shared preflight the first stage for sync, async, and each batch item.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 149-01-01: Send one unstamped SingleTenant message end to end</name>
  <reversibility rating="costly">The shared preflight ordering becomes the convergence contract inherited by Phases 150 and 151, though its internal module boundary remains replaceable.</reversibility>
  <files>lib/mailglass/outbound/preflight.ex, lib/mailglass/outbound.ex, test/mailglass/boundary_test.exs, test/mailglass/outbound/preflight_test.exs, test/mailglass/outbound/deliver_later_test.exs</files>
  <read_first>Read `149-CONTEXT.md` D-01/D-03, `149-RESEARCH.md` Patterns 1, Pitfall 1, and the resolved private-helper Boundary decision, `docs/api_stability.md` preflight and Tenancy sections, the root exports in `lib/mailglass.ex`, the `use Boundary` declaration plus `send/2`, `do_send/2`, `do_deliver_later/2`, `preflight_single/1`, and `prepare_outbound_message/1` in `lib/mailglass/outbound.ex`, the metadata assertions in `test/mailglass/boundary_test.exs`, and the existing setup/builders in both modified outbound test files.</read_first>
  <behavior>
    - Test 1: With tenancy configured as `Mailglass.Tenancy.SingleTenant`, no process stamp, and a Message whose tenant_id is nil, `Outbound.send/1` succeeds and the returned, persisted, and Fake-adapter-visible message/delivery tenant identity is `"default"`.
    - Test 2: The same valid unstamped message passed to `Outbound.deliver_later/2` with the existing durable async selection produces `{:ok, %Delivery{tenant_id: "default"}}`; assert the persisted delivery ownership and existing enqueue/job seam without changing payload design.
    - Test 3: A supported message with exactly one `to` recipient and at least one nonblank body crosses the new gate; the gate runs before tracking, suppression, rate limiting, rendering, routing, persistence, enqueue, or dispatch.
  </behavior>
  <action>Create internal `Mailglass.Outbound.Preflight` with `run/1`, returning a normalized `%Mailglass.Message{}`. Implement the resolved Boundary pattern: keep the module under the existing `Mailglass.Outbound` namespace, do not add `use Boundary`, do not add `Preflight` to Outbound or root exports, and depend only on `Mailglass.Message`, `Mailglass.Tenancy`, `Mailglass.TenancyError`, and `Mailglass.SendError`, which are already legal through Outbound's `deps: [Mailglass]`. Extend `boundary_test.exs` to pin `Mailglass.Outbound` at root-only dependencies and refute `Mailglass.Outbound.Preflight` from its exports. Per D-01, determine the configured resolver and allow the unstamped fallback only when it is `Mailglass.Tenancy.SingleTenant`; write `"default"` into `message.tenant_id` rather than merely checking it. Include the production shape of the pure gate from the outset: it validates the one-recipient/nonblank-body happy path but performs no telemetry, renderer, limiter, Repo, Oban, or adapter calls. In `Mailglass.Outbound`, make `Preflight.run/1` the first `with` stage in `do_send/2`, `do_deliver_later/2`, and `preflight_single/1`; remove the top-level batch-only `Tenancy.assert_stamped!/0` so each batch item uses the same resolver-aware gate. Preserve existing tracking→suppression→rate-limit→stream→Renderer order after the new gate, and do not alter batch persistence, async payload/job arguments, adapter outcome handling, or Task.Supervisor/Oban readiness semantics owned by Phases 150-151. Start with failing public-path tests, then implement and retain them.</action>
  <acceptance_criteria>
    - Source assertion: `Mailglass.Outbound.Preflight.run/1` exists and is the first fallible stage in sync, async, and per-item batch preflight.
    - Behavior assertion: unstamped SingleTenant sync and async paths persist tenant `"default"`; no hidden test-only tenant stamp supplies the value.
    - Boundary assertion: `Preflight.run/1` has no dependency on Renderer, Repo, Oban, RateLimiter, Suppression, Tracking, or an adapter.
    - Compilation assertion: Boundary compilation accepts the internal child module, Outbound still depends only on `Mailglass`, and Preflight is absent from Outbound exports.
    - Test command: `mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/mailglass/boundary_test.exs --warnings-as-errors</automated>
    <automated>mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs --warnings-as-errors</automated>
  </verify>
  <done>One valid unstamped SingleTenant message completes sync send and existing durable-async selection as tenant `"default"`, and all outbound modes enter the same pure gate before effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 149-01-02: Prove custom tenancy remains fail-closed</name>
  <reversibility rating="costly">Fail-closed custom-tenancy behavior is a published cross-tenant safety contract and changing it later would affect every adopter resolver.</reversibility>
  <files>lib/mailglass/outbound/preflight.ex, test/mailglass/outbound/preflight_test.exs, test/mailglass/tenancy_test.exs</files>
  <read_first>Read `149-CONTEXT.md` D-02, the assumption-delta decision above, `lib/mailglass/tenancy.ex` functions `current/0`, `tenant_id!/0`, `assert_stamped!/0`, `resolver/0`, and `default_tenant/0`, `lib/mailglass/errors/tenancy_error.ex`, and the process/env restoration patterns in `test/mailglass/tenancy_test.exs` and `test/mailglass/outbound/deliver_later_test.exs`.</read_first>
  <behavior>
    - Test 1: A configured custom resolver with no process stamp fails through the public outbound path with `%Mailglass.TenancyError{type: :unstamped}` and never normalizes to `"default"`.
    - Test 2: Empty or otherwise invalid stamped tenant context is rejected before Message ownership or any outbound effect.
    - Test 3: A valid custom tenant stamp is copied to the normalized Message and is the tenant identity later consumed by persistence/routing.
    - Test 4: Simulated async worker restoration loss remains fail-closed; SingleTenant fallback does not weaken `tenant_id!/0` or `assert_stamped!/0` semantics.
  </behavior>
  <action>Complete the resolver distinction in `Preflight.run/1` per D-02. For any resolver other than `Mailglass.Tenancy.SingleTenant`, obtain strict process-local context through the existing fail-loud tenancy API, require a non-empty binary tenant identity, and propagate the existing `%Mailglass.TenancyError{type: :unstamped}` contract with bounded non-PII context if useful; do not add a TenancyError type atom. Add config-isolated tests that restore `Application` tenancy and process state on exit. Keep `Tenancy.current/0`, `tenant_id!/0`, and `assert_stamped!/0` public semantics unchanged, and prove accepted custom resolution writes the same `tenant_id` noun used by downstream ownership.</action>
  <acceptance_criteria>
    - Behavior assertion: only SingleTenant can supply implicit `"default"`; every custom missing/invalid/restoration-loss case fails closed.
    - Error assertion: tests match `%Mailglass.TenancyError{type: :unstamped}` by struct/type, not message string.
    - Side-effect assertion: custom tenancy rejection leaves Delivery count and Fake deliveries unchanged.
    - Test command: `mix test test/mailglass/tenancy_test.exs test/mailglass/outbound/preflight_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/tenancy_test.exs test/mailglass/outbound/preflight_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Custom tenancy cannot cross into the default tenant on missing, invalid, or unrestorable context, while a valid custom identity reaches the normalized Message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller/process context → outbound preflight | Resolver configuration and process-local tenant identity become message ownership. |
| preflight → persistence/queue/routing | The normalized tenant identity controls all downstream tenant-scoped work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-149-01 | Elevation of Privilege | `Mailglass.Outbound.Preflight.run/1` | high | mitigate | Permit implicit `"default"` only for configured SingleTenant; strict custom context tests reject missing/invalid/restoration-loss cases before effects. |
| T-149-03 | Denial of Service | outbound stage ordering | medium | mitigate | Make pure preflight the first stage before renderer, limiter, persistence, job, and provider work. |
| T-149-SC | Tampering | package installs | high | mitigate | This plan has no install task; any install deviation must stop for the package-legitimacy audit, slopcheck, and a blocking human decision for `[ASSUMED]` or `[SUS]`. |
</threat_model>

<verification>
After each task run its focused command. At wave completion run `mix test test/mailglass/outbound/preflight_test.exs test/mailglass/outbound/deliver_later_test.exs test/mailglass/renderer_test.exs test/mailglass/tenancy_test.exs --warnings-as-errors` and keep feedback under 60 seconds.
</verification>

<success_criteria>
- FIRST-01 is demonstrated through real public sync and async-selection paths without a process stamp.
- FIRST-02 rejects custom tenant ambiguity before side effects.
- The tracer is retained production code and establishes the architecture consumed by later plans.
</success_criteria>

<output>
Create `.planning/phases/149-first-send-contract-foundation/149-01-SUMMARY.md` when done.
</output>
