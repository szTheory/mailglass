---
phase: 87-logo-tournament
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - .planning/phases/87-logo-tournament/tournament/round-2/
  - .planning/phases/87-logo-tournament/tournament/round-2.html
  - .planning/phases/87-logo-tournament/tournament/round-3.html
  - .planning/phases/87-logo-tournament/87-pre-flight.md
  - .planning/phases/87-logo-tournament/87-decision-record.md
  - brandbook-fable/assets/logo-primary.svg
  - brandbook-fable/assets/logo-typemark.svg
  - brandbook-fable/assets/logo-mark.svg
  - brandbook-fable/assets/logo-monochrome.svg
  - brandbook-fable/assets/logo-with-tagline.svg
  - brandbook-fable/assets/favicon.svg
  - brandbook-fable/assets/social-avatar.svg
  - brandbook-fable/assets/social-avatar-dark.svg
autonomous: false
requirements: [LOGO-07, LOGO-08]

must_haves:
  truths:
    - "At most 2 refinement rounds ran on the maintainer's pick(s); every round-2/3 variant changes exactly ONE named parameter family and is named for it"
    - "The maintainer's winner and rationale are recorded in 87-decision-record.md alongside the full selection history"
    - "8 canonical assets exist in brandbook-fable/assets/ with zero font-family and zero <text> anywhere — all logotype glyphs (including the tagline) are outlined paths"
    - "favicon.svg is a dedicated 16x16-grid redraw with at most 3 shape elements, not a scaled-down lockup"
    - "logo-with-tagline.svg is the ONLY asset carrying the tagline; social avatars are the only square-canvas plate exception"
  artifacts:
    - path: "brandbook-fable/assets/logo-primary.svg"
      provides: "Primary lockup, outlined paths, explicit brand hex fills"
      contains: "<title"
    - path: "brandbook-fable/assets/logo-monochrome.svg"
      provides: "currentColor mark with explicit fallback color on the root element"
      contains: "currentColor"
    - path: "brandbook-fable/assets/favicon.svg"
      provides: "Redrawn 16px-honest mark, <= 3 shapes"
      contains: "viewBox=\"0 0 16 16\""
    - path: ".planning/phases/87-logo-tournament/87-decision-record.md"
      provides: "Selection history, winner rationale, parameter ledger, canonical filename manifest (incl. brief-manifest name mapping)"
  key_links:
    - from: ".planning/phases/87-logo-tournament/87-01-CHECKPOINT.md"
      to: "tournament/round-2/"
      via: "round-2 variants derive ONLY from the option number(s) recorded in the Selection section"
      pattern: "## Selection"
    - from: "tournament round winner"
      to: "brandbook-fable/assets/*.svg"
      via: "promotion: winner outlined/cleaned then expanded to the 8-asset set; tournament files themselves never copied into brandbook-fable/"
      pattern: "assets/logo-"
    - from: "brandbook-fable/assets/*.svg"
      to: "Phase 88 brand book"
      via: "filenames documented in 87-decision-record.md manifest so BOOK-04..07 reference real paths"
      pattern: "logo-(primary|typemark|mark|monochrome|with-tagline)"
---

<objective>
Run the bounded refinement rounds on the maintainer's round-1 pick(s) (at most
2 rounds, single-parameter variants, same evidence-strip discipline), get an
explicit winner, then promote it to the complete canonical asset system in
`brandbook-fable/assets/` and write the decision record.

Purpose: LOGO-07 refinement half (<= 2 parameter-named rounds, selection +
rationale recorded) and LOGO-08 (canonical outlined-path assets, zero
font-family, accessible title/desc, unique IDs).

Output: round-2 variants + gallery (optionally round-3), 8 asset SVGs in
brandbook-fable/assets/, 87-decision-record.md.

BLOCKED-UNTIL: the `## Selection` section of 87-01-CHECKPOINT.md is filled in
with 1-2 picked option numbers. If it is empty, this plan MUST NOT proceed —
stop immediately and report the phase is still paused. This plan is structural
by design: the CONTEXT forbids pre-planning variant details because they depend
on the picks. Tasks below reference "the selected option(s)" abstractly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/87-logo-tournament/87-CONTEXT.md
@.planning/phases/87-logo-tournament/87-01-CHECKPOINT.md
@.planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md
@.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md
@.planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md
@brandbook-fable/tokens.css

<interfaces>
Asset filenames are LOCKED by 87-CONTEXT (these exact 8):
logo-primary.svg, logo-typemark.svg, logo-mark.svg, logo-monochrome.svg
(currentColor), logo-with-tagline.svg, favicon.svg, social-avatar.svg,
social-avatar-dark.svg.

KNOWN DISCREPANCY (resolve by documenting, not renaming): the Phase 85 brief's
File Manifest names two of these differently — `logo-mark-mono.svg` and
`social-avatar-light.svg`. CONTEXT (the later, phase-locked decision) wins.
The decision record MUST carry a filename manifest table mapping CONTEXT names
to the brief-manifest names so Phase 88 (BOOK-07 logo section) and the Phase
90 gate reference the real files. Per-file size budgets from the brief still
apply: primary 16 KB, typemark 12 KB, mark 8 KB, monochrome 8 KB, with-tagline
16 KB, favicon 3 KB, avatars 6 KB each.

Brand hexes for explicit fills (from tokens.css): Ink #0D1B2A, Glass #277B96,
glass-deep #1D637A, Ice #A6EAF2, Paper #F8FBFD. GitHub-rendering rule from
research: file assets use explicit hex fills; the monochrome asset is the one
currentColor variant and must set an explicit fallback (root element
style="color:#0D1B2A") so it renders Ink when opened standalone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify selection, build round-2 single-parameter variants + evidence gallery, browser-audit</name>
  <files>.planning/phases/87-logo-tournament/tournament/round-2/, .planning/phases/87-logo-tournament/tournament/round-2.html, .planning/phases/87-logo-tournament/87-pre-flight.md</files>
  <read_first>.planning/phases/87-logo-tournament/87-01-CHECKPOINT.md (the Selection section and any directional notes — this is the entire brief for this task), .planning/phases/87-logo-tournament/87-CONTEXT.md (Refinement + promotion section), .planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md (grid, optical corrections, self-audit), brandbook-fable/tokens.css</read_first>
  <action>
    GATE FIRST: read 87-01-CHECKPOINT.md. If the `## Selection` section is
    empty or absent, STOP this plan immediately with the message that the
    phase is paused awaiting maintainer selection — generate nothing. If the
    checkpoint records a full-set rejection instead of picks, this plan does
    not handle it (the replacement-round protocol re-runs plan 87-01's flow);
    stop and report.

    With 1-2 picks recorded: generate 4-6 round-2 variants TOTAL across the
    pick(s), in `.planning/phases/87-logo-tournament/tournament/round-2/`.
    Each variant changes exactly ONE named parameter family relative to its
    parent option and is filenamed for it:
    `variant-{parentN}-{family}-{value}.svg`, where family is one of: weight
    (stem S up/down), motif-intensity (the earned modification dialed
    subtler/stronger), gap (lockup/letter-space tightness), proportion
    (mark-to-wordmark scale or glyph width ratios), baseline (boundary-break
    depth / descender relationship). Apply the maintainer's directional notes
    from the checkpoint. All craft discipline from plan 87-01 Task 1 carries
    over verbatim: integer grid (x-height 100, S=22 reference, 0.90-0.94xS
    horizontals, 3-unit overshoot, 12-18% joint thinning), filled outline
    paths only, no <text>/font-family, per-file unique id prefixes
    (v{parentN}{letter}-), title/desc/role="img", <= 2 decimal coordinates,
    no plates, i dot untouched.

    Build `tournament/round-2.html` with the SAME six-cell evidence-strip
    format as round-1.html (large light, large dark, 32px+16px favicon row,
    mono/currentColor, fake README context, <= 1-line rationale naming the
    changed parameter), tokens inlined, SVGs inlined, light + dark sections,
    zero external refs. Append a `## Round 2` section to 87-pre-flight.md
    screening every variant against the same C-01..C-14 constraints (one row
    per variant x constraint, all PASS — fix or replace failures before
    showing). Playwright-render to gitignored `tmp/87-logo-tournament/
    round-2/` (full-page light + dark + per-variant close-ups), READ the
    screenshots, visually audit at full/32/16/mono/dark, fix and re-render
    until clean. Install no new packages.

    Commit SVGs + gallery as feat(87-02), pre-flight append as docs(87-02).
  </action>
  <verify>
    <automated>test -s .planning/phases/87-logo-tournament/tournament/round-2.html && xmllint --noout .planning/phases/87-logo-tournament/tournament/round-2/variant-*.svg && ls .planning/phases/87-logo-tournament/tournament/round-2/variant-*.svg | wc -l | awk '{exit ($1>=4 && $1<=6)?0:1}' && grep -l -e '<text' -e 'font-family' .planning/phases/87-logo-tournament/tournament/round-2/variant-*.svg | wc -l | grep -qx '0' && grep -Eo 'https?://[^"< ]+' .planning/phases/87-logo-tournament/tournament/round-2.html | grep -v 'www\.w3\.org' | wc -l | grep -qx '0' && ls tmp/87-logo-tournament/round-2/*.png | wc -l | awk '{exit ($1>=4)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - 87-01-CHECKPOINT.md Selection section contained explicit pick(s) before any generation happened
    - 4-6 variant SVGs exist, each named variant-{parentN}-{family}-{value}.svg with family in {weight, motif-intensity, gap, proportion, baseline}; each derives only from a picked option
    - round-2.html mirrors the round-1 evidence-strip format, self-contained, zero non-xmlns http(s) refs
    - 87-pre-flight.md has a Round 2 section with (variant-count x 14) rows, all PASS
    - Screenshots exist in tmp/87-logo-tournament/round-2/ and were visually audited before presentation
  </acceptance_criteria>
  <done>Round-2 field built strictly from the recorded picks, pre-screened, browser-audited, ready to present.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Maintainer picks the winner (round-2, optional round-3 hard cap)</name>
  <files>.planning/phases/87-logo-tournament/87-decision-record.md, .planning/phases/87-logo-tournament/tournament/round-3.html</files>
  <decision>Which round-2 variant is the fable logo — or which <= 4 candidates advance to a final round 3?</decision>
  <context>
    CONTEXT hard caps refinement at 2 rounds: round 3, if requested, presents
    AT MOST 4 candidates in the same evidence-strip format. No round 4 exists
    under any circumstances — after round 3 the maintainer must pick from
    what is on the table or stop the tournament. This checkpoint is never
    auto-approvable.
  </context>
  <action>
    Present `.planning/phases/87-logo-tournament/tournament/round-2.html` to
    the maintainer: the gallery path plus a one-paragraph summary naming each
    variant's changed parameter family (renders speak, not essays). Wait for
    the response — no default may be inferred. When it arrives, start
    87-decision-record.md and record the response VERBATIM (picks/winner,
    rationale, date) under a `## Round 2 Decision` heading. If the response
    requests round 3: build at most 4 round-3 candidates the same way as
    Task 1 (single-parameter naming, craft discipline, pre-flight Round 3
    section, round-3.html in the same evidence-strip format, Playwright
    screenshot audit to tmp/87-logo-tournament/round-3/), present it, pause
    again, and record the final pick verbatim under `## Round 3 Decision`.
    Round 3 is the hard cap: the next response must be a winner or a stop.
  </action>
  <options>
    <option id="pick-winner">
      <name>Pick the winner now</name>
      <pros>Tournament ends; promotion proceeds immediately</pros>
      <cons>None — variants were pre-screened</cons>
    </option>
    <option id="round-3">
      <name>Request round 3 (name <= 4 candidates + what to adjust)</name>
      <pros>One more single-parameter pass on the strongest candidates</pros>
      <cons>This is the final round — the next pause is pick-or-stop</cons>
    </option>
  </options>
  <verify>
    <automated>grep -qi -e 'winner' .planning/phases/87-logo-tournament/87-decision-record.md && grep -q 'variant-' .planning/phases/87-logo-tournament/87-decision-record.md</automated>
  </verify>
  <done>The maintainer's winner (after round 2, or round 3 at the hard cap) is recorded verbatim with rationale and date in 87-decision-record.md; no generation happened beyond the round-3 cap.</done>
  <resume-signal>Reply "winner: variant-{name}" with optional rationale, or "round 3: {<= 4 variant names} + notes". Record the response verbatim before proceeding.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Promote the winner to the canonical asset system + decision record</name>
  <files>brandbook-fable/assets/logo-primary.svg, brandbook-fable/assets/logo-typemark.svg, brandbook-fable/assets/logo-mark.svg, brandbook-fable/assets/logo-monochrome.svg, brandbook-fable/assets/logo-with-tagline.svg, brandbook-fable/assets/favicon.svg, brandbook-fable/assets/social-avatar.svg, brandbook-fable/assets/social-avatar-dark.svg, .planning/phases/87-logo-tournament/87-decision-record.md</files>
  <read_first>.planning/research/v1.9-brandbook-fable/LOGO-CRAFT.md (Small-Size Survival — favicon redraw rules), .planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md (SVG portability + GitHub rendering rules), .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md (Non-Negotiables + File Manifest budgets), brandbook-fable/tokens.css</read_first>
  <action>
    Outline/clean the winning variant (final pass: extrema-only on-curve
    points, <= 2 decimals, stems re-measured numerically, guide elements
    deleted, winding verified by render) and expand it to the full 8-asset
    set in `brandbook-fable/assets/` — author each asset fresh from the
    winner's geometry; NEVER copy a tournament file into brandbook-fable/.

    The 8 assets (CONTEXT-locked names): (1) logo-primary.svg — the primary
    lockup (mark + wordmark, or the typemark alone if the winner is an axis-A
    typemark), explicit brand hex fills. (2) logo-typemark.svg — standalone
    outlined wordmark. (3) logo-mark.svg — standalone mark, explicit hex
    fills. (4) logo-monochrome.svg — the mark with fill="currentColor"
    throughout and an explicit fallback on the root element
    (style="color:#0D1B2A") so it renders Ink standalone. (5)
    logo-with-tagline.svg — the ONLY asset carrying a subtitle: the locked
    essence line "Mail you can see through" set as OUTLINED PATHS (draw it on
    the same grid at reduced scale; zero font-family applies to the tagline
    too); mark it with a data-tagline attribute on the tagline group. (6)
    favicon.svg — a DEDICATED REDRAW on viewBox="0 0 16 16": integer
    coordinates, <= 3 shape elements total, stems >= 2 units, 1-2 colors,
    counters >= 2 units, >= 1 unit internal margin; if the winner cannot be
    redrawn under those constraints the mark is wrong — escalate, do not
    ship soup. (7) social-avatar.svg — square canvas, light surface (Paper
    #F8FBFD), mark centered; the square canvas is the single documented
    plate exception. (8) social-avatar-dark.svg — same on Ink #0D1B2A.

    Every asset: role="img", <title> + <desc> in reader-facing brand
    language, per-file unique id prefix (e.g. mglogo-, mgmark-, mgfav-), no
    gradients/filters/masks/<script>, no <text>, no font-family, zero
    planning-process vocabulary (no "phase", "tournament", "option",
    "variant", "milestone", or REQ-IDs anywhere in the files), per-file size
    within the brief budgets. No plate behind any mark except the two square
    avatars.

    Complete `.planning/phases/87-logo-tournament/87-decision-record.md`: the
    round-1 picks + maintainer rationale verbatim, the round-2 (and round-3
    if run) variant ledger with each variant's changed parameter family, the
    winner + final rationale, dates, rejection_count, and the canonical
    filename manifest table — including the mapping note that CONTEXT names
    logo-monochrome.svg / social-avatar.svg supersede the Phase 85 brief
    manifest's logo-mark-mono.svg / social-avatar-light.svg, so Phase 88 and
    the Phase 90 gate reference the shipped names.

    Run all gates (verify command), render the assets once via the browser
    tooling to tmp/87-logo-tournament/assets/ and READ the renders to confirm
    counters/fills/favicon legibility at 16px. Commit assets as feat(87-02),
    decision record as docs(87-02). Update the LOGO-05..08 checkboxes in
    .planning/REQUIREMENTS.md only if the file is not carrying unrelated
    dirty edits (per the known dirty-planning-tree gotcha, leave it to the
    orchestrator if in doubt).
  </action>
  <verify>
    <automated>xmllint --noout brandbook-fable/assets/logo-primary.svg brandbook-fable/assets/logo-typemark.svg brandbook-fable/assets/logo-mark.svg brandbook-fable/assets/logo-monochrome.svg brandbook-fable/assets/logo-with-tagline.svg brandbook-fable/assets/favicon.svg brandbook-fable/assets/social-avatar.svg brandbook-fable/assets/social-avatar-dark.svg && grep -l -e 'font-family' -e '<text' brandbook-fable/assets/*.svg | wc -l | grep -qx '0' && grep -Eo '<(path|circle|rect|ellipse|polygon|line)[ />]' brandbook-fable/assets/favicon.svg | wc -l | awk '{exit ($1<=3)?0:1}' && grep -l 'data-tagline' brandbook-fable/assets/*.svg | grep -qx 'brandbook-fable/assets/logo-with-tagline.svg' && grep -q 'currentColor' brandbook-fable/assets/logo-monochrome.svg && grep -L '<title' brandbook-fable/assets/*.svg | wc -l | grep -qx '0' && grep -ril -e 'phase' -e 'tournament' -e 'milestone' brandbook-fable/assets/ | wc -l | grep -qx '0' && test -s .planning/phases/87-logo-tournament/87-decision-record.md</automated>
  </verify>
  <acceptance_criteria>
    - All 8 CONTEXT-named assets exist in brandbook-fable/assets/, xmllint exits 0 on each
    - Zero font-family and zero <text> across assets/ (the with-tagline subtitle is outlined paths)
    - favicon.svg viewBox is 0 0 16 16 with <= 3 shape elements, integer coordinates
    - data-tagline appears in logo-with-tagline.svg and in no other asset
    - logo-monochrome.svg uses currentColor with an explicit root fallback color; other assets use explicit brand hexes
    - Every asset has <title> + <desc> and per-file unique IDs (`for f in brandbook-fable/assets/*.svg; do grep -o 'id="[^"]*"' $f | sort | uniq -d; done` outputs nothing)
    - Zero process vocabulary in any asset file; per-file sizes within brief budgets; nothing outside brandbook-fable/assets/ and .planning/ changed
    - 87-decision-record.md carries the full selection history, parameter ledger, winner rationale, and the filename-mapping manifest
  </acceptance_criteria>
  <done>The complete canonical outlined-path asset system ships in brandbook-fable/assets/ with all gates green, and the decision record documents how it was chosen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brandbook-fable/assets/*.svg -> GitHub/browsers | shipped assets render in third-party sandboxes (GitHub README <img>, browser tabs) |
| round-2/3 galleries -> maintainer's browser | locally-authored HTML opened from file:// |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03 | Information disclosure | gallery + asset external refs | mitigate | zero non-xmlns https?:// gate on round-2/3.html; assets contain no href/url() to anything remote |
| T-87-04 | Tampering | shipped SVGs (script/event-handler injection) | mitigate | no <script>, no on* attributes; xmllint + grep gates; hand-authored only |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; rendering uses already-installed tooling only |
</threat_model>

<verification>
- Plan refused to run while 87-01-CHECKPOINT.md Selection was empty (hard pause honored; no inferred default)
- <= 2 refinement rounds ran; every variant filename names its single changed parameter family; round 3 (if any) had <= 4 candidates and was the last round
- Round-2/3 galleries match the round-1 evidence-strip format and passed the same pre-flight + browser visual audit before presentation
- All Task 3 asset gates green; tournament artifacts remain entirely under .planning/ (git status shows no brandbook-fable/ changes outside assets/)
- 87-decision-record.md exists with selection history + filename manifest
</verification>

<success_criteria>
- Roadmap SC3 completed: maintainer's explicit selection + rationale recorded (LOGO-07)
- Roadmap SC4 satisfied: at most 2 parameter-named refinement rounds before promotion (LOGO-07)
- Roadmap SC5 satisfied: the 8 canonical assets ship in brandbook-fable/assets/ — outlined paths, zero font-family, accessible title/desc, unique IDs (LOGO-08)
- Phase 88 can consume the assets by filename from the decision-record manifest
</success_criteria>

<output>
Create `.planning/phases/87-logo-tournament/87-02-SUMMARY.md` when done,
naming the winner, the parameter trail that produced it, and the 8 shipped
asset paths.
</output>
