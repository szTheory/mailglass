---
phase: "105"
plan: "02"
type: execute
wave: 2
depends_on:
  - "105-01"
files_modified:
  - README.md
  - guides/migration-from-swoosh.md
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements:
  - DOCS-01
  - DOCS-04

must_haves:
  truths:
    - "README.md Quickstart section has a config :mailglass block with repo: and adapter: before the Mailglass.deliver() example"
    - "README.md Documentation index links to guides/learning-path.md"
    - "guides/migration-from-swoosh.md opens with the value-prop pitch before the subordinate-reference framing"
    - "guides/migration-from-swoosh.md dep pins read ~> 1.6 (not ~> 0.3)"
    - "docs_contract_test.exs asserts README config presence and migration opener keywords"
    - "mix test test/mailglass/docs_contract_test.exs exits 0"
  artifacts:
    - path: "README.md"
      provides: "Config-first Quickstart block; learning-path link in Documentation index"
    - path: "guides/migration-from-swoosh.md"
      provides: "Value-prop opener before subordinate-reference framing; bumped dep pins"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "Two new contract assertions: README config presence, migration opener keywords"
  key_links:
    - from: "README.md Quickstart"
      to: "config :mailglass block"
      via: "inserted verbatim from guides/getting-started.md:24-32"
      pattern: "config :mailglass"
    - from: "README.md Documentation index"
      to: "guides/learning-path.md"
      via: "new bullet link"
      pattern: "guides/learning-path.md"
    - from: "guides/migration-from-swoosh.md opener"
      to: "value-prop keywords (transport, framework layer, preview, webhooks, audit)"
      via: "new section before subordinate-reference paragraph"
      pattern: "framework layer"
---

<objective>
Fix the README Quickstart to be config-first (DOCS-01) and update the migration-from-swoosh guide
with the value-prop opener and bumped dep pins (DOCS-04). Both changes land new contract
assertions in docs_contract_test.exs in this single plan to avoid file-collision with Plan 03.

Purpose: A copy-paste of the README Quickstart currently omits the config :mailglass block, so
Mailglass.deliver() raises ConfigError. Fixing this (D-01/D-02/D-03) removes the most common
first-hour failure mode. The migration guide currently opens with a deferral instead of explaining
why to adopt mailglass — the value-prop opener (D-11) is what convinces a Swoosh user. The stale
~> 0.3 pins (D-13) would fail mix deps.get on a 1.x installation.

Output: README.md (config block added to Quickstart; learning-path link added to Documentation
index), guides/migration-from-swoosh.md (value-prop opener added; pins bumped to ~> 1.6),
test/mailglass/docs_contract_test.exs (two new test functions added to "README.md contract" and
"Guide contracts" describe blocks).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md
@.planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-01-SUMMARY.md
</context>

<interfaces>
From guides/getting-started.md lines 22-32 (the verbatim config block to reuse per D-02):

```
## 2) Configure mailglass

config :mailglass,
  repo: MyApp.Repo,
  adapter:
    {Mailglass.Adapters.Swoosh,
     swoosh_adapter: {Swoosh.Adapters.Postmark, api_key: System.fetch_env!("POSTMARK_API_KEY")}},
  telemetry: [default_logger: true]
```

From test/support/docs_helpers.ex extract_block_after_heading/2:
Matches pattern: ~r/##\s+#{heading}\n.*?```(?:elixir|bash)\n(.*?)\n```/s
New headings that anchor code blocks MUST use exactly ## (two-hash prefix).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix README Quickstart — config-first insertion + Documentation index learning-path link</name>

  <read_first>
    - README.md (lines 55-175 — Installation section (57-84), Quickstart section (86-122), and lines 245-275 for the Documentation index)
    - guides/getting-started.md (lines 22-32 — the verbatim config block source per D-02)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (D-01, D-02, D-03, D-04, D-10)
    - test/mailglass/docs_contract_test.exs (lines 1-88 — existing README contract assertions, to understand what must remain true and where to add the new assertion)
  </read_first>

  <action>
    PART A — README Quickstart config-first fix (D-01/D-02/D-03):

    Find the "## Quickstart" section in README.md (currently around lines 86-122). Currently it
    goes: bash block (deps.get / install / migrate / compile) → mailable defmodule block →
    deliver() example. The gap is: no config :mailglass block before deliver().

    Insert a new fenced elixir code block labeled with "# config/runtime.exs" immediately BETWEEN
    the bash installation block and the "Define a mailable:" paragraph. The block must be
    VERBATIM from guides/getting-started.md:24-32:
      config :mailglass,
        repo: MyApp.Repo,
        adapter: {Mailglass.Adapters.Swoosh, swoosh_adapter: {Swoosh.Adapters.Postmark,
                  api_key: System.fetch_env!("POSTMARK_API_KEY")}},
        telemetry: [default_logger: true]

    Frame it with honest prose per D-03: the installer generates the config/runtime.exs block;
    this shows the shape so adopters can confirm their repo: and adapter: are wired. Use language
    like "The installer generates a config block in config/runtime.exs. Confirm your repo and
    adapter are wired:" — do not say "the installer didn't do this" or imply it's a separate
    manual step.

    The heading "## Quickstart" currently exists and must remain. The new elixir code block must
    sit under it but does NOT need its own ## sub-heading (since the new contract assertion for
    DOCS-01 uses a simple File.read! =~ "config :mailglass" check, not extract_block_after_heading
    — there is no heading-extraction constraint here). Do not add a ## sub-heading unless it
    improves readability naturally.

    PART B — README Documentation index learning-path link (D-10):

    In the "## Documentation" section (around lines 249-271), add a new bullet for
    guides/learning-path.md. Place it as the FIRST bullet in the list (before getting-started.md),
    since learning-path is the top-level arc index:
      `[`guides/learning-path.md`](guides/learning-path.md) — first-week learning arc: ordered
       sequence from install to production`

    Or place it second after getting-started if first feels wrong — the critical requirement is
    that it appears in the Documentation index (D-10). Keep getting-started as the primary entry
    point since mix.exs main: "getting-started" is unchanged.

    Do NOT duplicate the full ordered sequence in the README — just the link (D-10, Honest-Surface:
    avoid two copies that can drift).
  </action>

  <verify>
    <automated>grep -c "config :mailglass" README.md</automated>
  </verify>

  <done>
    README.md Quickstart contains an elixir code block with "config :mailglass", "repo:", and
    "adapter:" placed before the Mailglass.deliver() example. README.md Documentation index
    contains a link to guides/learning-path.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix migration-from-swoosh.md — value-prop opener + bump stale ~> 0.3 pins</name>

  <read_first>
    - guides/migration-from-swoosh.md (full — opener (1-11), stale pins (26-27), canonical-lane pointers (84-87))
    - guides/upgrading-to-v1_0.md (first 5 lines — confirm file exists; opener must not contradict it)
    - guides/compatibility-and-deprecations.md (first 5 lines — confirm file exists; opener must not contradict it)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (D-11, D-12, D-13)
    - test/mailglass/docs_contract_test.exs (lines 227-248 — compatibility/upgrade assertions that must remain true)
  </read_first>

  <action>
    PART A — Value-prop opener (D-11):

    The current migration-from-swoosh.md opens with (line 1-11):
      # Migration from raw Swoosh
      This guide is now a subordinate raw-Swoosh migration reference.
      The canonical latest-0.x to 1.0 path lives in [upgrading-to-v1_0.md]...

    Insert a new opening section ABOVE the current line 3 paragraph. Add it immediately after the
    # heading, before "This guide is now a subordinate..." sentence.

    The new opener must include these value-prop keywords (required by the DOCS-04 contract
    assertion in D-12): "transport", "framework layer", "preview", "webhooks", "audit" (or
    "audit ledger"), "suppressions", "multi-tenancy". Shape the prose around:
    "Swoosh handles transport; mailglass adds the framework layer you'd otherwise rebuild —
    preview, webhooks, audit ledger, suppressions, and multi-tenancy."

    Optionally add 2-3 sentences expanding on how mailglass composes on top of Swoosh rather than
    replacing it, consistent with the project description in CLAUDE.md. Keep it tight: this is a
    pitch opener, not a full explanation.

    CRITICALLY: Do NOT delete or weaken the existing "This guide is now a subordinate raw-Swoosh
    migration reference" paragraph or the canonical-lane pointers to upgrading-to-v1_0.md and
    compatibility-and-deprecations.md (lines 84-87). These must remain intact after the opener.
    The test at docs_contract_test.exs:227-248 asserts those guides are "wired into Tier 1 docs" —
    do not break those assertions.

    Do NOT add a new ## sub-heading for the opener paragraph — place it as body text under the #
    heading. The DOCS-04 contract assertion (D-12) will locate the opener prose via byte/string
    position relative to the "subordinate" keyword and the first "## 1)" heading — so the opener
    must appear in the file BEFORE the text "subordinate" (line 3 currently) is no longer the
    very first prose line.

    Wait — re-read D-11: "Keep the subordinate-reference framing ... AFTER the pitch — do not
    delete them." So the structure becomes:
      # Migration from raw Swoosh
      [NEW VALUE-PROP OPENER PARAGRAPH]
      This guide is now a subordinate raw-Swoosh migration reference. ...
      [rest of file unchanged]

    This means the opener appears BETWEEN the # heading and the existing line-3 "subordinate"
    sentence, so the opener text is present BEFORE the word "subordinate" in the file.

    PART B — Stale dep pin fix (D-13):

    On lines 26-27 of migration-from-swoosh.md, change:
      {:mailglass, "~> 0.3"}
      {:mailglass_admin, "~> 0.3", only: [:dev]}
    to:
      {:mailglass, "~> 1.6"}
      {:mailglass_admin, "~> 1.6", only: [:dev]}

    The current published version is 1.6.2 (confirmed in STATE.md / project memory). The ~> 1.6
    pin is correct for the current major.minor. Do not use ~> 1.x (that is not valid Hex semver
    syntax); use the concrete major.minor ~> 1.6.
  </action>

  <verify>
    <automated>grep -q "framework layer" guides/migration-from-swoosh.md && ! grep -q "~> 0.3" guides/migration-from-swoosh.md</automated>
  </verify>

  <done>
    guides/migration-from-swoosh.md contains "framework layer" text before the "subordinate"
    paragraph. The file contains zero occurrences of "~> 0.3". The canonical-lane pointers to
    upgrading-to-v1_0.md and compatibility-and-deprecations.md are still present at the end.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add DOCS-01 and DOCS-04 contract assertions to docs_contract_test.exs</name>

  <read_first>
    - test/mailglass/docs_contract_test.exs (full — to find correct insertion points and mirror existing assertion style)
    - test/support/docs_helpers.ex (full — extraction contract for any new heading-scoped assertions)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (D-04 for DOCS-01 assertion, D-12 for DOCS-04 assertion)
    - README.md (Quickstart section — confirm the config block heading/location for the assertion to target)
    - guides/migration-from-swoosh.md (as edited in Task 2 — confirm the opener text for the string assertions)
  </read_first>

  <action>
    Add two new test functions.

    NEW TEST 1 — in the "README.md contract" describe block, add a test named
    "Quickstart contains a config-first block before the deliver() example" (D-04):

      readme = File.read!("README.md")
      config_pos = :binary.match(readme, "config :mailglass")
      deliver_pos = :binary.match(readme, "Mailglass.deliver()")
      assert config_pos != :nomatch, "README Quickstart is missing a config :mailglass block"
      assert deliver_pos != :nomatch, "README Quickstart is missing a Mailglass.deliver() call"
      {config_offset, _} = config_pos
      {deliver_offset, _} = deliver_pos
      assert config_offset < deliver_offset,
             "config :mailglass block must appear before Mailglass.deliver() in the README"
      assert readme =~ "repo:"
      assert readme =~ "adapter:"

    This mirrors the style of the existing "installation snippet targets the current stable
    surface" test (lines 6-52) and the "Quickstart snippet compiles" test (54-61). Insert it
    AFTER the "Quickstart snippet compiles" test and BEFORE the "README mentions the shipped
    runtime routing terms" test.

    NEW TEST 2 — in the "Guide contracts" describe block, add a test named
    "migration-from-swoosh opens with the value-prop pitch before subordinate framing" (D-12):

      migration = File.read!("guides/migration-from-swoosh.md")
      value_prop_keywords = ["transport", "framework layer", "preview", "webhooks", "audit",
                             "suppressions", "multi-tenancy"]
      for kw <- value_prop_keywords do
        assert migration =~ kw,
               "migration-from-swoosh.md is missing value-prop keyword: #{inspect(kw)}"
      end
      # Opener must appear before the subordinate-reference framing
      opener_pos = :binary.match(migration, "framework layer")
      subordinate_pos = :binary.match(migration, "subordinate")
      assert opener_pos != :nomatch, "migration-from-swoosh.md is missing 'framework layer' opener"
      assert subordinate_pos != :nomatch, "migration-from-swoosh.md is missing 'subordinate' reference"
      {opener_offset, _} = opener_pos
      {subordinate_offset, _} = subordinate_pos
      assert opener_offset < subordinate_offset,
             "value-prop opener must appear before 'subordinate' framing"
      # Stale pins are fixed
      refute migration =~ "~> 0.3", "migration-from-swoosh.md still contains stale ~> 0.3 pin"

    Insert this test AFTER the new "learning-path is registered in both mix.exs docs lists" test
    (added in Plan 01) and BEFORE the "Multi-tenancy routing example parses" test.

    Style notes:
    - Use assert/refute with descriptive failure messages (no "Oops" per brand voice).
    - Match strings directly (no extract_code_blocks/extract_block_after_heading needed here —
      the opener prose is not a fenced code block).
    - Each assertion is its own line (no multi-clause AND compression).
  </action>

  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs</automated>
  </verify>

  <done>
    docs_contract_test.exs contains:
      - "Quickstart contains a config-first block before the deliver() example" test in "README.md
        contract" describe, asserting config_offset < deliver_offset and repo:/adapter: presence.
      - "migration-from-swoosh opens with the value-prop pitch before subordinate framing" test in
        "Guide contracts" describe, asserting all 7 value-prop keywords and opener < subordinate
        byte ordering, and refuting ~> 0.3.
    mix test test/mailglass/docs_contract_test.exs exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Docs-only change — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-02-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package installs; slopcheck not applicable |

Docs-only change — no new attack surface. Example config snippets contain only placeholder
values (System.fetch_env!("POSTMARK_API_KEY"), MyApp.Repo) — no real credentials.
</threat_model>

<verification>
Run the full docs-contract suite:

```bash
mix test test/mailglass/docs_contract_test.exs
```

All tests must pass (exit 0).

Spot-checks:

```bash
grep -n "config :mailglass" README.md
grep -n "framework layer" guides/migration-from-swoosh.md
grep -n "~> 0.3" guides/migration-from-swoosh.md   # should return nothing
grep -n "learning-path" README.md
```
</verification>

<success_criteria>
- README.md Quickstart contains "config :mailglass" with repo: and adapter: before "Mailglass.deliver()"
- README.md Documentation index contains a link to guides/learning-path.md
- guides/migration-from-swoosh.md contains "framework layer" opener paragraph before the "subordinate" line
- guides/migration-from-swoosh.md dep pins are ~> 1.6 (zero occurrences of "~> 0.3")
- docs_contract_test.exs "Quickstart contains a config-first block" test passes
- docs_contract_test.exs "migration-from-swoosh opens with value-prop pitch" test passes
- mix test test/mailglass/docs_contract_test.exs exits 0
</success_criteria>

<output>
Create .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-02-SUMMARY.md when done.
</output>
