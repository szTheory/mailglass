---
phase: "105"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/learning-path.md
  - mix.exs
  - test/mailglass/docs_contract_test.exs
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "guides/learning-path.md exists and presents an ordered first-week arc linking existing guides"
    - "mix.exs lists guides/learning-path.md in both extras: and groups_for_extras: [Guides: ...]"
    - "docs_contract_test.exs asserts file presence and both registration points"
    - "mix test test/mailglass/docs_contract_test.exs exits 0"
  artifacts:
    - path: "guides/learning-path.md"
      provides: "Ordered first-week learning index over existing guides"
    - path: "mix.exs"
      provides: "Registration in extras: and groups_for_extras: [Guides: ...]"
    - path: "test/mailglass/docs_contract_test.exs"
      provides: "Contract assertion for learning-path registration"
  key_links:
    - from: "mix.exs extras:"
      to: "guides/learning-path.md"
      via: "string entry in the extras list"
      pattern: '"guides/learning-path.md"'
    - from: "mix.exs groups_for_extras: [Guides: ...]"
      to: "guides/learning-path.md"
      via: "string entry in the Guides group list"
      pattern: '"guides/learning-path.md"'
---

<objective>
Create the canonical first-week learning-path index guide, register it in both mix.exs docs
lists, and add a docs-contract assertion that proves both registration points are present.

Purpose: DOCS-03 requires a discoverable learning-path/index (guides/learning-path.md) as the
single source of truth for the first-week arc. Subsequent plans (105-02 and 105-03) will link
to this guide from the README Documentation index and from getting-started's Next steps section;
those links must point to an existing file.

Output: guides/learning-path.md (new), mix.exs (two list entries added), docs_contract_test.exs
(new test function in "Guide contracts" describe).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guides/learning-path.md — the ordered first-week arc</name>

  <read_first>
    - guides/getting-started.md (full — establishes the install/configure/send steps that precede the arc)
    - guides/jobs.md (lines 1-30 — the JTBD ramp-up this guide feeds into; preserve the "Current as of 2026-06-02" marker text)
    - guides/authoring-mailables.md (first 10 lines — confirm file exists and note its heading for the link label)
    - guides/preview.md (first 5 lines — confirm file exists and note its heading)
    - guides/webhooks.md (first 5 lines — confirm file exists and note its heading)
    - guides/testing.md (first 5 lines — confirm file exists and note its heading)
    - guides/telemetry.md (first 5 lines — confirm file exists and note its heading)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (D-06, D-08, D-09, D-10 for the exact arc sequence and dual-registration requirement)
    - test/support/docs_helpers.ex (full — confirms extract_block_after_heading matches ## only)
  </read_first>

  <action>
    Create guides/learning-path.md as a new file. It is the single source of truth for the
    first-week ordering. The README Documentation index and getting-started's Next steps will LINK
    to it — do not duplicate the sequence in those surfaces (D-10).

    Required structure:
    - A `# Learning Path` top-level heading followed by a brief description of the guide's purpose.
    - A `## Week 1 arc` section (or similar ## heading) that sequences the first-week path in
      this order per DOCS-02/D-06: jobs → authoring-mailables → preview → webhooks → testing →
      operate. Each step is a bullet with a link to the corresponding guide and a one-sentence
      description of what the step covers. Use relative links: `[Getting started](getting-started.md)`,
      `[What you can do with mailglass](jobs.md)`, `[Authoring mailables](authoring-mailables.md)`,
      `[Preview](preview.md)`, `[Webhooks](webhooks.md)`, `[Testing](testing.md)`,
      `[Telemetry and operating](telemetry.md)`.
    - A brief "## Going deeper" section pointing to multi-tenancy.md, dkim-setup.md,
      unsubscribe.md, and migration-from-swoosh.md as optional follow-ons.

    The guide must NOT contain any fenced code blocks (no Elixir/bash/SQL). If it had code
    blocks, they would need to sit under ## headings for extract_block_after_heading — avoid the
    issue entirely by keeping the guide prose + links only. No over-claims per the Honest-Surface
    lens; no PII-leaking examples.

    Brand voice per brandbook/brand-book.md: clear, exact, confident (not cocky), warm (not cute),
    technical (not intimidating). "A thoughtful maintainer." Direct word choices: "preview" not
    "experience the full rendering lifecycle".
  </action>

  <verify>
    <automated>test -f guides/learning-path.md && grep -c "## " guides/learning-path.md | grep -v "^0$"</automated>
  </verify>

  <done>
    guides/learning-path.md exists, contains at least one ## heading, and lists links to
    getting-started.md, jobs.md, authoring-mailables.md, preview.md, webhooks.md, testing.md,
    and telemetry.md in the documented first-week sequence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register guides/learning-path.md in both mix.exs docs lists + add contract assertion</name>

  <read_first>
    - mix.exs (lines 350-436 — the full docs/0 function: extras: list at ~383-405, groups_for_extras: at ~407-436)
    - test/mailglass/docs_contract_test.exs (full — to understand existing assertion patterns and find the correct insertion point for the new test)
    - .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-CONTEXT.md (D-09 for placement rule: early in Guides group; D-12 for contract-assertion strategy)
  </read_first>

  <action>
    PART A — mix.exs registration (D-09):

    In the extras: list (around line 388), add "guides/learning-path.md" immediately after
    "guides/getting-started.md" so the HexDocs sidebar ordering reflects the first-week arc
    sequence (getting-started leads, learning-path follows as the arc index).

    In the groups_for_extras: [Guides: ...] list (around line 413), add "guides/learning-path.md"
    in the same position — immediately after "guides/getting-started.md".

    Keep main: "getting-started" unchanged (D-09 explicitly requires this).
    Do not add to groups_for_extras: [Overview:] or [Contract:] — Guides group only.

    PART B — docs_contract_test.exs assertion (per D-09/D-12 Claude's Discretion):

    In the existing "Guide contracts" describe block, add a new test named
    "learning-path is registered in both mix.exs docs lists" that asserts:
      1. File.read!("mix.exs") contains the string "guides/learning-path.md" (will pass for both
         the extras: and groups_for_extras: entries, since grep finds the first occurrence — the
         extras: list entry). Add TWO separate assertions so both lists are verified individually:
         assert mix_exs =~ ~s("guides/learning-path.md") once for the presence check — but because
         one string grep would pass with just one entry, use a count-based approach:
         use Regex.scan(~r/"guides\/learning-path\.md"/, mix_exs) and assert length(matches) >= 2.
      2. File.exists?("guides/learning-path.md") is truthy.

    The count-based assertion (>= 2 occurrences of the quoted path string) is the most precise
    proof that the guide appears in BOTH lists, without hardcoding line positions.

    Insert the new test BEFORE the existing "Multi-tenancy routing example parses..." test so
    onboarding-related assertions are grouped near the top of "Guide contracts".
  </action>

  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs 2>&1 | tail -5</automated>
  </verify>

  <done>
    mix.exs extras: and groups_for_extras: [Guides: ...] both contain "guides/learning-path.md"
    immediately after "guides/getting-started.md". docs_contract_test.exs has a passing test
    "learning-path is registered in both mix.exs docs lists" that asserts File.exists? and
    Regex.scan count >= 2. mix test test/mailglass/docs_contract_test.exs exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Docs-only change — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package installs; slopcheck not applicable |

Docs-only change — no new attack surface, no input handling, no auth/data flow. No secrets or
credentials appear in the guide content (example links are relative, no API keys, no hostnames).
</threat_model>

<verification>
Run the full docs-contract suite:

```bash
mix test test/mailglass/docs_contract_test.exs
```

All tests must pass (exit 0). Also verify both registration sites manually:

```bash
grep -n "learning-path" mix.exs
```

Should show at least two lines: one in extras: and one in groups_for_extras: [Guides: ...].

Verify the guide file exists and has content:

```bash
wc -l guides/learning-path.md
```
</verification>

<success_criteria>
- guides/learning-path.md exists with a ## heading + ordered first-week sequence linking
  getting-started, jobs, authoring-mailables, preview, webhooks, testing, telemetry guides
- mix.exs extras: contains "guides/learning-path.md" (after "guides/getting-started.md")
- mix.exs groups_for_extras: [Guides: ...] contains "guides/learning-path.md" (after "guides/getting-started.md")
- docs_contract_test.exs "learning-path is registered in both mix.exs docs lists" test passes
- mix test test/mailglass/docs_contract_test.exs exits 0
</success_criteria>

<output>
Create .planning/phases/105-onboarding-docs-quickstart-fix-learning-arc/105-01-SUMMARY.md when done.
</output>
