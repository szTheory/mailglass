---
phase: 122-preview-surface-redesign
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/preview_live.ex
  - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
  - mailglass_admin/test/mailglass_admin/voice_test.exs
  - mailglass_admin/e2e/flows.spec.js
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements: [PREV-01]
must_haves:
  truths:
    - "The empty-mailables onboarding pane leads with the brandbook-canonical Empty string verbatim and surfaces `mix mailglass.gen.mailable` as the PRIMARY next step; the module-marker/router-list checks are demoted to a secondary checklist."
    - "The render-error card headline is 'This Mailable raised while rendering', names both the Mailable and the scenario, uses a recovery-oriented lead, and KEEPS the inline scrollable <pre> (no redirect to logs)."
    - "On the render-error transition, focus moves to the card OR the change is announced via role=status/aria-live."
    - "The dead `dark_chrome` attr is removed from preview/sidebar.ex."
    - "voice_test.exs and the preview e2e specs are updated in the SAME phase as the copy changes (green-only-forward); the start branch has assertSingleH1 coverage."
  artifacts:
    - "mailglass_admin/lib/mailglass_admin/preview_live.ex (onboarding D-09, error-card D-10, error-transition a11y D-08a, token normalization D-07)"
    - "mailglass_admin/lib/mailglass_admin/preview/sidebar.ex (dead dark_chrome attr removed D-06)"
    - "mailglass_admin/test/mailglass_admin/voice_test.exs (error headline grep updated + brandbook Empty grep added)"
    - "mailglass_admin/e2e/flows.spec.js + structural.spec.js (copy assertions updated; start-branch assertSingleH1)"
  key_links:
    - "voice_test.exs grep of the error headline + onboarding Empty string must match the new copy exactly (Pitfall-2 green-only-forward)."
    - "The orientation strip stays in the @mailables == [] branch — moving it is the ONLY way to break structural.spec.js:827-831 (D-11a)."
---

<objective>
Apply the alignment-and-polish copy + a11y + dead-code edits to Preview (D-06..D-10, D-08a) and ship
their paired test updates in the SAME commit window (D-11 / Pitfall-2 green-only-forward): brandbook
onboarding copy with the generator as the primary next step, the generalized recovery-oriented error
card with the inline `<pre>` kept, the error-transition focus/announce, the dead `dark_chrome` attr
removal, heading/spacing-token normalization, and the matched `voice_test.exs` / `flows.spec.js` /
`structural.spec.js` updates.

Purpose: Close the last voice/token/a11y gaps so Preview's chrome vocabulary matches the now-cleaned
operator surfaces — without importing any operator structure (no `data_state`, no no-data/no-match
split, no replay/PII machinery; D-01/D-08), and WITHOUT moving the already-empty-pane-only orientation
strip (D-11a).

Output: `preview_live.ex` onboarding + error arms re-voiced with brandbook strings and error-transition
a11y; `preview/sidebar.ex` dead attr deleted; `voice_test.exs` greps updated to the new copy + the
brandbook Empty string added; `flows.spec.js` / `structural.spec.js` copy assertions + start-branch
`assertSingleH1` updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-preview-surface-redesign/122-CONTEXT.md
@.planning/phases/122-preview-surface-redesign/122-PATTERNS.md
@.planning/phases/122-preview-surface-redesign/122-UI-SPEC.md

# Source surfaces + brandbook (read cited line ranges before editing)
@mailglass_admin/lib/mailglass_admin/preview_live.ex
@mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
@mailglass_admin/test/mailglass_admin/voice_test.exs
@mailglass_admin/e2e/flows.spec.js
@mailglass_admin/e2e/structural.spec.js
@brandbook/copy/microcopy.md
@brandbook/brand-book.md
</context>

<artifacts_this_phase_produces>
Symbols / strings changed by THIS plan (downstream voice/e2e tests bind to these):
- `preview_live.ex` empty-mailables arm (`:361-402`): headline+lead become the brandbook Empty string
  verbatim — **"No mailables discovered yet. Define one with `mix mailglass.gen.mailable` and it will
  appear here, ready to preview."**; `mix mailglass.gen.mailable` surfaced as PRIMARY next step (mono
  `<code>` chip, MAY carry `text-primary`); the `use Mailglass.Mailable` check + `mailables:` router-list
  escape demoted to a SECONDARY checklist; "Read preview setup" HexDocs link kept verbatim; the
  orientation strip at `:362` stays put (DO NOT MOVE — D-11a).
- `preview_live.ex` error arm (`:284-301`): headline "preview_props/0 raised an error" -> **"This Mailable
  raised while rendering"**; lead names BOTH `inspect(@current_mailable)` and `@current_scenario` with a
  recovery-oriented brandbook-voice lead; inline scrollable `<pre>` KEPT (no redirect to logs); struct-match
  at `:738-740` unchanged.
- `preview_live.ex`: error-transition a11y (D-08a) — focus-move to the error card OR `role="status"`/
  `aria-live` announce (reuse the Seam-B aria-live primitive); `motion-reveal` (existing token) on
  error-card + onboarding-pane entry; reduced-motion neutralizer honored; NO new keyframes.
- `preview/sidebar.ex`: `attr(:dark_chrome, ...)` at `:30` and `:72` + the `dark_chrome={@dark_chrome}`
  pass-through at `:56` REMOVED (dead — never read). Discovery-failure warning badge (`:113-125`) KEPT.
- `voice_test.exs`: `:67` grep "preview_props/0 raised an error" -> "This Mailable raised while rendering";
  NEW grep of the brandbook Empty string verbatim.
- `flows.spec.js` / `structural.spec.js`: copy assertions on changed onboarding/error strings updated;
  start-branch `assertSingleH1` added if not already covered (D-11c).
</artifacts_this_phase_produces>

<scope_guardrails>
## Do NOT (per D-01 / D-08 / D-11a — these are dead-UI / red-floor traps)
- Do NOT import `Components.data_state`, the no-data/no-match split, filters-gating, the replay modal,
  or any PII machinery — Preview has no records/filters/tenant-scope/async/auth for them to act on; they
  would ship as dead, unreachable UI.
- Do NOT MOVE the orientation strip (`preview_live.ex:362`). It is already empty-pane-only; moving it is
  the only way to break `structural.spec.js:827-831` / `flows.spec.js`. Only copy in the surrounding arm
  changes; the strip's own copy is byte-frozen (`shell.ex:427-436`).
- Do NOT adopt the operator left-rail nav — the sidebar-of-Mailables IS Preview's content navigation.
- Do NOT redirect the developer to logs in the error card — keep the inline `<pre>` (dev-only DX, D-10).
- Do NOT introduce a new Tailwind class (D-13). `text-primary`, `motion-reveal`, `sr-only`, `text-label`,
  `mono`/`font-mono`, `overflow-auto`, `whitespace-pre-wrap`, `max-h-80` all already ship. Confirm the
  committed `priv/static/app.css` is untouched before committing.
- "Oops" stays banned (none present). Match the render error by struct, never by message string
  (`:738-740` already correct — do not regress).
</scope_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Re-voice the empty-mailables onboarding + remove the dead dark_chrome attr (D-09, D-06, D-07)</name>
  <files>mailglass_admin/lib/mailglass_admin/preview_live.ex, mailglass_admin/lib/mailglass_admin/preview/sidebar.ex</files>
  <read_first>
    - `preview_live.ex:361-402` — the empty-mailables arm: orientation strip at `:362` (DO NOT MOVE),
      headline/lead, the two `<li>` recovery checks (`:380-384` module-marker, `:386-394` router list),
      the HexDocs link (`:396-401`).
    - `brandbook/copy/microcopy.md:17` — the Mailable Empty canonical string (use VERBATIM).
    - `brandbook/brand-book.md:69` — the **Mailable** noun lock.
    - `preview/sidebar.ex:30,56,72` — the dead `dark_chrome` attr declarations + pass-through;
      `:78-125` the render paths that never read it; `:113-125` the discovery-failure warning badge (KEEP).
    - `operator/shell.ex` eyebrow/header border + padding tokens — the normalization reference for D-07.
  </read_first>
  <action>
    Onboarding (D-09): set the headline + lead to the brandbook Empty string VERBATIM (microcopy.md:17) —
    the voice_test will grep it byte-for-byte, so do not paraphrase: "No mailables discovered yet. Define
    one with `mix mailglass.gen.mailable` and it will appear here, ready to preview." Surface
    `mix mailglass.gen.mailable` as the PRIMARY next step in a `mono` `<code>` chip (it MAY carry
    `text-primary` per UI-SPEC reserved-list #6 — existing class). Demote the two existing recovery `<li>`
    checks (the `use Mailglass.Mailable` compiled-and-loaded check + the explicit `mailables:` router-list
    escape) to a SECONDARY checklist below the primary action (wording within the brandbook
    thoughtful-maintainer voice is discretion). Keep the "Read preview setup" HexDocs link verbatim. Keep
    the orientation strip at `:362` exactly where it is (DO NOT MOVE — D-11a). Verify the long
    router-snippet `<code>` block keeps `overflow-auto`/`whitespace-pre-wrap` so it wraps/scrolls at 320px.
    Apply `motion-reveal` (existing token) to the onboarding-pane entry if not already present.
    Apply Mailable noun discipline; keep the plural sidebar collection label "Mailables".

    D-07 normalization: verify the eyebrow/header border + padding tokens match the operator shell exactly
    and fix any drift toward off-grid/arbitrary values; do NOT restructure layout or adopt operator nav.

    D-06 (sidebar.ex): delete the dead `dark_chrome` attr — the `attr(:dark_chrome, :boolean, ...)`
    declarations at `:30` and `:72` and the `dark_chrome={@dark_chrome}` pass-through at `:56`. It is never
    read in any `mailable_entry/1` clause (`:78-125`) or `mailable_label/1`, and `preview_live.ex` never
    passes it. Keep the discovery-failure warning badge (`:113-125`, icon + sr-only text) and the
    active-entry accent untouched. Pure dead-code removal, no visual change.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -Fq 'No mailables discovered yet. Define one with `mix mailglass.gen.mailable`' lib/mailglass_admin/preview_live.ex && ! grep -q 'dark_chrome' lib/mailglass_admin/preview/sidebar.ex && grep -nq 'orientation_strip' lib/mailglass_admin/preview_live.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <done>
    The onboarding arm leads with the brandbook Empty string verbatim, generator primary, recovery checks
    demoted to secondary, HexDocs link kept, orientation strip unmoved; the dead `dark_chrome` attr is gone
    from sidebar.ex; both modules compile warnings-as-errors clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generalize the error card copy + add error-transition a11y (D-10, D-08a)</name>
  <files>mailglass_admin/lib/mailglass_admin/preview_live.ex</files>
  <read_first>
    - `preview_live.ex:284-301` — the render-error arm: headline, lead, the inline `<pre>`.
    - `preview_live.ex:738-740` — the struct-match (`%Mailglass.TemplateError{}`) — do NOT regress.
    - `preview_live.ex:120-132` / `:720-749` — the `:__error__` route + synchronous render path
      (confirm the error arm is the single home for raised-render errors).
    - `inbound/evidence_card.ex:132-142` — the `role="status" aria-live="polite"` precedent to mirror.
  </read_first>
  <action>
    <!-- planner-discipline-allow: preview_props/0 raised an error -->
    Error card (D-10): change the headline "preview_props/0 raised an error" -> "This Mailable raised
    while rendering" (this is the error arm's single `<h1>`). In the lead, name BOTH the Mailable
    (`inspect(@current_mailable)`) and the scenario (`@current_scenario`) as load-bearing debugging
    context, using a recovery-oriented brandbook-voice lead (e.g. "Fix it in {Mailable} and save to
    reload — the full error is below." — exact wording is discretion within the brandbook voice). KEEP the
    inline scrollable `<pre>` (`overflow-auto max-h-80 whitespace-pre-wrap`) — do NOT redirect to logs and
    do NOT adopt the brandbook recipient-facing "…in your logs." Error line; the inline exception is the
    correct dev DX. "Oops" stays banned. Do NOT change the struct-match at `:738-740`.

    Error-transition a11y (D-08a): when the error card appears, move focus to it OR announce via
    `role="status"`/`aria-live="polite"` (reuse the Task-from-plan-01 aria-live primitive shape /
    `evidence_card.ex:132-142`; focus-move vs live-region vs both is discretion). Apply `motion-reveal`
    (existing token) to the error-card entry; honor the existing reduced-motion neutralizer; NO new
    keyframes, NO new CSS class.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -Fq 'This Mailable raised while rendering' lib/mailglass_admin/preview_live.ex && ! grep -q 'preview_props/0 raised an error' lib/mailglass_admin/preview_live.ex && grep -nq 'current_scenario' lib/mailglass_admin/preview_live.ex && grep -nq 'TemplateError' lib/mailglass_admin/preview_live.ex && grep -nq 'max-h-80' lib/mailglass_admin/preview_live.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <done>
    The error arm headline is "This Mailable raised while rendering", names the Mailable + scenario, uses a
    recovery-oriented lead, keeps the inline `<pre>` (max-h-80), retains struct-matching, and announces /
    focuses on the error transition; module compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Paired test updates — voice_test grep + e2e copy assertions + start-branch assertSingleH1 (D-11, D-11c, Pitfall-2)</name>
  <files>mailglass_admin/test/mailglass_admin/voice_test.exs, mailglass_admin/e2e/flows.spec.js, mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - `voice_test.exs:48-68` — the preview copy block; `:67` greps the old error headline (goes RED on D-10);
      `:30-45` the banned-words block with the `strip_scripts/1` dep-JS guard (leave as-is — see project
      memory "voice_test 'Oops' is dep-JS noise"); `:106-112` the operator brandbook-string grep pattern to mirror.
    - `flows.spec.js:58-82` — preview fixture helpers; `:409-462` the preview block; `:426-440` boundary/error
      tests (assert only testid visibility today — verify none grep the old headline).
    - `structural.spec.js:827-831` — the preview-orientation assertion (stays green if the strip is unmoved);
      the preview start-branch area for `assertSingleH1` coverage.
  </read_first>
  <action>
    <!-- planner-discipline-allow: preview_props/0 raised an error -->
    voice_test.exs (D-11): change the `:67` grep "preview_props/0 raised an error" ->
    "This Mailable raised while rendering" (matching D-10). ADD a new assertion that greps the brandbook
    Empty string VERBATIM so it matches the onboarding copy — mirror the operator brandbook-string grep at
    `:106-112`: assert the rendered empty-mailables HTML contains
    `No mailables discovered yet. Define one with \`mix mailglass.gen.mailable\``. Leave the banned-words
    block + `strip_scripts/1` guard untouched ("Oops" stays absent).

    flows.spec.js (D-11): update any copy-string assertion in the preview block touching the changed
    onboarding/error copy; if the boundary/error tests (`:426-440`) only assert testid visibility, leave
    them. Do NOT touch the two-theme independence lock or the toggle-a11y assertions added in plan 01.

    structural.spec.js / flows.spec.js (D-11c): add `assertSingleH1` coverage on the preview START branch
    if not already covered (the start/none-selected arm must render exactly one `<h1>`). Reuse the existing
    `assertSingleH1` helper; do not add a new one. Verify the orientation-strip assertion stays green by
    NOT moving the strip (confirm `structural.spec.js:827-831` is unchanged and still passes against the
    empty route).
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -Fq 'This Mailable raised while rendering' test/mailglass_admin/voice_test.exs && ! grep -q 'preview_props/0 raised an error' test/mailglass_admin/voice_test.exs && grep -Fq 'No mailables discovered yet. Define one with' test/mailglass_admin/voice_test.exs && node --check e2e/flows.spec.js && node --check e2e/structural.spec.js && grep -nq 'assertSingleH1' e2e/structural.spec.js e2e/flows.spec.js</automated>
  </verify>
  <done>
    `voice_test.exs` greps the new error headline + the brandbook Empty string verbatim (old headline grep
    gone); the e2e specs parse; the start branch has `assertSingleH1` coverage; the orientation-strip
    assertion is unchanged.
  </done>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` clean in `mailglass_admin`.
- Targeted gate (deterministic on main per project memory — do NOT rely on bare `mix test`):
  `cd mailglass_admin && mix test test/mailglass_admin/voice_test.exs` passes (the changed greps match).
- `node --check` parses `flows.spec.js` and `structural.spec.js`.
- `grep` gates in each task pass (brandbook Empty verbatim present; dead `dark_chrome` gone; new error
  headline present + old gone; struct-match + max-h-80 `<pre>` retained; assertSingleH1 on start branch).
- D-13 asset check: `git diff --quiet -- mailglass_admin/priv/static/app.css` (bundle untouched — no new class).
- D-11a check: `structural.spec.js:827-831` preview-orientation assertion unchanged; orientation strip
  still inside the `@mailables == []` branch.
</verification>

<success_criteria>
- Onboarding leads with the brandbook Empty string verbatim, generator primary, recovery checks secondary;
  orientation strip unmoved (D-09/D-11a).
- Error card generalized to "This Mailable raised while rendering", names Mailable + scenario, keeps the
  inline `<pre>`, announces/focuses on transition (D-10/D-08a).
- Dead `dark_chrome` attr removed (D-06); heading/token drift normalized (D-07).
- voice_test + e2e specs updated in the same phase (green-only-forward); start branch assertSingleH1
  covered (D-11/D-11c).
- No new Tailwind class; committed `priv/static/app.css` untouched (D-13).
</success_criteria>

<output>
Create `.planning/phases/122-preview-surface-redesign/122-02-SUMMARY.md` when done.
</output>
