---
phase: 122-preview-surface-redesign
plan: 03
type: execute
wave: 3
depends_on: ["122-01", "122-02"]
files_modified:
  - reference/demo_app/assets/e2e/persona-screenshots.spec.js
autonomous: true
requirements: [PREV-01]
must_haves:
  truths:
    - "The single `preview` persona cell is re-shot (NO new cells) across {northstar,fjordline-aps,helios-void} × {375,1440} × {light,dark}; that delta is the only-forward evidence — OR, if the demo webServer re-shoot needs a baseline-drifting `mix deps.get`, the shoot is deferred to Phase 123 with a captured deferred-item (the D-17 fallback), never a fabricated run and never a drifted baseline."
    - "The full v1.13 ratchet floor + D-THEME-PARITY (admin-chrome light/dark/system) stays green only-forward — no pillar re-score (that is Phase 123)."
    - "The committed `mailglass_admin/priv/static/app.css` bundle is untouched (no `mix assets.build` needed; TokenParity gate not tripped)."
  artifacts:
    - "reference/demo_app/assets/e2e/persona-screenshots.spec.js (re-shoot of the existing `preview` cell — no new cells) OR a captured deferred-item record if the D-17 fallback applies."
  key_links:
    - "Adding ANY new persona cell fires the persona drift-guard (D-12) — re-shoot only."
    - "A naive `mix assets.build` emits raw-inline daisyUI 5.5.19 theme blocks that BREAK TokenParityTest — only commit a bundle that survives the gate; prefer no rebuild at all (D-13)."
---

<objective>
Close the phase: re-shoot the single existing `preview` persona cell (NO new cells — D-12), confirm the
full inherited ratchet floor + D-THEME-PARITY stay green only-forward (NO pillar re-score — that is
Phase 123), and confirm the asset bundle is untouched (D-13). If the demo webServer re-shoot requires a
baseline-drifting `mix deps.get` (the 121 D-17 precedent / CONTEXT [121-04]), apply the documented D-17
fallback — DEFER the actual shoot to Phase 123 and capture it as a deferred-item, rather than fabricating
a run or drifting the baseline.

Purpose: Provide the only-forward screenshot evidence for the Preview redesign and lock the
green-only-forward guarantee, while honoring the persona drift-guard (no new cells) and the TokenParity
landmine (no naive bundle rebuild).

Output: a re-shot `preview` persona cell delta (or a captured deferred-item per D-17); a verified-green
floor; a confirmed-untouched `app.css` bundle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-preview-surface-redesign/122-CONTEXT.md
@.planning/phases/122-preview-surface-redesign/122-PATTERNS.md
@.planning/phases/122-preview-surface-redesign/122-UI-SPEC.md
@.planning/phases/121-inbound-surface-redesign/121-CONTEXT.md

@reference/demo_app/assets/e2e/persona-screenshots.spec.js
</context>

<artifacts_this_phase_produces>
- `reference/demo_app/assets/e2e/persona-screenshots.spec.js`: the single `preview` dev-open cell
  (`:70`, `route: "/dev/mail"`, expanded via `cellsFor` `:98-109` to {375,1440}×{light,dark}) is
  RE-SHOT — no cell added, no cell removed. The screenshot delta IS the only-forward evidence (D-12).
- OR (D-17 fallback): a captured deferred-item recording that the persona re-shoot is deferred to
  Phase 123 because the demo webServer boot needs a baseline-drifting `mix deps.get` — no fabricated run,
  no baseline drift this phase.
</artifacts_this_phase_produces>

<persona_reshoot_contingency>
## D-12 + D-17 — Persona re-shoot, NO new cells, with the documented fallback

The producer enumerates ONE `preview` cell already (`persona-screenshots.spec.js:70`). Re-run it in one
pass across {northstar,fjordline-aps,helios-void} × {375,1440} × {light,dark} via `cellsFor`. **Adding
any new cell fires the persona drift-guard — re-shoot ONLY.**

**The 121 D-17 precedent (CONTEXT decision [121-04]):** the demo webServer re-shoot may need a
baseline-drifting `mix deps.get` (deps/_build/tailwind are gitignored; a worktree/clean boot forces a
network refetch that drifts the lock). If — and only if — the shoot cannot run without that drift, apply
the D-17 fallback: **DEFER the actual shoot to Phase 123 and capture it as a deferred-item** (mirror how
121-04 deferred the operator-inbound re-shoot). Do NOT fabricate a run. Do NOT commit a drifted baseline
or a drifted `mix.lock`. Record the deferral explicitly in the SUMMARY.
</persona_reshoot_contingency>

<tasks>

<task type="auto">
  <name>Task 1: Re-shoot the single `preview` persona cell (no new cells) — or apply the D-17 deferral</name>
  <files>reference/demo_app/assets/e2e/persona-screenshots.spec.js</files>
  <read_first>
    - `persona-screenshots.spec.js:70` — the single `preview` dev-open cell (`route: "/dev/mail"`).
    - `persona-screenshots.spec.js:98-109` — `cellsFor` expansion (priority>2 → anchor square only;
      {375,1440}×{light,dark}); `:124-125` the shoot loop.
    - `.planning/phases/121-inbound-surface-redesign/121-CONTEXT.md` D-17 — the deferral fallback shape.
    - Project memory "Axe baseline producer runs locally" + "execute-phase worktrees impractical":
      persona/axe producers run locally end-to-end (node_modules + Chromium present, Playwright boots the
      server) — but the demo webServer boot can drift the baseline via `mix deps.get`.
  </read_first>
  <action>
    Attempt to re-run the persona producer for the single `preview` cell ONLY — do NOT add, rename, or
    remove any cell (that fires the drift-guard, D-12). Run it in one pass so the screenshot delta for
    {northstar,fjordline-aps,helios-void} × {375,1440} × {light,dark} is the only-forward evidence.

    BEFORE running, check whether the demo webServer boot will drift the baseline (a `mix deps.get` that
    re-bumps the lock — e.g. the swoosh 1.26.1 drift noted in project memory "demo_app swoosh lock drift").
    If the shoot runs WITHOUT drifting `reference/demo_app/mix.lock` or the persona baseline beyond the
    intended screenshot delta, commit the re-shot cell delta. If the shoot CANNOT run without a
    baseline-drifting `mix deps.get`, apply the D-17 fallback: DEFER the shoot to Phase 123, do NOT
    fabricate a run, do NOT commit a drifted lock/baseline, and record the deferral as a captured
    deferred-item in the SUMMARY (mirror 121-04). After any local `mix` run in `reference/demo_app`,
    `git checkout` the `mix.lock` if it drifted (transitive swoosh bump) before committing anything.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && test "$(grep -c 'kind: "dev-open", route: "/dev/mail"' reference/demo_app/assets/e2e/persona-screenshots.spec.js)" = "1" && node --check reference/demo_app/assets/e2e/persona-screenshots.spec.js && git diff --quiet -- reference/demo_app/mix.lock && echo "PERSONA-CELL-COUNT-OK + LOCK-CLEAN"</automated>
  </verify>
  <done>
    Either the single `preview` cell is re-shot (no cells added/removed; `reference/demo_app/mix.lock`
    not drifted) and its delta committed, OR the D-17 fallback is applied (shoot deferred to Phase 123,
    captured as a deferred-item, no fabricated run, no drifted baseline). The persona spec parses and
    still enumerates exactly one `preview` cell.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm asset bundle untouched + full inherited floor green only-forward (D-13)</name>
  <files>mailglass_admin/priv/static/app.css</files>
  <read_first>
    - Project memory "Token-parity bundle landmine": the committed `app.css` is canonical; a fresh
      `mix assets.build` emits raw-inline daisyUI 5.5.19 theme blocks that BREAK TokenParityTest.
    - `122-UI-SPEC.md` §Test Gate Contract → Asset pipeline + Floor.
  </read_first>
  <action>
    Confirm this phase introduced NO new Tailwind class — therefore NO `mix assets.build` is needed and the
    committed `mailglass_admin/priv/static/app.css` must be byte-identical (D-13). If a genuinely new class
    slipped in during plans 01/02, STOP and reconcile to an existing class rather than rebuilding (prefer
    NO rebuild); only if a new class is truly unavoidable, rebuild AND run
    `mix test test/mailglass_admin/components/token_parity_test.exs` BEFORE committing, and commit only a
    bundle that survives the gate. Re-confirm the full inherited v1.13 ratchet floor + D-THEME-PARITY
    (admin-chrome light/dark/system) hold green only-forward — this phase HOLDS the floor; it does NOT
    re-score the 54-cell pillars and does NOT arm the new judgment gates (that is Phase 123).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && git diff --quiet -- mailglass_admin/priv/static/app.css && echo "BUNDLE-UNTOUCHED" && cd mailglass_admin && mix test test/mailglass_admin/components/token_parity_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>
    `mailglass_admin/priv/static/app.css` is untouched (no new class introduced); TokenParityTest passes;
    no pillar re-score or judgment-gate arming was performed (deferred to Phase 123).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → demo `/dev/mail` (screenshot producer) | Dev/CI-only Playwright shoot of the dev preview surface; no tenant auth, no network ingress to production, no PII captured (the persona screenshots are dev fixtures). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-03 | Tampering | persona baseline / `mix.lock` drift | low | mitigate | A demo `mix deps.get` can drift `reference/demo_app/mix.lock` (swoosh transitive bump) and the persona baseline. Mitigation: `git checkout` the lock if it drifts; if the shoot cannot run without drift, apply the D-17 deferral rather than committing a drifted baseline. No production artifact affected. |

No high-severity threats are introduced. This plan is a dev-only screenshot re-shoot + a read-only floor
verification; it adds no route, no auth surface, no data flow, and no production asset change.
</threat_model>

<verification>
- Persona spec parses (`node --check`) and still enumerates exactly ONE `preview` dev-open cell (no new cells).
- `reference/demo_app/mix.lock` is not drifted in the committed diff (or the shoot was deferred per D-17).
- `mailglass_admin/priv/static/app.css` is byte-identical (`git diff --quiet`).
- `token_parity_test.exs` passes.
- No 54-cell pillar re-score and no judgment-gate arming performed (Phase 123 scope).
</verification>

<success_criteria>
- The single `preview` persona cell is re-shot (no new cells) OR deferred to Phase 123 per the D-17
  fallback with a captured deferred-item (D-12/D-17).
- The full inherited ratchet floor + D-THEME-PARITY hold green only-forward; no pillar re-score (D-13/floor).
- The committed asset bundle is untouched; TokenParity gate green (D-13).
</success_criteria>

<output>
Create `.planning/phases/122-preview-surface-redesign/122-03-SUMMARY.md` when done.
</output>
