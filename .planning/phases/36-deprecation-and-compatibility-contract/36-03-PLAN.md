---
phase: 36-deprecation-and-compatibility-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 36-01
  - 36-02
files_modified:
  - lib/mix/tasks/mailglass.docs.check.ex
  - lib/mix/tasks/mailglass.stability.check.ex
  - test/mailglass/docs_contract_test.exs
  - test/mailglass/docs_migration_smoke_test.exs
  - test/mailglass/compatibility_contract_test.exs
  - mix.exs
autonomous: true
requirements:
  - COMPAT-01
  - COMPAT-02
  - COMPAT-03
  - COMPAT-04
must_haves:
  truths:
    - "Maintainers can prove the canonical compatibility guide and upgrade guide stay wired into Tier 1 docs."
    - "Maintainers can verify every documented deprecated or legacy-supported path through one explicit proof inventory."
    - "The new compatibility proof stays lightweight and repo-native instead of becoming a Phase 37-style heavy enforcement system."
  artifacts:
    - path: "test/mailglass/compatibility_contract_test.exs"
      provides: "Maintainer-verifiable deprecated-path inventory and proof shape"
      min_lines: 120
    - path: "test/mailglass/docs_migration_smoke_test.exs"
      provides: "Executable proof that the canonical `0.x -> 1.0` guide remains current"
      min_lines: 100
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Tier 1 doc token checks for compatibility-guide and upgrade-guide wiring"
      contains: "compatibility-and-deprecations"
    - path: "mix.exs"
      provides: "Repo-native verification alias coverage for compatibility proof"
      contains: "verify.support_contract.core"
  key_links:
    - from: "test/mailglass/compatibility_contract_test.exs"
      to: "guides/compatibility-and-deprecations.md"
      via: "deprecated-path inventory assertions"
      pattern: "support-until|warning channel|proof artifact"
    - from: "test/mailglass/docs_migration_smoke_test.exs"
      to: "guides/upgrading-to-v1_0.md"
      via: "canonical upgrade smoke coverage"
      pattern: "upgrading-to-v1_0"
    - from: "mix.exs"
      to: "scripts/verify_support_contract.sh"
      via: "support-contract core alias"
      pattern: "verify\\.support_contract\\.core"
---

<objective>
Extend the repo’s existing lightweight docs/test checks so the compatibility contract is maintainable and verifiable without pulling Phase 37 heavy enforcement forward.

Purpose: Close COMPAT-04’s proof gap and keep COMPAT-01 through COMPAT-03 from drifting after publication.
Output: Compatibility contract tests, upgrade-guide smoke coverage, and small docs-check/alias updates wired into the existing support-contract verification path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-CONTEXT.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-RESEARCH.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-PATTERNS.md
@.planning/phases/35-stability-contract-audit/35-03-PLAN.md
@.planning/phases/35-stability-contract-audit/35-03-SUMMARY.md
@lib/mix/tasks/mailglass.docs.check.ex
@lib/mix/tasks/mailglass.stability.check.ex
@test/mailglass/docs_contract_test.exs
@test/mailglass/docs_migration_smoke_test.exs
@mix.exs
@scripts/verify_support_contract.sh
@.github/workflows/ci.yml

<interfaces>
From lib/mix/tasks/mailglass.docs.check.ex:
```elixir
@tier1_paths [...]
@tier1_surface_rules %{...}
def run(argv) do
```

From lib/mix/tasks/mailglass.stability.check.ex:
```elixir
# Exemptions (escape hatches and internals):
- `Mailglass.Message.update_swoosh/2`
- `Mailglass.Message.new/2`
- `Mailglass.Outbound.send/2`
```

From mix.exs:
```elixir
"verify.support_contract.core": [
  "test ... --warnings-as-errors"
],
"verify.docs.contract": [
  "test test/mailglass/docs_contract_test.exs --warnings-as-errors"
],
"verify.docs.migration": [
  "test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors"
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compatibility contract proof tests before wiring checks</name>
  <files>test/mailglass/compatibility_contract_test.exs, test/mailglass/docs_migration_smoke_test.exs</files>
  <behavior>
    - Test 1: The canonical compatibility guide contains the stable-lane versus compatibility-lane model plus the repo-truth support matrix rows from D-36-03 through D-36-08.
    - Test 2: The deprecated-path inventory explicitly covers each retained path with replacement, warning channel, `--warnings-as-errors` impact, support-until version, and proof artifact per D-36-23.
    - Test 3: The canonical `upgrading-to-v1_0` guide remains the single upgrade authority, while older guides are subordinate references.
  </behavior>
  <action>First create `test/mailglass/compatibility_contract_test.exs` as the maintainer-verifiable proof shape for COMPAT-04. The test should define one explicit inventory of retained deprecated or legacy-supported paths and assert, for each row, the documented replacement, warning channel, strict-CI impact, support-until version no earlier than `v2.0`, and proof artifact reference. Use actual repo surfaces from D-36-15 through D-36-16 and the research inventory, not placeholders. Then extend `test/mailglass/docs_migration_smoke_test.exs` so it exercises the canonical `guides/upgrading-to-v1_0.md` path and confirms `guides/upgrading-from-v0_1.md` and `guides/migration-from-swoosh.md` are subordinate references rather than competing authorities. Keep the proof docs-driven and deterministic; do not introduce compile hooks or runtime telemetry.</action>
  <verify>
    <automated>mix test test/mailglass/compatibility_contract_test.exs test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The repo has executable proof for the deprecated-path inventory and the canonical upgrade guide, and the proof shape is explicit enough for Phase 37 to inherit without redesign.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget lightweight docs checks and support-contract aliases to the new compatibility story</name>
  <files>lib/mix/tasks/mailglass.docs.check.ex, lib/mix/tasks/mailglass.stability.check.ex, test/mailglass/docs_contract_test.exs, mix.exs</files>
  <behavior>
    - Test 1: Tier 1 docs checks fail if the canonical compatibility guide or canonical `1.0` upgrade guide pointers drift out of root/admin docs.
    - Test 2: Support-contract verification includes the new compatibility proof without requiring a new CI bucket.
    - Test 3: Stability-check messaging still documents escape-hatch exemptions honestly after the compatibility-lane classification lands.
  </behavior>
  <action>Update the existing lightweight enforcement surfaces in the same style established by Phase 35. Extend `lib/mix/tasks/mailglass.docs.check.ex` and `test/mailglass/docs_contract_test.exs` so Tier 1 docs must reference the canonical compatibility guide and canonical `1.0` upgrade guide where appropriate, and so stale fragmented policy text is rejected. Adjust `lib/mix/tasks/mailglass.stability.check.ex` moduledoc and exemption wording so `Mailglass.Message.new/2` and `Mailglass.Outbound.send/2` are described using the new deprecated-versus-legacy compatibility vocabulary without changing the leak-check algorithm. Update `mix.exs` aliases so the new compatibility proof test runs inside existing repo-native verification paths such as `verify.support_contract.core` and the docs-specific aliases instead of creating a new required CI bucket. Keep `scripts/verify_support_contract.sh` and `.github/workflows/ci.yml` semantically unchanged unless a path rename or alias addition makes a small follow-on edit literally necessary.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/compatibility_contract_test.exs test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors && mix mailglass.docs.check && mix verify.support_contract.core</automated>
  </verify>
  <done>The compatibility contract is guarded by existing lightweight repo-native checks, and maintainers can verify the new docs/inventory story without a new heavy enforcement subsystem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compatibility docs -> verification | The compatibility contract only stays trustworthy if docs/tests encode the same retained-path inventory. |
| alias wiring -> CI truth | Required support-contract commands must continue proving the documented support story after Phase 36 lands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | T | test/mailglass/compatibility_contract_test.exs | mitigate | Encode one explicit deprecated-path inventory with replacement, warning-channel, strict-CI, support-until, and proof-artifact fields so maintainers can detect drift mechanically. |
| T-36-08 | R | lib/mix/tasks/mailglass.docs.check.ex / test/mailglass/docs_contract_test.exs | mitigate | Retarget existing Tier 1 checks to the canonical compatibility and upgrade guides so doc drift is caught before release. |
| T-36-09 | D | mix.exs support aliases | accept | Reuse existing support-contract and docs aliases instead of adding new CI buckets; this keeps enforcement light while preserving release-blocking coverage. |
</threat_model>

<verification>
Run the new compatibility proof test, the updated migration smoke, the docs-contract test, `mix mailglass.docs.check`, and the existing `mix verify.support_contract.core` alias to prove the Phase 36 contract with repo-native checks.
</verification>

<success_criteria>
Maintainers can verify the Phase 36 compatibility contract and deprecated-path inventory with small deterministic checks that fit the current repo workflow, and no new heavy enforcement system is introduced ahead of Phase 37.
</success_criteria>

<output>
After completion, create `.planning/phases/36-deprecation-and-compatibility-contract/36-03-SUMMARY.md`
</output>
