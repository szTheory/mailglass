---
phase: 36-deprecation-and-compatibility-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - guides/upgrading-to-v1_0.md
  - guides/upgrading-from-v0_1.md
  - guides/migration-from-swoosh.md
  - lib/mailglass/message.ex
  - lib/mailglass/outbound.ex
  - lib/mix/tasks/mailglass.upgrade.v0_2.ex
  - test/mailglass/docs_migration_smoke_test.exs
autonomous: true
requirements:
  - COMPAT-03
  - COMPAT-04
must_haves:
  truths:
    - "Adopters can follow one canonical latest-`0.x` to `1.0` path without guessing which older guides still matter."
    - "Every retained legacy bridge named in the upgrade path has a documented replacement, warning channel, and support horizon no earlier than `v2.0`."
    - "Warnings-as-errors adopters can tell which retained paths are safe to keep temporarily and which should be migrated immediately."
  artifacts:
    - path: "guides/upgrading-to-v1_0.md"
      provides: "Canonical latest-`0.x` to `1.0` upgrade guide with compatibility-lane inventory"
      min_lines: 140
    - path: "guides/upgrading-from-v0_1.md"
      provides: "Subordinate codemod-focused reference that now points to the canonical `1.0` upgrade guide"
      min_lines: 80
    - path: "guides/migration-from-swoosh.md"
      provides: "Subordinate raw-Swoosh migration reference aligned to the canonical `1.0` guide"
      min_lines: 50
    - path: "lib/mailglass/message.ex"
      provides: "Point-of-use docs for deprecated `Message.new/2` versus the stable setter lane"
      contains: "@deprecated"
    - path: "lib/mailglass/outbound.ex"
      provides: "Point-of-use docs classifying `send/2` as a compatibility bridge and `deliver/2` as canonical"
      contains: "deliver/2"
  key_links:
    - from: "guides/upgrading-to-v1_0.md"
      to: "lib/mailglass/message.ex"
      via: "deprecated `Message.new/2` replacement guidance"
      pattern: "Message\\.new|update_swoosh|to\\("
    - from: "guides/upgrading-to-v1_0.md"
      to: "lib/mailglass/outbound.ex"
      via: "legacy delivery bridge classification"
      pattern: "Outbound\\.send|Mailglass\\.deliver"
    - from: "lib/mix/tasks/mailglass.upgrade.v0_2.ex"
      to: "guides/upgrading-to-v1_0.md"
      via: "codemod task guidance path"
      pattern: "upgrading-to-v1_0|compatibility-and-deprecations"
---

<objective>
Publish the canonical latest-`0.x` to `1.0` upgrade path and align code-adjacent migration docs with the narrow compatibility lane.

Purpose: Satisfy COMPAT-03 and the public-inventory half of COMPAT-04 without freezing every historical path or inventing new warning infrastructure.
Output: One canonical upgrade guide, subordinate legacy guides, point-of-use source docs, and a targeted migration smoke that proves the canonical guide is the single upgrade authority before Phase 36’s broader proof pass.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-CONTEXT.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-RESEARCH.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-PATTERNS.md
@.planning/phases/36-deprecation-and-compatibility-contract/36-01-PLAN.md
@guides/upgrading-from-v0_1.md
@guides/migration-from-swoosh.md
@lib/mailglass/message.ex
@lib/mailglass/outbound.ex
@lib/mix/tasks/mailglass.upgrade.v0_2.ex
@test/mailglass/docs_migration_smoke_test.exs

<interfaces>
From lib/mailglass/message.ex:
```elixir
@doc since: "0.1.0"
@deprecated "Use native Mailglass.Message setters instead"
@spec new(Swoosh.Email.t(), keyword()) :: t()

@doc since: "0.1.0"
@spec update_swoosh(t(), (Swoosh.Email.t() -> Swoosh.Email.t())) :: t()
```

From lib/mailglass/outbound.ex:
```elixir
@doc since: "0.1.0"
@spec send(Message.t() | Swoosh.Email.t(), keyword()) ::
        {:ok, Delivery.t()} | {:error, Mailglass.Error.t()}

@doc since: "0.1.0"
defdelegate deliver(msg, opts \\ []), to: __MODULE__, as: :send
```

From lib/mix/tasks/mailglass.upgrade.v0_2.ex:
```elixir
@migration_guide_url "https://hexdocs.pm/mailglass/guides/upgrading-from-v0_1.html"
@shortdoc "Codemod to upgrade raw Swoosh calls to native Mailglass.Message setters"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical latest-`0.x` to `1.0` upgrade guide</name>
  <files>guides/upgrading-to-v1_0.md, guides/upgrading-from-v0_1.md, guides/migration-from-swoosh.md, test/mailglass/docs_migration_smoke_test.exs</files>
  <action>Create `guides/upgrading-to-v1_0.md` as the single canonical `0.x -> 1.0` guide per D-36-18 through D-36-21. It must target the latest real `0.x` line, not speculative older branches, and it must explicitly classify retained paths into: stable lane defaults, warning-emitting deprecated paths, and silent legacy-supported compatibility bridges. Include required code changes, sibling-package version expectations, strict-CI guidance, and a small deprecation-DX inventory table with these exact columns per D-36-23: surface, replacement, warning channel, `--warnings-as-errors` impact, support-until version, and proof artifact. Use the existing `guides/upgrading-from-v0_1.md` and `guides/migration-from-swoosh.md` as subordinate references: keep their focused walkthroughs, but rewrite their intros/links so they feed the canonical `1.0` guide rather than competing with it. In the same task, extend `test/mailglass/docs_migration_smoke_test.exs` so it proves `guides/upgrading-to-v1_0.md` is the canonical authority and the older guides are subordinate references. Keep the compatibility promise small and honest: `Mailglass.Message.new/2`, raw `%Swoosh.Email{}` delivery, `Mailglass.Outbound.send/2`, the `mix mailglass.upgrade.v0_2` codemod, and deprecated `verify.phase_*` aliases may be documented only if each has a replacement and horizon. Do not stabilize new legacy surfaces just because they are reachable.</action>
  <verify>
    <automated>mix test test/mailglass/docs_migration_smoke_test.exs --warnings-as-errors && rg -n "surface|replacement|warning channel|--warnings-as-errors impact|support-until version|proof artifact|Mailglass\\.Message\\.new/2|Mailglass\\.Outbound\\.send/2|Mailglass\\.deliver/2|mix mailglass\\.upgrade\\.v0_2|verify\\.phase_|mailglass_admin" guides/upgrading-to-v1_0.md guides/upgrading-from-v0_1.md guides/migration-from-swoosh.md</automated>
  </verify>
  <done>There is one canonical `0.x -> 1.0` guide, older migration docs are subordinate to it, and every retained compatibility path is documented with replacement, warning behavior, and horizon.</done>
</task>

<task type="auto">
  <name>Task 2: Align source-level migration docs with the compatibility lane</name>
  <files>lib/mailglass/message.ex, lib/mailglass/outbound.ex, lib/mix/tasks/mailglass.upgrade.v0_2.ex</files>
  <action>Update point-of-use source docs so they match the new compatibility contract per D-36-09 through D-36-16 and D-36-20. In `lib/mailglass/message.ex`, make the `Message.new/2` docs clearly read as a retained deprecated path with native setters as the preferred replacement. In `lib/mailglass/outbound.ex`, keep `deliver/2` as the canonical front door and rewrite `send/2` docs so they explicitly describe it as a legacy compatibility bridge rather than an equally preferred public verb; preserve runtime behavior. In `lib/mix/tasks/mailglass.upgrade.v0_2.ex`, update the guide URL and module docs so the codemod is framed as transitional tooling that helps reach the stable lane, not as long-term stable contract surface by itself. Document warning behavior honestly where it exists and keep silent legacy bridges explicitly labeled as such. Do not add new custom warnings or runtime shims.</action>
  <verify>
    <automated>rg -n "@deprecated|native Mailglass\\.Message setters|legacy compatibility bridge|canonical public verb|upgrading-to-v1_0|transitional" lib/mailglass/message.ex lib/mailglass/outbound.ex lib/mix/tasks/mailglass.upgrade.v0_2.ex</automated>
  </verify>
  <done>Code-adjacent docs, deprecation metadata, and task guidance all tell the same upgrade story as the canonical `1.0` guide without changing runtime behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upgrade docs -> adopters | Migration docs determine which legacy paths adopters keep or remove before adopting `1.0`. |
| source docs -> warnings-as-errors users | Point-of-use docs and metadata shape whether strict-CI adopters treat a path as safe to keep. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | T | guides/upgrading-to-v1_0.md | mitigate | Build the guide from current source truth and classify each retained path as deprecated or legacy-supported so silent over-promising does not enter the upgrade contract. |
| T-36-05 | I | lib/mailglass/outbound.ex | mitigate | Mark `send/2` as a compatibility bridge and `deliver/2` as canonical so adopters do not infer two equally preferred public lanes. |
| T-36-06 | R | lib/mix/tasks/mailglass.upgrade.v0_2.ex | mitigate | Reframe the codemod as transitional tooling tied to the canonical upgrade guide so maintainers can prove what it does and does not promise long-term. |
</threat_model>

<verification>
Confirm the canonical `1.0` guide exists, subordinate guides point to it, and each retained path named there has a documented replacement, warning channel, strict-CI impact, and `v2.0`-or-later support horizon.
</verification>

<success_criteria>
Adopters can upgrade from the latest supported `0.x` path to `1.0` using one guide, and the retained compatibility lane is explicit, small, and honest about warnings and support horizons.
</success_criteria>

<output>
After completion, create `.planning/phases/36-deprecation-and-compatibility-contract/36-02-SUMMARY.md`
</output>
