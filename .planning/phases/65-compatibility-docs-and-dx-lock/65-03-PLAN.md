---
phase: 65-compatibility-docs-and-dx-lock
plan: 03
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - mailglass_inbound/README.md
  - mailglass_inbound/docs/inbound-install.md
  - guides/compatibility-and-deprecations.md
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
  - lib/mix/tasks/mailglass.docs.check.ex
autonomous: true
requirements:
  - DX-01
must_haves:
  truths:
    - "The canonical inbound adoption path and compatibility story are locked by existing docs-contract seams, not prose review alone."
    - "The active compatibility topology stays explicit: `guides/compatibility-and-deprecations.md` is the repo-root guide and stable inbound guarantees route through `api_stability.md`."
  artifacts:
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Fail-closed package-local wording checks for README, install, and compatibility guidance"
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Tier 1 required-token enforcement for the adoption and compatibility story"
  key_links:
    - from: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      to: "guides/compatibility-and-deprecations.md"
      via: "required and forbidden adoption/compatibility assertions"
      pattern: "compatibility-and-deprecations"
    - from: "lib/mix/tasks/mailglass.docs.check.ex"
      to: "mailglass_inbound/README.md"
      via: "Tier 1 surface rules"
      pattern: "canonical adoption lane"
---

<objective>
Extend the inbound docs-contract seam and the Tier 1 docs checker so the adoption and compatibility story cannot drift silently.

Purpose: Turn the Phase 65 README/install/compatibility content into executable contract proof for DX-01 and the locked compatibility posture from D-03 through D-05.
Output: Package-local docs-contract assertions and Tier 1 docs-check rules that fail closed on adoption drift, wrong compatibility routing, and topology confusion.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-RESEARCH.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-PATTERNS.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-VALIDATION.md
@.planning/phases/64-contract-verification-hardening/64-04-SUMMARY.md
@.planning/phases/64-contract-verification-hardening/64-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the canonical adoption path and compatibility guide in the inbound docs-contract test</name>
  <read_first>
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    mailglass_inbound/README.md
    mailglass_inbound/docs/inbound-install.md
    guides/compatibility-and-deprecations.md
    mailglass_inbound/docs/api_stability.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-04-SUMMARY.md
  </read_first>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/inbound-install.md, guides/compatibility-and-deprecations.md, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <action>Extend `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` to encode D-01 through D-05 as explicit required and forbidden checks over `mailglass_inbound/README.md`, `mailglass_inbound/docs/inbound-install.md`, and `guides/compatibility-and-deprecations.md`. Add path constants and assertions that the README owns the canonical adoption lane, the install guide is subordinate, the README links to operator/testing/compatibility follow-through docs, and the compatibility guide routes stable guarantees to `mailglass_inbound/docs/api_stability.md`. Lock the deprecation-DX inventory headings for surface, bridge or replacement, warning/migration channel, `--warnings-as-errors` impact, support-until horizon, and proof artifact. Add explicit guards that README/install do not point at a repo-root `docs/compatibility-and-deprecations.md` file and do not reintroduce installer framing or alternate canonical-path wording. Tune README/install/guide wording only if the new assertions expose real drift.</action>
  <acceptance_criteria>
    - `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` reads `guides/compatibility-and-deprecations.md` and asserts README/install/guide parity for the canonical adoption path.
    - The new assertions require `mailglass_inbound/docs/api_stability.md`, the deprecation-DX inventory headings, and follow-through links to operator/testing/compatibility docs.
    - The new assertions fail if README or install guide introduce repo-root `docs/compatibility-and-deprecations.md` routing, installer framing, or second-authority wording.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The package-local docs-contract lane fails on any future adoption-path or compatibility-story drift across README, install, and guide prose.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten Tier 1 docs-check rules for the adoption and compatibility story</name>
  <read_first>
    lib/mix/tasks/mailglass.docs.check.ex
    mailglass_inbound/README.md
    mailglass_inbound/docs/inbound-install.md
    guides/compatibility-and-deprecations.md
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
  </read_first>
  <files>lib/mix/tasks/mailglass.docs.check.ex</files>
  <action>Update `lib/mix/tasks/mailglass.docs.check.ex` so Tier 1 required-token enforcement matches the final adoption and compatibility story from Task 1. Add or refresh required tokens for the canonical adoption lane, the `MailglassInbound.Ingress.CachingBodyReader` setup, async follow-through, and the active compatibility route through `guides/compatibility-and-deprecations.md` plus `mailglass_inbound/docs/api_stability.md`. Keep `guides/compatibility-and-deprecations.md` in `@tier1_paths` as the repo-root compatibility source for this phase, do not add a repo-root `docs/compatibility-and-deprecations.md` path, and add forbidden tokens where simple string matching can safely catch contradictory adoption or compatibility wording.</action>
  <acceptance_criteria>
    - `lib/mix/tasks/mailglass.docs.check.ex` required-token rules for `mailglass_inbound/README.md`, `mailglass_inbound/docs/inbound-install.md`, and `guides/compatibility-and-deprecations.md` match the final canonical adoption and compatibility story.
    - `@tier1_paths` and `@tier1_surface_rules` continue to treat `guides/compatibility-and-deprecations.md` as the repo-root compatibility doc and do not introduce a missing repo-root `docs/compatibility-and-deprecations.md` entry.
    - The checker includes at least one forbidden-token guard against contradictory canonical-path or compatibility wording that can be expressed safely with string matching.
  </acceptance_criteria>
  <verify>
    <automated>mix mailglass.docs.check</automated>
  </verify>
  <done>The root Tier 1 docs checker now fails closed on adoption-path and compatibility-route drift for inbound docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executable docs checks -> release-blocking verification | This plan changes the automated contract lanes that decide whether Tier 1 docs are acceptable. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-65-07 | Repudiation | high | inbound docs-contract test | mitigate | yes | Blocking acceptance: `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` asserts canonical README/install/compatibility routing and `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` passes. |
| T-65-08 | Tampering | high | Tier 1 docs checker | mitigate | yes | Blocking acceptance: `lib/mix/tasks/mailglass.docs.check.ex` encodes the final adoption and compatibility tokens, keeps the active guide topology explicit, and `mix mailglass.docs.check` passes. |
| T-65-09 | Spoofing | medium | compatibility topology | mitigate | no | Keep the repo-root compatibility story anchored to `guides/compatibility-and-deprecations.md` so adopters are not routed to a nonexistent second authority. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- `mix mailglass.docs.check`
- `mix verify.stability_contract`
</verification>

<success_criteria>
- README/install/compatibility wording is protected by the package-local docs-contract lane and the root Tier 1 docs checker.
- The active compatibility topology and stable-surface routing cannot drift without a failing automated check.
</success_criteria>

<output>
Create `.planning/phases/65-compatibility-docs-and-dx-lock/65-03-SUMMARY.md` when done
</output>
