---
phase: 65-compatibility-docs-and-dx-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/README.md
  - mailglass_inbound/docs/inbound-install.md
  - guides/compatibility-and-deprecations.md
autonomous: true
requirements:
  - DX-01
must_haves:
  truths:
    - "Adopters can complete one canonical inbound adoption path from `mailglass_inbound/README.md` without conflicting setup guidance."
    - "Inbound compatibility wording routes readers to the canonical stability inventory and does not widen the public contract beyond Phase 63."
  artifacts:
    - path: "mailglass_inbound/README.md"
      provides: "Canonical inbound adoption lane"
    - path: "mailglass_inbound/docs/inbound-install.md"
      provides: "Subordinate setup deep dive that matches the README sequence"
    - path: "guides/compatibility-and-deprecations.md"
      provides: "Repo-root compatibility/deprecation guidance for inbound stable surfaces"
  key_links:
    - from: "mailglass_inbound/README.md"
      to: "mailglass_inbound/docs/inbound-install.md"
      via: "canonical path plus deeper-setup link"
      pattern: "inbound-install\\.md"
    - from: "guides/compatibility-and-deprecations.md"
      to: "mailglass_inbound/docs/api_stability.md"
      via: "stable-surface compatibility routing"
      pattern: "mailglass_inbound/docs/api_stability\\.md"
---

<objective>
Make the inbound README the one canonical adoption path and align the compatibility/deprecation story around the active repo docs topology.

Purpose: Satisfy DX-01 without opening a second setup authority or implying a broader compatibility promise than the Phase 63 inventory allows.
Output: A coherent README/install/compatibility docs trio that routes every guarantee claim back to the canonical inbound stability inventory.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-RESEARCH.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-PATTERNS.md
@.planning/phases/65-compatibility-docs-and-dx-lock/65-VALIDATION.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-01-SUMMARY.md
@.planning/phases/64-contract-verification-hardening/64-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep the README as the only canonical inbound adoption lane</name>
  <read_first>
    mailglass_inbound/README.md
    mailglass_inbound/docs/inbound-install.md
    mailglass_inbound/docs/api_stability.md
    mailglass_inbound/docs/inbound-operator.md
    mailglass_inbound/docs/inbound-testing.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
    .planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/README.md, mailglass_inbound/docs/inbound-install.md</files>
  <action>Implement D-01 and D-02 by making `mailglass_inbound/README.md` the sole canonical inbound adoption checklist and `mailglass_inbound/docs/inbound-install.md` the supporting deep dive. Keep the README as one ordered manual path that covers exact dependency pins, `mix deps.get`, `mix ecto.migrate`, `body_reader: {MailglassInbound.Ingress.CachingBodyReader, :read_body, []}`, router plus mailbox wiring, provider mount points, async-mode choice, and explicit follow-through links to operator, testing, and compatibility guidance. Update `inbound-install.md` so it mirrors the same sequence and terminology, explicitly points back to the README as the canonical lane, and removes any wording that reads like a competing setup authority, installer flow, or contradictory provider/async story.</action>
  <acceptance_criteria>
    - `mailglass_inbound/README.md` contains one ordered adoption path covering dependency pinning, `body_reader`, router/provider wiring, async mode, and links to `inbound-operator.md`, `inbound-testing.md`, and `guides/compatibility-and-deprecations.md`.
    - `mailglass_inbound/docs/inbound-install.md` explicitly frames itself as deeper setup detail for the README path instead of a second canonical route.
    - Both docs use the same package-line pins and the same `MailglassInbound.Ingress.CachingBodyReader`, `MailglassInbound.Router`, and `MailglassInbound.Mailbox` vocabulary.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The README owns the inbound adoption story and the install guide deepens that story without creating contradictory setup authority.</done>
</task>

<task type="auto">
  <name>Task 2: Route inbound compatibility and deprecation guidance through the active guide topology</name>
  <read_first>
    guides/compatibility-and-deprecations.md
    mailglass_inbound/docs/api_stability.md
    mailglass_inbound/README.md
    .planning/METHODOLOGY.md
    .planning/phases/65-compatibility-docs-and-dx-lock/65-CONTEXT.md
    .planning/phases/63-inbound-contract-inventory-reconciliation/63-CONTEXT.md
  </read_first>
  <files>guides/compatibility-and-deprecations.md, mailglass_inbound/README.md</files>
  <action>Implement D-03, D-04, and D-05 by keeping `guides/compatibility-and-deprecations.md` as the active repo-root compatibility reference and not creating `docs/compatibility-and-deprecations.md`, because that repo-root file is absent from the live docs topology. Add an inbound-focused subsection that routes every guarantee claim to `mailglass_inbound/docs/api_stability.md`, states that inventoried stable inbound surfaces require a deprecation bridge or major-version change before semantic breakage, and states that internal or deferred surfaces may change without deprecation even when reachable, documented for troubleshooting, or mentioned by tests. Include the deprecation-DX inventory for the stable surfaces touched in this phase with concrete columns for surface, bridge or replacement, warning or migration channel, `--warnings-as-errors` impact, support-until horizon, and proof artifact.</action>
  <acceptance_criteria>
    - `guides/compatibility-and-deprecations.md` contains a `mailglass_inbound` compatibility subsection that links to `mailglass_inbound/docs/api_stability.md`.
    - The inbound compatibility subsection explicitly states that stable inventoried surfaces require a deprecation bridge or major-version change, while internal and deferred surfaces may change without deprecation even when reachable or documented.
    - The guide contains a deprecation-DX inventory covering surface, bridge or replacement, warning/migration channel, `--warnings-as-errors` impact, support-until horizon, and proof artifact.
    - No new repo-root `docs/compatibility-and-deprecations.md` file is created.
  </acceptance_criteria>
  <verify>
    <automated>mix mailglass.docs.check</automated>
  </verify>
  <done>The repo has one active compatibility story for inbound adoption, and it routes all stable-surface guarantees back to the canonical inbound inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/guide prose -> adopter setup behavior | Adopters will copy setup steps and compatibility rules directly from these docs. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-65-01 | Repudiation | high | `mailglass_inbound/README.md` + `mailglass_inbound/docs/inbound-install.md` | mitigate | yes | Blocking acceptance: README remains the sole canonical adoption lane, install guide explicitly subordinates itself to that lane, and `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` passes. |
| T-65-02 | Tampering | high | `guides/compatibility-and-deprecations.md` | mitigate | yes | Blocking acceptance: compatibility guidance routes to `mailglass_inbound/docs/api_stability.md`, states the stable-vs-internal/deferred rule exactly, keeps the deprecation-DX inventory explicit, and `mix mailglass.docs.check` passes. |
| T-65-03 | Spoofing | medium | repo docs topology | mitigate | no | Keep `guides/compatibility-and-deprecations.md` as the only repo-root compatibility doc in this phase and do not create a second repo-root `docs/compatibility-and-deprecations.md` authority. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors`
- `mix mailglass.docs.check`
</verification>

<success_criteria>
- `mailglass_inbound/README.md` is the one canonical inbound adoption lane and deeper setup docs no longer compete with it.
- The inbound compatibility story is explicit, narrow, and routed through the canonical stability inventory without creating a second repo-root compatibility document.
</success_criteria>

<output>
Create `.planning/phases/65-compatibility-docs-and-dx-lock/65-01-SUMMARY.md` when done
</output>
