---
phase: 77-motion-and-microinteraction-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - mailglass_admin/e2e/operator.spec.js
autonomous: true
requirements:
  - MOTION-01
  - MOTION-02
must_haves:
  truths:
    - "operator.spec.js includes a test that clicks a delivery row, reads delivery_id from URL, and asserts #delivery-detail-<id> is visible in the DOM"
    - "operator.spec.js includes a second-row click test verifying the id changes and the old element is gone (element replaced, not patched)"
    - "operator.spec.js includes a reduced-motion test using page.emulateMedia({ reducedMotion: 'reduce' }) confirming the detail element is still visible (not stuck at opacity:0)"
    - "operator.spec.js includes a SKIPPED inbound id-presence test with a comment explaining the seed dependency (Phase 78 to resolve)"
    - "npm run test:operator-browser in mailglass_admin/ passes all new and existing tests"
  artifacts:
    - path: "mailglass_admin/e2e/operator.spec.js"
      provides: "DOM id-presence and reduced-motion Playwright regression tests"
      contains: "delivery-detail-"
  key_links:
    - from: "operator.spec.js delivery id-presence test"
      to: "operator_live.ex:442 id=delivery-detail-<uuid>"
      via: "page.locator('#delivery-detail-' + deliveryId)"
      pattern: "delivery-detail-"
    - from: "operator.spec.js reduced-motion test"
      to: "app.css:274-282 global prefers-reduced-motion block"
      via: "page.emulateMedia({ reducedMotion: 'reduce' })"
      pattern: "emulateMedia"
---

<objective>
Extend operator.spec.js with three new tests (D-07): (1) delivery detail pane id-presence and element-replacement assertion (MOTION-01 regression gate), (2) reduced-motion suppression test (MOTION-02 regression gate), and (3) a skipped/commented inbound id-presence test documenting the seed dependency. These Playwright DOM-layer tests are the Nyquist gate for MOTION-01 — ExUnit substring tests cannot detect the id-attribute regression (the heroicons-inline lesson).

**Inbound e2e seed disposition (explicit):** The inbound HEEx fix ships in Plan 01. Its e2e id-presence assertion is SKIPPED in this plan because OperatorFixtures.seed_browser_scenario!() seeds zero inbound records — a navigable inbound row does not exist in the browser scenario. The skip carries a comment directing Phase 78 to extend the seed. Once Phase 78 seeds an inbound record, the skip comment and test.skip can be converted to a live test.

Purpose: Satisfy MOTION-01/MOTION-02 at the DOM/e2e verification layer. Plans 01 and 02 add the code and grep gate; Plan 03 adds the Playwright regression guard that catches the silent id-regression ExUnit cannot see.
Output: ~50 lines appended inside the existing test.describe block in operator.spec.js.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-motion-and-microinteraction-polish/77-CONTEXT.md
@.planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md
@.planning/phases/77-motion-and-microinteraction-polish/77-PATTERNS.md
@.planning/phases/77-motion-and-microinteraction-polish/77-01-SUMMARY.md

<interfaces>
<!-- Existing helpers in operator.spec.js (lines 1-30): -->
<!-- const tenantId = "browser-tenant"; -->
<!-- const selectedRecipient = "browser-selected@example.com"; -->
<!-- function deliveryRow(page, index) { return page.getByTestId("operator-delivery-row").nth(index); } -->
<!-- async function openOperator(page) { ... resets, logs in, navigates to deliveries view ... } -->

<!-- Existing test anatomy (lines 32-71): -->
<!-- test.describe("operator browser gate", () => { -->
<!--   test("desktop keeps list/detail in two panes...", async ({ page }) => { -->
<!--     await page.setViewportSize({ width: 1280, height: 900 }); -->
<!--     await openOperator(page); -->
<!--     await deliveryRow(page, 0).click(); -->
<!--     await expect(page).toHaveURL(/delivery_id=/); -->
<!--   }); -->
<!--   ... -->
<!-- }); // line 183 closes the describe block -->

<!-- Id-presence assertion pattern (from PATTERNS.md / RESEARCH.md Validation Architecture): -->
<!-- const deliveryId = new URL(page.url()).searchParams.get("delivery_id"); -->
<!-- await expect(page.locator(`#delivery-detail-${deliveryId}`)).toBeVisible(); -->
<!-- page.emulateMedia must be called BEFORE page.goto to take effect on initial page load. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delivery id-presence + reduced-motion + inbound-skipped e2e tests (D-07 / GAP-19)</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js (full file — lines 1-183; understand the test.describe block structure, existing helpers openOperator/deliveryRow, and the closing }) at line 183 which is where new tests insert before the final closing paren)
    - .planning/phases/77-motion-and-microinteraction-polish/77-PATTERNS.md (Pattern: Playwright Test Structure section; exact test code for id-presence test 1, reduced-motion test 2, and inbound skipped test 3 — including the emulateMedia-before-navigation ordering note)
    - .planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md Anchor 7 (id-presence assertion design: deliveryId from new URL(page.url()).searchParams.get("delivery_id"); inbound seed confirmation: seed_browser_scenario!() has zero inbound records)
    - mailglass_admin/test/support/operator_fixtures.ex:1-50 (confirm seed_browser_scenario! seeds only delivery rows; look for any inbound/InboundRecord insert — there are none, confirming the skip decision)
  </read_first>
  <action>Append three new test blocks inside the existing test.describe("operator browser gate", ...) block in operator.spec.js, before the closing `});` at the end of the file (line 183).

Test 1 — delivery id-presence + element-replacement (MOTION-01 regression gate):
Name: "delivery detail pane carries record-keyed id for animation re-fire"
Steps: setViewportSize(1280x900), openOperator(page), click deliveryRow(0), read deliveryId from new URL(page.url()).searchParams.get("delivery_id"), expect deliveryId toBeTruthy, assert page.locator(`#delivery-detail-${deliveryId}`) toBeVisible. Then click deliveryRow(1), read deliveryId2, expect deliveryId2 not.toEqual(deliveryId), assert #delivery-detail-${deliveryId2} toBeVisible AND #delivery-detail-${deliveryId} toHaveCount(0) (old element gone = element replaced not patched).

Test 2 — reduced-motion suppression (MOTION-02 regression gate):
Name: "motion-reveal is suppressed under prefers-reduced-motion"
CRITICAL: Call page.emulateMedia({ reducedMotion: "reduce" }) BEFORE setViewportSize and openOperator (emulateMedia must precede page.goto which is called inside openOperator).
Steps: emulateMedia first, setViewportSize(1280x900), openOperator(page), click deliveryRow(0), read deliveryId, assert #delivery-detail-${deliveryId} toBeVisible (under reduced-motion the CSS sets animation-duration: 0.01ms !important so element is immediately visible, not stuck at opacity:0).

Test 3 — inbound id-presence SKIPPED (seed dependency):
Use test.skip("inbound detail pane carries record-keyed id [SKIP: requires inbound seed in browser scenario]", ...) with a comment body explaining: OperatorFixtures.seed_browser_scenario!() seeds zero inbound records; the inbound detail pane HEEx fix (id={"inbound-detail-#{@detail.record.id}"}) ships in Plan 01 but its e2e assertion requires a navigable inbound row in the browser scenario; Phase 78 seed expansion is the gate to enable this test — remove the skip when Phase 78 seeds at least one inbound record.

Do not modify any existing tests. All three additions go inside the describe block before its closing `});`.</action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && npm run test:operator-browser 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "delivery detail pane carries record-keyed id" mailglass_admin/e2e/operator.spec.js returns 1
    - grep -c "motion-reveal is suppressed under prefers-reduced-motion" mailglass_admin/e2e/operator.spec.js returns 1
    - grep -c "inbound detail pane carries record-keyed id" mailglass_admin/e2e/operator.spec.js returns 1 (the skipped test)
    - grep -c "emulateMedia" mailglass_admin/e2e/operator.spec.js returns 1 (in the reduced-motion test)
    - grep -c "delivery-detail-" mailglass_admin/e2e/operator.spec.js returns >= 2 (id-presence assertions)
    - npm run test:operator-browser in mailglass_admin/ passes: the two new live tests pass, the skipped test shows as skipped (not failed), all existing tests remain green
    - The emulateMedia call in test 2 appears before the setViewportSize and openOperator calls (ordering critical — emulateMedia must precede goto)
  </acceptance_criteria>
  <done>npm run test:operator-browser in mailglass_admin/ is green: 2 new tests pass (id-presence + reduced-motion), 1 test skipped (inbound seed dependency), all pre-existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → OperatorBrowserServer | E2e tests connect to a local test server; no production data, no external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Information Disclosure | deliveryId extracted from URL in e2e test | accept | UUID is already in the URL; this is test-only code running against a local seed server with synthetic data. No real user data. |
| T-77-03-02 | Spoofing | test.skip on inbound test could mask a regression | accept | The skip is explicit and documented; the inbound HEEx fix ships regardless. Phase 78 enables the assertion. |
| T-77-03-SC | Tampering | No package installs in this plan | accept | No new npm/mix deps. Playwright harness already installed. |

No new untrusted input, no new auth surface, no PII. This plan adds e2e test cases to an existing Playwright suite.
</threat_model>

<verification>
After task:
1. `cd mailglass_admin && npm run test:operator-browser` — all tests pass; 2 new tests green, 1 skipped, no existing regressions
2. `grep -c "delivery-detail-" mailglass_admin/e2e/operator.spec.js` — returns >= 2
3. `grep -n "emulateMedia" mailglass_admin/e2e/operator.spec.js` — shows 1 match; confirm it appears before the openOperator call in the same test block
4. `grep -c "test.skip" mailglass_admin/e2e/operator.spec.js` — returns 1 (the inbound skip)
</verification>

<success_criteria>
- operator.spec.js contains the delivery id-presence test (asserts #delivery-detail-<uuid> visible + old element gone after switching rows)
- operator.spec.js contains the reduced-motion test (emulateMedia before navigation; asserts detail element visible under reduce)
- operator.spec.js contains the skipped inbound test with Phase 78 seed dependency comment
- npm run test:operator-browser: new live tests pass, inbound test skipped, no regressions
</success_criteria>

<output>
Create `.planning/phases/77-motion-and-microinteraction-polish/77-03-SUMMARY.md` when done
</output>
