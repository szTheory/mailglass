---
phase: 77-motion-and-microinteraction-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "The delivery detail pane div carries id={\"delivery-detail-#{@selected_delivery.id}\"} at operator_live.ex:442"
    - "The inbound detail pane div carries id={\"inbound-detail-#{@detail.record.id}\"} at inbound_live.ex:341"
    - "Selecting a different delivery causes LiveView to replace (not patch) the motion-reveal element, firing the entrance animation exactly once per selection"
    - "No CSS changes, no new Tailwind classes, no new deps introduced"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Delivery detail pane with record-keyed id on motion-reveal div"
      contains: "delivery-detail-#{@selected_delivery.id}"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Inbound detail pane with record-keyed id on motion-reveal div"
      contains: "inbound-detail-#{@detail.record.id}"
  key_links:
    - from: "operator_live.ex:442"
      to: "mg-reveal keyframe animation"
      via: "LiveView element replace on id change"
      pattern: "delivery-detail-#{@selected_delivery.id}"
    - from: "inbound_live.ex:341"
      to: "mg-reveal keyframe animation"
      via: "LiveView element replace on id change"
      pattern: "inbound-detail-#{@detail.record.id}"
---

<objective>
Fix the motion-reveal re-fire bug (GAP-19, sev 3) by adding record-keyed id attributes to the two bare motion-reveal divs in operator_live.ex and inbound_live.ex. Without an id, LiveView patches the element in-place on delivery/inbound selection change — no insertion event fires and the mg-reveal entrance animation never re-triggers. With a record-keyed id, each new selection causes LiveView to replace the element, firing the animation exactly once per selection.

Purpose: Satisfy MOTION-01 — entrance animations fire on mount (record replacement), not on every LiveView patch. This is the literal GAP-19 fix, with the inbound twin folded in under the same citation.
Output: Two one-line HEEx attribute additions; no CSS changes, no new classes, no deps.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md
@.planning/phases/77-motion-and-microinteraction-polish/77-CONTEXT.md
@.planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md
@.planning/phases/77-motion-and-microinteraction-polish/77-PATTERNS.md

<interfaces>
<!-- Analog pattern: preview/tabs.ex:84 — id-keyed motion-tab-swap -->
<!-- From mailglass_admin/lib/mailglass_admin/preview/tabs.ex:84 -->
<!-- <div id={"preview-tab-" <> Atom.to_string(@active_tab)} class="motion-tab-swap"> -->

<!-- Current operator_live.ex:441-445 (BARE — no id): -->
<!-- <% true -> %> -->
<!--   <div class="motion-reveal space-y-4"> -->
<!--     <DetailHeader.detail_header delivery={@selected_delivery} ... /> -->

<!-- Current inbound_live.ex:340-344 (BARE — no id): -->
<!-- <% true -> %> -->
<!--   <div class="motion-reveal space-y-4"> -->
<!--     <DetailHeader.detail_header detail={@detail} /> -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add record-keyed id to operator delivery detail pane (D-01 / GAP-19)</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex:438-462 (the cond-do true branch containing the bare motion-reveal div at line 442; read to confirm context and surrounding assigns)
    - mailglass_admin/lib/mailglass_admin/preview/tabs.ex:82-95 (the proven analog: id={"preview-tab-" &lt;&gt; Atom.to_string(@active_tab)} on motion-tab-swap; mirror this pattern)
  </read_first>
  <behavior>
    - operator_live.ex:442 div goes from class="motion-reveal space-y-4" to id={"delivery-detail-#{@selected_delivery.id}"} class="motion-reveal space-y-4"
    - @selected_delivery is the assigns map key confirmed non-nil in the true -> cond branch
    - No other lines on operator_live.ex change
  </behavior>
  <action>At operator_live.ex:442, add the id attribute per D-01. The existing line is `&lt;div class="motion-reveal space-y-4"&gt;`. Change it to `&lt;div id={"delivery-detail-#{@selected_delivery.id}"} class="motion-reveal space-y-4"&gt;`. This is a one-attribute insertion on a single line — do not alter indentation, surrounding tags, or any other content. Mechanism: LiveView's diff algorithm detects the changed id on each new delivery selection, performs element replacement (remove-old, insert-new) rather than in-place patch, and the new element's insertion fires the mg-reveal @keyframes exactly once. Cites GAP-19 (sev 3) per anti-churn contract.</action>
  <verify>
    <automated>grep -n 'id={"delivery-detail-#{@selected_delivery.id}"}' /Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/operator_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - operator_live.ex:442 contains `id={"delivery-detail-#{@selected_delivery.id}"}` on the motion-reveal div (confirmed by grep output showing exactly one match at line 442)
    - The attribute is present alongside class="motion-reveal space-y-4" on the same div tag
    - No other lines in operator_live.ex are modified (verify with `git diff --stat mailglass_admin/lib/mailglass_admin/operator_live.ex` showing +1/-1 or +1/-0 lines)
  </acceptance_criteria>
  <done>operator_live.ex line 442 carries id={"delivery-detail-#{@selected_delivery.id}"} on the motion-reveal div; mix compile exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add record-keyed id to inbound detail pane (D-02 corrected / GAP-19 twin)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex:337-358 (the cond-do true branch containing the bare motion-reveal div at line 341; verify @detail is the binding — not @selected_record — and that @detail.record.id is accessible)
    - mailglass_admin/lib/mailglass_admin/preview/tabs.ex:82-95 (same analog pattern as Task 1)
    - .planning/phases/77-motion-and-microinteraction-polish/77-RESEARCH.md Anchor 2 (the correction: use @detail.record.id, NOT @selected_record.id; rationale: @detail.record is an %InboundRecord{} struct guaranteed non-nil in the true -> branch; @selected_record could theoretically be nil on URL-injected inbound_id mismatch, causing nil.id crash)
  </read_first>
  <behavior>
    - inbound_live.ex:341 div goes from class="motion-reveal space-y-4" to id={"inbound-detail-#{@detail.record.id}"} class="motion-reveal space-y-4"
    - @detail is the binding rendered at line 341; @detail.record is the %InboundRecord{} struct
    - @detail.record.id is non-nil in the true -> branch (the true -> branch activates when @detail is non-nil per the cond condition)
    - CRITICAL: do NOT use @selected_record.id — @detail.record.id is the correct expression per RESEARCH.md Anchor 2 correction
    - No other lines in inbound_live.ex change
  </behavior>
  <action>At inbound_live.ex:341, add the id attribute per D-02 (corrected expression). The existing line is `&lt;div class="motion-reveal space-y-4"&gt;`. Change it to `&lt;div id={"inbound-detail-#{@detail.record.id}"} class="motion-reveal space-y-4"&gt;`. Use @detail.record.id, NOT @selected_record.id — see RESEARCH.md Anchor 2 for the correction rationale (nil-safety in URL-injected edge case). Same mechanism as Task 1: changed id causes LiveView to replace the element on inbound record selection, firing mg-reveal exactly once. Folded under GAP-19 citation per anti-churn contract.</action>
  <verify>
    <automated>grep -n 'id={"inbound-detail-#{@detail.record.id}"}' /Users/jon/projects/mailglass/mailglass_admin/lib/mailglass_admin/inbound_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - inbound_live.ex:341 contains `id={"inbound-detail-#{@detail.record.id}"}` on the motion-reveal div (confirmed by grep output showing exactly one match at line 341)
    - The expression uses @detail.record.id, not @selected_record.id
    - No other lines in inbound_live.ex are modified
    - cd mailglass_admin && mix compile --no-optional-deps --warnings-as-errors exits 0 (both files compile cleanly)
  </acceptance_criteria>
  <done>inbound_live.ex line 341 carries id={"inbound-detail-#{@detail.record.id}"} on the motion-reveal div; mix compile --no-optional-deps --warnings-as-errors exits 0 in mailglass_admin/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HEEx template render | id attribute value is interpolated from @selected_delivery.id / @detail.record.id — both are internal UUIDs already present in delivery_id / inbound_id URL params and already rendered in the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-01 | Information Disclosure | id={"delivery-detail-#{@selected_delivery.id}"} in HTML | accept | UUID is already present in the URL param (?delivery_id=<uuid>) and rendered in the page DOM; adding it to a div id exposes no new information. No PII. |
| T-77-01-02 | Information Disclosure | id={"inbound-detail-#{@detail.record.id}"} in HTML | accept | UUID is already present in the URL param (?inbound_id=<uuid>) and rendered in the page DOM; no PII. |
| T-77-01-SC | Tampering | No package installs in this plan | accept | No npm/mix deps added; no package legitimacy audit needed. |

No new untrusted input, no new auth surface, no PII in telemetry, no external I/O. This plan adds two HTML id attributes derived from internal record UUIDs. Attack surface is minimal/none.
</threat_model>

<verification>
After both tasks:
1. `grep -n 'id={"delivery-detail-#{@selected_delivery.id}"}' mailglass_admin/lib/mailglass_admin/operator_live.ex` — shows exactly one match at line 442
2. `grep -n 'id={"inbound-detail-#{@detail.record.id}"}' mailglass_admin/lib/mailglass_admin/inbound_live.ex` — shows exactly one match at line 341
3. `cd mailglass_admin && mix compile --no-optional-deps --warnings-as-errors` — exits 0
4. `cd mailglass_admin && mix test --seed 0 --warnings-as-errors --exclude flaky` — 187 tests, 1 pre-existing voice_test failure, no regressions
</verification>

<success_criteria>
- operator_live.ex:442 carries id={"delivery-detail-#{@selected_delivery.id}"} on the motion-reveal div
- inbound_live.ex:341 carries id={"inbound-detail-#{@detail.record.id}"} on the motion-reveal div
- mix compile --no-optional-deps --warnings-as-errors exits 0
- mix test --seed 0 --exclude flaky: 187 tests, 1 pre-existing voice_test failure only
- git diff --stat shows exactly +1/-1 (or +1/-0) in each of the two modified files
</success_criteria>

<output>
Create `.planning/phases/77-motion-and-microinteraction-polish/77-01-SUMMARY.md` when done
</output>
