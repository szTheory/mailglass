---
phase: 98-operator-deliveries-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/RATCHET-GAP-REGISTER.md
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
autonomous: true
requirements: [A11Y-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Four new operator GAP rows (grid-percentages, tracking-cleanup, empty-state-signal, seed-state-coverage) exist at sev>=3 with run_id, giving every downstream Phase 98 build task a valid anti-churn citation anchor"
    - "status_badge accepts :suppressed without an attr-validation warning and renders it as badge-outline/Unknown via the existing fallback"
    - "suppression_card.body_copy/1 returns a fallback string for a novel-shape suppression map instead of raising FunctionClauseError"
    - "The open_support_exemplar and confirm_replay handlers do not raise when selected_delivery is nil"
  artifacts:
    - path: ".planning/RATCHET-GAP-REGISTER.md"
      provides: "Four new sev>=3 operator GAP rows (GAP-06..GAP-09) seeded with run_id 2026-06-14-phase-98"
      contains: "GAP-06"
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "status_badge attr :status values list including :suppressed"
      contains: ":suppressed"
    - path: "mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex"
      provides: "body_copy/1 catch-all + headline/1 catch-all"
      contains: "defp body_copy(_)"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "CR-01/02/03 unit coverage"
      contains: "suppressed"
  key_links:
    - from: "components.ex status_badge attr values"
      to: "status_class/icon/label fallback clauses"
      via: ":suppressed falls through to the _status fallback (STATE-LD-05)"
      pattern: ":suppressed"
---

<objective>
Establish the anti-churn citation anchors for Phase 98 and fold in the three CR
nil-guard tech-debt fixes (CR-01/02/03) using the minimal in-place idiom already
present in each file. This is the foundation wave: it seeds the GAP register rows
that every subsequent Phase 98 build task must cite (sev>=3 anti-churn contract),
and it makes the operator surface defensively correct against the novel-shape /
nil states the later seed work (Plan 04) will reach.

Purpose: the RATCHET register currently has exactly ONE operator sev>=3 row
(GAP-01, already fixed in Phase 97), so Plans 02/03/04 would have no valid
citation. CR-01/02/03 must land before the seed adds a :suppressed row and a
novel-shape suppression that would otherwise raise.

Output: 4 new sev>=3 GAP rows; CR-01 (body_copy + headline catch-all); CR-02
(nil-safe selected_delivery reads in two handlers); CR-03 (:suppressed added to
the status_badge attr values list); ExUnit unit coverage for all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/98-operator-deliveries-surface/98-RESEARCH.md
@.planning/phases/98-operator-deliveries-surface/98-UI-SPEC.md
@.planning/RATCHET-GAP-REGISTER.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

components.ex status_badge attr (components.ex:158-183) — the values list ends at
:reconciled on line 181; :suppressed is ABSENT. Fallback clauses already render any
phantom atom: status_class/1 → "badge-outline" (line 230), status_icon/1 →
"hero-question-mark-circle" (line 255), status_label/1 → "Unknown" (line 280).

suppression_card.ex defp clauses (lines 51-57):
  defp headline(nil) / headline(%{reversibility: :immutable}) / headline(%{reversibility: :reversible})
  defp body_copy(%{reversibility: :immutable}) / body_copy(%{reversibility: :reversible}) / body_copy(%{reversibility_copy: copy})
  NEITHER headline/1 NOR body_copy/1 has a true catch-all → novel-shape map raises.
  label/1 (line 59-66) DOES have a catch-all (the pattern to mirror).

operator_live.ex CR-02 sites:
  :153 open_support_exemplar — `blank_to_nil(params["delivery_id"]) || socket.assigns.selected_delivery.id`
  :240-241 confirm_replay {:error, reason} branch — `socket.assigns.selected_delivery.id`
  Existing nil-safe idiom in this module: blank_to_nil/1 (line 819-820), guarded `with`/`case`.

COPY-LD-14 fallback string (UI-SPEC Copywriting Contract): the suppression card
zero/fallback body is "No active Suppression for this Delivery."
</interfaces>

<source_audit>
GOAL coverage: A11Y-01 (no-raise robustness + correct ARIA/badge fallback) anchored here.
REQ coverage: A11Y-01.
RESEARCH coverage: Pitfall 5 (headline/1 also lacks catch-all — A5); CR-02 idiom note;
  CR-03 attr-list correction (Code Examples); anti-churn sev>=3 contract (finding #3).
CONTEXT coverage: D-05 (CR-01/02/03 minimal in-place idiom).
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed four sev>=3 operator GAP rows in the RATCHET register</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    - .planning/RATCHET-GAP-REGISTER.md (Anti-Churn Contract, Column Schema, Severity Rubric, existing Gaps table)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (finding #3 — anti-churn; the four gaps this phase fixes)
  </read_first>
  <action>
    Append four new rows to the Gaps table in .planning/RATCHET-GAP-REGISTER.md, continuing
    the stable-ID sequence after GAP-05 (so GAP-06..GAP-09). Use surface=`deliveries`,
    status=`open`, run_id=`2026-06-14-phase-98`, first_seen_run=`2026-06-14-phase-98`,
    evidence PNG=`tmp/ui-audit/deliveries-768-light.png` (or `deliveries-390-light.png` for the
    390-specific rows). Rows:
    - GAP-06 | deliveries | operator_live.ex:397 | pillar=Spacing | sev=4 | fix sketch: master-detail
      grid uses lg:grid-cols-[minmax(22rem,28rem)_1fr] which violates IA-LD-03 (wrong percentages,
      banned lg: breakpoint); replace with md:grid-cols-[40%_60%] + the 1440 tier + 390px reveal-with-back.
    - GAP-07 | deliveries | operator_live.ex:404 | pillar=Type | sev=3 | fix sketch: deliveries-list card
      h2 and the in-pane component label spans carry arbitrary tracking-[0.08em] (IA-LD-04 / STATE-LD-13
      ban); replace with text-label uppercase font-bold text-secondary token.
    - GAP-08 | deliveries | operator/deliveries_list.ex:15 | pillar=Type | sev=3 | fix sketch: empty branch
      renders one generic copy; cannot distinguish filtered-empty (COPY-LD-01 + reset action) from
      truly-empty (COPY-LD-02); add filters_active? signal from operator_live.ex.
    - GAP-09 | deliveries | test/support/operator_fixtures.ex | pillar=Motion+A11y | sev=3 | fix sketch:
      single seed does not reach :suppressed / detail-error / filtered-empty / truly-empty states by URL;
      extend seed (timed hours_ago(7)+ to preserve row indices) + add per-state Playwright assertions.
    Reference the chosen severity in each row per the Severity Rubric. Do not renumber or modify
    GAP-01..GAP-05. Do not change the GAP-05 fixed status.
  </action>
  <acceptance_criteria>
    - `grep -c '^| GAP-0[6789] ' .planning/RATCHET-GAP-REGISTER.md` returns 4
    - `grep '2026-06-14-phase-98' .planning/RATCHET-GAP-REGISTER.md | wc -l` >= 4
    - GAP-06 row contains `sev | 4` semantics (severity column value 4); GAP-07/08/09 severity 3
    - GAP-01..GAP-05 rows are byte-unchanged (verify with `git diff` showing only appended lines)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^| GAP-0[6789] ' /Users/jon/projects/mailglass/.planning/RATCHET-GAP-REGISTER.md | grep -qx 4 && echo OK</automated>
  </verify>
  <done>Four new sev>=3 operator GAP rows (GAP-06..GAP-09) exist with run_id 2026-06-14-phase-98; existing rows untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-03 — add :suppressed to status_badge attr values list</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/components.ex (lines 158-183 attr list; lines 207-280 fallback clauses)
    - .planning/research/v1.11/SUMMARY.md (STATE-LD-05 — phantom-atom lock)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (Code Examples — CR-03 one-line correction; anti-patterns: do NOT add a non-fallback clause)
  </read_first>
  <behavior>
    - status_badge with status={:suppressed} renders without raising and without an attr-validation warning
    - the rendered markup contains the badge-outline class and the "Unknown" label (via the existing _status fallback)
    - NO new status_class(:suppressed)/status_icon(:suppressed)/status_label(:suppressed) clause is added (STATE-LD-05 lock)
  </behavior>
  <action>
    In components.ex, add the atom `:suppressed` to the `attr :status, :atom, values: [...]` list
    (the list currently ending at `:reconciled`). Cite A11Y-01 / CR-03 (D-05 tech-debt fold) and
    STATE-LD-05 as rationale — this is a CR nil-guard fold, not a GAP-cited build task, so no GAP-NN
    anchor is required. Do NOT add any non-fallback status_class/status_icon/status_label clause for
    :suppressed — the existing `_status` fallbacks at lines 230/255/280 already resolve it to
    badge-outline / hero-question-mark-circle / "Unknown" per STATE-LD-05's phantom-atom lock.
    One-line attr-list change only. Per D-05.
  </action>
  <acceptance_criteria>
    - `:suppressed` is present in the `attr :status` values list in components.ex
    - no `status_class(:suppressed)` / `status_icon(:suppressed)` / `status_label(:suppressed)` non-fallback clause exists
    - `mix compile --warnings-as-errors` shows no attr-validation warning for `status={:suppressed}`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && grep -A30 'attr :status, :atom,' lib/mailglass_admin/components.ex | grep -q ':suppressed' && ! grep -q 'status_class(:suppressed)' lib/mailglass_admin/components.ex && echo OK</automated>
  </verify>
  <done>:suppressed is in the attr values list; no explicit non-fallback :suppressed clause exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-01 — suppression_card body_copy/1 + headline/1 catch-all; CR-02 nil-safe handler reads; unit tests</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex (lines 51-57 headline/body_copy; line 59 label/1 catch-all pattern to mirror)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (lines 151-166 open_support_exemplar; lines 235-243 confirm_replay error branch; line 819 blank_to_nil)
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (existing test idiom)
    - .planning/research/v1.11/SUMMARY.md (COPY-LD-14 fallback string)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (CR-01 / CR-02 idiom; Pitfall 5 headline catch-all — A5)
  </read_first>
  <behavior>
    - body_copy(%{}) returns the COPY-LD-14 fallback string "No active Suppression for this Delivery." (no FunctionClauseError)
    - headline(%{}) returns a safe fallback string (no FunctionClauseError) — extends CR-01 to headline/1 per Research A5
    - rendering operator_live with selected_delivery: nil through the open_support_exemplar and confirm_replay error paths does not raise (the bare `.id` reads are made nil-safe)
  </behavior>
  <action>
    CR-01: in suppression_card.ex add `defp body_copy(_), do: "No active Suppression for this Delivery."`
    (COPY-LD-14 fallback) as the final clause after line 57, mirroring the existing `label/1` catch-all.
    ALSO add a `defp headline(_), do: "No suppression"` final clause (Research A5 / Pitfall 5 — headline/1
    likewise lacks a true catch-all and a novel-shape seed row would raise there). Minimal-idiom only.

    CR-02: in operator_live.ex replace the two bare `socket.assigns.selected_delivery.id` reads with a
    nil-safe read using `get_in(socket.assigns, [Access.key(:selected_delivery), Access.key(:id)])`:
    (1) line 153 in open_support_exemplar — `blank_to_nil(params["delivery_id"]) || get_in(...)`;
    (2) line ~240 in the confirm_replay `{:error, reason}` branch — `assign_delivery_state(..., get_in(...))`.
    Do NOT add a new branch to the happy path or change push_patch/flash control flow — the specs assert
    that flow (per D-05 / CR-02 idiom).

    Tests: in operator_live_test.exs add unit coverage — render SuppressionCard with
    suppression_state: %{} and assert it contains the fallback body string and does not raise; assert
    status_badge with status={:suppressed} renders "badge-outline" + "Unknown"; exercise the two handlers
    (or a focused helper) with selected_delivery nil to prove no raise. Cite GAP-09 (CR robustness for the
    seed states) as the anti-churn anchor. Place these CR-01/02/03 unit tests in a dedicated
    `describe "CR-01/02/03 nil-guards"` block so the Plan 02/03 Task 3 test additions append to distinct
    describe-block regions (no overlap with this plan's region).
  </action>
  <acceptance_criteria>
    - suppression_card.ex contains `defp body_copy(_)` and `defp headline(_)` final clauses
    - operator_live.ex contains `get_in(socket.assigns, [Access.key(:selected_delivery)` (>=2 occurrences) and NO bare `socket.assigns.selected_delivery.id`
    - new tests assert: body_copy fallback present; status_badge(:suppressed) → badge-outline + Unknown; nil selected_delivery does not raise
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && grep -q 'defp body_copy(_)' lib/mailglass_admin/operator/suppression_card.ex && grep -q 'defp headline(_)' lib/mailglass_admin/operator/suppression_card.ex && ! grep -q 'socket.assigns.selected_delivery.id' lib/mailglass_admin/operator_live.ex && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>CR-01/02 fixed with minimal idiom; ExUnit unit tests for CR-01/02/03 pass; no bare nil-deref reads remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator URL params → LiveView assigns | `?delivery_id=`, `?status=`, `?tenant_id=` cross from untrusted query string into render/handlers |

This plan adds NO new attack surface: it only hardens existing code paths against
nil/novel-shape inputs and seeds documentation rows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Denial of Service | suppression_card.body_copy/1, headline/1 | mitigate | Add catch-all clauses so a novel-shape suppression map cannot crash the LiveView (CR-01 / Research A5) |
| T-98-02 | Denial of Service | operator_live handlers nil-deref | mitigate | Nil-safe `get_in` reads so a missing selected_delivery cannot raise FunctionClauseError (CR-02) |
| T-98-03 | Tampering | status_badge attr validation | accept | `:suppressed` is added to the allowlist; phantom atoms still fall through to the neutral fallback — no new state introduced (STATE-LD-05) |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH Package Legitimacy Audit = N/A (zero new deps) |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` green
- `bash mailglass_admin/scripts/check-conformance.sh` green (no markup added)
- GAP register has GAP-06..GAP-09 at sev>=3 with run_id 2026-06-14-phase-98
</verification>

<success_criteria>
- Four sev>=3 operator GAP rows seeded; existing rows untouched
- CR-01/02/03 fixed using the minimal in-place idiom (no happy-path refactor, no non-fallback :suppressed clause)
- ExUnit unit coverage proves no-raise behavior for the novel-shape and nil cases
</success_criteria>

<output>
Create `.planning/phases/98-operator-deliveries-surface/98-01-SUMMARY.md` when done
</output>
</content>
</invoke>
