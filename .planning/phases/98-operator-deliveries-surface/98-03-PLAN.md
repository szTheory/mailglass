---
phase: 98-operator-deliveries-surface
plan: 03
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex
  - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
autonomous: true
requirements: [GROUP-01, A11Y-02]
nyquist_compliant: true

must_haves:
  truths:
    - "No tracking-[ remains anywhere in the operator in-pane components (suppression_card, support_cards, replay_modal) — the operator surface is genuinely token-clean"
    - "Every label/eyebrow span that previously used tracking-[0.08em] uses the IA-LD-04 label token text-label uppercase font-bold text-secondary"
    - "The suppression card heading copy is COPY-LD-14 (Suppression) and its present/absent bodies match the COPY-LD-14 lock"
    - "Flat-elevation token classes are preserved on these in-pane group containers (no shadow except the replay modal overlay tier)"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex"
      provides: "token labels (no tracking) + COPY-LD-14 copy"
      contains: "Suppression"
    - path: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      provides: "token dt labels (no tracking)"
      contains: "text-label"
    - path: "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
      provides: "token dt labels (no tracking)"
      contains: "text-label"
  key_links:
    - from: "in-pane component label spans"
      to: "IA-LD-04 label token"
      via: "replace tracking-[0.08em] with text-label uppercase font-bold text-secondary"
      pattern: "text-label uppercase font-bold text-secondary"
---

<objective>
Clean every remaining arbitrary tracking-[0.08em] site from the operator in-pane
components (suppression_card, support_cards, replay_modal) so the whole operator
surface is genuinely token-clean (D-03 "audit the WHOLE operator template"), and
apply the COPY-LD-14 suppression-card copy lock. These three components render
INSIDE the operator detail pane and were not touched by Phase 97's filters_form
cleanup, so they are operator-surface markup that Phase 98 owns.

Purpose: D-03 + IA-LD-04 + STATE-LD-13 ban arbitrary tracking; leaving these sites
means the "operator surface is token-clean" claim is false and the advisory
TRACK-GATE stays dirty. The global advisory-gate hard-fail FLIP is deferred to
Phase 99 (the script header assigns it there) — this plan proves cleanup with an
operator-scoped grep, not by flipping the global gate.

Output: tracking-free in-pane components + COPY-LD-14 suppression copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/98-operator-deliveries-surface/98-RESEARCH.md
@.planning/phases/98-operator-deliveries-surface/98-UI-SPEC.md
@.planning/RATCHET-GAP-REGISTER.md

<interfaces>
<!-- Verified tracking-[0.08em] sites across the operator in-pane components (grep). -->

suppression_card.ex: lines 24, 28, 32, 36 — `text-label font-bold uppercase tracking-[0.08em] text-secondary`
  on the Scope / Reason / Stream / Source <p> eyebrow labels.
  Heading at line 14: `<h3 ...>Suppression state</h3>` ← COPY-LD-14 changes to "Suppression".
  Present-absent bodies: headline/1 + body_copy/1 (Plan 01 already added catch-alls).
  Absent prose at line 43-45: "No active suppression entry matches this delivery." ← COPY-LD-14.

support_cards.ex: lines 67, 73, 113, 119 — `text-label font-bold uppercase tracking-[0.08em]`
  on the Webhook row ID / Provider event / Event ID / Provider event <dt> labels.

replay_modal.ex: lines 136, 140 — `text-label font-bold uppercase tracking-[0.08em]`
  on the Provider event / Delivery linkage <dt> labels.

IA-LD-04 label token (replacement): `text-label uppercase font-bold text-secondary`
  (where the original carried text-secondary). Where the original lacked text-secondary
  (support_cards/replay_modal <dt>), use `text-label uppercase font-bold` to preserve the
  exact prior color — do NOT add text-secondary if it was not there.

COPY-LD-14 (verbatim):
  heading "Suppression" (replaces "Suppression state")
  zero-state "No active Suppression for this Delivery." (replaces the absent prose)
  immutable body "This Suppression is permanent. Future sends to this address will be blocked."
  reversible body "This Suppression is reversible. Remove via the suppressions API or contact support."
  (Plan 01 set the body_copy/1 fallback to the zero-state string; this plan updates the
   immutable/reversible body_copy/1 returns + headline/1 if their copy drifts from COPY-LD-14.)
</interfaces>

<source_audit>
GOAL: GROUP-01 (token-clean group composition), A11Y-02 (token color discipline / contrast).
REQ: GROUP-01, A11Y-02.
RESEARCH: tracking-cleanup scope (Open Question 2 / A2 — Phase 98 cleans ALL operator-surface sites,
  defers the global gate flip to Phase 99); 11 verified sites (Conformance Gate Inventory).
CONTEXT: D-03 (drop tracking / apply label token), D-07 (flat elevation preserved).
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tracking-[0.08em] from suppression_card + apply COPY-LD-14 copy</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex (full file; lines 14, 24-36, 43-57)
    - .planning/research/v1.11/SUMMARY.md (IA-LD-04, STATE-LD-13, STATE-LD-15, COPY-LD-14)
    - .planning/phases/98-operator-deliveries-surface/98-UI-SPEC.md (Copywriting Contract — COPY-LD-14 rows)
  </read_first>
  <action>
    Cite GAP-07 (tracking cleanup) as the anti-churn anchor.
    Replace `text-label font-bold uppercase tracking-[0.08em] text-secondary` on lines 24/28/32/36 with
    the IA-LD-04 token `text-label uppercase font-bold text-secondary` (drop the arbitrary tracking).
    Apply COPY-LD-14 copy: change the h3 heading (line 14) from "Suppression state" to "Suppression";
    change the absent prose (line 43-45) to "No active Suppression for this Delivery."; update the
    body_copy/1 immutable clause to "This Suppression is permanent. Future sends to this address will be
    blocked." and the reversible clause to "This Suppression is reversible. Remove via the suppressions
    API or contact support." (the Plan 01 catch-all already returns the zero-state string). Use domain
    noun "Suppression"/"Delivery"; no "Email", no "Oops". Preserve the flat-elevation card classes
    (rounded-box border border-base-300 bg-base-200, no shadow).
  </action>
  <acceptance_criteria>
    - `grep -v '^#' lib/mailglass_admin/operator/suppression_card.ex | grep -c 'tracking-\['` returns 0
    - heading text is "Suppression" (not "Suppression state")
    - absent prose is "No active Suppression for this Delivery."
    - body_copy immutable/reversible clauses match COPY-LD-14 verbatim
    - `bash scripts/check-conformance.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && [ "$(grep -v '^#' lib/mailglass_admin/operator/suppression_card.ex | grep -c 'tracking-\[')" = "0" ] && grep -q '>Suppression<' lib/mailglass_admin/operator/suppression_card.ex && grep -q 'No active Suppression for this Delivery' lib/mailglass_admin/operator/suppression_card.ex && bash scripts/check-conformance.sh && echo OK</automated>
  </verify>
  <done>suppression_card is tracking-clean and carries COPY-LD-14 copy; conformance green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tracking-[0.08em] from support_cards + replay_modal dt labels</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/support_cards.ex, mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex (lines 67, 73, 113, 119)
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (lines 136, 140)
    - .planning/research/v1.11/SUMMARY.md (IA-LD-04, STATE-LD-14, STATE-LD-17)
    - .planning/phases/98-operator-deliveries-surface/98-UI-SPEC.md (Typography — label token lock)
  </read_first>
  <action>
    Cite GAP-07 (tracking cleanup) as the anti-churn anchor.
    In support_cards.ex (lines 67/73/113/119) and replay_modal.ex (lines 136/140), replace the dt class
    `text-label font-bold uppercase tracking-[0.08em]` with `text-label uppercase font-bold` (drop the
    arbitrary tracking; do NOT add text-secondary because these dt elements did not carry it — preserve
    the exact prior color). Letter-spacing is now token-owned (global h-rule / token). Do NOT touch the
    btn-sm/min-h-11 CTA classes (Phase 97 STATE-LD-14 already handled those — they are not tracking).
    Preserve flat-elevation classes; replay_modal keeps its shadow-overlay (modal tier exception per
    STATE-LD-17 — do not remove it).
  </action>
  <acceptance_criteria>
    - `grep -v '^#' lib/mailglass_admin/operator/support_cards.ex | grep -c 'tracking-\['` returns 0
    - `grep -v '^#' lib/mailglass_admin/operator/replay_modal.ex | grep -c 'tracking-\['` returns 0
    - the replaced dt classes contain `text-label uppercase font-bold`
    - replay_modal still contains `shadow-overlay` (modal exception preserved)
    - `bash scripts/check-conformance.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && [ "$(grep -v '^#' lib/mailglass_admin/operator/support_cards.ex | grep -c 'tracking-\[')" = "0" ] && [ "$(grep -v '^#' lib/mailglass_admin/operator/replay_modal.ex | grep -c 'tracking-\[')" = "0" ] && grep -q 'shadow-overlay' lib/mailglass_admin/operator/replay_modal.ex && bash scripts/check-conformance.sh && echo OK</automated>
  </verify>
  <done>support_cards + replay_modal dt labels are tracking-clean; replay modal overlay tier preserved; conformance green.</done>
</task>

<task type="auto">
  <name>Task 3: Operator-scoped tracking-clean assertion (ExUnit or scoped grep gate)</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (test idiom)
    - mailglass_admin/scripts/check-conformance-advisory.sh (the TRACK-GATE that stays advisory until Phase 99)
    - .planning/phases/98-operator-deliveries-surface/98-RESEARCH.md (finding #2 / Pitfall 1 — RATCHET-03 is advisory, do NOT claim it hard-fails; add your own assertion)
  </read_first>
  <action>
    Cite GAP-07 as the anti-churn anchor. Add an operator-scoped assertion proving 0 `tracking-[`
    in the operator surface markup, since the global advisory gate is NOT flipped in Phase 98 (deferred
    to Phase 99 per the script header). Implement as an ExUnit test in operator_live_test.exs that reads
    the operator source files (operator_live.ex + operator/suppression_card.ex + operator/support_cards.ex
    + operator/replay_modal.ex + operator/filters_form.ex + operator/deliveries_list.ex + operator/
    detail_header.ex), strips comment lines (reject lines starting with optional whitespace then '#'),
    and asserts none contain `tracking-[`. Do NOT flip check-conformance-advisory.sh to hard-fail (that
    is Phase 99's owned task). Do NOT assert "RATCHET-03 fails on tracking" (it does not — Pitfall 1).
  </action>
  <acceptance_criteria>
    - a new ExUnit test exists asserting 0 `tracking-[` across the operator source files (comment-stripped)
    - the test passes (proving the cleanup in Tasks 1-2 and Plan 02)
    - check-conformance-advisory.sh is NOT modified (still `exit 0`)
    - `mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` green
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && git diff --quiet scripts/check-conformance-advisory.sh && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>An operator-scoped tracking-clean ExUnit assertion guards regression; the global advisory gate is left for Phase 99.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is markup/copy-only; no inputs, routes, or data reads change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07 | Information Disclosure | suppression_card / replay_modal copy | accept | Copy is COPY-LD-14/13 locked text; no PII, no recipient/email rendered beyond existing mask_recipient paths |
| T-98-08 | Tampering | conformance advisory script | mitigate | Plan does NOT flip the advisory gate (Phase 99 owns it); regression guarded by an operator-scoped ExUnit assertion instead |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` green
- `bash mailglass_admin/scripts/check-conformance.sh` green
- 0 `tracking-[` across all operator source files (comment-stripped)
</verification>

<success_criteria>
- All operator in-pane components are tracking-clean with the IA-LD-04 label token applied
- COPY-LD-14 suppression copy in place
- Operator-scoped tracking assertion guards regression; global advisory flip deferred to Phase 99
</success_criteria>

<output>
Create `.planning/phases/98-operator-deliveries-surface/98-03-SUMMARY.md` when done
</output>
