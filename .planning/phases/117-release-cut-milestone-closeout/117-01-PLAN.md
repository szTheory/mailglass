---
phase: 117-release-cut-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass-publish-summary.json
  - .planning/publish/mailglass_admin-files.expected
  - .planning/publish/mailglass_admin-publish-summary.json
  - .planning/publish/mailglass_inbound-files.expected
  - .planning/publish/mailglass_inbound-publish-summary.json
  - .planning/milestones/v1.13-ROADMAP.md
  - .planning/milestones/v1.13-REQUIREMENTS.md
  - .planning/milestones/v1.13-MILESTONE-AUDIT.md
  - .planning/milestones/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/RETROSPECTIVE.md
  - .planning/STATE.md
autonomous: false
requirements:
  - REL-02
  - REL-03

must_haves:
  truths:
    - "All three Hex packages (mailglass 1.8.0, mailglass_admin 1.8.0, mailglass_inbound 1.4.x) are live on Hex (D-01)"
    - "mailglass_inbound carries the dep {mailglass, == 1.8.0} (D-05, D-13)"
    - "mix deps.get resolves all three packages from Hex in a fresh Phoenix app (REL-02)"
    - "post-publish-smoke passes (or only the known #32 hackney false-positive is red) (D-09)"
    - "Milestone v1.13 is audited (36/36 requirements status: passed), archived, and tagged v1.13 (D-08)"
  artifacts:
    - path: ".planning/publish/mailglass_admin-files.expected"
      provides: "Publish allowlist snapshot for mailglass_admin; must include the 4 new admin lib files (controllers/theme_controller.ex, mount_path.ex, mount_path_hook.ex, operator/tenants.ex)"
      contains: "operator/tenants.ex"
    - path: ".planning/milestones/v1.13-ROADMAP.md"
      provides: "Archived milestone roadmap"
    - path: ".planning/milestones/v1.13-REQUIREMENTS.md"
      provides: "Archived milestone requirements"
    - path: ".planning/milestones/v1.13-MILESTONE-AUDIT.md"
      provides: "Per-requirement pass/fail matrix with corrected scope counts (9 phases 109-117, 36 requirements)"
      contains: "status: passed"
  key_links:
    - from: ".planning/publish/mailglass_admin-files.expected"
      to: "prepublish-summary job in publish-hex.yml"
      via: "mix mailglass.publish.check"
      pattern: "mix mailglass.publish.check"
    - from: "PR #87 diff"
      to: "mailglass_inbound/mix.exs"
      via: "RP sed step"
      pattern: "== 1.8.0"
---

<objective>
Release cut and milestone closeout for mailglass v1.13 — Admin Design-System Stress Test & UX Uplift (v3).

Purpose: Move the v1.13 admin design-system uplift body (Phases 109–116, the 227-commit body already built and verified on local `main`) from `main` to Hex adopters as a linked-version MINOR release — mailglass 1.8.0 / mailglass_admin 1.8.0 (core+admin linked) with mailglass_inbound dragged via the D-13 exact-pin re-pin to `{:mailglass, "== 1.8.0"}` (per D-01, D-05) — then audit and archive the v1.13 milestone.

This phase writes essentially NO product/source code. It pushes the body, re-scores and verifies the open Release Please PR #87, gates on a maintainer go/no-go, confirms Hex publish, runs smoke, audits 36 requirements, and archives. Plan it like the Phase 108 release ceremony.

Output: All three packages live on Hex at target versions; inbound pinned to mailglass == 1.8.0; consumer + post-publish smoke green; milestone audit and git tag v1.13.

LIVE STATE (verified at plan time — use these facts):
- Release Please PR #87 ("chore: release main") is ALREADY OPEN. It currently scores a PATCH (1.7.1) ONLY because the v1.13 body is not on `origin/main`. origin/main is frozen at 766edf89; local main is 229 commits ahead. (D-02)
- The PUSH of the v1.13 body to origin/main is the load-bearing moment — it re-scores PR #87 from 1.7.1 to 1.8.0/1.8.0 from the now-visible feat commits (Phase 110 new public component API). Do NOT reach for `Release-As:`; only use it as a fallback if the natural re-score is somehow blocked. (D-02)
- Current versions: mailglass 1.7.0 / mailglass_admin 1.7.0 / mailglass_inbound 1.4.0.
- THE Wave-0 blocker: `.planning/publish/mailglass_admin-files.expected` is STALE. It is missing 4 admin lib files present on disk: `lib/mailglass_admin/controllers/theme_controller.ex`, `lib/mailglass_admin/mount_path.ex`, `lib/mailglass_admin/mount_path_hook.ex`, `lib/mailglass_admin/operator/tenants.ex`. `publish-hex.yml`'s `prepublish-summary` runs `mix mailglass.publish.check`, which FAILS CLOSED on a file delta. Must be regenerated and committed BEFORE PR #87 merges. (D-04)
- Reference baseline pin-bump is OUT OF SCOPE; baselines pin `~> 1.0`, which resolves any 1.x bump. Do not touch the 5-file baseline-coupling set. (D-07)
- Milestone audit scope is 9 phases (109–117) and 36 requirements (FND 5, PRIM 7, FORM 3, SHELL 6, DATA 5, GROUP 3, FLOW 4, RATCHET 5, REL 3). Do NOT trust gsd-sdk milestone.complete — it sweeps 999.x backlog dirs and inflates the matrix. (D-08)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/CLAUDE.md
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/117-release-cut-milestone-closeout/117-CONTEXT.md

Canonical references (READ before acting — CONTEXT.md mandates these):
- .planning/milestones/v1.12-phases/108-release-cut-milestone-closeout/108-01-PLAN.md (the 6-wave template this plan retargets to 1.8.0)
- .planning/milestones/v1.12-phases/108-release-cut-milestone-closeout/108-RESEARCH.md and 108-VALIDATION.md (supporting research + per-wave validation)
- MAINTAINING.md (Release Runbook 5 steps, Retract Decision Tree, required vs. advisory lanes, Publish Summary Snapshot Protocol)
- .github/workflows/release-please.yml (sed dep-pin sync step, auto-merge arming, cron self-heal)
- .github/workflows/publish-hex.yml (gate-ci-green, publish ordering core → inbound → admin, idempotency guards, fallback dispatch, prepublish-summary)
- .github/workflows/post-publish-smoke.yml (smoke job chain, inbound compatibility check)
- scripts/consumer_install_smoke.sh (DEP_MODE=hex consumer smoke — REL-02 Hex-resolution proof)
</context>

<artifacts_produced>
## Artifacts This Phase Produces

This is a release-ceremony + closeout phase. No new source symbols, components, or product docs. Artifacts:

- **Regenerated publish allowlist snapshots** (committed as `chore:`): `.planning/publish/mailglass-files.expected`, `mailglass_admin-files.expected`, `mailglass_inbound-files.expected`, and the three `*-publish-summary.json` files. The admin snapshot gains the 4 missing lib files.
- **Published Hex versions**: mailglass 1.8.0, mailglass_admin 1.8.0, mailglass_inbound 1.4.x (RP-determined; paired via the D-13 exact-pin drag).
- **Archive docs**: `.planning/milestones/v1.13-ROADMAP.md`, `.planning/milestones/v1.13-REQUIREMENTS.md`, `.planning/milestones/v1.13-MILESTONE-AUDIT.md`, and appended `.planning/RETROSPECTIVE.md`.
- **Updated planning docs**: `MILESTONES.md`, `PROJECT.md`, `ROADMAP.md`, `STATE.md`.
- **The `v1.13` git tag** (a milestone marker, distinct from the RP-created `*-v1.8.0` Hex release tags).
</artifacts_produced>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 0 — Pre-flight hygiene (reversible) — D-04
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1 (Wave 0): Verify green CI on local main + regenerate the stale publish allowlist (REL-02, D-04)</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-files.expected, .planning/publish/mailglass_admin-publish-summary.json, .planning/publish/mailglass_inbound-files.expected, .planning/publish/mailglass_inbound-publish-summary.json</files>

  <read_first>
    MAINTAINING.md — "Publish Summary Snapshot Protocol" section (mix mailglass.publish.check,
    regeneration steps, commit message guidance). Also read 108-VALIDATION.md Wave 0 Requirements.

    CRITICAL (CLAUDE.md "Things Not To Do"): Do NOT run `mix compile --no-optional-deps --force`
    on the shared _build during this step — it pollutes build artifacts and the /inbound route is
    compile-time-gated and order-sensitive. Use only `mix mailglass.publish.check --package <pkg>`.
    Per the demo_app swoosh-lock-drift gotcha, if any mix invocation re-bumps a reference mix.lock,
    `git checkout` that lock before committing — never commit transitive drift here.
  </read_first>

  <action>
    Step 0.1 — CI green confirmation on local main (REL-02 evidence):
    Run `gh run list --workflow ci.yml --branch main --limit 8` from repo root. Locate the last
    non-docs commit run (the last commit touching lib/, guides/, mailglass_admin/lib/, or
    mailglass_inbound/lib/ — docs(state): commits skip ci.yml entirely via path filters). Confirm
    all required lanes are green: "Support Contract Core (Elixir 1.18 / OTP 27)", "Support Contract
    Admin (Elixir 1.18 / OTP 27)", "Compile No Optional Deps (Elixir 1.18 / OTP 27)", "Trust Lane
    Repo Head (Elixir 1.18 / OTP 27)", "Installer Host Smoke". Note: this CI history is from local
    main; origin/main is frozen at 766edf89 so the freshest runs may predate the full v1.13 body —
    the authoritative green proof on the pushed body comes from the PR #87 checks in Task 2.

    Step 0.2 — Clean worktree check:
    Run `git status`. The allowlist snapshot captures untracked files, so the working tree must be
    clean before regeneration. The user may carry dirty .planning/ WIP — if so, stash only those
    paths, not source. Do not regenerate against a dirty source tree.

    Step 0.3 — Regenerate core allowlist:
    From repo root run `mix mailglass.publish.check --package mailglass`. This regenerates
    `.planning/publish/mailglass-publish-summary.json` and `.planning/publish/mailglass-files.expected`.
    Review the diff with `git diff .planning/publish/mailglass-files.expected`. v1.13 was admin+demo
    only, so the core allowlist is expected to be stable (exit 0, no or minimal delta).

    Step 0.4 — Regenerate admin allowlist (THE Wave-0 blocker, D-04):
    Run `mix mailglass.publish.check --package mailglass_admin`. The regenerated
    `.planning/publish/mailglass_admin-files.expected` MUST now include the 4 previously-missing
    admin lib files:
      - lib/mailglass_admin/controllers/theme_controller.ex
      - lib/mailglass_admin/mount_path.ex
      - lib/mailglass_admin/mount_path_hook.ex
      - lib/mailglass_admin/operator/tenants.ex
    Review with `git diff .planning/publish/mailglass_admin-files.expected` and confirm those 4
    lines are now present. The command must exit 0 (no unexpected/untracked files left over).

    Step 0.5 — Regenerate inbound allowlist:
    Run `mix mailglass.publish.check --package mailglass_inbound`. Expect exit 0. v1.13 inbound
    lib/ changes were docs: moduledoc strips only (no new files), so the inbound allowlist is
    expected stable. Update `.planning/publish/mailglass_inbound-*` if the command writes a new
    snapshot.

    Step 0.6 — Commit the regenerated snapshots:
    Stage ONLY the .planning/publish/ files:
    `git add .planning/publish/mailglass-files.expected .planning/publish/mailglass-publish-summary.json .planning/publish/mailglass_admin-files.expected .planning/publish/mailglass_admin-publish-summary.json .planning/publish/mailglass_inbound-files.expected .planning/publish/mailglass_inbound-publish-summary.json`
    Run `git status` first to confirm no reference mix.lock drift snuck in; if it did, `git checkout`
    those locks before staging. Commit: `chore: refresh publish allowlist snapshots for v1.8.0 release`
    NOTE: chore: commits do NOT trigger a Release Please version bump. This is intentional — the
    allowlist must be on the body BEFORE the release PR merges, but must not itself score a release.
  </action>

  <verify>
    <automated>mix mailglass.publish.check --package mailglass_admin && grep -q 'operator/tenants.ex' .planning/publish/mailglass_admin-files.expected && grep -q 'mount_path.ex' .planning/publish/mailglass_admin-files.expected</automated>
  </verify>

  <acceptance_criteria>
    `gh run list --workflow ci.yml --branch main --limit 8` shows a completed run where required
    lanes ("Support Contract Core", "Support Contract Admin", "Compile No Optional Deps", "Trust
    Lane Repo Head", "Installer Host Smoke") are all green on the last non-docs SHA.

    `mix mailglass.publish.check --package mailglass` exits 0.
    `mix mailglass.publish.check --package mailglass_admin` exits 0.
    `mix mailglass.publish.check --package mailglass_inbound` exits 0.

    `grep 'controllers/theme_controller.ex' .planning/publish/mailglass_admin-files.expected` returns a match.
    `grep 'mount_path.ex' .planning/publish/mailglass_admin-files.expected` returns a match.
    `grep 'mount_path_hook.ex' .planning/publish/mailglass_admin-files.expected` returns a match.
    `grep 'operator/tenants.ex' .planning/publish/mailglass_admin-files.expected` returns a match.

    `git show --stat HEAD` shows .planning/publish/ files changed with a
    "chore: refresh publish allowlist snapshots" message, and shows NO reference mix.lock files staged.
  </acceptance_criteria>

  <done>
    Green CI confirmed on local main. Publish allowlist regenerated so the admin snapshot includes
    the 4 previously-missing v1.13 admin lib files; core + inbound snapshots regenerated (stable).
    Snapshot committed as chore: on local main (not yet pushed — Task 2 pushes the body).
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 1 — Push body, re-score + verify PR #87 (reversible) — D-02, D-03, D-05
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 2 (Wave 1): Push v1.13 body to origin → re-score PR #87 to 1.8.0 → verify versions, inbound re-pin, CHANGELOGs (REL-02, D-02/D-03/D-05)</name>
  <files></files>

  <read_first>
    .github/workflows/release-please.yml — sed sync step that rewrites mailglass_admin/mix.exs and
    mailglass_inbound/mix.exs dep pins on the PR branch; auto-merge arming; cron self-heal.
    MAINTAINING.md — Step 2 "Review the release PR diff before merge" (load-bearing dep-pin sync).
    108-VALIDATION.md — Live State + Wave 1 criteria.
    CLAUDE.md "Reconcile planner branch to main" + branch-protection: confirm a direct
    `git push origin main` is allowed before relying on it (D-02 discretion: direct push vs ff path).
  </read_first>

  <action>
    Step 1.1 — Push the v1.13 body to origin/main (THE load-bearing moment, D-02):
    origin/main is frozen at 766edf89; local main carries the 227-commit v1.13 body plus the Task-1
    chore: allowlist commit. Push it:
    `git push origin main`
    If branch protection rejects a direct push, fall back to the fast-forward path the maintainer
    permits (CONTEXT D-02 discretion) — the binding requirement is that the FULL v1.13 body reaches
    origin/main so Release Please re-evaluates. Do NOT use `Release-As:` unless the natural re-score
    is demonstrably blocked.

    Step 1.2 — Wait for Release Please to re-score PR #87 (D-02):
    The push fires release-please.yml. Wait for it to complete:
    `gh run list --workflow release-please.yml --limit 3` and watch the latest run to success.
    Then re-read PR #87 — it is REGENERATED, not the stale 1.7.1 snapshot:
    `gh pr view 87 --json title,state` and `gh pr diff 87`.
    If RP did not update PR #87 within ~10 minutes of the push completing, dispatch it:
    `gh workflow run release-please.yml` (no inputs) and re-check.

    Step 1.3 — Verify manifest and @version bumps are 1.8.0 (D-01, D-03):
    In `gh pr diff 87`, confirm the regenerated PR shows:
    - `.release-please-manifest.json`: `"." : "1.8.0"` (core) and `"mailglass_admin": "1.8.0"`
      and `"mailglass_inbound"` bumped (RP-determined — see Step 1.5).
    - `mix.exs` has `@version "1.8.0"` (core)
    - `mailglass_admin/mix.exs` has `@version "1.8.0"`
    - `mailglass_inbound/mix.exs` has the RP-bumped `@version`
    If any core/admin version is NOT 1.8.0 (e.g. still 1.7.1), the body did not fully land or RP did
    not re-score — STOP, do not merge, investigate (re-confirm origin/main HEAD == local HEAD, then
    re-dispatch release-please.yml). The version target must reflect the pushed body, not stale #87.

    Step 1.4 — Verify the inbound D-13 exact-pin re-pin (REL-02, D-05):
    `gh pr diff 87 -- mailglass_inbound/mix.exs | grep '== 1.8.0'`
    Must return a `+` line containing `{:mailglass, "== 1.8.0"}` (RP sed step encodes this).
    Also verify the admin dep pin for REL-02 traceability:
    `gh pr diff 87 -- mailglass_admin/mix.exs | grep '== 1.8.0'`
    Must return a `+` line containing `{:mailglass, "== 1.8.0"}`.

    Step 1.5 — Confirm inbound scores a paired release (D-05):
    v1.13 inbound lib/ changes are docs: only (no feat/fix(inbound)). The inbound release is driven
    SOLELY by the D-13 exact-pin re-pin. Confirm PR #87 includes an inbound version bump + CHANGELOG
    entry. If a pin-only change does NOT score an inbound release in the regenerated PR, the executor
    must surface this and (per D-05) advance the inbound line with a `fix(inbound): re-pin to mailglass
    1.8.0` commit on the body, then re-dispatch release-please.yml so a paired inbound publish exists.
    Inbound is currently 1.4.0; record the RP-determined inbound target (expected 1.4.x).

    Step 1.6 — Review CHANGELOGs (REL-02):
    `gh pr diff 87 -- CHANGELOG.md` (core), `gh pr diff 87 -- mailglass_admin/CHANGELOG.md`,
    `gh pr diff 87 -- mailglass_inbound/CHANGELOG.md`. Confirm a `## [1.8.0]` heading for core+admin
    and a `## [1.4.x]` heading for inbound with feat/fix entries reflecting the v1.13 admin uplift.
    Light hand-curation is at Claude's discretion (CONTEXT) — RP-generated entries are sufficient
    for a routine ceremony.

    Step 1.7 — Check PR CI status (authoritative green proof on the pushed body):
    `gh pr checks 87`. All REQUIRED lanes must be green before auto-merge fires (advisory lanes —
    Operator Browser Gate, Preview Capture / Demo Browser Evidence Advisory, etc. — may be yellow/red
    without blocking). If any required lane is red, investigate before the Wave-2 checkpoint.

    Step 1.8 — Confirm auto-merge is armed:
    `gh pr view 87 --json autoMergeRequest` — the field must be non-null (RP arms it after PR
    creation). If null, arm it: `gh pr merge 87 --auto --squash`.

    Do NOT approve or merge yet. Task 3 is the irreversible maintainer go/no-go checkpoint.
  </action>

  <verify>
    <automated>test "$(git rev-parse origin/main)" = "$(git rev-parse HEAD)" && gh pr diff 87 -- mailglass_inbound/mix.exs | grep -q '== 1.8.0'</automated>
  </verify>

  <acceptance_criteria>
    `git rev-parse origin/main` equals `git rev-parse HEAD` (full v1.13 body + chore allowlist pushed).

    `gh pr diff 87` shows manifest "1.8.0" for `.` (core) and "mailglass_admin", and a bumped
    "mailglass_inbound"; @version "1.8.0" in mix.exs and mailglass_admin/mix.exs.

    `gh pr diff 87 -- mailglass_inbound/mix.exs | grep '== 1.8.0'` returns a `+` line containing
    `{:mailglass, "== 1.8.0"}`.

    `gh pr diff 87 -- mailglass_admin/mix.exs | grep '== 1.8.0'` returns a `+` line containing
    `{:mailglass, "== 1.8.0"}`.

    `gh pr diff 87 -- CHANGELOG.md | grep '## \[1.8.0\]'` returns a match; the admin CHANGELOG diff
    has `## [1.8.0]`; the inbound CHANGELOG diff has a new `## [1.4.` heading.

    `gh pr checks 87` shows all required lanes green (or orange while waiting; not red on required).
    `gh pr view 87 --json autoMergeRequest` returns a non-null autoMergeRequest.
  </acceptance_criteria>

  <done>
    v1.13 body pushed to origin/main; PR #87 re-scored from 1.7.1 to 1.8.0/1.8.0 (+ paired inbound
    1.4.x). Verified: correct manifest + @version attrs, inbound re-pin == 1.8.0, admin pin == 1.8.0,
    CHANGELOG entries, required CI green, auto-merge armed. Ready for the maintainer go/no-go.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 2 — Merge + publish (IRREVERSIBLE — maintainer go/no-go) — D-06
     ═══════════════════════════════════════════════════════════════════ -->

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (Wave 2): MAINTAINER GO/NO-GO — authorize merge of PR #87 (IRREVERSIBLE after Hex publish)</name>
  <files></files>

  <action>
    HARD HUMAN GATE — no autonomous action. The executor PAUSES here and presents the go/no-go to the
    maintainer (see what-built / how-to-verify / resume-signal below). The executor does not approve,
    merge, or dispatch any publish on its own. After the maintainer types "approved" and the publish
    fan-out is observed starting in Actions, proceed to Task 4 (Wave 3). If "blocked: [reason]", halt
    the ceremony and report. This is the single irreversible authorization point (D-06).
  </action>

  <what-built>
    Wave 0 verified green CI on local main and regenerated the stale publish allowlist — the admin
    snapshot now includes the 4 previously-missing lib files (theme_controller.ex, mount_path.ex,
    mount_path_hook.ex, operator/tenants.ex); committed as chore: (D-04).
    Wave 1 pushed the v1.13 body to origin/main, which re-scored PR #87 from 1.7.1 to 1.8.0/1.8.0
    (D-02), and verified the inbound re-pin {:mailglass, "== 1.8.0"} (D-05/D-13), the admin pin, and
    CHANGELOG entries for all three packages. Required CI is green and auto-merge is armed.
    The pipeline is fully hands-free once this checkpoint passes.

    IRREVERSIBILITY WARNING: After mailglass 1.8.0 publishes to Hex, Hex.pm allows revert only within
    60 minutes AND only if zero downloads have occurred. After that window, only `mix hex.retire`
    (deprecation) or a patch release remain. This is the one human go/no-go gate in the ceremony.
  </what-built>

  <how-to-verify>
    1. Visit https://github.com/szTheory/mailglass/pull/87 in a browser.
    2. Confirm the PR title is "chore: release main" and it is the REGENERATED PR (versions 1.8.0,
       not the old 1.7.1 snapshot).
    3. Confirm the Files Changed tab shows:
       - mailglass @version "1.8.0", mailglass_admin @version "1.8.0", mailglass_inbound @version bumped
       - mailglass_inbound/mix.exs dep: `{:mailglass, "== 1.8.0"}` (in the + lines)
       - mailglass_admin/mix.exs dep: `{:mailglass, "== 1.8.0"}`
       - CHANGELOG entries under ## [1.8.0] (core+admin) / ## [1.4.x] (inbound) look reasonable for
         the v1.13 admin design-system uplift
    4. Confirm required CI checks are green on the PR.
    5. If satisfied: APPROVE — auto-merge will squash-merge the PR; the hourly cron or a manual
       `gh workflow run release-please.yml` will tag the commit and fire the publish fan-out.

    After approving, monitor the Actions tab for:
    - release-please.yml completing (tag creation, GitHub Release)
    - publish-hex.yml fan-out triggered by `release: published`
    - prepublish-summary → gate-ci-green → publish-core → publish-inbound → publish-admin

    STALL RECOVERY (documented, NOT the default path — from MAINTAINING.md):
    - Anti-recursion stall (PR merged but no tag): `gh workflow run release-please.yml` (no inputs).
      A bot-merged release SHA gets no ci.yml run, so gate-ci-green can block with "no ci.yml runs
      found for SHA" — gate-ci-green now self-heals (dispatches ci.yml on the tag, waits ~30 min).
    - publish fan-out failed after tag: re-dispatch from the RELEASE TAG, NEVER from main:
      `gh workflow run publish-hex.yml --ref mailglass-v1.8.0 --field tag=mailglass-v1.8.0 --field dry_run=false --field package=all`
  </how-to-verify>

  <resume-signal>
    Type "approved" after authorizing the merge and observing that the publish fan-out has started
    (publish-hex.yml run visible in Actions), or type "blocked: [reason]" to halt and describe what
    you found in the PR diff.
  </resume-signal>

  <verify>
    <human-check>Maintainer reviewed PR #87 (versions 1.8.0/1.8.0/inbound, inbound re-pin == 1.8.0, CHANGELOGs, required CI green) and typed "approved"; publish-hex.yml fan-out is visible in the Actions tab.</human-check>
  </verify>

  <done>
    Maintainer authorized the merge of PR #87; publish-hex.yml fan-out has started (visible in Actions),
    or the ceremony was halted with a documented "blocked: [reason]".
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 3 — Confirm Hex live (read-only) — REL-02, D-05
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 4 (Wave 3): Confirm all three packages live on Hex at target versions + correct inbound pin (REL-02, D-05)</name>
  <files></files>

  <read_first>
    .github/workflows/publish-hex.yml — publish job ordering: publish-core → publish-inbound →
    publish-admin; idempotency guards (mix hex.info check before publish); advisory lane list (do
    not block on Operator Browser Gate / Demo Browser Evidence Advisory).
    MAINTAINING.md — Step 3 "Monitor the hands-free publish fan-out".
  </read_first>

  <action>
    Step 3.1 — Watch the publish fan-out in Actions:
    `gh run list --workflow publish-hex.yml --limit 3` to find the run triggered by `release:
    published`. Monitor jobs: prepublish-summary → gate-ci-green → publish-core → publish-inbound →
    publish-admin. All must succeed (or skip via idempotency — already-published is success). Advisory
    jobs may fail without blocking.

    Step 3.2 — Verify core on Hex:
    `mix hex.info mailglass 1.8.0`. Wait for Hex indexing if needed (up to ~5 min; publish-core has a
    built-in index wait). Output must contain "Released:".

    Step 3.3 — Verify admin on Hex:
    `mix hex.info mailglass_admin 1.8.0`. Output must contain "Released:".

    Step 3.4 — Verify inbound on Hex with the correct pin (REL-02, D-05/D-13):
    `mix hex.info mailglass_inbound <inbound_target>` (the RP-determined 1.4.x from Task 2 Step 1.5).
    Output must contain "Released:" AND must show the dependency `mailglass == 1.8.0`. Confirm:
    `mix hex.info mailglass_inbound <inbound_target> | grep 'mailglass'` returns `mailglass == 1.8.0`.

    Step 3.5 — HexDocs spot check (may lag Hex by 5–10 min):
    - https://hexdocs.pm/mailglass/1.8.0/
    - https://hexdocs.pm/mailglass_admin/1.8.0/
    - https://hexdocs.pm/mailglass_inbound/<inbound_target>/
    Each URL must return HTTP 200 (not 404 / redirect to latest).

    NOTE: the 60-minute revert window starts when publish-core completes (Step 3.2 green). Set a
    timer — Task 5 (consumer smoke) must complete within this window.
  </action>

  <verify>
    <automated>mix hex.info mailglass 1.8.0 | grep -q 'Released:' && mix hex.info mailglass_admin 1.8.0 | grep -q 'Released:'</automated>
  </verify>

  <acceptance_criteria>
    `mix hex.info mailglass 1.8.0` output contains "Released:".
    `mix hex.info mailglass_admin 1.8.0` output contains "Released:".
    `mix hex.info mailglass_inbound <inbound_target>` output contains "Released:" AND
    `mix hex.info mailglass_inbound <inbound_target> | grep 'mailglass'` returns `mailglass == 1.8.0`.
    All three publish-hex.yml jobs (publish-core, publish-inbound, publish-admin) show
    `completed: success` or `completed: skipped` (idempotent re-run) in `gh run view`.
  </acceptance_criteria>

  <done>
    mailglass 1.8.0, mailglass_admin 1.8.0, mailglass_inbound 1.4.x confirmed live on Hex. Inbound
    dependency shows mailglass == 1.8.0. 60-minute revert window timer started.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 4 — Resolution + smoke (within 60-minute window) — REL-02, REL-03, D-09
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 5 (Wave 4): Consumer smoke (DEP_MODE=hex) + post-publish-smoke green within the 60-minute window (REL-02, REL-03, D-09)</name>
  <files></files>

  <read_first>
    scripts/consumer_install_smoke.sh — DEP_MODE=hex path; OPS-01 guard (no hackney/finch in
    mix.lock); INCLUDE_INBOUND flag; inbound version var.
    .github/workflows/post-publish-smoke.yml — smoke job chain; inbound compatibility check (grep for
    mailglass == 1.8.0, sets include_inbound=true); known #32 hackney false-positive (distinguish
    from a real regression).
    MAINTAINING.md — Step 4 (60-minute window manual smoke) + the Retract Decision Tree.
  </read_first>

  <action>
    Step 4.1 — Run consumer smoke within the 60-minute window (REL-02 Hex-resolution proof):
    From repo root:
    `VERSION=1.8.0 VERSION_INBOUND=<inbound_target> DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh`
    The smoke creates a fresh phx.new sandbox, injects == 1.8.0 deps, runs mix deps.get, mix
    mailglass.install, mix compile --warnings-as-errors, boots, and hits GET /dev/mail/ → 200.
    Exit 0 = pass.

    Step 4.2 — Verify mix deps.get Hex resolution (REL-02):
    Confirm the smoke's `mix deps.get` step resolved mailglass from Hex at 1.8.0 and mailglass_inbound
    from Hex at the target version (visible in script output). This is the REL-02 Hex-resolution proof.

    Step 4.3 — Monitor post-publish-smoke (REL-03):
    `gh run list --workflow post-publish-smoke.yml --limit 3` to find the run triggered by `release:
    published`. Monitor: cron-guard → wait-for-index → wait-for-hexdocs → consumer-install →
    published-trust-journey → retracted-check. If no run fired within ~10 min of publish, dispatch:
    `gh workflow run post-publish-smoke.yml --field tag=mailglass-v1.8.0`
    All jobs must pass.

    Step 4.4 — D-09 known-noise classification (NON-BLOCKER):
    The issue #32 swoosh/hackney OPS-01 false-positive in `consumer-install` (output containing
    "hackney" / "missing hackney dependency") is an ACCEPTABLE non-blocker. If that is the only red,
    document that it matches #32 and proceed. ANY OTHER red is a real regression — evaluate it against
    the MAINTAINING.md Retract Decision Tree. Do not accept any non-#32 failure as noise.

    Step 4.5 — 60-minute decision (Retract Decision Tree):
    If both consumer smoke (4.1) and post-publish-smoke (4.3) pass (or only #32 noise is red), proceed
    to Task 6. If a real (non-#32) regression is found within 60 minutes of publish:
    - Zero downloads: `mix hex.publish --revert <version>` for affected packages.
    - Non-zero downloads or past 60 min: evaluate the Retract rules and open a patch release PR.
  </action>

  <verify>
    <automated>VERSION=1.8.0 DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh</automated>
  </verify>

  <acceptance_criteria>
    `scripts/consumer_install_smoke.sh` (with VERSION=1.8.0 DEP_MODE=hex INCLUDE_INBOUND=true) exits 0
    AND its `mix deps.get` output shows mailglass 1.8.0 and mailglass_inbound <inbound_target> fetched
    from Hex.

    `gh run view` of post-publish-smoke.yml shows all jobs passed, OR the only red job is
    `consumer-install` with output containing "hackney" (issue #32 false-positive, acceptable
    non-blocker per D-09). No other failure is accepted as noise.
  </acceptance_criteria>

  <done>
    Consumer smoke passed within the 60-minute window; Hex resolution of all three packages confirmed.
    Post-publish smoke passed (or only the #32 hackney noise red). Retract-window decision: proceed.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 5 — Milestone closeout — REL-03, D-08
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 6 (Wave 5): Milestone audit (9 phases / 36 reqs), archive, git tag v1.13 (REL-03, D-08)</name>
  <files>.planning/milestones/v1.13-ROADMAP.md, .planning/milestones/v1.13-REQUIREMENTS.md, .planning/milestones/v1.13-MILESTONE-AUDIT.md, .planning/milestones/MILESTONES.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/RETROSPECTIVE.md, .planning/STATE.md</files>

  <read_first>
    STATE.md — the v1.12 closeout precedent (2026-06-17) and the milestone.complete count-inflation
    gotcha. 108-01-PLAN.md Task 6 for the exact archive sequence + audit-file shape.
    .planning/REQUIREMENTS.md — the 36-requirement traceability table (all Complete except REL-02/03,
    which this phase closes) — this is the authoritative scope source for the audit.

    COUNT-INFLATION WARNING (D-08): Do NOT use `gsd-sdk milestone.complete` raw counts for the audit.
    The CLI counts ALL .planning/phases/ directories including leftover 999.1, 999.2 backlog dirs.
    Manually use the true v1.13 scope: 9 phases (109–117), 36 requirements (FND 5, PRIM 7, FORM 3,
    SHELL 6, DATA 5, GROUP 3, FLOW 4, RATCHET 5, REL 3).
  </read_first>

  <action>
    Step 5.1 — Stability contract baseline check:
    Run `mix verify.stability_contract` from repo root to confirm the shipped v1.8.0 codebase passes
    all contract checks. (If the demo/reference path re-bumps a mix.lock, `git checkout` it after.)

    Step 5.2 — Archive ROADMAP:
    `cp .planning/ROADMAP.md .planning/milestones/v1.13-ROADMAP.md`

    Step 5.3 — Archive REQUIREMENTS:
    `cp .planning/REQUIREMENTS.md .planning/milestones/v1.13-REQUIREMENTS.md`

    Step 5.4 — Write MILESTONE-AUDIT (REL-03, D-08):
    Create `.planning/milestones/v1.13-MILESTONE-AUDIT.md`. Include:
    - Header: "# v1.13 Milestone Audit — Admin Design-System Stress Test & UX Uplift (v3)"
    - Audit date (actual)
    - Verdict line: `status: passed`
    - True scope stats (CORRECTED, not from gsd-sdk): 9 phases (109–117), 36 requirements, packages
      shipped mailglass 1.8.0 / mailglass_admin 1.8.0 / mailglass_inbound 1.4.x
    - Per-requirement pass/fail table: ALL 36 REQ-IDs marked `status: passed` — FND-01..05, PRIM-01..07,
      FORM-01..03, SHELL-01..06, DATA-01..05, GROUP-01..03, FLOW-01..04, RATCHET-01..05, REL-01..03
    - Phase table: 109–117 all Complete
    - Evidence references: published Hex URLs (1.8.0/1.8.0/1.4.x), post-publish-smoke run URL, PR #87 URL

    Step 5.5 — Update MILESTONES.md:
    Add a v1.13 row with CORRECTED counts:
    `✅ v1.13 Admin Design-System Stress Test & UX Uplift (v3) — 9 phases (109–117), 36 reqs, shipped YYYY-MM-DD — mailglass 1.8.0 / mailglass_admin 1.8.0 / mailglass_inbound 1.4.x (see milestones/v1.13-MILESTONE-AUDIT.md)`

    Step 5.6 — Update PROJECT.md:
    Advance current-milestone notation to v1.13 shipped; update the "Current state" line to reflect
    1.8.0/1.8.0/1.4.x as the current live Hex versions.

    Step 5.7 — Update ROADMAP.md:
    Move Phase 117 to Complete; flip the v1.13 milestone entry to ✅ SHIPPED; move v1.13 into the
    shipped/archived milestone history list at the top; clear the active-milestone section (or stub
    it with "no active milestone — see /gsd-new-milestone for next"). Update the Progress table row
    for Phase 117. (Use scoped Edits — never Write the whole ROADMAP.)

    Step 5.8 — Remove REQUIREMENTS.md (or replace with stub), following the v1.11/v1.12 precedent:
    `git rm .planning/REQUIREMENTS.md` (it is archived in v1.13-REQUIREMENTS.md), or replace with a
    one-line stub: "# Requirements — awaiting next milestone definition".

    Step 5.9 — Append RETROSPECTIVE entry:
    Append a v1.13 section to `.planning/RETROSPECTIVE.md`:
    - Milestone summary (9 phases, fractal admin design-system uplift + release)
    - What worked: bottom-up fractal build order; gates-tightened-before-rescore anti-trap; allowlist-
      first publish; RP hands-free pipeline; clean-push re-score mechanism
    - Key lesson: the admin publish allowlist was stale by 4 lib files — refresh it before EVERY
      non-trivial admin release (D-04)
    - Key lesson: the clean push of the body (not Release-As:) is what flips RP from patch to minor;
      origin was frozen 229 commits behind
    - Release outcome: 1.8.0/1.8.0/1.4.x live; first linked-version cut since 1.7.0/1.7.0/1.4.0 (v1.12)

    Step 5.10 — Update STATE.md (docs(state): commit reflecting Phase 117 complete, v1.13 closed).

    Step 5.11 — Commit all milestone artifacts:
    `git add .planning/milestones/v1.13-ROADMAP.md .planning/milestones/v1.13-REQUIREMENTS.md .planning/milestones/v1.13-MILESTONE-AUDIT.md .planning/milestones/MILESTONES.md .planning/PROJECT.md .planning/ROADMAP.md .planning/RETROSPECTIVE.md .planning/STATE.md`
    Plus the REQUIREMENTS.md removal (`git rm .planning/REQUIREMENTS.md`) if taking that path.
    Commit: `docs(state): v1.13 complete — mailglass 1.8.0/1.8.0/1.4.x on Hex`

    Step 5.12 — Tag v1.13 (REL-03):
    `git tag v1.13` then `git push origin v1.13`.
    NOTE: the git tag v1.13 is a MILESTONE marker, distinct from the RP-created Hex release tags
    (mailglass-v1.8.0, mailglass_admin-v1.8.0, mailglass_inbound-v1.4.x).

    Step 5.13 — Push the commit: `git push origin main`.
  </action>

  <verify>
    <automated>ls .planning/milestones/v1.13-MILESTONE-AUDIT.md && grep -q 'status: passed' .planning/milestones/v1.13-MILESTONE-AUDIT.md && git tag -l v1.13 | grep -q v1.13</automated>
  </verify>

  <acceptance_criteria>
    `ls .planning/milestones/v1.13-*` returns at least three files: v1.13-ROADMAP.md,
    v1.13-REQUIREMENTS.md, v1.13-MILESTONE-AUDIT.md.

    `grep 'status: passed' .planning/milestones/v1.13-MILESTONE-AUDIT.md` returns a match.
    The audit file lists all 36 REQ-IDs (spot-check: `grep -E 'FND-05|PRIM-07|SHELL-06|RATCHET-05|REL-03' .planning/milestones/v1.13-MILESTONE-AUDIT.md` returns matches) and states "9 phases" (not inflated by 999.x dirs).

    `grep 'v1.13' .planning/milestones/MILESTONES.md` returns a row containing "1.8.0" and "9 phases".

    `git tag -l v1.13` returns "v1.13" AND `git ls-remote --tags origin v1.13` returns the tag ref on origin.
  </acceptance_criteria>

  <done>
    Milestone v1.13 audited (9 phases 109–117, 36/36 requirements status: passed,
    1.8.0/1.8.0/1.4.x on Hex). Three archive files created under .planning/milestones/. MILESTONES.md,
    PROJECT.md, ROADMAP.md, RETROSPECTIVE.md, STATE.md updated; REQUIREMENTS.md removed; v1.13 tag on
    origin. v1.13 milestone is CLOSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Hex publish | Irreversible: once mailglass 1.8.0 is on Hex and 60 min pass, it cannot be unpublished |
| RP PR diff → maintainer review | Load-bearing PR (dep-pin sed sync inside); must be human-reviewed before auto-merge fires |
| publish-hex.yml → hex-publish environment | HEX_API_KEY scoped to the hex-publish GitHub Environment; never exposed to PR jobs |
| local main → origin/main push | The clean push re-scores PR #87; an accidental/partial push could publish an unintended version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | .release-please-manifest.json | mitigate | Never hand-edit; let Release Please manage; MAINTAINING.md Step 2 prohibits manual edits; Task 2 verifies the regenerated values rather than authoring them |
| T-117-02 | Information Disclosure | HEX_API_KEY secret | mitigate | Key scoped to the hex-publish GitHub Environment only; never visible to PR jobs (CLAUDE.md Commit & Branch Conventions) |
| T-117-03 | Repudiation | Hex publish origin | mitigate | publish-hex.yml gated on gate-ci-green; publish only from the release tag (never main); all publish runs logged in GitHub Actions |
| T-117-04 | Tampering | clean push re-scoring to an unintended version | mitigate | Task 2 acceptance gates on `origin/main == HEAD` and re-verifies 1.8.0 in the regenerated PR before the Wave-2 human go/no-go authorizes the irreversible merge (D-03) |
| T-117-05 | Denial of Service | Anti-recursion stall (no publish fan-out after bot-merge) | accept | Self-healed by hourly cron + gate-ci-green auto-dispatch; manual recovery via `gh workflow run release-please.yml`; acceptable latency ≤ 60 min |
| T-117-06 | Elevation of Privilege | Dispatch publish-hex.yml from main | mitigate | Workflow docs + MAINTAINING.md state "never dispatch from main"; always use `--ref mailglass-v1.8.0 --field tag=mailglass-v1.8.0` |
| T-117-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no new packages; the ceremony uses the existing mix toolchain only (no new application attack surface) |
</threat_model>

<verification>
Full phase gate (all must pass before Phase 117 is marked Complete):

- Wave 0: `mix mailglass.publish.check --package {mailglass,mailglass_admin,mailglass_inbound}` each exit 0; the 4 missing admin lib files now in `mailglass_admin-files.expected`; chore commit on main (D-04).
- Wave 1: `git rev-parse origin/main == HEAD`; `gh pr diff 87` shows 1.8.0/1.8.0 + bumped inbound; inbound re-pin `== 1.8.0`; CHANGELOG `## [1.8.0]`; required CI green; auto-merge armed (D-02/D-03/D-05).
- Wave 2: maintainer go/no-go approved; publish fan-out visible in Actions (or stall recovery applied) (D-06).
- Wave 3: `mix hex.info mailglass 1.8.0` / `mailglass_admin 1.8.0` / `mailglass_inbound <target>` each contain "Released:"; inbound shows `mailglass == 1.8.0` (REL-02, D-05).
- Wave 4: `consumer_install_smoke.sh` exits 0 with DEP_MODE=hex VERSION=1.8.0; post-publish-smoke passes (or only #32 hackney noise red) (REL-02, REL-03, D-09).
- Wave 5: `ls .planning/milestones/v1.13-*` returns 3 files; MILESTONE-AUDIT.md `status: passed` with all 36 REQ-IDs and 9-phase scope; `git tag -l v1.13` + `git ls-remote --tags origin v1.13` confirm the tag (REL-03, D-08).
</verification>

<success_criteria>
1. mailglass 1.8.0 is live on Hex: `mix hex.info mailglass 1.8.0` contains "Released:".
2. mailglass_admin 1.8.0 is live on Hex: `mix hex.info mailglass_admin 1.8.0` contains "Released:".
3. mailglass_inbound 1.4.x is live with the correct pin: `mix hex.info mailglass_inbound <target> | grep mailglass` returns `mailglass == 1.8.0` (D-05/D-13).
4. mix deps.get resolves all three from Hex (consumer smoke passes — REL-02).
5. Post-publish smoke passes (REL-03), or only the known #32 hackney false-positive is red (D-09).
6. v1.13 milestone is archived: `.planning/milestones/v1.13-MILESTONE-AUDIT.md` exists with all 36 requirements `status: passed`, scope corrected to 9 phases (not inflated by 999.x dirs) (D-08).
7. git tag v1.13 exists on origin (REL-03).
</success_criteria>

<output>
Create `.planning/phases/117-release-cut-milestone-closeout/117-01-SUMMARY.md` when done.

Summary must include:
- Hex versions confirmed live (with `mix hex.info` output excerpts), including the RP-determined inbound target
- Inbound pin verification (`mailglass == 1.8.0`)
- Consumer smoke exit code + GET /dev/mail/ result + REL-02 Hex-resolution evidence
- post-publish-smoke run URL + outcome (and any #32 noise classification)
- Milestone audit: scope counts (9 phases, 36 reqs), all 36 REQ-IDs confirmed status: passed
- Git tag v1.13: `git tag -l v1.13` and push confirmation
- Any stall recovery steps taken (anti-recursion, gate-ci-green self-heal, fallback dispatch)
</output>
