---
phase: 45-inbound-telemetry-idempotency-foundation
plan: 10
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .credo.exs
  - test/mailglass/credo/checks_have_tests_test.exs
  - test/mailglass/credo/integration_test.exs
autonomous: true
requirements: [TELE-06]
must_haves:
  truths:
    - "Mailglass.Credo.StreamPolicyConsistent is registered in .credo.exs and runs under mix credo"
    - "mix credo --strict exits 0 with StreamPolicyConsistent active"
    - "The meta-test fails CI when a credo_checks/*.ex module is defined but NOT registered in .credo.exs (registration blind spot closed)"
    - "The meta-test still fails CI when a check has no matching *_test.exs (test-existence dimension preserved)"
    - "integration_test.exs exercises the SHIPPED .credo.exs params (loaded via Code.eval_file), not a stale hand-maintained duplicate"
    - "The .credo.exs GenSmtp-alias-key rationale comment states the truth: CR-01 coverage rides on the :mimemail/:gen_smtp_client atom keys, not the GenSmtp key"
  artifacts:
    - path: ".credo.exs"
      provides: "StreamPolicyConsistent registered in extra_checks; corrected GenSmtp-key comment"
      contains: "Mailglass.Credo.StreamPolicyConsistent"
    - path: "test/mailglass/credo/checks_have_tests_test.exs"
      provides: "meta-test asserting BOTH a matching test AND .credo.exs registration for every credo_checks/*.ex"
      contains: "Code.eval_file"
    - path: "test/mailglass/credo/integration_test.exs"
      provides: "integration test that loads the live .credo.exs config (no hardcoded @extra_checks duplicate)"
      contains: "Code.eval_file"
  key_links:
    - from: "test/mailglass/credo/checks_have_tests_test.exs"
      to: ".credo.exs"
      via: "Code.eval_file then build the registered-module set from the checks list and reject any defined-but-unregistered check"
      pattern: "Code.eval_file"
    - from: ".credo.exs checks list"
      to: "credo_checks/stream_policy_consistent.ex"
      via: "{Mailglass.Credo.StreamPolicyConsistent, []} entry in extra_checks"
      pattern: "StreamPolicyConsistent"
    - from: "test/mailglass/credo/integration_test.exs"
      to: ".credo.exs"
      via: "Code.eval_file to source the real check params instead of a duplicated literal"
      pattern: "Code.eval_file"
---

<objective>
Make the phase's DEFINING goal actually true: the "claimed-but-inert custom Credo guard"
defect class is SELF-DETECTING. This plan closes the two BLOCKERS by (1) registering the
real-but-inert `Mailglass.Credo.StreamPolicyConsistent` guard in `.credo.exs` so `mix credo`
runs it, and (2) extending the meta-test so it fails CI when ANY `credo_checks/*.ex` module
is defined but not registered — the exact blind spot that let the inert guard ship inside the
prior gap-closure cycle. It also folds in the two drift backstops on the same surface: correct
the factually-wrong `.credo.exs` GenSmtp-alias-key comment (WR-04) and de-duplicate
`integration_test.exs` so it loads the live config instead of a stale hand-maintained copy
(WR-05).

All `.credo.exs` and `test/mailglass/credo/` registration surface lives in this one atomic
plan — the blocker pair plus its drift backstops — so the registration self-detection mechanism
and the config it asserts cannot diverge across plans.

Purpose: TELE-06 lint enforcement is only real if every defined check is both registered AND
covered, with a meta-test that fails CI on either gap. After this plan a future inert guard
(defined-but-unregistered) fails CI loudly.
Output: registered StreamPolicyConsistent + a registration-aware meta-test + a self-sourcing
integration test + an honest GenSmtp-key comment, with `mix credo --strict` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/45-inbound-telemetry-idempotency-foundation/45-VERIFICATION.md
@.planning/phases/45-inbound-telemetry-idempotency-foundation/45-REVIEW.md

<interfaces>
<!-- Extracted contracts the executor needs — no codebase exploration required. -->

.credo.exs structure (the file under edit):
- `extra_checks` is a list bound at the top of `.credo.exs`; the same list is spliced into
  `config.configs |> hd() |> Map.fetch!(:checks)` (via `extra_checks ++ [<disabled-builtins>]`)
  AND stored again under the config's `:extra_checks` key.
- Custom checks are `{Mailglass.Credo.<Name>, params_keyword}` tuples (some builtins are bare
  `{Credo.Check..., false}` disable tuples — those are NOT custom checks).
- `Mailglass.Credo.StreamPolicyConsistent` is currently ABSENT from `extra_checks` (grep count 0).
  All other 16 credo_checks/*.ex modules appear exactly once.
- The GenSmtp-alias-key rationale comment is at `.credo.exs` ~lines 36-39 (inside the
  NoBareOptionalDepReference tuple's leading comment block).

Config-loading idiom (already used by credo_config_sentinel_test.exs:16):
- `{config, _binding} = Code.eval_file(".credo.exs")` returns the config map.
- `config.configs |> hd() |> Map.fetch!(:checks)` is the registered-checks list. Its value may be
  a flat keyword-style list of `{module, params}` tuples, OR grouped under `:enabled`/`:extra`/
  `:disabled` keys — handle both (see credo_config_sentinel_test.exs `flatten_checks/1` for the
  exact normalization shape to mirror).
- A registered entry can be `{module, params}` (custom + configured builtins) OR a bare `module`
  atom (rare) — when building the registered-module set, handle BOTH `{mod, _params} -> mod` and
  `mod when is_atom(mod) -> mod`.

Module-name derivation from a check file path (the registration check):
- `credo_checks/<name>.ex` registers as `Mailglass.Credo.<CamelizedName>` where
  `<CamelizedName> = Macro.camelize(Path.basename(path, ".ex"))`. Example:
  `credo_checks/stream_policy_consistent.ex` -> `Mailglass.Credo.StreamPolicyConsistent`.
  Build the module with `Module.concat([Mailglass, Credo, Macro.camelize(base)])`.

checks_have_tests_test.exs current state (the file under edit):
- One test asserting only `File.exists?("test/mailglass/credo/#{base}_test.exs")` for each
  `Path.wildcard("credo_checks/*.ex")` (the test-existence dimension — KEEP it).
- It never reads .credo.exs (the registration blind spot — ADD that dimension).

integration_test.exs current state (the file under edit):
- Holds a hand-maintained `@extra_checks` literal (lines 6-63) that is STALE: it carries
  `TelemetryEventConvention` with `required_root: :mailglass` (singular, pre-WR-02), a
  `NoBareOptionalDepReference` gated_modules map WITHOUT the `:mimemail`/`:gen_smtp_client`
  atom keys (pre-CR-01), omits StreamPolicyConsistent, and asserts `length(@extra_checks) == 13`.
- `@check_cases` is a separate list of synthetic bad/clean fixture sources keyed by check module
  (KEEP it — it is the real fixture corpus). `params_for/1` looks up params by check module from
  `@extra_checks`. The fix is to source params from the LIVE `.credo.exs` instead of the literal.
- `find` precedent for sourcing live params: credo_config_sentinel_test.exs `find_check/2`.
</interfaces>

@.credo.exs
@credo_checks/stream_policy_consistent.ex
@test/mailglass/credo/checks_have_tests_test.exs
@test/mailglass/credo/credo_config_sentinel_test.exs
@test/mailglass/credo/integration_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register StreamPolicyConsistent in .credo.exs + correct the GenSmtp-key comment (WR-04), keep mix credo --strict green</name>
  <read_first>
    - .credo.exs (the file under edit — read the whole config: the `extra_checks` list at the top, the NoBareOptionalDepReference comment block at ~36-39, and how `extra_checks` is spliced into `:checks` and stored under `:extra_checks`)
    - credo_checks/stream_policy_consistent.ex (the guard being registered — confirm the module name `Mailglass.Credo.StreamPolicyConsistent` and that it has no required params, so `[]` is the correct param keyword)
    - lib/mailglass/optional_deps/gen_smtp.ex (source of truth for WR-04: gen_smtp is an Erlang lib with NO `GenSmtp` Elixir module; `:mimemail`/`:gen_smtp_client` are the real call-site atoms; the inbound `OptionalGenSmtp.decode` alias path passes by MISSING the gated_modules map, not by matching a `GenSmtp` key)
  </read_first>
  <action>
    Add `{Mailglass.Credo.StreamPolicyConsistent, []}` to the `extra_checks` list in `.credo.exs`
    (the list bound at the top of the file). It must land in `extra_checks` so it is BOTH spliced
    into `:checks` (which registers it for `mix credo`) AND reflected in `:extra_checks` — placing
    it only in a disabled-builtin block or only in `requires:` does NOT register it. Group it
    sensibly near the other warning-category custom checks; `[]` selects the check's
    `param_defaults` (`mailable_module: Mailglass.Mailable`).

    Run `mix credo --strict` and confirm exit 0. Registering a previously-inert guard may surface
    REAL violations in `lib/` (a tracking-enabled mailable with no explicit `:bulk`/`:operational`
    stream). If it does, FIX the surfaced finding in `lib/` (declare the explicit stream per the
    CLAUDE.md open/click-tracking discipline). Only if a specific finding is a documented false
    positive, suppress it NARROWLY with a `# Reason:` + `# Tracking:` block at the offending site
    per the project's `check_credo_suppressions.sh` gate — never broaden or disable the check.
    The acceptance bar is "mix credo --strict exits 0 with StreamPolicyConsistent active."

    AUDITABILITY (scope-expansion record): IF registering StreamPolicyConsistent surfaces a real
    `mix credo --strict` finding that requires touching a `lib/` mailable (to add an explicit
    `:bulk`/`:operational` stream) or adding a narrow `# Reason:`/`# Tracking:` suppression at a
    `lib/` site, that `lib/` file is OUT of this plan's frontmatter `files_modified` but MUST still
    be edited to make the gate green AND MUST be recorded in the SUMMARY's files-changed list with a
    one-line note of what changed and why (the surfaced finding). The possible scope expansion must
    be auditable — do not silently edit a `lib/` file without recording it in the SUMMARY.

    Correct the GenSmtp-alias-key rationale comment in the NoBareOptionalDepReference tuple
    (~lines 36-39). Replace the false claim (that the `GenSmtp` key is load-bearing because the
    aliased call "resolves to the `GenSmtp` alias") with the truth: Credo does NOT resolve aliases;
    the aliased `OptionalGenSmtp.decode/2` call passes because its root `OptionalGenSmtp` is simply
    not a key in `gated_modules`; the `GenSmtp` key would only ever match a literal `GenSmtp.<fn>`
    call, which no Elixir code makes (the dep is the Erlang `:gen_smtp`); CR-01 coverage rides
    ENTIRELY on the `:mimemail` / `:gen_smtp_client` atom keys. You MAY drop the vestigial
    `GenSmtp` key only if no literal `GenSmtp.*` call site is expected anywhere; at minimum the
    comment must be made factually correct. Do NOT touch the `:mimemail`/`:gen_smtp_client` atom
    keys — those are the live CR-01 coverage.

    Do NOT alter any other check's params, the `included` path list, or the disabled-builtin block.
  </action>
  <acceptance_criteria>
    - `grep -c "StreamPolicyConsistent" .credo.exs` returns >= 1 (registered).
    - `grep -oE "Mailglass\.Credo\.[A-Za-z]+" .credo.exs | sort -u | wc -l` returns 17 (was 16; all 17 check modules now present).
    - CI proof: `mix credo --strict` exits 0 with StreamPolicyConsistent active (if a real finding surfaced, it is fixed in lib/ or narrowly suppressed with `# Reason:`/`# Tracking:`).
    - AUDITABILITY: if any `lib/` file was touched to clear a StreamPolicyConsistent finding (explicit stream declaration or narrow `# Reason:`/`# Tracking:` suppression), that `lib/` file path is listed in the SUMMARY's files-changed section with a one-line reason — even though it is not in this plan's frontmatter `files_modified`. (If no `lib/` file was touched, the SUMMARY states that explicitly.)
    - The NoBareOptionalDepReference comment no longer claims Credo resolves the `GenSmtp` alias; it states CR-01 coverage rides on the `:mimemail`/`:gen_smtp_client` atom keys (grep the comment region for the corrected wording; the false phrase "resolves to the `GenSmtp` alias" is gone).
    - `:mimemail` and `:gen_smtp_client` atom keys are unchanged: `grep -c ":mimemail" .credo.exs` and `grep -c ":gen_smtp_client" .credo.exs` each return >= 1.
  </acceptance_criteria>
  <verify>
    <automated>MISSING locally (toolchain caveat — local Elixir/OTP differs from CI's pinned 1.18/OTP 27; local `mix` is not the source of truth). CI proof: `mix credo --strict` exits 0. Source proof: `grep -c StreamPolicyConsistent .credo.exs` >= 1; `grep -oE "Mailglass\.Credo\.[A-Za-z]+" .credo.exs | sort -u | wc -l` == 17; the false "resolves to the `GenSmtp` alias" comment phrase is gone. Auditability proof: SUMMARY records any `lib/` file touched (or states none was).</automated>
  </verify>
  <done>StreamPolicyConsistent runs under mix credo (registered in extra_checks), mix credo --strict exits 0, the GenSmtp-key comment is factually correct, and any lib/ scope expansion is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the meta-test to assert .credo.exs registration for every check (close the blind spot, with teeth)</name>
  <read_first>
    - test/mailglass/credo/checks_have_tests_test.exs (the file under edit — the existing single `File.exists?` test is the test-existence dimension to PRESERVE; the registration dimension is what to ADD)
    - test/mailglass/credo/credo_config_sentinel_test.exs (the Code.eval_file idiom + `flatten_checks/1`/`find_check/2` normalization to MIRROR for loading the registered-checks list and handling enabled/extra/disabled grouping)
    - 45-REVIEW.md "Critical Issues" section (the EXACT recipe: the "every custom Credo check is registered in .credo.exs" test — registered-set from the checks list handling both `{mod, params}` and bare-atom entries; defined-set from `Path.wildcard("credo_checks/*.ex")` via `Module.concat([Mailglass, Credo, Macro.camelize(base)])`; reject defined not in registered; assert empty with an enumerating message)
    - credo_checks/stream_policy_consistent.ex (confirms the file→module name mapping: `stream_policy_consistent.ex` → `Mailglass.Credo.StreamPolicyConsistent`)
  </read_first>
  <action>
    Extend `test/mailglass/credo/checks_have_tests_test.exs` to cover BOTH halves of the defining
    goal. KEEP the existing test that asserts a matching `test/mailglass/credo/<base>_test.exs`
    exists for every `Path.wildcard("credo_checks/*.ex")` (the test-existence dimension — do NOT
    weaken it). ADD a second test, "every custom Credo check is registered in .credo.exs":

    - Load the live config: `{config, _binding} = Code.eval_file(".credo.exs")`.
    - Build the REGISTERED-module set from `config.configs |> hd() |> Map.fetch!(:checks)`,
      normalizing the value the same way credo_config_sentinel_test.exs does (handle the
      enabled/extra/disabled grouping), then mapping each entry to its module — handling BOTH
      `{mod, _params} -> mod` tuples AND bare `mod when is_atom(mod) -> mod` atoms — into a MapSet.
    - Build the DEFINED-module list from `Path.wildcard("credo_checks/*.ex")` via
      `Module.concat([Mailglass, Credo, Macro.camelize(Path.basename(path, ".ex"))])`.
    - Compute `unregistered = Enum.reject(defined, &MapSet.member?(registered, &1))` and
      `assert unregistered == []` with a message that ENUMERATES each defined-but-unregistered
      module (so a future inert guard fails CI loudly with its exact module name). Phrase the
      failure message with a "Defined-but-unregistered:" label followed by the enumerated module
      names, so the negative-state output is predictable and string-checkable.

    Do NOT add an allowlist / `@known_uncovered` / exclusion set for either test — every check must
    be both tested AND registered, period. Keep both tests small and self-contained.

    Prove the registration assertion has TEETH (the negative control, documented in the SUMMARY,
    NOT shipped as a permanent test edit): with StreamPolicyConsistent UN-registered (the state
    before Task 1), this new assertion FAILS naming `Mailglass.Credo.StreamPolicyConsistent`; with
    it registered (after Task 1) the assertion PASSES.

    The teeth-proof in the SUMMARY MUST be concrete and string-exact, not loose prose:
    (a) Record the EXACT failure-message string the assertion emits on the unregistered state — and
        that string MUST contain the literal `Mailglass.Credo.StreamPolicyConsistent` enumerated in
        the "Defined-but-unregistered:" list (copy the actual rendered message, including the label,
        not a paraphrase). Because local mix may not run (toolchain caveat), you may construct this
        exact string from the test's `assert ... == []` message expression evaluated against the
        defined/registered sets in the unregistered state — but it MUST be the literal string the
        assertion would print, naming `Mailglass.Credo.StreamPolicyConsistent`.
    (b) State explicitly that the FIRST CI run after this plan lands is the GATING confirmation: the
        assertion is present and GREEN with all 17 checks registered (passing precisely because
        Task 1 registered the 17th). The teeth-proof is "documented exact-string on the negative
        state + CI-green on the positive state," not a manual local run.

    Do not leave the check unregistered and do not commit a skipped/xfail variant.
  </action>
  <acceptance_criteria>
    - `test/mailglass/credo/checks_have_tests_test.exs` contains TWO tests: the preserved test-existence test (still globs `credo_checks/*.ex` and asserts a matching `*_test.exs`) AND a new "every custom Credo check is registered in .credo.exs" test.
    - The new test references `Code.eval_file(".credo.exs")` and `Path.wildcard("credo_checks/*.ex")` and `Macro.camelize` and builds the registered-module set handling both `{mod, params}` and bare-atom entries (`grep -E "Code.eval_file|Macro.camelize|is_atom" test/mailglass/credo/checks_have_tests_test.exs` finds all three).
    - The new test's failure message uses a "Defined-but-unregistered:" label and enumerates each defined-but-unregistered module.
    - No allowlist / `@known_uncovered` token in the file (`grep -ci "known_uncovered\|allowlist" test/mailglass/credo/checks_have_tests_test.exs` returns 0).
    - TEETH proof recorded in the SUMMARY with string-exact evidence: (a) the EXACT failure-message string the assertion emits on the unregistered state is quoted verbatim and contains `Mailglass.Credo.StreamPolicyConsistent` in the "Defined-but-unregistered:" list; (b) the SUMMARY states the first CI run is the gating confirmation that the assertion is present and green with all 17 checks registered.
    - CI proof: `mix test test/mailglass/credo/checks_have_tests_test.exs` exits 0 (both tests pass, given Task 1 registered the 17th check).
  </acceptance_criteria>
  <verify>
    <automated>MISSING locally (toolchain caveat). CI proof: `mix test test/mailglass/credo/checks_have_tests_test.exs`. Source proof: file contains two tests; `grep -E "Code.eval_file|Macro.camelize" test/mailglass/credo/checks_have_tests_test.exs` finds both; `grep -ci "known_uncovered\|allowlist" test/mailglass/credo/checks_have_tests_test.exs` == 0; the existing `File.exists?` test-existence assertion is still present; the failure message uses the "Defined-but-unregistered:" label. SUMMARY proof: the exact unregistered-state failure string (naming StreamPolicyConsistent) is quoted, and CI-as-gating-confirmation is stated.</automated>
  </verify>
  <done>The meta-test detects BOTH a missing test AND a missing .credo.exs registration; an unregistered (inert) check now fails CI, closing the defining-goal blind spot, and the teeth proof is recorded as a string-exact unregistered-state failure message plus the first-CI-run gating confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: De-duplicate integration_test.exs against the live .credo.exs (WR-05)</name>
  <read_first>
    - test/mailglass/credo/integration_test.exs (the file under edit — the stale `@extra_checks` literal at lines 6-63, the `@check_cases` fixture corpus to KEEP, `params_for/1`, and the `length(@extra_checks) == 13` assertion to replace)
    - test/mailglass/credo/credo_config_sentinel_test.exs (the `Code.eval_file(".credo.exs")` + `load_checks/1` + `flatten_checks/1` + `find_check/2` pattern to MIRROR for sourcing live params)
    - .credo.exs (the live config now carrying StreamPolicyConsistent (Task 1), the `:mimemail`/`:gen_smtp_client` atom keys, and `required_root: [:mailglass, :mailglass_inbound]` — the real params the integration test must exercise)
  </read_first>
  <action>
    Replace the hardcoded `@extra_checks` literal in `integration_test.exs` with params sourced from
    the LIVE config. Mirror credo_config_sentinel_test.exs: in `setup_all` (or a module attribute
    computed once) do `{config, _binding} = Code.eval_file(".credo.exs")` and normalize
    `config.configs |> hd() |> Map.fetch!(:checks)` to a flat `{module, params}` list (reuse the
    `flatten_checks/1` shape that handles enabled/extra/disabled grouping). Rewrite `params_for/1`
    to look up a check's params from that live list (the existing `find_check/2` shape works).

    KEEP `@check_cases` (the synthetic bad/clean fixture sources per check) — that is the real
    coverage and is not duplicated config. The `synthetic violations` and `synthetic clean code`
    tests stay as-is except that `params_for/1` now returns LIVE params.

    Replace the brittle `assert length(@extra_checks) == 13` with a self-updating assertion that
    cannot drift: assert that every check module referenced by `@check_cases` is found in the live
    registered-checks list (so adding a check to `.credo.exs` does not silently leave the
    integration corpus stale, and a renamed/removed check is caught). Do NOT re-assert a hardcoded
    count.

    After this, the integration test exercises the SHIPPED params (e.g. TelemetryEventConvention's
    list-form `required_root`, NoBareOptionalDepReference's `:mimemail`/`:gen_smtp_client` keys) and
    cannot diverge from production config.
  </action>
  <acceptance_criteria>
    - `integration_test.exs` no longer carries a hand-maintained `@extra_checks` literal: `grep -c "@extra_checks" test/mailglass/credo/integration_test.exs` returns 0 (the literal is removed), and the file references `Code.eval_file(".credo.exs")`.
    - The stale singular `required_root: :mailglass` literal is gone: `grep -c "required_root: :mailglass" test/mailglass/credo/integration_test.exs` returns 0.
    - The hardcoded `length(... ) == 13` count assertion is replaced by a self-updating "every @check_cases module is registered in the live config" assertion (`grep -c "== 13" test/mailglass/credo/integration_test.exs` returns 0).
    - `@check_cases` (the fixture corpus) is preserved (`grep -c "@check_cases" test/mailglass/credo/integration_test.exs` returns >= 1).
    - CI proof: `mix test test/mailglass/credo/integration_test.exs` exits 0 — synthetic violations still trigger each check and clean code still passes, now under live params.
  </acceptance_criteria>
  <verify>
    <automated>MISSING locally (toolchain caveat). CI proof: `mix test test/mailglass/credo/integration_test.exs`. Source proof: `grep -c "@extra_checks" test/mailglass/credo/integration_test.exs` == 0; `grep -c "required_root: :mailglass" test/mailglass/credo/integration_test.exs` == 0; `grep -c "Code.eval_file" test/mailglass/credo/integration_test.exs` >= 1; `@check_cases` preserved.</automated>
  </verify>
  <done>integration_test.exs sources its check params from the live .credo.exs (no stale duplicate, no hardcoded count), so it cannot drift from production config and exercises the real CR-01/WR-02 params.</done>
</task>

</tasks>

<verification>
- `Mailglass.Credo.StreamPolicyConsistent` appears in `.credo.exs` `extra_checks` and runs under `mix credo`; `mix credo --strict` exits 0 with it active.
- The all-17-checks count holds: 17 credo_checks/*.ex files and 17 registered `Mailglass.Credo.*` modules in `.credo.exs`.
- Any `lib/` file touched to clear a StreamPolicyConsistent finding is recorded in the SUMMARY (auditable scope expansion).
- `checks_have_tests_test.exs` now asserts BOTH test existence AND .credo.exs registration; an unregistered check fails CI (teeth proof recorded in SUMMARY as a string-exact unregistered-state failure message + first-CI-run gating confirmation).
- `integration_test.exs` loads the live `.credo.exs` (no `@extra_checks` literal, no `== 13` count) and still passes its synthetic violation/clean cases.
- The NoBareOptionalDepReference GenSmtp-key comment is factually correct (CR-01 coverage on the atom keys).
- No PII, no public-API change, no GH-Action edits, no `credo_checks/*.ex` source edits in this plan (except an unavoidable, SUMMARY-recorded `lib/` mailable fix if StreamPolicyConsistent surfaces a real finding).
</verification>

<success_criteria>
The "claimed-but-inert custom Credo guard" defect class is self-detecting: every custom check is
both REGISTERED in `.credo.exs` AND covered by a test, and the meta-test fails CI if EITHER is
missing — proven by the registration assertion's exact unregistered-state failure message naming
`Mailglass.Credo.StreamPolicyConsistent` and the first CI run confirming it green with all 17 checks
registered. `mix credo --strict` is green, any lib/ scope expansion is auditable in the SUMMARY, and
the integration test exercises the live config rather than a stale duplicate.
</success_criteria>

<output>
Create `.planning/phases/45-inbound-telemetry-idempotency-foundation/45-10-SUMMARY.md` when done.
Record (a) the registration-assertion TEETH proof — the EXACT unregistered-state failure string
naming `Mailglass.Credo.StreamPolicyConsistent` in the "Defined-but-unregistered:" list, plus the
statement that the first CI run is the gating confirmation it is present and green with all 17 checks
registered; and (b) any `lib/` file touched to clear a StreamPolicyConsistent finding (or an explicit
statement that none was).
</output>
