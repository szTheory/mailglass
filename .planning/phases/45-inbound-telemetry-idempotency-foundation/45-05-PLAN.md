---
phase: 45-inbound-telemetry-idempotency-foundation
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .credo.exs
  - credo_checks/telemetry_event_convention.ex
  - test/mailglass/credo/no_bare_optional_dep_reference_test.exs
  - test/mailglass/credo/telemetry_event_convention_test.exs
autonomous: true
requirements: [MIME-02, TELE-06]
must_haves:
  truths:
    - "A bare `:mimemail.decode/1` outside the gateway is caught by NoBareOptionalDepReference (CR-01 closed)"
    - "A bare `:gen_smtp_client` call outside the gateway is caught by NoBareOptionalDepReference"
    - "An under-segmented or non-mailglass `:telemetry.span/3` prefix is caught by TelemetryEventConvention (WR-02 closed)"
    - "The behavior tests prove the ATOM-KEY param (CR-01) and the CONFIGURED inbound root (WR-02) are what activate the catch — not a default that happens to pass"
  artifacts:
    - path: ".credo.exs"
      provides: "gated_modules keyed on the real Erlang call-site atoms + span-aware convention check entry"
      contains: ":mimemail"
    - path: "credo_checks/telemetry_event_convention.ex"
      provides: "span-aware walk clause covering :telemetry.span/3"
      contains: ":telemetry, :span"
    - path: "test/mailglass/credo/no_bare_optional_dep_reference_test.exs"
      provides: "Erlang-atom regression cases for :mimemail / :gen_smtp_client with explicit gated_modules + a default-params negative control"
      contains: ":mimemail"
    - path: "test/mailglass/credo/telemetry_event_convention_test.exs"
      provides: "span fixture cases (pass/fail) run with explicit required_root: [:mailglass, :mailglass_inbound]"
      contains: ":telemetry.span"
  key_links:
    - from: ".credo.exs gated_modules"
      to: "bare :mimemail / :gen_smtp_client call sites"
      via: "NoBareOptionalDepReference Map.fetch on the Erlang atom"
      pattern: ":mimemail =>"
    - from: ".credo.exs TelemetryEventConvention"
      to: "inbound :telemetry.span/3 events"
      via: "span-aware walk clause"
      pattern: ":telemetry, :span"
---

<objective>
Close CR-01 (BLOCKER) and WR-02 (WARNING): make the two custom Credo guards actually
fire for their stated targets, with regression/fixture tests that pass for the RIGHT
reason — i.e. the explicit param that activates each catch is threaded into the check,
and a negative control proves the default does NOT catch.

- **CR-01:** `NoBareOptionalDepReference` keys `gated_modules` on the phantom alias
  `GenSmtp`, but gen_smtp is an Erlang lib reached only via the atoms `:mimemail`
  and `:gen_smtp_client`. `Map.fetch(gated_modules, :mimemail)` always misses, so a
  bare `:mimemail.decode(...)` anywhere in inbound passes `mix credo --strict`. Fix
  is config-only (the check code already resolves Erlang-atom roots): add the real
  atom keys to `.credo.exs`. Add the Erlang-atom regression test the old Oban-only
  probe never covered — threading explicit `gated_modules` AND an in-scope inbound
  path prefix, plus a negative control proving the default (empty) params miss.
- **WR-02:** `TelemetryEventConvention.walk/5` only matches `:telemetry.execute`.
  Every inbound event is `:telemetry.span/3`, so the widened
  `required_root: [:mailglass, :mailglass_inbound]` checks nothing. Add a second
  `walk/5` clause for `:telemetry.span/3` with the `min_segments - 1` threshold
  (the runtime appends `:start`/`:stop`/`:exception`). Add fixture cases run with the
  CONFIGURED `required_root: [:mailglass, :mailglass_inbound]` (the check's default
  root is the bare `:mailglass` atom, so an inbound-rooted case run with defaults
  would fail for the wrong reason).

Purpose: restore the MIME-02 lint enforcement invariant and the TELE-06 convention
coverage for inbound spans, and make both self-verifying for the configured behavior.
Output: corrected `.credo.exs` + span-aware convention check + two extended tests.

Note: the recurrence backstops (check-coverage meta-test, `.credo.exs` config sentinel)
and the two pre-existing uncovered-check regression tests now live in plan 45-09 (wave 3),
so each plan stays at the 2-3 task target and the meta-test runs only after every check
has its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/45-inbound-telemetry-idempotency-foundation/45-GAPS-RESEARCH.md
@.planning/phases/45-inbound-telemetry-idempotency-foundation/45-VERIFICATION.md

<interfaces>
<!-- Extracted contracts the executor needs — no codebase exploration required. -->

NoBareOptionalDepReference check (credo_checks/no_bare_optional_dep_reference.ex) — DO NOT EDIT, fix is config-only:
- `root_module/1` already handles the Erlang-atom path: `defp root_module(root) when is_atom(root), do: {:ok, root}` (line 105) returns `{:ok, :mimemail}` for a bare `:mimemail.decode`.
- `allowed_module?/2` keys on the enclosing `defmodule` (the gateway module name).
- The ONLY defect is missing map keys in `.credo.exs` gated_modules — `Map.fetch(gated_modules, :mimemail)` misses.
- VERIFIED param_defaults (lines 5-14): `gated_modules` has NO `:mimemail`/`:gen_smtp_client` key, and `included_path_prefixes` defaults to `["lib/mailglass/"]` ONLY (NOT `mailglass_inbound/lib/`). So a `:mimemail` case run with default params produces ZERO issues — the test MUST thread explicit `gated_modules` AND use an in-scope filename, and the negative control asserts the default path misses.

Existing .credo.exs gated_modules (lines ~29-35) — keyed on Elixir-module aliases:
  Oban => [Mailglass.OptionalDeps.Oban, MailglassInbound.OptionalDeps.Oban],
  OpenTelemetry => Mailglass.OptionalDeps.OpenTelemetry,
  Mjml => Mailglass.OptionalDeps.Mjml,
  GenSmtp => [Mailglass.OptionalDeps.GenSmtp, MailglassInbound.OptionalDeps.GenSmtp],
  Sigra => Mailglass.OptionalDeps.Sigra

Existing .credo.exs TelemetryEventConvention entry (lines ~53-54):
  {Mailglass.Credo.TelemetryEventConvention, [required_root: [:mailglass, :mailglass_inbound], min_segments: 4]}

TelemetryEventConvention check (credo_checks/telemetry_event_convention.ex) current shape:
- VERIFIED param_defaults (line 5): `required_root: :mailglass` (a single atom, NOT a list — `:mailglass_inbound` is NOT a default root) and `min_segments: 4`.
- `run/2` builds `required_roots = Params.get(:required_root) |> List.wrap()` and `min_segments`.
- `walk/5` clause (line 30) matches ONLY `{{:., _, [:telemetry, :execute]}, meta, [event_ast, _measurements, _metadata]}`.
- `literal_atom_list/1` returns `{:ok, list}` for a literal atom list, `:error` otherwise (a var prefix → `:error` → no issue).
- Issue produced via `root_issue/4` with `trigger: ":telemetry.execute"` and a message referencing `min_segments`.

Existing test idiom (test/mailglass/credo/no_bare_optional_dep_reference_test.exs + telemetry_event_convention_test.exs):
- `use ExUnit.Case, async: true`; `setup_all` does `Application.ensure_all_started(:credo)`.
- NoBare helper: `defp run_check(source, filename), do: source |> SourceFile.parse(filename) |> NoBareOptionalDepReference.run([])` — note it HARDCODES `run([])` (default params). New cases need a third params arg threaded into `run/2`.
- Telemetry helper: `defp run_check(source), do: source |> SourceFile.parse("test/mailglass/credo/telemetry_event_convention_fixture.ex") |> TelemetryEventConvention.run([])` — also HARDCODES `run([])` (default root `:mailglass`). New inbound-span cases need an explicit `required_root` param threaded in.
- The 2-arg `SourceFile.parse(source, filename)` passes the filename, so path-gating IS exercised — pass an in-scope filename for path-gated checks (NoBareOptionalDepReference is path-gated; TelemetryEventConvention is not).
- Custom-check tests live in `test/mailglass/credo/` (NOT `test/credo_checks/`). The check source files live in `credo_checks/`.
</interfaces>

@.credo.exs
@credo_checks/no_bare_optional_dep_reference.ex
@credo_checks/telemetry_event_convention.ex
@credo_checks/no_pii_in_telemetry_meta.ex
@test/mailglass/credo/no_bare_optional_dep_reference_test.exs
@test/mailglass/credo/telemetry_event_convention_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01 — key gated_modules on the real gen_smtp call-site atoms + Erlang-atom regression test (with default-params negative control)</name>
  <read_first>
    - .credo.exs (the NoBareOptionalDepReference entry — the file being modified; locate the `gated_modules` map inside the NoBareOptionalDepReference tuple and the explanatory comment above it)
    - credo_checks/no_bare_optional_dep_reference.ex (lines 5-14 param_defaults — CONFIRM defaults have no `:mimemail` key and `included_path_prefixes: ["lib/mailglass/"]`; lines 50-112, especially root_module/1 line 105 and allowed_module?/2 — confirm NO code change is needed; the Erlang-atom path already works)
    - test/mailglass/credo/no_bare_optional_dep_reference_test.exs (the analog/source-of-truth test; its `run_check/2` hardcodes `run([])` — you will add a params-threading arity, not replace the existing one)
    - lib/mailglass/optional_deps/gen_smtp.ex (the sanctioned gateway with the bare `:mimemail.decode/2` call inside `defmodule Mailglass.OptionalDeps.GenSmtp`; this is the ONE allowed bare call site — the enclosing module must keep passing)
  </read_first>
  <action>
    In `.credo.exs` add two new keys to the `gated_modules` map in the NoBareOptionalDepReference
    entry, mapping the actual Erlang call-site atoms to the sanctioned gateway module:
    `:mimemail => Mailglass.OptionalDeps.GenSmtp` and
    `:gen_smtp_client => Mailglass.OptionalDeps.GenSmtp`. KEEP the existing
    `GenSmtp => [Mailglass.OptionalDeps.GenSmtp, MailglassInbound.OptionalDeps.GenSmtp]` alias key
    (inbound `mime.ex` reaches the gateway via `alias Mailglass.OptionalDeps.GenSmtp, as: OptionalGenSmtp`,
    whose call root resolves to `GenSmtp` — that path must keep passing). Update the explanatory
    comment block above `gated_modules` to note that gen_smtp is keyed on its Erlang atoms because
    there is no `GenSmtp` Elixir module (per the gateway moduledoc). Do NOT touch
    `credo_checks/no_bare_optional_dep_reference.ex` — `root_module/1` (line 105) already returns
    `{:ok, :mimemail}` for the bare-atom path; the only bug was the missing map keys (reject the
    `root_module/1` generalization alternative — it duplicates working logic).

    Then extend `test/mailglass/credo/no_bare_optional_dep_reference_test.exs`. The existing
    `run_check/2` hardcodes `NoBareOptionalDepReference.run([])`; ADD a `run_check/3`
    (source, filename, params) that calls `SourceFile.parse(source, filename) |> NoBareOptionalDepReference.run(params)`
    so cases can pass explicit params. Keep the existing four tests using the original `run_check/2`
    (they pin the Oban path the old probe covered). For the new cases pass an explicit `gated_modules`
    param of `%{:mimemail => Mailglass.OptionalDeps.GenSmtp, :gen_smtp_client => Mailglass.OptionalDeps.GenSmtp}`
    so the test pins the ATOM-KEY behavior INDEPENDENT of `.credo.exs` drift (45-09's config sentinel
    pins the config; this test pins the check behavior).

    CRITICAL filename trap: the check is path-gated via `included_path_prefixes` (default
    `["lib/mailglass/"]`), so each fixture MUST use an in-scope filename — use a `lib/mailglass/...`
    path (the default-covered prefix) so the path gate passes regardless of how the inbound prefix is
    configured. A default/out-of-scope filename makes the check silently inert and the test would pass
    for the wrong reason.

    Cases:
    (a) a non-gateway module with a bare `:mimemail.decode(raw)` call, filename
        `"lib/mailglass/inbound_bad_mime.ex"`, run via `run_check/3` with the explicit `gated_modules`
        param → `assert length(issues) == 1` and the message mentions `Mailglass.OptionalDeps.GenSmtp`;
    (b) the same `:mimemail.decode(raw)` call but inside `defmodule Mailglass.OptionalDeps.GenSmtp`,
        filename `"lib/mailglass/optional_deps/gen_smtp.ex"`, run via `run_check/3` with the explicit
        `gated_modules` param → `assert run == []` (sanctioned site);
    (c) a non-gateway module with a bare `:gen_smtp_client.send(x)` call, filename
        `"lib/mailglass/inbound_bad_smtp.ex"`, run via `run_check/3` with the explicit `gated_modules`
        param → `assert length(issues) == 1`;
    (d) NEGATIVE CONTROL — the SAME source/filename as case (a) but run via the existing `run_check/2`
        (default empty params, whose default `gated_modules` has no `:mimemail` key) →
        `assert run_check(source, "lib/mailglass/inbound_bad_mime.ex") == []`, proving the atom-key
        param is what activates the catch (the default misses).
  </action>
  <acceptance_criteria>
    - `.credo.exs` `gated_modules` contains literal keys `:mimemail => Mailglass.OptionalDeps.GenSmtp` and `:gen_smtp_client => Mailglass.OptionalDeps.GenSmtp`, AND retains the `GenSmtp => [...]` alias key.
    - `credo_checks/no_bare_optional_dep_reference.ex` is byte-for-byte unchanged (git diff shows no change to that file).
    - `test/mailglass/credo/no_bare_optional_dep_reference_test.exs` contains a new `run_check/3` arity that threads explicit params; cases (a)/(c) pass `gated_modules: %{:mimemail => ..., :gen_smtp_client => ...}` AND in-scope `lib/mailglass/...` filenames and each assert exactly one issue; case (b) asserts zero issues at the gateway; case (d) is a negative control asserting the SAME `:mimemail` source run with DEFAULT params (existing `run_check/2`) yields `== []`.
    - The four pre-existing tests are unchanged and still use the original `run_check/2`.
    - In CI (`mix test test/mailglass/credo/no_bare_optional_dep_reference_test.exs`): all cases pass — the explicit-param `:mimemail` case produces an issue, the default-param control produces none, the gateway case produces none.
    - In CI (`mix credo --strict`): green (no real bare `:mimemail`/`:gen_smtp_client` exists today; the gateway is recognized).
  </acceptance_criteria>
  <verify>
    <automated>MISSING in local tree (inbound deps unfetched; local Elixir 1.19 vs CI 1.18 — local `mix` is not the source of truth per the toolchain caveat). CI proof: `mix test test/mailglass/credo/no_bare_optional_dep_reference_test.exs` (credo_strict job + a credo-check test run). Source proof: grep `.credo.exs` for the two new atom keys; `git diff --exit-code credo_checks/no_bare_optional_dep_reference.ex`; grep the test for `run_check(source, filename, params)`/3, the explicit `%{:mimemail =>` param, and the `== []` negative-control assertion.</automated>
  </verify>
  <done>CR-01 closed: gated_modules keyed on `:mimemail`/`:gen_smtp_client`; a bare reference outside the gateway now raises a NoBareOptionalDepReference issue, asserted by a regression test that threads the explicit atom-key param with in-scope filenames AND a negative control proving the default misses; the check source is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-02 — add a span-aware walk clause to TelemetryEventConvention + fixture tests run with the configured inbound root</name>
  <read_first>
    - credo_checks/telemetry_event_convention.ex (the file being modified — full file; note param_defaults line 5 `required_root: :mailglass`, walk/5 line 30 matches only `:telemetry.execute`, root_issue/4 line 56, literal_atom_list/1 line 69)
    - credo_checks/no_pii_in_telemetry_meta.ex (the proven dual-clause pattern that already handles BOTH `:telemetry.execute` and `:telemetry.span` — mirror this clause structure)
    - test/mailglass/credo/telemetry_event_convention_test.exs (the analog test; its `run_check/1` hardcodes `run([])`, i.e. default root `:mailglass` — you will add a params-threading arity for inbound-rooted cases)
    - .credo.exs (the configured `required_root: [:mailglass, :mailglass_inbound], min_segments: 4` — the span threshold is `min_segments - 1`, i.e. 3)
  </read_first>
  <action>
    In `credo_checks/telemetry_event_convention.ex` add a SECOND `walk/5` clause matching
    `{{:., _, [:telemetry, :span]}, meta, [event_ast, _metadata, _fun]}` (span is arity 3:
    event prefix, start metadata, fun). The span event PREFIX is one segment shorter than the
    emitted event because the runtime appends `:start`/`:stop`/`:exception`, so validate
    `length(event) >= min_segments - 1` (i.e. `>= 3` for the configured `min_segments: 4`) AND
    `root in required_roots`. Refactor the shared root/length validation out of the existing
    `:telemetry.execute` clause into a private `validate/_` helper parameterized by the threshold
    (execute uses `min_segments`, span uses `min_segments - 1`) so the two clauses share logic and
    can't drift. Non-literal prefixes (a var) → `literal_atom_list/1` returns `:error` → no issue
    (preserve the execute clause's false-positive avoidance). For the span path set
    `trigger: ":telemetry.span"` on the issue (accurate editor jump-to); keep the message reporting
    `min_segments` (operators reason in final event-name terms, not prefix length). Update the
    moduledoc `check:` explanation to state the 4-level convention is enforced for BOTH
    `:telemetry.execute` and `:telemetry.span` events.

    Then extend `test/mailglass/credo/telemetry_event_convention_test.exs`. The existing `run_check/1`
    hardcodes `run([])` (default root is the bare `:mailglass` atom — `:mailglass_inbound` is NOT a
    default root, so an inbound-rooted case run with defaults would FAIL for the wrong reason). ADD a
    `run_check/2` (source, params) that calls
    `SourceFile.parse(source, "...fixture.ex") |> TelemetryEventConvention.run(params)`, and run the
    inbound-rooted span cases with the CONFIGURED params
    `[required_root: [:mailglass, :mailglass_inbound], min_segments: 4]` (mirroring real `.credo.exs`).
    Keep the existing three tests on the original `run_check/1`.
    Cases (all via `run_check/2` with the configured params unless noted):
    (a) 3-segment span prefix `[:mailglass_inbound, :ingress, :request]` → `assert run == []` (passes — runtime appends a 4th);
    (b) 2-segment span prefix `[:mailglass_inbound, :ingress]` → `assert length(issues) == 1`;
    (c) non-mailglass span root `[:my_app, :ingress, :request]` → `assert length(issues) == 1`;
    (d) (regression for the execute path, configured params) under-segmented `:telemetry.execute([:mailglass, :a, :b], ...)` → one issue;
    (e) (regression, configured params) 4-segment `:telemetry.execute([:mailglass, :a, :b, :stop], ...)` → `assert run == []`.
  </action>
  <acceptance_criteria>
    - `credo_checks/telemetry_event_convention.ex` has a `walk/5` clause matching `[:telemetry, :span]` with threshold `min_segments - 1`, and a shared `validate` helper used by both clauses.
    - The span clause sets `trigger: ":telemetry.span"`; the message still references `min_segments`.
    - Var-prefix span calls produce no issue (no false positive).
    - `test/mailglass/credo/telemetry_event_convention_test.exs` has a new `run_check/2` arity; the inbound span cases run with explicit `required_root: [:mailglass, :mailglass_inbound], min_segments: 4` (NOT defaults); the three pre-existing tests still use the original `run_check/1`.
    - In CI (`mix test test/mailglass/credo/telemetry_event_convention_test.exs`): the five new cases pass — a 3-seg `:mailglass_inbound` span prefix passes ONLY because the configured root includes `:mailglass_inbound`; a 2-seg span prefix and a non-mailglass span root both raise exactly one issue.
    - In CI (`mix credo --strict`): green (the real inbound spans are all ≥3-seg `:mailglass_inbound`-rooted prefixes — telemetry.ex helpers).
  </acceptance_criteria>
  <verify>
    <automated>MISSING locally (toolchain caveat — local `mix` not the source of truth). CI proof: `mix test test/mailglass/credo/telemetry_event_convention_test.exs` + `mix credo --strict`. Source proof: grep the check for a `[:telemetry, :span]` walk clause and the `min_segments - 1` threshold; grep the test for the new `run_check/2` arity and the explicit `required_root: [:mailglass, :mailglass_inbound]` param on the inbound span cases.</automated>
  </verify>
  <done>WR-02 closed: TelemetryEventConvention now inspects `:telemetry.span/3` prefixes against the inbound roots with the correct off-by-one threshold, locked by pass/fail fixture tests that pass the CONFIGURED `required_root` (proving the widened config — not the default — is what makes the inbound prefix pass); the widened `required_root` is no longer inert for inbound spans.</done>
</task>

</tasks>

<verification>
- `.credo.exs` `gated_modules` keys gen_smtp on `:mimemail` and `:gen_smtp_client` (atom keys), retaining the `GenSmtp` alias key.
- `credo_checks/telemetry_event_convention.ex` has a `:telemetry.span/3` walk clause with the `min_segments - 1` threshold; `credo_checks/no_bare_optional_dep_reference.ex` is unchanged.
- The two regression tests pass for the RIGHT reason: NoBare threads explicit `gated_modules` (with a default-params negative control); Telemetry threads the configured inbound `required_root`.
- CI `credo_strict` job is green and the credo-check test run passes (the proof, per the toolchain caveat — not a local `mix` run).
- No PII, no public-API change, no GH-Action edits in this plan.
</verification>

<success_criteria>
CR-01 and WR-02 are closed AND test-verified for the configured behavior: a bare
`:mimemail`/`:gen_smtp_client` reference outside the gateway raises a Credo issue when the
atom-key param is present (and provably does NOT under defaults), and an under-segmented or
wrong-rooted inbound `:telemetry.span/3` prefix raises a Credo issue when the inbound root is
configured. The recurrence backstops live in 45-09.
</success_criteria>

<output>
Create `.planning/phases/45-inbound-telemetry-idempotency-foundation/45-05-SUMMARY.md` when done.
</output>
