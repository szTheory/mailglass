---
phase: 45-inbound-telemetry-idempotency-foundation
plan: 08
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/mime.ex
  - mailglass_inbound/test/mailglass_inbound/mime_test.exs
  - lib/mailglass/optional_deps/gen_smtp.ex
autonomous: true
requirements: [MIME-01, MIME-04]
must_haves:
  truths:
    - "mime.ex moduledoc accurately states the max_depth guard bounds the representation walk, not decoder recursion, with Phase 46 deferral noted"
    - "The mime.ex contract bullet no longer claims boundary-bomb defense; it states the representation exceeded :max_depth"
    - "The mime_test describe no longer claims 'boundary-bomb' (renamed to representation max_depth guard); test bodies (never-raise/MIME-04) unchanged"
    - "gen_smtp.ex moduledoc attributes :undef to rescue (not catch :exit)"
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/mime.ex"
      provides: "honest moduledoc admonition + corrected contract bullet (guard kept)"
      contains: "Note"
    - path: "mailglass_inbound/test/mailglass_inbound/mime_test.exs"
      provides: "renamed describe (no boundary-bomb claim)"
      contains: "max_depth"
    - path: "lib/mailglass/optional_deps/gen_smtp.ex"
      provides: "corrected :undef-under-rescue attribution"
      contains: "rescue"
  key_links:
    - from: "mime.ex moduledoc/contract"
      to: "the actual collect_leaves max_depth behavior"
      via: "honest claim pointing at the never-raise test (not a non-existent DoS test)"
      pattern: "max_depth"
---

<objective>
Close the DOC honesty findings (Info): two moduledocs assert protections the code does
not deliver. The underlying threats stay deferred (boundary-bomb DoS hardening →
Phase 46); only the doc/test CLAIMS are corrected here. Doc/test-only — no behavior change.

- **mime.ex depth guard — KEEP, RELABEL:** `decode_and_build/2` calls
  `OptionalGenSmtp.decode(raw)` FIRST (mime.ex:128), which fully parses to any depth
  (`:mimemail` has no recursion limit), THEN `collect_leaves/3` walks the already-built
  tree and throws `@depth_exceeded` at `depth > max_depth` (mime.ex:173). So the guard
  bounds the INTERNAL REPRESENTATION walk, not decoder recursion — it does not stop the
  boundary-bomb it is documented (mime.ex:100-102 + contract bullet 19-22) to defend.
  Keep the guard (it gives a deterministic ceiling on the repr the pipeline iterates and
  is the seam Phase 46's real decoder limit plugs into), but correct the docs.
- **gen_smtp.ex moduledoc — fix `:undef` attribution:** when `:mimemail` is absent the
  call raises a class-`:error` `:undef` → `UndefinedFunctionError` → caught by `rescue`,
  NOT `catch :exit`. The current moduledoc (lines 30-33) wrongly bundles `:undef` under
  `catch :exit`.

Purpose: satisfy the honesty constraint — moduledocs/tests describe what the code truly
does. Aligns with the recurrence-guardrail convention: a "protects against X" claim must
point at a test named for X, or be downgraded to "bounds Y" + a Phase-N deferral note.
Output: honest mime.ex moduledoc + corrected contract bullet + renamed test describe +
corrected gen_smtp.ex moduledoc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/45-inbound-telemetry-idempotency-foundation/45-GAPS-RESEARCH.md
@.planning/phases/45-inbound-telemetry-idempotency-foundation/45-VERIFICATION.md

<interfaces>
<!-- Contracts the executor needs — extracted; no exploration required. -->

mime.ex current claims to correct:
- Contract bullet (mime.ex:19-22): "...could not be parsed (any of the three escape mechanisms, or the deep-nesting guard tripped)." → the "deep-nesting guard" phrasing overclaims DoS defense.
- Options doc (mime.ex:100-102): "`:max_depth` — maximum multipart nesting depth before the boundary-bomb guard trips and returns `:inbound_mime_invalid`". → "boundary-bomb guard" overclaims.
- Actual behavior (mime.ex:125-152, 157-193): `decode_and_build/2` calls `OptionalGenSmtp.decode(raw)` FIRST (full parse, no limit) at line 128, THEN `to_internal/2` → `collect_leaves/3` (line 157+) which `throw(@depth_exceeded)` at `depth > max_depth` (line 173) while re-walking the ALREADY-decoded tree. So the guard bounds the representation walk, not decoder recursion.
- The behavior is CORRECT and KEPT (deterministic ceiling on the iterated repr; Phase 46 plugs the real decoder limit into this seam). Only docs change.

mime_test.exs describe to rename (mime_test.exs:116): `describe "parse/2 — boundary-bomb / deep-nesting guard (T-45-12, V5)" do` — the test BODIES (lines 117-130: deeply_nested_multipart, assert {:error, %MIMEError{type: :inbound_mime_invalid}}, "nesting within max_depth still parses") assert never-raise/MIME-04, which is TRUE — keep them; only the describe string is wrong.

gen_smtp.ex moduledoc to correct (lib/mailglass/optional_deps/gen_smtp.ex:19-37): the "## MIME parse seam — never raises" three-mechanism list currently bundles `:undef` under `catch :exit` (lines 30-33). Actual: when `:mimemail` is absent the call raises class-`:error` `:undef` (`UndefinedFunctionError`) → absorbed by `rescue` (line 73-74), surfaced `{:error, {:error, exception}}`. `catch :exit` (lines 76-77) absorbs the iconv EXIT signal only (and that path is defensive since `{:encoding, :none}` skips iconv). The `decode/2` code itself is correct and UNCHANGED — only the moduledoc prose.
</interfaces>

@mailglass_inbound/lib/mailglass_inbound/mime.ex
@lib/mailglass/optional_deps/gen_smtp.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the mime.ex moduledoc + contract bullet (keep the guard) and rename the test describe</name>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/mime.ex (lines 1-123: moduledoc + the `parse/2` options doc at 96-106; lines 125-193: decode_and_build/2 + collect_leaves/3 — confirm decode-first-then-walk ordering so the corrected docs are accurate)
    - mailglass_inbound/test/mailglass_inbound/mime_test.exs (lines 116-131: the describe to rename + the test bodies that must stay)
    - .planning/phases/45-inbound-telemetry-idempotency-foundation/45-GAPS-RESEARCH.md (the DOC section, lines ~92-94: the exact correction intent)
  </read_first>
  <action>
    In `mailglass_inbound/lib/mailglass_inbound/mime.ex`:
    1. Add an ExDoc admonition to the moduledoc — a `> #### Note {: .info}` block — stating precisely
       what the `:max_depth` guard does and does not do: it bounds the depth of the INTERNAL
       REPRESENTATION walk (`collect_leaves/3` over the already-decoded tree), giving a deterministic
       structured ceiling on what the pipeline iterates; it does NOT limit the underlying
       `:mimemail` decoder recursion (the decoder fully parses to any depth before the guard runs), so
       it does NOT by itself defend against provider-fed deep-nesting (boundary-bomb) DoS. Note that
       provider-fed DoS hardening (a real decoder-level limit) is a Phase 46 concern that will plug
       into this same seam. Keep the guard and its `:max_depth` option.
    2. Fix the contract bullet (mime.ex:19-22): change "or the deep-nesting guard tripped" to "or the
       representation exceeded `:max_depth`" so the `:inbound_mime_invalid` cause list is accurate.
    3. Fix the `:max_depth` option doc (mime.ex:100-102): replace "before the boundary-bomb guard
       trips" with "before the representation-depth guard trips" (and keep the default note).
    Do NOT change any code in mime.ex (decode_and_build/2, collect_leaves/3, the @depth_exceeded
    sentinel all stay) — this is doc-only.

    In `mailglass_inbound/test/mailglass_inbound/mime_test.exs`: rename the describe at line 116 from
    `"parse/2 — boundary-bomb / deep-nesting guard (T-45-12, V5)"` to
    `"parse/2 — representation max_depth guard (MIME-04)"` (drop the "boundary-bomb" claim; the bodies
    assert never-raise/MIME-04, which is true). Do NOT change the test bodies.
  </action>
  <acceptance_criteria>
    - mime.ex moduledoc contains a `> #### Note {: .info}` admonition stating the guard bounds the representation walk (not decoder recursion) and that provider-fed DoS hardening is Phase 46.
    - mime.ex contract bullet reads "or the representation exceeded `:max_depth`" (no "deep-nesting guard tripped"); the `:max_depth` option doc no longer says "boundary-bomb guard".
    - No code change in mime.ex (decode_and_build/2, collect_leaves/3, @depth_exceeded unchanged).
    - mime_test.exs describe at line 116 no longer contains "boundary-bomb"; the describe references max_depth/MIME-04; the test bodies are unchanged.
    - In CI (`cd mailglass_inbound && mix test mailglass_inbound/test/mailglass_inbound/mime_test.exs`): the renamed describe's tests still pass; (`mix docs --warnings-as-errors` on inbound) the admonition renders without warnings.
  </acceptance_criteria>
  <verify>
    <automated>MISSING locally (inbound deps unfetched; toolchain caveat). CI proof: `cd mailglass_inbound && mix test` (mime_test) on inbound_test job; `mix docs --warnings-as-errors` (docs job). Source proof: grep mime.ex for `Note {: .info}` and absence of "boundary-bomb" in moduledoc/options; grep mime_test.exs to confirm "boundary-bomb" removed from the describe and `git diff` shows no body changes.</automated>
  </verify>
  <done>The mime.ex docs honestly describe the representation-depth guard and defer real DoS hardening to Phase 46; the test describe no longer overclaims; behavior is unchanged and never-raise (MIME-04) still holds.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the gen_smtp.ex moduledoc :undef attribution</name>
  <read_first>
    - lib/mailglass/optional_deps/gen_smtp.ex (lines 19-37: the "## MIME parse seam — never raises" three-mechanism list, especially lines 30-33 bundling `:undef` under `catch :exit`; lines 70-78: the actual `decode/2` rescue/catch clauses confirming `:undef` (UndefinedFunctionError) is a class-`:error` raise caught by `rescue`)
    - .planning/phases/45-inbound-telemetry-idempotency-foundation/45-GAPS-RESEARCH.md (the DOC section, line ~96: the exact reword intent)
  </read_first>
  <action>
    In `lib/mailglass/optional_deps/gen_smtp.ex` moduledoc, reword the three-mechanism list so each
    mechanism is attributed to the clause that actually absorbs it:
    - `rescue` absorbs raised errors (`erlang:error`: `no_boundary`, `missing_boundary`,
      `unterminated_quotes`, etc.) AND the `:undef` backstop — when `decode/2` is reached without the
      `available?/0` gate and `:mimemail` is absent, the call raises a class-`:error`
      `UndefinedFunctionError` (`:undef`), which `rescue` catches; surfaced as
      `{:error, {:error, exception}}`. (The normal degraded path returns
      `:gen_smtp_unavailable` upstream via the availability gate before `decode/2` is called.)
    - `catch :throw` absorbs thrown reasons (`bad_content_type`, `bad_disposition`, `badchar`).
    - `catch :exit` absorbs EXIT signals only — the `iconv:convert/3` exit when `:iconv` is absent;
      this is itself defensive because the mandatory `{:encoding, :none}` opt skips iconv.
    Move `:undef` out of the `catch :exit` bullet and into the `rescue` bullet. Do NOT change the
    `decode/2` code (the rescue + catch :throw + catch :exit clauses are correct). Keep the
    "all three are load-bearing for the never-raise contract (MIME-04)" closing note.
  </action>
  <acceptance_criteria>
    - gen_smtp.ex moduledoc attributes `:undef`/`UndefinedFunctionError` to `rescue` (not `catch :exit`); the `catch :exit` bullet references only the iconv exit signal.
    - No code change to `decode/2` (rescue + catch :throw + catch :exit clauses unchanged).
    - In CI (`mix docs --warnings-as-errors`): green; (`mix test`) existing gen_smtp tests pass (no behavior change).
  </acceptance_criteria>
  <verify>
    <automated>MISSING locally (toolchain caveat). CI proof: `mix docs --warnings-as-errors` (docs job) + `mix test` (gen_smtp tests). Source proof: grep gen_smtp.ex moduledoc to confirm `:undef` is described under `rescue` and the `catch :exit` bullet no longer lists `:undef`; `git diff` shows no change below the moduledoc.</automated>
  </verify>
  <done>The gen_smtp gateway moduledoc correctly attributes the `:undef` backstop to `rescue`; documentation accuracy restored with no behavior change; the never-raise contract still holds.</done>
</task>

</tasks>

<verification>
- mime.ex moduledoc has an honest `> #### Note {: .info}` admonition; contract bullet + option doc no longer claim boundary-bomb defense; no code change.
- mime_test.exs describe renamed (no "boundary-bomb"); test bodies unchanged.
- gen_smtp.ex moduledoc attributes `:undef` to `rescue`; `decode/2` code unchanged.
- Doc/test-only: no behavior change, no public-API change, never-raise (MIME-04) preserved.
- Underlying DoS hardening stays deferred to Phase 46 (not pulled into scope).
</verification>

<success_criteria>
Both moduledocs and the affected test describe accurately describe what the code does: the representation-depth guard is honestly labeled with a Phase 46 deferral note, and the `:undef` backstop is attributed to `rescue`. No behavior changes.
</success_criteria>

<output>
Create `.planning/phases/45-inbound-telemetry-idempotency-foundation/45-08-SUMMARY.md` when done.
</output>
