---
phase: 112-app-shell-navigation-tenant-seam
plan: 04
type: execute
wave: 4
depends_on:
  - 112-03
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/test/mailglass_admin/components_test.exs
  - mailglass_admin/test/mailglass_admin/operator/shell_test.exs
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - SHELL-05
must_haves:
  truths:
    - "Desktop and mobile shell navigation expose `aria-current=\"page\"` for the active page."
    - "Active nav state includes a visible non-color cue at both nav levels per D-09."
    - "The cue is implemented in shared primitives, not page-local nav copies."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "Shared nav active cue classes"
    - path: "mailglass_admin/test/mailglass_admin/components_test.exs"
      provides: "Component proof for active nav non-color cues"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "Shell renders Components.nav_link and Components.nav_pill"
      pattern: "Components\\.nav_(link|pill)"
---

<objective>
Strengthen active navigation cues in shared primitives so current state is clear without relying on color alone.

Purpose: operators under stress need an obvious current section indicator on both desktop and mobile chrome.
Output: updated `nav_link/1` and `nav_pill/1` active classes plus component and shell assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-app-shell-navigation-tenant-seam/112-CONTEXT.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-UI-SPEC.md
@.planning/phases/110-primitives/110-04-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/test/mailglass_admin/components_test.exs
@mailglass_admin/test/mailglass_admin/operator/shell_test.exs
</context>

## Artifacts this phase produces

- Shared active nav primitive cue that is visible without color perception.
- Component and shell tests for desktop/mobile current-state semantics.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active nav non-color cue tests</name>
  <files>mailglass_admin/test/mailglass_admin/components_test.exs, mailglass_admin/test/mailglass_admin/operator/shell_test.exs</files>
  <read_first>
    @mailglass_admin/test/mailglass_admin/components_test.exs
    @mailglass_admin/test/mailglass_admin/operator/shell_test.exs
    @mailglass_admin/lib/mailglass_admin/components.ex
  </read_first>
  <behavior>
    - Active `nav_link` has `aria-current="page"` and a structural cue such as border-left plus bold text.
    - Active `nav_pill` has `aria-current="page"` and a structural cue such as border-bottom/left/marker plus bold text.
    - Inactive nav items do not carry the active structural cue.
  </behavior>
  <action>Add component tests for `Components.nav_link/1` and `nav_pill/1` active/inactive class contracts and shell tests proving desktop and mobile active items are both rendered through the shared primitives. Browser-level structural geometry proof belongs to Plan 112-06 final integration. This implements D-09.</action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/components_test.exs mailglass_admin/test/mailglass_admin/operator/shell_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Tests fail if active navigation is represented only by background/text color or `aria-current` with no visible structural cue.</acceptance_criteria>
  <done>Tests fail if active navigation is represented only by background/text color or `aria-current` with no visible structural cue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update shared nav primitives and compiled CSS</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex, mailglass_admin/priv/static/app.css</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/components.ex
    @mailglass_admin/assets/css/app.css
    @mailglass_admin/scripts/check-conformance.sh
  </read_first>
  <behavior>
    - `nav_link_class(true)` and `nav_pill_class(true)` include visible geometry beyond color/tint.
    - Both primitives retain 44px target floor and `mg-focus-ring`.
    - The shipped CSS bundle is rebuilt if class changes alter generated CSS.
  </behavior>
  <action>Update only `MailglassAdmin.Components` nav primitive class helpers. Keep `nav_link/1` and `nav_pill/1` as the sole implementation surface; do not add shell-local active markup. Preserve `aria-current`, `min-h-11`, focus-ring utilities, disabled semantics, and hover behavior. Use tokenized classes only. Rebuild `mailglass_admin/priv/static/app.css` through the existing asset command if the class set changes the compiled bundle.</action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/components_test.exs mailglass_admin/test/mailglass_admin/operator/shell_test.exs --warnings-as-errors && cd mailglass_admin && ./scripts/check-conformance.sh && git diff --exit-code mailglass_admin/priv/static/app.css</automated>
  </verify>
  <acceptance_criteria>Active desktop and mobile nav cues are visibly structural, shared through primitives, accessible, and present in the compiled bundle.</acceptance_criteria>
  <done>Active desktop and mobile nav cues are visibly structural, shared through primitives, accessible, and present in the compiled bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView route state -> nav current state | Current page state controls visual and ARIA nav output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-09 | Spoofing | active nav UI | mitigate | Emit `aria-current="page"` and a visible non-color cue only for the active surface passed by the shell. |
| T-112-10 | Denial of Service | compiled CSS drift | mitigate | Rebuild and verify `priv/static/app.css` after class changes. |
| T-112-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `mix test mailglass_admin/test/mailglass_admin/components_test.exs mailglass_admin/test/mailglass_admin/operator/shell_test.exs --warnings-as-errors`, then `cd mailglass_admin && ./scripts/check-conformance.sh`.
</verification>

<success_criteria>
SHELL-05 is satisfied through shared primitives and tested at component and shell levels; final browser proof is planned in 112-06.
</success_criteria>

<output>
Create `.planning/phases/112-app-shell-navigation-tenant-seam/112-04-SUMMARY.md` when done.
</output>
