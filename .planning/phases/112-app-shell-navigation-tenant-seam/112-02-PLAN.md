---
phase: 112-app-shell-navigation-tenant-seam
plan: 02
type: execute
wave: 2
depends_on:
  - 112-01
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/test/mailglass_admin/operator/shell_test.exs
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
must_haves:
  truths:
    - "Exactly one accessible tenant canonicalizes to a URL carrying tenant_id per D-03 and D-04."
    - "Two or more accessible tenants render a selector/switcher instead of the old no-tenant dead end per D-03."
    - "Tenant scope survives filters, clear/back actions, surface switches, and copied URLs per D-04/D-05."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      provides: "Tenant selector/switcher shell composition and tenant-aware paths"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Delivery surface tenant-state flow"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Inbound surface tenant-state flow"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/tenants.ex"
      via: "operator_actor passed to MailglassAdmin.Operator.Tenants.list_tenants/2"
      pattern: "Operator\\.Tenants\\.list_tenants"
    - from: "mailglass_admin/lib/mailglass_admin/operator/shell.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "tenant selector emits URL patch preserving current surface"
      pattern: "tenant_id"
---

<objective>
Wire the admin shell to the core tenant selector and make tenant scope durable URL state across operator and inbound surfaces.

Purpose: this kills the no-tenant dead end, hides the pointless single-tenant picker, and preserves tenant scope across normal operator navigation.
Output: tenant-state assignments, auto-select URL canonicalization, multi-tenant switcher rendering, and LiveView regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-app-shell-navigation-tenant-seam/112-CONTEXT.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-RESEARCH.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-UI-SPEC.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md
@.planning/phases/111-forms/111-04-SUMMARY.md
@lib/mailglass/operator/tenants.ex
@mailglass_admin/lib/mailglass_admin/operator/tenants.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/inbound_live.ex
@mailglass_admin/test/mailglass_admin/operator_live_test.exs
@mailglass_admin/test/mailglass_admin/inbound_live_test.exs
</context>

## Artifacts this phase produces

- Shell-level tenant state contract for selected, auto-select, selector-required, and no-access states.
- Multi-tenant switcher UI using shell primitives and exact UI-SPEC copy.
- Regression tests for tenant scope persistence across operator and inbound actions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tenant-state LiveView regression tests</name>
  <files>mailglass_admin/test/mailglass_admin/operator/shell_test.exs, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    @mailglass_admin/test/mailglass_admin/operator/shell_test.exs
    @mailglass_admin/test/mailglass_admin/operator_live_test.exs
    @mailglass_admin/test/mailglass_admin/inbound_live_test.exs
    @mailglass_admin/test/support/endpoint_case.ex
  </read_first>
  <behavior>
    - Sole accessible tenant with no URL tenant patches to the same surface with `tenant_id`.
    - Two accessible tenants with no URL tenant renders a tenant selector/list with `Select tenant`.
    - Clear filters preserves the selected tenant on operator and inbound surfaces.
    - Detail back actions and cross-surface nav preserve tenant id and drop selected record ids.
  </behavior>
  <action>Add focused tests before implementation. Use existing helper functions and seed records under one or two tenant ids. Assert URL patches, not only assigns, for sole-tenant auto-select per D-03/D-04. Assert the multi-tenant state contains the UI-SPEC copy `Select a tenant`, `Choose a tenant to inspect its deliveries and inbound routing`, and no old instruction to manually add a `tenant_id` query. Add shell path tests for tenant switch URLs that keep the active surface and remove `delivery_id`/`inbound_id` when switching tenants. This covers D-03, D-04, and D-05.</action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/operator/shell_test.exs mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>The tests fail against current passive tenant-chip behavior and document every tenant URL persistence case named in 112-VALIDATION.md.</acceptance_criteria>
  <done>The tests fail against current passive tenant-chip behavior and document every tenant URL persistence case named in 112-VALIDATION.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin tenant auto-select and switcher state</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/operator/shell.ex
    @mailglass_admin/lib/mailglass_admin/operator_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @mailglass_admin/lib/mailglass_admin/components.ex
    @mailglass_admin/lib/mailglass_admin/operator/tenants.ex
  </read_first>
  <behavior>
    - `operator_actor` is passed to `MailglassAdmin.Operator.Tenants.list_tenants/2` before surface data loads.
    - Exactly one tenant and no `tenant_id` triggers a canonical `push_patch` to the current surface URL with that tenant id.
    - Multi-tenant unscoped state renders a selector and suppresses normal list/detail reads until a tenant is selected.
  </behavior>
  <action>In both LiveViews, resolve tenant state before `assign_delivery_state/3`, `assign_overview_state/2`, or `assign_inbound_state/3`. Add assigns such as `:tenant_options`, `:tenant_state`, and `:selected_tenant_id` if useful. Auto-select exactly one accessible tenant by patching the URL with `tenant_id`; do not assign invisible tenant state only. For two or more accessible tenants and blank URL tenant, render a shell/body selector using escaped tenant labels, `Primary CTA` copy `Select tenant`, and tenant switch links that preserve the current surface while removing incompatible selected ids. For zero tenants, render `No tenants available` copy from the UI spec. Do not add tenant CRUD, invites, pins, favorites, cross-tenant aggregate views, raw admin Repo queries, or fixture-derived tenant lists. Implement D-01 through D-05.</action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/operator/shell_test.exs mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Operator and inbound surfaces auto-select the sole tenant via URL, show a switcher for multi-tenant unscoped state, and preserve tenant id through filters, clear, back, surface nav, and refresh/copy URL flows.</acceptance_criteria>
  <done>Operator and inbound surfaces auto-select the sole tenant via URL, show a switcher for multi-tenant unscoped state, and preserve tenant id through filters, clear, back, surface nav, and refresh/copy URL flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> LiveView state | `tenant_id`, selected ids, filters, and theme values are untrusted request input. |
| core read model -> admin UI | Tenant list rows are authorized data crossing into shell rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03 | Elevation of Privilege | `operator_live.ex`, `inbound_live.ex` tenant param handling | mitigate | Treat URL tenant id as selection only; all data reads keep tenant-scoped read-model filters. |
| T-112-04 | Information Disclosure | tenant switcher | mitigate | Source switcher only from `MailglassAdmin.Operator.Tenants.list_tenants/2`, which composes the scoped core outbound projection and optional inbound gateway; never infer choices from admin Repo/session fixtures. |
| T-112-05 | Tampering | tenant switch links | mitigate | Build switch URLs with allowlisted query keys and drop selected record ids that do not carry across tenants. |
| T-112-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `mix test mailglass_admin/test/mailglass_admin/operator/shell_test.exs mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
SHELL-01, SHELL-02, and SHELL-03 are satisfied for server-rendered LiveView behavior without adding tenant-management features or widening tenant visibility.
</success_criteria>

<output>
Create `.planning/phases/112-app-shell-navigation-tenant-seam/112-02-SUMMARY.md` when done.
</output>
