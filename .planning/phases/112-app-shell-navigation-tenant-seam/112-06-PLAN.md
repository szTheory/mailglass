---
phase: 112-app-shell-navigation-tenant-seam
plan: 06
type: execute
wave: 6
depends_on:
  - 112-03
  - 112-04
  - 112-05
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/e2e/operator.spec.js
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
  - .planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - SHELL-06
must_haves:
  truths:
    - "Integrated browser proof covers tenant auto-select/switcher, theme no-FOUC, active nav cues, and pagination."
    - "Operator and inbound list UIs render honest result counts and page boundaries from Plan 112-05 metadata."
    - "Conformance gates prevent Phase 112 scope regressions: admin Repo tenant queries, concrete system theme, old no-tenant dead-end copy, and dishonest pagination."
    - "112-VALIDATION.md is updated with green automated evidence."
  artifacts:
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Phase 112 browser structural proof"
    - path: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      provides: "Delivery count and pagination UI"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      provides: "Inbound count and pagination UI"
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "Source-scope regression gates"
    - path: ".planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md"
      provides: "Validation status updates"
  key_links:
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "browser proof loads real operator shell"
      pattern: "Phase 112"
---

<objective>
Close Phase 112 with integrated browser proof and fail-closed conformance checks.

Purpose: the individual tasks prove seams in isolation; this plan proves the shell behavior works together and prevents reintroducing the known lived-experience traps.
Output: Phase 112 browser tests, conformance gates, validation evidence, and source audit closure.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-CONTEXT.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-RESEARCH.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-UI-SPEC.md
@.planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/inbound_live.ex
@mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
@mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/e2e/operator.spec.js
@mailglass_admin/scripts/check-conformance.sh
</context>

## Artifacts this phase produces

- Phase 112 browser proof for tenant, theme, navigation, and pagination behavior.
- Conformance checks for tenant/theme/pagination source regressions.
- Updated validation strategy status with exact command evidence.

## Scope Note

This final integration plan intentionally lists 10 files because it is the only plan that ties
already-built Phase 112 shell behavior into browser proof, conformance gates, and validation
evidence. It does not introduce a new feature seam; implementation work remains limited to list UI
pagination wiring, browser/conformance proof, and status updates. Splitting it would duplicate the
same LiveView/list test setup across two closing plans without reducing behavioral risk.

## Source Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 112 | Auto-select/list/switch tenants, persist scope, no-FOUC theme, active nav, honest pagination | 01-06 | COVERED | Roadmap goal decomposed across all six plans. |
| REQ | SHELL-01 | Sole tenant auto-selected and picker only for multi-tenant | 01,02,06 | COVERED | Core tenant read model plus shell auto-select proof. |
| REQ | SHELL-02 | Multi-tenant unscoped switcher from scoped core read model | 01,02,06 | COVERED | D-01/D-02 implemented through `Mailglass.Operator.Tenants`. |
| REQ | SHELL-03 | Tenant scope persists across surfaces/actions | 02,03,05,06 | COVERED | URL state preservation and integrated browser proof. |
| REQ | SHELL-04 | Theme picker persistence and no FOUC | 03,06 | COVERED | Namespaced cookie, root theme, no concrete system theme. |
| REQ | SHELL-05 | Non-color active nav cues | 04,06 | COVERED | Shared primitives plus browser assertions. |
| REQ | SHELL-06 | Honest pagination count/chrome/boundaries | 05,06 | COVERED | Real read metadata plus UI controls. |
| RESEARCH | D-01/D-02 recommendation | Unified tenant selector with scoped outbound projection plus optional inbound-only ids | 01,02 | COVERED | Core owns outbound projection; admin shell consumes a single selector seam that unions optional inbound ids without direct `MailglassInbound.*` references. |
| RESEARCH | Theme cookie seam | Cookie for explicit light/dark; system absence | 03 | COVERED | HTTP seam included if LiveView cannot set cookie for first paint. |
| RESEARCH | Pagination metadata | Count/page metadata from read models | 05 | COVERED | Totals are not inferred from list length. |
| CONTEXT | D-01 | Core operator tenant read model | 01,02 | COVERED | Tasks cite D-01. |
| CONTEXT | D-02 | `Tenancy.scope/2` tenant listing | 01,02 | COVERED | Tasks cite D-02. |
| CONTEXT | D-03 | Sole auto-select and multi-tenant switcher | 02 | COVERED | Tasks cite D-03. |
| CONTEXT | D-04 | URL tenant state | 02,03,05 | COVERED | Tasks cite D-04. |
| CONTEXT | D-05 | Build on existing path behavior | 02 | COVERED | Tasks cite D-05. |
| CONTEXT | D-06 | No `data-theme="system"` | 03 | COVERED | Tasks cite D-06. |
| CONTEXT | D-07 | Namespaced cookie persistence | 03 | COVERED | Tasks cite D-07. |
| CONTEXT | D-08 | Existing theme picker primitive remains source | 03 | COVERED | Tasks cite D-08. |
| CONTEXT | D-09 | Non-color nav cues | 04 | COVERED | Tasks cite D-09. |
| CONTEXT | D-10 | Real pagination metadata | 05,06 | COVERED | Read-model metadata in 05; UI consumes it in 06. |
| CONTEXT | D-11 | Pagination chrome/count rules | 06 | COVERED | Final UI and browser proof cite D-11. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integrated Phase 112 browser proof</name>
  <files>mailglass_admin/e2e/structural.spec.js, mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/e2e/operator.spec.js
    @mailglass_admin/test/support/operator_browser_server.ex
    @mailglass_admin/test/support/operator_fixtures.ex
  </read_first>
  <behavior>
    - Browser login with a sole tenant reaches a canonical URL containing `tenant_id`.
    - Multi-tenant unscoped browser state shows a tenant selector and selecting a tenant preserves surface state.
    - Explicit theme cookie affects root `<html>` before interaction; system has no root `data-theme`.
    - Active nav and pagination controls expose the structural cues and disabled boundaries from prior plans.
  </behavior>
  <action>Add a Phase 112 browser describe block and test data reset hooks using existing browser server fixtures. Prefer structural assertions over screenshots. Cover tenant, theme, nav, and pagination together where practical; keep the test grep-able with `Phase 112`. Do not add axe baseline, pixel-diff assertions, new npm packages, responsive table-to-card assertions, or full matrix re-score. This plan verifies D-01 through D-11 in the browser.</action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors && cd mailglass_admin && npm run test:operator-browser -- --grep "Phase 112"</automated>
  </verify>
  <acceptance_criteria>`npm run test:operator-browser -- --grep "Phase 112"` passes and covers tenant auto-select/switcher, no-FOUC theme root behavior, active nav cues, and pagination boundaries.</acceptance_criteria>
  <done>`npm run test:operator-browser -- --grep "Phase 112"` passes and covers tenant auto-select/switcher, no-FOUC theme root behavior, active nav cues, and pagination boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render pagination UI and add conformance regression gates</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex, mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/inbound/records_list.ex, mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/operator_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
    @mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
    @mailglass_admin/scripts/check-conformance.sh
    @mailglass_admin/test/mailglass_admin/operator_live_test.exs
    @mailglass_admin/test/mailglass_admin/inbound_live_test.exs
    @.planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
    @.planning/phases/110-primitives/110-04-SUMMARY.md
    @.planning/phases/111-forms/111-04-SUMMARY.md
  </read_first>
  <behavior>
    - List components always render `N results` or `1 result` for non-error list states.
    - Pagination chrome renders only when `total_pages > 1`.
    - First page previous and last page next are visible disabled controls with `aria-disabled`.
    - Gate fails on admin-side tenant Repo queries or direct tenant selector storage access.
    - Gate fails on concrete system theme attributes/strings in root/shell source.
    - Gate fails on old dead-end tenant copy and pagination totals derived from list length.
  </behavior>
  <action>Thread Plan 112-05 page metadata through `OperatorLive` and `InboundLive` assigns, including normalized `page` params in each surface's filter state. Update list components to accept `page_meta` and path information or precomputed pagination URLs. Render result count near the list heading/footer for populated and empty non-error states. Render Previous/Next controls only for multi-page results; boundary controls stay visible with disabled styling and `aria-disabled`. Page changes must preserve `tenant_id`, current theme choice, and compatible filters; selected delivery/inbound ids may be cleared when changing pages. Then extend `check-conformance.sh` using the existing shared `errors` counter and `BASH_SOURCE`-anchored paths. Add narrowly scoped grep gates for Phase 112 regressions: no `Repo.` tenant listing under `mailglass_admin/lib`, no concrete system root theme string, no old manual `tenant_id` dead-end copy in shell/list components, and no pagination count derived from `Enum.count(@deliveries)`, `Enum.count(@records)`, or equivalent assigned entries length. Use allowlisted patterns where needed so legitimate core read-model and test code remain valid. Do not create a separate script. Implement D-10 and D-11.</action>
  <verify>
    <automated>mix test mailglass_admin/test/mailglass_admin/operator_live_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors && cd mailglass_admin && ./scripts/check-conformance.sh</automated>
  </verify>
  <acceptance_criteria>Operator and inbound lists show honest counts, only show pagination for multi-page result sets, disable boundary controls, preserve tenant scope in pagination URLs, and the conformance script rejects the Phase 112 anti-patterns named in context/research.</acceptance_criteria>
  <done>Operator and inbound lists show honest counts, only show pagination for multi-page result sets, disable boundary controls, preserve tenant scope in pagination URLs, and the conformance script rejects the Phase 112 anti-patterns named in context/research.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase gate and update validation status</name>
  <files>.planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    @.planning/phases/112-app-shell-navigation-tenant-seam/112-VALIDATION.md
    @mailglass_admin/test/mailglass_admin/operator_live_test.exs
    @mailglass_admin/test/mailglass_admin/inbound_live_test.exs
  </read_first>
  <action>Run the full Phase 112 validation strategy and update `112-VALIDATION.md` task status rows from pending to green with exact command evidence and date. If a listed command changed because implementation used a more precise grep or test file split, record the exact command that passed. Keep changes limited to the Phase 112 validation artifact and any test stabilization required by the browser proof.</action>
  <verify>
    <automated>mix verify.preview && cd mailglass_admin && ./scripts/check-conformance.sh && npm run test:operator-browser -- --grep "Phase 112"</automated>
  </verify>
  <acceptance_criteria>`mix verify.preview`, conformance, and Phase 112 browser proof pass; `112-VALIDATION.md` records green evidence for SHELL-01..06.</acceptance_criteria>
  <done>`mix verify.preview`, conformance, and Phase 112 browser proof pass; `112-VALIDATION.md` records green evidence for SHELL-01..06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser automation -> app shell | Browser proof exercises authenticated shell flows through the same routes users hit. |
| source gates -> implementation | Conformance checks prevent unsafe source patterns from re-entering after tests pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-14 | Information Disclosure | tenant selector/browser flow | mitigate | Browser proof covers scoped selector behavior and conformance rejects admin raw Repo tenant reads. |
| T-112-15 | Tampering | theme persistence | mitigate | Browser proof rejects concrete system theme and validates explicit cookie-only choices. |
| T-112-16 | Information Disclosure | pagination counts | mitigate | Gate and tests reject totals derived from capped entry arrays. |
| T-112-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `mix verify.preview && cd mailglass_admin && ./scripts/check-conformance.sh && npm run test:operator-browser -- --grep "Phase 112"`.
</verification>

<success_criteria>
All SHELL-01..06 behaviors have integrated automated proof, conformance protection, and updated Phase 112 validation evidence.
</success_criteria>

<output>
Create `.planning/phases/112-app-shell-navigation-tenant-seam/112-06-SUMMARY.md` when done.
</output>
