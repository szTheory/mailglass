---
phase: 85-research-and-differentiation-brief
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-research-and-differentiation-brief/85-codex-audit.md
  - .planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md
autonomous: true
requirements: [BRIEF-01, BRIEF-02]
must_haves:
  truths:
    - "A row-addressable defect/gap register of the codex brandbook exists, with every defect row citing file-and-line evidence that is verifiable against the frozen brandbook/ at commit 09a84dd4"
    - "The audit records what codex did WELL (strengths register), so false differentiators are killed before the brief is written"
    - "The differentiation brief locks at most 12 differentiators, each with a one-line 'why it earns its bytes'"
    - "The brief contains the brand-book section outline (consumed by Phase 88) and an explicit kill-list of artifacts deliberately not shipped"
    - "The brief defines the brandbook-fable/ file manifest with size budgets (folder <= 500 KB, index.html <= 150 KB, no file > 100 KB)"
    - "Nothing exists under brandbook-fable/ — all Phase 85 artifacts live in .planning/"
  artifacts:
    - path: ".planning/phases/85-research-and-differentiation-brief/85-codex-audit.md"
      provides: "BRIEF-01 forensic defect/gap register + strengths register"
      contains: "| ID | File:Line | Finding | Severity | Fable response |"
      min_lines: 80
    - path: ".planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md"
      provides: "BRIEF-02 locked differentiators, section outline, kill-list, file manifest"
      contains: "## Kill-List"
      min_lines: 100
  key_links:
    - from: ".planning/phases/85-research-and-differentiation-brief/85-codex-audit.md"
      to: "brandbook/ (frozen at 09a84dd4)"
      via: "file:line citations in every defect row"
      pattern: "brandbook/[a-z-]+\\.(md|json|css|html|svg):[0-9]+"
    - from: ".planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md"
      to: ".planning/phases/85-research-and-differentiation-brief/85-codex-audit.md"
      via: "each differentiator cites the CDX defect row(s) it exploits or the CDX-S strength it must beat"
      pattern: "CDX-"
    - from: ".planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md"
      to: ".planning/research/v1.9-brandbook-fable/SUMMARY.md"
      via: "brief consumes settled research decisions (DTCG tokens, contrast fixes, GitHub SVG rules) instead of re-deciding them"
      pattern: "SUMMARY\\.md|PITFALLS-PORTABILITY"
---

<objective>
Produce the decision foundation for the entire fable brand book: (1) a forensic,
row-addressable audit of the frozen codex `brandbook/` baseline with file-and-line
evidence for every claim, and (2) a locked differentiation brief (<= 12
differentiators, brand-book section outline, kill-list, file manifest with size
budgets) that Phases 86-90 work from without ever re-reading codex files.

Purpose: This is the anti-thrash and anti-derivative contract for v1.9. An honest
A/B requires verified defects only and acknowledged codex strengths — false
differentiators discredit the comparison the maintainer will sign off in Phase 90.

Output: Two planning artifacts in `.planning/phases/85-research-and-differentiation-brief/`:
`85-codex-audit.md` and `85-differentiation-brief.md`. NOTHING under `brandbook-fable/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-research-and-differentiation-brief/85-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/v1.9-brandbook-fable/SUMMARY.md
@.planning/research/v1.9-brandbook-fable/BRAND-SYSTEMS.md
@.planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md
@prompts/mailglass-brand-book.md

Audit target (read-only, frozen at commit 09a84dd4 — verified identical to the
working tree via `git diff --quiet 09a84dd4 -- brandbook/`):
@brandbook/brand-book.md
@brandbook/tokens.json
@brandbook/tokens.css
@brandbook/index.html
@brandbook/README.md
@brandbook/brand-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the forensic codex-brandbook audit register (BRIEF-01)</name>
  <files>.planning/phases/85-research-and-differentiation-brief/85-codex-audit.md</files>
  <read_first>
    - .planning/phases/85-research-and-differentiation-brief/85-CONTEXT.md (locked audit decisions: row format, three pre-verified defects, strengths requirement)
    - .planning/research/v1.9-brandbook-fable/SUMMARY.md (settled decisions and the four v1.8 root failures — the audit grounds these in file:line evidence)
    - .planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md (28 pitfalls; map codex defects to pitfall IDs where they instantiate one)
    - brandbook/brand-book.md, brandbook/tokens.json, brandbook/tokens.css, brandbook/index.html, brandbook/README.md, brandbook/brand-audit.md (the audit targets — read fully)
    - brandbook/assets/logo-primary.svg, logo-mark.svg, logo-monochrome.svg, favicon.svg, social-avatar.svg (asset defect evidence)
    - brandbook/examples/*.svg (specimen coverage assessment)
  </read_first>
  <action>
    Create `.planning/phases/85-research-and-differentiation-brief/85-codex-audit.md` —
    a row-addressable defect/gap register of the codex `brandbook/` frozen at commit
    `09a84dd4`, per the CONTEXT.md locked audit decisions.

    Before writing, sanity-check the freeze: run `git diff --quiet 09a84dd4 -- brandbook/`
    (exit 0 confirms the working tree matches the frozen baseline; if non-zero, cite
    against `git show 09a84dd4:brandbook/<file>` instead of the working tree and note
    this in the methodology section).

    Document structure (exact headings, so downstream greps are deterministic):
    1. `## Methodology` — frozen commit cited (`09a84dd4`), verification method (every
       finding checked against actual file contents, line numbers from the frozen
       commit), severity scale definition (1=cosmetic .. 5=undermines the artifact's
       purpose), and the fable-response taxonomy: `exploit` (fable does this visibly
       better), `fix` (fable simply avoids it), `ignore` (real but not worth bytes).
    2. `## Defect Register` — one markdown table with header row exactly:
       `| ID | File:Line | Finding | Severity | Fable response |`
       Rows use stable IDs `CDX-01`, `CDX-02`, ... Every row's File:Line MUST point at
       a real line the executor has personally read — no straw men, no paraphrased
       claims. Where a defect instantiates a registered pitfall, append the pitfall ID
       (e.g. P-01, P-15) to the Finding cell.
    3. `## Strengths Register` — same table shape with IDs `CDX-S-01`, ... recording
       what codex did WELL, minimum 3 rows. Known strengths to verify and cite:
       semantic state tokens (tokens.json state group), callout/code role tokens,
       a defined dark token set (tokens.css `[data-theme="dark"]` block at line 108),
       and any others found (e.g. role descriptions on every token). Column 5 for
       strengths is "Fable response" = `match` or `beat`, with a phrase saying how.
    4. `## Killed Differentiator Candidates` — short list of draft differentiators
       that the strengths register disproves or weakens (e.g. "codex has no semantic
       state tokens" is FALSE — it does), so the brief never claims them.

    Three pre-verified defects to re-confirm with exact citations and then extend
    (these line numbers are verified against 09a84dd4):
    - `brandbook/assets/logo-primary.svg:17` — live `<text>` element with
      `font-family="Avenir Next, Avenir, Helvetica Neue, sans-serif"` (macOS-only
      primary font; renders as fallback or black-box elsewhere; GitHub `<img>` sandbox
      loads no fonts), plus the gradient fill `url(#mg-logo-primary-pane)` on line 14
      that brandbook/brand-book.md's own rules forbid — cite the forbidding line in
      brand-book.md as part of the evidence.
    - `brandbook/tokens.json:70` and `:75-76` — token descriptions reference "Phase 84
      contrast validation" — planning-process vocabulary leaked into a shipped artifact,
      referencing a validation that never ran (pitfall P-15).
    - Dark tokens defined but never demonstrated: `brandbook/tokens.css:108`
      `[data-theme="dark"]` block exists, but `brandbook/index.html` contains no theme
      toggle and never sets `data-theme` (the only "dark" in index.html is the
      `.logo-box.dark` CSS class at line 270 used for a static avatar swatch). Cite
      both files.

    Beyond these three, sweep for at least these candidate defect classes (verify each
    before registering; drop any that don't hold):
    - Contrast claims asserted but never computed (brand-book.md / brand-audit.md
      language vs. the absence of any contrast matrix anywhere in brandbook/).
    - Process/tournament artifacts shipped inside the reader-facing folder:
      `brandbook/logo-options.md`, `logo-concepts.md`, `logo-concepts.html`,
      `logo-creative-brief.md`, `brand-audit.md`, `assets/options/` (18 files),
      `assets/concepts/` (11 files) — v1.9 scope locks say galleries/audits live in
      `.planning/`, never in the brand folder.
    - `font-family` or live `<text>` in OTHER asset SVGs (grep all of brandbook/assets/
      and brandbook/examples/ for `font-family` and `<text`; register every hit).
    - index.html portability: external network requests, fetch()/ES modules, webfont
      URLs (grep for `http`, `fetch(`, `type="module"`, `@font-face`).
    - Accessibility of asset SVGs: missing `role="img"`/`<title>`/`aria-labelledby`.
    - Missing launch surfaces vs. the v1.9 target set: no landing-page specimen, no
      email-template specimen, no OG/social-card template, no copy library, no
      microcopy — register as gap rows (File:Line = `brandbook/ (absent)` is
      acceptable ONLY for absence-of-file gaps; everything else needs a real line).

    Severity and fable response are the executor's judgment per the methodology scale.
    Target 10-25 defect rows — completeness over padding; every row must survive a
    hostile re-check. Maintain the thoughtful-maintainer voice: specific, composed,
    no gloating.
  </action>
  <verify>
    <automated>PH=.planning/phases/85-research-and-differentiation-brief; git diff --quiet 09a84dd4 -- brandbook/ && grep -q '| ID | File:Line | Finding | Severity | Fable response |' "$PH/85-codex-audit.md" && grep -q 'logo-primary\.svg:17' "$PH/85-codex-audit.md" && grep -q 'tokens\.json:70' "$PH/85-codex-audit.md" && grep -q 'tokens\.css:108' "$PH/85-codex-audit.md" && [ "$(grep -c '^| CDX-[0-9]' "$PH/85-codex-audit.md")" -ge 10 ] && [ "$(grep -c '^| CDX-S-' "$PH/85-codex-audit.md")" -ge 3 ] && [ -z "$(grep -oE 'brandbook/[A-Za-z0-9_./-]+' "$PH/85-codex-audit.md" | sed 's/[.:]$//' | sort -u | while read -r f; do [ -e "$f" ] || echo "MISSING $f"; done)" ] && echo AUDIT-OK</automated>
  </verify>
  <acceptance_criteria>
    - 85-codex-audit.md contains a markdown table with the header row `| ID | File:Line | Finding | Severity | Fable response |`
    - At least 10 defect rows matching `^| CDX-[0-9]` and at least 3 strength rows matching `^| CDX-S-`
    - Every brandbook/ path cited anywhere in the audit exists on disk (loop check in <automated> prints nothing)
    - The three pre-verified defects appear with their exact citations: logo-primary.svg:17, tokens.json:70 (and 75-76), tokens.css:108 + index.html no-toggle evidence
    - A `## Killed Differentiator Candidates` section exists naming at least one false differentiator disproved by the strengths register
    - A `## Methodology` section cites commit 09a84dd4 and defines the severity scale and the exploit/fix/ignore taxonomy
    - No file is created or modified outside .planning/
  </acceptance_criteria>
  <done>BRIEF-01 satisfied: a row-addressable, file:line-evidenced defect/gap register with strengths and killed-candidate sections exists at .planning/phases/85-research-and-differentiation-brief/85-codex-audit.md and every citation verifies against the frozen baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Write and lock the differentiation brief (BRIEF-02)</name>
  <files>.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md</files>
  <read_first>
    - .planning/phases/85-research-and-differentiation-brief/85-codex-audit.md (Task 1 output — differentiators must cite CDX/CDX-S rows; killed candidates must not reappear)
    - .planning/phases/85-research-and-differentiation-brief/85-CONTEXT.md (locked brief decisions: starting draft list, kill-list seed, manifest size budgets)
    - .planning/research/v1.9-brandbook-fable/SUMMARY.md ("Open Decisions for Phases — Phase 85" bullet: banned motifs, compositional stance, ownable axis, non-negotiables; plus the settled index.html architecture for the section outline)
    - .planning/research/v1.9-brandbook-fable/BRAND-SYSTEMS.md (genre rule: engineer-operable design-system doc with thin logo-kit layer; steal/avoid lists informing the outline)
    - .planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md (decide fold-vs-reference for phase mappings; reference is the default per CONTEXT discretion)
    - .planning/REQUIREMENTS.md (BRIEF-02 text + milestone Out of Scope list the kill-list extends)
    - prompts/mailglass-brand-book.md (locked essence, voice, palette/type seeds the brief must not contradict)
  </read_first>
  <action>
    Create `.planning/phases/85-research-and-differentiation-brief/85-differentiation-brief.md` —
    the locked contract Phases 86-90 build from. Single file (CONTEXT discretion:
    brief + manifest folded together so downstream phases read exactly one document
    and never re-open codex files).

    Document structure (exact headings, deterministic for greps and Phase 88
    consumption):

    1. `## Differentiators` — one markdown table, header
       `| ID | Differentiator | Why it earns its bytes | Evidence |`,
       rows `DIF-01` ... `DIF-NN` with N <= 12 (hard cap; 8-12 expected). Start from
       the CONTEXT.md draft list — outlined font-independent logo; integrated custom
       typemark; demonstrated dark mode with toggle; live HTML component gallery;
       shipped WCAG contrast matrix; zero planning-language leakage; landing-page
       blueprint; branded email specimen; per-surface copy library + domain-noun
       microcopy; diagram language spec — then VALIDATE, PRUNE, OR REPLACE against the
       audit. Do not pad to 12. Every row's Evidence cell cites the CDX defect row(s)
       it exploits or the CDX-S strength it must beat. Any candidate listed in the
       audit's Killed Differentiator Candidates section is forbidden here. "Why it
       earns its bytes" is ONE line per row.

    2. `## Non-Negotiables` — the loudly-repeated single rules (Supabase pattern, per
       research): at minimum — outlined paths only, zero font-family in asset SVGs;
       zero planning-process vocabulary in any shipped artifact; dark mode demonstrated
       live on every specimen; every contrast claim computed, never asserted; zero
       network requests in any HTML.

    3. `## Banned Motif Families` — name them explicitly per the research open
       decision: P-21..P-25 cliché families from PITFALLS-PORTABILITY.md (cite the
       IDs), plus envelope-swoosh, Linear-slats resemblance, Glassdoor/Gmail
       resemblance, glassmorphism, paper-plane, mailbox, send-arrow, mascot, and
       literal broken glass (brand-book seed rule).

    4. `## Compositional Stance` and `## Ownable Axis` — short locked statements:
       left-aligned documentation-like composition (anti-centered-hero), and the
       ownable axis of Ink/Glass/Ice + transparency-as-meaning (Supabase
       color-ownership lesson). Two paragraphs max each.

    5. `## Brand-Book Section Outline` — the ordered section list for
       `brandbook-fable/index.html` and `brand-book.md` that Phase 88 implements
       verbatim. Order follows the settled research architecture: foundations before
       applications (orientation/essence -> voice -> color+tokens -> typography ->
       contrast matrix -> components gallery -> logo system -> specimens/applications
       -> usage and export policy). Each outline entry gets a one-line content note.

    6. `## Kill-List` — artifacts deliberately NOT shipped, each with a one-line
       reason. Seed from CONTEXT.md: personas, mission statements, mood boards,
       print/stationery, icon libraries, motion videos. Extend with at least: shipped
       tournament/options galleries inside the brand folder (the codex mistake — cite
       the CDX row), generated screenshot sets (Phase 999.2 backlog rule), PNG/binary
       assets (text-artifacts-only scope lock), and webfont files/@font-face.

    7. `## File Manifest` — table of the planned `brandbook-fable/` tree (~21 files
       across root, assets/, examples/, copy/) with a per-file purpose and size
       budget, plus the three hard budget lines stated verbatim: folder <= 500 KB,
       index.html <= 150 KB, no single file > 100 KB. Derive the file set from the
       differentiators + roadmap Phases 86-89 requirements (tokens.json, tokens.css,
       index.html, brand-book.md, README.md; assets/: primary lockup, typemark, mark,
       monochrome/currentColor mark, with-tagline variant, favicon, light+dark social
       avatars; examples/: landing-page.html, email-template.html, readme-header.svg,
       docs-page.svg, og-card.svg, diagram-language.svg; copy/: copy-blocks.md,
       microcopy.md). Map each file to the phase that authors it (86-89).

    8. `## Pitfall Mapping` — per CONTEXT discretion, do NOT duplicate the 28-pitfall
       register; reference `.planning/research/v1.9-brandbook-fable/PITFALLS-PORTABILITY.md`
       directly and list only the pitfall IDs each downstream phase must clear.

    Constraint: downstream phases (86-90) must be able to work from this brief + the
    brand strategy seeds (prompts/mailglass-brand-book.md) WITHOUT reading any
    brandbook/ file again — the brief carries forward every codex fact it needs
    (anti-derivative rule). Do not contradict the locked essence: "mail you can see
    through", thoughtful-maintainer voice, glass-as-metaphor-not-gimmick.

    Do NOT create anything under brandbook-fable/ — this phase ships planning
    artifacts only.
  </action>
  <verify>
    <automated>PH=.planning/phases/85-research-and-differentiation-brief; N=$(grep -c '^| DIF-' "$PH/85-differentiation-brief.md"); [ "$N" -ge 6 ] && [ "$N" -le 12 ] && grep -q '^## Kill-List' "$PH/85-differentiation-brief.md" && grep -q '^## Brand-Book Section Outline' "$PH/85-differentiation-brief.md" && grep -q '^## File Manifest' "$PH/85-differentiation-brief.md" && grep -q '^## Banned Motif Families' "$PH/85-differentiation-brief.md" && grep -q '^## Non-Negotiables' "$PH/85-differentiation-brief.md" && grep -q 'CDX-' "$PH/85-differentiation-brief.md" && grep -q '500 KB' "$PH/85-differentiation-brief.md" && grep -q '150 KB' "$PH/85-differentiation-brief.md" && test ! -e brandbook-fable && echo BRIEF-OK</automated>
  </verify>
  <acceptance_criteria>
    - 85-differentiation-brief.md contains a `## Differentiators` table where the count of rows matching `^| DIF-` is between 6 and 12 inclusive
    - Every DIF row has a non-empty one-line "Why it earns its bytes" cell and an Evidence cell referencing at least one CDX or CDX-S row from the audit
    - No differentiator repeats an entry from the audit's Killed Differentiator Candidates section
    - Headings `## Non-Negotiables`, `## Banned Motif Families`, `## Compositional Stance`, `## Ownable Axis`, `## Brand-Book Section Outline`, `## Kill-List`, `## File Manifest`, `## Pitfall Mapping` all present
    - The kill-list includes at minimum: personas, mission statements, mood boards, print/stationery, icon libraries, motion videos
    - The file manifest enumerates the brandbook-fable/ tree with per-file phase ownership and states the three size budgets (folder <= 500 KB, index.html <= 150 KB, no file > 100 KB)
    - `test ! -e brandbook-fable` passes — nothing was created under brandbook-fable/
    - No file is created or modified outside .planning/
  </acceptance_criteria>
  <done>BRIEF-02 satisfied: the brief locks <= 12 evidence-cited differentiators, the Phase 88 section outline, an explicit kill-list, and the budgeted brandbook-fable/ file manifest — and Phases 86-90 can consume it without re-reading codex files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation-only phase: two Markdown planning artifacts, no executable code, no dependencies installed, no untrusted input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Tampering | brandbook/ frozen baseline | mitigate | Tasks read brandbook/ only; verify gate runs `git diff --quiet 09a84dd4 -- brandbook/` to prove the baseline was not modified |
| T-85-02 | Repudiation | audit claims | mitigate | Every defect row carries file:line evidence; automated loop verifies every cited brandbook/ path exists |
</threat_model>

<verification>
- Both artifacts exist in `.planning/phases/85-research-and-differentiation-brief/` and pass their task-level automated gates (AUDIT-OK, BRIEF-OK).
- `git diff --quiet 09a84dd4 -- brandbook/` exits 0 (frozen baseline untouched).
- `test ! -e brandbook-fable` passes (roadmap success criterion 4).
- `git status --porcelain` shows changes only under `.planning/`.
</verification>

<success_criteria>
Maps 1:1 to the four roadmap Phase 85 success criteria:
1. Forensic audit exists as a row-addressable defect/gap register with file:line evidence for every claimed weakness (Task 1 gate).
2. Brief locks <= 12 differentiators, each with a one-line "why it earns its bytes" (Task 2 gate: 6 <= DIF count <= 12).
3. Brief contains the brand-book section outline and an explicit kill-list (Task 2 gate: heading greps).
4. Nothing exists under brandbook-fable/ (Task 2 gate: `test ! -e brandbook-fable`).
</success_criteria>

<output>
Create `.planning/phases/85-research-and-differentiation-brief/85-01-SUMMARY.md` when done.
</output>
