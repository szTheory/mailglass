---
phase: 108
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/publish/mailglass-files.expected
  - .planning/publish/mailglass-publish-summary.json
  - .planning/publish/mailglass_admin-publish-summary.json
  - .planning/publish/mailglass_inbound-publish-summary.json
autonomous: false
requirements:
  - REL-01
  - REL-02

must_haves:
  truths:
    - "All three Hex packages (mailglass 1.7.0, mailglass_admin 1.7.0, mailglass_inbound 1.4.0) are live on Hex"
    - "mailglass_inbound carries the dep {mailglass, == 1.7.0}"
    - "mix deps.get resolves all three packages from Hex in a fresh Phoenix app"
    - "post-publish-smoke passes (or only the known #32 hackney false-positive is red)"
    - "Milestone v1.12 is audited, archived, and tagged"
  artifacts:
    - path: ".planning/publish/mailglass-files.expected"
      provides: "Publish allowlist snapshot for mailglass; must include the 3 new guides + 2 modules"
    - path: ".planning/milestones/v1.12-ROADMAP.md"
      provides: "Archived milestone roadmap"
    - path: ".planning/milestones/v1.12-REQUIREMENTS.md"
      provides: "Archived milestone requirements"
    - path: ".planning/milestones/v1.12-MILESTONE-AUDIT.md"
      provides: "Per-requirement pass/fail matrix with corrected scope counts"
  key_links:
    - from: ".planning/publish/mailglass-files.expected"
      to: "prepublish-summary job in publish-hex.yml"
      via: "mix mailglass.publish.check"
      pattern: "mix mailglass.publish.check"
    - from: "PR #84 diff"
      to: "mailglass_inbound/mix.exs"
      via: "RP sed step"
      pattern: "== 1.7.0"
---

<objective>
Release cut and milestone closeout for mailglass v1.12 — Adopter Onboarding & Day-2 Confidence.

Purpose: Move the accumulated v1.7–v1.12 body of work (brand adoption, admin UI polish, design-system uplift, installer DX hardening, onboarding docs, a11y parity) from `main` to Hex adopters at versions mailglass 1.7.0 / mailglass_admin 1.7.0 / mailglass_inbound 1.4.0, then archive the v1.12 milestone.

Output: All three packages live on Hex; inbound pinned to mailglass == 1.7.0; post-publish smoke green; milestone audit and git tag v1.12.

LIVE STATE (supersedes RESEARCH.md — use these facts):
- Release Please PR #84 ("chore: release main") is ALREADY OPEN proposing 1.7.0/1.7.0/1.4.0.
- Inbound is 1.4.0 (MINOR), bumped by feat(99-01) touching mailglass_inbound/. No new fix(inbound): commit is needed.
- REL-02 inbound re-pin (== 1.7.0) is ALREADY ENCODED in PR #84 by the RP sed step.
- The one real Wave-0 blocker: .planning/publish/mailglass-files.expected is STALE (missing 3 guides + 2 modules). Must be regenerated and committed before PR #84 merges.
- Reference baseline pin-bump is OUT OF SCOPE; ~> 1.4 already resolves 1.7.0.
</objective>

<execution_context>
@/Users/jon/projects/mailglass/.planning/phases/108-release-cut-milestone-closeout/108-VALIDATION.md
@/Users/jon/projects/mailglass/MAINTAINING.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/ROADMAP.md
@/Users/jon/projects/mailglass/.planning/STATE.md

Key workflow files to read before acting:
- .github/workflows/release-please.yml (sed sync step, auto-merge arming, cron self-heal)
- .github/workflows/publish-hex.yml (gate-ci-green, publish ordering, idempotency guards, fallback dispatch)
- .github/workflows/post-publish-smoke.yml (smoke job chain)
- MAINTAINING.md (Release Runbook 5 steps, Retract Decision Tree, required vs. advisory lanes)
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 0 — Pre-flight hygiene (reversible)
     REG-01..04
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1 (Wave 0): Verify green CI + regenerate stale publish allowlist (REG-01..04)</name>
  <files>.planning/publish/mailglass-files.expected, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-publish-summary.json, .planning/publish/mailglass_inbound-publish-summary.json</files>

  <read_first>
    MAINTAINING.md — "Publish Summary Snapshot Protocol" section (mix mailglass.publish.check,
    regeneration steps, commit message guidance). Also read VALIDATION.md Wave 0 Requirements.

    CRITICAL: Do NOT run `mix compile --no-optional-deps --force` on the shared _build during this
    step — it pollutes build artifacts and the /inbound route is compile-time-gated. Use only
    `mix mailglass.publish.check` task invocations.
  </read_first>

  <action>
    Step 0.1 — CI green confirmation (REG-01):
    Run `gh run list --workflow ci.yml --branch main --limit 5` from repo root. Locate the last
    non-docs commit run (the last commit touching lib/, guides/, mailglass_admin/lib/, or
    mailglass_inbound/lib/ — docs(state): commits skip ci.yml entirely). Confirm all required
    lanes are green: "Support Contract Core (Elixir 1.18 / OTP 27)", "Support Contract Admin
    (Elixir 1.18 / OTP 27)", "Compile No Optional Deps (Elixir 1.18 / OTP 27)", "Trust Lane
    Repo Head (Elixir 1.18 / OTP 27)", "Installer Host Smoke". If no ci.yml run exists on the
    SHA because all recent commits were .planning/-only, force one:
    `gh workflow run ci.yml --ref main` then wait for completion before continuing.

    Step 0.2 — Clean worktree check:
    Run `git status` to confirm no staged or unstaged changes. Run `mix mailglass.repo.hygiene
    --check` if available (from MAINTAINING.md). The allowlist snapshot captures untracked files,
    so the working tree must be clean before regeneration.

    Step 0.3 — Regenerate core allowlist (REG-02):
    From repo root run `mix mailglass.publish.check --package mailglass`. This regenerates
    `.planning/publish/mailglass-publish-summary.json` and `.planning/publish/mailglass-files.expected`.
    The regenerated snapshot must include the five previously-missing entries:
    guides/learning-path.md, guides/production-go-live-checklist.md,
    guides/errors-and-troubleshooting.md, lib/mailglass/installer/doctor.ex,
    lib/mix/tasks/mailglass.doctor.ex. Review the diff with `git diff .planning/publish/mailglass-files.expected`.
    Confirm those five lines are now present.

    Step 0.4 — Verify admin allowlist (REG-03):
    Run `mix mailglass.publish.check --package mailglass_admin`. Expect exit 0 with no unexpected
    files (admin changes in v1.12 were to existing lib/mailglass_admin/live/ files, not new files).
    Update `.planning/publish/mailglass_admin-publish-summary.json` if the command writes a new
    snapshot.

    Step 0.5 — Verify inbound allowlist (REG-04):
    Run `mix mailglass.publish.check --package mailglass_inbound`. Expect exit 0. Update
    `.planning/publish/mailglass_inbound-publish-summary.json` if changed.

    Step 0.6 — Commit the regenerated snapshots:
    Stage only the .planning/publish/ files:
    `git add .planning/publish/mailglass-files.expected .planning/publish/mailglass-publish-summary.json .planning/publish/mailglass_admin-publish-summary.json .planning/publish/mailglass_inbound-publish-summary.json`
    Commit: `chore: refresh publish allowlist snapshots for v1.7.0 release`

    NOTE: chore: commits do NOT trigger a Release Please version bump. This is intentional.

    Step 0.7 — Push to main:
    `git push origin main`
    This push triggers the RP action, which runs against HEAD. Because PR #84 is already open
    (autorelease: pending), RP will re-evaluate and potentially update the PR to include the new
    chore commit (no-op, since chore: doesn't affect changelogs). The push also ensures the
    regenerated allowlist is on main before the release PR merges.
  </action>

  <acceptance_criteria>
    REG-01: `gh run list --workflow ci.yml --branch main --limit 5` shows a completed run where
    required lanes ("Support Contract Core", "Support Contract Admin", "Compile No Optional Deps",
    "Trust Lane Repo Head", "Installer Host Smoke") are all green.

    REG-02: `mix mailglass.publish.check --package mailglass` exits 0 AND
    `grep 'learning-path.md' .planning/publish/mailglass-files.expected` returns a match AND
    `grep 'doctor.ex' .planning/publish/mailglass-files.expected` returns a match.

    REG-03: `mix mailglass.publish.check --package mailglass_admin` exits 0.

    REG-04: `mix mailglass.publish.check --package mailglass_inbound` exits 0.

    Commit exists: `git show --stat HEAD` shows .planning/publish/ files changed with
    "chore: refresh publish allowlist snapshots" message.
  </acceptance_criteria>

  <done>
    Green CI run confirmed on the to-be-tagged SHA (or forced via gh workflow run). Publish
    allowlist regenerated to include the 5 previously-missing v1.12 entries. Snapshot committed
    as chore: on main and pushed.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 1 — Verify the open RP PR #84 (reversible, read/curate)
     REG-05..07
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 2 (Wave 1): Verify PR #84 contents — versions, inbound pin, CHANGELOGs (REG-05..07)</name>
  <files></files>

  <read_first>
    .github/workflows/release-please.yml — sed sync step (lines 139-263) that rewrites
    mailglass_admin/mix.exs and mailglass_inbound/mix.exs dep pins on the PR branch.
    MAINTAINING.md — Step 2 "Review the release PR diff before merge" (load-bearing dep-pin sync).
    VALIDATION.md — Live State section (inbound 1.4.0 from feat(99-01), re-pin already encoded).
  </read_first>

  <action>
    Step 1.1 — Pull the PR diff:
    Run `gh pr diff 84` to get the full diff of PR #84 ("chore: release main").

    Step 1.2 — Verify manifest and @version bumps (REG-05):
    In the diff, confirm:
    - `.release-please-manifest.json` shows `"." : "1.7.0"` (core) and
      `"mailglass_admin": "1.7.0"` and `"mailglass_inbound": "1.4.0"`
    - `mix.exs` has `@version "1.7.0"` (core)
    - `mailglass_admin/mix.exs` has `@version "1.7.0"`
    - `mailglass_inbound/mix.exs` has `@version "1.4.0"`
    If any version is wrong, do NOT merge. Stop and investigate which RP config is the discrepancy.

    Step 1.3 — Verify inbound re-pin (REG-06, REL-02):
    In the PR diff for `mailglass_inbound/mix.exs`, confirm the RP sed step encoded:
    `{:mailglass, "== 1.7.0"}` is present in the `mailglass_dep/0` function.
    Run: `gh pr diff 84 -- mailglass_inbound/mix.exs | grep '== 1.7.0'`
    This must return a `+` line containing `{:mailglass, "== 1.7.0"}`.

    Step 1.4 — Verify admin dep pin (REL-02 traceability):
    Run: `gh pr diff 84 -- mailglass_admin/mix.exs | grep '== 1.7.0'`
    This must return a `+` line containing `{:mailglass, "== 1.7.0"}`.

    Step 1.5 — Review CHANGELOGs (REG-07, REL-01):
    Inspect the CHANGELOG.md diff for all three packages in PR #84:
    `gh pr diff 84 -- CHANGELOG.md`
    `gh pr diff 84 -- mailglass_admin/CHANGELOG.md`
    `gh pr diff 84 -- mailglass_inbound/CHANGELOG.md`
    Confirm a `## [1.7.0]` (core+admin) and `## [1.4.0]` (inbound) heading exists with feat/fix
    entries. RP generates this from commit history since 1.6.2. Light hand-curation is acceptable
    (correct phrasing, remove duplicates) — edit via `gh pr checkout 84` and amend, then
    `git push origin HEAD:release-please--branches--main` if curating. For a routine ceremony,
    RP-generated entries are sufficient.

    Step 1.6 — Check PR CI status:
    Run `gh pr checks 84` to see required lane status. All required lanes must be green before
    auto-merge fires. If any required lane is red, investigate before proceeding to Task 3.
    Advisory lanes (Operator Browser Gate, Preview Capture Advisory, etc.) may show yellow/red
    without blocking.

    Step 1.7 — Confirm auto-merge is armed:
    `gh pr view 84 --json autoMergeRequest` — the `autoMergeRequest` field must be non-null
    (the RP workflow arms it automatically after PR creation). If it is null, arm it:
    `gh pr merge 84 --auto --squash`

    NOTE: Do not approve or merge manually yet. Task 3 is the irreversible maintainer checkpoint.
  </action>

  <acceptance_criteria>
    REG-05: `gh pr diff 84` shows manifest entries "1.7.0" for mailglass and mailglass_admin,
    "1.4.0" for mailglass_inbound, plus matching @version attrs in all three mix.exs files.

    REG-06: `gh pr diff 84 -- mailglass_inbound/mix.exs | grep '== 1.7.0'` returns at least one
    `+` line containing the literal `{:mailglass, "== 1.7.0"}`.

    REG-07: `gh pr diff 84 -- CHANGELOG.md | grep '## \[1.7.0\]'` returns a match. Similarly
    `gh pr diff 84 -- mailglass_admin/CHANGELOG.md | grep '## \[1.7.0\]'` and
    `gh pr diff 84 -- mailglass_inbound/CHANGELOG.md | grep '## \[1.4.0\]'` both match.

    `gh pr checks 84` shows all required lanes green (or orange if waiting; not red on required lanes).
    `gh pr view 84 --json autoMergeRequest` has a non-null autoMergeRequest.
  </acceptance_criteria>

  <done>
    PR #84 verified: correct versions in manifest and @version attrs, inbound re-pin == 1.7.0
    present, CHANGELOGs have entries, auto-merge armed, required CI green. Ready for maintainer
    go/no-go checkpoint.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 2 — Merge + publish (IRREVERSIBLE — maintainer checkpoint)
     REG-08
     ═══════════════════════════════════════════════════════════════════ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Wave 2): MAINTAINER GO/NO-GO — authorize merge of PR #84 (IRREVERSIBLE after Hex publish)</name>

  <what-built>
    Wave 0 verified a green CI run and regenerated the stale publish allowlist (REG-01..04).
    Wave 1 verified PR #84 contains the correct version bumps (1.7.0/1.7.0/1.4.0), the inbound
    re-pin {mailglass, "== 1.7.0"}, and CHANGELOG entries for all three packages (REG-05..07).
    Auto-merge is armed. The pipeline is fully hands-free once this checkpoint passes.

    IRREVERSIBILITY WARNING: After mailglass 1.7.0 publishes to Hex, Hex.pm allows revert only
    within 60 minutes AND only if zero downloads have occurred. After that window, only
    `mix hex.retire` (deprecation) or a patch release remain as options. This is the one human
    go/no-go gate in the ceremony.
  </what-built>

  <how-to-verify>
    1. Visit https://github.com/szTheory/mailglass/pull/84 in a browser.
    2. Confirm the PR title is "chore: release main".
    3. Confirm the Files Changed tab shows:
       - mailglass @version "1.7.0", mailglass_admin @version "1.7.0", mailglass_inbound @version "1.4.0"
       - mailglass_inbound/mix.exs dep: `{:mailglass, "== 1.7.0"}` (in the + lines)
       - CHANGELOG entries under ## [1.7.0] / ## [1.4.0] look reasonable for the v1.7–v1.12 work
    4. Confirm CI checks are green on the PR.
    5. If satisfied: APPROVE — auto-merge will squash-merge the PR; the hourly cron or a manual
       `gh workflow run release-please.yml` will tag the commit and fire the publish fan-out.

    After approving, monitor the Actions tab for:
    - release-please.yml completing (tag creation, GitHub Release)
    - publish-hex.yml fan-out triggered by `release: published`
    - prepublish-summary → gate-ci-green → publish-core → publish-inbound → publish-admin

    STALL RECOVERY (documented, NOT the default path):
    - Anti-recursion stall (PR merged but no tag): `gh workflow run release-please.yml` (no inputs)
    - gate-ci-green stall: self-heals automatically (dispatches ci.yml on tag, waits 30 min)
    - publish fan-out failed after tag: `gh workflow run publish-hex.yml --ref mailglass-v1.7.0 --field tag=mailglass-v1.7.0 --field dry_run=false --field package=all` (NEVER dispatch from main)
  </how-to-verify>

  <resume-signal>
    Type "approved" after authorizing the merge and observing that the publish fan-out has started
    (publish-hex.yml run visible in Actions), or type "blocked: [reason]" to halt and describe
    what you found in the PR diff.
  </resume-signal>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 3 — Confirm Hex live (read-only)
     REG-09..11
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 4 (Wave 3): Confirm all three packages live on Hex at target versions (REG-09..11)</name>
  <files></files>

  <read_first>
    .github/workflows/publish-hex.yml — publish job ordering: publish-core → publish-inbound
    → publish-admin; idempotency guards (mix hex.info check before publish). Advisory lane list
    (Operator Browser Gate, Preview Capture Advisory — do not block on these).
    MAINTAINING.md — Step 3 "Monitor the hands-free publish fan-out".
  </read_first>

  <action>
    Step 3.1 — Watch publish fan-out in Actions:
    Open `gh run list --workflow publish-hex.yml --limit 3` to find the run triggered by the
    `release: published` event. Monitor jobs: prepublish-summary → gate-ci-green → publish-core
    → publish-inbound → publish-admin. All must succeed (or skip via idempotency — already published
    is success). Advisory jobs may fail without blocking.

    Step 3.2 — Verify core on Hex (REG-09):
    Run `mix hex.info mailglass 1.7.0`. Wait for Hex indexing if needed (up to 5 minutes;
    publish-core has a built-in "Wait for Hex.pm to index" step). Output must contain "Released:".

    Step 3.3 — Verify admin on Hex (REG-10):
    Run `mix hex.info mailglass_admin 1.7.0`. Output must contain "Released:".

    Step 3.4 — Verify inbound on Hex with correct pin (REG-11, REL-02):
    Run `mix hex.info mailglass_inbound 1.4.0`. Output must contain "Released:" AND must show
    the dependency `mailglass == 1.7.0`. Confirm with:
    `mix hex.info mailglass_inbound 1.4.0 | grep 'mailglass'`
    which must return `mailglass == 1.7.0`.

    Step 3.5 — HexDocs spot check:
    Confirm HexDocs builds are live (may lag Hex by 5–10 minutes):
    - https://hexdocs.pm/mailglass/1.7.0/
    - https://hexdocs.pm/mailglass_admin/1.7.0/
    - https://hexdocs.pm/mailglass_inbound/1.4.0/
    Each URL must return HTTP 200 (not 404 / redirect to latest).

    NOTE: 60-minute revert window starts when publish-core completes (Step 3.2 green). Set a
    timer — Task 5 (manual smoke) must complete within this window.
  </action>

  <acceptance_criteria>
    REG-09: `mix hex.info mailglass 1.7.0` output contains "Released:".

    REG-10: `mix hex.info mailglass_admin 1.7.0` output contains "Released:".

    REG-11: `mix hex.info mailglass_inbound 1.4.0` output contains "Released:" AND
    `mix hex.info mailglass_inbound 1.4.0 | grep 'mailglass'` returns `mailglass == 1.7.0`.

    All three publish-hex.yml jobs (publish-core, publish-inbound, publish-admin) show
    `completed: success` or `completed: skipped` (idempotent re-run) in `gh run view`.
  </acceptance_criteria>

  <done>
    mailglass 1.7.0, mailglass_admin 1.7.0, mailglass_inbound 1.4.0 confirmed live on Hex.
    Inbound dependency shows mailglass == 1.7.0. 60-minute revert window timer started.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 4 — Resolution + smoke (within 60-minute window)
     REG-12..13
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 5 (Wave 4): Consumer smoke + post-publish-smoke (REG-12..13, REL-02)</name>
  <files></files>

  <read_first>
    scripts/consumer_install_smoke.sh — DEP_MODE=hex path (lines 47-76); OPS-01 guard (no
    hackney/finch in mix.lock); INCLUDE_INBOUND flag; inbound version var.
    .github/workflows/post-publish-smoke.yml — smoke job chain; inbound compatibility check
    (grep output for mailglass == 1.7.0, sets include_inbound=true); known #32 hackney
    false-positive (CLOSED issue — distinguish from real regression).
    MAINTAINING.md — Step 4 (60-minute window manual smoke), Retract Decision Tree.
  </read_first>

  <action>
    Step 4.1 — Run consumer smoke within 60-minute window (REG-12):
    Run from repo root:
    `VERSION=1.7.0 VERSION_INBOUND=1.4.0 DEP_MODE=hex INCLUDE_INBOUND=true WORK_DIR=$(mktemp -d) bash scripts/consumer_install_smoke.sh`
    The smoke creates a fresh phx.new sandbox, injects == 1.7.0 deps, runs mix deps.get, mix
    mailglass.install, mix compile --warnings-as-errors, boots, and hits GET /dev/mail/ → 200.
    Exit 0 = pass. If it fails: check whether the OPS-01 guard is the failure (hackney in
    mix.lock) — that is issue #32, a known false-positive; distinguish and evaluate against the
    Retract Decision Tree (MAINTAINING.md) if it's a real installer regression. Set a timer
    from Task 4 completion; this step must run within 60 minutes.

    Step 4.2 — Verify mix deps.get Hex resolution (REL-02):
    Confirm that the consumer smoke's `mix deps.get` step resolved mailglass from Hex at 1.7.0
    and mailglass_inbound from Hex at 1.4.0 (visible in smoke script output). This is the
    REL-02 Hex-resolution proof.

    Step 4.3 — Monitor post-publish-smoke (REG-13):
    Run `gh run list --workflow post-publish-smoke.yml --limit 3` to find the run triggered by
    `release: published`. Monitor jobs: cron-guard → wait-for-index → wait-for-hexdocs →
    consumer-install → published-trust-journey → retracted-check.
    If no run fired automatically within 10 minutes of publish, dispatch manually:
    `gh workflow run post-publish-smoke.yml --field tag=mailglass-v1.7.0`
    All jobs must pass. Exception: if `consumer-install` fails with "missing hackney dependency"
    in the OPS-01 guard output, that is the known swoosh-1.26.x/#32 false-positive — it is NOT
    a release blocker. Document which step failed and that it matches #32 before accepting it
    as noise.

    Step 4.4 — 60-minute decision:
    If both manual smoke (4.1) and post-publish-smoke (4.3) pass, proceed to Task 6.
    If a real regression is found (non-#32 failure) within 60 minutes of publish:
    - Zero downloads: `mix hex.publish --revert <version>` for affected packages (Retract rule 4)
    - Non-zero downloads or past 60 min: evaluate Retract rules 1–3 and open a patch PR

    Step 4.5 — guard-release-trigger follow-up:
    Confirm `guard-release-trigger.yml` was exercised by PR #84:
    `gh run list --workflow guard-release-trigger.yml --limit 5`
    If it ran and passed, note it for REQUIREMENTS.md "Future Requirements" follow-up: registering
    it as a required branch-protection check. No action needed now — this is informational.
  </action>

  <acceptance_criteria>
    REG-12: `scripts/consumer_install_smoke.sh` exits 0 AND `mix deps.get` output (in smoke log)
    shows mailglass 1.7.0 and mailglass_inbound 1.4.0 fetched from Hex.

    REG-13: `gh run view` of post-publish-smoke.yml shows all jobs passed, OR the only red job
    is `consumer-install` with output containing "hackney" (issue #32 false-positive, acceptable
    non-blocker). Do not accept any other failure as noise.
  </acceptance_criteria>

  <done>
    Consumer smoke passed within 60-minute window. Hex resolution of all three packages confirmed.
    Post-publish smoke passed (or only #32 hackney noise red). Retract window decision: proceed.
  </done>
</task>


<!-- ═══════════════════════════════════════════════════════════════════
     WAVE 5 — Milestone closeout
     REG-14..15
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 6 (Wave 5): Milestone audit, archive, git tag v1.12 (REG-14..15)</name>
  <files>
    .planning/milestones/v1.12-ROADMAP.md,
    .planning/milestones/v1.12-REQUIREMENTS.md,
    .planning/milestones/v1.12-MILESTONE-AUDIT.md,
    .planning/milestones/MILESTONES.md,
    .planning/PROJECT.md,
    .planning/ROADMAP.md,
    .planning/RETROSPECTIVE.md,
    .planning/STATE.md
  </files>

  <read_first>
    STATE.md — Session continuity entry for v1.11 closeout (2026-06-16) for exact precedent:
    "Archived milestones/v1.11-ROADMAP.md + v1.11-REQUIREMENTS.md + v1.11-MILESTONE-AUDIT.md;
    MILESTONES.md/PROJECT.md/ROADMAP.md/RETROSPECTIVE.md evolved; REQUIREMENTS.md removed
    (fresh for next milestone); tagged v1.12."
    RESEARCH.md Q9 — milestone closeout artifacts and gsd-sdk count-inflation gotcha.
    VALIDATION.md REG-14, REG-15.

    COUNT-INFLATION WARNING: Do NOT use `gsd-sdk milestone.complete` raw counts for the audit.
    The CLI counts ALL .planning/phases/ directories including leftover 999.1, 999.2 backlog dirs.
    Manually correct to true v1.12 scope: 5 phases (104–108), 13 requirements (INSTALL-01..04,
    DOCS-01..04, OPS-01/02, A11Y-01, REL-01/02).
  </read_first>

  <action>
    Step 5.1 — Stability contract baseline check:
    Run `mix verify.stability_contract` from repo root to confirm the shipped v1.7.0 codebase
    passes all contract checks. This is the last required-lane proof before archiving.

    Step 5.2 — Archive ROADMAP:
    `cp .planning/ROADMAP.md .planning/milestones/v1.12-ROADMAP.md`

    Step 5.3 — Archive REQUIREMENTS:
    `cp .planning/REQUIREMENTS.md .planning/milestones/v1.12-REQUIREMENTS.md`

    Step 5.4 — Write MILESTONE-AUDIT (REG-14):
    Create `.planning/milestones/v1.12-MILESTONE-AUDIT.md`. Include:
    - Header: "# v1.12 Milestone Audit — Adopter Onboarding & Day-2 Confidence"
    - Audit date: 2026-06-17 (or actual date)
    - Verdict: passed
    - True scope stats (CORRECTED, not from gsd-sdk): 5 phases (104–108), 13 requirements, packages shipped 1.7.0/1.7.0/1.4.0
    - Per-requirement pass/fail table: all 13 REQ-IDs (INSTALL-01..04, DOCS-01..04, OPS-01/02, A11Y-01, REL-01, REL-02) marked Complete
    - Phase table: 104–108 all Complete
    - Evidence references: published Hex URLs, post-publish-smoke run URL, PR #84 URL

    Step 5.5 — Update MILESTONES.md:
    Add v1.12 row with CORRECTED counts (5 phases, 13 requirements, 1.7.0/1.7.0/1.4.0):
    `✅ v1.12 Adopter Onboarding & Day-2 Confidence — 5 phases (104–108), 13 reqs, shipped YYYY-MM-DD — mailglass 1.7.0 / mailglass_admin 1.7.0 / mailglass_inbound 1.4.0 (see milestones/v1.12-MILESTONE-AUDIT.md)`

    Step 5.6 — Update PROJECT.md:
    Advance the current milestone notation from v1.12 planned to v1.12 shipped. Update
    "Current state" line to reflect 1.7.0/1.7.0/1.4.0 as current live Hex versions.

    Step 5.7 — Update ROADMAP.md:
    Move Phase 108 row to Complete. Update the v1.12 milestone entry to ✅ SHIPPED. Add v1.12
    to the milestone history list at the top. Clear the active milestone section (or stub it with
    "no active milestone — see /gsd-new-milestone for next").

    Step 5.8 — Remove REQUIREMENTS.md (or replace with stub):
    Following v1.11 precedent, remove `.planning/REQUIREMENTS.md` (it is archived in
    v1.12-REQUIREMENTS.md). Use `git rm .planning/REQUIREMENTS.md` or replace with a one-line
    stub: "# Requirements — awaiting next milestone definition".

    Step 5.9 — Write RETROSPECTIVE entry:
    Append a v1.12 retrospective section to `.planning/RETROSPECTIVE.md`. Include:
    - Milestone summary (5 phases, onboarding + release)
    - What worked: sequential docs phases, allowlist-first publish, RP hands-free pipeline
    - Key lesson: publish allowlist must be refreshed before every non-trivial release (5 missing entries)
    - Key lesson: RP sed step already encodes the inbound re-pin — no separate fix(inbound): needed when inbound already has a feat bump
    - Release outcome: 1.7.0/1.7.0/1.4.0 live; first real multi-milestone-spanning cut since 1.6.2

    Step 5.10 — Update STATE.md:
    Update with a docs(state): commit reflecting Phase 108 complete, milestone v1.12 closed.

    Step 5.11 — Commit all milestone artifacts:
    `git add .planning/milestones/v1.12-ROADMAP.md .planning/milestones/v1.12-REQUIREMENTS.md .planning/milestones/v1.12-MILESTONE-AUDIT.md .planning/milestones/MILESTONES.md .planning/PROJECT.md .planning/ROADMAP.md .planning/RETROSPECTIVE.md .planning/STATE.md`
    Optionally add REQUIREMENTS.md removal: `git rm .planning/REQUIREMENTS.md`
    Commit: `docs(state): v1.12 complete — mailglass 1.7.0/1.7.0/1.4.0 on Hex`

    Step 5.12 — Tag v1.12 (REG-15):
    `git tag v1.12`
    `git push origin v1.12`

    Step 5.13 — Push commit:
    `git push origin main`

    NOTE: The git tag v1.12 is a milestone marker, not a Hex release tag. The Hex release tags
    (mailglass-v1.7.0, mailglass_admin-v1.7.0, mailglass_inbound-v1.4.0) were created by the
    Release Please pipeline automatically.
  </action>

  <acceptance_criteria>
    REG-14: `ls .planning/milestones/v1.12-*` returns at least three files:
    v1.12-ROADMAP.md, v1.12-REQUIREMENTS.md, v1.12-MILESTONE-AUDIT.md.
    `cat .planning/milestones/v1.12-MILESTONE-AUDIT.md | grep 'REL-01'` returns "Complete" line.
    `cat .planning/milestones/v1.12-MILESTONE-AUDIT.md | grep 'REL-02'` returns "Complete" line.
    MILESTONES.md contains "v1.12" with "1.7.0" and "5 phases" (not inflated by 999.x dirs).

    REG-15: `git tag -l v1.12` returns "v1.12".
    `git ls-remote --tags origin v1.12` returns the tag ref on origin.
  </acceptance_criteria>

  <done>
    Milestone v1.12 audited (5 phases, 13/13 requirements Complete, 1.7.0/1.7.0/1.4.0 on Hex).
    Three archive files created under .planning/milestones/. MILESTONES.md, PROJECT.md, ROADMAP.md,
    RETROSPECTIVE.md, STATE.md all updated. REQUIREMENTS.md removed. v1.12 tag on origin. v1.12
    milestone is CLOSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Hex publish | Irreversible action: once mailglass 1.7.0 is on Hex and 60 min pass, it cannot be unpublished |
| RP PR diff → maintainer review | Load-bearing PR (dep-pin sync step inside); must be human-reviewed before auto-merge fires |
| publish-hex.yml → hex-publish environment | HEX_API_KEY scoped to the hex-publish GitHub Environment; never exposed to PR jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | .release-please-manifest.json | mitigate | Never hand-edit; let Release Please manage; MAINTAINING.md Step 2 explicitly prohibits manual edits |
| T-108-02 | Information Disclosure | HEX_API_KEY secret | mitigate | Key scoped to hex-publish GitHub Environment only; not visible to PR jobs per CLAUDE.md Commit & Branch Conventions |
| T-108-03 | Repudiation | Hex publish origin | mitigate | publish-hex.yml gated on `gate-ci-green`; requires the release tag (never main); all publish runs logged in GitHub Actions |
| T-108-04 | Denial of Service | Anti-recursion stall (no publish fan-out) | accept | Self-healed by hourly cron + gate-ci-green auto-dispatch; manual recovery via `gh workflow run release-please.yml`; acceptable latency ≤ 60 min |
| T-108-05 | Elevation of Privilege | Dispatch publish-hex.yml from main | mitigate | Workflow docs and MAINTAINING.md both state "never dispatch from main"; always use `--ref mailglass-v1.7.0 --field tag=mailglass-v1.7.0` |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no new packages; publish ceremony uses existing mix toolchain only |
</threat_model>

<verification>
Full phase gate (all must pass before Phase 108 is marked Complete):

REG-01: `gh run list --workflow ci.yml --branch main --limit 5` — green required lanes on last non-docs SHA
REG-02: `mix mailglass.publish.check --package mailglass` exits 0; grep 'learning-path.md' .planning/publish/mailglass-files.expected matches
REG-03: `mix mailglass.publish.check --package mailglass_admin` exits 0
REG-04: `mix mailglass.publish.check --package mailglass_inbound` exits 0
REG-05: `gh pr diff 84` shows "1.7.0", "1.7.0", "1.4.0" in manifest and all three @version attrs
REG-06: `gh pr diff 84 -- mailglass_inbound/mix.exs | grep '== 1.7.0'` returns a `+` line
REG-07: CHANGELOG.md diffs in PR #84 show ## [1.7.0] and ## [1.4.0] headings with feat/fix entries
REG-08: PR #84 merged; publish fan-out visible in GitHub Actions (or stall recovery applied)
REG-09: `mix hex.info mailglass 1.7.0` contains "Released:"
REG-10: `mix hex.info mailglass_admin 1.7.0` contains "Released:"
REG-11: `mix hex.info mailglass_inbound 1.4.0 | grep mailglass` returns `mailglass == 1.7.0`
REG-12: `scripts/consumer_install_smoke.sh` exits 0 with DEP_MODE=hex VERSION=1.7.0 VERSION_INBOUND=1.4.0
REG-13: post-publish-smoke.yml run passes (or only #32 hackney noise in consumer-install)
REG-14: `ls .planning/milestones/v1.12-*` returns 3 files; MILESTONE-AUDIT.md lists all 13 REQ-IDs Complete
REG-15: `git tag -l v1.12` returns "v1.12" and `git ls-remote --tags origin v1.12` confirms push
</verification>

<success_criteria>
1. mailglass 1.7.0 is live on Hex: `mix hex.info mailglass 1.7.0` contains "Released:"
2. mailglass_admin 1.7.0 is live on Hex: `mix hex.info mailglass_admin 1.7.0` contains "Released:"
3. mailglass_inbound 1.4.0 is live on Hex with correct pin: `mix hex.info mailglass_inbound 1.4.0 | grep mailglass` returns `mailglass == 1.7.0`
4. mix deps.get resolves all three from Hex (consumer smoke passes, REG-12)
5. Post-publish smoke passes (REG-13), or only the known #32 hackney false-positive is red
6. v1.12 milestone is archived: `.planning/milestones/v1.12-MILESTONE-AUDIT.md` exists with 13/13 requirements Complete, scope corrected to 5 phases (not inflated by 999.x dirs)
7. git tag v1.12 exists on origin (REG-15)
</success_criteria>

<output>
Create `.planning/phases/108-release-cut-milestone-closeout/108-01-SUMMARY.md` when done.

Summary must include:
- Hex versions confirmed live (with `mix hex.info` output excerpts)
- Inbound pin verification (`mailglass == 1.7.0`)
- Consumer smoke exit code + POST /dev/mail/ result
- post-publish-smoke run URL + outcome
- Milestone audit: scope counts (5 phases, 13 reqs), all 13 REQ-IDs confirmed Complete
- Git tag v1.12: `git tag -l v1.12` and push confirmation
- Any stall recovery steps taken (anti-recursion, gate-ci-green, fallback dispatch)
</output>
