---
phase: 113-data-display
plan: 03
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound/overview.ex
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "On the Inbound surface the operator sees a semantic table at >=768px and a card/list at <768px, both fed by the same @records (D-01)."
    - "Selecting a record works identically in table and card form: same phx-click=select_inbound/phx-value-id, same aria-selected, same non-color selected cue (D-01)."
    - "Result count is always visible and pagination reads from @page_meta — never faked from list length (D-02)."
    - "Outcome renders as icon+label+color via status_badge/1 (normalized through normalize_inbound_outcome/1) in both table cells and cards; outcome column is first; recipients stay masked (D-04)."
    - "Long record ids, tenant ids, provider message ids, mailbox names, and received timestamps truncate-with-title or no-wrap and never force horizontal scroll (D-07)."
    - "Distinct empty / error / permission-denied / stale states render via Components.data_state/1 on the inbound surface; permission-denied is never rendered as no-tenant/empty (D-05)."
    - "Stale-data stays render-time: no assign_async, no inbound-loading, no polling (D-06)."
    - "Every inbound KPI tile routes through Components.stat_card/1 (D-03)."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      provides: "Dual table+card presentation, four data-state branches via data_state/1, long-value handling, inbound nouns"
      contains: "inbound-records-table"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "Data-state signal plumbed into records_list and detail-error area; synchronous invariant preserved"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/overview.ex"
      provides: "Certified inbound KPI stat_card call sites"
    - path: "mailglass_admin/test/mailglass_admin/inbound_live_test.exs"
      provides: "Table/card/state/long-value/masking regression for both presentations"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "status_badge/1 via normalize_inbound_outcome/1, mask_recipient/1, data_state/1"
      pattern: "Components\\.(status_badge|mask_recipient|normalize_inbound_outcome|data_state)"
    - from: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      via: "records_list/1 call site passing records, page_meta, selected_record, empty_state/data-state signal"
      pattern: "RecordsList\\.records_list"
---

<objective>
Mirror Plan 02 on the Inbound surface (the declared sibling clone): upgrade `records_list/1` into a DUAL presentation — semantic `<table>` at `>=768px` (`hidden md:block`) and `<ul>` of card buttons at `<768px` (`md:hidden`) from the same `@records` (DATA-01, D-01/D-02) — using inbound nouns (Outcome, Mailbox, envelope recipient, received_at). Wire the four distinct data states via `Components.data_state/1` (DATA-03) keeping permission-denied distinct from no-tenant, keep outcome as icon+label+color via `status_badge/1` normalized through `normalize_inbound_outcome/1` (DATA-04), apply per-field long-value handling (DATA-05), and certify inbound KPI call sites route through `stat_card/1` (DATA-02).

Purpose: Close DATA-01/04/05 for the inbound surface and DATA-03/02's inbound slice while preserving every Phase 112 contract and the gate-locked synchronous-LiveView invariant. Runs in parallel with Plan 02 (deliveries) — disjoint files.
Output: Dual-presentation `records_list/1`, certified inbound KPIs, ExUnit regression for both presentations. No CSS bundle rebuild here (Plan 04 owns it).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-data-display/113-CONTEXT.md
@.planning/phases/113-data-display/113-PATTERNS.md
@.planning/phases/113-data-display/113-UI-SPEC.md
@.planning/phases/113-data-display/113-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan introduces testids `inbound-records-table`, `inbound-records-cards`, and per-badge `inbound-outcome-{outcome}`, and adds `data-state-empty`/`-error`/`-permission-denied`/`-stale` rendering to the inbound surface. (See Plan 01 for the full phase artifact list.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual table+card presentation in records_list/1 with long-value handling</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/records_list.ex, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (the file being modified — full current module: moduledoc declaring it a sibling clone, result-count header lines 33-38, the `<ul data-testid="inbound-records-list">` row markup lines 56-97 to be supplemented, `record_outcome/1` line 200, `matched_mailbox_label/1` 202-207, `normalize_inbound_outcome/1` usage line 80, `selected?/2`, `row_classes/2`, `format_datetime/1`)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (the Plan-02 sibling — apply the identical table+card structure; this is the design analog)
    - mailglass_admin/lib/mailglass_admin/components.ex (`status_badge/1`, `mask_recipient/1`, `normalize_inbound_outcome/1` — call sites pass only `status`+`size`; base `badge` class owned by component)
    - .planning/phases/113-data-display/113-PATTERNS.md (records_list section: inbound nouns, column/card order, new testids `inbound-records-table`/`-cards`, per-badge `inbound-outcome-{outcome}`; "Cross-file coupling": keep `inbound-record-row` reachable for `noMatchRow()`)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Responsive List Presentation (DATA-01)" inbound column + card order; "Long-Value Handling (DATA-05)" class table; "Severity/Status Encoding (DATA-04)" `inbound-outcome-{outcome}` requirement)
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs (the file being modified — existing list/detail/masking assertion patterns)
  </read_first>
  <behavior>
    - Test 1: with records present, the rendered list HTML contains both `inbound-records-table` and `inbound-records-cards` testids.
    - Test 2: the desktop table uses `<table>` with `<th scope="col">` in order Outcome, Mailbox, Tenant, Provider, Received.
    - Test 3: both presentations carry `phx-click="select_inbound"` and `phx-value-id`; the selected record carries `aria-selected="true"` in both.
    - Test 4: the existing `inbound-record-row` testid remains reachable (so `noMatchRow()` in structural.spec.js is not orphaned), and outcome badges carry `inbound-outcome-{outcome}` testids.
    - Test 5: envelope recipient renders via `mask_recipient/1` in both presentations.
    - Test 6: a record id renders with `title={record.id}` and `mono`/`truncate` in both presentations; result count reads from `@page_meta.total_count`.
  </behavior>
  <action>
    Apply the identical dual-presentation upgrade Plan 02 makes to deliveries, with inbound nouns. Keep the `inbound-result-count` header and `pagination_controls/1` unchanged. In the non-empty branch render BOTH from the same `@records`:
    (a) a `hidden md:block` wrapper with `<table data-testid="inbound-records-table" class="table w-full table-fixed">`, `<thead>` of `<th scope="col" class="text-label font-bold uppercase text-secondary">` in column order Outcome → Mailbox → Tenant → Provider → Received, and a `<tbody>` `<tr :for={record <- @records}>` whose first cell is `<Components.status_badge status={Components.normalize_inbound_outcome(record_outcome(record))} size={:sm} />` carrying a `data-testid={"inbound-outcome-#{...}"}` on the badge wrapper. Row selection wiring (`phx-click="select_inbound"`, `phx-value-id={record.id}`, `aria-current`/`aria-selected`, `row_classes/2` non-color cue) must match the card exactly.
    (b) a `md:hidden` `<ul data-testid="inbound-records-cards" class="divide-y divide-base-300">` of `<li><button>` cards in field order Outcome badge first → Mailbox → Tenant → Provider → Received, each metadata field with a visible `text-label font-bold uppercase text-secondary` label. Preserve the existing `inbound-records-list` testid and keep `inbound-record-row` reachable on at least one presentation so `noMatchRow()` (which filters `inbound-record-row` by the "No match" `.badge-warning`) still resolves — Plan 04 migrates the structural consumers deliberately; do not orphan them here.
    Apply UI-SPEC DATA-05 long-value classes: record/provider/tenant ids → `mono min-w-0 truncate` + `title`; mailbox → `min-w-0 truncate` + `title={matched_mailbox_label(record)}`; received timestamp → `mono whitespace-nowrap` + `title={format_datetime(record.received_at)}`; envelope recipient → `min-w-0 truncate` + `title={mask_recipient(...)}`. Every truncating flex/grid cell gets `min-w-0`; `table-fixed`; no `overflow-auto` default; no page-level horizontal scroll. Route ALL outcome/status rendering through `Components.status_badge/1` (via `normalize_inbound_outcome/1`) — no page-local badge map, no second mask helper, no `delivery_status_badge`/`inbound_status_badge` clone (RESEARCH anti-pattern). Keep interpolation HEEx-escaped (no `raw/1`).
    Add the six behavior tests above to `inbound_live_test.exs`, asserting BOTH presentations.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Both `inbound-records-table` and `inbound-records-cards` literals appear in records_list.ex.
    - The pre-existing `inbound-records-list` and `inbound-record-row` testids are still present (not orphaned).
    - Outcome badges carry `inbound-outcome-` testids; outcome column is first; recipients use `mask_recipient/1`.
    - No `defp badge_class`, no `inbound_status_badge`/`delivery_status_badge` helper, no second mask helper in records_list.ex.
    - `grep -v '^#' mailglass_admin/lib/mailglass_admin/inbound/records_list.ex | grep -c 'raw('` is `0`.
    - `mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` passes including the six new tests.
  </acceptance_criteria>
  <done>Inbound records render as a semantic desktop table and mobile cards from one assign set with identical selection semantics, normalized outcome badges, masked recipients, per-field long-value handling, preserved result-count/pagination, and legacy testids kept reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four distinct data-state branches on the inbound surface (synchronous-only)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/records_list.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (the file being modified — empty branch lines 39-54 with `empty_state` atom `[:no_tenant, :truly_empty, :filtered]` and `empty_heading/1`/`empty_body/1` lines 178-190; these seed the `:empty` no-data variant)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex (the file being modified — `records_list/1` call site lines 432-438 passing `empty_state={@empty_state}`; `empty_state_for/2` lines 639-650 → `:no_tenant|:filtered|:truly_empty`; `detail_error_for/2` 906-908 → `:not_found|nil` rendered at `data-testid="inbound-detail-error"` line 453; this is where the data-state signal is derived)
    - mailglass_admin/lib/mailglass_admin/components.ex (Wave-1 `data_state/1` — consume it)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Distinct Data-State Templates (DATA-03)" exact copy/testid/icon; "Copywriting Contract")
    - .planning/phases/113-data-display/113-PATTERNS.md (inbound_live section: synchronous-loading invariant locked by `structural.spec.js:937` — no `assign_async`/`inbound-loading`/"Loading InboundMessages..."; permission-denied as no-data is an Information-Disclosure regression)
  </read_first>
  <behavior>
    - Test 1: a no-data inbound render emits `data-state-empty` with a "No records"-family heading; the existing `:no_tenant`/`:truly_empty`/`:filtered` copy distinctions survive within the `:empty` variant.
    - Test 2: an error/unavailable signal emits `data-state-error` with "Record data unavailable" — distinct from empty.
    - Test 3: a permission-denied signal emits `data-state-permission-denied` with "Access restricted" — distinct testid/icon from `:no_tenant` and `:empty` (security).
    - Test 4: a stale signal emits `data-state-stale` with "Data may be out of date".
    - Test 5: the inbound_live source contains no `assign_async`, no `inbound-loading`, no "Loading InboundMessages" string.
  </behavior>
  <action>
    Introduce an inbound data-state signal so `records_list/1` (and/or the detail-error area) renders one of the four DISTINCT `Components.data_state/1` kinds. Drive the `:empty` variant from the existing `empty_state` atom (`:no_tenant`/`:truly_empty`/`:filtered`), preserving those copy distinctions, and render `:error`, `:permission_denied`, `:stale` as DISTINCT `data_state/1` calls that must NOT collapse into the `:no_tenant`/`:truly_empty` branches (D-05; permission-denied-as-no-data is a gate-relevant Information-Disclosure regression). Derive the signal in `inbound_live.ex` alongside `empty_state_for/2`/`detail_error_for/2` (the `:not_found` detail error may map to the `:error` data-state template). Keep everything render-time — do NOT add `assign_async`, polling, streams, an `inbound-loading` testid, or a "Loading InboundMessages..." string; `structural.spec.js:937` asserts their absence (D-06). Use exact UI-SPEC headings/bodies; domain nouns only; banned words enforced; never echo raw UUIDs/tenant/provider IDs.
    Add the five behavior tests above to `inbound_live_test.exs`.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - All four `data-state-*` testids reachable on the inbound surface via `Components.data_state/1`.
    - `grep -E 'assign_async|inbound-loading|Loading InboundMessages' mailglass_admin/lib/mailglass_admin/inbound_live.ex` returns nothing (synchronous invariant holds).
    - Permission-denied is distinct from `:no_tenant`/`:empty` (no Information-Disclosure collapse).
    - `mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` passes including the five new tests.
  </acceptance_criteria>
  <done>The inbound surface renders four distinct, render-time data states through `data_state/1` with UI-SPEC copy, the synchronous-LiveView invariant intact, and no permission-as-no-data collapse.</done>
</task>

<task type="auto">
  <name>Task 3: Certify inbound KPI stat_card call sites (DATA-02)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/overview.ex, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/overview.ex (the file being modified — the four `Components.stat_card` KPI tiles lines 25-51: total, no-match, accepted, no-match-rate)
    - mailglass_admin/lib/mailglass_admin/components.ex (`stat_card/1` real signature)
    - .planning/phases/113-data-display/113-PATTERNS.md (operator_live/overview certification pattern; gate-banned raw card shape)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Canonical Stat-Card Usage (DATA-02)")
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs (the file being modified)
  </read_first>
  <action>
    Verify every inbound-overview KPI tile routes through `Components.stat_card/1` (four call sites lines 25-51) and add a LiveView test asserting all four inbound KPI testids (`inbound-overview-total`, `-no-match`, `-accepted`, `-no-match-rate`) render through the primitive with meaningful (non-dash) values. If any page-local stat markup or `defp stat(` is found, migrate it to `Components.stat_card/1`; otherwise certification-only. Do not change `stat_card/1`. Add assertions to `inbound_live_test.exs`.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Components.stat_card' mailglass_admin/lib/mailglass_admin/inbound/overview.ex` is `>= 4`.
    - No `defp stat(` and no raw `class="card bg-base-200` stat shape in overview.ex.
    - A test asserts the inbound KPI tiles render meaningful non-dash values.
    - `mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <done>All inbound KPI tiles are certified to route through the canonical `stat_card/1` with meaningful values and no page-local stat markup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator actor → tenant-scoped inbound read model | Inherited from Phase 112; this plan renders only the already-scoped `@records` assign behind the optional-dependency gateway, never a raw admin Repo |
| record fields (envelope_recipient, id, provider/mailbox) → table/card render | Long untrusted values cross into truncation/title markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-08 | Information Disclosure | records_list recipient cells | mitigate | Envelope recipient rendered only via `Components.mask_recipient/1` in BOTH presentations; test asserts masking; no second mask helper |
| T-113-09 | Information Disclosure | cross-tenant inbound data | mitigate | Renders only the already tenant-scoped `@records` assign behind the optional-dependency gateway; no admin Repo read added (RESEARCH V4) |
| T-113-10 | Information Disclosure / Repudiation | permission-denied vs no-tenant | mitigate | `:permission_denied` is a distinct `data_state/1` kind, never rendered as `:no_tenant`/no-data |
| T-113-11 | Tampering (XSS) | long-value `title`/cell interpolation | mitigate | HEEx escaping preserved; no `raw/1`; acceptance greps `raw(` count == 0 |
| T-113-12 | Denial of Service | stale-data implementation | mitigate | Render-time only; no `assign_async`/polling/streams (synchronous invariant, gate-asserted) |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan/phase (RESEARCH audit "No install") |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs --warnings-as-errors` passes.
- Both presentations carry selection semantics and masked recipients.
- Four distinct data-state testids reachable; synchronous invariant intact.
- All inbound KPI tiles route through `stat_card/1`.
- Bundle rebuild deferred to Plan 04.
</verification>

<success_criteria>
- DATA-01 (inbound): semantic table >=768px, cards <768px, one assign set.
- DATA-02 (inbound slice): all inbound KPIs on canonical `stat_card/1`.
- DATA-03 (inbound slice): four distinct render-time data states, permission-denied never no-data.
- DATA-04 (inbound): outcome icon+label+color in both presentations with `inbound-outcome-{outcome}` testids, outcome column first.
- DATA-05 (inbound): per-field long-value handling, no horizontal overflow path.
</success_criteria>

<output>
Create `.planning/phases/113-data-display/113-03-SUMMARY.md` when done.
</output>
