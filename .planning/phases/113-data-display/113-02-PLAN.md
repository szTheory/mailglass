---
phase: 113-data-display
plan: 02
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "On the Deliveries surface the operator sees a semantic table at >=768px and a card/list at <768px, both fed by the same @deliveries (D-01)."
    - "Selecting a delivery works identically in table and card form: same phx-click/phx-value-id, same aria-selected/aria-current, same non-color border-l-4 selected cue (D-01)."
    - "Result count is always visible and pagination chrome appears only for multiple pages, read from @page_meta — never faked from list length (D-02)."
    - "Status renders as icon+label+color via status_badge/1 in both table cells and cards; recipients stay masked; the status column is first/leftmost (D-04)."
    - "Long delivery IDs, tenant ids, provider message ids, and timestamps truncate-with-title or no-wrap and never force horizontal scroll (D-07)."
    - "Distinct empty / error / permission-denied / stale states render via Components.data_state/1 on the deliveries surface (D-05)."
    - "Every operator KPI tile routes through Components.stat_card/1 with no page-local stat markup (D-03)."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      provides: "Dual table+card presentation, four data-state branches via data_state/1, long-value handling"
      contains: "operator-deliveries-table"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Certified stat_card call sites; data-state signal plumbed into deliveries_list"
    - path: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      provides: "Table/card/state/long-value/masking regression assertions for both presentations"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "status_badge/1 for status, mask_recipient/1 for recipient, data_state/1 for the four states, stat_card untouched"
      pattern: "Components\\.(status_badge|mask_recipient|data_state)"
    - from: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      via: "deliveries_list/1 call site passing deliveries, page_meta, selected_delivery, pagination paths"
      pattern: "DeliveriesList\\.deliveries_list"
---

<objective>
Upgrade the Deliveries surface from a list-of-buttons into a DUAL presentation: a semantic `<table>` at `>=768px` (`hidden md:block`) and a `<ul>` of card buttons at `<768px` (`md:hidden`), both rendered from the same `@deliveries` assign with identical selection semantics, result-count, and pagination (DATA-01, D-01/D-02). Wire the four distinct data states via the Wave-1 `Components.data_state/1` (DATA-03), keep status as icon+label+color via `status_badge/1` (DATA-04), apply per-field long-value handling (DATA-05), and certify all operator KPI call sites route through `stat_card/1` (DATA-02).

Purpose: Close DATA-01/04/05 for the operator surface and DATA-03/02's operator slice while preserving every Phase 112 contract (URL state, selection, masking, pagination). Runs in parallel with Plan 03 (inbound) — disjoint files.
Output: Dual-presentation `deliveries_list/1`, certified operator KPIs, and ExUnit regression for both presentations. No CSS bundle rebuild here (Plan 04 owns the single committed `priv/static/app.css`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-data-display/113-CONTEXT.md
@.planning/phases/113-data-display/113-PATTERNS.md
@.planning/phases/113-data-display/113-UI-SPEC.md
@.planning/phases/113-data-display/113-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan introduces testids `operator-deliveries-table` and `operator-deliveries-cards`, and adds `data-state-empty`/`-error`/`-permission-denied`/`-stale` rendering to the deliveries surface. (See Plan 01 for the full phase artifact list; `data_state/1` and the new icons are produced by Plan 01.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual table+card presentation in deliveries_list/1 with long-value handling</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (the file being modified — the full current module: result-count header lines 23-28, the `<ul data-testid="operator-deliveries-list">` row markup lines 59-97 to be supplemented, `selected?/2` 175-176, `row_classes/2` 178-182 with the `border-l-4 border-primary` non-color cue, `result_count_label/1` 168-173, `pagination_controls/1` 111-166, `label/1` 184-191, `format_datetime/1` 193-196)
    - mailglass_admin/lib/mailglass_admin/components.ex (`status_badge/1` and `mask_recipient/1` signatures — call sites pass `status`+`size` only; base `badge` class is owned by the component)
    - .planning/phases/113-data-display/113-PATTERNS.md (deliveries_list section: selection contract, new testids `operator-deliveries-table`/`-cards`, long-value rows; "Cross-file coupling": keep `operator-deliveries-list` reachable)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Responsive List Presentation (DATA-01)" deliveries column + card field order; "Long-Value Handling (DATA-05)" per-field class table; "Responsive Behavior Contract"; "Accessibility Contract" table/card semantics)
    - .planning/phases/113-data-display/113-RESEARCH.md (Pattern 1 dual presentation; Pattern 3 long-value)
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (the file being modified — existing list/detail/masking assertion patterns, e.g. the masking assertion and the `element("[data-testid='operator-deliveries-list']")` scoping pattern)
  </read_first>
  <behavior>
    - Test 1: with deliveries present, the rendered list HTML contains both `operator-deliveries-table` and `operator-deliveries-cards` testids.
    - Test 2: the desktop table uses `<table>` with `<th scope="col">` headers in the order Status, Recipient, Tenant, Provider, Event, Last event.
    - Test 3: both presentations carry `phx-click="select_delivery"` and `phx-value-id` for each delivery; the selected delivery carries `aria-selected="true"` in both.
    - Test 4: recipients render via `mask_recipient/1` in both presentations (no raw recipient string).
    - Test 5: a delivery id renders with a `title` attribute equal to the id and a `truncate`/`mono` class in both presentations.
    - Test 6: result count still reads from `@page_meta.total_count` (assert "1 result" for total_count 1 regardless of list length).
  </behavior>
  <action>
    Inside `deliveries_list/1`, keep the existing `operator-result-count` header above and `pagination_controls/1` below unchanged. Replace the single non-empty `<ul data-testid="operator-deliveries-list">` branch with TWO presentations rendered from the same `@deliveries`, both inside the non-empty branch:
    (a) a `hidden md:block` wrapper containing a semantic `<table data-testid="operator-deliveries-table" class="table w-full table-fixed">` with `<thead><tr>` of `<th scope="col" class="text-label font-bold uppercase text-secondary">` in column order Status → Recipient (masked) → Tenant → Provider → Event → Last event, and a `<tbody>` with a `<tr :for={delivery <- @deliveries}>` whose first cell hosts `<Components.status_badge status={delivery.status} size={:sm} />`. The row's selection wiring (`phx-click="select_delivery"`, `phx-value-id={delivery.id}`, `aria-current`/`aria-selected` from `selected?/2`, and the `row_classes/2` `border-l-4` non-color cue applied as a left-border accent) must match the card exactly.
    (b) a `md:hidden` `<ul data-testid="operator-deliveries-cards" class="divide-y divide-base-300">` of `<li><button …>` cards. Preserve the EXISTING `operator-deliveries-list` testid by keeping it on the cards `<ul>` (or wrapping) so `structural.spec.js` and `operator_live_test.exs` consumers do not orphan — Plan 04 migrates them deliberately; do not remove it in isolation. Card field order: Status badge first/prominent → Recipient (masked) → Tenant → Provider → Event → Timestamp, each metadata field with a visible `text-label font-bold uppercase text-secondary` label.
    Apply per-field long-value handling from the UI-SPEC DATA-05 table: delivery id and provider/tenant ids → `mono min-w-0 truncate` with `title={...}`; timestamp → `mono whitespace-nowrap` with `title={format_datetime(...)}`; recipient → `min-w-0 truncate` with `title={mask_recipient(...)}`. Ensure every flex/grid cell that truncates has `min-w-0`. Use `table-fixed` so columns cannot overflow; no page-level horizontal scroll, no `overflow-auto` default.
    Keep all rendering routed through `Components.*` — do NOT add a page-local badge class map, a second mask helper, or page-local stat markup. Keep `selected?/2`, `row_classes/2`, `result_count_label/1`, `label/1`, `format_datetime/1` as the shared helpers feeding both presentations. All interpolation stays HEEx-escaped (no `raw/1`) so long titles/recipients cannot inject markup.
    Add the six behavior tests above to `operator_live_test.exs`, asserting BOTH presentations (not just one) per RESEARCH Pitfall 2.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Both `operator-deliveries-table` and `operator-deliveries-cards` literals appear in deliveries_list.ex.
    - The pre-existing `operator-deliveries-list` testid is still present (not orphaned).
    - The desktop presentation uses `<table>` + `<th scope="col">`; the status column is first.
    - No `defp badge_class`, no second mask helper, and no raw `class="card bg-base-200 ..."` stat shape appears in deliveries_list.ex.
    - `grep -v '^#' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex | grep -c 'raw('` is `0`.
    - `mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` passes including the six new tests.
  </acceptance_criteria>
  <done>Deliveries render as a semantic desktop table and mobile cards from one assign set with identical selection semantics, masked recipients, per-field long-value handling, preserved result-count/pagination, and the legacy testid kept reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four distinct data-state branches on the deliveries surface</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (the file being modified — current empty branch lines 29-57 with `operator-empty-filtered`/`operator-empty-truly` and `filters_active?`; these seed the `:empty` no-data variant per D-05)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the file being modified — `deliveries_list/1` call site lines 504-524 and the assign pipeline; how `filters_active?` / page_meta are derived; where a data-state signal assign would be added)
    - mailglass_admin/lib/mailglass_admin/components.ex (the Wave-1 `data_state/1` signature and its `kind` values — consume it; do not reimplement)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Distinct Data-State Templates (DATA-03)" exact copy/testid/icon per kind; "Copywriting Contract")
    - .planning/phases/113-data-display/113-PATTERNS.md (D-05 seeding rule: existing empty branches seed no-data; permission/stale/error must NOT collapse into them)
    - .planning/phases/113-VALIDATION.md (the Nyquist behavior→test map for DATA-03 if present)
  </read_first>
  <behavior>
    - Test 1: a no-data deliveries render path emits `data-state-empty` with the "No deliveries" heading.
    - Test 2: an error/unavailable signal emits `data-state-error` with "Delivery data unavailable" — distinct testid and icon from empty.
    - Test 3: a permission-denied signal emits `data-state-permission-denied` with "Access restricted" — never the no-data testid/copy (security).
    - Test 4: a stale signal emits `data-state-stale` with "Data may be out of date".
    - Test 5: no two of the four states share a testid; the legacy filtered/truly-empty distinction is preserved within the `:empty` variant.
  </behavior>
  <action>
    Introduce a deliveries data-state signal so `deliveries_list/1` renders one of the four DISTINCT `Components.data_state/1` kinds when there is no row data to show. Reuse the existing `filters_active?` + true-empty branches to drive the `:empty` variant's filtered-vs-recorded copy (per UI-SPEC: "No {noun} match the current filters." vs "No {noun} have been recorded yet."), and render `:error`, `:permission_denied`, `:stale` as DISTINCT `data_state/1` calls — they must NOT collapse into the empty branch (D-05, RESEARCH Pitfall 3). Plumb the signal from `operator_live.ex` as a new assign (e.g. a `:data_state` atom or by passing the existing empty/error context through), keeping it render-time only — do NOT add `assign_async`, polling, or streams (D-06; the synchronous-LiveView invariant is gate-locked). Use the exact headings/bodies from the UI-SPEC Copywriting Contract; domain nouns only; "Oops"/"Whoops"/"Uh oh"/"Something went wrong"/bare dashes banned. Do not echo raw UUIDs/tenant/provider IDs into data-state copy.
    Add the five behavior tests above to `operator_live_test.exs`.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - All four `data-state-*` testids are reachable on the deliveries surface via `Components.data_state/1` (no page-local reimplementation).
    - The deliveries source contains no `assign_async`, no `inbound-loading`-style async addition (synchronous invariant holds).
    - Permission-denied copy/testid is distinct from no-data (no Information-Disclosure collapse).
    - `mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` passes including the five new tests.
  </acceptance_criteria>
  <done>The deliveries surface renders four distinct, render-time data states through the shared `data_state/1` primitive with UI-SPEC copy, no async machinery, and no permission-as-empty collapse.</done>
</task>

<task type="auto">
  <name>Task 3: Certify operator KPI stat_card call sites (DATA-02)</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the file being modified — the four `Components.stat_card` KPI tiles lines 369-403: failures, orphans, suppressions, all-clear; the `all_clear_state/1` 1112-1113, `all_clear_label/1` 1115-1119, `all_clear_severity/1` 1121 helpers)
    - mailglass_admin/lib/mailglass_admin/components.ex (`stat_card/1` real signature — `state` `[:ready,:empty,:loading,:unavailable]`, `severity` `[:neutral,:info,:success,:warning,:error]`)
    - .planning/phases/113-data-display/113-PATTERNS.md (operator_live section: certify call sites, do not introduce page-local card markup; the gate-banned raw `class="card bg-base-200 ..."` shape)
    - .planning/phases/113-data-display/113-UI-SPEC.md ("Canonical Stat-Card Usage (DATA-02)")
  </read_first>
  <action>
    Verify every operator-overview KPI tile already routes through `Components.stat_card/1` (it does — four call sites at lines 369-403) and add a LiveView test asserting all four operator KPI testids (`operator-overview-health-failures`, `-orphans`, `-suppressions`, `-allclear`) render through the stat-card primitive and that the all-clear tile renders a real readable value ("All clear" / "Needs attention" / "Unavailable"), never a bare dash. If any page-local stat-card-like markup is found (raw `class="card bg-base-200 ..."` or a `defp stat(`), migrate it to `Components.stat_card/1`; otherwise this is certification-only. Do not change `stat_card/1` itself. Add the assertions to `operator_live_test.exs`.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Components.stat_card' mailglass_admin/lib/mailglass_admin/operator_live.ex` is `>= 4`.
    - No `defp stat(` and no raw `class="card bg-base-200` stat shape in operator_live.ex.
    - A test asserts the all-clear tile renders a non-dash readable value.
    - `mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <done>All operator KPI tiles are certified to route through the canonical `stat_card/1` with a real all-clear value and no page-local stat markup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator actor → tenant-scoped deliveries read model | Inherited from Phase 112; this plan renders only the already-scoped `@deliveries` assign, never a raw admin Repo |
| delivery fields (recipient, id, provider ids) → table/card render | Long untrusted values cross into truncation/title/tooltip markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-04 | Information Disclosure | deliveries_list recipient cells | mitigate | Recipient rendered only via `Components.mask_recipient/1` in BOTH presentations; test asserts no raw recipient; no second mask helper added |
| T-113-05 | Information Disclosure | cross-tenant data via display | mitigate | Renders only the already tenant-scoped `@deliveries` assign; no admin Repo read introduced (RESEARCH V4 Access Control) |
| T-113-06 | Tampering (XSS) | long-value `title`/cell interpolation | mitigate | HEEx escaping preserved; no `raw/1`; acceptance criterion greps `raw(` count == 0 |
| T-113-07 | Information Disclosure | permission-denied state | mitigate | `:permission_denied` is a distinct `data_state/1` kind, never rendered as no-data |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan/phase (RESEARCH audit "No install") |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors` passes.
- Both presentations carry selection semantics and masked recipients (asserted in tests).
- Four distinct data-state testids reachable on the deliveries surface.
- All operator KPI tiles route through `stat_card/1`.
- Bundle rebuild deferred to Plan 04.
</verification>

<success_criteria>
- DATA-01 (deliveries): semantic table >=768px, cards <768px, one assign set, no squished columns.
- DATA-02 (operator slice): all KPIs on canonical `stat_card/1`, real all-clear value.
- DATA-03 (deliveries slice): four distinct render-time data states via `data_state/1`.
- DATA-04 (deliveries): status icon+label+color in both presentations, status column first.
- DATA-05 (deliveries): per-field long-value handling, no horizontal overflow path.
</success_criteria>

<output>
Create `.planning/phases/113-data-display/113-02-SUMMARY.md` when done.
</output>
