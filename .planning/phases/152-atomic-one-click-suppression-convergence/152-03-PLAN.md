---
phase: 152-atomic-one-click-suppression-convergence
plan: 03
type: execute
wave: 3
depends_on: [152-01, 152-02]
files_modified:
  - lib/mailglass/lifecycle.ex
  - guides/unsubscribe.md
  - guides/production-go-live-checklist.md
  - docs/api_stability.md
  - test/mailglass/docs/unsubscribe_guide_test.exs
  - test/mailglass/docs_contract_test.exs
  - test/mailglass/stability_contract_test.exs
  - test/mailglass/compliance/unsubscribe_test.exs
autonomous: true
requirements: [UNSUB-07, UNSUB-08, UNSUB-09, UNSUB-10, UNSUB-11]
must_haves:
  truths:
    - "Published API and guide text describe atomic event plus address_stream suppression convergence, exact empty success, and genuine failure semantics without attributing the body contract to RFC 8058."
    - "The existing lifecycle callback signature and compliance config key remain compatible while their one-click execution is documented as separate best-effort post-commit work per D-11/D-12/D-18."
    - "Operations guidance tells adopters how to verify matching-stream preflight blocking and isolation without exposing tokens or private content."
    - "Executable docs/stability/config contracts fail on drift from the shipped Phase 152 behavior."
  artifacts:
    - path: "guides/unsubscribe.md"
      provides: "Adopter-facing one-click convergence, lifecycle, replay, failure, and verification contract"
    - path: "docs/api_stability.md"
      provides: "Stable route, response, lifecycle compatibility, and suppression scope contract"
    - path: "test/mailglass/docs/unsubscribe_guide_test.exs"
      provides: "Executable guide assertions"
    - path: "test/mailglass/stability_contract_test.exs"
      provides: "Public API/config compatibility assertions"
  key_links:
    - from: "guides/unsubscribe.md"
      to: "lib/mailglass/lifecycle.ex"
      via: "documented handle_event/2 signature and post-commit separate-Multi semantics"
      pattern: "handle_event"
    - from: "docs/api_stability.md"
      to: "test/mailglass/stability_contract_test.exs"
      via: "contract assertions for route/body/config/callback stability"
      pattern: "unsubscribe"
---

<objective>
Publish and lock executable API, configuration, compatibility, and operational evidence for Phase 152.

Purpose: Ensure adopters and future maintainers receive the exact shipped one-click convergence contract, including the required lifecycle ordering change and real enforcement expectations.
Output: Updated lifecycle moduledoc, unsubscribe/API/production guidance, and drift-detecting contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-CONTEXT.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-RESEARCH.md
@.planning/phases/152-atomic-one-click-suppression-convergence/152-02-SUMMARY.md
@lib/mailglass/lifecycle.ex
@guides/unsubscribe.md
@docs/api_stability.md
@guides/compatibility-and-deprecations.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the lifecycle config and public one-click contract in executable docs</name>
  <files>lib/mailglass/lifecycle.ex, guides/unsubscribe.md, docs/api_stability.md, test/mailglass/docs/unsubscribe_guide_test.exs, test/mailglass/compliance/unsubscribe_test.exs</files>
  <behavior>
    - Docs tests require exact empty 200 for successful convergence and privacy no-ops, byte-empty 500 for genuine convergence failure, and no redirect on RFC 8058 POST per D-03/D-07/D-18.
    - Docs tests require canonical event plus immutable address_stream suppression, replay/concurrency convergence, and delivery-derived normalized tenant/address/stream scope per D-01/D-04..D-09.
    - Config tests preserve `:compliance, lifecycle:` and `handle_event(Ecto.Multi.t(), map()) :: Ecto.Multi.t()` while docs require its returned Multi to run separately best-effort after primary commit per D-11/D-12/D-18.
  </behavior>
  <action>Write failing docs/config assertions first, then revise the Lifecycle moduledoc, unsubscribe guide, and API stability section to match shipped Phase 152 semantics. Preserve the public callback signature and compliance lifecycle configuration key, but replace any claim that adopter work extends the primary unsubscribe transaction with the explicit compatibility sequence: call `handle_event(Ecto.Multi.new(), attrs)` only for created convergence after commit, then execute the returned Multi separately and best-effort. Document `created` versus replay behavior, canonical event/suppression identities, Delivery-derived scope, exact byte-empty responses, and stable empty 500 for true convergence failures. State accurately that RFC 8058 defines POST/no-redirect interoperability while Mailglass owns the empty-200 privacy compatibility contract. Include only bounded payload examples per D-14.</action>
  <verify>
    <automated>mix test test/mailglass/docs/unsubscribe_guide_test.exs test/mailglass/compliance/unsubscribe_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The public callback/config surface remains compatible, its post-commit semantics are unambiguous, and executable guide tests lock every public response and convergence claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add production, stability, and cross-contract evidence</name>
  <files>guides/production-go-live-checklist.md, test/mailglass/docs_contract_test.exs, test/mailglass/stability_contract_test.exs</files>
  <behavior>
    - The production checklist requires a real one-click POST followed by same-stream Outbound preflight blocking and transactional/unrelated-stream negative controls per D-09/D-10.
    - Stability/docs contracts require route, exact response bodies, callback signature/config compatibility, post-commit best-effort ordering, replay created-only effects, and explicit schema-prefix/tenant isolation.
    - Contract evidence does not promise exactly-once behavior in arbitrary host systems or include any Phase 153 generated-host/release proof.
  </behavior>
  <action>Add failing contract assertions before revising the production checklist. Make the operator procedure exercise a real signed bulk-delivery link and actual Outbound boundary, verify same-tenant/address/origin-stream suppression plus transactional/unrelated-stream isolation, and describe expected non-success/rollback behavior without logging the token or message content. Extend docs/stability tests so drift in the existing route, byte-empty response classifications, lifecycle signature/config key, post-commit ordering, created-only notifications, or explicit schema/tenant guarantees fails focused gates. Keep Phase 153 generated-host, release, and broad adopter-journey work excluded.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.docs.contract &amp;&amp; mix verify.stability_contract</automated>
  </verify>
  <done>Production guidance is behaviorally testable and the docs/stability gates detect drift across all five Phase 152 requirements without importing deferred Phase 153 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| implementation → published contract | Documentation and config claims may drift from runtime behavior. |
| operator procedure → logs/support artifacts | Tokens or private message data could leak during verification. |
| compatibility adapter → adopter implementation | Existing callbacks retain their shape but execute under new required ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-152-10 | Repudiation | docs/API stability claims | medium | mitigate | Executable docs and stability tests lock route, response, convergence, lifecycle, and prefix semantics to shipped behavior. |
| T-152-11 | Information Disclosure | operator verification guidance | high | mitigate | Procedures forbid token/private-content logging and use bounded domain facts per D-14. |
| T-152-12 | Denial of Service / Tampering | lifecycle compatibility documentation | medium | mitigate | Preserve callback/config shape while explicitly documenting separate post-commit best-effort execution and failure isolation per D-11/D-12/D-18. |

</threat_model>

<verification>
Run both task commands, `mix docs --warnings-as-errors`, and the complete Phase 152 gate in `152-VALIDATION.md`.
</verification>

<success_criteria>
- Every UNSUB-07..11 public/config/operational claim is documented and enforced by a focused automated contract.
- Lifecycle compatibility is preserved without contradicting commit-before-effects ordering.
- No deferred Phase 153 or new-scope work enters the package.
</success_criteria>

<output>
Create `.planning/phases/152-atomic-one-click-suppression-convergence/152-03-SUMMARY.md` when done.
</output>
