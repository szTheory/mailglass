---
phase: 144-signal-drift-integrity
plan: 05
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - CONTRIBUTING.md
  - test/scripts/release_trigger_recovery_test.exs
autonomous: true
requirements: [TRUTH-04]
must_haves:
  truths:
    - "TRUTH-04 / D-09: the existing hourly release-please recovery remains visible, idempotent, and mutually guarded with push/manual triggers."
    - "The preflight runs only for an incomplete release, no-ops for all-present tags or `autorelease: tagged`, and fails on partial linked-release state."
    - "D-10: maintainers can find the accepted hourly delay, exact symptoms, automatic recovery, and manual fallback in CONTRIBUTING.md without a topology replacement."
  artifacts:
    - path: "test/scripts/release_trigger_recovery_test.exs"
      provides: "Anti-vacuous recovery/preflight/docs contract"
    - path: "CONTRIBUTING.md"
      provides: "Durable maintainer recovery and cadence runbook"
  key_links:
    - from: "test/scripts/release_trigger_recovery_test.exs"
      to: ".github/workflows/release-please.yml"
      via: "bounded schedule, preflight, and should_run source contracts"
      pattern: "release-preflight"
    - from: "CONTRIBUTING.md"
      to: ".github/workflows/release-please.yml"
      via: "documented hourly self-heal and manual workflow_dispatch fallback"
      pattern: "17 \\* \\* \\* \\*"
  prohibitions:
    - "Must not replace release triggers, add a workflow, weaken partial-state failure, or cut a release."
    - "Must not describe recovery as immediate or guarantee an order that the hourly schedule does not provide."
  flagged_assumptions:
    - "TRUTH-04 unclassified probe: hourly recovery is the selected fix; its bounded delay is accepted only with executable preflight evidence and durable documentation."
---

<objective>
Turn the already-selected hourly anti-recursion recovery into a durable, anti-vacuous contract and maintainer-facing operational truth.

Purpose: A bot-merged release PR self-heals without double-firing or masking partial state, and future maintainers understand the bounded delay and recovery options.
Output: Recovery contract tests and corrected CONTRIBUTING.md runbook.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-signal-drift-integrity/144-CONTEXT.md
@.planning/phases/144-signal-drift-integrity/144-RESEARCH.md
@.planning/phases/144-signal-drift-integrity/144-01-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: release-trigger recovery path.
Decision: no-change.
Rationale: The detector's `alongside` signal describes exclusion between existing push, manual, and hourly invocations; repository evidence shows no new identity, key, entity, or generalized model, and the release train remains identified by the existing manifest-derived tag set.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the existing recovery state machine as an anti-vacuous contract</name>
  <files>test/scripts/release_trigger_recovery_test.exs</files>
  <read_first>
    .github/workflows/release-please.yml
    test/scripts/guard_release_trigger_test.exs
    test/scripts/required_checks_test.exs
    .release-please-manifest.json
    .planning/phases/144-signal-drift-integrity/144-CONTEXT.md
  </read_first>
  <behavior>
    - The workflow retains push-to-main, workflow_dispatch, and cron `17 * * * *`; the parser fails if any trigger block is absent.
    - Complete expected tags set `should_run=false`; partial tags exit 1; `autorelease: tagged` sets false; a pending untagged release sets true.
    - The release-please action is guarded by `steps.release-preflight.outputs.should_run == 'true'` so concurrent/repeated triggers converge through one preflight.
    - In-memory removal of each guard fails the same assertions.
  </behavior>
  <action>Create `Mailglass.Scripts.ReleaseTriggerRecoveryTest` with bounded helpers `extract_trigger_block!/2`, `extract_step_block!/2`, and `extract_action_block!/2`. Assert every parsed block is nonempty before semantic checks. Contract-test the existing `release-please.yml` exactly as selected by D-09: hourly cron, manual trigger, push trigger, manifest-derived expected tags, full-present no-op, partial-state hard failure, `autorelease: tagged` no-op, pending untagged run, and the release action's `should_run` guard. Add an in-memory negative control for each branch so matching only comments or an empty block cannot pass. Do not modify the workflow topology or invoke GitHub.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Each trigger, preflight, and action parser asserts a nonempty bounded block.
    - Tests cover full tags, partial tags, tagged label, pending untagged state, and action gating as distinct branches.
    - At least one in-memory negative control is present for every required recovery branch.
    - The focused ExUnit command exits 0 without network access or a release dispatch.
  </acceptance_criteria>
  <done>The selected recovery mechanics are executable contract evidence rather than comments that can drift unnoticed.</done>
</task>

<task type="auto">
  <name>Task 2: Document the bounded hourly self-heal and safe fallbacks where maintainers look</name>
  <files>CONTRIBUTING.md, test/scripts/release_trigger_recovery_test.exs</files>
  <read_first>
    CONTRIBUTING.md
    .github/workflows/release-please.yml
    test/scripts/release_trigger_recovery_test.exs
    .planning/phases/144-signal-drift-integrity/144-CONTEXT.md
  </read_first>
  <action>Rewrite the existing `If a release publishes but the tags/publish never fire` section in place per D-09/D-10. State that bot auto-merge can suppress the push event; the existing cron runs at minute 17 hourly, so recovery is bounded by the next hourly run (up to one hour, with the recorded incidents costing roughly 30 minutes); all-present tags and `autorelease: tagged` are successful no-ops; partial linked-tag state fails and requires reconciliation; `workflow_dispatch` is the direct manual recovery; and manually creating the missing GitHub releases remains the last-resort canonical `release: published` fan-out. Correct the stale claim that release-please runs only on push. Extend the test to require these exact operational facts from the bounded documentation section. Do not introduce a second runbook or promise immediate/FIFO behavior.</action>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The existing CONTRIBUTING.md recovery section names cron minute 17, hourly/up-to-one-hour delay, observed roughly-30-minute cost, workflow_dispatch, full-tag no-op, tagged-label no-op, and partial-state failure.
    - The stale sentence claiming push is the only release-please trigger is absent from the bounded recovery section, enforced structurally by the test rather than a file-wide grep.
    - Documentation and workflow assertions live in the same test module and the focused command exits 0.
  </acceptance_criteria>
  <done>Future maintainers can identify the anti-recursion symptom, wait bound, automatic behavior, and safe manual recovery without reading workflow implementation.</done>
</task>

</tasks>

## Artifacts this phase produces

Across Phase 144, new files are:

- `test/scripts/branch_protection_truth_test.exs` — `Mailglass.Scripts.BranchProtectionTruthTest`, `extract_job_block/2`, and `extract_step_block/2`.
- `test/scripts/icon_exists_gate_test.exs` — `Mailglass.Scripts.IconExistsGateTest`, `fixture_root!/1`, `copy_real_gate!/1`, and `run_gate/1`.
- `test/scripts/linked_release_concurrency_test.exs` — `Mailglass.Scripts.LinkedReleaseConcurrencyTest`, `extract_top_level_concurrency!/1`, and `extract_publish_job!/2`.
- `test/scripts/release_trigger_recovery_test.exs` — `Mailglass.Scripts.ReleaseTriggerRecoveryTest`, `extract_trigger_block!/2`, `extract_step_block!/2`, and `extract_action_block!/2`.
- `.planning/phases/144-signal-drift-integrity/COVERAGE.md` — reasoned no-external-API declaration.

Created non-file symbols/surfaces are test-only `with_env/2` and `write_branch_protection_verifier!/2`, Bash-local `extract_dynamic_icon_references`, scheduled `Verify branch protection` and terminal `Report branch protection outcome` workflow steps, the `classification=clean|drift|cannot_check` step-output contract, and shared `mailglass-linked-release-fanout` group. The phase creates no public function, Mix task, CLI option, workflow, job, desired-state registry, schema, dependency, or release artifact.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Every automated result is trustworthy without log inspection | 01-05 | COVERED | Honest outcomes, dynamic coverage, serialization, and recovery evidence close every named goal clause. |
| REQ | CONFORM-02 | Dynamic icon-existence coverage | 03 | COVERED | Real-gate non-contiguous computed negative fixture. |
| REQ | TRUTH-02 | Cannot-check never reports success | 01 | COVERED | Both GitHub workflows use authoritative terminal failure. |
| REQ | TRUTH-03 | Scheduled comparison and display-name regression | 01 | COVERED | Existing schedule, canonical verifier, anti-vacuous identity contract. |
| REQ | TRUTH-04 | Fix or formally accept anti-recursion gap | 05 | COVERED | Existing hourly fix is contract-tested and documented. |
| REQ | TRUTH-06 | Hygiene blocked versus cannot-check | 02 | COVERED | Three-state per-check and aggregate result. |
| REQ | TRUTH-08 | Linked release fan-out serialization | 04 | COVERED | Both workflows plus three idempotency guards. |
| RESEARCH | — | Closed clean/drift/cannot-check outcome model | 01-02 | COVERED | Workflow tracer and CLI expansion use the same semantics. |
| RESEARCH | — | Bounded dynamic extraction; fail closed if unresolved | 03 | COVERED | Fixture covers every identified admin form. |
| RESEARCH | — | Shared concurrency with no false FIFO claim | 04 | COVERED | Static group plus explicit pending-queue assumption. |
| RESEARCH | — | Recovery full/partial/tagged/pending branches | 05 | COVERED | Anti-vacuous source contract and runbook. |
| RESEARCH | — | No new package, Action, registry, workflow, or topology | 01-05 | COVERED | Every action is constrained to existing seams; COVERAGE.md declares no API integration. |
| CONTEXT | D-01 | Three unavailable-verification surfaces fail distinctly | 01-02 | COVERED | Both workflows and repo hygiene. |
| CONTEXT | D-02 | Branch Protection Advisory remains publish-gating | 01 | COVERED | Registry/needs assertion. |
| CONTEXT | D-03 | Existing schedule and canonical scripts only | 01 | COVERED | Verify-before-reassert; no new workflow/registry. |
| CONTEXT | D-04 | Display name, not YAML id, with anti-vacuity | 01 | COVERED | Parsed job block plus historical-id negative control. |
| CONTEXT | D-05 | Real-gate throwaway dynamic missing icon | 03 | COVERED | Guaranteed cleanup harness. |
| CONTEXT | D-06 | Whole bounded dynamic class; no historical hardcode/dependency | 03 | COVERED | Finite resolver plus fail-closed unresolved case. |
| CONTEXT | D-07 | Both concurrency patterns fixed together | 04 | COVERED | Atomic shared-group edit. |
| CONTEXT | D-08 | Already-published remains successful no-op | 04 | COVERED | Three package guards and message contract. |
| CONTEXT | D-09 | Existing hourly recovery selected and proven | 05 | COVERED | Workflow left topologically intact; evidence and docs added. |
| CONTEXT | D-10 | Accept bounded delay; no trigger replacement | 05 | COVERED | Maintainer docs state exact cadence/cost. |

Deferred ideas: none in Phase 144 CONTEXT.md. SEED-006 optimization, release cutting, topology redesign, product work, and the shipped 2026-07-28 remediation remain excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub trigger/preflight → release creation | Push, manual, and scheduled events converge on manifest/tag/label state before a privileged release action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-11 | Repudiation | Anti-recursion recovery visibility | medium | mitigate | Contract every trigger/state branch and document cadence, symptoms, and recovery in the existing maintainer runbook. |
| T-144-12 | Denial of service | Partial linked-tag state | high | mitigate | Preserve and test the partial-state hard failure rather than allowing a no-op or second release action. |
| T-144-13 | Tampering | Repeated/concurrent recovery triggers | high | mitigate | Preserve manifest-derived tags, tagged/full-state no-ops, and the single `should_run` action guard. |
</threat_model>

<verification>
Run `mix test test/scripts/release_trigger_recovery_test.exs --warnings-as-errors`, then `mix verify.ci_lane_contract`. Both must exit 0 without dispatching or cutting a release.
</verification>

<success_criteria>
The existing recovery topology is mechanically pinned, every state branch is non-vacuous, and CONTRIBUTING.md accurately documents the hourly delay and safe recovery behavior.
</success_criteria>

<output>
Create `.planning/phases/144-signal-drift-integrity/144-05-SUMMARY.md` when done.
</output>
