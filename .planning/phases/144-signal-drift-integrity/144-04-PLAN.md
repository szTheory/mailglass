---
phase: 144-signal-drift-integrity
plan: 04
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
  - test/scripts/linked_release_concurrency_test.exs
autonomous: true
requirements: [TRUTH-08]
must_haves:
  truths:
    - "TRUTH-08 / D-07: publish and post-publish smoke use the same release-independent concurrency intent with cancellation disabled."
    - "D-08: all three package publish paths retain successful already-published no-ops with explicit `nothing to do` semantics."
    - "A contract parser that misses either workflow concurrency block or any package idempotency block fails loudly."
  artifacts:
    - path: "test/scripts/linked_release_concurrency_test.exs"
      provides: "Anti-vacuous shared-concurrency and idempotency contract"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Release-independent publish serialization"
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "Matching release-independent smoke serialization"
  key_links:
    - from: ".github/workflows/publish-hex.yml"
      to: ".github/workflows/post-publish-smoke.yml"
      via: "equal static concurrency group with cancel-in-progress false"
      pattern: "mailglass-linked-release-fanout"
    - from: ".github/workflows/publish-hex.yml"
      to: "Hex package registry"
      via: "existing `mix hex.info` guards before each publish"
      pattern: "Released:"
  prohibitions:
    - "Must not cancel an in-progress release run, interpolate a tag/ref into the group, or leave the smoke workflow on its old independent pattern."
    - "Must not turn an already-published package into a failed release signal or cut a release during verification."
  flagged_assumptions:
    - "TRUTH-08 unclassified probe: GitHub repository-level concurrency serializes equal groups but does not promise FIFO for more than one pending run; the plan claims serialization plus idempotent safety, not queue-order guarantees."
---

<objective>
Serialize both linked-release workflow fan-outs under one ref-independent contract while preserving successful registry idempotency.

Purpose: Two release tags cannot race into false failure, and redundant work reports success rather than rewriting a shipped release as failed.
Output: Atomic workflow changes and a source contract covering both concurrency blocks and all package no-op guards.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-signal-drift-integrity/144-CONTEXT.md
@.planning/phases/144-signal-drift-integrity/144-RESEARCH.md
@.planning/phases/144-signal-drift-integrity/144-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize the complete linked-release fan-out without losing no-op success</name>
  <reversibility rating="costly">The shared concurrency identifier coordinates two production workflows repository-wide, so reversal requires an atomic change to both workflows and their contract.</reversibility>
  <files>.github/workflows/publish-hex.yml, .github/workflows/post-publish-smoke.yml, test/scripts/linked_release_concurrency_test.exs</files>
  <read_first>
    .github/workflows/publish-hex.yml
    .github/workflows/post-publish-smoke.yml
    test/scripts/guard_release_trigger_test.exs
    test/scripts/required_checks_test.exs
    .planning/phases/144-signal-drift-integrity/144-CONTEXT.md
    .planning/phases/144-signal-drift-integrity/144-RESEARCH.md
  </read_first>
  <behavior>
    - Both top-level concurrency blocks parse non-vacuously to the same static `mailglass-linked-release-fanout` group and `cancel-in-progress: false`.
    - Neither group depends on `github.ref`, release tag, nor workflow-dispatch tag input.
    - Core, admin, and inbound each retain a `mix hex.info` released-version guard, `skip=true`, a skipped publish condition, and successful explicit nothing-to-do text.
    - In-memory restoration of either old ref/tag-scoped group fails the contract.
  </behavior>
  <action>Create `Mailglass.Scripts.LinkedReleaseConcurrencyTest` with `extract_top_level_concurrency!/1` and `extract_publish_job!/2`; every extractor must assert its block is found before semantic checks. Start red against the current groups. Then replace both complete top-level concurrency blocks atomically with static group `mailglass-linked-release-fanout` and `cancel-in-progress: false` per D-07. Do not change triggers, jobs, gates, package order, or permissions. Preserve and contract-test each of the three existing `mix hex.info` idempotency steps, their `skip=true` output, and the corresponding publish-step condition; require successful summary/log text containing `nothing to do` for an already-published serialized run per D-08. Add in-memory old-pattern negative controls for both workflows; do not claim FIFO behavior beyond GitHub's documented equal-group serialization.</action>
  <verify>
    <automated>mix test test/scripts/linked_release_concurrency_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The parser finds exactly one top-level concurrency block in each workflow and asserts equal static groups plus `cancel-in-progress: false`.
    - Old publish and smoke tag/ref expressions are rejected by executable negative controls.
    - Exactly three nonempty publish job blocks retain released-version lookup, skip output, conditional publish, and explicit successful `nothing to do` messaging.
    - The focused ExUnit command exits 0.
  </acceptance_criteria>
  <done>Both release-event workflows serialize under one stable group, and every redundant package publish remains an observable success.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `test/scripts/linked_release_concurrency_test.exs`, module `Mailglass.Scripts.LinkedReleaseConcurrencyTest`, and bounded helpers `extract_top_level_concurrency!/1` and `extract_publish_job!/2`. It establishes `mailglass-linked-release-fanout` as the shared workflow group; it creates no new workflow, trigger, job, CLI, dependency, or package-release action.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release events → privileged Hex workflows | Multiple tags can enqueue privileged publishing/smoke work for one release train. |
| Workflow → Hex registry | Registry state determines whether retry is a safe no-op or a publish attempt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-08 | Denial of service | Linked release workflow fan-out | high | mitigate | Use one static repository concurrency group across both workflows with cancellation disabled. |
| T-144-09 | Tampering | Already-published package handling | high | mitigate | Preserve and contract-test three Hex lookups, skip outputs, publish conditions, and success messaging. |
| T-144-10 | Tampering | Tag/ref input in serialization | medium | mitigate | Static group and executable negative controls prevent untrusted event strings from partitioning the lock. |
</threat_model>

<verification>
Run the focused concurrency contract and `mix verify.ci_lane_contract`; both must exit 0. Do not dispatch or publish a release.
</verification>

<success_criteria>
Both workflows share one static non-cancelling serialization contract, old partitioning patterns fail tests, and all package retries remain successful no-ops.
</success_criteria>

<output>
Create `.planning/phases/144-signal-drift-integrity/144-04-SUMMARY.md` when done.
</output>
