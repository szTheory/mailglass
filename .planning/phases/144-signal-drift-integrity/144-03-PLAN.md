---
phase: 144-signal-drift-integrity
plan: 03
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
  - test/scripts/icon_exists_gate_test.exs
autonomous: true
requirements: [CONFORM-02]
must_haves:
  truths:
    - "CONFORM-02 / D-05: a missing icon routed through every supported bounded dynamic form fails the real ICON-EXISTS-GATE and the test fixture is always removed."
    - "D-06: the lasting gate resolves the dynamic-construction class against the existing vendored inventory; it does not encode historical icon names."
    - "The normal admin source tree still passes the same conformance command after negative-control cleanup."
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "Bounded dynamic Heroicon reference extraction within the existing gate"
    - path: "test/scripts/icon_exists_gate_test.exs"
      provides: "Real-gate negative and clean fixture harness"
  key_links:
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/assets/vendor/heroicons-inline.js"
      via: "normalized used-name versus vendored-key set difference"
      pattern: "comm -23"
    - from: "test/scripts/icon_exists_gate_test.exs"
      to: "mailglass_admin/scripts/check-conformance.sh"
      via: "isolated directory-layout execution of the real script"
      pattern: "System\\.cmd\\(\"bash\""
  prohibitions:
    - "Must not replace the existing ICON-EXISTS-GATE, vendored inventory, or shared error accumulator."
    - "Must not hardcode the two historical missing icons or add a dependency/icon registry."
  flagged_assumptions:
    - "CONFORM-02 empty probe: zero scanned icon references is a path/parser failure and remains exit 2, as established by IN-03."
    - "CONFORM-02 encoding probe: icon identity is exact lowercase ASCII inventory-key equality after removing the `hero-` prefix; fixtures pin hyphenated names and concatenated/interpolated segments."
---

<objective>
Prove and extend the existing icon-existence gate across the bounded dynamic forms used by admin components, using the real executable and a guaranteed-cleanup missing-icon fixture.

Purpose: The gate prevents the class of invisible-icon bug it claims to cover, rather than only detecting literal call-site strings.
Output: Dynamic reference extraction and anti-vacuous real-gate integration tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-signal-drift-integrity/144-CONTEXT.md
@.planning/phases/144-signal-drift-integrity/144-RESEARCH.md
@.planning/phases/144-signal-drift-integrity/144-PATTERNS.md
@.planning/phases/144-signal-drift-integrity/144-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make bounded dynamic icon construction fail the real inventory gate</name>
  <files>mailglass_admin/scripts/check-conformance.sh, test/scripts/icon_exists_gate_test.exs</files>
  <read_first>
    mailglass_admin/scripts/check-conformance.sh
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/assets/vendor/heroicons-inline.js
    test/scripts/conformance_advisory_test.exs
    .planning/phases/144-signal-drift-integrity/144-CONTEXT.md
    .planning/phases/144-signal-drift-integrity/144-PATTERNS.md
  </read_first>
  <behavior>
    - A fixture whose `<.icon>` receives a lookup-map or `option.icon` value containing a missing vendored name fails with the normalized missing name in output.
    - Helper-return, module-attribute, and finite literal concatenation/interpolation forms resolve to concrete inventory keys; a missing member fails.
    - A dynamic icon expression that cannot be finitely resolved fails closed with a remediation naming the source file/expression class.
    - A clean fixture and the real admin tree pass; temp fixture directories are absent after both success and failure cases.
  </behavior>
  <action>Create `Mailglass.Scripts.IconExistsGateTest` with `fixture_root!/1`, `copy_real_gate!/1`, and `run_gate/1` test helpers. The harness must build the real script's expected `scripts/`, `lib/`, and `assets/vendor/heroicons-inline.js` layout under a unique temp directory, register `on_exit` cleanup before writing fixtures, copy the real gate and inventory, and execute the copied real script. Start with failing tests for `@icon`, `option.icon`/lookup-map, `stat_severity_icon/1`-style finite helper clauses, and a concatenated or interpolated `hero-*` name whose complete missing name never appears as one literal token; assert the old literal-only behavior cannot satisfy that last case. Then add local Bash function `extract_dynamic_icon_references` inside `ICON-EXISTS-GATE` and extend only the used-icon extraction: retain its temp lifecycle, zero-scan guard, vendored-key extraction, `comm` diff, and shared `errors` accumulator; union literal matches with statically finite values flowing through `<.icon name={...}>` from module attributes, map fields, helper return clauses, and finite literal concatenation/interpolation. Emit a fail-closed diagnostic for a dynamic icon expression outside those bounded forms. Do not encode the historical icon names or add a registry/dependency per D-05/D-06.</action>
  <verify>
    <automated>mix test test/scripts/icon_exists_gate_test.exs --warnings-as-errors &amp;&amp; bash mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <acceptance_criteria>
    - The negative fixture's complete missing icon name is not present as a contiguous literal in its source, yet the real gate reports that complete normalized name and exits nonzero.
    - Each supported dynamic form is exercised, and an unbounded form has a fail-closed assertion.
    - Cleanup is registered before fixture writes and tests assert the fixture root is absent after execution.
    - The focused ExUnit test and normal Bash gate both exit 0 after the negative controls finish.
  </acceptance_criteria>
  <done>The existing gate has executable proof for the dynamic construction class, catches a deliberately missing computed name, and leaves no fixture behind.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `test/scripts/icon_exists_gate_test.exs`, module `Mailglass.Scripts.IconExistsGateTest`, and test helpers `fixture_root!/1`, `copy_real_gate!/1`, and `run_gate/1`. The existing `ICON-EXISTS-GATE` gains local Bash function `extract_dynamic_icon_references` and an unresolved-expression diagnostic; no public helper, CLI, registry, workflow, or dependency is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin source expressions → vendored icon inventory | Source-derived names must be completely and non-vacuously enumerated before comparison to shipped assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-06 | Tampering | Dynamic icon reference extraction | medium | mitigate | Exercise each bounded form plus a non-contiguous computed missing name against the real gate; fail closed on unresolved forms. |
| T-144-07 | Repudiation | Temporary negative fixture | low | mitigate | Register cleanup before writes and assert both failure evidence and post-run fixture absence. |
</threat_model>

<verification>
Run the focused fixture test, the normal conformance gate, and `mix verify.ci_lane_contract`; all must exit 0 after the deliberate negative subprocesses are asserted.
</verification>

<success_criteria>
A genuinely computed missing icon fails the real gate with its resolved name, bounded existing forms are covered, unbounded forms fail closed, and clean repository execution remains green.
</success_criteria>

<output>
Create `.planning/phases/144-signal-drift-integrity/144-03-SUMMARY.md` when done.
</output>
