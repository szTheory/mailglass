---
phase: 144-signal-drift-integrity
plan: 02
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - dev/mix/tasks/mailglass.repo.hygiene.ex
  - test/mix/tasks/mailglass.repo.hygiene_test.exs
autonomous: true
requirements: [TRUTH-06]
must_haves:
  truths:
    - "TRUTH-06 / D-01: repo hygiene reports branch-protection drift as blocked and unavailable verification as a distinct cannot-check status; both make aggregate status non-success."
    - "A verifier 403/tool absence/missing token cannot be worded as branch-protection drift or aggregate to pass."
  artifacts:
    - path: "dev/mix/tasks/mailglass.repo.hygiene.ex"
      provides: "Three-state branch-protection sub-check and honest aggregate status"
    - path: "test/mix/tasks/mailglass.repo.hygiene_test.exs"
      provides: "Executable outcome-classification regressions"
  key_links:
    - from: "dev/mix/tasks/mailglass.repo.hygiene.ex"
      to: "scripts/verify-branch-protection.sh"
      via: "branch_protection/1 command result classification"
      pattern: "verify-branch-protection\\.sh"
  prohibitions:
    - "Must not report an inaccessible GitHub endpoint or missing local prerequisite as verified branch-protection drift."
    - "Must not allow any cannot-check result to collapse the aggregate hygiene status to pass."
  flagged_assumptions:
    - "TRUTH-06 unclassified probe: `:unknown` is retained as the established per-check unavailable status, while aggregate status becomes non-success; tests pin text and JSON emitters to that distinction."
---

<objective>
Make the maintainer CLI tell the same truth as the workflow tracer: unavailable verification is distinct from verified drift and never aggregates to success.

Purpose: `mailglass.repo.hygiene` becomes trustworthy when GitHub credentials, tooling, or endpoint access are unavailable.
Output: Tested branch-protection outcome classification and aggregate-status semantics.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-signal-drift-integrity/144-CONTEXT.md
@.planning/phases/144-signal-drift-integrity/144-PATTERNS.md
@.planning/phases/144-signal-drift-integrity/144-01-SUMMARY.md
</context>

<assumption_delta_decision>
Reference: AD-144-01 in 144-RESEARCH.md.
Primary noun: existing signal-integrity contract.
Decision: no-change — retaining `:unknown` and changing its aggregate semantics refines an existing private Mix-task result; it creates no new model, entity, identity/key scheme, public API, or external API integration.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock repo-hygiene branch-protection classification with executable cases</name>
  <files>test/mix/tasks/mailglass.repo.hygiene_test.exs, dev/mix/tasks/mailglass.repo.hygiene.ex</files>
  <read_first>
    test/mix/tasks/mailglass.repo.hygiene_test.exs
    dev/mix/tasks/mailglass.repo.hygiene.ex
    scripts/verify-branch-protection.sh
    .planning/phases/144-signal-drift-integrity/144-CONTEXT.md
  </read_first>
  <behavior>
    - Missing verifier, missing `gh`, missing `GH_TOKEN`, and verifier/API failure yield a cannot-check per-check status with prerequisite-specific remediation.
    - Canonical verifier output beginning `DRIFT:` yields `:blocked` with drift remediation.
    - Canonical verifier exit 0 yields `:pass`; any blocked or cannot-check sub-check makes the aggregate non-success.
    - Text and JSON output preserve distinct status vocabulary.
  </behavior>
  <action>Add test helpers `with_env/2` and `write_branch_protection_verifier!/2` to the existing temp-repo harness so environment and verifier command results can be deterministically controlled without contacting GitHub; use a test-local PATH fixture for executable presence. Write failing cases for missing verifier, missing `gh`, missing token, inaccessible endpoint/non-drift nonzero output, explicit `DRIFT:` output, and clean output. Then update `branch_protection/1` and `status/1` in `Mix.Tasks.Mailglass.Repo.Hygiene`: retain the existing result map shape and `:unknown` per-check vocabulary, classify `DRIFT:` as `:blocked`, classify other unavailable/nonzero cases as `:unknown` with concrete recovery, and make aggregate status non-success whenever any check is `:unknown`. Ensure `--check` exit handling and JSON/text emitters expose the distinction per D-01; do not add a new public CLI surface.</action>
  <verify>
    <automated>mix test test/mix/tasks/mailglass.repo.hygiene_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Tests independently exercise clean, drift, missing prerequisite, and inaccessible-endpoint results without live network access.
    - `DRIFT:` is the only nonzero verifier output classified as verified drift; other nonzero output is cannot-check.
    - Aggregate status is non-success for either `:blocked` or `:unknown`, and JSON/text keep those labels distinct.
    - The focused ExUnit command exits 0.
  </acceptance_criteria>
  <done>Repo hygiene can no longer call a 403/tool/token failure drift or return an aggregate pass when branch protection was not checked.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new file or CLI. It extends the existing private `branch_protection/1` and `status/1` semantics and adds test-only helpers `with_env/2` and `write_branch_protection_verifier!/2` plus a PATH fixture in `test/mix/tasks/mailglass.repo.hygiene_test.exs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Mix task → `gh`/GitHub API | Local prerequisites and remote authorization determine whether protection truth can be observed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-04 | Tampering | `branch_protection/1` result classification | high | mitigate | Distinguish canonical drift output from all cannot-check failures and make both aggregate non-success. |
| T-144-05 | Repudiation | JSON/text hygiene reporting | medium | mitigate | Preserve explicit per-check status and remediation in both emitters under tests. |
</threat_model>

<verification>
Run the focused Mix-task test, then `mix verify.mix_tasks`; both commands must exit 0.
</verification>

<success_criteria>
Clean, drift, and cannot-check are distinct in data and output; only clean can contribute to an aggregate pass.
</success_criteria>

<output>
Create `.planning/phases/144-signal-drift-integrity/144-02-SUMMARY.md` when done.
</output>
