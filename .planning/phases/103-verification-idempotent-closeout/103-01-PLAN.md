---
phase: 103-verification-idempotent-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/RATCHET-GAP-REGISTER.md
autonomous: true
requirements: [RATCHET-02]
must_haves:
  truths:
    - "Every open sev-4/5 GAP row (GAP-06) is closed; the register has zero open sev-4/5 rows"
    - "All six carried-forward rows (GAP-01/04/06/07/08/09) are flipped open→fixed against verified-present live code"
    - "Each flipped row preserves its original first_seen_run and stamps run_id 2026-06-16-phase-103"
    - "The register is idempotent-clean: confirmed-fixed rows would skip on re-run"
  artifacts:
    - path: ".planning/RATCHET-GAP-REGISTER.md"
      provides: "Reconciled GAP register with six rows flipped to fixed and proving citations"
      contains: "2026-06-16-phase-103"
  key_links:
    - from: ".planning/RATCHET-GAP-REGISTER.md GAP-06"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex:409"
      via: "proving citation in status cell"
      pattern: "md:grid-cols-\\[40%_60%\\]"
---

<objective>
Reconcile the carried-forward GAP register (RATCHET-02): flip the six currently-open rows
(GAP-01, GAP-04, GAP-06, GAP-07, GAP-08, GAP-09) from `open` to `fixed`, each backed by a
proving citation against verified-present live code. No new fix code — the fixes already
landed in Phases 98–100. This is the FIRST step of the D-15 locked closeout ordering: the
register must be honest before the ratchet is activated or the audit regenerated, or both
produce a stale/vacuous result.

Purpose: Leave the single citation anchor for the v1.11 uplift in a clean, idempotent,
only-forward state with zero open sev-4/5 rows (the ROADMAP Phase 103 success criterion 2).
Output: An updated `.planning/RATCHET-GAP-REGISTER.md` with six flipped rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md
@.planning/RATCHET-GAP-REGISTER.md

<interfaces>
<!-- Register row schema (from RATCHET-GAP-REGISTER.md Column Schema). Executor uses this
     directly — no exploration needed. Existing closed rows GAP-02/03/05 already use the
     "Fixed in Phase NN: ... <component:line> <token>" status-cell idiom; copy it. -->

Columns (pipe table): GAP-NN | surface | component:line | pillar | sev | evidence PNG | fix sketch | status | run_id | first_seen_run

Honesty rules (D-09/D-10):
- status: fixed REQUIRES the finding be ABSENT in live code (not in the stale audit doc).
- first_seen_run NEVER changes (the permanent introduction anchor).
- run_id records the last touch → stamp 2026-06-16-phase-103 on every flip.
- No downgrades this phase — every open row is genuinely fixed.

DISC-1: GAP-06/07 register lines (397/404) are STALE; live fixes are at 409/419. The flip
citation MUST reference the verified current lines (409/419), per CONTEXT D-08.
</interfaces>

# Verified-present live code (proving citations — confirmed this session)
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
@mailglass_admin/lib/mailglass_admin/operator/support_cards.ex
@mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
@mailglass_admin/test/support/operator_fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm all six GAP fixes ABSENT-of-finding in live code</name>
  <files>(read-only verification — no files modified in this task)</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (lines 409, 419)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (lines 12, 18, 39)
    - mailglass_admin/lib/mailglass_admin/operator/support_cards.ex (lines 56, 102, 152)
    - mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex (lines 20, 33, 46, 66, 82)
    - mailglass_admin/test/support/operator_fixtures.ex (lines 128, 136)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-07/D-08 per-row close calls)
  </read_first>
  <action>
    Per D-10 (trigger vs evidence), flip ONLY against live code — never the stale audit. For
    each of the six rows, run a targeted grep proving the OLD finding is absent and the NEW
    token present:
    - GAP-06: confirm `md:grid-cols-[40%_60%]` and `min-[1440px]:!grid-cols-[33%_67%]` present
      at operator_live.ex:409, and `grep minmax( mailglass_admin/lib/mailglass_admin/operator_live.ex`
      returns no master-detail minmax.
    - GAP-07: confirm `text-label uppercase font-bold text-secondary` at operator_live.ex:419, and
      `grep -c 'tracking-\[' mailglass_admin/lib/mailglass_admin/operator_live.ex` returns 0.
    - GAP-08: confirm `attr :filters_active?` at deliveries_list.ex:12, testid split
      `operator-empty-filtered` / `operator-empty-truly` at :18, and `operator-empty-reset` at :39.
    - GAP-09: confirm `status: :suppressed` and `hours_ago(6)` in operator_fixtures.ex (L128/L136).
    - GAP-01: confirm `min-h-11` at support_cards.ex 56/102/152, and `grep -c 'btn-sm'
      mailglass_admin/lib/mailglass_admin/operator/support_cards.ex` returns 0.
    - GAP-04: confirm `text-label uppercase font-bold text-secondary` at filters_form.ex
      20/33/46/66/82.
    Record each proof inline as you go. Do NOT modify any code — fixes already landed in
    Phases 98–100 (D-07). If ANY finding is still present, STOP and surface it (a present
    finding may not be flipped — D-09 rubber-stamp ban); do not flip that row.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q 'md:grid-cols-\[40%_60%\]' mailglass_admin/lib/mailglass_admin/operator_live.ex && [ "$(grep -c 'btn-sm' mailglass_admin/lib/mailglass_admin/operator/support_cards.ex)" = "0" ] && [ "$(grep -c 'tracking-\[' mailglass_admin/lib/mailglass_admin/operator_live.ex)" = "0" ] && grep -q 'operator-empty-reset' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex && echo ALL_FIXES_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `grep md:grid-cols-[40%_60%]` in operator_live.ex returns a match (GAP-06).
    - `grep -c btn-sm` in support_cards.ex returns 0 (GAP-01).
    - `grep -c tracking-[` in operator_live.ex returns 0 (GAP-07).
    - `grep operator-empty-reset` in deliveries_list.ex returns a match (GAP-08).
    - `grep status: :suppressed` in operator_fixtures.ex returns a match (GAP-09).
    - `grep text-label uppercase font-bold text-secondary` in inbound/filters_form.ex returns matches (GAP-04).
    - No code file is modified by this task.
  </acceptance_criteria>
  <done>All six findings confirmed ABSENT (old) / PRESENT-fixed (new) in live code; each proof recorded; no code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the six rows open→fixed with proving citations, preserving first_seen_run</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    - .planning/RATCHET-GAP-REGISTER.md (the Gaps table at lines 132-143; existing closed rows
      GAP-02/03/05 for the status-cell idiom and run_id/first_seen_run column convention)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-08 per-row citations)
  </read_first>
  <action>
    Edit the Gaps table in .planning/RATCHET-GAP-REGISTER.md. For each of the six open rows,
    in place (never renumber, never delete):
    1. Set `status` to `fixed`.
    2. Set `run_id` to `2026-06-16-phase-103` (the last-touch field).
    3. Leave `first_seen_run` UNCHANGED (GAP-01/04 → 2026-06-13-phase-95-baseline;
       GAP-06/07/08/09 → 2026-06-14-phase-98).
    4. Update the `component:line` to the verified CURRENT line for GAP-06 (→ operator_live.ex:409)
       and GAP-07 (→ operator_live.ex:419), per DISC-1 (the 397/404 register lines are stale).
    5. Rewrite each `fix sketch` cell to the closed-row idiom matching GAP-02/03/05:
       "Fixed in Phase NN: <what changed> (<component:line>, <token/grep proof>)." Use the
       phase each fix actually landed in: GAP-06/07/08 → Phase 98; GAP-09 → Phase 98;
       GAP-01 → Phase 98/100 (use the verified token); GAP-04 → the inbound surface phase
       (Phase 99). Cite the verified token from Task 1 (e.g. GAP-06 → "minmax(22rem,28rem)
       removed, now md:grid-cols-[40%_60%] min-[1440px]:!grid-cols-[33%_67%]").
    Do NOT use `status: downgraded` anywhere (D-09). Do NOT touch the v1.7 frozen register
    `.planning/milestones/v1.7-phases/.../74-GAP-REGISTER.md`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && [ "$(grep -cE '^\| GAP-0[14689] .*\| open \|' .planning/RATCHET-GAP-REGISTER.md)" = "0" ] && [ "$(grep -cE '^\| GAP-07 .*\| open \|' .planning/RATCHET-GAP-REGISTER.md)" = "0" ] && [ "$(grep -c '2026-06-16-phase-103' .planning/RATCHET-GAP-REGISTER.md)" -ge 6 ] && grep -q 'operator_live.ex:409' .planning/RATCHET-GAP-REGISTER.md && grep -q 'operator_live.ex:419' .planning/RATCHET-GAP-REGISTER.md && echo REGISTER_RECONCILED</automated>
  </verify>
  <acceptance_criteria>
    - Zero rows in the Gaps table have `| open |` status (all of GAP-01/04/06/07/08/09 now `| fixed |`).
    - `grep -c '2026-06-16-phase-103'` returns ≥ 6 (one per flipped row).
    - GAP-06 row contains `operator_live.ex:409`; GAP-07 row contains `operator_live.ex:419` (DISC-1 corrected lines).
    - GAP-01 first_seen_run still reads `2026-06-13-phase-95-baseline`; GAP-06 first_seen_run still reads `2026-06-14-phase-98` (preserved).
    - `grep -c 'downgraded' .planning/RATCHET-GAP-REGISTER.md` is unchanged from baseline (no new downgrades introduced).
    - The frozen v1.7 register file is not in the diff.
  </acceptance_criteria>
  <done>All six rows show `fixed` + run_id 2026-06-16-phase-103 + preserved first_seen_run + a live-code proving citation; zero open sev-4/5 rows remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale audit doc → register flip | The stale `v1.11-MILESTONE-AUDIT.md` is an untrusted trigger, not evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Repudiation/Tampering | GAP register flip (rubber-stamp) | mitigate | D-09/D-10: flip ONLY against live `component:line` + verified grep (Task 1 gates Task 2); a present finding may not be flipped. |
| T-103-02 | Tampering | first_seen_run overwrite | mitigate | first_seen_run is the permanent introduction anchor — preserved unchanged; only run_id (last-touch) is stamped. |

No HIGH-severity unmitigated threats. This plan ships no product code, no routes, no auth, no crypto.
</threat_model>

<verification>
- `grep -cE '\| open \|' .planning/RATCHET-GAP-REGISTER.md` for GAP-01/04/06/07/08/09 returns 0.
- Each flipped row carries a live-code proving citation (token/grep visible in the status cell).
- `git diff --stat` shows only `.planning/RATCHET-GAP-REGISTER.md` modified.
</verification>

<success_criteria>
ROADMAP Phase 103 success criterion 2 partially met: every sev-4/5 GAP row (GAP-06) closed
and the register left idempotent-clean with zero open rows among the six carried-forward.
</success_criteria>

<output>
Create `.planning/phases/103-verification-idempotent-closeout/103-01-SUMMARY.md` when done.
</output>
