---
phase: 103-verification-idempotent-closeout
plan: 02
type: execute
wave: 2
depends_on: [103-01]
files_modified:
  - mailglass_admin/docs/ui-baseline-scores.json
  - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
  - .planning/RATCHET-GAP-REGISTER.md
autonomous: true
requirements: [RATCHET-01]
must_haves:
  truths:
    - "ui-baseline-scores.json is schema_version 2 with top-level prior and current blocks, each carrying its own run_id and surfaces map"
    - "The prior block is the Phase 95 flat scores migrated unchanged (run_id 2026-06-13-phase-95-baseline)"
    - "The current block is a genuine fresh re-score (run_id 2026-06-16-phase-103) where every cell meets-or-beats prior"
    - "The only-forward ratchet is live: compare_baselines(b[prior], b[current]) is called, not guarded by if false"
    - "An anti-vacuity guard asserts prior.run_id != current.run_id"
    - "The three 36-cell coverage tests read surfaces from a block, not the top level"
  artifacts:
    - path: "mailglass_admin/docs/ui-baseline-scores.json"
      provides: "schema-2 {prior, current} score baseline"
      contains: "\"schema_version\": 2"
    - path: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      provides: "Armed only-forward ratchet + anti-vacuity guard + block-scoped coverage tests"
      contains: "compare_baselines(b[\"prior\"], b[\"current\"])"
  key_links:
    - from: "ratchet_baseline_test.exs"
      to: "ui-baseline-scores.json prior/current blocks"
      via: "compare_baselines(b[\"prior\"], b[\"current\"])"
      pattern: "compare_baselines\\(b\\[\"prior\"\\], b\\[\"current\"\\]\\)"
---

<objective>
Activate the only-forward score ratchet (RATCHET-01): restructure
`mailglass_admin/docs/ui-baseline-scores.json` to schema_version 2 with `{prior, current}`
blocks, populate `current` with a GENUINE fresh 36-cell re-score from a new 18-cell PNG run,
arm `compare_baselines/2` at the live call site, add the anti-vacuity run_id guard, and fix
the three coverage tests that dereference the now-relocated `surfaces` key (Pitfall 1). This
is the SECOND step of the D-15 locked ordering (after GAP reconcile, before all-gates verify).

Purpose: Make the meet-or-beat ratchet merge-blocking in CI and commit the new floor the next
run must beat (ROADMAP Phase 103 success criterion 1). The fresh re-score is user-LOCKED as
genuine (D-02) — preview Motion+A11y at 2/2 should RISE after Phases 100/101/102, not be
re-affirmed.
Output: schema-2 JSON, armed ratchet test, register Seed Run Procedure promotion-step doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md
@.planning/phases/103-verification-idempotent-closeout/103-RESEARCH.md
@.planning/RATCHET-GAP-REGISTER.md

<interfaces>
<!-- Current state of the two files being edited. Executor uses these directly. -->

ui-baseline-scores.json (schema_version 1, flat top-level "surfaces"):
  Top-level keys: run_id, schema_version, pillar_rubric, grade_scale, surfaces.
  surfaces = {deliveries|inbound|preview}.{Spacing|Radius|Color|Type|Elevation|Motion+A11y}.{light|dark} = 1..4.
  Phase 95 prior values: preview Motion+A11y = 2/2 (the lowest cells); deliveries/inbound
  Spacing+Type = 3; everything else 3 or 4.

ratchet_baseline_test.exs anchors (VERIFIED this session):
  - L40: `if false, do: compare_baselines(%{}, %{})` — the inert hook to replace (D-03).
  - L46: `assert b["schema_version"] == 1` — bump to `== 2` (D-01).
  - L51-63: coverage test reads `get_in(b, ["surfaces", surface, pillar, theme])` — BREAKS
    after restructure (Pitfall 1); must read from a block.
  - L65-81: range test, same `get_in(b, ["surfaces", ...])` — same fix.
  - L87-104: `defp compare_baselines(prior, current)` — reads `["surfaces", ...]` off EACH
    operand. DO NOT rewrite (Phase 95 D-05). Feeding it b["prior"] / b["current"] works as-is.

Target schema-2 shape (Claude's discretion on key ordering / per-block vs top-level rubric):
  { "schema_version": 2,
    "prior":   { "run_id": "2026-06-13-phase-95-baseline", "surfaces": {…migrated unchanged…} },
    "current": { "run_id": "2026-06-16-phase-103",         "surfaces": {…fresh re-score…} },
    "pillar_rubric": "...", "grade_scale": "..." }
</interfaces>

@mailglass_admin/docs/ui-baseline-scores.json
@mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
@mailglass_admin/scripts/ui-audit.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh 18-cell PNG re-run and structured 36-cell re-score</name>
  <files>(produces tmp/ui-audit/*.png — GITIGNORED, never committed; scores captured for Task 2)</files>
  <read_first>
    - mailglass_admin/scripts/ui-audit.sh (the 18-cell capture matrix, reuse as-is)
    - .planning/RATCHET-GAP-REGISTER.md (Seed Run Procedure Steps 1-3; Six Conformance Pillars rubric at lines 102-111)
    - mailglass_admin/docs/ui-baseline-scores.json (the Phase 95 prior values to meet-or-beat)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-02 fresh-re-score lock; Discretion clause on re-score method)
  </read_first>
  <action>
    Follow the register Seed Run Procedure. Boot the demo app
    (`cd reference/demo_app && mix ecto.create && mix ecto.migrate && mix run priv/repo/seeds.exs &&
    mix phx.server` → :4015), then in a separate shell capture the 18 PNGs:
    `cd mailglass_admin && bash scripts/ui-audit.sh` → tmp/ui-audit/{surface}-{vp}-{theme}.png.
    Then GENUINELY re-score all 36 cells (3 surfaces × 6 pillars × 2 themes) against the
    six-pillar 1–4 rubric, using the structured-review method from Seed Run Procedure Step 3
    (multimodal subagent reading the 18 PNGs, or structured manual review). This is a REAL
    re-score (D-02), NOT a copy of the Phase 95 numbers: preview Motion+A11y (prior 2/2) is
    expected to RISE after Phases 100/101/102.
    Anti-vacuity check (A1): every re-scored cell MUST meet-or-beat its Phase 95 prior. If any
    cell would DROP below prior, STOP and surface it as a genuine regression finding (do NOT
    fudge the number — the armed ratchet is doing its job; reopen the relevant GAP row instead).
    NEVER commit PNGs; NEVER write under priv/static/ (would trip the bundle gate).
    Record the 36 fresh scores for Task 2.
    NOTE: if `agent-browser` (>=0.27) is unavailable on this machine, the PNG capture cannot
    run; surface that as a blocker for this task only (PNG capture is local/ad-hoc, never
    CI-gated per D-07) — do not fabricate scores.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && ls tmp/ui-audit/*.png 2>/dev/null | wc -l | grep -qE '1[0-8]' && echo PNGS_CAPTURED</automated>
  </verify>
  <acceptance_criteria>
    - `tmp/ui-audit/` contains the 18 captured PNGs (surface × viewport × theme).
    - 36 fresh scores recorded, each an integer in 1..4.
    - Every fresh cell ≥ its Phase 95 prior value (or a genuine regression is surfaced, not fudged).
    - No PNG is staged for commit; no file under priv/static/ is modified.
  </acceptance_criteria>
  <done>18 fresh PNGs captured (gitignored) and all 36 cells re-scored genuinely against the prior floor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restructure JSON to schema-2 {prior, current} and arm the ratchet test</name>
  <files>mailglass_admin/docs/ui-baseline-scores.json, mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs</files>
  <behavior>
    - Test "schema_version is present and supported" asserts `b["schema_version"] == 2` and passes.
    - The two coverage tests (all-36-present, range-1-4) read surfaces from a block (iterate
      ["prior","current"]) and pass against the schema-2 file.
    - A new test asserts `b["prior"]["run_id"] != b["current"]["run_id"]` (anti-vacuity, D-04) and passes.
    - A new/repurposed test calls `compare_baselines(b["prior"], b["current"])` and passes
      (every current cell meets-or-beats prior).
    - Deliberate-failure check: if current.run_id were copied from prior, the anti-vacuity test fails loudly.
  </behavior>
  <read_first>
    - mailglass_admin/docs/ui-baseline-scores.json (full current flat file to migrate)
    - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs (lines 40, 46, 51-81, 87-104)
    - .planning/phases/103-verification-idempotent-closeout/103-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 1, the Code Examples call-site snippet)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-01/D-03/D-04 and the Discretion clause on guard placement)
  </read_first>
  <action>
    Edit ui-baseline-scores.json: bump `schema_version` to 2. Create a top-level `prior` block
    = `{ "run_id": "2026-06-13-phase-95-baseline", "surfaces": {…the existing flat surfaces map,
    migrated UNCHANGED…} }` (D-02). Create a top-level `current` block =
    `{ "run_id": "2026-06-16-phase-103", "surfaces": {…the 36 fresh scores from Task 1…} }`.
    Keep `pillar_rubric` and `grade_scale` (top-level or per-block — discretion). Remove the
    old top-level flat `run_id` and `surfaces` keys.
    Edit ratchet_baseline_test.exs — three required edits + the Pitfall-1 fix, all pure ADD,
    DO NOT rewrite compare_baselines/2 (87-104):
    1. L46: change `== 1` to `== 2`.
    2. L40: replace `if false, do: compare_baselines(%{}, %{})` with a real test (discretion:
       own test or in setup_all). Recommended own test `"only-forward ratchet: no cell regresses
       prior committed baseline", %{baseline: b}` that first asserts D-04 then calls
       `compare_baselines(b["prior"], b["current"])`.
    3. Add the D-04 anti-vacuity assertion `assert b["prior"]["run_id"] != b["current"]["run_id"]`
       with a descriptive message.
    4. Pitfall 1: fix the two coverage tests (L51-63 all-present, L65-81 range) so their
       comprehensions read `get_in(b, [block, "surfaces", surface, pillar, theme])` iterating
       `block <- ["prior", "current"]` (validates both migrated prior AND fresh current). Update
       the "Missing cells" expected count math accordingly (now up to 72 cells across both blocks).
    Run the test after editing; it must be green.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors 2>&1 | tail -5 && grep -q '"schema_version": 2' docs/ui-baseline-scores.json && grep -q 'compare_baselines(b\["prior"\], b\["current"\])' test/mailglass_admin/ratchet_baseline_test.exs && python3 -c "import json,sys; d=json.load(open('docs/ui-baseline-scores.json')); assert d['schema_version']==2; assert d['prior']['run_id']=='2026-06-13-phase-95-baseline'; assert d['current']['run_id']=='2026-06-16-phase-103'; assert d['prior']['run_id']!=d['current']['run_id']; print('JSON_SCHEMA2_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors` exits 0.
    - ui-baseline-scores.json has top-level `prior` and `current` keys, each with its own `run_id`; `schema_version` is 2; no top-level flat `surfaces` key remains.
    - prior.run_id == `2026-06-13-phase-95-baseline` and prior surfaces equal the Phase 95 values unchanged.
    - current.run_id == `2026-06-16-phase-103`.
    - ratchet_baseline_test.exs contains `compare_baselines(b["prior"], b["current"])` and `b["prior"]["run_id"] != b["current"]["run_id"]`.
    - compare_baselines/2 function body (the `defp` at ~L87) is byte-for-byte unchanged.
    - The coverage tests dereference surfaces via a block (no bare top-level `["surfaces"]` read remains in the test).
  </acceptance_criteria>
  <done>schema-2 JSON committed, ratchet armed with anti-vacuity guard, coverage tests block-scoped, full ratchet test green.</done>
</task>

<task type="auto">
  <name>Task 3: Document the promotion step in the register Seed Run Procedure</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    - .planning/RATCHET-GAP-REGISTER.md (Seed Run Procedure section, Steps 1-5 at lines 51-83)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-06 promotion step)
  </read_first>
  <action>
    Append a "Promotion step (D-06)" subsection to the Seed Run Procedure so the next
    milestone's re-run advances the floor cleanly: document that on each future re-run the
    maintainer copies the previous `current` block into `prior` (verbatim), then writes the
    fresh measured run into `current` with a new `run_id` (format YYYY-MM-DD-phase-NN). State
    explicitly that prior.run_id MUST differ from current.run_id (the anti-vacuity guard enforces
    this) and that PNGs remain gitignored / only the JSON is committed. This is a docs-only edit.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -qi 'promotion' .planning/RATCHET-GAP-REGISTER.md && grep -q 'prior' .planning/RATCHET-GAP-REGISTER.md && echo PROMOTION_DOC_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - The Seed Run Procedure contains a promotion-step subsection describing copy current→prior then write fresh current.
    - It references the anti-vacuity run_id-must-differ rule.
    - Only `.planning/RATCHET-GAP-REGISTER.md` is modified by this task.
  </acceptance_criteria>
  <done>Promotion step documented; the apparatus is self-describing for the next milestone's re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON baseline → CI ratchet | The committed `prior`/`current` blocks are the sole source of truth for the merge-blocking gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03 | Tampering/Repudiation | vacuous quality gate (current copied verbatim into prior) | mitigate | D-04 anti-vacuity guard `b["prior"]["run_id"] != b["current"]["run_id"]` (PRIMARY in-scope control); fails loudly on a forgotten promotion (Task 2). |
| T-103-04 | Tampering | bundle drift via priv/static/ write | mitigate | Task 1 explicitly forbids writing under priv/static/; PNGs gitignored; bundle-clean gate confirmed in Plan 03. |

No HIGH-severity unmitigated threats. No product code, no routes, no auth, no crypto.
</threat_model>

<verification>
- `mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors` exits 0.
- The JSON is schema-2 with two genuinely-different run_ids; every current cell ≥ prior.
- `git diff --stat` shows ui-baseline-scores.json + ratchet_baseline_test.exs + RATCHET-GAP-REGISTER.md only; no PNGs, no priv/static/.
</verification>

<success_criteria>
ROADMAP Phase 103 success criterion 1 met: every cell meets-or-beats its prior committed
baseline (only-forward) and the committed baseline is updated to the new floor; the ratchet is
now merge-blocking with the anti-vacuity guard active.
</success_criteria>

<output>
Create `.planning/phases/103-verification-idempotent-closeout/103-02-SUMMARY.md` when done.
</output>
