---
phase: 103-verification-idempotent-closeout
plan: 03
type: execute
wave: 3
depends_on: [103-02]
files_modified:
  - .planning/phases/103-verification-idempotent-closeout/103-03-SUMMARY.md
autonomous: true
requirements: [RATCHET-01, RATCHET-02, CLOSE]
must_haves:
  truths:
    - "All CI gates are green: token-parity, conformance, conformance-advisory, motion grep, Playwright structural, the now-armed LLM-score floor, and bundle-clean"
    - "The prepare-only readiness note records the verified release posture: manifest 1.6.2/1.6.2/1.3.1 untouched, linked group = core+admin, inbound independent, exclude-paths/RELH-01 intact"
    - "The deferred inbound exact-pin re-pin is recorded as the single PENDING ceremony action — not performed"
    - "No version number, CHANGELOG, manifest, or inbound pin is edited"
  artifacts:
    - path: ".planning/phases/103-verification-idempotent-closeout/103-03-SUMMARY.md"
      provides: "All-gates verification result + prepare-only release readiness checklist"
      contains: "PENDING"
  key_links:
    - from: "103-03-SUMMARY.md readiness checklist"
      to: ".release-please-manifest.json (1.6.2/1.6.2/1.3.1)"
      via: "read-only verification, no edit"
      pattern: "1.6.2"
---

<objective>
Run the full all-gates verification battery green and write the prepare-only release readiness
note (CLOSE). This is the THIRD step of the D-15 locked ordering (after GAP reconcile and
ratchet activation, before the milestone audit). The readiness note changes NOTHING Release
Please owns — it records a verification checklist and the single deferred ceremony action.

Purpose: Confirm ROADMAP Phase 103 success criterion 3 (all gates green in CI) and stage the
linked-version release ceremony prepare-only (criterion 4, first half).
Output: A readiness note capturing gate results + the read-only release-posture verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md
@.planning/phases/103-verification-idempotent-closeout/103-RESEARCH.md
@.planning/phases/103-verification-idempotent-closeout/103-VALIDATION.md

<interfaces>
<!-- Gate commands (from RESEARCH Phase Requirements → Test Map). Run each; all must be green. -->

token-parity + ratchet + admin support contract:
  cd mailglass_admin && mix verify.support_contract.admin
full admin test + assets build + bundle-clean:
  cd mailglass_admin && mix verify.preview   # includes `git diff --exit-code priv/static/`
conformance (5 gates):    bash mailglass_admin/scripts/check-conformance.sh
conformance advisory:     bash mailglass_admin/scripts/check-conformance-advisory.sh
motion gate (frozen):     bash scripts/check_motion_conformance.sh
structural (Playwright):  cd mailglass_admin && npm run test:operator-browser   # pins --workers=1

Known flakes to EXCLUDE from pass/fail (project memory, not regressions):
  - voice_test "Oops" substring matches inlined phoenix.mjs dep JS (Pitfall 6).
  - Playwright requires --workers=1 (already pinned in test:operator-browser) — Pitfall 3.
  - 3 intentional structural skips (A3) — eyeball them; confirm none masks a regression.

Release posture (READ-ONLY — verify, never edit):
  .release-please-manifest.json → {".": "1.6.2", "mailglass_admin": "1.6.2", "mailglass_inbound": "1.3.1"}
  release-please-config.json     → linked group ["mailglass","mailglass_admin"]; root "." exclude-paths
                                    ["brandbook",".planning","prompts","mailglass_admin","mailglass_inbound"] (RELH-01)
  mailglass_inbound/mix.exs:127  → {:mailglass, "== 1.6.2"} (the deferred re-pin target — D-13)
</interfaces>

@.release-please-manifest.json
@release-please-config.json
@mailglass_inbound/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full all-gates verification battery</name>
  <files>(read-only gate execution — no source files modified)</files>
  <read_first>
    - .planning/phases/103-verification-idempotent-closeout/103-VALIDATION.md (test/gate map, manual-only verifications)
    - .planning/phases/103-verification-idempotent-closeout/103-RESEARCH.md (Phase Requirements → Test Map; Pitfalls 3 and 6)
    - mailglass_admin/mix.exs (verify.preview + verify.support_contract.admin aliases, ~180-194)
  </read_first>
  <action>
    Run each gate and capture pass/fail:
    1. `cd mailglass_admin && mix verify.support_contract.admin` (token-parity + armed ratchet + admin contract).
    2. `cd mailglass_admin && mix verify.preview` (full admin test + assets build + `git diff --exit-code priv/static/` bundle-clean). No markup changed in 103, so the bundle should already be clean from Phase 102 — do NOT rebuild to "fix" it; if it is dirty, that is a finding to surface.
    3. `bash mailglass_admin/scripts/check-conformance.sh`
    4. `bash mailglass_admin/scripts/check-conformance-advisory.sh`
    5. `bash scripts/check_motion_conformance.sh`
    6. `cd mailglass_admin && npm run test:operator-browser` (Playwright structural, --workers=1).
    Exclude the known voice_test "Oops" dep-JS flake from the verdict (Pitfall 6) and re-run any
    intermittent structural red once with --workers=1 (Pitfall 3). Eyeball the 3 intentional
    structural skips (A3) — confirm none masks a real regression. Record every command's exit
    status / summary for the readiness note. Do NOT modify any source to make a gate pass.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass/mailglass_admin && mix verify.support_contract.admin 2>&1 | tail -3 && bash /Users/jon/projects/mailglass/mailglass_admin/scripts/check-conformance.sh >/dev/null 2>&1 && bash /Users/jon/projects/mailglass/scripts/check_motion_conformance.sh >/dev/null 2>&1 && git -C /Users/jon/projects/mailglass diff --exit-code mailglass_admin/priv/static/ && echo GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.support_contract.admin` exits 0 (includes the armed ratchet test).
    - `mix verify.preview` exits 0, including `git diff --exit-code priv/static/` (bundle-clean).
    - `check-conformance.sh`, `check-conformance-advisory.sh`, and `check_motion_conformance.sh` each exit 0.
    - `npm run test:operator-browser` reports 0 unexpected failures (the 3 skips are intentional; the voice_test "Oops" dep-JS flake is excluded by policy).
    - No source file modified to coerce a green.
  </acceptance_criteria>
  <done>The full gate battery is green (with the documented known-flake exclusions); results recorded for the readiness note.</done>
</task>

<task type="auto">
  <name>Task 2: Verify release posture read-only and write the prepare-only readiness note</name>
  <files>.planning/phases/103-verification-idempotent-closeout/103-03-SUMMARY.md</files>
  <read_first>
    - .release-please-manifest.json (manifest 1.6.2/1.6.2/1.3.1)
    - release-please-config.json (linked group + root exclude-paths / RELH-01)
    - mailglass_inbound/mix.exs (line 127 — the {:mailglass, "== 1.6.2"} pin)
    - .planning/phases/103-verification-idempotent-closeout/103-CONTEXT.md (D-11/D-12/D-13)
    - .planning/milestones/v1.7-phases/79-verification-and-visual-regression-hardening/79-04-SUMMARY.md (prepare-only precedent — note DISC-2: it PERFORMED the re-pin; we DEFER it)
  </read_first>
  <action>
    Verify the release posture READ-ONLY (D-11) and record it in the SUMMARY readiness note —
    edit NOTHING Release Please owns:
    - manifest still `{".": "1.6.2", "mailglass_admin": "1.6.2", "mailglass_inbound": "1.3.1"}`.
    - linked group = `["mailglass","mailglass_admin"]`; inbound is independent.
    - root `.` `exclude-paths` still scopes core away from
      brandbook/.planning/prompts/mailglass_admin/mailglass_inbound (RELH-01 intact).
    - `mailglass_inbound/mix.exs:127` still reads `{:mailglass, "== 1.6.2"}`.
    Then write the prepare-only readiness note: record (a) the gate-battery results from Task 1,
    (b) that a linked ~1.6.2→1.7.0 bump IS pending once the pipeline is armed (D-12, illustrative
    — nothing here hard-codes it), and (c) the single PENDING ceremony action: the inbound
    `fix(inbound):` exact-pin re-pin to the new core version, DEFERRED (D-13) because the target
    version is unknowable pre-PR and a guessed pin desyncs the reference/demo baseline. Per DISC-2,
    explicitly note this is a DELIBERATE divergence from the v1.7/Phase-79 precedent (which
    performed the re-pin) — do NOT copy that precedent's edit. Do NOT bump versions, edit the
    manifest/CHANGELOG, or re-pin now.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/mailglass && grep -q '"mailglass_inbound": "1.3.1"' .release-please-manifest.json && grep -q '{:mailglass, "== 1.6.2"}' mailglass_inbound/mix.exs && grep -qE 'exclude-paths' release-please-config.json && grep -qi 'PENDING' .planning/phases/103-verification-idempotent-closeout/103-03-SUMMARY.md && git diff --exit-code .release-please-manifest.json release-please-config.json mailglass_inbound/mix.exs && echo POSTURE_INTACT</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code` on .release-please-manifest.json, release-please-config.json, and mailglass_inbound/mix.exs shows NO changes (read-only verification only).
    - 103-03-SUMMARY.md contains the gate-battery results and the read-only release-posture checklist.
    - The note records the inbound re-pin as the single PENDING ceremony action, explicitly deferred (D-13), and flags the DISC-2 divergence from the Phase-79 precedent.
    - No version number, CHANGELOG entry, manifest value, or inbound pin is edited.
  </acceptance_criteria>
  <done>All gates verified green; the prepare-only readiness note is written; nothing Release Please owns is touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepare-only ceremony → Release Please config | Versioning/manifest/pin are owned by Release Please; humans/agents never edit them. |
| CI gates → merge decision | The gate battery is the trust boundary protecting milestone quality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-05 | Tampering | accidental release train (root `.` claims all paths) | mitigate | `exclude-paths` in release-please-config.json (RELH-01) VERIFIED-INTACT read-only (Task 2); never edited (D-11). |
| T-103-06 | Tampering | guessed inbound pin desyncs reference baseline | mitigate | D-13: re-pin recorded as PENDING only; `mailglass_inbound/mix.exs` left at `== 1.6.2`; git diff --exit-code confirms untouched. |
| T-103-07 | Information disclosure | secret exposure to PR jobs | accept/mitigate | Existing `hex-publish` GitHub Environment isolates HEX_API_KEY; prepare-only never publishes — no new exposure. |
| T-103-08 | Tampering | source edited to coerce a green gate | mitigate | Task 1 forbids modifying source to pass a gate; bundle-clean re-confirmed, not rebuilt to mask drift. |

No HIGH-severity unmitigated threats. No product code, no routes, no auth, no crypto.
</threat_model>

<verification>
- Full gate battery green (with documented known-flake exclusions).
- `git diff --exit-code` on manifest, config, and inbound mix.exs all clean (read-only).
- Readiness note records gate results + posture checklist + the single PENDING re-pin.
</verification>

<success_criteria>
ROADMAP Phase 103 success criterion 3 met (all gates green in CI) and criterion 4 first half
met (release ceremony staged prepare-only, admin minor bump mechanically drags matched core +
inbound, nothing Release Please owns is touched).
</success_criteria>

<output>
Create `.planning/phases/103-verification-idempotent-closeout/103-03-SUMMARY.md` (this IS the
readiness note) when done.
</output>
