---
phase: 67-demo-app-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/demo_app/mix.exs
  - reference/demo_app/README.md
  - reference/host_app/SCOPE.md
  - test/reference_host/scope_lock_contract_test.exs
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "D-01 D-02 D-03: `reference/demo_app` remains the rich Phoenix demo and `reference/host_app` remains a narrow trust-proof host."
    - "D-04 D-05 D-06: local path mode is default, `MAILGLASS_DEMO_DEPS=hex` resolves published packages, and the switch is demo-app wiring only."
    - "Published Hex truth on 2026-06-01 is `mailglass ~> 1.3`, `mailglass_admin ~> 1.3`, and `mailglass_inbound ~> 0.3.0`."
  artifacts:
    - path: "reference/demo_app/mix.exs"
      provides: "Dual dependency mode with current published constraints"
    - path: "reference/demo_app/README.md"
      provides: "Boundary and dependency-mode quickstart wording"
    - path: "test/reference_host/scope_lock_contract_test.exs"
      provides: "Executable guard that the rich demo does not collapse into the reference host"
---

<objective>
Harden the separate demo-app boundary and make both dependency modes executable.
This plan covers DEMO-01 and DEMO-02 without adding stable Mailglass public APIs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-demo-app-foundation/67-CONTEXT.md
@.planning/phases/67-demo-app-foundation/67-RESEARCH.md
@.planning/phases/67-demo-app-foundation/67-VALIDATION.md
@.planning/phases/67-demo-app-foundation/67-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten and document demo dependency mode</name>
  <files>reference/demo_app/mix.exs, reference/demo_app/README.md</files>
  <read_first>
    - reference/demo_app/mix.exs
    - reference/demo_app/README.md
    - .planning/phases/67-demo-app-foundation/67-RESEARCH.md
  </read_first>
  <action>
    Update `reference/demo_app/mix.exs` so Hex mode uses `{:mailglass_inbound, "~> 0.3.0"}` while preserving `{:mailglass, "~> 1.3"}` and `{:mailglass_admin, "~> 1.3"}`. Keep local path dependencies as the default when `MAILGLASS_DEMO_DEPS` is unset. Update the README dependency-mode section to say local path mode is for maintainer iteration, Hex mode is for published-package smoke checks, and the demo does not define stable public API guarantees.
  </action>
  <verify>
    <automated>rg -n 'MAILGLASS_DEMO_DEPS|~> 0\.3\.0|~> 1\.3' reference/demo_app/mix.exs reference/demo_app/README.md && cd reference/demo_app && MAILGLASS_DEMO_DEPS=hex mix deps.get --only prod</automated>
  </verify>
  <acceptance_criteria>
    - `reference/demo_app/mix.exs` contains `{:mailglass_inbound, "~> 0.3.0"}` in Hex mode.
    - `reference/demo_app/mix.exs` still uses local `path:` dependencies when `MAILGLASS_DEMO_DEPS` is not `hex`.
    - `reference/demo_app/README.md` names `MAILGLASS_DEMO_DEPS=hex` and states that Hex mode is a published-package smoke path.
    - `cd reference/demo_app && MAILGLASS_DEMO_DEPS=hex mix deps.get --only prod` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Protect the reference-host boundary</name>
  <files>reference/host_app/SCOPE.md, test/reference_host/scope_lock_contract_test.exs</files>
  <read_first>
    - reference/host_app/SCOPE.md
    - test/reference_host/scope_lock_contract_test.exs
    - .planning/milestones/v1.3-phases/52-trust-scope-lock-reference-host-baseline/52-CONTEXT.md
    - .planning/phases/67-demo-app-foundation/67-CONTEXT.md
  </read_first>
  <action>
    Add or tighten source-level assertions that `reference/host_app` stays narrow: it must not contain demo dashboard, seeded B2B persona, Playwright evidence, operator-lab copy, or `MailglassDemo` modules. Update `reference/host_app/SCOPE.md` only if it needs a short note that the rich click-around demo now lives in `reference/demo_app`.
  </action>
  <verify>
    <automated>mix test test/reference_host/scope_lock_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/reference_host/scope_lock_contract_test.exs` exits 0.
    - The scope-lock test fails if `reference/host_app` contains `MailglassDemo`, `Northstar Ops`, or `demo dashboard`.
    - `reference/host_app/SCOPE.md` explicitly routes rich-demo behavior to `reference/demo_app` if that statement is not already present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo app -> public package contract | Demo wiring must not create stable Mailglass API guarantees. |
| rich demo -> reference host | Rich demo work must not pollute the narrow trust-proof host. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-67-01 | Tampering | high | `reference/host_app` boundary | mitigate | yes | Scope-lock assertions must fail on rich-demo markers inside `reference/host_app`; blocking command is `mix test test/reference_host/scope_lock_contract_test.exs`. |
| T-67-02 | Spoofing | high | Hex dependency mode | mitigate | yes | Published-mode smoke must run with `MAILGLASS_DEMO_DEPS=hex` and current Hex constraints; blocking command is the Task 1 `mix deps.get --only prod`. |
</threat_model>

<verification>
- `rg -n 'MAILGLASS_DEMO_DEPS|~> 0\.3\.0|~> 1\.3' reference/demo_app/mix.exs reference/demo_app/README.md`
- `cd reference/demo_app && MAILGLASS_DEMO_DEPS=hex mix deps.get --only prod`
- `mix test test/reference_host/scope_lock_contract_test.exs`
</verification>

<success_criteria>
- Maintainers can run `reference/demo_app` separately from `reference/host_app`.
- Maintainers can switch the demo between local path dependencies and published Hex constraints.
- No new stable Mailglass public API is introduced for dependency mode.
</success_criteria>

<output>
Create `.planning/phases/67-demo-app-foundation/67-01-SUMMARY.md` when done.
</output>
