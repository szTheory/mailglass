---
phase: 67-demo-app-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.demo.yml
  - reference/demo_app/Dockerfile
  - reference/demo_app/lib/mailglass_demo_web/router.ex
  - reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
  - reference/demo_app/assets/playwright.config.cjs
autonomous: true
requirements:
  - DX-01
  - DX-02
must_haves:
  truths:
    - "D-07 D-08 D-09: `compose.demo.yml` is the canonical local click-around entry and keeps Postgres, Phoenix demo, and browser evidence as separate concerns with cache-aware volumes."
    - "D-10: browser evidence waits for a real Phoenix readiness or healthcheck path through `service_healthy`, not only `service_started`."
    - "D-11 D-12: browser dependency installation uses lockfile-respecting `npm ci` and deterministic Playwright browser dependencies while preserving a future clean/no-cache lane."
  artifacts:
    - path: "compose.demo.yml"
      provides: "Canonical demo stack with health-gated browser evidence and preserved cache volumes"
    - path: "reference/demo_app/Dockerfile"
      provides: "Deterministic OS/browser dependency setup for the demo and evidence runner"
---

<objective>
Make the one-command Docker Compose demo stack reliable enough for local
click-around and later browser evidence by adding health-gated readiness and
deterministic browser dependency setup.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-demo-app-foundation/67-CONTEXT.md
@.planning/phases/67-demo-app-foundation/67-RESEARCH.md
@.planning/phases/67-demo-app-foundation/67-VALIDATION.md
@.planning/phases/67-demo-app-foundation/67-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phoenix readiness and Compose health gating</name>
  <files>reference/demo_app/lib/mailglass_demo_web/router.ex, reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex, compose.demo.yml</files>
  <read_first>
    - reference/demo_app/lib/mailglass_demo_web/router.ex
    - reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
    - compose.demo.yml
    - reference/demo_app/config/dev.exs
  </read_first>
  <action>
    Add a demo-only health route such as `GET /health` that returns HTTP 200 and a small plain text body such as `ok`. Wire `compose.demo.yml` so the `demo` service has a healthcheck that probes `http://localhost:4015/health`, and change `demo_e2e.depends_on.demo.condition` from `service_started` to `service_healthy`. Keep `docker compose -f compose.demo.yml up --build demo` as the README-supported entry.
  </action>
  <verify>
    <automated>docker compose -f compose.demo.yml config && rg -n 'health|service_healthy|4015' compose.demo.yml reference/demo_app/lib/mailglass_demo_web/router.ex reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex</automated>
  </verify>
  <acceptance_criteria>
    - `compose.demo.yml` contains a `demo` healthcheck that calls the demo health URL on port `4015`.
    - `compose.demo.yml` contains `demo_e2e.depends_on.demo.condition: service_healthy`.
    - The demo router exposes a health route under the demo app, not under Mailglass package source.
    - `docker compose -f compose.demo.yml config` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make browser dependency setup lockfile-based and deterministic</name>
  <files>reference/demo_app/Dockerfile, compose.demo.yml, reference/demo_app/assets/playwright.config.cjs</files>
  <read_first>
    - reference/demo_app/Dockerfile
    - compose.demo.yml
    - reference/demo_app/assets/package.json
    - reference/demo_app/assets/package-lock.json
    - reference/demo_app/assets/playwright.config.cjs
  </read_first>
  <action>
    Replace the evidence runner's `npm --prefix assets install --no-audit --no-fund` command with lockfile-respecting `npm --prefix assets ci --no-audit --no-fund`. Ensure Chromium and required system dependencies are installed deterministically, either in `reference/demo_app/Dockerfile` or in the evidence command with `npx --prefix assets playwright install --with-deps chromium`. Preserve `PLAYWRIGHT_BROWSERS_PATH`, `npm_config_cache`, and all existing named cache volumes.
  </action>
  <verify>
    <automated>docker compose -f compose.demo.yml config && rg -n 'npm --prefix assets ci|playwright install.*chromium|PLAYWRIGHT_BROWSERS_PATH|mailglass_demo_playwright' compose.demo.yml reference/demo_app/Dockerfile reference/demo_app/assets/playwright.config.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `compose.demo.yml` or `reference/demo_app/Dockerfile` contains `npm --prefix assets ci --no-audit --no-fund`.
    - Playwright browser installation includes `chromium` and is deterministic from the checked-in assets package files.
    - `PLAYWRIGHT_BROWSERS_PATH` remains configured for both demo app and evidence runner where relevant.
    - Existing named cache volumes for Mix, Hex, npm, Playwright, package deps, and package `_build` directories remain present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix process -> browser evidence runner | The evidence runner must not start assertions before the app is ready. |
| npm registry -> evidence image | Browser test dependency resolution must not drift away from the lockfile. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-67-03 | Denial of service | high | Compose readiness | mitigate | yes | Add demo healthcheck and require `service_healthy` before `demo_e2e`; blocking command is `docker compose -f compose.demo.yml config` plus source assertions. |
| T-67-04 | Tampering | high | Browser dependency setup | mitigate | yes | Use `npm ci` and deterministic Playwright install semantics; blocking source assertion checks `npm ci`, `chromium`, and cache env/volume preservation. |
</threat_model>

<verification>
- `docker compose -f compose.demo.yml config`
- `rg -n 'health|service_healthy|npm --prefix assets ci|playwright install.*chromium|PLAYWRIGHT_BROWSERS_PATH|mailglass_demo_playwright' compose.demo.yml reference/demo_app/Dockerfile reference/demo_app/lib/mailglass_demo_web`
</verification>

<success_criteria>
- `docker compose -f compose.demo.yml up --build demo` remains the canonical local entry.
- Browser evidence waits for an actually healthy demo service.
- The Compose setup preserves cache-aware local iteration while using lockfile-based browser dependency installation.
</success_criteria>

<output>
Create `.planning/phases/67-demo-app-foundation/67-02-SUMMARY.md` when done.
</output>
