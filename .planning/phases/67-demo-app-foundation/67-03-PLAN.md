---
phase: 67-demo-app-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 67-01
  - 67-02
files_modified:
  - reference/demo_app/lib/mailglass_demo/demo_data.ex
  - reference/demo_app/lib/mailglass_demo_web/router.ex
  - reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
  - reference/demo_app/priv/repo/seeds.exs
  - reference/demo_app/test
  - reference/demo_app/README.md
  - mix.exs
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DX-01
  - DX-02
must_haves:
  truths:
    - "D-13 D-14 D-15 D-16: setup/reset aliases stay idiomatic, `mix demo.reset` stays deterministic, and reset wording is explicitly destructive."
    - "D-17 D-18 D-19: dashboard/login/reset, `/dev/mail`, and `/ops/mail` remain demo-only surfaces using public seams and simple demo-only auth."
    - "D-20 D-21 D-22: Phase 67 prepares stable startup/reset/artifact paths for future `demo_browser_evidence.v1` without making DOM, selectors, routes, or copy stable public API."
    - "D-23 D-24: copy stays calm, evidence-first, and uses Mailglass domain language."
  artifacts:
    - path: "reference/demo_app/lib/mailglass_demo/demo_data.ex"
      provides: "Deterministic reset contract for seeded demo tables"
    - path: "reference/demo_app/README.md"
      provides: "Destructive reset wording and future evidence-artifact boundary"
    - path: "mix.exs"
      provides: "Repo-root Phase 67 verification lane"
---

<objective>
Make reset and verification deterministic enough for later demo data and browser
evidence phases, while keeping all surfaces demo-only and bounded.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-demo-app-foundation/67-CONTEXT.md
@.planning/phases/67-demo-app-foundation/67-RESEARCH.md
@.planning/phases/67-demo-app-foundation/67-VALIDATION.md
@.planning/phases/67-demo-app-foundation/67-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scriptable deterministic reset proof</name>
  <files>reference/demo_app/lib/mailglass_demo/demo_data.ex, reference/demo_app/priv/repo/seeds.exs, reference/demo_app/lib/mailglass_demo_web/router.ex, reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex, reference/demo_app/test</files>
  <read_first>
    - reference/demo_app/lib/mailglass_demo/demo_data.ex
    - reference/demo_app/priv/repo/seeds.exs
    - reference/demo_app/lib/mailglass_demo_web/router.ex
    - reference/demo_app/lib/mailglass_demo_web/controllers/page_controller.ex
    - reference/demo_app/config/test.exs
    - reference/demo_app/mix.exs
  </read_first>
  <action>
    Add tests or a focused support assertion proving `MailglassDemo.DemoData.reset!/0` truncates and reseeds deterministic outbound, inbound, suppression, evidence, and replay counts with restarted identities. Add a scriptable demo-only reset path for browser evidence if needed, such as a test/evidence-only route guarded by demo environment config or a command documented for `demo_e2e`. Keep the existing POST `/demo/reset` form for human click-around. Any HTTP reset path must call `DemoData.reset!/0`, be demo-only, and include destructive wording in code/docs.
  </action>
  <verify>
    <automated>cd reference/demo_app && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `cd reference/demo_app && mix test` exits 0.
    - A test proves two consecutive reset runs produce the same delivery, event, inbound, and suppression counts.
    - The reset implementation continues to use `RESTART IDENTITY CASCADE` or an equivalent deterministic identity reset.
    - Any browser-evidence reset route or command is under `MailglassDemo*` and not package source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Phase 67 verification lane and bounded evidence wording</name>
  <files>mix.exs, reference/demo_app/README.md, reference/demo_app/assets/e2e/demo.spec.js</files>
  <read_first>
    - mix.exs
    - reference/demo_app/README.md
    - reference/demo_app/assets/e2e/demo.spec.js
    - scripts/check_preview_capture_checkpoint.sh
    - mailglass_admin/dev/mailglass_admin/preview/capture_manifest.ex
    - .planning/milestones/v1.3-phases/59-ci-trust-lanes-checkpoint-evidence/59-CONTEXT.md
  </read_first>
  <action>
    Add a repo-root verification alias or script named `verify.phase67` that runs the Phase 67 fast proofs: reference-host scope lock test, demo-app test suite, Compose config validation, and source assertions for dependency mode, health gating, `npm ci`, and cache volumes. Update README wording to state that future browser artifacts are adoption evidence with a versioned label such as `demo_browser_evidence.v1`, and that demo DOM, selectors, routes, and copy are not stable public API. Keep the copy evidence-first and use Mailglass terms: preview, delivery, event, suppression, inbound record, evidence, routing trace, replay, and tenant.
  </action>
  <verify>
    <automated>mix verify.phase67 && rg -n 'demo_browser_evidence\.v1|not stable public API|preview|delivery|suppression|inbound record|evidence|routing trace|replay|tenant' reference/demo_app/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.phase67` exits 0.
    - `mix verify.phase67` includes `mix test test/reference_host/scope_lock_contract_test.exs`.
    - `mix verify.phase67` includes a demo-app test command.
    - `mix verify.phase67` includes `docker compose -f compose.demo.yml config`.
    - `reference/demo_app/README.md` contains `demo_browser_evidence.v1` and states that demo DOM, selectors, routes, and copy are not stable public API.
    - README reset wording contains `destructive` or `truncate` near the reset instructions.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser evidence -> demo database | Reset must be deterministic and safe only for demo data. |
| evidence artifacts -> public API claims | Future screenshots/checkpoints must not become stable contract truth. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-67-05 | Tampering | high | demo reset path | mitigate | yes | Tests must prove reset determinism and identity restart; reset must stay under `MailglassDemo*`; blocking command is `cd reference/demo_app && mix test`. |
| T-67-06 | Repudiation | high | Phase 67 verification lane | mitigate | yes | Add `mix verify.phase67` and README boundary wording so future phases cannot claim Phase 67 was ready without executable proof; blocking command is `mix verify.phase67`. |
</threat_model>

<verification>
- `cd reference/demo_app && mix test`
- `mix verify.phase67`
- `rg -n 'demo_browser_evidence\.v1|not stable public API|destructive|truncate|preview|delivery|suppression|inbound record|evidence|routing trace|replay|tenant' reference/demo_app/README.md`
</verification>

<success_criteria>
- Reset is deterministic, scriptable for evidence, and described with destructive wording.
- The Phase 67 proof lane is reusable before Phase 68 starts.
- Future browser evidence has a bounded artifact vocabulary without widening public API guarantees.
</success_criteria>

<output>
Create `.planning/phases/67-demo-app-foundation/67-03-SUMMARY.md` when done.
</output>
