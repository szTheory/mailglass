---
phase: 132-config-mailglass-identifier-foundation
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/config.ex
  - mailglass_inbound/lib/mailglass_inbound/application.ex
  - mailglass_inbound/test/mailglass_inbound/config_schema_test.exs
autonomous: true
requirements: [SCHEMA-04]
must_haves:
  truths:
    - "`MailglassInbound.Config.schema/0` returns `\"mailglass\"` by default, honors `config :mailglass_inbound, :schema, \"<name>\"`, accepts `\"public\"`, and reads from `:persistent_term` after warm."
    - "`MailglassInbound.Config.validate_at_boot!/0` validates + warms `:schema` and raises `%Mailglass.ConfigError{type: :invalid}` on a malformed identifier."
    - "`MailglassInbound.Application.start/2` calls `MailglassInbound.Config.validate_at_boot!/0` as its first statement (previously never called)."
    - "Inbound reuses `Mailglass.Identifier.validate!/2` from core — no inbound-local regex, no new inbound error type."
    - "Only `:schema` is cached in `:persistent_term`; `retention/0` and `rate_limit/0` stay on the existing uncached `validated/0` path."
    - "No runtime behavior change beyond boot validation now firing: no facade wiring, no `prefix:` injection."
  artifacts:
    - mailglass_inbound/test/mailglass_inbound/config_schema_test.exs
  key_links:
    - "`MailglassInbound.Config.schema/0` / `warm_schema/0` call `Mailglass.Identifier.validate!/2` (core) at the cache-write boundary."
    - "`:persistent_term` key `{MailglassInbound.Config, :schema}` written by boot-warm and self-heal, read by `schema/0`."
    - "`MailglassInbound.Application.start/2` → `MailglassInbound.Config.validate_at_boot!/0` (first statement)."
---

<objective>
Mirror the core schema-config contract onto the `mailglass_inbound` package on its own
version line (Design Phase A, inbound half): a `:schema` config key, a boot-validated
`:persistent_term`-cached `MailglassInbound.Config.schema/0` accessor reusing core's
`Mailglass.Identifier`, and wiring inbound's previously-uncalled `validate_at_boot!/0`
into `application.ex`.

Purpose: family coherence — the accessor name, default, validator, error type, cache-key
shape, and validate-once-at-boundary discipline must be identical across core + inbound.
Also fixes a pre-existing gap: inbound's `validate_at_boot!/0` was defined but never
invoked. ZERO runtime behavior change beyond boot validation now firing (no facade).

Output: `MailglassInbound.Config` `:schema` key, `schema/0`, `warm_schema/0`, boot-warm;
boot wiring in `application.ex`; inbound config unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-config-mailglass-identifier-foundation/132-CONTEXT.md
@.planning/phases/132-config-mailglass-identifier-foundation/132-PATTERNS.md
@mailglass_inbound/lib/mailglass_inbound/config.ex
@mailglass_inbound/lib/mailglass_inbound/application.ex
</context>

<artifacts_produced>
This plan produces these symbols:
- `MailglassInbound.Config` `:schema` NimbleOptions key (`type: :string`, `default: "mailglass"`)
- `MailglassInbound.Config.schema/0` — `@spec schema() :: String.t()`, `@doc since: "2.0.0"`
- `MailglassInbound.Config.warm_schema/0` — private self-heal helper (same name/shape as core Plan 01)
- persistent_term key `{MailglassInbound.Config, :schema}`
- sentinel atom `:__miss__` (same as Plan 01 — family coherence)
- boot-warm of `:schema` inside `MailglassInbound.Config.validate_at_boot!/0`
- `MailglassInbound.Application.start/2` now invokes `validate_at_boot!/0` (first statement)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add :schema key, schema/0 accessor, and boot-warm to MailglassInbound.Config</name>
  <files>mailglass_inbound/lib/mailglass_inbound/config.ex, mailglass_inbound/test/mailglass_inbound/config_schema_test.exs</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/config.ex lines 14-57 (`@schema` list — add the key here, before `@moduledoc`), lines 59-101 (moduledoc, incl. the "not called automatically" note at ~l.99-101 to update), lines 103-107 (`validate_at_boot!/0` to extend), lines 165-172 (private `validated/0` — the uncached path `retention`/`rate_limit` stay on)
    - .planning/phases/132-config-mailglass-identifier-foundation/132-CONTEXT.md decisions D-12, D-13, D-14 (inbound reuses core validator; only `:schema` cached)
    - .planning/phases/132-config-mailglass-identifier-foundation/132-PATTERNS.md section "mailglass_inbound/lib/mailglass_inbound/config.ex"
    - .planning/phases/132-config-mailglass-identifier-foundation/132-01-PLAN.md (match the sentinel atom `:__miss__` and helper name `warm_schema/0` exactly for family coherence)
  </read_first>
  <behavior>
    - With no app env set, `MailglassInbound.Config.schema/0` returns `"mailglass"`.
    - With `Application.put_env(:mailglass_inbound, :schema, "analytics")` and the cache cleared, `schema/0` returns `"analytics"` (self-heal from `:mailglass_inbound` env — NEVER from `:mailglass`).
    - After a read, `:persistent_term.get({MailglassInbound.Config, :schema}, :__miss__)` is the validated string, not the sentinel.
    - `validate_at_boot!/0` with `config :mailglass_inbound, :schema, "has-dash"` raises `%Mailglass.ConfigError{type: :invalid}`.
    - `validate_at_boot!/0` with a valid `:schema` returns `:ok` and warms `{MailglassInbound.Config, :schema}`.
    - `schema/0` with `config :mailglass_inbound, :schema, "public"` returns `"public"`.
    - `retention/0` and `rate_limit/0` still work and are NOT cached in `:persistent_term` (unchanged path).
    - Tests restore app env + erase the persistent_term key in `on_exit`.
  </behavior>
  <action>
    Three additive edits to `mailglass_inbound/lib/mailglass_inbound/config.ex`, mirroring
    core Plan 01 exactly.

    (1) NimbleOptions `:schema` key (D-12): add `schema:` inside the existing `@schema` list
    (l.14-57, before `@moduledoc`). Same shape as core: `type: :string`, `default:
    "mailglass"`, `doc:` naming the Postgres SCHEMA holding inbound tables, `"public"` as the
    explicit pre-2.0 opt-out, must be a valid unquoted Postgres identifier. Read from the
    `:mailglass_inbound` app env ONLY (boundary law — never `:mailglass`).

    (2) Hot-path accessor `schema/0` + private `warm_schema/0` (D-14): identical
    sentinel-miss self-heal to core, keyed `{__MODULE__, :schema}` (i.e.
    `{MailglassInbound.Config, :schema}`). `schema/0` reads
    `:persistent_term.get({__MODULE__, :schema}, :__miss__)`; on `:__miss__` calls
    `warm_schema/0`, else returns the cached string. `warm_schema/0` reads
    `Application.get_env(:mailglass_inbound, :schema, "mailglass")`, runs
    `Mailglass.Identifier.validate!(schema, :schema)` — REUSE the core validator (D-13); do
    NOT duplicate the regex or add an inbound-local error type — `:persistent_term.put`s the
    validated string, returns it. `@doc since: "2.0.0"`, `@spec schema() :: String.t()`.
    Validate once at the cache-write boundary; no `config_change/3`, no invalidation. Use the
    SAME sentinel atom `:__miss__` and helper name `warm_schema/0` as core Plan 01. ONLY
    `:schema` is cached (D-14) — leave `retention/0` and `rate_limit/0` on the existing
    uncached `validated/0` path (cold-path; do not add them to persistent_term).

    (3) Boot-warm (D-14/D-15): in `validate_at_boot!/0` (l.104-107), before returning `:ok`,
    read the `:schema` (from the `validated()` result or re-read `:mailglass_inbound` env),
    run `Mailglass.Identifier.validate!(schema, :schema)`, and `:persistent_term.put({__MODULE__,
    :schema}, <validated string>)` — mirroring core warming `:theme`/`:schema`. Keep the
    existing `_ = validated()` call so `retention`/`rate_limit` still validate. Still returns
    `:ok`; no spec change.

    Update the moduledoc note (~l.99-101) that currently says `validate_at_boot!/0` "is not
    called automatically": after D-15 wires it into `application.ex`, state that inbound's own
    `application.ex` now calls `validate_at_boot!/0` at boot to warm `:schema` and fail fast on
    a bad identifier (the on-demand `retention`/`rate_limit` reads are unaffected).

    Do NOT wire any facade or inject `prefix:` — pure additive.

    Write `mailglass_inbound/test/mailglass_inbound/config_schema_test.exs` covering every case
    in `<behavior>`. Use `async: false`; in `on_exit`, erase `{MailglassInbound.Config,
    :schema}` from `:persistent_term` and restore/delete the `:mailglass_inbound, :schema` app
    env. Assert malformed boot via `assert_raise Mailglass.ConfigError` matching struct field
    `type: :invalid`, never message text.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/config_schema_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/test/mailglass_inbound/config_schema_test.exs` passes (scoped per-file to avoid the known inbound full-suite pool flake).
    - `schema/0` returns `"mailglass"` by default, honors override + `"public"`, reads from `:persistent_term` after warm, and self-heals from `:mailglass_inbound` env only.
    - Inbound uses core's `Mailglass.Identifier.validate!/2` (no inbound-local regex/error).
    - `retention/0` and `rate_limit/0` are NOT cached in `:persistent_term`.
  </acceptance_criteria>
  <done>`MailglassInbound.Config` exposes a boot-validated, `:persistent_term`-cached `schema/0` reusing core's validator, with only `:schema` cached, and its unit test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MailglassInbound.Config.validate_at_boot!/0 into application start/2</name>
  <files>mailglass_inbound/lib/mailglass_inbound/application.ex</files>
  <read_first>
    - mailglass_inbound/lib/mailglass_inbound/application.ex lines 10-22 (`start/2`: `maybe_warn_fallback_mode/0` at l.11, `children` at l.13, `Supervisor.start_link` at l.22)
    - lib/mailglass/application.ex lines 7-11 (core's analog — `validate_at_boot!/0` as the first thing in `start/2`)
    - .planning/phases/132-config-mailglass-identifier-foundation/132-CONTEXT.md decision D-15
  </read_first>
  <action>
    Insert `MailglassInbound.Config.validate_at_boot!()` as the FIRST statement of
    `MailglassInbound.Application.start/2` — before the existing `maybe_warn_fallback_mode/0`
    call (l.11) and the `children` list (D-15). Inbound is the correct owner because `:schema`
    is inbound's OWN app env. A direct call is fine (inbound has no load-order concern that
    would require core's defensive `Code.ensure_loaded?`/`function_exported?` guard; matching
    the guarded form is acceptable if preferred, but not required). This fixes the pre-existing
    gap where `validate_at_boot!/0` was defined but never invoked, and warms the `:schema`
    persistent_term before children start. Do not otherwise change the supervision tree, the
    children, or add any facade/`prefix:` wiring.
  </action>
  <verify>
    <automated>cd mailglass_inbound && grep -q 'MailglassInbound.Config.validate_at_boot!' lib/mailglass_inbound/application.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `start/2` invokes `MailglassInbound.Config.validate_at_boot!/0` as its first statement.
    - `mailglass_inbound` compiles clean with `--warnings-as-errors`.
    - The supervision tree / children list is otherwise unchanged; no facade or `prefix:` wiring added.
  </acceptance_criteria>
  <done>Inbound's `application.ex` calls `validate_at_boot!/0` at boot, closing the pre-existing never-called gap and warming the `:schema` cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter `:mailglass_inbound` config → `Mailglass.Identifier.validate!/2` | The inbound `:schema` value (adopter-controlled) crosses into the shared core validator, the same chokepoint core uses, before any future inbound DDL/query builder (Phase 135) interpolates it. No SQL interpolation happens in THIS phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-04 | Tampering | inbound `:schema` value reaching a future inbound DDL/query builder | high | mitigate | Reuse core `Mailglass.Identifier.validate!/2` (D-13): same regex + 63-byte guard reject injection vectors and truncation-aliasing names; validated once at the cache-write boundary and frozen in `:persistent_term`. One validator across the family — no divergent inbound path. |
| T-132-05 | Spoofing | inbound reading `:schema` from the wrong app env (`:mailglass` instead of `:mailglass_inbound`) | medium | mitigate | D-12 boundary law: `warm_schema/0` reads `Application.get_env(:mailglass_inbound, :schema, ...)` only; test asserts self-heal pulls from the inbound env. |
| T-132-06 | Denial of Service | bad inbound `:schema` failing late (at first DB op) instead of at boot | low | mitigate | D-15 wires `validate_at_boot!/0` into `start/2` as the first statement so a malformed identifier fails the node at boot, not mid-request. |
</threat_model>

<verification>
Overall checks for this plan:
- `cd mailglass_inbound && mix test test/mailglass_inbound/config_schema_test.exs --seed 0` — green (scope per-file; the inbound full suite has a known pool flake — use `--seed 0` / per-file per project memory).
- `cd mailglass_inbound && mix credo --strict` — green (validate by RUNNING).
- `cd mailglass_inbound && mix compile --warnings-as-errors` — clean.
- Family coherence check: `schema/0`, default `"mailglass"`, shared `Mailglass.Identifier`, `%Mailglass.ConfigError{type: :invalid}`, key `{MailglassInbound.Config, :schema}`, sentinel `:__miss__`, `warm_schema/0` — all identical to core Plan 01.
- No facade wiring / no `prefix:` injection in the diff.
</verification>

<success_criteria>
- `MailglassInbound.Config` has the documented `:schema` key (default `"mailglass"`, `"public"` opt-out) read from `:mailglass_inbound` env only (SCHEMA-04).
- `MailglassInbound.Config.schema/0` is boot-validated, `:persistent_term`-cached, sentinel-self-healing, reusing core's `Mailglass.Identifier` (SCHEMA-04).
- `validate_at_boot!/0` warms `:schema` and fails fast; only `:schema` is cached (SCHEMA-04).
- `application.ex` `start/2` invokes `validate_at_boot!/0` (pre-existing gap closed).
- Inbound config test + `mix credo --strict` green; no facade wiring / no `prefix:` (ROADMAP success #5).
</success_criteria>

<output>
Create `.planning/phases/132-config-mailglass-identifier-foundation/132-02-SUMMARY.md` when done
</output>
