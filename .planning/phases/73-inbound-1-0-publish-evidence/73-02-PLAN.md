---
phase: 73-inbound-1-0-publish-evidence
plan: 02
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - MAINTAINING.md
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
  - .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md
autonomous: true
requirements: [REL-02, REL-03]
must_haves:
  truths:
    - "The maintainer runbook documents the inbound-only publish/fallback dispatch path (package=mailglass_inbound, tag-pinned, no forced core/admin) (REL-02)"
    - "The inbound-only dispatch path is documented and dry-run-rehearsed using the EXISTING publish-hex.yml wiring with zero workflow changes — it is not reinvented or rebuilt (D-06, REL-02)"
    - "Reference-app pins (reference/host_app/mix.exs, reference/demo_app/mix.exs) are left untouched below ~> 1.0; no pin is flipped to ~> 1.0 in this prepare-posture phase (D-09)"
    - "The stale Phase 38 runbook path is fixed to the archived location; MAINTAINING.md contains no .planning/phases/38- reference (D-10, SC-3)"
    - "An inbound-only dry-run dispatch rehearsal is either executed and its run URL captured, or staged as an exact gh command with the run URL marked pending — honestly either way (D-05/D-07)"
    - "A field-presence test asserts the inbound RELEASE-RECORD exists and carries the REL-03 headers + pending markers, asserting NO live external Hex/HexDocs state (D-08)"
    - "The runbook gating language (idempotency, package order, 'do not dispatch from main') is preserved verbatim, not loosened (security note)"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Inbound-only publish/fallback runbook wording with corrected archived Phase 38 path"
      contains: "mailglass_inbound-v1.0.0"
    - path: "mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs"
      provides: "Field-presence guard for the inbound RELEASE-RECORD + stale-path regression guard"
      contains: "73-01-RELEASE-RECORD.md"
  key_links:
    - from: "MAINTAINING.md"
      to: ".planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/"
      via: "corrected archived Phase 38 form path"
      pattern: "milestones/v1.0-phases/38-release-rehearsal"
    - from: "docs_contract_test.exs"
      to: ".planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md"
      via: "File.read! Path.expand reach-up + field-presence assertions"
      pattern: "73-01-RELEASE-RECORD"
---

<objective>
Document and stage the inbound-only publish path for REL-02 without rebuilding any
workflow mechanics, and close the evidence-completeness loop for REL-03 with a light
field-presence guard.

Concretely: (a) fix the stale Phase 38 runbook path in `MAINTAINING.md` (D-10) and
refine the inbound-only publish/fallback wording without loosening gating; (b) attempt
the inbound-only `dry_run=true` dispatch rehearsal against the reviewed ref and record
its run URL — or, if no green `ci.yml` run is resolvable on the ref, stage the exact
`gh workflow run` command and mark the run URL `pending` (both honest under D-05/D-07);
(c) extend `docs_contract_test.exs` with a string-presence-only guard that the inbound
RELEASE-RECORD exists, carries the REL-03 field headers + pending markers, and a
regression guard that `MAINTAINING.md` no longer cites `.planning/phases/38-`.

Purpose: REL-02 (inbound-only publish path documented + rehearsed) and REL-03
(evidence completeness asserted deterministically) — never a gate on live external state (D-08).
Output: corrected runbook, recorded rehearsal evidence, one test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-inbound-1-0-publish-evidence/73-CONTEXT.md
@.planning/phases/73-inbound-1-0-publish-evidence/73-RESEARCH.md
@.planning/phases/73-inbound-1-0-publish-evidence/73-PATTERNS.md

# The record authored in 73-01 (read for the field headers the test must assert)
@.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md

<interfaces>
<!-- Reach-up + string-presence test idiom the extension must mirror. -->
<!-- Source: mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs lines 404-417 -->

Existing canonical-lane test reads repo-root files via Path.expand("../../../<file>", __DIR__):
  maintaining = File.read!(Path.expand("../../../MAINTAINING.md", __DIR__))
  assert maintaining =~ "mailglass_inbound"
  assert maintaining =~ "mix verify.stability_contract"

The inbound RELEASE-RECORD lives at:
  .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md
reached from the test dir via:
  Path.expand("../../../.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md", __DIR__)
</interfaces>

<workflow_dispatch>
<!-- Verified publish-hex.yml dispatch inputs (lines 13-32) — package choice includes mailglass_inbound -->
<!-- dry_run=true SKIPS per-sibling `mix hex.publish --dry-run` + `MIX_PUBLISH=true mix deps.get`. -->
<!-- A dry-run proves input wiring + tag-pinned checkout + inbound prepublish summary + gate-ci-green + fan-out gating, NOT dependency resolution. -->
Reviewed ref under prepare posture: main @ 88155d3e (no mailglass_inbound-v1.0.0 tag cut yet).
Command:
  gh workflow run publish-hex.yml -f package=mailglass_inbound -f dry_run=true -f tag=<reviewed-ref>
Capture:
  gh run list --workflow=publish-hex.yml --limit 1
  gh run view <run-id> --json url,conclusion
</workflow_dispatch>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale Phase 38 runbook path and refine inbound-only publish/fallback wording</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md lines 250-339 (Release Runbook section, stale path at 256-257, inbound fallback at 298)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-PATTERNS.md (Fix 1 / Fix 2, lines 80-94)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-RESEARCH.md (Pitfall 3 + security note, lines 272-281, 447-452)
  </read_first>
  <action>
    Fix 1 (D-10): repoint the two stale bullets at `MAINTAINING.md:256-257` from
    `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-CHECKLIST.md` and
    `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-RECORD.md`
    to the archived location
    `.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-CHECKLIST.md` and
    `.planning/milestones/v1.0-phases/38-release-rehearsal-and-proof-artifacts/38-03-RELEASE-RECORD.md`.
    In the same paragraph, add a sentence citing the inbound-specific companion forms
    `.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md` and
    `73-01-RELEASE-CHECKLIST.md` as the inbound-only-slice record/checklist (the archived Phase 38
    forms remain the linked core/admin v1.0 record).
    Fix 2 (REL-02 refinement): in the fallback paragraph (line 298) keep the existing inbound-only
    recovery sentence and the `Do not dispatch from main` constraint verbatim; refine wording so the
    inbound-only `1.0.0` publish/fallback path is explicit — dispatch `package=mailglass_inbound`
    pinned to `mailglass_inbound-v1.0.0`, fan-out skips `publish-core` and does NOT trigger
    `publish-admin`, so no `mailglass`/`mailglass_admin` release is forced. Preserve the package-order
    and `mix hex.info` idempotency notes (lines 296-297) verbatim. Do NOT loosen any gating language
    (the publish-inbound/publish-admin success/skipped gating is a security control — security note).
  </action>
  <verify>
    <automated>grep -n "phases/38" MAINTAINING.md && echo FAIL || grep -q "milestones/v1.0-phases/38-release-rehearsal" MAINTAINING.md && grep -q "mailglass_inbound-v1.0.0" MAINTAINING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "phases/38" MAINTAINING.md` returns no matches (stale path eliminated, D-10).
    - `grep -c "milestones/v1.0-phases/38-release-rehearsal" MAINTAINING.md` returns >= 2 (both bullets repointed).
    - MAINTAINING.md contains `73-01-RELEASE-RECORD.md` (inbound companion cited).
    - MAINTAINING.md still contains the literal `Do not dispatch from` and the `mix hex.info` idempotency note (gating preserved).
    - MAINTAINING.md contains `mailglass_inbound-v1.0.0` (inbound-only fallback dispatch documented).
  </acceptance_criteria>
  <done>Runbook stale path fixed to archived location, inbound companion forms cited, inbound-only publish/fallback wording explicit, gating language preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rehearse (or stage) the inbound-only dry-run dispatch and record its evidence</name>
  <files>.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md</files>
  <read_first>
    - .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md (the record to update; written in 73-01)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-RESEARCH.md (Code Examples + Open Questions, lines 306-326, 367-376; Pitfall 1 over-claim warning lines 251-261)
    - .github/workflows/publish-hex.yml (dispatch inputs lines 13-32; dry-run skip lines 313/338/395/420)
  </read_first>
  <action>
    Attempt the inbound-only dry-run dispatch against the reviewed ref (current `main` @ the SHA the
    maintainer will tag): run `gh workflow run publish-hex.yml -f package=mailglass_inbound
    -f dry_run=true -f tag=<reviewed-ref>`, then `gh run list --workflow=publish-hex.yml --limit 1`
    and `gh run view <run-id> --json url,conclusion`. If the dispatch fires and resolves a green
    `ci.yml` run on the ref, capture the run URL + conclusion into the record's
    `Publish workflow run URL:` line and add a `## Dry-run rehearsal` section.
    If the dispatch cannot resolve (no green `ci.yml` run on the ref / `gh` not authenticated /
    gate-ci-green cannot resolve), do NOT force it: record the EXACT `gh workflow run` command and the
    resolved ref in a `## Dry-run rehearsal` section and leave `Publish workflow run URL:` as
    `pending` (D-05/D-07 — both honest under prepare posture).
    In that `## Dry-run rehearsal` section, state precisely what the dry-run proves and does NOT prove:
    it proves `package=mailglass_inbound` routing, tag-pinned checkout, the inbound prepublish summary,
    `gate-ci-green` resolution, `publish-core` skipped, `publish-inbound` gated-in, and `publish-admin`
    NOT triggered (no forced core/admin release). It does NOT prove `== 1.3.0` core-pin resolution or
    per-sibling `mix hex.publish --dry-run` — those are SKIPPED in dry-run mode (Pitfall 1; do not
    over-claim "dry-run validated dependency resolution"). Do NOT create the `mailglass_inbound-v1.0.0`
    tag and do NOT run a non-dry-run publish.
  </action>
  <verify>
    <automated>grep -q "Dry-run rehearsal" .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md && grep -q "package=mailglass_inbound" .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `73-01-RELEASE-RECORD.md` contains a `## Dry-run rehearsal` section.
    - The record contains the exact dispatch command substring `gh workflow run publish-hex.yml` with `package=mailglass_inbound` and `dry_run=true`.
    - The `Publish workflow run URL:` line is EITHER a captured `https://github.com/.../actions/runs/` URL OR the literal `pending` — never fabricated (D-05).
    - The rehearsal section states the dry-run does NOT prove `== 1.3.0` dependency resolution (no over-claim, Pitfall 1).
    - No `mailglass_inbound-v1.0.0` git tag created: `git tag --list mailglass_inbound-v1.0.0` returns empty.
  </acceptance_criteria>
  <done>Inbound-only dry-run rehearsal executed-and-captured or staged-as-command with run URL pending; over-claim guard wording present; no tag cut, no real publish.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the field-presence + stale-path-regression guard (D-08)</name>
  <files>mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs</files>
  <behavior>
    - Test: the inbound RELEASE-RECORD at the Phase 73 path is readable and `=~` each REL-03 header in ["Tag", "Publish workflow run URL", "Fallback", "Hex index", "HexDocs", "smoke", "60-minute"].
    - Test: the record `=~ "not run"` AND `=~ "pending"` (post-publish fields read as pending — Honest Surface Area).
    - Test: `MAINTAINING.md` does NOT contain `.planning/phases/38-` (stale-path regression guard, D-10).
    - Negative: the test must NOT perform any HTTP / live Hex / HexDocs lookup (string-presence only, D-08).
  </behavior>
  <read_first>
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs lines 404-417 (reach-up File.read! + assert =~ idiom) and the `refute ... =~` idiom used elsewhere in the file
    - .planning/phases/73-inbound-1-0-publish-evidence/73-PATTERNS.md (field-presence + pending-marker + regression-guard shapes, lines 113-135)
    - .planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md (the headers/markers the assertions target)
  </read_first>
  <action>
    Add one ExUnit test to `MailglassInbound.DocsContractTest` named
    `"inbound release record exists, carries REL-03 field headers, and reads pending honestly"`.
    Read the record via `File.read!(Path.expand("../../../.planning/phases/73-inbound-1-0-publish-evidence/73-01-RELEASE-RECORD.md", __DIR__))`.
    For each header in `["Tag", "Publish workflow run URL", "Fallback", "Hex index", "HexDocs", "smoke", "60-minute"]`,
    assert `record =~ header`. Then `assert record =~ "not run"` and `assert record =~ "pending"`.
    In the same test (or a second test in the same module), read
    `File.read!(Path.expand("../../../MAINTAINING.md", __DIR__))` and `refute maintaining =~ ".planning/phases/38-"`
    (stale-path regression guard, D-10).
    String-presence assertions ONLY — NO HTTPoison/:httpc/URL resolution, NO assertion that any
    hex.pm/hexdocs.pm URL resolves (D-08 / Pitfall 4). Do not hardcode `1.0.0`/`1.3.0` version literals.
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` exits 0.
    - `cd mailglass_inbound && mix verify.docs.contract.inbound` exits 0.
    - The new test references `73-01-RELEASE-RECORD.md` and asserts all 7 REL-03 headers + the `not run`/`pending` markers + the `refute ... ".planning/phases/38-"` regression guard.
    - `grep -E "HTTPoison|:httpc|hex.pm|hexdocs.pm" mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` shows no new live-state lookup added by this task (D-08).
  </acceptance_criteria>
  <done>Field-presence + pending-marker + stale-path-regression guards added; inbound docs-contract suite green; no live external-state assertion introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → runbook | Runbook is the operational release contract; a broken path or loosened gate misleads the release operator |
| test suite → release record | Evidence-completeness guard must not regress into a live-state gate that reds milestone closeout |
| `gh` dispatch → GitHub Actions | A dispatch must be dry-run and inbound-only; a real publish or tag would be irreversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Tampering | MAINTAINING.md release gating | mitigate | Task 1 preserves the `Do not dispatch from main`, package-order, and `mix hex.info` idempotency language verbatim; acceptance criteria grep-assert their presence. Loosening `publish-inbound`/`publish-admin` success/skipped gating is forbidden (it prevents HEX_API_KEY exposure on a failed run). |
| T-73-05 | Denial of Service | milestone closeout via a live-state gate | mitigate | Task 3 asserts string-presence ONLY; acceptance criteria grep-assert NO new `hex.pm`/`hexdocs.pm`/`:httpc` lookup. A gate asserting live `1.0.0` Hex state would fail deterministically and block closeout (D-08/Pitfall 4). |
| T-73-06 | Tampering | accidental real publish/tag during rehearsal | mitigate | Task 2 uses `dry_run=true` only and creates no tag; acceptance criteria assert `git tag --list mailglass_inbound-v1.0.0` is empty. Canonical `release: published` publish stays a deferred maintainer action (D-01/D-02). |
| T-73-07 | Spoofing | over-claimed dry-run evidence | mitigate | Task 2 records what the dry-run does NOT prove (`== 1.3.0` resolution skipped); `Publish workflow run URL` is a real captured URL or literal `pending`, never fabricated (D-05/Pitfall 1). |
| T-73-08 | Information disclosure | premature reference-pin flip | accept | Reference pins stay at `~> 0.3`/`~> 0.3.0`; this plan touches no `reference/*/mix.exs` file (D-09). Flipping to `~> 1.0` before publish would make apps resolve an unavailable version — out of scope here. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; nothing added to any registry (RESEARCH "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `grep -n "phases/38" MAINTAINING.md` returns nothing (D-10 fixed).
- `cd mailglass_inbound && mix verify.docs.contract.inbound` exits 0.
- `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --warnings-as-errors` exits 0.
- `mix verify.stability_contract` (root) green.
- `git tag --list mailglass_inbound-v1.0.0` returns empty; `reference/host_app/mix.exs` and `reference/demo_app/mix.exs` unchanged (still `~> 0.3` / `~> 0.3.0`).
</verification>

<success_criteria>
- REL-02: inbound-only publish/fallback path documented in the runbook; stale Phase 38 path fixed (SC-3, D-10); dry-run rehearsal executed-and-captured or honestly staged (D-07).
- REL-03: evidence completeness asserted by a string-presence-only field-presence guard (D-08); record reads pending honestly.
- No live external-state assertion; no real publish; no tag cut; no pin flip (D-01/D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/73-inbound-1-0-publish-evidence/73-02-SUMMARY.md` when done.
</output>
