---
phase: "999.1"
plan: "03"
title: "Credo Drift Guard And Final Verification Sweep"
type: execute
wave: 3
depends_on:
  - "999.1-02"
files_modified:
  - "credo_checks/no_planning_artifact_comments.ex"
  - ".credo.exs"
  - "test/mailglass/credo/no_planning_artifact_comments_test.exs"
  - "test/mailglass/credo/credo_config_sentinel_test.exs"
requirements:
  - "TBD (backlog phase)"
autonomous: true
must_haves:
  truths:
    - "A dedicated Credo check prevents planning-artifact comments from re-entering core/admin/inbound source paths."
    - "Check behavior is test-covered for both detection and false-positive avoidance."
    - "Final sweep proves zero planning-artifact token drift in full `.ex` file contents under scoped source paths, with no compile/lint/test regressions."
  artifacts:
    - path: "credo_checks/no_planning_artifact_comments.ex"
      provides: "Reusable lint rule for comment/docstring artifact prevention"
      contains: "Mailglass.Credo.NoPlanningArtifactComments"
    - path: ".credo.exs"
      provides: "Strict CI registration and path-scoping for the new drift-prevention guard"
      contains: "Mailglass.Credo.NoPlanningArtifactComments"
    - path: "test/mailglass/credo/no_planning_artifact_comments_test.exs"
      provides: "Executable coverage for deny and allow cases"
      contains: "flags planning artifact tokens in comments"
  key_links:
    - from: ".planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md"
      to: "credo_checks/no_planning_artifact_comments.ex"
      via: "Implements recommended Option A drift-prevention surface"
      pattern: "Option A: New Credo check (recommended)"
    - from: ".planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-VALIDATION.md"
      to: ".credo.exs"
      via: "Validation contract requires mix credo --strict to enforce guard continuously"
      pattern: "mix credo --strict"
---

<objective>
Implement the recommended Credo-based drift-prevention guard and close the phase with full verification that comment cleanup is durable.

Purpose: prevent reintroduction of planning-artifact prose and block false-negative lint coverage gaps.
Output: new `Mailglass.Credo.NoPlanningArtifactComments` check, strict registration, tests, and final full-scope zero-token verification evidence.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-PATTERNS.md
@.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-VALIDATION.md
@.credo.exs
@credo_checks/no_bare_optional_dep_reference.ex
@test/mailglass/credo/credo_config_sentinel_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add `NoPlanningArtifactComments` Credo check and register strict path scope</name>
  <read_first>
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-RESEARCH.md
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-CONTEXT.md
    - credo_checks/no_bare_optional_dep_reference.ex
    - .credo.exs
    - credo_checks/no_full_response_in_logs.ex
  </read_first>
  <files>credo_checks/no_planning_artifact_comments.ex, .credo.exs</files>
  <action>Create `credo_checks/no_planning_artifact_comments.ex` defining `Mailglass.Credo.NoPlanningArtifactComments` with banned regex list `~r/\\bD-\\d{1,3}\\b/`, `~r/\\bPhase\\s+\\d+(?:\\.\\d+)?\\b/`, `~r/\\bPlan\\s+\\d+(?:\\.\\d+)?\\b/`, `~r/\\[ASSUMED[^\\]]*\\]/`, `~r/\\bREQ-[A-Z0-9-]+\\b/`, and `~r/\\bGSD\\b/`. Restrict scanning to comment/docstring surfaces and include path prefixes `lib/mailglass/`, `mailglass_admin/lib/`, `mailglass_inbound/lib/`. Register the check in `.credo.exs` extra_checks with the same included path prefixes and an explicit allowlist option `allowed_literals: []` for future intentional exceptions.</action>
  <acceptance_criteria>
    - New check module file exists and defines `Mailglass.Credo.NoPlanningArtifactComments`.
    - Check file contains all six banned regex patterns verbatim.
    - `.credo.exs` registers `Mailglass.Credo.NoPlanningArtifactComments` with included path prefixes for core/admin/inbound source trees.
    - `mix credo --strict` executes with the new check loaded.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "defmodule Mailglass\.Credo\.NoPlanningArtifactComments" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "~r/\\bD-\\d{1,3}\\b/" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "~r/\\bPhase\\s+\\d+(?:\\.\\d+)?\\b/" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "~r/\\bPlan\\s+\\d+(?:\\.\\d+)?\\b/" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "~r/\\[ASSUMED[^\\]]*\\]/" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "~r/\\bREQ-[A-Z0-9-]+\\b/" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "~r/\\bGSD\\b/" credo_checks/no_planning_artifact_comments.ex</automated>
    <automated>rg -n "Mailglass\.Credo\.NoPlanningArtifactComments" .credo.exs</automated>
    <automated>rg -n "included_path_prefixes:\s*\[\"lib/mailglass/\", \"mailglass_admin/lib/\", \"mailglass_inbound/lib/\"\]" .credo.exs</automated>
    <automated>mix credo --strict</automated>
  </verify>
  <done>The recommended Credo guard is implemented and wired into strict linting with explicit scope.</done>
</task>

<task type="auto">
  <name>Task 2: Add guard tests and execute final zero-drift verification sweep</name>
  <read_first>
    - test/mailglass/credo/credo_config_sentinel_test.exs
    - .planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-VALIDATION.md
    - credo_checks/no_planning_artifact_comments.ex
    - .credo.exs
  </read_first>
  <files>test/mailglass/credo/no_planning_artifact_comments_test.exs, test/mailglass/credo/credo_config_sentinel_test.exs</files>
  <action>Create `test/mailglass/credo/no_planning_artifact_comments_test.exs` with three deterministic assertions: flags `# D-20` in a comment, flags `Phase 50.7` in a moduledoc/docstring, and ignores `\"Phase 50\"` inside executable string literals when not in comment/docstring context. Extend `test/mailglass/credo/credo_config_sentinel_test.exs` with assertions that `.credo.exs` includes `Mailglass.Credo.NoPlanningArtifactComments` and that `included_path_prefixes` exactly include `lib/mailglass/`, `mailglass_admin/lib/`, and `mailglass_inbound/lib/`. After tests pass, run a final full-file repository sweep for planning-artifact tokens under scoped source paths.</action>
  <acceptance_criteria>
    - New Credo check test file exists with the three explicit allow/deny scenarios.
    - Config sentinel test asserts check registration and exact included path prefixes.
    - Final `rg` sweep returns zero matches for planning-artifact tokens in full `.ex` file contents across scoped source paths.
    - Full validation command from phase validation doc passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mailglass/credo/no_planning_artifact_comments_test.exs --warnings-as-errors</automated>
    <automated>mix test test/mailglass/credo/credo_config_sentinel_test.exs --warnings-as-errors</automated>
    <automated>! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass mailglass_admin/lib mailglass_inbound/lib</automated>
    <automated>mix compile --no-optional-deps --warnings-as-errors && mix credo --strict && mix test --warnings-as-errors</automated>
  </verify>
  <done>Drift prevention is test-backed, wired into strict linting, and validated with a zero-match final source sweep.</done>
</task>
</tasks>

<threat_model>
## Threat Register

| Threat ID | Severity | Threat | Mitigation in this plan |
|-----------|----------|--------|--------------------------|
| TM-9991-07 | high | False-negative lint enforcement allows planning artifacts to re-enter comments | Dedicated Credo rule is path-scoped and regression-tested with config sentinel assertions. |
| TM-9991-08 | high | Check overreach causes false positives and developer bypass pressure | Test suite includes explicit literal-string allow case and comment/docstring-only detection requirement. |
| TM-9991-09 | high | Incomplete final sweep leaves latent artifact tokens in source | Mandatory zero-match `rg` sweep on scoped source paths plus full compile/credo/test gate. |
</threat_model>

<verification>
- `mix test test/mailglass/credo/no_planning_artifact_comments_test.exs --warnings-as-errors`
- `mix test test/mailglass/credo/credo_config_sentinel_test.exs --warnings-as-errors`
- `! rg -n --glob "*.ex" "D-\d+|Phase\s+\d+(?:\.\d+)?|Plan\s+\d+(?:\.\d+)?|\[ASSUMED|REQ-[A-Z0-9-]+|\bGSD\b" lib/mailglass mailglass_admin/lib mailglass_inbound/lib`
- `mix compile --no-optional-deps --warnings-as-errors && mix credo --strict && mix test --warnings-as-errors`
</verification>

<success_criteria>
The phase closes with enforced, tested, and CI-run drift prevention for planning-artifact comments across source packages, and final verification proves zero scoped planning tokens in full source-file scans without runtime behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-human-readable-code-comments-gsd-artifact-cleanup-backlog/999.1-03-SUMMARY.md`.
</output>
