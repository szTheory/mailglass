---
phase: 114-component-groups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/scripts/check-conformance.sh
autonomous: true
requirements: [GROUP-01, GROUP-02]
must_haves:
  truths:
    - "A single thin <.card> shell primitive exists in MailglassAdmin.Components (border + radius + surface + outer padding only)"
    - "The SPACE-GATE fails on a raw off-grid spacing literal in a group surface file and stays green on preview_live.ex / mt-0.5 / min-h-11 / h-3 / border-l-4"
    - "The GROUP-GATE fails on the same-tone bg-base-200 card-in-card signature"
    - "PRIMITIVE-DRIFT-GATE now enforces card as a public primitive"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "card/1 thin shell function component"
      contains: "def card(assigns)"
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "SPACE-GATE + GROUP-GATE + card in PRIMITIVE-DRIFT loop"
      contains: "SPACE-GATE"
  key_links:
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      via: "GROUP_SURFACES 8-file array scopes SPACE-GATE / GROUP-GATE"
      pattern: "GROUP_SURFACES"
---

<objective>
Build the enforcement substrate first: extract the thin `<.card>` shell primitive into
`MailglassAdmin.Components`, and add the SPACE-GATE + GROUP-GATE conformance gates (plus `card`
in the PRIMITIVE-DRIFT-GATE loop) to `check-conformance.sh`. Gates exist BEFORE the code they
enforce so the later sweep/box-prison plan can run them green.

Purpose: D-01/D-02 (single thin card shell, ≤20 lines, no layout-engine slots) + D-03 (SPACE-GATE
banning raw off-grid spacing in the 8 group surfaces, file-scoped, word-boundary anchored) + the
D-05/D-07 GROUP-GATE box-prison tripwire. This is Wave 1 of the phase — pure substrate, no group
modules touched yet.

Output: `Components.card/1`; SPACE-GATE, GROUP-GATE, and `card` PRIMITIVE-DRIFT entry in
`check-conformance.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-component-groups/114-CONTEXT.md
@.planning/phases/114-component-groups/114-RESEARCH.md
@.planning/phases/114-component-groups/114-PATTERNS.md
@.planning/phases/114-component-groups/114-UI-SPEC.md

# Analogs to copy from (read before editing):
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/scripts/check-conformance.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the thin <.card> shell into Components</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    - components.ex:358-408 (stat_card/1 — the attr/slot/@doc-since/def shape to mirror EXACTLY)
    - components.ex:410-449 (data_state/1) and 451-464 (data_state_icon/1 arity-1 dispatch — mirror for card_padding/1)
    - components.ex:652 (mt-0.5 legitimate use — do NOT touch)
  </read_first>
  <behavior>
    - card/1 with padding={:md} renders a div containing "rounded-box border border-base-300 bg-base-200 p-md" plus the inner_block slot content
    - card/1 with padding={:lg} renders "p-lg" instead of "p-md"
    - card/1 forwards arbitrary data-* attributes (data-testid, data-group-card) and class overrides through :global @rest onto the root element
    - padding defaults to :md when the attr is omitted
  </behavior>
  <action>
    Add a new `card/1` function component to `MailglassAdmin.Components` per D-01/D-02, mirroring the
    `stat_card/1` shape (components.ex:358-408). The shell is deliberately thin (≤20 lines):
    exactly one `attr :padding, :atom, values: [:md, :lg], default: :md`, one
    `attr :rest, :global, default: %{}`, one `slot :inner_block, required: true`, a `@doc since: "1.13.0"`,
    and a private `card_padding/1` helper (`:md -> "p-md"`, `:lg -> "p-lg"`) mirroring the arity-1
    dispatch shape of `data_state_icon/1` (components.ex:451-464). Render a plain `<div>` root with
    `class={["rounded-box border border-base-300 bg-base-200", card_padding(@padding)]}` and `{@rest}`
    so call sites pass `data-testid` / `data-group-card` / `class` overrides through `:global`.

    DROP the decorative leading `card` DaisyUI class (RESEARCH Open Question 2 — no DaisyUI card layout
    is relied on); the PRIMITIVE-DRIFT/GROUP gate signatures in Task 2 must match whatever you choose.
    Do NOT bake in `shadow-raised` or `data-group-card` (D-02 / RESEARCH Open Question 1) — those are
    applied at call sites in plan 03. Do NOT add header/footer/grid slots or absorb inter-card rhythm,
    `dl` grids, `<ol>` spacing, or routing wells (the documented premature-abstraction footgun, D-02).
    This task does NOT touch any group module or call site — those swaps happen in plan 03.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c "def card(assigns)" lib/mailglass_admin/components.ex</automated>
  </verify>
  <acceptance_criteria>
    - `Components.card/1` exists, ≤20 lines, one `:padding` attr (`:md`/`:lg`, default `:md`), one `:global` rest, one required `:inner_block`
    - `card_padding(:md)` -> `"p-md"`, `card_padding(:lg)` -> `"p-lg"`
    - No header/footer/grid slots; no `shadow-raised` / `data-group-card` baked in
    - `mix compile --warnings-as-errors` clean
  </acceptance_criteria>
  <done>The thin card shell compiles and is the single source for the group surface shell.</done>
</task>

<task type="auto">
  <name>Task 2: Add SPACE-GATE + GROUP-GATE + card PRIMITIVE-DRIFT entry to check-conformance.sh</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - check-conformance.sh:35-78 (PRIMITIVE-DRIFT-GATE public-definition loop — add `card` at ~line 53)
    - check-conformance.sh:83-116 (FORM-DRIFT FILTER_WRAPPERS — the file-scoped array idiom to copy for GROUP_SURFACES)
    - check-conformance.sh:118-136 (STATCARD-GATE literal-class grep — analog for GROUP-GATE)
    - check-conformance.sh:194-204 (GAP-GATE — the trailing boundary `([^0-9a-z-]|$)` idiom; note it only bans gap-3/4/6, SPACE-GATE closes gap-2)
    - 114-RESEARCH.md Pitfall 1 (the validated SPACE-GATE regex + the false-positive guard list)
  </read_first>
  <action>
    Extend `check-conformance.sh` with three additions per D-03/D-05/D-07 (idioms in PATTERNS.md
    "scripts/check-conformance.sh" section and RESEARCH Pitfall 1):

    (1) Define a `GROUP_SURFACES` bash array of EXACTLY these 8 files (NOT a recursive `$LIB` grep —
    17 other lib files use the same numerics, Pitfall 1):
    operator/support_cards.ex, operator/suppression_card.ex, operator/detail_header.ex,
    operator/timeline.ex, inbound/routing_trace.ex, inbound/evidence_card.ex,
    inbound/detail_header.ex, inbound/timeline.ex (build each path from `$LIB` like FORM-DRIFT's
    FILTER_WRAPPERS at lines 83-116).

    (2) SPACE-GATE: ban raw off-grid padding/margin/space-y/gap numerics in `GROUP_SURFACES` only,
    using the word-boundary-anchored pattern from RESEARCH Pitfall 1
    (leading `(^|[^a-z-])` rejects min-h-11/border-l-4; trailing `([^0-9.a-z-]|$)` rejects mt-0.5 and
    gap-3xl while catching mt-1 / p-6 / space-y-1 / gap-2). On match, echo a FAIL line naming the gate
    and increment `errors` (same shape as GAP-GATE at 194-204).

    (3) GROUP-GATE tripwire: ban the same-tone card-in-card signature
    (`bg-base-200 border border-base-300 rounded-box`) in `GROUP_SURFACES` only — a cheap tripwire,
    NOT the depth authority (Floki owns real depth, plan 04 / D-07). On match, FAIL + increment `errors`.

    (4) Add `card` to the PRIMITIVE-DRIFT-GATE public-definition loop (~line 53) so
    `Components.card/1` is enforced as the single shell source. Per RESEARCH, do NOT add the
    per-primitive gallery `awk` dispatcher assertion for `card` (composed specimens are a different
    shape — plan 02 registers them differently).

    Keep the gate signature consistent with the dropped-`card`-class decision from Task 1.
  </action>
  <verify>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `GROUP_SURFACES` is an explicit 8-file array; SPACE-GATE and GROUP-GATE scope to it only (no `$LIB` recursion)
    - `card` appears in the PRIMITIVE-DRIFT-GATE primitive loop
    - SPACE-GATE regex is word-boundary anchored per RESEARCH Pitfall 1
    - Script runs (exit code reflects current repo state — see Task 3 false-positive validation)
  </acceptance_criteria>
  <done>The three gates exist and are file-scoped to the 8 group surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Validate SPACE-GATE by running the script (plant + remove a p-6)</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    - 114-RESEARCH.md Pitfall 1 "Warning signs" + the verification-step checklist (lines 365-372)
    - Project MEMORY: validate the SPACE-GATE regex by RUNNING the script, never by inspection
  </read_first>
  <action>
    Per project MEMORY (validate credo/gates by running, not grep proofs) and RESEARCH Pitfall 1:
    prove the SPACE-GATE behaves correctly by running `bash scripts/check-conformance.sh` against
    deliberately planted then-removed input. Temporarily insert a raw `p-6` (or `mt-1`) into one
    `GROUP_SURFACES` file (e.g. a class in `operator/timeline.ex`), confirm the SPACE-GATE FAILS,
    then remove it. Separately confirm the gate stays GREEN on the false-positive guard set:
    `preview_live.ex` (out-of-scope file with `p-6`), and the legitimate non-spacing tokens
    `mt-0.5` (components.ex:652), `min-h-11`, `h-3`, `border-l-4` — none may trip the SPACE-GATE.
    Leave the file/script in a clean state (no planted tokens) at the end. Record the validation
    result (planted-fails / guard-green) in the SUMMARY. This task adds NO permanent code change
    beyond confirming Task 2's regex; if validation exposes a regex flaw, fix Task 2's pattern here.
  </action>
  <verify>
    <automated>cd mailglass_admin && git diff --quiet scripts/check-conformance.sh lib/ && echo "tree clean: no planted tokens left" && bash scripts/check-conformance.sh > /dev/null 2>&1; echo "gate-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - A planted `p-6`/`mt-1` in a GROUP_SURFACES file makes SPACE-GATE FAIL (verified by running)
    - The gate stays GREEN on `preview_live.ex`, `mt-0.5`, `min-h-11`, `h-3`, `border-l-4`
    - No planted tokens remain in the working tree
  </acceptance_criteria>
  <done>SPACE-GATE proven to catch the real defect and not false-fire, by running the script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a presentational function component and CI grep gates. No new route, input, auth surface, data flow, or destructive action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | scope fence (D-12) | mitigate | SPACE-GATE/GROUP-GATE are file-scoped to the 8 group surfaces only; no nav/auth/preview files touched. |
| T-114-02 | Information Disclosure | card/1 shell | accept | Shell is presentational; carries no PII. `data-*` test attributes are structural, not user data. |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero-Node, no new Hex dep — D-11). No legitimacy gate needed. |
</threat_model>

<verification>
- `cd mailglass_admin && mix compile --warnings-as-errors` clean (card/1 compiles)
- `cd mailglass_admin && bash scripts/check-conformance.sh` runs; SPACE-GATE validated per Task 3
- No group module or live view touched in this plan (scope fence D-12)
</verification>

<success_criteria>
- `Components.card/1` exists as a thin ≤20-line shell (one `:padding` attr, `:global` rest, one slot)
- SPACE-GATE + GROUP-GATE file-scoped to the 8 `GROUP_SURFACES`; `card` in PRIMITIVE-DRIFT loop
- SPACE-GATE proven (by running) to catch a planted `p-6` and to stay green on the guard set
- Working tree clean (no planted validation tokens)
</success_criteria>

<output>
Create `.planning/phases/114-component-groups/114-01-SUMMARY.md` when done
</output>
