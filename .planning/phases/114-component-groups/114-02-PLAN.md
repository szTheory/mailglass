---
phase: 114-component-groups
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/test/mailglass_admin/operator/shell_test.exs
autonomous: true
requirements: [GROUP-01]
must_haves:
  truths:
    - "Three composed-group gallery specimens render in gallery_live.ex with stable data-testid (gallery-composed-*)"
    - "Each specimen wraps the SAME group-assembling component functions the live views call (no hand-copied tree)"
    - "The composed-group root carries data-region; each group's outer shell carries data-group-card"
    - "The production operator/inbound detail columns carry data-region + group testids, bound to the specimen by a live-view smoke assertion"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      provides: "3 composed-group specimens + dispatcher entries"
      contains: "gallery-composed-support-triage"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "data-region on the detail-column wrapper"
      contains: "data-region"
    - path: "mailglass_admin/lib/mailglass_admin/inbound_live.ex"
      provides: "data-region on the detail-column wrapper"
      contains: "data-region"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/operator/support_cards.ex"
      via: "composed specimen calls SupportCards.support_cards (same fn the live view calls)"
      pattern: "SupportCards.support_cards"
    - from: "mailglass_admin/test/mailglass_admin/operator/shell_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      via: "smoke assertion that production detail column renders data-region + group testids"
      pattern: "data-region"
---

<objective>
Stand up the render-time proof targets: three composed-group gallery specimens in `gallery_live.ex`
(assembling the groups exactly as the live views do), the `data-region` / `data-group-card` test
instrumentation, and one live-view smoke assertion binding specimen↔reality. This gives plans 03
(sweep) and 04 (Floki/Playwright proofs) a stable, data-free tree to assert against.

Purpose: D-10 (composed-group specimens that call the SAME group-assembling functions the live views
call — closes the "lab renders in isolation but never as the composed group" gap; bound to reality by
one smoke assertion) + the `data-region`/`data-group-card` attribute placement (RESEARCH Open Question 1).

Output: three `gallery-composed-*` specimens + dispatcher entries; `data-region` on both live-view
detail-column wrappers; one live-view smoke ExUnit assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-component-groups/114-CONTEXT.md
@.planning/phases/114-component-groups/114-RESEARCH.md
@.planning/phases/114-component-groups/114-PATTERNS.md
@.planning/phases/114-component-groups/114-UI-SPEC.md

# Analogs to copy from (read before editing):
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/inbound_live.ex
@mailglass_admin/test/mailglass_admin/operator/shell_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-region to the operator + inbound detail-column wrappers</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex</files>
  <read_first>
    - operator_live.ex:562-594 (the `motion-reveal space-y-4` detail-column wrapper at :564 + group assembly)
    - inbound_live.ex:474-501 (identical-shape wrapper at :476 + group assembly)
    - 114-PATTERNS.md "operator_live.ex / inbound_live.ex" section
  </read_first>
  <action>
    Add the bare `data-region` attribute to the `class="motion-reveal space-y-4"` detail-column
    wrapper `<div>` in BOTH live views per D-10/D-12 (operator_live.ex:564, inbound_live.ex:476).
    This scopes the Floki ancestor-depth assertion (plan 04). Do NOT change `motion-reveal` or
    `space-y-4` — the `space-y-4` inter-card rhythm is the one live-view-owned token (D-12) and is
    NOT swept. Do NOT touch the group assembly calls or any other attribute. This is a one-line
    addition per file.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c "data-region" lib/mailglass_admin/operator_live.ex lib/mailglass_admin/inbound_live.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `data-region` present on the `motion-reveal space-y-4` wrapper in both live views
    - `motion-reveal` and `space-y-4` unchanged
    - `mix compile --warnings-as-errors` clean
  </acceptance_criteria>
  <done>Both production detail columns carry data-region for the depth proof to scope against.</done>
</task>

<task type="auto">
  <name>Task 2: Add three composed-group gallery specimens with data-region + data-group-card</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex</files>
  <read_first>
    - gallery_live.ex:1-160 (specimen dispatcher + testid convention `gallery-{component}-{state}` at :109)
    - gallery_live.ex:332-382 (existing per-component group specimen branches — the dispatcher shape to mirror)
    - operator_live.ex:562-594 + inbound_live.ex:474-501 (the EXACT composition order the specimens must reproduce by calling the same fns)
    - 114-PATTERNS.md "gallery_live.ex" section + RESEARCH Pitfall 5
  </read_first>
  <action>
    Add three composed-group specimen branches to the `render_specimen/1` dispatcher (mirror the
    existing branch shape at gallery_live.ex:332-382), plus their dispatcher/registry entries, per
    D-10. Each specimen wraps a `<div data-region class="space-y-4">` (matching the live views'
    wrapper) and calls the SAME group-assembling component functions the live views call — NEVER a
    hand-copied HEEx tree (Pitfall 5):
      - `gallery-composed-support-triage`: DetailHeader.detail_header + SupportCards.support_cards +
        OperatorTimeline.timeline + SuppressionCard.suppression_card (operator detail order).
      - `gallery-composed-routing-evidence`: RoutingTrace.routing_trace + EvidenceCard.evidence_card
        (inbound routing+evidence group).
      - `gallery-composed-detail-timeline`: inbound DetailHeader.detail_header + Timeline.timeline.
    Use stable `data-testid` values exactly `gallery-composed-support-triage`,
    `gallery-composed-routing-evidence`, `gallery-composed-detail-timeline` (the `gallery-{component}-{state}`
    convention, gallery_live.ex:109). Pass data-free / minimal assigns (these are structural specimens).
    Each group component is rendered through its `<.card>` shell carrying `data-group-card` — but
    since plan 03 owns the shell swap inside the group modules, here the specimen simply calls the
    group fns; the `data-group-card` lands on each group's outer shell once plan 03 swaps in `<.card>`.
    Per RESEARCH, these composed specimens are a DIFFERENT shape than per-primitive specimens — give
    them their own dispatcher branch; do NOT force the per-primitive `awk` dispatcher assertion onto them.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c "gallery-composed-" lib/mailglass_admin/gallery_live.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Three specimens exist with testids `gallery-composed-support-triage` / `-routing-evidence` / `-detail-timeline`
    - Each wraps `<div data-region class="space-y-4">` and calls the real group-assembling fns (no copied tree)
    - Composition order matches the corresponding live-view detail column
    - `mix compile --warnings-as-errors` clean
  </acceptance_criteria>
  <done>Three data-free composed-group specimens render at /dev/mail/gallery for the proofs to target.</done>
</task>

<task type="auto">
  <name>Task 3: Add the live-view smoke assertion binding specimen to reality</name>
  <files>mailglass_admin/test/mailglass_admin/operator/shell_test.exs</files>
  <read_first>
    - shell_test.exs:137-157 (Floki precedent; existing live-view render assertion shape in this file)
    - operator_live.ex:562-594 (the production detail column to assert against)
    - 114-RESEARCH.md Pitfall 5 + 114-VALIDATION.md Wave 0 item "one live-view smoke assertion"
  </read_first>
  <action>
    Add ONE live-view smoke ExUnit assertion (in `shell_test.exs`, or a sibling test file in the same
    dir if cleaner) that renders the production operator detail column and asserts it contains
    `data-region` AND the group testids (`operator-support-cards` / `operator-detail-header` /
    `operator-timeline` / `operator-suppression-card` — use whatever testids those group modules emit
    today, confirmed by reading the modules). This binds the gallery specimen structure to the real
    composition (D-10), so a specimen that drifts from production fails. Keep it to a single focused
    assertion; do NOT weaken or duplicate existing shell tests. Scope to a render that selects a
    delivery so the detail column is present.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - One smoke assertion verifies the production operator detail column renders `data-region` + the group testids
    - `mix test test/mailglass_admin/operator/shell_test.exs` passes
    - No existing shell test weakened
  </acceptance_criteria>
  <done>Specimen↔reality binding asserted; a drifted specimen would now fail the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds gallery specimens (dev-only route), a bare `data-region`/`data-group-card` test attribute, and a test. No new route, input, auth surface, or destructive action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03 | Information Disclosure | data-region / data-group-card / data-testid | mitigate | Attributes are structural test instrumentation carrying no PII; specimens are data-free / minimal-assign. |
| T-114-04 | Tampering | scope fence (D-12) | mitigate | Only gallery_live.ex + the two live-view detail-column wrappers + one operator test touched; no nav/auth/preview/list changes. |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero-Node, no new Hex dep — D-11). |
</threat_model>

<verification>
- `cd mailglass_admin && mix compile --warnings-as-errors` clean
- `cd mailglass_admin && mix test test/mailglass_admin/operator/shell_test.exs` passes
- Three `gallery-composed-*` testids present; `data-region` on both live-view wrappers
- Scope fence (D-12) holds — no list/overview/nav/preview file touched
</verification>

<success_criteria>
- Three composed-group specimens render with stable testids, each calling the real group-assembling fns
- `data-region` on both production detail-column wrappers; `data-group-card` flows onto each group shell (via plan 03's swap)
- One live-view smoke assertion binds specimen structure to production composition
</success_criteria>

<output>
Create `.planning/phases/114-component-groups/114-02-SUMMARY.md` when done
</output>
