---
phase: 114-component-groups
plan: 04
type: execute
wave: 3
depends_on: ["114-02", "114-03"]
files_modified:
  - mailglass_admin/test/mailglass_admin/group_nesting_test.exs
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03]
must_haves:
  truths:
    - "A Floki ExUnit ancestor-count test proves each composed group nests ≤2 elevation surfaces within its data-region"
    - "A Playwright Group: block proves [data-group-card] DIRECT siblings share a left edge (±1px) at 320 and 1280"
    - "A Playwright padding-floor leg proves no flush-to-container edge (rendered padding ≥ semantic token)"
    - "No horizontal overflow at 320 or 1280 for the composed groups"
  artifacts:
    - path: "mailglass_admin/test/mailglass_admin/group_nesting_test.exs"
      provides: "Floki top-down ancestor-depth proof (GROUP-02)"
      contains: "max_elevation_depth"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Group: describe block — sibling-x + padding-floor + overflow (GROUP-01/03)"
      contains: "data-group-card"
  key_links:
    - from: "mailglass_admin/test/mailglass_admin/group_nesting_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      via: "render_component on the composed-group specimen fns, scoped to data-region"
      pattern: "data-region"
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      via: "getByTestId(gallery-composed-*) then :scope > [data-region] > [data-group-card]"
      pattern: "gallery-composed-"
---

<objective>
Author the two authoritative render-time proofs against the composed-group specimens (plan 02) now
that the box-prison fix and elevation rule (plan 03) are in place: a Floki ExUnit ancestor-depth test
(GROUP-02 depth ≤2) and a Playwright `Group:` geometry block (GROUP-03 sibling-x alignment + GROUP-01
padding-floor / no-flush + no-overflow at 320 and 1280).

Purpose: D-07 (Floki top-down ancestor-count is the depth authority — grep is only a tripwire; Floki
0.38.4 has no parent API so recurse over `{tag, attrs, children}`) + D-08 (Playwright `[data-group-card]`
DIRECT-SIBLING-ONLY x-equality, never a descendant sweep) + D-09 (computed padding-floor covers
"next action obvious" coherent spacing + "no flush-to-edge"). All proofs reuse the locked Phase 113
substrate (D-11) — zero new dependency, zero-Node.

Output: `group_nesting_test.exs`; the `Group:` describe block in `structural.spec.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-component-groups/114-CONTEXT.md
@.planning/phases/114-component-groups/114-RESEARCH.md
@.planning/phases/114-component-groups/114-PATTERNS.md
@.planning/phases/114-component-groups/114-UI-SPEC.md

# Substrate analogs to copy from (read before editing):
@mailglass_admin/test/mailglass_admin/operator/shell_test.exs
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Floki ancestor-depth ExUnit proof (GROUP-02 depth ≤2)</name>
  <files>mailglass_admin/test/mailglass_admin/group_nesting_test.exs</files>
  <read_first>
    - operator/shell_test.exs:137-157 (Floki parse_fragment/find/attribute/text precedent — the API in use)
    - 114-RESEARCH.md "Code Examples — Floki ancestor-depth proof (D-07)" (the full top-down recursion + the elevation-class decision)
    - gallery_live.ex composed specimen fns added in plan 02 (the render_component targets) + the `data-region` placement
  </read_first>
  <behavior>
    - Rendering each composed-group specimen, max_elevation_depth within its data-region is ≤2
    - A node counts as ONE elevation step if it carries ANY of bg-base-200 | bg-base-100 | shadow-raised (so a node with both bg-base-200 and shadow-raised is depth 1, not 2)
    - support-cards composed group (post box-prison fix) is depth 2: section(raised) -> inset(bg-base-100)
    - The check recurses top-down over {tag, attrs, children} (no Floki.parent — it does not exist in 0.38.4)
  </behavior>
  <action>
    Create `test/mailglass_admin/group_nesting_test.exs` implementing the Floki top-down ancestor-depth
    proof per D-07 (full pattern in RESEARCH "Code Examples"). Define `@elevation_classes
    ~w(bg-base-200 bg-base-100 shadow-raised)`. `max_elevation_depth/1`: `Floki.parse_fragment/1`,
    `Floki.find("[data-region]")`, then recurse `deepest_chain/1` over each `{tag, attrs, children}`
    tuple, incrementing by 1 when a node carries ANY elevation class (count once per node, NOT once per
    class — so a node with both `bg-base-200` and `shadow-raised` is depth 1). Floki 0.38.4 has NO
    parent API (Pitfall 3) — count top-down, never reach for `Floki.parent`.

    Add one assertion per composed group: `render_component(&MailglassAdmin.GalleryLive.<composed_fn>/1,
    %{})` (the specimen fns from plan 02) and `assert max_elevation_depth(html) <= 2`. Cover all three
    groups (support-triage, routing-evidence, detail-timeline). This is the AUTHORITATIVE depth proof;
    the GROUP-GATE grep (plan 01) is only a tripwire.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/group_nesting_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `group_nesting_test.exs` exists; `max_elevation_depth/1` recurses top-down over `{tag, attrs, children}` (no `Floki.parent`)
    - A node counts once per node for ANY of `bg-base-200 | bg-base-100 | shadow-raised`
    - All three composed groups assert depth ≤2 and PASS
    - `mix test test/mailglass_admin/group_nesting_test.exs` green
  </acceptance_criteria>
  <done>Depth ≤2 is proven authoritatively in the fast mix-test lane for all three composed groups.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright Group: geometry block (sibling-x + padding-floor + overflow at 320/1280)</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - structural.spec.js:19-23 (PRIMITIVE_VIEWPORTS = [320, 768, 1280]), :47-56 (heading-settle idiom), :302-307 (Math.round), :496-501 (assertNoElementHorizontalOverflow), :857-861 (boundingBox x/width)
    - 114-RESEARCH.md "Code Examples — Scoped Playwright sibling-x equality (D-08/D-09)" (the `:scope > [data-region] > [data-group-card]` direct-sibling selector)
    - 114-PATTERNS.md "e2e/structural.spec.js" section + Pitfall 4 (geometry flake containment)
  </read_first>
  <action>
    Add a `Group:` describe block to `structural.spec.js` reusing the existing geometry substrate per
    D-08/D-09 (full pattern in RESEARCH "Code Examples"). For each of the three composed-group testids
    (`gallery-composed-support-triage`, `-routing-evidence`, `-detail-timeline`) at viewports 320 and
    1280 (from `PRIMITIVE_VIEWPORTS`):

    (1) `goto("/dev/mail/gallery")`, then SETTLE before measuring with
    `await expect(page.getByRole("heading", { name: "Component Gallery", level: 1 })).toBeVisible();`
    (mirrors :50-52, contains the geometry flake — Pitfall 4).

    (2) Sibling-x equality (D-08): locate cards via `region.locator(":scope > [data-region] >
    [data-group-card]")` — DIRECT SIBLINGS ONLY, never a descendant sweep (timeline rail / border-l-4
    children false-fail otherwise). `Math.round(box.x)` each, assert `Math.abs(x - minX) <= 1` across
    siblings. Assert count > 1.

    (3) Padding-floor / no-flush (D-09): assert each card's rendered left/right padding ≥ the semantic
    token floor (covers GROUP-01 coherent spacing + GROUP-02 "no flush-to-container edge"). Read
    computed padding via `boundingClientRect`/`getComputedStyle` on the card vs its first child, or the
    substrate's existing padding helper if present.

    (4) Overflow (D-08): `await assertNoElementHorizontalOverflow(region, "...@${vp}")` (line 496,
    already tolerates ≤1px) at both 320 and 1280.

    Adjust the intermediate selector to match where plan 02 placed `data-region` (specimen wrapper) and
    plan 03 placed `data-group-card` (each group shell).
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "Group" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - A `Group:` describe block asserts `[data-group-card]` DIRECT-sibling x-equality (±1px, `Math.round`) at 320 and 1280
    - A padding-floor leg asserts rendered padding ≥ semantic token (no flush-to-edge)
    - `assertNoElementHorizontalOverflow` passes at 320 and 1280 for all three composed groups
    - Measurement happens after the gallery heading is visible (flake containment)
    - `npx playwright test e2e/structural.spec.js -g "Group"` green
  </acceptance_criteria>
  <done>Alignment, no-flush, and no-overflow are proven at narrow and wide widths for all three composed groups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds an ExUnit test file and Playwright assertions against dev-only gallery specimens. No new route, input, auth surface, data flow, or destructive action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-08 | Information Disclosure | test assertions | accept | Tests assert on structural attributes (`data-region`, `data-group-card`, geometry) of data-free specimens; no PII in fixtures or assertions. |
| T-114-09 | Tampering | scope fence (D-12) | mitigate | Only the new Floki test + structural.spec.js Group block touched; no production code, nav/auth/preview, or lists changed. |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Floki + Playwright already present; zero new dep — D-11). |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/group_nesting_test.exs` green (depth ≤2 for all 3 groups)
- `cd mailglass_admin && npx playwright test e2e/structural.spec.js -g "Group"` green (sibling-x + padding-floor + overflow at 320/1280)
- Floki check recurses top-down (no `Floki.parent`); Playwright scoped to DIRECT siblings only
</verification>

<success_criteria>
- Floki ExUnit proof asserts depth ≤2 within each `data-region` for all three composed groups
- Playwright `Group:` block proves direct-sibling x-equality (±1px), padding-floor (no flush), and no overflow at 320 and 1280
- All proofs run on the locked Phase 113 substrate — zero new dependency, zero-Node
</success_criteria>

<output>
Create `.planning/phases/114-component-groups/114-04-SUMMARY.md` when done
</output>
