---
phase: 137-linked-2-0-release-ceremony-milestone-closeout
plan: 03
type: execute
wave: 3
depends_on: ["137-02"]
files_modified:
  - .planning/MILESTONES.md
  - .planning/STATE.md
  - CLAUDE.md
files_created:
  - .planning/milestones/v2.0-ROADMAP.md
  - .planning/milestones/v2.0-REQUIREMENTS.md
  - .planning/milestones/v2.0-MILESTONE-AUDIT.md
autonomous: false
requirements: [REL-03]

must_haves:
  truths:
    - "gsd-audit-milestone runs the 132-136 build against schema-isolation intent and reports status: passed"
    - "The milestone is archived: v2.0-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md exist"
    - "MILESTONES.md and STATE.md reflect the TRUE 132-137 span (not an over-counted leftover-dir figure)"
    - "MEMORY release-state file and CLAUDE.md current-state line read 2.0.0 / 2.0.0 / 2.0.0"
    - "A v2.0 milestone git tag exists"
  artifacts:
    - .planning/milestones/v2.0-ROADMAP.md
    - .planning/milestones/v2.0-REQUIREMENTS.md
    - .planning/milestones/v2.0-MILESTONE-AUDIT.md
    - .planning/MILESTONES.md
    - .planning/STATE.md
    - CLAUDE.md
  key_links:
    - "gsd-audit-milestone must scope to 132-136 (the built phases) vs schema-isolation intent — 137 is the ceremony itself"
    - "manual MILESTONES/STATE correction is load-bearing: gsd-sdk closeout verbs over-count leftover phase dirs (999.x backlog)"
  prohibitions:
    - "Do NOT trust gsd-sdk milestone.complete / phase.complete phase+plan+task counts verbatim — they include leftover 999.x backlog dirs; manually correct to the true 132-137 span (D-09)"
    - "Do NOT archive before the audit reports status: passed"
    - "Do NOT reintroduce an `==` inbound core pin while refreshing docs/state"
---

<objective>
Close out the v2.0 Postgres Schema Isolation milestone (REL-03). Standard GSD closeout with
one mandatory manual correction:

1. **Audit (gsd-audit-milestone):** audit the 132-136 build against the schema-isolation
   intent (default `mailglass` schema + upgrade path). Require `status: passed` before
   archiving.
2. **Complete + cleanup (gsd-complete-milestone -> gsd-cleanup):** archive to
   `milestones/v2.0-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md`; RP already created the three
   `-v2.0.0` tags in Plan 02.
3. **Manual correction (D-09):** gsd-sdk `milestone.complete`/`phase.complete` over-count
   leftover phase dirs (999.x backlog) and can inflate stats / mis-flag last-phase. Manually
   correct MILESTONES.md and STATE.md to the TRUE 132-137 span.
4. **Refresh durable pointers:** update the MEMORY release-state file and the CLAUDE.md
   current-state line to `2.0.0 / 2.0.0 / 2.0.0`.
5. **Tag the milestone:** create the `v2.0` git tag.

Purpose: leave the repo in a clean, accurately-recorded post-2.0 state. Output: three
archived milestone artifacts, corrected MILESTONES/STATE, refreshed MEMORY + CLAUDE.md, and
a `v2.0` tag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@CLAUDE.md
@.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-CONTEXT.md
@.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the milestone (132-136 vs schema-isolation intent) — require status: passed (REL-03, D-09)</name>
  <files>.planning/milestones/v2.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - D-09: gsd-audit-milestone scopes 132-136 vs schema-isolation intent (default `mailglass` schema + first-class upgrade path); 137 is the ceremony, not a built phase to audit
    - MEMORY project_gsd_sdk_plan_phase_finalization: gap-analysis (13e) scans the whole milestone vs one phase dir, so "N not covered" is expected noise
    - MEMORY project_milestone_complete_count_inflation: closeout verbs count ALL .planning/phases dirs (incl leftover 71-73 / 999.x backlog), inflating stats
    - The milestone intent + scope locks are in STATE.md "v2.0 Milestone Intent" / "v2.0 Scope Locks"
  </read_first>
  <action>
    Run the milestone audit (gsd-audit-milestone) over the built phases 132-136 against the
    schema-isolation intent. Confirm the load-bearing correctness proof landed: MIGR-05
    (SQLSTATE 45A01 immutability trigger regression under the non-public `mailglass` schema
    with no `search_path` pin). Confirm REL-01/REL-02 are satisfied by Plan 02's published +
    smoke-verified 2.0.0 state.

    Require the audit to report `status: passed`. If it reports gaps, resolve or explicitly
    document each before archiving (do NOT archive on an unpassed audit). Ignore known
    gsd-sdk noise: gap-analysis scanning the whole milestone dir and any inflated
    phase/plan/task counts from leftover 999.x backlog dirs — those are corrected manually in
    Task 2, not treated as real gaps.

    Write the audit result to .planning/milestones/v2.0-MILESTONE-AUDIT.md.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v2.0-MILESTONE-AUDIT.md && grep -qi 'status.*passed' .planning/milestones/v2.0-MILESTONE-AUDIT.md && echo AUDIT_PASSED</automated>
  </verify>
  <acceptance_criteria>
    v2.0-MILESTONE-AUDIT.md exists and reports `status: passed`; MIGR-05 load-bearing proof
    and REL-01/02 confirmed; gsd-sdk over-count/gap-scan noise explicitly discounted.
  </acceptance_criteria>
  <done>
    Milestone audit written with `status: passed`; the schema-isolation intent is verified
    against the 132-136 build with the correctness keystone confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Archive + manually correct MILESTONES/STATE to the true 132-137 span (REL-03, D-09)</name>
  <files>.planning/milestones/v2.0-ROADMAP.md, .planning/milestones/v2.0-REQUIREMENTS.md, .planning/MILESTONES.md, .planning/STATE.md</files>
  <read_first>
    - D-09: after gsd-complete-milestone -> gsd-cleanup, MANUALLY correct MILESTONES.md/STATE.md to the true 132-137 span
    - MEMORY project_milestone_complete_count_inflation: milestone.complete counts ALL phase dirs (incl leftover 71-73 / 999.x backlog), inflating phase/plan/task stats + pulling in non-milestone accomplishments; manually correct to true scope
    - MEMORY project_phase_complete_false_last_phase: phase.complete scans phase dirs not ROADMAP text; sanity-check is_last_phase (137 IS the true last phase of v2.0)
    - MEMORY project_gsd_sdk_commit_sweeps_planning: `gsd-sdk query commit` stages ALL dirty .planning/ paths — `git show --stat HEAD` after and split out unrelated WIP
    - True milestone span: 6 phases (132-137), 12 completed plans across 132-136 + this phase's plans; do NOT include 999.x backlog
  </read_first>
  <action>
    Run gsd-complete-milestone then gsd-cleanup to archive the milestone into
    .planning/milestones/v2.0-ROADMAP.md + v2.0-REQUIREMENTS.md (the audit already wrote
    v2.0-MILESTONE-AUDIT.md in Task 1).

    Then MANUALLY correct MILESTONES.md and STATE.md so the recorded scope is exactly the
    132-137 span. gsd-sdk closeout verbs over-count leftover phase dirs (999.x backlog) —
    verify and fix: total_phases = 6 (132-137), completed count reflects the real 132-137
    work only, and no non-v2.0 accomplishment is pulled in. Sanity-check that 137 is flagged
    as the true last phase of v2.0 (not falsely mis-flagged).

    After any `gsd-sdk query commit`, run `git show --stat HEAD` and split out any unrelated
    dirty .planning/ WIP that got swept in (soft-reset + restore as needed).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v2.0-ROADMAP.md && test -f .planning/milestones/v2.0-REQUIREMENTS.md && grep -q 'v2.0' .planning/MILESTONES.md && echo ARCHIVE_OK</automated>
  </verify>
  <acceptance_criteria>
    v2.0-ROADMAP.md + v2.0-REQUIREMENTS.md archived; MILESTONES.md records v2.0 with the true
    132-137 span (6 phases, no 999.x inflation); STATE.md reflects milestone-complete with
    correct counts; no unrelated .planning/ WIP swept into closeout commits.
  </acceptance_criteria>
  <done>
    Milestone archived to milestones/v2.0-*.md; MILESTONES/STATE corrected to the true
    132-137 span with over-count noise removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh MEMORY release-state + CLAUDE.md current-state to 2.0.0/2.0.0/2.0.0 (D-09)</name>
  <files>CLAUDE.md, (MEMORY release-state file under the project memory dir)</files>
  <read_first>
    - D-09: refresh the MEMORY release-state file and the CLAUDE.md current-state line to 2.0.0 / 2.0.0 / 2.0.0
    - CLAUDE.md "Current state (as of ...)" line currently names 1.6.2/1.6.2/1.3.1 (stale) — but STATE.md is the live source of truth; update the CLAUDE.md line to 2.0.0/2.0.0/2.0.0 and note STATE.md is authoritative
    - MEMORY convention: create a new project_v2_0_release_state file (mirroring prior project_v1_15_release_state) capturing the durable gotchas actually hit this cut (racing fan-outs, CI-the-body catch, manifest fallback if used, schema-adoption baseline advance)
    - 137-02-SUMMARY.md: the actual gotchas encountered this cut
  </read_first>
  <action>
    Update the CLAUDE.md current-state line to reflect live versions `mailglass` 2.0.0 /
    `mailglass_admin` 2.0.0 / `mailglass_inbound` 2.0.0, and that v2.0 Postgres Schema
    Isolation shipped. Keep the existing note that STATE.md is the live source of truth for
    milestone/phase status.

    Create/refresh the MEMORY project release-state file (e.g. project_v2_0_release_state)
    capturing: current live 2.0.0/2.0.0/2.0.0; that this was the first linked MAJOR cut on
    the loosened-`~>` pins (inbound core pin `~> 2.0`, floor dropped, NO `==` re-pin — the
    v1.15 keystone held); the reference-baseline schema-adoption advance (D-07 dogfoods the
    mailglass default); and whichever durable gotchas actually fired this cut per
    137-02-SUMMARY.md (racing fan-out red-but-published, CI-the-body catch, manifest fallback
    if used).

    Do NOT reintroduce any `==` inbound core pin language as a "recommendation" in the docs —
    the loosened `~>` is the standing keystone.
  </action>
  <verify>
    <automated>grep -q '2\.0\.0' CLAUDE.md && echo CLAUDE_MD_REFRESHED</automated>
  </verify>
  <acceptance_criteria>
    CLAUDE.md current-state line reads 2.0.0/2.0.0/2.0.0; a MEMORY release-state file records
    the 2.0 cut with the durable gotchas and the loosened-`~>` keystone intact.
  </acceptance_criteria>
  <done>
    CLAUDE.md + MEMORY release-state refreshed to 2.0.0/2.0.0/2.0.0 with this cut's durable
    lessons captured.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Tag the v2.0 milestone (REL-03)</name>
  <files>(no file change — git tag creation checkpoint)</files>
  <action>
    This is a blocking tag checkpoint. Confirm the three per-package `-v2.0.0` tags exist,
    then create + push the milestone-level `v2.0` git tag. Follow the how-to-verify steps.
  </action>
  <what-built>
    The milestone is audited (status: passed), archived to milestones/v2.0-*.md,
    MILESTONES/STATE corrected to the true 132-137 span, and CLAUDE.md + MEMORY refreshed to
    2.0.0/2.0.0/2.0.0. RP already created the three per-package `-v2.0.0` tags in Plan 02.
    The remaining artifact is the milestone-level `v2.0` git tag.
  </what-built>
  <how-to-verify>
    1. Confirm the three per-package release tags from Plan 02 exist:
       `git tag --list 'mailglass-v2.0.0' 'mailglass_admin-v2.0.0' 'mailglass_inbound-v2.0.0'`
       (all three should print).
    2. Create the milestone tag on the closeout commit:
       `git tag -a v2.0 -m "v2.0 Postgres Schema Isolation"` and push it:
       `git push origin v2.0`.
    3. Confirm: `git tag --list 'v2.0'` prints `v2.0`, and
       `git ls-remote --tags origin v2.0` shows it on the remote.
  </how-to-verify>
  <resume-signal>Type "v2.0 tagged + pushed" once the milestone tag exists locally and on the remote, or describe any tag conflict.</resume-signal>
  <verify>`git tag --list 'v2.0'` prints v2.0 and `git ls-remote --tags origin v2.0` shows it on the remote; three `-v2.0.0` package tags confirmed present.</verify>
  <done>`v2.0` milestone tag created + pushed; three per-package `-v2.0.0` tags confirmed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsd-sdk closeout output -> recorded milestone stats | verb counts include leftover 999.x dirs; not authoritative |
| gsd-sdk query commit -> git index | sweeps ALL dirty .planning/ paths, not just the closeout artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-09 | Tampering | MILESTONES/STATE counts | medium | mitigate | Manually correct to the true 132-137 span; discount 999.x backlog inflation (D-09) |
| T-137-10 | Tampering | closeout commit contents | medium | mitigate | `git show --stat HEAD` after each `gsd-sdk query commit`; split out unrelated .planning/ WIP |
| T-137-11 | Repudiation | premature archive | medium | mitigate | Require audit `status: passed` before gsd-complete-milestone |
| T-137-12 | Spoofing | stale current-state docs | low | mitigate | Refresh CLAUDE.md + MEMORY to 2.0.0/2.0.0/2.0.0; keep STATE.md as authoritative pointer |
</threat_model>

<verification>
- gsd-audit-milestone reports `status: passed` (v2.0-MILESTONE-AUDIT.md)
- v2.0-ROADMAP.md + v2.0-REQUIREMENTS.md archived
- MILESTONES.md + STATE.md reflect the true 132-137 span (no 999.x inflation)
- CLAUDE.md + MEMORY release-state read 2.0.0/2.0.0/2.0.0
- `v2.0` git tag exists locally and on the remote; three `-v2.0.0` package tags confirmed present

**Nyquist note (Dimension 8):** milestone-closeout/ops phase — no new library behavior, no
new ExUnit surface to sample. Verification is operational (audit status, archive artifacts,
corrected stats, tag existence). Fabricating unit tests here would be noise — explicitly N/A.
</verification>

<success_criteria>
- Milestone audit `status: passed` over 132-136 vs schema-isolation intent
- Milestone archived (v2.0-ROADMAP/REQUIREMENTS/MILESTONE-AUDIT.md)
- MILESTONES/STATE manually corrected to the true 132-137 span
- CLAUDE.md + MEMORY release-state refreshed to 2.0.0/2.0.0/2.0.0 (loosened-`~>` keystone intact)
- `v2.0` milestone tag created + pushed; three `-v2.0.0` package tags confirmed
</success_criteria>

<output>
Create `.planning/phases/137-linked-2-0-release-ceremony-milestone-closeout/137-03-SUMMARY.md` when done.
Record: the audit status; any gsd-sdk over-count corrected (before/after phase+plan+task
figures); which .planning/ WIP was split out of closeout commits; and confirmation the v2.0
tag + three package tags are present.
</output>
