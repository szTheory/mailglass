---
phase: 99-inbound-surface
plan: 04
type: execute
wave: 3
depends_on: ["99-02", "99-03"]
files_modified:
  - "mailglass_admin/test/support/operator_fixtures.ex"
  - "mailglass_admin/e2e/operator.spec.js"
  - "mailglass_admin/e2e/structural.spec.js"
  - "mailglass_admin/priv/static/app.css"
autonomous: true
requirements: [GROUP-02, GROUP-03]
must_haves:
  truths:
    - "D-11: The single browser seed path reaches accept, no_match, reject, bounce, failed, no-run, missing-evidence, suppression-flagged, filtered-empty, truly-empty/no-tenant, long-content, and detail-error inbound states by URL."
    - "D-12: The existing operator browser gate validates overview, responsive grid, mobile back, routing trace, evidence redaction, empty states, and the why-did-inbound-not-route JTBD."
    - "The browser gate keeps existing operator row-index expectations stable."
  artifacts:
    - path: "mailglass_admin/test/support/operator_fixtures.ex"
      provides: "Single seeded inbound state matrix"
    - path: "mailglass_admin/e2e/operator.spec.js"
      provides: "End-to-end why-did-inbound-not-route flow"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Responsive/a11y/empty-state structural assertions"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Compiled CSS consumed by browser gate"
  key_links:
    - from: "mailglass_admin/e2e/operator.spec.js"
      to: "mailglass_admin/test/support/operator_fixtures.ex"
      via: "/ops/browser-reset seed"
      pattern: "tenantId = \"browser-tenant\""
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "inbound-master-detail"
      via: "computed grid-template-columns"
      pattern: "inbound-master-detail"
---

<objective>
Make inbound states and JTBD flow reachable in the existing browser gate.

Purpose: Phase 99 is not complete until the user journey "why did inbound not route?" is proven in the same seeded, repeatable browser harness that Phase 98 established for Operator.
Output: Expanded single seed dataset, inbound browser flow assertions, responsive/a11y structural assertions, and rebuilt CSS for the current UI classes.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/RATCHET-GAP-REGISTER.md
@.planning/phases/99-inbound-surface/99-CONTEXT.md
@.planning/phases/99-inbound-surface/99-RESEARCH.md
@.planning/phases/99-inbound-surface/99-PATTERNS.md
@.planning/phases/99-inbound-surface/99-UI-SPEC.md
@.planning/phases/98-operator-deliveries-surface/98-04-SUMMARY.md
@mailglass_admin/test/support/operator_fixtures.ex
@mailglass_admin/e2e/operator.spec.js
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/package.json
@mailglass_admin/lib/mailglass_admin/inbound_live.ex
@mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex
@mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the single browser seed with inbound state coverage</name>
  <files>mailglass_admin/test/support/operator_fixtures.ex</files>
  <read_first>
    @mailglass_admin/test/support/operator_fixtures.ex
    @mailglass_admin/e2e/operator.spec.js
    @mailglass_admin/e2e/structural.spec.js
    @.planning/RATCHET-GAP-REGISTER.md
    @.planning/phases/99-inbound-surface/99-CONTEXT.md
    @.planning/phases/99-inbound-surface/99-RESEARCH.md
    @.planning/phases/98-operator-deliveries-surface/98-04-SUMMARY.md
  </read_first>
  <behavior>
    - Existing delivery row index expectations in `operator.spec.js` remain valid.
    - One reset through `/ops/browser-reset` creates all inbound states required by D-11.
    - Seeded no-match record can render a routing trace with failing clauses.
    - Seeded missing-evidence/no-run states can render no evidence and no timeline-run branches.
  </behavior>
  <action>Extend `OperatorFixtures.seed_browser_scenario!/0`; do not add a second seed script or browser endpoint. Preserve existing delivery insertion order and recipient constants used by `operator.spec.js`. Replace the single inbound-only seed block with a compact inbound seed matrix under tenant `@tenant_id`: an `:accept` record, a `:no_match` record with recipient `nomatch@browser-scenario.example`, subject `general route question`, and empty headers, a `:reject` record with an outcome reason, a `:bounce` record with an outcome reason, a `:failed` record with failure metadata, a record with no execution runs, a record with no stored evidence, a `suppression_flagged: true` record, and a long-subject/text-body record. Add or update private helpers so `insert_inbound_run!` accepts `outcome:`, `mailbox:`, `outcome_reason:`, `failure:`, and `source:` options while defaulting to the current accept behavior. Keep all raw SQL tenant values at `@tenant_id`. Use received_at values older than delivery rows so existing delivery row-index tests remain stable.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `operator_fixtures.ex` still contains `def seed_browser_scenario!`.
    - `operator_fixtures.ex` contains the strings `nomatch@browser-scenario.example`, `general route question`, `suppression_flagged: true`, `reject`, `bounce`, and `failed`.
    - `operator_fixtures.ex` has exactly one `def seed_browser_scenario!`.
    - `operator_fixtures.ex` contains no new public seed function for inbound states.
    - The automated operator LiveView test command exits 0.
  </acceptance_criteria>
  <done>The existing browser seed reaches all required inbound states without shifting operator delivery row assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the why-did-inbound-not-route browser flow</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    @mailglass_admin/e2e/operator.spec.js
    @mailglass_admin/test/support/operator_fixtures.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex
    @mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
    @.planning/phases/99-inbound-surface/99-CONTEXT.md
    @.planning/phases/99-inbound-surface/99-UI-SPEC.md
  </read_first>
  <behavior>
    - Browser user can navigate to `/ops/mail/inbound?tenant_id=browser-tenant`.
    - Overview is visible before selecting a row.
    - Selecting the seeded no-match row exposes routing trace and redacted evidence.
    - Raw provider source remains absent until reveal.
    - At 390px, selecting a row hides the list and shows `inbound-detail-back`.
  </behavior>
  <action>Add an inbound-specific test to `operator.spec.js` inside the existing `operator browser gate` describe block. Reuse `openOperator(page)` and `tenantId`. Navigate to `/ops/mail/inbound?tenant_id=${tenantId}`. Assert `inbound-overview` is visible and contains `No-match rate`. Locate the seeded no-match row by visible text `No match` plus `nomatch` masked or by a stable row containing the no-match subject; click it. Assert the URL contains `inbound_id=`, `inbound-routing-trace` is visible, `inbound-trace-clause` count is greater than 0, `inbound-evidence-card` is visible, `inbound-evidence-redacted` is visible, and `inbound-evidence-raw` has count 0 before reveal. Add a 390px pass in the same or a separate test: set viewport 390x844, open inbound, select a row, assert `inbound-records-list-card` is hidden and `inbound-detail-back` is visible, then click back and assert the list card is visible again.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build &amp;&amp; npx playwright test --config=playwright.config.cjs --workers=1 e2e/operator.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `operator.spec.js` contains a test title with `why-did-inbound-not-route`.
    - `operator.spec.js` contains `inbound-overview`, `inbound-routing-trace`, `inbound-evidence-redacted`, and `inbound-detail-back`.
    - `operator.spec.js` asserts `inbound-evidence-raw` count is 0 before reveal.
    - The automated Playwright operator spec command exits 0.
  </acceptance_criteria>
  <done>The real inbound no-match investigation flow is browser-verified from seeded data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend structural responsive and state assertions for inbound</name>
  <files>mailglass_admin/e2e/structural.spec.js, mailglass_admin/priv/static/app.css</files>
  <read_first>
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/e2e/operator.spec.js
    @mailglass_admin/test/support/operator_fixtures.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
    @.planning/phases/99-inbound-surface/99-CONTEXT.md
    @.planning/phases/99-inbound-surface/99-VALIDATION.md
    @.planning/phases/98-operator-deliveries-surface/98-04-SUMMARY.md
  </read_first>
  <behavior>
    - Inbound exposes exactly one `h1`.
    - Inbound master/detail grid ratio is approximately 40/60 at 768px and 33/67 at 1440px.
    - At 390px, the list fills the grid before selection and hides after selection with mobile back visible.
    - Inbound overview, routing trace, evidence redaction, and empty-state variants are structurally asserted.
    - Browser gate runs through the existing `test:operator-browser` lane.
  </behavior>
  <action>Extend `structural.spec.js`, reusing existing `openInbound(page)`, `parseGridColumns`, and `expectRatio`. Add inbound tests for exactly one h1, `inbound-overview` visible, responsive grid ratios at 768 and 1440, 390px full-width list before selection, 390px hidden list and visible `inbound-detail-back` after selection, `inbound-filters-toggle` and `inbound-detail-back` touch target heights at least 44px, routing trace visible/scannable at 390px on a no-match seeded URL, evidence redaction with no raw payload by default, detail-error URL with `inbound_id=does-not-exist`, filtered-empty URL using an impossible search term, truly-empty tenant URL `tenant_id=browser-empty`, and blank-tenant/no-tenant empty state. Run `mix mailglass_admin.assets.build` before browser tests and include the resulting `priv/static/app.css` diff in this plan if the bundle changes.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build &amp;&amp; npx playwright test --config=playwright.config.cjs --workers=1 e2e/operator.spec.js e2e/structural.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `structural.spec.js` contains `Inbound: master-detail grid follows 390/768/1440 responsive contract` or equivalent inbound responsive test title.
    - `structural.spec.js` contains `inbound-overview`, `inbound-filters-toggle`, `inbound-detail-back`, `inbound-routing-trace`, and `inbound-evidence-redacted`.
    - `structural.spec.js` contains a one-h1 assertion for inbound.
    - `structural.spec.js` uses `expectRatio(columns, 0.4)` and `expectRatio(columns, 0.33)` for inbound.
    - The automated two-file Playwright command exits 0.
  </acceptance_criteria>
  <done>Inbound responsive, a11y, empty-state, and no-match investigation structure is covered by the operator browser gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser reset route -&gt; seeded database | Test-only endpoint resets and seeds deterministic data. |
| Playwright browser -&gt; LiveView routes | Browser tests exercise authenticated operator flows with tenant-scoped params. |
| Seeded evidence -&gt; rendered page | Raw payload content in seeded evidence must remain redacted by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-16 | Information Disclosure | seeded raw evidence | mitigate | Browser tests assert `inbound-evidence-raw` is absent before reveal and only redacted placeholder is visible. |
| T-99-17 | Tampering | browser seed helpers | mitigate | Keep seed helpers private, test-only, and under existing `/ops/browser-reset` path; no runtime route or public API added. |
| T-99-18 | Repudiation | browser flow proof | mitigate | Add deterministic Playwright assertions for URL, testids, and seeded states so flow evidence is reproducible in CI. |
| T-99-19 | Denial of Service | expanded seed | accept | Seed runs only in test support and truncates tables before insert; no production code path executes the seed. |
| T-99-20 | Information Disclosure | cross-tenant browser URLs | mitigate | Use tenant-scoped URLs and include blank/empty tenant assertions that no foreign seeded rows appear. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install or new dependency is planned; existing Playwright dependency is already in the project. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix mailglass_admin.assets.build && npx playwright test --config=playwright.config.cjs --workers=1 e2e/operator.spec.js e2e/structural.spec.js`
</verification>

<success_criteria>
All Phase 99 inbound states are reachable from the single browser seed, and the existing operator browser gate validates the inbound overview, routing trace, evidence redaction, responsive behavior, and JTBD flow.
</success_criteria>

<output>
Create `.planning/phases/99-inbound-surface/99-04-SUMMARY.md` when done.
</output>
