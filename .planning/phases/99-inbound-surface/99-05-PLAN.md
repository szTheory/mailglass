---
phase: 99-inbound-surface
plan: 05
type: execute
wave: 4
depends_on: ["99-04"]
files_modified:
  - "mailglass_admin/lib/mailglass_admin/preview_live.ex"
  - "mailglass_admin/scripts/check-conformance-advisory.sh"
  - ".github/workflows/ci.yml"
  - "mailglass_admin/priv/static/app.css"
autonomous: true
requirements: [GROUP-02, GROUP-03]
must_haves:
  truths:
    - "D-13/D-14: All remaining admin raw text-lg/xl/2xl and arbitrary tracking violations are removed before the advisory gate flips fail-closed."
    - "D-14: `check-conformance-advisory.sh` exits nonzero on TYPE-lg/xl or TRACK violations and CI no longer marks the step continue-on-error."
    - "The compiled CSS bundle is rebuilt and committed clean after Phase 99 markup changes."
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance-advisory.sh"
      provides: "Fail-closed TYPE-lg/xl and TRACK conformance gate"
    - path: ".github/workflows/ci.yml"
      provides: "Hard-fail CI wiring for advisory arms"
    - path: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      provides: "Minimal remaining text-xl token cleanup"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt static CSS bundle"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mailglass_admin/scripts/check-conformance-advisory.sh"
      via: "Credo strict CI step"
      pattern: "bash mailglass_admin/scripts/check-conformance-advisory.sh"
    - from: "mailglass_admin/scripts/check-conformance-advisory.sh"
      to: "mailglass_admin/lib"
      via: "BASH_SOURCE-anchored LIB scan"
      pattern: "errors=0"
---

<objective>
Flip the remaining typography/tracking advisory gate to hard-fail and leave the admin CSS bundle clean.

Purpose: Phase 99 owns the global cleanup after Operator and Inbound have been token-cleaned. The gate must now prevent regressions instead of logging warnings.
Output: Minimal preview token cleanup, fail-closed advisory script, CI hard-fail step, rebuilt `priv/static/app.css`, and full admin/browser verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-inbound-surface/99-CONTEXT.md
@.planning/phases/99-inbound-surface/99-RESEARCH.md
@.planning/phases/99-inbound-surface/99-PATTERNS.md
@.planning/phases/99-inbound-surface/99-VALIDATION.md
@mailglass_admin/lib/mailglass_admin/preview_live.ex
@mailglass_admin/scripts/check-conformance-advisory.sh
@mailglass_admin/scripts/check-conformance.sh
@.github/workflows/ci.yml
@mailglass_admin/mix.exs
@mailglass_admin/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the final Preview text-xl violation only</name>
  <files>mailglass_admin/lib/mailglass_admin/preview_live.ex</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/preview_live.ex
    @.planning/phases/99-inbound-surface/99-CONTEXT.md
    @.planning/phases/99-inbound-surface/99-RESEARCH.md
    @.planning/phases/99-inbound-surface/99-UI-SPEC.md
  </read_first>
  <action>Perform the minimal D-14 cleanup in `PreviewLive`: replace the selected-scenario `h1` class token `text-xl` with `text-heading`. Do not change Preview IA, dark-mode behavior, route structure, sidebar, tabs, CTA copy, or any Phase 100-owned behavior. Leave `tracking-tight` untouched because the hardened Phase 99 gate only blocks arbitrary `tracking-[...]` utilities, not named Tailwind tracking classes.</action>
  <verify>
    <automated>bash -lc "! rg -n 'text-(lg|xl|2xl|3xl|4xl|5xl)\\b' mailglass_admin/lib/mailglass_admin/preview_live.ex"</automated>
  </verify>
  <acceptance_criteria>
    - `preview_live.ex` contains no `text-xl`.
    - `preview_live.ex` contains `text-heading font-bold text-base-content` in the selected-scenario `h1`.
    - `preview_live.ex` still contains `data-theme={if @dark_chrome, do: "mailglass-dark", else: "mailglass-light"}`.
    - No file under `mailglass_admin/lib/mailglass_admin/preview/` is modified by this task.
  </acceptance_criteria>
  <done>The only non-inbound type cleanup allowed by D-14 is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip advisory conformance arms to fail-closed</name>
  <files>mailglass_admin/scripts/check-conformance-advisory.sh, .github/workflows/ci.yml</files>
  <read_first>
    @mailglass_admin/scripts/check-conformance-advisory.sh
    @mailglass_admin/scripts/check-conformance.sh
    @.github/workflows/ci.yml
    @.planning/phases/99-inbound-surface/99-CONTEXT.md
    @.planning/phases/99-inbound-surface/99-RESEARCH.md
  </read_first>
  <behavior>
    - `check-conformance-advisory.sh` exits 1 when raw `text-lg/xl/2xl/3xl/4xl/5xl` or `tracking-[...]` appears under `mailglass_admin/lib`.
    - `check-conformance-advisory.sh` exits 0 with a clean OK message when no violations remain.
    - CI no longer has `continue-on-error: true` on the `Verify design-system conformance (advisory arms - TYPE-lg/xl + TRACK)` step.
  </behavior>
  <action>Update `check-conformance-advisory.sh` from advisory to hard-fail. Keep the BASH_SOURCE-anchored `SCRIPT_DIR` and `LIB` guard. Add `errors=0` after the lib guard. For the TYPE gate, keep the pattern `text-(lg|xl|2xl|3xl|4xl|5xl)\b`; when grep finds matches, print `FAIL: TYPE-GATE - raw large text-scale utility found (use text-heading/display tokens)` and increment `errors`. For the TRACK gate, keep the pattern `tracking-\[`; when grep finds matches, print `FAIL: TRACK-GATE - arbitrary tracking utility found (use text-label uppercase font-bold text-secondary)` and increment `errors`. Replace stale comments that mention defining `--tracking-eyebrow` with the locked Phase 96 rule that arbitrary tracking is removed. At the end, if `errors > 0`, print `FAIL: advisory design-system conformance violations found (${errors} gate(s) failed)` and exit 1; otherwise print `OK: advisory design-system conformance clean.` and exit 0. In `.github/workflows/ci.yml`, remove `continue-on-error: true` from the check-conformance-advisory step and update its comment to say the Phase 99 gate is hard-fail.</action>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance-advisory.sh</automated>
  </verify>
  <acceptance_criteria>
    - `check-conformance-advisory.sh` contains `errors=0`.
    - `check-conformance-advisory.sh` contains `FAIL: TYPE-GATE`.
    - `check-conformance-advisory.sh` contains `FAIL: TRACK-GATE`.
    - `check-conformance-advisory.sh` contains `OK: advisory design-system conformance clean.`
    - `check-conformance-advisory.sh` does not contain an unconditional clean-path `exit 0` before error-count handling or `define --tracking-eyebrow`.
    - In `.github/workflows/ci.yml`, the `run: bash mailglass_admin/scripts/check-conformance-advisory.sh` step is not preceded by `continue-on-error: true` in the same step block.
    - The automated script command exits 0 after cleanup.
  </acceptance_criteria>
  <done>The former advisory type/tracking arms now fail CI on regression.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild bundle and run final Phase 99 gates</name>
  <files>mailglass_admin/priv/static/app.css</files>
  <read_first>
    @mailglass_admin/priv/static/app.css
    @mailglass_admin/assets/css/app.css
    @mailglass_admin/mix.exs
    @mailglass_admin/package.json
    @.planning/phases/99-inbound-surface/99-VALIDATION.md
  </read_first>
  <action>Run the standalone Tailwind asset build from `mailglass_admin` after all Phase 99 markup and gate changes. Commit the generated `mailglass_admin/priv/static/app.css` diff if it changes. Then run the local final gates in increasing feedback cost order: `bash scripts/check-conformance.sh`, `bash scripts/check-conformance-advisory.sh`, the focused inbound ExUnit precheck, `mix verify.preview`, and `npm run test:operator-browser`. Do not edit `mailglass_admin/assets/css/app.css` or `brandbook/tokens.css` in this phase.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build</automated>
    <automated>cd mailglass_admin &amp;&amp; bash scripts/check-conformance.sh</automated>
    <automated>cd mailglass_admin &amp;&amp; bash scripts/check-conformance-advisory.sh</automated>
    <automated>cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/inbound_live_test.exs test/mailglass_admin/inbound/components_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin &amp;&amp; mix verify.preview</automated>
    <automated>cd mailglass_admin &amp;&amp; npm run test:operator-browser</automated>
    <automated>cd mailglass_admin &amp;&amp; git diff --exit-code priv/static/</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_admin/priv/static/app.css` is rebuilt after Phase 99 class changes.
    - `bash mailglass_admin/scripts/check-conformance-advisory.sh` exits 0.
    - `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs test/mailglass_admin/inbound/components_test.exs --warnings-as-errors` exits 0 before full preview/browser gates.
    - `cd mailglass_admin && mix verify.preview` exits 0.
    - `cd mailglass_admin && npm run test:operator-browser` exits 0.
    - `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after the generated bundle has been committed.
  </acceptance_criteria>
  <done>Phase 99 leaves the static bundle clean and all admin/browser gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree -&gt; CI conformance gate | CI trusts shell scripts to reject off-token source patterns. |
| Tailwind source scan -&gt; generated CSS | Class changes in HEEx influence committed static CSS. |
| Preview minimal cleanup -&gt; Phase 100 boundary | Non-inbound edit must not implement Preview IA or dark-mode scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-21 | Tampering | `check-conformance-advisory.sh` | mitigate | Convert warnings to counted failures; CI runs the script without `continue-on-error`. |
| T-99-22 | Repudiation | generated CSS bundle | mitigate | Require `mix mailglass_admin.assets.build` plus `git diff --exit-code priv/static/` after committing bundle output. |
| T-99-23 | Denial of Service | CI gate runtime | accept | Grep scans are bounded to `mailglass_admin/lib` and run in the existing Credo strict job. |
| T-99-24 | Tampering | Preview Phase 100 boundary | mitigate | Limit Preview edit to `text-xl` -> `text-heading`; acceptance criteria forbid `mailglass_admin/lib/mailglass_admin/preview/` edits. |
| T-99-25 | Information Disclosure | conformance logs | accept | Gate logs file/line/class tokens only; no runtime data, tenant ids, or raw evidence are processed. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install or new dependency is planned; `npm run test:operator-browser` uses existing project dependencies. |
</threat_model>

<verification>
- `bash mailglass_admin/scripts/check-conformance-advisory.sh`
- `cd mailglass_admin && mix test test/mailglass_admin/inbound_live_test.exs test/mailglass_admin/inbound/components_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix verify.preview`
- `cd mailglass_admin && npm run test:operator-browser`
- `cd mailglass_admin && git diff --exit-code priv/static/`
</verification>

<success_criteria>
The final large-type/tracking gate fails closed in CI, the only sanctioned Preview cleanup is minimal, and the committed CSS bundle is clean after all Phase 99 changes.
</success_criteria>

<output>
Create `.planning/phases/99-inbound-surface/99-05-SUMMARY.md` when done.
</output>
