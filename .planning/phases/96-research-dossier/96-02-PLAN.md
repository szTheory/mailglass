---
phase: 96-research-dossier
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v1.11/IA.md
autonomous: true
requirements:
  - RESEARCH-02
must_haves:
  truths:
    - ".planning/research/v1.11/IA.md exists and ends in a ## LOCKED DECISION block"
    - "Every LOCKED row carries an IA-LD-NN ID, a Decision cell stating literal pattern/class/structure, an Applies-to cell, a Constraint-binding cell, and a Closes-GAP cell"
    - "The dossier covers master-detail, filter/facet, and triage-grid IA patterns as they apply to the three admin surfaces (Operator/Inbound/Preview)"
    - "The Closes-GAP column references the open GAP rows relevant to IA findings"
    - "Honors governing CONTEXT decisions D-04 (every LOCKED row carries a Constraint-binding cell), D-05 (critic-then-lock adversarial synthesis before locking), and D-06 (live external web research from the gov.uk Design System / Nielsen, cited by URL and grounded with codebase file:line)"
  artifacts:
    - path: ".planning/research/v1.11/IA.md"
      provides: "IA dossier with adversarially-synthesized LOCKED DECISION block"
      contains: "## LOCKED DECISION"
  key_links:
    - from: "IA.md LOCKED DECISION block"
      to: "downstream phases 98 (Operator), 99 (Inbound), 100 (Preview)"
      via: "IA-LD-NN IDs cited in plan frontmatter / task actions"
      pattern: "IA-LD-[0-9]+"
---

<objective>
Research and write the IA dossier: distill gov.uk Design System IA patterns and Nielsen Norman Group usability principles into locked per-surface information architecture decisions covering master-detail layout, filter/facet interaction, and triage-grid composition — within the mailglass_admin hard design constraints (no new product features; flat elevation; semantic tokens; admin-only scope). End with an adversarially-synthesized LOCKED DECISION block.

Purpose: Phases 98 (Operator/Deliveries), 99 (Inbound), and 100 (Preview) cite IA-LD-NN IDs for their group/page/IA requirements instead of re-researching; the Phase 95 ratchet's Spacing and Color pillars have concrete per-surface IA specs.

Output: `.planning/research/v1.11/IA.md` — research body + adversarial synthesis + LOCKED DECISION block at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-research-dossier/96-CONTEXT.md
@.planning/RATCHET-GAP-REGISTER.md
@mailglass_admin/docs/design-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research IA principles from gov.uk Design System and Nielsen Norman Group</name>
  <files>.planning/research/v1.11/IA.md</files>
  <read_first>
    - .planning/phases/96-research-dossier/96-CONTEXT.md (D-01 through D-11 — authoritative spec; especially D-07 sourcing split and D-10 GAP mapping; read in full)
    - .planning/STATE.md (hard design constraints; v1.11 scope locks — no new routes/features; the three surfaces: Operator /ops/mail, Inbound /ops/mail/inbound, Preview /dev/mail)
    - mailglass_admin/docs/design-system.md (per-component conformance checklist; visual audit loop; State is URL-driven)
    - .planning/RATCHET-GAP-REGISTER.md (GAP-04: inbound filter labels off-token sev=2; GAP-05: gallery route gap sev=2; read all five rows)
    - .planning/REQUIREMENTS.md (GROUP-01..03, PAGE-01..03, RESP-01, FLOW-01/02, A11Y-01/02 — the IA+responsive+flow requirements these decisions will feed)
    - External: https://design-system.service.gov.uk/ (gov.uk Design System — primary external source; especially navigation, layout, and form patterns)
    - External: https://www.nngroup.com/articles/filters-vs-facets/ (Nielsen on filter/facet IA)
    - External: https://www.nngroup.com/articles/master-detail/ (Nielsen on master-detail pattern)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (orientation_strip, nav_link — the existing IA skeleton)
    - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex (current filter implementation — ground decisions here)
  </read_first>

  <action>
    Create `.planning/research/v1.11/IA.md` (the directory `.planning/research/v1.11/` is created by the MOTION plan if not yet present).

    Structure the dossier body with these sections (exact heading names at your discretion):
    1. Sources and Evidence — summarise the gov.uk Design System IA patterns (navigation, table/list, filter, triage) and Nielsen NN principles (master-detail, progressive disclosure, filter vs facet). Cite by URL + section. Note what is loved (clear progressive disclosure, stable URL state) and what is hated (hidden filters, ambiguous drill-down paths) from gov.uk and Nielsen evidence.
    2. Current mailglass_admin IA Inventory — map the three surfaces to their existing structure:
       - Operator (/ops/mail): orientation_strip → list → detail; support_cards triage; suppression card
       - Inbound (/ops/mail/inbound): routing_trace + evidence_card; filters_form
       - Preview (/dev/mail): device_frame; tabs; sidebar
       Ground with file:line citations to shell.ex, filters_form.ex, the operator/ and preview/ module files.
    3. Pattern Fit Analysis — for each surface, assess: does the current IA match the master-detail / filter / triage best-practice pattern? What deviates? Note the URL-driven state machine (existing strength per design-system.md) and any filter-label, hierarchy, or navigation gaps.
    4. Draft Decisions — propose specific locked decisions for: (a) orientation_strip position per viewport (mobile vs desktop), (b) filter panel disclosure pattern (visible vs collapsed), (c) master-detail split behaviour per viewport (390/768/1440), (d) triage-grid cell priority ordering on mobile, (e) navigation hierarchy (L1/L2 labelling), (f) empty/loading state placement per surface.
    5. Adversarial Synthesis — challenge each draft decision against (a) the hard design constraints (no new routes, flat elevation, semantic tokens only, no Node toolchain) and (b) the open GAP rows. Specifically: does any filter-label decision close GAP-04? Does the gallery route decision relate to GAP-05? Revise any decisions that fail the critic.
    6. LOCKED DECISION block — the table defined below.

    The LOCKED DECISION block MUST:
    - Use heading `## LOCKED DECISION`
    - Contain a markdown table with columns: `LD-ID | Decision | Applies-to (surface/archetype) | Constraint-binding | Closes-GAP`
    - Use ID prefix `IA-LD-NN` (e.g. IA-LD-01, IA-LD-02, ...)
    - Decision cell: state the LITERAL structural pattern/rule (e.g. "orientation_strip renders above content at 390px, collapses to icon-only at <768px", "filters_form disclosure: always visible at ≥768px, collapsed behind a button at 390px") — no vague prose
    - Constraint-binding cell: name the specific hard design constraint(s) (e.g. "no new routes; flat elevation; semantic tokens only")
    - Closes-GAP cell: reference the GAP-NN ID or "—" if none directly
    - Must include at minimum: one row per surface (Operator, Inbound, Preview), one filter/facet pattern row, one master-detail split row, one empty-state placement row
    - GAP-04 (inbound filter labels off-token) must appear in the Closes-GAP column of at least one row

    Do NOT modify any admin code, CSS, brandbook files, or the RATCHET-GAP-REGISTER.md.
  </action>

  <verify>
    <automated>grep -l "## LOCKED DECISION" .planning/research/v1.11/IA.md && grep -c "IA-LD-" .planning/research/v1.11/IA.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/research/v1.11/IA.md` exists
    - File contains `## LOCKED DECISION` heading
    - File contains at least 6 IA-LD-NN rows (grep -c "IA-LD-" returns >= 6)
    - Every row in the LOCKED DECISION table has a non-empty Constraint-binding cell
    - `GAP-04` appears at least once in the Closes-GAP column
    - File mentions all three surfaces: `Operator`, `Inbound`, `Preview`
    - File references gov.uk Design System and Nielsen Norman as sources
    - File contains `## LOCKED DECISION` exactly once (no duplicate headings)
  </acceptance_criteria>

  <done>IA.md exists, ends in a LOCKED DECISION block with at least 6 IA-LD-NN rows covering all three surfaces and filter/master-detail patterns, every row has a non-empty Constraint-binding cell, GAP-04 is in at least one Closes-GAP cell, and no locked decision violates the hard design constraints (no new routes, flat elevation, semantic tokens only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External web sources → dossier text | Researcher fetches gov.uk Design System and NNGroup pages; content is read-only input to a markdown file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02-SC | Tampering | No npm/pip/cargo installs in this plan | accept | Research-only phase; no package manager invocations |
| T-96-02-I | Information Disclosure | IA.md | accept | Docs-only research phase — no executable attack surface; the only integrity concern is that a LOCKED decision must not violate the hard design constraints (guarded by the D-04 constraint-binding cell + the downstream CI conformance/motion/token-parity gates) |
</threat_model>

<verification>
- `.planning/research/v1.11/IA.md` exists with content
- `grep "## LOCKED DECISION" .planning/research/v1.11/IA.md` returns a match
- `grep -c "IA-LD-" .planning/research/v1.11/IA.md` returns >= 6
- `grep "GAP-04" .planning/research/v1.11/IA.md` returns at least one match in the LOCKED DECISION section
- All three admin surfaces (Operator, Inbound, Preview) are named in the body
</verification>

<success_criteria>
IA.md is a complete, adversarially-synthesized research dossier that ends in a LOCKED DECISION block with stable IA-LD-NN IDs, literal structural pattern decisions, non-empty Constraint-binding cells, and GAP-04 in the Closes-GAP column — ready for downstream citation by Phases 98, 99, and 100 without re-reading the body.
</success_criteria>

<output>
Create `.planning/phases/96-research-dossier/96-02-SUMMARY.md` when done.
</output>
