---
phase: 96-research-dossier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v1.11/MOTION.md
autonomous: true
requirements:
  - RESEARCH-01
must_haves:
  truths:
    - ".planning/research/v1.11/MOTION.md exists and ends in a ## LOCKED DECISION block"
    - "Every LOCKED row carries a MOTION-LD-NN ID, a Decision cell stating literal token/class/value, an Applies-to cell, a Constraint-binding cell, and a Closes-GAP cell"
    - "No locked decision recommends ease-in, a spring, overshoot, height/width/padding animation, duration > 300ms, or a client JS hook"
    - "The Closes-GAP column references GAP-02 for the focusable-CTA / motion+a11y finding"
  artifacts:
    - path: ".planning/research/v1.11/MOTION.md"
      provides: "Motion dossier with adversarially-synthesized LOCKED DECISION block"
      contains: "## LOCKED DECISION"
  key_links:
    - from: "MOTION.md LOCKED DECISION block"
      to: "downstream phases 102 (Motion Pass) and 97 (Component Layer)"
      via: "MOTION-LD-NN IDs cited in plan frontmatter / task actions"
      pattern: "MOTION-LD-[0-9]+"
---

<objective>
Research and write the MOTION dossier: distill Emil Kowalski's animation principles (emilkowal.ski) and Apple/Material platform HIG into a locked easing/duration/property decision table that stays within the mailglass_admin hard design constraints (ease-out only, ≤300ms, transform/opacity only, no springs, prefers-reduced-motion respected, CSS+LiveView.JS only). End with an adversarially-synthesized LOCKED DECISION block.

Purpose: Phases 102 (Motion Pass) and 97 (Component Layer) cite MOTION-LD-NN IDs instead of re-researching; the Phase 95 ratchet's Motion+A11y pillar has a concrete spec to score against.

Output: `.planning/research/v1.11/MOTION.md` — research body + adversarial synthesis + LOCKED DECISION block at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-research-dossier/96-CONTEXT.md
@.planning/RATCHET-GAP-REGISTER.md
@mailglass_admin/docs/design-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research motion principles from Emil Kowalski and platform HIG</name>
  <files>.planning/research/v1.11/MOTION.md</files>
  <read_first>
    - .planning/phases/96-research-dossier/96-CONTEXT.md (D-01 through D-10 — authoritative spec; read in full)
    - .planning/STATE.md (hard design constraints block — the legal bounds every locked decision must fit within)
    - mailglass_admin/docs/design-system.md (motion vocabulary section lines ~79-102, the six named motions; the per-component checklist lines ~104-121)
    - .planning/RATCHET-GAP-REGISTER.md (GAP-02: preview empty-state focusable CTA / Motion+A11y sev=3; GAP-05: gallery route gap — the structural surface that verifies the matrix)
    - brandbook/brand-book.md (brand metaphor "clarity through panes" — content arrives by becoming visible; no sliding/bouncing)
    - External: https://emilkowal.ski/ui/great-animations (primary external source — fetch and read in full)
    - External: https://developer.apple.com/design/human-interface-guidelines/motion (Apple HIG motion)
    - External: https://m3.material.io/styles/motion/overview (Material Design 3 motion — secondary)
  </read_first>

  <action>
    Create `.planning/research/v1.11/MOTION.md`. The directory `.planning/research/v1.11/` must be created first if it does not exist.

    Structure the dossier body with these sections (exact heading names at your discretion per Claude's Discretion):
    1. Sources and Evidence — summarise the key principles from emilkowal.ski, Apple HIG, and Material HIG. Cite by URL + section. Ground each principle with a reference to the codebase motion vocabulary in design-system.md (file:line).
    2. Current mailglass_admin Motion Inventory — enumerate the six named motions already defined in design-system.md (reveal, timeline-in, tab-swap, overlay, row-state, flash) with their current class/duration/easing values. Identify which ones align with best-practice principles and which deviate.
    3. Hard-Constraint Compatibility Assessment — for each principle extracted from external sources, assess whether it can be implemented within: ease-out only (never ease-in), ≤300ms, transform/opacity only (no height/width/padding animation), no springs/overshoot, prefers-reduced-motion respected, CSS+LiveView.JS only (no client JS hook).
    4. Draft Decisions — propose specific locked decisions for: (a) easing tokens to use per motion type, (b) duration tokens per motion urgency tier, (c) which CSS properties are permitted vs. prohibited, (d) reduced-motion behaviour (snap vs. fade), (e) entrance vs. exit timing ratio rule, (f) mount-trigger rule (phx-mounted/element insertion, not every patch).
    5. Adversarial Synthesis — challenge each draft decision against (a) the hard design constraints and (b) the open GAP rows. Specifically: does any decision violate ease-out-only, ≤300ms, transform/opacity restriction, or the no-client-JS rule? Does the focusable-CTA decision for GAP-02 make the empty-state preview CTA keyboard reachable? Revise any decisions that fail the critic.
    6. LOCKED DECISION block — the table defined below.

    The LOCKED DECISION block MUST:
    - Use heading `## LOCKED DECISION`
    - Contain a markdown table with columns: `LD-ID | Decision | Applies-to (surface/archetype) | Constraint-binding | Closes-GAP`
    - Use ID prefix `MOTION-LD-NN` (e.g. MOTION-LD-01, MOTION-LD-02, ...)
    - Decision cell: state the LITERAL token/class/value (e.g. `ease-out` token, `duration-(--duration-fast)`, specific CSS property, exact motion class name) — no vague prose
    - Constraint-binding cell: name the specific hard design constraint(s) the decision satisfies (e.g. "ease-out only; ≤300ms; transform/opacity-only; CSS+LiveView.JS only")
    - Closes-GAP cell: reference the GAP-NN ID this decision lets a downstream phase close, or "—" if none directly
    - Must include at minimum: one row per named motion (reveal, timeline-in, tab-swap, overlay, row-state, flash), a reduced-motion rule row, a prohibited-properties row, a mount-trigger rule row
    - GAP-02 (preview empty-state focusable CTA) must appear in the Closes-GAP column of at least one row

    Do NOT modify any admin code, CSS, brandbook files, or the RATCHET-GAP-REGISTER.md.
  </action>

  <verify>
    <automated>grep -l "## LOCKED DECISION" .planning/research/v1.11/MOTION.md && grep -c "MOTION-LD-" .planning/research/v1.11/MOTION.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/research/v1.11/MOTION.md` exists
    - File contains `## LOCKED DECISION` heading (grep returns match)
    - File contains at least 6 MOTION-LD-NN rows (grep -c "MOTION-LD-" returns >= 6)
    - Every row in the LOCKED DECISION table has a non-empty Constraint-binding cell (no row has "| |" after the Applies-to column)
    - `GAP-02` appears at least once in the Closes-GAP column
    - File contains no `ease-in` in any locked decision (grep for ease-in in LOCKED DECISION section should return 0)
    - File contains no duration > 300ms locked in any row (no "400ms", "500ms", etc.)
    - File contains no "spring" or "overshoot" in the LOCKED DECISION block
  </acceptance_criteria>

  <done>MOTION.md exists, ends in a LOCKED DECISION block with at least 6 MOTION-LD-NN rows, every row has a non-empty Constraint-binding cell, GAP-02 is referenced in at least one Closes-GAP cell, and no locked decision violates the hard design constraints (ease-out only, ≤300ms, transform/opacity only, no springs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External web sources → dossier text | Researcher fetches emilkowal.ski and HIG pages; content is read-only input to a markdown file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01-SC | Tampering | No npm/pip/cargo installs in this plan | accept | Research-only phase; no package manager invocations |
| T-96-01-I | Information Disclosure | MOTION.md | accept | Docs-only research phase — no executable attack surface; the only integrity concern is that a LOCKED decision must not violate the hard design constraints (guarded by the D-04 constraint-binding cell + the downstream CI conformance/motion/token-parity gates) |
</threat_model>

<verification>
- `.planning/research/v1.11/MOTION.md` exists with content
- `grep "## LOCKED DECISION" .planning/research/v1.11/MOTION.md` returns a match
- `grep -c "MOTION-LD-" .planning/research/v1.11/MOTION.md` returns >= 6
- `grep "GAP-02" .planning/research/v1.11/MOTION.md` returns at least one match in the LOCKED DECISION section
- No row in the LOCKED DECISION table is missing the Constraint-binding cell
</verification>

<success_criteria>
MOTION.md is a complete, adversarially-synthesized research dossier that ends in a LOCKED DECISION block with stable MOTION-LD-NN IDs, literal token/class/value decisions, non-empty Constraint-binding cells, and GAP-02 in the Closes-GAP column — ready for downstream citation by Phases 97 and 102 without re-reading the body.
</success_criteria>

<output>
Create `.planning/phases/96-research-dossier/96-01-SUMMARY.md` when done.
</output>
