---
phase: 09-mailable-api-redesign-freeze
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/mailglass/mailable.ex
  - test/mailglass/mailable_test.exs
autonomous: true
requirements:
  - API-03
---

<objective>
Update the `Mailglass.Mailable` macro to eliminate `Swoosh.Email` leakage into the adopter's public API surface.

Purpose: Prevent adopter mailable code from depending directly on `Swoosh.Email` when constructing emails.
Output: Removes `import Swoosh.Email` in `__using__/1`, injects an `import Mailglass.Message, only: [...]` block while keeping the injection under 20 lines. Tests updated to reflect native Message usage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mailglass/mailable.ex
</context>

<interfaces>
From lib/mailglass/message.ex:
```elixir
def to(msg, address)
def from(msg, address)
def subject(msg, text)
def html_body(msg, content)
def text_body(msg, content)
def header(msg, name, value)
def attach(msg, attachment)
def put_tag(msg, tag)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Mailable Macro</name>
  <files>lib/mailglass/mailable.ex</files>
  <action>
    Locate `defmacro __using__(opts)` around line 125.
    Follow the `Macro Injection Pattern` (analog: `lib/mailglass/mailable.ex`) as documented in `09-PATTERNS.md`.
    Remove `import Swoosh.Email, except: [new: 0]`.
    Add `import Mailglass.Message, only: [to: 2, from: 2, subject: 2, html_body: 2, text_body: 2, header: 3, attach: 2, put_tag: 2]`.
    Ensure the top-level AST forms in the `quote` block remain ≤20 lines.
  </action>
  <verify>
    <automated>mix test test/mailglass/mailable_test.exs</automated>
  </verify>
  <done>Macro correctly imports `Mailglass.Message` functions instead of `Swoosh.Email`.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Mailable Unit Tests</name>
  <files>test/mailglass/mailable_test.exs</files>
  <action>
    Update any tests defining dummy mailables to use the new `Mailglass.Message` native setters (e.g., `attach/2` instead of `attachment/2`) if they previously relied on Swoosh macro injections.
  </action>
  <verify>
    <automated>mix test test/mailglass/mailable_test.exs</automated>
  </verify>
  <done>Tests pass with zero references to `Swoosh.Email` within dummy mailables.</done>
</task>

</tasks>

<must_haves>
  truths:
    - `use Mailglass.Mailable` no longer imports `Swoosh.Email`.
    - Native `Message` functions are available within mailables without prefix.
  artifacts:
    - path: "lib/mailglass/mailable.ex"
      provides: "Clean Mailable Macro injection"
  key_links:
    - from: "lib/mailglass/mailable.ex"
      to: "Mailglass.Message"
      via: "import statement"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Macro -> Adopter Module | Macro expansions must not execute arbitrary code or introduce unexpected dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | `Mailglass.Mailable` | accept | Macro injection is strictly limited to aliasing and delegation. |
</threat_model>

<verification>
mix test test/mailglass/mailable_test.exs --warnings-as-errors
</verification>

<success_criteria>
Adopters can define mailables using the native 8 field setters natively without any Swoosh macro namespace pollution.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mailable-api-redesign-freeze/09-02-SUMMARY.md`
</output>