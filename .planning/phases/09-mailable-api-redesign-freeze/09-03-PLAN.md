---
phase: 09-mailable-api-redesign-freeze
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/mix/tasks/mailglass.upgrade.v0_2.ex
  - test/mailglass/upgrade/v0_2_test.exs
autonomous: true
requirements:
  - API-05
---

<objective>
Implement the `mix mailglass.upgrade.v0_2` Igniter codemod.

Purpose: Provide an automated migration path that safely rewrites adopter code, converting `Swoosh.Email.*` calls to `Mailglass.Message.*` native setters, to drastically reduce the friction of upgrading to v0.2.
Output: An `Igniter.Mix.Task` that uses `Sourceror` to rewrite ASTs, safely skipping string literals and unknown Swoosh functions while providing dry-run functionality by default.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mailable-api-redesign-freeze/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Igniter Codemod Task</name>
  <files>lib/mix/tasks/mailglass.upgrade.v0_2.ex</files>
  <action>
    Create `Mix.Tasks.Mailglass.Upgrade.V0_2` using `Igniter.Mix.Task`.
    Follow the `Mix Task Definition Pattern` (analog: `lib/mix/tasks/mailglass.install.ex`) and the `Shared Patterns` (Error Handling / Mix Tasks) as documented in `09-PATTERNS.md`.
    Configure it with `schema: [apply: :boolean]` and `aliases: [a: :apply]`.
    If `--apply` is not provided, enforce `--dry-run` via `igniter.args.argv_flags`.
    Use `Igniter.Code.Common.update_all_matches/3` to traverse the AST via `Sourceror.Zipper`.
    Only match and rewrite the 8 known `Swoosh.Email` setter calls (`to, from, subject, text_body, html_body, header, attachment` -> `attach` in Mailglass, `put_tag` isn't in Swoosh but just in case, only rewrite standard Swoosh calls).
    Specifically map `Swoosh.Email.attachment/2` to `Mailglass.Message.attach/2`.
    Skip matching if the node is a string literal (using `Igniter.Code.String.string?/1`).
    If a `Swoosh.Email` function is encountered that isn't mapped, emit an `IO.warn` and leave it unchanged.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Task is implemented with safe rewriting rules and default dry-run logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codemod Unit Tests</name>
  <files>test/mailglass/upgrade/v0_2_test.exs</files>
  <behavior>
    - Test 1: Given a string containing `"Swoosh.Email"`, it is NOT rewritten.
    - Test 2: `Swoosh.Email.to(msg, "a@b.com")` is rewritten to `to(msg, "a@b.com")` (assuming local import).
    - Test 3: `Swoosh.Email.attachment(msg, path)` is rewritten to `attach(msg, path)`.
    - Test 4: Unknown functions like `Swoosh.Email.put_provider_option(msg, k, v)` emit a warning and are skipped.
  </behavior>
  <action>
    Implement ExUnit tests leveraging `Igniter.Test` helpers (`test_project()`, `apply_igniter/2`) to verify the codemod behaves exactly as described in the task constraints and handles string literals/comments safely.
  </action>
  <verify>
    <automated>mix test test/mailglass/upgrade/v0_2_test.exs</automated>
  </verify>
  <done>Tests prove the AST is correctly rewritten and edge cases are handled safely.</done>
</task>

</tasks>

<must_haves>
  truths:
    - Running the Mix task without `--apply` does not alter files.
    - Supported Swoosh calls are renamed correctly.
    - `attachment/2` is renamed to `attach/2`.
    - Strings/Comments are skipped.
  artifacts:
    - path: "lib/mix/tasks/mailglass.upgrade.v0_2.ex"
      provides: "Automated codemod logic"
    - path: "test/mailglass/upgrade/v0_2_test.exs"
      provides: "Codemod reliability proofs"
  key_links:
    - from: "lib/mix/tasks/mailglass.upgrade.v0_2.ex"
      to: "Sourceror"
      via: "AST Zipper traversal"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System -> AST Modifier | The tool reads local adopter files and rewrites them in place. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Repudiation | `Upgrade.V0_2` Mix Task | mitigate | Require explicit `--apply` flag. Default to safe `--dry-run` to prevent unintended code loss. |
</threat_model>

<verification>
mix test test/mailglass/upgrade/v0_2_test.exs validates AST modifications.
</verification>

<success_criteria>
Adopters can run `mix mailglass.upgrade.v0_2 --apply` and automatically migrate standard Swoosh pipelines into native `Mailglass.Message` pipelines with zero manual effort.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mailable-api-redesign-freeze/09-03-SUMMARY.md`
</output>