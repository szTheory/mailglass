---
phase: 47-inbound-test-helpers-generators
plan: 04
type: execute
wave: 3
depends_on: ["47-01", "47-03"]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
  - mailglass_inbound/mix.exs
  - mailglass_inbound/test/mailglass_inbound/mailbox_case_test.exs
  - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
autonomous: true
requirements: [ITEST-05]
user_setup: []

must_haves:
  truths:
    - "use MailglassInbound.MailboxCase checks out an Ecto sandbox on the ADOPTER's repo resolved from Application.get_env(:mailglass_inbound, :repo) — never a literal TestRepo"
    - "MailboxCase imports TestAssertions, sets Tenancy.put_current, resets CertCache (ETS) and S3Fetcher.Fake (process-dict) in setup, and stops the sandbox owner on exit"
    - "MailboxCase writes NO async-mode app-env key and snapshots nothing (sync execute/2 is structural) — no app-env leak across tests"
    - "The four Testing helpers ship in the Hex artifact and appear under a new ExDoc Testing group; the inbound docs-contract test asserts them"
    - "mix compile --no-optional-deps --warnings-as-errors stays green for the inbound package with all four helpers in lib/"
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex"
      provides: "MailglassInbound.MailboxCase ExUnit.CaseTemplate (ITEST-05)"
      contains: "use ExUnit.CaseTemplate"
    - path: "mailglass_inbound/mix.exs"
      provides: "ExDoc Testing group for the four helpers (D-47-02) + files: manifest coverage"
      contains: "Testing:"
    - path: "mailglass_inbound/test/mailglass_inbound/mailbox_case_test.exs"
      provides: "ITEST-05 self-tests incl. repo-from-app-env, no-TestRepo-literal, no app-env leak"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex"
      to: "Application.get_env(:mailglass_inbound, :repo)"
      via: "adopter repo resolution for Sandbox.start_owner! (NEVER TestRepo literal)"
      pattern: "Application.get_env\\(:mailglass_inbound, :repo\\)"
    - from: "mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex"
      to: "Mailglass.Webhook.Providers.SES.CertCache.reset/0 + MailglassInbound.S3Fetcher.Fake.reset/0"
      via: "process-global / process-dict reset in setup"
      pattern: "CertCache.reset|S3Fetcher.Fake.reset"
    - from: "mailglass_inbound/mix.exs"
      to: "ExDoc groups_for_modules Testing group"
      via: "the four helper modules listed"
      pattern: "Testing:"
---

<objective>
Build `MailglassInbound.MailboxCase` (ITEST-05) — the shipped `ExUnit.CaseTemplate` adopters `use` — and complete packaging so all four Testing helpers ship to Hex under a new ExDoc "Testing" group with the docs-contract test asserting them.

Purpose: This is the keystone that ties Plans 01/03 together into the adopter DX: `use MailglassInbound.MailboxCase` injects `TestAssertions`, sets up the sandbox on the adopter's repo, resets the only process-global state the fixtures touch, and (critically) snapshots NOTHING — because inbound achieves sync execution structurally via `Test.Ingress`, not by writing an async-mode app-env key. Shipping the case template is the one deliberate extension beyond outbound parity (outbound never shipped `MailerCase`).

Output: `mailbox_case.ex` (ships to Hex) + the `mix.exs` ExDoc/packaging edit + the docs-contract update + the MailboxCase self-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-inbound-test-helpers-generators/47-CONTEXT.md
@.planning/phases/47-inbound-test-helpers-generators/47-RESEARCH.md
@.planning/phases/47-inbound-test-helpers-generators/47-PATTERNS.md
@.planning/phases/47-inbound-test-helpers-generators/47-01-SUMMARY.md
@.planning/phases/47-inbound-test-helpers-generators/47-03-SUMMARY.md

<interfaces>
<!-- Confirmed against live code this session. Two corrections beyond RESEARCH/PATTERNS pseudocode are LOAD-BEARING. -->

CORRECTION A — repo location (RESEARCH COLLISION 1): MailglassInbound.TestRepo is at
  mailglass_inbound/test/support/test_repo.ex, NOT lib/, and is NOT in the package manifest.
  MailboxCase MUST resolve the adopter repo from app-env and raise if unset; the source
  must NOT contain the literal "TestRepo". The library's own self-tests work because
  config/test.exs:11 sets `config :mailglass_inbound, :repo, MailglassInbound.TestRepo`.

CORRECTION B — PubSub server name (verified live, supersedes RESEARCH pseudocode):
  The inbound app broadcasts on `Mailglass.PubSub` (the CORE lib's server) — see
  mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex:539:
    Phoenix.PubSub.broadcast(Mailglass.PubSub, topic, payload)
  Topic builder: MailglassInbound.PubSub.Topics.inbound_record_inserted(tenant_id)
    (pub_sub/topics.ex:33 → "mailglass:inbound:" <> tenant_id).
  So MailboxCase, IF it subscribes, MUST subscribe to `Mailglass.PubSub` (NOT
  `MailglassInbound.PubSub`, which does not exist). Subscription is best-effort/optional —
  the send-based capture (Plan 03) is the primary assertion path.

D-47-12 (confirmed): there is NO :async_execution_impl key anywhere. execute/2 reads
  :inbound_records/:source/:repo only; runner selection reads :mailglass_inbound,:async_adapter
  only in dispatch/2 (which Test.Ingress never calls). MailboxCase writes NO async app-env key
  → snapshots/restores NOTHING. Only teardown is Sandbox.stop_owner + next-setup resets.

Analog (test/support/mailer_case.ex): use ExUnit.CaseTemplate + `using do ... end`
  (lines 63-75) + `setup tags do ... end` (lines 100-210) with Sandbox.start_owner! +
  Tenancy.put_current + on_exit teardown. Tenancy tag handling: @tag tenant: :unset → nil
  (lines 103-109). DO NOT copy the :async_adapter / :async_adapter_impl snapshot (lines 120-204).

Resets (ses_provider_test.exs:26-28):
  Mailglass.Webhook.Providers.SES.CertCache.reset()   # cert_cache.ex:67 (process-global ETS)
  MailglassInbound.S3Fetcher.Fake.reset()             # s3_fetcher/fake.ex:33 (process-dict)

mix.exs packaging (mailglass_inbound/mix.exs):
  files: ~w(lib docs priv .formatter.exs mix.exs README* CHANGELOG* LICENSE*)  (line 113)
  groups_for_modules: Stable: [...], Internal: [MailglassInbound.OptionalDeps]  (after line 133)

docs-contract test (mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs):
  asserts a list of stable public modules appear in README + api_stability.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MailglassInbound.MailboxCase ExUnit case template (ITEST-05)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex, mailglass_inbound/test/mailglass_inbound/mailbox_case_test.exs</files>
  <read_first>
    - test/support/mailer_case.ex (lines 63-77 using-block; 93,103-118,151,185-207 sandbox+tenancy+teardown; 120-204 the async-impl snapshot to NOT copy)
    - mailglass_inbound/lib/mailglass_inbound/repo.ex (line 38: app-env repo facade — same resolution MailboxCase uses)
    - mailglass_inbound/config/test.exs (line 11: config :mailglass_inbound, :repo, MailglassInbound.TestRepo — why the self-tests work)
    - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex (line 539: Phoenix.PubSub.broadcast(Mailglass.PubSub, ...) — CORRECTION B)
    - mailglass_inbound/lib/mailglass_inbound/pub_sub/topics.ex (line 33: inbound_record_inserted/1)
    - mailglass_inbound/test/mailglass_inbound/ingress/ses_provider_test.exs (lines 26-28: CertCache/S3Fake reset)
    - mailglass_inbound/lib/mailglass_inbound/test_assertions.ex (Plan 03 output: the module the using-block imports)
  </read_first>
  <action>
    Create `mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` defining `MailglassInbound.MailboxCase` as an `ExUnit.CaseTemplate`. It ships in `lib/` (D-47-01), so reference ONLY core/runtime modules — never `Oban`/`ExAws`/`Plug.Test` (Pitfall 6). `using do quote do ... end` injects `import MailglassInbound.TestAssertions` and `alias MailglassInbound.{Fixtures, Test}`.

    `setup tags do ... end` (structure from `mailer_case.ex:100-210`, with the three locked divergences):
    1. Resolve `repo = Application.get_env(:mailglass_inbound, :repo) || raise "config :mailglass_inbound, :repo must be set for MailglassInbound.MailboxCase"` — NEVER the literal `TestRepo` (CORRECTION A / Pitfall 1). Then `pid = Ecto.Adapters.SQL.Sandbox.start_owner!(repo, shared: not tags[:async])`.
    2. Resolve `tenant_id` from `tags` (mirror analog `@tag tenant: :unset → nil` handling at lines 103-109; default a binary like "test-tenant"); if set, `Mailglass.Tenancy.put_current(tenant_id)`.
    3. Reset the ONLY process-global state the fixtures touch (D-47-12 substance): `Mailglass.Webhook.Providers.SES.CertCache.reset()` (process-global ETS) and `MailglassInbound.S3Fetcher.Fake.reset()` (process-dict) — call both even for non-SES tests (cheap, prevents cross-test bleed).
    4. Snapshot NOTHING in app-env (D-47-12). Do NOT copy the analog's `:async_adapter`/`:async_adapter_impl` snapshot/restore (lines 120-204); do NOT invent `:async_execution_impl` (it does not exist). Sync execution is structural via `Test.Ingress` calling `execute/2`.
    5. Optional best-effort PubSub subscription: if subscribing, use `Phoenix.PubSub.subscribe(Mailglass.PubSub, MailglassInbound.PubSub.Topics.inbound_record_inserted(tenant_id))` — server name `Mailglass.PubSub` (CORRECTION B), wrapped best-effort (the send-based capture from Plan 03 is the primary path). Do NOT reference `MailglassInbound.PubSub` (does not exist).
    6. `on_exit(fn -> Ecto.Adapters.SQL.Sandbox.stop_owner(pid) end)`; return `:ok`.

    Add a moduledoc placing it in the "Testing" surface, stating it ships for adopters to `use`, that the repo resolves from app-env, and that it snapshots no app-env (sync is structural). No `name: __MODULE__` singletons (CLAUDE.md).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/mailbox_case_test.exs --seed 0 -x</automated>
  </verify>
  <acceptance_criteria>
    - `MailglassInbound.MailboxCase` defines `use ExUnit.CaseTemplate`, a `using` block importing `MailglassInbound.TestAssertions`, and a `setup tags` block.
    - `grep -n "TestRepo" mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` returns NOTHING (the literal must be absent — Pitfall 1).
    - `grep -n "Application.get_env(:mailglass_inbound, :repo)" mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` confirms app-env repo resolution with a raise-if-unset.
    - `grep -n "async_execution_impl\|async_adapter_impl" mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` returns NOTHING (no snapshot, no mythical key — D-47-12).
    - `grep -n "MailglassInbound.PubSub\b" mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` returns nothing for a server reference; any subscribe uses `Mailglass.PubSub` (CORRECTION B).
    - `grep -n "CertCache.reset\|S3Fetcher.Fake.reset" mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` confirms both resets.
    - `grep -n "name: __MODULE__" mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` returns nothing.
    - `cd mailglass_inbound && mix test test/mailglass_inbound/mailbox_case_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <done>MailboxCase resolves the adopter repo from app-env (no TestRepo literal), resets CertCache + S3Fake, snapshots no app-env key, and any PubSub subscribe targets Mailglass.PubSub.</done>
</task>

<task type="auto">
  <name>Task 2: Package the four Testing helpers (mix.exs ExDoc group + manifest) and write the MailboxCase + docs-contract self-tests</name>
  <files>mailglass_inbound/mix.exs, mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs, mailglass_inbound/test/mailglass_inbound/mailbox_case_test.exs</files>
  <read_first>
    - mailglass_inbound/mix.exs (lines 113 files: manifest; 133+ groups_for_modules Stable/Internal)
    - mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs (the stable-module inventory assertions to extend)
    - test/support/mailer_case.ex (lines 100-210: behaviors the MailboxCase self-test should cover — sandbox checkout, tenancy, teardown)
    - mailglass_inbound/test/mailglass_inbound/test/ingress_test.exs (Plan 03 output: how to drive a capture inside a MailboxCase test)
  </read_first>
  <action>
    Edit `mailglass_inbound/mix.exs`: add a new `Testing:` group to `groups_for_modules` (D-47-02) listing `MailglassInbound.TestAssertions`, `MailglassInbound.MailboxCase`, `MailglassInbound.Test.Ingress`, `MailglassInbound.Fixtures`, slotted between `Stable:` and `Internal:` (PATTERNS lines 389-399). The `files:` manifest already globs `lib` so the four helpers (including the nested `lib/mailglass_inbound/test/ingress.ex`) ship automatically — verify with `mix hex.build` (Assumption A3). Do NOT add `:ex_unit` as a dep (D-47-02).

    Update `mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs` to assert the four Testing modules are part of the documented inventory (mirror the existing stable-module loop). The four modules raise the inbound public surface (sibling linked-version contract, CLAUDE.md).

    Write `mailglass_inbound/test/mailglass_inbound/mailbox_case_test.exs` (ITEST-05): a test module that `use MailglassInbound.MailboxCase`, then asserts — (1) the sandbox is checked out on the app-env repo (a `Test.Ingress` capture inside the case persists + asserts without a connection error); (2) the MailboxCase SOURCE contains no `TestRepo` literal (read the file and `refute content =~ "TestRepo"` — the RESEARCH Wave 0 explicit assertion); (3) no app-env leak — read `Application.get_env(:mailglass_inbound, :async_adapter)` (or any async key) before and after a case run and assert unchanged, and assert MailboxCase set no `:async_*` key (D-47-12). Run with `--seed 0` (MEMORY.md inbound-flake note).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/mailbox_case_test.exs test/mailglass_inbound/docs_contract_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_inbound/mix.exs` `groups_for_modules` contains a `Testing:` group listing all four helper modules.
    - `mix hex.build` (in `mailglass_inbound`) succeeds and the four modules are under `lib/` in the artifact (no manifest gap).
    - `docs_contract_test.exs` asserts the four Testing modules are in the documented inventory; it passes.
    - `mailbox_case_test.exs` asserts: a capture works inside the case (sandbox checked out on app-env repo), the MailboxCase source has no `TestRepo` literal, and no async app-env key leaks across a case run.
    - `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` exits 0.
    - `cd mailglass_inbound && mix test test/mailglass_inbound/mailbox_case_test.exs test/mailglass_inbound/docs_contract_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <done>The four Testing helpers ship to Hex under a new ExDoc Testing group, the docs-contract test asserts them, and MailboxCase is self-tested for app-env-repo resolution, no-TestRepo-literal, and no app-env leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MailboxCase` setup → process-global CertCache ETS + per-process sandbox | Resets shared ETS and owns a sandbox checkout per test |
| Packaged helpers → Hex artifact | The four lib/ modules become part of the public adopter-facing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-13 | Tampering (test pollution) | MailboxCase leaking a global app-env value across tests | mitigate (HIGH) | D-47-12: MailboxCase writes NO async-mode app-env key (sync `execute/2` is structural), so there is no leak surface; only ETS reset + Sandbox.stop_owner. Self-test asserts no `:async_*` key set and no leak across a case run. |
| T-47-14 | Tampering | wrong repo / cross-app sandbox checkout | mitigate (HIGH) | Repo resolved from `Application.get_env(:mailglass_inbound, :repo)` with raise-if-unset; the literal `TestRepo` is forbidden in source (grep + self-test assert its absence). (Pitfall 1) |
| T-47-15 | Information Disclosure | shipping internal-only helpers / real-PII to Hex | mitigate (MEDIUM) | Only the four intended adopter-facing helpers are added to the Testing group; `mix hex.build` confirms artifact contents; TestRepo stays in `test/support/` (not packaged). |
| T-47-16 | Tampering | shared CertCache ETS race under async | mitigate (MEDIUM) | MailboxCase resets CertCache in setup; SES-fixture tests use per-call unique cert URLs (Plan 01) / `async: false`. (Pitfall 2) |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/mailbox_case_test.exs test/mailglass_inbound/docs_contract_test.exs --seed 0` is green.
- `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` is green with all four helpers in lib/.
- `mix hex.build` (in mailglass_inbound) packages the four Testing modules; no `:ex_unit` dep added.
- MailboxCase source contains no `TestRepo` literal, no `:async_*` snapshot, and any PubSub subscribe targets `Mailglass.PubSub`.
- Phase-gate (run after all waves, by the orchestrator): inbound full suite `cd mailglass_inbound && mix test --seed 0` + core gen tests `mix test test/mix/tasks/ --seed 0` + `mix credo --strict` (custom checks) both packages.
</verification>

<success_criteria>
ITEST-05 satisfied: `use MailglassInbound.MailboxCase` sets up an Ecto sandbox on the adopter's app-env repo (never TestRepo), imports TestAssertions, sets tenancy, resets CertCache + S3Fake, snapshots no app-env key (no leak), and the four Testing helpers ship to Hex under a new ExDoc Testing group asserted by the docs-contract test.
</success_criteria>

<output>
Create `.planning/phases/47-inbound-test-helpers-generators/47-04-SUMMARY.md` when done
</output>
