---
phase: 47-inbound-test-helpers-generators
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/test/ingress.ex
  - mailglass_inbound/lib/mailglass_inbound/test_assertions.ex
  - mailglass_inbound/test/mailglass_inbound/test/ingress_test.exs
  - mailglass_inbound/test/mailglass_inbound/test_assertions_test.exs
autonomous: true
requirements: [ITEST-01, ITEST-02, ITEST-03, ITEST-04, ITEST-06]
user_setup: []

must_haves:
  truths:
    - "Test.Ingress.receive_inbound/2 drives the real Persist.persist/2 → Execution.execute/2 (SYNC) write path and captures the result by send(self(), {:inbound, msg, outcome, route})"
    - "Test.Ingress.receive_provider_payload/3 runs the REAL provider verify!/normalize seam before the same persist+execute+capture"
    - "Replaying the same message N times yields 1 inbound record + 1 fresh execution run (convergence), incl. SendGrid/SES via raw_mime dedupe"
    - "assert_inbound_received/1 fires correctly in all 4 matcher styles (no-arg / keyword / function predicate / pattern match)"
    - "assert_inbound_{accepted,rejected,ignored,bounced}/1 key off the locked outcome atoms; assert_inbound_routed_to/2 + assert_inbound_no_match/1 match the persisted route map; assert_no_inbound_received/0 refutes correctly"
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/test/ingress.ex"
      provides: "MailglassInbound.Test.Ingress — the real persist+route+execute driver + capture seam (ITEST-06)"
      contains: "send(self(), {:inbound,"
    - path: "mailglass_inbound/lib/mailglass_inbound/test_assertions.ex"
      provides: "MailglassInbound.TestAssertions — 4 matcher styles + outcome + routing + negative assertions (ITEST-01..04)"
      contains: "defmacro assert_inbound_received"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/test/ingress.ex"
      to: "MailglassInbound.Ingress.Persist.persist/2"
      via: "real persist with routes:/router:/repo: opts"
      pattern: "Persist\\.persist"
    - from: "mailglass_inbound/lib/mailglass_inbound/test/ingress.ex"
      to: "MailglassInbound.Execution.execute/2"
      via: "SYNC execute (never dispatch/2)"
      pattern: "Execution\\.execute"
    - from: "mailglass_inbound/lib/mailglass_inbound/test_assertions.ex"
      to: "the {:inbound, msg, outcome, route} mailbox tuple"
      via: "assert_received over the captured tuple"
      pattern: "assert_received \\{:inbound"
---

<objective>
Build the assertion + driver contract pair: `MailglassInbound.Test.Ingress` (ITEST-06) drives the real `Persist.persist/2 → Execution.execute/2` (sync) write path and emits the capture tuple `send(self(), {:inbound, msg, outcome, route})`; `MailglassInbound.TestAssertions` (ITEST-01..04) reads that tuple with the 4 matcher styles + outcome + routing + negative assertions.

Purpose: These two are a tightly-coupled contract — `Test.Ingress` defines the captured tuple shape, `TestAssertions` consumes it. Building them together (after Plan 01's Fixtures) avoids the executor scavenger-hunting the tuple shape or the outcome shape. This is the inbound analog of outbound's `Fake.Storage`-`{:mail,_}`-`TestAssertions` triangle, with the capture seam relocated into `Test.Ingress` (inbound has no `Fake.Storage`). Per D-47-05, the outbound `Fake.trigger_event/3` analogy is shape-different (it simulates a downstream webhook on an already-delivered message; inbound has no delivery row) — do NOT force that shape onto `Test.Ingress`.

Output: `mailglass_inbound/lib/mailglass_inbound/test/ingress.ex` + `test_assertions.ex` (both ship to Hex via the `lib` glob) + their self-tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-inbound-test-helpers-generators/47-CONTEXT.md
@.planning/phases/47-inbound-test-helpers-generators/47-RESEARCH.md
@.planning/phases/47-inbound-test-helpers-generators/47-PATTERNS.md
@.planning/phases/47-inbound-test-helpers-generators/47-01-SUMMARY.md

<interfaces>
<!-- Confirmed runtime signatures the driver drives + the captured shapes assertions read. -->

Persist (mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex:19-22, 281-293):
  persist(%{tenant_id, provider, message: %InboundMessage{}, evidence} = handoff, opts) :: {:ok, map()}
    opts reads :repo (default MailglassInbound.Repo), :routes, :router
    returns map with :status (:inserted | :duplicate), :route, :inbound_record, ...
    route map: %{status: :matched, mailbox: ...} | %{status: :no_match}  (route_compatibility/2)
    DEDUPE: Postmark on provider_message_id; SendGrid/Mailgun/SES on md5(raw_mime) when id nil (persist.ex:111-198)

Execution (mailglass_inbound/lib/mailglass_inbound/execution.ex:37-62) — SYNC:
  execute(%{status: :inserted} = persisted, opts) :: {:ok, normalized_result}
    reads :inbound_records, :source; %{status: :duplicate} short-circuits to {:ok, %{status: :skipped}}
    normalized_result via normalize_result/1 (execution.ex:44) carries the outcome
  DO NOT call dispatch/2 (execution.ex:14-35) — async, non-deterministic run counts (D-47-03/04)

Locked outcome atoms (mailglass_inbound/lib/mailglass_inbound/mailbox.ex:22):
  :accept | :ignore | {:reject, reason} | {:bounce, reason}
  ITEST-02 map: accepted→:accept, ignored→:ignore, rejected→{:reject,_}, bounced→{:bounce,_}

Provider verify!/normalize (ingress/provider.ex:39-56): see arities in Plan 01 interfaces.
  SES config seam: config: %{s3_fetcher: MailglassInbound.S3Fetcher.Fake, cert_cache_ttl_seconds: 86_400}
  Build handoff after normalize the way the plug does (plug.ex:404-434, :470).

Outbound analog (lib/mailglass/test_assertions.ex):
  4-style macro dispatch — lines 86-119; __match_keyword__/2 — lines 121-159;
  assert_no_mail_sent — lines 206-210; capture is {:mail, _msg} (lines 33-34).

Fixtures (from Plan 01 — read 47-01-SUMMARY.md for final API):
  build_inbound_message/1, build_postmark_payload/1, build_sendgrid_payload/1,
  build_mailgun_payload/1, build_ses_sns_payload/1.

Convergence driver precedent (inbound_idempotency_convergence_test.exs:99-102):
  {:ok, persisted} = Persist.persist(handoff(payload), []); _ = Execution.execute(persisted, source: :fresh)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build MailglassInbound.Test.Ingress — real persist+execute driver + capture seam (ITEST-06)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/test/ingress.ex, mailglass_inbound/test/mailglass_inbound/test/ingress_test.exs</files>
  <read_first>
    - mailglass_inbound/test/mailglass_inbound/properties/inbound_idempotency_convergence_test.exs (lines 99-102: the canonical persist→sync-execute driver this generalizes; lines 140-147: TRUNCATE CASCADE bulk reset)
    - mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex (lines 19-22, 111-198, 281-293: signature, per-provider dedupe, route map)
    - mailglass_inbound/lib/mailglass_inbound/execution.ex (lines 37-62: execute/2 sync return + duplicate short-circuit; 14-35: dispatch/2 — do NOT call)
    - mailglass_inbound/lib/mailglass_inbound/ingress/plug.ex (lines 404-434, 470: provider dispatch + handoff build after normalize)
    - mailglass_inbound/lib/mailglass_inbound/ingress/providers/ses.ex (lines 47,50,57,84,135: verify!/normalize + process-dict stash + s3_fetcher config seam)
    - lib/mailglass/test_assertions.ex (lines 33-34: outbound {:mail,_} capture analog)
  </read_first>
  <behavior>
    - receive_inbound(%InboundMessage{}, opts): builds the handoff, runs Persist.persist/2 (opts take :routes/:router/:repo/:evidence), then Execution.execute/2 with source: :fresh (SYNC), then send(self(), {:inbound, message, outcome, route}); returns {:ok, %{message, outcome, route, persisted}}.
    - receive_provider_payload(provider, payload, opts): runs the REAL provider verify!/normalize seam (config: opt passed to verify!, SES default s3_fetcher: S3Fetcher.Fake), builds the handoff like the plug, then the same persist+execute+capture.
    - Replaying the same message twice inserts exactly 1 inbound record and runs exactly 1 fresh execution (the duplicate short-circuits to :skipped) — verified for Postmark (id dedupe) AND SendGrid/SES (raw_mime dedupe).
    - Never calls dispatch/2.
  </behavior>
  <action>
    Create `mailglass_inbound/lib/mailglass_inbound/test/ingress.ex` — module `MailglassInbound.Test.Ingress` at the `lib/` path `lib/mailglass_inbound/test/ingress.ex` (a `lib/` path despite the `Test` in the name) so the `files: ~w(lib …)` manifest packages it. Do NOT place it in `test/support/`. It ships in `lib/`, so reference ONLY core/runtime modules — never `Oban`/`ExAws`/`Plug.Test` (Pitfall 6).

    Implement `receive_inbound/2`: build `handoff = %{tenant_id:, provider:, message:, evidence:}` (default `evidence` to `%{raw_payload: %{}}`); take `persist_opts = Keyword.take(opts, [:routes, :router, :repo])`; `with {:ok, persisted} <- Persist.persist(handoff, persist_opts), {:ok, outcome} <- Execution.execute(persisted, source: :fresh)` then `send(self(), {:inbound, message, outcome, persisted.route})` (THE CAPTURE SEAM — the inbound analog of outbound's `{:mail,_}` at `test_assertions.ex:33-34`) and return `{:ok, %{message:, outcome:, route: persisted.route, persisted:}}`. Drive `execute/2` SYNC, never `dispatch/2` (D-47-03/04 — async yields non-deterministic ExecutionRun counts, proven by the convergence test).

    Implement `receive_provider_payload/3`: dispatch to the real `provider.verify!`/`normalize` (mirror `plug.ex:404-434`), accept a `config:` opt passed straight to `verify!` and default SES `s3_fetcher:` to `MailglassInbound.S3Fetcher.Fake` (RESEARCH Open Q3); build the handoff the way the plug does after normalize (`plug.ex:470`), then the same persist+execute+capture chain. Exact opt names + return shape are Claude's Discretion (D-47-04).

    Decide whether the captured `outcome` slot is the raw outcome atom or the normalized `execute/2` result map — keep it consistent with the ITEST-02 matching in Task 2 (PATTERNS planner-action note). Document the choice in the moduledoc.

    Create `mailglass_inbound/test/mailglass_inbound/test/ingress_test.exs`: use Fixtures (Plan 01) to build messages; assert `receive_inbound/2` and `receive_provider_payload/3` both reach a captured `{:inbound, _, _, _}`; MANDATORY convergence case — replay the same message N times and assert exactly 1 inbound record + 1 fresh run; MANDATORY cross-provider dedupe case — a SendGrid (or SES) 2-replay yields 1 record (Pitfall 5: those dedupe on `md5(raw_mime)`, so the Fixtures payload MUST carry `evidence: %{raw_mime: ...}`).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/test/ingress_test.exs --seed 0 -x</automated>
  </verify>
  <acceptance_criteria>
    - `MailglassInbound.Test.Ingress` defines `receive_inbound/2` and `receive_provider_payload/3` at `lib/mailglass_inbound/test/ingress.ex`.
    - `grep -n "send(self(), {:inbound" mailglass_inbound/lib/mailglass_inbound/test/ingress.ex` confirms the capture seam.
    - `grep -n "dispatch" mailglass_inbound/lib/mailglass_inbound/test/ingress.ex` returns nothing (only `execute/2` is driven).
    - The self-test includes a replay-convergence case (N replays → 1 record + 1 fresh run) AND a SendGrid/SES raw_mime dedupe case.
    - `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` exits 0.
    - `cd mailglass_inbound && mix test test/mailglass_inbound/test/ingress_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <done>Test.Ingress drives the real sync persist+execute path, captures via send(self(),{:inbound,...}), converges on replay (incl. raw_mime-dedupe providers), and never calls dispatch/2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build MailglassInbound.TestAssertions — 4 matcher styles + outcome + routing + negative (ITEST-01..04)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/test_assertions.ex, mailglass_inbound/test/mailglass_inbound/test_assertions_test.exs</files>
  <read_first>
    - lib/mailglass/test_assertions.ex (lines 1-57 moduledoc/posture; 86-119 4-style macro dispatch; 121-159 __match_keyword__/2; 206-210 assert_no_mail_sent)
    - mailglass_inbound/lib/mailglass_inbound/test/ingress.ex (Task 1 output: the {:inbound, msg, outcome, route} tuple + the outcome shape decision)
    - mailglass_inbound/lib/mailglass_inbound/mailbox.ex (line 22: locked outcome atoms)
    - mailglass_inbound/lib/mailglass_inbound/inbound_message.ex (lines 60-78: fields the keyword matcher maps onto)
    - mailglass_inbound/lib/mailglass_inbound/ingress/persist.ex (lines 281-293: route map shape routing assertions match)
  </read_first>
  <behavior>
    - assert_inbound_received (no-arg): passes iff a {:inbound,...} tuple is in the mailbox.
    - assert_inbound_received(pattern) (struct pattern): asserts the captured message matches the pattern.
    - assert_inbound_received(fn): asserts the predicate returns true for the captured message.
    - assert_inbound_received(keyword): asserts per-key field matches (subject, from/to against [%{address:}], tenant→tenant_id, provider, envelope_recipient), with a flunk on unsupported keys.
    - assert_inbound_accepted/rejected/ignored/bounced: match the locked outcome atoms (:accept / {:reject,_} / :ignore / {:bounce,_}); each refutes the others.
    - assert_inbound_routed_to(mailbox): matches %{status: :matched, mailbox: ^mailbox}; assert_inbound_no_match: matches %{status: :no_match}.
    - assert_no_inbound_received/0: passes when nothing captured, fails when one is.
  </behavior>
  <action>
    Create `mailglass_inbound/lib/mailglass_inbound/test_assertions.ex` — module `MailglassInbound.TestAssertions`, shipping in `lib/` like the outbound analog. `import ExUnit.Assertions` at the top (compiles in the package — ExUnit is a bundled OTP app, no Hex dep per D-47-02). Moduledoc states the PII posture (failure messages may embed caller-supplied values in the adopter's own test output only, mirror `test_assertions.ex:47-52`) and that these are ExUnit `assert`/`flunk`, not domain errors (CLAUDE.md errors-as-contract).

    Copy the 4-style macro head-pattern dispatch from `test_assertions.ex:86-119` VERBATIM, retargeting the captured tuple from `{:mail, _msg}` to `{:inbound, msg, outcome, route}` (PATTERNS lines 42-70): no-arg presence, `{:%{}, _, _}` struct-pattern, `{:fn, _, _}` predicate, and the keyword fallback calling a `__match_keyword__/2` (copy the `Enum.each` + per-key `assert ..., "<field> mismatch"` shape from lines 121-159; map keys to `%InboundMessage{}` fields; end with the same `flunk("Unsupported matcher key: …")`). ITEST-02 outcome assertions key off the locked atoms at `mailbox.ex:22` — keep the match consistent with Task 1's captured-outcome shape decision. ITEST-03 routing assertions match the persisted route map (`persist.ex:281-293`): `assert_inbound_routed_to/2` → `%{status: :matched, mailbox: ^expected}`, `assert_inbound_no_match/1` → `%{status: :no_match}`. ITEST-04 `assert_no_inbound_received/0` → `refute_received {:inbound, _, _, _}` (analog `assert_no_mail_sent` lines 206-210). Failure `flunk` messages are clear and composed per brand voice (e.g. "No inbound message received in this test process"), never "Oops!". Exact signatures are Claude's Discretion (D-47).

    Create `mailglass_inbound/test/mailglass_inbound/test_assertions_test.exs`: drive captures via `Test.Ingress` (Task 1) + Fixtures, then self-test EACH assertion family on BOTH the positive and the negative path — all 4 matcher styles, each outcome assertion (matches its atom, refutes the others), both routing assertions, and `assert_no_inbound_received/0` (passes when nothing sent, fails when one is). This file is `async: true`-capable (the `send`-based capture is per-process).
  </action>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/test_assertions_test.exs --seed 0 -x</automated>
  </verify>
  <acceptance_criteria>
    - `MailglassInbound.TestAssertions` defines `assert_inbound_received/0,1`, `assert_inbound_accepted/_rejected/_ignored/_bounced`, `assert_inbound_routed_to/2`, `assert_inbound_no_match/1`, `assert_no_inbound_received/0`.
    - `grep -n "import ExUnit.Assertions" mailglass_inbound/lib/mailglass_inbound/test_assertions.ex` confirms the bundled-ExUnit import (no `:ex_unit` dep added to mix.exs).
    - The self-test exercises all 4 matcher styles, every outcome assertion (positive + refutes-others), both routing assertions, and the negative assertion (both paths).
    - No "Oops!" or non-composed flunk strings (brand voice).
    - `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` exits 0.
    - `cd mailglass_inbound && mix test test/mailglass_inbound/test_assertions_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <done>TestAssertions provides all 4 matcher styles + outcome + routing + negative assertions over the captured {:inbound,...} tuple; each family is self-tested on positive and negative paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Test.Ingress` (lib/) → real Persist + Execution write path | Drives the production DB write + mailbox execution synchronously in the test process |
| Adopter-supplied matcher values → assertion failure messages | Caller values embedded in ExUnit output (adopter's own test logs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-09 | Information Disclosure | assertion failure messages embedding PII | mitigate (MEDIUM) | Messages embed caller-supplied matcher values only, in the adopter's own test output (mirror `test_assertions.ex:47-52`); helpers emit NO telemetry (no PII spans added). (security V7) |
| T-47-10 | Tampering (test pollution) | `Test.Ingress` writing real records | accept (LOW) | Writes go to the adopter's sandboxed test DB, rolled back per test by `Sandbox.stop_owner` (MailboxCase, Plan 04); append-only table reset is TRUNCATE CASCADE, never DELETE. |
| T-47-11 | Spoofing | bypassing the real provider verify! seam | mitigate (LOW) | `receive_provider_payload/3` runs the REAL `verify!`/`normalize` (not a test copy), so fixtures must pass genuine verification — no weakened verifier. (D-47-04) |
| T-47-12 | Elevation / Access Control | cross-tenant capture/assertion | mitigate (LOW) | Captured tuple carries the message's `tenant_id`; assertions read the captured message; Fixtures default a tenant. (security V4) |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/test/ingress_test.exs test/mailglass_inbound/test_assertions_test.exs --seed 0` is green.
- `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` is green (neither lib/ module references optional deps).
- `Test.Ingress` drives `execute/2` only (no `dispatch/2`); convergence + raw_mime-dedupe replays pass.
</verification>

<success_criteria>
ITEST-01..04 + ITEST-06 satisfied: `Test.Ingress` drives the real sync persist+route+execute path with both entry points and a single fake-provider seam, capturing via `send(self(), {:inbound, msg, outcome, route})`; `TestAssertions` reads that tuple with 4 matcher styles + outcome + routing + negative assertions, each self-tested both ways.
</success_criteria>

<output>
Create `.planning/phases/47-inbound-test-helpers-generators/47-03-SUMMARY.md` when done
</output>
