---
phase: 30-per-domain-rate-limiting
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified: [lib/mailglass/rate_limiter.ex, guides/rate-limiting.md]
autonomous: true
requirements: [RATE-02]
must_haves:
  truths:
    - "System applies sensible default limits even when not explicitly configured."
    - "Operator can consult documentation on how to tune rate limits for reputation protection."
  artifacts:
    - path: "guides/rate-limiting.md"
      provides: "Operator guide for rate limiting"
---

<objective>
Establish sensible default limits for all bucket types to prevent unintentional bursts and provide a comprehensive guide for operators to tune these limits.

Purpose: Protect adopters from reputation damage by default while providing clear paths for customization.
Output: Sensible default configuration and an operator's guide for rate limiting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/mailglass/rate_limiter.ex
@.planning/phases/30-per-domain-rate-limiting/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and implement sensible defaults for all bucket types</name>
  <files>lib/mailglass/rate_limiter.ex</files>
  <action>
    Review the default limits.
    Propose:
    - `:tenant_recipient`: 100/min (existing)
    - `:global_recipient`: 1000/min (to protect shared IP reputation)
    - `:sender_domain`: 500/min (to protect sender domain reputation)
    Implement these as fallbacks in `RateLimiter.limits_for/2`.
  </action>
  <verify>
    <automated>mix test test/mailglass/rate_limiter_test.exs</automated>
  </verify>
  <done>Sensible defaults are active for all rate limit buckets.</done>
</task>

<task type="auto">
  <name>Task 2: Create Operator Guide for Rate Limiting</name>
  <files>guides/rate-limiting.md</files>
  <action>
    Create a new guide `guides/rate-limiting.md` that explains:
    - The three types of rate limits (tenant-recipient, global-recipient, sender-domain).
    - Why each one matters for reputation.
    - How to configure overrides in `config.exs`.
    - The `:transactional` bypass invariant (D-24).
    - How to handle `RateLimitError` in adopter code.
  </action>
  <verify>
    Check that file exists and covers all topics.
  </verify>
  <done>Comprehensive rate limiting guide is available for operators.</done>
</task>

</tasks>

<success_criteria>
1. Default limits are applied automatically without user configuration.
2. Documentation clearly explains the multi-bucket strategy and how to customize it.
</success_criteria>

<output>
After completion, create `.planning/phases/30-per-domain-rate-limiting/30-02-SUMMARY.md`
</output>
