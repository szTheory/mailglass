---
phase: 127-inbound-test-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
  - mailglass_inbound/docs/inbound-testing.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - DET-01
  - DET-02

must_haves:
  truths:
    - "`grep -n 'shared:' mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` returns no matches — every `shared:` reference (code + moduledoc) is gone"
    - "`grep -n 'seed 0' .github/workflows/ci.yml` returns no matches — the workaround comment and flag are deleted"
    - "The inbound suite passes across 20 independent random seeds (DET-02)"
    - "`mix compile --no-optional-deps --warnings-as-errors` in `mailglass_inbound/` exits 0 — no unused variable warning from the dropped `async?` computation"
    - "`docs_contract_test.exs` stays green — `inbound-testing.md` still contains `use MailglassInbound.MailboxCase, async: false` and `always \\`use MailglassInbound.MailboxCase, async: false\\``"
  artifacts:
    - mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex (modified: async: false default, shared: dropped, async? removed)
    - mailglass_inbound/docs/inbound-testing.md (modified: "Why async: false" explanation rewritten, shared-mode table row updated)
    - .github/workflows/ci.yml (modified: --seed 0 flag and its explanatory comment block deleted)
  key_links:
    - "MailboxCase.setup/1 → Ecto.Adapters.SQL.Sandbox.start_owner!(repo) — plain ownership, no shared: arg"
    - "ci.yml Run inbound tests step → `mix test --exclude property` — no --seed 0"
    - "inbound-testing.md Why async: false section → explanation matches new ownership model (serial by default, ETS reset still applies)"
---

<objective>
Root-cause fix for the `mailglass_inbound` shared-mode/async Ecto sandbox flake, then delete the
`--seed 0` workaround that was masking it. Two mechanical changes to three files; zero product-behavior
change (D-23 convergence holds — no lib runtime code, no schema change).

Purpose: Make the inbound test suite deterministic by construction so any future `mix ci` inbound step
(Phase 128) can use a plain `mix test --exclude property` invocation without a pinned seed.

Output: Modified `mailbox_case.ex` (default async: false, shared: dropped), updated
`inbound-testing.md` (explanation rewritten), updated `ci.yml` (--seed 0 deleted). No new symbols, no
new files — this phase produces only modifications to existing files. (DET-01, DET-02 — LD-8.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/milestone-cicd/SYNTHESIS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MailboxCase — default async: false, drop shared:, remove unused async? (DET-01)</name>
  <files>
    mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
  </files>
  <read_first>
    mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex (read in full — all 139 lines)
  </read_first>
  <action>
Apply the LD-8 "Option B" fix to `mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex`.
Three sub-changes, all in the same file, applied together:

**Sub-change A — Remove the async? computation and drop shared: from setup/1 (lines 91–101):**

The current `setup tags do` block opens with:

    async? = Map.get(tags, :async, true)

and later calls:

    pid = Ecto.Adapters.SQL.Sandbox.start_owner!(repo, shared: not async?)

Replace both lines. Remove the `async?` binding entirely (do not rename it to `_async?` —
remove the computation; it no longer serves a purpose). Change the `start_owner!` call to
plain ownership checkout with no keyword args:

    pid = Ecto.Adapters.SQL.Sandbox.start_owner!(repo)

CRITICAL — compile clean: `mailbox_case.ex` ships in `lib/` and is compiled under
`mix compile --no-optional-deps --warnings-as-errors`. An unused variable is a hard error.
Removing `async?` entirely avoids the warning. Do NOT leave a bound-but-unused `async?` in scope.

**Sub-change B — Rewrite the moduledoc `## Default setup` bullet (line 48):**

Current text:

    - `Ecto.Adapters.SQL.Sandbox.start_owner!(repo, shared: not tags[:async])`

Replace with:

    - `Ecto.Adapters.SQL.Sandbox.start_owner!(repo)` — plain ownership checkout; the suite runs serial by default (`async: false`)

**Sub-change C — Rewrite the moduledoc `## Supported tags` entry for `@tag async: false` (line 61):**

Current text (line 61):

    - `@tag async: false` — disable async (sandbox checks out in shared mode)

Replace with:

    - `@tag async: false` — always set; `MailboxCase` defaults serial execution

After all three sub-changes: run `grep -n 'shared:' mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex`.
The result MUST be empty. If any match remains, the task is not done.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/mailglass/mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors 2>&1 | tail -5
      grep -n 'shared:' /Users/jon/projects/mailglass/mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex; echo "shared: count: $?"
    </automated>
  </verify>
  <done>
    - `grep -n 'shared:' mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` returns no output (exit 1 = no matches)
    - `mix compile --no-optional-deps --warnings-as-errors` in `mailglass_inbound/` exits 0 with no warnings
    - The `async?` variable is gone from `setup/1` — not renamed, deleted
    - `start_owner!(repo)` has no keyword args
  </done>
</task>

<task type="auto">
  <name>Task 2: Update inbound-testing.md "Why async: false" + table row; delete --seed 0 from ci.yml (DET-02)</name>
  <files>
    mailglass_inbound/docs/inbound-testing.md
    .github/workflows/ci.yml
  </files>
  <read_first>
    mailglass_inbound/docs/inbound-testing.md lines 24–60 (the "Why async: false" section and Supported tags table)
    .github/workflows/ci.yml lines 363–375 (the "Run inbound tests" step)
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs lines 319–336 (the contract test — to confirm which substrings must survive)
  </read_first>
  <action>
**Part A — Rewrite `inbound-testing.md` §24–32 ("Why async: false is required"):**

The current section states the reason is shared-mode sandbox + ETS process-global state. After the
LD-8 fix, shared-mode is gone. The ETS reset rationale (CertCache, S3Fetcher.Fake) remains valid.
Rewrite the explanatory paragraph so it no longer says "shared mode":

Current lines 26–30:

    `MailboxCase` checks out an Ecto sandbox in shared mode and resets ETS-backed
    state (the SES cert cache, the S3 fetcher seam) in its `setup` callback. Both
    are process-global. Running `async: true` MailboxCase tests concurrently causes
    non-deterministic sandbox ownership conflicts and shared-state bleed across
    tests.

Replace with:

    `MailboxCase` resets ETS-backed state (the SES cert cache, the S3 fetcher seam)
    in its `setup` callback. Both are process-global. Running `async: true` MailboxCase
    tests concurrently causes non-deterministic shared-state bleed across tests.
    `MailboxCase` defaults to serial execution to prevent this by construction.

The mandatory rule line (line 32) MUST remain verbatim:

    The rule is absolute: **always `use MailglassInbound.MailboxCase, async: false`**.

Verify the contract test tokens still appear in the file:
- `"use MailglassInbound.MailboxCase, async: false"` (line 17 code block — untouched)
- `"always \`use MailglassInbound.MailboxCase, async: false\`"` (line 32 — must remain)

**Part B — Update the Supported tags table row (line 59):**

Current:

    | `@tag async: false` | Always set — sandbox is in shared mode |

Replace with:

    | `@tag async: false` | Always set — `MailboxCase` defaults serial execution |

NOTE: Line 510's `shared: true` in the StreamData property-test example is in a DIFFERENT context
(1000-iteration TRUNCATE pattern with an explicit `Sandbox.start_owner!/2` call — not MailboxCase).
Do NOT touch line 510 or its surrounding example; that is a separate adopter pattern that correctly
uses `shared: true` for a different reason.

**Part C — Delete `--seed 0` and its explanatory comment block from `.github/workflows/ci.yml`:**

The "Run inbound tests" step currently (lines 363–371) reads:

      - name: Run inbound tests
        # --exclude property: the 1000-run convergence property is gated
        # separately in the step below; this runs the fast unit subset first.
        # --seed 0: this suite has a known cross-file test-isolation flake under
        # random parallel ordering (duplicate-fingerprint / pool contention); a
        # pinned seed is the documented deterministic-green workaround. The real
        # isolation fix is tracked for v1.4 (Release Discipline / green main).
        working-directory: mailglass_inbound
        run: mix test --exclude property --seed 0

Delete the three-line `--seed 0` comment block (lines 366–369) and remove ` --seed 0` from
the `run:` line. Keep the `--exclude property` comment (lines 364–365) and the rest of the step.
Result:

      - name: Run inbound tests
        # --exclude property: the 1000-run convergence property is gated
        # separately in the step below; this runs the fast unit subset first.
        working-directory: mailglass_inbound
        run: mix test --exclude property

After editing: run `grep -n 'seed 0' .github/workflows/ci.yml`. The result MUST be empty.
  </action>
  <verify>
    <automated>
      grep -n 'seed 0' /Users/jon/projects/mailglass/.github/workflows/ci.yml; echo "seed 0 count: $?"
      grep -n 'shared mode' /Users/jon/projects/mailglass/mailglass_inbound/docs/inbound-testing.md | head -5
      cd /Users/jon/projects/mailglass/mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs --seed 12345 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    - `grep -n 'seed 0' .github/workflows/ci.yml` returns no output (DET-02 gate)
    - `grep -n 'shared mode' inbound-testing.md` returns no matches in the MailboxCase sections (lines 24–60); line 510 "shared: true" in the StreamData example is untouched
    - `docs_contract_test.exs` passes: both `"use MailglassInbound.MailboxCase, async: false"` and `"always \`use MailglassInbound.MailboxCase, async: false\`"` are still present in `inbound-testing.md`
    - The "Run inbound tests" `run:` line in `ci.yml` is exactly `mix test --exclude property`
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove determinism across 20 random seeds (DET-02 acceptance gate)</name>
  <files>
    (no files modified — verification only)
  </files>
  <read_first>
    (none — runs the test suite)
  </read_first>
  <action>
Run the inbound suite 20 times with independent random seeds to prove the fix is deterministic by
construction, not accidentally passing. This is the DET-02 "green across 20 random-seed runs" gate.

Execute the following loop from the repo root. Each iteration uses a fresh random seed (via `$RANDOM`
which produces a different value each invocation). Exit immediately on first failure.

    for i in $(seq 1 20); do
      echo "--- Seed run $i ---"
      (cd mailglass_inbound && mix test --exclude property --seed $RANDOM) || exit 1
    done

If any iteration fails, the loop exits non-zero and the task is NOT done. Investigate the failure
before marking complete.

NOTE on scope (per memory — inbound suite flake): `--exclude property` excludes the phase-45
1000-iteration StreamData convergence test which has a separate DB-pool TCP flake. DET applies
only to the unit subset (`--exclude property`), which is exactly what the ci.yml step runs.
The property test convergence step is out of scope for this phase.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/mailglass && for i in $(seq 1 20); do echo "--- Run $i ---"; (cd mailglass_inbound && mix test --exclude property --seed $RANDOM) || exit 1; done && echo "ALL 20 SEEDS PASSED"
    </automated>
  </verify>
  <done>
    - All 20 seed runs exit 0
    - The final line of output is "ALL 20 SEEDS PASSED"
    - No test flakes observed across the 20 runs
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → CI YAML | ci.yml is read by GitHub Actions; changes to run: commands affect what executes in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-01 | Tampering | ci.yml run: line edit | low | accept | Change is a deletion (removing --seed 0), not an addition; simpler post-edit than pre-edit; diff reviewable at a glance |
| T-127-02 | Repudiation | moduledoc rewrite | low | accept | The change is committed with a conventional commit message referencing LD-8; git log is the audit trail |
| T-127-03 | Information Disclosure | test suite output | low | accept | Test output is already visible in CI; no PII in test names or assertions |
</threat_model>

<verification>
Full phase acceptance (all must be true before marking Phase 127 complete):

1. `grep -n 'shared:' mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` → empty (DET-01)
2. `grep -n 'seed 0' .github/workflows/ci.yml` → empty (DET-02)
3. `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` → exits 0
4. `cd mailglass_inbound && mix test test/mailglass_inbound/docs_contract_test.exs` → passes
5. 20-seed determinism loop exits 0 with "ALL 20 SEEDS PASSED"
</verification>

<success_criteria>
1. The inbound suite is deterministic by construction: `MailboxCase` defaults `async: false`, uses plain `start_owner!(repo)` with no `shared:` arg — `grep shared: mailbox_case.ex` is empty (DET-01).
2. `--seed 0` is deleted from `ci.yml` and appears nowhere in the file (DET-02).
3. The inbound suite is green across 20 random-seed runs with `--exclude property` (DET-02).
4. `mailglass_inbound` compiles clean under `--no-optional-deps --warnings-as-errors` — no unused-variable warning from the removed `async?` binding.
5. `docs_contract_test.exs` remains green — `inbound-testing.md` retains the two required `async: false` substrings.
</success_criteria>

<output>
## Artifacts this phase produces

This phase produces **no new symbols and no new files**. All changes are modifications to existing files:

- `mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` — setup default + start_owner! call + moduledoc rewritten
- `mailglass_inbound/docs/inbound-testing.md` — "Why async: false" paragraph + table row rewritten
- `.github/workflows/ci.yml` — `--seed 0` flag and its stale comment block deleted

Create `.planning/phases/127-inbound-test-determinism/127-01-SUMMARY.md` when done.
</output>
