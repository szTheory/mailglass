---
phase: 121-inbound-surface-redesign
plan: 04
type: execute
wave: 3
depends_on: ["121-01", "121-02", "121-03"]
files_modified:
  - mailglass_admin/e2e/operator.spec.js
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/e2e/flows.spec.js
  - reference/demo_app/assets/e2e/persona-screenshots.spec.js
autonomous: true
requirements: [INB-01]

must_haves:
  truths:
    - "The operator browser gate (operator.spec.js:462-476) no longer asserts inbound-orientation visible on a POPULATED inbound view; it is split — the preview-orientation assertion is kept, the inbound assertion is replaced (D-15)."
    - "A NEW Inbound empty-pane-only judgment gate (modeled on the Deliveries gate operator.spec.js:402-456) asserts by data-testid count/presence: POPULATED → inbound-orientation count 0, inbound-filters visible; NO-DATA → inbound-empty-truly count 1, inbound-orientation count 1, inbound-filters count 0 (security boundary), inbound-master-detail count 0; NO-MATCH → inbound-filters visible, inbound-orientation count 0 (D-16)."
    - "NEW e2e assertions cover the two replay-modal behaviors on BOTH surfaces (Tab wraps last→first; Confirm disabled after first click) and the reveal disclosure (aria-expanded false→true) + re-redact collapse + aria-live region presence (D-16)."
    - "structural.spec.js inbound block (~1103-1264) is scanned and any inbound-orientation-on-populated or health-strip-over-empty assumption is updated; the locked PII boundary 1176-1177 (inbound-evidence-redacted visible + inbound-evidence-raw count 0 on first selection) is NOT weakened (D-15, D-10)."
    - "The operator-inbound persona cells are re-shot in one pass (no new cells) for only-forward D-THEME-PARITY proof; if the re-shoot env is unreachable, the proof is carried as an explicit open follow-up to Phase 123, not assumed (D-17)."
    - "The inherited v1.13 ratchet floor + D-THEME-PARITY (light/dark/system) is held green only-forward via the enumerated commands; the committed app.css is unchanged (no pillar re-score — Phase 123) (D-18)."
  artifacts:
    - "mailglass_admin/e2e/operator.spec.js (462-476 split; NEW Inbound empty-pane-only judgment test; NEW reveal-disclosure + re-redact + replay a11y assertions)"
    - "mailglass_admin/e2e/structural.spec.js (inbound block scanned + updated; 1176-1177 preserved)"
    - "mailglass_admin/e2e/flows.spec.js (NEW replay-modal Tab-trap + double-submit assertions on inbound + operator)"
    - "reference/demo_app/assets/e2e/persona-screenshots.spec.js (re-run only — operator-inbound cells, no code edit) + a per-cell matrix ledger in the SUMMARY"
  key_links:
    - "the Inbound judgment test keys on inbound-orientation / inbound-filters / inbound-empty-truly / inbound-master-detail testids emitted by Plan 01's render conditions"
    - "the reveal-disclosure assertions key on aria-expanded / aria-controls + inbound-evidence-re-redact + role=status emitted by Plan 02"
    - "the replay assertions key on the Tab-trap + phx-disable-with='Replaying…' from Plan 03 on both inbound-replay-confirm and operator-replay-confirm"
---

<objective>
Land the mandatory same-phase paired Playwright updates (D-15) that go RED the moment Plans 01-03's render-
condition + a11y changes land on the green-only-forward floor, add the new Inbound empty-pane-only judgment
gate (D-16), add the new a11y e2e coverage for the reveal disclosure + both replay modals (D-16), verify the
structural inbound block (D-15) WITHOUT weakening the locked PII boundary (D-10), and re-shoot the
operator-inbound persona cells for only-forward proof (D-17) — all while holding the v1.13 ratchet floor +
D-THEME-PARITY green only-forward and leaving the committed app.css byte-unchanged (D-18, no pillar re-score —
that is Phase 123).

This is the test/verification half of Phase 121. It asserts the behaviors built in Plans 01 (IA gating + data_state
+ D-07 copy), 02 (reveal disclosure + re-redact + telemetry), and 03 (replay Tab-trap + double-submit), so it
runs in the final wave after all three land.

Purpose: Lock the D-02/D-04 single-calm-pane end-state and the new a11y behaviors into the operator browser
ratchet (armed into the permanent floor in Phase 123), and produce the only-forward persona + matrix evidence.

Output: split paired test, new Inbound judgment gate, new reveal + replay a11y assertions, verified structural
block, re-shot persona cells, and a per-cell state×viewport×theme matrix ledger in the SUMMARY. Zero source
changes; zero app.css change (D-18).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/121-inbound-surface-redesign/121-CONTEXT.md
@.planning/phases/121-inbound-surface-redesign/121-PATTERNS.md
@.planning/phases/121-inbound-surface-redesign/121-UI-SPEC.md

# The shipped Deliveries judgment gate to model the Inbound gate on
@mailglass_admin/e2e/operator.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the paired orientation test + add the Inbound empty-pane-only judgment gate (D-15, D-16)</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js:402-456 (the shipped Deliveries empty-pane-only judgment gate to mirror) and :458-476 (the "inbound and preview surfaces render their orientation strips" test to SPLIT — :468 asserts inbound-orientation visible on a populated /ops/mail/inbound view)
    - mailglass_admin/e2e/operator.spec.js (openOperator helper + tenantId + the browser-login/browser-empty-tenant pattern used by the Deliveries gate at :434-436 for a genuine-no-data tenant)
    - .planning/phases/121-inbound-surface-redesign/121-PATTERNS.md (Test gate — paired updates + new judgment gate, with the Inbound gate testid expectations)
    - .planning/phases/121-inbound-surface-redesign/121-UI-SPEC.md (Test Gate Contract table)
  </read_first>
  <action>
    In operator.spec.js: SPLIT the "inbound and preview surfaces render their orientation strips" test
    (462-476). KEEP the preview-orientation assertion (470-475 — preview strip is unchanged this phase). REMOVE
    the inbound assertion that inbound-orientation is visible on the populated /ops/mail/inbound?tenant_id=…
    view (:467-468). Then ADD a new Inbound empty-pane-only judgment test in the judgment describe block,
    modeled verbatim on the Deliveries gate (402-456), asserting by data-testid count/presence (NEVER
    pixel/CSS-visibility — the markers are style="display:none" divs):
      - POPULATED (/ops/mail/inbound?tenant_id=${tenantId}): inbound-orientation toHaveCount(0); inbound-filters toBeVisible().
      - NO-DATA (a genuine-empty tenant via the browser-login/browser-empty-tenant pattern the Deliveries gate uses): inbound-empty-truly toHaveCount(1); inbound-orientation toHaveCount(1); inbound-filters toHaveCount(0) (the security-boundary assertion — D-02/D-05); inbound-master-detail toHaveCount(0).
      - NO-MATCH (an active filter matching zero rows, e.g. provider=no-such-provider): inbound-filters toBeVisible(); inbound-orientation toHaveCount(0).
    Reuse the Deliveries gate's tenant/login plumbing; do NOT introduce new seed routes. Add NO source change.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c "inbound-empty-truly\|inbound-master-detail" e2e/operator.spec.js && node -e "require('child_process').execSync('npx playwright test e2e/operator.spec.js --list', {stdio:'inherit'})" 2>&1 | tail -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - operator.spec.js no longer asserts `inbound-orientation` is toBeVisible() on a populated /ops/mail/inbound view (the :468 assertion is removed); the preview-orientation assertion is retained.
    - A new test asserts the Inbound POPULATED/NO-DATA/NO-MATCH judgment matrix using `.toHaveCount(...)` / `.toBeVisible()` exactly as specified (inbound-orientation count 0/1/0; inbound-filters visible/count-0/visible; inbound-empty-truly count 1 and inbound-master-detail count 0 on no-data).
    - `grep -c "inbound-empty-truly" e2e/operator.spec.js` ≥ 1 and `grep -c "inbound-master-detail" e2e/operator.spec.js` ≥ 1.
    - `npx playwright test e2e/operator.spec.js --list` enumerates the new test (parses without syntax error).
  </acceptance_criteria>
  <done>The paired inbound-orientation test is split (preview kept, inbound replaced) and a new Inbound empty-pane-only judgment gate asserts the POPULATED/NO-DATA/NO-MATCH testid matrix by count; the spec parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add reveal-disclosure + replay-modal a11y e2e on both surfaces; verify the structural inbound block (D-16, D-15, D-10)</name>
  <files>mailglass_admin/e2e/flows.spec.js, mailglass_admin/e2e/operator.spec.js, mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/flows.spec.js:100-130 (openInboundReplayModal helper) and the operator replay-modal flow helper; the existing inbound-replay-modal assertions (113-115, 404, 512)
    - mailglass_admin/e2e/structural.spec.js:1103-1264 (the inbound block — verify still green) and specifically 1176-1177 (the LOCKED PII boundary: inbound-evidence-redacted visible + inbound-evidence-raw count 0 on first selection — do NOT weaken)
    - .planning/phases/121-inbound-surface-redesign/121-UI-SPEC.md (A11y Contract rows: reveal disclosure, aria-live, replay focus-trap, replay double-submit lock) and Test Gate Contract (the NEW a11y e2e row)
  </read_first>
  <action>
    In flows.spec.js (and/or operator.spec.js a11y describe — keep both surfaces covered): ADD assertions for
    the two new replay-modal behaviors on BOTH the inbound and operator replay modals:
      (1) Tab focus-trap — open the modal, Tab from the last focusable wraps to the first (focus stays inside the
          dialog); assert document focus remains within the modal element after pressing Tab past the last
          control.
      (2) Double-submit lock — after the first click on inbound-replay-confirm / operator-replay-confirm, the
          button is disabled (assert toBeDisabled() or the "Replaying…" pending state), so a second click cannot
          re-fire.
    ADD reveal-disclosure assertions: on a selected record with a granting operator, the inbound-evidence-reveal
    button has aria-expanded transitioning false→true on click and aria-controls="inbound-evidence-raw"; the
    inbound-evidence-re-redact button appears in :revealed and collapses back to redacted (inbound-evidence-raw
    count 0 again); the role="status" aria-live region is present. Then SCAN the structural.spec.js inbound block
    (1103-1264) for any inbound-orientation-on-populated or health-strip-over-empty assumption and update it to
    match Plan 01's render conditions — but DO NOT weaken 1176-1177 (the redacted-by-default boundary stays:
    inbound-evidence-redacted visible + inbound-evidence-raw count 0 on first selection). Add NO source change;
    if a structural assertion legitimately needs no change, note that in the SUMMARY rather than editing it.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c 'inbound-evidence-redacted' e2e/structural.spec.js && grep -c 'aria-expanded' e2e/flows.spec.js e2e/operator.spec.js && npx playwright test e2e/flows.spec.js e2e/structural.spec.js --list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - structural.spec.js still asserts at line ~1176-1177 (or its post-edit equivalent) that inbound-evidence-redacted is visible AND inbound-evidence-raw toHaveCount(0) on first selection — the locked PII boundary is NOT weakened (grep confirms both testids still present).
    - flows.spec.js (and/or operator.spec.js a11y describe) contains: a Tab focus-trap assertion (focus stays within the modal after Tab) on BOTH inbound and operator replay modals; a double-submit assertion (Confirm toBeDisabled() / "Replaying…" after first click) on both; reveal aria-expanded false→true + aria-controls="inbound-evidence-raw"; an inbound-evidence-re-redact collapse returning inbound-evidence-raw to count 0; a role="status" aria-live region presence check.
    - `npx playwright test e2e/flows.spec.js e2e/structural.spec.js --list` enumerates the new tests (specs parse without syntax error).
    - No source file under mailglass_admin/lib is modified by this task.
  </acceptance_criteria>
  <done>Both replay modals' Tab-trap + double-submit and the reveal disclosure + re-redact + aria-live are e2e-asserted on both surfaces; the structural inbound block is verified with the PII boundary preserved; specs parse.</done>
</task>

<task type="auto">
  <name>Task 3: Re-shoot the operator-inbound persona cells + record the matrix ledger (D-17, D-18)</name>
  <files>reference/demo_app/assets/e2e/persona-screenshots.spec.js</files>
  <read_first>
    - reference/demo_app/assets/e2e/persona-screenshots.spec.js:69,120-122 (the operator-inbound cell enumeration × {northstar, fjordline-aps, helios-void} × {375,1440} × {light,dark} — re-run, NO new cells)
    - .planning/research/v1.14/DEFECT-REGISTER.md (Inbound row 288 — "no net-new headline defect; 121 applies 120's cleanup") for the persona-evidence framing
    - the project memory note: PERSIST_AXE_BASELINE / persona producer runs locally (node_modules + Chromium present); the persona producer auto-boots the operator browser server
  </read_first>
  <action>
    Re-run the persona-screenshots producer for the operator-inbound cells ONLY (no new cells — D-17): in one
    pass across {northstar, fjordline-aps, helios-void} × {375,1440} × {light,dark}. helios-void (zero-data) now
    proves the calm single-pane no-data IA; northstar proves error/high-count; fjordline-aps proves
    long-ID/non-ASCII/null. This is a re-run, not a code edit to the spec. Then record a per-cell
    state×viewport×theme matrix ledger in the SUMMARY: for each of the required states (happy / empty / no-match /
    loading / error / permission-denied / boundary / disconnected-reconnect) × {320/375/768/1024/1440/wide} ×
    {light/dark/system}, mark each cell automated / re-shoot-covered / documented-human-follow-up — none silently
    assumed. Hold the v1.13 ratchet floor + D-THEME-PARITY green only-forward by running the enumerated floor
    commands (token_parity_test.exs, verify.support_contract.admin, the ratchet_baseline + axe_baseline
    comparators, check-conformance + check-conformance-advisory) and confirming the committed app.css is
    byte-unchanged — NO pillar re-score (Phase 123). If the persona re-shoot environment is unreachable, carry
    the D-THEME-PARITY proof as an EXPLICIT open follow-up to Phase 123 in the SUMMARY rather than asserting it
    satisfied.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components/token_parity_test.exs 2>&1 | tail -5 && git diff --stat priv/static/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mailglass_admin/components/token_parity_test.exs` reports 0 failures (the inherited floor holds; D-18 TokenParity landmine not tripped).
    - `git diff --stat mailglass_admin/priv/static/app.css` shows no change (no mix assets.build; committed bundle is canonical — D-18).
    - The SUMMARY contains a per-cell state×viewport×theme matrix ledger where every cell is marked automated / re-shoot-covered / documented-human-follow-up — none silently assumed.
    - The operator-inbound persona cells are re-run (helios-void / northstar / fjordline-aps × {375,1440} × {light,dark}) OR, if the env is unreachable, the SUMMARY carries D-THEME-PARITY proof as an explicit Phase-123 follow-up.
    - No persona-screenshots.spec.js code edit (re-run only — no new cells, D-17).
  </acceptance_criteria>
  <done>The operator-inbound persona cells are re-shot (or the proof is explicitly deferred to Phase 123); the v1.13 ratchet floor + TokenParity hold green only-forward; app.css is byte-unchanged; the SUMMARY records the full per-cell matrix ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| genuine no-data render → tenant scope | The judgment gate's `inbound-filters` count-0 assertion is the regression alarm for any future re-introduction of the scope-widening toolbar in no-data |
| first-selection render → PII | The locked structural boundary (inbound-evidence-redacted visible + raw count 0 on first selection) is the regression alarm for any future default-reveal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-10 | Elevation of Privilege | Inbound judgment gate no-data security boundary (D-16) | high | mitigate | The new judgment gate asserts `inbound-filters` toHaveCount(0) on genuine no-data — arming a browser regression gate so any future change re-introducing the tenant-scope-widening toolbar in no-data fails CI. Asserting guard: Task 1 judgment test. |
| T-121-11 | Information Disclosure | locked PII boundary structural.spec.js:1176-1177 (D-10) | high | mitigate | The redacted-by-default boundary (inbound-evidence-redacted visible + inbound-evidence-raw count 0 on first selection) is explicitly preserved un-weakened; Task 2 grep confirms both testids remain. Any future default-reveal trips this gate. |
| T-121-12 | Tampering | TokenParity / committed app.css (D-18) | medium | mitigate | The floor verification asserts the committed app.css is byte-unchanged and token_parity_test.exs is green — preventing a naive mix assets.build from committing raw-inline daisyUI theme blocks that break the gate. Asserting guard: Task 3 token_parity_test + app.css diff. |
</threat_model>

<verification>
- `cd mailglass_admin && npx playwright test e2e/operator.spec.js e2e/flows.spec.js e2e/structural.spec.js --list` enumerates the new tests (specs parse).
- `cd mailglass_admin && mix test test/mailglass_admin/components/token_parity_test.exs` reports 0 failures.
- `git diff --stat mailglass_admin/priv/static/app.css` shows no change.
- structural.spec.js still asserts inbound-evidence-redacted visible + inbound-evidence-raw count 0 on first selection (PII boundary un-weakened).
- The SUMMARY carries the per-cell state×viewport×theme matrix ledger and the persona re-shoot result (or explicit Phase-123 deferral).
</verification>

<success_criteria>
The paired inbound-orientation test is split; a new Inbound empty-pane-only judgment gate locks the POPULATED/NO-DATA/NO-MATCH testid matrix (incl. the no-data security boundary); the reveal disclosure + re-redact + aria-live and both replay modals' Tab-trap + double-submit are e2e-asserted on both surfaces; the structural inbound block is verified with the PII boundary preserved; the operator-inbound persona cells are re-shot (or deferred explicitly); the v1.13 ratchet floor + TokenParity hold green only-forward with app.css byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/121-inbound-surface-redesign/121-04-SUMMARY.md` when done.
</output>