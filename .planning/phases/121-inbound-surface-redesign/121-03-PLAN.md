---
phase: 121-inbound-surface-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
autonomous: true
requirements: [INB-01]

must_haves:
  truths:
    - "Both replay modals (inbound/replay_modal.ex + operator/replay_modal.ex) contain a Tab / Shift+Tab focus-trap so keyboard focus wraps last→first / first→last and never escapes the open dialog (D-14, the one unmet APG line item) — applied IDENTICALLY across both surfaces (lockstep)."
    - "Both replay modals' Confirm button is double-submit-locked: it disables after the first click and shows the calm pending label 'Replaying…' (D-14), preventing the render→click double-fire."
    - "All existing modal APG affordances are preserved un-regressed: role=dialog, aria-modal, aria-labelledby, Escape via phx-key, initial focus, focus-restore to trigger, scrim/overscroll, reduced-motion neutralizer (D-14 — do NOT regress)."
    - "The replay gate order TENANT → CAPABILITY(:replay_inbound) → REPLAY, the :no_match-can-never-replay rule, and the struct-matched error copy are untouched (they live in inbound_live.ex confirm flow, not the modal — out of this plan's file scope) (D-13)."
  artifacts:
    - "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (Tab focus-trap + phx-disable-with='Replaying…' on inbound-replay-confirm)"
    - "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (the SAME Tab focus-trap + phx-disable-with='Replaying…' on operator-replay-confirm — identical implementation)"
  key_links:
    - "the focus-trap implementation (Claude's discretion: LiveView.JS focus-sentinel spans vs. scoped Tab keydown) is pure Phoenix/LiveView.JS, no new npm deps, and is byte-identical across the two modals"
    - "phx-disable-with='Replaying…' (or JS.set_attribute(disabled) + pending label) on the Confirm button is the double-submit lock; the idle label 'Confirm replay' is unchanged"
---

<objective>
Close the two genuine APG gaps on the replay confirmation modal, applied IDENTICALLY to BOTH replay modals
(`inbound/replay_modal.ex` + `operator/replay_modal.ex`) for cross-surface coherence (D-14, the two modals are
explicit lockstep siblings — the inbound docstring calls itself "Sibling of MailglassAdmin.Operator.ReplayModal,
SIMPLIFIED"):

1. **Tab / Shift+Tab focus-trap** — wrap last→first / first→last within the open dialog. `JS.focus_first` already
   sets INITIAL focus (in the LiveView span, not the modal) but does NOT contain Tab — this is the one APG line
   item currently unmet. Pick the least-surprise, smallest-correct-JS option (LiveView.JS focus-sentinel spans
   vs. a scoped Tab keydown handler — Claude's discretion); both are pure Phoenix/LiveView.JS, no new npm deps.
   Keep the two modals byte-identical in approach.
2. **Double-submit pending-lock** on the Confirm button — `phx-disable-with="Replaying…"` (or
   `JS.set_attribute(disabled)` + a calm pending label) so a second click cannot double-fire the
   render→click path the code already worries about. The idle label "Confirm replay" is unchanged.

Everything else the modal already does right (role="dialog", aria-modal, aria-labelledby, Escape via phx-key,
initial focus, focus-restore to trigger, scrim/overscroll, reduced-motion neutralizer) is already
APG-conformant and e2e-asserted — do NOT regress. The replay gate order, :no_match rule, and struct-matched
error copy live in inbound_live.ex's confirm flow (NOT the modal) and are explicitly out of this plan's file
scope (D-13). Background-content inert/aria-hidden and the btn-error-color question are deferred (Phase 123+).

Purpose: Bring both replay modals to full APG conformance for predictable consequential-action dialogs
(roadmap success-criterion 3), without downgrading the deliberate full-modal confirmation weight (D-13).

Output: Tab focus-trap + double-submit lock on both modal components, identical implementation, reusing only
existing classes / LiveView.JS (D-18 — no new Tailwind utility, no mix assets.build).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/121-inbound-surface-redesign/121-CONTEXT.md
@.planning/phases/121-inbound-surface-redesign/121-PATTERNS.md
@.planning/phases/121-inbound-surface-redesign/121-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Tab/Shift+Tab focus-trap to both replay modals (D-14, lockstep)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex (the full modal; role=dialog 32-34, phx-key="Escape" phx-window-keydown="close_replay" 35-36, the Close button 55-57, Cancel 61-63, Confirm 64-71)
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex (the sibling; role=dialog 27-33, the same Escape wiring 31-32, Close 51-53, Cancel 99-101, Confirm 102-110; this modal has additional focusable controls — target radios — inside the :ambiguous branch, so the focus-trap must wrap whatever focusables are present)
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex:527-531 (the existing JS.focus_first(to: "#inbound-replay-modal") initial-focus span — initial focus only, no Tab containment)
  </read_first>
  <action>
    Add a Tab / Shift+Tab focus-trap to BOTH modals so focus wraps last→first (Tab on the last focusable) and
    first→last (Shift+Tab on the first focusable) and never leaves the open dialog. Use the least-surprise,
    smallest-correct pure-Phoenix/LiveView.JS option (Claude's discretion): EITHER (a) focus-sentinel spans —
    a `tabindex="0"` sentinel span at the dialog's start and end whose `phx-mounted`/focus handler bounces focus
    to the last/first real control via `JS.focus(to: ...)`; OR (b) a scoped `phx-keydown` Tab handler on the
    dialog that calls a JS command to redirect focus at the boundaries. Whichever you pick, implement it
    IDENTICALLY in both modals (the inbound modal's focusables are Close/Cancel/Confirm; the operator modal also
    has the target radios in its :ambiguous branch — the trap must wrap the actual first/last focusable in the
    current render). Add NO new npm dependency and NO new Tailwind class. Do NOT remove or alter the existing
    role=dialog / aria-modal / aria-labelledby / Escape / scrim / overscroll / reduced-motion attributes, and do
    NOT touch the inbound_live.ex / operator_live.ex initial-focus + focus-restore spans (they stay — initial
    focus and focus-restore are already correct; this task adds only Tab containment).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3 && git diff --stat priv/static/app.css</automated>
  </verify>
  <acceptance_criteria>
    - Both inbound/replay_modal.ex and operator/replay_modal.ex contain new Tab-containment wiring (focus-sentinel spans OR a scoped Tab keydown handler) — the same mechanism in both files.
    - The existing `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and `phx-key="Escape"` attributes are still present in both modals (grep confirms none removed).
    - No new npm dependency is introduced (no package.json / assets/vendor change); the focus-trap is pure Phoenix/LiveView.JS.
    - `mix compile --warnings-as-errors` exits 0.
    - `git diff --stat priv/static/app.css` shows no change (no new Tailwind utility / no assets.build — D-18).
  </acceptance_criteria>
  <done>Both replay modals trap Tab/Shift+Tab focus within the open dialog via an identical pure-LiveView.JS mechanism; all prior APG attributes are preserved; compile is clean; app.css is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the double-submit pending-lock to both Confirm buttons (D-14, lockstep)</name>
  <files>mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex:64-71 (the inbound-replay-confirm button: phx-click="confirm_replay", class "btn btn-error min-h-11 px-5", label "Confirm replay")
    - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex:102-110 (the operator-replay-confirm button, rendered only when confirm_enabled?/2 — same phx-click="confirm_replay", same classes/label)
    - .planning/phases/121-inbound-surface-redesign/121-UI-SPEC.md (Copywriting Contract: Replay Confirm pending = "Replaying…", NEW — D-14; idle "Confirm replay" frozen)
  </read_first>
  <action>
    Add a double-submit pending-lock to BOTH Confirm buttons so a second click after the first cannot re-fire
    confirm_replay. Add `phx-disable-with="Replaying…"` to the inbound-replay-confirm button
    (inbound/replay_modal.ex:64-71) and the operator-replay-confirm button (operator/replay_modal.ex:102-110)
    — the pending label "Replaying…" exactly per the UI-SPEC Copywriting Contract; the idle label "Confirm
    replay" stays unchanged. If `phx-disable-with` is insufficient for the surface's interaction model, fall back
    to `JS.set_attribute("disabled", to: ...)` on phx-click plus a pending-label swap — but keep the two modals'
    approach identical. Do NOT change the gate logic (confirm_replay is handled in the LiveView — out of file
    scope), do NOT change the btn-error class (the consequential-color question is deferred, D-14), add NO new
    Tailwind class, do NOT run mix assets.build.
  </action>
  <verify>
    <automated>cd mailglass_admin && grep -c 'Replaying…' lib/mailglass_admin/inbound/replay_modal.ex lib/mailglass_admin/operator/replay_modal.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Replaying…' lib/mailglass_admin/inbound/replay_modal.ex` returns at least 1, and the same for operator/replay_modal.ex (the pending label is present in BOTH).
    - The idle label "Confirm replay" is still present in both modals.
    - The `btn btn-error` class on the Confirm button is unchanged in both (consequential-color deferred).
    - `mix compile --warnings-as-errors` exits 0.
    - `git diff --stat priv/static/app.css` shows no change.
  </acceptance_criteria>
  <done>Both Confirm buttons carry the "Replaying…" double-submit pending-lock identically; idle copy and btn-error class are unchanged; compile clean; app.css untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator click → confirm_replay event | A double-fired Confirm could append two replay runs to the append-only ledger from one intent |
| keyboard focus → modal modality | Focus escaping the open dialog (no Tab trap) lets a keyboard operator interact with background controls behind the scrim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-04 | Tampering | Confirm button double-submit (both modals, D-14) | medium | mitigate | phx-disable-with="Replaying…" disables Confirm after the first click, preventing a render→click double-fire that would append a duplicate replay run to the append-only ledger. Asserting guard: Plan 04 e2e "Confirm disabled after first click" on both surfaces + the grep in Task 2. |
| T-121-05 | Information Disclosure | modal modality / focus-trap (both modals, D-14) | low | mitigate | Tab/Shift+Tab focus-trap keeps keyboard focus inside the dialog so background controls are unreachable while the consequential confirmation is open (the one unmet APG line). Asserting guard: Plan 04 e2e "Tab wraps last→first" on both surfaces. |
| T-121-06 | Tampering | replay gate order (inbound_live.ex confirm flow) | high | accept | The TENANT → CAPABILITY(:replay_inbound) → REPLAY gate, :no_match rule, and struct-matched error copy are unchanged and out of this plan's file scope (D-13) — this plan touches only the modal presentation, never the authorization flow. |
</threat_model>

<verification>
- `cd mailglass_admin && mix compile --warnings-as-errors` exits 0.
- `grep -c 'Replaying…'` returns ≥1 for both replay_modal.ex files.
- The Tab-containment mechanism and the pending-lock are present and identical across both modals.
- `git diff --stat mailglass_admin/priv/static/app.css` shows no change.
</verification>

<success_criteria>
Both replay modals trap Tab/Shift+Tab focus and double-submit-lock Confirm with the "Replaying…" pending label, via an identical pure-LiveView.JS implementation; all prior APG affordances are preserved; the replay authorization flow and btn-error color are untouched; the committed app.css is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/121-inbound-surface-redesign/121-03-SUMMARY.md` when done.
</output>
