---
phase: 111-forms
plan: "02"
type: execute
wave: 2
depends_on:
  - 111-01
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
  - mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
  - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
must_haves:
  truths:
    - "D-01/D-02/FORM-01: Operator and inbound filter form modules are thin wrappers over Components.filter_field/1 and Components.filter_section/1, with no duplicate label/control HEEx bodies."
    - "D-04/D-05/FORM-02: Invalid normalized-away filter values surface specific recovery copy connected to the relevant controls."
    - "D-07/FORM-03: Ordinary filter patches preserve stable form and control identity by keeping form IDs, control IDs, names, types, and order deterministic."
  artifacts:
    - path: mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
      provides: "Operator thin wrapper over shared filter primitives"
    - path: mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
      provides: "Inbound thin wrapper over shared filter primitives"
    - path: mailglass_admin/lib/mailglass_admin/operator_live.ex
      provides: "Operator filter recovery errors and stable filter assigns"
    - path: mailglass_admin/lib/mailglass_admin/inbound_live.ex
      provides: "Inbound filter recovery errors and stable filter assigns"
    - path: mailglass_admin/test/mailglass_admin/operator_live_test.exs
      provides: "Operator invalid-filter recovery and URL-backed state tests"
    - path: mailglass_admin/test/mailglass_admin/inbound_live_test.exs
      provides: "Inbound invalid-filter recovery and URL-backed state tests"
  key_links:
    - from: mailglass_admin/lib/mailglass_admin/operator_live.ex
      to: mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
      via: "`filter_form` and `filter_errors` assigns passed into `FiltersForm.fields/1`"
      pattern: "filter_errors"
    - from: mailglass_admin/lib/mailglass_admin/inbound_live.ex
      to: mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
      via: "`filter_form` and `filter_errors` assigns passed into `FiltersForm.fields/1`"
      pattern: "filter_errors"
---

<objective>
Migrate the duplicated operator and inbound filter forms onto the shared public primitives and add recovery-oriented validation metadata.

Purpose: This plan delivers the mandatory FORM-01 target while preserving existing URL-backed filter behavior. It keeps the existing wrapper modules only as thin composition wrappers per D-02 and adds recovery copy for normalized/dropped invalid filters per D-05.

Output: Shared-primitive consumers for operator and inbound filters, `filter_errors` assignment in both LiveViews, and LiveView tests proving URL behavior remains tenant-safe.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/111-forms/111-CONTEXT.md
@.planning/phases/111-forms/111-RESEARCH.md
@.planning/phases/111-forms/111-VALIDATION.md
@.planning/phases/111-forms/111-01-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
@mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/inbound_live.ex
@mailglass_admin/test/mailglass_admin/operator_live_test.exs
@mailglass_admin/test/mailglass_admin/inbound_live_test.exs
</context>

## Artifacts this phase produces

- `MailglassAdmin.Components.filter_field/1` and `filter_section/1` are consumed by both filter forms.
- `MailglassAdmin.Operator.FiltersForm.fields/1` remains only as a composition wrapper for operator filter field specs.
- `MailglassAdmin.Inbound.FiltersForm.fields/1` remains only as a composition wrapper for inbound filter field specs.
- `OperatorLive` and `InboundLive` assign `filter_errors` maps derived from raw URL/form values before normalization.
- Operator and inbound LiveView tests assert invalid filter recovery copy and unchanged tenant/data boundaries.
- Later plans add Preview/replay certification, gallery states, conformance, and browser focus proof.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert operator and inbound filter wrappers to primitive consumers</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/filters_form.ex, mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/components.ex
    @mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
    @mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
    @mailglass_admin/lib/mailglass_admin/operator_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @.planning/phases/111-forms/111-CONTEXT.md
  </read_first>
  <behavior>
    - Wrapper modules call `MailglassAdmin.Components.filter_section/1` and `filter_field/1` for every rendered control.
    - Operator fields remain in this stable order: Tenant, Provider, Status, Event, Time window.
    - Inbound fields remain in this stable order: Tenant, Provider, Mailbox outcome, Time window, Search.
    - Field IDs and names are derived from `@form[:field]` metadata so ordinary LiveView patches keep DOM identity.
  </behavior>
  <action>Keep `MailglassAdmin.Operator.FiltersForm` and `MailglassAdmin.Inbound.FiltersForm` as thin wrappers per D-02. Alias `MailglassAdmin.Components` in each wrapper, add an `errors` map attr with default `%{}`, and replace direct native control markup with `Components.filter_section` plus `Components.filter_field` calls. Preserve current public wrapper function names and current call-site ownership. Use exact labels `Tenant`, `Provider`, `Status`, `Event`, `Time window`, `Mailbox outcome`, and `Search`. Add concise help copy for each control, pass field-specific recovery errors from the `errors` map, and build select options from the existing `status_values`, `event_values`, `outcome_values`, and `window_options` assigns. The wrappers may keep local `label/1` helpers for option copy, but all label/control/help/error HEEx must live in the shared primitives.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs test/mailglass_admin/operator_live_test.exs test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `operator/filters_form.ex` contains `Components.filter_field` and `Components.filter_section`.
    - `inbound/filters_form.ex` contains `Components.filter_field` and `Components.filter_section`.
    - Operator wrapper output still includes the labels `Tenant`, `Provider`, `Status`, `Event`, and `Time window`.
    - Inbound wrapper output still includes the labels `Tenant`, `Provider`, `Mailbox outcome`, `Time window`, and `Search`.
    - Existing URL-backed filter tests for operator and inbound remain green.
  </acceptance_criteria>
  <done>Both filter wrappers consume shared primitives and no longer own duplicated label/control markup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recovery errors for invalid filter values</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex, mailglass_admin/lib/mailglass_admin/inbound_live.ex, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/test/mailglass_admin/inbound_live_test.exs</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/operator_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @mailglass_admin/test/mailglass_admin/operator_live_test.exs
    @mailglass_admin/test/mailglass_admin/inbound_live_test.exs
    @.planning/phases/111-forms/111-RESEARCH.md
    @.planning/phases/111-forms/111-VALIDATION.md
  </read_first>
  <behavior>
    - Invalid operator `status`, `event`, and non-positive/non-integer `window_hours` values never reach read-model filters and render field-level recovery text.
    - Invalid inbound `outcome` and non-positive/non-integer `window_hours` values never reach read-model filters and render field-level recovery text.
    - Valid filters still push URL-backed state and survive remounts.
    - Tenant-scoped reads remain bounded by the normalized tenant parameter and existing read-model paths.
  </behavior>
  <action>Add a `filter_errors` assign to `OperatorLive` and `InboundLive`. Use the planner-chosen side-map shape from the research open question: `%{"field_name" => "message"}`. Add helper functions that compute `{normalized_params, filter_errors}` from raw params without weakening enum allowlists or positive-integer parsing. `handle_params/3` should compute errors from raw URL params, assign normalized params, assign `to_form(normalized, as: :filters)`, and assign `filter_errors`. `validate_filters` should update the form and errors from raw form values. `apply_filters` should push a patch only when the error map is empty; when errors exist, keep the user on the current URL and render the normalized form plus errors so the user can correct them. Use exact recovery copy: `Status was not applied. Choose a listed status.`, `Event was not applied. Choose a listed event.`, `Mailbox outcome was not applied. Choose a listed outcome.`, and `Time window was not applied. Choose a positive listed time window.` Pass `filter_errors` into the filter wrapper calls in each render.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `operator_live.ex` assigns `:filter_errors` in mount/handle_params/validate_filters/apply_filters paths.
    - `inbound_live.ex` assigns `:filter_errors` in mount/handle_params/validate_filters/apply_filters paths.
    - Operator tests assert the exact status, event, and time-window recovery strings for invalid URL params or invalid submitted form values.
    - Inbound tests assert the exact outcome and time-window recovery strings for invalid URL params or invalid submitted form values.
    - Existing valid filter tests still assert the same URL patch paths and masked record visibility behavior.
  </acceptance_criteria>
  <done>Invalid filter values are recoverable at the field level without broadening tenant/data trust boundaries or breaking URL-backed filters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL/form params -> LiveView normalization | Untrusted filters cross into read-model query parameters. |
| LiveView assigns -> filter wrapper primitives | Recovery text and normalized values render back into the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Tampering | `normalize_filter_params` and recovery helpers | mitigate | Keep existing enum allowlists and positive-integer parsing; invalid values become recovery errors and do not reach read-model filters. |
| T-111-05 | Information Disclosure | tenant-scoped operator/inbound reads | mitigate | Preserve existing tenant short-circuit and read-model calls; tests assert foreign records do not appear. |
| T-111-06 | Denial of Service | invalid filter submissions | mitigate | Do not crash on `String.to_existing_atom/1`; invalid values render field errors and keep the form correctable. |
| T-111-SC | Tampering | package installs | mitigate | No package install task exists; use existing Mix and conformance tooling only. |
</threat_model>

<verification>
Plan-level automated checks:

- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs test/mailglass_admin/inbound_live_test.exs --warnings-as-errors`
- `cd mailglass_admin && ./scripts/check-conformance.sh`
</verification>

<success_criteria>
- Operator and inbound filter wrappers no longer own duplicated native label/control markup.
- Invalid operator and inbound filter values produce field-specific recovery copy connected through the shared primitive.
- Existing valid filter URL behavior and tenant-safe data boundaries remain green.
- No runtime dependency, screenshot workflow, pixel-diff workflow, tenant listing, theme persistence, or pagination work is introduced.
</success_criteria>

<output>
Create `.planning/phases/111-forms/111-02-SUMMARY.md` when done.
</output>
