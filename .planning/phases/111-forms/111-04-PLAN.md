---
phase: 111-forms
plan: "04"
type: execute
wave: 3
depends_on:
  - 111-02
  - 111-03
files_modified:
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/e2e/structural.spec.js
  - mailglass_admin/scripts/check-conformance.sh
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
must_haves:
  truths:
    - "D-08/FORM-01: Repo-local conformance fails if duplicated filter label/control markup returns outside MailglassAdmin.Components."
    - "D-08/FORM-02: Gallery and Playwright structural tests prove labels, help/error IDs, invalid state, and non-color cues."
    - "D-07/FORM-03: Browser tests prove focus remains on the same operator and inbound filter controls across ordinary LiveView patches."
  artifacts:
    - path: mailglass_admin/lib/mailglass_admin/gallery_live.ex
      provides: "Form primitive gallery states for filter_field/filter_section and migrated filters_form specimens"
    - path: mailglass_admin/e2e/structural.spec.js
      provides: "Browser structural assertions for form labels, disabled/read-only states, replay radios, and focus persistence"
    - path: mailglass_admin/scripts/check-conformance.sh
      provides: "FORM-DRIFT-GATE for duplicated filter markup"
  key_links:
    - from: mailglass_admin/scripts/check-conformance.sh
      to: mailglass_admin/lib/mailglass_admin/components.ex
      via: "requires one public shared primitive implementation"
      pattern: "filter_(field|section)"
    - from: mailglass_admin/e2e/structural.spec.js
      to: mailglass_admin/lib/mailglass_admin/operator_live.ex
      via: "focus Provider control, submit/apply filters, assert document.activeElement.id is unchanged"
      pattern: "operator-filters"
    - from: mailglass_admin/e2e/structural.spec.js
      to: mailglass_admin/lib/mailglass_admin/inbound_live.ex
      via: "focus Search control, submit/apply filters, assert document.activeElement.id is unchanged"
      pattern: "inbound-filters"
---

<objective>
Add the fail-closed guard and browser structural proof for the completed form layer.

Purpose: Phase 111 is not complete until shared primitives are certified in the gallery, duplicate filter markup is blocked by a deterministic conformance gate, and LiveView patch focus behavior is proven in a real browser per D-07 and D-08.

Output: Gallery form specimens, FORM drift conformance gate, Playwright assertions, and final phase gate commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/111-forms/111-CONTEXT.md
@.planning/phases/111-forms/111-RESEARCH.md
@.planning/phases/111-forms/111-VALIDATION.md
@.planning/phases/111-forms/111-02-SUMMARY.md
@.planning/phases/111-forms/111-03-SUMMARY.md
@.planning/phases/110-primitives/110-04-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/scripts/check-conformance.sh
</context>

## Artifacts this phase produces

- Gallery `filter_field` and `filter_section` specimens for empty, filled, invalid, disabled, readonly text, and display-only read-only non-text states.
- Updated gallery `filters_form` specimens that render the migrated shared-primitive wrappers.
- `FORM-DRIFT-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- Playwright structural tests for gallery form semantics, operator/inbound filter focus persistence, Preview assigns structure, and operator replay radio structure.
- Final phase verification using `mix verify.preview`, conformance, and `npm run test:operator-browser`.

<tasks>

<task type="auto">
  <name>Task 1: Add form primitive gallery specimens</name>
  <files>mailglass_admin/lib/mailglass_admin/gallery_live.ex</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/gallery_live.ex
    @mailglass_admin/lib/mailglass_admin/components.ex
    @mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
    @mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
    @.planning/phases/111-forms/111-CONTEXT.md
    @.planning/phases/110-primitives/110-03-SUMMARY.md
  </read_first>
  <action>Add gallery specimens that certify the real shared form primitives per D-01 and D-08. Add `render_specimen` branches for `:filter_field` and `:filter_section` that call `MailglassAdmin.Components.filter_field/1` and `filter_section/1` directly. Add states named `text-empty`, `select-filled`, `invalid`, `disabled`, `readonly-text`, `readonly-select-display`, and `section`. Keep existing `gallery-{component}-{state}` anchors and the Phase 110 light/dark/system wrapper convention. Update the existing `:filters_form` specimens so they render the migrated operator wrapper and pass an `errors` map for an invalid state; do not introduce gallery-only filter markup that bypasses the public primitives.</action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "gallery|form"</automated>
  </verify>
  <acceptance_criteria>
    - `gallery_live.ex` contains `render_specimen(%{component: :filter_field}` and `render_specimen(%{component: :filter_section}` branches.
    - Gallery contains stable states `gallery-filter_field-invalid`, `gallery-filter_field-disabled`, `gallery-filter_field-readonly-text`, `gallery-filter_field-readonly-select-display`, and `gallery-filter_section-section`.
    - The invalid specimen renders recovery copy and `aria-invalid="true"` through `Components.filter_field/1`.
    - Existing `gallery-filters_form-empty` and `gallery-filters_form-filled` anchors remain reachable.
  </acceptance_criteria>
  <done>The gallery certifies the real shared form primitives and migrated filter form wrappers.</done>
</task>

<task type="auto">
  <name>Task 2: Add FORM drift conformance gate</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    @mailglass_admin/scripts/check-conformance.sh
    @mailglass_admin/lib/mailglass_admin/components.ex
    @mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
    @mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
    @.planning/phases/111-forms/111-CONTEXT.md
    @.planning/phases/110-primitives/110-04-SUMMARY.md
  </read_first>
  <action>Add a `FORM-DRIFT-GATE` block to the existing `check-conformance.sh` shared `errors` counter per D-02 and D-08. Keep the script cwd-independent through the existing `BASH_SOURCE` pathing. The gate must require `Components.filter_field/1` and `Components.filter_section/1`, require both operator and inbound filter wrappers to call `Components.filter_field`, and fail if those wrapper files contain direct native label/control tags or the old filter-control class signatures. Scope the native-control ban to the two filter wrapper files so other components can still render legitimate forms. Do not add a new script and do not use Node or package installs.</action>
  <verify>
    <automated>cd mailglass_admin && ./scripts/check-conformance.sh</automated>
  </verify>
  <acceptance_criteria>
    - `check-conformance.sh` contains a `FORM-DRIFT-GATE` label/message.
    - The gate checks public `Components.filter_field/1` and `Components.filter_section/1` definitions.
    - The gate checks both `operator/filters_form.ex` and `inbound/filters_form.ex`.
    - `cd mailglass_admin && ./scripts/check-conformance.sh` exits 0 on the migrated implementation.
  </acceptance_criteria>
  <done>Conformance fails deterministically if duplicated filter label/control markup returns outside the shared primitive implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add browser structural proof and run final phase gate</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/lib/mailglass_admin/operator_live.ex
    @mailglass_admin/lib/mailglass_admin/inbound_live.ex
    @mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
    @mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
    @mailglass_admin/lib/mailglass_admin/gallery_live.ex
    @.planning/phases/111-forms/111-VALIDATION.md
  </read_first>
  <action>Extend `structural.spec.js` with a Phase 111 form proof describe block. Reuse existing helper style for gallery wrappers, target-size checks, focus appearance, disabled distinction, and contrast. Add assertions that `filter_field` gallery states expose explicit labels, help/error IDs, `aria-describedby`, invalid non-color cue, disabled state, native read-only text, and display-only read-only select. Add operator focus-persistence coverage by focusing the Provider control inside `data-testid="operator-filters"`, recording `document.activeElement.id`, applying an ordinary filter patch through `Open delivery`, and asserting the same ID remains focused. Add inbound focus-persistence coverage by focusing the Search control inside `data-testid="inbound-filters"`, applying `Open record`, and asserting the same ID remains focused. Add structural checks for Preview assigns labels and operator replay target radios created in Plan 111-03. Do not add screenshots, pixel assertions, storage hooks, or package dependencies.</action>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser</automated>
    <automated>cd mailglass_admin && mix verify.preview && ./scripts/check-conformance.sh && npm run test:operator-browser</automated>
  </verify>
  <acceptance_criteria>
    - Browser tests assert `document.activeElement.id` is unchanged after ordinary operator and inbound filter patches.
    - Browser tests assert `aria-invalid="true"` and visible recovery copy on an invalid form primitive state.
    - Browser tests assert disabled and read-only/display-only form states are programmatically different.
    - Browser tests assert operator replay target radios have explicit IDs, labels, descriptions, and selected text.
    - Final phase gate `cd mailglass_admin && mix verify.preview && ./scripts/check-conformance.sh && npm run test:operator-browser` passes.
  </acceptance_criteria>
  <done>Phase 111 has fail-closed conformance and browser proof for shared form semantics and focus persistence.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Form controls sit on unified primitives; filters become shared primitives; controls are labelled/recoverable; focus survives patches | 111-01, 111-02, 111-04 | COVERED | Goal truths are split by primitive, consumer, and proof layers. |
| REQ | FORM-01 | Two divergent filter forms unified into shared `filter_field` / `filter_section` primitives | 111-01, 111-02, 111-04 | COVERED | Public primitives, thin wrappers, and conformance guard. |
| REQ | FORM-02 | Visible labels, help/error associations, recovery copy, non-color validation state | 111-01, 111-02, 111-03, 111-04 | COVERED | Component, LiveView, Preview/replay, and browser structural checks. |
| REQ | FORM-03 | Disabled/read-only distinction and focus preserved across patches | 111-01, 111-03, 111-04 | COVERED | Primitive semantics, Preview/replay audit, and browser active-element proof. |
| RESEARCH | validation | Use ExUnit, conformance, and Playwright structural checks; no screenshots or pixel diff | 111-01, 111-02, 111-03, 111-04 | COVERED | Commands match 111-VALIDATION.md. |
| RESEARCH | architecture | Shared primitives live in `MailglassAdmin.Components`; wrappers may remain thin | 111-01, 111-02 | COVERED | Uses D-01/D-02. |
| RESEARCH | focus | Preserve stable DOM identity for ordinary LiveView patches | 111-02, 111-04 | COVERED | Stable IDs from form metadata plus browser proof. |
| RESEARCH | surface-audit | Preview assigns and replay radios explicitly update or certify | 111-03, 111-04 | COVERED | Dedicated plan, not a sweep. |
| RESEARCH | package-audit | No packages required | 111-01, 111-02, 111-03, 111-04 | COVERED | No install tasks. |
| CONTEXT | D-01 | `filter_field` and `filter_section` in `MailglassAdmin.Components` | 111-01 | COVERED | Public functions implemented first. |
| CONTEXT | D-02 | Operator/inbound wrappers only thin; guard duplicate markup | 111-02, 111-04 | COVERED | Thin wrappers plus FORM drift gate. |
| CONTEXT | D-03 | Preview assigns and replay target radios explicit work | 111-03, 111-04 | COVERED | Dedicated update/certification plan. |
| CONTEXT | D-04 | Stable IDs, visible labels, help/error IDs, aria-describedby, aria-invalid | 111-01, 111-02, 111-04 | COVERED | Primitive and browser tests. |
| CONTEXT | D-05 | Specific recovery copy for invalid normalized/dropped filters | 111-02 | COVERED | Exact strings in LiveView tests. |
| CONTEXT | D-06 | Disabled vs read-only semantics | 111-01, 111-03, 111-04 | COVERED | Primitive, Preview, replay, and browser assertions. |
| CONTEXT | D-07 | Preserve focus via stable DOM identity; JS focus only for deliberate moves | 111-02, 111-04 | COVERED | No new focus helper for ordinary filters; active-element tests. |
| CONTEXT | D-08 | Existing non-pixel verification machinery; no dependency/workflow additions | 111-01, 111-02, 111-03, 111-04 | COVERED | ExUnit, shell conformance, Playwright only. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source files -> conformance script | Grep gates decide whether duplicate form markup can enter the codebase. |
| Browser -> LiveView patch cycle | Focus and form input values cross a server-rendered patch boundary. |
| Gallery/test DOM -> structural assertions | Browser proof must inspect real rendered DOM without screenshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-10 | Tampering | `check-conformance.sh` | mitigate | FORM drift gate checks exact source ownership and exits non-zero on duplicate wrapper markup. |
| T-111-11 | Denial of Service | LiveView filter patches | mitigate | Browser tests assert focus remains on the same stable control after ordinary patches. |
| T-111-12 | Repudiation | phase verification | mitigate | Final phase gate records deterministic Mix, conformance, and Playwright commands. |
| T-111-SC | Tampering | package installs | mitigate | No install tasks; final gate uses existing package scripts only. |
</threat_model>

<verification>
Plan-level automated checks:

- `cd mailglass_admin && ./scripts/check-conformance.sh`
- `cd mailglass_admin && npm run test:operator-browser`

Final Phase 111 gate:

- `cd mailglass_admin && mix verify.preview && ./scripts/check-conformance.sh && npm run test:operator-browser`
</verification>

<success_criteria>
- Gallery renders and browser-tests the new form primitive states.
- Conformance fails on reintroduced duplicated filter label/control markup outside `components.ex`.
- Browser structural tests prove focus persistence for operator and inbound filter patches.
- Final phase gate passes exactly: `cd mailglass_admin && mix verify.preview && ./scripts/check-conformance.sh && npm run test:operator-browser`.
- No runtime dependency, screenshot workflow, pixel-diff workflow, tenant listing, theme persistence, or data-display work is introduced.
</success_criteria>

<output>
Create `.planning/phases/111-forms/111-04-SUMMARY.md` when done.
</output>
