---
phase: "34"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mailglass_admin/mix.exs"
  - "mailglass_admin/test/support/citext_probe.ex"
  - "mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs"
autonomous: true
requirements:
  - MAT-03
must_haves:
  truths:
    - "Maintainers can run one explicit admin/operator support-contract bundle without relying on a tag-only smoke gate."
    - "The admin-package bootstrap probe fails loudly after bounded retries instead of returning a false green."
    - "Admin support verification remains package-local and does not get folded into the root package bootstrap lifecycle."
  artifacts:
    - path: "mailglass_admin/mix.exs"
      provides: "Explicit admin/operator support-contract alias"
      contains: "verify.support_contract.admin"
    - path: "mailglass_admin/test/support/citext_probe.ex"
      provides: "Honest bounded admin citext probe"
      contains: "raise"
    - path: "mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs"
      provides: "Regression coverage for admin probe success and exhausted-retry failure"
      contains: "citext probe exhausted"
  key_links:
    - from: "mailglass_admin/mix.exs"
      to: "mailglass_admin/test/mailglass_admin/operator_live_test.exs"
      via: "explicit admin support-contract alias file list"
      pattern: "verify\\.support_contract\\.admin"
    - from: "mailglass_admin/mix.exs"
      to: "mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs"
      via: "non-vacuous smoke coverage retained inside the admin support bundle"
      pattern: "post_installer_smoke_test"
    - from: "mailglass_admin/test/support/citext_probe.ex"
      to: "mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs"
      via: "probe retry contract covered by focused unit tests"
      pattern: "citext probe exhausted"
---

<objective>
Make `mailglass_admin` an honest authority for the admin/operator support-contract bundle, and remove the false-green bootstrap behavior that currently undermines trust in admin-package verification.

Purpose: Per D-34-02 through D-34-09, admin verification must stay package-local, explicit, and bootstrap-honest instead of being represented by a vacuous selector or hidden retry semantics.
Output: An explicit admin support-contract alias plus bounded admin citext-probe behavior with regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-verification-regression-closure/34-CONTEXT.md
@.planning/phases/34-verification-regression-closure/34-RESEARCH.md
@.planning/phases/34-verification-regression-closure/34-PATTERNS.md
@mailglass_admin/mix.exs
@mailglass_admin/test/support/citext_probe.ex
@mailglass_admin/test/test_helper.exs
@mailglass_admin/test/support/live_view_case.ex
@mailglass_admin/test/mailglass_admin/operator_live_test.exs
@mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs

<interfaces>
From mailglass_admin/test/support/citext_probe.ex:
```elixir
def run(opts \\ [])
```

From mailglass_admin/mix.exs:
```elixir
"verify.preview": [
  "compile --no-optional-deps --warnings-as-errors",
  "test --warnings-as-errors --exclude flaky"
]
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add an explicit admin/operator support-contract alias</name>
  <files>mailglass_admin/mix.exs</files>
  <read_first>mailglass_admin/mix.exs, .planning/phases/34-verification-regression-closure/34-CONTEXT.md, .planning/phases/34-verification-regression-closure/34-RESEARCH.md, mailglass_admin/test/mailglass_admin/operator_live_test.exs, mailglass_admin/test/mailglass_admin/post_installer_smoke_test.exs</read_first>
  <acceptance_criteria>
    - `mailglass_admin/mix.exs` contains a `verify.support_contract.admin` alias.
    - `verify.support_contract.admin` runs exactly these files in one invocation: `test/mailglass_admin/post_installer_smoke_test.exs` and `test/mailglass_admin/operator_live_test.exs`.
    - `verify.support_contract.admin` does not use `--only admin_smoke` or any tag-only selector.
    - Existing `verify.preview` remains available as the broader admin verification entrypoint.
  </acceptance_criteria>
  <action>In `mailglass_admin/mix.exs`, add `verify.support_contract.admin` per D-34-03, D-34-04, and D-34-05 as one explicit `mix test ... --warnings-as-errors` invocation over `test/mailglass_admin/post_installer_smoke_test.exs test/mailglass_admin/operator_live_test.exs`. Keep `verify.preview` intact as the broader admin-package authority, but stop treating the tag-only `admin_smoke` selector as the support-contract truth. Preserve package-local ownership: do not add any root-package bootstrap dependency or cross-package helper to make this alias work.</action>
  <verify>
    <automated>cd mailglass_admin && mix verify.support_contract.admin</automated>
  </verify>
  <done>The admin package exposes a single explicit, non-vacuous local command for its required operator support-contract coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the admin citext probe fail honestly after bounded retries</name>
  <files>mailglass_admin/test/support/citext_probe.ex, mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs</files>
  <read_first>mailglass_admin/test/support/citext_probe.ex, .planning/phases/34-verification-regression-closure/34-CONTEXT.md, .planning/phases/34-verification-regression-closure/34-PATTERNS.md, mailglass_admin/test/test_helper.exs, mailglass_admin/test/support/live_view_case.ex, mailglass_admin/test/support/admin_bootstrap.ex</read_first>
  <acceptance_criteria>
    - `mailglass_admin/test/support/citext_probe.ex` no longer ends the retry loop with `:ok` on exhaustion.
    - `mailglass_admin/test/support/citext_probe.ex` raises an exception message containing `citext probe exhausted for MailglassAdmin.TestRepo after`.
    - `mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs` contains one success-path assertion and one exhausted-retry assertion.
    - The public `run/1` API remains package-local and callable from existing admin helpers without sharing logic through the root package.
  </acceptance_criteria>
  <action>Refactor `MailglassAdmin.TestSupport.CitextProbe` per D-34-06 through D-34-09 so retry exhaustion is explicit instead of silently returning `:ok`. Preserve the public `run/1` entrypoint used by `mailglass_admin/test/test_helper.exs`, `LiveViewCase`, and `AdminBootstrap`, but extract the retry loop far enough that `mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs` can drive it deterministically with a test-only probe function or stub repo. On exhaustion, raise with an exact message shaped like `citext probe exhausted for MailglassAdmin.TestRepo after N attempts`; do not centralize this with the root probe or add blanket rescue behavior that hides real bootstrap failure.</action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/test_support/citext_probe_test.exs --warnings-as-errors && mix verify.support_contract.admin</automated>
  </verify>
  <done>The admin bootstrap seam is now truthful: a poisoned admin bootstrap fails immediately with a clear message, and that behavior is covered by focused regression tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin test bootstrap -> CI truth | A package-local bootstrap helper determines whether required admin verification can be trusted. |
| Admin smoke precedent -> support-contract bundle | A formerly tag-scoped smoke gate must remain non-vacuous inside the explicit admin contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Tampering / Misrepresentation | `mailglass_admin/test/support/citext_probe.ex` | mitigate | Raise on exhausted retries with an exact message so admin bootstrap failure cannot masquerade as green per D-34-06 and D-34-09. |
| T-34-05 | Repudiation | `mailglass_admin/mix.exs` alias | mitigate | Use an explicit two-file admin support-contract alias so the required admin job cannot pass with zero matching tests per D-34-05. |
| T-34-06 | Elevation of Privilege | `mailglass_admin` test bootstrap | mitigate | Keep admin bootstrap ownership local; do not route through a root-package helper that obscures which package actually failed. |
</threat_model>

<verification>
- `cd mailglass_admin && mix verify.support_contract.admin`
- `cd mailglass_admin && mix test test/mailglass_admin/test_support/citext_probe_test.exs --warnings-as-errors`
- `rg -n "verify\\.support_contract\\.admin|citext probe exhausted" mailglass_admin/mix.exs mailglass_admin/test/support/citext_probe.ex mailglass_admin/test/mailglass_admin/test_support/citext_probe_test.exs`
</verification>

<success_criteria>
The admin package can prove its operator-support coverage with one explicit alias, and its bootstrap seam now fails honestly instead of silently swallowing exhaustion.
</success_criteria>

<output>
After completion, create `.planning/phases/34-verification-regression-closure/34-02-SUMMARY.md`
</output>
