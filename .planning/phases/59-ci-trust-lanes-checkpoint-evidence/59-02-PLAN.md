---
phase: 59-ci-trust-lanes-checkpoint-evidence
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - .github/workflows/ci.yml
  - scripts/setup_branch_protection.sh
autonomous: false
requirements:
  - EVID-01
  - EVID-02
  - EVID-04
tags:
  - ci
  - trust-evidence
  - hex-first
  - branch-protection
  - upload-artifact

must_haves:
  truths:
    - "A new job `trust_lane_repo_head` exists in `.github/workflows/ci.yml` with literal name `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`; it runs `mix verify.reference_host.journey` from the repo root, validates the resulting `tmp/mailglass_trust_runner/checkpoint.json` via `bash scripts/check_trust_runner_checkpoint.sh`, and uploads that file as artifact `trust-runner-repo-head-${{ github.run_id }}` with `if-no-files-found: error` and `retention-days: 90` (EVID-01 + EVID-04)."
    - "A new job `trust_lane_clean_baseline` exists in `.github/workflows/ci.yml` with literal name `Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)`; it operates from `working-directory: reference/host_app`, runs `mix deps.get`, runs `bash ../../scripts/check_clean_baseline_hex_only.sh` (from Plan 01), runs `mix verify.reference_host.journey` from `reference/host_app/`, validates the resulting `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` via `bash scripts/check_trust_runner_checkpoint.sh --checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`, and uploads that file as artifact `trust-runner-clean-baseline-${{ github.run_id }}` with the same upload settings (EVID-02 + EVID-04)."
    - "Both new jobs use the exact pinned `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` SHA already present in `preview_capture_advisory` (D-11; CLAUDE.md pinned-Actions rule); no second SHA is introduced."
    - "Both new jobs use the exact pinned `actions/checkout`, `erlef/setup-beam`, and `actions/cache` SHAs already used by every required lane in `ci.yml` (no new pinned-Action surface)."
    - "Neither new job has an `if:` condition (Pitfall 2: `gate-ci-green` treats skipped as non-blocking; both lanes must always run on every `ci.yml` trigger)."
    - "`scripts/setup_branch_protection.sh` has `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` appended to `REQUIRED_CHECKS` AND mirrored as a `  - <name>` bullet in `print_expected_text` heredoc — both edits in the same commit as the `ci.yml` job additions (D-02 + Pitfall 1: atomic single-commit landing). The clean-baseline lane is intentionally NOT in `REQUIRED_CHECKS` per the EVID-01/EVID-02 reading locked in CONTEXT.md A1 — it's required at the publish-gate layer only (any non-`ADVISORY_LANES` job in `ci.yml` blocks `gate-ci-green`)."
    - "After merge to `main`, `./scripts/setup_branch_protection.sh main` (or `workflow_dispatch` on `branch-protection-drift.yml`) is run to push the new required check to GitHub's external branch-protection state, and `gh api repos/szTheory/mailglass/branches/main/protection | jq -r '.required_status_checks.contexts[]'` includes `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`."
    - "On a feature-branch CI run before merge, both new lanes report `success`; the two artifacts can be downloaded via `gh run download <id> -n trust-runner-repo-head-<id>` and `-n trust-runner-clean-baseline-<id>`; the downloaded JSON files pass `bash scripts/check_trust_runner_checkpoint.sh --checkpoint <file>` (EVID-04 round-trip)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Two new required jobs (trust_lane_repo_head, trust_lane_clean_baseline) following the preview_capture_advisory template"
      contains: "trust_lane_repo_head"
    - path: "scripts/setup_branch_protection.sh"
      provides: "REQUIRED_CHECKS array + print_expected_text heredoc both extended with the repo-head trust lane name"
      contains: "Trust Lane Repo Head (Elixir 1.18 / OTP 27)"
    - path: "tmp/mailglass_trust_runner/checkpoint.json"
      provides: "Repo-head trust runner checkpoint (produced by mix verify.reference_host.journey in lane, uploaded as artifact, validated by scripts/check_trust_runner_checkpoint.sh)"
      contains: "trust_runner.v1"
    - path: "reference/host_app/tmp/mailglass_trust_runner/checkpoint.json"
      provides: "Clean-baseline trust runner checkpoint (produced by mix verify.reference_host.journey from reference/host_app/, uploaded as artifact, validated by scripts/check_trust_runner_checkpoint.sh)"
      contains: "trust_runner.v1"
  key_links:
    - from: ".github/workflows/ci.yml::trust_lane_repo_head.name"
      to: "scripts/setup_branch_protection.sh::REQUIRED_CHECKS"
      via: "byte-identical literal string Trust Lane Repo Head (Elixir 1.18 / OTP 27)"
      pattern: "Trust Lane Repo Head \\(Elixir 1\\.18 / OTP 27\\)"
    - from: ".github/workflows/ci.yml::trust_lane_clean_baseline"
      to: "scripts/check_clean_baseline_hex_only.sh (from Plan 01)"
      via: "bash ../../scripts/check_clean_baseline_hex_only.sh invoked from working-directory: reference/host_app"
      pattern: "check_clean_baseline_hex_only"
    - from: ".github/workflows/ci.yml (both new jobs)"
      to: "scripts/check_trust_runner_checkpoint.sh"
      via: "bash scripts/check_trust_runner_checkpoint.sh [--checkpoint <abs-or-rel-path>]"
      pattern: "scripts/check_trust_runner_checkpoint\\.sh"
    - from: ".github/workflows/ci.yml (both new jobs)"
      to: "publish-hex.yml::gate-ci-green"
      via: "Neither job in ADVISORY_LANES; any failure blocks publish per the gate-ci-green allowlist filter"
      pattern: "(no edit to publish-hex.yml — default inclusion is correct)"
---

<objective>
Add the two trust lanes (`trust_lane_repo_head` and `trust_lane_clean_baseline`)
to `.github/workflows/ci.yml`, register the repo-head lane in
`scripts/setup_branch_protection.sh` (`REQUIRED_CHECKS` array + matching
`print_expected_text` heredoc bullet), and run the post-merge branch-protection
re-assertion as a blocking-human checkpoint so EVID-01 enforcement becomes
live on `main` immediately rather than waiting for the daily drift cron.

Purpose: This is the wiring commit that turns the Plan 01 preconditions
into the actual EVID-01/EVID-02/EVID-04 trust gates. D-02 and Pitfall 1
require all three edits (workflow + array + heredoc) to land in one commit
or `branch-protection-drift.yml`'s drift detector flags transient drift.
The post-merge re-assertion is intrinsically external (GitHub branch
protection is server-side state; editing the script doesn't change live
protection until the script runs), so the checkpoint can't be eliminated
by smarter local automation — it must be performed against `main` after
the wiring commit lands.

Output:
  - `.github/workflows/ci.yml` — two new jobs added near `preview_capture_advisory`
    (which is the canonical "runner → validator → upload-artifact" template):
      * `trust_lane_repo_head` — name `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`,
        runs from repo root, uploads `tmp/mailglass_trust_runner/checkpoint.json`
        as `trust-runner-repo-head-${{ github.run_id }}` with `retention-days: 90`.
      * `trust_lane_clean_baseline` — name `Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)`,
        `working-directory: reference/host_app` for deps install + Hex-first guard +
        journey run, uploads `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`
        as `trust-runner-clean-baseline-${{ github.run_id }}` with `retention-days: 90`.
  - `scripts/setup_branch_protection.sh` — `REQUIRED_CHECKS` array grows by one
    entry, `print_expected_text` heredoc grows by one matching bullet — atomic
    edit in the same commit as the workflow edit per D-02 + Pitfall 1.
  - `gh api repos/szTheory/mailglass/branches/main/protection` reflects the new
    required check after the post-merge `./scripts/setup_branch_protection.sh main`
    re-assertion completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-CONTEXT.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-PATTERNS.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-VALIDATION.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-01-SUMMARY.md

@.github/workflows/ci.yml
@.github/workflows/publish-hex.yml
@scripts/setup_branch_protection.sh
@scripts/check_trust_runner_checkpoint.sh
@scripts/check_clean_baseline_hex_only.sh
@reference/host_app/mix.exs
@reference/host_app/mix.lock
@lib/mix/tasks/mailglass.trust.run.ex

<interfaces>
<!-- Canonical strings and pinned SHAs Plan 02 must copy verbatim. -->
<!-- Any drift between these and the implementation breaks branch-protection lookup, gate-ci-green inclusion, or Dependabot batching. -->

Job-name literals (must be byte-identical across `ci.yml::name`, `REQUIRED_CHECKS` entry, and `print_expected_text` bullet):
  - `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`
  - `Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)`

Pinned Action SHAs (reuse exactly — no second SHA introduced; Dependabot bumps one place):
  - `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2`
  - `erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93  # v1.24.0`
  - `actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5`
  - `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4`

Artifact upload parameters (per D-11, D-12, D-13, D-14, Pitfall 6):
  - `name: trust-runner-repo-head-${{ github.run_id }}` (repo-head)
  - `name: trust-runner-clean-baseline-${{ github.run_id }}` (clean-baseline)
  - `if-no-files-found: error` (both)
  - `retention-days: 90` (both — NOT 14 like preview-capture; release ceremonies run quarterly)
  - `path:` is the exact file, NOT the parent directory (Pitfall 6):
    * `tmp/mailglass_trust_runner/checkpoint.json` (repo-head)
    * `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` (clean-baseline)

Checkpoint path resolution (per `lib/mix/tasks/mailglass.trust.run.ex:50-53`):
  - Runner uses `Path.expand(checkpoint_out, File.cwd!())` with default `tmp/mailglass_trust_runner/checkpoint.json`
  - Repo-head lane: cwd is repo root → checkpoint lands at `tmp/mailglass_trust_runner/checkpoint.json`
  - Clean-baseline lane: cwd is `reference/host_app/` (set via `working-directory:` on the `mix verify.reference_host.journey` step) → checkpoint lands at `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`
  - Validator invocation from repo root for the clean-baseline lane MUST pass `--checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` (do NOT use `working-directory:` on the validator step; per RESEARCH §"Open Question 1" resolution)
  - Repo-head lane validator invocation needs no `--checkpoint` flag (script default matches lane cwd)

Cache key prefixes (Pattern 4 + Pitfall 5 — distinct keys prevent cross-contamination):
  - Repo-head lane: `path: deps`, `key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}` (matches `compile_no_optional_deps:101-107`)
  - Clean-baseline lane: `path: reference/host_app/deps`, `key: ${{ runner.os }}-mix-reference-host-${{ hashFiles('reference/host_app/mix.lock') }}` (`-reference-host-` prefix scopes the cache to this lane)

`gate-ci-green` inclusion (no edit needed — RESEARCH §"Don't Hand-Roll"):
  - Any `ci.yml` job not listed in `publish-hex.yml::gate-ci-green::ADVISORY_LANES` is automatically a publish gate
  - Current ADVISORY_LANES: `['Operator Browser Gate']` (verifiable at `publish-hex.yml:139-141`)
  - Neither new lane is added to ADVISORY_LANES; default inclusion is the correct behavior per D-01

Postgres service container (per RESEARCH §"Assumption A3"):
  - Include the same `services.postgres` + `env` block as `preview_capture_advisory:685-704` in both new jobs
  - Trust runner exercises `webhook_ingest` + `operator_troubleshooting` stages that call into seam paths possibly touching the Ecto sandbox; safer to include and match existing required-lane convention
  - The full block (postgres service definition, healthcheck options, env vars MIX_ENV/POSTGRES_HOST/POSTGRES_USER/POSTGRES_PASSWORD) and the `Wait for postgres + create test DB` step are copied verbatim from `preview_capture_advisory` lines 685-704 and 737-742
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trust_lane_repo_head and trust_lane_clean_baseline jobs to ci.yml and atomically update setup_branch_protection.sh</name>
  <files>.github/workflows/ci.yml, scripts/setup_branch_protection.sh</files>
  <read_first>
    - .github/workflows/ci.yml (lines 676-808 = `preview_capture_advisory` 1:1 template; lines 686-704 = postgres service + env block; lines 706-713 = checkout + setup-beam pinned-SHA pattern; lines 720-728 = cache pattern; lines 737-742 = "Wait for postgres + create test DB"; lines 790-799 = upload-artifact pattern. Verify the upload-artifact SHA `ea165f8d65b6e75b540449e92b4886f43607fa02` is still pinned at line 791 — if Dependabot has bumped it, follow the new SHA and update the planner's reference)
    - scripts/setup_branch_protection.sh (lines 17-21 REQUIRED_CHECKS array; lines 23-42 print_expected_text heredoc — note where the bullet list ends with the blank line before "Expected non-context branch protection fields:")
    - scripts/check_trust_runner_checkpoint.sh (validator contract: default `CHECKPOINT_PATH="tmp/mailglass_trust_runner/checkpoint.json"`; `--checkpoint PATH` overrides; missing-file → "Trust runner checkpoint validation blocked: missing checkpoint at '<path>'" exit 1)
    - scripts/check_clean_baseline_hex_only.sh (Plan 01 output; invocation contract: cwd `reference/host_app/`, `bash ../../scripts/check_clean_baseline_hex_only.sh`, exits 1 on path-leakage, exits 0 on Hex-only)
    - lib/mix/tasks/mailglass.trust.run.ex lines 32-53 (default `--checkpoint-out tmp/mailglass_trust_runner/checkpoint.json`, resolved via `Path.expand(checkpoint_out, File.cwd!())`)
    - reference/host_app/mix.exs (Hex-only sibling constraints: `{:mailglass, "~> 1.2"}`, `{:mailglass_admin, "~> 1.2"}`, `{:mailglass_inbound, "~> 0.2"}`)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-CONTEXT.md (D-01..D-15 — all locked)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md ("Code Examples" lines 545-693 — full recommended `ci.yml` job structure with postgres service, cache, deps install, journey run, validator, optional human-glance step, upload)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-PATTERNS.md (lines 19-178 — `ci.yml` job assembly with substitution table for D-04..D-15; lines 136-178 — `setup_branch_protection.sh` additive in-place edit pattern)
  </read_first>
  <action>
    Make THREE edits in ONE commit (D-02 + Pitfall 1: atomic landing prevents `branch-protection-drift.yml` from flagging transient drift between the workflow edit and the branch-protection script edit):

    --- Edit A: `.github/workflows/ci.yml` — add `trust_lane_repo_head` job ---

    Insert AFTER the existing `preview_capture_advisory:` job (currently ends at line 808) and BEFORE `branch_protection_advisory:` (currently starts at line 810). Place at this position to maximize parallelism with existing required lanes (each `ci.yml` job runs in its own runner) and keep adjacent to the canonical `preview_capture_advisory` template for diff-readability.

    Job shape (lift verbatim from 59-RESEARCH.md "Code Examples / Repo-Head Trust Lane" lines 547-614 — that block was already verified against the codebase by the researcher). Per-line decisions:

      - `trust_lane_repo_head:` (snake_case job ID per `ci.yml` convention; this is the GitHub Actions job ID, separate from the human-readable `name:`)
      - `name: Trust Lane Repo Head (Elixir 1.18 / OTP 27)` (LITERAL string — no `${{ }}` interpolation per Pitfall anti-pattern; this string MUST byte-match the `REQUIRED_CHECKS` entry below)
      - `runs-on: ubuntu-latest`
      - `strategy.matrix.include: [{elixir: "1.18", otp: "27"}]` (D-03 — single matrix entry; do NOT broaden)
      - `services.postgres:` block copied verbatim from `preview_capture_advisory:685-698` (postgres:16-alpine, healthcheck options, port 5432 exposed)
      - `env:` block: `MIX_ENV: test`, `POSTGRES_HOST: localhost`, `POSTGRES_USER: postgres`, `POSTGRES_PASSWORD: postgres` (omit `CI: true` and `PREVIEW_CAPTURE_ROOT` — not relevant to trust runner)
      - Steps:
        1. `Checkout`: `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2`
        2. `Set up OTP + Elixir`: `erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93  # v1.24.0` with `elixir-version: ${{ matrix.elixir }}` and `otp-version: ${{ matrix.otp }}`
        3. `Cache deps`: `actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5` with `path: deps`, `key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}`, `restore-keys: ${{ runner.os }}-mix-` (single-path form per Pattern 4)
        4. `Install deps`: `run: mix deps.get`
        5. `Wait for postgres + create test DB` block copied verbatim from `preview_capture_advisory:737-742` (the `PGPASSWORD: postgres` env + `until pg_isready ...` loop + `mix ecto.create -r Mailglass.TestRepo --quiet`)
        6. `Run reference-host trust journey`: `run: mix verify.reference_host.journey` (D-04 — no flags; D-06 — keep default checkpoint path)
        7. `Validate trust checkpoint contract`: `run: bash scripts/check_trust_runner_checkpoint.sh` (D-04/D-05 — no `--checkpoint` flag; script default matches runner default)
        8. `Print checkpoint SHA for human-glance evidence` (RECOMMENDED — Discretion item from CONTEXT §"Claude's Discretion"; lifted from PATTERNS.md "$GITHUB_STEP_SUMMARY append" + RESEARCH "Code Examples" lines 603-607): writes a markdown header + `jq -r` extraction of `schema_version`, `claim_boundary`, `checkpoint_count`, `checkpoint_sha256` to `$GITHUB_STEP_SUMMARY`
        9. `Upload trust-runner repo-head artifact`: `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4` with `name: trust-runner-repo-head-${{ github.run_id }}`, `if-no-files-found: error`, `retention-days: 90`, `path: tmp/mailglass_trust_runner/checkpoint.json` (EXACT FILE, not directory — Pitfall 6)

      - Do NOT add an `if:` condition (Pitfall 2 — `gate-ci-green` treats skipped as non-blocking; lane must always run)
      - Do NOT add a `concurrency` override at the job level (workflow-level `concurrency.cancel-in-progress: true` already handles redundant runs)
      - Do NOT add the lane to `publish-hex.yml::gate-ci-green::ADVISORY_LANES` (default inclusion is the required publish-gate behavior per D-01)

    --- Edit B: `.github/workflows/ci.yml` — add `trust_lane_clean_baseline` job ---

    Insert IMMEDIATELY AFTER `trust_lane_repo_head:` and BEFORE `branch_protection_advisory:`. Job shape (lift verbatim from 59-RESEARCH.md "Code Examples / Clean-Baseline Trust Lane" lines 619-693). Per-line decisions:

      - `trust_lane_clean_baseline:` (job ID)
      - `name: Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)` (literal)
      - `runs-on: ubuntu-latest`
      - `strategy.matrix.include: [{elixir: "1.18", otp: "27"}]`
      - `services.postgres:` and `env:` blocks identical to `trust_lane_repo_head`
      - Steps:
        1. `Checkout` (pinned SHA as above)
        2. `Set up OTP + Elixir` (pinned SHA as above)
        3. `Cache reference-host deps`: `actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5` with `path: reference/host_app/deps`, `key: ${{ runner.os }}-mix-reference-host-${{ hashFiles('reference/host_app/mix.lock') }}`, `restore-keys: ${{ runner.os }}-mix-reference-host-` (Pattern 4 — distinct prefix prevents cache cross-contamination with the repo-head lane; Pitfall 5)
        4. `Install reference-host deps`: `working-directory: reference/host_app`, `run: mix deps.get`
        5. `Assert Hex-first resolution for sibling packages`: `working-directory: reference/host_app`, `run: bash ../../scripts/check_clean_baseline_hex_only.sh` (D-08; script ships in Plan 01)
        6. `Wait for postgres + create test DB` block (no `working-directory:` needed; `mix ecto.create` runs from repo root with `-r Mailglass.TestRepo`)
        7. `Run reference-host trust journey`: `working-directory: reference/host_app`, `run: mix verify.reference_host.journey` (D-10 — same command as repo-head, different cwd; checkpoint lands at `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` per `lib/mix/tasks/mailglass.trust.run.ex:50-53` cwd-relative resolution)
        8. `Validate trust checkpoint contract`: `run: bash scripts/check_trust_runner_checkpoint.sh --checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` (NO `working-directory:` — invoked from repo root; the `--checkpoint` flag is REQUIRED because the validator's default `tmp/mailglass_trust_runner/checkpoint.json` resolves against repo root, which is the WRONG file per Open Question 1 resolution)
        9. `Print checkpoint SHA for human-glance evidence` (recommended; writes "Trust Runner Checkpoint (clean baseline)" header + jq extraction from `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`)
       10. `Upload trust-runner clean-baseline artifact`: `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4` with `name: trust-runner-clean-baseline-${{ github.run_id }}`, `if-no-files-found: error`, `retention-days: 90`, `path: reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`

    --- Edit C: `scripts/setup_branch_protection.sh` — add repo-head entry to REQUIRED_CHECKS and matching bullet ---

    1. `REQUIRED_CHECKS` array (currently lines 17-21): append one line BEFORE the closing `)`:
       `  "Trust Lane Repo Head (Elixir 1.18 / OTP 27)"`
       Result: array now has 4 entries.

    2. `print_expected_text` heredoc (currently lines 23-42): append one bullet AFTER `  - Compile No Optional Deps (Elixir 1.18 / OTP 27)` and BEFORE the blank line that precedes `Expected non-context branch protection fields:`:
       `  - Trust Lane Repo Head (Elixir 1.18 / OTP 27)`
       Result: required-status-checks bullet list now has 4 entries; the non-context-fields section is untouched.

    Per CONTEXT A1 + RESEARCH A1 (LOCKED reading): the clean-baseline lane is intentionally NOT added to `REQUIRED_CHECKS`. EVID-01 explicitly says "required repo-head trust lane"; EVID-02 says "clean-baseline trust lane" without "required" qualifier. The clean-baseline lane is required at the publish-gate layer only (via default inclusion in `gate-ci-green`'s non-`ADVISORY_LANES` jobs). Reversibility: adding the clean-baseline name to `REQUIRED_CHECKS` later is a one-line change if a future phase decides to escalate it.

    --- Commit posture ---

    Conventional Commits: `ci(59): add trust_lane_repo_head + trust_lane_clean_baseline + register repo-head as required check`
    Per CLAUDE.md (Conventional Commits enforced, `ci(...)` is the correct prefix for `ci.yml` + `scripts/setup_branch_protection.sh` edits — the prefix-allowlist is enforced by `.github/workflows/pr-title.yml`).
    All THREE edits land in ONE commit. Do NOT split into multiple commits; Pitfall 1 explicitly forbids any non-atomic landing of (workflow + REQUIRED_CHECKS + heredoc).

    --- Pre-commit local verifications (mandatory before pushing) ---

    1. `actionlint .github/workflows/ci.yml` exits 0.
    2. `shellcheck scripts/setup_branch_protection.sh` exits 0 (it was clean before the edit; bullet append must not introduce new SC warnings).
    3. `bash scripts/setup_branch_protection.sh --print-expected | grep -Fq 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)'` succeeds.
    4. `MIX_ENV=test mix test test/scripts/required_checks_test.exs` reports `2 tests, 0 failures` (Plan 01's contract test must stay green after the array+heredoc edit because both sides got the same new entry).
    5. Local runner smoke: `rm -rf tmp/mailglass_trust_runner && MIX_ENV=test mix verify.reference_host.journey && bash scripts/check_trust_runner_checkpoint.sh` exits 0 (proves repo-head lane will pass).
    6. Local clean-baseline smoke: `(cd reference/host_app && rm -rf tmp/mailglass_trust_runner && mix deps.get && bash ../../scripts/check_clean_baseline_hex_only.sh && MIX_ENV=test mix verify.reference_host.journey) && bash scripts/check_trust_runner_checkpoint.sh --checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` exits 0 (proves clean-baseline lane will pass).
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && shellcheck scripts/setup_branch_protection.sh && grep -Fq 'trust_lane_repo_head:' .github/workflows/ci.yml && grep -Fq 'trust_lane_clean_baseline:' .github/workflows/ci.yml && grep -cF 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' .github/workflows/ci.yml | awk '{ if ($1 == 1) exit 0; else { print "expected 1 occurrence of repo-head name in ci.yml, found " $1; exit 1 } }' && grep -cF 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' scripts/setup_branch_protection.sh | awk '{ if ($1 == 2) exit 0; else { print "expected 2 occurrences (array + heredoc bullet) in setup_branch_protection.sh, found " $1; exit 1 } }' && bash scripts/setup_branch_protection.sh --print-expected | grep -Fq 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' && grep -cF 'ea165f8d65b6e75b540449e92b4886f43607fa02' .github/workflows/ci.yml | awk '{ if ($1 >= 3) exit 0; else { print "expected upload-artifact pinned SHA in >= 3 places (preview_capture + 2 trust lanes), found " $1; exit 1 } }' && MIX_ENV=test mix test test/scripts/required_checks_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/ci.yml` exits 0 (workflow syntax valid).
    - `shellcheck scripts/setup_branch_protection.sh` exits 0 (no new SC warnings).
    - Both new job IDs are present: `grep -Fq 'trust_lane_repo_head:' .github/workflows/ci.yml && grep -Fq 'trust_lane_clean_baseline:' .github/workflows/ci.yml`.
    - Both literal job names are present in `ci.yml` (each exactly once): `grep -cF 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' .github/workflows/ci.yml` reports `1`; `grep -cF 'Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)' .github/workflows/ci.yml` reports `1`.
    - Repo-head name appears exactly twice in `setup_branch_protection.sh` (once in array, once in heredoc bullet): `grep -cF 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' scripts/setup_branch_protection.sh` reports `2`.
    - Clean-baseline name does NOT appear in `setup_branch_protection.sh` (per A1 lock — clean-baseline is publish-gate-required only, not branch-protection-required): `grep -cF 'Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)' scripts/setup_branch_protection.sh` reports `0`.
    - `bash scripts/setup_branch_protection.sh --print-expected | grep -Fq 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)'` succeeds (heredoc emits the new bullet).
    - `bash scripts/setup_branch_protection.sh --print-expected-json | jq -r '.required_status_checks.contexts[]' | grep -Fxq 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)'` succeeds (the JSON view used by the live `gh api PUT` includes the new context).
    - Pinned upload-artifact SHA is reused (no second SHA introduced): `grep -cF 'ea165f8d65b6e75b540449e92b4886f43607fa02' .github/workflows/ci.yml` reports `>= 3` (preview_capture_advisory + repo-head + clean-baseline; if Dependabot bumped, the count may be different but the value must be uniform across all upload-artifact uses).
    - Pinned setup-beam, checkout, cache SHAs reused (no new SHAs): `grep -cF 'erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93' .github/workflows/ci.yml` reports `>= 17` (already 15+ in the file pre-edit; both new jobs add 1 each); `grep -cF 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd' .github/workflows/ci.yml` reports `>= 17`.
    - Both new jobs use `retention-days: 90` (the new value, distinct from preview_capture's `retention-days: 14`): `grep -c 'retention-days: 90' .github/workflows/ci.yml` reports `2`.
    - Both new jobs use `if-no-files-found: error` (D-12 — second enforcement layer for EVID-01): `grep -cE 'name: trust-runner-(repo-head|clean-baseline)-\$\{\{ github.run_id \}\}' .github/workflows/ci.yml` reports `2`, and `grep -c 'if-no-files-found: error' .github/workflows/ci.yml` reports `>= 3` (existing preview + 2 new).
    - Artifact paths point to exact files, not directories (Pitfall 6): `grep -E 'path: tmp/mailglass_trust_runner/checkpoint\.json' .github/workflows/ci.yml` finds 1 line for repo-head; `grep -E 'path: reference/host_app/tmp/mailglass_trust_runner/checkpoint\.json' .github/workflows/ci.yml` finds 1 line for clean-baseline.
    - No `if:` condition on either new job (Pitfall 2): `awk '/^  trust_lane_repo_head:/,/^  [a-z_]/{print}' .github/workflows/ci.yml | grep -E '^    if:' | wc -l` reports `0` (no top-level `if:` on the job); same check for `trust_lane_clean_baseline`.
    - Clean-baseline lane uses `working-directory: reference/host_app` on at least the deps-install, Hex-first-guard, and journey-run steps: `awk '/^  trust_lane_clean_baseline:/,/^  [a-z_]/{print}' .github/workflows/ci.yml | grep -cE '^        working-directory: reference/host_app'` reports `>= 3`.
    - Clean-baseline validator step uses the explicit `--checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` flag: `grep -F 'check_trust_runner_checkpoint.sh --checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json' .github/workflows/ci.yml` finds 1 line.
    - Plan 01 contract test still passes (proves array/heredoc edit kept both sides in sync): `MIX_ENV=test mix test test/scripts/required_checks_test.exs` reports `2 tests, 0 failures`.
    - Local repo-head smoke: `rm -rf tmp/mailglass_trust_runner && MIX_ENV=test mix verify.reference_host.journey && bash scripts/check_trust_runner_checkpoint.sh` exits 0 and the checkpoint file exists at `tmp/mailglass_trust_runner/checkpoint.json`.
    - Local clean-baseline smoke: `(cd reference/host_app && rm -rf tmp/mailglass_trust_runner && mix deps.get && bash ../../scripts/check_clean_baseline_hex_only.sh && MIX_ENV=test mix verify.reference_host.journey) && bash scripts/check_trust_runner_checkpoint.sh --checkpoint reference/host_app/tmp/mailglass_trust_runner/checkpoint.json` exits 0 and the checkpoint file exists at `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`.
    - After feature-branch push, CI run on the Phase 59 PR shows both new lanes reporting `success` in the GitHub Checks UI (manual verification on the PR page; recorded in commit notes).
    - After feature-branch push, both artifacts can be downloaded and re-validated: `RUN_ID=$(gh run list --workflow ci.yml --branch <feature-branch> --limit 1 --json databaseId --jq '.[0].databaseId') && gh run download $RUN_ID -n trust-runner-repo-head-$RUN_ID -D /tmp/r && bash scripts/check_trust_runner_checkpoint.sh --checkpoint /tmp/r/checkpoint.json && gh run download $RUN_ID -n trust-runner-clean-baseline-$RUN_ID -D /tmp/c && bash scripts/check_trust_runner_checkpoint.sh --checkpoint /tmp/c/checkpoint.json` exits 0 for both.
  </acceptance_criteria>
  <done>
    All three edits land in one commit; `actionlint` + `shellcheck` clean; both new jobs present with correct literal names; repo-head name mirrored across `ci.yml::name`, `REQUIRED_CHECKS` array, and `print_expected_text` heredoc bullet; clean-baseline name in `ci.yml` only; all pinned Action SHAs reused (no new SHAs); local smoke runs of both lanes (repo-head and clean-baseline) pass; Plan 01 contract test stays green; feature-branch CI shows both lanes green and artifacts downloadable + re-validatable.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Post-merge branch-protection re-assertion (push REQUIRED_CHECKS change to GitHub's live protection state)</name>
  <files>(no source edits — runs `scripts/setup_branch_protection.sh main` against the live repository to update GitHub's external branch-protection state for repo szTheory/mailglass on the `main` branch)</files>
  <read_first>
    - scripts/setup_branch_protection.sh (post-Task-1 state — `REQUIRED_CHECKS` includes the new trust-lane entry; the `gh api -X PUT` invocation at lines 82-86 is the operative call this checkpoint triggers)
    - .github/workflows/branch-protection-drift.yml (alternative trigger: workflow_dispatch path that runs the same script using the repo's stored `BRANCH_PROTECTION_PAT` secret)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md (Pitfall 1 — Required-Check Registration Race; explains why this checkpoint is mandatory and not deferrable to the daily cron)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-VALIDATION.md (Manual-Only Verifications table; this checkpoint is the entry there)
  </read_first>
  <action>
    What was built upstream (Task 1) and why this checkpoint exists:

    Task 1 landed `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` in `scripts/setup_branch_protection.sh::REQUIRED_CHECKS` AND in `print_expected_text`. GitHub branch protection is EXTERNAL state — editing the script does NOT change live protection on `main`. The script must be RUN against `main` (or `branch-protection-drift.yml` must be `workflow_dispatch`-triggered) to push the new required check to GitHub's branch-protection API. Until that happens, a PR could theoretically merge with the new trust lane red, violating EVID-01.

    The daily `branch-protection-drift.yml` cron runs at 06:37 UTC and would close this window automatically, but for an explicit phase-completion guarantee we re-assert immediately rather than waiting up to ~24 hours (Pitfall 1).

    Why this is `checkpoint:human-action` and not autonomous:
      - `./scripts/setup_branch_protection.sh main` requires a `BRANCH_PROTECTION_PAT` token with admin scope on the repo; that token lives in GitHub Secrets (`branch-protection-drift.yml` consumes it) and in the maintainer's local env. The `gh` CLI inside an ephemeral Claude session does not have it.
      - Alternative invocation: `gh workflow run branch-protection-drift.yml --ref main` triggers the workflow that reads the secret server-side. This also requires the maintainer's authenticated `gh` session (the `workflow` scope).
      - No CLI/API path exists that lets Claude perform this without elevated maintainer credentials. This is the rare `checkpoint:human-action` case per the planner's automation-first rule.

    Maintainer instructions (run AFTER the Task 1 commit is merged to `main` — squash-merge or fast-forward either work; the script reads the file's current state on `main`):

      Step 1: Pull latest `main` locally:
        git checkout main && git pull origin main

      Step 2: Confirm the script on `main` has the new entry (sanity check before pushing the change to GitHub):
        bash scripts/setup_branch_protection.sh --print-expected | grep -Fq 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)'

        Must succeed (exit 0). If not, Task 1's commit hasn't actually merged to `main` yet — pause and verify before proceeding.

      Step 3: Push the new required check to GitHub. Choose ONE of:

        Option A (FASTEST — local re-assertion, requires BRANCH_PROTECTION_PAT in env):
          GH_TOKEN=$BRANCH_PROTECTION_PAT ./scripts/setup_branch_protection.sh main

          Expects `BRANCH_PROTECTION_PAT` set in your local shell env. The script idempotently PUTs the full branch-protection payload via `gh api`. Exit 0 + the "OK: branch protection configured" line means success.

        Option B (workflow-dispatched, if you don't have the PAT locally):
          gh workflow run branch-protection-drift.yml --ref main
          gh run watch  # follow the dispatched run to completion

          Triggers the existing daily-cron workflow on-demand; it runs `setup_branch_protection.sh main` using the repo's stored `BRANCH_PROTECTION_PAT` secret. Watch the run to completion via `gh run watch`.

      Step 4: Confirm live state now lists the new check:
        gh api repos/szTheory/mailglass/branches/main/protection | jq -r '.required_status_checks.contexts[]' | sort

        Output MUST include the line `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` alongside the three pre-existing entries (Support Contract Core, Support Contract Admin, Compile No Optional Deps). Total: 4 entries after this checkpoint.

      Step 5: Confirm `branch_protection_advisory` job on the next `ci.yml` run reports NO DRIFT. Open the most recent `ci.yml` run on `main` (after Step 3 completes) and check the "Branch Protection Advisory" job summary — must say "Live branch protection matches the expected read-only ruleset."

      Step 6 (OPTIONAL but recommended — EVID-01 enforcement self-test with the new check name):
        gh workflow run gate-self-test.yml -f check_name='Trust Lane Repo Head ('
        gh run watch

        Uses the `check_name` input from Plan 01 to point the existing synthetic-failure self-test at the new required lane. The workflow opens a synthetic-failure draft PR, polls for the named check to fail, then closes the PR. A `result=blocked` outcome in the run summary proves the gate is enforcing halt-on-failure for the new required lane.

      Failure-handling notes:
        - If Step 4 shows the new entry is MISSING from live protection, Step 3 didn't actually run (check `gh run view` on the dispatched workflow or the local script's exit code). Re-run Step 3 before declaring the checkpoint complete.
        - If Step 5 shows DRIFT after Step 3 succeeded, the live state and the expected state disagree — typically because Task 1's commit hadn't actually merged to `main` when Step 3 ran. Re-run from Step 1.

    Resume-signal protocol:
      Type `approved` after confirming:
        - `gh api .../branches/main/protection | jq -r '.required_status_checks.contexts[]'` lists `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`.
        - `branch_protection_advisory` on the latest `main` CI run says "matches the expected read-only ruleset" (no drift).
        - (Optional) `gate-self-test.yml -f check_name='Trust Lane Repo Head ('` returned `result=blocked`.
      If anything failed, describe what failed and which step.
  </action>
  <verify>
    <human-check>
      Maintainer-executed end-to-end check (cannot be automated by Claude; requires `BRANCH_PROTECTION_PAT` admin token or authenticated `workflow_dispatch` access — see action body):

        1. `gh api repos/szTheory/mailglass/branches/main/protection | jq -r '.required_status_checks.contexts[]'` includes the literal line `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`.
        2. `branch_protection_advisory` job on the latest `main` CI run reports "matches the expected read-only ruleset" (no drift).
        3. (Optional, strongly recommended) `gh workflow run gate-self-test.yml -f check_name='Trust Lane Repo Head ('` returns `result=blocked` in the run summary — proves end-to-end EVID-01 enforcement is live.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Maintainer-side: `gh api repos/szTheory/mailglass/branches/main/protection | jq -r '.required_status_checks.contexts[]'` output contains exactly 4 entries, one of which is `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`; the other three are the pre-existing `Support Contract Core (Elixir 1.18 / OTP 27)`, `Support Contract Admin (Elixir 1.18 / OTP 27)`, `Compile No Optional Deps (Elixir 1.18 / OTP 27)`.
    - Maintainer-side: the `branch_protection_advisory` job in the next post-merge `main` CI run summary reads "Live branch protection matches the expected read-only ruleset" (no drift).
    - Maintainer-side (optional but strongly recommended): `gh workflow run gate-self-test.yml -f check_name='Trust Lane Repo Head ('` produces a run whose summary line reports `Tests check result: blocked` (using the new parameterized `check_name`-aware summary from Plan 01 Task 2). This is the conclusive EVID-01-is-enforcing-not-just-declared proof.
    - Maintainer types `approved` in the chat to release the checkpoint; if any of the three checks failed, maintainer instead describes which step failed and the observed output so the planner can recover.
  </acceptance_criteria>
  <done>
    Live GitHub branch protection on `szTheory/mailglass`'s `main` branch lists `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` as a required status check; `branch_protection_advisory` reports no drift on the next CI run; (optionally) `gate-self-test.yml` with `check_name='Trust Lane Repo Head ('` proves the gate blocks a synthetic failing PR; maintainer has typed `approved`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted PR author → `.github/workflows/ci.yml` (workflow edit) | Any PR can propose edits to `ci.yml`. The new required check makes merging contingent on the lane passing; this raises the cost of an attempted bypass. Defense: the same PR + required-check process that protects all of `ci.yml` (including the unrelated existing required lanes) protects the trust-lane edits. |
| Untrusted PR author → `scripts/setup_branch_protection.sh` (REQUIRED_CHECKS edit) | A PR could attempt to remove the new entry from `REQUIRED_CHECKS`. Defenses: (a) Plan 01's `test/scripts/required_checks_test.exs` Phase-27-lock-protection test fails if a stability-lock entry disappears (extended in a future phase to cover trust lanes if needed); (b) `branch_protection_advisory` job advisory output highlights drift between the array and live GitHub protection; (c) post-merge `branch-protection-drift.yml` daily cron re-asserts the expected state, so a temporary removal is auto-reverted within 24h. |
| GitHub-hosted runner → `mailglass`/`mailglass_admin`/`mailglass_inbound` on Hex (clean-baseline lane `mix deps.get`) | The clean-baseline lane downloads from Hex.pm. The Hex-first check guarantees the *resolution source* is Hex (catches a malicious path-override), but package-content trust is delegated to the existing `Hex Audit (Elixir 1.18 / OTP 27)` required lane which runs `mix hex.audit` against the same lockfile space. |
| Trust-runner checkpoint JSON → uploaded artifact (90-day retention) | The artifact is downloadable for 90 days by anyone with repo read access. The checkpoint contains stage/status/fixture_id rows and bounded evidence (`scenario`, `outcome`, `route_clause_dimensions`, `trace_card_count`, etc.) — no PII, no raw payloads. The existing `scripts/check_trust_runner_checkpoint.sh` validator (lines 137, 149-158) rejects forbidden evidence keys before upload, providing schema-level enforcement of the PII boundary. |
| Maintainer's `BRANCH_PROTECTION_PAT` → live branch protection (Task 2 checkpoint) | The token has admin scope on the repo. Step 3 uses it once to PUT the expected ruleset. The token never leaves the maintainer's local env or the repo Secrets (`branch-protection-drift.yml` consumes it from Secrets, never logs it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-A | Tampering / Elevation of Privilege | `.github/workflows/ci.yml` (workflow edit bypassing the new required check) | mitigate | Atomic three-touch commit per D-02 + Pitfall 1: workflow + REQUIRED_CHECKS array + print_expected_text heredoc land in one commit (Task 1). Post-merge re-assertion (Task 2 checkpoint) immediately registers the new check with GitHub's branch protection, eliminating the Pitfall-1 transient-drift window. EVID-01 enforcement is then live within minutes of merge, not at the next daily cron. The optional `gate-self-test.yml -f check_name='Trust Lane Repo Head ('` proves the new required check actually blocks a failing PR end-to-end. |
| T-59-B | Tampering | `reference/host_app/mix.lock` (path-dependency leakage in clean-baseline lane) | mitigate | Clean-baseline lane runs `scripts/check_clean_baseline_hex_only.sh` (Plan 01) AFTER `mix deps.get` in `working-directory: reference/host_app/`, inspecting the resolved lockfile (NOT `mix.exs` text) for `:hex` source on `mailglass`/`mailglass_admin`/`mailglass_inbound`. Per D-08, this catches future contributor `path:` overrides AND transitive resolution surprises. Distinct cache key (`-reference-host-` prefix per Pattern 4 + Pitfall 5) prevents the repo-head lane's dev-path-polluted `deps/` from masking the failure. |
| T-59-C | Information Disclosure | Checkpoint JSON uploaded as long-retention artifact | mitigate (defense in depth) | Three layers: (1) Runner-side: `lib/mailglass/reference_host/trust_checkpoint.ex` whitelist-encodes only `schema_version`, `claim_boundary`, `checkpoint_count`, `checkpoint_sha256`, and per-stage `{stage, status, fixture_id, evidence}` — never PII. (2) Validator-side: `scripts/check_trust_runner_checkpoint.sh:137-158` rejects forbidden evidence keys (`raw_payload`, `payload`, `headers`, `recipient`, `sender`, `subject`, `html`) recursively; lane fails before the upload step if any forbidden key appears. (3) Path-side: `path:` on `actions/upload-artifact` is the exact file (`checkpoint.json`), NOT the parent directory `tmp/mailglass_trust_runner/` — prevents accidental inclusion of any other file a future runner change might write alongside (Pitfall 6). |
| T-59-D | Tampering / Elevation of Privilege | `publish-hex.yml::gate-ci-green` `ADVISORY_LANES` allowlist (bypass via lane downgrade to advisory) | mitigate | Neither new lane is added to `ADVISORY_LANES` (current list: `['Operator Browser Gate']`). Default inclusion means any non-success outcome of either trust lane blocks `gate-ci-green`. Future PR that tries to add either lane name to `ADVISORY_LANES` requires editing `publish-hex.yml`, which is itself a required-check artifact and goes through code review — the editor would need to articulate why a trust gate should become advisory, which is the correct human-judgment surface for that decision. |
| T-59-E | Repudiation | Trust lane silently skipped due to conditional logic | mitigate | Neither new job has an `if:` condition (Pitfall 2 — `gate-ci-green::isAdvisory` filter at `publish-hex.yml:175` treats `skipped` as non-blocking). The lanes always run on every `ci.yml` trigger (push to `main`, `pull_request` to `main`, `workflow_dispatch`). Verifier: `awk '/^  trust_lane_(repo_head\|clean_baseline):/,/^  [a-z_]/{print}' .github/workflows/ci.yml \| grep -cE '^    if:'` reports `0`. |
| T-59-F | Repudiation | Artifact retention bypass (org-level billing-tier downgrade reducing retention cap) | accept | `retention-days: 90` is GitHub's default maximum (1-90 inclusive) and survives all current billing tiers including Free. If the org downgrades to a tier with reduced retention, in-flight artifacts are not retroactively deleted but new uploads cap at the lower value. This is an operational concern outside Phase 59's design surface; Phase 60 release-evidence ingest is resilient because it queries artifacts during the release ceremony (the same week, typically), not at 90 days. |
| T-59-SC | Supply Chain | Package legitimacy gate | n/a — no installs | Phase 59 installs no new external packages. All Actions reuse SHAs already pinned in `ci.yml` (Dependabot bumps one place). The clean-baseline lane DOES run `mix deps.get` against `reference/host_app/mix.lock`, but those packages (vetted at Phase 52 HOST-01/02) are the SAME packages already covered by the existing `Hex Audit (Elixir 1.18 / OTP 27)` required lane — no new supply-chain surface. RESEARCH.md "Package Legitimacy Audit" explicitly marks this section as not-applicable for this phase. |
</threat_model>

<verification>
- `actionlint .github/workflows/ci.yml` exits 0.
- `shellcheck scripts/setup_branch_protection.sh` exits 0.
- `grep -Fq 'trust_lane_repo_head:' .github/workflows/ci.yml && grep -Fq 'trust_lane_clean_baseline:' .github/workflows/ci.yml` succeed.
- `grep -cF 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)' scripts/setup_branch_protection.sh` reports `2` (array + heredoc bullet).
- `grep -cF 'Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)' scripts/setup_branch_protection.sh` reports `0` (A1 lock: clean-baseline is publish-gate-required only).
- `bash scripts/setup_branch_protection.sh --print-expected-json | jq -r '.required_status_checks.contexts[]' | grep -Fxq 'Trust Lane Repo Head (Elixir 1.18 / OTP 27)'` succeeds.
- Pinned upload-artifact SHA reused at least 3 times (preview + 2 trust lanes): `grep -cF 'ea165f8d65b6e75b540449e92b4886f43607fa02' .github/workflows/ci.yml` reports `>= 3`.
- Both new lanes use `retention-days: 90`: `grep -c 'retention-days: 90' .github/workflows/ci.yml` reports `2`.
- Local smoke for both lanes (repo-head and clean-baseline) passes from the executor's shell before push.
- `MIX_ENV=test mix test test/scripts/required_checks_test.exs` reports `2 tests, 0 failures` (Plan 01 contract test stays green; array + heredoc evolved together).
- Feature-branch CI run shows both new lanes green; artifacts downloadable + re-validatable via `scripts/check_trust_runner_checkpoint.sh`.
- Post-merge: `gh api repos/szTheory/mailglass/branches/main/protection | jq -r '.required_status_checks.contexts[]'` lists `Trust Lane Repo Head (Elixir 1.18 / OTP 27)`.
- Post-merge: `branch_protection_advisory` job on next `main` CI run reports NO DRIFT.
</verification>

<success_criteria>
1. (EVID-01) `Trust Lane Repo Head (Elixir 1.18 / OTP 27)` is a required check on `main` branch protection — verifiable via `gh api .../branches/main/protection | jq -r '.required_status_checks.contexts[]'` after Task 2 completion. The lane fails (exit non-zero) when the trust checkpoint is missing or malformed (delegated to `scripts/check_trust_runner_checkpoint.sh`'s pre-existing exit-1-on-missing behavior).
2. (EVID-02) `Trust Lane Clean Baseline (Elixir 1.18 / OTP 27)` exists in `ci.yml`, operates from `working-directory: reference/host_app/`, runs `scripts/check_clean_baseline_hex_only.sh` (from Plan 01) after `mix deps.get`, runs `mix verify.reference_host.journey`, and validates the resulting `reference/host_app/tmp/mailglass_trust_runner/checkpoint.json`. Path-dependency leakage on any sibling causes lane failure at the Hex-first guard step. Lane is required at the publish-gate layer (default `gate-ci-green` inclusion) per A1 lock; not in `REQUIRED_CHECKS` per the EVID-02-doesn't-say-required reading.
3. (EVID-04) Both lanes upload `trust_runner.v1` checkpoint JSON via `actions/upload-artifact@<pinned-v4-SHA>` with `if-no-files-found: error`, `retention-days: 90`, and `${{ github.run_id }}`-suffixed names (`trust-runner-repo-head-<id>` and `trust-runner-clean-baseline-<id>`). Phase 60 release ceremony can download via `pattern: 'trust-runner-*'` and re-validate with `scripts/check_trust_runner_checkpoint.sh`.
4. (Atomic landing) All three edits land in ONE commit; `branch-protection-drift.yml` cron does not flag transient drift; `branch_protection_advisory` job on the post-merge `main` CI run reports no drift after Task 2 re-assertion.
5. (Pinned-Actions hygiene) No new Action SHAs introduced; Dependabot continues to bump one place per Action across the entire `ci.yml`.
</success_criteria>

<output>
Create `.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-02-SUMMARY.md` when done.
</output>
