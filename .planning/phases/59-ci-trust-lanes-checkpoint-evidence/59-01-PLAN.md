---
phase: 59-ci-trust-lanes-checkpoint-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_clean_baseline_hex_only.sh
  - .github/workflows/gate-self-test.yml
  - test/scripts/required_checks_test.exs
autonomous: true
requirements:
  - EVID-01
  - EVID-02
tags:
  - ci
  - trust-evidence
  - hex-first
  - branch-protection

must_haves:
  truths:
    - "scripts/check_clean_baseline_hex_only.sh exits 0 when reference/host_app/mix.lock resolves all three siblings via :hex, exits 1 on :path/:git source for any sibling, and exits 1 when mix.lock is missing/empty."
    - "gate-self-test.yml accepts a check_name workflow_dispatch input that controls which required-check name prefix the synthetic-failure poll loop watches; default value preserves the existing 'Tests (' behavior so current invocations are non-breaking."
    - "test/scripts/required_checks_test.exs fails when scripts/setup_branch_protection.sh's REQUIRED_CHECKS array drifts from the bulleted list printed by print_expected_text (any name present in one but missing in the other raises a descriptive assertion)."
  artifacts:
    - path: "scripts/check_clean_baseline_hex_only.sh"
      provides: "Reusable Hex-source guard invoked from the clean-baseline lane and re-runnable locally for Phase 60 release ceremony"
      contains: "elixir -e"
      executable: true
    - path: ".github/workflows/gate-self-test.yml"
      provides: "Parameterized check_name input + name-prefix poll filter sourced from the input"
      contains: "check_name"
    - path: "test/scripts/required_checks_test.exs"
      provides: "ExUnit contract test asserting REQUIRED_CHECKS ↔ print_expected_text drift detection"
      contains: "REQUIRED_CHECKS"
  key_links:
    - from: ".github/workflows/gate-self-test.yml"
      to: "inputs.check_name"
      via: "${{ inputs.check_name }} substituted into the gh pr checks --jq selector"
      pattern: "startswith\\(.{0,40}check_name"
    - from: "scripts/check_clean_baseline_hex_only.sh"
      to: "reference/host_app/mix.lock"
      via: "File.read! + Code.eval_string in elixir -e, asserting first tuple element of each sibling entry is :hex"
      pattern: ":hex"
    - from: "test/scripts/required_checks_test.exs"
      to: "scripts/setup_branch_protection.sh"
      via: "Path.expand + File.read! of the script + regex/parse of REQUIRED_CHECKS array and print_expected_text bullets"
      pattern: "REQUIRED_CHECKS"
---

<objective>
Land the three Wave 0 preconditions that Plan 02 depends on, ahead of any
`ci.yml` lane addition. None of these touch branch-protection state or
`ci.yml` job listings, so this plan is independently mergeable and
independently verifiable.

Purpose: Plan 02 atomically lands a new `ci.yml` job, its `REQUIRED_CHECKS`
entry, and its `print_expected_text` bullet (D-02 + Pitfall 1: must be one
commit, or `branch-protection-drift.yml` flags transient drift). For Plan 02
to be a true single-commit landing, the reusable Hex-source script must
already exist, the gate-self-test workflow must already be parameterizable,
and the array/heredoc drift-detection contract test must already be tracking
the file. This plan ships those three preconditions.

Output:
  - `scripts/check_clean_baseline_hex_only.sh` — bash + inline `elixir -e`
    validator that asserts `mailglass`, `mailglass_admin`, `mailglass_inbound`
    entries in `reference/host_app/mix.lock` each have `:hex` as the first
    tuple element. Exits 1 on `:path`/`:git`/missing entry, exits 0 on
    Hex-only resolution.
  - `.github/workflows/gate-self-test.yml` — extended with a `check_name`
    `workflow_dispatch` input defaulting to `"Tests ("` (current literal),
    plumbed into the existing poll-loop `--jq` selector. Existing invocations
    that omit the input continue to behave identically (EVID-01 enforcement
    proof becomes observable for any required-check name without forking
    the workflow).
  - `test/scripts/required_checks_test.exs` — ExUnit contract test that
    parses `REQUIRED_CHECKS` from `scripts/setup_branch_protection.sh` and
    the bullet list inside its `print_expected_text` heredoc, then asserts
    they contain the same set of names (catches Pitfall 7 / Plan 02
    array-vs-heredoc drift before it ships).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-CONTEXT.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-PATTERNS.md
@.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-VALIDATION.md

@scripts/setup_branch_protection.sh
@scripts/check_trust_runner_checkpoint.sh
@.github/workflows/gate-self-test.yml
@reference/host_app/mix.lock
@test/reference_host/trust_runner_command_contract_test.exs

<interfaces>
<!-- These are the verbatim contracts Plan 02 will rely on. -->
<!-- Plan 01 ships the producers; Plan 02 wires them. -->

`scripts/check_clean_baseline_hex_only.sh` invocation contract (called from clean-baseline lane in Plan 02):
  - Working directory at call site: `reference/host_app/`
  - Invocation: `bash ../../scripts/check_clean_baseline_hex_only.sh`
  - Optional positional arg: lockfile path (default: `mix.lock` resolved against cwd)
  - Exit 0: all three siblings resolved via :hex
  - Exit 1: any sibling resolved via :path/:git OR missing OR mix.lock empty/absent
  - stdout: per-sibling "Hex-first OK: <name> resolved via :hex (version: <vsn>)"
  - stderr: error message prefixed "Clean-baseline Hex-first check blocked: ..."
    (matches `scripts/check_trust_runner_checkpoint.sh` failure-message convention)

`gate-self-test.yml` input contract (called by Plan 02 verification + ongoing repo hygiene):
  - New input: `check_name` (type: string, default: "Tests (")
  - Existing input: `cleanup_only` (preserved unchanged)
  - Poll loop's gh-CLI `--jq` filter uses the input value via `startswith("${{ inputs.check_name }}")`
  - Summary block + error/success echoes reference `${{ inputs.check_name }}` instead of the literal "Tests check"

`test/scripts/required_checks_test.exs` contract (regression surface for Plan 02 and beyond):
  - Module: `Mailglass.Scripts.RequiredChecksTest`
  - `use ExUnit.Case, async: true` (no DB, no application start)
  - Asserts: every name in `REQUIRED_CHECKS` array appears as `  - <name>` bullet in the `print_expected_text` heredoc
  - Asserts: every `  - <name>` bullet in the `print_expected_text` heredoc (above the blank line that precedes "Expected non-context branch protection fields:") appears in `REQUIRED_CHECKS`
  - Does NOT hardcode the post-Phase-59 trust-lane name; the test is name-agnostic, so it stays green during Plan 01 (3 entries) and Plan 02 (4 entries) without modification

Hex entry shape in `reference/host_app/mix.lock` (verified by Read):
  Each entry has the form `"name" => {:hex, :name_atom, "version", "inner_sha", [:mix], [], "hexpm", "outer_sha"}`.
  First tuple element is the source atom (`:hex`, `:path`, or `:git`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check_clean_baseline_hex_only.sh (reusable Hex-source guard)</name>
  <files>scripts/check_clean_baseline_hex_only.sh</files>
  <read_first>
    - scripts/check_trust_runner_checkpoint.sh (canonical analog: shebang, `set -euo pipefail`, default-with-override arg parsing, missing-file guard, heredoc-embedded interpreter, stderr-only error output prefixed with the script's purpose noun + "blocked:")
    - scripts/setup_branch_protection.sh (sibling shell-script convention; OWNER/REPO_NAME shell-var pattern is informative but not used here)
    - reference/host_app/mix.lock (verify each of the three siblings resolves to `{:hex, ...}` today, so the script's happy-path output is correct out of the box)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md (sections "Hex-First Source Script" lines 697-735, "Pitfall 3" guard pattern, "Pattern 2" Elixir term-evaluation justification)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-PATTERNS.md (`scripts/check_clean_baseline_hex_only.sh` section, lines 230-330, for verbatim wrapper shape)
  </read_first>
  <action>
    Create `scripts/check_clean_baseline_hex_only.sh` per D-08 (clean-baseline Hex-first enforcement via `mix.lock` term evaluation, not text grep), D-07 (lane runs from `reference/host_app/` working directory), and Pitfall 3 (preflight-guard missing/empty lockfile).

    Shape (follow `scripts/check_trust_runner_checkpoint.sh` wrapper conventions exactly):
      1. Shebang `#!/usr/bin/env bash`, blank line, one-line comment describing the script's purpose ("Assert reference/host_app/mix.lock resolves mailglass siblings via :hex source only.") and a second comment noting "Run from reference/host_app/ working directory."
      2. `set -euo pipefail`.
      3. `LOCK_PATH="${1:-mix.lock}"` — positional override for ad-hoc local invocation; default resolves against the caller's cwd.
      4. Missing/empty guard: `if [[ ! -s "$LOCK_PATH" ]]; then echo "Clean-baseline Hex-first check blocked: missing or empty $LOCK_PATH" >&2; exit 1; fi` (Pitfall 3; failure-message format mirrors `check_trust_runner_checkpoint.sh:40` convention).
      5. `elixir -e "..."` block that:
         - Reads the lockfile via `File.read!("$LOCK_PATH") |> Code.eval_string() |> elem(0)` (Pattern 2 — `mix.lock` is an Elixir term; this is the canonical parse path per the language's own reference implementation).
         - Iterates over the required list `[{"mailglass", :hex}, {"mailglass_admin", :hex}, {"mailglass_inbound", :hex}]`.
         - For each `{name, expected_source}`: `Map.get(lock, name)` → match `tuple when is_tuple(tuple) and elem(tuple, 0) == expected_source` → print `"Hex-first OK: #{name} resolved via :hex (version: #{elem(tuple, 2)})"` to stdout; on a tuple with a different first element, print `"Hex-first violation: #{name} resolved via #{inspect(elem(tuple, 0))}, expected :hex"` to stderr and `System.halt(1)`; on `nil`, print `"Hex-first violation: #{name} missing from $LOCK_PATH"` to stderr and `System.halt(1)`.
         - Exact source code is in 59-RESEARCH.md "Hex-First Source Script" (lines 700-735). Copy that block verbatim.

    File-mode contract: after writing, `chmod +x scripts/check_clean_baseline_hex_only.sh` so it matches every other `scripts/*.sh` in the repo (all current `.sh` files are `-rwxr-xr-x`; verifiable with `ls -la scripts/`).

    Do NOT: use `grep` against `mix.lock` text (D-08 explicitly rejects this; brittle to format changes and false-positives on `path:` substrings inside hex-source requirement metadata). Do NOT add the script to `scripts/` README or any other discovery surface in this task — it is wired by Plan 02's `ci.yml` job and is callable directly by humans; no extra documentation is in scope per Phase 59's stated boundary (no docs changes — DOCB-01..03 is Phase 61).
  </action>
  <verify>
    <automated>shellcheck scripts/check_clean_baseline_hex_only.sh && test -x scripts/check_clean_baseline_hex_only.sh && (cd reference/host_app && mix deps.get >/dev/null && bash ../../scripts/check_clean_baseline_hex_only.sh) && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck scripts/check_clean_baseline_hex_only.sh` exits 0 (no SC warnings).
    - `test -x scripts/check_clean_baseline_hex_only.sh` exits 0 (executable bit set; matches every other `scripts/*.sh`).
    - `head -1 scripts/check_clean_baseline_hex_only.sh` is exactly `#!/usr/bin/env bash`.
    - `grep -Fq 'set -euo pipefail' scripts/check_clean_baseline_hex_only.sh` succeeds.
    - `grep -Fq 'Clean-baseline Hex-first check blocked:' scripts/check_clean_baseline_hex_only.sh` succeeds (Pitfall 3 missing-file guard message).
    - `grep -Fq 'mailglass_admin' scripts/check_clean_baseline_hex_only.sh && grep -Fq 'mailglass_inbound' scripts/check_clean_baseline_hex_only.sh` succeeds (both siblings present in the required list).
    - `grep -Fqv 'grep' scripts/check_clean_baseline_hex_only.sh` (D-08 anti-pattern: must NOT use grep against mix.lock — there is no grep in the script).
    - Happy path: `(cd reference/host_app && mix deps.get >/dev/null && bash ../../scripts/check_clean_baseline_hex_only.sh)` exits 0 and stdout contains `Hex-first OK: mailglass`, `Hex-first OK: mailglass_admin`, `Hex-first OK: mailglass_inbound`.
    - Missing-lockfile sad path: `bash scripts/check_clean_baseline_hex_only.sh /nonexistent/mix.lock` exits 1 and stderr contains `Clean-baseline Hex-first check blocked: missing or empty`.
    - Synthetic path-leakage sad path (executor runs once, does NOT commit): copy `reference/host_app/mix.lock` to a temp dir, rewrite the `"mailglass"` entry's leading `:hex` atom to `:path`, then run `bash scripts/check_clean_baseline_hex_only.sh <tmp-copy>` — must exit 1 and stderr must contain `Hex-first violation: mailglass resolved via :path, expected :hex`.
  </acceptance_criteria>
  <done>
    `scripts/check_clean_baseline_hex_only.sh` exists, is executable, passes shellcheck, exits 0 on current `reference/host_app/mix.lock`, and exits 1 with the locked stderr message on a synthetic `:path`-override copy of that lockfile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize gate-self-test.yml with check_name input (EVID-01 enforcement-proof surface)</name>
  <files>.github/workflows/gate-self-test.yml</files>
  <read_first>
    - .github/workflows/gate-self-test.yml (current file — `workflow_dispatch.inputs.cleanup_only` shape at lines 13-19; poll loop with hardcoded `startswith("Tests (")` literal at lines 117-141; "Summary" step at lines 154-169 with the "Tests check result" wording)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-PATTERNS.md (section "`.github/workflows/gate-self-test.yml` — parameterize `check_name`" at lines 180-227 — exact substitution pattern, anti-pattern note)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md (Pitfall 7 — Negative-Path Test Missing — describes WHY this extension exists)
  </read_first>
  <action>
    Extend `.github/workflows/gate-self-test.yml` with a new `check_name` workflow_dispatch input and substitute its value into the existing poll loop. Keep the default such that current invocations (run via the Actions tab with no inputs) behave identically — this is a backwards-compatible parameterization, not a rewrite.

    Edits (single file, three edit regions):

      1. `workflow_dispatch.inputs` block (currently has only `cleanup_only` at lines 16-19): append a new input AFTER `cleanup_only`, following the same `description:` / `type:` / `default:` shape:
         - key: `check_name`
         - description: `"Required-check name prefix to poll (e.g., 'Tests (' for the test gate or 'Trust Lane Repo Head (' for the v1.3 trust gate)"`
         - type: `string`
         - default: `"Tests ("` (current hardcoded literal — preserves behavior of existing invocations)

      2. Poll loop step (currently lines 107-141): in the `--jq` selector at line 119, replace the literal string `"Tests ("` with `"${{ inputs.check_name }}"` (preserve the surrounding `select(.name | startswith(...))` syntax; the GitHub Actions expression interpolates BEFORE the shell parses the heredoc, so the quoting layers are: outer single-quoted jq filter → embedded double-quoted interpolation result). Replace the literal `"Tests check"` in the four `echo` lines (one each in the FAILURE/FAILED/CANCELLED/TIMED_OUT branch, the SUCCESS branch's two echoes, and the `*)` still-running branch) with the input value via `"${{ inputs.check_name }}check"` — see PATTERNS.md for the exact substitution shape. Pattern: `${{ inputs.check_name }}` appears in every echo where the old code said `Tests check` or `Tests`.

      3. Summary step (currently lines 154-169): replace the literal `"- Tests check result:"` with `"- ${{ inputs.check_name }}check result:"` and the literal `"Gate enforces halt-on-failure"` / `"Gate did NOT block the failing PR"` echoes can stay unchanged (they describe the gate generically; the per-run summary line is what carries the parameterized name).

    Anti-pattern guard (per PATTERNS.md): do NOT fork the workflow into a second self-test file. Do NOT change the synthetic-failure injection (`test/gate_self_test/intentional_failure_test.exs` content embedded at lines 73-86) — an `assert false` test fails every required lane that exercises ExUnit, including the trust-runner journey lane in Plan 02, so no fixture change is needed.
  </action>
  <verify>
    <automated>actionlint .github/workflows/gate-self-test.yml && grep -Fq 'check_name:' .github/workflows/gate-self-test.yml && grep -F '${{ inputs.check_name }}' .github/workflows/gate-self-test.yml | wc -l | awk '{ if ($1 >= 2) exit 0; else { print "expected check_name interpolation in at least 2 places, found " $1; exit 1 } }' && grep -cF 'startswith("Tests (")' .github/workflows/gate-self-test.yml | awk '{ if ($1 == 0) exit 0; else { print "stale hardcoded literal still present: " $1 " occurrences"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/gate-self-test.yml` exits 0 (no workflow-syntax errors).
    - `grep -Fq 'check_name:' .github/workflows/gate-self-test.yml` succeeds (new input declared).
    - `grep -Fq 'default: "Tests ("' .github/workflows/gate-self-test.yml` succeeds (default preserves existing invocation behavior).
    - `grep -cF '${{ inputs.check_name }}' .github/workflows/gate-self-test.yml` reports `>= 2` (interpolated in at least the jq filter and the summary line — typically 5+ sites total: jq filter, FAILURE echo, SUCCESS echo, still-running echo, summary line).
    - `grep -cF 'startswith("Tests (")' .github/workflows/gate-self-test.yml` reports `0` (the hardcoded literal that this task is replacing must be gone; if any residual `"Tests ("` literal remains, the input wasn't fully plumbed).
    - `grep -Fq 'type: string' .github/workflows/gate-self-test.yml` succeeds (input type declared correctly per GitHub Actions input-type rules).
    - The two existing inputs co-exist: `grep -cE '^      (cleanup_only|check_name):' .github/workflows/gate-self-test.yml` reports `2`.
    - YAML still parses cleanly: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gate-self-test.yml'))"` exits 0.
  </acceptance_criteria>
  <done>
    `gate-self-test.yml` accepts a `check_name` workflow_dispatch input defaulting to `"Tests ("`; the poll loop's `--jq` selector and all human-readable echoes/summary lines reference the input instead of the old literal; `actionlint` passes; YAML parses; existing invocations without the input continue to behave identically.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create test/scripts/required_checks_test.exs (REQUIRED_CHECKS ↔ heredoc drift contract)</name>
  <files>test/scripts/required_checks_test.exs</files>
  <behavior>
    - Test 1 ("REQUIRED_CHECKS array and print_expected_text bullets stay in sync"): parses `REQUIRED_CHECKS` array entries from `scripts/setup_branch_protection.sh` and the `  - <name>` bullets from inside the `print_expected_text() { cat <<'TEXT' ... TEXT }` heredoc; asserts the two sets are equal. Failure message names the offending entry and which side is missing it. Passes with the current file (3 entries on both sides) and will continue to pass after Plan 02 adds a 4th entry on both sides — the test is name-agnostic and pure drift detection.
    - Test 2 ("REQUIRED_CHECKS contains at least the Phase 27 lock entries"): asserts the three v1.0-stability-lock entries are still present (`"Support Contract Core (Elixir 1.18 / OTP 27)"`, `"Support Contract Admin (Elixir 1.18 / OTP 27)"`, `"Compile No Optional Deps (Elixir 1.18 / OTP 27)"`). This prevents an accidental deletion of an existing required check from going unnoticed and is the regression backstop Pitfall 7 calls out.
    - Async: true; no DB; no application start; pure file read + string assertions. Runtime well under 100ms.
  </behavior>
  <read_first>
    - test/reference_host/trust_runner_command_contract_test.exs (canonical pinned-tokens contract analog: `use ExUnit.Case, async: true`; `Path.expand("../../<repo-relative>", __DIR__)` module attrs; `Enum.each` with descriptive assertion messages; defp helper at the bottom)
    - test/reference_host/trust_runner_checkpoint_contract_test.exs (sibling content-contract analog using the same path-expand convention)
    - scripts/setup_branch_protection.sh (lines 17-21 REQUIRED_CHECKS array; lines 23-42 print_expected_text heredoc — note the heredoc terminates with `TEXT` followed by `}`; the bullets ` - <name>` end at the blank line that precedes "Expected non-context branch protection fields:")
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-PATTERNS.md (section "test/scripts/required_checks_test.exs" at lines 335-389 for exact module-naming + path-expand + test-name conventions)
    - .planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-RESEARCH.md (Pitfall 7 — Negative-Path Test Missing — describes the contract surface this test owns)
  </read_first>
  <action>
    Create `test/scripts/` directory (currently absent — verifiable with `find test -maxdepth 1 -type d -name scripts`; sibling directories `test/reference_host/`, `test/credo_checks/`, `test/scripts/` follow the one-directory-per-logical-surface convention). Inside it, create `required_checks_test.exs`.

    Module: `Mailglass.Scripts.RequiredChecksTest` (matches the `Mailglass.<Area>.<Thing>ContractTest` naming convention used by `Mailglass.ReferenceHost.TrustRunnerCommandContractTest` and `Mailglass.ReferenceHost.TrustRunnerCheckpointContractTest`).

    `use ExUnit.Case, async: true` (no DB; no application start; pure file read + string parse).

    Module attrs (use `Path.expand("../../scripts/setup_branch_protection.sh", __DIR__)` per the analog's `Path.expand("../../mix.exs", __DIR__)` pattern):
      - `@script_path Path.expand("../../scripts/setup_branch_protection.sh", __DIR__)`
      - `@v1_0_lock_entries [` three strings:
        - `"Support Contract Core (Elixir 1.18 / OTP 27)"`
        - `"Support Contract Admin (Elixir 1.18 / OTP 27)"`
        - `"Compile No Optional Deps (Elixir 1.18 / OTP 27)"` — these are the locked Phase 27 entries that must never disappear (Pitfall 7 regression backstop).

    Parse helpers (defp at module bottom):
      - `parse_required_checks/1` — given the script source, extracts every quoted string inside the `REQUIRED_CHECKS=( ... )` array. Recommended approach: split source on the literal `REQUIRED_CHECKS=(\n`, take the second chunk, split on the literal `\n)`, take the first chunk, then `Regex.scan(~r/"([^"]+)"/, chunk)` to extract each entry; return a `MapSet`. This survives whitespace changes inside the array but locks the array's textual shape.
      - `parse_print_expected_bullets/1` — given the script source, extracts the `  - <name>` bullets from inside the `print_expected_text()` heredoc. Recommended approach: split source on `cat <<'TEXT'\nExpected required status checks:\n`, take the second chunk, split on the first `\n\nExpected non-context branch protection fields:` (the blank-line separator BEFORE the second heredoc section — this gives only the required-checks bullets, NOT the non-context-fields bullets), then `Regex.scan(~r/^  - (.+)$/m, chunk)` returning a `MapSet`.

    Tests:
      1. `test "REQUIRED_CHECKS array and print_expected_text bullets stay in sync"` — read the script once, parse both sets, compute symmetric difference, assert the difference is empty with a failure message that lists which names are in the array but missing from the heredoc and vice versa. This is the Plan 02 safety net (without it, Plan 02 could update the array but forget the heredoc and `branch_protection_advisory` would silently pass the drift check while live branch-protection lookup would fail).
      2. `test "REQUIRED_CHECKS contains every Phase 27 stability-lock entry"` — assert each of `@v1_0_lock_entries` is present in the parsed array set, with a failure message that names the missing entry.

    Test location: create `test/scripts/` directory; ExUnit auto-discovers `.exs` files under `test/` so no `mix.exs` change is required (verifiable: the existing `test/reference_host/*.exs` files are discovered by `mix test` without any explicit registration).

    Do NOT: shell out to `bash scripts/setup_branch_protection.sh --print-expected` (would test the output but not the source-of-truth array definition — the array could differ from the heredoc and the shell-out would only see the heredoc; this test must read the source). Do NOT hardcode the post-Phase-59 trust-lane name in this test — the drift-detection logic is name-agnostic on purpose.
  </behavior>
  <action>
    See `<behavior>` above for the full implementation guidance. Write the module exactly as described.

    Important: this test runs in the standard `mix test` lane. Confirm the directory is auto-discovered by running `mix test test/scripts/required_checks_test.exs` after creation — it must pass with the current 3-entry array.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/scripts/required_checks_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `test/scripts/required_checks_test.exs` exists and `test/scripts/` directory is created (`test -d test/scripts && test -f test/scripts/required_checks_test.exs`).
    - `grep -Fq 'defmodule Mailglass.Scripts.RequiredChecksTest' test/scripts/required_checks_test.exs` succeeds (naming convention matches existing `Mailglass.<Area>.<Thing>ContractTest` siblings).
    - `grep -Fq 'use ExUnit.Case, async: true' test/scripts/required_checks_test.exs` succeeds.
    - `grep -Fq 'Path.expand("../../scripts/setup_branch_protection.sh", __DIR__)' test/scripts/required_checks_test.exs` succeeds (per-analog path convention).
    - `MIX_ENV=test mix test test/scripts/required_checks_test.exs` reports `2 tests, 0 failures` and exit 0 against the current `scripts/setup_branch_protection.sh` (3 entries, 3 bullets, in sync).
    - Drift-detection sad path (executor runs once, does NOT commit): temporarily append `"Synthetic Drift Check (Elixir 1.18 / OTP 27)"` to the `REQUIRED_CHECKS` array WITHOUT updating the `print_expected_text` heredoc, run `MIX_ENV=test mix test test/scripts/required_checks_test.exs` — must fail with a message naming `"Synthetic Drift Check"` as present in the array but missing from the heredoc. Revert the synthetic edit before committing.
    - Phase-27-lock-protection sad path (executor runs once, does NOT commit): temporarily comment out the `"Compile No Optional Deps (Elixir 1.18 / OTP 27)"` line in `scripts/setup_branch_protection.sh` (both in the array AND the heredoc, so test 1 still passes), run the test — test 2 must fail naming `"Compile No Optional Deps (Elixir 1.18 / OTP 27)"` as the missing Phase 27 lock entry. Revert before committing.
    - The test does NOT reference `"Trust Lane Repo Head"` anywhere (`grep -cF 'Trust Lane Repo Head' test/scripts/required_checks_test.exs` reports `0`) — name-agnostic by design; Plan 02's addition will not require editing this test.
  </acceptance_criteria>
  <done>
    `test/scripts/required_checks_test.exs` exists with two tests; both pass on the current 3-entry `scripts/setup_branch_protection.sh`; sad-path manual rehearsals (added drift entry, removed Phase-27-lock entry) demonstrate each test fails with a descriptive message and is reverted before commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor → `scripts/check_clean_baseline_hex_only.sh` (run inside CI lane or locally) | The lockfile path argument is untrusted in principle (positional `$1`); the script must not interpolate it into shell commands that could execute it. The `elixir -e` block embeds the path via string interpolation INSIDE a single-quoted Elixir literal that is then passed as a single argument to `elixir`; the path is read by `File.read!/1` only. No shell metachar expansion. |
| GitHub Actions runner → repo source files at test time | `test/scripts/required_checks_test.exs` reads `scripts/setup_branch_protection.sh` as plain text via `File.read!/1`. No exec, no eval of file content. Pure parsing. |
| Workflow dispatcher (any user with workflow_dispatch permission) → `gate-self-test.yml` `check_name` input | A malicious `check_name` value could be crafted to inject into the jq filter or echo strings. Mitigation: GitHub Actions evaluates `${{ inputs.check_name }}` at expression time, NOT shell-evaluation time, and the value lands inside a double-quoted jq string and bash double-quoted echo argument; jq treats it as a literal selector substring. The blast radius is limited to "the poll watches a check name nobody created" → poll times out after 25 min → workflow fails with a timeout message. No code execution path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-A | Tampering | `.github/workflows/ci.yml` workflow file (future Plan 02 edit) | mitigate | Out of scope for Plan 01 (Plan 02 ships the `ci.yml` job + REQUIRED_CHECKS entry atomically per D-02 + Pitfall 1). Plan 01 ships the `gate-self-test.yml` parameterization that makes the EVID-01 enforcement-proof observable for the new lane name. |
| T-59-B | Tampering | `reference/host_app/mix.lock` (path-dependency leakage) | mitigate | Plan 01 ships `scripts/check_clean_baseline_hex_only.sh` which inspects `mix.lock` post-`mix deps.get` and exits 1 if any of `mailglass`/`mailglass_admin`/`mailglass_inbound` has a non-`:hex` source. Plan 02 wires it into the clean-baseline lane so PR-time enforcement is automatic. |
| T-59-C | Information Disclosure | Checkpoint JSON uploaded as artifact | mitigate (delegated) | Out of scope for Plan 01 (no artifacts uploaded by Plan 01 surfaces). The existing `scripts/check_trust_runner_checkpoint.sh` rejects forbidden evidence keys (`raw_payload`, `payload`, `headers`, `recipient`, `sender`, `subject`, `html`) and is invoked by Plan 02 before any upload step runs. |
| T-59-D | Tampering / Elevation of Privilege | `publish-hex.yml::gate-ci-green` `ADVISORY_LANES` allowlist | mitigate | Out of scope for Plan 01 (no publish-gate change). The contract test in Plan 01 (`test/scripts/required_checks_test.exs`) protects the *required-check* allowlist on the branch-protection side; the publish-gate `ADVISORY_LANES` allowlist is protected by code review (it lives in `publish-hex.yml` which is itself a required-check artifact and any edit there flows through the same PR process). |
| T-59-SC | Supply Chain | Package legitimacy gate | n/a — no installs | Plan 01 installs no new packages. Verifications use already-pinned tooling (`shellcheck` on PATH or via `mix archive.install`, `actionlint` on PATH, the existing pinned `actions/checkout`/`erlef/setup-beam`/`actions/upload-artifact`/`actions/cache` SHAs in `ci.yml`). RESEARCH.md "Package Legitimacy Audit" section is explicitly not-applicable for this phase. |
</threat_model>

<verification>
- `shellcheck scripts/check_clean_baseline_hex_only.sh` exits 0.
- `(cd reference/host_app && mix deps.get && bash ../../scripts/check_clean_baseline_hex_only.sh)` exits 0.
- `actionlint .github/workflows/gate-self-test.yml` exits 0.
- `MIX_ENV=test mix test test/scripts/required_checks_test.exs` reports `2 tests, 0 failures`.
- `grep -cF 'startswith("Tests (")' .github/workflows/gate-self-test.yml` reports 0 (the stale hardcoded literal is gone; `check_name` interpolation replaces it).
- All three new/modified files passed individual task verify commands.
</verification>

<success_criteria>
1. `scripts/check_clean_baseline_hex_only.sh` exists, is executable, lints clean, exits 0 on current `reference/host_app/mix.lock`, exits 1 with the locked stderr message on a synthetic `:path`-override.
2. `.github/workflows/gate-self-test.yml` has a backwards-compatible `check_name` input plumbed through the poll loop and summary; current invocations (no input) behave identically because the default reproduces the previous hardcoded `"Tests ("` literal.
3. `test/scripts/required_checks_test.exs` exists and passes against the current 3-entry `scripts/setup_branch_protection.sh`; manual sad-path rehearsals demonstrate it would fail on either array/heredoc drift or removal of a Phase-27 lock entry.
4. Plan 02 has the prerequisites it needs: the Hex-source script is ready to wire, the gate-self-test enforcement-proof surface accepts arbitrary check names, and the array/heredoc contract is tracked.
</success_criteria>

<output>
Create `.planning/phases/59-ci-trust-lanes-checkpoint-evidence/59-01-SUMMARY.md` when done.
</output>
