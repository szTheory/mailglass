---
phase: 49-inbound-runtime-operator-tooling
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/operator/formatter.ex
  - mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex
  - mailglass_inbound/lib/mailglass_inbound/internal/prune.ex
  - mailglass_inbound/lib/mailglass_inbound/prune/worker.ex
  - mailglass_inbound/lib/mailglass_inbound/router/route.ex
  - mailglass_inbound/lib/mailglass_inbound/router.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
  - mailglass_inbound/test/mix/tasks/mailglass_inbound_doctor_test.exs
  - mailglass_inbound/test/mailglass_inbound/internal/doctor_test.exs
  - mailglass_inbound/test/mix/tasks/mailglass_inbound_replay_test.exs
  - mailglass_inbound/test/mailglass_inbound/internal/prune_test.exs
  - mailglass_inbound/test/mix/tasks/mailglass_inbound_prune_test.exs
autonomous: true
requirements: [IOPS-01, IOPS-02, IOPS-03, MIME-03]
must_haves:
  truths:
    - "mix mailglass.inbound.doctor runs DNS-free, exits 0 all-pass, 1 on fail (or warn under --strict), 2 cannot-diagnose; human + JSON output usable in CI"
    - "Doctor reports MIME backend name + version via OptionalDeps.GenSmtp.available?/0 + Application.spec(:gen_smtp, :vsn), no bare optional-dep ref (MIME-03)"
    - "Doctor route-conflict reuses Router.Matcher.matches_route?/2: subsumption + witness-probe → fail, regex-vs-regex → warn, findings name router.ex:line via Route.:source"
    - "mix mailglass.inbound.replay --record-id/--since/--tenant resolves selectors → id list → iterates Internal.Replay.replay/2 with [y/N]-default-No unless --yes; appends source: :replay (no UPDATE)"
    - "mix mailglass.inbound.prune enforces retention (records 90d/evidence 30d/fresh 90d/replay 30d, configurable, :infinity disables), batched LIMIT 1000 + advisory-lock single-run, runs synchronously with or without Oban"
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex"
      provides: "DNS-free check runner returning locked finding shape + summary"
      contains: "matches_route?"
    - path: "mailglass_inbound/lib/mailglass_inbound/internal/prune.ex"
      provides: "Oban-independent batched advisory-locked retention sweep prune/0"
      contains: "pg_try_advisory_lock"
    - path: "mailglass_inbound/lib/mailglass_inbound/operator/formatter.ex"
      provides: "render_human/2 + render_json/1 for doctor output"
      contains: "render_json"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex"
      to: "MailglassInbound.Router.Matcher.matches_route?/2"
      via: "route-conflict detection reuses the runtime matcher (never re-implement)"
      pattern: "Matcher\\.matches_route\\?"
    - from: "mailglass_inbound/lib/mailglass_inbound/internal/prune.ex"
      to: "MailglassInbound.Config"
      via: "reads retention windows from :mailglass_inbound config"
      pattern: "MailglassInbound\\.Config"
    - from: "mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex"
      to: "MailglassInbound.Internal.Replay.replay/2"
      via: "selector → id list → per-record iterate"
      pattern: "Replay\\.replay"
---

<objective>
Ship the three inbound operator mix tasks (IOPS-01 doctor, IOPS-02 replay, IOPS-03 prune) plus the supporting internal services, the shared output formatter, the route `:source` reflection field, and the optional Oban prune worker. MIME-03 lands inside the doctor's MIME-backend check.

Purpose: An operator running mailglass in production gets the inbound sibling of the shipped outbound toolbox — DNS-free pre-deploy config validation, CLI replay of failed/no-match records with confirmation, and retention enforcement with bounded batched deletes. Logic lives in unit-testable `Internal.*` services; the tasks are thin CLI shells.
Output: `Operator.Formatter`, `Internal.Doctor`, `Internal.Prune`, `Prune.Worker` (Oban-guarded), the `:source` field on `Router.Route` + `__CALLER__` capture in `route/2`, and the three `Mix.Tasks.Mailglass.Inbound.{Doctor,Replay,Prune}` shells.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-inbound-runtime-operator-tooling/49-CONTEXT.md
@.planning/phases/49-inbound-runtime-operator-tooling/49-RESEARCH.md
@.planning/phases/49-inbound-runtime-operator-tooling/49-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the live tree 2026-05-25. -->

DOCTOR — clone the CLI shell, change the exit model:
  lib/mix/tasks/mail.doctor.ex
    - run/1 shell (lines 23-35): OptionParser strict + validate_cli!/3 + Mix.Task.run("app.start").
    - validate_cli!/3 (lines 54-81): rejects positional/unknown/bad --format with Mix.raise.
    - render_output/2 (lines 90-95): "json" → render_json; "human" → render_human(verbose?: ...).
    - CRITICAL: mail.doctor Mix.raise's on BOTH CLI misuse AND check failures → collapses to exit 1.
      The inbound doctor MUST keep Mix.raise for CLI misuse only, and for findings call
      exit({:shutdown, N}) with N ∈ {0,1,2} (the three-state exit, D-49-05, Pitfall 6).
  exit({:shutdown, N}) idiom precedent: lib/mix/tasks/mailglass.webhooks.prune.ex:54.

FORMATTER — exact structural clone:
  lib/mailglass/deliverability/formatter.ex
    - render_human/2 (lines 8-22): summary line + per-finding render joined "\n\n".
    - render_finding/2 (lines 46-61): "[status] title" first line + verbose evidence gate
      (verbose? and Map.has_key?(finding, :evidence)).
    - render_json/1 (lines 61-64): Jason.encode!.
  DELTA: D-49-05 finding shape is %{check, status, title, observed, remediation, evidence}
    (no :why_it_matters, no :area). render_json emits ONE object %{summary: %{pass, warn, fail}, findings: [...]}.
    summary_line is "N pass, N warn, N fail" (cannot-diagnose is exit code 2, not a tally column).

ROUTE-CONFLICT — REUSE, never re-implement:
  mailglass_inbound/lib/mailglass_inbound/router/matcher.ex
    - matches_route?/2 (lines 30-35): recipient AND subject AND headers all match.
    - explain/2 (lines 57-76): per-clause verdicts (useful in finding evidence).
    - match primitives (lines 86-89): nil matcher = wildcard/catch-all; %Regex{} via Regex.match?;
      binary == binary. These define "broad" for subsumption.
  mailglass_inbound/lib/mailglass_inbound/router.ex
    - __mailglass_inbound_routes__/0 reflection (lines 64-72) — pass the router module via an opt
      so the doctor reflects a fixture router in tests.
    - route/2 macro (lines 48-62) — ADD source: {__CALLER__.file, __CALLER__.line} to the %Route{}.
  mailglass_inbound/lib/mailglass_inbound/router/route.ex
    - @type (lines 7-12) + defstruct (line 14) — ADD :source {String.t(), pos_integer()} | nil.
  Witness messages are safe to build: %InboundMessage{} defaults from/to/...: [], headers: %{}.

MIME-03 backend report (no bare optional-dep ref):
  lib/mailglass/optional_deps/gen_smtp.ex:57 — Mailglass.OptionalDeps.GenSmtp.available?/0.
  backend_version = Application.spec(:gen_smtp, :vsn) |> (is_list → List.to_string). Neither trips
  NoBareOptionalDepReference. finding status = if available?, do: :pass, else: :warn.

REPLAY — reuse the shipped single-record service unchanged:
  mailglass_inbound/lib/mailglass_inbound/internal/replay.ex
    - replay/2 (lines 13-28): {:ok, map()} | {:error, term()}; appends ExecutionRun source: :replay.
  DELTA (D-49-09): replay/2 STAYS single-record. The task resolves --record-id / --since <iso8601> /
    --tenant <id> (AND-combinable) into an id list via a small PARAMETERIZED selection query
    (from(r in InboundRecord, where: ...)), then iterates replay/2 per id. [y/N] default-No via
    Mix.shell().yes?/1; --yes/-y skips (never removes). Zero matches → exit 0 "nothing to replay".
    --dry-run reports count+scope, no change.

PRUNE — mirror Webhook.Pruner structure, UPGRADE to batched + run-without-Oban:
  lib/mailglass/webhook/pruner.ex
    - :infinity-disable (line 91): prune_status(_status, :infinity) → {:ok, 0} (no DELETE issued).
    - cutoff math + unbounded delete (lines 93-105) — copy the cutoff shape, REPLACE delete with batched.
    - Oban-guard wrapper (line 1 + 117-130): if Code.ensure_loaded?(Oban.Worker) ... else available?/0 → false.
    - telemetry emit shape (lines 107-115).
  lib/mix/tasks/mailglass.webhooks.prune.ex
    - exits-1-when-Oban-absent (lines 44-55) — UPGRADE: inbound runs prune/0 synchronously regardless;
      DROP the available?() gate + else exit({:shutdown,1}) (D-49-28).
  Batched idiom (NEW in repo, D-49-27): DELETE ... WHERE id IN (SELECT id ... WHERE <window>
    LIMIT 1000 lock: "FOR UPDATE SKIP LOCKED"), looped until affected < 1000; the whole sweep
    wrapped in session pg_try_advisory_lock(<stable bigint key>) (bail {:ok, :locked_out} if not
    acquired; pg_advisory_unlock in an after). See RESEARCH §Pattern 6 (lines 413-466).
  Window split (D-49-25): three physical tables —
    mailglass_inbound_replay_runs WHERE source=:replay age>replay_runs_days (30d)
    mailglass_inbound_replay_runs WHERE source=:fresh age>execution_runs_days (90d)
    mailglass_inbound_evidence age>evidence_days (30d)
    mailglass_inbound_records age>records_days (90d)
  Order child-first (D-49-26): replay_runs (both filters) → evidence → records. FKs on_delete: :nothing.
  CRITICAL: filter source via ExecutionRun (execution_run.ex:40 field :source), NEVER ReplayRun
    (replay_run.ex has no :source — compile-time field error). Pitfall 4.

CONFIG (from plan 01): MailglassInbound.Config.retention/0 supplies the windows.
TELEMETRY (from plan 01): MailglassInbound.Telemetry.prune/1 emits [:mailglass_inbound, :prune, :stop]
  with %{records_deleted, evidence_deleted, fresh_runs_deleted, replay_runs_deleted, status}.

OBAN gateway (already exists): mailglass_inbound/lib/mailglass_inbound/optional_deps.ex
  MailglassInbound.OptionalDeps.Oban — available?/0 (line 36), @compile no_warn_undefined (line 30).
Boot-warn pattern (do NOT auto-register cron): lib/mailglass/application.ex:88-111 +
  mailglass_inbound/lib/mailglass_inbound/application.ex:20-42.

BOUNDARY NOTE (orchestrator-resolved): Do NOT add `use Boundary, classify_to:` to the inbound
  mix tasks. mailglass_inbound does NOT run the :boundary compiler; adding it is out of scope for
  this phase. Add a one-line code comment in each task documenting this as a deliberate deviation
  from D-49-04's literal wording (the boundary LAW — inbound depends on core, never the reverse —
  is still honored; this is only the compile-time annotation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Author failing test scaffolds + fixtures for doctor / replay / prune</name>
  <files>mailglass_inbound/test/mix/tasks/mailglass_inbound_doctor_test.exs, mailglass_inbound/test/mailglass_inbound/internal/doctor_test.exs, mailglass_inbound/test/mix/tasks/mailglass_inbound_replay_test.exs, mailglass_inbound/test/mailglass_inbound/internal/prune_test.exs, mailglass_inbound/test/mix/tasks/mailglass_inbound_prune_test.exs</files>
  <read_first>
    - test/mix/tasks/mail_doctor_task_test.exs (run_task!/1 helper lines 182-190; human/JSON parity lines 120-122; catch_exit + capture_io pattern)
    - test/mailglass/webhook/pruner_test.exs (async: false + TRUNCATE CASCADE setup; :infinity → 0 deleted lines 73-80)
    - mailglass_inbound/test/mailglass_inbound/replay_test.exs (ReplayExecution/ReplayRepo process-dict stub pattern)
    - .planning/phases/49-inbound-runtime-operator-tooling/49-RESEARCH.md (§Validation Architecture, per-deliverable details 1, 2, 3 — exact doctor/replay/prune seams; the advisory-lock-under-sandbox caveat + fallback)
    - .planning/phases/49-inbound-runtime-operator-tooling/49-VALIDATION.md (Wave 0 Gaps)
  </read_first>
  <action>
    Build the test fixtures and RED scaffolds. Fixtures: a fake router module defined in the test with (a) a clean route set, (b) a broad-before-narrow pair (catch-all `recipient: nil` before a specific string), (c) a witness-probe shadow pair (exact-string route shadowed by an earlier broader one), (d) a regex-vs-regex pair; a fake mailbox implementing `process/1` and one that does NOT. doctor_test.exs (Internal.Doctor, pure unit, no DB/DNS): assert subsumption → fail, witness-probe shadow → fail, regex-vs-regex → warn, conflict findings include the `router.ex:LINE` string (Route.:source), missing-router → cannot-diagnose, mailbox-without-process → fail, MIME backend reports name + version (or :warn when absent). doctor CLI test (mailglass_inbound_doctor_test.exs): assert all three exit codes explicitly via `catch_exit` (0 all-pass, 1 on fail / any warn under `--strict`, 2 cannot-diagnose), human + JSON parity, bad `--format` → Mix.raise. replay test: seed N records across two tenants + two `--since` windows; assert selector AND-combine resolves the right id set, per-record `replay/2` invoked once each (stub-counted), `[y/N]` defaults No (empty/"n" → NO replay), `--yes` skips the prompt, zero matches → exit 0 "nothing to replay", and (real DB) a replay appends an ExecutionRun with `source: :replay` (count before/after). prune Internal test (async: false, TRUNCATE CASCADE setup): seed ≥1001 over-window records (+ evidence + fresh/replay runs) and assert ≥2 batch iterations + remaining count; advisory-lock single-run (acquire the lock in the test session via `Repo.query!("SELECT pg_try_advisory_lock($1)", [key])`, call `prune/0`, assert `{:ok, :locked_out}`, release — if cross-session locking is intractable under the sandbox, fall back to asserting the lock is attempted via telemetry/log per RESEARCH); child-first order (no FK violation); `:infinity` on a class → 0 deleted with no DELETE issued; `[:mailglass_inbound, :prune, :stop]` carries per-table counts only. prune CLI test: assert the task runs `prune/0` synchronously WITH and WITHOUT Oban (no exit-1-when-absent), `--dry-run` reports scope with no delete, typed confirmation above threshold, `--yes` skips. All scaffolds reference not-yet-existing modules → RED.
  </action>
  <acceptance_criteria>
    - `cd mailglass_inbound && mix test test/mailglass_inbound/internal/doctor_test.exs test/mailglass_inbound/internal/prune_test.exs --seed 0` fails (RED) on undefined `Internal.Doctor` / `Internal.Prune`.
    - The doctor test asserts ALL THREE exit codes (0/1/2) explicitly.
    - The prune test seeds ≥1001 rows to force ≥2 batch iterations and asserts the advisory-lock single-run guard.
    - The replay test asserts `[y/N]`-default-No performs no replay and `--yes` skips the prompt.
  </acceptance_criteria>
  <done>Five test files + fixtures exist encoding IOPS-01/02/03 + MIME-03 behaviors; suite RED on undefined modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Operator.Formatter + Internal.Doctor (DNS-free checks + route-conflict via matcher reuse) + Route :source + doctor task (three-state exit)</name>
  <files>mailglass_inbound/lib/mailglass_inbound/operator/formatter.ex, mailglass_inbound/lib/mailglass_inbound/router/route.ex, mailglass_inbound/lib/mailglass_inbound/router.ex, mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex</files>
  <read_first>
    - lib/mailglass/deliverability/formatter.ex (lines 8-22 render_human; 46-61 render_finding; 61-64 render_json)
    - lib/mix/tasks/mail.doctor.ex (lines 23-35 shell; 54-81 validate_cli!; 90-95 render_output)
    - lib/mailglass/optional_deps/gen_smtp.ex (line 57 available?/0)
    - mailglass_inbound/lib/mailglass_inbound/router/matcher.ex (lines 30-35 matches_route?; 57-76 explain; 86-89 primitives)
    - mailglass_inbound/lib/mailglass_inbound/router.ex (lines 48-62 route/2 macro; 64-72 reflection)
    - mailglass_inbound/lib/mailglass_inbound/router/route.ex (lines 7-14)
    - mailglass_inbound/lib/mailglass_inbound/inbound_message.ex (struct defaults for witness messages)
  </read_first>
  <behavior>
    - Internal.Doctor.run/1 (router module via opt) returns %{summary: %{pass, warn, fail}, findings: [...]} with the locked finding shape.
    - Missing router → cannot-diagnose (drives exit 2); ≥1 route required; each mailbox Code.ensure_compiled! + function_exported?(:process, 1); signing keys read from :mailglass_inbound config (NEVER verify a signature); MIME backend via GenSmtp.available?/0 + Application.spec.
    - Route-conflict: subsumption (broad-before-narrow) → fail; witness-probe shadow → fail; regex-vs-regex → warn; findings name router.ex:LINE via Route.:source.
    - The doctor task exits 0/1/2 per the locked mapping; --strict promotes warn→fail; bad --format → Mix.raise.
  </behavior>
  <action>
    Create `MailglassInbound.Operator.Formatter` cloning `Deliverability.Formatter`: `render_human/2` (summary line "N pass, N warn, N fail" + per-finding render joined "\n\n", keeping the "[status] title" first line + the `verbose? and Map.has_key?(finding, :evidence)` evidence gate) and `render_json/1` emitting ONE object `%{summary: %{pass, warn, fail}, findings: [...]}` via `Jason.encode!/1`. Adjust `render_finding/2` to the D-49-05 finding shape `%{check, status, title, observed, remediation, evidence}` (drop `:why_it_matters`/`:area`). Add `:source` (`{String.t(), pos_integer()} | nil`) to `Router.Route` `@type` + `defstruct`, and in the `route/2` macro set `source: {__CALLER__.file, __CALLER__.line}` on the built `%Route{}` (additive internal reflection). Create `MailglassInbound.Internal.Doctor.run/1` accepting the router module via an opt (defaulting to the configured router) and returning the locked summary+findings; checks are ALL DNS-free: router configured + compiles (absent → a cannot-diagnose marker the task maps to exit 2); ≥1 route; per route `Code.ensure_compiled!/1` + `function_exported?(mailbox, :process, 1)`; provider signing keys present (read the same `:mailglass_inbound` config the plug reads — NEVER verify a signature; finding text must be honest that this checks key PRESENCE only); MIME backend via `Mailglass.OptionalDeps.GenSmtp.available?/0` + `Application.spec(:gen_smtp, :vsn)` (MIME-03, status :pass when available else :warn); route-conflict REUSING `MailglassInbound.Router.Matcher.matches_route?/2` — (a) structural subsumption: an earlier `nil`/broader matcher preceding a narrower one → fail; (b) witness-probe: synthesize an `%InboundMessage{}` from a later route's exact-string matchers (set envelope_recipient/subject/headers; defaults make the rest safe) and run the earlier route's `matches_route?/2`; if it matches, the later route is shadowed → fail; (c) regex-vs-regex overlap → warn ("possible overlap, verify manually"); conflict findings name `router.ex:LINE` using `Route.source` (D-49-08). Create `Mix.Tasks.Mailglass.Inbound.Doctor` cloning `mail.doctor`'s shell (`OptionParser` `strict: [format: :string, strict: :boolean, verbose: :boolean]` + `validate_cli!/3` rejecting positional/unknown/bad-`--format` with `Mix.raise` and the brand-voice prefix "Inbound doctor blocked: ..." + `Mix.Task.run("app.start")`), delegating to `Internal.Doctor.run/1`, formatting via `Operator.Formatter`, and computing the THREE-STATE exit (D-49-05, Pitfall 6): keep `Mix.raise` for CLI misuse ONLY; for findings call `exit({:shutdown, N})` with `0` = all pass (or pass+warn without `--strict`), `1` = ≥1 fail (or any warn under `--strict`), `2` = cannot-diagnose (no router / boot failure). Add the one-line boundary-deviation comment (no `use Boundary` — see Boundary Note).
  </action>
  <acceptance_criteria>
    - `cd mailglass_inbound && mix test test/mailglass_inbound/internal/doctor_test.exs test/mix/tasks/mailglass_inbound_doctor_test.exs --seed 0` GREEN (all three exit codes; conflict fail/warn; MIME report; line numbers).
    - `grep -n "matches_route?" mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex` shows the matcher is REUSED (no re-implemented equality/regex logic).
    - `grep -n "Application.spec(:gen_smtp" mailglass_inbound/lib/mailglass_inbound/internal/doctor.ex` shows the no-bare-ref MIME version report.
    - `grep -n "source:" mailglass_inbound/lib/mailglass_inbound/router.ex` shows __CALLER__ capture in route/2.
    - `grep -c "use Boundary" mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex` returns 0 (boundary deviation honored).
    - `cd /Users/jon/projects/mailglass && mix credo --strict` green for the touched files (no bare optional-dep ref).
  </acceptance_criteria>
  <done>DNS-free doctor with three-state exit, matcher-reuse conflict detection naming line numbers, MIME-03 report with no bare ref, and the shared formatter all ship; no use Boundary on the task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Internal.Prune (batched advisory-locked sweep) + Prune.Worker (Oban-guarded) + replay & prune mix tasks</name>
  <files>mailglass_inbound/lib/mailglass_inbound/internal/prune.ex, mailglass_inbound/lib/mailglass_inbound/prune/worker.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex</files>
  <read_first>
    - lib/mailglass/webhook/pruner.ex (line 1 + 117-130 Oban guard; line 91 :infinity-disable; 93-105 cutoff+delete; 107-115 telemetry)
    - lib/mix/tasks/mailglass.webhooks.prune.ex (lines 44-55 exit-1-when-Oban-absent to UPGRADE)
    - lib/mix/tasks/mail.doctor.ex (shell pattern for the replay task: OptionParser strict + validate_cli! + app.start)
    - mailglass_inbound/lib/mailglass_inbound/internal/replay.ex (lines 13-28 replay/2 single-record)
    - mailglass_inbound/lib/mailglass_inbound/inbound_records/execution_run.ex (line 40 field :source) + replay_run.ex (NO :source)
    - mailglass_inbound/lib/mailglass_inbound/optional_deps.ex (lines 12-71 OptionalDeps.Oban)
    - mailglass_inbound/lib/mailglass_inbound/application.ex (lines 20-42 boot-warn pattern)
    - .planning/phases/49-inbound-runtime-operator-tooling/49-RESEARCH.md (§Pattern 6 lines 413-466 — the Stream.repeatedly batched loop + advisory-lock wrapper)
  </read_first>
  <behavior>
    - Internal.Prune.prune/0 deletes over-window rows in batches of ≤1000 looping until <1000; in-window rows kept.
    - The whole sweep is wrapped in session pg_try_advisory_lock; a concurrent second run returns {:ok, :locked_out} and deletes nothing.
    - Child-first order (replay_runs both filters → evidence → records); a mis-ordered delete fails loudly on the FK.
    - :infinity on a retention class disables that window (0 deleted, no DELETE issued).
    - [:mailglass_inbound, :prune, :stop] carries per-table counts only.
    - The prune mix task runs prune/0 synchronously WITH and WITHOUT Oban (no exit-1-when-absent).
    - The replay task resolves --record-id/--since/--tenant → id list → iterates replay/2; [y/N] default-No unless --yes; 0 matches → exit 0 "nothing to replay".
  </behavior>
  <action>
    Create `MailglassInbound.Internal.Prune.prune/0` (Oban-independent workhorse) reading windows from `MailglassInbound.Config.retention/0`. Mirror `Webhook.Pruner` STRUCTURE: `:infinity` on a class returns 0 without issuing a DELETE (copy the `prune_status(_, :infinity) → {:ok, 0}` shape); cutoff = `DateTime.add(now, -days * 86_400, :second)`. UPGRADE the delete to the batched idiom (D-49-27): per table, `repo.delete_all` of `from(r in schema, where: r.id in subquery(from(s in schema, where: ^window_filter, select: s.id, limit: 1000, lock: "FOR UPDATE SKIP LOCKED")))`, looped (Stream.repeatedly + Enum.reduce_while) until affected `< 1000`. Wrap the WHOLE sweep in a session `pg_try_advisory_lock(@prune_lock_key)` (`@prune_lock_key` a stable bigint constant, e.g. a truncated hash of "mailglass_inbound_prune"); if not acquired return `{:ok, :locked_out}`; `pg_advisory_unlock` in an `after`. Do NOT make the loop one giant transaction. Window split (D-49-25): three physical tables, four windows — `mailglass_inbound_replay_runs` source=:replay age>replay_runs_days; `mailglass_inbound_replay_runs` source=:fresh age>execution_runs_days; `mailglass_inbound_evidence` age>evidence_days; `mailglass_inbound_records` age>records_days. Filter `source` via `ExecutionRun` (it maps `:source`) or a schemaless query — NEVER `ReplayRun` (no `:source` field — compile error). Delete child-first (D-49-26): replay_runs (both source filters) → evidence → records; FKs are `on_delete: :nothing` so a mis-ordered delete fails loudly (the safety net — do NOT switch to CASCADE). Emit `MailglassInbound.Telemetry.prune/1` with `%{records_deleted, evidence_deleted, fresh_runs_deleted, replay_runs_deleted, status}` (counts only). Create `MailglassInbound.Prune.Worker` behind a top-of-file `if Code.ensure_loaded?(Oban.Worker)` guard: in the true branch `use Oban.Worker, queue: :mailglass_inbound_maintenance`, `available?/0 → true`, `perform/1` calls `Internal.Prune.prune/0`; in the `else` branch a stub with `available?/0 → false`. Prefer the `MailglassInbound.OptionalDeps.Oban` gateway where possible; the file-top `Code.ensure_loaded?` is the established exception. NEVER auto-register the cron (D-49-28) — mirror the consolidated `:persistent_term`-gated boot warning directing operators to `mix mailglass.inbound.prune`; document `0 3 * * *` for Phase 50 (do not register it). Create `Mix.Tasks.Mailglass.Inbound.Replay` (shell cloned from `mail.doctor`): resolve `--record-id` / `--since <iso8601>` (parse via `DateTime`/`NaiveDateTime`) / `--tenant <id>` (AND-combinable) into an id list via a small PARAMETERIZED `from(r in InboundRecord, where: ...)` query (never interpolate selectors), iterate `MailglassInbound.Internal.Replay.replay/2` per id; `[y/N]` default-No via `Mix.shell().yes?/1`; `--yes`/`-y` skips the prompt (never removes it); `--dry-run` reports count + scope with no change; zero matches → `exit 0` with "nothing to replay." Create `Mix.Tasks.Mailglass.Inbound.Prune` cloning `mailglass.webhooks.prune`'s shell but UPGRADING the Oban gate: run `Internal.Prune.prune/0` SYNCHRONOUSLY whether or not Oban is present (DROP the `available?()` gate + the `else exit({:shutdown, 1})` branch — only scheduling needs Oban). Destructive-confirmation tier (D-49-10): `--dry-run` + a typed confirmation above a row-count threshold (threshold is planner discretion) via `Mix.shell().prompt/1` + `--yes`/`-y` for cron/CI. Add the one-line boundary-deviation comment (no `use Boundary`) to both tasks.
  </action>
  <acceptance_criteria>
    - `cd mailglass_inbound && mix test test/mailglass_inbound/internal/prune_test.exs test/mix/tasks/mailglass_inbound_replay_test.exs test/mix/tasks/mailglass_inbound_prune_test.exs --seed 0` GREEN.
    - `grep -n "pg_try_advisory_lock" mailglass_inbound/lib/mailglass_inbound/internal/prune.ex` shows the single-run guard.
    - `grep -n "FOR UPDATE SKIP LOCKED" mailglass_inbound/lib/mailglass_inbound/internal/prune.ex` shows the batched lock idiom.
    - `grep -n "ReplayRun" mailglass_inbound/lib/mailglass_inbound/internal/prune.ex` shows `source` is NOT filtered via ReplayRun (filtered via ExecutionRun / schemaless).
    - `grep -c "available?()" mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex` returns 0 (no exit-1-when-Oban-absent gate; runs synchronously).
    - `grep -c "use Boundary" mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex` returns 0.
    - `cd /Users/jon/projects/mailglass && mix credo --strict` green for touched files (no bare Oban ref outside the gateway/guard).
  </acceptance_criteria>
  <done>Batched advisory-locked prune (with :infinity disable + child-first order + per-table telemetry), Oban-guarded worker (never auto-registered), and the replay + prune CLI tasks (replay [y/N] tier, prune destructive tier, prune sync-without-Oban) all ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args → mix tasks | --tenant/--record-id/--since are untrusted input; must be parameterized |
| concurrent prune runs (cron + manual) → DB | advisory lock serializes; double-delete prevented |
| doctor finding text → operator decision | must be honest about what it does/doesn't verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-11 | Tampering | SQL injection via CLI selectors (--tenant/--record-id/--since) | mitigate | All selection queries are parameterized Ecto (from/where with pinned ^vars); --since parsed via DateTime/NaiveDateTime; never string-interpolate selectors. |
| T-49-12 | Tampering / DoS | concurrent prune double-delete or lock contention | mitigate | pg_try_advisory_lock single-run guard — a second concurrent sweep returns {:ok, :locked_out} and deletes nothing (D-49-27). Test asserts the locked-out path. |
| T-49-13 | Spoofing | doctor "passed" misleads operator into thinking signatures are valid | mitigate | Doctor checks key PRESENCE only and NEVER verifies a signature (D-49-06); finding text is explicit about this. |
| T-49-14 | DoS | unbounded retention delete → replication-lag spike / long locks | mitigate | Batched LIMIT 1000 + FOR UPDATE SKIP LOCKED, looped; the sweep is NOT one giant transaction (D-49-27). |
| T-49-15 | Information Disclosure | prune telemetry leaking PII | mitigate | [:mailglass_inbound, :prune, :stop] carries per-table COUNTS + status only (D-49-29); validate by running credo. |
| T-49-16 | (Data loss) | accidental over-broad prune from a config typo | mitigate | NimbleOptions config validation (plan 01); typed-confirmation above a row-count threshold + --dry-run before destructive prune (D-49-10). |
| T-49-17 | Information Disclosure / Elevation | cross-tenant replay/prune | mitigate | Replay selection scoped by --tenant when provided; replay/2 loads tenant-scoped record+evidence; prune windows are global retention (not tenant-targeted) by design. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/internal/doctor_test.exs test/mailglass_inbound/internal/prune_test.exs test/mix/tasks/mailglass_inbound_doctor_test.exs test/mix/tasks/mailglass_inbound_replay_test.exs test/mix/tasks/mailglass_inbound_prune_test.exs --seed 0` green.
- `cd /Users/jon/projects/mailglass && mix credo --strict` green (no bare optional-dep ref; no PII in prune telemetry — RUN credo).
- `cd mailglass_inbound && mix compile --no-optional-deps --warnings-as-errors` green (Oban guarded; gen_smtp via gateway). Do NOT run `--no-optional-deps --force` on the shared `_build` (project memory).
- `mix.lock` unchanged.
</verification>

<success_criteria>
- `mix mailglass.inbound.doctor` is DNS-free with three-state exit + human/JSON output + MIME-03 report (IOPS-01, MIME-03).
- `mix mailglass.inbound.replay` resolves selectors → iterates single-record replay/2 with [y/N]-default-No unless --yes; appends source: :replay (IOPS-02).
- `mix mailglass.inbound.prune` enforces retention with batched LIMIT-1000 + advisory-lock single-run, :infinity-disable, child-first order, runs synchronously with or without Oban (IOPS-03).
- No `use Boundary` on the inbound tasks (orchestrator-resolved deviation from D-49-04 literal wording, boundary law still honored).
</success_criteria>

<output>
Create `.planning/phases/49-inbound-runtime-operator-tooling/49-03-SUMMARY.md` when done.
</output>
