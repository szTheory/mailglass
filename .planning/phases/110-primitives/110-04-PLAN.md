---
phase: 110-primitives
plan: "04"
type: execute
wave: 4
depends_on:
  - "110-03"
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements:
  - PRIM-01
  - PRIM-02
  - PRIM-03
  - PRIM-04
  - PRIM-05
  - PRIM-06
  - PRIM-07
must_haves:
  truths:
    - "D-02: `PRIMITIVE-DRIFT-GATE` fails on private shell/gallery primitive copies and old binary theme-toggle specimens."
    - "D-09: `STATCARD-GATE` fails on page-local stat helpers or overview stat/KPI markup that bypasses `Components.stat_card/1`."
    - "D-10: `ICON-EXISTS-GATE` compares every admin `hero-*` usage against `mailglass_admin/assets/vendor/heroicons-inline.js`."
    - "D-12 and D-13: normal primitive pointer targets are proven at least 44px by 44px in the compiled bundle."
    - "WCAG 2.2 AA text contrast, non-text contrast, focus appearance, and focus-not-obscured proof covers the new primitive states across light, dark, and system specimens."
    - "Playwright structural assertions prove gallery public primitive states, theme-picker radio semantics, disabled distinction, system wrapper absence of explicit theme, and stat-card no-wrap/overflow behavior at 320px."
  prohibitions:
    - statement: "No `@axe-core/playwright`, screenshot assertion, pixel-diff runner, or package install is added in Phase 110."
      status: active
      verification: "Final package/source scan."
    - statement: "No browser storage, match-media script, theme hook, cookie persistence, first-paint script, tenant listing, tenant switching, or tenant auto-select behavior is added."
      status: active
      verification: "Final source-scope scan."
    - statement: "No explicit system theme attribute appears in primitive or gallery output."
      status: active
      verification: "Structural system wrapper proof and source scan."
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "PRIMITIVE-DRIFT-GATE, STATCARD-GATE, ICON-EXISTS-GATE"
      contains: "ICON-EXISTS-GATE"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Compiled-bundle primitive target-size, theme-picker, gallery, icon-meaning, disabled, and stat-card layout proof"
    - path: "mailglass_admin/assets/vendor/heroicons-inline.js"
      provides: "Vendored icon inventory source of truth"
  key_links:
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/lib/mailglass_admin"
      via: "BASH_SOURCE-anchored LIB scan"
      pattern: "PRIMITIVE-DRIFT-GATE"
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/assets/vendor/heroicons-inline.js"
      via: "grep/sed/comm inventory comparison"
      pattern: "ICON-EXISTS-GATE"
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/gallery_live.ex"
      via: "gallery data-testid selectors"
      pattern: "gallery-(nav_link|nav_pill|tenant_chip|theme_picker|stat_card)"
---

<objective>
Add fail-closed gates and browser structural proof for the primitive layer.

Purpose: Phase 110 is only complete when copy drift, non-canonical stat cards, missing icons, weak target sizes, invalid theme-picker semantics, disabled ambiguity, and stat-card overflow cannot silently return.
Output: Extended conformance gates and Playwright structural assertions proving the primitive layer in the compiled bundle.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-primitives/110-CONTEXT.md
@.planning/phases/110-primitives/110-RESEARCH.md
@.planning/phases/110-primitives/110-VALIDATION.md
@.planning/phases/110-primitives/110-UI-SPEC.md
@.planning/phases/109-foundations-gate-tightening/109-03-SUMMARY.md
@.planning/phases/109-foundations-gate-tightening/109-04-SUMMARY.md
@.planning/phases/110-primitives/110-01-SUMMARY.md
@.planning/phases/110-primitives/110-02-SUMMARY.md
@.planning/phases/110-primitives/110-03-SUMMARY.md
@mailglass_admin/scripts/check-conformance.sh
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
@mailglass_admin/lib/mailglass_admin/operator_live.ex
@mailglass_admin/lib/mailglass_admin/inbound/overview.ex
@mailglass_admin/assets/vendor/heroicons-inline.js
@mailglass_admin/package.json
</context>

## Multi-Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
ROADMAP | goal | Shared atoms are single public components; states/themes/viewports meet WCAG/APG; `stat_card` and tri-state picker exist | 110-01..110-04 | COVERED | This plan closes the gates/proof.
ROADMAP | SC-1 | Gallery-inlined atoms promoted and copy-drift gate passes | 110-01, 110-02, 110-03, 110-04 | COVERED | PRIMITIVE-DRIFT-GATE.
ROADMAP | SC-2 | Primitive states in light/dark/system meet WCAG 2.2 AA and APG | 110-01, 110-03, 110-04 | COVERED | Browser structural matrix.
ROADMAP | SC-3 | Canonical `stat_card` shape enforced | 110-01, 110-02, 110-04 | COVERED | STATCARD-GATE and overflow proof.
ROADMAP | SC-4 | Three-way theme picker and compiled-bundle 44x44 proof | 110-01, 110-02, 110-03, 110-04 | COVERED | Radio and target-size proof.
ROADMAP | SC-5 | Every icon exists and is not sole meaning | 110-01, 110-04 | COVERED | ICON-EXISTS-GATE and structural label/access-name proof.
REQUIREMENTS | PRIM-01 | Promote gallery atoms to public components with no copy drift | 110-01, 110-02, 110-03, 110-04 | COVERED | Gate enforces final source state.
REQUIREMENTS | PRIM-02 | Render every primitive interaction state in light/dark/system at 320->wide | 110-01, 110-03, 110-04 | COVERED | Browser tests measure gallery output.
REQUIREMENTS | PRIM-03 | Disabled controls are visually and programmatically distinct | 110-01, 110-03, 110-04 | COVERED | Disabled structural assertions.
REQUIREMENTS | PRIM-04 | Canonical `stat_card` primitive | 110-01, 110-02, 110-04 | COVERED | STATCARD gate and layout proof.
REQUIREMENTS | PRIM-05 | Three-way system/light/dark theme picker | 110-01, 110-02, 110-03, 110-04 | COVERED | Radio semantics and no explicit system theme.
REQUIREMENTS | PRIM-06 | 44x44 default target proof; 24px exceptions gated or recorded | 110-04 | COVERED | No dense exceptions expected; normal primitives must pass 44px proof.
REQUIREMENTS | PRIM-07 | Icon-exists guard and non-icon-alone meaning | 110-01, 110-04 | COVERED | Gate plus structural meaning checks.
RESEARCH | R-01 | Add deterministic copy-drift, STATCARD, and icon gates | 110-04 | COVERED | `check-conformance.sh`.
RESEARCH | R-02 | Use structural/computed-style tests, not screenshots | 110-04 | COVERED | Playwright assertions only.
RESEARCH | R-03 | Prove target size in compiled bundle | 110-04 | COVERED | Browser test after asset build.
RESEARCH | R-04 | Prove stat-card layout beyond grep | 110-04 | COVERED | 320px overflow/no-wrap checks.
RESEARCH | R-05 | Keep package/dependency surface unchanged | 110-04 | COVERED | Final package diff.
CONTEXT | D-01 | Public primitive ownership | 110-01, 110-02, 110-03, 110-04 | COVERED | Gate checks final source.
CONTEXT | D-02 | Copy-drift guard | 110-04 | COVERED | PRIMITIVE-DRIFT-GATE.
CONTEXT | D-03 | Gallery public component state coverage | 110-03, 110-04 | COVERED | Structural gallery proof.
CONTEXT | D-04 | Theme choices and system absence of explicit value | 110-01, 110-02, 110-03, 110-04 | COVERED | Source and browser assertions.
CONTEXT | D-05 | Radio group, not independent pressed buttons | 110-01, 110-04 | COVERED | APG structural assertions.
CONTEXT | D-06 | No persistence/no-FOUC/root ownership | 110-02, 110-04 | COVERED | Final scope scan.
CONTEXT | D-07 | Add and migrate `stat_card` | 110-01, 110-02, 110-04 | COVERED | Gate verifies final source.
CONTEXT | D-08 | `stat_card` locked shape | 110-01, 110-02, 110-04 | COVERED | Gate and browser proof.
CONTEXT | D-09 | STATCARD gate | 110-04 | COVERED | This plan.
CONTEXT | D-10 | Icon-exists guard | 110-04 | COVERED | This plan.
CONTEXT | D-11 | Icons never carry meaning alone | 110-01, 110-04 | COVERED | Structural checks.
CONTEXT | D-12 | WCAG 24px minimum gate/exception | 110-04 | COVERED | 44px proof exceeds minimum; no dense exceptions expected.
CONTEXT | D-13 | 44x44 default target floor | 110-01, 110-04 | COVERED | Compiled-bundle proof.
UI-SPEC | UI-01 | Gate contracts | 110-04 | COVERED | PRIMITIVE/STATCARD/ICON gates.
UI-SPEC | UI-02 | Target-size proof | 110-04 | COVERED | Browser measurements.
UI-SPEC | UI-03 | Theme picker contract | 110-04 | COVERED | Radio and checked-state assertions.
UI-SPEC | UI-04 | Stat-card contract | 110-04 | COVERED | Overflow/no-wrap and severity assertions.
UI-SPEC | UI-05 | Icon contract | 110-04 | COVERED | Inventory and meaning checks.
UI-SPEC | UI-06 | Scope contract | 110-04 | COVERED | Final no-scope-creep scans.
VALIDATION | V-01 | PRIMITIVE-DRIFT gate | 110-04 | COVERED | `check-conformance.sh`.
VALIDATION | V-02 | STATCARD gate | 110-04 | COVERED | `check-conformance.sh`.
VALIDATION | V-03 | ICON-EXISTS gate | 110-04 | COVERED | `check-conformance.sh`.
VALIDATION | V-04 | Target-size, disabled, system, focus, contrast, and overflow structural checks | 110-04 | COVERED | `structural.spec.js`.
VALIDATION | V-05 | Full phase verification battery | 110-04 | COVERED | Final commands.

<tasks>

<task id="110-04-01" type="auto" files="mailglass_admin/scripts/check-conformance.sh">
  <name>Task 1: Add primitive drift, stat-card, and icon-exists gates</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    mailglass_admin/scripts/check-conformance.sh
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/lib/mailglass_admin/gallery_live.ex
    mailglass_admin/lib/mailglass_admin/operator_live.ex
    mailglass_admin/lib/mailglass_admin/inbound/overview.ex
    mailglass_admin/assets/vendor/heroicons-inline.js
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-RESEARCH.md
    .planning/phases/110-primitives/110-VALIDATION.md
    .planning/phases/110-primitives/110-UI-SPEC.md
  </read_first>
  <action>Add three fail-closed blocks to the existing `mailglass_admin/scripts/check-conformance.sh`, preserving `set -euo pipefail`, `SCRIPT_DIR`, `LIB`, the shared `errors` counter, cwd-independent behavior, and the final exit block. Add `PRIMITIVE-DRIFT-GATE` per D-02: fail if admin `lib/**/*.ex` contains private definitions for the named primitives outside `MailglassAdmin.Components`, fail if the old `theme_toggle` gallery identity remains, require public definitions for `nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, and `stat_card` in `components.ex`, and require the `GalleryLive` render dispatchers for each named primitive to call the matching `Components.*` function directly. The drift gate must also fail if a `GalleryLive` dispatcher for `nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, or `stat_card` recreates inline HEEx bodies instead of dispatching to `Components.*`, or if old primitive class signatures from the prior shell/gallery copies appear outside `MailglassAdmin.Components`. Scope these pattern checks so the single public implementation in `components.ex` is allowed and consumer calls are allowed. Add `STATCARD-GATE` per D-09: require `Components.stat_card` usage in both `operator_live.ex` and `inbound/overview.ex`, fail on private `defp stat` helpers in admin lib, and fail on the known old stat-card raw shape patterns that allowed wrapped display values in operator/inbound overview source. Add `ICON-EXISTS-GATE` per D-10: collect every `hero-*` name in admin `lib/**/*.ex`, strip the `hero-` prefix, compare to keys in `mailglass_admin/assets/vendor/heroicons-inline.js`, and fail with the missing icon names if any used icon is absent. Implement the icon comparison with existing shell tools available to this script, not a package install. Run the script from repo root and from `mailglass_admin/` to prove cwd-independent behavior.</action>
  <acceptance_criteria>
    - `check-conformance.sh` contains failure labels `PRIMITIVE-DRIFT-GATE`, `STATCARD-GATE`, and `ICON-EXISTS-GATE`.
    - Existing gates and final `errors` handling remain present and unchanged in behavior.
    - The primitive drift gate fails on private `defp nav_link`, `defp nav_pill`, `defp tenant_chip`, private old binary theme-toggle definitions, or the old gallery theme-toggle identity.
    - The primitive drift gate requires the `GalleryLive` dispatchers for `nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, and `stat_card` to call `Components.nav_link`, `Components.nav_pill`, `Components.tenant_chip`, `Components.theme_picker`, and `Components.stat_card` respectively.
    - The primitive drift gate fails if named primitive render bodies or old primitive class signatures are reintroduced outside `MailglassAdmin.Components`, while allowing the single public implementation and legitimate `Components.*` consumer calls.
    - The stat-card gate requires `Components.stat_card` in `operator_live.ex` and `inbound/overview.ex`, and fails on private `defp stat` helpers.
    - The icon gate compares used admin `hero-*` names against `heroicons-inline.js` keys and prints missing names before incrementing `errors`.
    - `bash mailglass_admin/scripts/check-conformance.sh` and `cd mailglass_admin && bash scripts/check-conformance.sh` both exit 0 after Plans 110-01 through 110-03.
  </acceptance_criteria>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh</automated>
    <automated>rg -n 'PRIMITIVE-DRIFT-GATE|STATCARD-GATE|ICON-EXISTS-GATE|heroicons-inline\\.js|Components\\.stat_card' mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>The hard conformance lane prevents primitive copy drift, non-canonical stat cards, and missing vendored icons from re-entering the admin source tree.</done>
</task>

<task id="110-04-02" type="auto" files="mailglass_admin/e2e/structural.spec.js">
  <name>Task 2: Add compiled-bundle structural proof for primitive states</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    mailglass_admin/e2e/structural.spec.js
    mailglass_admin/lib/mailglass_admin/gallery_live.ex
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/scripts/check-conformance.sh
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-VALIDATION.md
    .planning/phases/110-primitives/110-UI-SPEC.md
  </read_first>
  <action>Extend `mailglass_admin/e2e/structural.spec.js` inside the existing gallery/structural test style. Add tests that open `/dev/mail/gallery` and assert the expanded primitive cells exist for `nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, and `stat_card`, including hover-ready, focus-visible, disabled, loading where applicable, explicit loading non-applicability where not applicable, long-content, empty/unavailable, and severity states per D-03. Assert each primitive cell has light and dark wrappers plus a system wrapper with no explicit theme attribute per D-04. For `theme_picker`, assert exactly three radios with labels `System`, `Light`, and `Dark`, the selected specimen has the correct checked radio, and no picker option uses pressed-button semantics per D-05. For hover/focus coverage, drive hover on interactive primitive specimens and use keyboard or locator focus to assert the focus-visible ring appears in light, dark, and system wrappers. The focus proof must inspect computed styles rather than screenshots: visible outline/ring geometry, at least 2px focus indicator thickness or equivalent Phase 109 `mg-focus-ring` token output, at least 3:1 contrast against adjacent colors, and a bounding-box/intersection check proving focused controls are not obscured. For disabled primitives, assert the rendered disabled state has programmatic disabled evidence (`disabled` or `aria-disabled`) and a distinct computed style marker such as reduced opacity, disabled cursor, or disabled pointer behavior. Add computed-style contrast helpers for WCAG 2.2 AA: text contrast for primitive labels/values/copy across light, dark, and system specimens; non-text contrast for selected cues, borders that identify controls, meaningful icons, disabled markers, and focus indicators. Text contrast assertions must use the applicable AA threshold for the rendered text size/weight; non-text assertions must require at least 3:1 contrast for UI indicators that carry state or affordance. For PRIM-06 per D-12 and D-13, use the existing `assertTouchTarget` helper to measure normal primitive pointer targets in the compiled bundle: `nav_link`, `nav_pill`, each `theme_picker` option, and any icon-only primitive control must be at least 44px by 44px. For `stat_card`, set viewport width to 320px and assert long labels have a `title`, values have computed no-wrap behavior, numeric values use tabular numeric treatment when available from computed styles, the loading state exposes visible loading copy or `aria-busy`, and the document does not gain horizontal overflow from long stat content. For PRIM-07 per D-11, assert meaningful icon states also expose visible adjacent text or an accessible name; decorative icons remain acceptable only when text carries the meaning.</action>
  <acceptance_criteria>
    - `structural.spec.js` has Playwright tests covering gallery cells for `gallery-nav_link-*`, `gallery-nav_pill-*`, `gallery-tenant_chip-*`, `gallery-theme_picker-*`, and `gallery-stat_card-*`.
    - Tests assert light, dark, and system wrappers for primitive cells, and the system wrapper has no explicit theme attribute.
    - Tests assert hover-ready and focus-visible behavior for interactive primitive specimens in light, dark, and system wrappers.
    - Tests assert loading coverage for `stat_card` and explicit loading non-applicability for `nav_link`, `nav_pill`, `tenant_chip`, and `theme_picker`.
    - Tests assert `theme_picker` radio semantics, three visible labels, checked state, and absence of pressed-button semantics.
    - Tests assert disabled primitive state is programmatically disabled and visually distinct.
    - Tests assert normal primitive pointer targets are at least 44px by 44px using rendered bounding boxes from the compiled bundle.
    - Tests assert WCAG AA text contrast for primitive text and 3:1 non-text contrast for selected indicators, meaningful icons, disabled indicators, and focus indicators across light, dark, and system.
    - Tests assert focus appearance uses the shared Phase 109 focus-ring contract and focused primitive controls are not obscured.
    - Tests assert stat-card long-label and long-value behavior at 320px: title exists, value does not wrap, and page horizontal overflow does not increase.
    - Tests assert meaningful icon states have visible adjacent text or accessible names.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix mailglass_admin.assets.build</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/</automated>
    <automated>rg -n 'contrast|focus.*obscured|focus.*ring|hover-ready|loading' mailglass_admin/e2e/structural.spec.js</automated>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "gallery|touch targets|theme_picker|stat_card"</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>Browser structural tests prove the Phase 110 primitive contracts against rendered, compiled CSS output across the gallery surface.</done>
</task>

<task id="110-04-03" type="auto" files="none">
  <name>Task 3: Run final Phase 110 proof and source-scope audit</name>
  <files>none</files>
  <read_first>
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-VALIDATION.md
    .planning/phases/110-primitives/110-UI-SPEC.md
    mailglass_admin/scripts/check-conformance.sh
    mailglass_admin/e2e/structural.spec.js
    mailglass_admin/package.json
    mailglass_admin/package-lock.json
    mailglass_admin/mix.exs
  </read_first>
  <action>Run the final Phase 110 verification battery without modifying implementation code. Record command outputs in the plan summary. Verify the conformance script, focused component and shell tests, support-contract suite, preview suite, full operator browser suite, static bundle cleanliness, package manifest cleanliness, and Phase 110 scope scans. The scope scan must prove Phase 110 did not add browser storage, match-media scripts, theme hooks, first-paint theme scripts, screenshot/pixel-diff assertions, axe package usage, tenant listing/switching/auto-select behavior, recipient-facing template changes, or `brandbook/` changes. If a normal primitive target fails the 44px proof, fix the primitive to meet 44px rather than weakening the default; Phase 110 expects zero dense-control exceptions.</action>
  <acceptance_criteria>
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0.
    - Focused component and shell tests exit 0.
    - `mix verify.support_contract.admin` exits 0.
    - `mix verify.preview` exits 0 and leaves `priv/static/` clean.
    - `npm run test:operator-browser` exits 0.
    - Package manifests have no Phase 110 diff.
    - Source scans find no scope creep into theme persistence/no-FOUC/root behavior, tenant listing/switching/auto-select, screenshot/pixel-diff proof, axe dependency, recipient-facing templates, or `brandbook/`.
  </acceptance_criteria>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs test/mailglass_admin/operator/shell_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin && mix verify.support_contract.admin</automated>
    <automated>cd mailglass_admin && mix verify.preview</automated>
    <automated>cd mailglass_admin && npm run test:operator-browser</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/ package.json package-lock.json</automated>
    <automated>! rg -n '@axe-core/playwright|toHaveScreenshot|screenshot|localStorage|sessionStorage|matchMedia|phx-hook=.*theme|theme hook|tenant listing|auto-select|brandbook/' mailglass_admin/lib mailglass_admin/e2e mailglass_admin/package.json mailglass_admin/package-lock.json</automated>
  </verify>
  <done>All Phase 110 automated proof passes and the final source-scope audit confirms primitives work did not expand into deferred or out-of-scope areas.</done>
</task>

</tasks>

## Artifacts this phase produces

- Public component `MailglassAdmin.Components.nav_link/1`.
- Public component `MailglassAdmin.Components.nav_pill/1`.
- Public component `MailglassAdmin.Components.tenant_chip/1`.
- Public component `MailglassAdmin.Components.theme_picker/1`.
- Public component `MailglassAdmin.Components.stat_card/1`.
- Component contract tests in `mailglass_admin/test/mailglass_admin/components_test.exs`.
- Shell consumers of `Components.nav_link/1`, `Components.nav_pill/1`, `Components.tenant_chip/1`, and `Components.theme_picker/1`.
- Public shell helpers `MailglassAdmin.Operator.Shell.theme_choice/1` and `MailglassAdmin.Operator.Shell.set_theme_path/2`.
- LiveView event `set_theme` in `MailglassAdmin.OperatorLive` and `MailglassAdmin.InboundLive`.
- Operator and inbound overview consumers of `Components.stat_card/1`.
- Gallery specimens for public primitives and states under `gallery-nav_link-*`, `gallery-nav_pill-*`, `gallery-tenant_chip-*`, `gallery-theme_picker-*`, and `gallery-stat_card-*`.
- Light wrapper with `data-theme="mailglass-light"`, dark wrapper with `data-theme="mailglass-dark"`, and system wrapper with no explicit theme attribute for gallery specimen cells.
- `PRIMITIVE-DRIFT-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `STATCARD-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `ICON-EXISTS-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- Playwright structural assertions for primitive gallery state coverage, system wrapper absence of explicit theme, radio semantics, disabled distinction, 44px target sizes, icon meaning, and stat-card overflow/no-wrap behavior.
- Rebuilt `mailglass_admin/priv/static/app.css` when source class strings change.
- `.planning/phases/110-primitives/110-01-SUMMARY.md`.
- `.planning/phases/110-primitives/110-02-SUMMARY.md`.
- `.planning/phases/110-primitives/110-03-SUMMARY.md`.
- `.planning/phases/110-primitives/110-04-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source tree -> conformance gates | Regex/shell gates must scan the intended files and fail closed. |
| vendored icon map -> rendered admin icons | New icon class names must correspond to embedded SVGs. |
| compiled CSS bundle -> browser target-size proof | Source classes are only sufficient when compiled CSS renders the required dimensions. |
| primitive gallery -> phase verification | Gallery assertions must target real public components and not copied HEEx. |

## STRIDE Threat Register

Execution must stop on any HIGH or CRITICAL threat whose status is `open`. The executor must either complete the mitigation and rerun the listed verification, or create a blocking checkpoint that records why the threat cannot be closed.

| Threat ID | STRIDE | ASVS L1 Mapping | Severity | Status | Component | Mitigation / Verification |
|---|---|---|---|---|---|---|
| T-110-13 | Tampering | V14 Configuration | HIGH | planned | `check-conformance.sh` scoping | Preserve BASH_SOURCE anchoring and run from repo root plus package root. Verification: both script invocations. |
| T-110-14 | Tampering | V14 Configuration | HIGH | planned | copy-drift gate | Fail on private primitive definitions and stale gallery theme-toggle identity. Verification: PRIMITIVE-DRIFT-GATE. |
| T-110-15 | Tampering | V5 Input Validation | HIGH | planned | icon inventory | Compare used `hero-*` names against the vendored map. Verification: ICON-EXISTS-GATE. |
| T-110-16 | Spoofing | V5 Input Validation | HIGH | planned | disabled controls | Browser checks require programmatic disabled state and visual distinction. Verification: structural disabled tests. |
| T-110-17 | Denial of Service | V5 Input Validation | MEDIUM | planned | long stat content | 320px structural proof checks no horizontal overflow and no value wrapping. Verification: stat-card Playwright tests. |
| T-110-18 | Information Disclosure | V4 Access Control | HIGH | planned | scope creep into tenant listing/switching | Final source-scope scan rejects tenant listing or auto-select work in Phase 110. Verification: Task 3 scope scan. |
| T-110-SC | Tampering | V14 Configuration | LOW | accepted | npm/pip/cargo installs | No package installs; `@axe-core/playwright` remains out of Phase 110. Verification: package diff and source scan. |
</threat_model>

<verification>
Overall Phase 110 verification:
- `bash mailglass_admin/scripts/check-conformance.sh`
- `cd mailglass_admin && bash scripts/check-conformance.sh`
- `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs test/mailglass_admin/operator/shell_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix verify.support_contract.admin`
- `cd mailglass_admin && mix verify.preview`
- `cd mailglass_admin && npm run test:operator-browser`
- `cd mailglass_admin && git diff --exit-code priv/static/ package.json package-lock.json`
- `! rg -n '@axe-core/playwright|toHaveScreenshot|screenshot|localStorage|sessionStorage|matchMedia|phx-hook=.*theme|theme hook|tenant listing|auto-select|brandbook/' mailglass_admin/lib mailglass_admin/e2e mailglass_admin/package.json mailglass_admin/package-lock.json`
</verification>

<success_criteria>
Plan 110-04 is complete when the conformance script fails closed for primitive drift, non-canonical stat cards, and missing embedded icons; the browser suite proves primitive semantics, states, target sizes, disabled distinction, and stat-card layout against the compiled bundle; and the final Phase 110 verification battery passes with no package or scope-creep diff.
</success_criteria>

<output>
Create `.planning/phases/110-primitives/110-04-SUMMARY.md` when done.
</output>
