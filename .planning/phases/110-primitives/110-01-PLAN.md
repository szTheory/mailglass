---
phase: 110-primitives
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/test/mailglass_admin/components_test.exs
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - PRIM-01
  - PRIM-02
  - PRIM-03
  - PRIM-04
  - PRIM-05
  - PRIM-07
must_haves:
  truths:
    - "D-01: `MailglassAdmin.Components` exports `nav_link/1`, `nav_pill/1`, `tenant_chip/1`, `theme_picker/1`, and `stat_card/1` as the single public primitive surface."
    - "D-04 and D-05: `theme_picker/1` models exactly `:system`, `:light`, and `:dark` as one radio-group selection."
    - "D-08: `stat_card/1` renders label, no-wrap tabular value, and severity as icon plus visible label plus semantic color."
    - "D-11: meaningful icon usage has adjacent text or an accessible name, never icon-only meaning."
    - "Component tests assert enabled, disabled, selected/current, hover-ready, focus-visible, long-content, empty, unavailable, loading where applicable, explicit loading non-applicability where not applicable, and severity states for the new primitives."
  prohibitions:
    - statement: "No browser storage, match-media script, cookie persistence, first-paint theme script, or root theme resolution is introduced in Phase 110."
      status: active
      verification: "Plan 110-04 source-scope scan."
    - statement: "No screenshot or pixel-diff proof is added; Phase 110 remains structural and computed-style based."
      status: active
      verification: "Plan 110-04 package/source scan."
    - statement: "No recipient-facing email template or brandbook token change is part of Phase 110."
      status: active
      verification: "Final diff excludes recipient templates and `brandbook/`."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "Public primitive component API"
      exports: ["nav_link", "nav_pill", "tenant_chip", "theme_picker", "stat_card"]
    - path: "mailglass_admin/test/mailglass_admin/components_test.exs"
      provides: "Component contract tests for public primitive markup and states"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Rebuilt committed CSS bundle for new primitive class strings"
  key_links:
    - from: "mailglass_admin/test/mailglass_admin/components_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "Phoenix.LiveViewTest.render_component/2"
      pattern: "Components\\.(nav_link|nav_pill|tenant_chip|theme_picker|stat_card)"
    - from: "mailglass_admin/lib/mailglass_admin/components.ex"
      to: "mailglass_admin/assets/vendor/heroicons-inline.js"
      via: "hero-* class names compiled by vendored icon plugin"
      pattern: "hero-[a-z0-9-]+"
---

<objective>
Create the public primitive API that every later Phase 110 plan consumes.

Purpose: The shell, gallery, stat-card migrations, drift gates, and browser proof cannot be reliable until the named atoms live in `MailglassAdmin.Components` with explicit, tested contracts.
Output: Public component functions for `nav_link`, `nav_pill`, `tenant_chip`, `theme_picker`, and `stat_card`, plus focused component tests and a rebuilt CSS bundle.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-primitives/110-CONTEXT.md
@.planning/phases/110-primitives/110-RESEARCH.md
@.planning/phases/110-primitives/110-VALIDATION.md
@.planning/phases/110-primitives/110-UI-SPEC.md
@.planning/phases/109-foundations-gate-tightening/109-02-SUMMARY.md
@.planning/phases/109-foundations-gate-tightening/109-04-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/test/mailglass_admin/components_test.exs
@mailglass_admin/assets/css/app.css
@mailglass_admin/assets/vendor/heroicons-inline.js
@mailglass_admin/mix.exs
</context>

## Multi-Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
ROADMAP | goal | Shared atoms are single public components; states/themes/viewports meet WCAG/APG; `stat_card` and tri-state picker exist | 110-01..110-04 | COVERED | API, consumers, gallery, gates, structural proof.
ROADMAP | SC-1 | Gallery-inlined atoms promoted and copy-drift gate passes | 110-01, 110-02, 110-03, 110-04 | COVERED | Public functions first, consumers next, drift gate last.
ROADMAP | SC-2 | Primitive states in light/dark/system meet WCAG 2.2 AA and APG | 110-01, 110-03, 110-04 | COVERED | Component contracts, gallery states, browser proof.
ROADMAP | SC-3 | Canonical `stat_card` with truncation, tabular no-wrap value, icon+label+color severity | 110-01, 110-02, 110-04 | COVERED | Component, migrations, STATCARD gate.
ROADMAP | SC-4 | Three-way theme picker and compiled-bundle 44x44 proof | 110-01, 110-02, 110-03, 110-04 | COVERED | Radio primitive, shell usage, gallery states, target-size proof.
ROADMAP | SC-5 | Every icon exists and is not sole meaning | 110-01, 110-04 | COVERED | Component assertions and icon-exists gate.
REQUIREMENTS | PRIM-01 | Promote gallery atoms to public components with no copy drift | 110-01, 110-02, 110-03, 110-04 | COVERED | D-01, D-02.
REQUIREMENTS | PRIM-02 | Render every primitive interaction state in light/dark/system at 320->wide | 110-01, 110-03, 110-04 | COVERED | D-03 plus structural matrix.
REQUIREMENTS | PRIM-03 | Disabled controls are visually and programmatically distinct | 110-01, 110-03, 110-04 | COVERED | Component attrs and browser checks.
REQUIREMENTS | PRIM-04 | Canonical `stat_card` primitive | 110-01, 110-02, 110-04 | COVERED | D-07, D-08, D-09.
REQUIREMENTS | PRIM-05 | Three-way system/light/dark theme picker | 110-01, 110-02, 110-03, 110-04 | COVERED | D-04, D-05, D-06.
REQUIREMENTS | PRIM-06 | 44x44 default target proof; 24px exceptions gated or recorded | 110-04 | COVERED | D-12, D-13.
REQUIREMENTS | PRIM-07 | Icon-exists guard and non-icon-alone meaning | 110-01, 110-04 | COVERED | D-10, D-11.
RESEARCH | R-01 | Move primitives to `MailglassAdmin.Components` before migrating consumers | 110-01 | COVERED | Primary recommendation.
RESEARCH | R-02 | Use native radios or APG radiogroup for theme picker | 110-01, 110-04 | COVERED | Native radio contract required.
RESEARCH | R-03 | Pair STATCARD grep with component and structural assertions | 110-01, 110-02, 110-04 | COVERED | Tests plus gate.
RESEARCH | R-04 | Compare `hero-*` usage against `heroicons-inline.js` | 110-04 | COVERED | ICON-EXISTS-GATE.
RESEARCH | R-05 | Rebuild committed CSS after class changes | 110-01, 110-02, 110-03 | COVERED | Bundle listed where source classes change.
CONTEXT | D-01 | Public primitive ownership in `MailglassAdmin.Components` | 110-01, 110-02, 110-03 | COVERED | Export and consume public functions.
CONTEXT | D-02 | Concrete copy-drift guard against private/inlined named primitives | 110-04 | COVERED | PRIMITIVE-DRIFT-GATE.
CONTEXT | D-03 | Gallery renders real public components and named states | 110-03, 110-04 | COVERED | Gallery state expansion.
CONTEXT | D-04 | Theme choices are `:system`, `:light`, `:dark`; system has no explicit theme value | 110-01, 110-02, 110-04 | COVERED | Component and event-path mapping.
CONTEXT | D-05 | Theme picker is radio group, not independent pressed buttons | 110-01, 110-04 | COVERED | Native radio checks.
CONTEXT | D-06 | Phase 110 owns primitive only, not persistence/no-FOUC/root behavior | 110-01, 110-02, 110-04 | COVERED | Scope scans.
CONTEXT | D-07 | Add `stat_card` and migrate operator/inbound overviews | 110-01, 110-02 | COVERED | Public primitive then consumers.
CONTEXT | D-08 | `stat_card` locked shape | 110-01, 110-02, 110-04 | COVERED | Tests and structural proof.
CONTEXT | D-09 | Add deterministic STATCARD gate plus focused assertions | 110-04 | COVERED | STATCARD-GATE.
CONTEXT | D-10 | Guard used icons against vendored Heroicons map | 110-04 | COVERED | ICON-EXISTS-GATE.
CONTEXT | D-11 | Icons never carry meaning alone | 110-01, 110-04 | COVERED | Tests require labels/access names.
CONTEXT | D-12 | Gate 24x24 WCAG minimum or require documented exception evidence | 110-04 | COVERED | Target-size proof and exception gate.
CONTEXT | D-13 | Preserve 44x44 default for normal admin primitives | 110-01, 110-04 | COVERED | min-h/min-w contract and compiled proof.
UI-SPEC | UI-01 | No shadcn/registry; Phoenix.Component/Tailwind/daisyUI stack only | 110-01..110-04 | COVERED | No package or registry tasks.
UI-SPEC | UI-02 | Token spacing/type/color contracts for primitives | 110-01, 110-03, 110-04 | COVERED | Component classes and conformance.
UI-SPEC | UI-03 | `theme_picker` segmented radio contract | 110-01, 110-03, 110-04 | COVERED | Fieldset/radio checks.
UI-SPEC | UI-04 | `stat_card` shape and severity contract | 110-01, 110-02, 110-04 | COVERED | Component, consumers, gate.
UI-SPEC | UI-05 | Gallery certifies real public components across states/themes | 110-03, 110-04 | COVERED | Real-component gallery proof.
UI-SPEC | UI-06 | Icons inventory, meaning, color, and size contract | 110-01, 110-04 | COVERED | Component tests and guard.
VALIDATION | V-01 | Component tests for named primitives | 110-01 | COVERED | `components_test.exs`.
VALIDATION | V-02 | Shell/gallery no-copy proof | 110-02, 110-03, 110-04 | COVERED | Consumers plus drift gate.
VALIDATION | V-03 | Gallery/system/touch-target structural proof | 110-03, 110-04 | COVERED | Playwright additions.
VALIDATION | V-04 | STATCARD, icon, and drift gates | 110-04 | COVERED | `check-conformance.sh`.
VALIDATION | V-05 | Bundle-clean proof | 110-01, 110-02, 110-03, 110-04 | COVERED | Asset build and final diff.

<tasks>

<task id="110-01-01" type="auto" files="mailglass_admin/lib/mailglass_admin/components.ex">
  <name>Task 1: Add public primitive component functions</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/lib/mailglass_admin/inbound/overview.ex
    .planning/phases/110-primitives/110-CONTEXT.md
    .planning/phases/110-primitives/110-RESEARCH.md
    .planning/phases/110-primitives/110-UI-SPEC.md
    .planning/phases/109-foundations-gate-tightening/109-02-SUMMARY.md
  </read_first>
  <action>In `MailglassAdmin.Components`, add exported Phoenix function components `nav_link/1`, `nav_pill/1`, `tenant_chip/1`, `theme_picker/1`, and `stat_card/1` per D-01. Use the existing shell markup as the seed for `nav_link`, `nav_pill`, and `tenant_chip`, but make them public, attr-declared, and able to render disabled states without navigation: disabled nav primitives must render no `navigate`/`patch` action, must expose `aria-disabled="true"`, and must remain visually distinct from active and inactive states per D-12 and D-13. Implement `theme_picker/1` per D-04 and D-05 as a native `fieldset`/`legend` radio group with exactly the values `system`, `light`, and `dark`; each option has visible text and a 44px target class contract, and the component must not write an explicit system theme attribute or browser storage/hook behavior per D-06. Implement `stat_card/1` per D-07 and D-08 with attr declarations for `:label`, `:value`, `:severity`, `:severity_label`, optional `:id`, and optional state text for empty/unavailable cases. `stat_card/1` must render a single-line label with a `title`, a no-wrap `tabular-nums` value, and a closed severity set `:neutral`, `:info`, `:success`, `:warning`, `:error` where every severity has an icon and a visible label. Keep icons decorative only when adjacent visible text carries the meaning per D-11.</action>
  <acceptance_criteria>
    - `mailglass_admin/lib/mailglass_admin/components.ex` contains public `def nav_link(assigns)`, `def nav_pill(assigns)`, `def tenant_chip(assigns)`, `def theme_picker(assigns)`, and `def stat_card(assigns)`.
    - Each new public component has Phoenix `attr` declarations, and `theme_picker/1` restricts its selected value to `:system`, `:light`, and `:dark`.
    - Disabled `nav_link/1` and `nav_pill/1` output contains `aria-disabled="true"` and no LiveView navigation attribute.
    - `theme_picker/1` renders a `fieldset`, a `legend` containing `Theme`, three radio inputs, and visible labels `System`, `Light`, and `Dark`.
    - `stat_card/1` renders a label element with `title`, a value element containing `tabular-nums` and `whitespace-nowrap`, and severity output containing a `hero-*` icon plus visible severity text.
    - New class strings use semantic tokens and existing Phase 109 focus utilities; no raw type-scale, faux-bold, off-grid spacing, z-index, raw focus idiom, raw radius, raw shadow, raw border, or arbitrary size utilities are introduced.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'def (nav_link|nav_pill|tenant_chip|theme_picker|stat_card)\\(assigns\\)' mailglass_admin/lib/mailglass_admin/components.ex</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>`MailglassAdmin.Components` owns the public primitive functions and their component-level contracts are expressible through tests and downstream consumers.</done>
</task>

<task id="110-01-02" type="auto" files="mailglass_admin/test/mailglass_admin/components_test.exs, mailglass_admin/priv/static/app.css">
  <name>Task 2: Add component contracts and rebuild the CSS bundle</name>
  <files>mailglass_admin/test/mailglass_admin/components_test.exs, mailglass_admin/priv/static/app.css</files>
  <read_first>
    mailglass_admin/test/mailglass_admin/components_test.exs
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/assets/css/app.css
    mailglass_admin/mix.exs
    .planning/phases/110-primitives/110-VALIDATION.md
    .planning/phases/110-primitives/110-UI-SPEC.md
  </read_first>
  <action>Extend `MailglassAdmin.ComponentsTest` using the existing named-test and `render_component/2` pattern. Add tests for `nav_link/1` active, inactive, disabled, hover-ready class contract, focus-visible class contract, and long-label states; `nav_pill/1` active, inactive, disabled, hover-ready class contract, focus-visible class contract, and long-label states; `tenant_chip/1` with tenant, no tenant, long tenant ID, non-ASCII tenant, and explicit read-only/non-loading applicability; `theme_picker/1` with system, light, dark, disabled, hover-ready option contract, focus-visible option contract, and explicit non-loading applicability because Phase 110 does not own async persistence per D-06; and `stat_card/1` neutral, info, success, warning, error, empty/no-data, loading/resolving, unavailable, long-label, and long-value states. Treat loading as applicable to `stat_card/1` because KPI values can be resolving; treat loading as not applicable to `nav_link/1`, `nav_pill/1`, `tenant_chip/1`, and `theme_picker/1` in Phase 110 because they are immediate navigation, read-only display, or value-selection primitives. The tests must assert exact semantic markers: `aria-current="page"` only for current nav, `aria-disabled="true"` for disabled nav, radio `checked` state for the selected theme, no `aria-pressed` theme options, hover-ready selectors/classes, shared `mg-focus-ring` or equivalent focus-visible token contract, `aria-busy` or visible loading copy for the loading stat-card state, `title` on long/truncated labels, `tabular-nums` plus `whitespace-nowrap` on stat values, and icon plus visible severity label for every stat severity per D-08 and D-11. Rebuild the admin CSS bundle with `mix mailglass_admin.assets.build` after adding primitive class strings and include `mailglass_admin/priv/static/app.css` if the build changes it.</action>
  <acceptance_criteria>
    - `components_test.exs` has named tests covering all five new public primitive functions.
    - `components_test.exs` asserts active/current, inactive, disabled, hover-ready, focus-visible, long-content, selected/checked, empty/no-data, loading where applicable, explicit loading non-applicability where not applicable, unavailable, and severity cases.
    - `components_test.exs` asserts `theme_picker/1` has three radio inputs and does not use `aria-pressed`.
    - `components_test.exs` asserts `stat_card/1` severity is icon plus visible label plus semantic color class, not color alone.
    - `components_test.exs` asserts the shared focus-visible ring/token contract for interactive primitives and does not treat read-only `tenant_chip/1` or non-interactive `stat_card/1` as keyboard controls unless a future attr makes them actionable.
    - `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --warnings-as-errors` exits 0.
    - `cd mailglass_admin && mix mailglass_admin.assets.build` exits 0, and `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after committing any bundle delta.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'hover-ready|focus-visible|loading|not applicable' mailglass_admin/test/mailglass_admin/components_test.exs</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin && mix mailglass_admin.assets.build</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>Focused component contracts prove the public primitive API and the committed static CSS bundle is synchronized with new component class usage.</done>
</task>

</tasks>

## Artifacts this phase produces

- Public component `MailglassAdmin.Components.nav_link/1`.
- Public component `MailglassAdmin.Components.nav_pill/1`.
- Public component `MailglassAdmin.Components.tenant_chip/1`.
- Public component `MailglassAdmin.Components.theme_picker/1`.
- Public component `MailglassAdmin.Components.stat_card/1`.
- Component contract tests in `mailglass_admin/test/mailglass_admin/components_test.exs`.
- Shell consumers of `Components.nav_link/1`, `Components.nav_pill/1`, `Components.tenant_chip/1`, and `Components.theme_picker/1`.
- Theme event/path helpers in operator and inbound LiveViews for `system`, `light`, and `dark` values, with `system` mapping to no explicit theme query value.
- Operator and inbound overview consumers of `Components.stat_card/1`.
- Gallery specimens for public primitives and states under `gallery-nav_link-*`, `gallery-nav_pill-*`, `gallery-tenant_chip-*`, `gallery-theme_picker-*`, and `gallery-stat_card-*`.
- `PRIMITIVE-DRIFT-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `STATCARD-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `ICON-EXISTS-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- Playwright structural assertions for primitive gallery state coverage, system wrapper absence of explicit theme, radio semantics, disabled distinction, 44px target sizes, icon meaning, and stat-card overflow/no-wrap behavior.
- Rebuilt `mailglass_admin/priv/static/app.css` when source class strings change.
- `.planning/phases/110-primitives/110-01-SUMMARY.md`.
- `.planning/phases/110-primitives/110-02-SUMMARY.md`.
- `.planning/phases/110-primitives/110-03-SUMMARY.md`.
- `.planning/phases/110-primitives/110-04-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| component attrs -> rendered HEEx | Public component attrs can affect semantics, classes, and icon names. |
| rendered icon class -> vendored icon map | A `hero-*` class must correspond to an embedded SVG in `heroicons-inline.js`. |
| primitive theme control -> shell behavior | The primitive must expose a valid three-choice control without taking ownership of persistence or root theme behavior. |

## STRIDE Threat Register

Execution must stop on any HIGH or CRITICAL threat whose status is `open`. The executor must either complete the mitigation and rerun the listed verification, or create a blocking checkpoint that records why the threat cannot be closed.

| Threat ID | STRIDE | ASVS L1 Mapping | Severity | Status | Component | Mitigation / Verification |
|---|---|---|---|---|---|---|
| T-110-01 | Tampering | V5 Input Validation | HIGH | planned | `Components.theme_picker/1` | Use closed choice values `:system`, `:light`, `:dark` and native radio semantics. Verification: focused component tests. |
| T-110-02 | Spoofing | V5 Input Validation | HIGH | planned | disabled nav primitives | Disabled links must be programmatically disabled and non-navigating. Verification: component tests assert `aria-disabled` and no navigation attr. |
| T-110-03 | Tampering | V14 Configuration | MEDIUM | planned | icon classes | Plan 110-04 adds `ICON-EXISTS-GATE`; this plan keeps icons adjacent to text. Verification: component tests and later icon gate. |
| T-110-04 | Information Disclosure | V4 Access Control | MEDIUM | planned | `tenant_chip/1` | Keep `tenant_chip/1` read-only and do not add tenant listing or switching behavior. Verification: component tests and final scope scan. |
| T-110-SC | Tampering | V14 Configuration | LOW | accepted | package installs | Phase 110 installs no package-manager dependencies. Verification: final package manifest diff remains empty. |
</threat_model>

<verification>
Plan-level verification:
- `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --warnings-as-errors`
- `bash mailglass_admin/scripts/check-conformance.sh`
- `cd mailglass_admin && mix mailglass_admin.assets.build`
- `cd mailglass_admin && git diff --exit-code priv/static/`
</verification>

<success_criteria>
Plan 110-01 is complete when the public primitive functions exist in `MailglassAdmin.Components`, focused component tests prove their APG/WCAG-relevant markup states, all tests and conformance gates pass, and the committed CSS bundle is clean after the asset build.
</success_criteria>

<output>
Create `.planning/phases/110-primitives/110-01-SUMMARY.md` when done.
</output>
