---
phase: "38"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/mix/tasks/mailglass.publish.check.ex"
  - ".planning/publish/mailglass-publish-summary.json"
  - ".planning/publish/mailglass_admin-publish-summary.json"
  - ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md"
  - "test/mailglass/docs_contract_test.exs"
  - "test/mailglass/stability_contract_test.exs"
  - "mailglass_admin/test/mailglass_admin/mix_config_test.exs"
autonomous: true
requirements:
  - RELS-03
must_haves:
  truths:
    - "Maintainers can export one committed proof bundle showing the exact tarball contents, HexDocs inputs, release manifest versions, and admin publish pin that will ship at `v1.0`."
    - "The proof bundle is derived from the existing prepublish checker and package metadata instead of from a second packaging scanner."
    - "Sibling-package release truth stays inspectable: release manifest versions and the exact `{:mailglass, \"== <version>\"}` publish pin are both captured."
  artifacts:
    - path: "lib/mix/tasks/mailglass.publish.check.ex"
      provides: "Existing prepublish checker extended to export durable proof data"
      contains: "write reviewer summary"
    - path: ".planning/publish/mailglass-publish-summary.json"
      provides: "Machine-readable proof for the core package tarball and docs inputs"
      contains: "\"package\":\"mailglass\""
    - path: ".planning/publish/mailglass_admin-publish-summary.json"
      provides: "Machine-readable proof for the admin package tarball, docs inputs, and sibling pin"
      contains: "\"package\":\"mailglass_admin\""
    - path: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md"
      provides: "Canonical human-readable proof bundle summarizing package, docs, sibling release truth, and release rehearsal evidence"
      contains: "## Release rehearsal evidence"
  key_links:
    - from: "lib/mix/tasks/mailglass.publish.check.ex"
      to: ".planning/publish/mailglass-publish-summary.json"
      via: "existing prepublish check writes durable summary output"
      pattern: "mailglass-publish-summary\\.json|write_summary"
    - from: "lib/mix/tasks/mailglass.publish.check.ex"
      to: "mailglass_admin/mix.exs"
      via: "publish-mode `mailglass` exact pin is exported into proof data"
      pattern: "mailglass_dep|== 0\\."
    - from: ".planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md"
      to: ".release-please-manifest.json"
      via: "bundle records linked-version release truth"
      pattern: "manifest|mailglass_admin"
---

<objective>
Export one committed prepublish proof bundle from the repo’s existing release seams.

Purpose: Satisfy RELS-03 per D-38-14 through D-38-18 by turning the existing `mix mailglass.publish.check` path, package metadata, and linked-version files into durable release artifacts instead of inventing a parallel checker.
Output: Machine-readable publish summaries for both packages, one human-readable proof bundle, and targeted tests that keep the export shape and sibling pin honest.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-CONTEXT.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-RESEARCH.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-PATTERNS.md
@.planning/phases/38-release-rehearsal-and-proof-artifacts/38-VALIDATION.md
@MAINTAINING.md
@lib/mix/tasks/mailglass.publish.check.ex
@mix.exs
@mailglass_admin/mix.exs
@.release-please-manifest.json
@.planning/publish/mailglass-files.expected
@.planning/publish/mailglass_admin-files.expected
@test/mailglass/docs_contract_test.exs
@test/mailglass/stability_contract_test.exs
@mailglass_admin/test/mailglass_admin/mix_config_test.exs

<interfaces>
From lib/mix/tasks/mailglass.publish.check.ex:
```elixir
defp load_package_context(package) do
  %{
    package: package,
    version: version,
    manifest_version: manifest_version(manifest, package),
    expected_file: Path.join(repo_root, ".planning/publish/#{package}-files.expected"),
    helper_body: maybe_find_function_body(ast, :mailglass_dep, 0)
  }
end
```

From mix.exs / mailglass_admin/mix.exs:
```elixir
package: package(),
docs: docs()
defp mailglass_dep do
  {:mailglass, "== 0.3.2"}
end
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Export machine-readable prepublish summaries from the existing package checker</name>
  <files>lib/mix/tasks/mailglass.publish.check.ex, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-publish-summary.json</files>
  <read_first>lib/mix/tasks/mailglass.publish.check.ex, mix.exs, mailglass_admin/mix.exs, .release-please-manifest.json, .planning/publish/mailglass-files.expected, .planning/publish/mailglass_admin-files.expected, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-CONTEXT.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-RESEARCH.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-PATTERNS.md</read_first>
  <acceptance_criteria>
    - `mix mailglass.publish.check --package mailglass` writes `.planning/publish/mailglass-publish-summary.json`.
    - `mix mailglass.publish.check --package mailglass_admin` writes `.planning/publish/mailglass_admin-publish-summary.json`.
    - Each JSON file contains exact keys for `package`, `version`, `manifest_version`, `files`, `extras`, `groups_for_extras`, `source_url`, `source_ref` or `source_ref_pattern`, and package-specific release truth.
    - `.planning/publish/mailglass_admin-publish-summary.json` contains the exact publish pin field `mailglass_admin_publish_pin`.
    - The export is populated from the existing checker/package metadata path per D-38-16 through D-38-18, not from a new standalone repo scanner.
  </acceptance_criteria>
  <action>Extend `lib/mix/tasks/mailglass.publish.check.ex` so the existing prepublish run writes one machine-readable summary file per package into `.planning/publish/`: `mailglass-publish-summary.json` and `mailglass_admin-publish-summary.json`. Export concrete fields required by D-38-15 through D-38-18: package name, current `@version`, release-manifest version, expected allowlist path, actual tarball file list, tarball size, required files verdict, docs `extras`, docs `groups_for_extras`, `source_url`, `source_ref` or `source_ref_pattern`, and linked-version truth. For `mailglass_admin`, also export the exact publish-mode `{:mailglass, \"== <version>\"}` pin under a literal `mailglass_admin_publish_pin` key. Reuse the task’s current AST/package-context helpers and summary-writing seam; do not add a second packaging checker, a second allowlist parser, or a repo-wide grep-only export path.</action>
  <verify>
    <automated>mix mailglass.publish.check --package mailglass && mix mailglass.publish.check --package mailglass_admin && rg -n '"package"|"version"|"manifest_version"|"expected_file"|"files"|"tarball_size"|"required_files"|"extras"|"groups_for_extras"|"source_url"|"source_ref"|"source_ref_pattern"' .planning/publish/mailglass-publish-summary.json .planning/publish/mailglass_admin-publish-summary.json && rg -n '"mailglass_admin_publish_pin"|"manifest_version"|"files"|"extras"|"groups_for_extras"' .planning/publish/mailglass_admin-publish-summary.json</automated>
  </verify>
  <done>The prepublish checker now emits durable machine-readable proof for both packages without duplicating the packaging logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit one human-readable proof bundle and lock it with targeted contract tests</name>
  <files>.planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md, test/mailglass/docs_contract_test.exs, test/mailglass/stability_contract_test.exs, mailglass_admin/test/mailglass_admin/mix_config_test.exs</files>
  <read_first>.planning/phases/38-release-rehearsal-and-proof-artifacts/38-CONTEXT.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-RESEARCH.md, .planning/phases/38-release-rehearsal-and-proof-artifacts/38-PATTERNS.md, .planning/publish/mailglass-publish-summary.json, .planning/publish/mailglass_admin-publish-summary.json, test/mailglass/docs_contract_test.exs, test/mailglass/stability_contract_test.exs, mailglass_admin/test/mailglass_admin/mix_config_test.exs, MAINTAINING.md</read_first>
  <acceptance_criteria>
    - `38-01-PREPUBLISH-PROOF.md` contains the headings `## Tarball/package truth`, `## HexDocs input truth`, `## Sibling release truth`, and `## Release rehearsal evidence`.
    - `38-01-PREPUBLISH-PROOF.md` names `.planning/publish/mailglass-publish-summary.json` and `.planning/publish/mailglass_admin-publish-summary.json` explicitly.
    - `test/mailglass/docs_contract_test.exs` asserts the proof bundle includes `groups_for_extras`, `source_url`, `source_ref` or `source_ref_pattern`, and the summary JSON paths.
    - `test/mailglass/stability_contract_test.exs` continues to assert the semantic proof entrypoint and adds a focused assertion that `Mix.Tasks.Mailglass.Publish.Check` remains part of the stable release-proof surface.
    - `mailglass_admin/test/mailglass_admin/mix_config_test.exs` still proves the exact `{:mailglass, "== <version>"}` publish-pin literal shape consumed by the sibling-proof export.
  </acceptance_criteria>
  <action>Create `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md` as the single committed human-readable bundle for D-38-15, matching the filename assignment in `38-PATTERNS.md`. Summarize four concrete sections from D-38-15: tarball/package truth, HexDocs input truth, sibling release truth, and release rehearsal evidence. The bundle must point directly to the two exported JSON files and record the concrete source files that define truth: `mix.exs`, `mailglass_admin/mix.exs`, `.release-please-manifest.json`, `.planning/publish/*-files.expected`, `.github/workflows/publish-hex.yml`, and `.github/workflows/post-publish-smoke.yml`. In the HexDocs input truth section, require the exact docs-input fields from D-38-17, including `extras`, `groups_for_extras`, `source_url`, and `source_ref` or `source_ref_pattern`. In the release rehearsal evidence section, establish this file as the authoritative bundle that later tasks in Plans 38-02 and 38-03 will append with concrete install, upgrade, and release-record results, while any `38-02-*` and `38-03-*` docs remain supporting detail rather than replacing the bundle. Then extend `test/mailglass/docs_contract_test.exs` so this new bundle is treated as committed release truth and add a narrow assertion to `test/mailglass/stability_contract_test.exs` that `Mix.Tasks.Mailglass.Publish.Check` remains a stable proof seam rather than a temporary script. Keep `mailglass_admin/test/mailglass_admin/mix_config_test.exs` aligned with the exact publish-pin literal used by both Release Please and the new proof export.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors && cd mailglass_admin && mix test test/mailglass_admin/mix_config_test.exs --warnings-as-errors && cd .. && rg -n "Tarball/package truth|HexDocs input truth|Sibling release truth|Release rehearsal evidence|mailglass-publish-summary.json|mailglass_admin-publish-summary.json|groups_for_extras|source_url|source_ref|source_ref_pattern" .planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-PREPUBLISH-PROOF.md test/mailglass/docs_contract_test.exs</automated>
  </verify>
  <done>The phase has one committed proof bundle and targeted tests keep its package/docs/pin truth aligned with the actual release metadata.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepublish checker -> committed proof artifacts | Release truth crosses from executable checks into committed artifacts used for audit and release decisions. |
| linked-version metadata -> sibling publish contract | The admin package’s publish pin and manifest versions define whether both packages actually release as one line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `lib/mix/tasks/mailglass.publish.check.ex` | mitigate | Export proof from the existing checker/package metadata path so committed artifacts cannot drift from the release gate through parallel logic. |
| T-38-02 | Repudiation | `.planning/publish/*.json`, `38-01-PREPUBLISH-PROOF.md` | mitigate | Commit both machine-readable and human-readable proof with exact source-file references and targeted tests. |
| T-38-03 | Information Disclosure | proof bundle contents | accept | The exported fields are package/docs metadata and release workflow references only; no secrets or PII are included. |
</threat_model>

<verification>
- `mix mailglass.publish.check --package mailglass`
- `mix mailglass.publish.check --package mailglass_admin`
- `mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix test test/mailglass_admin/mix_config_test.exs --warnings-as-errors`
</verification>

<success_criteria>
Maintainers can inspect one committed prepublish proof document and its backing JSON summaries to verify tarball contents, HexDocs inputs, release manifest versions, and the admin sibling pin before publish.
</success_criteria>

<output>
After completion, create `.planning/phases/38-release-rehearsal-and-proof-artifacts/38-01-SUMMARY.md`
</output>
