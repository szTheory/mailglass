---
phase: 64-contract-verification-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/mime_error.ex
  - mailglass_inbound/lib/mailglass_inbound/signature_error.ex
  - mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
  - mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Stable inbound structured-error modules expose truthful package-line metadata."
    - "Stable inbound operator task modules expose package-line metadata without promoting `run/1` or `run/2` as API."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/mime_error.ex"
      provides: "Stable MIME error metadata aligned to `0.2.0`"
    - path: "mailglass_inbound/lib/mailglass_inbound/signature_error.ex"
      provides: "Stable signature error metadata aligned to `0.2.0`"
    - path: "mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex"
      provides: "Stable command-surface metadata for replay semantics"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/signature_error.ex"
      to: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      via: "Code.fetch_docs/1 module and `__types__/0` assertions"
      pattern: "def __types__"
    - from: "mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex"
      to: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      via: "module-level since assertions only"
      pattern: "@moduledoc since"
---

<objective>
Normalize package-version `since` metadata on the stable inbound structured errors and operator task modules.

Purpose: Keep the Phase 64 compiled-doc proof honest for the `0.2.0` surfaces that shipped after the initial inbound `0.1.0` slice.
Output: Stable error modules and Mix task modules tagged to the inbound package line, without widening runtime API guarantees.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-verification-hardening/64-CONTEXT.md
@.planning/phases/64-contract-verification-hardening/64-RESEARCH.md
@.planning/phases/64-contract-verification-hardening/64-PATTERNS.md

<interfaces>
From `mailglass_inbound/lib/mailglass_inbound/mime_error.ex`:
```elixir
def __types__() :: [:inbound_mime_invalid, :gen_smtp_unavailable]
```

From `mailglass_inbound/lib/mailglass_inbound/signature_error.ex`:
```elixir
def __types__() :: [:bad_signature, :missing_header, :malformed_header, :timestamp_skew, :subscribe_url_untrusted]
def new(type, opts \\ [])
```

From `mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex`:
```elixir
def __types__() :: [:s3_object_not_ready, :s3_fetch_failed]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag the three stable inbound error modules to `0.2.0` and keep the closed-set API narrow</name>
  <read_first>
    mailglass_inbound/lib/mailglass_inbound/mime_error.ex
    mailglass_inbound/lib/mailglass_inbound/signature_error.ex
    mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-PATTERNS.md
  </read_first>
  <files>mailglass_inbound/lib/mailglass_inbound/mime_error.ex, mailglass_inbound/lib/mailglass_inbound/signature_error.ex, mailglass_inbound/lib/mailglass_inbound/s3_fetch_error.ex</files>
  <action>Add `@moduledoc since: "0.2.0"` to `MailglassInbound.MIMEError`, `MailglassInbound.SignatureError`, and `MailglassInbound.S3FetchError` per D-02 and the Phase 45/46 introduction history. Preserve the existing `__types__/0` `@doc since: "0.2.0"` entries, keep `SignatureError.new/2` at `0.2.0`, and do not surface `message/1`, serializer details, or exception-internal helpers as new public contract points.</action>
  <acceptance_criteria>
    - All three error files contain `@moduledoc since: "0.2.0"`.
    - `__types__/0` remains tagged `@doc since: "0.2.0"` in all three files.
    - No new `@doc since:` annotation is added to `message/1` or other internal exception helpers.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/mime_error_test.exs test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The stable error modules are ready for exact module-level and closed-set compiled-doc assertions without freezing non-contract internals.</done>
</task>

<task type="auto">
  <name>Task 2: Correct stable operator Mix task metadata to the inbound package line</name>
  <read_first>
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex
    mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/lib/mix/tasks/mailglass.inbound.doctor.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.replay.ex, mailglass_inbound/lib/mix/tasks/mailglass.inbound.prune.ex</files>
  <action>Replace the existing milestone-line `@moduledoc since: "1.2.0"` tokens on `Mix.Tasks.Mailglass.Inbound.Doctor`, `Mix.Tasks.Mailglass.Inbound.Replay`, and `Mix.Tasks.Mailglass.Inbound.Prune` with `@moduledoc since: "0.2.0"` so the operator command contract follows the inbound package version line. Do not add `@doc since:` to `run/1` or `run/2`; D-03 and Phase 63 keep command semantics stable at the Mix task module level, not at direct `run/*` invocation level.</action>
  <acceptance_criteria>
    - All three Mix task files contain `@moduledoc since: "0.2.0"`.
    - None of the three files adds `@doc since:` to `run/1` or `run/2`.
    - The module-level wording still describes stable command semantics without promoting worker, queue, or internal service modules.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix compile --warnings-as-errors</automated>
  </verify>
  <done>The stable operator command modules reflect the inbound package release line and preserve the command-behavior-only contract boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error/task source metadata -> compiled docs | Stable error and operator task metadata become part of the package’s executable public contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03 | Tampering | stable structured-error metadata | mitigate | Tag the three stable error modules to `0.2.0` and keep module assertions separate from internal exception helpers. |
| T-64-04 | Elevation of privilege | operator task API boundary | mitigate | Keep module-level task metadata only; explicitly avoid promoting `run/1` or `run/2` into the public contract. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/mime_error_test.exs test/mailglass_inbound/signature_error_test.exs test/mailglass_inbound/s3_fetch_error_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Stable inbound error modules and operator task modules use truthful `0.2.0` `since` metadata.
- The contract stays narrow: no new guarantee is implied for exception internals or direct Mix task `run/*` calls.
</success_criteria>

<output>
Create `.planning/phases/64-contract-verification-hardening/64-02-SUMMARY.md` when done
</output>
