---
phase: 64-contract-verification-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_inbound/lib/mailglass_inbound/fixtures.ex
  - mailglass_inbound/lib/mailglass_inbound/test/ingress.ex
  - mailglass_inbound/lib/mailglass_inbound/test_assertions.ex
  - mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Adopter-facing inbound testing helpers expose truthful compiled-doc metadata as a separate testing contract bucket."
    - "Testing helper versions stay on the inbound package line and do not masquerade as runtime-stable APIs."
  artifacts:
    - path: "mailglass_inbound/lib/mailglass_inbound/fixtures.ex"
      provides: "Builder function metadata for the testing support surface"
    - path: "mailglass_inbound/lib/mailglass_inbound/test/ingress.ex"
      provides: "Driver metadata for the testing ingress seam"
    - path: "mailglass_inbound/lib/mailglass_inbound/test_assertions.ex"
      provides: "Assertion macro metadata for the testing contract bucket"
  key_links:
    - from: "mailglass_inbound/lib/mailglass_inbound/test_assertions.ex"
      to: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      via: "macro metadata assertions"
      pattern: "defmacro assert_inbound_"
    - from: "mailglass_inbound/lib/mailglass_inbound/test/ingress.ex"
      to: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      via: "function metadata assertions"
      pattern: "def receive_(?:inbound|provider_payload)"
---

<objective>
Normalize package-version `since` metadata on the adopter-facing inbound testing helpers.

Purpose: Make the testing bucket from Phase 63 executable without promoting those helpers into the runtime stable surface.
Output: `0.2.0` module and function metadata across fixtures, ingress drivers, assertions, and the case template.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-verification-hardening/64-CONTEXT.md
@.planning/phases/64-contract-verification-hardening/64-RESEARCH.md
@.planning/phases/64-contract-verification-hardening/64-PATTERNS.md

<interfaces>
From `mailglass_inbound/lib/mailglass_inbound/fixtures.ex`:
```elixir
def build_inbound_message(opts \\ [])
def build_postmark_payload(opts \\ [])
def build_sendgrid_payload(opts \\ [])
def sendgrid_fixture_config()
def build_mailgun_payload(opts \\ [])
def mailgun_fixture_config()
def build_ses_sns_payload(opts \\ [])
```

From `mailglass_inbound/lib/mailglass_inbound/test/ingress.ex`:
```elixir
def receive_inbound(%InboundMessage{} = message, opts \\ [])
def receive_provider_payload(provider, payload, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag fixture builders and ingress drivers to `0.2.0`</name>
  <read_first>
    mailglass_inbound/lib/mailglass_inbound/fixtures.ex
    mailglass_inbound/lib/mailglass_inbound/test/ingress.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-PATTERNS.md
  </read_first>
  <files>mailglass_inbound/lib/mailglass_inbound/fixtures.ex, mailglass_inbound/lib/mailglass_inbound/test/ingress.ex</files>
  <action>Add `@moduledoc since: "0.2.0"` to `MailglassInbound.Fixtures` and `MailglassInbound.Test.Ingress`, then add `@doc since: "0.2.0"` to the direct adopter-facing testing functions introduced in Phase 47: `build_inbound_message/1`, `build_postmark_payload/1`, `build_sendgrid_payload/1`, `sendgrid_fixture_config/0`, `build_mailgun_payload/1`, `mailgun_fixture_config/0`, `build_ses_sns_payload/1`, `receive_inbound/2`, and `receive_provider_payload/3`. Do not add metadata to internal helper functions or router route-data details called out as internal in the moduledoc per D-03 and D-04.</action>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound/fixtures.ex` and `mailglass_inbound/lib/mailglass_inbound/test/ingress.ex` each contain `@moduledoc since: "0.2.0"`.
    - Every listed public builder/driver function has `@doc since: "0.2.0"`.
    - No internal helper or route-data implementation function is newly tagged as part of the stable compiled-doc contract.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/fixtures_test.exs test/mailglass_inbound/test/ingress_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The fixture and ingress helper modules expose truthful `0.2.0` metadata for the adopter-facing testing bucket only.</done>
</task>

<task type="auto">
  <name>Task 2: Correct assertion and case-template metadata to the `0.2.0` testing line</name>
  <read_first>
    mailglass_inbound/lib/mailglass_inbound/test_assertions.ex
    mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-PATTERNS.md
  </read_first>
  <files>mailglass_inbound/lib/mailglass_inbound/test_assertions.ex, mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex</files>
  <action>Add `@moduledoc since: "0.2.0"` to `MailglassInbound.TestAssertions` and `MailglassInbound.MailboxCase`, and correct every existing `MailglassInbound.TestAssertions` macro `@doc since:` from `0.1.0` to `0.2.0` because the entire testing-helper surface shipped in Phase 47 / package `0.2.0`. Keep `__match_keyword__/2` and `__assert_outcome__/1` as internal helpers (`@doc false`) and do not invent module functions on `MailboxCase` just to satisfy the proof.</action>
  <acceptance_criteria>
    - `mailglass_inbound/lib/mailglass_inbound/test_assertions.ex` contains `@moduledoc since: "0.2.0"` and every public assertion macro is tagged `@doc since: "0.2.0"`.
    - `mailglass_inbound/lib/mailglass_inbound/mailbox_case.ex` contains `@moduledoc since: "0.2.0"`.
    - `__match_keyword__/2` and `__assert_outcome__/1` remain internal helpers rather than newly public contract entries.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/test_assertions_test.exs test/mailglass_inbound/mailbox_case_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The assertion macros and case template reflect the correct `0.2.0` testing-support release line without widening into runtime API guarantees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testing helper source metadata -> compiled docs | Adopter-facing testing helpers ship in `lib/` and therefore cross into the public package contract if metadata is wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-05 | Tampering | testing helper metadata | mitigate | Pin every adopter-facing testing helper module and direct function/macro to `0.2.0`, the package line where the helpers actually shipped. |
| T-64-06 | Elevation of privilege | testing/runtime boundary | mitigate | Keep the testing helpers in their own bucket and refuse to annotate `@doc false` internals or runtime-only helpers as public compiled-doc seams. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/fixtures_test.exs test/mailglass_inbound/test/ingress_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix test test/mailglass_inbound/test_assertions_test.exs test/mailglass_inbound/mailbox_case_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- Every adopter-facing testing helper module is tagged to the truthful `0.2.0` inbound package release line.
- The testing bucket remains separate from runtime-stable semantics and keeps internal helper macros/functions out of the compiled-doc contract.
</success_criteria>

<output>
Create `.planning/phases/64-contract-verification-hardening/64-03-SUMMARY.md` when done
</output>
