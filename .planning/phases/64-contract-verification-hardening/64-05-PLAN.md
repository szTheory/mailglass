---
phase: 64-contract-verification-hardening
plan: 05
type: execute
wave: 2
depends_on:
  - 64-01
  - 64-02
  - 64-03
  - 64-04
files_modified:
  - mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs
  - mailglass_inbound/mix.exs
  - mix.exs
  - test/mailglass/stability_contract_test.exs
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "The inbound package owns one compiled-doc stability proof lane."
    - "Root `mix verify.stability_contract` delegates to the inbound package support-contract lane instead of hard-coding inbound test files."
  artifacts:
    - path: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      provides: "Authoritative inbound compiled-doc proof for stable runtime, error, task, and testing surfaces"
    - path: "mailglass_inbound/mix.exs"
      provides: "Package-owned `verify.support_contract.inbound` alias and preferred test env"
    - path: "mix.exs"
      provides: "Root aggregate wiring that delegates to the inbound package lane"
  key_links:
    - from: "mailglass_inbound/mix.exs"
      to: "mix.exs"
      via: "root `cmd --cd mailglass_inbound mix verify.support_contract.inbound` delegation"
      pattern: "verify\\.support_contract\\.inbound"
    - from: "mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs"
      to: "mailglass_inbound/lib/mailglass_inbound"
      via: "Code.fetch_docs/1 assertions across runtime, error, task, and testing modules"
      pattern: "Code\\.fetch_docs"
---

<objective>
Create the authoritative inbound compiled-doc proof and wire both the package-local and root stability aliases through it.

Purpose: Satisfy PROOF-01 with a package-owned support-contract lane and keep the root stability gate at wiring-only responsibility per D-05 and D-16 through D-20.
Output: `mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs`, `verify.support_contract.inbound`, and root delegation to that lane.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-verification-hardening/64-CONTEXT.md
@.planning/phases/64-contract-verification-hardening/64-RESEARCH.md
@.planning/phases/64-contract-verification-hardening/64-PATTERNS.md
@.planning/phases/63-inbound-contract-inventory-reconciliation/63-01-SUMMARY.md

<interfaces>
Use the existing compiled-doc helper pattern from root/admin:
```elixir
defp docs!(module) do
  assert {:docs_v1, _, :elixir, _, _, metadata, docs} = Code.fetch_docs(module)
  %{metadata: metadata, docs: docs}
end

defp entry_meta!(module, kind, name, arity) do
  ...
end
```

Target inbound entrypoints for assertions:
```elixir
MailglassInbound.version/0
MailglassInbound.InboundMessage.suppression_flagged?/1
MailglassInbound.Ingress.CachingBodyReader.read_body/2
MailglassInbound.Router.__using__/1
MailglassInbound.Router.route/2
MailglassInbound.Mailbox.process/1
MailglassInbound.PubSub.Topics.inbound_record_inserted/1
MailglassInbound.MIMEError.__types__/0
MailglassInbound.SignatureError.__types__/0
MailglassInbound.S3FetchError.__types__/0
MailglassInbound.Fixtures.* public builders/config helpers
MailglassInbound.Test.Ingress.receive_inbound/2
MailglassInbound.Test.Ingress.receive_provider_payload/3
MailglassInbound.TestAssertions.* public macros
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the package-local inbound compiled-doc stability proof</name>
  <read_first>
    mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs
    test/mailglass/stability_contract_test.exs
    mailglass_admin/test/mailglass_admin/stability_contract_test.exs
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
    .planning/phases/64-contract-verification-hardening/64-PATTERNS.md
  </read_first>
  <files>mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs</files>
  <action>Create `mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs` as the D-01 authoritative compiled-doc proof for the inbound package. Reuse the root/admin `Code.fetch_docs/1` helper pattern and assert module-level `since` metadata for the stable runtime modules, structured error modules, stable Mix task modules, and testing-helper modules. Assert entry-level metadata only for the direct adopter-facing surfaces from D-03: `MailglassInbound.version/0`, `MailglassInbound.InboundMessage.suppression_flagged?/1`, `MailglassInbound.Ingress.CachingBodyReader.read_body/2`, `MailglassInbound.Router.__using__/1`, `MailglassInbound.Router.route/2`, `MailglassInbound.Mailbox.process/1`, `MailglassInbound.PubSub.Topics.inbound_record_inserted/1`, the three `__types__/0` functions, the direct `Fixtures` builders/config helpers, `MailglassInbound.Test.Ingress.receive_inbound/2`, `MailglassInbound.Test.Ingress.receive_provider_payload/3`, and the public `MailglassInbound.TestAssertions` macros. Do not assert metadata for provider modules, replay internals, workers, queue names, `@doc false` helpers, `run/1`/`run/2`, or generated implementation functions.</action>
  <acceptance_criteria>
    - `mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs` defines `docs!/1`, `entry_meta!/4`, and module-level assertion helpers mirroring the root/admin pattern.
    - The new test file covers all D-02 module groups and all D-03 direct entrypoints listed in the action.
    - The new test file explicitly excludes internal helpers, provider modules, replay internals, workers, queue names, direct Mix task `run/*` functions, and `@doc false` entries.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix test test/mailglass_inbound/stability_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The inbound package has one authoritative compiled-doc proof file that fails closed on any metadata drift across the stable runtime, error, task, and testing surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Own the inbound support-contract lane locally and delegate to it from root</name>
  <read_first>
    mailglass_inbound/mix.exs
    mix.exs
    test/mailglass/stability_contract_test.exs
    mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs
    mailglass_inbound/test/mailglass_inbound/docs_contract_test.exs
    .planning/phases/64-contract-verification-hardening/64-CONTEXT.md
  </read_first>
  <files>mailglass_inbound/mix.exs, mix.exs, test/mailglass/stability_contract_test.exs</files>
  <action>Implement D-16 through D-20 by making the inbound package own its support-contract lane: add `cli/0` preferred env entries for `verify.support_contract.inbound` and the package-local `verify.stability_contract` delegate; add `verify.support_contract.inbound` to `mailglass_inbound/mix.exs` as one `mix test ... --warnings-as-errors` invocation that runs `test/mailglass_inbound/docs_contract_test.exs` and `test/mailglass_inbound/stability_contract_test.exs` together; add package-local `verify.stability_contract` as a delegate to `verify.support_contract.inbound`; keep `verify.docs.contract.inbound` docs-only and unchanged in meaning; update root `mix.exs` so `verify.stability_contract` now calls `cmd --cd mailglass_inbound mix verify.support_contract.inbound` instead of the direct inbound docs test file; update `test/mailglass/stability_contract_test.exs` so root proof asserts only the new delegation string and remains wiring-only per D-05.</action>
  <acceptance_criteria>
    - `mailglass_inbound/mix.exs` contains `cli/0` with `preferred_envs` entries for `verify.support_contract.inbound` and `verify.stability_contract`.
    - `mailglass_inbound/mix.exs` defines `verify.support_contract.inbound` as one `mix test ... --warnings-as-errors` command that includes both `docs_contract_test.exs` and `stability_contract_test.exs`.
    - `mix.exs` contains `cmd --cd mailglass_inbound mix verify.support_contract.inbound` inside root `verify.stability_contract`.
    - `test/mailglass/stability_contract_test.exs` asserts the delegation string above and no longer hard-codes the old direct inbound docs-test command.
    - `verify.docs.contract.inbound` remains docs-only and retains its current meaning.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_inbound && mix verify.support_contract.inbound && cd .. && mix verify.stability_contract</automated>
  </verify>
  <done>The inbound package owns its own support-contract lane, root verification delegates to it, and the root stability test proves only the alias wiring rather than duplicating inbound surface assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package-local proof -> root aggregate gate | The inbound package’s contract proof becomes part of the root `mix verify.stability_contract` gate via alias delegation. |

## Blocking Rule

High-severity threats below are blocking. The plan is not acceptable until the listed mitigation is implemented and the named verification command passes.

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Blocking | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|----------|-----------------|
| T-64-10 | Repudiation | high | compiled-doc stability proof ownership | mitigate | yes | Blocking acceptance: `mailglass_inbound/test/mailglass_inbound/stability_contract_test.exs` exists as the single authoritative inbound compiled-doc proof and `cd mailglass_inbound && mix test test/mailglass_inbound/stability_contract_test.exs --warnings-as-errors` passes. |
| T-64-11 | Elevation of privilege | high | root verification alias wiring | mitigate | yes | Blocking acceptance: root `verify.stability_contract` delegates to `cmd --cd mailglass_inbound mix verify.support_contract.inbound`, `test/mailglass/stability_contract_test.exs` pins that exact delegation string, and both `cd mailglass_inbound && mix verify.support_contract.inbound` and `mix verify.stability_contract` pass. |
| T-64-12 | Tampering | medium | docs-only lane meaning | mitigate | no | Keep `verify.docs.contract.inbound` unchanged and docs-only while the broader support lane adds the compiled-doc proof separately, so the docs-only lane does not silently broaden in scope. |
</threat_model>

<verification>
- `cd mailglass_inbound && mix test test/mailglass_inbound/stability_contract_test.exs --warnings-as-errors`
- `cd mailglass_inbound && mix verify.support_contract.inbound`
- `mix verify.stability_contract`
</verification>

<success_criteria>
- `mailglass_inbound` has one authoritative compiled-doc proof file for the stable runtime, error, task, and testing surfaces.
- Root `mix verify.stability_contract` delegates to the inbound package-owned support-contract lane and the root proof remains wiring-only.
</success_criteria>

<output>
Create `.planning/phases/64-contract-verification-hardening/64-05-SUMMARY.md` when done
</output>
