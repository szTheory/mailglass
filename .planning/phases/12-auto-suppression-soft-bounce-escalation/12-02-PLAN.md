---
phase: 12-auto-suppression-soft-bounce-escalation
plan: 02
type: execute
wave: 2
depends_on: [12-01, 12-04]
files_modified:
  - lib/mailglass/suppression/escalation.ex
  - lib/mailglass/optional_deps/oban.ex
  - lib/mailglass/webhook/ingest.ex
  - priv/repo/migrations/20260428120000_add_suppression_escalation_lookup_index.exs
  - test/mailglass/suppression/escalation_test.exs
  - guides/suppression.md
autonomous: true
requirements: [SUPP-02]

must_haves:
  truths:
    - Soft-bounce escalation never evaluates synchronously in the webhook request cycle
    - Five `:deferred` events in seven days enqueue and execute an Oban-backed escalation path that inserts a distinguishable suppression row
    - The escalation action is configurable only as `:hard_suppress` or `{:suppress_for, days: pos_integer()}`
    - Operators can read one guide that explains the anchor-to-now sliding window and the permanence/removal policy that escalation interacts with
  artifacts:
    - path: lib/mailglass/suppression/escalation.ex
      provides: Oban worker, threshold evaluation, and escalation suppression insert logic
    - path: lib/mailglass/optional_deps/oban.ex
      provides: optional-dependency enqueue/availability path reused by suppression escalation
    - path: priv/repo/migrations/20260428120000_add_suppression_escalation_lookup_index.exs
      provides: covering index for tenant/address/deferred/window lookup
    - path: guides/suppression.md
      provides: operator-facing escalation, permanence, and removal semantics
  key_links:
    - from: lib/mailglass/webhook/ingest.ex
      to: lib/mailglass/suppression/escalation.ex
      via: async enqueue step for `:deferred` events only, never inline threshold evaluation
    - from: lib/mailglass/suppression/escalation.ex
      to: lib/mailglass/optional_deps/oban.ex
      via: optional-dependency gateway and boot-time warning path
    - from: lib/mailglass/suppression/escalation.ex
      to: priv/repo/migrations/20260428120000_add_suppression_escalation_lookup_index.exs
      via: indexed tenant/address/occurred_at lookup used by the 5-in-7 query
---

<objective>
Add asynchronous soft-bounce escalation without polluting the request path.

Purpose: Deferred-event history requires a windowed query and a durable write, so this plan moves that work behind the existing optional Oban boundary and documents the operator-facing policy in one place.
Output: escalation worker, async enqueue hook, lookup index migration, tests, and `guides/suppression.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
@.planning/phases/12-auto-suppression-soft-bounce-escalation/RESEARCH.md
@lib/mailglass/optional_deps/oban.ex
@lib/mailglass/webhook/ingest.ex
@lib/mailglass/webhook/providers/postmark.ex
@lib/mailglass/webhook/providers/sendgrid.ex
@lib/mailglass/events/reconciler.ex
@test/mailglass/properties/webhook_idempotency_convergence_test.exs

<interfaces>
From lib/mailglass/webhook/providers/sendgrid.ex and postmark.ex:
```elixir
:deferred
```

From lib/mailglass/optional_deps/oban.ex:
```elixir
available?()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Oban-backed soft-bounce escalation and async enqueueing</name>
  <read_first>
    - lib/mailglass/optional_deps/oban.ex
    - lib/mailglass/webhook/ingest.ex
    - lib/mailglass/webhook/providers/postmark.ex
    - lib/mailglass/webhook/providers/sendgrid.ex
    - .planning/phases/12-auto-suppression-soft-bounce-escalation/12-01-PLAN.md
  </read_first>
  <files>lib/mailglass/suppression/escalation.ex, lib/mailglass/optional_deps/oban.ex, lib/mailglass/webhook/ingest.ex, test/mailglass/suppression/escalation_test.exs</files>
  <behavior>
    - Only `:deferred` events enqueue escalation work per D-12-10
    - Default policy is `{count: 5, window_days: 7, action: :hard_suppress}` per D-12-11
    - Narrow config accepts only `:hard_suppress` or `{:suppress_for, days: pos_integer()}` per D-12-12
    - Escalation rows remain distinguishable from true hard-bounce rows through source and metadata per D-12-15
  </behavior>
  <action>Create `Mailglass.Suppression.Escalation` as the worker and policy evaluator for SUPP-02, using the existing optional-dependency patterns instead of direct unconditional `Oban.*` calls. Extend `Mailglass.Webhook.Ingest` with one async enqueue path for normalized `:deferred` events only; do not count or query historical events inside the webhook transaction, and do not reinterpret hard `:bounced` events as soft. Implement the policy surface exactly as D-12-11 and D-12-12 require, stamp escalation-derived suppressions with a distinct source plus metadata describing `count`, `window_days`, and chosen `action`, and emit a single boot-time `Logger.warning` through the optional-dep path when Oban is unavailable. Add `test/mailglass/suppression/escalation_test.exs` covering no-inline-evaluation, the 5-in-7 threshold, temporary suppression mode, and provenance markers.</action>
  <acceptance_criteria>
    - `rg -n ':deferred' lib/mailglass/webhook/ingest.ex lib/mailglass/suppression/escalation.ex` shows deferred-only enqueue/evaluation logic
    - `rg -n 'hard_suppress|suppress_for' lib/mailglass/suppression/escalation.ex` shows the narrow action set and no generic callback hooks
    - `rg -n 'Logger.warning|available\\?' lib/mailglass/optional_deps/oban.ex lib/mailglass/suppression/escalation.ex` shows the optional-dependency warning path
    - `mix test --warnings-as-errors test/mailglass/suppression/escalation_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mailglass/suppression/escalation_test.exs -x</automated>
  </verify>
  <done>Soft-bounce escalation is fully async, deferred-only, narrowly configurable, and distinguishable from true hard-bounce suppression rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the lookup index and publish operator-facing suppression guidance</name>
  <read_first>
    - priv/repo/migrations
    - guides/unsubscribe.md
    - .planning/ROADMAP.md
    - .planning/phases/12-auto-suppression-soft-bounce-escalation/12-CONTEXT.md
  </read_first>
  <files>priv/repo/migrations/20260428120000_add_suppression_escalation_lookup_index.exs, guides/suppression.md, test/mailglass/suppression/escalation_test.exs</files>
  <behavior>
    - The new index covers tenant-scoped deferred-event window scans used by the worker
    - The guide states anchor-to-now sliding-window semantics exactly
    - The guide documents both escalation policy and the non-removable `:complaint`/`:unsubscribe` removal contract
  </behavior>
  <action>Add `priv/repo/migrations/20260428120000_add_suppression_escalation_lookup_index.exs` with a covering index that matches the worker's tenant/address/occurred_at deferred-event lookup. Create or update `guides/suppression.md` with the exact default rule `5 deferred events within 7 days`, the allowed override surface, the fact that evaluation is async via Oban, the meaning of escalation-derived provenance, and the permanence/removal policy for `:complaint` and `:unsubscribe` rows per D-12-16 through D-12-19. Extend `test/mailglass/suppression/escalation_test.exs` with at least one assertion that the documented rule names and configuration keys match the implemented worker contract so the guide cannot drift silently.</action>
  <acceptance_criteria>
    - `rg -n 'deferred|tenant_id|occurred_at' priv/repo/migrations/20260428120000_add_suppression_escalation_lookup_index.exs` returns the indexed columns
    - `rg -n '5 deferred events within 7 days|hard_suppress|suppress_for|complaint|unsubscribe' guides/suppression.md` returns the documented policy text
    - `mix test --warnings-as-errors test/mailglass/suppression/escalation_test.exs -x` passes after the migration and guide contract assertions are added
  </acceptance_criteria>
  <verify>
    <automated>mix test --warnings-as-errors test/mailglass/suppression/escalation_test.exs -x</automated>
  </verify>
  <done>The worker's lookup path is indexed, and one canonical guide documents escalation plus permanence/removal semantics without policy drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook transaction -> background worker | Deferred-event evidence crosses from request-path append to async threshold evaluation |
| Worker query -> suppression write | Historical event counts inform durable suppression rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Denial of Service | `lib/mailglass/webhook/ingest.ex` | mitigate | Prohibit synchronous threshold evaluation and enqueue only async work for `:deferred` events |
| T-12-05 | Tampering | `lib/mailglass/suppression/escalation.ex` | mitigate | Restrict actions to `:hard_suppress` or `{:suppress_for, days: pos_integer()}` and store provenance metadata for every escalation row |
| T-12-06 | Information Disclosure | `guides/suppression.md` | mitigate | Document only tenant-safe, non-PII operator guidance and preserve source/provenance semantics for audits |
</threat_model>

<verification>
Run `mix test --warnings-as-errors test/mailglass/suppression/escalation_test.exs -x`.
</verification>

<success_criteria>
SUPP-02 is satisfied with an indexed, Oban-gated, deferred-only escalation flow that stays off the request path and exposes one documented operator contract.
</success_criteria>

<output>
After completion, create `.planning/phases/12-auto-suppression-soft-bounce-escalation/12-02-SUMMARY.md`
</output>
