---
phase: 100-preview-surface
plan: 03
type: execute
wave: 3
depends_on: ["100-02"]
files_modified:
  - "mailglass_admin/e2e/structural.spec.js"
  - "mailglass_admin/scripts/ui-audit.sh"
  - ".planning/RATCHET-GAP-REGISTER.md"
autonomous: true
requirements: [PAGE-03]
must_haves:
  truths:
    - "D-16: Browser coverage validates Preview light/dark chrome at 390, 768, and 1440 with one h1, mobile navigation, header controls, assigns form, tabs, pane, focus, touch targets, and WCAG-AA contrast."
    - "D-17: The 18-cell audit script no longer documents Preview dark mode as absent and captures Preview dark cells through `?theme=dark`."
    - "GAP-02 and GAP-03 are fixed only after automated Preview ExUnit, browser, and full preview verification pass."
  artifacts:
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "Focused Preview structural/a11y/responsive/theme matrix"
    - path: "mailglass_admin/scripts/ui-audit.sh"
      provides: "Preview dark audit capture contract"
    - path: ".planning/RATCHET-GAP-REGISTER.md"
      provides: "GAP-02/GAP-03 closure evidence for Phase 100"
  key_links:
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "/dev/mail/MailglassAdmin.Fixtures.HappyMailer/welcome_default"
      via: "direct fixture scenario URL"
      pattern: "HappyMailer/welcome_default"
    - from: "mailglass_admin/scripts/ui-audit.sh"
      to: "/dev/mail/?theme=dark"
      via: "Preview dark PNG capture"
      pattern: "preview-${vp}-dark"
    - from: ".planning/RATCHET-GAP-REGISTER.md"
      to: "mailglass_admin/e2e/structural.spec.js"
      via: "GAP-02/GAP-03 fixed status backed by automated Preview matrix"
      pattern: "GAP-03"
---

<objective>
Prove the Preview surface uplift end-to-end and close the Preview ratchet gaps.

Purpose: The implementation is not done until the structural layer validates the real preview-a-message-before-send flow in both themes and the audit script/gap register stop encoding the old dark-mode absence.
Output: Preview Playwright matrix, updated audit capture wording/URLs, full phase gates, and GAP-02/GAP-03 fixed rows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/RATCHET-GAP-REGISTER.md
@.planning/research/v1.11/SUMMARY.md
@.planning/phases/100-preview-surface/100-CONTEXT.md
@.planning/phases/100-preview-surface/100-RESEARCH.md
@.planning/phases/100-preview-surface/100-VALIDATION.md
@.planning/phases/100-preview-surface/100-PATTERNS.md
@.planning/phases/100-preview-surface/100-UI-SPEC.md
@.planning/phases/99-inbound-surface/99-04-SUMMARY.md
@.planning/phases/99-inbound-surface/99-05-SUMMARY.md
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/scripts/ui-audit.sh
@mailglass_admin/package.json
@mailglass_admin/mix.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend structural browser coverage for real Preview flows</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/package.json
    @.planning/phases/100-preview-surface/100-CONTEXT.md
    @.planning/phases/100-preview-surface/100-RESEARCH.md
    @.planning/phases/100-preview-surface/100-VALIDATION.md
    @.planning/phases/100-preview-surface/100-PATTERNS.md
    @.planning/phases/100-preview-surface/100-UI-SPEC.md
    @.planning/phases/99-inbound-surface/99-04-SUMMARY.md
    @.planning/phases/99-inbound-surface/99-05-SUMMARY.md
  </read_first>
  <behavior>
    - A real fixture scenario URL, `/dev/mail/MailglassAdmin.Fixtures.HappyMailer/welcome_default`, is used for Preview JTBD assertions.
    - The Preview matrix covers explicit light and dark admin themes at 390, 768, and 1440.
    - Browser proof asserts mobile Mailables navigation, exactly one h1, header controls, assigns form, tab strip, preview pane, focus rings, 44px controls, and WCAG-AA text/non-text contrast.
    - Browser proof asserts admin chrome theme and preview-frame theme toggle independently.
  </behavior>
  <action>Implement D-15 and D-16 in the existing `operator_browser_gate` lane, without adding a new harness. In `structural.spec.js`, rename the current `openPreview(page)` helper to `openPreviewEmpty(page)` and keep it pointed at `/ops/browser-preview-empty`. Add `openPreviewIndex(page, query = "")` that navigates to `/dev/mail/${query ? "?" + query : ""}` and expects `preview-shell`. Add `openPreviewScenario(page, query = "theme=light")` that navigates to `/dev/mail/MailglassAdmin.Fixtures.HappyMailer/welcome_default${query ? "?" + query : ""}` and expects `preview-shell`, `preview-header-controls`, `preview-assigns-form`, `preview-tab-strip`, and `preview-pane`. Add Preview tests named with the `Preview:` prefix for: explicit light/dark index and scenario themes; 390px mobile Mailables navigation with a scenario link reachable from `preview-mobile-mailables`; exactly one h1 on start, empty, and scenario branches; independent admin/frame theme toggles using `preview-admin-theme-toggle`, `preview-frame-theme-toggle`, `preview-shell`, and `preview-pane`; and a WCAG-AA contrast matrix over themes `[{ name: "light", query: "theme=light", expectedTheme: "mailglass-light" }, { name: "dark", query: "theme=dark", expectedTheme: "mailglass-dark" }]` and viewports `390`, `768`, `1440`. Reuse existing helpers `assertTextContrastAA`, `assertNonTextContrastAA`, `parseGridColumns`, and `expectRatio`; do not add screenshot diffing or a new Playwright config. Update existing generic Preview touch, focus, reduced-motion, and accent tests to call `openPreviewEmpty` or the new scenario helper by name.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build &amp;&amp; npx playwright test --config=playwright.config.cjs --workers=1 e2e/structural.spec.js --grep "Preview"</automated>
  </verify>
  <acceptance_criteria>
    - `structural.spec.js` contains `async function openPreviewEmpty`.
    - `structural.spec.js` contains `async function openPreviewIndex`.
    - `structural.spec.js` contains `async function openPreviewScenario`.
    - `structural.spec.js` contains `/dev/mail/MailglassAdmin.Fixtures.HappyMailer/welcome_default`.
    - `structural.spec.js` contains `preview-admin-theme-toggle`.
    - `structural.spec.js` contains `preview-frame-theme-toggle`.
    - `structural.spec.js` contains `preview-mobile-mailables`.
    - `structural.spec.js` contains `toHaveCount(1)` in a Preview h1 assertion.
    - The automated Playwright command exits 0 with `--workers=1`.
  </acceptance_criteria>
  <done>Preview structural browser coverage validates the real Preview JTBD flow across both themes and required viewport widths.</done>
</task>

<task type="auto">
  <name>Task 2: Update Preview audit capture contract</name>
  <files>mailglass_admin/scripts/ui-audit.sh</files>
  <read_first>
    @mailglass_admin/scripts/ui-audit.sh
    @.planning/phases/100-preview-surface/100-CONTEXT.md
    @.planning/phases/100-preview-surface/100-RESEARCH.md
    @.planning/phases/100-preview-surface/100-UI-SPEC.md
    @.planning/research/v1.11/SUMMARY.md
  </read_first>
  <action>Implement D-17. In `ui-audit.sh`, replace the stale Preview comment block that says Preview has no effective theme param. The new comment must state that Preview admin chrome uses the same `?theme=dark|light` contract as Operator and Inbound, and that the previewed Message/frame theme remains independent from the admin chrome. In the Preview loop, capture light cells with `$BASE/dev/mail/?theme=light` and dark cells with `$BASE/dev/mail/?theme=dark`; keep output names exactly `preview-${vp}-light` and `preview-${vp}-dark`. Do not add pixel-diff logic, commit PNGs, or change deliveries/inbound capture behavior.</action>
  <verify>
    <automated>bash -n mailglass_admin/scripts/ui-audit.sh</automated>
    <automated>bash -lc "! rg -n 'Preview has no theme param|theme param has no effect|absence if the surface ever gains dark support' mailglass_admin/scripts/ui-audit.sh"</automated>
  </verify>
  <acceptance_criteria>
    - `ui-audit.sh` contains `$BASE/dev/mail/?theme=light`.
    - `ui-audit.sh` contains `$BASE/dev/mail/?theme=dark`.
    - `ui-audit.sh` still contains `preview-${vp}-light`.
    - `ui-audit.sh` still contains `preview-${vp}-dark`.
    - `ui-audit.sh` does not contain `Preview has no theme param`.
    - `ui-audit.sh` does not contain `theme param has no effect`.
    - `bash -n mailglass_admin/scripts/ui-audit.sh` exits 0.
  </acceptance_criteria>
  <done>The 18-cell audit script now documents and captures Preview dark chrome through the Phase 100 URL contract.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Preview gates and close GAP-02/GAP-03</name>
  <files>.planning/RATCHET-GAP-REGISTER.md</files>
  <read_first>
    @.planning/RATCHET-GAP-REGISTER.md
    @.planning/phases/100-preview-surface/100-CONTEXT.md
    @.planning/phases/100-preview-surface/100-VALIDATION.md
    @mailglass_admin/e2e/structural.spec.js
    @mailglass_admin/scripts/ui-audit.sh
    @mailglass_admin/mix.exs
    @mailglass_admin/package.json
  </read_first>
  <action>After Tasks 1-2 are green, run the final Phase 100 gates in increasing cost order: focused Preview ExUnit, focused Preview Playwright, full `mix verify.preview`, full serialized browser gate, and bundle cleanliness. Only after those commands pass, update `.planning/RATCHET-GAP-REGISTER.md` rows `GAP-02` and `GAP-03`: set `status` to `fixed`, keep `first_seen_run` unchanged, set `run_id` to `2026-06-15-phase-100`, and update each fix sketch to cite Phase 100. `GAP-02` sketch must mention focusable `Preview the first Mailable` / `Read preview setup` actions plus the structural Preview matrix; `GAP-03` sketch must mention URL-driven Preview chrome dark mode and `preview-*-dark` capture via `?theme=dark`. Do not modify unrelated GAP rows.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build &amp;&amp; npx playwright test --config=playwright.config.cjs --workers=1 e2e/structural.spec.js --grep "Preview"</automated>
    <automated>cd mailglass_admin &amp;&amp; mix verify.preview</automated>
    <automated>cd mailglass_admin &amp;&amp; npm run test:operator-browser</automated>
    <automated>cd mailglass_admin &amp;&amp; git diff --exit-code priv/static/</automated>
    <automated>bash -lc "rg -n '^\\| GAP-02 .*\\| fixed \\| 2026-06-15-phase-100 \\|' .planning/RATCHET-GAP-REGISTER.md &amp;&amp; rg -n '^\\| GAP-03 .*\\| fixed \\| 2026-06-15-phase-100 \\|' .planning/RATCHET-GAP-REGISTER.md"</automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors` exits 0.
    - `cd mailglass_admin && mix mailglass_admin.assets.build && npx playwright test --config=playwright.config.cjs --workers=1 e2e/structural.spec.js --grep "Preview"` exits 0.
    - `cd mailglass_admin && mix verify.preview` exits 0.
    - `cd mailglass_admin && npm run test:operator-browser` exits 0.
    - `cd mailglass_admin && git diff --exit-code priv/static/` exits 0.
    - `RATCHET-GAP-REGISTER.md` row `GAP-02` has `status` fixed and `run_id` `2026-06-15-phase-100`.
    - `RATCHET-GAP-REGISTER.md` row `GAP-03` has `status` fixed and `run_id` `2026-06-15-phase-100`.
    - No `GAP-01`, `GAP-04`, `GAP-05`, `GAP-06`, `GAP-07`, `GAP-08`, or `GAP-09` status is changed by this task.
  </acceptance_criteria>
  <done>Preview automated gates pass and the two Preview sev-3 ratchet gaps are closed with Phase 100 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser tests -&gt; dev Preview route | Playwright exercises dev-only `/dev/mail` and synthetic fixtures. |
| Audit script -&gt; local browser capture | Local agent-browser capture writes gitignored PNG evidence. |
| Verification evidence -&gt; gap register | Automated gate results justify changing stable GAP row status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-11 | Repudiation | `structural.spec.js` Preview assertions | mitigate | Use named `Preview:` tests, stable `data-testid` hooks, and direct fixture URLs so pass/fail output records the JTBD path. |
| T-100-12 | Information Disclosure | browser fixture Preview route | mitigate | Use existing `MailglassAdmin.Fixtures.HappyMailer` synthetic fixtures; do not call `Mailglass.Outbound.deliver/2` or production send paths. |
| T-100-13 | Tampering | `ui-audit.sh` PNG capture | mitigate | Keep PNGs under gitignored `tmp/ui-audit`; update URLs/comments only, no committed binary evidence. |
| T-100-14 | Tampering | `.planning/RATCHET-GAP-REGISTER.md` | mitigate | Change only `GAP-02` and `GAP-03` after automated gates pass; acceptance criteria forbid unrelated row status changes. |
| T-100-15 | Denial of Service | browser gate reset state | mitigate | Run focused structural command and existing `npm run test:operator-browser` with serialized `--workers=1` per Phase 99 decision. |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install or new dependency is planned; Playwright dependency already exists. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix mailglass_admin.assets.build && npx playwright test --config=playwright.config.cjs --workers=1 e2e/structural.spec.js --grep "Preview"`
- `bash -n mailglass_admin/scripts/ui-audit.sh`
- `cd mailglass_admin && mix verify.preview`
- `cd mailglass_admin && npm run test:operator-browser`
- `cd mailglass_admin && git diff --exit-code priv/static/`
</verification>

<success_criteria>
Preview has machine-checked responsive, theme, focus, touch, contrast, JTBD, and audit-capture proof in both themes, and GAP-02/GAP-03 are closed with stable Phase 100 evidence.
</success_criteria>

<output>
Create `.planning/phases/100-preview-surface/100-03-SUMMARY.md` when done.
</output>
