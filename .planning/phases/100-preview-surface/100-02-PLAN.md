---
phase: 100-preview-surface
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - "mailglass_admin/lib/mailglass_admin/preview_live.ex"
  - "mailglass_admin/lib/mailglass_admin/preview/sidebar.ex"
  - "mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex"
  - "mailglass_admin/lib/mailglass_admin/preview/tabs.ex"
  - "mailglass_admin/priv/static/app.css"
autonomous: true
requirements: [PAGE-03]
must_haves:
  truths:
    - "D-08: Mailables/scenario navigation is reachable at 390px without adding a route or replacing native details/summary semantics."
    - "D-09/UI-SPEC: Preview emits stable kebab-case group hooks for shell, mobile Mailables, start, empty, header controls, assigns form, tab strip, and preview pane."
    - "D-10/D-11/D-12/D-13: Preview page groups use token rhythm, locked Preview copy, one focusable CTA branch, and 44px action controls."
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      provides: "Responsive Preview shell, mobile navigation placement, header controls, start/empty/show groups"
    - path: "mailglass_admin/lib/mailglass_admin/preview/sidebar.ex"
      provides: "Reusable Mailables navigation with non-h1 heading and native details/summary hierarchy"
    - path: "mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex"
      provides: "Assigns form test hook and 44px action buttons"
    - path: "mailglass_admin/lib/mailglass_admin/preview/tabs.ex"
      provides: "Tab strip and preview pane hooks"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Committed generated CSS bundle after HEEx class changes"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/preview/sidebar.ex"
      via: "desktop rail and mobile Mailables section"
      pattern: "preview-mobile-mailables"
    - from: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex"
      via: "`AssignsForm.assigns_form/1` inside `preview-assigns-form` group"
      pattern: "preview-assigns-form"
    - from: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      to: "mailglass_admin/lib/mailglass_admin/preview/tabs.ex"
      via: "`Tabs.tabs/1` exposes `preview-tab-strip` and `preview-pane`"
      pattern: "preview-pane"
---

<objective>
Compose the settled Preview components into the Phase 100 page-level IA and responsive group rhythm.

Purpose: PAGE-03 includes more than theme parsing; the Preview surface must be usable at 390/768/1440, expose machine-checkable group hooks, and close GAP-02's keyboard-actionable start/empty branch.
Output: Mobile-reachable Mailables navigation, exact Preview copy, grouped hooks, 44px controls, and rebuilt committed CSS.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/RATCHET-GAP-REGISTER.md
@.planning/research/v1.11/SUMMARY.md
@.planning/phases/100-preview-surface/100-CONTEXT.md
@.planning/phases/100-preview-surface/100-RESEARCH.md
@.planning/phases/100-preview-surface/100-VALIDATION.md
@.planning/phases/100-preview-surface/100-PATTERNS.md
@.planning/phases/100-preview-surface/100-UI-SPEC.md
@.planning/phases/97-cross-surface-component-layer/97-05-SUMMARY.md
@.planning/phases/98-operator-deliveries-surface/98-02-SUMMARY.md
@mailglass_admin/lib/mailglass_admin/preview_live.ex
@mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
@mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
@mailglass_admin/lib/mailglass_admin/preview/tabs.ex
@mailglass_admin/assets/css/app.css
@mailglass_admin/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive Preview shell groups and mobile Mailables navigation</name>
  <files>mailglass_admin/lib/mailglass_admin/preview_live.ex, mailglass_admin/lib/mailglass_admin/preview/sidebar.ex</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/preview_live.ex
    @mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
    @.planning/phases/100-preview-surface/100-CONTEXT.md
    @.planning/phases/100-preview-surface/100-RESEARCH.md
    @.planning/phases/100-preview-surface/100-PATTERNS.md
    @.planning/phases/100-preview-surface/100-UI-SPEC.md
    @.planning/research/v1.11/SUMMARY.md
    @.planning/phases/98-operator-deliveries-surface/98-02-SUMMARY.md
  </read_first>
  <action>Implement D-02, D-07, D-08, D-09, D-10, IA-LD-07, and IA-LD-08 in the render tree. Treat the Phase 97 `DeviceFrame`, `Tabs`, `Sidebar`, and `orientation_strip` work as settled; compose those components instead of re-uplifting them from scratch. In `PreviewLive.render/1`, keep the outer `preview-shell` wrapper from Plan 01 and change the page layout to token rhythm: outer classes include `min-h-screen bg-base-100 text-base-content px-md py-lg md:px-lg md:py-xl`; the child layout uses `grid gap-lg md:grid-cols-[20rem_1fr]`. Replace the current `hidden md:block` aside with a desktop rail carrying `data-testid="preview-sidebar-desktop"` and classes `hidden md:block rounded-box border border-base-300 bg-base-200 p-md`. Add a mobile section before main content with `data-testid="preview-mobile-mailables"` and classes `md:hidden rounded-box border border-base-300 bg-base-200 p-md`; its visible trigger/heading text is `Mailables`, and it reuses `Sidebar.sidebar/1` so the mailable-to-scenario hierarchy remains native details/summary. In `Sidebar.sidebar/1`, change the visible heading from `Mailers` to `Mailables`, render it as `h2` with `text-body font-bold text-base-content tracking-tight`, and preserve the existing inner `details`, `summary`, `min-h-11`, focus-ring, relative link, and `border-l-2` behavior. Keep scenario links relative; do not introduce a new route, socket assign for mobile disclosure, or custom client hook.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `preview_live.ex` contains `data-testid="preview-mobile-mailables"`.
    - `preview_live.ex` contains `data-testid="preview-sidebar-desktop"`.
    - `preview_live.ex` contains `md:grid-cols-[20rem_1fr]`.
    - `preview_live.ex` contains `px-md py-lg md:px-lg md:py-xl`.
    - `sidebar.ex` contains `<h2` and the text `Mailables`.
    - `sidebar.ex` still contains `<details` and `<summary`.
    - `sidebar.ex` still contains `patch={scenario_path`.
    - No new route file is modified by this task.
    - The automated ExUnit command exits 0.
  </acceptance_criteria>
  <done>The Preview Mailables/scenario navigation is reachable at 390px and remains the native Sidebar hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Apply locked Preview copy, group hooks, and 44px controls</name>
  <files>mailglass_admin/lib/mailglass_admin/preview_live.ex, mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex, mailglass_admin/lib/mailglass_admin/preview/tabs.ex</files>
  <read_first>
    @mailglass_admin/lib/mailglass_admin/preview_live.ex
    @mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
    @mailglass_admin/lib/mailglass_admin/preview/tabs.ex
    @.planning/phases/100-preview-surface/100-CONTEXT.md
    @.planning/phases/100-preview-surface/100-RESEARCH.md
    @.planning/phases/100-preview-surface/100-PATTERNS.md
    @.planning/phases/100-preview-surface/100-UI-SPEC.md
    @.planning/research/v1.11/SUMMARY.md
  </read_first>
  <action>Implement D-09, D-11, D-12, D-13, D-14, COPY-LD-04, COPY-LD-05, COPY-LD-06, COPY-LD-08, and GAP-02. In the selected scenario branch, wrap the controls in `data-testid="preview-header-controls"` and expose two separate icon buttons: `data-testid="preview-admin-theme-toggle"` with `phx-click="toggle_theme"` and aria labels `Switch admin chrome to dark theme` / `Switch admin chrome to light theme`; `data-testid="preview-frame-theme-toggle"` with `phx-click="toggle_preview_frame_theme"` and aria labels `Switch preview frame to dark chrome` / `Switch preview frame to light chrome`. Both buttons use `btn btn-ghost btn-sm btn-square min-h-11 focus-visible:ring-2 focus-visible:ring-primary`. In the start branch, add `data-testid="preview-start"`, use an `h1` with exact text `Render a real Message before you send it`, exact body `Pick a Mailable from the sidebar to render it through the same pipeline your production sends use.`, and exact CTA `Preview the first Mailable`; keep the CTA as a focusable `.link` only when `first_previewable(@mailables)` returns a target. In the no-Mailables branch, render exact heading `No Mailables discovered`, exact body `Preview scans loaded modules that use Mailglass.Mailable. Nothing was found yet.`, keep the `Read preview setup` focusable action, and do not render a bogus first-Mailable link. Keep explicit loading UI out of scope because Preview discovery/rendering remains synchronous in this phase. In `AssignsForm.assigns_form/1`, add `data-testid="preview-assigns-form"` to the form and replace both action button classes with `btn btn-primary min-h-11 px-5` and `btn btn-ghost min-h-11 px-5` respectively. In `Tabs.tabs/1`, add `data-testid="preview-tab-strip"` to the tablist and `data-testid="preview-pane"` to the active tab panel wrapper. Preserve tab ARIA from Phase 97.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `preview_live.ex` contains `data-testid="preview-header-controls"`.
    - `preview_live.ex` contains `data-testid="preview-admin-theme-toggle"` and `phx-click="toggle_theme"`.
    - `preview_live.ex` contains `data-testid="preview-frame-theme-toggle"` and `phx-click="toggle_preview_frame_theme"`.
    - `preview_live.ex` contains exact text `Render a real Message before you send it`.
    - `preview_live.ex` contains exact text `Pick a Mailable from the sidebar to render it through the same pipeline your production sends use.`
    - `preview_live.ex` contains exact text `Preview the first Mailable`.
    - `preview_live.ex` contains exact text `No Mailables discovered`.
    - `preview_live.ex` does not contain `Preview the first one`, `Pick a mailer`, or `No mailables discovered`.
    - `assigns_form.ex` contains `data-testid="preview-assigns-form"`.
    - `assigns_form.ex` action buttons do not contain `btn-sm`.
    - `tabs.ex` contains `data-testid="preview-tab-strip"` and `data-testid="preview-pane"`.
    - The automated ExUnit command exits 0.
  </acceptance_criteria>
  <done>Preview start, empty, header, assigns, and tab/pane groups have exact locked copy, hooks, and touch-target floors.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild committed CSS after Preview class changes</name>
  <files>mailglass_admin/priv/static/app.css</files>
  <read_first>
    @mailglass_admin/priv/static/app.css
    @mailglass_admin/assets/css/app.css
    @mailglass_admin/mix.exs
    @.planning/phases/100-preview-surface/100-VALIDATION.md
    @.planning/phases/99-inbound-surface/99-05-SUMMARY.md
  </read_first>
  <action>Implement D-18 after Tasks 1-2 class changes. From `mailglass_admin`, run the standalone Tailwind asset build with `mix mailglass_admin.assets.build`. Include the regenerated `mailglass_admin/priv/static/app.css` diff if the build changes it. Do not edit `mailglass_admin/assets/css/app.css`, `brandbook/tokens.css`, package manifests, or CI configuration in this plan. After committing the generated bundle, prove the generated asset tree is clean with `git diff --exit-code priv/static/` from `mailglass_admin`.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build</automated>
    <automated>cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin &amp;&amp; git diff --exit-code priv/static/</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_admin/priv/static/app.css` is rebuilt after Preview HEEx class changes.
    - `cd mailglass_admin && mix mailglass_admin.assets.build` exits 0.
    - `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors` exits 0.
    - `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after generated CSS is committed.
    - `mailglass_admin/assets/css/app.css` is not modified.
    - `brandbook/tokens.css` is not modified.
  </acceptance_criteria>
  <done>The committed static CSS bundle reflects the Preview page/group class changes and is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview navigation DOM -&gt; LiveView patch URLs | Sidebar and CTA links emit route state back to PreviewLive. |
| Mobile disclosure/navigation -&gt; page heading structure | Reused Sidebar content can duplicate headings if not demoted. |
| Assigns form -&gt; renderer | Existing form inputs update scenario assigns and trigger rendering. |
| HEEx class changes -&gt; generated CSS | Tailwind scans source classes and writes committed static CSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-06 | Information Disclosure | Preview navigation route scope | mitigate | Reuse existing `/dev/mail` relative links only; do not add production routes or public APIs per D-01. |
| T-100-07 | Denial of Service | mobile Sidebar duplication | mitigate | Reuse existing Sidebar data and links; no extra discovery call, no new socket event, and no custom client hook. |
| T-100-08 | Repudiation | a11y/source hooks | mitigate | Add stable `data-testid` hooks named in D-09/UI-SPEC so structural tests can prove the page shape. |
| T-100-09 | Tampering | assigns form buttons | accept | Existing `assigns_changed` safe-key coercion remains unchanged; this plan changes classes/test hooks only. |
| T-100-10 | Repudiation | generated CSS bundle | mitigate | Run `mix mailglass_admin.assets.build` and require `git diff --exit-code priv/static/` after committing generated output. |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install or new dependency is planned. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/preview_live_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix mailglass_admin.assets.build`
- `cd mailglass_admin && git diff --exit-code priv/static/`
</verification>

<success_criteria>
The Preview surface has mobile-reachable Mailables navigation, exact locked start/empty copy, stable structural hooks, 44px controls, one-h1-ready heading structure, and a clean rebuilt CSS bundle.
</success_criteria>

<output>
Create `.planning/phases/100-preview-surface/100-02-SUMMARY.md` when done.
</output>
