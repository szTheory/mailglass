---
phase: 148-release-and-adoption-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 148-01
files_modified:
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - test/scripts/release_trigger_recovery_test.exs
  - test/scripts/linked_release_concurrency_test.exs
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "Release Please changes core/admin to 2.4.0 without mutating inbound-owned version/docs artifacts when inbound stays 2.1.1 (D-01)."
    - "The protected prepublish run emits a credential-free proof artifact while retaining every existing release-SHA CI, tag, environment, idempotency, and concurrency guard (D-02, D-03)."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Manifest-diff-gated sibling pin synchronization"
      contains: "mailglass_inbound"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Protected release proof artifact emission"
      contains: "phase-148-release-proof"
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: ".release-please-manifest.json"
      via: "compare inbound manifest value on the release branch with origin/main before inbound-owned edits"
      pattern: "INBOUND.*CHANGED"
    - from: ".github/workflows/publish-hex.yml"
      to: "tmp/release-proof/phase-148.json"
      via: "prepublish-summary writes bounded command/SHA/version outcomes and uploads the artifact"
      pattern: "phase-148-release-proof"
  assumptions:
    - "FLAGGED UNRESOLVED (REL-01): the spec-less edge probe did not classify release-PR synchronization edges; workflow-contract tests must fail loud if inbound-owned paths re-enter a core/admin-only PR."
  prohibitions:
    - "MUST NOT represent a local-path consumer pass as proof that the published packages adopters install work."
    - "MUST NOT represent the Mailglass 2.4.0 release as satisfying the separately recorded external B2C production launch gates."
---

<objective>
Make the Release Please PR and protected prepublish evidence obey the independently versioned inbound boundary (D-01, D-02, D-03).

Purpose: Ensure the release branch, not just the final publish job, leaves inbound 2.1.1 unchanged and produces reviewable proof before publication.
Output: Tested release-PR sync predicates and a sanitized protected-workflow artifact.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
@.planning/phases/148-release-and-adoption-proof/148-RESEARCH.md
@.planning/phases/148-release-and-adoption-proof/148-PATTERNS.md
@.planning/phases/148-release-and-adoption-proof/148-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep inbound-owned files out of a core/admin-only release PR</name>
  <files>.github/workflows/release-please.yml, test/scripts/release_trigger_recovery_test.exs</files>
  <read_first>
    - .github/workflows/release-please.yml
    - test/scripts/release_trigger_recovery_test.exs
    - release-please-config.json
    - .release-please-manifest.json
    - mailglass_inbound/README.md
    - mailglass_inbound/docs/inbound-install.md
    - .planning/publish/mailglass_inbound-publish-summary.json
    - .planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
    - .planning/phases/148-release-and-adoption-proof/148-PATTERNS.md
  </read_first>
  <behavior>
    - "When only the linked core/admin manifest values change, the sync step edits core/admin README pins only and leaves all inbound-owned paths unchanged (D-01)."
    - "When the inbound manifest value changes in a standalone inbound release, the existing inbound README/install/publish-summary synchronization remains reachable."
    - "The PAT-backed release branch checkout, `prs_created` guard, bot commit, push, and auto-merge behavior remain intact (D-03)."
  </behavior>
  <action>Add a focused source-contract case to `Mailglass.Scripts.ReleaseTriggerRecoveryTest` before editing the workflow. In the sync step, compare the release branch's `mailglass_inbound` manifest value with `origin/main` and derive a single fail-closed `INBOUND_CHANGED` predicate. Apply inbound self-pin, inbound core-pin, inbound install-guide, inbound publish-summary, inbound `SYNC_PATHS`, and inbound commit wording only inside that predicate. Keep README.md and mailglass_admin/README.md core/admin pin sync unconditional for the linked release. Use existing release branch/PAT plumbing and manifest ownership; do not infer inbound's version from core or introduce another sync workflow.</action>
  <reversibility rating="reversible">The release-PR transformation is reversible before the release PR merges.</reversibility>
  <verify>
    <automated>mix test test/scripts/release_trigger_recovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The automated test proves core/admin-only manifest changes exclude all three inbound-owned paths from `git add` and substitutions.
    - The test also proves a real inbound manifest delta preserves the existing inbound synchronization branch.
    - The pinned Release Please action, PAT checkout, fixed release branch, preflight, push, and auto-merge steps remain present.
  </acceptance_criteria>
  <done>A core/admin-only Release Please PR cannot mutate inbound-owned release artifacts, while a genuine inbound release retains its synchronization path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upload sanitized protected prepublish evidence</name>
  <files>.github/workflows/publish-hex.yml, test/scripts/linked_release_concurrency_test.exs</files>
  <read_first>
    - .github/workflows/publish-hex.yml
    - test/scripts/linked_release_concurrency_test.exs
    - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
    - test/mailglass/webhook/ingest_auto_suppress_test.exs
    - test/mailglass/suppression_test.exs
    - test/mailglass/docs_contract_test.exs
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs
    - .planning/phases/148-release-and-adoption-proof/148-VALIDATION.md
  </read_first>
  <behavior>
    - "The protected prepublish job runs the exact canonical PROOF-02, PROOF-03, and Phase 147 operator test files (D-04, D-05, D-06)."
    - "The job writes `tmp/release-proof/phase-148.json` with tag/SHA, resolved versions, named commands, and outcomes only, then uploads `phase-148-release-proof-${{ github.run_id }}` for 90 days."
    - "The artifact step has no secret environment and never serializes command environments, logs containing package credentials, or recipient/message data."
  </behavior>
  <action>Extend the existing `prepublish-summary` job rather than creating another release mechanism or CI classification. Run the exact focused root suppression/docs command and the package-local operator test, writing only a small JSON manifest of the release ref/SHA, mailglass 2.4.0, mailglass_admin 2.4.0, mailglass_inbound 2.1.1, command labels, and pass/fail outcomes under `tmp/release-proof/phase-148.json`. Upload it with the already-pinned `actions/upload-artifact` SHA, name `phase-148-release-proof-${{ github.run_id }}`, `if-no-files-found: error`, and 90-day retention. Extend the workflow contract test to bind the exact canonical files and sanitized schema. Preserve the `hex-publish` environment, secret isolation, tag guards, release-SHA CI recovery, and non-cancelling linked concurrency per D-03.</action>
  <reversibility rating="reversible">Artifact generation can be removed without affecting published package contracts.</reversibility>
  <verify>
    <automated>mix test test/scripts/linked_release_concurrency_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The contract test names all four canonical test files and the exact artifact name/path/retention.
    - The JSON schema is bounded to ref, SHA, package versions, command labels, and outcomes; the upload step has no `HEX_API_KEY` binding.
    - Existing release-event guards, gate-ci-green, package idempotency, protected environments, and static concurrency tests remain green.
  </acceptance_criteria>
  <done>The protected prepublish job uploads the sanitized Phase 148 proof artifact and all release protections remain contract-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release branch → release artifacts | Automated substitutions must respect independent component ownership. |
| protected test execution → uploaded artifact | Evidence crosses into retained GitHub storage and must exclude secrets/PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-06 | Tampering | Release Please sync paths | high | mitigate | Manifest-delta gate and contract test prevent inbound-owned edits on core/admin-only releases. |
| T-148-07 | Information Disclosure | release-proof artifact | high | mitigate | Allowlisted JSON fields, no secret env on generation/upload, and contract assertions. |
| T-148-08 | Elevation | release PR credentials | high | mitigate | Preserve existing PAT checkout guards and restrict changes to the established workflow. |
| T-148-09 | Denial of Service | release-SHA CI gate | high | mitigate | Preserve the existing self-dispatch/recovery logic and gate dependency. |
| T-148-10 | Tampering | duplicate tag execution | high | mitigate | Retain static non-cancelling concurrency and idempotency guards. |
</threat_model>

## Artifacts this phase produces

- New symbols/functions: no production symbols; one new source-contract test case for inbound manifest-delta gating.
- New workflow jobs/inputs: none; existing Release Please sync and `prepublish-summary` are extended.
- New CLI/env flags: internal `INBOUND_CHANGED` shell variable only; public workflow inputs remain unchanged.
- New runtime artifacts: `phase-148-release-proof-${{ github.run_id }}` containing `tmp/release-proof/phase-148.json`.
- New repository files: none in this plan.

<verification>
Run both focused workflow-contract suites and `actionlint` through the repository's existing workflow validation command.
</verification>

<success_criteria>
A core/admin release PR leaves inbound-owned artifacts unchanged, and the protected prepublish run retains a sanitized, reviewable proof bundle without weakening any release control.
</success_criteria>

<output>
Create `.planning/phases/148-release-and-adoption-proof/148-02-SUMMARY.md` when done.
</output>
