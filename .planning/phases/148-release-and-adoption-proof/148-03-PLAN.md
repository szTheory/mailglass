---
phase: 148-release-and-adoption-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 148-02
files_modified:
  - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
autonomous: true
requirements:
  - PROOF-02
  - PROOF-03
  - REL-01
must_haves:
  truths:
    - "The exact canonical suppression, docs, and LiveView tests are green against the release candidate SHA (D-04, D-05, D-06)."
    - "The same consumer harness passes in local path mode before the protected release, without being mislabeled as published proof (D-07)."
    - "Core/admin publish checks and workflow contracts are green while inbound remains 2.1.1 and external launch gates remain separate (D-01, D-08, D-09)."
  artifacts:
    - path: ".planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md"
      provides: "Commit-bound pre-publication evidence and go/no-go inputs"
      contains: "Pre-publication evidence"
  key_links:
    - from: "canonical focused tests"
      to: ".planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md"
      via: "exact command, SHA, timestamp, exit status, and outcome recorded after execution"
      pattern: "exit status"
  assumptions:
    - "FLAGGED UNRESOLVED (PROOF-02): manual reviewer must judge whether the canonical focused test selection is complete because the spec-less probe supplied no edge taxonomy."
    - "FLAGGED UNRESOLVED (PROOF-03): manual reviewer must judge whether the docs-contract selection is complete because the spec-less probe supplied no edge taxonomy."
    - "FLAGGED UNRESOLVED (REL-01): a green local preflight cannot resolve the published-registry edge."
    - "DESCRIPTOR-LESS / FLAGGED UNVERIFIED: these transparency prohibitions have no machine-readable descriptor; a human reviewer must explicitly retain and assess them, and they must not be silently dropped."
  prohibitions:
    - "MUST NOT represent a local-path consumer pass as proof that the published packages adopters install work."
    - "MUST NOT represent the Mailglass 2.4.0 release as satisfying the separately recorded external B2C production launch gates."
---

<objective>
Produce honest, commit-bound pre-publication evidence for all Mailglass-owned proofs and release controls.

Purpose: Give the one-way release checkpoint concrete go/no-go evidence without claiming that local artifacts are published artifacts.
Output: Completed pre-publication sections in `148-RELEASE-PROOF.md`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
@.planning/phases/148-release-and-adoption-proof/148-VALIDATION.md
@.planning/phases/148-release-and-adoption-proof/148-01-SUMMARY.md
@.planning/phases/148-release-and-adoption-proof/148-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture focused behavior and path-mode adoption evidence</name>
  <files>.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md</files>
  <read_first>
    - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
    - test/mailglass/webhook/ingest_auto_suppress_test.exs
    - test/mailglass/suppression_test.exs
    - test/mailglass/docs_contract_test.exs
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs
    - scripts/consumer_install_smoke.sh
    - .planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
    - .planning/phases/148-release-and-adoption-proof/148-VALIDATION.md
  </read_first>
  <action>Run the exact PROOF-02 tests per D-04, the exact PROOF-03 docs-contract test per D-05, and the package-local operator LiveView test per D-06. Then run `scripts/consumer_install_smoke.sh` in `DEP_MODE=path` with explicit `MAILGLASS_PATH` and a fresh `mktemp -d` work directory per D-07. Record the current full commit SHA, UTC timestamp, exact commands, exit codes, test counts, and bounded outcomes in the ledger. Preserve the existing tests and script; do not create substitutes. Label path mode shift-left evidence and leave Hex mode pending.</action>
  <reversibility rating="reversible">This task records reproducible local evidence only.</reversibility>
  <verify>
    <automated>mix test test/mailglass/webhook/ingest_auto_suppress_test.exs test/mailglass/suppression_test.exs test/mailglass/docs_contract_test.exs --warnings-as-errors &amp;&amp; (cd mailglass_admin &amp;&amp; mix test test/mailglass_admin/operator_live_test.exs --warnings-as-errors) &amp;&amp; proof_work_dir=$(mktemp -d) &amp;&amp; DEP_MODE=path MAILGLASS_PATH="$PWD" WORK_DIR="$proof_work_dir" bash scripts/consumer_install_smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both automated test commands exit 0 at the SHA recorded in the ledger.
    - The ledger separately names unsubscribe, complaint, hard-bounce, B2C parse/package, live refresh, and foreign-tenant rejection outcomes.
    - The path-mode smoke entry includes its fresh work directory outcome and remains explicitly labeled non-published evidence.
  </acceptance_criteria>
  <done>Focused behavior and path-mode consumer evidence are green and recorded honestly against the candidate SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Capture release-workflow and package preflight evidence</name>
  <files>.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md</files>
  <read_first>
    - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
    - .github/workflows/publish-hex.yml
    - .github/workflows/release-please.yml
    - .github/workflows/post-publish-smoke.yml
    - release-please-config.json
    - .release-please-manifest.json
    - mix.exs
    - mailglass_admin/mix.exs
    - mailglass_inbound/mix.exs
    - .planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
  </read_first>
  <action>Run the linked-release, release-trigger-recovery, and post-publish-smoke workflow-contract tests, then run the established publish checks for `mailglass` and `mailglass_admin` only. Verify manifest/config membership is core/admin linked at target 2.4.0 and inbound independent at 2.1.1; run the repository's workflow lint and full `mix ci` gate. Record actual results and any blockers in the ledger without normalizing red/pending states to green. Add the exact structured line `ASVS L1 unresolved high-severity findings: N`, where N is the count remaining after reviewing every Phase 148 STRIDE mitigation; only zero permits Plan 04 to offer `go`. Confirm D-08 external launch gates remain recorded but excluded, and D-09 adds no Crosswake surface.</action>
  <reversibility rating="reversible">All operations are read-only checks or evidence updates before publication.</reversibility>
  <verify>
    <automated>mix test test/scripts/linked_release_concurrency_test.exs test/scripts/release_trigger_recovery_test.exs test/mailglass/publish/post_publish_smoke_contract_test.exs --warnings-as-errors &amp;&amp; mix mailglass.publish.check --package mailglass &amp;&amp; mix mailglass.publish.check --package mailglass_admin &amp;&amp; mix ci</automated>
  </verify>
  <acceptance_criteria>
    - Workflow contract and both package publish checks exit 0, and their exact results are recorded against the candidate SHA.
    - The ledger records `mix ci` and workflow-lint outcomes truthfully; any red result blocks Plan 04.
    - The ledger records `ASVS L1 unresolved high-severity findings: 0`; any nonzero or missing value blocks Plan 04.
    - The candidate version set is core/admin 2.4.0 with inbound 2.1.1, and no inbound publish check is used to make core/admin readiness pass.
  </acceptance_criteria>
  <done>All workflow, package, lint, and full-suite release gates are recorded and green before the one-way checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dirty/local workspace → release evidence | Evidence must bind to an exact commit and truthfully distinguish local state from publishable release state. |
| release candidate → protected publication | Every high-severity release control must be green before the one-way checkpoint. |

## ASVS L1 Release Gate

ASVS L1 applies at these primary boundaries: V4 Access Control is covered by protected publication prerequisites and bounded evidence handling; V5 Input Validation is covered by exact SHA/version assertions and fail-closed workflow/package checks. Every critical/high threat is dispositioned `mitigate`. High-severity findings are blocking: any unresolved high-severity finding stops this plan and all release progression.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-11 | Spoofing / Repudiation | local evidence ledger | high | mitigate | Record full SHA, exact commands, timestamps, exit codes, and pending/red states without normalization. |
| T-148-12 | Tampering | candidate package set | high | mitigate | Assert linked config/manifest versions and run core/admin publish checks only. |
| T-148-13 | Information Disclosure | captured command output | high | mitigate | Record bounded outcomes, never environments, tokens, recipient data, or message content. |
| T-148-14 | Denial of Service | missing release-SHA CI | high | mitigate | Require workflow contracts and preserve gate-ci-green self-dispatch before the checkpoint. |
</threat_model>

## Artifacts this phase produces

- New symbols/functions: none.
- New workflow jobs/inputs: none.
- New CLI/env flags: none; path smoke reuses `DEP_MODE=path`, `MAILGLASS_PATH`, and `WORK_DIR`.
- New runtime artifacts: a disposable fresh consumer directory outside the repository; it is not committed.
- New repository files: updates to `.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md` only.

<verification>
Every focused command and package preflight is green at the same candidate SHA; the ledger clearly leaves publication and Hex-mode proof pending.
</verification>

<success_criteria>
The maintainer has a complete, truthful go/no-go record for all reversible pre-release work, with no unresolved red gate hidden by local evidence.
</success_criteria>

<output>
Create `.planning/phases/148-release-and-adoption-proof/148-03-SUMMARY.md` when done.
</output>
