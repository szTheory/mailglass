---
phase: 148-release-and-adoption-proof
plan: 04
type: execute
wave: 4
depends_on:
  - 148-03
files_modified: []
autonomous: false
requirements:
  - REL-01
must_haves:
  truths:
    - "The maintainer explicitly accepts the one-way publication after reviewing the exact candidate SHA and evidence."
    - "The established Release Please → release.published → protected `hex-publish` path creates mailglass 2.4.0 and mailglass_admin 2.4.0 only; inbound stays 2.1.1 (D-01, D-02, D-03)."
  artifacts:
    - path: ".planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md"
      provides: "Go/no-go inputs reviewed before publication"
      contains: "Protected publication"
  key_links:
    - from: "maintainer approval"
      to: ".github/workflows/release-please.yml"
      via: "the existing release PR and automated release.published path only"
      pattern: "release-please"
  assumptions:
    - "FLAGGED UNRESOLVED (REL-01): publication and registry propagation remain external one-way edges until the protected runs complete."
  prohibitions:
    - "MUST NOT represent a local-path consumer pass as proof that the published packages adopters install work."
    - "MUST NOT represent the Mailglass 2.4.0 release as satisfying the separately recorded external B2C production launch gates."
---

<objective>
Pause at the one-way door, then let the maintainer authorize the established protected release path for core/admin 2.4.0 only (D-01, D-02, D-03).

Purpose: Make irreversible Hex publication explicit and prevent an executor from substituting an autonomous local command or a second release mechanism.
Output: Maintainer go/no-go and the externally running protected release ceremony.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
@.planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
@.planning/phases/148-release-and-adoption-proof/148-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Make the core/admin 2.4.0 publication go/no-go decision</name>
  <read_first>
    - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
    - .planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
    - .release-please-manifest.json
    - release-please-config.json
    - .github/workflows/publish-hex.yml
  </read_first>
  <decision>Authorize irreversible publication of mailglass 2.4.0 and mailglass_admin 2.4.0 while preserving mailglass_inbound 2.1.1.</decision>
  <context>Hex publication cannot be undone; retirement does not erase an already published package. Approval is valid only if Plan 03 records green canonical tests, workflow contracts, `mix ci`, and both core/admin publish checks at the exact candidate SHA.</context>
  <options>
    <option id="go">
      <name>Publish core/admin 2.4.0</name>
      <pros>Completes the locked release boundary using the existing protected path.</pros>
      <cons>Creates permanent external Hex versions and tags.</cons>
    </option>
    <option id="no-go">
      <name>Stop before publication</name>
      <pros>Leaves all external package state unchanged while blockers are corrected.</pros>
      <cons>REL-01 and Phase 148 remain incomplete.</cons>
    </option>
  </options>
  <action>Review the candidate SHA and every Plan 03 result in `148-RELEASE-PROOF.md`. Select `go` only when the canonical proofs, path smoke, workflow contracts, package publish checks, and full `mix ci` gate are green and the version set is exactly core/admin 2.4.0 with inbound 2.1.1. Otherwise select `no-go` and record the failing gate; this checkpoint must not trigger publication itself.</action>
  <verify>
    <automated>rg -n "mix ci.*(green|exit 0)|mailglass.*publish.check.*(green|exit 0)|mailglass_admin.*publish.check.*(green|exit 0)|2.4.0|2.1.1" .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md</automated>
    <human-check>Confirm the evidence belongs to the candidate SHA and choose `go` only if no red or unresolved Mailglass release gate remains.</human-check>
  </verify>
  <acceptance_criteria>
    - The maintainer returns `go` or `no-go` after reviewing the candidate SHA, exact version set, and all preflight results.
    - `no-go` halts the plan without triggering or approving publication.
  </acceptance_criteria>
  <done>An explicit go/no-go decision is recorded against the exact candidate SHA and version set before any irreversible publish action.</done>
  <resume-signal>Select: go or no-go.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Allow the established protected Release Please ceremony to finish</name>
  <read_first>
    - .github/workflows/release-please.yml
    - .github/workflows/publish-hex.yml
    - .github/workflows/post-publish-smoke.yml
    - .planning/phases/148-release-and-adoption-proof/148-RELEASE-PROOF.md
    - .planning/phases/148-release-and-adoption-proof/148-CONTEXT.md
  </read_first>
  <reversibility rating="one-way">Publishing packages to Hex creates permanent versions even if they are later retired.</reversibility>
  <action>After `go`, integrate the reviewed phase commits through the repository's normal protected-main process and allow the existing Release Please PR to auto-merge only after its required checks are green. Inspect that the PR changes `.release-please-manifest.json` and package versions to mailglass/mailglass_admin 2.4.0 while mailglass_inbound remains 2.1.1 and inbound-owned files are absent from the core/admin-only version delta. Do not manually run `mix hex.publish`, dispatch against `main`, create tags by hand, or introduce another release workflow. Wait for the canonical `release.published` events, `publish-hex.yml`, and `post-publish-smoke.yml`; return the release PR URL plus protected publish and smoke run URLs.</action>
  <instructions>Use the repository's protected-main/Release Please UI to approve or observe the already-tested release PR. Confirm the exact version delta before merge, then wait for the established protected workflows and copy their URLs back to the executor. No local credential or manual Hex command is required.</instructions>
  <verification>GitHub shows non-draft `mailglass-v2.4.0` and `mailglass_admin-v2.4.0` releases, protected publish and smoke runs tied to the release SHA, and no inbound 2.4.0 release.</verification>
  <verify>
    <automated>gh release view mailglass-v2.4.0 --json tagName,url,isDraft,isPrerelease &amp;&amp; gh release view mailglass_admin-v2.4.0 --json tagName,url,isDraft,isPrerelease</automated>
    <human-check>Confirm the Release Please PR and protected workflow UI show core/admin 2.4.0, inbound 2.1.1, and no inbound publication job on either release event.</human-check>
  </verify>
  <acceptance_criteria>
    - Both 2.4.0 GitHub releases exist through Release Please and their protected publish runs are available for inspection.
    - No `mailglass_inbound-v2.4.0` release/tag/package is created; inbound remains 2.1.1.
    - The executor receives URLs for the release PR, protected publish run, and release-triggered post-publish smoke run.
  </acceptance_criteria>
  <done>The protected ceremony has produced the two intended release records and supplied all run URLs needed for final read-only verification.</done>
  <resume-signal>Provide the release PR URL, publish run URL, and post-publish-smoke run URL after the protected ceremony has run, or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human go/no-go → protected main | Explicit approval authorizes an irreversible external transition. |
| protected release event → Hex | Only the established environment and tag-gated jobs may publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-15 | Repudiation | publication authorization | high | mitigate | Blocking decision records explicit go/no-go against the candidate evidence. |
| T-148-16 | Tampering | accidental inbound release | high | mitigate | Human inspection plus tested release predicates require inbound 2.1.1 unchanged. |
| T-148-17 | Information Disclosure / Elevation | Hex credentials | high | mitigate | Publication remains entirely inside `hex-publish`; no local/manual publish command. |
| T-148-18 | Denial of Service | missing release-SHA CI or duplicate tag run | high | mitigate | Required checks, self-dispatch recovery, static concurrency, and idempotency remain mandatory. |
</threat_model>

## Artifacts this phase produces

- New symbols/functions: none.
- New workflow jobs/inputs: none.
- New CLI/env flags: none.
- New external artifacts: GitHub releases/tags and Hex packages `mailglass` 2.4.0 and `mailglass_admin` 2.4.0; release/publish/smoke run URLs.
- New repository files: none.

<verification>
The checkpoint returns concrete release and run URLs while proving inbound 2.1.1 was not republished.
</verification>

<success_criteria>
The irreversible release occurs only after explicit approval and only through the protected core/admin path.
</success_criteria>

<output>
Create `.planning/phases/148-release-and-adoption-proof/148-04-SUMMARY.md` when done.
</output>
