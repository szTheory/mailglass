---
phase: 109-foundations-gate-tightening
plan: "03"
type: execute
wave: 3
depends_on:
  - "109-02"
files_modified:
  - mailglass_admin/scripts/check-conformance.sh
  - mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
  - mailglass_admin/docs/ui-baseline-scores.json
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-05
must_haves:
  truths:
    - "Hard conformance gates fail closed on z-index, focus-ring, scope/isolation, raw type-scale, and token-scope regressions."
    - "The existing `check-conformance.sh` shared errors counter and BASH_SOURCE-rooted LIB scoping are preserved."
    - "Ratchet schema v3 includes `system` in prior and current without changing pillar judgments."
    - "No full pillar re-score happens in Phase 109."
  artifacts:
    - path: "mailglass_admin/scripts/check-conformance.sh"
      provides: "Z-INDEX, FOCUS-RING, SCOPE/isolation, and extended TYPE/spacing gates"
    - path: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      provides: "schema v3 and light/dark/system theme axis assertions"
    - path: "mailglass_admin/docs/ui-baseline-scores.json"
      provides: "system cells seeded in prior and current without run-id changes"
  key_links:
    - from: "mailglass_admin/scripts/check-conformance.sh"
      to: "mailglass_admin/lib/mailglass_admin"
      via: "BASH_SOURCE-anchored LIB scan"
      pattern: "SCRIPT_DIR=.*BASH_SOURCE"
    - from: "mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs"
      to: "mailglass_admin/docs/ui-baseline-scores.json"
      via: "@themes includes system and JSON cells exist"
      pattern: "@themes.*system"
---

<objective>
Tighten the conformance and ratchet gates after Plan 109-02 has made the current code clean.

Purpose: Per D-06, D-07, and D-09, gates must be added to the existing hard gate and proven green before any baseline floor is changed. The ratchet moves to schema v3 by adding `system` cells copied from existing scores, not by re-scoring pillars.
Output: Hard conformance gates, ratchet schema v3, and seeded system cells in both prior and current blocks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
@.planning/phases/109-foundations-gate-tightening/109-RESEARCH.md
@.planning/phases/109-foundations-gate-tightening/109-UI-SPEC.md
@.planning/phases/109-foundations-gate-tightening/109-VALIDATION.md
@mailglass_admin/scripts/check-conformance.sh
@mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
@mailglass_admin/docs/ui-baseline-scores.json
</context>

<tasks>

<task id="109-03-01" type="auto" files="mailglass_admin/scripts/check-conformance.sh">
  <name>Task 1: Add hard gates to the existing conformance script</name>
  <files>mailglass_admin/scripts/check-conformance.sh</files>
  <read_first>
    mailglass_admin/scripts/check-conformance.sh
    mailglass_admin/assets/css/app.css
    .planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
    .planning/phases/109-foundations-gate-tightening/109-UI-SPEC.md
  </read_first>
  <action>Modify only `mailglass_admin/scripts/check-conformance.sh` per D-06. Preserve `set -euo pipefail`, `SCRIPT_DIR`, `LIB`, the shared `errors` counter, and the final `if [[ $errors -gt 0 ]]` block. Extend the existing TYPE-GATE pattern so raw `text-xl`, `text-2xl`, and `text-3xl` size utilities fail while `text-base-content` remains excluded by boundary-aware matching. Add a Z-INDEX gate that fails on numeric or arbitrary z-index utilities in admin `lib/**/*.ex`, a FOCUS-RING gate that fails on the pre-consolidation focus idioms, a SCOPE/isolation gate that requires `mg-admin-root` in both operator shell and preview shell roots, and a token-scope gate that fails on new theme hook/storage/picker creep in Phase 109. Add spacing coverage for arbitrary spacing utilities and off-contract fixed-size one-offs without banning documented semantic classes or current admitted scale utilities. Each new gate must increment `errors` independently and print a specific `FAIL:` message.</action>
  <acceptance_criteria>
    - `check-conformance.sh` still resolves `LIB` from `BASH_SOURCE[0]` and scans `${SCRIPT_DIR}/../lib`.
    - The script still uses one shared `errors` counter and one final exit block.
    - The script contains gate labels or failure messages for `Z-INDEX-GATE`, `FOCUS-RING-GATE`, `SCOPE-GATE`, and extended `TYPE-GATE`.
    - Running from repo root and from `mailglass_admin/` both scans the real admin `lib/` tree.
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0 on the current code after Plan 109-02.
  </acceptance_criteria>
  <verify>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh</automated>
    <automated>rg -n 'Z-INDEX-GATE|FOCUS-RING-GATE|SCOPE-GATE|text-xl|text-2xl|text-3xl|errors=0|BASH_SOURCE' mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>The hard conformance lane rejects the Phase 109 regressions through the existing CI-invoked script and remains cwd-independent.</done>
</task>

<task id="109-03-02" type="auto" files="mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs, mailglass_admin/docs/ui-baseline-scores.json">
  <name>Task 2: Bump ratchet schema to v3 and seed system cells without re-scoring</name>
  <files>mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs, mailglass_admin/docs/ui-baseline-scores.json</files>
  <read_first>
    mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
    mailglass_admin/docs/ui-baseline-scores.json
    .planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
    .planning/phases/109-foundations-gate-tightening/109-VALIDATION.md
  </read_first>
  <action>Per D-07 and D-09, update `ratchet_baseline_test.exs` from schema version 2 with light/dark themes to schema version 3 with light/dark/system themes. Add `"system"` to `@themes`, update all human-readable 36-cell messages to 54 cells per block, and assert `schema_version == 3`. In `ui-baseline-scores.json`, set `schema_version` to `3` and add a `system` key to every surface/pillar object in both `prior` and `current`, copying that same block's existing `light` score into `system`. Preserve both existing `run_id` values and every existing light/dark score. Do not run or record any pillar re-score; this is shape coverage only.</action>
  <acceptance_criteria>
    - `@themes` is exactly `["light", "dark", "system"]`.
    - `schema_version` is `3` in both the ExUnit assertion and `ui-baseline-scores.json`.
    - Both `prior` and `current` blocks contain 3 surfaces x 6 pillars x 3 themes.
    - Existing `prior.run_id` remains `2026-06-13-phase-95-baseline`; existing `current.run_id` remains `2026-06-16-phase-103`.
    - Every new `system` score equals that block's existing `light` score for the same surface/pillar.
    - No light or dark score value changes.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors</automated>
    <automated>rg -n '@themes \\["light", "dark", "system"\\]|schema_version.*3|54' mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs</automated>
    <automated>ruby -rjson -e 'b=JSON.parse(File.read("mailglass_admin/docs/ui-baseline-scores.json")); abort "schema" unless b["schema_version"]==3; abort "prior run_id" unless b["prior"]["run_id"]=="2026-06-13-phase-95-baseline"; abort "current run_id" unless b["current"]["run_id"]=="2026-06-16-phase-103"; %w[prior current].each{|block| b[block]["surfaces"].each{|_s,pillars| pillars.each{|_p,scores| abort "system missing" unless scores.key?("system"); abort "system changed" unless scores["system"]==scores["light"] }}}'</automated>
  </verify>
  <done>The ratchet schema includes system as a third theme axis, all cells are present, and the change is explicitly not a pillar re-score.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Z-INDEX-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `FOCUS-RING-GATE` in `mailglass_admin/scripts/check-conformance.sh`.
- `SCOPE-GATE` / isolation check in `mailglass_admin/scripts/check-conformance.sh`.
- Extended `TYPE-GATE` matching `text-xl`, `text-2xl`, and `text-3xl`.
- Extended spacing/token gate for arbitrary spacing and off-contract size one-offs.
- Ratchet schema version `3`.
- Ratchet theme axis `["light", "dark", "system"]`.
- `system` cells in every `prior` and `current` surface/pillar object in `mailglass_admin/docs/ui-baseline-scores.json`.
- `.planning/phases/109-foundations-gate-tightening/109-03-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source tree -> shell grep gate | Regex gates must scan the intended files and fail closed without false positives hiding real defects. |
| baseline JSON -> ExUnit ratchet | Missing or lowered cells can silently weaken the design-system ratchet unless tests fail closed. |
| phase boundary -> later-phase work | Phase 109 must not re-score pillars or introduce Phase 116 axe dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-109-09 | Tampering | check-conformance.sh scoping | mitigate | Preserve BASH_SOURCE anchoring, explicit lib-dir existence check, and shared `errors` counter. |
| T-109-10 | Tampering | gate regexes | mitigate | Use boundary-aware patterns, keep `text-base-content` false-positive exclusion semantic, and prove clean from two cwd positions. |
| T-109-11 | Repudiation | ratchet schema v3 | mitigate | ExUnit asserts schema, cell coverage, range, and only-forward comparison for all 54 cells. |
| T-109-12 | Tampering | score baseline | mitigate | Preserve run IDs and light/dark scores; copy light values into system cells only. |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs; package files are untouched. |
</threat_model>

<verification>
Overall plan checks:
- `bash mailglass_admin/scripts/check-conformance.sh`
- `cd mailglass_admin && bash scripts/check-conformance.sh`
- `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors`
- `ruby -rjson -e 'b=JSON.parse(File.read("mailglass_admin/docs/ui-baseline-scores.json")); abort "schema" unless b["schema_version"]==3; %w[prior current].each{|block| b[block]["surfaces"].each{|_s,pillars| pillars.each{|_p,scores| abort "system missing" unless scores.key?("system") }}}'`
</verification>

<success_criteria>
The current code is clean under the tightened hard gates, the ratchet includes system without re-scoring, and the implementation preserves the v1.11 tighten-before-baseline safety model for FND-05.
</success_criteria>

<output>
Create `.planning/phases/109-foundations-gate-tightening/109-03-SUMMARY.md` when done.
</output>
