---
phase: 109-foundations-gate-tightening
plan: "02"
type: execute
wave: 2
depends_on:
  - "109-01"
files_modified:
  - mailglass_admin/assets/css/app.css
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/lib/mailglass_admin/operator/shell.ex
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/gallery_live.ex
  - mailglass_admin/lib/mailglass_admin/preview_live.ex
  - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
  - mailglass_admin/lib/mailglass_admin/preview/tabs.ex
  - mailglass_admin/priv/static/app.css
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
must_haves:
  truths:
    - "D-01: Existing app.css token blocks are the only token source for semantic layer, focus, overlay, and isolation additions."
    - "D-02: Color stays only in daisyUI theme blocks; overlay and focus utilities use semantic variables instead of new color literals."
    - "Admin stacking contexts consume semantic layer classes instead of raw z-index utilities."
    - "Focus rings are opt-in through one semantic utility contract before the FOCUS-RING gate exists."
    - "Host-safe stacking isolation exists on admin shell roots without JS theme hooks or picker UI."
    - "The compiled CSS bundle is rebuilt and committed with source changes."
  artifacts:
    - path: "mailglass_admin/assets/css/app.css"
      provides: "z-index, overlay, focus-ring, and host-isolation utility contracts"
      contains: "--z-overlay-scrim"
    - path: "mailglass_admin/priv/static/app.css"
      provides: "rebuilt committed admin CSS bundle"
    - path: "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
      provides: "operator replay scrim/panel semantic layer consumption"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex"
      provides: "inbound replay scrim/panel semantic layer consumption"
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "toast layer semantic consumption"
  key_links:
    - from: "mailglass_admin/assets/css/app.css"
      to: "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
      via: "semantic layer classes on scrim and panel"
      pattern: "mg-layer-overlay-(scrim|panel)"
    - from: "mailglass_admin/assets/css/app.css"
      to: "mailglass_admin/lib/mailglass_admin/preview_live.ex"
      via: "mg-admin-root and mg-focus-ring class consumers"
      pattern: "mg-(admin-root|focus-ring)"
---

<objective>
Complete the token/class consolidation that FND-01 through FND-04 require before hard gates are added.

Purpose: Per D-01 through D-05, Phase 109 is a tokenize-the-stragglers phase: extend the existing `app.css` token blocks, consume semantic z/focus/overlay utilities in HEEx, and keep system-theme work at the CSS/root-layout layer without pulling in the later picker work from D-11.
Output: Semantic CSS utilities, migrated z/focus call sites, host stacking isolation, and rebuilt `mailglass_admin/priv/static/app.css`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
@.planning/phases/109-foundations-gate-tightening/109-RESEARCH.md
@.planning/phases/109-foundations-gate-tightening/109-UI-SPEC.md
@.planning/phases/109-foundations-gate-tightening/109-VALIDATION.md
@mailglass_admin/assets/css/app.css
@mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
@mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
@mailglass_admin/lib/mailglass_admin/components.ex
@mailglass_admin/lib/mailglass_admin/operator/shell.ex
@mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
@mailglass_admin/lib/mailglass_admin/gallery_live.ex
@mailglass_admin/lib/mailglass_admin/preview_live.ex
@mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
@mailglass_admin/lib/mailglass_admin/preview/tabs.ex
@mailglass_admin/lib/mailglass_admin/layouts.ex
@mailglass_admin/lib/mailglass_admin/layouts/root.html.heex
</context>

## Scope Rationale

Plan 109-02 intentionally modifies 11 files because the z-layer, focus-ring, root-isolation, and rebuilt bundle work is one semantic utility migration per D-03 and D-04. Splitting the focus-ring consumers or static bundle into a fifth plan would leave source/class/bundle state temporarily inconsistent and would make Plan 109-03's new fail-closed gates depend on an intermediate mixed contract. The touched files are limited to the single CSS token source, existing HEEx consumers of the utility contract, and the compiled CSS bundle; no unrelated UI redesign or later-phase picker work is included.

<tasks>

<task id="109-02-01" type="auto" files="mailglass_admin/assets/css/app.css">
  <name>Task 1: Add semantic layer, overlay, focus, and isolation utilities</name>
  <files>mailglass_admin/assets/css/app.css</files>
  <read_first>
    mailglass_admin/assets/css/app.css
    .planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
    .planning/phases/109-foundations-gate-tightening/109-UI-SPEC.md
  </read_first>
  <action>Extend the existing `:root` z-tier block and existing `@theme`/utility area only, per D-01: existing app.css token blocks are the only token source, and D-02: color stays only in daisyUI theme blocks. Add `--z-base: 0`, keep `--z-sticky: 10`, keep `--z-dropdown: 20`, add `--z-overlay-scrim: 30`, add `--z-overlay-panel: 40`, keep `--z-toast: 50`, and keep `--z-overlay`/`--z-modal` only as compatibility aliases to the new scrim/panel values. Add plain CSS utility classes `.mg-layer-base`, `.mg-layer-sticky`, `.mg-layer-dropdown`, `.mg-layer-overlay-scrim`, `.mg-layer-overlay-panel`, `.mg-layer-toast`, `.mg-overlay-scrim`, `.mg-focus-ring`, `.mg-focus-ring-inset`, and `.mg-admin-root`. `.mg-overlay-scrim` must derive from daisyUI semantic variables, not raw color literals. `.mg-focus-ring` must render a visible 2px focus outline with 2px offset using `--color-primary` and `--duration-instant`; `.mg-focus-ring-inset` is only for call sites where offset would clip. `.mg-admin-root` must set `isolation: isolate` and create a bounded admin stacking context. Do not create another token file, do not add color literals outside the daisyUI theme blocks, and do not add any JS theme hook or picker UI per D-10: CSS-layer system-theme plumbing is already correct, and D-11.</action>
  <acceptance_criteria>
    - `mailglass_admin/assets/css/app.css` contains `--z-base: 0`, `--z-overlay-scrim: 30`, `--z-overlay-panel: 40`, and `--z-toast: 50`.
    - `mailglass_admin/assets/css/app.css` contains `.mg-layer-overlay-scrim`, `.mg-layer-overlay-panel`, `.mg-layer-toast`, `.mg-overlay-scrim`, `.mg-focus-ring`, `.mg-focus-ring-inset`, and `.mg-admin-root`.
    - New color values are expressed with existing semantic CSS variables; no new raw hex literals are added outside the daisyUI theme blocks.
    - No `phx-hook`, `localStorage`, theme cookie, head script, or visible system/light/dark picker is introduced by this task.
  </acceptance_criteria>
  <verify>
    <automated>rg -n -- '--z-base|--z-overlay-scrim|--z-overlay-panel|\\.mg-layer-overlay-scrim|\\.mg-layer-overlay-panel|\\.mg-focus-ring|\\.mg-admin-root' mailglass_admin/assets/css/app.css</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
  </verify>
  <done>Existing `app.css` is the only token source and now exposes the semantic utilities needed for z layers, overlays, focus rings, and host-safe root isolation.</done>
</task>

<task id="109-02-02" type="auto" files="mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex, mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/lib/mailglass_admin/components.ex, mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/preview_live.ex">
  <name>Task 2: Migrate stacking contexts and admin roots to semantic utilities</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex, mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex, mailglass_admin/lib/mailglass_admin/components.ex, mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/preview_live.ex</files>
  <read_first>
    mailglass_admin/assets/css/app.css
    mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
    mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
    mailglass_admin/lib/mailglass_admin/components.ex
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/lib/mailglass_admin/preview_live.ex
    .planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
  </read_first>
  <action>Per D-03, migrate the two replay modal scrims to `.mg-layer-overlay-scrim` plus `.mg-overlay-scrim`, and add `.mg-layer-overlay-panel` to each replay modal dialog panel so panel ordering is explicit. Migrate `Components.flash/1` toast wrapper to `.mg-layer-toast`. Add `.mg-admin-root` to the operator shell root and the Preview shell root so the mountable admin owns a bounded stacking context. Keep `Operator.Shell.dark_chrome?/1`, `surface_paths/3`, `toggle_theme_path/2`, and `PreviewLive.admin_theme_attr/1` semantics intact; this phase proves CSS/root behavior and must not rebuild the 3-way picker or tri-state shell wiring from D-11.</action>
  <acceptance_criteria>
    - `operator/replay_modal.ex` and `inbound/replay_modal.ex` scrim wrappers contain `mg-layer-overlay-scrim` and `mg-overlay-scrim`.
    - Both replay modal dialog panels contain `mg-layer-overlay-panel`.
    - `components.ex` flash toast wrapper contains `mg-layer-toast`.
    - `operator/shell.ex` root shell wrapper contains `mg-admin-root`.
    - `preview_live.ex` `data-testid="preview-shell"` root wrapper contains `mg-admin-root`.
    - `Operator.Shell.dark_chrome?/1`, `surface_paths/3`, and `toggle_theme_path/2` still model the existing two-state operator override; no system picker labels or routes are added.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'mg-layer-overlay-scrim|mg-layer-overlay-panel|mg-layer-toast|mg-admin-root' mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex mailglass_admin/lib/mailglass_admin/components.ex mailglass_admin/lib/mailglass_admin/operator/shell.ex mailglass_admin/lib/mailglass_admin/preview_live.ex</automated>
    <automated>test "$(rg -n '\\bz-([0-9]+|\\[[^]]+\\])' mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex mailglass_admin/lib/mailglass_admin/components.ex | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>Modal scrims, modal panels, toast, and admin roots consume semantic utilities; raw numeric stacking classes are gone from the migrated call sites.</done>
</task>

<task id="109-02-03" type="auto" files="mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/gallery_live.ex, mailglass_admin/lib/mailglass_admin/preview_live.ex, mailglass_admin/lib/mailglass_admin/preview/sidebar.ex, mailglass_admin/lib/mailglass_admin/preview/tabs.ex, mailglass_admin/priv/static/app.css">
  <name>Task 3: Consolidate focus-ring consumers and rebuild the CSS bundle</name>
  <files>mailglass_admin/lib/mailglass_admin/operator/shell.ex, mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex, mailglass_admin/lib/mailglass_admin/gallery_live.ex, mailglass_admin/lib/mailglass_admin/preview_live.ex, mailglass_admin/lib/mailglass_admin/preview/sidebar.ex, mailglass_admin/lib/mailglass_admin/preview/tabs.ex, mailglass_admin/priv/static/app.css</files>
  <read_first>
    mailglass_admin/assets/css/app.css
    mailglass_admin/lib/mailglass_admin/operator/shell.ex
    mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
    mailglass_admin/lib/mailglass_admin/gallery_live.ex
    mailglass_admin/lib/mailglass_admin/preview_live.ex
    mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
    mailglass_admin/lib/mailglass_admin/preview/tabs.ex
    mailglass_admin/package.json
    mailglass_admin/mix.exs
    .planning/phases/109-foundations-gate-tightening/109-VALIDATION.md
  </read_first>
  <action>Per D-04, replace the copied focus-ring idioms across operator nav links, delivery row buttons, gallery copied nav specimens, preview shell buttons, preview sidebar controls, and preview tabs with `.mg-focus-ring` or `.mg-focus-ring-inset`. Use the inset variant only where the existing code intentionally used an inset ring because an offset ring would clip at the component boundary. While these touched files are open, remove off-contract arbitrary fixed-size and border-width utilities that FND-03 will gate in Plan 109-03 by replacing them with existing semantic size and border contracts from `app.css` or daisyUI. Preserve existing classes unrelated to focus and token discipline, preserve all URLs and events, and do not change visible theme controls beyond focus styling. Rebuild the standalone Tailwind bundle with `cd mailglass_admin && mix mailglass_admin.assets.build` and include `mailglass_admin/priv/static/app.css` in the task commit.</action>
  <acceptance_criteria>
    - `rg -n 'mg-focus-ring'` finds focus utility consumers in the operator shell, operator deliveries list, gallery, preview live view, preview sidebar, and preview tabs.
    - `rg -n 'focus-visible:ring-2 focus-visible:ring-primary|focus:outline|focus:outline-2|focus:outline-primary' mailglass_admin/lib/mailglass_admin` returns no matches.
    - The preview files touched by this task have no off-contract arbitrary fixed heights or non-semantic border-width utilities left for the FND-03 gate to catch.
    - Existing `aria-current`, `aria-selected`, `phx-click`, `patch`, `navigate`, and `data-testid` attributes are preserved.
    - `mailglass_admin/priv/static/app.css` is rebuilt after source CSS/class changes and committed with the same task.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && mix mailglass_admin.assets.build</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
    <automated>test "$(rg -n 'focus-visible:ring-2 focus-visible:ring-primary|focus:outline|focus:outline-2|focus:outline-primary' mailglass_admin/lib/mailglass_admin | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(rg -n '\bborder-(0|2|4|8|\[[^]]+\])\b|\b(?:w|h|min-w|max-w|min-h|max-h)-\[[^]]+\]' mailglass_admin/lib/mailglass_admin/preview_live.ex mailglass_admin/lib/mailglass_admin/preview/tabs.ex | wc -l | tr -d ' ')" = "0"</automated>
    <automated>cd mailglass_admin && mix verify.support_contract.admin</automated>
  </verify>
  <done>All focus-ring call sites consume the semantic utility, the bundle is rebuilt, and the current conformance/support-contract lanes remain green before gates are tightened.</done>
</task>

</tasks>

## Artifacts this phase produces

- CSS variables: `--z-base`, `--z-overlay-scrim`, `--z-overlay-panel`.
- Compatibility aliases: `--z-overlay`, `--z-modal` mapped to the new scrim/panel tier names if retained.
- CSS classes: `.mg-layer-base`, `.mg-layer-sticky`, `.mg-layer-dropdown`, `.mg-layer-overlay-scrim`, `.mg-layer-overlay-panel`, `.mg-layer-toast`, `.mg-overlay-scrim`, `.mg-focus-ring`, `.mg-focus-ring-inset`, `.mg-admin-root`.
- Migrated HEEx consumers in `operator/replay_modal.ex`, `inbound/replay_modal.ex`, `components.ex`, `operator/shell.ex`, `operator/deliveries_list.ex`, `gallery_live.ex`, `preview_live.ex`, `preview/sidebar.ex`, and `preview/tabs.ex`.
- Rebuilt `mailglass_admin/priv/static/app.css`.
- `.planning/phases/109-foundations-gate-tightening/109-02-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host app CSS -> mounted admin CSS | Host styles and z-index contexts can collide with the mountable admin surface. |
| CSS token source -> HEEx consumers | HEEx must consume semantic utilities rather than inventing local layer/focus values. |
| PR #86 baseline -> operator/inbound shell URLs | Theme and tenant scope from the merged baseline must not be regressed by class work. |

## STRIDE Threat Register

Execution must stop on any HIGH or CRITICAL threat whose status is `open`. The executor must either complete the mitigation and rerun the listed verification, or create a blocking checkpoint that records why the threat cannot be closed.

| Threat ID | STRIDE | ASVS L1 Mapping | Severity | Status | Component | Mitigation / Verification |
|---|---|---|---|---|---|---|
| T-109-04 | Tampering | V14 Configuration | HIGH | planned | CSS token source | Extend only `mailglass_admin/assets/css/app.css`; no parallel token source per D-01. Verification: file diff and `rg` for required utility selectors in `app.css`. |
| T-109-05 | Denial of Service | V14 Configuration | MEDIUM | planned | replay modal stacking | Split scrim/panel layer classes and add Playwright hit-test proof in Plan 109-04. Verification: structural `elementFromPoint` assertions in Plan 109-04. |
| T-109-06 | Information Disclosure | V4 Access Control | HIGH | planned | tenant-scope URL carry | Preserve `surface_paths/3`, `build_path`, and `theme_query` behavior; run support-contract and structural suites. Verification: `cd mailglass_admin && mix verify.support_contract.admin`. |
| T-109-07 | Tampering | V14 Configuration | MEDIUM | planned | host CSS collision | Add `.mg-admin-root` with `isolation: isolate`; later SCOPE gate verifies roots. Verification: `bash mailglass_admin/scripts/check-conformance.sh` after Plan 109-03. |
| T-109-08 | Elevation of Privilege | V3 Session Management | HIGH | planned | scope creep into JS theme hooks or picker UI | Do not add hooks, cookies, localStorage, or picker controls in Phase 109; final scope audit blocks these. Verification: Plan 109-04 package/source negative scans. |
| T-109-SC | Tampering | V14 Configuration | LOW | accepted | npm/pip/cargo installs | No package installs. `@axe-core/playwright` remains Phase 116. Verification: package manifests unchanged in Plan 109-04. |
</threat_model>

<verification>
Overall plan checks:
- `cd mailglass_admin && mix mailglass_admin.assets.build`
- `bash mailglass_admin/scripts/check-conformance.sh`
- `cd mailglass_admin && mix verify.support_contract.admin`
- `test "$(rg -n 'focus-visible:ring-2 focus-visible:ring-primary|focus:outline|focus:outline-2|focus:outline-primary' mailglass_admin/lib/mailglass_admin | wc -l | tr -d ' ')" = "0"`
- After the task commit: `cd mailglass_admin && git diff --exit-code priv/static/`
</verification>

<success_criteria>
FND-01 through FND-04 have a real implementation substrate: semantic z layers are present and consumed, focus rings are consolidated, host isolation exists, color remains owned by daisyUI theme blocks, system-theme work has not expanded into JS or picker UI, and the rebuilt static CSS is committed.
</success_criteria>

<output>
Create `.planning/phases/109-foundations-gate-tightening/109-02-SUMMARY.md` when done.
</output>
