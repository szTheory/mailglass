---
phase: 109-foundations-gate-tightening
plan: "04"
type: execute
wave: 4
depends_on:
  - "109-03"
files_modified:
  - mailglass_admin/e2e/structural.spec.js
autonomous: true
requirements:
  - REL-01
  - FND-01
  - FND-02
  - FND-04
  - FND-05
must_haves:
  truths:
    - "Structural Playwright coverage proves system-theme CSS/root behavior without a JS hook or picker UI."
    - "Replay modal panels hit-test above scrims in light, dark, and system contexts."
    - "WCAG 2.2 focus-not-obscured, focus appearance, non-text contrast, and target-size assertions extend the existing structural suite."
    - "Final phase proof is green, including bundle cleanliness and scope-creep checks."
  artifacts:
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "system-theme and WCAG 2.2 structural assertions"
    - path: "mailglass_admin/e2e/structural.spec.js"
      provides: "overlay panel-above-scrim elementFromPoint hit-test helper/assertions"
  key_links:
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/layouts/root.html.heex"
      via: "system/default route emits no explicit root data-theme"
      pattern: "data-theme"
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex"
      via: "operator replay modal hit-test"
      pattern: "elementFromPoint"
    - from: "mailglass_admin/e2e/structural.spec.js"
      to: "mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex"
      via: "inbound replay modal hit-test"
      pattern: "elementFromPoint"
---

<objective>
Add the structural WCAG/system proof and run the final Phase 109 validation battery.

Purpose: Per D-08 and D-10, Phase 109 proves CSS/root system behavior and WCAG 2.2 structural rules in the existing Playwright suite. Per D-09 and D-11, it does not add axe, a picker UI, a JS theme hook, or a pillar re-score.
Output: Extended structural assertions and a final green proof across conformance, ratchet, support-contract, preview, browser, bundle-clean, and scope-creep checks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
@.planning/phases/109-foundations-gate-tightening/109-RESEARCH.md
@.planning/phases/109-foundations-gate-tightening/109-UI-SPEC.md
@.planning/phases/109-foundations-gate-tightening/109-VALIDATION.md
@mailglass_admin/e2e/structural.spec.js
@mailglass_admin/lib/mailglass_admin/layouts.ex
@mailglass_admin/lib/mailglass_admin/layouts/root.html.heex
@mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
@mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
@mailglass_admin/package.json
@mailglass_admin/package-lock.json
</context>

<tasks>

<task id="109-04-01" type="auto" files="mailglass_admin/e2e/structural.spec.js">
  <name>Task 1: Extend structural Playwright assertions for system theme and WCAG 2.2</name>
  <files>mailglass_admin/e2e/structural.spec.js</files>
  <read_first>
    mailglass_admin/e2e/structural.spec.js
    mailglass_admin/lib/mailglass_admin/layouts.ex
    mailglass_admin/lib/mailglass_admin/layouts/root.html.heex
    mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
    mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
    .planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
    .planning/phases/109-foundations-gate-tightening/109-UI-SPEC.md
  </read_first>
  <action>Extend `mailglass_admin/e2e/structural.spec.js`, not a new spec and not axe, per D-08. Add helpers for system-theme cases that call `page.emulateMedia({ colorScheme: "light" })` and `page.emulateMedia({ colorScheme: "dark" })` before navigation. Add assertions that the default/system root layout has no explicit root `data-theme` and that the preview shell default/system branch also emits no explicit preview shell theme value, while explicit `theme=light` and `theme=dark` still emit the existing explicit values. Extend existing contrast/focus helpers to assert a 2px visible focus indicator and non-text contrast for focused controls. Extend the target-size block to cover the touched overlay, nav, preview theme/frame controls, and summary controls at the existing 44px project floor. Add an `elementFromPoint` hit-test helper and focused tests for `operator-replay-modal` and `inbound-replay-modal` proving each modal panel is the top hit-test target at its centroid above the scrim. Keep all assertions structural/computed-style based; do not add screenshots or pixel diff.</action>
  <acceptance_criteria>
    - `structural.spec.js` contains `emulateMedia({ colorScheme: "light" })` and `emulateMedia({ colorScheme: "dark" })` cases before navigation.
    - Default/system Preview root assertions prove no explicit `data-theme` on the root HTML element and no explicit Preview shell theme value for the default/system branch.
    - Explicit `theme=light` and `theme=dark` tests still expect `mailglass-light` and `mailglass-dark`.
    - Overlay hit-test assertions use `document.elementFromPoint` and cover both `operator-replay-modal` and `inbound-replay-modal`.
    - Focus appearance assertions verify visible focus indicator width >= 2px and non-text contrast >= 3:1 for representative controls.
    - Target-size assertions include touched replay modal buttons, operator nav, preview theme/frame buttons, and preview sidebar summary controls.
    - No `@axe-core/playwright`, `toHaveScreenshot`, or screenshot baseline is added.
  </acceptance_criteria>
  <verify>
    <automated>cd mailglass_admin && npm run test:operator-browser -- --grep "system|replay modal|focus|touch targets"</automated>
    <automated>rg -n 'colorScheme|elementFromPoint|operator-replay-modal|inbound-replay-modal|Focus Not Obscured|target-size|toHaveScreenshot|@axe-core/playwright' mailglass_admin/e2e/structural.spec.js</automated>
  </verify>
  <done>The browser structural suite proves the Phase 109 system-theme and WCAG 2.2 additions without adding an axe dependency or screenshot testing.</done>
</task>

<task id="109-04-02" type="auto" files="none">
  <name>Task 2: Run final proof and scope-creep audit</name>
  <files>none</files>
  <read_first>
    .planning/phases/109-foundations-gate-tightening/109-VALIDATION.md
    .planning/phases/109-foundations-gate-tightening/109-CONTEXT.md
    mailglass_admin/package.json
    mailglass_admin/package-lock.json
    mailglass_admin/scripts/check-conformance.sh
    mailglass_admin/test/mailglass_admin/ratchet_baseline_test.exs
    mailglass_admin/docs/ui-baseline-scores.json
  </read_first>
  <action>Run the Phase 109 final validation battery from `109-VALIDATION.md` after the structural spec changes. Refresh local Mix deps with `cd mailglass_admin && mix deps.get` only if ExUnit reports stale locked deps; do not treat dependency refresh as a product change. Confirm REL-01 remains true, all hard gates are green, the admin static bundle is clean after rebuild, no package files gained `@axe-core/playwright`, no JS theme hook/storage/picker UI landed, and `ui-baseline-scores.json` still has preserved run IDs. Do not create a pillar re-score artifact and do not modify `package.json` or `package-lock.json`.</action>
  <acceptance_criteria>
    - `gh pr view 86 --json state,mergedAt --jq '.state == "MERGED" and (.mergedAt != null)'` prints `true`.
    - `bash mailglass_admin/scripts/check-conformance.sh` exits 0.
    - `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors` exits 0.
    - `cd mailglass_admin && mix verify.support_contract.admin` exits 0.
    - `cd mailglass_admin && mix verify.preview` exits 0.
    - `cd mailglass_admin && npm run test:operator-browser` exits 0.
    - `cd mailglass_admin && git diff --exit-code priv/static/` exits 0 after the final rebuild and task commit.
    - `git diff --exit-code -- mailglass_admin/package.json mailglass_admin/package-lock.json` exits 0.
    - `rg -n '@axe-core/playwright|phoenix_storybook|phx-hook=.*theme|mailglass_admin:theme|localStorage' mailglass_admin/package.json mailglass_admin/package-lock.json mailglass_admin/lib/mailglass_admin` returns no matches.
    - Existing ratchet run IDs are unchanged; no Phase 109 full score run ID is introduced.
  </acceptance_criteria>
  <verify>
    <automated>gh pr view 86 --json state,mergedAt --jq '.state == "MERGED" and (.mergedAt != null)'</automated>
    <automated>bash mailglass_admin/scripts/check-conformance.sh</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors</automated>
    <automated>cd mailglass_admin && mix verify.support_contract.admin</automated>
    <automated>cd mailglass_admin && mix verify.preview</automated>
    <automated>cd mailglass_admin && npm run test:operator-browser</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/</automated>
    <automated>git diff --exit-code -- mailglass_admin/package.json mailglass_admin/package-lock.json</automated>
  </verify>
  <done>Every Phase 109 validation command is green, package scope did not expand, static assets are clean, and no pillar re-score happened.</done>
</task>

</tasks>

## Artifacts this phase produces

- Structural helper or assertion using `page.emulateMedia({ colorScheme })`.
- Structural helper or assertion using `document.elementFromPoint`.
- System/default theme assertions proving no explicit root theme value.
- Overlay hit-test assertions for `operator-replay-modal` and `inbound-replay-modal`.
- WCAG 2.2 structural assertions for focus visibility/non-text contrast and target size.
- Final validation evidence recorded in `.planning/phases/109-foundations-gate-tightening/109-04-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser runtime -> structural assertions | Computed CSS and DOM facts prove behavior without screenshots. |
| admin UI -> host app | System-theme and stacking changes must not add host-global JS/CSS collisions. |
| package manifests -> supply chain | Phase 109 must not add axe or Storybook dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-109-13 | Denial of Service | replay modal hit testing | mitigate | `elementFromPoint` assertions prove panel is above scrim for operator and inbound. |
| T-109-14 | Tampering | system-theme behavior | mitigate | Assert no explicit theme value for default/system and explicit values only for light/dark branches. |
| T-109-15 | Information Disclosure | cross-tenant PR #86 baseline | mitigate | Final support-contract and browser suites run after REL-01 merge to catch tenant-scope regressions. |
| T-109-16 | Tampering | scope creep into axe, JS hooks, picker UI | mitigate | Package diff and source grep block Phase 116 dependency, JS hook/storage, and picker artifacts. |
| T-109-17 | Repudiation | final gate proof | mitigate | SUMMARY records exact commands, results, and any advisory main-CI classification. |
| T-109-SC | Tampering | npm/pip/cargo installs | mitigate | Block package manifest changes and grep for forbidden test dependency in final proof. |
</threat_model>

<verification>
Overall phase checks:
- `gh pr view 86 --json state,mergedAt --jq '.state == "MERGED" and (.mergedAt != null)'`
- `bash mailglass_admin/scripts/check-conformance.sh`
- `cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix verify.support_contract.admin`
- `cd mailglass_admin && mix verify.preview`
- `cd mailglass_admin && npm run test:operator-browser`
- `cd mailglass_admin && git diff --exit-code priv/static/`
- `git diff --exit-code -- mailglass_admin/package.json mailglass_admin/package-lock.json`
- `rg -n '@axe-core/playwright|phoenix_storybook|phx-hook=.*theme|mailglass_admin:theme|localStorage' mailglass_admin/package.json mailglass_admin/package-lock.json mailglass_admin/lib/mailglass_admin` returns no matches.
</verification>

<success_criteria>
Phase 109 is complete when REL-01 is satisfied, semantic token/class consolidation is in place, the hard gates and ratchet schema v3 are green, structural WCAG/system assertions pass, the rebuilt bundle is clean, and there is no pillar re-score, axe dependency, JS theme hook, or picker UI.
</success_criteria>

<output>
Create `.planning/phases/109-foundations-gate-tightening/109-04-SUMMARY.md` when done.
</output>
