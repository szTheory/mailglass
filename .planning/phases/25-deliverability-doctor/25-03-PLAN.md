---
phase: 25-deliverability-doctor
plan: "03"
type: execute
wave: 3
depends_on:
  - "25-01"
  - "25-02"
files_modified:
  - lib/mailglass/deliverability.ex
  - lib/mailglass/deliverability/mx.ex
  - lib/mailglass/deliverability/bimi.ex
  - lib/mailglass/deliverability/formatter.ex
  - test/mailglass/deliverability/mx_test.exs
  - test/mailglass/deliverability/bimi_test.exs
  - test/mailglass/deliverability/formatter_test.exs
  - test/mailglass/deliverability_test.exs
autonomous: true
requirements:
  - DOCTOR-01
  - DOCTOR-02
  - DOCTOR-03
tags:
  - deliverability
  - dns
  - mx
  - bimi
  - formatter

must_haves:
  truths:
    - "MX and BIMI findings stay honest about ambiguity: no MX explains both misconfiguration and intentional send-only posture, and missing BIMI is not equated with broken deliverability."
    - "Human and JSON output derive from the same shared result model instead of separate bespoke shapes."
    - "Default human output is grouped by `SPF`, `DKIM`, `DMARC`, `MX`, and `BIMI`, while verbose evidence is available without changing the underlying status semantics."
  artifacts:
    - path: "lib/mailglass/deliverability/mx.ex"
      provides: "MX and Null MX analyzer"
      contains: "Null MX"
    - path: "lib/mailglass/deliverability.ex"
      provides: "runtime orchestration across SPF, DKIM, DMARC, MX, and BIMI analyzers"
      contains: "Mailglass.Deliverability.BIMI"
    - path: "lib/mailglass/deliverability/bimi.ex"
      provides: "BIMI readiness analyzer"
      contains: "default._bimi"
    - path: "lib/mailglass/deliverability/formatter.ex"
      provides: "shared human and JSON rendering"
      contains: "schema_version"
  key_links:
    - from: "lib/mailglass/deliverability/bimi.ex"
      to: "lib/mailglass/deliverability/dmarc.ex"
      via: "BIMI readiness depends on DMARC enforcement status in the shared result"
      pattern: "reject|quarantine|none"
    - from: "lib/mailglass/deliverability.ex"
      to: "lib/mailglass/deliverability/spf.ex, lib/mailglass/deliverability/dkim.ex, lib/mailglass/deliverability/dmarc.ex, lib/mailglass/deliverability/mx.ex, lib/mailglass/deliverability/bimi.ex"
      via: "runtime invokes every analyzer and passes DMARC posture into BIMI before building the shared result"
      pattern: "SPF|DKIM|DMARC|MX|BIMI"
    - from: "lib/mailglass/deliverability/formatter.ex"
      to: "lib/mailglass/deliverability/result.ex"
      via: "renderers use one result contract for both text and JSON"
      pattern: "findings|summary|schema_version"
    - from: "test/mailglass/deliverability/formatter_test.exs"
      to: "lib/mailglass/deliverability/formatter.ex"
      via: "tests pin grouped headers and machine output stability"
      pattern: "SPF|DKIM|DMARC|MX|BIMI|schema_version"
---

<objective>
Implement the remaining DNS analyzers and the shared formatter that turns the runtime contract into operator-facing output.

Purpose: Phase 25 is only useful if the last-mile presentation preserves the same uncertainty and remediation semantics as the analyzers. MX and BIMI also carry special trust caveats that need explicit copy rather than hidden assumptions.

Output:
- runtime orchestration that invokes all five analyzers and threads DMARC posture into BIMI
- MX analyzer that distinguishes no-MX, Null MX, and present MX cases
- BIMI readiness analyzer with DMARC prerequisite posture
- formatter that renders grouped human output and versioned JSON from one result model
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/mailglass/.planning/PROJECT.md
@/Users/jon/projects/mailglass/.planning/REQUIREMENTS.md
@/Users/jon/projects/mailglass/.planning/phases/25-deliverability-doctor/25-CONTEXT.md
@/Users/jon/projects/mailglass/.planning/phases/25-deliverability-doctor/25-RESEARCH.md
@/Users/jon/projects/mailglass/.planning/phases/25-deliverability-doctor/25-PATTERNS.md
@/Users/jon/projects/mailglass/lib/mailglass/deliverability.ex
@/Users/jon/projects/mailglass/lib/mailglass/deliverability/result.ex
@/Users/jon/projects/mailglass/lib/mailglass/webhook/providers/postmark.ex
@/Users/jon/projects/mailglass/test/support/deliverability_resolver_stub.ex
@/Users/jon/projects/mailglass/lib/mix/tasks/mailglass.gen.unsubscribe.ex
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 25-03-01: Implement MX and BIMI analyzers, then wire runtime orchestration across all analyzers</name>
  <files>lib/mailglass/deliverability.ex, lib/mailglass/deliverability/mx.ex, lib/mailglass/deliverability/bimi.ex, test/mailglass/deliverability/mx_test.exs, test/mailglass/deliverability/bimi_test.exs, test/mailglass/deliverability_test.exs</files>
  <read_first>
    - lib/mailglass/deliverability.ex
    - lib/mailglass/deliverability/result.ex
    - lib/mailglass/deliverability/resolver.ex
    - lib/mailglass/deliverability/spf.ex
    - lib/mailglass/deliverability/dkim.ex
    - lib/mailglass/deliverability/dmarc.ex
    - lib/mailglass/webhook/providers/postmark.ex
    - test/support/deliverability_resolver_stub.ex
    - .planning/phases/25-deliverability-doctor/25-CONTEXT.md
    - .planning/phases/25-deliverability-doctor/25-RESEARCH.md
  </read_first>
  <action>
    Create `lib/mailglass/deliverability/mx.ex` and `lib/mailglass/deliverability/bimi.ex` with the exact posture from D-25-17 and D-25-18, then update `lib/mailglass/deliverability.ex` so the runtime explicitly orchestrates all protocol analyzers per D-25-19 and D-25-21.

    Use `lib/mailglass/webhook/providers/postmark.ex` as the mapped analog for parser boundaries and normalization helpers: keep each analyzer's public entrypoint narrow, push record parsing into private helpers, and surface malformed external data as findings or normalized resolver errors.

    `Mailglass.Deliverability.MX` must:
    - treat explicit Null MX (`0 .`) as an intentional send-only `pass` or low-risk `warn` finding with explanatory copy
    - treat no MX answer as an ambiguity finding that explains both plausible interpretations
    - treat malformed or partial MX answers as `cannot_verify` or `fail` with evidence
    - preserve the raw MX facts in the shared `facts` bucket

    `Mailglass.Deliverability.BIMI` must:
    - resolve `default._bimi.<domain>` without inventing alternate selectors in Phase 25
    - treat missing BIMI as `warn`, not `fail`
    - require DMARC enforcement posture from the shared result before reporting BIMI readiness
    - explain `l=` logo-hosting and `a=` certificate caveats without claiming provider logo-display certainty

    `Mailglass.Deliverability.run/1` must be expanded in this plan so it:
    - invokes `Mailglass.Deliverability.SPF`, `DKIM`, `DMARC`, `MX`, and `BIMI` explicitly instead of returning only empty protocol buckets
    - aggregates all analyzer findings, facts, and resolver errors into the shared result contract
    - derives DMARC posture from the DMARC analyzer output and passes that posture into the BIMI analyzer before BIMI findings are finalized
    - keeps analyzer ordering deterministic: SPF, DKIM, DMARC, MX, then BIMI
    - preserves the one-domain, explicit-selector contract from Plan 25-01 while remaining persistence-free

    Add tests covering:
    - present MX
    - Null MX
    - missing MX ambiguity copy
    - missing BIMI warn semantics
    - BIMI + non-enforcement DMARC prerequisite warning
    - top-level runtime aggregation calling all five analyzers and surfacing BIMI findings that reflect DMARC enforcement posture
  </action>
  <verify>
    <automated>mix test test/mailglass/deliverability_test.exs test/mailglass/deliverability/mx_test.exs test/mailglass/deliverability/bimi_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Deliverability\\.(SPF|DKIM|DMARC|MX|BIMI)|def run' lib/mailglass/deliverability.ex` returns explicit analyzer orchestration.
    - `rg -n 'dmarc|bimi|resolver_errors|findings|facts' lib/mailglass/deliverability.ex` returns runtime aggregation and DMARC-to-BIMI handoff coverage.
    - `test -f lib/mailglass/deliverability/mx.ex` succeeds.
    - `rg -n 'Null MX|cannot_verify|exchange|preference' lib/mailglass/deliverability/mx.ex` returns the MX analyzer logic.
    - `test -f lib/mailglass/deliverability/bimi.ex` succeeds.
    - `rg -n 'default\\._bimi|l=|a=|dmarc' lib/mailglass/deliverability/bimi.ex` returns the BIMI readiness logic.
    - `rg -n 'all five|dmarc|bimi|aggregation|resolver_errors' test/mailglass/deliverability_test.exs` returns runtime orchestration coverage.
    - `rg -n 'Null MX|send-only|missing BIMI|enforcement' test/mailglass/deliverability/mx_test.exs test/mailglass/deliverability/bimi_test.exs` returns the trust-critical coverage.
    - `mix test test/mailglass/deliverability_test.exs test/mailglass/deliverability/mx_test.exs test/mailglass/deliverability/bimi_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>MX and BIMI findings now preserve the exact ambiguity and readiness semantics locked for Phase 25.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 25-03-02: Build the shared human and JSON formatter on top of the result contract</name>
  <files>lib/mailglass/deliverability/formatter.ex, test/mailglass/deliverability/formatter_test.exs</files>
  <read_first>
    - lib/mailglass/deliverability/result.ex
    - lib/mix/tasks/mailglass.gen.unsubscribe.ex
    - test/mix/tasks/mailglass.gen.unsubscribe_test.exs
    - .planning/phases/25-deliverability-doctor/25-CONTEXT.md
    - .planning/phases/25-deliverability-doctor/25-RESEARCH.md
  </read_first>
  <action>
    Create `lib/mailglass/deliverability/formatter.ex` as the only rendering layer for Phase 25.

    It must expose two concrete entrypoints:
    - `render_human(result, verbose?: boolean())`
    - `render_json(result)`

    `render_human/2` must produce:
    - one top summary line that includes the domain and the counts for `pass`, `warn`, `fail`, and `cannot_verify`
    - grouped sections in this exact order: `SPF`, `DKIM`, `DMARC`, `MX`, `BIMI`
    - per-finding copy that renders `title`, `why it matters`, `observed`, and `remediation`
    - evidence lines only when `verbose?: true`

    `render_json/1` must serialize the shared result with `Jason.encode!/1` and preserve `schema_version`, `summary`, `findings`, `facts`, and `resolver_errors` so future CI/admin surfaces do not scrape the human output.

    Add `test/mailglass/deliverability/formatter_test.exs` proving:
    - the section order is `SPF` then `DKIM` then `DMARC` then `MX` then `BIMI`
    - verbose output adds evidence lines while default output does not
    - JSON output includes `\"schema_version\":1`, `\"summary\"`, `\"findings\"`, and `\"resolver_errors\"`
  </action>
  <verify>
    <automated>mix test test/mailglass/deliverability/formatter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mailglass/deliverability/formatter.ex` succeeds.
    - `rg -n 'render_human|render_json|SPF|DKIM|DMARC|MX|BIMI|schema_version' lib/mailglass/deliverability/formatter.ex` returns the shared renderer contract.
    - `rg -n 'verbose|schema_version|resolver_errors|SPF.*DKIM.*DMARC.*MX.*BIMI' test/mailglass/deliverability/formatter_test.exs` returns section-order and JSON-shape coverage.
    - `mix test test/mailglass/deliverability/formatter_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The phase now has one renderer for both human and machine output, preserving the same truth semantics across both surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyzer findings -> formatter | Internal findings become operator-facing remediation copy |
| Shared result -> JSON output | In-memory data becomes a stable machine-consumed contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Integrity | MX analyzer | mitigate | Distinguish `mx_present`, `null_mx`, and `no_mx` explicitly so outbound-only domains are not falsely reported as broken. |
| T-25-10 | Integrity | BIMI analyzer | mitigate | Treat missing BIMI as non-fatal readiness guidance and require DMARC enforcement context before claiming BIMI readiness. |
| T-25-11 | Tampering | formatter / JSON contract drift | mitigate | Render both human and JSON output from the same `Result` shape and pin `schema_version`, section order, and required JSON keys in tests. |
| T-25-12 | Information Disclosure | verbose evidence | accept | Verbose output contains only DNS-derived facts already visible via public DNS; no mailbox content or credentials are rendered. |
</threat_model>

<verification>
  - mix test test/mailglass/deliverability_test.exs test/mailglass/deliverability/mx_test.exs test/mailglass/deliverability/bimi_test.exs --warnings-as-errors
  - mix test test/mailglass/deliverability/formatter_test.exs --warnings-as-errors
</verification>

<success_criteria>
The remaining protocol findings and both renderer surfaces preserve the same operator-trust semantics: honest ambiguity, explicit remediation, and no JSON/text drift.
</success_criteria>

<output>
After completion, create `.planning/phases/25-deliverability-doctor/25-03-SUMMARY.md`
</output>
