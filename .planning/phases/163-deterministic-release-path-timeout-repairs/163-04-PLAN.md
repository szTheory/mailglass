---
phase: 163-deterministic-release-path-timeout-repairs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mailglass/properties/idempotency_convergence_test.exs
  - test/mailglass/properties/webhook_idempotency_convergence_test.exs
  - lib/mailglass/webhook/ingest.ex
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
autonomous: false
gap_closure: true
requirements: [DTRM-01, DTRM-02]
must_haves:
  truths:
    - "D-01/DTRM-01: Historical reconstruction and current proof retain both 1,000-run invariants, their generators, non-transactional per-owner checkout, ten-minute owner bound, and cleanup/settle semantics."
    - "D-02/DTRM-01: Only a structured SQLSTATE 57014 reproduced at an immutable failing SHA and attributed to one fixture/session/isolation/query owner can authorize a current-tree repair."
    - "D-05/DTRM-02: The repaired property passes three consecutive unseeded pinned-toolchain invocations on the first attempt before any integration verdict."
    - "D-06/DTRM-02: Database proof preserves global database/job limits, property count, seed freedom, and protected topology."
    - "DTRM-01: An inconclusive bounded search pauses at a blocking maintainer decision; only supplied immutable failing-run evidence authorizes repair, while re-scope/defer leaves the requirement explicitly open."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md"
      provides: "Append-only original-run identity, historical reconstruction, structured attribution, repair, edge, and three-run proof ledger"
    - path: "test/mailglass/properties/idempotency_convergence_test.exs"
      provides: "Original committed-write property and conditional regression at an attributed test seam"
    - path: "test/mailglass/properties/webhook_idempotency_convergence_test.exs"
      provides: "Original webhook property and conditional regression at an attributed test seam"
    - path: "lib/mailglass/webhook/ingest.ex"
      provides: "Conditional finite transaction-local repair only when historical reconstruction attributes this owner"
  key_links:
    - from: "original protected failure"
      to: "disposable exact-SHA reconstruction"
      via: "run/job URL, 40-character head SHA, command, toolchain, and structured PostgreSQL metadata"
      pattern: "failing_sha|protected_run_id|57014|query_canceled"
    - from: "attributed reproduction"
      to: "current narrow regression and repair"
      via: "same named operation and structured Postgrex error boundary"
      pattern: "fixture|session|isolation|query"
    - from: "repair"
      to: "DTRM-02 focused proof"
      via: "three independent unseeded make-toolchain first attempts"
      pattern: "repetition: 3|first-attempt"
  flagged_assumptions:
    - "DTRM-01 boundary/precision is unresolved until the attributed seam records its finite threshold, integer-millisecond measurement rule, and below/equal/above behavior; equality is exhausted, not successful."
    - "DTRM-02 unclassified/manual-review candidate is resolved by a source-diff backstop plus append-only command/toolchain/first-attempt evidence; any reviewer ambiguity remains non-pass."
  prohibitions:
    - "RESOLVED/test — DTRM-01 transparency: MUST NOT call an unreproduced or multiply-attributed database hypothesis a repair; the evidence gate halts instead."
    - "RESOLVED/test — DTRM-02 proof integrity: MUST NOT use a fixed seed for post-repair proof, fewer than 1,000 runs, exclusions, retries, or broader database/CI deadlines."
    - "RESOLVED/judgment — DTRM-01 privacy: MUST NOT persist payloads, recipient/message content, tenant secrets, or raw exception text in diagnostics."
---

<objective>
Recover the immutable original SQLSTATE 57014 failure, reproduce and attribute it in a disposable exact-SHA environment, then repair only the demonstrated current-tree seam and prove it three times.

Purpose: Close the two database failures from `163-VERIFICATION.md` without converting current green diagnostics into speculative authority.
Output: An appended database ledger plus either an evidence-authorized regression/repair or a blocking maintainer decision that records supplied immutable evidence versus an explicit DTRM-01 re-scope/defer choice.

Execution-path status: Plans 163-04 through 163-08 supersede the failed/inconclusive execution path of 163-01 through 163-03 for gap closure. Preserve 163-01 through 163-03 and their summaries as immutable historical evidence; they are not dependencies or resumable prerequisites for this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-VERIFICATION.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-01-SUMMARY.md
@test/mailglass/properties/idempotency_convergence_test.exs
@test/mailglass/properties/webhook_idempotency_convergence_test.exs
@lib/mailglass/webhook/ingest.ex
@test/support/sandbox_ownership.ex

<interfaces>
- `%Postgrex.Error{postgres: %{code: :query_canceled}}` is the structured SQLSTATE 57014 contract; message-string matching is forbidden.
- `SandboxOwnership.checkout!/1` remains the sole ownership door and both properties retain `max_runs: 1000`.
- `Ingest.ingest_multi/3` keeps its result tuples and existing transaction-local timeout seam unless exact-SHA reconstruction proves that seam owns the cancellation.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reconstruct the original SQLSTATE 57014 at its immutable failing SHA</name>
  <reversibility rating="reversible">The task adds evidence and disposable diagnostics; it makes no current production change.</reversibility>
  <files>.planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md, test/mailglass/properties/idempotency_convergence_test.exs, test/mailglass/properties/webhook_idempotency_convergence_test.exs</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-VERIFICATION.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-01-SUMMARY.md
    - test/mailglass/properties/idempotency_convergence_test.exs
    - test/mailglass/properties/webhook_idempotency_convergence_test.exs
    - lib/mailglass/webhook/ingest.ex
    - lib/mailglass/webhook/replay.ex
    - test/support/sandbox_ownership.ex
    - .github/workflows/ci.yml
  </read_first>
  <action>Per D-01/D-02, locate the original observed failure from retained local artifacts or read-only GitHub Actions evidence. Inspect at most the ten most recent candidate protected runs whose deterministic-core job failed; require a terminal run/job URL, full head SHA, event, job identity, exact command/toolchain, and structured `query_canceled`/57014 evidence. Treat unavailable, malformed, message-only, or ambiguous evidence as `inconclusive` and append that verdict without editing a repair seam. If an immutable failing identity exists, create a disposable `mktemp -d` checkout/worktree at that SHA, use the pinned 2-vCPU toolchain and its original database image, and run the affected property at most three first attempts. A recorded historical seed may be used only to reconstruct the failure, never for post-repair proof. Cross-reference the two property modules as exact analogs: preserve their matching per-owner non-transactional checkout, cleanup, generator, and 1,000-run contracts while placing temporary stable operation labels around only the demonstrated setup, cleanup, aggregate, snapshot, or ingest boundary in the disposable reconstruction. Treat `lib/mailglass/webhook/replay.ex` as the transaction-pattern analog for `Ingest`: any ingest observation must remain inside `Repo.transact/1`, preserve finite `SET LOCAL` guards, `Repo.multi/1` result tuples, and post-commit telemetry. Capture structured code/severity/routine, effective session settings, normalized non-PII query shape, operation owner, elapsed integer milliseconds, and cleanup. Append all attempts and one `attributed` or `inconclusive` search verdict. If 57014 is not reproduced or has multiple plausible owners after the budget, stop at Task 2; the search verdict alone does not complete DTRM-01.</action>
  <verify>
    <automated>test -s .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md &amp;&amp; rg -n "failing_sha|historical reconstruction|attributed|inconclusive" .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md &amp;&amp; rg -n "max_runs: 1000" test/mailglass/properties/idempotency_convergence_test.exs test/mailglass/properties/webhook_idempotency_convergence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The ledger binds any reproduction claim to an exact run/job identity and 40-character failing SHA, not a remembered failure description.
    - At most ten remote candidates and three disposable first attempts are inspected; every result is retained append-only.
    - An attributed verdict names exactly one property and one operation owner with structured 57014 metadata; otherwise the verdict is inconclusive and no repair is authorized.
    - Current source retains both 1,000-run properties and all D-01 ownership/invariant markers.
  </acceptance_criteria>
  <done>The bounded historical search and append-only ledger entry are complete; an attributed result carries immutable evidence, while an inconclusive result requires Task 2's blocking maintainer decision and cannot complete DTRM-01.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Resolve missing immutable database failure evidence</name>
  <decision>Choose how DTRM-01 proceeds after the bounded historical search: supply immutable failing-run evidence, or deliberately re-scope/defer the requirement.</decision>
  <context>An inconclusive ledger is not reproduction or repair. No executor may speculate about a database seam, run the repair task, unlock Plan 163-06, or claim DTRM-01 complete without immutable structured 57014 evidence.</context>
  <options>
    <option id="supply-evidence">
      <name>Supply immutable evidence</name>
      <pros>Preserves DTRM-01 and permits evidence-gated attribution and repair.</pros>
      <cons>Requires a terminal run/job URL, full 40-character failing SHA, exact command/toolchain, and structured SQLSTATE 57014 artifact or log evidence; the executor must validate and append it before continuing.</cons>
    </option>
    <option id="rescope-defer-dtrm-01">
      <name>Re-scope or defer DTRM-01</name>
      <pros>Records an explicit maintainer decision when immutable evidence cannot be recovered.</pros>
      <cons>Task 3 and downstream Plans 163-06 through 163-08 remain blocked for DTRM-01; phase/requirements artifacts must be revised through the appropriate planning workflow before any completion claim.</cons>
    </option>
  </options>
  <resume-signal>Reply `supply-evidence` with the immutable run/job URL, 40-character failing SHA, exact command/toolchain, and structured 57014 evidence location; or reply `rescope-defer-dtrm-01` and explicitly state the approved scope/deferral destination.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regress, narrowly repair, and repeat the attributed database path</name>
  <precondition>Task 2 was resolved through `supply-evidence`, and the appended database ledger contains validated immutable evidence plus a reproduced structured 57014 bound to one failing SHA, one affected property, and one unambiguous operation owner; the re-scope/defer branch does not satisfy this precondition.</precondition>
  <reversibility rating="costly">Changing an ingest transaction guard affects every webhook ingestion, so that seam is eligible only when exact attribution excludes fixture-only repair.</reversibility>
  <files>test/mailglass/properties/idempotency_convergence_test.exs, test/mailglass/properties/webhook_idempotency_convergence_test.exs, lib/mailglass/webhook/ingest.ex, .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - test/mailglass/properties/idempotency_convergence_test.exs
    - test/mailglass/properties/webhook_idempotency_convergence_test.exs
    - lib/mailglass/webhook/ingest.ex
    - lib/mailglass/webhook/replay.ex
    - test/support/sandbox_ownership.ex
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - The attributed operation has a deterministic fail-first regression using structured Postgrex metadata.
    - The smallest fixture/session/isolation/query repair turns that regression green while preserving both complete properties.
    - Three consecutive current-tree pinned-toolchain property invocations pass unseeded on their first and only attempts.
    - The finite threshold records below/equal/above behavior in integer milliseconds; equality counts as exhaustion.
  </behavior>
  <action>Start RED at the attributed operation, then apply the mechanical repair selection from D-02: fixture residue permits only fixture cleanup/isolation correction; a property query permits only its local query/fixture change; the production transaction-local seam is eligible only when reconstruction proves it owns 57014 and fixture/lock alternatives are excluded. Keep the two property files cross-referenced as exact fixture/property analogs, preserving their corresponding checkout, cleanup, generator, and `max_runs: 1000` structures. For an attributed ingest repair, follow `lib/mailglass/webhook/replay.ex` as the transaction-local analog: keep the change inside `Repo.transact/1`, retain finite `SET LOCAL` guards, preserve `Repo.multi/1` success/error tuples and post-commit telemetry, and do not introduce message-string matching or global settings. Preserve schema/API behavior, owner bound, generators, and cleanup cardinality. Derive any finite local duration from reproduced measurements plus documented headroom. Record below/equal/above tests when controllable and explicitly record why an uncontrollable PostgreSQL boundary remains flagged. Run the affected property three separate times through `make toolchain`, unseeded, no retry; append command, versions, repair commit, every first-attempt exit/elapsed value, range, invariant markers, and source diff backstop per D-05/D-06.</action>
  <verify>
    <automated>make toolchain CMD='mix test test/mailglass/properties/idempotency_convergence_test.exs test/mailglass/properties/webhook_idempotency_convergence_test.exs --warnings-as-errors' &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The regression is machine-observed red before repair and green after repair at the same attributed operation.
    - The repair touches one permitted seam and keeps all bounds finite; no schema, API, dependency, workflow, global timeout, seed, exclusion, retry, or owner-bound change appears.
    - Three fully recorded current-tree first attempts pass with both 1,000-run invariants intact.
    - DTRM-01 boundary/precision and DTRM-02 review status are resolved or remain explicitly flagged with final sign-off blocked.
  </acceptance_criteria>
  <done>DTRM-01 has an attributed regression-backed repair and DTRM-02 has three honest focused database proofs, or execution remains explicitly blocked before any speculative change.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md` — appended symbols/fields: `failing_sha`, original run/job identity, reconstruction attempts, structured SQLSTATE owner, threshold edge results, repair identity, and three first-attempt proof rows.
- One private regression helper/test at the attributed property boundary; no public API or schema symbol.
- A conditional finite change in `lib/mailglass/webhook/ingest.ex` only if that exact transaction-local boundary is proven responsible.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub/local history → reconstruction | External or retained evidence must bind to immutable identities before it directs a repair. |
| Disposable failing checkout → current source | Historical behavior may diagnose a seam but cannot silently overwrite current code. |
| PostgreSQL error → evidence | Structured metadata is allowed; message/payload/tenant data is sensitive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-163-13 | Spoofing | original failing run identity | high | mitigate | Require exact SHA, event, run/job IDs, command, and structured 57014 before reconstruction authority. |
| T-163-14 | Tampering | property coverage and timeout policy | high | mitigate | Source-diff backstop plus both unchanged 1,000-run properties and no global policy edits. |
| T-163-15 | Repudiation | repeated proof | high | mitigate | Append every first attempt, including failures, with command/toolchain/elapsed data. |
| T-163-16 | Information Disclosure | diagnostic ledger | high | mitigate | Persist operation labels and normalized shapes only; exclude raw values and exception text. |
| T-163-SC | Tampering | package supply chain | low | accept | No package install, lockfile, action, or dependency change is in scope. |
</threat_model>

<verification>
Require an exact-SHA attributed reconstruction, a fail-first regression, the narrow current repair, and three recorded unseeded first-attempt property passes. An inconclusive reconstruction is a truthful blocked result, never success.
</verification>

<success_criteria>
- DTRM-01 is closed by immutable reproduction, one-owner attribution, and a narrow regression-backed repair.
- DTRM-02 focused proof consists of three complete first attempts with the prohibited shortcuts absent.
- All database edge candidates and kept prohibitions are resolved or visibly block later integration.
</success_criteria>

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-04-SUMMARY.md` when done.
</output>
