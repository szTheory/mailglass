---
phase: 163-deterministic-release-path-timeout-repairs
plan: 07
type: execute
wave: 3
depends_on: [163-06]
files_modified: []
autonomous: false
gap_closure: true
requirements: [DTRM-02, DTRM-04]
must_haves:
  truths:
    - "D-06/DTRM-02/DTRM-04: The immutable `repair_sha` receives a normally triggered protected pull-request or main run; manual dispatch and another SHA are ineligible."
    - "The handoff resumes only after the exact-SHA run reaches a terminal conclusion and exposes both named protected jobs for read-only reconciliation."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md"
      provides: "Existing immutable repair SHA and pending protected handoff consumed by this checkpoint"
  key_links:
    - from: "repair_sha"
      to: "normal protected CI run"
      via: "exact headSha plus pull_request or main event"
      pattern: "headSha|event|databaseId"
  prohibitions:
    - "RESOLVED/judgment — DTRM-02/DTRM-04 authority: MUST NOT substitute a manual dispatch, different SHA, advisory label, or nonterminal run for normal protected evidence."
---

<objective>
Pause after all automatable local work until the immutable repair SHA has a terminal, normally triggered protected run.

Purpose: Obtain the external evidence identity required by D-06 without granting dispatch or workflow-mutation authority.
Output: User-supplied exact `repair_sha` confirmation and terminal `protected_run_id` for Plan 163-08.

Execution-path status: Plans 163-04 through 163-08 supersede the failed/inconclusive execution path of 163-01 through 163-03 for gap closure. Preserve 163-01 through 163-03 and their summaries as immutable historical evidence; this handoff consumes only the repair SHA frozen by new Plan 163-06.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-06-SUMMARY.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Await the normal exact-SHA protected run</name>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-06-SUMMARY.md
    - .github/workflows/ci.yml
  </read_first>
  <what-built>Plans 163-04 through 163-06 reconstructed and repaired both timeout paths, proved three focused first attempts each, passed the unchanged complete local gates, and froze the Task 1 integration commit as `repair_sha` in `163-PROOF.md`.</what-built>
  <how-to-verify>
    1. Read `repair_sha` from `163-PROOF.md`; do not replace it with current HEAD.
    2. Let that exact commit proceed through the repository's normal protected pull-request or main path. Do not manually dispatch a substitute or edit workflow policy.
    3. Run the read-only query `gh run list --commit &lt;repair_sha&gt; --json databaseId,headSha,event,status,conclusion,url,workflowName` and select only a run whose `headSha` exactly equals `repair_sha`, whose event is the normal PR/main protection path, and whose status is terminal.
    4. Confirm the run exposes the named Core Deterministic Suite and Operator Browser Gate jobs. Their conclusions may be success or failure; Plan 163-08 records the truthful terminal verdict.
  </how-to-verify>
  <verify>
    <automated>proof=.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md; test -s "$proof" &amp;&amp; rg -q '^repair_sha: [0-9a-f]{40}$' "$proof" &amp;&amp; rg -q '^protected_handoff: pending$' "$proof"</automated>
  </verify>
  <acceptance_criteria>
    - The supplied SHA exactly matches the frozen value in `163-PROOF.md`.
    - The supplied run is normally triggered for that SHA and is terminal.
    - Both named job identities are visible for Plan 163-08; no success conclusion is inferred at the checkpoint.
  </acceptance_criteria>
  <resume-signal>Provide `repair_sha=&lt;40-hex&gt; protected_run_id=&lt;databaseId&gt;` after the exact normally triggered run is terminal.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- No file or symbol is created. The checkpoint yields two immutable inputs: `repair_sha` and `protected_run_id`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local proof → GitHub protected automation | A remote run is applicable only when its SHA and event match the frozen repair identity. |
| Human handoff → automated reconciliation | User-supplied IDs are untrusted until Plan 163-08 re-queries and validates them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-163-24 | Spoofing | protected run handoff | high | mitigate | Freeze SHA in proof, require normal event and terminal run, then independently re-query in Plan 163-08. |
| T-163-25 | Elevation of Privilege | workflow dispatch/policy | high | mitigate | Checkpoint authorizes observation only; no dispatch, merge, rerun, or workflow mutation. |
| T-163-SC | Tampering | package supply chain | low | accept | No packages, actions, or files change in this checkpoint. |
</threat_model>

<verification>
Verify the frozen SHA is present, then accept only a terminal normal protected run ID for that exact SHA.
</verification>

<success_criteria>
- Plan 163-08 receives immutable exact-SHA protected evidence inputs.
- No manual/different-SHA substitute or authority expansion occurs.
</success_criteria>

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-07-SUMMARY.md` when done.
</output>
