---
phase: 163-deterministic-release-path-timeout-repairs
plan: 03
type: execute
wave: 2
depends_on: [163-01, 163-02]
files_modified:
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
autonomous: false
requirements: [DTRM-02, DTRM-04]
must_haves:
  truths:
    - "D-05/DTRM-02: Database integration is accepted only after Plan 01's three first-attempt-visible focused passes and the unchanged `mix test --warnings-as-errors` deterministic-core command succeed."
    - "D-05/DTRM-04: Browser integration is accepted only after Plan 02's three first-attempt-visible complete-matrix passes and the unchanged `npm run test:operator-browser` command succeeds."
    - "D-06: Protected `core_deterministic_suite` and `operator_browser_gate` topology, 30-minute job deadlines, pinned toolchains/services, package command, worker count, retries, and all global database/Playwright bounds remain byte-unchanged from the Phase 163 baseline."
    - "DTRM-02 repeated-proof edge contract: exactly three consecutive unseeded pinned-toolchain focused database invocations must be green on their first and only attempts before the integration gate; missing, reordered, retry-assisted, or partially recorded repetitions fail closed."
    - "DTRM-04 protected browser-proof edge contract: exactly three consecutive complete one-worker focused gallery invocations must be green on first attempt before the operator-browser gate; a configured CI retry may aid diagnostics but cannot convert the focused verdict to pass."
    - "All six spec-less edge probes are either resolved in Plan 01/02 evidence or remain explicitly flagged-unverified in `163-PROOF.md`; no edge item disappears during final synthesis."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md"
      provides: "Final requirement, invariant, edge-probe, prohibition, unchanged-baseline, local-gate, and protected-run evidence ledger"
      contains: "DTRM-01"
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md"
      provides: "Nyquist task status and sign-off aligned to the executed three-plan decomposition"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "163-DATABASE-TIMEOUT-EVIDENCE.md"
      to: "unchanged deterministic core suite"
      via: "three focused first attempts precede full `mix test --warnings-as-errors`"
      pattern: "count=3|mix test --warnings-as-errors"
    - from: "163-BROWSER-TIMEOUT-EVIDENCE.md"
      to: "unchanged operator browser gate"
      via: "three complete focused first attempts precede `npm run test:operator-browser`"
      pattern: "count=3|test:operator-browser"
    - from: "Phase 163 baseline blobs"
      to: ".github/workflows/ci.yml and mailglass_admin/package.json"
      via: "recorded Git object IDs and byte comparison"
      pattern: "core_deterministic_suite|operator_browser_gate"
    - from: "committed Plan 03 Task 1 result"
      to: "eligible protected pull-request or main run"
      via: "blocking handoff freezes `git rev-parse HEAD` as the immutable repair SHA and waits for the normal protected trigger"
      pattern: "repair_sha|protected_run_id"
  prohibitions:
    - "FLAGGED-UNVERIFIED — MUST NOT accept global timeout relaxation, reduced proof cardinality, or changed protected topology as an integration repair."
    - "FLAGGED-UNVERIFIED — MUST NOT let retries, fixed seeds, missing repetitions, or second-attempt success manufacture a passing final verdict."
    - "FLAGGED-UNVERIFIED — MUST NOT copy PII-bearing diagnostic payloads into final proof or summaries."
---

<objective>
Carry both narrowly repaired tracer paths through their unchanged deterministic-core and operator-browser integration commands, reconcile protected-run evidence, and publish one honest Phase 163 proof ledger.

Purpose: Complete DTRM-02 and DTRM-04 only after repeated focused proof, while proving all locked invariants, edge items, and bounded release-path controls survived unchanged.
Output: Final Phase 163 proof ledger and completed Nyquist validation status.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-01-SUMMARY.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-02-SUMMARY.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
@.github/workflows/ci.yml
@mailglass_admin/package.json
@mailglass_admin/playwright.config.cjs

<interfaces>
- The protected database command is `.github/workflows/ci.yml` job `core_deterministic_suite`, step `mix test --warnings-as-errors`, on Elixir 1.18/OTP 27 with a 30-minute job deadline.
- The protected browser command is job `operator_browser_gate`, step `npm run test:operator-browser`, on Elixir 1.18/OTP 27/Node 22 with a 30-minute job deadline; the package script owns `--workers=1`.
- Focused evidence must precede integration evidence; existing CI retry behavior never substitutes for the three focused first attempts.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry both repaired paths through the unchanged deterministic-core and operator-browser commands</name>
  <precondition>Plans 163-01 and 163-02 each record one attributed repair and three consecutive first-attempt-visible focused passes; any unattributed or incomplete ledger halts integration.</precondition>
  <files>.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-01-SUMMARY.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-02-SUMMARY.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - .github/workflows/ci.yml
    - mailglass_admin/package.json
    - mailglass_admin/playwright.config.cjs
  </read_first>
  <action>Per D-05, validate both focused ledgers before running integration: each must contain one attributed boundary, count=3 consecutive first-attempt passes, exact command/toolchain, all elapsed values/ranges, and the database or gallery invariant/cardinality record. Fail closed on any missing, retry-assisted, fixed-seed, reduced-run, reduced-matrix, or unattributed entry. Record Phase 163 baseline Git blob IDs for `.github/workflows/ci.yml`, `mailglass_admin/package.json`, and `mailglass_admin/playwright.config.cjs` from the parent commit before Phase 163 implementation, then prove current bytes match. Run the unchanged protected command equivalents on the pinned local toolchain: `make toolchain CMD='mix test --warnings-as-errors'`, then from `mailglass_admin` run `CI=true npm run test:operator-browser` with the existing one-worker package script. Create `163-PROOF.md` and record exact command/toolchain, first-attempt result, elapsed range, source evidence links, unchanged blob comparison, and separate database/browser verdicts. Do not rerun a failed gate until diagnosing it; never overwrite a first failure with a later pass.</action>
  <verify>
    <automated>make toolchain CMD='mix test --warnings-as-errors' &amp;&amp; cd mailglass_admin &amp;&amp; CI=true npm run test:operator-browser</automated>
  </verify>
  <acceptance_criteria>
    - Both integration commands run exactly as owned by the protected jobs, without exclusions, seed, retry additions, worker changes, or deadline overrides.
    - The full database suite and full operator-browser suite are first-attempt green after their required three focused passes.
    - Workflow, package script, Playwright global defaults/retry/server lifecycle, and job deadlines match their recorded pre-Phase-163 Git blobs.
    - The proof ledger preserves failed attempts and distinguishes focused, local integration, and remote protected evidence.
  </acceptance_criteria>
  <done>Both repaired paths pass their unchanged complete local protected-command equivalents after valid focused proof, with baseline invariants byte-checked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Protected-run handoff: Freeze the committed repair SHA and await its normal protected run</name>
  <what-built>Task 1 has committed the repaired database and browser paths together with their unchanged local integration proof. The commit at `git rev-parse HEAD` is now the immutable repair SHA whose normal protected pull-request or main run must supply DTRM-02/DTRM-04 evidence.</what-built>
  <how-to-verify>
    1. From the clean Phase 163 worktree, capture the full 40-character output of `git rev-parse HEAD`; this is `repair_sha` and must not be recomputed from a later documentation-only commit.
    2. Let that exact commit reach the repository's existing pull-request or main protection path through the normal project handoff. Do not use manual dispatch, alter workflow topology, or substitute another SHA.
    3. Confirm an eligible protected CI run for `repair_sha` is visible and has reached a terminal conclusion. Use read-only inspection such as `gh run list --commit &lt;repair_sha&gt; --json databaseId,headSha,event,status,conclusion,url,workflowName`; authenticate if the runtime requests it.
    4. Resume with `repair_sha=&lt;40-hex SHA&gt;` and the eligible `protected_run_id=&lt;databaseId&gt;`. If the run is still queued/in progress or has not appeared, leave this checkpoint paused and check the same SHA again later.
  </how-to-verify>
  <resume-signal>Provide `repair_sha=&lt;40-hex SHA&gt; protected_run_id=&lt;id&gt;` after the normally triggered exact-SHA run reaches a terminal conclusion.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile exact protected-run evidence and close every requirement, edge probe, and prohibition</name>
  <precondition>The protected-run handoff supplied the immutable 40-character `repair_sha` captured immediately after Task 1's commit and the terminal `protected_run_id` normally triggered for that exact SHA.</precondition>
  <files>.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md, .planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-DATABASE-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
    - .planning/phases/162-protected-release-and-scheduled-control-recovery/162-CONTEXT.md
    - .github/workflows/ci.yml
  </read_first>
  <action>Use the handoff's immutable `repair_sha` and `protected_run_id`; do not resolve HEAD again after the checkpoint. Query that run and its jobs with authenticated read-only GitHub CLI calls, assert `headSha == repair_sha`, and require a pull-request or main protection event plus the named `Core Deterministic Suite (Elixir 1.18 / OTP 27)` and `Operator Browser Gate (Elixir 1.18 / OTP 27 / Node 22)` jobs. Record a machine-readable reconciliation block in `163-PROOF.md` containing `repair_sha`, `protected_run_id`, event, status, capture time, run URL, both job IDs/URLs/conclusions when available, `protected_verdict: success|pending|blocked|cannot-check`, and `final_signoff: pass|non-pass`. Per Phase 162's evidence rule, only the exact-SHA run with both successful jobs may produce `protected_verdict: success`; a manual/different-SHA run, missing or unsuccessful job, still-pending status, malformed response, or unavailable authenticated read produces its truthful non-success verdict without causing this evidence-recording task itself to fail. For every non-success protected verdict, keep `wave_0_complete: false` and `nyquist_compliant: false`, preserve the command/error or available IDs, and leave DTRM-02/DTRM-04 explicitly open for a later reconciliation of the same immutable SHA. On exact protected success, append immutable run/job URLs and conclusions. Reconcile all six edge probes explicitly: DTRM-01 boundary and precision from database evidence; DTRM-02 three-run edge contract; DTRM-03 boundary and monotonic/equality precision; DTRM-04 three-run plus protected-browser contract. Any unresolved item remains `FLAGGED-UNVERIFIED`, sets `final_signoff: non-pass`, and keeps both validation flags false even when the protected jobs succeeded. Recheck the three kept value/safety prohibitions (no broad deadline relaxation, no manufactured retry/coverage success, no PII diagnostics), add the source coverage audit verdict, update task IDs 163-01-01 through 163-03-02 and command/status rows in `163-VALIDATION.md`, and set `final_signoff: pass`, `wave_0_complete: true`, and `nyquist_compliant: true` only when every automated check, edge probe, prohibition review, and exact-SHA protected job is green.</action>
  <verify>
    <automated>proof=.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md; validation=.planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md; test -s "$proof" &amp;&amp; rg -n "^repair_sha: [0-9a-f]{40}$|^protected_run_id:|^protected_verdict: (success|pending|blocked|cannot-check)$|^final_signoff: (pass|non-pass)$|DTRM-01|DTRM-02|DTRM-03|DTRM-04|Core Deterministic Suite|Operator Browser Gate|first-attempt|elapsed|source boundary" "$proof" &amp;&amp; rg -n "163-03-02" "$validation" &amp;&amp; verdict=$(sed -n 's/^protected_verdict: //p' "$proof" | tail -1) &amp;&amp; signoff=$(sed -n 's/^final_signoff: //p' "$proof" | tail -1) &amp;&amp; flags=$(awk 'NR &gt; 1 &amp;&amp; /^---$/ {exit} NR &gt; 1 {print}' "$validation") &amp;&amp; case "$verdict:$signoff" in success:pass) printf '%s\n' "$flags" | rg -q '^nyquist_compliant: true$' &amp;&amp; printf '%s\n' "$flags" | rg -q '^wave_0_complete: true$' &amp;&amp; rg -q "Core Deterministic Suite.*success|success.*Core Deterministic Suite" "$proof" &amp;&amp; rg -q "Operator Browser Gate.*success|success.*Operator Browser Gate" "$proof" ;; success:non-pass|pending:non-pass|blocked:non-pass|cannot-check:non-pass) printf '%s\n' "$flags" | rg -q '^nyquist_compliant: false$' &amp;&amp; printf '%s\n' "$flags" | rg -q '^wave_0_complete: false$' ;; *) exit 1 ;; esac</automated>
  </verify>
  <acceptance_criteria>
    - Exact-SHA protected evidence identifies successful database and browser jobs; unavailable, pending, wrong-SHA, unsuccessful, or malformed evidence records a deterministic non-pass verdict against the same immutable repair SHA.
    - All DTRM requirements cite focused evidence, unchanged integration commands, and protected jobs with no prohibited shortcut.
    - All six spec-less edge probes are resolved, or the final verdict remains non-pass with the unresolved probe explicitly flagged.
    - The three kept prohibitions are reviewed with evidence, and no generic compliance/OWASP prohibition is added; breadcrumb: generic compliance items were dropped because they do not protect Phase 163 value or safety.
    - `163-VALIDATION.md` matches the six actual task IDs and is marked Nyquist compliant only after every task and protected gate succeeds.
  </acceptance_criteria>
  <done>The final ledger gives an honest exact-SHA protected verdict, complete DTRM traceability, and explicit closure or non-pass status for every edge probe and kept prohibition; a remote non-success state is durably recorded with both validation flags false and can be reconciled later against the same repair SHA.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-PROOF.md` — new final ledger with focused/local/protected evidence, immutable Git/GitHub identities, DTRM coverage, six edge-probe outcomes, and three value/safety prohibition outcomes.
- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md` — existing validation strategy updated with task `163-03-02`, executed statuses, and conditional Nyquist sign-off; no new schema beyond existing frontmatter keys.
- No new production symbol, API, schema field, CLI flag, dependency, workflow, worker, retry, or timeout configuration is expected in this integration plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Focused evidence → integration verdict | Missing or retry-assisted repetitions must not be promoted into a complete-gate claim. |
| Local commit → GitHub protected run | Run identity, commit SHA, job name, conclusion, and event must agree before remote evidence is applicable. |
| Diagnostic ledgers → final proof | Final synthesis must retain source attribution without copying sensitive payload values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-09 | Repudiation | focused/integration evidence ordering | high | mitigate | Validate three first-attempt focused runs before unchanged full gates and preserve every failed attempt. |
| T-163-10 | Tampering | protected GitHub run selection | high | mitigate | Require exact commit SHA and both named successful jobs; reject manual/different-SHA/pending/malformed substitutes. |
| T-163-11 | Denial of Service | CI/global timeout policy | high | mitigate | Byte-compare workflow/package/config baselines and reject job, global, retry, worker, or topology changes. |
| T-163-12 | Information Disclosure | final evidence synthesis | high | mitigate | Copy only source boundaries, timing, counts, immutable IDs, and conclusions; exclude raw query values and browser fixture content. |
| T-163-SC | Tampering | package/action supply chain | low | accept | No install, dependency, lockfile, action reference, or workflow change is planned. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V4 Access Control | Applicable | Read-only run inspection cannot change protected workflow authority or dispatch behavior. |
| V7 Error Handling and Logging | Applicable | Missing or malformed remote evidence becomes blocked/cannot-check, never inferred success. |
| V14 Configuration | Applicable | Protected job topology, deadlines, workers, retries, and global timeout settings are byte-compared to baseline. |
</threat_model>

<verification>
Run both unchanged local integration commands, then inspect an exact-SHA protected run with authenticated read-only `gh` commands. Verify `163-PROOF.md` contains four requirement verdicts, six edge-probe outcomes, three prohibition outcomes, exact run/job identities, commands/toolchains/repetition/elapsed/first-attempt/source-boundary fields, and the baseline blob comparison. Verify `163-VALIDATION.md` is true only on complete green evidence.
</verification>

<success_criteria>
- DTRM-02 and DTRM-04 have repeated focused proof followed by unchanged local and exact-SHA protected integration gates.
- All D-01 through D-06 decisions, full database/browser coverage, and bounded policy survive with evidence.
- Every edge probe and kept prohibition is visible and either closed or truthfully blocks final sign-off.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Repeated honest database-property and gallery-matrix proof without weakened invariants, coverage, or bounds | 163-01..03 | COVERED | Parallel narrow tracers converge on unchanged protected proof. |
| REQ | DTRM-01 | Reproduce/repair SQLSTATE 57014 with both 1,000-run invariants | 163-01 | COVERED | Attribution gates the only permitted repair. |
| REQ | DTRM-02 | Repeated property proof plus canonical protected CI without shortcuts | 163-01, 163-03 | COVERED | Three focused first attempts precede unchanged full and exact-SHA gates. |
| REQ | DTRM-03 | Reproduce/repair gallery timeout with complete live matrix | 163-02 | COVERED | Readiness and body clocks remain distinct. |
| REQ | DTRM-04 | Repeated browser proof plus operator gate without UI/matrix/retry/global changes | 163-02, 163-03 | COVERED | Three complete first attempts precede unchanged full and exact-SHA gates. |
| RESEARCH | — | Capture structured SQLSTATE/query/session before choosing a database repair | 163-01 | COVERED | Unattributed evidence halts. |
| RESEARCH | — | Separate readiness from matrix body and retain single-worker bounded lifecycle | 163-02 | COVERED | Existing probes and config remain owners. |
| RESEARCH | — | Record command, toolchain, count, elapsed range, first attempt, source boundary | 163-01..03 | COVERED | Required in every evidence ledger. |
| RESEARCH | — | Existing dependencies/toolchains only; no package install | 163-01..03 | COVERED | All supply-chain threats accept because no install occurs. |
| CONTEXT | D-01 | Preserve both 1,000-run properties, ownership, and committed writes | 163-01 | COVERED | Explicit invariants and verification. |
| CONTEXT | D-02 | Attribute 57014, then repair one local DB seam | 163-01 | COVERED | Precondition blocks ambiguous repair. |
| CONTEXT | D-03 | Preserve discovery, guards, widths, themes, overflow, clipping | 163-02 | COVERED | Every focused proof runs full spec. |
| CONTEXT | D-04 | Diagnose readiness separately and repair one finite local browser seam | 163-02 | COVERED | Precondition blocks ambiguous repair. |
| CONTEXT | D-05 | Repeated focused proof before integration verdict | 163-01..03 | COVERED | Count=3 first attempts are ordered before gates. |
| CONTEXT | D-06 | Protected topology/deadlines unchanged; no broad retry/global substitute | 163-03 | COVERED | Blob comparison and exact-SHA protected evidence. |

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-03-SUMMARY.md` when done.
</output>
