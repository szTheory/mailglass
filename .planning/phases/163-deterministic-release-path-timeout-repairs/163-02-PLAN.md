---
phase: 163-deterministic-release-path-timeout-repairs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/test/support/operator_browser_server.ex
  - mailglass_admin/playwright.config.cjs
  - mailglass_admin/e2e/gallery-matrix.spec.js
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
autonomous: true
requirements: [DTRM-03, DTRM-04]
must_haves:
  truths:
    - "D-03/DTRM-03: Gallery proof retains live specimen discovery, more-than-50 non-vacuity, every required stress cell, widths 320/390/768/1440, themes light/dark/system, and every overflow and 320px clipping assertion in both tests."
    - "D-04/DTRM-03: Existing boot-stage, TCP, `/ops/browser-ready`, and login probes distinguish server readiness from matrix execution before any timing boundary changes."
    - "D-04/DTRM-03: A repair is confined to the demonstrated readiness or individual-test boundary, remains finite, retains the single server owner and bounded lifecycle, and leaves the config-wide 30-second default unless individual-test evidence selects a local override."
    - "D-05/DTRM-04: Three consecutive one-worker focused gallery invocations are first-attempt green after repair; evidence records command, Node/Playwright toolchain, repetition count, per-run readiness/body/total elapsed milliseconds and ranges, first-attempt result, discovered cell count, and attributed boundary."
    - "D-06/DTRM-04: No UI, dependency, matrix, worker, retry, Playwright-global timeout, web-server-global lifetime, CI topology, or job deadline change manufactures browser proof."
    - "DTRM-03 precision: readiness/body timing uses monotonic integer milliseconds with equality counted as bound exhaustion; no wall-clock rounding or retry timing contributes to a pass."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md"
      provides: "Append-only readiness-versus-body attribution, repair-selection, matrix-cardinality, and repeated-proof record"
      contains: "first-attempt"
    - path: "mailglass_admin/e2e/gallery-matrix.spec.js"
      provides: "Complete live-discovery matrix plus a local finite deadline only if body evidence selects it"
      contains: "MATRIX_WIDTHS"
    - path: "mailglass_admin/test/support/operator_browser_server.ex"
      provides: "Existing staged readiness path plus durable monotonic timing labels only if current output cannot attribute boot"
      contains: "/ops/browser-ready"
  key_links:
    - from: "mailglass_admin/playwright.config.cjs"
      to: "mailglass_admin/test/support/operator_browser_server.ex"
      via: "webServer command and readiness URL"
      pattern: "OperatorBrowserServer\.run|browserReadyPath"
    - from: "server readiness completion"
      to: "mailglass_admin/e2e/gallery-matrix.spec.js"
      via: "Playwright starts the full live-discovery matrix only after webServer readiness"
      pattern: "gallery-matrix\.spec\.js"
    - from: "focused gallery proof"
      to: "163-BROWSER-TIMEOUT-EVIDENCE.md"
      via: "separate readiness, body, total, first-attempt, and coverage-cardinality records"
      pattern: "readiness|matrix body|first-attempt"
  flagged_assumptions:
    - "FLAGGED-UNVERIFIED — DTRM-03 boundary: the failing owner is unknown, so the selected readiness or test timeout minimum, maximum, exact threshold, and one-integer-millisecond-below/above behavior must be derived from measured reproduction before Task 2 can encode or document it."
  prohibitions:
    - "FLAGGED-UNVERIFIED — MUST NOT relax a global Playwright/web-server/CI deadline or increase workers to conceal readiness or matrix delay."
    - "FLAGGED-UNVERIFIED — MUST NOT add retries or accept a retry-assisted pass as focused browser proof."
    - "FLAGGED-UNVERIFIED — MUST NOT remove discovered cells, required stress cells, widths, themes, overflow checks, clipping checks, or expose fixture PII in timing diagnostics."
---

<objective>
Trace one real gallery run through server readiness and the complete matrix, attribute the timeout to exactly one clock, repair only that clock, and establish repeatable first-attempt-visible browser proof.

Purpose: Satisfy DTRM-03 and the focused portion of DTRM-04 while retaining the entire discovered gallery contract and bounded one-worker lifecycle.
Output: One append-only browser timeout evidence record and the smallest evidence-authorized readiness or individual-test change with full-matrix regression proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-PATTERNS.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
@mailglass_admin/test/support/operator_browser_server.ex
@mailglass_admin/playwright.config.cjs
@mailglass_admin/e2e/gallery-matrix.spec.js
@mailglass_admin/package.json
@.github/workflows/ci.yml

<interfaces>
- `OperatorBrowserServer.run!/0` owns app startup, `AdminBootstrap`, one Sandbox server owner, fixtures, TCP and HTTP readiness probes, then sleeps while Playwright runs.
- `playwright.config.cjs` has a 30,000ms config-wide test timeout, CI retry policy, one 300,000ms bounded webServer lifecycle, and `/ops/browser-ready` readiness URL.
- `gallery-matrix.spec.js` owns two tests, live discovery, the `> 50` guard, `STRESS_CELLS`, `MATRIX_WIDTHS`, `MATRIX_THEMES`, overflow checks, and 320px clipping checks.
- `npm run test:operator-browser` builds assets and invokes Playwright with `--workers=1`; this external contract remains unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one complete gallery run through boot readiness and matrix-body clocks</name>
  <reversibility rating="costly">Misattributing the clock can hide a server boot regression or weaken the full browser matrix, so ambiguous measurements cannot authorize a repair.</reversibility>
  <files>mailglass_admin/test/support/operator_browser_server.ex, mailglass_admin/e2e/gallery-matrix.spec.js, .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-PATTERNS.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-VALIDATION.md
    - mailglass_admin/test/support/operator_browser_server.ex
    - mailglass_admin/playwright.config.cjs
    - mailglass_admin/e2e/gallery-matrix.spec.js
    - mailglass_admin/package.json
  </read_first>
  <action>Per D-03 and D-04, build admin assets and run the complete gallery spec with the existing config and `--workers=1`, no added retry, and no narrowed test selector. Use a finite three-invocation diagnostic budget and preserve first-attempt failures. Correlate existing server stage output, TCP probe, `/ops/browser-ready`, and login probe with Playwright test start/finish. Add stable monotonic integer-millisecond stage labels only where current output cannot distinguish readiness completion from the two matrix bodies; add no endpoint, polling service, payload dump, or UI change. Capture discovered cell count, all required stress-cell checks, each width/theme axis, the last completed phase/cell for a failure, readiness elapsed, each test-body elapsed, total elapsed, config timeout owner, and Node/Playwright versions in `163-BROWSER-TIMEOUT-EVIDENCE.md`. Classify exactly one verdict: server boot, TCP/HTTP readiness, discovery/body test 1, stress/body test 2, or `unattributed`. If the timeout cannot be reproduced or the verdict remains `unattributed`, stop after honest evidence and do not begin Task 2.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build &amp;&amp; npx playwright test e2e/gallery-matrix.spec.js --config=playwright.config.cjs --workers=1; test -s ../.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Evidence names exactly one timeout owner, or explicitly records `unattributed` and halts before repair.
    - Readiness and each matrix body have separate monotonic millisecond measurements and first-attempt outcomes.
    - Both gallery tests, live discovery, non-vacuity, required stress cells, all four widths, all three themes, overflow, and 320px clipping remain executed.
    - No added retry, worker, route, UI mutation, dependency, global timeout, webServer lifetime, or CI change appears.
    - Durable labels contain stage/test IDs and timing only, never fixture payloads, recipient/message content, or tenant secrets.
  </acceptance_criteria>
  <done>A real one-worker gallery invocation reaches the existing server and complete live matrix and leaves a single-owner, non-PII timeout attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair only the attributed browser clock and prove three complete first attempts</name>
  <precondition>`163-BROWSER-TIMEOUT-EVIDENCE.md` contains one reproduced timeout and one unambiguous readiness or individual-test owner; an `unattributed` verdict halts this task.</precondition>
  <reversibility rating="costly">Changing shared Playwright defaults or server lifetime would affect every browser test, so the permitted decision stays at a proven readiness or named-test seam.</reversibility>
  <files>mailglass_admin/test/support/operator_browser_server.ex, mailglass_admin/playwright.config.cjs, mailglass_admin/e2e/gallery-matrix.spec.js, .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - mailglass_admin/test/support/operator_browser_server.ex
    - mailglass_admin/playwright.config.cjs
    - mailglass_admin/e2e/gallery-matrix.spec.js
    - mailglass_admin/package.json
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - "A readiness-owned failure changes only the existing bounded readiness path and still reaches the same `/ops/browser-ready` contract with one server owner."
    - "A matrix-body-owned failure uses a finite timeout on only the affected test/describe boundary; the config-wide 30,000ms default remains unchanged."
    - "The complete live matrix preserves discovered count > 50, required stress cells, 320/390/768/1440, light/dark/system, overflow, and 320px clipping on every proof run."
    - "Three consecutive focused invocations are green on their first and only attempts with `--workers=1`; configured CI retry behavior is not used as focused proof."
  </behavior>
  <action>Start RED with a regression or evidence assertion at the exact owner from Task 1. Select the repair mechanically: a server-stage/readiness failure permits only a bounded adjustment inside the existing server/readiness seam while retaining the same endpoint, single owner, and 300,000ms lifecycle ceiling; a named matrix-body failure permits only a finite test/describe-local Playwright timeout derived from the slowest measured body plus explicit headroom, leaving the config-wide 30,000ms default intact. Do not edit gallery markup, specimen selection, loop axes/assertions, `package.json`, dependency locks, retry policy, workers, `.github/workflows/ci.yml`, or job deadlines. Use monotonic integer milliseconds; treat equality with the chosen bound as exhausted and record measured one-millisecond-below/equal/above behavior when controllable, otherwise retain the DTRM-03 boundary assumption as flagged. Run the entire gallery spec three consecutive times with the existing config, one worker, and no newly added retry. Append command, Node/Playwright toolchain, count=3, every first-attempt result, readiness/body/total elapsed values and ranges, discovered cardinality, chosen boundary/value/rationale, and edge result to the evidence file per D-05/D-06 and DTRM-04.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; npx playwright test e2e/gallery-matrix.spec.js --config=playwright.config.cjs --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - Regression/evidence is red before and green after the local repair at the one attributed readiness or named-test seam.
    - Three consecutive complete gallery invocations are first-attempt green, one-worker, retry-free for focused proof, and recorded with all coverage cardinalities.
    - `playwright.config.cjs` retains its 30,000ms default, CI retry policy, and 300,000ms webServer lifecycle unless a readiness-only implementation change is proven without increasing that lifecycle ceiling.
    - `package.json`, admin UI source, dependencies, workflow topology, and job deadlines remain unchanged.
    - Evidence resolves the DTRM-03 boundary probe or retains it as explicit flagged-unverified; timing precision is monotonic integer milliseconds with equality treated as exhaustion.
  </acceptance_criteria>
  <done>The reproduced gallery timeout is narrowly repaired and the complete live matrix is repeatedly green on first attempt without reduced coverage or broadened global policy.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md` — new append-only evidence file; fields are command, Node/Playwright toolchain, repetition count, first-attempt result, monotonic readiness/body/total elapsed milliseconds and ranges, timeout owner/value, discovered count, stress/width/theme coverage, repair verdict, and flagged boundary status.
- Conditional private timing-label helper(s) in `operator_browser_server.ex` or `gallery-matrix.spec.js` only when current stage output is insufficient; no endpoint, public symbol, field, API, or flag is added.
- Conditional finite test/describe-local timeout in `gallery-matrix.spec.js` only when body measurements prove that owner; no config-global timeout change.
- No new dependency, package script, worker, retry, admin UI source change, workflow, or job deadline is expected.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright webServer → readiness endpoint | The browser runner must distinguish a bounded server boot/readiness failure from test-body execution. |
| Live gallery DOM → matrix assertions | Runtime-discovered specimens and geometry cross into the protected coverage contract. |
| Browser output → evidence | Timing and test IDs are useful, while seeded fixture content may contain sensitive data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-05 | Denial of Service | readiness/test timing repair | high | mitigate | Attribute readiness versus body first; keep a finite local seam; retain global, server-lifecycle, worker, and job bounds. |
| T-163-06 | Repudiation | Playwright retry/first-attempt reporting | high | mitigate | Run focused proof without a new retry and record every first attempt separately from existing CI retry configuration. |
| T-163-07 | Tampering | live gallery matrix | high | mitigate | Assert discovery non-vacuity, required stress cells, all width/theme axes, overflow, and clipping on every repeated run. |
| T-163-08 | Information Disclosure | browser timing diagnostics | high | mitigate | Persist only stage/test IDs, counts, axes, and timing; exclude rendered text, payloads, addresses, messages, and tenant secrets. |
| T-163-SC | Tampering | package supply chain | low | accept | No package install, dependency, lockfile, or action reference change is planned. |

## ASVS L1 Mapping

| Category | Applicability | Mitigation / Test |
|----------|---------------|-------------------|
| V5 Input Validation | Applicable | Live specimen discovery must remain non-vacuous and include required stress cells before matrix execution. |
| V7 Error Handling and Logging | Applicable | Readiness and body failures remain distinct, first-attempt-visible, and non-PII. |
| V14 Configuration | Applicable | Worker, retries, global timeout, server lifecycle, CI topology, and job deadlines remain unchanged. |
</threat_model>

<verification>
Execute the Task 1 focused spec and Task 2 full focused spec exactly as written. Confirm `163-BROWSER-TIMEOUT-EVIDENCE.md` contains one reproduced timeout, one attributed source boundary, count=3 first-attempt proof, separate readiness/body/total ranges, complete matrix cardinality, and resolved or explicitly flagged DTRM-03 boundary status. Run `git diff --check` on the owned files.
</verification>

<success_criteria>
- DTRM-03 has reproduced readiness-versus-body attribution and a regression-backed repair at one local finite seam.
- DTRM-04 has three complete, one-worker, first-attempt-visible focused passes without retries, matrix reduction, UI changes, or global deadline expansion.
- Any unresolved exact threshold boundary remains visibly flagged rather than silently inferred.
</success_criteria>

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-02-SUMMARY.md` when done.
</output>
