---
phase: 163-deterministic-release-path-timeout-repairs
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/test/support/operator_browser_server.ex
  - mailglass_admin/playwright.config.cjs
  - mailglass_admin/e2e/gallery-matrix.spec.js
  - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
autonomous: false
gap_closure: true
requirements: [DTRM-03, DTRM-04]
must_haves:
  truths:
    - "D-03/DTRM-03: Historical reconstruction and current proof retain live discovery, the >50 guard, all stress cells, 320/390/768/1440, light/dark/system, overflow, and 320px clipping checks."
    - "D-04/DTRM-03: Only an exact-SHA reproduced timeout attributed to readiness or one named test body can authorize a finite local repair."
    - "D-05/DTRM-04: The repaired full gallery spec passes three consecutive one-worker, zero-retry first attempts with complete readiness/body/total timings."
    - "D-06/DTRM-04: Browser proof leaves UI, package scripts, config-global timeout, retry policy, web-server lifecycle, worker count, CI topology, and job deadlines unchanged."
    - "DTRM-03: An inconclusive bounded search pauses at a blocking maintainer decision; only supplied immutable failing-run evidence authorizes repair, while re-scope/defer leaves the requirement explicitly open."
  artifacts:
    - path: ".planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md"
      provides: "Append-only original-run identity, historical reconstruction, attribution, repair, coverage, edge, and three-run proof ledger"
    - path: "mailglass_admin/e2e/gallery-matrix.spec.js"
      provides: "Complete matrix and conditional individual-test finite timeout/regression at an attributed body seam"
    - path: "mailglass_admin/test/support/operator_browser_server.ex"
      provides: "Existing staged readiness timings and conditional readiness-only regression/repair"
  key_links:
    - from: "original protected gallery failure"
      to: "disposable exact-SHA reconstruction"
      via: "run/job URL, full SHA, Playwright result/trace, and boot/body timing"
      pattern: "failing_sha|protected_run_id|timeout"
    - from: "readiness completion"
      to: "named gallery body"
      via: "existing boot/TCP/HTTP stages followed by test_body_start/test_body_finish"
      pattern: "browser-ready|test_body"
    - from: "local repair"
      to: "DTRM-04 focused proof"
      via: "three complete one-worker zero-retry first attempts"
      pattern: "workers=1|repetition: 3"
  flagged_assumptions:
    - "DTRM-03 boundary/precision is unresolved until the selected local deadline records minimum/maximum and one millisecond below/equal/above behavior; equality is exhausted."
    - "DTRM-04 unclassified/manual-review candidate is resolved by executable coverage/source backstops plus complete first-attempt timing rows; ambiguity remains non-pass."
  prohibitions:
    - "RESOLVED/test — DTRM-03 transparency: MUST NOT call three current green diagnostics a repair without an exact-SHA reproduced timeout and single owner."
    - "RESOLVED/test — DTRM-04 proof integrity: MUST NOT remove specimens/axes/assertions, add retries/workers, change UI, or broaden config-global/server/CI deadlines."
    - "RESOLVED/judgment — DTRM-04 transparency: MUST NOT infer missing timing labels or overwrite an initial failure with a later pass."
---

<objective>
Recover the immutable original gallery timeout, reproduce and attribute it in a disposable exact-SHA environment, then repair only the demonstrated current readiness or named-test seam and prove the full matrix three times.

Purpose: Close the browser failures from `163-VERIFICATION.md` while retaining every matrix dimension and bounded runner contract.
Output: An appended browser ledger plus either an evidence-authorized local repair or a blocking maintainer decision that records supplied immutable evidence versus an explicit DTRM-03 re-scope/defer choice.

Execution-path status: Plans 163-04 through 163-08 supersede the failed/inconclusive execution path of 163-01 through 163-03 for gap closure. Preserve 163-01 through 163-03 and their summaries as immutable historical evidence; they are not dependencies or resumable prerequisites for this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-CONTEXT.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-RESEARCH.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-VERIFICATION.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
@.planning/phases/163-deterministic-release-path-timeout-repairs/163-02-SUMMARY.md
@mailglass_admin/e2e/gallery-matrix.spec.js
@mailglass_admin/playwright.config.cjs
@mailglass_admin/test/support/operator_browser_server.ex
@mailglass_admin/package.json

<interfaces>
- `playwright.config.cjs` starts `OperatorBrowserServer.run!/0`, waits on `/ops/browser-ready`, and retains a 300-second bounded server lifecycle plus a 30-second config-wide test default.
- `gallery-matrix.spec.js` owns two named tests and the complete live-discovery/stress matrix; an attributed body repair belongs at an individual test/describe boundary, not global config.
- `npm run test:operator-browser` owns the protected one-worker command and remains unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reconstruct the original gallery timeout at its immutable failing SHA</name>
  <reversibility rating="reversible">The task uses disposable reconstruction and append-only evidence without changing current UI or timeout policy.</reversibility>
  <files>.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md, mailglass_admin/test/support/operator_browser_server.ex, mailglass_admin/e2e/gallery-matrix.spec.js</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-VERIFICATION.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-02-SUMMARY.md
    - mailglass_admin/test/support/operator_browser_server.ex
    - mailglass_admin/test/support/admin_bootstrap.ex
    - mailglass_admin/playwright.config.cjs
    - mailglass_admin/e2e/gallery-matrix.spec.js
    - mailglass_admin/e2e/structural.spec.js
    - mailglass_admin/package.json
    - .github/workflows/ci.yml
  </read_first>
  <action>Per D-03/D-04, locate the original observed gallery failure from retained Playwright artifacts or read-only GitHub Actions evidence. Inspect at most the ten most recent candidate protected operator-browser runs with a failed gallery job; require terminal run/job URL, full head SHA, event, exact command/toolchain, first failing test title, and trace/result evidence. Unavailable, malformed, different-spec, retry-only, or owner-ambiguous evidence is `inconclusive` and authorizes no repair. If an immutable failing identity exists, create a disposable `mktemp -d` checkout/worktree at that SHA, build assets, preserve its locked browser/toolchain and one-worker command, and execute the exact gallery spec at most three first attempts with retry disabled. Follow `mailglass_admin/test/support/admin_bootstrap.ex` as the server-owner lifecycle analog: retain the single owner process, established setup/seed ordering, bounded TCP and `/ops/browser-ready` probes, and stage-labelled output. Follow `mailglass_admin/e2e/structural.spec.js` as the browser structural-test analog: use its locator, geometry, and explicitly bounded wait conventions while retaining the gallery spec's live discovery and full matrix. Record integer-millisecond readiness/body/total values, discovered count and axes, the exact stage that exhausts its existing bound, trace/result identity, and cleanup. Append one `attributed` or `inconclusive` search verdict. If no timeout reproduces or ownership is not singular, stop at Task 2; the search verdict alone does not complete DTRM-03.</action>
  <verify>
    <automated>test -s .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md &amp;&amp; rg -n "failing_sha|historical reconstruction|attributed|inconclusive" .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md &amp;&amp; node --check mailglass_admin/e2e/gallery-matrix.spec.js &amp;&amp; mix format --check-formatted mailglass_admin/test/support/operator_browser_server.ex</automated>
  </verify>
  <acceptance_criteria>
    - Any reproduction claim is bound to exact run/job identity, full failing SHA, named test/stage, and retained result/trace evidence.
    - At most ten remote candidates and three disposable first attempts are inspected; retry success is never attribution or proof.
    - An attributed verdict names exactly one readiness or test-body owner; otherwise the ledger records inconclusive and no repair follows.
    - Current gallery source still contains every protected discovery, count, stress, viewport, theme, overflow, and clipping contract.
  </acceptance_criteria>
  <done>The bounded historical search and append-only ledger entry are complete; an attributed result carries immutable evidence, while an inconclusive result requires Task 2's blocking maintainer decision and cannot complete DTRM-03.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Resolve missing immutable gallery failure evidence</name>
  <decision>Choose how DTRM-03 proceeds after the bounded historical search: supply immutable failing-run evidence, or deliberately re-scope/defer the requirement.</decision>
  <context>An inconclusive ledger is not reproduction or repair. No executor may speculate about a readiness/test seam, run the repair task, unlock Plan 163-06, or claim DTRM-03 complete without immutable gallery-timeout evidence.</context>
  <options>
    <option id="supply-evidence">
      <name>Supply immutable evidence</name>
      <pros>Preserves DTRM-03 and permits evidence-gated attribution and repair.</pros>
      <cons>Requires a terminal run/job URL, full 40-character failing SHA, exact command/toolchain, named failing test/stage, and retained Playwright result or trace evidence; the executor must validate and append it before continuing.</cons>
    </option>
    <option id="rescope-defer-dtrm-03">
      <name>Re-scope or defer DTRM-03</name>
      <pros>Records an explicit maintainer decision when immutable evidence cannot be recovered.</pros>
      <cons>Task 3 and downstream Plans 163-06 through 163-08 remain blocked for DTRM-03; phase/requirements artifacts must be revised through the appropriate planning workflow before any completion claim.</cons>
    </option>
  </options>
  <resume-signal>Reply `supply-evidence` with the immutable run/job URL, 40-character failing SHA, exact command/toolchain, named failing test/stage, and result/trace evidence location; or reply `rescope-defer-dtrm-03` and explicitly state the approved scope/deferral destination.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regress, narrowly repair, and repeat the attributed gallery path</name>
  <precondition>Task 2 was resolved through `supply-evidence`, and the appended browser ledger contains validated immutable evidence plus a reproduced timeout bound to one failing SHA and one unambiguous readiness or named test-body owner; the re-scope/defer branch does not satisfy this precondition.</precondition>
  <reversibility rating="reversible">A finite readiness-local or individual-test deadline can be removed with its regression without changing public behavior.</reversibility>
  <files>mailglass_admin/test/support/operator_browser_server.ex, mailglass_admin/playwright.config.cjs, mailglass_admin/e2e/gallery-matrix.spec.js, .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md
    - mailglass_admin/test/support/operator_browser_server.ex
    - mailglass_admin/test/support/admin_bootstrap.ex
    - mailglass_admin/playwright.config.cjs
    - mailglass_admin/e2e/gallery-matrix.spec.js
    - mailglass_admin/e2e/structural.spec.js
    - mailglass_admin/package.json
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - The attributed readiness or named body operation has a fail-first regression tied to its existing finite clock.
    - A finite owner-local repair turns the regression green without changing UI, global defaults, server lifecycle, retries, workers, or matrix coverage.
    - Three consecutive full gallery invocations pass with one worker, zero retry, and complete first-attempt timing/coverage rows.
    - The selected integer-millisecond deadline records below/equal/above behavior, with equality exhausted.
  </behavior>
  <action>Start RED at the single attributed owner. If boot/readiness exhausted, repair only the existing readiness/probe boundary and retain the 300-second lifecycle, following `admin_bootstrap.ex` for single-owner setup/teardown ordering and the existing bounded readiness probes. If discovery or stress body exhausted, set a finite timeout only on that named test/describe boundary and retain the config-wide 30-second default, following `structural.spec.js` for locator, geometry, and explicitly bounded wait conventions. Do not alter selectors, specimens, dimensions, assertions, UI, package script, retries, workers, dependencies, workflow, or CI deadlines. Derive the finite local value from the reproduced slowest elapsed time plus explicit measured headroom and test below/equal/above semantics. Run the complete spec three separate times with `--workers=1`, retry disabled, on first attempt; append every readiness/body/total value, discovered count, full coverage inventory, result/trace identity, source diff backstop, local bound/rationale, and any failure per D-05/D-06.</action>
  <verify>
    <automated>cd mailglass_admin &amp;&amp; mix mailglass_admin.assets.build &amp;&amp; npx playwright test e2e/gallery-matrix.spec.js --config=playwright.config.cjs --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - The regression is observed red before repair and green after repair at the exact attributed clock.
    - The repair is readiness-local or individual-test-local and finite; global defaults, bounded lifecycle, one-worker package script, retry policy, CI, and UI remain unchanged.
    - Three complete first attempts have retained timings and all live-discovery/matrix checks; a missing timing row or retry invalidates the proof.
    - DTRM-03 boundary/precision and DTRM-04 review status are resolved or remain explicitly flagged with final sign-off blocked.
  </acceptance_criteria>
  <done>DTRM-03 has an attributed regression-backed repair and DTRM-04 has three honest focused browser proofs, or execution remains explicitly blocked before speculative change.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/163-deterministic-release-path-timeout-repairs/163-BROWSER-TIMEOUT-EVIDENCE.md` — appended fields: `failing_sha`, original run/job/result identity, reconstruction attempts, attributed stage/test, threshold edges, repair identity, and three complete timing/coverage rows.
- One private readiness or Playwright regression at the attributed boundary; no UI or public API symbol.
- One conditional finite local timeout at the proven readiness or individual-test seam; no global configuration change.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub/Playwright artifact → reconstruction | Failure identity and trace must be immutable and name this exact spec. |
| Server readiness → browser body | Separate clocks must not be conflated when choosing a repair. |
| Browser result → evidence | Retry or incomplete timing/coverage data must not become a green claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-163-17 | Spoofing | original gallery failure | high | mitigate | Require exact SHA, run/job/result identity, test title, and trace before authority. |
| T-163-18 | Tampering | matrix coverage/global policy | high | mitigate | Execute full source-backed inventory and byte-check package/config/workflow protected settings. |
| T-163-19 | Repudiation | first-attempt browser evidence | high | mitigate | Append all three complete timings and retain initial failures; zero retry. |
| T-163-20 | Denial of Service | selected browser clock | high | mitigate | Keep the change finite and local to the proven owner; global/server/job bounds remain. |
| T-163-SC | Tampering | package supply chain | low | accept | No package install, lockfile, action, or dependency change is in scope. |
</threat_model>

<verification>
Require an exact-SHA attributed reconstruction, fail-first local regression, complete unchanged matrix, and three recorded one-worker zero-retry first attempts. Inconclusive reconstruction remains blocked.
</verification>

<success_criteria>
- DTRM-03 is closed by immutable reproduction, single-clock attribution, and a narrow finite repair.
- DTRM-04 focused proof consists of three complete first attempts with all matrix coverage and bounded policies intact.
- All browser edge candidates and kept prohibitions are resolved or visibly block later integration.
</success_criteria>

<output>
Create `.planning/phases/163-deterministic-release-path-timeout-repairs/163-05-SUMMARY.md` when done.
</output>
