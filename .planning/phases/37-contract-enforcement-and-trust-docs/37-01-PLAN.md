---
phase: "37"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "guides/testing.md"
  - "lib/mailglass/test_assertions.ex"
  - "test/support/mailer_case.ex"
  - "test/support/oban_helpers.ex"
  - "test/mailglass/docs/testing_guide_test.exs"
autonomous: true
requirements:
  - PROOF-03
must_haves:
  truths:
    - "Adopters have one canonical testing guide that teaches the supported `deliver/2` and `deliver_later/2` paths before advanced lanes."
    - "The guide states helper and MailerCase behavior exactly as shipped, including Fake storage semantics, timeout-based waiting, ownership transfer, and Oban constraints."
    - "Cross-process and Oban guidance stays narrow, explicit, and recommendation-first instead of presenting a large mode matrix."
  artifacts:
    - path: "guides/testing.md"
      provides: "Canonical adopter-facing testing contract for inline, async, Oban, cross-process, and PubSub/webhook workflows"
      contains: "## deliver/2 baseline"
    - path: "test/mailglass/docs/testing_guide_test.exs"
      provides: "Deterministic docs proof for the testing guide's exact promises"
      contains: "canonical testing guide"
    - path: "test/support/mailer_case.ex"
      provides: "Source-of-truth MailerCase docs and setup semantics referenced by the guide"
      contains: "Fake.allow/2"
  key_links:
    - from: "guides/testing.md"
      to: "lib/mailglass/test_assertions.ex"
      via: "helper semantics for `last_mail/0`, `wait_for_mail/1`, and PubSub assertions"
      pattern: "last_mail|wait_for_mail|assert_mail_delivered|assert_mail_bounced"
    - from: "guides/testing.md"
      to: "test/support/mailer_case.ex"
      via: "default inline behavior, ownership transfer, and Oban mode guidance"
      pattern: "async: false|set_mailglass_global|Fake.allow/2|Oban"
    - from: "test/mailglass/docs/testing_guide_test.exs"
      to: "guides/testing.md"
      via: "exact section and wording assertions for the canonical testing contract"
      pattern: "deliver/2 baseline|deliver_later/2 baseline|cross-process|PubSub"
---

<objective>
Publish the canonical adopter-facing testing contract and lock it to the shipped helper and MailerCase semantics.

Purpose: Satisfy PROOF-03 without widening the public testing surface or inventing a large mode matrix. The docs must teach the stable default path first, then the explicit exceptions.
Output: A rewritten `guides/testing.md`, any necessary helper/module-doc alignment, and deterministic docs proof tied to the existing testing behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-PATTERNS.md
@guides/testing.md
@lib/mailglass/test_assertions.ex
@test/support/mailer_case.ex
@test/support/oban_helpers.ex
@test/mailglass/test_assertions_test.exs
@test/mailglass/mailer_case_test.exs
@test/mailglass/test_assertions_pubsub_test.exs
@test/mailglass/docs_contract_test.exs
@test/support/docs_helpers.ex

<interfaces>
From lib/mailglass/test_assertions.ex:
```elixir
@spec last_mail() :: Message.t() | nil
@spec wait_for_mail(timeout()) :: Message.t()
def assert_mail_delivered(delivery_or_id, timeout \\ 100)
def assert_mail_bounced(delivery_or_id, timeout \\ 100)
```

From test/support/mailer_case.ex:
```elixir
Application.put_env(:mailglass, :async_adapter_impl, Mailglass.Outbound.AsyncAdapter.Inline)
Mailglass.Adapters.Fake.set_shared(self())
defdelegate set_mailglass_global(context), to: Mailglass.MailerCase
```

From test/support/oban_helpers.ex:
```elixir
def maybe_create_oban_jobs do
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Rewrite guides/testing.md into the canonical testing contract</name>
  <files>guides/testing.md</files>
  <read_first>guides/testing.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-PATTERNS.md, lib/mailglass/test_assertions.ex, test/support/mailer_case.ex, test/support/oban_helpers.ex, test/mailglass/test_assertions_test.exs, test/mailglass/mailer_case_test.exs, test/mailglass/test_assertions_pubsub_test.exs</read_first>
  <acceptance_criteria>
    - `guides/testing.md` contains the exact headings `## deliver/2 baseline`, `## deliver_later/2 baseline`, `## Optional Oban lanes`, `## Cross-process and browser ownership`, `## PubSub and webhook assertions`, and `## Footguns and strict-CI posture`.
    - `guides/testing.md` states that `last_mail/0` reads Fake-backed delivery state rather than consuming the process mailbox.
    - `guides/testing.md` states that `wait_for_mail/1` waits up to a timeout and fails if no `{:mail, _}` arrives.
    - `guides/testing.md` documents `Fake.allow/2` before shared/global mode and names shared/global mode as the non-async fallback.
    - `guides/testing.md` documents `:inline` and `:manual` as the only Oban lanes and states both require `async: false` plus the `oban_jobs` table.
  </acceptance_criteria>
  <action>Rewrite `guides/testing.md` from top to bottom as the one canonical adopter-facing testing contract required by D-37-06 through D-37-10. Use this exact narrative order: `deliver/2` baseline, `deliver_later/2` baseline, optional Oban lanes, cross-process/browser/LiveView ownership transfer, PubSub/webhook assertion lane, then footguns and strict-CI posture. In the `deliver/2` and `deliver_later/2` sections, teach the stable default path as `Mailglass.Adapters.Fake` plus `Mailglass.TestAssertions` plus `Mailglass.MailerCase`. In the helper section, state the exact semantics already shipped: `last_mail/0` reads Fake storage and does not consume the mailbox; `wait_for_mail/1` is timeout-based; PubSub assertions are for delivery/webhook state, not mailbox state. In the cross-process section, recommend `Fake.allow/2` and normal sandbox ownership transfer first, and document shared/global mode only as the explicit `async: false` fallback via `setup :set_mailglass_global`. In the Oban section, document only `:inline` and `:manual`, both as `async: false` lanes requiring the `oban_jobs` table. Do not introduce global/shared as the baseline, do not promise optional deps by default, and do not describe any helper behavior that the current implementation does not ship.</action>
  <verify>
    <automated>rg -n "^## deliver/2 baseline$|^## deliver_later/2 baseline$|^## Optional Oban lanes$|^## Cross-process and browser ownership$|^## PubSub and webhook assertions$|^## Footguns and strict-CI posture$|last_mail/0 reads Fake|wait_for_mail/1|Fake.allow/2|shared/global|async: false|oban_jobs" guides/testing.md</automated>
  </verify>
  <done>The testing guide now teaches one honest, stable path first and explains the explicit Oban and cross-process exceptions without drifting from shipped behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic proof for the testing guide and align public module docs if needed</name>
  <files>lib/mailglass/test_assertions.ex, test/support/mailer_case.ex, test/support/oban_helpers.ex, test/mailglass/docs/testing_guide_test.exs</files>
  <read_first>lib/mailglass/test_assertions.ex, test/support/mailer_case.ex, test/support/oban_helpers.ex, test/mailglass/test_assertions_test.exs, test/mailglass/mailer_case_test.exs, test/mailglass/test_assertions_pubsub_test.exs, test/support/docs_helpers.ex, guides/testing.md</read_first>
  <acceptance_criteria>
    - `test/mailglass/docs/testing_guide_test.exs` exists and contains a test description mentioning `canonical testing guide`.
    - `test/mailglass/docs/testing_guide_test.exs` asserts the guide mentions `last_mail/0`, `wait_for_mail/1`, `Fake.allow/2`, `async: false`, and `oban_jobs`.
    - Any updated moduledoc text in `lib/mailglass/test_assertions.ex`, `test/support/mailer_case.ex`, or `test/support/oban_helpers.ex` matches the guide's exact semantics and does not describe mailbox or Oban behavior differently.
    - `mix test test/mailglass/docs/testing_guide_test.exs test/mailglass/test_assertions_test.exs test/mailglass/mailer_case_test.exs test/mailglass/test_assertions_pubsub_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>Create `test/mailglass/docs/testing_guide_test.exs` as the dedicated proof surface for PROOF-03. Use the existing docs-helper style from `test/support/docs_helpers.ex` to assert the exact section headings and promise-level statements from Task 1, including the `last_mail/0` Fake-storage wording, the timeout-based `wait_for_mail/1` behavior, the recommendation-first `Fake.allow/2` ownership-transfer guidance, the `shared/global` fallback wording, and the Oban `:inline` / `:manual` constraints (`async: false`, `oban_jobs` table). Then inspect `lib/mailglass/test_assertions.ex`, `test/support/mailer_case.ex`, and `test/support/oban_helpers.ex` and update their moduledoc wording only where it is necessary to keep the public module docs consistent with the new guide. Do not change runtime semantics in this task unless a current test proves the docs are wrong and a tiny documentation-only code comment or moduledoc fix cannot resolve it.</action>
  <verify>
    <automated>mix test test/mailglass/docs/testing_guide_test.exs test/mailglass/test_assertions_test.exs test/mailglass/mailer_case_test.exs test/mailglass/test_assertions_pubsub_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The canonical testing guide is now protected by deterministic assertions and any public helper/MailerCase docs speak with one exact contract voice.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing docs -> adopter test harness | Adopters copy the guide into their own test helpers and CI setup. |
| Helper docs -> cross-process ownership semantics | Inaccurate docs can make async-safe tests silently flaky or broaden global/shared usage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering / Misrepresentation | `guides/testing.md` | mitigate | Prove the guide against the shipped helper and MailerCase semantics with exact token and heading assertions. |
| T-37-02 | Denial of Service | `guides/testing.md`, `test/support/mailer_case.ex` | mitigate | Keep Oban guidance narrow and explicit so adopters do not accidentally run async-unsafe modes in merge-blocking CI. |
| T-37-03 | Elevation of Privilege | `guides/testing.md` cross-process section | mitigate | Recommend explicit ownership transfer first and document shared/global only as the non-async fallback. |
</threat_model>

<verification>
- `mix test test/mailglass/docs/testing_guide_test.exs test/mailglass/test_assertions_test.exs test/mailglass/mailer_case_test.exs test/mailglass/test_assertions_pubsub_test.exs --warnings-as-errors`
- `rg -n "deliver/2 baseline|deliver_later/2 baseline|Fake.allow/2|shared/global|async: false|oban_jobs|last_mail/0|wait_for_mail/1" guides/testing.md test/mailglass/docs/testing_guide_test.exs lib/mailglass/test_assertions.ex test/support/mailer_case.ex test/support/oban_helpers.ex`
</verification>

<success_criteria>
Adopters can follow one truthful testing story from the default Fake adapter path through the narrow Oban and cross-process exceptions, and the docs fail tests if those semantics drift.
</success_criteria>

<output>
After completion, create `.planning/phases/37-contract-enforcement-and-trust-docs/37-01-SUMMARY.md`
</output>
