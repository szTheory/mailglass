---
phase: "37"
plan: "03"
type: execute
wave: 2
depends_on:
  - "37-01"
  - "37-02"
files_modified:
  - "mix.exs"
  - "scripts/verify_support_contract.sh"
  - "lib/mix/tasks/mailglass.docs.check.ex"
  - "lib/mix/tasks/mailglass.stability.check.ex"
  - "MAINTAINING.md"
  - "test/mailglass/docs_contract_test.exs"
  - "test/mailglass/stability_contract_test.exs"
  - "mailglass_admin/test/mailglass_admin/stability_contract_test.exs"
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
must_haves:
  truths:
    - "Maintainers can run one semantic repo-root stability proof workflow that composes existing root/admin support-contract authorities and the no-optional-deps compile lane."
    - "The proof workflow fails when canonical testing or admin trust docs drift, when narrow public-surface checks drift, or when sibling-package stability assertions fail."
    - "The enforcement remains lightweight and actionable instead of becoming a manifest snapshot or raw export-diff system."
  artifacts:
    - path: "mix.exs"
      provides: "Semantic repo-root proof alias that wraps the existing support-contract lanes"
      contains: "verify.stability_contract"
    - path: "lib/mix/tasks/mailglass.docs.check.ex"
      provides: "Tier 1 docs token checks for the canonical testing and admin trust docs"
      contains: "guides/testing.md"
    - path: "test/mailglass/stability_contract_test.exs"
      provides: "Compiled-doc and proof-wiring assertions for the core stability workflow"
      contains: "verify.stability_contract"
  key_links:
    - from: "mix.exs"
      to: "scripts/verify_support_contract.sh"
      via: "semantic repo-root proof alias and shell entrypoint match"
      pattern: "verify\\.stability_contract|verify\\.support_contract\\.core"
    - from: "lib/mix/tasks/mailglass.docs.check.ex"
      to: "guides/testing.md"
      via: "testing contract is treated as Tier 1 truth"
      pattern: "guides/testing\\.md"
    - from: "lib/mix/tasks/mailglass.docs.check.ex"
      to: "mailglass_admin/docs/operator-trust.md"
      via: "admin trust contract is treated as Tier 1 truth"
      pattern: "operator-trust"
    - from: "test/mailglass/stability_contract_test.exs"
      to: "mailglass_admin/test/mailglass_admin/stability_contract_test.exs"
      via: "root and sibling stability assertions prove both sides of the documented contract"
      pattern: "since|deprecated|verify.stability_contract"
---

<objective>
Strengthen the existing support-contract workflow into one semantic repo-root stability proof and wire the new trust docs into the lightweight enforcement surface.

Purpose: Satisfy PROOF-01 and PROOF-02 by composing existing repo-native checks, compiled-doc assertions, docs-truth checks, sibling-package contract assertions, and the no-optional-deps compile lane.
Output: A semantic root proof alias/script, updated docs/stability checks, and contract tests that fail when the documented core/admin/testing surfaces drift.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-PATTERNS.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-01-PLAN.md
@.planning/phases/37-contract-enforcement-and-trust-docs/37-02-PLAN.md
@mix.exs
@scripts/verify_support_contract.sh
@lib/mix/tasks/mailglass.docs.check.ex
@lib/mix/tasks/mailglass.stability.check.ex
@MAINTAINING.md
@test/mailglass/docs_contract_test.exs
@test/mailglass/stability_contract_test.exs
@mailglass_admin/test/mailglass_admin/stability_contract_test.exs
@guides/testing.md
@mailglass_admin/docs/operator-trust.md

<interfaces>
From scripts/verify_support_contract.sh:
```bash
mix verify.support_contract.core
cd "$ROOT_DIR/mailglass_admin"
mix verify.support_contract.admin
mix compile --no-optional-deps --warnings-as-errors
```

From mix.exs:
```elixir
"verify.support_contract.core": [...]
"verify.docs.contract": [...]
```

From lib/mix/tasks/mailglass.docs.check.ex:
```elixir
@tier1_paths [...]
@tier1_surface_rules %{...}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the semantic repo-root proof alias and wire canonical trust docs into Tier 1 checks</name>
  <files>mix.exs, scripts/verify_support_contract.sh, lib/mix/tasks/mailglass.docs.check.ex, MAINTAINING.md</files>
  <read_first>mix.exs, scripts/verify_support_contract.sh, lib/mix/tasks/mailglass.docs.check.ex, MAINTAINING.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-CONTEXT.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-01-PLAN.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-02-PLAN.md, guides/testing.md, mailglass_admin/docs/operator-trust.md</read_first>
  <acceptance_criteria>
    - `mix.exs` contains a `verify.stability_contract` alias.
    - `verify.stability_contract` runs the existing core support-contract lane, the admin support-contract lane, and `compile --no-optional-deps --warnings-as-errors`.
    - `scripts/verify_support_contract.sh` either calls `mix verify.stability_contract` directly or otherwise mirrors the exact same three-bucket proof workflow.
    - `lib/mix/tasks/mailglass.docs.check.ex` lists `guides/testing.md` and `mailglass_admin/docs/operator-trust.md` in its Tier 1 path and token rules.
    - `MAINTAINING.md` names `mix verify.stability_contract` as the semantic proof entrypoint and keeps `scripts/verify_support_contract.sh` aligned with it.
  </acceptance_criteria>
  <action>In `mix.exs`, add a semantic alias named `verify.stability_contract` that composes exactly these existing repo-native proof primitives from D-37-01 through D-37-04: `verify.support_contract.core`, a shell into `mailglass_admin` running `mix verify.support_contract.admin`, and `compile --no-optional-deps --warnings-as-errors`. Then update `scripts/verify_support_contract.sh` so the shell entrypoint matches the alias exactly and remains the honest repo-root maintainer entrypoint. In `lib/mix/tasks/mailglass.docs.check.ex`, promote `guides/testing.md` and `mailglass_admin/docs/operator-trust.md` into the Tier 1 docs set and add exact required-token rules that enforce the canonical trust story: testing guide headings and `Fake.allow/2` / Oban wording on the core side, and `MailglassAdmin.Auth`, `subject_id`, `destructive_action`, and replay-vs-reconcile wording on the admin side. Update `MAINTAINING.md` so maintainers are told to use `mix verify.stability_contract` / `scripts/verify_support_contract.sh` as the semantic proof entrypoint, not a new heavy manifest or raw export tool.</action>
  <verify>
    <automated>bash scripts/verify_support_contract.sh && rg -n "verify\\.stability_contract|guides/testing\\.md|operator-trust|Fake.allow/2|destructive_action|Support Contract Core|Support Contract Admin" mix.exs scripts/verify_support_contract.sh lib/mix/tasks/mailglass.docs.check.ex MAINTAINING.md</automated>
  </verify>
  <done>The repo now exposes one semantic proof command and the Tier 1 docs checker treats the testing and admin trust docs as release-blocking truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend compiled-doc and docs-contract tests so proof drift fails deterministically</name>
  <files>test/mailglass/docs_contract_test.exs, test/mailglass/stability_contract_test.exs, mailglass_admin/test/mailglass_admin/stability_contract_test.exs</files>
  <read_first>test/mailglass/docs_contract_test.exs, test/mailglass/stability_contract_test.exs, mailglass_admin/test/mailglass_admin/stability_contract_test.exs, mix.exs, lib/mix/tasks/mailglass.docs.check.ex, guides/testing.md, mailglass_admin/docs/operator-trust.md, .planning/phases/37-contract-enforcement-and-trust-docs/37-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `test/mailglass/docs_contract_test.exs` asserts the canonical testing guide and admin trust doc are present in the Tier 1 contract story.
    - `test/mailglass/stability_contract_test.exs` contains an assertion referencing `verify.stability_contract`.
    - `test/mailglass/stability_contract_test.exs` and `mailglass_admin/test/mailglass_admin/stability_contract_test.exs` continue to assert stable compiled-doc metadata for public seams and add any missing proof-wiring assertions needed for Phase 37.
    - `mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors` exits 0.
    - `cd mailglass_admin && mix test test/mailglass_admin/stability_contract_test.exs test/mailglass_admin/operator_trust_doc_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>Extend the existing contract tests instead of creating a new heavy proof harness. In `test/mailglass/docs_contract_test.exs`, add exact assertions that the Tier 1 docs story now includes `guides/testing.md` and the admin trust doc pointer surfaces created in Plan 37-02. In `test/mailglass/stability_contract_test.exs`, keep the compiled-doc metadata coverage for stable core seams and add a focused assertion that the semantic proof workflow exists under `mix.exs` as `verify.stability_contract` and includes the existing support-contract lanes rather than a new manifest/export mechanism. In `mailglass_admin/test/mailglass_admin/stability_contract_test.exs`, preserve the current router/auth metadata coverage and add any minimal assertions needed to keep the new trust-doc wiring and stable admin seams aligned. Do not add DOM/component assertions or export-diff snapshots; keep the proof deterministic, text-level, and compiled-doc-driven.</action>
  <verify>
    <automated>mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors && cd mailglass_admin && mix test test/mailglass_admin/stability_contract_test.exs test/mailglass_admin/operator_trust_doc_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The semantic stability proof now fails deterministically when the documented core, testing, or admin trust contract drifts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Semantic proof alias -> maintainer release workflow | Maintainers use one command as the authoritative contract proof before release. |
| Tier 1 docs checker -> documented public contract | Docs checks determine whether canonical truth docs can drift silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07 | Tampering / Misrepresentation | `mix.exs`, `scripts/verify_support_contract.sh` | mitigate | Expose one semantic proof alias and keep the shell entrypoint aligned so maintainers do not run a weaker or different workflow by accident. |
| T-37-08 | Repudiation | `lib/mix/tasks/mailglass.docs.check.ex`, `test/mailglass/docs_contract_test.exs` | mitigate | Treat the testing and admin trust docs as Tier 1 truth with exact token assertions and deterministic tests. |
| T-37-09 | Denial of Service | `lib/mix/tasks/mailglass.stability.check.ex` and proof workflow | accept | Keep the enforcement lightweight and deterministic instead of adding heavy snapshots or broad export scans that are costly to maintain. |
</threat_model>

<verification>
- `bash scripts/verify_support_contract.sh`
- `mix test test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs --warnings-as-errors`
- `cd mailglass_admin && mix test test/mailglass_admin/stability_contract_test.exs test/mailglass_admin/operator_trust_doc_test.exs --warnings-as-errors`
- `rg -n "verify\\.stability_contract|guides/testing\\.md|operator-trust|Fake.allow/2|destructive_action" mix.exs scripts/verify_support_contract.sh lib/mix/tasks/mailglass.docs.check.ex test/mailglass/docs_contract_test.exs test/mailglass/stability_contract_test.exs mailglass_admin/test/mailglass_admin/stability_contract_test.exs`
</verification>

<success_criteria>
Maintainers can run one semantic repo-root proof workflow that stays lightweight, composes the current authorities honestly, and fails deterministically when the documented testing or admin contract drifts.
</success_criteria>

<output>
After completion, create `.planning/phases/37-contract-enforcement-and-trust-docs/37-03-SUMMARY.md`
</output>
