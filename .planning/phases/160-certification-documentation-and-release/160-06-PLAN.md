---
phase: 160-certification-documentation-and-release
plan: 06
type: execute
wave: 5
depends_on: [160-05]
files_modified:
  - .planning/release-target.json
  - test/mailglass/publish/post_publish_smoke_contract_test.exs
  - test/reference_host/trust_runner_checkpoint_contract_test.exs
autonomous: false
requirements: [REL-04]
must_haves:
  truths:
    - "Protected automation publishes exactly the authorized core, admin, and inbound candidate and no other ref/package/version."
    - "An exact-Hex generated host installs all three immutable public artifacts and repeats the durable trust journey without path dependencies."
    - "A missing, partial, retired, mismatched, or failed package result leaves REL-04 incomplete and visibly blocked."
  artifacts:
    - path: ".planning/release-target.json"
      provides: "Final publication and exact-Hex evidence record"
  key_links:
    - from: ".github/workflows/publish-hex.yml"
      to: ".github/workflows/post-publish-smoke.yml"
      via: "published exact versions and immutable release ref"
      pattern: "mailglass_inbound"
  prohibitions:
    - "No publication before a second explicit user authorization in this plan."
    - "No fallback to latest, path/git dependencies, manual local `mix hex.publish`, or partial success acceptance."
    - "No admin/operator UI behavior change."
---

<objective>Execute the explicitly authorized protected release and accept it only after an exact-Hex generated-host proof succeeds for all packages.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/release-target.json
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@scripts/generated_ecto_host_proof.sh</context>

<tasks>
<task type="checkpoint:decision" gate="blocking-human">
<name>Task 1: Confirm external merge/tag/publication execution</name>
<decision>Run the protected release sequence for the already authorized immutable candidate now.</decision>
<context>This action merges the reviewed release PR, creates public release/tag state, and permits protected Hex publication. It cannot be undone as an ordinary repository edit.</context>
<options>
<option id="publish"><name>Publish authorized candidate</name><pros>Runs the protected, audited, idempotent all-package path.</pros><cons>Creates public immutable package artifacts.</cons></option>
<option id="stop"><name>Stop before publication</name><pros>Leaves external state unchanged.</pros><cons>REL-04 remains pending.</cons></option>
</options>
<resume-signal>Type `publish &lt;candidate_sha&gt;` to authorize the protected sequence for that exact SHA, or `stop`.</resume-signal>
</task>

<task type="auto">
<name>Task 2: Run protected publication for the exact authorized candidate</name>
<precondition>The Task 1 response is exactly `publish &lt;candidate_sha&gt;`, and it matches the authorized SHA in `.planning/release-target.json`.</precondition>
<reversibility rating="one-way">Hex artifacts become public and cannot be fully unpublished after the registry's recovery window.</reversibility>
<files>.planning/release-target.json</files>
<read_first>.planning/release-target.json, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml</read_first>
<action>Use only the protected GitHub release workflow for the exact authorized ref. Monitor merge, tags/releases, CI Green, publish fan-out, idempotency guards, and all three package jobs. Stop on any mismatch or partial result; do not substitute local credentials or manually publish a missing package. Record immutable run URLs, release IDs, tag SHAs, and Hex checksums in the release target evidence section.</action>
<verify><automated>mix run scripts/release_policy.exs --verify-published .planning/release-target.json</automated></verify>
<done>All three exact authorized artifacts are public from one protected candidate, or the task stops with an explicit incomplete release state.</done>
<acceptance_criteria>Publication evidence is exact and complete; partial publication never counts as REL-04 success.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 3: Prove exact-Hex adoption and close the release target</name>
<files>.planning/release-target.json, test/mailglass/publish/post_publish_smoke_contract_test.exs, test/reference_host/trust_runner_checkpoint_contract_test.exs</files>
<read_first>.github/workflows/post-publish-smoke.yml, scripts/generated_ecto_host_proof.sh, test/mailglass/publish/post_publish_smoke_contract_test.exs</read_first>
<behavior>- A disposable host resolves exact Hex versions for all packages and contains no path/git overrides.
- It completes fresh install, durable sync/async delivery, upgrade/rollback/rerun, custom-module, multi-repo, and prefix proof.
- Missing, mismatched, retired, or failed package evidence leaves the release target open.</behavior>
<action>Run the protected post-publish workflow with the three exact versions from the target. Verify lock resolution and Hex package checksums before boot, then execute the Plan 01 journey against public artifacts. Record sanitized checkpoint digest and immutable workflow URL. Mark the target complete only when every package and journey assertion passes; do not alter admin/operator behavior to make the smoke pass.</action>
<verify><automated>mix test test/mailglass/publish/post_publish_smoke_contract_test.exs test/reference_host/trust_runner_checkpoint_contract_test.exs --warnings-as-errors &amp;&amp; mix run scripts/release_policy.exs --verify-complete .planning/release-target.json</automated></verify>
<done>The exact public artifacts install and complete the production-shaped journey, with a closed evidence record.</done>
<acceptance_criteria>REL-04 is complete only after exact-Hex proof for core, admin, and inbound succeeds.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Human authorization → protected workflow | Approval must bind one immutable candidate. |
| Protected environment → Hex | Release credentials can publish irreversible artifacts. |
| Hex artifacts → generated host | Public packages may differ from repository/path builds. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-15 | Elevation of Privilege | release credentials | high | mitigate | Explicit authorization, protected environment, job-local permissions, no secret output. |
| T-160-16 | Tampering | tag/ref/package set | high | mitigate | Immutable SHA/version/package equality before every publish job. |
| T-160-17 | Repudiation | partial publication | high | mitigate | Exact per-package release IDs/checksums and fail-closed completion record. |
| T-160-18 | Spoofing | exact-Hex proof | high | mitigate | Lock/checksum validation and rejection of path/git dependencies. |
</threat_model>

## Artifacts this phase produces
- Protected workflow run/release/tag/Hex checksum evidence for all packages.
- Exact-Hex generated-host checkpoint digest and immutable run URL.
- Final complete/incomplete release-target status with no secret values.

<verification>Verify protected runs and exact-Hex contract; no local publication fallback is allowed.</verification>
<success_criteria>REL-04 has explicit authority, protected all-package publication, and exact-Hex adoption proof.</success_criteria>
<output>Create `.planning/phases/160-certification-documentation-and-release/160-06-SUMMARY.md` when done.</output>
