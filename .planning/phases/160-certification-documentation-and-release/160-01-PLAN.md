---
phase: 160-certification-documentation-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generated_ecto_host_proof.sh
  - test/scripts/generated_ecto_host_proof_test.exs
  - test/reference_host/trust_runner_command_contract_test.exs
  - test/reference_host/trust_runner_checkpoint_contract_test.exs
  - test/fixtures/generated_host/custom_modules.exs
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "A disposable generated Phoenix/Ecto/Postgres host proves fresh core and inbound installation plus durable synchronous and asynchronous delivery."
    - "The same proof covers explicit custom modules, two configured repositories, a non-public schema prefix, upgrade, rollback, idempotent rerun, and both package orders."
    - "Every checkpoint is derived from observable database/runtime state and fails closed when a required stage is missing, duplicated, or reordered."
  artifacts:
    - path: "scripts/generated_ecto_host_proof.sh"
      provides: "One-command generated-host certification journey"
    - path: "test/scripts/generated_ecto_host_proof_test.exs"
      provides: "Mutation tests for all required host stages"
    - path: "test/fixtures/generated_host/custom_modules.exs"
      provides: "Deterministic custom Repo, tenancy, adapter, and prefix fixture"
  key_links:
    - from: "scripts/generated_ecto_host_proof.sh"
      to: "mix mailglass.gen.migration and mix mailglass_inbound.gen.migration"
      via: "generated host invokes the public package tasks rather than repo-local DDL"
      pattern: "gen.migration"
    - from: "scripts/generated_ecto_host_proof.sh"
      to: "test/reference_host/trust_runner_checkpoint_contract_test.exs"
      via: "sanitized ordered checkpoint manifest"
      pattern: "checkpoint"
  prohibitions:
    - "No admin/operator UI behavior, route, LiveView, styling, or visual change."
    - "No repo-local TestRepo, MailerCase, inline-only queue, or hand-written migration may substitute for the generated host."
    - "No release, tag, merge, credential, or public-version mutation."
---

<objective>Certify REL-01 through one production-shaped generated-host journey that exercises the additive v2.6 package family without relying on repository test shortcuts.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@scripts/generated_ecto_host_proof.sh
@test/scripts/generated_ecto_host_proof_test.exs
@test/reference_host/trust_runner_command_contract_test.exs</context>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Prove fresh install and one durable sync/async path in a generated host</name>
<files>scripts/generated_ecto_host_proof.sh, test/scripts/generated_ecto_host_proof_test.exs, test/reference_host/trust_runner_command_contract_test.exs</files>
<read_first>scripts/generated_ecto_host_proof.sh, test/scripts/generated_ecto_host_proof_test.exs, mix.exs, mailglass_inbound/mix.exs</read_first>
<behavior>- The test first removes each fresh-install, sync-send, atomic-enqueue, worker-run, or persisted-outcome checkpoint and proves the contract fails.
- The journey creates a disposable Phoenix/Ecto/Postgres app, installs core and inbound from local paths, runs public generated wrappers, and boots the host.
- Sync and Oban-backed async paths persist the expected delivery/event/private-payload lifecycle; an inline adapter or repository TestRepo cannot satisfy the proof.</behavior>
<action>Add the mutation/negative controls before extending the shell journey. Reuse the existing generated-host harness and public generator commands, but make the output an ordered sanitized checkpoint contract. Exercise a real running queue and query the generated host's configured Repo for durable outcomes. Preserve exact raw failure output without leaking message content or credentials.</action>
<verify><automated>mix test test/scripts/generated_ecto_host_proof_test.exs test/reference_host/trust_runner_command_contract_test.exs --warnings-as-errors</automated></verify>
<done>A fresh disposable host completes a real synchronous delivery and durable asynchronous enqueue/dispatch, and each missing stage is caught by a negative control.</done>
<acceptance_criteria>REL-01 fresh install and durable sync/async evidence is non-vacuous, ordered, and generated-host-owned.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 2: Expand certification across repo, prefix, upgrade, rollback, and rerun boundaries</name>
<files>scripts/generated_ecto_host_proof.sh, test/scripts/generated_ecto_host_proof_test.exs, test/reference_host/trust_runner_checkpoint_contract_test.exs, test/fixtures/generated_host/custom_modules.exs</files>
<read_first>scripts/generated_ecto_host_proof.sh, test/reference_host/trust_runner_checkpoint_contract_test.exs, guides/upgrading-to-v2_0.md</read_first>
<behavior>- Explicit custom Repo/tenancy/adapter modules are used by the running host, not merely compiled.
- Core-first and inbound-first journeys use distinct repos and non-public prefixes; upgrades, rollbacks, and reruns preserve sibling/host objects.
- Missing, duplicate, or equal-order checkpoints fail closed, and the final manifest contains no secrets, recipients, or bodies.</behavior>
<action>Extend the fixture and journey to create two configured Ecto repositories and distinct schema prefixes. Run public upgrade generation from real applied anchors, verify both rollback orders, rerun every up/down boundary for idempotency, and exercise custom module configuration through runtime behavior. Keep the two packages independent and leave shipped migration files untouched.</action>
<verify><automated>mix test test/scripts/generated_ecto_host_proof_test.exs test/reference_host/trust_runner_checkpoint_contract_test.exs --warnings-as-errors &amp;&amp; bash scripts/generated_ecto_host_proof.sh</automated></verify>
<done>The full REL-01 matrix succeeds from a disposable generated host and every required checkpoint is validated fail-closed.</done>
<acceptance_criteria>Fresh/install/upgrade/rollback/rerun/custom-module/multi-repo/non-public-prefix behavior is demonstrated by runtime and database evidence.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Generated host → package generators | Untrusted host configuration selects repos/modules/prefixes. |
| Queue/provider result → certification checkpoint | False or sensitive evidence could masquerade as durable success. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Tampering | checkpoint order | high | mitigate | Exact ordered manifest plus removal/duplication/equal-order mutation tests. |
| T-160-02 | Information Disclosure | proof artifact | high | mitigate | Closed sanitized fields; reject recipients, bodies, secrets, and provider payloads. |
| T-160-03 | Elevation of Privilege | custom module/repo selection | high | mitigate | Literal configured modules, existing atom resolution, scoped repos, and prefix assertions. |
</threat_model>

## Artifacts this phase produces
- Extended `scripts/generated_ecto_host_proof.sh` stages and checkpoint names.
- `test/fixtures/generated_host/custom_modules.exs` custom configuration fixture.
- Mutation cases for absent, duplicate, and equal-order host evidence.

<verification>Run the focused contract tests and the complete disposable-host command on the pinned Phase 159 toolchain.</verification>
<success_criteria>REL-01 is proven without repository-only helpers, UI changes, or publication.</success_criteria>
<output>Create `.planning/phases/160-certification-documentation-and-release/160-01-SUMMARY.md` when done.</output>
