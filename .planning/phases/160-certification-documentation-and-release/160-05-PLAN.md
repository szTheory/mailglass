---
phase: 160-certification-documentation-and-release
plan: 05
type: execute
wave: 4
depends_on: [160-01, 160-02, 160-03, 160-04]
files_modified:
  - .planning/release-target.json
  - CHANGELOG.md
  - mailglass_admin/CHANGELOG.md
  - mailglass_inbound/CHANGELOG.md
  - test/scripts/release_policy_test.exs
autonomous: false
requirements: [REL-03, REL-04]
must_haves:
  truths:
    - "The maintainer reviews the automation-proposed exact versions, proposal head/source SHAs, publishable-content digest, package set, changelogs, and green evidence before authorizing any merge/tag/publication."
    - "Authorization is specific to one candidate digest and cannot be reused after proposal identity, content, version, package set, or evidence changes."
    - "No publication begins in this plan; it ends with an authorized, immutable release target ready for the protected workflow."
  artifacts:
    - path: ".planning/release-target.json"
      provides: "Maintainer-approved immutable all-package candidate"
  key_links:
    - from: ".planning/release-target.json"
      to: ".github/workflows/publish-hex.yml"
      via: "exact proposal/content/package/version authorization"
      pattern: "candidate_digest"
  prohibitions:
    - "No semver chosen by the executor; use only the exact Release Please proposal."
    - "No merge, tag, release, publish, or credential access in this plan."
    - "No approval may be inferred from silence, auto-advance, prior releases, or a green dry-run."
---

<objective>Turn the reviewed automation proposal into a single-use authorized release target, while keeping all external mutation for the next checkpointed plan.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/release-target.json
@CHANGELOG.md
@mailglass_admin/CHANGELOG.md
@mailglass_inbound/CHANGELOG.md
@.github/workflows/release-please.yml</context>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Capture and validate the exact automation-proposed candidate</name>
<reversibility rating="costly">Changing an approved candidate requires regenerating and re-reviewing every package/version/proposal/content/evidence binding.</reversibility>
<files>.planning/release-target.json, CHANGELOG.md, mailglass_admin/CHANGELOG.md, mailglass_inbound/CHANGELOG.md, test/scripts/release_policy_test.exs</files>
<read_first>scripts/release_policy.exs, .planning/release-target.json, release-please-config.json</read_first>
<behavior>- Candidate values exactly match Release Please output, proposal head/source SHAs, and publishable-content digest.
- Changelogs contain only changes attributed to each selected package and contain no stale candidate version.
- Any changed proposal identity/content digest/version/package/evidence invalidates authorization; final merge/tag SHA remains unset until publication execution.</behavior>
<action>Run credential-free release-candidate generation and copy only its exact proposed versions into the inactive release target and changelogs. Validate all Phase 159 gates, Plans 01-02 proof, current Hex reconciliation, and dry-run package artifacts. Do not select or edit versions by judgment, and do not merge the generated release PR.</action>
<verify><automated>mix run scripts/release_policy.exs --validate-candidate .planning/release-target.json &amp;&amp; mix test test/scripts/release_policy_test.exs --warnings-as-errors</automated></verify>
<done>One exact candidate is reviewable and all evidence binds to its proposal identity and publishable-content digest, but authorization remains false.</done>
<acceptance_criteria>No guessed value or external mutation is present; any candidate drift fails.</acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking-human">
<name>Task 2: Authorize the exact candidate for merge/tag/publication</name>
<decision>Authorize the displayed core, admin, and inbound versions at the displayed proposal head/source SHAs, publishable-content digest, and package set for the protected release sequence.</decision>
<context>Merge, GitHub release/tag creation, and Hex publication are externally visible and costly to reverse. Approval applies only to this exact target; any regeneration returns to Task 1.</context>
<options>
<option id="authorize"><name>Authorize exact candidate</name><pros>Allows the next plan to use protected automation for this immutable candidate.</pros><cons>Creates public artifacts once the next checkpoint is separately approved.</cons></option>
<option id="reject"><name>Reject and regenerate</name><pros>Keeps all external state unchanged.</pros><cons>Requires correcting evidence/changelogs and producing a new candidate.</cons></option>
</options>
<resume-signal>Type `authorize &lt;candidate_digest&gt;` only if the displayed proposal SHAs, package versions, package set, content digest, and evidence are approved; otherwise describe the rejection.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Automation proposal → human authorization | A stale or swapped candidate must not inherit approval. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-13 | Spoofing | candidate identity | high | mitigate | Display and bind proposal head/source SHAs, publishable-content digest, versions, package set, and evidence digest. |
| T-160-14 | Repudiation | authorization | high | mitigate | Explicit candidate-digest-bearing response; no auto approval. |
</threat_model>

## Artifacts this phase produces
- Exact automation-proposed changelog/version candidate.
- Single-use human authorization bound to proposal identity, publishable-content digest, and evidence digest.

<verification>Validate the inactive candidate and record explicit maintainer response; do not invoke mutation workflows.</verification>
<success_criteria>An exact candidate is either explicitly authorized or rejected without external release effects.</success_criteria>
<output>Create `.planning/phases/160-certification-documentation-and-release/160-05-SUMMARY.md` when done.</output>
