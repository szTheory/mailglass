---
phase: 160-certification-documentation-and-release
plan: 04
type: execute
wave: 3
depends_on: [160-03]
files_modified:
  - release-please-config.json
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - .github/workflows/post-publish-smoke.yml
  - scripts/release_policy.exs
  - test/scripts/release_policy_test.exs
  - test/scripts/linked_release_concurrency_test.exs
  - test/mailglass/publish/post_publish_smoke_contract_test.exs
autonomous: true
requirements: [REL-03, REL-04]
must_haves:
  truths:
    - "Protected release policy treats core, admin, and independently versioned inbound as one explicitly selected release candidate without forcing equal versions."
    - "Candidate selection, tag/ref validation, fan-out ordering, idempotency, and exact-Hex smoke inputs are versioned repository logic with mutation tests."
    - "Preparation and dry-run paths use no publication credentials and cannot merge, tag, publish, or activate an unreviewed release target."
  artifacts:
    - path: "scripts/release_policy.exs"
      provides: "Pure candidate/ref/package-set policy extracted from workflows"
    - path: "test/scripts/release_policy_test.exs"
      provides: "Three-package candidate and hostile-input mutation suite"
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: "scripts/release_policy.exs"
      via: "credential-free candidate validation before release actions"
      pattern: "release_policy"
    - from: ".github/workflows/publish-hex.yml"
      to: ".planning/release-target.json"
      via: "exact core/admin/inbound candidate comparison"
      pattern: "mailglass_inbound"
  prohibitions:
    - "No merge, tag, GitHub release, Hex publication, or credential use in this plan."
    - "No invented next semver; only release automation may propose candidates."
    - "No public workflow/check identity or publication semantic change without compatibility tests."
    - "No admin/operator UI behavior change."
---

<objective>Prepare REL-04's protected three-package release path while keeping every irreversible external action disabled.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@release-please-config.json
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@.github/workflows/post-publish-smoke.yml
@.planning/release-target.json</context>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Extract exact three-package release policy and hostile-input controls</name>
<reversibility rating="costly">The release-target and protected workflow contract coordinates several public artifacts; undo requires coordinated workflow and manifest changes.</reversibility>
<files>scripts/release_policy.exs, test/scripts/release_policy_test.exs, test/scripts/linked_release_concurrency_test.exs</files>
<read_first>.github/workflows/release-please.yml, .github/workflows/publish-hex.yml, test/scripts/linked_release_concurrency_test.exs, .planning/release-target.json</read_first>
<behavior>- Empty, missing, duplicate, unknown, and conflicting package selections fail closed.
- Core/admin may remain linked while inbound carries its own candidate; all three are present in an all-package candidate.
- Untrusted ref/reason/action inputs are parsed as data and cannot inject shell, expression, or workflow commands.</behavior>
<action>Write fixture and mutation tests before extracting deterministic selection/ref validation from inline workflow bodies. Define an exact schema for baseline versions, candidate versions, package set, immutable candidate SHA, and evidence IDs. Preserve existing release triggers, concurrency, environment, check display names, and idempotency guards.</action>
<verify><automated>mix test test/scripts/release_policy_test.exs test/scripts/linked_release_concurrency_test.exs --warnings-as-errors</automated></verify>
<done>The repository can validate a three-package candidate without workflow credentials or external mutation.</done>
<acceptance_criteria>Inbound is first-class in policy while independent-version semantics and existing public identities remain intact.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 2: Wire dry-run candidate preparation and exact-Hex inputs without activating publication</name>
<files>release-please-config.json, .github/workflows/release-please.yml, .github/workflows/publish-hex.yml, .github/workflows/post-publish-smoke.yml, test/scripts/release_policy_test.exs, test/mailglass/publish/post_publish_smoke_contract_test.exs</files>
<read_first>scripts/release_policy.exs, test/mailglass/publish/post_publish_smoke_contract_test.exs, .github/workflows/post-publish-smoke.yml</read_first>
<behavior>- Release Please produces proposed core/admin/inbound values as reviewable output, but this plan does not merge its PR or create releases.
- Publish dry-run validates the immutable candidate, complete Phase 159 CI Green, Plan 01 certification, docs contracts, and exact package set.
- Post-publish workflow takes three explicit exact versions and refuses fallback to latest or a repository path dependency.</behavior>
<action>Update release configuration and workflow calls so inbound participates in candidate calculation and release notes without being forced onto the core/admin number. Teach protected publish and post-publish workflows to consume the extracted policy and all three exact versions. Keep live publish steps unreachable without an authorized published-release or explicit dispatch; exercise only credential-free tests and dry-run validation.</action>
<verify><automated>mix test test/scripts/release_policy_test.exs test/scripts/linked_release_concurrency_test.exs test/mailglass/publish/post_publish_smoke_contract_test.exs --warnings-as-errors &amp;&amp; actionlint .github/workflows/release-please.yml .github/workflows/publish-hex.yml .github/workflows/post-publish-smoke.yml</automated></verify>
<done>The protected path is prepared for all packages, exact-Hex proof is parameterized, and no external release state changed.</done>
<acceptance_criteria>REL-03/04 candidate policy is complete and credential-free through dry-run.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PR/release inputs → workflow scripts | Untrusted strings can influence refs, package set, and summaries. |
| Candidate target → protected publish environment | A malformed target could request unintended artifacts. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-10 | Tampering | candidate package set | high | mitigate | Exact schema, set equality, duplicate/unknown rejection, immutable SHA. |
| T-160-11 | Elevation of Privilege | workflow permissions | high | mitigate | Read-only preparation, job-local permissions, protected environment retained. |
| T-160-12 | Injection | release inputs | high | mitigate | Structured arguments, no eval, escaped summaries, hostile fixture tests. |
</threat_model>

## Artifacts this phase produces
- `ReleasePolicy` candidate/ref/package validators and CLI task inputs.
- Three-package release-target schema and dry-run workflow wiring.
- Exact-version post-publish workflow inputs for core, admin, and inbound.

<verification>Run release-policy, concurrency, post-publish, and actionlint contracts; confirm no release/tag/publication exists.</verification>
<success_criteria>Release preparation is deterministic, all-package, version-neutral until automation output, and externally inert.</success_criteria>
<output>Create `.planning/phases/160-certification-documentation-and-release/160-04-SUMMARY.md` when done.</output>
