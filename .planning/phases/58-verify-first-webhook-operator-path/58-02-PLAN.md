---
phase: 58-verify-first-webhook-operator-path
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - lib/mix/tasks/mailglass.trust.run.ex
  - lib/mailglass/reference_host/operator_diagnosis_proof.ex
  - lib/mailglass/reference_host/trust_checkpoint.ex
  - scripts/check_trust_runner_checkpoint.sh
  - test/reference_host/webhook_operator_path_test.exs
  - test/reference_host/trust_runner_command_contract_test.exs
  - test/reference_host/trust_runner_checkpoint_contract_test.exs
autonomous: true
requirements:
  - JOUR-03
  - JOUR-04
must_haves:
  truths:
    - "Operator troubleshooting checkpoint uses the existing operator_troubleshooting stage."
    - "Operator diagnosis evidence is deterministic for a :no_match routing scenario."
    - "Checkpoint artifacts preserve schema_version trust_runner.v1 and stage order install, preview, send, webhook_ingest, operator_troubleshooting."
    - "Checkpoint hash remains based on stage/status/fixture_id while Phase 58 evidence is validated separately."
  artifacts:
    - path: "lib/mix/tasks/mailglass.trust.run.ex"
      provides: "operator_troubleshooting stage evidence for :no_match diagnosis"
      contains: "Mailglass.ReferenceHost.OperatorDiagnosisProof"
    - path: "lib/mailglass/reference_host/operator_diagnosis_proof.ex"
      provides: "Executable deterministic no_match diagnosis helper derived from the operator routing-trace surface"
      contains: "no_match"
    - path: "lib/mailglass/reference_host/trust_checkpoint.ex"
      provides: "Additive evidence encoding with bounded Phase 58 claim language"
      contains: "trust_runner.v1"
    - path: "scripts/check_trust_runner_checkpoint.sh"
      provides: "Executable checkpoint validator for Phase 58 evidence"
      contains: "operator_troubleshooting"
    - path: "test/reference_host/trust_runner_checkpoint_contract_test.exs"
      provides: "Determinism and hash contract coverage"
      contains: "checkpoint_sha256"
  key_links:
    - from: "lib/mix/tasks/mailglass.trust.run.ex"
      to: "lib/mailglass/reference_host/operator_diagnosis_proof.ex"
      via: "OperatorDiagnosisProof.run/0 or equivalent"
      pattern: "OperatorDiagnosisProof"
    - from: "lib/mailglass/reference_host/operator_diagnosis_proof.ex"
      to: "mailglass_admin/test/mailglass_admin/inbound_live_test.exs"
      via: ":no_match routing-trace evidence shape"
      pattern: "no_match"
    - from: "lib/mix/tasks/mailglass.trust.run.ex"
      to: "lib/mailglass/reference_host/trust_checkpoint.ex"
      via: "TrustCheckpoint.encode(stage_records)"
      pattern: "TrustCheckpoint\\.encode"
    - from: "scripts/check_trust_runner_checkpoint.sh"
      to: "tmp/mailglass_trust_runner/checkpoint.json"
      via: "JSON schema and evidence validation"
      pattern: "trust_runner\\.v1"
    - from: "test/reference_host/webhook_operator_path_test.exs"
      to: "lib/mailglass/reference_host/operator_diagnosis_proof.ex"
      via: "shared helper assertions"
      pattern: "OperatorDiagnosisProof"
---

<objective>
Complete JOUR-04 operator diagnosis proof and align Phase 58 evidence with stable checkpoint semantics.

Purpose: Checkpoint one deterministic non-happy-path `:no_match` diagnosis under `operator_troubleshooting`, retire Phase 58 deferred wording, and validate the additive evidence contract without changing `mix verify.reference_host.journey`, `trust_runner.v1`, or the five stage keys, per D-01, D-02, D-03, D-09, D-10, and D-11.
Output: Runner operator evidence, checkpoint encoder support, shell validator checks, and contract tests for completed Phase 58 trust proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-verify-first-webhook-operator-path/58-CONTEXT.md
@.planning/phases/58-verify-first-webhook-operator-path/58-RESEARCH.md
@.planning/phases/58-verify-first-webhook-operator-path/58-PATTERNS.md
@.planning/phases/58-verify-first-webhook-operator-path/58-VALIDATION.md
@.planning/phases/57-deterministic-trust-runner-fixtures/57-CONTEXT.md
@.planning/phases/58-verify-first-webhook-operator-path/58-01-SUMMARY.md

<interfaces>
From `lib/mailglass/reference_host/trust_checkpoint.ex`:
```elixir
@schema_version "trust_runner.v1"
@stage_order %{
  "install" => 1,
  "preview" => 2,
  "send" => 3,
  "webhook_ingest" => 4,
  "operator_troubleshooting" => 5
}
```

From `lib/mailglass/reference_host/trust_checkpoint.ex`:
```elixir
rows
|> Enum.map(fn row -> "#{row["stage"]}|#{row["status"]}|#{row["fixture_id"]}" end)
|> Enum.join("\n")
```

From `mailglass_admin/lib/mailglass_admin/inbound_live.ex`:
```elixir
defp routing_trace_for(inbound_router, %{outcome: :no_match, record: record}) do
  if gateway_available?() do
    apply(@gateway, :explain_routes, [inbound_router, record])
  else
    []
  end
end
```

From `mailglass_admin/test/mailglass_admin/inbound_live_test.exs`:
```elixir
assert html =~ ~s(data-testid="inbound-routing-trace")
assert html =~ "Why this message did not match"
assert html =~ "n******@e******.com"
refute html =~ "nomatch@example.com"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic no_match operator evidence</name>
  <files>lib/mailglass/reference_host/operator_diagnosis_proof.ex, lib/mix/tasks/mailglass.trust.run.ex, test/reference_host/webhook_operator_path_test.exs</files>
  <read_first>
    - lib/mix/tasks/mailglass.trust.run.ex
    - lib/mailglass/reference_host/operator_diagnosis_proof.ex
    - test/reference_host/webhook_operator_path_test.exs
    - .planning/phases/58-verify-first-webhook-operator-path/58-CONTEXT.md
    - .planning/phases/58-verify-first-webhook-operator-path/58-RESEARCH.md
    - .planning/phases/58-verify-first-webhook-operator-path/58-PATTERNS.md
    - mailglass_admin/lib/mailglass_admin/inbound_live.ex
    - mailglass_admin/test/mailglass_admin/inbound_live_test.exs
    - mailglass_admin/test/support/inbound_fixtures.ex
  </read_first>
  <behavior>
    - Test 1: The runner's `operator_troubleshooting` stage emits evidence with `"scenario" => "no_match"`, `"outcome" => "no_match"`, `"stage" => "operator_troubleshooting"`, `"status_language" => "no matching mailbox route"`, `"finding" => "message did not match any configured inbound route"`, and `"remediation" => "review recipient, subject, and header route clauses"`.
    - Test 2: Operator evidence contains no raw recipient, sender, subject, raw payload, or rendered HTML fields; it may include `"recipient_masked" => true`, `"route_clause_dimensions" => ["recipient", "subject", "header:x-priority"]`, and `"trace_card_count" => 3`.
  </behavior>
  <action>Create `lib/mailglass/reference_host/operator_diagnosis_proof.ex` with a public `run/0` helper (or equivalently named public function) that executes one deterministic `:no_match` operator diagnosis scenario through the existing routing-trace or diagnosis surface instead of returning a hand-written checkpoint map. Build the scenario from the same facts pinned in `mailglass_admin/test/mailglass_admin/inbound_live_test.exs`: a no-match inbound record with recipient/subject/header route clauses whose trace exposes recipient, subject, and `header:x-priority` dimensions and three route cards. Use the same gateway/diagnosis path that `MailglassAdmin.InboundLive.routing_trace_for/2` relies on where callable from production code, or the existing inbound operator diagnosis helper if that is the public callable surface; do not scrape rendered LiveView HTML and do not duplicate private LiveView functions. The helper must derive bounded evidence fields from the executed trace/diagnosis result: `"scenario" => "no_match"`, `"outcome" => "no_match"`, `"stage" => "operator_troubleshooting"`, `"status_language" => "no matching mailbox route"`, `"finding" => "message did not match any configured inbound route"`, `"remediation" => "review recipient, subject, and header route clauses"`, `"route_clause_dimensions"` from the trace clauses, `"trace_card_count"` from the executed trace, `"recipient_masked" => true` from the existing masking/trace assertion surface, and boolean absence markers for raw payload/private recipient inclusion. Replace the `operator_troubleshooting` file-presence stage signal in `lib/mix/tasks/mailglass.trust.run.ex` so non-dry-run execution calls `Mailglass.ReferenceHost.OperatorDiagnosisProof.run/0` and derives checkpoint evidence from that result. Do not store rendered LiveView HTML, raw payload bytes, subject text, recipient addresses, sender addresses, or tenant-private data. Keep fixture ID `"trust.operator_troubleshooting.001"` and status `"completed"` for non-dry-run evidence, per D-09, D-10, and D-11.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/reference_host/webhook_operator_path_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'OperatorDiagnosisProof\\.run|Mailglass\\.ReferenceHost\\.OperatorDiagnosisProof' lib/mix/tasks/mailglass.trust.run.ex test/reference_host/webhook_operator_path_test.exs` returns matches.
    - `rg -n '"scenario" => "no_match"|"outcome" => "no_match"|"status_language" => "no matching mailbox route"|"recipient_masked" => true' lib/mix/tasks/mailglass.trust.run.ex lib/mailglass/reference_host/operator_diagnosis_proof.ex test/reference_host/webhook_operator_path_test.exs` returns all four evidence values.
    - `rg -n 'raw_payload|private_recipient|rendered_html|nomatch@example\\.com|nobody@example\\.com' lib/mix/tasks/mailglass.trust.run.ex` returns no matches except for literal safe boolean keys `raw_payload_included` and `private_recipient_included`.
    - `rg -n '"trust\\.operator_troubleshooting\\.001"' lib/mix/tasks/mailglass.trust.run.ex test/support/reference_host/trust_runner_fixtures.ex` returns at least one match.
  </acceptance_criteria>
  <done>The runner records deterministic, PII-safe `:no_match` diagnosis evidence under the existing `operator_troubleshooting` stage and does not introduce a new operator schema or stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode and validate Phase 58 checkpoint evidence</name>
  <files>lib/mailglass/reference_host/trust_checkpoint.ex, scripts/check_trust_runner_checkpoint.sh, test/reference_host/trust_runner_command_contract_test.exs, test/reference_host/trust_runner_checkpoint_contract_test.exs</files>
  <read_first>
    - lib/mailglass/reference_host/trust_checkpoint.ex
    - scripts/check_trust_runner_checkpoint.sh
    - test/reference_host/trust_runner_command_contract_test.exs
    - test/reference_host/trust_runner_checkpoint_contract_test.exs
    - lib/mix/tasks/mailglass.trust.run.ex
    - .planning/phases/58-verify-first-webhook-operator-path/58-CONTEXT.md
    - .planning/phases/58-verify-first-webhook-operator-path/58-RESEARCH.md
    - .planning/phases/58-verify-first-webhook-operator-path/58-PATTERNS.md
    - .planning/phases/58-verify-first-webhook-operator-path/58-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Two dry runs still emit deterministic equivalent checkpoints with `schema_version` exactly `"trust_runner.v1"` and stages exactly `["install", "preview", "send", "webhook_ingest", "operator_troubleshooting"]`.
    - Test 2: `checkpoint_sha256` remains the SHA-256 of ordered `stage|status|fixture_id` rows and is unchanged by evidence map key order.
    - Test 3: Non-dry-run checkpoint JSON contains `webhook_ingest.evidence.negative_reason == "bad_credentials"`, `webhook_ingest.evidence.verified_before_tenant == true`, `operator_troubleshooting.evidence.scenario == "no_match"`, and `operator_troubleshooting.evidence.raw_payload_included == false`.
    - Test 4: `scripts/check_trust_runner_checkpoint.sh` rejects missing or malformed Phase 58 evidence and accepts the runner-generated checkpoint.
  </behavior>
  <action>Update `Mailglass.ReferenceHost.TrustCheckpoint.normalize_row/1` so it preserves an optional `"evidence"` map from input rows while retaining normalized `"stage"`, `"status"`, and `"fixture_id"` fields. Keep `@schema_version` exactly `"trust_runner.v1"`. Replace the claim boundary string with exactly `"reference-host trust-journey confidence only; signed Postmark webhook verification and no-match operator diagnosis proven by deterministic runner evidence"`. Keep `checkpoint_sha256/1` based only on ordered `stage|status|fixture_id` rows. This hash contract is fixed for Phase 58: evidence maps must be validated separately and must not participate in `checkpoint_sha256`. If an executor believes hash semantics need to change, stop and add a `checkpoint:decision` plan/task instead of changing this task inline. Update `scripts/check_trust_runner_checkpoint.sh` to expect the new claim boundary, validate the five required stages in the same order, validate `webhook_ingest.evidence` keys/values from Plan 01, validate `operator_troubleshooting.evidence` keys/values from Task 1, and reject evidence containing keys named `raw_payload`, `payload`, `headers`, `recipient`, `sender`, `subject`, or `html`. Update `trust_runner_command_contract_test.exs` to replace the "deferred to Phase 58" expectation with completed proof tokens: `"signed Postmark webhook verification"`, `"no-match operator diagnosis"`, `"JOUR-03"`, and `"JOUR-04"`. Update `trust_runner_checkpoint_contract_test.exs` to assert the new boundary, evidence values, fixed stage order, and row-hash semantics.</action>
  <verify>
    <automated>MIX_ENV=test mix verify.reference_host.journey && ./scripts/check_trust_runner_checkpoint.sh && MIX_ENV=test mix test test/reference_host/trust_runner_command_contract_test.exs test/reference_host/trust_runner_checkpoint_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '@schema_version "trust_runner\\.v1"' lib/mailglass/reference_host/trust_checkpoint.ex` returns one match.
    - `rg -n 'signed Postmark webhook verification and no-match operator diagnosis proven by deterministic runner evidence' lib/mailglass/reference_host/trust_checkpoint.ex scripts/check_trust_runner_checkpoint.sh test/reference_host/trust_runner_command_contract_test.exs test/reference_host/trust_runner_checkpoint_contract_test.exs` returns matches in all four files.
    - `rg -n 'deferred to Phase 58|signed-negative webhook and non-happy-path diagnosis are deferred' lib/mailglass/reference_host/trust_checkpoint.ex scripts/check_trust_runner_checkpoint.sh test/reference_host/trust_runner_command_contract_test.exs test/reference_host/trust_runner_checkpoint_contract_test.exs reference/host_app/README.md` returns no matches.
    - `rg -n 'stage\\|status\\|fixture_id|checkpoint_sha256' lib/mailglass/reference_host/trust_checkpoint.ex scripts/check_trust_runner_checkpoint.sh test/reference_host/trust_runner_checkpoint_contract_test.exs` returns matches proving row-hash semantics are still asserted.
    - `MIX_ENV=test mix verify.reference_host.journey && ./scripts/check_trust_runner_checkpoint.sh` exits 0.
  </acceptance_criteria>
  <done>The checkpoint contract encodes and validates completed Phase 58 evidence while preserving `trust_runner.v1`, stage order, and stage/status/fixture_id hash semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound record -> operator diagnosis | Tenant-scoped inbound message facts are transformed into operator-facing no-match diagnosis evidence. |
| runner -> checkpoint artifact | Runner evidence becomes machine-readable proof consumed by local scripts and later automation. |
| checkpoint artifact -> shell validator | JSON evidence crosses from Elixir encoder into Bash/Python validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Spoofing/Tampering | `webhook_ingest` checkpoint evidence | mitigate | Validator must require `negative_status: 401`, `negative_reason: "bad_credentials"`, and `verified_before_tenant: true` before accepting the checkpoint. |
| T-58-02 | Information Disclosure | Checkpoint evidence JSON | mitigate | Validator rejects forbidden evidence keys `raw_payload`, `payload`, `headers`, `recipient`, `sender`, `subject`, and `html`; encoder/tests keep only bounded facts. |
| T-58-03 | Information Disclosure | `operator_troubleshooting` diagnosis evidence | mitigate | Evidence must use `recipient_masked: true`, `raw_payload_included: false`, `private_recipient_included: false`, route clause dimensions, and count data; no raw recipient, subject, payload, sender, tenant-private data, or rendered HTML is stored. |
| T-58-04 | Repudiation/Tampering | `TrustCheckpoint` and `scripts/check_trust_runner_checkpoint.sh` | mitigate | Preserve `schema_version: "trust_runner.v1"`, fixed stage order, and SHA-256 over ordered `stage|status|fixture_id` rows; add contract tests and shell validation for evidence semantics. |
</threat_model>

<verification>
Run:
```bash
MIX_ENV=test mix verify.reference_host.journey && ./scripts/check_trust_runner_checkpoint.sh
MIX_ENV=test mix test test/reference_host/webhook_operator_path_test.exs test/reference_host/trust_runner_command_contract_test.exs test/reference_host/trust_runner_checkpoint_contract_test.exs mailglass_admin/test/mailglass_admin/inbound_live_test.exs --warnings-as-errors
```
</verification>

<success_criteria>
- `JOUR-04` has deterministic `:no_match` operator evidence under `operator_troubleshooting`.
- `JOUR-03` negative-signature evidence is validated by the checkpoint script.
- Checkpoint artifacts keep `trust_runner.v1`, the five existing stage keys, and row-hash semantics.
- Phase 58 deferred wording is removed from the runner checkpoint contract and reference-host command contract.
</success_criteria>

<output>
After completion, create `.planning/phases/58-verify-first-webhook-operator-path/58-02-SUMMARY.md`.
</output>
