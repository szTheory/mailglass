---
phase: 120-deliveries-surface-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
autonomous: true
requirements: [DELIV-01]

must_haves:
  truths:
    - "On a genuine no-data Deliveries view (no rows, no active filters), the filters toolbar and Open-delivery CTA are NOT rendered; a single calm onboarding pane (empty-state body + orientation strip) is shown."
    - "On a no-match Deliveries view (no rows, filters active), the filters toolbar IS rendered so the operator keeps the Clear-filters escape; the orientation strip is NOT rendered."
    - "On a populated Deliveries view (rows present), the filters toolbar IS rendered and the orientation strip is NOT rendered, including the populated-but-unselected detail column."
    - "The populated-but-unselected detail column still renders the 'Select a delivery to inspect…' master-detail helper."
    - "The genuine no-data state does not expose any tenant-scope-widening control; the filters toolbar (the only scope-widening vector) is withheld there."
  artifacts:
    - "mailglass_admin/lib/mailglass_admin/operator_live.ex (Deliveries branch render conditions gated by no-data/no-match/populated)"
    - "mailglass_admin/test/mailglass_admin/operator_live_test.exs (assertions updated for empty-pane-only orientation + toolbar gating)"
  key_links:
    - "operator_live.ex Deliveries branch reads filters_active?/1 + @deliveries to discriminate no-data vs no-match vs populated"
    - "operator_live.ex no-data pane calls Shell.orientation_strip surface={:deliveries} (the ONLY caller after this change)"
---

<objective>
Apply the Phase 119 empty-pane-only IA pattern to the Deliveries branch of `operator_live.ex`: gate the
filters toolbar + "Open delivery" CTA on populated-OR-no-match (withhold in genuine no-data), and relocate
the `orientation_strip surface={:deliveries}` to render ONLY in the genuine no-data pane (remove it from
the populated-but-unselected detail column). Update the co-located ExUnit suite (`operator_live_test.exs`)
in the same plan so it asserts the new render conditions instead of the old always-visible orientation.

This resolves DEFECT-REGISTER **D-FILTERS-ON-EMPTY**, **D-ORIENT-REDUNDANT** (Deliveries application),
and **D-LABEL-TRIPLING** (side-effect of dropping the third "Deliveries" heading). Implements CONTEXT
decisions D-01..D-08, D-10 (ExUnit half), D-11, D-12.

Purpose: Convert the Deliveries surface from an info-dump (filters + empty + double orientation on a
zero-data tenant; orientation strip taxing every populated view) into a streamlined, least-surprise,
three-state IA, inheriting the cleaned-up 119 patterns.

Output: Render-condition-gated Deliveries branch + updated ExUnit assertions. Zero new components, zero
new tokens, zero new Tailwind classes (render-condition wrappers only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/120-deliveries-surface-redesign/120-CONTEXT.md
@.planning/phases/120-deliveries-surface-redesign/120-PATTERNS.md
@.planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md
@.planning/research/v1.14/DEFECT-REGISTER.md
@.planning/research/v1.14/STRESS-TEST-PROMPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the filters toolbar + Open-delivery CTA on populated-or-no-match; relocate orientation strip to the genuine-no-data pane only</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the Deliveries branch, the `@view != :overview` else-branch of render/1, ~488-647 — the FILTERS section :489-526 with submit "Open delivery" :519 and toolbar "Clear filters" :520-522; the detail-column cond with the `is_nil(@selected_delivery)` clause at :580-594 and the `@detail_error` clause :568-579; the master-detail grid :528-536; helpers filters_active?/1 :684-687, all_clear?/1, empty_page_meta/0)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the Overview empty-pane-only orientation gate at :461-469 — the in-file analog to copy the shape from)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (:68-99 no-data vs no-match branch emitting `operator-empty-truly` vs `operator-empty-filtered`+`operator-empty-reset`; :37-67 data_state capability — VERIFY, do not edit)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (:384-424 orientation_strip/1 + copy_for(:deliveries) — byte-frozen, DO NOT edit; data-testid="deliveries-orientation" is the seam)
    - .planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md (Interaction Contracts DELIV-01a/01b; States and Viewports table)
    - .planning/phases/119-app-shell-nav-overview-redesign/119-UI-SPEC.md (inherited empty-pane-only pattern, motion + a11y)
  </read_first>
  <action>
    Introduce the three-state IA discriminator in the Deliveries branch using the EXISTING truth — do NOT
    invent a new assign or flag (D-01). The discriminator is the pair `{@deliveries == [], filters_active?(@filter_params)}`:
    genuine no-data = `@deliveries == [] and not filters_active?(@filter_params)`; no-match =
    `@deliveries == [] and filters_active?(@filter_params)`; populated = otherwise. Reuse `filters_active?/1`
    (operator_live.ex:684-687).

    (1) FILTERS toolbar gating (D-02/D-03/D-04): wrap the entire FILTERS `<section data-testid="operator-filters">`
    (operator_live.ex:489-526, including the "Open delivery" submit at :519 and toolbar "Clear filters" at
    :520-522) in a render condition that is true in populated OR no-match, and false in genuine no-data.
    Use a HEEx `:if` on the section (or a surrounding `<%= if ... do %>`), keyed on
    `@deliveries != [] or filters_active?(@filter_params)`. Withhold the toolbar entirely in genuine no-data —
    an enabled control over an empty set is a least-surprise violation (STRESS-TEST C/E footgun). Security:
    withholding the toolbar in no-data removes the only scope-widening vector; `FiltersForm.fields` (:510-516)
    exposes only status/event/window, never a tenant control — do not add one (D-04).

    (2) Orientation strip relocation (D-05/D-06/D-07/D-08): DELETE the
    `<MailglassAdmin.Operator.Shell.orientation_strip surface={:deliveries} />` line from the detail-column
    `is_nil(@selected_delivery) ->` clause (operator_live.ex:580-581) — it currently fires on every
    populated-but-unselected view. KEEP the `operator-empty-detail` "Select a delivery to inspect…" helper
    that follows (:582-594) — it is the correct column-fill affordance, not redundant orientation (D-06).
    Then render the strip ONLY in the genuine-no-data pane: in the genuine-no-data state, present a single
    calm pane consisting of the existing `operator-empty-truly` empty-state (rendered by `deliveries_list`)
    followed by `Shell.orientation_strip surface={:deliveries}` wrapped in a `<div data-testid="deliveries-orientation">`-bearing
    element — model the wrapper shape on the Overview gate at operator_live.ex:461-469 (the `:if`-wrapped
    `operator-overview-orientation` div). Note: the strip primitive itself already emits
    `data-testid="deliveries-orientation"` (shell.ex), so do not double-wrap with a second conflicting testid;
    follow the Overview analog exactly. The orientation copy stays byte-frozen — change only WHERE it renders,
    never the text (D-07). D-LABEL-TRIPLING resolves automatically (D-08): the third "Deliveries" heading is
    gone from populated views, leaving nav item + page `<h1>` as the two canonical instances.

    (3) Matrix-state wiring (D-09 — verify, do not rebuild): error / permission-denied / stale route through
    the existing `deliveries_list` `data_state` capability (:37-67) and the existing detail-column
    `@detail_error` branch (:568-579). Do NOT build net-new error/permission UI — it would duplicate a tested
    primitive and diverge from locked voice tests. Preserve the master-detail grid template (:528-536) verbatim;
    this is gating + strip relocation, not a layout rewrite.

    (4) Motion + classes (D-11/D-13): add NO new Tailwind classes, NO new keyframes, NO bespoke motion CSS.
    The empty/no-data panes and the strip are token-clean. This means the committed
    `mailglass_admin/priv/static/app.css` MUST remain bit-identical — do NOT run `mix assets.build` in this
    plan (no class changes). If you find yourself adding a utility class, stop and reconsider; the redesign is
    render-condition gating only.

    Do not place fenced code blocks in this action; follow the in-file Overview analog (operator_live.ex:461-469)
    for the wrapper shape.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs 2>&1 | tail -20</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/app.css && echo "BUNDLE-CLEAN-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `mailglass_admin/lib/mailglass_admin/operator_live.ex` no longer renders `orientation_strip surface={:deliveries}` inside the `is_nil(@selected_delivery)` detail-column clause (grep: the strip call does not appear in that clause body).
    - The FILTERS `<section data-testid="operator-filters">` is wrapped in a render condition keyed on `@deliveries != [] or filters_active?(@filter_params)` (no new assign introduced; `filters_active?/1` reused).
    - `operator-empty-detail` "Select a delivery…" helper is still present in the populated-unselected detail column (D-06).
    - `mailglass_admin/priv/static/app.css` is byte-identical to HEAD (no `mix assets.build` run; `git diff --exit-code` passes).
    - The orientation strip copy in `shell.ex` is unmodified (`git diff --exit-code mailglass_admin/lib/mailglass_admin/operator/shell.ex` passes — byte-frozen).
    - `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes (after Task 2 updates assertions).
  </acceptance_criteria>
  <done>
    Filters toolbar + Open-delivery CTA render only in populated/no-match; orientation strip renders only in
    genuine no-data; "Select a delivery…" helper preserved; no new classes; bundle untouched; shell copy frozen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update operator_live_test.exs to assert the new empty-pane-only orientation + toolbar gating (paired ExUnit update — D-10)</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (:27-37 the populated/unselected test that currently asserts `data-testid="deliveries-orientation"` present on a populated view — GAP-07 comment at :36; :40-52 the adjacent empty-state test — the analog for asserting the strip's NEW location)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the gating from Task 1 — the same render conditions the test must now assert)
    - .planning/phases/120-deliveries-surface-redesign/120-PATTERNS.md (the operator_live_test.exs:37 paired-update section — refute on populated, assert in genuine no-data)
    - .planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md (Test Gate Contract — paired test updates table)
  </read_first>
  <action>
    Update the co-located ExUnit suite so it asserts the Task-1 end-state instead of the old always-visible
    orientation (this is the Pitfall-2 / 119-D-09 paired-update trap — the assertion goes RED on the
    green-only-forward floor the moment Task 1 lands, so it MUST change in the same plan).

    In the populated/unselected test (operator_live_test.exs:27-37): change the
    `assert html =~ ~s(data-testid="deliveries-orientation")` (the GAP-07 line at :37) to a
    `refute html =~ ~s(data-testid="deliveries-orientation")` — the strip is empty-pane-only now and MUST be
    absent on a populated view. Keep the existing assertions that `operator-master-detail`, "Recent deliveries",
    and the "Select a delivery to inspect…" helper are present (D-06 — the helper stays). Update the stale
    GAP-07 comment to describe the new empty-pane-only contract.

    In the adjacent empty-state test (operator_live_test.exs:40-52 — the genuine no-data case): ADD
    `assert html =~ ~s(data-testid="deliveries-orientation")` (the strip's NEW location), ADD
    `assert html =~ ~s(data-testid="operator-empty-truly")` (the calm no-data pane), and ADD
    `refute html =~ ~s(data-testid="operator-filters")` to lock toolbar-withheld-in-no-data (D-02/D-04). If
    the existing empty-state test drives a state that is genuine no-data (no active filters), use it directly;
    if it is ambiguous, set up the live view with default filter params (no active filters) and an empty
    delivery set so `filters_active?/1` is false. Do NOT assert tenant-widening controls exist — the no-data
    state must expose none (security boundary).

    If a no-match-specific assertion is cheap to add in this suite, also assert that with active filters and an
    empty set, `operator-filters` IS present and `deliveries-orientation` is ABSENT (locks the no-match state);
    otherwise leave the no-match locking to the Playwright judgment gate in Plan 02.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The populated/unselected test now `refute`s `data-testid="deliveries-orientation"` (was an `assert`).
    - The genuine-no-data test `assert`s `data-testid="deliveries-orientation"` AND `data-testid="operator-empty-truly"` AND `refute`s `data-testid="operator-filters"`.
    - The "Select a delivery to inspect…" helper assertion is preserved in the populated/unselected test.
    - `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes (green against the Task-1 implementation).
    - No assertion expects a tenant-scope-widening control in any empty/no-data state.
  </acceptance_criteria>
  <done>
    The ExUnit suite asserts the new render conditions (orientation empty-pane-only; toolbar withheld in
    no-data, kept in no-match; helper preserved) and passes green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (browser) → OperatorLive (URL-backed filter params) | Untrusted query params (`tenant_id`, status/event/window filters) cross into the LiveView; tenant scope must never widen here. |
| OperatorLive render branch → deliveries_list / shell primitives | Render-condition gating must not expose a control (e.g., a tenant selector) in a state where scope could be widened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-01 | Elevation of Privilege | Deliveries empty/no-data state (`operator_live.ex` Deliveries branch) | high | mitigate | Withhold the FILTERS toolbar entirely in genuine no-data (D-02/D-04) — it is the only scope-widening vector; `FiltersForm.fields` exposes only status/event/window, never a tenant control. Task 2 adds a `refute ... operator-filters` assertion in the no-data ExUnit test so a future change re-introducing the toolbar (or any tenant control) in no-data fails closed. |
| T-120-02 | Tampering | URL filter params in no-match state | low | accept | In no-match the prefilled single-tenant `tenant_id` stays scoped; no new param surface is introduced (render-condition change only). Existing param handling is unchanged and already tested. |
| T-120-03 | Information Disclosure | Recipient PII in delivery rows | low | accept | `mask_recipient/1` already applies in both table and card presentations (deliveries_list.ex, verified, not rebuilt — D-09). No change to data exposure in this plan. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes (orientation empty-pane-only + toolbar gating asserted).
- `git diff --exit-code mailglass_admin/priv/static/app.css` passes (no class changes → bundle untouched, D-13).
- `git diff --exit-code mailglass_admin/lib/mailglass_admin/operator/shell.ex` passes (orientation copy byte-frozen, D-07).
- No new assign/flag introduced (grep confirms `filters_active?/1` reuse, no new `assign(:...)` for the discriminator).
</verification>

<success_criteria>
- Three-state Deliveries IA (no-data / no-match / populated) driven off the existing truth, no new flag.
- Filters toolbar + Open-delivery CTA withheld in genuine no-data, present in populated/no-match.
- Orientation strip renders only in genuine no-data; removed from the populated-unselected detail column.
- "Select a delivery…" helper preserved; orientation copy and shell unchanged.
- ExUnit suite green; committed app.css bit-identical.
</success_criteria>

<artifacts_produced>
This phase is edit-heavy with no new public symbols. Render-condition predicates / testids / assertions introduced or relied upon:
- Render condition: `@deliveries != [] or filters_active?(@filter_params)` gating the `operator-filters` section (new inline render condition; no new function).
- Render condition: genuine-no-data predicate (`@deliveries == [] and not filters_active?(@filter_params)`) gating the `deliveries-orientation` strip wrapper (new inline render condition).
- ExUnit assertions: populated view `refute deliveries-orientation`; no-data view `assert deliveries-orientation` + `assert operator-empty-truly` + `refute operator-filters`.
- Testids relied upon (pre-existing, unchanged): `deliveries-orientation`, `operator-filters`, `operator-empty-truly`, `operator-empty-filtered`, `operator-empty-reset`, `operator-empty-detail`, `operator-master-detail`, `operator-detail-error`.
</artifacts_produced>

<output>
Create `.planning/phases/120-deliveries-surface-redesign/120-01-SUMMARY.md` when done.
</output>
