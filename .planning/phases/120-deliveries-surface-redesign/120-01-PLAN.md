---
phase: 120-deliveries-surface-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/test/mailglass_admin/operator_live_test.exs
autonomous: true
requirements: [DELIV-01]

must_haves:
  truths:
    - "On a genuine no-data Deliveries view (no rows, no active filters), the render tree is a SINGLE calm pane only: operator-empty-truly + orientation strip. The filters toolbar, the Open-delivery CTA, the operator-master-detail grid, and the 'Select a delivery…' helper are ALL withheld (the helper lives inside the withheld grid)."
    - "On a no-match Deliveries view (no rows, filters active), the filters toolbar IS rendered so the operator keeps the Clear-filters escape; the master-detail grid renders per existing behavior; the orientation strip is NOT rendered."
    - "On a populated Deliveries view (rows present), the filters toolbar IS rendered, the master-detail grid renders, and the orientation strip is NOT rendered (removed from the populated-but-unselected detail column)."
    - "The populated-but-unselected detail column still renders the 'Select a delivery to inspect…' master-detail helper (D-06 — proven positively, not just removed from no-data)."
    - "The genuine no-data state does not expose any tenant-scope-widening control; the filters toolbar (the only scope-widening vector) is withheld there."
  artifacts:
    - "mailglass_admin/lib/mailglass_admin/operator_live.ex (Deliveries branch: genuine-no-data renders single-calm-pane only; no-match/populated render filters toolbar + master-detail)"
    - "mailglass_admin/test/mailglass_admin/operator_live_test.exs (genuine-no-data test corrected: drops the 'Select a delivery…' assertion, adds orientation+empty-truly asserts, refutes filters; populated-unselected test positively proves the helper still renders — D-06)"
  key_links:
    - "operator_live.ex Deliveries branch reads filters_active?/1 + @deliveries to discriminate no-data vs no-match vs populated"
    - "operator_live.ex genuine-no-data branch renders ONLY operator-empty-truly + Shell.orientation_strip surface={:deliveries}; it withholds the operator-master-detail grid (and therefore the operator-empty-detail helper) entirely"
---

<objective>
Apply the Phase 119 empty-pane-only IA pattern to the Deliveries branch of `operator_live.ex`. In genuine
no-data (`@deliveries == [] and not filters_active?(@filter_params)`) render a SINGLE calm pane only —
`operator-empty-truly` + `orientation_strip surface={:deliveries}` — and WITHHOLD the filters toolbar, the
"Open delivery" CTA, the entire `operator-master-detail` grid, and (because the helper lives inside that
grid) the `operator-empty-detail` "Select a delivery…" helper (UI-SPEC lines 271-274, 199-207; D-03/D-05).
In no-match and populated states, render the filters toolbar + master-detail per existing behavior, with the
orientation strip removed from the populated-but-unselected detail column (D-05) and the "Select a delivery…"
helper RETAINED in that column (D-06). Update the co-located ExUnit suite (`operator_live_test.exs`) in the
same plan so it asserts the corrected render conditions: the genuine-no-data test drops its now-false
"Select a delivery…" assertion, and a populated-unselected assertion positively proves the helper still
renders (D-06).

This resolves DEFECT-REGISTER **D-FILTERS-ON-EMPTY**, **D-ORIENT-REDUNDANT** (Deliveries application),
and **D-LABEL-TRIPLING** (side-effect of dropping the third "Deliveries" heading). Implements CONTEXT
decisions D-01..D-08, D-10 (ExUnit half), D-11, D-12.

Purpose: Convert the Deliveries surface from an info-dump (filters + empty + double orientation on a
zero-data tenant; orientation strip taxing every populated view) into a streamlined, least-surprise,
three-state IA, inheriting the cleaned-up 119 patterns.

Output: Render-condition-gated Deliveries branch + updated ExUnit assertions. Zero new components, zero
new tokens, zero new Tailwind classes (render-condition wrappers only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/120-deliveries-surface-redesign/120-CONTEXT.md
@.planning/phases/120-deliveries-surface-redesign/120-PATTERNS.md
@.planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md
@.planning/research/v1.14/DEFECT-REGISTER.md
@.planning/research/v1.14/STRESS-TEST-PROMPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the filters toolbar + Open-delivery CTA on populated-or-no-match; relocate orientation strip to the genuine-no-data pane only</name>
  <files>mailglass_admin/lib/mailglass_admin/operator_live.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the Deliveries branch = the `@view != :overview` else-branch of render/1 opening at :488, closing at :646-647 — within it: the FILTERS `<section data-testid="operator-filters">` :489-526 with submit "Open delivery" :519 and toolbar "Clear filters" :520-522; the `<section data-testid="operator-master-detail">` grid :528-632 which contains the list-card aside :538-558, the detail-column `cond` with the `@detail_error` clause :568-579 and the `is_nil(@selected_delivery)` clause :580-594 — note the orientation strip is at :581 and the `operator-empty-detail` "Select a delivery…" helper is at :582-594, BOTH inside the master-detail grid; the ReplayModal :640-645; helpers filters_active?/1 :684-687)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the Overview empty-pane-only orientation gate at :461-469 — the in-file analog to copy the single-calm-pane shape from)
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (:68-99 no-data vs no-match branch emitting `operator-empty-truly` vs `operator-empty-filtered`+`operator-empty-reset`; :37-67 data_state capability — VERIFY, do not edit)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (:384-424 orientation_strip/1 + copy_for(:deliveries) — byte-frozen, DO NOT edit; data-testid="deliveries-orientation" is the seam)
    - .planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md (Interaction Contracts DELIV-01a/01b; States and Viewports table)
    - .planning/phases/119-app-shell-nav-overview-redesign/119-UI-SPEC.md (inherited empty-pane-only pattern, motion + a11y)
  </read_first>
  <action>
    Introduce the three-state IA discriminator in the Deliveries branch (the `<% else %>` opening at
    operator_live.ex:488) using the EXISTING truth — do NOT invent a new assign or flag (D-01). The
    discriminator is the pair `{@deliveries == [], filters_active?(@filter_params)}`: genuine no-data =
    `@deliveries == [] and not filters_active?(@filter_params)`; no-match = `@deliveries == [] and
    filters_active?(@filter_params)`; populated = otherwise. Reuse `filters_active?/1` (operator_live.ex:684-687).

    The authoritative render-tree contract (apply EXACTLY; UI-SPEC States table lines 271-274, single-calm-pane
    lines 199-207):
    - genuine no-data → a SINGLE calm pane: `operator-empty-truly` + `orientation_strip surface={:deliveries}`.
      NO filters toolbar, NO "Open delivery" CTA, NO `operator-master-detail` grid, and therefore NO
      `operator-empty-detail` "Select a delivery…" helper (the helper lives INSIDE the withheld grid at :582-594).
    - no-match → filters toolbar (Clear-filters escape) + master-detail grid per existing behavior; NO strip.
    - populated → filters toolbar + master-detail grid; NO strip on the populated-unselected detail column;
      the "Select a delivery…" helper RETAINED in that column.

    (1) Restructure the Deliveries `<% else %>` branch as a `cond`/`case` over the discriminator so the
    genuine-no-data arm renders ONLY the single calm pane and the no-match/populated arm renders the existing
    FILTERS section + master-detail grid. Concretely: wrap the genuine-no-data render so that when
    `@deliveries == [] and not filters_active?(@filter_params)` is true, the branch emits ONLY the
    `operator-empty-truly` empty-state (the `Components.data_state` pane — render it directly via
    `Components.data_state` or via `deliveries_list` with an empty set; whichever the Overview no-data analog
    uses) immediately followed by `<MailglassAdmin.Operator.Shell.orientation_strip surface={:deliveries} />`,
    and emits NOTHING else (no `operator-filters` section, no `operator-master-detail` section, no ReplayModal).
    Model the single-calm-pane wrapper shape on the Overview empty-pane-only gate at operator_live.ex:461-469.

    (2) FILTERS toolbar gating (D-02/D-03/D-04): the FILTERS `<section data-testid="operator-filters">`
    (operator_live.ex:489-526, including the "Open delivery" submit at :519 and toolbar "Clear filters" at
    :520-522) renders ONLY in the no-match/populated arm — i.e. when `@deliveries != [] or
    filters_active?(@filter_params)`. It is withheld in genuine no-data. Security: withholding the toolbar in
    no-data removes the only scope-widening vector; `FiltersForm.fields` (:510-516) exposes only
    status/event/window, never a tenant control — do not add one (D-04).

    (3) Master-detail grid gating (D-03 — the single-calm-pane fix): the entire `<section
    data-testid="operator-master-detail">` grid (operator_live.ex:528-632) also renders ONLY in the
    no-match/populated arm. In genuine no-data the grid is WITHHELD entirely — which is exactly what makes the
    `operator-empty-detail` "Select a delivery…" helper (:582-594, nested inside the grid's detail column) NOT
    render in no-data. This satisfies the single-calm-pane contract (D-03, UI-SPEC 271-274). D-06 is still
    honored: the helper is scoped to the populated-but-unselected case only, and the no-match/populated arm
    still renders the grid → detail column → the `is_nil(@selected_delivery)` clause → the helper.

    (4) Orientation strip relocation (D-05/D-06/D-07/D-08): DELETE the
    `<MailglassAdmin.Operator.Shell.orientation_strip surface={:deliveries} />` line from the detail-column
    `is_nil(@selected_delivery) ->` clause (operator_live.ex:581) — it currently fires on every
    populated-but-unselected view. KEEP the `operator-empty-detail` "Select a delivery to inspect…" helper that
    follows (:582-594) inside that clause — it is the correct populated-unselected column-fill affordance, not
    redundant orientation (D-06). The strip now renders ONLY in the genuine-no-data arm from step (1). The
    strip primitive already emits `data-testid="deliveries-orientation"` (shell.ex), so do not double-wrap with
    a second conflicting testid; follow the Overview analog exactly. The orientation copy stays byte-frozen —
    change only WHERE it renders, never the text (D-07). D-LABEL-TRIPLING resolves automatically (D-08): the
    third "Deliveries" heading is gone from populated views, leaving nav item + page `<h1>` as the two
    canonical instances.

    (5) Matrix-state wiring (D-09 — verify, do not rebuild): error / permission-denied / stale route through
    the existing `deliveries_list` `data_state` capability (:37-67) and the existing detail-column
    `@detail_error` branch (:568-579), all of which live inside the master-detail grid and thus apply in the
    no-match/populated arm. Do NOT build net-new error/permission UI — it would duplicate a tested primitive
    and diverge from locked voice tests. Make NO structural change to the master-detail grid template itself
    (:528-536) — the only change to the grid is that it is now gated out of genuine no-data; this is gating +
    strip relocation, not a layout rewrite.

    (6) Motion + classes (D-11/D-13): add NO new Tailwind classes, NO new keyframes, NO bespoke motion CSS.
    The empty/no-data panes and the strip are token-clean. This means the committed
    `mailglass_admin/priv/static/app.css` MUST remain bit-identical — do NOT run `mix assets.build` in this
    plan (no class changes). If you find yourself adding a utility class, stop and reconsider; the redesign is
    render-condition gating only.

    Do not place fenced code blocks in this action; follow the in-file Overview analog (operator_live.ex:461-469)
    for the single-calm-pane wrapper shape.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs 2>&1 | tail -20</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/app.css && echo "BUNDLE-CLEAN-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Genuine no-data (`@deliveries == [] and not filters_active?(@filter_params)`) renders a SINGLE calm pane only: a LiveView render of the Deliveries view with an empty delivery set and no active filters contains `data-testid="operator-empty-truly"` AND `data-testid="deliveries-orientation"` and does NOT contain `data-testid="operator-filters"`, does NOT contain `data-testid="operator-master-detail"`, and does NOT contain the string "Select a delivery to inspect" (the helper is withheld because the master-detail grid is withheld). Proven by the corrected Task-2 genuine-no-data ExUnit test.
    - The FILTERS `<section data-testid="operator-filters">` AND the `<section data-testid="operator-master-detail">` grid both render only when `@deliveries != [] or filters_active?(@filter_params)` (no new assign introduced; `filters_active?/1` reused).
    - `operator_live.ex` no longer renders `orientation_strip surface={:deliveries}` inside the `is_nil(@selected_delivery)` detail-column clause at :580-594 (grep: the strip call does not appear in that clause body); it renders only in the genuine-no-data arm.
    - `operator-empty-detail` "Select a delivery…" helper is still present in the populated-but-unselected detail column (D-06) — proven positively by the Task-2 populated-unselected assertion, not merely by its removal from no-data.
    - `mailglass_admin/priv/static/app.css` is byte-identical to HEAD (no `mix assets.build` run; `git diff --exit-code` passes).
    - The orientation strip copy in `shell.ex` is unmodified (`git diff --exit-code mailglass_admin/lib/mailglass_admin/operator/shell.ex` passes — byte-frozen).
    - `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes (after Task 2 updates assertions).
  </acceptance_criteria>
  <done>
    Genuine no-data renders a single calm pane only (operator-empty-truly + orientation strip); filters toolbar,
    Open-delivery CTA, master-detail grid, and the "Select a delivery…" helper are all withheld in no-data.
    No-match/populated render the toolbar + master-detail; the orientation strip is removed from the
    populated-unselected detail column; the "Select a delivery…" helper is retained there (D-06). No new
    classes; bundle untouched; shell copy frozen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct operator_live_test.exs for the single-calm-pane no-data contract — drop the now-false :51 "Select a delivery…" no-data assertion, add empty-truly/orientation/refute-filters/refute-master-detail + rename to "genuine no-data"; positively prove the D-06 helper in the populated-unselected test (paired ExUnit update — D-10)</name>
  <files>mailglass_admin/test/mailglass_admin/operator_live_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/operator_live_test.exs (:23-38 "renders the default detail prompt when no delivery is selected" = the POPULATED-UNSELECTED test — it inserts a delivery at :24, asserts `operator-master-detail` :31, asserts the "Select a delivery to inspect…" helper :34, and at :36-37 the stale GAP-07 comment + `assert ~s(data-testid="deliveries-orientation")`; :40-52 "renders the recent deliveries empty state" = the GENUINE-NO-DATA test — it inserts NO delivery, uses default filter params so `filters_active?/1` is false, asserts "No deliveries" :46, the empty-truly body :48-49, and AT LINE 51 asserts the "Select a delivery to inspect…" helper which now goes RED)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the gating from Task 1 — the same render conditions the tests must now assert: genuine no-data = single calm pane only; populated-unselected = master-detail + helper, no strip)
    - .planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md (Test Gate Contract — paired test updates table; States table lines 271-274)
  </read_first>
  <action>
    Update the co-located ExUnit suite so it asserts the Task-1 end-state instead of the old always-visible
    orientation (this is the Pitfall-2 / 119-D-09 paired-update trap — the assertions go RED on the
    green-only-forward floor the moment Task 1 lands, so they MUST change in the same plan).

    (A) POPULATED-UNSELECTED test, "renders the default detail prompt when no delivery is selected"
    (operator_live_test.exs:23-38): change the `assert html =~ ~s(data-testid="deliveries-orientation")` (the
    GAP-07 line at :37) to a `refute html =~ ~s(data-testid="deliveries-orientation")` — the strip is
    empty-pane-only now and MUST be absent on a populated view. KEEP the existing assertions that
    `operator-master-detail` (:31) and "Recent deliveries" (:30) are present, and KEEP the
    `assert html =~ "Select a delivery to inspect its event timeline and suppression state."` at :34 — this is
    the POSITIVE proof that the D-06 helper still renders in the populated-but-unselected case (do NOT remove
    this assertion; it is the load-bearing positive D-06 proof). Update the stale GAP-07 comment at :36 to
    describe the new contract ("orientation strip is empty-pane-only: absent on populated, present only in
    genuine no-data").

    (B) GENUINE-NO-DATA test, currently "renders the recent deliveries empty state"
    (operator_live_test.exs:40-52): this is the genuine no-data case (no delivery inserted, default filter
    params → `filters_active?/1` is false). Make the following changes so it matches the single-calm-pane
    contract:
    - REMOVE the now-false assertion at line 51:
      `assert html =~ "Select a delivery to inspect its event timeline and suppression state."` — the helper
      lives inside the `operator-master-detail` grid, which is WITHHELD in genuine no-data, so the helper no
      longer renders here. This assertion would go RED against Task 1; delete it.
    - KEEP the existing `assert html =~ "No deliveries"` (:46) and `assert html =~ "No deliveries have been
      recorded yet."` (:48-49).
    - ADD `assert html =~ ~s(data-testid="operator-empty-truly")` (the calm no-data pane).
    - ADD `assert html =~ ~s(data-testid="deliveries-orientation")` (the strip's NEW and only location).
    - ADD `refute html =~ ~s(data-testid="operator-filters")` (toolbar withheld — locks D-02/D-04).
    - ADD `refute html =~ ~s(data-testid="operator-master-detail")` (grid withheld — locks the single-calm-pane
      contract, D-03).
    - RENAME the test and fix its comment from the misleading generic "empty state" to make the state explicit,
      e.g. "renders a single calm pane (empty-truly + orientation strip) in genuine no-data (no rows, no active
      filters)". The name must say "genuine no-data", not "empty state".
    Do NOT assert any tenant-widening control exists — the no-data state must expose none (security boundary).

    (C) NO-MATCH locking (cheap to add in this suite — add it): add a test (or extend an existing
    filters-applied test) that drives the no-match state (active filters that yield zero rows, e.g. apply a
    status/window filter that matches nothing, or apply filters with an empty delivery set so `filters_active?/1`
    is true) and asserts `operator-filters` IS present (Clear-filters escape kept) AND `deliveries-orientation`
    is ABSENT (strip not in no-match) AND the in-pane `operator-empty-filtered` is present. This locks the
    no-match state in ExUnit; the Playwright judgment gate in Plan 02 also covers it.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The populated-unselected test (:23-38) now `refute`s `data-testid="deliveries-orientation"` (was an `assert` at :37), and STILL `assert`s the "Select a delivery to inspect its event timeline and suppression state." helper at :34 (positive D-06 proof retained).
    - The genuine-no-data test (formerly :40-52): the line-51 `assert ... "Select a delivery to inspect…"` is REMOVED; the test now `assert`s `data-testid="operator-empty-truly"` AND `data-testid="deliveries-orientation"`, and `refute`s `data-testid="operator-filters"` AND `data-testid="operator-master-detail"`.
    - The genuine-no-data test is renamed/commented to say "genuine no-data" (not a generic "empty state").
    - A no-match assertion exists (new or extended test) proving: `operator-filters` present + `operator-empty-filtered` present + `deliveries-orientation` absent.
    - `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes (green against the Task-1 implementation).
    - No assertion expects a tenant-scope-widening control in any empty/no-data state.
  </acceptance_criteria>
  <done>
    The ExUnit suite asserts the corrected render conditions: genuine no-data = single calm pane (empty-truly +
    orientation strip, no toolbar, no master-detail, no helper); populated-unselected positively proves the
    "Select a delivery…" helper still renders (D-06); no-match keeps the toolbar and withholds the strip. Suite
    passes green; the misleading "empty state" test name is corrected to "genuine no-data".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (browser) → OperatorLive (URL-backed filter params) | Untrusted query params (`tenant_id`, status/event/window filters) cross into the LiveView; tenant scope must never widen here. |
| OperatorLive render branch → deliveries_list / shell primitives | Render-condition gating must not expose a control (e.g., a tenant selector) in a state where scope could be widened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-01 | Elevation of Privilege | Deliveries empty/no-data state (`operator_live.ex` Deliveries branch) | high | mitigate | Withhold the FILTERS toolbar entirely in genuine no-data (D-02/D-04) — it is the only scope-widening vector; `FiltersForm.fields` exposes only status/event/window, never a tenant control. Task 2 adds a `refute ... operator-filters` assertion in the no-data ExUnit test so a future change re-introducing the toolbar (or any tenant control) in no-data fails closed. |
| T-120-02 | Tampering | URL filter params in no-match state | low | accept | In no-match the prefilled single-tenant `tenant_id` stays scoped; no new param surface is introduced (render-condition change only). Existing param handling is unchanged and already tested. |
| T-120-03 | Information Disclosure | Recipient PII in delivery rows | low | accept | `mask_recipient/1` already applies in both table and card presentations (deliveries_list.ex, verified, not rebuilt — D-09). No change to data exposure in this plan. |
</threat_model>

<verification>
- `cd mailglass_admin && mix test test/mailglass_admin/operator_live_test.exs` passes (orientation empty-pane-only + toolbar gating asserted).
- `git diff --exit-code mailglass_admin/priv/static/app.css` passes (no class changes → bundle untouched, D-13).
- `git diff --exit-code mailglass_admin/lib/mailglass_admin/operator/shell.ex` passes (orientation copy byte-frozen, D-07).
- No new assign/flag introduced (grep confirms `filters_active?/1` reuse, no new `assign(:...)` for the discriminator).
</verification>

<success_criteria>
- Three-state Deliveries IA (no-data / no-match / populated) driven off the existing truth, no new flag.
- Genuine no-data renders a SINGLE calm pane only (operator-empty-truly + orientation strip); filters toolbar, Open-delivery CTA, master-detail grid, and the "Select a delivery…" helper are ALL withheld there.
- No-match/populated render the filters toolbar + master-detail grid; orientation strip removed from the populated-unselected detail column; "Select a delivery…" helper retained there (D-06, positively proven).
- Orientation copy and shell unchanged; committed app.css bit-identical.
- ExUnit suite green: genuine-no-data test corrected (line-51 assertion dropped, renamed), populated-unselected test positively proves the helper, no-match locked.
</success_criteria>

<artifacts_produced>
This phase is edit-heavy with no new public symbols. Render-condition predicates / testids / assertions introduced or relied upon:
- Render condition: `@deliveries != [] or filters_active?(@filter_params)` gating BOTH the `operator-filters` section AND the `operator-master-detail` grid (new inline render conditions; no new function).
- Render condition: genuine-no-data predicate (`@deliveries == [] and not filters_active?(@filter_params)`) gating the single calm pane (`operator-empty-truly` + `deliveries-orientation` strip), and implicitly withholding `operator-master-detail` (and therefore `operator-empty-detail`).
- ExUnit assertions: populated-unselected view `refute deliveries-orientation` + `assert "Select a delivery to inspect…"` (positive D-06); genuine-no-data view drops the :51 helper assertion, `assert deliveries-orientation` + `assert operator-empty-truly` + `refute operator-filters` + `refute operator-master-detail`; no-match `assert operator-filters` + `assert operator-empty-filtered` + `refute deliveries-orientation`.
- Testids relied upon (pre-existing, unchanged): `deliveries-orientation`, `operator-filters`, `operator-empty-truly`, `operator-empty-filtered`, `operator-empty-reset`, `operator-empty-detail`, `operator-master-detail`, `operator-detail-error`.
</artifacts_produced>

<output>
Create `.planning/phases/120-deliveries-surface-redesign/120-01-SUMMARY.md` when done.
</output>
