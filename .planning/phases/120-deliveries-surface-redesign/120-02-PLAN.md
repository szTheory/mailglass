---
phase: 120-deliveries-surface-redesign
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - mailglass_admin/e2e/operator.spec.js
  - reference/demo_app/assets/e2e/persona-screenshots.spec.js
autonomous: true
requirements: [DELIV-01]

must_haves:
  truths:
    - "The operator browser gate (operator.spec.js) no longer asserts the orientation strip above a populated Deliveries list; it asserts the strip is absent on populated Deliveries and present on genuine no-data."
    - "A new empty-pane-only judgment assertion locks the Deliveries end-state: deliveries-orientation count 0 on populated, count 1 on genuine no-data; operator-filters AND operator-master-detail both absent in genuine no-data (single calm pane), filters present in no-match."
    - "The inherited v1.13 ratchet floor remains green only-forward via the enumerated commands (token_parity_test.exs, verify.support_contract.admin, ratchet_baseline + axe_baseline comparators, check-conformance + check-conformance-advisory); the committed app.css is unchanged."
    - "The full state×viewport×theme matrix (8 states × {320/390/768/1024/wide} × {light/dark/system}) is verified via an explicit per-cell ledger in the SUMMARY — each cell automated / re-shoot / documented-human-follow-up, none silently assumed."
    - "The Deliveries persona cells are re-shot post-redesign for only-forward D-THEME-PARITY proof; if the re-shoot env is unreachable, D-THEME-PARITY proof is explicitly carried as an open follow-up to Phase 123's re-score, not assumed satisfied here."
  artifacts:
    - "mailglass_admin/e2e/operator.spec.js (mobile orientation-order test + openOperator comments updated; new Deliveries empty-pane-only judgment test added)"
    - "reference/demo_app/assets/e2e/persona-screenshots.spec.js (re-run only — no code edit)"
  key_links:
    - "operator.spec.js Deliveries judgment test keys on deliveries-orientation + operator-filters testids emitted by the Plan 01 render conditions"
    - "persona-screenshots.spec.js re-shoots deliveries-{persona}-{375,1440}-{light,dark} cells against the redesigned surface"
---

<objective>
Land the mandatory Playwright paired-test updates (D-10) that go RED the moment Plan 01's render-condition
gating lands on the green-only-forward floor: update the mobile "orientation before list" assertion and the
`openOperator` heading-ambiguity comments in `operator.spec.js`, and ADD a new Deliveries empty-pane-only
judgment assertion modeled on the Overview gate. Re-shoot the Deliveries persona cells for only-forward
proof, and verify the inherited v1.13 ratchet floor + D-THEME-PARITY (light/dark/system) stay green.

Implements CONTEXT D-10 (Playwright half), D-13 (floor hold / asset verification), and the
DEFECT-REGISTER D-THEME-PARITY guardrail.

Purpose: Lock the Deliveries redesign end-state into the ratchet so D-FILTERS-ON-EMPTY / D-ORIENT-REDUNDANT
cannot silently regress, and prove only-forward improvement across the viewport×theme matrix.

Output: Updated + extended `operator.spec.js`; re-shot Deliveries persona cells; verified green floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/120-deliveries-surface-redesign/120-CONTEXT.md
@.planning/phases/120-deliveries-surface-redesign/120-PATTERNS.md
@.planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md
@.planning/research/v1.14/DEFECT-REGISTER.md
@.planning/phases/120-deliveries-surface-redesign/120-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the paired Playwright assertions + add the Deliveries empty-pane-only judgment gate (D-10)</name>
  <files>mailglass_admin/e2e/operator.spec.js</files>
  <read_first>
    - mailglass_admin/e2e/operator.spec.js (:83-115 the mobile "orientation before list" test asserting the strip above a populated list — `orientationBox.y < deliveriesBox.y` and the 390px `deliveries-orientation` visibility check; :25-32 the `openOperator` helper comments + the heading-ambiguity note about a second `<h2>Deliveries</h2>`; :113-114; the Overview empty-pane-only judgment test at :382-396 — THE analog for the new Deliveries gate)
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (the Plan 01 render conditions — the testids the new test keys on: deliveries-orientation, operator-filters, operator-empty-truly, operator-empty-filtered)
    - .planning/phases/120-deliveries-surface-redesign/120-PATTERNS.md (operator.spec.js paired-update sections + new-gate guidance)
    - .planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md (Test Gate Contract — paired test updates table)
    - .planning/phases/120-deliveries-surface-redesign/120-01-SUMMARY.md (how Plan 01 wired the discriminator + which states map to which fixture/route)
  </read_first>
  <action>
    Update the operator browser gate so it asserts the Plan-01 end-state instead of the old always-visible
    orientation. This is the Pitfall-2 / 119-D-09 trap: these assertions go RED on the green-only-forward
    floor the moment Plan 01 lands, so they MUST change in this phase.

    (1) Mobile "orientation before list" test (operator.spec.js:83-115): this test asserts the orientation
    strip renders ABOVE a populated Deliveries list (`orientationBox.y` < `deliveriesBox.y`) and that
    `deliveries-orientation` is visible at 390px on a populated `&view=deliveries` route. After Plan 01 the
    strip is empty-pane-only and is NOT rendered on a populated view, so this test is now wrong. Rework it:
    on a POPULATED Deliveries view assert `deliveries-orientation` has count 0 (strip absent); preserve any
    still-valid detail-section-order assertions that do not depend on the strip. If the test's sole purpose
    was strip-above-list ordering, repurpose it to assert the strip's absence on populated and move the
    strip-presence proof to the genuine-no-data state. Do not delete coverage — convert it.

    (2) `openOperator` helper comments + heading-ambiguity note (operator.spec.js:25-32, :113-114): the
    `level: 1` heading qualifier was added because the strip rendered a second `<h2>Deliveries</h2>` on
    populated views (the D-LABEL-TRIPLING third heading). After Plan 01 that `<h2>` is gone from populated
    views (D-08), so the ambiguity COMMENT is now stale — update the comment to reflect that the strip is
    empty-pane-only and the second heading no longer renders on populated views. The `level: 1` query stays
    (harmless and still correct) — do not weaken it.

    (3) NEW Deliveries empty-pane-only judgment gate: add a test modeled byte-for-shape on the Overview gate
    (operator.spec.js:382-396). Assert, across the relevant fixtures/routes:
    - On a POPULATED Deliveries view: `getByTestId("deliveries-orientation")` toHaveCount 0 AND
      `getByTestId("operator-filters")` is visible (toolbar present).
    - On a GENUINE NO-DATA Deliveries view (zero-data tenant, no active filters — use the helios-void persona
      tenant or the existing zero-data fixture/route the Plan 01 summary identifies):
      `getByTestId("deliveries-orientation")` toHaveCount 1 AND `getByTestId("operator-filters")` toHaveCount 0
      (toolbar WITHHELD — locks D-02/D-04) AND `getByTestId("operator-master-detail")` toHaveCount 0 (grid
      WITHHELD — locks the single-calm-pane contract D-03; this is also why the "Select a delivery…" helper does
      not render in no-data) AND `getByTestId("operator-empty-truly")` is visible.
    - On a NO-MATCH Deliveries view (active filters, empty result set): `getByTestId("operator-filters")` is
      visible (Clear-filters escape kept) AND `getByTestId("deliveries-orientation")` toHaveCount 0 (strip
      not in no-match). If a no-match fixture route is not readily reachable in this suite, drive it by
      navigating with a filter param that yields zero rows on a populated tenant.
    Name the test to make the judgment explicit (e.g., describing "deliveries orientation strip is
    empty-pane-only; filters toolbar withheld on no-data, kept on no-match"). Reuse the existing
    `openOperator` / `page.goto(.../ops/mail?...&view=deliveries)` helpers and tenant ids already in the file.

    Add NO new Tailwind classes anywhere; this task touches only the spec file. The committed
    `mailglass_admin/priv/static/app.css` MUST stay bit-identical (no `mix assets.build`).
  </action>
  <verify>
    <automated>cd mailglass_admin && npx playwright test e2e/operator.spec.js --workers=1 2>&1 | tail -30</automated>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/app.css && echo "BUNDLE-CLEAN-OK"</automated>
  </verify>
  <acceptance_criteria>
    - The mobile orientation-order test no longer asserts `orientationBox.y < deliveriesBox.y` on a populated list, nor asserts `deliveries-orientation` visible on a populated `&view=deliveries` route; it instead asserts the strip's absence on populated.
    - The `openOperator` heading-ambiguity comment is updated to state the strip is empty-pane-only (no second `<h2>Deliveries</h2>` on populated); the `level: 1` heading query is preserved.
    - A new judgment test exists asserting: populated → `deliveries-orientation` count 0 + `operator-filters` visible; genuine no-data → `deliveries-orientation` count 1 + `operator-filters` count 0 + `operator-master-detail` count 0 + `operator-empty-truly` visible; no-match → `operator-filters` visible + `deliveries-orientation` count 0.
    - `cd mailglass_admin && npx playwright test e2e/operator.spec.js --workers=1` passes (use `--workers=1` per the shared deterministic seed state precedent, STATE 99-05).
    - `mailglass_admin/priv/static/app.css` is byte-identical to HEAD (`git diff --exit-code` passes).
  </acceptance_criteria>
  <done>
    operator.spec.js asserts the empty-pane-only end-state (orientation absent on populated/no-match, present
    on no-data; toolbar withheld in no-data, kept in no-match) and passes green; bundle untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-shoot the Deliveries persona cells + verify the inherited v1.13 ratchet floor and D-THEME-PARITY hold green (D-13 / D-THEME-PARITY guardrail)</name>
  <files>reference/demo_app/assets/e2e/persona-screenshots.spec.js</files>
  <read_first>
    - reference/demo_app/assets/e2e/persona-screenshots.spec.js (:60 PERSONAS northstar/fjordline-aps/helios-void; :68 the deliveries surface registration `{ id: "deliveries", ..., suffix: "&view=deliveries", priority: 2 }`; :98-109 cellsFor anchor 375/1440 × light/dark; :147-159 the DEMO_EVIDENCE_RESET_TOKEN / make-demo requirement; :169 cell name format `${surface.id}-${persona}-${vw}-${theme}`) — RE-RUN ONLY, no code edit
    - .planning/phases/120-deliveries-surface-redesign/120-CONTEXT.md (D-13 floor hold; the persona re-shoot seam)
    - .planning/phases/120-deliveries-surface-redesign/120-UI-SPEC.md (Test Gate Contract — Floor section; asset pipeline guard)
    - .planning/research/v1.14/DEFECT-REGISTER.md (D-THEME-PARITY guardrail — only-move-forward)
  </read_first>
  <action>
    Hold the inherited v1.13 ratchet floor green only-forward and produce only-forward visual proof for the
    Deliveries surface. No code edits to the screenshot seam — it generates the Deliveries cells parametrically.

    (1) Verify the committed bundle is untouched (D-13): confirm `git diff --exit-code mailglass_admin/priv/static/app.css`
    passes. Because this phase adds zero new Tailwind classes, you MUST NOT commit a rebuilt bundle — a fresh
    `mix assets.build` emits raw-inline daisyUI 5.5.19 theme blocks that BREAK TokenParityTest (the committed
    bundle is canonical, MEMORY token-parity landmine). If app.css shows ANY diff, revert it
    (`git checkout mailglass_admin/priv/static/app.css`) — a diff means an accidental rebuild, not an intended
    change. NOTE: `npm run test:operator-browser` runs `mix mailglass_admin.assets.build` as its first step, so
    after ANY Playwright run in this plan, re-check + revert app.css before committing.

    (2) Run the inherited v1.13 floor gates that cover the Deliveries surface and confirm green only-forward.
    These are the canonical repo invocations (all are in the `<verify>` block — run them all):
    - TokenParityTest: `mix test test/mailglass_admin/token_parity_test.exs` (path is `test/mailglass_admin/`,
      NOT `test/mailglass_admin/components/` — there is no `components/` test dir in this repo).
    - Support-contract suite: `mix verify.support_contract.admin` (the mix alias in mailglass_admin/mix.exs —
      runs operator_live_test.exs + token_parity + ratchet_baseline + router/auth + stability/trust-doc with
      `--warnings-as-errors`).
    - Aesthetic + axe ratchet comparators: `mix test test/mailglass_admin/ratchet_baseline_test.exs
      test/mailglass_admin/axe_baseline_test.exs` (the ExUnit comparators that hold the score + WCAG baselines).
    - Conformance gates: `bash scripts/check-conformance.sh` and `bash scripts/check-conformance-advisory.sh`
      (the design-system grep gates over mailglass_admin/lib/*.ex).
    Do NOT re-score any pillar and do NOT regenerate any baseline (Phase 123 owns the re-score; the axe baseline
    producer `PERSIST_AXE_BASELINE=1 npm run test:operator-browser -- axe-baseline.spec.js` is NOT run here).
    The goal is meet-or-beat / zero regression against the existing committed baselines, not a new baseline.

    (3) Matrix verification (success criterion #2 — make it an enumerated checklist, not an assumption). Route
    every state through the existing `data_state` / `@detail_error` capability (D-09) — verify, do not rebuild.
    For EACH cell below, record in the SUMMARY whether it is covered by (a) an automated assertion (the Task-1
    operator.spec.js judgment gate, the Plan-01 ExUnit suite, or the floor gates above), (b) the persona
    re-shoot in step (4), or (c) an explicit documented human-judgment follow-up. No cell may be silently
    assumed covered.
    States (8): happy/populated · genuine no-data · no-match · loading · error · permission-denied · boundary
    (long UUID/module-name/non-ASCII/high-count/null→"Pending") · disconnected-reconnect.
    Named viewports (5): 320 · 390 · 768 · 1024 · wide (1440+).
    Themes (3): light · dark · system.
    Coverage mapping to record: the genuine-no-data / no-match / populated single-calm-pane-vs-toolbar+grid
    contract is locked by the Task-1 Playwright judgment gate + the Plan-01 ExUnit suite (automated);
    error/permission-denied/stale/boundary/responsive are pre-built+tested in `deliveries_list.ex` and covered
    by the inherited floor gates; the persona re-shoot in step (4) carries the 320/390-vs-1440 × light/dark
    visual proof for the three persona tenants. Any cell NOT landing in one of those buckets (e.g. system-theme
    at a specific viewport, or the disconnected-reconnect visual) MUST be recorded as a named human-judgment
    follow-up in the SUMMARY — do not claim #2 satisfied without the per-cell ledger.

    (4) Re-shoot the Deliveries persona cells for only-forward proof: with `make demo` up and
    `DEMO_EVIDENCE_RESET_TOKEN` set, re-run the persona screenshot seam scoped to the Deliveries surface so it
    regenerates `deliveries-{northstar,fjordline-aps,helios-void}-{375,1440}-{light,dark}`. The screenshots are
    git-ignored evidence (not committed) — the deliverable is confirmation that the cells re-shoot cleanly
    against the redesigned surface and show the streamlined IA (single calm pane — no filters/master-detail/
    double-orientation — on the helios-void zero-data cell; no orientation strip below a populated
    northstar/fjordline cell).

    (5) Re-shoot unreachable-env escape hatch (be explicit — WARNING-3 fix): if `make demo` /
    `DEMO_EVIDENCE_RESET_TOKEN` is not reachable in the execution environment, DO NOT fabricate evidence and DO
    NOT claim D-THEME-PARITY satisfied. Instead record in the SUMMARY: (i) the exact re-shoot command and the
    full cell matrix to verify, and (ii) an explicit statement that, because the re-shoot did not execute,
    D-THEME-PARITY proof for the Deliveries surface is CARRIED AS AN OPEN FOLLOW-UP to Phase 123's cross-surface
    re-score — it is NOT assumed satisfied by Phase 120. The in-suite substitutes that DO execute (TokenParity,
    ratchet/axe comparators, the Task-1 Playwright judgment gate, conformance gates) hold the floor green
    only-forward in the interim, but the persona visual-parity proof is owned by Phase 123 if deferred here.

    D-THEME-PARITY: when the re-shoot DOES run, re-verify light/dark/system render at parity on the Deliveries
    surface (the matrix is in the seam already) — only-move-forward, never regress.
  </action>
  <verify>
    <automated>cd mailglass_admin && git diff --exit-code priv/static/app.css && echo "BUNDLE-CLEAN-OK"</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs 2>&1 | tail -15</automated>
    <automated>cd mailglass_admin && mix verify.support_contract.admin 2>&1 | tail -20</automated>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/ratchet_baseline_test.exs test/mailglass_admin/axe_baseline_test.exs 2>&1 | tail -20</automated>
    <automated>cd mailglass_admin && bash scripts/check-conformance.sh 2>&1 | tail -20</automated>
    <automated>cd mailglass_admin && bash scripts/check-conformance-advisory.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code mailglass_admin/priv/static/app.css` passes (committed bundle bit-identical; no rebuilt bundle committed; app.css reverted if any Playwright run rebuilt it).
    - `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs` passes (correct path; TokenParity landmine avoided).
    - The enumerated floor gates all pass green only-forward, run via the exact commands: `mix verify.support_contract.admin`; `mix test test/mailglass_admin/ratchet_baseline_test.exs test/mailglass_admin/axe_baseline_test.exs`; `bash scripts/check-conformance.sh`; `bash scripts/check-conformance-advisory.sh`. NO pillar re-score and NO baseline regeneration performed.
    - The SUMMARY contains a per-cell matrix ledger (8 states × 5 named viewports {320/390/768/1024/wide} × 3 themes {light/dark/system}) where each cell is marked automated / re-shoot / documented-human-follow-up — no cell silently assumed covered.
    - The Deliveries persona cells `deliveries-{northstar,fjordline-aps,helios-void}-{375,1440}-{light,dark}` are re-shot against the redesigned surface; OR, if `make demo`/`DEMO_EVIDENCE_RESET_TOKEN` is unreachable, the SUMMARY records (i) the exact re-shoot command + matrix and (ii) an explicit statement that D-THEME-PARITY proof is carried as an OPEN FOLLOW-UP to Phase 123's re-score (not assumed satisfied by Phase 120) — no fabricated evidence.
    - `reference/demo_app/assets/e2e/persona-screenshots.spec.js` has no code edits (`git diff --exit-code` on that file passes).
  </acceptance_criteria>
  <done>
    Committed bundle untouched; TokenParityTest (correct path) + the enumerated inherited floor gates green
    only-forward with no re-score; per-cell matrix ledger recorded in the SUMMARY; Deliveries persona cells
    re-shot for only-forward D-THEME-PARITY proof — or, if deferred, the proof is explicitly carried as a
    Phase 123 open follow-up rather than assumed satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (browser) → OperatorLive (URL-backed filter params) | The judgment gate must lock that the no-data state withholds the only scope-widening vector (the filters toolbar). |
| CI ratchet floor → shipped app.css bundle | An accidental `mix assets.build` would emit raw-inline daisyUI theme blocks that break TokenParityTest and could ship a regressed bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-04 | Elevation of Privilege | Deliveries no-data state (regression vector) | high | mitigate | The new judgment gate asserts `operator-filters` count 0 in genuine no-data, so any future change re-introducing the toolbar (the scope-widening vector) in no-data fails the operator browser gate. This arms the D-04 security boundary into the ratchet. |
| T-120-05 | Tampering | Shipped `priv/static/app.css` | medium | mitigate | `git diff --exit-code priv/static/app.css` + TokenParityTest verification ensure the canonical bundle is not silently rebuilt/regressed; revert on any diff (D-13 landmine). |
| T-120-06 | Information Disclosure | Persona screenshot evidence | low | accept | Screenshots are git-ignored (D-02 precedent); never committed, never inlined — no PII leaks into the repo. |
</threat_model>

<verification>
- `cd mailglass_admin && npx playwright test e2e/operator.spec.js --workers=1` passes (paired updates + new judgment gate green).
- `cd mailglass_admin && mix test test/mailglass_admin/token_parity_test.exs` passes.
- The enumerated floor gates pass green only-forward: `mix verify.support_contract.admin`; `mix test test/mailglass_admin/ratchet_baseline_test.exs test/mailglass_admin/axe_baseline_test.exs`; `bash scripts/check-conformance.sh`; `bash scripts/check-conformance-advisory.sh`.
- `git diff --exit-code mailglass_admin/priv/static/app.css` passes (bundle untouched; no rebuild).
- `git diff --exit-code reference/demo_app/assets/e2e/persona-screenshots.spec.js` passes (re-run only, no code edit).
- Inherited v1.13 floor gates covering Deliveries confirmed green only-forward; no pillar re-score.
</verification>

<success_criteria>
- operator.spec.js asserts the single-calm-pane end-state (genuine no-data → orientation present + filters + master-detail both absent; populated/no-match → orientation absent, filters present) and passes.
- New Deliveries judgment gate locks D-05/D-02/D-03 + the D-04 security boundary into the ratchet.
- D-LABEL-TRIPLING heading-ambiguity comment updated; `level: 1` query preserved.
- Committed app.css bit-identical; TokenParityTest (correct path) + the enumerated inherited floor gates (verify.support_contract.admin, ratchet/axe comparators, both conformance scripts) green only-forward; no re-score.
- Per-cell matrix ledger (8 states × {320/390/768/1024/wide} × {light/dark/system}) recorded in the SUMMARY, each cell automated / re-shoot / documented-human-follow-up.
- Deliveries persona cells re-shot for only-forward D-THEME-PARITY proof; if deferred, the proof is explicitly carried to Phase 123 (not assumed satisfied here).
</success_criteria>

<artifacts_produced>
No new public symbols (edit-heavy test/verification plan). New judgment-gate assertions / testids relied upon:
- New Playwright judgment test: Deliveries empty-pane-only gate (populated → `deliveries-orientation` count 0 + `operator-filters` visible; no-data → `deliveries-orientation` count 1 + `operator-filters` count 0 + `operator-master-detail` count 0 + `operator-empty-truly` visible; no-match → `operator-filters` visible + `deliveries-orientation` count 0).
- Converted assertion: mobile orientation-order test now asserts strip absence on populated (was strip-above-list ordering).
- Updated comment: `openOperator` heading-ambiguity note reflects empty-pane-only strip (D-08).
- Re-shot persona cells (git-ignored evidence): `deliveries-{northstar,fjordline-aps,helios-void}-{375,1440}-{light,dark}`.
</artifacts_produced>

<output>
Create `.planning/phases/120-deliveries-surface-redesign/120-02-SUMMARY.md` when done.
</output>
