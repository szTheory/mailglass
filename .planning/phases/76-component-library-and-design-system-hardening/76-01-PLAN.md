---
phase: 76-component-library-and-design-system-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/test/mailglass_admin/components_test.exs
autonomous: true
requirements:
  - DS-01

must_haves:
  truths:
    - "Components.status_badge/1 is defined in components.ex and accepts :status and :size attrs"
    - "status_class/1, status_icon/1, status_label/1 each contain exactly one literal-string clause per atom — no interpolation"
    - "All 24 atoms from the frozen taxonomy are covered: 14 outbound, 6 inbound, 4 timeline"
    - "Components.normalize_inbound_outcome/1 maps :accept→:accepted, :reject→:rejected, :bounce→:bounced, pass-through for all others including nil"
    - "test/mailglass_admin/components_test.exs exists and passes with 24 atom-groups × 3 assertions each"
    - "The existing badge/1 Preview component is untouched"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/components.ex"
      provides: "status_badge/1, status_class/1, status_icon/1, status_label/1, normalize_inbound_outcome/1"
      contains: "def status_badge"
    - path: "mailglass_admin/test/mailglass_admin/components_test.exs"
      provides: "24-atom regression test covering CSS class, hero-* icon, text label"
      min_lines: 80
  key_links:
    - from: "mailglass_admin/test/mailglass_admin/components_test.exs"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "render_component(&Components.status_badge/1, ...)"
      pattern: "render_component.*status_badge"
---

<objective>
Define the unified `Components.status_badge/1` atom in `components.ex` — the single source of truth for every delivery, inbound-outcome, and timeline-event badge in the admin dashboard — and create the 24-atom regression test that prevents silent color/icon/label divergence.

Purpose: Every subsequent plan (76-02 through 76-06) depends on this component existing. Creating the test first (before the call-site rewires) ensures that any atom mismatch is caught at unit-test time, not at visual review time. This is the Wave 0 Nyquist requirement from 76-VALIDATION.md.

Output: `components.ex` extended with `status_badge/1` + three private literal-string helpers + `normalize_inbound_outcome/1`; new `test/mailglass_admin/components_test.exs` with ≥ 72 assertions.

Anti-churn citations: GAP-01 (sev 4), GAP-02 (sev 3), GAP-03 (sev 4), GAP-04 (sev 3), GAP-05 (sev 4), GAP-06 (sev 4).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md
@.planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md
@.planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md

<interfaces>
<!-- Key contracts the executor needs. All extracted from codebase. -->

From mailglass_admin/lib/mailglass_admin/components.ex (existing house pattern):
```elixir
# Private literal-string mapping (alert_class pattern, lines 86-89):
defp alert_class(:info), do: "alert-info"
defp alert_class(:success), do: "alert-success"
defp alert_class(:warning), do: "alert-warning"
defp alert_class(:error), do: "alert-error"

# attr declaration + @doc since (badge/1 lines 91-101):
attr :variant, :atom, values: [:warning, :stub], required: true
@doc since: "0.1.0"
def badge(%{variant: :warning} = assigns) do
  ~H"""
  <span class="badge badge-warning badge-sm gap-1">
    <.icon name="hero-exclamation-triangle" class="w-3 h-3" /> Error
  </span>
  """
end

# Icon rendering (lines 44-48):
def icon(assigns) do
  ~H"""
  <span class={[@name, @class]} aria-hidden="true"></span>
  """
end
```

From test/mailglass_admin/inbound/components_test.exs (house test pattern):
```elixir
defmodule MailglassAdmin.InboundComponentsTest do
  use ExUnit.Case, async: true
  import Phoenix.LiveViewTest
  alias MailglassAdmin.Inbound.RecordsList
  # ...
  html = render_component(&RecordsList.records_list/1, records: [], selected_record: nil)
  assert html =~ "No inbound records"
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status_badge/1 component and helpers to components.ex</name>
  <files>mailglass_admin/lib/mailglass_admin/components.ex</files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/components.ex (full file — house pattern for badge/1 at lines 86-114, icon/1 at lines 44-48, mask_recipient/1 @doc since: "1.5.0" at line 127)
    - .planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md (Gap 1 complete per-atom Heroicon mapping table — all 24 atoms; Gap 2 daisyUI badge class verification)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md (Canonical Status-Badge Taxonomy Table — Outbound, Inbound, Timeline sub-tables; Base Class Rule; Conflict Resolutions 1-5)
    - .planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md (components.ex pattern section — exact HEEx template shape, JIT discipline examples)
    - .planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md (D-03 sibling rule, D-04 icon+label, D-05 literal-string-only discipline)
  </read_first>
  <behavior>
    - status_badge/1 renders `<span class={["badge", size_class(@size), status_class(@status)]}><span class={[status_icon(@status), "w-3 h-3"]} aria-hidden="true"></span>{status_label(@status)}</span>`
    - status_class(:dispatched) returns "badge-primary"
    - status_class(:delivered) returns "badge-success"
    - status_class(:bounced) returns "badge-error"
    - status_class(:unknown) returns "badge-outline"
    - status_icon(:dispatched) returns "hero-paper-airplane"
    - status_icon(:queued) returns "hero-arrow-path"
    - status_label(:dispatched) returns "Dispatched"
    - normalize_inbound_outcome(:accept) returns :accepted
    - normalize_inbound_outcome(nil) returns nil (pass-through)
    - normalize_inbound_outcome(:no_match) returns :no_match (pass-through)
  </behavior>
  <action>
    Insert the following directly after the existing `badge/1` component block (line 114) and before `mask_recipient/1`. The insertion point is after `def badge(%{variant: :stub} = assigns)` ends. Do NOT modify or move the existing `badge/1` component (per D-03).

    Add (in this order):

    1. `normalize_inbound_outcome/1` — a public function (not defp) so call sites in other modules can import it. Map: `:accept` → `:accepted`, `:reject` → `:rejected`, `:bounce` → `:bounced`, any other atom (including nil) → pass-through unchanged. Include a `@doc since: "1.5.0"` and a short docstring: "Normalizes inbound @outcomes singular atoms to the canonical past-tense atoms expected by status_badge/1. The mailglass_inbound @outcomes schema (locked 1.0 contract) is never modified; normalization is admin-side only."

    2. `attr :status, :atom` with `values:` listing all 22 non-fallback atoms (outbound 14: :dispatched, :queued, :sent, :delivered, :deferred, :bounced, :failed, :rejected, :complained, :unsubscribed, :opened, :clicked, :autoresponded, :unknown; inbound 6: :accepted, :no_match, :rejected, :bounced, :ignore, :failed_ingest; timeline 4: :webhook_replay_requested, :webhook_replay_succeeded, :webhook_replay_failed, :reconciled), `required: true`.

    3. `attr :size, :atom, values: [:sm, :md], default: :sm`.

    4. `@doc since: "1.5.0"` with docstring: "Unified delivery, inbound, and timeline status badge. Renders an outline Heroicon (decorative, aria-hidden) and a text label inside a daisyUI badge container. The base badge class is always emitted by this component — call sites must NOT prepend 'badge' or 'badge badge-sm'. Use size: :sm (default) for list rows; size: :md for detail headers."

    5. `def status_badge(assigns)` rendering exactly: `<span class={["badge", size_class(@size), status_class(@status)]}><span class={[status_icon(@status), "w-3 h-3"]} aria-hidden="true"></span>{status_label(@status)}</span>`

    6. `defp size_class/1` with two literal clauses: `:sm` → `"badge-sm"`, `:md` → `"badge-md"`.

    7. `defp status_class/1` with one clause per atom returning exactly the literal strings from RESEARCH.md Gap 1 table: :dispatched/"badge-primary", :queued/"badge-primary", :sent/"badge-primary", :delivered/"badge-success", :deferred/"badge-warning", :bounced/"badge-error", :failed/"badge-error", :rejected/"badge-error", :complained/"badge-error", :unsubscribed/"badge-warning", :opened/"badge-success", :clicked/"badge-success", :autoresponded/"badge-outline", :unknown/"badge-outline", :accepted/"badge-success", :no_match/"badge-warning", :ignore/"badge-outline", :failed_ingest/"badge-error", :webhook_replay_requested/"badge-outline", :webhook_replay_succeeded/"badge-success", :webhook_replay_failed/"badge-error", :reconciled/"badge-warning". ZERO interpolation. One clause per atom.

    8. `defp status_icon/1` with one clause per atom returning exactly the literal hero-* strings from RESEARCH.md Gap 1 table: :dispatched/"hero-paper-airplane", :queued/"hero-arrow-path", :sent/"hero-paper-airplane", :delivered/"hero-check-circle", :deferred/"hero-exclamation-triangle", :bounced/"hero-x-circle", :failed/"hero-x-circle", :rejected/"hero-x-circle", :complained/"hero-exclamation-circle", :unsubscribed/"hero-bell-slash", :opened/"hero-envelope-open", :clicked/"hero-hand-thumb-up", :autoresponded/"hero-arrow-uturn-left", :unknown/"hero-question-mark-circle", :accepted/"hero-check-circle", :no_match/"hero-exclamation-triangle", :ignore/"hero-minus-circle", :failed_ingest/"hero-exclamation-circle", :webhook_replay_requested/"hero-arrow-path", :webhook_replay_succeeded/"hero-check-circle", :webhook_replay_failed/"hero-x-circle", :reconciled/"hero-exclamation-triangle". ZERO interpolation. One clause per atom.

    9. `defp status_label/1` with one clause per atom: :dispatched/"Dispatched", :queued/"Queued", :sent/"Sent", :delivered/"Delivered", :deferred/"Deferred", :bounced/"Bounced", :failed/"Failed", :rejected/"Rejected", :complained/"Complained", :unsubscribed/"Unsubscribed", :opened/"Opened", :clicked/"Clicked", :autoresponded/"Autoresponded", :unknown/"Unknown", :accepted/"Accepted", :no_match/"No match", :ignore/"Ignored", :failed_ingest/"Ingest failed", :webhook_replay_requested/"Replay requested", :webhook_replay_succeeded/"Replay succeeded", :webhook_replay_failed/"Replay failed", :reconciled/"Reconciled". ZERO interpolation. One clause per atom.

    JIT discipline (D-05, Pitfall 1): Every return value of every clause must be a complete literal string. The Tailwind scanner finds these literal strings in components.ex and emits them in the bundle. An interpolated form like `"badge-#{atom}"` would be tree-shaken to nothing.

    Note: the `badge/1` stub variant at line 112 contains `text-xs` — do NOT touch it in this plan (token migration is Wave 4/Plan 04).

    Anti-churn citations: GAP-01 (sev 4), GAP-02 (sev 3), GAP-03 (sev 4), GAP-04 (sev 3), GAP-05 (sev 4), GAP-06 (sev 4) — these gaps are the justification for this component.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -c 'def status_badge' mailglass_admin/lib/mailglass_admin/components.ex` returns 1
    - `grep -c 'defp status_class' mailglass_admin/lib/mailglass_admin/components.ex` returns 1 (the function head with multiple clauses counts as one defp name)
    - `grep -c 'defp status_icon' mailglass_admin/lib/mailglass_admin/components.ex` returns 1
    - `grep -c 'defp status_label' mailglass_admin/lib/mailglass_admin/components.ex` returns 1
    - `grep -c 'def normalize_inbound_outcome' mailglass_admin/lib/mailglass_admin/components.ex` returns 1
    - `grep -c 'def badge' mailglass_admin/lib/mailglass_admin/components.ex` returns 2 (existing two badge clauses — unchanged)
    - `mix compile --warnings-as-errors` exits 0 in mailglass_admin/
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 24-atom regression test file</name>
  <files>mailglass_admin/test/mailglass_admin/components_test.exs</files>
  <read_first>
    - mailglass_admin/test/mailglass_admin/inbound/components_test.exs (exact house pattern: defmodule, use ExUnit.Case async: true, import Phoenix.LiveViewTest, alias, render_component/2, assert html =~)
    - mailglass_admin/test/mailglass_admin/operator/shell_test.exs (lines 1-37: describe structure, render_component pattern, refute html =~ form)
    - .planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md (Gap 1 per-atom table — exact expected CSS class, icon, and label for every atom; Gap 4 Validation Architecture — test structure rules, Pitfall 5)
    - .planning/phases/76-component-library-and-design-system-hardening/76-VALIDATION.md (per-task verification map — Wave 0 requirements)
  </read_first>
  <behavior>
    - File compiles and all tests pass with `mix test test/mailglass_admin/components_test.exs`
    - "dispatched renders badge-primary + paper-airplane icon" test: html contains "badge-primary", "hero-paper-airplane", "Dispatched"
    - "delivered renders badge-success + check-circle icon" test: html contains "badge-success", "hero-check-circle", "Delivered"
    - "unknown renders badge-outline + question-mark-circle icon" test: html contains "badge-outline", "hero-question-mark-circle", "Unknown"
    - "accepted renders badge-success + check-circle icon" test: html contains "badge-success", "hero-check-circle", "Accepted"
    - "failed_ingest renders badge-error + exclamation-circle icon" test: html contains "badge-error", "hero-exclamation-circle", "Ingest failed"
    - "webhook_replay_succeeded renders badge-success" test: html contains "badge-success" (conflict resolution 4 verification)
    - normalize_inbound_outcome(:accept) returns :accepted
    - normalize_inbound_outcome(nil) returns nil
  </behavior>
  <action>
    Create `test/mailglass_admin/components_test.exs` with the following structure. This file covers the Wave 0 Nyquist gap from 76-VALIDATION.md.

    Module header: `defmodule MailglassAdmin.ComponentsTest do` with `use ExUnit.Case, async: true`, `import Phoenix.LiveViewTest`, `alias MailglassAdmin.Components`.

    Organize into four describe blocks corresponding to the four taxonomy sub-tables in 74-UI-SPEC.md:
    1. `describe "status_badge/1 — outbound delivery statuses (14 atoms)"` — test each of: :dispatched, :queued, :sent, :delivered, :deferred, :bounced, :failed, :rejected, :complained, :unsubscribed, :opened, :clicked, :autoresponded, :unknown
    2. `describe "status_badge/1 — inbound message outcomes (6 atoms)"` — :accepted, :no_match, :rejected, :bounced, :ignore, :failed_ingest
    3. `describe "status_badge/1 — timeline event markers (4 atoms)"` — :webhook_replay_requested, :webhook_replay_succeeded, :webhook_replay_failed, :reconciled
    4. `describe "normalize_inbound_outcome/1"` — test :accept→:accepted, :reject→:rejected, :bounce→:bounced, :no_match passthrough, :ignore passthrough, nil passthrough

    Per-atom test structure (Pitfall 5 prevention — one named test per atom, three separate assertions per test):
    ```
    test "dispatched renders badge-primary + paper-airplane icon" do
      html = render_component(&Components.status_badge/1, status: :dispatched, size: :sm)
      assert html =~ "badge-primary"
      assert html =~ "hero-paper-airplane"
      assert html =~ "Dispatched"
    end
    ```

    Every atom gets its own named test. No loops over atoms (Pitfall 5 — a loop that calls the same function for each atom masks a divergent atom because one failure stops the loop). Do NOT use Enum.each over atoms.

    Key correctness checks to include (reflecting conflict resolutions from 74-UI-SPEC.md):
    - :dispatched → "badge-primary" (NOT "badge-success" — Conflict 5)
    - :sent → "badge-primary" (NOT "badge-success" — Conflict 5)
    - :webhook_replay_succeeded → "badge-success" (NOT "badge-error" — Conflict 4)
    - :webhook_replay_requested → "badge-outline" (NOT "badge-error" — Conflict 4)
    - :accepted → "badge-success" (past-tense canonical form — Conflict 2)
    - No test for :suppressed (phantom atom eliminated — Conflict 1)

    Anti-churn citations: GAP-01 (sev 4), GAP-03 (sev 4), GAP-04 (sev 3), GAP-05 (sev 4), GAP-06 (sev 4) — this test directly prevents the Pitfall-5 silent-color-divergence failure mode these gaps document.

    NOTE: Do NOT include `badge-primary` anywhere in a comment or string in the test file except as part of `assert html =~` or `refute html =~` calls — the Tailwind scanner reads the test directory and could include classes found in test strings, but since `@source "../../lib"` excludes test/, this is a non-issue; literal strings in test assertions are safe.
  </action>
  <verify>
    <automated>cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --seed 0 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --seed 0` exits 0
    - Test output shows ≥ 72 tests (24 atom tests × 3 assertions = 72 assertions minimum; normalize tests add more)
    - No test uses Enum.each or a comprehension over atom lists
    - :dispatched test asserts "badge-primary" not "badge-success"
    - :webhook_replay_succeeded test asserts "badge-success" not "badge-error"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan adds a stateless HEEx function component and a test file. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01-SC | Tampering | No new packages installed in this plan | accept | No npm/pip/cargo installs; all changes are to .ex files using the existing vendored stack. No slopcheck required. |

This plan is a pure admin-dashboard CSS/component definition. No new auth, session, data-access, or input-validation surface is introduced. The `status_badge/1` component renders literal CSS class strings and text labels derived from enum atoms — no user input reaches the component shape or class output. ASVS L1 input validation, injection, and session categories do not apply.
</threat_model>

<verification>
After both tasks complete:

1. `cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3` exits 0
2. `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --seed 0` exits 0 with ≥ 24 tests
3. `grep -c 'defp status_class' mailglass_admin/lib/mailglass_admin/components.ex` returns 1
4. `grep -n 'badge-primary' mailglass_admin/lib/mailglass_admin/components.ex` shows literal string returns for :dispatched, :queued, :sent — no interpolation
5. `grep 'badge_class\|badge-ghost\|interpolat' mailglass_admin/lib/mailglass_admin/components.ex` returns empty (anti-pattern check)
</verification>

<success_criteria>
- `Components.status_badge/1` defined with icon + label rendering, 22 recognized atoms in attr :status values
- `Components.normalize_inbound_outcome/1` defined as a public function mapping singular inbound atoms to past-tense
- All three private helpers (`status_class/1`, `status_icon/1`, `status_label/1`) return only literal complete strings, one clause per atom
- Existing `badge/1` Preview component is present and unchanged (D-03)
- `test/mailglass_admin/components_test.exs` exists with ≥ 24 named tests (one per atom), each making 3 assertions (class, icon, label)
- `mix compile --warnings-as-errors` and `mix test test/mailglass_admin/components_test.exs` both exit 0
</success_criteria>

<output>
Create `.planning/phases/76-component-library-and-design-system-hardening/76-01-SUMMARY.md` when done.
</output>
