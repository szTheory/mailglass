---
phase: 76-component-library-and-design-system-hardening
plan: 05
type: execute
wave: 4
depends_on:
  - 76-01
files_modified:
  - mailglass_admin/lib/mailglass_admin/components.ex
  - mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex
  - mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex
  - mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex
  - mailglass_admin/lib/mailglass_admin/inbound/timeline.ex
  - mailglass_admin/lib/mailglass_admin/inbound_live.ex
  - mailglass_admin/lib/mailglass_admin/operator/filters_form.ex
  - mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex
  - mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex
  - mailglass_admin/lib/mailglass_admin/operator_live.ex
  - mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex
  - mailglass_admin/lib/mailglass_admin/preview/sidebar.ex
  - mailglass_admin/lib/mailglass_admin/preview/tabs.ex
  - mailglass_admin/lib/mailglass_admin/preview_live.ex
autonomous: true
requirements:
  - DS-02

must_haves:
  truths:
    - "All 15 listed files have zero raw text-sm, text-base, text-xs in HEEx classes after migration"
    - "All 15 files have zero raw gap-3, gap-4, gap-6"
    - "operator_live.ex:279-362 (Phase 75 Overview/orientation markup) is NOT touched — only lines 363+ are in scope (D-09)"
    - "preview/tabs.ex:113 inline style background #ffffff is replaced with var(--color-base-100)"
    - "hover:text-base-content is never modified in any file"
    - "tracking-[0.08em] is never modified in any file"
    - "mix compile and mix test --seed 0 both exit 0"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/preview/tabs.ex"
      provides: "Hex color eliminated — background: var(--color-base-100)"
    - path: "mailglass_admin/lib/mailglass_admin/operator_live.ex"
      provides: "Lines 363+ token-clean; lines 279-362 untouched"
  key_links:
    - from: "fifteen migrated files"
      to: "mailglass_admin/assets/css/app.css"
      via: "text-body/text-label/gap-sm/gap-md/gap-lg token definitions"
      pattern: "text-body|text-label|gap-sm|gap-md|gap-lg"
---

<objective>
Migrate all remaining admin HEEx files (not touched in Plans 76-02 through 76-04) from raw Tailwind type and spacing utilities to the design-system token scale. Also fixes the single hex color violation in `preview/tabs.ex:113` (GAP background: #ffffff → var(--color-base-100)).

This plan runs in parallel with Plan 76-04 (Wave 4) because there is zero file overlap between these two plans.

Scope: 15 files not touched by Plans 76-02/76-03/76-04. Applies same mapping: `text-sm`/`text-base` → `text-body`; `text-xs` → `text-label`; `gap-3` → `gap-sm`; `gap-4` → `gap-md`; `gap-6` → `gap-lg`. Same footgun rules.

EXCLUSION (D-09): `operator_live.ex:279-362` — Phase 75's Overview/orientation markup is already token-clean. Only lines 363+ (the pre-existing Deliveries `:else` branch) are in scope.

Anti-churn citations: GAP-08 (sev 3), GAP-16 (sev 3), GAP-17 (sev 3).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md
@.planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md
@.planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md

<interfaces>
<!-- Token mapping and footgun rules — same as Plan 76-04 -->

Token mapping (D-08):
  text-sm  → text-body
  text-base → text-body  (word-boundary: \btext-base\b to avoid hover:text-base-content)
  text-xs  → text-label
  gap-3    → gap-sm
  gap-4    → gap-md
  gap-6    → gap-lg

Critical exclusions:
  operator_live.ex lines 279-362 — DO NOT TOUCH (Phase 75 Overview/orientation, already token-clean, D-09)
  tracking-[0.08em] — DO NOT TOUCH
  hover:text-base-content — DO NOT TOUCH
  gap-1, gap-2 — DO NOT TOUCH
  font-mono — DO NOT TOUCH (GAP-18, sev-2, not a gate item for this phase)

preview/tabs.ex:113 — inline style: replace `background: #ffffff` with `background: var(--color-base-100)`
  This is NOT a Tailwind class replacement; it is a CSS property value in an inline style= attribute.
  Context: `style={"width: #{@device_width}px; height: 600px; border: 1px solid var(--color-base-300); border-radius: var(--radius-box); background: #ffffff;"}` 
  After: `background: var(--color-base-100)` (Paper on light, inverts to Ink on dark — correct device-frame base)

dt/dd label pattern — Footgun 1 (applies in evidence_card, routing_trace, replay_modal, etc.):
  `text-xs font-bold uppercase tracking-[0.08em]` → replace text-xs → text-label, leave rest

components.ex — the badge/1 stub variant at line 112 has `text-xs`:
  `<span class="text-secondary text-xs">—</span>`
  Migrate: `text-xs` → `text-label`

Already-tokenized reference: mailglass_admin/lib/mailglass_admin/operator/shell.ex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token migration — components.ex, inbound sub-components (evidence_card, filters_form, replay_modal, routing_trace, timeline), inbound_live.ex</name>
  <files>
    mailglass_admin/lib/mailglass_admin/components.ex,
    mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex,
    mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex,
    mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex,
    mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex,
    mailglass_admin/lib/mailglass_admin/inbound/timeline.ex,
    mailglass_admin/lib/mailglass_admin/inbound_live.ex
  </files>
  <read_first>
    - Each of the 7 files listed (read each before modifying; note all text-sm/text-base/text-xs/gap-3/gap-4/gap-6 occurrences using grep -n)
    - .planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md (Gap 3 footguns: Footgun 1 dt labels, Footgun 2 font-mono text-xs in Preview, Footgun 3 sm:grid-cols-2, Footgun 4 gap-2/gap-1, Footgun 5 label-text text-sm, Footgun 6 hover:text-base-content)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (lines 125, 183, 207, 248 — already-tokenized reference)
  </read_first>
  <action>
    For each file, apply the same protocol:
    1. Run `grep -nE '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' {file}` to enumerate occurrences.
    2. Apply migrations using the token mapping. Honor ALL footgun rules.
    3. After each file, run the grep gate again to confirm zero results.

    File-specific notes:

    components.ex — One known occurrence: badge/1 stub variant at line 112 has `class="text-secondary text-xs"`. Replace `text-xs` → `text-label`. This is the ONLY change to components.ex in this plan — the new status_badge/1 block added in Plan 76-01 already uses token-clean classes.

    inbound/evidence_card.ex — Check for `sm:grid-cols-2` container with `text-sm` (Footgun 3: safe to replace `text-sm` → `text-body` even on same element as responsive layout class). Also check for dt label patterns.

    inbound/filters_form.ex — Check for `gap-3`/`gap-4` in filter form layout; `text-sm`/`text-xs` on labels and inputs.

    inbound/replay_modal.ex — Modal content: check `text-sm`/`text-xs` in modal body text; `gap-3`/`gap-4` in modal layout.

    inbound/routing_trace.ex — Likely dt label patterns with `text-xs font-bold uppercase tracking-[0.08em]` (Footgun 1 — replace text-xs only).

    inbound/timeline.ex — Check for text-xs/text-sm on timeline labels and timestamps.

    inbound_live.ex — Check text-sm/text-xs on the live view template; gap-3/gap-4/gap-6 on layout divs.

    Anti-churn citations: GAP-08 (sev 3), GAP-16 (sev 3), GAP-17 (sev 3).
  </action>
  <verify>
    <automated>
      grep -E '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' \
        mailglass_admin/lib/mailglass_admin/components.ex \
        mailglass_admin/lib/mailglass_admin/inbound/evidence_card.ex \
        mailglass_admin/lib/mailglass_admin/inbound/filters_form.ex \
        mailglass_admin/lib/mailglass_admin/inbound/replay_modal.ex \
        mailglass_admin/lib/mailglass_admin/inbound/routing_trace.ex \
        mailglass_admin/lib/mailglass_admin/inbound/timeline.ex \
        mailglass_admin/lib/mailglass_admin/inbound_live.ex || echo "CLEAN"
      cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep returns zero for all 7 files (no raw text-sm/text-base/text-xs/gap-3/gap-4/gap-6)
    - `grep -c 'text-label' mailglass_admin/lib/mailglass_admin/components.ex` increased by 1 (the badge/1 stub line)
    - `mix compile --warnings-as-errors` exits 0 in mailglass_admin/
  </acceptance_criteria>
  <done>
    7 inbound/core files token-clean. components.ex badge/1 stub updated. No footgun violations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Token migration — operator sub-components, operator_live.ex (lines 363+), preview files, preview_live.ex, and hex fix in tabs.ex</name>
  <files>
    mailglass_admin/lib/mailglass_admin/operator/filters_form.ex,
    mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex,
    mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex,
    mailglass_admin/lib/mailglass_admin/operator_live.ex,
    mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex,
    mailglass_admin/lib/mailglass_admin/preview/sidebar.ex,
    mailglass_admin/lib/mailglass_admin/preview/tabs.ex,
    mailglass_admin/lib/mailglass_admin/preview_live.ex
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator_live.ex (lines 279-400 — understand the boundary: lines 279-362 are Phase 75 Overview/orientation markup already token-clean; lines 363+ are the pre-existing Deliveries :else branch IN scope; grep only from line 363)
    - mailglass_admin/lib/mailglass_admin/preview/tabs.ex (full file — line 113: the `background: #ffffff` in inline style)
    - mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex (Footgun 5: `label-text text-sm font-normal` — safe to replace text-sm → text-body; keep font-normal)
    - Each of the other 5 files (brief read to understand structure; then grep for violations)
    - .planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md (D-09 explicit exclusion of operator_live.ex:279-362)
    - .planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md (Gap 3 Footgun 2: font-mono text-xs in Preview — replace text-xs → text-label, leave font-mono; Footgun 5: label-text text-sm; Footfall 7: the #ffffff hex)
  </read_first>
  <action>
    For operator_live.ex — CRITICAL boundary enforcement (D-09):
    Lines 279-362 are the Phase 75 Overview/orientation markup and are ALREADY token-clean — DO NOT TOUCH them. Re-tokenizing those lines would be uncited churn (D-09).
    Lines 363+ are the pre-existing Deliveries `:else` branch — these are IN scope. Apply the token migration only to lines 363 and below.
    Protocol: Run `grep -nE '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' operator_live.ex` first to get a list of line numbers. Only act on line numbers ≥ 363.

    For preview/tabs.ex — hex fix (D-08):
    Locate line 113: `style={"width: #{@device_width}px; height: 600px; border: 1px solid var(--color-base-300); border-radius: var(--radius-box); background: #ffffff;"}`
    Replace `background: #ffffff` with `background: var(--color-base-100)`. This is an inline style= attribute value, NOT a Tailwind class — the grep gate for hex colors uses `grep -rE '#[0-9a-fA-F]{3,6}\b'` and will report this line. After the fix, the hex gate returns zero.
    Also apply normal token migration to any class= attributes in tabs.ex that have text-sm/text-xs/gap violations.

    For preview/assigns_form.ex — Footgun 5:
    `label-text text-sm font-normal` → `label-text text-body font-normal` (safe; font-normal is valid 400-weight; keep it)
    `font-mono text-xs` → `font-mono text-label` (Footgun 2 — font-mono stays; text-xs → text-label)

    For operator/filters_form.ex, operator/replay_modal.ex, operator/suppression_card.ex:
    Standard token migration. Check for dt label patterns (Footgun 1). Apply.

    For preview/sidebar.ex, preview_live.ex:
    Standard token migration. Check for Footgun 2 (font-mono text-xs) in any pre/code/textarea contexts.

    After all 8 files are migrated, run the full grep gate for each.

    Anti-churn citations: GAP-08 (sev 3), GAP-16 (sev 3), GAP-17 (sev 3).
  </action>
  <verify>
    <automated>
      grep -E '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' \
        mailglass_admin/lib/mailglass_admin/operator/filters_form.ex \
        mailglass_admin/lib/mailglass_admin/operator/replay_modal.ex \
        mailglass_admin/lib/mailglass_admin/operator/suppression_card.ex \
        mailglass_admin/lib/mailglass_admin/operator_live.ex \
        mailglass_admin/lib/mailglass_admin/preview/assigns_form.ex \
        mailglass_admin/lib/mailglass_admin/preview/sidebar.ex \
        mailglass_admin/lib/mailglass_admin/preview/tabs.ex \
        mailglass_admin/lib/mailglass_admin/preview_live.ex || echo "CLEAN"
      grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex" || echo "HEX-CLEAN"
      cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3 && mix test --seed 0 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep for text-(sm|base|xs)|gap-(3|4|6) returns zero for all 8 files
    - `grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex"` returns zero (hex #ffffff eliminated)
    - `grep 'background: var(--color-base-100)' mailglass_admin/lib/mailglass_admin/preview/tabs.ex` returns ≥ 1
    - operator_live.ex lines 279-362: `grep -n '\btext-(sm|base|xs)\b' operator_live.ex | awk -F: '$1 < 363'` returns zero (D-09 boundary respected — Phase 75 markup not touched)
    - `mix compile --warnings-as-errors` exits 0
    - `mix test --seed 0` exits 0 in mailglass_admin/
  </acceptance_criteria>
  <done>
    All 8 files token-clean. Hex #ffffff replaced with var(--color-base-100). operator_live.ex boundary enforced (lines 279-362 untouched). Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure CSS class name substitution and one inline style value replacement. No new data flows, auth surfaces, or input validation surfaces introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05-SC | Tampering | No new packages installed | accept | Pure HEEx token rename and one inline CSS value replacement in .ex files; no npm/pip/cargo installs. |

This plan is a pure token migration. Replacing `text-sm` with `text-body` and `#ffffff` with `var(--color-base-100)` has no behavioral impact on authentication, data access, or input validation. ASVS L1 categories do not apply.
</threat_model>

<verification>
After both tasks complete:

1. `grep -rE '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' mailglass_admin/lib/ --include="*.ex"` — MUST return zero (combined with Plan 76-04 files also being clean)
2. `grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex"` — returns zero
3. operator_live.ex: `grep -c 'orientation_strip\|overview' mailglass_admin/lib/mailglass_admin/operator_live.ex` — unchanged from before (Phase 75 markup not modified)
4. `cd mailglass_admin && mix compile --warnings-as-errors` — exits 0
5. `cd mailglass_admin && mix test --seed 0` — exits 0
</verification>

<success_criteria>
- All 15 files token-clean: zero raw text-sm/text-base/text-xs/gap-3/gap-4/gap-6
- Hex #ffffff in preview/tabs.ex:113 replaced with var(--color-base-100)
- operator_live.ex lines 279-362 untouched (D-09)
- hover:text-base-content and tracking-[0.08em] untouched in all files
- `mix compile --warnings-as-errors` and `mix test --seed 0` both exit 0 in mailglass_admin/
</success_criteria>

<output>
Create `.planning/phases/76-component-library-and-design-system-hardening/76-05-SUMMARY.md` when done.
</output>
