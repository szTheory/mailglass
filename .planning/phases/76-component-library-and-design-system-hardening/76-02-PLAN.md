---
phase: 76-component-library-and-design-system-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/operator/timeline.ex
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
autonomous: true
requirements:
  - DS-01

must_haves:
  truths:
    - "All five badge_class/1 private defps are deleted — grep returns zero"
    - "Every badge call site routes through Components.status_badge/1"
    - "Inbound records_list.ex and inbound/detail_header.ex apply normalize_inbound_outcome/1 before calling status_badge/1"
    - "The test suite remains green after rewire — no regression on any badge atom"
    - "No call site prepends 'badge' or 'badge badge-sm' to the component call (Base Class Rule)"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      provides: "Routes delivery.status through Components.status_badge/1"
      contains: "Components.status_badge"
    - path: "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
      provides: "Routes event.type through Components.status_badge/1"
      contains: "Components.status_badge"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      provides: "Routes normalized outcome through Components.status_badge/1"
      contains: "normalize_inbound_outcome"
    - path: "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
      provides: "Routes delivery.status through Components.status_badge/1"
      contains: "Components.status_badge"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex"
      provides: "Routes normalized outcome through Components.status_badge/1"
      contains: "normalize_inbound_outcome"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "Components.status_badge/1 call"
      pattern: "Components\\.status_badge"
    - from: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      to: "mailglass_admin/lib/mailglass_admin/components.ex"
      via: "normalize_inbound_outcome/1 + Components.status_badge/1"
      pattern: "normalize_inbound_outcome"
---

<objective>
Delete all five private `badge_class/1` functions and rewire every call site to `Components.status_badge/1`. This collapses the five divergent badge implementations (GAP-01..GAP-06) into the single source of truth defined in Plan 76-01.

Purpose: Eliminates the Pitfall-5 silent-color-divergence failure: list views and detail headers will now render identical badge colors, icons, and labels from the same function. Applies the five Conflict Resolutions from the frozen 74-UI-SPEC.md taxonomy.

Output: Five files modified (three named + two latent copies); `grep -rn 'defp badge_class' mailglass_admin/lib/` returns zero.

Anti-churn citations: GAP-01 (sev 4), GAP-02 (sev 3), GAP-03 (sev 4), GAP-04 (sev 3), GAP-05 (sev 4), GAP-06 (sev 4).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md
@.planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md
@.planning/phases/74-systematic-audit-and-ui-spec/74-UI-SPEC.md
@.planning/phases/76-component-library-and-design-system-hardening/76-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. Executor reads these before touching each file. -->

From 76-PATTERNS.md — deliveries_list.ex call site and deletion target:
  Current call site (lines 49-51):
    `<span class={["badge badge-sm", badge_class(delivery.status)]}>{label(delivery.status)}</span>`
  After rewire:
    `<Components.status_badge status={delivery.status} size={:sm} />`
  Deletion target (lines 80-84): five defp badge_class/1 clauses — delete all. Keep label/1 at line 86.

From 76-PATTERNS.md — timeline.ex call site and deletion target:
  Current call site (lines 52-54):
    `<span :if={event_badge(event.type)} class={badge_class(event.type)}>{event_badge(event.type)}</span>`
  After rewire (keep event_badge/1 guard, replace rendered content):
    `<Components.status_badge :if={event_badge(event.type)} status={event.type} size={:sm} />`
  Deletion target (lines 130-135): three defp badge_class/1 clauses — delete all.

From 76-PATTERNS.md — records_list.ex call site and deletion target:
  Current call site (lines 55-57):
    `<span class={["badge badge-sm", badge_class(record_outcome(record))]}>{outcome_label(record_outcome(record))}</span>`
  After rewire (with normalize_inbound_outcome/1 adapter):
    `<Components.status_badge status={Components.normalize_inbound_outcome(record_outcome(record))} size={:sm} />`
  Deletion target (lines 97-101): five defp badge_class/1 clauses — delete all. Leave record_outcome/1 and outcome_label/1.

From 76-PATTERNS.md — operator/detail_header.ex call site and deletion target:
  Current call site (lines 21-23):
    `<span class={["badge", badge_class(@delivery.status)]}>{label(@delivery.status)}</span>`
  After rewire (default :sm, no explicit size needed):
    `<Components.status_badge status={@delivery.status} />`
  Deletion target (lines 81-85): five defp badge_class/1 clauses (verbatim duplicate of deliveries_list) — delete all.

From 76-PATTERNS.md — inbound/detail_header.ex call site and deletion target:
  Current call site (lines 40-42):
    `<span class={["badge", badge_class(@outcome)]}>{outcome_label(@outcome)}</span>`
  After rewire:
    `<Components.status_badge status={Components.normalize_inbound_outcome(@outcome)} />`
  Deletion target (lines 142-146): five defp badge_class/1 clauses (verbatim duplicate of records_list) — delete all. Leave outcome_label/1 at lines 148-155.

Components.normalize_inbound_outcome/1 is a PUBLIC function defined in components.ex (76-01-SUMMARY will confirm). Call it as Components.normalize_inbound_outcome/1 from both inbound files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire operator/deliveries_list.ex and operator/timeline.ex</name>
  <files>
    mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex,
    mailglass_admin/lib/mailglass_admin/operator/timeline.ex
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (full file — call site at lines 49-51; badge_class/1 deletion target at lines 80-84; label/1 at line 86 — keep it; any import/alias block at top)
    - mailglass_admin/lib/mailglass_admin/operator/timeline.ex (full file — call site at lines 52-54; event_badge/1 guard at line 52 — KEEP unchanged; badge_class/1 deletion target at lines 130-135)
    - .planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md (exact current call site and deletion target excerpts for both files)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md (GAP-01 through GAP-03: exact lines cited for each copy)
  </read_first>
  <action>
    For deliveries_list.ex:
    1. Confirm there is an `alias MailglassAdmin.Components` or `import MailglassAdmin.Components` at the top of the module. If neither exists, add `alias MailglassAdmin.Components` to the existing use/alias block.
    2. Replace the badge call site (lines 49-51) with `<Components.status_badge status={delivery.status} size={:sm} />`. The replacement is a self-closing component — remove the surrounding `<span>` and the `{label(delivery.status)}` content entirely (the label is now rendered by status_badge/1 internally). The `label/1` function at line 86 itself is NOT deleted — verify it is still used for `delivery.last_event_type` at line 59 or elsewhere in the file before deciding to keep or remove it.
    3. Delete the ENTIRE `badge_class/1` private function block at lines 80-84 (GAP-01/GAP-02). All five clauses: the guard clause for [:delivered, :sent, :dispatched], the :deferred clause, the guard clause for [:failed, :bounced, :complained], the phantom :suppressed clause, and the fallback _status clause. Confirm no other code in the file calls badge_class/1 after the call site is replaced.

    For timeline.ex:
    1. Confirm alias/import block at top. Add `alias MailglassAdmin.Components` if not present.
    2. Replace the badge call site (lines 52-54). The `:if={event_badge(event.type)}` guard STAYS — it controls whether any badge renders at all. Replace the full `<span :if=... class=...>...</span>` block with `<Components.status_badge :if={event_badge(event.type)} status={event.type} size={:sm} />`. The `event_badge(event.type)` guard returns a truthy value or nil — this is preserved as the conditional. The event.type atom is passed directly to status_badge/1 (the timeline atoms are :webhook_replay_requested, :webhook_replay_succeeded, :webhook_replay_failed, :reconciled — all are in the taxonomy).
    3. Delete the ENTIRE `badge_class/1` block at lines 130-135 (GAP-03). All three clauses: the guard clause for the three replay types mapping to "badge badge-outline badge-error" (structural divergence + semantic error), the :reconciled clause, and the fallback _type clause. Confirm no other call to badge_class/1 exists in the file.

    Anti-churn citations: GAP-01 (sev 4), GAP-02 (sev 3), GAP-03 (sev 4).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5 && ! grep -rn 'defp badge_class' lib/mailglass_admin/operator/deliveries_list.ex lib/mailglass_admin/operator/timeline.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'defp badge_class' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex` returns 0
    - `grep -c 'defp badge_class' mailglass_admin/lib/mailglass_admin/operator/timeline.ex` returns 0
    - `grep -c 'Components.status_badge' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex` returns ≥ 1
    - `grep -c 'Components.status_badge' mailglass_admin/lib/mailglass_admin/operator/timeline.ex` returns ≥ 1
    - `mix compile --warnings-as-errors` exits 0 in mailglass_admin/
  </acceptance_criteria>
  <done>
    deliveries_list.ex: badge_class/1 deleted (5 clauses), call site replaced with Components.status_badge/1, label/1 retained.
    timeline.ex: badge_class/1 deleted (3 clauses), event_badge/1 guard retained, call site replaced with Components.status_badge :if={...}.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire inbound/records_list.ex, operator/detail_header.ex, inbound/detail_header.ex</name>
  <files>
    mailglass_admin/lib/mailglass_admin/inbound/records_list.ex,
    mailglass_admin/lib/mailglass_admin/operator/detail_header.ex,
    mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (full file — call site at lines 55-57; record_outcome/1 at line 88; outcome_label/1; badge_class/1 deletion target at lines 97-101; any existing alias block)
    - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex (full file — call site at lines 21-23; label/1 helper; badge_class/1 deletion target at lines 81-85 — LATENT GAP-05)
    - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex (full file — call site at lines 40-42; @outcome assign origin at line 27; outcome_label/1 at lines 148-155; badge_class/1 deletion target at lines 142-146 — LATENT GAP-06)
    - .planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md (exact call site and deletion target excerpts for all three files; normalize_outcome pattern)
    - .planning/phases/74-systematic-audit-and-ui-spec/74-GAP-REGISTER.md (GAP-04 sev 3, GAP-05 sev 4, GAP-06 sev 4)
    - .planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md (D-02: normalize in admin-side adapter ONLY; D-05: Base Class Rule — no "badge" prepend at call site)
  </read_first>
  <action>
    For inbound/records_list.ex (GAP-04):
    1. Add `alias MailglassAdmin.Components` if not already present.
    2. Replace the badge call site (lines 55-57 — `<span class={["badge badge-sm", badge_class(record_outcome(record))]}>{outcome_label(record_outcome(record))}</span>`) with `<Components.status_badge status={Components.normalize_inbound_outcome(record_outcome(record))} size={:sm} />`. The normalization happens inside the status= expression — the inbound :accept/:reject/:bounce atoms from @outcomes are mapped to canonical :accepted/:rejected/:bounced before reaching status_badge/1. The `mailglass_inbound` @outcomes schema ([:no_match, :accept, :ignore, :reject, :bounce, :failed]) is never modified.
    3. Check whether outcome_label/1 is used anywhere else in the file (search for `outcome_label` beyond the now-removed call site). If it is only used in the badge span that was just replaced, it may be left as dead code (do not delete it — leaving dead code is less risky than accidentally removing a helper still used elsewhere). If it appears elsewhere, leave it unchanged.
    4. Delete the ENTIRE `badge_class/1` block at lines 97-101 (GAP-04). All five clauses: :accept, :no_match, the guard for [:reject, :bounce, :failed], :ignore, and the fallback.

    For operator/detail_header.ex (GAP-05 LATENT DUPLICATE):
    1. Add `alias MailglassAdmin.Components` if not already present.
    2. Replace the badge call site (lines 21-23 — `<span class={["badge", badge_class(@delivery.status)]}>{label(@delivery.status)}</span>`) with `<Components.status_badge status={@delivery.status} />`. Omit the size attribute — defaults to :sm per D-05. Do NOT prepend "badge" (Base Class Rule).
    3. Delete the ENTIRE `badge_class/1` block at lines 81-85 (GAP-05). All five clauses — this is a verbatim duplicate of deliveries_list.ex:80-84. Do NOT leave any clause behind.

    For inbound/detail_header.ex (GAP-06 LATENT DUPLICATE):
    1. Add `alias MailglassAdmin.Components` if not already present.
    2. Replace the badge call site (lines 40-42 — `<span class={["badge", badge_class(@outcome)]}>{outcome_label(@outcome)}</span>`) with `<Components.status_badge status={Components.normalize_inbound_outcome(@outcome)} />`. The @outcome assign holds a singular inbound atom from the locked mailglass_inbound @outcomes schema; normalize_inbound_outcome/1 maps it to past-tense before status_badge/1.
    3. Delete the ENTIRE `badge_class/1` block at lines 142-146 (GAP-06). All five clauses — this is a verbatim duplicate of inbound/records_list.ex:97-101.
    4. outcome_label/1 at lines 148-155 — audit whether it is called anywhere else in the file outside the now-replaced span. Keep it as-is regardless (the risk of accidental deletion outweighs the benefit of removing dead code now).

    CRITICAL: After all three files are modified, run the grep gate below. It MUST return zero. If any badge_class/1 remains, do not proceed to verification.

    Anti-churn citations: GAP-04 (sev 3), GAP-05 (sev 4), GAP-06 (sev 4).
  </action>
  <verify>
    <automated>cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -5 && ! grep -rn 'defp badge_class' lib/ --include="*.ex" && cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --seed 0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex"` returns ZERO lines (all five copies deleted)
    - `grep -c 'Components.status_badge' mailglass_admin/lib/mailglass_admin/inbound/records_list.ex` returns ≥ 1
    - `grep -c 'normalize_inbound_outcome' mailglass_admin/lib/mailglass_admin/inbound/records_list.ex` returns ≥ 1
    - `grep -c 'Components.status_badge' mailglass_admin/lib/mailglass_admin/operator/detail_header.ex` returns ≥ 1
    - `grep -c 'normalize_inbound_outcome' mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex` returns ≥ 1
    - `cd mailglass_admin && mix compile --warnings-as-errors` exits 0
    - `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --seed 0` exits 0
    - `cd mailglass_admin && mix test --seed 0` exits 0 (full suite — confirms no regression)
  </acceptance_criteria>
  <done>
    All five badge_class/1 copies deleted. All five call sites route through Components.status_badge/1. Both inbound files apply normalize_inbound_outcome/1. Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan replaces private `badge_class/1` calls with `Components.status_badge/1` calls. All data (delivery.status, event.type, record outcome) was already rendered in the same components. No new trust boundary or input surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02-SC | Tampering | No new packages installed in this plan | accept | No npm/pip/cargo installs; changes are .ex call-site rewires only. |

This plan is a pure admin-dashboard call-site refactor. The data flowing into `status_badge/1` (delivery.status, event.type, inbound outcome atoms) originates from Ecto schema fields loaded in the parent LiveView — the same data that was already rendered by the old private functions. The normalization adapter maps known enum atoms; it does not accept free-form user input. No ASVS L1 input-validation, injection, authentication, or session category is implicated.
</threat_model>

<verification>
After both tasks complete:

1. `grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex"` — MUST return zero lines
2. `grep -rn 'Components.status_badge' mailglass_admin/lib/ --include="*.ex" | wc -l` — should show ≥ 5 call sites
3. `cd mailglass_admin && mix compile --warnings-as-errors` — exits 0
4. `cd mailglass_admin && mix test test/mailglass_admin/components_test.exs --seed 0` — exits 0
5. `cd mailglass_admin && mix test --seed 0` — exits 0 (no regression)
</verification>

<success_criteria>
- `grep -rn 'defp badge_class' mailglass_admin/lib/` returns zero results
- All five affected files compile cleanly
- `normalize_inbound_outcome/1` is applied in records_list.ex and inbound/detail_header.ex before passing to status_badge/1
- `mix test --seed 0` in mailglass_admin/ exits 0
- No call site prepends "badge" or "badge badge-sm" to the component (Base Class Rule enforced)
</success_criteria>

<output>
Create `.planning/phases/76-component-library-and-design-system-hardening/76-02-SUMMARY.md` when done.
</output>
