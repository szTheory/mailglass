---
phase: 76-component-library-and-design-system-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 76-02
files_modified:
  - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex
  - mailglass_admin/lib/mailglass_admin/operator/timeline.ex
  - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex
  - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
autonomous: true
requirements:
  - DS-02

must_haves:
  truths:
    - "All five badge-rewired files have zero raw text-sm, text-base, text-xs in HEEx classes"
    - "All five files have zero raw gap-3, gap-4, gap-6 in HEEx classes"
    - "The dt/dd label pattern (text-xs font-bold uppercase tracking-[0.08em]) is migrated to text-label, leaving font-bold/uppercase/tracking-[0.08em] untouched"
    - "hover:text-base-content is NOT migrated (it is not text-base)"
    - "mix compile and mix test --seed 0 both exit 0 after migration"
  artifacts:
    - path: "mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex"
      provides: "Token-clean type and spacing utilities"
    - path: "mailglass_admin/lib/mailglass_admin/operator/timeline.ex"
      provides: "Token-clean type and spacing utilities"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/records_list.ex"
      provides: "Token-clean type and spacing utilities"
    - path: "mailglass_admin/lib/mailglass_admin/operator/detail_header.ex"
      provides: "Token-clean type and spacing utilities"
    - path: "mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex"
      provides: "Token-clean type and spacing utilities"
  key_links:
    - from: "five token-migrated files"
      to: "mailglass_admin/assets/css/app.css"
      via: "text-body/text-label/gap-sm/gap-md/gap-lg token definitions"
      pattern: "text-body|text-label|gap-sm|gap-md|gap-lg"
---

<objective>
Migrate all raw Tailwind type and spacing utilities in the five files already modified by Plan 76-02 (badge rewire files) to the design-system token scale. These files are processed together in Wave 4 because they were touched in Wave 2 and their token migration can run in parallel with Plan 76-05's migration of the remaining files.

Scope: `text-sm` / `text-base` → `text-body`; `text-xs` → `text-label`; `gap-3` → `gap-sm`; `gap-4` → `gap-md`; `gap-6` → `gap-lg`. Apply footgun rules from RESEARCH.md Gap 3 (word-boundary grep, `hover:text-base-content` safety, `tracking-[0.08em]` exclusion).

Anti-churn citations: GAP-08 (sev 3), GAP-16 (sev 3), GAP-17 (sev 3).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md
@.planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md
@.planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md
@.planning/phases/76-component-library-and-design-system-hardening/76-02-SUMMARY.md

<interfaces>
<!-- Token mapping from 76-CONTEXT.md D-08 and RESEARCH.md Gap 3 -->

Token mapping:
  text-sm  → text-body
  text-base → text-body
  text-xs  → text-label
  gap-3    → gap-sm
  gap-4    → gap-md
  gap-6    → gap-lg

Footgun rules (from RESEARCH.md Gap 3):
  - NEVER replace `hover:text-base-content` — the substring "text-base" appears inside it but it is a different class
  - Use word-boundary grep: `\btext-base\b` NOT `text-base` (Footgun 6)
  - `tracking-[0.08em]` (43 occurrences) — do NOT touch
  - `gap-2`, `gap-1` — do NOT touch
  - For `text-xs font-bold uppercase tracking-[0.08em]` pattern (dt labels):
      replace only `text-xs` → `text-label`
      leave `font-bold`, `uppercase`, `tracking-[0.08em]` exactly as-is (Footgun 1)

From 76-PATTERNS.md — token migration examples for these 5 files:

deliveries_list.ex examples:
  line 16: gap-3 → gap-sm in `flex min-h-64 flex-col items-center justify-center gap-3 p-6`
  line 20: text-sm → text-body in `<p class="text-sm text-secondary">`
  line 44: text-sm → text-body in `<p class="truncate text-sm font-bold text-base-content">`
  lines 47, 54: text-xs → text-label in `<p class="mono mt-1 text-xs text-secondary">`

timeline.ex examples:
  line 19: gap-3 → gap-sm
  line 21: text-base → text-body
  line 25: text-sm → text-body
  lines 51, 56, 57, 59, 64: text-xs → text-label

operator/detail_header.ex examples:
  line 17: gap-4 → gap-md
  line 31: gap-3 → gap-sm AND text-sm → text-body (same class string)
  lines 33,36,40,44,49,53: text-xs → text-label (dt label pattern — leave font-bold/uppercase/tracking-[0.08em])

inbound/detail_header.ex examples:
  line 54: gap-3 → gap-sm AND text-sm → text-body
  lines 56,60,64,68,73,76: text-xs → text-label (dt label pattern — leave font-bold/uppercase/tracking-[0.08em])
  line 82: text-sm → text-body

Already-tokenized reference: mailglass_admin/lib/mailglass_admin/operator/shell.ex (lines 125, 183, 207, 248, 287, 326)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token migration — deliveries_list.ex, timeline.ex, records_list.ex</name>
  <files>
    mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex,
    mailglass_admin/lib/mailglass_admin/operator/timeline.ex,
    mailglass_admin/lib/mailglass_admin/inbound/records_list.ex
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex (read after badge rewire from 76-02; find every text-sm/text-base/text-xs/gap-3/gap-4/gap-6 occurrence)
    - mailglass_admin/lib/mailglass_admin/operator/timeline.ex (read after badge rewire from 76-02)
    - mailglass_admin/lib/mailglass_admin/inbound/records_list.ex (read after badge rewire from 76-02)
    - .planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md (exact per-file token migration examples for these three files)
    - .planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md (Gap 3 footgun inventory — Footgun 1 through Footgun 7)
    - mailglass_admin/lib/mailglass_admin/operator/shell.ex (lines 125-340 — already-tokenized reference for what token-clean markup looks like)
  </read_first>
  <action>
    For each of the three files:

    1. Run the grep gate first to enumerate ALL occurrences: `grep -nE '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' {file}`. Note each line number and surrounding context.

    2. Apply migrations one file at a time, one occurrence at a time:
       - `text-sm` → `text-body`
       - `text-base` → `text-body` (use `\btext-base\b` to avoid `text-base-content` — NEVER change `hover:text-base-content` or `text-base-content`)
       - `text-xs` → `text-label` (check for `badge-xs` false-positive — none verified in codebase; also check Footgun 1: if occurrence is `text-xs font-bold uppercase tracking-[0.08em]`, replace only `text-xs` → `text-label`, leave the rest)
       - `gap-3` → `gap-sm`
       - `gap-4` → `gap-md`
       - `gap-6` → `gap-lg`

    3. `tracking-[0.08em]` — do NOT touch any occurrence (D-08 explicit exclusion).

    4. `gap-2`, `gap-1` — do NOT touch.

    5. After migrating each file, immediately run: `grep -nE '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' {file}` and confirm zero results. If any remain, fix before moving to the next file.

    Anti-churn citations: GAP-08 (sev 3), GAP-16 (sev 3), GAP-17 (sev 3) — token drift is the documented gap for this work.
  </action>
  <verify>
    <automated>
      grep -E '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex mailglass_admin/lib/mailglass_admin/operator/timeline.ex mailglass_admin/lib/mailglass_admin/inbound/records_list.ex || echo "CLEAN"
      cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' deliveries_list.ex timeline.ex records_list.ex` returns zero results
    - `grep -v 'hover:text-base-content\|text-base-content' deliveries_list.ex | grep '\btext-base\b'` returns zero (hover prefix not accidentally stripped)
    - `grep 'tracking-\[0.08em\]' {files}` count unchanged from before migration (none removed)
    - `mix compile --warnings-as-errors` exits 0 in mailglass_admin/
  </acceptance_criteria>
  <done>
    deliveries_list.ex, timeline.ex, records_list.ex all token-clean. No hover:text-base-content modified. tracking-[0.08em] left untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Token migration — operator/detail_header.ex, inbound/detail_header.ex</name>
  <files>
    mailglass_admin/lib/mailglass_admin/operator/detail_header.ex,
    mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex
  </files>
  <read_first>
    - mailglass_admin/lib/mailglass_admin/operator/detail_header.ex (read after badge rewire from 76-02; dt label patterns at lines 33, 36, 40, 44, 49, 53; gap-3 at line 31; gap-4 at line 17)
    - mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex (read after badge rewire from 76-02; dt label patterns at lines 56, 60, 64, 68, 73, 76; gap-3 at line 54; multiple text-sm occurrences)
    - .planning/phases/76-component-library-and-design-system-hardening/76-PATTERNS.md (dt label pattern section; inbound/detail_header.ex section with exact line references)
    - .planning/phases/76-component-library-and-design-system-hardening/76-RESEARCH.md (Gap 3 Footgun 1 — dt label pattern with 38 occurrences of text-xs font-bold uppercase tracking-[0.08em])
  </read_first>
  <action>
    Apply the same token migration protocol as Task 1.

    Special attention for both detail_header files — the `dt/dd` label pattern (Footgun 1):
    These files contain many occurrences of: `<dt class="text-xs font-bold uppercase tracking-[0.08em]">Label</dt>`
    For EACH such occurrence: replace ONLY `text-xs` → `text-label`. Leave `font-bold`, `uppercase`, `tracking-[0.08em]` completely unchanged.
    The result: `<dt class="text-label font-bold uppercase tracking-[0.08em]">Label</dt>`

    For operator/detail_header.ex:
    - line 17: `gap-4` → `gap-md` (the outer flex wrapper)
    - line 31: `gap-3` → `gap-sm` AND `text-sm` → `text-body` in the same `dl` class string
    - lines 33, 36, 40, 44, 49, 53 (and any others): `text-xs` → `text-label` in dt labels (Footgun 1 — leave rest)

    For inbound/detail_header.ex:
    - line 54: `gap-3` → `gap-sm` AND `text-sm` → `text-body`
    - lines 56, 60, 64, 68, 73, 76 (and any others): `text-xs` → `text-label` in dt labels (Footgun 1)
    - line 82: `text-sm` → `text-body`
    - Any other text-sm/text-base occurrences found by grep

    After migration: run full grep gate for both files.

    Anti-churn citations: GAP-08 (sev 3), GAP-16 (sev 3), GAP-17 (sev 3).
  </action>
  <verify>
    <automated>
      grep -E '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' mailglass_admin/lib/mailglass_admin/operator/detail_header.ex mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex || echo "CLEAN"
      cd mailglass_admin && mix compile --warnings-as-errors 2>&1 | tail -3 && mix test --seed 0 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' operator/detail_header.ex inbound/detail_header.ex` returns zero
    - dt label pattern migrated: `grep -c 'text-label font-bold uppercase' operator/detail_header.ex` ≥ 3 (confirms dt labels were migrated)
    - dt label pattern migrated: `grep -c 'text-label font-bold uppercase' inbound/detail_header.ex` ≥ 3
    - `tracking-[0.08em]` count unchanged in both files (none removed)
    - `mix compile --warnings-as-errors` exits 0
    - `mix test --seed 0` exits 0 in mailglass_admin/
  </acceptance_criteria>
  <done>
    Both detail_header files token-clean. dt label pattern correctly migrated (text-xs → text-label; font-bold/uppercase/tracking-[0.08em] untouched). Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure CSS class name substitution in HEEx templates. No new data flows, no new auth surface, no new input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-SC | Tampering | No new packages installed | accept | Pure HEEx token rename in .ex files; no npm/pip/cargo installs. |

This plan replaces raw Tailwind utility class names with design-system token class names in HEEx. No behavior change — the token classes resolve to the same CSS values (text-body = text-sm value; gap-sm = gap-3 value at the defined scale). ASVS L1 categories do not apply.
</threat_model>

<verification>
After both tasks complete:

1. `grep -rE '\btext-(sm|base|xs)\b|\bgap-(3|4|6)\b' mailglass_admin/lib/mailglass_admin/operator/deliveries_list.ex mailglass_admin/lib/mailglass_admin/operator/timeline.ex mailglass_admin/lib/mailglass_admin/inbound/records_list.ex mailglass_admin/lib/mailglass_admin/operator/detail_header.ex mailglass_admin/lib/mailglass_admin/inbound/detail_header.ex` — returns zero results
2. `cd mailglass_admin && mix compile --warnings-as-errors` — exits 0
3. `cd mailglass_admin && mix test --seed 0` — exits 0
</verification>

<success_criteria>
- Five files token-clean: zero raw `text-sm`, `text-base`, `text-xs`, `gap-3`, `gap-4`, `gap-6`
- `hover:text-base-content` not modified in any file
- `tracking-[0.08em]` not modified in any file
- dt label pattern correctly migrated: `text-xs` → `text-label`, rest unchanged
- `mix compile --warnings-as-errors` and `mix test --seed 0` both exit 0 in mailglass_admin/
</success_criteria>

<output>
Create `.planning/phases/76-component-library-and-design-system-hardening/76-04-SUMMARY.md` when done.
</output>
