---
phase: 76-component-library-and-design-system-hardening
plan: 06
type: execute
wave: 5
depends_on:
  - 76-02
  - 76-03
  - 76-04
  - 76-05
files_modified:
  - mailglass_admin/priv/static/app.css
autonomous: false
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04

must_haves:
  truths:
    - "mix mailglass_admin.assets.build succeeds with no warnings"
    - "git diff --exit-code mailglass_admin/priv/static/ exits 0 (no uncommitted bundle drift)"
    - "The rebuilt app.css contains every literal badge class used by status_badge/1: badge-primary, badge-success, badge-warning, badge-error, badge-outline"
    - "The rebuilt app.css contains every new hero-* icon mask: hero-paper-airplane, hero-arrow-path, hero-check-circle, hero-exclamation-triangle, hero-x-circle, hero-exclamation-circle, hero-bell-slash, hero-envelope-open, hero-hand-thumb-up, hero-arrow-uturn-left, hero-question-mark-circle, hero-minus-circle"
    - "Token conformance grep returns zero for text-sm/text-base/text-xs/gap-3/gap-4/gap-6/font-medium/font-semibold/hex colors in all admin lib/ .ex files"
    - "grep returns zero for defp badge_class in all admin lib/ .ex files"
    - "Full test suite mix test --seed 0 exits 0"
  artifacts:
    - path: "mailglass_admin/priv/static/app.css"
      provides: "Committed rebuilt bundle with all new badge classes and hero-* icons"
      contains: "badge-primary"
  key_links:
    - from: "mailglass_admin/lib/mailglass_admin/components.ex"
      to: "mailglass_admin/priv/static/app.css"
      via: "Tailwind JIT scanner @source ../../lib — literal status_class/1 strings emitted"
      pattern: "badge-primary|hero-paper-airplane"
---

<objective>
The [BLOCKING] final gate for Phase 76: rebuild the admin asset bundle, run all conformance grep gates, and commit the clean bundle to the repo. This plan MUST succeed before Phase 76 can be considered complete.

Purpose: The JIT scanner emits only classes found literally in scanned source files at build time. Without this bundle rebuild, the new `badge-primary`, `badge-outline`, and twelve new `hero-*` icon masks are absent from `priv/static/app.css` — badges render unstyled in production and icon spans are invisible (D-11 / Pitfall 2).

Output: Rebuilt `mailglass_admin/priv/static/app.css` committed; all four DS-01..DS-04 conformance gates pass; `git diff --exit-code priv/static/` is clean.

Anti-churn citations: GAP-01 (sev 4), GAP-03 (sev 4), GAP-05 (sev 4), GAP-06 (sev 4) — bundle clean gate is the final verification that badge consolidation is production-complete.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-component-library-and-design-system-hardening/76-CONTEXT.md
@.planning/phases/76-component-library-and-design-system-hardening/76-04-SUMMARY.md
@.planning/phases/76-component-library-and-design-system-hardening/76-05-SUMMARY.md

<interfaces>
<!-- Bundle rebuild mechanics from 76-RESEARCH.md Gap 5 -->

Build command: `cd mailglass_admin && mix mailglass_admin.assets.build`
Output file: `mailglass_admin/priv/static/app.css`
Mechanism: `Mix.Task.run("tailwind", ["default", "--minify"])` → Tailwind v4.1.12 standalone binary
@source scan path: `mailglass_admin/lib/**/*.ex` (from app.css: `@source "../../lib"`)
Heroicons plugin: scans same @source paths for hero-* class strings

150KB ceiling: current bundle ~70KB; adding ~12 new hero-* icons adds ~8KB estimated → ~78KB total — well within 147KB ceiling.

BundleTest: `mailglass_admin/test/mailglass_admin/bundle_test.exs` — asserts bundle < 150_000 bytes + font set.

CI gate: `git diff --exit-code priv/static/` — must be clean before merge.

Conformance grep commands (from design-system.md:104-121 + RESEARCH.md Gap 3):
  ! grep -rE '\btext-(sm|base|xs)\b' mailglass_admin/lib/ --include="*.ex"
  ! grep -rE '\bfont-(medium|semibold)\b' mailglass_admin/lib/ --include="*.ex"
  ! grep -rE '\bgap-(3|4|6)\b' mailglass_admin/lib/ --include="*.ex"
  ! grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex"
  ! grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all conformance grep gates and fix any remaining violations</name>
  <files></files>
  <read_first>
    - .planning/phases/76-component-library-and-design-system-hardening/76-VALIDATION.md (per-task verification map — all DS-01..DS-04 structural grep requirements)
    - mailglass_admin/docs/design-system.md (lines 104-121 — the six conformance pillars and exact grep gate definitions)
    - .planning/phases/76-component-library-and-design-system-hardening/76-04-SUMMARY.md
    - .planning/phases/76-component-library-and-design-system-hardening/76-05-SUMMARY.md
  </read_first>
  <action>
    Run each conformance grep gate in sequence. For each gate that fails (returns non-zero results), fix the violating lines before proceeding to the next gate. Document any fixes needed in the SUMMARY.

    Gate 1 — Badge consolidation (DS-01):
    `grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex"`
    Expected: zero results. If any `defp badge_class` remains, fix the file immediately (delete the clause and rewire the call site per Plan 76-02 protocol).

    Gate 2 — Type token violations (DS-02):
    `grep -rE '\btext-(sm|base|xs)\b' mailglass_admin/lib/ --include="*.ex"`
    Expected: zero results. If any remain, apply the token migration (text-sm → text-body, text-base → text-body, text-xs → text-label) to the flagged lines. Honor the Footgun 1-7 rules from RESEARCH.md Gap 3.

    Gate 3 — Faux-bold violations (DS-02):
    `grep -rE '\bfont-(medium|semibold)\b' mailglass_admin/lib/ --include="*.ex"`
    Expected: zero results (CONTEXT.md D-08 confirmed these are already absent — this gate verifies no regression was introduced).

    Gate 4 — Spacing token violations (DS-02):
    `grep -rE '\bgap-(3|4|6)\b' mailglass_admin/lib/ --include="*.ex"`
    Expected: zero results. Fix any remaining occurrences.

    Gate 5 — Hex color violations (DS-02):
    `grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex"`
    Expected: zero results (the preview/tabs.ex #ffffff was fixed in Plan 76-05). If any remain, fix them.

    Gate 6 — bundle_test pre-check:
    `cd mailglass_admin && mix test test/mailglass_admin/bundle_test.exs --seed 0`
    The CURRENT bundle (before rebuild) may show drift from development. This is informational only at this stage; the rebuild in Task 2 produces the final artifact. Record current bundle size in the SUMMARY.

    All six gates must return zero / pass before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex" || echo "BADGE-GATE-PASS"
      grep -rE '\btext-(sm|base|xs)\b' mailglass_admin/lib/ --include="*.ex" || echo "TYPE-GATE-PASS"
      grep -rE '\bfont-(medium|semibold)\b' mailglass_admin/lib/ --include="*.ex" || echo "BOLD-GATE-PASS"
      grep -rE '\bgap-(3|4|6)\b' mailglass_admin/lib/ --include="*.ex" || echo "GAP-GATE-PASS"
      grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex" || echo "HEX-GATE-PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - All five grep gates return zero results (or the -PASS echo is the only output)
    - No remaining `defp badge_class` in any .ex file under mailglass_admin/lib/
  </acceptance_criteria>
  <done>
    All five conformance grep gates pass. Zero violations remain in admin lib/ .ex files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild asset bundle, verify JIT inclusion, run full test suite</name>
  <files>mailglass_admin/priv/static/app.css</files>
  <read_first>
    - mailglass_admin/lib/mix/tasks/mailglass_admin.assets.build.ex (build task invocation — `Mix.Task.run("tailwind", ["default", "--minify"])`)
    - mailglass_admin/test/mailglass_admin/bundle_test.exs (150KB ceiling assertion + font set spec — confirm the rebuild stays within budget)
    - mailglass_admin/lib/mailglass_admin/components.ex (the literal badge class and hero-* strings that must appear in the rebuilt bundle)
  </read_first>
  <action>
    1. Run the bundle rebuild: `cd mailglass_admin && mix mailglass_admin.assets.build`
       This invokes the Tailwind v4.1.12 standalone binary which scans `@source "../../lib"` (all .ex files under mailglass_admin/lib/). The literal strings from `status_class/1`, `status_icon/1`, and `size_class/1` in components.ex will be picked up and included in the output bundle.

    2. Verify JIT inclusion — confirm new classes are in the rebuilt bundle:
       `grep -c 'badge-primary' mailglass_admin/priv/static/app.css` — must return ≥ 1
       `grep -c 'hero-paper-airplane' mailglass_admin/priv/static/app.css` — must return ≥ 1
       `grep -c 'hero-check-circle' mailglass_admin/priv/static/app.css` — must return ≥ 1
       If any new badge class or hero-* icon is absent from the bundle, return to components.ex and confirm the literal string appears in a pattern-matched clause (not interpolated). Fix and rebuild.

    3. Check bundle size:
       `wc -c mailglass_admin/priv/static/app.css`
       Must be < 150,000 bytes. If it exceeds the ceiling, investigate what added size unexpectedly — the 150KB budget has ~73KB headroom per RESEARCH.md Gap 5.

    4. Run bundle_test to confirm size and font set:
       `cd mailglass_admin && mix test test/mailglass_admin/bundle_test.exs --seed 0`
       Must exit 0.

    5. Run full test suite including the 24-atom components_test:
       `cd mailglass_admin && mix test --seed 0`
       Must exit 0.

    6. Run `git diff --exit-code mailglass_admin/priv/static/` to check current drift.
       If the bundle has changed (expected — the rebuild produces a new app.css), this will show diff.
       Stage and commit `mailglass_admin/priv/static/app.css` as part of this plan's deliverable.

    COMMIT: Stage the rebuilt bundle with: `git add mailglass_admin/priv/static/app.css`
    This file MUST be committed in the same PR as the HEEx changes (D-11 / Pitfall 2 / CLAUDE.md #6).
    The commit message should include "rebuild admin bundle — badge-primary, hero-* icons, token migrations".

    After commit: run `git diff --exit-code mailglass_admin/priv/static/` again — it MUST return exit code 0 (no uncommitted drift).

    Anti-churn citations: GAP-01 (sev 4), GAP-03 (sev 4), GAP-05 (sev 4), GAP-06 (sev 4) — the bundle gate is the final production-readiness proof for badge consolidation.
  </action>
  <verify>
    <automated>
      grep -c 'badge-primary' mailglass_admin/priv/static/app.css
      grep -c 'hero-paper-airplane' mailglass_admin/priv/static/app.css
      git diff --exit-code mailglass_admin/priv/static/ && echo "BUNDLE-CLEAN"
      cd mailglass_admin && mix test --seed 0 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `cd mailglass_admin && mix mailglass_admin.assets.build` exits 0
    - `grep -c 'badge-primary' mailglass_admin/priv/static/app.css` ≥ 1
    - `grep -c 'hero-paper-airplane' mailglass_admin/priv/static/app.css` ≥ 1
    - `grep -c 'hero-check-circle' mailglass_admin/priv/static/app.css` ≥ 1
    - `grep -c 'hero-question-mark-circle' mailglass_admin/priv/static/app.css` ≥ 1
    - `wc -c mailglass_admin/priv/static/app.css | awk '{print $1}'` < 150000
    - `cd mailglass_admin && mix test test/mailglass_admin/bundle_test.exs --seed 0` exits 0
    - `cd mailglass_admin && mix test --seed 0` exits 0
    - `git diff --exit-code mailglass_admin/priv/static/` exits 0 (bundle committed)
  </acceptance_criteria>
  <done>
    Bundle rebuilt. All new badge and icon classes present in app.css. Bundle < 150KB. Full test suite green. git diff --exit-code priv/static/ clean. Phase 76 all gates passed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 76-01 through 76-06 have implemented the full Phase 76 scope:
    - DS-01: One unified `Components.status_badge/1` atom with icon + label; all five `badge_class/1` copies deleted; 24-atom regression test passing
    - DS-02: Admin-wide token migration — zero raw text-sm/text-base/text-xs/gap-3/gap-4/gap-6/font-medium/semibold/hex colors in admin lib/
    - DS-03: `support_cards.ex` restructured into Tier1/Tier2 hierarchy with semantic count colors
    - DS-04: Bundle rebuilt, `git diff --exit-code priv/static/` clean

    All conformance grep gates return zero. Full test suite passes.
  </what-built>
  <how-to-verify>
    1. Run the full conformance check: `grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex"` — must return zero
    2. Run the type token check: `grep -rE '\btext-(sm|base|xs)\b' mailglass_admin/lib/ --include="*.ex"` — must return zero
    3. Run the spacing token check: `grep -rE '\bgap-(3|4|6)\b' mailglass_admin/lib/ --include="*.ex"` — must return zero
    4. Run the hex check: `grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex"` — must return zero
    5. Run `git diff --exit-code mailglass_admin/priv/static/` — must exit 0
    6. Run `cd mailglass_admin && mix test --seed 0` — must exit 0
    7. Optional: start the app locally and visit the Deliveries and Inbound surfaces to confirm badges render with correct icons and colors (not the 390px overflow re-check — that is Phase 79)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 76 complete, or describe any failures found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan runs conformance greps, rebuilds a CSS bundle, and commits the artifact. No new auth, session, data-access, or input-validation surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-06-SC | Tampering | mix mailglass_admin.assets.build (Tailwind v4.1.12 standalone binary) | accept | The standalone binary is already pinned in mix.exs and in use. This plan invokes it identically to how CI invokes it. No new npm/pip/cargo packages are installed. |

The bundle rebuild invokes the already-pinned Tailwind v4.1.12 standalone binary from the `mix.lock`-committed position. The heroicons plugin is the already-vendored `mailglass_admin/assets/vendor/heroicons.js`. No supply-chain exposure beyond what was already accepted when these dependencies were first introduced.
</threat_model>

<verification>
Phase 76 final gate — all must pass before Phase 79 milestone closeout considers Phase 76 complete:

1. `grep -rn 'defp badge_class' mailglass_admin/lib/ --include="*.ex"` → zero results (DS-01)
2. `grep -rE '\btext-(sm|base|xs)\b' mailglass_admin/lib/ --include="*.ex"` → zero results (DS-02)
3. `grep -rE '\bfont-(medium|semibold)\b' mailglass_admin/lib/ --include="*.ex"` → zero results (DS-02)
4. `grep -rE '\bgap-(3|4|6)\b' mailglass_admin/lib/ --include="*.ex"` → zero results (DS-02)
5. `grep -rE '#[0-9a-fA-F]{3,6}\b' mailglass_admin/lib/ --include="*.ex"` → zero results (DS-02)
6. `grep -c 'badge-primary\|badge-success\|badge-error\|badge-warning\|badge-outline' mailglass_admin/priv/static/app.css` → ≥ 1 per class (DS-04 JIT verification)
7. `grep -c 'hero-paper-airplane\|hero-check-circle\|hero-x-circle' mailglass_admin/priv/static/app.css` → ≥ 1 each (DS-01/DS-04 icon inclusion)
8. `git diff --exit-code mailglass_admin/priv/static/` → exits 0 (DS-04 bundle-clean gate)
9. `cd mailglass_admin && mix test --seed 0` → exits 0 (regression-free)
</verification>

<success_criteria>
- All five conformance grep gates return zero (DS-01: badge_class absent; DS-02: type/bold/gap/hex tokens clean)
- Rebuilt `mailglass_admin/priv/static/app.css` contains all new badge-primary and hero-* icon class masks
- Bundle size < 150KB (within budget)
- `git diff --exit-code mailglass_admin/priv/static/` exits 0 — bundle committed and clean
- `mix test --seed 0` exits 0 in mailglass_admin/
- Human approval received confirming all gates pass
</success_criteria>

<output>
Create `.planning/phases/76-component-library-and-design-system-hardening/76-06-SUMMARY.md` when done.
</output>
