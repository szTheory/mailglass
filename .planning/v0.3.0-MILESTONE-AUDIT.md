---
milestone: v0.3.0
audited: 2026-04-30T16:22:05-04:00
status: tech_debt
scores:
  requirements: 11/11 runtime-satisfied
  phases: 8/8 in scope reviewed — 2/8 have formal VERIFICATION.md
  integration: 5/5 flows wired
  flows: 5/5 end-to-end paths passing, 1 with coverage caveat
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "Traceability / milestone artifacts"
    items:
      - "REQUIREMENTS.md still leaves SES-01, SES-03, SES-04, and SES-05 unchecked even though the old SES ingest blocker is closed in code and current tests pass."
      - "ROADMAP.md still marks Phase 19 blocked even though `mix test` now exits 0 locally (913 tests, 0 failures, 6 skipped on 2026-04-30)."
      - "Phases 14, 17, 18, 19, 20, and 21 still lack formal `*-VERIFICATION.md` artifacts, so milestone closure depends on SUMMARY / VALIDATION / publish evidence rather than uniform verifier reports."
  - phase: "Phase 18 release surface"
    items:
      - "CHANGELOG.md currently has duplicate `0.3.0` headings, which contradicts the Phase 18 summary's claimed single curated `0.3.0` section."
  - phase: "Phase 16/19 SES control-plane coverage"
    items:
      - "SES `SubscriptionConfirmation` is wired through the runtime control-plane branch, but regression coverage remains provider-level rather than plug-level."
  - phase: "Environment noise"
    items:
      - "Local test runs still emit intermittent `Postgrex.Error` FATAL 53300 (`too_many_connections`) warnings and repeated optional OTLP exporter warnings even when the suite finishes green."
nyquist:
  compliant_phases: ["Phase 15", "Phase 18", "Phase 19", "Phase 20"]
  partial_phases: ["Phase 16", "Phase 17"]
  missing_phases: ["Phase 14", "Phase 21"]
  overall: "PARTIAL"
---

# Milestone v0.3.0 — Audit Report

**Status:** `tech_debt`  
**Audited:** 2026-04-30 16:22:05 America/New_York  
**Scope:** Phases 14-21

## Executive Summary

The old `v0.3.0` milestone BLOCKER is closed. `Mailglass.Webhook.Ingest.ingest_multi/3` now accepts `:ses`, `derive_webhook_provider_event_id/3` has a `:ses` clause, the config schema now validates `:ses` and `:resend`, and the installer surface now mentions both providers explicitly. Current runtime evidence is green enough to treat all milestone requirements as satisfied in product behavior.

This milestone should still not be marked `passed`. The remaining debt is artifact quality and audit cadence: milestone traceability files are stale, several phases still have no formal `VERIFICATION.md`, `CHANGELOG.md` has duplicate `0.3.0` headings, and SES `SubscriptionConfirmation` still lacks plug-level regression coverage.

## Verification Evidence

- `mix test test/mailglass/webhook/` → `210 tests, 0 failures`
- `mix test test/mailglass/suppression/escalation_test.exs` → `4 tests, 0 failures`
- `mix test` → `20 properties, 913 tests, 0 failures, 6 skipped`
- `gsd-sdk query audit-uat --raw` → `summary.total_items: 0`
- Integration check: no live runtime/integration blocker remains in Resend, Mailgun, or SES Notification flows

## Requirements Coverage

| REQ-ID | Current runtime status | Artifact status | Final |
|---|---|---|---|
| RESEND-01 | Plug verification wired and tested | SUMMARY-backed, no formal phase verification file | satisfied |
| RESEND-02 | Normalize + ingest wired and tested | SUMMARY-backed, no formal phase verification file | satisfied |
| MAILGUN-01 | Verified and persisted through plug path | Formal verification present | satisfied |
| MAILGUN-02 | Replay cache + replay `200` behavior wired | Formal verification present | satisfied |
| MAILGUN-03 | Normalize + ingest wired and tested | Formal verification present | satisfied |
| SES-01 | Old ingest blocker closed; SES plug path persists events | REQUIREMENTS checkbox still stale | satisfied |
| SES-02 | D-07 override accepted; control-plane branch wired | Plug-level regression still missing | satisfied |
| SES-03 | RSA verify path now flows through ingest | REQUIREMENTS checkbox still stale | satisfied |
| SES-04 | Cert cache now reachable in full SES Notification flow | REQUIREMENTS checkbox still stale | satisfied |
| SES-05 | Normalized SES events now persist end-to-end | REQUIREMENTS checkbox still stale | satisfied |
| DELIV-04 | Release artifacts shipped and tests green | CHANGELOG duplication remains | satisfied |

## Cross-Phase Wiring

| Flow | Status | Notes |
|---|---|---|
| Resend POST → verify Svix → normalize → ingest → `mailglass_events` | PASS | Verified by Resend plug tests and milestone summaries |
| Mailgun POST → verify HMAC → replay gate → ingest | PASS | Replay fast path returns `200` as designed |
| SES Notification → verify RSA → normalize → ingest → `mailglass_events` | PASS | Old `:ses` ingest seam blocker is closed |
| SES SubscriptionConfirmation → control-plane `200` | PASS with caveat | Runtime path is wired; no plug-level regression test yet |
| Release surface → docs / installer / publish-check / Hex ship | PASS with caveat | CHANGELOG artifact drift remains |

## Phase Snapshot

| Phase | State observed in this audit | Notes |
|---|---|---|
| 14 Resend Webhook Provider & Core Ingest | Functionally complete | No `14-VERIFICATION.md`; closure is carried by Phase 17 + later release evidence |
| 15 Mailgun Webhook Provider | Complete | Strongest artifact set in milestone |
| 16 SES Webhook Provider & SNS Cache | Runtime-complete, paperwork-partial | Verification exists; Nyquist frontmatter still partial |
| 17 Unblock & Verify Resend | Functionally complete | No `17-VERIFICATION.md` |
| 18 Ship v0.3.x | Shipped | Publish evidence exists; `CHANGELOG.md` drift remains |
| 19 Fix SES Ingest BLOCKER + Plug-level Integration Test | Runtime-complete, roadmap stale | Old summary says blocked, but current full suite is green |
| 20 Config Schema & Installer Surface for SES + Resend | Complete | Old config and installer warnings are closed |
| 21 SES-02 D-07 Override + SUMMARY Frontmatter Backfill | Complete | Paperwork closure phase; no standalone validation artifact |

## Findings

1. No critical integration blocker remains. The prior `v0.3.0` audit's SES ingest failure is closed in code and covered by the current webhook suite.
2. Milestone tracking artifacts are behind the codebase. `REQUIREMENTS.md` and `ROADMAP.md` still describe pre-fix milestone state.
3. Formal verification cadence is inconsistent across the milestone. Only Phases 15 and 16 have `VERIFICATION.md` files.
4. The release requirement is behaviorally met, but `CHANGELOG.md` still contains duplicate `0.3.0` headings.
5. SES `SubscriptionConfirmation` should gain a plug-level regression test if you want uniform confidence across SES control-plane and notification paths.

## Recommended Next Steps

1. Update `.planning/REQUIREMENTS.md` to check off `SES-01`, `SES-03`, `SES-04`, and `SES-05`.
2. Update `.planning/ROADMAP.md` so Phase 19 is no longer marked blocked.
3. Backfill formal `*-VERIFICATION.md` reports for Phases 14, 17, 18, 19, 20, and 21 if you want milestone archive quality to match the workflow contract.
4. Fix the duplicate `0.3.0` headings in [CHANGELOG.md](/Users/jon/projects/mailglass/CHANGELOG.md:23).
5. Add a plug-level SES `SubscriptionConfirmation` regression test.

_Refreshed by `$gsd-audit-milestone` using live repo state, current test evidence, and integration-checker output._
