{
  "name": "mailglass-admin-browser-tests",
  "private": true,
  "scripts": {
    "test:operator-browser": "mix mailglass_admin.assets.build && playwright test --config=playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
