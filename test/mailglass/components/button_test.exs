defmodule Mailglass.Components.ButtonTest do
  use ExUnit.Case, async: true
  @moduletag :skip

  test "<.button> renders v:roundrect VML wrapper in final HTML" do
    # Implemented in Plan 05 — AUTHOR-02, D-11.
    flunk("not yet implemented")
  end
end
