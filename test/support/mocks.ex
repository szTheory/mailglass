defmodule Mailglass.Test.Mocks do
  @moduledoc """
  Mox mock declarations live here alongside the `Mox.defmock` calls in
  `test/test_helper.exs`. Each defmock is guarded by `Code.ensure_loaded?/1`
  on the underlying behaviour so tests can run before the behaviours are
  defined in later plans.
  """
end
